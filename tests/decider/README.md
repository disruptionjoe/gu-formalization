# tests/decider/

Single-decider certificates for the generation-index fork. These scripts are
computational certificates, not claim-status updates. They keep the current public
boundary explicit: `LOCATE stands; FORCE is not earned` unless a future governed
run supplies the gated source-action and integer-extraction legs without verdict
inflation.

## Shared Boundary

The decider family separates three things that are easy to conflate:

- The net-chiral selector / fiber channel reads `GAUGE` and stays 2-primary in
  the current computations.
- The tangential `Lambda^2_+` carrier is `TANGENTIAL` and order-3-bearing, but
  it is vectorlike and is not by itself a generation count.
- The integer `3` remains gated on the unbuilt twisted RS / IG source action and
  the category step from an order-3 class to a literal generation integer.

Do not use these scripts to change canon, claim status, verdicts, public posture,
paper status, or protected governance surfaces. A green run means the listed
certificate controls still reproduce their current arithmetic and fork boundary.

## Direct Certificates

| script | role | current shared outcome |
|---|---|---|
| `generation_index_fork_decider.py` | Full single-decider sweep over controls, carrier/fork arithmetic, and gated integer extraction. | Locate stands; integer extraction remains gated. |
| `fibered_boundary_reduction_decider.py` | Fibered-boundary reduction angle for the `S^6`-bundle over the `RP^3` spine. | Fiber channel lands gauge/2-primary; source-action application remains gated. |
| `bulk_inflow_generation_index.py` | Bulk anomaly-inflow / characteristic-class angle for the `-p_1/24` channel. | Bulk topology does not force three; fork for the net-counting selector reads gauge. |
| `adversarial_independent_recheck.py` | Independent adversarial recheck with separate Clifford construction and exact controls. | No fabricated three; gated RS leg remains walled off. |

## Process Gate

`process_gates/decider_readme_inventory_audit.py` keeps this README synchronized
with the live direct scripts in this directory and checks that the not-forced /
gated boundary remains visible.
