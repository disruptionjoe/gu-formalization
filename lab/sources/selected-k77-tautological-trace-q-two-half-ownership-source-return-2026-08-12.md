---
artifact_type: source_return
created: 2026-08-12
status: SOURCE_CONFIRMS_TRACE_REVERSAL_AND_DISTINGUISHED_METRIC_FIBRE_DIRECTION__SOURCE_SILENT_ON_HQ_ACTION_PARENT_AND_PHYSICAL_BLOCK
---

# Source return: trace direction versus Hermitian parent

## Question

Do the checked Weinstein/Curt materials supply the non-null direction used by
`H_q=iB gamma(q)`, and do they identify the resulting unitary block as the
operative action or Higgs construction?

## Return

The existing page/timecode-verified extraction supports:

- `SOURCE-CONFIRMS`: the raw fibre Frobenius form is trace-reversed;
- `SOURCE-CONFIRMS`: the metric fibre has one distinguished trace direction;
- `SOURCE-CONFIRMS`: the trace-reversed signature supplies the `(6,4)` normal
  route used in the `(1,3)+(6,4)=(7,7)` split; and
- `SOURCE-ASSERTS`: a full `U(64,64)` arena, Curt's two complex `(32,32)` Weyl
  halves, and gauge/Higgs-like/CKM/Yukawa functions in `varpi` components.

The checked sources do not print `q_g=g/2` inside `H_q`, do not state that the
operative connection satisfies `D_varpi H_q=0`, do not reconcile the fixed
trace direction with the full normal `Spin(6,4)` action, and do not identify a
specific equation-9.16 or `varpi` block as the observed Higgs/Yukawa channel.

## Disposition

```text
SOURCE-CONFIRMS:
  trace reversal;
  a distinguished metric-fibre trace direction;
  the authorial full parent and expository two-half target.

SOURCE-SILENT:
  H_q=iB gamma(g/2);
  D_varpi H_q=0;
  fixed-trace versus moving-normal symmetry reconciliation;
  equation-9.16 grading and physical scalar-doublet block.

REPO-DERIVES:
  q_g=g/2 is a global natural unit-negative vertical Clifford receiver;
  it gives exact (64,64) plus (32,32)+(32,32) Hermitian inertias;
  its split stabilizer is 42 and native J4/J10 are H_q anti-isometries;
  no new q datum or P1 use is required.
```

This closes an ownership/datum question while leaving source/action adoption
and the physical block untouched.
