---
artifact_type: source_reinspection
created: 2026-08-09
status: SOURCE_CONFIRMS_TWO_C32_32_WEYL_HALVES_AND_SEPARATE_U64_64_PRINCIPAL_GROUP__SOURCE_SILENT_OPERATIVE_RESIDUAL_ACTION_PARENT_AND_GLOBAL_TANGENT
---

# Source reinspection: K77 action-parent distinction

## Question

Do the checked source materials select the operative parent of the truncated
selected K77 action: `Spin(7,7)`, a block-preserving
`U(32,32) x U(32,32)`, or full `U(64,64)`?

## Source loci

- Weinstein's 2025 TOE transcript at `02:41:57` says that the chimeric bundle
  supports spinors and can be regarded as a bundle with a `U(64,64)` structure
  group. This is authorial support for the full principal-group arena.
- Curt's iceberg exposition at `00:17:36` splits the 128-dimensional spinor
  into two 64-dimensional Weyl pieces, each with split Hermitian signature
  `(32,32)`. At `02:40:55` he calls these two copies of `C^(32,32)`.
- Curt separately states at `01:30:42` that the principal bundle has structure
  group `U(64,64)`. The two statements are compatible but not identical: the
  block product preserves the Weyl halves separately; full `U(64,64)` permits
  off-diagonal transformations exchanging them.
- The 2021/Portal source material directs the use of the unitary spinor-frame
  bundle and the norm-squared residual action, but the checked passages do not
  say that the repo-selected `Cl1+Cl2+Cl5` residual is invariant under the full
  group or identify the symmetry retained after observation.

## Disposition

```text
SOURCE-CONFIRMS:
  - two C^(32,32) Weyl halves in the K77 complex presentation;
  - a separate full U(64,64) principal-group arena;
  - connection-valued varpi and group-valued epsilon source variables.

SOURCE-SILENT:
  - which group is the operative residual/action parent after observation;
  - whether the selected residual must enlarge to the 16,382/16,383 closures;
  - a global full-parent functional tangent, boundary condition or domain;
  - selection of either algebraic branch or its amplitude.
```

The exact full-parent stationarity calculation is therefore a source-directed
compatibility test. It cannot be reported as source selection of `U(64,64)` or
as a reason to collapse the two Weyl halves into the full group.
