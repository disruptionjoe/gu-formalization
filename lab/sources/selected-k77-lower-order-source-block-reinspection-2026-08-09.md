---
artifact_type: source_reinspection
created: 2026-08-09
status: SOURCE_CONFIRMS_GRAMMAR__SOURCE_SILENT_EXACT_BLOCKS
---

# Source return: selected K77 lower-order source blocks

## Located source commitments

The primary-source pack owns `(g,varpi,epsilon)` as the displayed source
coordinates and owns augmented torsion as the difference
`T=varpi-epsilon^{-1}d_0 epsilon`.  The moving-Shiab reinspection owns the
primitive epsilon rules

```text
delta B = D_B eta,
delta T = -D_B eta,
delta Phi_i = [Phi_i,eta]
```

at fixed `varpi` and fixed metric.  Thus `A=B+T=varpi` and `F_A` do not move,
while the split torsion term and the Shiab coefficients do.

## Return code

`SOURCE-CONFIRMS` the source-coordinate, two-connection, augmented-torsion and
moving-Shiab grammar used by this wave.

`SOURCE-SILENT` on the exact selected K77 coefficient
`-b+360(b+t)^2`, its two algebraic branch values, the rank-91 result, the
branch port of the earlier metric theorem, the action parent and all analytic
or quantum completions.  Those are repository constructions, not quotations.

The source does not identify the selected Spin parent with either two
`U(32,32)` halves or the full `U(64,64)` comparator.
