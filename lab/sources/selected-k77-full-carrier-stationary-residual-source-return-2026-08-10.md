---
artifact_type: source_return
created: 2026-08-10
return_code: SOURCE_CONFIRMS_FULL_FOUR_FIELD_GRAMMAR_SOUTHEAST_ZERO_AND_ADMITTED_NONZERO_RIVAL__SOURCE_SILENT_ON_W_GRAPH_BV_SELECTOR_DOMAIN_AND_MOVING_VARPI_FAMILY
---

# Source return: full-carrier stationary residual

## SOURCE-CONFIRMS

The draft extraction confirms four independent barred/unbarred fields, the
displayed southeast-zero block grammar, the connection one-form slots and the
minus-adjoint lower cells. It also explicitly admits an unspecified nonzero
southeast rival.

## SOURCE-CORRECTS

None in this wave. The correction is internal Layer 0: the source port acts on
full `Omega1(S)`, so the v0.155 `192+128` residual was conditional on an
invariant carrier that has not been constructed.

## SOURCE-SILENT

The source does not supply a W graph, physical W/mirror selector, unique
trace-`q`/Pin realization, moving-`varpi` determinant locus, BV quotient,
closed domain, Fredholm index or generation-count readout.

The exact full-rank and large-kernel results are repository calculations on
declared candidates, not quotations from Weinstein. Full `U(64,64)`, moving
Spin and two `U(32,32)` halves remain separate parent witnesses.
