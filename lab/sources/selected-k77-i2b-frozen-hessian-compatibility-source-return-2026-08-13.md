---
artifact_type: source_return
created: 2026-08-13
status: SOURCE_CONFIRMS_AND_SILENT
claim_ids: [SC-ACT-01, SC-ACT-04]
---

# Source return: frozen-Hessian compatibility

`SOURCE-CONFIRMS`: the located Weinstein material owns the bosonic I2B
residual-square grammar, distinguishes a source `Q_B` primalizer slot, and
uses moving Shiab/observation plus an affine source connection. Those are the
correct named owners for a covariant completion.

`SOURCE-SILENT`: the located source does not state the exact frozen
decomposition `H=H2+H0` with `rank(H0)=196`, the vanishing four cross blocks,
the forced `C0=0`, the combined rank-56 degree-one defect, or its cancellation
by a complete stationary moving Noether/BV identity.

The repository therefore falsifies only the naive constant-coefficient
completion. It does not attribute that stronger failure to GU.

`SOURCE_RETURN=SOURCE_CONFIRMS_I2B_RESIDUAL_SQUARE_AND_QB_SLOT__SOURCE_SILENT_FROZEN_COMPATIBILITY_DEFECT_AND_MOVING_NOETHER_COMPLETION`
