---
artifact_type: source_return
created: 2026-08-13
source_return: SOURCE_CONFIRMS_TILTED_GRAPH_INDEPENDENT_VARPI_AND_ADJOINT_GAUGE_GRAMMAR__SOURCE_SILENT_CL1_EXACT_FORM_GAUGE_SYMMETRY
target_claim: NONE-NOT-A-KILL
---

# Source return: principal exact-form map versus source gauge

The 2021 source action uses source coordinates `(epsilon,varpi)`, admits the
independent variation `varpi+s alpha`, and defines the distortion as the
difference between `varpi` and the epsilon-derived reference connection. The
already-filed selected implementation consequently distinguishes:

- the arbitrary physical `varpi/T` tangent;
- the tilted source-coordinate kernel; and
- the residual adjoint Spin action generated by the 91 `Cl2` bivectors.

The source does not print the selected 196-real bank, its exact rank-25
projected adjoint image, or a distinct `Cl1`-valued zero-form gauge symmetry
acting by `delta T=d xi`. The latter cannot be inferred from the fact that the
principal Hessian happens to annihilate `k tensor xi`.

Disposition: the source confirms the two-connection/adjoint gauge grammar and
is silent on the additional symmetry needed by the predecessor's gauge name.
The repository derives the retype; no source claim is weakened or killed.
