---
artifact_type: source_reinspection
created: 2026-08-08
status: SOURCE_CORRECTS_GAMMA_EPSILON_DIFFEO_IDENTIFICATION__MOVING_OPERATOR_REMAINS_RANK3
source_return: SOURCE-CORRECTS
---

# Source reinspection: epsilon, diffeomorphisms and the principal Ward packet

## Return

`SOURCE-CORRECTS` the v0.85 composition. The source owns an `H`-valued gauge
transformation `epsilon`, an independent connection/one-form `varpi`, and the
two-connection difference

```text
T_omega = varpi - epsilon^-1 d_0 epsilon.
```

It does not identify a spacetime diffeomorphism parameter `xi` with the
repository construction `gamma_epsilon(xi-flat)`. That identification is a
conditional soldering proposal, not a quoted GU transformation law.

## Primary-source loci

| locus | source content | disposition |
| --- | --- | --- |
| 2021 draft, equations 9.1--9.7, transcribed in `weinstein-gu-primary-source-pack-2026-07-30.md` | the action domain contains `(epsilon,varpi,g)`; the printed translation test varies `varpi+s alpha`; `Upsilon^B=odot F_A+*kappa_1T` | `SOURCE-CONFIRMS` the independent connection variation and its full curvature-plus-torsion response |
| 2021 draft, equations 8.1 and 9.2--9.7 | `Phi_i(epsilon)=Ad_(epsilon^-1)Phi_i^0`; logarithmic epsilon variation moves the reference connection, augmented torsion and Shiab coefficients | `SOURCE-CONFIRMS` primitive internal gauge-epsilon calculus |
| UCSD 2025 transcript, `00:17:01--00:25:03`, recorded as WGS-06 | epsilon is used as a gauge transformation in the two-connection distortion pattern | `SOURCE-CONFIRMS` epsilon's gauge role |
| same source corpus | no formula sets the internal logarithmic epsilon variation to the grade-one Clifford image of a spacetime diffeomorphism parameter | `SOURCE-SILENT` on `eta(xi)=gamma_epsilon(xi-flat)` |

WGS-06 already records the Layer-0 warning: source `epsilon` is not
automatically the repo's dynamical soldering datum. The gamma construction
may still be useful as an internal gauge tangent or a future soldering law,
but it cannot be silently inserted into the physical diffeomorphism Ward
packet.

## Consequence

For the current source-variable principal model, the direct torsion responses
cancel on the diffeomorphism orbit:

```text
delta_g T + delta_varpi T = -C_q xi + C_q xi = 0.
```

The full `varpi` response also contains the curvature derivative in
`odot F_A`. Exact calculation leaves a rank-three curvature packet. Thus the
source correction removes the gamma-created fourth direction but does not
remove the moving Shiab/Hodge/curvature/density/observation construction
burden.

## Boundary

This receipt does not supply the remaining rank-three moving operator,
complete lower-order metric or epsilon response, `K*`, a Green concomitant,
a common domain, or a reduced symplectic/BFV class. It changes no datum,
canon verdict or public posture.
