---
artifact_type: source_return
created: 2026-08-13
status: SOURCE_OWNS_PRINTED_ENDPOINT_I2B__REPO_DERIVES_FIXED_NATURAL_GRADE1_QB_LINE
---

# Source return: fixed-natural I2B owner

## Source-confirmed

- `SC-ACT-04` prints the bosonic second action as a residual square of the
  printed endpoint `Upsilon_B`, with a distinct primalizer slot `Q_B`.
- The printed endpoint and the actual first-action Euler covector are distinct
  source objects.
- The source supports the full `U(64,64)` arena and the two
  `C^(32,32)` halves/block-preserving presentation as distinct layers.

## Repository-derived

- On the actual traceless grade-one residual, every fixed natural `Q_B`
  allowed by either source-supported parent restricts to the trace/Hodge line
  up to one nonzero scale.
- Composing that classification with the exact endpoint certificate resolves
  the fixed-natural source I2B owner without choosing the unknown scale.

## Source-silent / still open

- the real-K77 normalization of `Q_B`;
- any moving or field-dependent primalizer or selected fundamental symmetry;
- the complete moving metric/section/Shiab/observation principal coefficients;
- the physical tangent/BV/BFV graph, global domain, positivity and spectrum.

The repository-composed `E_act/Q_u` square is not attributed to the source and
remains a separate comparator.
