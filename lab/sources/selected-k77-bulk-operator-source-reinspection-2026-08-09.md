---
artifact_type: primary_source_reinspection
created: 2026-08-09
status: SOURCE_CONFIRMS_ACTION_GRAMMAR__SOURCE_SILENT_COMPLETE_OPERATOR_AND_DOMAIN
---

# Source return: K77 bulk-operator admission

The primary-source pack was checked before pricing the analytic gate.

## What the source actually supplies

The 2021 action locus, transcribed in
`weinstein-gu-primary-source-pack-2026-07-30.md` at `WGS-01`, explicitly gives

```text
I1(g,epsilon,varpi)
T_omega = varpi - epsilon^-1 d0 epsilon
F_B + (1/2)d_B T + (1/3)[T,T]
```

and its displayed translation variation gives `Upsilon`. `WGS-04` separately
prints `Xi=D_omega Upsilon` and says the second equation is redundant when
`Upsilon=0`. These statements own the dependency grammar used by the admission
audit; they do not own the later analytic realization.

The affine curvature identity already used by the repo,
`F_A=F_B+d_B T+T wedge T`, is the allowed covariant reduction. It prevents
second derivatives of the gauge frame from being counted as independent
principal data while preserving first derivative epsilon dependence in `T`.
The audit records both the raw coordinate count and this reduced count.

## What the source does not supply

The 2025 TOE discussion at local `01:16:13--01:17:35` distinguishes the
one-time Hamiltonian problem from the multiple-time ultrahyperbolic problem.
At `01:25:01--01:25:42`, Weinstein calls repayment of that upstairs problem
technical debt. The checked source does not provide:

- a branch- and parent-specific full Hessian;
- a gauge-fixing functional and bulk ghost operator;
- Douglis--Nirenberg weights or a principal-symbol theorem;
- a field Riesz map or covector graph calculus;
- closed maximal/minimal graph domains;
- a surjective trace theorem, Green inverse or Krein-positive realization;
- a coupled bulk BV and boundary BFV complex.

## Return code

`SOURCE-CONFIRMS_TWO_CONNECTION_FIRST_ACTION_RESIDUAL_AND_XI_GRAMMAR__SOURCE-SILENT_MIXED_ORDER_GAUGE_FIXED_OPERATOR_PARENT_SPECIFIC_HESSIAN_DMAX_DMIN_AND_BV_BFV`.

This is a locus statement, not a truth guarantee for the repository's
mixed-order admission construction.
