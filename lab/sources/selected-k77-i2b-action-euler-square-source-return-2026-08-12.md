---
title: "Selected K77 corrected action-Euler square source return"
date: 2026-08-12
status: SOURCE_RETURN_COMPLETE
source_return: SOURCE_CONFIRMS_FIRST_ACTION_AND_PRINTED_ENDPOINT_FORMULAS__SOURCE_SILENT_CORRECTED_E_ACT_SQUARE__REPO_CONSTRUCTS_EXACT_LOCAL_RIVAL
---

# Source return

## Question

Ledger v0.225 left two second-action readings separate: the released draft's
literal norm square of its printed endpoint `Upsilon_print`, and the repository's
action-consistent possibility of squaring the actual first-action Euler
covector `E_act`.  This return asks what the source owns after the latter has
now been computed on the exact finite bank.

## Source boundary

The released source displays:

```text
I1B containing Fbar = F_B + 1/2 D_B T + 1/3 T wedge T,
Upsilon_print = Shiab(F_A) + *kappa T,
I2B = ||Upsilon_B||^2.
```

The prior source collision and exact repo result establish that the printed
endpoint is not the derivative of the displayed first action for the selected
full-domain Shiab.  The repo-corrected action covector is

```text
E_act = Shiab(Fbar) + L_T^! Shiab^! T + *kappa T.
```

The source does not print `||E_act||^2` as a replacement action, specify the
observer pairing `Q_u` used here, or derive the physical tangent/BV reduction
needed to discard transverse connection variations.

## Repo result and disposition

On the declared constant trace-`H_q`, fixed-background bank, the exact
Fréchet-adjoint companion is `2 S_q`.  Consequently `||E_act||^2_Q_u` and the
literal endpoint square have the same restricted value and the same complete
fixed-background Euler covector, despite different Fréchet maps.

- `SOURCE-CONFIRMS`: the displayed first-action and printed endpoint formulas.
- `SOURCE-SILENT`: the corrected `E_act` norm-square action, observer `Q_u`,
  physical tangent restriction, and BV quotient.
- `REPO-CONSTRUCTS`: the exact local corrected rival and its comparison with
  the literal endpoint.
- `SOURCE-CORRECTION-PRESERVED`: restricted equality does not restore the
  false full-domain identification of endpoint and action derivative.

No source claim, canon verdict, public posture, datum, quotient, or residue
count changes.
