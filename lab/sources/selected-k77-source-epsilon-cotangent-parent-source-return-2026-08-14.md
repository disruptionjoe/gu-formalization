---
artifact_type: source_return
created: 2026-08-14
status: SOURCE_CONFIRMS_EPSILON_AND_TWO_CONNECTION_VARIATION__REPOSITORY_DERIVES_COTANGENT_PARENT__SOURCE_SILENT_CARTAN_RESTRICTION_AND_OPPOSITE_COPY
---

# Source return: source-epsilon cotangent parent

The released draft owns the group-valued epsilon field, the connection
`B(epsilon)`, the distortion `T=varpi-B(epsilon)` and the moving invariant
forms.  The already-verified primitive variation gives
`delta B=D_B eta`, `delta T=-D_B eta` for
`eta=epsilon^-1 delta epsilon`.  Exact Green integration by parts gives the
unrestricted boundary term pairing the epsilon trace with
`i_n(E_B-E_T)`.

`SOURCE-CONFIRMS`: the field and variation grammar from which the canonical
preboundary pair is derived.

`REPOSITORY-DERIVES`: identifying
`<lambda,epsilon^-1 delta epsilon>` with the canonical left-trivialized
cotangent potential, constructing its rank-182 two-form and restricting it to
the exact Cartan subcarrier.

`SOURCE-SILENT`: a condition selecting `lambda in C`, a second epsilon/edge
field, an opposite-sign compensator, a boundary action for such a copy, a
global analytic domain, proper BFV or physical cohomology.

The source's single epsilon cannot be cited twice as two independent phase
spaces.  The Cartan restriction and opposite-copy question remain
repository-derived constructions.
