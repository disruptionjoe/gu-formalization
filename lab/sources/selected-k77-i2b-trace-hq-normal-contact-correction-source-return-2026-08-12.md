---
title: "Selected K77 I2B trace-Hq normal-contact correction source return"
date: 2026-08-12
status: SOURCE_COLLISION_COMPLETE
---

# Source return

## Return

`SOURCE-CONFIRMS`:

- the upstairs augmented torsion is a two-connection difference and enters
  the released residual with a nonzero `kappa` Hodge term;
- the source/exposition distinguishes two complex `C^(32,32)` Weyl halves
  from the full `U(64,64)` principal arena.

`SOURCE-SILENT`:

- the repository's particular trace-owned `H_q=iB gamma(q_g)` embedding;
- `D_varpi H_q=0`, the operative block reduction and a physical first-normal
  germ;
- the coupled on-shell prolongation, domain and state selecting a germ; and
- any identification of that solution data with P1, P2 or P3.

`REPO-PRIOR-ART-CORRECTS`:

- v0.194 owns the canonical trace `q_g=g/2`, full `(64,64)` Hermitian form
  and two `(32,32)` restrictions;
- the exact trace-`H_q` real form reaches rank 12 per normal, hence rank 120
  inside the 160-dimensional contact, leaving rank 40;
- every relevant grade-two direction preserves the two Weyl halves; and
- the scalar contact completion is pointwise source-realizable, while its
  on-shell value remains open.

## Layer-0 return

The source statement is two `C^(32,32)` carrier halves. The block product is
their stabilizer subgroup, not automatically two independent connections.
The generation notation `H^- = X(S^+)` is also distinct from the Hermitian
form `H_q`; using the same letter does not identify these objects.

```text
SOURCE_RETURN=SOURCE_CONFIRMS_TWO_CONNECTION_AUGMENTED_TORSION_AND_TWO_C32_32_CARRIER_HALVES__SOURCE_SILENT_TRACE_HQ_EMBEDDING_AND_ON_SHELL_GERM__REPO_PRIOR_ART_CORRECTS_REAL_FORM
```
