---
artifact_type: source_return
created: 2026-08-12
run_id: RUN-20260812-110014-gu-i2b-full-unitary-image-covariance
source_claims: [SC-GRP-01, SC-GRP-02, SC-ACT-02, SC-ACT-04]
disposition: SOURCE_CONFIRMS_FULL_U64_64_PARENT_TWO_WEYL_HALVES_AND_RESIDUAL_SQUARE__REPO_CORRECTS_V0202_POINTWISE_COMPLEMENT_FENCE__SOURCE_SILENT_ON_SELECTED_SHIAB_GLOBAL_CONNECTION_AND_MOVING_HQ_DERIVATIVES
---

# Source return: full-unitary image and covariance gate

## `SOURCE-CONFIRMS`

- `SC-GRP-01/02` place the authorial connection in a full `U(64,64)`
  principal arena and its Dirac representation.
- Curt's source exposition separately displays two complex `C^(32,32)` Weyl
  halves.  This supports a block `U(32,32)xU(32,32)` presentation when the
  halves are preserved, but does not identify that subgroup with the full
  parent.
- `SC-ACT-02/04` own the swervature/displasion shell and its residual-square
  bosonic action.

## `REPO-CORRECTS`

The exact K77 Clifford construction proves that the phase-completed
`Cl(7,7)` basis is all pointwise `u(64,64)`.  Therefore the v0.202 phrase
“full source-unitary complement” is wrong at the pointwise coefficient-fibre
level.  Its `0/2/4` bank already contains every full-unitary direction capable
of producing the grade-one target through the selected Shiab.

This correction does not collapse the source's full parent into the two-half
subgroup, and it does not identify a pointwise Lie algebra with a global
connection.

## `SOURCE-SILENT`

No inspected source:

- selects the repository's `comm/symi/symi` Shiab as Weinstein's preferred
  map;
- states the pointwise rank-`364 -> 365` exclusion;
- constructs the derivative of moving `H_q`, Hodge or Shiab;
- supplies a Bianchi-compatible global connection realizing the required
  residual cancellation; or
- derives the physical Higgs vacuum, quotient, BV class or analytic domain.

The next construction must therefore use global/derivative information or a
different source-typed selector, not an alleged missing pointwise
`u(64,64)` coefficient direction.
