# Source return: K77 independent-dual weight orbit

Return: `SOURCE-CORRECTS`.

The 2021 equation-9.16 extraction explicitly calls the barred and unbarred
fermions four distinct fields, places covariance factors on both sides and
introduces the matrix as an operator family rather than a uniqueness theorem.
That source grammar supports independent left/right field-coordinate changes.
It therefore corrects the repository's v0.174 inference that
`p=w_+w_-` is already invariant on the source-native action.

The source does not construct an anti-linear real-K77 identification, global
closed domain, quantum measure or physical normalization that would reduce
left/right equivalence to congruence. V0.174's `p` remains a conditional
invariant only after such a reality structure is built.

Full code:
`SOURCE_CONFIRMS_FOUR_INDEPENDENT_FIELDS_AND_COVARIANT_OPERATOR_FAMILY__SOURCE_CORRECTS_P_FROM_SOURCE_NATIVE_INVARIANT_TO_REALITY_CONGRUENCE_CONDITIONAL__SOURCE_SILENT_ON_ANTILINEAR_REALITY_GLOBAL_DOMAIN_AND_PHYSICAL_NORMALIZATION`.
