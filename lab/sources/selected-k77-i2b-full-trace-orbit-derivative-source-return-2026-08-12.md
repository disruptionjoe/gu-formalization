# Source return: I2B full normalized trace-orbit derivative

Status: source return for a repository-derived variational theorem; no source,
canon or public-posture promotion.

## Return code

`SOURCE_CONFIRMS_MOVING_EPSILON_CONJUGATION_GRAMMAR_AND_C32_32_WEYL_CARRIER_SPLIT__SOURCE_SILENT_ON_HQ_AND_PPLUS__REPO_DERIVES_FULL_NORMALIZED_TRACE_ORBIT_DPPLUS__FULL_U64_64_ACTION_PARENT_AND_COMPLETE_EULER_PREBOUNDARY_OPEN`

## What the source owns

The inspected source material owns four distinct statements relevant here:

- a moving `epsilon`-conjugated Clifford frame;
- the bosonic residual norm-square and formal-adjoint grammar;
- two complex Weyl/carrier halves of type
  `C^(32,32) + C^(32,32)`; and
- a separate full `U(64,64)` principal-group arena.

The carrier statement is not a source declaration of two independent
`U(32,32)` connection fields.  `U(32,32) x U(32,32)` is the subgroup that
would preserve the displayed carrier split; whether the operative action
connection reduces to it remains a construction question.

## What the source does not own

No inspected primary source:

- defines the repository `H_q`, `tau_q` or `P_+`;
- prints the 78-plus-13 trace-stabilizer/orbit decomposition;
- gives the complete 13-direction `dot P_+` family;
- identifies source `epsilon` with the repository transition family;
- selects the full `U(64,64)` action connection or its block-preserving
  subgroup; or
- supplies the complete arbitrary-field Euler/preboundary construction.

## Repository result

Inside the admitted selected real K77 action, the repository classifies all
91 `so(6,4)` directions.  The 78-dimensional normalized-trace stabilizer
commutes with `P_+`; every one of the 13 trace-orbit directions produces a
rank-56 `dot P_+=[L,tau]/2`, and their joint images span the complete
392-real target.  The moving first variation is covariant only when these
terms are retained.

This is a repository theorem about the fixed-norm tautological trace orbit.
It neither upgrades the construction to a source quotation nor collapses the
source `C^(32,32) + C^(32,32)` carrier split, the derived block-preserving
subgroup, the full `U(64,64)` parent and two independent connections into one
object.
