---
artifact_type: source_reinspection
created: 2026-08-09
status: SOURCE_CONFIRMS_ACTIONS_FIELDS_AND_MOVING_CONNECTION_GRAMMAR__SOURCE_SILENT_ALGEBRAIC_PORTS_COMPLETE_HESSIAN_AND_PARENT
---

# Source reinspection: K77 two-branch action-block port

## Question

Does Weinstein's primary material identify the repository's two algebraic
stationary branches, state that their action operators are identical, or
select the Spin, two-half or full-unitary action parent?

## Return

The primary source pack owns the first transgression action `I^B_1`, the
variables `(g,varpi,epsilon)`, the connection difference
`T_omega=varpi-epsilon^{-1}d_0 epsilon`, and the resulting obstruction
`Upsilon`. The audited norm-square instruction supplies a **distinct** second
Lagrangian; it is not a term whose relative coefficient with `I^B_1` is given.

The source does not name the repository's two `Q(sqrt(3))` stationary roots,
their exact block coefficients, the rank-91 or rank-1470 ports, or an action
parent among the selected Spin carrier, two `U(32,32)` halves and full
`U(64,64)` comparator. It also does not say that equality of a principal bank
implies equality of the complete Frechet operators.

```text
SOURCE-CONFIRMS:
  first transgression action on (g,varpi,epsilon);
  moving connection-difference grammar;
  distinct Upsilon norm-square construction.

SOURCE-SILENT:
  algebraic branch coefficients and branch selection;
  complete branch-indexed Hessian/Jacobian;
  equality of lower-order operators;
  action-parent selection, gauge fixing and analytic domain.
```

The exact ports in this wave are therefore repository constructions typed
against, but not quoted from, the source.
