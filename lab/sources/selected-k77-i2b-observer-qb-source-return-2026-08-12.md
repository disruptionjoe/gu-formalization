---
title: "Source return: observer-Q_B radial stationarity composition"
status: source-return
created: 2026-08-12
---

# Source return

## `SOURCE-CONFIRMS`

The inspected GU source reconstruction assigns the second bosonic action the
grammar

```text
I_2^B[Q_B] = 1/2 <Upsilon_B, Q_B Upsilon_B>
```

with a distinct primalizer/pairing slot `Q_B`.  It also keeps the ordinary and
gauge-rotated Levi-Civita connections distinct inside augmented torsion.  This
supports testing one pairing against the complete residual family rather than
testing kinetic rank and potential with unrelated comparators.

## `SOURCE-SILENT`

No inspected primary source defines the exact Hodge-adapted K77 object

```text
Q_u : A -> H_u^{-1} A^dagger H_u,
H_u = i B gamma(u),
```

selects a unit observer `u`, prints the Gram `diag(160,2)`, or derives the
shifted branch `r^2=-3 rho-9 kappa^2/160`.  Those are repository constructions
and calculations.  They must not be attributed to Weinstein.

## Disposition

```text
SOURCE-CONFIRMS-QB-SLOT-AND-TWO-CONNECTION-GRAMMAR
SOURCE-SILENT-EXACT-QU-OBSERVER-EQUATION-AND-SHIFTED-BRANCH
```

The result is therefore a conditional candidate completion.  It changes no
canon verdict, source claim or public posture.
