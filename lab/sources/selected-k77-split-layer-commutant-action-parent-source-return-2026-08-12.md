---
title: "Source return: split-native complex structure, two Weyl halves, and full unitary parent"
status: source_return
created: 2026-08-12
run_id: RUN-20260812-020740-gu-split-layer-commutant-action-parent
---

# Source return

## Loci

- Weinstein 2021 draft eq. (12.19), extracted in
  `gu-2021-draft-s11-s12-extraction-2026-08-03.md`, gives the authorial split
  `TX^(1,3) + N^(6,4)` inside `TY^(7,7)`.
- Draft eq. (11.6) explicitly branches the operative fermion modules under
  `Spin(1,3) x Spin(6,4)` into the complex-looking `2` and `16` factors.
- Curt's exposition, recorded in
  `selected-k77-action-parent-source-reinspection-2026-08-09.md`, gives two
  complex Weyl pieces `C^(32,32)` and the corresponding two-half reading.
- Weinstein's 2025 TOE statement at `02:41:57`, and Curt separately, give the
  full `U(64,64)` principal-group arena.

## Return codes

```text
SOURCE-CONFIRMS:
  - the operative 4+10 split and Spin(1,3) x Spin(6,4) branching;
  - two complex C^(32,32) Weyl halves;
  - a distinct full U(64,64) principal-group arena.

SOURCE-CORRECTS:
  - any unscoped statement that no native complex structure exists at the
    post-observation split layer;
  - any collapse of the two U(32,32) blocks into the full U(64,64) parent.

SOURCE-SILENT:
  - whether the action-owned connection preserves omega, J4, both, or neither;
  - whether the same-half Hermitian (32,32) form is derived from the split
    geometry rather than declared as unitary-bundle structure;
  - whether the real split-native J4 is the same structure used by the full
    complex unitary construction.
```

Thus the computation is source-directed and resolves a representation-layer
typing question.  It does not quote the source as selecting the action parent.
