---
artifact_type: source_reinspection
created: 2026-08-09
status: SOURCE_CONFIRMS_FULL_ADJOINT_ONE_FORM_AND_FIRST_ACTION_GRAMMAR__SOURCE_SILENT_SELECTED_FIELD_TANGENT_AND_BRANCH_HESSIAN
---

# Source reinspection: selected first-action tangent

## Question

Does Weinstein or Curt select the `321`-dimensional minimal known completion,
the `1,571`-dimensional low-grade candidate, or an expanded unitary parent as
the field tangent on which the first-action Hessian must close?

## Return

The existing primary-source packet confirms that `varpi` is a full
adjoint-valued one-form, that `T=varpi-epsilon^-1 d epsilon`, and that the
first transgression action uses the path-average curvature rather than the raw
residual. It does not state a `321`- or `1,571`-dimensional truncation, publish
the Hessian at either current algebraic branch, or say that the observed
low-grade truncation is invariant under the complete action.

Curt's iceberg transcript distinguishes the two `C^(32,32)` Weyl halves from
the later full `U(64,64)` principal group. Existing exact work already shows
that their invariant closures have dimensions `16,382` and `16,383`; that is
prior art, not a result of this Run.

```text
SOURCE-CONFIRMS:
  full adjoint-valued one-form; two-connection difference; first-action grammar
SOURCE-SILENT:
  321 versus 1571 tangent; exact branch Hessian; operative action parent
SOURCE-FORK:
  selected Spin carrier; two U(32,32) Weyl halves; full U(64,64)
```

## Layer-0 boundary

- `321` is the smallest already-known selected completion of the restricted
  `125`-field slice after adding the live `196` grade-one directions.
- `1,571` is the known selected low-grade coordinate set
  `10+1470+91`, not a proof of tangent completeness.
- `16,382` and `16,383` are expanded invariant parent closures, not alternative
  spellings of either selected tangent.
- A Hessian block with zero off-slice support proves only closure for that
block. It does not select the whole action tangent.
