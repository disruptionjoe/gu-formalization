# Source return: I2B global primalizer descent

Status: source return for a repository-derived bundle/variational theorem; no
source, canon or public-posture promotion.

## Return code

`SOURCE_CONFIRMS_MOVING_EPSILON_CONJUGATION_GRAMMAR__SOURCE_SILENT_ON_HQ_AND_PPLUS__REPO_DERIVES_SIGN_INSENSITIVE_ASSOCIATED_PPLUS_DESCENT_AND_MOVING_DPPLUS__UNITARY_PARENT_AND_FULL_EULER_PREBOUNDARY_OPEN`

## What the source owns

The inspected source material assigns a moving `epsilon`-conjugated Clifford
frame to the GU construction and gives the bosonic residual norm-square and
formal adjoint grammar.  It also distinguishes the source-sized full
`U(64,64)` principal arena from the two complex Weyl halves described as
`C^(32,32)`.

## What the source does not own

No inspected primary source:

- defines the repository `H_q` target involution or `P_+`;
- states that the tautological trace receiver globalizes this projector;
- prints `dot P_+=[L,tau]/2`;
- identifies the local associated transition with source `epsilon`;
- selects the full `U(64,64)` connection or its two-half block reduction as
  the operative action parent; or
- supplies the complete arbitrary-field Euler/preboundary construction.

## Repository result

On the admitted selected real K77 branch, the existing global trace direction,
Spin structure and associated transition law make `P_+` a sign-insensitive
global associated-bundle endomorphism.  A chosen global Spin frame is not
needed.  Differentiating the pure-frame orbit gives the exact nonzero term
`dot P_+=[L,tau]/2`, required for the moving first variation.

This is a repository theorem inside a conditional action parent.  It does not
upgrade the construction to a source quotation or identify selected Spin,
the two `U(32,32)` halves and full `U(64,64)`.
