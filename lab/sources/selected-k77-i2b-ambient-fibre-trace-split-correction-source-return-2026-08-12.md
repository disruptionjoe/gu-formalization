# Source return: I2B ambient/fibre trace-split correction

Status: source return for an append-only repository correction; no source,
canon or public-posture promotion.

## Return code

`SOURCE_CONFIRMS_AUTHORIAL_ONE_THREE_PLUS_SIX_FOUR_CARRIER_SPLIT_AND_C32_32_WEYL_SPLIT__SOURCE_SILENT_ON_PPLUS_SUBFAMILY_RANKS__REPO_CORRECTS_V0208_AMBIENT_FIBRE_TYPING`

## What the source owns

Draft eqs. `(12.18)-(12.19)`, extracted at
`lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md`, state in one
authorial convention

```text
gimmel: X^(1,3) -> Y^(7,7),
gimmel^* TY^(7,7) = TX^(1,3) + N_gimmel^(6,4).
```

This directly supports the base/fibre/ambient typing used in the correction.
The source also states two complex carrier halves of type
`C^(32,32) + C^(32,32)` and separately a full `U(64,64)` principal arena.
It does not thereby declare two independent `U(32,32)` connection fields.

## What the source does not own

No inspected source prints:

- the repository `H_q`, `tau_q` or `P_+`;
- the rank-56 projector derivatives;
- the rank-280 fibre image, rank-140 soldering image or rank-28 overlap;
- the repository assignment of the four mixed directions to an observation
  derivative packet; or
- the complete physical Euler/preboundary class.

## Repository correction

V0.208's exact 91-generator computation is an ambient `so(7,7)` computation.
The vertical `so(6,4)` subalgebra has 45 generators and gives a
`36+9` stabilizer/orbit split.  The other four q-moving directions mix base
and fibre.  This corrects the repository's interpretation; it is not a claim
that the source printed the exact projector theorem.

Source claims used: `SC-CHI-03`, `SC-SIG-52`, `SC-ACT-02`, `SC-ACT-04`,
`SC-GRP-01`, `SC-GRP-02`.
