---
artifact_type: primary_source_return
created: 2026-08-14
status: SOURCE_CONFIRMS_EPSILON_MOVING_GAUGE_ORBIT_AND_NONCHIRAL_TOTAL__SOURCE_SILENT_COADJOINT_EDGE_FIELD_BOUNDARY_GAUGE_RESTRICTION_CASIMIR_LOCKING_AND_DOMAIN
claim_status_change: none
canon_verdict_change: none
---

# Source return: endpoint coadjoint-edge cancellation

## Source confirms

- The source action contains the group-valued `epsilon` field and the
  connection distortion `T=varpi-epsilon^-1 d_0 epsilon`.
- The invariant Clifford frame moves by `epsilon` conjugation, supporting the
  full gauge orbit used by the exact endpoint moment map.
- Weinstein leaves the upstairs multiple-time/boundary problem as unresolved
  technical debt.
- Weinstein describes the total theory as non-chiral and proposes effective
  low-curvature luminous/dark separation rather than fundamental net chirality.

## Source is silent

The checked primary-source packet does not:

- restrict nonvanishing boundary gauge parameters to `ker(mu)` or to the
  six-dimensional `g_mu intersect ker(mu)` algebra;
- introduce a coadjoint-orbit-valued edge field or its KKS form;
- require the seven invariant values of the endpoint charge to remain fixed;
- choose charged boundary symmetry over gauge reduction;
- construct a functional BFV edge phase space, boundary action, quantum
  measure, closed domain, positive cohomology or W/mirror selection.

## Disposition

The moving gauge-frame parent is `SOURCE-CONFIRMS`.  The exact Kirillov rank,
regular stabilizer, failed charge-kernel closure, 84-dimensional coadjoint
orbit and diagonal cancellation witness are repository results.  The edge
field, invariant locking, boundary disposition and analytic/physical
completion remain `SOURCE-SILENT`.

Mathematical existence of `O_{-mu}` gives the conditional build a precise
route.  It is not evidence that Weinstein's unpublished source action chooses
or dynamics that route.
