---
artifact_type: source_reinspection
created: 2026-08-05
gate: PRE_SHIAB_GAUSS_DEFECT_ACTION_AND_BV_SYMBOL
decisive_return: SOURCE-SILENT
---

# Source reinspection: pre-Shiab Gauss defect action and BV symbol

## Decisive return: `SOURCE-SILENT`

The inspected Weinstein sources do not print the repaired action term tested
in this wave,

\[
 \int_X\langle \sigma_{\epsilon}(v_T),\,G_4(\operatorname{res}_H P_R\bar F+Q(II_s))\rangle_{DW}
 \mu_s,
\]

and do not provide its linearized diffeomorphism-BV complex. The term and its
quotient certificate are repository constructions. They must not be
attributed to Weinstein or Curt.

This silence does not erase the source ingredients already verified in the
controlling receipts:

- the 2021 draft's `I1B` pairs the full connection difference
  `T in Omega1(Y,ad P)` with a selected Shiab image of path-averaged curvature
  and includes the existing `kappa_1` quadratic term;
- the Portal, UCSD and Weinstein-Jaimungal material treats augmented torsion as
  a two-connection difference, observation through a section as mandatory,
  and the vertical metric as trace-reversed Frobenius;
- the source intends even tilted-gauge equivariance but does not turn the
  displayed `Xi=D Upsilon` redundancy into the required off-shell Noether or
  BV theorem; and
- no inspected source supplies the faithful restriction-first
  Gauss/second-fundamental-form Euler receiver or a physical quotient for it.

Those ingredient dispositions remain `SOURCE-CONFIRMS` or
`SOURCE-CORRECTS` in their original receipts. For this wave's decisive new
claim, the single return is `SOURCE-SILENT`.

## Layer 0

| phrase | object here | not identified with |
| --- | --- | --- |
| selected Shiab | the displayed ambient curvature contraction inside current `I1B` | restriction-first observed Gauss curvature |
| connection difference `T` | full adjoint-valued one-form on `Y` | its ten vertical coefficients `v_T` or its Euler covector |
| gravitational soldering `sigma_epsilon` | a required map from the selected adjoint-valued vertical coefficient slot to `Sym2(T*X)` | the identity on the raw `V* tensor ad P` carrier or the one-dimensional tautological trace `q` |
| pre-Shiab defect term | a newly declared observation-sector action horn | the existing bulk `I1B` action or a second copy of it |
| BV tangent | linearized even diffeomorphism/BRST symbol on the repaired action | odd super-IG, global master action, BFV boundary phase space or positive cohomology |
| quotient rank | exact non-null symbol-complex rank | a state count, generation count, physical degree count or global residue reduction |

## Construction consequence

The source cannot decide the gate. The repository must first test whether the
current action already owns the receiver. If the selected Shiab factorization
prevents that at the homogeneous locus, the replacement is admitted only as a
graded posit using existing fields, with its architecture choice, local scope
and remaining weld/domain burden explicit.
