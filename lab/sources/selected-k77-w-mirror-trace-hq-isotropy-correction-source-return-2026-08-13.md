---
artifact_type: source_return
created: 2026-08-13
run_id: RUN-20260813-235701-gu-w-mirror-trace-hq-isotropy-correction
status: SOURCE_CONFIRMS_TRACE_DIRECTION_GRAMMAR__SOURCE_SILENT_REPO_W_MIRROR_TRACE_HQ_POLARIZATION
---

# Source return: W/mirror trace-Hq isotropy correction

## Return codes

`SOURCE-CONFIRMS`:

- the metric-fibre trace direction is distinguished;
- the source program uses the full `U(64,64)` arena, while Curt displays two
  complex `C^(32,32)` carrier halves;
- the total theory is described as nonchiral with observed chirality assigned
  to a later decoupling mechanism.

`SOURCE-SILENT`:

- the repository's W and ASD-mirror projectors;
- identifying them with either ambient complex half;
- the exact same-sector zero and W--mirror rank-192 trace-Hq cross-pair;
- an action term, physical inner product, BV/BFV complex, domain or selector
  built from that cross-pair.

`SOURCE-CORRECTS-ATTRIBUTION` through already-filed repository source work:
the owned tautological trace receiver is normal/vertical, not the positive
base direction used in the predecessor. This correction follows the existing
source-typed ownership chain; it does not add a new source claim.

## Layer-0 warning

Base q, vertical trace q, observer time and a boundary conormal are distinct.
W/mirror one-form sectors, the two `C^(32,32)` carrier halves, their block
subgroup and the full parent are also distinct. The exact cross-pair is a
repository construction and must not be quoted as Weinstein's mechanism.
