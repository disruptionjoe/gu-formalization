---
artifact_type: source_return
created: 2026-08-12
run_id: RUN-20260812-163746-gu-i2b-real-primalizer-phase-gate
claim_ids: [SC-ACT-04]
return_code: SOURCE_CONFIRMS_AND_SILENT
---

# Source return: I2B real-primalizer phase gate

## `SOURCE-CONFIRMS`

The draft's bosonic second action is a norm square of the bosonic residual,
and its variation uses an adjoint. This owns the action slot that a typed
reconstruction denotes by `Q_B`. It does not make the norm positive.

The inspected source surfaces also keep the complex carrier description,
the two `C^(32,32)` halves and the later full `U(64,64)` parent in play. They
do not identify the carrier split with two independent connection fields.

## `SOURCE-SILENT`

The checked sources do not print:

- the real K77 residual primalizer;
- whether it is complex-bilinear or conjugation-sensitive;
- a fundamental symmetry or moving reduction selecting the latter;
- independent relative weights for the two Weyl halves;
- the rank-four Higgs principal form obtained in this wave;
- a global formal adjoint, Green domain, contour or physical spectrum.

## Repository result, not a quotation

The current real complex-bilinear trace pairing has four-real principal rank
two. Its action-owned `P_+` and `P_-` pieces remain rank two. Relative scalar
weights on the two Weyl-half traces also cannot exceed rank two on this
grade-two response.

A phase-even, conjugation-sensitive pairing gives the exact Lorentz block
`8 diag(1,-1,-1,-1) tensor I_4`, hence rank four off the null cone. But an
exact `U(1,1)` sub-block shows that pairing is not invariant under noncompact
unitary adjoint action. It is therefore a conditional repository construction,
not the published source action and not yet an owned datum.

```text
SOURCE-CONFIRMS: bosonic residual norm-square and adjoint grammar.
SOURCE-SILENT: exact Q_B real form and any reduction selecting phase-even Q_B.
REPO-DERIVES: a rank-four conditional pairing and its noncompact-unitary obstruction.
```
