---
title: "Source reinspection: moving Shiab, primitive epsilon variation and Green data"
date: 2026-08-05
status: source_collision_receipt
source_collision: SOURCE_CONFIRMS_DISPLAYED_MOVING_PHI_FAMILY_TILTED_B_T_AND_FIXED_EPSILON_TRANSLATION__SOURCE_CORRECTS_OVERBUNDLED_EPSILON_CHAIN__SOURCE_SILENT_ON_PRODUCT_SELECTOR_AND_GLOBAL_PHYSICAL_DOMAIN
---

# Source reinspection: moving Shiab, epsilon and Green data

## Question

Does Weinstein's written construction supply the moving Shiab and dependent
`epsilon` variation needed by the preceding I1B gate, and does that movement
select the preferred Shiab or a physical Green domain?

## Primary-source collision

| source window | mathematical content used | disposition |
| --- | --- | --- |
| 2021 draft, PDF pp. 41--43, equations (8.1), (9.2), (9.3) | the invariant forms enter as `Phi_i(epsilon)=Ad_(epsilon^-1) Phi_i^0`; the two coefficient products available later in the same draft are the commutator and `i` times the anticommutator | `SOURCE-CONFIRMS` a finite moving eight-channel low-grade family; `SOURCE-SILENT` on which member is preferred |
| 2021 draft, text immediately before (9.2) | Weinstein says he cannot currently locate the preferred historical highest-weight/Bianchi selector and that other Shiab choices are possible | `SOURCE-CONFIRMS` that movement is not selection |
| 2021 draft, equations (9.4)--(9.7) | `T_omega=varpi-epsilon^-1 d_0 epsilon`; the displayed translation test varies `varpi+s alpha` while holding `epsilon` fixed | `SOURCE-CONFIRMS` fixed-`epsilon` translation ownership and the two-connection chain |
| 2021 draft, displayed `B_omega=nabla_0+epsilon^-1d_0epsilon` | for the right logarithmic variation `eta=epsilon^-1 delta epsilon`, the standard Maurer--Cartan calculation gives `delta B=D_B eta`, `delta T=-D_B eta` when `varpi` is fixed | `SOURCE-CONFIRMS` the primitive connection chain, with the formula derived from the displayed definitions |
| prior repo reconstruction `rb1b-native-bosonic-shiab-reopener-2026-07-30.md` | already derives `delta Phi_i=[Phi_i,chi]` and the full two-term `D_epsilon Shiab` formula, but in a `(9,5)`-adjacent finite architecture | `SOURCE-CONFIRMS` the derivative grammar; `SOURCE-CORRECTS` any promotion of that fixture to K77 source identity |
| source corpus checked | no preferred product channel, global noncompact `Y14` operator domain, coupled Krein self-adjoint extension, physical hyperbolic evolution, or BFV phase space is specified | `SOURCE-SILENT` |

The PDF used for the collision is the official 2021 draft with SHA-256
`3f28d742234a9841fc8e51ff172053200aa3eddf3ece38154a3328b9ebd186d4`,
matching the repository's existing source receipt. The already rendered draft
transcription supplies the exact equation and page anchors used by the probe.

## Layer-0 correction

The predecessor's provisional owner ledger placed Hodge, density and
soldering derivatives inside “the epsilon chain.” That is too broad. At fixed
metric, section and other primitive fields, an `epsilon` variation moves:

1. `B(epsilon)` and therefore `T=varpi-B`;
2. the conjugated invariant forms inside `Shiab_epsilon`.

Hodge, density, metric and observation-section derivatives are separate
primitive variations unless an additional construction explicitly makes them
functions of `epsilon`. This repair narrows the primitive equation rather than
deleting those owners from the full action.

## Consequence for the construction

For an action density depending on `(B,T,Shiab_epsilon)`, the primitive
`epsilon` Euler row has the typed form

\[
 E_\epsilon=
 D_B^!(E_B-E_T)
 +(D_\epsilon\mathscr S_\epsilon)^!K_{\mathscr S},
\]

where `K_S` is the action covector for the Shiab operator. Metric/Hodge,
density and section equations remain separate rows. The same moving-Shiab
owner is required for the off-shell homogeneous gauge Ward identity.

Conjugation moves a candidate around its gauge orbit. Because this action is
invertible, it transports mixed-normal support and rank; it cannot by itself
pick a product channel or turn a live block into the zero annihilator needed
for zero-jet factorization.

## Verdict

`SOURCE_CONFIRMS_DISPLAYED_MOVING_PHI_FAMILY_TILTED_B_T_AND_FIXED_EPSILON_TRANSLATION__SOURCE_CORRECTS_OVERBUNDLED_EPSILON_CHAIN__SOURCE_SILENT_ON_PRODUCT_SELECTOR_AND_GLOBAL_PHYSICAL_DOMAIN`

This is enough to build the primitive epsilon chain and its formal Green pair.
It is not enough to name Eric's preferred Shiab or a physical domain.
