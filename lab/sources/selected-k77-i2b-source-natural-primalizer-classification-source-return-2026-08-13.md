---
artifact_type: source_return
created: 2026-08-13
source_claims: [SC-GRP-01, SC-GRP-02, SC-ACT-04]
return_code: SOURCE_CONFIRMS_NORM_SQUARE_TWO_HALVES_AND_FULL_PARENT__SOURCE_SILENT_REAL_K77_QB__REPO_DERIVES_FIXED_NATURAL_GRADE_ONE_PAIRING_UNIQUE_UP_TO_SCALE
---

# Source return: I2B source-natural primalizer classification

## `SOURCE-CONFIRMS`

- `SC-ACT-04` supplies a bosonic residual norm-square action and adjoint
  grammar.
- Weinstein supplies the full `U(64,64)` principal-group arena.
- Curt's exposition separately supplies two complex `C^(32,32)` Weyl halves.
  Their block-preserving subgroup and the full parent are compatible but not
  identical.

## `SOURCE-SILENT`

The checked sources do not print:

- the real K77 formula for `Q_B`;
- a positive fundamental symmetry or contour;
- independent weights for two connection fields;
- an action-selected reduction from full `U(64,64)` to the two-half subgroup;
- a field-dependent `Q_B`; or
- the constraint/BV--Koszul--Tate differential that would restrict arbitrary
  `varpi` variation.

`B` in `Q_B` denotes the bosonic residual-pairing slot in the repository's
typed reconstruction; it is not evidence that the primalizer depends on the
reference connection `B` or on augmented torsion `T`.

## `REPO-DERIVES`

For a fixed gauge-natural symmetric pairing, both source-supported parent
readings restrict to one nonzero scale on the live traceless Clifford-grade-one
residual:

```text
full U(64,64): trace line survives; center-product line vanishes;
two-half block: odd Hom_C(S_-,S_+) bifundamental has one Hermitian line.
```

That scale cannot change the Euler zero set.  This is an invariant-theory
consequence applied to a source-directed construction, not a quotation and not
a complete source-action derivation.

```text
SOURCE-CONFIRMS: bosonic norm square, two C^(32,32) halves, full U(64,64) arena.
SOURCE-SILENT: exact real K77 Q_B, moving reduction, field dependence, BV-KT tangent.
REPO-DERIVES: fixed natural Q_B is unique up to scale on the live grade-one branch.
```
