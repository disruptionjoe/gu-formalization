---
title: "Anchor council seat 2 — cosmology / dark energy (anchor #1: records↔DE)"
status: process
doc_type: council-seat-report
created: 2026-08-03
provenance: "Read-only council seat, anchor-and-triangulate campaign. Nothing executed; no verdict, bar, canon, or LANE-STATE movement licensed. Two external status checks via WebSearch. Filed verbatim by the orchestrator. The pack citation defect reported in the front-matter was fixed by the orchestrator the same day (pack anchor #1 entry, commit following this filing)."
---

# COUNCIL SEAT 2 — COSMOLOGY / DARK ENERGY

Anchor #1 (RECORDS↔DE). Read-only; nothing executed. Layer-0 typing applied to every load-bearing shared term before use.

**Front-load: the pack's citation for my own anchor is wrong.** `lab/process/CURRENT-RESEARCH-CONTEXT.md:46-50` cites `explorations/W187-law-shadow-reduction-audit-2026-07-14.md` for `r(N)=κ₀√N`. That file contains no `r(N)` — it is a law/shadow block-Hessian audit. Two files carry `label: W187`. The record-accretion law lives in `explorations/W187-gu-dressed-open-selfenergy-2026-07-14.md` §3 + `tests/W187_gu_dressed_open_selfenergy.py`. **VERIFIED_REPO_DISCONNECT.** Anyone following the pack lands on the wrong document.

---

# 1. HOLDINGS — what we uniquely hold

### 1.1 The θ-sector's earned character

`canon/theta-field-flrw-dark-energy-eos.md` carries seven corrections. After all of them, three things are still earned:

- **Result 1, algebraically exact and never retracted.** `M_KK = 2√2 H₀` exceeds the de Sitter Breitenlohner–Freedman bound `3H₀/2`, so the θ zero-mode is oscillating-and-damped, not slow-roll, and `⟨w_B⟩ = 0` exactly (`⟨p_B⟩ = ¼A²e^{-3H₀t}[(3H₀/2)² + ω_osc² − M_KK²] = 0`). This is the sector's only exact algebraic result and it survived DE-02 through DE-07 untouched.
- **The two-component structure** (constant umbilic `Λ_eff` at `w=−1` plus a dynamical oscillating θ) — reconstruction grade, structural, survived every correction.
- **W220's four-axis derived CHARACTER** (`explorations/W220-falsify-dark-energy-vs-desi-2026-07-14.md`): dynamical / sign-changing phantom-crossing / clock-coupled at `z = O(1)` / amplitude `O(1) ~ H²`. Verdict SURVIVES-WITH-TENSION; the character-negating ΛCDM null is disfavoured at 2.8–4.2σ by DESI DR2 itself.

Not earned, and I will not export any of it: the `+1.17` ratio (retracted — hardcoded `d ln ρ/dz = 3` bug), the `w_a` sign as a prediction (window-dependent because `w_DE(z)` is non-monotone), `f₀`/`B_i` (fits), and the crossing epoch `z ≈ 0.405` (W160: **provably** free).

**DARK-ENERGY-07** (2026-08-03, canon lines 284–313; register `M-H12`; audit `B13` in `lab/process/eleven-lens-audit-2026-08-03.md`) is the current statistical calibration. Its five items, and the one that matters most is item 5:

> The defensible headline: "excluded AS the DESI dark-energy signal; unconstrained-but-null as a family."

Family-level dAIC is `+1.9..+3.2` — below the repo's own decisive line. Everything decisive is the POINT/SIGNAL hypothesis with amplitude fixed from θ★. Any seat that states this sector as "family excluded" is over-claiming against our own audit.

### 1.2 The shape-failure mechanism — a structural REQUIREMENT, and our sharpest asset

DE-07 item 3 restated the exclusion mechanism and, in doing so, **strengthened** it. The honest mechanism is not amplitude:

- ΛCDM itself overshoots `+4.0 σ_A` on the same metric (the known Planck–DESI amplitude tension), so GU's `+5.7 σ_A` is an increment, not a smoking gun.
- At GU's own θ★-calibrated cosmology (which is essentially DESI's w0waCDM best-fit `H₀`/`Ω_m`), the **amplitude-marginalised SHAPE χ² is +19.3 vs ΛCDM** at `M² = 8`.
- θ★-calibration forces `Ω_m ≈ 0.352`, and **the θ field produces too little low-z evolution to justify it**.
- A correctly-shaped CPL model at the same amplitude gains **−22.8**.

That last pair is the export-grade content: it converts a negative result into a **quantitative requirement on any completion**. Not "GU must do better" — "a completion must supply ≈ +19.3 of amplitude-marginalised shape χ² on DESI DR2, concentrated at low z." Nobody else in the council holds a number of that shape.

### 1.3 W187's r(N) = κ₀√N — with a Layer-0 correction that changes the anchor

The pack says `r(N)=κ₀√N` is "the GU-native low-z-growing candidate." That needs four corrections before it can be used.

**(a) Typing `r`. HOMONYM (vs any dark-energy coefficient).** In W187, `r = |Σ_ext|/|Σ_internal|` — a ratio of self-energies deciding whether the Krein basin is operative, crossing a GU-native threshold `r* = √(M²−s_th)/√(M²−s_ext_th) ∈ [1.00, 1.58]` at `κ* = 1.059`, giving `N* = (κ*/κ₀)²`. It is not a density, not an equation of state, and does not appear in `H(z)`. It is a *coupling* ratio in a Fano–Anderson stand-in for bar-(b) selection. Calling it "the DE coefficient" is a category error until a bridge is supplied.

**(b) Typing `N`. SAME-OBJECT.** This is the genuine shared object and the real content of the anchor. `N` = the X4-shadow past 4-volume in Planck units (W146; `N_today ~ 1.2e243`), and it is the same `N` in `Λ = c/√N`, in W185's holographic tower, and in W166's arrow-of-time mode. The records↔DE connection is real *at the level of N*.

**(c) The exponent is a MODEL, not native.** W187 §5(b) concedes it explicitly: what is GU-native is the **direction** `dr/dN > 0` (records accrete, `J` grows); `√N` is a model choice, and the discharge argument works for any `p > 0`. Do not export the exponent.

**(d) The reciprocal identity — machine-checked, and the sharpest thing in this holding.** `explorations/W215-true-vacuum-dynamical-systems-2026-07-14.md` DS4c (test `tests/W215_true_vacuum_dynamical_systems.py`, 22/22):

> `r(N) = κ₀√N` is monotone increasing, and **`r · Λ = κ₀` is constant** — the accretion and the fade are reciprocal.

Because `Λ = c/√N` and `r = κ₀√N`. The two record laws are not independent knobs; fixing one fixes the other up to `κ₀c`.

**And here is the pre-existing obstruction the anchor must clear.** `explorations/W154-reverse-engineered-source-action-2026-07-14.md` test RE1 (25/25) computed, in July:

> Records only accrete and promotion is one-way, so mean `N(a)` is monotone increasing, hence `Λ_mean = c/√N(a)` is monotone **DECREASING**, hence `Q_mean = ρ_V' < 0` for ALL `a`: **monotone withdrawal, no zero-crossing.** The everpresent mean cannot produce the rise.

`explorations/W158-promotion-gate-boundary-term-C3-2026-07-14.md` RISEb then closed the obvious escape: the `q=5` indefinite Krein grading does **not** convert monotone accretion into a rise (`N_K = 9f₊ − 5f₋` stays monotone at every epoch).

So: the DE-native record law has **exactly the wrong monotonicity** for the DESI shape residual, and that was computed in-repo before the anchor was written. The growing law (`r`) is a homonym. The anchor is not dead — it is *one bridge away*, and the bridge exists (§1.4) — but "W187's r(N) is the GU-native low-z-growing DE coefficient" is not currently a true sentence.

### 1.4 The bridge is NOT missing — it is a named posit with proven necessity (new here)

`explorations/W230-close-a4-derive-w154-2026-07-14.md` (test 24/24) is the object M-H13 needs and does not cite. Its θ is SAME-OBJECT with the DE θ (`θ = π − ε⁻¹Bε`, the 2026-06-22 divergence-free proof, `canon/dark-energy-theta-divergence-free.md`). Verdict COMPLETED-POSIT:

> `θ ~ M⁻¹J` (J = the record current) holds **if and only if** the connection distortion has **no fundamental kinetic term**. Alignment = 1.00000000 exactly at `c_kin = 0`; 0.99655 / 0.92007 / 0.70318 at `c_kin = 1 / 10 / 100`, strictly `< 1` and monotone-decreasing.

So the DE field *is* the record current — under one named axiom, whose necessity (W230) and sufficiency (W180) are both machine-checked, and whose coupling **sign** is forced positive by C-positivity.

**REFEREE_CONJECTURE (mine, filed as a tension, not a kill).** The FLRW pipeline gives θ a kinetic term by construction: `tests/wave25/H44_de_backreacted_background.py:81-115` integrates `B'' = −(3 + H'/H)B' − (M²/H²)B`. Result 1's entire oscillating-and-damped character *is* that kinetic term. W230's posit requires `c_kin = 0`. These are in tension. Caveat that keeps it a conjecture and not a kill: W230's `L` is a *spatial-gradient* stiffness stand-in, and the FLRW mode is homogeneous (`k=0`), where a gradient term vanishes regardless. Deciding whether W230's `L` maps to the FLRW time-kinetic term is XS–S effort and it gates the whole M-H13 item. **This is the cheapest decisive move available to this seat.**

### 1.5 The 120-orders dissolution

`canon/dark-energy-theta-divergence-free.md:96` is scrupulous and I will not inflate it: the resolution is **STRUCTURAL** — ΛCDM fine-tunes because Λ is *forced constant*; GU's θ is not forced constant and is G-equivariant. Claim 1 (dynamic + equivariant) is unconditional; Claim 2 (divergence-free) rests on unproved Assumption 3. Quantitative tracking is explicitly disclaimed.

Adjacent, and worth holding separately because it is a *magnitude* statement: W146 reproduces `Λ l_p² = 1/√N₄ = 1.39e-122` against observed `2.85e-122` — a factor `3Ω_Λ = 2.05`. W185 organises this into the square-root tower `10^243.7 → 10^122.4 → 10^60.9 → 10^30.5`. Both are honestly graded in-repo as **RELABELS** of ported Sorkin/ADGS holographic counting (W138 gate G5), not GU derivations. Hold the structure; do not sell the number.

---

# 2. QUEUE — getting the maths correct

### 2.1 Spec for the r(N(z)) refit (register M-H13, I=H, E=M)

The register's one-line fix — "Re-solve background with r(N(z)); refit θ★; report Δχ²_shape" — is under-specified in three places. Precise spec:

**(i) What `N(z)` means.** *There is no `N(z)` anywhere in the DE pipeline.* `tests/wave25/H44_de_backreacted_background.py` has no record count at all. `N` must be defined as the past-4-volume of the observer's light cone in Planck units, evaluated on the *backreacted* background (self-consistently, since `N` depends on `H(z)` which depends on `N`). Two admissible normalisations: comoving-4-volume, or the W149 confirmed/boundary count `N_conf = π√N_bulk`. **These give different laws** and the choice is not free — W185's exponent-½ result says the maintained count is the AREA, not the 4-volume. Pick one, declare it, and note that W146's `Λ = c/√N` uses the *bulk* 4-volume.

**(ii) What the coefficient couples to.** Blocked on §1.4. Absent the W230 bridge the refit is a homonym refit. Two honest instantiations once the bridge is granted: (A) time-dependent amplitude `f₀ → f₀(N(z))` — the record content sets the θ normalisation; (B) time-dependent `M²(N(z))` — the record count sets the mass. These are physically distinct and give different `w(z)`; the pipeline supports both with modest surgery. (A) is the one that follows from `θ = M⁻¹J`.

**(iii) What freezes.** From `tests/wave46/H46C_theta_star_cmb_calibration.py`, held fixed and not to be touched: `r* = 144.43 Mpc`, `z* = 1089.92`, `r_drag = 147.09 Mpc`, `100θ* = 1.04110`, `ω_m h² = 0.1430` (all Planck 1807.06209 Table 2, +lensing column). Early physics untouched is *legitimate here* — the frozen θ density is `3.4e-5` of matter at `z=30` — but an `N`-coupled coefficient could break that, so the `z_start = 30` frozen-density assertion (H46C check Q1) **must be re-run**, not inherited. Also frozen: `z_start = 30` slow-roll attractor IC, the OQ2 band `M² ∈ {3,7,8}` plus the `[1,25]` bracket, and the DESI DR2 13-element mean + 13×13 covariance (byte-verified against the official Cobaya repo). Floating: `h` (solved by `brentq` on `θ★`), hence `Ω_m`.

**(iv) Pass/fail.** Two criteria, and cite both:
- **W129's pre-stated escape criterion**: an escape is an admissible point with `dAIC < +4`. Currently no point escapes; the softest is `M²=3` at `dχ² = +33.5` raw / `+14.9` profiled.
- **The DE-07 shape metric** — this is the one that actually tests the anchor. The refit must recover a large fraction of the `+19.3` amplitude-marginalised shape χ² at `M²=8`, against the `−22.8` a correctly-shaped CPL model achieves. A new free parameter (`κ₀`, or whatever sets `N*`) costs `+2` in AIC. **Pre-register the target before running**, per P-H28.

**Decidable now? NO** — gated on §1.4. **What IS decidable now, XS, and I recommend it first:** the inverse-problem feasibility check. Given the existing `bao_vector_from_E` machinery in `tests/wave29/H46_de_raw_bao_likelihood.py`, ask what monotone low-z-growing `ρ_DE(z)` deformation would recover `+19.3`, and whether *any* `√N`-shaped (or general `N^p`-shaped) deformation lies in that space. ~30 lines, no new physics, and it can kill M-H13 before the M-effort refit is scheduled. **CHEAP_NEW_COMPUTATION.**

### 2.2 The pending DE additions (register M-M18 / DE-12..15)

**VERIFIED_REPO_DISCONNECT:** the DE-12/13/14/15 item text exists only in `lab/process/improvement-register-2026-08-03.md:191`. No on-disk audit artefact carries it; the detail lives in a session transcript. Anyone scheduling these is working from a one-line summary.

Ranked by information-per-cost:

1. **DE-12 — DESI-w0waCDM positive control through the θ★ pipeline. ~30 lines, XS, DECIDABLE NOW, and it gates everything else.** Push DESI's own best-fit w0waCDM through H46C and check it returns `dAIC ≈ 0`. If it does not, the entire wave45/46/W129 chain is suspect and every number in §1.2 moves. This is a *pipeline-unbiasedness* certificate we do not have and should not proceed without. Run this first.
2. **DE-14 — SNe pure-shape leg (`M_B`-marginalised). E=M, DECIDABLE NOW, and the only one whose sign is unknown.** The named residual since DE-05. DE-07 item 4 **retracted** "adding SNe can only hurt a model with `H₀ = 63.75`" as a physics error: uncalibrated SN compilations carry no `H₀` information (`M_B` degeneracy); they constrain `Ω_m` and shape, and GU's calibrated `Ω_m = 0.352` sits **0.01σ** from DES-Y5's. So this leg can go either way and is the only decisive pending item. Cost driver: fetching a public compilation (none in-repo).
3. **DE-13 — ρ from public DR2 chains. E=S, DECIDABLE NOW (network).** Replaces the *assumed* `rho=-0.8` hardcoded at `tests/wave20/H43_de_shape_falsifier.py:161`. Scope it correctly: this touches only the H43 CPL-projection Mahalanobis (the "~3.2σ" → ~2.7σ, 2.5–3.0 over `ρ ∈ {−0.9,−0.8,−0.7}`). It does **not** touch the raw-BAO exclusion, which uses the byte-verified 13×13 covariance. High honesty value, low physics value.
4. **DE-15 — compressed 3-param Planck likelihood. E=S–M.** The register states its direction in advance ("strengthens exclusion"), which makes it confirmatory, not decisive. Schedule last.

---

# 3. EXPORTS — what this anchor imposes on the bigger structure

Stated as GU-side mathematics only. The TaF-facing half is fenced in §3.6.

**E1 — GRADE A (holds independent of r(N); already computed).** Any completion that places GU's θ-sector at the θ★-calibrated point must supply an additional low-z (`z ≲ 0.5`) evolution channel worth **≈ +19.3 in amplitude-marginalised shape χ² on DESI DR2**. Quantitative, pre-existing, and it survives DE-07's restatement — which *strengthened* rather than weakened it. Scope guard: this constrains the SIGNAL identification, not the family. The family is already driven to its ΛCDM limit and is unconstrained-but-null.

**E2 — GRADE B (conditional on the W230 posit).** *"The completion must contain a record-accumulation functional"* cashes out, GU-side, as a **type** requirement: a monotone, causally-past-supported counting functional `N` on Y14 whose X4 shadow is a 4-volume, admitting a well-defined `N(z)` on a backreacted FLRW background. It is not a numerical constraint. It becomes load-bearing only if the `c_kin = 0` axiom is granted, and W230 proved that axiom is *required* — it cannot be derived from Noether II, equivariance, gauge structure, or shiab (those leave a full 14-dimensional space of equivariant divergence-free currents, rank 14 over a Ψ-sweep).

**E3 — GRADE B (forced by the two record laws jointly; machine-checked, W215 DS4c).** **Amplitude reciprocity: `r(N) · Λ(N) = const`.** A record-coupled completion cannot independently choose its DE amplitude law and its basin-selection coupling law. The mandate's phrasing "amplitude 1/√N-forced, phase free" is correct on both halves — but the sign consequence must ship attached: `1/√N` forcing on Λ gives **monotone withdrawal** (W154 RE1, W158 RISEb), which is the *opposite* monotonicity from what the DESI shape residual requires. Exporting "amplitude 1/√N-forced" without that rider hands the next seat a result that is backwards.

**E4 — GRADE A (W160, exploration-tier, 27/27).** **Phase/epoch is FREE and provably so.** The frontier fluctuation two-point function is stationary (correlation depends on log-count separation, not absolute epoch), so pinning a crossing epoch requires a non-stationary rank-1 bump that breaks that stationarity. Envelope constraint: *no completion can be required to predict `z_x`, and no completion may be credited for matching `z = 0.405`.* Any record-accretion completion claiming an epoch prediction is over-claiming against this theorem.

**E5 — GRADE C (REFEREE_CONJECTURE).** Only `dr/dN > 0` is GU-native-directional. The export is "monotone, unbounded" — **not** `√N`. W187's own robustness argument works for any `p > 0`, and its honest residue is that saturation below threshold is not excluded GU-side.

### 3.6 The TaF-facing half — fenced, NOT exported

Per the ratified tri-repo division of labour (`RESEARCH-STATUS.md:71-72`; `lab/roadmap/tri-repo-division-of-labor-2026-07-02.md`): GU owns **boundary content** — what the boundary must supply, in exact mathematics; time-as-finality owns the **capability measure**; temporal-issuance owns the **source question**. Identity claims across the three remain gated.

Explicitly NOT mine to export:

- **The reservoir Krein SIGN.** W187's single irreducible datum: with a reinforcing (wrong-sign) reservoir, growing κ to any magnitude (tested to `50κ*`) never restores reality. Neither accretion nor fade rescues it. TI/TaF-owned, held as a GATED CONJECTURE.
- **The records/finality INTERPRETATION** — what a record *is*, what promotion *means*, the `q=5` finality-frontier semantics, the capability measure.
- Note also `RESEARCH-STATUS.md` Correction ADAPTER2-01 (2026-07-15): the cross-repo `bar(b) = finality-axis polarity` identity was **withdrawn** and returned to OPEN as a conjectural bridge. Do not cite it as live.

Everything in E1–E5 is stated as record-**count** mathematics and shape statistics, which is the GU side of the fence.

---

# 4. KILLS

### 4.1 What r(N) failure would mean — three distinct failures, do not conflate

**(a) The bridge fails.** If the `c_kin = 0` axiom is incompatible with the FLRW kinetic term (§1.4), M-H13 is not a physics item — it is a homonym, and anchor #1 downgrades from "never connected" to **"not connectable as stated."** This is the most likely outcome and the cheapest to learn (XS–S). Run it before scheduling the M-effort refit.

**(b) The bridge exists, the refit does not recover the shape.** Then the exclusion is **strengthened**, because GU has spent its one native low-z-growing candidate. DE-07's "excluded AS the DESI signal" becomes "excluded as the signal, with its named rescue spent." This is a real loss and should be pre-registered as such — otherwise a null result gets reported as "no change."

**(c) Saturation.** W187 §5(b)'s named residue: `κ_ext` saturating below `r* κ_int` is not excluded GU-side. If `r` saturates, the DE application *and* W187's bar-(b) magnitude discharge fail together.

**The asymmetry that must be protected:** (b) kills the DE anchor but leaves W187's bar-(b) narrowing intact, because the two uses of `r` are typed differently (§1.3a). A DE-side failure must not be reported as a bar-(b) failure.

### 4.2 Kills already fired that bound this anchor

| Kill | Source | Status |
|---|---|---|
| Monotone `N` ⇒ `Q_mean < 0` always; no rise from the mean | W154 RE1, `tests/W154_reverse_engineered_source_action.py` 25/25 | FIRED |
| `q=5` Krein grading does not convert monotone accretion into a rise | W158 RISEb | FIRED |
| Crossing epoch provably free | W160, 27/27 | FIRED |
| `+1.17` ratio; the sharp falsification coefficient | DARK-ENERGY-03 (hardcoded `d ln ρ/dz = 3` bug) | RETRACTED — **there is currently NO sharp GU falsification coefficient on w(z)** |
| "Adding SNe can only hurt" | DARK-ENERGY-07 item 4 | RETRACTED as a physics error |

Net: the rise must come from the non-monotone **fluctuation**, whose amplitude is forced (`Λ ~ 1/√N₄ ~ H²`, O(1) in `q_B`) but whose realisation is free. Even a successful `r(N(z))` refit inherits that free realisation.

### 4.3 Standing clocks (external status checked 2026-08)

- **DESI.** DR2 (arXiv:2503.14738) is the current constraint set, byte-verified in-repo. Full five-year observations completed April 2026; DESI's first full five-year dark-energy results and DR3 BAO are expected **2027**. **The DESI clock does not tick before 2027.** W242's rule holds: passive monitors consume no run unless data have actually arrived. Current post-DR2 literature is reconstruction work (e.g. arXiv:2602.05368), not new constraints.
- **The live tripwire — W220 FC-d.** DESI DR2 gives `w_a/(w₀+1) = −3.468` against the canon F1 kill-line `−3.5`. **Margin +0.032.** If DR3 or Euclid push the joint 2σ ratio past `−3.5`, FC-d fires and axis A4 falsifies (it would require `B_i > 3 M_Pl`, structurally unphysical in GU). This is the sharpest live falsifier the sector owns, and it is external-clock-gated, not agent-executable.
- **Euclid.** DR1 late 2026 (official wording), dark-energy results 2027. W242 deliberately declined to fix a date; keep it undated.
- **JUNO — not a DE clock, and it has partly ticked.** It is the W141 S2 neutrino-anchor clock. JUNO published first results from 59.1 days of data (Nature, 2026): world-leading `Δm²_21` and `θ_12`; mass ordering **not** at 3σ — combination gives a slight normal-ordering preference, `p(IO) ≈ 2–2.6%` (2.2–2.3σ). W141's assumed "JUNO resolves ordering 2027–2028" timeline is **intact**, and the first data are consistent with the assumed normal ordering without discharging it. The S2 kill condition (ton-scale 0νββ **detection** at `m_ββ ≤ ~5.4 meV`) is untouched. No GU-side consequence yet.

### 4.4 Kills filed against my own anchor

1. The pack's citation for anchor #1 points at the wrong W187 (§ front-matter). [Fixed by orchestrator same day.]
2. `r(N)` is not yet a coefficient of anything cosmological — HOMONYM until the bridge is decided.
3. The DE-native record law (`Λ = c/√N`) has the **wrong monotonicity**, computed in-repo before the anchor was written.
4. The bridge that would fix (2) requires `c_kin = 0`, in apparent tension with the pipeline's own kinetic term (REFEREE_CONJECTURE, §1.4).

---

# 5. COVERAGE

**Read in full:** `lab/process/CURRENT-RESEARCH-CONTEXT.md`; `canon/theta-field-flrw-dark-energy-eos.md` (incl. DARK-ENERGY-07); both W187 files; `explorations/W185-record-energy-economy-toy-model-2026-07-14.md`; `tests/wave25/H44_de_backreacted_background.py` (background solver); `tests/wave46/H46C_theta_star_cmb_calibration.py` (header/calibration spec).

**Read in relevant part (grep + targeted):** `canon/dark-energy-theta-divergence-free.md`; `explorations/W129-oq2-m2-band-sweep-de-exclusion-2026-07-14.md`; `explorations/W220-falsify-dark-energy-vs-desi-2026-07-14.md`; `explorations/W242-desi-intake-and-hourly-prediction-queue-2026-07-15.md`; `explorations/W154-reverse-engineered-source-action-2026-07-14.md`; `explorations/W158-promotion-gate-boundary-term-C3-2026-07-14.md`; `explorations/W215-true-vacuum-dynamical-systems-2026-07-14.md`; `explorations/W230-close-a4-derive-w154-2026-07-14.md`; `explorations/W146-substrate-sweep-theoretical-physics-2026-07-14.md`; `explorations/W151-gr-and-c-emergence-from-records-2026-07-14.md`; `lab/process/improvement-register-2026-08-03.md` (M-H12/M-H13/M-M18); `lab/process/eleven-lens-audit-2026-08-03.md` (B13); `RESEARCH-STATUS.md` DE rows + tri-repo fence; `tests/wave20/H43_de_shape_falsifier.py`; `tests/wave29/H46_de_raw_bao_likelihood.py`; `tests/W129_oq2_m2_band_sweep.py`.

**Named gaps (not read):** `explorations/W160-debit3-forced-binary-2026-07-14.md` (cited from RESEARCH-STATUS + W220 summaries, not read directly — E4 rests on secondhand statement of a 27/27 result); `explorations/W144-desi-fitted-issuance-function-2026-07-14.md`; `explorations/W149-*`; `lab/roadmap/tri-repo-division-of-labor-2026-07-02.md` (existence confirmed, contents not read — fence quoted from `RESEARCH-STATUS.md:71-72`); `tests/W187_gu_dressed_open_selfenergy.py` (header + grep only, not full); `tests/wave45/H46B_referee_grade_desi_verification.py`. No SNe compilation exists in-repo, so DE-14's cost estimate carries an unquantified data-acquisition term.

**Discipline:** nothing executed (read-only). No FD-read nulls cited, so P-H29 does not bind. Labels applied: 3 VERIFIED_REPO_DISCONNECT (pack citation; DE-12..15 provenance; `r(N)` typing), 2 CHEAP_NEW_COMPUTATION (§2.1 inverse-problem check; DE-12), 2 REFEREE_CONJECTURE (§1.4 `c_kin` tension; E5). Two external status checks via WebSearch, both reported with their uncertainty.

**Sources (external checks):**
- [DESI Data — Releases](https://data.desi.lbl.gov/doc/releases/)
- [DESI DR2 Results: March 19 Guide](https://www.desi.lbl.gov/2025/03/19/desi-dr2-results-march-19-guide/)
- [Dark Energy After DESI DR2: Observational Status, Reconstructions, and Physical Models (arXiv:2602.05368)](https://arxiv.org/pdf/2602.05368)
- [Measurement of reactor neutrino oscillation with the first JUNO data — Nature](https://www.nature.com/articles/s41586-026-10538-z)
- [Lessons from the first JUNO results (arXiv:2601.09791)](https://arxiv.org/abs/2601.09791)
