---
title: "Anchor council seat 1 — quantum foundations / measurement (anchor #4: positivity↔boundary)"
status: process
doc_type: council-seat-report
created: 2026-08-03
provenance: "Read-only council seat, anchor-and-triangulate campaign. No scripts executed; no verdict, bar, canon, or LANE-STATE movement licensed. Filed verbatim by the orchestrator."
---

# COUNCIL SEAT 1 — QUANTUM FOUNDATIONS / MEASUREMENT
## Anchor #4: POSITIVITY ↔ BOUNDARY

Pack read and binding. Read-only session; no writes, no verdict movement, no bar(b)/H59/count adjacency. All grades below are the artifacts' own unless labelled otherwise; where I add a grade I say so.

---

## 0. Layer-0 typing (done first, because two of these are load-bearing)

| Term | Type | Ruling |
|---|---|---|
| "observable algebra" | **HOMONYM** (settled 2026-08-03) | Three distinct objects: (a) the **compression** algebra `A = Π_RS(·)Π_RS` of the M-C3 probe — 43/44 generators violate `ker Γ`, and by Burnside `A = M(1664,C)` = the FULL matrix algebra; (b) the **Dirac / constraint-preserving** algebra (diagonal `Spin(9,5)`), which is the observable algebra on a constraint surface; (c) the interacting **region algebra** (type III, infinite rank). The hostile review calls the (a)/(b) conflation "a Layer-0 slip at exactly the point the READ-FIRST rule exists to catch." |
| `F` (admissible fundamental symmetries) | **HOMONYM** | `F_A(η_V)` (probe: commutant of an *algebra*) vs `F_H(η)` (Prop 1: defined for a *group*). The corrected note keeps them apart deliberately. Do not let an envelope statement merge them. |
| "Krein sign" / `C`-operator | **UNCERTAIN** | The finite-dimensional fundamental symmetry (`C²=1`, η-self-adjoint, `η(·,C·)>0`) is explicitly a *positive-majorant proxy*, "NOT the native GU ghost-clearance criterion" (draft-skeleton §1). The native H59 object is an interacting, renormalized `[P,S]=0`. Same symbol, unproven identity. |
| "Kramers wall" | **SAME MECHANISM, THREE OBJECTS** | (i) signed-readout Part K: `index_C` even for H-linear operators; (ii) C-07 generation-parity wall (refuted as a (9,5) artifact, firewall Round 2); (iii) the new algebra-level sentence (real commutant = `H` ⇒ no antilinear involution supplies a sign). All ride `J` antilinear, `J²=−1`. Only (iii) is licensed new. |
| "positivity" | **HOMONYM** | Krein positive-majorant (Prop 1) ≠ order-theoretic positive cone (signed-readout Part M: `w(x) ∈ G₊`) ≠ projected Born-level positivity (H59 checklist). Three notions, one word, all live on this anchor. |
| "boundary" | **UNCERTAIN — the biggest untyped term on the leg** | The 13-dim topological boundary (anchor #3), the firewall/adapter boundary (positivity supplier), and the analytic boundary of a BVP/boundary triple (M-H10/M-M7) are three objects. No in-repo artifact identifies any two. The envelope must not assume they coincide. |
| "compact stabilizer" | **HOMONYM, self-flagged** | W219's kinematic Cartan centralizer vs the dynamical good-stable stabilizer. W219 itself says the latter "remains undefined." |
| `Π_κ` / Pontryagin regime | **SAME-OBJECT** across rankN and M-M7, **different scope** (bulk blocked, boundary not). |

---

## 1. WHAT WE UNIQUELY HOLD (at earned strength)

### H1 — The uniqueness theorem killed our own headline. [EARNED; strongest item on the leg]
`papers/drafts/structurally-forced-internally-undecidable/draft-skeleton.md`

**Prop 1:** `F_H(η) ≠ ∅` ⟺ the image of `H` has compact closure in `GL(V)`.
**Thm 2:** when nonempty, `dim_R F_H(η) = Σ_λ dim_R(D_λ) a_λ b_λ`; singleton ⟺ no irreducible real `K`-type occurs on both signs.
**Cor 3:** for the canonical `O(p)×O(q)`, `F` is exactly `{diag(I_p,−I_q)}` — **not** a free `Z/2`.

Grade: proved, elementary, finite-dimensional; 77 machine assertions. Novelty conceded as "mathematically valid but likely standard" (§6.4). What it *did* is the earned part: it retracted the five-method W211 "one externally-owned bit" convergence, recorded as **Correction W211-01** at `RESEARCH-STATUS.md` (~:134). A repo-wide narrative was killed by its own mathematics. That is the credibility asset this seat carries.

### H2 — W219: uniqueness at native level, and an honest hole. [EXACT / OPEN]
`explorations/W219-native-good-stable-stabilizer-input-gate-2026-07-14.md`

`K_cartan = Z_G(P) = Sp(32)×Sp(32)` inside `G = Sp(32,32;H)`; dimension identity exact (`4160 + 4096 = 8256`). Positive and negative quaternionic modules are charged under **different** compact factors ⇒ inequivalent factor-labelled irreducibles ⇒ every `a_λ b_λ = 0` ⇒ `dim_R F_K(β) = 0`. Uniqueness, not a free bit, at the program-native level. 29/29.

The hole is stated by W219 itself and is not cosmetic: `DYNAMICAL-GOOD-STABLE-STABILIZER: NOT YET DEFINED`. Ten audited inputs (H23, W132, W173, W203, W206, W213–W216, W218) each supply something and none supplies the tuple (interacting vacuum + physical state space + observable algebra + isotropy).

### H3 — M-C3: `F = ∅` on the kinematic carrier, with the review's strengthening and its disclosure. [EXACT FINITE COMPUTATION, hostile-reviewed, corrections 1–12]
`explorations/observable-algebra-commutant-trichotomy-2026-08-03.md`
`lab/process/hostile-reviews/2026-08-03-trichotomy-review.md`

- **Existence leg (stronger than first stated):** Prop 1's necessity leg uses **no property of `η`**. One `η_V`-skew diagonal boost `D = M_{09}⊗I + I⊗σ_{09}` inside the algebra, preserving `V`, with exact `Re spec(D) = {−3/2,−1/2,+1/2,+3/2}` (rate `+1.5` = vector weight 1 + spinor weight 1/2; compact contrast direction `3e−17`). Therefore `F = ∅` **for every nondegenerate form and every invariance structure containing that direction**. The 1664-scale signature run is *not* load-bearing for the branch.
- **Classification leg:** `dim_C(commutant) = 1`, real commutant `H·I` (`dim_R 4`) ⇒ `End_A(V) = H·I` ⇒ candidates `{cI : c²=1} = {±I}`, and `(cJ)² = −|c|² ≠ 1` ⇒ `F ⊆ {±I}`. Residual family dimension `0` on the **elementary** warrant. **Theorem 2 is NOT invoked** — its hypotheses fail, and forcing it would contradict the computed `(832,832)`.
- **Signature is structural, not numerical:** `ωβ + βω = 0` exactly ⇒ `η_V(ωx,ωy) = −η_V(x,y)`; both `ω`-chirality halves (832 each) totally isotropic to `1.6e−16` ⇒ neutrality by force of the Clifford algebra.
- **The disclosure that keeps us honest:** `A` is the compression algebra, is by Burnside the *full* matrix algebra, and **the branch was chosen, not discovered** — the constraint-preserving covariant algebra lands on the **REDUCIBLE** branch (`commutant ⊇ span_C{I, ω_V}`, `dim_C ≥ 2`; all 91 diagonal generators preserve `V` to `3.9e−18`). 60/60, ~64 s.

### H4 — `F = ∅` is normal, not a wall; and the quotient exit is unexplored. [STANDARD PHYSICS + settled fork row + VERIFIED_REPO_DISCONNECT]
Gupta-Bleuler QED has `F_Lorentz = ∅` on the covariant space and is unitary on the physical quotient. `GEOMETER-VS-PHYSICS-OBJECTS.md:20` settles ghost clearance on the **keep-and-grade** native side. So `F = ∅` against a positive-majorant criterion already ruled non-native is not a wall — that is the correct reading, and it is *the* thing this seat should say loudest.

The BRST/quotient exit is named in three places and built in none:
- draft-skeleton §4.2 states only a conditional ("*if* a GU BRST complex is constructed, *if* `H⁰(Q)` carries the assumed representation, *if* the pairing descends…").
- `explorations/W207-decisive-bit-brst-cohomology-2026-07-14.md` "repeats the finite-dimensional Schur calculation and labels the resulting vector space as physical cohomology" (draft's own audit). Not a cohomology computation.
- `explorations/W173-brst-cohomology-mirror-sector-2026-07-14.md` builds a **free** BV bicomplex (`s²=0` on `Cl(9,5)=M(64,H)`), gets closed-not-exact, and names the sole demotion channel: the secondary constraint `C2 = 155.36`, which does not close without the **unbuilt `Y¹⁴` connection-curvature 2-form**.

**VERIFIED_REPO_DISCONNECT:** M-C3's own exit (b) has **no register ID, no owner, no cost line** anywhere in `lab/process/improvement-register-2026-08-03.md` (grep: zero BRST/quotient/cohomology rows). The exit the hostile review added as a required correction is not on the work queue.

### H5 — The Kramers wall at algebra level. [ONE sentence, review-licensed, signature-forked]
Verbatim licensed text: *on `ker Γ` the quaternionic (Kramers) wall persists at algebra level: the real commutant is `H`, so no antilinear involution can supply an extra sign.* This is the only new sentence the 2026-08-03 review licenses, and it licenses nothing verdict-adjacent.

Fence I must attach: this is **(9,5)-only**. Under (7,7) `= M(128,R)`, `J² = +1`, the wall is absent (firewall Round 2, direct computation), and register M-H4 flags that (7,7)'s `O(p,q)`-type Krein form "may FORCE the sign (9,5) leaves open." The pack lists SIGNATURE as UNDER-DETERMINED. So H5 is held **conditional on an unresolved fork**, and I will not export it unconditionally.

### H6 — The signed-readout boundary theorem. [CANON, internal tier, GU-independent]
`canon/signed-readout-boundary-theorem-RESULTS.md` · draft at `papers/drafts/signed-readout-boundary-theorem-2026-07-03.md`

(M) `R_w` monotone ⟺ every `w(x) ∈ G₊`; (P) provenance `Prov_w` monotone for **every** `w`; (C) the negative cone `w₋` is a **complete obstruction invariant** — monotone provenance and non-monotone readout coexist exactly when `w₋ ≠ 0`. (Z) finite Ginsparg-Wilson realization: `Q = −½Tr ε ∈ Z`, locally constant off codim-1, jumps `±1`. (K) H-linear ⇒ `index_C` even, `index_H = index_C/2` = the `KSp⁰(pt)=Z` augmentation. 22/22, exit 0. Unconditional on compact/finite; **OC1/OC2 labelled and undischarged** for the non-compact case, and the paper says the gap is *analytic, not topological*.

This is the one asset on the leg with a clean external shape (math-ph primary). Note its Part K is the same Kramers mechanism as H5, at index level — so H6 inherits the same (9,5)/(7,7) sensitivity wherever it is applied to GU.

### H7 — The Pontryagin / boundary-triple program. [two agreeing derivations, medium-high; program itself REFEREE_CONJECTURE]
`explorations/rankN-krein-tt-for-gu-2026-07-11.md`

At **finite rank** (Pontryagin `Π_κ`): every bounded operator is definitizable (Langer); PT-unbroken ⇒ quasi-Hermitian (Mostafazadeh) ⇒ standard TT closes — **and the ghost is removable, so keep-and-grade is trivial.** At **infinite rank / type III**: the implication fails (Krejčiřík–Siegl: bounded metric, *unbounded inverse*, quasi-similar not similar), and there is no definitizability theorem — no `η`-positive `Δ^{1/2}`, no `J`. Removability XOR: HORN Q (removable ghost, TT closes) vs HORN K (genuine ghost, TT open). Repo-native indication → HORN K (W52 `‖C‖→∞` at the exceptional locus; W53 places that locus at the free UV FP the AS trajectory approaches). Indication, not proof.

Consequence M-C3 correctly puts in its headline: `V` is a finite-rank `Π_832`-type carrier — exactly the regime rankN declares **non-transferable** and ghost-trivial. **Nothing on this leg's kinematic carrier transports to the interacting theory.** That fence is right and must survive into the envelope.

**VERIFIED_REPO_DISCONNECT:** register M-H10 records *zero repo citations* to the three generic-BVP papers (Bär–Ballmann arXiv:1101.1196; Bär–Bandara arXiv:1906.08581; non-compact boundary arXiv:2401.17784), grep-verified by the panel. The literature that constructs fields (iv)–(v) (Green form, common closed domain) generically has never been read into this repo.

---

## 2. DECISIVE-COMPUTATION QUEUE (ordered; cost; decidable now vs construction-blocked)

**DQ1 — Isotypic data of `η_V` under the maximal compact `Spin(9)×Spin(5)` inside the constraint-preserving algebra. DECIDABLE NOW. Cost S (hours–1 day).** `CHEAP_NEW_COMPUTATION`
The probe's C8 contrast run already shows the Dirac-sense algebra is REDUCIBLE (`commutant ⊇ span{I, ω_V}`). What is missing is the *number*: compute `(a_λ, b_λ, D_λ)` for the restriction of `η_V` to `V = ker Γ` under `Spin(9)×Spin(5)` and evaluate `Σ_λ dim_R(D_λ) a_λ b_λ`. This is the first time Theorem 2 would be applied to an algebra that is actually the observable algebra in the Dirac sense, on a group whose closure *is* compact so the hypotheses hold. Fixtures exist; the 1664-scale graph-commutant certificate runs in 64 s (`tests/observable-algebra/commutant_trichotomy_probe.py`). Highest value-per-cost item on the anchor. It settles the classification leg on the right object; it does **not** touch existence (Prop 1 still returns `∅` for any structure containing the boost).

**DQ2 — The (7,7) rerun of both legs. DECIDABLE NOW. Cost S.** `CHEAP_NEW_COMPUTATION` / fork-resolver
Under `Cl(7,7) = M(128,R)`: real commutant is `R·I` not `H·I`, and `J² = +1` makes antilinear `cJ` with `(cJ)² = +|c|² = 1` a *legitimate* involution candidate — so the "at most one" conclusion is not automatic and may genuinely change. Register M-H4 already asks for the (7,7) stabilizer commutant. The repo has a verified `Cl(7,7)` build (firewall Round 2) and the anchors are signature-identical (`bare 58.72`, `C2 155.36`, `ker Γ = 1664`), so this is a re-parameterisation, not a rebuild. Preregister the two outcomes before running: (i) larger candidate set ⇒ nonzero residual family ⇒ H5 and the "at most one" holding are (9,5) artifacts; (ii) `O(p,q)` structure forces the sign ⇒ M-H4's conjecture lands and the anchor gets its first *positive* result.

**DQ3 — Generalise the neutrality lemma off (9,5). DECIDABLE NOW. Cost XS–S.** `CHEAP_NEW_COMPUTATION`
The review's C4 already proves the signature structurally: `ωβ + βω = 0` ⇒ both chirality halves totally isotropic ⇒ neutral. State it as a signature-free lemma ("volume word anticommuting with the Krein involution and preserving the constraint surface ⇒ restricted form neutral"). Cheapest hardening available; makes Export E2 below signature-robust.

**DQ4 — W219's own next sub-goal: the representation type of the rolling mode `p(τ)`. DECIDABLE NOW (typing question). Cost S; M–L only on the nontrivial branch.** `VERIFIED_REPO_DISCONNECT`
W219 §6 specifies it completely: is `p` a singlet under `Sp(32,32;H)`, a field in the 91-dim spin-lift image, or a section-space variable the group does not act on? If singlet ⇒ its roll **cannot compactify** the noncompact group ⇒ isotropy stays the full noncompact arena ⇒ Prop 1 forbids an invariant positive majorant ⇒ **exit (a) closes negatively, cheaply**. W219 called this "the next hourly pickup" on 2026-07-14; three weeks later there is no follow-up artifact (only a passing mention in `explorations/council-dynamic-unity-assessment-2026-07-21.md`, which describes `p` as the record-count/conformal-scale mode with `N = e^{4p}` — i.e. scalar-shaped, i.e. *forecast singlet*). The cheapest decisive item on the anchor's own critical path is unclaimed.

**DQ5 — The physical-quotient / BRST construction on the record sector. CONSTRUCTION-BLOCKED, with a named blocker.** Cost: M–L *if* the blocker existed; unbounded to build the blocker.
Itemised, in order: (i) a nilpotent interacting GU BRST charge `Q` on the RS/`ker Γ` sector — W173 has only the **free** bicomplex; (ii) the state complex; (iii) `H⁰(Q)` genuinely computed — W207 does not do this; (iv) a nondegenerate **descended** pairing on `H⁰(Q)`; (v) the commutant of the physical algebra on `H⁰(Q)` and its positive-pairing classification — at which point Prop 1 / Thm 2 apply to the *quotient*, and the whole anchor becomes decidable. **The block is single and named:** `C2 = 155.36` does not close without the unbuilt `Y¹⁴` connection-curvature 2-form / distinguished null plane (W173). Everything downstream of (i) is gated on that one geometric object. Register action I would recommend (not taken here — read-only): open an ID for exit (b) with this five-step decomposition, since the hostile review made adding this exit a *required correction* and no queue item followed.

**DQ6 — M-H10 boundary-triple skeleton against the frozen (9,5)/RS symbol. PARTIALLY DECIDABLE (skeleton writeable now), DISCHARGE BLOCKED. Cost L.**
The generic half is literature work with zero repo cites. The GU-specific half is blocked by exactly the failure that sank the last attempt: `explorations/lp-lc-deficiency-decisive-2026-07-21.md` reached `LC-SELECTOR`, then on 2026-07-22 had its operator/domain/`w₁` conclusions **retracted to SOURCE-GAP** because it "did not construct the source-owned noncompact boundary operator or its minimal/maximal domains, Green form, trace space, and deck action." What survives from it is a real datum worth keeping: genuine ends enter `{q<0}`, where `K_S` is exactly null (`8.9e−16`) on the deficiency-relevant halves, so the `K`-definite spectral cut fails there. Any M-H10 skeleton must start by building the object whose absence caused the retraction.

**DQ7 — M-M7 `κ(M)` boundary Weyl function. BLOCKED on DQ6. Cost L. And it needs adjudication before write-up.** `REFEREE_CONJECTURE`
The register's framing is: `N_κ` vs Nevanlinna dichotomy = closed-completion vs firewall; the bulk type-III result blocks a bulk `Π_κ` but not the boundary, so "the wall becomes evidence FOR the hypothesis." **Flagged tension I could not resolve from the artifacts:** rankN's own result runs the other way at finite rank — *finite rank ⇒ definitizable ⇒ quasi-Hermitian ⇒ ghost REMOVABLE ⇒ keep-and-grade trivial* (HORN Q). So a **finite boundary `κ`** would, by the repo's own theorem transported to the boundary, make the boundary ghost removable — which is not obviously the firewall. These are not formally contradictory (bulk and boundary are different objects, as M-M7 says), but the inference "finite boundary `κ` ⇒ firewall" is supported by nothing in-repo and inherits an opposite-signed implication from the nearest theorem we own. Adjudicate before the conceptual write-up; otherwise M-M7 ships a sign error.

**Decidable now:** DQ1, DQ2, DQ3, DQ4 (all S or smaller, all on existing fixtures).
**Construction-blocked:** DQ5 (blocker named: `C2` / `Y¹⁴` curvature 2-form), DQ6 (blocker named: the source-owned noncompact boundary operator + domains), DQ7 (blocked on DQ6 *and* on an unadjudicated inference).

---

## 3. ENVELOPE EXPORTS (each as a constraint, with grade)

**E1 — "The physical state space is a quotient, not a subspace."**
Grade: **EARNED as a disjunction, not as a positive claim.** What is earned: on the kinematic carrier, positive-majorant selection of a *subspace* is unavailable (`F = ∅`, form- and algebra-independent). What is *not* earned: that the quotient exists — it is unbuilt at every step (DQ5). Export the constraint as: *any bigger structure must either supply a quotient (and then owns the `C2`/`Y¹⁴` construction) or abandon positive-majorant subspace selection.* Do not export "it is a quotient."

**E2 — "Positivity is boundary-supplied, not interior-supplied, for everything GU natively builds."**
Grade: **CANON-LEVEL BUT NARROWED TO TWO NAMED ESCAPES.** Source: `canon/good-stable-compactification-no-go-RESULTS.md` — the grading `Z` is a non-compact non-elliptic boost no Cartan involution commutes with; every native condensate is an extremal tensor component, and `W243`'s nilradical sits in its stabilizer, so the stabilizer always retains a non-compact parabolic. Surviving escapes: (i) GU-non-native NON-extremal charged vectors; (ii) denying Proposition 1. Standing caveat: the `Cl(9,5)=M(64,H)` rep-canonicity premise, which is falsification criterion 4 of the firewall hypothesis itself.
**Mandatory attachment:** M-C3's existence leg is a **dependent carrier-level instance of the same mechanism** and adds **no evidential weight** (review B4, and the no-go's own "one dependent proof chain" warning). Anyone counting these as two buttresses is double-counting.

**E3 — "The sign is unique given irreducibility — but irreducibility alone never forces it; compactness is the load-bearing antecedent."**
Grade: **EARNED, with the antecedent restored.** The register's original phrasing dropped "+ compact" and the review called it a governance-surface defect (A4). Correct form: *irreducible AND compact ⇒ sign forced; irreducible and non-compact ⇒ `F = ∅` and no sign is forced.* This run: irreducible, non-compact, `F = ∅`. Export the biconditional shape, never the short form.

**E4 — "Nothing on the finite-rank carrier transports to the interacting theory."**
Grade: **EARNED (theorem-backed, medium-high).** `V` is a `Π_832` Pontryagin-regime object; rankN proves that regime definitizable, ghost-removable, and explicitly non-transferable to the infinite-rank type-III region algebra. Any envelope claim that reads a finite-dimensional commutant as evidence about GU's interacting unitarity is refuted by our own artifact. This is the fence I would defend hardest, because it cuts against our side.

**E5 — "Whatever supplies positivity is not yet known to be the same boundary that carries the count."**
Grade: **REFEREE_CONJECTURE at best — currently UNTYPED, and that is the finding.** The 13-dim boundary (anchor #3), the firewall adapter, and the BVP boundary of M-H10/M-M7 are three objects with no in-repo identification. Anchor #3 independently reports that spin Dai-Freed inflow cannot carry `Z/3`. If the envelope wants one boundary doing both jobs, that identification is an unpaid bill, and I decline to export it.

**E6 — "The negative cone is a complete obstruction invariant for signed readout, unconditionally on compact spaces; the non-compact extension is analytic, not topological."**
Grade: **CANON, internal tier, GU-independent, unconditional in its stated tier.** Exportable as-is, including the OC1/OC2 fence. This is the anchor's cleanest external-facing export and the one least entangled with GU's open questions.

**E7 — "`F = ∅` is the normal covariant situation, so it is not a wall — and correspondingly it is not evidence."**
Grade: **EARNED (settled fork row + standard physics).** Symmetric obligation: because Gupta-Bleuler normality removes `F = ∅` as a wall, it equally removes it as a *result*. This anchor's `F = ∅` should be exported as a **type-correction**, not as a finding. The review's A8 is explicit that the "deny-Prop-1 / firewall branch" label is refuted: M-C3 does the opposite of denying Prop 1.

---

## 4. KILLS (what falsifies this anchor)

**K1 — The observable algebra is reducible with shared irreducible types.** Then `Σ_λ dim_R(D_λ) a_λ b_λ > 0`, the "at most one admissible fundamental symmetry" evaporates into a continuum (the draft's explicit `C_t = [[cosh 2t, −sinh 2t],[sinh 2t, −cosh 2t]] ⊗ I_r` family), and the anchor's classification leg dies. **This kill is already half-fired:** the constraint-preserving algebra IS reducible (`dim_C ≥ 2`). DQ1 decides whether the types are shared. Named as K2 in the panel's kill list. Cost to fire or clear: S.

**K2 — A compact dynamical stabilizer is exhibited.** If the derived good-stable reduction lands on a compact stabilizer containing the surviving structure, Prop 1's existence leg no longer returns `∅`, and Theorem 2 applied to *that* stabilizer governs — with isotypic data this anchor has never computed. Kills the `F = ∅` export outright. Forecast: unlikely (W213–W215 find no stationary good stable in built validity; W243 is unconditional for native builds; DQ4 likely returns "singlet ⇒ no compactification"). But it is the clean kill.

**K3 — (7,7) wins the signature fork.** `M(128,R)`, `J² = +1`: H5 has no content, Part K's evenness argument does not apply to GU, and the whole quaternionic leg restates. Register M-H4 additionally suggests (7,7) may *force* the sign — which would kill this anchor's "underdetermined" framing from the other side. Either way the anchor as currently stated does not survive intact. Cost to fire: DQ2, S.

**K4 — A closed completion supplies positivity internally.** Firewall falsification criterion 1. This anchor's E2 is a *scoped* no-go with two named escapes; a non-extremal charged construction, or an internal source action that closes without a boundary, ends the boundary-supply reading.

**K5 — `Cl(9,5) = M(64,H)` rep-canonicity fails.** Firewall criterion 4, "the single most likely way the hypothesis dies." Every number on this leg (`1664 = 13·128`, `(832,832)`, `H·I`, `Sp(32)×Sp(32)`) is downstream of it.

**K6 — The BRST quotient is built and `H⁰(Q)` carries a definite descended pairing.** Then positivity is interior-supplied after all, by the standard covariant route, and the boundary is not needed for *this* job. This is the kill I would most like someone to attempt, because it is the one the review specifically added and the queue specifically lacks.

**K7 — Self-kill, already fired and recorded:** the strong form ("parity proves an external object is necessary") is dead (firewall Round 2, direct (7,7) computation). The seat should not re-argue it.

---

## 5. COVERAGE

**Read in full:** the context pack; `explorations/observable-algebra-commutant-trichotomy-2026-08-03.md`; `lab/process/hostile-reviews/2026-08-03-trichotomy-review.md`; `explorations/W219-native-good-stable-stabilizer-input-gate-2026-07-14.md`; `papers/drafts/structurally-forced-internally-undecidable/draft-skeleton.md`; `papers/drafts/signed-readout-boundary-theorem-2026-07-03.md`; `explorations/rankN-krein-tt-for-gu-2026-07-11.md`; `canon/firewall-boundary-hypothesis.md`.
**Read in part:** `lab/process/improvement-register-2026-08-03.md` (Part B rows M-C1..M-M27, gates, queue); `lab/process/mathematician-panel-synthesis-2026-08-03.md` (U6/U10, kill list, tiers); `canon/good-stable-compactification-no-go-RESULTS.md`; `GEOMETER-VS-PHYSICS-OBJECTS.md` (fork rows :20, :21, :29); `RESEARCH-STATUS.md` (W211 row + Correction W211-01); headers of W207/W173/W132; `explorations/lp-lc-deficiency-decisive-2026-07-21.md` (verdict + retraction notice); `papers/drafts/open-krein-loop-unitarity-bistability/`; `tests/observable-algebra/commutant_trichotomy_probe.py` (structure only).

**Not run:** no scripts executed (read-only mandate). All numeric claims are quoted from artifacts and their machine receipts, not re-derived. Under P-H29 nothing here is a citable null or kill.

**Named gaps I could not close from the repo:**
1. Whether the three "boundary" objects are the same object (E5) — no artifact addresses it.
2. The M-M7 sign question (DQ7) — the register's inference and rankN's theorem point opposite ways at finite rank; no adjudicating artifact exists.
3. Whether DQ4 (`p`'s representation type) was ever attempted — only a descriptive mention found, no computation.
4. `κ` for the boundary has never been defined against a constructed operator; the one attempt retracted to SOURCE-GAP.

**Nothing in this report moves bar(b), H59, the count, canon, LANE-STATE, or any claim status.** The 2026-08-03 hostile review licenses nothing verdict-adjacent, and I inherit that fence. J5 remains the standing gate on any decisive deposit; the four decidable-now items above are all pre-deposit computations that would need their own hostile review before any bar moves.
