---
title: "Anchor council seat 4 — envelope (geometry/gravity/structure): the joint posit with counted surplus"
status: process
doc_type: council-seat-report
created: 2026-08-03
provenance: "Read-only council seat, anchor-and-triangulate campaign. Inputs: the context pack (post-correction) and seat reports 1–3, whose export grades were binding (no upgrades). Nothing executed; no verdict, bar, canon, count, H59, or LANE-STATE movement. Filed verbatim by the orchestrator. Adjudication: adjudication.md in this directory."
---

# COUNCIL SEAT 4 — ENVELOPE

## Layer-0 typing (envelope-level, done before assembly)

Inherited unchanged from the seats: `positivity` (three notions, seat1), `boundary` (three objects, UNCERTAIN, seat1 E5), `F` (homonym), `observable algebra` (three objects), `r` (homonym vs any DE coefficient, seat2), `128` (three-way, seat3), `(9,5)` split-vs-signature (fixed in pack, commit 68a3013).

New typings this seat must add because they span seats:

| Term | Type | Ruling |
|---|---|---|
| `N` | **HOMONYM across seats** | Seat2's `N` = past 4-volume in Planck units (`N_today ~ 1.2e243`), SAME-OBJECT across W146/W166/W185/W187/W215. Seat3's "count" = a `Z/3` torsion class in `π₁₃ˢ` plus an integer external index. The type wall runs **one way only**: `Hom(Z/3, Z) = 0` blocks torsion→integer, but `Z → Z/3` reduction is unobstructed. So the seat2/seat3 gap is an **unbuilt-construction gap, not a type wall**. Nothing in-repo constructs it. |
| `13` | **HOMONYM, unfenced** | (a) the RS multiplicity in `ker Γ = 13·128 = 1664`, signature-independent (`canon/firewall-boundary-hypothesis.md:227`); (b) the dimension of the link of the non-compact end of `Y¹⁴`; (c) the "13-dim receptacle" of anchor #3. No artifact relates (a) to (b)/(c). `VERIFIED_REPO_DISCONNECT`. |
| `RP³×S⁶` | **MODEL, not the object** | `canon/boundary-einvariant-and-the-tangential-fork.md:173` corrects: the true fiber link is a **9-dim S⁶-bundle over RP³**, not a product; `RP³` is the deformation-retract **spine** of the metric fiber, carried on the framed-bordism/J-homomorphism footing. Seat3 H4's "dim-13 link RP³×S⁶" is the working model. Fence travels. |
| `chirality` | **HOMONYM, load-bearing here** | (i) `Z2` parity of a condensate channel (W234/W237); (ii) net-chiral interior index (seat3 E2); (iii) 4D V−A / vectorlike (PH-K1); (iv) `ω`-chirality halves of `ker Γ`, both totally isotropic (seat1 H3). My §3(d) and §5 inferences are **conditional on (i)≡(ii)**, which no artifact establishes. |
| `record` (the W235 bit) | **UNCERTAIN vs seat2's records** | W235's "record vs redundancy" bit is a **records-interpretation** question, which seat2 §3.6 fences as **TaF-owned**. It is not established to be the same object as seat2's counting functional `N`. |

---

## 1. THE ENVELOPE POSIT

**One sentence.** *A completion of GU is: an SO(10)-class internal skeleton with forced hypercharge and an odd-form (Λ⁵/126) mass channel, living on a carrier whose net chirality is zero in the interior and whose odd count is therefore an integer index carried at a single edge object located at the non-compact end of Y¹⁴; that same edge supplies positivity — not as a positive-majorant subspace (unavailable, `F = ∅`, form- and algebra-independent) but either as a quotient it must then construct or by abandoning majorant selection; and the completion carries a monotone, causally-past-supported counting functional N on Y¹⁴ whose X⁴ shadow is a 4-volume, whose amplitude law and basin-coupling law are reciprocally locked, whose epoch is provably free, and which must supply ≈ +19.3 amplitude-marginalised shape χ² at low z on DESI DR2 that its own native monotonicity cannot currently produce.*

**The skeleton (seat3 E1, STRONG on group theory / ZERO on geometric origin).** `Y = T_3R + (B−L)/2` is forced — the naive B−L-alone hypercharge returns `n ∈ {−3,−1,1,3}` and fails. `dim Hom(S⁺⊗S⁺, Λ⁰) = 0` exactly and signature-independently (`p+q ≡ 2 mod 4`), so the mass channel is **forced odd-form**; the 126 sits at `k = 5` in `Λ⁵(V₄⊕V₁₀) = 2002 = C(14,5)`, Lorentz-singlet term `Λ⁰⊗Λ⁵ = 252 = 126 ⊕ 126̄`. Fences: the chain itself carries no surplus (textbook, hostile reading books SO(10) as imported); audit B16 / register P-M25 — the shipped PS-chain file is signature-stale and rooted at `Spin(7,7)`.

**The edge (seat3 E2, STRONG, with a live redirect).** Interior provably even / net-0 over the complete delimited class; any odd count is an external topological index, and that index is **any integer**. M-C4 is EXECUTED: the edge **cannot** be a spin Dai-Freed inflow wall, because the spin Dai-Freed ledger has no 3-primary column and `Hom(Z/3, 2-primary) = 0`. Redirect: framed or String; Pin removed. Surplus toward "3": **zero**. This is a no-go that locates.

**The positivity supply (seat1 E1/E2/E3, with every fence).** E1 is a **disjunction, not a positive claim**: any bigger structure must either supply a quotient (and then owns `C2 = 155.36` / the unbuilt `Y¹⁴` connection-curvature 2-form) or abandon positive-majorant subspace selection. E2 is canon-level but narrowed to two named escapes — (i) GU-non-native NON-extremal charged vectors, (ii) denying Proposition 1 — with the standing `Cl(9,5)=M(64,H)` rep-canonicity caveat, and with the **mandatory attachment** that M-C3's existence leg is a dependent carrier-level instance of the same mechanism and adds **no evidential weight**. E3 travels in biconditional form only: *irreducible AND compact ⇒ sign forced; irreducible and non-compact ⇒ `F = ∅` and no sign is forced.* E4 fences the whole leg: nothing on the finite-rank `Π_832` carrier transports to the interacting theory. E7: `F = ∅` is a type-correction, not a finding.

**The record functional (seat2 E2/E3/E4).** E2 is a **type** requirement, GRADE B, load-bearing only if the `c_kin = 0` axiom is granted. E3 is amplitude reciprocity `r(N)·Λ(N) = const` (W215 DS4c, 22/22) **with the rider attached**: `1/√N` forcing on Λ gives monotone withdrawal (W154 RE1 25/25, W158 RISEb — both FIRED), the **opposite** monotonicity from what the DESI shape residual requires. E4: phase/epoch is FREE and provably so (W160, 27/27) — no completion may be required to predict `z_x`, none credited for `z = 0.405`. E1 (GRADE A) is the envelope's only quantitative hook: ≈ +19.3 amplitude-marginalised shape χ² at low z, scope-guarded to the SIGNAL identification, not the family.

**The flavor-gravity coupling (seat3 E3, at its honest NEGATIVE surplus −4).** Included as stated: `REFEREE_CONJECTURE`, killer free parameter F_H1 (all three blocks are tensor products across both factors, so "meeting" does not pick the third; gamma-tracelessness does, with no hinge language). If it survives it improves by **removing free parameters**, −4 → ≈0, not → positive. Standing dead and not re-argued: hinge-as-locus (`II_s^H = 0`), hinge-as-meeting, hinge-as-inflow-interface, hinge-as-count, hinge ↔ 13-dim identification.

**Posit-internal assumptions (counted as free parameters, §2).** A1: the 13-dim count boundary ≡ the firewall/adapter boundary. A2: that object ≡ the analytic BVP boundary of M-H10/M-M7. Seat1 E5 declines both and I do not upgrade them. Circumstantial-only, explicitly **not** counted as evidence: `canon/firewall-boundary-hypothesis.md:40-44` calls the generation-sector external object "exactly a **candidate** for this boundary adapter" (candidacy, not identification); register M-M20 schedules "a minimal 13d defect action" **after** M-H10 — i.e. the queue already assumes A1∧A2. `VERIFIED_REPO_DISCONNECT`: the work queue assumes an identification no artifact establishes.

---

## 2. SURPLUS COUNT

Independent constraints, each with source export and grade. **seat1 E1 and E2 are merged into one row** — the seats flag that M-C3's existence leg is a dependent instance of the good-stable no-go's mechanism, so counting them separately is the double-count the charter forbids.

| # | Constraint | Source | Grade |
|---|---|---|---|
| C1 | Hypercharge forced: `Y = T_3R + (B−L)/2`, naive alternative fails | seat3 E1(i) | STRONG (exact rep theory) |
| C2 | Same-chirality scalar channel exactly 0 ⇒ mass channel forced **odd-form** | seat3 E1(ii) | STRONG, signature-independent |
| C3 | Interior even/net-0 ⇒ any odd count is an external integer index | seat3 E2 | STRONG |
| C4 | Edge cannot be a spin Dai-Freed wall; framed/String only | seat3 E2 (M-C4 EXECUTED) | STRONG no-go |
| C5 | Two breaking scales required (TeV vs ~10¹⁴ GeV, 11 decades) | seat3 E4 | HARD (arithmetic) |
| C6 | `M_PS ≥ 10¹³ GeV` floor | seat3 E5 | HARD |
| C7 | Positivity: quotient-or-abandon **and** boundary-supplied with exactly two named escapes | seat1 E1 ⊕ E2 (merged) | EARNED disjunction / CANON-narrowed |
| C8 | Compactness is the load-bearing antecedent (biconditional form only) | seat1 E3 | EARNED |
| C9 | Negative cone is a complete obstruction invariant for signed readout | seat1 E6 | CANON internal tier, GU-independent; applies to GU **only under A5** |
| C10 | ≈ +19.3 amplitude-marginalised shape χ² at low z on DESI DR2 | seat2 E1 | GRADE A |
| C11 | Record functional **type** requirement (monotone, causally-past-supported, X⁴ shadow = 4-volume, well-defined `N(z)`) | seat2 E2 | GRADE B, conditional on A6 |
| C12 | Amplitude reciprocity `r·Λ = const` | seat2 E3 | GRADE B, machine-checked (22/22) |

**Constraints: 12.**

Free parameters / posit-internal assumptions:

| # | Free parameter | Source |
|---|---|---|
| A1 | 13-dim count boundary ≡ firewall/adapter boundary | seat1 E5 declines; posit-internal |
| A2 | that object ≡ M-H10/M-M7 BVP boundary | seat1 E5 declines; posit-internal |
| A3⊕A4 | signature fork ⊕ carrier split — **merged, see §3(c)**; naive counting gives 2, the envelope earns the reduction to 1 | pack live forks |
| A5 | the record functional is a signed readout in seat1 E6's sense | required to apply C9 to GU |
| A6 | `c_kin = 0` | seat2 §1.4, W230 posit (REFEREE_CONJECTURE tension) |
| A7 | `N` normalisation: bulk 4-volume vs `N_conf = π√N_bulk` ("these give different laws and the choice is not free") | seat2 §2.1(i) |
| A8 | exponent `p` in `r ~ N^p` (only `dr/dN > 0` is native) | seat2 E5, GRADE C |
| A9 | DE amplitude normalization — GU has **no native absolute scale** (`PRED-NORM-RANK = RESOLVED_NO_GO`); `1/√N` is a ported Sorkin/ADGS import (W138 gate G5) | repo, cross-checked |
| A10 | imposter A/B | pack fork, seat3 Q1 |

**Free parameters: 10.**

**Netted pair (declared, not hidden):** seat2 E4 is entered as *both* a prohibition (may not predict `z_x`) *and* a certified freedom (phase/realization is free). Entering it once on each side and netting to zero is the honest treatment; it therefore appears in neither table.

**Three counts, all reported:**

- **Nominal (excluding seat3 E3):** 12 − 10 = **+3**.
- **With seat3 E3 at its honest −4:** 12 − 14 = **−1**.
- **Hostile / strong-tier only** (C1–C6, C10 count; C7–C9, C11, C12 are EARNED/GRADE-B/conditional and a referee discounts them): 7 − 10 = **−3**.

**Reading.** The envelope posit is informative only on the nominal count, only if seat3 E3 is excluded, and only if A1∧A2 are granted. It is **not** informative under a hostile count. That is the honest headline and the charter's "count, don't argue" applied to the envelope itself.

**Construction debts (neither constraint nor parameter; listed so they are not silently netted):** D1 — the quotient, unbuilt at all five steps, blocker named (`C2 = 155.36` / the `Y¹⁴` connection-curvature 2-form, seat1 DQ5). D2 — `DYNAMICAL-GOOD-STABLE-STABILIZER: NOT YET DEFINED` (W219, seat1 H2). D3 — the source-owned noncompact boundary operator, domains, Green form, trace space, deck action (seat1 DQ6; the object whose absence retracted `lp-lc-deficiency-decisive` to SOURCE-GAP). D4 — the dim-13 detector: `e`-invariant identically 0, M-M6 retyped (f-invariant lives on even stems, `α₁β₁` is odd-stem filtration 3), **the detector does not currently exist** (seat3 Q8).

---

## 3. COLLISION / REINFORCEMENT MATRIX

**(a) seat1 E5 vs seat3 E2 — can one boundary do positivity AND count?**
Status: **not established, and the queue assumes it anyway.** What it would take, itemised: (1) the count edge must be framed/String-capable (C4) and carry an integer index (C3); (2) the positivity boundary must be an analytic BVP boundary with a constructed operator, domains, Green form and trace space (D3); (3) an exhibited map identifying them. Genuine circumstantial convergence I can report at `REFEREE_CONJECTURE`: **both objects are located at the non-compact end of `Y¹⁴`.** The count object is the 13-dim link of that end (`canon/boundary-einvariant-and-the-tangential-fork.md:173`); the positivity object is the boundary reached by "genuine noncompact ends" of `F = GL(4,R)/O(3,1)` that cross `{q<0}` where `K_S` is exactly null (`explorations/lp-lc-deficiency-decisive-2026-07-21.md`); and M-M6 notes the f-invariant needs `(U,fr)²` corner data — "**the Y¹⁴ end has one naturally**." Two independent programs converging on one locus is suggestive and is **not** an identification. Counted as A1+A2, two free parameters, not as constraints. `VERIFIED_REPO_DISCONNECT`: M-M20 ("a minimal 13d defect action… after M-H10") schedules the identification's consequence without anyone having paid for it.

**(b) seat2 E3 reciprocity vs any DE-amplitude use elsewhere.**
Checked repo-wide. Every other DE-amplitude use is **downstream of the same law and inherits the rider**, and none is independent: W146's `Λ l_p² = 1/√N₄ = 1.39e-122`, W185's `10^243.7 → 10^122.4 → 10^60.9 → 10^30.5` tower — both graded in-repo as **RELABELS** of ported Sorkin/ADGS holographic counting (W138 gate G5); and the "dynamic transducer" core (`explorations/council-dynamic-unity-assessment-2026-07-21.md`) reads DE **sign and magnitude** off `N = e^{4p}` while its own assessment grades the DE amplitude a **PURE IMPORT** (`PRED-NORM-RANK = RESOLVED_NO_GO`). **Conclusion: there is no DE-amplitude use anywhere in the repo that escapes seat2 E3's wrong-monotonicity rider, and none that supplies an absolute scale.** Reinforcement, not collision — and it makes A9 unavoidable.

**(c) The (9,5)/(7,7) fork as it binds seat1 H5/E-items, seat3 Q2, and the carrier split.**
`CHEAP_NEW_COMPUTATION` (arithmetic performed here, trivially checkable): given fiber `(6,4)` in all live options — `(3,1)+(6,4) = (9,5)`; `(1,3)+(6,4) = (7,7)`; `(4,0)+(5,5) = (9,5)`. So **the ambient signature fork is the base-signature fork**, and the pack's author-declared third carrier option `TX^{1,3} ⊕ N^{6,4} ⊂ Y^{7,7}` is not a third independent fork — it is the `(7,7)` horn of the signature fork wearing carrier clothes. `REFEREE_CONJECTURE` consequence: `canon/boundary-einvariant…:174` states the `RP³` retract **requires the indefinite (Lorentzian) base**; a Riemannian base gives a contractible cone with no `RP³`. Therefore **C3∧C4 (the count edge exists) forbid the compact carrier `(4,0)+(5,5)`** — the side the finite census uses — collapsing the ternary carrier fork to a binary that is *the same binary* as the signature fork. That is the envelope's one earned parameter reduction (A3⊕A4 = 1, not 2). Downstream bindings, all unchanged in grade: seat1 H5 is `(9,5)`-only and not exported unconditionally; seat1 K3 fires on `(7,7)`; seat1 E6 Part K inherits the same sensitivity; seat3 Q2 must report **under both real forms** (the Euler-degree kill is explicitly "modulo the :24 grading fork"); P-M25's shipped PS chain is rooted at `Spin(7,7)`. Signature-**in**dependent and therefore safe across the fork: `ker Γ = 13·128 = 1664` (firewall canon:227), C2's scalar vanishing (`p+q ≡ 2 mod 4`), and seat1's neutrality lemma once DQ3 generalises it.

**(d) seat1 E1's quotient disjunction vs seat3 E2's external chirality — one edge object or two?**
As stated, **two different demands on possibly one object**, and the disjunction's two horns behave differently. The *abandon* horn is compatible with a single edge: positivity is boundary-supplied (C7) at the same non-compact end that carries the index (C3/C4), and nothing further is owed beyond A1∧A2. The *quotient* horn is **not** an edge object at all — it is a bulk cohomological construction (`H⁰(Q)` on the record sector), so taking it makes the count edge and the positivity mechanism **provably distinct objects**, A1∧A2 become vacuous, and the free-parameter count is unchanged only because two assumptions are replaced by construction debt D1. **Joint statement: the posit's "one edge" headline is available only on the abandon horn.** `REFEREE_CONJECTURE`.

Second, sharper collision on this cell, **cited by neither seat** — `VERIFIED_REPO_DISCONNECT`: `explorations/W237-channel-s-condensate-isotropy-2026-07-15.md` proves a structural theorem, **COMPACTIFY ⟺ Z2-ODD** on a null pair (`P = τ₁(null)` anticommutes with the grading `Z = τ₃(null)`; every Z2-even bilinear is a singlet or a boost; 44/44), with the verdict table: on the record-CONSERVED reading, channel D (Z2-odd, compactifying) is FORBIDDEN and channel S (Z2-even, chirality-safe) breaks 0 of 4096 non-compact generators ⇒ **no compactification, chirality KEPT, W224's singlet input-failure STANDS**; on the redundancy reading, compactification is delivered **only at the cost of chirality**. Two consequences for the envelope: (i) this is the *mechanism* behind C7 but **adds no independent evidential weight** — it is downstream of Proposition 1, exactly like M-C3's existence leg, and I decline to count it; (ii) it couples anchors #4 and #3 through a single bit. And **the bit is a record bit** (W235), which seat2 §3.6 fences as TaF-owned interpretation. Conditional on the `chirality` typing (i)≡(ii) being SAME-OBJECT, which no artifact establishes: seat3 E2 says the interior is already non-chiral, which would mean the redundancy row's "chirality KILLED" costs the interior nothing it has — i.e. seat3 E2 potentially **defuses** the objection to the unique native compactifier and thereby threatens seat1 K2/K4. That inference is `REFEREE_CONJECTURE` and is blocked at Layer-0 until `chirality` is typed.

**(e) seat2's N vs any record-count usage in seat3's boundary program.**
Grep-verified: the record-count corpus (W146, W149, W151, W154, W158, W163, W166, W185, W187, W215, W217) and the count/boundary corpus (`canon/external-by-structure-synthesis-RESULTS.md`, `explorations/inflow-no-three-primary-column-assembly-2026-08-03.md`, `canon/boundary-einvariant-and-the-tangential-fork.md`, M-H7/M-M6) share **zero citations in either direction**. `VERIFIED_REPO_DISCONNECT`. Layer-0 refinement that matters: the famous obstruction runs one way only — `Hom(Z/3, Z) = 0` blocks torsion→integer, but `Z → Z/3` reduction is unobstructed, so **the gap is unbuilt construction, not a type wall**. The only mod-3 structure banked anywhere is seat3's `3:9:1 = {3⁰,3¹,3²}` unique mod-3-clean even/even split at `n=14` — **fenced as multiplicity, not index**, and that fence forbids using it as the reduction target. Net: the two anchors' "counts" do not currently touch, and the envelope may not treat them as one object.

**Additional reinforcement found (f):** seat1's DQ4 (the representation type of the rolling mode `p`) and seat2's `N` are the **same variable** — `N = 4-volume = e^{4p}` (`explorations/council-dynamic-unity-assessment-2026-07-21.md:148`; W219:122-125). So the positivity anchor's cheapest decisive item is a typing question about the cosmology anchor's own record-count mode.

**Additional reinforcement found (g):** seat1 E6 part (C) — monotone provenance with non-monotone readout coexist exactly when the negative cone `w₋ ≠ 0` — is the general theorem whose shape **is** seat2's wrong-monotonicity problem (records accrete monotonically; the required DE residual rises). Under A5, `w₋ ≠ 0` is **necessary** for the rise. W158 RISEb is then a datum with a sharper meaning than seat2 gave it: the `q=5` grading `N_K = 9f₊ − 5f₋` *has* `w₋ ≠ 0` and still stayed monotone at every epoch — so `w₋ ≠ 0` is **not sufficient** under accretion dynamics, because accretion restricts the reachable set. E6's OC1/OC2 non-compact fence applies and is undischarged. `REFEREE_CONJECTURE`.

**Additional collision found (h):** seat1 E4 (nothing on the finite-rank carrier transports) bites one of seat3's holdings, not its exports: seat3 H3's Krein reading ("the imposter is the unique summand pairing like a spin-1/2 Dirac field") is a finite-rank `Π_832`-carrier statement and may not be cited as evidence about the interacting spectrum. Seat3's E1/E2 are index/rep-content statements and are **not** bitten.

---

## 4. BACK-PRESSURE (ranked; each tied to the seat's own queue item)

1. **seat1 DQ4 — read before you run.** `VERIFIED_REPO_DISCONNECT`: seat1 reports "no follow-up artifact"; `explorations/W224-native-good-stable-dynamical-vacuum-2026-07-15.md` (35/35) answers it — the only built vacuum candidate is an internal singlet, isotropy is the full non-compact `Sp(32,32;H)`, no good-stable grading supplied — **EXACT for the singlet-isotropy/Prop-1 consequence, SOURCE-AUDIT for the representation-type identification itself**. *Envelope reason:* DQ4 gates whether C7's escape (a) is closed; if the identification is upgraded from SOURCE-AUDIT, the envelope's positivity limb stops resting on a forecast.
2. **seat3 Q2 — chirality grading of the 128, under both real forms.** *Envelope reason:* it is the only item that pays two anchors at once — it fires or clears PH-K1 **and** it is the same `ω`-grading computation on the same builder (`tests/oq_rk1_cl95_explicit_rep.py`) that seat1 DQ2 needs for its `(7,7)` rerun. One run, two anchors. `CHEAP_NEW_COMPUTATION`.
3. **seat2 §2.1's XS inverse-problem feasibility check (before M-H13).** *Envelope reason:* C10 is the envelope's **only** quantitative hook (see §5, JP6); if no `N^p`-shaped deformation lies in the space that recovers +19.3, the envelope loses its single data-facing prediction cheaply rather than after an M-effort refit.
4. **seat2 §2.2 DE-12 (pipeline-unbiasedness positive control, ~30 lines).** *Envelope reason:* every number in C10 is downstream of the wave45/46/W129 chain; the envelope is staking its testability on an uncertified pipeline.
5. **seat1 DQ2 — the `(7,7)` rerun.** *Envelope reason:* per §3(c) this fork is *the same* fork as the carrier split, so resolving it retires the envelope's largest merged free parameter (A3⊕A4) and simultaneously decides whether H5/E6-Part-K have GU content.
6. **seat3 Q3 — `[σ(D_RS), P₃]`, the gate.** *Envelope reason:* it decides whether seat3 E3's −4 stays in the count at all; a first-order commutator amputates the flavor-gravity limb and moves the envelope from −1 to +3 by subtraction.
7. **seat3 Q5 — 16⊗144.** *Envelope reason:* if 126 ⊂ 16⊗144 the seesaw channel and the mediator channel are one channel, which — combined with C2's odd-form forcing — would pin the mass mechanism to `Λ⁵` specifically among odd forms. Preregister both outcomes.
8. **seat1 DQ1 — isotypic data on the Dirac-sense algebra.** *Envelope reason:* it decides the classification leg on the right object; note it does **not** touch C7 (the existence leg uses no property of `η`), so the envelope needs it for honesty, not for survival.
9. **seat2 §1.4 — does W230's spatial-gradient `L` map to the FLRW time-kinetic term?** *Envelope reason:* A6 gates C11 entirely; XS–S.
10. **seat1 DQ6/DQ7 and seat3 Q8's M-H7 half.** *Envelope reason:* A1∧A2 cannot be paid without D3 and D4; and DQ7 must be adjudicated before write-up or M-M7 ships a sign error (rankN's finite-rank theorem implies the opposite of the register's "finite boundary κ ⇒ firewall").

---

## 5. WHAT THE POSIT PREDICTS OR FORBIDS THAT NO SINGLE ANCHOR DOES

**JP1 — The posit FORBIDS the compact carrier `(4,0)+(5,5)`.** From C3∧C4 (a count edge exists at the link of the non-compact end) plus `canon/boundary-einvariant…:174` (the `RP³` spine requires an indefinite base; a Riemannian base gives a contractible cone with no `RP³`). No seat states this; it needs seat3's edge and a canon correction seat3 did not cite. **testable-now** (a reading of two existing artifacts plus the census's declared carrier). `REFEREE_CONJECTURE`.

**JP2 — The signature fork and the carrier fork are one fork, so the envelope has one fewer free parameter than naive counting gives.** Given fiber `(6,4)`: `(3,1)→(9,5)`, `(1,3)→(7,7)`, and the author-declared third carrier option is the `(7,7)` horn. **testable-now** (arithmetic, done above). `CHEAP_NEW_COMPUTATION`.

**JP3 — A single bit decides both positivity-internally-supplied and native chirality, and that bit is a *record* bit.** W237's COMPACTIFY ⟺ Z2-ODD theorem plus its verdict table: no completion gets both a native null-pair compactifier and native chirality. Joint prohibition no seat holds: **the completion may not have an internally-supplied positive majorant via a native null-pair condensate together with native chirality.** **construction-blocked** at dynamical grade (W235 forces only kinematic grade; the dynamical mirror-sector quartics, source action, physical state space and observable algebra are all OPEN) **and fence-blocked** (the bit is records-interpretation, TaF-owned per seat2 §3.6). Adds mechanism, **not** evidential weight, to C7. `REFEREE_CONJECTURE`.

**JP4 — The rise requires a nonempty negative cone AND a reachability mechanism; a fixed-signed-weight additive record functional cannot produce it.** From seat1 E6(C) (under A5) `w₋ ≠ 0` is necessary; from W158 RISEb (`N_K = 9f₊ − 5f₋`, monotone at every epoch) it is not sufficient under monotone accretion. Neither seat states the conjunction. **testable-now** as an extension of seat2's XS inverse-problem check: ask not only whether some `N^p` deformation recovers +19.3, but whether **any** signed readout with `w₋ ≠ 0` does, under accretion-restricted reachability. Fences: E6's OC1/OC2 non-compact gap is undischarged and the record sector is non-compact. `REFEREE_CONJECTURE` / `CHEAP_NEW_COMPUTATION`.

**JP5 — The posit predicts NO absolute number anywhere; it is a ratio-and-shape structure.** C5∧C6 demand two scales; A9 records that GU has no native absolute scale (`PRED-NORM-RANK = RESOLVED_NO_GO`) and that the DE amplitude is a pure import; seat2 E4 forbids epoch credit; the `μ_DW` fork records that the geometry fixes only dimensionless ratios. **Joint consequence: `C10`'s +19.3 shape χ² is the envelope's single quantitative hook, and it is a *shape*, not a magnitude.** Corollary prohibition: any completion claiming a derived absolute scale, a derived DE magnitude, or a derived crossing epoch is over-claiming against the envelope, not merely against one seat. **testable-now** (bookkeeping over existing grades).

**JP6 — The "one edge" headline is available only on the abandon horn of C7.** §3(d). Taking the quotient horn makes the count edge and the positivity mechanism provably distinct objects and converts A1∧A2 into construction debt D1. **construction-blocked** (D1's blocker is single and named: `C2 = 155.36` does not close without the unbuilt `Y¹⁴` connection-curvature 2-form).

**JP7 — Clock-gated joint exposure.** Two external clocks bear on the envelope and neither is agent-executable: W220 FC-d (`w_a/(w₀+1) = −3.468` vs kill-line `−3.5`, **margin +0.032**; DESI DR3/Euclid 2027) and the neutrino ordering (JUNO/NOvA/T2K 2026–28; JUNO's first 59.1-day results leave the ordering below 3σ, `p(IO) ≈ 2–2.6%`). **The DESI clock does not tick before 2027.** **clock-gated.**

---

## 6. KILLS FOR THE ENVELOPE POSIT ITSELF

**Collapses the posit (single-anchor failure sufficient):**

- **EK1 — seat3 E2's interior-even theorem fails** (the delimited class turns out incomplete). Then chirality can be interior, no edge is needed for the count, A1∧A2 are moot, C3/C4 vanish, and the entire "external edge" spine goes. The envelope reduces to a signature-forked internal skeleton with an unresolved positivity disjunction. **This is the single anchor failure the posit does not survive.**
- **EK2 — a closed completion supplies positivity internally** (seat1 K4 / firewall falsification criterion 1; either escape (i) non-extremal charged vectors, or escape (ii)). Combined with EK1's availability this ends the boundary-centric reading; the posit's distinctive content is exactly the boundary.
- **EK3 — A1∧A2 are refuted** (the three boundary objects are shown genuinely distinct). The posit survives only as a **disjunction of two or three completions**, its "one coherent structural picture" claim dies, and its nominal surplus falls from +3 to ≤ +1 (one or two assumptions become construction debts with no compensating constraint).

**Survives, with amputation or with improvement:**

- **`Cl(9,5) = M(64,H)` rep-canonicity fails** (seat1 K5). Kills seat1's numeric leg and the imposter-block details, but C1, C2 (signature-independent), C3, C5, C6, C10 stand, and `ker Γ = 1664` is signature-independent. **Survives, positivity limb amputated.**
- **DQ1 returns shared irreducible types** (seat1 K1, already half-fired). Kills the classification leg; C7 rests on the existence leg (which uses no property of `η`) plus the good-stable no-go. **Survives.**
- **`(7,7)` wins the fork** (seat1 K3). H5 empties, Part K's evenness argument does not apply to GU — but per JP2 this **retires A3⊕A4** and fixes the carrier. **Survives and the surplus improves.**
- **PH-K1 fires (imposter vectorlike)** — seat3's own sharpest cheap kill, pointed at seat3's holding. Seat3 E3's −4 becomes moot. **Survives; nominal surplus improves from −1 to +3 by subtraction.** Note the asymmetry worth stating plainly: **the two cheapest kills on the whole board (seat3 Q2, seat1 DQ2) both improve the envelope's counted surplus**, which is exactly why the envelope should be pushing for them rather than defending against them.
- **`c_kin = 0` incompatible with the FLRW kinetic term** (seat2 §1.4). A6 fails, C11 detaches, the record↔DE limb is severed, and seat2 contributes only E1 and E4. **Survives with the record functional amputated — and JP5 then leaves the envelope with a shape constraint and no record mechanism to satisfy it.**
- **Neutrino ordering flips to inverted** (seat3 seesaw K3, clock-gated). The M_R band dies as written; C6's floor is structural (Majorana VEV ≤ PS scale) and stands. **Survives.**
- **C10 is retracted or DE-12 fails.** The structure survives intact but loses its only quantitative hook (JP5). **Survives as unfalsifiable-by-current-data** — which is the outcome the envelope should least want and can buy off for XS cost.
- **Seesaw K4 (126-Yukawa contamination of the third-generation Dirac mass, an O(1) effect from the very multiplet supplying M_R).** Not costed into seat3's surplus and, per seat3, the first thing a hostile referee reaches for. It does not touch C2's odd-form forcing. **Survives; the seesaw number does not.**

---

## Coverage note

**Read in full, in the mandated order:** the context pack (anchor #1 correction noted and applied); seat1-quantum-foundations.md; seat2-cosmology.md; seat3-particle-flavor.md.

**Consulted to resolve the specific collision questions (targeted read/grep only):** `GEOMETER-VS-PHYSICS-OBJECTS.md` (fork rows: carrier split, ghost clearance, gravity functional, `μ_DW`); `canon/firewall-boundary-hypothesis.md` (the "candidate for this boundary adapter" sentence; the `{2,7,13}`-smooth / `ker Γ` signature-independence line); `canon/boundary-einvariant-and-the-tangential-fork.md` §7 (the RP³-is-the-spine correction and the indefinite-base requirement — the load-bearing citation for JP1/JP2); `explorations/inflow-no-three-primary-column-assembly-2026-08-03.md`; `explorations/lp-lc-deficiency-decisive-2026-07-21.md`; `explorations/W219-native-good-stable-stabilizer-input-gate-2026-07-14.md` §6; `explorations/W224-native-good-stable-dynamical-vacuum-2026-07-15.md` (front-matter/grade only); `explorations/W237-channel-s-condensate-isotropy-2026-07-15.md` (front-matter + §3–§4 verdict table); `explorations/council-dynamic-unity-assessment-2026-07-21.md` (the `N = e^{4p}` identification and the DE-amplitude import grade); `lab/process/improvement-register-2026-08-03.md` (rows M-C4, M-H3, M-H4, M-H7, M-H10, M-H12, M-H13, M-M4, M-M6, M-M7, M-M19, M-M20, M-M22, M-M23, M-M24, P-M25, gates J2/J5, Revision 1 notes).

**Not read, and bounding this report:** `W224` and `W237` bodies beyond front-matter and the verdict table (both are load-bearing for §3(d) and JP3, and I have stated them at front-matter grade only — the `chirality` Layer-0 typing that would let JP3's second half be asserted is not done anywhere and I have not done it); `W234`, `W231`, `W235` (cited only through W237's summaries); the 2021 draft (gitignored); no seat's cited artifact was re-derived.

**Discipline:** nothing executed; no scripts run; no file created or modified. No export was upgraded; every fence attached by a seat travels with the export it belongs to — including seat1 E2's mandatory no-double-count attachment, which I applied both to M-C3 (as instructed) **and**, on my own reading, to W224/W237, since those are also downstream of Proposition 1. Every new cross-connection I introduce (§3 f/g/h, JP1–JP4, JP6) is labelled `REFEREE_CONJECTURE` or `CHEAP_NEW_COMPUTATION` and is counted as posit-internal, never as an export. Under P-H29 nothing here is a citable null or kill. **Nothing in this report moves any verdict, bar, canon, count, H59, or LANE-STATE.**
