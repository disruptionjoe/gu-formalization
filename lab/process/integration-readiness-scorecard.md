---
title: "Integration Readiness Scorecard"
status: living_surface
doc_type: channel_readiness_ledger
created: 2026-07-19
updated: 2026-07-19T22:30:00-05:00
owner_item: CONSTRUCTION-SPACE-EXPLORATION
axiom: lab/process/boundary-adapter-standing-axiom.md
update_rule: "Each channel updates its row when evidence moves; integration begins only when every row's three questions reach YES and the payload consistency check passes."
integration_note: "2026-07-19 evening: all seven fanout swings returned and are integrated below. Channel swing docs: explorations/channel-swing-CH-*-2026-07-19.md; probes in tests/channel-swings/ (all verified exit 0 by the parent)."
---

# Integration Readiness Scorecard

**Q1** — construction exists that fits GU (or the greater GU-type class)?
**Q2** — leg's parameter boundaries understood?
**Q3** — adapter range/parameters understood?

## CH-GR

- **Q1: YES (conditional).** Branch-(a) curvature-locked VEV computes to an
  EXACT cancellation identity (Q^TF = -[t2]^TF on box(h)=0; sigma*kappa^2=1
  with kappa^2=1; hard Z/2 sign gate — wrong sign has no real solution;
  Kerr-robust). Conditions: K2 gauge covariance (theorem-vs-artifact, the
  sharpest open threat), action-level derivation, nonlinear extension.
- **Q2: YES (strengthened).** Branch space computed 3->1; K3 kernel purity
  (scale-free only, per exact C2 law); K5 two-sided condition (must FAIL on
  matter backgrounds or it is Einstein-in-disguise).
- **Q3: YES (candidate freeze).** One Z/2 (sigma=+1, value known) + one pure
  number (kappa^2=1, value known, provenance pending) + action-level
  structure (DEM-GR-3) + B.5 pass-throughs (DEM-GR-4). "Branch selection"
  DELETED as a payload item — forced by computation.

## CH-QM

- **Q1: YES (finite-fixture strength).** 9-dim toy built and run: right
  orientation passes all six certificates; wrong orientation fails
  COHOMOLOGICALLY first with the VZ zombie mode on repair; locally inert
  (Q4 structural). Gap: lift to the 192-dim triplet with a real d_RS,-1 +
  compensator.
- **Q2: YES (exercised).** The checklist is now a runnable battery.
- **Q3: YES (sharpened, scope narrowed).** One Z/2, holonomy-stored,
  non-retuning structural; transmitted as the action-level Noether identity
  (not the bare differential); typed J_quat-COMMUTING in (9,5) — it fixes
  the Krein sign (+arrow) and provably does NOT carry generations.

## CH-COSMO

- **Q1: PARTIAL (was NO).** Candidate projector constructed, GU-native in
  type (fiber Frobenius), automatically helicity-0; gauge-covariant with
  exactly ONE slicing residue; vector/tensor residues discharge by Schur.
  Remaining: the multi-scalar block diagonalization + Z_theta>0 —
  C10-action-dependent.
- **Q2: PARTIAL (strengthened).** Construction-side boundaries itemized;
  data-side bounds live (F1 margin +1.11).
- **Q3: STRONG PARTIAL.** The absolute scale is an empirical POINT:
  rho_DE^(1/4) ~ 2.24 meV (~1e-123 M_Pl^4, <10%); evolving split one-sided
  f0 <= 0.027 canonical. Sign OPEN and conditionally an indirect RELATIONAL
  orientation observable — the only candidate global readout channel for
  payload item 1 (global w(z) does not violate local unreadability).

## CH-SM

- **Q1: PARTIAL (strengthened).** All 16 Spin(10)->G_SM chains enumerated:
  R0-EQUIVALENT (exact SM content per 16, all anomalies zero, chirality
  genuine, hypercharge normalization BOUGHT: sin^2=3/8 boundary; ONE
  physical Y-line mod Weyl). Selector still unexhibited.
- **Q2: YES.** Sharp list + alignment rigidity + varying-constants tripwire
  with named bound classes; R1 classes (proton decay, n-nbar) staged.
- **Q3: YES (conditional) — and possibly VANISHING.** Metric-type VEV forces
  Spin(10)->PS (Sym^2(10)=1+54 IS the metric slot); range collapses 16->6
  conditional on H-GR'; rank drop provably spinorial (nu_c outside all
  tensor irreps; 126 route emits matter parity Z/2). Real form: only (6,4)
  survives — GU's own trace reversal lands on the unique survivor
  (retrodiction-grade). Residual free content ~ zero: the chain datum may
  be DERIVABLE, deleting payload item 4.

## CH-REC

- **Q1: PARTIAL (was OPEN).** Co-flip holds at zero import in the finite
  inventory; decoupling costs exactly one extra Z/2 (accounting identity);
  arrow lives in record-law + orientation, not the propagator. CH-SRC
  discharged the EXISTENCE half of T3 (a C_0-member action exists with
  record direction = tau, no free sign slot).
- **Q2: YES.** Boundary exactly characterized.
- **Q3: YES (conditional on T3-on-W229).** Zero new payload items; the
  N->5 trigger is exactly: W229's record direction not Noether-derivable.

## CH-SRC (build channel; ledger: lab/process/source-action-build-ledger.md)

- Skeleton EXISTS at toy grade: B.1-B.4 all exhibited (master equation;
  constraint = gauge invariance via Noether; compensator necessary;
  cohomological with the VZ decoupling trap disqualified). C2 law exact
  (2.000000000000). SRC-COH-1: one Krein form in every slot (relative sign
  = hidden import). 20 constraints frozen, NO computed conflict. Honest
  residual: the C2 analog persists at full norm — closure requires the B.5
  global data (the p2c object). Freeze condition met on first branch.

## Signature competition (CH-SIG-95 vs CH-SIG-77)

- **(7,7) port verdict: CLOSE-PATH, no cheap kill.** QM toy survives
  verbatim (bit retyped: J'-sector-exchanging — natively the "discharging
  world" structure); GR identity survives EXACTLY; COSMO projector
  survives; fiber (6,4)/PS chain ports untouched; (7,7) is
  TRANSCRIPT-COMPATIBLE (same trace-reversal path, opposite X4 sign
  convention). Odd index TYPE-CONSISTENT (Kramers wall gone) but
  reachable-not-forced: the rank/count integer import survives.
- **Debits (7,7):** shiab selector family doubles (quaternionic cut
  collapses); ind_H "/8" arithmetic, eta=0 mechanism (its particle-hole
  symmetry IS J_quat — possibly fatal to that proof), and Pin/KO 2-primary
  all NEED-RECOMPUTE. Losing the wall loses the wall's work.
- **Krein Gram fork in (7,7):** canonical Gram ANTICOMMUTES with J'
  (forced scalar i, J'-odd); chirality-twisted Gram commutes. The weld
  must pin one (new named choice: GRAM-PIN-77).
- **Payload tally:** N core heading to 3 in BOTH signatures (orientation +
  scale + rank/count integer), with the chain item conditionally derivable
  and the branch item computed away. The live difference is TYPING and
  proof-debt, not count.

## Overall: the decisive computations (ranked)

1. **K2 (CH-GR):** gauge covariance of the cancellation identity —
   theorem-vs-artifact for the night's strongest result.
2. **(7,7) real-index generation arithmetic (CH-SIG-77):** rebuild the
   24/8=3 packaging with the real/KO divisor for M(128,R). Outcomes:
   forced-3 (jackpot) / free-integer (tie) / real-class blocks 3 (the
   fork's only remaining cheap kill).
3. **T3-on-W229 (CH-REC/CH-SRC):** is W229's record direction
   Noether-derived? The N<=4 vs N=5 decider in (9,5).
4. **54-direction weld (CH-SIG-95, g2):** does CH-GR's curvature-locked
   branch overlap Sym^2's 54 as CH-SM's conditioning requires?
5. **Rank-pin fold (CH-SM/CH-QM):** can the generation integer fold into
   existing finite data rather than standing as its own item?

Integration proper begins when K2 and the signature comparison resolve;
the welded-payload consistency check and the P0 endings govern as before.
