---
title: "Hostile review: K94 centralizer-13 global compatibility census"
status: complete
reviewed_artifact: explorations/conditional-build/selected-k94-rsap-centralizer13-global-compatibility-census-2026-08-15.md
created: "2026-08-15"
verdict: PASS_CENTRALIZER13_GLOBAL_COMPATIBILITY__ALL_HIGHER_LAYERS_OPEN
---

# Hostile review

## Mechanism omission attack

Total centralizer excess six has only the positive partitions `6`, `4+2` and
`2+2+2`. Separating whether zero owns `6`, `4`, `2` or none produces exactly
the seven recorded mechanisms. Zero occurs at most once; nonzero primary
types form unordered multisets. There is no eighth owner pattern.

## Counting-convention drift attack

The generic dynamic program is calibrated against the two independent
predecessor censuses. With the same nonsemisimple filter it returns exactly
K91's `714` rows and K92's `673` rows. Fully semisimple rows and pure-zero rows
are counted separately and routed to K88 and K89 rather than mixed into the
new `645`.

## Local-to-global promotion attack

Unlike K93, K94 performs the global signature convolution. Each passing row
contains `(3,4)` or `(4,3)` in the sum of its locally optimal plus-signature
sets. The direct-sum involution is therefore simultaneous, balanced and still
half-excess optimal.

## Structural types mistaken for connected orbits attack

The `645` are finite signed structural configurations, not a claimed count of
connected `SO_0(7,7)` orbits. K89 closed that refinement only for the pure
nilpotent cone. K94 keeps the remaining connected-orbit seam open.

## One positive layer mistaken for surjectivity attack

Centralizer dimensions at least `15` remain unchecked. A single missed orbit
there can still defeat the zero-neighborhood horn. No zero neighborhood,
surjectivity or RSAP claim is licensed.

## Verdict

Accept centralizer-`13` global balanced compatibility and the sharp
`13/78/3/88` schedule. Advance the exact multiset program to all higher
centralizers; do not promote this result beyond signed structural grade.
