---
title: "Hostile review: PW2F-R2B2B2H4 source/active real-form scope exit"
status: complete_after_scope
doc_type: hostile_review
updated_at: "2026-08-04"
---

# Hostile review: PW2F-R2B2B2H4

## Reviewed claim

The literal source-directed/Curt-rival `(7,7)` real comparator cannot be identified directly
with the active `(9,5)` right-H real form. The published source does not supply
the global data needed to evaluate the surviving two-stage moving-J port, so
source attribution exits evidence scope while independent construction remains
open. Only separate conditional-active C4 banks become eligible downstream.

## Real-form and Clifford review

Pass. Inertia mismatch blocks a real metric isometry. The square-`+1` versus
square-`-1` reality relation makes every real intertwiner zero, and the exact
finite linear system independently confirms it. `M(128,R)` and `M(64,H)` have
the same real algebra dimension but inequivalent minimal real modules.
Common `M(128,C)` complexification is retained and explicitly prevented from
masquerading as a real/right-H equivalence.

Lens verdict: `PASS`.

## Bundle topology and source review

Pass after scope restriction. The first draft overread absent source topology
as a global nonexistence result. That is not earned. PW1 provides a legitimate
two-stage construction problem: a global `G/H` section followed by an active
`P_H`-to-`P_nat` identification. The source packet supplies neither stage's
global transition/classifying data and explicitly leaves the real-form map to
construction. The correct verdict is `EVIDENCE_SCOPE_EXIT`, while global
existence remains undecided.

Lens verdict: `PASS_AFTER_SCOPE`.

## Variational and bank-admission review

Pass. Closing the source-attribution gate does not build a coefficient bank.
The downstream admission is limited to two distinct conditional-active banks:
`I1 A4` and `I2B C4`. They retain separate action ownership, cannot be merged,
and cannot be called source-derived. Multi-index Green/Helmholtz and
projective `kappa1` remain blocked until the banks are actually assembled.

Lens verdict: `PASS`.

## Rival-track and promotion review

Pass. Literal real-form non-equivalence strengthens only the partial TG-1
evidence already recorded. The source convention is still unresolved, Curt
has no separate complete action/observation packet, and there is no same-domain
no-refit discriminator. Curt therefore remains formally separate inside the
Eric lane and the conjunctive third-lane gate remains not promoted.

Lens verdict: `PASS`.

## Disposition

Accepted only as
`PUBLIC_SOURCE_TO_ACTIVE_REAL_FORM_PORT_EVIDENCE_SCOPE_EXIT__INDEPENDENT_MOVING_J_PORT_OPEN__SEPARATE_CONDITIONAL_ACTIVE_BANKS_ADMITTED`.

P1/P2/P3 remain unchanged and unused. No global nonexistence theorem, bank,
canon, protected claim, public posture, publication, or physics status is
claimed.
