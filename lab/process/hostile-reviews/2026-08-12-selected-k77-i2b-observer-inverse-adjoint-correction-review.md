---
title: "Hostile review: observer inverse-adjoint correction"
status: hostile-review
created: 2026-08-12
---

# Hostile review: observer inverse-adjoint correction

## Charge 1 — summary outruns artifact

The correction proves only a finite fixed-field response change.  It cannot
be summarized as a physical observer equation or a dynamical selection.
Likewise RB4's exact moving family and `SO(3)` stabilizer do not mean the action
selects `u`; RB5 explicitly blocks descent from the coarse epsilon plane.

## Charge 2 — rigor defending a superseded object

The v0.216 hostile review correctly named the inverse-adjoint rule but its
probe still reused v0.215's wrong involutive boosted control.  That mismatch
let the prose fence outrun its test.  Separately, proposing another minimal
`SO(3)` construction defended an already-built RB4 object while missing the
RB5 coarse-flag obstruction and RB6/RB7 action-selection results.

## Charge 3 — downstream disposition

- **dissolved:** the old fixed-field block list; the claim that the spatial
  blocks rather than the timelike block move; another moving-`u`/`SO(3)` build
  as the next gate.
- **survives:** exact diagonal naturality; failed vertical basicness; coarse
  observation nonselection; refined-flag possibility; coupled contact;
  current-action dynamic selection and analytic/BV/domain burdens.
- **needs-recheck:** the full `SC-ACT-04` constrained-`u` variation, its Ward
  identity under any refined flag, and whether the observer is auxiliary,
  gauge, propagating or external.

Required Layer-0, prior-art, Clifford/Krein, variational, symplectic and
analytic lenses all agree on the scoped correction.  The dissent is that
RB6/RB7 may already kill dynamic selection; rejected at Layer 0 because they
test W177 and old finite action-shaped parents, not the current `SC-ACT-04`
observer completion.

## Verdict

```text
CORRECTION_REQUIRED_AND_PASSED__V0216_OWNERSHIP_VERDICT_SURVIVES__DUPLICATE_SO3_SUCCESSOR_RETIRED__CURRENT_SC_ACT_04_U_EULER_IS_NEXT
```

No ledger verdict, canon verdict or public posture changes.
