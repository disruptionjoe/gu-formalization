---
artifact_type: hostile_post_review
created: 2026-08-05
gate: FULL_DOMAIN_SHIAB_ACTION_ADAPTER_PLUS_OBSERVED_EQUATION_RECEIVER
status: PASS_AFTER_MATERIAL_SCOPE_AND_CERTIFICATE_REPAIRS
review_lenses:
  - summary_outruns_artifact
  - defended_superseded_object
  - source_layer0_attribution
---

# Hostile review: candidate-Shiab Riemann factor route and observed receiver

## Final verdict

**HOSTILE POST-REVIEW: PASS AFTER MATERIAL SCOPE AND CERTIFICATE REPAIRS.**

The rank-ten kernel theorem and the narrowly scoped route disposition survive
three independent reviews. The local Gauss formula survives as a construction
target, not as a completed source-action Euler theorem.

Final reruns:

- main SymPy certificate: `48 exact + 3 source + 3 repo + 11 type + 8 planted = 73 PASS`;
- independent Sage/QQ reconstruction: rank-ten kernel, nonzero Riemann
  projector/rank count, and Gauss variation pass;
- conditional ledger v0.2: `15 exact + 2 provenance + 2 type + 3 planted = 22 PASS`.

## Lens 1: summary outruns artifact

The first draft failed this lens in six places.

1. Its generic projector fixture double-antisymmetrized to zero. The repaired
   fixture is nonzero. The certificate now also proves the exact carrier count:
   `dim Sym^2(Lambda^2 E*) = 4186`, four-form/Bianchi kernel `1001`, and
   Riemann-projector rank `3185`; Sage independently replays the nonzero
   projector and dimensions.
2. Vanishing ambient Einstein response was initially allowed to stand in for
   vanishing complete Shiab output. The repair imports and regression-checks
   the predecessor's complete-Riemann theorem
   `S_candidate I_Riem = -2 J G_14`, so the `Cl^5` ambiguity is closed on all
   ten kernel witnesses.
3. “Smallest” was unsupported. The report now claims only one parameter-free
   local surviving construction.
4. Stabilizer naturality and first variation were described too broadly. The
   formula is tensorial on a fixed orthogonal `H + N_s` fixture; four derivative
   slots and a two-input/ten-output cotangent pairing are replayed exactly.
   Moving-section/global IG descent remains open.
5. Metric-normal `N_s` was conflated with canonical vertical `V_s`. The report
   now states they coincide only on the fixed orthogonal fixture and names the
   section-dependent transition as unbuilt.
6. Generic `Cl^5` and normal residue were called retained without construction.
   They are now explicitly open blocks required of the next total receiver.

No summary/artifact mismatch remains at the declared grade.

## Lens 2: defended superseded object

The first draft called `comm/symi/symi` an ambient action operator. That was a
stale-object error: principal Bianchi selects it only as a candidate member of
the displayed family, while action/Helmholtz selection rank remains zero.

The repaired packet consistently calls it the Bianchi-selected
displayed-family candidate. It does not rebuild or defend:

- the printed endpoint as the live action Euler;
- a second localized copy of `I1B`;
- a horizontal-only receiver in place of the exact four-plus-ten field map and
  inverse-transpose equation dual;
- direct pullback of the degree-13 Euler row;
- `D_B E_act` or `Xi` as the full Noether totalization;
- a guessed `Cl^5`/normal quotient.

The one-bulk-action architecture, `M^-T` convention, retained normal-jet debt,
complete Noether owner list and formal-versus-analytic adjoint boundary remain
intact. This lens passes.

## Lens 3: source attribution and Layer 0

The first draft incorrectly counted two repository results as source checks and
used `SOURCE-CORRECTS` for conclusions actually supplied by mathematical
typing. Repairs now distinguish:

- `SOURCE-CONFIRMS`: upstairs action, augmented torsion, section/pullback
  grammar;
- `TYPE/REPO-CORRECTS`: direct form pullback versus equation/density receiver,
  and fibre metric versus Einstein trace;
- `SOURCE+REPO-LAYER0-CORRECTS`: written adjoint Shiab versus later spinor
  reconstruction;
- `SOURCE-SILENT`: faithful observed Einstein receiver and Gauss/`II` as the
  complete K77 Euler map;
- `REPO-THEOREM`: complete candidate Riemann response and live action Euler
  superseding the printed endpoint.

The new rank-ten theorem is not attributed to Weinstein or Curt. Source
guidance motivates the diagram; it does not certify commutation. This lens
passes.

## Ledger adjudication

The initial v0.2 draft incorrectly tightened `LT-GR1`. That row is now exactly
unchanged from v0.1 because the local Gauss tensor does not decide the
`|II|^2` versus mean-curvature-norm functional horn.

Only `LT-GR1b` migrates. It carries an explicit construction scope:

```text
BIANCHI_SELECTED_CANDIDATE_POST_SHIAB_RIEMANN_RESPONSE_FACTORIZATION_ONLY
```

Within that scope, `OVER_DETERMINED/GENUINE_FALSIFICATION` is earned: the
candidate's complete Riemann kernel maps onto all ten restriction-first
Einstein components. It is not a falsification of GU, K77, the complete
`E_act`, observed GR, or every possible Shiab. Revival requires either a
different source-natural Shiab that retains this information or an
independently action-owned pre-Shiab Gauss route.

No representation, anomaly/count, `LT-SM3b`, `LT-GR5`, `LT-GR6`, P1/P2/P3,
canon, public posture or observed-physics row moves.

## Accepted next gate

`OBSERVED_GAUSS_RECEIVER_SOURCE_ACTION_OWNERSHIP_AND_MOVING_DEFECT_VARIATIONAL_CLOSURE`

The next wave must compare direct tagged variation of the single K77 bulk
action with the complete moving observation-jet cotangent dual. It must build,
not merely name, the `N_s <-> V_s` transition and the normal/`Cl^5` residue;
include section, density, Hodge/Krein, soldering, both connections and normal
jets; and use the full Ward/Noether totalization. A local scalar Gauss identity
or a second downstairs action is not an exit.
