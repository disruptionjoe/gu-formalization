---
artifact_type: hostile_review
created: "2026-08-16"
targets:
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-two-ps-channels-have-distinct-upstairs-owners-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-source-owner-intersection-2026-08-16.md
  - tests/channel-swings/joe_directed_he4_distinct_ps_channel_owners_probe.py
status: pass_exact_owner_split__required_conditional_build_route_repair
classification: BRIDGE_OR_SEMANTIC_BOUNDARY
mandatory_lenses: [d5_branching, invariant_theory, source_ledger_archaeology, k77_clifford_horn, operator_action_typing, comparator_contamination, prior_art, falsification, claim_ceiling, test_mutation]
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# Hostile review — HE-4 distinct Pati-Salam channel owners

## Verdict

`EXACT_OWNER_SPLIT_SURVIVES__FAMILY_RANK_REMAINS_CONDITIONAL__NEXT_QUEUE_MUST_BE_REVERSED_TO_CONDITIONAL_COMPOSITION`.

The exact representation result survives hostile review. In the multiplicity-
one decomposition

```text
16 x 144 = 45 + 54 + 210 + 945 + 1050,
```

the Pati-Salam invariant counts are exactly `0,1,1,0,0`. The `54` and `210`
therefore own the two lines separately. The zeroes for `945` and `1050` are
individual consequences of nonnegative additivity, not an inference from
dimension alone. The conclusion that no **single irreducible D5 summand** owns
both lines is sound.

The family-rank and physical conclusions remain conditional. One assumed
nonzero selected owner gives one algebraic family covector and hence a
two-dimensional kernel in the three-copy family space; that statement does
not identify a family, prove an operator mass rank, or survive observation and
quotient automatically.

The proposed next route does not survive unchanged. This is a conditional-build
channel. It must not turn the missing source-action selection into a forward
derivation queue. Selection is a dependency fence and an explicit horn input.
The next work should assume each selection horn and test its downstream K77,
equation-9.16, cross-half, observation and family-kernel compatibility.

## Evidence replayed

- `PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_he4_distinct_ps_channel_owners_probe.py` — `27/27` pass.
- `sage tests/generation-sector/q5_spin10_vector_spinor_product_sage.py` — independent Weyl-character route, `28/28` pass.
- The source-claim register rows `SC-OP-04`, `SC-ACT-01`, `SC-FER-01`,
  `SC-FER-03`, `SC-CHI-51` and `SC-CHI-52` were checked against the prose.
- P-54-WELD, Resolver Waves F/G, the selected-K77 action-owned leakage
  composition, the K77 grade-saturated carrier result and the equation-9.16
  extraction were checked for horn and object transfer.

## Ten-lens attack

### 1. Skeptical D5/branching mathematician

The small branches close exactly:

```text
45  -> 15 + 24 + 3 + 3                         no singlet
54  -> 20 + 24 + 9 + 1                         one singlet
210 -> 15 + 24 + 45 + 45 + 40 + 40 + 1        one singlet
```

HE-3 independently supplies total invariant dimension two for the full
product. Restriction is additive on a direct sum and every invariant-space
dimension is a nonnegative integer. Thus

```text
dim Inv_PS(945) + dim Inv_PS(1050) = 0
```

forces both terms to be zero individually. A full large-irrep branch is not
needed. The independent Sage replay confirms the five multiplicity-one D5
summands and removes the most obvious circular-dictionary concern.

### 2. Invariant-theory and Schur-lemma critic

`Hom_D5(54,210)=0` because the two modules are inequivalent irreducibles. This
means D5 equivariance alone provides no linear intertwiner and no symmetry-
forced relation between the two owner coefficients. It does **not** prove that
an ambient K77 module, reducible source object, nonlinear composite, discrete
symmetry or action relation cannot connect them. HE-4's body mostly preserves
this distinction, but its Schur sentence and the probe label should be narrowed
to “D5 symmetry alone.”

### 3. Source-ledger archaeologist

The source register owns the parent fields and candidate grammar, not either
irrep selector. `SC-FER-01` owns `epsilon,varpi,nu,zeta`; `SC-OP-04` owns the
candidate four-field equation-9.16 arrangement; `SC-ACT-01` owns the augmented-
torsion action grammar; `SC-FER-03` records functional assignments rather than
derived observed equations; `SC-CHI-51/52` own the VEV-in-operator and
curvature-coaxing claims. None names a `54`, `210`, Pati-Salam-singlet
projector, family covector or selected equation-9.16 cell. HE-4B reports that
boundary faithfully.

Direct K77 grade-six provenance should nevertheless point at the carrier
receipts themselves, especially
`explorations/conditional-build/selected-k77-grade5-unitary-parent-euler-closure-2026-08-10.md`
and
`explorations/conditional-build/selected-k77-trace-hq-connection-compatibility-2026-08-13.md`,
rather than making a reader recover them through a later composition.

### 4. Current-K77/Clifford horn auditor

The old exact `q6` construction is a fixed `Cl(9,5)=M(64,H)` result. Its
dimension template and the complex identity
`Lambda^6(V_10)_C ~= Lambda^4(V_10)_C` may guide a new construction, but its
quaternionic projector, right-H reality, public/native port and rank-`252`
downstream image do not transfer to K77.

Current K77 work admits ambient grade-six connection directions. That is not
yet the moving internal `Lambda^6(V_10)` subbundle, its Hodge adapter, or the
one Pati-Salam volume line. HE-4B correctly calls the present `210` placement a
plausible parent rather than a construction. “Non-current `Cl(9,5)` horn” is
the accurate label; “stale” risks making a valid horn-specific theorem sound
arithmetically invalid.

### 5. Operator/action-selection typist

An internal grade-six coefficient of `T_omega` retains its `T*Y` coindex. It
does not become a separate four-dimensional scalar. Equation 9.16 displays
connection cells but does not label their Clifford grade, PS line, family row,
background value or physical quotient.

The critical channel-contract error is in the work queue, not the type ledger.
HE-4A asks whether the source carrier/action “selects” an owner, and HE-4B's
step 6 asks to vary the source action to derive stationary selection. That is
forward action construction. In this conditional-build path, a nonzero
selected `210`, selected `54`, both, or neither must instead be declared as the
conditional horn. The calculation may then test whether the assumed horn can
be represented in the current K77 carrier, reach the correct equation-9.16
cross-half block, and survive observation/quotient. Missing action selection
remains a dependency fence, not the next research task.

### 6. Conventional-comparator contamination detector

The reviewed artifacts repeat the mandatory routing notice and explicitly
forbid the ordinary family-index, net-chirality and scalar-Higgs substitutions.
The `54` and `210` labels are used as complex D5 bridge coordinates only. No
standard Higgs VEV is imported, and P-54's direct-weld failure remains binding.
This lens passes.

### 7. Novelty and prior-art auditor

The five-summand decomposition, total-two PS count, standard small-irrep
branches, P-54 direct-weld failure, old-horn `q6` template and source parent
grammar all pre-existed. The defensible novelty is their exact intersection:
the two HE-3 invariant lines are located in different irreducible owners and
the combined large remainder is proved singlet-free. HE-4's “mostly a
high-value composition” language is accurate.

### 8. Falsification and kill-branch designer

The next probe should preregister four conditional horns rather than search for
an action-selected horn:

| assumed horn | immediate exact target | kill condition inside this path |
|---|---|---|
| `H210` | nonzero selected `210`, no `54` | no real K77 internal PS port, wrong equation-9.16 half/cell, zero family row, or removal by observation/quotient |
| `H54` | nonzero selected quadratic `54`, no `210` | no typed composite port/cell, zero row, or removal downstream |
| `HBOTH` | both selected | no typed relative relation; test proportional and independent family rows separately |
| `HNONE` | neither selected | mediator path has no algebraic family separation |

No horn is permitted to fit a family vector, invent a scalar potential or
promote carrier admission to stationarity.

### 9. Claim-ceiling editor

The exact owner split does not imply family-row independence. A single nonzero
row has rank one only at the algebraic three-copy-to-one-partner gate. It does
not prove the rank of the complete barred/unbarred operator, identify the
third family, construct a mass, or establish scale, threshold or observable.
HE-4 states these ceilings repeatedly and survives this charge.

The Schur wording is the one repair: absence of a D5 intertwiner means no
relation is **forced by D5 symmetry**, not that every relation is impossible.

### 10. Test-harness and mutation reviewer

The probe independently reconstructs `45`, `54` and `210`, checks D5
irreducibility, closes each PS branch by dimension, reruns the full product
invariant count, subtracts characters without negative multiplicities and
checks the conjugate and wrong-half controls. Planted false ownership of the
`45` fires. The independent Sage Q5 route confirms the product decomposition.

The harness does not compute separate large-irrep branches, but that is not a
coverage defect because the zero remainder plus nonnegative additivity is a
proof. The one wording mutation needed is to rename the Schur check so it
tests absence of a D5-forced intertwiner rather than suggesting that Schur's
lemma governs every possible source relation.

## Required fixes before integration

1. **Reverse the next-work queue to conditional build.** In HE-4A §4, do not
   queue deriving whether the source action selects `54` or `210`. In HE-4B
   §§4–5, remove the instruction to vary the action for stationary selection.
   Declare `H210`, `H54`, `HBOTH` and `HNONE` as conditional inputs; prioritize
   K77 port, equation-9.16 cell/half placement, observation/quotient and family-
   kernel composition. Keep absent action selection in the dependency table.
2. **Narrow the Schur claim.** Replace “Schur's lemma ... therefore no
   relation” with `Hom_D5(54,210)=0`, so D5 symmetry alone supplies no linear
   intertwiner or forced coefficient/family-row relation. Make the same scope
   explicit in the probe's check label.
3. **Tighten horn provenance.** Replace “stale `Cl(9,5)`” with “non-current
   `Cl(9,5)`” and, if the current-K77 grade-six admission claim remains, cite
   the direct K77 grade-saturated/phase-complete carrier receipts. Continue to
   state that ambient grade-six admission is not the internal PS-singlet port
   and not selection.

No arithmetic or representation decomposition needs repair.

## Path-relative reprioritization

1. **Highest priority — `H210` downstream conditional composition.** Assume a
   nonzero selected `210` and absent `54`; construct the current-K77 internal
   PS line and test its actual equation-9.16 cross-half reach, real/half
   compatibility, observation map and algebraic family kernel. This is cheap,
   sharp and killable without deriving a source action.
2. **Second — `HBOTH` relation split.** If both ports are admitted, keep
   proportional and independent family covectors as separate conditional
   branches. Test downstream composition before any phenomenology.
3. **Third — `H54` composite control.** Preserve P-54's linear negative
   control. Admit only an explicitly assumed quadratic `W(theta)` horn and test
   the same operator/observation interfaces; do not reconstruct a scalar-Higgs
   model.
4. **Retire inside this path — single-irrep common owner and source-action
   derivation.** The first is exactly excluded by HE-4. The second belongs to a
   different functional channel and must not consume this conditional-build
   lane.

The HE-4 path remains fertile as a downstream compatibility falsifier, not as
a program to derive the missing action selection. Its next swing should end
with a horn-by-horn compatibility table and a family-kernel statement at the
highest layer actually reached.

## Final claim ceiling

After the required route repair, the strongest warranted result remains:

> The two PS-invariant lines in `16 x 144` lie separately in `54` and `210`;
> no single irreducible D5 owner aligns them. Under an explicitly assumed
> nonzero one-owner horn, one algebraic family covector is available, but all
> K77 port, operator-cell, observation, quotient and physical-rank conclusions
> remain to be tested.

No canon, source-claim, ledger verdict, particle spectrum, generation count,
mass, chirality theorem, scale, threshold, observable or public posture moves.
