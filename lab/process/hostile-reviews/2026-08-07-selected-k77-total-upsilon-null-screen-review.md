# Hostile review: K77 total raw-Upsilon and labelled ambient null screen

Status: `PASS AFTER RAW-OBJECT, NULL-SCREEN, AND SYMPLECTIC SCOPE FENCES`

## Charge 1 — where the summary outruns the artifact

1. **Bianchi closure is not raw-`Upsilon` naturality.** The exact identity is
   for the complete superconnection curvature. It neither cancels the
   algebraic `kappa_1 T` term nor proves `Xi=D Upsilon`.
2. **Curvature cancellation is only one grade slice.** The old graph cancels
   its grade-two curvature target exactly. Calling that total source
   naturality omits the mandatory `T` term and the live grade-one output.
3. **The null screen is labelled.** Its rank and signature are exact only
   after retaining a reciprocal null leg. A null rotation changes a held-out
   split if that label is forgotten.
4. **An ambient screen is not a physical quotient.** The existing 4D
   `10 -> 6 -> 2` result is a different carrier and remains unrecomputed.
5. **No surplus inflation.** Bianchi and the null-homotopy formula are
   identities, not independent constraints.

## Charge 2 — where rigor defends a superseded or mistyped object

The predecessor program fitted the selected-Shiab curvature image and was
correct on that slice. Continuing to harden that fit as though it were the
source's complete raw object would defend a superseded truncation. The source
formula already requires `Upsilon_B=Shiab(F_A)+kappa_1 T`. The coupled
all-grade object is now primary.

Likewise, reusing the non-null Koszul projector at a null covector would defend
an inapplicable normalization. The reciprocal-labelled homotopy replaces it;
it does not promote the screen to a canonical physical quotient.

## Specialist dispositions

| lens | hostile question | disposition |
| --- | --- | --- |
| differential geometry | are connection, curvature and raw residual distinguished? | yes |
| superconnection algebra | is the Bianchi sign/convention controlled? | yes; a single documented adjoint convention and an odd-only failure control |
| hyperbolic PDE | is null division avoided? | yes; reciprocal null homotopy only |
| symplectic geometry | has Euler or phase-space reduction been constructed? | no; explicitly fenced |
| representation theory | is the live failure graded? | yes; grades one and two, rank four |
| source criticism | is the total object source-owned? | yes for the two-term formula; not for the repo's proposed repair |
| Krein/operator theory | is positivity or a common domain inferred? | no |
| exact computation | are controls capable of firing? | yes; odd-only Bianchi and forgetful-screen plants both fire |

## Final disposition

`PASS_AFTER_RAW_OBJECT_NULL_SCREEN_AND_SYMPLECTIC_SCOPE_FENCES`.

Accepted:

- exact full linearized superconnection Bianchi closure;
- exact labelled ambient null screen of rank 12 and signature `(6,6)`;
- persistent exact grade-two curvature cancellation; and
- exact rank-four total raw-`Upsilon` defect with live grades one and two.

Refused:

- a no-go for every possible total source graph;
- identification of `D F=0`, `Xi=D Upsilon` and the action Noether identity;
- a canonical screen after forgetting the reciprocal label;
- identification with the 4D physical null quotient;
- Euler, preboundary, symplectic, BV/BFV, Green-domain or positivity closure;
- any surplus, residue, P1/P2/P3, canon or public-posture movement.

Next gate:

```text
COUPLED_ALL_GRADE_RAW_UPSILON_GRAPH_INCLUDING_KAPPA_T
THEN_OBSERVATION_EULER_PREBOUNDARY_SYMPLECTIC_DESCENT
```
