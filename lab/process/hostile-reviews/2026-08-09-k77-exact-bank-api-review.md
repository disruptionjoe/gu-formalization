---
title: "Hostile review: K77 exact coefficient-bank API"
date: 2026-08-09
verdict: PASS_WITH_FENCES
---

# Hostile review: K77 exact coefficient-bank API

## Pre-wave

No new scientific fork is assumed. The bank preserves the selected-K77
conditional scope already declared by its producer and explicitly rejects a
port to the two `U(32,32)` halves or full `U(64,64)`. The finite serialization
space is decided wholesale; parent extension, analytic domain and physics
quotient are outside it. Success owns only a repository process object and
kills only recursive rebuilding by ordinary consumers.

## Required lenses

- **Layer-0 semantics:** source grammar, coefficient construction and cache are
  distinct. `SOURCE-SILENT` is correct.
- **Prior art:** the v0.122 theorem and v0.123 completion remain authoritative;
  this Run re-materializes rather than reclaims them.
- **Differential geometry / representation:** atomic form/Clifford labels,
  causal covectors, receiver split and parent fences survive serialization.
- **Variational and symplectic:** first-action coefficients are not a raw
  residual Jacobian, reduced phase space or BV quotient.
- **Operator/Krein:** finite exact matrices do not establish a closed domain,
  adjoint, Green inverse or positive physical form.
- **Exact-computation / adversarial:** direct cells, all six independent rank
  matrices and seven planted failure modes pass.
- **Distributed provenance:** content and dependency hashes detect stale or
  mutated materializations; the source of authority is not transferred to the
  cache.

## Charge 1 — where the summary outruns the artifact

The dangerous summary is “the K77 carrier is now built.” What is actually
built is one selected-Spin, first-transgression, fixed-operator grade-two bank.
The API schema and report correctly keep the ten remaining metric completion,
expanded tangent/equation quotient, unitary parents, domains and BV-BFV open.
With those fences retained, the summary does not outrun the artifact.

## Charge 2 — where rigor defends a superseded or mistyped object

The API could have made the frozen fixed-operator v0.122 summand the permanent
scientific object even though v0.123 completed the epsilon moving terms. The
fixture therefore carries the v0.123 zero-correction/equality receipt and is
described as a reusable coefficient substrate, not the entire remaining
Hessian. It also refuses to identify the selected Spin parent with either
unitary parent. No superseded object is promoted.

## Charge 3 — if this stands, what else must change

- `functional-channel-operating-contract-v1.0`: **needs-recheck/update** — name
  the efficient core-plus-triggered lens routing and current v0.124 surface.
- `lab/process/CURRENT-RESEARCH-CONTEXT.md`, the historical service-Lane view,
  `NEXT-STEPS`, `RESEARCH-STATUS`, and READMEs:
  **needs-recheck/update** — replace the process prerequisite with the next
  scientific gate.
- v0.122/v0.123 scientific artifacts: **survive** unchanged.
- selected Spin versus two-half/full-unitary parent fork: **survives**.
- recursive predecessor build as the normal consumer path: **dissolved**.
- physics verdicts, canon, residue, quotients and P1/P2/P3: **survive**.

## Trap capture

No mathematical path-dependency trap is added. The detected duplicate builder
was a process-session visibility failure, not a repeatable scientific inference
trap, and the Runtime claim plus the content-addressed consumer gate already
own its prevention. Creating a ninth or unrelated scientific trap chain would
misfile it.

## Verdict

`PASS_WITH_FENCES`. The API is a legitimate efficiency and provenance result.
It may be used by the next Build only if dependency verification remains on and
the bounded exact replay remains green. It cannot be cited as evidence for a
larger action parent, a quotient, an analytic domain or recovered physics.
