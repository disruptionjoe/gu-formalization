---
artifact_type: hostile_review
created: 2026-08-09
verdict: CANDIDATE_SURVIVES_ONLY_AS_FIXED_OPERATOR_LEAKAGE_THEOREM__TOTAL_SOURCE_DISPOSITION_OPEN
reviewed_artifact: explorations/conditional-build/selected-k77-fixed-operator-metric-epsilon-leakage-2026-08-09.md
---

# Hostile review: fixed-operator metric/epsilon leakage

## Pre-wave

No physical signature, stationary branch or action-parent fork is assumed:
both algebraic branches and all three causal representatives are evaluated on
the selected first-action parent only.  The finite search space is 101 source
directions by all 1,274 grade-two equation covectors, so the frozen-operator
subproblem can be decided wholesale.  Success introduces no object.  It can
kill only the claim that the fixed-operator 321 restriction is closed.

## Charge 1: where the summary outran the artifact

The raw rank-89 result invites “321 is killed.”  The certificate does not say
that.  It holds Hodge, Phi, Shiab, the density/frame and observation fixed,
whereas the source assigns them to the moving geometry.  The report therefore
says only that the frozen shortcut leaks and gives the precise cancellation
burden for the total moving derivative.

## Charge 2: where rigor could defend the wrong object

The prior v0.36 principal metric cross and v0.106 moving-Shiab epsilon cross
are genuine prior art but are not this matrix.  Conversely, this exact matrix
cannot substitute for either or for the full source-coordinate Hessian.  The
review rejects both supersessions and retains the first/residual-square and
selected/two-half/full-parent distinctions.

## Charge 3: what changes if the result stands

- `dissolved`: fixed-Hodge/Phi/Shiab/frame/observation closure as a shortcut
  from the minimum-known 321 tangent to the full grade-two equation dual.
- `survives`: the 321 candidate under a possible action-owned moving-term
  cancellation; the 1,571 rival without promotion; both exact branches and
  all parent scopes.
- `needs-recheck`: the total moving source Hessian, expanded parents,
  derivative jets, gauge/ghost closure, analytic domains and coupled BV--BFV.

## Required lenses

- `layer0_semantics`: PASS after fixed summand and total moving Hessian are
  separated.
- `prior_art`: PASS; v0.36 and v0.106 are credited without being rebranded.
- `representation_theory`: PASS; all 1,274 receivers and the 24/1,250 split
  are tested by grade label.
- `variational_bicomplex`: PASS with fence; the computed object is the exact
  fixed-operator derivative of the first-action Euler covector.
- `symplectic`: PASS with fence; nonzero leakage is not a BV differential,
  presymplectic quotient or obstruction to an action-owned cancellation.
- `analytic_krein`: PASS with fence; finite algebraic rank decides no closed
  domain, positivity, hyperbolicity, Wick contour or unitarity question.
- `source_criticism`: PASS; `SOURCE-CONFIRMS` the moving owner and
  `SOURCE-SILENT` the frozen truncation and cancellation.

## Verdict

`CANDIDATE_SURVIVES_ONLY_AS_FIXED_OPERATOR_LEAKAGE_THEOREM__TOTAL_SOURCE_DISPOSITION_OPEN`.

The result gains information by converting a vague “finish metric/epsilon”
task into a rank-resolved cancellation target.  It does not change a physics
verdict, select a tangent, or settle any datum.
