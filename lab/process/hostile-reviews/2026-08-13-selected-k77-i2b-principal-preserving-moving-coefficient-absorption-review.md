---
artifact_type: hostile_review
created: 2026-08-13
run_id: RUN-20260813-223221-gu-i2b-moving-coefficient-absorption
subject: selected-k77-i2b-principal-preserving-moving-coefficient-absorption
verdict: PASS_WITH_PRINCIPAL_FINGERPRINT_FENCE
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Hostile review: principal-preserving moving-coefficient absorption

## Pre-wave

- **Fork assumed:** none.
- **Search space:** every vector in the 140-cell cubic and 280-cell quartic
  compatibility targets for the fixed endpoint tableau; wholesale decision is
  possible because the constrained absorber is onto both complete targets.
- **New un-owned object:** none.
- **What dies on success:** term-by-term assembly of already-known
  principal-preserving coefficient jets as separate cubic/quartic obstruction
  searches. Their physical and variational roles do not die.

## Charge 1 — where does the summary outrun the artifact?

The phrase “moving coefficients are absorbed” is too broad. The artifact proves
only that **lower-order representatives at a fixed principal fingerprint** have
zero constrained class through quartic order. A changed `Q_B` base value,
derivative-dependent primalizer, new action owner, singular receiver or
reduced field domain changes the principal problem and is not covered.

Disposition: `SURVIVES` after binding every summary to
`principal-preserving`, `algebraic zero-order coefficient`, and
`through quartic order`.

## Charge 2 — where is rigor defending a superseded or mistyped object?

The source-faithful fixed-natural endpoint owner was resolved after the older
`E_act/Q_u` principal comparison. This wave correctly uses that endpoint owner
and keeps the zero-principal `E_act/Q_u` object as a separate rival. Applying
the endpoint quotient to the rival would be a mistyped transfer.

Likewise, the real-`u` source-normal contact is an off-shell field-jet image,
not a new coefficient field or physical tangent. It enters this theorem only
conditionally as a lower-order representative.

Disposition: endpoint owner `SURVIVES`; `E_act/Q_u` transfer `DISSOLVED`;
physical/BV interpretation `NEEDS_RECHECK`.

## Charge 3 — if this stands, what else must change?

- term-by-term coefficient expansion as a torsion-search queue item:
  `DISSOLVED` through the tested cubic/quartic orders;
- known coefficient packets as inputs to stationary selection, Green and
  observation calculations: `SURVIVES`;
- higher nonlinear formal integrability: `NEEDS_RECHECK`;
- principal-changing source action or primalizer: `NEEDS_RECHECK` with a new
  structure fingerprint;
- physical tangent/BV/BFV and analytic/global domain: `NEEDS_RECHECK`;
- ledger, canon, claim status and public posture: empty change list.

## Specialist core

- **Layer-0 semantics:** coefficient derivatives and principal changes are
  separated explicitly.
- **Prior art:** the wave composes pushed exact ranks and existing moving
  packets; it does not claim those ingredients are new.
- **Spencer/EDS:** the absorber is restricted to prolonged-symbol kernels.
- **Variational bicomplex:** only algebraic zero-order coefficient fields have
  first spatial jets automatically below the principal symbol.
- **Symplectic geometry:** formal absorption is not a BV/BFV quotient or
  presymplectic reduction.
- **Analytic/hyperbolic/Krein:** no existence, propagation, energy, positivity
  or spectrum follows.
- **Contrary:** a deficient exact fixture fires, and every principal-changing
  horn is retained.

## Verdict

`PASS_WITH_PRINCIPAL_FINGERPRINT_FENCE`.

The universal linear-algebra consequence is valid and useful. It must not be
advertised as full nonlinear integrability or as proof that moving geometry is
physically irrelevant.
