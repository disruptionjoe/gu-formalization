---
title: "Hostile review: selected-K147 radical-module factorization gate"
status: completed
doc_type: hostile_review
created: "2026-08-16"
target: lab/process/selected-k147-native-i1b-t0-radical-module-factorization-gate.json
---

# Hostile review: K147 radical-module factorization gate

## Strongest overclaim attack

The order-eight principal symbol is now demonstrably live for the selected
`I1B` metric polynomial.  The exact composition

```text
sigma_8(S_4)(xi)=-A(xi)^T (K C_1(xi))^4 K A(xi)
```

has rank six at the spacelike control and has nonzero radical leakage.  What
remains only a safe structural ceiling is the Brinkmann coefficient-jet bound:
this coefficient-frozen principal calculation does not prove that every
profile derivative through order six survives.  Thus `a_0,...,a_6` and
`b_0,...,b_6` remain the safe generic profile-jet cap, not a proved nonzero
dependence at every order.

The exact combinatorics

```text
dim J^8(R^4)=495,
dim J^8(H_n)=9*495=4455
```

give the universal finite test module.  K147 now books exact order-eight
principal leakage and hence failure of full differential-module factorization.
It does not book a sixth-profile-jet term or the lower curved null-microlocal
composition.

## Strongest contrary construction and mistyping attack

A pass need not enumerate 4,455 evaluations: a covariant factorization
`ell_n S_4=Q ell_n` could prove all coefficients at once.  Conversely, one
exact nonzero coefficient on one radical-valued jet is a complete failure
certificate.

The actual selected-branch witness is decisive.  For fixed
`n=(1,0,0,1)`,

```text
ell_n=[1,0,0,-2,0,0,0,0,0,1].
```

At the spacelike derivative covector `xi=(0,1,0,0)`,

```text
ell_n sigma_8(S_4)(xi)=[-64,0,0,-256,0,0,0,0,0,-64].
```

This row is not proportional to `ell_n`: its first entry forces `q=-64`, but
then its fourth entry would be `+128`, not `-256`.  Equivalently,
`h=(2,0,0,1,0,...,0)` obeys `ell_n h=0` yet leaks by
`-64*2-256=-384`.  This is a real selected-branch principal coefficient, not
the earlier planted logical control.

The main mistyping danger is to identify the spacelike derivative covector
`xi` with the fixed null characteristic `n`.  The witness disproves
preservation of `H_n` under arbitrary section derivatives and therefore the
full differential module.  It does not show leakage at the null-symbol
restriction `xi=n`.  The frozen null restricted residual is zero, and an
independent null covector control has zero order-eight term.  These facts are
compatible and preserve the narrower null-microlocal route.

The carrier audit remains sound: `P^5` acts on the distortion carrier, while
the radical test is posed for the typed metric operator `S_4=-A^*R_4A`.

## Why lower terms cannot repair the failure

A differential identity `ell_n S_4=Q ell_n` must hold separately at its
highest homogeneous symbol order.  Operators of order at most seven contribute
no order-eight coefficient.  Equivalently, one can prescribe the normalized
eighth jet while setting all lower jets to zero.  Connection, curvature,
density, moving-pairing and other lower-order corrections therefore cannot
cancel the exact `-384` witness.

Only a genuinely omitted order-eight term in the asserted principal
composition could repair it.  The probe attacks that seam by composing the
complete principal `A`, `K C_1`, `K`, and `A^T` packet and replaying the action
ranks.  The final replay passes `63/63` exact checks.

## Weakest source and reproducibility seam

The source confirms a moving eight-member displayed Shiab family but does not
select `comm/symi/symi` as Weinstein's preferred historical member.  The
active target is nevertheless the already selected conditional
`comm/symi/symi` branch.  For that branch the repository owns the mathematical
local action, exact frozen arithmetic, algebraic Hodge/pairing, principal
`A/C/K` packets, induced transport laws, and a Ricci-flat Brinkmann fixture.

It does not yet serialize those owned formulas in one common `Cl(7,7)`
evaluator carrying the full moving connection, Shiab, Hodge, density, formal
Euler adjoint, curved `A/A^*`, and Leibniz-complete lower composition.  The
trace-adapted `Cl(9,5)` port and bounded low-carrier adjoint fixtures do not
close that seam.

The probe directly composes the complete order-eight selected-branch symbol,
but it does not evaluate the missing lower curved operator.  Its principal
failure may be cited as an `I1B` failure of the full module.  Neither the
synthetic controls nor the frozen null zero may be cited as a curved
null-microlocal pass.  Building the unified evaluator from the owned
conditional formulas remains authorized implementation; it does not select a
new source Shiab, pairing, action, gauge, domain, or physical owner.

## Exact source-closure verdict

```text
selected frozen comm/symi/symi branch:        CLOSED;
conditional local mathematical operator:      OWNED;
finite order/jet factorization criterion:     CLOSED;
preferred source Shiab selector:              NOT SOURCE-CLOSED;
complete moving Cl(7,7) E(D_B), A and A*:     NOT YET SERIALIZED FROM OWNED FORMULAS;
full differential-module factorization:       FAIL_ORDER8_PRINCIPAL_SYMBOL;
frozen null restricted residual:              ZERO_BY_L_N^5;
independent null order-eight control:          ZERO;
curved null-microlocal lower composition:      NOT COMPUTED;
preferred-source GU claim:                    DOES NOT MOVE;
selected conditional K146 next gate:          AUTHORIZED TO PROCEED.
```

The operative serialization verdict remains
`EVALUATOR_NOT_SERIALIZED_FROM_OWNED_CONDITIONAL_FORMULAS`, not
`EVALUATOR_NOT_SOURCE_CLOSED`.  K146 owns the mathematical local operator, and
Joe's authorization permits bounded implementation of the selected branch.
The preferred historical selector remains missing and must not inherit the
conditional result.

The strongest honest classification is

```text
full differential-module radical preservation = FAIL;
curved null-microlocal preservation = UNDEFINED_LOWER_COMPOSITION_OPEN.
```

No quotient endomorphism, inverse, parametrix, domain, BFV class, positive
state space, physical propagation law, or superposition claim follows.

## Switch consequences

- **Full differential-module route:** stop.  The single order-eight witness
  dominates a 4,455-coefficient enumeration; lower terms cannot reverse it.
- **Selected conditional route:** narrow authorized serialization to the exact
  null-characteristic symbol/transport module and its lower curved restricted
  residual.  Replay K132/K135, preserve K125 moving-frame covariance, and
  require an independent formal-adjoint Green check.
- **Preferred-source route:** remains held.  Every result stays conditional on
  the repository-selected `comm/symi/symi` branch.
- **Serialization failure:** if composition requires an additional scientific
  choice, stop at that exact owner gap rather than filling it by convenience.
- **Adverse null-microlocal result:** exact leakage at `xi=n`, its covariant
  null controls, or a nonzero curved restricted remainder `K P^5 K A h` kills
  the corresponding narrowed descent or restricted-inverse claim.
- **Positive null-microlocal result:** a null-characteristic transport identity
  permits only that scoped microlocal descent.  It cannot restore the failed
  full module or make `S_4` an exact Schur complement, inverse, or dynamics.

No canon, ledger, particle, phenomenology, published-paper, or GU-wide verdict
changes.
