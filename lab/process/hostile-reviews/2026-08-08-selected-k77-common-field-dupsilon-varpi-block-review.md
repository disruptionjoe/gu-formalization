---
artifact_type: hostile_review
created: 2026-08-08
status: PASS_WITH_HORN_SCOPE__NEXT_OWNER_REORDERED_TO_EPSILON_PLUS_PHYSICAL_METRIC
target: selected-k77-common-field-dupsilon-varpi-block-2026-08-08.md
---

# Hostile review: common-field D-Upsilon varpi block

## Lenses

Layer-0 semantics, exact carrier/representation typing, differential and
symplectic geometry, variational PDE, Krein/operator theory,
complex/path-integral scope, source criticism, repo archaeology and constraint
accounting were applied after the exact and independent reruns.

## Charge 1 — where the summary could outrun the artifact

**Fired and repaired.** The exact result does not kill the older metric
diagnostic absolutely. It proves that a rank-four metric Ward load cannot be
the `g-g` Gram block of a **fixed-`epsilon` `(g,varpi)` completion** satisfying
`J R=0`, because the actual source-`varpi` response on that orbit has rank
three. The full source action also contains `epsilon`; its unbuilt Fréchet
response is a live fourth-column route.

The summary therefore uses “not importable on the fixed-`epsilon` horn,” not
“falsified.”

## Charge 2 — whether rigor defends a superseded or mistyped object

**Fired.** The prior queue could have spent another wave hardening the
rank-four metric-only diagnostic. That object is not yet typed as a block of
the stationary common-field Gram Hessian. The current exact source block
forces the owner order:

1. construct `D_epsilon Upsilon` and the physical `D_g Upsilon` together;
2. prove the complete `J R=0`;
3. only then test whether the old metric diagnostic is reproduced.

The printed `Xi=D_omega Upsilon` cannot shortcut step 1 because it is an
exterior covariant prolongation, not a Fréchet derivative with respect to the
field `epsilon`.

## Charge 3 — what else must change if the result stands

| item | disposition | consequence |
| --- | --- | --- |
| v0.82 next gate | survives, narrowed | the `varpi` block is closed; metric plus epsilon now lead before K/adjoint |
| old rank-four metric diagnostic | needs-recheck | compare only after the full source-epsilon horn is assembled |
| source `Xi=D_omega Upsilon` | survives | remains a redundancy/prolongation, not an epsilon field derivative |
| independent observation column | dissolved | observation remains a dependent normal-jet term inside `D_g Upsilon` |
| external datum repair | dissolved | P1/P2/P3 cannot manufacture the missing Fréchet blocks |
| matter/grade-one/fermion blocks | survives | separately typed downstream common-field summands |
| symplectic/BV-BFV/domain claims | survives as open | no reduced class or common domain follows from the bulk rank theorem |

## Controls and reruns

- Main exact probe: `68/68 PASS`.
- Independent Sage/QQ route: `15/15 PASS`.
- Removing the full-versus-horizontal carrier fence is rejected.
- Promoting an orbit-only left-inverse completion to a physical metric block is
  rejected by the rank-six transverse projector.
- Importing the rank-four metric diagnostic on the fixed-`epsilon` horn is
  rejected by rank factorization.
- Adding an already-owned source-`epsilon` response is retained as a type-level
  revival control, not counted as a pass.

## Verdict

`PASS_WITH_HORN_SCOPE__NEXT_OWNER_REORDERED_TO_EPSILON_PLUS_PHYSICAL_METRIC`.

No scientific verdict, canon, public posture, residue, quotient or external
datum changes.
