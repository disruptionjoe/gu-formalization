---
artifact_type: hostile_review
created: 2026-08-14
target: explorations/conditional-build/selected-k77-vrs5-curvature-vev-reopener-qualification-2026-08-14.md
verdict: SURVIVES_AS_EXACT_REOPENER_DISJOINTNESS_AND_RERANK__NONPARALLEL_SUCCESSOR_REMAINS_UNSOLVED
---

# Hostile review: VRS-5 curvature/VEV reopener qualification

## Verdict

`SURVIVES_AS_EXACT_REOPENER_DISJOINTNESS_AND_RERANK__NONPARALLEL_SUCCESSOR_REMAINS_UNSOLVED`.

The result correctly preserves the old scalar-jet cancellation while refusing
to transfer it to a different canonical curvature orbit. The next hypothesis
is selected by owned operator proximity and potential exclusion power, not
promoted as a solution.

## Strongest attacks

### The old trace closure already supplies the missing opposite row

Rejected on carrier identity. Its total curvature is `C/32448` and is nonzero
on nine labelled mixed trace planes. Canonical `F_BZ` is zero on those planes,
has a different support and is not proportional to `C`. Gauge conjugation
cannot turn zero into nonzero on a fixed tangent plane.

### The curvature cell is action-owned, so choosing its value is not fitting

Rejected for SR-1C. Action ownership establishes that the field derivative may
carry such a cell; it does not make a first-jet coordinate freely adjustable
after `B_Z` has been fixed as metric-derived. A legal value must be emitted by
the same canonical connection jet and must preserve every source equation.

### The old and new branches could be two descriptions of the same root

Rejected exactly. Substitution of `t=-1/104` into the SR-1C polynomial gives
`-1397/4`, not zero.

### The counterfactual required-r calculation proves no scalar repair exists

Narrowed. It proves that the already solved rational `r` is not an automatic
repair and that a single fixed value cannot cancel both roots under either
displayed normalization. A future canonical reconstruction could emit a
root-dependent algebraic scalar projection together with compensating rows;
that would be a new constructed branch, not this graft.

### Ranking the nonparallel graph first assumes it can cancel the trace

Rejected as a promotion; accepted as the live uncertainty. The parallel kill
sets the graph adjoint to zero. The nonparallel second-jet image is the nearest
existing action-owned source of a nonzero opposite trace. The proposed next
gate explicitly asks for an image witness or a cokernel obstruction before a
large solve.

### A nonparallel witness would immediately give a background

Rejected. Any witness must be returned to the same carrier and recomputed
against translation, differentiated Bianchi, primitive epsilon, total metric
stationarity and further formal integrability. Open realization and analytic
existence remain later gates.

## Required successor

Construct the constrained SR-1D second-jet linear map and decide whether
`-rho L1` lies in its metric graph image while translation/Bianchi and
primitive epsilon vanish. Preserve both algebraic embeddings until the exact
image/cokernel result is known. Do not enter VRS-6 without a complete
stationary background.
