---
title: "Hostile review: selected-K144 curved local-inverse owner gate"
status: completed
doc_type: hostile_review
created: "2026-08-16"
target: explorations/conditional-build/selected-k144-native-i1b-t0-curved-local-inverse-owner-gate-2026-08-16.md
---

# Hostile review: K144 curved local-inverse owner gate

## Strongest overclaim attack and correction

The first draft incorrectly generalized the scalar degree obstruction to the
full system. K135's generalized principal coefficient is nilpotent, so a
finite polynomial inverse exists at frozen constant-coefficient grade. The
exact matrix control `P_s^2=sP_s` supplies a curved local differential inverse
and defeats “positive order implies nonlocal.”

Disposition: corrected before publication. K144 now proves non-identifiability
from frozen data, not nonexistence of a local inverse. Local, parametrix, and
Green routes all remain open for the complete curved `I1B` evaluator.

## Strongest contrary construction

A covariant symbol calculus could build `C^-1` microlocally. The stronger
contrary construction is already exact and local: the nilpotent matrix control
has the same frozen point matrix for every slope but a slope-dependent local
inverse. Both demonstrate that the complete variable-coefficient Hessian and
operator composition, not frozen powers, own the decision.

Disposition: incorporated as a primary result rather than treated as an
exception.

## Quantization attack

The scalar control gives a nonzero first correction in left quantization and
zero first correction in Weyl quantization. That does not mean observable
physics changes with notation: symbol transforms relate representations of
the same operator. The result must therefore be phrased as a requirement to
declare the convention and invariant equivalence class, not as physical
nonuniqueness.

Disposition: wording corrected at source. The control is used only to prove
that an unqualified coefficient is under-typed.

## Action-custody and Noether attack

The full coupled Hessian is local and inherits action-owned diffeomorphism
identities. It would be wrong to say the action owns no curved coefficient at
all. The missing object is narrower: a reduced five-class metric
endomorphism obtained only after distortion inversion. K144 preserves the
coupled local route and directs K145 to test its compatibility complex without
`C^{-1}`.

Disposition: survived. The artifact separates local coupled ownership from
nonlocal reduced ownership in every result surface.

## Reproducibility and propagation seam

Neither scalar nor matrix control certifies the full Clifford system. The probe
treats both as planted opposite horns and revalidates K138/K143's serialized
action-specific inputs. A future coefficient must derive the complete curved
`I1B` composition; it may not import either control's value.

Disposition: bounded. No canon, ledger, particle, phenomenology, published
paper, or GU-wide verdict changes.
