---
artifact_type: hostile_review
created: 2026-08-11
work_item: SELECTED_K77_NATURAL_TRACE_CONSTRAINT_GATE
verdict: SCOPED_SURVIVES
lenses: [layer0_semantics, prior_art, representation_theory, analytic_operator, hyperbolic_pde, symplectic_variational, exact_computation]
---

# Hostile review — selected K77 natural trace-constraint gate

## Verdict

`SCOPED_SURVIVES__UNIQUE_PROPAGATED_SPIN_NATURAL_ZERO_ORDER_TRACE_LINE_RETAINS_COMPLETE_RANK128_JORDAN_IMAGE__ZERO_ORDER_ROUTE_KILLED__DERIVATIVE_NONLOCAL_BV_DERIVED_AND_OPERATOR_COMPLETION_ROUTES_OPEN`

## Charge 1 — where does the summary outrun the artifact?

The phrase “all constraints fail” would outrun the theorem. The exact search is
complete only for Spin-natural **zero-order spinor-valued** maps
`Omega1(S) plus Omega0(S) -> S`. Derivative, pseudodifferential, nonlocal,
higher-rank, BV-derived, and operator-modifying routes survive. The artifact
and ledger must retain that scope.

## Charge 2 — where is rigor defending a superseded or mistyped object?

No same-object defect remains: the probe replays and reuses the actual
dimension-1920 real-K77 source-shaped evolution. The main mistyping risk is to
call the physical relation `2 Gamma(zeta)-nu=0` a ghost/BV differential. The
source types both fields as physical, and the constraint is not a null
direction of the Green form merely because its kernel is propagated.

The older `Q_j=0` characteristic repair is direction-fitted and cannot be
substituted for the common natural relation. Its deliberate planted control
fires.

B2C1 is genuine prior art for the rank-128 Jordan image and failed fixed
quotient, but it uses the projected W131 object. The present claim is narrower
and incremental: complete common zero-order constraint classification on the
current full real-K77 operator. It must not be advertised as discovering the
Jordan image itself.

## Charge 3 — what else must change if this stands?

- **dissolved:** a natural zero-order gamma-trace constraint as the repair of
  the rank-128 generalized characteristic chains.
- **survives:** derivative/action-derived BV constraints; explicit nonlocal
  ultrahyperbolic constraints; weak closed realizations; graph selection;
  observation, chirality, mirror, index and count.
- **needs-recheck:** the old source-admitted wedge-Shiab/nonzero-southeast
  semisimple completion on the current real-K77 action-owned operator. It is an
  operator modification, not a constraint, and therefore is not reached by
  this no-go.

## Analytic and symplectic review

Invariance is checked by exact row-space propagation for all three observed
generators. Non-diagonalizability after restriction follows because the
square-zero image lies in the constraint kernel and the restricted `Q_j`
remains onto. No positivity claim is inferred from an indefinite pairing, and
no boundary quotient or BFV charge is manufactured from a kernel restriction.

No trap entry is added: the propagation-versus-removal and physical-field-
versus-BV distinctions are already governed by `PD-ULTRAHYPERBOLIC-DOMAIN`
and the existing B2C3 Layer-0 record.
