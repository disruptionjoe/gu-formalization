---
title: "Hostile review: B5 strict-massless extension dependence"
status: completed
doc_type: hostile_result_review
created: "2026-08-21"
reviewed_artifact: explorations/b5-strict-massless-extension-dependence-2026-08-21.md
verdict: survives_with_sharpened_scope
target_claim: internal target B5-STRICT-MASSLESS-EXTENSION-DEPENDENCE; verdict exact M=0 global operator-kernel discriminator survives review
canon_verdict_change: none
---

# Hostile review: B5 strict-massless extension dependence

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

Scope: review of the undeformed strict folded operator kernel on the named flat
B5 half-cylinder. No source action, physical quotient, positivity or GU verdict
is under review.

```gu-typed-objects
result: hostile review preserves the exact coflip-real massless characteristic mode and two-domain operator-kernel discriminator while blocking a BV/BRST cohomology promotion
carrier: L2 sections of the rank-1920 complex folded carrier E=S plus V* tensor S over the named half-cylinder LAYER=ambient CHIRALITY=S-FULL-DIRAC
pairing: auxiliary positive L2 topology plus program-native split Green form B_dr ON=independent-B5-curved-strict-carrier
real_structure: relative Gamma-natural anti-linear coflip fixing the selected complex Fourier mode
grading: folded ghost-field Hessian grading, not yet stage-separated global BRST domains
action_owner: repository-construction strict massless bulk action; boundary polarizations remain action/source-unselected
target: strongest overclaim, contrary construction and reproducibility seam for the M=0 operator-kernel result MAP-TYPE=evaluation
```

## Strongest overclaim

The mode is nongauge at the characteristic-symbol level, but that is not yet a
global BV or BRST cohomology class. A folded maximal-isotropic Hessian domain
does not automatically split into closed domains for `A`, `K` and `A^vee`, and
it does not choose whether cohomology means an algebraic quotient or a reduced
closed-range quotient. The result is therefore stated only as global operator-
kernel extension dependence.

## Strongest contrary construction

The zero tangential mode cannot provide the claimed witness: at `M=0` its
solutions are constant in `r` and no nonzero constant mode is square-
integrable. The positive tangential mode is load-bearing because
`-e0+i e4` is complex-null and produces exponential decay. Replacing `e4` by a
negative tangential direction removes the real decaying characteristic root.
These adverse cases support, rather than weaken, the exact scope.

The coflip condition is also load-bearing. A positive tangential direction
with coflip sign `+1` is exchanged with the opposite Fourier mode, so a
single-mode trace need not be coflip-real. Index `4` has metric sign `+1` and
coflip sign `-1`, making the complete mode fixed. The probe checks both signs.

## Weakest reproducibility seam

The proof uses the real Witt extension theorem rather than serializing a
`1920 x 1920` change-of-basis matrix. This is decision-grade because the prior
certificate already proves nondegenerate split signature `(960,960)`, and the
new exact calculation proves that `v` is nonzero, coflip-real and Green-null.
Those hypotheses are exactly what Witt extension needs. A low-dimensional
split-form control certifies the one-line replacement pattern defining
`L_hit` and `L_miss`.

## Verdict

The massless operator-kernel discriminator survives. The claim is sharpened to
two named coflip-real Witt polarizations and one exact periodic decaying mode.
The review rejects Hilbert self-adjointness, Fredholmness, BV/BRST cohomology,
positivity, physical-state, particle, source-selection and GU conclusions.
The next gate must construct stage-compatible closed domains before asking
whether the mode survives cohomology.
