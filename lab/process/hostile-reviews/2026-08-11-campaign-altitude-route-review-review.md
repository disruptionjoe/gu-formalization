# Hostile review: campaign-altitude route review 2026-08-11

Target: `explorations/campaign-altitude-route-review-2026-08-11.md`
(Discovery output; Joe-directed run). Reviewed same-run under the standing
three charges. Verdict:
`SURVIVES_WITH_SCOPE_NOTES__ALL_RECOMMENDATIONS_REMAIN_PROPOSALS`.

## Charge 1 — where the summary outruns the artifact

- **The F1 cost number.** The exact facts are: family construction available
  2026-08-01 (B2C4, active Cl(9,5)); v0.163 explicitly leaves Shiab-family
  changes open; nine waves v0.164-v0.172 before the v0.173 port. The
  "four to six waves" counterfactual is judgment layered on those facts and
  is correctly carried as SCOPED in the manifest. It must not be cited as a
  measurement. The defensible measured statement is only: the port was
  available and named for nine waves before it ran.
- **The rank-2 re-entry claim.** The review says the v0.168 kill premise
  (empty positive-symmetrizer cone) is false for the completed operator.
  That is exactly what v0.173 certifies — at flat principal grade, via
  finite-Clifford-group averaging. Analytic lens: flat-principal positivity
  does not by itself yield a curved/global closed domain, boundary theory,
  or lower-order control. The review's claim is a work-ordering claim
  (retry the standard route first), not a prediction that it succeeds. Any
  reading of the brief as "the Cauchy problem is now solvable" outruns the
  artifact.
- **F2's strength.** "No named resolver" is the register's own current
  text, and "depth 10, over threshold" is today's fork-depth audit output.
  The review does not claim the fork is resolvable, only that its pricing
  is absent from the queue. Within bounds.

## Charge 2 — where rigor defends a superseded or mistyped object

- **Layer-0 on "standard symmetric-hyperbolic route".** The phrase denotes
  the same object v0.168 killed it for: the source-shaped one-time
  evolution on the observed carrier — not ordinary Lorentzian Cauchy theory
  ported to ambient (7,7) Y14, which v0.168's fence separately forbids, and
  not the nonlocal ambient ultrahyperbolic route, which stays separately
  typed. The review uses the phrase in that same sense; no homonym slip
  found.
- **Typing of v0.170-v0.172.** The review types them as completed kills /
  conditional fallback ingredients of the old operator's repair era, which
  matches each artifact's own scope language. It does not erase them; the
  Craig-Weinstein ingredient is retained as fallback, consistent with
  v0.170's "conditional flat principal-domain ingredient" typing.
- **M-H9 status.** The register entry's current head is FALSIFIED AT TIER 1
  with "SIGNATURE-AMBIENT now has no named resolver"; the trailing
  "Remaining: Tier 1..." sentence belongs to the superseded earlier
  same-day status block. The review reads the current head, not the stale
  tail.
- **"No wave since 08-08 names a register item."** Checked by commit-log
  grep and by the v0.173 queue content (construction ranks only). A rank
  naming a register item inside a ledger JSON this grep missed would
  falsify the sentence; marked needs-recheck below.

## Charge 3 — what else must change if this stands

- `dissolved`: nothing — this review changes no row, verdict, residue,
  fork, canon or posture by construction.
- `survives`: the standing queue ranks 1-3; every kill and fence cited; the
  multi-writer ownership of the steering surfaces (this review deliberately
  does not touch them).
- `needs-recheck`:
  - whether any post-08-08 ledger `next_work_queue` rank names a register
    item (grep-level check only);
  - if the cadence adopts rank 4(b) (fork-posture decision), the fork
    registry needs a posture row and the fork-depth audit needs a
    priced/acknowledged state, or it will keep firing on a deliberately
    parked fork;
  - if rank-2 re-entry on the corrected operator fails, v0.170's
    Craig-Weinstein ingredient returns from fallback to live, and this
    review's needs-recheck-then-retire list must be re-scoped.

## Required lenses

- Layer-0: applied above (one-time evolution vs ambient ultrahyperbolic;
  operator-specific vs family-universal kills).
- Prior art / novelty: `lab/process/novelty-check.py` run on the review's
  operative terms; the register audit of 2026-08-08 and the eleven-lens
  stale-dashboard meta-pattern are cited as prior recordings, not claimed
  as new findings.
- Analytic lens (positivity claim): engaged above; flat-principal
  symmetrizer positivity is not a domain result.

No new trap filed: the route-ordering failure mode (locally-greedy queue
never asks the altitude question) is proposed for the queue's own
pre-flight text rather than the trap registry, since it is a scheduling
behavior, not a mathematical misstep.
