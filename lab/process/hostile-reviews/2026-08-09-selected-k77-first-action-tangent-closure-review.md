---
artifact_type: hostile_review
created: 2026-08-09
verdict: CANDIDATE_SURVIVES_WITH_SCOPE_NARROWING_AND_INDEXING_TRAP
reviewed_artifact: explorations/conditional-build/selected-k77-first-action-tangent-closure-2026-08-09.md
---

# Hostile review: selected K77 first-action tangent closure

## Pre-wave

No signature or parent fork is assumed; both exact algebraic branches are
tested and the selected Spin, two-`U(32,32)`-half and full-`U(64,64)` parents
remain distinct.  The finite search space is the complete `196 x 1274`
grade-one/grade-two block plus the `196 x 196` grade-one self block, so this
connection subproblem can be decided wholesale.  Success introduces no new
object.  It can kill only grade-one-to-grade-two leakage as a reason to reject
the minimum-known `321` completion.

## Charge 1: where the summary outran the artifact

The raw computation initially invited “the `321` tangent is closed.”  That is
too broad.  Only the first-action connection self and connection/grade-two
cross blocks were computed.  Metric/epsilon, epsilon/epsilon, derivative jets
and all expanded-parent Hessians remain open.  The report now says only that
`321` survives this connection-block gate and is neither selected nor complete.

## Charge 2: where rigor defended the wrong object

The provisional mixed matrix used the positional first `196` low-grade basis
elements as grade one.  Because the basis is interleaved by form slot, that
slice was `28` grade-one plus `168` grade-two directions.  Its apparent
rank-`177` off-slice leakage was a theorem about the wrong bank.  Grade-label
filtering plus an independent exact Euler finite difference removes it.

The review also found a superseded work target: v0.93 already owns the
expanded-parent closure dimensions and pairing multiplicities.  The next Run
must build parent Hessians, not recompute those dimensions.

## Charge 3: what changes if the result stands

- `dissolved`: grade-one-to-grade-two first-action leakage as an argument that
  the minimum-known `321` field set is immediately non-closed.
- `survives`: the `1,571` low-grade rival, source silence on tangent selection,
  all three action parents, and the distinction between first and
  residual-square actions.
- `needs-recheck`: metric/epsilon and epsilon/epsilon blocks, derivative jets,
  expanded-parent Hessians, gauge/ghost closure, `Dmax/Dmin`, Krein positivity,
  BV--BFV and observed physics.

## Required lenses

- `layer0_semantics`: PASS after narrowing; Hessian, residual Jacobian and
  residual-square Gram remain distinct.
- `prior_art`: PASS after removing the parent-multiplicity recomputation from
  the claimed novelty.
- `variational_bicomplex`: PASS; the analytic second variation is controlled
  by direct Euler finite differences.
- `representation_theory`: PASS after bank selection by grade labels.
- `symplectic`: PASS with fence; no quotient, boundary charge or phase space is
  inferred from the zero block.
- `analytic_krein`: PASS with fence; full rank and inertia `(97,99)` do not
  imply positivity, hyperbolicity or a closed operator domain.
- `source_criticism`: PASS; `SOURCE-SILENT` is recorded for tangent and parent
  selection.

## Verdict

`CANDIDATE_SURVIVES_WITH_SCOPE_NARROWING_AND_INDEXING_TRAP`.

The exact finite theorem is strong.  Its effect is deliberately small: it
removes one leakage mechanism and advances the operator dependency chain by
two named conditions.  No verdict, residue, quotient, datum, canon or public
posture moves.
