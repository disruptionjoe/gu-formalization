---
artifact_type: hostile_review
created: 2026-08-04
target: explorations/resolver-wave-k-conditional-active-shiab-b1-variation-2026-08-04.md
gate: RESOLVER-WAVE-K-NATIVE-SHIAB-MONOLITHIC-B1-VARIATION-AND-PORT-PLACEMENT
verdict: PASS_AFTER_MATERIAL_REPAIR_AND_SCOPE_NARROWING
---

# Resolver Wave K specialist pre-assessment and hostile post-review

## Pre-assessment

Three divergent lenses were applied before construction.

| lens | preregistered burden | kill condition |
|---|---|---|
| source/differential geometry | collide the displayed B1 grammar with the exact trace-reversed fibre and distinguish bosonic/spinor Shiab homonyms | source signature, bracket, trace insertion, or action type is silently changed |
| real Clifford/representation theory | distinguish `(7,7)` and `(9,5)` real carriers and test every attempted port before using spinors | a sign convention is treated as harmless after the real Clifford algebra is formed |
| variational/PDE | construct one action, differentiate it directly and by owners, retain a positive calibration and planted failures, and grade Green/Ward/domain separately | the action is zero/mass-only, routes disagree, or an owner identity is promoted to a theorem it did not compute |

The expected result was conditional: a nonvacuous local candidate could earn a
next gate, but any source identity, physical fermion result, or global Ward/
Green/domain claim required its own exact construction.

## Hostile review findings

### 1. Source and differential geometry

Verdict before repair: `REPAIR REQUIRED`.

- The source writes `(1/3)[T,T]`; the probe used `(1/3)T wedge T` without a
  bracket-normalization theorem. The mismatch could therefore obstruct only
  the repository `q_wedge` comparator, not Weinstein's source equation.
- The source holds its epsilon/Shiab object fixed in the displayed translation
  variation. A moving active Shiab owner may be a repository hypothesis but is
  not automatically owed by the source.
- The kappa/Hodge channel needed the same explicit `ad -> ad*` lowering wrapper
  as the Shiab output.
- The exact `(7,7)` ledger passed and remained the correct first successor.

Repair: renamed the quadratic map `q_wedge`, changed the source verdict to
`NOT_TESTED`, typed both degree-thirteen summands through `Dual13`, and removed
the claim that a moving source owner is required.

### 2. Variational and PDE

Verdict before repair: `REPAIRS REQUIRED`.

- Direct and owner B/T variations were exact and nonzero, but the B route
  needed an explicit nonzero assertion.
- The “direct Ward” check was tautological because it named the sum of two
  owner contributions as a direct derivative.
- The degree-thirteen port used one fixture, and its second claimed
  restrict/vary route was the first equality reversed.
- The wrong-degree plant never called the rejecting adapter.
- The selected Green channel was zeroth-order and could not earn a live
  preboundary or analytic-domain claim.
- A positive exact transgression calibration was needed to prove that the
  mismatch test could pass elsewhere.

Repair: added the nonzero B check, demoted Ward to a nonvacuous owner-
cancellation witness, removed the duplicate port claim, executed the bad-
degree adapter, retained the zeroth-order label, and imported Wave J's exact
positive cyclic calibration.

### 3. Clifford and representation theory

Verdict before repair: `REPAIR REQUIRED`.

- The first draft used `TAU=e8`, an arbitrary positive vertical direction.
  Wave I orders the normalized negative DeWitt trace at index `10`; adjacent
  machinery likewise uses `STRACE=e10`.
- Native-grade filtering is not `R_J` or a source-to-active port.
- The lowerer was tested on three bivectors, not globally.
- The port was one fixed-background fixture, not a rank-252/global theorem.
- A source `(7,7)` gate must rebuild `Cl(7,7)=M128(R)`, its real spinors and
  pairings; it may not flip signs inside the quaternionic `(9,5)` machinery.

Repair: rebuilt the action and fixtures with normalized `TAU=e10`, checked the
entire exercised output is public-`u(K)` typed and native-grade, and narrowed
the lowerer/port/covariance statements to their exercised panels.

## Rerun and final review

After the repairs, the normalized-trace probe passes `24 exact + 8 source +
20 type + 8 planted = 60` assertions. The
action remains nonzero, all four channels remain live, direct and owner
derivatives agree, and the candidate-local `q_wedge` mismatch remains nonzero.
The `(7,7)/(9,5)` signature ledger, source nonchirality/effective-chirality
receipts, strict `R_J` rejection, bad-degree adapter, omitted-owner controls,
and positive cyclic calibration all pass.

The final grade is deliberately narrow:

```text
NORMALIZED_TRACE_FIXED_ACTIVE_95_QWEDGE_B1_CANDIDATE_MISMATCH
```

It does not kill the source B1 equation. It does not construct the `(7,7)`
fermion carrier, derive effective Standard Model chirality, prove a Ward/Green/
domain theorem, or map individual particles. Those are the K77 successors.
