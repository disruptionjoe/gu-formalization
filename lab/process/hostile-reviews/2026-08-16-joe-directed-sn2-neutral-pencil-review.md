---
artifact_type: hostile_review
created: "2026-08-16"
targets:
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-source-native-neutrino-conditional-build-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-equation916-k77-neutral-restriction-2026-08-16.md
  - tests/channel-swings/joe_directed_sn2_equation916_k77_neutral_restriction_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-neutral-pencil-valuation-classifier-2026-08-16.md
  - tests/channel-swings/joe_directed_sn2_neutral_pencil_valuation_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-neutral-reality-charge-admissibility-2026-08-16.md
  - tests/channel-swings/joe_directed_sn2_neutral_reality_charge_probe.py
status: PASS_AFTER_FIVE_MATERIAL_SCOPE_REPAIRS__EXACT_NEUTRAL_COUNTS_AND_CONDITIONAL_PENCIL_CLASSIFIER_SURVIVE__NEUTRAL_CLOSURE_SCALE_REALITY_AND_COEFFICIENT_INCIDENCE_REMAIN_UNSELECTED
classification: SOURCE_NATIVE_ROUTE
mandatory_lenses: [source_criticism, equation_916_cell_custody, k77_carrier_count, q0_vs_sm1, f_occurrence, observation_invariance, newton_valuation, schur_smith, singular_jordan, centre_class_convention, charge_b_minus_l, local_k77_reality, grassmann_transpose, two_plus_one_chirality_custody, comparator_routing, conditional_build_authority, proof_engineering, reprioritization]
canon_verdict_change: none
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`. The reviewed object is Weinstein's
> candidate equation-(9.16) rolled `Omega1/Omega0` grammar on declared K77
> observed-neutral subcarriers. Standard `nu_L/nu_R`, `126`-VEV and textbook
> seesaw constructions appear only as non-adjudicating controls.

# Hostile review — SN-2 source-native neutral pencil

## Verdict

`PASS_AFTER_FIVE_MATERIAL_SCOPE_REPAIRS`.

The finite mathematics survives independent replay. The exact K77 carrier
ledger has three different levels which must not be collapsed:

| declared object | `Q0` | `SM1` |
| --- | ---: | ---: |
| four observed `2 x 16` blocks across both ambient halves | **16** | **8** |
| minimal four-field column | **32** | **16** |
| second-`F`-in-the-two-zeta-slots horn | **48** | **24** |

The first row is the observed four-block carrier count, the second includes
one `F` occurrence in each of the four source fields, and the third adds one
further `F` occurrence in each zeta slot. None is `20`. The row and column
carriers have equal dimensions, but barred and unbarred variables remain four
independent classical fields; equality of dimensions is not a reality map.

Under the separately declared `SN2-NEUTRAL-CLOSED` horn, the formal source
block pencil

```text
D(t) = [[A(t), B(t)],
        [-C(t), 0]]
```

has the stated scalar Newton classifier and, when `A` is unimodular over the
valuation ring, the exact block equivalence to `diag(A,C A^-1 B)`. A strict
bilateral nonnegative-integer fixed-heavy valuation pattern with a light
valuation two has the unique minimum `(v(A),v(B),v(C))=(0,1,1)`, provided the
leading composition is nondegenerate. The one-sided `(0,0,2)` and `(0,2,0)`
patterns have the same scalar root slopes but do not make both independent
cross legs small. Singular `A`, cancellation, protected zeros, delayed Smith
factors, fractional slopes and Jordan chains remain genuine rival branches.

The local charge/reality classifier also survives. `Q=0` keeps both the weak-
doublet `L` line and the all-left SM-singlet `N^c` line; `SM1` keeps only
`N^c`. In the all-left convention, `B-L(N^c)=+1`, while physical `nu_R` has
the conjugate charge `-1`. The southeast source cells split exactly into two
net centre-class-`0` positions and two net centre-class-`2` positions. There
is no uniform class-`2` southeast rule, and centre class is not `B-L`.

What does **not** survive is any stronger reading. The source does not select
`Q0` or `SM1`, one or two `F` occurrences, neutral closure, the southeast
branch, relative scales, a nondegenerate composition, either local pairing
horn, a local reality graph, an `X_D` or `X_R` coefficient, or a source-slot-
to-observed-line incidence. The calculations are exact conditional carrier
and pencil algebra. They are not a neutrino mass, Majorana construction,
hierarchy, pole, family count or prediction.

## Independent replay

The three new probes and every available hostile/selftest mode were rerun
without editing the execution artifacts:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_sn2_equation916_k77_neutral_restriction_probe.py
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_sn2_equation916_k77_neutral_restriction_probe.py --selftest
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_sn2_neutral_pencil_valuation_probe.py
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_sn2_neutral_pencil_valuation_probe.py --selftest
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_sn2_neutral_reality_charge_probe.py
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_sn2_neutral_reality_charge_probe.py --selftest
```

All exited zero. The neutral-restriction probe reports `51` checks and rejects
`9/9` mutations. The valuation probe reports `108` checks, including its six
planted mutants; it has no separate selftest branch, so the `--selftest` call
replays the same complete suite. The reality/charge probe reports `35` checks
and rejects `9/9` mutations.

The evidence ceiling remains important. Much of each executable is exact
arithmetic plus source-string and artifact-regression custody. This is strong
evidence for the finite ranks, cell ledger, Newton slopes, matrix identities,
charge table, centre split and Grassmann signs. It is not evidence that any
declared horn or coefficient exists in the source geometry.

## Twenty-lens hostile attack

### 1. Primary-source criticism — survives

The draft says the four fields can begin with operators “like” equation
(9.16), and the later spoken seesaw wording is explicitly prospective. The
released southeast zero is a preferred candidate shape, while the draft also
admits nonzero-southeast rivals. SN-2 preserves that strength. It does not
upgrade the matrix to a unique operator or the spoken expectation to a result.

### 2. Equation-(9.16) cell custody — survives

The row order `(bar-zeta-,bar-zeta+,bar-nu-,bar-nu+)`, column order
`(zeta+,zeta-,nu+,nu-)`, twelve permitted cells and four southeast zeros agree
with the rendered extraction. Four unbarred `varpi_ab` owners recur across the
upper quadrants, while four barred/formal-star partners remain independent in
the lower-left quadrant. “Twelve permitted cells” does not mean twelve
independent scalars or twelve nonzero restricted maps.

### 3. K77 carrier-count lens — survives

The arithmetic `16/8`, `32/16`, `48/24` follows from four complex `2 x 16`
blocks, two versus one retained internal neutral weights, and the declared
field/F-occurrence multiplicities. Both ambient halves are present. The count
is complex rank on the declared observed subcarrier; it is neither a real
dimension, a family multiplicity nor a physical degree-of-freedom count.

### 4. `Q0` versus full-SM-singlet restriction — survives

`L` is electrically neutral but is an `SU(2)_L` doublet with `Y=-1/2`.
`N^c` is the only full SM singlet in the one-`16` dictionary. Therefore
`Q0 != SM1`. Neither projector is selected by the source, and neither is an
invariant Spin(10) subrepresentation.

### 5. `F`-occurrence ambiguity — repair required

The rank `48/24` is obtained by retaining the second **F occurrence** in each
zeta slot. It is not the rank of the full `Omega1` carrier or the full
gamma-traceless `832`. The read packet's horn wording “full `Omega1` including
the second occurrence” is too broad and must be replaced by “both declared F
occurrences in each zeta slot.” Any Z/RS remainder remains outside this count.

### 6. Observation, compression and invariance — repair required

A fixed observation map can kill a leading coefficient and raise its
valuation. A compression `PDP` need not be an invariant restriction, and its
eigenvalues need not be source-operator eigenvalues. `SN2-NEUTRAL-CLOSED` is
therefore load-bearing, not a theorem derived from neutral K77 weights.

Because the source row and column fields are independent, the horn should be
typed with separate row and column restriction/return maps and the required
intertwining equations for `A`, `B` and `C`. Any regular identification used
to form a square formal pencil must also be named. This does not impose a
reality relation; it prevents the phrase “invariant image” from hiding a
row/column type mismatch.

### 7. Block Newton/valuation lens — survives

For the scalar polynomial `lambda^2-a lambda+bc`, the lower Newton polygon
gives the stated two branches. The exhaustive triples agree. The result
classifies a declared valuation ray; it does not select the ray, relate the
independent `B` and `C` coefficients, or manufacture the fixed unit `M`.

### 8. Schur/Smith lens — survives

For `A in GL_r(k[[t]])`, exact unimodular row and column operations give
`diag(A,C A^-1 B)`. Thus kernel and light Smith data are controlled by the
composition, not by nonzero `B` and `C` separately. This is equivalence of
matrix pencils, not similarity, so Smith statements must not be restated as
global spectral equivalence.

### 9. Singular and Jordan lens — survives

The fixtures correctly retain mixed fixed/direct sectors for singular `A`,
exact zeros versus delayed modes, nilpotent zero Jordan chains and the
equality-wall Jordan block. Root valuations alone do not determine geometric
multiplicity, diagonalizability or an analytic particle pole.

### 10. Centre-class convention — survives and must be repeated exactly

With row classes `(1,3,1,3)`, column classes `(3,1,3,1)` and the action-
bilinear convention `c_cell=-c_row-c_col mod 4`, the southeast entries are:

```text
(bar-nu-,nu+) : 0      (bar-nu-,nu-) : 2
(bar-nu+,nu+) : 2      (bar-nu+,nu-) : 0.
```

Hence the split is exactly two class `0` and two class `2`. The statement that
a class-`2` insertion can exchange D5 `16/16bar` parents is a different typed
fact and must not be shortened to “the southeast is class 2.”

The apparent cross-file collision is therefore adjudicated rather than
counted as a sixth repair. The neutral-restriction artifact's statement that a
generic **D5 adjoint/class-0 internal coefficient** preserves the parent
`16/16bar` sector is a safely scoped parent-representation control. The
reality artifact computes the different **ambient action-cell** character:
the relevant connection component has class `2`, the one-form leg has class
`2`, and their action-cell sum is net class `0`. Both statements can remain,
provided synthesis keeps the labels `D5 parent-module control` and `D7
action-cell net class` explicit. Neither statement licenses a uniform
class-`2` southeast summary.

### 11. `B-L`, SM charge and all-left convention — survives

`Q`, weak type, hypercharge, `B-L` and ambient centre class are independent
tests. `L N^c` is `Q`- and `B-L`-neutral but still requires a dual weak
doublet of `Y=+1/2`. `N^cN^c` is an SM singlet but carries `B-L=+2` and needs
an opposite-charge coefficient. The physical `nu_R` convention reverses that
last sign. None of this proves that `B-L` is broken or selected.

### 12. Local K77 reality and pairing — repair required

The two banked pairing horns and their noncharacteristic graded fixed graphs
are exact local options. The source selects neither horn nor the remaining
pairing invariant. The section titled “a noncircular local quotient exists”
must say **local fixed graph/reality restriction**, not quotient. Taking a
fixed locus is not forming a quotient, and the probe's result label must not
say the independent fields are “conditionally quotiented.” Global reality,
null/characteristic closure and analytic domain remain open.

### 13. Grassmann-transpose lens — survives

For symmetric `P`, `PX+X^T P=0` makes `PX` alternating. For skew `P`,
`PX-X^T P=0` also makes `PX` alternating because `P^T=-P`. These are the
correct local Grassmann conditions. They do not imply that the source star is
the corresponding transpose, that `C=B^T`, or that an arbitrary new `X_D` or
`X_R` insertion passes.

### 14. `2+1`, imposter and emergent-chirality custody — survives

`zeta` and `nu` are `Omega1` and `Omega0` source fields, not standard
`nu_L/nu_R` labels. Ambient half, four-dimensional Weyl chirality, form degree
and effective low-curvature chirality remain separate. Both ambient halves
are retained. No rolled-block count becomes three families, and source F,
family multiplicity and the Z/internal-`144` partner are not identified.

### 15. Comparator-routing lens — survives

The artifacts carry the required notice and are source-native. Conventional
`126`, textbook seesaw and old `Cl(9,5)` probes are novelty/adverse controls
only. Their success or failure cannot move the vote on this K77/equation-(9.16)
route without the six-part typed bridge required by the routing method.

### 16. Conditional-build authority — survives with a strict stop fence

Declaring `Q0|SM1`, the F-occurrence horn, `SN2-NEUTRAL-CLOSED`, `SE0`, a
valuation ray, a pairing horn or a coefficient type is allowed. Deriving an
action, vacuum, selector, external scale, reality owner or symmetry-breaking
datum is not. Missing owners are dependency fences, not the next assignments.

### 17. Coefficient-incidence lens — repair required

The reality/charge artifact classifies minimum **type requirements** for
`X_D` and `X_R`; it does not establish either coefficient or attach `L/N^c`
to a particular equation-(9.16) source slot. Its headline and successor must
therefore say “not excluded after a declared slot-to-observed incidence,” not
suggest an admitted neutral bilinear already exists. The only admissible next
census is a decomposition of already-admitted `varpi` coefficient types. It
may not introduce a conventional scalar owner or a new source coefficient.

### 18. Claim-status and dependency wording — repair required

The phrase `dependency-free` in the execution grades is misleading. The
arithmetic is exact, but the physicalized restriction depends on banked K77
receipts and declared neutral, F-occurrence, closure, southeast, scale and
reality horns. Use `EXACT CONDITIONAL ARITHMETIC` and list the horns. This is a
status repair, not a mathematical downgrade.

### 19. Proof-engineering lens — survives with a regression ceiling

The probes are deterministic, exact and mutation-sensitive. The valuation
suite genuinely checks exhaustive small valuation triples and explicit block
identities. Several source, wording and ceiling checks are string assertions;
they prevent known regressions but cannot prove source geometry, closure,
coefficient existence or global descent. No test count should be used as
evidence that an unselected horn is natural.

### 20. Reprioritization lens — the wave is fertile once more, then must stop

SN-2 has produced a sharper conditional classifier than SN-1: it identifies
the exact neutral carrier alternatives, the necessary closure horn, the
valuation patterns, the singular rivals and the minimum local coefficient
types. The remaining finite source-native question is whether the already-
admitted connection representation contains either typed coefficient packet
on a declared source-slot/neutral-line incidence. That is a bounded
representation census, not an owner search.

If that census returns no packet, or if the next step would require selecting
closure, relative scales, a reality horn, a vacuum or a new coefficient, pause
the neutrino lane. Do not respond by importing a `126` or manufacturing a
scale relation.

## Exact mandatory repairs for integration

1. Normalize the horn vocabulary to `SN2-NEUTRAL = Q0 | SM1`; either replace
   `H-Q0/H-SM1` or declare them exact aliases once. Do not let two namespaces
   look like two additional assumptions.
2. Replace “full `Omega1` including the second F occurrence” with “both F
   occurrences in each zeta slot.” State that `48/24` is an F-only subcarrier
   count, not the full `Omega1` or `832` rank.
3. Type `SN2-NEUTRAL-CLOSED` using separate row/column restriction-return maps
   and their `A/B/C` intertwining equations. Continue to call it declared and
   unproved.
4. Replace “local quotient” and “conditionally quotiented” with “local fixed
   graph/reality restriction.” State that neither pairing/reality horn is
   selected and no global domain follows.
5. Replace `dependency-free` with `exact conditional` where the grades depend
   on declared horns, and cap `X_D/X_R` as type requirements pending an actual
   source-slot-to-observed-line coefficient-incidence census.

These repairs do not alter any finite rank, Newton slope, Smith identity,
centre-class cell, charge assignment or Grassmann sign.

## Exact claim ceiling after repair

The strongest supported statement is:

> On the source-aligned real-K77 four-block observation carrier, the declared
> `Q0` and `SM1` restrictions have exact complex ranks `16/8`; the minimal
> four-field F subcarrier has ranks `32/16`; and the horn retaining both F
> occurrences in each zeta slot has ranks `48/24`. Under a separately declared
> typed `SN2-NEUTRAL-CLOSED` restriction and the released `SE0` branch, the
> formal `A/B/C/0` pencil has the stated scalar Newton classifier and exact
> invertible-`A` Schur/Smith reduction. A strict bilateral integer fixed-heavy
> `t^2` pattern minimally requires the unselected valuation horn `(0,1,1)` and
> nondegenerate leading composition. The two banked local K77 pairing horns
> make specified `X_D/X_R` types locally admissible only after a declared
> source-slot/observed-line incidence. No horn, scale, reality condition or
> coefficient is selected or constructed.

No claim is earned about a physical neutrino mass matrix, Dirac or Majorana
mass, seesaw hierarchy, pole, mixing matrix, family count, threshold,
neutrinoless double beta decay, action, vacuum, global domain or empirical
prediction.

## Reprioritization

### Within the neutrino path

| item | priority | fertility | disposition |
| --- | ---: | ---: | --- |
| existing-`varpi` observed coefficient-incidence census for `X_D/X_R` | 1 | `8/10` structural | one bounded next wave; use only already-admitted coefficient representations and declared incidence horns |
| exact SN-2 carrier/Newton/Smith/Jordan classifier | 2 | `9/10` as banked structure | integrate after the five scope repairs |
| actual ranks of twelve neutral block maps | reopen only | `4/10` | requires concrete restricted coefficients and closure; do not fabricate them |
| selection of `Q0/SM1`, F occurrence, `SE0`, relative scales or reality | dependency fence | not scored | off limits unless supplied independently |
| physical mass/pole/phenomenology | closed here | `0/10` | requires quotient, domain, background and analytic owners absent from SN-2 |

### Relative to the other exploration paths

The neutrino lane is now the best active lane for one more bounded source-
native wave because it has a finite coefficient-incidence question that does
not yet require action or external-datum construction. The H210 derivative
adapter remains paused after CB-8; its isolated zero-order CB-2--CB-6 chain is
the strongest banked H210 result, but continuation requires another unowned
first-jet horn. The extra-vector lane remains retired because its remaining
attachment is action-selection work.

Priority therefore becomes:

```text
1. one finite SN-3 existing-varpi X_D/X_R incidence census;
2. retain the isolated H210 CB-2--CB-6 result as banked/reopen-only;
3. keep extra vectors retired.
```

After the incidence census, reprioritize again. Do not extend the neutrino
path merely by adding another neutral, scale, reality, coefficient or vacuum
horn.
