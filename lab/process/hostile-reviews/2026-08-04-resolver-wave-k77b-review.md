---
artifact_type: hostile_review
created: 2026-08-04
target: explorations/resolver-wave-k77b-source-bracket-displayed-shiab-b1-variation-2026-08-04.md
gate: RESOLVER-WAVE-K77B-SOURCE-BRACKET-NORMALIZATION-DISPLAYED-SHIAB-AND-B1-VARIATION
verdict: PASS_AFTER_MATERIAL_NORMALIZATION_AND_SCOPE_REPAIR
---

# Resolver Wave K77-B specialist pre-assessment and hostile post-review

## Pre-assessment

Three divergent specialists fixed the burdens before the construction.

| lens | preregistered burden | kill condition |
|---|---|---|
| source/differential geometry | separate connection difference, spacetime torsion, density, Euler covector, and observed equation; collide every decisive result with the draft | a source paraphrase is treated as a typed map, raw products silently leave `ad`, or a source-silent selector is called selected |
| variational/PDE | normalize the quadratic, differentiate the same B1 action independently, distinguish live from vacuous fixtures, and retain derivative/Green debt | a top-form density is called its endpoint, a zero/zero channel is promoted, or a constant algebraic fixture is called a full Euler/Green theorem |
| real Clifford/representation theory | work in exact `Cl(7,7)`, certify the real adjoint and invariant multiplicities, and keep K95/right-H machinery out | a B-self output is called adjoint-valued, low-grade Phi is called unique, or complex multiplicity is promoted to real equivariance without a check |

Expected verdict:

```text
SOURCE_BRACKET_NORMALIZED
LITERAL_OR_CANONICAL_LOW_GRADE_RAW_MAP_MAY_FAIL_AD_CODOMAIN
CORRECTED_FAMILY_SELECTION_REMAINS_OPEN
```

## Initial construction receipt

The first deterministic run passed `21 exact + 1 Sage + 6 source + 12 type +
8 planted = 48` checks. All three hostile reviewers returned
`REPAIR_REQUIRED`; none challenged the source bracket normalization or the
canonical low-grade raw-map codomain counterexample.

## Hostile post-review findings and repairs

### 1. Product normalization was materially wrong

The initial executable used

```text
(XY-YX)/2
i(XY+YX)/2
```

while the draft appendix defines the products without halves. This was not a
common scale: the first Shiab summand contains one exercised product node and
the second contains two. The initial endpoint numbers therefore could not be
attributed to the source normalization.

Repair: both products now use the exact unhalved appendix definitions and the
entire bank was rerun. The qualitative classification survives: six channels
are nonvacuous counterexamples and two are zero/zero controls. The numerical
rows changed and the report now carries the repaired values.

### 2. Source permission had been overstated

The draft emits commutator and `i`-symmetric operations, but does not choose an
operation independently at each of the three product occurrences in equation
(9.3).

Repair: the eight-member bank is now `SOURCE_INSPIRED_NODEWISE_BANK`, not a
source-selected or source-permitted family. The source collision is
`SOURCE_CONFIRMS_MAGIC_BRACKET_FREEDOM`, and candidate kills apply only to the
explicit maps constructed by the probe.

### 3. The invariant family was too broadly named

Sage certifies two complexified copies each of the vector and bivector modules,
but that alone did not certify the displayed grade-13 and `i`-grade-12
real-adjoint maps or exhaust every Shiab built from contraction, volume,
bracket, symmetric product, and sums.

Repair: all four low/high Phi copies now pass exact infinitesimal equivariance
for all 91 `so(7,7)` generators, have disjoint coefficient grades, and lie in
the real B-skew adjoint. The report calls them the full Phi carrier *inside the
displayed ansatz*. A broader source-natural rival table is mandatory at the
next gate.

### 4. Adjoint closure was only sampled

The first run evaluated closure on one curvature fixture. That did not by
itself prove closure for arbitrary B-skew inputs.

Repair: the implemented B-adjoint anti-involution is exhaustively checked on
all 16,384 Clifford blades times all fourteen generators. The universal
identities then show that both the commutator and `i` times anticommutator
preserve B-skewness. The original sampled liveness bank remains as a separate
calculation.

### 5. Connection and curvature typing needed narrower fences

`T=A-B` had been recorded as source-established, and the proposed
3,185-dimensional algebraic-curvature selector had been called the whole
curvature module.

Repair: `A=B+T` is `SOURCE_DERIVED/UNCERTAIN`, conditional on matching the
draft's reference-connection notation. The next gate must construct an
explicit Spin/soldering injection of the algebraic Riemann-curvature module
into `Omega2(Y,ad P)` and impose the Bianchi restriction before using that
submodule as a selector.

### 6. Domain and reality controls were incomplete

The fixtures looked typed but did not fail closed on their degree or adjoint
membership, and ad closure did not guarantee that every sampled B1 density was
real.

Repair: both curvature inputs are checked as degree-two real-adjoint-valued;
every `T` and `delta T` is checked as degree-one real-adjoint-valued; and all
sixteen channel/fixture densities are exactly real. Derivative, `d_BT`, Green,
moving-epsilon/Hodge, metric Euler, Noether/BV, domain, observation, and
physics remain explicitly open.

## Repaired rerun

The stabilized primary probe passes:

```text
28 exact + 1 Sage + 6 source + 12 type + 8 planted = 55 PASS
```

The K77-B scope audit, compilation, JSON uniqueness, inventory checks, and
diff hygiene pass.

## Final specialist reruns

All three specialists returned `PASS` after the material repairs.

- Source/differential geometry confirmed the unhalved operations, source-
  inspired attribution, conditional `A=B+T` identity, displayed-ansatz versus
  broader-rival boundary, typed curvature injection debt, and fixture domains.
- Variational/PDE confirmed all six live endpoint counterexamples, both
  vacuity controls, all sampled density-reality checks, mass-channel
  cancellation, and the open derivative/Green/moving-field/domain boundary.
- Real Clifford/representation theory confirmed all 91-generator Phi checks,
  the complexified Sage multiplicities, universal B-adjoint closure, narrow
  canonical-low-grade raw kill, and K77/K95 separation.

Final verdict: `PASS_AFTER_MATERIAL_NORMALIZATION_AND_SCOPE_REPAIR`. This is a
source-normalization and candidate-map classification gate, not a selected B1
action or physical field-equation result.
