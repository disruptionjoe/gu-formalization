# Hostile review: K77 Wave 2 up/back/over target

Date: 2026-08-04
Disposition: **PASS AFTER SCOPE FENCES**

## Charge 1: find where the summary outruns the artifact

The exciting reading was: “Eric’s missing square has been reconstructed.” That
overstates the evidence. The exact algebra verifies one uniquely sign-compatible
matrix under explicit curvature and mixed-Bianchi relations. The 2025 source
says the object was never released and recalls it tentatively. The report was
therefore repaired to say **source-bound reconstruction with exact algebra**,
not source-exact GU operator.

Likewise, the universal `Delta^2` block formula does not construct the mixed
maps `U,V`, a common action variation, or an observed Dirac equation. Those
claims are forbidden.

## Charge 2: find where the lane defends a stale or superseded object

The tempting stale move was to reuse the trace-`q` left/right pair as the
missing mixed paths simply because they were the latest surviving K77 maps.
Layer 0 rejects that identification: the bosonic connection difference is
zeroth-order, the trace-`q` family is a fermionic principal-symbol repair, and
the source diagram's cross maps mix `/S` and `ad` carriers.

The exact path test then makes the rejection computational: both plus and minus
path response matrices have rank two, so no nonzero projective trace-`q`
coefficient works in that direct adapter.

## Specialist hostile checks

### Representation/type review

Pass after making `B`, `F`, `U`, and `V` explicit. The review rejects any direct
identity between `d_A-d_B` and `gamma(q)A`. Equation `10.10` remains a mixed-cell
inventory, not a stable pure-RS rule.

### Variational/PDE review

Pass with boundary. Cancellation of the principal differential paths in a
formal square is not a domain, hyperbolicity, Green identity, or physical field
equation. The next gate must derive `U,V` from the common action and then inspect
their actual differential orders.

### Source/provenance review

Pass after adding the mandatory collision: Portal confirms the architecture and
unfinished cancellation burden; TOE bounds the sign reconstruction but also
says it is unreleased; the draft caveat blocks promotion. The older repo cell
audit is cited so this work is synthesis, not a false novelty claim.

## Planted controls

All eight plants fired correctly:

- a one-minus sign placement was rejected;
- a self-derived fermion square was not called independent;
- connection difference was not relabeled trace-`q`;
- partial cancellation did not promote the gate;
- no positive Hilbert pairing replaced the Krein structure;
- caveated equation `10.10` was not stabilized by assertion;
- P1/P2/P3 were not spent on missing maps;
- no particle or field-equation recovery was claimed.

## Final hostile verdict

The packet may claim:

1. the exact two-connection cancellation identity at source-bounded
   reconstruction grade;
2. the exact universal two-complex totalization formula;
3. rank-two failure of the direct K77 trace-`q` path adapter;
4. the narrowed next object: stabilized action-derived mixed maps `U,V`.

It may not claim a source-exact cyclic GU operator, a selected K77 coefficient,
a completed source action, an observed Dirac equation, a physical domain, use
of P1/P2/P3, or permission to open Wave 3.

Final rerun: **51/51 PASS**.
