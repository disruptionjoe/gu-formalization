---
artifact_type: hostile_review
created: 2026-08-13
status: SCOPED_SURVIVAL
target: selected-k77-i2b-frozen-hessian-compatibility
lenses: [layer0_semantics, prior_art, variational, spencer_pde, principal_bundle, hyperbolic, krein, symplectic, source, contrary]
---

# Hostile review: frozen-Hessian compatibility

## Verdict

`SCOPED_SURVIVAL`. The exact statement “no constant lower-order correction
extends the principal compatibility through the frozen residual-square
Hessian” survives. Any stronger statement about the complete stationary,
moving, covariant, nonlinear, Noether, or BV system is rejected.

## Charge 1 — where could the summary outrun the artifact?

The frozen computation omits derivatives of `Q_B`, `H_q`, Shiab, the section,
observation and the affine source connection. Those are precisely the terms a
covariant divergence can use. The rank-56 defect is therefore a requirement
on that moving completion, not a theory-level inconsistency.

Likewise, `H0` full rank is an algebraic Hessian statement at one conditional
branch. It is not a physical mass spectrum, positive energy, or stability
theorem. The branch is nonstationary on the unrestricted 196-cell bank.

## Charge 2 — is rigor defending a mistyped object?

The principal `Cl1` compatibility family remains distinct from the source's
rank-25 `Cl2` adjoint gauge image. The calculation does not relabel it gauge.
It also keeps the stationary principal two-jet distinct from the complete
coefficient derivative of the stationary linearized action.

The source `Q_B` remains unconstructed. The already-built fixed residual
pairing may be used for this scoped comparator, but it cannot silently settle
the source action's moving primalizer.

## Charge 3 — what survives, dissolves, or needs recheck?

- **dissolved:** a constant algebraic `C0` repair;
- **survives:** moving covariant completion through all existing coefficient
  owners;
- **survives:** nonzero preboundary contact and the need for BFV reduction;
- **needs recheck:** arbitrary-field stationary coefficient derivatives;
- **needs recheck:** whether the covariantized family is a source Noether/BV
  identity;
- **survives:** global descent, higher Spencer, hyperbolic/Krein domain,
  physical carrier, spectrum and stability burdens.

## Independent controls

- the zeroth-order Hessian is exact symmetric rank `196`;
- all four first-order cross blocks vanish exactly;
- `B00+B01` spans all 196 equation rows, forcing `C0=0`;
- the four remaining degree-one defects have ranks `14,14,14,14` and combined
  rank `56`;
- a nonzero-`C0` plant breaks the principal equation and a missing-`H0` plant
  erases the real obstruction.

No ledger row, canon verdict, residue, quotient count, external datum, or
public posture changes.
