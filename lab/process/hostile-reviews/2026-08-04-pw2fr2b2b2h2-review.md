---
title: "Hostile review: PW2F-R2B2B2H2 I2B residual/primalizer/pairing second jet"
status: complete_after_repair
doc_type: hostile_review
updated_at: "2026-08-04"
---

# Hostile review: PW2F-R2B2B2H2

## Reviewed claim

The scoped conditional active fixed-background `I2B` residual square has an
exact second residual/primalizer/pairing jet whose direct mixed coefficient
equals the complete five-family off-shell Hessian product rule.

## Differential-geometric and type review

Pass after repair. The probe keeps the residual 13-form density, external
Hodge primalizer, internal Clifford/Krein scalar pairing, and the action
Hessian families distinct. Its base norm independently matches the existing
full-carrier coordinate injection at `981/64`, and both first pairing slots
match the accepted `dstar` constructor. Hodge square is checked on the actual
residual rather than inherited by assertion.

The first draft wrongly required the intrinsic mixed pairing slot to be
nonzero. Exact evaluation gives pairing jet `(981/64,0,4293/128,0)`. This is
not vacuous because one first slot is live and the complete mixed action is
live. The acceptance condition was repaired to test exact five-family
reassembly without preselecting each family's value.

## Variational and PDE review

Pass at second-jet grade. The direct mixed action is `-103/256`. Independent
route assembly gives `-409/1024 + 3/1024 + 0 - 3/512 + 0`, exactly the same
value. Normal, residual-second, and one first-moving-primalizer family are
live. Frozen-primalizer and omitted-mixed-residual controls both change the
answer.

This is not a quartic tensor bank. No multi-index formal adjoint, Green
concomitant, Helmholtz quotient, or projective classifier is inferred.

## Source and Layer-0 review

Pass with narrow attribution. The pinned source confirms the `I1` grammar;
the repository reconstruction separately preserves the manuscript `I2B`
residual-square glyph. The active `(9,5)` residual/primalizer jet is
repository-derived and is not identified with the unported `(7,7)` pairing.

The fixed-background operator-induced residual is also kept distinct from the
complete global/source-epsilon curvature graph. That graph remains the first
open dependency before complete coefficient-bank assembly.

## Disposition

Accepted only as
`CONDITIONAL_ACTIVE_FIXED_BACKGROUND_I2B_SECOND_RESIDUAL_PRIMALIZER_PAIRING_JET_CLOSED`.

P1/P2/P3 remain unchanged and unused. Curt remains formally separate inside
the Eric lane. `TG-1 AND TG-2 AND TG-3` remains not promoted. No canon,
protected claim, public posture, publication, or physics status changes.
