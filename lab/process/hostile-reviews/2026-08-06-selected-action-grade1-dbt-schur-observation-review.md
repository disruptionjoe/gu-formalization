---
artifact_type: hostile_review
created: 2026-08-06
target: explorations/conditional-build/selected-action-grade1-dbt-schur-observation-2026-08-06.md
disposition: ACCEPT_EXACT_FINITE_RESULT__REQUIRE_LT_GR1_DEMOTION__REFUSE_PHYSICAL_OR_RESIDUE_PROMOTION
---

# Hostile review: grade-one Schur and observation completion

## Two-sided Layer-0 charge

### Where the summary initially outran the artifact

The first exploratory calculation Schur-complemented only the formal
`d_B T` block.  That was exact for the wrong cross.  The curvature term
`<T,S(F_B)>` adds `F L delta-g`, so the source-variable metric cross is
`(F-E)L`, not `-E L`.  The report and durable probe use the repaired full
cross and explicitly preserve the failed partial route as a control.

### Where rigor could have defended a superseded object

The 34-variable metric-plus-horizontal-`Cl2` carrier was already superseded
by the source's full adjoint one-form once the adjacent `Cl1` Euler block was
found.  Proving the graph theorem more strongly would not repair that carrier.
This wave includes the 196-dimensional grade-one bank instead of defending
the obsolete truncation.

## Mandatory symplectic review

The Schur complement integrates out a finite nondegenerate algebraic block.
It is not a presymplectic reduction, BV cohomology, BFV phase space or
physical-state quotient.  The first-order `d_B T` density still owns a Green
concomitant.  No characteristic kernel is promoted to a transition amplitude
or positive state before the covariant presymplectic kernel, boundary
conditions and Green form are constructed.

## Variational/PDE review

- Accepted: `T=varpi-B_LC(g)` is applied before ranks are read.
- Accepted: curvature and `d_B T` metric crosses are both included.
- Accepted: `F L D=0` and `W[D;LD]=0` close exactly.
- Accepted: the Fourier Schur convention gives `P_0-Q` at `kappa_1=1`;
  multiplying the general operator by nonzero `kappa_1` gives the kernel-
  equivalent pencil `zP_0-Q`, `z=kappa_1^2`.
- Open: the finite symbols do not establish a common closed domain.

## Representation/Krein review

The `1+104+91` projectors are complete and multiplicity-free.  Their analytic
inverse reproduces all three selected eigenvalues.  An exhaustive hostile
rerun checks all `38,416` matrix entries.  The `(97,99)` inertia is a warning:
algebraic invertibility does not supply positivity.

## Source and observation review

The primary-source packet supports the full connection-difference carrier and
the need to retain horizontal plus vertical observed coefficients.  It is
silent on the completed Hessian, Schur pencil and physical modes.  Horizontal
pullback does not erase the cross; adding the vertical receiver raises the
rank by one.  This is preservation, not global faithfulness.

## Coefficient-locus review

Independent Sage factorization confirms the determinant factors and exact
number-field nullities.  The positive `N2` root is the only positive locus
with two extra null modes and shares no factor with the timelike/spacelike
determinants.  However its metric projection is not the old graph TT plane
modulo gauge.  Therefore:

- accept `DISCRETE_TWO_MODE_CAUSAL_CANDIDATE`;
- reject `PHYSICAL_GRAVITON_RECOVERED`;
- reject any reduction of the 84 continuous coordinates;
- reject booking a new finite fork until little-group/Green/domain typing.

## Ledger disposition

The old `LT-GR1 SAME/DERIVED_CONDITIONAL` reading depended on the graph-only
kernel surviving full source completion.  Its own revival trigger fires.
Move it to `NEEDS/MISSING_CONSTRUCTION`.  This is not an ultimate GU kill:
the exact `N2` locus and unassembled source/domain banks are revival routes.
The other four touched rows receive distance-only migrations.

## Reruns

- primary exact probe: `54/54 PASS`;
- independent Sage factor/kernel audit: `18/18 PASS`;
- exhaustive `196^2` Hessian comparison: `55/55 PASS` including the added
  exhaustive assertion;
- ledger migration: `47/47 PASS`;
- both scope audits, inventory and predecessor regressions: PASS before
  closeout.
