---
artifact_type: hostile_review
created: 2026-08-09
verdict: CANDIDATE_SURVIVES_WITH_TWO_LAYER0_CORRECTIONS
---

# Hostile review: selected K77 lower-order source blocks

## Pre-wave

- Fork assumed: the already-declared conditional selected K77/Spin-native
  branch only; no physical-signature or action-parent settlement.
- Search space: six rational samples determine every quadratic coefficient on
  91 epsilon directions; two algebraic branches and the prior 10-dimensional
  metric block are decidable wholesale in that finite scope.
- New unowned object on success: none.
- What dies: only the claims that these two selected lower-order blocks remain
  unbuilt.  Expanded parents, complete Hessians and analytic realization do
  not die.

## Charge 1 — where the summary outran the artifact

The initial summary called the v0.111 branch equation a generic
"raw-residual-zero" fact while differentiating the first transgression
packet.  That is the wrong object: the raw residual uses `F_A`, not the
`1,1/2,1/3` path-average packet.  A second draft then inferred zero Jacobian
from zero residual.  Both statements were rejected before disposition.  The
surviving artifact states the actual nonzero rank-91 derivative.

## Charge 2 — where rigor defended a superseded object

v0.119 listed the lower-order metric block as missing although v0.95 already
contained its complete local fixed-`varpi` theorem.  Recomputing it would have
defended a stale frontier.  The present wave instead checks the missing
premise—both new branches have `Upsilon*=0`—and ports the old theorem.

## Charge 3 — what changes if this stands

- `dissolved`: selected branch-dependent lower-order raw-residual epsilon
  block; selected fixed-`varpi` lower-order metric port.
- `survives`: complete first-action Hessian, residual-square Hessian,
  first-action endpoint charge, principal `-q eta`, branch choice and action
  choice.
- `needs-recheck`: both blocks on each `U(32,32)` half and full `U(64,64)`,
  complete nonhomogeneous parent operator, gauge/ghost complex, Riesz/domain,
  Green/Krein and coupled BV-BFV.

## Required lenses

- `layer0_semantics`: **PASS after repair**. Raw residual, transgression
  packet, action Euler, Jacobian and endpoint momentum are separately typed.
- `prior_art`: **PASS after repair**. v0.95 is composed, not rebuilt.
- `symplectic`: **PASS**. A raw residual Jacobian does not erase the nonzero
  first-action endpoint moment map.
- `analytic`: **PASS WITH FENCE**. Finite exact ranks make no positivity,
  closed-domain, Green, determinant or reflection-positivity claim.
- `representation`: **PASS WITH FENCE**. Spin 91, two `U(32,32)` halves and
  full `U(64,64)` remain distinct.

Verdict:
`CANDIDATE_SURVIVES_WITH_TWO_LAYER0_CORRECTIONS__LOWER_EPSILON_RANK91_BOTH__METRIC_PRIOR_ART_PORTED`.
