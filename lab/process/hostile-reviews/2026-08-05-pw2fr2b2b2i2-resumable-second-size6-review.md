---
artifact_type: hostile_review
created: 2026-08-05
target: PW2F-R2B2B2I2-RESUMABLE-SECOND-UNCOVERED-SIZE6-FULL-EVALUATOR
verdict: PASS_AT_APPEND_ONLY_EXACT_5_OF_380_ONLY
reviewers:
  - finite-group orbit and canonical-witness lens
  - symbolic exact-action and committed-predecessor lens
  - heldout experimental-design lens
  - coverage, source fidelity, and Layer-0 lens
---

# R2B2B2I2 resumable second-size-six hostile review

## Final verdict

`PASS_AT_APPEND_ONLY_EXACT_5_OF_380_ONLY`.

The packet closes exactly one additional six-cell orbit and advances durable
coverage from `4/380` to `5/380`. It does not establish universal coverage and
cannot promote the reduction engine, fallback replacement, or either bank.

## Finite-group and exact-action lens

The covered set is reconstructed from the byte-pinned committed predecessor
ledger. Selection is lexicographic before evaluator output. Representative
`(0,0;0,0,2,2)` has exactly six labels, every canonical witness resolves, and
both generators close. All twelve directed edges are non-self. Geometry, both
Phi layers, Hodge, residual, moving primalizer, and action each pass `48/48`;
mixed residual, primalizer, action, and moving-Hodge controls are live `6/6`;
the obsolete forward lift rejects.

The exact mixed actions are `-31/144`, `-697/144`, `-31/144`, `-409/144`,
`871/72`, and `1015/72` in canonical label order.

Lens verdict: `PASS_COMPLETE_SELECTED_SIZE6_ORBIT`.

## Resumption and resource lens

The successor pins the prior probe plus the exact prior ledger bytes at
revision `40e93a9`. Its new entry records both pins and does not rewrite the
earlier coverage entry. The shard unit remains one complete canonical orbit;
the first exact failure would retain the fallback. Both discovery and frozen
replay ran as single serialized heavy processes.

The 336 raw equalities use zero fitted parameters. Independence is not
established, so no surplus claim is allowed.

Lens verdict: `PASS_APPEND_ONLY_AND_RESOURCE_BOUNDED`.

## Heldout experimental-design lens

The same six dense seeds remain frozen outside the 1,925-cell lattice with
nontrivial closed S3 orbits. This Run correctly retains `6/6` preregistered
and `0/6` executed; no heldout pass is claimed.

Lens verdict: `PASS_FROZEN_NOT_EXECUTED`.

## Source, Layer-0, and coverage lens

The finite reduction is repository-derived and source-silent. Active `(9,5)`
and public `(7,7)` remain distinct, as do lattice and dense confirmation,
representative and bank coverage, and the separate `I1 A4` and `I2B C4`
banks. The exact boundary is `5/380`; the other `375` representatives remain
open. The engine is admitted but unpromoted and the 1,925-cell fallback stays
live.

Lens verdict: `PASS_AT_5_OF_380_ONLY`.

## Boundary

Dense heldout execution, 375 representatives, both complete banks,
Green/Helmholtz, domain, observation, and physics remain open. P1/P2/P3 remain
unchanged and unused. Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`;
`TG-1 AND TG-2 AND TG-3` remains `NOT_PROMOTED`. No canon, protected status,
public posture, publication, datum, or physics state changes.
