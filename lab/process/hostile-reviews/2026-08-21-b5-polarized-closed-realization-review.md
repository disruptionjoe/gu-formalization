---
title: "Hostile review: B5 polarized closed realizations"
status: completed
doc_type: hostile_result_review
created: "2026-08-21"
reviewed_artifact: explorations/b5-polarized-closed-realization-2026-08-21.md
canon_verdict_change: none
---

# Hostile review

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

```gu-typed-objects
result: hostile review of the Fourier-modal closed-realization theorem, contrary polarization and bounded-deformation kernel witness
carrier: rank-1920 strict folded L2 carrier on the flat B5 half-cylinder LAYER=ambient CHIRALITY=S-FULL-DIRAC
pairing: auxiliary positive graph topology and program-native split Green trace form ON=independent-B5-curved-strict-carrier
real_structure: relative Gamma-natural coflip at the fixed positive normal
grading: linear abelian BV ghost-field fold on the named constant-coefficient product
action_owner: repository-construction strict massless bulk action; kernel witness uses an explicit non-action-owned bounded deformation
target: hostile evaluation of global closedness and extension-sensitive kernel claim ceilings MAP-TYPE=evaluation
```

## Strongest overclaim or hidden assumption

The dangerous inference is that fixed-mode `H^1` estimates are uniform in the
tangential Fourier label. They need not be: `C(k)` grows with `k` and the
tangential operator is ultrahyperbolic. The theorem does not use a uniform
estimate. It defines the global realization as the natural Hilbert direct sum
of the individually closed mode operators. Closedness of that direct sum is
standard and does not imply elliptic regularity, a Calderon projector or an
`H^1(M_plus)` domain.

A second overclaim would call the realizations self-adjoint. The vanishing
program-native Krein Green form is not the auxiliary positive-Hilbert adjoint
identity. The result says closed, coflip-compatible and Green-isotropic only.

## Strongest contrary construction or counterexample

Allowing a mode-dependent polarization `L_(U,k)` would require compatibility
with the torus coflip permutation and a separate closed measurable/direct-sum
condition. The theorem covers constant `U` only. This is enough to refute a
blanket graph-trace obstruction but not enough to promote the whole pointwise
`O(960)` function space of arbitrary boundary fields.

The two opposite-component domains are distinct, but their undeformed
massless kernels have not been computed. The exact kernel separator adds the
bounded endomorphism `M=B P_v`; it therefore demonstrates extension
sensitivity for the bounded-deformation family, not for the action-owned
`M=0` operator.

## Weakest reproducibility, source, control or propagation seam

The functional-analytic theorem is structural; the finite rational probe
cannot materialize the full Clifford carrier. Its dependencies are the prior
exact inverse of `B_dr`, the constant-coefficient flat model and the coflip-
real split decomposition. All three remain independently replayed by the
predecessor certificates. If `B_dr` became singular or the coefficients became
`r`-dependent, the modal proof would need replacement.

Propagation must retain `constant polarization`, `Fourier-modal direct-sum
domain`, and `bounded-deformation-only kernel witness`. Dropping any phrase
would create a stronger theorem than the proof.

## Verdict

The graph-trace obstruction is absent for every constant coflip-fixed
maximal-isotropic polarization on the named flat product. Two distinct closed
global realizations are exact. The bounded-deformation kernel witness is sound
at its stated grade. Strict massless extension dependence, positivity,
physical state space, source selection, particle content and a GU verdict
remain open.
