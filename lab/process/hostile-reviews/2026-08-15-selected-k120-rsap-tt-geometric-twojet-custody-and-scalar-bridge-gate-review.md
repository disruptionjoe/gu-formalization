---
title: "Hostile review — selected K120 geometric two-jet custody and scalar bridge"
status: complete
created: "2026-08-15"
target_claim: K119_NEXT_GATE__THE_EXISTING_GEOMETRIC_OWNERS_SELECT_A_COMPLETE_THREE_FIELD_MAP
verdict: TT_GEOMETRIC_PARTIAL_CLOSURE_SURVIVES__SCALAR_BRIDGE_REMAINS_ONE_DIMENSIONAL_AND_UNOWNED
lenses: [layer0_semantics, differential_geometry, variational_bicomplex, source_criticism, symplectic_geometry, exact_algebra, krein_operator, claim_status]
---

# Hostile review — K120 geometric two-jet custody and scalar bridge

## Strongest attacks

1. **The map was guessed.** Rejected for the TT legs. The source fixes
   `T=varpi-B_LC(g)`, and differentiation forces the displayed `DB_LC` and
   `D2B_LC` terms. The only explicit freedom is the scalar injection.
2. **Set `lambda=1` by calling both fields `theta`.** This is a coordinate
   convention, not an equality between `I_sc` and the invariant `I1B` radial
   line. Their quadratic terms, interaction roles and stationary backgrounds
   are separately owned.
3. **Use the desired `theta-v-v` coefficient to determine `lambda`.** K119
   proves why this has no selection force: the map would be fitted from the
   target cubic. K120 permits that only as a declared conditional match, not
   action ownership.
4. **The native pairing normalizes the radial line.** It normalizes `Phi1`
   inside `I1B`; it does not identify the units or sign of the separate
   observed scalar. Equating the quadratic actions imposes a cross-action
   bridge and still leaves a sign without an oriented owner.
5. **Observation supplies the inverse lift.** The complete observation germ
   transports already selected fields and equations. Its affine section map
   and nonzero section--field cross jet do not manufacture a scalar map from
   `I_sc` to the `T` radial line.
6. **The preboundary class could select the slope.** The current action has a
   live unrestricted boundary moment map and no selected physical boundary
   class. Its pulled radial column scales with `lambda`, so the existing
   packet records rather than removes the ambiguity.
7. **Partial closure is being hidden as failure.** Rejected. K120 explicitly
   upgrades `I1B`: both TT columns and the nonlinear Levi-Civita second jet
   are now owned independently. The missing object has shrunk to one scalar
   bridge.
8. **This disproves any observed/native scalar relation.** It does not. A
   source/action/background or boundary theorem may select the bridge later.

## Verdict

Pass at local geometric-custody and scalar-bridge-obstruction grade. The
strongest current hypothesis is now precise: `I1B` is the leading primitive
action candidate because its TT two-jet is source-coordinate-owned, but it
cannot inherit the `I_sc` scalar coefficient until K121 proves a normalized,
oriented bridge between the two scalar objects.
