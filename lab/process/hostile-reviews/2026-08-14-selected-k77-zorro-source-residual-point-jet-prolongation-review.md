# Hostile review: canonical-Zorro source-residual point jet

Verdict:

```text
CANDIDATE_SURVIVES__POINT_JET_THEOREM_ONLY__OPEN_BACKGROUND_NOT_PROMOTED
```

## Charge 1: where could the summary outrun the artifact?

The universal construction proves one real connection **one-jet** with
`T_y=F_varpi(y)=0`.  It does not construct a solution on a neighborhood.  In
particular, curved `B_Z` forces `Alt(DT)=-F_BZ`, so the first prolongation is
live.  All wording was narrowed from “background exists” to “point jet exists.”

The identity differentiated-Shiab model is a firing control only.  It proves
that the next question can genuinely return either outcome; it supplies no
rank or image statement about the actual selected-K77 Shiab.

## Charge 2: is rigor defending a superseded object?

No.  The old `b Phi1` branches stay killed on canonical Zorro geometry.  This
packet changes the independent object rather than rescuing them: `B_Z` stays
dependent, while source `varpi` receives its own first jet.  It also keeps the
full/two-half unitary parents distinct from the selected real-K77 finite
control.

## Charge 3: what else changes if this stands?

- `dissolved`: a pointwise curvature-of-`B_Z` obstruction to the existence of
  any source residual-zero jet;
- `survives`: exclusion of both homogeneous `b Phi1` candidates;
- `survives`: `SR-1 BACKGROUND-MISSING` at open-background grade;
- `needs-recheck`: the actual `j^2 varpi -> j^1 Upsilon_B` image on the
  canonical curvature module;
- `needs-recheck`: remaining first-action metric, observation and preboundary
  equations after that image gate.

## Required lenses

- **Layer-0 semantics:** connection value, connection jet and connection
  curvature remain separate; pass.
- **Prior art:** the theorem composes the source-coordinate owner and the
  canonical Zorro obstruction without retracting either; pass.
- **Principal-bundle geometry:** arbitrary first connection jets are legal
  locally, while patching and gauge descent remain open; pass.
- **Variational bicomplex:** one Euler component and the residual-square
  consequence do not settle all first-action equations; pass.
- **Symplectic/BFV:** fixed-boundary legality is preserved and no phase-space
  quotient is inferred; pass.
- **Analytic/PDE:** no formal-integrability, hyperbolicity or existence theorem
  is inferred from a point jet; pass.

No ledger or canon change is licensed.
