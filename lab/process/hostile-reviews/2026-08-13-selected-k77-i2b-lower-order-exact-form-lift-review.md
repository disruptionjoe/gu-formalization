---
artifact_type: hostile_review
created: 2026-08-13
run_id: RUN-20260813-115145-gu-i2b-lower-order-exact-form-lift
target_claim: NONE-NOT-A-KILL
verdict: SCOPED_RESULT_SURVIVES__FIXED_HQ_RESTRICTED_CRITICAL_BRANCH_ONLY
---

# Hostile review: I2B lower-order exact-form lift

## Pre-wave

No fork is assumed beyond the explicitly tested fixed-`H_q`, selected
real-K77 fixture. The finite `196 x 14` restriction can be decided wholesale
at the chosen branch and covectors. Success introduces no object. It removes
only the possibility that the principal exact-form kernel automatically
survives the already-owned lower-order Hessian at this branch.

Required lenses: Layer-0 semantics, prior art, variational bicomplex,
PDE/microlocal analysis, Krein/operator theory, gauge/BV, symplectic geometry,
source criticism and contrary review.

## Charge 1 — where does the summary outrun the artifact?

It would outrun the artifact if it said the characteristic kernel, null cone,
physical modes or gauge cohomology were removed. The probe proves only that
the fourteen non-gauge exact-form columns are not in the kernel of the full
linearized action Hessian at one constant fixed-`H_q` restricted critical
branch. Lower-order terms do not change the principal symbol. The branch is
not stationary on the full 196-cell field bank, so no spectrum or stability
claim is available.

## Charge 2 — where is rigor defending a superseded or mistyped object?

The immediate trap is to reuse the retracted identification of
`K(k): Cl1 -> Omega1(Cl1)` with the source gauge map
`Cl2 -> Omega1(Cl1)`. This review does not. It treats `K(k)` only as a raw
principal exact-form family and preserves the actual rank-25 source map for
the later physical-carrier/BV gate.

A second trap is to compute only `(D Upsilon)^*D Upsilon`. That factorization
is complete only at zero residual. The tested branch has nonzero K-null
residual, and the exact `Upsilon.D2Upsilon` term is nonzero, rank-bearing and
changes the matrix. The probe plants its omission explicitly.

## Charge 3 — if this stands, what else must change?

- `dissolved`: the claim that these fourteen directions necessarily survive
  as a full finite-frequency kernel at the tested branch.
- `dissolved`: another frequency scan on this same branch; the cross block is
  identically zero and the lower-order restriction already has rank 14.
- `survives`: the coefficientwise principal syzygy and null rank jump.
- `survives`: raw `168/168` principal quotient dimensions, without gauge or
  particle interpretation.
- `survives`: the distinct rank-25 source gauge/BV distribution and physical-
  carrier quotient burden.
- `needs-recheck`: the full Hessian on an actual source/action-owned stationary
  jet, including moving `Q_B`, geometry, observation and coupled fields.
- `needs-recheck`: global domain, preboundary/BFV, spectrum and comparison
  with Einstein `2/2` at like-for-like physical grade.

## Verdict

`SCOPED_RESULT_SURVIVES__FIXED_HQ_RESTRICTED_CRITICAL_BRANCH_ONLY`.

The arithmetic is exact and the local disposition survives. No ledger row,
canon verdict, residue, quotient count, external datum or public posture may
move from this result.
