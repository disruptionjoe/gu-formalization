---
artifact_type: hostile_review
created: 2026-08-07
target: ac-g1-propagation-pointer-baseline-2026-08-07
verdict: PASS_AFTER_REPAIR
---

# Hostile review: AC-G1 propagation, pointer and inherited baseline

## Two-sided Layer-0 charges

### Charge A — summary outruns the artifact

The dangerous reading is that `Cl(7,7)` is now proved anomalous, or that the
`Cl(9,5)` theory is anomaly-free. Neither follows. The correction proves only
that the filed `Sp(64)` defusal depends on the quaternionic horn and therefore
cannot be advertised on the settled real horn. The repaired surfaces now say
that both full cancellation claims remain open.

### Charge B — the fence defends a superseded object

Leaving `AC-G1` active after repairing the prose would preserve an adverse
count generated solely by stale wording. The review required an append-only
successor: old `AC-G1` remains historical and `AC-G1a` owns the settled-horn
construction burden. This repair was applied.

## Specialist lenses

- **Anomaly and bordism:** PASS AFTER REPAIR. The successor demands both the
  local `I_16` and global Dai-Freed/bordism/eta legs. It does not replace the
  missing group by `SO(128)` or `BSO(128)` without construction.
- **Real representation theory:** PASS. `M(64,H)` and `M(128,R)` are kept
  non-isomorphic as real algebras. `Sp(64)` and right-`H` are not ported into
  the real carrier. The actual settled-horn automorphism group remains open.
- **Layer-0 typing:** PASS. Pincer defusal, local anomaly cancellation, global
  anomaly cancellation and choice of real horn are four separate objects.
- **Source fidelity:** PASS WITH SILENCE. No primary Weinstein source located
  in this Run supplies the settled-horn replacement group or its anomaly
  theorem. The return is `SOURCE-SILENT`, not a source correction or no-go.
- **Repository archaeology:** PASS AFTER REPAIR. Current `CANON.md`, four live
  `RESEARCH-STATUS.md` summaries, the ledger owner, context pack, lane pointer
  and shared campaign pointer now agree. Historical derivations remain intact
  with their original horn scope.
- **Symplectic geometry:** NOT TRIGGERED. This propagation changes no action,
  variation, constraint quotient, boundary charge or observation descent.

## Baseline attack

The `49` count could be mistaken for today's process-gate state. The baseline
record now types it as historical and records that the exact per-gate manifest
was not deposited. A future cleanup may reconstruct a named manifest, but this
Run does not fabricate it.

## Pointer attack

The Eric/Curt JSON is a historical campaign archive as well as a current
coordination surface. Overwriting its old PW2F pointer without provenance
would lose information. The repair preserves that value under
`historical_active_next_swing` and names the functional contract as the
authoritative owner.

## Verdict

`PASS_AFTER_REPAIR`. The stale-premise count may move from
`OVER_DETERMINED` to `NEEDS` because an independent earlier finder escalated
the row and this Run acts as its separate disposition owner. No anomaly
verdict, canon promotion or public-posture movement is licensed.
