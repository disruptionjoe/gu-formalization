---
artifact_type: hostile_review
created: 2026-08-12
verdict: SURVIVES_SCOPED_AFTER_REPAIR__CONDITIONAL_HQ_WITT_COMPATIBILITY_ONLY__Q_AND_PHYSICAL_BLOCK_UNSELECTED
canon_verdict_change: none
---

# Hostile review: two-half Hermitian/Witt compatibility

## Pre-wave

- **Fork assumed:** the already admitted real `Cl(7,7)` construction only.
  No full-versus-block action-parent or physical-Higgs choice is assumed.
- **Search dimension:** the finite `128x128` real/complex compatibility and
  Lie-algebra stabilizers are decided wholesale. Global descent, Euler/BV and
  analytic domains are not.
- **New object:** success introduces a conditional normalized non-null
  covector line `q` unless an existing observation/action object owns it.
- **What dies:** only algebraic incompatibility between a full `(64,64)` form
  and two `(32,32)` Weyl-half restrictions. Fully split-equivariant same-half
  Hermitian construction dies; no source claim or parent is selected.

Required lenses `layer0_semantics`, `prior_art`, `analytic`, `variational` and
`symplectic` were applied.

## Charge 1 — where does the summary outrun the artifact?

Four promotions were rejected.

1. The construction proves existence after `q`; it does not derive `q`.
2. `H_q` gives a full unitary form and nondegenerate half restrictions, but it
   does not prove the action uses the direct-product block subgroup.
3. The equation-9.16 `+/-` labels are not source-defined as `omega` chirality,
   so the exact block port is conditional.
4. A live diagonal or exchange `varpi` tensor is not thereby the Higgs. The
   scalar-doublet representation, action terms and constraint surplus remain
   uncomputed.

The report was repaired to price a freely selected `q` as a 13-dimensional
orbit before gauge rather than calling it a free one-bit choice. P1 can orient
an existing line but cannot supply the line.

## Charge 2 — where is rigor defending a superseded or mistyped object?

C3-prime correctly studied the native complex carrier `(S_R,J10)=C^64` and
found one cross-paired Krein space. The source-sized two-half question instead
lives on the external complexification `S_R tensor C=C^128`. Treating the
C3-prime neutral halves as a refutation of Curt's two `(32,32)` description
would defend the wrong carrier.

Conversely, the present `H_q` must not erase C3-prime: scalar `i` is not the
real endomorphism `J10`, and the Witt rotation changes the position of the
Weyl polarization. “One full form” and “two nondegenerate restrictions” are
related presentations, not identical Layer-0 objects.

## Charge 3 — if this stands, what else changes?

| item | disposition | reason |
|---|---|---|
| full `U(64,64)` Hermitian geometry on source-sized carrier | `survives` | exact `H_q` signature `(64,64)` |
| two `(32,32)` Weyl-half restrictions | `survives` | exact nondegenerate block restrictions |
| split alone supplies those forms | `dissolved` | invariant same-half spaces are exactly `0/0` |
| fixed-`q` full-Spin symmetry | `dissolved` | stabilizer dimension is `78`, not `91` |
| moving-`q` family naturality | `survives` | exact rational Spin transport |
| action/observation owns `q` | `needs-recheck` | no selector or global section built |
| equation-9.16 signs equal `omega` halves | `needs-recheck` | source leaves grading undefined |
| Higgs/Yukawa block | `needs-recheck` | source assignment only |
| Euler/BV/domain/positivity/index/count | `needs-recheck` | outside this finite gate |

No canon or ledger verdict changes. No new trap is filed: the repeated traps
here—native versus external complexification, construction versus selection,
and P1-orients-but-does-not-create—are already standing Layer-0 controls and
are executable in the probe.

## Controls and verdict

The exact probe passes `53/53`. Four planted controls reject omission of the
external scalar `i`, the zero covector, a frozen-`q` frame move and reuse of
the old neutral-half `B` presentation as two nondegenerate forms.

**Verdict:**
`SURVIVES_SCOPED_AFTER_REPAIR__CONDITIONAL_HQ_WITT_COMPATIBILITY_ONLY__Q_AND_PHYSICAL_BLOCK_UNSELECTED`.
