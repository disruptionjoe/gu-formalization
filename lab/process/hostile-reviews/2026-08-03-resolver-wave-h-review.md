---
artifact_type: hostile_review
created: 2026-08-03
target: RESOLVER-WAVE-H-PUBLIC-NATIVE-REDUCTION-THETA-Z-DESCENT-AND-TOTAL-EULER
verdict: PASS_AFTER_REPAIRS
reviewers:
  - Clifford/representation and exact-algebra lens
  - source/differential-geometry and Layer-0 lens
  - variational/PDE and adjoint lens
---

# Resolver Wave H hostile review

## Final verdict

`PASS_AFTER_REPAIRS` at the narrowed grade

```text
LOCAL_CHOSEN_J_MOVING_REDUCTION_AND_COMBINED_PORT_FIXTURE
```

The repaired executable passes `79/79` checks:

- 26 exact symbolic/rational checks;
- 9 numerical 128-by-128 native controls;
- 1 independent Sage dimension check;
- 8 source receipts;
- 23 type/boundary checks; and
- 12 planted failures.

The combined local `Psrc(T_omega)` is a genuine advance over Wave G's
separate formula-level fixtures. It is not a source-owned public-to-native
reduction, actual `Theta_Z` descent, source-action variation, or Euler/domain
result.

All three lenses re-reviewed the repaired packet. Each reproduced `79/79`
and the scope gate; the final inventory-only corrections update the channel
probe count to 156, add the Wave H scope gate to its selected inventory, give
the full rerun command, and remove the last stale “adapted reduction frame”
headings.

## Representation and exact-algebra lens

Final verdict: `PASS`.

The reviewer independently reproduced the original full run and confirmed:

- `16384=8256+8128` and one-form dimensions `229376 -> 115584`;
- final rank 252 and kernel 229124;
- idempotence and fixed-coindex self-adjointness;
- moving-`J` public covariance and frozen-`J` failure;
- exact tilted left/right laws;
- differentiated idempotence; and
- the decisive failure of Lie-homomorphism functoriality.

Two scope guards were absorbed. The sparse Reynolds map is valid only after
the public-`u(K)` type gate, not on arbitrary complex Clifford elements. The
non-Spin frame's full-`Sp` membership is inherited from immutable Wave G;
Wave H itself rechecks the inverse and combined covariance.

## Source/differential-geometry lens

Initial verdict: `MUST_FIX`; repaired verdict: `PASS` at chosen-local-fixture
grade.

The original summary promoted a chosen anti-linear `J` to “the” missing
public/native reduction. That outran the artifact. The repair now states:

\[
 P_J:\mathfrak u(K)\longrightarrow\mathfrak{sp}(K,J),
 \qquad
 P_{J_h}(\operatorname{Ad}_h X)
 =\operatorname{Ad}_hP_J(X).
\]

The codomain moves to `sp(K,J_h)`; it does not return to one fixed native
group. `J_red` is an extra local `U/Sp`-type reduction field with
source-silent ownership and dynamics. It is distinct from P1/P2/P3,
`epsilon_src`, `epsilon_IG`, and `Theta_Z`.

Repairs also:

- type the public carrier explicitly by Clifford phase convention;
- reject real complementary and imaginary native ill-typed plants;
- check `J conjugate(J)=-1`, Krein compatibility, `(64,64)` inertia, and all
  15 grade representatives;
- use numeric labels for tolerance-based 128-by-128 checks;
- remove the SymPy-object-array performance trap;
- narrow the tilted calculation to the chosen local `A0=0` jet convention;
- call `s_ref' = h s_ref` a stipulated paired-frame law, not a source-derived
  adapted Zorro frame; and
- replace permissive negative-string matching with a bounded source receipt.

The bounded source audit uses the draft reconstruction's public-group
sections, Portal `01:12:17-01:13:55` and `02:25:46-02:33:13`, TOE
`00:20:51`, `00:26:28-00:29:16`, and `02:41:57`, plus the primary-pack
`REAL-FORM-FORK` ledger. It finds no source-owned `J_red` construction.

## Variational/PDE and adjoint lens

Initial verdict: `MUST_FIX`; repaired verdict: `PASS` at projector-first-jet
grade.

The moving-projector derivative formula and differentiated idempotence were
correct. The overclaim was that `dS=-4` varied the displayed source action.
It varies only the auxiliary quadratic

\[
 S_{aux}=\tfrac12\langle P_sx,P_sx\rangle_{tr}.
\]

The report, registry, executable labels, and owner surfaces now say exactly
that. `I1B+IF` variation and actual Euler covectors remain untested. The old
“active/transverse” plant is retyped as a projector image/kernel
linear-algebra control.

The local adjoint statement is now declared on the real fixed-density trace
pairing. Separate checks establish Reynolds self-adjointness, `q6` grade
orthogonality, and `Pext` image/kernel orthogonality before inferring the
composite. Density-dual and primal Krein adjoints remain open.

## Two-sided meta-review

**Where did the summary outrun the artifact?** In two places: a chosen moving
`J` family was compressed into a constructed public/native reduction, and an
auxiliary quadratic derivative was compressed into source-action variation.
Both are now repaired on every owner surface.

**Was exact work defending a stale or superseded object?** No. The combined
port directly closes Wave G's named local gap. It remains deliberately
conditional and may be superseded if actual `Theta_Z` descent, source-owned
`J` dynamics, or total Euler tangency fails.

## Remaining boundary

Actual nonconstant `Met(X)`/Zorro transitions, the `Theta_Z` cocycle,
Spin/`J_red`/source/`Psrc` descent, general `tau_A0`, source-action variation,
global density/Krein adjoints, total Euler/Ward/Green data, an analytic
domain, and physical no-leakage remain open. P1/P2/P3 remain unchanged and
unused. Curt stays formally separate and no third lane is promoted.
