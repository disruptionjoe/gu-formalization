---
artifact_type: hostile_review
created: 2026-08-12
run_id: RUN-20260812-060541-gu-varpi-radial-half-exchange
verdict: SURVIVES_AFTER_LAYER_REPAIR__FULL_PARENT_COMPONENT_AND_MOVING_SOLDERED_DOUBLET_ONLY__PURE_RADIAL_POTENTIAL_ZERO
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Hostile review: trace-radial varpi half-exchange gate

## Charge 1 — where the summary outran the artifact

The first draft said the full connection “owns the radial Higgs.” The exact
result is narrower. The full `u(64,64)` carrier **admits** the trace-aligned
component, while the action and source do not select it. The input one-form is
not itself the weak doublet: the doublet appears only after the moving
equivariant composite `(X,q) -> Xq`. An exact infinitesimal test over all 12 SM
generators was added before retaining the representation statement.

Likewise, rank-four derivative incidence is only a kinetic **carrier**, not a
positive or normalized kinetic action. Half exchange is only ambient-Weyl
incidence, not observed chirality or a Yukawa texture. The isolated self-wedge
zero excludes only a potential generated by that decomposable radial cell
alone.

## Charge 2 — where rigor defended a superseded or mistyped object

The previous phrase “full `U(64,64)` or possible two `U(32,32)` varpi” was too
loose at the connection-component layer. The exact Hermitian geometry admits
two nondegenerate Weyl restrictions, but `gamma(q)` anticommutes with ambient
chirality. Therefore the radial cell is in the half-exchanging complement of
the block product. Treating the two-half block algebra as a complete
alternative parent would delete the tested component by definition.

The `so(6,4)` connection is also the wrong smaller object for the radial
coefficient: every skew generator sends q orthogonally to q. The radial
component enters only after the larger full-unitary/grade-one connection and
the soldering receiver are kept distinct.

## Charge 3 — what else changes if the result stands

- **needs-recheck:** every successor that restricts physical `varpi` to the
  block-diagonal two-half algebra before testing Higgs/Yukawa cells.
- **survives:** the two `(32,32)` half Hermitian forms, the exact SM branching,
  the post-Higgs stabilizer, the rank-ten lower-order soldering chain, and the
  20-dimensional J-selection burden.
- **survives:** a distinct full-`varpi` doublet and moving angular/background
  connection terms as the negative control and likely potential owner.
- **dissolved:** none. No ledger or canon verdict is changed by this scoped
  component theorem.

## Required lenses

- `layer0_semantics`: separates input cell, soldered output, doublet and Higgs
  action.
- `prior_art`: reuses the rank-ten receiver, exact SM representation and
  full/two-half Hermitian map.
- `Clifford/Krein`: checks `H_q`-unitarity and odd block parity exactly.
- `representation`: requires moving-receiver equivariance, not output counting.
- `symplectic/variational`: refuses invented momenta, Euler equations and VEVs.
- `analytic`: refuses positivity or domain claims from a finite derivative
  incidence matrix.

## Verdict

`SURVIVES_AFTER_LAYER_REPAIR__FULL_PARENT_COMPONENT_AND_MOVING_SOLDERED_DOUBLET_ONLY__PURE_RADIAL_POTENTIAL_ZERO`.

