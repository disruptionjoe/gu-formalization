---
artifact_type: hostile_review
created: 2026-08-06
target: selected-action-offgraph-dbt-principal-symbol
verdict: PASS_SCOPED__PARITY_COMPLETED_CARRIER_AND_OBSERVATION_DOMAIN_OPEN
---

# Hostile review: off-graph `d_B T` principal symbol

## Charge 1: where does the summary outrun the artifact?

The tempting overstatement is “the full `d_B T` term has rank 24/24/22.” The
exact result is the off-diagonal Euler block between the complete grade-one
bank and the 24-dimensional horizontal Lorentz `Cl2` bank. The full
`Omega1(Y,ad P)` carrier contains other Clifford grades, and the grade-one
algebraic Hessian, observation receiver and common domain are unbuilt.

A second overstatement is “the null rank loss gives two physical modes.” The
rank `22` is the rank of an unreduced parity-completed first-order block. Its
two-dimensional deficit has not been divided by gauge, constraints or
boundary generators and is not a particle or quotient count.

## Charge 2: is rigor defending a superseded or mistyped object?

The original plan called the metric-10 plus horizontal-`Cl2`-24 packet the
full independent source carrier. The source actually declares a full
adjoint-valued one-form. The exact same-grade zero would have defended the
too-small packet if the parity test had stopped there. The adjacent-grade
calculation corrects the target: the 34-variable packet is a useful observed
subcarrier but not action-invariant under `d_B T`.

## Mandatory symplectic-geometry lens

The formal-adjoint Euler block, not the raw density block, owns the bulk
equation. Their ranks differ on the null orbit (`12` raw versus `11` Euler),
so a raw-rank physical reading would be wrong. The live adjacent-grade term
also has a Green concomitant. Until a common domain and boundary quotient are
specified, it is neither discarded as a total derivative nor promoted as a
Hamiltonian observable. The graph curvature's two transverse tensor
characteristics remain a separate exact result.

## Other hostile lenses

- **Representation theory:** same-grade zeros follow parity. Live Shiab image
  counts show they are not a zero-map theorem. `Cl1-Cl2` is the first tested
  nonzero adjacent block; untested grades remain open.
- **Variational PDE:** the action is mixed order. A first-order off-diagonal
  symbol cannot be added to the second-order Fierz--Pauli block as an ordinary
  homogeneous Hessian rank. The next complex needs filtered weights.
- **Differential geometry:** `T=varpi-B_LC(g)` remains the correct source
  tangent. Enlarging the Clifford coefficient bank adds no new source field;
  it stops truncating an existing one.
- **Source criticism:** Weinstein supplies full `ad P`, coefficient `1/2` and
  translation variation, not the selected parity ranks or receiver.
- **Constraint accounting:** rank 12 is a constraint/equation rank, not twelve
  fitted parameters. Residue and quotient counts remain frozen.

## Preregistered endings

- `DBT_BULK_EULER_NONZERO`: passes on the parity-completed source carrier.
- `DBT_BULK_EULER_ZERO_BOUNDARY_LIVE`: rejected; same-grade zero is not the
  full source result.
- `DBT_SYMBOL_BREAKS_GAUGE_OR_REALITY`: not fired. The exact real cross-block
  is compatible with the source difference tangent; full Ward/domain closure
  remains open rather than failed.

## Verdict

PASS at scoped principal-symbol grade. The current 34-variable packet is not
action-invariant under the source-written derivative term. The nearest
construction is the already source-allowed grade-one companion plus its
algebraic Hessian and observation receiver. No verdict, residue, datum,
quotient, canon or public-posture change is warranted.
