# Hostile review: pre-Shiab Gauss defect action and BV symbol

Status: `PASS AFTER MATERIAL SOLDERING, ACTION-HORN AND BV-SCOPE REPAIRS`

## Charge 1 — find where the summary outruns the artifact

The first construction draft was too strong in four ways.

1. It wrote the raw vertical coefficient
   `v_T in V* tensor ad(P)` as though it were already an ordinary symmetric
   tensor. That pairing is ill-typed. The repaired action now contains the
   explicit map `sigma_epsilon(v_T)` and reports the global equivariant map as
   the next missing object.
2. It called the `20-4=16` result a BV quotient before writing the differential.
   The certificate now constructs `d0(k)`, checks `Jd0=0`, `d0*J=0`,
   `ker J=im d0`, and `im J=ker d0*` exactly.
3. It risked reading rank 16 as a count of Einstein components or physical
   degrees. The report now calls it only the dimension/rank of the full
   two-field non-null symbol quotient.
4. It risked promoting non-null exactness through the characteristic cone.
   The null control instead leaves six non-gauge kernel directions and keeps
   the Green/domain problem open.

After repair, no summary/artifact mismatch remains at the declared local
fixed-slot linearized grade.

## Charge 2 — find where rigor defends a superseded or mistyped object

The exact current-action obstruction could have been used either to abandon
the moving geometry or to keep defending `I1B` by adding more section-chain
terms. Both reactions are rejected.

- The obstruction is not the old circular receiver test. It is a direct
  action statement: at `T=0`, every current-`I1B` curvature value row factors
  through the selected Shiab, while all moving-section curvature derivatives
  of the bilinear term carry an explicit `T`.
- The repair is not attributed back to current `I1B`. It is a separate
  restriction-first defect replacement horn, source-silent and not yet welded
  to the bulk action.
- The one-dimensional tautological trace `q` is not defended as a replacement
  for the missing full ten-dimensional soldering map.
- The conditional minimal even BV skeleton from the prior localization wave
  is superseded here only at the non-null linearized symbol grade by an actual
  differential and exact complex. It is not promoted to nonlinear/global BV.

## Specialist attacks

| lens | attack | disposition |
| --- | --- | --- |
| differential geometry | does Gauss occur before the killed contraction? | yes; `res_H P_R barF+Q(II)` is the action input |
| variational bicomplex | is the cross block an action Hessian rather than an equation glued by hand? | yes; the two blocks are exact transposes through `W_DW` |
| representation/gauge theory | can raw adjoint-valued `v_T` pair with `Sym2`? | no; material repair adds `sigma_epsilon` and keeps its global owner open |
| Krein geometry | did the proof replace the fibre metric with a positive dot product? | no; exact Gram inertia is `(6,4)` |
| BRST/BV | was the quotient manufactured from dimensions? | no; the full `4->20->20->4` complex is exact for timelike and spacelike covectors |
| hyperbolic PDE | what happens at the null cone? | rank drops to 10 and six non-gauge characteristic kernel directions remain |
| source archaeology | did Weinstein print the repaired term? | no; decisive return is `SOURCE-SILENT` |
| epistemic systems | was a current-action kill allowed to become a GU-wide kill? | no; rival repaired horn is constructed and graded |

## Ledger disposition

`LT-GR2c` remains `NEEDS/MISSING_CONSTRUCTION`, but its distance is materially
reduced. The current action is now scoped unable at `T=0`; the repaired local
action and one exact conditional quotient exist; the remaining objects are
the global `sigma_epsilon`, nonlinear bulk/defect weld and characteristic
domain. This does not earn `SAME/PUT_IN`, because the raw source carrier is not
yet connected to the fixed gravitational slot.

`LT-GR1b` remains a genuine falsification of only the selected post-Shiab
factor route. `LT-GR2b/d` and `LT-SM8` do not move: no nonzero VEV, screening
or positive physical cohomology was constructed.

## Final result

`PASS_AFTER_MATERIAL_SOLDERING_ACTION_HORN_AND_BV_SCOPE_REPAIRS`

Accepted next gate:

```text
CONSTRUCT_EPSILON_IG_GRAVITATIONAL_SOLDERING_MAP_AND_NONLINEAR_PRE_SHIAB_DEFECT_WELD
```
