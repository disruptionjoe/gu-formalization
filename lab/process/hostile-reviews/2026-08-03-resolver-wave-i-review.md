---
artifact_type: hostile_review
created: 2026-08-03
target: RESOLVER-WAVE-I-ACTUAL-METX-ZORRO-THETA-DESCENT
verdict: PASS_AFTER_REPAIRS
reviewers:
  - Clifford/representation and C-versus-C-star lens
  - source/differential-geometry and Layer-0 lens
  - variational/PDE, determinant, and density lens
---

# Resolver Wave I hostile review

## Final verdict

`PASS_AFTER_REPAIRS` at the narrowed grade

```text
LOCAL_NONLINEAR_METX_THETA_RECONSTRUCTION_AND_RIESZ_PORTED_SPIN_FIXTURE
```

The repaired executable passes `77/77` checks:

- 43 exact symbolic/rational checks;
- 1 numerical 128-by-128 chosen-`J_H`/Krein check;
- 7 bounded primary-source receipts;
- 13 type and boundary checks; and
- 13 planted failures.

All three hostile reviewers reran the repaired packet and returned `PASS`.
The result is a real local construction advance. It remains a source-silent
Theta reconstruction and associated projector-family fixture, not a global
source-owned `Theta_Z`, actual source-action variation, Euler/Ward/Green
system, analytic domain, or physical no-leakage theorem.

## Clifford/representation and C-versus-C-star lens

Initial verdict: `MUST_FIX`; final verdict: `PASS`.

The decisive error was hidden by the original positive-plane rotations. The
projector's first leg had been transported by the vector representation `O`,
although a raw adjoint-valued source one-form has first leg in `C*` and must
transform by `O^-T`.

The repair replaces one rotation with the exact split-preserving Lorentz
boost

\[
 s=\frac54+\frac34e_{03},
\]

for which `rank(O-O^-T)=2`. It then types Wave H's projector carrier as
Riesz-raised and constructs

\[
 \sharp_\eta O^{-T}=O\sharp_\eta,
 \qquad
 P_{\rm raw}=\flat_\eta P_{\rm raised}\sharp_\eta.
\]

The imported `T_omega` fixture remains raw in `C*`; it is raised only before
entering the inherited projector. The incorrect vector law on the raw leg
now fails. All 252 selected image basis vectors and representative grade-3,
grade-10, public-complement, and grade-6 kernel sectors intertwine.

The review also required:

- renaming patch projectors so they do not collide with external-ledger
  P1/P2/P3;
- scoping `(9,5)` as the chosen Wave-H branch while the live rival `(7,7)`
  branch remains untested;
- calling the positive Spin product a coherent local lift and the negative
  product a planted inconsistency, not a global `w2` obstruction;
- keeping `J` chosen and source-silent rather than intrinsic/source-owned;
- checking the actual 128-by-128 `J_H` and Krein `K`; and
- marking the A9F rank-128 imposter hinge untouched rather than inferring
  anything from the rank-252 source projector.

## Source/differential-geometry and Layer-0 lens

Initial verdict: `MUST_FIX`; final verdict: `PASS`.

The source only says that `C <-> TY` is missing the data of a connection and
gives the Zorro chain. It does not state the constructed formula

\[
 \Theta_{\Gamma,h}(v,\dot h)
 =\bigl(\dot h-\Gamma(v)^Th-h\Gamma(v),h^\flat v\bigr).
\]

Every owner surface now calls this `Theta_recon`, a connection-induced
candidate. Exact operator identities are checked on overlaps `01`, `12`,
`02`, and their triple composition. The direct map is written independently
of its sequential composition, and omission of the Hessian term fails.

The repairs also keep separate:

- observer metric `g_obs`;
- arbitrary tautological fibre metric `h`;
- observer Levi-Civita `Gamma`;
- source gauge transformation `epsilon`; and
- the Levi-Civita-derived tilted connection `A0`, for which the intended
  observer/Zorro/Y connection remains `UNCERTAIN`.

The adapted-frame fixture now uses an arbitrary tautological fibre metric
distinct from `g_obs`. Source receipts use bounded passages, directly anchor
trace reversal, and do not use the 2025 projection-to-contraction correction
as support for Theta or Psrc.

## Variational/PDE, determinant, and density lens

Initial verdict: `REPAIR_REQUIRED`; final verdict: `PASS`.

Independent exact arithmetic confirmed

\[
 \det D\widehat f_{02}
 =\bigl(\det B_{02}\bigr)^4
 =\frac1{1296}.
\]

The committed probe now compares the direct 14-by-14 determinant with the
block formula, rather than checking only the latter. It distinguishes the
metric determinant law

\[
 \det G_2(\det D)^2=\det G_0
\]

from the absolute density law

\[
 \sqrt{|\det G_2|}\,|\det D|=\sqrt{|\det G_0|}.
\]

The duplicated frozen-connection plant was removed. Independent omitted-
Hessian and wrong-Christoffel-sign plants fail. The jet-order fence records
that the metric-bundle map uses first coordinate jets, while tangent
prolongation and Christoffel/contact cancellation expose second coordinate
jets; no source-field Euler or PDE order follows.

## Two-sided meta-review

**Where did the summary outrun the artifact?** It called a source-silent
formula “actual Theta_Z,” called an associated projector family a source
subbundle theorem, called a chosen `J` intrinsic, and did not expose the
raw-covector/raised-vector musical map. All four are repaired.

**Was exact work defending a superseded object?** No. Wave H explicitly named
this dependency. The chosen `(9,5)`/chosen-`J` route remains conditional, and
the live rival `(7,7)` branch is not killed. The result is current, but its
scope is deliberately local and reconstructive.

## Remaining boundary

Global Lorentz-section and spin-lift existence, the identity of `A0`, source
ownership/uniqueness of `Theta_recon`, assembly of both tilted connections on
the overlaps, the moving Shiab contraction, actual source-action variation,
total Euler tangency, Ward/Green/BV data, a common closed domain, and physical
no-leakage remain open. External-ledger P1/P2/P3 remain unchanged and unused.
Curt stays formally separate and no third lane is promoted.
