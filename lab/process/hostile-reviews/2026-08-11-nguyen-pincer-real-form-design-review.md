# Hostile review: nguyen pincer real form design packet

Target: `explorations/nguyen-pincer-real-form-design-packet-2026-08-11.md`
Run: `RUN-20260811-234500-gu-five-frontier-design-packets` (same-run self-review by the design pass; the executing wave reviews independently).

## Self-hostile review (the three standing charges)

**Charge 1 — where the summary outruns the artifact.** (i) The packet's
headline calls the content-level dilemma "the strongest surviving form" of
the pincer; that is a design judgment resting on CB-C's lattice, whose
typed-content instantiation (C3) has not run — until it does, the group-level
horns might yet be the sharper bite. Guard: headline claim graded PROPOSED;
O3 exists. (ii) The `B`-structure and commutant expectations are stated with
enough confidence to shape C1/C2's expected outputs; they are
reconstructions, and the checks are designed to be able to refute them
(controls prove the solvers can answer both ways). (iii) "Both outcomes are
wins" is honest at program level but asymmetric in cost: O2 concedes the
generation ambition at 14d; the outcome table says so explicitly. No other
overrun found; list otherwise empty.

**Charge 2 — where rigor defends a superseded or mistyped object.** Audit of
every (9,5)-side object used: `Sp(64)`, `Sp(1)`, `J_quat`, KSp evenness,
`Omega~15(BSp(64))` appear only as retired-horn contrast, controls, or
harness patterns, never as premises — consistent with the fork registry's
import ban. W232's real-side facts are reused as table lookups but its
content premise is stale; C3/C5 re-run on typed content rather than citing
its verdict. The one deliberate reuse of a superseded-adjacent object is the
-13 truncation as a planted control (its known value `13/37800` is the
control target, not a claim). Residual risk: the packet types the parent
from the 2021 draft's p.42 ladder while the operative parent is
SOURCE-SILENT — defending the ladder where the action may not use it is
exactly a mistyped-object risk; O4 exists for this, and C1's output is a
ladder verdict, not an action-parent verdict. List otherwise empty.

**Charge 3 — what else must change if the result stands.**
- If O1 stands: CANON.md:185's "remain OPEN" clause and RESEARCH-STATUS rows
  1999/2017/2206/2225 — **needs-recheck** (harden to constructed-negative
  for chiral use); the chirality fences (Rung 1, PH-K1-PHYSICAL, the
  Witten-1983 gate, W221's re-scoped SURVIVES) — **survives** (corroborated);
  any K77-frontier text implying future chiral use of the carrier —
  **needs-recheck**; currently-RESOLVED canon rows dissolved — **none**
  (the anomaly surfaces all carry OPEN/NEEDS today; stated explicitly).
- If O2 stands: AC-G1a — **dissolved** (satisfied by construction, with the
  concession recorded); the sentence pattern "GU is anomaly-consistent" —
  **needs-recheck** everywhere it appears without the vectorlike concession
  attached; the generation-count program — **survives** but with its burden
  relocated entirely onto the standing exits; dissolved canon rows —
  **none** (same reason; stated explicitly).
- If O4 stands: the register gains a narrowed open datum (the parent
  choice) — nothing dissolves; the packet's own aiming sections —
  **needs-recheck** per candidate. Empty lists are stated as empty.
