---
artifact_type: hostile_review
created: 2026-08-04
target: explorations/resolver-wave-k77a-real-spinor-observation-atomic-particle-crosswalk-2026-08-04.md
gate: RESOLVER-WAVE-K77A-REAL-SPINOR-OBSERVATION-ATOMIC-CROSSWALK
verdict: PASS_AFTER_MATERIAL_REPAIR_AND_SCOPE_NARROWING
---

# Resolver Wave K77-A specialist pre-assessment and hostile post-review

## Pre-assessment

Three divergent specialists fixed the burdens before construction.

| lens | preregistered burden | kill condition |
|---|---|---|
| real Clifford/representation theory | reuse the exact all-real `Cl(7,7)` carrier; derive the `(1,3)+(6,4)` branching, invariant pairings, one-16 packet, and DRS dimensions without `(9,5)` imports | any claimed block, pairing, charge, or dimension fails an exact or independent representation check |
| source/differential geometry | attach Eric/Curt's descriptions as exact search directives while fencing stale source/repo homonyms | a source paraphrase is promoted beyond its locator, or a superseded exterior-spinor construction is defended |
| variational/PDE | separate bare-operator from Krein-bilinear incidence and carrier support from action, vacuum, Euler, domain, pole, and no-leakage | a kinematic incidence is called a selected VEV/mass, or a local row pass overrides joint coherence |

The council additionally preregistered a five-scope kill ladder. Mandatory
physics targets never inherit the death of a narrower candidate map.

Expected verdict:

```text
NONCHIRAL_PARENT_AND_OBSERVATION_BLOCKS_CONFIRMED
VEV_BLOCK_CLASSIFIED_NOT_SELECTED
EFFECTIVE_CHIRALITY_NOT_DERIVED
```

## Initial construction receipt

The first deterministic run passes `56 exact + 11 source + 17 type + 13
planted = 97` checks. Independent hostile reviews remain required before the
gate can close.

## Hostile post-review: first pass

All three reviewers returned `REPAIR_REQUIRED` while preserving the headline
real-carrier verdict.

### 1. Real Clifford and representation theory

- Two rank claims used `numpy.linalg.matrix_rank` while being labelled exact.
- The charge packet was imported and partly hard-coded; the prose made it
  sound derived from K77.
- The active Pati--Salam packet's Step 0 still asserted the stale
  `R128 -> C64 -> Weyl C32` story.
- Another PC1 file called a fixed `c(v)` Spin-equivariant before later
  contradicting itself. Perfect `B` pairing proves duality, not an
  equivariant `S+ ~= S-` identification.
- F/Q/Z arithmetic did not construct K77 invariant subspaces/projectors.
- The group-level observation image needed the diagonal `Z2` quotient.

Repair: numerical ranks were replaced by exact projector, inverse, and
intertwiner identities. The probe now computes `Q=T3L+Y` state by state for
all eight all-left rows, but explicitly imports the independently verified
internal D5/Pati--Salam packet. Both stale files are fenced and a fixed-`c(v)`
commutator plant fires. F/Q/Z are downgraded to exact K77 kernel dimension plus
source branch arithmetic, and the group image is typed as
`(Spin(1,3) x Spin(6,4))/diag(Z2)`.

### 2. Source and differential geometry

- Four source locators were vague or mistimed.
- Negative source audits had been falsely represented as Eric/Curt speaker
  claims.
- Every row needed a machine-resolvable description, locator, and source
  grade.
- Empirical physics, SM intermediate structure, and source-only predictions
  were all called mandatory without a scope distinction.
- Open, unrun coherence gates were mislabeled with a failure disposition.
- The corrected exterior-action owner path was nonexistent.

Repair: timestamps now cover Curt steps 13--28 exactly and Eric's cosmology
passage is precisely located. Speaker claims are separated from a whole-source
silence audit. Every row's claim IDs resolve through the checked registry.
Targets are partitioned into 25 program-mandatory, seven SM-shadow, and five
source-lane obligations. Unrun gates are `OPEN_NOT_RUN`; an observed locked-leg
failure has a separate `LOCAL_PASS__JOINT_FAIL -> MECHANISM_KILL` policy and
plant. The owner is the actual root `DERIVATION-PROGRESS.md`, whose corrected
no-spinor theorem is now existence/content checked.

### 3. Variational and PDE

- DRS leakage was only kernel-to-trace for three representative coefficients,
  not two-way or coefficient-space complete.
- “Physical KX” overstated constructed invariant-Gram incidence.
- The two Gram representatives had no completeness or action-selection
  theorem.
- The prior K95 gate was sequencing, not a mathematical dependency.

Repair: leakage and Gram claims were narrowed exactly; reverse/K-adjoint
leakage, coefficient classification, Gram completeness, action selection,
VEV, and domain all remain open. The campaign dependency is explicitly
sequencing-only.

## Repair rerun

The repaired primary probe passes `60 exact + 11 source + 18 type + 14
planted = 103` checks. The K77-A scope audit passes.

## Final specialist reruns

All three specialists returned final `PASS` with no remaining blocker.

- Representation/Clifford confirmed the inverse/projector rank witnesses,
  both 91-bivector Gram-invariance checks, statewise charge dictionary, stale
  spinor fences, F/Q/Z downgrade, group quotient, and K77/K95 separation.
- Source/differential geometry confirmed exact locators, speaker/audit
  separation, scoped obligations, actual-row inherited coherence failure,
  corrected owner path, and source-versus-independent-construction grades.
- Variational/PDE confirmed the one-direction representative leakage scope,
  constructed-Gram terminology, open action/VEV/domain boundary, and current
  `103/103` receipt.

The final verdict is `PASS_AFTER_MATERIAL_REPAIR_AND_SCOPE_NARROWING`. This is
a K77 kinematic carrier and reconstruction-ledger gate, not physical particle
recovery.
