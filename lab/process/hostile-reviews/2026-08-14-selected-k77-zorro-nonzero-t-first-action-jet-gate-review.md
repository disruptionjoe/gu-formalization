# Hostile review — canonical-Zorro nonzero-T first-action jet gate

## Strongest overclaim

“The first canonical nonzero-`T` action/Bianchi jet is a GU stationary
background.” Rejected. The construction is local and formal. Primitive
epsilon, the total fixed-`varpi` metric/observation graph, higher formal
integrability, the full field tuple and global/domain conditions remain open.

## Strongest contrary construction

The prior zero-`T` obstruction could have survived if the invariant nonzero
point value missed its quotient. It does not: the quotient condition has two
real roots, and a thirteen-cell rational symmetric correction kills all 196
action plus 5,096 Bianchi rows. The contrary obstruction is therefore tested,
not assumed away.

## Strongest mistyping risk

The predecessor's certificate is supported on fourteen action cells, but the
action map has rank `195` in `196` rows. The cokernel is one-dimensional, not
fourteen-dimensional. This result records both quantities separately.

## Weakest propagation seam

The direct density `-t(27+728t^2)` is nonzero at both roots. It proves a live
rank-one volume partial, not the total source metric Euler, because `E_B` and
the moving-Shiab/Hodge/frame/observation graph have not been pulled back at
fixed `varpi` on this nonhomogeneous jet.

## Disposition

`SCOPED_PASS__CANONICAL_ZORRO_NONZERO_T_ACTION_BIANCHI_JET_NOT_YET_FALSIFIED__SOURCE_METRIC_EPSILON_COMPLETION_OPEN`.
