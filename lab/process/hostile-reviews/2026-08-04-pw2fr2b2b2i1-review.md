---
artifact_type: hostile_review
created: 2026-08-04
target: PW2F-R2B2B2I1-S3-UNIVERSAL-OWNER-CONORMAL-GEOMETRIC-TRANSPORT
verdict: PASS_AT_GEOMETRIC_TRANSPORT_ONLY
reviewers:
  - finite-group action and exact algebra lens
  - nonlinear metric and coframe lens
  - interpolation and computational reproducibility lens
  - source fidelity and Layer-0 lens
---

# R2B2B2I1 hostile review

## Final verdict

`PASS_AT_GEOMETRIC_TRANSPORT_ONLY`.

The packet durably certifies both S3 generators on the complete owner/conormal
geometric layer. It does not certify the remaining evaluator stack and cannot
promote the 380-representative engine or either coefficient bank.

## Finite-action lens

Both generators have their registered group orders, preserve `G4`, induce
bijections of all ten `Sym2` owners, and preserve the active `ETA` action in
the point-orthonormal frame. Exact transport covers the DeWitt tensor, ten
first and one hundred ordered second owner derivatives, forty connection
columns, ten normalized-trace directions, the 35-point lattice, and all 1,925
joint labels.

The orbit census `{1:2, 3:115, 6:263}` and independent Burnside count both
return 380.

Lens verdict: `PASS`.

## Nonlinear geometry lens

The dense held-out uses owners `(0,9)` and independent conormals
`(1,-1,2,3)` and `(-2,3,1,-1)`. Both generators transport the exact nonlinear
Zorro metric, symmetric coframe, moving normalized trace, and density. The
coframe uses the constant `ETA`-isometry conjugation; no moving compensator is
fitted.

Lens verdict: `PASS`.

## Interpolation and reproducibility lens

The first run's five-element pseudo-orbits were impossible for S3 and exposed
a verifier typing bug: lattice points and polynomial exponent labels had been
mixed. The repair applies the conormal-point action actually used by the
accepted interpolation bank. After repair, orbit sizes divide six and the
orbit/Burnside results agree. The standalone exact probe avoids importing the
resource-heavy Shiab chain and completes under pinned SymPy `1.14.0`.

Lens verdict: `PASS_AFTER_TYPED_REPAIR`.

## Source and Layer-0 lens

This finite reduction is repository-derived and source-silent. Active `(9,5)`
and public `(7,7)` presentations remain distinct. Shared label action does not
merge `I1 A4` and `I2B C4`; a geometric certificate is not full evaluator
equivariance.

Lens verdict: `PASS`.

## Boundary

`Phi`/Hodge/Shiab, residual, moving primalizer, and both action evaluators
remain open at universal grade. The 380-representative engine is admitted but
unbuilt and unpromoted; the 1,925-cell fallback remains live. P1/P2/P3 remain
unchanged and unused. Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.
`TG-1 AND TG-2 AND TG-3` remains `NOT_PROMOTED`. No canon, protected status,
public posture, publication, datum, or physics state changes.
