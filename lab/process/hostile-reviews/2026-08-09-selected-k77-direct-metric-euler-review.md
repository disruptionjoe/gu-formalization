# Hostile review: selected K77 direct metric Euler

Date: 2026-08-09  
Verdict: `CANDIDATE_SURVIVES__RANK_ONE_GENERATED_DEMAND__NO_VERDICT_MOVEMENT`

## Three charges

### 1. Where the summary could outrun the artifact

Calling the surviving covector “the cosmological equation” would overreach.
The calculation proves only a **cosmological-type trace** in the normalized
selected first action. It has not been identified with observed `Lambda`, the
source `theta_omega`, a VEV, curvature covariation, magnitude or radiative
screening. The report and ledger use “generated trace demand” instead.

Calling the nine-dimensional kernel “the Einstein sector” would also
overreach. It is the algebraic traceless kernel of this finite homogeneous
metric first variation, not the linearized Einstein operator, its Bianchi
complex or its physical helicity quotient.

### 2. Where rigor could defend a superseded or mistyped object

The seven-owner normal decomposition was already shown trivialization-
dependent. Requiring a canonical vertical `B/T` lift before computing this
first variation would defend that superseded owner split. Exact connection
criticality makes the total first derivative lift-independent on the admitted
low-grade tangent, so no new lift is manufactured.

Conversely, co-moving naturality cannot be used to set a nonzero vacuum
density's metric stress to zero. It transports the tensor packet; the top-form
volume still moves. The nonzero `7/18252` action value is the planted control
that prevents this false cancellation.

### 3. What must change if the result stands

- `dissolved`: the direct-ten-metric-Euler-open gate;
- `survives`: the 321-versus-1,571 tangent choice, full Hessian, action BV,
  trace soldering, domain and odd BFV;
- `needs-recheck`: those downstream calculations, because the present branch
  is not a full metric saddle;
- `new named demand`: an action-owned dynamic VEV/curvature metric stress equal
  to the negative rank-one trace, without a fitted fixed `Lambda`;
- `survives unchanged`: all verdicts, residue, five booked quotients,
  P1/P2/P3, canon and public posture.

## Required lenses

### Layer-0 semantics

Accepts the equality of fixed-`B/T`, fixed-source and co-moving first
derivatives only at the connection-critical branch and within the complete
admitted low-grade tangent. Rejects extending that equality to the old fixture
or to an unconstructed higher-grade source domain.

### Prior art

This is a genuine composition of two existing exact artifacts: the all-ten
normal naturality bank and the v0.106 common connection branch. Neither
artifact previously evaluated the nonzero branch action value against the
rank-one gimmel density covector. The composition is new; its ingredients are
not rebranded as new.

### Differential geometry

Accepts `rho(h)=-2 Tr(g^-1 h)` from direct differentiation of
`G(g)=g direct-sum D_g` and the full ten-direction basis. The six
off-diagonal zero entries are kernel directions, not omitted equations.

### Representation and real Clifford geometry

Accepts functorial Phi/Shiab transport on the selected real K77 Spin-native
parent. Rejects transfer to either `U(32,32)` half or the full `U(64,64)`
parent without their own action and reality calculation.

### Variational bicomplex

Accepts that field-lift changes are annihilated by `E_B=E_T=0` and that a
residual-square action has zero first variation at `Upsilon=0`. Concludes that
the branch is not a valid background for promoting a full Hessian/BV complex
until the trace is canceled or the branch is replaced.

### Symplectic geometry

Accepts the ordering correction: stationarity precedes presymplectic
linearization and reduction. A rank-one Euler obstruction is not itself a
constraint quotient, moment map, boundary charge or BFV class.

### Microlocal PDE and hyperbolicity

Cannot discriminate physical propagation from this homogeneous algebraic
covector. Nine zero algebraic directions are not characteristic modes, and
the one trace equation supplies no principal symbol or domain.

### Krein/operator theory

Accepts the exact real indefinite calculation but finds no positivity,
fundamental symmetry, Riesz map or maximal domain. The trace demand is prior to
those analytic questions.

### Complex/path-integral analysis

Rejects calling the branch a saddle of the functional integral while one
first variation is nonzero. Even after cancellation, contour choice,
determinants, reflection positivity and fluctuation domains would remain open.

### Source criticism

Weinstein's dynamic cosmological VEV grammar makes the new trace demand a
high-value source comparison target. It does not establish the cancellation;
`SOURCE-CONFIRMS` applies to the grammar and `SOURCE-SILENT` to this exact
branch and coefficient.

## Controls and rerun receipt

- primary exact route: `45/45 PASS`;
- independent Sage/QQ route: `11/11 PASS`;
- old noncritical branch blocks false lift-independence;
- trivial branch blocks a matcher that labels every branch nonzero;
- nonzero action density blocks false co-moving cancellation;
- rank-one and off-diagonal plants block a ten-failure overread;
- raw residual zero is not promoted to full stationarity;
- no verdict, datum, quotient or action-parent promotion is admitted.
