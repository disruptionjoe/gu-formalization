---
title: "Hostile review: B5 strict BV action bridge"
status: complete
doc_type: hostile_review
created: "2026-08-20"
target: explorations/b5-strict-bv-action-bridge-2026-08-20.md
verdict: pass_with_scope_fence
canon_verdict_change: none
---

# Hostile review: B5 strict BV action bridge

## Strongest overclaim

The canonical coarse BV action is not the native full-rank B5 source action.
Its master equation is exact and genuine, but only for the declared abelian
quadratic carrier and canonical stage evaluation. It does not select the
native multiplicity Gram, prove a nonlinear master equation, or lift the
coarse arrows to the 20-slot representation carrier.

Likewise, the family `H(a,t)` proves existence, not normalization. Both `a`
and `t` remain free. The live `SS` term is compatible with the strict
generator only because it is placed in a separate gauge-inert Euler spectator
slot and not inserted into the strict differential.

## Strongest contrary route

The current Stage-B Hessian has exact kernel `(1,-2,1)`, so a filtered
field/antifield graph generator can close its quadratic abelian master
equation at coarse grade. The result must not be summarized as “the current
H9 has no action.” It has no action under the strict stage-preserving
generator tested here; the graph-mixing action route remains open and needs
its own degree and embedding proof.

## Mistyping attack

The dangerous substitutions are:

```text
H^T=H                 == classical master equation,
canonical stage SELF  == native Gram-uniform formal adjointness,
coarse exact complex  == actual 128/1792 complex.
```

All three are false. The certificate independently tests `K A=0`, labels the
pairing as canonical coarse evaluation, and keeps the full-rank lift open.

## Reproducibility and failure paths

The probe passes `29/29` in exact rational arithmetic. A planted symmetric
Hessian fails `K A=0`, proving Hessian symmetry is insufficient. Two distinct
`H(a,t)` members and an `a=0` member prove the family and non-normalization.
The current full-nine witness is replayed exactly: it fails
`H_current(0,1,1)=0` and passes `H_current(1,-2,1)=0`.

## Verdict

Pass at formal compact-core coarse multiplicity grade. Preserve these fences:

- strict coarse BV bridge constructed, native full-rank lift open;
- current `H9` excluded only from the strict-generator branch;
- graph-mixing action route open;
- `SELF-CANONICAL-COARSE` is not a native Gram result;
- no normalization of `a`, `t` or the multiplicity Gram;
- no coflip, domain, quotient, historical source recovery, particle result or
  GU verdict;
- next gate is `B5-NATIVE-BV-HESSIAN-LIFT`.
