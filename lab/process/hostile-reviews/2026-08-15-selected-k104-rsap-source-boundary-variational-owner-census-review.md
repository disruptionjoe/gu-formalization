---
title: "Hostile review: K104 RSAP source boundary-variational owner census"
status: complete
created: "2026-08-15"
target: explorations/conditional-build/selected-k104-rsap-source-boundary-variational-owner-census-2026-08-15.md
verdict: PASS_WITH_RELEASED_SOURCE_FULL_G_EQUIVARIANT_AND_LOCAL_FORMAL_CEILING
---

# Hostile review

> **GU-COMPARATOR-ROUTING — scope before inference.** This is a source-native
> boundary selector result. Standard Higgs, family-index and chirality models
> do not adjudicate it. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: SOURCE_NATIVE_ROUTE`

## Strongest overclaim

“No boundary action can select the balanced horn.” The result does not prove
that. It proves that the checked released source does not display such a law
and that a boundary functional retaining full `G` equivariance cannot have
the fixed non-`G`-invariant set `lambda_h=0` as its Euler zero set without a
moving balanced order parameter. An explicitly symmetry-breaking boundary
action or a source-derived moving `R` remains possible.

## Strongest contrary construction

A nonhomogeneous curvature tensor at a stationary solution could have a
smooth gapped `7|7` eigenspace split of the balanced real type. If its
construction is natural, remains defined on the zero-charge stratum, and its
boundary variation selects zero balanced flux plus right gauge, it would own
the missing stack without an externally supplied `R_0`. No such construction
is currently certified; K100 correctly leaves it open.

## Strongest covariance loophole

The source epsilon field transforms and can carry
`R_epsilon=epsilon R_0 epsilon^-1`. This does restore covariance, as the new
probe verifies. It does not choose `R_0`: an equivariant map on the free
epsilon torsor is fixed by its value at the identity. Calling the transported
field source-owned would silently import the exact seed under dispute.

## Strongest variational loophole

A scalar boundary term can change which Lagrangian graph is stationary even
though its field-space Hessian has zero antisymmetric part. This does not
invalidate the result. The claim is that such a term alone neither names the
balanced 42-plane without `R` nor constructs the full 98D coisotropic quotient
and gauge law. It may be one ingredient of a future owned boundary principle.

## Source ceiling

`SC-META-52` confirms that Weinstein calls the upstairs problem one of
boundary conditions and records uncertainty about handling it. That is an
admission of an open problem, not a selection of zero flux, an edge mode, a
multiplier, or a polarization. Unreleased source material remains outside the
verdict.

## Verdict

Pass at the stated ceiling. The fixed balanced zero level is exactly
`H_bal`-invariant and exactly not full-`G` invariant. Moving-`R` covariance is
the exact repair, but the balanced seed, boundary equation, and right-gauge
declaration remain three distinct unowned selectors.
