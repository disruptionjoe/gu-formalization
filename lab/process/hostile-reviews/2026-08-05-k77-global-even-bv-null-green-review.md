---
title: "Hostile review: K77 global even BV, null split and Green domain"
date: 2026-08-05
status: PASS_AFTER_MATERIAL_SCOPE_REPAIRS
reviewed_rows: [LT-GR1b, LT-GR2b, LT-GR2c, LT-GR2d, LT-SM8]
---

# Hostile review: K77 global even BV, null split and Green domain

## Charge 1: find where the summary outruns the artifact

### Finding 1 — “formal even CME” was too easy to read as the full GU BV theory

The new chain proves a complete homogeneous ordinary-gauge Ward owner and
checks the closed Lie representation/Jacobi hypotheses of the standard
minimal BV construction. It does **not** build:

- the diffeomorphism BV algebra used by the null quotient;
- the proposed odd square-root-of-connections/super-IG algebra;
- an analytic BV phase space;
- the quantum master equation; or
- positive physical cohomology.

**Repair required and made:** every promoted occurrence now says “formal
minimal homogeneous-gauge BV/CME,” and the diffeomorphism and odd closures are
explicit exclusions. `LT-SM8` does not move.

### Finding 2 — the Green claim initially exceeded the exact symbol

The executable calculation is at a flat observation background. From that
symbol one may conclude that harmonic gauge yields trace reversal times the
wave operator. One may not silently import the curved lower-order terms and
call the operator normally hyperbolic on every globally hyperbolic `X`.

**Repair required and made:** the unconditional part is restricted to the flat
globally hyperbolic defect. The curved statement is conditional on a separate
proof that the completed gauge-fixed operator is normally hyperbolic. Bär's
composition theorem is used only after that hypothesis. The global coupled
noncompact `Y14` domain remains open.

### Finding 3 — `4+4+2` is a dimension filtration, not a canonical splitting

Exact arithmetic proves:

```text
dim ker J = 10
rank(C restricted to ker J) = 4
dim(ker J cap ker C) = 6
rank im D0 = 4
dim physical quotient = 2.
```

It supplies explicit representatives for the final two dimensions, but does
not canonically choose a four-dimensional complement of the constraint kernel
inside `ker J`.

**Repair required and made:** durable prose now calls `10 -> 6 -> 2` an exact
dimension filtration/quotient. “Four constraint violations” is retained only
as plain-language codimension shorthand, not a canonical direct-sum theorem.

### Finding 4 — 84 is a prequotient coefficient count, not 84 observables

The parameter result says the independently written `lambda_def` must be
charged before any quotient. It does not say all 84 reals survive gauge,
normalization, field redefinition or physical observation.

**Repair required and made:** every meter says “before quotient”; no global
residue reduction is booked, and the normalization quotient stays unranked.

## Charge 2: find where rigor is defending a superseded or mistyped object

### Finding 5 — eliminating all six null directions would solve the wrong problem

The predecessor correctly found six non-gauge null directions, but “non-gauge”
is not synonymous with “unphysical defect.” Adding the harmonic constraint
shows that four dimensions are removed by the constraint and the surviving
quotient is exactly two-dimensional. Any future domain that kills those two
physical wave representatives fails this gate.

**Disposition:** the old undifferentiated six-direction burden is superseded
by the constraint/gauge quotient. The earlier calculation remains correct;
its physical typing is narrowed.

### Finding 6 — the `lambda_def` alias fork defended names rather than roles

`kappa_1` is an internal quadratic `T` gain. `source_norm` is an abstract
rescaling label absent from the invariant basis and absent from CB-D's charged
83 coordinates. `lambda_def` is the relative coefficient of a separately
supported `X` action. No same-source map identifies them.

**Disposition:** retire the discrete alias fork and charge `lambda_def` as the
84th prequotient real. A future quotient can reduce the count only by an
explicit rank theorem.

### Finding 7 — displayed Shiab selection is not universal Shiab uniqueness

The Bianchi selector chooses `(comm,symi,symi)` within the eight displayed
product rows. The full source-natural Shiab family remains larger/open.

**Disposition:** the composed owner uses the selected displayed horn and says
so. It does not revive the killed post-Shiab factorization or claim historical
uniqueness.

## Source charge

The primary sources confirm the moving `epsilon` ingredients and warn about
the upstairs multiple-time analytic debt. They do not print the new formal
homogeneous-gauge BV completion, harmonic null quotient, defect Green complex
or normalization adjudication.

Decisive source return: `SOURCE-SILENT`.

## Independent routes and planted failures

- Primary SymPy certificate: exact null ranks, harmonic restriction, explicit
  plus/cross quotient, nonabelian closure/Jacobi, trace reversal and parameter
  typing.
- Independent Sage/QQ route: reconstructs the null `10 -> 6 -> 2` quotient and
  nonabelian Lie closure without importing the primary probe.
- Planted failures reject: erasing all six null directions, promoting the
  defect Green complex to `Y14`, equating Green propagation with positivity,
  equating formal homogeneous-gauge CME with odd/analytic BV, silently
  quotienting `lambda_def`, and consuming P1/P2/P3.

## Verdict

**PASS AFTER MATERIAL SCOPE REPAIRS.**

The wave earns one `LT-GR2c` distance reduction, one additional ranked local
quotient, and a residue correction to 84 prequotient reals/10 discrete forks.
It earns no verdict change, global domain, observed physics, positivity,
vacuum, cosmology or canon/public-posture movement.
