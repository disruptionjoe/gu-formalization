---
artifact_type: hostile_review
created: 2026-08-03
target: RESOLVER-WAVE-F-ACTUAL-SOURCE-SHIAB-FERMION-EULER-WEIGHT-SELECTION
verdict: PASS_AFTER_REPAIRS
review_lenses:
  - source_and_differential_geometry
  - variational_pde_and_source_root
  - full20_krein_index_and_right_H
meta_charges:
  - summary_outruns_artifact
  - rigorous_work_defends_stale_or_superseded_object
---

# Resolver Wave F hostile review

## Final verdict

**PASS_AFTER_REPAIRS.** All three reviewers independently reran the repaired
Wave-F probe and scope gate:

```text
29 exact/deterministic-native
 1 independent Sage
 4 source
12 type
13 planted
59/59 PASS

resolver_wave_f_scope_audit.py: PASS
```

Python compilation and `git diff --check` also pass. The actual-source gate
does not become partially constructed. It records
`OPEN -> OPEN_REBASED_COMPONENT_PROJECTOR_BUILT`, because the executable
constructs only an exterior projector after grade-six typing. No claim,
canon, public posture, lane, count, datum, VEV, mass, quotient, domain, or
no-leakage verdict moves.

## Blocking findings and dispositions

| hostile finding | repair | final disposition |
|---|---|---|
| The first draft wrote `P252^0=...q6` as though `q6` from the native adjoint had been built. | The constructed map is now `Pext^0=j5(1/9)pi_V5 delta` on an input already in `C* tensor Lambda6(C*)`; `q6` is `OPEN_FORMULA_ONLY`. | closed by retyping and rebase |
| Constant signed-permutation transport was compressed into actual source/Zorro descent. | The fixture is scoped to two-leg signed-permutation transport, constant composition, one explicit split-preserving permutation, one split mover, and one projector-chain example. Actual `U_(Theta,epsilon)`, tilted descent, and `Theta_Z/Y14` overlaps are open. | closed by demotion |
| `T_omega` was called connection-valued. | It is now the tensorial difference of two affine connections and an adjoint-valued source one-form, not itself an affine connection. | closed by Layer-0 repair |
| A stale `P252^!E=0` suggested an available source projection. | The projected equation is only the candidate `(P_source)^!E=0` after `q6`, Riesz, and source transport exist; active and transverse equations remain open. | closed by retyping |
| The full real exterior Hom-space was counted as dimension two. | The two support branches and their independent Hodge-star twists give real dimension four; `[a:b]` is only the star-even two-dimensional subansatz. | closed constructively |
| The half-weight looked representation-wide. | `[2:1]` and `(160/7)(a-2b)^2` are fenced as Wave E's one-simple-blade comparator; representation-wide extension is open. | closed by scope |
| The zero selector ideal looked like a varied Euler flat direction. | It now records that `[a:b]` is absent from the displayed field registry: action blindness, not Euler flatness. | closed by Layer-0 repair |
| The toy linear comparator was described as N1. | It is explicitly a toy derivative control; the actual N1 `J_D/J_F` bridge remains open. | closed by scope |
| The displayed kappa result was stated as an unqualified no-selection theorem. | Only direct selection by the displayed kappa term is refuted; complete moving Shiab plus fermion selection remains not evaluable. | closed by scope |
| The auxiliary's zero principal symbol was inferred from a label. | Direct algebraic variation, Hessian rank two, and an isolated zero-jet/no-IBP registry are checked. Coupled nonpropagation remains open. | closed constructively and by fence |
| Projector idempotence and Krein claims were too broad. | All 252 image blades and one off-image input are checked; full Krein self-adjointness of `Pext^0` is explicitly not independently certified. | closed by evidence fence |

## Two-sided Layer-0 meta-review

### Where did the summary outrun the artifact?

The first draft compressed a formula-level `q6` into a constructed port,
model signed permutations into source descent, two star-even maps into the
whole real Hom-space, and an absent field into an Euler statement. The
repaired report, registry, READMEs, and fail-closed audit preserve all four
distinctions.

### Was rigorous work defending a stale or superseded object?

No after repair. The coarse `epsilon_plane` is retained only as RB5's negative
control, not rehabilitated as the owner of the `4+10` split. The actual-source
gate is rebased around the missing generic-adjoint `q6` and the source-owned
tilted `epsilon_src/Theta_Z` transport. The one-blade `[a:b]` comparator is
not mistaken for that upstream port. P1/P2/P3 do not replace any missing map.

## Earned result

- `Pext^0`: exact rank-252 projector after grade-six exterior typing, with
  forced normalization `1/9`.
- Generic-native-adjoint `q6`: open.
- Actual tilted source/Zorro transport and global descent: open.
- Coarse Clifford-plane ownership of the internal split: refuted by a live
  split-moving control.
- Full real exterior Hom-space: dimension four; `[a:b]` is the star-even
  dimension-two subansatz.
- Displayed kappa direct selection of the one-blade half-weight: refuted.
- Complete Shiab/fermion selection and active/transverse Euler closure: open.
- Auxiliary: can force the isolated one-blade star-even ratio only; not
  source-derived, required, or proved coupled-nonpropagating.
- `chi=0`: no canonical physical KO basepoint or P3/count inference.

Final route decision: **REBASE** to
`RESOLVER-WAVE-G-Q6-NATIVE-SP-TILTED-SOURCE-PORT-AND-TRANSVERSE-EULER`.
