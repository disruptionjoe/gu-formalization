---
artifact_type: hostile_review
created: 2026-08-13
verdict: SCOPED_SURVIVES
canon_verdict_change: none
---

# Hostile review: trace-Hq full-connection compatibility

## Pre-wave charges

- **Fork assumed:** only the conditional real-K77 substrate used by the
  predecessor exact banks; no signature or action-parent settlement.
- **Search space:** the complete `2^14` Clifford basis is decided wholesale.
- **New unowned object:** none; `q_g`, `B`, `H_q`, chirality and all monomials
  already exist.  The result counts rather than chooses the affine freedom.
- **What dies if successful:** Hermitian compatibility alone as the selector
  of full versus block parent, luminous versus mirror half, or a physical
  fermion carrier.

## Required lenses

- **Layer-0 semantics:** full parent, block subgroup, carrier halves,
  connection fields, compatible reduction, action selection and physical
  quotient remain distinct.
- **Prior art:** the fixed split-spin dimension-42 result and the moving-Hq
  Higgs phase result are preserved; the new theorem begins only at the
  complete source-sized Clifford basis.
- **Clifford/Krein:** exact signed-permutation arithmetic; no tolerance or
  complexified signature decision.
- **Principal-bundle:** the moving equation is an affine torsor, not a unique
  connection.
- **Variational and symplectic/BV:** compatibility is not stationarity,
  characteristic reduction or cohomology.
- **Analytic:** no positivity, closed domain, spectrum or index conclusion.

## Charge 1 — where does the summary outrun the artifact?

The strongest possible overreach would be “the source connection is the
trace-Hq unitary connection.”  The artifact proves only that the
repository-constructed `H_q` admits a full unitary connection arena of the
right dimension.  It does not identify this form with the source's unstated
Hermitian form.  The report now says “compatible with,” not “derived by.”

The phrase “saturates `u(64,64)`” also depends on the predecessor's exact
theorem that the `16,384` Clifford words are an independent basis of
`End_R(S_R)`.  That receipt is explicitly inherited.  Multiplying individual
basis vectors by `i` preserves real-linear independence, and `dim_R
u(64,64)=128^2`, so the dimension closure is licensed.

## Charge 2 — where is rigor defending a superseded or mistyped object?

The tempting mistyping is to replace Curt's two `C^(32,32)` carrier halves by
two independently source-selected `U(32,32)` connections.  The source return
rejects that inference.  The exact even algebra merely proves that a
block-preserving subgroup exists inside the full parent.

The other superseded route is direct application of the bosonic moving
projector to `W/192/640/832`.  Canon already rejects that cross-bundle move.
This result does not revive it; the physical carrier remains downstream of an
induced fermion/BV operator.

## Charge 3 — if this stands, what else must change?

- **Dissolved:** `D_varpi H_q=0` as a unique action-parent or luminous-half
  selector.
- **Survives:** trace-Hq compatibility; full `U(64,64)`; the block subgroup;
  two `C^(32,32)` carrier halves; the 20-dimensional `J` selection burden;
  action/BV/domain half asymmetry; physical-carrier construction.
- **Needs recheck:** ledger distances that still say “construct `D_varpi
  H_q`” should be migrated in a separate envelope-authorized Compose step;
  verdicts and canon should not move.

## Controls and reproducibility

The exact probe checks all monomials rather than samples.  Plants reject an
all-real coefficient bank, reject equating Hermitian compatibility with block
preservation, and reject freezing the moving `H_q` representative.  All fire.

## Verdict

`SCOPED_SURVIVES`.  The mathematical theorem stands.  Its scientific effect
is negative selection information: compatibility is broad enough to admit
both parents and therefore cannot decide the physical carrier.  No verdict,
canon, accounting, datum or public-posture change is licensed.
