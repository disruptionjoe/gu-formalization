---
artifact_type: hostile_review
created: 2026-08-07
target: explorations/conditional-build/selected-source-varpi-cartan-composition-2026-08-07.md
verdict: PASS_WITH_SCOPE_FENCES
---

# Hostile review: selected source-varpi / Cartan composition

## Charges

1. Find where the summary outruns the executable artifact.
2. Find where the lane is defending a superseded object.
3. Enforce Layer 0 between `delta B`, `delta T`, `delta A`, a tangent test
   direction, a graph lift and an Euler variation.
4. Require a symplectic review before any action/preboundary promotion.

## Findings

- The exact rational composition is sound: for nonzero `t`, the endpoint
  algebraic response `-t K(alpha)` reconstructs each target using
  `alpha=-K^{-1}(target)/t`. The opposite sign and zero-background plants fire.
- The source receipt is properly split. Fixed-epsilon `varpi` motion and the
  endpoint `F_A` residual are source-explicit; the connection identity and
  four-column lift are repository constructions.
- The earlier phrase "independent-varpi connection component of delta B" is a
  superseded owner label. Independent `varpi` motion has `delta B=0` and moves
  `delta T=delta A=alpha`.
- The pointwise lift is not an actual global normal jet. No transition law,
  Spencer compatibility, atlas descent or full action Hessian has been tested.
- Zero coefficient freedom is conditional on the four target columns, the
  selected operator and nonzero `T_*`; it is not a derivation of the target
  columns or a reduction of the global residue ledger.
- Symplectic fence: carrier/tangent realization produces no presymplectic
  current or physical quotient. The next Euler/preboundary pass must retain an
  explicit symplectic reviewer and rerun after any repair.

## Disposition

`PASS_WITH_SCOPE_FENCES`. Book local source-coordinate realizability and
advance to the covariant graph/Spencer gate. Do not claim source selection,
global integrability, stationarity, Euler recovery, a quotient, a new datum or
physics recovery.
