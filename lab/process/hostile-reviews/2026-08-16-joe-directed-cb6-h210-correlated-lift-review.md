---
artifact_type: hostile_review
created: "2026-08-16"
targets:
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb6-h210-full-correlated-lift-naturality-2026-08-16.md
  - tests/channel-swings/joe_directed_cb6_h210_full_correlated_lift_naturality_probe.py
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb6-h210-equation916-observed-composition-2026-08-16.md
  - tests/channel-swings/joe_directed_cb6_h210_equation916_observed_composition_probe.py
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb6-h210-three-horn-compatibility-2026-08-16.md
  - tests/channel-swings/joe_directed_cb6_h210_three_horn_compatibility_probe.py
status: PASS_AFTER_TWO_MATERIAL_SCOPE_REPAIRS__INTRINSIC_KAPPA_NATURALITY_AND_STAGED_ZERO_ORDER_CHAIN_SURVIVE__EIGHT_BRANCH_LEDGER_IS_NOT_A_WITNESS_EXISTENCE_PROOF__FULL_D0_VARPI_COLLISION_NEXT
classification: BRIDGE_OR_SEMANTIC_BOUNDARY
mandatory_lenses: [source_custody, conditional_build_scope, induced_gram_graph_geometry, clifford_sign_and_both_halves, full_hn_kappa_naturality, associated_bundle_global_descent_ceiling, cb5_successor_correction, exact_ranks_kernels_strata, family_exact_sequence_stabilizers, equation916_cell_order_parity, d0_varpi_collision, three_horn_eight_branch_logic, ps_reduction, twistor_boundary, functor_order, adverse_mutations, novelty_duplication, reprioritization]
canon_verdict_change: none
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — source-native conditional build.** This review
> concerns Weinstein's equation-(12.22) F/imposter, equation-(11.6) Q/Z,
> `2+1`, Pati--Salam recombination, and emergent-chirality claims. Ordinary
> family indices, net-chirality arguments, scalar-Higgs/VEV models,
> conventional `SO(10)` mass mechanisms, anomaly selectors, and familiar
> low-energy particle models are controls only. They do not adjudicate this
> mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.
>
> `H210` is assumed. `H210-FCORR`, `H210-ALIGN`, and `H210-PSRED` are
> independent declared horns. Constructing or deriving an action, selector,
> observer graph/background, family row, moving PS reduction, physical
> quotient, external datum, mass, scale, threshold, spectrum, observable, or
> phenomenology is outside this review.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# Hostile review — CB-6 H210 correlated-lift composition

## Verdict

`PASS_AFTER_TWO_MATERIAL_SCOPE_REPAIRS`.

CB-6A contains the main mathematical advance and survives. For a
nondegenerate graph plane `H_J=im(L_J)` and its metric-orthogonal complement
`N_J=im(K_J)`, the separate horizontal and normal cocycles, induced Grams,
Clifford traces, and correlated lift fit together exactly. Given an admitted
orthogonal overlap and Spin lift, the complete horizontal/normal square is
functorial, retains both ambient halves, and has zero ambient Clifford trace.
The two-field replay passes all nine tested squares per field and fires every
geometric mutation.

CB-6B also survives at its stated zero-order stage-composition grade. The
only admitted forward H210 arrows remain the zero-order parts of equation-
(9.16) cells `(1,2)` and `(0,3)`. The order is

```text
M_3 tensor 16 --r tensor T--> Z/internal-144 partner
              --O_J--> observed vector-spinor
              --Gamma_H,J^intr--> intrinsic trace
              --kappa_J--> correlated F-shaped carrier.
```

`kappa_J` is downstream, the final map remains odd/off-diagonal, both
conjugate packages remain present, and the banked wrong-order witness is
maximal: intrinsic downstream rank `16` versus upstream F projection rank
`0` per internal half. The full `d0+varpi` cells remain untyped because their
two summands have incompatible source-labelled/K77 half behavior.

CB-6C survives only after a material scope qualification. Its eight rows are
an exact **Boolean prerequisite and typed-sentence ledger**. They establish
that the three declared horn roles are different and that no source-labelled
sentence may be used without its named prerequisites. They do not prove the
existence of concrete FCORR, ALIGN, and PSRED witnesses on one GU observer
atlas. The probe assumes three independent bits and then exhausts the eight
bit patterns; it cannot use those stipulated countermodels as an independent
existence or unrestricted categorical-independence theorem.

The wave therefore advances conditional carrier geometry, not the source
prediction. `SC-GEN-53`, canon, and public posture do not move.

## Independent replay

The three declared selftests were rerun from the repository root without
editing the execution artifacts.

```bash
sage -python tests/channel-swings/joe_directed_cb6_h210_full_correlated_lift_naturality_probe.py --selftest
sage -python tests/channel-swings/joe_directed_cb6_h210_equation916_observed_composition_probe.py --selftest
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_cb6_h210_three_horn_compatibility_probe.py --selftest
```

All close with zero failures.

- CB-6A reproduces the two `64`-dimensional ambient halves, all `9/9`
  complete kappa squares over each of `GF(1009)` and `GF(1013)`, both Gram
  cocycles, rank descent, the upstream-zero/downstream-nonzero witness, four
  geometric mutations per field, and six semantic mutations.
- CB-6B reproduces all six intrinsic rank strata, identical two-half and
  two-field fingerprints, the full family-kernel ledger, odd parity, source
  cell order, the maximal banked wrong-order witness, and `15/15` planted
  semantic controls.
- CB-6C reproduces `10/10` exact bookkeeping checks and fires `15/15`
  semantic mutants.

The passing probes are strong regression evidence for their encoded claims.
They do not enlarge those claims: CB-6A's finite transition sample is not an
observer-atlas or triple-overlap construction, and CB-6C's Boolean model is
not a witness-existence theorem.

## Eighteen-lens hostile attack

### 1. Source-custody lens — survives

The source distinctions are preserved:

```text
F_corr = equation-(12.22) correlated trace carrier / imposter referent,
M_3    = abstract family multiplicity,
Z      = equation-(11.6) normal-RS sector containing the internal 144 partner.
```

Equation (12.22) labels only `S(TX) tensor S(N)` as the imposter. The p.53
imposter-named rows remain ambiguous and do not license renaming all of the
internal `144`. The H210 port starts in Z, not F. `SC-PRE-52` owns the
distinct approximately `144`-dimensional partner claim.

### 2. Conditional-build-scope lens — survives

All three artifacts assume H210 and refrain from manufacturing an action,
selector, graph, family row, PS reduction, quotient, or external datum. The
banked graph and row are certificate fixtures, not selected physical data.
No conventional family-index, net-chirality, or Higgs/VEV route is smuggled
back into the lane.

### 3. Induced-Gram and graph-geometry lens — survives

The formulas

```text
L_J=[I;J],
K_J=[-eta_H^-1 J^T eta_V;I],
G_H=L_J^T eta L_J,
G_N=K_J^T eta K_J
```

give the correct metric-orthogonal graph split whenever both induced Grams
are nondegenerate. For `g=((a,b),(c,d))`, the distinct cocycles

```text
A=a+bJ,
D=d-c eta_H^-1 J^T eta_V
```

satisfy `gL=L'A` and `gK=K'D`. Using `A` for the normal frame, freezing
either Gram, or freezing `K` fails exactly. The two coframe transports must
remain separate.

### 4. Clifford-sign and both-halves lens — survives

The induced inverse Grams give `Gamma_H j_H=4I` and
`Gamma_N~ j_N~=10I`. The coefficient pair `+1/4,-1/10` cancels the ambient
trace; reversing the normal sign fails. The horizontal and normal one-form
components both flip the input ambient half as required, and the even Spin
transport preserves the two source halves. No luminous half is selected and
no ordinary net-chirality conclusion is available.

### 5. Full H/N kappa-naturality lens — survives conditionally

The complete right-domain square, including `A^-T`, `D^-T`, Spin conjugation,
and the domain inverse, passes. Omitting the right-domain factor fails. The
universal algebra is valid for an admitted orthogonal transition with a
compatible Spin lift and invertible graph cocycles. That is a genuine
improvement over CB-5's horizontal-only result.

### 6. Associated-bundle/global-descent ceiling — material scope repair

CB-6A proves naturality **given** the admitted O/Spin overlap data. It does
not construct an actual observer atlas, select a graph section, verify a
global Spin lift or triple-overlap sign cocycle, or prove preferred PS
descent. Consequently “associated-bundle morphism” is safe as a conditional
representation theorem on admitted overlaps; “globally descended GU
operator” is not. The finite three-transition replay is a regression test of
the formulas, not by itself a proof that the required global atlas exists.

### 7. CB-5 successor-correction lens — survives and is load bearing

CB-5's fixed `(eta_H,gamma_H)` trace agrees with the intrinsic graph-plane
trace only at the flat chart. On the isotropic fixture its per-half matrix
rank is `32` rather than the intrinsic `48`; at the banked fixture both ranks
are `64` but the maps are unequal. CB-5's projector algebra remains correct
for its transported chart decoration, but its nonflat trace is not the
canonical Clifford trace of `H_J`. All successor synthesis must use the
intrinsic CB-6A ranks and ownership language.

### 8. Exact ranks, kernels, and strata lens — survives with local scope

The intrinsic internal-complex rank sequence

```text
flat/null/isotropic/non-null/paired/banked = 0/8/12/16/12/16
```

is reproduced over both fields and both halves. The corresponding family-
input kernels are `48/40/36/32/36/32`. These are pointwise ranks in declared
graph fixtures. They do not identify an admitted physical stratum, prove
genericity in a solution space, or supply a quotient.

### 9. Family exact-sequence and stabilizer lens — survives with one wording fence

For `K=ker(r)`, `L=M_3/K`, and any stage `B:16->W`, the sequence

```text
0 -> K tensor 16 -> ker(r tensor B) -> L tensor ker(B) -> 0
```

and `dim ker(r tensor B)=48-rank(B)` are correct. The banked kernel is exactly
`K tensor 16`, dimension `32`. `Stab(r)` is abstractly isomorphic to
`Hom(L,K) semidirect GL(K)`, while the kernel-preserving group is the larger
parabolic. Any concrete block semidirect-product display depends on a
splitting; no complement or named family is canonical.

### 10. Equation-(9.16) cell/order/parity lens — survives

The source row and column reversal is carried correctly. Cells `(1,2)` and
`(0,3)` host only the conditional forward zero-order H210 terms. Their
reverse-shaped partners remain separate and do not become adjoints merely
because the source prints bars and stars. The pp/mm cells remain zero-
multiplicity controls. The downstream composition remains odd and therefore
does not migrate into a diagonal cell.

### 11. Full `d0+varpi` collision lens — survives as the sharp blocker

With the section-11.2 labels read as ambient spin halves, `d0` preserves the
half while H210 `varpi` flips it. The two summands printed in one cell do not
currently have a common source-labelled/K77 codomain. The product grading
repairs support only after a one-form relabeling that conflicts with the
identity-grade source labels. Therefore no full-cell rank, kernel,
cancellation, spectrum, family kernel, adjoint, or kappa postcomposition is
defined. The zero-order stage remains valid in isolation.

### 12. Three-horn/eight-branch lens — survives only as a prerequisite ledger

The eight rows correctly enumerate which sentences require FCORR, ALIGN,
and PSRED. The witness types are distinct: source interpretation of a map,
provenance-line identification, and reduction data on transitions. This
blocks every attempted horn collapse.

The stronger phrases “proves logical and categorical independence” and
“proves joint compatibility” require qualification. The code defines the
three horns as independent bits; its pairwise countermodels are then true by
construction. It does not exhibit three simultaneous geometric witnesses or
define categories in which a formal independence theorem is proved. What
survives is:

```text
given witnesses for any selected horns,
the corresponding typed claims are noncircular and no other horn role follows.
```

### 13. Pati--Salam-reduction lens — remains a declared ceiling

CB-6A supplies full graph/Spin covariance, not preferred PS labels. CB-6C is
right that `H210-PSRED` has a separate job, but it supplies no normalizer-
valued cocycle. Assuming a PSRED witness permits conditional descent;
replaying the definition cannot establish that such a witness exists. A
fixed PS embedding remains excluded on nonzero mixed graphs by CB-4.

### 14. Twistor-boundary lens — survives as a control only

Twistor prior art supplies `Pi_4`, the positive embedding of a 4D
gamma-traceless vector-spinor into ambient `ker Gamma_14`, and the exact
warning `Pi_4 != Pi_14`. It supplies neither the correlated trace-image
adapter's source provenance nor ALIGN or PSRED. The third-order Bach-flat
detour operator is not the first-order rolled GU operator, so no Penrose,
Bach, or cohomological detour repairs equation (9.16) here.

### 15. Functor-order lens — survives decisively

The two operations do not commute:

```text
P_Fcorr T_H210 = 0,
kappa_J Gamma_H,J^intr O_J T_H210 != 0
```

on every tested nonflat witness, with maximal banked internal rank `16`.
Observation creates the F-shaped adapter downstream. It does not uncover a
pre-existing F component and does not recover the consumed gamma-traceless
normal leg.

### 16. Adverse-mutation lens — algebra survives; semantic plants are scoped

The geometric mutations in CB-6A are substantive: frozen normal frame,
fixed Grams, wrong normal sign, and missing domain transport each break an
exact identity. CB-6B's rank and parity checks are likewise substantive.
The dataclass/string mutations in CB-6A/B and the Boolean/flag mutations in
CB-6C are useful repository regression guards, but they are not independent
proofs of source interpretation, horn existence, or physical meaning.

### 17. Novelty and duplication lens — A high, B medium, C low

CB-6A closes a real moving-normal debt and materially corrects CB-5's
geometric ownership. CB-6B adds a useful explicit stage ledger, intrinsic
rank propagation, and wrong-order witness, though its cell placement and
family sequence substantially reuse CB-2/CB-5. CB-6C is mostly disciplined
bookkeeping. Another all-three Boolean or formal overlap replay without a
concrete declared PSRED witness would mostly restate its assumptions.

### 18. Reprioritization lens — pivot to the derivative-half obstruction

The correlated carrier square has passed its bounded gate. The next fertile
question is not another kappa-rank wave and not a horn-election exercise. It
is the unresolved source/operator junction inside the full equation-(9.16)
cells, approached in reverse-conditional form: assume a homogeneous full
cell exists, derive the necessary grading/duality equations, and classify
which source-faithful conventions can satisfy them without altering the
source labels or constructing an action.

## Mandatory integration repairs

1. Scope CB-6A's descent language to a conditional natural morphism on
   admitted O/Spin overlaps. Do not say that CB-6 constructs an observer
   atlas, global Spin cocycle, or globally preferred PS-labelled operator.
2. Replace CB-6C's unrestricted “logical and categorical independence” and
   “joint compatibility proved” wording with **Boolean prerequisite
   independence and conditional type-coherence given horn witnesses**. The
   all-three row is a permitted conditional sentence, not an existence
   theorem.
3. Do not make a horn-independent “joint overlap replay” the automatic next
   swing. Without a concrete declared PSRED cocycle it is definitional. Move
   the full `d0+varpi` half/duality obstruction ahead of it; retain all-three
   overlap testing only if an already-given conditional witness supplies
   nontrivial data to test.
4. Keep all intrinsic ranks separate from CB-5 chartwise ranks. In
   particular, isotropic is intrinsic rank `12` complex, not the legacy
   chartwise rank `8`.
5. Preserve F/imposter, `M_3`, and Z/internal-`144` custody, both halves, the
   downstream position of kappa, and the nonphysical family-kernel ceiling.

## Kill, repair, survive ledger

| item | decision | consequence |
|---|---|---|
| induced-Gram graph/normal split | survive | intrinsic Clifford ownership on nondegenerate graph planes |
| full local H/N kappa square | survive conditionally | natural for admitted O/Spin overlaps with full domain transport |
| actual observer-atlas or triple-cocycle construction | not proved | no global physical descent claim |
| CB-5 fixed trace is intrinsic off flat | kill | it remains a chartwise transported decoration |
| intrinsic rank sequence `0/8/12/16/12/16` | survive | pointwise internal-complex fixture ranks |
| family exact sequence | survive | basis-free algebraic kernels only |
| upstream H210 already contains F | kill | H210 is Z and has zero upstream F projection |
| kappa recovers the internal-144 leg | kill | it constructs a new normal trace-image partner |
| kappa belongs inside equation-(9.16) `varpi` | kill | kappa is strictly downstream of observation and trace |
| zero-order `(1,2)/(0,3)` staged composition | survive | conditional carrier chain on both halves |
| full `d0+varpi` homogeneous cell | kill at current typing | no rank, kernel, adjoint, spectrum, or mass statement |
| eight branch prerequisite table | survive | exact conditional sentence ledger |
| Boolean probe proves actual horn witnesses coexist | kill | witness existence remains assumed |
| unrestricted categorical-independence theorem | kill | no categories or concrete witness models are supplied |
| all-three conditional typed sentence | survive after repair | valid only given all three witnesses |
| twistor route supplies FCORR/ALIGN/PSRED | kill | projector control only |
| source prediction or physical family identified | kill | `SC-GEN-53` does not move |

## Reprioritized path

| path-relative item | priority | fertility | hostile decision |
|---|---:|---:|---|
| reverse-conditional full `d0+varpi` typing classifier | 1 | `8/10` | assume a homogeneous full cell, derive necessary grading/duality relations, and exhaust source-faithful convention classes |
| global O/Spin descent ceiling audit | 2 | `6/10` | distinguish formal naturality, a Spin cocycle, and actual triple-overlap descent without selecting a graph |
| all-three PS overlap replay | 3 conditional | `4/10` with witness, `1/10` without | run only against a concrete declared PSRED cocycle; otherwise tautological |
| additional intrinsic rank strata | retired | `1/10` | current strata already expose rank variation and the CB-5 correction |
| more Boolean horn matrices | retired | `0/10` | prerequisite lattice is complete |
| full twistor/Bach/Penrose detour | deferred | `2/10` | does not repair the source half collision and needs off-scope data |
| action, selector, graph, family-row, reduction, or external-datum construction | off limits | not scored | violates the conditional-build contract |

## Recommended next conditional swing

Run a bounded **CB-7 reverse-conditional equation-(9.16) half/duality gate**:

1. begin with the superposition hypothesis that the displayed
   `d0+varpi_-+` and `d0+varpi_+-` cells admit one homogeneous typed
   realization;
2. derive the necessary parity equation for the derivative, H210 zero-order
   term, barred-row duality, form degree, and ambient half labels;
3. enumerate only source-faithful possibilities: same-half density dual,
   opposite-half density dual, degree-twisted grading, and any explicitly
   declared Shiab/duality horn already present in prior art;
4. require both forward cells, both reverse-shaped cells, both ambient
   halves, and source section-11.2 labels to pass simultaneously;
5. kill any branch that works only by silently relabeling `zeta+/-`, treating
   bars as field adjoints, inserting kappa into varpi, or deleting one half;
6. if no branch passes, bank a conditional no-go for the current source/K77
   grammar; if one passes, carry only its typed zero/full-cell distinction to
   the next gate; and
7. stop before constructing an action, background, selector, external datum,
   family row, PS reduction, physical domain, quotient, mass, or spectrum.

This is a stronger discriminator than another all-three overlap replay. It
attacks the only remaining source/operator collision that currently prevents
the conditional zero-order chain from becoming a typed full displayed cell.

## Strict claim ceiling

CB-6 proves an intrinsic, conditional, co-moving correlated H/N carrier map
on admitted graph/Spin overlaps and a correctly ordered composition of the
conditional H210 zero-order equation-(9.16) arrows through that map. It also
provides an exact eight-row prerequisite ledger for FCORR, ALIGN, and PSRED.
It does not construct an actual global observer atlas or PS reduction, prove
that the three horn witnesses exist together, resolve the derivative-half
collision, establish a common adjoint/domain, or select source provenance,
a family, a luminous half, a physical quotient, an internal-`144` particle,
a mass, scale, threshold, spectrum, observable, or phenomenology. The parent
carrier remains non-chiral and both conjugate halves remain present.
