---
title: "Hostile review — selected-K117 RSAP TT symbol-order custody"
status: complete
doc_type: hostile_review
created: "2026-08-15"
artifact: explorations/conditional-build/selected-k117-rsap-tt-symbol-order-custody-and-moving-hessian-gate-2026-08-15.md
lenses: [layer0_semantics, variational_pde, principal_symbol, krein_operator, source_criticism, action_ownership, exact_algebra, claim_status]
---

# Hostile review

## Strongest objections

1. **`theta h^2` looks algebraic, so K116 may treat it as a mass.** Not after
   provenance is restored. The owner explicitly says it was evaluated at a
   fixed nonzero TT symbol and gives `delta J_hh=d z E_hh`. Freezing `z`
   suppresses differential order; it does not erase it.

2. **Absorb `z` into `u`.** That makes the spacetime background depend on the
   Fourier variable and changes its type. The `z=0` control separates the
   operators exactly.

3. **The observed `(a+beta theta)R` horn selects the full action.** It selects
   an observed scalar `hh` reduction only. The repository already constructed
   a full-pencil completion with the same inherited `hh` block and different
   `h-v` response. The complete moving `Y14` third derivative is open.

4. **The hh-only horn might still own the spectral connection.** Its literal
   divergence operator has first-order coefficient `B=K^-1 dK`. The unique
   simultaneous `K/C` connection requires `2A_*`; their difference has
   determinant `-(dr/r)^2`, so no lower-order repair can match it.

5. **Use `(1/2)C dC` anyway.** That formula assumed a constant Krein form.
   Here `K(r)` moves. Solving both compatibility equations gives the unique
   diagonal connection in K117.

6. **The field-redefinition completion proves zero transport.** It does not
   select itself. It proves non-identifiability from the inherited `hh` block.

7. **The correction kills the keep-and-grade route.** It does not. The
   constant-background `hh` horn has an exact positive spectral majorant. What
   remains open is the complete moving action pencil and whether its Euler
   operator preserves instantaneous spectral sectors.

8. **Proceed directly to the 98D attachment.** Premature. Carrier, background,
   operator and domain ownership must survive before attachment.

## Verdict

Pass at exact symbol-order and observed-horn grade. K116's concrete mass
deformation must be retained only as hypothetical algebra, not as the
inherited action target. K118 begins at the already-named full-moving `D3I`
owner and may not choose between the two completion controls.
