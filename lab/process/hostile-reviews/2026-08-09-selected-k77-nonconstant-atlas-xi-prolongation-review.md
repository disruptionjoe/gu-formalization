# Hostile review: selected K77 nonconstant atlas and Xi prolongation

Date: 2026-08-09
Verdict: `CANDIDATE_SURVIVES__GENERIC_AFFINE_AND_SOURCE_REDUNDANCY_GRADE__NATIVE_K77_MOVING_GEOMETRY_OPEN`

## Three charges

### 1. Where the summary could outrun the artifact

“A global `Y14` solution exists” is false. The construction lives on a
contractible coordinate ball with frozen `Phi`, Shiab, Hodge, density and
observation coefficients. Nonconstant gauge transitions are exact, but the
native geometric coefficients have not been moved.

“GU predicts two dark-energy values” is also false. The two values are the
intersection of the source family with a repository-imposed homogeneous
ansatz. They are witnesses, not source-selected magnitudes.

### 2. Where rigor could defend a superseded or mistyped object

Treating `Xi=0` as an independent third equation would repeat the v0.108
failure at one derivative higher. The source explicitly defines
`Xi=D_omega Upsilon` and calls it redundant when `Upsilon=0`.

Conversely, deriving only tensor conjugation would defend a curvature atlas
while leaving the connection unpatched. The affine `g^-1 dg` term is
load-bearing and the negative control proves it.

### 3. What must change if the result stands

- `dissolved`: generic nonconstant affine gauge-patching blocker;
- `dissolved`: `Xi` as an independent amplitude-selection equation;
- `survives`: actual moving K77/Y14 coefficient and observation prolongation;
- `needs-recheck`: the two homogeneous branches under moving geometry and the
  complete selected-action primitive epsilon Euler/Noether identity;
- `survives unchanged`: verdicts, global residue, five booked quotients,
  P1/P2/P3, canon, public posture and distinct action parents.

## Required and divergent lenses

### Layer-0 semantics

Accepts the source's redundant Euler relation and rejects its identification
with either an independent scalar equation or an off-shell gauge identity.
Accepts frozen-frame branches only as a repository construction grade.

### Prior art

Earlier three-patch packets proved constant conjugation and associated-bundle
descent. This wave is not a duplicate because it carries the affine connection
term and verifies recomputed curvature/distortion under nonconstant maps.

### Gauge and differential geometry

The direct/sequential affine cocycle and covariant tensors are exact. The
faithful `2x2` coefficient model proves the generic gauge law, not the full
selected K77 representation or nontrivial `Y14` topology.

### Variational bicomplex

The displayed `Xi` factors through `Upsilon` and adds no stationary equation.
The complete primitive-epsilon variation of the selected reconstructed action,
including moving Shiab, still needs a direct derivation.

### Symplectic geometry

No presymplectic form has been pulled back to the two ansatz branches. Whether
the remaining amplitude is a gauge direction, boundary charge, physical
modulus or obstructed direction remains undecided.

### Spencer/formal integrability

Constant coefficients make prolongation close automatically. This is useful
existence evidence but the native moving coefficients can generate new
compatibility conditions; that is the next decisive calculation.

### Microlocal PDE

An open coordinate-ball solution does not establish a Cauchy/boundary problem,
constraint propagation, characteristic quotient or common Green domain.

### Krein/operator theory

No positive majorant, closed operator or maximal dissipative domain is
selected. The exact algebraic number field does not change that.

### Complex/path-integral analysis

Algebraic-conjugate classical branches do not select a contour or real saddle
weight. No determinant, measure or reflection-positivity result is present.

### Cosmology and source criticism

The source supports redundancy and the two-to-one bar, not the branch values.
No inference to observed dark energy, screening or DESI is admissible.

## Controls and rerun receipt

- main exact route: `42/42 PASS`;
- independent Sage/`QQ(sqrt(3))` route: `14/14 PASS`;
- omission of `g^-1 dg` breaks curvature covariance;
- reversed transition order breaks connection descent;
- off-shell `Xi` covariance is nonvacuous;
- no verdict, residue, quotient, datum, canon or posture moves.
