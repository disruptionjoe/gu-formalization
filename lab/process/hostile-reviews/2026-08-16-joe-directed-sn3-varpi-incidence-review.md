---
artifact_type: hostile_review
status: review
doc_type: joe-directed-sn3-varpi-incidence-hostile-review
created: 2026-08-16
work_item: SN3-HOSTILE-REVIEW
channel: majorana_126_neutrino_mechanism
title: "Hostile review of SN-3: the grade-five parent packets and shared-owner obstruction survive, but the X_D port assignment composes the centre classes incorrectly, the X_R bridge owes a zero-order/coindex descent, and the Cartan conventions must be harmonized"
grade: "INDEPENDENT THIRTEEN-LENS HOSTILE REVIEW plus post-repair delta verification. Original replay: SN3-A 51/51, SN3-B 56/56 plus 9/9 mutants, and SN3-C 69/69 plus 10/10 planted controls; narrow dependencies CB-A, CS-1 and independent MJ-1 also replayed as recorded below. Post-repair replay: SN3-A 56/56, SN3-B 65/65 plus 11/11 mutants, SN3-C 69/69 plus 10/10 planted controls. All three material typing repairs and both intended failure modes are now applied. Final verdict: PASS_AFTER_REPAIR__BANK_AND_DEPENDENCY_FENCE."
disposition: HOSTILE_FINDINGS_PRESERVED_AS_HISTORY__ALL_THREE_MATERIAL_REPAIRS_VERIFIED__CLASS_COMPOSITION_AND_CROSSED_CARTAN_FAILURE_MODES_NOW_TESTED__GRADE5_XD_XR_INTERNAL_PARENT_CONTENT_SURVIVES__CELL_CUSTODY_AND_SHARED_OWNER_VALUATION_OBSTRUCTION_SURVIVE__BANK_AND_PAUSE_LANE
canon_verdict_change: none
steering_effect: "The required hostile repairs are applied and verified. Bank the grade-one/five X_D and grade-five X_R internal parent census, exact cell custody, formal pp/mm native-one-form compatibility, conditional-only descended X_R southeast match, and shared-owner valuation obstruction. Pause this lane. Reopen only if read-only archaeology finds an already-owned source-native zero-order/coindex descent and slot-to-observed-line incidence receipt; do not construct either antecedent here."
canonical_effect: pending_integration
depends_on:
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-source-native-neutrino-conditional-build-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-wave-neutral-pencil-reprioritization-2026-08-16.md
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-existing-varpi-xd-incidence-census-2026-08-16.md
  - tests/channel-swings/joe_directed_sn3_existing_varpi_xd_incidence_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-existing-varpi-xr-incidence-census-2026-08-16.md
  - tests/channel-swings/joe_directed_sn3_existing_varpi_xr_incidence_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-source-slot-neutral-line-incidence-classifier-2026-08-16.md
  - tests/channel-swings/joe_directed_sn3_source_slot_neutral_line_incidence_probe.py
  - lab/active-research/joe-directed/class-shift/cs1-first-order-shift-is-the-chirality-grading-2026-08-15.md
  - lab/active-research/joe-directed/ledger-advancement/la8-rae2-is-refuted-at-the-settled-form-leg-and-the-open-fork-is-not-load-bearing-2026-08-15.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This review tests the
> source-native K77 connection-parent route. Standard Higgs/VEV, ordinary
> family-index or net-chirality, and scalar-`126` results bind only their named
> comparators. They do not decide this route without an explicit typed bridge.
> The D5 arithmetic below is transported only as internal representation
> arithmetic; no scalar field, action or VEV is imported.

# SN-3 hostile review

## Verdict first

SN-3 has a real, useful result, but not the complete one claimed by SN3-A's
port table.

The exact internal representation census survives. Selected K77 grades one
and five contain the `X_D` internal type, with grade five the richer parent
packet. Grade five also contains the unique `X_R` internal singlet line and
its conjugate, with the correct observed charges and same-Weyl transpose type.
The fixed-`H_q` dimensions, equation-(9.16) owner recurrence, crossed reverse
custody, Q0/SM1 formal endpoint counts, absent displayed southeast owner, and
shared-owner valuation obstruction all survive.

One load-bearing composition does not survive. SN3-A equates a pure-internal
odd Clifford component of centre class two with a **positional** net-class-two
`pm/mp` connection cell. That omits the connection one-form leg. The banked
CS-1/SN-2 convention says the native one-form leg is itself class two. Hence

```text
internal odd Clifford class 2 + connection one-form leg class 2 = net class 0.
```

On the native one-form reading, the grade-one/five internal packet can
formally align with the class-diagonal `pp/mm` positions, not the `pm/mp`
positions. A `pm/mp` reading could be discussed only after an independently
typed contraction/descent removes or compensates the coindex. LA-8 says the
actual observation pullback returns a four-dimensional one-form, and SN-3
constructs no replacement descent. Therefore the asserted `pm/mp` X_D ports
and the phrase “pp/mm wrong-grade controls” are killed as filed.

The same issue narrows SN3-B. Its grade-five `X_R` **internal parent packet**
survives, and a hypothetical zero-order class-two insertion would match the
two class-two southeast positions. But a component of `varpi` is still a
one-form. Calling its pure-internal Clifford summand “Lorentz scalar” does not
dispose of the differential-form coindex. Since the source supplies no
southeast cell at all, this does not change the negative placement result; it
does mean the southeast class match must be stated as conditional on an
independently supplied zero-order descent.

Verdict:

```text
PASS_AFTER_THREE_MATERIAL_TYPING_REPAIRS_AND_TWO_PROBE_REPAIRS.
```

## Twelve divergent lenses

### Lens 1 — source custody

Equation 9.16 remains a candidate matrix on four distinct classical fields,
with exact reversed row/column order, twelve printed supported cells and four
southeast zeros. The source admits an unspecified nonzero southeast rival but
does not name its cells or coefficients. All three SN-3 artifacts preserve
candidate strength and independent barred/unbarred custody. **Survives.**

### Lens 2 — Clifford and representation arithmetic

The exact exterior census gives

```text
X_D multiplicities (+1/2,-1/2):
q=1 (1,1), q=2 (0,0), q=3 (2,2), q=5 (2,2).

X_R:
q=1 no B-L=-2;
q=3 one B-L=-2 line but Y=-1;
q=5 one (-2,0,+1,0) line and one conjugate line.
```

CB-A independently reproduces the grade-five `2+2` doublet and two-singlet
full-`252` counts. The independent MJ-1 verifier reproduces the real
`252 = 126 + 126bar` internal decomposition and the rank-one same-Weyl block.
**Internal parent content survives.** “Grade five is strongest” is valid only
as a representation-parent/multiplicity ranking, not as an incidence ranking.

### Lens 3 — K77 real and half typing

The review finds no collapse among:

1. the real grade-five `252` and its two conjugate complex Hodge eigenspaces;
2. the two ambient K77 Weyl halves exchanged by a bare odd Clifford monomial;
3. the two four-dimensional Weyl factors; and
4. the independent barred/unbarred source fields.

SN3-B states these distinctions explicitly. SN3-A also keeps bare `X` versus
Gram-inserted `B X` parity separate. **Survives.** Neither internal Hodge half
may be identified with an ambient K77 half.

### Lens 4 — equation-9.16 cell and centre bookkeeping

The positional pattern is exactly

```text
[[0,2],
 [2,0]]
```

in every quadrant. `pp/mm` occupy net-class-zero positions; `pm/mp` occupy
net-class-two positions. This positional ledger survives. The error is the
next inference: a position's net class is not automatically the internal
Clifford coefficient's class. For an actual connection cell, the class-two
one-form leg must also be composed. CS-1 explicitly says the six
`varpi`-only, class-diagonal cells require a class-two insertion. **SN3-C's
positional table survives; SN3-A's internal-grade-to-`pm/mp` port assignment
does not.**

### Lens 5 — observed Q0 versus SM1 charges

Q0 retains the two internal neutral line types `L^0,N^c`; SM1 retains only
`N^c`. Thus a formal `L <-> N^c` endpoint predicate is empty on SM1 and is
available only on Q0. SN3-C correctly calls `4` and `1` the endpoint-pair
counts per printed cell and `2` the Q0 `L/N^c` orientations. Those are
cardinalities of formal line predicates, not ranks, multiplicities of a
restricted map, or proofs of nonzero incidence. **Survives with its existing
ceiling.**

### Lens 6 — form and coindex typing

This is the decisive adverse lens. A pure-internal Clifford endomorphism can
be Lorentz-scalar in its **endomorphism** indices while the connection
component carrying it remains a one-form. Observation pullback returns a 4D
one-form. SN-3 supplies no covariant coindex contraction that makes it a
zero-order scalar coefficient. Therefore:

- `X_D` internal representation membership is not a zero-order Yukawa map;
- the `pm/mp` zero-order port reading is unearned;
- a grade-five `X_R` internal line is not yet an SE zero-order coefficient;
- the native one-form class composition must be used whenever an actual
  printed `varpi` cell is discussed.

**Material repair required in SN3-A, SN3-B, and the SN3-A probe wording.**

### Lens 7 — Grassmann symmetry

The internal same-Weyl transpose signs `sym/anti/sym` for grades `1/3/5`
survive. With the antisymmetric four-dimensional charge-conjugation form,
grade five gives an alternating total one-line coefficient, while grade three
does not. This is an internal packet predicate. It does not identify
`bar-nu` with `nu`, choose a K77 local fixed graph, or prove that an equation-
(9.16) SE bilinear has this symmetry. **Representation-level result survives;
source-bilinear promotion remains blocked.**

### Lens 8 — crossed reverse custody

The complete source-position statement is exact:

```text
pp <-> bar-pp,
mm <-> bar-mm,
pm <-> bar-mp,
mp <-> bar-pm.
```

Thus the `pm/mp` reverse partners are crossed. **The custody theorem
survives.** After the class-composition repair it is no longer the reverse
custody of the grade-one/five native X_D candidate positions: the formal
native one-form positions are `pp/mm`, whose reverse labels are preserving.

### Lens 9 — fixed-`H_q` scope

The exact real intersections survive:

```text
grade 1: 1, exactly span(gamma(q));
grade 2: 36, q-avoiding monomials;
grade 5: 126, q-containing monomials.
```

The nonzero defects have distinct Clifford masks, so hidden linear
cancellation does not enlarge the kernels. The grade-five `126` here is a
`q`-dependent real subspace dimension, not one complex Hodge eigenspace and
not a conventional scalar `126`. Because fixed `q` breaks the weak group used
for the doublet count, no weak multiplicity may be inferred inside that
subspace. SN3-A already states these fences. **Survives.**

### Lens 10 — valuation and shared-owner logic

The four unbarred owners recur once in `A` and once in `B`; barred partners
own `C` independently. With one valuation per owner and cellwise leading
supports, identical nonzero support status in `A` and `B` forces
`v(A)=v(B)`. The strict `(0,1,1)` horn therefore requires every valuation-zero
owner surviving in `A` to vanish in `B`, valuation-one support to survive in
`B`, and barred valuation-one support to survive in `C`. The direct source
cells prevent cross-owner cancellation from invalidating the minimum formula.
Invertibility, neutral closure, and nondegeneracy of `C_0 A_0^-1 B_0` remain
separate. **Necessary theorem survives; sufficiency remains explicitly
unclaimed.**

### Lens 11 — comparator routing and authority

The use of D5 weights and Hodge labels is protected by a narrow typed bridge
to the pure-internal endomorphism summand. MJ-2's conventional scalar-carrier
null result cannot bind the later full-`U(64,64)` connection parent. Conversely,
MJ-1's standard scalar action/VEV interpretation cannot be imported into it.
**Routing survives.** The repair above is source-native and does not revive a
standard `126` route.

### Lens 12 — falsification, conditional-build authority, and next-action

The probes reject the intended wrong-grade, wrong-charge, lost-half,
same-label reverse, selected-SE and promoted-incidence mutants. They do not
test the missing class composition: the SN3-A probe asserts positional class
two and internal odd grade separately, then treats equality of the two
numbers as composition. A required mutant is:

```text
internal_class=2, form_leg_class=2
must produce net_class=0, not 2.
```

No admissible next step may construct a contraction, action, vacuum, external
datum, new coefficient, SE owner, relative scale or reality selector merely
to rescue the hypothesis. An archaeology-only search for an already-banked
descent/incidence receipt is allowed. LA-8 currently supplies the adverse
one-form result, not the desired receipt. **The lane is dependency-fenced
after repair.**

### Lens 13 — Cartan-convention compatibility

SN3-A and the banked CB-A representation census use

```text
T3L=(w4+w5)/2,   T3R=(w4-w5)/2.
```

SN3-B instead prints

```text
T3L=(w4-w5)/2,   T3R=(w4+w5)/2.
```

The two are related by `e5 -> -e5`, the D5 diagram/outer automorphism that
exchanges the two `SU(2)` factors and can exchange spinor/Hodge labels. It is
not the same convention in one fixed weight basis. Because SN3-B retains both
conjugate internal Hodge lines, its **existence and multiplicity** result is
invariant under this relabelling. Its named winner is not: in the banked CB-A
convention the `X_R` line is

```text
-e1-e2-e3+e4-e5,
```

not the printed `-e1-e2-e3+e4+e5`; the conjugate changes accordingly. The
grade-three `Y=-1` failure, Grassmann sign, K77 parent membership and SE
positional class table are unchanged. **Parent-packet existence survives, but
SN3-B's displayed Cartan formula, named line, charge fixture and probe must be
harmonized before cross-artifact integration.**

## Exact survival and kill ledger

| statement | hostile verdict | repair or ceiling |
|---|---|---|
| grade `1/5` contain internal `X_D` types; grade `5` has `2+2` copies | **SURVIVES** | parent representation only |
| grade `5` contains the unique internal `X_R` line and conjugate | **SURVIVES** | parent representation only |
| grade `3` fails `X_R` by `Y=-1` and one-line Grassmann type | **SURVIVES** | no family tensor may be invented |
| fixed-`H_q` dimensions `1,36,126` | **SURVIVES** | no weak count; no Hodge identification |
| every quadrant has positional net classes `0,2;2,0` | **SURVIVES** | positional total only |
| `pm/mp` are the grade-one/five native X_D ports | **KILLED AS FILED** | omits class-two one-form leg |
| `pp/mm` are wrong-grade X_D controls | **KILLED AS FILED** | native one-form composition instead gives net class zero |
| crossed `pm/mp` reverse-owner custody | **SURVIVES** | cell-ledger fact, no longer X_D-port fact |
| Q0 gives formal `L/N^c` orientations; SM1 gives none | **SURVIVES** | counts, not ranks or incidence |
| displayed branch has no X_R southeast port | **SURVIVES** | source-admitted rival remains unselected/unlabelled |
| hypothetical class-two zero-order X_R matches two SE positions | **SURVIVES CONDITIONALLY** | first require zero-order/coindex descent |
| internal Hodge conjugacy and ambient K77 halves are distinct | **SURVIVES** | both retained |
| SN3-B's named `X_R` weight in the same Cartan basis as SN3-A | **KILLED AS FILED** | harmonize by the D5 outer automorphism; existence/multiplicity survives |
| shared owners generically force `v(A)=v(B)` | **SURVIVES** | on common nonzero cellwise support |
| strict `(0,1,1)` needs leading-support separation | **SURVIVES** | necessary, not sufficient |
| actual X_D observed incidence exists | **FAILS / UNWITNESSED** | missing line map and coindex descent |
| actual X_R SE incidence exists | **FAILS / UNWITNESSED** | four displayed zeros; rival unowned |

## Required material repairs

1. **SN3-A artifact and probe:** separate `internal Clifford class`,
   `connection form-leg class`, and `positional net cell class`. Delete the
   claim that grade-one/five content selects `pm/mp` and that `pp/mm` are
   wrong-grade controls. On the native one-form reading record `pp/mm` only as
   formal class-compatible positions; actual observed incidence still fails.
   Preserve the crossed `pm/mp` custody theorem as a source-ledger result.
2. **SN3-B artifact/probe ceiling:** replace “Lorentz-scalar pure-internal
   summand” with “Lorentz-scalar in the endomorphism indices, with the
   connection one-form coindex still present.” State that the two SE class-two
   matches apply only to a hypothetical zero-order descent; no such descent or
   source placement is constructed.
3. **SN3-B Cartan harmonization:** adopt the already-banked CB-A convention
   `T3L=(w4+w5)/2`, `T3R=(w4-w5)/2`; replace the winner and conjugate by their
   `e5 -> -e5` images and rerun the charge/singlet tests. If the alternate
   convention is retained for a local calculation, declare the D5 outer
   automorphism explicitly and do not compare named Hodge/half labels across
   the two files without transporting them.
4. **Probe repairs:** add the explicit `2+2=0 mod 4` mutant above. The current
   51/51 SN3-A result is internally consistent with its own conflated model;
   passing it does not protect the port assignment. Add a cross-probe Cartan
   fixture requiring SN3-A, SN3-B and CB-A to use the same `T3L/T3R` functions
   or to declare and test the automorphism between them.

The SN3-C artifact needs no material arithmetic repair. Its positional ledger,
formal endpoint language, and shared-owner theorem should control the
integration wording.

## Reproduction receipt

Run on 2026-08-16 from the repository root:

```text
SN3-A                                             51/51 PASS
SN3-B                                             56/56 PASS; 9/9 mutants
SN3-C                                             69/69 PASS
SN3-C --selftest                                  69/69 PASS; 10/10 plants
CB-A representation census                        PASS
CS-1                                             105/105 PASS
CS-1 --selftest                                   13/13 mutations drove failure
MJ-1 independent verifier                         31/31 PASS
CB-A-convention direct X_R enumeration             q5 unique (-1,-1,-1,+1,-1)
MJ-1 primary probe                                NOT RUN: ModuleNotFoundError sympy
LA-8 form-leg probe                               NOT RUN: ModuleNotFoundError sympy
```

The two environment failures do not create contrary arithmetic. The
independent MJ-1 implementation does not require `sympy` and passed. LA-8's
committed artifact remains dependency evidence for the one-form conclusion,
but this review does not claim an independent replay of its sympy script.

## Claim ceiling

After repair, SN-3 establishes only the following conditional facts:

- the full pointwise K77 parent has internal grade-one/five `X_D` content and
  internal grade-five `X_R` content;
- grade five is the richest selected internal representation packet;
- the printed source grammar has the exact owner/cell/half ledger recorded by
  SN3-C and no displayed southeast owner; and
- generic scaling of the shared `A/B` owners does not populate the strict
  `(0,1,1)` horn without leading-support separation.

It establishes no source-selected connection component, observed-line map,
coindex contraction, Lorentz-scalar zero-order coefficient, nonzero neutral
rank, closed neutral subpencil, southeast completion, action, vacuum, external
datum, scale owner, local or global reality owner, quotient, domain, physical
chirality, hierarchy, mass, pole, mixing, family count, threshold, observable
or phenomenology.

## Reprioritization

### Within this lane

1. **Repair and bank** the internal parent census, source cell ledger, and
   shared-owner valuation obstruction.
2. Permit one **read-only archaeology check** for an already-owned
   source-native zero-order/coindex descent together with an equation-(9.16)
   slot-to-observed-line receipt.
3. If that receipt is absent, **pause the neutrino lane**. Do not create the
   missing descent, coefficient or southeast owner inside the conditional
   build.
4. Do not repeat the grade census, fixed-`H_q` calculation, conventional
   scalar-`126` route, neutral endpoint count or abstract `(0,1,1)` analysis.

### Relative to the current exploration path

SN-3 was fertile as a finite parent-content and obstruction census. It is no
longer the best active target because both physical-looking incidences terminate
at the same missing source-native descent/placement owner. The conditional-
build portfolio should now prefer a branch whose next test composes already-
owned maps and can be falsified without adding an action, vacuum, datum,
coefficient or selector. SN-3 remains banked evidence and should reopen only
when such an owner arrives independently; otherwise further swings here would
duplicate the hourly progression or manufacture an off-limit antecedent.

## Post-repair verification addendum

This addendum preserves the hostile findings above as the review history and
checks only the repaired SN3-A/SN3-B deltas. No original negative finding is
erased: each is either implemented as a correction or retained as the claim
ceiling.

### Delta audit

| original required repair | repaired evidence | post-repair verdict |
|---|---|---|
| compose internal Clifford class, connection form-leg class and positional cell class | SN3-A now computes `2+2=0 mod 4`, places the native one-form packet formally at `pp/mm`, and rejects the old dropped-form-leg rule | **APPLIED** |
| remove `pm/mp` X_D-port promotion while preserving crossed custody | SN3-A calls `pm/mp` crossed custody ledger-only and records label-preserving `pp/mm` reverse ownership | **APPLIED** |
| keep the X_R `varpi` coindex and make the class-two SE fit conditional on a zero-order descent | SN3-B distinguishes pure-internal class `2`, coindex class `2`, raw class `0`, and an unbuilt descended class-`2` insertion | **APPLIED** |
| harmonize SN3-B with the banked CB-A/SN3-A Cartan convention | SN3-B now uses `T3L=(w4+w5)/2`, `T3R=(w4-w5)/2` and the unique line `-e1-e2-e3+e4-e5` | **APPLIED** |
| add failure controls for the class-composition and crossed-Cartan errors | SN3-A rejects dropping the form leg; SN3-B rejects the old crossed-Cartan candidate and tests the coindex explicitly | **APPLIED** |

All three material repairs and both intended failure modes are therefore
applied. One nonblocking durability note remains: the SN3-B probe fixes the
canonical formulas locally and rejects their swap, but does not import and
string-compare the SN3-A/CB-A definitions. This addendum independently checked
all three files and found them equal. A future shared Cartan helper would make
that equality mechanically coupled, but no scientific or integration blocker
remains in this wave.

### Repaired validation receipt

Run from the repository root after the repairs:

```text
SN3-A                                             56/56 PASS
SN3-B                                             65/65 PASS; 11/11 mutants
SN3-C                                             69/69 PASS
SN3-C --selftest                                  69/69 PASS; 10/10 plants
```

The repaired A/B outputs now test the two failure modes that the original
green probes missed. SN3-C required no delta repair and remains the controlling
source-cell/shared-owner ledger.

### Final survival ledger after repair

- **Bank:** grade-one/five internal `X_D` parent content; grade-five internal
  `X_R` parent content; fixed-`H_q` dimensions; distinct internal Hodge and
  ambient K77 halves; exact eight-cell and reverse-owner custody; Q0/SM1 formal
  endpoint counts; no displayed southeast owner; and the shared-owner
  leading-support obstruction.
- **Corrected and bank:** the native grade-one/five `varpi` one-form has formal
  class compatibility at `pp/mm`; `pm/mp` crossed custody is ledger-only.
- **Conditional only:** a descended class-two grade-five `X_R` insertion would
  match two southeast positions, but the descent is unbuilt and the source
  places no such coefficient.
- **Still fails:** actual X_D observed-line/coindex incidence and actual X_R
  southeast incidence.

### Refreshed next priority

The SN-3 finite census is complete and should be banked. This lane is not the
next active mathematical target: both apparent continuations require the same
missing source-native descent/placement owner. Permit only a bounded read-only
archaeology check for an already-existing zero-order/coindex descent **and**
slot-to-observed-line receipt. If no such receipt is already banked, keep the
lane paused and prioritize a conditional-build branch whose next falsifier
composes already-owned maps. Do not build an action, vacuum, external datum,
new coefficient, southeast owner, relative scale or reality selector to reopen
SN-3.

Final post-repair disposition:

```text
PASS_AFTER_REPAIR__BANK_AND_DEPENDENCY_FENCE.
```
