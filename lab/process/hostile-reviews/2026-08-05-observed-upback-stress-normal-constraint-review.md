---
artifact_type: hostile_review
date: 2026-08-05
target: explorations/conditional-build/observed-upback-stress-normal-constraint-vacuum-2026-08-05.md
verdict: PASS_WITH_MATERIAL_REPAIRS__HILBERT_STRESS_NOT_SOURCE_TOTALIZATION__CONSTRAINT_QUOTIENT_NOT_SINGLE_POLE__QUADRATIC_VACUUM_NOT_FULL_ACTION
---

# Hostile review: observed stress, normal constraints and variable vacuum

## Review charges

1. **`SUMMARY_OUTRUNS_ARTIFACT`:** find every place the prose promotes a
   finite action theorem, plane-wave control, symbol quotient or quadratic
   stationary calculation beyond its actual scope.
2. **`DEFENDS_SUPERSEDED_OR_MISTYPED_OBJECT`:** find every place rigor is being
   spent defending literal `VU`, a momentum-free stress, rank-only physical
   recovery or the already-killed ordinary ambient Cauchy horn.

## Specialist hostile readings

| lens | objection | repair / disposition |
| --- | --- | --- |
| variational bicomplex | mixed reciprocity constructs a derivative of the metric Euler term, not the nonlinear term itself | required the radial fundamental-theorem reconstruction and zero-field boundary; accepted |
| spin/Krein geometry | the earlier failed Higgs test showed that omitting the pairing can reverse a physical conclusion | exact control uses `bar psi=psi^dagger gamma0`; no pairing-free bilinear is cited |
| general relativity | symmetry and conservation do not prove the tensor appears with the Einstein operator | report separates Hilbert-stress construction from gravitational placement and exposes the double pole |
| hyperbolic PDE | a two-dimensional null quotient can hide repeated characteristics/generalized solutions | determinant and inverse of the TT field matrix are computed; pole order two is stated explicitly |
| BV/symplectic | constraint-compatible kernel modulo gauge is not phase-space multiplicity | report calls it a characteristic quotient only and retains generalized-partner debt |
| gauge geometry | connection current and stress live in different Euler rows | equality remains open and requires derivative/soldering data |
| cosmological dynamics | invertible quadratic vacuum equation proves only stationarity, not stability or the full nonlinear vacuum | inertia `(6,4)` is reported; the existing `T`-cubic and non-equilibrium branches remain open |
| source criticism | the primary source says “should” and admits unfinished cancellations | decisive return is `SOURCE-CORRECTS`; no formula is attributed to Weinstein |
| exact computation | a finite toy could be fitted to the desired radial identity | the identity is proved generally and then tested; parameter count was declared zero before consequence |
| science council | a scoped failure could trigger an orthodox rejection of the whole geometry | only the repaired quadratic placement and quadratic vacuum horn are killed; cancellation, alternative placement and constrained ambient horns remain live |

## Material repairs required before pass

### Repair 1: do not call `VU` the stress tensor

The first draft inherited the source receipt's shorthand that “up-and-back”
is a diagonal composite. The primary source does not publish this equality.
The artifact now distinguishes:

```text
T_H                         nonlinear metric Euler covector
V_raw = D_psi T_H          mixed return derivative
VU                          primalized linear Hessian response
integral V_raw(t psi) psi  exact reconstruction of T_H
```

### Repair 2: constraint closure is not GR recovery

The exact `10 -> 6 -> 2` quotient remains correct. It was insufficient as the
summary diagnosis because `det J_TT=-z^2`. The report now states that plus and
cross survive with a double-pole generalized partner, and the next gate is
single-pole placement rather than another quotient replay.

### Repair 3: the vacuum result is quadratic-horn only

The initial wording “the current action has only the zero vacuum” outran the
calculation. The full source action has nonlinear `T` dependence. The accepted
claim is only:

```text
homogeneous observed rank-ten quadratic horn
+ fixed nonzero gain
+ zero curvature/source
=> v=0 is the unique stationary point, with inertia (6,4).
```

Ledger `LT-GR2d` is scope-corrected from
`PROVEN_UNABLE_BY_CURRENT_ACTION` to `MISSING_CONSTRUCTION`; the full nonlinear
`T`-cubic/non-equilibrium vacuum must be computed before adding a selector.

## Planted failures with power

- Remove `gamma0` from the Dirac pairing: the current changes.
- Equate a `2 x 2` `VU` matrix with a two-component stress covector: the type
  check fails.
- Substitute an Einstein single pole for the repaired metric response: exact
  inverse disagrees (`1/z` versus `-kappa/z^2`).
- Call the indefinite quadratic stationary point a stable minimum: the
  `(6,4)` inertia rejects it.
- Call linear shift response screening: doubling the source doubles `v`.

## Final disposition

`PASS_WITH_MATERIAL_REPAIRS__HILBERT_STRESS_NOT_SOURCE_TOTALIZATION__CONSTRAINT_QUOTIENT_NOT_SINGLE_POLE__QUADRATIC_VACUUM_NOT_FULL_ACTION`.

Accepted at:

- general common-action radial-transgression theorem;
- exact flat Lorentzian Krein-Dirac control;
- flat observed harmonic-symbol/conditional defect-Green grade;
- repaired pre-Shiab quadratic action pole grade; and
- homogeneous observed quadratic-vacuum grade.

Not accepted as a released Weinstein map, connection-current equality, full
moving/curved 85-direction constraint theorem, global `Y14` domain, Einstein
recovery, full nonlinear vacuum, screening, magnitude or cosmology.

## Required disposition tuple and conditional grades

```text
fork_assumed: K77_OBSERVATION_FIRST_PRE_SHIAB_REPAIRED_ACTION
search_space_dim: rank-10 Sym2 gravitational sector plus one exact 4-component Krein-Dirac control
free_object_delta: 0
selector_parameters_added: 0
global_residue_delta: 0
```

| row | grade earned here | hostile boundary |
| --- | --- | --- |
| `LT-GR2b` | `T3` scoped quadratic-vacuum computation | full nonlinear `T`-cubic and non-equilibrium vacuum remain open |
| `LT-GR2c` | `T3` action stress plus exact defect quotient | double pole prevents `T4` Einstein recovery |
| `LT-GR2d` | `T3` scope correction | magnitude and screening remain unbuilt |
| `LT-GR5` | `T3` exact constraint quotient and pole diagnosis | generalized propagation partner remains |
| `LT-GR6` | `T3` action-owned Hilbert stress; `T4` not earned | source totalization, current interface and single-pole placement remain open |

These grades are conditional-match grades, not claim-status or canon
promotions.
