---
artifact_type: hostile_review
created: 2026-08-10
status: SCOPED_SURVIVES__CURRENT_SOURCE_OWNED_RELATIVE_SELECTOR_KILLED__EXPLICIT_EXTERNAL_DATUM_ROUTE_LIVE
reviewed_artifact: explorations/conditional-build/selected-k77-relative-chiral-transgression-ownership-2026-08-10.md
---

# Hostile review: relative chiral-transgression ownership

## Pre-review

- **Fork assumed:** none.
- **Search space:** the current same-bundle `A0` reference/bitorsor, observed
  chiral transgression and v0.150 real-pairing cone.  This can decide the
  current ownership claim, not every relative or quantum construction.
- **New un-owned object on success:** a nonidentity boundary component/integer,
  tangential chiral reduction and real-pairing ratio.
- **What dies:** only the claim that the current source-owned reference data
  themselves select a nonzero relative class and real action coefficient.

## Required lenses

**Layer-0 semantics:** The report correctly separates ambient `P_H`, observed
chiral `SL(2,C)`, bitorsor nonemptiness, winding, continuous Chern--Simons
value and real action pairing.  This charge survives.

**Prior art:** v0.115 already owns the same-bundle relative bitorsor and says it
adds zero selectors.  v0.69/v0.114 already establish the boundary-charge and
classical no-selection fences.  The report explicitly composes rather than
rebrands them.  This charge survives.

**Analytic:** The calculation is finite/topological.  It makes no positivity,
Fredholm or common-domain claim.  The generic interpolation is continuous and
therefore cannot be read as quantized without endpoint data.  This charge
survives at algebraic grade; analytic domain work remains open.

**Symplectic/BV--BFV:** The existing moment-map result prevents silently
declaring every large boundary transformation gauge.  The report retains the
charged horn and does not book a quotient.  This charge survives.

## Charge 1: where the summary outruns the artifact

The first draft tendency was to say the relative route is killed.  That is too
wide.  The exact evidence kills only the **current source-owned same-bundle
identity selector**.  A supplied large-gauge sector, relative APS datum, new
action term or quantum measure is untested.  The final report uses the scoped
claim and retains the explicit external-datum route.

## Charge 2: where rigor defends a mistyped object

The ambient thirteen-boundary `P_H` bitorsor is not the observed
three-boundary Lorentzian chiral connection.  Treating the source-owned `A0`
as already reduced to `SL(2,C)` would make a precise theorem about the wrong
object.  The final report exposes the reduction/pullback as a separate burden.

## Charge 3: what else must change if the result stands

- `v0.150 internal source-owned relative selector`: **dissolved**; canonical
  identity has zero winding.
- `relative/compact-support/APS theories`: **survives**; not tested wholesale.
- `explicit external integer datum`: **survives**; must be coupled and priced.
- `real-pairing choice`: **needs-recheck** after the selected action owns a
  chiral reduction.
- `P3 assignments and current residue`: **survives** unchanged.
- `Euler/BV/common-domain work`: **needs-recheck** only after positive-surplus
  coupling.

## Verdict

```text
SCOPED_SURVIVES__CURRENT_SOURCE_OWNED_RELATIVE_SELECTOR_KILLED__
EXPLICIT_EXTERNAL_DATUM_ROUTE_LIVE
```

No ledger verdict, canon verdict, P1/P2/P3 assignment or public posture should
move.  Five ledger rows may move in distance/evidence only.
