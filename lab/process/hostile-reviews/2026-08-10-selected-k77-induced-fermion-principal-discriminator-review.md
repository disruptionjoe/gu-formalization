---
artifact_type: hostile_review
created: 2026-08-10
status: SURVIVES_SCOPED__PRINCIPAL_PARTIAL_ONLY
reviewed: explorations/conditional-build/selected-k77-induced-fermion-principal-discriminator-2026-08-10.md
verdict_change: none
---

# Hostile review: induced-fermion principal discriminator

## Pre-review

- **Fork assumed:** labelled K77 only; no parent or physical carrier is
  assumed. K77/K95 is not adjudicated here.
- **Search space:** one exact source-guided principal symbol, four natural
  Lorentz submodules and three planted generic rank-192 controls. Finite ranks
  are decidable wholesale; global cohomology/domain is not.
- **New un-owned object on success:** none. A lower-order selector remains
  unowned.
- **What dies:** only principal-order unique carrier or parent selection.

## Charge 1 — where the summary outruns the artifact

The phrase “induced fermion selector” outruns the artifact if it is read as a
physical selector. The exact result is a characteristic-symbol discriminator:
generic planes fail, but `W` and its ASD mirror are identical and every
natural sector has the same dimension-normalized half-kernel. The scoped
summary therefore survives only as `PRINCIPAL_PARTIAL`; “the operator selects
the generation carrier” is rejected.

The `640/832` and `W/mirror` ranks are exact over the Gaussian rationals. The
finite-field route is a separate whole-space and planted-control certificate,
not a substitute for characteristic-zero equality.

## Charge 2 — where rigor could defend a superseded or mistyped object

Applying moving-Spin or two-half **connection** projectors directly to the
fermion subspaces would repeat the boson-versus-fermion carrier type error.
At principal order all three connection parents necessarily coincide because
the connection potential is zero order. The ablations belong in the complete
operator, not in a manufactured principal projector.

Likewise, `W` is a conditional representation submodule, not an accepted
physical carrier. The calculation is still worth running because its mirror
and random controls ask whether the principal symbol adds selectivity beyond
the old Casimir. It does, but not enough.

## Charge 3 — if this stands, what else changes

### Dissolved

- `dissolved`: released-source principal order as a unique `W` selector.
- `dissolved`: principal-symbol choice among full `U(64,64)`, moving Spin and
  two `U(32,32)` halves.

### Survives

- `survives`: Wave 2's exact K77 null cone and `1920/1024/896` ranks.
- `survives`: `W` and its ASD mirror are genuine natural Lorentz submodules,
  unlike planted generic rank-192 planes.
- `survives`: all fixed-`W` theorems at their explicitly conditional grade.
- `survives`: the source-full parent and both reductions as distinct
  lower-order/domain ablations.

### Needs recheck

- `needs-recheck`: all sixteen draft-9.16 cells with the known ambient-half
  sign/parity collision.
- `needs-recheck`: the exact global Krein/reality adjoint and barred/unbarred
  field relation.
- `needs-recheck`: BV differential, constraint propagation and physical
  cohomology.
- `needs-recheck`: domain, Green form, positivity and spectrum.
- `needs-recheck`: mirror removal, mass scramble and count bridge.

## Required lenses

**Layer-0 semantics.** Characteristic kernel, physical kernel, cohomology and
generation count are distinct. Pass after narrowing.

**Prior art.** Wave 2 already owned the principal operator; the new work is its
five-way carrier composition. Pass. It is not a new operator claim.

**Analytic/operator.** Exact finite-dimensional symbol ranks establish the
characteristic set only. They do not provide a closed realization, Fredholm
index, spectral projector or positive energy. Pass after fence.

**Representation.** The polynomial Lorentz projectors are stronger than the
old scalar Spin(10) Casimir. The exact W/mirror equality is the decisive
negative control. Pass.

**Variational.** The principal symbol is common to all connection-parent
ablations. Parent selection must be tested in zero-order/action/BV data. Pass.

**Symplectic geometry.** No presymplectic current, constraint surface or
coisotropic reduction is built. Booking a quotient would be invalid. Pass.

**Source.** `SOURCE-CONFIRMS` principal grammar and `SOURCE-SILENT` selection
are correctly separated. Pass.

## Dissent

The representation lens objects to calling the result purely negative. A
generic rank-192 plane does not reproduce the half-kernel rule, so the
principal operator really does recognize natural module structure. This may
be useful once a separate orientation/reality mechanism chooses between the
two mirrors. The dissent does not rescue unique selection at this gate.

## Verdict

`SURVIVES_SCOPED__PRINCIPAL_PARTIAL_ONLY`.

No ledger verdict, canon verdict, residue, quotient, datum, P1/P2/P3 or public
posture may move. The next admissible Build is the exact lower-order
draft-9.16 `varpi`/reality/BV/domain discriminator with all three parent
ablations.
