---
artifact_type: hostile_review
created: "2026-08-16"
targets:
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb1-h210-k77-rs-intertwiner-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb2-h210-equation916-cross-half-composition-2026-08-16.md
  - tests/channel-swings/joe_directed_cb1_h210_k77_rs_intertwiner_probe.py
  - tests/channel-swings/joe_directed_cb2_h210_equation916_composition_probe.py
status: conditional_composition_survives__required_typing_repairs
classification: BRIDGE_OR_SEMANTIC_BOUNDARY
mandatory_lenses: [source_fidelity, equation916_identity_grade, clifford_signature, gamma_trace, d5_ps_duality, k77_real_complex_hq, rs_grade_owner, four_corners, operator_bilinear, rank_kernel, observation_ceiling, conditional_scope, prior_art, mutation_reproducibility]
canon_verdict_change: none
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This is a hostile review
> of a source-native conditional composition through complex `Spin(10)` and
> Pati--Salam bridge coordinates. Ordinary family indices, net-chirality
> tests, scalar-Higgs VEVs, and conventional SO(10) mass mechanisms do not
> adjudicate Weinstein's imposter/emergent-chirality proposal without a typed
> bridge. Read `lab/methods/source-native-comparator-routing.md` before reuse.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# Hostile review — CB-1/CB-2 H210 conditional composition

## Verdict

`H210_CONDITIONAL_ZERO_ORDER_COMPOSITION_SURVIVES__FOUR_REQUIRED_TYPING_REPAIRS__OBSERVATION_NEXT`.

The combined result survives at a narrower and useful grade. Under the
declared `H210` horn, the current real `Cl(7,7)` carrier admits a nonzero,
injective, Pati--Salam-equivariant, gamma-traceless internal vector-spinor map.
Its relative `A_6:B_4` coefficient is fixed to `-2:3`, written in CB-1's
normalization as `-2/5,+3/5`. Its coefficients have shifted Clifford grades
`3/5` (or `5/7` in the Hodge-dual presentation), so a pure grade-six owner
blade is not itself the Rarita--Schwinger port.

Those odd shifted grades exchange ambient K77 Weyl halves. With the inherited
opposite-half density-dual reading of the released equation-9.16 row reversal,
that is exactly the parity required by the zero-order summands in cells
`(1,2)=d0+varpi_-+` and `(0,3)=d0+varpi_+-`, together with the displayed
reverse-shaped cells `(2,1)` and `(3,0)`. The bare `varpi_++/varpi_--` cells
remain exact wrong-channel controls. Thus CB-1 now closes CB-2's stated
zero-order parity gate; it does not close the derivative-cell collision,
global density/adjoint, fixed-`H_q` real-form fork, observation, quotient, or
physical domain.

Four repairs are required before integration:

1. CB-2 still describes the CB-1 parity/rank composition as `TYPE_MISSING` and
   queues it as the next gate, although the completed CB-1 probe settles it for
   the zero-order summand.
2. CB-2's phrase “full `384` carrier” is too broad. The `384` is the full
   **counted internal `16/144` D5 sector** in that finite packet, after
   suppressing external multiplicities and excluding the known observer
   complement. Its `64/320` rank/kernel pair is not a rank/kernel theorem for
   the complete GU fermion carrier.
3. The row-to-output permutation must remain explicitly typed as the inherited
   opposite-half density-dual bridge. It is not a source-supplied field reality
   condition, formal adjoint, or globally primalized operator.
4. CB-1 must state its raised/lowered Clifford and coframe convention for the
   displayed contraction formula, or present `-2/5,+3/5` as the chosen
   normalization of the invariant `-2:3` ratio. The executable hard-codes the
   two coefficients and proves their consequences; it does not independently
   implement `i_{e_a} phi_4` to derive that convention-sensitive display.

> [!NOTE]
> **Root integration disposition.** All four repairs were completed after this
> review: CB-2 now records the zero-order parity/rank pass, the `384` count is
> typed as the reduced internal D5 sector, the inherited density-dual bridge is
> explicit, and the CB-1 probe now constructs the metric-raised contraction
> word and derives `-2/5,+3/5` from `0/1 - 4/10`. The verdict survives.

No action construction is requested by these repairs. The next swing should
be the typed observation of the off-diagonal zero-order port, with fixed
trace-`H_q` retained as an adverse reality horn. H54 fallback and a switch
below SN-1 are premature because H210 has not been killed.

## Conditional-build contract enforced by this review

The horn is `H210`: assume a compatible nonzero `210` Pati--Salam channel and
no `54`. This review does not derive why the horn turns on. Constructing or
varying a source action, solving for a vacuum/background/selector, importing
an external datum, and fitting a family covector are off limits. The target is
only downstream geometric compatibility.

The controlling source rows are the packet's
`SC-GEN-57/51/53/59/02/04/56/50/52` and
`SC-CHI-50/54/51/53/03`. They require the following interpretation:

- the proposed `2+1` split is representation-theoretic, can look the same at
  low energy, and is supposed to reunify differently at higher energy;
- the fundamental carrier remains non-chiral and four-cornered while chirality
  is effective/emergent;
- both conjugate effective packages remain present; and
- an algebraic family kernel is not a named family, mass, energy threshold,
  observed chirality, or prediction.

## Evidence replayed

- `PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_cb1_h210_k77_rs_intertwiner_probe.py` — all reported checks pass, including the `-2/5,+3/5` trace, all `21` Pati--Salam generators, rank `64/16`, D-odd control, shifted grades, fixed-`H_q` phase split, and three planted false routes.
- `PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_cb2_h210_equation916_composition_probe.py --selftest` — `22/22` pass and `4/4` planted false controls are rejected.
- Both probes also pass under `python3 -O`; their outcomes do not rely on
  ordinary assertion execution.
- `git diff --check` is clean at review time.
- The mandatory H210 packet, equation-9.16 source extraction, HE-2/HE-3/HE-4
  owner chain, CR-B carrier correction, generic K77 four-field port, trace-
  `H_q` compatibility receipt, and observation-to-family campaign were
  inspected as dependencies.

## Fourteen-lens attack

### 1. Source fidelity and equation-9.16 identity grade

The displayed matrix identity is preserved exactly:

```text
rows:    (bar-zeta-minus, bar-zeta-plus, bar-nu-minus, bar-nu-plus)
columns: (zeta-plus, zeta-minus, nu-plus, nu-minus).
```

CB-2 does not relabel the source glyphs and correctly limits its placement to
the zero-order `varpi` summands. It also preserves the source facts that the
four barred/unbarred fields are independent and that equation 9.16 is a
candidate (“operators like”), not a selected unique operator.

The row reversal alone is not a global primalizer. The source extraction
types a density-dual operator, pairing, Hodge data, reality map, and formal
adjoint as separate constructions. CB-2's positional `ROW_OUTPUT` permutation
is consistent with the banked opposite-half pairing and with the D7 duality
used in prior K77 work, but it must remain named as that bridge. It cannot be
silently upgraded to `bar-zeta = zeta*` or a common-domain adjoint.

### 2. Exact Clifford, signature, and gamma trace

The probe rebuilds all fourteen signed-permutation generators, checks their
squares, and uses the source-aligned split

```text
external (1,3), internal A_6(positive) + B_4(negative).
```

For component coefficients `a` on `A_6` and `b` on `B_4`, the exact
signature-aware gamma trace is proportional to `6a+4b`. Gamma tracelessness
therefore fixes `a:b=-2:3`; CB-1 chooses `a=-2/5`, `b=+3/5`. The replay returns
zero, while the planted `3/10` projector returns nonzero. This ratio survives.

At review time the code began with the reduced coefficients rather than an
explicit contraction. The root repair now computes
`c(i_{(theta^a)^sharp}phi_4)` under declared upper/lower-index conventions and
derives the displayed coefficients. This closes the presentational weakness
without changing the invariant ratio.

### 3. D5/Pati--Salam representation and duality

`phi_4=vol(B_4)` and `phi_6=vol(A_6)` commute with all `15+6=21`
`spin(6)+spin(4)` generators. The component tensor passes the full tested
Pati--Salam infinitesimal action, including the free covector index. This is
stronger than citing multiplicity one alone.

The complex D5 bridge remains essential. The invariant bilinear lives in
`16 x 144`, while its operator form is

```text
16 -> 144* = 144bar.
```

This dualization explains why the bilinear partners are cross-effective-half
although the raw unbarred arrow lies inside one effective package. The
same-effective-half products `16 x 144bar` and `16bar x 144` have zero PS
invariants and remain binding adverse controls.

### 4. Real Cl(7,7), complexification, and fixed trace-Hq

The representation map exists on the real K77 signed-permutation carrier
before choosing `H_q`. Conventional `(4,2,1)` and `(4bar,1,2)` names enter only
after complexification; this distinction is correctly retained.

For the repository's fixed trace axis `q=10`, the owner blades are directly
`H_q`-skew, but the full RS port is not admitted with one common phase:

```text
A_6 grade-5 words: 6 real phases
B_4 grade-3 words: 1 real phase + 3 i phases.
```

Componentwise phase completion produces separate trace contributions
`(-9/5,+9/5)` rather than zero and singles out `q`, so it does not preserve the
same full-PS gamma-traceless tensor. The simultaneous fixed-`H_q` port remains
`TYPE_MISSING`. This is an adverse horn, not a source-level no-go, because the
trace-owned `H_q` is repository-constructed and the source's defining
Hermitian form is unspecified.

### 5. Rarita--Schwinger grade and owner typing

The strongest correction in CB-1 survives:

| object | `phi_4` presentation | Hodge-dual `phi_6` presentation |
|---|---:|---:|
| `210` owner | grade 4 | grade 6 |
| gamma-traceless RS port | grades 3 and 5 | grades 5 and 7 |

The admitted grade-six blade is an owner representative, not the complete
`Omega^0 -> Omega^1_RS` coefficient tensor. The earlier fixed-one-form generic
port is therefore adjacent prior art, not an H210 construction. This kills the
pure-grade-six-port reading without killing H210.

### 6. D parity and the two PS family blocks

The chosen even normalizer representative preserves ambient K77 chirality,
reverses `vol(B_4)`, and exchanges the two balanced `phi_4` eigenspaces. This
supports the scoped statement that the H210 singlet line is D-odd and acts
with opposite orientation sign on the two complex PS family blocks, up to an
orientation convention.

The probe does not build named PS weight projectors or independently normalize
the two restricted matrix elements. Its direct exact result is the normalizer
parity plus balanced eigenspaces; “opposite named-family Clebsches” should not
be read as a measured or basis-selected family assignment.

### 7. Four corners, effective halves, and emergent chirality

CB-2 retains both effective packages:

```text
A = 3 x 16    + 144bar
B = 3 x 16bar + 144.
```

The forward arrows and their conjugates are both recorded. No ambient half is
deleted, and no net-chirality theorem is substituted for Weinstein's stated
fundamental-non-chiral/effective-chiral mechanism.

The word “half” remains bookkeeping until a physical selector or quotient is
supplied. The conditional zero/nonzero H210 comparison is not a derivation of
the luminous/high-curvature regimes or of an energy threshold.

### 8. Operator arrow versus barred bilinear and cell indexing

Under the inherited opposite-half density-dual bridge, the exact forward
placements are

```text
(1,2): nu+ -> zeta-     zero-order varpi_-+
(0,3): nu- -> zeta+     zero-order varpi_+-.
```

The reverse-shaped displayed partners are `(2,1)` and `(3,0)`. The tempting
bare pp/mm cells `(0,2)` and `(1,3)` correspond to the wrong PS products and
are correctly rejected.

CB-1's odd grade support now supplies the missing ambient-half exchange for
the zero-order arrows. This is the key successful composition. It does not
repair the `d0` terms occupying the same cells: the source's ambient-half
labels and the repository's derivative parity remain in the known Layer-0
collision.

### 9. Rank, kernel, and forward/reverse block counting

Every nonzero Clifford component in CB-1 is invertible, so the direct-sum RS
map is injective. It has rank `64` on one real ambient K77 Weyl half and, after
factoring the external multiplicity in the complex PS comparison, rank `16`
per internal Weyl copy.

For a declared nonzero family covector `r:F -> C`, the internal map
`r tensor T` consequently has rank `16` and kernel dimension `32=2*16`.
Disjoint block addition then gives the conditional packet:

| counted assembly | rank | kernel on counted domain |
|---|---:|---:|
| one forward family arrow | 16 | 32 |
| conjugate forward pair | 32 | 64 on the 96 family inputs |
| four declared nonzero forward/reverse blocks | 64 | 320 on the 384 counted sector |

The arithmetic is exact. The typing must be repaired: `384` is not the
complete source field carrier and `320` is not a physical kernel. External
spin multiplicity, the observer complement, density/domain data, and any
physical quotient are outside this finite internal-sector count.

### 10. Observation and quotient ceiling

Literal observation is pullback/contraction. It can preserve mixed and
vertical contributions, but it is not automatically injective. Prior
observation work also retains mirror families and a `1536`-dimensional
additional block and supplies no physical quotient making that complement
acyclic.

Therefore only

```text
rank(after typed pullback) <= rank(before pullback)
```

is presently licensed. The nonzero survival of either off-diagonal H210
block, equality of rank, quotient rank, luminous selection, and observed
family spectrum all remain `TYPE_MISSING`.

### 11. Conditional-build scope and action-off-limits audit

Both artifacts explicitly assume H210 and refuse to derive a source action,
stationary background, external selector, or fitted family row. They do not
smuggle in an ordinary Higgs scalar or family index. This lens passes.

The fixed-`H_q` obstruction must not be “repaired” by choosing a new Hermitian
form inside this lane. Likewise, observation may use the already-declared
conditional port but may not solve for what turns it on.

### 12. Prior art and novelty

Already banked: the four-field matrix, the generic K77 zero-form port, full
Clifford phase admission, the four complex D5 pairing ladders, the distinct
`54/210` owner split, the effective-half census, and the observation ceiling.

New here is their exact composition:

1. the H210 PS volume line is turned into the full gamma-traceless RS tensor
   on current K77 rather than a fixed one-form times a grade-six blade;
2. its relative `-2:3` coefficient, injectivity, D parity, shifted grades, and
   fixed-`H_q` fork are computed; and
3. the operator/bilinear duality locates the zero-order H210 summand in the
   off-diagonal equation-9.16 cells while excluding pp/mm.

That is genuine bounded novelty. It is not source-action selection or a
physical `2+1` derivation.

### 13. Reproducibility and mutation controls

The probes are deterministic and float-free. CB-1 uses exact fractions and
signed permutations; CB-2 is a finite immutable cell/rank ledger. The planted
wrong `3/10` projector, fixed one-form route, pure-grade-six route, pp/mm cell
set, kernel `31`, rank `32`, and same-half misclassification all fail as
intended. Optimized-mode replay also passes.

Two harness limitations should remain visible:

- CB-1's fixed-one-form plant checks a known nonzero vector-generator action
  rather than running a separate mutated full tensor through every
  equivariance equation; and
- The pre-repair CB-1 coefficient check started from asserted fractions. The
  integrated probe now executes the raised-contraction convention directly.

The remaining fixed-one-form mutation limitation does not overturn the
independent gamma-trace/equivariance/rank results.

### 14. Claim-ceiling editor

The strongest warranted statement is conditional and pre-observation:

> Assuming H210 and the inherited opposite-half density-dual bridge, the
> exact current-K77 H210 RS tensor is nonzero, injective, PS equivariant,
> gamma traceless, and odd in ambient Clifford grade. Its zero-order operator
> summands fit the released off-diagonal `varpi_-+/varpi_+-` cells and one
> nonzero family covector leaves a two-dimensional family multiplicity kernel
> in the counted internal sector.

This does not derive a family covector, identify a third family, prove a mass
kernel, establish the full equation-9.16 operator, select a background, remove
mirrors, survive observation/quotient, or predict a scale, threshold, or
observable.

## What survives and what is killed

Survives:

- the real K77 H210 representation port and its complex PS interpretation;
- exact `-2:3` relative coefficients and gamma-trace cancellation;
- infinitesimal PS equivariance under all 21 tested generators;
- rank `64` on a real ambient half and rank `16` per internal complex copy;
- D-odd chosen-normalizer behavior and opposite orientation sign;
- shifted grades `3/5` or `5/7`;
- the fixed trace-`H_q` common-phase obstruction as a typed adverse fork;
- off-diagonal zero-order equation-9.16 placement under the density-dual horn;
- pp/mm as zero-channel controls; and
- the conditional two-dimensional family multiplicity kernel before
  observation.

Killed:

- a fixed internal one-form times a pure grade-six blade as the invariant RS
  port;
- owner-grade admission as proof of complete connection-port admission;
- one common fixed-`H_q` phase for the full PS-equivariant RS tensor;
- “cross-half bilinear” interpreted as a raw unbarred cross-half arrow;
- placement in bare `varpi_++/varpi_--` cells;
- use of the southeast-zero block for this displayed 2021 horn; and
- calling the `384`-dimensional counted packet the complete GU fermion
  carrier.

Still `TYPE_MISSING`:

- a source-supplied Hermitian/reality/primalizer choice;
- the complete `d0+varpi` cell with reconciled derivative parity;
- a common formal-adjoint domain and nonzero reverse completion;
- a source-selected coefficient or family covector;
- fixed-`H_q` full-PS compatibility, unless that adverse horn is deliberately
  weakened to the stabilizer of `q`;
- typed observation/pullback rank and physical quotient;
- luminous/dark selection, family identity, mass, scale, threshold,
  observable, or prediction.

## Required repairs before integration

### Repair 1 — CB-2 stale composition gate (`HIGH`, result routing)

In CB-2 §6 item 10, §7's first `TYPE_MISSING` bullet, and §8's “next gate”
paragraph, replace the statement that CB-1 parity/rank admission remains open.
Use:

> CB-1 now supplies an odd-grade, injective rank-16 internal H210
> intertwiner, so the zero-order `varpi_-+/varpi_+-` half-parity composition
> passes. The full `d0+varpi` cells remain `TYPE_MISSING` because the released
> derivative placement retains the Layer-0 half-label collision.

Also change CB-1's own reproduction/summary label that says equation-9.16 is
“the next conditional gate” when the two files are integrated together; that
label was accurate only before CB-2 composition.

### Repair 2 — rank-sector boundary (`HIGH`, claim ceiling)

In CB-2 §4, replace every unqualified “full `384` carrier” with:

> full counted internal `16/144` D5 sector (`384` complex dimensions after
> suppressing external multiplicities)

Add after the rank table:

> The `64/320` pair is rank/nullity on this reduced family/imposter sector.
> It excludes external spin multiplicity and the known observer complement;
> it is not the rank/nullity of the complete GU fermion field space.

### Repair 3 — density-dual horn label (`MEDIUM`, source typing)

In CB-2's outcome and §2, prefix the `ROW_OUTPUT` interpretation with:

> Under the inherited opposite-half density-dual bridge used by the K77
> equation-9.16 construction,

Keep the existing sentence that barred fields are independent. Add that this
bridge does not supply a field reality condition, global primalizer, or formal
adjoint. No cell coordinates need to change.

### Repair 4 — contraction convention (`MEDIUM`, mathematical exposition)

In CB-1 immediately before the displayed formula, define whether `Gamma_a`
denotes Clifford multiplication by a frame vector or coframe covector, how
indices are raised in signature `(6,4)`, and how `c(i_{e_a}phi_4)` is encoded.
Alternatively replace “canonical projection reduces exactly to” with:

> Fix the overall normalization so that the two PS-equivariant component
> coefficients are `-2/5` on `A_6` and `+3/5` on `B_4`. Their ratio `-2:3`
> is forced by `6a+4b=0`; the following executable checks this normalized
> tensor exactly.

The latter matches what the probe directly proves.

### Editorial repair (`LOW`)

CB-2 §8 repeats the line “reprioritized rather than repaired by relabeling the
source or inventing an”. Delete the duplicate fragment.

## Path-relative reprioritization

1. **Next: typed observation of the off-diagonal zero-order port.** Carry only
   the H210 `varpi_-+/varpi_+-` summands through the already-declared literal
   pullback/contraction map. Compute image/intersection/kernel on the counted
   family/imposter sector and state what the known complement prevents. Do
   not derive the coefficient or selector.
2. **Adverse branch inside that swing: fixed trace-Hq.** Keep two subhorns
   explicit: real K77/complex PS port before `H_q`, and the fixed trace-`H_q`
   unitary requirement. The latter currently fails full-PS common-phase
   admission; test only whether observation weakens or preserves that
   obstruction. Do not choose a new Hermitian form.
3. **Do not run H54 yet.** H54 is the fallback only if the H210 observation
   port is zero or ill-typed. Its direct weld remains absent and its quadratic
   route remains less fertile.
4. **Do not switch below SN-1 yet.** H210 passed the exact K77 and zero-order
   cell gates. Reprioritize below SN-1 only if typed observation annihilates
   every allowed block or makes the required real/operator type impossible.

The path remains fertile for one bounded observation swing. Its conviction is
high for the exact pre-observation algebra, moderate for source-operator
placement because of the density/derivative fork, and low for any physical
promotion.

## Final claim ceiling

After the required repairs, CB-1 and CB-2 establish a coherent conditional
H210 zero-order geometry. They do not establish Weinstein's proposed physical
`2+1` spectrum. No canon, source-claim, ledger verdict, particle content,
generation count, chirality theorem, action, background, family identity,
mass, scale, threshold, observable, or public posture moves.
