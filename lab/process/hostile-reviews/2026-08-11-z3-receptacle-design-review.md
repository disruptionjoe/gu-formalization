# Hostile review: z3 receptacle design packet

Target: `explorations/z3-receptacle-design-packet-2026-08-11.md`
Run: `RUN-20260811-234500-gu-five-frontier-design-packets` (same-run self-review by the design pass; the executing wave reviews independently).

## Self-hostile review (the three standing charges)

**Charge 1 — where does the summary outrun the artifact?** Two places,
now repaired in the text: (i) an earlier draft of lens 1 stated
`Omega^String_13 = Z/3` as fact; it is memory-grade and is now flagged —
the in-repo record supports only `pi_13(tmf) = Z/3` at fetched grade.
(ii) "the Y^14 end has one naturally" (M-M6's corner remark) is a register
sentence, not a construction; the packet types it as candidate-R1 only.
Residual risk accepted and stated: the D4 design assumes `[M^13, SO]`
computations on the model are tractable at bounded cost; if not, D4
splits, and the packet's "bounded" framing was optimistic — the wave should
re-scope rather than push. No other summary sentence exceeds its citation.

**Charge 2 — where is rigor defending a superseded or mistyped object?**
Checked three known hazards: (i) the anchor fact's "framed/String or
replace the sole-bridge premise" is STALE relative to `cb-c` F3
(2026-08-05) — the packet states the both-columns death and does not defend
the anchor's weaker form; (ii) the dim-3 surrogate's positive class is
never used as a dim-13 fact (control-only in D4); (iii) `RP^3 x S^6` is
used strictly as MODEL, with D1-D3 exactly the model-to-true-object
promotion gates, per the M-M28 fence. One deliberate scope choice a critic
could attack: the packet treats the equivariant channel (B3) as
out-of-scope for the dim-13 program rather than as a fifth decisive step —
grounds: `exhaustiveness-by-type` types it as the already-named decider on
K3-side geometry, not a receptacle route; if a reviewer shows an
equivariant structure ON `L^13` itself, that is new work this packet did
not design. NEEDS-RECHECK by the wave: whether `Omega^Spin_13 = 0`'s ABP
citation in Wave C covers the twisted/equivariant refinements lens 1
bifurcates (the packet believes it does not, which is why the equivariant
row stays open).

**Charge 3 — what else must change if this stands?** Dispositions:
- *Dissolved:* none — this packet is design input and dissolves nothing.
  (Empty list stated explicitly.)
- *Survives:* the located-not-forced posture (survives every outcome by
  construction); the M-C1 chain retirement; the multiplicity fence; the
  spin-wall double death; the single-decider/B3 posture; P3's reinstated
  external status.
- *Needs-recheck (by the executing wave, on its own authority):* the M-H7
  register row's next-step field if D1-D4 are adopted (the row predates
  this packet's step decomposition); the M-M6 Tier-3 hold if D5 returns
  TYPE-COMPLETE or the tmf detection fact confirms (a second detector
  would re-rank the instrument program); the AC-F3 conditional-ledger row
  if D6 ever produces a positive-surplus dictionary (that is horn H2's
  canon-level supersession, two-phase rule applies); the O9 gate-rewording
  edit (still owed per `mh7` Section 6, untouched here).
