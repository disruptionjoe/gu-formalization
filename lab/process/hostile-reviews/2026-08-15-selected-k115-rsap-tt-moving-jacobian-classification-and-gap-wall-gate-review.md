---
title: "Hostile review: K115 TT moving-Jacobian classification and gap-wall gate"
status: complete
created: "2026-08-15"
target: explorations/conditional-build/selected-k115-rsap-tt-moving-jacobian-classification-and-gap-wall-gate-2026-08-15.md
verdict: PASS_WITH_SERIALIZED_OWNER_STATIONARY_BACKGROUND_GLOBAL_PATCHING_ATTACHMENT_AND_QUANTUM_DOMAIN_CEILINGS
---

# Hostile review

> **GU-COMPARATOR-ROUTING — scope before inference.** This review concerns a
> source-native Krein/Green moving-frame owner problem. Standard Higgs,
> family-index and chirality constructions do not adjudicate it. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.

Classification: `SOURCE_NATIVE_ROUTE`.

## Sign and convention charge

The artifact does not identify a field Jacobian and parallel transport with
the same sign. It states the moving-frame equation `F^-1 dF=A_C` and obtains
`F=F0 exp(Delta phi G)`. It separately solves `dT+A_C T=0` and obtains
`T=exp(-Delta phi G)`. They are inverse moving factors.

## Completeness charge

This is a classification, not one ansatz. If two invertible frames have the
same Maurer--Cartan form, their quotient is constant. Because `G` is fixed and
the base is one scalar interval, no path ordering or extra functional modulus
is hidden.

## Gap-wall charge

The conclusion is deliberately limited to a bounded invertible extension in
the same frame through a simple wall. Divergent reciprocal stretch does not
exclude patched bundles, singular variables, degenerate operators, or a new
action that changes the spectral family.

## Ownership charge

An explicit reconstructed frame is not called source owned. The audit records
that `alpha_II` enters the unified packet as a reconstructed coefficient and
is charged as `U7`; the absorbed source supplies no typed two-field TT map.
The result remains current-custody scoped and is reopened by exact new source
or action evidence.

## Physical-carrier charge

A local `2D` frame trivialization neither supplies a stationary moving
background nor attaches the TT carrier to the conditional `98D` phase/BFV
complex. It gives no positive closed domain or physical cohomology.

## Verdict

Pass at exact local Jacobian-classification and componentwise gap-wall grade.
Serialized source/action ownership, global patching, stationary background,
non-invariant/nonlinear/boundary/cohomological attachment, positive domain and
physical BFV cohomology remain open.
