---
artifact_type: hostile_review
status: review
doc_type: joe_directed_tw3_coherence_gluing_hostile_review
created: 2026-08-16
work_item: RUN-20260817-TW3-HOSTILE-REVIEW
channel: joe_directed_superposition_twistor_conditional_build
title: "Hostile review of TW3-A/B/C: the Krein/conjugate dual gluing survives, the raw algebraic dual has a different corner map, and the rolled descent must carry that distinction"
grade: "INDEPENDENT TWENTY-ONE-LENS HOSTILE REVIEW with exact replay of TW3-A (150/150), TW3-B (55/55), and TW3-C (102/102). The tensor-factor, current-K77 pairing, corner-classifier, projector, rolled-compression, and stabilizer calculations survive. Required repairs are limited to the Hermitian sign convention, explicit TW3-B custody in TW3-C, raw-dual versus Riesz-identified target typing, and relative rather than unqualified canonical-descent wording. No action, background, global reduction, Tw2 field, analytic domain, quotient, physical cohomology, state, family selector, or superposition theorem."
disposition: PASS_AFTER_REQUIRED_DUALITY_CUSTODY_REPAIRS__TW3A_FORMAL_TENSOR_COMMUTATION_BANKED__TW3B_EXACT_BILINEAR_VERSUS_KREIN_DUAL_CORNER_CLASSIFIER_BANKED__TW3C_FORMAL_ROLLED_DESCENT_BANKED_AFTER_TARGET_RETYPE__NO_FURTHER_FINITE_OWNED_GATE__TWISTOR_PATH_DEPRIORITIZED
canon_verdict_change: none
steering_effect: "Apply the bounded TW3-B/TW3-C target-convention and Hermitian-sign repairs, register the three new execution artifacts, then bank TW3. Do not open a TW4 from currently owned fibre data: the remaining reopeners are independently supplied global normal U(3,2) reduction data or a genuinely non-product normal/mixed operator."
canonical_effect: pending_integration
mandatory_lenses: [source_criticism_and_superposition_custody, two_plus_one_imposter_partner, emergent_chirality_four_corners, current_k77_clifford_real_form, layer0_object_identity, tensor_factor_naturality, strict_algebraic_dual_vs_krein_overline, bilinear_form, hermitian_krein_form, contragredient_action, central_sign_vs_orientation_component, corner_permutation, pi4_vs_pi14, rolled_compression_and_order, u32_stabilizer_descent, topology_and_global_section_ceiling, null_symbol_vs_physical_cohomology, prior_art_and_novelty, falsification_and_probe_adequacy, routing_and_audit, reprioritization]
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/conditional-build-channel-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-source-owner-intersection-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-two-ps-channels-have-distinct-upstairs-owners-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw1-normal-twistor-spin-lift-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw2-four-dimensional-detour-symbol-factorization-2026-08-16.md
  - lab/process/hostile-reviews/2026-08-16-joe-directed-twistor-conditional-composition-review.md
  - lab/active-research/joe-directed/superposition-twistor/tw3a-normal-spin-detour-intertwining-2026-08-16.md
  - tests/channel-swings/joe_directed_tw3a_normal_spin_detour_intertwining_probe.py
  - lab/active-research/joe-directed/superposition-twistor/tw3b-dual-pairing-orientation-corner-gluing-2026-08-16.md
  - tests/channel-swings/joe_directed_tw3b_dual_pairing_orientation_gluing_probe.py
  - lab/active-research/joe-directed/superposition-twistor/tw3c-rolled-compression-stabilizer-descent-2026-08-16.md
  - tests/channel-swings/joe_directed_tw3c_rolled_compression_stabilizer_descent_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This review crosses a
> standard conformal twistor-spinor detour construction and a conditional
> current-K77 pairing/compression bridge. Results about a standard Higgs/VEV,
> ordinary family index or net chirality, SO(10) `126` Majorana mechanism,
> anomaly selector, VEV-only breaking or familiar vector-mass route bind only
> those named comparators. They are not evidence for or against Weinstein's
> source-native `2+1` or emergent-chirality mechanism without an explicit
> typed bridge. Read `lab/methods/source-native-comparator-routing.md` before
> reuse.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# Hostile review: TW3 coherence and gluing

## Verdict first

The TW3 wave survives after four bounded custody repairs. Its strongest new
result is not a new base-normal interaction. It is an exact distinction
between two dual constructions that prior prose had allowed to blur.

TW3-A proves only the formal tensor theorem

```text
(A tensor I)(I tensor S)=(I tensor S)(A tensor I)
```

for `A` in the TW2 symbol blocks and for the TW1 selected, central-sign, and
opposite-orientation lifts, after the TW2 target has been represented as a
primal matrix copy. The identity map from a fixed normal half to its raw dual
is not equivariant. That is not a failure of the detour map: a raw dual must
carry the contragredient action, and any primal-looking action requires a
declared Riesz identification.

TW3-B supplies the missing exact current-K77 pairing data and shows that two
honest Riesz conventions have different corner incidence:

```text
strict complex-bilinear B14 dual:  ++ <-> +-,   -- <-> -+;
Krein/conjugate TW2 overline:      ++ <-> -+,   +- <-> --.
```

The first keeps the base-Weyl label and exchanges the normal half. The second
flips the base-Weyl label and keeps the normal half. Only the second matches
TW2's displayed chiral `overline(Tw)` sequence. Neither convention deletes a
corner; both exchange the ambient K77 half. `Tw*` may therefore abbreviate
the TW2 target only after the conjugate/Krein convention is stated. A strict
algebraic star is a different target, not a failed version of the same one.

TW3-C proves exact base-supported `Pi4`, gamma-trace, rolled-`R4`, and lifted
`U(3,2)` covariance, but its present text and probe fall back to TW2's generic
matrix-copy wording and do not import TW3-B. The same-carrier commutator tests
the Riesz-identified Krein/conjugate target, not the raw strict `B14` dual.
TW3-C must say so and carry both corner maps as a non-collapse control.

The associated-bundle result is valid with one qualification. Relative to a
supplied component-tagged lifted `U(3,2)` reduction and TW1's exponential-path
representative, `S_J` gives an equivariant associated endomorphism. Changing a
transition-function lift by the central sign does not make that endomorphism
projective. Replacing the endomorphism itself by `-S_J`, however, gives the
distinct negative endomorphism; replacing the orientation component gives
`S_J^-1`. Thus “canonical once supplied” is licensed only relative to those
declared component and representative data.

Final disposition:

```text
PASS_AFTER_REQUIRED_DUALITY_CUSTODY_REPAIRS
__BANK_TW3A_B_C
__NO_FURTHER_FINITE_ALREADY_OWNED_GATE
__DEPRIORITIZE_TWISTOR_PATH
```

## Twenty-one hostile lenses

### Lens 1 — source and superposition custody

The source is silent on the TW1 lift, TW2 cubic symbol, both TW3 dual
conventions, and the associated descent. Every result is repository-derived
conditional kinematics. The superposition hypothesis remains a reverse-build
horn, not a source fact or quantum-superposition theorem. No action,
background, datum, selector, or physical state is smuggled in. **Survives.**

### Lens 2 — `2+1`, imposter, and partner custody

No phase eigenspace, Krein-sign subspace, corner, null-symbol class, or
stabilizer representation is identified with the source imposter `F`, the
abstract multiplicity space `M_3`, or the distinct internal partner `144`.
No family row or ordinary family index occurs. **Survives.**

### Lens 3 — emergent chirality and four corners

Both ambient K77 halves and all four corners remain. The two dual conventions
permute those corners differently, but neither selects a luminous half or
constructs observation/decoupling. A corner permutation is not emergent
chirality. The parent remains fundamentally non-chiral. **Survives.**

### Lens 4 — current K77 and Clifford real form

The internal pairing certificate is built in exact real `Cl(6,4)` and the
parent selection rule in exact real `Cl(7,7)`. TW2's rational `Cl(2,2)` is a
four-dimensional symbol model, joined to K77 only through the typed tensor
and base-compression bridge. No K95/`Cl(9,5)` form is transferred. **Survives.**

### Lens 5 — Layer-0 object identity

`J_N`, `J10`, `S_J`, `-S_J`, `S_J^-1`, `B10/B14`, `G10`, the raw linear
dual, the conjugate dual, a Riesz-identified matrix copy, `Pi4`, `Pi14`,
first-order `R4`, cubic `N3`, associated descent, and a global section are
distinct objects. The wave's only substantive defect is that TW3-C does not
yet preserve all of these distinctions in its own custody. **Requires repair.**

### Lens 6 — tensor-factor naturality

TW3-A's primalized square is exact but tautological: the base map and normal
lift act on separate factors. Its exact rank and null-cohomology multiplication
adds bookkeeping, not a coupling. More covectors, phases, or corners cannot
make this seam fertile. **Survives and banks at low fertility.**

### Lens 7 — strict algebraic dual versus Krein/conjugate overline

TW3-B's distinction is load-bearing and correct. The complex-bilinear `B14`
Riesz map keeps base chirality and exchanges normal chirality. The TW2
`overline(Tw)` line already flips base chirality; the normal sesquilinear Riesz
map keeps the normal label after conjugation and bilinear exchange cancel.
Consequently the two corner maps differ exactly as reported. Strict `Tw*` and
`overline(Tw)` must not be used as untyped synonyms. **Survives exactly.**

### Lens 8 — bilinear forms

The normal certificate proves `C10^T=-C10`, `C10^2=-1`, and invariance under
all 45 even Spin generators. It also proves that the complex-bilinear form
vanishes within each `J10` half and pairs the two halves nondegenerately. The
ambient `C14` selection identities independently keep base chirality, exchange
normal chirality, and exchange ambient chirality. These are exact algebraic
forms, not positive metrics. **Survives.**

### Lens 9 — Hermitian and Krein forms

`G10=C10 J10` is real symmetric, involutive, and neutral. There is one sign
imprecision in TW3-B: the restriction induced literally by `G10` is

```text
h_delta(x,y)=delta i conjugate(x)^T C10 y
             =conjugate(x)^T G10 y,
J10 y=delta i y.
```

The probe instead uses `+i` on both halves. On the minus half this differs by
an overall negative scalar, so nondegeneracy, `(8,8)` inertia, corner typing,
and contragredient covariance are unchanged. The formula and probe should
nevertheless agree. **Requires a sign-convention repair; theorem survives.**

### Lens 10 — contragredient action and Riesz covariance

The raw dual action is `rho(g)^(-T)`. TW3-B's rational noncompact Spin control
is important because the selected lift happens to commute with `C10` and
could otherwise hide a primal/dual error. The exact identity

```text
C10^(-1) rho(g)^(-T) C10=rho(g)
```

holds, while replacing `rho(g)^(-T)` by the primal raw-dual action fails.
TW3-A's identity-primalizer failure is therefore correctly retyped as a need
for a Riesz owner, not as non-equivariance of the true dual construction.
**Survives.**

### Lens 11 — central sign versus orientation component

`S_J` and `-S_J` have the same square and preserve the same forms, but are
distinct endomorphisms with phase exponents shifted by four. `J_N -> -J_N`
crosses the odd-complex-dimensional orientation fence and uses `S_J^-1`,
whose square has the opposite fixed-volume sign. A sign on an overlap
representative is yet another occurrence: it cancels in conjugation. None of
these three uses of sign may be merged. **Survives; TW3-C wording must remain
relative to the selected exponential-path representative.**

### Lens 12 — corner permutation

The two exact permutations are bijections on the same four-corner inventory.
TW3-C's sentence that no corner is “exchanged” is too broad: the normal Spin
action itself does not permute corners, but the Krein/conjugate `R4` target
flips the base label and exchanges ambient halves. The strict `B14` target
instead flips the normal label. **Requires wording repair.**

### Lens 13 — `Pi4` versus `Pi14`

The probe preserves the asymmetric exact facts:

```text
Pi4^2=Pi4,
Pi14,base^2 != Pi14,base,
Pi4 != Pi14,base,
Pi14,base Pi4=Pi4 Pi14,base=Pi4.
```

Only the already `Pi4`-traceless target embeds with zero normal one-form legs.
No projector or source-carrier equality follows. **Survives.**

### Lens 14 — rolled compression and operator order

`R4=[[Q,T],[-T#,0]]` is a first-order compressed rolled symbol. `N3` is a
third-order cubic middle symbol. The wave never equates them. TW3-C's
commutator uses the `R4` matrix copy; it does not establish a raw-dual
operator, curved detour, lower-order completion, or total normal/mixed
complex. **Survives after target-convention repair.**

### Lens 15 — lifted `U(3,2)` stabilizer descent

The vector and Spin commutator maps have the same coefficient kernel of
dimension 25 and orbit rank 20. Lie-algebra equality plus connected
`U(3,2)` and the central overlap sign supports the associated endomorphism on
the declared lifted reduction. This is stronger than a projective class, but
only relative to the supplied component and TW1 exponential-path lift.
**Survives with qualified canonicality.**

### Lens 16 — topology and global-section ceiling

Fibre equivariance does not construct a global normal complex structure,
principal `U(3,2)` reduction, Spin lift of that reduction, transition cocycle,
or global endomorphism on the actual GU geometry. TW3-C records these as
missing. Its theorem is “if that lifted reduction is supplied, the formula
descends,” not “the reduction exists.” **Survives.**

### Lens 17 — null symbol versus physical cohomology

TW3-A correctly multiplies the null middle-symbol dimensions by internal
eigenspace multiplicities. They remain characteristic symbol classes. No
global solution, BV/BFV cohomology, positive state space, particle count,
family count, or physical superposition sector follows. **Survives.**

### Lens 18 — prior art and novelty

TW1 already owned the component-tagged lift and spectra. TW2 already owned
the cubic identities, rank table, `Pi4/Pi14` adapter, and compressed `R4`.
The prior hostile review predicted formal tensor commutation and named dual
typing as the only potentially nontrivial seam. TW3-A and most of TW3-C are
therefore closure/replay. TW3-B's exact bilinear-versus-Krein corner classifier
is the genuine bounded addition. **Novelty claim is properly narrow.**

### Lens 19 — falsification and probe adequacy

All three selftests pass and reject their declared mutants. TW3-B's use of a
generic rational boost is a sound adverse control. TW3-C's commutator checks
are intentionally structural identities and therefore cannot adjudicate raw
duality; its probe must import TW3-B's two corner maps and reject their
collapse before the terminal gluing claim is complete. No further numerical
strata are needed. **Requires one custody assertion, not another research
wave.**

### Lens 20 — routing and audit

Before integration, the routing audit sees the historical nine gaps plus the
three unregistered TW3 execution artifacts. Register TW3-A, TW3-B, and TW3-C
as `BRIDGE_OR_SEMANTIC_BOUNDARY`; hostile reviews are outside that audit's
derived registry scope, so this review should not be inserted merely for
symmetry. No baseline gap needs repair in this wave. The hostile-lens audit
retains only the known unrelated independent-owner debts `LT-GR4` and
`AC-F3`. **Required integration repair; no scientific effect.**

### Lens 21 — reprioritization

Once the bounded repairs are applied, every finite already-owned seam is
closed: tensor commutation, genuine dual covariance, corner incidence,
projector embedding, rolled compression, component signs, and stabilizer
descent. Re-running these with more fixtures has negligible fertility. A
global reduction or genuinely non-product normal/mixed term would be new
owner data and is not an admissible TW4 construction in this channel.
**Bank and deprioritize.**

## Mandatory repairs

### Repair 1 — make TW3-B's Hermitian convention literal

In
`lab/active-research/joe-directed/superposition-twistor/tw3b-dual-pairing-orientation-corner-gluing-2026-08-16.md`,
replace

```text
h_delta(x,y)=i conjugate(x)^T C10 y
```

with

```text
h_delta(x,y)=delta i conjugate(x)^T C10 y
             =conjugate(x)^T G10 y,
where J10 y=delta i y.
```

Add that a common `+i` convention differs by an overall minus sign on
`S10-` and therefore leaves the neutral `(8,8)` inertia and Riesz target type
unchanged. In
`tests/channel-swings/joe_directed_tw3b_dual_pairing_orientation_gluing_probe.py`,
change the literal induced minus-half form to

```text
h_minus = -ii * x_minus.conjugate_transpose() * c10k * x_minus
```

and retain the same exact inertia and pseudo-unitarity checks.

### Repair 2 — carry TW3-B into TW3-C's declared dependencies and target

Add TW3-B and its probe to TW3-C's `depends_on`. Near TW3-C's first display
of `R4`, insert:

> The displayed self-carrier matrix is TW2's target after the
> Krein/conjugate Riesz identification established in TW3-B. On a raw strict
> complex-bilinear `B14` dual, the target action is contragredient and its
> corner map exchanges the normal half instead. The same-carrier commutator
> below tests the former convention only; it does not identify the two duals.

Replace “nothing about the cubic's algebraic dual target beyond TW-2's
declared fibre-pairing identification” with:

> For the TW2 `overline(Tw)` target, TW3-B identifies the displayed matrix
> copy with the Krein/conjugate Riesz target. The strict complex-bilinear
> `B14` dual is a distinct contragredient target with a different corner map;
> TW3-C neither identifies it with `overline(Tw)` nor promotes either Riesz
> map to an analytic adjoint.

### Repair 3 — correct TW3-C's corner and canonicality wording

Replace TW3-C's sentence “so no corner is selected, exchanged or removed”
with:

> The normal Spin action alone preserves the internal-half label and removes
> no corner. On the Krein/conjugate TW2 target, `R4` flips the base label,
> giving `++ <-> -+` and `+- <-> --` and exchanging ambient halves. On the
> distinct strict `B14` algebraic dual, the corner map is `++ <-> +-` and
> `-- <-> -+`. Both are four-corner bijections; this matrix-copy theorem uses
> the first.

Where TW3-C says the descended endomorphism is canonical, qualify it as:

> canonical relative to the supplied orientation component, lifted
> `U(3,2)` reduction, and TW1 exponential-path representative. Central signs
> on transition lifts do not make it projective; replacing the endomorphism
> itself by `-S_J` gives the distinct negative endomorphism.

This preserves the exact stabilizer result while preventing overlap-sign
independence from being mistaken for equality of `S_J` and `-S_J`.

### Repair 4 — make the terminal probe test dual-convention custody

In
`tests/channel-swings/joe_directed_tw3c_rolled_compression_stabilizer_descent_probe.py`,
load TW3-B as prior art and assert all of the following before declaring
terminal gluing:

```text
bilinear_dual = {"++":"+-", "+-":"++", "--":"-+", "-+":"--"}
krein_dual    = {"++":"-+", "-+":"++", "+-":"--", "--":"+-"}
bilinear_dual != krein_dual
tw2_matrix_target == krein_dual
```

Add a hostile mutant rejecting their collapse, and label every `R4`
same-carrier commutator as the Krein/conjugate Riesz-identified matrix copy.
This is a custody repair backed by TW3-B's exact probe, not a request to redo
the pairing or create a new operator.

### Repair 5 — restore routing coverage

Register these paths as `BRIDGE_OR_SEMANTIC_BOUNDARY` in
`lab/process/source-native-comparator-routing-registry.json`:

```text
lab/active-research/joe-directed/superposition-twistor/tw3a-normal-spin-detour-intertwining-2026-08-16.md
lab/active-research/joe-directed/superposition-twistor/tw3b-dual-pairing-orientation-corner-gluing-2026-08-16.md
lab/active-research/joe-directed/superposition-twistor/tw3c-rolled-compression-stabilizer-descent-2026-08-16.md
```

No repair changes a source claim, the TW1 square, TW2's cubic identities,
TW3-B's two corner maps, the `Pi4/Pi14` relations, the common `u(3,2)`
stabilizer, or any canon verdict.

## Independent executable replay

The review ran the declared environments from the GU repository root:

```text
TW3-A Sage exact probe plus selftest     150/150 PASS
TW3-B Sage exact probe plus selftest       55/55 PASS
TW3-C Python exact probe plus selftest    102/102 PASS
```

The outputs independently reproduce:

- both TW1 component square signs and central-sign separation;
- exact TW2 `T,Q,N3,T#` identities, ranks, and null fingerprints;
- formal tensor commutation on every declared corner and covector fixture;
- exact real current-K77 bilinear and neutral Krein forms;
- raw contragredient covariance and the failure of primal raw-dual action;
- the two distinct four-corner permutations;
- `Pi4/Pi14` nonidentity and target embedding;
- the first-order rolled `R4` compression; and
- the common 25-dimensional vector/Spin stabilizer kernel.

Before integration the comparator-routing audit reports `12` unclassified
paths: the frozen baseline nine plus TW3-A/B/C. Registering those three
execution artifacts restores the expected baseline; this hostile review is
outside the audit's derived registry scope. The hostile-lens audit fails only
on the known unrelated `LT-GR4` and `AC-F3` independent-owner debts. Those
failures neither touch nor weaken TW3.

## Survive / kill ledger

| proposition | hostile disposition |
|---|---|
| TW3-A matrix-copy intertwining is exact | **SURVIVES; FORMAL TENSOR NATURALITY ONLY** |
| identity identifies a fixed normal half with its raw dual equivariantly | **KILLED** |
| raw dual transforms contragrediently | **SURVIVES EXACTLY** |
| invariant Riesz identification makes the matrix copy transform primally | **SURVIVES EXACTLY** |
| strict `B14` algebraic dual and Krein/conjugate overline have the same corner map | **KILLED** |
| only the Krein/conjugate map matches TW2's displayed chiral overline | **SURVIVES EXACTLY** |
| both dual conventions retain all four corners | **SURVIVES EXACTLY** |
| common `+i` is literally the `G10` restriction on both halves | **KILLED; MINUS HALF NEEDS `-i`** |
| each Hermitian half has neutral `(8,8)` inertia | **SURVIVES EXACTLY** |
| selected and central-sign lifts are the same endomorphism | **KILLED** |
| central sign on an overlap makes associated descent projective | **KILLED** |
| opposite orientation gives the inverse component-tagged lift | **SURVIVES EXACTLY** |
| `Pi4=Pi14` | **KILLED** |
| first-order `R4` equals cubic `N3` | **KILLED** |
| lifted `u(3,2)` stabilizer kernel has dimension 25 | **SURVIVES EXACTLY** |
| associated descent constructs a global reduction or section | **KILLED / NEVER ESTABLISHED** |
| null symbol classes are physical cohomology | **KILLED / NEVER ESTABLISHED** |
| TW3 proves emergent chirality or quantum superposition | **KILLED / NEVER ESTABLISHED** |

## Final reprioritization

The superposition-twistor path should now be banked and deprioritized. TW3-B
adds a valuable exact semantic correction, but after that correction TW3-A
and TW3-C are formal functorial closure. There is no remaining finite gate on
already-owned objects whose outcome could discriminate the horn.

Legitimate reopeners are external to the current conditional inventory:

1. an independently owned global component-tagged normal `U(3,2)` reduction
   and its lifted topology; or
2. an independently owned genuinely non-product normal/mixed term in the
   rolled or detour operator.

Neither is permission to construct an action, background, global `J_N`,
`Tw^2` field, domain, quotient, or physical state in this channel. Until one
arrives, another twistor wave would repeat tensor-factor covariance rather
than advance the superposition hypothesis.
