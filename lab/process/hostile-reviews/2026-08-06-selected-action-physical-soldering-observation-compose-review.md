# Hostile review — selected-action physical soldering/observation composition

Date: 2026-08-06
Disposition: **PASS AFTER SCOPE REPAIR**

## Review charges

The review carried both mandatory charges:

1. find where the summary outruns the artifact; and
2. find where exact rigor is defending a superseded or mistyped object.

It also included the standing symplectic-geometry lens because the result
touches action variation, preboundary structure and observation descent.

## Variational-bicomplex review

The first draft risked calling `L^T E_A` the physical metric equation. That is
too strong. The complete equation is
`E_g^direct + L^!E_A + E_g^obs`, and the nonlinear version requires second
observation/soldering jets after integration by parts. The report was repaired
to claim only the local principal soldered/observation block.

The exact inverse-transpose observation theorem is sufficient at first-jet
germ grade, but it does not imply global ambient bulk-shell faithfulness. The
existing `y^2` control was retained.

## Symplectic-geometry review

The original gate asked for Euler and presymplectic response, but a rank
calculation alone would have proved only the bulk receiver. The review required
an explicit Green identity. The added four-vertex exact complex produces both
the formal-adjoint bulk term and the unrestricted boundary potential.

The boundary potential is not a BFV phase space, charge or quotient. Its
Dirichlet vanishing and unrestricted nonzero value are alternative domains,
not contradictory verdicts. The report now says “preboundary owner” and keeps
BFV open.

## Differential-geometric and gauge review

The gauge-rotated Levi-Civita derivative was already built. Recomputing it is
used only as an immutable exact owner and rank witness, not reported as a new
construction. A moving epsilon adds a connection-gauge image, while the
co-moving frame is a change of representation. Neither is identified with
physical metric motion.

The complete observation map acts on derivative direction. Tensoring it with
the connection carrier preserves the rank-ten soldering image. A naive
tangential-only pullback does not and is rejected by the graph-conormal plant.

## Source review

Weinstein's source language supports the gauge-rotated Levi-Civita,
two-connection augmented torsion and richer-than-naive observation route. It
does not state the exact chain theorem. The scoped return
`SOURCE-CONFIRMS_AND_SOURCE-SILENT` is correct; the exact result is attributed
to the repository.

## Constraint and prognosis review

No new field, coefficient, selector, quotient or datum was introduced. The
section-response term is a derivative of the already-present observation
field. P1/P2/P3 remain unused.

No verdict moves. Five distances move because one named gate closes at local
principal grade. The next gate is not “more first-jet verification”; it is the
second-jet nonlinear Euler/presymplectic construction, followed by the already
named BV/domain/BFV sequence.

## Final refusal set

The review refuses promotion to:

- nonlinear physical stationarity;
- equality of connection current and Hilbert stress;
- global ambient shell equivalence;
- diffeomorphism or odd super-IG cohomology;
- a reduced BFV phase space;
- global Krein/Green positivity or well-posedness;
- Einstein recovery, cosmology, particles, Q1 or unitarity.

With these fences explicit and planted, the local principal chain is admitted.

Post-repair reruns: main exact packet `39/39`; ledger migration packet `55/55`;
scope, functional-contract, predecessor, inventory, front-door, routing, strict
JSON, Python AST and whitespace checks pass. The protected-surface audit names
the intentional `RESEARCH-STATUS.md` update; the runbooks and active-research
inventory failures are pre-existing unrelated surface drift outside this Run's
scope.
