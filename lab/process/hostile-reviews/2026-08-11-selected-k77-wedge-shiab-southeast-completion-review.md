# Hostile review: selected K77 wedge-Shiab/southeast completion

Verdict: `SURVIVES_WITH_SCOPE_REPAIR__NO_PHYSICS_OR_ACTION_SELECTION_PROMOTION`.

## Charge 1 — summary outruns artifact

The exact probe establishes a flat principal operator family, not that the source action selects it. Calling the `960`-dimensional null half physical, gauge, BV, chiral, or generational would outrun the certificate. The report now says only principal semisimplicity and records both weights as unselected.

Two finite fields are strong exact arithmetic controls, not by themselves a proof over every characteristic. The identities used for the real statement are coefficientwise Clifford identities; positivity is separately justified over characteristic zero by the finite-group average containing the identity.

## Charge 2 — rigor on a superseded or mistyped object

The v0.163 theorem remains correct for every southeast-only repair, but it does not cover a changed Shiab middle block. The v0.172 zero-fermion restriction failure also survives; this construction changes the operator instead of inventing a quotient. Conversely, K95's quaternionic negative reciprocal sign is wrong-type on real K77 and is retained only as a planted failure.

The selected Spin operator is not identified with Curt's possible two `U(32,32)` halves or with a full `U(64,64)` parent. Those are explicit rival parents.

## Charge 3 — downstream disposition

- `dissolved`: the claim that the rank-128 Jordan remainder is unavoidable throughout the source-admitted wedge/southeast family; the K95 minus-sign port to K77.
- `survives`: the southeast-only BV no-go; the zero-fermion owned-quotient failure; both free chiral weights; the action/reality, global-domain, observation, BV, index and count burdens.
- `needs-recheck`: every downstream statement that treated the old Jordan remainder as universal rather than operator-specific; compatibility with the actual selected action.

## Required lenses

Layer-0 semantics and prior art produced the operator-change/southeast-only separation. The analytic lens refuses to infer a global closed domain from a positive principal symmetrizer. The symplectic lens refuses to identify that symmetrizer or the null characteristic half with a covariant-phase-space/BV reduction.

No new trap is filed: the repeatable error—porting a coefficient across a real-form fork—is already covered by the signature/parity dependency discipline. No ledger verdict or canon change is warranted.
