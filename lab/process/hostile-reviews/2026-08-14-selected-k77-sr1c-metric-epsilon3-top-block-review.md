---
artifact_type: hostile_review
created: 2026-08-14
target: explorations/conditional-build/selected-k77-sr1c-metric-epsilon3-top-block-2026-08-14.md
verdict: SURVIVES_WITH_SCOPE_NARROWING__SELECTED_TOP_BLOCK_ZERO_ONLY
---

# Hostile review: selected-K77 SR-1C metric `epsilon^3` top block

## Verdict

`SURVIVES_WITH_SCOPE_NARROWING__SELECTED_TOP_BLOCK_ZERO_ONLY`.

The exact coefficient bank supports the advertised zero and the reduction of
the safe selected metric envelope to `(3,2,2)`. It does not support replacing
the missing lower `O_SR1C` operator by zero, declaring either branch
stationary, or exporting the result beyond the selected K77 action grammar.

## Strongest attacks

### The zero might be caused by dead inputs

Rejected. On every tested covector, the metric connection bank has rank nine,
the primitive epsilon bank rank 91, the metric curvature bank rank six, and
its selected-Shiab image rank six. Only the final action pairing vanishes.

### The two polarized halves might cancel accidentally

Rejected. Both halves have zero nonzero entries separately. The
epsilon-curvature half dies through `k wedge k=0`; the other half dies through
the selected same-grade pairing.

### The wrong independent-variable graph might have been used

Rejected at the stated scope. The derivation explicitly holds `varpi` fixed
and uses `delta T=-delta B` for both metric and primitive epsilon variations.
It does not feed `-delta B` through the independent-`varpi` response.

### Moving geometry might restore an `epsilon^3` term

Not at the stated top derivative count. Moving Shiab, Hodge, frame, density
and lowerer coefficients depend on undifferentiated primitive epsilon in this
source chart. Their metric adjoint returns can populate the remaining
order-two-or-lower bank, which is still open, but do not add the missing third
epsilon derivative to this coefficient.

### Three causal representatives are not a global theorem

Accepted as a scope fence. The result is an exact local selected-symbol
certificate on the repository's canonical timelike, spacelike and null
representatives. It does not establish a global operator domain, nonlinear
formal integrability, or a statement for expanded action parents.

### Rank zero could be promoted to a branch kill

Rejected. The coefficient is branch-independent and removes one top-order
obligation. It supplies no lower branch evaluator and no compatible stationary
jet. Both roots remain not yet falsified.

## Required successor

Serialize the branch-dependent lower `j^1(E_B-E_T)` coefficients and every
moving Shiab/Hodge/frame/density/lowerer return in the common basis. Evaluate
both exact roots, use a held-out coefficient check, and only then solve the
surviving `(3,2,2)` compatible jets.
