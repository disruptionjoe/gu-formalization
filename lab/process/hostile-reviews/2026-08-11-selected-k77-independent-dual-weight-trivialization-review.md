# Hostile review: K77 independent-dual weight trivialization

Verdict: `SURVIVES_WITH_SCOPE_REPAIR__CONSTANT_SOURCE_NATIVE_ORBIT_ONLY`.

## Charge 1 — where the summary outruns the artifact

The exact theorem concerns constant nonzero scalar weights and even
chirality-preserving connection cells on the selected real-K77 carrier. If
the weights become functions on `Y`, differentiating the field redefinition
creates additional connection terms; no function-valued trivialization is
claimed. Transported rank-640 observation is not a proof of physical
observation basicness on a global closed domain.

The equality of operator matrices proves classical field-coordinate
equivalence. It does not prove equality of a quantum measure, determinant or
Pfaffian; anomalies and Jacobians remain outside this packet.

## Charge 2 — rigor defending a superseded or mistyped object

V0.174 rigorously classified congruent quadratic Grassmann pairings, but the
source parent has four independent barred/unbarred fields. Its `p` invariant
was therefore one inference too early. The two pairing lines remain valid
conditional reality branches; they are not source-native weight selectors.

Holding the observation matrix fixed while changing field coordinates would
manufacture a difference. The geometrically same observable is transported
as `O R`; the certificate includes the held-fixed version only as a firing
plant.

## Charge 3 — downstream disposition

- `dissolved`: a source-native invariant `p`; a need for nonlinear
  covariance, Noether or fixed unit-block normalization to select the two
  constant weights.
- `survives`: v0.174's two local pairing lines as conditional reality
  branches; anti-linear reality; moving global Green/domain; physical
  observation, BV/cohomology, mirror, index and count.
- `needs-recheck`: any downstream parameter count charging `p` before a
  reality congruence; any use of a fixed coordinate observation matrix; any
  extension from constant weights to functions or to the quantum measure.

No canon or ledger verdict changes. The next gate is reality/domain, not
another scalar selector search.
