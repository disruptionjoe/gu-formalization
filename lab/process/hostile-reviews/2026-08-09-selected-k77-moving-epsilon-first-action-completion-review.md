# Hostile review: selected K77 moving-epsilon first-action completion

Date: 2026-08-09
Verdict: `SURVIVES_WITH_PARENT_AND_QUOTIENT_FENCES`

## Pre-wave

1. **Fork assumed:** the conditionally selected real, grade-preserving
   `Spin(7,7)` parent.  This is declared scope, not a settlement of the full
   action parent.
2. **Search space:** `91` primitive epsilon inputs by `1,274` grade-two
   receivers, on two exact branches and three causal representatives.  The
   finite coefficient question can be decided wholesale; global analysis
   cannot.
3. **New unowned object:** none.
4. **What dies on success:** the claim that omitted lower Cartan or
   moving-Shiab epsilon terms could rescue the 321-field selected-Spin
   truncation.

## Charge 1 — where might the summary outrun the artifact?

The sentence “epsilon cannot cancel” would be too broad.  The certificate says
that the complete primitive-epsilon **grade-two first-action block on the
selected Spin parent** equals its fixed principal summand.  It does not cover a
full `U(64,64)` parent that mixes the selected blocks, nor a source-derived
equation quotient, another action, boundary reduction or analytic domain.

The summary also must not say the moving Shiab is zero.  v0.106 proves it is
rank 91 on grade one.  The new theorem is a grade-two pairing zero, with that
grade-one block as a strong nonvacuity control.

## Charge 2 — is rigor defending a superseded or mistyped object?

The first implementation did: it recursively recomputed v0.122 and then began
recomputing the same bank.  That was stopped.  The accepted certificate starts
from the v0.106 carrier and factors the shared exact tensors once.

Scientifically, the old v0.106 outer moving-Shiab term was incomplete as the
full mixed derivative.  The present wave does not defend it by citation; it
adds the inner moving-Shiab occurrence and the lower Cartan term.  Both vanish
independently, so the old grade-two zero survives for the right reason.

## Charge 3 — what else must change?

- **Needs recheck:** the 321-field candidate should no longer be described as
  awaiting a generic “moving metric/epsilon cancellation.”  Epsilon is now
  closed negatively; only metric columns remain to compute, and they cannot
  cancel distinct epsilon input columns.
- **Survives:** the 1,571-field source tangent remains a candidate, not a
  promotion.  A smaller expansion or source/action-owned equation quotient is
  still possible.
- **Survives:** selected Spin, two `U(32,32)` halves and full `U(64,64)` remain
  distinct.
- **Survives:** P1/P2/P3, residue, canon and public posture do not move.

## Required lenses

- **Layer-0 semantics:** primitive source epsilon, diagonal gauge
  characteristic and boundary symmetry are not synonyms.  Pass.
- **Prior art:** v0.66, v0.67, v0.94/v0.97, v0.106 and v0.122 were checked as
  different objects before composition.  Pass.
- **Representation:** the theorem uses Spin grade preservation.  Porting it to
  expanded unitary parents without recomputation is prohibited.  Pass with
  fence.
- **Variational bicomplex:** every explicit `D_epsilon E_T` Shiab occurrence
  and the full `D_B eta` chain are present.  Pass.
- **Symplectic:** no gauge quotient is inferred from the word epsilon; the
  endpoint momentum and charged boundary transformations remain distinct.
  Pass.
- **Analytic/Krein:** no positivity, contour, hyperbolicity, `Dmax/Dmin`, Green
  operator or unitarity claim is made.  Pass.
- **Source:** the derivative grammar is source-confirmed; the bank and
  truncation verdict are repository-derived.  Pass.

## Validation and process disposition

The primary route passes `399/399`.  The independent Sage route checks six
separate structural zeros over all `91*1,274` pairs (`695,604` exact scalar
checks) and passes `6/6`.  The independent route does not read the primary
columns.

The recursive-predecessor replay cost is a real automation defect.  The next
Run must build a versioned exact coefficient-bank API with source/construction
hashes, stale-cache rejection and a bounded equivalence replay before another
heavy Hessian extension.  This process gate is mandatory and ranked ahead of
the remaining metric calculation.

No ledger verdict class, canon verdict or public posture changes in this
review.
