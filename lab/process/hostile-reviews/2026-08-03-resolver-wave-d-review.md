---
artifact_type: hostile-review
created: 2026-08-03
target: RESOLVER-WAVE-D-NATIVE-126-CONNECTION-PLACEMENT
verdict: PASS_AFTER_REPAIRS
review_lenses:
  - exact representation and exterior algebra
  - Krein/right-H/C plus primary-source typing
  - summary-versus-artifact and stale-object audit
---

# Hostile review: Resolver Wave D

## Verdict

**PASS AFTER REPAIRS.** The local `OPEN -> PARTIAL_CONSTRUCTED` transition is
earned. No reviewer found a contradiction in the real-252 contraction, the
native K/right-H/C classification, or the `4+5=9` full-Spin coefficient lock.
The review did prevent the one-form-output comparator from being overstated as
a pure 144 mediator.

## Repairs required and made

1. The exterior carrier is now typed covariantly as
   `N* tensor Lambda6(N*)`, with the pseudo-metric musical appearing only in
   contraction. The untested word "orthogonal" and the over-strong word
   "unique" were removed.
2. The Sage certificate no longer defines the D7 hook by subtraction. It
   checks the explicit irreducible hook of highest weight
   `(1,0,0,0,0,1,1)` and the two explicit middle-degree seven-form summands.
3. The N1 provenance controls now use `M3(C)`: a rank-two skew `Y_C` and a
   full-rank anti-Hermitian `Y_K`.
4. The full-20 support matcher now examines every written `I` and `R` slot for
   every pure-spinor source slot. For the planted five-form representative,
   each source has exactly one desired 144 component plus paired
   `imGamma16` and `kerGamma16` components. The report now says explicitly
   that `P_R` retains the low-`R` 16 and that no allowed `X`-only `P0` has been
   constructed.
5. Layer 0 now separates the affine connection `A`, the tensorial source
   residue `v_s=res_s^V(A-A0)`, and Weinstein's gauge-rotated distortion
   `T_omega`. Their identification remains open.
6. The TOE projection-versus-contraction correction is scoped to Eric and
   Curt's curvature-map discussion. The exact Wave-D
   `V* tensor Lambda6 -> Lambda5` map remains `SOURCE-SILENT`.
7. The grade-ten result is described only as an abstract multiplicity-one
   comparator carrier. No executable Hodge--wedge map or use of P1 is claimed.
8. The next construction is recorded as several independent arrows—source
   selection, moving soldering/descent, full-20 identification and companion
   treatment, reciprocal K/C blocks, `P0/Y` ordering, and Euler/Ward
   closure—not as "one arrow away."
9. The K density is written with the actual pairing and adjoint projector as
   `Herm(P0^dagger K_G c_rho(T) Y_K P0)`; the C density remains the distinct
   alternating `Alt(P0^T C c_rho(T) Y_C P0)` object.

## Rerun receipt

- Exact signed exterior algebra: `31/31`.
- Independent Sage D5/D7 characters: `12/12`.
- Native Cl(9,5), K/right-H/C, written-contraction, and full-20 probe: `27/27`.
- Direct packet total: **59 exact/boundary assertions plus 11 planted or
  hostile controls = 70/70**.
- Scope, inventory, JSON, compilation, predecessor-regression, and whitespace
  gates are part of final closeout; no promotion is allowed if any fails.

## Durable boundary

This review admits a canonical local real-252 effective kernel derived from a
native grade-six tensorial connection coefficient. It does **not** admit a
pure 144 mediator, source-selected background, moving full-Sp descent,
complete `P0/rho/Y_K/Y_C/C` kernel, stationary VEV, mass, count, or use of
P1/P2/P3. The next gate remains
`RESOLVER-WAVE-E-SOURCE-OWNED-MOVING-252-FULL20-PLACEMENT`.
