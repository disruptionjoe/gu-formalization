---
artifact_type: hostile_review
created: 2026-08-08
status: PASS_AFTER_SCOPE_REPAIRS
subject: selected K77 stationary two-layer Hessian factorization
---

# Hostile review: stationary two-layer Hessian factorization

## Charges

### Summary-outruns-artifact reviewer

**Charge fired.** The factorization theorem was already present in the v0.43
and v0.53 chain. The new contribution is its composition with v0.81 and the
resulting owner reduction. The result and registry now say `COMPOSED`, not
`NEWLY_DERIVED_FROM_GU`.

### Superseded-object reviewer

**Charge fired.** A naive efficiency reading would again delete moving
operator terms at residual zero. v0.53 already retracted that move because
`F_A*` and `T*` can be nonzero while their total residual cancels. The report
now retains physical `(D Shiab)F_A*` and `(D Hodge)T*` inside `D Upsilon`.

### Symplectic geometry

The bulk Gram Hessian does not determine its Green concomitant,
presymplectic current, coisotropic reduction or BFV phase space. Pass only
with those downstream owners explicit.

### Variational PDE

The on-shell Hessian identity is exact. Off-shell, `D2 Upsilon`, `D K` and
receiver derivatives return; the planted off-shell fixture confirms the
scope boundary.

### Krein/operator theory

The indefinite control shows injective `D Upsilon` can still have a null Gram
form. No kernel, energy or closed-domain result follows before the actual
pairing is supplied.

### Complex/path-integral analysis

The real finite quadratic operator neither chooses a contour nor defines a
functional determinant or measure. These are not hidden in `K`.

### Source criticism

The primary-source loci confirm norm-square grammar and redundancy but remain
silent on `D Upsilon`, `K` and the physical complex. Source disposition is
correctly split.

## Verdict

`PASS_AFTER_SCOPE_REPAIRS`. The next Build may use the first-derivative
reduction, but it must construct the complete coupled `D Upsilon`, prove
`J R=0`, and retain Green/Krein/symplectic obligations.
