# Hostile review: b5 five field packet design

Target: `explorations/b5-five-field-packet-design-2026-08-11.md`
Run: `historical-investigation` (same-run self-review by the design pass; the executing wave reviews independently).

## Self-hostile-review (the three standing charges)

**Charge 1 — where the summary outruns the artifact.** Highest-risk
sentences audited: (a) "fields (i)/(ii) largely owned" — the owning results
are at *finite associated-bundle / formal first-order-expression grade*,
and the S4 probe's intertwiner residuals (`1.34e-15` against plant residual
`2`) are floating-point evaluations of algebraic quantities, not
certified-exact arithmetic; this packet therefore carries the grade
qualifier at every use, and Wave One is required to redo its load-bearing
algebra exactly. Not FD — P-H29's letter does not apply — but the spirit
(machine-band reads) is flagged: **needs-recheck** at Wave One, by design.
(b) "the audit is stale in detail" — checked against the audit's own scope:
the audit asked about repo-owned FREEZING of packet fields; the post-audit
results freeze nothing in the certificates, so the audit's operative
verdict (contract rejects all packets) is *current*; only its per-field
narrative is superseded. The compressed-state section states both.
Empty-list check: no sentence in this packet asserts a packet field is
currently frozen — confirmed by re-reading the per-field verdict lines.

**Charge 2 — where rigor defends a superseded or mistyped object.** The
sharpest instance is the audit's OWN field list: field (ii) as "ten phase
values" and field (v) as "one canonical domain" are, per DC-H1/C1,
demands for objects proven not to exist in that form (a global section of a
nontrivial local system; a canonical point of a positive-dimensional
moduli). This packet deliberately re-types both rather than defending the
07-29 spec verbatim — and flags that the re-typing itself is PROPOSED until
a wave certifies it on the packet objects. Second instance: any reader
taking the full20 "written differential" as W131 — the Layer-0 table
carries the proper-suboperator correction. Third: M-H10 quoted as the named
route while its literature premise is gapped — stated in two sections, not
repaired here.

**Charge 3 — what else must change if this packet's reading stands.**
Dissolved: nothing — this packet binds nothing and dissolves nothing by
itself (explicitly empty list). Survives: the audit's BLOCKED operative
verdict and the fail-closed contract (unchanged until a wave freezes
fields); the imposed-wall comparator as the standard-side positive control;
the RB7/W177 kills on exact evidence; the eleven-pair census as the
unconditional repo state. Needs-recheck, if Wave One lands outcome (a):
`CURRENT-STATE.yaml:66-70` findings sentence ("ten phase invariants ... remain
unselected" — already stale in coefficient-level detail per S4/DC-H1);
the packet-contract schema for field (ii); the register M-H10 row's
"Bär–Ballmann ... generically" premise; `explorations/decision-tree-Q1a-
fiber-end-classification-2026-07-21.md`'s unbannered "unique and forced"
domain claim (banner owed regardless, recorded by C1); and the
`sa-y8`/full20 "Next" chains, which Wave One would partially discharge.
Each of these is a consumer surface, not a verdict: the flips, if any,
belong to the executing wave under the CS workflow and hostile-review
rules.
