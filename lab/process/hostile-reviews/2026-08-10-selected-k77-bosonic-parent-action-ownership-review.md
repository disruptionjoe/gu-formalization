---
artifact_type: hostile_review
created: 2026-08-10
status: CANDIDATE_SURVIVES_WITH_ZERO_BRANCH_SCOPE__HARD_REDUCTION_NOT_ACTION_DERIVED__NONZERO_BRANCH_NORMAL_HESSIAN_REQUIRED
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Hostile review: selected K77 bosonic parent action ownership

## Pre-wave

- **Fork assumed:** labelled K77 is the executed comparator; no bosonic parent,
  two-half reduction or physical carrier is assumed selected.
- **Search space:** exact wholesale composition over `16384` connection
  directions, with two intersecting binary decompositions. The zero-branch
  question is decidable wholesale. The full nonzero-branch Hessian is not.
- **New un-owned object:** the negative result introduces none. A hard
  restriction would require a multiplier, penalty, BV differential, boundary
  condition or declared field-domain posit not present in the action.
- **What dies:** only the claim that the written action dynamically derives
  `P_epsilon varpi=varpi`, and the substitution of
  `D_varpi chi_epsilon=0` for that equation.

## Charge 1 — where the summary outruns the artifact

The initial candidate combined two exact facts too aggressively. The full
quadratic `T` norm gives a nonzero complement Hessian at `T=0`; separately,
the source-residual first-order graph preserves both `B`-adjoint sectors. The
second fact is not the full selected-action Hessian at the nonzero stationary
branch. Therefore “the complement is dynamical on every action branch” is not
established.

The safe claim is narrower and still decisive: the written action does not
produce a hard complement constraint at the zero branch, and any physical
parent reduction must survive a separate nonzero-branch normal-Hessian test.

## Charge 2 — where rigor could defend a superseded or mistyped object

The `8128+8256` `B`-adjoint split and the `8192+8192` Weyl block/coset split are
not interchangeable. The nonzero cross cells prove this without convention.
Similarly, the gauge-rotated Levi-Civita term is the reference connection in a
difference; it is not the variable connection. Treating its compatibility as
compatibility of all `varpi` silently deletes an odd connection sector.

The review also rejects using the fixed fermion `W` or its Casimir as a repair.
Bosonic parent ownership must be completed before the induced fermion operator
can be asked to select a physical carrier.

## Charge 3 — downstream disposition

- **Dissolved:** hard-action-constraint interpretation of
  `P_epsilon varpi=varpi`; `D_varpi chi=0` as a synonym for the `B`-adjoint
  restriction; immediate promotion to a fermion selector before the parent
  normal Hessian.
- **Survives:** the moving rank-`8128` bundle; the full-unitary comparator; the
  exact local first-order sector closure; the zero and nonzero invariant
  stationary branches; the moving Spin bundle as a conditional consistent
  truncation/real-form posit; every fixed-`W` result at conditional grade.
- **Needs recheck:** the full selected-action normal Hessian at
  `T*=-(kappa_1/312)Phi1`; all four parity-by-block cells; possible heavy-mode
  integration; global/BV/domain restrictions; full-U versus two-half parent;
  the induced Dirac/RS operator and physical carrier.

## Required lenses

- **Layer-0 semantics — ACTUAL MATH, very high.** All four object distinctions
  are typed and the two decompositions have unequal cross-cell dimensions.
- **Prior art — ACTUAL MATH, very high.** The result composes immutable exact
  receipts; it does not relabel a new evaluator as independent evidence.
- **Variational/analytic — ACTUAL MATH, high.** The zero-branch Hessian result
  is exact; stability, positivity, integration cycle and the nonzero Hessian
  remain open.
- **Symplectic geometry — ACTUAL MATH, high.** A nonzero bulk Hessian direction
  is not a characteristic kernel or quotient. The review makes no claim about
  the eventual reduced covariant phase space.
- **Principal-bundle/Clifford — ACTUAL MATH, high.** Block compatibility and
  `B`-adjoint parity are distinct reductions; odd `Phi1` gives an explicit
  incompatibility on the known nonzero invariant line.
- **Adversarial scope — ACTUAL MATH, very high.** The strongest surviving
  result is deliberately branch-scoped, and it preserves the conditional
  moving-Spin route rather than declaring the full theory dead.

## Controls

The exact composition probe passes `38/38`. A planted `P`-only action has zero
complement Hessian and is rejected by the actual full norm. A block-even plant
passes `D chi=0`, while the odd branch fails it. These controls separate the
claimed mechanisms instead of merely confirming dimensions.

## Verdict

```text
CANDIDATE_SURVIVES_WITH_ZERO_BRANCH_SCOPE
HARD_REDUCTION_NOT_ACTION_DERIVED
NONZERO_BRANCH_NORMAL_HESSIAN_REQUIRED
```

This review changes no ledger verdict, canon verdict, quotient, datum or public
posture. It changes the dependency order: nonzero-branch bosonic normal
Hessian first, induced fermion operator second.
