# Hostile review — K149 minimal moving evaluator gate

## Strongest overclaim

The generic engine passing its Green and connection plants does not mean the
curved `I1B` operator has been evaluated. K149 has no executable
`moving_selected_shiab_coefficient`; therefore it cannot report the restricted
residual as zero, nonzero, or basic on the null quotient.

## Strongest contrary construction or mistyping

A user could feed the API K132's frozen coefficient as a constant and obtain a
formally valid differential operator. That construction would erase the moving
Shiab, Hodge, pairing, density, one-form connection, Clifford-adjoint
connection, and coefficient derivatives. It is a regression fixture, not the
curved action. Likewise `formal_transpose()` is correct only for coordinate
density and standard pairings; the target must use
`weighted_formal_adjoint()` with its actual moving inputs.

## Weakest reproducibility or propagation seam

The closure audit uses explicit adapter names as an executable interface. An
equivalent future adapter under a different name will require updating the
audit and recording the semantic equivalence. Until then the result is
reproducible as a code-surface absence, not a mathematical non-existence
theorem.

## Verdict

Accept the generic differential-algebra result and the exact serialization
boundary. Reject any curved lower value, preferred-Shiab recovery, quotient
endomorphism, inverse, domain, BFV, physical propagation, positivity, or GU-wide
inference. K150 should implement only the first target adapter and demand
frozen plus rationally rotated covariance controls before continuing.
