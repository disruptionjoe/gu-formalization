---
artifact_type: hostile_review
status: review
doc_type: joe_directed_twistor_conditional_composition_hostile_review
created: 2026-08-16
work_item: RUN-20260817-001411-HOSTILE-REVIEW
channel: joe_directed_superposition_twistor_conditional_build
title: "Hostile review of TW-1, TW-2 and AR-6: the normal-twistor Spin lift and exact cubic symbol/compression results survive; standard-detour normalization, dual-target typing, comparator evidence grade and routing coverage require repair"
grade: "INDEPENDENT SIXTEEN-LENS HOSTILE REVIEW with exact replay of TW-1 (85/85 and 8/8 mutants), TW-2 (128/128; selftest 132/132), direct twistor/reverse-J/local-detour antecedents (47/47, 112/112, 43/43, 66/66, 54/54, 50/50), M-H17 (10/10), numerical generation-inertia replay, and corrected AR-1 (59/59). The pre-repair routing audit fails because TW-1 and TW-2 are unregistered and AR-6 evades derived scope through an inaccurate stewardship-record declaration. One old B2C2B probe has a stale campaign-pointer assertion; its algebraic successor inputs replay independently."
disposition: "PASS_AFTER_REQUIRED_SCOPE_AND_AUDIT_REPAIRS__TW1_EXACT_COMPONENT_TAGGED_SPIN_LIFT_BANKED__TW2_EXACT_Q_CUBIC_SYMBOL_AND_ROLLED_BASE_COMPRESSION_BANKED__STANDARD_GSS_LINE_ONLY_UP_TO_SCALAR_OR_PENDING_DIRECT_COMPARISON__AR6_TYPED_OVERLAP_CLOSED__NO_ACTION_BACKGROUND_DOMAIN_PHYSICAL_COHOMOLOGY_OR_SUPERPOSITION_CLAIM"
canon_verdict_change: none
steering_effect: "Bank TW-1 and TW-2 at their exact conditional ceilings and close AR-6 as a typed numerical overlap. Do not extend TW-2 by inventing a Tw2 field owner, action, total connection, background, analytic adjoint/domain or physical quotient. The only worthwhile next twistor swing is one bounded finite coherence/gluing test on already-owned TW-1/TW-2 objects; if it reduces to tensor-product commutation, bank it and deprioritize the path. H210 and neutrino remain paused at their explicit triggers; extra vectors remain retired."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/conditional-build-channel-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw1-normal-twistor-spin-lift-2026-08-16.md
  - tests/channel-swings/joe_directed_tw1_normal_twistor_spin_lift_probe.py
  - lab/active-research/joe-directed/superposition-twistor/tw2-four-dimensional-detour-symbol-factorization-2026-08-16.md
  - tests/channel-swings/joe_directed_tw2_four_dimensional_detour_symbol_factorization_probe.py
  - lab/active-research/joe-directed/archaeology/ar6-mh17-generation-inertia-type-crosswalk-2026-08-16.md
  - lab/active-research/joe-directed/archaeology/ar1-dropped-commitments-ledger-2026-08-15.md
  - explorations/conditional-build/selected-k77-twistor-bv-positive-state-seven-gate-2026-08-13.md
  - explorations/conditional-build/selected-k77-reverse-j-descent-census-2026-08-14.md
  - explorations/conditional-build/selected-k77-moving-j-stueckelberg-cone-2026-08-14.md
  - explorations/conditional-build/selected-k77-twistor-carrier-weyl-integrability-gate-2026-08-14.md
  - explorations/conditional-build/selected-k77-local-twistor-bach-detour-composition-gate-2026-08-14.md
  - explorations/positivity-exit-criteria-design-packet-2026-08-11.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This review crosses a
> standard conformal twistor-spinor detour theorem, a conditional current-K77
> compression, a conventional free M-H17 positivity comparator and a distinct
> generation-sector Krein calculation. Results about ordinary family indices,
> net chirality, standard Higgs/VEV or SO(10) `126` models, anomaly selectors,
> VEV-only breaking or familiar vector-mass routes bind only those named
> comparators. They do not adjudicate Weinstein's source-native `2+1` or
> emergent-chirality mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# Hostile review: TW-1, TW-2 and AR-6

## Verdict first

The wave survives after four bounded scope and audit repairs.

TW-1 is the strongest clean result. On the declared orientation-aligned
component of the current real-K77 normal-twistor horn, the vector quarter-turn
has an explicit equivariant Spin lift with

```text
S_J^2=-J10,
S_J^4=-1,
S_J^8=1,
S_hat_J^2=-Jhat.
```

The opposite normal-complex-orientation component has the opposite square sign
relative to the same fixed normal volume, while the two central-sign Spin
preimages on one component have the same square. Both K77 ambient halves and
all four observation corners survive. This is exact associated-bundle
kinematics, not selection of `J_N`, a physical imaginary unit or quantum
superposition.

TW-2's exact rational theorem also stands: inside the declared
`span{Q^3,qQ}` grammar the unique projective cubic annihilating the twistor
image on both sides is

```text
N_3=Q^3-(1/4)qQ=(3/4)qQ-(1/2)TcT#.
```

The non-null symbol sequence is exact; on the nonzero null cone its two middle
cohomologies have dimension six for Dirac spinors and three per Weyl
orientation. The `Pi_4` target embeds in ambient `ker Gamma_14`, and the
base-compressed rolled current-K77 symbol contains the first-order
`Q,T,-T#` blocks. Direct equality with the third-order detour operator fails.

The required qualification is prior-art exactness. Gover--Somberg--Soucek
construct the standard middle map and identify its leading symbol only up to a
nonzero multiple as `K*R_2K`, equivalently a multiple of `R_1K*K`. The new
probe does not construct `K`, `Tw^2`, `R_2` or `K*`, and proves uniqueness only
inside its declared two-dimensional `Q`-cubic grammar. Until a direct
comparison or a cited representation-uniqueness theorem closes that seam,
`N_3` is an exact normalized cubic complex symbol consistent with the standard
detour line, not an independently normalized formula extracted from GSS.

AR-6's conclusion is correct: the two occurrences of `(96,96,0)` are a
numerical collision between different constructors, not one object. The
generation calculation restricts a Clifford-derived form to an embedded
kinematic subspace; M-H17 descends a planted coordinate form through a
separate rational quotient complex whose `pair_count=96` is an input. Equal
inertia supplies noncanonical abstract isometries after scalar extension, not
a carrier, chain, pairing or physical bridge. The conclusion does not require
the generation calculation to be exact, but the artifact must stop calling
that NumPy/eigensolver replay exact and must not evade the routing registry by
calling this direct comparator adjudication a pointer-only stewardship record.

Final disposition:

```text
PASS_AFTER_REQUIRED_SCOPE_AND_AUDIT_REPAIRS
__BANK_TW1_AND_TW2
__CLOSE_AR6_AS_TYPED_OVERLAP
__NO_PHYSICAL_OR_CANON_VERDICT_MOVE
```

## Sixteen hostile lenses

### Lens 1 — source and superposition horn

The source is silent on the TW-1 Spin lift, the TW-2 detour factorization and
the AR-6 object comparison. Both execution artifacts correctly declare horns
rather than source facts. TW-1 assumes a supplied normal orthogonal complex
structure. TW-2 assumes Bach-flat base compatibility and zero total current
only to ask a conditional fit question. Neither assumption is dynamically
selected. **Survives.**

### Lens 2 — `2+1`, imposter and partner custody

No artifact turns a twistor eigenspace, null symbol class or neutral Krein
direction into a family. The source imposter `F`, abstract multiplicity space
`M_3` and distinct internal partner `144` remain separate. AR-6 expressly
denies that equal inertia identifies the generation triplet with M-H17. No
ordinary family index enters. **Survives.**

### Lens 3 — emergent chirality and both halves

TW-1's lift is Clifford-even and commutes with ambient chirality. Its exact
spectra retain both complex rank-64 ambient halves and all four rank-32
observation blocks. TW-2 retains both four-dimensional chiral detour
sequences, tensors each with both internal rank-16 factors and does not delete
a corner. Fundamental non-chirality and still-unconstructed emergent observed
chirality remain distinct. **Survives.**

### Lens 4 — Layer-0 object identity

TW-1 keeps `J_N`, its infinitesimal spin image `j_tilde`, the group lift
`S_J`, the spinor volume `J10` and the rolled endomorphism `Jhat` distinct.
TW-2 keeps the standard four-dimensional detour, its algebraic cubic symbol,
the first-order rolled K77 operator, `Pi_4`, `Pi_14`, symbol cohomology and
physical cohomology distinct. AR-6 keeps embedded subspace restriction and
cohomological quotient distinct. **Survives.**

### Lens 5 — Spin-cover component, orientation and sign

The rational certificate proves the selected sign rather than assuming it:

```text
T=sqrt(32)S_J,
T^2=-32J10,
T^-2=+J10/32.
```

Changing `S_J` to its other central-sign lift leaves the square unchanged.
Replacing `J_N` by `-J_N` changes the normal complex orientation in odd
complex dimension five and crosses the component fence. The artifact reports
the sign only relative to the fixed `NORMAL` orientation. **Survives.**

### Lens 6 — Clifford real form

TW-1 uses an exact real `Cl(7,7)` bank with normal signature `(6,4)` and no
K95 transfer. TW-2 uses rational `Cl(2,2)` only for the complexified
four-dimensional symbol identities and explicitly makes the K77 statement
through the independently typed base compression. AR-6 records that the
generation probe's K95/K77/Euclidean robustness does not type the separate
coordinate comparator. **Survives.**

### Lens 7 — stabilizer, equivariance and naturality

The complete normal `45`-generator calculation gives orbit rank `20` and
centralizer dimension `25` in both vector and Spin representations, with
identical infinitesimal coefficient kernels. This is the expected
`u(3,2)`. Conjugation by a co-moving Spin lift preserves the square relation;
central overlap signs cancel in conjugation. The statement constructs no
global section or preferred reduction. **Survives.**

### Lens 8 — spectral multiplicities

The five-weight enumeration agrees with the exact Clifford square:

```text
S10+: zeta8^7 x10, zeta8^3 x6;
S10-: zeta8^1 x10, zeta8^5 x6.
```

Multiplication by the base-Weyl rank gives `20/12` in each appropriate
observation block and identical total spectra on the two ambient halves. The
labels are convention-tagged normal half-spin labels, not physical energy or
family multiplicities. **Survives.**

### Lens 9 — conformal and twistor geometry

The antecedents correctly distinguish the base twistor carrier, normal
`O(6,4)/U(3,2)` orbit and local spin-tractor bundle. The strict Lorentzian
holomorphic route is conformally-flat scoped; the curved local-twistor
connection exists generically, while its standard detour complex closes on
the Bach-flat locus. Base Bach-flatness does not close a normal/mixed total
twist. **Survives.**

### Lens 10 — detour prior art and normalization

The cited GSS theorem supports the standard Bach-flat detour complex, its
orders `1/3/1`, chiral splitting and leading factorization through the
`Tw^2` bundle. It supports that leading line only up to nonzero scalar. The
probe supplies a new exact `1/4` normalization inside a bounded `Q`-cubic
grammar, but it does not reconstruct the GSS `K*R_2K` symbol or prove that the
grammar exhausts every standard equivariant cubic. The original “normalized
top-symbol line of the standard detour” wording therefore outruns the receipt.
**Requires a claim-ceiling repair; the algebraic theorem survives.**

### Lens 11 — symbol, order and domain

`T#` is an algebraic metric-dual symbol, not a formal adjoint on an analytic
domain. The displayed matrices identify dual target fibres using the declared
algebraic fibre metric/pairing. This identification must be stated where
`N_3:Tw->Tw*` is displayed. The rolled K77 block is first order and `20x20` in
the compressed Dirac presentation; `N_3` is third order on the rank-12
twistor target. No direct operator equality, Green domain or self-adjoint
realization follows. **Survives after explicit dual-target typing.**

### Lens 12 — null cohomology versus physical states

At nonzero null covectors the exact ranks are `(4,2,4)` and the two middle
Dirac symbol cohomologies are `(6,6)`; per Weyl orientation they are
`(2,1,2)` and `(3,3)`. These are characteristic-symbol classes on the null
cone. They are not global solutions, propagating particle counts, BV/BFV
cohomology, a positive state space or superposition. **Survives.**

### Lens 13 — `Pi_4`, `Pi_14` and rolled compression

The exact identities are correctly asymmetric:

```text
Pi_4^2=Pi_4,
Gamma_4 Pi_14,base=(5/7)Gamma_4,
Pi_14,base Pi_4=Pi_4 Pi_14,base=Pi_4.
```

Only an already four-dimensionally gamma-traceless target embeds with zero
normal one-form legs. The source projectors and operators are unequal. The
compressed rolled symbol contains `Q,T,-T#`; it does not become the cubic
middle map. **Survives.**

### Lens 14 — M-H17 carrier, pairing and provenance

The M-H17 exact fixture starts from `Q^(384)`, inserts a rank-192 coordinate
differential and descends `diag(+I_96,-I_96,0,0)` to a rational
`(96,96,0)` quotient form. The generation script starts from a complex
`14x128` Clifford module, numerically selects a 192-dimensional top-Casimir
subspace and restricts `eta_V tensor beta_S`. It uses no cochain differential
or quotient. Agreement of inertia does not commute these constructions.
**AR-6's closure survives.**

### Lens 15 — audit, registry and `doc_type` honesty

Before repair, the routing audit reports `11` unclassified artifacts against
the frozen baseline `9`; the two new gaps are TW-1 and TW-2. They must be
registered as `BRIDGE_OR_SEMANTIC_BOUNDARY`.

AR-6 declares `doc_type: stewardship_record`, which excludes it from derived
scope under an audit comment saying such records merely point at artifacts and
do not contain or border comparators. AR-6 directly compares the M-H17
comparator, reproduces its result and adjudicates the inference. The
declaration is therefore semantically false even though AR-6 voluntarily
carries the full notice. Retype it as `comparator_type_crosswalk`, register it
as `BRIDGE_OR_SEMANTIC_BOUNDARY`, and delete the exclusion rationale.

AR-6 also calls both constructors exact. The M-H17 fixture is exact rational;
`ghost_parity_krein.py` uses NumPy `eigh/eigvalsh`, a `1e-3` sector tolerance
and `1e-9` residual/sign thresholds. Retype the evidence as a static
constructor crosswalk plus exact rational M-H17 replay and numerical
generation replay. **Required repair; the scientific conclusion is unchanged.**

### Lens 16 — falsification, novelty and claim ceiling

TW-1 rejects wrong square sign, group-lift/volume identification,
central-sign/component confusion, wrong stabilizer, half deletion and family
selection. TW-2 rejects the `1/3` coefficient, omitted correction,
`Pi_4=Pi_14`, null exactness, direct rolled identity and half deletion. The
new content is bounded: TW-1 closes the previously explicit vector-to-spin
adapter; TW-2 adds the four-dimensional cubic identity, causal rank table and
base compression; AR-6 closes a stale numerical-overlap question by type.
None licenses an action, selected background, total current, analytic domain,
physical quotient, positive pairing, family selector, emergent-chirality
mechanism or quantum-superposition theorem. **Survives.**

## Required repairs

1. **Restore routing coverage.** Register TW-1 and TW-2 as
   `BRIDGE_OR_SEMANTIC_BOUNDARY`.
2. **Retype AR-6 honestly.** Replace `doc_type: stewardship_record` with
   `doc_type: comparator_type_crosswalk`, remove the audit-exclusion defense,
   and register AR-6 as `BRIDGE_OR_SEMANTIC_BOUNDARY`.
3. **Correct AR-6's evidence grade.** Call the M-H17 replay exact rational and
   the generation replay numerical; rename “Exact constructor inventory” to
   “Constructor inventory.” Narrow the repository-map negative to the cited
   or current constructor inventory unless an exhaustive search receipt is
   added.
4. **Narrow TW-2's standard-theorem attribution.** Either construct the
   standard `K*R_2K` symbol or cite a theorem proving the declared `Q`-cubic
   grammar exhaustive. Otherwise call `N_3` an exact normalized cubic complex
   symbol consistent with, rather than a normalization extracted from, the
   standard detour line.
5. **Expose the algebraic dual identification.** State that the displayed
   `Tw->Tw*` matrix uses the fibre metric/pairing to identify the dual target
   with a matrix copy; retain the ban on analytic-adjoint/domain promotion.

No repair changes the TW-1 square, the TW-2 `1/4` identity/ranks/compression,
the AR-6 type mismatch, any source claim, canon verdict or physics posture.

## Independent executable replay

The review ran the following from the GU repository root.

```text
TW-1 new probe                         85/85 PASS
TW-1 selftest mutants                   8/8 rejected
TW-2 new probe                        128/128 PASS
TW-2 selftest                         132/132 PASS

twistor/BV seven-gate                  47/47 PASS
J10/BV/Green                          112/112 PASS
reverse-J census                       43/43 PASS
moving-J cone                          66/66 PASS
twistor carrier/Weyl                   54/54 PASS
local-twistor/Bach detour              50/50 PASS

M-H17 rational comparator              10/10 PASS
generation-sector inertia               numerical (+96,-96,0) in K95/K77/Euclidean controls
AR-1 corrected ledger                  59/59 PASS
```

The old
`eric_curt_wave3d_b2c2b_super_ig_rs_tangent_noether_probe.py` retains one
stale textual failure, “the campaign advances only the guided B2C2B pointer,”
after later campaign progression. Its n=14 intertwiner is prior art, not a new
TW-2 receipt, and the current exact local-twistor and TW-2 probes independently
establish every mathematical identity used here. The stale campaign assertion
must not be misreported as an algebraic failure or silently counted green.

Before the required registry/doc-type repair:

```text
source_native_comparator_routing_audit.py
  FAIL 4/5 test groups
  113 discovered / 102 registered / 11 unclassified
  new gaps: TW-1 and TW-2
  hidden by exclusion: AR-6
```

The post-repair acceptance condition is `5/5 PASS` with the historical
unclassified gap restored to at most nine and all three new paths registered.

## Survive / kill ledger

| proposition | hostile disposition |
|---|---|
| selected-component `S_J^2=-J10` | **SURVIVES EXACTLY** |
| opposite component has the same fixed-volume sign | **KILLED** |
| changing central-sign lift changes the square | **KILLED** |
| rolled `S_hat_J^2=-Jhat` | **SURVIVES EXACTLY** |
| TW-1 selects `J_N` or a physical imaginary unit | **KILLED / NEVER ESTABLISHED** |
| `N_3=Q^3-(1/4)qQ` in the declared grammar | **SURVIVES EXACTLY** |
| standard GSS normalization is directly reproduced | **NOT YET ESTABLISHED; NARROW WORDING** |
| non-null symbol sequence exactness | **SURVIVES EXACTLY** |
| null symbol classes are physical cohomology | **KILLED / NEVER ESTABLISHED** |
| `Pi_4=Pi_14` | **KILLED** |
| rolled compression contains `Q,T,-T#` | **SURVIVES EXACTLY** |
| rolled first-order operator equals cubic detour map | **KILLED BY TYPE AND ORDER** |
| the two `(96,96,0)` results are one object | **KILLED BY CONSTRUCTOR TYPE** |
| AR-6's generation replay is exact | **KILLED; NUMERICAL** |
| action/background/domain/physical state built here | **OFF LIMITS / ABSENT** |

## Reprioritization

Relative to the four paths named by the channel:

| path | priority after review | reason |
|---|---:|---|
| superposition-twistor | **1, one bounded swing only** | owns two new exact conditional bridges, but both now meet explicit unowned-owner fences |
| H210 | **paused** | CB-8 reopen requires an independent source-native result implying `L_q|_Z=0`; do not manufacture it |
| source-native neutrino | **paused** | reopen requires both zero-order/coindex descent and observed-line incidence; do not substitute `126` |
| extra vectors | **retired from this channel** | remaining source-native attachment belongs to action/observation owners outside this conditional lane |

TW-1 and TW-2 themselves should be banked, not iterated. The strongest
admissible next conditional swing is a finite `TW-3` coherence/gluing gate
using only already-owned objects:

1. tensor the TW-2 `Pi_4` detour symbol with the existing normal half-spin
   factors;
2. test whether TW-1's selected-component Spin lift and opposite-component
   orientation reversal intertwine `T,N_3,T#` and the compressed rolled
   `Q,T,-T#` subblocks;
3. carry both ambient K77 halves and all four corners;
4. state the algebraic dual/pairing convention explicitly; and
5. stop before any global `J_N` section, `Tw^2` field, action, total
   connection/current, background, domain, quotient or positivity claim.

If this is only formal tensor-product commutation, bank it immediately and
deprioritize the entire twistor path. A nontrivial component/orientation or
dual-target obstruction would be the only fertile result. Constructing the
missing owners is not an admissible successor in this channel.
