---
title: "Hostile review: D7 first deeper stratum ownership gate"
status: complete
reviewed_artifact: explorations/conditional-build/selected-k86-rsap-d7-first-deeper-stratum-ownership-gate-2026-08-15.md
created: "2026-08-15"
verdict: PASS_WITH_EXACT_THREE_WAY_CENSUS_AND_TYPE_MISSING_SUCCESSOR_CEILING
---

# Hostile review

## “First” means nearest attack

It does not. The `A3+A1` row is first only under target-rank order: it loses
two target directions and one allowed map direction from the completed `A3`
origin. The source/action does not state that its deformation follows the
smallest-rank-loss branch. The artifact labels `A3+A1` nearest, not selected.

## Incomplete root-system attack

The probe constructs all `84` `D7` roots, fixes the twelve-root `A3`, and
tests the rational span after adding each of the remaining `72` roots. The
subsystem root counts are exactly `14`, `20`, and `24`, with multiplicities
`12`, `48`, and `12`. These are `A3+A1`, `A4`, and `D4`; the census is
exhaustive for one-root rank-four extensions of the fixed abstract `A3`.

## Complex type determines real stratum attack

It does not. Root-subsystem type fixes the complex centralizer root count, not
the ambient real-form support, real centralizer component, or selected
coadjoint degeneration. K81 already proves that the needed ambient support
maps are absent. This packet does not reopen or bypass that gate.

## Ceiling equals construction attack

The values `84`, `81`, and `79` come only from
`2 rank(dJ) <= 98 + rank(pi)`. They are upper bounds. The registry explicitly
marks each as not achieved by a construction. The displayed `A3+A1` product
is dimensional revival arithmetic, not a chart or attachment.

## Local-origin equals ambient-origin attack

The local `A3` origin sets the principal factor moment to zero while retaining
the abstract `S72` leaf. It does not make the ambient `so(7,7)` covector zero
and does not specify how that leaf degenerates. Therefore local closure does
not imply an ambient successor.

## Comparator relapse attack

Ordinary Higgs, ordinary family-index and net-chirality assumptions do not
choose an ambient root or support map. The artifact correctly routes them out
of this classical moment-map ownership gate.

## Verdict

Accept the complete three-way abstract census, target ranks and pointwise
ceilings. Preserve the selected ambient successor as repository-scoped
`TYPE_MISSING`; do not attach the conditional `A3+A1` template without the
named revival datum. The zero-charge local gate may proceed independently,
but global gluing remains open.
