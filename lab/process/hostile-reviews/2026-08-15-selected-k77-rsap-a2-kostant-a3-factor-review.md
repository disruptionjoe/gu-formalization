---
title: "Hostile review: A2 Kostant bridge and split-A3 factor"
status: complete
doc_type: hostile_review
created: "2026-08-15"
target: explorations/conditional-build/selected-k77-rsap-a2-kostant-a3-factor-2026-08-15.md
verdict: PASS_WITH_A2_A3_TRANSITION_REQUIRED
---

# Hostile review: `A2` Kostant bridge and split-`A3` factor

## Strongest overclaim

The result is a complete *regular local atlas*, not a singular or global
single-chart construction. Kostant transversality holds while the centralizer
dimension remains the rank. It does not cross subregular or zero strata.

Likewise the `A3` result constructs the split principal factor, not its gluing
to `A2`, every real form, or a global RSAP.

## Strongest contrary construction or mistyping

Using `f^2` as the second mixed slice direction would leave `su(2,1)`. The
correct real direction is `i f^2`; the probe checks the defining-form equation
and real rank eight. For split `A3`, an arbitrary positive definite
self-adjoint model would miss real nilpotents. The signature `(2,2)` reverse
form includes the size-four nilpotent and the complete regular real-Jordan
census.

## Primitive and moment challenge

The proof does not take a singular limit of Cartan angles. It replaces them
with a smooth orbit-times-slice chart and uses the exact cotangent lift of the
overlap diffeomorphism. That preserves the tautological one-form, hence the
symplectic form; equivariance intertwines the moment map. This is classical
local gluing and licenses no prequantum or physical conclusion.

## Weakest reproducibility seam

The exact probe certifies the triples, real-form equations, orbit/slice ranks,
centralizer dimensions, cotangent pairing, `A3` signature, nilpotent control
and all dimension/rank schedules. The general regular-Jordan symmetrizer
census is mathematical reasoning recorded in the artifact rather than an
enumeration of continuously many matrices. The next overlap map is still
absent and explicitly gated.

## Verdict

`PASS_WITH_A2_A3_TRANSITION_REQUIRED`. The old `A2` nonsemisimple
`TYPE-MISSING` gate closes locally. The split `A3` factor constructs, but
higher-root gluing remains open.
