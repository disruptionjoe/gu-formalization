---
title: "Hostile review — selected K77 zero-seed H640 action-closure controls"
artifact_type: hostile_review
created: 2026-08-11
verdict: SURVIVES_SCOPED__QQ_ZERO_SEED_H640_AND_TWO_PRIME_CONTROLS__PRINCIPAL_ACTION_MODULE_NOT_PHYSICAL_CARRIER__FULL_BV_KT_REQUIRED
canon_verdict_change: none
---

# Three-charge hostile review

## Review core

- **Layer-0 semantics:** the minimal invariant principal-action module is not
  a source-selected carrier, BV cohomology, reduced phase space, or global
  analytic domain.
- **Prior art:** v0.176 owns the natural/random sectors; v0.181 owns the first
  supplied-seed `H640`. This wave asks the adjacent zero-seed and discriminator
  question and is not a novelty claim for the matrices or named sectors.
- **Analytic:** the load-bearing zero-seed rank is recomputed over `QQ`; the
  natural/random controls agree over two exact finite fields. No local rank is
  promoted to a closed-domain theorem.
- **Symplectic:** invariance under spatial evolution is not coisotropic
  reduction, a BFV quotient, or a nonzero covariant-phase-space class.

## Charge 1 — where could the summary outrun the artifact?

“The source/action selects `H640`” would outrun the artifact. The source owns
the zero-form ingredient; the repository's conditional principal operator
generates the module. Lower-order `varpi`, the independent barred variables,
antifields, the Koszul--Tate differential, observation, and the global domain
are untested. The scoped summary says only that a supplied `W` or mirror seed
is unnecessary at principal grade.

“Exact real-K77 theorem” would also overreach if based only on modular ranks.
The load-bearing zero-seed computation therefore has an independent exact
`QQ` certificate. The 640/832/random comparator relations remain two-prime
exact evidence and are not used to settle a global real-form fork.

## Charge 2 — is rigor defending a superseded or mistyped object?

The obvious trap fired during review: equal rank 1280 was initially mistaken
for equality of the old-640 and 832 generated modules. The joined-rank check
rejects that reading. They are distinct, intersect exactly in `H640`, and span
rank 1920. The final artifact preserves the relation rather than a rank-only
summary.

The prior one-form rank-640 sector is not `H640`: the former is entirely in
`Omega^1(S)`, while the latter projects as `512+128` across
`Omega^1(S)+Omega^0(S)`. The random planes are planted controls and are not
candidate physical projectors. None of these objects is defended as the
physical carrier.

## Charge 3 — if this stands, what else changes?

### Dissolved

- The need to supply `W`, mirror, or their union merely to generate `H640`
  dissolves at conditional principal grade.
- The queued “control H640 before choosing H640 versus 1920” gate is complete.

### Survives

- W-versus-mirror physical selection, chirality, horn/`p`, P3, index, count,
  null BFV, and global analytic closure all survive.
- Selected Spin, two `U(32,32)` halves, and full `U(64,64)` remain distinct.
- The full 1,920 carrier remains a mandatory hostile control.

### Needs recheck

- preservation of `H640` by every lower-order `varpi` and reality block;
- closure under the barred/dual and antifield sectors;
- the full BV/Koszul--Tate differential, observation map, boundary complex,
  and closed domain;
- whether physical cohomology on `H640` differs from the full-1,920 control.

## Verdict

`SURVIVES_SCOPED__QQ_ZERO_SEED_H640_AND_TWO_PRIME_CONTROLS__PRINCIPAL_ACTION_MODULE_NOT_PHYSICAL_CARRIER__FULL_BV_KT_REQUIRED`.

Migrate distance/evidence only. Do not move a verdict, datum, quotient, canon
claim, or public posture.
