# Hostile review: K77 Wave 2 stabilized mixed cross maps

Date: 2026-08-04
Disposition: **PASS AFTER RETYPING AND SCOPE FENCES**

## Charge 1: find where the summary outruns the artifact

The exciting reading was “the action constructs `U:B->F` and `V:F->B`.” The
artifact establishes the raw mixed Hessian blocks
`U_raw:B->F!` and `V_raw:F->B!`. Calling them field-to-field maps silently
chooses dual-to-field primalizers that have not been globally built.

The actual K77 receipt is also deliberately narrow: one frozen one-form sector,
the tautological-`q` variation and one actual connection direction. Rank two
there proves nonvacuity and coefficient sensitivity for that witness; it does
not prove the full moving sixteen-cell Hessian, global descent, Green domain or
physical source map.

## Charge 2: find where the lane defends a stale or superseded object

The stale object was the preceding formal totalization
`Delta=[[D,V],[U,F]]`, treated as though equation `10.10` already had that
endomorphism topology. Visual inspection shows instead a rectangular symmetry
to fields to density-dual equations complex. The old formula remains a useful
grammar only after primalization.

A second stale identification equated the internal grading of the unreleased
two-connection square with the Bose--Fermi grading. Entrywise target matching
is rejected until a typed comparison functor is constructed.

## Specialist hostile checks

### Variational/Helmholtz review

Pass. The finite common-action Hessian is exactly symmetric and both raw mixed
blocks are nonzero. The K77 bilinear checks pass coefficientwise. The review
forbids converting sensitivity rank two into coefficient-selection rank.

### Representation and operator review

Pass after adding explicit `B`, `F`, `B!`, and `F!` types. Two exact
primalizer choices change both composites, demonstrating that raw reciprocity
does not select a field endomorphism. A full moving Krein/Hodge/density owner
and Green identity remain required.

### Source/provenance review

Pass. The author draft confirms the mixed rectangular topology and carries an
explicit instability caveat. The older cell audit is cited. Portal confirms
the intended crossed-path architecture but not the missing primalizers; the
later two-connection construction remains source-bounded and unreleased.

## Planted controls

All seven plants fired correctly:

- identity primalizers were not called source-selected;
- a visible mixed cell was not promoted to a stabilized global map;
- one reciprocal fixture was not promoted to a full-carrier proof;
- raw dual-valued blocks were not composed;
- the two-connection grading was not relabeled Bose--Fermi;
- no external datum manufactured missing analytic geometry;
- no observed Higgs, Yukawa, Dirac, particle or domain claim was emitted.

## Final hostile verdict

The packet may claim construction of action-derived **raw** mixed Hessian
blocks, exact finite and frozen-K77 reciprocity, K77 sensitivity rank two, and
the Layer-0 kill of direct entrywise target matching.

It may not claim global field-to-field cross maps, a selected trace-`q`
coefficient, a source-exact two-connection comparison, full sixteen-cell
closure, observed physics, use of P1/P2/P3 or permission to open Wave 3.

Final rerun: **46/46 PASS**.
