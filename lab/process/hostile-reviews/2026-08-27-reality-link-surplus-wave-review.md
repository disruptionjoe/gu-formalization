---
title: "Hostile review: reality, link and Lambda5 surplus wave"
status: process
operational_state: complete
doc_type: hostile_review
created: "2026-08-27"
typed_objects: EXEMPT-PROSE-ONLY
target_claim: NONE-NOT-A-KILL
target_claim_note: "Reviews three internal/comparator result ceilings; it excludes no source claim."
---

# Hostile review: reality, link and surplus wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

The strongest signature overclaim would be “the orthogonal horn forces the
sign.” It does not. The finite disconnected symmetry reduces `R+R` to diagonal
`R`, but the surviving real involution still has two signs and no physical
observable or action chooses one. The exact commutants are carrier typing, not
an ambient-signature decision or physical stabilizer theorem.

The strongest topology overclaim would identify the global link with
`RP3 x S6`. Triviality is proved only on the fixed `RP3` spine for the filed
normal model. The global extension is conditional and, even under it, the new
`w2(TX)` and `7p1(TX)` formulas are necessary obstruction tests rather than a
constructed framing. Vanishing does not prove the remaining KO/torsion data or
produce a Pontryagin--Thom class.

The strongest comparator overclaim would count one dual `126` contraction as a
native field or Majorana mass. Q6 already forbids that promotion: the raw
degree-five carrier has the wrong connection adjoint class, the real halves are
conjugate, and the source-owned VEV/mass stages are open. Calling the surplus
negative would be equally unsound because maps and owner gaps are not scalar
parameters; `SURPLUS_UNCOMPUTABLE_PREPARAMETER` is the maximum licensed result.

The weakest reproducibility seam is that the real-algebra probe uses a minimal
faithful division-algebra model and the global Pontryagin formula records the
free/rational degree-four part. Both ceilings are explicit. Independent future
work should reconstruct the actual physical quotient commutant and evaluate
the characteristic classes on a frozen global `TX` packet before any stronger
claim. All three clean baselines pass before twelve planted mutations are run;
all twelve are caught.
