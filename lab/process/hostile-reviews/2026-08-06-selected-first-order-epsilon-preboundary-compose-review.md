---
artifact_type: hostile_review
created: 2026-08-06
target: selected-first-order-epsilon-preboundary-compose-2026-08-06.md
status: PASS_AFTER_SCOPE_REPAIR
---

# Hostile review: selected first-order epsilon / preboundary Compose

## Charge A — where the summary outruns the artifact

The first draft said the “selected preboundary class closes.” That was too
broad. What closes is the compact-core Green identity under the explicit
Dirichlet trace condition on epsilon gauge data. The unrestricted boundary
flux remains live, and neither artifact constructs a physical BFV phase
space. The status and ledger were repaired accordingly.

The second overreach would have called `comm/symi/symi` Weinstein's selected
Shiab. The source explicitly lacks the historical selector. The row is
selected only inside the repository's eight displayed products by the
principal Bianchi/nonzero criterion.

## Charge B — where a fence defends a superseded or mistyped object

Ledger v0.24 still listed primitive epsilon construction as open. The exact
2026-08-05 epsilon/Green artifact already built it for all eight product rows.
Since the later selected row lies in that enumerated domain, repeating the
construction would defend a superseded queue burden. The queue now asks only
for moving geometry, action/observation composition and global unrestricted
reduction.

## Mandatory symplectic-geometry review

The presymplectic distinction is load-bearing:

- Dirichlet trace makes the boundary flux zero by domain choice.
- Unrestricted trace retains a preboundary one-form.
- A retained one-form need not descend to a nonzero BFV charge after quotient.
- Neither outcome supplies a physical transition or increments the quotient
  count.

The report therefore says “compact Dirichlet Green closure,” never “physical
preboundary closure.”

## Variational/PDE review

The primitive Euler row includes both `D_B^!(E_B-E_T)` and the moving-Shiab
adjoint. Omitting either changes the equation. The formal
`H10 intersect H1_0 -> H9` graph supplies the stated integration by parts but
does not establish global noncompact `Y14` evolution, constraint propagation,
maximal dissipativity, self-adjointness or positivity.

## Exactness review

The composition reads immutable exact registries rather than spending a wave
replaying unchanged K77 calculations. A new exact rational interval model
independently reproduces the bulk-plus-boundary identity, zero Dirichlet flux,
and live unrestricted flux `11`.

## Disposition

Passed after narrowing the status, source attribution and quotient language.
Five ledger distances move; every verdict, residue item, revival trigger,
datum and quotient count is frozen.
