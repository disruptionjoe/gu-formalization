---
title: "Hostile review: selected K77 I2B trace-Hq normal-contact correction"
date: 2026-08-12
verdict: V0220_REAL_FORM_DISPOSITION_REFUTED__SCOPED_TRACE_HQ_RANK120_CONTACT_SURVIVES
---

# Hostile review

## Pre-wave and required lenses

- Fork assumed: none; the embedded real form is the object under review.
- Search space: 16 response coordinates, 32 supports and 10 normals; decidable
  wholesale at this grade.
- New unowned object: none.
- What dies on success: only v0.220's rank-80/cokernel-80 disposition and its
  scalar exclusion.

Lenses used: **Layer-0 semantics**, **prior art**, **Krein/real analytic**,
**representation theory**, **principal-bundle geometry**, and **Symplectic**
geometry.

## Charge 1 — where the summary outran the artifact

V0.220 called a grade-only `B`-skew bank the source real `u(64,64)` tangent.
V0.194 had already derived the source-sized trace-`H_q` Hermitian structure,
whose exact phases differ. The old rank was therefore exact for the wrong
embedded real form.

The correction itself would overreach if it called the scalar dynamically
selected. Rank-120 proves pointwise availability only. It proves neither a
stationary solution, global reduction, common analytic domain nor physical
phase-space class.

## Charge 2 — where rigor defended a mistyped object

The B-skew, observer-`H_u` and trace-`H_q` banks have ranks `8`, `10`, and `12`.
Equal abstract dimensions or the label `u(p,q)` do not license substitution
between their concrete embeddings. The source-owned carrier statement must
also remain two `C^(32,32)` halves; its block stabilizer and full parent are
different objects.

A second homonym is trapped: generation-hinge `H^- = X(S^+)` is not the
Hermitian form `H_q` restricted to a negative half.

## Charge 3 — if the result stands

- **Dissolved:** v0.220 rank `80/160`, rank-80 cokernel and scalar-exclusion
  disposition.
- **Dissolved:** identification of the remaining contact with the recurring
  rank-128 fermionic defect.
- **Survives:** source ownership of the normal-contact operator, v0.219's
  restricted-data non-identifiability theorem and contact discriminant.
- **Survives:** the two `C^(32,32)` carrier halves and their distinct block
  subgroup/full-parent typing.
- **Needs recheck:** coupled Euler normal prolongation on a genuine stationary
  trace-`H_q` background.
- **Needs recheck:** closure under the complete trace-q stabilizer and global
  associated-bundle descent.
- **Needs recheck:** `D_varpi H_q=0`, Green/BFV/domain and physical spectrum.

## Contrary tests

The old B-skew bank remains a firing control at rank eight. Observer `H_u`
gives the distinct rank ten. `kappa=0` deletes the contact. The synthetic
`1+3` module action disagrees with the derived trivial `SO(3)` action, and the
16-response bank fails closure under the full split stabilizer. These prevent
the corrected finite rank from becoming a global representation theorem.

## Symplectic and analytic disposition

No off-shell normal jet is promoted to covariant phase space. The prolonged
Euler solution set, gauge quotient, boundary form and common domain must be
built first. Finite exact rank supplies no positivity, hyperbolicity or
quantum measure.

## Verdict

`V0220_REAL_FORM_DISPOSITION_REFUTED__SCOPED_TRACE_HQ_RANK120_CONTACT_SURVIVES`.

No claim status, canon verdict or public posture changes.
