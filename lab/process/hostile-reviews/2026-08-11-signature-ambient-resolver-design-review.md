# Hostile review: SIGNATURE-AMBIENT resolver design packet

Target:
`explorations/signature-ambient-relative-sign-resolver-design-packet-2026-08-11.md`
(Joe-directed design run). Reviewed same-run under the standing three
charges. Verdict:
`SURVIVES_WITH_ONE_DEMOTION_APPLIED_IN_DRAFTING__RESOLUTION_REMAINS_THE_WAVE'S_TO_EXECUTE`.

## Charge 1 — where the summary outruns the artifact

- **Fired during drafting, recorded here.** The packet's first draft claimed
  the achievable-set asymmetry lets a signature label self-type its writing
  convention, which would have resolved the draft's convention outright.
  That claim is false as stated: with a free overall sign on the fiber form
  the portrait is mirror-symmetric (now certified as T6), and the label
  argument was demoted to corroboration under positive normalization before
  filing. The load-bearing quantity was moved to the uniform-sum balance,
  which is invariant under exactly the relabelings that killed the claim.
  This is the same failure class that killed M-H9 — an invariance the
  designer's first pass missed — caught this time by the packet's own
  review stage.
- **The residual strong claim.** Outcome (a)'s resolution rests on the
  uniform-reading presumption for one printed equation line (12.19). The
  packet grades this SCOPED with explicit upgrade (C3 formula) and defeat
  (outcome b/c) conditions, and the balance arithmetic itself is certified
  (T4). The presumption must not be reported as a certificate; the outcome
  table's grades are the licensed strengths.
- **"Exactly the registry's two horns."** True as unordered pairs on
  fourteen dimensions (T6), but only because the registry lists (9,5) and
  (7,7); if the registry ever admits a third horn the balance pin no longer
  partitions outcomes. Scope-bound to the current registry state.

## Charge 2 — where rigor defends a superseded or mistyped object

- **Layer-0 on "ambient signature."** The route resolves the induced-metric
  object only; `REAL-CLIFFORD-FORM` (the algebra the source computes in) is
  registry-marked distinct, and the packet routes its pressure note through
  the outcome table instead of silently discharging it. No conflation
  found.
- **The fiber-form identification.** The equivalence "draft `N_ג` metric =
  narrated raw/traceless/trace-flipped construction" is a source-narration
  bridge, not a displayed identity; the packet assigns it to C3 with grade
  consequences. Correctly typed.
- **The scope note on the 2026-08-08 mirror artifact.** The packet claims
  notation-mirror and global form-negation are indistinguishable from
  signature labels alone, gently narrowing that artifact's "notation is the
  only explanation left." Checked: the artifact's evenness control rules
  out base-sign changes, not global negation composed with convention
  flip; the narrowing is real but does not touch the artifact's operative
  conclusions, because every quantity this packet uses is invariant across
  the ambiguity. The scope note is fair and correctly non-retractive.
- **Trace-regime bit.** "Both sources' labels land on trace-flipped
  members under any reading": transcript `(4,6)`/draft `(6,4)` are
  balance-2 ten-dimensional forms, and the family's balance-2 members are
  exactly the trace-flipped side under either overall sign (T1 + T6).
  Sound.

## Charge 3 — what else must change if this stands

- `dissolved`: nothing by this packet itself — it changes no row, verdict,
  residue, fork, canon, or posture by construction.
- `survives`: the mirror artifact's operative conclusions (with the scope
  note); dc-h1; the iceberg reinspection's `SOURCE-UNTYPED` typing; the
  eleven-file red gate as a live obligation routed to the wave.
- `needs-recheck` (all conditional on outcome (a) executing):
  - which K77 sign-sensitive certificates consumed a same-sign composite
    assumption — the Wave C audit list must be built from the certificates
    themselves, not from memory;
  - the `REAL-CLIFFORD-FORM` pressure-note dissolution wording — the
    settlement's `settled_how` cites source-typed arithmetic, and the wave
    should verify the dissolution does not overwrite that provenance;
  - whether any downstream surface treated the transcript's spoken
    horizontal as authoritative over the draft display — those citations
    inherit the transcript-error typing.

## Required lenses

- Layer-0: applied via the packet's object table (four rows) and re-run by
  the wave as a precondition.
- Prior art / novelty: in-repo prior art enumerated and cited; the
  packet's novelty claims are relative ("new relative to those"); the
  classical DeWitt-supermetric literature is flagged for the wave's
  independent citation check, not asserted from memory into any
  disposition.
- Analytic lens (signature/positivity claims): all signature claims are
  finite exact linear algebra, certified over both conventions with
  planted controls; no analytic-domain claim is made anywhere in the
  packet.
- Symplectic/Krein lens: engaged only in Wave C's audit trigger (Krein
  inertias listed among sign-sensitive certificates); no Krein claim is
  made here.

No new trap filed: the failure class this packet self-caught
(absolute-vs-invariant confusion under relabeling freedom) is already the
recorded lesson of M-H9 and the declared-base route; the packet's
contribution is the invariant restatement, not a new hazard.
