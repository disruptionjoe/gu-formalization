---
artifact_type: hostile_review
created: 2026-08-05
target: PW2F-R2B2B2I2-AFFINE-FIRST-MOVING-FIRST-SIZE3-FULL-EVALUATOR
verdict: PASS_AT_AFFINE_LAYER_AND_EXACT_3_OF_380_ONLY
reviewers:
  - affine differential and exact-action lens
  - finite-group orbit and stabilizer lens
  - moving Hodge and mixed-jet lens
  - coverage, source fidelity, and Layer-0 lens
---

# R2B2B2I2 affine and first-size-three hostile review

## Final verdict

`PASS_AT_AFFINE_LAYER_AND_EXACT_3_OF_380_ONLY`.

The packet closes the first-moving affine layer and one complete non-fixed
orbit. It advances durable full-evaluator coverage from `2/380` to `3/380`,
not to universal coverage, and cannot promote the engine or either bank.

## Affine differential lens

All ten owners are tested at a zero origin plus four coordinate conormals.
Both preregistered dense controls reconstruct only after the affine-origin
coefficient `1-sum(xi)` is included; the prior naive linear model is therefore
not smuggled back in. Both S3 generators pass all 100 edges through geometry,
eight moving Shiab families, residual, moving primalizer, and action
derivative. Trace motion is live `50/50`, action derivative `20/50`, and the
order-three old-forward-lift plant rejects.

Lens verdict: `PASS_NONVACUOUS_AFFINE`.

## Orbit and mixed-jet lens

The representative is selected lexicographically from the resolver's 115
size-three orbits before evaluator output is read. Its three labels are the
owner pairs `(0,0)`, `(4,4)`, and `(7,7)` at quartic point `(0,0,0,4)`.
Both generators close, all six directed edges pass, and five are non-self.
Every full-evaluator layer passes `24/24` slots. Mixed residual, primalizer,
action, and moving-Hodge product-rule controls are live `3/3`; exact mixed
actions are `-727/144`, `-727/144`, and `107/9`.

Lens verdict: `PASS_COMPLETE_SELECTED_ORBIT`.

## Coverage and reproducibility lens

The probe depends only on a byte-pinned durable fixed-stratum probe and
registry, and replays under pinned SymPy `1.14.0` plus NumPy `2.5.1`. The
1,368 raw transport equalities have zero fitted parameters, but their
independent rank is not established and no surplus is claimed.

The exact durable boundary is `3/380`. Calling it universal would inflate
coverage by more than 126 times. The other `377` representatives and dense
universal heldouts remain open.

Lens verdict: `PASS_AT_3_OF_380_ONLY`.

## Source and Layer-0 lens

The finite reduction is repository-derived and source-silent. The active
`(9,5)` reconstruction and public `(7,7)` presentation remain distinct.
Affine first motion is not mixed second-order universal coverage, and one
orbit is not a bank, Green operator, domain, observation, or physics result.

Lens verdict: `PASS`.

## Boundary

The remaining 377 representatives, both complete banks, Green/Helmholtz,
domain, observation, and physics remain open. The 1,925-cell fallback stays
live. P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; `TG-1 AND TG-2 AND TG-3` remains
`NOT_PROMOTED`. No canon, protected status, public posture, publication,
datum, or physics state changes.
