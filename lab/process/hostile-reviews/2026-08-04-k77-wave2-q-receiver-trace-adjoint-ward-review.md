---
title: "Hostile review: K77 trace-q receiver ownership and adjoint/Ward selection"
status: complete
created: 2026-08-04
result: "REPAIRED_PARTIAL__TRACE_Q_OWNS_RECEIVER__WARD_LEAVES_ONE_COEFFICIENT"
---

# Hostile review

## Charge

Attack the swing in both directions:

1. find every sentence where the summary outruns the instantiated object; and
2. find whether an earlier fence is defending a superseded free-`q` problem.

No exact metric-fibre or principal-symbol result may promote observation,
physics, a physical domain, effective chirality, particles, generations,
mass, seesaw, or P1/P2/P3 use.

## 1. Primary-source professor

**Question:** Did Weinstein explicitly say to insert the trace line into D916?

**Finding:** No.  The 2025 Weinstein--Jaimungal source confirms trace-reversed
Frobenius and its `Spin(6) x Spin(4)` purpose.  Into the Impossible calls one
metric-space direction distinguished.  The rendered draft does not put that
direction in equation 9.16.  Final grade:

```text
TRACE REVERSAL / DISTINGUISHED DIRECTION: SOURCE-CONFIRMS
TRACE-q D916 INSERTION:                  SOURCE-SILENT
```

## 2. Metric-bundle geometer

**Question:** Is `t_g=g` really intrinsic, or does it depend on choosing zero
or an affine trivialization in the metric fibre?

**Finding:** The fibre of fixed-signature metrics is an open subset of the
vector space `Sym2 T-star_x X`, so its tangent is canonically that vector
space.  Positive radial scaling remains in the same component, and
`d/ds|0 exp(s)g=g`.  Under base change, `g` and `t_g` both transform as
`L^T g L`.  The construction is natural and nowhere zero.

## 3. Exactness referee

**Question:** Did the artifact call a numerical check exact?

**Finding:** Yes in the first draft.  The arbitrary-frame DeWitt check used
floating inversion, and positive definiteness used floating eigenvalues.
Both were replaced: the final probe uses an integral unimodular frame with an
exact integer inverse and Sylvester minors computed by an exact recursive
determinant.  This is material repair one.

## 4. Layer-0 Clifford referee

**Question:** Is the predecessor's “odd covector” the same type as the vertical
trace vector?

**Finding:** Not literally.  Clifford multiplication takes a vector in the
chimeric quadratic bundle.  The final report corrects `q` to a section of
`C=V plus H-star`; the chimeric metric lowers it afterward.  Because the trace
vector lies in `V`, the type now closes conditionally.

## 5. Krein-adjoint referee

**Question:** Was only the 128-spinor transpose tested?

**Finding:** No.  The final probe includes all 14 output and 14 input form
indices with pairing `diag(eta_a B)`.  It proves left/right exchange block by
block and independently finds the nonzero `dq` lower-order terms.  It still
does not assemble the global Hodge, density, moving pairing, domain, or all
sixteen source blocks; the report keeps those open.

## 6. Connection-current referee

**Question:** Was a one-form coefficient variation mislabeled a gauge
connection current?

**Finding:** Yes in the first draft.  It was replaced by an actual even
spin-connection generator `gamma_0 gamma_1` carried by an ad-valued one-form
direction.  The final exact witness is one nonzero direction plus the general
linear formula, not the full connection-current census.  This is material
repair two.

## 7. Ward/BV referee

**Question:** Does covariance select a left/right coefficient?

**Finding:** No.  Left and right both intertwine under the same moving even
transition.  The exact test evaluates several linear combinations and gives
selection rank zero.  The `q` current is composite: it pulls back through
`D_g q[delta g]=delta g/2` into the metric/soldering equation, with the other
moving geometric derivatives still owed.

## 8. Source-bracket homonym referee

**Question:** Does the draft's commutator or `i`-anticommutator freedom select
the Clifford commutator or anticommutator found here?

**Finding:** No typed identification exists.  The source operation is in the
coefficient/Lie-algebra factor; the new operation orders `gamma(q)` and the
form-spinor contraction.  The initial prose treated the verbal match as
support and was corrected.  This is material repair three.

## 9. Superseded-fence reviewer

**Question:** Is the prior `-14` constraint-surplus fence still defending a
problem already solved elsewhere in the repo?

**Finding:** Yes on the conditional trace branch.  RB4 already constructed the
canonical negative trace line but did not connect it to the later `q` receiver.
The present exact type map makes that connection.  The free projective `q`
parameters fall from 13 to zero.  One projective placement coefficient remains,
so the new surplus is `-1`, not a promotion to confirmation.

## 10. Physics and provenance referee

**Question:** Did the construction become a particle, generation, chiral
family, mass, or seesaw result?

**Finding:** No.  It is an ambient operator/action construction.  P1/P2/P3 are
unused, Wave 3 remains closed, Curt remains formally separate inside the Eric
lane, and the conjunctive third-lane gate remains unpromoted.

## Final disposition

```text
REPAIRED_PARTIAL__TRACE_Q_OWNS_RECEIVER__WARD_LEAVES_ONE_COEFFICIENT
```

- Geometry ownership of `q=t_g/2`: exact conditional construction.
- Source attribution of the insertion: `SOURCE-SILENT`.
- Free-`q` parameter cost: retired on this branch.
- Left/right adjoint and `dq` terms: exact.
- One actual connection direction and `q` current: exact and nonzero.
- Ward coefficient selection: rank zero.
- Constraint surplus: improved from `-14` to `-1`.
- Full zero-order reality, moving-pairing action, full-`H` descent, and domain:
  open.
- P1/P2/P3: unused.
- Wave 3: not admitted.
