---
artifact_type: hostile_review
created: 2026-08-12
target_claim: NONE-NOT-A-KILL
verdict: SURVIVES_AFTER_SCOPE_REPAIR__SPLIT_NATIVE_J_EXACT__HERMITIAN_FORM_AND_ACTION_SELECTION_OPEN
---

# Hostile review — split-layer commutant/action parent

## Pre-wave and required lenses

No action parent, Hermitian form, chirality mechanism or ambient-signature
resolver is assumed.  The finite commutant is decidable wholesale; connection
selection and global associated-bundle descent are not.  The review uses
Layer-0 semantics, prior art/source, exact real representation theory,
Krein/Hermitian typing, principal-bundle geometry and the mandatory
symplectic/variational caution.

## Charge 1 — where does the summary outrun the artifact?

The first draft of the correction expected the commutant calculation to
"verify the Hermitian `(32,32)` forms."  It cannot.  Exact invariant real
bilinear spaces on the two halves are `0/0/2/2`, so the same-half Hermitian
form is additional unitary-bundle structure at this grade.  The result proves
complex type and the `32+32` complexification, not Hermitian inertia.

Likewise, `D_varpi J=0` is not equivalent to the two-block reduction.
`D_varpi omega=0` preserves the Weyl blocks; `D_varpi J=0` is a finer split
condition.  Neither follows from action stationarity here.

## Charge 2 — where is rigor defending a mistyped object?

The full-Spin no-`J` theorem was correct but was applied to the wrong layer.
The source's Standard-Model branching occurs only after reduction to
`Spin(1,3) x Spin(6,4)`, whose commutant is larger.  Conversely, one real
64-dimensional half with `J` is `C^32`, not `C^(32,32)`; the latter is the
complexification of the real half with both conjugate eigenspaces retained.

The full `U(64,64)` group, its block subgroup, the external complex scalar
`i`, the real endomorphism `J4`, and ambient chirality `omega` must remain five
typed objects.

## Charge 3 — what else changes?

- **dissolved:** the unscoped claim that complex structure is absent at every
  source-operative layer; the identification of block preservation with
  split-`J` preservation.
- **survives:** the ambient full-Spin commutant theorem; C1/C2 arithmetic; the
  source's full parent and two-half statements; all ledger verdicts, residue,
  quotients and P1/P2/P3.
- **needs-recheck:** the action-owned covariant derivatives of `omega` and
  `J4`; same-half Hermitian form descent; global bundle compatibility; local
  and global anomaly calculations; physical chiral cohomology.

No new path-dependency trap is needed: the repeatable error is a direct
instance of the existing Layer-0 wrong-object and source-locus controls.

## Verdict

`SURVIVES_AFTER_SCOPE_REPAIR__SPLIT_NATIVE_J_EXACT__HERMITIAN_FORM_AND_ACTION_SELECTION_OPEN`.

No canon verdict or public posture changes.  The result is construction-grade
compatibility, not source-action selection.
