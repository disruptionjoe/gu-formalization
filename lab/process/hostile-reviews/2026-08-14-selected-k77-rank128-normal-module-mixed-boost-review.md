---
artifact_type: hostile_result_review
created: 2026-08-14
status: SURVIVES_AS_HELD_SCOPED_MODULE_THEOREM_AND_NEW_CONVENTION_EXPLANATION
result: explorations/conditional-build/selected-k77-rank128-normal-module-mixed-boost-2026-08-14.md
canon_verdict_change: none
---

# Hostile review: rank-128 normal module and mixed boosts

## Verdict

The packet survives as an anti-redo composition of the held v0.228 module
theorem with a new exact characteristic-zero explanation of the `21+24`
convention split. Its scope is narrower than a graph repair or physical
carrier theorem.

## Strongest overclaim

The strongest overclaim would be:

> Because `ker(R_obs)=N* tensor S` is a full `so(6,4)` module, the selected
> H640 graph, physical quotient and fermion dynamics are fully
> `Spin(6,4)`-covariant.

Rejected. The exact carrier inclusion passes `45/45`; the selected graph lift,
its complement and the zero-form-seed trivialization pass only `21/45`. A
configuration module is not a physical BV quotient.

## Strongest contrary interpretation

The strongest contrary interpretation is that the 24 mixed failures prove
only compact-subgroup copies exist. Rejected. That reading applies the vector
action `A` to a covector label. In characteristic zero, the correct dual action
is `-A^T=eta A eta^(-1)`. The canonical inclusion then intertwines all 45
generators, while the selected graph remains non-natural under all 24 mixed
boosts.

This convention correction does not repair the selected graph. It relocates
the failure from the carrier to the chosen splitting, exactly as v0.228
requires.

## Weakest reproducibility seam

The large defect images are replayed on the held `GF(1009)` fixture rather
than rebuilt as dense rational 1,920-dimensional matrices. This is acceptable
because:

1. containment in the 1,280-dimensional observation kernel is exact;
2. the finite-field rank certificate reaches the structural upper bound 1280;
3. the canonical module and sign-convention identities are independently
   checked over `QQ`; and
4. the predecessor ambient theorem independently reproduces all ten
   individual rank-128 leaks over `GF(1009)` and `GF(1013)`.

A future defect in the held fixture would reopen the equality of the filed
defect sum with the module, not the characteristic-zero dual-representation
identity.

## Firing controls

- One repeated rank-128 image has rank 128, not 1280.
- The wrong vector-on-covector convention passes exactly 21, not 45,
  generators.
- The canonical carrier passes 45 while the selected graph fails 24.
- No graph correction, action owner, BV/KT quotient, domain, positivity,
  index, generation count or W/mirror selection is inferred.

## Final disposition

```text
held full normal-module theorem:       survives
characteristic-zero convention result: survives
selected graph full naturality:        rejected
carrier-to-physics promotion:          rejected
next gate: one action/BV moving graph or physical symmetry reduction
```
