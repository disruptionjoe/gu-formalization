---
title: "Hostile review — selected-K116 RSAP TT frame-consistency correction"
status: complete
doc_type: hostile_review
created: "2026-08-15"
artifact: explorations/conditional-build/selected-k116-rsap-tt-frame-consistency-correction-and-transport-gate-2026-08-15.md
---

# Hostile review

> **K117 SYMBOL-ORDER CORRECTION:** this historical review predates the
> discovery that the inherited response is kinetic `d z E_hh`, not K116's
> zero-order mass insertion. Its frame-custody objections survive; its
> concrete `H dpsi` owner target and final next-step sentence do not. Use the
> K117 hostile review for the current gate.

## Strongest objections

1. **Perhaps `K`, `M0`, and `vv^T` were deliberately redefined in one new
   frame.** They were not: the provenance records the columns of `U` as the
   free eigenvectors, explicitly gives `h=q0+qm`, and retains the pre-change
   `K` and `M0`. A congruence must transform all three quadratic matrices.

2. **A coordinate change might preserve the old discriminant anyway.** It
   does not. The two consistent pencils are similar at the dynamics level and
   both give `b(alpha^2 b+4u)`. The mixed pencil gives a different polynomial
   at generic exact rational controls.

3. **The alpha-one control could remain special after correction.** It does
   not: `[L0,L1]=bH` has determinant `-b^2`, and `A_C` is explicitly nonzero
   at `alpha=1` when `du` is nonzero.

4. **One erroneous instantiation might invalidate the abstract connection
   work.** It does not. The identities `A_C=(1/2)C dC`, the minimal covariant
   quadratic, the boundary-only support exclusion, and integration of a
   one-generator Maurer--Cartan form are algebraic statements that re-run on
   the corrected `C` and `H`.

5. **The correction proves action ownership.** It does not. It repairs the
   target. A literal moving-background Hessian and its derivative terms still
   have to be derived from one source/action coordinate system.

6. **The correction changes the 98D or physical-domain result.** It does not.
   This remains a local `2D` reconstruction-grade packet with no stationary
   background, selected closed positive domain, typed BFV attachment or
   physical cohomology.

## Verdict

Pass at exact local correction grade. The prior concrete K110--K115 packet
must remain visibly superseded wherever current agents encounter it. The
historical next-owner conclusion is superseded by K117. The current next swing
must construct the full moving `D3 I_selected` owner before selecting a new
transport target.
