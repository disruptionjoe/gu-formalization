---
title: "Hostile review: real-K77 draft-9.16 blockwise rival"
date: 2026-08-04
status: complete
verdict: "REPAIRED_PARTIAL__CONDITIONAL_RIVAL_PASSES__SOURCE_SIGN_COMMON_VARIATION_AND_FULL_H_OPEN"
canon_verdict_change: none
---

# Hostile review: K77 Wave-2 actual-carrier D916 attempt

## Verdict

The actual-carrier algebra advances, but the provisional completion claim
fails.  The repaired packet contains a valid conditional total-graded rival,
an exact source-sign obstruction, a corrected Spin-equivariant bracket, and
an honest smaller successor gate.

Final status:

```text
PARTIAL__CONDITIONAL_TOTAL_GRADED_D916_RIVAL_BUILT__
SOURCE_SIGN_IDENTIFICATION_FULL_CONNECTION_VARIATION_AND_FULL_H_DESCENT_OPEN
```

## Reviewer 1: primary-source archaeology and Layer 0

Charge: find where the construction changes the meaning of a source glyph.

Finding: rendered draft section 11.2 explicitly places `zeta_minus` in
`Omega1(S_minus)`, `zeta_plus` in `Omega1(S_plus)`, `nu_plus` in
`Omega0(S_plus)`, and `nu_minus` in `Omega0(S_minus)`.  The first pass instead
used total form-parity times chirality as the meaning of all four signs.  That
reverses the one-form labels.

Disposition: accepted.  The total grading is retained as a conditional rival,
not a source identification.  The source extraction now records both locators
and the collision.

## Reviewer 2: Clifford and representation theory

Charge: decide whether the collision is merely verbal or algebraic.

Finding: it is exact.  The selected `Phi d` flips ambient `J`; wedge `d` and
contraction `-d-times` preserve it.  In equation 9.16, `Phi d` and `d` occupy
top-row derivative cells with identical displayed input/row signs.  Under
either uniform same-half or cross-half barred-row convention, they must have
the same ambient parity.  No convention fits `(-1,+1)`.

The auxiliary `G=(-1)^form J` does make all three classes odd and reproduces
the six derivative cells after relabeling.  That proves a rival exists; it
does not erase the source collision.

Disposition: accepted.  Wave 2 remains partial and the successor enumerates
Shiab parity, degree-dependent duality, and source-sign repair branches.

## Reviewer 3: symplectic geometry and Lie superalgebra

Charge: verify that the projected odd bracket is basis-independent and
equivariant, not merely shaped correctly.

Finding: the first-pass bracket summed coordinate functionals against the
same bivector basis without applying the inverse trace metric.  It passes a
selected generator accidentally but fails exact noncompact generators.  For
`gamma_ab`, the inverse trace metric contributes the relative weight
`-eta_a eta_b`.

Disposition: accepted and repaired.  The weighted bracket passes exact
infinitesimal `Spin(7,7)` equivariance; the unweighted construction is a
planted failure.  Full source-`H` equivariance is still open.

## Reviewer 4: global differential geometry

Charge: find where a local or finite descent witness is promoted to a global
geometric theorem.

Finding: the nonconstant three-patch `h(x)=1+xN` construction exactly verifies
moving Clifford, grading, contraction, and connection descent.  It does not
construct an actual atlas of `Y=Met(X)`, prove existence of the moving Spin
reduction for arbitrary `X`, or turn the reduced bracket into a fixed-full-`H`
equivariant map.

Disposition: accepted.  The report now calls this a conditional moving-orbit
witness and keeps global reduction/full-`H` descent in the gate.

## Reviewer 5: variational bicomplex and current ownership

Charge: check whether the actual common action variation was recomputed.

Finding: a nontrivial real-spin connection direction differentiates the
1920-coordinate fermion action to a nonzero current exactly once, and the
duplicate bridge fails.  But the probe does not combine every connection
direction with the predecessor's differential `JF/QF`, moving bosonic
coefficients, and full even Ward contraction on one common core.

Disposition: accepted.  The result is a compatibility witness, not complete
shared-core rederivation.

## Reviewer 6: Krein/operator and proof engineering

Charge: locate placeholder types, tautological claims, or analytic overreach.

Findings:

1. The exact K77 Hodge signs and a nontrivial connection-wedge adjoint pass.
2. The full moving-density multi-index adjoint of `Phi d_A` is not computed.
3. The sixteen-cell ledger types all positions, but `W_rs` remains a slot
   label rather than an actual coefficient construction.
4. Compact support is a variational core, not a physical closed domain.
5. Source, type, exact, and planted checks remain counted separately.

Disposition: accepted.  No physical-domain or actual-zero-order claim is
made.

## Repaired checks

The final deterministic probe passes:

```text
8 source + 23 type + 39 exact + 9 planted = 79 PASS
```

The hostile review therefore returns:

```text
PASS_WITH_MATERIAL_SCOPE_REPAIR__WAVE2_PARTIAL
```

No P1/P2/P3 use, observation theorem, physics recovery, generation count,
claim-status change, canon change, lane change, third-lane promotion, or
public-posture change is made.
