---
artifact_type: hostile_review
created: 2026-08-04
subject: RENDEZVOUS-ACTION-CURRENT-RIESZ-SUPERIG-WARD
verdict: PASS_WITH_MATERIAL_SCOPE_REPAIRS__GATE_PARTIAL
review_mode: inline_science_council_and_specialist_lenses
---

# Hostile review: K77 Wave-2 action/current/Riesz/Ward rendezvous

## Charges and repairs

| lens | attack | repair/disposition |
|---|---|---|
| primary-source geometry | “No bridge” could be narrated as a Weinstein theorem. | Downgraded to the source-faithful primary architecture. The source displays one total residual and no second bridge, but does not prove all bridge actions inconsistent. |
| variational bicomplex | A moving Shiab might be used to erase K77-B3. | Separated Euler roots: the moving response enters `E_epsilon`; at fixed epsilon it is zero and cannot repair the translation derivative. |
| dependency geometry | The finite fixture varies `B` and the compensator independently although source `B=B(epsilon)`. | Retained the split only as a derivative-path audit; the frozen physical roots are `(epsilon,T,Z,...)` and the final `E_epsilon` composes both paths. |
| current/Green analysis | The finite algebraic `JF` might replace RB1's differential current. | Physical ownership remains `JF=D_A^coad QF` with its preboundary. The finite `A^2` current is only the ownership/Hessian comparator. |
| Krein/operator theory | “Riesz” might imply positivity, boundedness, or a domain. | Renamed and graded it as a real indefinite pointwise pseudo-musical; exact positive and negative controls are retained. |
| gauge/Noether theory | A zero Ward contraction might be used to select the no-bridge action. | Exact controls show both bridge terms are separately gauge invariant. Ward validates transformation ownership, not bridge policy. |
| BV geometry | `Xi=D Upsilon` or the even Ward identity might be called the odd super-IG master equation. | Kept all three distinct. The even minimal BV skeleton is conditional; `TG-2/TG-3` remain open. |
| superalgebra/representation theory | The real mixed bracket might be promoted to source-group or simultaneous-Krein closure. | Scoped to pointwise `Sp(S,Omega)`. Source-group projection, Krein compatibility, field/parameter identification, gamma-domain preservation, and odd action remain explicit burdens. |
| gravity/physics | A local finite action architecture might be called physical recovery. | Observed/Frobenius trace reversal, section emission, vacuum, domain, poles, and every physics row remain held out. |
| epistemics/proof engineering | Rigor could continue defending the superseded printed endpoint or N1 bridge family. | The endpoint is replaced by the actual Euler derivative; bridges survive only as negative-test comparators. The gate remains partial rather than manufacturing completion. |

## Independent checks

The arithmetic defect has an independent hand route:

```text
actual translation derivative     = -211/21
advertised endpoint pairing       =  -68/7 = -204/21
actual - advertised               =   -7/21 = -1/3
```

The Ward route is independent of that calculation. Under the complete gauge
direction, `S_g(X)=g S_0(g^-1 X g)g^-1` has

```text
delta_g S(X) = [xi,S(X)] - S([xi,X]).
```

Adding the transformed input `delta X=[xi,X]` leaves `[xi,S(X)]`, so the
trace action is invariant. Omitting the first term or the connection's
`[xi,D]` contribution produces the planted nonzero Ward residuals.

For the rolled bracket, `mu_Omega(u,v)=u v^T Omega+v u^T Omega` satisfies

```text
mu^T Omega + Omega mu = 0,
mu(Hu,v)+mu(u,Hv) = [H,mu(u,v)]
```

for every `H in sp(S,Omega)`. Symmetry and the two-step odd Jacobi identity
then follow algebraically, not from sampled coincidence.

## Two mandatory meta-charges

**Summary outruns artifact:** caught and repaired at uniqueness, positivity,
source-group closure, odd Ward, and physical-recovery scope.

**Artifact defends a superseded object:** caught at the proposed printed
endpoint and the previously hardened current-bridge fork. Those calculations
remain controls; they no longer set the primary architecture.

## Verdict

`PASS_WITH_MATERIAL_SCOPE_REPAIRS__GATE_PARTIAL`.

The artifact earns one frozen local action-first no-bridge Euler/current/
pseudo-Riesz/even-Ward architecture and a real pointwise mixed super-IG
bracket. It does not earn a complete odd action, odd Ward/BV identity, common
domain, observation quotient, physical equation, external datum use, or
campaign advance to Wave 3.
