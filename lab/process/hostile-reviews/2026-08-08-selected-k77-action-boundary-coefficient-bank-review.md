# Hostile review — selected K77 action boundary coefficient bank

Date: 2026-08-08
Disposition: **PASS AFTER NARROWING**

## Charges

### 1. Symplectic geometer

The initial temptation was to say the action had constructed a BFV phase
space.  It has not.  The calculation constructs the local action covector and
shows that two opposite, independent endpoint restrictions can be inserted
into the already-proved canonical endpoint dressing.  A global moment map,
charge algebra, polarization and reduction remain open.  The report was
narrowed accordingly.

### 2. Variational-bicomplex reviewer

A firing bank would be worthless if its entries were fitted after seeing the
answer.  The probe instead differentiates one written action analytically in
all 1,470 basis directions, checks dense held-out `B` and `T` derivatives by
an exact five-point formula, and fires a deleted-derivative plant.  This charge
passes.

### 3. Representation and K77 reviewer

The 105-dimensional `Cl1 + Cl2` coefficient tangent is not the full
`U(64,64)` algebra.  The result now states this in its status, Layer-0 table,
scope fence and next gate.  One nonzero rank-ten minor proves generic
nonvanishing on this selected polynomial family only.

### 4. Krein/operator reviewer

The tested form is the scalar Clifford-product form on the low-grade
coefficient basis.  It is exact, indefinite and nondegenerate on the image,
but it is not thereby the full analytic Krein pairing or a positive energy.
The raw/observed inertias `(4,6,0)` and `(5,5,0)` are reported rather than
explained away.

### 5. Observation and source reviewer

The rational observation graph is a complete equation-dual fixture with an
exact inverse.  It proves the complete receiver can preserve the bank and
that tangential pullback cannot; it is not the global physical section.
Weinstein supplies the nonlinear action/T chain, while the selected
`comm/symi/symi` channel and the bank are repo constructions.  The preferred
historical Shiab, BFV identification and full/global extension are
`SOURCE-SILENT`.

## Two-sided synthesis audit

- **Summary outruns artifact:** repaired by replacing "BFV/action-owned phase
  space" with "local endpoint dressing accepts the action covector."
- **Fence defends superseded object:** avoided.  The v0.75 arbitrary-`K` kill
  is preserved, while the v0.74 universal contact and `40/40` endpoint
  theorems remain useful consumers of the new covector.

## Independent rerun

```text
Python exact probe: 44/44 PASS
Independent Sage reconstruction: 14/14 PASS
normal support fingerprint: 13,14,12,16,13,16,13,12,5,8
raw/observed Gram determinants: exact and nonzero
```

No claim-status, canon-verdict or public-posture change is authorized.

