---
artifact_type: hostile_review
created: 2026-08-06
target: explorations/conditional-build/moving-gimmel-hodge-frame-owner-2026-08-06.md
status: PASS_AFTER_SCOPE_FENCES
---

# Hostile review — moving gimmel / Hodge / frame owner

## Reviewer A: where the summary outruns the artifact

The strongest possible overclaim would be “the Hodge contribution cancels.”
The probe proves something narrower: the metric/Hodge component response in a
fixed coordinate frame is the co-moving-frame response of one natural owner
packet. It does not transport Phi, Shiab, the Krein pairing, fields,
gauge-rotated Levi-Civita distortion or the observation map through the same
frame, and therefore cannot prove selected-action Euler cancellation.

Repair required and applied: the report says “owner fusion,” retains the full
selected-action composition, and plants fixed-frame and conformal controls.

## Reviewer B: where the fence defends a superseded or mistyped object

The queue had separately named moving metric/Hodge and moving observation/
frame data. For natural tensors those are coordinate descriptions coupled by
functoriality. Retaining them as independent free coefficients would defend a
superseded decomposition and could double-count the first-order burden.

Repair required and applied: the next gate names one fused gimmel/frame owner
packet and separately lists only genuinely additional structures such as
Krein-domain, soldering, BV and global-boundary data.

## Mandatory symplectic-geometry lens

The frame compensator is a local field-dependent change of frame, not a
symplectic reduction. It does not by itself make the presymplectic potential
basic, kill boundary flux or construct a BFV quotient. Any future claim that
the action variation vanishes must show the complete pulled-back potential
and its boundary term in one frame convention. The current report preserves
that burden.

## Differential-geometry lens

The calculation uses the actual `Sym2(T*X)` ten-plane, including its four
diagonal directions, and differentiates the trace-reversed DeWitt metric rather
than substituting the non-isomorphic `Lambda2 + Lambda3` ten. Exact inertia
`(6,4)` plus base `(1,3)` gives `(7,7)`. An independent Sage rational
quadratic-form reconstruction agrees on determinant, signatures, trace, rank
and compensator. The selected `h` is trace-free but not claimed to solve a
field equation.

## Variational/PDE lens

`delta vol_G=0` is a local TT identity. The conformal plant has density
response `-8`, so the conclusion does not extend to arbitrary metric motion.
Pointwise split inertia does not establish a positive energy, a maximally
dissipative boundary condition or a global Green domain.

## Constraint-accounting lens

Owner fusion changes no physical parameter count: the current `84`, `>=19`,
`9` and four scoped quotients remain fixed. No external datum is consumed.

## Verdict

Pass after fences. The durable result is exactly:

```text
TT density derivative zero;
fixed-frame Hodge derivative nonzero;
exact co-moving-frame compensator exists;
Hodge and coframe are one functorial owner packet;
complete selected-action/observation variation remains open.
```
