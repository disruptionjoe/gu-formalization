---
title: "Hostile review: K111 RSAP TT spectral-transport connection and owner gate"
status: complete
created: "2026-08-15"
target: explorations/conditional-build/selected-k111-rsap-tt-spectral-transport-connection-owner-gate-2026-08-15.md
verdict: PASS_WITH_KINEMATIC_CONNECTION_SELECTED_CUBIC_OWNER_AND_FULL_ACTION_CEILINGS
---

# Hostile review

> **GU-COMPARATOR-ROUTING — scope before inference.** This review concerns a
> source-native Krein/Green and BFV operator completion. Standard Higgs,
> family-index and chirality constructions do not adjudicate it. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.

Classification: `SOURCE_NATIVE_ROUTE`.

## Strongest algebra charge

The proposed coefficient might be fitted. It is not. Differentiating `C^2=1`
forces the off-diagonal transport, and `(1/2)C dC` is the unique connection
that is simultaneously `C`-parallel and `K`-compatible on the two simple
one-dimensional eigenspaces. A general commuting `K`-skew difference is zero.

## Strongest trivialization charge

The connection is flat for the one-scalar family, but that does not make it
the original trivial connection. Replacing `Box` by `Box_A` adds explicit
first- and zero-order terms. Gauging `A` to zero simultaneously changes the
field frame; it does not prove the original variable-background operator
commuted with `C`.

## Strongest action-owner charge

The selected cubic owns `u(x)vv^T`, not `A_C`. It contains no TT derivatives,
so its derivative Hessian is zero while `A_C` is generically nonzero when
`du` is nonzero. The full action may contain compensating derivative terms;
K111 does not exhaust or reject that possibility.

## Strongest analytic overclaim

A metric connection normally hyperbolic operator has Green operators, but this
still does not choose a closed self-adjoint positive-Hilbert domain, conserved
energy, Fock representation or physical cohomology. Those ceilings remain.

## Strongest carrier overclaim

This remains a two-field observed TT bundle. The connection does not supply a
map into the conditional `98D` balanced phase/BFV carrier, nor does it derive
the balanced seed or boundary law.

## Verdict

Pass at exact kinematic spectral-connection and selected-cubic owner grade.
The moving Green completion is canonical and unique, the current action-owner
failure is coefficient-level rather than rhetorical, and full-action/BFV
revival remains open.
