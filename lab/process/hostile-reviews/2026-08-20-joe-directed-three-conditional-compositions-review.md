---
artifact_type: hostile_review
status: final_pass_after_repair
doc_type: independent_hostile_review
created: 2026-08-20
work_item: THREE-CONDITIONAL-COMPOSITIONS-HR
title: "Hostile review: SA2 two-Lorentz classifier, BP2 non-uniform corner incidence, and Z3R2 asymmetric equivariant-module inventory"
reviewed_artifacts:
  - lab/active-research/joe-directed/soldered-ad/sa2-two-lorentz-source-package-classifier-2026-08-20.md
  - tests/channel-swings/joe_directed_sa2_two_lorentz_source_package_classifier.py
  - lab/active-research/joe-directed/bit1-price/bp2-nonuniform-corner-decoupling-owner-census-2026-08-20.md
  - tests/channel-swings/joe_directed_bp2_nonuniform_corner_decoupling_owner_census.py
  - lab/active-research/joe-directed/z3-receptacle/z3r2-asymmetric-equivariant-module-inventory-2026-08-20.md
  - tests/channel-swings/joe_directed_z3r2_asymmetric_equivariant_module_inventory_probe.py
verdict: FINAL_PASS_AFTER_REPAIR__ALL_FOUR_MANDATORY_REPAIRS_CLOSED__ALL_THREE_EXACT_CORES_SURVIVE__NO_ARC_HAS_AN_ADMISSIBLE_IMMEDIATE_CONTINUATION
canonical_effect: none
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: **`BRIDGE_OR_SEMANTIC_BOUNDARY`.**

# Hostile review — three conditional compositions

```gu-typed-objects
result: independent hostile-review verdict after four bounded repairs across SA2, BP2 and Z3R2
carrier: three distinct repository constructions reviewed together LAYER=UNTYPED CHIRALITY=S-CHIRALITY-UNTYPED
pairing: hostile charges paired with their producing artifacts and exact probes ON=reviewed-artifact-set
real_structure: UNTYPED across the combined review set
grading: conditional exact cores plus adversarial review; no physical selection
action_owner: N/A -- the review introduces no source action or physical owner
target: the three conditional-composition results MAP-TYPE=evaluation
```

## Verdict first

The initial wave was scientifically useful and its three exact cores survived,
but its first materialization was not ready to integrate. Four bounded repairs
were mandatory. The post-repair delta audit closes all four:

1. **Closed — SA2 graph closure.** The load-bearing implication is now
   certified from the operators actually known to annihilate a simultaneous
   `PS + graph-Lorentz` invariant, namely `k_PS` and
   `[delta(so13),k_PS]`. The repaired exact replay gives:

   ```text
   rank(k_PS)                         = 21
   rank([delta(so13),k_PS])           = 45
   rank(k_PS + [delta(so13),k_PS])    = 45
   ```

   The theorem and executable now use this valid graph-invariance implication;
   the `drop-graph-commutators` mutant exercises the repaired load-bearing step.
2. **Closed — Z3R2 symmetry custody.** The artifact and probe now distinguish
   `Aut(C3)=C2`, which fixes the trivial character, from the enlarged
   `AGL(1,3)` label action. Translations are typed as tensor-by-character
   equivalences, not group or fixed-module automorphisms.
3. **Closed — BP2 materialization custody.** BP2 now has repository-local
   status, full artifact metadata, dependencies and script custody, the uniform
   routing notice and classification, and no staged/materialization wording.
4. **Closed — notation, routing, and census labels.** SA2 B01 now states the
   tested doubled-weight result `(2,2)+(0,0)`, ordinary and doubled Weyl labels
   are cross-walked, and the exact-arithmetic grade is grammatical. BP2's first
   census column now says “built relevant structure” in both prose and code.
   Z3R2 now carries the full uniform `GU-COMPARATOR-ROUTING — scope before
   inference` notice rather than a local paraphrase.

With those repairs closed, the final wave verdict is:

```text
SA2   SURVIVE AND BANK AS CLOSURE; STOP PHYSICAL-SELECTION WORK.
BP2   SURVIVE AS CONDITIONAL INCIDENCE; DEPENDENCY-FENCE CONSTRUCTION.
Z3R2  SURVIVE AS STRUCTURAL 1+2 INVENTORY; STOP UNTIL A TYPED MAP EXISTS.
```

No result builds or selects an action, background, vacuum, external datum,
coefficient, family row, alignment, phase activation, scale, physical quotient,
domain, ordinary family index, net chirality, standard Higgs route, or physical
Lorentz embedding.

## Independent lens audit

| lens | hostile question | ruling |
|---|---|---|
| 1. source fidelity | Is the target Weinstein's representation-origin `2+1` rather than an ordinary three-family index? | **Pass.** All three keep the source target distinct from conventional counting. |
| 2. conditional-build boundary | Does any arc manufacture an off-limit owner? | **Pass.** Missing action, row, alignment, selector, phase, quotient, domain, scale, and maps remain fences. |
| 3. `F/M_3/144` homonyms | Are imposter, multiplicity, and partner collapsed? | **Pass.** BP2 and Z3R2 are especially explicit; SA2 also preserves equation-(12.22) versus equation-(11.6) provenance. |
| 4. nonchirality/emergent chirality | Is a Weyl or rho calculation promoted to a net-chirality verdict? | **Pass.** SA2 limits “no Weyl submodule” to an intermediate representation; Z3R2 calls rho spectral, not a count. |
| 5. four corners and bars | Are both K77 halves, all four corners, and independent bars retained? | **Pass.** BP2's two halves are an external direct sum; no bar is made an adjoint. |
| 6. exact representation branching | Do SA2's dimensions and restrictions close? | **Pass after notation repair.** Independent replay gives 80/80 and all dimensions close; B01's text label is wrong although its tested object is right. |
| 7. PS owner invariance | Are the `54` and `210` lines really PS-fixed and endo-moving? | **Pass.** Exact rational matrices give orbit rank `0` under block and `3` under endo for both. |
| 8. graph-closure logic | Does the computed Lie closure imply the claimed simultaneous-invariant zero? | **Pass after repair.** The certificate now checks the logically available `k_PS+[delta,k_PS]` span directly and obtains rank 45. |
| 9. family exact-sequence naturality | Is BP2 more than a basis-dependent rank count? | **Pass.** The canonical short exact sequence, `GL(3,Q)` covariance, row-line rescaling, and rank `k=0,8,12,16` controls are exact. |
| 10. bounded-inventory honesty | Is H210 called globally unique? | **Pass, with one probe-label cleanup.** The prose says unique only within five candidates. Rename the probe's first census column from “built port” to “built relevant structure,” or set non-port entries false, so code and table mean the same thing. |
| 11. phase/corner logic | Is `MASSIVE => B`, `A -> B`, or physical activation inferred? | **Pass.** B1P1's adverse control confirms massive admits both B and CTRL40; BP2 explicitly refuses both promotions. |
| 12. `C3`, AGL, and module automorphisms | Are three different symmetry notions separated? | **Pass after repair.** `Aut(C3)`, tensor-by-character affine equivalences, and fixed-module centralizer automorphisms are now separately typed. |
| 13. realification and rho custody | Are conjugate complex characters mistaken for two real sectors, or rho for a family count? | **Pass.** `R^14 + R^2_rot tensor R^12` and the convention-A/geometric-Q split are correctly fenced. |
| 14. bridge and naturality obstruction | Does existence of many equivariant quotients become a canonical quotient? | **Pass.** Scalar matrices already force every fully automorphism-natural covector to zero. The conclusion is explicitly conditional on that naturality group. |
| 15. prior art and novelty | Are old results repackaged as discoveries? | **Pass.** BP2 labels its exact sequence/census as the narrow new join; SA2 identifies its new composition; Z3R2 claims only the stabilizer/naturality join. |
| 16. executable rigor and mutants | Do the new probes exercise adverse routes rather than only print green? | **Pass after repair.** SA2 kills 8/8 including the retargeted graph-commutator mutant, BP2 fires 17/17 semantic mutants, and Z3R2 kills 7/7 including symmetry-type custody. |
| 17. portfolio value | Which result changes what should be attempted next? | **SA2 ranks first as closure, BP2 second as source-direct dependency localization, Z3R2 third as a remote structural receptacle. None earns an immediate continuation vote.** |

## Arc-by-arc hostile rulings

### SA2 — strongest wave result, then stop

**Survives.** Exact doubled-weight branching gives identical restrictions of
both internal `16` halves, removes pure Weyl submodules from each endogenous
`F` sector, retains two Weyl copies in each `Z` sector as a contrary control,
moves both PS owner lines by three boosts, and closes the relevant internal
algebra to `so(6,4)`. Under the block horn the external-spin/internal-partner
factorization remains exact.

**Killed.** Factorized simultaneous PS custody on the endogenous horn; either
known HE4 owner line as an endogenous scalar; treating inert `M_3` repetition
as a missing-type repair; any claim that this selects the physical Lorentz
embedding.

**Stop.** Bank the discriminator. Reopen only for an already-owned full-corner,
non-PS, endogenous intertwiner preserving source provenance. The action-owned
embedding selector remains off limits here. Fertility: `9/10` as closure,
`0/10` as an admissible immediate construction path.

### BP2 — exact incidence, no decoupling owner

**Survives.** For nonzero `r:M_3 -> L` and declared `B:S_16 -> W`,

```text
0 -> ker(r) tensor S_16 -> ker(r tensor B) -> L tensor ker(B) -> 0,
rank(r tensor B)=rank(B),
dim ker(r tensor B)=48-rank(B).
```

At rank 16 this is rank 16/kernel 32 per independent half. H210 is the only
candidate in the explicitly bounded inventory with a built current-K77
right-direction port and the correct equation-(9.16) cells.

**Killed.** The claim that owned geometry already identifies the kernel as two
true families or the quotient as the imposter; global H210 uniqueness; a
massive-to-B implication; an A-to-B transition; physical phase activation.

**Stop.** The first missing owners are `r`, `H210-ALIGN`, and activation. They
are expressly off limits. Fertility: `7/10` as closure/localization and `2/10`
for continuation only after an independent ownership event.

### Z3R2 — honest structural survivor, remote from the source carrier

**Survives.** The geometric Q kernel has honest multiplicities `(14,12,12)`,
all isotypes occupied, an exact singleton-plus-conjugate-pair fingerprint, rho
class `(0,2,1)/3`, realification `R^14 + R^2_rot tensor R^12`, and no nonzero
one-line-per-isotype quotient natural under the full module automorphism group.

**Killed.** Reading `Z/3` order as a generation count; identifying Q with `F`,
`M_3`, or `144`; treating rho as a selector; calling an arbitrary equivariant
rank-three quotient canonical; claiming an owned bridge to the dimension-13
receptacle or current K77.

**Stop.** The affine-action theorem is a useful inventory classifier after its
symmetry custody is repaired, but there is no source-native map into Q and no
map from Q to the relevant GU carriers. Fertility: `7/10` for this bounded
swing, `2/10` for immediate continuation.

## Voting, conviction, and reprioritization

Seventeen review lenses cast a path-value vote after scope penalties:

```text
SA2   10 votes  -- largest pruning/closure value
BP2    5 votes  -- most source-direct exact dependency localization
Z3R2   2 votes  -- exact but bridge-remote structural survivor
```

The continuation vote is `0/17` for all three under the present lane contract.
That is not a vote against the hypotheses; it means every next constructive
step would require an unowned or off-limit interface.

The highest-conviction results are not identical to the raw ranking:

1. **Very high conviction:** BP2's exact kernel sequence and rank formula.
2. **Very high conviction after the closure-check repair:** SA2's block/endo
   incompatibility for factorized simultaneous PS custody.
3. **Very high conviction but low source relevance:** Z3R2's module-
   automorphism naturality obstruction.
4. **High conviction:** H210 is the sole viable candidate only in BP2's stated
   five-item inventory, never globally.

Path-relative priority is therefore: bank SA2 first, bank BP2 second with its
dependency fence, bank Z3R2 third as a receptacle fact, and move the next wave
to a different independently fertile source-native lane. Do not reopen H210's
paused derivative adapter, the banked twistor composition, neutrino without
both reopen antecedents, or any action/external-datum construction through
these results.

## Exact replay receipts

Run from the GU repository root on 2026-08-20:

```text
python3 tests/channel-swings/joe_directed_sa2_two_lorentz_source_package_classifier.py --selftest
  PASS: clean baseline; 8/8 false routes caught.
  Baseline receipt embedded by selftest: 80/80 exact checks.

_local/cas-venv/bin/python tests/channel-swings/joe_directed_bp2_nonuniform_corner_decoupling_owner_census.py --repo . --selftest
  PASS: 90/90 exact/custody checks; 17/17 semantic mutants fired.

python3 tests/channel-swings/joe_directed_z3r2_asymmetric_equivariant_module_inventory_probe.py --selftest
  PASS: 78 exact checks; 7/7 mutations killed.

python3 tests/channel-swings/joe_directed_he3_four_corner_partner_placement_probe.py
  PASS: 23/23.

python3 tests/channel-swings/joe_directed_cb2_h210_equation916_composition_probe.py
  PASS: 22/22.

python3 tests/channel-swings/joe_directed_cb6_h210_three_horn_compatibility_probe.py
  PASS: CB-6C 10/10 plus all printed family/covariance/horn controls.

python3 tests/channel-swings/joe_directed_b1p1_three_corner_support_prices_the_chiral_grant.py
  PASS: 91/91, including contrary MASSIVE/PRESENT controls.

python3 tests/channel-swings/joe_directed_rsc1_unique_channel_lives_on_the_gamma_trace.py
  PASS: 103/103; 15 planted false propositions remain false.

python3 tests/channel-swings/joe_directed_z3r1_nu_trivial_w_untwisted.py
  PASS: 62/62.

python3 tests/rs-function-space/rho-38-adjudication/legC_equivariant_rho.py
  PASS: 580 hard assertions; Q h=(14,12,12), class=(0,2,1)/3.

_local/cas-venv/bin/python tests/channel-swings/k77_global_chimeric_spin_reduction_probe.py
  PASS: 53/53.
```

Independent closure diagnostic used for mandatory repair 1:

```text
rank(k_PS)=21
rank([delta(so13),k_PS])=45
rank(k_PS+[delta(so13),k_PS])=45
rank(k_PS+delta+[delta,k_PS+delta])=45
```

One pre-existing dependency receipt is not green and must not be hidden inside
the new-wave summary:

```text
python3 tests/channel-swings/joe_directed_sa1_soldered_ad_selector.py
  100/105; failures S30, L01-L04.
```

`S30` is stale fork-registry archaeology. `L01-L04` run SymPy-dependent nested
probes with the system Python; direct execution in the repository CAS
environment gives K77 `53/53`, while the LA8 dependency now reports `76/78`
because its byte-identity/verbatim ledger expectations drifted. SA2's new
matrix/branching certificate and its HE3 dependency are independently green,
so this is not a kill of SA2. It is a pre-existing receipt-drift debt and must
not be restated as a current `SA1 105/105` replay.

The routing audit is expectedly red before post-commit registration:

```text
source_native_comparator_routing_audit: 147 derived / 139 registered /
8 unclassified, baseline 5
```

The three excess paths are exactly SA2, BP2, and Z3R2. After the four repairs,
commit the artifacts first, then register only the committed in-scope paths as
required by the 2026-08-17 registration discipline, and rerun the audit.

## Post-repair delta receipt

Focused reinspection confirms all four mandatory repairs and the additional
BP2 census-label cleanup:

- SA2 now proves the graph-invariance implication with the exact
  `k_PS+[delta(so13),k_PS]` rank-45 span, fixes B01's doubled-weight label,
  cross-walks ordinary/doubled Weyl notation, and retargets the closure mutant.
- BP2 now has final repository-local frontmatter/routing custody, contains no
  staged/materialization language, and consistently calls the first census
  field “built relevant structure.”
- Z3R2 now separates `Aut(C3)=C2`, tensor-by-character affine equivalences, and
  fixed-module automorphisms, and carries the uniform routing notice verbatim.

Fresh post-repair execution receipts:

```text
SA2 baseline: 80/80 exact checks, exit 0.
SA2 selftest: clean baseline; 8/8 false routes caught, exit 0.
BP2 selftest: 90/90 exact/custody checks; 17/17 semantic mutants, exit 0.
Z3R2 selftest: 78 exact checks; 7/7 mutations killed, exit 0.
```

The standing SA1/LA8 dependency-receipt drift recorded above is unchanged and
remains explicitly outside the three repaired execution cores. Final hostile
verdict: **PASS AFTER REPAIR**; bank all three results with the existing stop
boundaries and no immediate continuation promotion.
