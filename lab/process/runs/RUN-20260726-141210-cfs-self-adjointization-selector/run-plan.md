---
run_id: RUN-20260726-141210-cfs-self-adjointization-selector
status: completed_scoped_result
repository: gu-formalization
authority: "Joe direct chat: if another valuable swing follows the Finster/Krein admissibility result, execute it"
starting_revision: ac289beb5512
channels:
  - source_action
  - spectral_analysis
  - representation_collision
maximum_grade: "EXACT FINITE CONDITIONAL CFS ACTION COMPARATOR AND REPRESENTATION-SENSITIVITY NO-GO / NO GU SOURCE ACTION, PHYSICAL CFS LIFT, BARYOGENESIS RATE, CLAIM, CANON, OPERATOR, OR VERDICT MOVEMENT"
write_boundary:
  - explorations/W246-cfs-self-adjointization-selector-ambiguity-2026-07-26.md
  - lab/deep-research/README.md
  - lab/process/source-action-build-ledger.md
  - lab/process/runs/RUN-20260726-141210-cfs-self-adjointization-selector/run-plan.md
  - tests/README.md
  - tests/W246_cfs_self_adjointization_selector_ambiguity.py
external_action_authorization: "Local exact computation, git commit, and non-force push only; no hardware, provider/account access, purchase, contact, data request, paper activation, submission, publication, or other external action."
completed_at: 2026-07-26T19:22:34Z
outcome: MATCHED_CFS_TOYS_EXIST__REPRESENTATION_SENSITIVE_NO_SELECTOR
---

# CFS self-adjointization selector ambiguity

## Objective

Attempt the narrow reopener left by W245: construct both GU branch specimens
as self-adjoint operator-valued universal measures inside one matched finite
causal-action problem, then determine whether the causal action ranks the
stable branch in a representation-independent way.

## Canonical positive attempt

First test the metric-lowering map

\[
x=JH
\]

and a fixed-local-trace version of it. This is the most direct candidate
because it uses the branch's declared adjoint metric and produces an ordinary
self-adjoint operator.

Then test the faithful Hermitian dilation

\[
\Phi_\lambda(H)=
cI_4+
\begin{pmatrix}
0&H+\lambda H^\dagger\\
H^\dagger+\lambda H&0
\end{pmatrix},
\qquad \lambda\neq\pm1 .
\]

Every admitted \(\Phi_\lambda\) must be checked for:

- self-adjoint output;
- fixed local trace;
- common spin dimension and signature bound;
- injectivity given the fixed block grading;
- unitary equivariance;
- one unchanged rule for both branches; and
- identical symmetric measure weights and constraints.

## Preregistered discriminator

1. If all admissible faithful maps give the same action ordering, bank a
   conditional finite selector and state the remaining physical-lift burden.
2. If two equally admitted faithful maps reverse the ordering, return
   `REPRESENTATION_SENSITIVE_NO_SELECTOR`.
3. If the metric-lowering map erases the fork or violates the common
   constraints, return `CANONICAL_MAP_FAIL`.
4. A favorable result under one hand-picked map is not selection.

## Exact arena

Use W216's branch matrices at \(\xi=\pm a\):

\[
H_+(\xi)=\xi\tau_3+\Delta\tau_1,\qquad
H_-(\xi)=\xi\tau_3+i\Delta\tau_2.
\]

Freeze rational parameters \(a=1/2\), \(\Delta=1\), and fixed-local-trace
shift \(c=1/10\). Use equal two-point measures, spin dimension \(n=2\) for
the dilation, and one common boundedness ceiling. Compare \(\lambda=7/10\)
with \(\lambda=9/10\), both strictly inside the injective range and one
connected fixed-signature sector.

## Local Model Learning Gate

- Baseline: W245 proves domain admissibility only and leaves the
  branch-to-measure map missing.
- Local delta: either construct a matched action selector or exhibit the
  smallest faithful representation change that reverses its verdict.
- Generated-not-encoded: the same map, trace, weights, dimension, constraints,
  and causal Lagrangian apply to both branches; both verdict directions are
  preregistered.
- Checkpoint: exact rational action values plus a numerical parameter sweep.
- Decision: advance only a representation-invariant selector; otherwise stop
  and require a physically derived CFS local-correlation map.
- Hardware boundary: none.

## Nonclaims

This finite comparator does not derive a GU source action, identify the CFS
physical wave-evaluation map, prove minimizer uniqueness in the unrestricted
measure class, calculate baryogenesis, or predict baryon asymmetry.

## Completion receipt

Completed. The metric-lowering construction produces matched two-point CFS
measures and an exact stable-branch preference of \(3/100\). The faithful,
unitary-equivariant family \(\Phi_\lambda\) then reverses the action ordering
inside one connected fixed-signature sector:

- at \(\lambda=7/10\), the bad-minus-good margin is
  \(1517/5000>0\);
- at \(\lambda=9/10\), it is \(-43/200<0\).

The result is an exact finite representation-sensitive no-selector. It
neither selects a GU branch nor supplies a physical CFS local-correlation map.
The seam is stopped until a physical map, target-independent universal
property, or robust invariant ordering is derived.

Validation:

- W246 comparator: `23/23`;
- W245 regression: `16/16`;
- W186 regression: `36/36`;
- W216 regression: `28/28`;
- Python compile, exact artifact checks, and `git diff --check`: pass.
