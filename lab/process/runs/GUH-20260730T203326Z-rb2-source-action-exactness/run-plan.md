---
run_id: GUH-20260730T203326Z-rb2-source-action-exactness
status: completed
repository: gu-formalization
workflow: direct-chat-construction-swing
mode: execute
lane_id: "1"
work_item: RB2-SOURCE-ACTION-EXACTNESS-SHOOTOUT
starting_revision: f5c022bc8ec1
opened_at: 2026-07-30T20:33:26Z
completed_at: 2026-07-30T21:45:40Z
write_boundary:
  - NEXT-STEPS.md
  - explorations/README.md
  - explorations/rb2-source-action-exactness-shootout-2026-07-30.md
  - lab/process/runs/GUH-20260730T203326Z-rb2-source-action-exactness/run-plan.md
  - tests/README.md
  - tests/channel-swings/rb2_source_action_exactness_probe.py
claim_status_change: none
canon_change: none
public_posture_change: none
---

# RB2 source-action exactness shootout

## Objective

Execute RB2 of the post-N3 source-action/external-datum campaign. Consume the
three RB1 candidates plus its \(\lambda\ne1\) hostile control family and
construct:

1. one explicit weak Euler system for each N1 current bridge;
2. the conditional first variation of the source-shaped first-order action;
3. separate coefficient-exactness, internal-gauge Ward, and Diff-covariance
   dispositions;
4. a no-double-count and parent-elimination ledger;
5. typed Euler-equivalence classes; and
6. the smallest lawful family emitted to RB3, or a bounded geometry-return
   packet if the class dies.

The output may not terminate at `SOURCE-ACTION-MISSING`,
`EXTERNAL-DATUM-NEEDED`, or `MORE-HARDENING-REQUIRED`.

## Protocol and construction fork

Layer 0 runs before the seven axes. The action, its Euler residual, a Ward
identity, source exactness, a BV master equation, and a physical equation are
different objects until related by explicit maps.

The active native fork is:

- \(Y^{14}=\operatorname{Met}_{3,1}(X)\) with actual
  \(\operatorname{Sym}^2T^*X\) fibre;
- \(\operatorname{Sp}(32,32;\mathbb H)\), right-\(\mathbb H\), and indefinite
  \(G/\kappa_{\mathfrak g}\) pairings;
- the gamma-traceless full-20 carrier; and
- the frozen Spin/stabilizer arena.

\(U(128)\), positive-Hilbert projection, exterior \(6+4\), ghost-subtracted
RS, and generic fixed-plane full-\(Sp\) are hostile comparators. A kill must
name its fork and may not transfer to the other.

## Frozen architecture census

Candidates:

1. `N1_JD_bridge`;
2. `N1_total_current_bridge`;
3. `source_reference_lambda1`.

Hostile control family:

4. `source_orbit_constrained_lambda_not1`.

No independent soldered current was built in RB1. The control family does not
count as a fourth candidate.

## Read-only triage incorporated before executable fixtures

Three parallel read-only reviews were commissioned before the shared
executable kernel. They exposed four questions that this run freezes rather
than hiding:

1. the cited four-real Shiab family is spinorial
   \(\Lambda^2V^*\otimes S\to V^*\otimes S\), while the source bosonic action
   requires an \(\epsilon\)-dependent
   \(\Omega^2(\operatorname{ad}P)\to
   \Omega^{13}(\operatorname{ad}^*P)\) map;
2. a field-dependent total current leaves current-Hessian and \(Z\)-variation
   terms even when its direct \(A\)-current cancels;
3. gauge covariance is blind to \((a,b)\) and \(\lambda\); the source
   coefficients must be tested by the stronger transgression/Fréchet
   exactness identity, while \(\lambda=1\) is selected only by dependency and
   field-space rank; and
4. the complete Diff identity needs moving-\(s\) formulas scheduled for RB4,
   so RB2 must grade Diff closure as partial and forbid a Diff-based kill.

These are inputs to the preregistered executable tests below, not conclusions
silently fitted after those tests.

## Expected verdict before executable calculation

- The two N1 bridges should both remain locally admissible but generically
  Euler-distinct.
- `N1_total_current_bridge` should not fully cancel the matter sector once
  \(D_AJ_F\), \(D_ZJ_F\), and the \(Q_F\) Green flux are retained.
- The source coefficients \((1/2,1/3)\) should be uniquely selected only
  conditional on the source cyclic/transgression identities
  \(2a=1,3b=1\).
- The native source-shaped branch should be `BLOCKED`, not killed, if no
  bosonic Shiab carrier map is found.
- Wrong coefficients and \(\lambda=2\) should still pass a gauge-covariance
  control; any Ward test selecting them is suspect.
- Parent-retained and eliminated systems should agree only at classical
  antifield-zero grade with \(Z_U\ne0\).

## Kill and block conditions

Kill only the affected N1 bridge on:

- wrong current carrier;
- failed complete first variation;
- nonreal native action;
- a nonclosing internal-stabilizer Ward contraction after all current
  Hessians and background responses are included;
- duplicate bridge/current or parent insertion; or
- loss of a five-leg interface.

Block, but do not kill, the source-shaped native branch on:

- absent bosonic
  \(\mathscr S_{\epsilon}^{\rm bos}:
  \Omega^2(\operatorname{ad}P)\to\Omega^{13}(\operatorname{ad}^*P)\);
- undeclared \(\epsilon\)-variation of that map;
- undeclared defect-connection policy; or
- undeclared constrained tangent/multiplier system for the
  \(\lambda\ne1\) reference orbit.

Failure on the source \(Y^{7,7}/U(64,64)\)-type surface does not follow from a
native-map block, and failure of a generic fixed-plane full-\(Sp\) comparator
does not kill the stabilizer branch.

## Controls

Require rejection or correct disposition of:

- a spinorial Shiab inserted into the bosonic action;
- a frozen-current total-cancellation toy promoted to the field-dependent
  action;
- omitted \(D_\epsilon\Gamma\), \(D_\epsilon\mathscr S\), \(D_ZC\), or
  \(D_AJ_F\);
- dropped \(Q_F\) boundary flux;
- parent and eliminated shadow included simultaneously;
- sign-flipped \(+Z_U/2\) eliminated coefficient;
- a false \(P_{\rm IG}+q_F\) field equivalence;
- source residual square added beside the first-order action;
- both \(K\)- and \(C\)-Yukawa rival terms inserted at once;
- a source or N1 term registry that silently includes external P3;
- a five-leg owner not backed by an action, supplied-background,
  external-interface, or conditional-map registry;
- a Ward test that rejects wrong \((a,b)\) or \(\lambda\ne1\);
- a nonzero-boundary total derivative treated as zero;
- generic full-\(Sp\) fixed-plane motion promoted to the stabilizer; and
- any VEV, mass, index, or count inference.

## Held out

Zero-order placement, VEV, stationarity, curved solution, complete CME,
moving-full-\(Sp\) soldering, complete Diff closure, global domain, physical
reduction, physical mass, P3 pushforward, index, count, canon verdict, and
public posture remain held out.

## Planned validation

- compile and run the new RB2 probe;
- run the RB1, N1, N3, Weinstein-source, and post-N3 rebase regressions;
- run exact path/count and diff-hygiene checks;
- obtain a final hostile audit of formulas, grades, and architecture
  dispositions; and
- commit, push, and finish-verify only the declared scope.

## Completed disposition

RB2 emits exactly two N1 records to RB3:

1. `N1_JD_bridge`, at complete classical fixed-geometry antifield-zero
   five-field bridge-sector grade; and
2. `N1_total_current_bridge`, at the same grade with an algebraic
   \(A\)-Hessian bulk response and a total-only \(Z\)-Hessian Green flux.

The records are formally distinct and separated by a nondegenerate finite
control. No native solution-space inequivalence theorem is claimed.

The independent cyclic noncommutative fixture selects
\((a,b)=(1/2,1/3)\) from the translation-sector identities
\(2a=1,3b=1\). Homogeneous adjoint covariance remains coefficient- and
\(\lambda\)-blind, so it is not used as a selection argument. The native
source-shaped record is blocked first at the missing density-dual bosonic
Shiab map. Two native construction routes return through RB1 and RB2; the
source-real-form expression remains comparator-only. P3 remains an external
relative-\(KO\) interface.

This is not a complete N1 action, native Ward identity, Diff closure, CME,
domain theorem, VEV, physical mass, anomaly/index result, or generation
count.

## Correction log

- Ran Layer 0 before the seven axes and separated the proved spinorial
  Shiab map from the required bosonic density-dual action map.
- Removed a double-Hodge density-dual convention exposed by hostile review.
- Replaced coefficient arithmetic with an actual cyclic noncommutative
  variation fixture and a noncyclic failure plant.
- Added the explicit \(\epsilon\)-map response and allowed intrinsic
  proved-zero response as a separate policy.
- Corrected the constrained \(\lambda\ne1\) endpoint covector from \(E_B\)
  to \(E_B-E_\theta\).
- Downgraded finite homogeneous conjugation to a covariance proxy and split
  the N1 and conditional-source stabilizers.
- Downgraded parent elimination to a unique classical antifield-zero
  auxiliary-shell solution lift; no Green, Ward, or BV transport is claimed.
- Added per-leg grades, per-term connection arguments, separate
  action/supplied/external/conditional registries, boundary owners, and a
  mutation-tested action digest.
- Removed the false \(A\)-Hessian Green owner: its response is algebraic in
  the \(A\)-variation. The common \(S_{20}\) \(Q_F\) flux and total-only
  \(Z\)-Hessian flux remain.

## Validation completed

All commands exited zero:

- `tests/channel-swings/rb2_source_action_exactness_probe.py`
- `tests/channel-swings/rb1_source_repo_current_musical_probe.py`
- `tests/channel-swings/unified_source_datum_packet_v0_probe.py`
- `tests/channel-swings/actual_sym2_c14_orbit_probe.py`
- `tests/channel-swings/full20_curvature_irrep_open_bv_probe.py`
- `tests/channel-swings/n2a_n4a_intersection_probe.py`
- `tests/channel-swings/unified_source_variation_probe.py`
- `tests/channel-swings/weinstein_primary_source_reinspection_contract.py`
- `tests/channel-swings/post_n3_source_datum_ten_wave_rebase_contract.py`
- `process_gates/roadmap_current_routing_links_audit.py`
- `process_gates/explorations_readme_surface_map_audit.py`
- `process_gates/explorations_top_level_file_boundary_audit.py`
- `process_gates/tests_manifest_count_audit.py`
- `process_gates/tests_root_readme_inventory_audit.py`
- `process_gates/next_steps_frontdoor_guard_audit.py`
- `process_gates/source_action_readme_inventory_audit.py`
- `process_gates/changed_public_path_hygiene_audit.py`
- `git diff --check`

The live channel-swing count is 142. The frozen action-registry digest is
`8aa9601a2f1d6ab4ce2b8f25b622ce1217ffb13b00b1c4682e0bf9fbfb4c7d84`
and its legal-mutation plant changes the digest. Three independent hostile
reviews ended with no required blockers after the \(A\)-Hessian boundary
overcount was corrected.
