# Run Plan — Eric-lane ten-wave campaign and C0 execution

- Run ID: `GUH-20260731T181634Z-eric-curt-ten-wave-campaign-c0`
- Date: 2026-07-31
- Branch: `agent/weinstein-guided-source-action`
- Lane: 1 — Observerse/GU truth status
- Status: complete

## Controlling objective

Keep the independent native construction and the Eric-guided construction as
the two scientific lanes. Inside the Eric lane, execute Curt Jaimungal's
iceberg as a separately tagged rival/checklist track rather than creating a
third construction lane prematurely. Scaffold ten dependency-ordered waves
and execute Wave 1's C0 carrier bridge.

This run does not return to “a source action or external datum is missing.” It
starts from the built G1--G3 action/Euler/Ward/preboundary machinery and asks
which carrier that machinery can honestly inhabit.

## Source and construction tags

Every campaign assertion must use exactly one tag:

- `ERIC_REQUIRED` — directly required by a checked Weinstein source;
- `CURT_CANDIDATE` — supplied by Curt's secondary reconstruction;
- `PAIRED_IMPLICATION` — mathematically required when the two source surfaces
  are read together;
- `REPO_SYNTHESIS` — a concrete native construction proposed by this repo;
- `UNSUPPORTED` — a stronger claim with no current source or construction.

No tag changes a research grade by itself.

## Layer-0 precondition

| shared term | Curt candidate | active Eric-lane candidate | required disposition |
|---|---|---|---|
| rank-14 carrier | `C=VY+pi*T*X` with `(7,7)` form | `TY=H+V` with DeWitt/gimmel `(9,5)` form | split underlying bundle from metric/Clifford carrier |
| Zorro map | source-natural identification | connection-dependent musical plus horizontal split | type the dependence and naturality group |
| spinor | complex `U(64,64)`-type presentation | right-`H` `Sp(32,32;H)` reduction | compare complexification separately from real structure |
| action | source first/second-layer architecture | built G2/G3 field graph and candidate squares | no transfer before carrier-dependent operators are ported |
| Higgs | connection component | `varpi` versus `T=A-B` extraction fork | no scalar, pullback, or equal-dimension collapse |
| count | three carrier pieces | physical chiral index plus P3 | no decomposition-to-generation inference |

Layer 0 runs before all structural/no-go comparisons.

## Third-lane promotion gate

Curt remains a rival track within the Eric lane unless all three conditions
hold:

1. `TG-1 CARRIER`: an exact load-bearing non-equivalence survives convention,
   complexification, and source-typing checks;
2. `TG-2 DYNAMICS`: the Curt-specific carrier supports a coherent action,
   variation, gauge law, and observation map that cannot be represented as a
   rival inside the Eric lane; and
3. `TG-3 DISCRIMINATOR`: at least one downstream physical leg produces a
   different no-refit mathematical or empirical discriminator on the same
   comparison domain.

Signature mismatch alone can fire `TG-1`; it cannot create a lane without
`TG-2` and `TG-3`.

## Wave 1 question

Given a Lorentz metric `h` and a connection split of the metric bundle, does

`VY + pi*T*X --(1 + sharp_h)--> VY + pi*TX --Z_nabla--> TY`

identify:

1. the underlying real vector bundles;
2. their displayed real bilinear forms;
3. their real Clifford modules and reality structures; or
4. only their complexifications?

## Pre-registered expected verdict

Expected: the metric musical plus a chosen connection gives a rank-14 vector
bundle isomorphism. If the `(7,7)` and `(9,5)` signatures are literal real
forms, Sylvester inertia prevents a real metric isometry and the real Clifford
algebras remain inequivalent, while their complexifications admit a bridge.
That would fire `TG-1` only. The result should therefore keep Curt inside the
Eric lane and make the target-blind census carrier-parametric until dynamics
and a discriminator are built.

**Disposition correction after Layer 0.** Literal real `(7,7)` does establish
local non-equivalence to the active `(9,5)` carrier, but it supplies only
partial `TG-1` evidence: Curt's reported ordered blocks `(4,6)+(1,3)` do not
sum to `(7,7)` under one fixed convention. The source convention must be
cleared before `TG-1` itself can pass.

## Wave 1 kill and control conditions

The Wave 1 result is invalid if it:

1. identifies rank equality with metric or Clifford equivalence;
2. treats a connection-dependent horizontal split as canonical from a bare
   four-manifold;
3. calls complexified equivalence a real-form equivalence;
4. transfers right-quaternionic structure to the `(7,7)` branch without an
   intertwiner;
5. reads a signature fork as a generation count or physical ghost verdict;
6. creates a third lane from signature mismatch alone; or
7. forgets that Curt's `(7,7)` slide/exposition is secondary and explicitly
   correction-prone.

Planted controls must include a trivial vertical lift, a real-isometry claim,
a reality-preserving complex phase claim, and a signature-only lane promotion.

## Writable surface

- one ten-wave campaign specification under `lab/specifications/`;
- one machine campaign registry under `lab/process/`;
- one Wave 1 exploration result under `explorations/`;
- one exact Wave 1 probe under `tests/channel-swings/`;
- bounded index, roadmap, and prior-packet reconciliations;
- this run receipt and Brain memory after commit/push.

## Validation

- exact rational musical, trace-reversal, DeWitt-inertia, Lorentz-naturality,
  complexification, and promotion-gate checks;
- planted false claims;
- JSON parse and duplicate-key audit;
- Python compile;
- paired Curt--Eric graph, Curt crosswalk, Eric-native atlas, and source-action
  regressions;
- `git diff --check`, channel inventory, hostile consistency read;
- commit, push, memory, and canonical sync finish.

## Nonclaims

No carrier is dynamically selected. No full global Zorro naturality theorem,
action port, physical Hilbert/Krein completion, Higgs, Yukawa matrix, Standard
Model group, generation count, field equation, cosmology, or third lane is
claimed.

## Result

- ten dependency-ordered waves are scaffolded with five provenance tags;
- the underlying rank-14 bundles identify after `sharp_h` and a declared
  horizontal split;
- literal real `(7,7)` and active `(9,5)` metric/Clifford carriers do not;
- both complexify to `M(128,C)`, without transporting the active real,
  Krein, or right-`H` structure;
- `TG-1` has only partial evidence, `TG-2` and `TG-3` remain open, and no
  third lane is promoted; and
- the exact probe passes `36 exact + 9 planted = 45`.

All declared JSON, compile, neighboring-contract, source-reinspection,
source-action, target-blind RB6, and actual-fibre regressions pass. The Wave 2
port census ran immediately afterward under its separate receipt.
