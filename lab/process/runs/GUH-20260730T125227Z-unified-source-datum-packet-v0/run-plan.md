---
run_id: GUH-20260730T125227Z-unified-source-datum-packet-v0
status: completed
repository: gu-formalization
workflow: joe-directed-n1-unified-source-datum-construction
mode: execute
run_type: progress
lane_id: "1"
work_item: SOURCE-OWNED-CHIMERIC-BV-CAMPAIGN-N1
starting_revision: 35106bfb4346
opened_at: 2026-07-30T12:52:27Z
completed_at: 2026-07-30T13:44:00Z
claim_status_change: none
canon_change: none
public_posture_change: none
external_action_authorization: github_commit_and_push_only
write_boundary:
  - lab/process/runs/GUH-20260730T125227Z-unified-source-datum-packet-v0/run-plan.md
  - explorations/unified-source-datum-packet-v0-2026-07-30.md
  - tests/channel-swings/unified_source_datum_packet_v0_probe.py
  - explorations/README.md
  - tests/README.md
---

# N1 unified source-action and external-datum packet v0

## Authorization and decision question

Joe said “Go” after accepting the ten-persona campaign scaffold. This run
executes Swing N1 only:

> Can the current GU-owned pieces be assembled into one finite, explicitly
> typed source-action/datum candidate family carrying the Standard
> Model/Yukawa, Krein/BV, gravity/dark-energy, count/index, and UV/causality
> legs without target coding, cellwise coefficient fitting, or summing
> actions that live on different manifolds?

The need for a source action and external datum is the premise. A result that
merely rediscovers either need is inadmissible.

## Construction fork

Program-native objects are used where the repo has selected them:

- \(Y^{14}=\operatorname{Met}_{3,1}(X^4)\) with the actual
  \(\operatorname{Sym}^2T^*X\) fibre;
- `Cl(9,5)`, the native Krein form, and `Sp(32,32;H)`;
- the geometric gamma-traceless Rarita--Schwinger carrier and the \(g=1\)
  projector cure;
- the keep-and-grade ghost/BV posture; and
- the induced \(\lvert II\rvert^2\) gravity family.

Standard machinery is used where it binds either construction: currents of
embedded submanifolds, BV cotangent pairing, the real \(KO\) action on a
quaternionic Fredholm family, the quaternionic Hopf/Bott clutching class, and
bulk-plus-defect variational calculus.

The standard positive-Hilbert quotient, exterior `6+4` ten, `U(128)`,
ghost-subtracted gravitino, and an integer set to three are hostile controls.

## Collision and source check

Read before construction:

- repository governance, the geometer/physics fork, and Layer 0 plus L1--L7;
- the ten-persona N1 preregistration;
- the 27-row source-action requirements and term-by-term pass;
- the full-20 first write, native polarization, and DeWitt transport;
- the vertical source-action reduction/Hessian start;
- W125, W203, and W229 source-action partial builds;
- the P1/P2 corrected ledger and the current function-space/index boundary;
- the older untracked native-packet audit.

The old untracked run is completed-blocked and is superseded in several
finite-algebra fields by later full-20 work. It remains untouched and outside
this run’s write boundary.

## Construction target

Build a finite family

\[
\mathfrak C_0=
\left(
S_{\rm packet}^{(0)},
L_\sigma,
\widehat e_n,
\mathcal F_{\rm BV}^{(0)},
\mathcal B_0,
\mathcal R_0
\right)
\]

with:

1. a mathematically defined \(X^4/Y^{14}\) action-domain interface;
2. every classical and BV term written as an integrand or global cocycle;
3. distinct Krein-sesquilinear and charge-conjugation-bilinear branches;
4. a P1/P2 orientation interface and separately typed P3 topology family;
5. finite action, background, orbit, end, topology, reduction, regulator,
   higher-bracket, and rival-search budgets;
6. a frozen possible open-BV grammar that later work may narrow but not
   enlarge after reading an obstruction; and
7. a sealed held-out observable manifest.

## Pre-registered expected verdict

```text
UNIFIED-FINITE-CANDIDATE-WRITTEN
BULK-PLUS-DEFECT-ACTION-DOMAIN-WELD
P1/P2-ORIENTATION-AND-P3-KO-TWIST-SEPARATELY-TYPED
NO-FREE-CELL-FIT
NO-SCIENTIFIC-LEG-YET-SOLVED
```

## Kill and go conditions

**Kill N1** if any action term remains only a `T_i` label, an \(X^4\)
integrand is inserted into a \(Y^{14}\) integral without a map, literal
pullback is called vertical-scalar retention, P1/P2 and P3 are identified,
the topology family includes target `3`, one coefficient is introduced per
observer cell, either bilinear branch inherits the other’s symmetry without
a map, a positive-Hilbert/exterior-ten comparator replaces the native
construction, or one of the five legs is absent from the packet.

**Go to N2a/N4a/N3** if one finite family exists with all terms and
transformations typed, all choices charged, every requirement row assigned
an owner/interface, the action-domain map and datum coupling executable, and
the held-out wall sealed.

## Controls

- section-current equality on a finite bulk/defect fixture;
- literal pullback erases a vertical covector while coefficient restriction
  retains it;
- primitive/doubled orientation holonomy gives `-1/+1`;
- Hopf clutching degrees `-1/0/+1`, with target `3` rejected;
- all selector rivals have one active real selector coordinate;
- missing-leg, duplicate-datum, exterior-ten, raw-domain-sum,
  cross-bilinear-transfer, free-cell, and target-coded-count plants fail;
- BV monomials have total ghost number zero;
- the frozen open-BV grammar is finite; and
- changing any held-out value cannot change the sealed construction hash.

## Planned validation

- Run the new packet probe and its planted controls.
- Re-run the source-action requirements consistency probe.
- Re-run the vertical--Krein, full-20 native-polarization, and DeWitt-loop
  transport controls.
- Run Python compilation and whitespace checks.
- Review the full action, datum, Layer-0, L1--L7, parameter, requirement, and
  held-out ledgers adversarially.
- Commit and push only the scoped files, then close with the scoped
  repository-session guard.

## Result

```text
UNIFIED-FINITE-CANDIDATE-WRITTEN
BULK-PLUS-DEFECT-ACTION-DOMAIN-WELD
P1/P2-ORIENTATION-AND-P3-KO-TWIST-SEPARATELY-TYPED
NO-FREE-CELL-FIT
NO-SCIENTIFIC-LEG-YET-SOLVED
```

The final construction is a finite-grammar family, not a claim of finite
functional dimension. It writes:

- one bulk-plus-defect action on \((Y,sX)\), using \(s_*\) rather than a raw
  \(X^4/Y^{14}\) sum;
- the full-20 mapping-torus carrier, its separately typed \(K\) and
  \(C_{\varepsilon_C}\) bilinear branches, and a semidirect BV skeleton;
- the P1/P2 loop cocycle on \(S^1_{\rm DW}\);
- a target-blind relative-\(KO\) P3 input
  \(\nu^*\widehat e_n\), \(n\in\{-1,0,+1\}\), with an explicit support map,
  boundary trivialization, tensor-connection comparator, and global
  curvature selector; and
- separately charged scalar, section-valued, topology, domain, regulator,
  open-BV, and discrete search freedom.

The construction hash is:

```text
1efdffd34e3ad5358fed16c08cda9ecf681df676e817560bf36b436d79658ffb
```

The executable contract passes 73 checks.

## Corrections fired before sealing

The planted free-cell matcher initially admitted `cell_37`; its regex was
corrected before reading the verdict. The hostile pass then removed or
replaced:

- an unmapped auxiliary \(S^4\);
- direct placement of \(L_\sigma\) on \(Y\) and loss of the non-scalar
  mapping-torus transport;
- physical use of a virtual \(KO\) pair and an impossible global
  \(H_n\cong\underline{\mathbb R}^4\) selector;
- a double-counted classical/BV action;
- a wrong W203 bridge sign;
- an unspecified \(V_{\rm src}\);
- direct Clifford multiplication of a Lie-valued covector;
- the \(\epsilon_{\rm IG}/\varepsilon_C\) notation collision;
- hidden section-valued background freedom;
- double accounting of \(m_R\); and
- the false \(10\times3\) BV word count, replaced by the exact chosen-grammar
  ceiling \(210(10+10^2+10^3)=233{,}100\).

## Validation receipt

Passed:

- `tests/channel-swings/unified_source_datum_packet_v0_probe.py` (73/73);
- `tests/spec-consistency/source_action_requirements_consistency.py`;
- `tests/channel-swings/vertical_krein_weld_probe.py`;
- `tests/channel-swings/full20_native_polarization_probe.py`;
- `tests/channel-swings/full20_dewitt_loop_transport_probe.py`;
- `tests/W229_source_action_znu_completion.py` (39/39);
- `tests/channel-swings/vertical_source_action_reduction_probe.py` (11/11);
- `tests/channel-swings/full20_chimeric_bv_first_write_probe.py`;
- `tests/W125_built_candidate_assembly.py`;
- `tests/W125_sac4_subprincipal_built.py`;
- Python compilation of the new probe;
- exploration/test manifest, research-posture, public-path, repository
  entrypoint, and portfolio process gates; and
- `git diff --check`.

`process_gates/lab_process_readme_surface_map_audit.py` remains red on the
pre-existing process-surface inventory drift (`runs/` plus six already
tracked direct files absent from its expected set). This run did not modify
that process README or gate and does not treat the unrelated failure as
scientific evidence.
