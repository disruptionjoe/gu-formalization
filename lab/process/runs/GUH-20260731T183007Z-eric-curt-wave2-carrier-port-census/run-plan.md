# Run Plan — Eric/Curt Wave 2 carrier-port census

- Run ID: `GUH-20260731T183007Z-eric-curt-wave2-carrier-port-census`
- Date: 2026-07-31
- Branch: `agent/weinstein-guided-source-action`
- Lane: 1 — Observerse/GU truth status
- Campaign wave: `ECW2-G3.5-CENSUS`
- Status: complete (scientific Wave 2 remains partial)

## Controlling question

After Wave 1 separates the underlying rank-14 bundle from the literal real
metric/Clifford carriers, which parts of the already-built IG/action grammar
transfer to every surviving carrier reading, which require a branch-specific
port, and which exist only on the active right-quaternionic branch?

This is the first half of Wave 2. It freezes the carrier-port matrix before a
full term-rank and constraint-surplus computation. It reuses RB6's
target-blind grammar rather than proposing physics-labelled terms.

## Layer-0 precondition

The census separates:

- a formula schema from an evaluated operator;
- existence of a branch-native primitive from equality with the active
  primitive;
- complexified existence from preservation of a real/Krein/right-`H`
  structure;
- an invariant-concomitant `H` from a physical Hessian;
- a commutator `Q` from charge conjugation or a selected complex structure;
- action grammar from a stationary action solution.

## Branches

1. `R95_ACTIVE`: `(9,5)`, `Cl(9,5)=M(64,H)`, active right-`H` packet.
2. `R77_BASE_FLIP`: total `(7,7)` by reversing the base block.
3. `R77_VERTICAL_FLIP`: total `(7,7)` by reversing the vertical block.
4. `C14_COMMON`: common complexification `M(128,C)` with no selected real
   structure.

The two `(7,7)` rows are comparators generated by the Wave 1 source ambiguity,
not claims about Curt's intended convention.

## Pre-registered expected verdict

Expected: affine IG/group/cocycle/distortion identities and the schematic
first-order action grammar transfer. Metric, Hodge, Clifford, Krein/reality,
Shiab, pseudo-musical, Euler-dual, Green, Ward, and spectral realizations are
branch-native and require explicit ports. The right-`H` structure exists only
on the active branch unless separately reconstructed. The common complex
container cannot decide physical adjoints or reality.

## Kill conditions

The census fails if it:

1. marks a primitive `SHARED_EXACT` merely because its formula has the same
   spelling;
2. transfers the active right-`H` structure to a real `(7,7)` comparator;
3. calls complexification a physical pairing/reality selection;
4. imports a flag, Standard Model group, Higgs doublet, count, P3, or target
   projector into RB6's grammar;
5. calls an unevaluated field-Gram formula a stationary Hessian;
6. treats a vanishing isotropic commutator as absence of all action-field
   commutators; or
7. computes a constraint surplus before the independent term rank and all
   branch-specific choices are charged.

## Outputs

- machine-readable carrier/primitive port census;
- concise Wave 2 partial result;
- deterministic dependency/status probe with planted false transfers;
- campaign/index/roadmap reconciliation.

## Exit

Wave 2 may be marked only `PARTIAL` unless it also computes the full
carrier-specific term-space rank, coefficient count, independent constraint
rank, and ablation matrix. The next Wave 2 swing must do that computation on
the surviving branch-native primitives.

## Nonclaims

No carrier, action, coefficient, background, physical mode, third lane, field
equation, Higgs, generation count, anomaly result, or cosmology is selected.

## Result

- 22 primitives are classified over four carrier readings;
- `CP-01`, `CP-04`, `CP-11`, `CP-12`, and `CP-13` form the exact portable
  vector-bundle/trace/affine core;
- every metric/Hodge/Clifford/Krein/reality/action-pairing/adjoint/residual
  realization is charged as conditional, schema-only, branch-native, or
  active-built;
- equal Hodge-square parity is explicitly separated from equality of Hodge
  operators;
- `SURPLUS_UNCOMPUTABLE` remains mandatory; and
- the exact probe passes `30 exact + 8 planted = 38`.

The run is complete, but the scientific Wave 2 exit is not reached. The next
bounded swing is `ECW2b-TERM-RANK-ABLATION`: complete monomial generation,
identity quotient, coefficient and constraint rank, and target-blind ablation.
