---
run_id: GUH-20260730T191725Z-rb1-source-repo-current-musical
status: complete
repository: gu-formalization
workflow: direct-chat-construction-swing
mode: execute
lane_id: "1"
work_item: RB1-SOURCE-REPO-DICTIONARY-DEPENDENCY-DAG-CURRENT-MUSICAL
starting_revision: 85edd29d5202
opened_at: 2026-07-30T19:17:25Z
write_boundary:
  - NEXT-STEPS.md
  - explorations/README.md
  - explorations/rb1-source-repo-current-musical-2026-07-30.md
  - lab/process/runs/GUH-20260730T191725Z-rb1-source-repo-current-musical/run-plan.md
  - tests/README.md
  - tests/channel-swings/rb1_source_repo_current_musical_probe.py
claim_status_change: none
canon_change: none
public_posture_change: none
closed_at: 2026-07-30T20:22:19Z
---

# RB1 source/repository dictionary, dependency DAG, and current musical

## Objective

Execute RB1 of the post-N3 source-action/external-datum campaign. Construct,
rather than merely request:

1. a Layer-0 dictionary between Weinstein's source objects and the inherited
   N1 repository objects;
2. an exact dependency DAG for the N1 action and its first variations;
3. density-dual formulas for the fermion and defect currents, including
   Green terms; and
4. the native-real connection-current musical induced by the gimmel metric,
   Hodge/density convention, and invariant adjoint pairing.

The output must freeze a finite RB2 architecture family or emit the bounded
geometry-return packet required by the campaign.

## Frozen inputs

- `explorations/post-n3-source-datum-ten-wave-rebase-2026-07-30.md`;
- `explorations/unified-source-datum-packet-v0-2026-07-30.md`;
- `explorations/n2a-n4a-intersection-handoff-2026-07-30.md`;
- `explorations/unified-source-datum-variational-emission-map-2026-07-30.md`;
- `explorations/weinstein-primary-source-reinspection-overlooked-answers-2026-07-30.md`;
- the checked source/transcript packet cited by that reinspection; and
- the fixed datum ledger: one P1/P2 orientation line, a separate action-domain
  weld, and a separate P3 relative-\(KO\) comparator with no index or count.

## Layer-0 precondition

Before comparing formulas, type each shared term by domain, codomain, real
form, exterior degree, density dual, invariant pairing, gauge law, and
varied/fixed status. At minimum adjudicate:

- source \(\epsilon\) versus moving \(\epsilon_{\rm IG}\);
- source \(\varpi\) versus the full N1 field tuple;
- source \(B_\omega\) versus \(A\);
- source \(T_\omega\) versus \(\theta\);
- source \(\Upsilon^B\) versus \(\mathcal E_A\);
- source \((\Upsilon^F)_{\rm ad}\) versus \(J_D\), \(J_F\), and
  \(J_D+J_F\);
- source \(\Xi=D_\omega\Upsilon\) versus an off-shell Noether identity; and
- source spin-zero components versus the future VEV and physical mass.

Use `SAME`, `DIFFERENT`, `CANDIDATE`, or `ABSENT`; no load-bearing homonym
may remain unnamed.

## Construction branch and comparators

The active branch uses the program-native
\(\operatorname{Sym}^2T^*X\), \(Cl(9,5)\), \(Sp(32,32;\mathbb H)\),
geometric gamma-traceless projector, indefinite Krein pairing, and induced
gravity construction. Exterior-ten, complex \(U(128)\), positive-Hilbert,
and standard ghost-subtracted objects are controls or typed forks, not silent
substitutions. A no-go must identify the branch on which it bites and be
rechecked on the other branch before it is generalized.

## Pre-registered expected verdict

Expected before calculation:

- \(B_\omega\) and \(A\) should be at most a candidate identification after a
  common native-real arena is supplied;
- \(T_\omega\) and N1 \(\theta\) should be different objects related, if at
  all, by a typed projection or composite map;
- \((\Upsilon^F)_{\rm ad}\) should not be identified by prose with either
  \(J_D\) or \(J_D+J_F\);
- a connection-current musical should exist formally on a nondegenerate
  metric and invariant-pairing branch, but its native-real/global
  compatibility may remain conditional;
- false dependency flags should vanish exactly, while true dependencies
  should expose the missing moving-projector/restriction terms; and
- the result should narrow RB2 without selecting an action by source
  authority.

## Kill conditions

Kill only the source-shaped comparison if:

- \(T_\omega\) cannot map into the N1 field arena without changing its
  representation, degree, or reality class;
- \((\Upsilon^F)_{\rm ad}\) cannot land in the native connection Euler dual;
- the required invariant pairing or connection-current musical is degenerate
  on the native real form; or
- the source comparison requires a silent physics-default substitution.

Do not kill the independent N1 construction merely because the source
dictionary fails. If all frozen action architectures become ill-typed, emit
the campaign's bounded geometry-return packet and reenter at the earliest
invalidated wave.

## Controls

Plant and require rejection of:

- a false direct \(A\)-dependency;
- a wrong Hodge degree;
- a positive optimizer metric substituted for
  \(G/\kappa_{\mathfrak g}\);
- a dropped Green or boundary term;
- a silent \(U(64,64)\) or \(U(128)\) replacement of
  \(Sp(32,32;\mathbb H)\);
- a coefficient reparameterization counted as new evidence;
- a Krein spinor pairing used as the connection musical;
- an early P3/index/count inference; and
- a source formula promoted to a repository identity by authority.

## Held out

RB1 does not select the bridge, prove exactness, place the zero-order VEV,
establish stationarity or a global domain, compute an index, infer a
generation count, move canon, or change public posture.

## Planned outputs

- `explorations/rb1-source-repo-current-musical-2026-07-30.md`;
- `tests/channel-swings/rb1_source_repo_current_musical_probe.py`;
- navigation and next-step updates; and
- this completed run record with the exact RB1 disposition.

## Completion disposition

`RB1-GO` was earned without selecting an action. The swing built:

1. a complete required-arrow Layer-0 register while leaving
   source/repository alignment unresolved;
2. the formal affine family
   \(A_\lambda=A-\lambda U\),
   \(B_\lambda=\Gamma_{\rm conn}+(1-\lambda)U\), with
   \(\lambda=1\) uniquely preserving the source's displayed
   \(\epsilon\)-only dependency and \(\lambda\ne1\) retained as a charged
   reference-orbit control family;
3. the N1 varied-primal Euler-root DAG plus coefficient, map, supplied-field,
   background-response, formal-adjoint, Green, and alias owners;
4. the fixed-geometry/fixed-projector variation of all nine full-20 blocks,
   including the graded
   \(\widehat J_D/\widehat Q_F/\widehat J_F\) split; and
5. the native indefinite
   \(G/\kappa_{\mathfrak g}\) connection-current pseudo-musical at pointwise
   and compact-core weak grade.

RB2 receives three candidates—`N1_JD_bridge`,
`N1_total_current_bridge`, and `source_reference_lambda1`—plus the
`source_orbit_constrained_lambda_not1` hostile control family. The
Spin/stabilizer arena is frozen; moving-full-\(Sp\) soldering is held to RB3.

## Appended correction to the preregistered datum wording

The frozen-input list above recorded the then-current ledger as one P1/P2
orientation line, an action-domain weld, and P3. Hostile review showed that
the \(s_!\) and \(s_*\) weld and stratified measure are action infrastructure,
not external datum. The corrected external-datum ledger is one P1/P2
orientation line plus the separate
\(P3=(\nu,\widehat e_n,n)\) relative-\(KO\) comparator. This correction is
appended rather than rewriting the preregistration.

## Hostile-review corrections incorporated

Three independent specialist reviews attacked the construction before
closure. Their accepted corrections include:

- source attribution of \(A_\omega=B_\omega+T_\omega\) downgraded to
  `DERIVED-RECONSTRUCTION`;
- two hand-picked affine endpoints replaced by the full \(\lambda\)-family;
- duplicate-path validation hardened with a frozen typed-ledger digest;
- induced-metric, moving-support, shape-derivative, and BV
  derivative/Green orders repaired;
- the vector-spinor \(A\)-variation and all nine full-20 block derivatives
  checked by a finite-difference fixture;
- the density-dual covariant derivative, \(2/12\)-form Hodge sign, and
  fourteen-dimensional graded Green identity repaired;
- internal-\(Sp\) and Diff Ward contracts separated;
- every architecture given a concrete five-leg matrix, action schema,
  frozen symmetry, and complete held-out wall; and
- the four emitted records classified as three candidates plus one control.

## Validation

All commands exited `0`:

- `python3 -m py_compile tests/channel-swings/rb1_source_repo_current_musical_probe.py`;
- `python3 tests/channel-swings/rb1_source_repo_current_musical_probe.py`;
- `python3 tests/channel-swings/unified_source_datum_packet_v0_probe.py`;
- `python3 tests/channel-swings/n2a_n4a_intersection_probe.py`;
- `python3 tests/channel-swings/unified_source_variation_probe.py`;
- `python3 tests/channel-swings/weinstein_primary_source_reinspection_contract.py`;
- `python3 tests/channel-swings/post_n3_source_datum_ten_wave_rebase_contract.py`;
  and
- `git diff --check`.

The post-N3 rebase contract intentionally verifies its earlier frozen
ledger wording; the appended RB1 correction above and the RB1 artifact are
the later disposition. No canon claim, formal verdict, or public posture
changed.
