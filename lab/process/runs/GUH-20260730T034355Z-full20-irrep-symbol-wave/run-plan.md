---
run_id: GUH-20260730T034355Z-full20-irrep-symbol-wave
status: completed
repository: gu-formalization
workflow: joe-directed-north-star-construction
mode: execute
run_type: progress
lane_id: "1"
work_item: SOURCE-OWNED-CHIMERIC-BV-CAMPAIGN-S2-BRIDGE
starting_revision: a27a885b543b
opened_at: 2026-07-30T03:43:55Z
completed_at: 2026-07-30T04:23:49Z
claim_status_change: none
canon_change: none
public_posture_change: none
external_action_authorization: github_commit_and_push_only
write_boundary:
  - lab/process/runs/GUH-20260730T034355Z-full20-irrep-symbol-wave/run-plan.md
  - explorations/full20-irrep-symbol-noether-wave-2026-07-29.md
  - tests/channel-swings/full20_irrep_symbol_noether_probe.py
  - explorations/README.md
  - tests/README.md
---

# Full-20 irrep-symbol and Noether-defect wave

## Authorization and purpose

Joe asked to orchestrate and run the next wave of swings after S1. This Run
executes the cheapest dependency-bounded continuation:

1. compute the actual generic principal-symbol support of the nine carrier
   maps and three gauge maps on the exact 20-slot observer ledger;
2. apply the all-slot, connectivity, and four-joint-orbit gates; and
3. only if the support survives, compute the action-derived quadratic
   Noether defect far enough to type the next compensator demand.

The Run does not select coefficients from P1, P2, P3, a desired cohomology,
the `3E+` retract, a generation count, or a preferred four-orbit witness. It
does not solve the full quadratic master equation, construct the roll maps,
or promote the relative fixed-\(A\) census to a full ambient BV theory.

## Collision and owner check

The completed S1 owner is
`explorations/full20-chimeric-bv-first-write-2026-07-29.md` at revision
`a27a885`. The completed untracked
`GUH-20260729T131135Z-b5-native-packet-source-audit` is outside this Run's
write boundary and remains untouched.

Existing representation and finite-Clifford results are inputs, not new
claims:

- the exact 20-slot, rank-1920, 136-cell allowed symbol ledger;
- the 39 adjoint/mirror support orbits;
- the exact gamma-trace split \(V\otimes S=I\oplus R\);
- the finite vertical projected-RS nonzero controls on both \(X\) families;
- S1's nine carrier maps and three gauge maps; and
- the fixed-\(A\) relative BV arrow schema.

## Layer 0

| shared term | object in this Run | object it must not be collapsed with |
| --- | --- | --- |
| support | nonzero projections of the twelve written symbol tensors | the 136-cell representation-theoretic allowed envelope |
| generic symbol | the tensor is nonzero, equivalently nonzero on a Zariski-open set of covectors | invertibility or exactness for every non-null covector |
| carrier operator | \(D_{\mathbf c}:\mathcal E\to\mathcal E\) | the density-dual Euler operator \(H_{\mathbf c}\), \(q_U\), or \(Q_{\rm BV}\) |
| raw carrier composition | \(D_{\mathbf c}R_{\mathbf r}:S\to\mathcal E\), computed as a necessary principal pre-screen | the action-derived gauge-degeneracy test \(H_{\mathbf c}R_{\mathbf r}\) |
| connectivity | graph connectivity of actual irrep support | Noether closure, nilpotency, a physical quotient, or generation count |
| adjoint | algebraic/complex symbol transpose until the native packet is composed | the final Krein formal adjoint on a common closed domain |
| Noether defect target | \(H_{\mathbf c}R_{\mathbf r}\), not computed because graded polarization is unfrozen | the raw composition, or a compensator selected from desired support or cohomology |

The Layer-0 verdict across the table is
`DISTINCT-OBJECTS/NOT-IDENTIFIED`. Each computed object is meaningful only at
its explicitly named complexified generic-symbol or fixed-\(A\) carrier
grade. Native real/Krein, common-domain, full ambient gauge, and
rolled-complex identifications remain `UNCERTAIN`, not free passes.

No new substrate candidate is proposed, so the ratified L1--L7 tuple is not
changed. L7 remains the program-native indefinite/Krein branch and is not
replaced by a positive-Hilbert quotient.

## Construction fork

The tested object uses:

- \(Y^{14}=\operatorname{Met}(X^4)\) with
  \(TY=TX\oplus\operatorname{Sym}^2T^*X\), not the exterior `6+4`
  numerical comparator;
- the `Sp(32,32;H)`/Krein keep-and-grade construction, not `U(128)` or
  positive-Hilbert ghost removal;
- the geometric gamma-traceless Rarita--Schwinger carrier, not the
  ghost-subtracted gravitino;
- separate base and gimmel metrics; and
- no count/index reading from support, orbits, or multiplicities.

Any kill must state whether it belongs only to this native construction or
also survives the standard-physics comparator.

## Preregistered expectation, kill, and go

Before the new computation, the expected result is:

```text
ACTUAL-FORMULA-SUPPORT-STRICTLY-SMALLER-THAN-136
CANONICAL-CORE-ALL-SLOT-AND-CONNECTED-LIKELY
GENERIC-QUADRATIC-NOETHER-DEFECT-NONZERO-LIKELY
```

The expectation is not a target. The exact cell set, orbit count, coefficient
links, and defect equations are held out.

**Support kill:** terminate the S2 critical path if every nonzero
support-level transpose/mirror-closed stratum of the written natural-map
family is disconnected, omits an \(X\) family, has fewer than four off-core
joint orbits, or becomes connected only by one coefficient per desired irrep
cell or a retrospective projector. Native adjoint-compatible strata are not
enumerable until the five-field Krein/Green/domain packet is constructed.

**Support go:** retain every finite coefficient stratum whose actual symbol
support covers all 20 slots and connects the observer graph. Do not crown a
stratum by elegance.

**Noether kill:** if the defect cannot be typed without silently fixing the
native Krein adjoint, full ambient gauge semantics, or target-selected
compensators, return `NOETHER-DEFECT-BLOCKED-BY-NATIVE-PACKET` rather than
manufacturing closure.

**Noether go:** expose the independent defect components and derive the
smallest naturality/degree representation class any compensator would have to
occupy. Do not select its coefficient or support retrospectively.

## Parallel wave

- representation branch: independently derive all twelve formula supports;
- source-action branch: derive \(H_{\mathbf c}R_{\mathbf r}\) and the
  compensator typing barrier;
- hostile branch: search for duplicate owner results and audit every
  Layer-0 transfer;
- integrator: build one executable certificate, reconcile disagreements, and
  run planted controls.

The branches are read-only until integration. Their syntheses do not replace
their conflicting inputs.

## Controls and validation

1. Reproduce the independent 20/1920/136 ledger but never use it as the
   formula-support answer.
2. Separate horizontal and vertical symbol components; plant horizontal-only
   and vertical-only covectors.
3. Require all eight \(X\) irreps and both product-rule \(X\) families.
4. Plant omission of \(X\), collapse of `S`/`imGamma`/low-`kerGamma`
   provenance, and a formula-blind sector-envelope matcher.
5. Verify support using both representation multiplicities and explicit
   finite Clifford/gamma-trace matrices where feasible.
6. Distinguish tensor-nonzero/generic support from all-covector rank,
   invertibility, and exactness.
7. Compute graph components and joint mirror/transpose orbits on actual
   support.
8. Check coefficient-link invariance under basis rephasing and relabeling of
   the four \(H\)-types within each of the three provenance sectors.
9. Derive the carrier composition first and proceed to the Noether defect
   only if the native packet permits it; reject a hand-written zero residual
   or a compensator selected by the desired four bridges.
10. Compile and run the new probe, the exact observer ledger, Krein/mirror
    orbit reduction, S1 probe, and relevant finite Clifford controls; run
    index gates and `git diff --check`.

## Held-out wall

Forbidden selectors and conclusions:

- P1/P2/P3 or endpoint transport;
- desired `3E+` versus `3E-` cohomology;
- generation, family, or chiral-index counts;
- a chosen stationary background or mass;
- lexicographic selection of four support orbits;
- positive-Hilbert replacement of the Krein pairing; and
- treating a connected support graph as a master-equation or physical pass.

## Execution and result

The preregistered strict-subset expectation flipped:

```text
FULL20-FORMULA-SUPPORT-SATURATES-136
FOUR-PRIMITIVE-SYMBOL-FAMILIES
RAW-PRINCIPAL-CARRIER-DET-M-LOCUS-NONEMPTY
NATIVE-NOETHER-BLOCKED-BY-FIVE-FIELD-PACKET
NO-NEW-COMPENSATOR-YET-DEMANDED
```

At the generic complex observer grade, with nonzero base and
symmetric-fibre covector projections, the analytically derived nine-block
carrier manifest occupies all 136 independently allowed cells:
68 base plus 68 fibre. The blocks are placements of four primitive symbols
\(c,T,\delta,Q\), linked by eight open principal coefficients and the fixed
normalization of \(Q\). The gauge-map manifest has 32 cells and reaches all
20 targets.

The full formula graph is connected. Within support-level
transpose/mirror-closed strata with fixed \(Q\), every two-paired-bridge
choice connects all 20 slots and adds 12 or 16 joint orbits beyond the
13-orbit \(Q\) core. These are support results only.

The exactly computable raw principal composition is

\[
D_{\mathbf c}R_{\mathbf r}
=
\left(F_Sq,F_Ijq,F_RU_\xi\right)
\]

with

\[
\begin{aligned}
F_S&=ar_S+b_{SI}r_I-\frac{13}{14}b_{SR}r_R,\\
F_I&=b_{IS}r_S-\frac67dr_I+\frac{13}{7}b_{IR}r_R,\\
F_R&=b_{RS}r_S+\frac17b_{RI}r_I+\frac67r_R.
\end{aligned}
\]

Equivalently, a nonzero raw kernel vector exists on
\(\det M(\mathbf c)=0\). This is generically a complex dimension-7
hypersurface in the eight carrier coefficients. The unquotiented joint
\((\mathbf c,\mathbf r)\) incidence locus has local complex dimension 8 at
the all-nonzero rational witness; overall \(\mathbb C^*\) scaling of
\(\mathbf r\) is redundant.

This does not compute \(H_{\mathbf c}R_{\mathbf r}\) or
\(R_{\mathbf r}^{!}H_{\mathbf c}\). The five-field native
Krein/polarization/Green-domain packet remains unfrozen. The lower-principal
remainder is a five-map spanning list plus \(\mu_0r_RT\); the latter is not
an independent \(\mu_0r_R=0\) equation.

## Control disposition

Implemented:

- explicit base and fibre edge manifests for \(c,T,Q\), compared with the
  independent 136-cell representation ledger;
- exact low-\(R\) projected-\(Q\) coefficients \(-9/14\) and \(-33/35\), plus
  nonzero low-to-\(X\) projection coefficients;
- all nine linked-family deletion controls, including planted removal of
  fixed \(Q\);
- horizontal-only and vertical-only 68-cell/four-component controls;
- omitted-\(X\) and collapsed-provenance failures;
- exact incidence, support-level orbit, and paired-bridge strata;
- intrinsic Clifford identities in dimensions 4, 8, and 14, including the
  actual \((3,1)+(6,4)\) observer sign split;
- a planted base-plus-fibre null covector showing why null-only sampling
  cannot certify the three raw equations;
- an all-nonzero rational kernel vector, coefficient perturbation, and
  vacuous-\(r=0\) rejection; and
- P1/P2/P3/count/retract API fences and refusal of the unfrozen native
  packet.

Partially implemented or unrun:

- The support edge lists are an analytic derivation plus executable manifest
  consistency check. The Run did not construct all 20 observer projectors
  and recompute every \(U_t^*\sigma(e^a)U_s\); that remains an explicit
  hardening follow-up.
- The preregistered formula-blind zero-map matcher was not represented as a
  fake passing control. Instead, the evidence grade was downgraded and the
  missing 20-projector audit was recorded.
- Relative observer-irrep amplitudes, basis rephasing invariance, and
  type/mirror-preserving relabeling invariance were not computed. They are
  not used in the support verdict.
- Native adjoint-compatible coefficient strata were not enumerated because
  the five-field packet remains missing.

## Validation

Passed:

- `python3 -m py_compile tests/channel-swings/full20_irrep_symbol_noether_probe.py`
- `python3 tests/channel-swings/full20_irrep_symbol_noether_probe.py`
- `python3 tests/channel-swings/full20_chimeric_bv_first_write_probe.py`
- `python3 tests/shiab_b5_observer_symbol_multiplicity_matrix.py`
- `python3 tests/shiab_b5_krein_mirror_orbit_reduction.py`
- `python3 tests/shiab_b5_native_packet_contract.py`
- `python3 tests/channel-swings/vertical_krein_weld_probe.py`
- `python3 tests/channel-swings/b5_normalized_transport_from_differential_probe.py`
- the exploration surface/top-level, test-manifest, changed-public-path, and
  protected-surface process gates; and
- `git diff --check`.

Three independent hostile rereads corrected the initial draft's action/carrier
collapse, projector-evidence overgrade, unquotiented-dimension headline,
\(\mu_0r_R=0\) overreach, signature placement, Noether direction, and
compensator rule before this receipt was frozen.

## Receipt

- Service outcome: `completed_conditional`.
- Preregistered expectation: formula support strict subset — `FLIPPED`.
- Positive result: analytic unpolarized carrier manifest saturates all 136
  allowed cells and the raw `det M(c)=0` locus is nonempty.
- First residual: the five-field native Krein/polarization/Green-domain
  packet.
- Second residual: a full 20-projector machine rederivation of the analytic
  support manifest.
- Scientific grade: active research; no claim, canon, paper, or public
  posture change.
- Held-out data: P1, P2, P3, retract, cohomology, count, and endpoint signs
  remained unused.
- External action: scoped branch commit and push only.
