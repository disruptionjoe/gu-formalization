---
title: "NAMES: the homonym disambiguation table (grown on collision, not by ritual)"
status: process
doc_type: names-registry
created: 2026-08-10
maintenance: "Add an entry WHEN a same-name collision is found — no obligation to pre-register symbols.
  Entries carry TYPE and per-file sense, because the collisions that bit were type confusions, not
  spelling confusions. Eight seeded from the 2026-08-08..10 sessions."
---

# NAMES — same name, different objects

| name | sense 1 (type; where) | sense 2+ (type; where) | bite |
|---|---|---|---|
| `Met(X^4)` | space of **Riemannian** metrics: convex cone, **contractible** (used in `canon/no-go-class-relative-map.md` Sec 2.3) | space of **Lorentzian** metrics = `Y14`: fibre `GL(4,R)/O(3,1)`, homotopy `RP^3 x R^+`, **non-contractible** (Sec 2.1, `w2-y14`) | D8: Riemannian fact applied under Lorentzian name, load-bearing for the FH Option-B elimination |
| "the 14" | `Y14 = Met(X4)`: **total space**, `4 + 10` symmetric fibre | exterior-grading count `Lambda^1+Lambda^2+Lambda^3` (**totals 18** over a 4-base) | coefficients silently crossing a fork; no natural `GL(4)` iso `S^2 =~ Lambda^2+Lambda^3` |
| "the metric" | base metric `g` on `X^4` (Lorentzian, 4d) | DeWitt/gimmel fibre metric on `Y14` (metric-on-metrics, `(7,3)->(6,4)`) | two objects, one word, related by an action; conflation is the classic bug |
| `C` / "the parity" | ghost parity: **LINEAR** involution — the Krein form `K = eta_V (x) beta_S` implementing the Cartan involution | (b) PT/Krein **C-operator** (existence near-closed-negative, 2026-07-06); (c) **antilinear chiralizer** `C = J_quat . G` (frame charge exactly 0); (d) monodromy **coflip** `C_perp = K J_obs` (the `C -> -C` transport) | four objects, one letter; the `0.00e+00` frame charge belongs to (c) only and does NOT transfer to (a) |
| `sigma` | the **section** `sigma : X4 -> Y14` (a chosen metric per point; `pi . sigma = id`) | the **orientation / time-reversal ledger bit** (external datum; the `sigma` in DU's `sigma = w1` posit) | which sense `sigma = w1` means changes the claim entirely — a section is not an orientation class |
| "carrier" | the supplied fixed 192-dim `W` (`j=1` sector) — **conditional input**, not derived (CARRIER-20260810) | `ker(Gamma)` matter content generally (e.g. the 40 SM-neutral states: 32 in 16-type, **8 in 144-type — provably NOT in W**) | the 2026-08-10 canon byproduct sentence was false under sense 1 |
| `SIGNATURE-AMBIENT` vs `REAL-CLIFFORD-FORM` | ambient signature fork `(9,5)` vs `(7,7)` — **OPEN**, depth 10 over threshold | which real Clifford algebra **the source computes in** — settled `Cl(7,7)=M128(R)` 2026-08-04, rationale corrected 2026-08-08 (author-asserted; K95 = geometry-derived comparator) | registry says "Distinct" in so many words; conflated twice by two authors in one month |
| class `C` | `C_fin`: the certified 7-row census (enum-completeness, post-V15-2) | `C_inv`: the semantic invariant class (NOT certified); also "category C" elsewhere in the same file | the externality chain's strength depends on which one a sentence means |
