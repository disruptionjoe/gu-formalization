---
artifact_type: research_assessment
created: 2026-08-08
status: historical_native_evidence
subject: conditional-build authorial dependencies
claim_or_verdict_change: none
---

# Authorial dependency assessment

This native assessment preserves the scientific content of a 2026-08-08
deliberation without retaining its private review-orchestration record.

## Finding

Depending on Weinstein's stated `(1,3)` base is appropriate for a
formalization program. The defect was not the dependency; it was failure to
make the antecedent and its blast radius explicit.

At the assessed revision, the conditional build stood on:

- base `(1,3)`: author-stated, not derived;
- fibre `(6,4)`: selected by a Pati--Salam criterion that passes its repository
  check but remains an external physical target rather than an action-derived
  necessity; and
- ambient `(7,7)`: the arithmetic consequence, also printed in the draft's
  equation (12.19).

The repository already distinguished signature-robust results from
horn-specific ones and retained the demoted `(9,5)` branch as a comparator.
That architecture makes a future horn change survivable only if concrete
objects are not imported across horns without an adapter.

## Recommendations recorded at the time

1. Name the imported antecedents wherever the conditional ledger's meaning
   depends on them.
2. Make horn robustness or specificity observable for affected results.
3. Audit the existing import ban rather than assuming it held.

These are historical recommendations, not a present claim that all three
tasks remain open or that any verdict should move. Current authority remains
the native ledger, current-state, path-dependency, claim, and correction
surfaces at the branch being read.

## Persistent ceiling

The base `(1,3)` remains authorial evidence unless separately derived.
Group-theoretic or metric-geometric criteria that cannot see the base sign do
not convert that authorial choice into an action-owned result.
