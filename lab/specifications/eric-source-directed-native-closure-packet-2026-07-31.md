---
title: "Eric source-directed native closure packet"
status: conditional_construction
doc_type: specification
created: 2026-07-31
branch: agent/weinstein-guided-source-action
run: lab/process/runs/GUH-20260731T160344Z-eric-source-directed-native-closure/run-plan.md
certificate: lab/process/eric-source-directed-native-closure-certificate.json
probe: tests/channel-swings/eric_source_directed_native_closure_probe.py
---

# Eric source-directed native closure packet

## 1. Purpose and verdict vocabulary

This packet completes the existing ten-row Weinstein source crosswalk at the
level of a **single conditional mathematical construction**. It replaces ten
separate “find a familiar equation” tasks with five shared native objects and
states the exact readout that would earn each familiar physics name.

There are three deliberately separate verdicts:

1. **Typed conditional construction:** the formulas and ownership relations
   compose without a type contradiction.
2. **Finite exact positive control:** an exact rational/matrix fixture realizes
   the decoder algebra and rejects planted shortcuts.
3. **Actual `Y^14` emergence:** the same objects are selected by the native
   action, globalize, close on a physical domain, and pass the physics tests.

This run establishes 1 and 2. Every row remains open at 3. A fixture containing
`u(1) + su(2) + su(3)` is intentionally target-coded and is never evidence that
the action emits that algebra.

### Subsequent C0 source-reconciliation precondition

The Curt-iceberg reconciliation adds one prerequisite before C1 is globalized.
The source chimeric carrier

\[
C(Y)=VY\oplus\pi^*T^*X
\]

must first use the tautological metric musical map and then a natural
connection split,

\[
VY\oplus\pi^*T^*X\xrightarrow{1\oplus\sharp_h}
VY\oplus\pi^*TX\xrightarrow{?}TY.
\]

The resulting source carrier is presented with a `(7,7)` metric and
`U(64,64)`-type complex spinors. The
active packet uses the DeWitt/gimmel metric on `TY`, total signature `(9,5)`,
and a right-quaternionic `Sp(32,32;H)` reduction. No theorem presently
identifies those carriers. C0 must construct the full Zorro map and compare
the bilinear forms, Clifford modules, right-H structure, transition laws, and
structure-group reductions. It may return either an explicit bridge or a
non-equivalence certificate.

C0 does not reopen the already-built inhomogeneous-group, derivative-cocycle,
distortion, or action grammar. It determines which carrier the action census
and C1 must use and supplies the minimal primitives needed for enumeration. If
the carriers are inequivalent, run the target-blind census first and then port
or recompute only the surviving carrier-dependent Hodge/Clifford/Krein/Shiab/
Euler/Green/Ward realization before G2/G3 is treated as the source-carrier
parent. See
[`curt-jaimungal-gu-iceberg-claim-reconciliation-2026-07-31.md`](../sources/curt-jaimungal-gu-iceberg-claim-reconciliation-2026-07-31.md).

Accordingly, later `right-H` requirements name the active `(9,5)` branch. If
C0 retains a genuinely complex container without that reduction, replace them
by the corresponding C0-selected reality/container compatibility conditions;
do not import a quaternionic structure merely to satisfy this packet.

## 2. Layer-0 typing

Let

\[
Y=\operatorname{Met}(X),\qquad \dim X=4,\quad \dim Y=14,
\]

and carry the finite datum as a conditional family parameter

\[
d=(P1,P2,P3)\in\mathfrak D.
\]

`P1` is the retained orientation/sign datum, `P2` remains type-unknown, and
`P3` is consumed only by a final physical count/index interface. None may
contain a gauge group, particle table, background, projector, flavour matrix,
or cosmological fit.

The parent bosonic fields are the already-built G2/G3 objects

\[
B=A_{\rm LC}(\epsilon,g),\qquad T=A-B,
\]

with action

\[
I_{G2}
=\int_Y T\wedge\mathscr S_\epsilon
 \left(F_B+\frac12D_BT+\frac13q(T,T)\right)
 +\frac{\kappa_1}{2}\int_YT\wedge\flat_1T.
\]

Its exact Euler covector remains

\[
E_A=E_T
=\mathscr S_\epsilon(F_B)
 +\frac12(L+L^!)T+M_\epsilon(T,T)+\kappa_1\flat_1T.
\]

The compressed shortcut

\[
\mathscr S_\epsilon(F_{B+T})+\kappa_1T
\]

stays killed. The symbol `L` in this displayed G2 equation is the previously
defined distortion linearization; it is not the observation lift introduced
below.

## 3. The five shared closure objects

### C1. Observation, equation-dual, and domain packet

At a stationary native background `bar(Phi)_d`, let `V_Y` be the linearized
native field space and `V_X` the proposed observation field space. Construct

\[
L_d:V_X\to V_Y,\qquad R_d:V_Y\to V_X,
\]

\[
R_dL_d=1,\qquad P_d=L_dR_d,\qquad Q_{\rm off}=1-P_d.
\]

The field retract is not the map on Euler covectors. The algebraic dual is

\[
L_d^\vee:V_Y^\vee\to V_X^\vee.
\]

Only after choosing bundle pairings `G_Y,G_X` may it be represented on primal
fields by the Riesz adjoint

\[
L_d^!=\sharp_XL_d^\vee\flat_Y
     =G_X^{-1}L_d^\dagger G_Y:V_Y\to V_X.
\]

For the restricted action

\[
I_X[\phi;d]=I_Y[L_d\phi;d],
\]

the observed Euler equation is

\[
E_X(\phi;d)=L_d^\vee E_Y(L_d\phi;d)=0.
\]

The action Hessian is first an Euler-space map

\[
H_Y:V_Y\to V_Y^\vee\otimes\operatorname{Dens},
\qquad
H_X=L_d^\vee H_YL_d.
\]

It cannot be hit by the field projector `Q_off`. Construct an explicit native
Riesz map `sharp_Y` (Hodge/DeWitt/Krein as appropriate) and only then set

\[
K_Y=\sharp_YH_Y:V_Y\to V_Y.
\]

The corresponding primalized equations and Hessian are

\[
\sharp_XE_X=L_d^!\sharp_YE_Y,
\qquad
K_X=\sharp_XH_X=L_d^!K_YL_d.
\]

For this primalized operator require both

\[
K_X=R_dK_YL_d
\]

and the closure condition

\[
Q_{\rm off}K_YL_d=0,
\qquad\text{equivalently}\qquad
K_YL_d=L_dK_X.
\]

`R K_Y L` without the second equation is not a sector. The packet also owns a
closed right-quaternionic Krein domain, boundary condition, causal observation
background, and all Riesz maps that turn Euler covectors into primal fields.

### C2. Stationary orbit, stabilizer, and one bosonic Hessian

Solve the complete coupled equations before applying physics labels:

\[
E_Y(\bar\Phi_d;d)=0.
\]

Then compute the action-selected stabilizer

\[
\mathfrak k_d
=\operatorname{Lie}\operatorname{Stab}(\bar\Phi_d)
\]

and the bulk Hessian `H_B=Hess I_Y`. Its physical reduction is staged because
the relevant objects do not inhabit one quotient space:

1. restrict `H_B` to the admitted closed bulk domain/image after the primalized
   leakage test;
2. take bulk gauge cohomology or the appropriate derived critical quotient;
3. reduce the characteristic distribution of the preboundary form in the
   **boundary** trace space; and
4. prove that the restricted bulk operator descends through both reductions.

Call the descended operator `H_B^phys` only after those four steps.

All gauge, spin-two, heavy-scalar, and light-scalar readouts come from this one
operator. Off-diagonal readout blocks are predictions; they may not be erased
to manufacture independent sectors.

### C3. Minimal odd action and action-derived currents

Use the source-directed carrier

\[
\mathcal E_{01}=S\oplus(T^*Y\otimes S)
\simeq\Omega^0(Y,S)\oplus\Omega^1(Y,S).
\]

This is a two-degree carrier, not three generations. Let

\[
K_{01}=K_S\oplus(G^{-1}\otimes K_S)
\]

be the trace-reversed/Krein pairing and start from the least-inserted
first-order differential expression

\[
\mathcal D_{01,A}
=\begin{pmatrix}
c_G\circ\nabla_A^S&0\\
0&c_G\circ\nabla_A^{T^*Y\otimes S}
\end{pmatrix},
\qquad
\mathcal D_{\rm sym}
=\frac12(\mathcal D_{01,A}+\mathcal D_{01,A}^{\times}).
\]

Here `x` denotes the Krein adjoint. `D_sym` is only a formally
Krein-symmetric differential expression until its principal symbol is checked;
symmetrization could otherwise cancel the first-order part. For the vertical
ad-valued coefficient

\[
v=\Pi_{\rm vert}T=\sum_i\nu^i\otimes\Phi_i,
\]

define

\[
m_v=\sum_i c_G(\nu^i)\rho(\Phi_i),
\qquad
M_v=m_v\oplus(1_{T^*Y}\otimes m_v),
\]

\[
M_v^{\rm phys}=\frac12(M_v+M_v^\times).
\]

The minimal odd action is

\[
\boxed{
I_{\rm odd}
=\operatorname{Re}_{\mathbb H}
\int_Y\bar\Psi
(\mathcal D_{\rm sym}+M_v^{\rm phys})\Psi\,d\mu_G .}
\]

After a reality domain is constructed this is equivalently the Krein-paired
bilinear. No independent flavour matrix, generation mixing, `P_0`,
Seiberg--Witten moment map, or curvature vertex is inserted. If a target-blind
naturality census forces additional gamma-trace maps, their coefficients are
charged before any physical comparison.

The current is not guessed:

\[
\langle J_A,a\rangle
=\left.\frac d{dt}\right|_0
I_{\rm odd}[A+ta].
\]

It is a density-dual connection covector. The bridge to a primal connection
one-form requires the explicit Hodge/DeWitt/Krein Riesz map already exposed by
N3. The coupled Ward identity retains the moving
`A,epsilon,Psi,bar(Psi)` owners, the derived `v/T` graph return, and the
boundary term.

### C4. Frobenius-fibre trace reversal and metric readout

On the actual symmetric metric fibre `Sym^2 T^*X`, not an exterior `6+4`
surrogate, retain the four-dimensional Frobenius trace reversal

\[
\tau_F(h)=h-\frac12\operatorname{tr}_g(h)g.
\]

It is Frobenius self-adjoint and involutive in four dimensions: it negates the
trace line and fixes the traceless symmetric subspace. On Euler covectors use
the dual map `tau_F^vee`; the Frobenius/DeWitt Riesz identification makes its
coordinate matrix agree with `tau_F` but does not erase the type distinction.
Every gravitational and cosmological observation stress uses

\[
E_{g,X}=\tau_F^\vee L_g^\vee E_{g,Y},
\]

before its transverse-traceless, scalar, or homogeneous projection. Omitting
`tau_F` changes the object being compared.

### C5. Preboundary quotient, positive majorant, and evolution

Descend G3's action-derived preboundary form rather than declaring it already
physical. Construct

\[
L_\partial^T\Omega_YL_\partial=\Omega_X,
\qquad
R_\partial L_\partial=1,
\qquad
\ker\Omega_Y=\ker R_\partial,
\qquad
W_Y/\ker\Omega_Y\simeq W_X.
\]

This is a presymplectic characteristic quotient only. A BFV construction
still requires the boundary gauge action, moment map, ghosts, BFV symplectic
form, charge, and boundary master equation.

On the reduced odd domain, a fundamental symmetry satisfies

\[
\Theta^2=1,\qquad \Theta^\times=\Theta,
\qquad (u,v)_\Theta=[u,\Theta v]_{01}>0.
\]

If `D_X^x=D_X`, then

\[
H_{\rm form}=\Theta D_X
\]

is Hilbert self-adjoint and represents the Krein quadratic form. It is not yet
the time Hamiltonian. Only after the observation operator has the split

\[
D_X=C_t\nabla_t+D_\Sigma+M_X
\]

may one define the covariant generator

\[
H_{\rm cov}=-iC_t^{-1}(D_\Sigma+M_X),
\qquad
i\nabla_t\Psi=H_{\rm cov}\Psi.
\]

If `nabla_t=partial_t+Gamma_t`, the ordinary-time Hamiltonian contains the
additional `-i Gamma_t` transport term, together with whatever measure and
time-dependent-`Theta` correction is required to preserve the physical inner
product. Temporal gauge and a time-independent Hilbert structure may not be
assumed silently.

Ambient signature `(9,5)` does not select this time or domain automatically.

## 4. The completed ten-row readout crosswalk

| familiar name | native readout from the shared packet | what must be true before the name is earned | actual `Y^14` gate |
|---|---|---|---|
| Maxwell | a one-dimensional compact ideal `u` in the centre of the action-selected stabilizer `k_d`, whose infinitesimal connection image lies in the gauge-mass kernel; current from `delta_A I_odd` | unique compact abelian ideal, one positive-residue transverse massless pole, zero leakage, physical Hodge/Riesz map | derive the stationary stabilizer, its connection image, and the closed photon domain |
| Yang--Mills | `L_A^vee E_A=0` and its nonabelian block in `H_B`, coupled to the same action-derived current | second-order hyperbolic connection block, self-coupling fixed by the bracket, Ward identity, and survival of `F^2` ablation | compute the reduced exact G2/G3 Hessian |
| Einstein | `Pi_TT tau_F^vee L_g^vee E_g=0` after the staged bulk/boundary reduction | exactly two positive-residue massless TT modes, Einstein/Lichnerowicz principal symbol, nonlinear constraint closure, universal coupling to total observation stress | construct the metric lift/domain and nonlinear reductions |
| Higgs | a heavy scalar orbit of `H_B` sourced by `Pi_vert T`, with odd incidence `partial_h M_v` | one complex weak doublet, three orbit Goldstones, one stable radial mode, one photon kernel, and the same incidence in gauge mass and fermion bilinear | obtain the representation and stationary VEV without inserting them |
| Dirac | the `Omega^0` block of the reduced Euler Hessian of `I_odd` | closed first-order 4D Clifford principal symbol, right-H compatibility, physical chiral cohomology, K-paired zero-order channel, action-derived current | build the global operator and common domain |
| Schrodinger | covariant Hamiltonian evolution of C5 after solving the reduced Dirac-type operator for one observed time derivative | source-selected positive majorant/time, presymplectic characteristic reduction followed by BFV closure, self-adjoint generator, preserved physical domain and norm | construct G4 boundary reduction, future BFV charge/master equation, and the time split |
| weak force | the compact three-dimensional simple ideal of the pre-breaking stabilizer and its broken Hessian incidence | compact `A1` ideal, correct massless pre-breaking modes, rank-three breaking with one abelian kernel, compatible odd representations | derive the stabilizer and scalar orbit target-blindly |
| strong force | the compact eight-dimensional simple ideal of the unbroken stabilizer | compact `A2` ideal, eight positive-norm massless transverse modes, compatible `3/bar(3)` odd incidence; confinement remains downstream | derive the ideal and quantum scale from the native orbit |
| dark energy | a **candidate distinct** light scalar mode of `H_B` whose future `tau_F^vee L_g^vee` variation must give conserved homogeneous stress | non-gauge positive-norm light mode, stationary amplitude, protected scale, no Higgs reuse, no fitted PP3/DESI value | solve the scalar/trace background family and observation stress |
| dark matter | a separate physical decomposition `P_lum+Q_dark=1` **inside the admitted physical odd domain**, not the off-observation complement `Q_off`; both sectors remain in the common dynamics and stress | low-curvature suppression of `P_lum D Q_dark`, positive norm, mass/stability/abundance, and source-derived curvature threshold where mixing returns without changing either projector | derive the physical decomposition, common stress, gap, and recoupling threshold |

## 5. What the executable control establishes

The exact probe constructs and checks:

- a nonorthogonal field retract with a distinct equation-dual and exact action
  chain rule;
- a compatible-pairing fixture where the Riesz adjoint `L^!=R` for a reason;
- a leakage plant that passes `RDL` but fails `(1-LR)DL`;
- four-dimensional Frobenius trace reversal on the ten-dimensional symmetric
  fibre;
- a target-coded compact `u(1)+su(2)+su(3)` algebra, its centre, derived rank,
  negative Killing form, Jacobi identity, and exact basis change;
- one explicitly coupled finite `U(2)`-doublet action whose represented vacuum
  orbit is exactly the three-dimensional Goldstone kernel, whose orbit
  incidence generates the gauge mass matrix, and whose equivariant Yukawa map
  has the same infinitesimal incidence; its single Hessian also carries one
  photon kernel, eight colour controls, two spin-two controls, one heavy radial
  scalar, and one distinct light scalar control;
- one Krein-self-adjoint odd operator with `K`-paired cross-chirality mass,
  action-derived current, low-curvature dark decoupling, declared-curvature
  recoupling, and a Hilbert-self-adjoint form representative;
- a preboundary characteristic quotient and positive/negative **isotropy**
  controls. No BFV charge or physical closed domain is claimed by the fixture.

Only the finite `U(2)` gauge--doublet block is nontrivially coupled. Colour,
spin two, and the light scalar are deliberately decoupled plants. The Yukawa
map is an equivariant representation-level incidence check, not yet a
Krein-adjoint-completed realistic odd block.

The exact model proves that the crosswalk's arrows are mutually compatible and
can share parent objects. It does not prove locality, patch descent,
hyperbolicity, anomaly cancellation, a Standard Model spectrum, a generation
count, cosmological stability, or any empirical prediction.

## 6. Actual emergence gates, in efficient order

1. Build C0's chimeric/Zorro metric--Clifford--real-form bridge, freeze the
   carrier, and construct only the invariant primitives required to enumerate
   natural action terms.
2. Run G3.5's target-blind census and ablation on that carrier. If C0 found
   inequivalence, port or recompute only the surviving G2/G3 realization.
3. Lift C1 from matrices to global bundle/differential-operator maps, construct
   `sharp_Y`, and prove patch descent, compatibility with the C0-selected
   real/container structure (including right-H compatibility if that reduction
   survives), domain
   preservation, and nonlinear primalized leakage
   `Q_off sharp_Y E_Y(L phi)=0` for the exact G3 Euler map.
4. Solve a one-parameter family of native stationary backgrounds without
   Standard Model or PP3 labels.
5. Compute C2's actual Hessian and stabilizer simultaneously, then perform the
   staged bulk-domain, gauge-cohomology, and boundary-characteristic reductions;
   retain every off-diagonal block and prove descent.
6. Freeze C3 on the same background/domain, vary it once, and extend the G3
   Ward/BV packet with its exact current.
7. Replace C3's block-diagonal compatibility expression with the source-shaped
   shortened `0 -> 1 -> 13 -> 14` complex and test its adjacent compositions,
   cohomology, and square/factorization relation to the bosonic Hessian.
7. Diagonalize the bosonic Hessian while decomposing the odd operator under the
   action-selected stabilizer. This single computation tests gauge ideals,
   spin two, EWSB, both scalar scales, odd representations, and dark mixing.
8. Only after the physical domain and time split exist, construct C5's
   Hamiltonian evolution.
9. Run anomaly, index/P3, flavour misalignment, proton decay, confinement,
   FLRW/PP3, DESI, abundance, and other
   empirical comparisons last and without calibration leakage.

## 7. Kill conditions carried forward

Kill or retract the corresponding arrow if the actual construction requires a
post-spectrum projector, fitted Standard Model background, independent copied
equation, omitted trace reversal, frozen Krein pairing, nonclosed domain,
noncompact/wrong-sign gauge ideal, extra spin-two pole, reused scalar mode,
arbitrary positive majorant, moving dark projector, or block-to-generation
inference. Before declaring a source-directed route dead, recheck the Weinstein
claim ledger for a local construction instruction and record the Layer-0 sense
in which the failure was obtained.
