---
work_item: SOURCE-OWNED-CHIMERIC-BV-CAMPAIGN-RB5
status: completed
created: 2026-07-31
branch: agent/operator-anomaly-big-swing
---

# RB5 flag ownership, spectral preflight, and Hessian eligibility

## Target

Execute the first ownership test generated by RB4:

1. type the actual \(\epsilon_{\rm IG}\) orbit available in the repository;
2. decide whether it admits an equivariant descent to the complete
   trace-reversed complex--Cartan flag;
3. test the conditional spectral/polar construction
   \[
   H\mapsto P_W,\qquad Q\mapsto J;
   \]
4. propagate the composite variation far enough to decide whether a physical
   gauge-quotiented Hessian is eligible; and
5. preserve every carried Standard Model/Yukawa, quantum/Krein/BV,
   gravity/cosmology, UV/causality, and P3/index/count boundary.

This investigation does not reopen the generic question whether a source action and
external datum are needed. It tests two rival ownership routes for the flag
already typed in RB4.

## Layer 0

The following are distinct until a map is built:

| phrase | object in RB5 | not identified with it |
| --- | --- | --- |
| source \(\epsilon\) | a gauge transformation in the inhomogeneous gauge group, entering \(T_\omega=\varpi-\epsilon^{-1}d_0\epsilon\) | N1's varied soldering field |
| abstract N1 \(\epsilon_{\rm IG}\) | a declared section of an unspecified IG/soldering bundle | a typed homogeneous orbit |
| RB3 \(\epsilon_{\rm plane}\) | a reduction to an unframed Clifford fourteen-plane, locally \(G/H_{\rm Cl}\) | a full frame or a complex--Cartan flag |
| flag refinement | a reduction \(G/L_{\rm flag}\) retaining \(P_W,J,t,\Omega_{\mathbb C}\) | data derived from the forgetful Clifford-plane quotient |
| spectral witness \(H,Q\) | generic \(G_{\rm DW}\)-self-adjoint and \(G_{\rm DW}\)-skew endomorphisms used to validate calculus | a GU-native concomitant of \(A,U,P_{\rm IG},s,\epsilon\), or curvature |
| conditional projector | a target-blind spectral/sign projector of a gapped \(H\) | a projector chosen because its rank is four |
| conditional \(J\) | a polar normalization of an admissible \(Q\) | a complex structure selected by the observer or Krein form |
| off-shell Hessian fixture | a finite classifier with planted gauge, stable, modulus, and unstable sectors | the physical coupled BV Hessian |
| determinant trace gate | the local Lie-algebra condition removing the central complex trace | a global complex-volume section, \(\mathbb Z_6\) quotient, or hypercharge normalization |
| count | a chiral/Fredholm/family-index output on a common domain | a rank, orbit dimension, Hessian multiplicity, or fixed-set dimension |

Trace reversal remains first: raw Frobenius \((7,3)\) is a hostile comparator;
the native vertical fibre is DeWitt \((6,4)\).

## Pre-registered expected verdict

RB4 pre-registered this swing before execution. The expected result is:

```text
RB3 CLIFFORD-PLANE epsilon -> COMPLETE FLAG:
  EXPECTED TO FAIL OR REMAIN UNTYPED AT THE SEED-STABILIZER TEST.

GENERIC GAPPED H / ADMISSIBLE Q:
  EXPECTED TO CONSTRUCT P_W / J WITH EXACT COVARIANCE AND DIFFERENTIABLE
  FUNCTIONAL CALCULUS, BUT NOT TO ESTABLISH GU SOURCE OWNERSHIP.

PHYSICAL GAUGE-QUOTIENTED HESSIAN:
  EXPECTED TO REMAIN INELIGIBLE UNTIL A SOURCE-DERIVED MAP, STATIONARY
  BACKGROUND, FULL COUPLED LINEARIZED/BV COMPLEX, AND DOMAIN EXIST.

OFF-SHELL FINITE HESSIAN CLASSIFIER:
  EXPECTED TO DISTINGUISH GAUGE ZEROS, STABLE PHYSICAL DIRECTIONS,
  MODULI, AND INSTABILITY, ONLY AS A CONTROL.
```

The hostile reviewer separately pre-registered these kills before inspecting
the result. Its preregistration arrived before the source/stabilizer and
executable reports.

## Rival route A: \(\epsilon_{\rm IG}\) ownership

Let

\[
G=\operatorname{Sp}(32,32;\mathbb H),\qquad
H_{\rm Cl}^0=\operatorname{Spin}_0(9,5).
\]

RB3's explicit unframed Clifford-plane field is locally

\[
\epsilon_{\rm plane}\in\Gamma(P_G/H_{\rm Cl}).
\]

A \(G\)-equivariant map

\[
G/H_{\rm Cl}\longrightarrow G/L_{\rm flag}
\]

exists only when the seed flag is fixed by \(H_{\rm Cl}\), equivalently
\(H_{\rm Cl}\subseteq gL_{\rm flag}g^{-1}\) for a homogeneous target.
Existence of a transported flag after choosing a local lift is insufficient:
the result must be invariant under changing the lift \(g\mapsto gh\).

Ownership requires more than one fixed seed. The complete fixed-flag set
modulo the normalizer must be unique or finite, with any residual component
or orientation separately sourced. A trivial stabilizer transports every
seed and therefore represents maximal seed smuggling.

## Rival route B: spectral/polar ownership

The conditional calculus is:

\[
H^{\dagger_G}=H,\qquad
P_-=\mathbf1_{(-\infty,0)}(H),
\]

with a target-blind sign/contour rule and a uniform gap, and

\[
Q^{\dagger_G}=-Q,\qquad
J=Q(-Q^2)^{-1/2},
\]

with invertibility, an admissible real functional-calculus branch, and
smoothness.

The executable may validate:

- rank and signature measured after applying the frozen spectral rule;
- covariance under joint \(O(6,4)\) conjugation;
- analytic versus finite-difference projector and polar derivatives;
- failure at gap closure or singular \(Q\);
- raw-Frobenius, frozen-flag, and extra-central-\(U(1)\) controls; and
- the shape of a quotient-Hessian classifier.

It may not call its planted \(H,Q\) GU-native or source-owned.

## Source-provenance gate

A source-owned candidate must be a natural concomitant of already varied
fields. For each proposed \(H,Q\), the investigation must type every adapter:

\[
\text{form/adjoint indices}
\longrightarrow
\operatorname{End}(V_{6,4}),
\]

including the representation, contractions, Hodge/Riesz map, vertical
restriction, real form, density, and dependence on the flag itself.

If the adapter already requires \(P_W,J,\Omega_{\mathbb C}\), a \(6+4\)
block, four chosen eigenvectors, or a target-labelled gamma basis, the
candidate is circular.

At least one alternative natural contraction must be compared before a
spectral selector can be called canonical. A pointwise gap is not a global
uniform gap or an eigenbundle reduction.

## Hessian eligibility

If the flag is composite,

\[
\mathfrak f=\mathfrak f(H(\phi),Q(\phi)),
\]

the varied objects remain the original fields \(\phi\). The chain rule is

\[
\mathcal E_\phi
\mapsto
\mathcal E_\phi
+(D_\phi H)^!(D_HP_W)^!\mathcal E_P
+(D_\phi Q)^!(D_QJ)^!\mathcal E_J
+\cdots.
\]

No independent flag Hessian may double-count those directions.

A physical quotient Hessian is eligible only at a stationary, domain-equipped
solution and must be the cohomology of the full coupled linearized/BV complex,
including connection--Goldstone mixing, constraints, ghosts, boundary
conditions, Hodge/density motion, and retained-mode leakage. Before that,
the executable Hessian is a classifier control only.

## Kill conditions

1. Kill the direct RB3 \(\epsilon_{\rm plane}\to\mathfrak f\) descent if
   \(H_{\rm Cl}\) moves any prerequisite flag component.
2. Kill any local-lift construction that changes under \(g\mapsto gh\).
3. Kill ownership from mere covariance; eigenvalues, eigenbundles, topology,
   defects, and boundary values are gauge-invariant data.
4. Kill any \(H,Q\) whose seed already contains the desired flag.
5. Kill a post-selected “rank-four sector”; the spectral rule is frozen
   before its rank is read.
6. Kill the spectral branch at gap closure, signature mismatch, nonuniform
   gap, or an untyped source-to-endomorphism adapter.
7. Kill the polar branch for singular \(Q\), an unavailable square-root
   branch, Jordan obstruction, failure of \(J^2=-1\), or failure of
   \(G\)-orthogonality.
8. Kill uniqueness if equally natural contractions return different flags.
9. Kill a physical Hessian claim before stationarity and the full coupled
   linearized/BV/domain packet.
10. Kill determinant-one at local trace grade; global determinant-line
    triviality, parallel volume, central quotient, and fermion lift remain.
11. Kill five-leg closure inferred from finite pointwise matrices.
12. Kill any index/count reading from ranks, dimensions, eigenvalues,
    nullities, or transcript phrases.

## Five-leg boundary

| leg | admissible RB5 result |
| --- | --- |
| SM/Yukawa/provenance | type the common flag and local extra-\(U(1)\) gate; no physical SM group, hypercharge, retained mode, or Yukawa placement |
| quantum/Krein/BV | preserve \(G_{\rm DW}\), \(K/C\), right-\(\mathbb H\), and identify the full BV Hessian obligation; no CME or physical quotient claim |
| gravity/cosmology | keep trace reversal and flag/section stress explicit; no value or stationary cosmology |
| UV/causality | preserve the moving Clifford plane and \(g=1\) principal packet; no curved subprincipal or common-cone upgrade |
| P3/index/count | P3 remains a separate relative real-\(KO\) input on an unbuilt common domain; no count |

## Planned outputs

- `tests/channel-swings/rb5_epsilon_flag_ownership_spectral_hessian_probe.py`
- `explorations/rb5-epsilon-flag-ownership-spectral-hessian-2026-07-30.md`
- scoped append/correction to RB4, N1, and N3 owners
- scoped integration updates to `NEXT-STEPS.md`,
  `explorations/README.md`, and `tests/README.md`

No canon, claim-status, public-posture, mass, stationary-vacuum, anomaly,
unitarity, domain, index, or generation-count change is authorized.

## Execution result

### Route A: coarse soldering ownership

The explicit RB3 finite-local orbit is

\[
\mathcal O_{\rm Cl}
=
G/H_{\rm Cl},
\qquad
H_{\rm Cl}^0=\operatorname{Spin}_0(9,5).
\]

The target flag does not descend. The executable constructs all 91
\(\mathfrak{so}(9,5)\) vector generators:

```text
preserve V_3,1 plus V_6,4 split:   51
move the split:                    40
full vector commutant nullity:      1
same-coset lift-change residual:    0.404397515
```

The full commutant is scalar and contains no nontrivial projector. Therefore
no \(H_{\rm Cl}\)-fixed \(4+10\) split exists, hence no complete
\((P_W,J,t,\Omega_{\mathbb C})\) flag exists at the seed.

The global associated bundle/reduction remains conditional. This is a local
stabilizer obstruction, not a promotion of the RB3 orbit to global existence.

Verdict:

```text
epsilon_plane -> complete flag: REFUTED
```

The correct direction is the refinement

\[
G/L_{\rm flag}\longrightarrow G/H_{\rm Cl}.
\]

The RB3 \(A_0\)-induced connection factors through the coarse quotient and
is flat along the flag-refinement fibre.

### Route B: conditional spectral/polar construction

The planted source-shaped fixtures, which intentionally encode the desired
\(6+4\)/complex geometry, pass:

```text
H gap:                                  1.100
measured P rank/signature:              4 / (0,4)
complement rank/signature:              6 / (6,0)
frozen-P residual:                      1.940205721
spectral derivative max error:          2.461e-11
frozen-J residual:                      2.173961650
polar derivative max error:             7.216e-11
```

Gap closure, singular \(Q\), raw Frobenius, frozen flags, and the local
central-\(U(1)\) controls all fire.

Verdict:

```text
spectral/polar flag calculus: CONSTRUCTED CONDITIONALLY
GU source ownership of H,Q:   UNBUILT
```

The target-blind owner audit finds no complete adapter from current
distortion, curvature, mixed vertical, section-Hessian, or spinless-source
carriers to \(\operatorname{End}(V_{6,4})\). The executable fixtures remain
explicit plants.

### Hessian eligibility

The finite classifier distinguishes:

```text
gauge kernel:               2 explicit directions
strictly positive quotient: [0.8, 1.4, 2.1, 2.9, 3.8]
modulus quotient:           [0, 1.4, 2.1, 2.9, 3.8]
unstable quotient:          [-0.6, 1.4, 2.1, 2.9, 3.8]
```

This validates the classifier, not the GU Hessian. The physical Hessian is
ineligible because no source-derived composite or stationary solution exists,
and the full coupled linearized/BV/domain packet is unbuilt.

### Determinant-one gate

The local complex trace detects the central \(J\) direction and exact
subtraction removes it in the fixture. Global determinant-line triviality,
a parallel complex volume, the \(\mathbb Z_6\) kernel, hypercharge
normalization, and the fermion lift remain unbuilt.

## Ownership disposition

```text
coarse epsilon_plane owns flag:       REFUTED
refined epsilon_flag:                 TYPED / UNSELECTED
generic H -> P and Q -> J:            CONSTRUCTED CONDITIONALLY
source-derived H,Q:                   UNBUILT
off-shell Hessian classifier:         CONSTRUCTED CONTROL
physical coupled quotient Hessian:    INELIGIBLE
global SM determinant-one result:     INELIGIBLE
P1/P2/P3 ledger:                      UNCHANGED
```

## Validation receipt

Passed:

```text
python3 -B tests/channel-swings/rb5_epsilon_flag_ownership_spectral_hessian_probe.py
python3 -B tests/channel-swings/rb4_observer_cartan_moving_family_probe.py
python3 -B tests/channel-swings/rb3_moving_soldering_spinzero_probe.py
python3 -B tests/channel-swings/vertical_source_action_reduction_probe.py
python3 -B tests/W246_cfs_self_adjointization_selector_ambiguity.py
uv run --with-requirements requirements.txt python -B tests/big-swing/vg_v3_j_commutant_conformal_native.py
uv run --with-requirements requirements.txt python -B tests/W240_z2even_compact_image_nogo.py
uv run --with-requirements requirements.txt python -B tests/W243_charged_corridor_closure.py
python3 -B process_gates/tests_root_readme_inventory_audit.py
python3 -m py_compile tests/channel-swings/rb5_epsilon_flag_ownership_spectral_hessian_probe.py
git diff --check
```

The inherited no-goes remain scoped and active. The conditional \(J\) plant
does not commute with the trace-line projector and therefore does not
contradict VG-V3; it is a symmetry-breaking input. The vertical flag is not
the ambient \(Sp\) good-stable and does not evade W240/W243.

## Final boundary

```text
epsilon_plane->FLAG:                 REFUTED
epsilon_flag REFINEMENT:             TYPED / ACTION OPEN
SPECTRAL/POLAR RECOVERY:             CONDITIONAL CALCULUS BUILT
SOURCE CONCOMITANT:                  BLOCKED BY UNBUILT ADAPTER
PHYSICAL HESSIAN:                    INELIGIBLE
EXTERNAL DATUM FALLBACK:             CONTINUOUS FLAG SPURION
```

No stationary solution, source-derived flag, physical compactification,
Standard Model identification, mass, cosmological prediction, anomaly/CME
closure, common domain, index, or generation count is claimed.
