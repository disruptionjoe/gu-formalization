---
artifact_type: governed_run_plan_and_receipt
status: complete
owner_id: gu-formalization
created_at: 2026-08-12T20:54:25Z
envelope_sha256: ef8d08cf93183ac1689c9b7415158a384f8448f8e482ba11aa82e80f888e7899
---

# Trace-Hq normal-contact correction wave

## Pre-wave

- Fork assumed: none.  The source-sized unitary embedding and the observer
  contact are the objects being reconciled.
- Search space: the complete sixteen-coordinate live response, all thirty-two
  matching grade-two source supports, ten normal directions, the exact
  fixed-observer `SO(3)` generators and the trace-q split stabilizer.  It is
  finite and decidable wholesale at this grade.
- New unowned object on success: none.  The trace-owned Hermitian form `H_q`,
  the two carrier halves `C^(32,32)_+ + C^(32,32)_-`, their derived
  block-preserving `U(32,32) x U(32,32)` subgroup, the full `U(64,64)` arena,
  augmented torsion and live response all pre-exist.
- What dies on success: the v0.220 rank-eight/rank-eighty disposition if it
  used the wrong real-form embedding; the claim that its cokernel is already
  a module if the response bank is not closed under the named stabilizer; and
  the recurring-rank-128 analogy if dimensions and carrier types disagree.

The gate is live because v0.194 already owns the trace-Hq unitary arena while
v0.220 used the older B-skew comparator.  This is not answered by the held
rank computation: the two embeddings give different real phases on the same
grade-two supports.

## Adaptive specialist preflight

- **Layer 0:** distinguish the B-skew comparator, trace-owned `u(H_q)`, and
  observer-dependent `u(H_u)`; distinguish a quotient vector space from a
  module and the `SO(3)` stabilizer from the complete split stabilizer.  Also
  distinguish the Hermitian form `H_q` from the generation-hinge notation
  `H^- = X(S^+)`, `H^+ = X(S^-)`; chirality reversal makes the latter labels
  opposite to their source-spinor labels.
- **Prior art/source:** v0.194 owns `H_q=iB gamma(q_g)` and its `(64,64)` plus
  `(32,32)+(32,32)` inertias; the source owns the two-connection torsion term
  but is silent on this repository embedding.
- **Construction versus selection:** phase-completing the existing tangent
  constructs its correct image; it does not select an on-shell jet value.
- **Representation/commutant:** derive the actual stabilizer action on the
  response bank before naming a cokernel module.
- **Krein/real structures:** use the exact trace-Hq skew phase, not the
  grade-only B-adjoint shortcut.
- **Principal-bundle/descent:** test closure under the actual fixed-q split
  stabilizer; do not infer a global associated bundle from fixed-frame rank.
- **Variational/symplectic:** keep the off-shell normal contact separate from
  coupled Euler prolongation and reduced phase-space data.
- **Exact computation:** compare the old and corrected banks coefficientwise,
  retain the kappa-zero and wrong-real-form plants, and test scalar membership.
- **Contrary path:** retain the possibility that operative `D_varpi H_q=0`
  fails globally; the pointwise trace-Hq result must remain conditional on
  that separate action-owned gate.

## Plan

1. Replay v0.220 and the immutable trace-Hq ownership theorem.
2. Replace the grade-only B-skew source phases by the exact trace-Hq phases on
   every target-relevant support and recompute image, cokernel and scalar
   completion membership over all ten normals.
3. Derive the actual fixed-observer stabilizer action on the live response,
   test full split-stabilizer closure and compare the surviving cokernel with
   the rank-128 fermionic leakage module.
4. Source-return, hostile-review, append-correct the ledger, validate, commit,
   push, inspect the GU mailbox once and close the claim.

## Runtime collision note

System Runtime contains an unrelated untracked System Attention run envelope,
so this Run makes no Runtime repository write.  Its central GU claim uses the
digest above; the durable plan and receipt live in the owner repository.

## Receipt

The hostile real-form check fired. V0.220's operator ownership survives, but
its grade-only B-skew embedding did not represent the trace-owned source-sized
Hermitian form. The corrected exact ranks per normal are `8` for B-skew, `10`
for observer `H_u`, and `12` for trace `H_q`. Across ten normals the operative
contact is rank `120/160`, leaving a local rank-40 cokernel.

The primary carrier is two `C^(32,32)` Weyl halves; their block subgroup is a
derived stabilizer, not the primary object. All live grade-two contact cells
preserve both halves. Hermitian form `H_q` is kept distinct from generation
hinge `H^- = X(S^+)`.

The scalar completion is pointwise source-realizable. Its on-shell value,
full-stabilizer orbit, global bundle, coupled Euler prolongation, domain and
physical spectrum remain open. The rank-40 bosonic contact cokernel is not the
rank-128 fermionic leakage module. No verdict, residue, quotient, fork, datum,
P1/P2/P3, canon or public posture moved.

Validation:

- main exact probe: `46/46 PASS`;
- append-only ledger: `25 exact + 4 planted PASS`;
- durability audit: `23 exact + 4 planted PASS`;
- v0.220 ledger and durability: `29+4` and `22+4 PASS`;
- v0.194 trace-q ownership audit: PASS;
- process-gate inventory: `406/406 PASS`;
- Python compile, strict JSON parse and `git diff --check`: PASS.

Inherited reds are unchanged: the source-surface map has nineteen previously
unlisted historical files; the process-surface audit predates the live ledger
surface; and the tests-root audit has eleven unclassified signature probes
(`266` tracked versus `255` documented). No new file from this investigation is missing
from its required current README surface.
