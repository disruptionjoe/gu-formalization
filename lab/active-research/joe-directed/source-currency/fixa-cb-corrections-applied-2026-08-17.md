---
artifact_type: exploration
status: exploration
doc_type: stewardship_record
created: 2026-08-17
work_item: FIX-A
channel: source_currency
title: "FIX-A: SCUR-1's five conditional-build correction blocks applied, append-only. V1 (excluded-mechanism attribution: title clause, source_collision token, row D2, G2 re-scoped), V2 (vertical-leg = Lorentz-scalar typing: row E2 both cells, section-8 row iii), V5 (replica absolutism: row F2) in CB-A; V3 (ten-4D-scalars bridge setting, plus a dated compute-on-the-contraction note in section 6.4 item 4) and V4 (only-condensate chirality attribution) in CB-B. Every original sentence struck byte-exact, never deleted; every verdict cell token unchanged; SCUR-1's evidence probe stays 52/52 with its pins unfired because the strikes preserve the quoted bytes. The canon-adjacent sixth SCUR-1 diff (shiab-existence-cl95) is NOT applied; it awaits independent second verification per repo rule."
grade: "APPLICATION OF RECORD over an already-adjudicated audit. Nothing is re-derived and no currency call is re-adjudicated: each block quotes SCUR-1 section 3's finding and section 7's proposed wording at its verified anchor. All five anchors byte-verified at SCUR-1's cited loci before editing (section 1 table); the applied state is byte-matched by tests/channel-swings/joe_directed_fixa_cb_corrections.py (69/69, selftest 6/6). NOT: a canon edit, a ledger edit, a verdict/grade/count/bar movement, a re-adjudication of any SCUR-1 finding, or an application of the sixth (canon-adjacent) diff."
target_claim: "INTERNAL — the five sentences SCUR-1 section 3 typed VIOLATES-CORRECTION (three in cb-a-representation-content-2026-08-05.md, two in cb-b-lagrangian-terms-2026-08-05.md), quoted verbatim in section 2 below. SCUR-1's typing is quoted, not re-minted: no registered GU source claim is attacked, and each block is bound to its exact sentence and to the register item that corrects it."
target_claim_verdict: "CARRIED UNCHANGED from SCUR-1 — SPLIT, claim-indexed: the five sentences assert readings the 2026-08-14..17 corrections refute; the mathematics under all five is banked and untouched (Schur on the comparator carrier, branching multiplicities on Y, the multiplicity-replication conditional, SM-9's anomaly legs). This artifact moves no verdict; it applies the correction blocks SCUR-1 proposed."
canon_verdict_change: none
ledger_edit: none
priority_change: none
steering_effect: unchanged
canonical_effect: "none — correction-of-record application. Five append-only blocks land in two exploration files; no canon file, ledger row, registry, verdict cell token, grade, count, bar, or public posture moves. The mint inheritance list (SCUR-1 section 6) is unchanged and remains Arc-1's."
depends_on:
  - lab/active-research/joe-directed/source-currency/scur1-source-currency-audit-2026-08-17.md
  - explorations/conditional-build/cb-a-representation-content-2026-08-05.md
  - explorations/conditional-build/cb-b-lagrangian-terms-2026-08-05.md
  - explorations/conditional-build/cb-e-source-contact-rows-2026-08-05.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
  - lab/methods/source-native-comparator-routing.md
  - VERIFICATION.md
  - lab/active-research/joe-directed/four-d-mode-decomposition/md1-form-leg-survives-ad-leg-is-untyped-2026-08-14.md
  - lab/active-research/joe-directed/vz-repair/vz4-pullback-is-a-contraction-2026-08-15.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he1-imposter-separation-invariant-2026-08-14.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/carrier/crb-carrier-is-four-corners-not-one-weyl-2026-08-15.md
scripts:
  - tests/channel-swings/joe_directed_fixa_cb_corrections.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

> **Audit-scope disclosure, stated rather than exploited (the AR-2/SCUR-1
> pattern).** `doc_type: stewardship_record` places this file outside the
> derived scope of `process_gates/source_native_comparator_routing_audit.py`.
> The declared type is honest — this is a maintenance pass applying an
> already-adjudicated audit's correction blocks; it adjudicates no physics —
> and the routing notice and classification above are carried anyway, so the
> exclusion buys nothing it would not otherwise have.

```gu-typed-objects
result: FIX-A application of record — five append-only correction blocks (V1, V2, V5 in CB-A; V3, V4 in CB-B), originals struck byte-exact, verdict cells unmoved
carrier: the two conditional-build files cb-a-representation-content-2026-08-05.md and cb-b-lagrangian-terms-2026-08-05.md LAYER=UNTYPED CHIRALITY=N/A
# LAYER is declared UNTYPED because the closed vocabulary (ambient/observed/source-print/toy) has no token for repository text about the source; the ambiguity is declared, not resolved (SCUR-1's own declaration, carried).
pairing: NONE
real_structure: N/A
grading: N/A
action_owner: repository-construction
target: the five VIOLATES-CORRECTION sentences typed by SCUR-1 section 3, corrected against register items 5, 6, 7 and 8 (with 3) MAP-TYPE=not-a-map
```

# FIX-A — applying SCUR-1's five conditional-build correction blocks

SCUR-1 adjudicated; this artifact applies. Section 7 of
`scur1-source-currency-audit-2026-08-17.md` proposed six diffs with owners
named. The five that target the conditional-build pack are applied here as
append-only correction blocks in the house pattern (CB-E's E3/E9 block, the
GEOMETER-VS-PHYSICS-OBJECTS WITHDRAWN cell, and the routing method's
`[!CAUTION]` block are the models). The sixth —
`canon/shiab-existence-cl95.md:84`'s reword — is **deliberately not applied**:
SCUR-1 itself marks it "owner: canon steward, second verification required",
and no second verification exists yet.

## 0. PREFLIGHT — lenses declared before any edit

1. **Correction-of-record practice.** Every block is dated, states what is
   withdrawn (verbatim), which register item corrects it, what survives, and
   who established it (SCUR-1, with the underlying owner artifacts named:
   MD-1, VZ-4, HE-1, ST-1, CR-B, IV-20260815, SC-CHI-01/SC-FER-03, the drafts
   loci). Undated fences are not corrections (SCUR-1 preflight lens 2).
2. **Append-only editing.** No byte of any original assertion is deleted:
   strikes wrap the original bytes exactly (`~~...~~`), frontmatter gets
   bracketed annotations appended (YAML-safe: no colon-space inside plain
   scalars, no double quotes inside the quoted title), and the dated blocks
   are appended after each file's filed footer.
3. **Probe lifecycle.** SCUR-1's probe byte-matches the five violation quotes;
   the strikes were designed byte-preserving so the audit's evidence stays
   verifiable, and the pin question is settled by RUNNING the probe after
   application, not by prediction (section 3). The new probe follows
   VERIFICATION.md's "Probe and mutation-harness discipline" (2026-08-17):
   baseline-first selftest, machinery-only mutations, crash-rejection,
   planted-positive control for absence checks, exit 0 on success.
4. **Supersession hygiene.** No scope creep beyond SCUR-1's typed findings:
   the only verdict cell touched is E2's, which SCUR-1 section 7 explicitly
   instructs ("E2's verdict cell re-based per V2"); D2's OVER-DETERMINED,
   F2's conditional SAME, SM-6's and SM-9's verdicts do not move. SM-3's
   parallel caveat, which SCUR-1 did not type, is not touched.
5. **Adversarial reading, both directions.** Each strike was argued against
   before applying (does it hit only what SCUR-1 refuted? does it convert a
   premise correction into a verdict change?), and V5 carries SCUR-1's own
   two-sided caveat verbatim rather than a smoothed version (section 4).
6. **Shared-checkout concurrency.** Immediately before editing, both files
   were re-checked for a concurrent application (zero `2026-08-17` markers
   present) and the destination queue for a duplicate artifact (none).
   Nothing under `vz-repair/` (Arc-5 live), `lab/process/`, `canon/`, or any
   ledger/registry is touched; no git operations.
7. **Gate compliance.** Kill-typing is quoted from SCUR-1 (frontmatter
   `target_claim`/`target_claim_verdict` above, INTERNAL form — the same form
   SCUR-1 used); the typed-carrier block above satisfies FX-2; the routing
   notice and honest `doc_type` follow the SCUR-1/AR-2 disclosure pattern.

## 1. Anchor verification — every SCUR-1 quote found at its cited locus before editing

| finding | SCUR-1 cited locus | anchor status at apply time |
|---|---|---|
| V1 title clause | `cb-a:5` | verified byte-exact at line 5 |
| V1 `source_collision` token | `cb-a:42` | verified byte-exact at line 42 |
| V1 row D2 middle column | `cb-a:315` | verified byte-exact at line 315 |
| V2 row E2 (both quoted sentences) | `cb-a:407` | verified byte-exact at line 407 |
| V5 row F2 clause | `cb-a:473` | verified byte-exact at line 473 |
| G2 re-scope target | `cb-a:476` | verified byte-exact at line 476 |
| V3 bridge-setting clause | `cb-b:959` | verified byte-exact at line 959 |
| V4 only-condensate clause | `cb-b:1131-1132` | verified byte-exact (sentence opens 1131, quoted phrase on 1132) |

**All six diff anchors anchored; none drifted; nothing needed improvisation.**
One consumer instance beyond the quoted loci was marked in place and is
disclosed as such: CB-A section 8 row (iii) restates E2's withdrawn shape
constraint verbatim ("the Higgs must consume the vertical form leg (E2)") and
is named by SCUR-1 as V2's first downstream consumer; leaving it live while
striking its twin at E2 would be the exact re-import failure mode the Z/3
packet exhibited. It received the same strike-plus-pointer treatment, recorded
here rather than silently. Similarly CB-B section 6.4 item 4 received the
dated one-line note SCUR-1's V3 finding itself calls for ("sends a future wave
to compute the bridge on the dead decomposition **unless corrected**").

## 2. The five blocks, before and after

**V1 — CB-A, excluded-mechanism attribution (severity HIGH).**
Struck in place: title clause "the source's own stated effective-chirality
mechanism is excluded by Schur, not merely unbuilt" (bracket-annotated inside
the YAML string); frontmatter token
`~~SOURCE-CONTRADICTED-ON-VEV-INDUCED-EFFECTIVE-CHIRALITY~~`; D2's
"~~the source's stated mechanism — VEV/low-curvature mass decoupling into
chiral sectors — **cannot work**. See §5.2~~". Corrected to (SCUR-1 §7's
wording, quoted): "the exclusion binds a `G_SM`-preserving **mass** mechanism
on the conjugate-pair (`16 ⊕ 16bar`) comparator carrier; the source's stated
mechanism is the VEV-conditional **decoupling** typed as SG4 bit 2 (ST-1), on
the four-corner carrier (CR-B), with dark-sector partner placement
(SC-FER-03), and is **NOT excluded by this row**." G2's "cannot remove it by
mass" carries a scope-re-set bracket (scope correction, not a refutation).
Banked untouched: the entire §5.2 Schur proof, D2's kill table, the
OVER-DETERMINED verdict token.

**V2 — CB-A, vertical-leg = Lorentz-scalar typing (severity HIGH).**
Struck in place: "~~the Lorentz-scalar components are exactly those with
**both** legs vertical:~~" (middle column) and the verdict-cell constraint
"~~, with an exact shape constraint: the Higgs must consume the vertical form
leg. This is a tightening act of type (iii)~~", plus §8 row (iii)'s verbatim
restatement. Corrected to: the observation reduction is a CONTRACTION sending
every ad-valued one-form to exactly ONE 4D one-form (MD-1, VZ-4,
canon-integrated); no component descends to a 4D scalar; "It's not Kaluza
Klein." Banked untouched: `V*₁₀ ⊗ Λ²V₁₀ = 10 ⊗ 45` and its 6-doublet
multiplicities (left unstruck mid-cell), row E3's class exclusion, the
NEEDS-U4 verdict token, and MD-1's INERT-AD fork nuance (recorded in the
block).

**V5 — CB-A, replica absolutism (severity LOW; SCUR-1's closest call).**
Struck in place: "~~GU has no mechanism that could make generation 2 differ
in rep content~~". Corrected to: the source names exactly such a mechanism —
imposter "for representation theoretic reasons", HE-1's forced subtractive
partition, mirror block inside the 144; replicas exact only at the
effective/low-energy layer. The block quotes SCUR-1 §4's two-sided caveat
verbatim (a reviewer weighing the row-context fence more heavily would
downgrade to PREDATES-BUT-CONSISTENT-WITH-SUPERSEDED-ANTECEDENT). The verdict
cell "SAME (conditionally on `U5` acting by multiplicity)" is untouched.

**V3 — CB-B, ten-4D-scalars bridge setting (severity MODERATE).**
Struck in place: "~~`Lambda^1(V_14)` gives `Lambda^1(V_4)` plus **ten 4D
scalars**~~". Corrected to: contraction, not a Kaluza-Klein mode split; the
bridge QUESTION survives verbatim and §6.4 item 4 now carries the dated
compute-on-the-contraction-map note. The block records that GVPO's Higgs/VEV
cell carried exactly this clause and was withdrawn 2026-08-15 — CB-B:959 was
the same clause, un-withdrawn until now.

**V4 — CB-B, only-condensate chirality attribution (severity LOW-MODERATE).**
Struck in place: "~~only by an unbuilt mirror-gapping condensate~~".
Corrected to: the SG4-bit-2 VEV-conditional decoupling selector (ST-1) plus
the dark-sector partner-placement obligation (SC-FER-03; IV-20260815 §3.3)
with the subtractive 2+1 (HE-1); a mirror-gapping condensate is one
comparator-shaped option, not the only route. Banked untouched: the
vectorlike-carrier statement, the leg-(i) anomaly verdict, the W224 teeth.

## 3. Probe-pin disposition — pins did not fire, and why that is by design

SCUR-1's certificate pre-declared: "if an owner applies the proposed
correction blocks and strikes an original sentence, the corresponding
violation-quote check goes red — the signal to retire or re-baseline this
probe." The strikes here were deliberately **byte-preserving** — `~~...~~`
wraps the original characters without altering one of them, and the blocks
quote the originals besides — so every SCUR-1 needle still byte-matches at
its file, the audit's evidence base remains verifiable in place, and the
anticipated red never occurs:

```
tests/channel-swings/joe_directed_scur1_source_currency_audit.py (post-application)
    -> 52/52 checks, exit 0
tests/channel-swings/joe_directed_scur1_source_currency_audit.py --selftest
    -> baseline green FIRST, 6/6 machinery-corruption mutations caught, exit 0
```

So per the lifecycle rule (pins fire → refresh or minimally re-pin; pins do
not fire → leave the probe alone), **SCUR-1's probe was not edited**. It
remains green and non-vacuous — its needles match real, now-struck text —
and its ageing note stays armed for any future edit that alters the quoted
bytes. The new probe (`joe_directed_fixa_cb_corrections.py`) additionally
re-asserts all eight SCUR-1 needles as its append-only invariant, so a later
deletion of a struck original goes red in BOTH probes.

## 4. Hostile review — two-sided, before shipping

- **Overreach charge (did any edit exceed SCUR-1's typing?).** Two in-place
  edits sit beyond the five quoted loci: §8 row (iii) and §6.4 item 4. Both
  are named consumers inside SCUR-1's own findings (V2's consumer list; V3's
  "unless corrected" clause), both restate or dispatch the exact withdrawn
  content, and both are disclosed in section 1. The route_disposition and
  gate_after frontmatter tokens of CB-A ("...EXCLUDED...",
  "...OVER_DETERMINED") were NOT edited: SCUR-1 did not quote them, and the
  appended blocks govern their reading.
- **Underreach charge (did anything typed stay uncorrected?).** All eight
  quoted sentences across the five findings are struck or bracket-annotated at
  their loci; grep confirms no unmarked ASSERTION instance of any withdrawn
  clause remains in either file, classified occurrence by occurrence:
  "consume the vertical form leg" — 3 in CB-A (`:407` struck, `:492` struck,
  `:630` the withdrawn-text quotation inside CORRECTION V2);
  "mirror-gapping condensate" — 4 in CB-B (`:1138` struck, `:1143` and
  `:1458` corrective context inside the bracket and block, `:1447` the block
  heading's quotation); "ten 4D scalars" — 2 in CB-B (`:959` struck, `:1421`
  the block heading's quotation).
- **Verdict-movement charge.** Checked token by token (probe checks P1–P6):
  D2 `OVER-DETERMINED`, F2 `SAME (conditionally...)`, E2 `NEEDS-U4`, SM-9
  "exactly-same as physics", SM-6 "DETERMINED GIVEN U2 AND U13" all present
  and unmoved; `canon_verdict_change: none` untouched in CB-A.
- **The V5 defense.** The strongest objection SCUR-1 recorded (the row's
  conditional fence nearly wins) is printed inside CORRECTION V5 itself, not
  summarized away.
- **Sixth-diff discipline.** `canon/shiab-existence-cl95.md` is untouched;
  nothing in these blocks paraphrases its proposed reword into effect.

## 5. Gate results — prior state or better, measured not asserted

| gate | before FIX-A | after FIX-A |
|---|---|---|
| `kill_target_claim_audit` | 3 red = baseline 3, exit 0; hatch 10; internal-target 6; ledger 8/8 | 3 red = baseline 3, exit 0; hatch 10; internal-target 7; ledger 8/8. The +1 internal-target is NOT this artifact — it is a concurrent lane's `parity-crosscheck/pcx1-signature-parity-clause-does-not-fire-2026-08-17.md` (SCUR-1 §6 item 7's cross-check, running in parallel; verified by enumerating the gate's internal-target set). This artifact's head fields carry no kill-language, so the gate skips it; the INTERNAL-form typing above is carried voluntarily, quoted from SCUR-1 |
| `correction_propagation_audit` | exit 0 (NEEDS_RECHECK report-only set unchanged) | exit 0, same set (scans canon/ and docs/ only; FIX-A touches neither) |
| `source_native_comparator_routing_audit` | 5 UNCLASSIFIED = baseline 5, exit 0 (scope 126) | 6 UNCLASSIFIED, exit 1 (scope 127) — **the +1 is not FIX-A's**: the sixth is `vz-repair/vz5-ii-s-subprincipal-2026-08-17.md`, Arc-5's live in-flight file, timestamp-created 09:32 during this session by the concurrent lane FIX-A is ordered not to touch. FIX-A's own writes contribute zero: the CB files are out of scope by filename, this artifact is out of scope by honest doc_type (disclosed above, notice carried anyway) and is absent from the unclassified list. Attribution verified by enumerating the unclassified set before and after: the diff is exactly {vz5} |
| `typed_carrier_declaration_audit` | 1 red (pre-existing, another lane's `selected-k151-...-2026-08-17.md`), exit 1 | 1 red, the SAME single pre-existing file, exit 1 (scope grew 9→12 files with concurrent filings); this artifact triggers (scripts + certificate) and complies with its typed-objects block, adding no red |
| `tests_manifest_count_audit` | OK (channel-swings row is prose, not a counted row) | OK |

## 6. POSTFLIGHT — lenses after the work

1. **Diff-surface auditor.** Exactly four files written: CB-A (7 in-place
   edits + 1 appended section), CB-B (3 in-place edits + 1 appended section),
   this record, and the new probe. SCUR-1's artifact and probe: zero bytes
   changed. No canon, docs, ledger, registry, process, or vz-repair file
   touched; no git.
2. **Reader-of-the-future.** A reader of CB-A/CB-B now meets, at every stale
   locus, the struck original plus a dated pointer to a block stating what
   replaced it and why — the same experience CB-E's E3/E9 gives — instead of
   silently re-importing a pre-correction source reading.
3. **Instrument auditor.** Both probes green with working failure paths:
   SCUR-1's 52/52 + 6/6; FIX-A's 69/69 + 6/6 with a planted-positive control
   guarding its absence checks (VERIFICATION.md rule 4).
4. **Comparator-boundary auditor.** Every corrected sentence moves ATTRIBUTION
   or TYPING toward the source-native reading; no comparator mathematics was
   weakened, and each block ends by banking it explicitly.
5. **Mint-boundary auditor.** Everything SCUR-1 assigned to Arc-1 (ledger-row
   re-scoping, residue grades T3, CB-C's mechanism menu, registry pointers,
   the PD-SIGNATURE-PARITY cross-check) remains named-not-done; the blocks
   list those consumers so the mint cannot miss them, and edit none of them.

## 7. Blunt paragraph — did any correction change a verdict rather than a premise?

**No, and this was checked token by token rather than asserted.** All five
blocks correct what a sentence *attributed to the source* or *typed as fact*
(mechanism attribution on D2/SM-9, KK-scalar mode typing on E2/SM-6, replica
absolutism on F2); every verdict token those sentences sat next to is
byte-unchanged (probe checks P1–P6), and the one verdict CELL edited at all —
E2's — was edited on SCUR-1 §7's explicit instruction, retains its NEEDS-U4
classification, and loses only the shape constraint that rode the dead scalar
typing. The honest near-miss to record: striking D2's "cannot work" clause
weakens what the OVER-DETERMINED verdict *appears* to cover — that is the
point of the correction, and it is a scope re-statement (comparator carrier,
per SCUR-1's SPLIT typing), not a verdict movement; the row still says
OVER-DETERMINED, now against the mechanism class the proof actually binds.
Whether any downstream ledger row's verdict must move on this re-scoping is
exactly the question SCUR-1 §6 items 1–4 hand to the v0.259-or-later mint,
and it is left there.

## 8. Certificate

```
_local/cas-venv/bin/python tests/channel-swings/joe_directed_fixa_cb_corrections.py
    -> 69/69 checks, exit 0
_local/cas-venv/bin/python tests/channel-swings/joe_directed_fixa_cb_corrections.py --selftest
    -> clean baseline verified FIRST (69/69), then 6/6 machinery-corruption
       mutations each produce a genuine [FAIL] (wrong-file redirect,
       missing-file path, empty-needle guard, count corruption,
       contrary-control inversion, blinded-absence-detector caught by the
       planted positive); crash-catches rejected by design; exit 0
_local/cas-venv/bin/python tests/channel-swings/joe_directed_scur1_source_currency_audit.py
    -> 52/52 checks, exit 0 (post-application; pins unfired, probe untouched)
```

Check inventory (exact, no floats anywhere; total 69): 5 block-presence + 2
umbrella-heading pins (the umbrella phrase is the absence-detector marker,
positively pinned where it must exist); 8 SCUR-1 needle re-assertions
(append-only invariant); 8 struck spans; 3 dated bracket markers; 6
corrected-content pins; 6 verdict-preservation pins; 11 register-owner
citation pins; 9 cross-link and provenance anchors (including SCUR-1 §7's
"NOT applied" heading and the SCUR-1 probe's untouched ageing note and
needle table); 3 no-expansion absence checks on CB-C/CB-D/CB-E; 1
planted-positive detector-power control; 7 exact count assertions (3 + 2 = 5
blocks; 8 + 3 locus pointers; 12 + 4 strike delimiters, parity-checked). A
first-draft certificate wrote 62 here from a hand count; the wired total is
69 and the printed number above is the measured one — the correction is
recorded rather than overwritten silently.

---

*Filed 2026-08-17, FIX-A, source-currency channel. Application of record
only: no canon, ledger, registry, verdict, grade, count, bar, or audited
adjudication moves on this artifact; the sixth SCUR-1 diff is not applied;
Arc-1 inherits SCUR-1 section 6 unchanged.*
