---
artifact_type: joe_directed_native_research_result
created: 2026-08-26
status: THEORY_WIDE_FALSIFIER_NOT_YET_ADMISSIBLE__W229_RECORD_CURRENT_BRANCH_BOUNDED_NO_GO_PRESERVED__K77_NATIVE_FORWARD_PACKET_REQUIRED
title: "TWG-1: the theory-wide gravity test is action-complete black-hole existence, not an imported-metric residual"
target_claim: GU-AS-RELEASED-STRONGFIELD
target_claim_verdict: NOT_ADJUDICATED
target_claim_note: "The target is the checked released GU action grammar on the selected K77 real form after its fields, coefficients, variations, constraints, boundary conditions and observed reduction are frozen. Arbitrary future completions are new constructions, not members silently retained inside this target."
bears_on: [LT-GR9, SC-ACT-01, SC-ACT-02, SC-ACT-03, SC-ACT-04, SC-ACT-05, SC-ACT-06]
historical_name: ELProjectedGRShadowTheorem
canonical_effect: pending_integration
steering_effect: unchanged
grade: "EXACT corpus/quantifier composition and executable claim-ceiling contract; inherited exact/computed residual certificates replayed separately; no new nonlinear black-hole solution, action ownership, ledger verdict, canon verdict, prediction, confirmation or public-posture change"
registry: lab/process/theory-wide-gravity-falsifier-contract.json
scripts:
  - tests/channel-swings/joe_directed_twg1_theory_wide_gravity_falsifier_contract.py
depends_on:
  - lab/process/conditional-physics-ledger-v0.263.json
  - explorations/W225-gravity-projected-shadow-schwarzschild-cheap-read-2026-07-15.md
  - explorations/W236-gravity-theta-sector-residual-built-action-2026-07-15.md
  - explorations/W238-gravity-willmore-residual-kerr-2026-07-15.md
  - explorations/recovery-nogo-gr-w229-swing3-adjudication-2026-07-16.md
  - explorations/conditional-build/w154-w229-k77-action-owner-qualification-2026-08-24.md
  - explorations/conditional-build/gravitational-anchor-bucket-disposition-and-first-fitting-construction-2026-08-23.md
---

# TWG-1: the theory-wide gravity test is action-complete black-hole existence

## Result first

The repository was right that strong-field gravity is the nearest route to a
whole-theory falsification, but the historical one-line test was not typed
tightly enough to deliver that verdict.

The exact current result is:

```text
W203/W229 record-current exact-vacuum branch:  BOUNDED NO-GO
GU-as-released on the selected K77 horn:        NOT YET ADJUDICABLE
arbitrary future things called GU:              NOT A FROZEN TEST TARGET
```

The first line is real. On the imported Schwarzschild control, the built
linear residual vanishes, the principled quadratic trace-free `Q(B)` residual
does not, and the W229 vacuum source law gives `Psi=0 -> J=0 -> theta=0`.
Neither `kappa` nor `Z_U` can tune a zero source tensor into the required
canceller. The three-swing defense found no admissible survivor in its frozen
construction census.

That does **not** yet falsify the whole released theory. W154/W229 is a
conditional K95 reconstruction, not an admitted selected-K77 action owner;
the current corpus has no coefficient-complete K77 strong-field action, no
native Lorentzian observed reduction, and no stationary horizon-bearing
solution space over which a universal residual statement could quantify.
The strongest honest advance is therefore to replace the loose slogan with an
executable falsifier contract whose next failure can actually be theory-wide.

```gu-typed-objects
result: a three-domain falsification hierarchy and action-complete strong-field decision contract
carrier: repository records typing the not-yet-constructed selected-K77 source-to-observed strong-field domain LAYER=UNTYPED CHIRALITY=N/A
pairing: NONE
real_structure: selected Spin(7,7) real form transported through an explicit real action/reduction bridge
grading: bosonic gravitational section equation with gauge/constraint quotient and perturbation complex
action_owner: repository-construction contract over source-print SC-ACT-01-through-SC-ACT-06; the physical action owner remains UNTYPED until field coefficient variation boundary and Hilbert roles are complete
target: GU-AS-RELEASED-STRONGFIELD as a claim-domain adjudication MAP-TYPE=not-a-map
```

## The crucial correction: freeze what “the whole theory” means

A theory-wide falsification needs a bounded quantifier domain. Three domains
were being mixed:

| domain | what it contains | current status |
| --- | --- | --- |
| `D0-W229-RECORD-CURRENT` | the reconstructed W203/W229 record-current exact-vacuum branch and its predeclared escape census | `BOUNDED_NO_GO`; valuable, but branch-local |
| `D1-GU-AS-RELEASED-STRONGFIELD` | the checked released action grammar `SC-ACT-01` through `SC-ACT-06` on selected K77, after its complete native field/variation/domain and observed reduction are frozen target-blind | the real whole-theory target; `NOT_ADJUDICATED` |
| `D2-ARBITRARY-FUTURE-COMPLETIONS` | any new action, field, constraint, boundary term or adapter later proposed under the GU name | invalid as a finite falsification target |

`D2` is not a protective halo around `D1`. If `D1` is killed and someone later
adds an unowned term, that term defines a new construction. It does not erase
the falsification of the released theory. Conversely, a failure inside `D0`
cannot be promoted to `D1` merely because both use GU notation or a
fourteen-dimensional carrier.

This fixes the moving-target problem without overreaching: the whole-theory
claim is strong because the released action grammar is finite, and honest
because future genuinely new theories remain logically possible.

## Arc 1: residual composition, with the ceiling preserved

The inherited strong-field certificates compose as follows:

1. W225 imports Schwarzschild and finds every built linear-in-`M` projected
   section term zero. This is a comparator clear, not a native solution.
2. W236 closes the W229 theta row conditionally: in the record-current vacuum,
   `Psi=0` gives `J=0`, then the screened operator gives `theta=0`.
3. W238 extends the linear Willmore clear to Kerr in harmonic gauge, with the
   rotation multipoles carried by the componentwise-harmonic argument.
4. The forced-coefficient recovery test retains a nonzero trace-free
   quadratic `Q(B)` residual and no W229 source/YM canceller.
5. The no-go defense preserves that result after a history audit, alternate-
   construction attempt and hostile adjudication.

The radial falloff is not used as the theorem's load-bearing datum. Existing
records use both `O(M^2/r^4)` schematic order and `M^2/r^6` component order
under different normalization/component conventions. What survives every
receipt is the fact needed here: the principled trace-free residual is
nonzero and quadratic in `M`, while the W229 exact-vacuum source is zero.

Therefore:

```text
D0 result = BOUNDED_NO_GO.
D1 result does not follow.
```

## Arc 2: released-action completeness audit

The source register has a finite census: `SC-ACT-01` through `SC-ACT-06`.
That is enough to freeze the attribution boundary, but not yet enough to run
the strong-field equations.

| source object | what it owns | why it does not yet close strong field |
| --- | --- | --- |
| `SC-ACT-01` | first bosonic augmented-torsion action `I1B` | no complete selected-K77 observed strong-field variation/reduction |
| `SC-ACT-02` | first-order swervature-equals-displasion equation and annotated Einstein-like recovery sketch | annotation is not the derived four-dimensional equation or its coefficients |
| `SC-ACT-03` | Dirac-pair architecture | architecture does not construct the stationary gravity operator |
| `SC-ACT-04` | bosonic residual-square action `I2B` | no native black-hole background, physical domain or observed perturbation operator |
| `SC-ACT-05` | total boson/fermion residual alternatives | zero-fermion packaging supplies no separately derived strong-field matter/ray coupling |
| `SC-ACT-06` | claimed Euclidean moduli/deformation complex | not a constructed Lorentzian hyperbolic reduction or stationary horizon solution |

The tempting shortcut is W154/W229. Its own qualification result rules that
out: K95 and K77 have different real vector-form inertias and different real
Clifford module types; common dimension and complexification are not a real
action bridge. Its coefficients and Euler/Hilbert roles are incomplete, and
the literal W154 pointwise identity conflicts with a genuine nonzero-`Z_U`
W229 completion. The branch remains useful evidence at its filed grade, but
cannot own `D1`.

## Arcs 3 and 4: the five missing owners

The 2026-08-23 gravitational disposition named the right five requirements.
This wave makes them the admission boundary rather than a list of desirable
extras.

| gate | required object | current state |
| --- | --- | --- |
| `SF-1` | complete selected-K77 vacuum equation, projected residual, `alpha_W`, `E_s^theta`, all Euler and Hilbert roles | `MISSING` |
| `SF-2` | observed matter coupling and the timelike/null motion equation | `MISSING` |
| `SF-3` | native-to-observed Lorentzian reduction, gauge/constraint quotient, causal region, boundary data and hyperbolic problem | `MISSING` |
| `SF-4` | a native stationary horizon-bearing asymptotically admissible background, not imported Schwarzschild/Kerr | `MISSING` |
| `SF-5` | branch perturbation operator, physical boundary conditions, shadow map and leading ringdown spectrum | `MISSING` |

No current artifact jointly owns even `SF-1 + SF-4`. That is why rerunning the
imported Schwarzschild residual cannot make the next result theory-wide. The
missing object is not “more numerical precision”; it is the native solution
space itself.

## Arc 5: the three theory-wide kill horns

Once `D1` and `SF-1` through `SF-3` are frozen before target confrontation,
exactly three honest strong-field kill horns remain.

### `K1-NONEXISTENCE`

The complete admitted equations have no stationary horizon-bearing solution
in the frozen physical domain. This kills released GU strong-field recovery
without needing to compare a Schwarzschild component.

### `K2-UNIVERSAL-PHYSICAL-RESIDUAL`

Stationary candidates exist, but every candidate has a nonzero residual that
survives gauge, constraints and boundary/domain legality. A coordinate
artifact, target-shaped source, or failure outside the physical quotient does
not fire this horn.

### `K3-UNIVERSAL-OBSERVABLE-CONFLICT`

Native stationary solutions survive, but every solution in the frozen class
violates the preregistered motion, shadow or leading ringdown comparator after
the theory's independently fixed nuisance freedom and the data uncertainty
are accounted for.

Any one horn falsifies `GU-AS-RELEASED-STRONGFIELD`. A clear has a lower
existential burden: at least one target-blind native horizon-bearing solution
must survive the physical quotient and reproduce the frozen motion, shadow
and leading ringdown outputs. That clears the strong-field leg at the stated
grade; it does not prove uniqueness or every other GU claim.

## Invalid escapes and false kills

The executable contract rejects all of these:

- importing Schwarzschild or Kerr as GU's stationary solution;
- defining a source or ambient term to be minus the observed residual;
- transferring K95 ownership to K77 through notation, dimension or
  complexification;
- promoting `D0` into `D1` without complete action ownership;
- calling the unbounded future-completion class “the whole theory”; and
- treating the zero linear residual as an exact clear while the nonlinear
  residual, native background and observables remain open.

The last point is important: W225/W236/W238 are good tests with real teeth.
Their zeroes are worth retaining. They are simply not the quantifier domain
needed for the final verdict.

## The next wave is now executable

The successor is a three-packet sequence, not another metric substitution.

1. **`TWG-2A-K77-ACTION-COMPLETE-INTERFACE`.** Freeze `SC-ACT-01` through
   `SC-ACT-06` as one selected-K77 field/action/variation domain and derive
   the observed vacuum operator through `SF-1` and `SF-3` before choosing a
   black-hole metric. Failure here is already decision-grade if it proves the
   released grammar inconsistent or unable to define a legal observed
   strong-field problem.
2. **`TWG-2B-STATIONARY-HORIZON-SOLVE`.** On the admitted operator, solve a
   preregistered stationary spherical class. Only if it survives, move to the
   axisymmetric class. This is where `K1` or `K2` can fire.
3. **`TWG-2C-MOTION-SHADOW-RINGDOWN`.** Derive `SF-2` and `SF-5`, then confront
   the frozen shadow and leading ringdown outputs without retuning. This is
   where `K3` can fire or LT-GR9 can clear.

This sequencing preserves the theory-wide ambition and removes the old
target-import loophole. The next expensive computation will either create the
first legitimate black-hole solution space for released GU or fail in a way
that can count against the whole released theory.

## Claim ceiling

TWG-1 changes no ledger row, action ownership, claim status, canon verdict,
prediction, confirmation, public posture or scheduled priority. `LT-GR9`
remains `NEEDS / MISSING_CONSTRUCTION`. The canonical effect is a pending
integration proposal: replace the historical imported-metric slogan with the
three-domain and five-owner contract only after independent owner review.

Reproduce the contract and its planted false-promotion controls with:

```bash
python3 tests/channel-swings/joe_directed_twg1_theory_wide_gravity_falsifier_contract.py
```

Durable in GU Formalization; scheduled priority unchanged; canonical delta
pending independent integration.
