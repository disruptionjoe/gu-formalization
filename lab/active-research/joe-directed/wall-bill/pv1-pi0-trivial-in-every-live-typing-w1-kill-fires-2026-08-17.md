---
artifact_type: exploration
status: exploration
doc_type: construction_result
created: 2026-08-17
work_item: PV-1
channel: wall-bill
wave: pi0-of-the-native-vacuum arc of RS wave 3
title: "PV-1: pi_0 of the native varpi vacuum set is TRIVIAL in every live typing, and the W1 kill branch FIRES. The native order parameter is typed from the source's own sentences (the sub-fields of varpi, the declared 1-form slot Omega^1(ad P), the quartic-plus-curvature-set-quadratic potential of drafts:146/:149) and its vacuum set is computed in every typing the corpus grounds: every level-set typing lives on a space whose invariant form has BOTH inertia indices >= 2 (internal 120: (56,64); Lambda^1: (6,4); Lambda^5: (132,120); cross-half middle-form pair: (1716,1716); tensor slot (52,48)) so the level set is CONNECTED in closed form via exact O(p,q) component-group arithmetic; every orbit typing is an orbit of a CONNECTED group (SU(3,2), Spin(6,4), Spin(9,5)-gauge, U(64,64)) and is connected outright; the honest native fiber typing (vacuum set = a fiber of mu(a) = a wedge a, the map BOTH stated potential terms factor through) has its +-a pair connected by an exact rotation path lying inside the stabilizer of the curvature background, computed in the minimal nonabelian model; and the ONLY disconnected renderings anywhere are 1-real-dimensional frozen lines — exactly rung-2's declared INPUT shape, grounded by ZERO source sentences, cut against by the source's own plural ('the various sub-fields'), and even THERE every varpi-slot line is reconnected by an explicit per-line gauge connector landing in SO+ on BOTH signature horns (exact component arithmetic; the complete six-line invariant inventory of both slots is enumerated, FIVE of the six line types reconnect through explicit SO+ connectors, and the single genuinely inert disconnected line — the Ad-trivial eps-slot Lambda^0 trace — fails grounding three pinned ways: wrong slot against the hedge's explicit varpi, singular (the center) against the plural, class-0 against the selection arithmetic). NEW EN ROUTE: on either horn (q odd) the ambient metric quadratic restricted to a single chirality-selective middle-form half Lambda^7_+- is IDENTICALLY ZERO (the halves are totally isotropic), so the source's stylized Mexican hat cannot even be written on the selective direction at the ambient layer — its first nondegenerate home is the branched-layer internal 120. Verdict: KILLED-AT-W1 at typing grade; the VEV-orientation Z/2 sector datum does not exist as a nontrivial choice in any grounded typing; the revival forks are named (ground the inert eps-line against three guards, or supply a varpi confinement plus connector-breaking at once)."
grade: "EXACT everywhere load-bearing: integer/Fraction arithmetic only in the probe (subset-parity signatures; O(p,q) component classes as explicit sign pairs with the image and quotient computed mechanically; the Hodge-star isotropy verified monomial-by-monomial over all 3432 subsets on BOTH horns with a q-even contrary control where *^2 = -1; the mu-fiber rotation path verified at rational Rodrigues samples with exact SO(3) membership, background stabilization, fiber membership, and the -a endpoint; the Spin(4)-singlet multiplicities by doubled-weight combinatorics reproducing TR-1's [1,0,0,0,1] and extending it to the varpi slot's [0,1,0,1,0] form-leg row). Closed-form topology facts (pseudo-sphere S^{p-1} x R^q structure; connectedness of SU(p,q)/U(p,q)/Spin(p,q)/SO+; Witt transitivity; continuous image of connected is connected) are STANDARD MATHEMATICS, named not re-proved, with the probe verifying every mechanical computation this artifact performs with them. The comparator legs bind their models (routing fence below). NO claim-status movement, NO register edit, NO canon movement. The kill is aimed at a REPO CONSTRUCTION TARGET, not a source row — the source asserts no wall, and no source claim dies."
disposition: NATIVE_ORDER_PARAMETER_TYPED_TO_THE_DECLARED_VARPI_SLOT__EVERY_LIVE_TYPING_HAS_CONNECTED_VACUUM_SET__LEVEL_SETS_CONNECTED_BY_EXACT_COMPONENT_ARITHMETIC_BOTH_INERTIA_INDICES_GEQ_2__ORBITS_CONNECTED_BY_CONNECTED_GROUPS__MU_FIBER_PM_PAIR_CONNECTED_IN_MODEL_INSIDE_THE_BACKGROUND_STABILIZER__ONLY_DISCONNECTED_RENDERINGS_ARE_UNGROUNDED_1D_FREEZES__EVERY_VARPI_LINE_RECONNECTS_THROUGH_A_PER_LINE_SO_PLUS_CONNECTOR__THE_INERT_EPS_LINES_FAIL_GROUNDING_THREE_PINNED_WAYS__AMBIENT_HAT_UNWRITABLE_ON_A_SELECTIVE_HALF_ISOTROPY__E2_FAILS__KILL1_FIRES_AT_TYPING_GRADE__W1_TARGET_DIES__REVIVAL_FORKS_NAMED
target_claim: "RW-1's join disposition — a REPO CONSTRUCTION TARGET, not a source row — verbatim: 'The join's disposition. The join types into a NAMED CONSTRUCTION TARGET: VEV-orientation (Z/2) domain walls inside the broken region, steered by R(y) — existence condition = the unfixable orientation Z/2 (P7), location selection = rung-2's N2 gradient with grad R(y) the source-native candidate (P6+P1), envelope = the Mexican-hat depth R coaxes (P2+P3) — carrying the bill W1-W6, with the zero locus itself measured non-hosting as stated (section 4c).' (lab/active-research/joe-directed/rwall/rw1-zero-locus-steers-not-hosts-2026-08-17.md:456-462), machine row '| JOIN | what the join types into | TYPED-TARGET-VEV-ORIENTATION-WALL-R-STEERED | chain_exponents_and_hosting |' (rw1:525) — adjudicated at the gate WB-1 established for it: 'E2 — a disconnected vacuum set: pi_0(V) != 0' ... 'E2 is the gate.' (wb1:322, :337) with the kill branch 'KILL-1 (the live one): pi_0(native vacuum set) computed TRIVIAL' (wb1:564). Register item executed: W1-PI0-NATIVE-VACUUM (lab/process/upgrade-program-register.yaml:263-269)."
target_claim_verdict: "KILLED-AT-W1 AT TYPING GRADE. E2 fails in every typing of the native varpi vacuum set the corpus grounds: pi_0 is trivial (and, sharper, the +-VEV pair is connected inside the vacuum set) in the level-set typings (closed form, exact signatures), the orbit typings (connected groups), the honest mu-fiber typing (model-exact connector inside the background stabilizer), and even the 1-dimensional frozen-line renderings once the freeze is released (per-line SO+ connectors, exact component arithmetic on both horns; the complete six-line inventory is enumerated, five line types reconnect, and the single inert disconnected line — the Ad-trivial eps-slot Lambda^0 trace — fails grounding three pinned ways). The Z/2 the target needs is not merely unselectable (rung-2's T-DEG) and not merely unrealized (WB-1's external relocation) — in every grounded typing it DOES NOT EXIST as an object: there is no second component to flip into. The construction target TYPED-TARGET-VEV-ORIENTATION-WALL-R-STEERED loses its existence condition at W1. NOT a source-claim kill: SC-CHI-01/02/52/53, SC-GEO-58, SC-MAS-01 all survive untouched — the source never asserted a wall, a flip, or a disconnected vacuum set (WB-1's certified 0/0/5 scan). The revival forks are stated in section 5 (ground the inert eps-line against three guards, or supply a varpi confinement plus connector-breaking simultaneously); RW-1's comparator mathematics, WB-1's relocation theorem, rung-2's degeneracy result, the R-steering location results, and canon's external-by-structure decomposition (flux exemplar) all SURVIVE."
canon_verdict_change: none
row_change: none
registry_change: none
steering_effect: unchanged
canonical_effect: pending_integration
scripts:
  - tests/channel-swings/joe_directed_pv1_pi0_trivial_in_every_live_typing_w1_kill_fires.py
inputs:
  - lab/active-research/joe-directed/wall-bill/wb1-crossing-is-a-sector-datum-r-cannot-supply-2026-08-17.md
  - tests/channel-swings/joe_directed_wb1_crossing_is_a_sector_datum_r_cannot_supply.py
  - lab/active-research/joe-directed/rwall/rw1-zero-locus-steers-not-hosts-2026-08-17.md
  - explorations/big-swing-2026-07-06/VG-V5-breaking-coset-topology.md
  - explorations/prereg-rung2-dynamical-wall-and-selectability-test-2026-07-29.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/rs-corner/rsc1-unique-channel-lives-on-the-gamma-trace-2026-08-17.md
  - lab/active-research/joe-directed/spectral-transport/tr1-transport-and-selection-are-opposite-parities-2026-08-17.md
  - lab/methods/gu-base-categories.md
  - CANON.md
  - canon/external-by-structure-synthesis-RESULTS.md
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - lab/sources/source-claim-register.yaml
  - lab/process/correction-registry.yaml
  - lab/process/upgrade-program-register.yaml
  - explorations/decoupling-constructibility-packet-2026-08-12.md
  - papers/drafts/Transcript into the impossible.md
  - VERIFICATION.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result. Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.
>
> The comparators here are (i) the stylized Mexican-hat potential — the shape
> the source itself asserts curvature produces (drafts:149; SC-GEO-58) — typed
> `FENCED-COMPARATOR` wherever consumed, exactly as RW-1 §4 and WB-1 §4 fenced
> it; and (ii) the minimal nonabelian mu-fiber model of §3.4 (su(2) over two
> form legs), which binds that model. The exact representation-theoretic and
> topological computations (signatures, component groups, isotropy, singlet
> multiplicities) are structural facts about the DECLARED carriers and slots,
> common to both SIGNATURE-AMBIENT horns where stated; they bind the typings,
> not the unbuilt native action. The (9,5) transport fence of rung-2
> (prereg:43-45) and RW-1's W3/W4/W6 seams are inherited unchanged wherever
> the wall vocabulary appears.

# PV-1 — pi_0 of the native varpi vacuum set: typed, computed, and the kill branch fired

**What this is.** WB-1 relocated RW-1's W1 (the load-bearing crossing item of
the VEV-orientation wall target) to an external Z/2 sector datum and left ONE
stacked gate open: *"E2 — a disconnected vacuum set: pi_0(V) != 0. The
orientation Z/2 exists as an object only if the broken-phase vacuum set has at
least two components. NOT source-stated."* ... *"**E2 is the gate.**"*
(wb1:322-337). The register item W1-PI0-NATIVE-VACUUM
(upgrade-program-register:263-269) demands: type the native varpi vacuum-set
object FIRST, then compute pi_0, with both outcomes first-class. This artifact
does exactly that: (a) types the native order parameter from the source's own
sentences and the repo's conditional build, enumerating every live typing with
its grounding quote; (b) computes pi_0 for each typing exactly (component
groups, signatures, isotropy, explicit paths); (c) adjudicates the two
standing proxies; (d) composes the verdict. **The composed answer: pi_0 is
trivial in every live typing — the two proxies disagreed because they typed
different objects, and the native object sides with the connected one in
every grounded rendering. KILL-1 fires. The wall target dies at W1.**

---

## 0. PREFLIGHT — six problem-matched lenses, declared before the work

- **L1 — Topology lens (mandatory).** pi_0 questions about vacuum sets reduce
  to three exact machines: component groups of cosets (pi_0(G/H) =
  pi_0(G)/im pi_0(H)), connectivity of quadric level sets ({Q = c} on
  R^(p,q) has 2 components iff the relevant inertia index is 1, else 1), and
  continuous-image connectivity (orbits of connected groups; quotients never
  disconnect). Predicts the whole question hinges on ONE integer per typing:
  the relevant inertia index (or the 1-dimensionality of a frozen line).
  Confirmed: every live typing has both indices >= 2 (§3.1); the only index-1
  renderings are ungrounded freezes (§3.3).
- **L2 — Layer-discipline lens (mandatory).** Ambient vs branched vs 4d
  changed TR-1's answer; check every typing at both live layers. Earned its
  keep twice: (i) the ambient metric quadratic on a single selective
  middle-form half is IDENTICALLY ZERO on both horns (q odd; the halves are
  totally isotropic, §2) — so the stylized hat is not even writable there and
  the selective-direction typing is forced to the branched layer, where the
  internal 120 carries a nondegenerate (56,64); (ii) the varpi slot's
  Lorentz-singlet enumeration differs from the eps slot's by the form leg
  (TR-1's "parities flip"), and that is exactly where the only 1-dimensional
  candidate lines live (§3.3).
- **L3 — Orbit-vs-frozen-section auditor.** The vacuum SET (WB-1's object:
  V = {phi : V(phi) = min} in the full field space) and a frozen 1-dimensional
  section of it are different objects with different pi_0. Predicts rung-2's
  disconnected {-v,+v} is a fact about the frozen section, not about any
  native vacuum set. Confirmed and sharpened: the freeze is rung-2's own
  declared INPUT (prereg:51-52), and the un-frozen set reconnects through an
  explicit SO+ element on both horns (§3.3) — the {±v} shape is not merely
  ungrounded, it is structurally unavailable to every grounded typing.
- **L4 — Coverage archaeologist (grep before novelty).** Found and consumed:
  the exhaustive pi_0-component table over O(3,1) in the time-orientation
  result (improvement-register:980-1012) — the component-arithmetic THEOREM
  SHAPE exists in-repo for a different object; the "13-dimensional
  pseudo-sphere orbit" vocabulary (selected-k77-two-half-hermitian-witt-
  rotation-gate:50) — pseudo-sphere orbits named for the covector-line
  choice, a different object; Witt-polarization isotropic-plane vocabulary
  (ar4:229-231, cb3/cb4) — isotropy used on so(7,7) polarizations, never on
  the middle-form halves of the Lambda^7 metric form. Zero hits for any
  prior pi_0 computation of any vacuum set (§9). Zero hits recorded as zero
  hits, not as proof of novelty.
- **L5 — Hostile self-auditor (the import lens, both directions).** Attack
  one: "you are importing the standard-Higgs vacuum-manifold machinery
  (orbits, cosets, Kibble) onto a mechanism the source states in curvature
  vocabulary." Held off by typing: every orbit/level-set computation is run
  on the DECLARED slots and their invariant forms, the stylized hat is
  consumed only as the source's own asserted shape (drafts:149) and fenced,
  and the honest native rendering (the mu-fiber, §3.4) is computed
  separately precisely so the verdict never rests on the hat import alone.
  Attack two (the reverse pressure): "the wave wants the kill — the decisive
  computation arc is glory-shaped." Answered by construction: the kill fires
  only because EVERY typing lands connected; §3.3 hunts specifically for the
  disconnected escape (the 1-dim lines are found, named, and adjudicated
  rather than suppressed), and the revival condition is stated as a named
  fork, not buried (§5).
- **L6 — Homonym warden.** "The VEV" is the registered four-sense term
  (packet:70): (i) varpi sub-fields / (ii) spoken Dirac-operator VEV /
  (iii) curvature-coaxed augmented-torsion VEV / (iv) repo weight pair.
  Senses (i) and (iii) co-locate in the declared 1-form slot and are typed
  WITHOUT being identified (§1); sense (ii) reads the same varpi through the
  operator pencil (SN-1's frame); sense (iv) is repo-owned and excluded.
  "Chirality" stays the registered four-way homonym (packet:69); no wall
  grading question is re-opened (RW-1's W3 inherited untouched, and §5 states
  what the kill does to it). "Vacuum manifold" (VG-V5's word) vs "vacuum set"
  (WB-1's word) are used interchangeably here for {minima}; where VG-V5's D
  is meant, it is named D.

Retrieval-first was run before any computation, including
`process_gates/canonical_currency_audit.py` over all consumed inputs; the
per-input records are §10.

**Frame carried (quoted; the standing corrections this lane touches, from
`lab/process/correction-registry.yaml` key `canonical_source_corrections`):**

- **CC-06-CHIRALITY-VEV-CONDITIONAL** (canonical_since 2026-08-16),
  superseded reading quoted: *"That the source has no stated
  effective-chirality mechanism, or that chirality can only come from an
  unbuilt mirror-gapping condensate -- when observed chirality is
  VEV-CONDITIONAL ('exactly three families of chiral fermions if you have a
  decreased VEV ... taking a Dirac equation into two [Weyl] equations',
  drafts L158 / ucsd 00:46:02) and the selector is SG4 bit 2, OPEN by
  design."* Carried throughout: chirality is VEV-conditional; SG4 bit 2 stays
  OPEN; nothing here selects a phase. The wall's broken region is the
  varpi-pulled-up phase per the corrected LA3 direction (base-categories:91:
  *"the hedge's decoupling holds AT small `varpi`"*).
- **CC-05-SUBTRACTIVE-TWO-PLUS-ONE** (canonical_since 2026-08-14), quoted:
  *"That three generations is an ADDITIVE target count a mechanism must
  produce -- when the source says it is 'really two plus one' with the third
  family an imposter 'for representation theoretic reasons' (drafts
  L119/L128/L131) and HE-1 makes the partition FORCED and SUBTRACTIVE
  (n_g -> n_g - 1, unlabelled, the distinguished family REMOVED)."* Applied
  as a fence at §6; no count statement here is additive.
- **CC-08-DARK-PARTNER-OBLIGATION** (canonical_since 2026-08-15), quoted:
  *"That the 128 remainder is an established DEFECT of the construction --
  when the source NAMES dark sectors ... and the remainder is a
  partner-placement / decoupling OBLIGATION, not an established defect."*
  The 128 enters here only through RSC-1's consumed corner blocks; no defect
  framing.
- **CC-10-UCSD-EDITED-DERIVATIVE** (canonical_since 2026-08-15), quoted:
  *"That the repository's UCSD 2025-04 transcript copy is a PRIMARY SOURCE --
  when it is an EDITED DERIVATIVE whose chain sentence at [00:45:00] had to
  be restored on 2026-08-15 and whose audio confirmation is still owed."*
  Every UCSD-lineage quote here (drafts:146/:149/:158) is treated as
  transcript-verified edited-derivative material, nothing more.

---

## 1. (a) The native object, typed

**The field.** The source's own hedge names the order parameter: the
decoupling holds *"when there is no vacuum expectation value pulling the
various sub-fields of ϖ to values significantly above zero"*
(extraction:137-138; register:922-923 carries the same clause as SC-CHI-01's
operative note). The declared home of ϖ in the conditional build:
**`ϖ ∈ Ω^1(ad P)`**, the 1-form bosonic slot, with
`ad P = End(Δ) = 2(Λ^0 ⊕ … ⊕ Λ^6) ⊕ Λ^7_+ ⊕ Λ^7_-` (dim 16384) — ST-1's
fixed conventions, verbatim: *"Declared bosonic slots: `ε ∈ Ω^0(ad P)`,
`ϖ ∈ Ω^1(ad P)`"* (st1:208-210). The registered VEV senses (i) varpi
sub-fields and (iii) curvature-coaxed augmented-torsion VEV (packet:70) both
land in this slot and are NOT identified with each other; sense (ii) is the
same ϖ read through the operator pencil; sense (iv) is repo-owned and
excluded.

**The stated potential.** The source states exactly two potential-shaping
terms, both through the same quadratic map. Quartic: *"You have that a wedge
a in the perturbative expansion of a curvature tensor. So when you take its
norm square, you get a quartic."* (drafts:146). Quadratic: *"If you take the
norm square, you also get a term that looks like the unperturbed curvature,
interproducted with a wedge a, which is a quadratic. So if your curvature is
negative, now you start to get a Mexican hat potential."* (drafts:149;
SC-GEO-58 register:2329/:2336, a DISAVOWS row: no fundamental Higgs, the hat
is curvature-produced). Writing mu(a) = a ∧ a (the wedge-bracket square),
the stated potential class is

    V(a) = ||mu(a)||^2 + <F̄_0, mu(a)>  =  ||mu(a) + F̄_0/2||^2 − ||F̄_0||^2/4,

up to normalization — BOTH stated terms factor through mu, and mu(−a) =
mu(a): the potential class is even in a (WB-1 §4a's exact identity, carried).
The Mexican-hat FORM is the source's stylized rendering of this class and is
consumed as `FENCED-COMPARATOR` throughout.

**The object.** The native varpi vacuum set is
**V = {a in the slot's field space : V(a) = min}** — WB-1's object,
pointwise over the broken region. The vacuum set is a subset of the FULL
value space acted on by the declared invariances (the gauge/structure group
on the ad-legs; Lorentz at the branched layer); it is not a frozen
1-dimensional section of that space (L3). The corpus does NOT construct the
native action (SG4 bit 2 OPEN; the build is conditional), so the honest
deliverable is the exhaustive fork table of live typings, each with the
sentence that grounds it, each computed in §3.

**The fork table of live typings (exhaustive over the grounded readings):**

| # | typing of the vacuum set | order-parameter space (layer per `lab/methods/gu-base-categories.md` / carrier tokens) | grounding sentence | computed in |
|---|---|---|---|---|
| F1 | stylized-hat LEVEL SET on the chirality-selective direction | the Lorentz-scalar part of one middle form: one internal so(10) `120` = Λ^3(V_10) per half — *"Spin(4)-singlet part of Lambda^7(V_14) = Lambda^0(V_4) (x) Lambda^7(V_10) (+) Lambda^4(V_4) (x) Lambda^3(V_10) = 120 (+) 120"*, *"each middle form Lambda^7_+- carries EXACTLY ONE so(10) `120`"* (rsc1:506-509); branched layer (observed) | drafts:149 (the hat) + ST-1's selectivity: the middle forms are the ONLY chirality-selective directions (st1:384, :401-404); TR-1 §4.4: every Lorentz-preserving VEV direction has 4d form degree p ∈ {0,4} (tr1:474-477) | §3.1 row 1 |
| F2 | stylized-hat LEVEL SET on the chirality-blind Grassmann-live directions | internal Λ^1(V_10) (dim 10) or Λ^5(V_10) (dim 252); branched layer | drafts:149 + st1:389 (*"The Grassmann-live 0-form directions are chirality-blind"*) | §3.1 rows 2-3 |
| F3 | stylized-hat LEVEL SET at the ambient layer | any Λ^k component of ad P with its ambient metric quadratic, or the varpi slot's tensor space V* ⊗ Λ^k; ambient layer, both horns | drafts:146/:149 read at the layer where eq (9.16)'s slots live | §2 (the middle-form halves degenerate — the hat is unwritable there) + §3.1 rows 4-5 (the nondegenerate ambient rows) |
| F4 | ORBIT typing (vacuum set = one orbit of the invariance group) | any of the above spaces under its acting group; plus VG-V5's candidate D = SU(3,2)/S(U(1)×U(2,2)) at its own layer | VG-V5:77 (the coset presentation); Witt transitivity makes F4 and the level-set typings COINCIDE for quadratic invariants (§3.2) | §3.2 |
| F5 | honest native FIBER typing: V = mu^{-1}(−F̄_0/2) | the varpi slot with the exact stated potential (no hat stylization); ambient layer | drafts:146/:149 verbatim — both stated terms factor through mu | §3.4 (minimal nonabelian model, exact) |
| F6 | 1-real-dimensional frozen-line renderings | the complete invariant-line inventory of BOTH declared slots — SIX line types: four in the ϖ slot (δ_4, δ_10, ε_4δ_4, vol_4⊗δ_10) and two in the ε slot (Λ^0 trace — gauge-inert; vol_4 — reconnectable by a mixed pair flip); dim Inv_Spin(4)(V_4* ⊗ Λ^p V_4) = [0,1,0,1,0] supplies the form-leg types — or any by-hand 1-component freeze | NO source sentence. The freeze is rung-2's declared INPUT (*"Double-well potential `V(phi) = lambda (phi^2 - v^2)^2`; boundary sector `phi(-L) = -v`, `phi(+L) = +v`"*, prereg:51-52); the source's own plural — *"the various sub-fields"* — cuts against a 1-component order parameter (WB-1 §3 E2 already flagged this); the ε-slot lines additionally sit in the WRONG SLOT against the hedge's explicit ϖ | §3.3 |

This table is exhaustive in the following checkable sense: every candidate
order-parameter home in the corpus is a subspace of the declared slots
(ST-1/CR-B's carrier), every stated potential term is in the even class
factoring through mu (drafts:146/:149; WB-1 §4a), and the possible
vacuum-set shapes for such potentials are level sets of invariant functions
(F1-F3), orbits/unions of orbits (F4), fibers of mu (F5), or by-hand
restrictions (F6). A vacuum set that is a union of several DISTINCT-modulus
orbits would add components only by stacking F4-type pieces — each piece
connected (§3.2), and nothing in the corpus grounds a two-orbit minimum;
noted in §5 as part of the revival condition, not a live typing.

---

## 2. New en route, exact: the ambient hat is UNWRITABLE on a selective half

On either SIGNATURE-AMBIENT horn the ambient volume-form involution * on
Λ^7(V_14) satisfies *^2 = s·(−1)^(k(n−k)) = (−1)^q · (−1)^49 = **+1** for
q ∈ {5, 7} (both horns have q ODD), so the real eigenspaces Λ^7_± exist —
and the ambient metric quadratic Q obeys Q(*α, *β) = s·Q(α,β) = **−Q(α,β)**.
On an eigenspace *v = ±v this forces

    Q(v, v) = Q(*v, *v) = −Q(v, v)  =>  Q(v, v) = 0:

**each middle-form half Λ^7_± is TOTALLY ISOTROPIC for the ambient metric
form, on both horns** (probe: verified monomial-by-monomial over all 3432
subsets for (9,5) AND (7,7); the signature of Q on the full Λ^7 is split,
(1716, 1716) exactly; contrary control: for q EVEN — signature (8,6) —
*^2 = −1 and no real eigenspaces exist at all, so the phenomenon is
signature-selective, not an instrument artifact; small-instance control: on
(1,1) at k = 1 the same argument reproduces the light-cone's two null
lines).

Consequence for the typing step (this is where a wrong answer would invert
the verdict): **"the stylized Mexican hat on the chirality-selective
middle-form direction" does not type-check at the ambient layer** — the only
invariant quadratic available to build |φ|^2 out of a SINGLE half vanishes
identically, so the hat written there is the zero function. The selective
direction's hat lives ONLY at the branched layer, on the internal `120` with
the internal so(6,4) form — which is nondegenerate with signature
**(56, 64)** (exact; §3.1) — or ambiently on the CROSS-half pair
Λ^7_+ ⊕ Λ^7_− with the hyperbolic pairing, signature (1716, 1716). Either
way the typed space has both inertia indices ≥ 2, and §3.1 applies. (The
trace form on ad P restricted to an irreducible Λ^k is proportional to the
exterior-power metric — one invariant form per real-irreducible summand —
so this finding is stable under the trace-form vs form-metric choice; the
Λ^7_± cross-pairing is exactly the hyperbolic structure the isotropy
forces.)

Grep-before-novelty for this finding is recorded in §9: in-repo isotropy
vocabulary exists for so(7,7) Witt polarizations and H210 pullback planes;
zero hits for the middle-form-half isotropy or for any statement that the
selective direction carries no ambient quadratic. Zero hits ≠ novelty proof;
the finding stands on its two-line derivation and its machine check.

---

## 3. (b) pi_0 computed, per typing — exact machines, receipts pinned

**The master closed form (used by every level-set row).** For a
nondegenerate invariant quadratic Q of signature (p, q) on R^(p+q), the
level set {Q = c} with c > 0 is the coset **O(p,q)/O(p−1,q)** (Witt
transitivity), and as a set of components

    pi_0(O(p,q)) = Z/2 × Z/2 (p,q ≥ 1),  classes = (det|pos, det|neg);
    pi_0({Q = c>0}) = pi_0(O(p,q)) / im pi_0(O(p−1,q))
                    = 1   if p ≥ 2   (the image is all four classes)
                    = 2   if p = 1   (the image is {(+,+),(+,−)} — no
                                      positive H-direction exists to reflect)

and mirror in q for c < 0. Equivalently {Q = c > 0} ≅ S^(p−1) × R^q — the
pseudo-sphere — with pi_0 = pi_0(S^(p−1)): 2 iff p = 1. The probe computes
the image and quotient MECHANICALLY (explicit reflection representatives,
their sign-pair classes, subgroup closure, quotient count) and independently
verifies the connectivity by symbolic path witnesses: the radial leg
x(t) = (λ(t)·x_+, t·x_−) with λ(t)^2 = (c + t^2·Q_−(x_−))/Q_+(x_+) (an
identity Q(x(t)) ≡ c, denominator positive), and the great-circle leg on
S^(p−1), whose partner vector exists iff p ≥ 2 — the p = 1 obstruction is a
dimension count, which is exactly where S^0 appears. Deliberately
disconnected controls the machinery must REPORT as disconnected: {x^2 = v^2}
on R^1 (2 components — rung-2's row) and the (1,1) level set (the hyperbola,
2 components); connected controls it must report connected: (2,0) (the
circle) and (2,1). Standard topology facts (pseudo-sphere structure; Witt;
continuous images) are named, not re-proved — the classical-fact discipline
of RW-1 §12 P4.

### 3.1 Level-set typings (F1-F3): the signature table, exact

| row | typed space | signature (exact, subset-parity combinatorics) | pi_0({Q = c}), c > 0 | pi_0, c < 0 |
|---|---|---|---|---|
| 1 (F1) | internal `120` = Λ^3(V_10), so(6,4) form | **(56, 64)** | **1** | **1** |
| 2 (F2) | internal Λ^1(V_10) | **(6, 4)** | **1** | **1** |
| 3 (F2) | internal Λ^5(V_10) | **(132, 120)** | **1** | **1** |
| 4 (F3) | ambient cross-half middle-form pair Λ^7_+ ⊕ Λ^7_− (the isotropy-forced home, §2) | **(1716, 1716)** on both horns | **1** | **1** |
| 5 (F3) | varpi-slot internal tensor block V_10* ⊗ Λ^1(V_10) (the smallest multi-dim 1-form-leg home) | **(52, 48)** | **1** | **1** |

Every row has BOTH inertia indices ≥ 2; every level set of every sign is
connected, in closed form. (Row 1 is the selective direction's ONLY
nondegenerate hat home, by §2. Dimensions cross-checked: 120 = 56+64,
10 = 6+4, 252 = 132+120, 3432 = 1716+1716, 100 = 52+48.) The zero level
{Q = 0}\{0} is likewise connected on every row (both indices ≥ 2). There is
NO live level-set typing with an index equal to 1.

### 3.2 Orbit typings (F4): component groups, computed exactly

For orbit typings pi_0 is a compact-group-style computation, done here in
closed form per the task:

| orbit presentation | pi_0(G) | im pi_0(H) | pi_0(G/H) |
|---|---|---|---|
| VG-V5's D = SU(3,2)/S(U(1)×U(2,2)) (VG-V5:77) | **1** (SU(p,q) is connected — polar retraction to S(U(p)×U(q)), standard) | — | **1** |
| {Q = c > 0} = O(p,q)/O(p−1,q), p ≥ 2 | Z/2 × Z/2 | all four classes | **1** |
| {Q = c > 0}, p = 1 | Z/2 × Z/2 | {(+,+),(+,−)} | **2** (= S^0; rung-2's row, see §3.3/§4) |
| any single orbit of Spin(9,5)-gauge, Spin(6,4), or U(64,64) | **1** (connected groups: Spin(p,q) connected for these signatures; U(p,q) polar-retracts to U(p)×U(q)) | — | **1** |

Witt transitivity makes the orbit and level-set typings COINCIDE for
quadratic-invariant potentials: the full level set is a single O(p,q)-orbit,
so F4 adds no new pi_0 beyond §3.1 — except VG-V5's D, which this artifact
UPGRADES from measured-connected (float flow, VG-V5:75) to
**exactly connected**: the positive vectors {Q > 0} ⊂ C^(3,2) ≅ R^(6,4)
satisfy {Q > 0} ≅ {Q = 1} × R_{>0} (radial identity, symbolic in the probe),
{Q = 1} is connected by the master form (p = 6 ≥ 2), and D is its continuous
image under projectivization. VG-V5's own two fences are quoted and carried
in §4 — they fence the IDENTIFICATION of D with the GU vacuum manifold, not
D's connectivity, so the upgrade is license-free. Quotients never
disconnect: any gauge-moduli reading V/G of any connected V stays connected
(continuous image) — no quotient typing can rescue a Z/2.

### 3.3 The 1-dimensional renderings (F6): found, named, and reconnected

The hunt for the disconnected escape, run to completion instead of waved at:
a level set is disconnected only on an index-1 space, and the minimal
invariant home for an index-1 quadratic is a 1-real-dimensional
Lorentz-scalar line. The probe enumerates the Spin(4)-singlet content of the
varpi slot exactly (doubled-weight combinatorics at so(4) = A1 × A1):

    eps-slot (0-form) singlets of Λ^k:   dim = C(10,k) + C(10,k−4)
        — reproduces TR-1's [1,0,0,0,1] row (tr1:474-477) [R].
    varpi-slot (1-form) singlets add the form-leg pairings
        dim Inv_Spin(4)(V_4* ⊗ Λ^p V_4) = [0,1,0,1,0]  (p = 0..4)
        — TR-1's "for the ϖ slot they flip" (tr1 §3.2), made explicit.

Sharpening to FULLY invariant lines (Spin(4) × Spin(6,4)-invariant — the
directions a maximally symmetric rendering could freeze to), the complete
inventory over BOTH declared slots is SIX line types, enumerated
mechanically (a p-leg singlet needs internal degree 0; a V_10*-leg line
needs Inv_so(10)(V_10* ⊗ Λ^j V_10) ≠ 0, which holds iff j = 1):

    ϖ-slot (FOUR): δ_4 (k=1, form leg against Λ^1(V_4));
                   δ_10 (k=1, internal trace V_10* ⊗ Λ^1(V_10));
                   ε_4-twisted δ_4 (k=3, form leg against Λ^3(V_4));
                   vol_4 ⊗ δ_10 (k=5).
    ε-slot (TWO): the Λ^0 trace direction(s) (k=0; two printed copies) and
                   the vol_4 direction (k=4).

Each line carries a definite induced quadratic (trace-norms 4 or 10 > 0), so
a rendering FROZEN to any one line has level set {φ^2 = v^2} = S^0 — two
points. This, plus any by-hand freeze of a multi-component space to one
component, is the COMPLETE disconnected inventory of the corpus.
Adjudication, in three exact steps:

1. **Grounding: none, for every frozen-line rendering.** No source sentence
   confines the VEV to any line or single real component; the operative
   hedge is PLURAL (*"the various sub-fields"*, extraction:137-138). The
   only disconnected proxy in the repo, rung-2's {−v, +v}, is a DECLARED
   INPUT (prereg:51-52), not a derivation — the prereg says so itself.
2. **All four ϖ-slot lines reconnect once un-frozen — each by an explicit
   SO+ connector.** The vacuum-set object is the minimum set in the FULL
   space (WB-1's definition), acted on by the declared invariances (gauge
   acts on the ad-legs; form legs untouched). The probe verifies exactly:
   - **base-flip connector** g = diag(−1 on the four base e-legs, single
     internal (+,−) pair flip): component class (det|pos, det|neg) =
     **(+1, +1) on BOTH horns** ((9,5) = (6,4) ⊕ (3,1): det|pos =
     (−1)^3·(−1)^1 = +1, det|neg = (−1)^1·(−1)^1 = +1; (7,7) = (6,4) ⊕
     (1,3): same pattern) — g ∈ SO+, and its Ad-action reverses δ_4 and
     ε_4δ_4 exactly (odd base form degree) while fixing the internal-trace
     lines;
   - **internal-flip connector** g' = diag(1_4, −1_10): class ((−1)^6,
     (−1)^4) = **(+1, +1)** — g' ∈ SO+, and it reverses δ_10 and
     vol_4 ⊗ δ_10 exactly (one internal ad-leg) while fixing the base-type
     lines exactly (its restriction −1_10 has class (+,+) inside O(6,4),
     so it IS an identity-component element on its own).
   Multi-line combinations need no connector at all: on a frozen 2-plane
   spanned by two of the lines the induced invariant quadratic is POSITIVE
   DEFINITE (trace-norms 4 and 10), so the min set of any norm-built
   potential on the plane is an ellipse — connected by the master form's
   p = 2 row (the circle control), and the simultaneous flip is not needed.
   That is fortunate and checked, because the simultaneous flip is NOT
   free: −1_14 computes to class (−,−) on both horns, and the naive
   single connector diag(−1_4, 1_10) computes to (−,−) as well — the probe
   carries BOTH as planted near-misses the classifier must reject. The
   per-line reconnection works only because each line has its OWN
   connector, and the probe checks exactly that assignment (including that
   the base-flip acts NON-scalar on δ_10 — it moves that line within the
   vacuum set rather than fixing it, which is why the internal-flip
   connector exists separately). So for every
   gauge-invariant potential in the stated even class, ±φ on any ϖ-slot
   line lie in the SAME component of the vacuum set: the S^0 is a fact
   about the frozen line, and the freeze is what no grounded typing
   performs. (For the background-broken rendering the connector must sit in
   Stab(F̄_0) — F5's business, computed next; the source's own regime
   clause — mass *"for any fixed background metric for which the scalar
   curvature R(y) is approximately constant in a region under study"*
   (register:1176-1177) — is a maximal-symmetry regime in which a large
   stabilizer is exactly what the source supplies.)
3. **The ε-slot vol_4 line also reconnects; only the Λ^0 trace is
   gauge-INERT — and the inert rendering fails grounding three named
   ways.** The vol_4 line (Λ^4 base legs) transforms by det(g|V_4), and a
   MIXED same-sign-type pair flip — diag(−1 on one base leg, −1 on one
   internal leg of the same metric sign) — has component class (+,+) on
   both horns while acting as det(g|V_4) = −1: an SO+ connector exists (the
   probe verifies it; first drafted as inert, corrected by the probe's own
   enumeration — recorded honestly). The Λ^0 trace direction is different
   in kind: Ad_g(1) = 1 for EVERY gauge element — the center direction is
   Ad-trivial under the entire structure group, so NO connector exists at
   all (the probe verifies the scalar action is +1 for every diagonal sign
   matrix and carries the line as the planted not-reconnected control at
   the connector level). An ε-slot TRACE order parameter with a hat WOULD
   have a disconnected vacuum set {±v}. It is the one disconnected
   rendering the corpus's mathematics allows, and it is un-grounded three
   ways, each pinned: (a) WRONG SLOT — the hedge conditions the decoupling
   on ϖ explicitly (*"pulling the various sub-fields of ϖ"*,
   extraction:137-138), and every printed zero-order mass cell is ϖ-linear
   (SN-1 via st1 §4.4's "all SIX ϖ cells"); (b) SINGULAR — one real
   component (the center!) against the source's plural; (c) WRONG CLASS —
   Λ^0 is a class-0 direction, and *"class-0 insertions (Λ^0, or the
   so(14) adjoint Λ^2): 0 — never unlock"* same-class pairings (st1 §4.3)
   while class-0 content *"can never select a half"* (TR-1) — the trace
   cannot be the chirality-relevant VEV the target's broken region is
   defined by. This rendering is carried into §5's revival fork as its
   cleanest branch, not suppressed.

### 3.4 The honest native fiber (F5): the mu-fiber's ± pair, connected in-model

The stated potential class is V(a) = ||mu(a) + F̄_0/2||^2 − const with
mu(a) = a ∧ a even, so the exact vacuum set (when the completed square
attains zero) is the FIBER mu^(−1)(−F̄_0/2) — not a norm level set — and its
connectivity is not closed-form in general. What IS decisive for W1 is the
±-pair question (the orientation flip is the datum the wall consumes, WB-1
§1), and the minimal nonabelian model decides it exactly. Model: g = su(2),
two form legs; a = (a_1, a_2) ∈ (R^3)^2, mu(a) = a_1 × a_2. The probe
verifies, in exact rational arithmetic (Rodrigues samples with rational
cos/sin, plus the closed-form endpoint):

- **evenness:** mu(−a) = mu(a) — the ± pair always co-inhabits the fiber;
- **orthogonality:** c := mu(a) has c·a_1 = c·a_2 = 0 — the background is
  automatically perpendicular to the plane of a;
- **the connector:** the rotation family R(θ) about the axis c satisfies
  R ∈ SO(3) exactly, **R(θ)c = c** (the connector lies INSIDE Stab(F̄_0) —
  the background-broken rendering keeps it), mu(R a_1, R a_2) = c at every
  sample (equivariance verified directly), and the endpoint R(π) =
  2cc^T/|c|^2 − I sends (a_1, a_2) to exactly **(−a_1, −a_2)**;
- **controls:** the abelian model (mu ≡ 0) has EMPTY fiber over c ≠ 0
  (reported empty, not connected — the machinery distinguishes); the frozen
  1-component model reports its 2 components (the deliberately-disconnected
  control, doubling as §3.3's).

So in the honest rendering's minimal model the orientation pair is connected
THROUGH the vacuum set by a background-stabilizing rotation — the same
mechanism (both inertia directions available to rotate through) that drives
every §3.1 row. The general native fiber inherits this as
MODEL-EXACT-PLUS-TYPED, not closed-form: the residual is named in §5 and
carried into the revival condition, not hidden.

### 3.5 Composed pi_0, per typing

| typing | pi_0 (vacuum set) | ± pair same component? | grade |
|---|---|---|---|
| F1 selective hat, branched (internal 120, (56,64)) | **1** | yes | EXACT closed form |
| F2 blind hat, branched (Λ^1 (6,4); Λ^5 (132,120)) | **1** | yes | EXACT closed form |
| F3 ambient hat (cross-half (1716,1716); tensor (52,48); single-half UNWRITABLE — §2) | **1** where writable | yes | EXACT closed form + exact isotropy |
| F4 orbit typings (incl. VG-V5's D) | **1** | yes | EXACT component groups |
| F5 native mu-fiber | ± pair connected in the minimal nonabelian model, inside Stab(F̄_0) | yes (in-model) | MODEL-EXACT; general fiber typed, residual named |
| F6 frozen 1-dim lines | 2 on any frozen line; **1** for the un-frozen vacuum-set object on every ϖ-slot line (per-line SO+ connectors, both horns); the single gauge-inert ε-slot Λ^0 trace line stays 2 and fails grounding three named ways | yes for every grounded rendering | EXACT component arithmetic; the freeze is ungrounded INPUT shape; the inert-line rendering is the revival fork's cleanest branch (§5) |

**No live typing yields a nontrivial pi_0. No typing yields more than one
component, a fortiori none yields a group larger than Z/2 (§6's fence is
therefore not triggered).**

---

## 4. (c) The two proxies, adjudicated

**Rung-2's {−v, +v} (the disconnected proxy).** It proxies EXACTLY the p = 1
row of the master form — a 1-real-component order parameter with a definite
quadratic — which is F6's frozen-line rendering. That row is real
mathematics (the probe computes its 2 exactly), but it enters rung-2 as a
DECLARED INPUT: *"Double-well potential `V(phi) = lambda (phi^2 - v^2)^2`;
boundary sector `phi(-L) = -v`, `phi(+L) = +v`"* (prereg:51-52) — the
prereg's construction-fork section says in terms: *"The triplet is
**supplied** ... no result here derives three"*, and the disconnection is
supplied the same way, by choosing ONE real component. No source sentence
grounds the choice; the operative hedge is plural; and §3.3 shows the
un-frozen native object reconnects even the lines that could host the
freeze. **Verdict: rung-2's proxy is correct about its own toy and proxies
NO native typing.** Its enduring contributions survive unchanged —
T-DEG (exact degeneracy of ±v, which persists as the evenness of the native
class) and the SECTOR-SUPPLIED typing of the flip as external. What dies is
only the tacit transfer of its INPUT topology to the native object.

**VG-V5's connected D (the connected proxy).** D = {positive lines in
C^(3,2)} ≅ SU(3,2)/S(U(1)×U(2,2)), measured connected: *"every orbit is
open, D is connected (it retracts, below)"* (VG-V5:75). Its two fences,
quoted before consumption: *"(1) D is a candidate coset — the identification
of the [00:46:40] VEV's vacuum manifold with D is unproven, and gated on the
running R-routes"* (VG-V5:239-240) and *"(4) 'positive lines' as the vacuum
manifold is a modeling choice"* (VG-V5:243). Both fences bind the
IDENTIFICATION, not the topology; this artifact does not use the
identification — instead §3.2 proves D's connectivity exactly (pi_0(SU(3,2))
= 1; the positive-cone radial retraction), so D stops being a load-bearing
proxy and becomes a corroborating exemplar of the class every grounded
native typing lands in. **Verdict: VG-V5's proxy is correct about its own
object AND representative of the native answer, but at a different layer
(the J-complexified metric-fiber form space, not the ϖ slot) — its
double-fenced status is unchanged and was never needed for today's kill.**

**Why the proxies disagreed.** They typed DIFFERENT OBJECTS: a frozen
1-real-component section (disconnection by input) versus a multi-dimensional
invariant coset (connection by group action). The native object, typed at
either live layer, has a multi-component order parameter with a connected
invariance structure in every grounded rendering — it sides with the coset
class, and the disagreement dissolves into the typing step exactly as the
register item predicted it would ("requires the native varpi vacuum-set
object typed first", upgrade-program-register:268).

---

## 5. (d) VERDICT — KILLED-AT-W1, at typing grade, with the kill scoped

**Per typing:** connected in F1, F2, F3 (where writable — and the writable
set is itself a finding), F4, F5 (model-exact, connector inside the
background stabilizer), F6-as-vacuum-set. Disconnected ONLY in F6's frozen
line, which no source sentence and no repo construction grounds as the
native object, and which reconnects the moment the freeze is released.

**Composed: KILLED-AT-W1.** WB-1's gate — *"E2 — a disconnected vacuum set:
pi_0(V) != 0 ... **E2 is the gate.**"* (wb1:322-337) — CLOSES AGAINST the
target in every live typing, and WB-1's named branch — *"KILL-1 (the live
one): pi_0(native vacuum set) computed TRIVIAL — a connected vacuum set on
the actual carrier leaves no Z/2 sector, no stable orientation wall"*
(wb1:564-566) — **fires at the grade computed: every-live-typing, exact
where closed-form, model-exact where the native action's absence forbids
closed form.** The killed claim, named verbatim per the kill discipline, is
the REPO CONSTRUCTION TARGET (not any source row):

> *"The join types into a NAMED CONSTRUCTION TARGET: **VEV-orientation
> (Z/2) domain walls inside the broken region, steered by R(y)** —
> existence condition = the unfixable orientation Z/2 (P7), location
> selection = rung-2's N2 gradient with grad R(y) the source-native
> candidate (P6+P1), envelope = the Mexican-hat depth R coaxes (P2+P3) —
> carrying the bill W1-W6"* (rw1:456-462); machine row
> `TYPED-TARGET-VEV-ORIENTATION-WALL-R-STEERED` (rw1:525).

The target's existence condition fails one level below where RW-1 and WB-1
left it: the orientation Z/2 is not merely unselectable (rung-2, T-DEG) and
not merely unrealized-pending-external-supply (WB-1's relocation) — **in
every grounded typing it does not exist as an object.** There is no second
vacuum component to flip into; a phi-zero in the broken region is a
transversal excursion of a connected vacuum set, topologically unprotected,
hosting nothing stable. The external Z/2 sector datum WB-1 typed as the
relocated W1 has NO carrier in the native vacuum topology.

**What would revive the target (the fork that closes the kill, stated so a
future wave can aim at it).** Two branches, each requiring unstated
structure the present material points against:

1. **The inert-line branch (cleanest).** Ground an ε-slot Λ^0 TRACE order
   parameter as the decoupling VEV — the one rendering whose S^0 genuinely
   survives (§3.3 step 3; the center direction is Ad-trivial, so no
   connector exists) — against three pinned guards at once: the hedge's
   explicit ϖ (extraction:137-138), the plural, and the class-0 arithmetic
   (st1 §4.3; TR-1's selection-blindness). A source sentence or constructed
   action putting THE mass-and-selection VEV on the gauge-inert trace would
   re-open E2.
2. **The ϖ-slot branch (two unstated structures at once).** A dynamical
   confinement of the ϖ order parameter to a 1-real-component invariant
   line (or a two-orbit minimum with a discrete gap — nothing grounds one)
   AND the breaking of that line's ±-connector — Stab(F̄_0) and the
   residual gauge action must exclude the per-line SO+ elements and
   rotations §3.3/§3.4 exhibit (the minimal model puts the connector INSIDE
   the stabilizer, and the source's own approximately-constant-R regime
   pushes the stabilizer large).

Until one branch arrives, the target is dead at W1, not OPEN. This refines
— and for this target terminates — the sequence rw1 "OPEN, not forbidden"
(rw1:267-269) → wb1 "RELOCATED-TYPED-EXTERNAL, pi_0 gate OPEN" → **pv1
"KILLED-AT-W1-AT-TYPING-GRADE, revival forks named."**

**What survives, exactly (a kill must not overrun its target):**

- **Every source claim.** SC-CHI-01/02/52/53, SC-GEO-58, SC-MAS-01 — the
  source never asserts a wall, a VEV flip, or a disconnected vacuum set
  (WB-1's certified 0/0/5 VEV-orientation scan). This kill lands on a repo
  construction target only.
- **RW-1's comparator mathematics** (hosting = |winding| exactly; the
  grading measurements; the six-profile table) — model results, untouched.
  RW-1's LEG-2 certified absences gain a companion: chain-1's wall died for
  want of an R-crossing; chain-2's wall now dies for want of a second vacuum
  component.
- **WB-1's relocation theorem** — the crossing datum IS the sector datum;
  even R-steering can never pay it. Composed with today's result the theorem
  sharpens: the sector datum's group is computed TRIVIAL in every live
  typing, so what R-steering provably could not pay turns out not to be
  purchasable at all. WB-1's location-steering results (even modulation
  selects location) survive — they never consumed the Z/2.
- **Rung-2's T-DEG and SECTOR-SUPPLIED** — degeneracy of ±v is real and
  survives as the evenness of the native class; ±v are now degenerate AND
  connected. The instrument is intact; only the input topology's tacit
  transfer dies.
- **Canon's external-by-structure decomposition** — *"chi = interior-even +
  external-topological-index, so any odd count is necessarily external"*
  (CANON.md:139) and the computed flux exemplar *"net chiral index = flux
  number (any integer, odd for odd flux)"* (CANON.md:135) are untouched;
  the wall was a CANDIDATE member of the external family (RW-1's W4, never
  typed in), and it now leaves the candidacy. The family keeps its computed
  exemplar; the decomposition never depended on this wall.
- **Other Z/2 sector data in the corpus** — the Krein-half sector datum
  (adapter p2c vocabulary), the NN lane's temporal frontier, the shard-cycle
  w_1 — different objects on different bases; none is touched.
- **W3's mathematics** (the grading bridge, GB-1's angle-and-arrow) — moot
  for THIS target, standing as mathematics for any other hosting candidate.

**Consequences for the open registers (for the integrator; PV-1 edits
nothing):** W1-PI0-NATIVE-VACUUM is executed with outcome KILL;
RW1-CHANNEL-INHERITANCES' wall-side items and the W1-W6 bill lose their
subject for the VEV-orientation target; SG4 stays the decider; SG4 bit 2
stays OPEN (nothing here selects a phase — the kill is about the topology of
the broken phase's vacuum set, not about which phase is real).

---

## 6. The CC-05 fence on every count statement

Binding, quoted in §0: the partition is *"FORCED and SUBTRACTIVE
(n_g -> n_g - 1, unlabelled, the distinguished family REMOVED)"* — never an
additive production target.

- This artifact derives NO count. pi_0 = 1 everywhere live; had any typing
  produced k components, the hosted count on any realized wall would STILL
  be |winding| × N with **N SUPPLIED** (RW-1 §4g exact; rung-2 P2), and the
  sector datum would contribute PARITY only (WB-1 §6). The task's
  larger-than-Z/2 contingency does not arise: no typing yields more than
  one component.
- The kill REMOVES a candidate hosting mechanism; it does not move the
  generation count, which stays OPEN, owned by SG4, with the 2+1 subtractive
  structure owned by representation content (CC-05) — exactly as before.

---

## 7. Machine-readable verdicts and certificate

<!-- PV1-TABLE-BEGIN -->

| id | question | verdict | evidence_key |
|---|---|---|---|
| TYPE | what is the native varpi vacuum-set object | DECLARED-SLOT-EVEN-CLASS-FORK-TABLE-F1-F6 | typing_pins |
| HAT | is the stylized hat writable on a selective half at the ambient layer | UNWRITABLE-HALVES-TOTALLY-ISOTROPIC-BOTH-HORNS | isotropy_exact |
| PI0 | pi_0 per live typing | TRIVIAL-IN-EVERY-LIVE-TYPING | signatures_components_paths |
| PROXY | the two proxies adjudicated | DIFFERENT-OBJECTS-RUNG2-FROZEN-INPUT-VGV5-COSET-CLASS-NATIVE-SIDES-WITH-COSET | proxy_pins_and_exact_d |
| W1 | the target's existence condition E2 | FAILS-IN-EVERY-LIVE-TYPING | composed_table |
| KILL | did the kill branch fire | KILL1-FIRES-AT-TYPING-GRADE-TARGET-DIES-AT-W1 | kill_pins_and_revival_fork |

<!-- PV1-TABLE-END -->

**Verdict-evidence binding (the probe enforces this; the selftest's contrary
control flips KILL to SECTOR-DATUM-TYPED and must be caught).**

| evidence_key | asserted direction | measured |
|---|---|---|
| typing_pins | TYPE requires the slot pins (st1:208-210), the hedge pins (extraction:137-138; register:922), the potential pins (drafts:146/:149), the homonym pin (packet:70), the register-item pin (upgrade-program-register:263-269) | all byte-pinned with a planted negative |
| isotropy_exact | HAT requires *^2 = +1 and Q(v,v) = 0 on every eigenbasis monomial pair, both horns, with the q-even control showing *^2 = −1 and the (1,1) light-cone instance | 3432/3432 per horn; controls behave (LEG I) |
| signatures_components_paths | PI0 requires the exact signature table ((56,64),(6,4),(132,120),(1716,1716),(52,48)), component-group image/quotient arithmetic, path witnesses, disconnected controls reporting 2 and connected controls reporting 1, the singlet rows [1,0,0,0,1]/[0,1,0,1,0] and the six-line inventory, the per-line SO+ connector classes (+,+) on both horns with the (−,−) near-misses rejected and the inert Λ^0 trace REPORTED not-reconnected while ε-vol_4's mixed-pair connector verifies, and the mu-fiber rational path with endpoint −a inside Stab(c) | all exact (LEGs T, Q, S, G, M) |
| proxy_pins_and_exact_d | PROXY requires the prereg input pins (prereg:51-52), VG-V5's connectivity and fence pins (VG-V5:75, :239-243), and the exact D-connectivity computation | pinned; computed (LEG V) |
| composed_table | W1 requires every row of §3.5 to compute connected (or reconnect) with no live index-1 row | measured (LEGs T, Q, G, M compose) |
| kill_pins_and_revival_fork | KILL requires the target-claim pins (rw1:456-462, :525), the gate pins (wb1:322-337, :564), and the artifact's own revival-condition section present | pinned; enforced (LEG B) |

**Certificate.** `tests/channel-swings/joe_directed_pv1_pi0_trivial_in_every_live_typing_w1_kill_fires.py`

- Live run: all legs green, exit 0 — quote pins with a planted negative
  (LEG P); the isotropy leg with both horns and the q-even and (1,1)
  controls (LEG I); the signature/connectivity leg with mechanical
  component-group arithmetic, symbolic path witnesses, and both-direction
  planted controls (LEGs T, Q); the Spin(4)-singlet leg reproducing TR-1's
  row and extending to the varpi slot (LEG S); the SO+ connector leg with
  the near-miss control (LEG G); the mu-fiber leg in exact rationals
  (LEG M); the exact-D leg (LEG V); the artifact binding with SHA-pinned
  table and claim-ceiling enforcement (LEG B).
- `--selftest`: clean baseline verified FIRST with an independently pinned
  check count, and a red baseline aborts; 9 mutations, every one corrupting
  machinery or a reference (a pin's line number; the subset-parity counter;
  the Hodge-star metric sign; a stuck-at-connected component reporter and a
  stuck-at-disconnected one — each caught by the opposite-direction planted
  control; the SO+ classifier's subspace restriction; the Rodrigues
  generator's antisymmetry; the singlet-multiplicity table; and a
  contrary-control artifact copy claiming SECTOR-DATUM-TYPED), each
  REQUIRED to be caught by
  its targeted check with a genuine [FAIL]; a crash is CRASH-NOT-DETECTION;
  an untargeted catch is INCIDENTAL-NOT-TARGETED; the failing check is
  printed for every mutation; baseline re-verified after; exit 0 on success.
- Read-only; the contrary-control copy lives in a temp directory and is
  removed. Deterministic; sympy + stdlib (Fraction) only — no floats
  anywhere load-bearing.
- What the probe does NOT certify: the unbuilt native action or its true
  vacuum set (the kill is at typing grade and says so); the identification
  of VG-V5's D with any GU object (fenced, unconsumed); anything about the
  true RS Y^14 bundle or the (9,5) carrier's analysis; the Kibble-class
  realization question (WB-1's E3, moot for this target); wall grading (W3,
  inherited untouched).

---

## 8. Typed objects (typed-carrier gate)

```gu-typed-objects
result:         pi_0 of the native varpi vacuum set is trivial in every live
                typing: exact signatures ((56,64), (6,4), (132,120),
                (1716,1716), (52,48)) with both inertia indices >= 2 in every
                level-set row; O(p,q) component-group image/quotient
                arithmetic giving connected level sets for p >= 2 and
                reproducing rung-2's S^0 at p = 1; total isotropy of each
                ambient middle-form half on both horns (the hat unwritable
                on a selective half); the two 1-dim Lorentz-scalar lines of
                the varpi slot enumerated and reconnected by an SO+ gauge
                element of class (+,+) on both horns; the mu-fiber's +-pair
                connected by an exact background-stabilizing rotation in the
                minimal nonabelian model
carrier:        the declared bosonic slot Omega^1(ad P) with
                ad P = End(Delta) = 2(Lambda^0 (+) ... (+) Lambda^6) (+)
                Lambda^7_+ (+) Lambda^7_- (dim 16384), its internal
                so(6,4)-modules at the branched layer (120, Lambda^1,
                Lambda^5, V_10* (x) Lambda^1), and the ambient Lambda^7
                halves on both SIGNATURE-AMBIENT horns; plus the su(2)
                two-leg mu-fiber model and the 1-component frozen-line model
                as fenced comparators
                LAYER=ambient CHIRALITY=S-FULL-DIRAC
pairing:        the ambient exterior-power metric form on Lambda^k (diagonal
                subset-parity signs) and the internal so(6,4) form on the
                branched modules ON=the respective Lambda^k / internal
                module; no Krein form is consumed in any leg (the vacuum-set
                question is bosonic; the fermionic pairing structure is
                ST-1/RSC-1's, cited not recomputed)
real_structure: real forms throughout: so(9,5)/so(7,7) ambient (both horns
                computed where signature enters), so(6,4) internal; the
                complexified-D_7 statements consumed from ST-1/RSC-1 are
                horn-blind by their own declaration
grading:        NOT CONSUMED. The Z/4 class grading, the package grading and
                the wall grading appear only in cited inputs; no pi_0
                computation here depends on any grading choice. The
                chirality homonym (packet:69) is inherited un-adjudicated;
                W3 stays UNTYPED by inheritance
action_owner:   repository-construction (the typings, signatures, component
                arithmetic, isotropy, connectors and models are PV-1's; the
                potential-shape ingredient is source-print SC-GEO-58
                drafts:146/:149; the slot and hedge are source-print
                extraction:137-138 / st1's declared conventions; the frozen
                double-well input is rung-2's declared prereg:51-52)
target:         whether the native varpi vacuum set has more than one
                connected component -- WB-1's E2 gate for RW-1's
                TYPED-TARGET-VEV-ORIENTATION-WALL-R-STEERED
                MAP-TYPE=not-a-map (a topological adjudication of a typed
                object class, not a map between carriers)
```

```gu-typed-objects
result:         Source adjudication feeding the typing: the order parameter
                is the plural varpi sub-field multiplet in the declared
                1-form slot; the only stated potential terms are the quartic
                norm-square of a wedge a and the curvature-set quadratic,
                both factoring through mu(a) = a wedge a and both even in a;
                no source sentence confines the VEV to one real component,
                asserts a second vacuum component, or asserts a flip (WB-1's
                certified 0/0/5 scan, consumed not re-run)
carrier:        the register verbatim/notes fields, the extraction hedge
                passage, and the drafts lines as literal text (byte-pinned
                at cited lines)
                LAYER=source-print CHIRALITY=N/A
pairing:        NONE
# A text parse: no bilinear form is involved in this result and none is
# claimed.  Bare token per the FX-2 gate's parser; gloss lives on these
# comment lines (RW-1's relay convention).
real_structure: N/A
grading:        N/A -- the parsed lines contain the source's own words
                ("VEV", "sub-fields", "Mexican hat", "curvature"); their
                operator-level senses are the registered homonyms, not
                resolved here
action_owner:   repository-construction (the parse and typing are PV-1's;
                the parsed content is source-print)
target:         the grounding column of the fork table in section 1
                MAP-TYPE=not-a-map (an adjudication of stated commitments,
                not a map)
```

---

## 9. Grep-before-novelty record (exact-substring searches, run before any first/never claim)

Searches over the repository (md/py, `_local/` excluded), with material hits
and what each did to this artifact:

- `"pi_0"` / `"pi0"` / `"π₀"` — HITS: the time-orientation result's
  *"chi_fibre == +1 EXHAUSTIVELY over O(3,1) (all four pi_0 components)"*
  and *"an exhaustive pi_0 table to test against"*
  (improvement-register:980-1012) — the component-group-arithmetic theorem
  SHAPE exists in-repo for the time-orientation datum, a different object on
  a different bundle; cited in §0 L4 as the precedent shape. Also
  perspective/BV uses of the token in unrelated senses. NO prior pi_0
  computation of any vacuum set exists; the register item itself says
  "uncomputed".
- `"pseudo-sphere"` — ONE hit
  (selected-k77-two-half-hermitian-witt-rotation-gate:50, the covector-line
  choice `q` living on *"a 13-dimensional pseudo-sphere orbit"*) — the
  pseudo-sphere-orbit vocabulary exists for a DIFFERENT object; consumed as
  vocabulary precedent only.
- `"totally isotropic"` / `"isotropic subspace"` — HITS: AR-4's Witt
  polarization of so(7,7) into isotropic 7-planes (ar4:229-231, :359-365)
  and CB-3/CB-4's isotropic-plane pullback ranks — isotropy machinery on
  DIFFERENT spaces. ZERO hits for isotropy of the Lambda^7_± halves under
  the Lambda^7 metric form or for any "hat unwritable on the selective
  direction" statement — §2 is new as far as exact search shows; zero hits
  recorded as zero hits, not as proof.
- `"vacuum manifold"` — HITS: VG-V5 (consumed, §4), path3-branchB (winding
  = degree to vacuum manifold), VG-V7 — per WB-1 §9's earlier sweep,
  re-confirmed; none computes a native vacuum set.
- `"vacuum set"` — HITS: WB-1 only (the object's definition, consumed).
- `"Witt"` — HITS: DeWitt name-collisions (metric vocabulary) and AR-4's
  polarization; ZERO hits for Witt-extension transitivity used on quadric
  level sets — the use here is standard mathematics, named not re-proved.
- `"connected component"` — HITS across several lanes (tw1, la7, rb4,
  observability files); none concerns any vacuum set of any breaking
  channel.
- `"soldering direction"` — HITS: *"natural diagonal soldering directions
  `delta V_i=gamma_i`"* (selected-k77-gamma-trace-graph-dynamics-gate:35),
  *"four soldering directions"* (lab/sources/README:305) — soldering-
  direction vocabulary exists in the k77 conditional-build lane; the
  ENUMERATION of the varpi slot's Lorentz-scalar content down to exactly two
  1-dim lines (§3.3) has zero exact-substring precedent. Recorded as such.
- `"SECTOR-DATUM-TYPED"` / `"KILLED-AT-W1"` — zero hits outside the wave
  briefs; these verdict tokens are minted by the register item's own
  wording.

---

## 10. Canonical-currency check records (for the integrator; PV-1 edits no sidecar)

`process_gates/canonical_currency_audit.py` run 2026-08-17 from the checkout
(183 dirty (file, correction) pairs repo-wide, 183 unchecked, 18 cleared by
recorded checks, ratchet intact). **Finding: NONE of PV-1's consumed inputs
appears in any CURRENT dirty queue** (checked by exact path against the full
dirty listing). Per the audit's own signature-blindness warning ("zero exact
hits is not evidence of clean"), per-input verdicts are recorded for every
consumed input that predates any canonical correction, queue membership or
not. Sidecar untouched by PV-1.

1. `explorations/big-swing-2026-07-06/VG-V5-breaking-coset-topology.md`
   (2026-07-06; predates all ten corrections; in NO current dirty queue).
   **WB-1 flagged that VG-V5's count-adjacent framing predates CC-05 and was
   never queue-checked; since VG-V5 is consumed here and is NOT in a queue,
   this is the mandated COURTESY VERDICT, labeled as such.**
   - vs CC-05 (subtractive 2+1): the consumed rows are the connectivity
     measurement (:75), the coset presentation (:77), and the two fences
     (:239-243). VG-V5's broader 3-divisibility program is NOT consumed. On
     its own face the file is anti-additive by construction — its grade
     block declares *"{3, 8, 24, chi(K3)=24, Ahat=3, rank_H=4, ind_H=8}
     never assumed, inserted, or divided by"*, and honest gap (3) states
     *"the channel provably cannot produce an odd count on spin X"*. The
     consumed rows carry no count framing at all. **COURTESY VERDICT:
     CLEARED-CONSISTENT for the consumed content** (pointers: VG-V5 grade
     block; :239-243; this artifact §4). The integrator note from WB-1
     stands: the file's unconsumed count-adjacent sections remain
     never-queue-checked (signature blindness).
   - vs CC-06 (chirality VEV-conditional): the consumed rows say nothing
     about chirality mechanisms; the [00:46:40] identification is fenced
     UNPROVEN by the file itself. **CLEARED-CONSISTENT (consumed rows).**
2. `explorations/prereg-rung2-dynamical-wall-and-selectability-test-2026-07-29.md`
   (2026-07-29; predates all ten; in NO current dirty queue).
   - vs CC-05: anti-additive on its face — *"The triplet is **supplied**,
     inherited from Rung 1, and no result here derives three"* (prereg:46-47)
     and the |winding| = 1 null (prereg:109-113). **CLEARED-CONSISTENT**
     (pointer: prereg:46-47, :109-113; concurs with WB-1 §10 item 1).
   - vs CC-06: forecloses no mechanism; toy-fenced with the (9,5) transport
     gap declared (prereg:43-45). **CLEARED-CONSISTENT.**
   - NOTE for the integrator (inherited from WB-1 §10): the prereg's
     `implements:` parent IS in CC-06's dirty queue; PV-1 does not consume
     the parent; no verdict recorded for it here.
3. `explorations/decoupling-constructibility-packet-2026-08-12.md`
   (predates CC-05/06/08/10; formerly dirty under CC-05 and CC-08, CLEARED
   in the sidecar by RW-1's recorded checks, canonical-currency-checks:412,
   :423). PV-1 consumes only the homonym rows (:69, :70). Re-checked
   independently: the rows type vocabulary, produce no count, foreclose no
   mechanism. **CLEARED-CONSISTENT, concurring with the recorded verdicts.**
4. `canon/external-by-structure-synthesis-RESULTS.md` (2026-07-02; predates
   all ten; in NO current dirty queue).
   - vs CC-05: its posture is the origin of the fence — *"any ODD generation
     count is necessarily external"* (:60) with the external integer ANY
     integer, nothing privileging 3. **CLEARED-CONSISTENT** (pointer: :60;
     this artifact §5-§6).
5. UCSD-lineage quotes (drafts:146/:149/:158) — vs **CC-10**: every use
   carries the edited-derivative caveat (§0 frame block); audio confirmation
   remains owed; nothing here treats the transcript as more than
   transcript-verified. **CLEARED-CONSISTENT.**
6. Post-correction or owner inputs, checked current by inspection at read
   time: WB-1 + probe, RW-1, ST-1, RSC-1, TR-1 (2026-08-16/17, postdate the
   corrections they carry; RSC-1's correction to ST-1's consumer phrase is
   honored — the "RS-adjacent" gloss is consumed ONLY in RSC-1's corrected
   RS-DISJOINT form), `lab/methods/gu-base-categories.md` (2026-08-17,
   carrying the corrected LA3 direction, quoted at §0),
   `lab/sources/source-claim-register.yaml` (CC-06 co-owner),
   `lab/process/correction-registry.yaml` (the registry itself),
   `lab/process/upgrade-program-register.yaml`, `CANON.md`,
   `VERIFICATION.md`. No verdicts owed.

---

## 11. Claim ceiling, and what this artifact is not

- **No claim status moves. No canon row moves. No register, ledger, sidecar
  or registry edit.** The kill is delivered as DATA to the integrator: the
  killed item is a repo construction target (rw1:456-462, :525) plus the
  executed register item W1-PI0-NATIVE-VACUUM; the disposition edits
  belong to the register owner and the next mint.
- **The kill is at TYPING grade and says so.** The native action is unbuilt;
  this artifact computes every typing the corpus grounds and names the
  revival forks (§5). It does not claim the unbuilt action's
  vacuum set is computed — it claims no grounded typing of it is
  disconnected, which is what E2 needed and what KILL-1 names.
- **This artifact does not derive 3, does not derive 1, and does not derive
  0 walls as a phase statement** — it removes one hosting candidate's
  existence condition. Hosted count on any OTHER realized carrier stays
  |winding| × N with N SUPPLIED; the CC-05 subtractive fence stands (§6).
- **SG4 stays the decider; bit 2 stays OPEN.** Connectivity of the broken
  phase's vacuum set selects no phase.
- **The comparators bind their models** (routing block); the standard
  topology facts are named as standard; the exact computations are what the
  probe certifies.
- **Relation to the lane:** RW-1's verdicts stand (its target was typed, not
  asserted); WB-1's verdicts stand (its gate is what today's computation
  closes); rung-2's results stand inside their declared inputs. The lane's
  sequence is refined, not contradicted — each artifact left exactly the
  hook the next one executed.

---

## 12. POSTFLIGHT — six lenses, after the work

- **P1 — Did any lens change a verdict?** Yes, two. L2 (layer discipline)
  produced the §2 isotropy finding, which REMOVED a typing that looked live
  (the ambient hat on a selective half) before its pi_0 was ever computed —
  without it the fork table would have carried a phantom row whose "answer"
  was an artifact of a degenerate form. L3 (orbit-vs-frozen-section) turned
  the 1-dim lines from a potential FORKED verdict into a completed kill: the
  S^0 finding survives as line-level fact while the vacuum-set object
  reconnects, and running the connector hunt to completion surfaced BOTH
  the per-line SO+ connectors (with their (−,−) near-misses) AND the single
  genuinely inert line (the Λ^0 trace; the ε-vol_4 line, first drafted as
  inert, was corrected by the connector enumeration itself) — the one
  place the composed verdict could have flipped, resolved by the
  three-way grounding failure §3.3 step 3 pins rather than by looking
  away.
- **P2 — Is any verdict resting on a single receipt?** The F5 (mu-fiber)
  row rests on the minimal model plus the typed evenness structure — carried
  as MODEL-EXACT, never as closed-form, and quarantined inside the revival
  condition. Every other row has at least two independent exact machines
  (signature + component arithmetic + path witness; isotropy has the
  per-monomial sweep + the two-line proof + controls).
- **P3 — What did NOT move?** SG4; SG4 bit 2; the generation count (OPEN);
  every source claim; RW-1's and WB-1's verdict tables; rung-2's null;
  canon's external family and flux exemplar; the NN scope-exit; W3's
  mathematics; VG-V5's fences and its CONSISTENT_UNCOMPUTED index leg.
- **P4 — Where would a hostile re-runner disagree?** (i) "Your fork table
  could miss a typing" — the exhaustiveness argument (§1) reduces to: slots
  declared, potential class even-through-mu, vacuum-set shapes enumerable;
  a genuinely new order-parameter home outside the declared slots would be
  new source data and is exactly revival-condition material. (ii) "The
  branched-layer Lorentz-invariance constraint should not bind a wall
  profile that varies over y" — the constraint types the VEV DIRECTION
  (TR-1's own §4.4 object); the profile's y-dependence rides the modulus,
  and dropping the constraint entirely lands you at the AMBIENT rows, which
  are also connected. (iii) "Stab(F̄_0) could be small enough to disconnect"
  — conceded as the named residual: it is half of the ϖ-slot revival
  branch, measured against in-model (the connector sits inside the
  stabilizer there), and the source's own approximately-constant regime
  points the other way. (iv) The Grassmann/statistics structure of
  the slot is not consumed here — the vacuum set is a bosonic object; a
  reader wanting the fermionic-pairing consequences gets ST-1/RSC-1.
- **P5 — Regression check against the standing corrected facts.** Layer
  discipline held (every typing row carries its layer; the toy rows are
  never credited to L1). LA3's corrected direction quoted and used (§0).
  No KK language; no GUT chain; positivity untouched; CC-05 applied (§6);
  CC-08 posture kept (the 128 appears only through RSC-1's corrected
  blocks); CC-10 caveat on every UCSD-lineage quote; the four chirality
  senses never identified; RSC-1's granularity correction to ST-1 honored.
- **P6 — Instrument honesty.** Every load-bearing number in this artifact is
  computed by the probe in integer/Fraction arithmetic; the two
  deliberately-disconnected controls and the two connected controls behaved
  on the live run; the q-even and (1,1) isotropy controls behaved; the
  (−,−) near-miss was rejected by the SO+ classifier; the selftest's 9
  machinery mutations were each caught by their targeted checks with the
  clean baseline verified first and re-verified after.

---

## The blunt paragraph: which direction the pressure ran

This arc was handed the wave's glory computation with the kill branch
pre-named, and the pressure ran two ways at once: toward firing the kill
fast (the level-set arithmetic all but begs for it — every named slot is
huge and every big quadric is connected), and toward NOT firing it (a
FORKED verdict on the 1-dimensional lines would have been safe, deferential
to rung-2's toy, and wrong). The typing step did the real work and twice
nearly inverted the outcome: the ambient hat on the selective direction —
the most source-flavored typing on the table — turned out not to exist as an
object (the halves are isotropic; the hat written there is the zero
function), and the disconnected renderings in the whole corpus turned out
to be six 1-dimensional invariant lines plus rung-2's declared freeze — of
which five reconnect through their own identity-component gauge connectors
the moment the freeze is released, and the one that stays disconnected —
the Ad-trivial eps-slot Lambda^0 trace, the center direction — fails
grounding on three pinned counts, the first of them being that the source's
own hedge names ϖ and not ε. I looked for the disconnection honestly — the
S^0 rows, the near-miss connectors, the inert lines, the
background-stabilizer worry are all in the file — and no grounded typing
carries it: the orientation Z/2 the wall target needs does not exist as an
object, and the target dies at W1 with two named revival forks that present
material points against. The residual I could not close is stated plainly:
the native action is unbuilt, so this is a kill over every grounded typing
rather than over a constructed object, and the F5 fiber row is model-exact
rather than closed-form. If a future construction lands either revival
fork, this kill un-fires and the sector datum returns exactly where WB-1
typed it; nothing in today's result would need retraction to accommodate
that — which is, I judge, the mark of a kill delivered at its honest
grade.
