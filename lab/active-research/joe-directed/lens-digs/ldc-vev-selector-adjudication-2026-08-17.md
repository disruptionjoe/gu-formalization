---
artifact_type: exploration
status: exploration
doc_type: adjudication
created: 2026-08-17
work_item: LD-C
channel: lens-digs
target_claim: NONE-NOT-A-KILL
title: "LD-C: five VEV/decoupling-selector concerns adjudicated, one card each, no flatten. (6) Stability: the decreased-VEV point's extremum question is NOT nobody's row — SRC-2 computed it (symmetric point never a minimum for any nonzero background curvature, SOURCE_NATIVE_ROUTE, conditional on the SG4-undeclared norm), SRC-3/4 remove boundedness, and four ledger revival triggers already demand stationary vacua/Hessians; the LIVE residue is the UNMADE JOIN (zero cross-references between the SRC arc and bit 2/G6/SC-CHI-01's adherence) plus no trigger naming the chiral point's own Hessian. (10) Backreaction: the one-way selector typing is GRADE-CORRECT at GU-as-stated (C5 predeclares the phase a free modulus because no dynamics is stated) and becomes a gap only at built-action grade, where the printed psi-bar-varpi-psi cells make the varpi EOM fermion-sourced and phase selection a self-consistency problem no row types; one reverse-direction instance is already computed and adverse (BD-2: 126 channel repulsive at MAC grade). (16) Value-vs-mechanism: LIVE but half-implemented already — RA-D4's trigger_conjuncts carry exactly the needed buckets (SOURCE_DECLARED_OPEN vs SOURCE_STATED_CONDITION) and canon prose recognizes the value-commitment ('the chiral point GU's phenomenology commits to'); what is missing is the typed record on the G6 grant itself and the COUPLING RULE, whose absence has already cost three misses: SRC-2/3/4 are adverse-mechanism partials and none pinged SC-CHI-01's adherence. The true state is neither of the item's two options: value SOURCE-ASSERTED-TWICE-HEDGED, mechanism ONE-ROUTE-KILLED (RA-D2) + STEELMAN-ADVERSE + rest MISSING_CONSTRUCTION. (17) 'The value is observed' DISSOLVES as posed — it stacks the PCX-1 identification error, SG-1's Layer-0 homonym (phase-CHIRAL means massless/unbroken, not handedness; the observed world is handed AND massive), and a binning error (the source says 'decreased'/'significantly above zero', small-nonzero, which the binary cannot hold) — and the census refutes its premise: effectively ALL typed surfaces treat the bit as open, which is the fence working, not a layer error. Its residue is real and DIFFERENT: two live surfaces bin the observed epoch OPPOSITELY (ST-1 §6 vs SCUR-1 §1 row 6) and no bin convention is declared. (18) Bit 1 IS the invariance-selection (local fermionic ghost-subtracting invariance, ABSENT/PRESENT, GRADED_IG provenance after C1 kills SUGRA_4D; field-space DEPENDENT). Independence, three senses, all decidable from banked artifacts: coordinate YES (full 2x2, bijective corner map), consequence NO ('neither bit alone forces'), consistency-conditioned NO — the consistent support is 3 corners, not 4, so ABSENT forces MASSIVE and CHIRAL forces PRESENT hence carrier A. The corollary (chiral grant collides with the B-tilt unless the mass map is non-uniform) is ALREADY BANKED in GP vocabulary (escape-corners canon :60; lega1 :257 prices the two conjuncts) but typed NOWHERE on the post-ST-1 bit-2-routing surfaces: granting bit-2-chiral is presented as free when the banked corner map prices it at bit 1 = PRESENT. Parent's 'sharpest = 16, 17' ranking: 16 survives diminished, 17 does not survive as posed; the most consequential pair was 18 + 6."
grade: "SOURCE-AND-SURFACE PHILOLOGY plus one exact derivation from a banked predeclaration (the dependence lemma is computed by parsing leg_a's frozen VERTEX map, not by restating it). Every quoted locus is pinned as an on-disk substring or parsed-field assertion by the probe: 90 checks, exit 0. 12 planted false facts each observed False; one deliberately-wrong verdict (the parent's uncorrected 'nobody's row' reading) planted and caught as absent-from-this-file while the corrected verdict is pinned present; the absence checks (SRC-arc-to-SC-CHI-01 join unmade) carry a planted-positive control proving the detector's power. --selftest verifies the CLEAN BASELINE first, then 8/8 machinery-corruption mutations (parser, path, row-lookup, normalizer, detector, support-builder, ledger-version, planted-fact evaluator) each drive exit 1 via a genuine [FAIL] line, crash-without-[FAIL] counted as CRASH-NOT-DETECTION, the selftest itself exiting 0. NOT: a potential, a vacuum, a spectrum, a carrier decision, a claim-status movement, an edit to any predeclaration, register, ledger, or method file, or any statement about which corner nature occupies."
disposition: FIVE_CARDS_NO_FLATTEN__STABILITY_PREMISE_CORRECTED_JOIN_UNMADE__BACKREACTION_GRADE_SCOPED_GAP_AT_LANE1__VALUE_MECHANISM_TYPING_HALF_IMPLEMENTED_COUPLING_RULE_MISSING__VALUE_IS_OBSERVED_DISSOLVES_WITH_OPPOSITE_BINNING_RESIDUE__BIT1_IS_INVARIANCE_SELECTION_BITS_CONSISTENCY_CORRELATED_TYPING_GAP_ON_ROUTING_SURFACES__PARENT_RANKING_HALF_SURVIVES
canon_verdict_change: none
steering_effect: unchanged
canonical_effect: pending_integration
depends_on:
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - papers/drafts/Transcript into the impossible.md
  - canon/gu-forces-field-space-declaration-RESULTS.md
  - canon/escape-corners-campaign-RESULTS.md
  - tests/gu-forces/leg_a_forcing_enumeration.py
  - tests/escape-corners/lega1_flipped_chiral_adjudication.md
  - lab/process/conditional-physics-ledger-v0.259.json
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/sg4-axis/sg1-c6a-scope-narrowing-2026-08-16.md
  - lab/active-research/joe-directed/indefiniteness-typing/itc-positivity-rows-are-five-not-ten-2026-08-15.md
  - lab/active-research/joe-directed/carrier-notation/cn2-notation-carries-the-answer-2026-08-15.md
  - lab/active-research/joe-directed/parity-crosscheck/pcx1-signature-parity-clause-does-not-fire-2026-08-17.md
  - lab/active-research/joe-directed/source-currency/scur1-source-currency-audit-2026-08-17.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src1-source-steelman-of-the-vev-2026-08-14.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src2-mexican-hat-is-automatic-2026-08-14.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src3-potential-unbounded-below-2026-08-14.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src4-eddy-completion-cannot-rescue-the-potential-2026-08-15.md
  - lab/active-research/joe-directed/majorana-126-neutrino/bd2-126-channel-is-repulsive-2026-08-14.md
  - lab/methods/gu-base-categories.md
scripts:
  - tests/channel-swings/joe_directed_ldc_vev_selector_adjudication.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY` — the findings here are typings,
> philology, and one exact lemma about a banked label map. Comparator vocabulary
> ("Mexican hat", "Majorana", "seesaw", "condensate") appears only inside
> quotations of already-routed artifacts, each of which carries its own notice;
> nothing here advances or retards any physics row. Registration in
> `lab/process/source-native-comparator-routing-registry.json` is the method
> owner's call; the notice and classification string are in the exact form
> `process_gates/source_native_comparator_routing_audit.py` requires.

# LD-C — five selector concerns, one verdict card each


```gu-typed-objects
result: LD-C source-adjudication — five verdict cards (items 6, 10, 16, 17, 18) on the SG4-bit-2 selector, the 16-vs-17 dedupe decision, and the bit-1 consistency correlation
carrier: repository text and structured records (SC-CHI-01 and its register neighbors, the SRC-1..4 arc, canon SG4 surfaces, leg_a_forcing_enumeration.py VERTEX map, v0.259 rows) LAYER=UNTYPED CHIRALITY=N/A
# LAYER is UNTYPED per the LD-D precedent: the closed vocabulary has no token for
# repository text about the repository; the physics layers quoted are typed by
# their owners inline (bit 2 = PHASE at Layer 2; bit 1 = INVARIANCE selection).
# Block added at commit by the session owner as a governance-field compliance
# edit (typed-carrier gate, live scope); content untouched.
pairing: NONE
real_structure: N/A
grading: N/A
action_owner: repository-construction (adjudication cards); source-print for every quoted locus
target: the five concern verdicts and their proposed register items MAP-TYPE=not-a-map
```

## 0. Mandate and contract

LD-C of the four-agent lens-dig wave. Five concerns, each SOURCE-ADJUDICATION-
shaped: what does the source actually commit to about the VEV/decoupling
selector (SG4 bit 2), and what does the repository actually hold. The
no-flatten contract binds: one card per concern, dedupe only by full quotation
with the nuance shown, a fidelity delta per item against the parent's
one-liner. The parent elevated items 16 and 17 as "the sharpest"; that ranking
is treated below as a hypothesis under attack (§12). No git; live siblings
(`vz-repair/`, `ct-hardening/`, `lab/methods/`, other `lens-digs/ld*`)
untouched — one observation about a file the ct-hardening sibling owns is
FLAGGED (§7.4, LA3), not repaired.

## 1. Preflight — six problem-matched lenses, kill-switch recorded first

**Lens 1 — source philology, both directions.** Every claim about source
commitment is checked against the verbatim locus, not a citation of it:
register `SC-CHI-01` (:909–:932), the p.52 hedge in the s11–s12 extraction
(:137), drafts transcript L155/L158. Standing risk both ways: crediting the
source with the mechanism it never supplies, or denying it the value it
twice-hedgedly asserts. *Specific trap pre-named:* L155's "I don't know what
to do" is about the Killing form, not the VEV (§2, D).

**Lens 2 — debt taxonomy.** Item 16 is adjudicated against the ledger's ACTUAL
taxonomy (`verdict_kinds`, v0.259), RA-D4's `trigger_conjuncts` buckets,
LT-SM8's minted kind, and RA-D2's falsification — the question is whether the
wanted distinction is absent, distributed, or already minted, under
`unknown_kind_rule: NEW_KIND_REQUIRED__FORCED_FIT_FORBIDDEN`.

**Lens 3 — dynamical-systems ownership.** Items 6/10 require separating four
objects that the one-liners fuse: stationarity (critical point), stability
(Hessian sign), boundedness (global), and self-consistency (backreaction/gap
equation). Each gets its own owner search; conflating them is exactly how
"nobody's row" gets asserted falsely.

**Lens 4 — layer discipline (PCX-1/SG-1 fences).** Every use of "observed"
below names its layer and bridge status. The homonym SG-1 diagnosed — phase
value spelled `CHIRAL` (massless/unbroken) vs chirality-as-handedness — is
load-bearing for item 17 and is never allowed to do silent work.

**Lens 5 — adversarial reading, both directions.** For each card the strongest
mis-credit reading and the strongest mis-deny reading are constructed before
the verdict; the parent's elevation of 16/17 is attacked, and my own downgrade
of 17 is then itself attacked (§9).

**Lens 6 — instrument integrity (VERIFICATION.md, seven rules).** The probe
pins quotes on disk, derives the dependence lemma by parsing the frozen
predeclaration, and carries planted falses, a wrong-verdict control, a
planted-positive for every absence check, and a baseline-first selftest whose
mutations corrupt machinery, never predicates.

**Cheapest kill-or-switch, recorded before computing.** (i) If
`canon/escape-corners-campaign-RESULTS.md` already types the bit-dependence AS
a statement about the SG4 residual, item 18 collapses to ALREADY-COVERED
entirely. Outcome: it does not — it derives the chain in GP/corner vocabulary
(:60) and never touches the residual's presentation; the residue in §7.5
survives. (ii) If `SC-CHI-01`'s adherence evidence already cites the SRC arc,
item 6's residue dies. Outcome: it cites
`explorations/frontier-design-packets-index-2026-08-11.md`, `NEXT-STEPS.md`,
`lab/process/CURRENT-RESEARCH-CONTEXT.md` — no SRC file; the residue survives.

## 2. The evidence loci, verbatim (each pinned by the probe)

**A. `SC-CHI-01`** (`lab/sources/source-claim-register.yaml:909`, polarity
`ASSERTS`, grade extraction-verified, core hard-core, adherence ADHERED):

> "Thus we assert that a non-chiral total theory splits at the emergent level
> into two separate chiral theories and that the one above the dashed line
> corresponds to matter in our world with the other sectors not labeled by F
> to the left and above the line are currently dark to us."

Register note: "Conditioned in the same passage on no VEV 'pulling the various
sub-fields of varpi to values significantly above zero', and introduced as
'The idea being explored here' - hedged strength."

**B. The p.52 hedge** (`lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md:137`):

> "The idea being explored here is that the full operator depicted decouples
> effectively into two separate Dirac like operators, when there is no vacuum
> expectation value pulling the various sub-fields of ϖ to values
> significantly above zero."

Three separately load-bearing words: "idea being explored" (strength hedge),
"sub-fields" (the VEV is componentwise, not one dial), "significantly above
zero" (the condition is small-VEV, not zero-VEV).

**C. Drafts transcript L158** (`papers/drafts/Transcript into the impossible.md:158`):

> "the fermionic extension gives you exactly three families of chiral fermions
> **if** you have a **decreased VEV** in the total space taking a Dirac
> equation into two [Weyl] equations because **the mass is actually a
> variable**"

**D. Drafts transcript L155** — "and I don't know what to do because we're in
a maximally compact subgroup" — is the author's openness declaration about
**the indefinite Killing form** ("We wasted the seventies work because we
wanted to avoid indefinite signature on the killing form"), i.e.
`SC-META-53`'s family (polarity `UNCERTAIN`, "Well, I don't know"). It is NOT
a VEV-mechanism statement. The parent's evidence line bundles L155's "I don't
know what to do" with the VEV selector; that bundling conflates two distinct
source-openness declarations (fidelity delta, §12).

**E. SG4's two bits** (`canon/gu-forces-field-space-declaration-RESULTS.md:64-78`):
"The completion is not narrowed to a point but to a **2-dimensional
residual** -- neither bit alone forces a unique carrier: **Bit 1 --
invariance-selection:** does SG4 gauge the graded-IG scalar-spinor eps
sub-slot? [this IS the SG4 declaration]. **Bit 2 -- phase:** chiral/unbroken
vs massive/super-Higgs (the 'mass is a variable' modulus)." Four corners "map
**bijectively**" onto {A(−42), B(−38), −40, live-inconsistency}.

**F. The frozen corner map** (`tests/gu-forces/leg_a_forcing_enumeration.py:56-74`,
SHA-pinned by SG-1): `INVARIANCE = {"ABSENT", "PRESENT"}` ("local fermionic
(ghost-subtracting) invariance"); `PHASE = {"MASSIVE", "CHIRAL"}` ("broken
massive point / unbroken chiral (decreased-VEV) point"); field-space
"DEPENDENT: it is fixed once (invariance, phase) are fixed"; and

```python
VERTEX = {
    ("ABSENT",  "MASSIVE"): {"carrier": "B", ...  "index": -38},
    ("PRESENT", "CHIRAL"):  {"carrier": "A", ...  "index": -42},
    ("PRESENT", "MASSIVE"): {"carrier": "CTRL40", "index": -40},
    ("ABSENT",  "CHIRAL"):  {"carrier": "INCONSISTENT", ...},
}
```

C5's own source string: "two VACUUM PHASES of one stated modulus" **"(A at the
chiral point, B at the massive point)"**; C5's note: "Declares the PHASE axis
a free MODULUS", "explicitly REFUSES to pick a phase".

**G. The SRC arc** (all `SOURCE_NATIVE_ROUTE`, all
`canonical_effect: pending_integration`): SRC-1 — the source's mechanism is a
curvature-induced Mexican hat in the ad-valued one-form; no Higgs. SRC-2 —
"the symmetric point is never stable" for ANY nonzero background curvature
(exact, 21/21). SRC-3 — the potential is "UNBOUNDED BELOW", "CONDITIONAL on
the load-bearing assumption that the norm-square uses the Ad-invariant
pairing... **SG4 leaves the actual quadratic form undeclared**" (16/16).
SRC-4 — the eddy completion cannot rescue; boundedness reduces to the sign of
one undeclared composite coefficient (44/44).

**H. Ledger v0.259 rows:** RA-D2 ("the stated VEV/mass mechanism produces
low-energy chirality") = `OVER_DETERMINED / GENUINE_FALSIFICATION`, revival
"an exact chiral physical carrier not obtained by equivariant mass splitting".
RA-D4 `trigger_conjuncts`: "a positive BRST cohomology" → bucket
`SOURCE_DECLARED_OPEN` (SC-META-53); "chiral light spectrum" → bucket
`SOURCE_STATED_CONDITION` (":158"). LT-SM8 =
`SOURCE_DECLARED_OPEN__NO_MECHANISM_SUPPLIED` (IT-C's minted kind). Stationarity
demands live in triggers: RA-A8 "a stationary vacuum with computed
identity-component stabilizer", RA-E6 "an exact vacuum Hessian and
one-dimensional unbroken U(1)_em", RA-G3 "a stationary odd-form VEV in the
located 126 channel", RA-A1 "an exact stabilizer theorem for the built
source-action vacuum". The G6-blocked rows (LT-GR2d, RA-G3, AC-F1) are all
`MISSING_CONSTRUCTION`; no bit-2-adjacent row carries `ONE_BIT`.

**I. The GP-corner chain, banked 2026-07-10**
(`canon/escape-corners-campaign-RESULTS.md:59-64`): "'Too massive' and
'decreased VEV' are opposing demands on one dial, reconcilable only by an
unstated hierarchy. At the chiral point GU's phenomenology commits to, the
spin-3/2's massless surface is codim-1 ..., GP's hypotheses populate, and its
conclusion ... collides with 'We will never find space time Susie' — UNLESS
the demanded SUSY is the upstairs one. Which is corner (b)." Priced at
`tests/escape-corners/lega1_flipped_chiral_adjudication.md:257-258`: "corner
(a) fires only if GU's physical vacuum is taken at the chiral point AND the
mass map is uniform across the fermionic extension".

**J. The routing/fence artifacts:** ST-1 §5 V1 (bit 2 = "Whether the insertion
IS on", layer "Observed, VEV-conditional", "Owned by Lane 1", "Not decided
here, by design"); SG-1 ("Bit 2 is a PHASE bit"; the Layer-0 homonym — "the
phase axis carrying a value literally spelled CHIRAL meaning
massless/unbroken"); CN-2 §4.4 (mapping the content fork onto bit 2 is "an
identification CR-B makes, not one this enumeration licenses"); PCX-1 ("the
layers do not meet", identifications need constructed receipts); SCUR-1 §1
row 6 ("Observed chirality VEV-conditional", "SC-CHI-01 = SG4 bit 2") and its
D2 correction ("the m→0 branch IS the source's claimed phase", selector "OPEN
by design").

---

## 3. CARD — item 6, stability

**One-liner under adjudication:** "is the decreased-VEV phase an *extremum* of
anything? No owned potential; the phase's dynamical stability is nobody's row."

**(i) Strongest reconstruction.** SC-CHI-01's split is conditioned on a phase
(ϖ sub-fields near zero). A phase that is merely *stipulated* is not physics:
if no potential owns ϖ, nothing says the decreased-VEV point is stationary,
stable, or reachable, and every chirality claim conditioned on it inherits an
unexamined dynamical debt. If nobody owns that question, the debt is invisible.

**(ii) What the repo holds.** More than the one-liner allows. (a) A steelmanned
potential EXISTS and is owned: SRC-1 extracts the source's own mechanism (the
quartic and quadratic both from ‖F‖², "if your curvature is negative, now you
start to get a Mexican hat potential"); SRC-2/3/4 compute on it at exact grade,
classification `SOURCE_NATIVE_ROUTE` — not comparator-scoped. (b) The
decreased-VEV point's stability is COMPUTED there: it is a critical point, and
SRC-2's exact tracelessness result makes it "never stable" for any nonzero
background curvature — no sign condition needed, which is *stronger* than the
source's own "if". SRC-3/4 remove boundedness (two independent causes;
completion sign-inert or worse), all CONDITIONAL on the SG4-undeclared
quadratic form. (c) Stationarity/stability demands already exist in the
ledger's revival-trigger family: RA-A1, RA-A8, RA-E6 ("an exact vacuum
Hessian"), RA-G3 ("a stationary odd-form VEV") — evidence locus H.

**(iii) The dig (ownership archaeology).** Who owns the JOIN between (b) and
the selector? Nobody. Probe-verified: the strings `SC-CHI-01`, `bit 2`,
`bit-2` occur ZERO times in SRC-2 and SRC-3 (SRC-3 routes forward only to
"SG4/Lane-1 property"); SC-CHI-01's adherence evidence cites no SRC file; no
ledger row and no trigger names the decreased-VEV point's own stationarity or
Hessian sign — RA-A8/E6/G3's demands are aimed at *other* completions (the
A1-lock, U(1)_em, the 126 channel). The K106–K108 positivity fence is the
wrong territory: those gates own state-space definiteness (quotient pairing
positivity), not vacuum stability — the Hessian of a potential and the
signature of a state-space form are different objects (Lens 3), and IT-C's
class-P census confirms the positivity rows never carry a vacuum-stationarity
demand. So the question is not unowned; the ANSWER is unrouted: the repo's
strongest source-native dynamics says the phase SC-CHI-01 needs is a hilltop
of an unbounded-below potential (conditional on the undeclared norm), and that
adverse partial sits `pending_integration` with no edge to bit 2, G6, or the
SC-CHI-01 hedge-watch.

**(iv) VERDICT: LIVE-MODERATE — premise corrected, residue live.** The
question "is the decreased-VEV phase an extremum of anything?" is answered
in-repo at steelman grade (critical point: yes; minimum: never, for F₀ ≠ 0;
bounded: no; all conditional on the SG4 norm). "No owned potential" and
"nobody's row" are both false as stated. What is live: the unmade join, and
the absent chiral-point row. The sharpest true sentence: **the phase's
stability is a function of SG4's own undeclared quadratic form — the
stability question routes INTO the selector it was supposed to ground.**

**(v) Proposed register item (LIVE).** One annotation, two placements: on
`SC-CHI-01`'s adherence (adverse-direction partial: SRC-2/3/4, source-native,
conditional-on-norm, hedge-watch armed) and on the vacuum trigger family (add
"stationarity and Hessian sign of the decreased-VEV point under the declared
SG4 norm" as a named condition, alongside RA-A8/RA-E6's existing demands).

**(vi) Fidelity delta vs parent.** Parent's "no owned potential; nobody's row"
is refuted in both halves; parent's evidence gloss "src1/src2 … comparator-
scoped" is wrong (both are classified `SOURCE_NATIVE_ROUTE`, and that matters:
comparator-scoped results could not bear on this card at all). What survives
of the parent's instinct is the unrouted-adverse-partial finding, which is
smaller and sharper than the one-liner.

---

## 4. CARD — item 10, backreaction

**One-liner under adjudication:** "if mass is a variable, the fermion content
can select the VEV, not just vice versa. We've typed a one-way selector; the
source's wording permits a self-consistency loop."

**(i) Strongest reconstruction.** Eq (9.16)'s printed zero-order cells are
ϖ-linear fermion bilinears — schematically ψ̄ϖψ. In ANY action containing
those cells plus ϖ kinetics, varying ϖ makes its equation of motion
fermion-sourced: the condensate feeds the VEV that feeds the masses. A
selector that is also sourced is a fixed point, not a dial; "bit 2 selects"
would then be backwards-compatible prose for "bit 2 is solved."

**(ii) What the repo holds.** One-way language on every routing surface: ST-1
V1 "Whether the insertion IS on"; C5 "Declares the PHASE axis a free MODULUS
... explicitly REFUSES to pick a phase"; SCUR-1 row 6 "Observed chirality
VEV-conditional". But the reverse direction is not virgin territory: BD-1
proposed a condensate gate typed "an untyped dynamical question" (still open
per the bd2-su32 collision note: "not executed here, not discharged, and
still open"); BD-2 computed the one instance that exists — the 126 channel is
REPULSIVE at one-boson-exchange (MAC) grade, "the 126 cannot condense at any
coupling" — an *adverse* reverse-direction data point, comparator-adjacent;
and LT-SM8's distance already carries bosonic self-sourcing ("sources the
connection by [R,D_A R]").

**(iii) The dig.** Is the one-way typing a commitment or an artifact? Neither:
it is GRADE-CORRECT. The enumerations measured what GU-AS-STATED forces, and
GU-as-stated supplies no ϖ dynamics — with no potential and no EOM stated, no
loop is derivable, so predeclaring the phase a free modulus (C5) was the
honest coding, and "the source's wording permits a self-consistency loop"
mis-locates the permission: L158's sentence is itself one-directional
(decreased VEV ⟹ two Weyl equations); what permits the loop is the PRINTED
COUPLING plus variational logic, which only exists at built-action grade. The
gap is therefore real but lives at exactly one address: Lane 1. No Lane-1
build obligation, row, or trigger types "phase selection at built-action
grade is a self-consistency (gap-equation) problem, not an input dial" —
while the same ledger's triggers DO demand stationary vacua (H), i.e. bosonic
stationarity without the fermion-source term. Joined with card 6: the SRC
potential is purely bosonic; a fermion-sourced ϖ EOM is a second, independent
channel by which the "free modulus" posture fails at built-action grade.

**(iv) VERDICT: LIVE-MODERATE.** The concern survives with its address
corrected: not "we've typed a one-way selector" (the typing is grade-correct
where it was made) but "no surface types the grade-boundary — C5's free
modulus silently extends from GU-as-stated grade to built-action grade, where
it is false whenever the printed cells enter the action."

**(v) Proposed register item (LIVE).** A typed condition on the Lane-1/SG4
build obligations: "any action containing the printed ϖ cells has a
fermion-sourced ϖ EOM; the phase is then a self-consistency solution, and
C5's free-modulus posture is scoped to GU-as-stated grade. One computed
reverse-direction instance: BD-2 (126 channel repulsive at MAC grade,
comparator-adjacent); condensation criterion untyped (BD-1)."

**(vi) Fidelity delta vs parent.** "We've typed a one-way selector" — true of
surface language, but the typing is correct at its grade; the defect is the
untyped grade boundary. "The source's wording permits a self-consistency
loop" — the loop's permission comes from the printed coupling, not the
wording; and the parent missed that the reverse direction has one computed,
adverse instance (BD-2) plus an open typed gate (BD-1).

---

## 5. CARD — item 16, value vs. mechanism conflation

**One-liner under adjudication:** "`SC-CHI-01` *asserts* the split. The source
may have already committed the **value**; what's open is the **mechanism**. We
routinely phrase bit 2 as 'unknown,' but *value-known-mechanism-open* and
*both-open* are different debts with different revival triggers, and we've
never typed which one we hold."

**(i) Strongest reconstruction.** Debts with different revival shapes must be
typed apart or news mis-routes: a value-debt revives on observational or
identification news; a mechanism-debt revives on construction news. Holding a
value-committed debt while phrasing it as both-open makes adverse-mechanism
news read as "progress on an open bit" when it should read as "the source's
asserted value is failing to be dynamically realized" — an adherence event.

**(ii) What the repo holds.** The source side is register-typed with exactly
the needed resolution: SC-CHI-01 polarity `ASSERTS` with the two hedges noted
(A), against SC-META-53's `UNCERTAIN` (D) — the ASSERTS-vs-UNCERTAIN axis IS
a value-commitment axis. The ledger side half-implements the distinction:
RA-D4's `trigger_conjuncts` carry buckets `SOURCE_DECLARED_OPEN` vs
`SOURCE_STATED_CONDITION` — IT-C executed precisely the "different debts in
one trigger" split, one row away from bit 2. Canon prose recognizes the
value-commitment: "At the chiral point GU's phenomenology commits to" (I).
SCUR-1's D2 correction states the whole structure informally: "the m→0 branch
IS the source's claimed phase", selector "OPEN by design". And the ledger's
BEHAVIOR already answers the item's question: every bit-2-blocked row is
`MISSING_CONSTRUCTION`, no bit-2-adjacent row is `ONE_BIT`, every
vacuum-family revival trigger is construction-shaped — the de facto debt is
mechanism-shaped, i.e. value-committed.

**(iii) The dig (against LT-SM8 as coverage).** IT-C's minted kind
`SOURCE_DECLARED_OPEN__NO_MECHANISM_SUPPLIED` does NOT cover this: its
defining condition is a head demand "the source names as an open problem"
with polarity `UNCERTAIN` or a to-be-figured-out clause — the SC-META-53
shape, both-open. Bit 2's shape is the complement: polarity `ASSERTS`
(hedged) on the value, mechanism unsupplied — and the mechanism side is not
even "open" simpliciter: one formalization route is KILLED (RA-D2,
`GENUINE_FALSIFICATION`, with SCUR-1's attribution correction: D2's model is
mirror-lifting, "the m→0 branch IS the source's claimed phase"), the
steelmanned dynamics is ADVERSE (SRC-2/3/4, conditional-on-norm), the rest is
`MISSING_CONSTRUCTION`. So the item's own dichotomy is too coarse: the held
debt is **value: SOURCE-ASSERTED-TWICE-HEDGED (+ epoch-indexed by "currently
dark to us"); mechanism: ONE-ROUTE-KILLED + STEELMAN-ADVERSE + REST-UNBUILT.**
What is genuinely absent is (a) this record AT the grant locus (G6 carries
demand-side consumers only), and (b) the COUPLING RULE between the fields:
"mechanism news resolving against the value fires the value's hedge-watch."
The rule's absence has already cost three misses — SRC-2, SRC-3, SRC-4 are
adverse-mechanism partials and none produced an SC-CHI-01 adherence
annotation (probe-verified absence, planted-positive controlled).

**(iv) VERDICT: LIVE-MODERATE.** Live: the typed record and the coupling rule
are missing, and the miss has three dated instances. Moderate, not high: the
distinction is already register-carried on the source side (polarity +
hedge-note), bucket-carried one row away (RA-D4), and behaviorally implied by
the trigger family — what remains is a mint-and-join, not a discovery.

**(v) Proposed register item (LIVE).** On G6 (equivalently a register note on
SC-CHI-01): `value_status: SOURCE_ASSERTED_HEDGED (p.52: "idea being
explored" + the ϖ-if; epoch-indexed)`, `mechanism_status: ROUTE_KILLED
(RA-D2) + STEELMAN_ADVERSE (SRC-2/3/4, conditional on undeclared norm) +
MISSING_CONSTRUCTION (rest)`, `coupling_rule: adverse mechanism resolution
fires the hedge-watch → adherence regression, not progress`. No new taxonomy
kind is needed (`FORCED_FIT_FORBIDDEN` not triggered — the fields compose
existing kinds).

**(vi) Fidelity delta vs parent.** "We've never typed which one we hold" —
true at single-locus grade, false at distributed grade: the ASSERTS/UNCERTAIN
polarity axis, RA-D4's buckets, and the all-construction trigger family
jointly hold the answer; and the item's dichotomy misses that the mechanism
side is deader than "open" (killed route + adverse steelman). The
different-revival-triggers stake is the part of the one-liner that fully
survives, sharpened into the coupling rule.

---

## 6. CARD — item 17, "the value is observed"

**One-liner under adjudication:** "our universe *is* chiral. Bit 2's value at
our epoch is **data**, not theory. What's genuinely open is only whether GU's
action *realizes* the observed value. Half our prose treats the value as
undecided; that's a layer error of our own."

**(i) Strongest reconstruction.** Handedness chirality is among the
best-established facts in physics. If bit 2's CHIRAL value is the phase in
which the source's split obtains, and our matter is the split's above-line
block, then at our epoch the bit is pinned by observation, and prose keeping
it "undecided" confuses the theory-side question (does GU realize it?) with
the value question (what is it?), understating what is known.

**(ii) What the repo holds (the census).** Every typed surface treats the
bit's VALUE as open: canon E ("2-dimensional residual"), C5 ("free MODULUS
... REFUSES to pick"), ST-1 V1 ("Not decided here, by design"), SG-1 ("SG4 is
by definition the UNWRITTEN declaration"), G6 (demand-side grant), README
("selects which emergent split is dynamically realized"). Simultaneously two
surfaces carry an observational ANCHOR without typing it: the layer NAME
itself (L4 "VEV-observed"; ST-1's "Observed, VEV-conditional") and SCUR-1's
"the m→0 branch IS the source's claimed phase". So the item's premise "half
our prose treats the value as undecided" is quantitatively wrong: effectively
ALL of it does — and one surface (SCUR-1) states the source-side
value-commitment while keeping the selector open.

**(iii) The dig, including the parent's planted hostile question.** The
hostile question — is "the value is observed" itself a layer error? —
resolves YES, three ways, each with a banked fence:

1. **Identification (the PCX-1 lesson).** "Our universe is chiral" is a datum
   about the observed layer. Reading it as THIS selector's value requires
   identifying our sector with the above-line block of the split. That
   identification is *asserted by the source* (A: "corresponds to matter in
   our world") — it is an adjudicand of this program, not a constructed
   receipt; PCX-1's standard ("the layers do not meet" without a receipt)
   refuses exactly this transport. Using the source's assertion to read the
   bit off observation assumes the thing under adjudication.
2. **Homonym (the SG-1 lesson).** Bit 2's value spelled `CHIRAL` means
   massless/unbroken — the decreased-VEV point (F). Observed handedness is
   not that: the observed world is handed AND massive. Deriving
   "bit 2 = CHIRAL" from "the universe is chiral" is the Layer-0 homonym
   SG-1 diagnosed, doing load-bearing work.
3. **Binning (new here).** The source's own words place our epoch at
   SMALL-NONZERO ϖ: "decreased VEV" (C), "significantly above zero" (B) —
   a regime the binary {CHIRAL, MASSIVE} cannot hold without a declared
   convention. The repo's two live surfaces bin it OPPOSITELY: ST-1 §6
   Channel 1 puts "observed massive states" in the phase WITH spectrum ("the
   phase with spectrum is the same phase the observed-layer quote already
   conditions on") — the VEV-on side; SCUR-1's D2 note puts observed
   chirality on "the m→0 branch". Both cite the source; both are right about
   the small-VEV regime; the BIT is too coarse for either to be its value.

Against my own downgrade (Lens 5, reverse attack): could "undecided" prose
still be an error because the source HAS committed the value? No — that is
card 16's territory (source-side commitment, hedged), and the repo surfaces
that keep the SELECTOR open while quoting the source's claim (SCUR-1) are
doing precisely the right two-register bookkeeping. The error the item
alleges would only exist if a surface DENIED the source's assertion; none
does (probe-pinned: SC-CHI-01 adherence is ADHERED, the assertion is quoted
on the frontier surfaces).

**(iv) VERDICT: DISSOLVES as posed — with a LIVE-MODERATE residue that is a
different claim.** As posed, the item stacks the identification error, the
homonym, and the binning error, and its premise fails the census. The residue
banked from the dig: the OPPOSITE-BINNING instance (ST-1 §6 vs SCUR-1 row 6)
plus the undeclared bin convention. That residue does not vindicate the item:
it says the binary cannot carry "the observed value" at all, which is the
opposite of "the value is observed."

**(v) Proposed register item (for the residue).** Declare the bit-2 binning
convention at the observed epoch — either "CHIRAL bin includes the
decreased/small-nonzero regime (per F's 'decreased-VEV point' comment), and
the observed epoch's bin assignment is CONDITIONAL on the unconstructed
identification (PCX-1 discipline)" or "the binary is inadmissible as an
observed-value carrier; observed-epoch statements must use the continuous
modulus" — and reconcile ST-1 §6 Channel 1 with SCUR-1 §1 row 6 under
whichever convention is chosen.

**(vi) Fidelity delta vs parent.** The parent's one-liner has the error
direction inverted: the repo's undecided-prose is its fences working (SG-1,
PCX-1), and the item's own move commits the homonym those fences exist to
stop. "Half our prose" is quantitatively wrong (≈ all, and correctly so).
What the parent's instinct detected — something IS untyped about the
observed epoch — is real, but its correct name is the missing bin
convention, not a repo-side layer error.

---

## 7. CARD — item 18, bit 1

**One-liner under adjudication:** "SG4 is a *two*-bit result. Everything
routes to bit 2 alone; whether the bits are independent has never been
checked."

### 7.1 What bit 1 actually is (the parent's admitted gap, closed)

Bit 1 is the **invariance-selection**: "does SG4 gauge the graded-IG
scalar-spinor eps sub-slot? [this IS the SG4 declaration]" (E) — coded as
`INVARIANCE = {"ABSENT", "PRESENT"}`, "local fermionic (ghost-subtracting)
invariance", with provenance `{"SUGRA_4D", "GRADED_IG"}` relevant only when
PRESENT, and SUGRA_4D amputated by C1 ("we will never find spacetime
supersymmetry") so the live PRESENT-provenance is the upstairs graded-IG
door. Field-space (`CONSTRAINED`/`FULL`/`BARE`) is explicitly DEPENDENT:
"fixed once (invariance, phase) are fixed" (F). So the residual's whole
content is the pair, and bit 1 is the declaration bit itself.

### 7.2 Independence — three senses, all now decided from banked artifacts

- **Coordinate independence: YES.** The 2×2 square is fully realized — the
  four corners map bijectively onto {B(−38), A(−42), −40, INCONSISTENT} (E,
  F). No corner is excluded from the map; neither bit's VALUE SET depends on
  the other.
- **Consequence independence: NO — and canon says so verbatim:** "neither bit
  alone forces a unique carrier" (E). The carrier is a joint function.
- **Consistency-conditioned independence: NO — computed, not restated.** The
  probe parses the frozen `VERTEX` map and computes: the consistent support
  (corners with a live carrier) is {(ABSENT,MASSIVE)→B, (PRESENT,CHIRAL)→A,
  (PRESENT,MASSIVE)→−40} — three corners, not four, hence NOT a product set.
  Given consistency: **ABSENT ⟹ MASSIVE**, and **CHIRAL ⟹ PRESENT ⟹ carrier
  A**. The correlator is the GP/VZ mechanism at the (ABSENT, CHIRAL) corner
  ("ungauged massless CHARGED spin-3/2 -> GP bites").

### 7.3 The corollary, and where it already lives

Joined with ST-1's binding (the SC-CHI-01 hedge = bit 2) and a UNIFORM
reading of the modulus: granting the source's asserted phase (chiral point)
plus consistency forces bit 1 = PRESENT — the graded-IG door — which is
carrier A's corner, while the banked tilt is B-leaning ("~6 commitments,
ZERO counter-tilts") and **B sits at the massive point** (C5's own source
string: "A at the chiral point, B at the massive point"). This
corner-collision is NOT new: canon banked it 2026-07-10 in GP vocabulary —
"At the chiral point GU's phenomenology commits to, ... GP's hypotheses
populate, ... UNLESS the demanded SUSY is the upstairs one. Which is corner
(b)" (I) — and lega1 priced its firing conditions: "only if GU's physical
vacuum is taken at the chiral point AND the mass map is uniform across the
fermionic extension" (I). The second conjunct is exactly the escape the
source's own "sub-fields" wording (B) and SN-1's separately-scaled
components keep open.

### 7.4 What is genuinely unchecked (the residue)

The dependence is typed NOWHERE on the surfaces built since ST-1 bound the
hedge to bit 2. Canon E presents the residual as a free 2-bit square; G6
presents the chiral grant with demand-side consumers only; ST-1's V1 hands
bit 2 to Lane 1 with no note that its CHIRAL resolution constrains bit 1;
"everything routes to bit 2 alone" is thereby half-true with an unprinted
price: **a chiral grant is also an invariance grant, on pain of GP, unless
the mass map is non-uniform.** (CN-2 §4.4 already fenced one over-routing
onto bit 2 — the carrier fork, "an identification CR-B makes" — which shows
the routing habit and its cost are a known pattern.) One adjacent
observation, flagged for the ct-hardening owner and not repaired here: the
LA3 cell of `lab/methods/gu-base-categories.md` reads "exists only under the
`SC-CHI-01` hedge / SG4 bit 2 grant ... in the `varpi -> 0` phase the arrow
is absent" — the hedge's decoupling holds AT ϖ→0, so the cell's two halves
point opposite ways as written; CT-1's §LA3 quotient-token finding is
adjacent but does not name this direction flip.

**(iv) VERDICT: ALREADY-COVERED-IN-SUBSTANCE (canon escape-corners :59-64 in
GP vocabulary; lega1 :257 prices the two conjuncts) — with a LIVE-MODERATE
typing residue on the post-ST-1 routing surfaces.** The parent's "never been
checked" is right about the surfaces and wrong about the substance: the
material was banked five weeks ago; the unmade part is the join to ST-1's
hedge-binding and the typed statement on the residual/grant surfaces.

**(v) Proposed register item (for the residue).** A dependence note on canon
E's residual and on G6: "the 2-bit square's consistent support is three
corners; given consistency, ABSENT ⟹ MASSIVE and CHIRAL ⟹ PRESENT (⟹ A
corner). A bit-2-CHIRAL grant therefore carries a bit-1 price unless the
mass map is non-uniform across the fermionic extension — lega1's two
conjuncts; the non-uniform escape is the source's own 'sub-fields' wording
and SN-1's separately-scaled components."

**(vi) Fidelity delta vs parent.** Bit 1 named (the parent admitted never
checking); "never been checked" corrected to "checked-in-substance in 2026-07
canon, never typed onto the 2026-08 routing surfaces"; and the parent's
implicit frame "independence is one question" split into three senses with
three different answers.

---

## 8. The 16-vs-17 dedupe, answered with the nuance explicit

**NOT duplicates — different objects, different verdicts, different repairs.**

- Item 16's object is the DEBT the repo holds against the source: how the
  grant's value-status and mechanism-status are typed, and how news routes
  between them. Its verdict is LIVE-MODERATE; its repair is a typed record
  plus a coupling rule.
- Item 17's object is the EVIDENCE for the value at our epoch: whether
  observation, rather than the source's assertion, pins bit 2. Its verdict is
  DISSOLVES-as-posed; its residue's repair is a binning convention plus one
  reconciliation.

The full quotations (§5, §6 one-liners) share only the clause "the value" —
but 16 grounds the value in the SOURCE's hedged assertion (correct, card 16
banks it) while 17 grounds it in OBSERVATION (incorrect as posed, three
fences). A repo that fixed 16 completely (minted the record, added the
coupling rule) would leave 17's residue untouched (the bin convention), and
vice versa. The surviving nuance that keeps both cards: **value-committed-BY-
SOURCE and value-readable-FROM-DATA are different epistemic states, and this
repository is currently in the first, not the second.** The parent's pairing
of 16/17 as one "sharpest" cluster was itself a mild flatten of exactly this
distinction.

## 9. Hostile review, inline

**Strongest mis-credit reading available (and refused).** "The source asserts
the split, our world is chiral, therefore the source's value is confirmed and
only the mechanism is owed." Refused at three joints: the assertion is
twice-hedged (B); the observed world does not sit at the unbroken point
(homonym, §6); and RA-D2 plus the SRC arc mean the mechanism side currently
points AGAINST dynamical realization, which under card 16's coupling rule is
hedge-watch material, not background.

**Strongest mis-deny reading available (and refused).** "The hedges mean the
source commits to nothing; bit 2 is fully open and SC-CHI-01 is decorative."
Refused: polarity ASSERTS is register-typed; the epoch-indexed clause
("currently dark to us") and canon's own "the chiral point GU's phenomenology
commits to" (I) show both source and canon treat the chiral point as the
phenomenological anchor; and the entire dark-sector/partner-placement
obligation structure (SCUR-1 register 8) exists only because the source's
value-commitment is taken seriously.

**Am I straining against the parent's ranking?** The downgrades are
evidence-forced, not posture: 17's dissolution rests on three banked fences
plus a census the probe pins; 6's premise-correction rests on four SRC files
on disk. Where the parent was right, it is said plainly: 16's
revival-trigger stake survives as the coupling rule; 18's "never checked" is
right about every post-ST-1 surface.

**Weakest seam of this file.** The card-18 corollary uses ST-1's binding
(hedge = bit 2) as if it were a licensed identification; CN-2's §4.4 warns
that mappings onto bit 2 are steps, not termini. The card survives the seam
because its verdict does not depend on the binding: the dependence lemma is
about the VERTEX map alone, and the corollary is quoted from canon's own
GP-chain (I), which predates and does not use ST-1. The binding only sharpens
WHO pays the price (the G6 grant).

**Second seam.** The census (item 17) samples the surfaces that carry "bit 2"
tokens plus the canon/methods/routing files; a prose surface phrasing the
value as undecided WITHOUT the token would evade it. Mitigation: the
top-level surfaces (RESEARCH-STATUS, CANON.md, CURRENT-STATE.yaml) were
checked for the token and carry none, and the claim is scoped to TYPED
surfaces accordingly.

## 10. The probe

`tests/channel-swings/joe_directed_ldc_vev_selector_adjudication.py` — 90
checks, exit 0. Pins: every §2 quote as an on-disk substring (register,
extraction, transcript L155/L158 including the L155-is-Killing-form
disambiguation, canon E, VERTEX and C5, SRC-1..4 headline clauses and
classifications, ledger rows RA-D2/RA-D4-buckets/LT-SM8/stationarity-triggers,
escape-corners :59-64, lega1 :257, ST-1 V1 and §6, SG-1, CN-2 §4.4, PCX-1,
SCUR-1 row 6 and D2, G6/L4/LA3 cells); computes the dependence lemma from the
parsed VERTEX (support size 3, non-product witness, both implications, the
A-corner corollary); verifies the absence claims (no SRC↔bit-2/SC-CHI-01
token join; no ONE_BIT on the G6-blocked rows; no "bit 2" on top-level
surfaces) each with a planted-positive control; pins this file's five verdict
lines; plants 12 false facts (each observed False) and one deliberately-wrong
verdict ("nobody's row" as LIVE-HIGH) whose absence-here and correction-here
are both asserted. `--selftest`: clean baseline verified FIRST (red baseline
aborts), then 8 machinery-corruption mutations — VERTEX-parser corner-drop,
register-path swap, ledger row-lookup crosswire, quote-normalizer case-fold,
absence-detector blinding, support-builder corruption, ledger-version
rollback, planted-fact evaluator inversion — each required to drive exit 1
via a genuine `[FAIL]` line; nonzero-exit-without-[FAIL] is counted
CRASH-NOT-DETECTION and fails the selftest; selftest exits 0.

## 11. Postflight — five lenses

**Lens A — verdict-typing auditor.** Five cards, five verdicts, each from the
allowed set, each aimed at the wave-internal concern (no repo claim is
killed; `target_claim: NONE-NOT-A-KILL` is correct). The one repo-surface
defect observed (LA3 direction flip) is flagged to its live owner, not
adjudicated.

**Lens B — reproduction auditor.** The honest ratio: cards 6 and 18 are
mostly retrieval — their value is the CORRECTION of the parent's premises by
banked artifacts (SRC arc; escape-corners GP-chain) plus two unmade joins.
The only computed novelty is the dependence lemma's explicit form (support
size, implications) — derived from a frozen predeclaration, claimed as a
typing, not as mathematics.

**Lens C — layer regression check.** No clause above credits the total theory
with chirality, reads the bit off observation, or moves a phase claim across
the PCX-1 boundary; the homonym is named at every use; "observed" appears
only layer-tagged.

**Lens D — both-failure-classes check.** Mis-credit risk peaked at card 16
(value "committed") — bounded by quoting both hedges in every statement of
it; mis-deny risk peaked at card 17's dissolution — bounded by §9's reverse
attack and by banking the residue as LIVE.

**Lens E — what did NOT move.** SG4 unchanged as decider; the 2-bit residual
unchanged (this file TYPES its internal dependence, choosing no corner); the
B-tilt unchanged; SC-CHI-01's adherence unchanged (a watch is PROPOSED);
no ledger row, register entry, canon file, or predeclaration edited;
`canonical_effect: pending_integration`.

## 12. Blunt paragraph — did the parent's "sharpest" ranking survive?

**Half of it.** Item 16 survives as live — but diminished: its central
dichotomy is too coarse for the actual state (the mechanism side is
route-killed and steelman-adverse, not "open"), and its "never typed"
premise is half-false (IT-C's buckets execute the distinction one row away;
the polarity axis carries it at the register). What fully survives of 16 is
the revival-routing stake, sharpened into the coupling rule whose absence
has three dated misses. Item 17 does NOT survive as posed: it inverts the
error direction — the "undecided" prose it indicts is the repository's
fences (SG-1's homonym, PCX-1's identification standard) doing their job,
and the item's own move commits the fenced error; its real content, found by
the census it demanded, is the opposite-binning defect, which is a
DIFFERENT claim with a different repair. Meanwhile the two items the parent
did NOT elevate carried the most consequential material: item 6's dig found
the repository already holds an adverse, source-native, pending-integration
answer to the stability question with zero routing into the selector's
grant-and-adherence structure, and item 18's dig found the two bits are
consistency-correlated with a banked corollary (a chiral grant prices bit 1)
that none of the post-ST-1 routing surfaces prints. The parent elevated the
two most philosophical items; the two most consequential were 18 and 6.
