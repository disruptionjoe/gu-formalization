---
artifact_type: exploration
status: exploration
doc_type: construction_result
created: 2026-08-17
work_item: DS-1
channel: grading-bridge
wave: reverse-scaffold wave 3 (direction-supply arc; shops GB1-SIGMA2-DIRECTION-SUPPLY)
title: "DS-1: the sigma_2-direction inventory, shopped exhaustively — the Gamma-odd stock is real, all of it sits exactly at GB-1's Krein-only pole (the Gamma-oddness residual is IDENTICALLY ZERO for every class-2 direction, so the angle condition costs nothing), and its Krein leg is K-parity-MIXED componentwise: at both physical horns an odd-grade Clifford monomial anticommutes with the Krein word beta_S iff its TIMELIKE index count is odd, every one of the seven odd grades contains both parities (censuses exact), multiplication by the volume word FLIPS the parity (so Hodge-dual grades and the two duality halves of the middle grade are parity-entangled and every Lambda^7_± irrep direction has nonzero commutator AND anticommutator with K), and the signature-true square of a sigma_2-type (t-odd) monomial is +1 exactly at grades k = 3 mod 4 — the middle grade included — and -1 at k = 1 mod 4. Named exemplar carrying every commutation-grade leg at once, both horns: the Lorentz-singlet-compatible middle-grade monomial e4 e5 e6 e7 e8 e9 e10 (Gamma-odd; K-anticommuting; square +1; contains no 4d index, so it anticommutes with any base-normal transverse symbol). BUT no candidate's Krein leg can be CERTIFIED: breaks-vs-preserves is a function of the adjoint, the reality map is SOURCE-SILENT (SN-1's standing UNDEFINED_WITHOUT_REALITY_MAP), and the same commutation fact flips verdict under the opposite self-adjointness assignment. Gapping leg priced: TB-4/O2 BITE the pure-cross rows (Lambda^3, Lambda^7_-+, and Lambda^11 by module identity: induced form rank <= 128 on the 896, >= 768 unpaired, and the middle-grade duality halves are additionally NILPOTENT at operator grade), and do NOT bite Lambda^1/Lambda^5/Lambda^9/Lambda^13 (diagonal blocks live; form-rank census OPEN). The freeze seam COMPOSES: paying W3 with class-2 stock freezes the interior graded trace and relocates the count to the kernel-index face, which is exactly the face the wall hosts on — no tension. VERDICT: CONDITIONAL — the arrow is neither paid nor dead; one common gate (the reality map) plus per-row gapping prices and one component datum, each with a named owner. W3's kill branch did NOT fire: the carrier OFFERS the direction class."
grade: "EXACT for everything load-bearing: the Gamma-parity and K-commutation parities are proved as monomial sign arithmetic and INDEPENDENTLY verified on explicit Cl(14) gamma matrices on C^128 (TR-1's own Kronecker construction, re-run and agreeing), at BOTH SIGNATURE-AMBIENT horns via their beta_S words (LD-B's K = eta_V (x) beta_S, beta_S = the spacelike product); the per-grade censuses are exact binomial identities cross-checked against matrix sweeps; the signature-true squares use signature-true generators (timelike squares -1) and match the closed form (-1)^(k(k-1)/2 + t) exactly; the transverse-symbol rule and the exemplar certificates are exact matrix identities; the nilpotency and rank-64 of a middle-grade duality-half action are exact; the freeze seam is verified on the lattice model (trace 0 at machine zero along the sigma_2 family; the sigma_3 contrast moves it). REPRODUCED, not recomputed: RSC-1's block table, rank bound and branching; TR-1's class table, freeze lemma, one-way Hom facts; LD-B's parity criterion and Lambda^1 rank stratum; ST-1's spinor squares; GB-1's circle. All four upstream instruments re-run by subprocess with pinned verdict strings: three green; GB-1's probe pinned at its measured post-landing state (its destination-uniqueness check reds on DS-1's own mount-required landing — deviation D5 — with every instrument leg green). CONDITIONAL and typed as such: every 'breaks/preserves Krein' verdict is conditional on the SOURCE-SILENT reality map (SN-1); no reality map is supplied, chosen, or needed for any exact statement here. NOT: an action, a vacuum, a scale, a spectrum, a count, a selector closure, a claim-status movement, or any registry/sidecar/ledger/canon edit."
disposition: INVENTORY_SHOPPED_EXHAUSTIVELY_SEVEN_ODD_GRADES_MIDDLE_SPLIT__GAMMA_ODD_EXACT_FOR_EVERY_ROW_BOTH_AMBIENT_GRADINGS_SO_THE_WHOLE_STOCK_SITS_AT_THE_KREIN_ONLY_POLE__K_PARITY_IS_TIMELIKE_COUNT_PARITY_AND_EVERY_ROW_IS_MIXED__VOLUME_WORD_FLIPS_IT__SIGMA2_SQUARES_PLUS_ONE_EXACTLY_AT_K_3_MOD_4__EXEMPLARS_NAMED_BOTH_HORNS__KREIN_LEG_UNDEFINED_WITHOUT_REALITY_MAP_SN1__TB4_O2_BITE_PURE_CROSS_ROWS_ONLY__FREEZE_SEAM_COMPOSES_KERNEL_INDEX_FACE__ARROW_CONDITIONAL_NOT_PAID_NOT_DEAD__NO_KILL
target_claim: "The register item GB1-SIGMA2-DIRECTION-SUPPLY, verbatim title from lab/process/upgrade-program-register.yaml:271: 'The W3 missing arrow: a source-native supply of the Gamma-odd, Krein-breaking mass direction (SC-CHI-02's m = R/4 is the charge-0 pole exactly). TR-1 section 8.3 supplies the typed inventory to shop: the Gamma-odd stock is exactly the class-2 rows (odd-k epsilon directions); the Krein leg of each candidate is reality-map-gated (SN-1); the gapping leg priced by TB-4's rank bound. If the carrier offers no such direction, W3 dies at this arrow' (id at :270, owner at :273, activation 'next grading-adjacent wave; shop TR-1's class-2 inventory first' at :275)."
target_claim_verdict: "SHOPPED TO A CONDITIONAL — NOT ARROW-PAID, NOT ARROW-DIES, NO KILL. The kill condition ('If the carrier offers no such direction, W3 dies at this arrow') did NOT fire: the carrier OFFERS the direction class — every odd grade contains Gamma-odd, K-anticommuting components at both physical horns (censuses exact; exemplars named), the whole class-2 stock carries Gamma-oddness residual IDENTICALLY ZERO (GB-1's pole condition is met by class membership, not by tuning), and the exit such a direction makes is automatically the Krein-only one GB-1's iff demands. The arrow is NOT PAID because the Krein leg's verdict predicate — BREAKS vs PRESERVES Krein-self-adjointness — is a function of the adjoint, and the adjoint is exactly what the source does not supply: SN-1's UNDEFINED_WITHOUT_REALITY_MAP stands, and the identical commutation fact yields opposite verdicts under the two self-adjointness assignments. Surviving candidates are each blocked on typed conditions with owners: C-DS1-REALITY (common, the reality map — source-silent; SN-1 owner with GB-1's Link D holders), C-DS1-GAP per row (TB-4's supplement price on the pure-cross rows Lambda^3/Lambda^7_-+/Lambda^11; an OPEN form-rank census on Lambda^1/Lambda^5/Lambda^9/Lambda^13), and C-DS1-COMPONENT (which direction inside the module the VEV picks — the K-parity is component-inhomogeneous in every row; Lane 1 / SG4-bit-2-adjacent, NOT closed here). Even fully paid, the route still owes the rest of RW-1's W-bill and the external Z/2 sector datum WB-1 relocated W1 to — nothing here launders those."
canon_verdict_change: none
row_change: none
registry_change: none
steering_effect: unchanged
canonical_effect: pending_integration
scripts:
  - tests/channel-swings/joe_directed_ds1_the_stock_sits_at_the_pole_and_waits_on_the_reality_map.py
depends_on:
  - lab/process/upgrade-program-register.yaml
  - lab/active-research/joe-directed/grading-bridge/gb1-the-bridge-is-one-angle-and-one-missing-arrow-2026-08-17.md
  - tests/channel-swings/joe_directed_gb1_the_bridge_is_one_angle_and_one_missing_arrow.py
  - lab/active-research/joe-directed/spectral-transport/tr1-transport-and-selection-are-opposite-parities-2026-08-17.md
  - tests/channel-swings/joe_directed_tr1_transport_and_selection_are_opposite_parities.py
  - lab/active-research/joe-directed/rs-corner/rsc1-unique-channel-lives-on-the-gamma-trace-2026-08-17.md
  - lab/active-research/joe-directed/rwall/rw1-zero-locus-steers-not-hosts-2026-08-17.md
  - lab/active-research/joe-directed/wall-bill/wb1-crossing-is-a-sector-datum-r-cannot-supply-2026-08-17.md
  - lab/active-research/joe-directed/lens-digs/ldb-bit2-direction-and-krein-parity-2026-08-17.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn1-observed-neutrino-mass-pencil-2026-08-16.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - tests/function-space-ext/dirac_spectral_flow_section.py
  - tests/function-space-ext/krein_spectral_flow_probe.py
  - lab/methods/gu-base-categories.md
  - CANON.md
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - lab/sources/source-claim-register.yaml
  - lab/process/correction-registry.yaml
  - lab/process/homonym-register.yaml
  - VERIFICATION.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result. Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.
>
> The comparator legs here are (i) the model face of the freeze seam on the
> continuum/lattice fiber of `tests/function-space-ext/dirac_spectral_flow_section.py`
> (RW-1 §4's and GB-1 §2's class — binds that model), and (ii) explicit Cl(14)
> gamma matrices on `C^128` — the carrier's FIBER algebra, used to witness
> commutation-parity and square-sign facts, not spectra (TR-1 LEG 5's
> instrument, extended with signature-true generator squares). "Krein",
> "sigma_2-type", "mass direction", "gap", "tachyonic" are repository/comparator
> vocabulary throughout; the source's own words are "mass", "decouples",
> "[Weyl] equations". The exact parity and census statements are structural
> facts about the Clifford algebra of `V_14` at the two SIGNATURE-AMBIENT horns
> evaluated on GU's own declared bosonic slot; they move no GU row. This
> artifact is ABOUT whether a typed inventory contains an object with three
> stated properties — a semantic-boundary adjudication — hence the
> classification.

> [!NOTE]
> **Registry debt, declared rather than silently incurred (LD-A's precedent;
> GB-1 and TR-1 did the same).** `process_gates/source_native_comparator_routing_audit.py`
> was already red at HEAD before this artifact existed; DS-1 adds one
> unclassified path to the printed gap. The registry row is a transcription
> owed to the method owner (`lab/process/source-native-comparator-routing-registry.json`
> is outside DS-1's write scope; the gate's own comment forbids an auditor
> guessing). The entry, for the integrator:
>
> ```json
> { "path": "lab/active-research/joe-directed/grading-bridge/ds1-the-stock-sits-at-the-pole-and-waits-on-the-reality-map-2026-08-17.md",
>   "classification": "BRIDGE_OR_SEMANTIC_BOUNDARY" }
> ```
>
> The typed-carrier gate's pre-existing red (sibling `selected-k151`) and the
> k149 certificate-shape allowlist red are other lanes' debts, untouched.
>
> **One NEW red, caused by this landing and declared (deviation D5):** GB-1's
> probe pins destination uniqueness — `L5-dest-artifact` asserts
> `grading-bridge/` holds exactly the GB-1 artifact ("no duplicate seed").
> DS-1's mount-required artifact path sits in that directory, so at HEAD the
> GB-1 probe now reports `checks: 114  failures: 1 ['L5-dest-artifact']` —
> every instrument leg still green, exactly one failing check, and DS-1's own
> probe PINS that exact post-landing state (LEG 5) so any further movement in
> GB-1's probe fails loudly. DS-1 is a register-scheduled successor arc, not a
> duplicate seed; the pin refresh (admit scheduled successors, or move the
> uniqueness check to a prefix test) is owed to GB-1's owner / the integrator.
> DS-1 edits no existing file, so it cannot perform the refresh itself.

## Typed objects

```gu-typed-objects
result:         the exhaustive shop of the class-2 (odd-grade epsilon-slot)
                direction inventory for GB-1's missing arrow: Gamma-parity
                ODD for every row with residual identically zero (the whole
                stock sits at the Krein-only pole); K-commutation parity =
                timelike-index-count parity at both horns, every row mixed,
                volume-word multiplication flips it; signature-true sigma_2
                squares +1 exactly at grades k = 3 mod 4; transverse-symbol
                rule (anticommutes with c(e_n) iff n not in the monomial);
                named exemplar e4 e5 e6 e7 e8 e9 e10 carrying all
                commutation-grade legs at both horns
carrier:        the Clifford fiber algebra of V_14 acting on Delta = C^128
                (explicit Pauli-Kronecker gammas, TR-1's construction),
                graded by the ambient volume word; horns entered through
                LD-B's Krein word beta_S = product of the spacelike gammas
                ((9,5): p = 9; (7,7): p = 7) and through signature-true
                generator squares (timelike squares -1); the epsilon slot
                ad P = End(Delta) = Sum_k Lambda^k V_14 is the source's
                declared 0-form slot; corners nu_± in Omega^0(S_±), zeta_±
                in Omega^1(S_±) = S_-+ (+) R^(±) (RSC-1's split) are the
                pairing carriers
                LAYER=ambient CHIRALITY=S-FULL-DIRAC
                # This is the FIBER algebra with signature-true squares,
                # NOT the (9,5) real/quaternionic form: no real structure
                # is constructed and no reality map is supplied.  The
                # commutation parities are representation-independent sign
                # arithmetic; the matrices are the second instrument.
pairing:        cross-chirality Krein form K = eta_V (x) beta_S ON=V (x) S
                and beta_S ON=S (LD-B's object, ldb:188 and :853; consumed
                for COMMUTATION algebra only — no signature, no norm, and
                no self-adjointness verdict is evaluated here, because the
                adjoint needs the SOURCE-SILENT reality map, SN-1)
real_structure: SOURCE-SILENT and load-bearing BY ITS ABSENCE: the one
                verdict the arrow needs (BREAKS vs PRESERVES
                Krein-self-adjointness) is a function of the adjoint;
                SN-1's UNDEFINED_WITHOUT_REALITY_MAP is the standing gate
                and this artifact's headline conditional
grading:        Gamma_amb = i(g_1..g_14), the ambient volume word — GB-1's
                N1, sense (a) of the four-way homonym; Gamma_W (the Z/4
                package grading) agrees with it on every epsilon-slot row
                (Delta p = 0, TR-1 section 3.2), so every verdict here is
                stated for BOTH ambient operators at once and TB-0 is
                satisfied by declaration; the branched gamma_5/gamma_int
                faces are not consumed; no bare cross-sense token is used
action_owner:   repository-construction (the parity theorems, censuses,
                exemplars and both instruments); the epsilon slot is
                source-print (draft eq (9.16), the rho(epsilon) wrap, s9
                extraction :68-:74); the DECOMPOSITION into Lambda^k rows
                and every selectivity/parity fact about them is
                repository-derived; whether any insertion is ON is
                source-action, SG4 bit 2, Lane 1 — OPEN (CC-06)
target:         GB1-SIGMA2-DIRECTION-SUPPLY (register:270-276): does the
                declared slot contain a Gamma-odd, Krein-breaking, gapping-
                priced direction  MAP-TYPE=not-a-map (an inventory
                adjudication on a class of directions, not a map)
```

```gu-typed-objects
result:         the shop's composition verdict: the class-2 stock pays
                GB-1's angle condition by class membership (residual
                identically zero), the Krein leg is one common typed gate
                (the reality map), the gapping leg is TB-4/O2 pricing that
                bites exactly the pure-cross rows, the freeze seam composes
                (class-2 payment relocates the count to the kernel-index
                face, which is the wall's own hosting face), and the arrow
                is CONDITIONAL with named owners — not paid, not dead
carrier:        the cited register title, GB-1 Link D, TR-1 sections
                3.2/3.3/4.1/4.2/5/8.3, RSC-1 sections 4.2/4.3/5.2, RW-1's
                W-bill, WB-1's W1 relocation, LD-B's parity table and
                SN-1's gate, as literal pinned text
                LAYER=source-print CHIRALITY=N/A
pairing:        NONE
# A chain/inventory adjudication over pinned text plus the instrument
# results of block 1: no bilinear form is evaluated in this result and
# none is claimed.  Bare token per the FX-2 parser; gloss on these
# comment lines.
real_structure: N/A
grading:        N/A — every grading token used is typed in block 1 and in
                section 3; no bare cross-sense use
action_owner:   repository-construction (the composition and the verdict
                are DS-1's; every consumed sentence is pinned to its owner)
target:         the register item's five-way fork (ARROW-PAID / ARROW-DIES
                / CONDITIONAL), adjudicated CONDITIONAL
                MAP-TYPE=not-a-map
```

# DS-1 — the direction shop: what the carrier offers, and what still gates it

**The task, from the register.** GB-1 reduced W3 to one missing arrow — *"a
source-native supply of the Gamma-odd, Krein-breaking mass direction"* — and
TR-1 §8.3 typed the shelf to shop: *"the Γ-odd stock is exactly the class-2
rows of §3.2 (odd-k ε directions; even-k ϖ content). The Krein leg of each
candidate is reality-map-gated (SN-1); the gapping leg is priced by TB-4's
rank bound"* (tr1:696-699). DS-1 walks the shelf end to end. The answer,
measured: the stock is real and better-placed than the register priced it —
**every class-2 direction sits exactly at GB-1's Krein-only pole for free**
(the Γ-oddness residual `{M, Γ}` that GB-1 computed as `2|cos φ|` on the
model circle is identically zero for the whole class, §4.1) — but no candidate
can be *certified*, because the one predicate the arrow's Krein leg turns on
(breaks vs preserves Krein-self-adjointness) is a function of the adjoint,
and the adjoint is the reality map the source never supplies (SN-1). The
verdict is CONDITIONAL, with the conditions typed and owned (§7).

---

## 0. The source frame, carried (quoted, not paraphrased)

Compression re-imports default frames at every boundary; the operative
sentences travel verbatim with their IDs.

- **CC-06-CHIRALITY-VEV-CONDITIONAL** (`correction-registry.yaml:299-306`),
  superseded reading quoted: *"That the source has no stated
  effective-chirality mechanism, or that chirality can only come from an
  unbuilt mirror-gapping condensate -- when observed chirality is
  VEV-CONDITIONAL ('exactly three families of chiral fermions if you have a
  decreased VEV ... taking a Dirac equation into two [Weyl] equations',
  drafts L158 / ucsd 00:46:02) and the selector is SG4 bit 2, OPEN by
  design."* Carried: `SC-CHI-01`'s declared total is NON-CHIRAL and *"splits
  at the emergent level"* (register:913-915), conditioned on *"no vacuum
  expectation value pulling the various sub-fields of ϖ to values
  significantly above zero"* (extraction:137-138). **A found direction
  SUPPLIES a capability; it does not close the selector. SG4 bit 2 stays
  OPEN** (SC-CHI-01; the chirality frame is SC-CHI-01/SC-CHI-02's,
  VEV-conditional throughout). Nothing below decides, assumes, or advances
  any bit.
- **CC-05-SUBTRACTIVE-TWO-PLUS-ONE** (`correction-registry.yaml:263-269`),
  quoted: *"That three generations is an ADDITIVE target count a mechanism
  must produce -- when the source says it is 'really two plus one' with the
  third family an imposter 'for representation theoretic reasons' (drafts
  L119/L128/L131) and HE-1 makes the partition FORCED and SUBTRACTIVE
  (n_g -> n_g - 1, unlabelled, the distinguished family REMOVED)."*
  Carried: no count is produced, hosted, or moved here; the wall route's
  ceiling (hosting, multiplicity SUPPLIED) is inherited from RW-1 W5
  unchanged.
- **CC-08-DARK-PARTNER-OBLIGATION** (`correction-registry.yaml:363-368`),
  quoted: *"That the 128 remainder is an established DEFECT of the
  construction -- when the source NAMES dark sectors (Looking-Glass, dark
  spinorial, 'Rarita-Schwinger matter and more') and the remainder is a
  partner-placement / decoupling OBLIGATION, not an established defect."*
  Carried: the 128 enters only through TB-4/O2's rank price, as the
  obligation whose gapping endpoint is priced — never as a defect.
- **Homonym fences.** "Chirality" is the fenced four-way homonym; the
  register's `CHIRAL` entry rule binds: *"write 'CHIRAL (massless/unbroken)'
  when naming the PHASE value ... for handedness use the CN-2 closed
  vocabulary S-HALF-SAME / S-HALF-OPPOSITE, never the bare word"*
  (homonym-register `CHIRAL` entry, :242 ff., disambiguator block). No bare
  cross-sense use below; grading operators are always named (Γ_amb / Γ_W).
  "Rarita-Schwinger" is a registered THREE-way homonym (register token at
  :1187 ff., provenance rsc1-2026-08-17): **RS-1** ambient γ-traceless
  `R^(±)` (832), **RS-2** internal `144`, **RS-3** the 4d spin-3/2 sector
  `Q` (192). Every RS mention below names its sense; the 128 sits inside
  RS-2 hence RS-1 and NOT RS-3 (the register rule, respected).

---

## 1. PREFLIGHT — seven problem-matched lenses, declared before the work

- **L1 — Krein / indefinite-metric specialist (mandatory).** The Krein
  question for a MASS DIRECTION is not "what is K's signature" but "does the
  insertion stay in the Krein-self-adjoint class" — and that class predicate
  `D^† K = K D` (instrument's own membership test,
  `dirac_spectral_flow_section.py:15,:108`) consumes the ADJOINT. Prediction
  recorded before computing: on the complexified carrier only the COMMUTATION
  algebra `[c(ω), K]`, `{c(ω), K}` is decidable; the breaks/preserves verdict
  will split into (commutation parity) × (self-adjointness assignment), the
  second factor reality-map-gated. Confirmed — and the commutation parity
  turned out COMPONENTWISE, not modulewise (§4.2), which no input predicted.
- **L2 — Reality-structure lens (mandatory).** SN-1's gate is the standing
  fence: *"The source extraction treats barred and unbarred variables as
  independent and does not supply the global Hodge/Krein/reality adjoint.
  Therefore a matrix between barred and unbarred fields is not automatically
  a Majorana mass"* (sn1:207-209), recorded as `Majorana status:
  UNDEFINED_WITHOUT_REALITY_MAP` (sn1:214). Binding condition, set before
  computing: no clause below may output BREAKS or PRESERVES un-conditioned;
  the deliverable per row is the exact two-branch table plus the gate. Also
  carried: LD-B's own Λ¹ caveat — the induced mass SHAPE's rank *"equals it
  only after composing with a charge conjugation C, which is NOT computed
  here and which the source does not supply"* (ldb:861) — the same gate one
  instrument earlier.
- **L3 — Layer-typist (base-categories discipline).** The *"one unforgivable
  regression"*: *"crediting the total theory (L1) with chirality, or the
  package (L3) with an unconditional spectrum. Chirality is L4-conditional
  (LA3)"* (`gu-base-categories.md:122-124`). Design answer: the shop types
  DIRECTIONS available in a declared slot; availability is L1 structure;
  whether any direction is ON is the LA3/G6 grant, Lane 1's, untouched. The
  wall handoff stops where GB-1 stopped (LA1 non-injective, base-cat :89;
  inverse a named non-arrow, :106); nothing here reaches observed content.
- **L4 — Claim-targeting auditor.** Kills and payments must name real claims;
  the wave's measured failure mode is targeting disavowed claims. Fired
  during retrieval, twice: (i) the mount's parenthetical "which the source
  names — the middle forms" is not a source fact — no source surface names
  the middle-form direction (certified absence, §3.2, deviation D1); GB-1's
  Link D verbatim admits *"a source-attested or repository-constructed"*
  supply (gb1:397-398), so the arrow's demand survives the correction.
  (ii) The register title's inventory "(odd-k epsilon directions)" and
  TR-1 §8.3's "(odd-k ε directions; even-k ϖ content)" differ by the ϖ
  annex — typed at §3.3, deviation D2.
- **L5 — Composition-seam auditor (the wave-3 lens).** Un-composed adjacency
  is the measured failure class; wave 2 shipped TWO results this arc must
  compose, not merely cite: TR-1's freeze (class-2 families freeze the
  graded trace) and GB-1's circle (full charge iff Krein-only exit). The
  design answer is §6: the freeze is not in tension with paying W3 — it is
  the MECHANISM that relocates the count to the face the wall hosts on, and
  the sum-vs-product distinction (operator sums of class-2 terms stay
  class-2; TR1-COMPOSITE-PARITY's flank concerns insertion COUNTS in
  pairing invariants) is typed so the two do not blur.
- **L6 — Homonym warden.** Three fenced token families are load-bearing here
  (chirality four-way; `C`/"the parity" on the Krein side; Rarita-Schwinger
  three-way). Design answer: §0's fences; RS tokens always sensed; the two
  ambient grading OPERATORS (Γ_amb volume word vs Γ_W package class) are
  named per TR-1 §3.1 and every Γ-parity verdict is stated for both (they
  agree on the ε slot — TB-0 satisfied by declaration, not by silence).
- **L7 — Hostile self-auditor (the import lens, carried from GB-1 §6).** The
  named strongest attack: "you found textbook pseudoscalar-mass components
  in a Clifford algebra and called it a supply — the σ₂-type direction was
  never in doubt as ALGEBRA; the question is whether GU's action writes it."
  §10 runs the attack inline; the short answer is that the shop's ceiling is
  typed everywhere as CAPABILITY (the slot contains the class), the ON
  switch stays Lane 1's, and the two genuinely new constraints run AGAINST
  comfort: the Krein leg cannot be certified at all without the reality
  map, and the gapping leg's TB-4 price bites the selective rows hardest.

**Kill-or-switch, recorded before computing.** If every K-anticommuting
component of every odd grade had vanished at the physical horns (e.g. if
K-parity had been GRADE-homogeneous with the wrong sign), the inventory
would contain no Krein-exiting direction, and this artifact would have led
with ARROW-DIES and the two kill-target quotes of §8. Measured: the
opposite — every grade is K-parity-mixed with exact nonzero censuses both
ways (§4.2). The kill branch is closed for this arc.

---

## 2. Retrieval — prior art, the honest ratio, and the novelty greps

Searched before computing, by object and by mechanism: *sigma_2-type,
direction supply, Krein-breaking, beta_S, timelike count, K-parity,
odd-timelike, signature-true, square sign, Hodge flip, transverse symbol,
epoch wall, class-2 inventory, rank 128, nilpotent insertion, middle form.*

| Banked result consumed | Owner | Status |
|---|---|---|
| The four-grading enumeration; the circle: hosted charge = −sin φ, Γ-residual 2\|cos φ\|, full charge iff Krein-only exit (φ = ±π/2); transport closed; Link D typed | GB-1 (gb1:185-220, :254-343, :396-414) | **consumed unchanged; probe re-run** — every instrument leg green; its LEG-5 destination-uniqueness pin goes red at HEAD by DS-1's own mount-required landing, and DS-1's probe pins the exact post-landing state `checks: 114  failures: 1 ['L5-dest-artifact']` (deviation D5) |
| The class table (§3.2), the freeze lemma (§4.1), the kernel-index face (§4.2), TB-0..TB-5 (§5), the one-way middle-form arrow (§3.3), the §8.3 shopping list | TR-1 | **consumed unchanged; probe re-run green** (LEG 5, pinned "90/90 checks pass"); the (0,7)/(4,3) parity facts re-verified on the same gamma construction ([R], LEG 3) |
| ζ_± = S_∓(64) ⊕ R^(±)(832); the three-block Λ² table; O1/O2 (rank ≤ 128 on the 896, ≥ 768 unpaired, at most 64 of the 128); C2's blind shapes; the RS three-way homonym | RSC-1 (rsc1:341-342, :389-399, :413-422, :444-446, :583-588, :604-606) | **[R] quoted, not recomputed**; the pure-cross-row identification for Λ³ is a NEW COMPOSITION of RSC-1's own table entries (§5.2) |
| K = eta_V ⊗ beta_S, beta_S the spacelike product; halves totally null iff q odd; {K, χ} = 0 exactly at odd q; both horns odd; the Λ¹ rank stratum 64/32 and its C-composition caveat | LD-B (ldb:188, :208, :211, :240-249, :861) | **[R] consumed at its recorded grade**; the componentwise K-parity of INSERTIONS (§4.2) is the adjacent question LD-B did not ask |
| Sym²(S_+) = Λ³ ⊕ Λ⁷_+, Λ²(S_+) = Λ¹ ⊕ Λ⁵; blind-live directions; "the class-2 insertion count must be ODD" | ST-1 (st1:456; §4.1, §4.5) | **[R] quoted** |
| W-bill W1-W6; the die quote; the σ₃/σ₂ wall pair | RW-1 (rw1:260-307, :824-826) | **consumed unchanged** |
| W1 RELOCATED to an external Z/2 sector datum; the π₀ stacked gate | WB-1 (wb1:9, :13); register W1-PI0-NATIVE-VACUUM :263-269 | **consumed unchanged** — the debt DS-1 must not launder |
| SN-1's reality-map gate | SN-1 (sn1:207-214) | **quoted; the shop's common conditional** |
| Canon count doorway: net-0 class row; flux exemplar; external-by-structure | CANON.md:135/:136/:139; instrument:18/:29 | **quoted and pinned; both instruments re-run green** |

**Honest ratio: roughly 40% of the exact content is reproduction**, [R]-tagged
in the probe before anything new runs. **The novelty greps, recorded per the
rule** (zero hits is NOT evidence of new; nonzero hits were read):
`"timelike content"` 0; `"sigma_2-type stock"` 0; `"Krein-breaking component"`
0; `"signature-true"` 0; `"K-parity-mixed"` 0; `"Hodge-flip"` 0;
`"anticommutes with beta_S"` 0; `"transverse-free"` 0; `"epoch wall"` /
`"timelike wall"` 0/0. **Three nonzero collisions, read and adjudicated:**
(i) `"odd-timelike"` — 1 hit, `tests/big-swing/vg_v8_t5_map_attempt.py:493`:
*"the 16/16bar direction chi_int and every odd-timelike-count channel is
P-ODD"* — the SAME sign-arithmetic shape (odd timelike count ⇔ anticommutes
with a spacelike volume word) for a DIFFERENT operator (its `P`/`Q5` is the
spacelike INTERNAL 5-volume on a Mannheim-map positivity scan, not LD-B's
Krein word on the corner carrier). The bare parity arithmetic of §4.2 is
therefore NOT claimed as new; what is new is its statement for `beta_S` at
both horns, the per-grade censuses, the volume-word flip, and the
composition with the W3 arrow. The probe pins the collision line as
REQUIRED-PRESENT (an attribution that must not silently rot). (ii)
`"K-parity"` — 1 hit, a `DECK-parity` substring false positive (q3 probe),
unrelated. (iii) `"square sign"` — hits in the twistor arc
(orientation/volume square signs, tw3a/tw3b), a different object; read and
unrelated. Destination duplicate check (memory rule): `grading-bridge/`
contains only GB-1 and this artifact; no duplicate seed; probe namespace
`joe_directed_ds1_*` fresh.

---

## 3. (a) The inventory, enumerated completely

### 3.1 What the register's shelf is

TR-1 §3.2 (tr1:356-361) types every declared-slot deformation direction by
one Z/4 integer. The class-2 ε-slot row, verbatim from the table: *"ε ∈
Ω⁰(ad P) | Λ^k, k odd — Λ¹, Λ³, Λ⁵, **Λ⁷_± (the source's chirality
mechanism)** | **2** | **ODD** | **ODD** (Δp = 0, parities agree)"*
(tr1:358). The register title freezes the shop to exactly this row family:
*"the Gamma-odd stock is exactly the class-2 rows (odd-k epsilon
directions)"* (register:271). At `D_7`, `ad P = End(Δ) = Σ_{k=0}^{14} Λ^k
V_14` (rsc1:333), so the odd-k ε shelf, enumerated as GRADES with their
module identifications, is complete at seven grades with the middle grade
split:

| # | grade | dim | module identity | RSC-1 block channels of its Grassmann-live (Λ²) square at the 1-form corner ζ_+ (insertion convention) | 0-form corner Λ²(ν) | source status |
|---|---|---|---|---|---|---|
| 1 | Λ¹ | 14 | vector V; self-dual under −w₀ | trace 1, cross 1, RS-1 block 1 — **all three blocks live** (rsc1:394) | live (Λ²(S) = Λ¹ ⊕ Λ⁵, st1 §4.1 [R]) | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED |
| 2 | Λ³ | 364 | self-dual | trace 0, cross 1, RS-1 block 0 — **pure cross** (rsc1:395) | dead (no Λ³ in Λ²(S)) | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED |
| 3 | Λ⁵ | 2002 | self-dual | trace 1, cross 1, RS-1 block 2 — **all three blocks live** (rsc1:396) | live | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED |
| 4 | Λ⁷_+ | 1716 | middle form, (Λ⁷_+)* = Λ⁷_− | at ζ_−: pure cross, mult 1 (mirror of rsc1:397-398); at ζ_+: 0 | dead (Grassmann-dead at 0-form corners, st1 title [R]) | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED (see D1) |
| 5 | Λ⁷_− | 1716 | middle form | at ζ_+: **pure cross, mult 1 — ST-1's unique selective channel, located on the gamma trace** (rsc1:413-418) | dead | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED (see D1) |
| 6 | Λ⁹ | 2002 | ≅ Λ⁵ as a module (Hodge) | = row 3's channels (Hom spaces see the module class) | live | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED |
| 7 | Λ¹¹ | 364 | ≅ Λ³ | = row 2's channels — **pure cross** | dead | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED |
| 8 | Λ¹³ | 14 | ≅ Λ¹ | = row 1's channels | live | SLOT-SOURCE-PRINT / DIRECTION-REPO-DERIVED |

Grade-vs-module note, load-bearing: rows 6-8 are the SAME modules as rows
1-3 (so every invariant-channel multiplicity transfers exactly), but they
are DIFFERENT operator directions in `End(Δ)` — `c(⋆ω) ∝ c(ω)·Γ_amb` — and
§4.3 measures that the K-parity of a component FLIPS under that volume-word
factor. The shelf is eight rows as directions, five as modules.

### 3.2 Source status, adjudicated (the D1 correction)

The SLOT is source-print: eq (9.16) displays the `ρ(ε)` covariance wrap and
the ϖ cells (s9 extraction :68-:74, :82-:99); the corners are the draft's
printed p.51/eq (11.6) content (RSC-1 §4.4 [R]). The DIRECTIONS are not:
exact-substring scans over `lab/sources/*.md` (all extraction and register
surfaces) return **zero** hits for "middle form"/"middle-form", and the s11-s12
extraction contains no `Λ⁷`/seven-form token — certified in the probe with a
planted positive (LEG 6). The source's ONE stated deformation is SC-CHI-02's
`dslash_A psi_L(y) = (R(y)/4) psi_R(y)` (register:940) with SC-MAS-01's
*"stylized massive Dirac Equation with mass m = R(y)/4"* (register:1176) —
one real coupling, γ₅-even at the branched face (TR-1 §4.4), the `φ = 0`
charge-zero pole exactly (GB-1 §3 Link D; the register title says the same).
**So every row of the shelf is REPO-AVAILABLE-ONLY at direction grade**;
"the source's chirality mechanism" in TR-1's table cell is a compression of
"the unique chirality-selective direction OF the source's declared slot"
(ST-1 §4.5(3)'s theorem, a repository derivation). GB-1's Link D asked for
*"a source-attested **or repository-constructed** zero-order deformation"*
(gb1:397-398) — the shelf survives on the second disjunct. Deviation D1
records the mount-vs-measured mismatch.

### 3.3 The ϖ annex (typed, not shopped — the D2 boundary)

TR-1 §8.3 adds "even-k ϖ content" to the Γ-odd stock; the register title
does not. The difference is TB-0's own subject: ϖ-slot parities FLIP between
the two ambient operators (Γ_W-odd but Γ_slot-EVEN for even-k ϖ content;
the reverse for odd-k — tr1:360-361), so the ϖ annex is Γ-odd only under
ONE of the two ambient gradings, while every ε row is odd under BOTH. The
register scoped the shop to the unambiguous stock; DS-1 shops exactly that
and types the annex as: OPEN under TB-0 (declare the grading first), owned
by ST-1's Ω¹ column (consumed, not retyped — TR-1 §10's own fence), with
the note that any annex candidate would additionally face the same SN-1
gate and W4 exit-typing as the ε rows. Nothing below consumes the annex.

---

## 4. (b) The Krein leg — the SN-1 gate applied, and what is exact beneath it

### 4.1 The free half: the whole stock sits at GB-1's pole

Exact, both ambient operators, every row: an odd-grade Clifford element
anticommutes with the volume word, so **`{c(ω), Γ_amb} = 0` identically for
every direction of every row** — verified as sign arithmetic and on the
explicit gammas (probe LEG 3), and it is TR-1's parity theorem (tr1:365-369
[R]) read at operator grade. Consequence, stated against GB-1's circle: the
Γ-oddness residual GB-1 measured as `2|cos φ|` on the model circle
(gb1:278-282) is **identically zero for class-2 stock** — in circle
coordinates the entire inventory sits AT `φ = ±π/2`, the pole where *"FULL
canon-graded charge holds IF AND ONLY IF the exit is Krein-only"*
(gb1:283-289). The angle condition W3 and W4 fused into is met by CLASS
MEMBERSHIP, not by tuning. What remains of the Krein leg is exactly one
question: does the candidate actually EXIT (break the Krein condition), and
that is where the gate lives.

### 4.2 The exact commutation layer (unconditional)

LD-B's Krein form on the carrier is `K = eta_V (x) beta_S` *"with `beta_S`
the product of the spacelike gammas"* (ldb:188; typed block ldb:853); ε
insertions act on the spinor factor, so their K-algebra is their
`beta_S`-algebra on both corner types. Both physical horns have odd
spacelike count (`(9,5)`: p = 9; `(7,7)`: p = 7) and odd timelike count
(LD-B's criterion: halves null iff q odd, ldb:208, :211 [R]). **Theorem
(monomial K-parity; proved as sign arithmetic, verified on explicit Cl(14)
matrices at both horns, probe LEG 3):** for a grade-k Clifford monomial
`c(ω)` with `j` spacelike and `t = k − j` timelike indices,

```
    c(ω) · beta_S = (−1)^(k·p − j) · beta_S · c(ω);   with k, p odd:
    c(ω) ANTICOMMUTES with beta_S  ⇔  t is ODD.
```

(Consistency pin: the full volume word has `t = q` odd, reproducing LD-B's
`{K, χ} = 0` at odd q.) The per-grade census — exact binomials
`Σ_t C(p, k−t)·C(q, t)`, cross-checked against matrix sweeps:

```
    grade k:                1     3     5     7     9    11    13
    dim Λ^k:               14   364  2002  3432  2002   364    14
    (9,5) K-anti (t odd):   5   190   991  1716  1011   174     9
    (9,5) K-comm (t even):  9   174  1011  1716   991   190     5
    (7,7) K-anti (t odd):   7   182  1001  1716  1001   182     7
    (7,7) K-comm (t even):  7   182  1001  1716  1001   182     7
```

Readings, each machine-checked:

1. **Every row is K-parity-MIXED** — no odd grade is K-homogeneous — and
   **every row's K-anticommuting (σ₂-type) stock is NONEMPTY at both
   horns**. The register's kill condition fails at the widest gate.
2. **The K-parity is a COMPONENT datum, not a module datum.** Which
   component the VEV picks decides the parity — a direction question of
   exactly the kind Lane 1 / the bit-2 selector owns. (Even the
   Lorentz-singlet constraint does not decide it: both singlet splits of
   the middle grade, `(0,7)` and `(4,3)` — TR-1 §4.4's `120 ⊕ 120`,
   rsc1:506-509 — contain both parities; witnesses at t = 1 and t = 2 are
   pinned in the probe.)
3. **The volume word flips the parity.** `{beta_S, Γ_amb} = 0` at odd q, so
   `c(ω) ↦ c(ω)·Γ_amb` (the Hodge companion) flips K-parity — measured
   exactly. Two consequences: the dual grade pairs (1,13), (3,11), (5,9)
   have componentwise OPPOSITE parities under the duality map (visible in
   the census symmetry), and — sharper — the **duality halves `Λ⁷_±` are
   parity-entangled**: a middle-form IRREP direction is
   `c(ω)(1 ± phase·Γ_amb)/2`-shaped, one term of each parity, so **every
   `Λ⁷_±` direction has BOTH a nonzero commutator and a nonzero
   anticommutator with K**. No middle-form irrep direction is K-inert; none
   is purely σ₂ either.
4. **The prior-art attribution:** the bare parity arithmetic (odd timelike
   count ⇔ anticommutes with a spacelike volume word) appears once before,
   for a different operator on a different scan
   (`tests/big-swing/vg_v8_t5_map_attempt.py:493`) — [R]-adjacent, §2.

### 4.3 The gate (why no candidate can be certified)

The class predicate is Krein-SELF-ADJOINTNESS: `D^† K = K D`
(CANON.md:136's class; instrument:15). For an insertion `M`:

- if the realization makes `M` **self-adjoint** (`M† = M` — which the count
  object itself presupposes: TR-1's freeze lemma hypotheses are
  *"self-adjoint D, unitary Hermitian Gamma"*, tr1:110-114):
  **BREAKS ⇔ [M, K] ≠ 0** — i.e. any nonzero K-anticommuting component
  breaks; a purely t-even direction PRESERVES (the σ₁-type: in class,
  Γ-odd, exactly the model's non-gapping class member, gb1:238-240).
- if the realization makes `M` **anti-self-adjoint** (`M† = −M`):
  the verdicts FLIP (breaks ⇔ nonzero K-COMMUTING component).

Which branch a given component sits on is a function of the adjoint on the
physical carrier — the global Hodge/Krein/reality adjoint that SN-1 records
as not supplied: *"The source extraction treats barred and unbarred
variables as independent and does not supply the global Hodge/Krein/reality
adjoint"* (sn1:207-208); `UNDEFINED_WITHOUT_REALITY_MAP` (sn1:214). On the
complexified instrument the branch can be CHOSEN (dress by `i`); on the
real form it is FIXED by the real structure — and the real form is exactly
what is not constructed (the fence every wave-2 artifact carries:
"explicitly NOT the (9,5) real-form fork"). TR-1 §6 already consumed this
as the missing theorem's first input: *"(i) the reality map / inner product
making D(t) self-adjoint (SOURCE-SILENT; SN-1's standing
UNDEFINED_WITHOUT_REALITY_MAP)"* (tr1:597-599); TR-1 §6's K-B branch (the
map could fail to make ANY admissible realization self-adjoint) is the
radical version and stays open.

**Per-row deliverable (Γ-ODD ∧ KREIN-BREAKING), exhaustive:**

| row | Γ-ODD | KREIN-BREAKING |
|---|---|---|
| Λ¹, Λ³, Λ⁵, Λ⁹, Λ¹¹, Λ¹³ (each) | **YES** — exact, residual ≡ 0, both ambient operators | **CONDITIONAL-ON (C-DS1-REALITY ∧ C-DS1-COMPONENT):** on the self-adjoint branch, breaking ⇔ the picked direction has a nonzero t-odd component (stock nonempty, census §4.2); the branch itself is SN-1-gated; a purely t-even pick PRESERVES (σ₁-type, no exit, no charge — canon's doorway unopened) |
| Λ⁷_+, Λ⁷_− (each) | **YES** — same | **CONDITIONAL-ON (C-DS1-REALITY only):** the component condition is FREE for irrep directions — §4.2(3): every duality-half direction has a nonzero anticommutator AND commutator with K, so on the self-adjoint branch EVERY `Λ⁷_±` direction breaks (and on the anti-branch also breaks, via its commuting part). The middle rows are the only rows whose Krein exit is component-unconditional — but the branch assignment, hence the verdict's very definedness, remains SN-1's |

**One more exact free fact rides with the exit.** Because the Γ-residual is
zero for the whole class (§4.1), any class-2 exit that does occur is
automatically the KREIN-ONLY exit — GB-1's three-way iff (gb1:283-289) is
satisfied on the exiting branch with no further condition. The arrow's
"σ₂-type" demand decomposes cleanly: Γ-oddness = class arithmetic (free);
pole condition = same fact (free); Krein exit = reality-map-gated
(the gate); kinetic-symbol leg = §5.3 (a codimension condition, free per
row).

---

## 5. (c) The gapping leg — TB-4's price, and where O2 bites

TB-4, verbatim (tr1:558-566): *"'The count at the decoupled/massive
endpoint' is defined only if that endpoint is exhibited as gapped/Fredholm.
At one selective insertion it is NOT: the induced form has rank ≤ 128 on
the 896 of each one-form corner, leaving ≥ 768 directions unpaired
(rsc1:444, quoted) — one middle-form insertion cannot gap a corner. A
gapped endpoint therefore needs multiple insertions (the class-2 count must
be ODD — st1:456) or class-0/blind supplements, and every such supplement
re-raises the selectivity problem it was brought in to solve."* RSC-1's O2,
verbatim (rsc1:584): *"The whole multiplicity-1 channel is the trace ×
traceless cross block, so its bilinear form is off-diagonal, `rank ≤ 128`
on the 896, reaching **at most 64 of the 128** for EVERY direction."*

### 5.1 Where the shape bound bites (exact census over the shelf)

O2's mechanism is the block SHAPE: a form whose only nonzero invariant
content is the 64×832 cross block has rank ≤ 2·64 = 128 (rsc1:441-446).
That shape holds for exactly the rows whose Grassmann-live square at the
one-form corner is PURE CROSS in §3.1's table:

- **BITES — Λ⁷_∓ (at ζ_±), Λ³, and Λ¹¹ (≅ Λ³):** form rank ≤ 128 on the
  896; ≥ 768 of the RS-1 module `R^(±)` unpaired; at most 64 of the 128
  remainder reached. One such insertion CANNOT gap a one-form corner —
  RW-1's W2 (*"gapped bulk on both sides"*, rw1:270-271) fails at one
  insertion on this stock, and TB-4's supplement price is incurred (odd
  class-2 count, st1:456 [R]; or class-0/blind supplements — which for W3
  purposes need not be selective, see §5.4). The Λ³/Λ¹¹ extension of the
  bite is a new composition of RSC-1's own table entries (rsc1:395: Λ³'s
  Λ² content is cross-only), stated here and probe-checked against the
  banked numbers; the middle-form case is RSC-1's own O2 [R].
- **Additionally, the middle rows are nilpotent at operator grade:** a
  duality-half direction acts one-way (tr1:392-408: *"A SINGLE middle-form
  direction is therefore a nilpotent one-way map between the halves — not
  a two-sided mass"*), `M² = 0` exactly — verified on the instrument
  (`c(ω)·P_+` squares to zero, rank 64, probe LEG 3). No operator gap
  exists either until the adjoint supplies the return arrow — and the
  adjoint is again SN-1's object. The Krein gate and the gapping price
  MEET at the middle rows: both wait on the same map.
- **DOES NOT BITE — Λ¹, Λ⁵, Λ⁹ (≅ Λ⁵), Λ¹³ (≅ Λ¹):** their Grassmann-live
  squares live in ALL THREE blocks (trace, cross, RS-1 — rsc1:394, :396),
  so the off-diagonal shape argument does not apply and no 128 ceiling
  follows from O2. Banked positive stock: LD-B's Λ¹ stratum — insertion
  rank on the half-spinor 64 generic, 32 on the null cone (ldb:240-249
  [R]) — i.e. a generic non-null Λ¹ component is INVERTIBLE on the spinor
  factor; and every Clifford MONOMIAL of any grade is invertible on Δ
  (square ±1 — §5.2), so full-operator-rank directions exist in every row.
  What is NOT banked anywhere: the invariant-FORM rank census for
  Λ¹/Λ⁵-family insertions on the 896 (the diagonal blocks' contribution).
  Typed OPEN as C-DS1-GAP(blind): the natural successor computation, same
  instruments as RSC-1.

### 5.2 The signature-true square refinement (new, exact)

The gapping leg of a WALL profile also needs the fiber direction to square
positively on its block (the JR transverse problem's `m²`). With
signature-true generators (timelike squares −1), a grade-k monomial obeys

```
    c(ω)² = (−1)^(k(k−1)/2 + t) · I    exactly (verified both horns);
    on the σ₂-stock (t odd):  c(ω)² = +1  ⇔  k ≡ 3 (mod 4).
```

So the K-anticommuting stock **squares to +1 exactly at grades k ∈ {3, 7,
11} — the middle grade included — and to −1 at k ∈ {1, 5, 9, 13}**. Read
with §5.1: the grades whose σ₂-components gap-compatibly square (+1) are
exactly the grades TB-4/O2 price hardest (pure-cross rows), and the grades
that escape O2 (Λ¹/Λ⁵ family) carry σ₂-components of NEGATIVE square —
whose self-adjoint realization would need the `i`-dressing that only the
unsupplied real structure can grant or refuse. The two legs brace each
other: **no row gets both legs free.** (Λ¹'s special case, stated for
concreteness: its σ₂-type components are exactly the timelike vectors —
K-breaking ⇔ timelike at k = 1 — and a timelike mass direction squares to
−1; its spacelike components square to +1 but K-commute, the σ₁-type. The
Λ¹ trade-off is the sharpest instance of the brace.)

### 5.3 The kinetic-symbol leg (GB-1's third anticommutation, priced free)

Link D's phrase *"anticommuting with BOTH the kinetic symbol and the
ambient grading"* (gb1:400-401) reads, on the carrier, as anticommutation
with the TRANSVERSE symbol `c(e_n)` of the wall normal (the model's σ₁ is
its one transverse symbol; a codimension-1 wall consumes one). Exact rule
(verified): a monomial anticommutes with `c(e_n)` iff `n ∉ ω`; components
avoiding any fixed normal exist in every grade (`C(13, k) > 0` for k ≤ 13).
No full-symbol reading is available on the carrier — no `Λ^k` with
`0 < k < 14` anticommutes with ALL fourteen `c(e_a)` (the two-sided sign
demand is contradictory; only Λ¹⁴ does, and it is class-0/Γ-even, TR-1
§4.3's trace-mover) — so the transverse reading is also the ONLY reading
under which Link D's kinetic clause is satisfiable, recorded as a typed
sharpening of the arrow's wording, not a deviation from it.

### 5.4 The exemplars (named, certificate at commutation grade)

Both horns, the middle grade: `ω = e4 e5 e6 e7 e8 e9 e10` — six spacelike
internal and ONE timelike internal index (t = 1), a component of the
`(0,7)` Lorentz-singlet split of the middle grade (all-internal, so it is
Spin(4)-inert and contains no 4d index). Certificate, each clause an exact
matrix identity in the probe: Γ_amb-odd ✓; K-anticommuting at both horns ✓
(t = 1); signature-true square +1 ✓ (k = 7 ≡ 3 mod 4); anticommutes with
every base-normal transverse symbol ✓ (no 4d index). The grade-3 companion
`ω = e4 e5 e10` carries the same four clauses. What the certificate is NOT:
a Krein-breaking certificate (the branch is SN-1's, §4.3), a gap
certificate (the corner FORM price of §5.1 stands; a monomial is not an
irrep direction, and the duality-half projection of the k = 7 exemplar
re-enters the mixed-parity, nilpotent regime), or any statement that the
VEV picks it (Lane 1's). It is the existence witness the register's kill
clause asked about.

---

## 6. (d) The freeze seam — TR-1 × GB-1 composed, and no tension found

**The worry the mount names:** a class-2 direction anticommutes with the
grading, so along any such family TR-1's freeze holds — *"`tr(Γ P_<0) = 0`
— not conserved: **identically zero**"* (tr1:416-424) — is paying W3 with
class-2 stock self-defeating? **Composed answer: the freeze is the
mechanism, not an obstruction.** Typed in four exact steps:

1. **Operator sums of class-2 terms stay class-2.** `D_0` Γ-odd plus any
   sum `Σ m_i c(ω_i)` with all grades odd is Γ-odd (linearity of the
   anticommutator) — so a class-2-paid wall operator sits in the Γ-odd
   class at every point of its family, INCLUDING multi-insertion gapping
   packages assembled under TB-4's price. (Distinct from
   TR1-COMPOSITE-PARITY's flank: *"two class-2 insertions compose to class
   0"* concerns PRODUCTS — insertion counts inside pairing invariants
   (st1:456's odd-count rule) — not operator sums. The two statements are
   both true and about different compositions; blurring them is exactly
   the un-composed-adjacency failure class, so the distinction is typed
   here and probe-checked on matrices: the SUM of two odd-grade monomials
   anticommutes with Γ_amb; their PRODUCT commutes.)
2. **Along the whole family the interior trace is frozen** (TR-1 §4.1) —
   so the LD-A drift face is EXCLUDED on the paid route, and TB-1 is
   satisfied per se rather than threatened: *"A ±1 booked there is
   inadmissible per se. The only admissible ambient count is the KERNEL
   INDEX"* (tr1:536-541).
3. **The kernel-index face is exactly the wall's hosting face.** TR-1
   §4.2: *"For class-2 families the count is a kernel index or it is
   nothing, and only external/topological data (flux, winding — RW-1's
   wall, GB-1's direction supply) can make it nonzero"* (tr1:441-444). The
   wall's hosted mode at GB-1's pole IS a kernel element and a genuine Γ
   eigenstate (charge ±1 exact, gb1 §2.2(2-3)); the winding is the
   external datum (CANON.md:135's family, W4's typing question).
4. **Model face, verified on the repository's own lattice fiber** (probe
   LEG 4): `D = σ₁ ⊗ P + m·σ₂ ⊗ I` (the pole operator) is Γ-odd for every
   m; `tr(Γ P_<0) = 0` at machine zero along the whole sweep (the freeze,
   at the pole, in the model that measured the 21.8-unit drift on the
   OTHER mass direction); the σ₃-mass contrast moves the trace ([R] LD-A's
   phenomenon, TR-1's LEG-6 shape re-verified). GB-1's probe (re-run
   green) carries the exact-sympy version: the pole mode is the Γ
   eigenstate of charge −1.

**Seam verdict: CONSISTENT-AND-LOAD-BEARING.** Paying W3 with class-2
stock is not merely compatible with the transport dichotomy — it is the
payment that ROUTES the count onto the only face TR-1 left standing
(kernel index, external data), which is the face RW-1's wall supplies data
for. The two wave-2 results compose into one sentence: *the direction that
makes the wall canon-graded is the direction that makes the interior books
close at zero, and the wall's winding is then the only entry left.*

---

## 7. (e) VERDICT — CONDITIONAL, three typed conditions, named owners

**Not ARROW-PAID:** a payment requires the three-leg certificate per
direction; the Krein leg's predicate is undefined without the reality map
(§4.3) — there is nothing to certify, only to condition.

**Not ARROW-DIES:** the kill clause (*"If the carrier offers no such
direction"*, register:271) fails — the offered stock is nonempty in every
row at both horns, with exemplars named (§5.4) and the pole condition free
(§4.1).

**CONDITIONAL**, with the surviving candidates' blockers typed:

| condition | content | rows blocked | owner |
|---|---|---|---|
| **C-DS1-REALITY** (common) | the global Hodge/Krein/reality adjoint: decides self-adjointness per component, hence BREAKS vs PRESERVES (§4.3), the middle rows' return arrow (§5.1), and the −1-square rows' realizability (§5.2). SOURCE-SILENT (sn1:207-214) | ALL | SN-1's standing gate; jointly GB-1 Link D's holders (SC-CHI-02 register owner + the wave executing RW-1's typed target + external-by-structure owner for the exit typing, gb1:407-414); the real-form fork is SIGNATURE-AMBIENT-adjacent |
| **C-DS1-GAP(cross)** | TB-4's supplement price: one insertion cannot gap a one-form corner (form rank ≤ 128 on 896); need odd class-2 counts or blind supplements (§5.1) | Λ³, Λ⁷_+, Λ⁷_−, Λ¹¹ | TB-4's owner arithmetic (TR-1 §5) + RSC-1 O2; for W3 the supplements need NOT be selective — selection is SG4 bit 2's job and stays OPEN, so TB-4's "re-raises the selectivity problem" clause prices the SELECTOR's route, not this arrow |
| **C-DS1-GAP(blind)** | the invariant-form rank census on the 896 for all-three-block rows: OPEN, uncomputed anywhere | Λ¹, Λ⁵, Λ⁹, Λ¹³ | successor computation, RSC-1's instruments; LD-B's C-composition caveat (ldb:861) applies — it is the same reality map again at the form level |
| **C-DS1-COMPONENT** | which direction inside the module the VEV picks: K-parity is componentwise (§4.2), so the exit is a direction datum — EXCEPT for the middle rows, where every irrep direction breaks on either branch (§4.3) | all except Λ⁷_± | Lane 1 / SG4-bit-2-adjacent (the selector's territory; OPEN by design, CC-06) |

**What the route still owes if this arrow is ever paid (no launder):**
RW-1's W-bill outside W3 — above all **W1, RELOCATED by WB-1 to an
external Z/2 sector datum** (*"VERDICT ON W1: RELOCATED to an external Z/2
sector datum (= winding mod 2 ...), with a stacked gate OPEN: pi_0 of the
NATIVE varpi vacuum set is uncomputed"*, wb1:9), registered as
`W1-PI0-NATIVE-VACUUM` (register:263-269) and owed by the wave-3 π₀ arc
(the sector-datum sibling; see deviations D4/D4' — that sibling LANDED at
DS-1's ship time claiming the W1 kill FIRES at typing grade, un-adjudicated
here; if it stands, this debt row converts from "owed" to "killed, revival
forks named" and the integrator composes);
**W2** both-sides gapping beyond the fiber (TB-4 composition, §5.1); **W4**
external-family membership typing for the winding (rw1:287-293, canon
external-by-structure owner); **W5** multiplicity SUPPLIED, CC-05
subtractive fence (rw1:294-301); **W6** the LA1 shadow, untyped
(rw1:302-307; gb1 §4 — LA1 NOT injective, base-cat:89, inverse a named
non-arrow, :106). The count stays OPEN; the selector stays OPEN; nothing
here derives 3, or 1.

---

## 8. Kill-target declaration (rule of the wave: kills name their claims)

No kill is declared. Had the inventory come back empty, the kill would
have targeted, exactly:

1. **GB-1's W3 residue arrow** — gb1:396-401, verbatim:
   *"**Link D — THE MISSING ARROW (exactly one, typed).**
   **MISSING-ARROW-W3-DIRECTION:** a source-attested or repository-
   constructed zero-order deformation of the Dirac-type operator whose
   fiber direction carries a nonzero `sigma_2`-type component — Γ-odd and
   Krein-breaking, i.e. anticommuting with BOTH the kinetic symbol and the
   ambient grading — on the physical carrier."* — and through it,
2. **the canon-charged hosting leg of RW-1's typed target** — rw1:824-826,
   verbatim: *"W1 and W3 are the two items on which it can still die, and
   finding either unsuppliable would be a first-class result of the same
   rank as this join."* (W3's own statement at rw1:276-286 quoted in GB-1's
   target_claim and re-pinned in the probe.)

Neither is a source row (both are repository bill items on a construction
target); no registered source claim was in scope for killing. The measured
answer runs the other way: suppliable-in-class, certifiable-by-no-one until
the reality map exists.

---

## 9. Canonical-currency check records (for the integrator; DS-1 edits no sidecar)

`process_gates/canonical_currency_audit.py` run 2026-08-17 at work start
(corpus 3652 files; registry 10 corrections; sidecar 40 recorded checks;
TOTAL dirty 183 (file, correction) pairs; warn-only by design; exit 0).
Checked by exact-substring search over the full audit output: **exactly one
of DS-1's consumed inputs appears in the dirty queue.** Per-input records:

1. `lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md`
   — **DIRTY-UNCHECKED under CC-08** at this run.
   - vs **CC-08** (the 128 is a partner-placement obligation, not a
     defect): DS-1 consumes the file ONLY for the eq (9.16) display
     structure (the `ρ(ε)` wrap and ϖ cells, :68-:74, :82-:99) as the
     source-print grounding of the SLOT (§3.2); the 128 appears in DS-1
     exclusively through TB-4/O2's rank price with RSC-1's
     obligation-framing kept (§0, §5.1); no defect/surplus framing is
     imported from or into the extraction. **CLEARED-CONSISTENT**
     (pointers: s9 extraction :68-:99; this artifact §3.2, §5.1, §0
     CC-08 quote).
2. `lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md` — the
   mount flagged it as sitting in dirty queues; **measured at run time it
   is in NO dirty queue** (its CC-06/CC-08 pairs were cleared by RW-1's
   integrator, sidecar records `canonical-currency-checks.yaml:393-411`;
   TR-1 §11 records the same). Following TR-1's precedent, DS-1 records
   its own verdicts anyway:
   - vs **CC-06**: the consumed content IS the correction's upstream — the
     §11.6 hedge (extraction:135-142) is quoted in §0 and every chirality
     statement here is VEV-conditional with SG4 bit 2 OPEN; the shop
     supplies capability, never the switch. **CLEARED-CONSISTENT**
     (pointers: extraction:137-138; this artifact §0, §3.2, §7).
   - vs **CC-08**: the 128 enters only as the priced obligation (§5.1).
     **CLEARED-CONSISTENT.**
3. All other consumed inputs — GB-1 + probe, TR-1 + probe, RSC-1, RW-1,
   WB-1, LD-B, SN-1, ST-1, both function-space instruments,
   `upgrade-program-register.yaml`, `correction-registry.yaml`,
   `source-claim-register.yaml`, `homonym-register.yaml`, `CANON.md`,
   `gu-base-categories.md`, `VERIFICATION.md` — **absent from every dirty
   queue at this run** (grep over the audit output; all either post-date
   every `canonical_since` or sit in no signature). **CLEAN-BY-AUDIT.**
   No STALE-FOUND anywhere. DS-1 records these here and edits no sidecar.

Operative correction sentences relied on are quoted verbatim in §0 (CC-05
registry:263-269; CC-06 registry:299-306; CC-08 registry:363-368).

---

## 10. Hostile review, inline — the strongest attacks

**"The σ₂-type direction was never in doubt as algebra — any Clifford
algebra has anticommuting elements. You have shopped a triviality and
dressed the unsurprising nonemptiness as a result."** Three answers, each
measured. (i) The nonemptiness was NOT free at the stated gate: the
register's kill clause turns on the CARRIER's offering — and the carrier
came within one parity of refusing: had K-parity been grade-homogeneous
with the commuting sign (as it IS for the even-q horns' commuting-K
structure, ldb:194-200), the entire Γ-odd stock would have been σ₁-type
and W3 would have died here. The measured content is WHICH parity rules
(timelike count), that it is componentwise, and its censuses. (ii) The
free-pole fact (§4.1) is a genuine reduction of GB-1's arrow: the residual
GB-1 had to measure angle-by-angle is identically zero for the whole
inventory — the arrow's angle condition vanishes as a separate demand.
(iii) The two new braces run AGAINST the comfortable ending: the square
sign locks the O2-priced grades to the gap-compatible σ₂-stock (§5.2), and
the reality gate makes certification impossible today. A triviality-shop
would have produced ARROW-PAID; this one cannot.

**"Your Krein leg outputs no verdict — 'conditional on the reality map' is
a refusal to compute."** The refusal is the theorem. The predicate
BREAKS/PRESERVES consumes the adjoint (§4.3, two-branch table exact); any
unconditional verdict emitted today would be exactly the object SN-1's
gate exists to block, and the flip under the branch swap is machine-
verified, not rhetorical. What CAN be computed unconditionally was, all of
it: parities, censuses, flips, squares, exemplars, both horns.

**"The exemplar `e4..e10` is a monomial, not an irrep direction — the
physical VEV lives in `Λ⁷_±`, and you concede the projection is nilpotent
and parity-mixed. The exemplar certifies nothing physical."** Conceded and
typed in place (§5.4 lists exactly this). The exemplar answers the
register's existence clause at the algebra layer where the clause is
decidable today. The irrep-direction statement that survives projection is
§4.3's: every `Λ⁷_±` direction breaks on either branch — stronger, not
weaker, on the exit question; what the projection costs is the operator
gap (nilpotency), which is priced, not hidden.

**"You extended O2 to Λ³/Λ¹¹ without recomputing anything — a shape
argument on quoted multiplicities."** Correct, and typed as exactly that
(§5.1: "a new composition of RSC-1's own table entries"). The composition
consumes three banked numbers (Λ³'s Λ² content: trace 0, cross 1, RS-1
block 0 — rsc1:395) and O2's own mechanism sentence (rsc1:441-446). The
probe pins all three; a re-runner who distrusts the composition recomputes
one Klimyk row, not a new instrument.

**"The ϖ annex could contain the arrow and you refused to shop it."** The
register title scoped the shop (§3.3, D2); the annex's Γ-oddness is
TB-0-contingent (it flips between the two ambient operators), so shopping
it before a grading declaration would manufacture exactly the ambiguity
TB-0 exists to forbid. It is typed, owned (ST-1's Ω¹ column), and left.

---

## 11. Machine-readable verdicts and certificate

<!-- DS1-TABLE-BEGIN -->

| id | question | verdict | evidence_key |
|---|---|---|---|
| ENUM | the class-2 inventory, enumerated completely | EIGHT-ROWS-SEVEN-GRADES-ALL-REPO-DERIVED-AT-DIRECTION-GRADE | inventory_pins |
| POLE | the Γ-oddness leg and GB-1's angle condition | RESIDUAL-IDENTICALLY-ZERO-WHOLE-CLASS-AT-KREIN-ONLY-POLE | gamma_residual_zero |
| KPAR | the K-commutation layer | TIMELIKE-PARITY-COMPONENTWISE-EVERY-ROW-MIXED-STOCK-NONEMPTY | kparity_census |
| KREIN | the Krein-breaking leg per candidate | CONDITIONAL-UNDEFINED-WITHOUT-REALITY-MAP | sn1_gate_and_branch_table |
| GAP | the gapping leg per candidate | TB4-O2-BITE-PURE-CROSS-ROWS-ONLY-SQUARES-PLUS-AT-3-MOD-4 | gap_price_and_squares |
| SEAM | class-2 payment vs the transport dichotomy | CONSISTENT-FREEZE-ROUTES-TO-KERNEL-INDEX-FACE | freeze_seam_model |
| SHOP | the register item's fork | CONDITIONAL-NOT-PAID-NOT-DEAD-NO-KILL | condition_table |

<!-- DS1-TABLE-END -->

**Verdict-evidence binding (probe LEG 7 enforces; the selftest's contrary
control flips SHOP to ARROW-PAID in a temp copy and must be caught):**

| evidence_key | asserted direction | measured |
|---|---|---|
| inventory_pins | ENUM requires the register title, TR-1 §3.2 row, RSC-1 block entries and the middle-form source-absence all pinned | pins green; "middle form" 0 hits in lab/sources (planted positive fires) |
| gamma_residual_zero | POLE requires {c(ω), Γ_amb} = 0 for sampled components of every grade, and the sum-stays-odd fact | exact zeros, every grade; sum odd, product even |
| kparity_census | KPAR requires the t-parity rule at both horns, censuses matching binomials, the volume-word flip, and duality-half mixedness | all exact; censuses match; flip verified; both brackets nonzero for half-projected middle directions |
| sn1_gate_and_branch_table | KREIN requires sn1:207-209/:214 pinned and the branch-flip fact (same component, opposite verdicts under M† = ±M) | pinned; flip verified as algebra on witnesses |
| gap_price_and_squares | GAP requires TB-4/O2 pins, the pure-cross row identification against RSC-1's printed entries, nilpotency + rank 64 of a half-projected middle direction, and the square table (−1)^(k(k−1)/2+t) | pinned; M² = 0 and rank 64 exact; squares match closed form at both horns, all grades |
| freeze_seam_model | SEAM requires the lattice pole family Γ-odd with trace 0 at machine zero and the σ₃ contrast moving | max |trace| < 1e-9 along sweep; contrast moves > 0.5; both instruments and TR-1's probe re-run green with pinned strings; GB-1's probe pinned at its exact post-landing state (D5) |
| condition_table | SHOP requires the CONDITIONAL verdict with all four condition rows present and the no-launder list naming W1/W2/W4/W5/W6 | present; claim-ceiling strings pinned |

**Certificate.**
`tests/channel-swings/joe_directed_ds1_the_stock_sits_at_the_pole_and_waits_on_the_reality_map.py`

- Live run: **107/107 checks, exit 0** (~16 s). LEG 1 byte pins (34, with a
  planted negative); LEG 2 exact class/census arithmetic (binomial
  identities; planted-false propositions observed False); LEG 3 the Cl(14)
  instrument (Clifford relations; TR-1 parity facts re-verified [R]; the
  t-parity rule swept over every odd grade at both horns with per-grade
  census cross-checks, exhaustive at grade 1; volume-word flip; duality-half
  mixedness; signature-true squares vs the closed form; transverse rule;
  both exemplar certificates at both horns; middle-half nilpotency and rank
  64; the branch-flip witness; a planted Krein-BREAKING control the gate
  must flag and a planted PRESERVING control it must pass — the mount's two
  required plants); LEG 4 the freeze-seam model face (lattice pole family
  frozen at machine zero; σ₃ contrast moves; sum-vs-product Γ-parity);
  LEG 5 four subprocess re-runs with pinned agreement strings (both
  function-space instruments green; TR-1's probe "90/90 checks pass";
  GB-1's probe pinned at its exact post-landing state
  `checks: 114  failures: 1 ['L5-dest-artifact']` — instrument legs green,
  the one red is DS-1's own landing collision, deviation D5); LEG 6
  certified absences (the middle-form source-absence and the novelty
  phrasings, each with a planted positive and a planted near-miss;
  wave-tree hits reported-not-failed per GB-1's precedent; the vg_v8_t5:493
  prior-art line REQUIRED-PRESENT); LEG 7 artifact binding (SHA-pinned
  verdict table, closed verdict vocabulary, claim-ceiling strings, the
  register-title quote verbatim, both kill-target quotes present).
- `--selftest`: clean baseline verified FIRST (count pinned independently)
  and a red baseline aborts; 10 mutations, every one corrupting machinery
  or a reference (a pin's line number; the census binomial; the beta_S
  index set; the square-sign closed form; a Kronecker factor of the gamma
  construction; the exemplar's index set; the absence-scan token list; a
  subprocess pin string; the freeze family's mass matrix; the
  artifact-copy contrary control flipping CONDITIONAL to ARROW-PAID), each
  REQUIRED to be caught by its targeted check with a genuine [FAIL]; a
  crash is CRASH-NOT-DETECTION and fails; an untargeted catch is
  INCIDENTAL-NOT-TARGETED and fails; the failing checks are printed for
  every mutation; baseline re-verified after; exit 0 iff green.
  Tolerances: none load-bearing — every comparison is on exact-integer-
  valued matrices or exact binomials (norm thresholds sit at 1e-9 against
  integer-scale violations of size 2), so no tolerance can absorb a plant.
- Read-only outside the temp directory; deterministic; numpy + sympy +
  stdlib.
- What the probe does NOT certify: any statement about the true RS-1
  bundle over Y¹⁴ or the (9,5) real/quaternionic form (no reality map, no
  real structure — the gate is the point); any invariant-FORM rank beyond
  the quoted banked bounds (C-DS1-GAP(blind) is OPEN); the ϖ annex; any
  file phrasing these results in tokens outside the scanned families; and
  LD-B's numerical sweep itself (consumed at its recorded grade).

---

## 12. Claim ceiling, and what this artifact is not

- **No claim status moves. No canon row moves. No register, registry,
  sidecar, or ledger edit.** The generation count stays OPEN; SG4 stays
  the decider; bit 2 stays the selector, OPEN (CC-06); the wall route
  still rides SG4 under its bill.
- **A found direction is a capability, not a closure.** Chirality remains
  VEV-conditional (SC-CHI-01, extraction:137-138); nothing here turns any
  insertion ON, selects a half, or reads a count.
- **The comparator binds its models.** The lattice/sympy legs bind the
  `dirac_spectral_flow_section.py` class; the Cl(14) legs are fiber-algebra
  facts (commutation, squares), not spectra of any GU operator; no GU row
  is advanced or retarded, including by the negative clauses.
- **No kill.** §8 names what a kill would have targeted; the branch closed
  without firing.
- **The 2+1 stays subtractive; the 128 stays an obligation; the ϖ hedge
  stays ϖ's** (CC-05, CC-08, §0; the ε/ϖ slot mismatch stays the register
  owner's flagged question, tr1 §3.2 [R]).
- **W6 is not advanced; W1's relocation is not absorbed.** The no-launder
  list (§7) is a debt schedule, not a down payment.

---

## 13. POSTFLIGHT — the seven lenses, after the work

- **P1 — Did any lens change a verdict?** Yes, two. L4 (claim-targeting)
  rewrote the enumeration's source-status column before any computation:
  the mount's "which the source names — the middle forms" failed its grep,
  and the whole shelf is typed REPO-AVAILABLE-ONLY at direction grade (D1)
  — which in turn made GB-1's "or repository-constructed" disjunct
  load-bearing. L1 (Krein specialist) demoted the planned headline: the
  first draft aimed at "the middle forms break Krein" as a positive
  finding; the branch-flip computation showed the sentence is undefined
  without the reality map, and the headline moved to the gate itself.
- **P2 — Is any verdict resting on a single receipt?** KPAR rests on sign
  arithmetic PLUS the matrix instrument (two derivations, one
  implementation each); its thinnest external dependency is LD-B's
  identification of `beta_S` as the spacelike product (ldb:188, :853) —
  if LD-B's convention were ever flipped (timelike product), the t-parity
  rule re-derives with `t ↔ j` and every census transposes; the MIXEDNESS
  and nonemptiness verdicts survive the flip (both parities are nonempty
  in every row), so SHOP's conditional is convention-robust. GAP's
  pure-cross extension rests on three quoted RSC-1 numbers (pinned).
- **P3 — What did NOT move?** SG4; bit 2 (OPEN); the count (OPEN); GB-1's
  eight verdicts; TR-1's five; RSC-1's O1-O4/C1-C3; RW-1's bill; WB-1's
  relocation; LD-B's cards; SN-1's gate (consumed, not weakened); every
  canon row.
- **P4 — Where would a hostile re-runner disagree?** (i) The reading of
  Link D's "kinetic symbol" as the transverse symbol (§5.3) — defended by
  the unsatisfiability of the full-symbol reading, but a re-runner could
  demand GB-1's holders ratify the sharpening; (ii) the exemplars' worth,
  given the irrep-projection concession (§10 third attack); (iii) whether
  the ϖ annex should have been shopped under Γ_slot despite the register's
  scoping (§3.3) — the artifact's answer is TB-0, but the annex is the
  natural successor if the integrator declares the grading.
- **P5 — Regression check against the standing corrected facts.** Layers
  named throughout (L1 slot availability vs L4 conditional chirality;
  §3.2, §7); contraction-not-KK untouched (no mode split; W6 handed off at
  LA1 exactly as GB-1 left it); no-GUT untouched; 2+1 subtractive quoted
  and obeyed (no additive three); the 128 an obligation (§0, §5.1);
  chirality VEV-conditional with the selector OPEN (§0, §7, §12);
  "Krein"/"σ₂-type" fenced as repository vocabulary (routing block).
- **P6 — Instrument honesty.** The planted negative (pins), the planted
  breaking/preserving pair (the mount's required plants), the absence-scan
  planted positives and near-misses, and the prior-art REQUIRED-PRESENT
  pin all fire correctly on the live run; the selftest's 10 mutations are
  each caught by their targeted checks with the clean baseline verified
  first and re-verified after; every load-bearing comparison is
  integer-exact so no tolerance exists to absorb a plant.
- **P7 — Seam audit (L5's own postflight).** The freeze seam is typed with
  the sum/product distinction (§6.1) and probe-checked; the TB-4
  selectivity clause is explicitly re-scoped for W3 (§7 C-DS1-GAP(cross)
  row: the arrow needs no selectivity, so the supplement price is a price,
  not a regress); no other wave-2 pair required composition.

---

## Deviations from the mount, recorded

- **D1.** The mount's task (a) parenthetical *"(which the source names —
  the middle forms —)"* is not a source fact: "middle form"/"middle-form"
  scores ZERO over `lab/sources/*.md` and the s11-s12 extraction carries
  no Λ⁷ token (certified with a planted positive, probe LEG 6). The
  middle-form DIRECTIONS are repository-derived (ST-1 §4.5's theorem on
  the source's declared slot); the slot is source-print (eq (9.16)).
  Every inventory row is typed SLOT-SOURCE-PRINT /
  DIRECTION-REPO-DERIVED (§3.2), and the arrow survives on GB-1's own
  "or repository-constructed" disjunct (gb1:397-398).
- **D2.** The register title's inventory "(odd-k epsilon directions)"
  differs from TR-1 §8.3's "(odd-k ε directions; even-k ϖ content)". DS-1
  shops the register's named inventory exhaustively and types the ϖ annex
  as TB-0-contingent, owned by ST-1's Ω¹ column, unshopped (§3.3).
- **D3.** The mount flags the s11-s12 extraction as sitting in dirty
  queues; measured at run time it is in no dirty queue (cleared 2026-08-17
  by RW-1's integrator, sidecar :393-411). DS-1 recorded its own verdicts
  anyway (§9), TR-1's precedent.
- **D4 (superseded mid-run by D4').** At write time no wave-3 artifact
  named PV-1 existed; the only `pv1-*` file was
  `photon-extra-vector-spectrum/pv1-available-orbits-retain-an-extra-massless-vector-2026-08-14.md`
  — an UNRELATED prior arc (extra massless vector, 2026-08-14; the name
  collision stands and future consumers should cite by path). The sector
  datum itself was always unambiguous: WB-1's relocated W1, registered as
  `W1-PI0-NATIVE-VACUUM` (register:263-269).
- **D4' (measured at ship time — the destination re-check).** The wave-3
  sibling LANDED while DS-1 ran:
  `lab/active-research/joe-directed/wall-bill/pv1-pi0-trivial-in-every-live-typing-w1-kill-fires-2026-08-17.md`
  (work_item PV-1), whose title states verbatim: *"pi_0 of the native
  varpi vacuum set is TRIVIAL in every live typing, and the W1 kill branch
  FIRES ... Verdict: KILLED-AT-W1 at typing grade; the VEV-orientation Z/2
  sector datum does not exist as a nontrivial choice in any grounded
  typing; the revival forks are named"* (pv1:9). DS-1 does NOT adjudicate
  or consume that result (a concurrent landing outside its input list);
  the composition is the integrator's, and §7's no-launder schedule
  carries the pointer. Two content notes for the integrator, from DS-1's
  own instruments: (i) PV-1's en-route clause that the ambient quadratic
  is identically zero on a single middle-form half is CONSISTENT with the
  odd-q nullity structure DS-1's Krein leg rides on (LD-B's parity, §4.2);
  (ii) if PV-1's W1 kill stands under integration, the VEV-orientation
  wall target dies at W1 REGARDLESS of W3's arrow — DS-1's inventory
  verdict (the stock, its pole membership, its gates) is unchanged in
  content, but its named consumer route would be dead at a different bill
  item, and the arrow's remaining live consumers would be whatever wall
  realization survives PV-1's named revival forks.
- **D5.** DS-1's mount-required artifact path (`grading-bridge/ds1-...`)
  trips GB-1's probe check `L5-dest-artifact` ("grading-bridge/ holds
  exactly the GB-1 artifact — no duplicate seed"): at HEAD the GB-1 probe
  now reports `checks: 114  failures: 1 ['L5-dest-artifact']`, all
  instrument legs green. DS-1 cannot refresh the pin (no edits to existing
  files); its own probe pins the exact post-landing state so any FURTHER
  movement in GB-1's probe fails loudly, and the refresh is recorded as
  owed to GB-1's owner / the integrator (routing-note block above). While
  DS-1 ran, a concurrent wave-3 sibling landed
  (`spectral-transport/mp1-composites-inherit-one-horn-never-both-2026-08-17.md`
  — the TR1-COMPOSITE-PARITY arc); GB-1's own scan reports it SAME-WAVE,
  not failed — the shared-checkout concurrency the workspace rules warn
  about, observed live.
- No other deviations: two files written, no git operations, no existing
  file touched, pre-existing gate reds (typed-carrier `selected-k151`;
  k149 certificate-shape) left as other lanes' debts.

---

## The blunt paragraph: which direction the pressure ran

The register wrote this arc a hero's part: find the σ₂ direction, pay
GB-1's arrow, hand the wall route its last missing piece — and the algebra
looked eager to cooperate, because the Γ-odd stock turned out not merely
nonempty but sitting wholesale at the exact pole GB-1 said the charge
needs, with named exemplars carrying every commutation-grade clause at
both horns. The honest report is that the shop still could not close the
sale, and the reason is structural, not a shortfall of effort: the one
predicate the arrow is named for — BREAKS the Krein structure — is a
statement about an adjoint the source never supplies, and the same
computed component flips verdict under the two self-adjointness
assignments, so any unconditional "Krein-breaking direction found" written
today would be exactly the laundered object SN-1's gate was built to
catch. I typed the strongest true thing instead: the carrier offers the
class (kill branch closed), the pole is free, the exit — if the reality
map ever grants one — is automatically Krein-only, the freeze seam
composes into the kernel-index face rather than against it, and the whole
residue is three owned conditions, the deepest of which (the reality map)
is the same single object now gating this arrow, the middle rows' return
arrow, the blind rows' form ranks, and TR-1's missing theorem all at once.
The two places a hostile re-runner should press first are the
transverse-symbol reading of Link D's kinetic clause and the unshopped ϖ
annex; the computation I would run next is the blind-row form-rank census,
because it is the only gate on the schedule that needs no new physics —
just RSC-1's instruments pointed at the diagonal blocks. One more thing
the destination re-check surfaced at ship time and I refuse to smooth
over: the concurrent π₀ arc landed claiming W1's kill fires at typing
grade (D4'), which — if it stands — means the wall route this arrow was
priced for may already be dead one bill item upstream, and the honest
reading of DS-1 then shifts from "the supply half of a live construction"
to "the supply half of whichever realization survives PV-1's revival
forks"; the inventory verdict itself does not move either way.

## Reproduce

```
_local/cas-venv/bin/python tests/channel-swings/joe_directed_ds1_the_stock_sits_at_the_pole_and_waits_on_the_reality_map.py
_local/cas-venv/bin/python tests/channel-swings/joe_directed_ds1_the_stock_sits_at_the_pole_and_waits_on_the_reality_map.py --selftest
```

Expected: `107/107 checks pass`, exit 0 (~16 s); selftest: clean baseline
verified first (107, pinned), then `10/10 mutations caught`, each via its
targeted genuine `[FAIL]` line, 0 crash-only, 0 missed, baseline
re-verified, exit 0 (~36 s).
