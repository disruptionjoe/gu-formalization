---
artifact_type: integration_review
status: exploration
doc_type: conditional_build_wave_reprioritization
created: 2026-08-16
work_item: CB-H210-WAVE
channel: high_energy_two_plus_one_prediction
target_claim: SC-GEN-53
title: "H210 conditional wave: the exact K77 imposter port reaches the off-diagonal zero-order fermion cells; typed observation is next"
grade: "ROOT SYNTHESIS after inline divergent preflight, source-ledger archaeology, two exact multi-lens execution packets, and an independent fourteen-lens hostile review. CB-1 passes 61 exact checks after the raised-contraction convention repair; CB-2 passes 22/22 plus four adverse controls. CONDITIONAL on H210, a nonzero family covector, and the inherited opposite-half density-dual bridge. No action, selector, external datum, physical quotient, mass, family identity, scale, threshold, or observable is derived."
disposition: H210_REAL_K77_RS_PORT_AND_OFFDIAGONAL_ZERO_ORDER_COMPOSITION_PASS__FIXED_HQ_IS_ADVERSE_SUBHORN__FULL_DERIVATIVE_CELL_OBSERVATION_AND_QUOTIENT_TYPE_MISSING__TYPED_OBSERVATION_NEXT
canon_verdict_change: none
steering_effect: "On the next Go, run CB-3: carry only the off-diagonal zero-order H210 port through the owned observation maps, with fixed trace-Hq as an adverse subhorn. Keep source-action construction and external selector work off limits."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb1-h210-k77-rs-intertwiner-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb2-h210-equation916-cross-half-composition-2026-08-16.md
  - lab/process/hostile-reviews/2026-08-16-joe-directed-cb1-cb2-h210-composition-review.md
scripts:
  - tests/channel-swings/joe_directed_cb1_h210_k77_rs_intertwiner_probe.py
  - tests/channel-swings/joe_directed_cb2_h210_equation916_composition_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This is a source-native
> conditional composition expressed partly in complex D5/Pati--Salam bridge
> coordinates. Ordinary family indices, net-chirality tests, scalar-Higgs
> VEVs, and conventional SO(10) mass mechanisms do not adjudicate Weinstein's
> imposter/emergent-chirality proposal without a typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# H210 conditional wave — integrated result

## Outcome

The preferred conditional `H210` route passed its first two downstream gates.

1. The unique Pati--Salam `210` orientation line has an exact real-K77,
   gamma-traceless vector-spinor port.
2. That port has the ambient-half parity required by the off-diagonal
   zero-order `varpi_-+ / varpi_+-` summands in the released equation-9.16
   matrix, under the explicitly inherited opposite-half density-dual bridge.
3. One declared nonzero family covector couples one unlabelled family
   direction to the imposter sector and leaves a two-dimensional family
   multiplicity kernel before observation.

This is the first source-shaped composition in the path that simultaneously
has an exact K77 tensor, exact Pati--Salam typing, exact equation-9.16 cell
support, and exact family-kernel arithmetic. It remains a conditional
pre-observation statement.

## Conditional-build contract

The horn is:

```text
H210 = assume a compatible nonzero 210 channel and no 54 channel.
```

This wave did not ask what action, vacuum, background, selector, or external
datum turns H210 on. Those routes are off limits in this channel. The wave
asked only whether the assumed horn fits the already-owned geometry.

The mandatory source packet controlled every executor and reviewer:

- Eric Weinstein's `2+1` is representation-theoretic and imposter-shaped,
  not three copies produced by an ordinary family index;
- the imposter is claimed to look the same after low-energy restriction and
  reunify differently higher up;
- the parent carrier remains non-chiral/four-cornered while observed chirality
  is effective; and
- no algebraic kernel may be renamed the third family, a mass spectrum, or an
  observed threshold.

## Exact CB-1 result

For the internal split `N^(6,4)=A6+B4`, define

```text
phi4 = vol(B4),
phi6 = vol(A6).
```

With covariant coframe components and the metric-raised contraction, the
canonical RS projection is

```text
T_a = c(i_(theta^a sharp) phi4) - (4/10) Gamma_a c(phi4).
```

The probe derives rather than plants the contraction coefficient. Relative to
`Gamma_a phi4`, it is `0` on `A6` and `1` on `B4`, hence

```text
A6: -2/5,
B4: +3/5,
gamma trace: 6(-2/5)+4(+3/5)=0.
```

Exact consequences:

- all `15+6=21` Pati--Salam generators satisfy the full free-index
  intertwining identity;
- the real `Cl(7,7)` map exists before choosing `Hq` and has rank `64` on one
  ambient real Weyl half;
- after complex PS factorization it is injective of rank `16` per internal
  Weyl copy;
- the selected normalizer makes the orientation line D odd and gives opposite
  orientation eigenvalues on the two conventional PS blocks;
- every component flips ambient K77 Weyl chirality; and
- the owner tensor is grade `4/6`, while the RS port has shifted grades `3/5`
  or `5/7`.

This kills the earlier loose reading that a pure admitted grade-six blade, or
a fixed one-form times that blade, is already the H210 RS port.

## Exact CB-2 result

The equation-9.16 row reversal is load-bearing. The invariant is a bilinear

```text
16 x 144 -> 1_PS,
```

but its unbarred operator form is

```text
16 -> 144* = 144bar.
```

Therefore the bilinear factors are cross-effective-half while the unbarred
operator arrow stays within one effective package. Under the inherited
opposite-half density-dual bridge, the conditionally compatible zero-order
summands are

```text
(1,2): varpi_-+   nu+ -> zeta-,
(0,3): varpi_+-   nu- -> zeta+,
```

with displayed reverse-shaped partners `(2,1)` and `(3,0)`. The visually
tempting pp/mm ports are wrong-channel controls because `16 x 144bar` and
`16bar x 144` have zero PS invariants.

The full cells also contain `d0`. Their derivative parity remains in the
known Layer-0 collision; the zero-order placement does not repair it.

## Rank and kernel ceiling

For a declared nonzero `r:F=C^3->C`, the internal map is

```text
r tensor T : F tensor 16 -> 144bar.
```

It has rank `16` and kernel `ker(r) tensor 16`, complex dimension `32`. Across
the conjugate pair the forward rank is `32`. A separately declared four-cell
forward/reverse completion has rank `64` and kernel `320` on the counted
`384`-dimensional internal `16/144` D5 sector.

The number `384` is not the full GU fermion carrier, and `320` is not a
physical kernel. External multiplicities, the observation complement,
density/adjoint data, domain, and quotient are not in this finite count.

## Real-form fork

The fixed trace-`Hq` construction is adverse but not source-dispositive:

```text
owner phi4/phi6: admitted;
A6 RS words:      six real phases;
B4 RS words:      one real plus three imaginary phases;
one common phase: impossible.
```

Componentwise phase completion destroys gamma tracelessness. Thus the real
K77 representation map passes, while simultaneous full-PS, gamma-traceless,
fixed-trace-`Hq` connection realization remains `TYPE_MISSING`. The source's
defining Hermitian form is unspecified, so this is an explicit adverse subhorn
rather than a source-level H210 kill.

## Hostile-review repairs integrated

The independent review required four repairs; all are now integrated:

1. CB-2 records CB-1 parity/rank as passed for the zero-order summand.
2. The `384/64/320` count is typed as the reduced internal D5 sector, not the
   full GU carrier.
3. Row-to-output identification is named as the inherited density-dual bridge,
   not source-supplied reality or adjointness.
4. CB-1 and its probe now declare and execute the raised-contraction/coframe
   convention that derives `-2/5,+3/5`.

The full conditional composition survives those repairs.

## Reprioritization

| next path-relative move | priority | fertility | disposition |
|---|---:|---:|---|
| typed observation of off-diagonal H210 zero-order port | 1 | `9/10` | next swing |
| fixed trace-`Hq` realization | 2 | `6/10` | adverse subhorn inside observation packet |
| `HBOTH` proportional/independent rows | 3 | `5/10` | wait until a 54 port is conditionally admitted |
| `H54` quadratic composite | 4 | `3/10` | fallback only; no direct native weld |
| source action / selector / external datum | off limits | not scored | dependency fence only |

Relative to the wider exploration portfolio, H210 remains ahead of SN-1 for
the next swing. If typed observation annihilates every permitted zero-order
block, demote H210 below SN-1 and switch; do not manufacture a selector or
repair the source matrix.

## Next conditional scaffold

On the next `Go`, perform CB-3:

1. reuse the exact CB-1 tensor and only the CB-2 off-diagonal zero-order cells;
2. write the owned observation/pullback maps on the counted internal sector;
3. compute whether each forward and conjugate block survives and at what rank;
4. keep fixed trace-`Hq` as a separate adverse branch;
5. preserve the full `d0+varpi` collision as a fence;
6. state the zero/nonzero H210 comparison without inventing an energy scale;
7. hostile-review whether surviving algebraic rank can be stated before any
   physical quotient; and
8. reprioritize H210 against SN-1 on the exact survival result.

## Plain English

Assuming the H210 channel is present, we now know exactly how it can connect
one combination of the three family-shaped pieces to the imposter-shaped
piece. The connector exists in the real K77 algebra and lands in the right
zero-order slots of Weinstein's displayed fermion matrix. Two independent
family combinations are left untouched. The next question is whether the
observation map preserves that connector or erases it. We are still not
claiming what switches the connector on, which family it is, or that it has a
measured mass.
