---
artifact_type: exploration
status: exploration
doc_type: source-owner-intersection
created: 2026-08-16
work_item: HE-4B
channel: high_energy_two_plus_one_prediction
target_claim: SC-GEN-53
title: "HE-4B: the two Pati-Salam channels have unequal source-native prospects: the 54 has no direct distortion owner, while the 210 has a connection-grade parent but no K77 singlet port, equation-9.16 placement, or stationary activation"
grade: "SOURCE/REPOSITORY COMPOSITION over exact prior receipts. The 54 direct weld is excluded by the complex D5 Hom-space and survives only as an unselected quadratic distortion composite. The 210-to-internal-grade-six identification is a complexified D5/Hodge template; the prior exact q6 construction is on the non-current Cl(9,5) native horn and is not promoted to the source-aligned K77 carrier. Current K77 work admits grade-six connection directions but supplies no specific Pati-Salam-singlet port or action selection. No physical operator rank, VEV, mass, scale, threshold, observable, or prediction is derived."
disposition: DISTINCT_SOURCE_PROSPECTS__54_DIRECT_OWNER_ABSENT_AND_QUADRATIC_ONLY__210_PARENT_CONNECTION_PLAUSIBLE_BUT_K77_PS_SINGLET_PORT_CELL_PLACEMENT_AND_STATIONARY_ACTIVATION_TYPE_MISSING__ONE_CHANNEL_ASYMMETRY_IS_A_KILLABLE_HYPOTHESIS_ONLY
canon_verdict_change: none
steering_effect: "Use H210/H54/HBOTH/HNONE as declared conditional inputs and test downstream K77, equation-9.16, observation, and family-kernel compatibility.  Missing action selection is a dependency fence, not this path's work queue; do not import an external selector or a standard scalar Higgs/VEV."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he3-four-corner-partner-placement-and-family-rank-2026-08-16.md
  - explorations/p54-weld-typing-2026-07-21.md
  - explorations/channel-swing-CH-SM-2026-07-19.md
  - explorations/resolver-wave-f-source-port-action-ownership-2026-08-03.md
  - explorations/resolver-wave-g-q6-native-tilted-source-port-2026-08-03.md
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - explorations/decoupling-constructibility-packet-2026-08-12.md
  - explorations/conditional-build/selected-k77-action-owned-leakage-composition-2026-08-10.md
  - explorations/conditional-build/selected-k77-varpi-radial-half-exchange-gate-2026-08-12.md
  - explorations/conditional-build/selected-k77-grade5-unitary-parent-euler-closure-2026-08-10.md
  - explorations/conditional-build/selected-k77-trace-hq-connection-compatibility-2026-08-13.md
  - lab/sources/source-claim-register.yaml
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`. The `54` and `210` labels are
> complexified vertical-D5 representation comparators. The object under audit
> is whether either label has a correctly typed source-native GU owner. Registry
> integration remains the routing-method owner's call.

# HE-4B — source-owner intersection for the two Pati-Salam channels

## Outcome first

The two Pati-Salam channels isolated after HE-3 do **not** currently have
equal source-native standing.

| complexified D5 owner | conventional tensor label | best GU-side candidate | present result |
|---|---|---|---|
| `54` | `Sym^2_0(V_10)` | traceless quadratic composite of the native distortion | no direct owner; composite only; source/action unselected |
| `210` | `Lambda^4(V_10) ~= Lambda^6(V_10)` after internal Hodge duality | an internal grade-six coefficient of the source connection/distortion one-form | plausible parent only; current-K77 singlet port, operator cell, and stationary activation missing |

This kills the simplest version of HE-3's proposed **common upstairs
mediator**: the repository supplies no single source-owned field or map that
identifies the quadratic metric-composite `54` with the linear
connection-grade `210`, or fixes a relative coefficient between them.

It opens a narrower and more source-faithful hypothesis:

```text
SOURCE-NATIVE ONE-CHANNEL ASYMMETRY:
the 210 connection channel survives, while the 54 composite is absent.
```

That is a hypothesis, not a result. If it survived all gates, the pre-quotient
family coupling would have only one contraction row available. This artifact
does not construct that row, prove that it is nonzero, promote its algebraic
rank to a physical rank, or identify an observed family.

## 1. Layer-0 object table

| phrase | typed object | rule in this artifact |
|---|---|---|
| source-aligned `K77` | ambient `Spin(7,7)`/real-carrier horn | never replaced by a `Cl(9,5)` quaternionic receipt |
| internal `10` | vertical normal/fibre representation with real signature `(6,4)` | its complexification is the D5 vector used by the `54`/`210` labels |
| `54` | `Sym^2_0(V_10)` | not the invariant metric and not the linear distortion value |
| `210` | `Lambda^4(V_10)`, complex-linearly dual to `Lambda^6(V_10)` | not automatically a source-selected connection component |
| source connection | `T_omega` or `varpi` in `Omega^1(Y,ad P)` | retains its one-form coindex; it is not a four-dimensional scalar |
| equation-9.16 cell | one displayed `varpi`, `d_0+varpi`, or starred/adjoint-shaped block | does not name a Clifford grade, PS singlet, family row, or background value |
| carrier admission | a component lies in the allowed connection tangent | not action selection, stationarity, or physical survival |
| VEV language | the source's nonzero connection/distortion background in the operator | never replaced here by a separate standard scalar Higgs |
| one channel | one surviving algebraic contraction space before quotient and observation | not a mass matrix, spectrum, scale, threshold, or prediction |

The routing-method correction is load-bearing: an `ad`-valued one-form stays a
one-form under the source observation contraction. Calling an internal
connection coefficient a scalar because it carries only vertical Clifford
indices would repeat the refuted Kaluza--Klein translation.

## 2. Multi-lens source audit

### 2.1 Source-ledger archaeologist

The primary-source register owns the broad parent objects:

- `SC-FER-01`: the unified field includes `epsilon`, `varpi`, `nu`, and
  `zeta`;
- `SC-OP-04`: equation (9.16) is a candidate four-field operator whose
  displayed cells contain `varpi` and `d_0+varpi`;
- `SC-FER-03`: subfields of the unified field are assigned Higgs-like, CKM,
  Yukawa, and gauge functions, explicitly as assignments rather than derived
  observed equations;
- `SC-ACT-01`: the first bosonic action owns the whole augmented-torsion
  field `T_omega=varpi-epsilon^{-1}d_0 epsilon`;
- `SC-CHI-51` and `SC-CHI-52`: the source describes a VEV in the
  Dirac/Rarita-Schwinger-like operator and a curvature-coaxed augmented-torsion
  mechanism.

None of those rows names a `54`, a `210`, an internal Hodge-dual grade-six
singlet, its Pati-Salam parity, its equation-9.16 cell, or its family-space
coefficient. The source owns the parent connection grammar, not the proposed
irrep selection.

### 2.2 Bundle/operator typist

The source connection has type

```text
T_omega in Omega^1(Y,ad P).
```

Even if an adjoint coefficient is projected to an internal
`Lambda^6(V_10)` component, the result still has a `T^*Y` coindex. A physical
operator claim would additionally need:

1. the current-K77 real-carrier inclusion;
2. the moving `4+10` split and internal Hodge map;
3. the unique Pati-Salam-singlet projector;
4. its placement in the correct barred/unbarred equation-9.16 cells;
5. the source action's stationary nonzero selection; and
6. observation, quotient, reality, and domain survival.

No item may be skipped by calling the coefficient a Higgs VEV.

### 2.3 Clifford-grade specialist

Resolver Waves F and G are genuine prior art. At a fixed `Cl(9,5)` native
reduction they counted all `210` internal grade-six blades, built the generic
native grade-six coefficient projector `q6`, and then contracted to a distinct
rank-`252` `Lambda^5(V^*)` image.

Only part of that result transfers here:

```text
transferable template:  dim Lambda^6(V_10)=210 and
                        Lambda^6(V_10)_C ~= Lambda^4(V_10)_C;

not transferable:       the Cl(9,5)=M(64,H) K/right-H native projector,
                        public-to-native port, or rank-252 downstream object.
```

The source-aligned route is ambient K77. The direct carrier receipts
`selected-k77-grade5-unitary-parent-euler-closure` and
`selected-k77-trace-hq-connection-compatibility` admit ambient grade-six
connection directions and find no pointwise first-action Hessian radical on
them, but that is not a projector onto the one internal
Pati-Salam-singlet line. The old `Pext -> 252` construction is also the wrong
downstream target for HE-4B: it intentionally converts the grade-six
connection coefficient to a five-form kernel rather than preserving and
testing the `210` singlet.

### 2.4 Comparator-routing critic

The July 19 CH-SM exploration said that the metric slot “IS” the `54`. That
sentence is superseded by the July 21 P-54 weld:

```text
native linear value:             V_10;
conventional D-even selector:    Sym^2_0(V_10)=54;
Hom_D5(V_10,54):                 0.
```

The invariant DeWitt/Frobenius metric lies in the singlet of
`Sym^2(V_10^*)`; its traceless projection is zero. The native CH-GR stress
contracts the vertical pair and leaves no free `54` indices. Therefore there
is no direct metric or distortion `54` owner.

The only retained algebraic candidate is the separately defined composite

```text
W_AB(theta)
  = contract_base(theta_A theta_B)
    - (1/10) G_AB trace_G(contract_base(theta theta)).
```

It is quadratic in the native distortion. No inspected source action selects
this composite, proves it nonzero with the required stabilizer, or inserts it
into the fermion operator. The old direct-weld wording must not recur.

### 2.5 Action-selection adversary

The current selected-K77 action result is deliberately adverse to an easy
promotion. It shows that grade-six directions belong to an admitted
connection tangent and are not removed by a pointwise Hessian radical. It
also states the converse boundary explicitly: an admitted field tangent is
not a gauge orbit, a coupled-solution tangent, a BV characteristic
distribution, a selected background, or a physical domain.

Equation (9.16) does not repair that gap. It displays `varpi` cells but leaves
their internal grade and background placement unspecified, and its ambient
half-sign/derivative-cell reconciliation remains open. The decoupling design
packet already records the exact source datum as missing: which `varpi`
subfields occupy which cells, under which horn, and with which nonzero
background.

Thus “the action owns grade six” is allowed. “The action selects the `210`
Pati-Salam singlet that couples the HE-3 cross-half families” is not.

### 2.6 Novelty auditor

Already owned:

- the correction that the direct `54` weld fails;
- the conditional quadratic `54` composite;
- the old-horn internal grade-six count and `q6` template;
- the source ownership of the parent connection and equation-9.16 grammar;
- the source silence on the `varpi`-VEV cell placement; and
- the distinction between field-tangent admission and action selection.

New in this artifact is only their intersection with HE-3's two-channel gate:

1. the two channel owners have unequal source-native prospects;
2. no common source-owned mediator currently relates them; and
3. a killable **one-channel source asymmetry** is more economical than trying
   to force two independent family rows to align.

This is a route refinement, not new source evidence.

## 3. Source-owner ledger

| required object | `54` route | `210` route |
|---|---|---|
| complex D5 carrier | `Sym^2_0(V_10)` | `Lambda^4(V_10) ~= Lambda^6(V_10)` |
| direct native field owner | absent by `Hom_D5(V_10,54)=0` | parent connection admits grade-six directions |
| construction candidate | quadratic `W(theta)` | internal grade-six coefficient of `T_omega/varpi` |
| source attestation of candidate | absent | parent connection only; specific projection absent |
| source-aligned K77 port | not applicable at linear grade | `TYPE_MISSING` |
| PS-singlet projector | comparator-owned | `TYPE_MISSING` on current K77 carrier |
| equation-9.16 cell | `TYPE_MISSING` | `TYPE_MISSING` |
| action-selected nonzero background | `TYPE_MISSING` | `TYPE_MISSING` |
| family-space row | `TYPE_MISSING` | `TYPE_MISSING` |
| observation/quotient/domain survival | `TYPE_MISSING` | `TYPE_MISSING` |

The table does not say that the `210` exists physically and the `54` does
not. It says only that the former has a closer correctly typed parent in the
current source grammar.

## 4. The one-channel hypothesis and its kill branches

### Conditional horns

```text
H210:  assume a compatible nonzero internal-grade-six 210 PS channel and no 54;
H54:   assume a compatible nonzero quadratic 54 PS channel and no 210;
HBOTH: assume both, keeping proportional and independent family rows separate;
HNONE: assume neither.
```

These are conditional-build inputs, not conclusions about what the action
selects. Under `H210` or `H54`, only one algebraic contraction channel is
available before observation and quotient. The downstream build must still
test the family coefficient, cross-half block, and physical descent. Deriving
the source action or supplying an external datum that chooses a horn is
off-limits here.

### Predeclared outcomes

| downstream exact outcome under an assumed horn | effect | next disposition |
|---|---|---|
| no current-K77 real-carrier `210` singlet port exists | killed | stop using the old `q6` template on this horn |
| port exists but misses the required equation-9.16 cross-half cells | killed for HE-3 | return to another source-owned operator placement |
| assumed `H210` reaches the cells with a nonzero row | conditional build survives this gate | test quotient/observation and the two-dimensional algebraic kernel |
| assumed `HBOTH` reaches the cells | one-channel horn not in force | test proportional and independent rows separately |
| assumed `H54` reaches the cells | quadratic horn survives this gate | test its downstream composition without deriving its action origin |
| neither survives | current mediator path stalls | reprioritize the 2+1 path |
| one survives algebraically but its family row is zero or removed by quotient | no physical consequence | retire the apparent channel |

No branch is allowed to fit a family vector, insert a scalar potential, or
choose a background amplitude to obtain the desired outcome.

## 5. Strongest next bounded conditional build

Construct a current-K77 `q_210^PS` gate and stop before the obsolete
rank-`252` projection:

1. build the internal `Lambda^4(V_10) <-> Lambda^6(V_10)` Hodge adapter on
   the source-aligned moving `4+10` split;
2. isolate the unique Pati-Salam-singlet line inside that internal `210`;
3. embed it into the actual selected-K77 full connection tangent with the
   inherited pairing, real structure, grading, and half placement;
4. apply the exact equation-9.16 cell grammar and test whether it reaches the
   HE-3 cross-half `16 x 144` blocks;
5. carry the P-54 negative control in the same probe: no linear distortion or
   invariant metric may masquerade as a `54`;
6. under `H210`, compute the one-row family kernel and follow it through the
   observation/quotient maps; under `HBOTH`, keep proportional and independent
   rows as separate branches.

This sequence is cheaper and sharper than a full large-irrep branching or a
premature action construction. It asks whether each declared horn fits the
rest of the source geometry. The absent dynamical selector remains a recorded
dependency, not an invitation to build it in this channel.

## Claim ceiling

This artifact is a source-owner audit and route hypothesis. It does not build
a K77 `210` port, a `54` composite in the action, a source-selected vacuum, a
fermion mass, a Yukawa matrix, a family-row rank, a physical quotient, a
scale, a threshold, an observable, or a prediction. It does not change
`SC-GEN-53`, canon, or public posture.
