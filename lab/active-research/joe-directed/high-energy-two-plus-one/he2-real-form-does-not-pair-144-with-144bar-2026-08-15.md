---
artifact_type: exploration
status: exploration
doc_type: real-form-reality-gate
created: 2026-08-15
work_item: HE-2
channel: high_energy_two_plus_one_prediction
target_claim: SC-GEN-53
title: "HE-2: HE-1's own named kill is REFUTED. Spin(6,4) reality does NOT pair the 144 with the 144bar, and it fails to do so in BOTH senses of the phrase. LEG A (antilinear, real-form dependent): the Spin(6,4) conjugation ANTICOMMUTES with internal chirality, so conj(144) = 144bar and the Frobenius-Schur indicator of the 144 is 0 -- COMPLEX type, exactly as in the compact form, because (6-4) and (10-0) are the same class mod 8. LEG B (bilinear, real-form BLIND): -w_0 is the nontrivial D5 diagram automorphism, so 144^* = 144bar and NO invariant bilinear form on the 144 exists under ANY real form of so(10,C) -- no real form can make this sector vectorlike, and the SIGNATURE-AMBIENT fork cannot reach the question. Both horns computed: (7,7) and (9,5) differ only in sign(J^2) (+1 Majorana-Weyl vs -1 symplectic-Majorana-Weyl, a uniform doubling), and in BOTH the ambient reality PRESERVES ambient chirality while FLIPPING both the external and the internal chirality factor -- it acts as CPT, not as a doubling. HE-1's n_g -> n_g - 1 SURVIVES. Also closes A3's named residual R2 and supplies the second independent implementation HE-1 asked for. HONEST CEILING: n_g remains an INPUT, and the repository's standing non-chirality of GU's total content is upstream of this file and untouched by it."
grade: "EXACT integer/Z[i] arithmetic on explicit Clifford algebras (13 signatures, Jordan-Wigner, relations verified elementwise) plus exact integer weight combinatorics and Fraction Weyl dimensions; no float is load-bearing anywhere. 204/204 checks, exit 0 (tests/channel-swings/joe_directed_he2_real_form_reality_probe.py). Ranks over F_p (p = 998244353 = 1 mod 4, i |-> s with s^2 = -1) are each SANDWICHED between the F_p lower bound and an independent codomain/theory upper bound, so every rank is pinned exactly rather than probabilistically. Non-vacuity established three ways: nine textbook-known positive controls whose reality types were fixed before the run ((9,1) Majorana-Weyl, (5,5) split real, (7,3) quaternionic, (10,0)/(0,10) complex, (1,3)/(3,1)/(4,0) matching the repo's own 2026-08-13 signature-chirality artifact, (7,7)/(9,5)/(5,9) matching canon/shiab-existence-cl95); one CONTRARY-CONSTRUCTION control on D_4, where -w_0 = id and the analogous gamma-traceless vector-spinor IS self-dual -- a group where the collapse WOULD happen and this machinery detects it; and 8/8 PLANTED FALSE assertions each observed False. Failure path independently verified at harness level: tests/channel-swings/joe_directed_he2_mutation_control.py injects four targeted machinery defects (predeclared reality table, (6,4) index raising, the antilinear B word, the chirality labelling) and 4/4 mutants exit non-zero. STANDARD REPRESENTATION THEORY throughout: mod-8 Clifford periodicity, Frobenius-Schur, the D5 diagram automorphism. NOT: an index, a generation count, a physical carrier, a dynamical statement, a scale, a threshold, or any claim-status movement."
disposition: HE1_SURVIVES_ITS_OWN_NAMED_KILL__REALITY_DOES_NOT_PAIR_144_WITH_144BAR_IN_EITHER_SENSE__FS_INDICATOR_ZERO_AND_NO_INVARIANT_BILINEAR_IN_ANY_REAL_FORM__FORK_ROBUST_ACROSS_BOTH_SIGNATURE_AMBIENT_HORNS__A3_RESIDUAL_R2_CLOSED__HE1_SINGLE_IMPLEMENTATION_SEAM_CLOSED__N_G_STILL_AN_INPUT_AND_TOTAL_CONTENT_NONCHIRALITY_IS_UPSTREAM_AND_UNTOUCHED
canon_verdict_change: none
steering_effect: unchanged
canonical_effect: pending_integration
depends_on:
  - lab/active-research/joe-directed/high-energy-two-plus-one/he1-imposter-separation-invariant-2026-08-14.md
  - lab/methods/source-native-comparator-routing.md
  - canon/shiab-existence-cl95.md
  - explorations/p77-real-index-W1-builder-2026-07-19.md
  - explorations/p77-real-index-W2-twin-2026-07-19.md
  - explorations/W221-falsify-generation-count-structure-2026-07-14.md
  - papers/drafts/hardening-pass-2026-07-03/A3-enum-completeness-route-a-classification-attempt.md
  - explorations/signature-chirality-conjugation-check-2026-08-13.md
  - explorations/chirality-grading-and-77-rerun-2026-08-03.md
  - lab/active-research/joe-directed/majorana-126-neutrino/mj1-exact-126-majorana-block-2026-08-14.md
  - lab/active-research/joe-directed/anomaly-cancellation/ac1-rs-content-cannot-obstruct-and-anomalies-cannot-select-2026-08-14.md
scripts:
  - tests/channel-swings/joe_directed_he2_real_form_reality_probe.py
  - tests/channel-swings/joe_directed_he2_mutation_control.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY` — see §5, which separates the
> source-native leg (a fact about GU's own declared internal real form) from the
> fork-1 comparator leg (the net-chirality readout). Registration in
> `lab/process/source-native-comparator-routing-registry.json` is the method
> owner's call; the notice and classification string above are in the exact form
> `process_gates/source_native_comparator_routing_audit.py` requires.

# HE-2 — the real form does not pair the `144` with the `144bar`

## 0. The gate, verbatim, and what answering it decides

HE-1 (2026-08-14, 62/62 exact) established in **complex** `so(10)` that the
gamma-traceless vector-spinor `144` contains the Pati-Salam content of exactly
**one** family-shaped block, with multiplicity exactly one, and that the block is
the `16bar`'s — a **mirror**, never a copy — so that `n_g` chiral `16`s plus one
`144` leaves net chirality `n_g - 1`. HE-1 then fenced its own ceiling and named
its own kill, verbatim:

> **FENCE 4 — complex reps, one real form unaddressed.** All computation is on
> complex `so(10)` modules. GU's internal group is `Spin(6,4)` […] A real or
> quaternionic structure that pairs `144` with `144bar` would change the
> net-chirality arithmetic. Declared as a ceiling, not resolved.

and, in its own hostile review:

> **Strongest contrary construction.** Real form. […] a real or quaternionic
> structure pairing `144` with `144bar` would make the whole sector vectorlike
> and collapse the net-chirality arithmetic to `n_g -> n_g`. Classified:
> **NOT-YET-FALSIFIED**, with the real-form fork named as the single object that
> could falsify it.

This file executes that gate. **The kill fails.** The answer is not marginal and
is not fork-sensitive.

**HE-1's four fences travel forward unchanged**, in particular Fence 1 (the
object is `WG-P03`'s sector, not "the imposter", whose referent is `RESOLVED(A)`
onto the `Cl(9,5)` 128) and Fence 3 (**every number here is a multiplicity, not a
generation count**; `n_g` is an input). Fence 4 is the fence this file removes,
and only the fence — not the ceilings above it.

---

## 1. Prior-art sweep, by mechanism — and the honest ratio

Swept by mechanism (*real form, reality, conjugation, Frobenius-Schur,
quaternionic, Majorana, self-dual, contragredient, diagram automorphism,
`Cl(6,4)`, `Spin(6,4)`, `144bar`*), not by label. **The `16` half of BOTH legs of
this file was already owned, exactly, by three earlier artifacts.** That is
stated first because it is the largest fact in this section.

| Result | Owner | Status before HE-2 |
|---|---|---|
| **`Cl(6,4)` internal conjugation `Jf` EXCHANGES the two Weyl `16`s; `Jf^2 = +1`; "there is NO real form on a single Weyl 16"; minimal real block `16 (+) 16bar` = 32 real dims** | `explorations/p77-real-index-W1-builder-2026-07-19.md` §4, exhaustive subset search, `P = e0 e2 e4 e7 e9` | **exact — this IS Leg A, for the `16`** |
| `omega_int^2 = -1` on `Cl(6,4)`; `Cl(6,4) = M(32,R)` (1023/1023 integer-exact) | `explorations/p77-real-index-W2-twin-2026-07-19.md` | exact |
| **`16^* = 16bar` "uses only weights (the `D5` diagram automorphism), *no signature* — it holds in *every real form*"; no same-chirality invariant bilinear** | `papers/drafts/hardening-pass-2026-07-03/A3-…-classification-attempt.md` Part D | **exact — this IS Leg B's method, for the `16`** |
| Kramers signs `C^2 = -1` on `(9,5)`, `+1` on `(7,7)`, by Frobenius-Schur multiplication | A3 Part C (`C4`) | exact |
| `S(6,4) = C^16` is complex type, "a real form of the `SO(10,C)` Weyl `16`"; the chirality-correlated branch `S+(10) = (4,2,1) (+) (4bar,1,2)` | `explorations/W221-falsify-generation-count-structure-2026-07-14.md` (b) | exact |
| `Cl(9,5) = M(64,H)`, index `(9-5) mod 8 = 4`; `Cl(7,7) = M(128,R)` | `canon/shiab-existence-cl95.md` Step 2 + failure-mode row | canon |
| `dim Hom_{Spin(9,5)}(S^+ (x) S^+, Lambda^0) = 0` — no same-chirality scalar bilinear | `canon/shiab-existence-cl95.md` CORRECTION SHIAB-05 | canon, computed |
| Antilinear conjugation FLIPS chirality in Lorentzian, PRESERVES in Euclidean (4d) | `explorations/signature-chirality-conjugation-check-2026-08-13.md` H2 | exact |
| `PH-K1-KINEMATIC`: the `Cl(9,5)` 128 block is kinematically vectorlike, `64+64`, both signatures | `explorations/chirality-grading-and-77-rerun-2026-08-03.md` | CONFIRMED |
| "Weinstein's total theory remains explicitly non-chiral" | `CURRENT-STATE.yaml:172` | standing |
| Exact `Cl(10)` machinery over `Z[i]`, charge conjugation, `S+ x S+` degree structure | `tests/channel-swings/joe_directed_majorana_126_block_probe.py` (MJ-1, 55/55) | exact |
| The whole of §3.4 (the `144`'s PS and SM branchings, the mirror, multiplicity 1, `n_g -> n_g - 1`) | HE-1 | exact, complex `so(10)` |

**Honest ratio: roughly two thirds of the machinery and one half of each leg's
content was already owned, and is cited rather than re-claimed.**

### 1.1 What is actually new here

1. **The extension of both legs from the `16` to the `144`**, on an explicit
   gamma-traceless kernel constructed over `Z[i]` — not by analogy. The `144` is
   not a spinor and the transport is not automatic: it needs the vector `10` to
   be a real `so(6,4)` module (verified: all 45 generators are real matrices) and
   it needs the conjugation to intertwine the gamma-trace (verified as an exact
   matrix identity `T^* = eta · B T (I (x) B^{-1})`, which is where the `(6,4)`
   metric is load-bearing — mutation M2 kills the run if the index raising is
   dropped).
2. **A3's named residual `R2`, closed.** A3 recorded, as an open item: *"'antilinear
   = T-type / chirality-preserving' depends on which chirality operator (full 14d
   vs internal 10d) one grades by, and reconciling the census's 'preserving' with
   the internal `conj(16)=16bar` 'swap' is not machine-closed here."* §3.3 closes
   it exactly, in both horns: the ambient conjugation **preserves the ambient 14d
   chirality** and **flips both the external `gamma5` factor and the internal
   `Gamma_11` factor**. Both statements are true simultaneously because the
   ambient chirality *factorises* as `gamma5_ext (x) CHI_int`, and `(-1)(-1) = +1`.
   That is the whole reconciliation, and it is 100% of why the sector stays chiral.
3. **A second independent implementation of HE-1 §3.2** (§3.4), which is exactly
   the hardening HE-1 named as its own weakest reproducibility seam.
4. **The joint statement**: the two legs have *different* real-form dependence and
   answer HE-1's kill in the *same* direction. That redundancy is the result's
   robustness, and it is what makes the SIGNATURE-AMBIENT fork unable to reach
   the question at all.

---

## 2. Preflight — six specialist lenses, each proposing a route

Written before the probe was written. Each lens proposes a route and stakes a
falsifiable prediction.

**Lens 1 — Clifford / real-form specialist.** *Route:* do not reason from
"`Spin(6,4)` is non-compact, therefore something changes." Build `Cl(6,4)`
explicitly over `Z[i]`, solve for the antilinear intertwiners `B_±` satisfying
`Gamma_a^* = ± B Gamma_a B^{-1}`, and read two signs off them: whether `B`
commutes or anticommutes with the chirality element, and the sign of `B^* B`.
Those two signs *are* the Frobenius-Schur trichotomy. *Prediction:* `(6-4) mod 8
= 2`, the same class as compact `(10-0) mod 8 = 2`, so `Spin(6,4)` will behave
**exactly like compact `Spin(10)`**: conjugation flips chirality, `16` is
complex type. The lens's stake is that the non-compactness is a red herring
here — the mod-8 class, not the compactness, is what governs.

**Lens 2 — representation theorist.** *Route:* the phrase "reality pairs `144`
with `144bar`" is a **two-way homonym** and the two senses have *different*
real-form dependence. Sense A is the antilinear one (`conj(V) ~= V`?) and is
real-form dependent. Sense B is the bilinear one (`V^* ~= V`?, i.e. does an
invariant bilinear form exist) and is a **complex-linear** condition, therefore
determined by `so(10,C)` alone and identical in every real form. Sense B, not
sense A, is what actually controls whether a mass term exists — i.e. what
"vectorlike" means. Compute **both**. *Prediction:* for compact groups the two
coincide (via the invariant Hermitian form) and for non-compact ones they need
not; here they will happen to agree, and sense B will turn out to be the
*stronger* statement because no real form can touch it.

**Lens 3 — Kaluza-Klein / dimensional-reduction phenomenologist.** *Route:* the
internal reality type alone does not decide chirality; the ambient one does. The
physical spinor is an ambient `Spin(7,7)` or `Spin(9,5)` object and the ambient
Weyl condition *correlates* external and internal chirality. Build the ambient
by factorisation, `Gamma_mu = gamma_mu (x) I` and `Gamma_{4+a} = gamma5 (x)
Gamma_a`, verify the ambient chirality factorises, and then ask the only question
that matters: does the ambient reality map send `(ext+, int+)` to `(ext-, int-)`
— which is CPT and costs nothing — or to `(ext+, int-)` — which would be a
genuine doubling? *Prediction:* the former, in both horns, because the external
factor `(1,3)`/`(3,1)` is *also* in a chirality-flipping mod-8 class, and two
flips make a preservation. The lens notes in advance that a 10d MW spinor
reduced on `SO(6)` gives one chiral `4` of `SU(4)` by exactly this mechanism, so
the machinery has a textbook check available.

**Lens 4 — honesty auditor.** *Route:* this area has extremely high prior-art
density and the repository has already burned an eight-count of false-novelty
claims in one session. Grep for the exact objects — `Cl(6,4)`, `Spin(6,4)`,
`144bar`, `w_0`, *diagram automorphism*, *self-dual*, *real-form-blind* — before
writing a line of the artifact, and if the substance already exists, lead with
that and grade the swing down accordingly. Binding condition: state the ratio in
the artifact, at the top, not in a footnote.

**Lens 5 — source-fidelity reader.** *Route:* the internal `Spin(6,4)` is not an
assumption of convenience; it descends from `Y14 = Met(X4)`, the Frobenius metric
and the trace-reversal (`canon/shiab-existence-cl95.md` Step 1, UCSD transcript
`[00:43:04]`), and it is **common to both horns** of SIGNATURE-AMBIENT. So a
result kept *internal* is fork-robust by construction. Conversely, anything said
about `n_g` is not source-native: the source's total theory is recorded as
non-chiral, so `n_g` is a comparator input and must be fenced as one. Also:
nothing here may rename the imposter (`RESOLVED(A)`, HE-1 Fence 1).

**Lens 6 — adversary / kill designer.** *Route:* design, before computing, what a
genuine collapse would have to look like, then build a control that exhibits it.
A collapse needs the half-spinor to be self-**dual**, and self-duality holds
exactly when `-w_0 = id`, i.e. for `D_n` with `n` **even**. So `D_4` — where the
`8_s` is real and the analogous gamma-traceless vector-spinor `56` *is* self-dual
— is a group in which the collapse genuinely happens. Run it. If the machinery
cannot see the collapse where it really occurs, its "no collapse" verdict at
`D_5` is worthless. Additionally: plant assertions that are false by construction
and require the run to observe each as False, and mutate the machinery itself and
require the mutants to die.

**Cheapest kill-or-switch condition, recorded before computing.** If the
`Spin(6,4)` conjugation *preserves* internal chirality, or if `-w_0` fixes the
`144`'s highest weight, HE-1's net-chirality arithmetic collapses to `n_g -> n_g`
on the spot and this file reports a kill, not a survival.

**One credible contrary route, recorded before computing.** Even with the `144`
complex, the *field content* could still be vectorlike if GU's ambient spinor is
not Weyl-projected — a full ambient Dirac or non-Weyl Majorana spinor gives
left-handed content `16 + 16 + 16bar + 16bar` and `n_g = 0` outright. That would
not be a real-form effect and would not be this file's finding, but it would make
this file's survival idle. It is exactly what §6 reports as the weakest seam.

---

## 3. The swing — exact results

Probe: `tests/channel-swings/joe_directed_he2_real_form_reality_probe.py`,
**204/204 exact checks, exit 0**, `_local/cas-venv/bin/python`, run from the
repository root. Failure path: `joe_directed_he2_mutation_control.py`, **4/4
mutants die**, plus 8/8 planted false assertions observed False inside the probe
itself.

### 3.1 Leg A — the antilinear (Frobenius-Schur) type of the `144`

The reality structure is *solved for*, not postulated: with the Jordan-Wigner
gammas partitioned into the purely-real set `R` and the purely-imaginary set `S`,
the words `B_S = prod_{a in S} Gamma_a` and `B_R = prod_{a in R} Gamma_a` are
verified elementwise to satisfy `Gamma_a^* = eta · B Gamma_a B^{-1}` with
`eta = -1` and `+1` respectively, for every `a`. Truncating either word kills the
run (mutation M3).

Thirteen signatures, each compared against the textbook `(p-q) mod 8` answer
fixed **before** the run:

```
  Cl(10, 0)  (p-q)%8=2  omega^2=-1  J FLIPS     chirality              ->  COMPLEX
  Cl( 0,10)  (p-q)%8=6  omega^2=-1  J FLIPS     chirality              ->  COMPLEX
  Cl( 6, 4)  (p-q)%8=2  omega^2=-1  J FLIPS     chirality              ->  COMPLEX   <-- GU internal
  Cl( 4, 6)  (p-q)%8=6  omega^2=-1  J FLIPS     chirality              ->  COMPLEX   <-- other convention
  Cl( 9, 1)  (p-q)%8=0  omega^2=+1  J PRESERVES chirality   J^2=+1     ->  REAL
  Cl( 5, 5)  (p-q)%8=0  omega^2=+1  J PRESERVES chirality   J^2=+1     ->  REAL
  Cl( 7, 3)  (p-q)%8=4  omega^2=+1  J PRESERVES chirality   J^2=-1     ->  QUATERNIONIC
  Cl( 1, 3)  (p-q)%8=6  omega^2=-1  J FLIPS     chirality              ->  COMPLEX
  Cl( 3, 1)  (p-q)%8=2  omega^2=-1  J FLIPS     chirality              ->  COMPLEX
  Cl( 4, 0)  (p-q)%8=4  omega^2=+1  J PRESERVES chirality   J^2=-1     ->  QUATERNIONIC
  Cl( 7, 7)  (p-q)%8=0  omega^2=+1  J PRESERVES chirality   J^2=+1     ->  REAL       <-- horn A
  Cl( 9, 5)  (p-q)%8=4  omega^2=+1  J PRESERVES chirality   J^2=-1     ->  QUATERNIONIC  <-- horn B
  Cl( 5, 9)  (p-q)%8=4  omega^2=+1  J PRESERVES chirality   J^2=-1     ->  QUATERNIONIC
```

Rows 5–10 are positive controls whose answers were known before the run
((9,1) is the 10d Majorana-Weyl case, (1,3)/(4,0) reproduce the repository's own
2026-08-13 signature-chirality table, (7,7)/(9,5) reproduce canon). Rows 3 and 4
are the result, in both sign conventions: **the internal `Spin(6,4)` sits in the
same mod-8 class as compact `Spin(10)`, and its Weyl `16` is COMPLEX type.** This
reproduces `p77-real-index-W1` §4 exactly, by a different construction.

The extension to the `144` is then explicit rather than by analogy:

- `V (x) S` is built as a `32 x 320` gamma-trace matrix `T` with the `(6,4)`
  metric raising the index. `rank T|_{V (x) S+} = 16` (F_p lower bound meeting the
  16-dimensional codomain upper bound), so `dim ker = 144`, giving
  `10 (x) 16 = 144 (+) 16bar`; and the image lands **entirely in `S-`**, which is
  the exact certificate that the gamma-trace submodule is a `16bar`, not a `16`.
- An **explicit `Z[i]` basis of the 144** is exhibited, not merely counted:
  `x_{a,s} = e_a (x) s − e_1 (x) (eta^{aa} Gamma_1 Gamma_a s)` for `a = 2..10`
  and `s` a basis of `S+`. All 144 are verified gamma-traceless, rank 144, and
  `CHI = +1`.
- All 45 `so(6,4)` generators on the vector `10` are verified to be **real
  matrices**, so conjugation is equivariant on `V`; and `T^* = eta · B T
  (I (x) B^{-1})` is verified as an exact matrix identity, so conjugation
  preserves gamma-tracelessness.
- Applying `J` to each of the 144 basis vectors: every image is gamma-traceless,
  every image has `CHI = -1`, the image is 144-dimensional, and the 288 vectors
  together have rank 288 — so `144` and `conj(144)` **intersect in `{0}`**.
- Run through **both** antilinear families, `eta = -1` and `eta = +1`
  independently, with identical verdicts.

```
LEG A:   conj(144) = 144bar  !=  144.
         Frobenius-Schur indicator of the 144 under Spin(6,4) = 0.  COMPLEX type.
```

Weight certificate for non-isomorphism: the two weight multisets differ, and are
related exactly by `e_5 -> -e_5`. Racah on `W(D5)` (1920 elements) confirms each
is **irreducible**, with highest weights `(3,1,1,1,1)/2` and `(3,1,1,1,-1)/2`,
and the Weyl dimension formula returns 144 for both.

### 3.2 Leg B — the bilinear (self-duality) type, and why no real form can reach it

An invariant bilinear form on `V` exists iff `V^* ~= V`. This is a
**complex-linear** condition on `g`-module maps; a real form `g_0` has
`g_0 (x) C = g`, so the space of such invariants is *identical* for every real
form. The method is A3 Part D's, applied here to the `144`.

`W(D5)` is constructed explicitly (1920 signed permutations with even sign
changes) and `w_0` is *found*, not quoted: the unique element with
`w(rho) = -rho`. It acts as `e_5 -> -e_5`, the nontrivial `D5` diagram
automorphism. Hence

```
LEG B:   144^* = 144bar  !=  144     (and 16^* = 16bar, (16bar)^* = 16, (144bar)^* = 144)
         =>  dim Inv(144 (x) 144)    = 0   in EVERY real form of so(10,C)
             dim Inv(144 (x) 144bar) = 1
```

**No real form of `so(10,C)` can make the `144` self-dual.** The
SIGNATURE-AMBIENT fork, the compactness of the internal group, the choice of
`(6,4)` versus `(4,6)` — none of these can reach this question, because it is
settled in the complexification that all of them share. This is the stronger of
the two legs, and it makes HE-1's Fence 4 unresolvable *in the collapse
direction* by any signature choice whatsoever.

**Second route to Leg B, using no Weyl group at all — and it is HE-1's own
fact.** The mod-4 coordinate-sum class *is* the character of the `Z/4` centre of
`Spin(10)`. The probe reads the classes straight off the weight multisets:
`16 -> 1`, `16bar -> 3`, `10 -> 2`, `144 -> 3`, `144bar -> 1`. An invariant
bilinear on `V (x) V` requires the centre to act trivially, and
`3 + 3 = 6 ≡ 2 (mod 4) ≠ 0`, whereas `3 + 1 = 4 ≡ 0`. So

```
144 (x) 144    : centre class 2  ->  NO singlet
144 (x) 144bar : centre class 0  ->  singlet allowed
```

This is a **third** independent arrival at the same verdict, and it is the
sharpest form of the result: the centre is a property of the *complex* group, so
**Leg B follows directly from HE-1's own §3.1 mod-4 computation**. HE-1 already
held, without noticing it, the fact that forecloses its own named kill.

**Contrary-construction control, and it is not decorative.** `D_4` is a group
where `-w_0 = id`. The probe runs it: the analogous `so(8)` gamma-traceless
vector-spinor with highest weight `(3,1,1,1)/2` (dimension 56, Weyl formula) **IS
self-dual**. So the machinery does detect the collapse where the collapse really
occurs; the `D_5` "no" is informative and not an artefact of a dead functional.
`D_5` is odd-rank and `D_4` is even-rank, and that parity — nothing about
signature — is the entire mechanism.

### 3.3 Both horns of SIGNATURE-AMBIENT, and A3's residual `R2`

The ambient is built by factorisation and cross-checked against a direct
`Cl(P,Q)` build:

```
Gamma_mu    = gamma_mu (x) I_32          mu = 0..3     (external (1,3) or (3,1))
Gamma_{4+a} = gamma5_ext (x) Gamma_a     a  = 1..10    (internal (6,4))
```

Verified elementwise: the 14 ambient Clifford relations; the resulting signature
is `(1+6, 3+4) = (7,7)` and `(3+6, 1+4) = (9,5)` respectively; and the ambient
chirality **factorises** as `gamma5_ext (x) CHI_int` up to a unit.

| | horn A `(7,7)` | horn B `(9,5)` |
|---|---|---|
| `(P-Q) mod 8` | 0 | 4 |
| ambient algebra | `M(128,R)` (canon) | `M(64,H)` (canon) |
| ambient reality vs ambient chirality | **PRESERVES** | **PRESERVES** |
| `sign(J_amb^2)` | **`+1`** | **`-1`** |
| reality condition available | Majorana-Weyl | symplectic-Majorana-Weyl |
| `dim_R` of the `J`-fixed set inside `S+_amb` | **64** | **0** |
| acts on the external chirality factor | **FLIPS** | **FLIPS** |
| acts on the internal chirality factor | **FLIPS** | **FLIPS** |
| explicit sector identity `Q(++)^* = B Q(--) B^{-1}` | holds | holds |

The last three rows are the substance, and they **close A3's named residual
`R2`**. Both of the following are true at once, with no tension:

- graded by the **full 14d** chirality, the ambient conjugation is
  chirality-**preserving** (the census's "T-type");
- graded by the **internal 10d** chirality, it **swaps** `16 <-> 16bar`
  (`conj(16) = 16bar`).

They reconcile because the ambient chirality factorises and the conjugation flips
*both* factors: `(-1) x (-1) = +1`. The explicit sector identity
`Q(++)^* = B Q(--) B^{-1}` (with `Q(eps,delta) = (I + eps·gamma5)(I + delta·CHI_int)
= 4 P(eps,delta)`) states precisely that the reality map carries the
`(ext+, int+)` sector **onto** the `(ext-, int-)` sector — i.e. it identifies a
left-handed multiplet with the *antiparticle* of its right-handed conjugate. That
is CPT. It is not a second, independent copy.

The `sign(J_amb^2)` row independently reproduces A3's Kramers signs
(`C^2 = -1` on `(9,5)`, `+1` on `(7,7)`) by a different route — explicit
factorised Clifford words plus an exact real-nullity count — and the
`dim_R = 64` vs `0` row is the operational content: `(7,7)` admits a genuine
Majorana-Weyl spinor; `(9,5)` admits none, and requires symplectic doubling.
**That doubling is uniform** — it doubles families and `144`s alike — so it
rescales the arithmetic and does not change its sign.

### 3.4 HE-1 re-derived, independently — its own weakest seam closed

HE-1 named its weakest reproducibility seam as *"the Racah/Weyl decomposition
machinery is bespoke to this probe and has no second implementation in the
repository […] A second implementation via an independent branching route is the
cheapest hardening available."* This file supplies one: weight multisets read off
an explicit `Cl(6,4)` Clifford kernel, then decomposed by Racah on
`W(D_3) x W(D_2)` (96 elements), with `SU(4)` types labelled by `D_3` highest
weight rather than by dimension.

```
16         -> 1x(4,2,1)    1x(4bar,1,2)
16bar      -> 1x(4,1,2)    1x(4bar,2,1)
144        -> 1x(4,1,2)    1x(4bar,2,1)    <-- exactly the 16bar's blocks, multiplicity 1
              1x(20,2,1)   1x(20bar,1,2)   1x(4,3,2)   1x(4bar,2,3)
10 (x) 16  -> 2x(4,1,2)    2x(4bar,2,1)    <-- NEGATIVE CONTROL: multiplicity 2 before
              1x(20,2,1)   1x(20bar,1,2)   1x(4,3,2)   1x(4bar,2,3)     gamma-tracelessness
```

Everything load-bearing in HE-1 §3.1–§3.2 reproduces exactly and **nothing
moved**:

- the mod-4 chirality class (`16` has coordinate sums `= 1 (mod 4)`, `10 (x) 16`
  has `= 3 (mod 4)`, so `10 (x) 16` sits in the `16bar` class);
- `10 (x) 16 = 144 (+) 16bar` with the gamma-trace submodule certified to be the
  `16bar` by an image-support computation, not by assumption;
- the `144` contains **both** `16bar` blocks with multiplicity **exactly one**;
- **neither** `16` block appears in the `144` at all;
- the 128-state exotic remainder;
- HE-1's own negative control: multiplicity **2** before gamma-tracelessness.

`16|_PS = (4,2,1) + (4bar,1,2)` also reproduces W221's independently banked
chirality-correlated branch.

**One correction of record, minor and naming-only.** W221 (2026-07-14) line 79
writes *"`Cl(6,4)` has `(6-4) mod 8 = 2` => complex type `M(16,C)`"*. The complex
type `M(16,C)` belongs to the **even** subalgebra `Cl^0(6,4) ~= Cl(6,3)`; the
ungraded `Cl(6,4)` is `M(32,R)`, as `p77-real-index-W2-twin` independently states
in the same repository. W221's substantive conclusion — that `S(6,4) = C^16` is
the complex-type Weyl spinor, a real form of the `SO(10,C)` `16` — is **correct
and is confirmed here**; only the algebra it is attributed to is misnamed. The
operative fact, `omega_int^2 = -1` (so the Weyl `16`s are the `±i` eigenspaces and
no real form exists on a single one), is verified directly.

### 3.5 The readout

For `n_g` chiral `16`s plus one `144`, the mirror block has multiplicity exactly
one and is **not** paired with itself by any reality structure available to
`Spin(6,4)`, in either sense of "paired":

```
                                 complex so(10)   Spin(6,4), horn (7,7)   Spin(6,4), horn (9,5)
FS indicator of the 144                 0                 0                       0
invariant bilinear on 144 (x) 144       0                 0                       0
net chirality, n_g 16s + one 144    n_g - 1           n_g - 1                 n_g - 1
```

**`n_g -> n_g - 1` SURVIVES. It does not collapse to `n_g -> n_g`.** The
verdict is identical on both horns; the horns differ only by the uniform
symplectic doubling recorded in §3.3.

---

## 4. What this changes, and what it does not

| HE-1 item | after HE-2 |
|---|---|
| **Fence 4** (real form unaddressed) | **REMOVED.** Resolved in the survival direction, by two independent legs, one of which is real-form-blind. |
| HE-1's *"strongest contrary construction: real form"*, classified `NOT-YET-FALSIFIED` | **The contrary construction is REFUTED.** The single object HE-1 named as able to falsify it, cannot. |
| HE-1's *"weakest reproducibility seam: single implementation"* | **CLOSED** for §3.1–§3.2 by an independent Clifford-kernel + `D_3 (+) D_2` Racah route. |
| The `n_g -> n_g - 1` map | unchanged, and now known to be real-form-stable |
| HE-1's kill of candidate `6B` (ν and ζ are not two same-chirality generations) | **strengthened**: HE-1 banked it "subject to Fence 4"; that subjection is now discharged |
| Fences 1, 2, 3 | unchanged and still binding |
| `SC-GEN-53` | still a typed seed; still does **not** become a prediction packet |
| Repository count `{1,3}` | untouched |
| A3 residual `R2` | **closed** (§3.3) |
| A3 residuals `R1`, `R3` | untouched |

---

## 5. Comparator routing — which route does this bind?

`lab/methods/source-native-comparator-routing.md` fork 1 covers exactly this
boundary: *"Ordinary family index or net chirality versus Weinstein's `2+1`."*
This file straddles it, and the two halves must be reported separately.

**Source-native half — this BINDS.** The reality type of the `Spin(6,4)` Weyl
`16` and of the `144` is a fact about **GU's own declared internal group**, which
descends from `Y14 = Met(X4)`, the Frobenius metric and the trace-reversal
(`canon/shiab-existence-cl95.md` Step 1) and is common to both horns of the open
SIGNATURE-AMBIENT fork. The statement *"no real form of `so(10,C)` can make the
`144` self-dual"* is about the source-native carrier and is fork-proof. HE-1
named a specific candidate kill located in GU's own real form; that kill is
refuted, and the refutation binds source-natively. This is a **negative** result
in the source-native register: it removes a candidate obstruction. It does not
add a positive one.

**Comparator half — this does NOT bind.** The statement *"net chirality is
`n_g - 1`"* is fork-1's conventional comparator: an ordinary count of unpaired
left-handed multiplets. Under the boundary's **symmetric** rule (the reading
applied in `LA-3`, same channel-week), a comparator result cannot advance a GU
row in *either* direction — so HE-2's survival is **not** evidence that GU became
more favourable, exactly as an HE-2 collapse would not have been evidence that it
became more adverse. The source's total theory is recorded as **explicitly
non-chiral** (`CURRENT-STATE.yaml:172`), and the open burden there is a
low-curvature luminous/dark decoupling, not a net-chirality target.

**Forbidden summaries, named so they are not written.** *"HE-2 shows GU is
chiral."* No: it shows the real form does not *destroy* whatever chirality the
ambient chirality assignment supplies. *"The 2+1 is now derived."* No: `n_g` is
still an input, the partition is still unlabelled and subtractive, and at the
repository's own live value `n_g = 1` the map gives `0 + 1` and **no `2+1` at
all** — HE-1's own §6 already refused this and the refusal is inherited verbatim.

---

## 6. Postflight — inline hostile review

**Strongest overclaim available, and where it is refused.** *"HE-2 proves the
`144` sector is chiral, so GU produces chiral fermions."* Both halves fail. The
first conflates the two senses of §2 Lens 2: what is proved is that the `144` is
not self-conjugate and not self-dual, i.e. that **nothing in the real form makes
it vectorlike**. Chirality of the actual field content is a different question,
decided by the ambient chirality assignment, which this file takes as given
rather than deriving. The second half runs straight into
`CURRENT-STATE.yaml:172`. Classified: **type-missing** if the ambient-Weyl input
is left implicit. A weaker but still wrong version — *"the real form makes the
sector chiral"* — is also refused: the real form does not *make* it chiral, it
**fails to make it vectorlike**, which is a strictly weaker and purely negative
statement. That distinction is the whole discipline of this file.

**Strongest contrary construction available against HE-2.** Not a real form —
Leg B forecloses that entire class of attack. The live one is a **carrier**
attack: if GU's declared spinorial content is not a single ambient Weyl (or
symplectic-Majorana-Weyl) spinor but a full ambient Dirac or non-Weyl Majorana
object, then the ambient chirality tie of §3.3 never engages, the 4d left-handed
content is `16 + 16 + 16bar + 16bar` and `144 + 144 + 144bar + 144bar`, the
sector is vectorlike, and `n_g = 0` outright. This is **not** a real-form effect
and it would have been equally true of HE-1's complex computation — but it would
make both files idle. The repository's own standing position leans this way:
`PH-K1-KINEMATIC` (the `Cl(9,5)` 128 block is kinematically vectorlike, `64+64`,
in both signatures) and `CURRENT-STATE.yaml:172`. Classified: **route-alive, and
upstream of this file.** HE-2 does not adjudicate it, does not claim to, and is
explicitly conditional on it. Recording it plainly is the point: HE-1's named
kill was *the wrong kill*, and this is the right one.

**Second contrary construction (already recorded by HE-1, untouched here).** The
`2+1` could be carried entirely in flavour space by the banked `Z/3` texture
(`yukawa-scoping` H28), needing no `144` at all. HE-2 adjudicates nothing about
it. Classified: **route-alive**.

**Weakest seam.** Two, stated in order of severity.

1. **The `n_g` input, and what it is worth.** Everything above is a statement
   about *how a `144` behaves relative to a chiral `16`*. It presupposes that
   chiral `16`s exist to subtract from. That presupposition is not established
   anywhere in this repository — it is the standing open item — and on the
   repository's own live value `n_g = 1` the map returns `0`, not `2+1`. HE-2
   makes the subtraction rule real-form-stable; it does not supply its input.
   This is the seam that matters, and it is upstream of both HE-1 and HE-2.
2. **The ambient construction is one representative, not a classification.** §3.3
   builds the ambient by one factorisation `gamma_mu (x) I`, `gamma5 (x)
   Gamma_a`. That construction is verified against a direct `Cl(P,Q)` build for
   the reality type, and the reality type is a signature invariant, so the
   *verdict* is representative-independent. But the specific sector identity
   `Q(++)^* = B Q(--) B^{-1}` is checked on this representative and on the two
   antilinear families it admits, not proved for every equivalent embedding of
   `so(1,3) (+) so(6,4)` into `so(7,7)`/`so(9,5)`. Mitigation actually in place:
   the reality type agrees with the unfactorised build in both horns; the Kramers
   signs independently reproduce A3's, obtained by a different method; and the
   `dim_R` fixed-set counts (64 and 0) are exact. Residual risk: low, and
   confined to §3.3's *reconciliation* language rather than to the verdict.

**A verdict this file DOES bank, claim-indexed.** Against **HE-1's own Fence 4
and its own `NOT-YET-FALSIFIED` contrary construction**: the specific claim
*"a real or quaternionic structure pairing `144` with `144bar` would make the
whole sector vectorlike"* is **refuted at its antecedent** — no such structure
exists for `Spin(6,4)`, in either sense of "pairing", and Leg B shows none exists
for any real form of `so(10,C)`. The consequent is not evaluated because the
antecedent is false. Classified: **HE-1's named kill is KILLED.**

**Source-silent, recorded as such.** Everything HE-1 recorded as source-silent
remains so: the energy scale; the threshold; the sign; which family is
distinguished; whether the 128-state exotic remainder has a partner. HE-2 adds
nothing to any of them. The remainder still carries no SM singlet, still cannot
be lifted by any singlet VEV, and still would require a `144bar` that GU's
declared field content does not obviously supply — and §3.2 now certifies that
this `144bar` genuinely is a *different module*, so the problem is real rather
than an artefact of computing in the complex form. **HE-1 posed exactly that
disjunction ("either a real defect of the construction or an artefact of
computing in the complex form — and which one it is, is exactly the real-form
fork"). It is the first.**

---

## 7. Claim ceiling

- **Exact, and load-bearing:** the reality type of the half-spinor for all
  thirteen signatures; `conj(144) = 144bar` under `Spin(6,4)` with FS indicator
  `0`; `144^* = 144bar` under `so(10,C)`, hence no invariant bilinear form on the
  `144` in any real form; the explicit `Z[i]` kernel basis of the `144` and its
  disjointness from its conjugate (rank 288); the ambient chirality
  factorisation, the preserve/flip/flip pattern and the sector identity in both
  horns; `sign(J_amb^2) = +1` on `(7,7)` and `-1` on `(9,5)` with fixed-set real
  dimensions 64 and 0; the re-derivation of HE-1 §3.1–§3.2; the `D_4`
  contrary-construction control.
- **Standard representation theory, claimed novel by nobody:** mod-8 Clifford
  periodicity; the Frobenius-Schur trichotomy; the `D5` diagram automorphism;
  Racah's formula. Half of each leg was already banked in this repository (§1)
  and is cited, not re-claimed.
- **GU-native only in the selection and the use:** which real form is computed
  in (`Spin(6,4)`, from `Y14 = Met(X4)`), which two horns are swept, which
  modules are selected (`WG-P03`), and what the answer is used to conclude about
  HE-1's Fence 4.
- **NOT claimed:** an index; a generation count; that `n_g = 3`; that GU's field
  content is chiral; a physical carrier (`Pi_RS^phys` still does not exist); a
  dynamical or mass-matrix statement; an energy scale; a threshold; a resolution
  of the SIGNATURE-AMBIENT fork (this file is *robust to* it, which is not the
  same as *deciding* it); that any experiment must see anything.
- **Claim-status movement:** none. `canon_verdict_change: none`. The count stays
  `{1,3}`. `SC-GEN-53` remains a typed seed. `canonical_effect:
  pending_integration`.
