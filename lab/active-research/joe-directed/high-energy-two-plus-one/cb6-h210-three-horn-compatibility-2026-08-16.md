---
artifact_type: exploration
status: exploration
doc_type: conditional_build_three_horn_compatibility_matrix
created: 2026-08-16
work_item: CB-6C
channel: high_energy_two_plus_one_prediction
target_claim: SC-GEN-53
title: "CB-6C: FCORR, ALIGN, and PSRED form an eight-branch prerequisite ledger; their conjunction conditionally types the complete source-labelled moving-PS chain"
grade: "EXACT Boolean/type-prerequisite, rational family-kernel/stabilizer, functor-order, both-half, and semantic-mutation certificate over the CB-6A intrinsic graph-plane and CB-6B staged zero-order interfaces. CONDITIONAL on H210 and on whichever additional horn witnesses are declared. The ledger does not construct or prove coexistence of concrete horn witnesses. No action, selector, observer graph/background, family-row fit, reduction construction, physical quotient, external datum, mass, scale, threshold, spectrum, observable, or phenomenology is constructed."
disposition: EIGHT_BRANCH_PREREQUISITE_MATRIX_EXACT__HORN_ROLES_DISTINCT_BY_TYPE__ALL_THREE_SENTENCE_CONDITIONALLY_COHERENT_GIVEN_WITNESSES__WITNESS_COEXISTENCE_NOT_PROVED__FULL_D0_VARPI_NEXT
canon_verdict_change: none
steering_effect: "Use the all-three branch only when all three concrete witnesses are separately declared. Keep the other seven branches as prerequisite/failure localizers. The next gate is the reverse-conditional full d0+varpi half/duality classifier, not another Boolean overlap replay and not derivation of any horn, action, selector, family row, graph, reduction, or external datum."
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb3-h210-source-observation-functor-crosswalk-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb4-h210-fixed-versus-comoving-ps-typing-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb5-h210-source-fq-bridge-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb6-h210-full-correlated-lift-naturality-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb6-h210-equation916-observed-composition-2026-08-16.md
scripts:
  - tests/channel-swings/joe_directed_cb6_h210_three_horn_compatibility_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — source-native conditional build.** This artifact
> concerns Weinstein's equation-(12.22) F/imposter, equation-(11.6) Q/Z,
> `2+1`, Pati--Salam recombination, and emergent-chirality claims. Ordinary
> family indices, net-chirality arguments, scalar-Higgs/VEV models,
> conventional `SO(10)` mass mechanisms, anomaly selectors, and familiar
> low-energy particle models are irrelevant comparators without a typed
> bridge. Read `lab/methods/source-native-comparator-routing.md` before reuse.
>
> `H210` is assumed. `H210-FCORR`, `H210-ALIGN`, and `H210-PSRED` are
> independent declared horns. Constructing or deriving an action, selector,
> observer graph/background, family row, moving PS reduction, physical
> quotient, external datum, mass, scale, threshold, spectrum, observable, or
> phenomenology is outside scope.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# CB-6C — three-horn compatibility matrix

## Result first

The three post-H210 assumptions have distinct prerequisite roles. Given
concrete witnesses for any selected subset, the corresponding source sentence
is noncircular and no unselected horn role follows from the typed ledger. The
Boolean certificate does not prove that concrete witnesses exist together or
establish unrestricted categorical independence. The roles live at different
type levels:

| horn | witness type | exact job | cannot supply |
|---|---|---|---|
| `H210-FCORR` | source-interpretation identification for an intrinsic representation morphism | identifies the CB-6A observation-induced `Z -> F_corr` adapter with Weinstein's intended equation-(12.22) reveal | family provenance or a moving PS reduction |
| `H210-ALIGN` | provenance-line isomorphism | identifies `L=M_3/ker(r)` with the source F/imposter provenance line | the correlated carrier map or PS descent |
| `H210-PSRED` | normalizer-valued observer-normal cocycle, or stronger PS-valued reduction | lets the preferred moving PS channel and its zero-order operator labels descend | source-reveal meaning or family alignment |

CB-6A now supplies the horn-independent intrinsic associated-carrier map

```text
C_J := kappa_J Gamma_H,J^intr O_J T : 16 -> F_corr
```

on admitted nondegenerate graph overlaps, with its conjugate on the other
ambient half. CB-6B supplies the staged zero-order composition beginning in
the off-diagonal equation-(9.16) `varpi_-+ / varpi_+-` summands. Those exact
representation and operator facts exist before any of the three horns is
declared. `H210-FCORR` changes the source interpretation of `C_J`; it does not
create or naturalize the map.

Only the all-three branch licenses the complete conditional sentence:

> The basis-free family quotient aligned with F/imposter provenance enters
> the conditional H210 Z/internal-144 partner port, and its intrinsic
> observation-induced correlated-F output realizes the source-intended reveal
> inside a preferred moving Pati--Salam-descended zero-order operator chain,
> with the conjugate chain retained.

This sentence is coherent but wholly conditional. It does not say why any
horn is true, select the row or graph, construct the reduction, resolve the
full `d0+varpi` cell, or make the chain physical.

## 1. Objects and functor order

Keep the three source objects distinct:

```text
F_corr = the equation-(12.22) correlated trace carrier labelled imposter;
M_3    = the abstract three-dimensional family-multiplicity space;
Z      = the equation-(11.6) normal-RS sector containing the internal 144.
```

For a separately declared nonzero row `r:M_3 -> C`, put

```text
K = ker(r),                 dim_C K=2,
L = M_3/K,                 dim_C L=1.
```

The two forward staged chains are

```text
M_3 tensor 16_+ --r tensor T_+--> Z_A/144bar_-
  --O_J--> H_J* tensor S_- --Gamma_H,J^intr--> S_+
  --kappa_J--> F_corr,-,

bar(M_3) tensor 16bar_- --bar(r) tensor bar(T)_---> Z_B/144_+
  --O_J--> H_J* tensor S_+ --Gamma_H,J^intr--> S_-
  --kappa_J--> F_corr,+.
```

The first arrows are the zero-order parts of cells `(1,2)` and `(0,3)`.
`kappa_J` is strictly downstream of literal observation and intrinsic graph
trace; it is not an equation-(9.16) coefficient. The upstream H210 tensor is
Z-shaped and obeys

```text
P_Fcorr T_H210 = 0,
```

while the banked intrinsic downstream map has rank `16` per internal half.
Thus the chain is observation induced and cannot be reversed into a claim
that H210 was already F or that its consumed gamma-traceless normal leg was
recovered.

## 2. Complete eight-branch matrix

The word *global* below means descent of the preferred moving PS labels under
a separately declared `H210-PSRED` cocycle. The intrinsic `C_J` is natural as
an associated-carrier morphism on admitted O/Spin overlaps without PSRED; CB-6
does not construct the underlying observer atlas or its global cocycle.

| FCORR | ALIGN | PSRED | strongest well-typed conditional sentence | prohibited promotion |
|---:|---:|---:|---|---|
| 0 | 0 | 0 | The unlabelled family row enters Z and the intrinsic associated-carrier map reaches `F_corr` on admitted O/Spin overlaps; the off-diagonal zero-order chain is typed without a preferred global PS label. | no source-reveal, imposter-family, preferred moving-PS, or observer-atlas claim |
| 1 | 0 | 0 | `C_J` may be called Weinstein's intended `Z -> F_corr` reveal, but its family quotient remains unlabelled and its preferred PS operator label is only local. | no selected family may be called the imposter; no global PS chain |
| 0 | 1 | 0 | `L` has F/imposter provenance, so the upstream H210 arrow is conditionally an imposter-provenance-to-Z-partner bridge; `C_J` remains only the intrinsic algebraic adapter and PS labels remain local. | no intended-reveal claim for `C_J`; no global PS chain |
| 0 | 0 | 1 | The H210/off-diagonal zero-order chain has preferred moving-PS descent; its family row remains unlabelled and `C_J` remains an algebraic F-carrier adapter. | no imposter provenance or source-reveal interpretation |
| 1 | 1 | 0 | The aligned imposter-provenance quotient composes through Z to the source-intended correlated-F reveal on the intrinsic graph-plane carrier; preferred PS operator labels do not globally descend. | no global PS-recombined operator sentence |
| 1 | 0 | 1 | The source-intended correlated-F reveal sits in a preferred moving-PS-descended zero-order chain, but no selected family quotient may be called the imposter line. | no family provenance claim |
| 0 | 1 | 1 | The imposter-provenance quotient enters its Z/internal-144 partner in a preferred moving-PS-descended zero-order chain, but the downstream F-carrier map is not identified with the source-intended reveal. | no source-reveal claim for `C_J` |
| 1 | 1 | 1 | The complete conditional source-labelled moving-PS chain quoted above is well typed on both conjugate halves. | no derivation, selection, full-cell, quotient, or physical promotion |

Every branch retains the exact representation map, the basis-free row and
kernel, and both conjugate halves. A false horn removes only the sentences
whose prerequisite includes that horn; it does not erase the lower-grade
algebra.

## 3. Why the all-three diagram is noncircular

The all-three branch can be displayed as

```text
                   ALIGN (provenance metadata)
             L  ------------------------------> ell_imposter
             |
             | induced by r tensor T
             v
Z/internal-144 partner --O_J; Gamma_H,J^intr; kappa_J--> F_corr
             |                                      |
             | PSRED: descend preferred PS labels   | FCORR: identify this
             | on the zero-order operator chain     | map with source reveal
             v                                      v
      moving-PS associated operator          source-intended interpretation
```

The diagram does not use its conclusion to create any premise:

1. `C_J` is exact before FCORR and remains exact when FCORR is false.
2. `L` and `K` are defined by the declared row before ALIGN and remain defined
   when ALIGN is false.
3. Local PS covariance exists before PSRED; PSRED supplies only the cocycle
   needed for preferred subgroup descent.
4. The three declared horn flags can therefore be toggled independently in
   the prerequisite ledger, producing all eight rows without changing the
   underlying algebraic maps. These stipulated Boolean countermodels are
   bookkeeping, not geometric witness-existence models.

At the present typed level, FCORR interprets a morphism, ALIGN is an
isomorphism in a provenance-line ledger, and PSRED is structure on transition
functions. The artifact supplies no rule deriving one role from another. It
does not define categories rich enough to promote that observation to an
unrestricted categorical-independence theorem.

## 4. Basis-free family kernels and stabilizers

For any downstream stage `B:16 -> W`, including `T`, raw observation, or the
intrinsic correlated map, there is an exact sequence

```text
0 -> K tensor 16
  -> ker(r tensor B)
  -> L tensor ker(B)
  -> 0.
```

Consequently

```text
dim_C ker(r tensor B) = 32 + dim_C ker(B) = 48-rank(B).
```

At the banked receiver `B=C_J` is injective and the kernel is exactly
`K tensor 16`, of complex dimension `32`. On the intrinsic isotropic stratum
its dimension is `36`; at the flat stratum it is `48`. No complement of K is
canonical, and no one-dimensional quotient is a named family.

There are two useful stabilizers:

```text
Stab(r)   = {g in GL(M_3) : r o g = r},
P_K       = {g in GL(M_3) : gK=K}.
```

`Stab(r)` fixes the normalized row and acts trivially on `L`; abstractly it is
`Hom(L,K) semidirect GL(K)`. `P_K` preserves only the row line/kernel and may
rescale `L`. The row itself already reduces family-basis symmetry to the first
group (or the second if only its line is retained). None of FCORR, ALIGN, or
PSRED changes the algebraic kernel or these stabilizers. ALIGN only licenses a
source-provenance interpretation of `L`; it does not choose a basis vector in
`M_3`.

The conjugate package carries `bar(r)`, `bar(K)`, and the conjugate exact
sequence. The parent non-chiral carrier requires both. No luminous/dark
selector is supplied.

## 5. Algebraic and semantic falsifiers

The exact pure-Python certificate checks:

1. all eight Boolean branches and the absence of every pairwise implication;
2. the claim-prerequisite lattice displayed in the matrix;
3. rational rank/kernel identities for stage ranks `0,8,12,16`;
4. normalized-row and row-line stabilizers, plus basis covariance;
5. the upstream-zero/downstream-rank-16 functor-order witness;
6. odd final parity and mirrored fingerprints on both ambient halves; and
7. the distinction between intrinsic associated-carrier descent and the
   additional preferred-PS descent predicate.

Semantic mutants must fire if they collapse any pair of horns, claim source
reveal without FCORR, claim imposter-family provenance without ALIGN, claim a
preferred global moving-PS chain without PSRED, rename Z/internal `144` as F,
recover the consumed normal leg, delete a conjugate half, promote `kappa` into
`varpi`, resolve the full `d0+varpi` cell, name a family, promote a kernel to a
mass/spectrum, or assert the banked `32`-dimensional kernel on a noninjective
stage.

Reproduce with:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_cb6_h210_three_horn_compatibility_probe.py
PYTHONDONTWRITEBYTECODE=1 python3 tests/channel-swings/joe_directed_cb6_h210_three_horn_compatibility_probe.py --selftest
```

## Reprioritization

The prerequisite-ledger question is closed. An all-three overlap replay is
worth doing only if a concrete conditional PSRED cocycle is already supplied;
without one it merely restates the horn. The next fertile gate is instead the
reverse-conditional derivative-half/duality classifier for the full
`d0+varpi` cells: assume a homogeneous cell exists and exhaust the
source-faithful grading conventions it would require. This must not construct
the reduction, graph, family row, source action, or external datum.

## Strict claim ceiling

CB-6C proves an exact eight-row Boolean prerequisite and typed-sentence
ledger. Given separately declared witnesses, the all-three sentence is a
coherent noncircular enrichment of the exact CB-6A/CB-6B carrier chain. It
does not prove that any horn is true, that concrete witnesses coexist, or that
any is source/action selected. It does not construct a family row,
graph/background, moving PS
reduction, action, selector, physical quotient, external datum, luminous
sector, free observed internal `144`, common operator domain, mass, scale,
threshold, spectrum, observable, or phenomenology. The total carrier remains
non-chiral, both halves remain present, and `SC-GEN-53`, canon, and public
posture do not move.
