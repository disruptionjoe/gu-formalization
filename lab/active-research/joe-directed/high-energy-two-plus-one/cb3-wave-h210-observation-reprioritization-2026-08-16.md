---
artifact_type: exploration
status: exploration
doc_type: conditional_build_wave_synthesis_and_reprioritization
created: 2026-08-16
work_item: CB-3-WAVE
channel: high_energy_two_plus_one_prediction
target_claim: SC-GEN-53
title: "CB-3 synthesis: the H210 port survives the banked raw pullback at full rank, but imposter alignment and co-moving intrinsic observation remain separate conditional gates"
grade: "EXACT scoped synthesis of the CB-3 source crosswalk, signed-permutation Cl(7,7) pullback ranks, exact banked receiver-jet substitution, and twelve-lens hostile review. CONDITIONAL on H210; source-shaped imposter-to-partner language additionally requires H210-ALIGN. No source action, background, selector, external datum, physical quotient, mass, family identity, scale, threshold, observable, or public prediction is derived."
disposition: F_IMPOSTER_AND_144_PARTNER_SEPARATED__RAW_PULLBACK_RANK_EXACT__BANKED_RECEIVER_INJECTIVE__H210_ALIGNMENT_SEPARATE__FREE_144_INDEX_CONTRACTED__COMOVING_INTRINSIC_SURVIVAL_NEXT
canon_verdict_change: none
steering_effect: "Keep H210 first for one finite co-moving Cartan/Spin naturality square. Declare H210-ALIGN only as a conditional provenance horn, never as a fitted family selector. Carry both conjugate halves, fixed trace-Hq, the full d0+varpi collision, and B5 mirrors/extras. Demote below SN-1 if the naturality square fails or yields only coordinate-dependent rank."
depends_on:
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb1-h210-k77-rs-intertwiner-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb2-h210-equation916-cross-half-composition-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb3-h210-literal-pullback-rank-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb3-h210-source-observation-functor-crosswalk-2026-08-16.md
  - lab/process/hostile-reviews/2026-08-16-joe-directed-cb3-h210-observation-review.md
scripts:
  - tests/channel-swings/joe_directed_cb3_h210_literal_pullback_probe.py
  - tests/channel-swings/joe_directed_cb3_h210_source_observation_crosswalk_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — source-native conditional build.** This wave tests
> Weinstein's F/Q/Z, `2+1`, imposter, Pati--Salam reunification, and emergent-
> chirality proposal on the banked GU carrier. Ordinary family indices, net-
> chirality arguments, scalar-Higgs VEVs, and conventional `SO(10)` mass
> mechanisms are controls only and do not adjudicate this mechanism. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.
>
> Horn `H210` is assumed. Deriving or varying the source action, choosing a
> vacuum/background, fitting a family row, or importing an external datum is
> outside this channel.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# CB-3 wave synthesis — H210 typed observation

## Outcome first

The H210 path survives this wave, but in a narrower and cleaner form than it
entered.

1. Weinstein's source-labeled imposter is the F-shaped
   `128 = S(TX) tensor S(N)` term in equation (12.22).
2. The internal `144` is the distinct Z-sector partner predicted in
   `SC-PRE-52`. It is not itself the imposter.
3. `H210` supplies a conditional nonzero row from the abstract family-
   multiplicity space `M_3` to that partner sector. It does not identify the
   coupled quotient line with F provenance.
4. Literal graph pullback has exact signature-sensitive ranks. At the
   repository's already-banked receiver test point, the contracted map is
   injective: internal complex rank `16`, real ambient-half rank `64`, and
   family-input kernel `32`.
5. That contraction consumes the free normal covector index. Its codomain is
   `T*X tensor s^*S`, not a still-free Z/internal-`144`. This is a nonzero
   spinor-valued 4D one-form map, not preservation of a free `144` particle
   representation.
6. Raw rank depends on how the graph and tensor are held relative to a
   splitting. Intrinsic local observation therefore remains open until the
   graph, one-form leg, Clifford frame, and H210 tensor pass one co-moving
   K77 Cartan/Spin naturality square.

In plain English: we found that the proposed connector is not erased by the
particular observation test geometry already used in the repo. But we have not
yet shown that this survival is coordinate-independent, that it is the
imposter family that uses the connector, or that any physical particle survives
a quotient.

## Inline pre-flight and archaeology result

Root's pre-flight used source-criticism, representation, differential-
geometric, Clifford, naturality, family-symmetry, emergent-chirality,
falsifier, and efficiency lenses before dispatch.

The archaeology changed the problem rather than merely supplying citations:

- HE-1 Fence 1 and `SC-GEN-03/05` already resolved the imposter referent as F,
  not `144`.
- `SC-PRE-52` separately owns the predicted `144` partner.
- equation (12.22)'s F/Q/Z branching is associated-bundle restriction, while
  VZ-4's literal pullback is contraction by `ds`; these are consecutive typed
  stages, not rival meanings from which one may choose.
- the selected-K77 section-jet work already banked a particular exact
  rank-four receiver jet, so another arbitrary-J-only sweep would have left an
  inexpensive composition undone.

This is why CB-3 was split into an exact literal-pullback calculation and an
independent source/functor crosswalk.

## Exact result

For the CB-1 tensor

```text
T_a = c_a Gamma_a phi_4,
c_a = -2/5 on A_6,       c_a = +3/5 on B_4,
```

and raw graph slope `J:H->V`, literal pullback gives

```text
(O_J T)_mu = Gamma(w_mu) phi_4,
w_mu = sum_a c_a J_(a mu)e_a,
O_J T : 16 -> T*X tensor s^*S.
```

On each internal complex Weyl `16`:

| weighted row-space | rank | kernel |
|---|---:|---:|
| zero | 0 | 16 |
| contains a non-null vector | 16 | 0 |
| totally isotropic dimension 1 | 8 | 8 |
| totally isotropic dimension 2 | 12 | 4 |
| totally isotropic dimension 3 | 14 | 2 |
| totally isotropic dimension 4 | 15 | 1 |

Both real ambient K77 Weyl halves have the same fingerprint, multiplied by
four. Two null rows with nonzero mutual pairing restore injectivity because
their span contains a non-null vector.

For nonzero `r in M_3*`, the invariant family-kernel sequence is

```text
0 -> ker(r) tensor S_16 -> ker(r tensor O_J T)
  -> (M_3/ker(r)) tensor ker(O_J T) -> 0,
```

so its complex dimension is `32 + dim ker(O_J T)`. No complement, basis
vector, named family, or mass kernel is selected.

### Banked receiver test point

Substituting the exact receiver jet already used by the K77 observation prior
art gives weighted Gram matrix

```text
diag(614591/3553225,
     1171823/13608721,
     715/29241,
     1027/64009).
```

Every entry is nonzero. The raw contraction is therefore injective on the
internal `16`; its family-input kernel is exactly the two-dimensional
multiplicity hyperplane tensored with `16`, dimension `32`. This is an exact
repository test-point receipt, not a selected physical section.

## Two conditional horns, not one hidden assertion

```text
H210       = assume the nonzero 210 row exists on M_3;
H210-ALIGN = identify M_3/ker(r) with the source's F/imposter provenance line.
```

Raw rank statements require only `H210`. The source-shaped sentence “the
imposter reunifies with the 144 partner through this row” requires both.
`H210-ALIGN` is available as a declared conditional horn for the next
composition; constructing or fitting it is off limits.

CB-2's historical symbol `F` for family multiplicity is read as `M_3` in this
synthesis. This is a notation repair only; its exact rank and cell formulas do
not change.

## Observation is a staged chain

```text
associated restriction
  -> literal differential-form contraction
  -> co-moving K77 Cartan/Spin naturality
  -> eventual physical quotient/domain.
```

- Restriction retains the F and Z representation sectors in the rank-14
  associated fibre.
- Contraction maps an ambient one-form to four components and consumes the
  free normal covector index.
- Co-moving naturality must decide whether the raw rank is intrinsic when the
  graph, Clifford frame, and tensor transform together.
- A physical quotient would still have to address B5's
  `3E_+ + 3E_- + X`, `dim_C X=1536`.

None of these stages may be substituted for a later one.

## Hostile-review result and claim ceiling

The twelve-lens hostile review independently replayed CB-3A in the declared
CAS environment and CB-3B under system Python. It upheld the source correction,
Clifford rank theorem, both-half fingerprint, and family dimensions. It
required the `M_3/F` homonym repair, `H210-ALIGN`, the banked-jet substitution,
the invariant kernel exact sequence, the contracted codomain, and staged
observation chain integrated above.

Fixed trace-`Hq` remains an adverse `TYPE_MISSING` horn: CB-1's real tensor
passes before the repository-constructed unitary constraint, but no common
phase realizes the full PS/gamma-traceless port there. The released full
`d0+varpi` equation-9.16 cells retain their derivative-half collision. Both
conjugate arrows remain. No mirror or extra sector is removed.

This wave derives no source action, background, vacuum, selector, external
datum, physical family identity, luminous quotient, chirality selection, mass,
scale, threshold, observable, or phenomenology. Canon and public posture do
not move.

## Reprioritization

| path-relative item | priority | fertility | decision rule |
|---|---:|---:|---|
| co-moving K77 Cartan/Spin naturality square for `O_J T` | 1 | `8/10` for one finite swing | pass advances intrinsic local survival; fail demotes H210 below SN-1 |
| `H210-ALIGN` downstream composition | carried horn | `7/10` if assumed, `0/10` as a derivation task | declare it when testing the source-shaped story; never fit the row |
| full off-diagonal `d0+varpi` naturality composition | 2 | `6/10` | enter only after the zero-order co-moving square passes |
| quotient/domain against `3E_+ + 3E_- + X` | 3 | `4/10` here | remains downstream and risks overlap with the hourly action/domain program |
| fixed trace-`Hq` realization | adverse control | `4/10` | carry the failure; do not invent a new Hermitian datum |
| another generic-J rank sweep | retired | `0/10` | the exact strata and banked test point are complete |
| derive action/background/selector or search external data | off limits | not scored | violates the conditional-build contract |

Relative to the wider non-hourly portfolio, H210 remains the best focus for
one more bounded swing because the co-moving square is exact, local, and
decisive. SN-1 moves ahead immediately if that square fails or merely restates
coordinate covariance without an intrinsic contracted map.

## Next scaffold

The next `Go` wave should assume `H210`, carry `H210-ALIGN` as an explicit
separate horn for source-shaped interpretation, and compute exactly one square:

```text
T --literal contraction at J--> O_J T
|                               |
| co-moving K77 Cartan/Spin     | induced 4D one-form/spin transport
v                               v
T' --literal contraction at J'-> O_(J') T'.
```

Use the banked receiver jet as the positive control, flat and totally
isotropic strata as adverse controls, both ambient halves, and the fixed-
`Hq` failure as a separate subhorn. Stop before the full `d0+varpi` cell,
physical quotient, action, or spectrum unless the square commutes.
