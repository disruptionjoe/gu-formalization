---
artifact_type: exploration
status: exploration
doc_type: join_adjudication
created: 2026-08-17
work_item: RW-1
channel: rwall
wave: reverse-scaffold wave (ARC-1 of the first wave)
title: "RW-1: the R(y)/domain-wall join, constructed and typed. The source commits to WHERE at position grade (the transition is indexed by local scalar curvature: SC-CHI-02's R(y), the §12.9 'low-curvature regions' surface, SC-CHI-52's epoch dynamics, SC-CHI-53's two-way reconnection) and even commits to a SIGN — 'So if your curvature is negative, now you start to get a Mexican hat potential' (drafts:149) — but NEVER to a crossing: the CROSSING and POSITIVE token scans over all eight Weinstein primary surfaces return zero hits, exactly one negative-side passage exists, and that changes what the join can be. Exact Jackiw-Rebbi comparator on the repository's own operator class: a zero of the mass hosts NOTHING without a sign change (the y^2 touch-profile, the one-sided broken-to-unbroken profile, and the no-zero profile all host exactly 0 modes; hosted count = |winding| exactly, all closed-form), so the R(y) ZERO LOCUS AS THE SOURCE STATES IT does not host — and the source-shaped (sigma_3) wall's hosted mode carries ambient Gamma-charge EXACTLY 0 (it is sigma_2-graded); the ambient-Gamma-charged wall is the sigma_2 deformation the source never states, and the two walls are the same operator under a grading-rotating unitary. The join therefore types into a NAMED CONSTRUCTION TARGET, re-aimed: VEV-orientation (Z/2) domain walls inside the broken region — whose existence condition is exactly the orientation-unselectability rung-2 proved, and whose location-selection ingredient is exactly rung-2's N2 finding with grad R(y) the source-native candidate — carrying a six-item hosting bill (W1-W6). Hosting, not selection: the preregistered |winding| = 1 null binds, the winding/number question stays OPEN, and this join does not derive 3."
grade: "EXACT where computed: the comparator is closed-form sympy (fiberwise Pauli class algebra with exact eigenvalues; Jackiw-Rebbi modes verified symbolically for generic m(y); six hosting profiles with exact antiderivatives, limits and integrals; Gamma-charges 0 and -1 exact; the grading-rotation unitary exact; chain-vanishing exponents 1 vs 1/2 exact). CERTIFIED where scanned: 29 byte pins at cited lines with a planted negative; the sign scan and the join rescan each carry a planted positive the detector must flag and a planted near-miss it must not. FLOAT-GRADE cross-checks only where the repository's own instruments are re-run (dirac_spectral_flow_section.py, rung2 probe; both exit 0). The comparator binds the model only (routing fence below). NO claim-status movement, NO register edit, NO canon movement, NO kill."
disposition: SOURCE_COMMITS_TO_POSITION_AND_THE_NEGATIVE_SIDE_NEVER_A_CROSSING__A_ZERO_WITHOUT_A_SIGN_CHANGE_HOSTS_NOTHING_EXACT__SOURCE_SHAPED_WALL_MODE_IS_GAMMA_NEUTRAL_EXACTLY__JOIN_TYPES_INTO_VEV_ORIENTATION_WALLS_STEERED_BY_R_NOT_THE_ZERO_LOCUS_ITSELF__SIX_ITEM_HOSTING_BILL__WINDING_OPEN__NOT_A_KILL
target_claim: "NONE-NOT-A-KILL. No registered claim is killed. SC-CHI-01/02/52/53 and SC-GEO-58 all survive, each retyped at its own layer (§1). The one aim this artifact corrects is the stage-2 wording of a PROPOSED, unexecuted upgrade-register item (LD-A's UR-BIT2-BASE, lda-...-2026-08-17.md:456: 'connect its R(y) zero locus to the external-by-structure domain wall') — executed here, the connection lands one object over: the zero locus as stated steers, the VEV-orientation wall hosts. A proposal's wording is not a registered claim; nothing is killed."
canon_verdict_change: none
row_change: none
registry_change: none
steering_effect: unchanged
canonical_effect: pending_integration
scripts:
  - tests/channel-swings/joe_directed_rw1_zero_locus_steers_not_hosts.py
depends_on:
  - lab/sources/source-claim-register.yaml
  - papers/drafts/Transcript into the impossible.md
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - lab/sources/weinstein-gu-primary-source-pack-2026-07-30.md
  - lab/sources/gu-paper-reference-surfaces.md
  - lab/active-research/joe-directed/lens-digs/lda-sg4-bit2-type-and-transport-2026-08-17.md
  - explorations/nielsen-ninomiya-domain-wall-records-as-rows-2026-07-10.md
  - explorations/prereg-rung2-dynamical-wall-and-selectability-test-2026-07-29.md
  - tests/channel-swings/rung2_dynamical_wall_selectability_probe.py
  - tests/function-space-ext/dirac_spectral_flow_section.py
  - explorations/decoupling-constructibility-packet-2026-08-12.md
  - canon/external-by-structure-synthesis-RESULTS.md
  - lab/methods/gu-base-categories.md
  - lab/methods/source-native-comparator-routing.md
  - CANON.md
  - VERIFICATION.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result. Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.
>
> The comparator here (§4) is the Jackiw-Rebbi zero-mode computation run on the
> repository's OWN operator class — the continuum fiber of
> `tests/function-space-ext/dirac_spectral_flow_section.py`, the faithful model
> for the `CANON.md:136` row — with the SOURCE's own profile ingredient
> `m = R(y)/4` (`SC-CHI-02`, register:940). Its results bind that model. They
> adjudicate which construction the join could be, not whether GU realizes it.
> The Mexican-hat form used in §4(f) is the stylized shape the source itself
> asserts curvature produces (drafts:149); it is still a stylized comparator
> form and is typed `FENCED-COMPARATOR` wherever consumed.

# RW-1 — the R(y) / domain-wall join: adjudicated, lineage-verified, priced, and re-aimed

**What this is.** Three banked facts have never been composed: (1) the source
states position dependence for the chirality transition literally (`R(y)`);
(2) canon requires any odd chiral count to be external to the bulk mechanism;
(3) the NN lane names the external escape: the domain-wall mechanism — and
LD-A certified zero co-occurrence of the two vocabularies across the corpus
(verdict card 7, `LIVE-HIGH`; proposed item `UR-BIT2-BASE`). RW-1 constructs
the join and decides what it is. Both outcomes were first-class going in: a
named construction target, or an exact bill of what is missing that kills the
wall route for GU-as-declared. The measured answer is the first, with a
correction to the join's aim that neither LD-A nor the NN lane had: **the
R(y) zero locus, as the source states it, steers a wall; it does not host
one.**

---

## 0. PREFLIGHT — six problem-matched lenses, declared before the work

- **L1 — Layer-typist.** Every source locus must be typed into the four-layer
  Source-Layer category (`lab/methods/gu-base-categories.md` §1: L1
  declared-total / L2 pullback / L3 pm-package / L4 VEV-observed) and every
  carrier utterance into the LAYER/CHIRALITY/MAP-TYPE codomain. Predicts the
  R(y) equation's own layer is neither L1 nor L4 but a toy attached to the
  LA3 arrow. Confirmed (§1).
- **L2 — Index/transport analyst (inherited from LD-A, re-aimed).** LD-A
  measured that the graded count has no transport theorem across the
  transition. The wall question is the complementary one: what does a LOCUS
  have to supply for a count to live ON it? Predicts the answer is a winding,
  not a zero. Confirmed exactly (§4c).
- **L3 — Coverage archaeologist.** Grep before claiming anything is new.
  Found three things that materially changed this artifact: SC-GEO-58's
  negative-side sign datum (register:2336, drafts:149), which no wall
  discussion had consumed; rung-2's N2 finding (a linear gradient makes wall
  LOCATION selectable — prereg:95), which is the R(y) join's real point of
  contact; and R3(d-ii)'s dark-typing (packet:259), which the hosting bill
  must not duplicate.
- **L4 — Source-fidelity reader.** Verbatim fields over paraphrase. Caught
  the token trap that would have voided the sign scan: the source says
  "your curvature is negative" (drafts:149), not "negative curvature" — a
  noun-phrase-only scan misses the operative assertion. The scan's families
  were designed off the verbatim line, then certified with planted controls.
- **L5 — Hostile self-auditor (the import lens).** The named strongest
  attack, carried inline from the brief: am I importing a domain-wall
  expectation from comparator model-building the way the seesaw expectation
  was almost imported? §9 runs the attack; the short answer is that the
  measured guards (Gamma-neutrality, the missing crossing, the rung-2 null)
  each point AGAINST the naive import, and the join survives only in the
  conditional, re-aimed form.
- **L6 — Homonym warden.** "Chirality" is a registered four-way homonym
  (packet:69: ambient 14d grading / observed 4d Weyl / Cl^0 complex
  structure / base-Lorentz label). Predicts the wall-hosted mode's "chirality"
  and the canon count's "chirality" are different senses and the join must
  type the bridge. Confirmed, and sharper than predicted: the mismatch is
  EXACT (Gamma-charge 0, §4d), not merely undeclared.

Retrieval-first was run before any computation, including the
canonical-currency check over the CC-05/CC-06 dirty queues; the per-input
verdicts are §11.

---

## 1. Layer-typed source adjudication: what the source commits to about WHERE and HOW

The four register loci plus the repo's §12.9 rendering plus the sign locus,
each with its own layer stated. The codomain is the four-layer Source-Layer
category **L** of `lab/methods/gu-base-categories.md` (L1 declared-total, L2
pullback, L3 pm-package, L4 VEV-observed; LA3 = the VEV-conditional
decoupling arrow L1 -> L4, grant G6).

| locus | verbatim core (pinned) | its own layer | WHERE it puts the transition | HOW |
|---|---|---|---|---|
| `SC-CHI-01` (draft p.52, register:913, hedge :922) | "a non-chiral total theory splits at the emergent level into two separate chiral theories", conditioned on no VEV "pulling the various sub-fields of varpi to values significantly above zero" | the L4 declaration itself, source-print; the hedge is grant G6 | no location — "at the emergent level" | threshold ("significantly above zero"): LD-A's Reading B |
| `SC-CHI-02` + `SC-MAS-01` (draft p.61 eq (12.13), register:940, :948, :1176) | `dslash_A psi_L(y) = (R(y)/4) psi_R(y)`; "Decoupling at R(y) ~ 0 in (12.15)-(12.17)"; mass "R(y)/4 for any fixed background metric for which the scalar curvature R(y) is approximately constant in a region under study" | a STYLIZED TOY (draft twice flags "stylized"), printed at source-print grade, living on ambient Y, attached to the LA3 arrow as its mechanism illustration — NOT itself an L4 phase declaration and NOT an L1 property | position-indexed, literally: the free variable y; the critical set is the zero locus of R | exact-zero locus (Reading A) via the direct chain m = R/4 |
| repo §12.9 surface (`gu-paper-reference-surfaces.md:56`) | "appears chiral in low-curvature regions via R(y) coupling" | repo reconstruction of the same toy (that file's own grade: Reconstruction) | codimension-0 REGIONS, plural | region/effective — magnitude, not sign |
| `SC-CHI-52` (2025, register:1613) | "the scalar curvature drops and the masses drop. If the mass drops sufficiently, then a Dirac type operator decouples into Weyl type operators" | mechanism narrative for LA3 (spoken, auxiliary); adds the dynamics: curvature coaxes the VEV | epoch-indexed ("when things flatten out") | threshold ("sufficiently"): Reading B, on the VEV chain |
| `SC-CHI-53` (2025, register:1639, :1641) | "when gravity gets low enough ... a decoupling into matter sectors"; luminous "will be connected to matter that is currently dark when gravity becomes strong enough" | L4 phase assignment plus its REVERSAL (hard-core, spoken) | epoch/position-indexed in BOTH directions — both phases are inhabited in one world | magnitude story: low |R| decouples, high |R| reconnects |
| `SC-GEO-58` mechanism note (register:2329, :2336; drafts:149, ucsd 00:43:04) | "there's no Higgs. The Higgs is an illusion ... So if your curvature is negative, now you start to get a Mexican hat potential. Minimal coupling and Yukawa coupling are the same thing." | the sign locus: the VEV-potential's quadratic sign supplied by background curvature (source-print, spoken; the UCSD copy is an edited derivative — §11) | not a location claim | the NEGATIVE side is named: curvature < 0 is what produces the VEV-forming potential |

**What the source commits to (the composed adjudication).**

- (i) **WHERE — position dependence is literal, three ways.** The transition
  is indexed by the local/epochal value of scalar curvature: the free
  variable in eq (12.13), "regions" on the repo's own surface, and the
  two-way epoch narrative of SC-CHI-52/53. The condition of the LA3 arrow
  (grant G6) is therefore a FIELD over the base, not a global bit — LD-A §7's
  base claim, confirmed at the arrow.
- (ii) **HOW — two mass chains, presented as one stylized story.** Chain 1
  (direct): m = R/4, zero at R = 0, sign of m = sign of R. Chain 2 (VEV):
  R < 0 -> Mexican hat -> VEV -> mass, via "Minimal coupling and Yukawa
  coupling are the same thing"; zero wherever the potential is not
  symmetry-breaking, sign of m set by the VEV orientation, not by R. The
  source identifies the chains at stylized grade (WGS-05's three-way link)
  and does not construct the identification; §4(f) measures that as
  functions of R the two chains CANNOT be equal near the transition
  (vanishing order 1 vs 1/2, supports differ).
- (iii) **The chiral phase is a REGION, not an interface.** In every locus
  the chiral behavior is the bulk content of the low-|R| regime
  (codimension 0). No locus asserts localized carriers on the critical set.
- (iv) **The sign structure the source supplies is one-sided.** Exactly one
  passage carries a curvature sign, and it names the NEGATIVE side as the
  VEV-producing one. The CROSSING scan (families "changes sign", "crosses
  zero", "sign change", "flips sign" with a curvature co-token, over all
  eight Weinstein primary surfaces) returns ZERO hits; the POSITIVE scan
  returns ZERO. Certified with a planted positive and a planted near-miss
  (probe LEG 2).
- (v) **Layer discipline on the toy.** The R(y) equation is a stylized toy
  attached to LA3. Crediting L1 (the declared total) with chirality via it,
  or reading it as an unconditional ambient-grading theorem, is the
  "one unforgivable regression" of the base-categories reference (§1.4).
  Its honest content is: it parametrizes WHERE the G6 grant holds.
- (vi) **Reading A vs Reading B is a two-chain fact, not one mechanism's
  ambiguity.** LD-A §12 found the source locates the transition "in two
  incompatible ways" (exact zero locus vs strictly positive threshold).
  Composed with (ii): Reading A is chain 1's zero set; Reading B is chain
  2's threshold. The tension LD-A typed as source-internal is, at this
  grade, the seam between the two chains the source stylizes into one — a
  reconciliation CANDIDATE for the register owner, recorded here as an
  observation, not adjudicated.

---

## 2. Lineage verification: is the wall canon names THE SAME OBJECT as the R(y) transition?

**Quoted, both sides.**

The wall the NN lane names (and canon's external mechanism realizes):

> "the paper's escape -- the count is **external by structure**, supplied by a
> boundary / net-self-dual chiral background via anomaly inflow -- is
> structurally the **domain-wall fermion** escape from N-N (Kaplan 1992;
> Jackiw-Rebbi 1976): a single chiral mode lives on the boundary of an extra
> dimension while the bulk stays vectorlike."
> (`explorations/nielsen-ninomiya-domain-wall-records-as-rows-2026-07-10.md:28-31`)

with its own scope-exit:

> "that GU's specific generation sector *is* this edge mode is unbuilt -- it
> rides SG4, exactly as the carrier bit does." (same file, :90)

The source's R(y) transition:

> `dslash_A psi_L(y) = (R(y)/4) psi_R(y)` (register:940); "Decoupling at
> R(y) ~ 0" (register:948); "appears chiral in low-curvature regions via R(y)
> coupling" (`gu-paper-reference-surfaces.md:56`); "If the mass drops
> sufficiently, then a Dirac type operator decouples into Weyl type
> operators" (register:1613).

**Verdict: `NEIGHBOR-SHARED-INGREDIENT` — not the same object.** Four
measured divergences, then the one shared ingredient:

1. **WHERE.** The wall's chirality lives ON a codimension-1 interface with
   the bulk gapped on BOTH sides. The source's chirality is the bulk content
   OF a codimension-0 region ("regions", plural; "approximately constant in
   a region under study"). Opposite supports.
2. **HOW.** The wall's count is the SIGN CHANGE of the mass across the
   interface (winding). The source's transition is a MAGNITUDE story (drops,
   sufficiently, low enough, strong enough) plus one one-sided sign datum
   (negative side named). §4(c) measures the difference: a zero without a
   sign change hosts exactly 0 modes.
3. **WHAT carries it.** The wall's carrier is a localized normalizable zero
   mode. The source's carrier is the delocalized field content of the
   low-|R| region. §4(c)'s width measurement connects them: as the crossing
   flattens (k -> 0), the wall mode delocalizes into the region content —
   the two pictures are the two LIMITS of one family, and they coincide
   nowhere the source actually speaks.
4. **BASE (against the NN lane's candidate specifically).** The NN lane's
   candidate wall is the autoregressive/temporal frontier of the
   records-as-rows frame. The R(y) locus is a curvature level set in Y.
   Different bases; identifying them was never claimed by the NN lane
   (its own scope-exit above) and is not claimed here.
5. **The shared ingredient (why the join is real anyway).** A JR wall
   construction consumes exactly one field-theoretic input: a
   position-dependent Dirac mass profile. The source SUPPLIES that input,
   literally: m(y) = R(y)/4 with the free variable printed. Whenever the
   profile has a sign-crossing zero, the wall picture and the region picture
   carry the SAME topological datum (count = |winding|, §4c) — so the two
   neighbors share their topological class exactly when W1 (below) holds,
   and are disjoint mechanisms otherwise.

This settles the LD-A card-7 wording: "the source has been holding out a
concrete one — the zero locus of R(y)" (lda:409) is right about the PROFILE
and one object off about the WALL: the locus as stated (a zero with only the
negative side sourced) steers where a wall would sit and how wide its mode
would be; it does not itself host. What hosts, on the source's own materials,
is one object over — §3 and §5.

---

## 3. The hosting question, typed exactly: what a wall must supply

Preregistration honored: rung-2's null binds this section. "|winding| = 1:
generic single-defect energetics select a unit wall, so the accessible count
reduces entirely to the SUPPLIED multiplicity N and the run has demonstrated
hosting, not selection (the Jackiw-Rebbi standard)" (prereg:109-113). This
route delivers HOSTING of the external odd count. It does not derive 3, and
the winding/number question stays OPEN throughout.

The bill. For the canon-required external odd count to live on a wall, the
construction must supply, in order:

- **W1 — a sign-crossing zero (the load-bearing missing item).** The mass
  profile must cross zero with odd winding on a codimension-1 locus. A zero
  alone is INSUFFICIENT — measured exactly: the touch-profile y^2 (a zero,
  no sign change) hosts 0 modes; the one-sided profile (mass -> 0 on one
  side, the broken-to-unbroken interface of chain 2) hosts 0; the no-zero
  profile hosts 0 (§4c). The source states the zero (Reading A), the
  threshold (Reading B), and the negative side (drafts:149) — never the
  crossing (LEG 2: zero CROSSING hits, zero POSITIVE hits). Status: OPEN,
  not forbidden — no source row asserts R >= 0 everywhere either; this is
  absence of assertion, not assertion of absence.
- **W2 — gapped bulk on both sides (normalizability).** |m| bounded away
  from 0 at both ends, with the low-|R| region's width setting only the
  MODE's width, not the count: the count is plateau-robust, the localization
  is not (width^2 = 1/(2k) -> infinity as the crossing flattens, §4c). The
  source's "approximately constant in a region" regime is the delocalizing
  limit.
- **W3 — the grading bridge (measured, and it is the sharpest item).** The
  hosted mode's charge is graded by the WALL grading, and on the
  source-shaped mass direction that charge is EXACTLY ZERO in the ambient
  Gamma grading (§4d). For the hosted count to be the canon chi (the
  cross-chirality Krein grading of `CANON.md:136/139`), a typed bridge from
  wall grading to ambient grading is required. This is the four-way
  chirality homonym (packet:69) met concretely, and it is PH-K1-PHYSICAL's
  open map, not a detail: the sigma_2 wall (which DOES host ambient
  Gamma-charge -1 exactly) differs from the source-shaped sigma_3 wall only
  by a grading-rotating unitary (§4e) — the count is a grading question,
  not an operator question.
- **W4 — external typing of the background.** Every gapping wall direction
  exits the (Gamma-odd, Krein-self-adjoint) interior class (§4a, exact),
  consistent with canon: nonzero hosted count REQUIRES leaving the class,
  and the wall background is a candidate member of canon's external family
  (the 1D analog of "net chiral index = flux number", CANON.md:135). Whether
  a metric-curvature winding is an ADMISSIBLE external background in the
  class-C sense is the external-by-structure owner's typing call — open.
- **W5 — multiplicity is supplied, never derived.** Hosted count =
  |winding| x N with N the SUPPLIED internal multiplicity (§4g, exact;
  rung-2's P2 confirms rank 3 for supplied N = 3 at float grade). And the
  CC-05 fence binds: even a hosted N = 3 would be an ADDITIVE count, while
  the source's partition is SUBTRACTIVE 2+1 with the third family a
  spin-3/2-native imposter — so the wall route's ceiling is hosting the
  external odd count; the 2+1 structure remains owned by representation
  content, not by wall degeneracy.
- **W6 — the observed shadow.** The wall, if built, lives on Y; observed
  content passes through the observation contraction (LA1), which is not
  injective and has no inverse (base-categories N2). What a 13-dimensional
  locus's hosted mode looks like after contraction is untyped. UNTYPED is
  the honest token.

---

## 4. The comparator control, exact — TYPED AS COMPARATOR, fenced

**Routing fence, repeated at the point of use.** Everything in this section
is computed on the repository's own operator class — Gamma = sigma_3, Krein
K = sigma_1, D_0 = sigma_1 (x) p, the continuum fiber of
`tests/function-space-ext/dirac_spectral_flow_section.py` — with zero-order
walls in the complete Pauli basis. It binds this model. It is the standard
Jackiw-Rebbi comparator run to show the detector class works and to measure
which construction the join could be. It moves no GU row. "Krein",
"Gamma-odd", "sigma_2-graded" and the whole grading vocabulary here are
repository vocabulary, not source vocabulary — the source's own §12.9 words
are "mass", "scalar curvature", "decouples", and the source never uses the
repository's pairing or grading terms.

**(a) Fiberwise class/gap algebra (exact eigenvalues; the continuum form of
LD-A's lattice enumeration).** D(p) = sigma_1 p + m M over the complete
Hermitian zero-order basis:

| M | Gamma-odd | Krein-self-adj | fiber spectrum | gaps? | in canon class? |
|---|---|---|---|---|---|
| sigma_0 | no | yes | m - p, m + p | no | no |
| sigma_1 | yes | yes | -(m + p), m + p | no | **yes** |
| sigma_2 | yes | no | +-sqrt(p^2 + m^2) | **yes** | no |
| sigma_3 (source-shaped: the standard Dirac mass, m = R/4) | no | no | +-sqrt(p^2 + m^2) | **yes** | no |

Exactly two gapping directions exist and BOTH leave the interior class; the
one class member does not gap. The wall background is external-by-class-
algebra — exactly the shape canon's decomposition requires of an odd-count
carrier.

**(b) The mode reduction, generic profile.** For the source-shaped wall
D = sigma_1 p + sigma_3 m(y): D psi = 0 iff psi' = -sigma_2 m psi, so the
two solutions are exp(-int m) chi_+ and exp(+int m) chi_- with chi_+- the
sigma_2 eigenvectors — verified symbolically for GENERIC m(y). For the
sigma_2 wall the solutions are Gamma eigenvectors exp(-+int m) e_2/e_1.
Planted failing control: the wrong-chirality candidate exp(-int m) chi_- has
symbolic residual sqrt(2) m(y) exp(-int m) (1, i)/... != 0 and the residual
detector is REQUIRED to flag it (probe L4-b-04; the selftest corrupts the
detector and the control catches it).

**(c) The hosting table (exact; count = |winding| in every row).**

| profile m(y) | what it models | winding | hosted zero modes | mode | ambient Gamma-charge |
|---|---|---|---|---|---|
| tanh y | a VEV-orientation kink inside the broken region (chain 2, Wall-2) | +1 | 1 | sech(y) chi_+ (integral of sech^2 = 2, exact) | **0** (exact) |
| -tanh y | the anti-kink | -1 | 1 | sech(y) chi_- | **0** |
| k y (= R/4, R linear through 0) | chain 1's zero locus WITH a crossing granted (Wall-1) | +1 | 1 | exp(-k y^2/2) chi_+; width^2 = 1/(2k) -> infinity as k -> 0+ | **0** |
| 2 + tanh y | a low-but-nonzero-|R| region (no zero) | 0 | **0** | — | — |
| y^2 | "R ~ 0" as stated: a zero WITHOUT a sign change | 0 | **0** | — | — |
| 1 - tanh y | chain 2's R = 0 boundary: broken -> unbroken, gapless end | UNDEFINED (gapless end) | **0** | — | — |

Three contrary controls, all exact, all hosting zero: **the source's stated
condition — a zero of R — is measured INSUFFICIENT for hosting.** The
sign change is the load-bearing datum, and it is the one datum no source
surface states.

**(d) The grading measurement (the new against-the-join fact).** The
source-shaped (sigma_3) wall's hosted mode is a sigma_2 eigenstate with
ambient Gamma-charge EXACTLY 0 (chi_+-^dag sigma_3 chi_+- = 0, exact). The
wall that hosts ambient Gamma-charge is the sigma_2 wall: its mode is a
Gamma eigenstate with charge EXACTLY -1, and that wall's class exit is
Krein-only — precisely the exit canon's control says a nonzero net count
must make. The source's stylized toy does not distinguish these two mass
directions (it writes one real coupling); the distinction is the W3 bridge.

**(e) Grading transport.** U = exp(-i pi sigma_1/4) is unitary, commutes
with the kinetic term, carries the sigma_3 wall to the sigma_2 wall, and
does NOT preserve Gamma. The two walls are the same operator; the hosted
Gamma-charge (0 vs -1) is carried entirely by which grading is transported.
The count is a grading question, not an operator question — the wall-side
face of LD-A's "the ruler tilts" finding.

**(f) The two source-identified chains disagree at the zero locus (exact,
stylized forms).** Chain 1: m_1 = R/4 vanishes to order 1 in R, support all
R. Chain 2 (Mexican-hat VEV at the source's own stylized grade): m_2
proportional to sqrt(max(-R,0)) — vanishing order 1/2, support R <= 0 only,
identically 0 for R > 0. The source presents these as one story (WGS-05's
three-way link); as functions of R near the transition they cannot be equal.
Typing WHICH chain carries the transition is part of the bill; they give
DIFFERENT wall candidates (Wall-1 needs the R-crossing; Wall-2's hosting
walls are VEV-orientation kinks, and its R = 0 boundary is the non-hosting
one-sided row above).

**(g) Multiplicity.** ker(D (x) I_N) = ker(D) (x) C^N (verified at N = 3
with symbolic v): hosted count = |winding| x N, N SUPPLIED. Cross-check:
the rung-2 probe re-run live returns exit 0, VERDICT: SECTOR-SUPPLIED,
"derived winding +1, supplied multiplicity 3 -> accessible rank 3" (float
grade, the repo's own instrument).

---

## 5. What is GU-natively present vs missing, typed

**Present (source-stated or repo-banked):**

- **P1 — the profile ingredient.** m(y) = R(y)/4 with the free variable
  printed (register:940, :1176). Source-print, toy-fenced. This is the one
  field-theoretic input a JR construction consumes.
- **P2 — the negative side of the sign.** "So if your curvature is negative,
  now you start to get a Mexican hat potential" (drafts:149; register:2336).
  Source-print, spoken; the UCSD copy is an edited derivative (§11).
- **P3 — the dynamics of the dial.** Curvature coaxes the VEV; masses track
  it; both directions inhabited (SC-CHI-52/53). Source-print, no scale
  supplied (register's own custody note).
- **P4 — the operator class and its class algebra.** The repository's own
  model for CANON.md:136, within which the wall computation runs exactly
  (§4a); the interior net-0 theorem and its control (nonzero flow requires
  leaving the class) — banked canon.
- **P5 — the external requirement and its family.** chi = interior-even +
  external-topological-index (CANON.md:139); index = flux for the computed
  external exemplar (CANON.md:135). Banked canon; the wall winding is the
  1D analog AND a new family-member candidate, untyped (W4).
- **P6 — the wall machinery and the selection instrument.** rung-2: derived
  (not imposed) kink; orientation and location provably unselectable from
  inside at that rung; N2: a linear gradient makes location selectable
  (prereg:95, probe [N2]). Standard-field comparator grade, transport to the
  (9,5) carrier open — rung-2's own fence.
- **P7 — the existence condition for Wall-2 is already proved in the
  needed direction.** Orientation is a Z/2 the action provably cannot fix
  (rung-2). An unfixable Z/2 is exactly what makes domains of both
  orientations degenerate — the raw material for orientation walls. (What
  is NOT proved: that domains of both orientations are realized; that is a
  dynamics/initial-data question, open.)

**Missing (each typed, none waved at):**

- **M1 = W1.** The sign-crossing datum for chain 1 (R > 0 regions). No
  source assertion either way; the one sign passage names the negative
  side only. OPEN.
- **M2 = W3.** The grading bridge from the wall grading to the ambient
  Gamma/Krein grading (the four-way homonym adjudication). The
  source-shaped wall hosts Gamma-charge exactly 0; without the bridge, the
  hosted count is not yet canon's chi. OPEN, now with an exact statement of
  why it is load-bearing.
- **M3 = W4.** External-family membership typing for a curvature/VEV
  winding (vs the computed flux exemplar). Owner: external-by-structure
  lane. OPEN.
- **M4.** The chain-1/chain-2 identification as a constructed map (WGS-05's
  own Layer-0 debt), now with the exact exponent obstruction (§4f) any
  construction must absorb. OPEN.
- **M5.** Transport of the whole wall construction from the standard-field
  comparator to the (9,5)/ker-Gamma carrier (rung-2's declared gap,
  inherited unchanged). OPEN.
- **M6 = W6.** The observed shadow of a Y-side wall through the observation
  contraction. UNTYPED.

**The join's disposition.** The join types into a NAMED CONSTRUCTION TARGET:
**VEV-orientation (Z/2) domain walls inside the broken region, steered by
R(y)** — existence condition = the unfixable orientation Z/2 (P7), location
selection = rung-2's N2 gradient with grad R(y) the source-native candidate
(P6+P1), envelope = the Mexican-hat depth R coaxes (P2+P3) — carrying the
bill W1-W6, with the zero locus itself measured non-hosting as stated
(§4c). The kill branch did NOT fire: nothing in the bill is
unsuppliable-in-principle; W1 is unasserted, not contradicted. Per the
rung-2 null, the target's ceiling is HOSTING the external odd count
(|winding| = 1 generic); it does not derive 3, and no stage of it may be
written up as a count derivation.

> [!NOTE]
> **STATUS PV1-20260817 — the kill branch fired downstream, at W1.** This
> disposition's named construction target
> `TYPED-TARGET-VEV-ORIENTATION-WALL-R-STEERED` was killed at typing grade
> by PV-1 (`lab/active-research/joe-directed/wall-bill/pv1-pi0-trivial-in-every-live-typing-w1-kill-fires-2026-08-17.md`):
> pi_0 of the native varpi vacuum set is trivial in every typing the corpus
> grounds, so the orientation flip W1 requires has no carrier to live in.
> Nothing in THIS artifact moves: the steers-not-hosts verdict, the
> R-steering results, and the bill W1-W6 stand as mathematics; what died is
> the construction target they were typed for. Revival forks are named in
> PV-1 and registered as PV1-REVIVAL-FORKS; either landing un-fires the
> kill with nothing here needing retraction.

---

## 6. What each channel inherits, per layer — versionless deltas, each a typed condition gained, none a launder

1. **Grant poset G6 / conditional-physics ledger owner.** G6 (the SC-CHI-01
   VEV hedge) gains a BASE: the source states its condition position- and
   epoch-indexed (§1 i). Any row occupying G6 as a global bit inherits the
   typed condition "per-region under approximately-constant R (SC-MAS-01,
   register:1176); global use must fence the R-variation." More indebted,
   never toward G0.
2. **CANON.md:136 / function-space-ext owner.** Gains the wall-side scope
   note: every gapping zero-order wall deformation exits the class (exact,
   §4a); the exit is not a defect but the doorway — an external count can
   appear only through it, and the sigma_2 wall is the minimal 1D exemplar
   (charge -1, Krein-only exit). Complements LD-A's UR-BIT2-TRANSPORT fence
   from the other side.
3. **external-by-structure owner (`canon/external-by-structure-synthesis-RESULTS.md`).**
   The external family {flux, instanton, APS} gains a NAMED candidate member
   to type: mass-profile winding (JR/Kaplan wall), with the membership
   question (is a curvature/VEV winding admissible external data in the
   class-C sense?) explicitly open (W4). Until typed, wall-hosted counts may
   not be cited as canon-external.
4. **SG4 / gu-forces declaration owner.** Bit 2 gains: if the phase is a
   field over the base (§1 i), the PHASE axis is fibered, and the wall route
   RIDES SG4 rather than resolving it — the NN lane's scope-exit survives
   this join unchanged (nn:90).
5. **Source-claim register owner (SC-CHI-02/52/53, SC-GEO-58).** Gains three
   register-note-grade facts: (a) the CROSSING/POSITIVE absence over the
   eight primary surfaces (certified, LEG 2); (b) the negative-side sign
   datum's role in the wall question (drafts:149 consumed here for the first
   time in this lane); (c) the two-chain reading of the LD-A §12 Reading-A/B
   tension (§1 vi) as a reconciliation candidate. The "uncarried dynamical
   half" adherence notes on 52/53 can now point at a named construction
   target instead of at nothing.
6. **NN lane / records-as-rows.** Gains the typed condition: its temporal
   frontier and the R(y)-steered VEV wall are DIFFERENT candidate walls on
   DIFFERENT bases (§2 divergence 4); any future "the source hands GU a
   wall" sentence must route through the W1-W6 bill, not the frontier
   analogy.
7. **rung-2 / conditional-source-action program.** Gains the source-native
   candidate for its own N2 slot: the target-blind location-selecting
   gradient rung-2 planted by hand exists in the source as grad R(y)
   (P1+P6). Condition inherited unchanged: standard-field comparator until
   the (9,5) transport (M5).

---

## 7. Machine-readable verdicts and certificate

<!-- RW1-TABLE-BEGIN -->

| id | question | verdict | evidence_key |
|---|---|---|---|
| ADJ | what does the source commit to about WHERE/HOW | POSITION-INDEXED-MAGNITUDE-NO-CROSSING | source_sign_scan |
| LIN | wall mechanism vs R(y) transition | NEIGHBOR-SHARED-INGREDIENT | hosting_table_and_sign_scan |
| HOST | what must a wall supply for the count to live on it | BILL-OF-SIX-WINDING-OPEN | hosting_table |
| GRADE | which grading carries the hosted count | GAMMA-NEUTRAL-ON-SOURCE-SHAPED-WALL | gamma_charge_exact |
| JOIN | what the join types into | TYPED-TARGET-VEV-ORIENTATION-WALL-R-STEERED | chain_exponents_and_hosting |
| NOV | does any prior artifact construct this join | NO-PRIOR-JOIN | join_rescan |

<!-- RW1-TABLE-END -->

**Verdict-evidence binding (the probe enforces this; the selftest's contrary
control flips LIN to SAME-OBJECT and must be caught).**

| evidence_key | asserted direction | measured |
|---|---|---|
| source_sign_scan | ADJ requires CROSSING = 0, POSITIVE = 0, NEGATIVE = exactly the one UCSD passage | 0 / 0 / 3 hits, all three the same passage (drafts:149, ucsd:235, register:2336) |
| hosting_table_and_sign_scan | NEIGHBOR requires: zero-without-crossing hosts 0 AND one-sided hosts 0 AND the shared ingredient is real (tanh hosts 1) | 0 / 0 / 1, exact |
| hosting_table | the bill requires count = \|winding\| exactly, contrary controls 0 | six profiles, all exact, law holds in every row |
| gamma_charge_exact | GRADE requires charge 0 on the source-shaped wall and -1 on the sigma_2 wall | 0 and -1, exact |
| chain_exponents_and_hosting | JOIN requires exponent 1 vs 1/2 and the one-sided row non-hosting | measured, exact |
| join_rescan | NOV requires zero joiners outside the naming wave | only the LD-A pair and RW-1's own files join the token families |

**Certificate.** `tests/channel-swings/joe_directed_rw1_zero_locus_steers_not_hosts.py`

- Live run: all legs green, exit 0 (84 checks: 30 quote pins + planted
  negative; certified sign scan with planted positive and near-miss;
  certified join rescan with planted positive and near-miss; the exact
  comparator including the planted wrong-chirality control; both repo
  instruments re-run green; the artifact binding with SHA-pinned table).
- `--selftest`: clean baseline verified FIRST (84/84, count pinned
  independently) and a red baseline aborts; 9 mutations, every one
  corrupting machinery or a reference (a pin's line number, the scan
  families, the Pauli constants, the residual flagger, the winding
  normalisation, the normalizability judge, the cross-check path, and the
  contrary-control artifact copy), each REQUIRED to be caught by its
  targeted check with a genuine [FAIL]; a crash is CRASH-NOT-DETECTION and
  fails; an untargeted catch is INCIDENTAL-NOT-TARGETED and fails; the
  failing check is printed for every mutation; baseline re-verified after
  the mutations; exit 0 on success.
- Read-only; planted corpora and the contrary-control copy live in a temp
  directory and are removed. Deterministic; sympy + numpy + stdlib.
- What the probe does NOT certify: anything about the true RS Y^14 bundle;
  the (9,5) transport; any file joining the wall and curvature vocabularies
  in words outside the scanned token families; and the two stylized-form
  fences (§4 routing paragraph, §4f).

---

## 8. Typed objects (typed-carrier gate)

```gu-typed-objects
result:         Exact Jackiw-Rebbi comparator on the repository's operator
                class: both gapping zero-order Pauli directions exit the
                (Gamma-odd, Krein) class; hosted zero-mode count = |winding|
                exactly across six profiles with three non-hosting contrary
                controls (no-zero, zero-without-crossing, one-sided); the
                source-shaped sigma_3 wall's mode has ambient Gamma-charge
                exactly 0 while the sigma_2 wall's mode has exactly -1; the
                two walls are unitarily equivalent with the difference
                carried by grading transport
carrier:        D = sigma_1 (x) p + M (x) m(y) on C^2 (x) L^2(R), the
                continuum fiber of the periodic-lattice cross-chirality
                Krein-Dirac model of
                tests/function-space-ext/dirac_spectral_flow_section.py
                LAYER=toy CHIRALITY=S-FULL-DIRAC
pairing:        cross-chirality Krein form K = sigma_1, K^2 = I,
                K Gamma = -Gamma K ON=C^2 (x) L^2(R) (the model Krein space,
                NOT the (9,5) / Cl(9,5) S = H^64 carrier; used here only as
                the class-membership test every gapping wall fails)
real_structure: complex Hermitian symbolic; no quaternionic or real
                structure used or claimed; explicitly NOT the (9,5)
                real-form fork
grading:        Gamma = sigma_3, the model's declared elliptic grading.
                HOMONYM-AMBIGUOUS against (a) the wall grading sigma_2 that
                carries the hosted charge, (b) the ambient-half grading of
                the decoupling packet's R3, and (c) observed 4d Weyl
                chirality — the four-way homonym; the bridge is W3 and is
                UNTYPED
action_owner:   repository-construction (the operator class and Krein test
                are the repository's; the profile deformation typed against
                it is source-print, SC-CHI-02 register:940, SC-GEO-58
                drafts:149)
target:         whether the R(y) transition locus can host the
                canon-required external odd count, and on which grading
                MAP-TYPE=not-a-map (a hosting/membership adjudication on a
                class, not a map between carriers)
```

```gu-typed-objects
result:         Source adjudication: position dependence literal (three
                rows + the repo surface); exactly one curvature-sign
                passage on the eight Weinstein primary surfaces and it
                names the negative side; zero crossing assertions; zero
                positive-side assertions; the two mass chains are
                source-identified at stylized grade only and disagree at
                exponent grade at the zero locus
carrier:        the register verbatim/notes fields and the primary-surface
                lines as literal text (byte-pinned at cited lines)
                LAYER=source-print CHIRALITY=N/A
pairing:        NONE
# A text parse: no bilinear form is involved in this result and none is
# claimed.  Bare token per the FX-2 gate's parser; gloss lives on these
# comment lines (LD-A's relay: an indented continuation line would be
# read as part of the value).
real_structure: N/A
grading:        N/A — the parsed lines contain the source's own words
                ("chiral", "decouples"); their operator-level grading
                sense is exactly the four-way homonym this artifact types
                at W3, not resolved here
action_owner:   repository-construction (the parse and scans are RW-1's;
                the parsed content is source-print)
target:         the WHERE/HOW commitment structure of the R(y) transition
                across its five source loci plus the sign locus
                MAP-TYPE=not-a-map (an adjudication of stated commitments,
                not a map)
```

---

## 9. Hostile review, inline — the strongest attack, named in the brief

**"You are importing a domain-wall expectation from comparator
model-building the way the seesaw expectation was almost imported. Kaplan
walls host chiral fermions in every textbook; the source says R(y); you
went looking for the textbook object in GU and — surprise — found a way to
keep it alive as a 'construction target' instead of admitting the source
never describes wall-hosted matter at all."**

The attack is the right one, and three of this artifact's own measurements
are exactly what it predicts would be missing if the import had happened:

1. **The naive import claims the wrong grading, and the measurement caught
   it.** An imported wall expectation reads "wall hosts chiral mode" and
   books it against canon's chi. Measured: on the source-shaped mass
   direction the hosted charge in the canon grading is EXACTLY ZERO (§4d).
   The import would have been wrong in a checkable way, and §3's W3 types
   the gap instead of papering it.
2. **The naive import fabricates W1, and the scan caught it.** The wall
   needs a crossing; the source states a zero, a threshold, and the
   negative side. The certified CROSSING/POSITIVE absence (LEG 2) is this
   artifact's own evidence AGAINST the object it was sent to find. It is
   reported as the headline, not buried.
3. **The naive import inflates hosting into selection, and the
   preregistered null blocks it.** |winding| = 1 generic (prereg:111);
   every hosting statement here carries the null; nothing derives 3; the
   CC-05 subtractive fence is applied to the multiplicity itself (W5).
4. **What survives the attack is conditional and re-aimed.** The join's
   surviving form — VEV-orientation walls steered by R — was not the
   sought object (the zero locus); it emerged from the source's OWN sign
   datum (drafts:149) plus rung-2's OWN unselectability result. The
   comparator's role was to REFUTE the sought identification (§4c contrary
   controls) and to price the survivor. That is the opposite data-flow
   from an import.

Where the attack lands and stays landed: the Mexican-hat form in §4f and
the entire §4 are comparator objects; if the (9,5) transport (M5) changes
the class algebra, the bill's items W3/W4 must be re-derived there. That
residual is named, fenced, and inherited by rung-2's own open gap rather
than discharged rhetorically.

**Second attack, self-run: "Your one-sided-profile result (chain 2's R = 0
boundary hosts nothing) leans on the Mexican-hat stylization plus a
normalizability convention. A hostile re-runner using a different hosting
criterion (resonances, near-zero modes) would report differently."**
Conceded as scope: the JR/index standard (normalizable zero modes) is the
counting standard canon's external mechanism uses, and it is the one
measured. Near-zero physics on gapless-adjacent walls is real physics and
is NOT adjudicated here; the claim is only that the COUNT the canon
decomposition tracks gets no contribution from a gapless-end interface.

---

## 10. Claim ceiling, and what this artifact is not

- **No claim status moves. No canon row moves. No register row is edited.
  No ledger row is edited.** The generation count stays OPEN; SG4 stays the
  decider; the wall route RIDES it.
- **This artifact does not derive 3.** It does not derive 1 either: it
  prices what a hosting construction would deliver (|winding| x N, with
  |winding| = 1 the preregistered generic null and N SUPPLIED) and leaves
  the winding/number question OPEN.
- **The comparator binds the model.** Every §4 number is exact IN THE
  MODEL; none is a statement about the true RS Y^14 bundle or the (9,5)
  carrier.
- **The construction target is a target.** Naming it creates no
  presumption it can be completed; W1-W6 is the bill, and W1/W3 are the
  items on which it can die. Both outcomes remain first-class for the
  executing wave.
- **Relation to LD-A.** This artifact executes the join LD-A's card 7 said
  was missing, honors its deflation (hosting, not the number), and corrects
  one object-identification in a PROPOSED item's wording (§ frontmatter
  target_claim). LD-A's verdicts are otherwise untouched; its card-2
  transport finding and this artifact's W3 are complementary faces of the
  same grading fact.

---

## 11. Canonical-currency check records (for the integrator; RW-1 edits no sidecar)

Dirty inputs consumed, per `process_gates/canonical_currency_audit.py`
(CC-05/CC-06 queues and their neighbors), each with the check verdict
recorded here as the probe standard requires:

1. `lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md` — dirty
   under **CC-06** and **CC-08**.
   - vs CC-06 (chirality is VEV-conditional; selector SG4 bit 2, OPEN):
     the consumed content IS the correction's upstream — the §11.6 hedge
     passage (:137-138) is quoted verbatim here and every use keeps the
     VEV-conditional reading (§1 row 1). **CLEARED-CONSISTENT**
     (pointer: extraction :137-138; this artifact §1).
   - vs CC-08 (the 128 remainder is a partner-placement obligation, not a
     defect): consumed content (§12.9/12.20 renderings) is used as
     mechanism/partner data; no "defect/surplus" framing imported.
     **CLEARED-CONSISTENT** (pointer: extraction §4; this artifact §1-§2).
2. `explorations/decoupling-constructibility-packet-2026-08-12.md` — dirty
   under **CC-05** and **CC-08**.
   - vs CC-05 (the 2+1 is subtractive with a spin-3/2 imposter): the
     consumed rows (R3 dark-typing :259, R4a spontaneous-asymmetry, the
     chirality homonym row :69) contain no additive-count framing, and this
     artifact applies the CC-05 fence to its own multiplicity statement
     (W5). **CLEARED-CONSISTENT** (pointer: packet :259, :69; this artifact
     W5).
   - vs CC-08: R3 treats the mirror half as a placement obligation —
     consistent by construction. **CLEARED-CONSISTENT**.
3. `lab/sources/weinstein-gu-primary-source-pack-2026-07-30.md` — dirty
   under **CC-10** (the UCSD transcript copy is an edited derivative, not a
   primary source).
   - The consumed block (WGS-05, :236-291) renders the 2021 DRAFT's §12.9
     equations, not UCSD material — unaffected by the correction. The
     UCSD-derived quotes this artifact does consume (drafts:149, :158) are
     taken from `papers/drafts/Transcript into the impossible.md` (not in
     the dirty queue) WITH the correction's caveat carried: the UCSD lineage
     is an edited derivative whose audio confirmation is still owed, and
     nothing here treats it as more than transcript-verified.
     **CLEARED-CONSISTENT** (pointer: pack :236-291; CC-10 owner banner).

Non-dirty load-bearing inputs (checked current by inspection at read time):
`lab/sources/source-claim-register.yaml` (co-owner of CC-06),
`lab/active-research/joe-directed/lens-digs/lda-...-2026-08-17.md` and
`lab/methods/gu-base-categories.md` (both postdate every canonical_since),
the NN lane and rung-2 files (in no dirty queue), `CANON.md`,
`VERIFICATION.md`.

---

## 12. POSTFLIGHT — six lenses, after the work

- **P1 — Did any lens change a verdict?** Yes, two, both against the
  drafted direction. L3 (coverage archaeologist) surfaced drafts:149, which
  converted the sign question from "silent" to "negative-side-named" and
  re-aimed the entire join from the zero locus to the VEV-orientation wall.
  L6 (homonym warden) predicted a typing gap at the grading; the
  measurement (§4d) came back sharper — exact neutrality — turning a
  declared unknown into a quantified obstruction.
- **P2 — Is any verdict resting on a single receipt?** Two known thin
  points, both fenced: the one-sided/chain-2 non-hosting row rests on the
  stylized Mexican-hat form (fenced in §4 and §9); the NEGATIVE-side
  reading rests on one passage in an edited-derivative lineage
  (drafts:149 = ucsd 00:43:04; CC-10 caveat carried in §11). Neither
  carries a verdict alone: ADJ needs only the certified absences, which are
  scan-grade over eight surfaces.
- **P3 — What did NOT move?** SG4 remains the decider; bit 2 remains the
  selector; the generation count remains OPEN; LD-A's five cards stand;
  the NN lane's scope-exit stands; rung-2's null stands and is applied.
- **P4 — Where would a hostile re-runner disagree?** The winding-undefined
  convention for gapless-end profiles (an alternative convention assigns
  half-integer winding; the COUNT measurement is convention-free and is
  what the verdicts consume), and the normalizability criterion (§9 second
  attack, conceded as scope). Both are printed, neither is hidden.
- **P5 — Regression check against the standing corrected facts.** Four
  layers: every source locus in §1 carries its layer; contraction-not-KK:
  the wall story here is a Y-side profile plus the LA1 contraction (W6),
  no KK mode-split is used and none is attributed ("It's not Kaluza
  Klein.", drafts L29, untouched); no-GUT: no unification chain appears;
  positivity polarity: untouched; 2+1 subtractive: applied as a fence at
  W5, not regressed. "Majorana", "Krein", "ghost": zero times in the
  source, and this artifact's uses of "Krein" are all inside repository
  vocabulary fences (§4).
- **P6 — Instrument honesty.** The probe's three planted controls (pin
  negative, sign near-miss, join near-miss) and three planted positives
  all fired correctly on the live run; the selftest's 9 machinery
  mutations were each caught by their targeted checks with the clean
  baseline verified first and re-verified after.

---

## The blunt paragraph: which direction the pressure ran

The series wanted this arc to land its biggest swing: the brief itself
frames the R(y) locus as "the source says WHERE chirality switches" meeting
"canon requires exactly that mechanism," and the buildable answer — declare
the zero locus a wall and hand the wave a construction — was sitting there
fully formed. The pressure ran toward building it, and the honest report is
that the three decisive measurements all pushed back: the source never
states the crossing a wall needs (certified absence, not vibes); a zero
without a crossing hosts exactly nothing (three contrary controls, exact);
and the wall shaped like the source's own mass term hosts a mode whose
canon-grading charge is exactly zero — the naive join would have been wrong
twice before it started. What survived is smaller and differently aimed
than the brief's framing: R(y) steers and sizes a wall it does not itself
constitute; the hosting candidate the source's materials genuinely supply
is the VEV-orientation Z/2 wall, whose existence condition rung-2 had
already proved in the needed direction without anyone noticing that was
what it had done. I judge the surviving target real and worth an executing
wave under its bill, and I flag plainly: W1 and W3 are the two items on
which it can still die, and finding either unsuppliable would be a
first-class result of the same rank as this join.
