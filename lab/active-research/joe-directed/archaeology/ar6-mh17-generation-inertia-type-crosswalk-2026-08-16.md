---
artifact_type: exploration
status: closed_exact_scope
doc_type: comparator_type_crosswalk
created: 2026-08-16
work_item: AR-6
channel: archaeology
target_claim: NONE-NOT-A-KILL
title: "AR-6: M-H17 and the generation triplet share an inertia, not an object"
grade: "STATIC CONSTRUCTOR/TYPE CROSSWALK plus exact rational M-H17 replay and numerical generation-sector replay. No new mathematical probe: the distinction is carried by carrier, scalar-field, differential, pairing and quotient provenance already explicit in the constructors."
disposition: CLOSED_AS_TYPED_NUMERICAL_OVERLAP__NO_OWNED_CARRIER_CHAIN_OR_PAIRING_MAP
canon_verdict_change: none
row_change: "AR1 historical row 8 closed by additive correction; historical table and probe preserved"
registry_change: registered_this_wave
canonical_effect: none
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/conditional-build-channel-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/archaeology/ar1-dropped-commitments-ledger-2026-08-15.md
  - explorations/positivity-exit-criteria-design-packet-2026-08-11.md
  - explorations/portfolio-correction-wave-2026-08-12.md
  - explorations/lane-a-mailbox-drain-pass-2026-08-13.md
  - canon/ghost-parity-krein-synthesis.md
  - tests/generation-sector/ghost_parity_krein.py
  - tests/channel-swings/portfolio_mh17_comparator_h0_inertia_probe.py
---

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

# AR-6 — M-H17 / generation-inertia type crosswalk

## Result first

The standing generation-sector Krein inertia `(96,96,0)` and the M-H17
free-comparator descended `H^0` inertia `(96,96,0)` are **not the same object**
and are not identified by any map in the cited current constructor inventory.
They are a deliberately parameter-matched numerical overlap. The M-H17 fixture supplies
`pair_count=96` as an input; it does not derive `96` or a chain map from the
generation triplet. Equal nondegenerate inertia implies at most the existence
of noncanonical abstract isometries after extending the comparator scalars,
not an identification, a preferred bridge, or a physical conclusion.

This closes historical AR1 row 8 as

```text
CLOSED_AS_TYPED_NUMERICAL_OVERLAP__NO_OWNED_CARRIER_CHAIN_OR_PAIRING_MAP
```

It does not move M-H17 on its intended interacting RS/`ker Gamma` carrier.
The interacting K77 BRST differential, physical carrier, descended pairing,
quotient, domain and positivity verdict remain open. It also says nothing
about a source action, the source-native `2+1` mechanism, family selection or
emergent chirality.

## Constructor inventory

| typed feature | generation-sector triplet | M-H17 free comparator |
|---|---|---|
| scalar field | complex numerical Clifford module | exact rational fixture (`Fraction`) |
| parent carrier | `C^14 tensor C^128`, dimension `1792` | coordinate space `V0 = Q^(4n)`, `n=96`, dimension `384` |
| selected carrier | `Wt`, the 192-dimensional top-Casimir self-dual `SU(2)+` sector inside `ker Gamma` | `H^0 = V0 / im(Q_-1)`, dimension `384-192=192` |
| differential | none in this calculation | `Q_-1: Q^192 -> Q^384`, inclusion into the last two coordinate blocks; `Q_0=0` |
| pairing before restriction/descent | actual Clifford-derived `K = eta_V tensor beta_S` | planted coordinate Gram `diag(+I_96,-I_96,0,0)` |
| reported form | `B = Wt^dagger K Wt`, checked Hermitian before numerical cleanup | descended coordinate Gram `diag(+I_96,-I_96)` |
| quotient role | none; this is a kinematic subspace restriction | `im(Q_-1)` is exactly the radical removed in forming `H^0` |
| result | inertia `(96,96,0)` in K95, K77 and Euclidean fixtures | descended inertia `(96,96,0)` in the abstract comparator |

## Eight-lens adjudication

1. **Layer-0 carrier lens.** `Wt` is an embedded subspace selected inside an
   explicit Clifford module. M-H17 `H^0` is a quotient of a separate
   coordinate complex. No repository map has source `Wt` and target `H^0`, or
   conversely.
2. **Field/differential lens.** The generation probe applies no cochain
   differential. The comparator's conclusion depends on a rank-192
   `Q_-1` and `Q_0=0`. Therefore the phrase “the same `H^0` object” is
   ill-typed before any matrix comparison.
3. **Pairing lens.** The generation form is obtained by restricting
   `eta_V tensor beta_S`. The comparator form is specified directly as
   coordinate signs plus a radical. Agreement of their inertias does not
   identify their pairing constructors.
4. **Quotient/cohomology lens.** The M-H17 result is a descended form on a
   quotient. The generation result is a form on a kinematic subspace. A
   quotient interpretation cannot be transported to `Wt` without a chain
   map compatible with both differentials and pairings; none is owned.
5. **Real-form K95/K77 lens.** The generation script independently reproduces
   the inertia in `(9,5)`, `(7,7)` and `(14,0)`. The comparator fixture has no
   Clifford action or K95/K77 real-form datum. Real-form robustness on one
   side is not a bridge to an untyped side.
6. **Basis/frame-sensitivity lens.** Inertia is basis-invariant for each fixed
   nondegenerate form. After scalar extension, the common inertia allows a
   large family of abstract isometries, with no distinguished member. The
   generation triplet embedding, coordinate quotient and physical/ghost
   labeling provide no common frame that would select one.
7. **Archaeology/novelty lens.** The drain pass already recorded “Nothing
   connects them,” and the portfolio wave fenced the result as a free
   comparator rather than interacting K77 positivity. Constructor inspection
   now supplies the missing reason: `pair_count=96` is deliberately injected
   into the comparator.
8. **Source/conditional-build claim-ceiling lens.** This crosswalk is a type
   cleanup only. The conditional-build packet forbids turning a numerical
   comparator collision into a source action, physical quotient, family row,
   chirality selector or ordinary-family-index substitute. No source-native
   claim moves.

## Reproduction receipt and no-new-probe rationale

Read-only reruns at GU commit `e9c1529c` returned one exact rational comparator
receipt and one numerical/machine-certified generation receipt:

```text
tests/channel-swings/portfolio_mh17_comparator_h0_inertia_probe.py
  10/10 exact checks PASS
  H0_INERTIA_96_96_0__COMPARATOR_PC3_FAIL__PHYSICAL_K77_OPEN

tests/generation-sector/ghost_parity_krein.py  [numerical NumPy eigensolver]
  (9,5): (+96,-96,0)
  (7,7): (+96,-96,0)
  (14,0): (+96,-96,0)
```

No new probe is added. The two existing probes already reproduce every
numerical statement at issue. The decisive evidence here is static type and
constructor provenance: one script restricts a Clifford-derived form to an
embedded triplet, while the other creates a rational quotient fixture with
`96` as an argument default. A string/import audit would merely encode current
source layout and would be more brittle than the mathematical distinction.

## One-paragraph adjudication

The standing generation-sector inertia is the restriction of the
Clifford-derived Krein form to the selected 192-dimensional self-dual triplet
inside `ker Gamma`; it is a kinematic subspace calculation with no differential
or quotient. The M-H17 `(96,96,0)` is the descended inertia of a separately
planted free-comparator complex whose `pair_count=96` is an input, with
`Q_-1` killing two radical coordinate blocks. The carriers, scalar fields,
pairings and homological roles differ, and no map between them is constructed.
Equal inertia supplies only a noncanonical abstract isometry after scalar
extension, not identity or a source-native or physical bridge; interacting
K77 positivity remains open.
