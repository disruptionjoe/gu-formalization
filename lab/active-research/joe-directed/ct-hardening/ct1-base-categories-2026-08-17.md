---
artifact_type: exploration
status: exploration
doc_type: reference_design_record
created: 2026-08-17
work_item: CT-1
channel: ct-hardening
title: "CT-1: the three base categories are written down ONCE — lab/methods/gu-base-categories.md states the Source-Layer category (4 objects; observation typed contraction-not-projection per VZ-4/WG-B06; decoupling VEV-conditional per SC-CHI-01/SG4 bit 2; three named non-arrows), the Grant poset (8 assumption-set objects + 1 bucket from v0.259; order by inclusion; the never-launder law with IM-1's launder control as the enforcement receipt), and the Carrier category (11 objects, 4 markers, 11 MAP-TYPE arrow classes + 2 non-arrow tokens; registered homonyms as non-objects) — and the typed-carrier gate now cross-checks its LAYER/MAP-TYPE token constants against the reference's machine block on every run, red on drift, behavior-preserving on the existing tree (same single pre-existing red before and after, byte-identical output except the one added [codomain] line). No thirteenth object was demanded anywhere; four typing disagreements/gaps are recorded (gate-vs-register homonym seeds; three senses of 'layer'; pullback-vs-contraction granularity; G3-vs-G6 unidentified) rather than adjudicated."
grade: "EXACT integer counters only; no float anywhere (the probe sweeps its result surface and asserts none). Probe tests/channel-swings/joe_directed_ct1_base_categories.py: 99/99 checks, exit 0 — 14 parse checks (schemas, unique IDs, object counts 4/8/11 with every category <= 12, marker/arrow-class roles, non-arrows, codomain block), 2 receipt checks (24 distinct receipt paths, every one resolving to an existing file), 10 codomain checks (gate agreement both ways, five planted drift fixtures each caught, one CONTRARY reorder control that must pass), 49 exact source substrings read from their owning files (ST-1's four-layer sentence, IV-20260815's not-competing-quotations sentence, VZ4-01 in canon, WG-B06's contraction correction, SC-GEN-55/56 and SC-CHI-01 verbatims, the p.52 varpi hedge, no-GUT at drafts L125, 2+1-imposter at L119, KK disavowal at L29, IM-1's zero-launder pins, the v0.259 grant/condition/migration/taxonomy strings, register presence of so(1,3) and absence of ad(P_H)/so(3,1), SG4 bit 2 in canon, and more), 5 planted-false propositions each observed False, 3 detector-power fixtures (a 13-object category, a dangling receipt, a bare-homonym object name — each planted defect DETECTED), and 16 artifact/runtime checks (this artifact's conformance; gate --selftest GREEN by subprocess; the two new files audited green in dated scope; live scan printed as reconciliation, not asserted). Failure path: --selftest verifies the CLEAN BASELINE FIRST, then 10/10 machinery/reference mutations (ref-gone, row-regex-blind, limit-loose, root-elsewhere, layer-expect-drift, map-expect-drift, quote-drift, gate-gone, fence-blind, bare-name-blind) each drive exit 1 via genuine [FAIL] lines with crash-catches rejected; --selftest --poison poisons the baseline and requires the refusal path. Gate --selftest (edited): clean fixture baseline verified first, three new codomain controls (live green, planted mismatch caught, missing reference caught), then 10/10 planted false facts each exit 1. NOT: a physics result, a claim movement, a canon/ledger/registry edit, an adjudication of any recorded disagreement, or a retroactive judgment of any artifact."
disposition: THREE_BASE_CATEGORIES_WRITTEN_ONCE_WITH_RECEIPTS__LAYER_4_OBJECTS_WITH_CONTRACTION_AND_VEV_CONDITIONAL_ARROWS_AND_THREE_NAMED_NON_ARROWS__GRANT_POSET_8_PLUS_BUCKET_WITH_NEVER_LAUNDER_LAW_AND_MINT_ENFORCEMENT_RECEIPT__CARRIER_11_OBJECTS_11_ARROW_CLASSES_WITH_REGISTERED_HOMONYMS_AS_NON_OBJECTS__GATE_CODOMAIN_WIRED_BEHAVIOR_PRESERVING__NO_THIRTEENTH_OBJECT_DEMANDED__FOUR_DISAGREEMENTS_RECORDED_NOT_ADJUDICATED
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
priority_change: none
steering_effect: unchanged
canonical_effect: pending_integration
depends_on:
  - lab/methods/gu-base-categories.md
  - process_gates/typed_carrier_declaration_audit.py
  - lab/active-research/joe-directed/carrier-decl/fx2-typed-carrier-declaration-2026-08-16.md
  - lab/active-research/joe-directed/carrier/crb-carrier-is-four-corners-not-one-weyl-2026-08-15.md
  - lab/active-research/joe-directed/integration-review/session-015qsi-coherence-integration-repair-2026-08-15.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/vz-repair/vz4-pullback-is-a-contraction-2026-08-15.md
  - lab/active-research/joe-directed/four-d-mode-decomposition/md1-form-leg-survives-ad-leg-is-untyped-2026-08-14.md
  - lab/active-research/joe-directed/soldered-ad/sa1-the-selector-is-built-and-the-bundle-horn-is-soldered-2026-08-16.md
  - lab/active-research/joe-directed/integration-mint/im1-two-movers-four-debts-and-three-adjudications-2026-08-17.md
  - lab/active-research/joe-directed/source-currency/scur1-source-currency-audit-2026-08-17.md
  - lab/process/conditional-physics-ledger-v0.259.json
  - lab/process/homonym-register.yaml
  - lab/sources/source-claim-register.yaml
  - lab/specifications/six-axis/six-axis-template.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
  - lab/methods/source-native-comparator-routing.md
  - canon/no-go-class-relative-map.md
  - canon/gu-forces-field-space-declaration-RESULTS.md
scripts:
  - tests/channel-swings/joe_directed_ct1_base_categories.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: **`INTERNAL_STRUCTURAL_ONLY`.**
>
> Everything below is repository-internal structural work: a reference
> document stating three context categories, a probe, and a two-surface
> drift check wired into an existing process gate. No physics object is
> computed, no comparator is run, no source claim is tested, and every
> object and arrow in the reference is QUOTED from the artifact that owns it.
>
> **REQUIRED INTEGRATION WRITE, not performed here.** This artifact was
> produced under a write scope limited to its own three paths, on a checkout
> shared with concurrent agents (Arc-5, FIX-A, FIX-B live during this build),
> so it edits no registry and no README. One one-line write belongs to the
> canonical integrator, without which
> `process_gates/source_native_comparator_routing_audit.py` goes red
> (6 unclassified > baseline 5; `UNCLASSIFIED_BASELINE` must NOT be raised):
>
> ```json
> { "path": "lab/active-research/joe-directed/ct-hardening/ct1-base-categories-2026-08-17.md",
>   "classification": "INTERNAL_STRUCTURAL_ONLY" }
> ```
>
> A `ct-hardening/` row in the joe-directed README threads table is an
> integrator courtesy, not gate-required (no inventory gate pins that table).
> `protected_surface_diff_audit` is red on these uncommitted paths by
> construction, as it was for FX-2, and resolves on review/commit.

# CT-1 — the base-category reference exists; the gate can no longer drift from it

**The one-paragraph answer.** The repository already reasons in three context
categories it had never written down: the four-layer structure of the source
construction (ST-1's *"four layers (declared total / pullback / ± package /
observed-VEV-conditional)"*, IV-20260815's *"different layers, not competing
quotations"*), the assumption-set poset the conditional-physics ledger's rows
move in (IM-1's *"zero rows laundered; DERIVED_CONDITIONAL -> DERIVED occurs
nowhere"*), and the typed-carrier vocabulary FX-2's gate enforces token by
token. CT-1 writes the three down ONCE, in
`lab/methods/gu-base-categories.md`, with a receipt on every object and every
arrow, the non-injectivity statements that forbid the X-vs-f(X) confusion
class, the never-launder rule stated as the poset law with the mint's launder
control as its enforcement receipt, and the registered homonyms typed as
non-objects. The typed-carrier gate now cross-checks its `LAYER=` and
`MAP-TYPE=` token constants against the reference's machine block on every
run and reds on drift — wired behavior-preservingly: on the identical tree,
old and new gate output are byte-identical except the one added `[codomain]`
line, and the red list is the same single pre-existing k151 red.

---

## 0. PREFLIGHT — retrieval before work, then six problem-matched lenses

**Retrieval ran first.** Prior categorical/typed-object surfaces were
searched for and read before a line was written, and the reference CITES
rather than rivals each: `GEOMETER-VS-PHYSICS-OBJECTS.md` (the
standard-vs-native fork table; *"you must IDENTIFY which one you are using
and WHY"*), the six-axis template's Layer-0 semantic-alignment precondition
(SAME-OBJECT / HOMONYM / UNCERTAIN markings — the closest existing thing to
a base-category discipline), FX-2's typed-carrier block format and its five
closed vocabularies, CN-2's chirality tokens (the CHIRALITY codomain owner —
deliberately NOT re-wired here), the homonym register (`lab/process/
homonym-register.yaml`, 38 entries, whose own header forbids rival surfaces),
the v0.259 ledger's `taxonomy`/`migrations`/`conditional_hypotheses`
machine surfaces, and IM-1 (the mint whose launder control is the poset
law's enforcement receipt). Nothing found already states the three
categories; everything found supplies their receipts.

**Lens 1 — category-theory-as-schema / olog practice.** A context category
for working mathematicians is a small, boring, total surface: objects
enumerable at a glance, arrows with stated dom/cod, laws stated once. The
danger is pretending to more structure than exists. Route: the Source-Layer
category is a genuine finite category (4 objects, 3 generating arrows, 1
recorded composite, identities); the Grant poset is a genuine poset
(inclusion of assumption sets); the Carrier surface is HONESTLY typed as a
schema-category presentation whose "arrows" are arrow LABELS with dom/cod
supplied per use — stated in the reference §3, not buried. *Cheapest kill:*
any claimed composition or inverse the corpus does not record.

**Lens 2 — terminology management.** The reference must not itself coin,
collide, or launder names. Route: zero coined objects (every object name is
quoted from its owner; where no subscript is attested for MD-1's sense of
the shared `ad(P_H)` token, the referent is written out, as SA-1 §2.3 does,
rather than a subscript invented); the word "layer" is itself flagged as a
three-sense near-collision (gate stratum axis / source-construction layers /
six-axis Layer-0) with register candidacy left to the homonym lane.
*Cheapest kill:* an object name in the reference that appears in no cited
receipt.

**Lens 3 — type-system design.** A reference that types the repository's
maps must keep the injectivity facts load-bearing, because the X-vs-f(X)
confusion class is the repository's costliest recurring error (V3c, the
withdrawn KK clause, the GVPO cell). Route: every Source-Layer arrow carries
an injectivity/conditionality cell; the forbidden inferences are stated as
such (§1.4 of the reference), with ST-1's "one unforgivable regression"
quoted at the exact slot it binds. *Cheapest kill:* an arrow whose direction
or injectivity contradicts its own receipt.

**Lens 4 — source philology.** Every source-facing statement must be bound
to register IDs and loci, at the correct layer, with the five standing
corrections un-regressed. Route: the reference's §5 carries the five
guardrails (positivity polarity, different-layers, contraction-not-
projection, no-GUT, 2+1 subtractive) each verified at its owner via SCUR-1's
ten-item register; the probe byte-matches the verbatims (drafts L125, L119,
L29; SC-GEN-55/56; SC-CHI-01 and the p.52 hedge). *Cheapest kill:* a quoted
sentence that is not in its cited file.

**Lens 5 — adversarial reading.** Strongest attack, run before writing: **is
any "object" actually two objects (the homonym class), and is any arrow
direction wrong?** Findings: (a) the near-miss was C11 — an early draft
named it "`ad(P_H)` in MD-1's sense", which leads with the bare registered
token; renamed to lead with the referent, and the probe now has a
bare-name detector with a planted-positive control; (b) "layer" itself is
the two-objects case at the vocabulary level — recorded as D2 rather than
silently normalized; (c) arrow directions were each checked against their
receipts — `s^*` runs L1 -> L2 (ambient to pulled-back, VZ-4's map), the
package INCLUDES into the total (L3 -> L1, not the reverse — the reverse is
non-arrow N1, the unbuilt selection), and decoupling runs L1 -> L4
conditionally (SC-CHI-01's split sentence). The tempting wrong direction —
package as image of a projection from the total — is exactly the S-HALF-SAME
trap and is typed as a named absence. *Cheapest kill:* found and fixed at
C11; no other object splits under this attack.

**Lens 6 — process-gate architect.** The wiring must make drift impossible
without changing behavior. Route: the check reads module constants (so
runtime mutations are caught too), anchors the reference path to the gate's
own `__file__` (so selftests in tempdirs still find it), fails closed on a
missing reference, adds reds rather than crashing, and is itself
selftested (planted mismatch + missing file, both required caught). Proof
obligation discharged in §4: old-vs-new gate output on the identical tree is
byte-identical except the one added informational line. *Cheapest kill:* any
FX-2 probe check that changes state — none does (67/67 before and after;
its 10/10 mutation selftest still GREEN).

---

## 1. The three categories, compact

Full statement with per-row receipts: `lab/methods/gu-base-categories.md`.
Summary:

**Source-Layer category L (4 objects).** L1 declared-total (unsubscripted
four-corner Dirac arena; CR-B §4.1, eq 9.16/11.6 extractions) · L2 pullback
(Weyl-pullback effective generation; SC-GEN-55) · L3 pm-package
(`Omega^0(S+) (+) Omega^1(S-)`; SC-GEN-56; `W_+` in ST-1 §3) · L4
VEV-observed (post-decoupling chiral phase; SC-CHI-01; SG4 bit 2). Arrows:
LA1 observation `s^*` L1->L2, **contraction** (VZ-4 [A2], WG-B06, MD-1
E1-E3), NOT injective ([A4], [B7]) · LA2 package inclusion L3->L1 (CR-B two
class-homogeneous halves) · LA3 decoupling L1->L4, **VEV-conditional**
(SC-CHI-01 hedge = SG4 bit 2 = Grant node G6; ST-1 §5 V1) · LA4 = LA1 after
LA2 (the source's own effective-generation composite). Named non-arrows: N1
no half-selection L1->L3 (unbuilt; the S-HALF-SAME trap), N2 no inverse
L2->L1 (REDUCTION-FIDELITY), N3 no operative-half assignment L4->L3 (IV
§3.3). Forbidden by LA1's non-injectivity: crediting `X` with properties of
`s^*(X)` or conversely (V3c's error class); crediting the total with
chirality or the package with an unconditional spectrum (ST-1 Lens 4).

**Grant poset G (8 objects + 1 bucket).** G0 empty/unconditional (15
`DERIVED` rows) · G1 {GRANT-ACA1-C1} (AC-A1, inherited by AC-A2/A3 via
"none after AC-A1"; *"the grant is declared, not derived"*) · G2-G4 the
declared-selection conditionals (embedding RA-B1..B5; chiral 16 shadow
AC-D1..D5; stabilizer RA-A3) · G5 {INHERITANCE_BRIDGE} (LT-SM8
`named_condition` with its own advance-or-withdraw rule; RA-D4 conjunct-1
pricing) · G6 {SC-CHI-01 VEV if} (the source-conditional family; demand-side
occurrences LT-GR2d, RA-G3, AC-F1; bound to SG4 bit 2 by ST-1) · G7
{HYP-TW-COHERENCE-01 antecedent} (the ledger's own conditional-mechanism
hypothesis on LT-SM8) · G8 bucket: in-row stated conditionals (RA-F2).
Order: inclusion of assumption sets, empty set at top; a row occupies the
union of the conditions it carries (LT-SM8 at G5 ∪ G7). **The never-launder
law:** position moves only along recorded `migrations[]` arrows (258 records
at v0.259; exemplar AC-A1 `NEEDS/MISSING_CONSTRUCTION ->
SAME/DERIVED_CONDITIONAL ... not laundered to DERIVED`); moves toward the
top require a dated discharge receipt; the always-legal direction is more
indebted; enforcement receipt = IM-1's launder control (re-typing minted
AC-A1 unconditional MUST fail the validator) plus its permanent grep pin and
`rows_laundered: []`; taxonomy extension law
`NEW_KIND_REQUIRED__FORCED_FIT_FORBIDDEN`.

**Carrier category C (11 objects, 4 markers, 11 arrow classes + 2 non-arrow
tokens).** Objects: the four LAYER strata (ambient / observed / source-print
/ toy), the three CHIRALITY readings (S-FULL-DIRAC / S-HALF-OPPOSITE /
S-HALF-SAME — the last kept precisely so silent selection of a refuted
reading can be NAMED), and four homonym-disambiguated carriers
(`so(1,3)_endo`, `so(1,3)_H`, `ad(P_H)_{u(64,64)}`, and MD-1's ad-leg sense
written out as `Lambda^2` of the 14-dim chimeric carrier). Markers (legal,
name no object): UNTYPED, S-CHIRALITY-UNTYPED, N/A-on-spinor-free,
HOMONYM-AMBIGUOUS — the CN-2 principle in categorical form. Arrow labels:
the MAP-TYPE vocabulary with per-token receipts (projection = the razor for
the V3b error; contraction = WG-B06's corrected typing; inclusion;
restriction = V3a's surviving ground; pullback; pushforward and evaluation =
vocabulary members with no live exemplar yet, stated rather than implied;
quotient; isomorphism = `s^*` on the horizontal subbundle per VZ4-01;
homomorphism; intertwiner; plus not-a-map and UNTYPED as declared
non-arrow/unknown). Non-objects: bare registered homonym tokens, with the
register as the disambiguation surface and the gate-vs-register seed gap
recorded (D1).

## 2. The thirteenth-object question — answered, with the pressure points named

**No category demanded a thirteenth object.** Final counts: 4 / 8(+1 bucket)
/ 11, each under the budget of 12. The honest pressure points, so the next
reader knows where the budget will strain first:

1. **Carrier sits at 11 and its axes are already heterogeneous.** The next
   registered homonym that needs subscripted exemplars (the register holds
   38 tokens; only `so(1,3)`/`ad(P_H)` senses are carried here) pushes C to
   12 and then out. If that happens, the finding is that exemplar carriers
   do not belong in the grammar category at all and should live only in the
   register — a schema question for the channels, not a silent edit.
2. **G8 is a bucket by construction.** If in-row stated conditionals
   multiply (more RA-F2-class rows), the bucket hides structure; the
   ledger's own answer would be naming the conditions (the
   `named_condition` mechanism LT-SM8 already uses), not widening this
   file.
3. **The Source-Layer category is the one I believe is complete.** Four
   layers is ST-1's own closed enumeration, and every clause of ST-1's §5
   verdict table lands in one of the four. Nothing in CR-B, IV-20260815,
   VZ-4 or the register wants a fifth.

## 3. Typing disagreements recorded (not adjudicated)

Recorded in the reference §4 with both citations each: **D1** the gate's
seeded `HOMONYMS` constant (`so(1,3)`, `so(3,1)`, `ad(P_H)`) vs the register
(38 entries; no `so(3,1)`/`ad(P_H)` rows) — FX-2's declared supersession has
not been performed; both surfaces live; this build changes neither. **D2**
three senses of "layer" (gate stratum axis / source-construction layers /
six-axis Layer-0) — unregistered near-collision, flagged for the homonym
lane. **D3** `s^*` typed `pullback` (construction; SC-GEN-55/56) and
`contraction` (operation; WG-B06/VZ-4/MD-1) — reconciled as VZ-4 states it
(the false member of the triple is `projection`), both tokens kept. **D4**
the chiral-16-shadow selection (G3) and the VEV/phase condition (G6) are
adjacent in prose (CR-B §9; ST-1 §5) but unidentified in the ledger — kept
incomparable.

## 4. The codomain wiring receipt (before/after, measured)

Edit: `process_gates/typed_carrier_declaration_audit.py` gains a comment
block naming the reference as the LAYER/MAP-TYPE codomain, a
`codomain_drift()` function (reference path anchored to the gate's own
`__file__`; reads module constants; fails closed on missing
file/block/line/duplicate), one call in `audit()` that appends drift reds
and prints one `[codomain]` line, a `codomain_drift` stats key, and three
selftest controls (live green required; planted mismatch and missing
reference each required caught). CHIRALITY and OWNER vocabularies are
deliberately NOT wired (codomain owners CN-2 and FX-2, stated in the
reference §3.4).

Measured, not asserted:

- **Old gate vs new gate on the IDENTICAL final tree:** output
  byte-identical except one added line
  (`typed_carrier_declaration_audit[codomain]: ... match`); same red list
  (exactly the pre-existing
  `explorations/conditional-build/selected-k151-...-2026-08-17.md` red, a
  sibling lane's file, untouched); same exit code 1.
- **Scope counters across this session** moved 9 -> 15 files in dated
  scope, 8 -> 13 triggered, 11 -> 17 blocks by final validation: the delta
  is concurrent sibling lanes writing DURING the build (Arc-5's `vz5`,
  FIX-A's `fixa`, FIX-B's `pcx1`, a k151 hostile review) plus this build's
  two markdown files, both green in dated scope (the reference: in scope,
  untriggered, no block needed; this artifact: triggered, one live block,
  zero defects — the gate's own audit of exactly these two files returns
  scope 2, red 0). The red list never changed. Zero new reds from this
  build.
- **Gate `--selftest`:** SELF-TEST GREEN (clean fixture baseline first, the
  three new codomain controls, then all ten planted false facts caught);
  `--selftest --poison-baseline` still refuses with mutations NOT run, exit
  1.
- **FX-2's probe against the edited gate:** 67/67, exit 0, and its
  `--selftest` still catches 10/10 mutations after the edit — the donor
  instrument lost no power.

## 5. Worked example — this artifact's own typed declaration

```gu-typed-objects
result: CT-1 codomain wiring — the typed-carrier gate's LAYER and MAP-TYPE token sets equal the base-category reference's object/arrow token lines
carrier: token surfaces in process_gates/typed_carrier_declaration_audit.py and lab/methods/gu-base-categories.md section 3.4 LAYER=UNTYPED CHIRALITY=N/A
  # LAYER honestly UNTYPED: the carrier is repository text; the stratum axis does not apply, and declaring that is the CN-2-legal move
pairing: NONE
real_structure: N/A
grading: N/A
action_owner: repository-construction (CT-1 directed brief; enforcement lives in the gate's audit loop)
target: equality of the two token surfaces, checked every audit run MAP-TYPE=isomorphism
```

One declared-ambiguous slot (the LAYER marker), counted by the gate's
census — the same honest-residue pattern as FX-2's worked block 2.

## 6. HOSTILE REVIEW — the strongest attacks, answered or conceded

**"The Carrier 'category' is not a category."** Conceded at face value and
typed in the reference itself (§3, "Honesty about mathematical status"): it
is a schema presentation whose arrows are labels with dom/cod supplied per
use. The alternative — inventing dom/cod assignments the corpus never
declared — would coin structure. The Layer category and Grant poset are
genuine categories; the Carrier surface is the grammar the gate enforces.

**"LA3 typed `quotient` smuggles a BV/BRST quotient GU does not have."** The
reference anticipates this: the token types SC-CHI-01's *"splits at the
emergent level into two separate chiral theories"* as a declared
partition-into-sectors, with an explicit disclaimer that no constructed
BV/BRST quotient exists in the repository (IV-20260815 §3.2 is cited at the
arrow). The typing is of the source's sentence, not of an unbuilt
construction.

**"The Grant poset's order is presented upside down."** The reference states
the convention once, precisely: order by inclusion, DRAWN with the empty set
at top, so "moving to the top" = becoming unconditional — matching the
never-launder rule's phrasing. Both directions are spelled out; the law is
direction-explicit (toward-top requires a recorded discharge).

**"You wired only two of the gate's five vocabularies — the others can
still drift."** True, and deliberate: CHIRALITY's codomain owner is CN-2 and
OWNER's is FX-2; wiring them to THIS reference would create rival ownership,
the exact FX-3 failure class. The reference names both owners and leaves the
wiring to a later CT stage as an explicit, visible gap.

**"The reference's own tables could rot — receipts move, files rename."**
The probe resolves all 24 distinct receipt paths on every run and
byte-matches 49 load-bearing substrings; a renamed file or a reworded owner sentence is a
probe red, not silent rot. What the probe cannot catch is a receipt whose
file exists and sentence survives but whose MEANING is superseded — the
same ceiling every citation surface in this repository has, and the
supersession-hygiene rule (SCUR-1 §preflight: only a dated correction
supersedes) is the standing mitigation.

## 7. CERTIFICATE

`tests/channel-swings/joe_directed_ct1_base_categories.py` — **99/99
checks, exit 0** (validation run 2026-08-17).

```
  [P] 14   parse: reference exists; object/arrow schemas parse; IDs unique;
           counts 4 / 8 / 11 with <= 12 enforced per category; roles split
           object / arrow-class / markers / bucket / non-arrow; LA1-LA4,
           N1-N3, codomain block present; LA1 carries NOT-injective
  [R]  2   receipts: 24 distinct cited paths, every one resolves
  [C] 10   codomain: gate drift check green both ways; five planted drift
           fixtures (missing token, extra token, no block, missing file,
           duplicate token) each caught; CONTRARY reorder control passes
  [S] 49   exact substrings read from owners: ST-1 four-layer sentence;
           IV-20260815 not-competing-quotations + §3.3; VZ4-01 in canon;
           routing CAUTION contraction sentence; REDUCTION-FIDELITY; CR-B
           §4.1/§9; SC-GEN-55/56, SC-CHI-01, p.52 hedge; drafts L125 no-GUT,
           L119 2+1-imposter, L29 KK disavowal; SG4 bit 2 in canon;
           escape-corners REFUTED-AS-FILED; SA-1 intersect-in-zero + §2.3
           dims; IM-1 zero-launder pins; v0.259 grant/condition/migration/
           taxonomy/meter strings; register so(1,3) present, ad(P_H) and
           so(3,1) absent; six-axis and GVPO doctrine sentences; CN-2 token
  [F]  5   planted-false propositions, each observed False (a category over
           12; a bare-token object name; a dangling receipt; gate-reference
           drift; a G3=G6 identification)
  [D]  3   detector power on synthetic positives: a 13-object fixture, a
           dangling-receipt fixture, a bare-homonym-name fixture — each
           planted defect DETECTED by the probe's own checker
  [A/G] 16 this artifact (routing notice; INTERNAL_STRUCTURAL_ONLY under the
           routing audit's regex; NONE-NOT-A-KILL; registry write printed;
           exactly one live block, zero defects, exactly one declared-
           ambiguous slot; depends_on names the reference; scripts: names
           the probe) and runtime (gate --selftest GREEN by subprocess; the
           two new files audit green in dated scope; live scan printed as
           dated reconciliation; no float in any result surface)
```

Failure paths, both layers:

- **Probe `--selftest`:** clean baseline verified FIRST (subprocess exit 0
  required before any mutation; `--selftest --poison` forces the refusal
  path and exit 1), then 10/10 machinery/reference mutations each drive
  exit 1 via genuine `[FAIL]` lines: `ref-gone`, `row-regex-blind`,
  `limit-loose`, `root-elsewhere`, `layer-expect-drift`, `map-expect-drift`,
  `quote-drift`, `gate-gone`, `fence-blind`, `bare-name-blind`. A mutation
  run must still print its certificate line (a crash without one is
  CRASH-NOT-DETECTION and fails the selftest per VERIFICATION.md rule 3).
- **Gate `--selftest` (edited):** clean fixture baseline verified before any
  mutation; the codomain instrument's three controls (live green, planted
  mismatch caught, missing reference caught); then 10/10 planted false facts
  each exit 1; `--selftest --poison-baseline` exits 1 with the refusal
  printed.

```
_local/cas-venv/bin/python tests/channel-swings/joe_directed_ct1_base_categories.py
    -> CERTIFICATE: 99/99 checks pass; 0 planted-false observed true.      exit 0
_local/cas-venv/bin/python tests/channel-swings/joe_directed_ct1_base_categories.py --selftest
    -> clean baseline first, then 10/10 mutations each exit 1.             exit 0
_local/cas-venv/bin/python process_gates/typed_carrier_declaration_audit.py --selftest
    -> SELF-TEST GREEN.                                                    exit 0
```

## 8. POSTFLIGHT — six lenses, run after the build

**Schema reviewer.** Three categories, one file, five role labels, two
wired token surfaces, four recorded disagreements. No nesting, no YAML
dependency; the tables parse with two regexes and the codomain block with
one. The thirteenth-object budget was never approached in L, was consumed
to 8/12 in G, and sits at 11/12 in C with the strain point named (§2).

**Adversarial author.** Can I write a compliant artifact that cites the
reference while regressing a corrected fact? Yes — the reference cannot
police prose that never enters a typed block; its §5 guardrails are
citations, not enforcement. The enforcement surface remains the routing
method, the typed-carrier gate, and review. Recorded honestly here: this
reference REDUCES the reconstruction cost of the corrected facts to one
lookup; it does not make regression mechanically impossible.

**Gate maintainer.** The codomain check costs one file read per audit run
(measured: no visible runtime change; the live scan still completes in
about a second). Its failure messages name the exact drift. Extending a
vocabulary is now a two-surface, one-token-each diff — gate constant plus
reference line — and forgetting either surface is a red, which is the
point.

**Concurrency auditor.** Live sibling writes landed DURING this build
(vz5, fixa, pcx1, a k151 review — scope 9 -> 13 mid-session). Discipline
held: none of their paths was touched; the probe pins per-file facts and
prints repo-wide totals as reconciliation only; the before/after gate
comparison was run on the identical final tree so sibling writes cannot
masquerade as wiring effects.

**Source-fidelity check.** Every source sentence in the reference is quoted
with its locus and byte-matched by the probe; the five standing corrections
are carried as guardrails with their owners cited; polarity is preserved
(ambient indefiniteness attested, observed positivity OPEN not disavowed;
chirality VEV-conditional, not total; contraction, not projection; no GUT;
2+1 subtractive with `n_g` an input). Nothing here re-adjudicates any of
them.

**Retirement/rot auditor.** The reference rots three ways: receipts rename
(probe red), owners supersede quoted sentences with dated corrections
(probe red on byte-match, forcing a reference update — correct behavior),
and the categories themselves get extended silently (the gate equality
check makes the LAYER/MAP-TYPE case a red; the unwired vocabularies and the
G/L tables rely on the probe's counts and the thirteenth-object rule).
Retirement condition: if a later ratified surface absorbs the reference
(e.g. a chat-ratified methods consolidation), this file should be
superseded BY DATED CORRECTION, not deleted — the same supersession
hygiene it cites.

## 9. Blunt paragraph — where the categories are most likely wrong

Where I would bet against my own file, in order. **First, the Grant poset's
G6.** I typed "the VEV if" as one assumption-set object bound to SG4 bit 2,
but its ledger occurrences are three NEEDS rows whose distance texts name
DIFFERENT missing suppliers (a Lambda^5 singlet VEV selection, a dark-energy
VEV normalization, a physical-half selection); if those are three different
grants rather than one family with three demands, G6 is two-or-three objects
wearing one name — the exact homonym class this reference polices, sitting
in its own §2. The mitigation is that the binding to SG4 bit 2 is ST-1's,
not mine, and D4 already refuses the neighboring identification; but a
future ledger pass could legitimately split G6, and the reference §6's
thirteenth-object rule says that split is a REPORT, not an edit. **Second,
LA3's `quotient` token.** The split sentence is source-attested, but typing
a declared partition with a categorical token risks readers importing
quotient PROPERTIES (universal maps, descent) the source never states; the
disclaimer is at the arrow, but tokens travel farther than disclaimers — if
any downstream block cites LA3 to justify an actual quotient construction,
the token choice was too strong and should be revisited toward `not-a-map`
plus prose. **Third, the Carrier category's stratum objects.** I inherited
FX-2's LAYER axis wholesale (ambient/observed/source-print/toy), but
"observed" in that axis and L4 "VEV-observed" in the Source-Layer category
share a word while meaning different things (stratum of content vs
conditional phase), and D2 flags "layer", not "observed"; if a future
collision is paid for on "observed", this reference named the adjacent
hazard but not that one. What I am confident in: the four Source-Layer
objects and the three generating arrows, because every one is a direct
quotation of a settled, probe-pinned artifact — that part of the reference
is transcription, not modeling.
