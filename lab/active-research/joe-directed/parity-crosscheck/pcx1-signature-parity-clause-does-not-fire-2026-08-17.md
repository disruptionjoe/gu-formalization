---
artifact_type: exploration
status: exploration
doc_type: stewardship_record
created: 2026-08-17
work_item: PCX-1
channel: parity_crosscheck
title: "PCX-1: the PD-SIGNATURE-PARITY x HE-1 cross-check SCUR-1 requested, run to a verdict. The chain's pre-declared invalidation clause DOES NOT FIRE: the Kramers-constrained multiplicity lives at the ambient GU-native Cl(9,5)-carrier layer (canon no-go), HE-1/HE-2's subtractive n_g -> n_g - 1 lives at the complex-so(10) +/- package/comparator layer with n_g an INPUT, and CR-B places that comparator's only chiral input on the SG4-bit-2-conditional effective half — the layers do not meet, and no receipt identifies the two indices (the 2026-08-08 hostile review already recorded that none exists in the punchline's own direction either). SCUR-1's arithmetic is CORRECT at its layer (observed 3 = 4 - 1, exactly) and is banked; its firing is REFUSED at the layer boundary. Secondary legs: on (9,5) HE-2's symplectic doubling is uniform, so the subtraction preserves raw-multiplicity evenness (8 -> 6) for every n_g; and the clause's disjunct (b) requires a CONSTRUCTED odd-offset mechanism OUTSIDE the Kramers sector, while the candidate mechanism is an unconstructed partner-placement obligation whose (9,5) content sits inside the uniformly doubled sector. The chain's steering STANDS with a dated re-verification note PROPOSED (not applied: the registry schema has no self-execution provision). A typed firing condition is left armed."
grade: "ADJUDICATION with a byte-matched evidence probe and exact integer arithmetic; no float anywhere. Every operative sentence of the chain (YAML source of truth and generated .md), the canon no-go, HE-1's fences, HE-2's horn table and comparator rule, CR-B's verdict, the 2026-08-08 review precedent and SCUR-1's watch is byte-pinned (36 pins); the package-layer and (9,5)-raw-layer arithmetic is computed by an explicit engine; the verdict is COMPUTED by an explicit two-disjunct firing evaluator over grounded premises, with two contrary controls in which the same evaluator provably FIRES, so DOES-NOT-FIRE is discriminating rather than vacuous. 98/98, exit 0; --selftest verifies the clean baseline FIRST, then 6/6 machinery/reference corruptions each caught via a genuine [FAIL] (crash-not-detection rejected). NOT: a physics derivation, a canon or ledger edit, a fork resolution, a verdict movement on HE-1/HE-2/CR-B/SCUR-1, or any edit to path-dependencies.yaml/.md."
target_claim: "INTERNAL — SCUR-1's supersession-watch (scur1-source-currency-audit-2026-08-17.md, section 2 and section 6 item 7), verbatim: 'HE-1's forced subtractive partition (observed 3 = n_g − 1 ⟹ constrained multiplicity n_g = 4, EVEN) plausibly fires the chain's own pre-declared invalidation clause'; and, as adjudicand, PD-SIGNATURE-PARITY's own invalidates_if clause in lab/process/path-dependencies.yaml. No GU source claim is targeted."
target_claim_verdict: "SPLIT, claim-indexed, led by the failure. The FIRING does not survive: the clause does not fire, because the multiplicity SCUR-1's sketch feeds into it lives at a different layer from the index the clause constrains, and the identification that would connect them is constructed by no receipt (HE-1 Fence 3 and the HE-1/HE-2/CR-B NOT-lists refuse it explicitly; the 2026-08-08 hostile review records that the punchline's own direction was never constructed either). Two compression slips in the sketch are named: the clause does not 'name a parity as invalidating' (it names an identification failure and an outside-the-sector odd offset), and the candidate offset is a '-1', not the clause's '+1' (parity-equivalent; recorded as a wording nit so no future firing stalls on a sign technicality). BANKED: SCUR-1's arithmetic is exact at its own layer — observed 3 = n_g - 1 forces n_g = 4, EVEN, at the +/- package layer — and its instinct that nobody had re-run the chain was right; this file is that re-run. The chain's steering stands."
canon_verdict_change: none
ledger_edit: none
priority_change: none
steering_effect: "unchanged — PD-SIGNATURE-PARITY's steering STANDS, now with a dated re-verification receipt; a schema-conformant chain step recording this cross-check is PROPOSED to the process steward in section 6, not applied."
canonical_effect: "none — adjudication only. path-dependencies.yaml/.md are untouched: the registry's generator schema (process_gates/path_dependency_audit.py: id, headline, trigger, naive, chain, check, traps, invalidates_if, related) carries no status-note or self-execution mechanism, so the step-4 self-execution condition of this task is unmet by construction and any registry change is the owner's."
depends_on:
  - lab/process/path-dependencies.yaml
  - lab/process/path-dependencies.md
  - canon/no-go-quaternionic-parity-generation-sector.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he1-imposter-separation-invariant-2026-08-14.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he2-real-form-does-not-pair-144-with-144bar-2026-08-15.md
  - lab/active-research/joe-directed/carrier/crb-carrier-is-four-corners-not-one-weyl-2026-08-15.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/source-currency/scur1-source-currency-audit-2026-08-17.md
  - lab/process/hostile-reviews/2026-08-08-majorana-weyl-conditional-resolver-review.md
  - explorations/twentyfive-lens-council-on-the-signature-decision-2026-08-08.md
  - process_gates/path_dependency_audit.py
scripts:
  - tests/channel-swings/joe_directed_pcx1_signature_parity_clause_does_not_fire.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact borders the
> fork-1 conventional comparator (ordinary net chirality, `n_g`). Any statement
> here about `n_g - 1` binds only that comparator at its stated layer and is
> not evidence for or against Weinstein's source-native mechanism without a
> typed bridge. Read `lab/methods/source-native-comparator-routing.md`.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY` — the whole content of this
> file is the boundary: it adjudicates whether a comparator-layer multiplicity
> may be transported into a registry chain whose constrained index lives on the
> ambient carrier. It answers no, for now, and types the condition under which
> the answer flips.

```gu-typed-objects
result: PCX-1 adjudication — PD-SIGNATURE-PARITY's invalidates_if clause evaluated against HE-1/HE-2/CR-B at their stated layers
carrier: registry text (lab/process/path-dependencies.yaml) and the banked HE-1/HE-2/CR-B results at their declared layers LAYER=UNTYPED CHIRALITY=N/A
# LAYER is UNTYPED for the same reason as SCUR-1: the closed vocabulary has no token for repository text about the repository; the two PHYSICS layers under adjudication are typed inline (ambient GU-native Cl(9,5) carrier vs complex-so(10) +/- package comparator).
pairing: NONE
real_structure: N/A
grading: N/A
action_owner: repository-construction
target: whether the chain's pre-declared invalidation clause fires on 2026-08-17 MAP-TYPE=not-a-map
```

# PCX-1 — the parity chain survives its own invalidation clause; the sketch dies at the layer boundary

SCUR-1's one finding that "could move a live steering surface": HE-1's
subtractive partition plausibly fires `PD-SIGNATURE-PARITY`'s own pre-declared
invalidation clause, and nobody had re-run the chain since 2026-08-14. This
file is the re-run, to a verdict, with the clause text, the arithmetic and the
verdict pinned by `tests/channel-swings/joe_directed_pcx1_signature_parity_clause_does_not_fire.py`
(98/98, exit 0; selftest 6/6).

## 0. PREFLIGHT — six problem-matched lenses, declared before the reads

1. **Kramers/parity arithmetic.** Every parity statement must be computed on a
   named integer at a named layer, never inherited. Committed in advance: the
   probe computes `net_chirality`, the (9,5) raw doubling, and every parity by
   engine; SCUR-1's "plausibly" is not inherited in either direction.
2. **Layer/type discipline.** The chain's constrained multiplicity and HE-1's
   subtracted multiplicity must each be assigned their owner's own layer
   declaration, quoted; transport between them requires a constructed
   receipt or it is refused (`PD-STRUCTURE-TRANSPORT`'s own rule, applied to a
   chain about parity). The brief's hostile question — *are you importing
   HE-1's subtraction into a chain whose multiplicity lives at a different
   layer?* — is treated as the central question, not a footnote.
3. **Steering-surface governance.** A registry chain is a steering surface: a
   wrong survival keeps dead steering alive, a wrong kill retires live
   steering. Both failure modes priced before reading; the generator schema is
   checked for a self-execution provision BEFORE any thought of editing.
4. **Claim-indexed verdicts.** The adjudicand is the clause's own two
   disjuncts, quoted, each evaluated separately; the internal target is
   SCUR-1's watch sentence, and the verdict on it leads with what fails and
   banks what survives (the week's misaimed-critique discipline).
5. **Adversarial reading in BOTH directions.** The strongest pro-firing
   construction (the source's own subtractive account) and the strongest
   anti-firing construction (comparator fencing) are both built and answered
   in section 5 before the verdict is banked.
6. **Evidence-class philology.** The clause says "is shown". The chain's own
   evidence vocabulary (EXACT | THEOREM | AUTHOR-STATED | CONDITIONAL | OPEN)
   and its own check sentence ("a named SOURCE resolver, not a mathematical
   settlement") supply the bar that "shown" must clear; AUTHOR-STATED does not
   clear it, by the chain's own standard.

**Retrieval before work.** The adjacent prior adjudication is the 2026-08-08
Majorana-Weyl conditional-resolver hostile review, which read this same clause
eleven days ago and recorded the state of the identification then. It is
quoted in section 2.4 and byte-pinned; this file's verdict is continuous with
it, not novel against it.

## 1. The chain, its punchline, and the clause — quoted in full

Owner of record: `lab/process/path-dependencies.yaml` (the generated
`lab/process/path-dependencies.md` renders it; "Edit the YAML, never this
file"). The chain's registry entry, verbatim where operative:

**Punchline (headline):**

> The ambient horn is not cosmetic: (9,5) forbids an odd generation count
> and (7,7) permits it. A computation on the wrong horn cannot reach three.

**Trigger:** "Any work touching the generation count, chirality, spinor
reality, Kramers/quaternionic structure, or that builds on a Cl(9,5) carrier."

**The two evidence rows under adjudication:**

> - fact: "Quaternionic structure forces Kramers doubling, hence EVEN
>   multiplicity."  — evidence: THEOREM, receipt:
>   `canon/no-go-quaternionic-parity-generation-sector.md`
> - fact: "Three generations is ODD. So (9,5) structurally forbids the
>   target; (7,7) permits it."  — evidence: CONDITIONAL, receipt:
>   `explorations/twentyfive-lens-council-on-the-signature-decision-2026-08-08.md`

**The check (what the chain licenses/forbids downstream):**

> State which horn the work stands on. If Cl(9,5), say so and say whether the
> result is horn-robust. Treat equation (12.19) as a named SOURCE resolver,
> not a mathematical settlement. Do NOT let a (9,5) result silently stand in
> for the source-aligned reconstruction.

**The pre-declared invalidation clause (`invalidates_if`), verbatim:**

> The generation count is shown NOT to be the index whose parity Kramers
> constrains; or a mechanism supplies the odd '+1' outside the Kramers-
> constrained sector.

Two disjuncts. (a) an identification failure — the observed generation count
is *shown* not to be the Kramers-constrained index; (b) a locational fact — a
mechanism supplies an odd offset *outside* the Kramers-constrained sector.
Note what the clause does **not** say: it does not name any parity value as
invalidating. SCUR-1's compression ("…makes the Kramers-constrained
multiplicity 4, EVEN, which is exactly the parity its clause names as
invalidating") re-imported a frame the clause does not contain — an even
multiplicity in the Kramers sector is what row 6 *asserts*, not what the
clause *fears*. The relay into this task inherited the same compression. The
adjudication below works from the clause's actual text.

## 2. The owners, at their own stated layers

### 2.1 The constrained index (row 6's receipt): ambient, (9,5), GU-native

`canon/no-go-quaternionic-parity-generation-sector.md` (THEOREM,
reconstruction-grade): every operator built from GU's own building blocks on
the `Cl(9,5) = M(64,H)` reconstruction module commutes with `J_quat`; by
Kramers, every GU-native Hermitian carrier there has even signature.

> GU's quaternionic structure forces an EVEN generation index. An odd count
> such as 3 cannot arise from GU's own building blocks; **reaching an odd
> index requires importing a non-quaternionic (non-Clifford) object.**

Three of its own qualifications are load-bearing here, all quoted and pinned:
"**Reading** the generation count as the index of such a carrier, the count is
forced even" — the identification is a *reading*, hypothesized, not derived;
C-06: "**GU neither forces nor forbids three generations; it under-determines
the count**"; and the signature caveat: the wall "DISSOLVES under a defensible
alternative real-class signature such as (7,7)". **Layer of the constrained
multiplicity: the index/signature of a GU-native Hermitian carrier on the
ambient (9,5) reconstruction module.** On (7,7) there is no Kramers constraint
for the clause to defend at all.

### 2.2 The subtracted multiplicity (HE-1, 2026-08-14): complex so(10) package layer

HE-1's map — "`n_g` chiral families plus one `144` leaves net chirality
`n_g - 1`" (§3.6, exact, 62/62) — with its own fences, inherited unchanged by
HE-2:

> **FENCE 3 — multiplicity, not index.** Every number below is a multiplicity
> in a branching […] Per repository rule, **no count here is a generation
> count.** The net-chirality result takes `n_g` as an *input* and returns
> `n_g - 1`; it does not derive `n_g`.

Grade line: "NOT: an index, a generation count, a physical-carrier statement,
a real-form statement…". **Layer: complex-so(10) ± package content;
comparator.**

### 2.3 HE-2 (2026-08-15) and CR-B (2026-08-15): horn stability and the carrier condition

HE-2 makes the subtraction real-form-stable on **both** horns — and its (9,5)
column is exactly what blocks the import of the subtraction into the Kramers
sector as a parity change: "(9,5) admits none [no Majorana-Weyl], and requires
symplectic doubling. **That doubling is uniform** — it **doubles families and
`144`s alike** — so it rescales the arithmetic and does not change its sign."
Its §5 typing rule: the net-chirality readout is fork-1's conventional
comparator, and "a comparator result cannot advance a GU row in *either*
direction." Its NOT-list refuses "an index, a generation count, a physical
carrier."

CR-B prices the carrier horns the subtraction is conditional on:

> On the total declaration the conventional n_g comparator has no chiral
> input; on a source-stated effective half it does. Which effective split is
> dynamically realized remains SG4 bit 2 and is not constructed here.

So the only horn on which HE-1's `n_g` even *has* a chiral input is the
SG4-bit-2-conditional effective half — downstream of the VEV/decoupling
selection, not the ambient layer where the no-go's carrier lives.

### 2.4 The precedent: the identification was never settled true either

`lab/process/hostile-reviews/2026-08-08-majorana-weyl-conditional-resolver-review.md`
read this clause eleven days before SCUR-1 and recorded the standing state:

> No listed receipt constructs the GU operator whose index is the observed
> family count. Therefore "(9,5) forbids three generations" overstates the
> theorem. […] This is exactly the invalidation clause already carried by
> `PD-SIGNATURE-PARITY`: if generation count is not the index whose parity is
> constrained, **the downstream link does not fire.**

The identification "observed count = Kramers-constrained index" is
unconstructed **in both directions**: never shown true (2026-08-08, still
true — nothing in HE-1/HE-2/CR-B claims it, per their NOT-lists), never shown
false. Row 7 carries evidence class CONDITIONAL for exactly this reason, and
still does (probe check `span:row7-conditional`).

## 3. The exact arithmetic, layer by layer — no floats, no inheritance

At the **± package layer** (the only layer where `n_g - 1` is defined):

```
subtractive reading (HE-1):   observed 3 = n_g - 1   =>   n_g = 4     4 % 2 = 0  EVEN
identity reading (row 7):     observed 3 = n_g       =>   n_g = 3     3 % 2 = 1  ODD
HE-1 §3.6, pinned:            "n_g = 4:  family blocks net  3"
```

**SCUR-1's arithmetic is exact and correct at this layer**: if the observed
three are subtractive, the package-layer multiplicity behind them is 4, even.
Banked.

At the **(9,5) raw layer** (HE-2 §3.3, uniform symplectic doubling):

```
before subtraction:  2 x 4 = 8   EVEN
after subtraction:   2 x 3 = 6   EVEN
sweep n_g = 1..8:    2·n_g and 2·(n_g - 1) EVEN for every n_g   (probe-computed)
```

The subtraction never manufactures an odd raw multiplicity on (9,5): both
sides of the pairing are inside the uniformly doubled sector, so Kramers
evenness of raw multiplicities is preserved through it. Whether one observed
family corresponds to one symplectic-Majorana-Weyl pair or one raw Weyl copy
is exactly the `dim_R S± / dim_C S` homonym the repository fences
(HE-1 Fence 2's adjudication citation); no owner adjudicates it and this file
does not either.

At the **ambient GU-native carrier layer** (where the clause's index lives):
there is **no number to compute**, because no receipt constructs the carrier
whose index is either `n_g` or `n_g - 1` or the observed count. That absence
is the verdict's ground, and it is pinned at all four owners (§2.4 plus the
three NOT-lists).

## 4. The adjudication — computed, disjunct by disjunct

**Disjunct (a) — "The generation count is shown NOT to be the index whose
parity Kramers constrains." DOES NOT FIRE.** For (a) to fire, some receipt
must establish what the Kramers-constrained index *is* (an identification)
and that it differs from the observed count. No such receipt exists: HE-1
refuses ("no count here is a generation count"; `n_g` an input), HE-2 refuses
("NOT: an index, a generation count, a physical carrier"), CR-B refuses (same
words, and places the comparator's chiral input on an SG4-bit-2-conditional
half "not constructed here"), and the 2026-08-08 review records that the
punchline's own direction was never constructed either. What the week
supplies is: AUTHOR-STATED at the source layer (the subtractive "really two
plus one", register item 5) plus EXACT at the comparator package layer
(`n_g -> n_g - 1`) plus UNCONSTRUCTED at the ambient carrier layer. By the
chain's own evidence discipline — its check demands that even an authorial
equation be treated as "a named SOURCE resolver, not a mathematical
settlement" — that stack is not "shown". **The layers do not meet.**

**Disjunct (b) — "a mechanism supplies the odd '+1' outside the
Kramers-constrained sector." DOES NOT FIRE.** The candidate mechanism is the
144's mirror-block Dirac pairing removing one family at Pati-Salam breaking:
an odd offset of **−1** (the clause writes '+1'; parity-equivalent as an
offset — recorded as a wording nit for the owner so no genuine future firing
stalls on the sign). It fails (b) twice over. *Location:* on (9,5) — the only
horn with a Kramers-constrained sector — the 144 content is subject to the
same uniform symplectic doubling as the families ("doubles families and
`144`s alike"), i.e. the pairing's carrier sits inside the constrained
sector, and the no-go's own escape clause requires the odd import to be
FOREIGN ("importing a non-quaternionic (non-Clifford) object") — nothing
banked exhibits that. *Construction:* the mechanism is not constructed at
all — HE-2's correction block retypes it as "a partner-placement and
physical-decoupling obligation", and the insertion that would feed it is
SG4 bit 2, open by design (ST-1). An unconstructed mechanism cannot have been
*shown* to sit anywhere.

**VERDICT: CLAUSE DOES NOT FIRE.** Typed, in order of force:

1. **Layer mismatch (primary).** The constrained multiplicity is an ambient
   GU-native (9,5)-carrier index; the subtracted multiplicity is a
   complex-so(10) package/comparator number whose only chiral input lives on
   the SG4-bit-2-conditional effective half. No identification receipt
   connects them, in either direction. SCUR-1's sketch dies here — as a
   *firing*. Its arithmetic is banked (section 3).
2. **Arithmetic (secondary).** Even force-transported to (9,5), the banked
   uniform doubling makes the subtraction parity-preserving on raw
   multiplicities (8 → 6, even → even, all `n_g`): the sketch's premise that
   the subtraction changes the constrained parity is not what HE-2 banked.
3. **Conditionality (tertiary).** `n_g` is an input; the comparator rule is
   symmetric ("cannot advance a GU row in *either* direction"); the effective
   split is unconstructed. Every rung of the would-be firing is conditional
   on an object that does not yet exist.

**Why not CONDITIONAL (fires on one horn):** no horn currently fires it. On
(7,7) there is no Kramers constraint to invalidate (the no-go dissolves there
by its own caveat — the clause has no work to do). On (9,5) the firing needs
the identification receipt, which does not exist. The SG4-bit-2 effective
half is where a genuine firing would naturally be built — that is a **watch
with a typed trigger**, not a horn-typed firing, and it is left armed:

> **FIRING CONDITION (armed, typed).** The clause fires the day a receipt
> constructs the identification of the Kramers-constrained ambient carrier
> index with the package count `n_g` on the SG4-bit-2-selected effective half
> — then observed 3 = 4 − 1 makes the constrained index EVEN, disjunct (a)
> fires, and row 7 with the headline's "cannot reach three" dissolves — or
> the day a constructed mechanism supplies the odd offset via a non-GU-native
> (foreign) object outside the J_quat-commutant, firing disjunct (b). The
> probe's contrary controls A and B are precisely these two configurations,
> and the evaluator fires on both.

## 5. Hostile review inline — both directions

**The brief's named attack — "are you importing HE-1's subtraction into a
chain whose multiplicity lives at a different layer?"** That attack is
correct, and it lands on SCUR-1's sketch, not on this verdict: the sketch's
phrase "the Kramers-constrained multiplicity" presupposes the constrained
index *is* `n_g`, which is the unconstructed identification. This file's
verdict is that the import fails; the attack and the verdict are the same
fact.

**Strongest PRO-FIRING construction, built honestly.** "The source's own
account — subtractive 2+1 (drafts L119/L128/L131), VEV-conditional effective
chirality (L158), four-corner non-chiral total (CR-B) — already entails that
the observed 3 is an effective, post-decoupling count, not an ambient carrier
index. So disjunct (a) is shown by the source itself; stop hiding behind
'unconstructed'." Answer, three parts. (i) The clause's bar is *shown*, and
the chain's own check sentence sets that bar above authorial statement
("a named SOURCE resolver, not a mathematical settlement") — the same bar
row 3 of the chain applies to equation (12.19) itself. (ii) The no-go
pre-priced this exact posture in C-06: the count is under-determined, the
literal-index reading is a *reading* — the chain never asserted the
identification as settled, which is why row 7 is CONDITIONAL. A conditional
cannot be invalidated by making its antecedent *less likely*; only by showing
it false. (iii) What the source attests is a route whose selector
(SG4 bit 2) and partner placement are open obligations; the day they are
constructed, the firing condition above fires and this verdict flips by its
own stated rule. The pro-firing construction is thus not refuted — it is
*scheduled*, with a typed trigger.

**Strongest ANTI-VERDICT construction from the other side.** "The verdict
proves too little: if no identification exists in either direction, the chain
's punchline is equally ungrounded and should be retired anyway." No: the
punchline's rows 1–5 (horn non-isomorphism, notation, Majorana-Weyl
availability) are EXACT/AUTHOR-STATED and untouched — HE-2 independently
re-derived rows 4–5 verbatim ((7,7) REAL, (9,5) QUATERNIONIC, MW only on
(7,7)). The generation-count consequence is carried by row 7, which is typed
CONDITIONAL in the registry itself. A chain whose conditional step is marked
CONDITIONAL and whose invalidation clause is armed and now receipted is a
*healthy* steering surface. Retiring it would discard the exact rows that the
k134 wave and BD-2 cite it for (section 7).

**What this file could still be wrong about, stated.** (i) The premise
"no identification receipt exists" is verified at the named owners and the
2026-08-08 review, not by an unbounded repo sweep; a receipt unknown to all
five surfaces would flip disjunct (a) — the firing condition names exactly
this. (ii) The inside-the-sector location of the 144 pairing on (9,5) rests
on HE-2's uniform-doubling sentence plus the no-go's all-GU-native-blocks
theorem; a constructed mechanism could in principle be built foreign — the
firing condition names that too. (iii) The SMW-pair-vs-raw-copy counting
homonym is left unadjudicated, deliberately, with its fence cited.

## 6. Applied vs proposed

**Applied: nothing.** The registry's generator
(`process_gates/path_dependency_audit.py`) defines a fixed schema — `id,
headline, trigger, naive, chain, check, traps, invalidates_if, related` —
with no status-note, firing, or self-execution mechanism; the chain's own
text pre-authorizes nothing on invalidation. Step 4's self-execution
condition is therefore unmet by construction, and — the verdict being
DOES-NOT-FIRE — there is no invalidation to execute anyway.
`lab/process/path-dependencies.yaml` and `.md` are untouched by this task.

**Proposed to the process steward (owner's call), schema-conformant:** append
one chain step to `PD-SIGNATURE-PARITY` in the YAML, after step 7, and
regenerate the `.md` with `--write`:

```yaml
      - fact: >-
          Cross-checked 2026-08-17 (SCUR-1's watch): HE-1/HE-2's subtractive
          observed-3 = n_g - 1 lives at the +/- package/comparator layer with
          n_g an INPUT and no chiral input on the four-corner total
          declaration (CR-B); no receipt identifies the Kramers-constrained
          ambient carrier index with n_g, and on (9,5) the symplectic
          doubling is uniform, so the invalidation clause does NOT fire. It
          fires the day a receipt constructs that identification on the
          SG4-bit-2 effective half (observed 3 = 4 - 1 then makes the
          constrained index EVEN and step 7 dissolves), or a constructed
          foreign mechanism supplies the odd offset outside the sector.
        evidence: EXACT
        receipt: tests/channel-swings/joe_directed_pcx1_signature_parity_clause_does_not_fire.py
```

This supersedes SCUR-1 §7's proposed note for the same locus (same owner,
same file) with the cross-check now run rather than requested. Two mechanical
constraints for the owner, verified here: the k134 probe byte-requires both
`PD-SIGNATURE-PARITY` and `cannot reach three` in the generated `.md`
(pinned: `tests/channel-swings/selected_k134_...probe.py:57`), and this
file's probe requires row 7 to remain the chain's single CONDITIONAL step —
the proposed step is additive and EXACT, so both survive. If the owner
instead rewords existing rows, this probe's pins go red by design (the ageing
signal, per SCUR-1's pattern).

## 7. Downstream census — who cites the chain, and what each inherits

Every current citer of `PD-SIGNATURE-PARITY` and its punchline, with what it
inherits from this verdict:

| citer | what it uses | inherits |
|---|---|---|
| `explorations/conditional-build/selected-k134-native-i1b-t0-kappa-hodge-fingerprint-and-fourier-pencil-2026-08-16.md` §0.1 (+ its probe line 56–57, + its 2026-08-16 hostile review line 36) | the chain as the fork discipline: stands on `Cl(7,7)`, no silent port to `Cl(9,5)` | **unchanged and re-receipted** — it consumes rows 1–5 (horn non-transportability), which HE-2 independently re-confirmed; its probe's byte-pin on "cannot reach three" is safe under the proposed additive diff |
| `lab/active-research/joe-directed/baryon-number-and-proton-decay/bd2-su32-image-retains-the-classic-xy-half-2026-08-17.md` §4.1 | step 0 only (the (6,4) fibre is base-sign independent, EXACT) | **unchanged** — nowhere near rows 6–7 |
| `process_gates/needs_provides_composition_audit.py:797` | names the chain as "the composed evidence chain" for the `SIGNATURE-AMBIENT::Cl(7,7)` horn token | **unchanged** — the composition claim is about rows 1–5; had the clause fired, this row would have needed re-typing, which is exactly why the census was owed |
| `lab/process/hostile-reviews/2026-08-08-majorana-weyl-conditional-resolver-review.md` | the clause itself, as the guard that kept a conditional from becoming a resolver | **strengthened** — its "the downstream link does not fire" reading is now receipted from the other direction |
| `lab/active-research/joe-directed/source-currency/scur1-source-currency-audit-2026-08-17.md` §2, §6 item 7, §10 | the watch this file answers | **discharged** — the watch converts to the armed, typed firing condition of section 4; SCUR-1's package-layer arithmetic is banked correct |
| `canon/no-go-quaternionic-parity-generation-sector.md` (row 6's receipt, upstream) | supplies the THEOREM row | **untouched** — its own banner discipline (reopener = SIGNATURE-AMBIENT settling) is orthogonal to this clause and still governs; its C-06 under-determination is load-bearing in this verdict |
| `explorations/twentyfive-lens-council-on-the-signature-decision-2026-08-08.md` (row 7's receipt, upstream) | supplies the CONDITIONAL row ("the decisive form is lens 4") | **untouched** — row 7 remains CONDITIONAL, which this verdict confirms is its correct type |

No citer currently consumes rows 6–7 as a settled prohibition; the punchline
steers through the check sentence (declare your horn), and that steering
stands.

## 8. POSTFLIGHT — five lenses, after the work

1. **Verdict-inflation auditor.** The tempting inflations were both refused:
   "the chain is vindicated" (no — row 7 remains CONDITIONAL and the firing
   condition is armed, typed, and closer to constructible than it was a week
   ago) and "SCUR-1 was wrong" (no — its arithmetic is exact at its layer,
   its watch was the correct instinct, and this file exists because of it).
2. **The opposite failure, wrong-survival.** A wrong DOES-NOT-FIRE keeps dead
   steering alive. Priced: the verdict's ground is an absence (no
   identification receipt), verified at five surfaces and armed with the
   exact condition that flips it; the probe's contrary controls prove the
   evaluator CAN fire, so the survival is not an artifact of a dead detector.
3. **Comparator-boundary auditor.** No comparator number was converted into a
   GU verdict: `n_g - 1` enters only as the package-layer arithmetic SCUR-1's
   sketch already used, is banked at that layer, and is blocked at the
   boundary — which is the verdict itself.
4. **Concurrency auditor.** Writes: this file, its probe, and nothing else.
   `vz-repair/` (Arc-5), `explorations/conditional-build/` and
   `source-currency/` (FIX-A) untouched; `path-dependencies.yaml/.md`
   untouched; no git operations; shared-checkout discipline held.
5. **Reader-of-the-future.** The one sentence a later agent needs: *the
   parity chain's "cannot reach three" survives because nobody has yet built
   the object that would connect the observed three to the index Kramers
   actually constrains — the day someone builds it on the SG4-bit-2 half, the
   clause fires at observed 3 = 4 − 1, and the probe's control A is already
   the shape of that firing.*

## 9. Certificate

```
_local/cas-venv/bin/python tests/channel-swings/joe_directed_pcx1_signature_parity_clause_does_not_fire.py
    -> 98/98 checks, exit 0
       (35 byte pins across 9 files; 35 needle guards; 9 file-presence
       checks; 2 chain-span checks, one confirming row 7 is the chain's
       single CONDITIONAL step; 7 exact-arithmetic checks including the n_g
       sweep; 4 evaluator checks: live DOES_NOT_FIRE with empty disjunct
       list, contrary control A fires disjunct (a), contrary control B fires
       disjunct (b); 6 planted-false assertions each observed False)
_local/cas-venv/bin/python tests/channel-swings/joe_directed_pcx1_signature_parity_clause_does_not_fire.py --selftest
    -> clean baseline verified FIRST (98/98, exit 0), then 6/6
       machinery/reference corruptions each caught via a genuine [FAIL] and
       red exit (path-table redirect; reference-needle tamper EVEN->ODD;
       net_chirality drops the subtraction; smw_raw breaks uniform doubling;
       fabricated identification receipt — which flips the live verdict to
       FIRES and is caught by the verdict pin, demonstrating the detector's
       discrimination on the exact premise at issue; blanked-needle guard).
       Crash-not-detection rejected (a red exit without a [FAIL] line fails
       the selftest; Traceback in stderr fails it too); exit 0
```

Ageing, by design: the pins quote live owner text. If the process steward
applies the proposed step 6 diff, all pins survive (additive). If any owner
rewords a pinned sentence, the corresponding pin goes red — the signal to
re-baseline this probe against the new text, not to trust it silently.

## 10. Blunt paragraph — which way the pressure ran

The pressure ran toward FIRES, from three directions at once: the task
arrived pre-narrated ("plausibly DISSOLVED … and it is still steering"), a
kill is the more interesting deliverable than a survival, and SCUR-1's
arithmetic is genuinely correct and genuinely seductive — 3 = 4 − 1 is exact,
and 4 is even, and it takes real effort to notice that the clause never asks
whether 4 is even; it asks whether anyone has *shown* which object the 4
belongs to. I resisted by doing the one thing the sketch did not: reading
row 6's receipt to its own layer declaration, where the no-go turns out to
have pre-priced the entire episode (the identification is a "reading", the
count "under-determined") — and by finding that the 2026-08-08 hostile review
had already adjudicated the identification's absence in the punchline's OWN
favor-direction, which makes DOES-NOT-FIRE the symmetric, not the timid,
verdict: the same missing receipt that keeps the punchline from being a
theorem keeps the clause from firing. The residual temptation was then the
opposite one — to round the survival up to vindication — and the guard
against that is the armed firing condition: this chain is one constructed
identification away from losing its punchline, the probe already contains the
firing's exact shape as control A, and the proposed registry step hands the
owner the tripwire. Survival with a loaded gun on the wall is the honest
verdict; both quiet endings were refused.

---

*Filed 2026-08-17, PCX-1, parity-crosscheck channel. Adjudication only: no
canon, ledger, registry, fork, verdict, count or bar moves on this artifact;
the one registry change is PROPOSED in section 6 and owned by the process
steward. Writes: this file and its probe. No git.*
