---
artifact_type: exploration
status: exploration
doc_type: versionless-ledger-delta
created: 2026-08-15
work_item: LA-1
channel: conditional_ledger_advancement
title: "LA-1: the 'embedding selected' grant is not a second grant -- it is the stabilizer grant, and the embedding inside it is worth 0 bits. Six rows get a unified named condition and a narrowed revival trigger at UNCHANGED verdict and reason kind. Group A is empty: both STALE_PREMISE rows were already fully dispositioned before this swing. Zero verdicts moved."
grade: "EXACT finite weight-lattice arithmetic over the 16 of so(10) in fractions.Fraction, 36/36, zero floats, seven named negative controls plus a 142-line exact sweep of the T3R/(B-L) 2-plane. NOT: a source action, a vacuum, a stabilizer selection, a carrier projection, a chirality mechanism, a generation count, a compactness proof independent of the source's own maximal-compact step, or any verdict/reason-kind movement."
disposition: GROUP_A_EMPTY_ALREADY_BANKED__EMBEDDING_GRANT_COLLAPSES_INTO_STABILIZER_GRANT_ZERO_BITS__SIX_ROWS_CONDITION_UNIFIED_AND_REVIVAL_NARROWED__AC_D_CASCADE_COMPARATOR_ROUTED_NOT_ADVANCED__RA_C1_TYPING_SEAM_ESCALATED
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
steering_effect: unchanged
canonical_effect: pending_integration
base_revision: a148ed80
ledger_base: lab/process/conditional-physics-ledger-v0.258.json
ledger_edit: none -- this document is a versionless delta for the canonical owner to disposition
depends_on:
  - lab/process/conditional-physics-ledger-v0.258.json
  - lab/methods/source-native-comparator-routing.md
  - explorations/conditional-build/cb-a-representation-content-2026-08-05.md
  - explorations/channel-swing-CH-SM-2026-07-19.md
  - lab/active-research/pati-salam-chain-verification.md
  - lab/active-research/joe-directed/photon-extra-vector-spectrum/pv2-observation-cannot-reach-the-extra-vectors-2026-08-14.md
  - explorations/precontract-wave-0c-typed-identity-theorem-scope-2026-08-05.md
scripts:
  - lab/active-research/joe-directed/ledger-advancement/la1-hypercharge-line-zero-bit-recheck.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# LA-1 — the embedding grant is the stabilizer grant

## Result first, without inflation

**Zero verdicts moved. Zero reason kinds moved.** Six rows carry a proposed
condition / revival-trigger / mapping-grade migration at unchanged
`verdict` and `reason_kind`. That is the whole positive product of this swing,
and it is smaller than the candidate pool suggested.

Three findings carry the delta.

**1. Group A is empty. Both `STALE_PREMISE` rows were already fully
dispositioned before this swing began.** `AC-G1` carries
`row_status: SUPERSEDED` with successor `AC-G1a`; it is already outside the
82-row active denominator, so there is nothing to retype. `LT-SM3b` is a faithful
transcription of the ledger disposition prescribed by its own evidence file
(`precontract-wave-0c-...:137-146`): its verdict, reason kind and revival trigger
are **verbatim** from that block, and its distance is a faithful paraphrase of
the same sentence (evidence: *"keep the exact gamma-traceless T3 line, separate
it from the adjoint Shiab, and extend the Riemann adapter through the action"*;
ledger: *"separate source adjoint Shiab from reconstructed spinor vertex and
extend the Riemann adapter through the action"*). Its premise is not outdated; it
is correctly recorded as having *been* stale. The "cheapest genuine wins on the
board" had already been banked.

**2. The RA-B rows' grant and RA-A3's grant are the same grant, and the
"embedding" inside it is worth 0 bits.** `RA-A3` says *"none after the
**stabilizer** is selected"*; `RA-B1..B5` say *"none after the **embedding** is
selected"*. These read as two conditions. They are one. Once the selected
stabilizer is `G_SM`-shaped, the branching of the `16` is unique: the
hypercharge line `Y = a·T3R + b·(B−L)` reproduces the Section-11.3 multiset for
**exactly two** `(a,b)`, and the `SU(2)_R` Weyl element `w4 ↔ w5` conjugates one
into the other — **one physical hypercharge line, 0 bits**. So there is no
second selection event for the source action to make, and the revival trigger
"a different selected embedding" names a mode that mostly cannot fire.

**3. Newly surfaced and deliberately NOT advanced: the cascade is 11 rows, not
6.** `AC-D1..AC-D5` (the five 4D SM anomaly rows) have exactly the same shape —
`SAME / DERIVED_CONDITIONAL`, `distance: none after the chiral 16 shadow is
selected`, `mapping_grade: EXACT`. They were not in the candidate pool. They are
**routed, not advanced**: their grant is "a complete chiral `16` shadow is
selected in 4D", which is precisely the object fenced by
`source-native-comparator-routing.md` fork 1, and precisely what `AC-A4` (14D
anomaly cancellation forces net bulk chirality **zero**) and `AC-F1`
(`NEEDS / MISSING_CONSTRUCTION`) say the balanced bulk does not supply. Granting
it would import the disputed conclusion as a premise.

---

## 1. PREFLIGHT — six specialist lenses, run inline

### Lens 1 — ledger / claims archivist: re-derive the pool from v0.258

Re-derived directly from `rows`, not inherited. The ledger holds **84 row
records**, of which 2 (`LT-GR2`, `AC-G1`) are `row_status: SUPERSEDED`, leaving
the **82 active targets** the denominator declares.

The correct machine-derived pool for "zero remaining computation" is
*rows whose `distance` begins with "none"* **and** whose `reason_kind` is
`DERIVED_CONDITIONAL`. That query returns **11 rows**, not 6:

| block | rows | stated condition |
|---|---|---|
| RA-A3 | 1 | `none after the stabilizer is selected` |
| RA-B1..B5 | 5 | `none after the embedding is selected` |
| AC-D1..D5 | 5 | `none after the chiral 16 shadow is selected` |

**`AC-D1..D5` were absent from the inherited list.** That is the largest single
ranking movement of this preflight.

`STALE_PREMISE` audit, exact, answering the "are there more than two?" question:
the 12 occurrences in the file decompose as **1** taxonomy vocabulary entry +
**2** row `reason_kind`s + **1** row `mapping_grade` + **6** escalation
`allowed_dispositions` boilerplate lists + **2** escalation `status` strings.
**No third row qualifies.** The other ten occurrences are vocabulary, not
typings.

### Lens 2 — representation theorist: what does "the embedding" actually range over?

The centralizer of `su(3)_c ⊕ su(2)_L` in the rank-5 Cartan of `so(10)` is
`5 − 2 − 1 = 2`-dimensional. So commutation alone does **not** pin hypercharge:
there is a genuine 2-plane, spanned by `T3R` and `B−L`, and the naive `B−L`-only
choice is a live wrong answer inside it. This is why "an embedding is selected"
sounds like it might be an independent grant.

It is not, because the 2-plane is cut down to a single ray mod Weyl by the
multiset condition. Re-verified exactly below.

### Lens 3 — source-fidelity reader: is the chain source-native or imported?

Source-native. `Spin(7,7) → Spin(1,3) × Spin(6,4) → Spin(1,3) × Spin(6) ×
Spin(4) ≅ SL(2,C) × SU(4) × SU(2) × SU(2) → SL(2,C) × SU(3) × SU(2) × U(1)` is
the source's own eq. 4.6, and the six-row table is the source's own §11.3. The
rows therefore sit on the source's written object, not on a borrowed comparator
— with one honest caveat carried forward: what is source-native is the *chain
and table*; the claim that the `16` **arrives** as a normal-bundle Weyl spinor
under observation is exactly what is not tested
(`pati-salam-chain-verification.md:196-218`, scope section, verbatim).

### Lens 4 — comparator-routing auditor: which rows may I touch?

- `RA-A3`, `RA-B1..B5`: source-native. The object is Weinstein's own reduction
  chain and his own quantum-number table. **Touchable.**
- `AC-D1..D5`: the four-dimensional SM anomaly conditions are admissible
  comparators under fork 4, and their enabling grant ("a chiral `16` shadow") is
  the fork-1 object. Advancing them would require a typed bridge satisfying all
  six items of the bridge burden; no such bridge exists.
  **Routed, not advanced.**
- `LT-SM3b`: not comparator-typed; declined for a different reason (§3).

### Lens 5 — epistemics / grant-hygiene: what would laundering look like here?

Two specific laundering moves are available and both are refused:

1. Retyping `DERIVED_CONDITIONAL → DERIVED` "because the condition is now
   named". A named condition is still a condition; `DERIVED` with a condition
   *is* `DERIVED_CONDITIONAL`. That relabel destroys information and is
   forbidden by the channel's rule 4. Refused.
2. Granting "the action selects an embedding" *generically* and then quietly
   using the specific standard line. Refused: the grant below names the line.

The honest movement class is the one v0.258 itself used four times — a
condition/grade migration at unchanged verdict and reason kind.

### Lens 6 — honesty auditor: prior-art check before any novelty claim

Grepped before computing. **The uniqueness theorem is already in the repo** and
this swing claims no novelty for it:

- `explorations/channel-swing-CH-SM-2026-07-19.md:69-76` — kill class 3:
  *"Y = a*T3R + b*(B-L) reproduces the SM multiset for exactly two lines,
  (a,b) = (1, 1/2) [standard] and (-1, 1/2) [flipped]. The SU(2)_R Weyl element
  (w4 <-> w5) preserves the 16 and maps one to the other ... **ONE physical
  hypercharge line.**"*
- `channel-swing-CH-SM-2026-07-19.md:238` — *"hypercharge line: UNIQUE mod
  Spin(10) Weyl — 0 bits"*.
- `cb-a-representation-content-2026-08-05.md:153-155` — *"once `U1` fixes a
  `G_SM`-shaped stabilizer, the branching of the `16` is unique"*.
- `pati-salam-chain-verification.md:144-152` — Step 5, the embedding-ambiguity
  probe, with the `B−L`-only control failing.

The only genuinely new arithmetic in this swing is **the type**: the published
outputs carry float residues (`sum Y = 5.55112e-16`); the re-check below is
exact rationals throughout, plus a 142-line sweep the prior artifacts did not
run. That sweep is what produced finding §5.2, which is not in the prior art.

### Cheapest kill-or-switch condition, recorded before working

> **If the hypercharge-line solve returns anything other than exactly two
> solutions related by the `SU(2)_R` Weyl element, the entire "the embedding is
> 0 bits" thesis dies and this swing reports zero rows moved.**

It returned exactly two. The condition did not fire.

---

## 2. THE EXACT RE-CHECK — 36/36, zero floats

`la1-hypercharge-line-zero-bit-recheck.py`, run under `_local/cas-venv`.
Everything is `fractions.Fraction`; no float appears anywhere.

```text
CERTIFICATE: 36/36 exact checks pass, zero floats.

[PASS] chiral 16 = even-minus-sign weights                                16
[PASS] exactly one chirality reproduces the Section-11.3 table under
       Y = T3R + (B-L)/2                                                  matching chirality: 16bar
[PASS] the other chirality is the exact CP conjugate (n -> -n)
[PASS] every hypercharge on the 16 satisfies 6Y in Z                      n = [-4,-3,0,1,2,6]
[PASS] gcd of the nonzero 6Y values is 1, so Y = 1/6 is the exact
       minimal unit                                                       gcd = 1
[PASS] Tr Y = 0 exactly (not a float residue)                             Tr Y = 0
[PASS] Tr Y^3 = 0 exactly                                                 Tr Y^3 = 0
[PASS] the 16 splits into exactly 6 blocks under su(3)xsu(2)_LxT3Rx(B-L)  sizes = [1,1,2,3,3,6]
[PASS] EXACTLY TWO lines (a,b) reproduce the Section-11.3 multiset        {(1,1/2), (-1,1/2)}
[PASS] the SU(2)_R Weyl element w4<->w5 preserves the 16 as a set
[PASS] w4<->w5 negates T3R, fixes T3L and B-L: ONE physical line, 0 bits
[PASS] control MISSES the multiset: T3R alone            (a,b)=(1,0)
[PASS] control MISSES the multiset: (B-L)/2 alone        (a,b)=(0,1/2)
[PASS] control MISSES the multiset: B-L alone            (a,b)=(0,1)
[PASS] control MISSES the multiset: wrong slope          (a,b)=(2,1/2)
[PASS] control MISSES the multiset: wrong slope          (a,b)=(1,1)
[PASS] control MISSES the multiset: X-line               (a,b)=(-4,3)
[PASS] control MISSES the multiset: sign-flipped B-L     (a,b)=(1,-1/2)
[PASS] 142 lines swept, exactly 2 reproduce the multiset
```

The block key is `(colour tag, isospin tag, T3R, B−L)` — all `(a,b)`-independent
weight data. `Y` never enters the key, so the solve is not circular. The
assignment search enumerates the 4 shape-respecting bijections and solves a
2-unknown system overdetermined by 6 equations; no search over the rationals is
performed and no solution can be missed within the 2-plane.

**Compactness (the RA-A3 half) is not computed here — it is cited, and it comes
from this same joe-directed program.** `PV-2` (`pv2-observation-cannot-reach-
the-extra-vectors-2026-08-14.md:56`, 22/22 exact) established that
*"the Standard Model's 12 generators sit entirely inside `k`"*, where `k =
so(6) ⊕ so(4)` is the maximal compact of `so(6,4)` reached by the source's own
eq.-4.6 step. `u(1)_Y ⊂ k`, maximal compacts are unique up to conjugacy, and
`6Y ∈ Z` above gives commensurable eigenvalues. Compactness and quantization
therefore follow from the *same* stabilizer grant. They are not a second grant.

---

## 3. THE DELTA — proposed row transitions against base revision `a148ed80`

Nothing below has been written to the ledger. This is a versionless delta for
the canonical owner.

### 3.1 The one grant, named

Every transition below carries **exactly this condition and no other**:

> **GRANT U1 (unchanged in strength, now named).** The source action exists and
> is stationary, and the vacuum it selects has an identity-component stabilizer
> that (a) is `G_SM`-shaped — Lie algebra `su(3) ⊕ su(2)_L ⊕ u(1)` — and (b) is
> contained in the maximal compact `Spin(6) × Spin(4) < Spin(6,4)` reached by
> the source's own eq.-4.6 reduction step, with `su(3)_c` regularly embedded.
> Nothing else is granted. In particular the *existence* of the stabilizer
> theorem remains `RA-A8: NEEDS / MISSING_CONSTRUCTION` and is **not** supplied
> here.

This grant is strictly *not stronger* than what these six rows already carry. It
is the same grant, written out.

### 3.2 Proposed transitions (6 rows)

| row | verdict | reason_kind | proposed verdict | proposed reason_kind | what changes | confidence |
|---|---|---|---|---|---|---|
| `RA-A3` | SAME | DERIVED_CONDITIONAL | **SAME (unchanged)** | **DERIVED_CONDITIONAL (unchanged)** | distance, revival_trigger, mapping_grade, evidence | high on arithmetic, medium on wording |
| `RA-B1` | SAME | DERIVED_CONDITIONAL | **SAME (unchanged)** | **DERIVED_CONDITIONAL (unchanged)** | distance, revival_trigger, mapping_grade, evidence | high |
| `RA-B2` | SAME | DERIVED_CONDITIONAL | **SAME (unchanged)** | **DERIVED_CONDITIONAL (unchanged)** | as RA-B1 | high |
| `RA-B3` | SAME | DERIVED_CONDITIONAL | **SAME (unchanged)** | **DERIVED_CONDITIONAL (unchanged)** | as RA-B1 | high |
| `RA-B4` | SAME | DERIVED_CONDITIONAL | **SAME (unchanged)** | **DERIVED_CONDITIONAL (unchanged)** | as RA-B1 | high |
| `RA-B5` | SAME | DERIVED_CONDITIONAL | **SAME (unchanged)** | **DERIVED_CONDITIONAL (unchanged)** | as RA-B1 | high |

#### RA-A3 — compact quantized U(1)_Y

```text
distance
  old: none after the stabilizer is selected
  new: none after the stabilizer is selected inside the source's own
       maximal-compact step Spin(6) x Spin(4) < Spin(6,4); quantisation is
       6Y in Z on the 16 with minimal unit exactly 1/6

revival_trigger
  old: a different stabilizer or noncompact hypercharge embedding
  new: a selected stabilizer that is not G_SM-shaped, or one not contained in
       a maximal compact subgroup

mapping_grade
  old: EXACT_GIVEN_U1
  new: EXACT_GIVEN_U1__QUANTISATION_6Y_IN_Z_EXACT__MINIMAL_UNIT_ONE_SIXTH__
       COMPACTNESS_ENTAILED_BY_SOURCE_MAXIMAL_COMPACT_STEP__
       NONCOMPACT_REVIVAL_MODE_SUBSUMED_NOT_INDEPENDENT

evidence  (append)
  pv2-observation-cannot-reach-the-extra-vectors-2026-08-14.md:56
  la1-embedding-grant-is-zero-bit-and-group-a-is-already-banked-2026-08-15.md
```

**What actually moved:** the revival trigger listed two apparently independent
failure modes. The second is not independent — a noncompact hypercharge
embedding *is* a stabilizer outside a maximal compact. Two named revival modes
collapse to one. Stated as **subsumption, not elimination**: if the action
selected a stabilizer outside every maximal compact, hypercharge could still be
noncompact, so the mode is not closed, only relocated into the first clause.

#### RA-B1..RA-B5 — Q_L, u^c, d^c, L_L, e^c representation and hypercharge

```text
distance
  old: none after the embedding is selected
  new: none after the stabilizer is selected; the embedding is not a further
       selection event -- the hypercharge line is unique mod the Spin(10) Weyl
       group (exactly two solutions (a,b) = (1,1/2) and (-1,1/2), conjugate
       under the SU(2)_R Weyl element w4<->w5), 0 bits

revival_trigger
  old: a different selected embedding
  new: a selected stabilizer that is not G_SM-shaped; a physical carrier not
       equal to a complete 16; or a special (non-regular) su(3)_c embedding
       outside the A1-lock's regular-embedding scope

mapping_grade
  old: EXACT_BRANCHING
  new: EXACT_BRANCHING__EMBEDDING_ZERO_BIT_MOD_WEYL_EXACT__
       GRANT_UNIFIED_WITH_RA_A3_SINGLE_U1_STABILIZER_CONDITION

evidence  (append)
  channel-swing-CH-SM-2026-07-19.md:69-76,238
  la1-hypercharge-line-zero-bit-recheck.py  (36/36 exact, zero floats)
```

**What actually moved:** three things, none of them a verdict.
(i) The condition is **named** rather than gestured at.
(ii) The condition is **unified** with RA-A3's — the REPRESENTATION axis carries
one named grant across these six rows where it appeared to carry two.
(iii) The revival trigger is **narrowed**: within the regular `G_SM`-shaped
class the embedding cannot differ, so the old trigger over-advertised how easily
these rows could be revived. The new trigger keeps the two modes that *can*
fire, and the third (non-regular embedding) is carried explicitly rather than
silently assumed away.

### 3.3 Migration records, in ledger schema shape

```json
[
 {"row_id": "RA-A3",
  "old": ["SAME", "DERIVED_CONDITIONAL", "EXACT_GIVEN_U1"],
  "new": ["SAME", "DERIVED_CONDITIONAL",
          "EXACT_GIVEN_U1__QUANTISATION_6Y_IN_Z_EXACT__MINIMAL_UNIT_ONE_SIXTH__COMPACTNESS_ENTAILED_BY_SOURCE_MAXIMAL_COMPACT_STEP__NONCOMPACT_REVIVAL_MODE_SUBSUMED_NOT_INDEPENDENT"],
  "scope": "finite weight-lattice quantisation and maximal-compact containment only; stabilizer existence, source action, vacuum and carrier projection excluded",
  "evidence": "la1-embedding-grant-is-zero-bit-and-group-a-is-already-banked-2026-08-15.md",
  "meaning_changed": false},

 {"row_id": "RA-B1..RA-B5",
  "old": ["SAME", "DERIVED_CONDITIONAL", "EXACT_BRANCHING"],
  "new": ["SAME", "DERIVED_CONDITIONAL",
          "EXACT_BRANCHING__EMBEDDING_ZERO_BIT_MOD_WEYL_EXACT__GRANT_UNIFIED_WITH_RA_A3_SINGLE_U1_STABILIZER_CONDITION"],
  "scope": "complexified branching of the 16 and the exact two-solution hypercharge-line solve only; stabilizer selection, real-form reality structure, physical carrier and generation count excluded",
  "evidence": "la1-hypercharge-line-zero-bit-recheck.py",
  "meaning_changed": false}
]
```

`meaning_changed: false` is deliberate and is the conservative call: no target's
meaning changed, only the precision with which its condition is stated. The
canonical owner may reasonably book these as `true` on the ground that a
narrowed revival trigger changes what would revive the row; that disposition is
theirs, not this channel's.

**Denominator effect: none.** 82 active targets before, 82 after. No row is
added, superseded, split or merged.

---

## 4. DECLINED, WITH THE ONE-LINE REASON

| row(s) | why it did not move |
|---|---|
| `AC-G1` | Already `row_status: SUPERSEDED` with successor `AC-G1a`; outside the 82-row active denominator. Nothing left to disposition. |
| `LT-SM3b` | Already correctly typed — the ledger row transcribes its evidence file's prescribed disposition verbatim. Its premise is recorded as having been stale, which is the terminal disposition, not a pending one. |
| `LT-SM3b` (successor proposal) | Declined separately: minting `LT-SM3c` on the `AC-G1a` precedent would risk double-counting against the live `LT-SM3` and would move the denominator on this channel's authority. Escalated instead (§6). |
| `AC-D1..AC-D5` | Comparator-routed. Grant is "a complete chiral `16` shadow in 4D" — the fork-1 object, contradicted by `AC-A4` (net bulk chirality zero) and open at `AC-F1`. No typed bridge exists. |
| `RA-F2` | `DERIVED_CONDITIONAL` but not zero-computation: its distance carries live work (`W` and mirror in observed `H640`, action parent and physical replica cohomology open). |
| `RA-A1`, `RA-A2`, `RA-A8` | These *are* the grant. Advancing them by the grant is circular. |
| `RA-D2`, `LT-GR4`, `AC-F3` | `ESCALATED_PENDING_DISPOSITION` in the escalation registry while their rows carry provisional reason kinds. That is a live adjudication process, not a stale record; not this channel's to close. |

---

## 5. POSTFLIGHT — five specialist lenses, run inline

### 5.1 Strongest overclaim available (and refused)

> *"Six rows advanced from conditional to derived: the Standard Model's
> representation content and hypercharge quantization now follow from GU with
> zero free parameters."*

Every clause of that is locally defensible from the artifacts and it is still
false in the way that matters. The six rows describe the branching of a complete
`16` of `so(10)` under a `G_SM`-shaped stabilizer. **That stabilizer does not
exist yet** — `RA-A8` is `NEEDS / MISSING_CONSTRUCTION` and `RA-A1`'s distance
still reads "construct the smallest action-stationary nonhomogeneous
reduced-curvature plus full-II orbit and compute its stabilizer". Everything
banked here is downstream of an object nobody has built. The correct sentence is
narrower: *the condition these rows wait on is one condition, not two, and the
part of it that looked like a second choice is worth zero bits.*

A second, subtler overclaim also refused: the arithmetic is **textbook**. CB-A
says so itself (`:291-294`) — this is the standard `SO(10) ⊃ PS ⊃ SM` embedding,
and the constraint surplus "is not GU's alone".

### 5.2 Strongest contrary reading — and it lands on the repo, not on GU

The 142-line exact sweep found something the prior artifacts did not run, and it
cuts against an in-repo count rather than for it:

> **All five 4D anomaly conditions hold on 142/142 lines in the `T3R`/`B−L`
> 2-plane** — including all seven named wrong lines. `Tr Y = 0`, `Tr Y³ = 0`,
> `Σ_coloured Y = 0`, `Σ_doublets Y = 0` and the even-doublet count are
> **entailed by the completeness of the `16` as an anomaly-free `so(10)` irrep**,
> not by the hypercharge line.

CB-A §4 counts *"11 independent constraints (6 hypercharges + 5 anomaly
conditions), 0 free parameters, surplus +11"*. On the hypercharge-line question
the five anomaly conditions contribute **zero** discriminating power. The honest
surplus for *that* question is 6 constraints cutting a 2-parameter family to 2
solutions, 1 mod Weyl. The +11 figure double-counts.

This is filed as a **finding against a repo count, not a proposed ledger
change** — `AC-D1..D5` are comparator-routed and out of scope for this channel,
and the arithmetic does not touch their `SAME` verdicts, which remain correct
*given* their grant.

> **Cross-reference, to prevent double-counting.** The concurrent route LA-3
> (`la3-chiral-16-shadow-is-a-comparator-and-the-grant-is-inert-2026-08-15.md`)
> independently reached the same fork-1 routing call on `AC-D1..D5` and the same
> "zero rows advance" conclusion, and found a **sharper** algebraic fact: the
> five anomaly channels are linear functionals of **rank 4, not 5**, on the
> signed multiplicity lattice. That is a different cut from this one — LA-3
> varies the *content* at fixed line; §5.2 here varies the *line* at fixed
> content. The two are complementary evidence against the same `+11` count, not
> two independent confirmations of one claim. Where they overlap, LA-3's
> rank-4 relation is the stronger statement and should be cited in preference.

### 5.3 Weakest seam — an internal typing inconsistency this channel will not resolve

`RA-C1` ("all six hypercharges arise from one line") is `SAME / **DERIVED**`,
`mapping_grade: EXACT_ZERO_FREEDOM`, with `revival_trigger: a selected embedding
outside the unique Weyl orbit`. `RA-B1..B5` carry the *same* six hypercharge
values under the *same* dependence and are `DERIVED_**CONDITIONAL**`. CB-A's own
dependency census puts `C1` squarely in the `U1`-dependent list
(`:194`, "`U1` | **19** | A1–A6, A8, B1–B6, C1–C6, G4"). Neither row has ever
been migrated — both typings date from ledger creation.

One of the two is wrong, and the two repairs point opposite ways:

- **(a)** `RA-C1` is overtyped and should be `DERIVED_CONDITIONAL` — which
  *decrements* the unconditional-`DERIVED` count; or
- **(b)** `RA-B1..B5` are undertyped relative to `RA-C1`/`RA-B9` — which is the
  promotion this channel is forbidden to make unilaterally.

The same seam touches `RA-B9` (`DERIVED`, but `revival_trigger: failure of the
**selected** Spin(10) reduction`). Escalated in §6. This channel declines to
pick, precisely because the direction that would look like progress here is the
one with no independent warrant.

### 5.4 Source-fidelity lens — what the grant does and does not buy

The grant names the *standard* Pati-Salam hypercharge line. A hostile reader can
say the line was chosen to match the answer. The repo's own defence is exact and
holds: the line is not fitted, it is the **unique** line mod Weyl that closes on
the `16`, and the two nearest alternatives (`B−L` alone, `T3R` alone) fail
explicitly. What the grant genuinely cannot buy is the *geometric origin* — that
the `16` arrives as a normal-bundle Weyl spinor under observation — which is the
whole GU-specific claim and is untouched here.

### 5.5 Honesty auditor — the count, plainly

**6 rows carry a proposed migration. 0 verdicts moved. 0 reason kinds moved.
14 rows declined or routed** (`AC-G1`, `LT-SM3b`, `AC-D1`, `AC-D2`, `AC-D3`,
`AC-D4`, `AC-D5`, `RA-F2`, `RA-A1`, `RA-A2`, `RA-A8`, `RA-D2`, `LT-GR4`,
`AC-F3` — 14 distinct row ids). Three escalations filed. One in-repo count
corrected downward.

The candidate pool suggested up to 8 movable rows across two groups. Group A
delivered **zero**, because it was already banked. Group B delivered a condition
migration, not an advancement. Anyone reading this as "6 rows advanced" is
reading it wrong.

---

## 6. ESCALATED TO THE CANONICAL OWNER — not resolved here

1. **`RA-C1` / `RA-B1..B5` / `RA-B9` typing seam** (§5.3). Rows with identical
   `U1` dependence are split across `DERIVED` and `DERIVED_CONDITIONAL`.
   Both repairs named; neither enacted.
2. **`LT-SM3b` successor question.** `AC-G1`, adjudicated `STALE_PREMISE`, was
   given `row_status: SUPERSEDED` plus successor `AC-G1a` carrying the live
   construction burden. `LT-SM3b`, also adjudicated `STALE_PREMISE`, carries a
   live `distance` with no successor and no `row_status`. Either the `AC-G1`
   precedent should extend to it, or the two cases differ for a reason worth
   recording. This channel could not tell which without the Shiab-layer context
   and declined to mint a row.
3. **CB-A `+11` constraint surplus** (§5.2) overcounts by 5 on the
   hypercharge-line question.

---

## 7. CLAIM CEILING

**Established (exact, 36/36, zero floats):** within the complexified 16 of
`so(10)`, the hypercharge line reproducing the source's §11.3 table is unique up
to the `SU(2)_R` Weyl element; `6Y ∈ Z` with minimal unit exactly `1/6`;
`Tr Y = Tr Y³ = Tr Q = 0` exactly; the five 4D anomaly conditions do not
discriminate lines in the 2-plane.

**Granted, not established — the single condition every moved row carries:**
GRANT U1 of §3.1. The action, its stationarity, its vacuum, and the `G_SM`-shaped
stabilizer inside the maximal compact are **assumed**, per the channel premise.

**Not established, explicitly:** any source action; any vacuum; the stabilizer
theorem (`RA-A8`); a physical carrier projection delivering a complete chiral
`16` in 4D; compactness independent of the maximal-compact step; the generation
count; effective chirality; the `2+1` construction; and **any verdict or
reason-kind movement whatsoever**.
