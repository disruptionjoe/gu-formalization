---
artifact_type: exploration
status: exploration
doc_type: ledger-axis-assessment-delta
created: 2026-08-15
work_item: LA-4
channel: conditional_ledger_advancement
base_revision: a148ed80
ledger_base: lab/process/conditional-physics-ledger-v0.258.json
ledger_edit: none -- this document is a versionless assessment for the canonical owner to disposition
delta_kind: VERSIONLESS_AXIS_ASSESSMENT__NOT_A_LEDGER_EDIT
axis_scope: REPRESENTATION (35 active rows of the 82-row v0.258 denominator)
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
steering_effect: unchanged
canonical_effect: pending_integration
title: "LA-4: the REPRESENTATION axis's 35 rows are controlled by exactly 13 independent grant columns over 22 addressable signature classes, and the grant/row network has a VERTEX CUT OF SIZE ONE -- the operative second action reaches 28 of the 29 open rows, is named by 2 of them (RA-E1, RA-E3), and is the thing next_work_queue rank 2 (RA-A1/A2/A6/A8/G4) is instructed to assume. The 29th row, RA-E2 -- the axis's only open row not downstream of that cut -- has its route refuted by MD-1 (2026-08-14, 67/67) and by the 2026-07-30 Layer-0 datum packet, neither of which the ledger cites. Six revival triggers (RA-B1..B5, RA-C1) quantify over an empty set and are unfireable as typed. Across 54 version steps and 244 migrations the ledger has changed ZERO verdicts and ZERO reason kinds."
grade: "EXACT integer / GF(2) / fractions.Fraction linear algebra plus exhaustive combinatorial search over a 14-atom grant vocabulary; no float is load-bearing anywhere. 37/37, exit 0, via lab/active-research/joe-directed/ledger-advancement/la4-representation-axis-incidence-probe.py. Certificate splits as 25 [E] exact results, 7 [C] controls that must fire, 2 [R] reproductions of filed ledger facts, 2 [T] declared table inputs. Non-vacuity established seven ways: deleting b1 must move the rank (12), merging b1 with b4 must drop it (12), separating b2 from b5 must raise it (14), making b1 universal must collapse the dual packing to 1, re-parenting RA-E2 must make the root reach 29/29, empty-signature rows must not count as open, and re-typing RA-A3 must break the 6-row class. A four-way sensitivity sweep on every contestable attribution leaves rank 13 unchanged. NOT: a source action, a vacuum, a stabilizer, a Higgs carrier, a chirality mechanism, a generation count, a physics result of any kind, or any verdict / reason-kind movement."
disposition: THIRTEEN_INDEPENDENT_GRANTS_TWENTYTWO_SIGNATURE_CLASSES__ONE_VERTEX_CUT_REACHING_28_OF_29__RA_E2_ROUTE_REFUTED_BY_UNCITED_MD1__SIX_REVIVAL_TRIGGERS_QUANTIFY_OVER_THE_EMPTY_SET__ZERO_VERDICT_DEPARTURES_IN_54_VERSION_STEPS__B2_B5_UNOBSERVABLE_PAIR
rows_advanced: 0
rows_escalated: [RA-E2, RA-B1, RA-B2, RA-B3, RA-B4, RA-B5, RA-C1, RA-A6]
rows_touched_structurally: [RA-A1, RA-A2, RA-A3, RA-A4, RA-A5, RA-A6, RA-A7, RA-A8, RA-B1, RA-B2, RA-B3, RA-B4, RA-B5, RA-B6, RA-B7, RA-B8, RA-B9, RA-C1, RA-D2, RA-D3, RA-D4, RA-E1, RA-E2, RA-E3, RA-E4, RA-E5, RA-E6, RA-E7, RA-F1, RA-F2, RA-F3, RA-G1, RA-G2, RA-G3, RA-G4]
depends_on:
  - lab/process/conditional-physics-ledger-v0.258.json
  - lab/methods/source-native-comparator-routing.md
  - lab/process/perspective-passes/INDEX.md
  - lab/process/science-council-program-efficiency-2026-08-04.md
  - lab/process/path-dependencies.md
  - explorations/conditional-build/cb-a-representation-content-2026-08-05.md
  - explorations/conditional-build/selected-k77-i2b-action-euler-principal-owner-comparison-2026-08-13.md
  - explorations/conditional-build/selected-k77-action-concomitant-residual-flag-gate-2026-08-12.md
  - explorations/unified-source-datum-packet-v0-2026-07-30.md
  - lab/active-research/joe-directed/four-d-mode-decomposition/md1-form-leg-survives-ad-leg-is-untyped-2026-08-14.md
  - lab/active-research/joe-directed/ledger-advancement/la1-embedding-grant-is-zero-bit-and-group-a-is-already-banked-2026-08-15.md
  - lab/active-research/joe-directed/ledger-advancement/la3-chiral-16-shadow-is-a-comparator-and-the-grant-is-inert-2026-08-15.md
scripts:
  - lab/active-research/joe-directed/ledger-advancement/la4-representation-axis-incidence-probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# LA-4 — the REPRESENTATION axis has thirteen grants and one cut vertex

## Result first, without inflation

**Zero rows advance. Zero verdicts move. Zero reason kinds move.** This is a
structural assessment of the axis, not a construction, and its whole positive
product is six structural facts and eight escalations.

The one number the brief asked for:

> **The 35 REPRESENTATION rows are 6 unconditional rows plus 29 conditional
> rows, and the 29 are controlled by exactly 13 independent grant columns,
> addressable as 22 distinct condition signatures, over a presupposition DAG
> whose largest root reaches 28 of the 29.**

`13` is the rank of the grant→row incidence matrix over both `GF(2)` and `Q`,
and it equals the number of *distinguishable* grant supports — the two counts
agreeing is the certificate that no grant is hiding behind another except the
one pair the computation names.

---

## 0. PREFLIGHT — six specialist lenses, run inline

### Lens P1 — ledger archivist: re-derive the work list from v0.258 itself

Re-derived from `rows`, not inherited. The ledger holds **84 row records**; two
(`LT-GR2`, `AC-G1`) carry `row_status: SUPERSEDED`, leaving the **82 active
targets** the denominator declares. **Both superseded rows are off-axis**
(`LAGRANGIAN` and `ANOMALY_CONSISTENCY`), so all **35 REPRESENTATION rows are
active** and the brief's scope statement is exact.

The 35, machine-derived: `RA-A1..A8` (8), `RA-B1..B9` (9), `RA-C1` (1),
`RA-D2..D4` (3), `RA-E1..E7` (7), `RA-F1..F3` (3), `RA-G1..G4` (4).

**What moved, and it is not what the brief expected.** The brief's ranking
implies `RA-A8`/`RA-A1` is "where real movement runs". Re-derivation from the
ledger's own `migrations` array says the opposite about *observed* movement:

| fact | value |
|---|---:|
| migrations recorded, v0.174 → v0.258 | 244 |
| of those, changing a **verdict** | **0** |
| of those, changing a **reason kind** | **0** |
| REPRESENTATION migrations | 141 |
| distinct REPRESENTATION rows they touch | **13 of 35** |
| REPRESENTATION rows untouched since v0.174 | **22** |
| share of axis service taken by `RA-G2` alone | 32/141 ≈ 23% |

`RA-A1` has migrated **once** in 84 versions; `RA-A8` three times; `RA-A3`,
`RA-B1..B5`, `RA-C1` **never**. The rows the brief names as the live front are
among the least-serviced on the axis. That is not an objection to the brief —
`RA-A8` may well be where movement *should* run — but the preflight must record
that the ledger's own service history points at `RA-G2`/`RA-E3`/`RA-E1`/`RA-F2`.

### Lens P2 — perspective-pass roster auditor: which lenses are actually new

Read `lab/process/perspective-passes/INDEX.md` (41 passes, six families) before
choosing. Verdict on the assigned roster, so no lens is wasted:

- **New to the roster, no overlap:** port-Hamiltonian / bond-graph, MIMO control,
  network flow, PDE well-posedness (Hadamard), reliability / FMEA, queueing,
  systems-engineering traceability, numerical conditioning, optimization duality,
  order/lattice theory, model theory / definability.
- **Partial overlap, scoped away:** *information theory* overlaps pass 40
  (`information-coding-holographic`) — but pass 40 is about homological product
  codes on the physics carrier, not about bits-per-grant on the ledger. Scoped to
  the ledger. *Category-theoretic functorial semantics* overlaps pass 14
  (`higher-derived-geometer`) — but pass 14 is derived stacks on the substrate,
  not functoriality of the CB-A → ledger row map. Scoped to the ledger.
- **The decisive scoping rule for all of them:** every one of the 41 passes takes
  *the physics* as its object. This assessment takes *the ledger* as its object.
  That is why the roster is non-duplicative, and it is also the limit of what
  these lenses can deliver — see the postflight.

### Lens P3 — comparator-routing auditor: which rows may this artifact touch

Read `lab/methods/source-native-comparator-routing.md` first, as instructed.

- **Structural claims about row wiring** (signatures, rank, DAG, revival-trigger
  emptiness) are claims about the *ledger*, not about either the GU-native
  mechanism or a comparator. They route freely and adjudicate nothing physical.
- `RA-E7` (`16·16·H`, `10/126` channel), `RA-G3` (`126` channel, Majorana scale)
  and `RA-B6` (neutrino spectrum) **border fork-2 and fork-4 comparators**. This
  artifact makes no statement about their physics content, only about their
  position in the incidence graph. Their comparator status is untouched.
- `RA-E2`'s finding (§2.1) is **source-native on both sides**: MD-1's object is
  the source-declared observation contraction `s^*`, and CB-A:E2's object is the
  ambient decomposition of `Ω¹(Y,ad)`. No conventional comparator enters. It is
  nevertheless **escalated, not adjudicated**, because MD-1 itself declares an
  open Layer-0 fork (`SOLDERED-AD` vs `INERT-AD`).
- `LA-3` already routed `AC-D1..D5` as fork-1 comparators. Those rows are off my
  axis; I import LA-3's routing verdict and add nothing.

### Lens P4 — grant-hygiene auditor: what laundering would look like here

Three laundering moves are available to an assessment of this shape. All three
are refused, before any computation:

1. **Converting a fan-out count into an advance.** "Grant `b1` and 22 rows
   close" is a *conditional* statement. It advances nothing. Every number in §1
   and §2 is stated as a conditional and none is used to move a verdict.
2. **Re-typing `DERIVED_CONDITIONAL → DERIVED` because the condition is now
   named or now provably 0-bit.** Already refused once in this channel (LA-1 §1,
   Lens 5) and refused again here. §2.5 finds a defect in six rows and the
   proposed repair is a *revival-trigger* rewrite, not a verdict change.
3. **Converting a structural fact about the ledger into a physics claim.** The
   single most available version is: "the axis has one cut vertex, therefore GU
   reduces to one problem." Refused. The cut vertex is a property of *how the
   rows are currently written*, and a different decomposition of the same physics
   would give a different graph. §4 states this as the strongest contrary reading.

### Lens P5 — prior-art / honesty auditor: grep before any novelty claim

Grepped by mechanism before computing, per the standing rule that zero hits from
an exact-substring search is not evidence of novelty.

| candidate finding | prior art found | what remains new |
|---|---|---|
| the hypercharge line is 0 bits | `channel-swing-CH-SM-2026-07-19.md:69-76,238`; `pati-salam-chain-verification.md:144-152`; LA-1 §2 (36/36) | **nothing** — cited, not re-claimed. Used only as an input to §2.5 |
| the action owner is an open fork | `PD-I2B-ACTION-OWNER` in `lab/process/path-dependencies.md:339`; `selected-k77-i2b-action-euler-principal-owner-comparison-2026-08-13.md:127`; `residue.moving_q_u_full_euler` | **nothing about the fork.** New: that the fork's *trigger* does not reach the five rows and the queue rank that assume it (§2.2) |
| the vertical form leg is not a 4D scalar | `unified-source-datum-packet-v0-2026-07-30.md:128` (Layer-0 register, "the object it is not"); `ten-perspective-next-ten-swing-council-scaffold-2026-07-30.md:169`; `eric-native-physics-equation-replacement-atlas-2026-07-31.md:116`; MD-1 (2026-08-14, 67/67) | **nothing about the physics.** New: that `RA-E2` still prescribes the refuted map, and that MD-1 is cited **zero** times in the ledger JSON (§2.1) |
| U1 is the highest-fan-out unknown | `cb-a-representation-content-2026-08-05.md:205-206` ("fixing `U1` alone converts 19 of 41 rows") | New: the ledger carries **no grant index at all** (`"U1"`, `"U4"`, `"grant"` each occur **0** times in the v0.258 JSON), and 5 of CB-A's 19 have left the axis (§2.6) |
| the ledger banks grade migrations at unchanged verdict | LA-1 §1 Lens 5 ("the honest movement class") | New: the **magnitude** — 244 migrations, 0 verdict changes, 54 version steps (§2.4) |

### Lens P6 — scope auditor: the hard rules, checked before writing

No git. No `repo-session-sync`. No ledger edit — this file is a versionless
assessment against `a148ed80`. Writes confined to
`lab/active-research/joe-directed/ledger-advancement/` with the `la4-` prefix
(this file and one probe script). No float is load-bearing. No vacuously-true
assertion is filed; §2.5 is specifically about the *dual* defect, a vacuously-
false one. **No row advances, so no grant is carried into a derivation.**

### Cheapest kill-or-switch condition, recorded before working

> **If the GF(2) rank and the Q rank of the incidence matrix disagree, or if
> either differs from the count of distinguishable grant supports, the whole
> degrees-of-freedom thesis is unsound and this swing reports a number it does
> not stand behind.**

They agree at `13` three ways. The condition did not fire.

---

## 1. THE COMPUTATION — 37/37, exit 0, zero floats

`la4-representation-axis-incidence-probe.py`, run under `_local/cas-venv`.

### 1.1 The grant vocabulary, and how it was derived

Fourteen atoms, each read off the rows' own `distance` + `revival_trigger` +
`mapping_grade` strings in v0.258. An atom is assigned to a row when the row's
own text names the object as something that must be **built or granted** for the
row to close. Revival triggers of already-closed rows are *falsifiers*, not
conditions, and are deliberately excluded — that separation is the
controllability/observability split of §2.3 and is the single most consequential
modelling decision in this artifact.

| atom | object | ledger anchor |
|---|---|---|
| `b1` | action-stationary vacuum | `RA-A8` "Construct an action-stationary vacuum"; `RA-E6` "an exact vacuum Hessian" |
| `b2` | selection of one `J` from the 20-dim family | `RA-A2` `J_ACTION_SELECTION`; `RA-A6` `J_DESCENT` |
| `b3` | nonhomogeneous reduced-curvature + full-`II` orbit / residual flag | `RA-A1`, `RA-D4` `NONHOMOGENEOUS_FLAG`, `RA-F1` `RESIDUAL_FLAG` |
| `b4` | globally computed identity-component stabilizer | `RA-A1`/`RA-G4` `NONHOMOGENEOUS_STABILIZER_OPEN` |
| `b5` | global `μ₆` quotient / global gauge embedding / descent | `RA-A6` `GLOBAL_MU6`, `RA-A2` `GLOBALIZATION` |
| `b6` | nonzero radial `varpi` coefficient | `RA-A8` `RADIAL_SELECTION`; `RA-E4` trace-radial |
| `b7` | gauge-boson / scalar mass matrix, vacuum Hessian | `RA-G4`, `RA-E4` `MASS_MATRIX_OPEN` |
| `b8` | BV/KT, BFV, domain, positive physical cohomology | `RA-D4`, `RA-F1`, `RA-G2` |
| `b9` | the operative completed second action + its full moving principal map | `RA-E1`, `RA-E3` `OPERATIVE_SECOND_ACTION_..._OPEN` |
| `b10` | 14D→4D observed scalar descent adapter | `RA-E2` revival; `RA-E3` `SCALAR_DESCENT` |
| `b11` | physical carrier projection / retyping | `RA-E3` `RETYPING`, `RA-G1`, `RA-G2` |
| `b12` | zero-order coupling / odd-form singlet VEV | `RA-E7`, `RA-G3` |
| `b13` | typed nonzero index/count observable paired with P3 | `RA-F1` `INDEX_COUNT_OPEN`, `RA-F3` |
| `b14` | replacement source-owned Shiab / restricted port | `RA-D2` `REPLACEMENT_SHIAB_OR_FUNCTIONAL_BUILD_OPEN` |

### 1.2 The numbers

```text
CERTIFICATE: 37/37 exact checks pass, zero floats.

35 rows  =  6 unconditional  +  29 conditional
  unconditional: RA-A7, RA-B7, RA-B8, RA-B9, RA-C1, RA-D3
GF(2) rank of the open incidence matrix          13
Q rank of the open incidence matrix              13
distinguishable grant supports                   13   (of 14 atoms)
the one indistinguishable pair                   (b2 J_SELECTION, b5 GLOBAL_DESCENT_MU6)
distinct condition signatures among 29 open rows 22
largest signature class                          6 rows: RA-A3 + RA-B1..RA-B5
presupposition-DAG roots                         3   (b9, b10, b14)
rows reached by root b9                          28 / 29
the one open row not reached by b9               RA-E2
maximum pairwise condition-disjoint packing      4   (RA-A1, RA-D2, RA-E2, RA-F3)
cheapest certificate frontier (rows closed)      k=1→1  k=2→6  k=3→8  k=4→11  k=5→14  k=6→17  k=7→19
signature-size distribution over 29 open rows    {1:2, 2:11, 3:12, 4:3, 5:1}, 77 atom-edges total
```

### 1.3 Sensitivity — the rank is robust

The three contestable attributions, swept exactly (rank / signatures / open):

| perturbation | result |
|---|---|
| baseline | **13 / 22 / 29** |
| drop `b1` from the six "embedding/stabilizer selected" rows (`RA-A3`, `RA-B1..B5`) | 13 / 22 / 29 |
| drop `b1` from the six rows where the vacuum was inferred rather than stated (`RA-B6`, `RA-D4`, `RA-E4`, `RA-E5`, `RA-E7`, `RA-F2`) | 13 / **23** / 29 |
| re-type `RA-C1` as a `U1` row, per CB-A's reading rather than v0.258's | 13 / 22 / **30** |
| delete `b1` from the axis entirely (nobody proposes this) | **12** / 22 / 29 |

**Rank 13 survives every perturbation short of deleting the vacuum grant.** The
signature count 22 is soft by ±1.

---

## 2. THE LENS FINDINGS

Each lens returns at most three findings, tied to row ids, checked against the
41 passes and against the repo's prior art. Lenses that returned nothing are
listed at §3 and are reported as such.

### 2.1 Systems engineering / requirements traceability — `RA-E2`'s route is refuted, by two artifacts the ledger does not cite

`RA-E2` ("the Higgs component is a Lorentz scalar") is typed
`NEEDS / MISSING_CONSTRUCTION`, `mapping_grade: EXACT_SHAPE_CONSTRAINT`, with

> `distance:` **use and propagate the vertical form leg through observation**
> `revival_trigger:` an exact 14D-to-4D vertical-scalar adapter
> `evidence:` `cb-a-representation-content-2026-08-05.md:E2`

Its parent, `CB-A:E2`, states the shape constraint verbatim: *"in an
adjoint-valued one-form `Ω¹(Y, ad)`, the Lorentz-scalar components are exactly
those with **both** legs vertical: `V*₁₀ ⊗ Λ²V₁₀ = 10 ⊗ 45`, which hosts 6
`(1,2,+1/2)` and 6 `(1,2,−1/2)` … **the Higgs must consume the vertical form
leg**"* (`:407`).

Two filed artifacts bear directly on that instruction and neither is in the row:

1. **`unified-source-datum-packet-v0-2026-07-30.md:128`**, Layer-0 register,
   *five days before CB-A was written*: the entry `vertical scalar` names the
   object as `res_s^V(A−A_0)` followed by a declared trace/orbit projector, and
   names the **object it is not** as *"the differential-form pullback `s^*(a_V)`,
   which is zero or an `X`-one-form."*
2. **`md1-form-leg-survives-ad-leg-is-untyped-2026-08-14.md`** (67/67 exact,
   `fractions.Fraction`, nine planted controls that fire) makes it computational:
   *"Every ad-valued one-form on `Y14` descends to exactly one 4D one-form"*;
   *"The vertical legs are contracted in, not split off. They enter the same 4D
   one-form"*; *"The contraction is lossy: `s^*` annihilates a 10-dimensional
   space of form legs. **Those ten do not become 4D scalars.**"* And it closes the
   alternative: under the disavowed KK projection, `Sym²(T*X4)`'s Lorentz-trivial
   component is **exactly 1-dimensional** (control `B12` fires at 10), so that
   reading yields one scalar per ad direction — not the `10 ⊗ 45` sector CB-A:E2
   counted.

**`md1-form-leg` occurs 0 times in `conditional-physics-ledger-v0.258.json`, and
`RA-E2` has not migrated since v0.174.**

The finding is not "the Higgs is dead" — that inference is refused under §0/P4
and under the comparator-routing fence. The finding is a **typing defect**:
`RA-E2` is filed as `MISSING_CONSTRUCTION` while two candidate constructions of
the named adapter are built and exact, and both return a Lorentz type other than
the one the counted sector requires. The row's `distance` names the map
(`s^*`, "through observation") that the repo's own Layer-0 register typed as the
wrong object; the live route is `res_s^V` plus a **declared trace/orbit
projector**, which is a different and currently unowned object.

**Escalation, not adjudication.** MD-1 declares `SOLDERED-AD` vs `INERT-AD` open
and assumes `SECTION-VS-OBSERVERSE`. `RA-E2` should carry that fork, migrate its
`distance` and `mapping_grade` on MD-1 evidence, and its `revival_trigger`
should stop asking for an adapter that has been built twice. `RA-E1`, `RA-E3`
and `RA-E4` inherit the same `CB-A:E2` shape premise and should be checked
against MD-1 in the same pass.

**Second traceability finding.** The axis's own source rows partly live on other
axes: `CB-A:C2..C6` alias to `AC-D5/D4/D3/D2/AC-C2` and `CB-A:D1` aliases to
`AC-F1`. So of CB-A's 41 representation-content rows, 35 are `RA-*` and 6 are
`AC-*`. An axis-scoped agent — this one, by construction — cannot see them.

### 2.2 Network flow / critical path — the axis has a vertex cut of size one, and two rows name it

Build the presupposition DAG from edges each licensed by a quoted v0.258 string:
`b9 ≺ b1` (an "action-stationary" vacuum presupposes a named action);
`b1 ≺ {b2,b3,b4,b6,b7,b8,b12}` (`RA-A8`: *"an action-stationary vacuum that
selects `J` and a nonzero radial varpi coefficient and compute its
identity-component stabilizer"*; `next_work_queue` rank 5: *"After an
action-owned background exists, construct the physical projector/domain"*);
`b4 ≺ b5` (`RA-A6`: *"prove the selected `J` reduction descends"*);
`b8 ≺ {b11,b13}`.

The DAG has three roots. Their reach over the 29 open rows:

| root | object | reach |
|---|---|---:|
| `b9` | the operative completed second action | **28 / 29** |
| `b10` | the observed scalar-descent adapter | 2 / 29 |
| `b14` | a replacement source-owned Shiab | 1 / 29 |

`b9` is a **cut vertex of size one**: removing it disconnects 28 of 29 open rows
from any construction path. And `b14`'s second disjunct is *"or advance the
coupled-functional build"* — i.e. `b9` again — so the true independent-root
count on this axis is **two**, and one of the two (`b10`) is the row §2.1
refutes.

**`b9` is named explicitly by exactly 2 of the 35 rows: `RA-E1` and `RA-E3`.**
Ten rows say "stationary" / "vacuum" / "source action" in their own text
(`RA-A1, A2, A4, A6, A8, E1, E6, F1, G3, G4`) and **only `RA-E1` says which
action**. The rest inherit an unresolved three-way fork silently — the fork the
repo has already registered as `PD-I2B-ACTION-OWNER`, whose `CHECK` reads *"Name
the action/residual owner before importing any symbol or Spencer result"*, and
whose open item is *"whether GU's operative second action is the literal
endpoint square, an action-consistent `E_act` square, or a fuller source
completion"* (`selected-k77-i2b-action-euler-principal-owner-comparison-2026-08-13.md:127`).

**The actionable consequence is a scheduling defect, and it is in the ledger's
own queue.** `next_work_queue` rank 1 is `RA-E1, RA-E3, RA-E4, RA-E5, LT-SM5,
LT-SM6`; rank 2 is `RA-A1, RA-A2, RA-A6, RA-A8, RA-G4` with the instruction
*"Construct the smallest action-stationary nonhomogeneous reduced-curvature plus
full-II vacuum and compute its global stabilizer."* Rank 1's deliverable **is**
`b9`. Rank 2's instruction **presupposes** `b9`. The two ranks are presented as
independent tracks; the DAG says rank 2 is a strict descendant of rank 1. The
`unified-source-datum-packet-v0` Layer-0 register is explicit about the stakes:
`stationary` means *"a zero of the complete bulk, defect, section, ghost-free
Euler system including boundary terms"* and specifically **not** *"W177 or a
retained local truncation."*

**Proposed repair, cheap:** give the five rank-2 rows an explicit action-owner
field, or re-order rank 2 behind rank 1. `PD-I2B-ACTION-OWNER`'s trigger is
currently scoped to *transferring* a symbol between the three rivals; it does not
fire on *constructing* against an unnamed one. Widening the trigger by one clause
is the whole fix.

### 2.3 MIMO control — the axis is controllable in 13 directions and unobservable in one

**Controllability.** All 14 grant atoms have non-empty support, so every atom is
reachable; the reachable subspace has dimension 13 because two columns coincide.

**Observability — the one failure.** `b2` (`J_SELECTION`) and `b5`
(`GLOBAL_DESCENT_MU6`) have **identical support** `{RA-A2, RA-A6, RA-A8}`. From
row status alone, no observation on this axis can distinguish which of the two
moved. That single coincidence is the entire reason the rank is 13 and not 14.

They are **not** the same object. CB-A types them as different unknowns: `U1`
(the action-selected stabilizer, which owns the `J` selection, `cb-a:182`) and
`U6` (the global form's central/discrete data, *"`failed` as a derivation"*,
`cb-a:187`). So the axis is unobservable with respect to its own declared
unknown vocabulary.

**Actionable, and cheap.** `RA-A6` is the only row that could separate them: its
`mapping_grade` carries **both** `GLOBAL_MU6` and `J_DESCENT` as open in one
token. Splitting `RA-A6` into a `μ₆`-global-form row and a `J`-descent row
restores observability and raises the axis rank from 13 to 14 — the probe's
control `separating b2 from b5 must raise the rank to 14` verifies this exactly.
This is a one-row split with a computed justification, and it is the only place
on the axis where a split *adds* information rather than diluting the
denominator.

### 2.4 Queueing theory — the axis is an accumulator, not a queue

Arrival and service, measured from the ledger's own `migrations`:

- 244 migrations across 54 version steps (v0.174 → v0.258);
- **0** change a verdict; **0** change a reason kind; all 244 change only
  `mapping_grade`;
- 141 of the 244 are REPRESENTATION, concentrated on **13 rows**;
- **22 of 35 REPRESENTATION rows have not been touched in 84 ledger versions**;
- `RA-G2` alone absorbs 32 (23% of axis service), `RA-E3` 26, `RA-E1` and
  `RA-F2` 18 each.

The departure rate for *row completion* is exactly zero over the whole measured
window. Little's law degenerates: `L = 29` open, `μ_departure = 0`, so mean
residence time is unbounded. Every unit of service is **re-service of a job
already in the system**.

This is not a criticism of grade migration, which LA-1 correctly identifies as
the ledger's honest movement class. It is the measurement the science council's
Seat 3 asked for and could not obtain: **coverage theatre is defined there as
`residue_count` growing while the `residue_tightness` histogram stays flat**, and
the histogram is `residue.tightness_provisional = {T4:1, T3:3, T2:1}` — five
graded items for 82 rows, unchanged. The detector as specified fires.

**Actionable:** the guard tuple lives at ledger level, not row level. Putting a
`tightness` grade on each row is what makes the histogram diffable per wave, and
it is the only version of the guard that can distinguish "`RA-G2` was serviced 32
times and got tighter" from "`RA-G2` was serviced 32 times and moved sideways."

### 2.5 Model theory / definability — the `RA-C1` seam decided, and a second defect underneath it

The filed seam (respected, not re-found): `RA-C1` is `DERIVED` while
`RA-B1..B5` carry the same `U1` dependence at `DERIVED_CONDITIONAL`.

Split the shared condition by Beth's criterion — is the object *implicitly*
defined by the base theory, hence *explicitly* definable?

- **(i) the stabilizer is `G_SM`-shaped.** Not definable over the base. This is
  `b1 + b4`, and it is a genuine parameter.
- **(ii) the hypercharge line, given (i).** *Implicitly* defined: LA-1's exact
  36/36 recheck finds exactly two `(a,b)` reproducing the §11.3 multiset,
  conjugate under the `SU(2)_R` Weyl element `w4 ↔ w5` — one physical line, **0
  bits**. By Beth, implicit definability gives explicit definability, so (ii) is
  a *theorem of the base*, not a choice.

`RA-C1`'s claim ("all six hypercharges arise from one line") is (ii)-only, so
`DERIVED` is right. `RA-B1..B5`'s claims are (ii)-only at branching grade, so
their `DERIVED_CONDITIONAL` correctly marks the inherited (i). **Both filed
verdicts are locally defensible. The seam is not in the verdict.**

**The defect is in the revival triggers, and it is worse than the seam.**

| row | revival trigger | the set it quantifies over |
|---|---|---|
| `RA-C1` | "a selected embedding outside the unique Weyl orbit" | **∅** — the orbit is unique (LA-1, exact) |
| `RA-B1..B5` | "a different selected embedding" | **∅** — same theorem |
| `RA-A3` | "a different stabilizer **or** noncompact hypercharge embedding" | disjunct 1 **live**; disjunct 2 **∅** |

**Six of the 35 rows are unrevivable as typed**, and the live condition — the
stabilizer, `b1 + b4` — is named in exactly one of the six (`RA-A3`). This is
the dual of the channel's ban on vacuously-true assertions: a revival trigger
over an empty set is a **vacuously-false falsifier**, and a row with one is
unfalsifiable, which is the property the ledger's whole revival-trigger column
exists to prevent.

**Proposed repair, no verdict movement:** rewrite `RA-B1..B5` and `RA-C1`
revival triggers to name the stabilizer, and record the embedding disjunct as
`PROVEN_UNSELECTABLE` on LA-1's evidence. That is the same movement class v0.258
itself used four times — condition/grade migration at unchanged verdict and
reason kind.

### 2.6 Category-theoretic functorial semantics — the ledger dropped its own grant index

The map `CB-A (41 rows, 2026-08-05) → v0.258 (35 RA rows + 6 AC aliases)` is
functorial on **objects** (every source row has a canonical target, recorded in
`aliases`) but **forgets the morphisms**: CB-A's dependency structure — the
`U1..U7` unknowns and the per-row `NEEDS-Uk` labels that carry the whole fan-out
argument — has no image in the ledger.

Checked exactly against the v0.258 JSON: the strings `"U1"`, `"U4"` and
`"grant"` each occur **0 times**. Every occurrence of `U6` is the substring in
`MU6`. The ledger records *what each row needs* in prose, and *nowhere* records
*which named unknown* supplies it.

Consequences, both measurable:

1. **The program's highest-fan-out number is not recomputable from the ledger.**
   CB-A's *"fixing `U1` alone converts 19 of 41 rows"* (`:205-206`) cannot be
   checked, updated, or re-derived from v0.258 by anything but a hand pass like
   this one. This assessment is that hand pass, and it gets `b1`'s in-axis
   fan-out at 22 and `b4`'s at 10 — different objects from CB-A's `U1`, on a
   different denominator, ten days later.
2. **Axis scoping silently truncates fan-out.** 5 of CB-A's 19 `U1` rows
   (`CB-A:C2..C6`) now live on `ANOMALY_CONSISTENCY`. Any axis-scoped assessment
   under-reports `U1` by those five, and there is no index that would reveal it.

**A small, independently checkable inconsistency while in there.** CB-A's
dependency census (`:194`) lists `U1`'s rows as *"A1–A6, A8, B1–B6, C1–C6, G4"*
— a string naming **20** labels under a stated count of **19**. Stated
honestly: the census arithmetic does **not** adjudicate, because it closes under
either reading (`19+5+4+4+2+1+0+11 = 46` minus the five double-counts `D2`,
`D4`, `F1`, `G1`×2 `= 41`; or `20+…= 47` minus six, with `A6` also
double-counted, `= 41`). What does adjudicate is that `A6`'s own row is typed
**`NEEDS-U6`** (`:222`) and `A6` is separately listed as `U6`'s only row, so the
range should read `A1–A5` and the count 19 is the right number. A one-character
fix, filed for completeness. Distinct from the already-known "+11 constraint
surplus overcounts by 5" defect, which is a Lagrangian constraint count on a
different table entirely.

### 2.7 Optimization duality — the cheapest certificate set, computed both ways

**Primal.** Exhaustive max-coverage over the 14 atoms — for each budget `k`, the
maximum number of the 29 open rows a `k`-grant certificate closes:

| `k` | rows closed | the optimal certificate |
|---:|---:|---|
| 1 | 1 | `b10` |
| 2 | **6** | `b1 + b4` |
| 3 | 8 | `b1 + b4 + b7` |
| 4 | 11 | `b1 + b3 + b4 + b7` |
| 5 | 14 | `b1 + b3 + b4 + b7 + b8` |
| 6 | 17 | `+ b12` |
| 7 | 19 | `+ b11` |

**The single most efficient certificate available on this axis is `b1 + b4` —
two grants, six rows — and it is exactly the class LA-1 already unified**
(`RA-A3`, `RA-B1..B5`). LA-1 did not find one instance among many; it found the
axis's unique two-grant optimum. Everything after that is expensive: five grants
buy 14 of 29, and the marginal rate falls to 2 rows per grant.

**Dual.** The maximum set of open rows that are pairwise condition-disjoint is
**4**, with the exhibited certificate `{RA-A1, RA-D2, RA-E2, RA-F3}`. No single
grant touches two of those four, so **at least four mutually independent
constructions are required** to close the axis — a certified lower bound
independent of any construction plan.

The primal–dual gap is the honest measure of how non-decomposable the axis is:
the dual says "≥4 independent efforts", the primal says "even 7 well-chosen
grants leave 10 rows open".

### 2.8 Reliability engineering / FMEA — the failure modes, ranked by blast radius

Rows blocked if a grant proves unsupplyable, transitively through the DAG:

| grant | direct | transitive | note |
|---|---:|---:|---|
| `b9` operative second action | 2 | **28 / 29** | the cut vertex; a single-point failure with no redundancy |
| `b1` action-stationary vacuum | 22 | 28 / 29 | inherits `b9`'s blast radius |
| `b4` global stabilizer | 10 | 12 / 29 | |
| `b8` BV/BFV physical cohomology | 9 | 10 / 29 | |
| `b7` mass matrix / vacuum Hessian | 8 | 8 / 29 | |
| `b11` physical carrier projection | 5 | 5 / 29 | |

**The FMEA finding proper:** the axis has **no redundancy anywhere**. Every one
of the 14 atoms is the sole owner of at least one row, so no grant is
substitutable, and the minimum covering set is all 14. There is exactly one
place where redundancy is even written down — `RA-D2`'s `distance` offers
*"a genuinely different source-owned Shiab/restricted port **or** advance the
coupled-functional build"* — and both disjuncts route to `b9`. A designed system
with a 28/29 single point of failure and one nominal redundancy that is not
independent would be rejected at review; that is the correct engineering read of
the axis and it should be said plainly.

### 2.9 Information theory — where the axis's entropy actually lives

LA-1 found that one grant is worth 0 bits. Generalising across the 14:

| grant | selection entropy | source |
|---|---|---|
| `b4`, hypercharge-line half | **exactly 0 bits** | LA-1 36/36; `channel-swing-CH-SM:238` "UNIQUE mod Spin(10) Weyl — 0 bits" |
| `b2`, the `J` selection | 20-dim orbit with a **provisionally 0-dimensional** local quotient | `residue.canonicity_distance.reductions.provisional_unbooked`: `J_LOCAL_ORBIT_DIM20_TO_POINT_IF_PHYSICAL_BV_ACTION_STABILIZER_RANK20` — unbooked |
| `b5`, the global form | `fixed_q_orbit_dimension_before_gauge: 0`; the `μ₆` kernel is *computed*, conditional on an unselected rank-one `v_PSB` | `residue.conditional_q_reduction`; `cb-a:222` |
| `b9`, the action owner | a **named 3-way discrete fork**, ≥ log₂3 ≈ 1.58 bits, third horn an open class so unbounded above | `selected-k77-i2b-...-2026-08-13.md:127` |
| `b13`, the count | *"at least one bit plus one integer with open codomain"* | `residue.external_discrete` |
| `b1`, the vacuum | **function-valued**, not a finite selection | `residue.function_valued_at_least: 19` |
| `b6`, the radial coefficient | a real parameter (`STATIONARY_NONZERO_AMPLITUDE`) | `residue.conditional_q_reduction.open` |

**The generalisation of LA-1, stated as a consequence:** the entire *finite
discrete* information content of this 35-row axis is roughly **one ≥log₂3 fork
plus one external bit plus one integer**. Everything else is either provably or
provisionally 0-bit, or is function-valued and therefore not a selection problem
at all.

**Actionable:** the axis's remaining work is an **existence** problem, not a
**selection** problem. Every probe that tests a selector against `b2`, `b4` or
`b5` is attacking a 0-bit half. That is the axis-scoped instance of the science
council's §2.1 finding — three artifacts in one day returning three
non-selections of one quantity — and it says which lens the next probe should
use: variational/analytic existence, not representation-theoretic selection.

### 2.10 PDE well-posedness (Hadamard) — the axis fails two of three conditions, in opposite directions

Read the axis as a solution map, grant vector → row-status vector.

- **Existence: FAILS.** `RA-D2` is `OVER_DETERMINED / GENUINE_FALSIFICATION`,
  `EXACT_ROUTE_KILL_ON_FIXED_W`. And `RA-E1`/`RA-E3` record the sharper form:
  the printed endpoint's holonomic second-jet image is **full rank 196 and
  reaches the target**, while the fixed-bank action-derived `E_act` principal
  projection is **rank 0**. The data sits in the range of one operator and not
  the other. That is a compatibility failure, not a search failure — no amount
  of searching the `E_act` side finds it.
- **Uniqueness: OVER-HOLDS.** Wherever a grant exists at all, the downstream
  answer is unique. `RA-C1` is `EXACT_ZERO_FREEDOM`; the hypercharge line is 0
  bits. There is no non-uniqueness anywhere on the axis.
- **Continuous dependence: FAILS.** The frontier of §2.7 is a step function:
  `k=1 → 1 row`, `k=2 → 6 rows`. There is no grant whose partial supply produces
  partial row movement, and the two rows with signature size 1 are exactly the
  two whose routes are refuted (`RA-E2`, §2.1) or dead (`RA-F3`, `DEAD_ROUTE`).

**The synthesis this lens supplies:** the axis is **over-determined in the small
and under-determined in the large**, with no intermediate regime. That is the
structural reason §2.4's measurement looks the way it does — a system with no
continuous-dependence regime can only emit grade refinements, because there is
no partial-credit direction for a verdict to move along.

### 2.11 Numerical conditioning — every cheap row is already refuted or dead

Signature-size distribution over the 29 open rows: `{1:2, 2:11, 3:12, 4:3, 5:1}`,
77 atom-edges total. Reading signature size as a condition number (how many
simultaneous inputs are needed before any output appears):

- the two `|sig| = 1` rows are `RA-E2` (refuted route, §2.1) and `RA-F3`
  (`DEAD_ROUTE`);
- **therefore every live row on the axis has condition number ≥ 2**;
- 11 rows sit at 2, and **6 of those 11 are the single `b1+b4` class**, so the
  live `|sig|=2` frontier is really 6 distinct efforts, not 11.

There is no row on this axis that amplifies a single input into an output. That
is worth stating because it is the precise sense in which "partial progress"
here buys nothing: a wave that supplies half of `b1+b4` closes zero rows.

### 2.12 Port-Hamiltonian / bond graph — 10 dissipative elements, 7 distinct, and no feedback loop

In bond-graph terms a grant is a **source** (an external port injecting effort),
not a dissipative element — which is exactly why the channel's laundering rule is
the right one: a row advanced on a grant has energy injected from outside, and
removing the port from the graph while keeping the row is a violation of the
interconnection structure, not a bookkeeping preference.

Assigning causal strokes: 25 of 35 rows are **pure sinks** — the stroke points
inward, and no work on the row can back-propagate to determine a grant.

The 10 rows carrying a reverse edge are those whose `mapping_grade` records a
negative or exclusion result: `RA-A1`, `RA-C1`, `RA-D2`, `RA-D4`, `RA-E1`,
`RA-E3`, `RA-E5`, `RA-F3`, `RA-G2`, `RA-G4`. But four of them (`RA-A1`, `RA-D4`,
`RA-E5`, `RA-G4`) carry the *same* exclusion token,
`INVARIANT_HQ_NONSELECTION_EXACT`, so the axis has **7 distinct reverse edges,
not 10**.

**Consequence: the axis cannot bootstrap.** Grant → row is the only causality,
so no volume of row construction determines `b9`. The only row work that acts on
the grant side is exclusion work, which shrinks the grant's candidate space.
That is a direct, schedulable recommendation and it agrees with the science
council's Method A (compute the space, then exclude it wholesale) reached from a
completely different direction.

### 2.13 Order / lattice theory — the frontier is 8 classes, not 29 rows

The 22 condition signatures, ordered by inclusion:

- **8 minimal elements** form the antichain frontier — the correct granularity
  for a work queue: `{b10}` (`RA-E2`), `{b13}` (`RA-F3`), `{b1,b12}`
  (`RA-E7`, `RA-G3`), `{b1,b4}` (`RA-A3`, `RA-B1..B5`), `{b1,b7}` (`RA-A4`),
  `{b1,b8}` (`RA-F2`), `{b8,b11}` (`RA-G2`), `{b1,b2,b5}` (`RA-A2`, `RA-A6`);
- **11 maximal elements**;
- **poset height 3** — the longest strict inclusion chain has three links, e.g.
  `{b1,b7} ⊂ {b1,b3,b7} ⊂ {b1,b3,b4,b7}` = `RA-A4 ⊂ RA-E5 ⊂ RA-G4`.
- The family is **very far from a lattice**: 199 of 231 signature pairs have a
  union that is not a realised signature, and 97 have a non-empty intersection
  that is not realised. So the signatures do not close under either operation —
  the axis's condition structure is a bare poset, not an algebra, and there is no
  "combine two rows' conditions" move available.

**Actionable:** `next_work_queue` currently ranks *rows*. The order-theoretic
object it should rank is the **8-element minimal antichain**, because a row that
is not minimal cannot be attacked before its predecessor and putting it in a
queue rank is a scheduling error of the kind §2.2 documents concretely.

### 2.14–2.18 Science council seats 2–6, applied to this axis

**Seat 2 — was the conditional pivot correct, and what has not been tried.**
Vindicated on this axis, for a reason the council could not yet measure: the
pivot's payoff is that the axis has **exactly one live root**, and component
mapping is what exposed that. The council's own top untried move — *"the
deliberately-wrong complete construction (strawman total action) … change one
fork, count the rows that move"* — is now a **pre-registered prediction rather
than an exploration**: stipulate any admissible `b9` and the count is **28 of
29**, with `RA-E2` the sole exception and §2.1 showing even that exception is
re-parented onto the action by MD-1's third clause (*"whether they reappear as
independent 4D fields is a dynamical question about the action"*). If the
strawman is built and the count is not 28, this assessment's DAG is wrong and
should be discarded. That is the cheapest available falsifier for everything in
§2.

**Seat 3 — efficiency of the mapping itself.** The council ruled the missing
denominator was the real defect. The denominator now exists. The **new** defect
one layer down is that the ledger dropped the grant index (§2.6): with 82 rows
and no `U1..U7` column, the fan-out argument that justified building the ledger
cannot be run against the ledger. The council's guard tuple is at ledger level;
§2.4 shows why it must be at row level. And the council's coverage-theatre
detector, applied as specified, fires: 244 migrations, 0 verdict departures, a
five-item tightness histogram for 82 rows.

**Seat 4 — refuse the self-flattering conversion.** The most available one here:
"the axis has a single cut vertex, therefore GU reduces to one problem, therefore
the program is one theorem from closure." **Refused.** The cut vertex is a
property of how the rows are currently *written*; a different decomposition of
the same physics gives a different graph, and `b9` being a single vertex says
nothing about its difficulty — it is function-valued with a three-horn fork and
an open third horn, i.e. the hardest object on the axis, not the easiest. The
second available conversion — "MD-1 refutes `RA-E2`, therefore the GU Higgs
mechanism is excluded" — is refused under `source-native-comparator-routing.md`
and under MD-1's own open `SOLDERED-AD` fork. §2.1 files a **typing defect**, and
that is all it files.

**Seat 5 — where to point pre- and post-reviews.** Two standing questions this
axis's evidence would have earned, both mechanical:
- **PRE-4:** *Does every row this wave touches name its action owner?* Would have
  caught `next_work_queue` rank 2 (§2.2). Ten rows say "stationary"; one says
  which action.
- **POST-3:** *Does every revival trigger this wave writes quantify over a
  non-empty set?* Would have caught `RA-B1..B5` and `RA-C1` (§2.5) at the moment
  the 0-bit theorem was banked in July, not eight months of versions later.

**Seat 6 — the honest ranking for this axis.** Scored under the council's Rule R
(branches changed × probability ÷ cost, tie-broken to non-substitutable moves
whose negative outcome is as informative as the positive):
1. **Re-order `next_work_queue` rank 2 behind rank 1, or give its five rows an
   action-owner field** (§2.2). Cost: minutes. Changes the status of 5 rows'
   schedulability and prevents a repeat of the wave-K failure mode the council
   measured at seven waves. Non-substitutable: only the ledger owner can do it.
2. **Migrate `RA-E2` against MD-1 and re-check `RA-E1/E3/E4`'s inherited
   `CB-A:E2` premise** (§2.1). Cost: hours. Both outcomes informative — either
   the row's route is dead on the source-declared horn, or the `SOLDERED-AD`
   fork is what is really blocking it, and that fork is decidable finite work.
3. **Rewrite the six empty revival triggers and split `RA-A6`** (§2.5, §2.3).
   Cost: hours. Restores falsifiability to 6 rows and observability to the axis,
   raising the rank from 13 to 14.

Ranked below all three: adding rows. The denominator is not the constraint.

---

## 3. LENSES THAT RETURNED NOTHING

Reported because a lens that finds nothing is information.

- **Reliability MTBF / availability modelling proper.** Ranked failure modes
  (§2.8) are real; time-to-failure and availability numbers are not, because
  there is no repair-time data and any figure would be invented. Not filed.
- **Queueing beyond the degenerate case.** Once the departure rate is exactly
  zero, every classical queueing quantity — utilisation, `L`, `W`, waiting-time
  distributions — is either degenerate or undefined. §2.4 reports the one thing
  the lens can support and stops. There is no `M/M/1` reading of this axis.
- **Port-Hamiltonian in its quantitative form.** The energy-balance / Dirac-
  structure machinery needs a metric on the state space; the ledger has none, and
  the residue's `canonicity_distance` is explicitly `physical_residue:
  UNCOMPUTED_UNTIL_SCOPES_COMPOSE`. §2.12 uses only the causality-and-ports half,
  which is the part that survives without a metric. Anything quantitative here
  would be decoration.
- **PDE well-posedness beyond the three Hadamard conditions.** Symbol, ellipticity
  and energy estimates have no ledger-level referent. The lens returns exactly
  three facts and no more; anything about "the ledger's principal symbol" would
  be metaphor. Not filed.
- **Numerical conditioning as an actual condition number.** There is no norm on
  the grant space, so κ(M) is not defined. §2.11 uses signature size as an
  ordinal proxy and says so. The lens's quantitative half is empty.
- **Category theory beyond the forgetful-functor observation.** No sheaf
  condition, no gluing failure, no adjunction was found that is more than
  restatement. §2.6's finding is really a traceability finding wearing categorical
  vocabulary, and it is honest to say so.

---

## 4. POSTFLIGHT — five specialist lenses, run inline

### Lens Q1 — hostile reviewer: the strongest overclaim in this artifact

**The grant vocabulary is mine, and every downstream number inherits it.** The
14 atoms are a hand-audited reading of 35 prose strings. A different reader could
merge `b3` into `b1`, or split `b8` into BV, BFV and domain, and the rank would
move. The certificate's `[T]` tag marks the table as a *declared input*, not a
derived result, and the sensitivity sweep (§1.3) tests only three perturbations I
could think of, not the space of readings.

**What survives that objection.** Three things are basis-independent:
(a) the migration statistics (§2.4) — read directly off the ledger, no
vocabulary involved; (b) the six empty revival triggers (§2.5) — a property of
quoted strings plus one banked theorem; (c) `RA-E2`'s uncited refutation (§2.1)
— two quoted artifacts and a grep count of zero. **Those three would survive a
complete rewrite of the atom table. The number 13 would not necessarily.** The
honest headline is therefore "13, on a stated and reproducible vocabulary, robust
to every perturbation I could construct" — not "13, full stop."

### Lens Q2 — contrarian: the strongest contrary reading

**"The cut vertex is an artifact of writing rows in the object language of the
action."** Every row that says "action-stationary vacuum" was *written* by an
agent already committed to the construction route. A ledger written from the
observable side — start from the measured `SU(3)×SU(2)×U(1)` and ask what pulls
back — would have a completely different DAG, plausibly with no single root at
all. On that reading §2.2 measures the repo's habit, not GU's structure.

**This reading is strong and I cannot refute it.** What I can say is that it is
*testable and cheap*: the science council's Seat 2 alternative #2 ("work backward
from a required output") is exactly the rewrite it proposes, and `K77-B3`'s
restricted-domain route already does it locally. If someone runs the backward
ledger and it has no cut vertex, §2.2's scheduling recommendation is wrong and
should be withdrawn. I do not think that outcome is likely — `b9` is upstream of
"stationary" in *any* variational formulation — but the reading is not disposed
of by saying so.

### Lens Q3 — seam auditor: the weakest seam

**The `b9 ≺ b1` edge.** It carries 28 of the 29 rows and is the least directly
quoted edge in the DAG. Its licence is an inference — *an "action-stationary"
object presupposes a named action* — supported by `PD-I2B-ACTION-OWNER`'s CHECK
and by the `unified-source-datum-packet` Layer-0 register entry for `stationary`,
but **no ledger row states it.** If the A-block's "action" is in fact the
*released first action* (which exists), then `b9 ⊀ b1`, the DAG has 3 independent
roots instead of an effective 2, and §2.2's scheduling finding weakens to "rank 2
should declare which action it means" — still a real finding, but a much smaller
one.

**This is the load-bearing seam and it is decidable in an afternoon:** read the
five rank-2 rows' evidence files and record which action their stationarity
condition is written against. `selected-k77-action-concomitant-residual-flag-gate-2026-08-12.md:98`
says only *"Construct the smallest target-blind action-stationary nonhomogeneous
orbit"* — target-blind, and action-unnamed. That is suggestive, not decisive.

**Second seam, smaller:** the `[T]` condition table treats `RA-A3` and
`RA-B1..B5` as carrying `b1` as well as `b4`. §1.3 shows the rank is unchanged if
they do not, so nothing numerical rests on it, but the *reading* of §2.5 does:
if the embedding rows genuinely need only `b4`, then "the stabilizer" and "the
vacuum" are separable and the six-row class is cheaper than stated.

### Lens Q4 — exactness auditor

37/37, exit 0. All arithmetic is Python `int`, `GF(2)` XOR, or
`fractions.Fraction`; the file contains no `float`, no `numpy`, no `math`. The
two rank computations are independent implementations of the same elimination
over different fields and agree, which is a real cross-check and not a
tautology — a bug in the pivot logic would generically break `GF(2)` and `Q`
differently. Seven controls fire, including three mutation controls that must
*move* the answer and do. **No N/N certificate is claimed for anything not in
the script:** §2.1, §2.5, §2.6 and §2.9 are argued from quoted strings and
grep counts, not computed, and are labelled as such.

### Lens Q5 — routing and scope auditor

No verdict, reason kind, residue, quotient, P1/P2/P3 assignment, canon verdict
or public posture moves. No row advances. No grant is carried into a derivation.
No comparator result is used to adjudicate a source-native row. Every escalation
in the frontmatter is a proposal to the canonical owner, not an action. The
`target_claim` is `NONE-NOT-A-KILL` and is honest: the objects adjudicated are
ledger condition strings, revival triggers, an alias map and a queue ordering.
**No registered GU source claim is targeted, attacked or defended anywhere in
this document.**

---

## 5. WHAT THE CANONICAL OWNER IS BEING ASKED TO DISPOSITION

Eight escalations, none of them a verdict change.

| # | rows | proposal | evidence |
|---|---|---|---|
| 1 | `RA-A1, RA-A2, RA-A6, RA-A8, RA-G4` | give each an explicit action-owner field, or re-order `next_work_queue` rank 2 behind rank 1 | §2.2; `PD-I2B-ACTION-OWNER`; `unified-source-datum-packet-v0:stationary` |
| 2 | `RA-E2` | migrate `distance` + `mapping_grade` on MD-1 evidence; attach the `SOLDERED-AD` fork; the named adapter is built, not missing | §2.1; MD-1 67/67; datum packet `:128` |
| 3 | `RA-E1, RA-E3, RA-E4` | re-check the inherited `CB-A:E2` shape premise against MD-1 in the same pass | §2.1 |
| 4 | `RA-B1..B5, RA-C1` | rewrite six revival triggers that quantify over ∅; record the embedding disjunct as `PROVEN_UNSELECTABLE` on LA-1's evidence | §2.5; LA-1 36/36 |
| 5 | `RA-A6` | split into a `μ₆`-global-form row and a `J`-descent row; restores axis observability and raises the rank 13 → 14 | §2.3 |
| 6 | ledger-level | add a cross-axis grant index; `"U1"`, `"U4"`, `"grant"` occur 0 times in v0.258 | §2.6 |
| 7 | ledger-level | move the guard tuple from ledger level to row level so the tightness histogram is diffable per wave | §2.4; council §5.5 |
| 8 | `cb-a-representation-content-2026-08-05.md:194` | the `U1` row lists 20 labels under a count of 19; the range should read `A1–A5` | §2.6 |

---

## 6. WHAT THIS ARTIFACT IS NOT

Not a source action. Not a vacuum, a stabilizer, a Higgs carrier, a chirality
mechanism, a generation count or a mass matrix. Not a physics result of any
kind. Not a claim about GU's viability, difficulty or prospects. Not a
comparator adjudication. Not a ledger edit. Not an advance of any row.

It is a structural measurement of how 35 ledger rows are wired to their
conditions, and a list of eight places where the wiring is wrong or unrecorded.
