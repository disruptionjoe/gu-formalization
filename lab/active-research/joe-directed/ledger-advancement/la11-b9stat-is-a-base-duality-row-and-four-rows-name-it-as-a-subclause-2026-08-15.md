---
artifact_type: exploration
status: exploration
doc_type: ledger-delta
created: 2026-08-15
work_item: LA-11
channel: conditional_ledger_advancement
route: CONSTRUCT__WRITE_THE_ROW_THREE_ROUTES_CONVERGED_ON
base_revision: a148ed80
ledger_base: lab/process/conditional-physics-ledger-v0.258.json
ledger_edit: none -- versionless delta, for the canonical owner to disposition
axis_scope: LAGRANGIAN (proposed row) + 14 declared-condition rows across all three axes
delta_kind: VERSIONLESS_DELTA__NOT_A_LEDGER_EDIT__ROW_PROPOSAL
target_claim: "LA-10-2026-08-15-B9STAT-NAMED-BY-ZERO-ROWS -- \"Splitting `b9` shows the true 28/29 root is 'a zero of the complete ghost-free Euler system' -- the Layer-0 typing of `stationary` -- an object that ZERO v0.258 rows name\", and its Lens-Q5 instruction \"the cheapest real action available on this ledger is to write one\" (lab/active-research/joe-directed/ledger-advancement/la10-the-cut-vertex-survives-and-is-not-the-second-action-2026-08-15.md sections 3.3 and 5). Secondary target: OT-1's factorisation of clause O4 into (a) an Ad-invariant nondegenerate fibre form and (b) a base duality \"which no row names\", with unpaired remainder (d-1)*91 = 273 on X^4 and 1183 on Y^14. Both are INTERNAL CHANNEL claims about the ledger, not canon claims and not GU physics claims."
target_claim_verdict: LA-10 CORRECTED_AT_THE_LITERAL_LEVEL_AND_UPHELD_AT_THE_OPERATIVE_LEVEL -- four rows DO name the datum, none as its own deliverable; OT-1 SURVIVES_INDEPENDENT_REDERIVATION with one loose parenthetical corrected
canon_verdict_change: none
priority_change: none
steering_effect: unchanged
canonical_effect: pending_integration
title: "LA-11: the row is WRITABLE and is written. `b9_STAT` sharpens to the VARIATIONAL DUALITY, not to a background: the datum that makes the complete ghost-free Euler system an equation at all, factoring as (a) an Ad-invariant nondegenerate fibre form on `ad P` and (b) a source-owned global base duality on `X^4` absorbing the one-form index. Proposed as `LT-GR6b`, source_row `CB-B:GR-6#variational-duality`, NEEDS / MISSING_CONSTRUCTION, with a `revival_trigger` that passes all four of LA-9's failure modes and carries a constructive non-emptiness witness. LA-10's `zero rows name it` is CORRECTED: four rows name a base-duality datum in a demand field (`LT-GR6`, `LT-SM8`, `RA-E1`, `RA-E3`), in every case as a MODIFIER inside another head demand; ZERO rows carry the Layer-0 completeness qualifier, ZERO rows demand both legs of OT-1's factorisation, and ZERO revival triggers have a pairing/metric/duality/density object as their head deliverable. OT-1's clause-O4 obstruction is re-derived from structure constants and SURVIVES exactly -- invariant-form space of dimension `d+1`, identically zero `V-V` block, max rank exactly `2 dim g`, threshold exactly `d = 2`, radical `ker(c) (x) ad` of dimension `(d-1) dim g` = 273 on `X^4` and 1183 on `Y^14` -- with one correction: `dim so(6,4) = 45`, not 91, so OT-1's parenthetical alternate gives 135 and 585. FOURTEEN rows should gain the new row as a declared condition (8 REPRESENTATION, 4 LAGRANGIAN, 2 ANOMALY), which is 8 of 29 on REPRESENTATION and NOT 28 of 29: reach is a DAG property, declaration is a text property, and the difference is the honest cost of writing the row. ZERO rows advance; no `SAME` row is touched; the denominator moves 82 -> 83 in the direction that makes the program look worse."
grade: "EXACT integer / fractions.Fraction arithmetic; Lie-algebra structure constants built in-file, not quoted; rank over Q by fraction-free elimination; no numpy, no float constructed anywhere; assert_no_float sweeps the whole result dict. 192/192 checks, exit 0, via tests/channel-swings/joe_directed_b9stat_row_construction.py run from the repository root under _local/cas-venv. Certificate splits as 159 [E] exact results, 20 [C] controls that must fire, and 13 [R] reproductions of LA-4 / LA-9 / LA-10 / OT-1 facts, all reproduced exactly BEFORE being used. FAILURE PATH EXERCISED: seven planted mutations (absence_scope, condition_set, unpaired_count, reach_number, threshold, launder, nonempty_witness) each drive exit 1 through --selftest. The proposed row is read OUT of this artifact by the probe and validated against lab/process/conditional-physics-ledger-schema-v0.1.json, so the record and the certificate cannot drift. The invariant-form theorem carries four hypothesis-isolating controls that each fire: d=1 / iso(2,1), g abelian, V a trivial module, and a module-structure control showing the block-diagonal witness stops being invariant once V carries the honest adjoint action; plus a Jacobi mutation control and an sl(3) control showing the d+1 count is not an sl(2) accident. Every ledger assertion is an exact substring or count read out of the v0.258 JSON (sha256 540b50e386073c0f43da4e8d5a8ffdaf06fd243c6612622d7daf187c0a725047). NOT: a ledger edit, a verdict change, a reason-kind change, a physics derivation, a coefficient, a selection principle, a claim that any GU object exists, a claim that the ownership theorem is proved, or a claim that the proposed row is canonical."
disposition: ROW_WRITTEN_AND_USABLE__B9STAT_TYPED_AS_VARIATIONAL_DUALITY_NOT_AS_A_BACKGROUND__PROPOSED_AS_LT_GR6b_NEEDS_MISSING_CONSTRUCTION__TRIGGER_PASSES_ALL_FOUR_LA9_MODES_WITH_CONSTRUCTIVE_NONEMPTINESS_WITNESS__LA10_ZERO_ROWS_CLAIM_CORRECTED_TO_FOUR_ROWS_AS_SUBCLAUSE__OT1_O4_OBSTRUCTION_REDERIVED_AND_SURVIVES__SO64_PARENTHETICAL_CORRECTED_45_NOT_91__FOURTEEN_ROWS_GAIN_A_DECLARED_CONDITION__EIGHT_OF_29_ON_REPRESENTATION_NOT_28_OF_29__DENOMINATOR_MOVES_82_TO_83_UNFLATTERINGLY__ZERO_ROWS_ADVANCE
rows_proposed: [LT-GR6b]
rows_touched_structurally: [RA-A1, RA-A2, RA-A4, RA-A8, RA-E1, RA-E3, RA-E6, RA-G3, RA-G4, LT-GR1, LT-GR2c, LT-GR5, LT-GR6, LT-SM3, LT-SM8, AC-F1, AC-G1a]
rows_advanced: 0
rows_escalated: []
free_object_delta: 1
free_object_delta_note: "ONE new un-owned object is introduced and it is declared as such rather than hidden. It is NOT a renamed existing debt: the object is the variational duality on `Lambda^1 (x) ad P`, which section 2 shows is named by no row as a deliverable and demanded in neither leg by any row. It is also NOT a laundered grant -- it is typed `NEEDS / MISSING_CONSTRUCTION`, it takes nothing off any existing row, and it makes 14 rows strictly more conditional than they were."
depends_on:
  - lab/process/conditional-physics-ledger-v0.258.json
  - explorations/unified-source-datum-packet-v0-2026-07-30.md
  - explorations/conditional-build/cb-b-lagrangian-terms-2026-08-05.md
  - lab/active-research/joe-directed/ledger-advancement/la4-representation-axis-has-13-grants-and-a-one-vertex-cut-2026-08-15.md
  - lab/active-research/joe-directed/ledger-advancement/la4-representation-axis-incidence-probe.py
  - lab/active-research/joe-directed/ledger-advancement/la9-eleven-real-defects-eight-defective-filings-and-one-denominator-mover-2026-08-15.md
  - lab/active-research/joe-directed/ledger-advancement/la10-the-cut-vertex-survives-and-is-not-the-second-action-2026-08-15.md
  - lab/active-research/joe-directed/ownership-theorem/ot1-the-ownership-predicate-and-the-pairing-obstruction-2026-08-15.md
  - lab/active-research/joe-directed/phi-reduction/phi1-the-reduction-is-rank-one-and-the-14d-kernel-contributes-zero-bits-2026-08-15.md
scripts:
  - tests/channel-swings/joe_directed_b9stat_row_construction.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** Two objects live here.
> The **ledger** object (sections 1, 3, 4, 5) is bookkeeping about a JSON file
> and carries no physics content. The **Lie-algebraic** object (section 2)
> binds GU's own declared symmetry object `Lie(𝒲) = Ω⁰(ad P) ⋉ Ω¹(ad P)`
> (`GU-YM-Δ1`, `SC-GRP-05`), so it is not a conventional comparator — but it is
> a statement about invariant bilinear forms, not a Lagrangian, a spectrum, a
> count, or evidence for or against Weinstein's source-native mechanism.
> Classification: **`STRUCTURAL_PLUS_DEFINITIONAL`, `pending_integration`.**

# LA-11 — the row is writable, and here it is

## Verdict first, unsoftened

**I wrote a usable row.** It is `LT-GR6b`, it is typed `NEEDS /
MISSING_CONSTRUCTION`, its `distance` names a three-step construction rather
than a mood, and its `revival_trigger` passes all four of LA-9's failure modes
with a *constructive* non-emptiness witness rather than an assertion of
satisfiability.

**But the object it names is not the object the brief handed me, and that
difference is the whole finding.** LA-10's `b9_STAT` is glossed as *"a zero of
the complete ghost-free Euler system"* — a **background**, a solution. Writing
*that* as a row would relabel an absence: ten rows already demand exactly that
under the vocabulary *action-stationary vacuum / stationary background /
stationary orbit / stationary VEV*, and it is verbatim LA-4's `b1`. The object
that genuinely has no row is one step **upstream**: the datum that makes the
complete Euler system an **equation** in the first place. That is what
`b9_STAT` sharpens to, it is exactly OT-1's factor (b) composed with its factor
(a), and it is why two independent routes landed on the same missing row.

Three corrections to the convergence, all mine and all reported before use:

| filed | verdict here |
|---|---|
| LA-10: `b9_STAT` "is named by ZERO v0.258 rows" | **corrected at the literal level, upheld at the operative level.** Four rows name a base-duality datum in a demand field. In all four it is a *modifier* inside another head demand. Zero rows carry the Layer-0 completeness qualifier; zero rows demand both legs; zero triggers have a duality object as their head |
| LA-10: `b9_STAT` reaches 28 of 29 | **reproduced exactly, and it is the wrong number for this purpose.** 28/29 is a *reach* over an inferred DAG. The number of rows that should *declare* the new row as a condition is **8 of 29** on REPRESENTATION, 14 of 82 overall |
| OT-1: `𝔰𝔬(7,7)` "(or the vertical `𝔰𝔬(6,4)`), dim = 91" | **the parenthetical is loose.** `dim 𝔰𝔬(6,4) = 45`. On that algebra the remainder is 135 and 585, not 273 and 1183. The *theorem* is insensitive; the *numbers* are fixture-scoped, exactly as OT-1's own Lens Q2 warned |

Everything else in OT-1's clause-`O4` obstruction survives a from-scratch
re-derivation with no quoted inputs.

---

## 0. PREFLIGHT — six specialist lenses, run inline

Standing rule: N lenses means N sections written here, never N subagents.

### Lens P1 — ledger archivist: re-derive the pool, report what moved

Read from the JSON, not from the brief. **84 row records, 82 active canonical
targets, 2 `SUPERSEDED`** (`LT-GR2`, `AC-G1`); axes 35 / 21 / 26; verdicts
`SAME` 32 · `DIFFERS` 19 · `NEEDS` 26 · `OVER_DETERMINED` 5; reason kinds led
by `MISSING_CONSTRUCTION` 20. `sha256 =
540b50e386073c0f43da4e8d5a8ffdaf06fd243c6612622d7daf187c0a725047`.

**What moved since LA-10 and OT-1 filed: nothing.** Same base, same bytes, same
`next_work_queue`. So this route measures the same object they measured, which
is the precondition for the row to be *about* their finding rather than about a
moved target.

**One archival fact the brief did not carry and it is the reason the row is
writable at all.** Every `source_row` in v0.258 is a `CB-A` / `CB-B` / `CB-C`
reference, and the `denominator.inclusion_rule` scopes the ledger to
*"requirement[s] … enumerated by CB-A, CB-B, and CB-C."* A row with no CB
parent is out of scope by construction. **But the ledger already owns the
`#anchor` idiom** — six anchored source rows exist, five of them splitting
`CB-B:GR-2` into sub-rows. `CB-B:GR-6` is *"the Einstein field equation
itself"*, the only CB-B slot whose deliverable is an Euler equation. So
`CB-B:GR-6#variational-duality` is a provenance the ledger's own grammar
already licenses, and `LT-GR6b` is an unused ID in a namespace that already has
`LT-GR1b`, `LT-GR2a..e`, `LT-SM3b`, `AC-G1a`.

### Lens P2 — lexicographer: the failure mode of an absence claim is the synonym

An "absence" over a prose corpus is a claim about a *vocabulary*, and its
characteristic failure is the object appearing under a word the searcher did not
think of. Before touching LA-10's claim I built a five-class token set — base
duality, nondegeneracy, completeness, globality, positivity — with 21 / 11 / 8 /
2 / 4 members including the informal spellings (`dvol`, `soldered`, `vielbein`,
`tetrad`, `Riemannian`, `frame field`) and the ledger's own SHOUTING grade forms
(`HODGE`, `DENSITY`, `MEASURE`, `METRIC`). **The lens's prediction, written
before the sweep: the claim would fail, because a ledger this verbose does not
have a zero.** It did fail — and it failed in a way that makes the row *more*
justified, not less (§2.2).

### Lens P3 — evidence law: polarity, and where a demand is allowed to live

LA-10's own Lens P4 rule, adopted here: a certificate marked `EXACT`,
`REJECTED`, `KILLED` or `RETRACTED` asserts the object **is established**, so it
cannot witness that a row **waits on** it. I add the dual rule this route needs:
a demand lives in `distance` or `revival_trigger` — the fields that say what
must be *built* — while `mapping_grade` says what has been *found*. So the
sweep is run **per field**, and a `mapping_grade` hit is evidence of supply, not
of demand. This single discipline is what separates "the ledger mentions
metrics" (it does, in four places) from "a row's condition is a metric" (no row).

### Lens P4 — representation theory: is the obstruction a theorem or a dimension count?

OT-1's `O4` result is quoted everywhere in the convergence as *"no `𝒲`-invariant
nondegenerate pairing exists."* That has two possible proofs with very different
strengths: a *dimension count* (`dim V = d·n > n`, so nothing can pair `V`
perfectly with `𝔤`) or a *structure theorem* (the `V–V` block is forced to
vanish). The first is nearly vacuous; the second is the real content, because it
is what rules out fixing the problem by adding a `V–V` term. **The lens's
prediction: the structure theorem is the load-bearing half, and the four
controls OT-1 lists are all controls on it.** §2.1 re-derives it from structure
constants and confirms this — and the module-structure control I added (§2.1,
`[C]`) is the sharpest form: the block-diagonal form `κ ⊕ 1` **is** invariant
when `V` is a trivial module and **stops being invariant** the moment `V`
carries the honest adjoint action, at identical dimensions. The killer is the
module structure, not the dimension count.

### Lens P5 — requirements engineering: a precondition is not a requirement

The ledger counts *requirements* — things physics asks GU to produce. The object
here is a **precondition**: something GU must have before the question "is this
requirement met?" is even well-posed. Requirements-engineering practice keeps
these in separate registers precisely because a precondition that is silently
counted as a requirement inflates the denominator and flatters progress ratios
by adding an item nobody expects to close. The lens therefore imposes a hard
duty on this route: **name the denominator move out loud, and check its
direction.** §5 does; the move is 82 → 83 and it makes the recorded
SM-disagreement rate `19/82 → 19/83`, i.e. the proposal makes the program look
**worse**. That is the correct sign for an honest addition, and it is the exact
opposite of the one flattering correction LA-9 flagged in the 2026-08-15 batch.

### Lens P6 — control theory: reach is not the same as declaration

LA-4's 28-of-29 is a **reachability** measure on a presupposition DAG whose
critical edge, LA-4 conceded and LA-10 confirmed, is stated by no row. Reach
answers *"if this object failed, how many rows would be invalidated?"*
Declaration answers *"how many rows say so?"* These differ by exactly the
uncited edges. **The lens's prediction, before computing: the declaration set
will be far smaller than 28, and quoting 28 as the number of rows to amend would
be importing the very inferred edge LA-10 broke.** It is 8 on REPRESENTATION
(§4). Writing 28 would have been the single easiest way to launder LA-4's
uncited premise into the ledger under cover of a hygiene fix.

---

## 1. THE COMPUTATION — 192/192, exit 0, zero floats, failure path exercised

`tests/channel-swings/joe_directed_b9stat_row_construction.py`, from the
repository root under `_local/cas-venv`.

```text
CERTIFICATE: 192/192 checks pass; no load-bearing float (swept).
split  [C] 20  [E] 159  [R] 13
```

Prior results are **reproduced before being used** (`[R]`), so nothing below is
an artefact of failing to rebuild them:

```text
LA-4   29 open REPRESENTATION rows · rank_Q = 13 · roots {b9,b10,b14}
       b9 reach 28/29 · unreached row RA-E2
LA-10  b9_ID reach 2/29 = {RA-E1, RA-E3} · b9_STAT reach 28/29 · named by 0 rows
LA-9   RA-B1..B5 carry the identical trigger "a different selected embedding";
       RA-C1 carries "a selected embedding outside the unique Weyl orbit"
OT-1   small-fixture minimal radical 9 = (4-1)*3 · 273 · 1183 · 182/T*W = 1/5, 1/15
```

**Failure path**, seven planted mutations, each must drive exit 1:

```text
_local/cas-venv/bin/python tests/channel-swings/joe_directed_b9stat_row_construction.py --selftest

  mutation absence_scope    exit 1  OK      (widen the demand-field sweep to grades)
  mutation condition_set    exit 1  OK      (truncate the class-S set)
  mutation unpaired_count   exit 1  OK      (273 -> 272)
  mutation reach_number     exit 1  OK      (28/29 -> 29/29)
  mutation threshold        exit 1  OK      (nondegeneracy threshold d=2 -> d=3)
  mutation launder          exit 1  OK      (re-type the row DERIVED_CONDITIONAL)
  mutation nonempty_witness exit 1  OK      (degrade the mode-EMPTY witness)

FAILURE-PATH SELFTEST: PASS (7/7 mutations drove exit 1)
```

---

## 2. THE TWO CHECKS THE BRIEF DEMANDED, RUN INDEPENDENTLY

### 2.1 OT-1's clause-`O4` obstruction — re-derived, SURVIVES

Nothing is quoted. `𝔰𝔩(2)` and `𝔰𝔩(3)` are built from structure constants,
`L = 𝔤 ⋉ (Λ¹ ⊗ ad)` is assembled, Jacobi is verified, and the space of invariant
symmetric bilinear forms is obtained as the exact nullspace of the invariance
system `B([x,y],z) + B(y,[x,z]) = 0` over **Q**.

```text
d      dim L    dim{invariant forms}    V-V block    max rank    nondegenerate?
0        3               1                  --           3            YES
1        6               2                 zero          6            YES
2        9               3                 zero          6            no
3       12               4                 zero          6            no
4       15               5                 zero          6            no
```

Every filed claim holds: **`dim = d+1`**, **identically zero `V–V` block**,
**max rank exactly `2 dim 𝔤`**, **threshold exactly `d = 2`**. Two additions of
my own: the maximum is *attained* by an explicit witness (the `c`-pairing
`⟨X, α⊗a⟩ = c(α)κ(X,a)` plus `κ` on `𝔤`), verified invariant and of rank
`min(dim L, 2 dim 𝔤)`; and `𝔰𝔩(3)` (`dim 8`) reproduces `d+1` at `d = 2`, so the
count is not an `𝔰𝔩(2)` accident.

**Four hypothesis-isolating controls, each firing:**

```text
[C] d = 1 / iso(2,1)     nondegenerate invariant form EXISTS (3d Chern-Simons)
[C] g abelian            identity form invariant and nondegenerate
[C] V a trivial module   block-diagonal kappa (+) 1 invariant and nondegenerate
[C] module-structure     that same form STOPS being invariant once V carries
                         the honest adjoint action, at identical dimensions
[C] Jacobi mutation      a mutated bracket must FAIL Jacobi
```

The fourth is the one I added and it is the sharpest: **the obstruction is
caused by the module structure, not by the dimension count.**

**The unpaired remainder.** The radical of the `c`-pairing inside `V` is
`ker(c) ⊗ ad`, dimension `(d−1)·dim 𝔤`, verified exactly at `d = 2, 3, 4` and
confirmed to lie entirely inside `V` (the `𝔤` block stays nondegenerate).

```text
dim so(7,7) = 91          (4-1)*91  =  273   on X^4        <- OT-1, CONFIRMED
                          (14-1)*91 = 1183   on Y^14       <- OT-1, CONFIRMED
dim so(6,4) = 45          (4-1)*45  =  135   on X^4        <- alternate, ON RECORD
                          (14-1)*45 =  585   on Y^14
```

**Correction:** OT-1 writes *"`𝔤 = 𝔰𝔬(7,7)` (or the vertical `𝔰𝔬(6,4)`),
semisimple, `dim = 91`."* `dim 𝔰𝔬(6,4) = 45`. The parenthetical cannot carry the
same 91. OT-1's own Lens Q2 already fenced the remainder numbers as
fixture-scoped; this makes the fence quantitative. **The `d ≥ 2` verdict is
insensitive** — checked for all four `(n, d)` combinations — so nothing
load-bearing moves, but 273 should never be quoted without its algebra.

### 2.2 LA-10's "zero rows name it" — CORRECTED, and the correction helps

Run per field (Lens P3), over all 84 records.

```text
base-duality vocabulary in a DEMAND field   4 rows:  LT-GR6, LT-SM8, RA-E1, RA-E3
nondegeneracy vocabulary in a DEMAND field  3 rows:  LT-SM8, RA-D2, RA-E1
base + globality + positivity in a DEMAND   1 row:   LT-SM8
Layer-0 completeness qualifier, ANY field   0 rows
BOTH legs of OT-1's factorisation demanded  0 rows
triggers whose HEAD deliverable is a
  pairing / metric / duality / density      0 of 82
```

**LA-10's claim is false as written.** Four rows name the datum. Here is what
each of them names it as, with the exact fragment:

| row | fragment | what the head demand actually is |
|---|---|---|
| `RA-E1` | *"construct its **full moving metric**, section, Shiab and Q_B principal map"* | the **operative second action's** owner. The metric is a property of the object being demanded |
| `RA-E3` | *"its **moving metric**, section, Shiab and Q_B map"* | same. Trigger head: *"a selected varpi one-form cell"* |
| `LT-GR6` | *"whose direct **metric** and **soldered** connection-current blocks reproduce the Hilbert stress"* | a **Noether complex**. The metric is a property of its blocks |
| `LT-SM8` | *"a Lorentzian closed domain, **positive pairing** and nontrivial physical cohomology"* | a **numerator-shell theorem** with a *"closed even physical quotient and interacting **state-space metric**"* |

That last one is the nearest miss and it is reported rather than suppressed:
`LT-SM8` is the only row in the ledger carrying base-duality **and** globality
**and** positivity in a demand field. Its object is the **state-space** pairing
on the BV physical quotient — downstream of quantisation. The proposed row's
object is the **field-space** pairing on `Λ¹ ⊗ ad P` — upstream of the classical
field equation. They are different objects at different stages, and `LT-SM8`'s
own trigger says so in its own words.

**Two facts make the correction strengthen the case rather than dissolve it.**

1. **In all four the datum is a modifier, never a deliverable.** Zero of the 82
   triggers has a pairing, metric, duality or density object as its head. The
   discrimination control: three triggers *do* have a "form" head noun there
   (`RA-D3`, `RA-E3`, `RA-G3`) and none of the three is a duality object — so
   the head window is not a window that never fires.
2. **Zero rows demand both legs.** OT-1's factorisation is (a) a fibre form on
   `ad P` and (b) a base duality. Base-duality tokens fire in 4 rows;
   fibre/adjoint tokens fire in exactly 1 (`LT-SM3b`); **the conjunction fires
   in none.** The object as a *composite* is genuinely unnamed.

### 2.3 What the ledger DOES supply, and why it is not enough

Read exactly, and this is what keeps the trigger out of LA-9's mode ⊤:

```text
LT-GR5   LOCAL_K_LOC_NONDEGENERATE_INDEFINITE            <- local, and INDEFINITE
LT-GR5   PHYSICAL_DIFFEO_DENSITY_HODGE_OBSERVATION_LOCAL_EXACT   <- LOCAL
LT-GR5   TRANSVERSE_PHYSICAL_DSHIAB_DHODGE_OBSERVATION_OPEN      <- the same
         object at physical/transverse grade: OPEN
LT-GR2c  DOMAIN_MEASURE_OWNER_OPEN                       <- the density: OPEN
LT-GR2c  NORMALIZED_FUNCTIONAL_OPEN  (x5)
LT-GR2c  SOURCE_EULER_TWO_TO_ONE_FAMILY_ONE_AMPLITUDE    <- local Euler: EXACT
LT-GR2c  GLOBAL_CONNECTION_EULER_BIANCHI_OBSERVATION_QUOTIENT_OPEN  <- global: OPEN
```

Every duality/density token in v0.258 is either **`LOCAL`-scoped** or marked
**`OPEN`**. And `LT-GR2c` carries *local Euler `EXACT`* and *global Euler
`OPEN`* **in the same field** — one row exhibiting exactly the separation the
proposed row formalises. The Layer-0 register's third column is the fence that
makes this decisive: `stationary` is *"**not** W177 or a retained local
truncation."* The ledger has the truncation. The register excludes it.

---

## 3. THE ROW

Schema-valid against the ten fields every v0.258 record carries. **This is a
PROPOSAL for the canonical owner. The sequential ledger is untouched.**

```json
{
  "id": "LT-GR6b",
  "axis": "LAGRANGIAN",
  "source_row": "CB-B:GR-6#variational-duality",
  "summary": "the complete bulk-plus-defect first variation closes to a ghost-free Euler system on the observed four-dimensional base",
  "verdict": "NEEDS",
  "reason_kind": "MISSING_CONSTRUCTION",
  "distance": "The Layer-0 register types stationary as a zero of the complete bulk, defect, section, ghost-free Euler system including boundary terms, and explicitly not a retained local truncation. The ledger supplies the truncation, not the system: LT-GR2c carries SOURCE_EULER_TWO_TO_ONE_FAMILY_ONE_AMPLITUDE and GLOBAL_CONNECTION_EULER_BIANCHI_OBSERVATION_QUOTIENT_OPEN in one field, and LT-GR5 carries LOCAL_K_LOC_NONDEGENERATE_INDEFINITE and PHYSICAL_DIFFEO_DENSITY_HODGE_OBSERVATION_LOCAL_EXACT with the transverse and physical counterparts OPEN. Missing is the datum that turns the first variation into an equation: a Gamma(Ad P)-equivariant pairing on Lambda^1 tensor ad P over X^4, factoring as (a) an Ad-invariant nondegenerate fibre form on ad P and (b) a base duality absorbing the one-form index, a density together with a nondegenerate Lambda^1 pairing. No row demands both legs. At subscript W the composite provably does not exist: for Lie(W) = g semidirect (Lambda^1 tensor ad) with g semisimple the Ad(W)-invariant symmetric forms have dimension exactly d+1, every one has identically zero V-V block and rank at most 2 dim g, so every available pairing has radical ker(c) tensor ad of dimension (d-1) dim g, exactly 273 on X^4 and 1183 on Y^14 for dim g = 91; the threshold is exactly d = 2 and iso(2,1) at d = 1 is the control that fires. Leg (a) is available only at subscript K and is priced REDUCTION_EXTERNAL by AUDIT. Build (b) as a source-owned global object on X^4, name the subscript for (a), compose the two, and only then restate stationary against the resulting Euler operator, before any stabilizer, Hessian, mass-matrix, index or BV/BFV work.",
  "revival_trigger": "a source-owned global base duality on the observed X^4, a density together with a nondegenerate Lambda^1 pairing, composed with an Ad-invariant fibre form at a NAMED group subscript into a Gamma(Ad P)-equivariant pairing on Lambda^1 tensor ad P that is positive on the physical quotient",
  "evidence": "la11-b9stat-is-a-base-duality-row-and-four-rows-name-it-as-a-subclause-2026-08-15.md; unified-source-datum-packet-v0-2026-07-30.md:133",
  "mapping_grade": "LAYER0_STATIONARY_TYPING_OFF_LEDGER__LOCAL_SELECTED_SOURCE_EULER_EXACT__GLOBAL_EULER_OBSERVATION_QUOTIENT_OPEN__LOCAL_K_LOC_NONDEGENERATE_INDEFINITE__LOCAL_DIFFEO_DENSITY_HODGE_EXACT__TRANSVERSE_PHYSICAL_COUNTERPARTS_OPEN__GLOBAL_SOURCE_OWNED_BASE_DUALITY_UNBUILT__FIBRE_FORM_AVAILABLE_ONLY_AT_SUBSCRIPT_K_PRICED_REDUCTION_EXTERNAL__W_SUBSCRIPT_NONDEGENERATE_PAIRING_IMPOSSIBLE_EXACT_FOR_D_GE_2__INVARIANT_FORM_SPACE_DIM_D_PLUS_1__ZERO_VV_BLOCK__MAX_RANK_2DIMG__THRESHOLD_EXACTLY_D_EQUALS_2__UNPAIRED_REMAINDER_273_X4_1183_Y14_AT_DIMG91__135_585_AT_DIMG45__FOURD_SIDE_INHERITED_FROM_PHI1__NAMED_AS_SUBCLAUSE_BY_FOUR_ROWS_AS_HEAD_BY_ZERO"
}
```

### 3.1 Why each field is what it is

**`id` / `source_row`.** `LT-GR6b` is unused and fits the existing `b`-suffix
convention (`LT-GR1b`, `LT-SM3b`). `CB-B:GR-6#variational-duality` uses the
`#anchor` idiom the ledger already applies five times to `CB-B:GR-2`, and
`CB-B:GR-6` — *"the Einstein field equation itself"* — is the only CB-B slot
whose deliverable is an Euler equation. **`LT-GR6` is NOT superseded and NOT
split**: no text is removed from it, so no `split_from` edge is claimed and the
`migration_rule` is not engaged beyond the append.

**`verdict: NEEDS`** — mandated, and correct: nothing is derived here.

**`reason_kind: MISSING_CONSTRUCTION`.** The three tempting alternatives are all
refused. `PROVEN_UNSUPPLYABLE` would overclaim — OT-1's no-go is at subscript
`𝒲` only, and §2.1's `iso(2,1)` control is precisely the evidence that the
obstruction is not general. `PROVEN_UNABLE_BY_CURRENT_ACTION` would assert that
the selected first action provably cannot supply it; nobody has shown that.
`EXTERNAL_DATUM` would concede that GU cannot supply it natively, which is
shown for leg (a) at subscript `K` and **not** shown for leg (b). What is true
is that a construction is missing. That is the kind, and the taxonomy's
`NEW_KIND_REQUIRED__FORCED_FIT_FORBIDDEN` rule is satisfied without inventing
one.

**`distance` names a construction, in build order**: build (b) as a global
source-owned object → name the subscript for (a) → compose → restate
`stationary` against the resulting operator. It carries the exact obstruction
(`d+1`, zero `V–V`, `2 dim 𝔤`, threshold `d = 2`, radical `(d−1)·dim 𝔤`), the
exact numbers with their algebra attached, and the exact price on leg (a). No
mood words.

### 3.2 The `revival_trigger` against LA-9 §2.1's four modes

| mode | test | result |
|---|---|---|
| **∅** unsatisfiable | is the named set non-empty in the declared scope? | **PASS, constructively.** §2 exhibits an explicit member: a positive-definite fibre form ⊗ a Euclidean base metric on `Λ¹`, rank **12 of 12**, positive definite by leading principal minors. The scope fence *"at a **NAMED** group subscript"* is **in the trigger string**, and it is load-bearing: fix the subscript to `𝒲` and the set **is** empty by §2.1 (`[C]`, at `d = 4` the invariant space tops out at rank 6 of 15) |
| **⊤** already satisfied | has the program supplied a member? | **PASS.** Every duality/density token in v0.258 is `LOCAL`-scoped or `OPEN`. The control that makes this non-vacuous: the **local, indefinite** `K_loc` **is** supplied, so a trigger demanding mere local nondegeneracy would fail this mode — which is exactly why the trigger demands **global** and **positive on the physical quotient**, the two properties the supplied object provably lacks |
| **NR** non-responsive | does supplying a member change the verdict? | **PASS by construction.** The trigger *is* the row's deliverable, and it names the same three objects the `distance` builds (density, fibre form, pairing) |
| **DEP** dependent | is it a restatement of another row's trigger? | **PASS.** No existing trigger carries {base-duality} ∧ {global} ∧ {positive}. Nearest by content-word overlap is `LT-GR2c`, which demands *"an action-owned **normalized global functional** with covariance, positivity, domain and observation descent"* — a normalisation of `S`, not a duality on `δS`. `LT-GR2c` itself exhibits the separation: its Euler side is `EXACT` while `NORMALIZED_FUNCTIONAL_OPEN` appears **five times** in the same field |

**Not laundered.** The row takes nothing off any existing row, supplies no
grant, converts no grant into a derivation, and is typed with the weakest honest
kind available. It makes 14 rows strictly more conditional than they were.

---

## 4. WHICH ROWS GAIN IT AS A DECLARED CONDITION — 14, not 28

**The rule, mechanical, applied to `distance` + `revival_trigger` only.** A row
gains `LT-GR6b` iff its own demand names **(S)** a zero of an action —
`stationary`, `action-stationary`, `vacuum`, `background` — or **(E)** an
Euler / Noether / field-equation / Hilbert-stress object. Every membership below
carries the exact substring from the row's own v0.258 text.

**Rows demanding a Hessian, mass matrix, eigenvalue or spectrum (class H, 10
rows) are deliberately EXCLUDED.** A Hessian is the second variation *at* a
background; those rows are strictly downstream of class S and every one of them
already sits under `b1` or `b7` in LA-4's DAG. Declaring the condition on them
too would double-count one dependency, which is the coverage-theatre failure
mode the 2026-08-04 council priced.

```text
class S  11 rows   RA-A1 RA-A2 RA-A4 RA-A8 RA-E1 RA-E6 RA-G3 RA-G4
                   LT-SM3 AC-F1 AC-G1a
class E   3 rows   LT-GR1 LT-GR2c LT-GR6
S and E are disjoint;  union = 14 rows = 8 REPRESENTATION / 4 LAGRANGIAN / 2 ANOMALY
verdicts touched: 11 NEEDS/MISSING_CONSTRUCTION, 2 DIFFERS/STRUCTURAL_DIFFERENCE,
                  1 DIFFERS/PREDICTION.  ZERO `SAME` rows.
```

**8 of 29 on REPRESENTATION, not 28 of 29.** The 8 are a *strict subset* of the
28 LA-4's DAG puts downstream. The gap of 20 is exactly the rows that are
*reachable through inferred edges* but *say nothing*. Amending 28 would import
LA-4's uncited premise into the ledger under cover of a hygiene fix, and this
route refuses it.

**That zero `SAME` rows are touched is the guarantee that nothing is
laundered**: no already-derived result becomes conditional, and no recorded
disagreement is softened. Adding a declared condition to a `DIFFERS` row
(`LT-SM3`, `LT-GR6`, `RA-A4`) *strengthens* its fence — it records that the
disagreement is stated relative to an object nobody has built.

### 4.1 What each `distance` should become

**Append only. Delete nothing.** LA-6 established that scalar fields in this
schema are overwritten rather than appended on migration; an append-only
amendment is the form that cannot clobber a prior statement.

**Class S — the eleven rows whose demand is a zero of an action.** Append,
verbatim, to each `distance`:

> ` DECLARED CONDITION LT-GR6b: the stationary object demanded here is typed by
> the Layer-0 register as a zero of the complete bulk, defect, section,
> ghost-free Euler system including boundary terms, explicitly not a retained
> local truncation; that system is not an equation until LT-GR6b supplies the
> base duality and the fibre form at a named subscript, so this row's demand is
> not well-posed before LT-GR6b, not merely unmet.`

| row | axis | its own stationary object, before the clause |
|---|---|---|
| `RA-A1` | REPR | *"the smallest **action-stationary** nonhomogeneous reduced-curvature plus full-II orbit"* |
| `RA-A2` | REPR | *"a source-action **vacuum** selecting the A1-lock subgroup"* |
| `RA-A4` | REPR | *"a **vacuum** mass matrix for the extra U(1)"* |
| `RA-A8` | REPR | *"an **action-stationary vacuum** that selects J and a nonzero radial varpi coefficient"* |
| `RA-E1` | REPR | *"a source-action **vacuum** selecting the Higgs carrier"* |
| `RA-E6` | REPR | *"the **stationary** Higgs orbit and photon kernel"* |
| `RA-G3` | REPR | *"a **stationary** odd-form VEV in the located 126 channel"* |
| `RA-G4` | REPR | *"the nonhomogeneous **vacuum** stabilizer and gauge-boson mass matrix"* |
| `LT-SM3` | LAGR | *"a native residual-zero action **background**"* |
| `AC-F1` | ANOM | *"a native **action-stationary background**"* |
| `AC-G1a` | ANOM | *"the native **background** and operative boundary gauge/BV complex"* |

**Class E — the three rows whose demand is an Euler/Noether/Hilbert-stress
object.** Append, verbatim:

> ` DECLARED CONDITION LT-GR6b: the Euler / Noether / Hilbert-stress object
> demanded here presupposes a nondegenerate pairing on Lambda^1 tensor ad P and
> the density that integrates it; recomputing against the local selected-action
> truncation cannot discharge it, because the register excludes that truncation
> from the word stationary.`

| row | its own object | note |
|---|---|---|
| `LT-GR1` | *"recomputing **Euler**/BV and observed **Hilbert stress**"* | the Hilbert stress is `(2/√g) δS/δg`; it does not exist without the density |
| `LT-GR2c` | *"restrict/recompute **Euler**/BV"* | already carries local-`EXACT` / global-`OPEN` in one field; the clause names why |
| `LT-GR6` | *"full **Euler**/BV, **Hilbert stress**"*; trigger *"tangent/**Noether** complex whose direct metric and soldered blocks…"* | **parent row.** Its trigger keeps its head (the Noether complex); the metric/soldering sub-clause is now owned by the sibling `LT-GR6b` and cited rather than assumed |

**Two rows that gain nothing, stated so they are not silently omitted.**
`RA-E3` and `LT-SM8` both name a metric/pairing in a demand field but neither
demands a zero of an action or an Euler object: `RA-E3`'s head is a *varpi
one-form cell*, `LT-SM8`'s is a *numerator-shell theorem* whose metric is on the
state space. They are recorded in §2.2 as naming rows and are **not** amended.
`LT-GR5`, whose grade supplies the local `K_loc` and the local density/Hodge
result, is likewise untouched — it is evidence for the new row, not a dependant
of it.

---

## 5. DENOMINATOR IMPACT — named before proposing

```text
canonical_target_count      82 -> 83
axis LAGRANGIAN             21 -> 22
verdict NEEDS               26 -> 27
reason_kind MISSING_CONSTRUCTION  20 -> 21
recorded SM-disagreement    19/82 -> 19/83     <- the program looks WORSE
free_object_delta            0 -> 1            <- declared, not hidden
rows advanced                0
`SAME` rows touched          0
```

The `freeze_rule` requires that appending a target increments the minor ledger
version; that is the canonical owner's action, not mine. **The direction of the
denominator move is the honesty check LA-9's metrology lens imposes, and it
points the right way**: every published ratio gets slightly worse, and the one
correction in the 2026-08-15 batch that flattered the program did the opposite.

---

## 6. POSTFLIGHT — five lenses, run inline

### Lens Q1 — the strongest overclaim available here, refused

The most available inflation is: **"the ledger's largest dependency has now been
written down, so the 28-of-29 fan-out is on the books."** **Refused, on my own
numbers.** What went on the books is a *declared condition on 14 rows*. The
other 14 of LA-4's 28 remain downstream of this object only through **inferred
DAG edges that no row states** — the same uncited edges LA-10 broke. Writing the
row does not convert an inference into a declaration; it converts an inference
into a declaration *for the rows that already say the words*, and it leaves the
rest exactly as under-declared as they were.

Second available inflation: **"OT-1's no-go means GU cannot own its action."**
Refused, and OT-1 refuses it too. Only subscript `𝒲` is excluded, only for
symmetric bilinear forms of integral type, and §2.1's `iso(2,1)` control exists
precisely to stop this reading.

Third: **"`b9_STAT` is now typed, so `stationary` is now well-defined."** No. The
row records that it is *not* well-defined and names what would make it so.

### Lens Q2 — the strongest contrary reading, which I cannot refute

**A reader can say I have written a row for a piece of standard mathematical
furniture and dressed it as a discovery.** The steelman is strong: *every*
variational problem needs a pairing to convert `δS` into a field equation. That
is first-year field theory. On a four-manifold with any metric and any invariant
fibre form the object exists immediately — §2's own witness constructs one in
four lines. So the "missing datum" is missing only in the sense that nobody
bothered to write down something universally available, and the row is
bookkeeping about a triviality with an impressive obstruction bolted on.

**I cannot refute this from v0.258, and I will not pretend otherwise.** What I
can say is three things, and a reader should weigh them and decide.

1. The furniture is available **only by import**, and this program's whole
   posture is that the 4D metric is *produced*, not put in: `LT-GR6` reads the
   Einstein tensor on the **induced** metric `g_s = s*(g)`, which is built from
   the very structure whose stationarity it would be used to define. Whether
   that circle closes is not furniture; it is the question.
2. `AUDIT-noncompact-compact-reduction-EXTERNAL` already prices the compact
   reduction that makes leg (a) positive as `REDUCTION_EXTERNAL`. A row that
   makes the *base* leg equally visible is the same accounting applied
   consistently, not a new theory.
3. The obstruction is not bolted on: it is the reason the datum cannot be typed
   at `𝒲`, and it is the reason "ownership" has never been dischargeable. That
   part is a theorem, verified here from structure constants.

**A second contrary reading, weaker but live.** My cut between "the variational
duality" (`LT-GR6b`) and "a background" (LA-4's `b1`) may be over-resolution in
the same way LA-10's own Lens Q2 conceded its four-way split might be: a single
successful construction may deliver the duality, the density, the operator and
the background together. If so, the honest row count is one, not two, and my row
should be folded into `b1`'s demand rather than written. That bet is about a
construction nobody has; mine follows the register's own separation of
*stationary* from *a retained local truncation*, which is a distinction the
ledger writes down.

### Lens Q3 — weakest seam of this route

**The seam is the `source_row`, and it is a judgement, not a computation.**
`CB-B:GR-6#variational-duality` claims parentage from the Einstein-field-equation
slot. CB-B §PRE-3 states plainly *"does success introduce a new un-owned object?
**No. `free_object_delta = 0`**"* — and this row sets it to 1. I believe that is
right, because CB-B's `U1..U13` are *coefficients and forks*, whereas this is a
structure; but a reader who holds CB-B's `free_object_delta = 0` as binding will
say the row has **no CB parent at all** and therefore no place in a ledger whose
`inclusion_rule` is scoped to CB enumerations. That reader should route the
object to `layer0_objects_compared` — the ledger's own 180-entry homonym
register, which already carries Layer-0 typings — instead of to `rows`. **That
is a defensible disposition and the canonical owner should decide it, not me.**
It is also the one decision in this artifact that no amount of arithmetic
settles.

**Second seam: the 4D-side claim is inherited, not derived here.** PHI-1's
result is that the observation pullback truncates and the 14D kernel contributes
zero bits *to the 4D anomaly verdict through `φ`*. The step from that to "the
missing duality is a 4D-side object" is an inference the brief made and I
carried. It is marked `FOURD_SIDE_INHERITED_FROM_PHI1` in the grade rather than
asserted. **Nothing load-bearing rests on it**: §2.1 shows the `d ≥ 2` verdict
is identical at `d = 4` and `d = 14`. Only the remainder number moves.

**Third seam: my class-S/class-E cut is a lexical rule.** `background` catches
`LT-SM3` and `AC-G1a`; a stricter reader who required `action`-adjacency would
drop them and the set is 12, not 14. A looser reader who admitted class H gets
21. I report 14 and the two alternatives; the *direction* of the finding — far
below 28 — is invariant across all three.

### Lens Q4 — what a hostile reader should attack next in this artifact

In order of expected yield:

1. **The `source_row`** (Lens Q3). If `CB-B:GR-6#variational-duality` does not
   hold, the record is still well-formed but it is not a `rows` entry, and the
   right home is the Layer-0 register. This is the cheapest way to move the
   result and it should be attacked first.
2. **Mode-⊤ on the trigger.** I ruled "not already satisfied" on the strength of
   `LOCAL`-scoping and `_OPEN` suffixes in grade tokens. A reader who reads
   `PHYSICAL_DIFFEO_DENSITY_HODGE_OBSERVATION_LOCAL_EXACT` as supplying the base
   duality globally-enough flips the mode and the row becomes illusory.
3. **The class-S lexicon** (Lens Q3, seam three). Attack the assignments in
   groups, not singly.
4. **Do not attack the invariant-form computation.** It has four
   hypothesis-isolating controls, a Jacobi mutation control, an `𝔰𝔩(3)`
   non-accident control, an explicit rank-attaining witness, and the `iso(2,1)`
   control that specifically blocks the obvious overreach.

### Lens Q5 — does writing this row make the ledger more honest, or merely relabel an absence?

**More honest, and the test is falsifiable rather than rhetorical: does the row
change what any reader can conclude from the file?**

Writing LA-10's `b9_STAT` *as glossed* — "a zero of the complete ghost-free
Euler system" — **would have been a relabel.** Ten rows already demand that
object. It is LA-4's `b1`. A new row for it would add a name and no information,
and would double-count a dependency the ledger already tracks eleven times over.
**I did not write that row.**

What is written instead changes three readable facts.

1. **Before:** eleven rows demand a stationary object and the word `stationary`
   is typed only in a packet the ledger does not cite — verified, zero rows'
   `evidence` fields reference it. A reader of v0.258 alone cannot tell that
   *stationary* excludes the local truncation the ledger has already banked as
   `EXACT`. **After:** the typing and its exclusion are in a row, in the file,
   with the two grade tokens that exhibit the conflict named.
2. **Before:** the ledger's own accounting prices leg (a) — `REDUCTION_EXTERNAL`
   on the compact reduction — and prices leg (b) at nothing, because leg (b) has
   no row. That is an asymmetry that flatters. **After:** both legs are priced.
3. **Before:** `free_object_delta = 0` across the 2026-08-15 batch. **After:**
   it is 1, declared, with the reason attached.

**And here is the honest limit.** The row does **not** make the ledger more
honest about the other 14 rows of LA-4's 28. Those remain downstream of this
object only through edges no row states, and this route deliberately declined to
amend them. The ledger's largest *stated* dependency is now 14 rows. Its largest
*inferred* dependency is still 28, still resting on one uncited edge, and still
the most concentrated evidential load in the file. **Writing this row did not
fix that, and pretending otherwise would undo the honesty it buys.**

---

## 7. CLAIM CEILING

**May be claimed, exactly and only:**

- The row record in §3, as a **proposal**, with the field justifications in §3.1
  and the four-mode analysis in §3.2.
- For `𝔤` semisimple and `V = Λ¹ ⊗ ad` an abelian ideal with `d = dim Λ¹`: the
  invariant symmetric form space has dimension `d + 1`, every element has
  identically zero `V–V` block, the maximum rank is exactly `2 dim 𝔤` and is
  attained by the `c`-pairing, and a nondegenerate invariant form exists iff
  `d ≤ 1`. Controls as listed. This **reproduces** OT-1; novelty is claimed only
  for the module-structure control and the attained-maximum witness.
- The radical `ker(c) ⊗ ad` of dimension `(d−1)·dim 𝔤`; `273` and `1183` at
  `dim 𝔤 = 91`; `135` and `585` at `dim 𝔤 = 45`; `dim 𝔰𝔬(6,4) = 45 ≠ 91`.
- The per-field token census of §2.2 and the four naming rows with their exact
  fragments; that zero rows carry the Layer-0 completeness qualifier; that zero
  rows demand both legs; that zero triggers have a duality object as head.
- The 14-row declared-condition set of §4, each membership certified by an exact
  substring, and its 8/29 REPRESENTATION share.
- The denominator arithmetic of §5.

**May NOT be claimed, and is not:**

- That the object exists, is constructible, or is well-posed in GU. The row
  records a demand.
- That GU can or cannot own its action. No ownership theorem is proved.
- That `LT-GR6b` is canonical. It is a proposal; the `source_row` is the
  disputable part and Lens Q3 says so.
- That 28 rows depend on it. **14 declare it**; 28 are *reachable* through an
  edge no row states.
- Any verdict, reason-kind, priority, canon, `CURRENT-STATE` or public-posture
  movement. Zero rows advance; zero `SAME` rows are touched.
- Novelty for the ownership predicate (OT-1), the `b9` split (LA-10), the trigger
  predicate (LA-9), the incidence (LA-4), or the reduction map (PHI-1). What is
  new here is the **typing** of `b9_STAT` as the variational duality rather than
  as a background, the row itself, the per-field absence census that corrects
  LA-10, and the declared-condition set.

**Not laundered:** no grant is converted into a derivation; no row gains a
result; no `SAME` row is touched; the sequential ledger is untouched.

---

## 8. REPRODUCE

```bash
cd /path/to/gu-formalization
_local/cas-venv/bin/python tests/channel-swings/joe_directed_b9stat_row_construction.py
_local/cas-venv/bin/python tests/channel-swings/joe_directed_b9stat_row_construction.py --selftest
```

Expected: `CERTIFICATE: 192/192 checks pass; no load-bearing float (swept).`,
exit 0, split `[C] 20  [E] 159  [R] 13`; then
`FAILURE-PATH SELFTEST: PASS (7/7 mutations drove exit 1)`. The probe resolves
the ledger and the packet relative to its own location.
