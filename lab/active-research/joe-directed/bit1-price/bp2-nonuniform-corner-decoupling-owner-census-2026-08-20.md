---
artifact_type: exploration
status: exploration
doc_type: conditional_build_owner_census
created: 2026-08-20
work_item: BP-2
channel: bit1-price
title: "BP2: minimum non-uniform corner incidence and bounded source-native owner census"
grade: "EXACT conditional linear-algebra, cell-incidence, conjugate-half, and reduced-D5 control; source meanings and owner statuses remain pinned semantic custody"
disposition: CONDITIONAL_INCIDENCE_SURVIVES__NO_OWNED_DECOUPLING_MAP__H210_ONLY_BOUNDED_RIGHT_DIRECTION_CELL_TYPED_CANDIDATE__ROUTE_DEPENDENCY_FENCED
canonical_effect: none
classification: BRIDGE_OR_SEMANTIC_BOUNDARY
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/conditional-build-channel-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/bit1-price/b1p1-three-corner-support-prices-the-chiral-grant-2026-08-17.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he3-four-corner-partner-placement-and-family-rank-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb1-h210-k77-rs-intertwiner-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb2-h210-equation916-cross-half-composition-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/cb6-h210-three-horn-compatibility-2026-08-16.md
  - lab/active-research/joe-directed/rs-corner/rsc1-unique-channel-lives-on-the-gamma-trace-2026-08-17.md
scripts:
  - tests/channel-swings/joe_directed_bp2_nonuniform_corner_decoupling_owner_census.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: **`BRIDGE_OR_SEMANTIC_BOUNDARY`.**

# BP2 — non-uniform corner-decoupling owner census

```gu-typed-objects
result: conditional rank and kernel formula for the minimum non-uniform family incidence, with no owned physical decoupling map
carrier: M_3 tensor S_16 family-stage domain and L_selected tensor W codomain LAYER=toy CHIRALITY=S-CHIRALITY-UNTYPED
pairing: NONE
real_structure: complex modules with the conjugate half retained independently
grading: family-index grading and unbarred-versus-conjugate-half split
action_owner: repository-construction -- the incidence is conditional and no source action owns a decoupling map
target: the conditional family-stage incidence map MAP-TYPE=homomorphism
```

## Result first

Accept B1P1's declared support theorem

```text
CHIRAL => PRESENT => carrier A
```

and retain the standing massive-point tilt toward carrier B. The smallest
basis-free incidence compatible with a non-uniform family response is

```text
0 -> K_uncoupled -> M_3 --r--> L_selected -> 0,
dim K_uncoupled = 2,  dim L_selected = 1,
```

with a separately declared nonzero row `r`. For any declared H210 stage
`B:S_16 -> W`, the induced family-stage map has the canonical kernel sequence

```text
0 -> K_uncoupled tensor S_16
  -> ker(r tensor B)
  -> L_selected tensor ker(B)
  -> 0.
```

Consequently, if `rank(B)=k`,

```text
rank(r tensor B) = k,
dim ker(r tensor B) = 32 + (16-k) = 48-k.
```

At the current K77 H210 rank `k=16`, one unbarred half has rank `16` and
kernel dimension `32`; the independent conjugate half gives total rank `32`
and family-input kernel dimension `64`. No barred field is identified with its
unbarred partner.

This is a conditional incidence theorem, not a constructed decoupling map.
`K_uncoupled` is not licensed as “the two true families,” and `L_selected` is
not licensed as “the imposter,” unless the separate `H210-ALIGN` horn is
declared. The theorem does not turn the B tilt into `MASSIVE => B` and does not
produce an `A -> B` transition.

## Conditional-build boundary

The direction of reasoning is reversed from a source-action construction:
starting from the superposition/support hypothesis, determine what incidence
would have to be true for it to coexist with the known geometry. The following
are not constructed or selected here: an action, vacuum, background, external
datum, coefficient, family row, scale, carrier selector, physical quotient,
operator domain, spectrum, or named-family assignment. No ordinary comparator
mechanism is imported to fill those gaps.

The source-native objects remain distinct throughout:

```text
F        = the equation-(12.22) source imposter/correlated-trace summand;
M_3      = the abstract three-dimensional family multiplicity space;
144      = the distinct internal vector-spinor partner representation.
```

Equal dimensions, subrepresentation remainders, or an unowned quotient line do
not identify any two of these objects. The total fermionic system remains
four-corner and nonchiral; barred fields remain independent.

## Ten-lens preflight

1. **Source custody.** B1P1 supplies the three-corner support implication; HE4
   keeps `F`, `M_3`, and `144` distinct; CB6 fences `H210-ALIGN`.
2. **Type theory.** A row on `M_3` and an intertwiner on `S_16` have different
   types. Their tensor product exists conditionally; neither factor supplies
   the other.
3. **Exact homological algebra.** The kernel sequence above is canonical and
   does not require choosing a basis or splitting `M_3`.
4. **Representation direction.** H210 supplies a current-K77 rank-16 port in
   the required `16 -> 144bar` direction and its conjugate.
5. **Equation-9.16 incidence.** The forward cells are `(1,2)` and `(0,3)`;
   the reverse cells are `(3,0)` and `(2,1)`. The pp/mm cells `(0,2)` and
   `(1,3)` are wrong-channel controls.
6. **Covariance.** `GL(3)` family-coordinate changes and nonzero row rescaling
   preserve rank, kernel dimension, and quotient-line incidence.
7. **Conjugation custody.** The two halves mirror the same ranks but are kept
   as an external direct sum, not collapsed by a reality assumption.
8. **Adversarial representation control.** RSC1 gives
   `mult_D5(120,16 tensor 144)=0`, while the same detector gives
   `mult_D5(120,16 tensor 16)=1`. The `120` route therefore distinguishes
   representation types but points in the wrong direction for this bridge.
9. **Ownership.** The family row, source-provenance alignment, and phase/cell
   activation are independently unowned; compatibility cannot manufacture
   them.
10. **Archaeology.** CB1/CB2/CB6 already contain nearly all algebraic
    ingredients. The new contribution is the bounded owner census and the
    basis-free certificate tying them to B1P1's corner price.

## Exact certificate

Let

```text
0 -> K -> M_3 -> L -> 0
```

be the exact sequence defined by a nonzero `r:M_3 -> C`, with `L=im(r)`.
Tensoring over `C` with `S_16` is exact. The quotient of
`ker(r tensor B)` by `K tensor S_16` is the kernel of the induced map
`id_L tensor B`, hence is `L tensor ker(B)`. This yields

```text
0 -> K tensor S_16
  -> ker(r tensor B)
  -> L tensor ker(B)
  -> 0.
```

This is stronger than a dimension count: the repository probe constructs vectors
in `K tensor S_16`, constructs lifts of `L tensor ker(B)`, and checks both are
annihilated by the composite at `k=0,8,12,16`. It also checks family-basis
covariance and row-line rescaling.

## Equation-9.16 placement

Using CB2's released row/column order, the permitted zero-order H210 incidence
is exactly:

| half | forward cell | typed arrow |
|---|---:|---|
| A | `(1,2)` | `nu+ (3 x 16) -> zeta- (144bar)` |
| B | `(0,3)` | `nu- (3 x 16bar) -> zeta+ (144)` |

with displayed reverse partners `(2,1)` and `(3,0)`. The bare pp/mm cells
`(0,2)` and `(1,3)` cross the effective-package typing and remain negative
controls. Only zero-order incidence is in scope. No claim is made about a
complete first-order cell or whether a term is physically activated.

If a low-`varpi` horn separately declares the H210 zero-order term absent,
this particular incidence is blind there. That does not show that all possible
incidences vanish, and the repository probe does not own the phase declaration.

## Bounded owner census

“Unique” below means unique within this explicitly enumerated five-candidate
inventory, not unique across the repository or future mathematics.

| candidate | built relevant structure | right representation direction | exact needed cells | owns `r` | owns `F` alignment | owns activation | verdict |
|---|---:|---:|---:|---:|---:|---:|---|
| H210 | yes | yes | yes | no | no | no | only bounded viable port; fenced |
| H54 | no direct linear port in packet | no | no | no | no | no | declared alternative, not an incidence owner |
| ST1/RSC1 `120` | yes | no | no | no | no | no | exact wrong-direction control |
| F/Q/Z projectors, observation, correlated lift | yes | no family action | no new cell owner | no | no | no | sector separators, family blind |
| density-dual and cell reversal | yes | no family action | cell typing only | no | no | no | preserves bars/cells, supplies no row |

The census has exactly one candidate satisfying all three bounded structural
filters — built relevant structure, required representation direction, and correct
equation-9.16 cells — and no candidate satisfying the missing ownership
filters. H210 survives as a conditional carrier. The route is fenced at:

```text
r               nonzero M_3 discriminator,
H210-ALIGN      identification of L_selected with source F provenance,
phase/cell horn activation of the typed zero-order incidence.
```

A fourth, logically later dependency remains: any mechanism reconciling the
standing A-at-chiral/B-at-massive tilt. Nothing here derives `MASSIVE => B` or
moves a state from A to B.

## RSC1 `120` control

At reduced `D5`, exact weight arithmetic gives

```text
mult(120, 16 tensor 144) = 0,
mult(120, 16 tensor 16)  = 1,
Sym^2(144) contains 120 with multiplicity 1,
Lambda^2(144) contains 120 with multiplicity 3.
```

The first two equalities are replayed by the repository probe using RSC1's exact
machinery. The latter two remain repository-owned RSC1 controls. RSC1's unique
selective ambient channel lies in the gamma-trace × gamma-traceless cross
block, not in `Lambda^2(R)`. These statements and the H210 port live at
different symmetry levels and do not contradict one another. For BP2 they show
that the `120` comparator is representation-sensitive but cannot supply the
required family-to-partner bridge.

## Executable versus semantic claims

Executable in the repository probe:

- ranks and kernels at `k=0,8,12,16`;
- kernel-sequence inclusion and quotient lifts;
- `GL(3,Q)` covariance and row rescaling;
- independent conjugate-half direct sums;
- exact equation-9.16 forward, reverse, and wrong-channel cells;
- reduced-D5 `120` zero and positive control;
- the finite owner-census reduction;
- presence of cited custody statements in the current checkout.

Pinned or semantic only:

- Weinstein-source meanings of `F`, `M_3`, and `144`;
- the standing A/B tilt and low-energy interpretive language;
- ownership status of `r`, `H210-ALIGN`, and activation;
- completeness of the deliberately finite candidate inventory.

Not claimable from this wave:

- a selected non-uniform map;
- `MASSIVE => B` or an `A -> B` transition;
- identification of the kernel with two physical families;
- identification of the quotient with the source imposter;
- a mass, spectrum, threshold, action, vacuum, background, scale, external
  datum, selector, physical quotient, or operator domain;
- a global absence theorem for future source-native maps.

## Hostile review and mutants

The mutation harness rejects:

- a selected map or A-to-B transition;
- promotion of kernel or quotient to named sectors without ALIGN;
- collapse of `F` into `144`;
- deletion or identification of a conjugate half;
- activation without a separate phase declaration;
- global uniqueness language;
- any off-limit construction claim.

The exact controls additionally expose row-zeroing, basis-rank loss, bar-half
deletion, forward/pp-mm cell swaps, and corruption of the RSC1 representation
input.

## Kill / survive / fertility

**Survives:** a minimal conditional incidence pattern exists and is exactly
certifiable; H210 is the sole right-direction, right-cell candidate in the
bounded inventory.

**Killed:** the claim that current owned geometry already distinguishes the
two-dimensional source-family sector from the one-dimensional imposter sector,
or already constructs their non-uniform decoupling.

**Fertility:** `7/10` for closure and dependency localization; `2/10` for
further construction while `r`, `H210-ALIGN`, and activation remain unowned.
The next useful swing is an ownership event on one of those dependencies, not
another replay of representation compatibility.

## Reproduction

Run from the repository root:

```bash
_local/cas-venv/bin/python \
  tests/channel-swings/joe_directed_bp2_nonuniform_corner_decoupling_owner_census.py \
  --repo . --selftest
```

The explicit `--repo` argument makes the dependency checkout visible to the
probe; dependency loading and custody checks remain read-only.
