---
artifact_type: exploration
status: exploration
doc_type: structural_print_and_proposal
created: 2026-08-17
work_item: B1P-1
channel: bit1-price
target_claim: "NONE-NOT-A-KILL — INTERNAL targets only. (1) The LA3 conditionality cell of `lab/methods/gu-base-categories.md`, whose prior wording inverted the direction of the `SC-CHI-01` hedge it cites (a repository wording, corrected here under the granted methods write). (2) The post-ST-1 routing surfaces' implicit presentation of SG4 bit 2 as independently grantable (a typing gap, printed here). No registered GU source claim is attacked, weakened, or reinterpreted: `SC-CHI-01` is quoted with its polarity preserved verbatim (`ASSERTS`, twice-hedged, epoch-indexed), and its adherence verdict is untouched."
title: "B1P-1: the bit-1 price is printed. SG4's two bits have consistent support on THREE corners of the 2x2, not four — recomputed from the SHA-pinned frozen VERTEX map (SHA-256 3043d29e...80197, byte-identical, unedited) — so given consistency ABSENT implies MASSIVE and CHIRAL implies PRESENT implies carrier A, while BOTH converses fail. A bit-2 CHIRAL grant is therefore also a bit-1 PRESENT grant landing on carrier A, colliding with the standing B-tilt ('A at the chiral point, B at the massive point') unless the mass map is non-uniform across the fermionic extension. The collision is NOT new: canon banked it 2026-07-10 in Grisaru-Pendleton vocabulary (escape-corners :59-64) and lega1 priced its two firing conditions (:257); what was missing is the print, now added as a dated G6 addendum in lab/methods/gu-base-categories.md plus PROPOSED (unapplied) diffs for the canon SG4 surface and the three ledger rows whose triggers reference the chiral grant. Second delivery: the LA3 cell's direction flip is FIXED (the hedge's decoupling holds AT varpi -> 0, so the arrow is PRESENT there and ABSENT in the massive phase); CT-1's 99-check probe is GREEN after the edit with zero pin refreshes needed. Third delivery: the SC-CHI-01 coupling rule is stated as a methods note with a proposed location, wired by a PROPOSED source-register diff carrying SRC-1..4 as adverse-mechanism partials, and the gate question is answered by measurement rather than guess — CC-06's signature does NOT reach SRC-1..4 (family 2 hits 4/4, family 1 hits 0/4, conjunction False on all four), which is exactly the blindness CC-06's own registry entry declares."
grade: "EXACT where computational: the correlation is RECOMPUTED by parsing the frozen VERTEX map out of the SHA-pinned predeclaration (support size, non-product witness, both implications, both failing converses, the unique chiral carrier) — never restated from prose. Every quoted locus is pinned as an on-disk substring at its owner. The three absence claims (zero SC-CHI-01/bit-2 tokens in SRC-1..4; zero SRC paths in the register; the old LA3 wording gone) each carry a planted-positive control proving the detector fires. The CC-06 reach measurement is computed against the LIVE correction registry through the gate's own signature_match, not hardcoded. 91/91 checks, exit 0, no floats. NOT: a potential, a vacuum, a spectrum, a carrier decision, a bit adjudication, a claim-status movement, a canon/ledger/register/sidecar/routing-registry edit, or any statement about which corner GU or nature occupies."
disposition: BIT1_PRICE_PRINTED_AT_G6__CONDITIONAL_STRUCTURE_NOT_A_VERDICT__LA3_DIRECTION_FLIP_FIXED_CT1_GREEN_ZERO_PIN_REFRESH__COUPLING_RULE_STATED_AND_PROPOSED__CC06_MEASURED_BLIND_TO_SRC_ARC__THREE_PROPOSED_DIFFS_UNAPPLIED
canon_verdict_change: none
ledger_edit: none
register_edit: none
priority_change: none
steering_effect: unchanged
canonical_effect: pending_integration
depends_on:
  - tests/gu-forces/leg_a_forcing_enumeration.py
  - canon/escape-corners-campaign-RESULTS.md
  - tests/escape-corners/lega1_flipped_chiral_adjudication.md
  - canon/gu-forces-field-space-declaration-RESULTS.md
  - lab/methods/gu-base-categories.md
  - lab/methods/claim-status-consistency.md
  - lab/methods/source-native-comparator-routing.md
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - papers/drafts/Transcript into the impossible.md
  - lab/process/conditional-physics-ledger-v0.259.json
  - lab/process/correction-registry.yaml
  - lab/process/canonical-currency-checks.yaml
  - lab/process/upgrade-program-register.yaml
  - lab/active-research/joe-directed/lens-digs/ldc-vev-selector-adjudication-2026-08-17.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/sg4-axis/sg1-c6a-scope-narrowing-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src1-source-steelman-of-the-vev-2026-08-14.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src2-mexican-hat-is-automatic-2026-08-14.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src3-potential-unbounded-below-2026-08-14.md
  - lab/active-research/joe-directed/majorana-126-neutrino/src4-eddy-completion-cannot-rescue-the-potential-2026-08-15.md
  - process_gates/canonical_currency_audit.py
  - process_gates/kill_target_claim_audit.py
  - process_gates/typed_carrier_declaration_audit.py
scripts:
  - tests/channel-swings/joe_directed_b1p1_three_corner_support_prices_the_chiral_grant.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `INTERNAL_STRUCTURAL_ONLY` — the object of every finding here
> is repository-internal structure: a frozen enumeration's label map, one
> methods-file wording, one register's adherence pointers, and three unapplied
> diffs. The result binds NO comparator and is NOT evidence about the
> source-native mechanism in either direction. Comparator and physics vocabulary
> (Grisaru-Pendleton, super-Higgs, Mexican hat, gravitino) appears only inside
> quotations of already-routed artifacts, each of which carries its own notice.
> Per the method's registration discipline, a registry row for this path is the
> method owner's call after commit; this task holds no routing-registry write.

# B1P-1 — the bit-1 price, printed

## 0. Mandate and contract

Three register items, one touch:

| Item | Register title (verbatim head) | What this artifact does |
|---|---|---|
| `BIT1-PRICE-PRINT` | "Print the bit-1 price on routing surfaces: consistency support is 3 corners, so CHIRAL implies PRESENT implies carrier A" | Recomputes the correlation from the frozen map; PRINTS it as a dated G6 addendum in `lab/methods/gu-base-categories.md`; supplies PROPOSED (unapplied) diffs for the canon SG4 surface and three ledger rows |
| `CT1-LA3-WORDING` | "gu-base-categories LA3 cell: two halves point opposite ways as written" | Fixes the cell (one sentence, dated); runs CT-1's probe; reports the pin status |
| `SRC-SCCHI-COUPLING-RULE` | "Adverse-mechanism news must fire SC-CHI-01's hedge-watch: three dated misses" | States the rule as a methods note with a proposed location; supplies the PROPOSED source-register diff; answers the gate-sensing question by measurement |

**Writes actually performed.** Two granted methods edits inside
`lab/methods/gu-base-categories.md` (the G6 addendum, the LA3 cell), this
artifact, and its probe. **Nothing else.** No canon, ledger, source-register,
correction-registry, currency-sidecar or routing-registry file is edited; every
such change below is a diff to be applied by its owner. `rwall/` and
`rs-corner/` are untouched (live siblings). No git operation was run.

**The one thing this artifact must not do**, stated before any work: it must not
decide either bit. §6 attacks itself on exactly that, and §10 answers bluntly.

---

## 1. Preflight — six problem-matched lenses, kill-switch recorded first

**Kill-switch, recorded before the computation.** If parsing the frozen
`VERTEX` map showed a consistent support of FOUR corners — i.e. if
`(ABSENT, CHIRAL)` carried a live carrier — the entire premise of
`BIT1-PRICE-PRINT` would be false, the bits would be independent given
consistency, and this task would return "the register item's premise is
refuted, nothing to print." It showed three. Recorded here so the finding
cannot be read as constructed after the fact.

**Lens 1 — predeclaration-integrity lens.** The correlation is only worth
anything if it is read off a map frozen BEFORE anyone wanted this conclusion.
`tests/gu-forces/leg_a_forcing_enumeration.py` is SHA-pinned from outside by
SG-1's probe precisely so it cannot be amended. Verified: SHA-256
`3043d29ef2ca97b527113b16a399f7f5256ba8df85902ccff3b3d69a58380197`, identical
to `joe_directed_sg1_c6a_scope_narrowing.py:78`'s `LEG_A_SHA256`. The file was
read and never opened for writing. This probe re-pins the same hash so any
future edit reds two instruments, not one.

**Lens 2 — the adjudication boundary (the lens that constrains the writing).**
A *price* and a *verdict* are different objects. The price is a conditional:
IF bit 2 is granted CHIRAL AND the corner map's consistency labels are taken as
given, THEN bit 1 is PRESENT. It asserts nothing about whether bit 2 IS chiral.
The discipline this imposes on every printed sentence: no clause may assert a
bit value, a carrier, or a phase; every statement is `given consistency` or
`granting X`; and the standing escape must be printed next to the price, never
below the fold. Enforced by the probe (P06/P07) and re-attacked in §6.

**Lens 3 — direction-of-implication lens (the LA3 defect's own class).** The
defect being fixed is a flipped conditional. The repair is worthless if the
repair itself flips something. Two directions are load-bearing here and both
are checked in both directions: (a) the hedge — decoupling holds AT small
`varpi`, so the LA3 arrow is PRESENT at `varpi -> 0`, ABSENT above it; (b) the
correlation — `CHIRAL => PRESENT` holds and `PRESENT => CHIRAL` FAILS. The
probe asserts the failing converses explicitly as CONTRARY controls, so a
future edit that "strengthens" the price into an equivalence turns it red.

**Lens 4 — layer-regression lens (ST-1's unforgivable regression).** Crediting
the total theory (L1) with chirality, or the package (L3) with an unconditional
spectrum, is the standing regression. Everything printed here lives on the
GRANT poset and on a label map; nothing credits L1 with chirality, and the G6
addendum states its own conditionality in its first clause. Additionally SG-1's
Layer-0 homonym is respected: `CHIRAL` on the PHASE axis means
massless/unbroken, NOT handedness, and no sentence here reads the bit off
observation.

**Lens 5 — ownership lens.** Four surfaces want this print and exactly one is
writable by this task. Owned elsewhere: `canon/gu-forces-field-space-declaration-RESULTS.md`
(canon owner), the v0.259 ledger (mint), `lab/sources/source-claim-register.yaml`
(source owner), `lab/process/canonical-currency-checks.yaml` (currency owner),
the routing registry (method owner). Writable: `lab/methods/gu-base-categories.md`
under an explicit grant. Therefore: one print, three proposals. A proposal that
silently became an edit would be the worst failure available on this task, so
the probe asserts the three proposal targets are byte-unchanged in the ways it
can cheaply check.

**Lens 6 — false-novelty lens (the standing session defect).** Before claiming
anything is new, grep. Done: the correlation's substance is banked in
`canon/escape-corners-campaign-RESULTS.md` :59-64 (2026-07-10) and priced in
`tests/escape-corners/lega1_flipped_chiral_adjudication.md` :257; the explicit
support-size form is LD-C §7.2 (2026-08-17, same day, another agent). **Nothing
in this artifact is claimed as new physics or new mathematics.** The only new
things are: the *print* at a routing surface, the LA3 repair, the coupling
rule's statement, and the CC-06 reach measurement.

---

## 2. Canonical-currency check on inputs (recorded for the integrator; no sidecar written)

Run through `process_gates/canonical_currency_audit.py`'s own
`signature_match` against the live `lab/process/correction-registry.yaml`, with
the gate's `strict_predate` and `owner_exempt` rules. **This is a record, not a
sidecar write** — `lab/process/canonical-currency-checks.yaml` is untouched.

| Input | Doc date | CC-05 sig / predates | CC-06 sig / predates | Verdict |
|---|---|---|---|---|
| `canon/escape-corners-campaign-RESULTS.md` | 2026-07-10 | False / True | False / True | **NOT DIRTY** for either (signature miss). Hand-check: the material imported (:55-64, the GP corner chain) makes no generation-count claim at all and treats the source's VEV/chirality mechanism as STATED — consistent with CC-06's corrected reading, not the superseded one. See the caveat below |
| `tests/escape-corners/lega1_flipped_chiral_adjudication.md` | none parsed | False | False | **NOT DIRTY** (no frontmatter date; outside the dated scope). Hand-check: :257's two conjuncts are conditions, not count or mechanism claims |
| `tests/gu-forces/leg_a_forcing_enumeration.py` | none parsed | False | False | **OUT OF SCOPE** (the gate's corpus is markdown). SHA-pinned and unedited |
| `canon/gu-forces-field-space-declaration-RESULTS.md` | 2026-07-10 | False / True | False / True | **NOT DIRTY** for either |
| `lab/methods/gu-base-categories.md` | 2026-08-17 | **True** / False | False / False | **NOT DIRTY** — post-dates CC-05's `canonical_since` 2026-08-14; and its §5 item 5 carries the 2+1-subtractive guardrail explicitly. Re-measured after this task's edits: unchanged |
| `lab/active-research/joe-directed/lens-digs/ldc-...-2026-08-17.md` | 2026-08-17 | False | False | **NOT DIRTY** (post-dates both) |
| `lab/.../src1..src4` | 2026-08-14/15 | False | False / True | **NOT DIRTY** by signature — and this miss is itself a finding, see §5.3 |
| `lab/sources/source-claim-register.yaml` | none parsed | False | True / owner | **OWNER-EXEMPT** for CC-06 (listed `co_owners`) |

**The honest caveat on the two 2026-07-10 canon inputs.** "Not dirty" here means
"outside the signature", not "verified current". CC-05's registry entry declares
its own hole verbatim — *"'net chirality' / 'chiral count' -- rejected here
because it is the count-CARRIER topic rather than the partition topic; it is a
real hole"* — and `escape-corners` :50-53 discusses a net `+3` count without the
2+1/imposter partition, i.e. it sits inside that declared hole. **This does not
touch the import**: the passage imported by this task (:55-64) contains no count
claim, and nothing printed here depends on a generation count. Flagged for the
currency owner, not repaired (not this task's surface).

---

## 3. Piece 1 — BIT1-PRICE-PRINT

### 3.1 The correlation, recomputed (not restated)

Parsed by `ast.literal_eval` out of the SHA-pinned predeclaration
(`tests/gu-forces/leg_a_forcing_enumeration.py` :68-74). Axes:
`INVARIANCE = {ABSENT, PRESENT}`, `PHASE = {MASSIVE, CHIRAL}`; four keys, so the
2x2 is fully enumerated.

| corner (invariance, phase) | carrier | index | field space | live? |
|---|---|---|---|---|
| `(ABSENT, MASSIVE)` | **B** | -38 | CONSTRAINED | yes |
| `(PRESENT, CHIRAL)` | **A** | -42 | FULL | yes |
| `(PRESENT, MASSIVE)` | CTRL40 | -40 | BARE | yes |
| `(ABSENT, CHIRAL)` | **INCONSISTENT** | — | — | **no** |

The indices are carried as opaque labels; this artifact performs no arithmetic
on them and computes no index and no generation count.

**Computed results, each an assertion in the probe:**

1. **Consistent support = 3, not 4.** The excluded corner is exactly
   `(ABSENT, CHIRAL)`, carried in the predeclaration's own comment as
   *"ungauged massless CHARGED spin-3/2 -> GP bites, no SUSY"*.
2. **The support is NOT a product set.** Projections are full on both axes
   (`{ABSENT, PRESENT}` x `{CHIRAL, MASSIVE}` = 4 elements) while the support
   has 3; the single non-product witness is `(ABSENT, CHIRAL)`. A support that
   is not a product of its projections is precisely a *correlated* pair.
3. **`ABSENT => MASSIVE`** given consistency (the only ABSENT-consistent corner
   is `(ABSENT, MASSIVE)`).
4. **`CHIRAL => PRESENT`** given consistency (the only CHIRAL-consistent corner
   is `(PRESENT, CHIRAL)`).
5. **`CHIRAL => carrier A`, uniquely** — the carrier set over CHIRAL-consistent
   corners is exactly `{A}`.
6. **Both converses FAIL, and this is load-bearing.** `PRESENT` is consistent
   with CHIRAL *and* MASSIVE (carriers `{A, CTRL40}`); `MASSIVE` is consistent
   with ABSENT *and* PRESENT (carriers `{B, CTRL40}`). So the price runs one way
   only: it prices a CHIRAL grant, and prices nothing at all in the
   MASSIVE/ABSENT direction. Any reading of this artifact as an equivalence, or
   as forcing carrier A, is refuted by its own probe (CONTRARY controls V09/V10).

### 3.2 The receipts, verbatim

**Receipt 1 — canon, banked 2026-07-10** (`canon/escape-corners-campaign-RESULTS.md` :55-64):

> **NO invariant mass channel** (16 x 16 = 10 + 120 + 126, singlet-free -- computed), so its mass is
> strictly VEV-borne on the ONE stated modulus ... **and the DECREASE of that same modulus is
> GU's stated generation mechanism.** "Too massive" and "decreased VEV" are opposing demands on one
> dial, reconcilable only by an unstated hierarchy. At the chiral point GU's phenomenology commits
> to, the spin-3/2's massless surface is codim-1 (or worse under uniform descent), GP's hypotheses
> populate, and its conclusion (massless spin-2 partner + SUSY of all couplings) collides with "We
> will never find space time Susie" -- UNLESS the demanded SUSY is the upstairs one. Which is corner
> (b).

**Receipt 2 — the price, `tests/escape-corners/lega1_flipped_chiral_adjudication.md` :257:**

> *corner (a) fires only if GU's physical vacuum is taken at the chiral point AND the mass map is
> uniform across the fermionic extension* — two conjuncts the transcript neither states nor
> excludes. That is a narrowed corner, not a closed one.

**The join.** Receipt 1 is the collision in Grisaru-Pendleton vocabulary; §3.1
is the same collision in the corner map's vocabulary; receipt 2 is its two
firing conditions. The second conjunct — a **non-uniform mass map** across the
fermionic extension — is the standing escape, and it is not a hypothetical: the
source's own p.52 wording is *"pulling the various **sub-fields** of `varpi`"*,
plural and componentwise, and SN-1's separately-scaled components keep it live.
**The price is therefore conditional structure, not a verdict.**

### 3.3 The print — APPLIED (the one granted write)

Added to `lab/methods/gu-base-categories.md` immediately after the Grant-poset
object table (§2.1), at the G6 node. Verbatim head and the load-bearing clauses
(the full text is in the file; the probe pins seven of its clauses):

> **G6 addendum — the bit-1 price of a bit-2 CHIRAL grant (added 2026-08-17 by
> B1P-1, register item `BIT1-PRICE-PRINT`; annotation only — no object is
> coined, no row moves, no corner is selected).** ... the **consistent support
> is THREE, not four** ... A three-element support inside a 2x2 is not a product
> set, so the two bits are **correlated given consistency**: `ABSENT =>
> MASSIVE`, and `CHIRAL => PRESENT => carrier A`, the only consistent chiral
> corner. Both converses FAIL ... so this is a one-way price, not an
> equivalence ... **The price:** granting bit 2 = CHIRAL at G6 also grants bit 1
> = PRESENT and lands on carrier A — which collides with the standing B-tilt ...
> **This is banked, not new, and it is not a verdict** ... The **escape is named
> and open**: the second conjunct — a NON-uniform mass map across the fermionic
> extension — is exactly what the source's own *"sub-fields"* plural (p.52) and
> SN-1's separately-scaled components leave available. ... SG4 remains the sole
> decider, the residual remains 2-bit, the B-tilt is unmoved. What this addendum
> adds is only that the two bits are **not independently grantable**, and that a
> router handing bit 2 to a lane as "the open phase bit" is handing bit 1 with
> it unless the mass map is non-uniform.

**Why this file does not violate its own §6.** `gu-base-categories.md` §6 says
it supplies "no physics claim, no verdict, no claim movement". The addendum
supplies none: its content is a property of a frozen label map that the
repository itself froze, stated conditionally, with both converses printed as
failing and the escape printed inline. It coins no object (the object count
stays 4/8/11, probe-verified), adds no table row, and moves no status.

### 3.4 PROPOSED diff A — the canon SG4 surface (NOT applied)

Target: `canon/gu-forces-field-space-declaration-RESULTS.md`, inserted after the
four-corner table (:71-78), before "The tie-breaker between these corners...".
Versionless, annotation-only, moves no status, changes no row.

```diff
 | ABSENT | chiral | **live inconsistency** | (uncured) |
 
+**Consistency addendum (proposed 2026-08-17, B1P-1, register item
+`BIT1-PRICE-PRINT`; annotation only -- no row moves, no corner is selected, the
+residual stays 2-bit and SG4 stays the sole decider).** The four corners map
+bijectively onto the four completions, but only THREE carry a LIVE carrier: the
+`(ABSENT, chiral)` row is the live-inconsistency row. A three-element support
+inside a 2x2 is not a product set, so the two bits are CORRELATED GIVEN
+CONSISTENCY: `ABSENT => MASSIVE`, and `CHIRAL => PRESENT => carrier A` (the only
+consistent chiral corner). BOTH CONVERSES FAIL -- `PRESENT` admits chiral and
+massive, `MASSIVE` admits ABSENT and PRESENT -- so this is a one-way price and
+forces nothing about which corner GU occupies. Consequence for anything routing
+to this residual: a bit-2 CHIRAL grant is also a bit-1 PRESENT grant and lands
+on carrier A, colliding with this file's own B-tilt (C5's source string, "A at
+the chiral point, B at the massive point"), UNLESS the mass map is non-uniform
+across the fermionic extension. Banked 2026-07-10 in GP vocabulary:
+`canon/escape-corners-campaign-RESULTS.md` :59-64. Priced there:
+`tests/escape-corners/lega1_flipped_chiral_adjudication.md` :257 ("corner (a)
+fires only if GU's physical vacuum is taken at the chiral point AND the mass map
+is uniform across the fermionic extension"). Recomputation from the frozen
+`VERTEX` map:
+`lab/active-research/joe-directed/bit1-price/b1p1-three-corner-support-prices-the-chiral-grant-2026-08-17.md`.
+
 The tie-breaker between these corners is exactly the escape-corners TRIPLE test applied to a BUILT
```

### 3.5 PROPOSED diff B — the ledger rows whose triggers reference the chiral grant (NOT applied)

Measured, not assumed. Scanning `lab/process/conditional-physics-ledger-v0.259.json`
(87 rows) for revival triggers that reference the chiral grant gives **exactly
three**; two further G6-blocked rows are listed and deliberately EXCLUDED because
their triggers are vacuum/observer-functional demands, not chiral-grant demands.

| Row | Verdict / kind | `revival_trigger` (verbatim) | In scope? |
|---|---|---|---|
| `RA-D4` | NEEDS / MISSING_CONSTRUCTION | "a positive BRST cohomology with chiral light spectrum" | **YES** — and its `trigger_conjuncts[1]` already buckets the chiral conjunct as `SOURCE_STATED_CONDITION` citing `:158`, so the annotation has a slot waiting |
| `RA-D2` | OVER_DETERMINED / GENUINE_FALSIFICATION | "an exact chiral physical carrier not obtained by equivariant mass splitting" | **YES** |
| `AC-F1` | NEEDS / MISSING_CONSTRUCTION | "a physical chiral carrier with a derived index" | **YES** |
| `RA-G3` | NEEDS / MISSING_CONSTRUCTION | "a stationary odd-form VEV in the located 126 channel" | no — vacuum demand, not a chiral-grant demand |
| `LT-GR2d` | NEEDS / MISSING_CONSTRUCTION | "a native or explicitly external normalized observer functional ..." | no |

Proposed annotation, append-only, **status-inert** (verdict, `reason_kind`,
`distance`, `revival_trigger` all unchanged; no `migrations[]` entry, because
nothing moves):

```diff
   "id": "RA-D4",
   "trigger_conjuncts": [
     { "conjunct": "chiral light spectrum, i.e. no light mirror partners",
       "bucket": "SOURCE_STATED_CONDITION",
       "register": "SC-GEN-54 notes; Transcript into the impossible.md:158",
-      "note": "the source's own conditional mechanism (decreased VEV in the ..." }
+      "note": "the source's own conditional mechanism (decreased VEV in the ...",
+      "bit1_price_note": "B1P-1, 2026-08-17 (BIT1-PRICE-PRINT), annotation only: this conjunct IS SG4 bit 2 = CHIRAL. On the frozen VERTEX support (3 live corners, not 4) CHIRAL => bit 1 PRESENT => carrier A, so satisfying this conjunct also fixes the invariance bit -- UNLESS the mass map is non-uniform across the fermionic extension (lega1 :257's second conjunct). Both converses fail, so nothing is forced in the MASSIVE/ABSENT direction. Receipts: escape-corners :59-64; lega1 :257. No verdict, kind, distance or trigger changes." }
   ]
```

```diff
   "id": "RA-D2",
   "revival_trigger": "an exact chiral physical carrier not obtained by equivariant mass splitting",
+  "bit1_price_note": "B1P-1, 2026-08-17 (BIT1-PRICE-PRINT), annotation only: 'chiral' here is SG4 bit 2 = CHIRAL. Given the frozen VERTEX map's 3-corner consistent support, a chiral resolution also grants bit 1 = PRESENT (carrier A) unless the mass map is non-uniform (lega1 :257). Status-inert: this row stays OVER_DETERMINED / GENUINE_FALSIFICATION."
```

```diff
   "id": "AC-F1",
   "revival_trigger": "a physical chiral carrier with a derived index",
+  "bit1_price_note": "B1P-1, 2026-08-17 (BIT1-PRICE-PRINT), annotation only: same price as RA-D2's trigger -- a chiral carrier is a bit-2 CHIRAL grant, which given consistency is also a bit-1 PRESENT grant (carrier A), escapable only by a non-uniform mass map. Status-inert."
```

**Deliberate non-proposal.** No diff identifies G3 with G6 (the reference's
recorded disagreement D4 stands), and no diff touches `RA-G3` or `LT-GR2d`.

---

## 4. Piece 2 — CT1-LA3-WORDING

### 4.1 The defect

LD-C flagged it and did not repair it (§7.4): the LA3 cell's two halves point
opposite ways. `SC-CHI-01`'s hedge says the operator decouples *"when there is
no vacuum expectation value pulling the various sub-fields of `varpi` to values
significantly above zero"* — the decoupling holds **at** small `varpi`. The
predeclaration codes the same thing: `PHASE`'s `CHIRAL` value is the *"unbroken
chiral (decreased-VEV) point"*. So the L1 -> L4 decoupling arrow **exists** in
the `varpi -> 0` phase. The cell said it is **absent** there.

### 4.2 Before / after (cell 5 of row LA3; cells 1-4 and 6 untouched)

**BEFORE:**

> **VEV-conditional**: exists only under the `SC-CHI-01` hedge / SG4 bit 2 grant
> (Grant-poset node G6); in the `varpi -> 0` phase the arrow is absent. Not
> injective as declared: sectors below the dashed line are dark, not deleted

**AFTER:**

> **VEV-conditional**: exists only under the `SC-CHI-01` hedge / SG4 bit 2 grant
> (Grant-poset node G6) — and the hedge's decoupling holds AT small `varpi`, so
> the arrow is PRESENT in the `varpi -> 0` (decreased-VEV, bit-2 CHIRAL) phase
> and ABSENT once a VEV pulls the `varpi` sub-fields significantly above zero
> (bit-2 MASSIVE) [DIRECTION CORRECTED 2026-08-17 by B1P-1, register item
> `CT1-LA3-WORDING`: the prior wording read "in the `varpi -> 0` phase the arrow
> is absent", which inverts the hedge it cites]. Not injective as declared:
> sectors below the dashed line are dark, not deleted

One corrected sentence; dated; the old wording quoted inside the marker rather
than deleted (the repo's struck-not-deleted convention), which is why the probe
must distinguish "the inverted claim is asserted" from "the inverted string
appears inside a correction marker" — it does, by requiring the marker context.

### 4.3 CT-1 probe status after the edit

`_local/cas-venv/bin/python tests/channel-swings/joe_directed_ct1_base_categories.py`
→ **`CERTIFICATE: 99/99 checks pass; 0 planted-false propositions observed
true`, exit 0.**

**Zero pin refreshes were needed, and that is by construction, not luck.** The
three CT-1 pins that touch this row are:

- `P10` — `arrow_by_id["LA3"][3].strip() == "quotient"`: the **type** cell,
  untouched.
- `P11` — `"VEV-conditional" in arrow_by_id["LA3"][4]`: the substring is
  preserved verbatim at the head of the corrected cell.
- `R02` — `len(receipt_paths(...)) == 24`: `receipt_paths` reads only each
  table row's **last** cell; neither edit adds or removes a receipt path, and
  the G6 addendum is prose (not a table row), so its cited paths are outside
  that count. Verified: still 24.

The addendum was likewise built against CT-1's parser: it adds no
`| ID | ... |` row, so the object counts stay 4 / 8 / 11 (`P04`-`P06`), the
`<= 12` budget is untouched (`P07`), and no `GA*` row relating G3 to G6 is
created (`F05`). The `gu-token-codomain` block is untouched, so the gate's
three-surface codomain triangle (`C01`-`C03`, `C10`) is unaffected.

`tests/channel-swings/joe_directed_ct1_base_categories.py` was **not edited.**

---

## 5. Piece 3 — SRC-SCCHI-COUPLING-RULE

### 5.1 (a) The rule, as a methods note section

**Proposed location:** `lab/methods/claim-status-consistency.md`, as a new
section immediately after "Consistency Rules". Rationale: that file is already
the repo's owner surface for *status drift* — its stated job is "to stop
path-dependent status drift: future agents should not have to rediscover that an
older 'resolved' phrase was later narrowed" — and this rule is exactly that
failure in the source-claim direction. It is `status: canon`, `doc_type:
runbook`, and **not writable by this task**; the text below is a proposal for its
owner. No new file is created (the register item's own instruction).

> ## Coupling Rule: Adverse-Mechanism News Fires the Hedge-Watch
>
> *(proposed 2026-08-17 by B1P-1; register item `SRC-SCCHI-COUPLING-RULE`;
> origin LD-C cards 6 + 16)*
>
> A source claim can be **value-committed and mechanism-open at the same time**.
> In `lab/sources/source-claim-register.yaml` that state is already typed:
> `polarity: ASSERTS` plus a hedge note, as against `polarity: UNCERTAIN`. Such a
> claim holds a *mechanism* debt, which revives on construction news — but the
> value it already asserts is exposed to construction news running the OTHER way.
> With no rule joining the two fields, adverse mechanism news reads as "progress
> on an open bit" when what it is, is "the source's asserted value is failing to
> be dynamically realized."
>
> **The rule.** When a repository artifact resolves, at any grade, AGAINST the
> mechanism a hedged `ASSERTS` claim depends on, the same change must:
> 1. name the claim by its `SC-` ID;
> 2. add a dated pointer under that claim's `adherence` block — a *hedge-watch*
>    ping — recording the artifact path, the direction (`adverse`), and the
>    conditionality the artifact itself declares; and
> 3. state explicitly that no status moved.
>
> **What the rule does NOT do.** It does not change the claim row's polarity, it
> does not change the adherence verdict, and it does not move any ledger row. The
> register's own standing rule governs: *"weakening an ASSERTS row requires a new
> source edition or an adjudication artifact, never silent reinterpretation."* A
> hedge-watch is a routing obligation on the reader, not a verdict on the source.
> Symmetrically, it may not be cited as evidence that the source is wrong: the
> adverse artifacts are conditional on assumptions the source has not declared.
>
> **Why it exists — three dated misses.** SRC-2 (2026-08-14), SRC-3 (2026-08-14)
> and SRC-4 (2026-08-15) each computed adverse, `SOURCE_NATIVE_ROUTE`,
> `pending_integration` results on the potential that `SC-CHI-01`'s VEV condition
> needs — the symmetric point is a critical point but NEVER a minimum for any
> nonzero background curvature; the potential is unbounded below from two
> independent causes; the eddy completion cannot rescue it — and none produced an
> adherence ping. Measured 2026-08-17: **zero** occurrences of `SC-CHI-01` and
> **zero** of "bit 2"/"bit-2" across all four SRC artifacts, and **zero** SRC
> paths anywhere in the register.
>
> **Scope.** Stated generally; armed today on exactly one claim, `SC-CHI-01`.
> Extending it to other hedged `ASSERTS` rows is the register owner's call.

### 5.2 (b) PROPOSED source-register diff — wiring SRC-1..4 into SC-CHI-01 (NOT applied)

Target: `lab/sources/source-claim-register.yaml`, the `SC-CHI-01` record
(:909-935). The claim row (`polarity`, `claim`, `verbatim`, `locus`, `grade`,
`notes`, `core`) is **not touched**; the adherence VERDICT is **not touched**.
One new sub-block under `adherence`.

```diff
   adherence:
     adherence: ADHERED
     evidence:
     - explorations/frontier-design-packets-index-2026-08-11.md:41-53
     - NEXT-STEPS.md:10-17
     - lab/process/CURRENT-RESEARCH-CONTEXT.md:1639-1640
     pinned_at: b6d31fdb272e4c9c8d56454bb9575d4f2d2a26f1
     date: 2026-08-11
     note: The p.52 non-chiral-split assertion is now quoted on the frontier surface with 'kills must name
       which claim they kill'; adherence is one day old and lives in the INDEX - regresses to PARTIAL if
       the executing wave skips the source-claim column.
+    hedge_watch:
+      armed: '2026-08-17'
+      by: 'B1P-1 (register item SRC-SCCHI-COUPLING-RULE; origin LD-C cards 6+16)'
+      rule: >-
+        Adverse-mechanism news on this claim's VEV condition obliges a dated pointer
+        here naming the artifact, its direction and its declared conditionality. It
+        never moves this row's polarity (ASSERTS) and never moves a ledger verdict.
+      status_effect: >-
+        NONE. adherence stays ADHERED; polarity stays ASSERTS; no ledger row moves.
+        What this block changes is RELAY: statements about SC-CHI-01 carry this
+        pointer, so adverse-mechanism news is not relayed as progress on an open bit.
+      source_native_steelman:
+      - path: lab/active-research/joe-directed/majorana-126-neutrino/src1-source-steelman-of-the-vev-2026-08-14.md
+        note: the source's own mechanism, extracted (quartic and quadratic both from ||F||^2; no Higgs). The three partials below compute ON this steelman.
+      adverse_mechanism_partials:
+      - path: lab/active-research/joe-directed/majorana-126-neutrino/src2-mexican-hat-is-automatic-2026-08-14.md
+        date: '2026-08-14'
+        direction: adverse
+        finding: the symmetric point is a critical point but NEVER a minimum, for ANY nonzero background curvature (exact, 21/21)
+        classification: SOURCE_NATIVE_ROUTE
+        conditionality: conditional on the SG4-undeclared quadratic form
+        canonical_effect: pending_integration
+      - path: lab/active-research/joe-directed/majorana-126-neutrino/src3-potential-unbounded-below-2026-08-14.md
+        date: '2026-08-14'
+        direction: adverse
+        finding: the potential is UNBOUNDED BELOW from two independent causes (16/16)
+        classification: SOURCE_NATIVE_ROUTE
+        conditionality: conditional on the Ad-invariant pairing; SG4 leaves the quadratic form undeclared
+        canonical_effect: pending_integration
+      - path: lab/active-research/joe-directed/majorana-126-neutrino/src4-eddy-completion-cannot-rescue-the-potential-2026-08-15.md
+        date: '2026-08-15'
+        direction: adverse
+        finding: the eddy/Chern-Simons completion does NOT rescue boundedness; it reduces to the sign of one undeclared composite coefficient (44/44)
+        classification: SOURCE_NATIVE_ROUTE
+        conditionality: sign of an undeclared composite coefficient
+        canonical_effect: pending_integration
+      miss_record: >-
+        Three dated misses before this wiring (SRC-2 08-14, SRC-3 08-14, SRC-4 08-15):
+        each produced zero adherence pings. Measured 2026-08-17 - zero occurrences of
+        'SC-CHI-01' and zero of 'bit 2'/'bit-2' in all four SRC artifacts, and zero SRC
+        paths anywhere in this register.
+      bit1_price_pointer: >-
+        The phase this hedge conditions on is SG4 bit 2. On the frozen VERTEX support
+        (3 live corners, not 4) a CHIRAL grant also grants bit 1 = PRESENT (carrier A),
+        escapable only by a non-uniform mass map (lega1 :257). See
+        lab/active-research/joe-directed/bit1-price/b1p1-three-corner-support-prices-the-chiral-grant-2026-08-17.md.
```

`hedge_watch` is a **new key**, declared as new rather than forced into
`evidence` (the ledger taxonomy's `NEW_KIND_REQUIRED__FORCED_FIT_FORBIDDEN`
principle, applied by analogy). If the register owner prefers no new key, the
degenerate form is: append the four SRC paths to `evidence` and fold
`miss_record` + `status_effect` into `note`. That variant loses the
`direction: adverse` typing, which is the whole point, so it is offered second.

### 5.3 (c) Can an existing gate carry the sensing cheaply? — measured, and the answer is NO as written

The brief's own hypothesis was that the canonical-currency audit's `CC-06`
signature "already touches this family". **Measured against the live registry
through the gate's own `signature_match`, it does not reach the SRC arc at all.**

`CC-06-CHIRALITY-VEV-CONDITIONAL` is `match: all_families` — a conjunction over
two token families:

| | family 1 (`chirality mechanism`, `effective chirality`, `chiral sectors`, `produces chirality`, `chirality is produced`, `source's stated mechanism`) | family 2 (`vev`, `vacuum expectation`, `condensate`, `symmetry breaking`, `decoupling`) | conjunction |
|---|---|---|---|
| SRC-1 | **MISS** | HIT (`vev`, `condensate`, `symmetry breaking`) | **False** |
| SRC-2 | **MISS** | HIT (`vev`) | **False** |
| SRC-3 | **MISS** | HIT (`vev`, `symmetry breaking`) | **False** |
| SRC-4 | **MISS** | HIT (`vev`) | **False** |

SRC-2/3/4 do not contain the string "chiral" at all. This is *exactly* the hole
CC-06's own registry entry declares: *"A document that forecloses the source's
route implicitly -- by listing chirality origins without ever writing 'chirality
mechanism' or 'effective chirality' -- escapes."*

**Widening CC-06 is the wrong repair, and priced.** Family 1 is the entry's
chirality-mechanism ANCHOR (`anchor_match` reads family 1 alone). Adding
stability/potential vocabulary to it would make CC-06 fire on files that never
touch the chirality mechanism, and the entry has already rejected bare
"chiral"/"chirality" with a measured reason — *"315 pre-date files under a
looser draft of this entry, dominated by non-consumers"*. A currency correction
and a hedge-watch are also different objects: CC-06 asks "does this file assert a
superseded READING?", the hedge-watch asks "does this file's RESULT run against
an asserted claim's mechanism?" Forcing the second into the first is the
forced-fit the registry's own taxonomy forbids.

**`kill_target_claim_audit.py` does not see them either** — measured. Its trigger
is kill-language (`kill|no-go|falsif|dead|fatal`) in the frontmatter
title/result/status; "UNBOUNDED BELOW", "never stable", "does NOT rescue" are all
outside that token set, and the gate's current scan sits at its scope baseline
of 3 red, none an SRC file.

**The cheap hook that DOES exist** (proposal for the gate owner, ~a dozen lines,
no new gate, no red/green semantics change). `kill_target_claim_audit.py`
already (i) parses frontmatter across a 3877-file derived scope, (ii) carries
`CLAIM_ID = SC-[A-Z]+-\d+`, and (iii) loads the register. Add one recognised
frontmatter key it already knows how to read:

```yaml
bears_on: SC-CHI-01        # direction: adverse | supporting | conditional
```

scanned with the existing `CLAIM_ID` regex and emitted as a per-claim WARN
listing each run (never a red — FX-3's rule that a gate which reds on a work
queue gets deleted, and the currency gate's own "the dirty set is WARN-ONLY"
precedent). The register owner's adherence sweep then reads one printed list
instead of grepping.

**Cheaper still, requiring no code at all:** SRC-1 already carries
`target_claim: SC-GEO-58`. SRC-2 and SRC-3 carry **no `target_claim` key**;
SRC-4's is free prose with no `SC-` ID. A convention that every
source-mechanism gate carries an `SC-` ID in `target_claim` would put the whole
arc inside the existing gate's coverage line with zero gate edits. That is a
one-line-per-artifact convention change and is the recommended first move.

---

## 6. Hostile review, inline

**Strongest attack (the brief's own): does printing the price shade into
ADJUDICATING bit 1, which is not this task's to do?** Taken at full strength,
the attack is: "You have written `CHIRAL => PRESENT => carrier A` on a methods
surface. Routers read methods surfaces. In practice a reader now believes bit 1
is PRESENT and the carrier is A — you have adjudicated by publication, and the
'annotation only' header is a fig leaf."

**Answer, in three parts, each mechanically enforced.**

1. **The printed object is a conditional whose antecedent is unresolved and
   printed as unresolved.** Every price sentence is of the form *granting X, Y
   follows*. Nowhere is X granted. The addendum states in the same paragraph
   that "SG4 remains the sole decider, the residual remains 2-bit, the B-tilt is
   unmoved." A conditional with an open antecedent is not a verdict on the
   consequent; asserting otherwise would make the entire ledger's
   `DERIVED_CONDITIONAL` machinery a body of verdicts.
2. **The one-wayness is printed, not buried, and is probe-enforced.** If this
   were adjudication-by-publication, the profitable move would be to print
   `CHIRAL <=> PRESENT` or to omit the converses. Instead both failing converses
   are printed in the addendum and asserted as CONTRARY controls in the probe
   (V09/V10): `PRESENT` is consistent with MASSIVE, `MASSIVE` with ABSENT and
   PRESENT. A future edit that upgrades the price to an equivalence turns the
   probe red. That is the opposite of a fig leaf: it is a tripwire against the
   attack's own failure mode.
3. **The escape is printed inline, at equal weight.** `lega1` :257's second
   conjunct — a non-uniform mass map — is in the addendum's own text, sourced to
   the author's plural "sub-fields". A document that wanted to adjudicate would
   put the escape in a footnote. A reader of the addendum cannot reach "bit 1 is
   PRESENT" without also reading "unless the mass map is non-uniform", which is
   an OPEN question the addendum names as open.

**Where the attack lands, conceded.** It lands on *salience*, not on content.
Printing a conditional at a routing node does raise the probability that a
careless reader compresses it to its consequent — and the standing measured
failure mode in this program is exactly frame regression under compression. Two
mitigations, both taken: (a) the addendum's first parenthetical is "no corner is
selected", before any content; (b) the price is stated as a constraint on
GRANTING, i.e. addressed to routers making a grant, not to readers wondering
what is true. What is NOT available as a mitigation is not printing it: the
register item exists because the unprinted state has its own measured cost —
routers have been handing bit 2 to lanes as "the open phase bit" while its
CHIRAL resolution silently carries bit 1.

**Second attack — is the correlation an artifact of one file's editorial
choice?** Yes, and it is stated as such. `(ABSENT, CHIRAL)`'s `INCONSISTENT`
label is a *judgement recorded in a predeclaration* (the GP/VZ argument), not a
theorem. If that label is ever withdrawn, the support becomes four corners, the
bits decouple, and the price vanishes. The addendum therefore never says "the
bits are correlated" full stop; it says "correlated **given consistency**", and
the probe's kill-switch (§1) is exactly the four-corner case. The right target
for anyone who dislikes the price is the `INCONSISTENT` label at
`leg_a_forcing_enumeration.py` :74 — and attacking it requires an SG4-grade
argument, not an edit here.

**Third attack — does the LA3 fix change the file's meaning beyond a wording
repair?** It changes the cell from a false statement to a true one about which
phase the arrow lives in. It does not change LA3's `type` (`quotient`), its
domain/codomain, its non-injectivity, or its receipts. It does not make L4
unconditional — "VEV-conditional" is preserved verbatim and remains the head of
the cell. The strongest version of this attack is that the corrected cell now
makes the chiral phase sound like the default; answered by the corrected text
naming BOTH phases explicitly and tying each to its bit-2 value.

**Fourth attack — the coupling rule is a manufactured obligation.** Refused on
evidence: the misses are dated and measured (zero tokens, zero paths), the
mechanism side is already register-typed on the polarity axis, and the rule
explicitly forbids itself from moving any status. It creates one obligation
(a dated pointer) on one claim.

**Fifth attack — the mis-DENY direction.** "The hedges mean the source commits
to nothing; the hedge-watch is theatre." Refused: `polarity: ASSERTS` is
register-typed, canon's own prose says "the chiral point GU's phenomenology
commits to", and the epoch clause ("currently dark to us") is a commitment about
our epoch. A claim that is asserted-and-hedged is neither open nor settled, and
the hedge-watch is precisely the instrument for that state.

---

## 7. The probe

`tests/channel-swings/joe_directed_b1p1_three_corner_support_prices_the_chiral_grant.py`
— **91 checks, exit 0**, no floats.

**LEG V — the correlation, recomputed.** Re-pins the predeclaration's SHA-256
against SG-1's constant; `ast`-parses `VERTEX`; computes the consistent support
(3), the excluded corner, the non-product witness, both implications, the unique
chiral carrier, and the frozen index labels. **Two CONTRARY controls** (V09,
V10) assert the converses FAIL and must PASS — they smell like they weaken the
result and are exactly what makes it honest.

**LEG Q — receipts.** Fourteen exact substrings read from their owners (canon
:59-64's three clauses, lega1 :257's conjuncts, the canon SG4 bit-1/bit-2
statement, C5's "A at the chiral point, B at the massive point", the p.52 hedge,
the register verbatim + hedge note, transcript L158, the predeclaration's
GP-bites comment, LD-C §7.2, and all three register-item IDs). Line-locus checks
confirm :59-64 and :257 actually carry their clauses at those line numbers.

**LEG P — the print.** Seven clauses of the G6 addendum pinned in
`lab/methods/gu-base-categories.md`, including the "no corner is selected"
disclaimer, both receipts, the failing converses and the non-uniform escape.

**LEG L — the LA3 fix.** The row re-parsed with CT-1's own regex: 6 cells, type
`quotient`, `VEV-conditional` preserved, corrected direction present, dated
marker present, and the inverted claim ASSERTED nowhere (the old string survives
only inside the correction marker, and the check requires that context). Also
recomputes CT-1's `R02` receipt-path count independently (24) and the object
counts (4/8/11).

**LEG M — the misses and the coupling rule.** Absence checks (zero `SC-CHI-01`,
zero `bit 2`/`bit-2` in SRC-1..4; zero SRC paths in the register), each with a
**planted-positive control** proving the detector fires on a synthetic positive
(VERIFICATION.md rule 4). The `target_claim` state of all four SRC files. The
CC-06 reach measurement computed live through the gate's own `signature_match`
(family 2 = 4/4, family 1 = 0/4, conjunction False x4), with a planted-positive
control on a synthetic string that hits both families.

**LEG F — six planted-false propositions**, each required to be observed False:
the support is a product set; `PRESENT => CHIRAL`; `(ABSENT, CHIRAL)` carries a
live carrier; CC-06 reaches SRC-2; the register cites an SRC path; the inverted
LA3 claim is asserted in the reference.

**LEG A/G — artifact + runtime.** This file's routing notice, classification in
the audit's accepted regex form, `NONE-NOT-A-KILL`, `canonical_effect:
pending_integration`, `canon_verdict_change: none`, the dated frontmatter,
`scripts`/`depends_on` wiring, and exactly one `gu-typed-objects` block
validating clean. By subprocess: CT-1's probe still exits 0, and the
typed-carrier gate's `--selftest` is GREEN.

**`--selftest`.** Verifies the **CLEAN BASELINE FIRST** (an unmutated subprocess
must exit 0 and print its certificate before any mutation is attempted; a red
baseline aborts rather than banking false catches), then injects ten
machinery/reference corruptions via `B1P1_MUTATE` — VERTEX corner-drop,
support-builder corruption (admits INCONSISTENT), SHA-constant corruption,
reference-path swap, ROOT relocation, quote-needle drift, absence-detector
blinding, CC-family blinding, arrow-row-regex blinding, planted-false evaluator
inversion. **Every mutation corrupts machinery or a reference; none loosens a
check's predicate** (VERIFICATION.md rule 2). Each must drive exit 1 **through a
genuine `[FAIL]` line on a run that still prints its certificate**; a nonzero
exit without one is counted CRASH-NOT-DETECTION and fails the selftest
(rule 3). The selftest exits 0 on success (rule 5). `--selftest --poison`
poisons the baseline and requires the refusal path.

---

## 8. Postflight — five lenses

**Lens A — did anything decide a bit?** No. Grepped this artifact and the
addendum for unconditional bit/carrier assertions: every occurrence of `PRESENT`,
`CHIRAL`, `carrier A` in a consequent position sits under an explicit `given
consistency` or `granting`. The failing converses are printed in both documents.
`SG4 remains the sole decider` appears in the addendum. The kill-switch (§1) was
recorded before the computation.

**Lens B — reproduction honesty.** The ratio: §3.1 is genuinely computed (from a
frozen file this task never opened for writing); §3.2 is retrieval; §3.3-3.5 and
§5 are writing and proposing; §5.3's CC-06 table is genuinely measured and is the
one place this task contradicted its own brief's hypothesis. Nothing here is
claimed as new physics or new mathematics — §1 Lens 6 grepped first.

**Lens C — instrument honesty.** The probe's own weakest points, named: (i) the
absence checks are token-based, so a file that pings SC-CHI-01 by paraphrase
without the ID would read as a miss — bounded by the planted-positive controls
proving only that the DETECTOR works, not that the tokens are the only route;
(ii) the LA3 "inverted claim not asserted" check is context-based (marker
present) rather than semantic; (iii) CT-1's green is asserted by subprocess exit
code plus its certificate line, per rule 7, not by its summary alone.

**Lens D — both failure classes.** Mis-credit risk (the price read as a verdict)
peaked in §3.3 and is bounded by the converse controls, the inline escape, and
the disclaimer-first ordering. Mis-deny risk (the price dismissed as trivial
bookkeeping) peaked in §6's second attack and is bounded by naming what actually
changes: routers can no longer hand bit 2 to a lane as free.

**Lens E — what did NOT move.** SG4 unchanged as decider. The 2-bit residual
unchanged. The B-tilt unchanged. `SC-CHI-01`'s polarity, hedges, adherence
verdict and evidence unchanged. No ledger row, canon file, register entry,
correction registry, currency sidecar, routing registry or predeclaration
edited. `tests/gu-forces/leg_a_forcing_enumeration.py` byte-identical (hash
re-verified after the work). CT-1's probe unedited. `rwall/` and `rs-corner/`
untouched. `canonical_effect: pending_integration`.

---

## 9. Certificate

| Field | Value |
|---|---|
| Probe | `tests/channel-swings/joe_directed_b1p1_three_corner_support_prices_the_chiral_grant.py` |
| Result | **91/91 checks pass, exit 0**; 6 planted-false propositions all observed False; 2 contrary controls PASS; 4 planted-positive controls across 3 detectors fire; no load-bearing float (swept) |
| Selftest | clean baseline verified FIRST, then 10/10 machinery/reference mutations each exit 1 via a genuine `[FAIL]` line on a certificate-printing run; `--poison` refusal path exercised; selftest exits 0 |
| Predeclaration | `tests/gu-forces/leg_a_forcing_enumeration.py` SHA-256 `3043d29e...80197` — matches SG-1's pin, byte-identical, never opened for writing |
| CT-1 probe after the edits | `99/99 checks pass`, exit 0 — **zero pin refreshes required** |
| Files written | `lab/methods/gu-base-categories.md` (2 granted edits); this artifact; its probe |
| Files proposed, not applied | `canon/gu-forces-field-space-declaration-RESULTS.md`; `lab/process/conditional-physics-ledger-v0.259.json` (3 rows); `lab/sources/source-claim-register.yaml` (`SC-CHI-01` adherence); `lab/methods/claim-status-consistency.md` (new section); `process_gates/kill_target_claim_audit.py` (`bears_on` hook) |
| Currency | CC-05/CC-06 verdicts recorded in §2; sidecar NOT written |
| Routing | `INTERNAL_STRUCTURAL_ONLY`; registry row is the method owner's call |
| Classification of effect | `canonical_effect: pending_integration`; `canon_verdict_change: none`; `steering_effect: unchanged` |

---

## 10. Blunt paragraph — could anything printed here be read as deciding a bit value?

**One sentence could be, and it is the one I most want to defend, so here it is
at full strength.** The addendum contains "granting bit 2 = CHIRAL at G6 also
grants bit 1 = PRESENT and lands on carrier A." A reader who skips the leading
"granting" walks away believing carrier A. That risk is real and I do not think
it is fully eliminable by wording; what I have done is make the misreading
expensive — the disclaimer is the first parenthetical, both converses are
printed as failing in the same paragraph, and the escape clause is in the same
sentence-neighbourhood rather than a footnote. **But the honest answer to the
question as asked is: no, nothing printed here decides a bit, and the reason is
structural rather than rhetorical.** Deciding bit 1 would require asserting its
value; every statement made is a conditional whose antecedent is exactly the bit
this repository has spent months declining to decide, and the conditional runs
in one direction only — I could not have written the equivalence without lying,
and the probe would catch me if I tried. The genuinely new epistemic content is
smaller than it looks and worth stating plainly: **it is not "carrier A", it is
"bit 2 is not free."** Everything else — the collision, the GP mechanism, the
two firing conditions, the non-uniform escape — was banked in canon five weeks
ago and is quoted, not derived. If someone later relays this artifact as "the
chiral point forces carrier A", that relay will have dropped the antecedent, the
converses and the escape — three separate compression failures, each of which
this document and its probe were built to make visible. The one thing I would
flag to the integrator as genuinely load-bearing and NOT settled by me: the
`INCONSISTENT` label on `(ABSENT, CHIRAL)` is an editorial judgement in a
predeclaration, and the entire price rests on it. It should be attacked there,
by someone with an SG4-grade argument, not here.

---

```gu-typed-objects
result: B1P-1 — the bit-1 price printed at the G6 grant node (three-corner consistent support; CHIRAL => PRESENT => carrier A given consistency, both converses failing), the LA3 direction flip repaired, and the SC-CHI-01 coupling rule stated with three unapplied diffs
carrier: repository text and structured records (the frozen VERTEX label map in leg_a_forcing_enumeration.py, the Grant-poset and Source-Layer tables of lab/methods/gu-base-categories.md, SC-CHI-01's register record, v0.259 rows RA-D2/RA-D4/AC-F1, the CC-06 correction-registry signature) LAYER=UNTYPED CHIRALITY=N/A
# LAYER is UNTYPED per the LD-C/LD-D precedent: the closed vocabulary has no token
# for repository text about the repository. The physics layers quoted are typed by
# their owners inline (bit 2 = PHASE; bit 1 = INVARIANCE selection; PHASE-CHIRAL is
# SG-1's Layer-0 homonym meaning massless/unbroken, never handedness).
# CHIRALITY=N/A is honest here and is NOT the unsubscripted-S defect: no spinor
# bundle is a carrier of this artifact — the carriers are label maps and text.
pairing: NONE
real_structure: N/A
grading: N/A
action_owner: repository-construction (the print, the repair, the proposals); source-print for every quoted source locus
target: the printed conditional price, the corrected LA3 cell, the coupling rule and its three proposed diffs MAP-TYPE=not-a-map
```
