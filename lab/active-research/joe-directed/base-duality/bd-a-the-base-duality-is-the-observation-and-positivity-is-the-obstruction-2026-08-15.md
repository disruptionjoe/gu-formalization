---
artifact_type: exploration
status: exploration
doc_type: construction-result
created: 2026-08-15
work_item: BD-A
channel: base_duality
route: CONSTRUCT__BUILD_THE_OBJECT_OR_PRODUCE_AN_EXACT_OBSTRUCTION
base_revision: a148ed80
ledger_base: lab/process/conditional-physics-ledger-v0.258.json
ledger_edit: none -- versionless delta, for the canonical owner to disposition
target_claim: "LA-11-2026-08-15-LT-GR6b-DISTANCE -- the `distance` field of the
  proposed row `LT-GR6b`: \"Missing is the datum that turns the first variation
  into an equation: a Gamma(Ad P)-equivariant pairing on Lambda^1 tensor ad P
  over X^4, factoring as (a) an Ad-invariant nondegenerate fibre form on ad P
  and (b) a base duality absorbing the one-form index, a density together with
  a nondegenerate Lambda^1 pairing. No row demands both legs. At subscript W the
  composite provably does not exist ...\", and its `revival_trigger`
  (lab/active-research/joe-directed/ledger-advancement/la11-b9stat-is-a-base-duality-row-and-four-rows-name-it-as-a-subclause-2026-08-15.md
  section 3). Secondary target: OT-1 section 2 \"The named datum ... Neither is
  supplied\" and OT-2 section 5 item 3 \"Three rows, not two, are blocked at the
  base-duality datum.\" All three are INTERNAL CHANNEL claims about the ledger
  and about repository objects. NO GU source claim is targeted, attacked or
  defended: SC-GEO-03 and SC-GEO-07 are used as SUPPLY, and this artifact
  RAISES their adherence load rather than lowering it."
target_claim_verdict: "LA-11 CORRECTED AT TWO LOAD-BEARING POINTS AND UPHELD AT
  A THIRD -- (i) the `at subscript W the composite provably does not exist`
  clause is a DOMAIN ERROR: on the module Lambda^1 (x) ad the W-invariant
  symmetric forms number d(d+1)/2 and a nondegenerate one exists at every d,
  including d = 4 and d = 14, where OT-1's theorem about forms on the ALGEBRA
  Lie(W) gives d+1 with max rank 2 dim g; (ii) leg (b) is NOT unbuilt -- it is
  the observation itself, source-owned verbatim at SC-GEO-07, so the ledger's
  asymmetric pricing of (a) and (b) is CORRECT rather than flattering; (iii)
  UPHELD: the composite the row wants, with positivity, is genuinely blocked,
  and this artifact makes the block exact. OT-1 SURVIVES with its scope
  narrowed to the cross block. OT-2's three-row bucket SURVIVES as a bucket but
  its blocking datum is re-typed from `base duality` to `positivity`."
canon_verdict_change: none
priority_change: none
steering_effect: unchanged
canonical_effect: pending_integration
rows_touched_structurally: [LT-GR1, LT-GR5, LT-GR6, LT-SM3b, LT-SM8, LT-GR2c]
rows_advanced: 0
rows_proposed: []
free_object_delta: -1
free_object_delta_note: "One un-owned object is REMOVED, not added. `LT-GR6b`'s
  leg (b) -- `a source-owned global base duality on the observed X^4` -- is not
  a free object: it is the observation section, already granted by every row
  that presupposes an observed four-dimensional geometry, and named verbatim by
  the source at SC-GEO-07. What remains genuinely un-owned after this artifact
  is narrower and already priced: the fibre Cartan involution
  (AUDIT-noncompact-compact-reduction-EXTERNAL) and the moving pairing."
grade: "EXACT integer / fractions.Fraction arithmetic. Lie algebras built from
  structure constants in-file and Jacobi-verified; so(p,q) matrix bases built,
  not quoted; Sylvester inertia by exact rational symmetric congruence, never
  by eigenvalue; ranks over Q by fraction-free elimination; no numpy, no float
  constructed anywhere; assert_no_float sweeps the whole result dict. 68/68
  checks, exit 0, via tests/channel-swings/joe_directed_bda_base_duality_construction.py
  run from the repository root under _local/cas-venv. Certificate splits as 40
  [E] exact results, 19 [C] controls that must fire, and 9 [R] reproductions of
  OT-1 / LA-11 / selected-k77 facts, all reproduced BEFORE being used. The
  364-dimensional composite inertia is obtained by DIRECT rational congruence
  on the full 364 x 364 Gram matrix, not by the tensor signature law, and the
  law is separately certified on 30 small cases. FAILURE PATH EXERCISED: nine
  planted mutations (module_rank, so64_dim, composite_sig, timelike,
  positivity, chi, pencil, onshell, artifact_drift) each drive exit 1 through
  --selftest. NOT: a ledger edit, a verdict change, a physics derivation, a
  coefficient, a selection principle, a claim that GU's action is owned, a
  claim that any GU quantity is stationary, or a claim that the moving pairing
  has been built."
disposition: OBJECT_CONSTRUCTED_AND_CONDITIONED__BASE_DUALITY_IS_THE_OBSERVATION_SECTION_SOURCE_OWNED_AT_SC_GEO_07__GLOBALITY_IS_EXACTLY_THE_LINE_FIELD_CONDITION_CHI_ZERO_ALREADY_FILED_2026_08_08__COMPOSITE_IS_NONDEGENERATE_AND_GAMMA_AD_P_EQUIVARIANT_AT_EVERY_d__LA11_SUBSCRIPT_W_CLAUSE_IS_A_DOMAIN_ERROR_MODULE_VS_ALGEBRA__POSITIVITY_IS_THE_REAL_OBSTRUCTION_AND_IS_EXACT__LORENTZIAN_BASE_ADMITS_NO_POSITIVE_COMPOSITE_FOR_ANY_NONZERO_FIBRE_FORM__POSITIVITY_AND_EQUIVARIANCE_AT_THE_NONCOMPACT_SOURCE_GROUP_ARE_JOINTLY_UNSATISFIABLE__TWO_CARTAN_INVOLUTIONS_ARE_THE_EXACT_PRICE_ONE_OF_WHICH_IS_FREE__OT1_UNPAIRED_273_IS_THE_SAME_SUBSPACE_AS_THE_DEFINITE_273__RAISING_IS_INERT_ON_SHELL_AND_BITES_ONLY_ON_THE_HESSIAN_ROWS_LA11_EXCLUDED__ZERO_ROWS_ADVANCE
depends_on:
  - lab/active-research/joe-directed/ledger-advancement/la11-b9stat-is-a-base-duality-row-and-four-rows-name-it-as-a-subclause-2026-08-15.md
  - lab/active-research/joe-directed/ownership-theorem/ot1-the-ownership-predicate-and-the-pairing-obstruction-2026-08-15.md
  - lab/active-research/joe-directed/ownership-theorem/ot2-lt-sm3b-is-not-an-ownership-row-and-the-cheapest-pair-is-the-terminal-pair-2026-08-15.md
  - lab/sources/source-claim-register.yaml
  - canon/w2-y14-spin-structure.md
  - explorations/rb1-source-repo-current-musical-2026-07-30.md
  - explorations/conditional-build/selected-k77-physical-section-faithfulness-gate-2026-08-08.md
  - explorations/conditional-build/selected-k77-lorentzian-chiral-class-pairing-2026-08-10.md
  - explorations/k77-wave2-two-connection-shifted-superconnection-action-owner-2026-08-04.md
  - explorations/canon-met-x4-contractibility-type-defect-2026-08-09.md
  - explorations/no-selector-for-the-base-sign-2026-08-08.md
  - explorations/W208-decisive-bit-lawvere-fixed-point-2026-07-14.md
  - explorations/W183-external-input-open-system-reframe-2026-07-14.md
  - explorations/big-swing-2026-07-03/AUDIT-noncompact-compact-reduction-EXTERNAL.md
scripts:
  - tests/channel-swings/joe_directed_bda_base_duality_construction.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `SOURCE_NATIVE_ROUTE`

Every object manipulated below is one the source declares: `Y¹⁴ = Met(X⁴)`
(`SC-GEO-03`), the observation as *a choice of metric ℷ on X* (`SC-GEO-07`),
and `𝒲 = 𝒢 ⋉ Ω¹(ad P)` (`SC-GRP-05`, `GU-YM-Δ1`). No conventional comparator
adjudicates anything here; the one comparator-adjacent word in the target
string — *positive* — is routed explicitly in §5 against the repository's own
settled Krein fork rather than imported.

# BD-A — the base duality is the observation, and positivity is the obstruction

## Verdict first, unsoftened

**The object is constructible. I built it. The demand that blocks it is not the
base duality — it is positivity, and positivity fails for an exact reason that
the source's own group supplies.**

Three results, in decreasing order of how much they move.

**1. Leg (b) is not missing. It is the observation.** `Y¹⁴ = Met(X⁴)` is the
bundle of pointwise **Lorentzian** metrics on `X⁴` (`canon/w2-y14-spin-structure.md`
Scope). A section of that bundle *is* a Lorentzian metric on `X⁴` — not an
analogue of one, the thing itself. The source names it in those words:
`SC-GEO-07` verbatim, *"each observation of Y via **a choice of metric gimel on
X** actually induces a metric and connection on Y."* So "a source-owned global
base duality on the observed `X⁴`, a density together with a nondegenerate `Λ¹`
pairing" is a redescription of `ℷ`, and `ℷ` is granted by every row that
presupposes an observed four-dimensional geometry.

**2. The clause `at subscript 𝒲 the composite provably does not exist` is a
domain error, and it inverts the answer.** OT-1's theorem is about symmetric
forms on the **algebra** `Lie(𝒲) = 𝔤 ⋉ V`. `LT-GR6b`'s trigger asks for a
pairing **on the module** `V = Λ¹ ⊗ ad P`. On the module the `V`-directions act
trivially on `V` — verified from structure constants, not assumed — so
`𝒲`-invariance *is* `Γ(Ad P)`-invariance, and:

```text
              domain                 # invariant forms   max rank
d = 4   algebra  Lie(W)  dim 15            5 = d+1        6 of 15
d = 4   module   V       dim 12           10 = d(d+1)/2  12 of 12
```

Same group. Two domains. Opposite answers. A nondegenerate `𝒲`-invariant
pairing on `Λ¹ ⊗ ad P` exists at **every** `d` tested, `d = 1,2,3,4`, and an explicit nondegenerate
member is exhibited at `d = 14` as well (`[E]`, with a degenerate-base control
that fires). The row's
`distance` welds OT-1's impossibility proof about one object onto its own
trigger's demand for the other.

**3. What is actually blocked is positivity, and here it is exactly.** With the
base pairing Lorentzian — which `Met_Lor(X⁴)` forces — the composite is
**never** positive-definite, for **any** nonzero fibre form. Not "not yet": the
signature arithmetic forbids it. And an `Ad`-invariant *definite* fibre form
exists iff the algebra is of compact type, which `𝔰𝔬(7,7)` is not (its invariant
trace form has inertia `(49, 42)`). So the trigger's two clauses — `Γ(Ad P)`-
equivariance at the source's group, and positivity — are **jointly
unsatisfiable**. They become satisfiable only after two Cartan involutions, one
per factor, and exactly one of those two is free.

| the trigger's clause | verdict | condition |
|---|---|---|
| a source-owned base duality on `X⁴` | **SUPPLIED** | it is `ℷ`, `SC-GEO-07`; zero new grants |
| ... **global** | **CONDITIONAL, already filed** | a line field on `X⁴`; for closed `X⁴`, `chi(X^4) = 0` (2026-08-08 gate) |
| a density **together with** a nondegenerate `Λ¹` pairing | **ONE datum, not two** | the pairing determines the density (§6) |
| composed with an `Ad`-invariant fibre form | **SUPPLIED** | the trace form; unique up to scale on a simple factor |
| into a `Γ(Ad P)`-equivariant pairing on `Λ¹ ⊗ ad P` | **CONSTRUCTED** | §3; nondegenerate, inertia `(189, 175)` |
| ... **positive on the physical quotient** | **BLOCKED, exactly** | needs a fibre Cartan involution (`REDUCTION_EXTERNAL`) and a timelike covector line (free, = the line field above) |

---

## 0. PREFLIGHT — six specialist lenses, run inline

Standing rule: N lenses means N sections written here, never N subagents.

### Lens P1 — target triage: is this still the right target after LA-11, OT-2 and PHI-2?

**Yes, and its rank went UP, but the object to attack moved.** Re-derived from
what landed today rather than from the brief.

- **LA-11** wrote the row and, in its own Lens Q4, ranked what a hostile reader
  should attack: `source_row` first, **mode-⊤ on the trigger second**, class-S
  lexicon third, and *"do not attack the invariant-form computation."* This
  route attacks item 2 and obeys the instruction on item 4 — §1 **reproduces**
  the invariant-form theorem and does not contest a single number in it. It
  contests the theorem's **domain of application**, which is a different target
  and is exactly what LA-11's own Lens Q2 flagged as the reading it could not
  refute from the ledger.
- **OT-2** raised the stake: it moved `LT-SM3b` into the same blocked bucket as
  `LT-GR1` and `LT-GR6` and concluded *"Three rows, not two, are blocked at the
  base-duality datum."* Three rows blocked on one datum is the highest
  concentration on the LAGRANGIAN axis. That is a **rank increase** for this
  target, and it is the reason this route is worth a swing.
- **PHI-2** is on the ANOMALY axis, moved zero rows, and its `phi_ext` result
  changes no premise used here. **It does not touch this target.** The one thing
  it does supply is a discipline transfer: PHI-2 broke a filed relation
  (`LA-3`'s rank 4 → rank 5) by *widening the target lattice*, i.e. by finding
  that a result was true on a smaller object than the one it was being applied
  to. That is structurally the same failure this route finds in `LT-GR6b`'s
  `distance`, and I looked for it because PHI-2 had just found one.
- **What moved in the ranking, stated plainly:** before today the target was
  "build the base duality." After LA-11 named the composite and OT-2 stacked
  three rows on it, the target is "decide the composite." The base-duality half
  turned out to be the cheap half; the expensive half is the last five words of
  the trigger.

### Lens P2 — differential geometry, densities and orientation: what does a metric-bundle section actually give you?

The bundle `π : Met_Lor(X⁴) → X⁴` has fibre the space of Lorentzian inner
products on `T_xX`, `≅ GL(4,ℝ)/O(3,1)`, homotopy type `ℝP³ × ℝ⁺`
(`canon/no-go-class-relative-map.md` §2.1, and the type-defect finding
`explorations/canon-met-x4-contractibility-type-defect-2026-08-09.md`, which
established that the Riemannian contractibility fact does **not** transfer).
Three consequences the lens insists on before any construction:

1. A **section** of this bundle is a Lorentzian metric on `X⁴`. There is no gap
   between "observation" and "base metric"; they are the same datum under two
   names. This is the whole of leg (b).
2. Because the fibre is **not contractible**, a global section is **not**
   automatic. This is exactly where the "global" in the trigger has teeth.
3. A density is *not* independent data once the pairing is fixed: `|det N|^{-1/2}`
   transforms with weight 1 under frame change. §6 certifies this exactly. So
   the trigger's conjunction "a density together with a nondegenerate `Λ¹`
   pairing" is one object, and the ledger should stop counting it as two.

**Prediction written before the sweep:** the "base duality" will turn out to be
already owned and the real cost will be somewhere in the qualifiers. It was:
the cost is in `global` and in `positive`.

### Lens P3 — source archivist: read the register before deciding what GU denies

The brief's hypothesis — that `Y¹⁴ = Met(X⁴)` **denies** a base density and
`Λ¹` pairing, because those are what a chosen metric supplies — is testable
against `lab/sources/source-claim-register.yaml`, and it **fails**. Two
verbatim entries decide it:

- `SC-GEO-03`, hard-core, `ADHERED`: *"In this work we will consider the
  strongest assumption beyond those of Einstein by working within the
  Einsteinian Observerse... Note that in this case, **Y doesn't usually have a
  pre-existing metric**."* The denial is about **`Y`**, not `X`.
- `SC-GEO-07`: *"each observation of Y via **a choice of metric gimel on X**
  actually induces a metric and connection on Y identifying the Topological
  spinors with the metric spinors."* The metric on `X` is not merely permitted;
  it is the **input** of the observation, and the metric on `Y` is the
  **output**.

And `SC-GEO-06`: chimeric spinors are defined *"without making a choice of
metric **on Y**."* Every no-metric clause in the register is scoped to `Y`.

**So the hypothesis is refuted, and refuted in the informative direction.** The
move that defines the theory does not deny the base duality; it *consumes* one.
What it denies is a *pre-existing* metric on the 14-dimensional total space —
which is why the gimmel metric is built from the tautological fibre datum
instead of posited. The correct statement of GU's foundational move for this
purpose is: **the metric on `X⁴` is demoted from background to field, and the
metric on `Y¹⁴` is derived from it.** Demotion is not denial, and §7 computes
exactly what the demotion costs.

### Lens P4 — representation theory of `𝒲`: predict the two answers before computing them

`V = Λ¹ ⊗ ad` with `𝔤` acting only on the second factor. Then

```text
Sym^2(V)^*  ⊃  (Sym^2 (Λ^1)^* (x) Sym^2 (ad)^*)^g  (+)  (Λ^2 (Λ^1)^* (x) Λ^2 (ad)^*)^g
```

For `𝔤` simple the invariant symmetric forms on `ad` are one-dimensional
(Killing) and the invariant **antisymmetric** ones are zero, so the second
summand dies and the count is `dim Sym²(Λ¹)^* × 1 = d(d+1)/2`. On the algebra
`Lie(𝒲)` the count is OT-1's `d+1`. **Both numbers written before running
anything; the probe returns `10` and `5` at `d = 4` (`[E]`).** The lens's point
is that these are *different representations of the same group*, so quoting a
theorem about one as a theorem about the other is a type error, not a
disagreement — which is why §2 reports it as a domain correction and not as a
refutation of OT-1's mathematics.

### Lens P5 — Krein / indefinite-metric operator theory: what does `positive` cost?

`GEOMETER-VS-PHYSICS-OBJECTS.md` records this program's ghost clearance as
settled on the **geometric** side: *keep-and-grade* via a Krein form
`[P,S] = 0` with `P` a Cartan involution, explicitly **not** a positive Hilbert
space. `W208` computed the same thing from the other end: *"preserving the
positive-definite total metric `η₊` forces the group compact; the non-compact
boosts break `η₊`."* `W219` located the compact centralizer of the Krein parity
in `Sp(32,32;ℍ)` as `Sp(32) × Sp(32)`.

**So a trigger clause demanding a positive object is, on this program, a
demand for a Cartan involution — and the repository already knows what that
costs.** The lens's instruction to the construction: do not treat "positive on
the physical quotient" as a mild extra; price it, and price it in the same
currency the repository already uses for the fibre
(`AUDIT-noncompact-compact-reduction-EXTERNAL`). §5 does. The lens also
predicts, correctly, that the base and the fibre will need the *same kind* of
object, because `O(3,1) ⊃ O(3) × O(1)` is a maximal-compact reduction exactly
as `G ⊃ K` is.

### Lens P6 — grep-before-novelty: what already exists, credited and not re-claimed

The brief's channel has produced eight false novelty claims. This sweep was run
before writing a line of the construction, and it found substantial prior art
for **every** component. Nothing below is claimed as new.

| object | owner, date | relation to BD-A |
|---|---|---|
| the composite pairing itself — *"a nondegenerate gimmel metric `G` and a nondegenerate real invariant adjoint pairing `κ_𝔤` convert an adjoint-valued connection equation covector into an adjoint-valued one-form. **Positivity is neither used nor available.**"* | `RB1` (`explorations/rb1-source-repo-current-musical-2026-07-30.md` §5), 2026-07-30 | **This is the object.** Built on `Y¹⁴` with `G`, sixteen days before `LT-GR6b` filed it as unbuilt. BD-A adds the `X⁴` reading, the exact inertias, and the positivity theorem — not the map |
| the map's reuse as *"the indefinite connection pseudo-musical"* | `RB2`, `k77-wave2-*` (≥ 8 artifacts), 2026-07-30 → 2026-08-05 | the object is load-bearing across a whole wave; it is not an unnamed gap |
| `g_s = s*G` is derived from `s`, *"and the literal induced metric depends on `ds`, so its density/Hodge/norm chains are first order in the section variation"* | `RB1` correction 4 | the **moving** problem, stated exactly, before this route |
| *"the smallest action-first repair is to build a **moving** bosonic pseudo-musical"* with a named six-item burden | `k77-wave2-two-connection-shifted-superconnection-action-owner-2026-08-04.md` §6 | the genuine open residual; §7 scopes where it can and cannot bite |
| the section/graph fork: `√det ḡ` vs `√det g` agree pointwise on the `dφ = 0` slice and differ in the gradient sector, *"flagged, not resolved"* | `W126`, 2026-07-13 | the two candidate base metrics (`ℷ` vs `s*G`) are an already-open fork, not a new one |
| **a spin four-manifold does not automatically admit a global Lorentz metric; `S⁴` is spin with `χ = 2`**, and *"Weinstein rejects Curt's claim that the needed global section is already present"* | `selected-k77-physical-section-faithfulness-gate-2026-08-08.md`, status `ARBITRARY_X_LORENTZ_SECTION_FALSE` | **this is the `global` clause, already owned and source-attested.** BD-A only identifies it as the content of the trigger's qualifier |
| every nonzero invariant real form on `𝔰𝔩(2,ℂ)_ℝ ≅ 𝔰𝔬(3,1)` has neutral inertia `(3,3)` | `selected-k77-lorentzian-chiral-class-pairing-2026-08-10.md` | reproduced here as `[R]` from structure constants |
| positivity forces compactness; the Cartan involution as the compact-reduction direction | `W208`, `W219`, `W237`, 2026-07-14/15 | the positivity theorem's Lie-theoretic half is theirs |
| an indefinite pairing admits complex-conjugate spectral pairs above an exceptional point | `W183`, `W178`, 2026-07-14 | the phenomenon in §7.2 is theirs; BD-A only places it on **this** pairing |
| no convention-independent structure selects the base sign | `no-selector-for-the-base-sign-2026-08-08.md` | why §5 reports the `273/91` split as *definite/indefinite* and refuses to say which is *positive* |

**Concurrent work, not prior art, recorded because it changes the novelty
claim.** `bd-c-met-x-is-an-argument-not-a-background-2026-08-15.md` landed in
this same directory one minute before this file, from an independent route, and
**it reaches the same leg-(b) conclusion with a stronger source locus than
mine.** It finds the base duality named as *the second argument of the source's
own first action*, `I^B_1 : G × MET(X^{1,3}) → ℝ` (draft p.43 eq 9.1), with a
printed `Hodge Star` brace at p.44 eq 9.4 — evidence strictly better than my
`SC-GEO-07` reading, and it independently reports the positivity subclause as
source-disavowed. It also proves the canonical horn is refused by an exact
naturality obstruction (the `λ·I`-invariant symmetric forms on `ℝ⁴` are
zero-dimensional), which this route does not have.

**So "the base duality is the observation" is NOT a BD-A novelty claim.** Two
independent routes reached it the same day. What BD-C does not contain, and
what this artifact is therefore for, is §2 (the module/algebra domain
correction, with both counts computed), §5 (the exact positivity inertias and
the timelike/spacelike/null trichotomy) and §7 (on-shell inertness and the
relocation of the positivity condition onto class H). One BD-C result is used
below rather than re-derived: its exact identity `s^*(tautological horizontal
metric) = s`, which sharpens — but does not close — this artifact's own H4 seam
(§9), since the full chimeric pullback still carries the vertical `|ds|²` term
that `W126` flagged.

**The narrow novelty claim, and it is all of it:** (i) that `LT-GR6b`'s
`distance` applies an algebra theorem to a module demand, with the two counts
computed side by side; (ii) that the trigger's leg (b) is `SC-GEO-07`'s `ℷ`,
so the ledger's asymmetric pricing is correct rather than flattering; (iii) the
exact inertias `(49, 42)`, `(189, 175)`, `(273, 91)` and the timelike /
spacelike / null trichotomy on `ker(c) ⊗ ad`; (iv) the observation that OT-1's
unpaired `273` and the definite `273` are the **same subspace**; (v) that
raising is inert on-shell and therefore bites only on the Hessian rows LA-11
deliberately excluded. Nothing else.

---

## 1. THE COMPUTATION — 68/68, exit 0, zero floats, failure path exercised

`tests/channel-swings/joe_directed_bda_base_duality_construction.py`, from the
repository root under `_local/cas-venv`.

```text
CERTIFICATE: 68/68 checks pass; no load-bearing float (swept).
split  [C] 19  [E] 40  [R] 9
```

Prior results are reproduced **before** being used (`[R]`):

```text
OT-1/LA-11   invariant forms on Lie(W) number d+1; V-V block identically zero;
             max rank exactly 2 dim g = 6; nondegenerate iff d <= 1
OT-1         radical (d-1) dim g = 273 on X^4, 1183 on Y^14 at dim g = 91
LA-11        dim so(6,4) = 45, not 91                          CONFIRMED
k77 2026-08-10  so(3,1) trace form has neutral inertia (3,3)   CONFIRMED
k77 2026-08-08  chi(S^4) = 2, so S^4 carries no timelike line field
```

**Failure path**, nine planted mutations, each must drive exit 1:

```text
_local/cas-venv/bin/python tests/channel-swings/joe_directed_bda_base_duality_construction.py --selftest

  mutation module_rank      exit 1  OK   (module max rank at d=4 is 6, not 12)
  mutation so64_dim         exit 1  OK   (dim so(6,4) = 91)
  mutation composite_sig    exit 1  OK   (364 composite is (190,174))
  mutation timelike         exit 1  OK   (spacelike block is definite)
  mutation positivity       exit 1  OK   (Lorentzian base admits a positive composite)
  mutation chi              exit 1  OK   (chi(S^4) = 0)
  mutation pencil           exit 1  OK   (indefinite B always gives a real spectrum)
  mutation onshell          exit 1  OK   (the moving term survives on-shell)
  mutation artifact_drift   exit 1  OK   (a headline number vanishes from this file)

FAILURE-PATH SELFTEST: PASS (9/9 mutations drove exit 1)
```

The `artifact_drift` mutation exists because this artifact's headline numbers
are read back **out of this file** by the probe; the record and the certificate
cannot drift apart without exit 1.

**Two honest limits on the failure path, stated rather than left to be found.**
(i) The mechanism is an environment variable selecting planted assertions inside
the same file, each run as a fresh subprocess — the effect is a mutated copy,
but it is not literally a separate file. (ii) Every mutation asserts the exact
**negation** of a headline fact against the value the unmutated run actually
computed, so a firing mutation certifies that the computed value is the claimed
one and not merely that the harness can fail. But **nine mutations cover nine
headline facts, not all 68 checks.** The 30-case tensor-law sweep, the
Jacobi verification, the 35-inertia positivity sweep and the frame-change
determinant identity are covered only by their own internal controls.

---

## 2. THE DOMAIN CORRECTION — the same group, two domains, opposite answers

Built from structure constants; `𝔰𝔩(2,ℝ)` assembled in-file and Jacobi-verified;
`L = 𝔤 ⋉ (Λ¹ ⊗ ad)` assembled; both invariance systems solved as exact
nullspaces over **ℚ**.

```text
d   ALGEBRA Lie(W)                       MODULE V = Lambda^1 (x) ad
    dim   #inv   maxrank   nondeg?       dim   #inv   maxrank   nondeg?
1     6      2      6/6     YES            3     1      3/3      YES
2     9      3      6/9     no             6     3      6/6      YES
3    12      4      6/12    no             9     6      9/9      YES
4    15      5      6/15    no            12    10     12/12     YES
```

Left half: OT-1's theorem, reproduced exactly — `d+1`, `V–V` block identically
zero, max rank exactly `2 dim 𝔤`, threshold exactly `d = 2`. **Not contested.**

Right half: on the module, the count is `d(d+1)/2` and the maximum rank is
**full at every `d`**. `12 of 12` on the module against `6 of 15` on the
algebra, at the same `d = 4`, for the same group.

**Why the two differ, in one line.** OT-1's vanishing is forced by
`B([v,X], u) + B(X, [v,u]) = 0` with `v, u ∈ V`, `X ∈ 𝔤`. That equation needs
the **cross bracket** `[v,X] ∈ V`, which exists only because `B` is defined on
all of `Lie(𝒲)`. Restricted to the module, `ad(v)|_V = 0` — verified in the
probe, not assumed (`[E]`, with the control that `ad(X)|_V ≠ 0` firing) — and
the invariance condition is vacuous in the `V`-directions. So `𝒲`-invariance of
a form **on `V`** is precisely `Γ(Ad P)`-invariance, which is what the trigger
asked for in the first place.

**What this does to the three artifacts, precisely.**

- **OT-1 is not wrong.** Its named datum is a pairing `Λ¹ ⊗ ad P ⟶ (ad P)^*`,
  i.e. the `𝔤`–`V` **cross block**, which is what raising an index on
  `Lie(𝒲)^*` requires. That object is impossible, and at `d ≥ 2` it is
  impossible twice over — by OT-1's structure theorem and, more crudely, by
  `dim V = 4 · 91 = 364 ≠ 91 = dim 𝔤`. OT-1's scope survives verbatim.
- **LA-11's `revival_trigger` changed the object in transcription.** It asks for
  *"a `Γ(Ad P)`-equivariant pairing **on** `Λ¹ ⊗ ad P`"* — a form `V × V → ℝ`,
  not a map `V → 𝔤^*`. That is the module object, and it exists.
- **LA-11's `distance` then welds the impossibility proof of the first object
  onto the demand for the second**, in one sentence: *"At subscript `W` the
  composite provably does not exist: for `Lie(W) = g semidirect (Lambda^1
  tensor ad)` ... every available pairing has radical `ker(c) tensor ad` of
  dimension `(d-1) dim g`."* Read against the trigger's own object, that is
  false: at subscript `𝒲` the composite exists, is nondegenerate, and is unique
  up to the choice of a symmetric form on `Λ¹` and a scale on each simple
  factor.

This is a defect in a proposed row that is being handed to a canonical owner. It
is the single most consequential thing in this artifact.

---

## 3. THE CONSTRUCTION

> **BD-A.** Let `s ∈ Γ(Met_Lor(X⁴))` be the observation section — the source's
> `ℷ` (`SC-GEO-07`) — and let `κ` be an `Ad`-invariant symmetric form on
> `ad P`. Define
>
> ```text
>   B_s( alpha (x) a , beta (x) b )  =  (g_s^{-1})(alpha, beta) * kappa(a, b)
> ```
>
> on `Λ¹ ⊗ ad P`, where `g_s^{-1}` is the inverse of the section metric and the
> integrating density is `|det g_s|^{1/2}`, determined by `g_s` (§6).
>
> **`B_s` is symmetric, nondegenerate, `Γ(Ad P)`-equivariant, and invariant
> under all of `𝒲`.** All four verified exactly (`[E]`), with a non-invariant
> fibre form as the control that the invariance test can fail.

That is the whole construction. It is four lines because the object is four
lines; the work was in deciding *what* to build, not in building it.

**It is not new.** `RB1` §5 built the identical map on `Y¹⁴` with the gimmel
metric on 2026-07-30 and stated its own ceiling in the sentence that matters
here: *"The map is real and gauge-equivariant because `G` is geometric and
`κ_𝔤` is invariant... **Positivity is neither used nor available.**"* BD-A's
contribution is to read it on `X⁴` with `ℷ`, to compute its inertia exactly,
and to prove that the missing positivity is not an omission.

### 3.1 Exact inertias, at GU scale, by direct congruence

`𝔰𝔬(p,q)` matrix bases built in-file; the invariant trace form's Gram matrix
computed as `tr(X_i X_j)`; inertia by exact rational symmetric congruence. The
`364 × 364` composite is diagonalized **directly**, not by the tensor law; the
tensor law is separately certified on 30 small cases and agrees.

```text
fibre algebra      dim   invariant trace form   composite on Lambda^1 (x) ad
so(7,7)             91         (49, 42)          dim 364   (189, 175)
so(7,7), K-reduced  91         (91,  0)          dim 364   (273,  91)
so(6,4)             45         (24, 21)          dim 180   ( 93,  87)
so(6,4), K-reduced  45         (45,  0)          dim 180   (135,  45)
```

The `(273, 91)` and `(135, 45)` rows are the ones to look at twice. **`273` is
OT-1's unpaired remainder on `X⁴`, and `135` is LA-11's corrected alternate.**
They are not a coincidence and they are not the same statement — §5.

---

## 4. WHAT "GLOBAL" MEANS, EXACTLY

The trigger says *global*. That word is the only part of leg (b) that is not
free, and its content is already owned.

A global section of `Met_Lor(X⁴)` is equivalent to a **line field** on `X⁴` (a
rank-1 subbundle of `TX`), because a Lorentzian metric is exactly the data of a
line field plus a Riemannian metric, and Riemannian metrics always exist. For
**closed** `X⁴` a line field exists iff `chi(X^4) = 0`.

```text
manifold      chi   spin    global Lorentz section?
S^4            2    yes     NO
K3            24    yes     NO
CP^2           3    no      NO
T^4            0    yes     yes
S^1 x S^3      0    yes     yes
S^2 x S^2      4    yes     NO
```

**This is already the repository's, with a source receipt.**
`selected-k77-physical-section-faithfulness-gate-2026-08-08.md` filed it as
status `ARBITRARY_X_LORENTZ_SECTION_FALSE`, with the `S⁴` case and the note
that *"the primary source agrees on scope... **Weinstein rejects Curt's claim
that the needed global section is already present**."* BD-A adds only the
identification: **that gate IS the trigger's `global` clause.** Two spin
4-manifolds this repository actively argues about — `S⁴` and `K3` — fail it.

The honest reading of the qualifier is therefore not "nobody has built the
global object" but **"the global object exists iff `X⁴` admits a line field,
the source declines to assume it, and the repository has already recorded
that."** A row demanding it is demanding a topological hypothesis on `X⁴`, and
should say so.

---

## 5. THE OBSTRUCTION — positivity, exactly

### 5.1 A Lorentzian base admits no positive composite. For any fibre form.

Signatures multiply: `sig(N ⊗ κ) = (p_N p_κ + n_N n_κ, p_N n_κ + n_N p_κ)`,
certified directly on 30 exact cases and by direct congruence at 364. With `N`
Lorentzian, `p_N = 3` and `n_N = 1`, the negative part is `3 n_κ + p_κ`, which
vanishes only if `κ = 0`. Swept over all 35 nonzero fibre inertias `(p_κ, n_κ)`
with each part at most 5; **zero counterexamples** (`[E]`). The sweep is over
inertias rather than matrices, which is legitimate exactly because Sylvester's
law makes inertia a complete congruence invariant and the tensor law is
certified separately. The control fires: a **Euclidean** base with a positive
fibre form gives `(20, 0, 0)`.

That control is not decorative. **It is exactly the witness LA-11 used to pass
LA-9's mode ∅ on this trigger** — *"a positive-definite fibre form ⊗ a Euclidean
base metric on `Λ¹`, rank 12 of 12, positive definite by leading principal
minors."* The witness is real linear algebra and it is **out of scope**:
`Y¹⁴ = Met_Lor(X⁴)` is the bundle of *Lorentzian* metrics
(`canon/w2-y14-spin-structure.md` Scope), so a Euclidean base pairing is not a
member of the declared set. The non-emptiness argument for the trigger needs a
different witness, and §5.3 supplies the only one available.

### 5.2 A definite fibre form forces a compact group

An `Ad`-invariant positive-definite form on `𝔤` exists iff `𝔤` is of compact
type. Certified from structure constants: `𝔰𝔲(2)` has a one-dimensional
invariant space whose generator is definite; `𝔰𝔩(2,ℝ)` has a one-dimensional
invariant space whose generator is `(2,1)` — **the control that the difference
is definiteness and not absence fires**. At GU's scale the same fact reads off
the table: `𝔰𝔬(7,7)`'s invariant form is `(49, 42)`.

So `Γ(Ad P)`-equivariance **at the source's group** and positivity are jointly
unsatisfiable. This is the same statement `W208` made from the other side
(*"preserving the positive-definite total metric forces the group compact"*),
now on `Λ¹ ⊗ ad P`.

### 5.3 What positivity actually costs: two Cartan involutions, one of them free

Positivity is recoverable, and the price is exact:

- **fibre:** a Cartan involution `θ` on `ad P`, i.e. a reduction `G → K`. This
  is `CG-1`'s result at its ceiling and is priced `REDUCTION_EXTERNAL` by
  `AUDIT-noncompact-compact-reduction-EXTERNAL`. **Already on the books.**
- **base:** a timelike covector line `L ⊂ T^*X⁴`, i.e. a reduction
  `O(3,1) → O(3) × O(1)`. **This is the same line field whose existence is leg
  (b)'s `global` condition.** It costs nothing beyond the section.

The base half is free *given the section*, and this is the correction to LA-11's
§6 Lens Q5 item 2, which held that the ledger *"prices leg (a) ... and prices
leg (b) at nothing. That is an asymmetry that flatters."* **The asymmetry is
correct.** Leg (b) is the observation, already granted; leg (a) is a group
reduction, correctly priced. Making the pricing symmetric would have been the
error.

### 5.4 The `273`, and why it is the same subspace twice

With `κ` compact-reduced and `N` Lorentzian, the composite splits along a
covector `c`:

```text
c timelike    ker(c) inertia (3,0,0)    ker(c) (x) ad :  DEFINITE      dim 273
c spacelike   ker(c) inertia (2,1,0)    ker(c) (x) ad :  INDEFINITE    dim 273
c null        ker(c) inertia (2,0,1)    ker(c) (x) ad :  DEGENERATE    dim 273
```

All three fire; the timelike case is confirmed at full scale as `(273, 0, 0)`
with `dim 𝔤 = 91`.

**`ker(c) ⊗ ad` is exactly OT-1's radical.** OT-1's every-available-pairing
`⟨X, α ⊗ a⟩ = c(α) κ(X,a)` has radical `ker(c) ⊗ ad` of dimension
`(d−1) dim 𝔤 = 273`. So:

> the `273` directions that are **unpaired** by the cross block at subscript `𝒲`
> are, for a **timelike** `c`, exactly the `273` directions on which the module
> pairing at subscript `K` is **definite**.

Two different pairings, one subspace, opposite roles. The physical quotient that
the trigger demands positivity on is the annihilator of a timelike line — the
transverse sector — and the complementary `91 = 1 × dim 𝔤` is the temporal
sector a constraint must remove. OT-1's remainder is not debris; it is the
physical block, and this is the first place the two computations meet.

**One thing I will not say.** Which of `273` and `91` is *positive* rather than
*negative* is the base sign convention, and
`no-selector-for-the-base-sign-2026-08-08.md` established that nothing
convention-independent selects it. Definiteness of each block is
convention-independent; the word *positive* in the trigger is not. A trigger
that says *positive* rather than *definite* is one convention away from
unsatisfiable, for free.

---

## 6. THE TRIGGER'S TWO CONJUNCTS ARE ONE DATUM

*"a density together with a nondegenerate `Λ¹` pairing"*. Under a frame change
`A`, `det(A^T N A) = det(A)² det(N)` — certified exactly on seven integer frames,
with a degenerate control that fires. So `|det N|^{-1/2}` carries density weight
1 and is **determined** by `N`. The conjunction names one object, not two, and
the ledger should not count it twice when scoring the row's distance.

---

## 7. WHERE THE SECTION-DEPENDENCE BITES, AND WHERE IT DOES NOT

`B_s` depends on `s`, the field. `RB1` correction 4 stated this exactly:
*"`g_s = s^*G` is derived from `s`... so its density/Hodge/norm chains are first
order in the section variation."* At least eight artifacts name a **moving**
Hodge/density/pairing as an open construction, with the sharpest statement of
the burden at `k77-wave2-two-connection-shifted-superconnection-action-owner-2026-08-04.md`
§6. So the question is not whether the moving problem is open — it is — but
where it can and cannot bite.

### 7.1 On-shell, raising is inert. So it cannot obstruct the Euler system.

For any nondegenerate `B(x)`, the zero sets of `E` and `B^{-1}E` coincide —
certified on an 81-point exact grid with a moving **indefinite** `B`, and with a
degenerate `B` as the control that separates them. And

```text
D( B^{-1} E )  =  B^{-1} DE  -  B^{-1} (dB) B^{-1} E
```

whose second term vanishes identically at a zero of `E`. Certified exactly at
both critical points, with an off-shell control showing the term does **not**
vanish there.

**Consequence, and it corrects the row's own headline sentence.**
`LT-GR6b`'s `distance` opens *"Missing is the datum that turns the first
variation into an equation."* Both halves are wrong. The datum is not missing
(§3), and it is **not what turns `δS` into an equation** — the canonical pairing
between a space and its dual does that, with no metric, no density and no
choices, and `E = 0` in `Lie(𝒲)^*` is already an equation. The pairing raises an
index; raising is a relabelling of a zero set.

### 7.2 Off-shell, and on the Hessian, it bites hard — and that is exactly where LA-11 declined to look

The mass-matrix question is the pencil `det(H − λB)`. Exact 2×2 discriminants
over the integers:

```text
B definite   :  disc = (a-c)^2 + 4b^2   >= 0 on all 729 integer Hessians
B indefinite :  disc = (a+c)^2 - 4b^2   NEGATIVE on a large exact witness set
sharpest     :  H = [[0,1],[1,0]], B = diag(1,-1)  ->  lambda^2 + 1,
                roots +-i, no real root
```

So an indefinite pairing admits **complex** normal-mode frequencies. This is not
new — `W183` found the same opposite-Krein-sign collision above an exceptional
point, and `W178` computed one — but its placement is: **positivity of `B_s` is
exactly the condition that the mass matrix has a real spectrum.**

That is a correction to LA-11 §4. LA-11 excluded **class H** — the ten rows
demanding a Hessian, mass matrix, eigenvalue or spectrum — from the declared-
condition set, on the ground that they sit downstream of class S. For the
*first* variation that exclusion is right, and §7.1 strengthens it: raising is
inert on-shell, so class S genuinely does not need this row. But the trigger's
**positivity** clause is a condition on the *second* variation, and it binds
class H and nothing else. **The rows that most need `LT-GR6b`'s hardest clause
are precisely the ten rows it declined to touch.** If the row is filed as
written, its condition is declared on the rows that do not need it and withheld
from the rows that do.

---

## 8. WHAT MOVES, AND UNDER WHAT CONDITION

**Zero rows advance.** No verdict, reason kind, priority, canon, `CURRENT-STATE`
or public posture moves. The sequential ledger is untouched.

What a canonical owner can do with this:

| row / object | movement available | condition |
|---|---|---|
| `LT-GR6b` (proposed, not yet filed) | **`distance` must be repaired before filing.** Strike the `at subscript W the composite provably does not exist` clause or re-scope it to `Lie(𝒲)`; strike the `Missing is the datum that turns the first variation into an equation` opening; collapse leg (b) to a citation of `SC-GEO-07` plus the 2026-08-08 globality gate; move the head demand to **positivity** | none — this is a defect repair, not a result |
| `LT-GR6b`'s `revival_trigger` | replace *positive on the physical quotient* with *definite on the transverse sector, at a named Cartan involution on each factor* | the base sign has no selector (`2026-08-08`), so *positive* is convention-dependent and *definite* is not |
| the 14-row declared-condition set (LA-11 §4) | **class H should be added and class S re-examined**, on §7 | §7.1's on-shell inertness and §7.2's pencil result |
| `LT-GR1`, `LT-GR6`, `LT-SM3b` (OT-2's three-row bucket) | the bucket **survives**, with its blocking datum re-typed from `base duality` to `fibre positivity at a named subscript` | leg (b) is supplied; leg (a) remains `REDUCTION_EXTERNAL` |
| `LT-GR5`'s `PHYSICAL_DIFFEO_DENSITY_HODGE_OBSERVATION_LOCAL_EXACT` | may be read as supplying the base duality **locally**, which it does | globality is the separate, already-filed line-field condition — not a grade upgrade |

**The one thing that would be a real advance and is not here:** the **moving**
pairing, with the six-item burden `k77-wave2-two-connection-shifted-...` §6
already wrote down. §7 narrows where that burden bites (second variation,
off-shell terms, BV/symplectic structure) and shows it does not reach the Euler
system. Narrowing a burden is not discharging it.

---

## 9. HOSTILE REVIEW — run on my own result

**H1. "You proved a tautology and dressed it as a construction."** The strongest
form: *a section of a metric bundle is a metric — this is a definition unfolding,
and the whole of §3 is `N ⊗ κ`, which any graduate student writes in one line.*
**Largely conceded, and it is the correct reading of §3 and §4.** The
construction is trivial once you notice which object is being asked for. What is
not trivial is §2 (two counts that differ, computed), §5 (an exact
impossibility with three firing controls), and §7.2 (a placement that inverts a
filed classification). If the reader takes only one thing, it should be §2, and
§2 is not a tautology — it is a defect in a row that was about to be filed.

**H2. "Your `SC-GEO-07` reading is a quote mined out of the Zorro passage."**
Fair pressure, and the register itself flags Zorro as `auxiliary` rather than
`hard-core`. So I checked it against a `hard-core` entry: `SC-GEO-03` scopes the
no-pre-existing-metric claim to **`Y`** in the same verbatim sentence
(*"Note that in this case, Y doesn't usually have a pre-existing metric"*), and
`SC-GEO-06` scopes the no-metric-choice claim to **`Y`** as well. Three
independent register entries, two hard-core, all scoping the denial to `Y`. The
reading survives. What would break it: a register entry scoping a denial to `X`.
There is none.

**H3. "You have re-imported a background metric on `X⁴`, which is exactly what
GU refuses."** **This is the strongest objection and it is half right.** `ℷ` is
not a background — it is a field, and §7 is the honest accounting of what that
costs: the pairing co-varies, so every second-variation, symplectic and BV
statement acquires moving terms. What I deny is that this makes the base duality
*absent*. A field-valued datum is a datum. The repository's own `LT-GR6` already
reads the Einstein tensor on the induced `g_s = s^*(g)`; if that is legitimate,
so is reading the pairing on `ℷ`. If it is not, then `LT-GR6` has a bigger
problem than `LT-GR6b`.

**H4. "You conflated `ℷ` with `s^*G`."** **Partly true, and it is a real seam,
now narrowed by concurrent work.** Two candidate base metrics exist: the
section's own value `ℷ` (zeroth order in `ds`) and the induced graph metric
`s^*G` (first order in `ds`). `W126` (2026-07-13) flagged this fork — *"the two
agree POINTWISE on the `dφ = 0` slice and differ only in the gradient sector —
flagged, not resolved."* BD-C (concurrent, Lens P6) proves the exact identity
`s^*(tautological horizontal metric) = s`, which closes the **horizontal**
block: on that block there is no fork at all. What remains open is the full
chimeric pullback, whose vertical `|ds|²` term is precisely `W126`'s gradient
sector. **Nothing in §2, §5 or §7 depends on which:** both candidates are
Lorentzian sections of the same bundle, so every inertia, every control and the
entire positivity theorem are identical. Only §7's moving terms differ, and
there I report the fork rather than pick.

**H5. "Your positivity theorem assumes the physical quotient is `ker(c) ⊗ ad`."**
Conceded as a restriction. I computed the trichotomy for a **codimension-1**
quotient by a covector. A quotient that is not of that form — a genuine BV
physical quotient with ghosts, or a non-split subspace — is not covered, and
`LT-SM8`'s state-space object is explicitly a different, downstream one. What
survives unconditionally is §5.1: **no quotient at all is positive on the full
`Λ¹ ⊗ ad P`**, so *some* quotient is mandatory, and §5.4 characterizes the
codimension-1 ones exactly.

**H6. "`d(d+1)/2` is a `𝔰𝔩(2,ℝ)` accident."** The count is a representation-
theoretic prediction written before computing (Lens P4) and it holds at
`d = 1,2,3,4`. It is not an accident of `d`. It **is** fixture-scoped in `𝔤`:
the argument needs `𝔤` simple, so that the invariant symmetric forms on `ad` are
one-dimensional and the antisymmetric ones vanish. For semisimple `𝔤` with `k`
simple factors the count is `k · d(d+1)/2`. **This does not touch the load-
bearing conclusion**, which is only that a **nondegenerate** member exists — and
that needs one nondegenerate `N` and one nondegenerate `κ`, both available.

**H7. "You are hiding behind `[R]` reproductions to claim you did not
overreach."** The reproductions are load-bearing in exactly one direction: they
establish that I did not move a number OT-1 or LA-11 computed. Every one of the
five reproduced facts is reproduced from structure constants and Betti numbers,
with nothing quoted. If a reader thinks the reproductions are too easy, the
correct response is that the theorem they reproduce is easy — which was Lens
P4's point about the dimension count and OT-1's own Lens P4 conceded it.

---

## 10. POSTFLIGHT — five lenses, run inline

The preflight lenses were chosen for *deciding what to build*. These are chosen
for *what the built object is*, and four of the five bind objects the preflight
did not touch.

### Lens Q1 — gauge theory / constraint analysis: is the `91` really the temporal sector?

§5.4 says the negative block at a compact fibre form is `1 × dim 𝔤 = 91`, the
timelike covector tensored with the adjoint. In ordinary gauge theory that block
is `A_0`, removed by the Gauss constraint, and its removal is not optional — it
is what makes the theory have a positive physical inner product at all. So the
`273/91` split is the transverse/temporal split, and the trigger's *"positive on
the physical quotient"* is, in this reading, a demand that GU produce a Gauss
law. **What the lens refuses to conclude:** that GU *has* one. The constraint
structure is `LT-SM8`'s and the BV/BFV descent is `AC-G1a`'s, both open. The
lens's honest output is that the arithmetic of the split is exactly the standard
one, which makes the demand legible, not discharged.

### Lens Q2 — Krein / PT-symmetric spectral theory: is demanding positivity even the right demand?

**No, and this is the sharpest thing in the postflight.**
`GEOMETER-VS-PHYSICS-OBJECTS.md` settles ghost clearance on the geometric side:
*keep-and-grade* via a Krein form, explicitly **not** a positive Hilbert space,
with the table's own warning that *"a team hunting spacetime SUSY concludes 'no
guardian'."* A trigger clause demanding a **positive** pairing is the physics-
default construction on a fork this program has already settled the other way.
The source-native form of the same demand is: a nondegenerate pairing **plus a
fundamental symmetry `J`** with `J² = 1` and `B(·, J·)` definite — which is
precisely the two Cartan involutions of §5.3, and which GU's own ghost
clearance already uses (`P` = the Cartan involution, `W173`, `W219`). **So the
row should demand a Krein structure, not positivity**, and if it does, the
demand is *already half-supplied* by the settled Krein posture rather than
externally priced twice.

### Lens Q3 — measure theory: is the density really free?

§6 shows `|det N|^{-1/2}` is determined by `N`. The lens presses on two gaps.
(i) **Weyl structures.** If the intended object had `D` independent of `N` —
a volume not tied to the metric — the conjunct would be two data, and §6 would
be answering the wrong question. Nothing in `LT-GR6b`'s text suggests that, and
GU's action uses the metric volume, so I read them as tied. (ii) **Non-orientable
`X⁴`.** The canon scope fixes `X⁴` oriented (`canon/w2-y14-spin-structure.md`),
so densities and top forms agree and the distinction does not arise here — but a
row that dropped the orientation hypothesis would need it back. Both are
recorded, neither is load-bearing at the declared scope.

### Lens Q4 — global analysis on infinite-dimensional manifolds: what did the fibrewise computation NOT reach?

Everything computed here is **fibrewise and finite-dimensional**. `Lie(𝒲)` is
infinite-dimensional, and the step to it is the one OT-1 stated and I inherit
verbatim: because `𝒢 = Γ(Ad P)` acts *pointwise by arbitrary functions*, any
invariant pairing **of integral type** `∫ B_x(·,·) dvol` needs `B_x`
`ad P_x`-invariant pointwise. **Pairings of some other type are not excluded and
are not claimed to be.** Three further things the fibrewise result does not
reach, stated so no reader takes them: (a) any **domain** statement — closure,
Fredholmness, self-adjointness of the raised operator; (b) the passage from a
pointwise definite form to a **positive Hilbert space**, which needs
completeness and a measure on an infinite-dimensional space; (c) any claim that
`B_s` is **bounded** or invertible as an operator between Sobolev completions.
`RB1` fenced exactly these (*"not a positive Riesz theorem; not a global
Hilbert-space isomorphism; not a boundedness, closure, or Fredholm result"*) and
that fence applies unchanged to BD-A.

### Lens Q5 — ledger metrology: does this route make the file more honest, and in which direction does the denominator move?

**More honest, and the move is unusual: this route makes the program look
BETTER, and that is the direction that should be distrusted.** LA-11's route
correctly noted that an honest addition moves ratios the unflattering way; this
route removes a demand, which moves them the flattering way. So the burden is
higher, and I discharge it by naming exactly what is removed and what is not.

```text
free_object_delta                 +1 (LA-11 proposed)  ->  net 0 if filed as repaired
rows advanced                      0
`SAME` rows touched                0
demands REMOVED       leg (b) as an unbuilt object  (it is SC-GEO-07's gimel)
                      the `at subscript W` impossibility on the module object
demands SHARPENED     `global`      -> the already-filed line-field / chi condition
                      `positive`    -> a Cartan involution on each factor
                      density+pairing -> one datum
demands ADDED         class H gains the positivity condition (LA-11 excluded it)
```

**Net: one un-owned object removed, one mis-scoped impossibility removed, three
demands sharpened, one demand added to ten rows that did not carry it.** The
flattering half is the removal, and its whole justification is a source quote
plus a definitional unfolding, both of which a reader can check in under a
minute. The unflattering half — class H — is the one the reader should weigh
more, because it is the one I had to look for.

---

## 11. CLAIM CEILING

**May be claimed, exactly and only:**

- For `𝔤` simple and `V = Λ¹ ⊗ ad` with `d = dim Λ¹`: the `𝒲`-invariant
  symmetric forms **on the module `V`** number `d(d+1)/2` and a nondegenerate
  one exists at every `d ∈ {1,2,3,4}`; the `V`-directions of `Lie(𝒲)` act
  trivially on `V`. Together with the reproduced algebra theorem (`d+1`, zero
  `V–V` block, max rank `2 dim 𝔤`), this establishes that a statement about
  forms on `Lie(𝒲)` does not transfer to forms on `V`.
- The construction `B_s = g_s^{-1} ⊗ κ` is symmetric, nondegenerate,
  `Γ(Ad P)`-equivariant and `𝒲`-invariant. **Novelty is NOT claimed:** `RB1` §5
  built the same map on `Y¹⁴` on 2026-07-30.
- The exact inertias `(49, 42)`, `(24, 21)`, `(189, 175)`, `(93, 87)`,
  `(273, 91)`, `(135, 45)`, and that the `364` figure is by direct congruence.
- That a Lorentzian base pairing admits no positive-definite composite for any
  nonzero fibre form; that an `Ad`-invariant definite fibre form exists iff the
  algebra is of compact type; that `𝔰𝔬(7,7)` is not of compact type; hence that
  equivariance at the source group and positivity are jointly unsatisfiable.
- The timelike / spacelike / null trichotomy on `ker(c) ⊗ ad`, and that its
  dimension `(d−1) dim 𝔤 = 273` is OT-1's unpaired remainder.
- That a nondegenerate `Λ¹` pairing determines the density.
- That on-shell the section-dependence of the pairing drops out of
  `D(B^{-1}E)`, and that an indefinite pairing admits complex pencil
  eigenvalues. **Novelty is NOT claimed for the phenomenon** (`W183`, `W178`).
- That a global section of `Met_Lor(X⁴)` is a line field and, for closed `X⁴`,
  `chi(X^4) = 0`. **Novelty is NOT claimed:** this is the 2026-08-08 gate.

**May NOT be claimed, and is not:**

- That GU's action is owned, that any quantity is action-owned, or that OT-1's
  ownership predicate is discharged. `O4` still needs a **cross-block** pairing,
  which remains impossible at `𝒲`.
- That the moving pairing has been built. It has not; §7 only bounds where it
  bites.
- That the base duality being supplied advances any row. Zero rows advance.
- That the Euler system is an equation in the sense `LT-GR6b` means. §7.1 says
  the *raising* is inert; it says nothing about the completeness, boundary
  terms, defect terms or ghost-freeness the Layer-0 typing of `stationary`
  demands.
- Any domain, boundedness, Fredholm, Hilbert-space or measure statement
  (Lens Q4).
- That `X⁴` does or does not satisfy `chi(X^4) = 0`. The source declines to
  assume it; so do I.
- That `LT-GR6b` should or should not be filed. Its `distance` has a defect;
  whether the repaired row belongs in `rows` or in `layer0_objects_compared`
  is the canonical owner's call, exactly as LA-11's own Lens Q3 said.

**Not laundered:** no grant is converted into a derivation; no row gains a
result; no `SAME` row is touched. The one asymmetry this route creates — that it
*removes* a demand — is named in Lens Q5 with its direction and its
justification.

---

## 12. REPRODUCE

```bash
cd /path/to/gu-formalization
_local/cas-venv/bin/python tests/channel-swings/joe_directed_bda_base_duality_construction.py
_local/cas-venv/bin/python tests/channel-swings/joe_directed_bda_base_duality_construction.py --selftest
```

Expected: `CERTIFICATE: 68/68 checks pass; no load-bearing float (swept).`,
exit 0, split `[C] 19  [E] 40  [R] 9`; then
`FAILURE-PATH SELFTEST: PASS (9/9 mutations drove exit 1)`. The probe resolves
this artifact relative to its own location and fails if the headline numbers are
not in it.
