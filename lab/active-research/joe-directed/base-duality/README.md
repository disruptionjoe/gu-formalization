---
title: "Base duality — delta index"
status: active_research
doc_type: overview
updated_at: "2026-08-15"
---

# Base duality — delta index

Artifacts of the base-duality wave. Three routes were run **concurrently and
independently** at one object: the "source-owned global base duality on `X^4`"
that `OT-1`, `OT-2` and `LA-11` had converged on as the blocking datum.

**Headline: it was never the obstruction.** The object is supplied by the
source, the question that framed it was malformed, and the real blocker sits
one layer down in the fibre.

Nothing here edits the ledger. Everything is a versionless delta for the
canonical owner; see `bd-disposition-packet-2026-08-15.md`.

> [!WARNING]
> **`BD-A/B/C/D` here are NOT `BD-1/BD-2`.** The numbered `BD-` gates belong to
> `baryon-number-and-proton-decay/`; the lettered ones are this wave. The
> joe-directed index already cites *"BD-2's `$` correction"*, meaning the baryon
> thread. Cite by namespace, not by prefix.

## Deltas

| id | route | outcome | certificate |
|---|---|---|---|
| `bd-a` | construct it | **Constructible.** The parent's hypothesis (that GU structurally denies it) is **refuted**. Found a **domain error** that inverts the answer, and relocated the obstruction to **fibre positivity** | 68/68, 9/9 mutations |
| `bd-b` | subscript lattice | **The fibre subscript is inert** — a two-line rank bound in which it never appears. 18 subscripts, verdict tracks one integer. The sweep question was malformed | 131/131, 14/14 mutations |
| `bd-c` | what the source supplies | **`MET(X^{1,3})` is the second argument of the source's own first action.** The density, the pairing and their base-side owner are printed in one equation | 87/87, 10/10 mutations |

## The three findings that matter

**1. The object is declared, not missing.** The source's first action has a
product domain whose second factor is the space of Lorentzian metrics on the
base, and its integrand carries the draft's own printed brace labels *Hodge
Star* and *Zorro Metric*. Separately the source declines a **canonical** metric
on `X^4` as purpose, not omission. Both are true at once: GU demotes the base
metric from background to field, and demotion is not denial. Leg (b) is the
observation.

**2. A domain error inverted the answer.** `OT-1`'s theorem is about invariant
forms on the **algebra**; `LT-GR6b`'s trigger asks for a pairing on the
**module**. At `d = 4` the algebra reading gives max rank 6 of 15, the module
reading 12 of 12. *Same group, opposite answers.* `BD-B` independently
confirmed the fork and found the pairing names **three** distinct objects.

**3. The bare-module obstruction is fibre definiteness; physical positivity is
source-open.** With a Lorentzian base the composite is never positive-definite for any
nonzero fibre form; an `Ad`-invariant definite fibre form requires compact
type, and the source group's trace form is `(49, 42)`. So equivariance at the
source group and definiteness are **jointly unsatisfiable** on that bare module.
The source says avoiding indefinite Killing-form signature was a mistake, but
also leaves the observed-sector handling unknown and invokes maximal-compact
experimental shielding. `CORRECTION IV-20260815` withdraws the old claim that
the source disavows positive physical cohomology.

`BD-D`'s 182-dimensional screen is free linearised Yang--Mills symbol
cohomology, not GU's derived physical quotient. Its exact inertias and
one-involution Krein majorant survive as comparator results; GU still owes its
Gauss law, interacting BRST/BV complex, domain and physical positivity.

## The relay defect, located to one word

`OT-1` wrote that leg (b) is *"the **observer's** metric geometry."* Correct.
One artifact later that had become *"a **source-owned** global base duality"*
to be **built**.

The word that dropped out is **observer** — and with it the fact that the
object is an argument the observer supplies, not a background the theory owes.
Both theorems are untouched; what failed was the relay.

The repository already knew: a source re-inspection filed **2026-08-09**
records the metric variation as explicit through `MET(X)`, six days before two
routes converged on calling that leg unsupplied.

## Why three routes, and not one

`BD-B` recorded this against its own result, and it is the method finding of
the wave:

> Had `BD-A` not fired, this file would have shipped a complete sweep of one of
> three objects while presenting it as *the* sweep.

`BD-B`'s enumeration was complete **within one domain**, and it did not know
there were three when it started. A single route would have produced a
confident, certified, correctly-computed answer to the wrong question.
Concurrent independent routes caught a scope error no single route could see.

`BD-A` likewise disclaimed priority to `BD-C` on the shared conclusion rather
than double-counting it, and `BD-B` disclaimed its base-leg computation as not
novel against `BD-C`. The convergence is a quorum of **two** independent
sources on that point, not three.

## Known limits, carried forward

- The 2021 draft PDF is **not in the checkout**. Every draft quotation in
  `bd-c` is register- or extraction-mediated — the same curated surface that
  carried a refuted clause into a mandatory method. Reading the draft's action
  section directly is the named next action.
- The in-repo source pack displays **no variation of the base metric**, so
  whether GU *derives* that argument or merely *declares* it is `SOURCE-SILENT`.
- `BD-B` located the three-domain fork but did **not** adjudicate it. What
  survives regardless: the fibre subscript is inert on all three domains.
- `BD-A`'s construction sections are restatement; a 2026-07-30 artifact already
  built the identical map and recorded that positivity is neither used nor
  available.
