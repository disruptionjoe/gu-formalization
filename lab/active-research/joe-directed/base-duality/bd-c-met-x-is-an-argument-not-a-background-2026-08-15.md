---
artifact_type: exploration
status: exploration
doc_type: source-determination
created: 2026-08-15
work_item: BD-C
channel: base_duality
route: SOURCE__WHAT_DOES_WEINSTEIN_ACTUALLY_SUPPLY_ON_X4
base_revision: 4c4b0e14
target_claim: "SC-GEO-03 (ASSERTS, hard-core, draft-2021 p.17): 'The work is set in the Einsteinian Observerse, Y = Met(X) the bundle of pointwise metrics, on which Y usually has no pre-existing metric until a section induces one.' Secondary registered claims read: SC-GEO-02 (p.16, eq 3.1, the Observerse triple), SC-GEO-04 (p.17, physics happens mostly on Y and is misread via metric pull-back), SC-GEO-06 (p.20, eq 3.13, spinors without a choice of metric on Y), SC-GEO-07 (p.21, eq 3.17, the Zorro construction: each observation is a CHOICE of metric gimel on X), SC-ACT-01 (p.44, eq 9.4, the first-order bosonic action with its g_gimel-subscripted pairing). Internal targets: LA-11's proposed row LT-GR6b and its revival_trigger clause 'a source-owned global base duality on the observed X^4'; OT-1's sentence 'Neither is supplied.' about its factor (b)."
target_claim_verdict: "SC-GEO-03 UPHELD AND SHARPENED -- the source's refusal of a canonical metric on X^4 is confirmed at five independent loci across three editions, and the reason is now exact (naturality: every declared reduction contains the scalings). LA-11's LT-GR6b trigger CORRECTED: its source horn is NOT a missing construction; it bifurcates on the word source-owned, and both horns are already decided (canonical = refused by design; declared-and-used = satisfied by eq 9.1 since 2021). OT-1 IS NOT CORRECTED AT ITS IDENTIFICATION -- its leg-(b) bullet already reads 'the observer's metric geometry', which is exactly right; what is corrected is the SCOPE of its headline 'Neither is supplied.', which is ledger-scoped in place and was relayed as source-scoped one artifact later, where LA-11's distance turned it into 'build (b) as a source-owned global object on X^4'. The frame regression is at the relay, not at either theorem; OT-1's clause-O4 no-go at subscript W is untouched and stands."
canon_verdict_change: none
priority_change: none
steering_effect: unchanged
canonical_effect: pending_integration
title: "BD-C: the source supplies the base duality on X^4 by NAME, as the second ARGUMENT of its own action -- `I^B_1 : G x MET(X^{1,3}) -> R` (draft p.43, eq 9.1), with the pairing subscripted by the Zorro metric `g_gimel` and a printed `Hodge Star` brace label (p.44, eq 9.4). It supplies NO canonical one, and the reason is exact rather than rhetorical: every structure the source declares on X^4 -- smooth structure, spin structure, 'one dimension of time' -- has a structure group containing the scalings `lambda*I`, and the space of `lambda*I`-invariant symmetric forms on `R^4` is ZERO-dimensional and the invariant density space is ZERO. The duality appears exactly at the `O(1,3)` reduction, which is exactly a point of `MET(X^{1,3})`, i.e. exactly the observation. Therefore LT-GR6b's revival trigger does not have one source horn but two, and neither is `MISSING_CONSTRUCTION`: read as `canonical`, the source REFUSES it by design commitment; read as `declared and used`, the source has ALREADY SATISFIED it since 2021 and LA-9 mode-TOP fires. The pullback route adds nothing: `s^*(tautological horizontal metric) = s`, exactly -- the base duality obtained by observation IS the observation."
grade: "SOURCE DETERMINATION with quote-and-locus discipline, plus EXACT fractions.Fraction linear algebra (no numpy, no float constructed anywhere; assert_no_float sweeps the result dict). 87/87 checks, exit 0, via tests/channel-swings/joe_directed_bdc_met_x_argument.py from the repository root under _local/cas-venv. Split: 35 [Q] quote-at-locus checks against three primary surfaces, 15 [R] reproductions of repository facts made BEFORE use, 11 [E] exact results, 26 [C] controls -- including two ANTI-DRIFT checks that require this artifact's printed certificate to equal the probe's actual output, so the record and the certificate cannot diverge. FAILURE PATH EXERCISED: ten planted mutations each drive exit 1 through --selftest. NOT: a physics derivation, a construction of any GU object, a ledger edit, a register edit, a claim that GU works or fails, a coefficient, a count, or a canon movement."
disposition: SOURCE_SUPPLIES_NO_CANONICAL_BASE_DUALITY_ON_X4_BY_EXACT_NATURALITY_OBSTRUCTION__BUT_DECLARES_ONE_AS_THE_SECOND_ARGUMENT_OF_ITS_OWN_FIRST_ACTION__LT_GR6b_BASE_TRIGGER_BIFURCATES__CANONICAL_HORN_REFUSED_BY_DESIGN__DECLARED_HORN_SATISFIED_SINCE_2021__GLOBAL_LORENTZ_SECTION_REMAINS_CONDITIONAL__OBSERVED_SECTOR_POSITIVITY_IS_SOURCE_OPEN_OR_UNSUPPLIED_NOT_DISAVOWED__PULLBACK_IDENTITY_S_STAR_TAU_EQUALS_S__OT1_LEG_B_NOT_SUPPLIED_CORRECTED__WITHDRAWN_CLAUSE_STILL_LIVE_IN_GEOMETER_VS_PHYSICS_OBJECTS
rows_proposed: []
rows_advanced: 0
free_object_delta: 0
depends_on:
  - lab/sources/source-claim-register.yaml
  - lab/sources/transcripts/portal-special-gu-first-look-2020-04-02.md
  - papers/drafts/Transcript into the impossible.md
  - lab/sources/weinstein-gu-primary-source-pack-2026-07-30.md
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - lab/sources/selected-k77-source-tangent-branch-source-reinspection-2026-08-09.md
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/ledger-advancement/la11-b9stat-is-a-base-duality-row-and-four-rows-name-it-as-a-subclause-2026-08-15.md
  - lab/active-research/joe-directed/ownership-theorem/ot1-the-ownership-predicate-and-the-pairing-obstruction-2026-08-15.md
  - lab/active-research/joe-directed/four-d-mode-decomposition/md1-form-leg-survives-ad-leg-is-untyped-2026-08-14.md
  - explorations/conditional-build/selected-k77-physical-section-faithfulness-gate-2026-08-08.md
scripts:
  - tests/channel-swings/joe_directed_bdc_met_x_argument.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`.
>
> This artifact makes **no** conventional-comparator claim. It reads the primary
> sources for what they supply on `X^4` and reports it. Section 4.4 distinguishes
> the source-confirmed ambient Killing-form posture from the separate, unresolved
> question of positivity on an observed physical quotient.

> [!CAUTION]
> **CORRECTION IV-20260815 — post-verification source semantics.** The original version inferred
> from the `[00:45:00]` Killing-form passage that the source *disavows positivity
> on the physical quotient*. That inference crossed two objects. The passage
> endorses ambient noncompact/indefinite structure and says the author does not
> know how nature handles it while experiments are shielded by a maximal compact
> subgroup. It neither constructs nor rejects a positive observed-sector
> quotient. Every former “source disavows physical positivity” conclusion below
> is therefore withdrawn; the honest source status is **open/unsupplied**.

# BD-C — the base duality is the second argument, not the missing datum

## Verdict first, unsoftened

LA-11's `LT-GR6b` demands, as its revival trigger, *"a source-owned global base
duality on the observed `X^4`, a density together with a nondegenerate `Λ¹`
pairing."* I went to the primary sources to decide whether that is satisfiable,
satisfied, or refused.

**It is all three, because "source-owned" is two different demands wearing one
word, and the source has already answered both.**

| horn | reading of "source-owned" | source verdict | LA-9 mode |
|---|---|---|---|
| **A** | *canonical* — produced from `X^4` and the source's declared inputs alone, with no further choice | **REFUSED**, and the refusal is the theory's stated purpose. Exact reason in §3: the invariant-form space is `0`-dimensional | mode **∅** fires |
| **B** | *declared and used by the source's own construction* | **ALREADY SATISFIED**, since the April 2021 draft. The metric on `X` is the **second argument of the first action**, `I^B_1 : 𝒢 × MET(X^{1,3}) → ℝ`, and the pairing that makes the action a number is subscripted `g_ℷ` with a printed **Zorro Metric** brace label | mode **⊤** fires |

Under horn A the trigger names something the source spent its whole
construction avoiding. Under horn B it names something the source wrote down
five years ago. **Under neither horn is the reason kind `MISSING_CONSTRUCTION`.**
Nothing is missing; a *distinction* is missing, and it is a distinction the
source itself draws — between the theory (on `Y¹⁴`, metric-free) and the
observation (a choice of `ℷ` on `X⁴`).

Three further findings, each certified below:

1. **The pullback route adds nothing.** `s^*(tautological horizontal metric) = s`,
   exactly. Whatever base duality observation delivers on `X^4` **is** the
   observation. There is no second, independent object to construct (§3.4).
2. **The trigger fuses an ambient posture with an unresolved physical demand.**
   Its last words are *"positive on the physical quotient."* At UCSD
   `[00:45:00]` Weinstein defends indefinite signature on the **Killing form**,
   then says he does not know how nature handles the indeterminacy and that a
   maximal compact subgroup shields experiment. That passage does not decide
   whether a positive observed-sector quotient exists or is required (§4.4).
3. **A correction of record with a live blast radius.** The clause the routing
   method withdrew on 2026-08-14 as *not source-attested and refuted* is still
   present, in almost the same words, at `GEOMETER-VS-PHYSICS-OBJECTS.md:25` —
   the file `AGENTS.md` names as step 1 of "Start here" (§6.1).

---

## 0. PREFLIGHT — six specialist lenses, run inline

Standing rule: N lenses means N sections written here, never N subagents.

### Lens P1 — re-derive the target: is BD-C still the right route today?

**What moved in the ranking since the brief was written: one thing, and it moves
BD-C up, not down.**

I read LA-11, OT-1, OT-2 and PHI-2 before touching a transcript. LA-11's own
**Lens Q2** already names the strongest objection to its own row — *"a reader
can say I have written a row for a piece of standard mathematical furniture and
dressed it as a discovery"* — and then says, in terms: *"I cannot refute this
from v0.258, and I will not pretend otherwise."* That sentence is a *routing
instruction*. It says the question is not decidable from the ledger and must be
decided somewhere else. The only "somewhere else" that can decide it is the
source. So BD-C is not a parallel route to LA-11; it is **the route LA-11
explicitly deferred to**, and it was ranked as a research option when it is
actually a *blocking* dependency of a row already proposed for canonical
adoption.

Second ranking move, and it is unflattering to the convergence: LA-11 and OT-1
converged on "the base duality nobody names," and OT-1 wrote *"Neither is
supplied."* But a source reinspection filed **six days earlier**
(`lab/sources/selected-k77-source-tangent-branch-source-reinspection-2026-08-09.md`)
already records, in a table, `metric variation | explicit through MET(X) |
must move the gimmel/Hodge/Phi packet and the derived connection`. **The
repository already knew the source's action takes the base metric as an
argument, and two routes converged on calling it unsupplied anyway.** That is
not a novelty failure by either route — neither claimed novelty for the source
reading — but it is exactly the failure mode of measuring the ledger instead of
the source, and it is why this route runs.

Third: PHI-2 and OT-2 are downstream. PHI-2's containment result and OT-2's
"cheapest pair" both take the base-duality object as given-and-missing. If the
object is *declared-and-varied* rather than *missing*, their arithmetic is
untouched but their **prose** inherits a wrong noun. That is a cheap fix and it
is named in §6, not performed here.

### Lens P2 — primary-source philology: what counts as an attribution here

Three editions are in play and they are **not** interchangeable:

| edition | in-repo surface | what it can settle |
|---|---|---|
| `draft-2021` (April 1 2021 v1.0, `sha256:3f28d742…`) | **PDF not in the checkout.** Verbatims reachable only through `lab/sources/source-claim-register.yaml` and two page-verified extraction files | equations, page loci, definitions |
| `portal-2020` (Portal special, 2020-04-02) | full transcript, timestamped per paragraph | design intent, the construction in the author's own narration |
| `ucsd-seminar-2025` (April 2025, UCSD, via Keating) | full transcript at `papers/drafts/Transcript into the impossible.md` | the current statement of inputs and posture |

**The discipline this imposes, and I follow it throughout: no draft sentence is
quoted here except through a register `verbatim:` field or a page-verified
extraction file, and each such quote is reported as *register-mediated*, not as
my own reading of the PDF.** I did not have the PDF. Saying otherwise would be
the exact attribution failure this route exists to avoid.

One consequence, recorded before use: **the four sentences I most want (the
draft's own words about a volume form, a density, or an integration measure on
`X^4`) do not exist in any in-repo surface.** Both draft-extraction files were
swept for `volume|density|measure|integral|dvol|orient` and §11–§12 returns
**zero hits**. I therefore cannot quote the draft on the measure, and I say so
rather than paraphrasing an absence into a presence.

### Lens P3 — differential geometry of `Met(X)`: what is a section, really

The lens's job is to stop a category error before it happens. `Y = Met(X)` is a
**fibre bundle over `X`** whose fibre at `x` is the set of nondegenerate
symmetric bilinear forms on `T_xX` of the declared signature. Therefore:

> A section `s : X → Met(X)` **is** a metric on `X`. Not "induces one",
> not "determines one up to something". Is one. The words *section* and *metric
> on `X`* denote the same object.

**The lens's prediction, written before reading the draft extraction: the source
will not use two words for this, and if it does use two, they will be
definitionally identified rather than related by a construction.** §2.3
confirms it at the sharpest possible place — draft eq (11.4) calls `ℷ` *"the
metric as an embedding `ℷ : X → Y`"*, one phrase, both nouns.

The immediate corollary, which is the load-bearing move of this whole artifact:
**any question of the form "does the section induce a base duality on `X^4`?"
is malformed.** It is asking whether a metric induces a metric. The real
question — the one §3.4 answers exactly — is whether there is any *other* route
from source-owned data to a duality on `X^4`. There is not.

### Lens P4 — gauge theory: which pairing does a variational problem actually need

A first variation `δI` is a linear functional on the tangent space of field
space. To be *written down at all* the action needs a **density on the
integration domain**; to be *read as a tensor equation* the Euler covector needs
a **duality on the field bundle**. These are two different demands and
`LT-GR6b` fuses them.

**The lens's prediction: the source will supply both, on `Y¹⁴`, and the base
object will enter only as the parameter that produces them.** §2.4 confirms the
supply: the register records `T_ω ∈ Ω¹(Y, ad P)`, a `*` the draft's own brace
labels call *Hodge Star*, and an inner product subscripted `⟨·,·⟩_{g_ℷ}`. So
the pairing `LT-GR6b` calls missing is **written in the source's equation
(9.4)**, and its `X^4` content is exactly `ℷ`. *(That the `*` is specifically
the `Y`-Hodge star follows from `T_ω` being a `Y`-form; I mark that step
`RECONSTRUCTED` — the draft's brace label says `Hodge Star`, not which
manifold's.)*

The lens also flags the one place `LT-GR6b` is right and stays right: the
composite it demands is `Γ(Ad P)`-equivariant on `Λ¹ ⊗ ad P`, and at subscript
`𝒲` OT-1's exact no-go stands. Nothing here touches that. What this route
touches is the *base* leg, and only the base leg.

### Lens P5 — history-of-physics reading of stated intent

The lens asks what problem the author says he is solving, because a
construction's *purpose* constrains what it is allowed to supply. Weinstein
states the purpose three times in one lecture and it is always the same: the
metric has been *"assigned far too many responsibilities"* and the theory's job
is to take them away. **The lens's prediction: a volume form will be named
explicitly in that list of responsibilities, because it is the least glamorous
one and therefore the one a rhetorician would omit unless he meant it.** It is
named first (§2.1, `[00:53:11]`).

The reason this matters for `LT-GR6b`: a row demanding a **canonical
background** density on bare `X^4` would re-assign the exact duty the source's
stated motivation is to strip. But the source's own observed action is allowed
to depend on the chosen metric argument. This history-of-physics reading says
nothing by itself about positivity after observation or reduction.

### Lens P6 — epistemics of attribution: how this route fails

The characteristic failure of a source-determination route is **quoting a
sentence that says something narrower than the claim it is used for.** Two
specific traps here, both of which I hit and had to back out of:

1. **"the lousy foundation of the space of all metrics"** (UCSD `[00:12:15]`)
   reads, on a fast pass, as Weinstein disparaging `Met(X)` — which would be
   incoherent, since `Met(X)` is his own `Y`. It is not: he immediately says
   *"That's an infinite dimensional badly behaved function space."* The target
   is the infinite-dimensional space of metric **fields**, i.e. GR's
   configuration space. Portal `[01:10:36]` draws the same fence in advance:
   *"not at a field level, at a pointwise tensorial level."* **Two objects, one
   name.** A route that missed this would have manufactured a source
   contradiction out of a homonym.
2. **"It's got a metric on the 4"** (Portal `[01:12:17]`) reads as a metric on
   `X^4`. It is not. Portal `[01:11:22]` is explicit that the object sits on
   `π*` of the cotangent bundle of `X` — i.e. over `Y`, not over `X` (§2.2).
   This is the single most attractive misquotation available on this route and
   it would have produced a *false positive* — "the source does supply a base
   duality after all."

Both traps are recorded here rather than silently avoided, because the second
one is precisely the shape of the finding, and a reader is entitled to see that
I nearly got it backwards.

---

## 1. THE COMPUTATION — 87/87, exit 0, zero floats, failure path exercised

`tests/channel-swings/joe_directed_bdc_met_x_argument.py`, from the repository
root under `_local/cas-venv`.

```text
CERTIFICATE: 87/87 checks pass; no load-bearing float (swept).
split  [C] 26  [E] 11  [Q] 35  [R] 15
```

The last two `[C]` checks are **anti-drift**: they require the two lines printed
immediately above to equal the probe's own totals. The record cannot drift from
the certificate without failing the run.

**Failure path**, ten planted mutations, each must drive exit 1:

```text
_local/cas-venv/bin/python tests/channel-swings/joe_directed_bdc_met_x_argument.py --selftest

  mutation invariant_dim         exit 1  OK   (claim a nonzero GL(4)-invariant form exists)
  mutation taut_pullback         exit 1  OK   (deny s^*(tautological) = s)
  mutation quote_line            exit 1  OK   (shift three quote line numbers by one)
  mutation o13_contains_2I       exit 1  OK   (claim 2I lies in O(1,3))
  mutation withdrawn_absent      exit 1  OK   (claim the withdrawn clause is gone)
  mutation ledger_sha            exit 1  OK   (corrupt the v0.258 sha256)
  mutation trigger_text          exit 1  OK   (corrupt the LT-GR6b trigger string)
  mutation action_domain         exit 1  OK   (misstate the domain as MET(X^{9,9}))
  mutation reduction_escape      exit 1  OK   (claim 2I fails to stabilise the time line)
  mutation classification_count  exit 1  OK   (zero out the SOURCE-CONFIRMED census)

FAILURE-PATH SELFTEST: PASS (10/10 mutations drove exit 1)
```

Repository facts are **reproduced before being used** (`[R]`): the v0.258
`sha256 = 540b50e386073c0f43da4e8d5a8ffdaf06fd243c6612622d7daf187c0a725047`;
`LT-GR6b` is confirmed **absent** from the sequential ledger (it is a proposal
and this route does not edit it); LA-11's trigger string and `distance`
sentence are read **out of LA-11's own artifact**; OT-1's `Neither is
supplied.` and its `O4 = OWNED_G` line are read out of OT-1's; MD-1's
contraction formula and the banked `S^4` Lorentz-section obstruction are read
out of theirs.

---

## 2. WHAT THE SOURCE SUPPLIES ON `X^4` — the four questions, answered with loci

Every quotation below carries its file, its line and its timestamp or page, and
every one is checked mechanically by the probe at that exact locus. A wrong
line number fails the run.

### 2.1 Q1 — is there a metric on `X^4`? No, and the absence is the thesis

**`SOURCE-CONFIRMED`.** The refusal is stated at five independent loci across
three editions, and it is stated as *purpose*, not as a limitation.

> *"So, one thing we can do is to take a manifold `X^d` as the starting point
> and see if we can create an entire universe from no other data—**not even with
> a metric**. So, since we don't choose a metric, what we instead do is to work
> over the space of all possible point-wise metrics."*
> — Portal special, `[02:18:03]` (`lab/sources/transcripts/portal-special-gu-first-look-2020-04-02.md:445`)

> *"a four-dimensional **proto-spacetime—not a spacetime, but a proto-spacetime
> with no metric**—would give rise to a 14-dimensional observerse portion called
> `Y`."*
> — Portal special, `[02:19:09]` (`:447`)

> *"we've decided to remain restricted to anything we can generate only from
> `X^d`, in this case `X^4`. So we generated `Y^14` from `X^4`, and then we
> generated chimeric tangent bundles on top of that. … and **we have not made
> any other choices**."*
> — Portal special, `[02:30:09]` (`:481`)

> *"`Y 14` is going to be the space of point wise Lorentzian metrics on an
> **`x four` that has not yet become spacetime**. So imagine that you're given
> spacetime … but you're going to use what mathematicians would call forgetful
> functor, **forget the metric**, at least initially."*
> — UCSD 2025, `[00:04:08]` (`papers/drafts/Transcript into the impossible.md:23`)

> *"It has an automatic metric. **You haven't chosen a metric.**"*
> — UCSD 2025, `[00:48:49]` (`:170`)

And in the draft, register-mediated (`SC-GEO-03`, `draft-2021` p.17):

> *"In this work we will consider the strongest assumption beyond those of
> Einstein by working within the Einsteinian Observerse to search for new
> physics. **Note that in this case, `Y` doesn't usually have a pre-existing
> metric.**"*

Note the polarity carefully, because the register's own paraphrase is looser
than its verbatim: the verbatim says `Y` has no pre-existing metric. That `X`
has none is not said in *that* sentence; it is said in the four spoken loci
above and is structurally forced by `Y = Met(X)` — a bundle of *all* pointwise
metrics is not a bundle you build if you have one.

**The disavowal fence.** The Kaluza–Klein reading is excluded by the source in
terms, and I quote it in its **primary** form rather than the repository's
normalisation:

> *"The `x four` is contained here in this `y 14` that is endogenous. It doesn't
> it's not extra dimensions. It's not Kaluza Klein. The space that is four
> dimensional births its own 14 dimensional ambient space."*
> — UCSD 2025, `[00:06:32]` (`:29`)

Nothing in this artifact reduces, compactifies, or descends along a fibre.

### 2.2 Q2 — a volume form / density / orientation on `X^4`, metric-free?

**`SOURCE-CONFIRMED` negative, and it is stronger than a silence: the source
names the volume form as a duty it is trying to remove.**

> *"So the question is how do we liberate the definition? How do we get the
> metric out from its responsibilities? It's been assigned far too many
> responsibilities. **It is responsible for a volume form, for differential
> operators, it's responsible for measurement, it's responsible for being a
> dynamical field**, part of the field content of the system."*
> — Portal special, `[00:53:11]` (`:137`)

The volume form is item **one** in the list. And the source never re-assigns it
to a new owner on `X^4`. What it does instead is move the whole apparatus
upstairs:

> *"It turns out that if this is `X^4`, and this is this particular endogenous
> choice of `U^14`, we have a 10-dimensional metric along the fibers. So we have
> a `g^10_{μν}`. **Further, for every point in the fibers, we get a metric
> downstairs on the base space. So if we pull back the cotangent bundle, we get
> a metric `g^4_{μν}` on `π*` of the cotangent bundle of `X`.**"*
> — Portal special, `[01:11:22]` (`:185`)

**This is the sentence that decides Q4, and it is the sentence a fast reader
misquotes.** The metric is on `π*(T*X)` — the pullback of the cotangent bundle
**to `U^14`** — and it exists *"for every point in the fibers."* It is a metric
on a bundle over `Y`, not a metric on `X`. Immediately after:

> *"the chimeric bundle is going to be the vertical tangent space of 10
> dimensions to `U`, direct sum the cotangent space, which we're going to call
> horizontal, to `U`. And the great thing about the chimeric bundle is that it
> has an a priori metric. It's **got a metric on the 4, a metric on the 10**,
> and we can always decide that the two of them are naturally perpendicular to
> each other."*
> — Portal special, `[01:12:17]` (`:187`)

"A metric on the 4" is the horizontal block of a bundle over `U^14`. It is not
a metric on `X^4`.

**On orientation specifically.** The most the source declares is recoverable
from its own input inventory, stated at UCSD `[00:49:16]` (`:173`):

> *"it's also the entire universe **without making any choices**. … it's
> basically the result of **choosing four degrees of freedom, one dimension of
> time on those four degrees of freedom, and a spin structure**."*

A spin structure implies orientability, so an orientation is arguably in the
declared inputs. **An orientation is not a density** — it identifies densities
with top forms and supplies neither. §3.2 makes that exact.

**Reported absence, not paraphrased into presence.** I swept the page-verified
§11–§12 extraction (`gu-2021-draft-s11-s12-extraction-2026-08-03.md`) for
`volume|density|measure|integral|dvol|orient`: **zero hits** `[C]`. The §9
extraction *does* contain `density`, and the control that keeps this honest
checks that its occurrences are **repository commentary** — *"It does not supply
the missing density"* — and not quoted draft text `[C]`. So the sweep is not a
null routine, and it is a sweep over two extraction files, **not** a statement
about the PDF. The draft's own words on an integration measure are **not in this
checkout**, and I do not have the PDF `[C]`. This is a gap in what I can certify
and it is the one place a reader should demand the PDF before trusting a
stronger statement.

### 2.3 Q3 — does the section `s` (or `s^*`) induce a canonical metric or pairing on `X^4`?

**`SOURCE-CONFIRMED`, and the question is malformed in the source's own
vocabulary: the section IS the metric.**

The draft says it in one phrase. From the page-verified extraction of §11.1
(`lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md`), draft p.50,
eq (11.4), verbatim:

> *"To apply the above to our situation we recognize that `ζ` represents a
> spinor valued 1-form and `ν` a spinor on `Y` with `U` representing the
> Horizontal and `V` the Vertical normal bundle `N_ℷ` **to the metric as an
> embedding `ℷ : X → Y`**. (11.4)"*

*The metric as an embedding.* One noun phrase, both objects. And the Zorro
sentence, register-mediated (`SC-GEO-07`, `draft-2021` p.21, eq (3.17)):

> *"each observation of `Y` **via a choice of metric `ℷ` on `X`** actually
> induces a metric and connection on `Y` identifying the Topological spinors
> with the metric spinors."*

Read the direction of that arrow. It runs `X → Y`: **a choice on `X` induces
structure on `Y`.** It does not run `Y → X`. There is no source sentence in
which the ambient construction hands `X^4` a metric.

MD-1's contraction result is consistent and adds the one thing the source does
not: `(s^*ω)_μ = ω_μ + ω_{(ab)} ∂_μ g_{ab}`, surjective onto `T^*X`. **That is
a statement about how forms descend, not about how a duality descends.** A
surjection `Ω¹(Y) ↠ Ω¹(X)` does not transport an inner product; transporting
one requires a splitting, and the splitting is `ℷ`. `[R]`, reproduced from
`md1-form-leg-survives-ad-leg-is-untyped-2026-08-14.md` before use.

### 2.4 Q4 — a source-owned density on `X^4` that is NOT metric-derived?

**`SOURCE-CONFIRMED` negative on `X^4`; `SOURCE-CONFIRMED` positive on `Y^14`
and on the chimeric bundle — and the difference between those two sentences is
the entire finding.**

What the source owns without any choice:

- the tautological horizontal metric on `π*(T^*X)` **over `Y`** (Portal
  `[01:11:22]`, quoted above);
- the fibre metric `g^{10}` on the vertical bundle **over `Y`** (same locus);
- hence *"an a priori metric"* on the chimeric bundle `C = V ⊕ H^*`
  (Portal `[01:12:17]`; draft eq (3.10), `SC-GEO-05`), which is what buys
  spinors *"without making a choice of metric on `Y`"* (draft (3.13),
  `SC-GEO-06`).

What the source **does not** own, and says so:

> *"So the question is, what are we missing? And the answer is that we're
> **missing exactly the data of a connection**. So this bundle, chimeric `C` — we
> have `C` is equal to the tangent bundle of `U` up to a choice of a connection
> `θ`."*
> — Portal special, `[01:13:00]` (`:191`)

> *"It happens, however, that what is missing to turn the canonical chimeric
> bundle on `Y` into the tangent bundle of `Y` **is, in fact, a connection on
> the space `X`**."*
> — Portal special, `[02:23:30]` (`:459`)

So even the *ambient* density is not free: the tautological metric lives on `C`,
and turning it into a density on `TY` costs a connection on `X`. The
Shiab/tautological machinery therefore cannot be the missing base-duality owner —
it is downstream of a choice on `X`, not upstream of one.

**And now the positive half of Q4, which is the part nobody checked.** The
source's *first action* names the base metric as an argument. From the
repository's own primary source pack (`weinstein-gu-primary-source-pack-2026-07-30.md`,
draft §9.1, eq (9.1)):

```text
I^B_1 : 𝒢 × MET(X^{1,3}) ⟶ ℝ
```

and, register-mediated at `SC-ACT-01` (`draft-2021` p.44, eq (9.4)), verbatim:

```text
I^B_1(ω_Y, ℷ_X) = I^B_1((ε_Y, ϖ_Y), ℷ_X)
  = ⟨ T_ω , * ( ⊙_ω ( F_{B_ω} + ½ d_{B_ω}T_ω + ⅓[T_ω,T_ω] ) + κ₁/2 T_ω ) ⟩_{g_ℷ}
```

with the register recording the draft's own printed brace labels on (9.4):
*Shifted Torsion / **Hodge Star** / Einstein Ricci Shiab / Metric Curvature /
C-S Like Terms / **Zorro Metric***.

**Read the three things `LT-GR6b` says are missing, and find all three printed
in this one equation.** A density — the `⟨·,·⟩` that turns a top-degree object
into a number. A nondegenerate `Λ¹` pairing — the `*`, labelled *Hodge Star* by
the author. Their base-side owner — `ℷ_X`, the second argument, whose induced
object the author labels *Zorro Metric*. `LA-11` writes *"Build (b) as a
source-owned global object on `X^4`."* The source did not leave (b) to be built.
It put it in the signature.

---

## 3. WHY THERE IS NO CANONICAL ONE — exact, not rhetorical

The quotations establish *intent*. This section establishes *necessity*, so the
finding does not rest on reading an author's mood. All arithmetic is over
`fractions.Fraction`; no float is constructed anywhere.

### 3.1 The obstruction is the scalings, and it is one line

**Theorem (exact, `[E]`).** Let `V = ℝ⁴`. The space of symmetric bilinear forms
`B` on `V` satisfying `gᵀBg = B` for `g = 2·I` is **zero-dimensional**.

*Proof.* `(2I)ᵀ B (2I) = 4B`, so invariance forces `4B = B`, i.e. `3B = 0`,
i.e. `B = 0`. The probe computes this as an exact nullspace over `ℚ` in the
10-dimensional space `Sym²(V^*)` and returns `dim = 0`. ∎

**Corollary.** There is no `GL(4,ℝ)`-invariant metric on `ℝ⁴`, hence no metric
on `X^4` natural in the smooth structure alone.

### 3.2 The same argument kills every density, including the oriented one

A weight-one density transforms by `|det g|`. For `g = 2·I`, `|det g| = 16 ≠ 1`,
so an invariant density must vanish. `[E]`

**And the obstruction survives every reduction the source declares** — this is
the part that matters, because the source *does* declare structure on `X^4`:

| declared input (UCSD `[00:49:16]`) | structure-group reduction | does it contain `2·I`? | duality? |
|---|---|---|---|
| four degrees of freedom | `GL(4,ℝ)` | yes | **none** |
| a spin structure (⇒ orientable) | `GL⁺(4,ℝ)`, `det = 16 > 0` | yes | **none** |
| one dimension of time | stabiliser of a line `ℓ ⊂ TX` | yes (`2·I` fixes every line) | **none** |
| — *not declared* — | `O(1,3)` | **no**: `λ·I ∈ O(1,3) ⟺ λ² = 1` | **yes** |

All four rows are checked exactly by the probe. The third column is the whole
argument: **every structure the source puts on `X^4` has a structure group
containing the scalings, and the scalings are exactly what kills the invariant
form and the invariant density.** The duality appears in the fourth row and
only there — and the fourth row is not a structure the source declares. It is
what a point of `MET(X^{1,3})` supplies.

**Controls that must fire, and do.** `η = diag(1,−1,−1,−1)` is invariant under
`−I`, nondegenerate (`det η = −1`), of exact Sylvester signature `(1,3,0)`, and
its density factor under `−I` is exactly `1`. Under `−I` alone the invariant-form
space is the full `Sym²`, dimension `10` — so the no-go is driven by the
*scalings*, not by discrete elements, and the machinery is not returning zero
by construction. A Euclidean control returns `(4,0,0)`, so the signature routine
is not constant.

### 3.3 The fibre has no distinguished point

`diag(2,1,1,1)ᵀ · η · diag(2,1,1,1) ≠ η`, and the image still has signature
`(1,3,0)`. `[E]` So `GL(4,ℚ)` moves `η` inside the fibre of `Met(X)`: the fibre
carries a transitive action with no fixed point, and "pick the canonical metric
at `x`" is not a definable operation.

### 3.4 The pullback identity: `s^*τ = s`

Let `τ` be the tautological horizontal metric — at `y ∈ Met(X)_x`, `τ_y := y`
acting on `T_xX` (this is exactly Portal `[01:11:22]`, *"for every point in the
fibers, we get a metric downstairs on the base space"*). Let `s` be a section.
Then

```text
(s^* τ)_x = τ_{s(x)} = s(x).
```

**`[E]`, verified entrywise over `ℚ` on three fibre witnesses** — `η`, the
Euclidean `δ`, and an explicitly non-diagonal rational Lorentzian form of
verified signature `(1,3,0)` — with the non-vacuity control that `τ` is not
constant on the fibre (`η ≠ δ`).

**This is the sentence that closes the route.** Observation is the only map the
source provides from ambient data to `X^4`, and what it delivers as a base
duality is, identically, the observation itself. There is no second object to
construct, and `LT-GR6b`'s `distance` step *"build (b) as a source-owned global
object on `X^4`"* has no content beyond "choose `ℷ`" — which is step zero of
the source's own action, not step one of a construction.

---

## 4. WHAT THIS DOES TO `LT-GR6b`

### 4.1 The trigger, read against the source

LA-11's `revival_trigger`, read out of LA-11's artifact by the probe `[R]`:

> *"a source-owned global base duality on the observed `X^4`, a density together
> with a nondegenerate `Λ¹` pairing, composed with an `Ad`-invariant fibre form
> at a **NAMED** group subscript into a `Γ(Ad P)`-equivariant pairing on
> `Λ¹ ⊗ ad P` that is positive on the physical quotient"*

Clause by clause:

| clause | source verdict |
|---|---|
| *a density together with a nondegenerate `Λ¹` pairing* | **supplied**, by `g_ℷ` and the printed `Hodge Star`, eq (9.4) |
| *source-owned* | **bifurcates** — §4.2 |
| *global* | **conditional on the topology of `X`**, and the source knows it — §4.3 |
| *`Ad`-invariant fibre form at a NAMED subscript* | **available at subscript `G`**; OT-1 itself returns `O4 = OWNED_G` on the `𝔰𝔬(7,7)` trace form `[R]` |
| *positive on the physical quotient* | **source-open/unsupplied** — the source affirms ambient Killing-form indefiniteness but does not determine observed-sector positivity; §4.4 |

### 4.2 The bifurcation, and why neither horn is `MISSING_CONSTRUCTION`

**Horn A — "source-owned" = canonical.** Then §3 settles it: no such object
exists for *any* theory on a bare 4-manifold, and the source's design
commitment is to not have one. This is **LA-9 mode ∅**: the named set is empty
in the declared scope. Not empty by accident — empty by the theorem in §3.1 and
by the author's stated purpose in §2.1–2.2.

**Horn B — "source-owned" = named and used by the source's own construction.**
Then `MET(X^{1,3})` in eq (9.1) satisfies it, and has since 2021. This is
**LA-9 mode ⊤**: already satisfied. LA-11 ruled mode-⊤ `PASS` on the strength
of `LOCAL`-scoping and `_OPEN` suffixes **in v0.258 grade tokens** — a ledger
test, correctly executed against the ledger. Against the *source* the mode
flips, and LA-11's own Lens Q4 predicted exactly this as attack surface #2.

**Therefore the reason kind is wrong either way.** `MISSING_CONSTRUCTION`
asserts a thing that could be built and has not been. Under horn A it cannot be
built; under horn B it is built. The honest kinds are, respectively,
`EXTERNAL_DATUM` (the base metric is an observer input, priced exactly as
`AUDIT-noncompact-compact-reduction-EXTERNAL` prices the compact reduction) or —
if the canonical horn is what the row means — a *structural* kind naming a
design refusal, which the taxonomy's `NEW_KIND_REQUIRED__FORCED_FIT_FORBIDDEN`
rule would license. **`RECONSTRUCTED`: I am not the canonical owner and do not
choose between them. I am reporting that the current kind is unavailable under
both readings.**

### 4.3 The word *global* is the only genuinely open clause

`MET(X^{1,3})` is the source's own domain, and it can be **empty**. A global
Lorentzian metric on a closed `X^4` exists iff `χ(X) = 0`. `[R]`, reproduced
from `explorations/conditional-build/selected-k77-physical-section-faithfulness-gate-2026-08-08.md`:
*"a spin four-manifold does not automatically admit a global Lorentz metric:
`S^4` is spin and has Euler characteristic two."*

And the source is on record refusing the claim that the global section is
already there — `SC-META-54` (`DISAVOWS`, `toe-2025` `[01:18:06]`): *"There
isn't a global section."* The register grades that exchange semantically
uncertain, and I do not read a settled posture out of it in either direction.

**So the one clause of the trigger that is neither supplied nor refused is
`global`, and it is a topological hypothesis on `X`, not a construction.** A row
whose real content is "assume `χ(X) = 0`, or restrict to `X` admitting a
Lorentz section" is a very different row from one demanding a missing object.

### 4.4 Ambient Killing-form indefiniteness does not decide physical positivity

The trigger ends *"that is positive on the physical quotient."* At UCSD
`[00:45:00]` (`:155`):

> *"this spin 10 is not right. **We wasted the seventies work because we wanted
> to avoid indefinite signature on the killing form**, and I don't know what to
> do because we're in a maximally compact subgroup. We're shielded
> experimentally from understanding how nature handles the indeterminacy of the
> killing form."*

**`SOURCE-CONFIRMED`, but only for the ambient Killing-form posture.** This
supports the repository's rule that noncompact/Krein structure cannot be
rejected merely because the raw invariant form is indefinite. The next two
sentences matter equally: Weinstein says he does not know what to do, and that
experiment is shielded by a maximal compact subgroup. They leave open exactly
the question `LT-GR6b` asks at a later layer: whether an observed physical
quotient carries an adequate positive or definitized pairing.

The original version of this section called that later demand “disavowed by the
source.” **Withdrawn.** It conflated an ambient invariant form with a reduced
physical state space. The corrected source classification is:

- ambient noncompact/Killing-form indefiniteness: `SOURCE-CONFIRMED`;
- maximal-compact experimental shielding: `SOURCE-CONFIRMED` as a stated
  interpretation, not a constructed selector;
- a positive observed physical quotient: `SOURCE-OPEN/UNSUPPLIED` in the
  available material.

Thus positivity must not be bundled into the phrase *source-owned base
duality*. It is a distinct physicalization debt whose owner, quotient and
mechanism still have to be constructed.

### 4.5 Where the frame actually regressed — and it is NOT at OT-1's identification

Claim-indexed discipline requires naming which route said what. **OT-1 got the
object right.** Its leg-(b) bullet reads, verbatim `[R]`:

> *"**(b)** is the **observer's metric geometry** — the base structure that turns
> `Λ¹` into something contractible."*

*The observer's metric geometry.* That is exactly what the source supplies, and
exactly where the source supplies it. OT-1 identified the object correctly on
its first pass.

What OT-1 then wrote above that bullet is `"Neither is supplied."` — a
**ledger-scoped** sentence in source-sounding words, immediately followed by
*"it is the object LA-10 §3.3 reported that **zero v0.258 rows name**."* Read
in place, the scope is the ledger. Read out of place, it says the source does
not supply it.

**And it was read out of place one step later.** LA-11 carries the object
forward as *"a **source-owned** global base duality on the observed `X^4`"* and
instructs *"**Build** (b) as a source-owned global object on `X^4`."* Between
OT-1's bullet and LA-11's `distance`, "the observer's metric geometry" became
"an object to be built," and the word *observer* — the one word that would have
sent a reader to `MET(X^{1,3})` — dropped out.

So the correction is not that OT-1 mis-identified anything. It is that a
ledger-scoped absence was relayed as a source-scoped absence across one
artifact boundary, and the relay lost the noun that carried the answer. Both
routes' theorems are untouched.

---

## 5. THE DETERMINATION TABLE — every claim, classified

| # | claim | class |
|---|---|---|
| 1 | The source declares no metric on `X^4`; `Y = Met(X)` exists so that none is chosen | `SOURCE-CONFIRMED` |
| 2 | The source names *a volume form* as a metric responsibility it intends to remove | `SOURCE-CONFIRMED` |
| 3 | The source supplies no density/volume form on `X^4` by any other owner | `SOURCE-CONFIRMED` (absence, at every in-repo surface; the draft PDF is not in this checkout) |
| 4 | The tautological metric lives on `π*(T*X)` over `Y`, not on `T*X` over `X` | `SOURCE-CONFIRMED` |
| 5 | The chimeric bundle has an a priori metric; identifying `C ≅ TY` costs a connection on `X` | `SOURCE-CONFIRMED` |
| 6 | The section `ℷ` **is** a metric on `X` — *"the metric as an embedding `ℷ : X → Y`"* | `SOURCE-CONFIRMED` |
| 7 | Observation runs `X → Y`: a choice on `X` induces metric+connection on `Y` | `SOURCE-CONFIRMED` |
| 8 | `I^B_1 : 𝒢 × MET(X^{1,3}) → ℝ` — the base metric is the second argument | `SOURCE-CONFIRMED` |
| 9 | Eq (9.4) prints `Hodge Star` and `Zorro Metric` brace labels on a `g_ℷ`-subscripted pairing | `SOURCE-CONFIRMED` (register-mediated) |
| 10 | The source's declared inputs are: four degrees of freedom, one time dimension, a spin structure | `SOURCE-CONFIRMED` |
| 11 | The source affirms ambient Killing-form indefiniteness, reports maximal-compact experimental shielding, and leaves the physical resolution open | `SOURCE-CONFIRMED` for the quoted posture; observed-sector positivity remains `SOURCE-OPEN/UNSUPPLIED` |
| 12 | The source disavows Kaluza–Klein | `SOURCE-CONFIRMED` |
| 13 | No `GL(4,ℝ)`-invariant symmetric form or density on `ℝ⁴`; obstruction survives `GL⁺` and the line stabiliser; dies at `O(1,3)` | `REPOSITORY-DERIVED` (exact, this artifact) |
| 14 | `s^*(tautological horizontal metric) = s`, exactly | `REPOSITORY-DERIVED` (exact, this artifact) |
| 15 | `MET(X^{1,3})` can be empty; `S^4` is the witness | `REPOSITORY-DERIVED` (`[R]`, banked 2026-08-08) |
| 16 | `s^*` is a contraction, surjective onto `T*X` | `REPOSITORY-DERIVED` (`[R]`, MD-1) |
| 17 | `LT-GR6b`'s reason kind is unavailable under both readings of *source-owned* | `RECONSTRUCTED` — my inference from 1–14, not a source statement |
| 18 | The right replacement kind is `EXTERNAL_DATUM` or a new structural kind | `RECONSTRUCTED` — a proposal for the canonical owner, not a determination |
| 19 | The source's `X`/`Y` split maps onto "classical on `X`, quantum on `Y`" as a *design* rationale for keeping `X` metric-free | `SPECULATIVE` — UCSD `[00:04:08]` says the split; that it *explains* the metric-freedom is my reading |
| 20 | GU's variational problem is therefore of the same type as GR's (metric both field and duality-provider) and inherits neither more nor less circularity | `SPECULATIVE` — stated so a reader can attack it; see §6.2 |

Census, checked by the probe: `SOURCE-CONFIRMED` is the majority class and
strictly exceeds both `RECONSTRUCTED` and `SPECULATIVE`.

---

## 6. HOSTILE REVIEW, RUN INLINE AGAINST MY OWN ATTRIBUTION

### 6.1 Correction of record — the withdrawn clause is still live

`lab/methods/source-native-comparator-routing.md` withdrew, on 2026-08-14, the
clause *"…and vertical connection components may appear as four-dimensional
scalars after reduction,"* on two grounds: not source-attested, and refuted by
MD-1 and LA-8.

**`GEOMETER-VS-PHYSICS-OBJECTS.md:25` still carries it**, in the Higgs/VEV row,
as *"vertical components may become four-dimensional scalars after
observation/reduction."* `[C]`, asserted positively by the probe with its line
number.

This is not a cosmetic drift. `AGENTS.md` §"Start here" item 1 makes that file
the **first** thing every agent reads, and the routing method's own `[!CAUTION]`
block says the clause was recorded rather than deleted precisely because *"every
reader of it re-imported the refuted step."* The withdrawal reached the method
and not the mandatory table. **I am not authorised to edit that file on this
route and have not.** It is reported for the canonical owner.

A second, minor transcription note, also checked: the routing method renders the
KK disavowal capitalised and with curly apostrophes (*"It’s not extra
dimensions…"*); the primary transcript reads *"…it doesn't it's not extra
dimensions. It's not Kaluza Klein."* — lower case, straight apostrophes. Harmless
for meaning, but the repository copy is a normalisation, not the primary string.

### 6.2 Is each quote actually saying what I claim, in context? Four self-attacks

**Attack 1 — "`It's got a metric on the 4`" proves a base metric.** This is the
strongest attack on my central negative and I nearly made the error myself
(Lens P6). Answer: the immediately preceding sentence at `[01:11:22]` fixes the
carrier as `π*` of the cotangent bundle of `X`, and the sentence quoted is
describing the *chimeric bundle over `U`*, whose summands are named *vertical
tangent space to `U`* and *cotangent space … to `U`*. Both summands are bundles
over `U`. The reading survives.

**Attack 2 — "the lousy foundation of the space of all metrics" contradicts
`Y = Met(X)`.** Answer: he continues *"That's an infinite dimensional badly
behaved function space,"* and Portal `[01:10:36]` pre-empts the homonym —
*"not at a field level, at a pointwise tensorial level."* Two different objects.
I do **not** use `[00:12:15]` as evidence for anything about `Y`, and a reader
should not either.

**Attack 3 — I am over-reading `MET(X^{1,3})`, which may be notation for the
observerse rather than a variational argument.** This is the strongest attack on
my *positive* finding and it is partly right. What I can certify: `MET(X^{1,3})`
is the second factor of the action's declared domain (`[Q]`, source pack, draft
§9.1 eq 9.1); the action is written `I^B_1(ω_Y, ℷ_X)` with two arguments
(`[Q]`, `SC-ACT-01` verbatim); and the inner product is subscripted `g_ℷ`
(`[Q]`, same verbatim). What I **cannot** certify from this checkout: that the
draft anywhere *varies* `ℷ`. The displayed variation in the source pack is
`d/ds|₀ I^B_1(ε, ϖ + sα) = ⟨α, Υ^B_ω⟩` — a variation in `ϖ`, holding `ℷ` fixed.
The repository's own source reinspection of 2026-08-09 types metric variation as
*"explicit through `MET(X)`"*, but that is a repository reading of the domain,
not a quoted variation. **So my horn-B claim is exactly this and no more: the
base duality is a declared argument of the source's action. Whether the source
varies it is `SOURCE-SILENT` in what I can see.**

**Attack 4 — the whole thing is GR's situation and therefore no finding.** In
GR the metric is both the field and the supplier of the measure, and nobody
calls that a missing construction. Correct, and I record it as claim 20,
`SPECULATIVE`. But the consequence runs *against* `LT-GR6b`, not for it: if
GU's variational setup is type-identical to GR's, then a row demanding that GU
supply what GR also does not supply is a row about field theory in general, not
about GU. Either way `MISSING_CONSTRUCTION` does not survive.

### 6.3 What I did NOT establish, stated plainly

- I did **not** read the 2021 PDF. Every draft quotation here is
  register-mediated or extraction-mediated, and is labelled so.
- I did **not** find the draft's words on an integration measure, on `X` or on
  `Y`. Zero hits in both in-repo extractions.
- I did **not** show that GU's action is well-defined, convergent, or that its
  Euler equation exists. Nothing here advances any row.
- I did **not** touch OT-1's clause-`O4` no-go at subscript `𝒲`. It is untouched
  and it stands.
- I did **not** determine whether `LT-GR6b` should be rewritten, merged into
  `b1`, or routed to the Layer-0 register. That is the canonical owner's call,
  and LA-11's own Lens Q3 already flagged the `source_row` as the disputable
  part.

---

## 7. POSTFLIGHT — five lenses, run inline

### Lens Q1 — the strongest overclaim available here, refused

**"The source already has the base duality, so `LT-GR6b` is vacuous and OT-1's
ownership obstruction dissolves."** Refused, twice over. First, OT-1's `O4`
no-go is about `𝒲`-invariance of a pairing on `Lie(𝒲) = 𝔤 ⋉ (Λ¹ ⊗ ad)`; the
source's `⟨·,·⟩_{g_ℷ}` is not `𝒲`-invariant and was never claimed to be. The
theorem is untouched. Second, what dissolves is the *base leg's typing*, not
the row: the row's other content — the composite, the subscript discipline, the
14-row declared-condition set — is unaffected by this route.

**Second available inflation: "the source contradicts itself — it says no metric
on `X` and then puts `MET(X)` in the action."** Refused. The source's own
resolution is `SC-GEO-04` and the Zorro construction: the theory lives on `Y`,
and `ℷ` is how an *observer* reads it. That is a coherent position, and calling
it a contradiction would be exactly the "attack a version of GU Weinstein does
not claim" failure this repository has committed before.

### Lens Q2 — the strongest contrary reading, which I cannot refute

**A reader can say I have found a notational fact and inflated it into a
retyping.** The steelman: `MET(X^{1,3})` in a function's domain may be
bookkeeping — the author declaring what the action depends on — while the real
question `LT-GR6b` asks is whether GU can *derive* that dependence away. On that
reading nothing I found is news; the row is about derivation and I answered
about declaration.

**I cannot refute this, and Attack 3 above is why: I cannot show the draft varies
`ℷ`.** What I can say is that a row typed `MISSING_CONSTRUCTION` with a
`distance` reading *"build (b) as a source-owned global object on `X^4`"*
instructs a reader to construct an object the source has already named, and that
instruction is wrong whatever the deeper question turns out to be. Fixing the
instruction is cheap; deciding the deeper question needs the PDF and a variation
computation neither this route nor LA-11 ran.

### Lens Q3 — weakest seam of this route

**The seam is the missing PDF, and it is not repairable from inside this
checkout.** Six of my twelve `SOURCE-CONFIRMED` draft claims are
register-mediated. The register is a curated surface; the routing method's own
`[!CAUTION]` block is the proof that curated surfaces in this repository have
carried non-attested sentences into mandatory documents. I mitigated by
preferring spoken transcripts (which *are* in-repo and line-checkable) for every
load-bearing negative, and by using the register only for the draft-specific
positives. **But a reader who wants the horn-B finding at full strength should
open `Geometric_Unity-Draft-April-1st-2021.pdf` p.43–44 and read (9.1) and (9.4)
directly.** That is the single highest-value next action on this route.

**Second seam: my "absence" claims are sweeps over in-repo surfaces.** Zero hits
for `volume|density|measure` in §11–§12 is evidence about two extraction files,
not about the draft. I say "not in this checkout," never "not in the source."

### Lens Q4 — what a hostile reader should attack next

In order of expected yield:

1. **Attack 3 / Lens Q2 — the declaration-versus-variation gap.** Open the PDF
   at §9.1 and check whether `ℷ` is varied. If it is, horn B strengthens to
   "the source varies it too" and the retype is forced. If the draft never
   varies `ℷ`, the row's real content is "GU has not written its metric Euler
   equation," which is a *different* and much better row than the one proposed.
2. **The `global` clause (§4.3).** If someone can show the source restricts to
   `X` with `χ = 0`, the last open clause closes and the trigger is fully
   decided.
3. **The classification of claim 3 (absence of a density owner).** It is an
   absence over a corpus, the failure mode Lens P2 of LA-11 names: a synonym I
   did not think of. I swept `volume|density|measure|integral|dvol|orient`; a
   reader should extend the token set.
4. **Do not attack §3.** The naturality obstruction is three lines of exact
   rational algebra with an `O(1,3)` control that fires, a `−I` control showing
   the no-go is caused by scalings rather than by the machinery, and a Euclidean
   signature control. It will not move.

### Lens Q5 — does this change what to work on?

**Yes, in four specific places, and three of them make the program look worse,
which is the correct direction for an honest source reading.**

1. **`LT-GR6b` should not be adopted with `reason_kind: MISSING_CONSTRUCTION`.**
   Both readings of its own trigger word defeat that kind. This is a *blocking*
   input to a proposal already written, and it arrived before adoption rather
   than after — the cheapest possible moment.
2. **The 14 declared-condition amendments in LA-11 §4 should wait.** They append
   the sentence *"…is not well-posed before `LT-GR6b`, not merely unmet"* to
   eleven `NEEDS` rows and three `DIFFERS` rows. If the base leg is declared
   rather than missing, that sentence over-claims on all fourteen.
3. **OT-1's `"Neither is supplied."` needs one scope word** — *supplied* →
   *supplied by any v0.258 row*, or *derived*. Its leg-(b) **identification is
   already correct** (§4.5) and must not be touched. The word that needs
   restoring is downstream, in LA-11's `distance`: *"build (b) as a
   source-owned global object on `X^4`"* should read *"name the observation
   datum `ℷ` that supplies (b), and decide whether GU derives it or declares
   it."* Named, not performed: I am not editing other routes' artifacts.
4. **`GEOMETER-VS-PHYSICS-OBJECTS.md:25` should be reconciled with the routing
   method's withdrawal** (§6.1). Every agent reads that table first.

And the honest limit: **this route did not build anything.** It determined that
one leg of one proposed row is mistyped, and it did so by reading. If the
canonical owner concludes that the interesting question was always "does GU
derive its base metric away, or merely declare it?", then the ledger still has
no row for *that* — and BD-C has not written it either.

---

## 8. CLAIM CEILING

**May be claimed, exactly and only:**

- The twenty entries of §5 with their classifications, each `SOURCE-CONFIRMED`
  entry at the locus printed beside it in §2.
- The exact results of §3: zero-dimensional invariant-form space under scaling;
  zero invariant density; survival under `GL⁺` and the line stabiliser; failure
  at `O(1,3)`; `s^*τ = s` on three rational witnesses; the fibre has no fixed
  point. Controls as listed.
- The bifurcation of §4.2 and the clause-by-clause reading of §4.1, **as an
  analysis of `LT-GR6b`'s text against the source**, not as a ledger action.
- The corrections of record in §6.1 (withdrawn clause still live in
  `GEOMETER-VS-PHYSICS-OBJECTS.md`; KK-quote normalisation).
- That the repository's own 2026-08-09 source reinspection already recorded
  `metric variation | explicit through MET(X)`.

**May NOT be claimed, and is not:**

- That GU works, fails, owns its action, or does not. No ownership theorem is
  proved or refuted here.
- That the draft varies `ℷ`. That is `SOURCE-SILENT` in this checkout (§6.2,
  Attack 3).
- That `LT-GR6b` should be deleted, kept, merged or rewritten. Only that its
  current `reason_kind` is unavailable under both readings of its own trigger.
- That any row advances, any verdict moves, or any grade changes. Zero rows
  touched; the ledger, the register, `CURRENT-STATE.yaml`, `canon/` and every
  process gate are untouched by this route.
- Novelty for: the Zorro construction (source), `Met(X)` as the substrate
  (source), the `MET(X)` action domain (source pack 2026-07-30; reinspection
  2026-08-09), the `S^4` obstruction (2026-08-08), the contraction formula
  (MD-1), the `O4` obstruction (OT-1), or the row (LA-11). **What is new here is
  the source determination itself** — the exact naturality obstruction, the
  `s^*τ = s` identity, the bifurcation of *source-owned*, and the two
  corrections of record.

**Not laundered:** no grant is converted into a derivation; no row gains a
result; no `SAME` row is touched; nothing outside the two declared paths is
written.

---

## 9. REPRODUCE

```bash
cd /path/to/gu-formalization
_local/cas-venv/bin/python tests/channel-swings/joe_directed_bdc_met_x_argument.py
_local/cas-venv/bin/python tests/channel-swings/joe_directed_bdc_met_x_argument.py --selftest
```

Expected: `CERTIFICATE: 87/87 checks pass; no load-bearing float (swept).`,
exit 0, split `[C] 26  [E] 11  [Q] 35  [R] 15`; then
`FAILURE-PATH SELFTEST: PASS (10/10 mutations drove exit 1)`. The probe resolves
every primary surface relative to its own location and fails on a wrong line
number, a wrong page locus, a corrupted trigger string or a corrupted ledger
hash.
