---
title: "Consolidated disposition packet — joe-directed deltas, base revision a148ed80"
status: exploration
doc_type: overview
created: "2026-08-15"
channel: conditional_ledger_advancement
ledger_base: lab/process/conditional-physics-ledger-v0.258.json
base_revision: a148ed80
ledger_edit: "none -- versionless delta packet for the canonical owner"
target_claim: "NONE-NOT-A-KILL -- this packet adjudicates no source claim. It consolidates repository-internal corrections already certified in their own artifacts, each cited below."
canonical_effect: pending_integration
---

# Consolidated disposition packet

Everything the joe-directed channel produced on 2026-08-15 that requires a
**canonical-owner decision**, in one place, with the artifact and certificate
that established each item. The channel never edits the ledger; this is the
handoff.

> [!CAUTION]
> **CORRECTION IV-20260815.** The original packet said the source disavows
> positivity on the physical quotient and treated the free Yang--Mills symbol
> quotient as if it removed a GU-owned object. Both claims are withdrawn. The
> source affirms ambient Killing-form indefiniteness while leaving observed-
> sector positivity open/unsupplied; BD-D computes a comparator symbol
> cohomology, not GU's physical quotient.

Items are ordered by consequence. Nothing here is a grant laundered into a
derivation: where a result holds only under a condition, the condition is
stated as the row's condition.

---

## 1. `LT-GR6b` MUST NOT BE FILED AS WRITTEN — four independent defects

`LA-11` proposed a new `LAGRANGIAN` row. Three later gates each found a
separate defect in it. The row's *subject* survives; its **statement does not**.

| # | defect | established by |
|---|---|---|
| 1 | Its `distance` contains a **domain error**. `OT-1`'s theorem is about invariant forms on the **algebra**; the trigger asks for a pairing on the **module**. At `d = 4`: algebra gives max rank 6 of 15, module gives **12 of 12**. The clause *"at subscript `W` the composite provably does not exist"* is **false of the trigger's own object**, and a nondegenerate member exists at `d = 14`. | `bd-a`, 68/68 |
| 2 | The subscript clause is **malformed in principle**, not merely wrong. The fibre subscript is **inert** — an exact rank bound in which it never appears. Any subscript-indexed clause here is a category error. | `bd-b`, 131/131 |
| 3 | The final clause *"positive on the physical quotient"* is a **separate physicalization debt**, not a base-duality clause. The source confirms ambient Killing-form indefiniteness but leaves observed-sector positivity open/unsupplied. BD-D prices only an imported free Yang--Mills symbol quotient. | corrected `bd-c`; corrected `bd-d` |
| 4 | Its single `reason_kind` fuses distinct statuses: the action's metric-dependent pairing is declared; existence of a global Lorentz section is conditional; the fibre Cartan/maximal-compact selector is external; and GU's action-derived BRST/BV quotient is unbuilt. | corrected `bd-c`; corrected `bd-d` |

Two further corrections to the same artifact:

- Its mode-`∅` witness used a **Euclidean** base metric; the bundle is
  Lorentzian (`bd-a`).
- Its §4 row-class assignment is **exactly backwards**. Positivity is a
  second-variation condition, and an indefinite pairing admits complex pencil
  eigenvalues — so the **ten Hessian / mass-matrix / spectrum rows** are the
  ones that need this clause, and the **eleven action-zero rows** do not
  (`bd-a`).

**Recommended disposition.** Do not file as written. Replace the compound row
with separately typed debts:

1. **Formal action metric/pairing — declared.** `MET(X^{1,3})` is the action's
   second argument and `g_ℷ`/Hodge data supply the metric-dependent family. This
   does not establish metric variation or the full action.
2. **Global Lorentz section — conditional topology.** A section must exist on
   the chosen `X`; this is not a missing algebraic pairing.
3. **Fibre Cartan/maximal-compact selection — external/open.** A named
   involution yields a positive majorant, but the source does not construct the
   physical selector.
4. **Actual GU action/BRST/BV quotient — missing construction.** The 182D
   screen is a free Yang--Mills symbol comparator. GU still owes its Gauss law,
   interacting complex, quotient, domain and physical positivity.

No Hessian or spectrum row inherits BD-D's `n_κ = 0` condition without a typed
carrier/action/quotient bridge.

**Open and NOT settled here:** whether the object belongs in `rows` at all.
`CB-B §PRE-3` asserts `free_object_delta = 0`, so a reader holding that binding
places it in the `layer0_objects_compared` register instead. `LA-11` flagged
this itself. No arithmetic settles it.

---

## 2. The denominator has TWO independent movers

`LA-7`'s `LT-SM1` split and `LA-11`'s new row **each** move 82 → 83, and they
were produced independently without sight of each other. Applying both gives
**84**, not 83.

`LA-9` concluded exactly one denominator-mover existed; that conclusion was
correct **within its scan** and is superseded by `LA-11`, which was written
afterwards. Whoever integrates must not apply the two deltas as if either
alone were the whole move.

SM-disagreement therefore reads `19/84`, not `19/82`. **The program looks
worse, which is the correct sign for honest additions.**

---

## 3. Corrections to already-filed artifacts

| target | correction | source |
|---|---|---|
| `OT-1` | `dim so(6,4) = 45`, not 91. Its parenthetical alternate gives 135 and 585, not 273 and 1183. The `d >= 2` verdict is insensitive, but **273 must never be quoted without its algebra**. | `LA-11` |
| `OT-1` | Its **antisymmetric escape hatch is CLOSED**, and harder than predicted: the invariant antisymmetric space is **empty** at the named subscript for `d >= 2`, with parity killing it at every subscript at GU scale. `BD-B` predicted an identical lattice and was refuted by its own computation. | `bd-b` |
| `OT-1` | Its **theorem itself survives** a from-scratch re-derivation with **no correction**. | `bd-b` |
| `OT-2` | Its fibre leg is typed at the chimeric frame algebra, but the source's own definition builds the gauge group and module on a **reductive** structure group whose **Killing form is degenerate**. The leg remains available via the trace form; remainders there are lower bounds, not the exact numbers quoted for the simple case. | `bd-b` |
| `OT-2` | Its three-row blocked bucket **survives**, with the blocking datum **re-typed from base duality to fibre positivity**. `A_OWN` reachable-now goes 2 → 1. | `bd-a`, `ot-2` |
| `LA-10` | *"ZERO v0.258 rows name it"* is **false as written** — four rows name a base-duality datum as a modifier inside another head demand. Upheld as it matters: the head-deliverable count is genuinely zero. | `LA-11` |
| `LA-8` | Its finding that the soldering fork is **not** load-bearing was scoped to `RA-E2` at the settled form leg and **remains correct there**. The fork became **verdict-load-bearing for `AC-D1..D5`** only when the anomaly target lattice was extended. Not a contradiction; a scope change. | `PHI-2` |
| `PHI-1` | Its `rank(phi) <= 1` **does not survive** lattice extension — extended rank is 3, and rank 1 was an artefact of the spin-1/2 projection. Harmless: the target's own bound is 25. Its **zero-bit and containment results survive**, the former with a sharper corollary. | `PHI-2` |
| method | `GEOMETER-VS-PHYSICS-OBJECTS.md` carried the scalars clause withdrawn from the routing method on 2026-08-14. **Withdrawn in place 2026-08-15**; repository-wide sweep finds no other live copy. | `bd-c` |

---

## 4. Results that stand on their own

- **`HE-1` survives its own named kill.** The 144 is **complex** under
  `Spin(6,4)` (Frobenius–Schur 0), by two independent legs, one of which is
  real-form **blind** — so no real form of the complexified algebra can make it
  self-dual. `n_g -> n_g - 1` is real-form stable. It does **not** supply its
  own input: the live attack is the carrier, not the real form. `HE-2`,
  204/204 plus a separate 4/4 mutation harness.
- **The ledger's 82 rows resolve to ~32 formal degrees of freedom**, and the
  near-universal cut vertex is **real** — but its *identification* was broken
  and corrected. 32 is **vocabulary-relative**: stable under audit, unstable
  under refinement. `LA-4/5/6`, corrected by `LA-10`, 195/195.

---

## 5. What this packet does NOT do

It moves **no ledger row**. Across the whole day: one conditional advance
(`LA-2`) and one status move (`LA-7`). Every other gate returned zero verdicts
moved, and most returned it *correctly*.

The honest summary is that this channel is currently very good at producing
**precise accounts of why rows do not move**, which is measurement rather than
advancement. The corrections above are real and several would have propagated
into canon uncorrected, but a canonical owner reading this packet should not
mistake its length for movement.
