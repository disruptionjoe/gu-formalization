---
artifact_type: exploration
status: exploration
doc_type: construction_result
created: 2026-08-17
work_item: TR-1
channel: spectral-transport
wave: reverse-scaffold wave 2 (spectral-transport arc; executes UR-BIT2-TRANSPORT)
target_claim: "The upgrade-register item UR-BIT2-TRANSPORT, verbatim title (lab/process/upgrade-program-register.yaml:158): 'Spectral-transport fence: the signed count moves 21.8 units with ZERO crossings (gapped throughout) — any future bit-2 counting argument must address transport, not crossings'. Owner: joe-directed channels, 'fence first, theorem later' (:160). The measured phenomenon behind it is LD-A card 2 (LIVE-HIGH), whose operative sentences are adjudicated here at the real carrier: 'along the standard Dirac mass sweep the graded trace tr(Gamma P_<0) moves from 0 to -21.845865 with no zero crossings' (lda-sg4-bit2-type-and-transport-2026-08-17.md:766-768, measured at :303-316) and 'canon-class ∩ {[D,Gamma] = 0} = {0}' (lda:295). Secondary internal target, resolved as a side effect: LD-A's own declared residual 'whether the model's Gamma is R3's ambient-half grading X is UNTYPED' (lda:892-893)."
target_claim_verdict: "FENCE EXECUTED AT THE REAL CARRIER, AND SHARPENED INTO A PARITY DICHOTOMY — NOT A KILL; NOTHING IS RETRACTED AND NO CLAIM MOVES. At the four-corner declared total at D_7, every deformation direction either declared bosonic slot supplies carries a Z/4 centre class in {0, 2}, and that one integer decides BOTH capacities at once, in opposite directions. Class-2 directions (all odd-k eps-slot directions, including the source's own chirality-selective middle forms Lambda^7_±) are the ONLY directions that can give either package half a self-pairing (ST-1 A2, reproduced) — and they ANTICOMMUTE with the chirality grading, so along any such family the graded trace tr(Gamma P_<0) is FROZEN AT ZERO identically (exact linear algebra, machine-witnessed at 8.5e-16 on the carrier's own Cl(14)): the LD-A transport phenomenon is EXCLUDED there, and so is ever reading a ±1 from interior spectral asymmetry — for the odd class the only count object is the integer KERNEL INDEX, which only external/topological data can make nonzero (CANON.md:135/139's doorway). Class-0 directions (even-k eps-slot, including the Lambda^14 pseudoscalar) COMMUTE with the grading — exactly the algebra class of the model mass that moved 21.8 units with zero crossings — so transport is UNPROTECTED there (witnessed: 18.5 units moved on Cl(14) by the Gamma-aligned even direction); but class-0 content can ONLY cross-pair the two halves (a Dirac-type coupling) and can never select a half. NO DIRECTION HAS BOTH CAPACITIES. At the branched layer the split collapses further: every Lorentz-preserving VEV direction of every Lambda^k has 4d form degree p in {0,4} — EVEN — so NO declared-slot VEV direction is gamma_5-odd, and the 4d-Weyl-graded trace has no protected deformation direction at all; the LD-A model's (odd kinetic + even mass) algebra is the branched layer's generic shape, which resolves LD-A's UNTYPED residual: for the source-named middle-form deformation the model's Gamma cannot be the ambient grading (the middle form is ambient-ODD and the trace freezes) and reads correctly only at the 4d-Weyl face. The fence is stated as a five-item transport bill (TB-0..TB-5); the missing theorem's shape and its kill branches are named, not claimed. The generation count stays OPEN; SG4 bit 2 stays OPEN; the 2+1 stays subtractive; the 128 stays a placement obligation."
grade: "EXACT for everything load-bearing: Z/4 centre arithmetic (CS-1's layer, integer); Weyl dimension formula as exact Fractions over the 42 positive roots of D_7; Klimyk/Racah-Speiser with dimension saturation on doubled integer weights; exact multiset-subtraction identities for the spinor squares (remainder empty); the Racah alternating sum over W(D_2) for the Spin(4)-invariant vector [1,0,0,0,1]; the freeze lemma (pure linear algebra); explicit Cl(14) gamma matrices on C^128 as a second, independent instrument for every parity claim. MODEL-GRADE and typed as such: the N=24 lattice reproduction of LD-A's numbers (float64, binds the model), the Cl(14) toy kinetic term (the fiber algebra, not the Y14 operator), and the 18.5-unit motion witness (existence of motion, not a magnitude claim). 90/90 checks, exit 0; --selftest verifies the clean baseline FIRST then catches 12/12 machinery/reference mutations each via its targeted genuine [FAIL] line (0 crash-only, 0 missed, 0 incidental). NOT: an action, a vacuum, a scale, a spectrum, a reality map, a count, a claim-status movement, or any registry/sidecar/ledger/canon edit."
disposition: FENCE_EXECUTED_AND_SHARPENED__EVERY_DECLARED_SLOT_DIRECTION_HAS_CLASS_0_OR_2__CLASS_2_SELECTS_BUT_FREEZES_THE_TRACE__CLASS_0_MOVES_THE_TRACE_BUT_ONLY_CROSS_PAIRS__NO_DIRECTION_HAS_BOTH__BRANCHED_LAYER_HAS_NO_GAMMA5_ODD_VEV_DIRECTION_AT_ALL__ODD_CLASS_COUNT_IS_KERNEL_INDEX_ONLY_EXTERNAL_DATA_MOVES_IT__TRANSPORT_BILL_TB0_TO_TB5__THEOREM_SHAPE_AND_KILL_BRANCHES_NAMED_NOT_CLAIMED__NO_CLAIM_MOVES
canon_verdict_change: none
row_change: none
registry_change: none
steering_effect: unchanged
canonical_effect: pending_integration
scripts:
  - tests/channel-swings/joe_directed_tr1_transport_and_selection_are_opposite_parities.py
depends_on:
  - lab/process/upgrade-program-register.yaml
  - lab/active-research/joe-directed/lens-digs/lda-sg4-bit2-type-and-transport-2026-08-17.md
  - tests/channel-swings/joe_directed_lda_sg4_bit2_type_and_transport.py
  - tests/function-space-ext/dirac_spectral_flow_section.py
  - tests/function-space-ext/krein_spectral_flow_probe.py
  - CANON.md
  - VERIFICATION.md
  - lab/active-research/joe-directed/rs-corner/rsc1-unique-channel-lives-on-the-gamma-trace-2026-08-17.md
  - lab/active-research/joe-directed/bit1-price/b1p1-three-corner-support-prices-the-chiral-grant-2026-08-17.md
  - lab/active-research/joe-directed/rwall/rw1-zero-locus-steers-not-hosts-2026-08-17.md
  - lab/active-research/joe-directed/grading-bridge/gb1-the-bridge-is-one-angle-and-one-missing-arrow-2026-08-17.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/class-shift/cs1-first-order-shift-is-the-chirality-grading-2026-08-15.md
  - lab/methods/gu-base-categories.md
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - lab/sources/source-claim-register.yaml
  - lab/process/correction-registry.yaml
  - papers/drafts/Transcript into the impossible.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result. Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.
>
> The comparator legs here are (i) the repository's own faithful model for the
> `CANON.md:136` row (`tests/function-space-ext/dirac_spectral_flow_section.py`,
> re-run and pinned; its N=24 rebuild reproduces LD-A's measured numbers, which
> bind that model only), and (ii) a toy kinetic term on the carrier's own
> complexified Cl(14) fiber algebra (`C^128`), used to witness algebra-class
> facts, not spectra. "Mass", "chirality", "spectral flow", "Dirac-type",
> "Majorana-type" are comparator/repository vocabulary throughout — the source's
> own words are "mass", "decouples", "vial [Weyl] equations". The exact
> representation-theoretic statements (centre classes, Hom spaces, branchings,
> parities) are structural facts about complexified `D_7` on GU's own printed
> corners and declared slots, identical on both SIGNATURE-AMBIENT horns; they
> move no GU row. This artifact is ABOUT which grading and which deformation
> class can carry a count across the bit-2 transition — a semantic-boundary
> adjudication — hence the classification.

> [!NOTE]
> **Registry debt, declared rather than silently incurred (LD-A's precedent,
> lda:50-63).** `process_gates/source_native_comparator_routing_audit.py` was
> already red at HEAD before this artifact existed; TR-1 adds one unclassified
> path to the printed gap. The registry row is a transcription owed to the
> method owner (`lab/process/source-native-comparator-routing-registry.json` is
> outside TR-1's write scope; the gate's own comment forbids an auditor
> guessing). The entry, for the integrator:
>
> ```json
> { "path": "lab/active-research/joe-directed/spectral-transport/tr1-transport-and-selection-are-opposite-parities-2026-08-17.md",
>   "classification": "BRIDGE_OR_SEMANTIC_BOUNDARY" }
> ```
>
> The typed-carrier gate's pre-existing red (sibling `selected-k151`) and the
> k149 certificate-shape allowlist red are other lanes' debts, untouched.

## Typed objects

```gu-typed-objects
result:         the parity dichotomy at the four-corner declared total: every
                declared-slot deformation direction has Z/4 centre class 0 or
                2; class-2 directions (the only self-pairing-capable ones,
                including the selective middle forms) anticommute with the
                chirality grading and freeze tr(Gamma P_<0) at zero
                identically; class-0 directions commute with it (the
                grading-breaking transport class) and can only cross-pair the
                halves; no direction has both capacities; at the branched
                layer no Lorentz-preserving VEV direction is gamma_5-odd
carrier:        the four printed corners nu_± in Omega^0(S_±), zeta_± in
                Omega^1(S_±) at complexified D_7, with zeta_± = S_∓(64) (+)
                R^(±)(832) (RSC-1's landed split), and the declared bosonic
                slots eps in Omega^0(ad P), varpi in Omega^1(ad P) with
                ad P = End(Delta) = Sum_k Lambda^k V_14
                LAYER=ambient CHIRALITY=S-FULL-DIRAC
                # The result types deformation DIRECTIONS against gradings on
                # the declared total; it selects no half, no corner, no phase.
pairing:        NONE
# A parity/class result: the only pairing content consumed is the SYMMETRY
# TYPE of invariant bilinears (ST-1's Sym^2/Lambda^2 columns, reproduced for
# the spinor squares), used to type which pairings each class admits.  No
# bilinear is evaluated and no Krein realization is chosen; bare token per
# the FX-2 parser, gloss on these comment lines.
real_structure: SOURCE-SILENT and not needed: every exact statement is
                complexified D_7 centre/weight arithmetic, identical on both
                SIGNATURE-AMBIENT horns.  The freeze lemma is stated for any
                realization in which the graded trace is defined at all
                (self-adjoint D, unitary Hermitian Gamma) — the count object
                PRESUPPOSES that structure, so the conditional costs nothing.
grading:        THREE typed gradings, never bare: Gamma_W (the Z/4-class /
                package grading; W_+ = nu_+ (+) zeta_- class 3, W_- class 1);
                Gamma_slot (the 14D volume word, GB-1's N1; equals
                (-1)^p Gamma_W with p the form degree); gamma_5 x gamma_int
                (the branched D_2 x D_5 pair, RSC-1's reduced layer).  The
                eps-slot parities agree between Gamma_W and Gamma_slot; the
                varpi-slot parities FLIP — typed in section 3, and the fence
                item TB-0 exists because of exactly this.
action_owner:   repository-construction (the parity table, the freeze lemma,
                both instruments); the corners, slots and the middle-form
                direction are source-print (draft eq (9.16), (11.6), p.51);
                whether any insertion is ON is source-action, SG4 bit 2,
                Lane 1 — not decided here (CC-06)
target:         the scope of any future bit-2 counting argument: which
                (grading, deformation-class) pairs can host or move a signed
                count MAP-TYPE=not-a-map (an admissibility adjudication on
                classes, not a map between carriers)
```

```gu-typed-objects
result:         instrument leg: LD-A's model numbers reproduced and pinned
                (n_-(1.5) = -21.845865, min|spec| = 1.5, zero crossings; the
                sigma-basis class enumeration; the intersection identity
                canon-class cap {[D,Gamma]=0} = {0}); the freeze and the
                contrast witnessed on the carrier's own Cl(14) fiber algebra
                (odd family: max |trace| = 8.5e-16; Gamma-aligned even
                direction Lambda^14: trace moves 18.5 units); the kernel-index
                face (odd operator, unequal graded kernel: index 1, trace 0)
carrier:        (i) D(m) = sigma_1 (x) P + m M (x) I on C^2 (x) C^24, the
                periodic-lattice cross-chirality Krein-Dirac model of
                tests/function-space-ext/dirac_spectral_flow_section.py;
                (ii) explicit Cl(14) gammas on C^128 (Pauli Kronecker
                construction) with a generic one-gamma kinetic element — the
                FIBER algebra of the carrier, not the Y14 operator
                LAYER=toy CHIRALITY=S-FULL-DIRAC
pairing:        cross-chirality Krein form K = sigma_1 (x) I_24 ON=C^2 (x) C^24
                (model (i) only; used solely for the class-membership
                enumeration [R lda 2(iii)]; model (ii) uses no pairing)
real_structure: complex Hermitian float64; no quaternionic or real structure
                used or claimed; explicitly NOT the (9,5) real-form fork
grading:        (i) Gamma = sigma_3 (x) I_24 (the model's declared grading);
                (ii) Gamma_amb = i(g_1..g_14), gamma_5 = g_1..g_4, gamma_int =
                i(g_5..g_14), Hermitian involutions with Gamma_amb =
                gamma_5 . gamma_int, machine-verified.  HOMONYM-AMBIGUOUS
                which physical grading model (i)'s Gamma is — that ambiguity
                is LD-A's declared residual and is RESOLVED CONDITIONALLY in
                section 4.5: for the source-named middle-form deformation it
                cannot be the ambient grading and reads at the 4d-Weyl face
action_owner:   repository-construction (both instruments); the deformation
                directions typed against them are source-print (SC-CHI-02
                register:940; the middle form of ad P)
target:         the algebra class of the LD-A transport phenomenon — which
                gradings and directions realize (odd kinetic + even mass)
                MAP-TYPE=not-a-map
```

# TR-1 — transport and selection are opposite parities

**The register item, executed.** `UR-BIT2-TRANSPORT` (register:158): *"Spectral-transport
fence: the signed count moves 21.8 units with ZERO crossings (gapped throughout)
— any future bit-2 counting argument must address transport, not crossings"* —
owner *"joe-directed channels"*, activation *"fence first, theorem later"*. LD-A
measured the phenomenon on the repository's own model for the `CANON.md:136`
row; TR-1 executes the fence AT THE REAL CARRIER: the four-corner declared
total at `D_7`, with RSC-1's landed block structure, and the deformation family
the source itself supplies. The answer is sharper than the demand: at the real
carrier the fence is a **parity dichotomy** — one Z/4 integer per direction
decides both whether a direction can select chirality and whether it can move
the count, and it always decides them oppositely.

**The demand under the source's own sentence.** `Transcript:158`, verbatim:
*"And what you find is the fermionic extension gives you exactly three families
of chiral fermions if you have a decreased VEV in the total space taking a
Dirac equation into two vial equations because the mass is actually a
variable to your point."* The inference from the coupled total to the
decoupled chiral content IS a transport across the transition. The transport
bill of §5 is what makes any such inference admissible.

---

## 0. The source frame, carried (quoted, not paraphrased)

Compression re-imports default frames at every boundary; the operative
sentences travel verbatim with their IDs.

- **CC-06-CHIRALITY-VEV-CONDITIONAL** (`correction-registry.yaml:300-306`),
  superseded reading quoted: *"That the source has no stated
  effective-chirality mechanism, or that chirality can only come from an
  unbuilt mirror-gapping condensate -- when observed chirality is
  VEV-CONDITIONAL ('exactly three families of chiral fermions if you have a
  decreased VEV ... taking a Dirac equation into two [Weyl] equations',
  drafts L158 / ucsd 00:46:02) and the selector is SG4 bit 2, OPEN by
  design."* The register row it binds, `SC-CHI-01` (register:913-915),
  verbatim: *"Thus we assert that a non-chiral total theory splits at the
  emergent level into two separate chiral theories and that the one above the
  dashed line corresponds to matter in our world with the other sectors not
  labeled by F to the left and above the line are currently dark to us."* —
  conditioned in the same passage (extraction:137-138) on *"no vacuum
  expectation value pulling the various sub-fields of ϖ to values
  significantly above zero"*. The declared total is NON-CHIRAL; the split is
  emergent-level and VEV-conditional; **SG4 bit 2 is OPEN and nothing here
  closes it or assumes it closed.**
- **CC-05-SUBTRACTIVE-TWO-PLUS-ONE** (`correction-registry.yaml:264-269`),
  quoted: *"That three generations is an ADDITIVE target count a mechanism
  must produce -- when the source says it is 'really two plus one' with the
  third family an imposter 'for representation theoretic reasons' (drafts
  L119/L128/L131) and HE-1 makes the partition FORCED and SUBTRACTIVE
  (n_g -> n_g - 1, unlabelled, the distinguished family REMOVED)."* Nothing
  below treats three as an additive production target; no count is produced,
  hosted, or moved.
- **CC-08-DARK-PARTNER-OBLIGATION** (`correction-registry.yaml:364-368`),
  quoted: *"That the 128 remainder is an established DEFECT of the
  construction -- when the source NAMES dark sectors (Looking-Glass, dark
  spinorial, 'Rarita-Schwinger matter and more') and the remainder is a
  partner-placement / decoupling OBLIGATION, not an established defect."*
  The 128 appears below only through RSC-1's rank bound, as an obligation
  whose gapping endpoint is priced, never as a defect.

---

## 1. PREFLIGHT — six problem-matched lenses, declared before the work

- **L1 — Index/transport analyst (LD-A's L2, inherited and re-aimed).**
  Concern: the interesting question is never "does the number change" but
  "which grading defines the number, and does the family stay odd for THAT
  grading." Design answer: every trace statement below names its grading
  operator from a typed three-member list (§3), and the adjudication (§4) is
  a parity computation per (grading, direction) pair, not a single verdict.
  This lens predicted the grading split; it did not predict that ONE integer
  would decide it — that is the measured surprise.
- **L2 — Central-character arithmetician (CS-1's instrument).** Concern:
  parities are decidable by the centre before any weight computation, and a
  weight computation that disagrees with the centre is wrong. Design answer:
  every parity is computed TWICE — Z/4 class arithmetic (LEG 2) and explicit
  Cl(14) matrices (LEG 5), with the weight/Klimyk layer (LEG 3) supplying the
  Hom-space facts the classes only bound. No clause argues from class alone
  where a Hom space is claimed (RSC-1's Lens-2 binding, inherited).
- **L3 — Coverage archaeologist.** Concern: eight false-novelty claims in one
  session is the documented failure mode; ST-1's A2 already owns the
  class-partition half of my headline. Design answer: retrieval-first (§2)
  with the honest ratio printed; ST-1 A2 (st1:456), CS-1's class layer,
  RSC-1's split, GB-1's grading enumeration and LD-A's phenomenon are all
  consumed with [R] tags; the novelty claim is confined to the TRANSPORT
  reading (the parity-capacity dichotomy and the freeze application) and
  grep-certified as phrased (§2, LEG 7).
- **L4 — Layer-typist (gu-base-categories discipline).** Concern: the
  unforgivable regression is crediting the declared total (L1) with
  chirality; the freeze result is exactly the kind of sentence that could be
  misread as "the total is chiral and protected." Design answer: every
  section names its layer; the freeze is stated as a property of deformation
  FAMILIES on the non-chiral total, and §10 lists the misreadings it fences.
  LA3's corrected direction (base-categories:91: *"the hedge's decoupling
  holds AT small `varpi`"*) is consumed as corrected by B1P-1.
- **L5 — Source-fidelity reader.** Concern: verbatim fields over paraphrase;
  the hedge names ϖ (the 1-form slot) while the typed selective channel
  lives in the 0-form slot's middle-form direction — a slot mismatch a
  careless read would smooth over. Design answer: §3.2 types BOTH slots and
  flags the mismatch as a standing question for the register owner, with the
  parity map computed for each slot separately (they differ, and differently
  per grading — the TB-0 fence item exists because of this).
- **L6 — Hostile self-auditor (the import/deflate lens).** Concern: the
  seductive failure is inflating "ambient trace frozen" into "GU's count
  story is broken" (a kill) or "the model matches the branched layer" into
  "the 21.8 transports to GU" (an import). Design answer: §9 runs both
  attacks; the freeze is typed as a hosting statement about ONE object class
  along ONE family class; every model number carries its model fence; the
  kill branches (§6) are named as branches, not fired.

**Kill-or-switch, recorded before computing.** If the middle-form insertion
had come out class-0 (grading-EVEN), the LD-A phenomenon would be FORCED onto
the ambient count carrier by the source's own selective direction, the
dichotomy would be false, and this artifact would have led with "the fence is
a single unprotected family, build the theorem now." Measured: class 2, odd,
frozen — the dichotomy stands.

---

## 2. Retrieval — prior art, the honest ratio, and the novelty greps

Searched before computing, by object and by mechanism: *class-2 insertion,
class-0, anticommut/commut with grading, graded trace, frozen, middle-form
insertion, nilpotent insertion, one-way, Spin(4)-singlet, gamma_5-even,
protected grading, transport, spectral flow, UR-BIT2-TRANSPORT.*

| Banked result consumed | Owner | Status |
|---|---|---|
| The transport phenomenon: graded trace moves 21.8 units, zero crossings, non-integer interior; canon-class ∩ decoupled = {0}; the σ-basis enumeration | LD-A card 2 (lda:238-374) | **[R] reproduced**: the N=24 rebuild returns −21.845865 at m=1.5, gap 1.5, and the enumeration and intersection identity re-verify (probe LEG 6) |
| `CANON.md:136` verbatim class and conclusion; :135 flux exemplar; :139 external-by-structure | canon | **quoted and pinned**; both instruments re-run green (LEG 6) |
| "Class-0 content never unlocks a same-class pairing; the class-2 insertion count must be ODD" | ST-1 A2 (st1:456) | **[R] reproduced from the class side** (LEG 2); the spinor squares Sym²(S_±) = Λ³ ⊕ Λ⁷_±, Λ²(S_±) = Λ¹ ⊕ Λ⁵ reproduced by exact multiset subtraction (LEG 3) |
| The Z/4 centre classes cls(V)=2, cls(S_+)=3, cls(S_-)=1, additive; class degeneracy of the corner summands | CS-1; RSC-1 (rsc1:109-113) | **[R] consumed and re-verified** (LEG 2) |
| ζ_± = S_∓(64) ⊕ R^(±)(832); the rank bound rank ≤ 128 on 896, ≥ 768 unpaired; the Spin(4)-singlet 120 ⊕ 120 | RSC-1 (rsc1:341, :444, :506) | **[R]**: the split re-verified by Klimyk + saturation; the singlet dims re-verified (LEGs 3-4); the rank bound quoted, not recomputed |
| The four-grading enumeration; "the count lives on N1, the ambient Γ, with N2 supplying the admissibility structure"; the direction-supply re-aim of W3 | GB-1 (gb1:185-207, :9) | **consumed unchanged**; TR-1 works on the deformation-direction axis GB-1 did not touch |
| W3's grading-bridge demand; the W-bill; the wall route's ceiling | RW-1 (rw1:276-286) | **consumed unchanged**; §8 hands TR-1's inventory to its arrow |
| The bit-1 price; the three-corner support | B1P-1 | **consumed**; no bit is granted here, so the price is not incurred |

**Honest ratio: roughly 30% of the exact content is reproduction** ([R]-tagged
in the probe before anything new runs). **The novelty greps, recorded per the
AR-3 rule** (zero hits is NOT evidence of new; these certify only that the
phrasings and objects do not occur): `grep -rn "frozen at zero"` (1 hit, an
unrelated numerical-tolerance sentence in rb6), `"middle-form insertion is"`
(0), `"no Lorentz-invariant VEV"` (0), `"Spin(4)-singlet"` (RSC-1's
decomposition only — the parity reading absent), `"gamma_5-even"` /
`"gamma5-even"` (0), `"protected grading"` (0), `"class-2 insertion"` (CS-1 /
ST-1 / LD-B: the pairing-unlock content, never the operator-parity or
transport content), `"nilpotent"` + insertion/VEV (0 relevant), `"one-way"` +
spinor (0), `"model's Gamma"` (LD-A's UNTYPED declaration only). Probe LEG 7
re-executes four exact-phrase scans over the 7363-file corpus with a planted
positive and a planted near-miss.

---

## 3. (a) The typing: the count object, the gradings, the deformation families

### 3.1 The count object and its candidate gradings

The count object is the graded trace `n_- = tr(Gamma P_<0)` — LD-A's
formulation (lda:301, instrument:18), adjudicated onto its carrier by GB-1:
*"grade by **N1, the ambient Γ**, with N2 supplying the admissibility
structure"* (gb1:199). TR-1 consumes that adjudication and types what GB-1's
enumeration did not need: **at the four-corner total there are two distinct
ambient grading OPERATORS wearing the one word**, plus the branched pair:

| grading | operator | eigenspaces on the corners | receipt |
|---|---|---|---|
| `Gamma_slot` | the 14D volume word χ acting on the spinor factor (GB-1's N1 as printed: "the product of all fourteen gammas") | +1 on ν_+ ⊕ ζ_+, −1 on ν_- ⊕ ζ_- (grades by the HALF THE FORM IS VALUED IN; each ζ_± is χ-homogeneous even though reducible as S_∓ ⊕ R^(±)) | packet:69; gb1:187 |
| `Gamma_W` | the Z/4-class grading, (−1)^p·χ with p the form degree | +1 on W_+ = ν_+ ⊕ ζ_- (class 3), −1 on W_- = ν_- ⊕ ζ_+ (class 1) — the PACKAGE split, the "two separate chiral theories" of SC-CHI-01 | CS-1; rsc1:109-113; base-categories L3 |
| `gamma_5 × gamma_int` | the branched pair under D_7 ⊃ D_2 × D_5, with Γ_amb = γ_5·γ_int (machine-verified on Cl(14)) | the 4d-Weyl and internal-chirality faces at RSC-1's reduced layer (pre-contraction; the observed sense proper is downstream of LA1 = W6, GB-1 §4) | rsc1 §4.4-4.5; gb1:189-190 |

The two ambient operators agree on the 0-form corners and differ by a sign on
the 1-form corners. **Which one the bit-2 ±1 is stated on is not decided
anywhere** — SC-CHI-01's split is the `Gamma_W` package split; GB-1's N1
citation chain is the volume word. TR-1 does not decide it either; it computes
the parity table for BOTH and finds the headline results hold for both on the
source-named direction (§3.2), while the ϖ-slot rows DIFFER — which is why
the fence's first item is a grading declaration (TB-0).

### 3.2 The deformation families, typed by one integer

Every deformation direction either declared bosonic slot supplies transforms
in a `Spin(14)`-module of Z/4 centre class 0 or 2 — there is no third value:

| slot | direction | centre class | Γ_W-parity | Γ_slot-parity | pairings class-allowed | selective? |
|---|---|---|---|---|---|---|
| ε ∈ Ω⁰(ad P) | Λ^k, k odd — Λ¹, Λ³, Λ⁵, **Λ⁷_± (the source's chirality mechanism)** | **2** | **ODD** | **ODD** (Δp = 0, parities agree) | same-half self-pairings ONLY | middle forms: YES (ST-1 §4.5(3), RSC-1); Λ¹, Λ⁵: Grassmann-live but blind (st1 §4.5(2)) |
| ε ∈ Ω⁰(ad P) | Λ^k, k even — Λ⁰, Λ² (the adjoint), Λ⁴, Λ⁶, **Λ¹⁴ (the pseudoscalar, the Γ-aligned direction)** | **0** | **EVEN** | **EVEN** | cross-half (Dirac-type) ONLY | NO — class arithmetic alone: a class-0 direction cannot give either half a self-shape |
| ϖ-type Ω¹(ad P) content | V ⊗ Λ^k, k even | **2** | **ODD** | **EVEN** (Δp = 1, parities FLIP) | same-half (in the Γ_W sense; these are ST-1's Ω¹-column unlockers, 172 shapes at one insertion, st1:9) | ST-1's Ω¹ column — NOT retyped here |
| ϖ-type Ω¹(ad P) content | V ⊗ Λ^k, k odd (incl. V ⊗ Λ⁷_±) | **0** | **EVEN** | **ODD** | cross-half (Γ_W sense) | NO (class) |

Three exact facts carried by the table (probe LEG 2, and independently LEG 5):

1. **The parity theorem.** A class-2 insertion maps class-c content to class
   c+2: it swaps W_+ ↔ W_- content — it ANTICOMMUTES with `Gamma_W`. A
   class-0 insertion preserves each half — it COMMUTES. For the ε slot the
   same parities hold verbatim for `Gamma_slot` (form degree preserved); for
   the ϖ slot they flip (form degree shifts by one).
2. **The pairing rule** (ST-1 A2, reproduced from the class side): a
   same-half self-pairing needs total class 0 mod 4, hence insertion class 2
   (3+3+2 = 0 = 1+1+2); a cross-half pairing needs insertion class 0
   (3+1+0 = 0). *"Class-0 content never unlocks a same-class pairing; the
   class-2 insertion count must be ODD"* (st1:456) — quoted, reproduced,
   owned by ST-1.
3. **The dichotomy** (new): reading 1 and 2 together, the set of directions
   that can select (self-pair a half) and the set of directions that commute
   with the grading (the transport-capable class, §4) are COMPLEMENTS. The
   both-capabilities set is empty — checked as arithmetic, not asserted.

**The slot mismatch, flagged (L5).** SC-CHI-01's hedge conditions on ϖ (the
1-form slot); the typed unique selective channel is the ε-slot middle form
(ST-1 §4.5(3), RSC-1). Both slots are typed above precisely so that neither
reading of the hedge escapes the table: every direction of EITHER slot falls
in one of the four rows. Which slot the physical VEV lives in is a register
owner / Lane-1 question, not TR-1's.

### 3.3 The selective direction is a one-way arrow (exact, new)

The middle-form directions act on the half-spinors one-way (probe LEG 3, four
Hom spaces, computed by Klimyk AND read off the verified spinor squares,
agreeing):

```
    dim Hom(Lam^7_- (x) S_+, S_-) = 1        dim Hom(Lam^7_- (x) S_-, S_+) = 0
    dim Hom(Lam^7_+ (x) S_-, S_+) = 1        dim Hom(Lam^7_+ (x) S_+, S_-) = 0
    dim Hom(Lam^7_± (x) S_a, S_a) = 0        (class-forbidden, weight-confirmed)
```

A SINGLE middle-form direction is therefore a nilpotent one-way map between
the halves — not a two-sided mass. Any self-adjoint realization needs the
reverse arrow from the ADJOINT, whose direction is set by the inner
product/reality map (SOURCE-SILENT — SN-1's fence; on any realization the
pair is grading-odd regardless, so the freeze below is unconditional on
this). This sharpens what "the mass is actually a variable" (Transcript:158)
can mean at one insertion order: the selective variable is a one-way
coupling, and the two-sided Dirac coupling of the massive phase requires
either both middle forms or class-0 content.

---

## 4. (b) The adjudication: FORCED / POSSIBLE / EXCLUDED, per (grading, family)

### 4.1 The freeze (ambient gradings, class-2 families): EXCLUDED

**Lemma (exact linear algebra; the count object presupposes its hypotheses).**
If D is self-adjoint, Γ is a Hermitian involution, and {D, Γ} = 0, then every
eigenvector with E ≠ 0 satisfies ⟨ψ, Γψ⟩ = 0 (Γψ is an eigenvector of
eigenvalue −E, orthogonal to ψ), hence `tr(Γ P_<0) = 0` — not conserved:
**identically zero**. A class-2 deformation family D(t) = D_0 + f(t)·M with
D_0 and M both Γ-odd stays Γ-odd for every t, so along the whole family the
graded trace is frozen at zero. Witnessed on the carrier's own Cl(14) fiber
algebra: max |trace| = 8.5e-16 over the sweep (probe LEG 5), and on the
lattice model's σ₂ family (LEG 6).

Since the source's selective middle-form direction is class 2 and odd for
BOTH ambient gradings (§3.2): **the LD-A phenomenon is EXCLUDED on the
ambient graded trace along the source-named family — and so is ever reading a
±1 from it.** Not "protected": FROZEN. There is nothing to transport, and
nothing for a bit-2 count to read, on this object along this family.

### 4.2 The index face: what the odd class CAN carry

The freeze does not make the odd class countless — it relocates the count.
For a Γ-odd operator the kernel is Γ-stable and carries the integer
`ind = dim ker_+ − dim ker_-`, while the nonzero spectrum contributes exactly
0 (LEG 5 witnesses both on an unequal-halves block operator: index 1, trace
0). That is precisely canon's structure: *"net chiral index = flux number"*
(CANON.md:135, the external exemplar) and *"`chi = interior-even +
external-topological-index`, so any odd count is necessarily external"*
(CANON.md:139). **For class-2 families the count is a kernel index or it is
nothing, and only external/topological data (flux, winding — RW-1's wall,
GB-1's direction supply) can make it nonzero.** VERIFICATION.md:24's *"the
only unconditionally computable integer is 1"* sits exactly here.

### 4.3 The unprotected class (class-0 families): the LD-A phenomenon's home

A class-0 deformation COMMUTES with the ambient grading — the exact algebra
class of the model's grading-breaking mass. There, the protection identity
fails at every f(t) ≠ 0; `CANON.md:136`'s class — verbatim, *"Net chiral
spectral flow 0 for self-adjoint, chirality-odd, Krein-self-adjoint Fredholm
families"* — excludes the operator at every interior point (hypothesis (ii)
fails: odd kinetic + even mass is neither odd nor even), and LD-A's identity
*"canon-class ∩ {`[D,Gamma] = 0`} = {0}"* (lda:295) closes the other end.
Witnessed at the carrier's fiber algebra: the Γ-aligned even direction (the
Λ¹⁴ pseudoscalar — the exact carrier analog of the model's σ₃, which is the
model's own Γ-direction) moves the graded trace **18.5 units** with the
family gapped (LEG 5); the model itself moves **−21.845865** at m = 1.5 with
min|spec| = 1.5 and zero crossings (LEG 6, LD-A's number reproduced to 6
decimals). Motion is direction-dependent within the class (some even
directions preserve balance by accidental symmetry); what is class-wide is
the ABSENCE of any protection theorem. And by §3.2's pairing rule, class-0
content only cross-pairs the halves — it is the Dirac-type coupling of the
massive/non-chiral phase, and it is selection-blind by arithmetic.

**Verdict grammar, applied.** On class-0 families the phenomenon is
POSSIBLE-AND-MODEL-MATCHED (motion witnessed in two instruments; magnitudes
bind their models); the phenomenon's PRECONDITION — no protection — is
FORCED (class arithmetic); the phenomenon itself is not forced (motion is
dynamical).

### 4.4 The branched layer: no γ₅-odd VEV direction exists at all (exact, new)

A VEV direction that preserves 4d Lorentz invariance is a Spin(4)-singlet,
and `dim Inv_{Spin(4)}(Λ^p V_4) = [1,0,0,0,1]` for p = 0..4 (exact Racah sum,
LEG 4). So every Lorentz-preserving direction of every Λ^k has 4d form degree
p ∈ {0, 4} — **EVEN** — and internal degree q = k or k−4 with k's parity. On
the explicit Cl(14) algebra (LEG 5): both Lorentz-singlet components of the
middle form — (p,q) = (0,7) and (4,3), the 120 ⊕ 120 of rsc1:506 —
**commute with γ₅ and anticommute with γ_int and Γ_amb**, and the parity
table for all k is

```
    (ambient, 4d, internal) parity of any Lorentz-singlet Lambda^k direction
        = ((-1)^k, +1, (-1)^k),      with  ambient = 4d x internal.
```

Consequence: **at the branched layer the 4d-Weyl-graded trace has NO
protected deformation direction at all.** Every declared-slot VEV direction —
selective or blind, class 2 or class 0 — acts γ₅-evenly, i.e. in the model's
grading-breaking mass class. The stylized SC-CHI-02 equation is already this
shape: `dslash_A psi_L(y) = (R(y)/4) psi_R(y)` (register:940) has a γ₅-odd
kinetic term and a γ₅-even mass, mass *"m = R(y)/4 for any fixed background
metric"* (register:1176). (This is the pre-contraction face of the observed
sense; the observed sense proper is downstream of the non-invertible LA1
contraction — GB-1's W6 handoff, untouched here.)

### 4.5 LD-A's declared residual, resolved conditionally

LD-A §8/§10: *"whether the model's `Gamma` is R3's ambient-half grading X is
UNTYPED"* (lda:892-893). Typed now, by the mass-direction comparison: the
model realizes (Γ-odd kinetic + Γ-EVEN mass). At the ambient gradings, the
even-mass role can only be played by class-0 directions — never by the
source's selective middle form, which is ambient-ODD and freezes the trace
(§4.1). At the branched γ₅ face, EVERY Lorentz-invariant direction including
the middle form plays it (§4.4). **So: if the deformation under study is the
source-named selective direction, the model's Γ cannot be the ambient
grading and reads correctly at the 4d-Weyl face; if the deformation is
class-0 (even-k) content, the model's Γ can be either.** Either way LD-A's
measured phenomenon lands on a real algebra class of the carrier — the
residual is resolved into a condition, not an ambiguity.

### 4.6 Verdict summary per candidate object

| count object | along class-2 (selective-capable) families | along class-0 families |
|---|---|---|
| tr(Γ_W P_<0), ambient package grading | **FROZEN-EXCLUDED** (≡ 0 exactly; §4.1) — count lives only in the kernel index, external data required (§4.2) | **UNPROTECTED** (LD-A class; §4.3) — and the family is selection-blind |
| tr(Γ_slot P_<0), ambient volume word | same as Γ_W for ε-slot families; ϖ-slot parities flip (§3.2) — declare the grading (TB-0) | same, with the ϖ-slot flip |
| tr(γ₅ P_<0), branched 4d face | **UNPROTECTED for every VEV direction whatsoever** (§4.4): no γ₅-odd direction exists | same — the distinction collapses at this face |
| tr(γ_int P_<0), internal | kinetic is internal-mixed even at t = 0: no freeze theorem applies; no source sentence states a count here — typed NOT-THE-COUNT-CARRIER | same |

---

## 5. (c) THE FENCE — the transport bill, enforceable prose

Any future bit-2 counting argument — any argument that reads, produces,
moves, or compares a signed chirality count across the SG4 bit-2 transition —
must supply ALL applicable items before its count is admissible:

- **TB-0 — GRADING DECLARATION.** Name the grading operator the count is
  stated on, from the typed inventory (Γ_W package / Γ_slot volume word /
  branched γ₅ / γ_int — GB-1's four nodes plus §3.1's two-operator
  distinction), with its parity row from §3.2. A count sentence without a
  grading token is inadmissible; the ϖ-slot parity flip (§3.2) makes the
  declaration load-bearing, not cosmetic.
- **TB-1 — AMBIENT BOOKS CLOSE AT ZERO.** On either ambient grading, along
  any class-2 (selection-capable) family: the interior spectral-asymmetry
  trace is identically zero (§4.1). A ±1 booked there is inadmissible per se.
  The only admissible ambient count is the KERNEL INDEX, and a nonzero index
  requires named external/topological data — route through canon's doorway
  (CANON.md:135/139, quoted in §4.2), RW-1's W-bill (W1-W6), and GB-1's
  direction supply. No interior VEV transport can substitute.
- **TB-2 — EVEN-CLASS TRANSPORT LAW.** Along any class-0 family (and along
  EVERY VEV family at the branched γ₅ face, §4.4): the family leaves
  `CANON.md:136`'s class at every point where the deformation acts — the row
  is verbatim *"Net chiral spectral flow 0 for self-adjoint, chirality-odd,
  Krein-self-adjoint Fredholm families"*, its class satisfies *"canon-class ∩
  {`[D,Gamma] = 0`} = {0}"* (lda:295), and it therefore cannot transport a
  count to, from, or across the transition in either direction. An argument
  wanting a number carried across must supply a transport law stated on a
  class that CONTAINS grading-even deformations and both endpoints (shape in
  §6), or must not carry the number.
- **TB-3 — NO INTEGER AT INTERIOR POINTS.** Absent a quantization theorem,
  the unprotected trace is non-integer at interior points (model: −7.578316
  at m = 0.15, lda:305; carrier fiber witness: 18.508, §4.3). Reading an
  integer anywhere except at an endpoint equipped with an exhibited
  protection (Γ-oddness, or an index identity) is inadmissible.
- **TB-4 — ENDPOINT EXISTENCE.** "The count at the decoupled/massive
  endpoint" is defined only if that endpoint is exhibited as gapped/Fredholm.
  At one selective insertion it is NOT: the induced form has rank ≤ 128 on
  the 896 of each one-form corner, leaving ≥ 768 directions unpaired
  (rsc1:444, quoted) — one middle-form insertion cannot gap a corner. A
  gapped endpoint therefore needs multiple insertions (the class-2 count
  must be ODD — st1:456) or class-0/blind supplements, and every such
  supplement re-raises the selectivity problem it was brought in to solve
  (ST-1 §4.5(2); RSC-1 C2).
- **TB-5 — THE DICHOTOMY MUST BE FACED.** Any argument in which one
  mechanism both selects the surviving half and moves the count must name
  its direction's class and then explain which horn it evades: class-2
  selects but freezes (TB-1); class-0 moves but only cross-pairs and cannot
  select (§3.2). No declared-slot direction has both capacities; a mechanism
  claiming both is either two mechanisms (name both, bill both) or an error.

The bill composes with, and does not supersede, RW-1's hosting bill (W1-W6)
and GB-1's direction-supply arrow: TB-1 routes ambient counts INTO them.

---

## 6. (d) What would discharge the fence, and the kill branches

**The missing theorem's SHAPE (named, not claimed) — a mixed-parity family
index theorem.** For families `D(t) = D_odd(t) + f(t)·M_even` on a Z/2-graded
Krein space with cross-chirality K, connecting a Γ-odd (chiral-phase)
endpoint to a grading-mixed (coupled/massive) endpoint: an identity

```
    tr(Gamma P_<0)(t_1) - tr(Gamma P_<0)(t_0)
        = [endpoint spectral-asymmetry terms (eta-type)]
        + [local/topological density integrated over the family]
```

stated on a class containing both endpoints — an eta-invariant /
relative-index statement across a grading-breaking deformation, exactly the
extension LD-A's proposal named (lda:347-350: *"either extend the
`CANON.md:136` class to cover a grading-breaking mass deformation, or record
the exclusion"*; the exclusion is hereby recorded, with the class typed).
Its proof would consume, at minimum: (i) the reality map / inner product
making D(t) self-adjoint (SOURCE-SILENT; SN-1's standing
`UNDEFINED_WITHOUT_REALITY_MAP`); (ii) the source action fixing D_0 and the
physical gauge slice (the RS-BRST gap — CANON.md's Not-Yet-Canon row: the
count is blocked on the source action); (iii) a Fredholm/gapped massive
endpoint (TB-4's bill); (iv) the W3/W6 bridges for any layer crossing
(RW-1, GB-1). Until then the fence stands.

**Kill branch K-A (fence becomes unnecessary).** A measured
integer-quantization of the unprotected trace along the physical family — a
new protection theorem or symmetry — would moot TB-2/TB-3 and be a
first-class result; OR the count is re-hosted off the interior trace entirely
onto a wall/flux kernel index (RW-1's target, GB-1's angle), in which case
interior transport is moot and the fence transfers to the wall's own bill
(W1-W6) — TB-1 already routes there.

**Kill branch K-B (the count becomes unhostable).** If the reality map, once
supplied, fails to make any admissible realization self-adjoint (no spectral
`P_<0` at all), or if the massive endpoint is provably non-Fredholm at every
admissible insertion order (TB-4's obstruction upgraded from one insertion to
all), then NO graded-trace count exists on the interior route in either
phase, and bit 2 must be re-typed as a phase label without an integer — the
radical form of LD-A card 11's provenance point. Either branch firing would
be reported as a result, not a failure.

---

## 7. Machine-readable verdicts and certificate

<!-- TR1-TABLE-BEGIN -->

| id | question | verdict | evidence_key |
|---|---|---|---|
| T-AMB | can the LD-A phenomenon occur on the ambient graded trace along the source's VEV family | FROZEN-EXCLUDED | odd_family_trace_zero |
| T-OBS | can it occur on the observed 4d Weyl trace | UNPROTECTED-MODEL-MATCHED | no_gamma5_odd_direction_and_lda_pin |
| DICH | can one insertion direction both select a half and move the count | OPPOSITE-PARITIES | both_capabilities_set_empty |
| FENCE | is UR-BIT2-TRANSPORT executed at the real carrier | EXECUTED-SHARPENED | transport_bill |
| NOV | is the composition new | NEW-AS-COMPOSED | absence_scans |

<!-- TR1-TABLE-END -->

**Verdict-evidence binding** (probe LEG 8 enforces this; the selftest's
contrary control M12 flips DICH in a temp copy and must be caught):

| evidence_key | asserted direction | measured |
|---|---|---|
| odd_family_trace_zero | FROZEN requires the odd-family trace = 0 to machine precision | max 8.5e-16 over the Cl(14) sweep; σ₂ lattice sweep 0 identically |
| no_gamma5_odd_direction_and_lda_pin | UNPROTECTED requires [1,0,0,0,1] (no γ₅-odd VEV direction) AND the model excursion pinned | Inv vector exact; n_-(1.5) = −21.845865 reproduced; Cl(14) motion 18.508 |
| both_capabilities_set_empty | OPPOSITE-PARITIES requires the both-capabilities class set empty | empty (classes {0,2} partition; self-pair ⇔ 2; cross-pair ⇔ 0) |
| transport_bill | EXECUTED requires TB-0..TB-5 present with their receipts | §5, each item carrying its quoted receipt |
| absence_scans | NEW-AS-COMPOSED requires 0 corpus hits on the result phrasings, planted controls behaving | 0 hits / 7363 files; positive flagged; near-miss not flagged |

**Certificate.**
`tests/channel-swings/joe_directed_tr1_transport_and_selection_are_opposite_parities.py`

- Live run: **90/90 checks, exit 0.** Eight legs: 29 byte pins + planted
  negative + two instrument SHA-256 pins; the class-arithmetic leg with 5
  planted-false propositions; the exact D_7 weight/Klimyk leg (Weyl dims as
  Fractions, multiset identities to empty remainder, the four one-way Hom
  facts by two agreeing instruments, planted positive on the same code
  path); the W(D_2) invariant leg with planted control; the Cl(14) leg
  (Hermitian-involution guards so the graded trace is non-vacuous, parity
  sweeps, the freeze at 8.5e-16, the 18.5-unit contrast, the index face);
  the LD-A model contact (pinned −21.845865, class enumeration, intersection
  identity, both instruments re-run green by subprocess); the certified
  absence scans; the artifact binding with SHA-pinned verdict table.
- `--selftest`: clean baseline verified FIRST (90 green, 0 [FAIL], count
  pinned independently at 90) and a red baseline aborts; **12/12 mutations
  caught, each via the genuine [FAIL] line of the check TARGETED at it**
  (pin line number, instrument SHA, class table, positive roots, spinor
  parity, Klimyk ρ, W(D_2) sign, freeze machinery, LD-A pin, re-run path,
  absence detector, and the artifact-copy contrary control); a crash is
  CRASH-NOT-DETECTION and fails; an untargeted catch is
  INCIDENTAL-NOT-TARGETED and fails; the failing check is printed for every
  mutation; the baseline is re-verified after the mutations. Exit 0.
- Read-only; mutation copies live in a temp directory. Deterministic;
  numpy + stdlib.
- What the probe does NOT certify: anything about the true RS Y¹⁴ bundle or
  the (9,5) real form; any spectrum, scale, or vacuum; the ϖ-slot
  selectivity content (ST-1's Ω¹ column, consumed not retyped); and any
  future file phrasing the same result in words outside the scanned
  families.

---

## 8. What each consumer inherits, per layer — versionless deltas, none a launder

1. **UR-BIT2-TRANSPORT owner (upgrade register).** The item is EXECUTED as a
   fence: §5's bill is the enforceable form; the "theorem later" half is
   typed in §6 with its consumption list and kill branches. Proposed
   disposition (integrator's call): status DONE with receipt = this artifact
   + probe, the theorem half re-registered under the §6 shape if wanted.
2. **CANON.md:136 / function-space-ext owner.** Gains the carrier-side scope
   note from the deformation axis: the row's class contains the class-2
   (selective) families — where its conclusion is the FREEZE, trace ≡ 0 —
   and excludes every class-0 family at every acting point. Complements
   RW-1's wall-side note (rw1 §6.2) and LD-A's model-side fence: three faces
   of one scope statement.
3. **GB-1 / the W3 arrow.** Its "source-native supply of the σ₂-type
   (Γ-odd, Krein-breaking) mass direction" now has a typed inventory to
   shop in: the Γ-odd stock is exactly the class-2 rows of §3.2 (odd-k ε
   directions; even-k ϖ content). The Krein leg of each candidate is
   reality-map-gated (SN-1); the gapping leg is priced by TB-4's rank bound.
4. **ST-1.** A2's partition acquires its operator-parity reading and its
   transport consequence; §4.5(2)'s blind-live directions (Λ¹, Λ⁵) are now
   also typed FROZEN on the ambient trace (class 2). No ST-1 number moves.
5. **LD-A.** Card 2's verdict inherits the carrier execution it called for;
   its §8 UNTYPED residual is resolved conditionally (§4.5). Its measured
   numbers are reproduced and pinned, and its scope fence ("binds the model")
   is confirmed — the model's algebra class is real at the carrier, at the
   faces §4.5 names.
6. **RW-1 / the wall route.** TB-1 routes every ambient bit-2 count into its
   W-bill: the wall is no longer one candidate among interior alternatives —
   on class-2 families it is (with flux) the ONLY count-bearing route left
   standing. Its ceiling (hosting, |winding| = 1, never 3 — CC-05) is
   unchanged.
7. **Source-claim register owner (SC-CHI-01/02/52/53).** Gains one
   note-grade fact: the hedge's slot (ϖ) and the typed selective channel's
   slot (ε) differ, and the parity map differs per slot AND per grading
   (§3.2) — a reading question for the register, flagged not adjudicated.

---

## 9. Hostile review, inline — the strongest attacks

**"Your freeze is trivial: of course an anticommuting deformation keeps the
spectrum symmetric. You have dressed a one-line lemma as a result."** The
lemma is one line; the RESULT is which physical directions fall in its
hypothesis. Before TR-1, nothing in the corpus said whether the source's own
selective direction commutes or anticommutes with the count's grading — LD-A
explicitly declared it UNTYPED (lda:892) and its model's mass sat on the
OTHER side. The content is the composition: (selective ⇔ class 2 ⇔ frozen)
and (transport-capable ⇔ class 0 ⇔ selection-blind), each leg exact, plus
the branched-layer collapse (§4.4) that no protected direction exists at the
γ₅ face at all. The lemma is the cheapest part and is priced as such.

**"You proved the trace is zero on the odd class and nonzero motion exists
on the even class — so the count story should just use the even class, and
your dichotomy is a fancy way of saying 'use a Dirac mass'."** The even
class is selection-BLIND by the same arithmetic (ST-1's A2, reproduced): it
cross-pairs the halves — it is the coupling whose REMOVAL is the transition,
not a chirality selector. An argument using only class-0 content has no
mechanism for "which half is matter" — that is the bit the count was
supposed to read. The dichotomy is precisely that the bit-selecting
directions and the trace-moving directions never coincide; calling one horn
"just use a Dirac mass" concedes the other horn.

**"The Cl(14) instrument is a toy: a one-gamma kinetic term is not the
rolled-up operator, and 18.5 is not a physics number."** Conceded and typed
(§ typed objects, block 2): the Cl(14) leg witnesses ALGEBRA-CLASS facts
(parities, freeze, existence of motion) on the carrier's genuine fiber
algebra; 18.5 is an existence witness, not a magnitude claim; every
magnitude binds its model. The parity facts it witnesses are independently
exact (centre + weight arithmetic) — the matrices are the second instrument,
not the proof.

**"Your branched-layer γ₅ claims smuggle the observed layer past the LA1
contraction."** The claims are stated at RSC-1's reduced layer (the
branching D_7 ⊃ D_2 × D_5, pre-contraction), the same layer RSC-1's §4.5
computes at; the observed sense proper is W6's and is handed off exactly as
GB-1 hands it off (gb1 §4). The T-OBS verdict row's "observed" is defined in
§4.4 with this fence in place.

**"You resolved LD-A's residual by fiat."** By computation: the middle form
is ambient-odd (two instruments), so a model whose mass is grading-EVEN
cannot be modelling the ambient grading when the deformation is the middle
form. The resolution is conditional on which deformation is under study, and
§4.5 states the condition rather than hiding it.

**"Fence items are cheap; you have built no theorem."** Correct, and that is
the register item's own activation order: *"fence first, theorem later"*
(register:160). The theorem's shape, consumption list, and kill branches are
§6; claiming it would be the overreach this file exists to prevent.

---

## 10. Claim ceiling, and what this artifact is not

- **No claim status moves. No canon row moves. No register, registry,
  sidecar, or ledger edit.** The generation count stays OPEN; SG4 stays the
  decider; bit 2 stays OPEN; the wall route stays a target under its bill.
- **Not a kill.** `target_claim` is a register item executed, not attacked.
  Three specific non-kills: (a) `CANON.md:136` is untouched — within its
  class its conclusion holds and is here shown to be the freeze; (b) LD-A's
  card 2 is confirmed and executed, not superseded; (c) SC-CHI-01/02/52/53
  survive with their layers, per the frame of §0.
- **The freeze is not "the total theory is chiral" and not "GU cannot get a
  count".** It is: one object class (interior graded trace) reads zero along
  one family class (class-2) — which routes the count to the kernel-index /
  external mechanism canon already requires. The unforgivable regression
  (crediting L1 with chirality) is fenced, not committed.
- **The dichotomy is kinematic.** It types directions, not dynamics: it does
  not say the transition happens, at what scale, or in which slot. Lane 1
  owns all three.
- **Model numbers bind models** (−21.845865, 18.508, min|spec| = 1.5). Exact
  statements are complexified-D_7 structural facts, horn-independent.
- **Not done, and named:** the ϖ-slot selectivity content (ST-1's Ω¹
  column) is consumed, not retyped; the Krein/self-adjointness leg of every
  candidate direction is reality-map-gated (SN-1); the true-Y¹⁴ operator and
  the (9,5) transport are untouched (rung-2's and canon's standing
  residuals).

---

## 11. Canonical-currency check records (for the integrator; TR-1 edits no sidecar)

`process_gates/canonical_currency_audit.py` run at work time (2026-08-17;
corpus 3649 dated files, registry 10 corrections, sidecar 40 recorded
checks, exit 0). **Measured: NONE of TR-1's named inputs appears in the
current dirty queue.** The brief flagged
`lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md` as DIRTY under
CC-06 and CC-08; at run time both pairs are already CLEARED-CONSISTENT in the
sidecar (records dated 2026-08-17, `by: RW-1`, transcribed by the
integrator, canonical-currency-checks.yaml:393-410). TR-1 records its own
per-input verdicts regardless, scoped to the corrections whose signatures its
consumption sits inside:

1. `lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md`
   - vs **CC-06** (chirality VEV-conditional; selector SG4 bit 2 OPEN):
     the consumed content IS the correction's upstream — the §11.6 hedge
     passage (extraction:137-138) is quoted verbatim in §0 and every use
     below keeps the VEV-conditional reading; no clause decides or assumes
     the bit. **CLEARED-CONSISTENT** (pointer: extraction:137-138; this
     artifact §0, §3.2, §10).
   - vs **CC-08** (the 128 is a partner-placement obligation, not a defect):
     the 128 enters only through RSC-1's rank bound as the obligation whose
     gapping endpoint TB-4 prices; no defect/surplus framing imported.
     **CLEARED-CONSISTENT** (pointer: extraction §2; this artifact §5 TB-4).
2. `papers/drafts/Transcript into the impossible.md` (:158, :149 consumed) —
   in no dirty queue; the UCSD-derived lineage caveat (CC-10) is carried:
   nothing here treats the transcript as more than transcript-verified, and
   no verdict rests on it alone (the operative uses are frame quotes).
   **CLEARED-CONSISTENT (noted).**
3. `lab/sources/source-claim-register.yaml` (SC-CHI-01/02/52/53, SC-MAS-01)
   — co-owner of CC-06, in no dirty queue; verbatim fields quoted with
   polarity and hedges preserved (§0, §4.4). **CLEARED-CONSISTENT (noted).**
4. All remaining named inputs (LD-A + probe, RSC-1, RW-1, GB-1, B1P-1,
   ST-1, CS-1, gu-base-categories, CANON.md, VERIFICATION.md, both
   instruments, upgrade register, correction registry) — checked against the
   full dirty queue by exact-substring search over the audit output: zero
   entries. All post-date every `canonical_since` or sit in no signature.
   **CLEAN-BY-AUDIT** (pointer: audit output, TOTAL dirty 183 pairs, none
   naming these files).

Operative correction sentences relied on are quoted verbatim in §0 (CC-05,
CC-06, CC-08 `superseded_reading` fields, correction-registry.yaml:264-269,
:300-306, :364-368).

---

## 12. POSTFLIGHT — the six lenses, after the work

- **P1 — Did any lens change a verdict?** Yes, two. L5 (source-fidelity)
  surfaced the ϖ/ε slot mismatch, which forced the parity table to carry
  both slots and BOTH ambient gradings — and that in turn surfaced the
  Γ_W/Γ_slot parity flip on ϖ content, which became TB-0's justification.
  L2 (centre arithmetic) caught the vacuity bug in the first Cl(14)
  instrument draft (an anti-Hermitian volume word makes every graded trace
  identically zero) — the Hermitian-involution guard checks exist because
  of it, and the freeze number 8.5e-16 is non-vacuous BECAUSE the same
  instrument moves 18.5 units on the even direction.
- **P2 — Is any verdict resting on a single receipt?** T-AMB rests on the
  parity theorem plus the freeze lemma — two instruments for the parity
  (centre, matrices) and pure linear algebra for the lemma. T-OBS's
  no-protected-direction leg rests on the [1,0,0,0,1] invariant vector
  (exact, planted-controlled) — single computation, two consequences
  cross-checked (the (0,7)/(4,3) matrix sweep). NOV rests on scans and is
  typed scan-bounded.
- **P3 — What did NOT move?** SG4 remains the decider; bit 2 remains OPEN;
  the count remains OPEN and external-by-structure; the wall target remains
  under its bill; ST-1/RSC-1/GB-1/RW-1/LD-A verdicts all stand, each
  inheriting only a typed condition or an executed demand.
- **P4 — Where would a hostile re-runner disagree?** On how much the
  dichotomy constrains a MULTI-insertion story (two class-2 insertions
  compose to class 0: the composite can cross-pair while each factor
  selects — the bill's TB-4/TB-5 handle this by pricing, not forbidding;
  a re-runner might want a sharper composite-parity clause, and that is a
  legitimate successor arc). And on the branched-layer fence: a reader who
  wants γ₅ statements only post-contraction will discount §4.4 to the
  reduced layer — which is where it is typed.
- **P5 — Regression check against the standing corrected facts.** Four
  layers named throughout; contraction-not-KK untouched (no mode split
  used); no-GUT untouched (no unification chain); positivity polarity
  untouched (no positivity claim; the Krein leg is typed open);
  2+1 subtractive quoted and obeyed (no additive three anywhere); the 128
  an obligation (§0, TB-4). "Majorana"/"Krein" appear only inside
  repository-vocabulary fences.
- **P6 — Instrument honesty.** All planted controls fired correctly on the
  live run (planted-negative pin, 5 planted-false class propositions,
  Klimyk planted positive, W(D_2) planted control, absence planted
  positive + near-miss); the selftest's 12 mutations were each caught by
  their targeted checks with the clean baseline verified first and
  re-verified after; the one design bug found en route (vacuous trace) is
  recorded in P1 rather than silently repaired.

---

## The blunt paragraph: which direction the pressure ran

The brief framed the task as executing LD-A's fence at the real carrier, and
the expected shape was that the source's mass term would break the grading
there the way the model's did — the pressure ran toward confirming the
phenomenon on the ambient count carrier and writing an urgent theorem
demand. The arithmetic pushed back immediately: the source's own selective
direction is grading-ODD at the ambient layer — the exact opposite of the
model's mass — and the trace it was supposed to push around is frozen at
zero along that whole family. What survived the push-back is, I judge,
stronger than the expected result: the ONE integer that decides whether a
direction can select a half also decides, oppositely, whether it can move
the count; the phenomenon LD-A measured is real at the carrier but lives
exactly where selection cannot (class-0 directions, and the branched γ₅
face, where NO VEV direction is protected); and the interior route to a
bit-2 ±1 closes in both parity classes at once, which routes every future
counting argument through canon's external doorway with a five-item bill on
the door. The two places a hostile re-runner should press are named in
P4 — the composite-parity question for multi-insertion stories is the real
open flank, and I would run it as the successor arc before trusting TB-5
against a determined constructor.

---

## Deviations from the brief

1. **Input currency:** the brief listed the s11-s12 extraction as DIRTY
   under CC-06/CC-08; measured at run time it was already cleared (sidecar
   records by RW-1's integrator, 2026-08-17). TR-1 recorded its own
   verdicts anyway (§11).
2. **One input beyond the named list:** GB-1
   (`grading-bridge/gb1-...-2026-08-17.md`), a same-day wave-2 sibling that
   owns the four-grading enumeration and the N1 count adjudication, was
   surfaced by the novelty greps and consumed — required for honest typing
   of task (a); without it §3.1 would have re-derived (worse) what GB-1
   already typed.
3. **Task (b)'s framing presumed one answer shape** ("does the real
   deformation break the grading the way the model's mass did") — the
   measured answer is layered: NO at the ambient gradings for the
   source-named direction (frozen), YES for class-0 directions and for
   every direction at the branched γ₅ face. The verdict table carries the
   split rather than a single token.
4. No other deviations: two files written, no git operations, no existing
   file touched, pre-existing gate reds left as other lanes' debts.

## Reproduce

```
_local/cas-venv/bin/python tests/channel-swings/joe_directed_tr1_transport_and_selection_are_opposite_parities.py
_local/cas-venv/bin/python tests/channel-swings/joe_directed_tr1_transport_and_selection_are_opposite_parities.py --selftest
```

Expected: `90/90 checks pass`, exit 0; selftest: clean baseline verified
first (90, pinned), then `12/12 mutations caught` each via its targeted
genuine `[FAIL]` line, 0 crash-only, 0 missed, baseline re-verified, exit 0.
