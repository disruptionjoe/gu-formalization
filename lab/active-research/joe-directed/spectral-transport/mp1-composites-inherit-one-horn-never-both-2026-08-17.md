---
artifact_type: exploration
status: exploration
doc_type: construction_result
created: 2026-08-17
work_item: MP-1
channel: spectral-transport
wave: reverse-scaffold wave 3 (spectral-transport arc; executes TR1-COMPOSITE-PARITY)
target_claim: "The upgrade-register item TR1-COMPOSITE-PARITY, verbatim title (lab/process/upgrade-program-register.yaml:257): 'The composite-parity flank of TR-1's dichotomy: two class-2 insertions compose to class 0, so a multi-insertion constructor meets TB-4/TB-5 as pricing, not prohibition — run this before trusting the dichotomy against a full mechanism; theorem shape = mixed-parity family index theorem (TR-1 section 6)'. Origin (register:258): 'TR-1 blunt paragraph + section 6 (tr1-transport-and-selection-are-opposite-parities-2026-08-17.md); the arc's own named successor'. The flank as TR-1's own P4 states it (tr1:869-872): 'On how much the dichotomy constrains a MULTI-insertion story (two class-2 insertions compose to class 0: the composite can cross-pair while each factor selects — the bill's TB-4/TB-5 handle this by pricing, not forbidding; a re-runner might want a sharper composite-parity clause, and that is a legitimate successor arc)'."
target_claim_verdict: "THE FLANK IS EXECUTED AND THE DICHOTOMY SURVIVES COMPOSITION — CLOSED AT THE TERM LEVEL, PRICED AT THE MECHANISM LEVEL; NOT A KILL AND NO CLAIM MOVES. (a) TYPED: the four composite shapes a constructor can build from class-2 insertions are the linear multi-direction family (a SUM — it never leaves the Gamma-odd subspace and is not a class composite at all), the tensor/monomial composite inside an invariant (class = 2·(number of class-2 factors) mod 4 — CS-1's corollary (d), the class-arithmetic owner), the operator product (Gamma-even at even factor count), and the successive-deformation/rotation composite (GB-1's circle — angles add; a composite of rotations is a rotation and inherits GB-1's exact no-transport invariant). (b) SELECTION IS DESTROYED, NOT INHERITED, AT EVEN ORDER — exact at the carrier: any even-count composite has centre class 0 and the centre acts by i^cls on every irrep, so the same-half self-pairing Hom space is EXACTLY zero (rigorous centre argument, not an allowedness heuristic); machine-witnessed twice at the decisive object: the full 1716^2-dimensional composites Lam^7_a (x) Lam^7_b carry ZERO odd-form content (Klimyk AND a Racah/Brauer sum over all 322,560 elements of W(D_7), dimension-saturated to 2,944,656); the same-sense pair cannot even close an invariant loop (Inv(Lam^7_- (x) Lam^7_-) = 0) while the opposite-sense pair closes exactly one; and at the operator level the one-way arrows compose to ZERO in the same sense (A_eps^2 = 0 and A A' = 0 for any two same-sense middle-form arrows — exact from TR-1's four Hom facts, machine zero on Cl(14)), so the only nonzero composites cost BOTH selective senses or the reality-map-gated adjoint. (c) TRANSPORT: the even composite commutes with the grading and CAN move the count — and the identity is sharper than the worry: the Hermitian composite of the two disjoint-support middle-form words IS the pseudoscalar, (i/2)[W',W] = Gamma_amb EXACTLY, so the curvature of the two-insertion family is the grading itself, TR-1's own mover direction (motion 18.508381432260148, equal to TR-1's pinned witness because the directions are equal); the model face is identical ((i/2)[sigma_2,sigma_1] = sigma_3 — LD-A's 21.8-unit mass direction IS the composite of the model's two odd directions, n_-(1.5) = -21.845865 re-pinned). But the composite's motion READS class-0 tilt and nothing else: along the LINEAR span of the factors the operator stays odd and the trace is frozen at zero on the whole two-parameter square (max 5.7e-15 over a 5x4 grid), the graded trace is a POINT FUNCTION of the operator so a constant family has no holonomy, and the kernel index is pinned at the graded dimension (exact finite-dimensional identity ind = n_+ - n_-; witnessed constant across a two-parameter odd family) — 0 at the four-corner total's 960|960, movable by external data only (CANON.md:135/139). The composite even term reaches the OPERATOR only as a new nonlinear/second-order term, and the family then leaves CANON.md:136's class at every acting point (neither odd nor even — witnessed) with a non-integer interior trace: TB-2/TB-3 bind verbatim. (d) THE PRICE, enforceable: parity decomposition is exhaustive and unique (even-of-odd = 0, lda:295), so a both-capacities mechanism IS two mechanisms — its odd part and its even part — and TB-5's fork is forced as arithmetic, not rhetoric; TB-4 gains a floor: selective gapping of one one-form corner needs AT LEAST SEVEN middle-form insertions (rank <= 128 per direction, rsc1:444, plus rank subadditivity: 6·128 = 768 < 896 <= 7·128; generic 6-sum rank measured exactly 768, 7-sum 896; seven is odd, so the ST-1 odd-count rule does not collide); and any count carried ACROSS the transition by the mixed family requires exactly the mixed-parity family index theorem of TR-1 section 6 — the composite analysis shows every transport-capable multi-insertion family IS mixed-parity, so the theorem shape is FORCED for every constructor, carried here and not claimed. (e) NO KILL: TR-1's verdicts are per (grading, family) and single-insertion (tr1:242), and the both-capabilities-empty claim (tr1:379) is about single directions; nothing here contradicts either — the composite lattice obeys the same partition. The generation count stays OPEN; SG4 bit 2 stays OPEN; the 2+1 stays subtractive; the 128 stays a placement obligation."
grade: "EXACT for everything load-bearing: composite Z/4 centre arithmetic over the full monomial lattice (integer, CS-1's layer); the centre-scalar rigor clause (class-forbidden implies Hom = 0 exactly); the composite middle-form decompositions by TWO independent instruments (mult-aware Klimyk, and a Racah/Brauer alternating sum over all 322,560 elements of W(D_7) applied to the raw 2,944,656-weight product multisets), dimension-saturated as exact Fractions; the one-way-arrow composition corollary (from TR-1's reproduced Hom facts); the operator identities on the carrier's own Cl(14) fiber algebra (nilpotence, same-sense vanishing, adjoint reversal, A^dag A = P_+, the disjoint-pair commutator = Gamma_amb, all machine-zero residuals); the finite-dimensional index-pinning identity; the seven-insertion floor's inequality (rank subadditivity + rsc1:444's per-direction bound). MODEL-GRADE and typed as such: the freeze-grid and mover witnesses (float64, existence statements), the generic rank witnesses (768 exactly and 896 at 6 and 7 summands — achievability is generic, the floor is the exact clause), the N=24 lattice re-pin, and the sympy 2x2 GB-1 face. 86/86 checks, exit 0; --selftest verifies the clean baseline FIRST (count pinned independently) then catches 12/12 machinery/reference mutations each via its targeted genuine [FAIL] line (0 crash-only, 0 missed, 0 incidental), including a contrary control that flips a verdict row in an artifact COPY; TR-1's entire probe is re-run by subprocess (90/90, exit 0) and SHA-pinned. NOT: an action, a vacuum, a scale, a spectrum, a reality map, a count, a sufficiency proof for gapping, a claim-status movement, or any registry/sidecar/ledger/canon edit."
disposition: FLANK_EXECUTED__DICHOTOMY_IS_COMPOSITION_CLOSED_AT_TERM_LEVEL__EVEN_ORDER_COMPOSITES_HAVE_CLASS_0_AND_LOSE_SELECTION_EXACTLY__SAME_SENSE_ARROW_COMPOSITES_VANISH__DISJOINT_PAIR_COMMUTATOR_IS_THE_PSEUDOSCALAR_SO_THE_CURVATURE_OF_THE_TWO_INSERTION_FAMILY_IS_THE_GRADING__LINEAR_ODD_FAMILIES_FREEZE_ON_THE_WHOLE_SQUARE_AND_INDEX_IS_PINNED_AT_GRADED_DIMENSION__MIXED_ROUTE_EXISTS_AS_TWO_BILLED_MECHANISMS__TB4_GAINS_SEVEN_INSERTION_FLOOR__THEOREM_SHAPE_OF_TR1_S6_IS_FORCED_FOR_EVERY_CONSTRUCTOR__NO_KILL_NO_CLAIM_MOVES
canon_verdict_change: none
row_change: none
registry_change: none
steering_effect: unchanged
canonical_effect: pending_integration
scripts:
  - tests/channel-swings/joe_directed_mp1_composites_inherit_one_horn_never_both.py
depends_on:
  - lab/process/upgrade-program-register.yaml
  - lab/active-research/joe-directed/spectral-transport/tr1-transport-and-selection-are-opposite-parities-2026-08-17.md
  - tests/channel-swings/joe_directed_tr1_transport_and_selection_are_opposite_parities.py
  - lab/active-research/joe-directed/rs-corner/rsc1-unique-channel-lives-on-the-gamma-trace-2026-08-17.md
  - lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md
  - lab/active-research/joe-directed/grading-bridge/gb1-the-bridge-is-one-angle-and-one-missing-arrow-2026-08-17.md
  - lab/active-research/joe-directed/class-shift/cs1-first-order-shift-is-the-chirality-grading-2026-08-15.md
  - lab/active-research/joe-directed/lens-digs/lda-sg4-bit2-type-and-transport-2026-08-17.md
  - CANON.md
  - VERIFICATION.md
  - lab/methods/gu-base-categories.md
  - lab/process/correction-registry.yaml
  - lab/process/homonym-register.yaml
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - papers/drafts/Transcript into the impossible.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result. Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.
>
> The comparator legs here are (i) the repository's own faithful model for the
> `CANON.md:136` row, consumed through TR-1's probe (re-run whole, 90/90,
> SHA-pinned) and re-pinned on a rebuilt N=24 lattice; (ii) a toy composite
> algebra on the carrier's own complexified Cl(14) fiber (`C^128`), used to
> witness algebra-class facts, not spectra; (iii) an exact sympy face of GB-1's
> 2x2 wall-circle model. "Mass", "Dirac-type", "Majorana-type", "spectral
> flow" are comparator/repository vocabulary throughout. The exact
> representation-theoretic statements (composite centre classes, the composite
> Hom-space zeros, the one-way-arrow composition corollary) are structural
> facts about complexified `D_7` on GU's own printed corners and declared
> slots, identical on both SIGNATURE-AMBIENT horns; they move no GU row. This
> artifact adjudicates which COMPOSITE deformation shapes can host, move, or
> select — a semantic-boundary adjudication — hence the classification.

> [!NOTE]
> **Registry debt, declared rather than silently incurred (TR-1's precedent,
> tr1:66-81).** `process_gates/source_native_comparator_routing_audit.py` was
> already red at HEAD before this artifact existed; MP-1 adds one unclassified
> path to the printed gap. The registry row is a transcription owed to the
> method owner (`lab/process/source-native-comparator-routing-registry.json` is
> outside MP-1's write scope). The entry, for the integrator:
>
> ```json
> { "path": "lab/active-research/joe-directed/spectral-transport/mp1-composites-inherit-one-horn-never-both-2026-08-17.md",
>   "classification": "BRIDGE_OR_SEMANTIC_BOUNDARY" }
> ```
>
> The typed-carrier gate's pre-existing red (sibling `selected-k151`) and the
> k149 certificate-shape allowlist red are other lanes' debts, untouched.

## Typed objects

```gu-typed-objects
result:         the composite-parity adjudication at the four-corner declared
                total: every insertion MONOMIAL (tensor composite, operator
                product, or curvature term) carries Z/4 centre class
                2·(number of class-2 factors) mod 4 — so even-order composites
                are class 0, lose selection capability EXACTLY (centre-scalar
                argument plus two weight instruments), and land on TR-1's
                class-0 horn; linear multi-direction families of class-2
                factors stay Gamma-odd everywhere (freeze; index pinned at
                graded dimension); the only both-capacity object is a
                MIXED-parity family, which is two billed mechanisms
carrier:        the four printed corners nu_± in Omega^0(S_±), zeta_± in
                Omega^1(S_±) at complexified D_7 with zeta_± = S_∓(64) (+)
                R^(±)(832) (RSC-1's landed split), and composites of the
                declared bosonic slots' directions: monomials in
                ad P = End(Delta) = Sum_k Lambda^k V_14 content, operator
                products of middle-form one-way arrows, two-parameter
                deformation families and their curvature terms
                LAYER=ambient CHIRALITY=S-FULL-DIRAC
                # The result types composite deformation SHAPES against
                # gradings on the declared total; it selects no half, no
                # corner, no phase, and switches no insertion ON.
pairing:        NONE
# A parity/class result on composites: the only pairing content consumed is
# the SYMMETRY TYPE of invariant bilinears (ST-1's A2 partition and odd-count
# rule, reproduced at composite order), used to type which pairings each
# composite class admits.  No bilinear is evaluated and no Krein realization
# is chosen; bare token per the FX-2 parser, gloss on these comment lines.
real_structure: SOURCE-SILENT and not needed: every exact statement is
                complexified D_7 centre/weight arithmetic, identical on both
                SIGNATURE-AMBIENT horns.  The one place the reality map
                enters is typed and fenced: the reverse arrow of a one-way
                middle-form insertion is its ADJOINT, whose direction the
                inner product/reality map sets (SN-1's fence, inherited
                through TR-1 §3.3 unchanged) — the composite results hold on
                any realization because parities are realization-blind.
grading:        Gamma_W (the Z/4-class package grading) and Gamma_slot (the
                14D volume word), TR-1 §3.1's typed pair, never bare; the
                composite parity statements hold for BOTH on eps-slot
                monomials (form degree preserved) and flip together on
                varpi-slot content exactly as TR-1 §3.2 typed (TB-0's
                declaration duty is inherited, not re-litigated); the
                branched gamma_5 face is consumed from TR-1 §4.4 unchanged
action_owner:   repository-construction (the composite class table, the
                composition corollary, all three instruments); the slots and
                the middle-form direction are source-print (draft eq (9.16),
                (11.6), p.51); whether any insertion is ON — and a fortiori
                whether any PRODUCT of insertions is on — is source-action,
                SG4 bit 2, Lane 1 (CC-06), not decided here
target:         the scope of TB-4/TB-5 against multi-insertion constructions:
                which composite shapes can select, which can move the count,
                and what each is charged MAP-TYPE=not-a-map (an admissibility
                adjudication on composite classes, not a map between carriers)
```

```gu-typed-objects
result:         instrument leg: the composite decompositions
                Lam^7_a (x) Lam^7_b carry zero odd-form content (both
                instruments, dimension-saturated 2,944,656); the one-way
                arrows A = W_h P_+ satisfy A^2 = 0, A A' = 0 (same sense),
                A^dag A = P_+, and (i/2)[W',W] = Gamma_amb for the
                disjoint-support pair; the two-parameter odd family's trace
                is 5.7e-15-frozen on a 5x4 grid while the composite direction
                moves 18.508381432260148 (= TR-1's pinned mover, directions
                identical); the N=24 lattice composite identity
                (i/2)[sigma_2 (x) I, sigma_1 (x) I] = sigma_3 (x) I with
                n_-(1.5) = -21.845865 re-pinned; the sympy wall-circle face
                (angle addition, rigid-pair rotation, sigma_1 sigma_2 =
                i sigma_3)
carrier:        (i) explicit Cl(14) gammas on C^128 (Pauli Kronecker
                construction, TR-1 LEG 5's instrument rebuilt and re-guarded)
                with 7-word insertions and their products — the FIBER algebra
                of the carrier, not the Y14 operator; (ii) the N=24
                periodic-lattice cross-chirality model of
                tests/function-space-ext/dirac_spectral_flow_section.py,
                rebuilt; (iii) M_2(C) with Gamma = sigma_3 (GB-1's wall-circle
                fiber), exact sympy
                LAYER=toy CHIRALITY=S-FULL-DIRAC
pairing:        NONE
# Model (ii) carries LD-A's cross-chirality Krein form in its own owner
# files; nothing here evaluates it — only the graded trace is read.
real_structure: complex Hermitian float64 (i, ii) and exact sympy (iii); no
                quaternionic or real structure used or claimed; explicitly
                NOT the (9,5) real-form fork
grading:        (i) Gamma_amb = i(g_1..g_14), Hermitian involution
                (machine-verified guard, non-vacuous trace); (ii) Gamma =
                sigma_3 (x) I_24; (iii) Gamma = sigma_3.  Each used only
                against its own carrier; no cross-model identification
action_owner:   repository-construction (all three instruments); the
                deformation directions composed are source-print (the middle
                form of ad P; SC-CHI-02's one real coupling)
target:         the algebra class of composite deformations — which composite
                shapes commute with the grading and which freeze
                MAP-TYPE=not-a-map
```

# MP-1 — composites inherit one horn, never both

**The register item, executed.** `TR1-COMPOSITE-PARITY` (register:256-258),
title verbatim in the frontmatter. TR-1 proved the parity dichotomy for
SINGLE insertions — *"a parity computation per (grading, direction) pair, not
a single verdict"* (tr1:242) — and its own postflight named the flank: a
determined constructor could use MULTIPLE insertions whose composite has net
class 0, since *"two class-2 insertions compose to class 0: the composite can
cross-pair while each factor selects"* (tr1:870-871), and the blunt paragraph
ranked it: *"the composite-parity question for multi-insertion stories is the
real open flank"* (tr1:912). MP-1 runs the flank. The answer, measured: the
dichotomy is **composition-closed** — every composite TERM has a definite
parity and sits on exactly one horn, with selection destroyed (not diluted:
destroyed, Hom = 0 exactly) at every even order — and the mixed multi-term
route EXISTS, is constructible from the selective factors themselves (their
commutator IS the grading), and is **priced**: two bills, a seven-insertion
floor, and TR-1 §6's theorem shape forced.

---

## 0. The source frame, carried (quoted, not paraphrased)

Compression re-imports default frames at every boundary; the operative
sentences travel verbatim with their IDs.

- **CC-06-CHIRALITY-VEV-CONDITIONAL** (`correction-registry.yaml:300-306`),
  superseded reading quoted: *"That the source has no stated
  effective-chirality mechanism, or that chirality can only come from an
  unbuilt mirror-gapping condensate -- when observed chirality is
  VEV-CONDITIONAL ('exactly three families of chiral fermions if you have a
  decreased VEV ... taking a Dirac equation into two [Weyl] equations',
  drafts L158 / ucsd 00:46:02) and the selector is SG4 bit 2, OPEN by
  design."* The register row it binds, `SC-CHI-01` (register:913-915),
  verbatim: *"Thus we assert that a non-chiral total theory splits at the
  emergent level into two separate chiral theories and that the one above the
  dashed line corresponds to matter in our world with the other sectors not
  labeled by F to the left and above the line are currently dark to us."* —
  conditioned in the same passage (extraction:137-138) on *"no vacuum
  expectation value pulling the various sub-fields of ϖ to values
  significantly above zero"*. The declared total is NON-CHIRAL; the split is
  emergent-level and VEV-conditional; **SG4 bit 2 is OPEN and nothing here
  closes it or assumes it closed** — MP-1 types composite SHAPES, and whether
  any of them is ON is Lane 1's.
- **CC-05-SUBTRACTIVE-TWO-PLUS-ONE** (`correction-registry.yaml:264-269`),
  quoted: *"That three generations is an ADDITIVE target count a mechanism
  must produce -- when the source says it is 'really two plus one' with the
  third family an imposter 'for representation theoretic reasons' (drafts
  L119/L128/L131) and HE-1 makes the partition FORCED and SUBTRACTIVE
  (n_g -> n_g - 1, unlabelled, the distinguished family REMOVED)."* Nothing
  below treats three as an additive production target; the seven-insertion
  floor is a GAPPING price, not a count.
- **CC-08-DARK-PARTNER-OBLIGATION** (`correction-registry.yaml:364-368`),
  quoted: *"That the 128 remainder is an established DEFECT of the
  construction -- when the source NAMES dark sectors (Looking-Glass, dark
  spinorial, 'Rarita-Schwinger matter and more') and the remainder is a
  partner-placement / decoupling OBLIGATION, not an established defect."*
  The 128 appears below only through RSC-1's rank bound inside the TB-4
  floor, as an obligation whose gapping is priced, never as a defect.
- **The CHIRAL homonym fence** (`homonym-register.yaml:242-265`),
  disambiguator quoted: *"write \"CHIRAL (massless/unbroken)\" when naming
  the PHASE value ... for handedness use the CN-2 closed vocabulary
  S-HALF-SAME / S-HALF-OPPOSITE, never the bare word."* Below, phase values
  are written "CHIRAL (massless/unbroken)" / "MASSIVE" when named at all;
  handedness content is carried by the typed gradings (Γ_W, Γ_slot, γ₅) and
  the CN-2 tokens of the typed-objects blocks, never by the bare word.

---

## 1. PREFLIGHT — six problem-matched lenses, declared before the work

- **L1 — Composition/lattice arithmetician.** Concern: "composite" is four
  different constructions (sum, tensor monomial, operator product, group
  composite), and an argument that types one and quietly quotes another is
  the classic way a dichotomy "survives" vacuously. Design answer: §3 types
  all four with their DIFFERENT algebra (sums do not add classes; monomials
  do), each with its own instrument, and the verdict table carries them as
  separate rows. This lens decided the file's structure: the register title's
  "compose to class 0" is true of monomials and products, FALSE of sums —
  and the sum case is where the freeze already wins.
- **L2 — Central-character rigorist (CS-1's instrument, inherited).**
  Concern: "class-forbidden" must be upgraded from allowedness bookkeeping to
  an exact vanishing theorem before "capability destroyed" may be claimed —
  and a weight computation that disagrees with the centre is wrong. Design
  answer: §4 states the centre-scalar argument as a one-paragraph proof
  (centre acts by i^cls on each irrep; an invariant needs total scalar 1),
  and every composite zero is ALSO computed by two independent weight
  instruments with dimension saturation; no clause argues from class alone
  where a Hom space is claimed (RSC-1's Lens-2 binding, inherited).
- **L3 — Coverage archaeologist.** Concern: eight false-novelty claims in one
  session is the documented failure mode; ST-1 already computed a
  two-insertion composite (st1:308) and named the two-insertion escape as its
  own contrary route (st1:195-199). Design answer: retrieval-first (§2) with
  the honest ratio printed; ST-1's two-insertion zero, its odd-count rule
  (st1:229, :456), TR-1's parity table and one-way arrows, RSC-1's rank
  bound, GB-1's circle and CS-1's corollary (d) are all consumed with [R]
  tags; the novelty claims are confined to the composite-lattice closure, the
  same-sense vanishing, the commutator identity, the index pinning, and the
  seven-insertion floor — each grep-certified as phrased (§2).
- **L4 — Layer-typist (gu-base-categories discipline).** Concern: the
  unforgivable regression (base-categories §1.4: crediting L1 with chirality
  or the package with an unconditional spectrum) has a composite-specific
  variant — reading "the constructor can build a mixed route" as "GU has a
  mechanism". Design answer: every constructive clause below is a KINEMATIC
  availability statement at a named layer; the mixed route is typed as a
  shape a constructor WOULD have to pay for, with the ON switch owned by
  Lane 1 (CC-06); §10 lists the misreadings fenced.
- **L5 — Source-fidelity reader.** Concern: the source supplies ONE stylized
  coupling (`SC-CHI-02`, register:940, one real coupling) — a multi-insertion
  story is repository extrapolation, and the artifact must not smuggle it
  into the source's mouth. Design answer: the constructor is named as a
  hypothetical adversary of TB-5 throughout (the register item's own frame:
  "a determined constructor"); no source sentence is read as asserting
  multiple insertions; the source's own hedge plural ("sub-fields", p.52) is
  cited only where B1P-1 already banked it, unchanged.
- **L6 — Hostile self-auditor (the import/deflate lens).** Concern: two
  seductive failures — inflating "the commutator is the grading" into "GU
  generates its own transport" (an import of a dynamical mechanism from a
  kinematic identity), and deflating the mixed route into "forbidden" (which
  would overclaim against the register title's own expectation of pricing).
  Design answer: §9 runs both attacks; the commutator identity is typed as
  algebra (what the direction IS, not what dynamics does); the mixed route
  is typed EXISTS-PRICED with its bills; the kill-question (§5.4) quotes
  TR-1's actual claim before declaring the non-kill.

**Kill-or-switch, recorded before computing.** If any even-order composite
had carried a nonzero same-half self-pairing Hom (violating the centre
argument at the carrier), the dichotomy would fail under composition, TB-5
would be unenforceable against two-insertion stories, and this artifact would
have led with the counterexample and a demand to rewrite TB-5. Measured: the
composite odd-form content is zero at the decisive object, twice,
dimension-saturated — the dichotomy is composition-closed.

---

## 2. Retrieval — prior art, the honest ratio, and the novelty greps

Searched before computing, by object and by mechanism: *composite, two
class-2 insertions, two-insertion, multi-insertion, compose, product of
insertions, nilpotent, one-way, odd count, commutator, curvature, holonomy,
path-dependence, rank bound, seven, mixed-parity, angle addition.*

| Banked result consumed | Owner | Status |
|---|---|---|
| The parity dichotomy per (grading, direction); the class table; the freeze lemma; the one-way Hom facts; the branched collapse; TB-0..TB-5; the §6 theorem shape; the P4 flank | TR-1 (tr1:242, :356-379, :395-400, :416-424, :560-572, :581-603, :869-872, :912-913) | **[R] consumed and re-verified**: the whole TR-1 probe re-run by subprocess (90/90, exit 0, SHA-pinned); the four one-way Hom facts recomputed on this probe's own Klimyk (LEG 3) |
| "Class-0 content never unlocks a same-class pairing; the class-2 insertion count must be ODD" (A2); "Same-class pairings need total insertion class 2, hence an ODD number of class-2 insertions"; the two-insertion contrary route COMPUTED: "TWO class-2 insertions (Λ^3 ⊗ Λ^3, ν_+ diagonal): 0" | ST-1 (st1:456, :229, :195-199, :308) | **[R] reproduced from the class side at composite order** (LEG 2); ST-1's single computed two-insertion instance is the banked seed this file generalizes — the honest-ratio anchor |
| The class-shift rule `c_F = c_E + 2k` and corollary (d): insertions shift by `c_T`, `ad P` carries classes {0,2} only, "can NEVER change (b)" — **the centre-class arithmetic owner** | CS-1 (cs1:216, :234-238; corollary (e) at :239-242) | **[R] quoted and consumed as the composite-class law's owner**; the composite table (§3) is corollary (d) iterated, nothing more |
| ζ_± = S_∓(64) ⊕ R^(±)(832); the rank bound "rank ≤ 2·64 = 128, at least 832 − 64 = 768 directions of R stay unpaired" | RSC-1 (rsc1:341, :444) | **[R] quoted, not recomputed**; composed with subadditivity into the §6 floor |
| The wall circle; angle addition U(θ)M(φ)U(θ)† = M(φ−θ); "the charge in the TRANSPORTED grading never moves" | GB-1 (gb1:317, :330-336) | **[R]**: the 2x2 face re-verified in exact sympy (LEG 5); §3.4 types the circle as the model face of successive-deformation composites |
| canon-class ∩ {[D,Γ]=0} = {0} (the parity-decomposition exhaustiveness seed); the LD-A model numbers | LD-A (lda:295, :303-316) via TR-1 LEG 6 | **[R]**: the intersection identity re-witnessed; n_-(1.5) = −21.845865 re-pinned on a rebuilt lattice (LEG 4) |
| CANON.md:135/:136/:139 verbatim; "the only unconditionally computable integer is 1" | canon; VERIFICATION.md:24 | **quoted and pinned** |
| "Nonzero cross blocks can compose to zero" — a pencil-valuation counterexample (CA⁻¹B = 0 with nonzero blocks) | SN-2 (`majorana-126-neutrino/sn2-neutral-pencil-valuation-classifier-2026-08-16.md:346`) | **read and typed UNRELATED**: matrix blocks of a seesaw pencil, not insertion composites; recorded so the collision is countable |

**Honest ratio: roughly 35% of the exact content is reproduction** ([R]-tagged
in the probe before anything new runs) — and the file's seed sentence is
ST-1's own contrary route, executed once at one pair and generalized here.
**The novelty greps, recorded per the AR-3 rule** (zero hits is NOT evidence
of new; these certify only that the phrasings and objects do not occur):
`grep -rF "composition-closed"` (0), `"inherit one horn"` (0), `"composite
insertion"` (0), `"composite parity"` (0; hyphenated `"composite-parity"` 2 —
the register item and TR-1's P4, i.e. the task itself), `"curvature of the
two-insertion"` (0), `"seven insertions"` / `"seven middle-form"` (0),
`"price floor"` (0), `"compose to zero"` (1 — SN-2's pencil counterexample,
read, unrelated, table row above), `"two-insertion"` (ST-1's artifact and
probe — the banked seed), `"nilpotent one-way"` (TR-1 only), `"same-sense"`
(0). Probe LEG 7 re-executes four exact-phrase scans over the corpus with a
planted positive and a planted near-miss.

---

## 3. (a) The typing: what a composite IS, shape by shape

The register title says "two class-2 insertions compose to class 0". Measured:
that sentence is true for exactly TWO of the four constructions a constructor
could mean by it, and the other two are where the fence already holds. All
four, typed, with the net Z/4 centre class computed (class arithmetic owner:
CS-1, corollary (d), quoted in §2):

| # | composite shape | what it is | net Z/4 class | grading behavior | instrument |
|---|---|---|---|---|---|
| C-i | **linear multi-direction family** `D(s,t) = D_0 + f(s)·M_1 + g(t)·M_2`, both factors class-2 | a SUM — an affine family inside the Γ-odd subspace; **not a class composite at all** (classes add over ⊗, not over +; each TERM keeps class 2) | each term 2 | Γ-odd at every (s,t): the freeze lemma (tr1:416-424) applies pointwise on the whole square | LEG 4 grid |
| C-ii | **tensor/monomial composite in an invariant** `T_1 ⊗ … ⊗ T_n` (the n-insertion term of a pairing) | the object of ST-1's insertion bookkeeping; cls = Σ cls(T_i) = 2·#(class-2 factors) mod 4 | **0 for even count, 2 for odd** — no third value | pairing capability set by the total class (§4) | LEG 2, LEG 3 |
| C-iii | **operator product** `M_1 M_2` (composition in End of the corner space; equivalently a second-order/nonlinear insertion term) | shifts content class by cls(T_1)+cls(T_2); for two class-2 factors: by 0 | **0** (Γ-even: commutes with Γ_W and Γ_slot on ε-slot content) | the register title's object; decomposes as `M_1M_2 = ½{M_1,M_2} + ½[M_1,M_2]`, both parts Γ-even; the anticommutator is exactly the cross term of `D(s,t)²` (the GAPPING face), the commutator is the curvature of the two-parameter family (the TILT face) | LEG 4 |
| C-iv | **successive deformation / rotation composite** (conjugation by the grading-rotating unitaries, GB-1's circle) | group composition; `U(θ_1)U(θ_2) = U(θ_1+θ_2)` — a composite of rotations is a rotation | not a class object; acts on directions | rotates mass direction and grading RIGIDLY, so GB-1's invariant survives every composite: *"the charge in the TRANSPORTED grading never moves"* (gb1:332) — the no-transport verdict is composition-stable | LEG 5 (exact sympy) |

**The slot generality.** C-ii's arithmetic covers BOTH declared slots and all
mixtures: ε-slot factors carry cls(Λ^k) = 2k mod 4, ϖ-slot content carries
cls(V ⊗ Λ^k) = 2+2k mod 4 (TR-1 §3.2's table, consumed), and the composite
class is the sum regardless of provenance. The Γ_W/Γ_slot parity flip on
ϖ-slot content (TB-0's reason) passes through composition unchanged: a
monomial's Δp is the sum of its factors' Δp, so the two ambient gradings
agree on a composite exactly when the total form-degree shift is even — typed
here, and TB-0's declaration duty is inherited, not weakened.

**GB-1's angle arithmetic IS the model face of this question — in two typed
senses, and only two.** (1) The CONJUGATION composite: angles add, the
(operator, grading) pair rotates rigidly, and the relative angle — GB-1's
exact invariant — is preserved by every composite rotation; so successive-
deformation composites inherit GB-1's no-transport result rather than eroding
it (LEG 5 verifies `U(θ_1)U(θ_2) = U(θ_1+θ_2)` and the rigid-pair identity in
exact sympy). (2) The PRODUCT composite: the model fiber's Z/2 parity algebra
(σ_1, σ_2 odd; I, σ_3 even w.r.t. Γ = σ_3) multiplies parities exactly as
the Z/4 classes add — `σ_1σ_2 = iσ_3`: odd·odd = even — and the identity
`M(φ_1)M(φ_2) = cos(φ_1−φ_2)·I + i·sin(φ_1−φ_2)·σ_1` (exact sympy) shows the
composite's even/odd split is a function of the RELATIVE ANGLE alone. What
GB-1's circle is NOT: a face of the sum shape C-i (a sum of two gapping-plane
directions is another direction in the plane, not a parity event). The model
face of C-i is the freeze grid itself.

---

## 4. (b) The selection leg: capability is DESTROYED at even order, not inherited

### 4.1 The centre-scalar argument (exact, the carrier proof)

The centre of `Spin(14)` (m = 7 odd: Z/4, faithful — CS-1 H1) acts on every
irreducible summand of every object here by the scalar `i^cls`. An invariant
vector in a tensor product exists only if the product of the central scalars
is 1, i.e. only if the total class is 0 mod 4. This is not an allowedness
heuristic: **a nonzero Hom out of (or invariant in) a product whose total
class is nonzero would intertwine two different central characters and is
therefore EXACTLY zero.** Applied to composites:

```
    same-half self-pairing with an n-factor composite:
        cls(W_± ⊗ W_± ⊗ T_1 ⊗ … ⊗ T_n) = 2 + 2·#(class-2 factors)  mod 4
        = 0  iff  #(class-2 factors) is ODD
    cross-half pairing with the same composite:
        cls(W_+ ⊗ W_- ⊗ T_1 ⊗ … ⊗ T_n) = 0 + 2·#(class-2 factors)  mod 4
        = 0  iff  #(class-2 factors) is EVEN
```

So over the ENTIRE composite lattice — any number of factors, any mixture of
slots — selection capability (a same-half self-pairing) belongs to the
odd-count monomials and cross-pairing to the even-count monomials, and the
both-capabilities set is empty at every order (probe LEG 2 checks the
partition over the lattice and 5 planted-false propositions). This is exactly
ST-1's A2 — *"Class-0 content never unlocks a same-class pairing; the class-2
insertion count must be ODD"* (st1:456) — now read as the
**composition-closure of TR-1's dichotomy**: the one integer that decides
both capacities for a single direction decides them for every composite term,
oppositely, by the same arithmetic. A class-0 composite of two class-2
insertions does not inherit its factors' selection capability; it loses it
with certainty. The worry's phrase "the composite can cross-pair while each
factor selects" (tr1:871) is TRUE and is the whole point: cross-pairing is
the OTHER horn.

### 4.2 The weight instruments: the decisive object, computed twice (new)

The class argument is rigorous, but per this file's L2 binding a Hom claim
carries a weight computation. The decisive object is the two-middle-form
composite `Λ^7_a ⊗ Λ^7_b` (dim 1716² = 2,944,656) — the composite of the
source's own selective directions. Computed by mult-aware Klimyk AND by a
Racah/Brauer alternating sum over all 322,560 elements of W(D_7) applied to
the raw product weight multisets (RSC-1's two-instrument precedent), both
dimension-saturated as exact Fractions:

```
    odd-form content of Lam^7_- (x) Lam^7_-:   Lam^1: 0  Lam^3: 0  Lam^5: 0  Lam^7_+: 0  Lam^7_-: 0
    odd-form content of Lam^7_+ (x) Lam^7_-:   Lam^1: 0  Lam^3: 0  Lam^5: 0  Lam^7_+: 0  Lam^7_-: 0
    Inv(Lam^7_- (x) Lam^7_-) = 0          (the same-sense pair closes NO invariant loop)
    Inv(Lam^7_+ (x) Lam^7_-) = 1          (the opposite-sense pair closes exactly one)
    mult(Lam^2, Lam^7_+ (x) Lam^7_-) = 1  (planted positive: the detector sees even content)
    mult(2·hw(Lam^7_-), Lam^7_- (x) Lam^7_-) = 1  (planted positive on the same-sense product)
```

Every irreducible constituent of the composite is an even form or an
even-class mixed-weight module — the composite is 100% class-0 content, as
the centre requires, with zero remainder against the dimension count. ST-1's
single computed instance (`Λ^3 ⊗ Λ^3` on the ν_+ diagonal = 0, st1:308) is
the banked seed; this is the same fact at the selective directions
themselves, with the full composite decomposed.

### 4.3 The operator face: same-sense composites VANISH (new, exact)

TR-1 §3.3's four Hom facts (reproduced on this probe's Klimyk, [R]) say the
middle-form insertion is one-way: `dim Hom(Λ^7_- ⊗ S_+, S_-) = 1`,
`dim Hom(Λ^7_- ⊗ S_-, S_+) = 0`, same-half maps 0. Corollary, exact with no
new computation: the insertion operator `A` of any Λ^7_- direction kills S_-
and maps S_+ into S_-, so for ANY TWO same-sense arrows `A, A'`:

```
    A ∘ A' = 0        (the middle leg is the vanishing Hom)
    in particular A² = 0  — TR-1's nilpotence (tr1:400), now closed under composition
```

Witnessed on the carrier's own Cl(14) fiber algebra at machine zero
(LEG 4: `A = W_h P_+` with `P_± = (1±Γ_amb)/2`; `‖A²‖ = 0`, `‖AA'‖ = 0`
exactly for two distinct 7-words), together with the guards that make the
statements non-vacuous and the structural identities
`A† = W_h P_∓` (the reverse arrow IS the adjoint — SN-1's reality-map fence
inherited unchanged) and `A†A = P_+` **exactly**: the "mass-squared" of a
one-way arrow is the half-projector — gapping content in D², never count
content. Consequence for the constructor: **the nonzero composites cost both
selective senses** (a Λ^7_+ factor with a Λ^7_- factor), or the adjoint
(reality-map-gated). And the two senses together are per-half-selection-
neutral at the pairing level — ST-1 §4.5's table gives `Λ^7_-` to Sym²(ν_+)
and `Λ^7_+` to Sym²(ν_-) symmetrically — so a constructor who buys the
nonzero composite has bought BOTH halves' self-shapes and must re-break the
symmetry by coefficient/vacuum choice, which is Lane-1 business (V1/V2), not
carrier structure. Selection does not survive the purchase as structure.

### 4.4 Verdict, selection leg

**CAPABILITY-DESTROYED-AT-EVEN-ORDER.** A class-0 composite of two class-2
insertions inherits nothing of its factors' selection capability: the
same-half self-pairing Hom space is exactly zero (centre argument, §4.1),
the composite content is 100% class-0 (two instruments, §4.2), and the
same-sense operator composites vanish identically (§4.3). The dichotomy's
selection horn is composition-closed. Proved at the carrier; the model faces
(§5) agree.

---

## 5. (c) The transport leg: what the composite can move, and what its motion means

### 5.1 The composite commutes — and the sharpest identity in the file

Class 0 ⟹ the composite commutes with the grading (TR-1's parity theorem,
consumed; Cl(14) witnesses at machine zero). Sharper, and new: for the two
disjoint-support middle-form words `W = i·g_1…g_7`, `W' = i·g_8…g_14`
(Hermitian, Γ-odd, anticommuting — disjoint 7-words anticommute since
(−1)^{49} = −1), the Hermitian composite is

```
    (i/2)[W', W]  =  Γ_amb        EXACTLY  (machine-zero residual on C^128)
```

**The curvature of the two-insertion family is the grading itself.** The
even output of the two odd selective factors is not merely SOME class-0
direction — it is the Λ^14 pseudoscalar, TR-1's own Γ-aligned mover, and
inserting it moves the graded trace by **18.508381432260148** — equal to
TR-1's pinned mover witness to the last digit because the directions are
equal, not merely similar (LEG 4; same D_0, same coefficient). The model face
is the same identity one level down: `(i/2)[σ_2 ⊗ I, σ_1 ⊗ I] = σ_3 ⊗ I` on
the N=24 lattice — **LD-A's 21.8-unit mass direction IS the composite of the
model's two odd directions** — with `n_-(1.5) = −21.845865` re-pinned on the
rebuilt lattice ([R]). The overlap rule is typed with it: 7-words sharing s
indices commute for s odd and anticommute for s even, so the surviving
Hermitian composite is the anticommutator `½{M_1,M_2}` (the exact cross term
of `D(s,t)²` — gapping) for commuting pairs and the commutator
`(i/2)[M_1,M_2]` (the tilt) for anticommuting pairs; both are Γ-even; both
are class-0 content and selection-blind by §4.

### 5.2 But the linear family never carries it: the freeze on the whole square

The constructor's actual handle on the declared slots is the linear family
C-i. Every term is Γ-odd; the sum is Γ-odd; the freeze lemma applies at every
(s,t): witnessed max |tr(Γ P_<0)| = **5.7e-15 over a 5×4 grid** in both
parameters (LEG 4). The composite's evenness reaches the OPERATOR only as a
new term — a second-order/nonlinear insertion, exactly the escape SN-1 named
and ST-1 §4.4 quoted (*"a separately scaled heavy owner, independently scaled
components, or a nonlinear insertion"*) — at which point the family is
MIXED-parity: at every acting point it is neither Γ-odd nor Γ-even
(witnessed: commutator and anticommutator with Γ both nonzero), it has left
`CANON.md:136`'s class — verbatim *"Net chiral spectral flow 0 for
self-adjoint, chirality-odd, Krein-self-adjoint Fredholm families"* — and its
interior trace is non-integer (witnessed −6.446…): **TB-2 and TB-3 bind the
composite route word for word.**

### 5.3 Holonomy: the count is a point function, and a constant has none

Task (c) asks whether the two-parameter family hides a path-dependence. In
finite dimensions the answer is structural: `tr(Γ P_<0)` is a spectral
functional of the OPERATOR — a point function on the family's parameter
space, not a path integral — so "transport" along any path is just the
difference of endpoint values. On the odd family the function is identically
zero (§5.2), and a constant function has trivial holonomy: no ordering of
the two insertions, no loop in the (s,t) square, and no interleaving of
selective factors can accumulate a count. Where path-dependence CAN enter is
exactly where the finite-dimensional argument ends: the infinite-dimensional
eta layer, where the difference of spectral asymmetries along a family is
computed by an integrated density and gapless crossings contribute — which is
precisely the missing identity TR-1 §6 names (*"a mixed-parity family index
theorem"*, tr1:581-582, with its eta-type endpoint terms). The holonomy
question does not evade the theorem shape; it IS the theorem shape, carried
and not claimed.

### 5.4 The kernel index is pinned at the graded dimension (exact, new as stated)

For a Γ-odd operator on a finite graded space, `ind = dim ker_+ − dim ker_-
= n_+ − n_-` IDENTICALLY (the off-diagonal block B : V_+ → V_- has
`dim ker B − dim ker B† = n_+ − rank B − (n_- − rank B†)` and
`rank B = rank B†`). So along EVERY interior odd family — one insertion or
fifty, linear or not — the odd class's only count object (TR-1 §4.2) is
frozen at the structural constant. Witnessed: constant index 1 across a
two-parameter odd family on 3|2 halves; generic zero kernel on equal halves
(LEG 4). At the four-corner declared total the halves are 960|960 (CS-1's
banked split, consumed), so the interior index is 0 for every composite
construction, and only EXTERNAL data — unequal graded content: flux, winding
(CANON.md:135 *"net chiral index = flux number"*; :139 *"any odd count is
necessarily external"*) — can move it. Composition does not touch canon's
doorway; it walks through it or not at all. `VERIFICATION.md:24`'s *"the only
unconditionally computable integer is 1"* sits exactly here, unmoved.

### 5.5 Verdict, transport leg

**MIXED-ROUTE-EXISTS-PRICED.** The composite can commute, and can move the
trace — the constructor can even manufacture the maximal mover from the
selective factors themselves (§5.1). But its motion reads class-0 tilt
(LD-A's phenomenon, model-matched, selection-empty by §4), the linear family
never carries it (frozen on the square), the count has no holonomy to
exploit, the kernel index is pinned at the graded dimension, and the moment
the even term enters the operator the family is mixed-parity and owes TB-2,
TB-3, and §6's theorem. Nothing is prohibited; everything is billed.

---

## 6. (d) THE PRICE — what TB-4/TB-5 actually charge the mixed route

Enforceable prose, composing with (never superseding) TR-1's TB-0..TB-5:

- **P-1 — THE PARITY SPLIT IS EXHAUSTIVE (TB-5 enforced as arithmetic).**
  Every deformation term decomposes uniquely as even part + odd part
  (`M = ½(M + ΓMΓ) + ½(M − ΓMΓ)`; the intersection is {0} — *"canon-class ∩
  {[D,Gamma] = 0} = {0}"*, lda:295, re-witnessed), and every insertion
  MONOMIAL is already parity-pure (§3 C-ii/C-iii). So a mechanism claiming
  both capacities is not facing a rhetorical fork: it IS two mechanisms — its
  odd part (selects; frozen trace; pinned index) and its even part (moves;
  selection-blind) — and TB-5's clause *"either two mechanisms (name both,
  bill both) or an error"* (tr1:572) is hereby enforced by decomposition:
  name the odd part and pay TB-1; name the even part and pay TB-2/TB-3.
  There is no third part.
- **P-2 — THE TB-4 FLOOR: SEVEN.** TB-4 (tr1:560-566) already prices the
  gapped endpoint by RSC-1's rank bound at one insertion. Composed with rank
  subadditivity it becomes a floor: every middle-form direction's induced
  Grassmann-live form on a one-form corner has rank ≤ 128 (*"rank ≤ 2·64 =
  128"*, rsc1:444), so k selective insertions yield rank ≤ 128k, and
  nondegeneracy on the 896 needs `128k ≥ 896`, i.e. **k ≥ 7** — with 6
  insertions at least 128 directions stay unpaired (generic 6-sum rank
  measured exactly 768; 7-sum reaches 896, so the floor is tight as an
  inequality). Seven is ODD, so the floor does not collide with ST-1's
  odd-count rule (st1:456) — the arithmetic composes. NECESSARY, not
  sufficient: achievability at 7 is a generic-witness statement (model
  grade), invariant-compatibility at 7 is NOT computed here, and every
  blind supplement (Λ^1, Λ^5 content, which CAN touch Λ²(R)) re-raises the
  selectivity problem exactly as TB-4 already says.
- **P-3 — THE BOTH-SENSES PRICE.** Nonzero even composites of selective
  factors require both selective senses or the adjoint (§4.3). Both senses
  feed both halves' self-shapes symmetrically; the adjoint is set by the
  reality map (SOURCE-SILENT, SN-1's fence). So the mixed route's selective
  leg is symmetric-until-broken, and the breaking is a vacuum/coefficient
  selection owned by Lane 1 (SG4 bit 2; CC-06) — a named condition, not
  carrier structure. Any composite-route argument must state which of the
  two prices it pays.
- **P-4 — THE THEOREM DEMAND IS UNIVERSAL.** Any argument carrying a count
  ACROSS the transition on a family with transport capability is, by §3-§5,
  on a MIXED-parity family — so the identity it needs is exactly TR-1 §6's
  mixed-parity family index theorem, with §6's consumption list unchanged
  (reality map; source action; Fredholm endpoint per TB-4; W3/W6 bridges).
  The composite analysis removes the last apparent alternative: there is no
  parity-pure multi-insertion family with both capacities to state a cheaper
  theorem on. The theorem shape is FORCED for every constructor — carried
  here, not claimed, exactly as TR-1 fenced it.

**The strengthened fence, stated once.** *Composition-closed dichotomy:*
over the full composite lattice of declared-slot content (both slots, all
orders, all four shapes of §3), selection capability ⟺ total class 2 ⟺ odd
class-2 factor count, and grading-evenness ⟺ total class 0 ⟺ even count; the
both-capabilities set is empty at every order. What would still evade it:
(i) the §6 theorem itself, proved (that is discharge, not evasion); (ii)
external/topological data moving the kernel index through canon's doorway
(that is the licensed route, CANON.md:135/139, RW-1's wall under its W-bill);
(iii) content outside the declared slots or outside Spin(14)-equivariance
(H3's named escape in CS-1 — e.g. the inhomogeneous gauge action), which
would be a NEW mechanism to type, not a composite of the typed ones.

---

## 7. Machine-readable verdicts and certificate

<!-- MP1-TABLE-BEGIN -->

| id | question | verdict | evidence_key |
|---|---|---|---|
| CP-TERM | does any composite term carry both capacities | DICHOTOMY-CLOSES-UNDER-COMPOSITION | composite_lattice_partition |
| CP-SEL | does a class-0 composite of class-2 insertions inherit selection | CAPABILITY-DESTROYED-AT-EVEN-ORDER | composite_odd_content_zero |
| CP-TRA | can the composite move the count, and what does its motion read | MIXED-ROUTE-EXISTS-PRICED | commutator_is_grading_and_freeze_grid |
| CP-HOL | does the two-parameter family hide a path-dependent count | NO-HOLONOMY-POINT-FUNCTION | grid_frozen_and_index_pinned |
| CP-PRICE | what do TB-4/TB-5 charge the mixed route | TWO-BILLS-PLUS-SEVEN-FLOOR | parity_split_and_rank_floor |
| CP-KILL | does any of this kill TR-1's dichotomy | NO-KILL-CLAIM-RESTATED | tr1_rerun_green |

<!-- MP1-TABLE-END -->

**Verdict-evidence binding** (probe LEG 8 enforces this; the selftest's
contrary control flips CP-TERM in a temp copy and must be caught):

| evidence_key | asserted direction | measured |
|---|---|---|
| composite_lattice_partition | CLOSES requires the both-capabilities set empty over the composite lattice | empty at every checked order; 5 planted-false propositions False |
| composite_odd_content_zero | DESTROYED requires zero odd-form content in Λ^7⊗Λ^7 by both instruments, dimension-saturated | 0 on all five odd targets × both products × both instruments; saturation 2,944,656 exact; planted positives fire |
| commutator_is_grading_and_freeze_grid | EXISTS requires the composite mover identity and its motion; PRICED requires the linear-family freeze and the mixed-class exit | (i/2)[W′,W] = Γ_amb at machine zero; motion 18.508381432260148 = TR-1 pin; grid max 5.7e-15; mixed family neither odd nor even, interior trace non-integer |
| grid_frozen_and_index_pinned | NO-HOLONOMY requires the trace constant on the square and the index constant on the family | grid max 5.7e-15; index ≡ 1 on 3|2 across the family, = n_+ − n_- |
| parity_split_and_rank_floor | TWO-BILLS requires unique parity decomposition (even∩odd = {0}); SEVEN requires 6·128 < 896 ≤ 7·128 with witnesses | decomposition exact over draws; ranks 768 (six) and 896 (seven) measured; ceil(896/128) = 7 |
| tr1_rerun_green | NO-KILL requires TR-1's probe green at HEAD and its claim quoted per-(grading, family) | subprocess 90/90 exit 0, SHA-pinned; tr1:242, :379 byte-pinned |

**Certificate.**
`tests/channel-swings/joe_directed_mp1_composites_inherit_one_horn_never_both.py`

- Live run: **86/86 checks, exit 0** (~30 s). Eight legs: 30 byte pins +
  planted negative + the TR-1-probe SHA pin; the composite class-arithmetic
  leg (lattice partition, both slots, planted-false propositions); the exact
  D_7 weight leg (mult-aware Klimyk AND the 322,560-element Racah/Brauer sum
  on the raw 2,944,656-weight product multisets, dimension saturation as
  exact Fractions, TR-1's four one-way Hom facts [R], planted positives on
  both products); the Cl(14) leg (Hermitian-involution guards, one-way
  nilpotence and same-sense vanishing, adjoint reversal, A†A = P_+, the
  disjoint-pair commutator = Γ_amb, the freeze grid, the composite-mover
  agreement with TR-1's pin, mixed-class exit with non-integer interior
  witness, parity-decomposition uniqueness, the index-pinning witnesses, the
  N=24 lattice composite identity with the LD-A pin); the exact-sympy GB-1
  face (angle addition, rigid-pair rotation, parity products, the
  relative-angle product identity); the rank-floor leg (exact inequality +
  generic 768/896 witnesses + subadditivity guard); the TR-1 whole-probe
  re-run by subprocess (90/90 pinned); the certified absence scans; the
  artifact binding with SHA-pinned verdict table.
- `--selftest`: clean baseline verified FIRST (86 green, 0 [FAIL], count
  pinned independently at 86) and a red baseline aborts; **12/12 mutations
  caught, each via the genuine [FAIL] line of the check TARGETED at it**
  (pin line number, TR-1-probe SHA, class table, composite-class arithmetic,
  Klimyk reflection sign, product-multiset builder,
  Racah sign-set generator, half-projector machinery, freeze-grid machinery,
  mover pin, absence detector — plus the artifact-copy contrary control that
  flips CP-TERM); a crash is CRASH-NOT-DETECTION and fails; an untargeted
  catch is INCIDENTAL-NOT-TARGETED and fails; the failing check is printed
  for every mutation; the baseline is re-verified after the mutations.
  Exit 0.
- Read-only; mutation copies live in a temp directory. Deterministic;
  numpy + sympy + stdlib.
- What the probe does NOT certify: sufficiency of seven insertions (only the
  floor); anything about the true RS Y¹⁴ bundle or the (9,5) real form; any
  spectrum, scale, or vacuum; the infinite-dimensional eta layer (§5.3's
  fence); and any future file phrasing the same result in words outside the
  scanned families.

---

## 8. What each consumer inherits, per layer — versionless deltas, none a launder

1. **TR1-COMPOSITE-PARITY owner (upgrade register).** The item is EXECUTED:
   the flank is closed at the term level and priced at the mechanism level;
   the register title's own expectation ("meets TB-4/TB-5 as pricing, not
   prohibition") is confirmed with the pricing made exact (P-1..P-4).
   Proposed disposition (integrator's call): status DONE with receipt = this
   artifact + probe; the theorem half stays live under TR-1 §6's shape,
   which §6/P-4 here make UNIVERSAL for transport-capable constructors.
2. **TR-1.** P4's flank is discharged; the dichotomy gains its
   composition-closure clause (§6 fence); TB-4 gains the seven-insertion
   floor and TB-5 gains the parity-decomposition enforcement (P-1) — both
   proposed as versionless strengthenings for the integrator, neither
   relaxing anything. TR-1's verdicts, table, and claim ceiling are
   untouched (its probe re-run green inside this probe).
3. **ST-1.** A2's odd-count rule acquires its full composite reading: the
   rule IS the composition-closure of the dichotomy's selection horn, and
   ST-1's own two-insertion contrary route (st1:195-199, :308) is generalized
   from one computed pair to the whole lattice with the selective directions
   decomposed. No ST-1 number moves.
4. **RSC-1.** Its rank bound (rsc1:444) is composed into the first
   multi-insertion price floor (P-2); its two-instrument discipline is the
   template LEG 3 follows. Nothing in RSC-1 moves.
5. **GB-1.** Its circle is typed as the model face of the
   successive-deformation composite (§3.4): angle addition = composite
   closure, and its no-transport invariant is composition-stable — a
   strengthening of its §2.4 reading, computed in its own exact-sympy idiom.
6. **LD-A.** Its measured mover direction acquires a composite identity: the
   model's σ₃ mass IS `(i/2)[σ₂, σ₁]` — the composite of the model's two odd
   directions — so the model's grading-tilt class is exactly what composite
   evenness manufactures, one more face of "the phenomenon is real and lives
   where selection cannot".
7. **The Majorana/126 successor arc (via ST-1 §6/RSC-1 §7.3).** The arc's
   surviving selective route gains a composite fence: no even-order composite
   can substitute for the odd-count selective insertions its textures need,
   and any second-order term it invokes is a named nonlinear-insertion price
   (SN-1's escape route), not a loophole.

---

## 9. Hostile review, inline — the strongest attacks

**"The composite class arithmetic is trivial — classes add, so of course
even counts give class 0. You have dressed bookkeeping as a result."** The
arithmetic is one line; the RESULT is what it closes and what it fails to
close. Before MP-1, the corpus had ONE computed two-insertion instance
(st1:308) and a named worry that the arithmetic's conclusion ("the composite
can cross-pair while each factor selects") might let a constructor through
TB-5. The content here is the closure: the exhaustiveness of the parity
split at the term level (P-1, with the decomposition identity), the exact
vanishing at the decisive composite object by two weight instruments (§4.2),
the operator-level same-sense vanishing (§4.3), the commutator-is-the-grading
identity (§5.1), the index pinning (§5.4), and the seven floor (P-2) — none
of which is class bookkeeping alone, and two of which (the commutator
identity, the floor) are the file's genuinely new exact objects.

**"You have handed the constructor the mixed route: your own §5.1 shows the
selective factors manufacture the maximal mover. That kills TB-5."** It
prices TB-5's fork and confirms it. The manufactured mover is class-0
content: selection-blind by §4, exactly the horn TB-5 already bills through
TB-2/TB-3. What the constructor gains from §5.1 is knowing WHICH two odd
directions to multiply; what the constructor cannot gain — by the
exhaustiveness of the parity split — is a single term that both selects and
moves. TB-5's "two mechanisms" clause was rhetoric with a checklist; P-1
makes it a decomposition theorem.

**"The seven-insertion floor is numerology: rank subadditivity is generic
hand-waving and you did not verify invariant-compatibility at seven."**
Conceded in scope and typed so (P-2 says NECESSARY, not sufficient; the
generic 768/896 witnesses are model grade; invariant-compatibility at seven
is named as not computed). The floor's exact content is only the inequality
`128·6 = 768 < 896`, which stands on RSC-1's per-direction bound (quoted,
banked, two-instrument) and rank subadditivity (linear algebra). A
constructor who beats the floor must beat the per-direction bound, i.e.
overturn RSC-1 — a named, falsifiable dependency.

**"Your no-holonomy claim is finite-dimensional; the physical setting is a
function space where spectral flow through gapless points is exactly a
path-dependent quantity."** Yes — and that is the fence, not a leak (§5.3):
the finite statement is that NOTHING in the composite structure creates
path-dependence; where path-dependence lives (the eta layer, gapless
crossings) is precisely TR-1 §6's theorem shape, which P-4 makes the
universal toll booth. The claim is "no evasion via holonomy", not "no
holonomy in quantum field theory".

**"The 18.508 agreement is circular: you built N = Γ_amb and then measured
Γ_amb's own mover number."** The identity IS the finding: that the composite
of the two disjoint selective words equals the grading direction exactly —
`(i/2)[W′,W] − Γ_amb = 0` at machine zero — is a statement about the
carrier's Clifford algebra that nobody had written down, and TR-1's mover
number then FOLLOWS for the composite direction instead of being a separate
measurement. The number equality is the receipt that the identification is
exact, printed as such, not as an independent witness.

**"You killed TR-1's dichotomy by exhibiting a family with both
capacities."** No — and the kill grammar is applied before the denial
(rule: kills name their target claim). TR-1's dichotomy claim is *"a parity
computation per (grading, direction) pair, not a single verdict"* (tr1:242)
with *"the both-capabilities set is empty"* (tr1:379) quantified over single
directions/classes; TR-1's own P4 states the composite question as OPEN and
successor-owned. MP-1 answers the successor question in TR-1's favor at the
term level and prices the family level. Nothing TR-1 asserts is contradicted;
its probe runs green inside this one.

---

## 10. Claim ceiling, and what this artifact is not

- **No claim status moves. No canon row moves. No register, registry,
  sidecar, or ledger edit.** The generation count stays OPEN; SG4 stays the
  decider; bit 2 stays OPEN; the wall route stays a target under its bill.
- **Not a kill.** `target_claim` is a register item executed, not attacked.
  Three specific non-kills: (a) TR-1's dichotomy and bill survive
  strengthened (§9's last attack answered); (b) ST-1's A2/odd-count rule is
  generalized, not corrected; (c) `CANON.md:135/136/139` are consumed as the
  doorway, untouched.
- **The mixed route's existence is kinematic availability, not a mechanism
  claim.** No insertion is ON (CC-06); no scale, vacuum, action, or reality
  map is supplied; the constructor is TB-5's hypothetical adversary, not GU.
- **The seven floor is a gapping price, not a count.** It bounds insertions
  needed to gap one corner's self-form; it produces no integer three, no
  family structure, and no spectrum (CC-05 obeyed).
- **Model numbers bind models** (18.508381432260148, −21.845865, 5.7e-15,
  768/896 generic ranks). Exact statements are complexified-D_7 structural
  facts, horn-independent.
- **Not done, and named:** sufficiency at seven insertions;
  invariant-compatibility of a seven-direction selective form; the
  infinite-dimensional eta layer (§5.3); the ϖ-slot selectivity content
  (ST-1's Ω¹ column, consumed not retyped); the Krein/self-adjointness leg
  of every candidate composite (reality-map-gated, SN-1); the true-Y¹⁴
  operator and the (9,5) transport (rung-2's and canon's standing
  residuals).

---

## 11. Canonical-currency check records (for the integrator; MP-1 edits no sidecar)

`process_gates/canonical_currency_audit.py` run at work time (2026-08-17;
registry 10 canonical corrections, sidecar 40 recorded checks, exit 0; TOTAL
dirty 183 (file, correction) pairs, all DIRTY-UNCHECKED, none known-stale).
**Measured: NONE of MP-1's named inputs appears in the current dirty queue**
(exact-substring check of every input path against the full dirty list: zero
entries; all inputs post-date every `canonical_since` or are correction
owners/sidecar surfaces themselves). Per-input verdicts, scoped to the
corrections whose signatures the consumption sits inside:

1. `lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md`
   - vs **CC-06** (chirality VEV-conditional; selector SG4 bit 2 OPEN): the
     consumed content is the §11.6 hedge passage (extraction:137-138), quoted
     verbatim in §0; every composite statement below it is a shape statement
     with the ON switch left to Lane 1; no clause decides or assumes the
     bit. **CLEARED-CONSISTENT** (pointer: extraction:137-138; this artifact
     §0, §6 P-3, §10).
   - vs **CC-08** (the 128 is a partner-placement obligation, not a defect):
     the 128 enters only inside P-2's floor via RSC-1's rank bound, as the
     obligation whose gapping is priced; no defect/surplus framing imported.
     **CLEARED-CONSISTENT** (pointer: extraction §2; this artifact §6 P-2).
2. `papers/drafts/Transcript into the impossible.md` (:158 consumed as frame)
   — in no dirty queue; the UCSD-derived lineage caveat (CC-10) is carried:
   nothing here treats the transcript as more than transcript-verified, and
   no verdict rests on it (the use is a frame quote inherited from TR-1).
   **CLEARED-CONSISTENT (noted).**
3. `lab/sources/source-claim-register.yaml` (SC-CHI-01 quoted in §0;
   SC-CHI-02 cited as the source's ONE stylized coupling, L5's fence) —
   co-owner of CC-06, in no dirty queue; verbatim fields quoted with polarity
   and hedges preserved. **CLEARED-CONSISTENT (noted).**
4. All remaining named inputs (TR-1 + probe, RSC-1, ST-1, GB-1, CS-1, LD-A,
   CANON.md, VERIFICATION.md, gu-base-categories, upgrade register,
   correction registry, homonym register) — checked against the full dirty
   queue by exact-substring search over the audit output: zero entries.
   **CLEAN-BY-AUDIT** (pointer: audit output, TOTAL dirty 183 pairs, none
   naming these files).

Operative correction sentences relied on are quoted verbatim in §0 (CC-05,
CC-06, CC-08 `superseded_reading` fields, correction-registry.yaml:264-269,
:300-306, :364-368).

---

## 12. POSTFLIGHT — the six lenses, after the work

- **P1 — Did any lens change a verdict?** Yes, one, and it restructured the
  file: L1 (composition taxonomist) caught that the register title's
  "compose to class 0" is FALSE for the sum shape C-i — a sum of class-2
  terms is not a class-0 object, it is an odd family — which is why the
  freeze-on-the-square result (§5.2) exists as a separate row instead of
  being folded into "the composite commutes". Without that split the file
  would have conceded the constructor a two-parameter family with even
  content it does not have.
- **P2 — Is any verdict resting on a single receipt?** CP-SEL rests on the
  centre argument plus two independent weight instruments plus the operator
  face — three legs. CP-TRA's identity leg rests on one machine-zero
  residual, cross-checked by the number agreement with TR-1's pin and by the
  lattice face one level down. CP-PRICE's floor rests on RSC-1's banked
  bound (quoted) plus subadditivity — its named falsifiable dependency.
  CP-HOL is structural (point-function) plus two witnesses.
- **P3 — What did NOT move?** SG4 remains the decider; bit 2 remains OPEN
  (CHIRAL (massless/unbroken) vs MASSIVE unselected); the count remains OPEN
  and external-by-structure; the wall target remains under its bill;
  TR-1/ST-1/RSC-1/GB-1/LD-A verdicts all stand, each inheriting only a
  strengthening or an executed demand.
- **P4 — Where would a hostile re-runner disagree?** On sufficiency at seven
  (P-2 names it uncomputed — the natural successor computation is the
  invariant-compatibility of a seven-direction middle-form system, a
  finite Klimyk/rank problem); and on the eta layer (§5.3's fence) — a
  re-runner who wants the mixed-parity index theorem STATED rather than
  shaped will find only TR-1 §6's shape here, on purpose.
- **P5 — Regression check against the standing corrected facts.** Layers
  named throughout; contraction-not-KK untouched (no mode split);
  no-GUT untouched; positivity polarity untouched (no positivity claim);
  2+1 subtractive quoted and obeyed (the seven floor is a gapping price, not
  a count); the 128 an obligation (§0, P-2); "CHIRAL" appears only inside
  the homonym fence's disambiguated forms; "Majorana"/"Dirac-type" only
  inside repository-vocabulary fences.
- **P6 — Instrument honesty.** All planted controls fired correctly on the
  live run (planted-negative pin; 5 planted-false class propositions; the
  two product-multiset planted positives; the Racah/Klimyk agreement is
  itself a cross-instrument control; absence planted positive + near-miss);
  the selftest's 12 mutations were each caught by their targeted checks with
  the clean baseline verified first and re-verified after; the one design
  hazard found en route — the disjoint-pair product is anti-Hermitian, so a
  naive "Hermitian part" of it is ZERO and a careless mover witness would
  have been vacuous — is recorded here and answered in the probe by the
  commutator construction with an explicit non-vacuity guard. One harness
  incident, recorded rather than silently repaired (VERIFICATION.md rule-6
  class, new instance): the absence-detector mutation's sentinel string was
  first copied verbatim from TR-1's probe, whose own mutation tuple carries
  that literal in the scanned corpus — the mutant's "absence" scan then hit
  TR-1's probe file and the catch went INCIDENTAL-NOT-TARGETED (measured:
  11/12 on the first selftest run). The sentinel is now MP-1-unique
  (grep-verified 0 corpus hits) and the selftest passes 12/12; the lesson —
  a planted sentinel shared between probes is a corpus collision waiting in
  every OTHER probe's scan — is offered to the method owner.

---

## The blunt paragraph: which direction the pressure ran

The register item framed this as the dangerous flank — the place a determined
constructor might walk through TR-1's fence — and the pressure while
computing ran toward finding the evasion: the composite CAN cross-pair, its
evenness CAN move the trace, and the sharpest identity in the file hands the
constructor the recipe (the commutator of the two selective words is the
grading itself). What stopped the evasion is that every one of those gains
lands on the horn the bill already covers: the composite's selection
capability is not diluted but exactly zero (centre argument, two instruments,
and the same-sense operator composites vanish identically), the linear family
never carries the even content (frozen on the whole square, index pinned at
the graded dimension), and the only way to put the even content into the
operator is a nonlinear term that makes the family mixed-parity — which is
TB-2/TB-3's class and TR-1 §6's theorem demand, now forced rather than
optional. I judge the result stronger than the register item expected: not
just "pricing, not prohibition" confirmed, but the pricing made exhaustive
(P-1's decomposition), quantified (P-2's seven floor), and closed against
the last parity-pure alternative. The two places a hostile re-runner should
press are named in P4 — sufficiency at seven is a finite computation someone
should run before the floor is quoted as more than a floor, and the eta
layer is where a genuinely new mechanism would have to live.

---

## Deviations from the brief

1. **Input currency:** the brief required per-input verdicts for every
   consumed input in the audit's dirty queue; measured at run time the queue
   (183 pairs) names NONE of MP-1's inputs, so §11 records the negative
   result with per-input CLEARED/CLEAN verdicts anyway, TR-1's precedent.
2. **The register title's phrase "compose to class 0" is typed as
   shape-dependent** (§3): true for monomials/products (C-ii/C-iii), false
   for the sum family (C-i) — the brief's (a) asked for the typing and the
   answer required distinguishing a case the title's phrasing elides;
   recorded as a finding, not a correction (the title is about the monomial
   arithmetic, and for that shape it is exact).
3. **Task (c)'s holonomy question is answered structurally** (point-function
   argument) rather than by a path-integral computation, because in finite
   dimensions there is no path object to integrate — the honest content is
   the fence to the eta layer (§5.3), and inventing a finite-dimensional
   "holonomy computation" would have been theater.
4. **Ship-time environment, recorded for the integrator (shared checkout,
   concurrent wave-3 siblings).** During MP-1's pre-flight sweep two
   concurrent wave-3 artifacts landed in the shared checkout
   (`grading-bridge/ds1-the-stock-sits-at-the-pole-and-waits-on-the-reality-map-2026-08-17.md`;
   `wall-bill/pv1-pi0-trivial-in-every-live-typing-w1-kill-fires-2026-08-17.md`).
   Neither is an MP-1 input, MP-1's absence scans ran green with both
   present, and nothing here consumes or adjudicates them. Two sibling
   probes were RED at ship time for reasons that predate and do not involve
   MP-1's files (no MP-1 path appears in any failing check's output):
   GB-1's `L5-dest-artifact` no-duplicate-seed pin now sees DS-1's artifact
   in its directory (the GB-1/DS-1 lanes' event), and LD-A's probe carries
   four stale legs (its st1:441 pin targets the A2 sentence that the
   integrator's RSC1-20260817 correction note shifted to st1:456 — the line
   MP-1 and TR-1 pin — and its wall-token absence scan now sees RW-1/WB-1's
   own files). Both are other lanes' integration debts, untouched. All
   other sibling probes re-run green at ship time (TR-1 90/90 inside this
   probe; RSC-1 103/103; ST-1 94/94; RW-1 84; WB-1 93; B1P-1 91/91; LD-B,
   LD-C exit 0).
5. No other deviations: two files written, no git operations, no existing
   file touched, pre-existing gate reds (typed-carrier selected-k151;
   certificate-shape k149 allowlist; comparator-routing gap) left as other
   lanes' debts with MP-1's own contribution declared (routing notice).

## Reproduce

```
_local/cas-venv/bin/python tests/channel-swings/joe_directed_mp1_composites_inherit_one_horn_never_both.py
_local/cas-venv/bin/python tests/channel-swings/joe_directed_mp1_composites_inherit_one_horn_never_both.py --selftest
```

Expected: `86/86 checks pass`, exit 0; selftest: clean baseline verified
first (86, pinned), then `12/12 mutations caught` each via its targeted
genuine `[FAIL]` line, 0 crash-only, 0 missed, baseline re-verified, exit 0.
