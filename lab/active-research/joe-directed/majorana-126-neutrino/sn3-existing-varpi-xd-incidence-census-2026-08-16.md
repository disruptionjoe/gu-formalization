---
artifact_type: exploration
status: exploration
doc_type: source-native-existing-varpi-xd-incidence-census
created: 2026-08-16
work_item: SN3-A
channel: majorana_126_neutrino_mechanism
title: "SN-3A: selected K77 grades one and five contain conditional X_D parent types; native one-form class composition formally aligns them with pp/mm, not pm/mp"
grade: "EXACT conditional finite census, hostile-repaired. Internal D5 exterior weights, full pointwise real-U parent signs, bare-versus-Gram chirality incidence, fixed-Hq intersections, and equation-9.16 cross-degree cell classes/custody are checked exactly. The native class composition internal 2 plus connection-form 2 equals positional 0 and includes an adverse old-rule mutant. Grade-five representation content is the strongest parent packet. The observation coindex and source-slot-to-observed-line incidence remain unwitnessed. No action, vacuum, selector, external datum, quotient, analytic domain, physical mass, or conventional scalar-126 mechanism is constructed."
disposition: GRADE1_AND_GRADE5_XD_PARENT_TYPES_LOCATED__GRADE5_STRONGEST__GRADE2_HAS_NO_XD__GRADE3_IS_COMPLETION_CONTROL_ONLY__FIXED_HQ_DIMS_1_36_126_WITH_Q_DEPENDENCE__NATIVE_ONE_FORM_CLASS2_PLUS_INTERNAL_CLASS2_FORMALLY_ALIGNS_WITH_PP_MM_NET0__PM_MP_CROSSED_REVERSE_CUSTODY_IS_LEDGER_ONLY__OBSERVED_LINE_AND_COINDEX_INCIDENCE_UNWITNESSED
canon_verdict_change: none
steering_effect: "Bank the grade-one/grade-five X_D parent census, the pp/mm formal native-one-form class compatibility, and pm/mp crossed custody as a ledger fact only. Do not call pm/mp zero-order X_D ports, flatten the moving grade-one doublet into four fixed-Hq generators, infer weak multiplicity inside a fixed-q subspace, or manufacture the missing observation/coindex incidence."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-source-native-neutrino-conditional-build-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-neutral-reality-charge-admissibility-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-equation916-k77-neutral-restriction-2026-08-16.md
  - lab/process/hostile-reviews/2026-08-16-joe-directed-sn3-varpi-incidence-review.md
  - explorations/conditional-build/cb-a-representation-content-2026-08-05.md
  - explorations/conditional-build/selected-k77-full-u6464-action-bank-2026-08-08.md
  - explorations/conditional-build/selected-k77-varpi-radial-half-exchange-gate-2026-08-12.md
  - lab/active-research/joe-directed/ledger-advancement/la8-rae2-is-refuted-at-the-settled-form-leg-and-the-open-fork-is-not-load-bearing-2026-08-15.md
scripts:
  - tests/channel-swings/joe_directed_sn3_existing_varpi_xd_incidence_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`. The operative object is an already-
> admitted Clifford component of the K77 connection parent and its conditional
> equation-(9.16) cross-degree incidence. The labels `126` and `126bar` below
> name the two complex Hodge halves of the source-native internal grade-five
> space; they do not import a scalar field, action or VEV.

# SN-3A — existing-`varpi` Dirac incidence census

## Result first

The exact internal representation census finds the SN-2 dual-doublet type

```text
X_D = colour singlet weak doublet, Y=+1/2, B-L=0
```

in both selected odd Clifford grades:

| pure-internal grade | `X_D` copies | conjugate copies | selected support | verdict |
|---:|---:|---:|---|---|
| `1` | `1` | `1` | yes | parent type located |
| `2` | `0` | `0` | yes | excluded by representation |
| `3` | `2` | `2` | no | full-unitary completion control only |
| `5` | `2` | `2` | yes | **strongest parent packet** |

At grade five, each complex Hodge half of
`Lambda^5(V_10)_C = 126 + 126bar` contributes one positive- and one
negative-hypercharge weak member through its `(15,2,2)` summand. The full
complexification of the real `252` therefore has two copies of each sign.
This is representation membership only.

The hostile class-composition repair separates three grades:

```text
internal odd Clifford class 2
+ native connection one-form-leg class 2
= positional net class 0.
```

Therefore the native grade-one/five one-form packet formally aligns with the
four cross-degree `pp/mm` positions, not `pm/mp`:

```text
(bar-zeta-, nu+) : varpi_++       <-> (bar-nu-, zeta+) : -bar-varpi_++^*
(bar-zeta+, nu-) : varpi_--       <-> (bar-nu+, zeta-) : -bar-varpi_--^*
```

The source ledger separately has crossed `pm/mp` reverse custody, but those
positional net-class-two cells are not native one-form `X_D` ports. They could
be discussed as zero-order ports only after an independently supplied descent
removes or compensates the one-form coindex. No such descent exists here.

But no source receipt identifies any formally compatible position with the
observed `L,N^c` lines,
nor supplies the needed one-form coindex/Clifford contraction. Thus the exact
result stops at

```text
GRADE1_AND_GRADE5_XD_PARENT_TYPES_LOCATED;
GRADE5_STRONGEST;
NATIVE_ONE_FORM_FORMALLY_ALIGNS_WITH_PP_MM;
PM_MP_CROSSED_CUSTODY_IS_LEDGER_ONLY;
OBSERVED_LINE_AND_COINDEX_INCIDENCE_UNWITNESSED.
```

No zero-order Dirac coefficient, action term or physical mass is obtained.

## 1. Divergent-lens conditional-build preflight

1. **Source semantics.** `SC-OP-04/05` release a rolled operator and a rival
   lower-right completion; they do not publish an observed-neutrino matrix.
   `SC-FER-03`, `SC-GEO-58` and `SC-META-57` assign possible Higgs/Yukawa
   functions to the connection without deriving their observed incidence.
2. **Full-U parent versus D5 comparator.** First type the candidate as a
   pure-internal Clifford component of K77 `varpi`; use D5 branching only as
   representation arithmetic. A standard scalar `126` is non-adjudicating.
3. **Exact representation theory.** Test colour singlet, weak-doublet
   multiplicity, `Y` and `B-L` separately. Dimension matching is not a proof.
4. **Clifford grade.** Keep selected live grades `1,2,5` distinct. Grade `3`
   is an adverse completion control, not selected support.
5. **Equation-(9.16) custody.** Keep internal Clifford class, connection
   form-leg class and positional net cell class separate. Preserve each
   printed field owner, direction and barred reverse partner.
6. **Observation typing.** `varpi` is an `Omega^1(Y,End S)` connection. Its
   observation pullback is a four-dimensional one-form, not automatically a
   Lorentz scalar or a zero-order Yukawa coefficient.
7. **Both halves and reality.** Retain both ambient K77 Weyl halves, both
   complex grade-five Hodge halves, and all four independent barred/unbarred
   source fields. These are three different distinctions.
8. **Fixed versus moving `H_q`.** A frozen trace metric asks for membership in
   one `u(H_q)`. The earlier weak doublet is a moving `(X,q,H_q)` composite;
   its four real orbit/radial components are not four frozen generators.
9. **Hostile controls.** Plant grade `2`, grade `3`, a full-SM-singlet
   carrier, equal-dimension bar identification and radial-line-only inference.
   Mutate class composition by dropping the class-two form leg; the probe must
   expose the resulting false `pm/mp` selection.
10. **Conditional-build discipline.** Assume a fitting coefficient only to
    test how it would fit. Building an action, vacuum, selector, new
    coefficient or external datum is outside this path.

This preflight makes the efficient task finite: an exact grade census, a
fixed-`H_q` linear intersection, and a four-cell source crosswalk.

The fermion-semantics guardrail is equally important. `SC-GEN-56` assigns the
three-generation claim to the pullback package; `SC-GEN-57` says those
families are structurally `2+1`, with the third an imposter; `SC-GEN-58`
assigns the generation claim to the rolled `0-1-13-14` complex; and
`SC-PRE-52` places the third generation in a later recombination with the
additional `144`-coupled spin-one-half sector. None licenses an ordinary
three-copy family index, identifies `zeta/nu` with `nu_L/nu_R`, or turns
ambient Clifford half exchange into observed net chirality. Those familiar
routes therefore remain controls, not shortcuts to the missing incidence.

## 2. Typed bridge and exact representation census

Use an internal isotropic vector-weight basis with weights `+/-e_i`. In the
convention used by the exact probe,

```text
B-L = (2/3)(w1+w2+w3)
T3L = (w4+w5)/2
T3R = (w4-w5)/2
Y   = T3R + (B-L)/2.
```

The probe enumerates `Lambda^k(V_10)` over exact rational weights. Colour
singlet multiplicities are obtained by an exact `SU(3)` Weyl alternant and
weak-doublet multiplicities by subtracting the `T3L=3/2` contamination from
the `T3L=1/2` weight multiplicity. The result is

| `k` | dimension | `B-L=0`, `Y=+1/2` doublets | `Y=-1/2` doublets |
|---:|---:|---:|---:|
| `1` | `10` | `1` | `1` |
| `2` | `45` | `0` | `0` |
| `3` | `120` | `2` | `2` |
| `5` | `252` | `2` | `2` |

The grade-five count sharpens the earlier parent statement: there is one
`X_D` copy per complex Hodge half, hence two in the full complexified real
carrier. It does not choose one half as physical or identify conjugate source
fields.

## 3. Full pointwise parent, selected support and half incidence

These three questions are not interchangeable.

| grade | real Clifford transpose type | pointwise full-U admission | selected support |
|---:|---|---|---|
| `1` | `B`-skew | real monomial | live |
| `2` | `B`-skew | real monomial | live, but no `X_D` |
| `3` | `B`-self | only `i` times the monomial in complex completion | not live |
| `5` | `B`-skew | real monomial | live |

For a pure-internal odd monomial `X` at grade one or five,

```text
[X,Omega_4]=0,       {X,Omega_14}=0,
{B X,Omega_4}=0,    [B X,Omega_14]=0.
```

So the bare internal coefficient exchanges ambient halves, while the
invariant-Gram insertion has the required local opposite-four-dimensional-
Weyl incidence and retains the ambient grading. This is a local Clifford
statement, not emergent physical chirality, a quotient, or a mass term.

## 4. Fixed-`H_q` sharpening

Fix one unit normal `q` and `H_q=B gamma(q)`. For selected real monomials the
fixed trace-unitary condition is

```text
X^T H_q + H_q X = 0.
```

Exact integer matrices give

| pure-internal grade | parent dimension | dimension in fixed `u(H_q)` | explicit rule |
|---:|---:|---:|---|
| `1` | `10` | `1` | exactly `span(gamma(q))` |
| `2` | `45` | `36` | monomials not containing `q` |
| `5` | `252` | `126` | monomials containing `q` |

The nonzero constraint images are distinct signed twice-Clifford monomials,
so there are no hidden linear cancellations. The dimensions are exact.

The fixed `q` breaks the weak group used to state the `X_D` multiplicity.
Therefore `126` here is the dimension of a `q`-dependent real intersection,
not a weak-doublet count and not the conventional scalar representation. The
earlier radial theorem certifies the one fixed grade-one line. Its four-real-
component weak doublet belongs to the moving soldering composite formed from
the radial coefficient and the orbit of `q`; it cannot be flattened into four
members of this one-dimensional frozen intersection.

## 5. Exact equation-(9.16) cell ledger and native class composition

The positional cross-degree pattern is `[[0,2],[2,0]]` in both directions.
All eight source cells and owners are

| positional class | direction | row, column | owner |
|---:|---|---|---|
| `0` | forward `Omega^0 -> Omega^1` | `bar-zeta-, nu+` | `varpi_++` |
| `2` | forward | `bar-zeta-, nu-` | `varpi_+-` |
| `2` | forward | `bar-zeta+, nu+` | `varpi_-+` |
| `0` | forward | `bar-zeta+, nu-` | `varpi_--` |
| `0` | reverse `Omega^1 -> Omega^0` | `bar-nu-, zeta+` | `-bar-varpi_++^*` |
| `2` | reverse | `bar-nu-, zeta-` | `-bar-varpi_+-^*` |
| `2` | reverse | `bar-nu+, zeta+` | `-bar-varpi_-+^*` |
| `0` | reverse | `bar-nu+, zeta-` | `-bar-varpi_--^*` |

For a native connection cell the positional class must be compared with the
**composite**, not directly with the internal Clifford grade:

```text
internal grade-one/five class = 2
native connection form leg   = 2
composite cell class          = 2+2 = 0 mod 4.
```

Consequently `pp/mm` are the only formal class-compatible native one-form
positions. This is still not actual observed `X_D` incidence.

The crossed `pm/mp` reverse relation remains an exact source-ledger theorem:

```text
varpi_+-  <-> bar-varpi_-+
varpi_-+  <-> bar-varpi_+-.
```

It is **not** an `X_D`-port theorem. A zero-order `pm/mp` reading would need an
independently supplied coindex descent or compensating class shift. Observation
instead retains the one-form coindex. The `pp/mm` reverse owners preserve
their labels. None of these cell facts identifies `zeta,nu` with observed
`L,N^c`; the read packet explicitly forbids that relabelling.

## 6. Neutral horn and observation/coindex ceiling

The compensating positive-hypercharge doublet is relevant only on the
`SN2-NEUTRAL=Q0` horn. The full-SM-singlet horn removes the `L` endpoint and
cannot host `L N^c`.

Even on `Q0`, two incidence maps remain absent:

1. a source-slot-to-observed-line restriction placing `L,N^c` in one formal
   `pp/mm` forward/reverse pair; and
2. a locally covariant contraction of the observed four-dimensional one-form
   coindex with the spinor/Clifford structure appropriate to the proposed
   operator.

MD-1/LA-8 establish that observation pullback of a `varpi` one-form remains a
four-dimensional one-form. Rolled `Omega^0 <-> Omega^1` compatibility is
exact, but it does not erase that coindex or turn the parent type into a
scalar coefficient. This is the decisive negative row.

## 7. Archaeology and novelty audit

- **CB-A already owns** the internal D5 grade representation counts. This
  artifact reproduces the relevant `1/2/3/5` rows exactly and joins them to
  SN-2's `X_D` predicate; it does not claim the branching as new.
- **The selected full-U action bank already owns** pointwise parent admission
  and selected live grades `1,2,5`. It did not locate the `X_D` type or assign
  it to source cells.
- **The K77 half-incidence work already owns** the bare-versus-Gram Clifford
  parity. It is reused as a typed Lorentz/Weyl predicate, not as physical
  chirality.
- **The radial and moving-doublet gates already own** the grade-one radial
  membership and moving composite. This census supplies the frozen-`H_q`
  `1/36/126` intersection theorem and prevents moving/fixed flattening.
- **MD-1 and LA-8 already own** the one-form observation correction. It is
  load-bearing here and prevents a scalar-Yukawa overclaim.
- **SN-2 already owns** the `X_D` requirement, neutral horns, independent
  barred fields and the need for a slot-to-line incidence.
- **The SN-3 hostile review owns the material correction** that internal
  class two must be composed with the native class-two one-form leg. The
  resulting `2+2=0` rule kills the original `pm/mp` port inference while
  preserving `pm/mp` crossed custody as a source-ledger fact.

The new result is therefore the combined finite census: selected grade-one
and grade-five parent types, grade-five multiplicity by both Hodge halves,
exact fixed-`H_q` dimensions, the hostile-repaired native `2+2=0` class
composition and formal `pp/mm` compatibility. Crossed `pm/mp` reverse custody
survives only as a ledger fact. Actual observed incidence remains negative.

## Claim ceiling and next probe

**Established conditionally:** in the pure-internal portion of the pointwise
full K77 unitary parent, selected grades one and five contain the exact
representation type SN-2 calls `X_D`; grade five is the strongest packet.
The fixed-`H_q` intersections have dimensions `1,36,126`. Native internal
class two plus one-form-leg class two gives positional net class zero, so the
four `pp/mm` cross-degree positions are formally compatible. The four
positional class-two `pm/mp` cells retain crossed reverse custody but are not
native one-form `X_D` ports.

**Not established:** a selected component value; source-slot-to-observed-line
incidence; coindex contraction; physical Lorentz-scalar descent; a source
action, vacuum, external datum, selector, reality quotient, analytic domain,
hierarchy, physical chirality, Yukawa texture or neutrino mass.

The next admissible probe is not another representation census. It is a
source-native **incidence audit**: locate an independently supplied receipt
for the observed `L,N^c` line map and one-form coindex contraction, then test
it against the exact formal `pp/mm` positions. Conditional-build discipline forbids
inventing either map merely to make the candidate work.

## Reproduction

```bash
python3 tests/channel-swings/joe_directed_sn3_existing_varpi_xd_incidence_probe.py
```

Expected final line:

```text
PASS: selected grades one and five contain conditional X_D parent types; native class 2+2=0 formally aligns the one-form packet with pp/mm, while crossed pm/mp custody is ledger-only. Actual neutral-line/coindex incidence remains unconstructed.
```
