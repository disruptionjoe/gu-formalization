---
artifact_type: exploration
status: exploration
doc_type: source-native-neutrino-wave-integration-and-reprioritization
created: 2026-08-16
work_item: SN2-WAVE
channel: majorana_126_neutrino_mechanism
title: "SN-2 wave: correct the neutral carrier, classify every conditional scale branch, retain only local coefficient admissibility, and run one existing-varpi incidence census before stopping"
grade: "INTEGRATION SYNTHESIS over three exact conditional source-native certificates and one independent eighteen-lens hostile review. Neutral restriction: 51 checks plus 9/9 mutants. Valuation: 108 checks including six mutants. Reality/charge: 35 checks plus 9/9 mutants. The finite rank, cell, Newton, Schur/Smith, Jordan, charge, centre and Grassmann results are exact; neutral closure, F occurrence, relative scales, reality horn, coefficient incidence, action, vacuum, quotient and physical interpretation remain declared or missing."
disposition: EXACT_NEUTRAL_COUNTS_AND_CONDITIONAL_PENCIL_CLASSIFIER_BANKED__STRICT_BILATERAL_T2_REQUIRES_UNSELECTED_0_1_1_PLUS_NONDEGENERATE_COMPOSITION__XD_XR_ARE_TYPE_REQUIREMENTS_PENDING_SLOT_INCIDENCE__ONE_EXISTING_VARPI_CENSUS_NEXT__THEN_REPRIORITIZE
canon_verdict_change: none
steering_effect: "The neutrino lane is first for one more bounded source-native representation census: test only already-admitted varpi coefficient types against the declared X_D/X_R source-slot-to-observed-line requirements. Do not construct an action, vacuum, selector, scale relation, new coefficient, reality owner, quotient, or conventional 126. If no packet is present, or continuation needs another unowned horn, pause this lane."
canonical_effect: pending_integration
depends_on:
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-source-native-neutrino-conditional-build-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-equation916-k77-neutral-restriction-2026-08-16.md
  - tests/channel-swings/joe_directed_sn2_equation916_k77_neutral_restriction_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-neutral-pencil-valuation-classifier-2026-08-16.md
  - tests/channel-swings/joe_directed_sn2_neutral_pencil_valuation_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-neutral-reality-charge-admissibility-2026-08-16.md
  - tests/channel-swings/joe_directed_sn2_neutral_reality_charge_probe.py
  - lab/process/hostile-reviews/2026-08-16-joe-directed-sn2-neutral-pencil-review.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`.

# SN-2 wave — neutral rolled-pencil integration

## Outcome

This wave makes the source-native seesaw hypothesis much more precise without
turning it into a mass claim.

1. The source object is the rolled `Omega1/Omega0` equation-(9.16) pencil. Its
   `zeta/nu` fields are not standard `nu_L/nu_R` labels.
2. The exact observed neutral carrier is not the previously floated
   `20`-complex-dimensional working count.
3. A `t^2/M`-like valuation branch is algebraically available, but only after
   two independent unselected horns: typed neutral closure and relative block
   scaling. The southeast zero alone still does not generate it.
4. The banked local K77 pairing horns allow exact Dirac- or right-Majorana
   **type requirements** to be written after a declared source-slot incidence.
   They do not supply the required coefficients or a global reality condition.

The neutrino path earns one more bounded representation census. It does not
earn an action search, scale fit, vacuum construction, standard `126` import,
or phenomenology pass.

## 1. Exact neutral carrier alternatives

The one-`16` charge dictionary contains two electromagnetic-neutral all-left
weights, `nu_L` and `nu_L^c`, but only `nu_L^c` is a full Standard-Model
singlet. The source selects neither restriction.

| declared carrier | `SN2-NEUTRAL=Q0` | `SN2-NEUTRAL=SM1` |
|---|---:|---:|
| four observed `2 x 16` K77 blocks, both ambient halves | **16** | **8** |
| minimal four-field column, one `F` occurrence per source field | **32** | **16** |
| both declared `F` occurrences retained in each zeta slot | **48** | **24** |

The last row is an F-only subcarrier count. It is not the dimension of full
`Omega1` or of the complete gamma-traceless `832`. The barred row carrier has
the same dimension as the unbarred column carrier, but the fields remain
independent; equal dimension is not a reality map.

The restricted zero-order grammar retains the source's four-by-four block
support: twelve permitted cells and four southeast zeros. Four unbarred
`varpi_ab` owners recur in the two upper quadrants, while four barred/formal-
star partners remain independently named. A permitted cell can still project
to zero.

## 2. Neutral closure is a separate horn

Neutral weights alone give only a compression. The exact pencil classifier
uses the declared horn `SN2-NEUTRAL-CLOSED`, which contains:

- separate restriction/return maps for the unbarred `Z,N` column spaces;
- separate restriction/return maps for the barred `bar-Z,bar-N` row spaces;
- the three `A/B/C` intertwining equations; and
- a regular valuation-zero row-to-column identification used only to form the
  square formal pencil.

The last map is not a reality condition or adjoint. None of these maps is
source-selected. Without them, projected eigenvalues are compression data,
not eigenvalues of a closed source subpencil.

## 3. Exact valuation theorem

Under that horn, write

```text
D(t) = [[A(t), B(t)],
        [-C(t), 0]].
```

For the scalar primitive with valuations
`alpha=v(A), beta=v(B), gamma=v(C)`, the two Newton slopes are

```text
{alpha, beta+gamma-alpha}       if 2 alpha < beta+gamma,
{(beta+gamma)/2 twice}          if 2 alpha >= beta+gamma.
```

For an invertible heavy block `A`, exact unimodular row/column operations give

```text
diag(A, C A^-1 B).
```

Therefore the unique minimum strict bilateral nonnegative-integer pattern
with fixed heavy slope zero and light slope two is

```text
(v(A),v(B),v(C)) = (0,1,1),
```

and it additionally requires a nondegenerate leading composition
`C_0 A_0^-1 B_0`. `B` and `C` remain independent. Calling their two small
parameters one `t` is itself a declared diagonal ray, not a reality relation.

The exact adverse branches matter:

- uniform `A,B,C ~ t` gives only linear slopes, recovering SN-1;
- one-sided `(0,0,2)` or `(0,2,0)` mimics slopes `0,2` without bilateral small
  mixing;
- singular `A` mixes heavy and direct sectors;
- leading composition can cancel, be rank-deficient, or fill at higher order;
- fractional Puiseux slopes and nontrivial zero Jordan chains occur; and
- algebraic zero multiplicity is not a protected physical zero mode.

This is conditional pencil algebra, not a mass spectrum.

## 4. Reality, charge and centre-class intersection

Observed charge and ambient centre class are separate filters.

| candidate observed bilinear | exact observed type | missing coefficient type requirement |
|---|---|---|
| `L N^c` | `Q=0`, `B-L=0`, but weak doublet with `Y=-1/2` | `X_D`: dual weak doublet, `Y=+1/2`, `B-L=0`, after a declared printed cross-degree slot incidence |
| `N^c N^c` | full SM singlet with all-left `B-L=+2` | `X_R`: SM singlet with `B-L=-2`, after a declared southeast slot incidence and that cell's exact centre requirement |

Neither row states that a coefficient exists. It says only what an already-
admitted coefficient would have to be for that local bilinear not to be
excluded.

The ambient action-cell ledger is exact:

```text
SE cells: two net centre class 0, two net centre class 2.
```

There is no uniform class-two southeast rule. Separately, the D5 parent-module
control says a generic class-zero adjoint preserves `16/16bar`, while a typed
class-two insertion can exchange them. In the D7 action-cell bookkeeping, the
relevant class-two connection component plus class-two one-form leg has net
cell class zero. These are different levels. None identifies centre class two
with `B-L=2`.

The two banked local K77 pairing horns both give exact noncharacteristic
Grassmann-compatible fixed graphs on the independent doubled fields. A fixed
graph is not a quotient. Neither horn is selected, neither proves full
equation-(9.16) invariance, and neither supplies a global/null/analytic domain.

## 5. What the source hypothesis now amounts to

The strongest source-native conditional hypothesis is:

> If one declared neutral/F-occurrence restriction closes under the relevant
> equation-(9.16) blocks; if the existing connection representation supplies
> the required observed coefficient incidence; if the `A/B/C` blocks occupy
> the fixed-heavy bilateral valuation horn; and if one banked local reality
> graph extends to the later global operator/domain, then the rolled pencil is
> algebraically capable of a seesaw-like slope separation.

Every “if” names a different type level. SN-2 proves the consequences and the
minimum requirements, not the antecedents.

## 6. Hostile review and repairs

The independent eighteen-lens review returned
`PASS_AFTER_FIVE_MATERIAL_SCOPE_REPAIRS`. Integration applies all five:

1. one horn namespace: `SN2-NEUTRAL=Q0|SM1`;
2. `48/24` is explicitly the both-F-occurrences zeta horn, not full `Omega1`;
3. row and column neutral closure maps are typed separately;
4. local reality is a fixed graph/restriction, not a quotient; and
5. `X_D/X_R` are type requirements pending source-slot incidence, while the
   arithmetic grades are exact conditional rather than physically
   dependency-free.

The review independently replayed all `51`, `108`, and `35` checks and all
available mutants. It found no change to a finite rank, Newton slope,
Schur/Smith identity, centre-class cell, charge assignment, or Grassmann sign.

## 7. Reprioritization

### Within the neutrino lane

| item | priority | fertility | disposition |
|---|---:|---:|---|
| existing-`varpi` source-slot/observed-line coefficient-incidence census for `X_D/X_R` | 1 | `8/10` structural | one bounded next wave; decompose only already-admitted coefficient representations |
| SN-2 carrier/Newton/Smith/Jordan classifier | banked | `9/10` as structure | complete after hostile repairs |
| actual ranks of the twelve neutral block maps | reopen only | `4/10` | needs concrete restricted coefficients and closure |
| choose `Q0/SM1`, F occurrence, relative scale or reality | dependency fence | not scored | do not select inside this conditional lane |
| physical mass, pole, mixing, threshold or phenomenology | closed here | `0/10` | requires later quotient/domain/background owners |

The next wave must not introduce a new scalar or coefficient. It may ask only
whether the already-admitted `varpi` representation contains the typed
`X_D/X_R` packets on declared source-slot incidences. If neither occurs, or if
continuation needs another unowned horn, pause the neutrino lane.

### Relative to the exploration portfolio

1. one finite SN-3 existing-`varpi` coefficient-incidence census;
2. retain the isolated H210 CB-2--CB-6 chain as banked/reopen-only;
3. keep the extra-vector lane retired.

The H210 derivative branch remains closed by CB-8. Extra-vector attachment
still overlaps action selection. The neutrino incidence census is therefore
the most fertile non-overlapping source-native calculation for one more wave.

## Claim ceiling

SN-2 constructs no action, vacuum, selector, scale owner, external datum,
reality owner, global domain, reduction, physical quotient, mass, hierarchy,
pole, mixing matrix, family count, threshold, neutrinoless-double-beta claim,
observable, or phenomenology. Conventional `126` results remain controls and
cannot adjudicate this source-native route without the full typed bridge.
