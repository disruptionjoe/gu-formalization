---
artifact_type: exploration
status: exploration
doc_type: source-native-equation916-k77-neutral-restriction
created: 2026-08-16
work_item: SN-2
channel: majorana_126_neutrino_mechanism
title: "SN-2: equation 9.16 has two distinct exact neutral restrictions on the observed K77 F carrier. Electromagnetic Q=0 retains nu_L and all-left nu_L^c and has complex rank 16 on the four observed 2x16 blocks; full-SM-singlet restriction retains only nu_L^c and has rank 8. The minimal four-field pencils have column ranks 32 and 16, while including the second F occurrence in each zeta slot gives 48 and 24. No source-faithful count is 20."
grade: "EXACT CONDITIONAL integer/Fraction carrier, charge, centre-class and source-cell arithmetic over banked K77/source receipts and declared neutral/F-occurrence horns. Four observed K77 2x16 blocks on both ambient halves; exact ranks 16/8 on the Q=0/SM-singlet carrier horns; exact minimal rolled-field ranks 32/16 and both-F-occurrence zeta ranks 48/24; exact 12-supported/4-southeast-zero equation-9.16 block grammar; four recurring unbarred varpi owners plus four independent barred partners; D5 centre-class-0 versus class-2 parent-module control. SOURCE-NATIVE conditional restriction. NOT an action, selected gauge group, vacuum, scale, mass, Majorana map, physical quotient, family row, standard 126, or seesaw derivation."
disposition: TWO_NEUTRAL_HORNS_PRESERVED__FOUR_K77_BLOCK_RANKS_Q0_16_SMSINGLET_8__MINIMAL_FOUR_FIELD_RANKS_32_16__SECOND_F_HORN_48_24__UNSUPPORTED_20_REJECTED__SOURCE_SEESAW_LANGUAGE_SELECTS_NEITHER_HORN
canon_verdict_change: none
steering_effect: "Use a block pencil, not a scalar 4x4 mass matrix. Declare Q0 versus SM-singlet and one-F versus two-F zeta horns before any successor calculation. Preserve both ambient halves, all four barred/unbarred fields, source row/column order, and the centre-class constraint."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md
  - explorations/resolver-wave-k77a-real-spinor-observation-atomic-particle-crosswalk-2026-08-04.md
  - lab/process/resolver-wave-k77a-atomic-particle-crosswalk.json
  - lab/active-research/joe-directed/carrier/crb-carrier-is-four-corners-not-one-weyl-2026-08-15.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn1-observed-neutrino-mass-pencil-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
scripts:
  - tests/channel-swings/joe_directed_sn2_equation916_k77_neutral_restriction_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`. SN-2 restricts the released
> equation-9.16 grammar on the source-aligned K77 observation carrier. It does
> not use an ordinary `126` or standard seesaw mechanism.

# SN-2 — the exact K77 neutral restriction of equation 9.16

## Result first

There is not one source-selected meaning of “the neutral neutrino sector.”
Two exact conditional restrictions must remain separate:

1. `SN2-NEUTRAL=Q0` keeps every state of electromagnetic charge `Q=0`. In one internal
   `16` these are `nu_L` and the all-left conjugate `nu_L_c`.
2. `SN2-NEUTRAL=SM1` keeps only full Standard-Model singlets. In one internal `16` this
   is `nu_L_c`; `nu_L` is electrically neutral but belongs to a weak doublet.

The exact dimension ledger is:

| object | `SN2-NEUTRAL=Q0` | `SN2-NEUTRAL=SM1` |
|---|---:|---:|
| one internal `16` or `16bar` | 2 | 1 |
| one observed `2 x 16` block | 4 | 2 |
| all four observed K77 blocks, both ambient halves | **16** | **8** |
| one graded `F+` or `F-` | 8 | 4 |
| minimal four-field column `(zeta+,zeta-,nu+,nu-)` | **32** | **16** |
| include the second `F` occurrence in both `zeta` slots | **48** | **24** |

The barred row carrier has the same dimension as the corresponding unbarred
column carrier, but the draft declares barred and unbarred variables to be
independent classical fields. They are not identified, quotiented, or added
into a self-adjoint state space here.

No row in this exact ledger has complex dimension `20`. The committed SN-1
artifact did not claim such a count; SN-2 rejects the unsupported later
working hypothesis without attributing it to SN-1. Four equal `2 x 16` blocks
with one or two retained internal weights have rank `8` or `16`, so `20` also
fails the elementary block-multiplicity checksum.

## 1. Source meaning and the two neutral horns

The exact K77 charge dictionary uses the all-left convention. It contains

```text
nu_L   : T3L=+1/2, Y=-1/2, Q=0
nu_L_c : T3L=0,    Y=0,    Q=0
```

Only the second line is a full SM singlet. Charge conjugation changes the
physical-language name of the right-handed state; it does not collapse the two
all-left basis states.

This dictionary is exact for the imported D5/Pati--Salam/SM representation
packet. It is not a proof that an action has selected that subgroup as the
unbroken physical gauge group. `SN2-NEUTRAL=Q0` and `SN2-NEUTRAL=SM1` therefore remain declared
conditional restriction horns.

Weinstein's 2025 wording does not choose between them. At `02:42:55` he says
the southeast-zero rolled shape is what he *thinks will be found* to be a
seesaw mechanism. The seminar twin uses neutrinos as an example of wildly
different masses. Neither passage identifies the source fields `nu` and
`zeta` with an ordinary `nu_L/nu_R` pair, supplies a reality map, or selects
electromagnetic neutrality rather than full-SM-singlet restriction.

The notation collision is load-bearing:

```text
source nu   = an Omega^0(Y,S) field
source zeta = an Omega^1(Y,S) field
```

The source symbol `nu` is not by itself the observed neutrino.

## 2. Both ambient halves and all four observation blocks

Up to the harmless global naming of chirality, the four K77 observation blocks
are

```text
ambient + : (2_L x 16)    + (2_R x 16bar)
ambient - : (2_L x 16bar) + (2_R x 16)
```

Each `2` is the two-component four-dimensional Weyl carrier. Since both `16`
and `16bar` have two `Q=0` states and one full-SM singlet, each block has rank
`4` or `2`. Deleting either ambient half would change the source-native object
and silently replace fundamental nonchirality by a selected effective half.

One graded `F+` or `F-` consists of two such blocks, hence ranks `8` or `4`.
The minimal rolled-field carrier uses one `F` occurrence in each of
`zeta+`, `zeta-`, `nu+`, and `nu-`, giving `32` or `16` columns.

The p.51 rolled complex places a second `F+` or `F-` inside the
gamma-traceless `832` in each `zeta` slot, in addition to the standalone `F`.
If both occurrences are retained, the honest ranks are `48` and `24`.
Choosing which `F` occurrence is observed requires a declared projector horn;
it is not settled by the word “neutral.”

## 3. The source-faithful block pencil

At zero momentum, set only the derivative parts `d_0` and `d_0-star` to zero.
For either restriction projector `P_R`, define

```text
V_ab^R = P_R varpi_ab P_R,
C_R(V_ab) = the corresponding restricted star-odot cell.
```

In the source row order

```text
(bar-zeta-, bar-zeta+, bar-nu-, bar-nu+)
```

and column order

```text
(zeta+, zeta-, nu+, nu-),
```

the exact neutral grammar is

\[
D_{0,R}=\begin{pmatrix}
C_R(V_{++})&C_R(V_{+-})&V^R_{++}&V^R_{+-}\\
C_R(V_{-+})&C_R(V_{--})&V^R_{-+}&V^R_{--}\\
-\bar V_{++}^{\star,R}&-\bar V_{+-}^{\star,R}&0&0\\
-\bar V_{-+}^{\star,R}&-\bar V_{--}^{\star,R}&0&0
\end{pmatrix}.
\]

Thus the released restriction has twelve permitted block cells and four exact
southeast zeros. “Permitted” is not “proved nonzero”: a projected cell can
vanish if its connection component has the wrong charge or representation
type.

The smallest faithful coefficient custody is also not twelve unrelated
scalars. Four unbarred maps `V_pp,V_pm,V_mp,V_mm` recur through the contracted
upper-left and raw upper-right quadrants. Four barred/formal-star partners
occur in the lower-left quadrant. No source reality map identifies the barred
partners with the unbarred four.

SN-1's twelve scalar symbols were sufficient for its uniform-homogeneity
lemma. They deliberately suppressed the internal-state and shared-owner legs.
SN-2 restores those legs; it does not retract the scaling lemma.

## 4. Charge and centre-class constraints

For a neutral-to-neutral restricted cell, only the charge-zero component of
the coefficient survives:

\[
P_R\,V_q\,P_R=0\qquad(q\ne0).
\]

Charge neutrality alone is not enough to authorize every map among the
retained states. Under the exact D5 centre grading,

```text
class(16)=1, class(16bar)=3, class(adjoint)=0.
```

Therefore an adjoint/class-zero internal coefficient preserves the parent
`16` versus `16bar` sectors. A `16 <-> 16bar` block needs a separately typed
class-two insertion. The fact that both endpoints have `Q=0` does not supply
that insertion.

The neutral weight spaces are invariant under the centralizer of the chosen
charge, not under the full Spin(10) group. Their centre labels record their
parent modules; SN-2 does not promote a neutral projector to a Spin(10)
subrepresentation.

At the ambient D7 corner level the exact classes are

```text
nu+ : 3,  nu- : 1,  zeta+ : 1,  zeta- : 3.
```

This preserves the source's two opposite-half class-homogeneous packages and
the prior result that a class-two bosonic insertion is required to recouple
them. It does not create a bare Majorana mass.

## 5. Novelty and prior-art boundary

The ingredients were already banked separately:

- K77-A constructed the four observation blocks and exact charge dictionary;
- CR-B computed the D5/D7 centre classes and four-corner pairing census;
- the August 4 trace-`q` wave assembled the sixteen equation-9.16 cells;
- SN-1 proved uniform linear scaling on the released zero-order grammar.

SN-2's novelty is the exact composition of those facts into the two neutral
restriction horns, the dimension correction, the shared cell-owner custody,
and the second-`F` alternative. It does not reconstruct any of the predecessors.

## 6. Claim ceiling and successor

**Proved conditionally:** the exact dimensions and block support above under
`SN2-NEUTRAL=Q0` or `SN2-NEUTRAL=SM1`, with either the minimal one-`F` or explicit both-`F`-occurrences zeta
horn.

**Not proved:** that either charge restriction is dynamically selected; which
`F` occurrence is physical; that any permitted cell is nonzero; a source
action, stationary background, coefficient scale, reality map, mass,
Majorana condition, physical quotient, pole, hierarchy, or empirical
neutrino prediction.

The next source-native calculation, if pursued, is finite: declare one of the
four horns, type each projected `varpi_ab` under the charge centralizer and
centre class, and compute the actual ranks of the twelve permitted block maps.
It must not be replaced by constructing an action, importing a `126`, or
calling this rolled `Omega^1/Omega^0` operator a standard neutrino mass matrix.

## Reproduction

```bash
python3 tests/channel-swings/joe_directed_sn2_equation916_k77_neutral_restriction_probe.py
python3 tests/channel-swings/joe_directed_sn2_equation916_k77_neutral_restriction_probe.py --selftest
```
