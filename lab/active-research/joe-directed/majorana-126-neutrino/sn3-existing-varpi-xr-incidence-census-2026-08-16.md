---
artifact_type: exploration
status: exploration
doc_type: source-native-existing-varpi-xr-incidence-census
created: 2026-08-16
work_item: SN3-B
channel: majorana_126_neutrino_mechanism
title: "SN-3B: K77 grade five contains the conditional X_R representation packet, while its class-two southeast fit requires an unbuilt zero-order/coindex descent and no source incidence is witnessed"
grade: "EXACT conditional representation and incidence census. Exterior weights of internal D5 grades 1, 3 and 5 are enumerated without floating point in the canonical CB-A/SN3-A Cartan convention; SM, B-L, same-Weyl Grassmann, K77 parent, real-half, one-form-coindex and source southeast-cell predicates are tested separately. The grade-five representation packet is exact. Zero-order/coindex descent, actual source-cell incidence, parent selection, coefficient ownership, action, vacuum, scale, reality quotient, analytic domain and mass are absent."
disposition: GRADE5_XR_REPRESENTATION_PACKET_LOCATED_IN_FULL_U6464_AND_SPIN_SKEW_HORNS__REAL252_RETAINS_BOTH_CONJUGATE_INTERNAL_LINES__BOTH_AMBIENT_K77_HALVES_RETAINED__PURE_INTERNAL_CLASS2_BUT_RAW_VARPI_COINDEX_COMPOSITION_CLASS0__TWO_CLASS2_SE_MATCHES_ONLY_AFTER_UNBUILT_ZERO_ORDER_COINDEX_DESCENT__NO_SOURCE_SE_INCIDENCE_WITNESS__NO_XR_COEFFICIENT_CONSTRUCTED
canon_verdict_change: none
steering_effect: "The finite X_R parent census is complete. Do not repeat a standard scalar-126 carrier/VEV calculation. The only remaining X_R antecedent is an independently supplied source-native zero-order incidence in one of the two class-2 southeast cells; conditional-build discipline forbids manufacturing it."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-source-native-neutrino-conditional-build-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-neutral-reality-charge-admissibility-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-wave-neutral-pencil-reprioritization-2026-08-16.md
  - explorations/conditional-build/selected-k77-full-u6464-action-bank-2026-08-08.md
  - explorations/conditional-build/selected-k77-grade5-unitary-parent-euler-closure-2026-08-10.md
  - explorations/conditional-build/selected-k77-majorana-reality-graded-domain-scope-2026-08-11.md
  - explorations/conditional-build/selected-k77-wedge-shiab-southeast-completion-2026-08-11.md
  - explorations/resolver-wave-d-native-126-connection-placement-2026-08-03.md
  - lab/active-research/joe-directed/majorana-126-neutrino/mj1-exact-126-majorana-block-2026-08-14.md
  - lab/active-research/joe-directed/majorana-126-neutrino/mj2-no-native-126-carrier-2026-08-14.md
scripts:
  - tests/channel-swings/joe_directed_sn3_existing_varpi_xr_incidence_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`. The operative object is an already-
> admitted Clifford component of the K77 connection parent and its possible
> equation-(9.16) cell incidence. No scalar `126`, Higgs field or VEV is
> imported.

# SN-3B — existing-`varpi` right-Majorana incidence census

## Result first

The K77 full-`U(64,64)` parent contains an exact internal grade-five packet
with every representation-level property that SN-2 required of `X_R`:

```text
SM singlet
B-L = -2 in all-left N^c N^c convention
same-Weyl charge-conjugating type
Grassmann-compatible internal symmetry
pure-internal endomorphism centre class 2
both conjugate internal Hodge lines retained in the real 252
both ambient K77 Weyl halves retained
```

Grades one and three do not pass. Grade one cannot reach `B-L=-2`. Grade three
does reach a `B-L=-2` line, but that line has `Y=-1`, not `Y=0`, and its
same-Weyl internal block is antisymmetric rather than the symmetric type
needed for a single identical `N^c` line.

The first centre-class line refers only to the pure-internal endomorphism. A
raw `varpi` component still carries its one-form coindex, also of class two,
so their unprocessed composition has net class zero. Matching a class-two
southeast cell requires an additional zero-order/coindex descent that has not
been built. Nor has any source-admitted grade-five component been placed in a
southeast equation-(9.16) cell. The displayed candidate has four southeast
zeros, and the source admits an unspecified nonzero rival without selecting or
typing it. Therefore the finite census ends at

```text
GRADE5_XR_PARENT_PACKET_LOCATED;
ZERO_ORDER_COINDEX_DESCENT_UNBUILT;
ACTUAL_SOURCE_SE_INCIDENCE_UNWITNESSED.
```

There is no `X_R` coefficient, Majorana term or mass construction here.

## 1. Eight-lens conditional-build preflight

1. **Source-native versus conventional comparator.** Use the internal D5
   weight identities only after typing their carrier as an internal Clifford
   summand of K77 `varpi`; do not import a scalar-`126` action or VEV.
2. **Exact observed weights.** Test `SU(3)`, `SU(2)_L`, `Y`, `Q` and `B-L`
   separately. `B-L=-2` alone is insufficient.
3. **Clifford grades.** Census grades one, three and five independently; do not
   infer grade-five placement from grade-five parent membership.
4. **Grassmann symmetry.** Join the internal transpose sign to the
   antisymmetric four-dimensional charge-conjugation form. A representation
   weight alone is not a same-Weyl quadratic.
5. **Centre class and source cell.** Recompute the four southeast net classes
   and retain `varpi`'s one-form coindex. Pure-internal class two is not yet a
   zero-order cell insertion, and ambient centre class two is not `B-L=2`.
6. **Full-unitary custody.** Distinguish the full `U(64,64)` parent, the
   Spin-native B-skew sector and the block-diagonal two-half parent. Odd grades
   exchange the two ambient Weyl halves.
7. **Real K77 and independent fields.** Retain both conjugate internal
   grade-five Hodge lines, both ambient K77 Weyl halves, and all four
   independent barred/unbarred source fields. The internal Hodge split and the
   ambient spinor-half split are different gradings. Do not select one complex
   Hodge half as a real field or identify bars with adjoints.
8. **Hostile promotion control.** Parent membership, action-covector support,
   southeast type compatibility and actual source incidence are four distinct
   predicates. Only the first three can pass in this wave.

The efficient calculation is a finite exterior-weight census followed by the
source-cell intersection. An action search, vacuum construction, standard
`126` calculation or phenomenology pass would answer a different question and
is off limits.

## 2. The typed structural bridge

The conventional MJ-1 calculation contains useful raw D5 representation
arithmetic, but none of its scalar-Higgs interpretation is transported. The
bridge used here is narrower and structural:

1. choose the already-declared observer split of the fourteen-dimensional
   Clifford vector into four-dimensional and internal ten-dimensional parts;
2. restrict an ambient grade-`q` K77 Clifford coefficient to its Lorentz-scalar
   pure-internal `Lambda^q(V_10)` summand;
3. apply the banked observed K77 charge dictionary to that internal summand;
4. retain the full real K77 grade-five space, whose complexification contains
   conjugate `126` and `126bar` Hodge halves; and
5. ask whether the resulting component has an exact equation-(9.16) source
   cell.

This bridge transports no action term, VEV, quotient, analytic domain or
observable. Consequently the labels `126/126bar` may be used as names for the
two complex D5 Hodge types, but a standard scalar-`126` mechanism has no
inferential role.

## 3. Exact grade-one, grade-three and grade-five census

Use an isotropic weight basis `u_i,v_i` for the internal vector `10`, with
weights `+e_i,-e_i`. The exact Pati--Salam/SM Cartan convention is

```text
B-L = (2/3)(w1+w2+w3)
T3L = (1/2)(w4+w5)
T3R = (1/2)(w4-w5)
Y   = T3R + (B-L)/2.
```

The probe enumerates all `10`, `120` and `252` exterior monomials. It also
checks the relevant `SU(3)` and `SU(2)_L` raising and lowering generators on
the named lines, so a zero Cartan weight inside a larger multiplet cannot be
mistaken for a singlet.

| internal grade | exact `B-L=-2` content | SM-singlet `X_R` line | result |
|---:|---|---|---|
| `1` | none | none | `FAIL_B_MINUS_L` |
| `3` | exactly `-e1-e2-e3` | none: `T3R=0`, hence `Y=-1` | `FAIL_HYPERCHARGE`; also wrong one-line Grassmann symmetry |
| `5` | six weight monomials | exactly `-e1-e2-e3+e4-e5` | `PASS_XR_PARENT_PACKET` |

For the grade-five winner,

```text
(B-L,T3L,T3R,Y) = (-2,0,+1,0).
```

Its conjugate is unique as well:

```text
+e1+e2+e3-e4+e5,
(B-L,T3L,T3R,Y) = (+2,0,-1,0).
```

This is the same Cartan convention used by CB-A and SN3-A. Swapping the
`T3L/T3R` formulas would instead name `-e1-e2-e3+e4+e5`; that is a different
Cartan labeling on this fixed basis, not a silent outer automorphism permitted
inside the present certificate.

The two internal lines are retained together in the real `252` subspace of
the K77 grade-five package. Naming one the `126` and the other the `126bar`
depends on the complex Hodge convention; the census does not select either
convention or discard either line. Separately, an odd grade-five Clifford
operator exchanges the two ambient K77 Weyl halves, and the census retains
both sides of that exchange. Internal Hodge conjugacy is not the ambient
half-spinor split.

## 4. Same-Weyl and Grassmann type

For the internal D5 same-Weyl blocks, the transpose sign on grade `q` is

```text
(-1)^(q(q-1)/2).
```

Thus

| grade | internal block | full one-line Grassmann coefficient after the antisymmetric 4D charge-conjugation form |
|---:|---|---|
| `1` | symmetric | alternating; passes this predicate |
| `3` | antisymmetric | symmetric; fails for one identical `N^c` line |
| `5` | symmetric | alternating; passes |

An extra antisymmetric family tensor could change the grade-three Grassmann
answer, but constructing or selecting one is prohibited here and would not
repair grade three's `Y=-1` failure. Grade five passes without that extra
datum.

## 5. Parent custody is not action or cell custody

Three levels must remain separate.

| level | grade 1 | grade 3 | grade 5 |
|---|---|---|---|
| full pointwise real `u(64,64)` parent | present, B-skew | present as `i` times B-self | present, B-skew |
| Spin-native B-skew connection sector | present | absent | present |
| selected fixed-background action-covector support | live | not live | live |

Every odd Clifford grade exchanges the two ambient Weyl halves. Therefore an
odd direction is not a connection generator in the chirality-preserving
block-diagonal `U(32,32) x U(32,32)` adjoint. Full `U(64,64)` admits it, and
the Spin-native B-skew horn admits grades one and five, but the source selects
neither action parent. The later theorem that unitary covariance forces a
full field carrier also does not convert every carrier component into a
selected connection coefficient.

This is why the exact grade-five packet is parent-horn conditional even before
source-cell incidence is asked.

## 6. Exact southeast centre-class and coindex intersection

SN-2 fixed the action-bilinear class convention. On the southeast rows and
columns,

```text
row classes:    cls(bar-nu-) = 1, cls(bar-nu+) = 3
column classes: cls(nu+)     = 3, cls(nu-)     = 1
c_cell = -c_row-c_column mod 4.
```

Therefore the exact table is

| southeast cell | net class required |
|---|---:|
| `(bar-nu-,nu+)` | `0` |
| `(bar-nu-,nu-)` | `2` |
| `(bar-nu+,nu+)` | `2` |
| `(bar-nu+,nu-)` | `0` |

The **pure-internal grade-five endomorphism** exchanges half-spinors and has
class two. But `varpi` is an ad-valued one-form, not that endomorphism alone.
Its vector coindex also has class two, so the raw composition has

```text
2 + 2 = 0 mod 4.
```

Pure-internal Lorentz scalarity does not erase this coindex. Consequently the
raw grade-five `varpi` type aligns with the two class-zero southeast positions,
not the class-two positions, and it still is not a zero-order `Omega0 ->
Omega0` map.

If a future source-native `SN3-ZERO-ORDER-COINDEX-DESCENT` horn removes or
transmutes the coindex while producing a declared class-two zero-order
insertion, the grade-five `X_R` packet would then type-match exactly

```text
(bar-nu-,nu-),
(bar-nu+,nu+).
```

Those are conditional matches, not present incidences. No such descent is
built or source-selected here. This centre/coindex test is separate from the
observed `B-L=-2` test; neither charge neutralizes or implies the other.

## 7. The decisive incidence ceiling

All four southeast cells are zero in the displayed equation-(9.16) candidate.
The source explicitly admits a rival with a nontrivial lower-right quadrant,
but supplies no cell formula, Clifford grade, coefficient owner or selection.
Hence

```text
parent membership                 PASS at grade 5 on declared horns
observed X_R representation       PASS at grade 5
same-Weyl Grassmann type          PASS at grade 5
raw varpi coindex composition      net class 0; not a zero-order SE map
class-2 southeast compatibility   CONDITIONAL in two cells after an unbuilt descent
actual source southeast incidence FAIL / UNWITNESSED
```

The banked K77 wedge-Shiab/southeast completion does not fill the last row. It
is a **principal operator family** constructed to repair Jordan propagation.
It is not a zero-order `X_R` insertion and does not place the grade-five
observed singlet in either class-two cell.

Nor does the selected action-covector's live grade-five support fill it.
Nonzero response somewhere in the action bank is not an equation-(9.16)
southeast cell incidence.

## 8. Archaeology and novelty audit

The result is a join of banked facts, with one important scope correction.

- **MJ-1 already owns** the exact D5 `1/3/5` same-Weyl decomposition and the
  grade-five singlet block. It is a conventional comparator. SN-3B reuses only
  that representation arithmetic through the explicit internal-Clifford
  bridge above; it does not reuse the scalar `126` or VEV mechanism.
- **MJ-2 already owns** zero `126` multiplicity in its declared `epsilon` and
  `$` scalar-VEV comparator carriers. That does not test the later full-
  `U(64,64)` K77 `varpi` coefficient bank. Its phrase “no native carrier” must
  not be transported across that missing bridge. SN-3B does not retract MJ-2's
  scoped calculation; it prevents its conventional conclusion from erasing
  the source-native grade-five parent packet.
- **Resolver Wave D already owns** a different K95/right-H route: native grade
  six contracts to an effective real grade-five `252`. It left source-owned
  nonzero placement open. That real-form and contraction route is not the
  direct K77 B-skew grade-five parent membership classified here.
- **The full K77 bank already owns** grade-five action support and **the
  grade-five closure artifact already owns** its parent burden. Neither had
  decomposed the pure-internal grade-five component against the SN-2 `X_R`
  packet or intersected it with the four exact southeast cell classes.
- **SN-2 already owns** the `X_R` requirement and the local reality/charge
  separation. SN-3B's new content is the finite grade-by-grade inventory and
  the negative actual-incidence row.

Thus the genuinely new statement is narrow:

> The source-native K77 parent is representation-rich enough to contain the
> complete right-Majorana compensator type at grade five, but the released
> source grammar supplies neither the zero-order/coindex descent needed to
> place its pure-internal class-two endomorphism nor an incidence in either
> southeast cell where a descended class-two insertion could fit.

## Claim ceiling and reprioritization consequence

**Established conditionally:** under the full-`U(64,64)` or Spin-native
B-skew parent horn and the declared observer split, the pure-internal
grade-five coefficient space contains one `X_R` line and its conjugate. It
passes exact SM, `B-L`, same-Weyl and Grassmann tests. Conditional on an
additional zero-order/coindex descent with class-two output, it would match
exactly two southeast cell types.

**Not established:** the zero-order/coindex descent; that the source chooses
either parent; that `varpi` has a nonzero value in the relevant grade-five
direction; that either equation-(9.16) southeast cell contains it; that a
local reality horn preserves it; or that an action, vacuum, quotient, global
domain, coefficient, hierarchy or physical mass exists.

The `X_R` representation census should now be banked rather than repeated.
Continuing this branch would require a new source-native southeast-incidence
owner. Conditional-build discipline forbids inventing that owner, deriving a
vacuum to select it, or falling back to the standard scalar-`126` route.

## Reproduction

```bash
python3 tests/channel-swings/joe_directed_sn3_existing_varpi_xr_incidence_probe.py
```

Expected final line:

```text
PASS: grade 5 contains the conditional X_R parent packet, but no source southeast incidence is witnessed.
```
