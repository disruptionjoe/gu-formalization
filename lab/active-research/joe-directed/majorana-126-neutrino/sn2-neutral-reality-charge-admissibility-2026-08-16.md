---
artifact_type: exploration
status: exploration
doc_type: source-native-neutral-reality-charge-admissibility-gate
created: 2026-08-16
work_item: SN-2
channel: majorana_126_neutrino_mechanism
title: "SN-2: local neutral-bilinear admissibility requires independent source-cell, K77-reality, Grassmann, centre-class, and observed-charge tests"
grade: "EXACT conditional local classifier. It composes the identity-grade equation-9.16 four-field grammar, the banked real-K77 Z/4 corner classes, the exact observed neutral charge dictionary, and the two already-classified local graded reality/pairing horns. It neither constructs nor selects a zero-order coefficient, action, global domain, vacuum, quotient, physical mass, or phenomenology."
disposition: "DIRAC_CAPABLE_REQUIRES_A_PRINTED_CROSS_DEGREE_CELL_WITH_WEAK_DUAL_TYPE__RIGHT_MAJORANA_CAPABLE_REQUIRES_A_DECLARED_SOUTHEAST_CELL_WITH_ITS_EXACT_ZERO_OR_TWO_CENTRE_REQUIREMENT_AND_OPPOSITE_B_MINUS_L__Q_ZERO_IS_NOT_SM_SINGLET__LOCAL_REALITY_GRAPH_IS_NONCIRCULAR_BUT_NOT_GLOBAL_ADJOINT_OR_DOMAIN__ZETA_NU_ARE_FORM_DEGREES_NOT_NEUTRINO_CHIRALITIES"
canon_verdict_change: none
steering_effect: unchanged
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - lab/sources/source-claim-register.yaml
  - explorations/resolver-wave-k77a-real-spinor-observation-atomic-particle-crosswalk-2026-08-04.md
  - explorations/conditional-build/selected-k77-action-adjoint-weight-classification-2026-08-11.md
  - explorations/conditional-build/selected-k77-graded-green-reality-graphs-2026-08-11.md
  - lab/active-research/joe-directed/class-shift/cs1-first-order-shift-is-the-chirality-grading-2026-08-15.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn1-observed-neutrino-mass-pencil-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
scripts:
  - tests/channel-swings/joe_directed_sn2_neutral_reality_charge_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`. SN-2 classifies conditional local
> bilinears on Weinstein's released four-field/K77 carrier. It does not import
> or test the conventional `126` mechanism.

# SN-2 — neutral reality and charge admissibility

## Result first

Electric neutrality is only the first and weakest check on a candidate
neutrino bilinear. The exact observed neutral lines are

```text
L   = nu_L:   (SU(3), SU(2)_L, Y, Q, B-L) = (1, 2, -1/2, 0, -1)
N^c = nu_L^c: (SU(3), SU(2)_L, Y, Q, B-L) = (1, 1,    0, 0, +1).
```

`N^c` is the all-left representative of the physical right-handed neutrino.
The physical `nu_R` has `B-L=-1`; silently assigning that physical charge to
the all-left `N^c` row reverses the charge test.

The exact local classification is:

| candidate in all-left notation | `Q` | full observed type before a coefficient | minimum conditional coefficient | status |
| --- | ---: | --- | --- | --- |
| `L N^c` | `0` | weak doublet, `Y=-1/2`, `B-L=0` | `X_D` type requirement: dual weak doublet, `Y=+1/2`, `B-L=0`, in one declared printed cross-degree cell after a slot-to-line incidence horn | `DIRAC_NOT_EXCLUDED_AFTER_TYPED_INCIDENCE__COEFFICIENT_MISSING` |
| `N^c N^c` | `0` | SM singlet, `Y=0`, `B-L=+2` | `X_R` type requirement: SM singlet, `B-L=-2`, in one declared southeast cell after a slot-to-line incidence horn, with that cell's exact `0` or `2` centre requirement | `RIGHT_MAJORANA_NOT_EXCLUDED_AFTER_TYPED_INCIDENCE__COEFFICIENT_MISSING` |
| neutral component of `L L` | `0` | `Y=-1`, `B-L=-2`; its weak tensor and Grassmann symmetry are not a bare singlet statement | `X_L`: compensating weak tensor, `Y=+1`, `B-L=+2`, with the required source-cell and transpose type | `LEFT_MAJORANA_CAPABLE_ONLY_AFTER_MORE_TYPING` |
| `bar L L` or `bar N N` | `0` | charge-neutral density pairing | a Lorentz/operator incidence joining the required Weyl slots | `NOT_AUTOMATICALLY_A_ZERO_ORDER_MASS` |

Thus `Q=0` neither implies an SM singlet nor distinguishes Dirac from
Majorana. `L` itself is electrically neutral but belongs to a weak doublet.
`N^c N^c` is an SM singlet but fails `B-L` neutrality. `L N^c` is `B-L`
neutral but is not an SM singlet without a further coefficient.

These are representation-level admissibility statements, not masses. SN-2
declares the possible coefficient types and asks how they would fit. It does
not construct their owner or origin.

## 1. Source grammar is a separate axis

Draft equation 9.16 starts with four independent classical fields:

```text
nu, bar-nu     in Omega^0(Y,S)
zeta, bar-zeta in Omega^1(Y,S).
```

The barred row order is

```text
(bar-zeta-minus, bar-zeta-plus, bar-nu-minus, bar-nu-plus)
```

and the unbarred column order is

```text
(zeta-plus, zeta-minus, nu-plus, nu-minus)^T.
```

These labels encode form degree and the source's displayed half labels. They
do **not** say

```text
zeta = nu_L,       nu = nu_R,
```

or the reverse. The source's seesaw-shaped zero is an `Omega^1/Omega^0`
block statement, not a published assignment of those degrees to four-
dimensional neutrino chirality.

Accordingly the source-cell and observed-representation tests must be joined
only by a declared observation restriction. No such restriction is selected
here.

## 2. Exact source-cell centre-class classifier

CS-1 fixes the ambient real-K77 `Z/4` classes:

```text
cls(nu+)   = 3       cls(nu-)   = 1
cls(zeta+) = 1       cls(zeta-) = 3.
```

For an action cell with barred row class `c_r` and unbarred column class
`c_c`, the **net central character still required by the cell** is

```text
c_cell = -c_r-c_c mod 4.
```

This is the action-bilinear convention used in CS-1. For the winning uniform
reading, the row classes are `(1,3,1,3)` and column classes are `(3,1,3,1)`.
The six printed `varpi`-only cells have `c_cell=0`: their underlying class-2
adjoint component and class-2 one-form leg combine to net class zero. Calling
the total cell class zero must not be shortened to “the connection component
has class zero.”

Applied to the printed row/column grammar:

| equation-9.16 quadrant | source arrow read from the barred row | printed status | class requirement |
| --- | --- | --- | ---: |
| northwest | `zeta+ -> zeta-`, `zeta- -> zeta+` through the Shiab-shaped cells | nonzero | coefficient grammar already includes the displayed class shift; no neutrino interpretation follows |
| northeast | four `bar-zeta/nu` cells; two carry derivatives and two are `varpi`-only | nonzero | the two `varpi`-only cells have net `c_cell=0`; derivative cells obey the separate order-one rule |
| southwest | four `bar-nu/zeta` cells; two carry derivatives and two are `varpi`-only | nonzero | the two `varpi`-only cells have net `c_cell=0`; derivative cells obey the separate order-one rule |
| southeast `(bar-nu-,nu+)`, `(bar-nu+,nu-)` | two diagonal-in-class cells | zero in the released candidate; nonzero rivals admitted | net `c_cell=0` |
| southeast `(bar-nu-,nu-)`, `(bar-nu+,nu+)` | two cross-class cells | zero in the released candidate; nonzero rivals admitted | net `c_cell=2` |

This yields the sharp local intersection:

```text
Dirac-capable cross-degree candidate:
  one exact printed source cell with its order/class type
  AND observed dual weak type AND B-L 0.

Right-Majorana-capable southeast candidate:
  one exact southeast cell with c_cell 0 or 2 as listed above
  AND observed SM-singlet type AND opposite B-L charge.
```

The two uses of the word “charge” are not interchangeable. Ambient K77 centre
class is a central character of the source representation. `B-L` is an
observed internal gauge character. In particular,

```text
ambient centre class 2  !=  B-L charge 2.
```

An object must pass both tests separately. Passing either southeast centre
test cannot break or neutralize `B-L`, and passing the observed charge test
cannot decide which southeast cell contains the neutral line. The missing
source-slot-to-observed-line restriction is therefore load-bearing rather than
a notational nuisance.

## 3. The two already-banked local pairing horns

No new pairing family is introduced. The complete tested Spin-natural,
degree-diagonal local classification supplies two projective horns:

| horn | pairing line on `(Omega1+,Omega1-,Omega0+,Omega0-)` | pairing symmetry | operator condition | Grassmann coefficient |
| --- | --- | --- | --- | --- |
| `H_sym` | `(1,1,1,1)` | symmetric | `P X + X^T P=0` | `P X` alternating |
| `H_skew` | `(1,-1,-1,1)` | skew | `P X - X^T P=0` | `P X` alternating |

Both horns were checked at full carrier rank `1920`, over `GF(1009)` and
`GF(1013)`, in all fourteen directions. Both define exact noncharacteristic
graded Lagrangian reality graphs. The source selects neither.

A new candidate `X_D`, `X_R`, or `X_L` must be checked against the appropriate
transpose identity for the declared horn. The fact that the pre-existing
operator passes both classifications does not automatically admit an
arbitrary new zero-order insertion.

## 4. A noncircular local fixed graph can be declared

Start with independent odd variables `(psi,bar)` as the source does. Let `K`
be entrywise conjugation on the complexification of the real K77 carrier and
let `P` be one of the two invariant pairing horns. Define

```text
R_P(psi,bar) = (P^-1 K(bar), P K(psi)).
```

Then

```text
R_P^2 = 1,
Fix(R_P) = { (psi,bar) : bar=P K(psi) }.
```

This is a local fixed graph/reality restriction, not a quotient. The
construction does not write `bar=psi^dagger` and does not use a formal
adjoint to manufacture its own definition. It begins from the doubled source
variables, supplies an anti-linear exchange, and tests the bilinear transpose
identities separately. For a noncharacteristic normal coefficient `A`, the
banked graded Green graph criterion is

```text
P^T A + A^T P = 0.
```

Both horns pass in their respective adjoint parity.

The ceiling is strict. This pointwise/noncharacteristic fixed graph does not:

- replace the four source fields by adjoints inside the source transcription;
- prove that the complete bulk equation-9.16 operator preserves the graph;
- select a horn or a zero-order coefficient;
- close the null/characteristic, Sobolev, Calderon, BFV, or physical domain; or
- identify a pole, eigenvalue, mass, hierarchy, or particle.

The source variables remain independent before the horn is declared.

## 5. Minimal additional typed data

The next conditional construction does not need an action search. It needs one
finite typed coefficient packet:

| datum | `X_D` Dirac-capable packet | `X_R` right-Majorana-capable packet |
| --- | --- | --- |
| source slot | one declared printed cross-degree `Omega0 <-> Omega1` cell | one declared source-admitted southeast `Omega0 -> Omega0` rival cell |
| ambient/net cell class | the exact printed cross-degree cell type; net `0` for the existing `varpi`-only cells | `0` in `(bar-nu-,nu+)`/`(bar-nu+,nu-)`, or `2` in `(bar-nu-,nu-)`/`(bar-nu+,nu+)` |
| observed representation | dual of the `L N^c` weak-doublet type | SM singlet |
| hypercharge | compensates `Y=-1/2` | `0` |
| `B-L` | `0` | `-2` for all-left `N^c N^c`, equivalently `+2` for physical `nu_R nu_R` |
| four-dimensional type | explicit opposite-Weyl Lorentz contraction | explicit same-Weyl charge-conjugating contraction |
| pairing horn | choose `H_sym` or `H_skew` | choose `H_sym` or `H_skew` |
| Grassmann test | corresponding `P`-transpose identity | corresponding `P`-transpose identity |
| reality covariance | intertwine the declared `R_P` | intertwine the declared `R_P` |

This table is a requirement, not an inventory claim. The repository has not
identified either packet with an action-owned connection component. The
conventional scalar realization of such quantum numbers is outside this
source-native gate and has no inferential force here.

## 6. Eight-lens preflight and novelty audit

1. **Independent-source-field lens:** bars and unbars stay independent until
   an explicit `R_P` horn is declared.
2. **Real-Clifford lens:** entrywise `K` is a local anti-linear involution that
   preserves ambient half; it is not by itself an invariant duality pairing.
3. **Invariant-bilinear lens:** `P_sym` and `P_skew` are the complete tested
   local action-natural lines; neither selects a coefficient.
4. **Charge/centre lens:** source `Z/4`, `Q`, hypercharge, weak type, and `B-L`
   are separate predicates.
5. **Four-dimensional-chirality lens:** `L`, `N^c`, and physical `nu_R` must
   not be interchanged, and source plus/minus labels are not those names.
6. **Grassmann-transpose lens:** the operative condition is alternation of
   `P X`, not an untyped demand that `X` be Hermitian.
7. **Representation-typing lens:** `Q=0` is weaker than full-SM singlet status;
   every coefficient declares its source and observed types.
8. **Hostile semantic lens:** local real structure, invariant bilinear,
   Grassmann symmetry, fixed graph, global reality/domain, and physical mass
   remain six different objects.

Prior ownership is dense:

- K77-A owns the four complex `2 x 16` blocks, their conjugation exchange, and
  the imported exact charge dictionary.
- The action-adjoint classification owns the two pairing lines.
- The graded Green result owns the noncharacteristic local fixed graphs.
- CS-1 owns the four corner classes and class-shift rule.
- SN-1 owns the uniform-scaling result and the statement that the released
  southeast zero is not a hierarchy mechanism.

SN-2's new content is only the exact cross-classification of those banked
objects on the two neutral observed lines and the resulting minimum
`X_D/X_R` typed packets. It does not re-claim the predecessor legs.

## 7. Claim ceiling and successor

**Established conditionally:** after a declared source-slot-to-observed-line
incidence, if a locally covariant coefficient satisfies one row of the
minimum-datum table, then the corresponding neutral bilinear is not excluded
by the tested source-cell, charge, centre-class, local reality, or Grassmann
tests. `X_D` and `X_R` are type requirements, not admitted coefficients.

**Not established:** that either coefficient exists in the source action;
that an equation-9.16 source slot is `L` or `N^c`; that a horn is selected;
that `B-L` is broken; that a global reality condition or physical quotient
exists; or that any neutrino has a Dirac or Majorana mass.

The next bounded source-native calculation is an observed coefficient-
incidence census: restrict the already-admitted equation-9.16 coefficient
types to the exact neutral lines and ask whether an `X_D` or `X_R` packet is
present. It must remain a conditional representation calculation. Constructing
an action, vacuum, external selector, conventional scalar owner, or
phenomenology is off limits.

## Reproduction

```bash
python3 tests/channel-swings/joe_directed_sn2_neutral_reality_charge_probe.py
```

Expected final line:

```text
PASS: SN2 separates source cells, local reality, Grassmann parity, centre class, and observed neutral charges.
```
