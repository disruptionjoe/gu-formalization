---
artifact_type: exploration
status: exploration
doc_type: source-native-neutrino-descent-receipt-archaeology-absence-census
created: 2026-08-17
work_item: SN-4
channel: majorana_126_neutrino_mechanism
title: "SN-4: the licensed read-only archaeology finds NEITHER an already-owned source-native zero-order/coindex descent NOR an equation-9.16 slot-to-observed-line receipt anywhere in the source-native K77 record; every token-shaped candidate is a different object by its own lineage line, and the neutrino-lane pause stands"
grade: "EXACT record census with lineage-receipt verification, never token match. 274 selected-k77 conditional-build files, 154 selected-k77 lab/sources files, 24 k77-wave2 files, and 5 named singletons (the s9 extraction, the Curt-Iceberg zero-order reinspection, the K77 bank custody record, the K77-A atomic crosswalk, Resolver Wave D) are swept under 8 declared pattern families; every hit list is pinned; 23 candidate lineage quotes are byte-verified in their owning files; a planted fake receipt is caught and credited by the machinery and a planted wrong-lineage candidate is rejected; zero credits over the real corpus. NOT constructed: any descent, coefficient, southeast owner, action, vacuum, external datum, family row, scale relation, quotient, domain, or conventional 126 mechanism."
disposition: ABSENT_BOTH__DESCENT_ABSENT_AND_NEAREST_MECHANISM_AFFIRMATIVELY_REFUTED_BY_MD1_RECORD__RECEIPT_ABSENT_WITH_SLOT_HALF_AND_OBSERVED_LINE_HALF_BANKED_SEPARATELY_BUT_JOIN_OWNED_NOWHERE__EVERY_TOKEN_CANDIDATE_TYPED_AS_DIFFERENT_OBJECT_BY_ITS_OWN_LINEAGE_LINE__NEUTRINO_LANE_PAUSE_STANDS__REOPEN_ANTECEDENT_EMPTY_IN_CURRENT_RECORD
target_claim: "NONE-NOT-A-KILL at the source layer. The target is INTERNAL: the antecedent of SN-3's reopen condition, quoted verbatim in section 2 ('SN-3 reopens only if archaeology finds an already-owned source-native zero-order/coindex descent and a matching equation-(9.16) slot-to-observed-line receipt.'). No source claim is touched: SC-OP-04 displays equation 9.16 at candidate strength only, SC-OP-05 admits the southeast rival WITHOUT populating it, and the s9 extraction certifies no descent — the source never claimed to own either object, so their absence kills no source claim."
target_claim_verdict: "REOPEN ANTECEDENT EMPTY AS CURRENTLY SPECIFIED. Both objects are ABSENT from the licensed corpus: no file owns a source-native class-two zero-order Omega0 -> Omega0 varpi insertion (the nearest mechanism-shaped reading, scalar descent of the pulled-back connection, is affirmatively refuted in the routing method's own withdrawn-clause record via MD-1/LA-8), and no file joins a printed equation-9.16 cell to an observed L^0/N^c line (the slot half is banked in the s9 extraction, the observed-line half in the K77-A crosswalk, and the join exists nowhere). SN-3's pause therefore stands. This verdict types the record; it does not restart SN work and constructs nothing on the forbidden list."
canon_verdict_change: none
steering_effect: "The SN lane's reopen condition is now adjudicated, not merely pending: the archaeology it licensed has been run and both antecedent objects are ABSENT, so the neutrino lane stays paused and the next exploration wave should reopen the broader portfolio exactly as SN-3 directed. Do not re-run this archaeology without new record: the probe pins the searched corpus, patterns, and lineage quotes, and fails loudly if any of them drifts. The verdict flips only on the two future objects named in section 8."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/sources/source-claim-register.yaml
  - lab/sources/gu-2021-draft-s9-fermionic-operator-extraction-2026-08-04.md
  - lab/sources/curt-iceberg-fermion-zero-order-reinspection-2026-08-04.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-wave-varpi-incidence-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-existing-varpi-xr-incidence-census-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-existing-varpi-xd-incidence-census-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-source-slot-neutral-line-incidence-classifier-2026-08-16.md
  - lab/process/hostile-reviews/2026-08-16-joe-directed-sn3-varpi-incidence-review.md
  - lab/process/homonym-register.yaml
  - explorations/conditional-build/selected-k77-four-field-zero-order-port-2026-08-10.md
  - explorations/conditional-build/selected-k77-wedge-shiab-southeast-completion-2026-08-11.md
  - lab/sources/selected-k77-wedge-shiab-southeast-completion-source-return-2026-08-11.md
  - explorations/k77-wave2-trace-q-coefficient-zero-order-reality-selection-2026-08-04.md
  - explorations/k77-wave2-actual-draft916-k77-blockwise-adjoint-descent-2026-08-04.md
  - explorations/resolver-wave-k77a-real-spinor-observation-atomic-particle-crosswalk-2026-08-04.md
  - explorations/resolver-wave-d-native-126-connection-placement-2026-08-03.md
scripts:
  - tests/channel-swings/joe_directed_sn4_descent_receipt_archaeology.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `SOURCE_NATIVE_ROUTE`. The adjudicated objects are the two
> source-native antecedents named by SN-3's steering — a zero-order/coindex
> descent of the K77 `varpi` component and an equation-(9.16)
> slot-to-observed-line receipt. No comparator result is used to adjudicate
> either; comparator-adjacent candidates (the Curt-Iceberg Higgs placement,
> seesaw analogies) enter only to be typed and rejected under their own
> routing fences.

```gu-typed-objects
result: SN-4 -- the two SN-3 reopen antecedents (source-native zero-order/coindex descent;
  equation-9.16 slot-to-observed-line receipt) are ABSENT from the licensed source-native
  K77 record; every token-shaped candidate is a different object by its own lineage line
carrier: the equation-9.16 printed sixteen-cell grammar (s9 extraction) together with the
  selected-k77/k77-wave2 record files that could own its descent or receipt
  LAYER=source-print CHIRALITY=S-FULL-DIRAC
  # CN-2/s9: `S` at eq (9.16) is the full Dirac bundle, both Weyl halves; this census
  # inherits that typing and never selects a half.
pairing: NONE
real_structure: N/A
  # bars remain independent classical fields throughout the record (SC-OP-04); no reality
  # map is imposed or adjudicated here.
grading: internal Clifford grade, connection one-form-leg class, and positional Z/4 net
  cell class kept separate exactly as the SN-3 hostile repair requires; the sought descent
  is typed against that separation
action_owner: repository-construction
  # this census is a repository record adjudication; CREDIT itself demanded source-action
  # custody (SOURCE-STATES/DISPLAYS/SELECTS) on the candidate's side, and none had it.
target: the pair (SN3-ZERO-ORDER-COINDEX-DESCENT, equation-9.16 slot-to-observed-line
  receipt), adjudicated ABSENT MAP-TYPE=not-a-map
  # the verdict is a record census, not a map; the sought descent itself, if it ever
  # appears, would be MAP-TYPE=contraction with declared Omega0 -> Omega0 class-two output.
```

# SN-4 — the licensed archaeology, run to a verdict

## 0. Verdict first

| object | SN-3 specification locus | verdict | nearest near-collision, typed and rejected |
|---|---|---|---|
| source-native zero-order/coindex descent | SN3-B section 6; wave doc section 3 | **ABSENT** | "zero-order port" (a cross-degree `Omega0 -> Omega1` cell, not an `Omega0 -> Omega0` insertion) |
| equation-9.16 slot-to-observed-line receipt | SN3-A result block; SN3-C sections 3-4 | **ABSENT** | seesaw-slot analogy (southeast zero retained, carried with explicit not-a-prediction fences) |

Neither half-credit survives inspection. The receipt's two ingredients exist
separately — the slot ledger is banked in the s9 extraction and the observed
charge dictionary in the K77-A crosswalk — but the receipt IS the join, and
the join is owned nowhere. That is ABSENT, not PARTIAL: SN-3 asked for the
incidence, not for its prerequisites, which SN-3 itself already banked.

The neutrino-lane pause stands. This verdict types the record; it does not
restart SN work, and nothing on the forbidden list (descent, coefficient,
southeast owner, action, vacuum, external datum, family row, scale relation,
quotient, domain, conventional 126 mechanism) is constructed.

## 1. Preflight lenses (six, problem-matched)

1. **Source philology.** The specification must come from SN-3's own text,
   not from a paraphrase; every credit or rejection must quote the candidate
   file's own words. Both sides are quoted below and byte-verified by the
   probe.
2. **Record linkage.** The `v_PSB` near-collision
   (`lab/process/homonym-register.yaml`, cp1 provenance: two distinct objects,
   same token, same stabilizer dimension) proves token joins are structurally
   blind. Every join below is lineage-verified: what the file says it is, what
   it cites, what carrier it types.
3. **Operator-order/degree/class typing.** Four gradings must never be
   conflated: differential-operator order (zero-order vs first-order), form
   degree (`Omega0` vs `Omega1`), internal Clifford grade, and the Z/4
   positional net cell class. The sought descent is a statement in all four at
   once: a degree-preserving zero-order `Omega0 -> Omega0` insertion with
   class-two output from a grade-five one-form coefficient. Candidates that
   satisfy one grading are not candidates that satisfy the conjunction.
4. **Supersession hygiene.** A superseded or corrected candidate does not
   count as owned; conversely a correction record (the routing method's
   withdrawn clause) counts as evidence and must not be skipped because it
   sits inside a banner.
5. **Adversarial reading, both directions.** The lane wants to reopen — so
   every near-miss is a temptation to credit. Symmetrically, a tidy pause is
   comfortable — so every genuine owned object is a temptation to
   over-reject. Section 9 runs both attacks.
6. **Homonym discipline.** "Descent" already has two senses in this record
   (global/overlap descent in the s9 extraction; zero-order/coindex descent in
   SN-3), and "zero-order port" is centimeters from "zero-order insertion."
   Neither is in the homonym register yet; both are typed explicitly below.

Retrieval before work: SN-3's four artifacts and hostile review, the SN-2
read packet, the s9 extraction, the source claim register rows SC-OP-04/05
and SC-FER-03, the routing method, and the homonym register were read before
any sweep was run.

## 2. The specification, in SN-3's own words

The license (wave doc, `steering_effect`, quoted verbatim):

> "Permit only a read-only archaeology check for an already-owned
> source-native zero-order/coindex descent together with an equation-9.16
> slot-to-observed-line receipt. If absent, pause this lane; do not construct
> the missing descent, coefficient, southeast owner, action, vacuum, external
> datum, family row, scale relation, quotient, domain, or conventional 126
> mechanism."

The reopen condition (wave doc, section "Reprioritization"):

> "SN-3 reopens only if archaeology finds an already-owned source-native
> zero-order/coindex descent and a matching equation-(9.16)
> slot-to-observed-line receipt."

**Object 1 — the descent** (SN3-B, section 6, the defining sentence):

> "If a future source-native `SN3-ZERO-ORDER-COINDEX-DESCENT` horn removes or
> transmutes the coindex while producing a declared class-two zero-order
> insertion, the grade-five `X_R` packet would then type-match exactly
> `(bar-nu-,nu-)`, `(bar-nu+,nu+)`."

with the wave doc's typing constraint: "A pure-internal endomorphism may be
scalar in its endomorphism indices while its `varpi` coefficient still
carries a one-form coindex." So a credit requires, jointly: (a) an
`Omega0 -> Omega0` (degree-preserving, zero-order) insertion, (b) built from a
`varpi` coefficient, (c) with declared class-two output, (d) with
**source custody** — already owned, not constructible ("source-native ...
already-owned"). SN3-B's own census line for the current record:
`ZERO_ORDER_COINDEX_DESCENT_UNBUILT`.

**Object 2 — the receipt** (SN3-A, the defining sentences):

> "But no source receipt identifies any formally compatible position with the
> observed `L,N^c` lines, nor supplies the needed one-form coindex/Clifford
> contraction."

and its census line `OBSERVED_LINE_AND_COINDEX_INCIDENCE_UNWITNESSED`; on the
Majorana side SN3-C's exact custody: "displayed X_R ports = 0," and
"source-populated X_R positions = 0." So a credit requires, jointly: (a) a
named printed equation-(9.16) cell (slot), (b) a named coefficient owner,
(c) an identified observed line (`L^0`/`N^c` or a southeast pair), and (d) a
placement — an incidence witness, not custody prose (SC-FER-03's
accommodation list is "source assignments, not derivations", s9 fence).

## 3. What was searched, exactly

Corpus (pinned by the probe; a count drift fails the probe by design):

| group | glob | count |
|---|---|---:|
| G1 | `explorations/conditional-build/selected-k77-*.md` | 274 |
| G2 | `lab/sources/selected-k77-*.md` (source-return custody records) | 154 |
| G3 | `explorations/k77-wave2-*.md` (the eq-9.16 blockwise work, incl. both `draft916` files) | 24 |
| singletons | s9 extraction; Curt-Iceberg zero-order reinspection; `k77-exact-bank-source-custody`; K77-A atomic crosswalk; Resolver Wave D | 5 |

Pattern families (regexes as compiled by the probe):

| family | regex | result |
|---|---|---|
| F1 coindex | `coindex` | **0 hits in the entire corpus** — the concept vocabulary exists only in the SN lane and the 08-03 resolver waves; the K77 record never states the object in any spelling of this token |
| F2 observed lines | `N\^c\|L\^0\|neutral line\|observed line\|nu_R\|seesaw` | 0 hits in G1; 0 hits in G2; exactly 6 files in G3, every one a seesaw-analogy mention with an explicit fence (section 6) |
| F3 particle words | `\b(lepton\|neutrino)\b` (case-insensitive) | exactly 1 hit in G1+G2+G3, and it is itself a fence: "the seesaw source analogy is not a mass or neutrino prediction." |
| F4 zero-order and varpi | `zero.order\|zeroth` AND `varpi` | 15 files in G1, list pinned; none contains an `Omega0 -> Omega0` insertion claim |
| F5 southeast | `southeast\|lower.right` (case-insensitive) | 22 files in G1, list pinned; adjudicated in section 5 |
| F6 eq 9.16 | `9\.16` | 14 files in G1, list pinned |
| F7 degree-preserving arrows | `Omega\^?0.*->.*Omega\^?0` (line-bounded) | exactly 2 lines in G1, both typed: the displayed operator's total block signature, and a twistor codifferential complex |
| F8 concept terms | `interior product\|iota_\|zero-order insertion\|degree-preserving insertion` | 5 files, all symplectic/boundary/observation contractions (section 5, row 7) |

On top of the sweeps, a **credit classifier** encodes the section-2
conjunctions and is run over every file in G1+G2+G3+singletons: zero credits
for the descent, zero credits for the receipt. Its power is proven by planted
controls: a synthetic fake receipt satisfying all conjuncts MUST be found by
the pattern families and credited by both predicates (it is), and a synthetic
wrong-lineage candidate carrying the tempting tokens ("zero-order port",
"adjoint descent", `varpi`, eq 9.16) MUST be rejected (it is).

## 4. Descent candidates, adjudicated on lineage

Every rejection quotes the candidate's own file. Spec side for all rows:
SN3-B section 6 as quoted in section 2.

**D1. `selected-k77-four-field-zero-order-port-2026-08-10.md` — REJECTED:
different object under a colliding token.** Its own typing line:

> "Its ordinary `Omega0(S) -> Omega1(S)` connection cell has exact rank
> `128`"

The "zero-order port" is the cross-degree northeast cell — input degree zero,
output degree one, coindex retained — precisely the raw cell SN3-B classified
as net class zero. It is not degree-preserving, removes nothing, and its own
ceiling adds: "image inclusion does not construct the graph map or make it
physical."

**D2. `lab/sources/curt-iceberg-fermion-zero-order-reinspection-2026-08-04.md`
— REJECTED: a secondary search directive, not an owned mechanism.** The
closest descent-shaped record in the entire corpus is the row typed
`CURT-RECONSTRUCTS-ZERO-ORDER-PLACEMENT`, and the same row's own fence reads:

> "the exact `varpi_rs` cell and reduction representation are not identified"

with the file's closing custody: "`SOURCE-SILENT`: the exact draft `varpi_rs`
cell carrying a physical Higgs, the barred/unbarred reality map, and the
coefficient selecting trace-`q` left versus right placement." Three further
defects: it is Curt's reconstruction under a `SOURCE-CORRECTS` collision
(Weinstein's two-layer and adjoint-Higgs corrections), it aims at a Higgs
doublet rather than the grade-five `X_R` line, and its underlying mechanism —
vertical components appearing as scalars after pullback — is **affirmatively
refuted** in the current record: the routing method's withdrawn-clause
correction states, from MD-1,

> "an ad-valued one-form descends to a **one-form**, not to scalars"

So the nearest mechanism is not merely unowned; its scalar-descent reading is
dead on the books.

**D3. `k77-wave2-trace-q-coefficient-zero-order-reality-selection-2026-08-04.md`
— REJECTED: locates a port to build, owns nothing.** Its own words: "Curt has
located a zero-order port worth building" and

> "It does **not** identify which `varpi_rs` block carries the physical
> Higgs"

A directive to search is the opposite of an already-owned object.

**D4. `k77-wave2-actual-draft916-k77-blockwise-adjoint-descent-2026-08-04.md`
— REJECTED: "descent" here is the other homonym, and it is open.** Its result
line ends `FULL_H_DESCENT_OPEN`: the descent in this filename is global
H-equivariant/overlap descent — the s9 extraction's sense ("The draft does
not prove overlap descent") — not a coindex descent, and the file's own
verdict leaves even that other object OPEN.

**D5. `selected-k77-wedge-shiab-southeast-completion-2026-08-11.md` —
REJECTED: principal family, repository-built, selection-silent.** SN3-B
already ruled it "a principal operator family ... not a zero-order `X_R`
insertion"; the file's own line confirms independently:

> "This is a real construction gain, not a theory selection."

and its `lab/sources` source-return record types the custody exactly:

> "Disposition: `SOURCE_CONFIRMS` on operator-family grammar; `SOURCE_SILENT`
> on selection."

Source-admission of a family grammar is not source-ownership of a descent.

**D6. `selected-k77-natural-trace-constraint-gate-2026-08-11.md` — REJECTED:
its zero-order route is a killed constraint, not an insertion.** Its own
status: `NATURAL_ZERO_ORDER_CONSTRAINT_ROUTE_KILLED`.

**D7. The F7/F8 arrow and contraction hits — REJECTED: typed non-candidates.**
The only two `Omega0 ... -> ... Omega0` lines in G1 are the displayed
operator's total signature, "D = [ A  B ] : Omega1(S) + Omega0(S) ->
Omega1(S) + Omega0(S)." (whose southeast corner is the displayed zero), and
the twistor complex "Omega0(V) --d_D--> Omega1(V) --M_D--> Omega1(V)
--delta_D--> Omega0(V)", whose final arrow is a first-order codifferential.
The `iota` hits are presymplectic/boundary/null-screen contractions and the
Y14 observation contraction `s^*(iota_V F_A)` — which outputs a one-form on
X, the exact MD-1 fact that kills the scalar reading.

## 5. Receipt candidates, adjudicated on lineage

Spec side for all rows: SN3-A/SN3-C as quoted in section 2.

**R1. The s9 extraction (slot half) — owns the slots, joins nothing.** Its
cell ledger prints `southeast-zero` in all four southeast positions; the
southeast fork section records `SE!=0` as "explicitly source-admitted" while
"neither source supplies a uniqueness theorem or selects the nonzero map";
and the register's SC-OP-04 note certifies the ceiling: "It certifies no
global adjoint, descent, or domain." SC-OP-05's verbatim is the whole
southeast story the source owns: "noting that other versions of the theory
exist including one with a non-trivial map in the lower right quadrant of the
operator." A fork admission with no cell, no owner, no line is not a receipt.

**R2. The K77-A atomic crosswalk (observed-line half) — owns the dictionary,
joins nothing.** Its title's own ceiling: "no physical particle is yet
recovered"; the file contains **zero** occurrences of `varpi` and zero
mentions of eq 9.16 — it is carrier-indexed (which representations the K77
spinor space contains), not slot-indexed (which printed cell lands on which
line). The receipt is precisely the join it does not perform.

**R3. Resolver Wave D — placement explicitly open.** Its own title:
"source-owned moving full-20 placement remains open"; its grade repeats
"total P0/Y placement, source selection, VEV, and mass remain open."

**R4. The seesaw-analogy hits (all six G3 files) — analogy with fences, not
incidence.** The sharpest of them,
`k77-wave2-dirac-derham-superig-rebase-2026-08-04.md`, says:

> "The southeast zero is Weinstein's stated seesaw slot. It is retained as a"

zero — a slot-shaped analogy about a cell that is kept EMPTY, with "mass
hierarchy and spectrum open". The one file in the whole corpus containing the
word "neutrino" contains it inside its own fence:

> "the seesaw source analogy is not a mass or neutrino prediction."

**R5. `selected-k77-southeast-zero-graph-gate-2026-08-10.md` and
`selected-k77-unrestricted-southeast-bv-kernel-2026-08-11.md` — negative and
independence results.** The first is typed by its own frontmatter
`status: exact_scoped_negative_result`; the second proves southeast
*independence* — "it holds for every southeast matrix at a fixed non-null"
momentum — the structural opposite of a placement.

**R6. `selected-k77-nonzero-fermion-stationary-schur-reduction-2026-08-10.md`
— seesaw-shaped algebra, source-silent.** Its Schur map `S_A: ker C ->
coker B` is a kernel quotient of the displayed candidate; its own
source-return line begins
`SOURCE_SILENT_MAXIMAL_RANK_EFFECTIVE_MAP_NONZERO_STATIONARY...`.

**R7. SC-FER-03 accommodation prose — custody, not incidence.** The register
row's own note: the s9 extraction "fences these as source assignments, not
derivations" — and SN3-C already typed the same sentence as "source custody,
not any of those incidence witnesses."

## 6. Near-collision register (the v_PSB discipline, applied)

Three unregistered near-collisions were live in this search and are named so
the next reader does not re-fight them:

1. **"zero-order"**: *zero-order port* (cross-degree cell whose INPUT is a
   zero-form; D1/D3) vs *zero-order insertion* (degree-preserving
   `Omega0 -> Omega0` map; SN3-B's object). Same token, disjoint objects.
2. **"descent"**: *global/overlap/H-descent* (s9 extraction, D4) vs
   *zero-order/coindex descent* (SN3-B's object). The s9 extraction itself
   certifies the first is absent too, but absence of the first is not
   absence-or-presence of the second.
3. **"seesaw slot"**: the source's *analogy* for the retained southeast zero
   (R4) vs an *incidence receipt* for a populated southeast cell. The analogy
   is source-confirmed; the receipt does not exist.

None of these is in `lab/process/homonym-register.yaml` (checked: no
registered token matches descent/zero/port/slot/southeast). Registering them
belongs to the register's owner, not to this write-scoped artifact.

## 7. Supersession and correction check

No candidate file adjudicated above carries a `[!CAUTION]`/`[!WARNING]`/
`SUPERSEDED` file-level banner (probe-pinned). Two internal correction
records were found and both were USED rather than skipped: the s9
extraction's superseded provisional-grading section (its repaired disposition
is the one relied on, and `SOURCE-CORRECTS-SIGNS: NONE FOUND` still holds),
and the routing method's withdrawn-clause caution (which is the MD-1/LA-8
refutation of the scalar-descent reading — evidence FOR this verdict, and the
probe asserts the banner remains visible). No candidate was credited and then
superseded; every rejection stands on the candidate's current text.

## 8. What future object flips ABSENT

The verdict is probe-pinned and flips only on new record:

1. **For the descent**: a record with source custody (SOURCE-STATES /
   SOURCE-DISPLAYS / SOURCE-SELECTS, or a new primary extraction) supplying a
   declared `Omega0 -> Omega0` insertion built from a `varpi` coefficient
   with class-two output — SN3-B's conjunction, all four legs at once. A
   repository construction of the same shape does NOT flip the verdict: the
   license says "already-owned source-native", and conditional-build
   discipline forbids manufacturing it.
2. **For the receipt**: a record naming a printed equation-(9.16) cell AND
   its coefficient owner AND the observed line(s) it lands on, as a
   placement — e.g. a source-typed southeast owner in `(bar-nu-,nu-)` or
   `(bar-nu+,nu+)`, or a source slot-to-`L,N^c` map for the cross-degree
   cells.
3. Mechanically: any drift in the pinned corpus counts (274/154/24), pattern
   hit lists, or candidate lineage quotes makes the probe fail, which is the
   signal to re-adjudicate — including the arrival of any new `selected-k77-*`
   or `k77-wave2-*` file.

## 9. Hostile review, inline

**Attack 1 — "you strained toward reopening: the Curt zero-order placement
plus the wedge-Shiab southeast family jointly cover both objects; crediting
them reopens the lane."** Rejected on lineage, not on preference: the Curt
row's own file says the cell "are not identified" and closes `SOURCE-SILENT`
on exactly the needed datum; the wedge family's own file says "not a theory
selection" and its source-return says `SOURCE_SILENT` on selection. A union
of two silences is not an ownership. Moreover the joint object would still be
first-order (principal family) on the descent leg — failing the zero-order
conjunct outright.

**Attack 2 — the reverse bias: "you strained toward the tidy pause and
over-rejected; demanding the literal token conjunction (`Omega0 -> Omega0` +
class-two + varpi + SOURCE-verb) makes ABSENT unfalsifiable by vocabulary
choice."** Two answers. First, the sweeps were run at concept level as well
as token level: F8 caught every interior-product/contraction mechanism in the
corpus regardless of naming, F7 caught every degree-preserving arrow in any
spelling of `Omega0`, and the F2/F3 observed-line families used six
alternative vocabularies; every hit was read and typed, not counted and
dismissed. Second, the planted fake receipt is written in exactly the natural
prose a genuine receipt would use, and the machinery both finds and credits
it — so the detector demonstrably fires on a real object written plainly.

**Attack 3 — "the corpus fence is gerrymandered: the resolver waves of
2026-08-03 use `coindex` and were excluded."** The resolver waves are the SN
lane's own ancestors (SN-3 cites Wave D directly), and the two that matter
were adjudicated anyway: Wave D (R3) leaves placement open in its own title,
and the K77-A crosswalk (R2) joins nothing. The remaining coindex mentions in
rb5-rb7/wave-g/h/i/j are the vocabulary's birthplace in kill/typing contexts,
not ownership records; and SN-3, which cites them, itself certifies the
descent `UNBUILT` — a claim this archaeology set out to break and could not.

**Attack 4 — "a fork-shaped gate should fear the false ABSENT as much as the
false FOUND."** Agreed, and that is why the flip conditions in section 8 are
stated as an exact conjunction with a mechanical staleness alarm, why the
probe fails on ANY corpus growth in the pinned globs, and why the two
half-owned ingredients of the receipt are named rather than flattened into
"nothing exists."

## 10. Postflight lenses (six)

1. **Source philology.** Every credit-relevant quote was re-checked against
   its owning file by the probe (23 lineage receipts, 9 spec anchors); no
   quote is paraphrased.
2. **Record linkage.** No join in this artifact rests on a token equality;
   the three near-collisions that could have produced a false FOUND are
   named in section 6 with both senses quoted.
3. **Typing.** The four-grading separation (operator order / form degree /
   Clifford grade / Z/4 cell class) was applied to every candidate; no
   candidate was rejected on a grading it did not claim.
4. **Supersession hygiene.** Zero credited-then-superseded candidates; both
   internal correction records were consumed as evidence (section 7).
5. **Adversarial symmetry.** Both bias directions were attacked (section 9);
   the strongest pro-reopen composite (Curt + wedge-Shiab) fails on its own
   files' words, not on mine.
6. **Reproducibility.** The verdict re-derives mechanically: patterns, paths,
   counts, quotes, planted controls, and selftest are all in the probe; a
   reader can flip any pin and watch the certificate refuse.

## 11. Claim ceiling

Established: the two SN-3 reopen antecedents are absent from the licensed
source-native K77 record as of this adjudication, with the search corpus,
patterns, and rejection lineages pinned. Nothing else. In particular this
artifact does NOT establish: that the objects cannot exist; that any future
source release lacks them; any statement about conventional 126 mechanisms
(controls remain controls under the routing method); any neutral rank,
closure, scale, reality, action, vacuum, domain, mass, mixing, family count,
or phenomenology. It moves no canon verdict and edits no SN-3 artifact.

## 12. Reproduction

```bash
_local/cas-venv/bin/python tests/channel-swings/joe_directed_sn4_descent_receipt_archaeology.py
_local/cas-venv/bin/python tests/channel-swings/joe_directed_sn4_descent_receipt_archaeology.py --selftest
```

Certificate: 61/61 checks pass, 0 fail, 0 crash, exit 0. Selftest: clean
baseline verified FIRST (61/61), then 7/7 machinery-corruption mutations
(corrupted observed-line pattern, corrupted corpus root, corrupted spec
anchor, force-credit classifier, force-nocredit classifier, corrupted lineage
quote, disabled whitespace normalization) each caught by 1-9 genuine [FAIL]
checks with zero crashes; a crash is rejected as a catch by construction.
Planted controls: the fake receipt is found by three pattern families and
credited by both predicates; the wrong-lineage candidate token-matches and is
rejected by both predicates.
