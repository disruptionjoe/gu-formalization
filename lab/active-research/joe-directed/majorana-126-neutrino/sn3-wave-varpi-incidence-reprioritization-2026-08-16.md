---
artifact_type: exploration
status: exploration
doc_type: source-native-neutrino-varpi-incidence-wave-integration-and-reprioritization
created: 2026-08-16
work_item: SN3-WAVE
channel: majorana_126_neutrino_mechanism
title: "SN-3 wave: grade five contains the strongest conditional Dirac and Majorana parent packets, but native one-form composition, absent southeast placement, and shared-owner scaling fence the neutrino lane"
grade: "INTEGRATION SYNTHESIS over three exact conditional source-native certificates and one independent thirteen-lens hostile review. The internal exterior-weight, K77 parent, fixed-Hq, equation-9.16 cell, neutral-line cardinality, reverse-custody, and shared-owner valuation statements are exact. Zero-order/coindex descent, source-slot-to-observed-line incidence, nonzero restricted maps, southeast placement, neutral closure, action, vacuum, scale owner, coefficient, quotient, domain, and physical interpretation remain absent or declared."
disposition: GRADE5_STRONGEST_XD_XR_INTERNAL_PARENT_PACKET_BANKED__NATIVE_CONNECTION_FORM_LEG_FORCES_2_PLUS_2_TO_NET0__XD_ONLY_FORMAL_PP_MM_CLASS_COMPATIBILITY__XR_SE_MATCH_ZERO_ORDER_DESCENT_CONDITIONAL_AND_SOURCE_PLACEMENT_ABSENT__GENERIC_SHARED_OWNER_SCALING_CANNOT_POPULATE_0_1_1__NEUTRINO_LANE_PAUSED_PENDING_ALREADY_OWNED_DESCENT_AND_INCIDENCE_RECEIPT
canon_verdict_change: none
steering_effect: "Bank the finite parent census, exact source cell ledger, and leading-support obstruction. Permit only a read-only archaeology check for an already-owned source-native zero-order/coindex descent together with an equation-9.16 slot-to-observed-line receipt. If absent, pause this lane; do not construct the missing descent, coefficient, southeast owner, action, vacuum, external datum, family row, scale relation, quotient, domain, or conventional 126 mechanism."
canonical_effect: pending_integration
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-source-native-neutrino-conditional-build-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn2-wave-neutral-pencil-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-existing-varpi-xd-incidence-census-2026-08-16.md
  - tests/channel-swings/joe_directed_sn3_existing_varpi_xd_incidence_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-existing-varpi-xr-incidence-census-2026-08-16.md
  - tests/channel-swings/joe_directed_sn3_existing_varpi_xr_incidence_probe.py
  - lab/active-research/joe-directed/majorana-126-neutrino/sn3-source-slot-neutral-line-incidence-classifier-2026-08-16.md
  - tests/channel-swings/joe_directed_sn3_source_slot_neutral_line_incidence_probe.py
  - lab/process/hostile-reviews/2026-08-16-joe-directed-sn3-varpi-incidence-review.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact uses D5
> weights and `126/126bar` only as internal representation arithmetic after a
> typed bridge to the pure-internal K77 connection parent. Standard Higgs/VEV,
> ordinary family-index or net-chirality, and scalar-`126` mechanisms bind only
> their named comparators. They do not adjudicate Weinstein's source-native
> mechanism. Read `lab/methods/source-native-comparator-routing.md` before
> reusing any comparator result. Classification: `SOURCE_NATIVE_ROUTE`.

# SN-3 wave — existing-`varpi` incidence integration

## Outcome

SN-3 found real source-native representation support, and it also found why
that support is not yet an equation-(9.16) neutrino mechanism.

1. Grade five is the strongest selected internal parent packet for both
   `X_D` and `X_R` type requirements.
2. The connection's one-form leg is load-bearing. Internal Clifford class two
   does not directly select positional net-class-two cells; on the native
   one-form reading, `2+2=0 mod 4`.
3. The displayed equation 9.16 has no southeast owner, and no zero-order
   descent turns the grade-five `X_R` parent line into one.
4. The same four unbarred owners populate both `A` and `B`, so generic owner
   scaling gives `v(A)=v(B)`. The strict SN-2 `(0,1,1)` branch needs special
   leading-support separation plus the already-declared closure and
   nondegeneracy horns.

This finishes the bounded existing-parent census. It does not justify
constructing the missing incidence data.

## 1. Exact internal parent census

In one harmonized CB-A Cartan convention,

```text
B-L = (2/3)(w1+w2+w3),
T3L = (w4+w5)/2,
T3R = (w4-w5)/2,
Y   = T3R + (B-L)/2.
```

The selected internal grades give the Dirac-capable doublet counts

| grade | `Y=+1/2` | `Y=-1/2` | selected support | result |
|---:|---:|---:|---|---|
| `1` | `1` | `1` | yes | `X_D` parent type |
| `2` | `0` | `0` | yes | excluded by representation |
| `3` | `2` | `2` | no | full-unitary completion control |
| `5` | `2` | `2` | yes | strongest `X_D` parent packet |

For the right-Majorana packet:

- grade one has no `B-L=-2` line;
- grade three has a `B-L=-2` line but `Y=-1` and the wrong one-line
  Grassmann symmetry; and
- grade five has the unique internal line
  `(-1,-1,-1,+1,-1)`, with `(B-L,T3L,T3R,Y)=(-2,0,+1,0)`, plus its conjugate.

The real grade-five `252` retains both complex Hodge halves. That internal
Hodge pair is not the pair of ambient K77 Weyl halves; both ambient halves are
also retained. Bars remain independent source fields.

The fixed-`H_q` intersections are exactly

```text
grade 1:   1   (the radial q line),
grade 2:  36   (q-avoiding monomials),
grade 5: 126   (q-containing monomials).
```

These are fixed-`q` real subspace dimensions, not weak-doublet counts and not
a scalar `126` field.

## 2. The form leg repairs the cell inference

SN3-C's positional ledger is exact in every `2 x 2` quadrant:

```text
[[0,2],
 [2,0]].
```

Thus `pp/mm` are positional net class zero and `pm/mp` are positional net
class two. But an internal odd Clifford coefficient is only one factor in a
native connection component. Its one-form leg also has class two:

```text
internal class 2 + connection one-form class 2 = net class 0.
```

Therefore selected grade-one/five `X_D` content is formally class-compatible
with the cross-degree `pp/mm` positions on the native one-form reading. That
is still not an observed coefficient: pullback returns a four-dimensional
one-form, and no covariant coindex contraction or source-slot-to-`L,N^c` map
has been supplied.

The exact source reverse-custody ledger remains:

```text
pp <-> bar-pp,
mm <-> bar-mm,
pm <-> bar-mp,
mp <-> bar-pm.
```

The crossed `pm/mp` statement is a cell-custody fact, not a grade-one/five
`X_D` incidence fact.

## 3. Majorana parent content stops before the southeast block

The displayed source branch has four southeast zeros. The source admits an
unspecified nonzero rival but gives no cell, Clifford grade, coefficient, or
selection. A hypothetical **zero-order** class-two grade-five packet would be
compatible with the two class-two southeast positions

```text
(bar-nu-,nu-),
(bar-nu+,nu+).
```

That statement is conditional on a separately supplied
`SN3-ZERO-ORDER-COINDEX-DESCENT`. A pure-internal endomorphism may be scalar in
its endomorphism indices while its `varpi` coefficient still carries a
one-form coindex. No such descent and no southeast placement exists here.

Hence the strongest exact statement is:

```text
GRADE5_XR_INTERNAL_PARENT_PACKET_PRESENT;
ZERO_ORDER_DESCENT_MISSING;
SOURCE_SE_INCIDENCE_ABSENT.
```

## 4. Neutral incidence and valuation obstruction

`SN2-NEUTRAL=Q0` retains formal `L^0,N^c` endpoint lines and therefore two
formal `L <-> N^c` orientations per printed cross-degree cell. `SM1` retains
only `N^c` and has no `X_D` orientation. These are endpoint cardinalities,
not ranks or nonzero matrix elements.

The four unbarred owners `pp,pm,mp,mm` each occur once in northwest `A` and
once in northeast `B`. With one declared valuation `w_j` per owner,

```text
v(A)=min{w_j : a_j != 0},
v(B)=min{w_j : b_j != 0}.
```

Generic common leading support forces `v(A)=v(B)`. The strict
`(v(A),v(B),v(C))=(0,1,1)` branch requires:

1. valuation-zero support survives in `A`;
2. every such owner is annihilated in the leading neutral restriction of
   `B`;
3. valuation-one support survives in `B`;
4. independent barred valuation-one support survives in `C`; and
5. neutral closure, invertibility of `A_0`, and nondegeneracy of
   `C_0 A_0^-1 B_0` hold separately.

This is a necessary support theorem, not a selected texture or hierarchy.

## 5. Hostile review and applied repairs

The independent thirteen-lens review returned
`PASS_AFTER_THREE_MATERIAL_TYPING_REPAIRS_AND_TWO_PROBE_REPAIRS`.
Integration applies them:

1. internal Clifford class, connection form-leg class, and positional net
   cell class are now separate; the old `pm/mp` X_D-port assignment is killed;
2. the `X_R` southeast class match is conditional on an unbuilt zero-order
   coindex descent; and
3. every named weight uses the same CB-A `T3L/T3R` convention.

The repaired probes add explicit class-composition and crossed-Cartan adverse
controls. SN3-C required no arithmetic repair.

## 6. Reprioritization

### Within the neutrino lane

| item | status | next rule |
|---|---|---|
| grade-one/five `X_D` and grade-five `X_R` parent census | banked | do not repeat |
| exact equation-(9.16) owner/cell/reverse ledger | banked | use SN3-C as custody source |
| strict `(0,1,1)` valuation horn | dependency-fenced | reopen only with leading-support separation and closure receipts |
| zero-order/coindex descent plus slot-to-observed-line incidence | missing | archaeology-only search for an already-owned receipt |
| action, vacuum, scale selector, new coefficient, southeast owner | off limits | do not construct in this channel |
| physical mass, pole, mixing or phenomenology | closed here | requires later quotient/domain/background owners |

### Relative to the current exploration path

The neutrino lane should now pause. Both the Dirac-looking and
Majorana-looking routes terminate at the same missing source-native descent
and placement datum. The H210 derivative branch is already closed and the
extra-vector lane remains retired. The next exploration wave should reopen
the broader portfolio and prefer a branch whose next discriminator composes
already-owned maps without requiring action selection or an external datum.

SN-3 reopens only if archaeology finds an already-owned source-native
zero-order/coindex descent and a matching equation-(9.16)
slot-to-observed-line receipt.

## Claim ceiling

SN-3 constructs no source-selected connection component, observed-line map,
coindex contraction, Lorentz-scalar zero-order coefficient, nonzero neutral
rank, closed neutral subpencil, southeast completion, action, vacuum, external
datum, family row, scale owner, reality owner, quotient, domain, physical
chirality, hierarchy, mass, pole, mixing, family count, threshold, observable,
or phenomenology. Conventional scalar-`126` results remain controls and do
not adjudicate this source-native route.
