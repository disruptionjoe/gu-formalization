---
title: "TW-3A: normal-Spin versus four-dimensional detour tensor intertwining"
status: exploration
doc_type: conditional_build_exact_tensor_intertwining_result
artifact_type: exact_cyclotomic_normal_spin_and_rational_detour_compatibility_certificate
created: 2026-08-16
updated_at: 2026-08-16
work_item: TW-3A
channel: joe_directed_superposition_twistor
target_claim: HYP-TW-COHERENCE-01
grade: "EXACT fibrewise tensor-product theorem over Q(zeta_8) and Q, on the already-owned TW-1 normal half-spin spectra and TW-2 Cl(2,2) principal symbols. The TW-2 algebraic-dual target is used only in its declared primalized matrix-copy convention. No internal pairing, genuine dual-target adapter, action, background, global normal-J field, Tw2 field, analytic domain, quotient, physical state, family selector, or superposition theorem."
disposition: NORMAL_SPIN_AND_4D_DETOUR_COMMUTE_ONLY_BY_FORMAL_TENSOR_FACTOR_SEPARATION__CENTRAL_SIGN_AND_ORIENTATION_REVERSAL_ADD_NO_OBSTRUCTION__RANK_AND_NULL_COHOMOLOGY_FACTOR_BY_INTERNAL_EIGENSPACE__GENUINE_DUAL_TARGET_REQUIRES_CROSS_HALF_PAIRING_OWNER_DEFERRED_TO_TW3B__TW3A_BANK_AND_DEPRIORITIZE
source_return: SOURCE_SILENT
probe: tests/channel-swings/joe_directed_tw3a_normal_spin_detour_intertwining_probe.py
canon_verdict_change: none
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/conditional-build-channel-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw1-normal-twistor-spin-lift-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw2-four-dimensional-detour-symbol-factorization-2026-08-16.md
  - lab/process/hostile-reviews/2026-08-16-joe-directed-twistor-conditional-composition-review.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# TW-3A — normal-Spin/detour intertwining

## Result first

The bounded TW-3A coherence test closes, and its direct result is not fertile.
On the only target convention already licensed by TW-2—the algebraic-dual
twistor fibre represented by a **primalized matrix copy**—the TW-1 normal Spin
lift and every TW-2 four-dimensional block commute for the elementary reason
that they act on separate tensor factors.

For each normal half `N_kappa`, each four-dimensional Weyl source, and

```text
A(xi) in {T(xi), Q(xi), N_3(xi), T#(xi)},
```

set

```text
A_N(xi) = A(xi) tensor I_N,
G_source = I_source tensor S_J,
G_target = I_target tensor S_J.
```

Then exactly

```text
A_N(xi) G_source = G_target A_N(xi).
```

The same equation holds for the central-sign mate `-S_J` and for the
opposite-orientation lift `S_J^-1`. It holds at positive, negative, and two
independent nonzero-null covectors, on both four-dimensional Weyl halves, both
normal half-spin factors, both ambient K77 halves, and all four observation
corners `++`, `--`, `+-`, `-+`.

Nothing in this square couples the two geometries. It neither constrains the
normal lift nor refines the detour symbol. TW-3A therefore establishes exactly
the hostile review's low-fertility outcome:

```text
FORMAL_TENSOR_FACTOR_COMMUTATION_ONLY.
```

There is one sharply typed seam, but it is not solved here. A genuine target

```text
Tw* tensor N_kappa*
```

carries the contragredient internal action `S_J^-T`, not the same matrix-copy
action `S_J`. An internal primalizer or pairing `b:N_kappa -> N_lambda*` must
satisfy

```text
b S_J = S_J^-T b.
```

The identity map fails this equation on each fixed normal half. The exact
spectra show that reciprocal phases occur across the opposite normal half,
so a cross-half pairing is spectrally possible; the spectra do not construct,
normalize, or own that pairing. That is the precise TW-3B question.

## Conditional horn and stop boundary

The horn is the conjunction of the already-declared TW-1 and TW-2 horns:

```text
TW3A-HORN:
  assume one supplied normal orthogonal complex structure on a declared
  orientation component, take its owned fibrewise Spin lift, and tensor the
  owned four-dimensional detour/base-compressed symbols with the corresponding
  normal half-spin factors.
```

No source statement selects this horn. The source is silent on this finite
intertwining. The result stops at pointwise tensor algebra. It does not build
or select an action, background, external datum, global `J_N` section,
connection, `Tw^2` field, analytic adjoint or domain, quotient, positive
pairing, physical state, coefficient, family row, scale, or observable.

The source-custody fence remains load-bearing:

- Weinstein's total carrier remains fundamentally non-chiral.
- Observed chirality remains proposed as emergent/effective and is not built
  by this commuting square.
- Both K77 ambient halves and all four observation corners remain present.
- The source imposter `F`, abstract family-multiplicity space `M_3`, and the
  separately predicted partner `144` remain distinct and absent from the
  construction.
- No ordinary family index, net-chirality substitute, standard Higgs/VEV, or
  legacy `Cl(9,5)` object enters.

## Ten-lens preflight and assessment

| lens | disposition |
|---|---|
| source fidelity | `SOURCE_SILENT`; this is conditional K77/twistor kinematics, not a quotation or inferred source mechanism. |
| comparator routing | The standard 4D detour binds its named geometry; only the explicitly typed tensoring is a conditional GU bridge. |
| `2+1` / imposter / partner | No eigenspace is a family, `F`, `M_3`, or `144`; no row or alignment is introduced. |
| emergent chirality | Normal Spin preserves its half; both base chiral sequences and all four corners remain. No physical half is selected. |
| Layer-0 objects | `S_J`, `-S_J`, `S_J^-1`, `T`, `Q`, `N_3`, `T#`, matrix-copy dual and genuine algebraic dual remain distinct. |
| current real form | Internal data are the exact current-K77 normal `(6,4)` half-spin spectra over `Q(zeta_8)`; base identities use exact rational `Cl(2,2)`. |
| component and sign | Central sign changes eigenphases but not the square; orientation reversal inverts phases and changes the fixed-volume square sign. Neither changes formal commutation. |
| dual typing | The same-factor square is licensed only on TW-2's declared matrix-copy target. A genuine dual target needs `b S=S^-T b`; this owner is deferred. |
| symbol/rank/cohomology | Positive and negative non-null strata remain exact; nonzero-null middle symbol cohomology tensors by each `10/6` internal eigenspace. |
| falsification/novelty | Wrong-factor action, corner deletion, frozen orientation and family promotion are rejected. The only new information is closure plus the exact cross-half dual-pairing seam. |

## Exact construction

### Internal lift

Let `zeta_8=exp(i pi/4)`. TW-1 gives the selected-component spectra

| normal half | selected `S_J` spectrum | opposite `S_J^-1` spectrum |
|---|---|---|
| `N_+` | `zeta_8^7 x 10`, `zeta_8^3 x 6` | `zeta_8^1 x 10`, `zeta_8^5 x 6` |
| `N_-` | `zeta_8^1 x 10`, `zeta_8^5 x 6` | `zeta_8^7 x 10`, `zeta_8^3 x 6` |

Over the exact cyclotomic field the probe rechecks

```text
selected:   S_J^2=-J10,  S_J^4=-1,  S_J^8=1;
central:   (-S_J)^2=S_J^2;
opposite:  (S_J^-1)^2=+J10.
```

Thus orientation reversal exchanges the two displayed spectral patterns, and
the selected `N_+` and `N_-` spectra are reciprocal multisets.

### Base detour

The probe independently rebuilds TW-2's rational `Cl(2,2)` matrices and
checks, for each covector,

```text
Q T=(1/2)T c,
T# T=(3/4)q I,
N_3=Q^3-(1/4)qQ,
N_3 T=0,
T# N_3=0.
```

The four-dimensional restrictions have dimensions

```text
2 --T--> 6 --N_3--> 6 --T#--> 2
```

on each Weyl orientation. `Q` is also restricted between the opposite Weyl
twistor factors and included in every commutation check. This is still a
principal-symbol theorem; `T#` is the algebraic metric-dual symbol, not an
analytic adjoint.

### Tensor-factor theorem

For a rectangular base map `A:V->W`, the probe constructs the actual exact
Kronecker matrices and verifies

```text
(A tensor I_N)(I_V tensor S)
  = A tensor S
  = (I_W tensor S)(A tensor I_N)
```

for `S=S_J,-S_J,S_J^-1`. This exhausts the content of every passing TW-3A
intertwining square. The computation does not hide a component-dependent
coefficient or a mixed base-normal operator.

## Rank and cohomology by internal eigenspace

Let `E_lambda` be an `S_J` eigenspace of multiplicity `m`, where `m=10` or
`m=6`. The tensor-restricted detour sequence has dimensions

```text
2m -> 6m -> 6m -> 2m.
```

Its exact fingerprints are:

| covector stratum | `m` | ranks `(T,N_3,T#)` | middle symbol cohomology `(H_1,H_2)` |
|---|---:|---:|---:|
| `q != 0` | `10` | `(20,40,20)` | `(0,0)` |
| `q != 0` | `6` | `(12,24,12)` | `(0,0)` |
| `q=0`, `xi != 0` | `10` | `(20,10,20)` | `(30,30)` |
| `q=0`, `xi != 0` | `6` | `(12,6,12)` | `(18,18)` |

Summing the two eigenspaces on any one corner recovers TW-2's internal-rank-16
receipt:

| covector stratum | ranks `(T,N_3,T#)` | middle symbol cohomology |
|---|---:|---:|
| `q != 0` | `(32,64,32)` | `(0,0)` |
| `q=0`, `xi != 0` | `(32,16,32)` | `(48,48)` |

These are characteristic-symbol dimensions. They are not global solutions,
particle multiplicities, family counts, positive states, BV cohomology, or
physical superposition sectors.

## Four-corner and half custody

The probe carries the complete source inventory

```text
++ : S4+ tensor N+  in ambient S14+;
-- : S4- tensor N-  in ambient S14+;
+- : S4+ tensor N-  in ambient S14-;
-+ : S4- tensor N+  in ambient S14-.
```

Every covector and every lift variant is checked on each row. The base detour
changes four-dimensional Weyl orientation; TW-3A does not reinterpret that
arrow as an ambient physical chirality transition. In particular, the
four-corner inventory is the parent typing receipt, not four independent
observed complexes or a luminous/dark selector.

## The genuine-dual fence handed to TW-3B

TW-2's matrices use an algebraic fibre pairing to display the dual twistor
target as a copy of the primal twistor fibre. Tensoring that display with
`I_N` is enough for the formal result above. It is not enough to promote the
target to `Tw* tensor N*`.

On a genuine internal dual the action is `S^-T`. In the eigenbasis used by
the exact certificate,

```text
I_N S != S^-T I_N
```

on both normal halves and both orientation components. Reciprocal spectral
multiplicities match cross-half:

```text
Spec(S|N_+)^-1 = Spec(S|N_-).
```

This proves only a necessary spectral compatibility for a cross-half
primalizer. It does not prove that the current K77 bilinear restricts with the
needed chirality, symmetry, sign, nondegeneracy, normalization, or four-corner
incidence. Those are detailed pairing questions and belong to TW-3B.

## Hostile mutants

The self-test fires four required adverse plants:

1. a noncommuting operator is placed on the four-dimensional spin factor and
   falsely called the internal Spin action;
2. one of the four K77 corners is deleted;
3. the opposite orientation is frozen to the selected-component square sign;
4. a cyclotomic lift eigenspace is promoted to a named physical family.

All four are rejected. The first is an exact failed matrix square; the second
and third fail exact inventory/square certificates; the fourth violates the
declared type and claim ceiling.

## Fertility and disposition

TW-3A fertility is `2/10`: the requested intertwining closes exactly but is
only tensor-factor functoriality. Repeating it with more covectors, phases,
corners, or rank strata cannot reveal a new coupling.

The dual-target seam raises one bounded successor question rather than saving
TW-3A itself: TW-3B may test whether an already-owned current-K77 algebraic
spin pairing supplies the required cross-half map and four-corner incidence.
It must not invent a pairing, action, background, global field, domain, or
physical quotient. If that pairing is automatic Spin invariance or fails its
typed incidence, bank the result and deprioritize the twistor path.

## Reproduction and claim ceiling

```text
DOT_SAGE=/tmp/gu-tw3a-sage sage -python \
  tests/channel-swings/joe_directed_tw3a_normal_spin_detour_intertwining_probe.py

DOT_SAGE=/tmp/gu-tw3a-sage sage -python \
  tests/channel-swings/joe_directed_tw3a_normal_spin_detour_intertwining_probe.py --selftest
```

The strongest licensed statement is:

> On the declared TW-1/TW-2 conditional horn and TW-2's primalized
> matrix-copy target convention, the normal Spin lift, its central-sign mate,
> and its opposite-orientation inverse intertwine `T,Q,N_3,T#` exactly on
> both Weyl halves and all four current-K77 corners because the two actions
> occupy separate tensor factors. Rank and null symbol cohomology factor by
> the `10/6` internal eigenspaces. A genuine algebraic-dual target requires an
> additional cross-half pairing owner and is not established here.

It is not licensed to say that the source selected the normal twistor, that
TW-3A constructs an interacting base-normal complex, that an eigenspace is a
family or physical sector, that the characteristic classes are physical
cohomology, or that GU has derived quantum superposition.
