---
title: "TW3-C: rolled base compression, normal-twistor stabilizer descent, and the terminal finite gluing theorem"
status: exploration
doc_type: conditional_build_exact_associated_bundle_composition_result
artifact_type: exact_current_k77_rolled_compression_and_component_tagged_stabilizer_descent
created: 2026-08-16
updated_at: 2026-08-17
work_item: TW3-C
channel: joe_directed_superposition_twistor_conditional_build
target_claim: HYP-TW-COHERENCE-01
grade: "EXACT pointwise rational Cl(2,2) base-symbol plus exact blade-algebra Cl(6,4) normal-lift theorem. Conditional on a component-tagged lifted U(3,2) reduction. No action, background, total current, global reduction/section, Tw2 field, analytic domain, quotient, positive pairing or physical state."
disposition: BASE_SUPPORTED_PI4_AND_R4_INTERTWINE_ALL_SELECTED_OPPOSITE_AND_CENTRAL_NORMAL_LIFTS__LIFTED_U3_2_DESCENT_CANONICAL_GIVEN_REDUCTION_NOT_PROJECTIVE__GLOBAL_INSTANTIATION_DATA_MISSING__FORMAL_TENSOR_PRODUCT_CLOSURE_BANKED__TWISTOR_PATH_DEPRIORITIZED
source_return: SOURCE_SILENT
probe: tests/channel-swings/joe_directed_tw3c_rolled_compression_stabilizer_descent_probe.py
canon_verdict_change: none
ledger_row_changes: none
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/conditional-build-channel-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/high-energy-two-plus-one/he4-path-reprioritization-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw1-normal-twistor-spin-lift-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw2-four-dimensional-detour-symbol-factorization-2026-08-16.md
  - lab/active-research/joe-directed/superposition-twistor/tw3b-dual-pairing-orientation-corner-gluing-2026-08-16.md
  - lab/process/hostile-reviews/2026-08-16-joe-directed-twistor-conditional-composition-review.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# TW3-C — rolled compression and stabilizer descent

## Result first

The last bounded finite gluing question closes positively, but formally.

Let TW-2's base-supported compressed rolled symbol be

```text
R_4(xi) = [ Q(xi)     T(xi)  ]
          [ -T#(xi)       0  ]
```

on `Tw_4 direct-sum S_4`, where `Tw_4=im(Pi_4)` and `T#` is the algebraic
metric-dual symbol, not a formal analytic adjoint. Tensor with either normal
complex half-spin factor. TW3-B fixes the target convention: the displayed
same-carrier matrix is the **Krein/conjugate Riesz-identified copy**, not the
raw strict complex-bilinear `B14` dual. On this base-supported carrier,
TW-1's simultaneous rolled lift restricts to

```text
S_hat_J |_(base compression) = I_(Tw_4 direct-sum S_4) tensor S_J.
```

Therefore, exactly,

```text
(I tensor S_J)(R_4(xi) tensor I)
  = (R_4(xi) tensor I)(I tensor S_J).
```

This Krein/conjugate matrix-copy equality holds for:

- the selected orientation component and its opposite component;
- both central-sign Spin preimages on either component;
- non-null, null and generic base covectors;
- both normal half-spin labels;
- both ambient K77 halves and all four `2 x 16` observation corners under the
  TW3-B Krein map `++ <-> -+`, `+- <-> --`; and
- every base-chirality input/output block of the rolled symbol.

The same restricted lift preserves the embedded `Pi_4` target and
intertwines four-dimensional gamma trace. This does **not** say `Pi_4=Pi_14`:

```text
Pi_4^2=Pi_4,
Pi_14,base^2 != Pi_14,base,
Pi_4 != Pi_14,base,
Pi_14,base Pi_4 = Pi_4 Pi_14,base = Pi_4.
```

The complete normal stabilizer calculation also closes the descent question.
The vector and Spin commutator maps have the same coefficient kernel:

```text
rank = 20,
kernel dimension = 25 = dim_R u(3,2).
```

Hence, relative to a **supplied** component-tagged lifted `U(3,2)` reduction
and TW1's selected exponential-path representative, `S_J` and its rolled
restriction define associated-bundle endomorphisms canonically. They are not
merely projective: changing a transition-function lift by the central Spin
sign does not change the equivariance condition. Choosing `-S_J` as the
endomorphism is different—it gives the distinct negative endomorphism, not an
identical representative. The repository has not
constructed the global `J_N` reduction or section needed to instantiate that
endomorphism globally. Associated descent is conditional naturality, not
global existence.

The scientific consequence is closure rather than a new open frontier. No
component, central-sign, gamma-trace or stabilizer obstruction appears. The
composition reduces exactly to tensor-product commutation. Bank TW3-C and
deprioritize this twistor path until an independent owner supplies a global
reduction or a genuinely non-product normal/mixed operator.

## Conditional horn and claim ceiling

The horn is:

```text
TW3C-HORN:
  assume the TW-1 component-tagged normal orthogonal complex reduction and
  use only TW-2's already-owned base-supported Pi4/R4 compression;
  test finite fibrewise and overlap compatibility.
```

This artifact does not construct, choose or import:

- a source action, selected background or total twisted Yang--Mills current;
- a global `J_N` field, principal reduction, section or transition cocycle;
- a `Tw^2` field or first-order auxiliary owner;
- a curved/lower-order GU detour operator;
- a formal adjoint, analytic domain, quotient, positive pairing or state;
- an imposter/partner alignment, family row, scale or observed particle; or
- a standard index, Higgs/VEV, `126`, or legacy `Cl(9,5)` substitute.

## Specialist-lens assessment

| lens | result |
|---|---|
| source fidelity | `SOURCE-SILENT`. The source owns neither this normal Spin lift nor the detour/compression composition. |
| `2+1` / imposter / partner | `F`, `M_3`, and the internal `144` remain separate and untouched. No family inference occurs. |
| emergent chirality / corners | The lift preserves normal and ambient chirality; both ambient halves and all four corners remain. No net index or observed selector is produced. |
| Layer 0 | `Pi_4`, `Pi_14`, `R_4`, cubic `N_3`, `J_N`, `S_J`, associated descent and global sections remain distinct. |
| Clifford real form | Base identities use exact rational `Cl(2,2)` and the normal lift uses exact current `(6,4)` blade algebra inside K77 typing. No K95 transfer. |
| compression / gamma trace | On base one-form legs `g_J=I`; thus the rolled lift preserves `Pi_4` and gamma trace. The ambient projector remains unequal. |
| operator grammar | `R_4` is the first-order Krein/conjugate Riesz matrix copy and tensors with identity normally. The raw strict dual is contragredient. This theorem never equates `R_4` to cubic `N_3`. |
| dual-target custody | Krein/conjugate dual flips base only: `++<->-+`, `+-<->--`. Strict `B14` dual flips normal only: `++<->+-`, `--<->-+`. Both retain all four. |
| Spin cover / component | Central-sign lifts give the same descent; selected and opposite orientation components give distinct inverse endomorphisms. |
| stabilizer / associated bundles | The exact common kernel is lifted `u(3,2)`. Equivariance gives a canonical endomorphism relative to the supplied reduction, component and TW1 exponential representative. |
| topology / globality | Descent does not construct the reduction, section, cocycle or global bundle datum required to instantiate the local formula. |
| falsification | Wrong target transport, frozen moving frames, projector equality, projective-only descent, half deletion and physical promotion are explicit failing mutants. |

## 1. Base-supported compression

The ambient rolled lift from TW-1 is

```text
S_hat_J = (g_J tensor S_J) direct-sum S_J,
g_J = I_BASE direct-sum J_N.
```

TW-2's compressed one-form has only base legs. Consequently `g_J` is the
identity on its form factor. Both the twistor one-form summand and zero-form
spinor summand therefore carry the same normal operator `S_J`. This proves
the compressed intertwiner without identifying the full ambient carrier with
the base carrier.

The exact probe reconstructs `Pi_4`, `Pi_14,base`, `Q`, `T`, `T#`, and `R_4`
over `Fraction` for non-null, null and generic covectors. It consumes TW3-B's
duality result and labels every same-carrier commutator as the
Krein/conjugate Riesz-identified copy. The raw strict `B14` dual instead
transforms contragrediently and is not represented by that unqualified primal
matrix. The factorized equality is checked separately on the base matrix and
normal Clifford legs, so no floating tensor-product comparison is involved.

## 2. Gamma trace and projector preservation

On the compressed one-form carrier,

```text
Gamma_4 tensor I_10-spin
```

acts only on the base factor, while the normal lift acts only on the internal
factor. They commute. Hence `ker Gamma_4` and `Pi_4` are invariant under all
four component/central representatives tested:

```text
selected:  +S_J, -S_J;
opposite:  +S_J^-1, -S_J^-1.
```

This is a statement about the already base-supported embedded target. The
ambient `Pi_14` does not preserve arbitrary base-supported vector-spinors;
only the `Pi_4` target embeds in its kernel. The positive intertwiner cannot be
rephrased as projector equality.

## 3. Both halves, raw duality, and four corners

The normal lift commutes with `J10` and retains the TW-1 spectra

```text
S10+: zeta8^7 x10 plus zeta8^3 x6,
S10-: zeta8^1 x10 plus zeta8^5 x6.
```

The base rolled symbol is decomposed by both input and output Weyl projectors.
Every resulting Krein/conjugate matrix block, zero or nonzero, is an internal
identity tensor and therefore intertwines either normal half. In the
observation branching,

```text
S14+ = (++ ) + (--),
S14- = (+-) + (-+),
```

the target convention determines a nontrivial corner permutation:

```text
Krein/conjugate Riesz copy: ++ <-> -+,  +- <-> --  (base flips);
strict B14 raw dual:        ++ <-> +-,  -- <-> -+  (normal flips).
```

Both maps exchange ambient halves and retain all four corners. Neither selects
or removes a corner. The theorem is compatible with the source's non-chiral
total carrier; it does not construct the later effective-chirality mechanism.

## 4. Stabilizer and component descent

For all `45` normal bivector generators, the probe independently compares

```text
A -> [A,J_N]
and
A_tilde -> [A_tilde,S_J].
```

Both maps have rank `20`, and stacking them still has rank `20`; hence their
coefficient kernels are identical and dimension `25`. A complete lifted
stabilizer Lie algebra therefore commutes with both lifts; connectedness of
`U(3,2)` promotes this to its lifted identity component. The probe also uses
an explicit finite adapted-plane Spin overlap. Replacing that representative
by its central negative changes neither commutation nor the associated-bundle
quotient relation.

The classification is therefore:

| question | exact answer |
|---|---|
| canonical associated endomorphism relative to a supplied component-tagged lifted `U(3,2)` reduction and TW1 exponential representative? | **YES** |
| only projective because of the Spin double-cover sign? | **NO** |
| is the alternative endomorphism `-S_J` identical? | **NO; it is the distinct negative endomorphism** |
| selected and opposite components define the same endomorphism? | **NO; they give inverse, orientation-tagged endomorphisms** |
| does this theorem construct the global reduction or section? | **NO; global-data-dependent and absent** |

A non-stabilizer generator moves `J_N` and fails to commute with both lifts.
Thus freezing the local lift while moving the normal frame is an exact adverse
control; covariance requires co-moving conjugation as TW-1 already states.

## 5. What does and does not compose

The exact composition now owned is

```text
component-tagged normal reduction
  -> equivariant S_J on normal spinors
  -> restricted rolled lift on (Tw_4 + S_4) tensor S_10
  -> exact intertwining of Pi_4, Gamma_4 and R_4.
```

The following tempting compositions remain false or unowned:

```text
Pi_4 = Pi_14                         false;
R_4 = N_3                            false by carrier and order;
associated descent = global section  false;
symbol invariance = physical state   false;
base product intertwining = total curved/mixed GU complex  unowned.
```

In particular, the strict complex-bilinear raw dual carries the
contragredient action and the distinct normal-flipping corner map. Only after
TW3-B's conjugate/Krein Riesz identification does the target become the
same-carrier primal matrix copy used in the `R_4` commutators. Neither reading
constructs a formal adjoint or resolves the unowned `Tw^2` auxiliary.

## 6. Mutants and falsifiers

The exact probe rejects:

1. transforming only the target rather than both sides of the `R_4` square;
2. using the selected lift on one side and the opposite lift on the other;
3. using the primal action on the raw strict `B14` dual;
4. collapsing the strict-`B14` and Krein/conjugate corner maps;
5. freezing `S_J` under a moving non-stabilizer frame;
6. replacing `Pi_4` by `Pi_14` or declaring them equal;
7. identifying first-order `R_4` with cubic `N_3`;
8. inferring a global section from associated descent;
9. calling transition-sign-independent descent merely projective;
10. identifying the endomorphisms `S_J` and `-S_J`;
11. identifying selected and opposite component lifts;
12. deleting one normal half; and
13. promoting an intertwined symbol to a physical state space.

The horn would be killed by a nonzero commutator on any compressed subblock,
a mismatch between vector and Spin stabilizer kernels, loss of one corner, or
transition-lift-sign dependence of the associated quotient. None occurs.

## 7. Reprioritization and fertility

TW3-C is high-value closure and low-fertility continuation:

```text
closure value:              8/10,
new twistor-path fertility: 2/10.
```

The hoped-for nontrivial component or stabilizer obstruction does not appear.
The selected/opposite distinction remains exactly the orientation sign already
known from TW-1, and every compressed operator square is formal
tensor-product commutation. Bank the theorem and deprioritize the
superposition-twistor path.

The path may reopen only if an independent owner supplies one of:

- a global component-tagged normal `U(3,2)` reduction;
- a genuinely non-product normal/mixed term in the rolled/detour operator; or
- an action-owned total complex whose overlap law is not already formal.

This channel must not manufacture any of those owners.

## Reproduction

```text
python3 tests/channel-swings/joe_directed_tw3c_rolled_compression_stabilizer_descent_probe.py
python3 tests/channel-swings/joe_directed_tw3c_rolled_compression_stabilizer_descent_probe.py --selftest
```

The strongest licensed statement is:

> Relative to a supplied component-tagged lifted `U(3,2)` normal reduction and
> TW1's exponential-path representative, the normal Spin lift descends and
> intertwines TW2's base-supported `Pi_4`, gamma trace and first-order
> compressed rolled symbol in its Krein/conjugate Riesz matrix copy. The raw
> strict `B14` dual is contragredient and has the different normal-flipping
> corner map. Transition lift signs do not make descent projective, while
> `-S_J` remains a distinct negative endomorphism. The global reduction needed
> to instantiate either endomorphism is unbuilt.

No physical superposition, emergent chirality, global complex, action,
background, domain, quotient or positive state is established.
