---
artifact_type: independent_verification
status: integration_repair
doc_type: adversarial_verification
created: 2026-08-15
work_item: IV-20260815-SESSION-015QSI
channel: integration_review
session_trailer: "Claude-Session: https://claude.ai/code/session_015qsiNaasV9Pabea3Cf9K64"
session_terminal_commit: fb854a13c0b3d956c83c1e06648c201a988fc785
verification_anchor: c1ba71594ea7
repair_base: 5f32f92a0c4d
target_claim: "INTERNAL — the 2026-08-15 verification handoff's claim that one 38-commit session formed a source-coherent, mechanically verified integration packet whose listed artifacts all had canonical_effect pending_integration. No registered GU source claim is attacked or defended wholesale; each source-facing correction below is bound to its exact sentence and layer."
target_claim_verdict: "SPLIT. The session contains reusable exact calculations and valuable repairs, but the handoff is not integration-safe as written: the exact GU commit count is 39; four probes were red at the verification anchor; three archaeology selftests could pass on a red baseline; canonical_effect was not uniform; ambient Killing/Krein indefiniteness had been conflated with positivity on an observed quotient; the total four-corner carrier had been conflated with the source's one-Weyl effective-generation statement; and the SU(3,2) chain had been promoted from best-supported reconstruction to source determination. This repair preserves the exact results, narrows their claim ceilings, and records the unresolved selection/action/BRST debts."
canon_verdict_change: none
ledger_edit: none
register_edit: routing entry for this verification artifact only
canonical_effect: pending_integration
probe: tests/channel-swings/joe_directed_session_015qsi_integration_audit.py
grade: "INDEPENDENT ADVERSARIAL VERIFICATION. Exact Git trailer/commit inventory; artifact-frontmatter census; source-register and correction-marker controls; session-scoped execution of all touched Python entry points plus relevant process gates. No scientific result is promoted by this record."
---

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`.

# Session 015qsi — coherence and integration repair

## Verdict

The session is **not one canon-ready result**. It is a mixed packet containing:

1. exact results that survive unchanged;
2. correct computations whose GU meaning needed narrowing;
3. source determinations that were stronger than their evidence;
4. conventional comparators that do not bind the GU-native mechanism; and
5. archaeology/control artifacts invalidated by later commits in the same
   session.

The repair does not discard the wave. It separates those five classes so later
owners can integrate the sound parts without importing the overclaims.

## 1. Exact session scope

The exact trailer query returns **39 commits in this repository**, not 38. The
terminal session commit is `fb854a13`. Three separately governed Runtime
commits carry the same trailer; they are not GU repository commits and are not
folded into the count.

The verification run was anchored at clean/even commit `c1ba7159`, after the
session and before later K112/RSAP work. The later work is unrelated and is not
evidence for or against this packet.

## 2. The GU-native story the surviving results must fit

- `Y = Met(X)` is endogenous. A section is the observation metric; it is not a
  Kaluza--Klein compactification or an additional projected dimension.
- The ambient geometry is deliberately noncompact/Krein. A positive observed
  sector, if obtained, requires a physical quotient and/or maximal-compact,
  Cartan or observer selection that the released source does not construct.
- The declared total fermionic arena is non-chiral and contains four corners.
  The source separately describes one effective generation as a Weyl pullback
  and the zero-form/one-form package as the input to its three-generation
  claim. Those are different layers, not competing quotations.
- The rank-10 normal bundle supplies the arena in which the internal real forms
  appear. Fibrewise subgroup inclusions do not themselves construct a global
  structure-group reduction or select it dynamically.
- Observation pullback is contraction along the section. It is not projection
  onto a horizontal sector, and normal one-form components are not thereby
  four-dimensional scalars.

## 3. Source-layer corrections

### 3.1 Ambient indefiniteness is not a disavowal of physical positivity

The UCSD sentence says that avoiding an indefinite Killing form was the old
mistake, then says the handling of that indefiniteness is unknown and
experimentally shielded by the maximal compact subgroup. It supports the
ambient noncompact/Krein posture. It does **not** disavow positivity on a later
observed physical quotient.

Accordingly, the source polarity is:

- ambient indefinite Killing/Krein structure: source-attested;
- positive physical quotient: source-open / source-unsupplied;
- maximal-compact or Cartan selection: mathematically available but not forced
  by the released GU construction.

The base-duality packet and `IT-C` now carry that distinction explicitly.

### 3.2 The computed null screen is a comparator construction

`BD-D`'s exact free-symbol quotient and inertias remain useful. The object is
the ghost-number-zero symbol cohomology of an imported linearized Yang--Mills
complex. GU has no source-derived Gauss law, interacting BRST charge or BV
physical quotient in the repository.

The general theorem survives on its stated module: an indefinite fibre form
cannot become definite on a nonzero `Gamma(Ad P)`-equivariant subquotient of
`Lambda^1 tensor ad` under the theorem's irreducibility and factorization
hypotheses. It does not quantify over enlarged ghost/antifield complexes,
nonlinear cohomology or a future GU action whose physical carrier is not such a
subquotient.

### 3.3 Total carrier and effective Weyl carrier are different layers

The full-Dirac/four-corner declaration and its centre-class arithmetic survive.
The source also explicitly says that one effective generation is the pullback
of a properly understood Weyl spinor, and separately assigns the
`Omega^0(S+) direct-sum Omega^1(S-)` package to its three-generation claim.

The repair therefore withdraws the global statement that "one Weyl spinor" is
only repository compression. What remains open is which source-attested half or
total carrier is operative after the still-unbuilt connection/VEV selection.

The `Spin(6,4)` `144` remains complex and has no invariant self-pairing in the
tested sense. The conventional `n_g -> n_g - 1` map remains conditional. The
128 remainder is a partner-placement and physical-decoupling obligation, not
an established intrinsic defect, because the total arena contains conjugate
corners and no physical carrier or mass operator has selected among them.

### 3.4 The right chain is a reconstruction with explicit datum costs

The literal `Spin(6,4) -> Spin(3,2) -> G_SM` nesting and the commuting-factor
reading are killed by exact group theory. The adjacent maximal-compact sentence
makes `SU(3,2)` the best-supported correction to the middle token.

What is established is a compatible fibrewise chain of inclusions. What is not
established is the audio token, direct inspection of the absent 2021 PDF, a
global compatible complex/determinant reduction of the normal bundle, or a GU
selection of the maximal-compact reduction. The source/register grade therefore
remains transcript-uncertain / reconstruction / partial; `SC-GRP-50` remains
proposed and unwritten.

### 3.5 VZ was repaired, not closed

`VZ4` correctly replaces projection language by contraction, upholds the
horizontal restricted identification, and kills the larger KK-scalar
corollary. This independent review supplies the required second verification
for that narrow wording correction, which is now integrated in
`canon/no-go-class-relative-map.md`. It makes no VZ-verdict movement. The larger
Velo--Zwanziger question remains conditional on the actual GU operator, action,
interacting constraint complex, background and physical carrier, matching the
source's own statement that the issue is unknown and is where testing should
begin.

## 4. Integration disposition

### Eligible after this wording repair

- `MET(X)` as the explicit metric argument of the first action, with no
  canonical metric selected from the pre-observation input alone;
- the base/fibre inertia computations and translation-depth lattice, at their
  stated algebra/module scopes;
- the `D_7` centre-class and first-order-into-dual class-shift results;
- the `Spin(6,4)` reality type of the `144`;
- the CN-2 notation repairs;
- the H10 coefficient and Hermiticity-precondition repairs;
- VZ4's contraction correction, now integrated after independent second
  verification; and
- AR-4's genericity control.

### Remain proposals or open conditions

- any ledger row replacing `LT-GR6b`;
- a positive GU physical quotient or interacting BRST/BV realization;
- the fibre Cartan/maximal-compact selector;
- operative carrier selection and the physical fate of the 128 remainder;
- source registration of the disputed chain; and
- a full VZ clearance.

### Comparator-only / metrology-only

The ordinary anomaly lattice, conventional `126` Majorana route, standard net
chirality arithmetic, and ledger degree-of-freedom/cut-vertex measurements
remain useful controls and bookkeeping. Without a six-part typed bridge they
do not advance or damage Weinstein's source-native construction.

## 5. Verification and control repair

At the clean verification anchor, 44 session-touched Python entry points were
run: 40 passed and four failed. All four failures were caused by later commits
inside the same session invalidating earlier expectations. `AR-1`, `AR-2` and
`AR-3` also had mutation selftests that could accept a red baseline as evidence
that every mutation fired.

The repair requires each archaeology selftest to run and accept its clean/null
baseline before any mutation, and it retypes or pins every later-session mover
instead of raising a moving repository-wide count. Final command results are
recorded below after integration validation.

## 6. Governance correction

The handoff's sentence "Every artifact below has `canonical_effect:
pending_integration`" was false. `AR-3` correctly carries `none`; `VZ4` carried
`proposed_diff_pending_second_verification` until this independent review
integrated its narrow correction; and several non-index artifacts omitted the
field. This repair makes the README describe the field as explicit per artifact
and adds the missing values without forcing different effects into one label.
README index files are indexes, not research artifacts, and are not required to
carry artifact frontmatter.

Nine older artifacts remain unclassified in the routing registry. The audit is
green at its declared baseline, not clean. This repair does not guess their
authors' classifications; the backlog remains explicit.

## 7. Final validation record

Validated from the integrated working tree before commit:

- **44/44 standalone Python surfaces passed**: the 43 session-touched Python
  files other than the repository-wide `scripts/reproduce_all.py` orchestrator,
  plus this review's integration audit. This includes the four probes that were
  red at the verification anchor.
- The integration audit passed **57/57** checks; its selftest first accepted the
  clean baseline and then caught **4/4** planted scope, metadata, correction and
  routing mutations.
- The formerly incomplete AR-2 selftest passed a clean baseline and caught
  **13/13** mutations. During this review its `type_counts` control was found to
  have drifted to the corrected live value and was repaired from `15 -> 15`
  vacuity to a real `15 -> 14` mutation.
- Heavy failure-path suites passed: BD-D **16/16**, CR-B **14/14**, SC-A **4/4**;
  the paired BD-C, IT-C, HE-2, CH-3, VZ4, AR-1 and AR-3 controls also passed in
  their repair streams.
- Ten relevant gates passed: certificate shape, current-state currency,
  kill-target typing, joe-directed README inventory, source-native comparator
  routing, correction propagation, retracted-claim citation, canon frontmatter,
  source-geometry contract and root README entrypoint mapping.
- The routing gate remains explicitly baseline-green at **9 UNCLASSIFIED**; the
  kill-target gate remains baseline-green with **3 pre-existing red** surfaces;
  correction propagation still reports its seeded older `NEEDS_RECHECK` set.
  None is represented as a clean backlog.
- `scripts/reproduce_all.py --quick --tracked-only --list` completed and found
  **1505** tracked test certificates. Registry JSON parsed, and `git diff
  --check` was clean.
