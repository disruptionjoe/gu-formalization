---
artifact_type: integration_review
status: complete
doc_type: conditional_path_integration_review
created: 2026-08-20
work_item: THREE-CONDITIONAL-COMPOSITIONS
title: "Three conditional compositions: the Lorentz discriminator closes strongest, the H210 incidence remains unowned, and the occupied 1+2 module has no canonical family quotient"
classification: BRIDGE_OR_SEMANTIC_BOUNDARY
canonical_effect: none
depends_on:
  - lab/methods/source-native-comparator-routing.md
  - lab/active-research/joe-directed/conditional-build-channel-read-packet-2026-08-16.md
  - lab/active-research/joe-directed/soldered-ad/sa2-two-lorentz-source-package-classifier-2026-08-20.md
  - lab/active-research/joe-directed/bit1-price/bp2-nonuniform-corner-decoupling-owner-census-2026-08-20.md
  - lab/active-research/joe-directed/z3-receptacle/z3r2-asymmetric-equivariant-module-inventory-2026-08-20.md
  - lab/process/hostile-reviews/2026-08-20-joe-directed-three-conditional-compositions-review.md
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: **`BRIDGE_OR_SEMANTIC_BOUNDARY`.**

# Three conditional compositions — integration and reprioritization

## Result in plain language

This wave found one strong structural discriminator and two exact dependency
localizers. None authorizes another construction in this channel today.

1. **SA2 closes the most.** The already-soldered bundle admits two candidate
   Lorentz embeddings, but only the block embedding preserves the source's
   factorized external-Weyl/internal-partner grammar. Under the endogenous
   graph embedding the source `F` package has no pure Weyl submodule, both
   exact Pati--Salam owner lines move, and simultaneous PS plus graph-Lorentz
   invariance forces full internal `so(6,4)` invariance, where
   `Inv(16 tensor 144)=0`. This kills factorized PS-partner custody on that
   horn without selecting the block embedding physically.
2. **BP2 tells us exactly what a 2+1 decoupling map would have to contain.** A
   nonzero family row and a declared rank-`k` partner map have kernel dimension
   `48-k` per half. H210 is the only built right-direction, right-cell port in
   the explicitly bounded five-candidate inventory, but the row, source
   alignment and phase activation remain independent missing owners.
3. **Z3R2 finds a genuine occupied singleton-plus-pair structure, but not a
   family mechanism.** The geometric Q module has honest multiplicities
   `(14,12,12)`. Its nontrivial characters form a conjugate pair, while module
   automorphisms obstruct any canonical one-line-per-isotype quotient. No map
   reaches the dimension-13 receptacle or the GU `F/M_3/144` carriers.

The correct portfolio action is therefore **bank all three and leave this
particular path**. The absent next arrows are precisely the off-limit owners
the conditional-build channel must not manufacture.

## Twenty-lens preflight and what it changed

| lens | preflight ruling carried into execution |
|---|---|
| source `2+1` | representation-origin asymmetry, never an ordinary family index |
| comparator routing | conventional Higgs, `126`, index and chirality checks remain controls only |
| `F/M_3/144` custody | imposter, multiplicity space and partner stay distinct |
| chirality | total carrier remains nonchiral; observed chirality remains emergent |
| K77 carrier | both halves and all four corners remain present |
| equation (9.16) | rows, columns, form legs and independent bars remain exact |
| soldering | SA1 already settled the bundle as soldered; do not reopen inert-ad |
| Lorentz fork | compare block and endogenous embeddings without selecting either |
| action boundary | the physical embedding selector is action-owned and off limits |
| branching | exact weights and dimensions precede source interpretation |
| PS owners | test the distinct `54` and `210` lines, not a generic VEV |
| support price | retain B1P1's one-way `CHIRAL => PRESENT => A` statement |
| family algebra | use a basis-free exact sequence, not named families |
| H210 census | predeclare a finite inventory; make no repository-global uniqueness claim |
| phase semantics | infer neither `MASSIVE => B` nor an `A -> B` transition |
| bare `Z/3` | group order is not occupancy or a family count |
| equivariant module | distinguish K3 Q, dimension-13 and K77 carriers |
| symmetry custody | separate `Aut(C3)`, affine tensor-by-character equivalence and fixed-module automorphisms |
| archaeology | claim only the new compositions of already-owned results |
| falsification | every arc states kill, survive, dependency fence and ceiling |

The preflight prevented three tempting but invalid shortcuts: using the action
to choose the Lorentz horn, naming `ker(r)` as the true-family plane, and
turning an order-three module fingerprint into a family selector.

## Exact results and ceilings

### SA2 — structural discriminator

The endogenous restrictions are exact. In doubled Lorentz labels,

```text
10 -> (2,2) + (0,0)
16+ = 16- -> (3,1) + (1,3).
```

Each endogenous `F+/-` has dimension `64` and zero `(1,0)` or `(0,1)` Weyl
summands, while each `Z+/-` retains two of each as an adverse control. Both PS
owner tensors have block orbit rank `0` and endogenous orbit rank `3`.
Simultaneous PS and graph-Lorentz invariance gives invariance under
`k_PS + [delta(so(1,3)),k_PS]`, whose exact rank is `45`; hence the factorized
partner line vanishes because the full D5 invariant multiplicity is zero.

This does not exclude every non-PS mixed endogenous intertwiner, and it does
not select the observed Lorentz embedding.

### BP2 — minimum incidence and owner fence

For `0 -> K -> M_3 -> L -> 0` and declared `B:S_16 -> W`,

```text
0 -> K tensor S_16 -> ker(r tensor B) -> L tensor ker(B) -> 0,
rank(r tensor B)=rank(B),
dim ker(r tensor B)=48-rank(B).
```

At H210 rank `16`, each half has rank `16` and kernel `32`; the independent
conjugate pair has rank `32` and kernel `64`. This is an exact incidence
scaffold, not an owned decoupling map. The route stops at `r`, `H210-ALIGN`
and phase/cell activation.

### Z3R2 — occupied isotypic split and naturality obstruction

The honest geometric Q kernel is

```text
C^14 tensor 1 + C^12 tensor chi + C^12 tensor chibar.
```

`Aut(C3)=C2` fixes the trivial character and exchanges the nontrivial pair.
The enlarged `AGL(1,3)=S3` label comparison additionally includes
tensor-by-character translations; it is not the automorphism group of a fixed
module. The `(14,12,12)` fingerprint has singleton-plus-pair stabilizer under
that declared comparison, and all sectors are occupied. Yet
`End_C[C3](V_Q)=M14(C)+M12(C)+M12(C)`, so no nonzero isotype covector is
natural under the full fixed-module automorphism group. Rank-three equivariant
quotients exist only after choosing extra covectors.

That is a structural receptacle fact, not an owned source bridge.

## Vote, conviction and path-relative priority

The hostile review's seventeen lenses voted:

```text
SA2    10  strongest pruning and closure value
BP2     5  closest source-direct dependency localization
Z3R2    2  exact but bridge-remote structural receptacle
```

Continuation votes under this channel's contract were `0/17` for every arc.

The highest-conviction statements are slightly differently ordered:

1. BP2's exact kernel sequence and rank formula;
2. SA2's repaired graph-invariance closure and factorized-custody kill;
3. Z3R2's fixed-module naturality obstruction;
4. H210's uniqueness only inside BP2's explicitly bounded inventory.

## New scaffold and reopen conditions

The next series of swings should not continue any of these three spines by
inventing their missing owners. Instead use this scaffold:

1. begin from a new independently fertile source-native horn;
2. carry the block/endo Lorentz distinction as a compulsory type check;
3. if family multiplicity appears, demand an already-owned covector or
   natural quotient before using `2+1` language;
4. if order-three topology or equivariance appears, demand an explicit typed
   map to the actual K77/source carrier before interpreting it;
5. retain the BP2 exact sequence and Z3R2 automorphism test as adverse controls;
6. stop again at the first action, selector, alignment, coefficient or datum.

Legitimate reopen triggers are correspondingly narrow:

- **SA2:** an already-owned full-corner, non-PS endogenous intertwiner that
  preserves source provenance, or an independently action-owned embedding
  selection outside this channel;
- **BP2:** independently owned `r`, `H210-ALIGN` and phase/cell activation;
- **Z3R2:** an independently owned typed map from the geometric Q module to
  the dimension-13 or actual source/K77 carrier, plus a natural quotient datum.

Absent those events, the final dispositions are `BANKED CLOSURE`,
`DEPENDENCY_FENCED`, and `STRUCTURAL INVENTORY ONLY`, respectively.

## Validation receipt

- SA2: `80/80`, with `8/8` false routes killed; HE3 `23/23`.
- BP2: `90/90`, with `17/17` semantic mutants; B1P1 `91/91`, CB2 `22/22`,
  HE3 `23/23`, and RSC1 `103/103` dependency controls.
- Z3R2: `78` exact checks, `7/7` mutations; Leg C `580` hard assertions and
  Z3R1 `62/62`.
- Independent hostile review: all three cores pass after four material typing
  repairs; no arc receives an immediate-continuation vote.

SA1's aggregate predecessor harness retains separate receipt drift
(`100/105`), documented by the hostile review. SA2 independently recomputes
its load-bearing geometry; the drift is not restated as a green predecessor
receipt and is not used to strengthen this result.
