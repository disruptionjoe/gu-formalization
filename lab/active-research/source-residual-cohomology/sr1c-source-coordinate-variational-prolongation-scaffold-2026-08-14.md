---
title: "SR-1C source-coordinate variational-prolongation scaffold"
status: active_research
doc_type: research_run_scaffold
created: "2026-08-14"
lane_id: SRC-RES-COH-01
swing_id: SR-1C
role: forward_enabling_packet
channel_scaffold: superposition-twenty-lens-hypothesis-council-reverse-scaffold-2026-08-14.md
claim_grade: "FORWARD ENABLING PACKET WITH RETAINED LENS AUDIT; NO NEW GU SOLUTION OR PHYSICAL COHOMOLOGY RESULT"
canon_verdict_change: none
---

# SR-1C source-coordinate variational-prolongation scaffold

## Scope correction

This artifact is the forward enabling packet for `VRS-5`, not the complete
superposition-channel scaffold. The twenty lenses were initially applied here
as an audit checklist; the corrected council, vote, conviction review and
reverse conditional-building sequence are recorded in
[`superposition-twenty-lens-hypothesis-council-reverse-scaffold-2026-08-14.md`](superposition-twenty-lens-hypothesis-council-reverse-scaffold-2026-08-14.md).

The variational packet below remains the live background construction because
the reverse scaffold presently needs its missing stationary-background premise.

## Result first

The source-residual/superposition scaffold remains directionally sound, but its
old linear ordering is no longer execution-grade. The canonical-`B_Z`,
nonzero-`T` branch has already passed the printed residual, true translation
Euler and inherited Bianchi rows at field one-jet grade. The next live gate is
not another `J` census and is not full formal integrability in isolation. It is
a **minimal source-coordinate variational prolongation** on the same exact
two-root witness.

The immediate task is:

```text
derive E_B on the admitted witness;
determine and construct exactly the field two-jet needed by
  D_B^!(E_B-E_T);
evaluate primitive E_epsilon and the complete fixed-varpi metric Euler row;
then evaluate the dependent observation receiver;
only after those rows close, continue to the complete formal-integrability
  tower and total boson--fermion carrier.
```

`SR-1` remains `BACKGROUND-MISSING`; `SR-2` remains blocked. This artifact
changes the execution scaffold, not a scientific verdict.

## Locked input

Use the canonical Levi-Civita Zorro/DeWitt connection `B_Z` and the exact
algebraic coefficient ring

```text
A = QQ[t]/(28392 t^2+91 t-351).
```

Carry both real embeddings

```text
t_+ = (-91+sqrt(39870649))/56784,
t_- = (-91-sqrt(39870649))/56784
```

without choosing a floating representative. Hold fixed the admitted local
formal data

```text
C  = Phi1 wedge Phi1,
T  = t Phi1,
DT = -F_BZ+(-t/312-t^2)C+Q,
```

where the exact symmetric correction is

```text
Q_(0,a)^0a = -9/56,  a in {1,2,3,10},
Q_(0,a)^0a =  5/56,  a in {4,5,6,7,8,9,11,12,13},
all other Q = 0.
```

The symmetric-`DT` action map has rank `195` in `196` rows. Its cokernel is
one-dimensional and has a fourteen-cell certificate support. Do not retype
that support size as a cokernel dimension.

For this bulk search, freeze compact-support or boundary-vanishing variations.
Unrestricted endpoint transformations and their moment map remain a separate
boundary/BFV horn. Treat zero fermions as a provisional input to be explicitly
checked at the total-field-completion stage, not as an automatically stationary
fermion sector.

## Twenty-lens reassessment

| lens | finding | consequence for this run |
|---|---|---|
| Layer-0 semantics | Action Euler rows, printed residuals and observation receivers are different object types. | Close each under its actual owner; do not substitute one for another. |
| Source criticism | `B_Z` is repository-canonical, not proved source-unique. | Keep a derived rival Zorro reconstruction as a fallback, not a concurrent target. |
| Source-coordinate geometry | The primitive fields are `(g,varpi,epsilon)` and `T=varpi-B_Z(g)`. | Use `E_varpi=E_T` and fixed-`varpi` `delta T=-delta B`; do not impose artificial independent `B,T` stationarity. |
| Variational bicomplex | `E_epsilon` contains the divergence `D_B^!(E_B-E_T)`. | Derive the Euler dependency graph before solving rows. |
| Jet-order determinacy | The admitted field one-jet does not determine that divergence. | Construct the minimal compatible field two-jet inside this gate. |
| Metric geometry | The direct density `-t(27+728t^2)` is nonzero at both roots but is only one partial. | Compute the complete fixed-`varpi` metric/Hodge/frame/volume graph before declaring an obstruction. |
| Zorro geometry | The canonical connection vanishes on the nine mixed trace-curvature planes while the old homogeneous branches do not. | Preserve the labelled canonical geometry and do not import the excluded branches. |
| Clifford representation | The exact computation is selected-Spin K77 graded. | Do not transfer it to either `U32` half or the full `U64` carrier without a typed adapter. |
| Algebraic geometry | The two amplitudes are conjugate roots of one irreducible exact polynomial. | Work over `A` and report each real embedding only after exact reduction. |
| Certificate theory | The action obstruction quotient is one-dimensional with fourteen-cell support. | Preserve the exact rank, support and thirteen-cell correction separately. |
| Spencer/formal integrability | Some prolongation is needed now, but the full tower is a later burden. | Solve only the minimal jet extension needed for the Euler rows, then run the complete tower if they close. |
| PDE/hyperbolicity | A compatible formal jet is not a local germ, open solution or well-posed evolution. | Make formal tower, local germ and global background distinct later gates. |
| Microlocal symbol | Symbol data from another background does not decide this branch. | Recompute the common gauge/Euler symbol only after the source Euler jet closes. |
| BV/derived geometry | RF-1 and RF-2 exhausted the cheap fixed-`J` and moving-split tests. | Pause further `J`/BV descent until an actual `K_total,L_total` carrier exists. |
| Boundary/BFV | Unrestricted endpoint transformations carry live momentum. | Keep the bulk boundary horn frozen and type any edge completion separately. |
| Observation functoriality | Observation is a dependent receiver of the source fields. | Evaluate its induced return after underlying Euler closure; do not add an independent observation Euler row. |
| Fermionic completion | The witness is presently bosonic. | Later certify zero-fermion Euler closure or construct a nonzero-fermion correction. |
| Operator/Krein theory | Domain, adjointability and positivity require an actual common complex. | Defer pairing and positivity work; the finite jet supplies no Hilbert-space result. |
| Homological/local-to-global | Point jet, formal tower, local germ, global background and `H^1` are distinct. | Replace the overloaded old `F0` by explicit stages. |
| Adversarial/phenomenological | Survival is only `NOT-YET-FALSIFIED`; failure of an owned row kills only the declared branch. | Preserve the asymmetric kill rule and defer measurable-channel claims to `SR-7`. |

## Revised forward scaffold

```text
S0  source owner and canonical labelled geometry
    DONE at the declared reconstruction grade
     |
S1  exact point/action/Bianchi field one-jet
    DONE for both algebraic nonzero-t roots
     |
S2  minimal source-coordinate variational prolongation
    E_B, required two-jet, primitive epsilon, total fixed-varpi metric row
    NEXT
     |
S3  total-field completion
    fermion Euler rows, common gauge carrier, reducibility, boundary horn
     |
S4  full formal integrability and existence
    compatible tower -> local germ -> legal global background/domain
     |
S5  replay SR-1 on the actual carrier
    K_total, L_total, L_total K_total=0
     |
S6  SR-2 and later physical-cohomology swings
```

The reverse-falsification spine is synchronized rather than discarded:

```text
RF-1 fixed-J census:                 complete
RF-2 moving-split Stueckelberg cone: complete
RF-3 total coupled descent:          paused until S5 owns K_total,L_total
```

## Execution packets

### Packet 0 result: owner-operator gate

Current GU truth returns `TYPE-MISSING` before Packet A can be evaluated. The
missing `O_SR1C` packet is the common-basis evaluator for
`j^1(E_B-E_T)` on the exact algebraic branch plus the fixed-`varpi`
derivatives of `B_Z`, Shiab, Hodge, frame, density and lowerer. The dependency
identity and separate exact receipts do not serialize those outputs on one
witness. Construct and held-out validate `O_SR1C`; do not set an absent slot
to zero. See
[`selected-k77-sr1c-owner-operator-type-gate-2026-08-14.md`](../../../explorations/conditional-build/selected-k77-sr1c-owner-operator-type-gate-2026-08-14.md).

### Packet A: source-coordinate dependency graph

1. Derive the action variation in primitive coordinates `(g,varpi,epsilon)`.
2. Serialize `E_B-E_T` as the dependent connection covector needed by the
   epsilon and fixed-`varpi` metric variations.
3. Separate coefficient, formal-adjoint, Hodge, frame, volume, moving-Shiab
   and receiver contributions before evaluating the branch.
4. Record the exact jet order and carrier of every term.

**Stop condition:** an unowned or ill-typed derivative prevents forming the
row. Return `TYPE-MISSING` with the smallest missing source map; do not replace
it by zero.

### Packet B: minimal compatible prolongation

1. Determine all field-two-jet variables on which
   `D_B^!(E_B-E_T)` depends.
2. Impose holonomicity, algebraic symmetry and inherited differential Bianchi
   relations on those variables.
3. Solve over `A`, preserving both real embeddings.
4. Emit an exact sparse witness or an exact left-cokernel/inconsistency
   certificate.

**Stop condition:** if an owned compatibility equation excludes one root,
kill that root at the named row while continuing the conjugate branch unless
exact conjugacy proves the same disposition.

### Packet C: joint variational closure

Evaluate on the same witness:

```text
E_varpi = E_T,
E_epsilon = D_B^!(E_B-E_T)+(D_epsilon S)^!K_S,
E_g at fixed varpi, including dependent B_Z, T, Shiab, Hodge, frame and volume.
```

Then compute the induced observation receiver. It is a consistency check on
the source graph, not an additional independently varied action field.

**Pass:** all owned bulk Euler rows vanish exactly and the receiver is
compatible on at least one algebraic branch.

**Kill:** an exact nonzero owned row or cokernel certificate survives all
admitted minimal two-jet freedom for the declared branch.

**Underdetermined:** allowed only when the exact owner/type or a strictly
higher jet is proved missing. Name the minimal successor object.

### Packet D: only after Packet C passes

Continue the full Spencer/formal-integrability tower, check the provisional
fermion sector, construct a local germ or prove its obstruction, and then test
global Observerse and boundary legality. Only after those steps may `SR-1`
be replayed and `SR-2` become eligible.

## Acceptance and prohibitions

The run succeeds as a research run if it produces one of:

- an exact source-coordinate stationary two-jet admitted to total-field
  completion;
- an exact branch-specific kill certificate at the first failing owned row;
  or
- a minimal, typed `TYPE-MISSING` result that identifies the precise source
  map or higher jet required next.

Do not:

- retry the exhausted zero-`T` family or either excluded homogeneous branch;
- choose one floating root before exact quotient-ring reduction;
- fill unowned higher derivatives with zero;
- promote the direct density partial to a total metric obstruction;
- count the observation receiver as an independent Euler equation;
- start another `J` census, Hessian factorization, positivity or domain run;
- infer a formal solution, global background, physical cohomology,
  superposition, Born rule, particle spectrum or empirical prediction from a
  successful finite jet.

## Existing exact controls

Before reporting a new disposition, rerun:

```bash
sage -python tests/channel-swings/selected_k77_zorro_nonzero_t_first_action_jet_probe.py
python3 tests/channel-swings/selected_k77_nonzero_t_epsilon_jet_order_gate_probe.py
sage -python tests/channel-swings/selected_k77_zorro_differentiated_shiab_second_jet_probe.py
python3 tests/channel-swings/source_residual_cohomology_sr1_total_residual_complex_background_probe.py
```

Any new probe must preserve exact arithmetic, serialize its carrier and row
owners, distinguish the two real embeddings, and include positive and negative
controls for jet-order determinacy.
