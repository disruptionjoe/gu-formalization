---
title: "B5 Krein-adjoint and mirror-orbit reduction"
status: completed
doc_type: run_plan_and_receipt
run_id: RUN-20260725-031112-gu-formalization-progress
owner_service_id: RUN-20260725-031112-gu-formalization-progress
parent_run_id: RUN-20260725-031112-repository-work-cycle-nbl-hourly
owner_id: gu-formalization
workflow: repo-progress-run
workflow_revision: sha256:3cc3db78e03c512e64206aa63ee96059c981f018888ed7b215776368fc38104d
orchestration_workflow: repository-work-cycle
orchestration_workflow_revision: sha256:ca14ff3517db01c5b0decfed879b44f6646b4ba910ea0582e605ae863eb7c499
mode: execute
lane_id: "1"
starting_revision: 8f2aebfc63c1ee0e8e0429280545a6bfb77771cd
opened_at: 2026-07-25T03:14:56-05:00
completed_at: 2026-07-25T03:21:14-05:00
method_refs: []
---

# B5 Krein-adjoint and mirror-orbit reduction

## Phase Open

Scheduled/non-interactive Progress phase under parent
`RUN-20260725-031112-repository-work-cycle-nbl-hourly`.

Resolved packet:

```yaml
repo: gu-formalization
workflow: system-runtime#repo-progress-run
workflow_revision: sha256:3cc3db78e03c512e64206aa63ee96059c981f018888ed7b215776368fc38104d
mode: system-canon#execute
run_id: RUN-20260725-031112-gu-formalization-progress
parent_run_id: RUN-20260725-031112-repository-work-cycle-nbl-hourly
lane_id: "1"
starting_revision: 8f2aebfc63c1ee0e8e0429280545a6bfb77771cd
method_refs: []
resume_capsule: null
```

The branch `agent/operator-anomaly-big-swing` began clean and even with its
upstream. Repository session sync passed, the resolved writer-claim path was
absent, and this Run acquired it. The two recent human-directed Woit/twistor
Runs and the immediately preceding B5 matrix Run are complete and pushed.
No live Run overlaps the declared footprint.

## Objective

Advance Lane 1 `B5-INDEPENDENT-RECONSTRUCTION` through the first largest safe
portion of `B5-NATIVE-REAL-KREIN-ADJOINT-FREEZE`.

The preceding Run closed the complexified observer-symbol space at 20 slots
and 136 ordered one-dimensional cells. This Run will compute the exact orbit
geometry generated by:

1. formal-adjoint transpose;
2. the declared normal-chirality mirror support exchange; and
3. their product.

It will then derive the real coefficient dimensions under a Krein-adjoint
condition for both possible coflip types:

- linear mirror exchange; and
- antilinear real/quaternionic mirror exchange.

Every unfrozen native phase remains symbolic. The intended material effect is
to replace a 136-cell qualitative residual with an exact phase-sensitive
admissibility theorem and the smallest remaining native packet. The Run will
not choose a differential, a favorable phase, a Green domain, or a positive
Hilbert quotient.

## Purpose And Lane Selection

- Purpose connection: this directly tests whether the strongest frozen GU
  carrier class can support a native mirror-breaking differential rather than
  importing one by coefficient choice.
- Selected numbered Lane: Lane 1, `GU truth testing`.
- Owner manifest: `LANES.yaml`,
  SHA-256 `5c535ae8674718dc2f2bfedf21bfe4c04ac9cceafe62bbfe1428e3814da9f083`,
  definition/control revisions `1/1`.
- Selection basis: `LANE-STATE.yaml` names this exact continuation as the
  protected lead. Lane 2 has no internally executable lead; Lane 3 is ready
  but lower scientific value and its protected Lean kernel needs a reviewed
  context.
- Emergency state:
  `repos/private/system-runtime/operations/lane-emergency-revocations.yaml`,
  revision `1`, entries `[]`, SHA-256
  `8a992d3eb3f61b51ef83aa7cb8f85a1865fd0bf76c1f690429fa200a1c698723`.

## Construction-Fork Guard

The carrier and adjoint are program-native: observer restriction of the
`(9,5)` Rarita-Schwinger/Krein carrier, not the unreduced `Spin(14,C)`
carrier and not a conventional positive-Hilbert physical quotient.

The computation will explicitly keep apart:

- formal Krein adjoint, whose support action is cell transpose;
- a linear coflip, which is not the same structure as an antilinear
  real/quaternionic conjugation; and
- common closed operator domains and Green boundary data, which representation
  multiplicities alone cannot select.

A no-go is acceptable only at the construction and phase assignment actually
tested.

## Concrete First Attempt

Extend the exact 20-slot matrix certificate with an independent reduction
certificate that:

1. reconstructs all 136 cells without copying a target count;
2. proves transpose and mirror are fixed-point-free commuting involutions;
3. classifies their joint orbits and identifies every cell fixed by their
   product;
4. derives the formal-adjoint real dimension;
5. derives the mirror-even and mirror-breaking dimensions for linear coflip;
6. derives the complete phase-parametric dimension family for antilinear
   coflip; and
7. contains hostile controls that fail if provenance is collapsed, `X` is
   omitted, coflip type is conflated, or a native phase assignment is silently
   chosen.

If the matrix does not admit this exact reduction, adapt to the independent
Lane 1 anomaly-hardening T1 only after recording the precise local failure.

## Expected Writable Surfaces

- `runs/RUN-20260725-031112-gu-formalization-progress.md`
- `tests/shiab_b5_krein_mirror_orbit_reduction.py`
- `tests/README.md`
- `explorations/shiab-operator/b5-krein-mirror-orbit-reduction-2026-07-25.md`
- `LANE-STATE.yaml` only if the semantic next residual moves

Off limits: owner canon, claims, scientific verdicts/grades, public posture,
`LANES.yaml`, the durable portfolio, System Runtime, another repository, and
all non-GitHub external actions.

## Validation Plan

- Run the new exact certificate.
- Re-run the 20-slot matrix certificate and the earlier D7 intertwiner anchor.
- Parse owner YAML/JSON.
- Run applicable test/exploration inventory, changed-path, protected-surface,
  research-posture, and process-gate audits.
- Run Python compilation and `git diff --check`.
- Revalidate authority, Lane control, emergency state, writer claim, and
  footprint before every consequential effect and closeout.

## Flow Contract

- Required flows:
  `standard-run-safety-check`, `select-lane`, `create-run-plan`,
  `revalidate-lane-selection`, `append-run-receipt`.
- Conditional flows expected:
  `classify-artifact-disposition`, `rerank-next-work`, and
  `refresh-lane-state` only if the semantic next residual moves.
- Required-flow exceptions: none anticipated.

## Stop Conditions

- A competing writer or overlapping Run appears.
- Authority, Lane control, emergency state, or the frozen carrier ledger
  changes.
- The reduction requires selecting an unfrozen native phase, domain, lower
  order term, physical quotient, or target-theory coefficient.
- A conclusion would change a claim, canon verdict, public posture, Lane
  control, or require a non-GitHub external action.

## Execution Notes

The concrete first attempt succeeded.

1. The new certificate reconstructed all 136 cells from the prior exact
   observer matrix and proved that formal-adjoint transpose and mirror support
   exchange are commuting fixed-point-free involutions.
2. Their joint action has exactly 39 orbits: 29 four-cell orbits and ten
   two-cell orbits. The latter are precisely the adjoint edges joining a slot
   to its declared mirror.
3. Formal Krein self-adjointness reduces the complex ordered-cell space to 68
   complex edge parameters, hence real dimension 136, independently of the
   normalized unit phases.
4. A linear coflip splits that space into 68 mirror-even and 68
   mirror-breaking real directions.
5. For an antilinear coflip, the ten special edges carry the complete residual
   phase sensitivity. All 1024 normalized sign assignments collapse to eleven
   dimension pairs:
   `(58,78), (60,76), ..., (78,58)`.
6. Hostile controls proved that omitting `X`, collapsing provenance, or
   conflating linear and antilinear coflips changes or destroys the result.

No native phase was selected. The work therefore closes the algebraic
adjoint/mirror support reduction but does not close the native differential,
Green form, common domain, mirror obstruction, symbol exactness, or BV
cohomology.

`classify-artifact-disposition` classified the new executable certificate,
research result, navigation updates, Lane summary, and this Run record as
versioned knowledge. Generated bytecode and captured output remained scratch
and were not staged.

`refresh-lane-state` was invoked because the semantic residual moved from a
qualitative 136-cell native-adjoint freeze to ten explicit phase invariants
plus one Green/domain packet. The durable portfolio, Lane definitions,
claims, canon, verdicts, grades, papers, and public posture did not change.

## Next-Work Handoff

- Current work: `B5-INDEPENDENT-RECONSTRUCTION`.
- Current disposition: `CONTINUE`.
- Durable priority owner: GU Lane A stewardship.
- Recommended next:
  `B5-NATIVE-PHASE-AND-DOMAIN-PACKET`.

Within Lane 1:

| rank | candidate | reason | exact next gate |
|---:|---|---|---|
| 1 | B5 native phase/domain packet | Direct continuation of the protected lead; this Run reduced the unknown algebra to ten phase invariants | Supply slot-pairing phases, coflip type/phases, differential adjoint sign, Green form, and one common closed domain |
| 2 | `ANOMALY-DESCENT-HARDENING` T1 | Strongest independent Lane 1 alternative | Reconstruct the exact class rather than quoting an ambient bordism table |

Across numbered Lanes, Lane 1 remains first. Lane 2 has no internally
executable computation; Lane 3 `PROOF-STABLE-KERNELS` is ready but lower
scientific value and its next protected Lean effect needs a reviewed context.

Overturning evidence is a defect in the 20-slot matrix, a source-owned phase
or domain packet, proof that the required common domain cannot exist, an
official Lane 2 release, or a capacity-backed Lane 3 hardening request.

## Receipt

- Phase result: `progressed`.
- Service outcome: `progressed`.
- Lane: `1`; manifest
  `sha256:5c535ae8674718dc2f2bfedf21bfe4c04ac9cceafe62bbfe1428e3814da9f083`;
  definition/control revisions `1/1`.
- Material effect:
  `B5-KREIN-MIRROR-ORBIT-REDUCTION-COMPLETE`.
- Exact result: 136 ordered complex cells reduce to 68 formal-adjoint edges
  and 39 joint orbits (`29 x 4 + 10 x 2`). The adjoint space has real
  dimension 136. Linear coflip gives `(68,68)` parity dimensions; antilinear
  coflip gives exactly eleven pairs from `(58,78)` through `(78,58)`.
- Residual:
  `B5-NATIVE-PHASE-AND-DOMAIN-PACKET` with five typed fields:
  slot-pairing phases, coflip linearity/phases, differential formal-adjoint
  sign, Green boundary form, and common closed domain.
- Actual footprint:
  `tests/shiab_b5_krein_mirror_orbit_reduction.py`;
  `tests/README.md`;
  `explorations/shiab-operator/b5-krein-mirror-orbit-reduction-2026-07-25.md`;
  `LANE-STATE.yaml`; and this Run record.
- Scientific boundaries:
  no differential, favorable coefficient, native phase, Green domain,
  physical quotient, mirror obstruction, symbol exactness, BV cohomology,
  claim, canon verdict, grade, paper state, or public posture changed.
- Validation:
  the new exact certificate passed its algebraic and hostile controls;
  the prior 20-slot matrix certificate remained
  `B5-COMPLEX-OBSERVER-SYMBOL-MATRIX-COMPLETE`;
  the independent D7 certificate remained `2` per chiral block and `4` for
  full Dirac; Python compilation passed; owner YAML and portfolio JSON parsed;
  test inventory, exploration boundary, changed-public-path, protected
  surface, research-posture, README-entrypoint, and process-gate inventory
  audits passed; and staged/unstaged `git diff --check` passed.
- Required flows attested:
  `standard-run-safety-check`, `select-lane`, `create-run-plan`,
  `revalidate-lane-selection`, and `append-run-receipt`; no exceptions.
- Conditional flows invoked:
  `classify-artifact-disposition`, `rerank-next-work`, and
  `refresh-lane-state`. `evaluate-run-with-rubric` was not invoked.
- Pinned Progress graph:
  `system-runtime#repo-progress-run@sha256:3cc3db78e03c512e64206aa63ee96059c981f018888ed7b215776368fc38104d`.
- Effect-boundary revalidation:
  owner authority
  `sha256:1007e5871c0311e3ea9deead63fdeee1513d2058acdea44ad7c11dd3482057d8`;
  Lane manifest
  `sha256:5c535ae8674718dc2f2bfedf21bfe4c04ac9cceafe62bbfe1428e3814da9f083`;
  emergency state revision `1`, entries `[]`,
  `sha256:8a992d3eb3f61b51ef83aa7cb8f85a1865fd0bf76c1f690429fa200a1c698723`;
  writer claim remained owned by this Run.
- Method refs/effect: `[]` / `null`.
- Attention route: `none`; awareness pointer: `none`; methodology-learning
  pointer: `none`.
- External effects:
  routine non-force GitHub versioning only; no other external action.
- Starting revision:
  `8f2aebfc63c1ee0e8e0429280545a6bfb77771cd`.
- Resulting revision:
  recorded by the enclosing owner-service result after commit and push.
- `phase_close`: this receipt closes the only formal phase. No further owner
  content effect may be added under this packet.
