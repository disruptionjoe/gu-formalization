---
artifact_type: exploration
status: exploration (W174 / TEAM FAMILY-SELECT; 5 personas inline; one deterministic test; the W133 two-family standoff attacked as a GU-STRUCTURAL SELECTION problem -- does GU's specific structure FORCE the positivity/keep-and-grade family, breaking W133's symmetric permanent-declaration?)
created: 2026-07-14
label: W174
hypothesis: "advances W133 (H71) / feeds H59. W133 established that the two consistent quantizations of GU's one ghost-carrying Lagrangian are two INEQUIVALENT quantizations, each saturating exactly one of two JOINTLY-UNSATISFIABLE Kallen-Lehmann axioms -- analyticity/microcausality (the removal/Lee-Wick family, pays the off-axis microcausality violation ~1/m2) vs positivity (the graded/keep-and-grade family, pays the odd-cut leak) -- one-to-one with the CLOP band endpoints, and concluded PERMANENT DECLARATION (dispersion alone does not select), the two costs being EXACTLY SYMMETRIC (0,1) vs (1,0). W174 asks whether GU's SPECIFIC structure (the proven-indefinite (9,5) q=5 tangent Krein signature, the record substrate, the C-operator = consistent-global-ledger W150, the finality frontier / firewall) SELECTS the positivity family as FORCED, breaking that symmetry, or whether the selection is a re-declaration in disguise."
title: "W174 VERDICT: NARROWED. GU's substrate is NOT axiom-neutral, so W133's symmetric permanent-declaration is BROKEN AT THE FRAMING LEVEL -- but the selection is NOT closed to OPERATIVE-grade forcing. TWO convergent GU-structural arguments both lean on the positivity/keep-and-grade family and against the removal family: (ARG-1) the removal family TRUNCATES the negative-norm sector, giving a definite physical space = effective q=0 = a CLOSED COMPLETION (H_C+ = whole space, finality frontier empty), which GU's proven (9,5) q=5 indefiniteness (W131) + firewall criterion 1 (no closed completion) forbid, whereas the graded family gives a PROPER H_C+ with a q=5 latent remainder = frontier never empty (W150 verdict D); (ARG-2) the removal family PAYS a bounded ~1/m2 microcausality violation (off-axis absorptive content, N_A=1) while the graded family PRESERVES microcausality (N_A=0), so a record causal-ordering requirement excludes removal and forces graded. BOTH arguments converge on graded, which is the genuine advance over W133's symmetry: within GU the two axioms are not interchangeable, the structure carries a directional/indefinite commitment (finality frontier, irreversible promotion, proven-indefinite q=5) aligned with analyticity+graded and against positivity-truncation. BUT both arguments pass through the SAME two open joints: (i) the tangent-(9,5)->asymptotic-state Krein DESCENT (the field-space signature's descent to the asymptotic space where the ghost truncation actually lives -- 'stated as origin, not proven', W150 verdict D; = the W49/W121 C-operator derivation W133 already named as the only route to unconditional selection; UNGATED, program-internal, unproven), and (ii) the identification of the S-matrix/off-shell ghost structure with the record-finality SEMANTICS (TI/TaF-gated by the W150 one-way rule; the removal family's acausality is bounded and macroscopically emergent-causal per Grinstein-O'Connell-Wise, so a MACROSCOPIC finality DAG cannot exclude it -- only MICRO-causality does, and asserting micro-record-ordering as a GU theorem is a re-declaration in disguise). Because the forcing rests on those two open joints -- one ungated-but-unproven, one gated -- it is NOT OPERATIVE-grade. VERDICT: NARROWED, not POSITIVITY-FAMILY-FORCED, not GENUINELY-FREE-DECLARATION. bar (b) does NOT clear; UNCHANGED as a status (engine, re-posed), but the re-pose is SHARPENED from W133's 'free declaration' to a single named, program-internal, ungated descent question: does the tangent (9,5) q=5 Krein indefiniteness descend to the asymptotic state space, forbidding the removal-family truncation? No canon change."
grade: "EXACT for the reproduced W133 arithmetic (the jointly-unsatisfiable-axiom weights eps^2=+1 / eps=-1, the (9,5)=(3,1)+(6,4) split, the KL-deviation pairs (0,1)/(1,0)); NUMERICAL-CONTROLLED for the reproduced W133 CLOP-band dispersion residual selecting +1 (positive control PC1); STRUCTURAL/ARGUED for the two selection arguments and, decisively, for the identification of their shared joints as the honest ceiling; the two joints are cited from standing artifacts (firewall canon: (9,5) DECLARED INPUT, closed completion 'NOT proven impossible'; W150 verdict D: tangent->asymptotic descent 'stated as origin, not proven'; W150 one-way gate). The adversarial persona's re-declaration tripwire is enforced as a test guard (S3): no joint is asserted as proven, so no forcing is manufactured. Scalar core only (no spin-2 tensor numerators -- W124 Stage C / W134). Test: tests/W174_family_selection_gu_structure.py, 15/15, exit 0, positive controls PC1-PC3 (PC3 = eps=+1 negative control: no split). NO canon / RESEARCH-STATUS / claim-status / verdict / posture change. bar (b) UNCHANGED. H59 and H71 remain OPEN."
depends_on:
  - explorations/path2-wave5-evencut-family-discriminator-2026-07-14.md
  - explorations/W150-substrate-sweep-consensus-crypto-2026-07-14.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - explorations/W132-graded-optical-theorem-physical-subspace-2026-07-14.md
  - explorations/W131-covariant-operator-y14-2026-07-14.md
  - canon/firewall-boundary-hypothesis.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W174_family_selection_gu_structure.py
external_refs:
  - "Kallen (1952) / Lehmann (1954) -- the spectral representation whose two axioms (real-axis support, nonnegative weight) both families deviate from"
  - "Lee & Wick, Negative metric and the unitarity of the S-matrix, NPB 9 (1969) 209 -- the removal family"
  - "Coleman, Acausality, Erice 1969 -- acausal response of Lee-Wick theories"
  - "Grinstein, O'Connell & Wise, Causality as an emergent macroscopic phenomenon, PRD 79 (2009) 105019 -- the removal family's microcausality violation is bounded and macroscopically emergent-causal (the load-bearing GAP-2a)"
  - "Cutkosky, Landshoff, Olive & Polkinghorne, NPB 12 (1969) 281 -- the CLOP band whose endpoints are the two families"
  - "Bognar, Indefinite Inner Product Spaces, Springer 1974 -- maximal positive subspaces (the graded family's state-space datum)"
---

# W174 -- does GU's structure SELECT the positivity family, or is it a free declaration?

**Role.** W133 (`path2-wave5-evencut-family-discriminator`) closed the even-cut discriminator with
a sharp, symmetric verdict: the two consistent quantizations of GU's one ghost-carrying Lagrangian
are two INEQUIVALENT quantizations, each saturating exactly one of two JOINTLY-UNSATISFIABLE
Kallen-Lehmann axioms, and the two costs are EXACTLY SYMMETRIC -- KL-deviation pairs `(0,1)` (graded)
vs `(1,0)` (removal), each the full cut at its own locus. As physics-in-general the fork is a
PERMANENT DECLARATION: dispersion alone does not select, and the observable imprint is ~1e-61. W133
noted one asymmetry it did not develop: WITHIN GU the keep-and-grade datum is already declared, and
the ONLY visible route to an unconditional selection is a GU-internal derivation of the C-operator
from structure (W49/W121). W174 develops exactly that opening: does GU's SPECIFIC structure -- the
proven-indefinite `(9,5)` q=5 tangent Krein signature, the record substrate, the C-operator =
consistent-global-ledger (W150), the finality frontier / firewall -- SELECT the positivity/graded
family as FORCED (breaking W133's symmetry), or is any such selection a re-declaration in disguise?

Five personas inline, sequentially. Deterministic test: `tests/W174_family_selection_gu_structure.py`
(15/15, exit 0). Units `M = 1` where dimensionless; all cut statements in the fixed positive
normalization of W120/W124/W133. Binding: the W138 kill battery discipline (honest gating, one-way
TI/TaF rule); scalar core only.

## 0. Construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md discipline)

| Object | Constructions in play | Handling |
|---|---|---|
| **The negative-norm (ghost) sector** | (i) keep-and-grade (Krein C-operator, maximal positive subspace); (ii) remove/truncate (Lee-Wick pair contour) | BOTH priced; the question is whether GU structure excludes (ii) |
| **The (9,5) signature** | DECLARED INPUT (firewall canon), reconstruction-grade via Cl(9,5)=M(64,H) | Used as a declared premise; its declared-ness is GAP-1a, stated not hidden |
| **"Records are causally ordered"** | (i) MACROSCOPIC/emergent (GOW-compatible); (ii) MICRO-causal (axiom A at off-shell scale) | Only (ii) excludes removal; (ii) is TI/TaF-gated -- GAP-2b |
| **The finality frontier / firewall** | consensus-finality reading of firewall criterion 1 (W150 verdict D) | Conditional-register; the S-matrix<->finality identification is the gated joint |

## 1. Persona 1 -- S-matrix/dispersion theorist: the two-family map, reproduced

The object is W133's: the Lagrangian fixes the ghost residue sign `eps = -1`; Krein-weight
multiplicativity makes every even cut carry `eps^2 = +1` and every odd cut carry `eps = -1`, so the
two Kallen-Lehmann axioms

- **(A)** all absorptive content on the real axis (analyticity => microcausality), and
- **(P)** nonnegative spectral weight (positivity)

are **jointly unsatisfiable** (EXACT arithmetic; a normal residue `eps = +1` satisfies both). The two
families are the two ways to keep exactly one axiom:

| | keeps | pays | state-space datum |
|---|---|---|---|
| **Graded (keep-and-grade)** | (A): dispersion-complete, `N_A = 0` | (P): odd-cut leak, `N_P = 1` | Krein C-operator = maximal positive subspace |
| **Removal (Lee-Wick)** | (P): ghost-free spectrum, `N_P = 0` | (A): even-cut content off-axis, `N_A = 1` | truncated asymptotic space + pair contour |

Positive control PC1 reproduces W133's sum rule: among the CLOP band `{-1/2, 0, +1/2, +1}`, the
once-subtracted dispersion relation (given the agreed real part, agreed asymptotics, real-axis-only
cuts, Schwarz reality) selects `+1` -- the GRADED even-cut answer -- uniquely (residual `~3e-4` vs
`0.5-1.6` for the alternatives). PC2 reproduces the exact axiom arithmetic and the symmetric KL pairs
`(0,1)` / `(1,0)`. PC3 is the `eps=+1` negative control: both axioms hold, no split -- the whole
selection question tracks the Krein sign / the indefiniteness. **This is W133 in full; W174 adds
nothing here except the reproduction the brief required. The novelty is whether GU BREAKS the `(0,1)`
vs `(1,0)` symmetry.**

## 2. Persona 2 -- causality/microcausality specialist: does GU's finality forbid removal's ~1/m2 violation?

The removal family's price is precisely a bounded microcausality violation. Its even-cut absorptive
content is relocated to conjugate branch points OFF the real axis (W133 D4: the `(++)` pair channel
has a genuine discontinuity across a ray in the upper half plane, `|disc|` matching the continued cut
to 2%), i.e. it gives up upper-half-plane analyticity, which by the standard theorems IS
microcausality. The violation is bounded, timescale `~ 1/M` (`~1/m2`), and on GU's fixed-scale branch
`m2 ~ few meV` puts the smearing at `r* ~ 20-30 um`.

**The selection argument (ARG-2).** GU's records must be causally ordered (a finality DAG, an
irreversible promotion frontier). The graded family PRESERVES microcausality (`N_A = 0`); the removal
family violates it (`N_A = 1`). So record causal-ordering excludes removal and forces graded. `->`
OPERATIVE.

**Where it fails, honestly (GAP-2a, load-bearing).** The removal family's acausality is not gross: it
is *bounded* and *macroscopically emergent-causal* -- this is exactly the Grinstein-O'Connell-Wise
result, "Causality as an emergent macroscopic phenomenon." A finality DAG built from MACROSCOPIC
records cannot resolve a `~1/m2` (sub-mm) smearing; the check A5 exhibits the scale gap
(`r* ~ 50 um << ` any macroscopic record scale). So a MACROSCOPIC record-ordering requirement does
NOT exclude the removal family. Only a MICRO-causality requirement -- axiom (A) enforced at the
off-shell scale -- does.

**Where the remaining step is gated (GAP-2b).** Upgrading "records are causally ordered" from MACRO
to MICRO is a temporal-issuance / time-as-finality SEMANTIC statement about what finality means at the
off-shell scale. By the W150 one-way rule, GU may not import a TI/TaF result to force a GU-side
conclusion. Asserting "GU's records are MICRO-causally ordered, therefore removal is excluded" is
therefore a **re-declaration in disguise**, not a structural forcing. **ARG-2 does not force
unconditionally; it narrows.**

## 3. Persona 3 -- GU-structure specialist: does (9,5) q=5 / the firewall force graded? (ARG-1)

This is the stronger argument and the heart of W174. It does not go through causality at all; it goes
through the *indefiniteness* directly.

**The move.** The removal family's defining act is to TRUNCATE the negative-norm sector out of the
physical/asymptotic space, leaving a DEFINITE (positive) physical space. In signature terms the
physical space has effective `q = 0`. The graded family KEEPS the negative directions and grades them
via the C-operator, so the physical space `H_C+` is a PROPER maximal positive subspace of an
indefinite Krein space, with a permanent `q = 5` latent remainder.

**The record-substrate reading (W150 verdict D, reproduced as the reading, not re-derived).** Effective
`q = 0` `<=>` `H_C+` = the whole space `<=>` a CLOSED COMPLETION `<=>` the finality frontier is empty.
`q > 0` `<=>` `H_C+` proper `<=>` the frontier is never empty. So:

> **Removal family** `=` truncate `=` effective `q = 0` `=` **closed completion**.
> **Graded family** `=` keep-and-grade `=` `q = 5` latent `=` **frontier never empty**.

**The forcing.** GU's structure asserts (i) the `(9,5)` signature with `q = 5` indefinite (W131), and
(ii) firewall criterion 1 -- no closed completion (every reconstruction lands on a boundary). A closed
completion (`q = 0`) is exactly what firewall criterion 1 forbids. Therefore the removal family's
`q = 0` is EXCLUDED, and the graded family is FORCED. `->` OPERATIVE, and NOT via a gated semantic --
via GU's own proven signature.

**Why it is not closed, honestly (three named gaps, checks A1-A3).**

- **GAP-1a:** `(9,5)` is a **DECLARED INPUT** per the firewall canon ("canon already concedes (9,5)
  is a DECLARED INPUT"), not proven. The forcing inherits the declaration.
- **GAP-1b:** the firewall canon states plainly that a closed completion is "**obstructed for the
  routes tried, NOT proven impossible**" -- criterion 1 is the *live frontier*, not a theorem. So
  "closed completion forbidden" is itself a conjecture the argument leans on.
- **GAP-1c (decisive):** `(9,5)` is the **TANGENT / field-space** signature. The ghost truncation the
  removal family performs lives in the **asymptotic STATE (Krein) space**, an infinite-dimensional
  object. The descent of the tangent indefiniteness to the asymptotic-space indefiniteness is, in
  W150's own words, "stated as origin, not proven as a dimension count of the infinite-dim `H_C+`."
  If that descent fails -- if the asymptotic physical space is definite despite the tangent `(9,5)` --
  the removal family's truncation is perfectly fine and ARG-1 evaporates.

GAP-1c is the same object W133 named as the only route to unconditional selection: the GU-internal
C-operator derivation (W49/W121). **ARG-1 is a genuine, GU-native, UNGATED forcing that is not yet
closed because its central premise -- the tangent->asymptotic descent -- is unproven.**

## 4. Persona 4 -- symbolic engineer: the test

`tests/W174_family_selection_gu_structure.py` (15/15, exit 0). Positive controls PC1-PC3 reproduce
W133 (the CLOP-band residual selecting `+1`; the jointly-unsatisfiable weights and symmetric KL pairs;
the `eps=+1` negative control with no split). The selection bookkeeping encodes both arguments and
every gap as explicit checks: A1-A3 (ARG-1: the `(9,5)=(3,1)+(6,4)` split, truncation=closed
completion vs graded=frontier-never-empty, and the three named gaps making the forcing conditional);
A4-A6 (ARG-2: the `N_A` direction, the macroscopic-emergence scale gap GAP-2a, the one-way-gate
GAP-2b); S1-S3 (synthesis: symmetry broken, forcing not closed, and a **re-declaration tripwire** that
enforces the adversarial guard -- the test asserts that no joint is asserted as proven, so the file
cannot manufacture forcing); V1-V2 + H1 (verdict NARROWED, bar (b) unchanged, honesty guard). The
test is deterministic and reuses the exact W133 bubble/dispersion machinery for PC1.

## 5. Persona 5 -- adversarial skeptic: steelman GENUINELY-FREE, and where it holds

**Steelman.** "W133's `PERMANENT DECLARATION` stands. Both W174 arguments are the declared
keep-and-grade datum viewed twice. ARG-1 says 'GU is indefinite, so it must grade' -- but `(9,5)` is a
DECLARED INPUT and 'no closed completion' is an unproven live frontier, so this is 'GU declares
indefinite, therefore GU is graded', a tautology. ARG-2 says 'records are causal, so no removal' -- but
the removal family is macroscopically causal (GOW), so this needs MICRO-causality, which is a TI/TaF
semantic GU may not import. Strip both gated/unproven joints and you are left with W133 exactly:
keep-and-grade is DECLARED, dispersion makes it consistent, nothing FORCES it. GENUINELY-FREE."

**Where the steelman holds -- most of the way, and the verdict concedes it.** Neither argument reaches
OPERATIVE-grade forcing. ARG-1 rests on GAP-1c (the unproven descent), ARG-2 on GAP-2b (the gated
semantic). If you assert either joint as a GU theorem you have merely re-declared. The test's S3
tripwire is built precisely to stop that: no joint is asserted as proven anywhere in the file.

**Where the steelman overreaches, by exactly one step.** "GENUINELY-FREE / W133 unchanged" is too
strong. W133's headline symmetry -- `(0,1)` vs `(1,0)`, "neither cost is parametrically smaller,
therefore a free coin" -- is a statement about *physics-in-general*, a Lagrangian floating free of any
substrate. GU is not that. GU's substrate carries a **directional, indefinite commitment**: a finality
frontier that advances irreversibly, and a *proven-indefinite* `q=5` sector. Both W174 arguments,
independently, read that commitment as leaning on analyticity+graded and against positivity-truncation.
The coin is *weighted*. That is a real change from W133: within GU the two axioms are NOT
interchangeable, and the weighting is not a gated import -- it is the shape of GU's own (declared)
substrate. What is missing is not a *reason to prefer* graded (there are two, convergent); what is
missing is the *proof that the preference is forced* -- and that missing piece is a single, named,
program-internal, UNGATED object (GAP-1c = the C-operator descent). So the honest verdict is neither
FORCED nor FREE: **NARROWED**, with the residual gap named and located inside GU (not in experiment,
not in a gated repo).

## 6. Verdict

**W174 VERDICT: NARROWED.** (Not POSITIVITY-FAMILY-FORCED; not GENUINELY-FREE-DECLARATION.)

- **W133's symmetry is BROKEN at the framing level.** Two convergent GU-structural arguments -- ARG-1
  (removal-truncation = closed completion, forbidden by `(9,5)` q=5 / firewall criterion 1; graded =
  proper `H_C+`, frontier never empty) and ARG-2 (removal pays a bounded `~1/m2` microcausality
  violation, `N_A=1`; graded preserves microcausality, `N_A=0`) -- both lean on the positivity/
  keep-and-grade family and against removal. Within GU the two Kallen-Lehmann axioms are NOT
  interchangeable; the substrate's directional/indefinite commitment weights the coin. This is the
  genuine advance over W133's exactly-symmetric `(0,1)`/`(1,0)`.
- **The forcing is NOT closed.** Both arguments pass through the same two open joints: (i) the
  tangent-`(9,5)` -> asymptotic-state Krein DESCENT (ARG-1's GAP-1c; unproven, program-internal,
  UNGATED; = the W49/W121 C-operator derivation W133 named as the only route to unconditional
  selection), and (ii) the S-matrix/off-shell `<->` record-finality SEMANTIC identification (ARG-2's
  GAP-2b; TI/TaF-gated; the removal family's acausality is macroscopically emergent-causal per GOW, so
  only micro-causality excludes it, and asserting micro-record-ordering as a GU theorem is a
  re-declaration). ARG-1 additionally leans on GAP-1a (`(9,5)` declared) and GAP-1b (closed completion
  "not proven impossible").
- **Does GU's finality/record structure forbid the removal family's microcausality violation?** Not at
  the level GU can currently assert. It forbids MACROSCOPIC acausality, which the removal family does
  not exhibit (GOW: emergent macroscopic causality). Forbidding the removal family requires either
  MICRO-causality (gated) or the `(9,5)` descent (ARG-1, unproven). So: no unconditional exclusion --
  but a real structural lean, from two directions, toward exclusion.
- **Effect on bar (b).** b does NOT clear and is UNCHANGED as a status: the Krein grading is still
  engine, re-posed. But the re-pose is SHARPENED. W133 left it as "a free declaration, empirically a
  permanent declaration." W174 leaves it as: **one named, program-internal, ungated descent theorem
  away from forced** -- namely, does the tangent `(9,5)` q=5 Krein indefiniteness descend to the
  asymptotic state space, forbidding the removal-family truncation? That is a strictly narrower, more
  actionable frontier than "declaration," and it coincides with the standing C-operator-derivation
  frontier (W49/W121, W150 verdict D).

## 7. What this does NOT do

No spin-2 tensor numerators (W124 Stage C / W134 owns those; scalar core only). No claim that either
family is internally inconsistent (W133 stands: both are consistent quantizations). No claim of an
unconditional (structural or experimental) discriminator -- the honest answer is NARROWED, and the two
open joints are named and located. No assertion of either open joint as a GU theorem (the S3 tripwire
enforces this: doing so would be a re-declaration). No derivation of the C-operator from GU structure
(named as GAP-1c, the residual). No canon / RESEARCH-STATUS / claim-status / verdict / posture change.
**bar (b) UNCHANGED. H59 and H71 remain OPEN.**

**Artifacts:** this file + `tests/W174_family_selection_gu_structure.py` (15/15, exit 0).
