---
title: "PW2F-R2B2B2H4 source-to-active real-form port evidence-scope exit"
status: reconstruction
doc_type: exploration
updated_at: "2026-08-04"
run_id: RUN-20260804-092420-gu-formalization-pw2fr2b2b2h4-source-active-scope-exit
---

# PW2F-R2B2B2H4 source-to-active real-form port evidence-scope exit

## Question

H3 closed the local conditional active source-epsilon connection, curvature,
and operator orbit. Its exact remaining blocker was global: either construct
the public-source-to-active real-form bundle morphism/descent atlas or prove
the honest scope exit before assembling the separate quartic banks.

This swing asks:

> Does the published source provide the global real-form, transition,
> classifying, reduction, and active-bundle data required to identify its
> epsilon orbit with the conditional active `Spin(9,5)` right-H port?

The literal source-directed/Curt-rival `(7,7)` real comparator, public
mixed-sign complex `U(64,64)` container, possible moving-J
`Sp(32,32;H)` reduction, and active
`Spin(9,5)`/DeWitt/Clifford/soldered bundle are kept separate.

## Exact literal real-form obstruction

The literal comparator and active real metric carriers have different inertias:

```text
source: (7,7)
active: (9,5)
```

Sylvester inertia therefore forbids a real metric isometry. The real Clifford
and reality structures also differ:

```text
Cl(7,7) = M(128,R),  real module dimension 128,  R^2 = +1
Cl(9,5) = M(64,H),   real module dimension 256,  J^2 = -1.
```

For a real intertwiner `T R = J T`, applying the relation twice gives
`T = -T`, so `T=0`. The exact finite fixture independently solves the four
linear intertwiner equations and finds rank four with only the zero solution.

Both algebras complexify to `M(128,C)`. That common complex container is
genuine and useful, but it transports none of the literal inertia, reality
square, right-H module, active Clifford action, soldering, connection, or
domain data.
Complexification is therefore not the missing real-form port.

## The surviving two-stage construction opportunity

PW1 already gives the honest reverse interface. For

```text
G = U(64,64),  H = Sp(32,32;H),  dim(G/H) = 8128,
```

a source bundle first needs a global section `J` of its associated `G/H`
bundle, producing some `H`-reduction `P_H`. It then needs a separate
`H`-bundle isomorphism

```text
P_H -> P_nat
```

compatible with the active `Spin_0(9,5)` reduction, trace-reversed DeWitt
Clifford representation, soldering, and transition class. Odd Chern-class
vanishing on the Krein-sign subbundles is only a necessary first-stage screen,
not a sufficiency theorem.

This opportunity is not killed. It is a repository-built moving-J reduction
problem at reconstruction grade. The source epsilon may locally lift J, but a
local lift is not a global section and does not supply the stage-two active
identification.

## Source-evidence scope decision

The pinned primary-source packet explicitly marks the source `U(64,64)`
notation versus repository `Sp(32,32;H)` as a `REAL-FORM-FORK` and says the
source-to-repository real-form map must be constructed. It supplies no global
transition functions, classifying map, odd-Chern data, `P_H`, `P_nat`, bundle
morphism, or descent atlas. H3's local covariance calculation supplies none of
those global data either.

The exact disposition is therefore:

```text
PUBLIC_SOURCE_TO_ACTIVE_REAL_FORM_PORT_EVIDENCE_SCOPE_EXIT
INDEPENDENT_MOVING_J_PORT_OPEN
```

This is not `GLOBAL_PORT_NONEXISTENT`. It is the narrower finding that the
public source cannot support attribution of the required global port on the
available evidence. A future repository construction or new source-bearing
global packet could reopen the port.

## Downstream consequence

The H4 provenance/type blocker is closed by scope exit. Later construction may
now assemble the two banks only as separately tagged conditional-active
objects:

```text
I1 A4  !=  I2B C4
```

Neither bank is source-derived, neither is assembled here, and neither may be
merged. Multi-index Green/Helmholtz reduction, the live C3 return, and
projective `kappa1` classification remain downstream of the complete bank
assembly.

Earned result:

```text
PUBLIC_SOURCE_TO_ACTIVE_REAL_FORM_PORT_EVIDENCE_SCOPE_EXIT__INDEPENDENT_MOVING_J_PORT_OPEN__SEPARATE_CONDITIONAL_ACTIVE_BANKS_ADMITTED
```

## Campaign fences and next gate

P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`. `TG-1 AND TG-2 AND TG-3` remains
`NOT_PROMOTED`. The exact literal real-form obstruction strengthens only the
already-partial TG-1 evidence; it does not clear the unresolved source
convention, complete a Curt-specific dynamics, or create a same-domain
discriminator.

Resume at
`PW2F-R2B2B2I-SEPARATE-CONDITIONAL-ACTIVE-I1-A4-AND-I2B-C4-BANKS-THEN-MULTIINDEX-GREEN-HELMHOLTZ`.
Construct both complete banks separately before Green/Helmholtz reduction or
projective classification.
