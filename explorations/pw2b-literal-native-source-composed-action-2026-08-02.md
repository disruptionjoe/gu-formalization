---
title: "PW2B active-real-form bridge and source-orbit action-order comparator"
status: active_research
doc_type: construction_result
created: 2026-08-02
branch: agent/null-clifford-omega1-repair
run: system-runtime#meta/runs/RUN-20260802-170319-gu-formalization-pw2b-literal-native-action/run-plan.md
registry: lab/process/pw2b-literal-native-source-port.json
action_registry: lab/process/pw2b-source-composed-action-order-registry.json
probes:
  - tests/channel-swings/pw2b_literal_native_source_port_probe.py
  - tests/channel-swings/pw2b_source_composed_action_order_probe.py
grade: "PW2B LOCAL ACTIVE-REAL-FORM MEMBERSHIP AND MOVING-PROJECTOR PASS; ACTUAL PUBLIC-SOURCE BUNDLE PORT AND SOURCE-COORDINATE JACOBIAN OPEN; COMPATIBLE POINTWISE SOURCE-ORBIT FIXTURE MAKES VARYING-VARPI ORDER TWO ATTAINABLE WITH ONE GREEN LAYER; LITERAL DERIVED-K/MOVING-SHIAB COEFFICIENT AND FULL ROOT WARD OPEN. The distortion-derived grade-3/11 bridge lies in the established right-H/Krein/C-plus Sp word class, stabilizes the quaternionic reduction, and moves the separate Clifford projector with the exact differentiated-idempotence law. A block-unitriangular matrix is retained only as a structural comparator, not the actual source-coordinate inverse. The written one-half/one-third transgression obeys C=one-half(F_A+F_B)-one-sixth q(T,T). On a compatible pointwise curvature-orbit jet, conjugacy derives dK instead of adding an independent jet owner. An exact noncommutative identity-Shiab fixture varying varpi with epsilon and the metric fixed has density ceiling one, Euler ceiling two, an attained order-two coefficient, and one Green layer. It does not yet prove that K_full=h(T)^-1 D_B h(T) with every moving Shiab/Hodge/density slot has that nonzero coefficient. Shifting the same polynomial to free-epsilon jet ownership makes fourth order attainable with two Green layers, but the complete literal epsilon/metric coefficient, off-shell Ward/BV complex, covariant phase space, and analytic domain remain open. P1/P2/P3 remain unchanged and unused; Curt remains separate and no third lane is promoted."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# PW2B active-real-form bridge and source-orbit action comparator

## Result first

PW2B closes active-real-form membership and moving-projector gates that PW2A
left abstract, but it does not close the public-source bundle port.  On the
candidate active component, the existing
distortion bridge

\[
u(T)=c_3\,\operatorname{Alt}(\operatorname{pr}_2T)
     +c_{11}\,*\operatorname{Alt}(\operatorname{pr}_2T)
\in \Lambda^3\oplus\Lambda^{11}
\]

is in the already tested right-\(\mathbb H\), Krein, and \(C_+\) real-form
class.  Thus \(h=\exp u\) lies in the active symplectic group locally.  It
stabilizes the first, quaternionic reduction and moves the second,
Clifford-plane reduction:

\[
\widehat Q_{\rm Cl}=\operatorname{Ad}_{h^{-1}}Q_{\rm Cl},
\qquad
\widehat p=\operatorname{Ad}_{h^{-1}}p_2\operatorname{Ad}_h.
\]

The tangent projector obeys

\[
\dot p=[p_2,\operatorname{ad}_u],
\qquad
p_2\dot p+\dot p\,p_2=\dot p,
\qquad
\operatorname{tr}\dot p=0.
\]

A separate structural block-unitriangular comparator \(I+N\), with \(N^2=0\),
has inverse \(I-N\).  It is not the Jacobian of
\((\epsilon,\varpi)\mapsto(\epsilon e^{u(T)},\varpi)\).  The actual
source-coordinate inverse, public-source-to-active-bundle identification,
global atlas, holonomy descent, and domain preservation remain open.

The second result is a variational orbit-jet theorem, not yet the complete
literal action port.  The written source-curvature grammar is exactly

\[
\mathcal C(B,T)
=F_B+\frac12D_BT+\frac13q(T,T)
=\frac12(F_A+F_B)-\frac16q(T,T),
\qquad A=B+T.
\]

Here \(A:=B+T\) is the **repository algebraic endpoint** used to normalize
the transgression.  It is not thereby identified with the draft's separately
named \(A_\omega\); RB1 found no checked source locator for that equality.

The minus sign is load-bearing.  A plus-one-sixth version was proposed during
the pre-assessment and rejected against the executable repository convention.

For a candidate source composition

\[
\widehat B=B+K_{\rm full},
\qquad
\widehat T=T-K_{\rm full},
\qquad
\widehat A=A,
\qquad
F_{\widehat B}=\operatorname{Ad}_{h^{-1}}F_B,
\]

the first jet of \(K_{\rm full}\) must be determined by curvature conjugacy.  An
arbitrary \(dK\) keeps \(A\) fixed but leaves the source curvature orbit.  The
probe builds a compatible pointwise curvature-orbit jet with this property;
it does not prove that the jet is the image of the literal
\(K=h(T)^{-1}D_Bh(T)\) map on an atlas and domain.

## Plain English

The previous construction knew how to rotate the candidate Clifford geometry,
but had not shown that this rotation lived in the actual quaternionic/Krein
group used by the native branch.  PW2B now checks that locally.  The rotation
does not destroy the quaternionic structure; it moves the smaller Clifford
frame inside it.  A structural tangent comparator is reversible; reversibility
of the actual source-coordinate change remains open.

The transgression is then evaluated on an exact noncommutative identity-Shiab
orbit-jet fixture.  If one pretends that \(K\) and its derivative are
independent, the fixture looks higher-order.  On the compatible orbit jet, the
moved reference connection has conjugate curvature, so its derivative is
constrained.  In the sector that varies
\(\varpi\) while holding \(\epsilon\) and the metric fixed,
the exact fixture has a first-jet density, a second-order Euler coefficient,
and one boundary integration.  This makes order two attainable and supplies a
conditional ceiling when the literal derived \(K\) and moving contractions are
algebraic in the same jets.  The actual native coefficient remains open.

That does not make the entire theory second order.  Free \(\epsilon\) already
enters through a derivative, and the same exact polynomial then supports a
fourth-order Euler term and two Green layers.  The metric also moves the
Levi-Civita, Hodge, density, trace-reversal, and Shiab slots.  Those full
coefficients and the coupled Ward identity are PW2C.

## Layer 0

| object | type | PW2B disposition |
| --- | --- | --- |
| \((\epsilon,\varpi,g_X)\) | source roots | source explicit |
| \(B_\epsilon\) | gauge-rotated Levi-Civita connection | source explicit; not a frozen reductive projection |
| \(T_\epsilon\) | source distortion | source explicit |
| repository \(A:=B+T\) | algebraic transgression endpoint | fixed under the local candidate composition; not identified with draft \(A_\omega\) |
| source \(\epsilon\) | \(H\)-valued gauge coordinate | not the Clifford reduction by notation |
| \(J_{\mathbb H}\) | first reduction \(U/Sp\) | stabilized by the active \(Sp\) bridge |
| \(Q_{\rm Cl},p_2\) | second reduction \(Sp/Spin\) | transported by conjugation |
| \(u(T)\) | grade-3/11 Lie-algebra coordinate | repository-derived; source-silent |
| \(K_{\rm full}=h^{-1}D_Bh\) | full connection one-form difference in a literal candidate composition | not computed by the Delta-only return |
| \(K_{\rm red}=\operatorname{pr}_2K_{\rm full}\) | grade-two reduced Maurer--Cartan return | the object tested by the Delta/Hodge-null panel; not \(K_{\rm full}\) |
| curvature return | adjoint-valued two-form | not a connection one-form |
| exact Euler, \(\Upsilon\), \(\Xi\), Ward, BV | different variational objects | not identified |
| P1/P2/P3 | discrete orientation/channel/KO datum ledger | wrong type for this continuous port and unused |

TOE 00:20:51 and 00:26:28--00:29:16 support the trace-reversal requirement.
The exact symmetric-fibre inertia \((6,4)\) and active total signature
\((9,5)\) are repository-derived.  Raw Frobenius has \((7,3)\) and is an
executable planted failure.  The public source's \(U(64,64)\)/\((7,7)\)-type
presentation is not proved equivalent to the active induced \(P_{\rm mix}\),
\(Sp(32,32;\mathbb H)\), and \(\operatorname{Spin}_0(9,5)\) bundles.  Curt's
\((7,7)\) comparator is not substituted into this lane.

## Three specialist pre-assessments

1. The variational-bicomplex/PDE lens required separate derivative ledgers for
   varying-\(\varpi\) fixed-\(\epsilon\)/metric, free-\(\epsilon\), and metric
   sectors.  It predicted that the first could drop order while the literal
   moving-coefficient packet, complete root Ward, and domain remained open.
2. The Clifford/Spencer/real-form lens required literal right-\(\mathbb H\),
   Krein, and charge-reality tests, a moving-projector derivative, and a local
   inverse before calling the bridge a native port.  The first two passed;
   hostile review kept the actual source-coordinate Jacobian open and retained
   the block-unitriangular matrix only as a structural comparator.
3. The symplectic/reductive lens required a source-to-native action functor,
   not merely a representation inclusion, and deferred covariant phase space
   until the source-composed preboundary and Ward owners exist.

One normalization disagreement was resolved before construction.  Under the
repository's polarized convention \(q(A,A)=A\wedge A\), the correct identity
contains \(-\tfrac16q(T,T)\), not \(+\tfrac16q(T,T)\).

## Active-real-form membership and moving projector

The exact sparse-Clifford probe checks representative grade-three and
grade-eleven blades individually and their \((5/3,4/3)\) combination against
the established right-\(\mathbb H\), Krein, and \(C_+\) word identities.
It also evaluates \([p_2,\operatorname{ad}_u]\) on live grade-two and odd
samples: grade-two directions move out of the old carrier and an odd sample
returns into it.  Freezing \(p_2\) therefore fails the covariance test.

The five-pair coefficient panel is

\[
(0,0),\ (1,0),\ (5/3,4/3),\ (1,1),\ (1,-1).
\]

The pairs \((1,0)\) and \((5/3,4/3)\) have equal
\(\Delta=c_3^2-c_{11}^2\) and the same established linear-bridge grade-two
reduced Maurer--Cartan return \(K_{\rm red}\).  Both Hodge-null pairs have
\(\Delta=0\): \(K_{\rm red}\) vanishes while \(u\) remains nonzero and the
projector derivative is directly nonzero.  No vanishing of \(K_{\rm full}\)
is claimed.  The source selects neither coefficient
nor their ratio, and physical constraint surplus remains uncomputed.

## Source-composed action order

The exact noncommutative identity-Shiab fixture compares two pointwise jets
with the same fixed total connection:

* an arbitrary independent \(dK\); and
* the \(dK\) compatible with \(F_{B+K}=F_B\) at an identity source point.

The first leaves the reference-curvature orbit.  The second stays on it at
that point and removes the independent second-jet symbol.  It is not yet
shown to lie in the image of the literal grade-3/11 \(h(T)\) derivative, and
the moving Shiab/Hodge/density coefficients are not in this fixture.  With
\(\epsilon\) and the metric
fixed, let \(t_0\) be the varying \(\varpi\) field value and \(t_1\) its first
jet.  The constrained density
\(L(t_0,t_1)\) has

\[
E_{\varpi}=\frac{\partial L}{\partial t_0}
-D\frac{\partial L}{\partial t_1},
\]

with a live \(t_2\) coefficient and no \(t_3\) or \(t_4\).  Its first Green
identity is exact.  This is an attainability result and conditional order
ceiling, not the literal native principal coefficient.

For free source \(\epsilon\), reinterpret \(t_0=\epsilon_1\) and
\(t_1=\epsilon_2\).  The exact Hessian
\(\partial^2L/\partial t_1^2\) is nonzero, so

\[
E_\epsilon=-D\frac{\partial L}{\partial t_0}
+D^2\frac{\partial L}{\partial t_1}
\]

reaches \(\epsilon_4\).  The two-layer Green identity also closes in the
fixture.  This proves attainability after the ownership shift, not that every
moving native coefficient contributes that principal term.

## Source collision

**SOURCE-CONFIRMS:** the draft owns \((\epsilon,\varpi)\),
\(B_\epsilon\), \(T_\epsilon\), and the one-half/one-third grammar.  TOE
02:19:17--02:20:33 owns the gauge-rotated Levi-Civita and double-coset
continuation.  TOE 00:20:51 and 00:26:28--00:29:16 own the trace-reversal
requirement; the exact \((6,4)/(9,5)\) real form is repository-derived.

**SOURCE-CORRECTS:** \(B_\epsilon\) is the full gauge-rotated connection,
not a frozen reductive projection.  Shiab is a covariant contraction, not the
projector used to define the moving Clifford reduction.  Field pullback is
not an unspecified pushdown of the Euler covector.  The repository algebraic
endpoint \(A:=B+T\) is not silently promoted to the draft's \(A_\omega\).

**SOURCE-SILENT / REPOSITORY-DERIVED:** the grade-3/11 bridge and coefficients,
the identification of the public \(U/(7,7)\)-type source presentation with
active \(P_{\rm mix}/Sp/Spin(9,5)\), the moved Clifford projector, the
multigraded order comparator, and the missing complete Ward/BV/domain packet.

## Hostile specialist post-review

Three lenses reviewed the completed packet: source/Layer-0/reductive geometry,
variational Euler/Green/Ward ownership, and evidence/campaign/inventory.  The
review materially narrowed the result before acceptance:

- “fixed varpi” was corrected to varying \(\varpi\) with \(\epsilon\) and the
  metric fixed;
- full \(K_{\rm full}\) was separated from Delta-only grade-two
  \(K_{\rm red}\);
- Hodge-null claims were limited to \(K_{\rm red}\), and direct live
  projector motion was added;
- the unitriangular matrix was downgraded to a structural comparator, with
  the actual source-coordinate Jacobian left open;
- the action result was downgraded to compatible pointwise identity-Shiab
  orbit-jet attainability/conditional ceiling;
- \(A:=B+T\) was separated from the draft's unchecked \(A_\omega\);
- the public-source/active-real-form bundle fork and precise trace-reversal
  attribution were restored.

After repair, all three verdicts pass.  The active-bridge probe passes
`22 exact + 12 type + 4 source + 7 planted = 45`; the action probe passes
`16 exact + 13 type + 3 source + 8 planted = 40`; the campaign scaffold passes
`50 exact + 14 planted = 64`.  PW2A regressions, strict JSON/AST, the 204-probe
inventory, and diff checks also pass.

## Boundary and next gate

PW2B does not enable PW3.  The next highest-information construction is
`PW2C-LITERAL-DERIVED-K-MOVING-SHIAB-FREE-EPSILON-METRIC-WARD`:

1. derive the literal \(K=h(T)^{-1}D_Bh(T)\) jet from the grade-3/11 bridge
   and insert it into every moving Shiab/Hodge/density/Krein slot;
2. evaluate every free-\(\epsilon\) and metric/Levi-Civita/trace-reversal
   derivative and determine the actual principal coefficients;
3. derive the full off-shell source Ward identity and its Green current;
4. construct the resulting BV tangent complex and covariant phase space;
5. only then ask whether a closed right-\(\mathbb H\)/Krein domain exists.

No global source equivalence, source-selected coefficient, positive constraint
surplus, Standard Model recovery, generation count, quantum theory, or
cosmological prediction is claimed.
