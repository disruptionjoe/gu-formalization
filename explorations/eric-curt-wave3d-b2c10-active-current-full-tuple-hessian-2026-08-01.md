---
title: "Eric/Curt Wave 3D-B2C10 — Active Source Current, Graph Owner, and Full-Tuple Hessian"
status: active-research
doc_type: exploration
updated_at: "2026-08-01"
---

# Eric/Curt Wave 3D-B2C10 — Active Source Current, Graph Owner, and Full-Tuple Hessian

## Result

B2C10 resolves B2C9's first active discriminator and narrows the source-action
fork.

The semisimple nonzero-southeast repair contributes a genuine extra
connection-one-form current on the active trace-reversed

\[
(3,1)+(6,4)=(9,5),\qquad \mathrm{Cl}(9,5)=M(64,\mathbb H)
\]

carrier.  The channel survives all three algebraic reality conditions:
right-\(\mathbb H\), Krein, and `C+`.  It also survives the elementary
symplectic-real doublet restriction: the right-\(\mathbb H\) partner
reinforces rather than cancels it.  Setting \(\zeta=0\) kills every bilinear
in the written draft current while leaving this channel nonzero.  Therefore
the candidate found in B2C9 is not an artifact of the scalarized comparator:

\[
\boxed{J_{\rm SE}\sim \ell\,\bar\nu\gamma^a\rho(\delta B_a)\nu\ne0.}
\]

This gives a conditional incompatibility with the draft's three-monomial
current on the **shared-connection** reading.  It is not a no-go against every
two-connection or cyclic completion.

A nonzero natural zero-order seed exists algebraically.  If
\(T=A-B\in\Omega^1(\operatorname{ad}P)\), then

\[
M_{00}(T)=c(T)L
\]

passes the same right-\(\mathbb H\), Krein, and `C+` action-reality tests on
the aligned active witness.  A hostile misaligned component fails before
reality completion, so this is an existence result rather than a universal
raw-\(c(T)\) theorem.
The natural injection

\[
M_{10}(T)\nu=(T_a\nu)_a
\]

also survives, provided \(M_{01}\) is its forced Krein adjoint.  One cannot
select only one off-diagonal block.

But these admissible \(T\)-linear terms do **not** solve the current mismatch.
For any coefficient \(q\), their connection returns have the form

\[
J_A^{(T)}=qJ_T,\qquad J_B^{(T)}=-qJ_T.
\]

Consequently

\[
\delta A=\delta B
\quad\Longrightarrow\quad
J_A^{(T)}+J_B^{(T)}=0.
\]

The southeast current is unchanged for every tested coefficient and, by the
linearity of \(T=A-B\), for the whole \(T\)-only family.  At coefficient one
the term can transfer the extra current from the \(B\) owner to the \(A\)
owner, but cannot delete it.  This is a useful constructive result: the
natural distortion block is real and correctly typed, yet it forces the next
question to be **connection ownership**, not coefficient fitting.

B2C10 also constructs two owner returns that B2C9 left formal.

1. On the observed Lorentz four-plane, the linearized torsion-free
   Levi--Civita spin lift lands exactly in the active connection algebra.
   Returning the active current through this map and primalizing with the
   trace-reversed `Sym2` DeWitt form gives the same number on both sides of the
   adjoint identity.  The fibre form has the required inertia `(6,4)`, not an
   ordinary positive Frobenius signature.
2. For
   \(B=u\omega_{\rm LC}u^{-1}-u_au^{-1}\), the reduction tangent and its
   transpose agree, including the value and first-jet \(u,u_a\) owners.  The
   induced local law contains the required \(-d\chi\) term.

These are an actual normal-frame observation-slice LC map and a genuine local
reduction graph.  They are not yet the nonlinear ambient \(Y^{14}\) LC graph,
its moving Hodge operator, or the complete induced chimeric projectors.

Finally, an exact rational full-tuple action comparator includes an independent
connection \(A\), a graph-derived connection \(B(u,u_1,g_1)\), physical
\(\zeta/\nu\) fields and their first jets, barred variables, a moving density,
moving lowerer and coefficients, and a \(T\)-linear zero-order term.  It
satisfies

\[
G(\phi)R_\chi(\phi)=0
\]

and the differentiated identity

\[
\boxed{H_\phi(v,R_\chi)+(D R_\chi[v])^!G(\phi)=0}
\]

exactly, with the two terms separately nonzero:

```text
H(v,R) = -736/9
G(DR[v]) = 736/9
```

Freezing the reduction owner breaks the Ward identity.  Treating the isolated
Hessian as a gauge kernel, or omitting the derivative of the field-dependent
generator, also fails.  A separate mixed derivative verifies that the moving
density/lowerer/coefficient packet is not inert bookkeeping.

The symmetrized finite boson-plus-fermion Green comparator is skew and
nonzero.  Every tested zero-order \(M_0\) coefficient leaves it unchanged, as
it must: a zero-order block alters Euler currents but not the principal
boundary form.  Therefore adding \(M_0\) does not repair the B2C5 result that
the positive spectral half is not automatically Green-isotropic.  No common
closed physical domain is selected here.

The probe passes

```text
25 computational exact + 5 source receipts + 12 type-level + 15 planted = 57 PASS
```

No P1, P2, or P3 datum is used or changed.

## Layer 0 — the objects being compared

| term | object in B2C10 | not identified with |
|---|---|---|
| connection-shaped contraction | equivariant curvature-to-one-form Euler target described by Weinstein | a connection coordinate or scalar stress |
| active fermion current | connection Euler covector restricted to `sp(32,32;H)` tangents | observed Maxwell/Yang--Mills current |
| `bar-nu nu` channel | southeast principal connection variation | a Majorana mass or generation count |
| `M00=c(T)L` | zero-order distortion-dependent operator | the first-order southeast principal block |
| right-`H` doublet control | coefficient-level symplectic-real restriction | complete Grassmann/BV integration theory |
| LC owner return | observed normal-frame linearized torsion-free spin lift | nonlinear ambient `Y14` LC theorem |
| trace-reversed DeWitt form | raw Lorentz `Sym2` Frobenius paired after trace reversal, inertia `(6,4)` | positive Frobenius norm |
| full-tuple Hessian | exact finite graph-owned differentiated Ward comparator | final active GU Hessian or odd super-IG symmetry |
| Green current | principal integration-by-parts boundary form | a domain, Poisson inverse, or prequantum lift |

The key semantic point is that Eric's connection-shaped contraction types the
Euler target.  It does not say that an Euler covector is literally the
connection variable, and it does not identify a fermion connection current
with a four-dimensional observed force current.

## Primary-source collision

### `SOURCE-CONFIRMS`

- TOE official/local `01:34:49--01:35:13` / `01:36:35--01:36:56` says the
  relevant curvature map is a **contraction**, not a projection, and is
  connection/one-form-shaped.
- Draft equation `(9.18)` gives the bilinear current alphabet
  \(\bar\nu\zeta+\bar\zeta\nu+\mathscr S_\omega\bar\zeta\zeta\).
- The 2021 draft permits versions with a nonzero southeast map.
- TOE `02:44:06--02:45:13` announces a two-connection on-shell mechanism but
  says it has not been released.

### `SOURCE-CORRECTS`

- The active nonzero-southeast repair is a source-admitted repository
  construction, not the modern source-selected rolled corner.  TOE local
  `02:42:55` and the UCSD record at `00:36:13` retain a zero southeast corner.
- The natural \(T\)-linear block is not a magic current cancellation.  It
  changes owner allocation and is invisible to a shared displacement.
- “Connection-shaped” does not license replacing a connection Euler covector
  by a connection coordinate.

### `SOURCE-SILENT`

The checked sources do not provide:

- the active projection of the current;
- the coefficient or owner law canceling the new channel;
- a complete natural equivariant \(M_0\) basis;
- the nonlinear ambient LC/reduction/Hodge/density/projector response;
- the constrained Grassmann/BV action and full active Hessian;
- a common Green domain, BFV quotient, Poisson anchor, or prequantum lift.

The active-current, owner-return, and finite Ward results here are therefore
repository constructions, not recovered Weinstein formulas.

## L1 — active current and reality

For the deterministic active tangent

\[
X=\gamma_0\gamma_1,
\]

the probe verifies

\[
XJ=J\bar X,
\qquad \beta X+X^\dagger\beta=0,
\qquad X^TC_+ + C_+X=0.
\]

With the B2C5 repaired coefficients

\[
L=-\frac{11}{16}P_+-\frac{33}{32}P_-,
\]

the current on the observed `(0,1,2,9)` directions is nonzero for both a
generic field and its \(J\)-paired doublet.  On the fixture the doublet is
exactly twice the single-field value.  This is a nonvanishing witness, not a
claim that these four numbers are physical currents or observables.

The natural \(T\)-linear seeds tested are

\[
M_{00}=c(T)L,
\qquad
M_{10}:\nu\mapsto(T_a\nu)_a,
\qquad
M_{01}=M_{10}^{\ddagger}.
\]

The displayed aligned `M00` witness and the forced off-diagonal pair pass the
active algebraic gates.  A misaligned raw `c(T)L` component fails, so a
universal tensor formula still needs its action-real projection and
equivariance theorem.  Completeness of the natural basis is open.  A
contracted-curvature one-form may enter the same templates, but its
connection variation is differential and cannot be equated to the algebraic
southeast current without the missing integration-by-parts and owner map.

## L2/L3 — graph and symmetry

The observed LC test uses the actual normal-frame linearized formula

\[
\omega_{\mu ab}
=\frac12(\partial_bh_{\mu a}-\partial_ah_{\mu b}),
\qquad
\rho(\omega_\mu)=\frac14\omega_{\mu ab}\gamma^a\gamma^b.
\]

The trace-reversed fibre pairing is

\[
\langle h,k\rangle_{\rm TR}
=\operatorname{tr}(\eta h\eta k)
-\frac12\operatorname{tr}(\eta h)\operatorname{tr}(\eta k),
\]

with exact numerical spectrum

```text
-2,-2,-2,-1,+1,+1,+1,+2,+2,+2.
```

This is the remembered Frobenius-fibre trace reversal and is load-bearing in
the returned metric owner.

The finite full-tuple action then supplies the ordinary local-gauge chain rule.
It is deliberately an ordinary-gauge test.  It neither constructs nor assumes
the open local odd super-IG symmetry rejected conditionally in B2C6.

## L4 — dynamics and the construction fork

The shared-connection current mismatch is now real.  There are four honest
construction routes:

1. **two connection owners:** keep the extra current but show the source
   equation reads the correct owner combination rather than \(J_A+J_B\);
2. **source-current correction:** derive an additional active
   \(\bar\nu\nu\) term from the selected action rather than appending it;
3. **different semisimple operator:** find a source-compatible semisimple
   repair whose active shared current retains the written alphabet;
4. **different constrained complex:** justify a quotient/restriction that
   removes the channel while preserving observation and the other legs.

Route 1 has the highest immediate information value because Weinstein has
announced, but not released, a two-connection on-shell construction and the
natural \(T\)-block already proves that current ownership is mathematically
live.  The next swing should build and compare the two rival draft coupling
architectures before final domain work.

## L5/L6/L7 and boundary

- **L5 quantum:** right-\(\mathbb H\)/`C+` compatibility survives; no Hilbert
  space, propagator, common closed domain, BV quotient, or quantization follows.
- **L6 physics:** the object is typed as a connection current and returns to a
  trace-reversed metric owner.  No observed Maxwell current, Yukawa coupling,
  mass, count, cosmological amplitude, or prediction is claimed.
- **L7 sourcing:** all constructions and source timestamps are explicit.
  The transcript supplies the target grammar, not the missing formula.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.  `TG-1 AND TG-2 AND TG-3`
remains false, so no third lane is promoted.

## Next gate

Run

```text
ECW3D-B2C11-TWO-CONNECTION-CURRENT-OWNERSHIP-AND-SOURCE-ACTION-SELECTION
```

Construct both draft-admitted coupling architectures with explicit \(A/B\)
owners, determine which connection-one-form combination the bosonic Euler
target sees, and test whether the active \(\bar\nu\nu\) channel is canceled,
transferred, or required.  A successful candidate must derive its coefficient,
preserve the full-tuple Ward identity and the Standard Model/gravity/quantum/
cosmology legs, and consume no P1/P2/P3 datum.

Only after that owner/action selection should
`ECW3D-B2C6B-FINAL-TOTAL-ACTION-COMMON-GREEN-DOMAIN-REPLAY` run on the actually
selected principal action.  The present preliminary replay proves only that a
zero-order \(M_0\) does not change the Green obstruction.
