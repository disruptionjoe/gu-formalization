---
artifact_type: exploration
status: exploration
doc_type: construction_result
created: 2026-08-24
work_item: CANON-SCOPE-INDEPENDENT-VERIFICATION
target_claim: "INTERNAL — two canon scope clauses: chirality-half nullity across p+q=14 signatures, and the inference from bare same-chirality scalar Hom-vanishing to insertion-dependent self-mass absence."
target_claim_verdict: "TWO SCOPE NARROWINGS CONFIRMED. The chirality halves are totally null at the physical odd-q horns (9,5) and (7,7), but definite at the q=0 Euclidean control; the governing exact relation is {K,chi}=0 iff q is odd. The Shiab/MOVE-4 vanishing binds Hom(S+ tensor S+, Lambda0) at zero insertion only; it does not bind Hom(S+ tensor S+ tensor T2, Lambda0), whose Z/4 obstruction is removed by one class-2 insertion. No action term, reality map, scale, mass or spectrum is constructed."
grade: "EXACT structural parity and Z/4 arithmetic with typed Hom-domain separation; independent of the predecessor numerical Clifford sweep and D7 decomposition code. Cross-checked against both predecessor live probes and hostile selftests."
canon_verdict_change: none
canonical_effect: scoped_wording_integrated
priority_change: none
rows_advanced: 0
free_object_delta: 0
scripts:
  - tests/channel-swings/canon_scope_independent_verification_probe.py
---

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
> Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY` — the exact representation-
> theoretic scope corrections bind the repository's internal carriers; the
> words “Majorana” and “self-mass” remain comparator taxonomy and supply no
> source-native mechanism.

# Independent verification of two canon scope boundaries

## Result

Two due upgrade-register findings survive a genuinely different verification
route and are now safe to integrate as scope corrections.

First, let `chi` be the fourteen-dimensional chirality operator and let
`beta_S` be the product of the `p=14-q` spacelike gamma matrices used by the
canon Krein convention. Because `chi` anticommutes with every gamma,

```text
chi beta_S = (-1)^p beta_S chi = (-1)^q beta_S chi.
```

Therefore `{K,chi}=0` exactly when `q` is odd. On a same-chirality pair the
Krein form then vanishes, so the halves are totally null at `(9,5)` and
`(7,7)`. At `(14,0)`, `beta_S` is proportional to `chi`; the two halves are
definite `(+96,0,0)` and `(0,-96,0)`. The full triplet still has total
signature `(+96,-96,0)`, so only the half-nullity scope changes. No physical-
horn conclusion moves.

Second, MOVE-4 proves the bare statement

```text
Hom_Spin(S+ tensor S+, Lambda0) = 0.
```

An insertion-dependent pairing asks a different typed question,

```text
Hom_Spin(S+ tensor S+ tensor T2, Lambda0),
```

where `T2` has `Z/4` class 2. Exact class arithmetic gives `3+3=2 mod 4`
bare and `3+3+2=0 mod 4` after one insertion; two insertions restore class 2.
Thus the bare vanishing never licensed a universal absence claim for the
inserted domain. ST-1's separate exact decomposition confirms nonzero shapes
in that allowed one-insertion domain. This is only carrier availability. The
source action must still select an operative term and supply the reality map,
scale, domain and physical spectrum.

## Independent route and controls

The new probe imports neither predecessor instrument. It freezes their
artifacts by SHA-256, derives the parity and class statements symbolically,
distinguishes the two Hom domains, checks the proposed wording across every
canon carrier, and catches eight targeted mutations after a clean baseline.
The original LDB and ST-1 live probes and their baseline-first hostile
selftests are rerun separately as reproduction controls.

The strongest overclaim would be “ST-1 constructs a GU Majorana mass.” It does
not: it constructs invariant-shape availability after a declared class-2
insertion. The strongest contrary concern is that the correction weakens the
physical Krein horn. It does not: both `(9,5)` and `(7,7)` have odd `q`, so the
null-half and cross-pairing results remain exact there. The weakest seam is
source/action selection, which remains explicitly open rather than inferred
from a nonzero Hom space.

```gu-typed-objects
result: odd-q criterion for chirality-half nullity and the q=0 Euclidean control
carrier: the fixed 192-dimensional self-dual triplet inside ker(Gamma) in V tensor S LAYER=ambient CHIRALITY=S-HALF-OPPOSITE
pairing: K = eta_V tensor beta_S ON=the-fixed-192-dimensional-triplet, beta_S the product of spacelike gammas
real_structure: Spin(p,q), p+q=14; physical horns (9,5) and (7,7), control (14,0)
grading: Z/2 chirality generated by chi
action_owner: repository-construction
target: the two 96-dimensional chirality halves MAP-TYPE=restriction
```

```gu-typed-objects
result: bare zero-insertion scalar-Hom vanishing does not bind the class-2 inserted Hom domain
carrier: S+ tensor S+ bare, versus S+ tensor S+ tensor T2 after one declared class-2 insertion LAYER=ambient CHIRALITY=S-HALF-SAME
pairing: invariant scalar Hom into Lambda0 ON=the-bare-or-inserted-same-chirality-domain; nonzero inserted shapes are availability only
real_structure: complexified D7 class arithmetic with the Cl(9,5) bare result retained at its exact real scope
grading: Z/4 center class, cls(S+)=3 and cls(T2)=2
action_owner: source-action
target: an operative same-corner self-pairing term MAP-TYPE=homomorphism; selection, reality, scale and spectrum remain open
```

## Exact bounded consequence

The canon repair changes no verdict. It removes one even-`q` scope overreach
and one bare-to-inserted domain overreach. Ledger v0.263, all 91 terminal
dispositions, source ownership, predictions, confirmations, agenda priority,
papers and public posture remain unchanged.
