---
title: "MORE-PREDICTIONS hunt: enumerating further sigma/tau-forced OBSERVABLE signs in UN-WIRED domains, to manufacture consilience beyond the one dark-energy leg. Adversarial on FORCED-vs-FITS and OBSERVABLE-vs-INTERNAL and WIRED-vs-UN-WIRED. VERDICT: the hunt RE-CONFIRMS the parsimony ledger from the prediction side -- of ~9 candidate domains, ZERO yield a clean forced+observable+strictly-un-wired NEW prediction, and MOST collapse to FITS / not-forced / internal / already-wired-and-packaged. The single genuine NEW payoff is ONE previously-unenumerated OBSERVABLE window: because sigma is now identified (proposal grade) as a w1 REFLECTION / TIME-REVERSAL 't Hooft anomaly (anomaly-inflow-swing + pin-bordism-cardinality, Omega^{Pin+}_14), a PARITY/T anomaly generically forces a parity-ODD cosmological response that LCDM forbids (CMB TB/EB cross-correlation / cosmic birefringence / circularly-polarized primordial GW). This is technically a SECOND OBSERVABLE READOUT of the same sigma (so 'wired' in the strict Krein sense), but it lands in a PREVIOUSLY-UNCONNECTED empirical domain (photon/GW parity, not the theta-sector DE background), so it converts the sigma cluster from ONE observable leg to TWO -- which is exactly the consilience the council asked to manufacture, IF the unbuilt descent from the fiber w1 anomaly to a spacetime parity-odd term (a gravitational theta / Chern-Simons coefficient with definite sign) is constructed. Honest grade: FORCED only at the proposal grade of the anomaly ID + one unbuilt descent map; observable NOW (birefringence beta is measured, ~3-sigma isotropic hint); a genuinely new data window the repo has not packaged. All other candidates -- neutrino Majorana/Dirac, protected {q=0} edge mode, DE w(z) shape, arrow-of-time, tau flavor/mixing, monopoles/theta_QCD -- are graded FITS or not-forced or internal or WIRED, explicitly, no inflation."
status: active_research
doc_type: exploration
created: 2026-07-21
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
directed_by: "Joe direct chat, 2026-07-21 (hunt for MORE observable predictions in un-wired domains; one synchronous pass, read-only)"
inputs:
  - explorations/anomaly-inflow-swing-2026-07-21.md
  - explorations/pin-bordism-cardinality-2026-07-21.md
  - explorations/parsimony-unexplained-joints-ledger-2026-07-21.md
  - explorations/per-leg-recovery-state-2026-07-21.md
  - explorations/blockbuster-p1-de-sign-covariance-2026-07-19.md
  - explorations/lp-lc-deficiency-decisive-2026-07-21.md
  - explorations/wave-swing3-the-outside-2026-07-21.md
  - explorations/decision-tree-Q3-one-anchor-vs-two-2026-07-21.md
  - explorations/three-seam-prongB-no-fourth-generation-2026-07-21.md
  - explorations/global-anomaly-leg-2026-07-20.md
  - explorations/prediction-package-pp2-matter-parity-stable-state-2026-07-19.md
  - explorations/prediction-package-pp3-de-curve-family-2026-07-20.md
  - explorations/mirror-sector-prediction-candidate-disposition-2026-07-21.md
probe: none (deliberate; see Section 6 -- the load-bearing uncertainty is the unbuilt fiber->spacetime descent, not probe-checkable at this grade; the Pin-table controls this hunt would rest on already exist and pass in tests/channel-swings/pin_bordism_cardinality_probe.py, 35/35)
---

# MORE-PREDICTIONS hunt -- un-wired sigma/tau-forced observables

Adversarial, read-only, one synchronous pass. The charge (from the parsimony
ledger): GU currently rides on **ONE** observable -- the dark-energy
no-phantom-crossing (`w >= -1`, PP1). Manufacture consilience by finding further
`sigma`- or `tau`-forced signs in **UN-WIRED** domains: predictions GU **forces**
(not merely fits), that are **observable**, and that are **NOT already wired to
the same Krein-sign co-flip** (so they would be genuine independent evidence, not
internal unification). Discipline: **do not inflate a fit into a prediction.** Most
candidates will fail on one of the three axes; say so.

## 0. The bar, and the WIRED baseline that defines "un-wired"

A prediction is **GOLD** only if it is **FORCED** (by banked GU structure, not a
free fit) **AND OBSERVABLE** (exposed to data, can come back wrong) **AND UN-WIRED**
(not just another readout of the one `sigma` co-flip already carrying PP1). Three
tags per candidate: `{FORCED | FITS | OPEN}`, `{OBSERVABLE-now |
observable-in-principle | not-observable}`, `{UN-WIRED | WIRED}`.

**What `sigma` is already wired to (the baseline -- these are NOT new).** `sigma`
= `w1` = the reflection / time-orientation `Z/2`, doing **quadruple duty** by the
construction axiom SRC-COH-1: the QM Krein sign = the GR vacuum-cancellation sign
= the DE sign = the record/time arrow, co-flip **audited to be ONE bit**
(per-leg-recovery leg 10; parsimony Sec 3). Of those four, **exactly ONE is
observable** (the DE sign, PP1); the other three are **INTERNAL-only** unification.
So the following are, by definition, **WIRED** and yield no new consilience:

- the DE sign / no-phantom-crossing itself (PP1) -- the one leg we already have;
- the DE `w(z)` **shape** / thawing curve family (PP3) -- same theta-sector, WIRED;
- the arrow of time (CH-REC co-flip) -- WIRED and, moreover, not independently
  observable (we cannot measure a second, arrow-reversed universe);
- the GR vacuum-cancellation sign and the QM Krein superselection sign -- WIRED and
  INTERNAL-only.

**The two structural facts that make un-wired hunting POSSIBLE.** (1)
`Q3-TWO-INDEPENDENT`: the generation sector (`tau`, `Z/3`, and its deck-Z/2) is a
**different external datum** from `sigma`; the generation **count does not depend on
`sigma`**, and the **DE sign does not depend on the generation deck-bit**. So
anything genuinely forced in the `tau` sector would be **strictly un-wired** from
the DE sign. (2) The quaternionic **Kramers** structure (`S = H^64`, `J^2 = -1`,
even index) is **explicitly NOT `sigma`** -- it is the thing that KILLS the mod-2
index anomaly, disambiguated from `sigma = w1` in anomaly-inflow-swing. So anything
forced by the Kramers/quaternionic structure is also structurally distinct from the
DE-sign bit. These two are the only genuinely `sigma`-independent forcing sources in
the committed inventory, so the hunt concentrates there -- and, adversarially, finds
that neither currently forces a clean observable.

**The NEW structural input this hunt exploits (why it is worth running now).** As of
2026-07-21, `sigma` is no longer just "the Krein orientation": anomaly-inflow-swing +
pin-bordism-cardinality identify it (PROPOSAL grade) as a genuine **'t Hooft anomaly
of the `w1` REFLECTION / TIME-REVERSAL `Z/2`**, receptacle `Omega^{Pin+}_14`,
cardinality 1, PROTECTED. A reflection/T anomaly is a **parity/T** object -- and
parity/T anomalies have observable faces (parity-odd correlators) in domains that
have nothing to do with the DE background. That is the one seam this hunt opens.

---

## 1. Candidate table (all domains, honestly tagged)

| # | Candidate (domain) | Forcing source | FORCED / FITS / OPEN | Observable? | WIRED / UN-WIRED | Concrete observable + confirm/refute |
|---|---|---|---|---|---|---|
| **A** | **Cosmological PARITY violation** (CMB TB/EB, cosmic birefringence `beta`, circularly-polarized primordial GW) | `sigma` = `w1` **reflection/T 't Hooft anomaly** (anomaly-inflow-swing; `Omega^{Pin+}_14`) | **FORCED at proposal grade** (a genuine reflection/T anomaly generically forbids `TB=EB=0`; but the anomaly ID is proposal-grade AND the fiber->spacetime descent is unbuilt) | **OBSERVABLE-now** (birefringence `beta` is measured; ~3-sigma isotropic hint exists; TB/EB and GW circular polarization are live CMB/GW targets) | **WIRED to `sigma`, but a SECOND OBSERVABLE readout in a PREVIOUSLY-UNCONNECTED domain** (photon/GW parity, not the theta DE background) | A nonzero parity-odd 2-pt function (`beta != 0`, `TB/EB != 0`) with a **sign set by `sigma`** and hence **correlated with the DE-sign side**; refute = parity-odd correlators pinned to zero at the sensitivity where the anomaly predicts them, OR a `beta` sign anti-correlated with the DE side. **This is the one gold-adjacent item.** |
| **B** | **Neutrino Majorana vs Dirac** (0nubb) | `T^2 = -1` / Pin+ / quaternionic Kramers reflection structure | **FITS, not forced** (T^2=-1 is suggestive of a Majorana/topological-SC class; but a **pseudoreal** carrier's invariant is *antisymmetric*, which would *forbid* a symmetric Majorana mass -- the sign of the argument is not even fixed, and the physical-neutrino <-> quaternionic-carrier identification is source-gated) | observable-in-principle (neutrinoless double beta decay) | UN-WIRED (rides on Kramers, not `sigma`) -- **if it were forced** | Majorana => 0nubb nonzero; Dirac => absent. **Not forced either way at current grade** -- do not package. The repo's only Majorana-adjacent object (PP2 matter parity, seesaw via 126) is **conditional on the un-forced 126-vs-16 route**. |
| **C** | **Protected gapless edge mode** at `{q=0}` -> a physical massless/topological mode | anomaly-inflow => `{q=0}` wall anomaly-protected-gapless (excision forbidden) | **FORCED at proposal grade** (the protection is proposal-grade; that a *fiber-symbol* gapless mode descends to a *spacetime* massless field is a further leap) | observable-in-principle **but BRIDGE-GATED** (a spacetime massless mode would show in `Delta N_eff` / fifth-force; the fiber->finite-`z*` bridge is UNOWNED) | partially WIRED (its one data-shadow, the two-time `z*` -> low-`l` correlation, is the SAME theta/cosmology sector) | Extra light DOF (`Delta N_eff`) or a future-conditioned low-`l` correlation; **already named as bridge-gated in wave-swing3** -- not a new gold item, and gated on the same unowned bridge. |
| **D** | **DE amplitude / `w(z)` shape** | theta-sector realization | shape **FORCED** given the realization (PP3 one-parameter thawing locus, frozen slope); amplitude **OPEN** (free `f0`, no native scale) | OBSERVABLE-now | **WIRED** (same theta/DE sector as PP1) | Already packaged (PP3, PP1). More of the one observable, **not** a new un-wired domain. |
| **E** | **Arrow of time / thermodynamic** correlation with DE sign | CH-REC co-flip (SRC-COH-1) | FORCED as an internal co-flip | **not-observable** (the arrow is not independently measurable; no arrow-reversed comparison universe) | **WIRED + INTERNAL-only** | No new observable. Collapses (this is one of the three internal legs the parsimony ledger already discounted). |
| **F** | **`tau` / flavor: mixing-angle / mass-ratio / anomaly relation** among generations | `tau` (`Z/3`), generation universality | **not forced** (three-seam-prongB: GU does NOT force exactly three, does NOT forbid a fourth; masses/mixings are **imported**; `pred-norm-rank`/flavor selector is a free import) | observable-in-principle (PMNS/CKM, mass ratios) | UN-WIRED (`tau` is Q3-independent of `sigma`) -- **but nothing is forced** | The one place a forced result would be **strictly un-wired consilience** -- but the sector forces **no** observable at current grade. Collapses to not-forced. |
| **G** | **Discrete/topological relics** (monopoles, `theta_QCD`, cosmological defects) | Pin+ SPT `theta`-term; SO(10) breaking | monopoles: **not GU-distinctive** (generic SO(10) `G_SM` breaking, WIRED to the imported SM selector); `theta_QCD`: **not forced** (a reflection/T anomaly could bias `theta` to 0 or pi, but this is speculative) | observable-in-principle | monopoles WIRED to SM-import; `theta`-term folds into **A** (it is A's spacetime face) | The physically interesting piece (the SPT `theta`-term / gravitational Chern-Simons) **is** candidate A; the rest is generic or not-forced. |
| **H** | **Generation count = 3** (as a hard prediction) | `dim Lambda^2_+ = 3`, `Z/3 in Z/24` | **not forced** (three-seam-prongB: `B-FAILS`; "three" is a cardinal pun / homotopy-fixed count, not a forced cap; quaternionic wall even-favours a 4th) | -- | -- | Dead as a prediction. Recorded so the hunt does not resurrect it. |
| **I** | **Dark matter** (mirror sector, stable relic) | PP2 matter parity `P_M`; mirror sector | **not forced** (PP2 conditional on un-forced 126 route; mirror candidate `BLOCKED_PRODUCIBILITY` -- the K-negative modes are ghost-or-physical unresolved) | observable-in-principle | UN-WIRED **if forced** | Genuinely un-wired domain, but **blocked/conditional**, not forced. Collapses. |

---

## 2. The shortlist of GENUINE new predictions

**Exactly one item survives to the shortlist, and it survives with heavy, explicit
caveats.** Honesty first: this is NOT a clean "forced + observable + strictly
un-wired" prediction. It is the one candidate that opens a **new observable data
window** in a domain previously unconnected to the DE background, and thereby is the
one route by which the one-bit theory can gain a **second observable leg** -- which
is the specific thing the council asked to manufacture.

### GOLD-ADJACENT (the one worth building): Candidate A -- cosmological PARITY violation

**The claim.** If `sigma` is a genuine 't Hooft anomaly of the `w1` reflection /
time-reversal `Z/2` (anomaly-inflow-swing, PROPOSAL grade; receptacle
`Omega^{Pin+}_14`, `T^2 = -1`), then reflection/T is realized **anomalously** -- it
has **no symmetry-preserving realization**. A theory in which parity/T is anomalous
generically produces **parity-ODD** cosmological correlators that a parity-symmetric
`LCDM` forbids:

- CMB **`TB` and `EB`** cross-power (nonzero only under parity violation);
- **cosmic birefringence** (a rotation `beta` of the CMB linear-polarization plane,
  `E -> B`);
- **circular polarization / chirality** of the primordial gravitational-wave
  background (a parity-odd GW power asymmetry).

**Why this is the RIGHT face of the anomaly.** The anomaly is a **reflection**
anomaly, and reflection/T anomalies descend to **parity-odd** response terms (a
gravitational `theta` / Chern-Simons / Pontryagin coupling, the 3+1d
topological-superconductor `theta = pi` face of `Omega^{Pin+}_4 = Z/16`). This is
also **coherent with GU's standing "chirality is boundary/external"** posture
(no-net-chirality-without-a-boundary; global-anomaly-leg): the bulk is
parity-symmetric, and the parity content lives exactly at the **boundary/inflow**
seam where `sigma` lives. So the anomaly and the parity observable are the same seam
seen twice.

**Grade (unflinching).**
- **FORCED:** only at the **proposal grade of the anomaly ID** AND conditional on an
  **unbuilt descent map** from the fiber `w1` anomaly to a **spacetime** parity-odd
  term **with a definite sign**. The *existence* of some parity-odd response is
  generic to a genuine reflection anomaly; the *sign* and *amplitude* are not yet
  computed. So this is "FORCED-in-principle, un-quantified," not "FORCED-now."
- **OBSERVABLE-now:** yes -- cosmic birefringence `beta` is a measured quantity with
  a persistent **~3-sigma isotropic hint**; `TB/EB` and GW circular polarization are
  live targets (CMB S4, LiteBIRD; GW background searches).
- **WIRED / UN-WIRED (the crux, stated honestly):** **technically WIRED** -- it is a
  **second readout of the same `sigma`**. But it lands in a **previously-unconnected
  empirical domain** (photon/GW parity, not the theta-sector DE background), so it is
  the one move that upgrades the `sigma` cluster from **1 observable leg to 2**. A
  purist calls this "unification with a second observable"; by Whewell's own
  criterion (one hypothesis predicting independently-measured facts in unconnected
  domains) a **second observable readout** is exactly what converts unification into
  **consilience**. The parsimony ledger's deficit was never "too few bits" -- it was
  "**only one of the four readouts is observable**." A second observable readout, even
  of the same bit, is the cure it named.

**The kill / confirm test (the payoff -- a CROSS-DOMAIN correlation `LCDM` cannot
make).** Because both the DE sign and the parity-odd sign are readouts of the **same**
`sigma`, GU predicts they **CO-VARY**: the sign of the cosmic-birefringence `beta`
(or the handedness of the primordial GW background) is **fixed by the same bit** that
puts `w` on the `w >= -1` side. `LCDM` has no reason to correlate dark-energy EOS
with photon-polarization parity at all. So:
- **CONFIRM:** a nonzero parity-odd correlator (`beta != 0`, or `TB/EB != 0`, or GW
  circular-polarization asymmetry) whose sign matches the GU-predicted correlation
  with the DE side. This would be the **second observable leg**, and by GU's
  one-bit rigidity it drags evidential credit onto the whole `sigma` cluster.
- **REFUTE:** parity-odd correlators pinned to **zero** at the sensitivity where the
  built (sign-definite) anomaly predicts them nonzero; OR a measured parity-odd sign
  that is **anti-correlated** with the DE side. Either kills the identification of
  `sigma` as a physically-descending reflection anomaly.

**Grade of the object: proposal (fixture-supported).** The Pin/`Omega^{Pin+}_14`
substrate and controls are fixture-grade and already banked
(pin-bordism-cardinality, 35/35). What is **operator/construction-grade and unbuilt**
-- the two gates that would move this from proposal to a packageable prediction -- are
named in Section 5.

**Relation to prior work (additive, not redundant).** The corpus has NO cosmological
parity-violation observable. `stochastic-parity-breaking-test` is about whether
**chirality is derived-vs-inserted** in reduction (a Witten-1981 emergence question),
not an observable. `W236` is the gravity vacuum cheap-read (`E_s^theta = 0`), not
parity. The parsimony ledger already noted that confirming `w >= -1` or computing the
exact Pin class would upgrade the cluster; **this hunt's new contribution is naming a
SECOND, independently-measured data window (parity-odd cosmology) that the reflection
anomaly points at** -- a leg the repo has not enumerated.

---

## 3. Everything that COLLAPSED (the honest majority)

- **B Neutrino Majorana/Dirac -- FITS, not forced.** `T^2 = -1`/Pin+ is suggestive,
  but (i) a pseudoreal carrier's invariant is antisymmetric, which would *forbid* a
  symmetric Majorana mass -- the argument's *sign is not even fixed*; (ii) the
  physical-neutrino <-> quaternionic-carrier identification is source-gated. The
  repo's only Majorana-adjacent object (PP2 seesaw via 126) is conditional on the
  un-forced 126-vs-16 route. **Do not package.** (The single sharpening a future pass
  could try: is the physical `nu` in a real or pseudoreal sub-rep of the carrier? If
  real -> Majorana allowed/forced; if pseudoreal -> Dirac forced. That fork is
  currently source-gated, hence OPEN.)
- **C Protected `{q=0}` edge mode -- FORCED(proposal) but BRIDGE-GATED.** Its only
  data-shadow (two-time `z*` -> low-`l` correlation) is the SAME theta sector and is
  **unowned/bridge-gated** (wave-swing3). Not a new independent window.
- **D DE `w(z)` shape -- FORCED but WIRED.** Already PP3; same DE observable.
- **E Arrow of time -- WIRED + not-observable.** No comparison universe. Collapses.
- **F `tau` flavor/mixing -- UN-WIRED but NOT forced.** The one sector where a forced
  result would be *strictly* un-wired consilience -- and it forces **nothing**
  observable (masses/mixings imported; count not forced). The most disappointing
  collapse: the cleanest consilience slot is empty.
- **G/H Monopoles / `theta_QCD` / generation-count-3 -- not-forced or WIRED or dead.**
  The one live piece (the SPT `theta`-term) is A's spacetime face.
- **I Dark matter / mirror sector -- UN-WIRED but BLOCKED/conditional.** Genuinely a
  new domain, but PP2 is conditional and the mirror candidate is
  `BLOCKED_PRODUCIBILITY` (ghost-vs-physical fork unresolved). Not forced.

---

## 4. The count (honest)

- **Candidate domains examined:** 9 (A-I).
- **Clean GOLD (FORCED-now + OBSERVABLE-now + strictly UN-WIRED):** **0.**
- **Genuine NEW observable window surfaced (the one shortlist item):** **1** --
  Candidate **A**, cosmological parity violation. It is a *second observable readout*
  of `sigma` in a previously-unconnected domain (so consilience-generating), FORCED
  only at proposal grade + one unbuilt descent map, observable now.
- **Collapsed to FITS / not-forced / not-observable / internal / already-wired:**
  **8** (B-I).

**This is itself a finding, and it re-confirms the parsimony ledger from the
prediction side.** A dedicated adversarial hunt for more observables did **not** find
GU secretly forcing a zoo of independent signals. It found what the ledger already
said: GU's evidential surface is **one bit with (currently) one observable readout**.
The hunt's positive yield is precisely to locate the **one place a SECOND observable
readout could come from** -- and it is not a new bit, it is a **new observable face of
the same `sigma`**, made available by the (new, proposal-grade) reflection-anomaly
identification. That is the honest shape of "manufacturing consilience" here: not new
forced bits (there are none to find), but a **new data window onto the one bit**.

---

## 5. What would move Candidate A from proposal to packageable (the two gates)

Both are construction-grade and currently unbuilt; both are downstream of the same
source-action bottleneck (B5) everything else waits on, but they are **sharply named**:

1. **Bank the anomaly ID at operator grade.** The `sigma`-as-'t Hooft-anomaly reading
   is PROPOSAL, resting on the un-banked operator-grade deck action `U N U^{-1} = -N`
   and the exact order of `sigma`'s class in `Omega^{Pin+}_14`
   (pin-bordism-cardinality's single remaining number). Until the anomaly is a
   theorem, "parity violation is forced" is a graded proposal.
2. **Build the descent map fiber `w1` -> spacetime parity-odd term, WITH SIGN.**
   Compute the coefficient of the induced gravitational `theta` / Chern-Simons (or
   axion-photon) term that the Pin+ SPT bulk deposits on the boundary, and its sign as
   a function of `sigma`. This is what turns "some parity-odd response" into "`beta`
   of *this* sign, correlated with the DE side *this* way" -- i.e., into PP1's
   companion packet. Only after (2) can a kill threshold be pre-registered.

If both close, A is a **second frozen prediction packet** (call it the parity leg),
and the `sigma` cluster has two observable legs whose predicted co-variance is a
genuine cross-domain, `LCDM`-forbidden test -- the consilience upgrade the parsimony
ledger priced at "+1 to +2 decades."

## 6. Why no probe

A probe was considered and **deliberately skipped** (per the "only if it sharpens"
discipline). The load-bearing uncertainty for the one surviving candidate is the
**unbuilt fiber->spacetime descent with a definite sign** (Section 5, gate 2), which
is construction-grade and **not probe-checkable** at this grade -- it is the same
source-action gap as everywhere else. The Pin-bordism substrate a probe would rest on
(`Omega^{Pin+}_4 = Z/16`, `Omega^{Pin-}_2 = Z/8`, all Pin groups nonzero 2-groups at
`n = 14,15`, the `w1` non-implication) is **already reproduced and passing** in
`tests/channel-swings/pin_bordism_cardinality_probe.py` (35/35). A new probe would
re-assert those controls or perform a trivial parity-selection-rule tautology
(`<TB> = 0` under exact parity), neither of which sharpens the real gate. Honest call:
no probe adds signal here.

---

## 7. Boundary

Exploration tier, read-only interpretation. **Only this one file was written**; no
probe, no other file touched. GU otherwise read-only. No commit, no push. No edit to
LANE-STATE, research-portfolio, NEXT-STEPS, canon, PP1/PP2/PP3, the anomaly-inflow
swing, pin-bordism-cardinality, the parsimony ledger, per-leg-recovery, the
decision-trees, or any other agent's artifact, or any claim/canon/verdict/ledger
file. No claim-status, canon-verdict, paper-status, or public-posture change; the
externality of `sigma`/`tau`, the verdicts `LC-SELECTOR`, `Q3-TWO-INDEPENDENT`,
`B-FAILS`, `CARDINALITY-1 + PROTECTED` (proposal), PP1-frozen, and the parsimony
lean are **consumed, not moved**. The proposal-grade anomaly-inflow identification is
**consumed, not promoted**. Nothing routes externally (Joe alone publishes).

**Contribution.** An adversarial hunt for more observable `sigma`/`tau`-forced
predictions in un-wired domains finds **zero clean gold** and re-confirms the "one
observable bit" ledger from the prediction side, while surfacing **one genuinely new
observable window** the corpus had not enumerated: cosmological **parity violation**
(CMB TB/EB, cosmic birefringence, chiral GW), forced-in-principle by the (proposal-
grade) `w1` reflection/T anomaly, observable now, and -- though a second readout of
the same `sigma` -- consilience-generating because it opens a second, previously-
unconnected data window onto the one bit, with a named two-gate build path and a
cross-domain co-variance kill test `LCDM` cannot make.

---

## CONSTRUCTION-MODE DEEP DIG (2026-07-21, follow-up)

Follow-up charge (Joe): do not accept the 8 collapsed domains as final. For EACH,
in **construction mode** (the way the parity leg became a prediction only once we
posited *building* the descent-with-a-sign): (1) WHY exactly did it collapse; (2)
what SPECIFIC missing construction, if BUILT, turns it into a FORCED observable --
name the object; (3) is that construction TRACTABLE at reconstruction grade and does
it stay HONEST (forced-not-inserted, gate-at-the-back, posits labeled); (4) `tau`
flavor especially. Then rank by (a tractable construction exists AND it yields an
UN-WIRED observable), separating **genuinely dead (a proven obstruction)** from
**merely hard / not-yet-built**. This section is additive; the Section-1 table stands.

### The organizing insight: ONE master construction lights up FIVE domains

Digging in construction mode surfaces a structural fact the first pass missed: **five
of the collapsed domains share a single build-object -- the reflection-anomaly
descent map with a definite sign** (Candidate A's gate 2: the map that pushes the
fiber `w1` / `Omega^{Pin+}_14` SPT bulk to spacetime response terms, with signs).
That one map, once built, admits **several independent readouts**:

| readout of the ONE descent map | domain | observable |
|---|---|---|
| gravitational-`theta` / Chern-Simons coefficient | A (parity) | birefringence `beta`, TB/EB, chiral GW |
| QCD `theta`-vacuum pinning (does matching fix `theta`?) | G (strong-CP) | neutron EDM |
| Pin+ boundary-fermion reality (the `Z/16` class) | B (nu character) | 0nubb |
| is the protected `{q=0}` mode a propagating massless field? | C (edge mode) | `Delta N_eff` |
| microscopic T-odd face of the arrow co-flip | E (arrow) | EDM (degenerate) |

**So the highest-leverage build is not domain-by-domain -- it is the ONE descent
map, then harvesting its faces.** This is the same lift-many-legs-at-once shape the
program already knows from B5; here it is one level down (the descent, not the full
source action) and correspondingly more tractable. The three domains that do NOT ride
this gate (D, F, I) are treated on their own objects below.

### Per-domain construction dig

**B -- Neutrino Majorana vs Dirac.**
1. *Collapsed as:* FITS-not-forced -- `T^2=-1` is suggestive but the argument's SIGN
   is unfixed (pseudoreal carrier -> antisymmetric invariant -> *forbids* symmetric
   Majorana mass -> would force DIRAC; the naive "T^2=-1 => Majorana" is backwards),
   and the physical-`nu` <-> carrier identification is source-gated.
2. *Build-object:* the **Frobenius-Schur indicator of the `nu`-carrying sub-rep**
   inside `M(64,H)` after the committed section/orientation reduction, AND -- the
   sharper GU-native route -- the **Pin+ boundary-fermion content read off the `Z/16`
   class** (`Omega^{Pin+}_4 = Z/16` counts T^2=-1 Majorana surface modes). Both are
   discrete-invariant computations, not fits. FS = -1 (pseudoreal) forbids Majorana =>
   Dirac forced (0nubb ABSENT -- a sharp falsifier); FS = +1 (real) allows Majorana.
3. *Tractable & honest:* the FS-indicator branching is a **finite rep-theory
   computation -- tractable at reconstruction grade**; the Pin+ reading rides the
   master descent gate. Honesty: the `nu_R` <-> sub-rep identification is a labeled,
   source-gated posit (gate-at-the-back). Forced-not-inserted: the indicator is
   *computed*. **Named risk:** if `nu_R` lands in the complex `16` of Spin(10) (FS=0),
   the reality route is silent and it **reduces to PP2's un-forced 126-vs-16 fork** --
   so the honest likely outcome is "reduces to the already-conditional route," not a
   clean new forcing.
4. *Observable / wired:* 0nubb; **UN-WIRED** (rides Kramers/reality + Pin+ boundary,
   not the DE `sigma`).

**C -- Protected `{q=0}` edge mode -> physical massless/topological mode.**
1. *Collapsed as:* FORCED-at-proposal but BRIDGE-GATED; its one data shadow (two-time
   `z*` -> low-`l`) is the SAME theta/cosmology sector.
2. *Build-object:* the **N2 operator-lift deficiency-index computation at the true
   `q<0` ends** (discrete indices => finite `z*` / live protected mode; continuum =>
   washout) -- wave-swing3 named this "the single analytic computation the whole
   boundary picture waits on" -- PLUS the descent reading "**is the protected mode a
   propagating massless spacetime field?**" (if yes -> `Delta N_eff` / dark radiation,
   a *cleaner, un-wired* observable than the low-`l` correlation).
3. *Tractable & honest:* the **N2 index step is analytic and owned-adjacent -- the
   most tractable single step in this whole dig** (lp-lc-deficiency already did the
   true-end count; the operator-grade N2 cardinality is the remaining piece). The
   fiber->`z*` bridge stays unowned (labeled posit, gate-at-the-back). Forced-not-
   inserted: the index is computed, not chosen.
4. *Observable / wired:* `Delta N_eff` (un-wired) OR low-`l` (partially WIRED). The
   `Delta N_eff` face is the one worth targeting because it is independent of the DE
   sector.

**D -- DE `w(z)` shape beyond PP3.**
1. *Collapsed as:* FORCED (shape) but WIRED -- same theta/DE sector; already PP3.
2. *Build-object:* the only *un-wiring* is the **B5 absolute-scale datum with its
   overdetermined web** (PP3 council: one scale sets DE scale + spectral cut +
   C-strip width ~0.1 mm). Build B5's scale slot -> measuring the DE shape *predicts*
   the spectral-cut / C-strip -- an un-wired cross-check.
3. *Tractable & honest:* this **IS the B5 source-action closure -- the load-bearing
   blocked spine. NOT tractable at reconstruction grade.** Honest.
4. *Observable / wired:* spectral-cut / C-strip (un-wired IF the web is real), but
   **B5-gated (lowest tractability of the hard set)**.

**E -- Arrow of time.**
1. *Collapsed as:* WIRED + not-observable (no comparison universe).
2. *Build-object:* there is **no construction that makes "the arrow" separately
   observable** -- its only observable face is a **microscopic T-odd effect** (an
   EDM), which is precisely the descent map's T-odd readout, i.e. it **folds into A/G**
   and is degenerate with `theta_QCD` and CP phases.
3. *Tractable & honest:* n/a as an independent leg.
4. *Observable / wired:* none of its own. **Structurally subsumed by A/G** (the arrow
   IS `sigma`; `sigma`'s observable faces are A and G). See "genuinely dead," below.

**F -- `tau` flavor / mixing (the priority slot). Deep treatment.**
1. *Collapsed as:* UN-WIRED (Q3-independent of `sigma`) but **NOTHING FORCED** --
   masses/mixings imported; the count is not even forced (`B-FAILS`); "generation
   universality" is a *signature-sameness*, not a mass relation.
2. *Build-objects tried (three), and what each hits:*
   - **(F-i) Weyl / discrete-flavor symmetry of the `Lambda^2_+` triplet.** The
     cleanest idea: the `j=1` `su(2)_+ = Lambda^2_+` triplet carries a root-system
     Weyl group (the S^3 / A_1 / BC_1 M^2-band from PP3), whose residual `Z_2` would
     be a **mu-tau reflection => theta_23 = 45 deg and Dirac CP phase delta = +-90
     deg** -- both OBSERVABLE (neutrino oscillations), both currently data-favored,
     both un-wired. **BUT this is OBSTRUCTED by GU's own banked branching:**
     three-seam-prongB (operator/fixture grade) proves the family index branches as a
     **Spin(4) DOUBLET (mult 2)**, and the `Lambda^2_+` "3" is the Lorentz/gauge index
     carried by *each* generation, **not** a family label -- the identification
     "generation ≡ self-dual component" is **canon-quarantined as a cardinal pun**.
     So an `Lambda^2_+`-Weyl flavor symmetry acts on the *internal* index, not on
     families; deploying it as a PMNS symmetry would be **INSERTED, not forced** --
     it fails the honesty gate on a *banked* obstruction.
   - **(F-ii) Krein form on the generation-multiplicity space.** If the family space
     (the mult-2 doublet, or the 64 identical `j=1` copies) carried a Krein form whose
     signature forced a texture/ordering. **BUT** the banked result is that all copies
     carry the **identical** `(+32,-32,0)` signature -- a *sameness* invariant that
     forces **degeneracy / universality**, i.e. the OPPOSITE of a mixing hierarchy
     (degenerate masses => mixing undefined). The realistic hierarchical structure is
     exactly the imported Yukawa. So this route **forces the wrong observable**.
   - **(F-iii) `tau`-trit `Z/3` phase as a CP phase.** The anchor `Z/6 = Z/2 x Z/3`;
     could the trit's `Z/3` phase appear as a discrete leptonic `delta_CP` (e.g. a
     120-deg phase)? **BUT** the trit controls *count admissibility* (degree mod 3 on
     the double cover), not a continuous Yukawa phase; mapping `Z/3 -> delta_CP` needs
     the imported Yukawa. Speculative, low tractability; rides the imported block.
3. *Tractable & honest:* the two *natural, tractable* constructions (F-i, F-ii) are
   **obstructed by banked results** (the branching contradiction; the universality
   sameness). F-iii is not obstructed but is not forced either (imported-Yukawa
   block). Honesty forbids deploying F-i (it would be inserted against GU's own
   branching).
4. *Verdict on the priority slot:* **the highest-value slot has a banked obstruction
   against its most natural filling.** It is NOT proven-impossible in the absolute
   sense -- a **source-action family object (B5) could supply a genuine 3-family
   structure carrying a flavor symmetry** -- but every construction tractable *today*
   is either obstructed (F-i, F-ii) or reduces to the imported Yukawa (F-iii). So
   `tau`-flavor is **"tractable routes obstructed; sector reopenable only via the
   blocked B5 family object"** -- closer to dead than the descent-gated domains, and
   the honest, disappointing answer for the slot that would have been the biggest win.

**G -- Monopoles / `theta_QCD`.**
1. *Collapsed as:* monopoles not GU-distinctive (generic SO(10), WIRED to the imported
   SM selector); `theta_QCD` not forced; the live piece (the SPT `theta`-term) is A's
   spacetime face.
2. *Build-object:* the descent map's **QCD-sector readout -- does reflection-anomaly
   matching PIN `theta_QCD` (to 0, solving strong-CP, or to pi)?** If matching forces
   `theta_QCD = 0`, that is strong-CP solved *by the reflection anomaly* (not by
   imported Peccei-Quinn), and the observable is a **forced-zero neutron EDM from the
   QCD sector** -- a high-profile un-wired prediction.
3. *Tractable & honest:* rides the master descent gate + a **bounded anomaly-matching
   argument**. Honesty: forced-not-inserted IFF matching genuinely pins `theta`
   (**named risk: it may equally leave `theta` free** -- matching does not always
   fix a `theta`-angle). Gate-at-the-back on the descent + the matching lemma.
4. *Observable / wired:* neutron EDM; **UN-WIRED** from the DE sign. Monopoles remain
   non-distinctive (near-dead as a *distinctive* prediction, not proven-impossible).

**I -- Dark matter / mirror sector.**
1. *Collapsed as:* UN-WIRED (genuinely a new domain) but BLOCKED_PRODUCIBILITY (the
   K-negative 96 modes are ghost-or-physical unresolved) + PP2 conditional on the
   un-forced 126 route.
2. *Build-object:* the **operator-grade physical-state / Born-rule superselection rule
   (the L7 gap)** that decides the Turok-Bateman ghost-vs-physical fork -- if the
   K-negative modes are producible asymptotic states -> a mirror relic/collider
   observable; if ghosts -> no mirror DM -- PLUS PP2's 126-vs-16 selector.
3. *Tractable & honest:* the L7 Born-rule rule is a **named blocked object
   (source-gated, B5-adjacent) -- not tractable at reconstruction grade**; two gates
   (Born-rule + 126), both source-gated. Honest: the ghost fork must be *decided*, not
   assumed.
4. *Observable / wired:* DM relic / collider missing-parity; **UN-WIRED**. Low
   near-term tractability.

### Ranked list (by: a tractable construction exists AND it yields an un-wired observable)

Ranked best-to-worst on the joint criterion, all honest about grade:

1. **C (protected mode -> `Delta N_eff`)** -- the **most tractable single step** (the
   N2 deficiency-index count is analytic and owned-adjacent) and un-wired if it yields
   `Delta N_eff`. Residual block: the fiber->`z*` / propagating-field bridge (unowned).
   *Merely hard.*
2. **B (nu Majorana/Dirac -> 0nubb)** -- a **finite, tractable rep-theory computation**
   (FS indicator + Pin+ `Z/16` reality), sharp un-wired observable. Named risk:
   reduces to PP2's un-forced 126 fork if `nu_R` is in the complex `16`. *Merely hard.*
3. **G (`theta_QCD` -> neutron EDM)** -- rides the master descent gate + a bounded
   matching lemma; highest-profile un-wired observable (strong-CP). Named risk:
   matching may not pin `theta`. *Merely hard.*
   - (A, the shortlisted parity leg, sits at this same tier -- it is the descent
     map's *first* readout; B, G, C, E all ride the same build.)
4. **I (dark matter / mirror)** -- genuinely un-wired new domain, but gated on TWO
   source-gated decisions (Born-rule ghost fork + 126 selector). *Merely hard, low
   near-term tractability.*
5. **D (DE shape beyond PP3)** -- mostly WIRED; the one un-wiring (overdetermined B5
   scale web) is gated on B5, the hardest bottleneck. *Merely hard, lowest tractability.*
6. **F (`tau` flavor/mixing)** -- HIGHEST value if fillable, but the tractable routes
   (F-i Weyl/mu-tau, F-ii Krein-on-family) are **obstructed by banked results**;
   reopenable only via the blocked B5 family object. *Obstructed-tractable-routes /
   sector-reopenable-only-via-B5.*
7. **E (arrow of time)** -- **no independent observable of its own**; subsumed into
   A/G. *Structurally dead as an independent leg.*

### Genuinely dead vs merely hard (the honest separation)

- **Genuinely dead / proven obstruction (do not re-open on the same route):**
  - **E as an independent observable** -- structural, not practical: the arrow *is*
    `sigma`, so it cannot be a *second* independent readout; its only observable face
    is the T-odd descent term already carried by A/G. Subsumed, not separately live.
  - **F-i and F-ii (the tractable flavor constructions)** -- obstructed by *banked,
    operator/fixture-grade* results: the family ≠ `Lambda^2_+` triplet branching
    (three-seam-prongB) kills the mu-tau/Weyl route as INSERTED; the identical-signature
    universality kills the Krein-on-family route (it forces degeneracy, the wrong
    observable). These specific routes are proven-obstructed.
  - **Monopoles as a *distinctive* prediction** -- generic SO(10), WIRED to the
    imported SM selector; no GU-specific forcing (not proven-impossible, but
    proven-non-distinctive).
- **Merely hard / not-yet-built (a named, non-refuted construction exists):**
  B, G, C (all ride the master reflection-anomaly descent gate; C's index step is the
  most tractable), I (Born-rule + 126, source-gated), D (B5 scale web, source-gated),
  and **F's absolute reopening** (a B5 family object *could* carry a genuine flavor
  symmetry -- not proven-impossible, only proven-obstructed on every route tractable
  today).

**Bottom line of the deep dig.** No collapsed domain resurrects into a clean
forced-now observable, so the Section-4 count stands. But construction mode changes
the *shape* of the opportunity: the biggest lever is the **single reflection-anomaly
descent-with-signs** (A's gate 2), which is one construction feeding **four un-wired
observable faces at once** -- parity (A), strong-CP/neutron-EDM (G), Majorana/Dirac
0nubb (B, via the Pin+ reality reading), and `Delta N_eff` (C, jointly with the
owned-adjacent N2 index step). The genuinely dead items are E (subsumed into `sigma`)
and the *tractable-today* flavor routes F-i/F-ii (banked-obstructed); the priority
`tau`-flavor slot is honestly the disappointment -- its most natural fillings are
obstructed by GU's own branching, and it can reopen only through the same blocked B5
family object everything else waits on.
