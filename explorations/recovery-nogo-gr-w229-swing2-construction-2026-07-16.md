---
title: "Recovery no-go GR W229 Swing 2 construction attempt"
status: exploration
doc_type: recovery_no_go_defense
created: 2026-07-16
run_id: GUH-20260716T170900Z-gr-swing2
target: RECOVERY-NOGO-GR-W229-VACUUM
test: tests/recovery-contract/gr_nogo_swing2_construction_gate.py
register: lab/process/recovery-no-go-defense-register.json
---

# Recovery no-go GR W229 Swing 2 construction attempt

Operational result: `NO_SURVIVOR` for Swing 2 of
`RECOVERY-NOGO-GR-W229-VACUUM`.

This does not strengthen the repo verdict, change claim status, move canon,
alter public posture, or call the GR recovery question exhausted. It records a
bounded construction attempt after the branch-local W229 exact-vacuum no-go and
Swing 1 scope/fork audit.

## Decision question

Does the nonzero exact-vacuum Schwarzschild `Q^TF(B)` residual survive the best
already named construction forks, or only the W229 record-current vacuum
fingerprint where `J[Psi] = 0`?

Answer: the tested Swing 2 fork produced no admissible survivor.

## Construction fork

The gravity benchmark remains the GU-native `|II|^2` /
second-fundamental-form residual on `Y14 = Met(X4)`. The standard
Schwarzschild metric is the imported exact-vacuum comparator benchmark.

Swing 2 did not rerun the W229 record-current branch. It tested the best
already named escape family:

1. a fundamental-stiffness or bare-theta branch;
2. an ambient/H-class balance; and
3. a boundary-conditioned response under an explicit adapter assumption.

Standard Einstein dynamics is used only as a comparator. It is not a GU
source law and cannot be imported as the cancellation tensor.

## Swing 2 survivor criteria

An admitted Swing 2 survivor must provide all of the following before it can
face Swing 3 adversarial adjudication:

- a source-owned construction, not a tensor defined as `-Q^TF(B)`;
- `O(M^2)` or otherwise benchmark-matched order for the exact-vacuum residual;
- trace-free tensor shape in the same slot as `Q^TF(B)`;
- construction provenance that does not merge W229 with another action branch;
- a first falsification test that can fail without changing the target.

The algebraic fact that a tensor equal to `-Q^TF(B)` would cancel the residual
is not evidence. It is the target definition. A real survivor must derive the
tensor from a GU source object before looking at the benchmark.

## Attempted constructions

| construction | Swing 2 result | why it fails or survives |
|---|---|---|
| W203 ultralocal limit | `NO_SURVIVOR` | In `Psi = 0` vacuum it inherits the same zero record-current source unless a different non-record source is supplied. That different source is outside W203/W229 and cannot be silently merged. |
| Fundamental-stiffness / bare-theta branch | `NO_SURVIVOR` | It is a legitimate different construction class, but the current record has no source-owned bare-theta field equation, boundary data, normalization, or stress tensor that lands in the trace-free `Q^TF(B)` slot. |
| Ambient or H-class balance | `NO_SURVIVOR` | Prior work maps the ambient/H-class possibility and the OQ2-A functional binary, but the current source does not freeze the higher-codimension first variation or the required coefficient. It supplies a route to build, not a current cancellation tensor. |
| Boundary-conditioned adapter response | `NO_SURVIVOR` | A typed adapter assumption can be tested downstream, but no frozen adapter packet supplies an exact-vacuum GR source tensor or a physical state-preserving construction that cancels `Q^TF(B)`. |
| Standard Einstein dynamics | `INVALID_ESCAPE` | It supplies the target benchmark, not GU dynamics. Importing the Einstein tensor or a target-shaped stress tensor would be target import. |

## Axis and source check

Layer 0 semantic alignment: exact-vacuum GR recovery means a single frozen GU
construction supplies the gravitational field equation or source balance, not
only compatibility with an imported Schwarzschild metric.

| axis | failed W229 branch | Swing 2 attempt |
|---|---|---|
| L1 substrate | record-current induced-YM branch-3 action family | alternate theta/ambient/adapter constructions considered separately |
| L2 observer | imported Schwarzschild benchmark with `Psi = 0` | same benchmark, no target import |
| L3 pairing | GU-native gimmel / DeWitt fiber pairing | unchanged unless the alternate action derives a new stress slot |
| L4 causal order | exact-vacuum comparator geometry | unchanged |
| L5 emergence | source/YM cancellation tensor required | absent for every tested alternate fork |
| L6 coordination loop | W229 source law and zero record current | not merged with other branches |
| L7 positivity or state-space metric | not load-bearing for this tensor cancellation | adapter positivity remains assumption-capped |

The axis deltas are real enough to count as a Swing 2 construction attempt, but
they do not produce a survivor because the required source tensor remains
absent.

## First falsification test

The gate for this note checks two things:

1. The cancellation detector has teeth: a predeclared tensor `S = -Q^TF(B)`
   would algebraically cancel the residual, and a metric-proportional or zero
   tensor would not.
2. The admissibility detector rejects the target-shaped `S = -Q^TF(B)` unless
   it is source-owned, benchmark-independent, trace-free, and branch-clean.

The real register candidates fail the second test. They are therefore
`NO_SURVIVOR`, not `SURVIVOR_CANDIDATE`.

## Next work

Proceed to Swing 3 for `RECOVERY-NOGO-GR-W229-VACUUM` unless daily
stewardship changes the lane. Swing 3 should adjudicate the no-survivor result
and record an exact resurrection trigger, while preserving that this protocol
alone changes no claim status or verdict.

Paper seed proposal: none.
