---
title: "B2C8 source-forked two-connection square and active Euler map: a source-compatible connection complex is constructed; Euler completion remains conditional"
status: active_research
doc_type: construction_result
created: 2026-08-01
branch: agent/null-clifford-omega1-repair
run: RUN-20260801-154304-gu-formalization-ecw3d-b2c8-direct
predecessor: explorations/eric-curt-wave3d-b2c7-two-connection-somatic-obstruction-2026-08-01.md
probe: tests/channel-swings/eric_curt_wave3d_b2c8_source_forked_two_connection_square_euler_map_probe.py
registry: lab/process/eric-curt-wave3d-b2c8-source-forked-two-connection-square-euler-map.json
grade: "B2C8 AUTHOR-STATED UNRELEASED TWO-CONNECTION PROGRAM / SOURCE-COMPATIBLE REPO-CONSTRUCTED CONNECTION-COMPLEX PASS / NARROWED EULER KILL / CONDITIONAL COMPLETION ROUTE. Finite architecture controls construct exact inverse connection and independent-dual fermion lowerers with the active trace-reversed (9,5) Hodge signs, indefinite Krein form, and right-H compatibility inherited from the active factors. The explicit 2021 F_A/d_B deformation row and tentative 2025 F_B/D_B four-token object remain separate. The source selects neither shifted-cone placement nor the A=B nilpotence locus. Both repo-reconstructed degree-correct readings are compatible with Eric's connection-level program: they are A=B complexes and vanish for arbitrary nonflat common curvature. The tested action-derived connection Euler owner is then S(F_B) and generically nonzero, so the bare four-entry cone is not the complete Euler obstruction. Two exact finite residual-alphabet fits fail with fixture-local compatibility codimensions 10 and 8. The 2021 source-shaped residual also misses E_T. Finite constant-conjugation covariance and exact orbit decompositions make an action-complete D E_T deformation a viable conditional Noether route, but no local gauge-tangent derivative or full graph-tuple Hessian is executed here. No total fermion action, global domain, observation reduction, Poisson anchor, prequantum lift, Standard Model recovery, count, or cosmological prediction is claimed."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# B2C8 source-forked square and active Euler-map discriminator

## Result first

B2C8 finds a useful split rather than a binary verdict.

The 2025 four-entry recollection has a clean mathematical reconstruction as a
shifted mapping cone. It really does square to a zero-order two-connection
obstruction, and its coefficient relations are rigid enough that the normalized
form has one algebraic similarity class after unrestricted overall and relative
summand rescaling. Whether the action pairing permits that relative rescaling
is open. The obstruction is the difference
between the two connections and their curvatures. It is not the complete GU
Euler equation.

That first result is a concrete repo construction compatible with Weinstein's
stated square-root-of-connections program. The author-stated program is
unreleased and does not select either shifted placement or the `A=B` locus, so
the reconstruction is not attributed back to him. The narrowed negative result concerns
only an additional identification not fixed by the source: that the bare
four-entry connection complex already encodes the entire action-derived
Einstein/GU Euler system.

The decisive counterexample is constructive and exact. Set the two connections
equal but retain nonzero curvature:

\[
A=B,\qquad T=A-B=0,\qquad F_B\ne0.
\]

Every minimal 2025 cone reconstructed here satisfies

\[
\mathbb D^2=0,
\]

while the action derived in G2 gives

\[
E_T\big|_{T=0}=\mathscr S_\epsilon(F_B)\ne0
\]

on the exact held-out fixture. Therefore that literal cone becomes a complex
too early: equality of the two connections suffices, even when the GU action is
off shell. An invertible Hodge/Krein primalizer cannot turn its zero into the
nonzero Euler owner.

The 2021 deformation architecture identifies a precise candidate enlargement.
Its repo-inferred source-shaped residual

\[
R_{21}=\mathscr S_\epsilon(F_A)+\kappa_1\flat_1T
\]

does not equal the complete action derivative. The natural candidate second
arrow is therefore the connection-component derivative `D E_T`. If local
gauge invariance and the complete graph gauge tangent are established with a
consistent coadjoint/primalized convention, Noether covariance would give

\[
D E_T[R_\gamma]=\operatorname{ad}^*_{\gamma}E_T,
\]

or, after an explicitly fixed invariant pairing and the convention used by the
finite model, `[gamma,E_T]`. B2C8 verifies finite constant-conjugation
covariance of `E_T` and exact decompositions of that conjugation orbit into
the source-shaped core plus each forced correction. It does not yet execute
`D E_T` on a local gauge tangent. The action-complete deformation is therefore
a strong conditional construction route, not yet a completed square and not a
formula Weinstein supplied.

## Plain English

The 2025 idea can be made into a beautiful little machine. Put two covariant
derivatives, a curvature term, and an identity into a two-by-two box. With the
two minus signs Weinstein recalls, squaring the box cancels all derivatives and
leaves only differences between the two connections.

That is one concrete connection-level behavior compatible with Eric's
description; the unreleased source does not select it. The remaining question
is whether a larger total operator makes it also carry the Einstein
obstruction. If the two connections happen to agree, the bare box squares to
zero for every curvature. The actual GU action does not: it still asks the
common curvature to satisfy the contracted Einstein-like equation.

The older 2021 construction is a different machine: a proposed deformation
complex along a gauge orbit. Its short source-shaped field equation is
incomplete, but the repo now has the complete connection Euler component.
The next constructive test is to differentiate that component along the actual
local graph gauge tangent. The finite covariance check says this route is
compatible; it does not substitute for that derivative.

## 1. Layer 0

| shared term | object used here | object kept distinct |
|---|---|---|
| 2021 square | `delta_2 delta_1` in a three-term ordinary-gauge deformation complex | a two-by-two rolled Dirac or somatic operator |
| 2025 `D^2` | two reconstructed shifted mapping cones compatible with the spoken tokens | a published or author-selected matrix |
| contraction | the equivariant curvature map followed by action-owned degree conversion | an idempotent projection |
| curvature | `F_A,F_B` in `Omega2(ad P)` or their representation action | the density-dual Euler covector in `Omega13(ad* P)` |
| local primalizer | inverse of the action's nondegenerate Hodge/Krein lowering map | an inverse presymplectic form or prequantum lift |
| on shell | zero of the complete returned action Euler tuple | merely `A=B`, flatness, or vanishing of a compressed residual |
| `Z_var` | the independently reconstructed difference between `E_T` and the affine segment core | a coefficient defined into a candidate square |
| physical `zeta,nu` | the one-form and zero-form fermion fields | gauge ghosts or odd parameters |

The active geometry remains the geometer-native, trace-reversed branch

\[
(3,1)+(6,4)=(9,5),\qquad
\operatorname{Cl}(9,5)=M(64,\mathbb H).
\]

The draft's literal `(7,7)` carrier remains a separate Curt rival. No map in
this packet transports the active right-quaternionic data between them.

## 2. Primary-source collision

### 2021 manuscript-explicit branch

The official 2021 PDF, SHA-256
`3f28d742234a9841fc8e51ff172053200aa3eddf3ece38154a3328b9ebd186d4`,
was re-read visually at pages 43--49.

For `d=14`, equations `(10.1)--(10.9)` give

\[
\Omega^0(\operatorname{ad}P)
\xrightarrow{\delta_1}
\Omega^1(\operatorname{ad}P)\oplus\Omega^0(\operatorname{ad}P)
\xrightarrow{\delta_2}
\Omega^{13}(\operatorname{ad}P),
\]

with

\[
\delta_1=
\begin{pmatrix}
d_A\\DL_\epsilon
\end{pmatrix},
\]

and

\[
\delta_2=
\begin{pmatrix}
\mathscr S_\omega d_A+\kappa_1*
&
F_A\wedge K_\omega(\,\cdot\,)
-\kappa_1*d_B
\end{pmatrix}.
\]

Here `K_omega` is deliberately only a placeholder for the manuscript's inner
glyph. The draft types the whole second component but does not define the inner
glyph as a derivative or as another copy of the fixed map
`S:Omega2 -> Omega13`. Interpreting it as the linearization of the moving
contraction is repo-derived and remains to be verified against the local gauge
tangent.

The source status is mixed but usable:

- the domains, `delta_1`, and the displayed `F_A/d_B` second row are
  manuscript-explicit;
- `delta_2` is introduced as a posit sought in the linearized equations;
- the page-49 combined Bose/Fermi diagram is explicitly inherited,
  potentially inconsistent, and unstabilized; and
- the page-46 fermion block is introduced only as an operator “like” the one
  shown and permits alternate lower-right blocks.

Thus the bosonic three-term branch is stable enough to test. The combined
fermionic matrix is not stable source law.

### Tentative 2025 branch

The TOE transcript supplies the following at `02:38:12--02:45:13`:

- `d_A^2` acts by curvature;
- the `0 -> 1 -> 13 -> 14` rolled spine uses contraction followed by Hodge;
- an unreleased two-connection “new `D^2`” has tokens recalled as something
  resembling `D_A,F_B,id,D_B`;
- there are two negative signs in the second column; and
- the object is said to become a complex on shell.

Weinstein says “I think,” corrects himself while recalling the signs, and
states that the object has never been released. The source does not select:

- row-major versus column-major token order;
- exterior covariant derivative versus a rolled Dirac operator;
- the curvature representation action;
- the grading shifts and identity carrier;
- the connection ownership relative to the 2021 fields;
- the contraction/Hodge insertion;
- lower-order terms, domain, or boundary conditions; or
- which component or tuple is meant by “the equations.”

Disposition: `SOURCE-CONFIRMS_PLUS_SOURCE-CORRECTS_PLUS_SOURCE-SILENT` for the
2021/2025 fork. Under the fail-closed three-disposition protocol, the page-49
warning is recorded as the explicit negative receipt correcting any stable
combined-diagram reading. `SOURCE-CORRECTS` also separately records
Weinstein's definite `projection -> contraction` correction.

## 3. Action-owned local primalizers

B2C8 does not invent new intertwiners. It inverts the lowering maps already
owned by the action pairings.

### Connection slot

For an adjoint-valued one-form `u`, define

\[
b_A(u)=\kappa^\flat(*_1u)
\in\Omega^{13}(Y,\operatorname{ad}^*P).
\]

Because in signature `(9,5)`

\[
*_{13}*_{1}=+1,
\]

the local connection primalizer is

\[
\boxed{R_A(e)=*_{13}\kappa^\sharp(e).}
\]

In density coordinates,

\[
(R_Ae)_\nu{}^J
=|\det G|^{-1/2}G_{\nu\mu}\kappa^{JI}e^\mu{}_I.
\]

This map is indefinite but nondegenerate. It is not a positive Hilbert Riesz
map, a Moore--Penrose inverse, or a symplectic sharp.

### Independent-dual fermion slot

For

\[
Q_F=\Omega^1(S_\mathbb H)\oplus\Omega^0(S_\mathbb H),
\]

the B2C5 independent-dual action owns

\[
b_F=
\operatorname{diag}(\beta^\flat *_1,\beta^\flat *_0),
\]

so

\[
\boxed{
R_F=
\operatorname{diag}(*_{13}\beta^\sharp,-*_{14}\beta^\sharp).
}
\]

The zero-form minus sign is forced by

\[
*_{14}*_{0}=-1,
\]

whereas the one-form block has positive Hodge square. The exact finite model
verifies both inverse identities and right-`H` compatibility.

The charge form

\[
C_+=-\beta J
\]

is separately nondegenerate and skew. It is a compatibility/reality gate for
the primalized operator, not another factor in the independent-dual Riesz map.
Multiplying both `beta` and `C_+` into one unexplained dualization would change
the linearity class and double-count the Krein pairing.

When the moving lowerer changes, its inverse response is forced:

\[
\delta R=-R(\delta b)R.
\]

The probe verifies this universal inverse-response identity for an arbitrary
finite nondegenerate lowerer. It does not yet decompose `delta b` into the
moving Hodge, density, Krein, projector, `epsilon`, and metric responses; that
active calculation belongs to B2C9.

### Scope boundary

`R_A` primalizes only the connection equation `E_A=E_T`. A statement about
the full tuple

\[
\mathfrak G=(E_A,E_\epsilon,E_g)
\]

also needs

\[
R_\epsilon:\Omega^{14}(\operatorname{ad}^*P)\to\Omega^0(\operatorname{ad}P)
\]

with the corresponding split-Hodge sign, and the inverse density-valued
trace-reversed DeWitt map for `E_g`. Those returns remain B2C9 work.

## 4. The two degree-correct 2025 reconstructions

Let `E` be an associated bundle and let curvature act by wedge/representation.
The natural row-major reading is the downward-shifted cone

\[
\mathcal C^k_\downarrow
=\Omega^k(E)\oplus\Omega^{k-1}(E),
\]

\[
\mathbb D_K^\downarrow
=
\begin{pmatrix}
D_A&-K\\
1&-D_B
\end{pmatrix}.
\]

The equally degree-correct column-major reading is

\[
\mathcal C^k_\uparrow
=\Omega^k(E)\oplus\Omega^{k+1}(E),
\]

\[
\mathbb D_K^\uparrow
=
\begin{pmatrix}
D_A&-1\\
K&-D_B
\end{pmatrix}.
\]

These are repo reconstructions. The source selects neither.

Write

\[
A=B+T,
\qquad
F_A=F_B+D_BT+q(T,T).
\]

For the spoken `K=F_B` choice, Bianchi gives

\[
(\mathbb D_{F_B}^\downarrow)^2
=
\begin{pmatrix}
F_A-F_B&-TF_B\\
T&0
\end{pmatrix},
\]

\[
(\mathbb D_{F_B}^\uparrow)^2
=
\begin{pmatrix}
F_A-F_B&-T\\
F_BT&0
\end{pmatrix}.
\]

For the 2021-curvature comparator `K=F_A`,

\[
(\mathbb D_{F_A}^\downarrow)^2
=
\begin{pmatrix}
0&-F_AT\\
T&F_B-F_A
\end{pmatrix},
\]

\[
(\mathbb D_{F_A}^\uparrow)^2
=
\begin{pmatrix}
0&-T\\
TF_A&F_B-F_A
\end{pmatrix}.
\]

Their differential-operator derivation uses graded Leibniz, Bianchi, and the
relation between `D_A` and `D_B`. The executable instantiates the resulting
order/sign algebra exactly on noncommuting rational matrices, retaining the
distinction between `TF_B` and `F_BT`. It does not independently execute
exterior derivatives, graded jets, or Bianchi; that stronger promotion remains
open.

### Coefficient family

The generous downward family

\[
\mathbb D_{K;a,b,c,d}^\downarrow
=
\begin{pmatrix}
aD_A&-bK\\
c&-dD_B
\end{pmatrix}
\]

becomes a common-connection complex only when

\[
a=d,
\qquad
bc=a^2
\]

for nonzero entries. On the sampled generic nonzero stratum, four coefficients
minus these two constraints, the overall operator scale, and unrestricted
relative rescaling of the two shifted summands leave one algebraic similarity
class. This is not yet a physical equivalence: an action-owned Krein pairing
may restrict the allowed relative rescaling. Degenerate zero-coefficient
strata are not classified.

The exact probe checks both directions of the closure criterion over a finite
generic nonzero coefficient grid, accepts the non-unit representative
`a=d=2,b=4,c=1`, rejects a nearby wrong coefficient, and rejects the wrong
curvature-column sign.

## 5. Why the connection cone alone is not the Euler obstruction

There are two independent discriminators against that stronger identification.

### It becomes a complex on the wrong locus

In a faithful representation, every displayed cone square contains `T`, so
nilpotence forces

\[
T=0.
\]

In a nonfaithful representation it only forces `rho(T)=0`; the probe retains
that distinction.

But `T=0` is equality of the endpoint and rotated Levi--Civita connections,
not the complete G2/G3 Euler equation. At `T=0,F_B != 0`, every cone above
vanishes while

\[
E_T=\mathscr S_\epsilon(F_B)\ne0.
\]

At the same locus the previously reconstructed mixed owner satisfies

\[
\widehat H_{AB}=Q_{\rm seg}=F_B,
\]

which remains nonzero. So the minimal cone is a repo-constructed,
source-compatible connection-level equality complex. It cannot, without additional blocks, be
the complete generalized-Einstein obstruction as written.

### Its tested finite residual alphabet is too small

The full Euler owner is

\[
E_T
=\mathscr S_\epsilon(F_B)
+\frac12(L+L^!)T
+M_\epsilon(T,T)
+\kappa_1\flat_1T.
\]

The cone supplies curvature differences, `T`, and ordered products such as
`TF_B`. It does not supply the formal-adjoint response, the noncyclic
two-input polarization `M_epsilon`, or all moving Shiab/Hodge/density and
Levi--Civita graph responses.

B2C8 nevertheless gives the source-motivated fit every chance to work. In the
same nondegenerate twelve-coordinate exact fixture used to reconstruct `E_T`:

\[
\operatorname{rank}
[\mathscr S(F_A),*T]=2,
\]

while

\[
\operatorname{rank}
[\mathscr S(F_A),*T,E_T]=3.
\]

Even the expanded four-column alphabet has

\[
\operatorname{rank}
[\mathscr S(F_B),\mathscr S(D_BT),\mathscr S(q(T,T)),*T]=4,
\]

but adjoining `E_T` raises the rank to five.

Within this fixture, the wedge-pairing reconstruction supplies twelve
independent coordinate outputs. The two tested candidate spans therefore have
compatibility codimensions

\[
12-2=10,
\qquad
12-4=8.
\]

Both fail. A deliberately planted target inside the four-column span passes,
so the matcher is capable of accepting a real fit. These `10` and `8` numbers
are fixture-local codimensions, not globally independent physical constraints
or a complete source-family surplus. The result still avoids the orthodox
reflex that a source-shaped construction is uninformative: a constrained fit
would have passed this exact test, but did not.

Scope:

> The bare four-entry shifted mapping-cone family does not emit the complete
> G2 connection Euler owner under the tested exterior-covariant,
> representation-wedge, active-local-primalizer reading.

This does not kill an unreleased cyclic operator with additional contraction,
moving, fermionic, or lower-order blocks.

## 6. What survives from the 2021 deformation complex

Allowing coefficients, the repo's moving-contraction reading of the 2021
composition has the schematic form

\[
\begin{aligned}
\delta_2\delta_1\gamma={}&
a_1r\,\mathscr S([F_A,\gamma])
+c_1s\,K_\omega[DL_\epsilon\gamma]F_A\\
&+\kappa_1\left(
b_1r*D_A\gamma-d_1s*D_BDL_\epsilon\gamma
\right).
\end{aligned}
\]

Equivariance and the tilted-displacement identity require

\[
a_1r=c_1s,
\qquad
b_1r=d_1s.
\]

After the two compatibility constraints and a common output normalization, one
coefficient ratio remains. Identifying it with the source-owned relative
`kappa_1` scale is the repo's normalized reading; action-pairing equivalence
has not been classified.

For the repo-inferred source-shaped residual this would yield

\[
\delta_2\delta_1\gamma
=\operatorname{ad}^*_{\gamma}R_{21}.
\]

The source does not itself establish that identity for the complete action,
and `R_21 != E_T`. The exact fixture produces a nonzero

\[
Z_{21}=E_T-R_{21}
\]

whose gauge orbit is also nonzero.

The constructive candidate is forced by the action rather than coefficient
fitting. Replace the compressed second arrow, on the connection component, by

\[
\delta_{2,A}=D E_T.
\]

The independently reconstructed connection Euler owner is exactly equivariant
under one finite constant conjugation when all tested finite slots and the
noncentral Shiab insertion move. The probe verifies

\[
E_T^g=gE_Tg^{-1}.
\]

At the infinitesimal algebraic-orbit level, using the probe's convention,

\[
[\gamma,E_T]
=[\gamma,R_{21}]+[\gamma,Z_{21}]
\]

and independently

\[
[\gamma,E_T]
=[\gamma,\mathscr S(Q_{\rm seg})+\kappa_1\flat T]
+[\gamma,Z_{\rm var}].
\]

Both correction orbits are nonzero in the exact fixture. These equalities are
linearity checks for constant conjugation, not executions of `D E_T` on the
local gauge tangent. A durable deformation theorem still requires the local
inhomogeneous gauge law, the derivative term in the gauge transformation, the
coadjoint-to-adjoint identification after the action-owned pairing, and the
full graph tangent. Conditional on those, local gauge invariance would imply

\[
D E_T[R_\gamma]=\operatorname{ad}^*_{\gamma}E_T.
\]

The full-tuple statement for `(E_A,E_epsilon,E_g)` remains B2C9 work.

### Stabilizer and centre boundary

An adjoint deformation square represents the primalized Euler owner through
`ad(E)`. The full parameterized adjoint family is blind to central components;
evaluation at one fixed test parameter is additionally blind to that
parameter's stabilizer. The probe verifies:

- a nonzero central `M_2` Euler component acts trivially; and
- the same adjoint representation is faithful on the three-dimensional
  traceless quotient.

The scalar-centre failure is decisive for the finite `M_2` fixture and is a
warning for a `U`-type draft carrier or reduced `U(1)` component. The latter
conclusion remains conditional on the actual reduction and owner channel. It
is not a no-go for the center-free active `sp(32,32;H)` adjoint; that active
adjoint was not executed in this probe.

## 7. Construction disposition

### Built

- exact source-separated 2021 and 2025 operator registry;
- the two degree-correct minimal shifted placements of the 2025 tokens;
- exact finite algebra representations of the squares for `K=F_B` and the
  `K=F_A` comparator; the full differential-operator/graded-jet identity is
  not executed here;
- sampled generic-nonzero coefficient closure equivalence;
- trace-reversed connection lowerer and local inverse `R_A` in a finite
  architecture control with inherited active-factor checks;
- independent-dual fermion lowerer and frozen local inverse `R_F` at the same
  grade;
- the load-bearing zero-form Hodge sign and `C_+` separation;
- exact fixture-local compatibility-codimension discrimination against the
  connection Euler owner; and
- finite constant-conjugation covariance and exact core/correction orbit
  decompositions supporting a conditional `D E_T` Noether route.

### Not built

- a source-selected 2025 matrix;
- the full differential-operator shifted-cone identity with independent jets,
  graded Leibniz, and Bianchi controls;
- `D E_T` evaluated on the local inhomogeneous gauge tangent, or any full
  graph-tuple Hessian square;
- a same-bundle constrained-real moving fermion operator;
- `R_epsilon` and the metric Euler inverse needed for the full graph tuple;
- off-diagonal `zeta/nu` terms and their independent-dual total action;
- the total fermion current and stress return into `mathfrak G`;
- a common Green domain and completed total preboundary packet;
- observation descent, kernel quotient, reduced Poisson anchor, or prequantum
  line bundle; or
- a stationary vacuum, Standard Model representation recovery, anomaly/index
  theorem, generation count, cosmological amplitude, or PP3 prediction.

No P1, P2, or P3 datum was used. Missing local maps and total-action terms are
construction debts, not external datum.

## 8. Seven-axis disposition

| layer | B2C8 result |
|---|---|
| Layer 0 | 2021 deformation, 2025 cone, curvature, Euler covector, local primalizer, presymplectic sharp, and prequantum lift remain separate |
| L1 geometry | actual symmetric metric fibre and trace reversal give `(9,5)`; literal `(7,7)` remains a rival |
| L2 fields | `A` free, `B=A_LC(epsilon,g)` graph-derived, `T=A-B`; no second independent connection added |
| L3 symmetry | finite constant-conjugation covariance supports a conditional `D E_T` deformation route; the local gauge tangent and full tuple remain open |
| L4 dynamics | the 2025 cone is confirmed as a connection-level complex but rejected as the complete Euler obstruction by itself; the action-complete 2021 enlargement is a conditional target |
| L5 quantum | `R_A/R_F` have finite architecture controls plus inherited active-factor checks; total Green/domain, reduction, Poisson, and prequantization remain open |
| L6 physics | no SM, mass, count, dark-sector, or cosmological recovery claimed |
| L7 evidence | official PDF visual reinspection, 2025 transcript collision, three specialist lenses, and `46 exact + 15 type-level + 11 planted = 72 PASS`; the type-level rows are declarations, not independent computational evidence |

## 9. Highest-information successor

Proceed to

`ECW3D-B2C9-OFFDIAGONAL-SOMATIC-TOTAL-CURRENT-AND-PREBOUNDARY-RETURN`.

The input is now sharper:

1. Construct `D E_T` on the actual local inhomogeneous gauge tangent, fixing
   the coadjoint/primalized sign convention. Keep the 2021 source-shaped row
   as a comparator; do not call the candidate a complex until this composition
   is executed.
2. Keep the 2025 shifted cone as a kinematic two-connection scaffold and
   possible block grammar. Do not use its bare square as the Einstein equation.
3. Enumerate the surviving B2C5 candidate family and source-fork-compatible
   off-diagonal completions in the independent-dual `zeta/nu` action. Vary
   each before selecting `p`, `M_0`, or a southeast block.
4. Construct `R_epsilon` and the inverse metric Euler lowering map so the
   factorization target is the full returned tuple, not only `E_A`.
5. Derive the off-diagonal fermion current and stress return into
   `(E_A,E_epsilon,E_g)` and test whether the Bose and Fermi routes now carry
   one common total Euler owner.
6. Add the fermionic Green term to `Theta_G3`, yielding the selected
   B2C9 boson-plus-fermion-sector `Theta_tot,omega_tot`. Only afterward replay
   the final observation, domain, kernel, charge, and quotient analysis.

The provisional B2C6B/G4 domain work may continue on existing sector packets,
but its final verdict must be recomputed after B2C9 changes the total boundary
form.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; the conjunction
`TG-1 AND TG-2 AND TG-3` remains false.

## Validation boundary

The executable probe reports:

```text
ECW3D-B2C8: 46 exact + 15 type-level + 11 planted = 72 PASS
```

The `46` exact rows are computations. The `11` planted rows are executable
wrong-degree, wrong-sign, wrong-owner, arbitrary-fit, centre, frozen-inverse,
and target-span rejections. The `15` type-level rows are explicit declarations,
not independent computational evidence. The result is a finite algebra
representation plus finite primalizer architecture controls with inherited
active-factor checks. It is not a differential or analytic operator theorem on
the actual `Y^14` bundle.
