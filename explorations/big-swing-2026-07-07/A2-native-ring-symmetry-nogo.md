---
artifact_type: exploration
status: exploration
created: 2026-07-07
title: "A2 (route A2, the no-go twin), FINISHED as a BOUNDED no-go: the strong no-go is REFUTED and the weak one is PROVEN. The native invariant ring on the P-even condensate space (generated by t_n = tr(Phi^n) and q_n = Str(Phi^n) = tr(P Phi^n)) has invariance group Inv = U(96) x U(96) (block-diagonal K-unitaries), which FIXES the Q5 axis -- so 'the ring is blind to the mirror direction' is FALSE and A1's scan is live. But under the GU-native intertwiner chi (Clifford volume; unitary involution; commutes with all 91 gauge generators; chi P chi = -P, chi Q5 chi = -Q5) the invariants split by parity: t_n chi-EVEN, q_n chi-ODD. The mirror-hiding order parameter q2 is chi-odd, so every chi-SYMMETRIC native potential has a chi-symmetric (blind or degenerate) vacuum and cannot select which half gaps; selection requires a chi-ODD native coupling whose sign chi flips. The alignment import is exactly ONE non-native BIT -- A1's sign(lq + l4/192) = A4's sign of tr(Q5 Phi^2), seen from the symmetry side."
grade: "THEOREM (bounded no-go, kinematic/potential scope; 15/15 checks, powered controls, main-loop run to exit 0). This REPLACES the original A2 route, whose build agent hit the 64k output-token cap and produced no result. Honest reframing: A1 and A4 already exhibit native potentials that DO select the mirror-hiding vacuum, so the STRONG no-go A2 set out to prove is false; the bounded no-go proven here is the true and complementary statement. No forbidden target imported; no dynamics; every statement 'the native ring / potential class forces X', never 'GU forces X'. Count untouched, stays OPEN."
depends_on:
  - explorations/big-swing-2026-07-07/A1-native-potential-alignment.md
  - explorations/big-swing-2026-07-07/A3-orientation-z2.md
  - explorations/big-swing-2026-07-07/A4-basin-stability.md
  - explorations/big-swing-2026-07-06/VG-V8-t5-map-attempt.md
  - canon/ghost-parity-krein-synthesis.md
scripts:
  - tests/big-swing/as_a2b_native_ring_symmetry_nogo.py
---

# A2: the no-go twin, finished as a bounded no-go

**Route A2 of the 2026-07-07 alignment swing.** The original A2 route (the adversarial twin of A1:
"prove no GU-native invariant potential can see the alignment datum") hit the 64k output-token cap
during its build and produced no result. Finished here in the main loop, following the A1 method:
reduce analytically, confirm fast, run to exit 0. Script:
`tests/big-swing/as_a2b_native_ring_symmetry_nogo.py`.

## The honest reframing

A2 was designed to attempt the STRONG no-go. But A1 (the mirror-hiding corner is the global minimum on
the open coupling region `lq < -l4/192`) and A4 (the aligned configuration is the global minimum under
`tr(Q5 Phi^2)` with `h < -m^2`) already EXHIBIT native potentials that select the mirror-hiding vacuum.
So the strong no-go is false, and stating it would contradict the swing's own theorems. The true,
complementary statement -- the bounded no-go -- is what this route proves.

## What is proven (15/15 machine-checked, both the refutation and the no-go)

1. **Invariance group `Inv = U(96) x U(96)`.** The native invariant ring is generated by
   `t_n = tr(Phi^n)` (chi-even) and `q_n = Str(Phi^n) = tr(P Phi^n)`. The group preserving all of them
   is `Inv = { K-unitary S : [S,P] = 0 }` = block-diagonal unitaries on the two Krein halves
   (verified: `||[S,P]|| ~ 3e-14`, `||S^K K S - K|| ~ 6e-14`, all `t_n, q_n` invariant to `~1e-12`).
2. **`Inv` FIXES the Q5 axis** (`||S Q5 S^-1 - Q5|| ~ 6e-14`). So the ring is NOT blind to the mirror
   direction -- **the STRONG no-go is refuted and A1's scan is live.**
3. **`chi` is a native symmetry outside `Inv`.** A3's Clifford-volume intertwiner is a unitary
   involution (`chi^2 = I`, `tr chi = 0`), commutes with the sampled so(9,5) gauge generators
   (`~5e-14`), satisfies `chi P chi = -P`, `chi Q5 chi = -Q5`, and is norm-reversing (`{chi,P}=0`,
   `||[chi,P]|| = 27.7 > 0`), so `chi ∉ Inv`.
4. **Parity split:** `t_n` are **chi-even**, `q_n` are **chi-odd** (both to `~1e-12`). The mirror-hiding
   order parameter `q2 = tr(P Phi^2) = tr(A^2) - tr(B^2)` is chi-odd.
5. **The bounded no-go.** Every chi-symmetric native potential (even in the `q`'s) has a chi-symmetric
   vacuum: the two mirror-hiding corners `(u,0)` and `(0,v)` are exactly degenerate (`V(u*,v*) =
   V(v*,u*)` to `0.0e+00`), so which half gaps is not selected. A chi-ODD native coupling
   (`tr(Q5 Phi^2)`-type) selects the orientation, and its **sign** is what chi flips
   (`V(u,0;+h) = V(0,u;-h)` exactly) -- so the sign is fixed by no native invariant.

**Conclusion.** The alignment import is exactly **one non-native bit**: the sign of the flip-odd
coupling, which is `A1`'s `sign(lq + l4/192)` and `A4`'s sign of `tr(Q5 Phi^2)` seen from the symmetry
side. Controls discriminate: a generic imported P-even operator does NOT commute with the gauge algebra
(`||[D,gauge]|| ~ 368`), and a scrambled weight `tr(R Phi^2)` is not `Inv`-invariant -- so nativeness is
exactly the property that forbids a fixed selecting sign.

## Where this leaves the swing

A1 (phase, THEOREM), A3 (orientation discharged, THEOREM), A4 (basin, THEOREM), and A2 (bounded no-go,
THEOREM) now agree from four directions: the mirror-hiding vacuum is native in direction, positivity, and
stability, and undetermined only in a single orientation-odd coupling sign that no kinematics or invariant
fixes -- a question for the unbuilt source action. Count untouched; stays OPEN.
