---
artifact_type: exploration
status: exploration (TEAM BRANCH3-COEFFICIENTS, W203; five personas inline, one worker, no sub-agents; deterministic test, 32/32 exit 0, positive controls first)
created: 2026-07-14
wave: W203
label: W203
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (report value under either outcome); RUTHLESS self-verification; tri-repo gating STRICT
route: LEGENDRE / AUXILIARY-FIELD (extend W167 T* = -shiab(F); NOT a full-functional brute force)
title: "W203 -- the BRANCH-3 source action with FIXED coefficients (debit-3). VERDICT: BRANCH-3-ACTION-BUILT / COEFFICIENTS-FIXED-UP-TO-ONE-NORMALIZATION-WHOSE-SIGN-IS-#1. W180 discharged C3 CONDITIONAL with the bridge S_bridge = (1/2kappa)<theta, M theta> - <theta, J[Psi]> but FLAGGED M = 'the Frobenius Gram' as a modelling choice. W203 turns the conditional bridge into a branch-3 dynamical-IG source action and PINS the coefficients via the Legendre route (theta is Gaussian, so theta* = kappa M^{-1} J and S_eff = -(kappa/2)<J, M^{-1} J> exactly). THE COEFFICIENT PIN (new, machine-checked): the kernel M is NOT free -- EQUIVARIANCE under the Krein-anti-self-adjoint gauge action (the same Sigma the program is built on) forces M by Schur; the space of equivariant symmetric kernels on the 14-frame is EXACTLY one-dimensional (nulldim=1) and its generator is the CLIFFORD METRIC eta (signature (9,5), INDEFINITE), NOT the positive-definite Gram W180 used (the Gram is invariant only under the maximal COMPACT subgroup). So every RELATIVE coefficient is FORCED (M by Schur, the source coupling by the C3 identity J = delta S_D/delta A, the potential absent); exactly ONE coefficient is undetermined -- the overall scale kappa = the eta-from-gimmel-area (W151) = Newton's-G normalization, undetermined-BY-NORMALIZATION (like G in GR), NOT fitted-to-a-data-window, so W203 does NOT repeat debit-2's fitted-epoch failure mode. THE KREIN CONSEQUENCE: because the forced M is indefinite, the induced self-energy sign is a genuine Krein readout, sign(S_eff) = -sign(kappa)*sign(J.eta.J); J.eta.J is TWO-SIDED on generic Psi (W180's 'structural Legendre minus gives S_eff<0' was an artifact of the positive Gram) and pinned POSITIVE exactly when the record current is confined to the C-positive subspace. Under (9,5)=(3,1)+(6,4) the (6,4)-fiber part is the DeWitt-fiber Krein bit W168/W202 compute as #1, so the branch-3 sign REDUCES to #1 by an independent route. HONEST LIMITS: the action still SECRETLY ASSUMES W154 (the linear coupling hard-codes 'theta is sourced by the RECORD current' = the marble/wood identification); the residue is the same named eta-magnitude bridge (Z_U nonlocal completion, W180 ultralocal) plus the interacting C-operator (#1). No fifth object; no canon movement; count {1,3}; H41 unbuilt (narrowed)."
grade: "exploration / STRONG on the coefficient pin (Schur uniqueness nulldim=1 and the signature (9,5) are exact linear algebra, machine-checked) and on the Krein consequence (the two-sidedness on generic Psi and the C-positive pinning are direct computations); STRUCTURAL on the branch-3 mapping (the identification of S_bridge with the Cycle-1 Branch-3 first-order parent schema is a schema match, and the completion to the nonlocal Z_U term is NOT built). COMPUTED (deterministic, tests/W203_branch3_source_action_fixed_coefficients.py, 32/32 exit 0, positive controls first): PC1 W131 exact algebra + Krein anti-self-adjointness of all 91 Sigma (the gauge action the kernel respects); PC2 the (9,5)=(3,1)+(6,4)/q=5 split; PC3 W180's C3 identity S_D=A.J and delta S_D/delta A=J; PC4 W180/W167 Legendre minus with the Hilbert Gram. NEW-DERIVED: KER1 Schur uniqueness nulldim=1; KER2/KER3 the forced kernel signature (9,5) and its proportionality to eta; KER4 the Hilbert Gram is NOT equivariant under the full gauge action; SGN1 J.eta.J two-sided on generic Psi; SGN2 the Gram pairing always positive (the suppression artifact); SGN3 the C-positive subspace pins the sign positive; FIB1-FIB3 the (3,1)+(6,4) block decomposition of the pairing; COEF1-5 the coefficient ledger (relative forced; one scale undetermined-by-normalization, not target-fitted); W154-1/2 the localized W154 dependence. CITED (not re-derived): W180 (the bridge, the C3 identity, theta_induced=kappa M^{-1}J), W167 (the Legendre minus T*=-shiab(F)), W161 (GU's linear April-2021 law, c_R=0), W154 (the reverse-engineered marble/wood identification), W151 (route-beta / the eta-from-gimmel-area bridge, only the sign forced), W168/W202 (the reservoir Krein sign #1 on the (6,4) DeWitt fiber and its signature-robustness), W132/W137 (records = C-positive subspace), W150 (the q=5 finality frontier), the Cycle-1 Branch-3 dynamical-IG current gate (the S_IG_dyn parent schema and its provenance ledger). No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the debit count stays {1,3}; H41 unbuilt; H59 OPEN; no forbidden target {3,8,24,chi(K3),Ahat} assumed or inserted. Zero em dashes."
construction: "program-native where the objects are GU's (Y14, the record field Psi in carrier B / ker Gamma, the Krein/C-operator structure K_S, the (9,5) split and its (3,1)+(6,4) base/DeWitt-fiber decomposition, the q=5 finality frontier, the promotion-gate current J, the displaced-torsion / connection-distortion theta, the shiab-Einstein projection). Standard-field where the machinery binds any construction (Schur's lemma fixing the invariant bilinear form on an irreducible rep; the Legendre / auxiliary-field elimination of a Gaussian field -- PORTED, reused from W122/W167; the induced-gravity / equation-of-state stance that the connection has no fundamental kinetic term -- PORTED Sakharov/Jacobson; the everpresent-Lambda sign -- PORTED Sorkin). Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md; the reverse-engineered dark-energy-is-the-record-current identification (W154 marble/wood) is carried and named as the standing conditionality; the ultralocal (algebraic eta kernel) vs nonlocal (Z_U gradient / induced-YM kernel) fork is carried and named as the magnitude residue; the C-positive-subspace-proxy fork (the +1 eigenspace of K_S vs the interacting C-operator) is carried and named as exactly question #1. Tri-repo gating STRICT: the signature and the fiber Krein grade are computed GU-side; whether the Krein grading is physically operative is flagged TaF-owned; no cross-repo identity asserted."
depends_on:
  - explorations/W180-build-matter-connection-bridge-c3-2026-07-14.md
  - explorations/W167-reduction-direct-sign-alpha-beta-2026-07-14.md
  - explorations/W161-lens-foundational-action-2026-07-14.md
  - explorations/W154-reverse-engineered-source-action-2026-07-14.md
  - explorations/W160-debit3-forced-binary-2026-07-14.md
  - explorations/W202-signature-crux-bach-branch-2026-07-14.md
  - explorations/cycle-gates-and-audits/cycle1-branch3-dynamical-ig-current-gate-2026-06-24.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W203_branch3_source_action_fixed_coefficients.py
---

# W203 -- the branch-3 source action with fixed coefficients (debit-3)

Test: `tests/W203_branch3_source_action_fixed_coefficients.py` (32/32, exit 0). Deterministic
(seed 20260714); the algebra runs on the repo's verified `Cl(9,5) = M(64,H)` representation via
`gen_sector_bridge`. Five personas ran inline in one worker, sequentially (no sub-agents):
(1) variational-calculus specialist, (2) geometer, (3) coefficient-pinning specialist,
(4) coherence-first synthesizer, (5) skeptic. The four positive controls run first.

## 0. The object, and the smart route

Debit-3 is "the source action whose variation gives the connection distortion `theta` = the
Euler-Lagrange derivative of a gauge-invariant action" (C3 of the 2026-06-22 divergence-free proof).
The Cycle-1 gate named the missing object precisely: a **source-forced dynamical-IG action**
`S_IG_dyn[A, eps, U, ...]` with FIXED coefficients, whose connection equation reads
`D_A* F_A = theta_eff` with `theta_eff = g_A^2(c_theta theta - J_IG)` -- the "Branch 3" total-current
route. W180 got most of the way there: it built the matter->connection bridge

```
S_bridge[Psi, theta]  =  (1 / 2 kappa) <theta, M theta>_Krein  -  <theta, J[Psi]>_Krein ,
delta/delta theta = 0   =>   theta_induced = kappa M^{-1} J[Psi] ,   S_eff = -(kappa/2)<J, M^{-1} J> ,
```

discharging C3 CONDITIONAL. But W180 explicitly flagged ONE modelling choice (its Section 4): "`M`
= Gram (the Frobenius Gram of the frame) is a fixed positive equivariant kernel standing in for the
induced-YM kernel." That is the coefficient debit-2 warned about: an object left FITTED (or
hand-chosen) rather than DERIVED. W203's job is to pin it.

**The route is the Legendre / auxiliary-field elimination, not a 14-dimensional brute force** (Joe's
2026-07-14 redirect, honored). `theta` is Gaussian in `S_bridge`, so its elimination is exact and
two-line -- the SAME structural move as W167's `T* = -shiab(F)`. The branch-3 source action is

```
S[Psi, A, theta]  =  Re<Psi, K_S c(A) Psi>_Krein   +   (1/2 kappa) <theta, M theta>_Krein   -   <theta, J[Psi]>_Krein
                     |___________________________|       |_______________________________________________________|
                       S_D  (the C3 identity leg)          S_bridge  (the branch-3 dynamical-IG leg)
```

with `c(A) = sum_a A_a e_a` the Clifford contraction of the connection, `K_S = e_0 e_1 ... e_8` the
spinor Krein form, and `J^a = Re<Psi, K_S e_a Psi>` the promotion-gate record current.

## 1. Persona 1 (variational-calculus specialist) -- the first variation, and the two field equations

Two stationarity conditions, both exact.

- **`delta S / delta A = 0`.** `S_D = Re<Psi, K_S c(A) Psi>` is EXACTLY linear in `A` (test `PC3a`:
  `S_D(A) = A . J`, residual `4.3e-14`), so `delta S_D / delta A_a = J^a` (test `PC3b`, residual
  `2.8e-8`, finite-difference-limited). This is W180's C3 identity reproduced: the record current IS
  the EL derivative of an explicit gauge-invariant action, so `theta := J` discharges C3's literal
  statement. Restoring the `F`-dependence of `S_D` through minimal coupling, the connection equation
  is the same-source Yang-Mills law `D_A* F_A = J` (SA-G9, the marble/wood same-source closure).
- **`delta S / delta theta = 0`.** `S_bridge` is Gaussian in `theta`, so `(1/kappa) M theta = J`, i.e.
  `theta_induced = kappa M^{-1} J[Psi]` -- a FUNCTIONAL of the record field. Substituting back gives
  the record-current self-energy `S_eff = -(kappa/2) <J, M^{-1} J>` with the structural Legendre MINUS
  (test `PC4`, the W167/W180 control). The boundary term is the promotion-gate `S_gate = n_a J^a`
  whose gauge variation over `Stab(n)` vanishes (Noether II, `D_A* J = 0`); nothing in the boundary
  construction is disturbed.

So the branch-3 action reproduces the Cycle-1 first-order parent schema: `theta_eff = J[Psi]`, the
connection sourced by the record current. What was left open at Cycle-1 was the coefficient list. That
is Sections 2-3.

## 2. Persona 2 (geometer) -- the shiab / Legendre picture, and where the kernel comes from

The Legendre elimination here is the record-field analogue of W167's torsion elimination. W167:
GU's linear April-2021 action is Gaussian in the displaced torsion `T`, so `T* = -shiab(F)` and
`S_eff = -(1/2)|shiab(F)|^2`, a Ricci-class (Weyl-annihilated) self-energy. W203: the connection
distortion `theta` plays the role of `T`; it is Gaussian; `theta* = kappa M^{-1} J` and
`S_eff = -(kappa/2)<J, M^{-1} J>`. Both carry the same structural minus; both are exact because the
eliminated field is algebraic (no fundamental kinetic term -- the induced / Sakharov-Jacobson stance).

The geometer's decisive observation is about the KERNEL `M`. `M` is the metric on the space the
connection distortion lives in -- `ad P`-valued, indexed by the 14-frame `a`. The gauge group acts on
that index by the VECTOR representation: `[e_a, Sigma_ij] = sum_b (T_ij)_{ba} e_b` (the closure onto
`span(e)` is W158's `SG2`, reproduced as `KER0`, residual `0`). A kernel `M` that is to be gauge-
covariant (so that `theta_induced = kappa M^{-1} J` transforms as `theta` must) has to be EQUIVARIANT:
`T_ij^T M + M T_ij = 0` for every generator. The frame carries the IRREDUCIBLE vector rep of
`so(9,5)`, so by Schur the invariant symmetric form is UNIQUE up to scale -- and for the vector rep
that invariant is the defining metric. **The kernel is not a choice; it is the Clifford metric `eta`.**

## 3. Persona 3 (coefficient-pinning specialist) -- what is forced, what is free, what is fitted

The central computation (test block `[KER]`): solve the equivariance constraint over the 105-dim space
of symmetric `14x14` kernels.

- **`KER1`: the equivariant kernel space is EXACTLY one-dimensional** (nulldim `= 1`). Schur, verified.
- **`KER2/KER3`: the unique kernel has signature `(9,5)` and is proportional to `eta`** (diagonal
  `(+1 x9, -1 x5)`, off-diagonal `0` to `6.7e-16`). It is **INDEFINITE**.
- **`KER4`: the Hilbert-Frobenius Gram is NOT equivariant** (`T^T Gram + Gram T != 0`). W180's
  positive-definite `M = Gram` is invariant only under the maximal COMPACT subgroup `SO(9)xSO(5)`, not
  the full non-compact gauge group. So W180 used a kernel that respects less symmetry than the theory
  has; the correct, fully-equivariant kernel is the indefinite `eta`.

The coefficient ledger for the whole branch-3 action (test block `[COEF]`), in the Cycle-1
provenance vocabulary (`source_forced | geometry_forced | normalization | imported_control |
target_fitted`):

| coefficient | status | what fixes it |
|---|---|---|
| kernel `M` (shape) | **geometry_forced** | Schur / equivariance -> `M ~ eta` uniquely (`KER1/KER3`) |
| source coupling `<theta, J>` | **geometry_forced** | the C3 identity `J = delta S_D/delta A` (`PC3`, W180) |
| potential `V(theta)` | **absent** | `theta` is Gaussian; no self-interaction coefficient exists |
| overall scale `kappa` (`= c_theta^{-1}` up to `M`'s scale) | **normalization** | `= eta-from-gimmel-area` (W151); any gimmel-volume derivation uses native `lambda_GU=1/2` ([normalization ledger](gimmel-dewitt-normalization-ledger-2026-07-20.md)); SIGN = the Krein datum (Section 4); MAGNITUDE undetermined |
| gradient stiffness `Z_U` (`\|D_A U\|^2`) | **NOT BUILT** | the nonlocal completion; W180/W203 are ultralocal (Section 5) |

The honest headline: **all RELATIVE coefficients are forced; exactly ONE coefficient is undetermined
-- the overall scale `kappa`.** And `kappa` is undetermined **by NORMALIZATION** (it sets Newton's `G`
via Jacobson's route, which DEFINES `G` from the entropy density rather than predicting a
dimensionless number), **NOT fitted to a data window**. No epoch, no DESI/Rubin target, nothing from
the observable side enters the action (test `COEF5`). **This is the crucial difference from debit-2's
failure mode.** Debit-2 failed because the crossing epoch `z = 0.405` was a genuine free parameter
matched to DESI (W160 proved it PROVABLY free). W203's `kappa` is a different animal: a single overall
scale that every induced-gravity theory carries and that no data window pins. So branch-3 with fixed
coefficients is NOT a repeat of debit-2 -- provided one distinguishes "undetermined normalization"
(acceptable, like `G`) from "fitted parameter" (the debit-2 disease). The one thing that IS a genuine
physics datum, not a convention, is `kappa`'s SIGN -- and that is Section 4.

## 4. Persona 4 (coherence-first synthesizer) -- does the built action cohere the picture, or break it?

Build the fitting story first. The records-fundamental / derived-Lambda / tachyon picture wants:
(i) the connection/metric to be a shadow of the record field (route-beta); (ii) the induced Einstein
term ATTRACTIVE (`eta > 0`, `a1 > 0`, W151); (iii) the same-source closure `D_A* F = J`. The branch-3
action delivers all three by construction: `theta_induced = kappa M^{-1} J` makes the connection a
functional of `Psi`; the boundary term is gauge-invariant; the field equation is same-source. So far
it COHERES -- and it coheres BETTER than W180 because the kernel is now forced, not chosen.

But building the action HONESTLY surfaces a sign subtlety that W180's positive Gram had hidden, and
this is where coherence is tested. With the FORCED indefinite kernel `M ~ eta`, `M^{-1} = eta` (up to
scale), so `S_eff = -(kappa/2c) (J . eta . J)`, and the sign is

```
sign(S_eff)  =  -sign(kappa) * sign( J . eta . J ) ,     J . eta . J = sum_{9 pos} J^2 - sum_{5 neg} J^2 .
```

Test block `[SGN]`:

- **`SGN1`: on generic `Psi` the pairing `J.eta.J` is TWO-SIDED** (both signs occur over 400 draws).
  The induced self-energy sign is a genuine `(9,5)` Krein readout -- it is NOT structurally negative.
- **`SGN2`: with the Hilbert Gram the pairing is ALWAYS positive.** This is exactly why W180 read a
  fixed "structural Legendre minus gives `S_eff < 0`": the positive-definite kernel SUPPRESSED the
  Krein grading. That fixed-sign reading was a kernel artifact, not a theorem.
- **`SGN3`: on the C-positive subspace the pairing is FORCED positive** (100% over 400 draws; the
  C-positive subspace proxied by the `+1` eigenspace of the Hermitian Krein form `K_S`).

So the coherent story does NOT break -- but it sharpens. The attractive/healthy sign that the picture
wants is delivered **IF AND ONLY IF the record current is confined to the C-positive subspace**, which
is precisely the standing GU postulate "records = the C-positive subspace" (W132/W137). W180 asserted
the sign was forced by C-positivity; W203 shows WHERE that assertion actually bites (it pins the
otherwise two-sided eta-pairing) and grounds it on the ACTUAL indefinite kernel instead of papering it
over with a positive Gram. The picture coheres, and the one genuine physics bit it now rests on is the
C-positive-subspace question -- which is question #1 (Section 6).

## 5. Persona 5 (skeptic, RUTHLESS) -- does W203 re-assume W154? is a coefficient still fitted? is "branch-3" real here?

Three sharp challenges, answered honestly.

- **Does the action still assume W154?** YES, and W203 does not pretend otherwise (test block
  `[W154]`). The linear coupling `-<theta, J[Psi]>` hard-codes that `theta` is sourced by the RECORD
  current `J`. The genuine Cycle-1 Branch-3 current is `J_IG = [U, P]` -- an IG-sector current arising
  from the IG sector's OWN parent field `P`, with NO a-priori relation to the record field.
  Identifying `J_IG = J[Psi]` is EXACTLY the reverse-engineered marble/wood identification (W154:
  dark energy = the record content), which is the unbuilt eta-from-gimmel-area bridge. So W203 does
  NOT remove the W154 dependence; it LOCALIZES it -- the action's entire `Psi`-dependence enters
  through the single object `J` (test `W154-1`: `theta = 0` when `J = 0`). This is progress of a
  limited kind: the assumption is one identification, named and isolated, not diffused through the
  construction. But asserting the action is asserting W154, still a HYPOTHESIS.
- **Is a coefficient still fitted rather than derived?** The relative coefficients are derived (Schur;
  the C3 identity). The one undetermined coefficient `kappa` is NOT fitted-to-data -- no target window
  enters (`COEF5`). Its SIGN is a genuine physics datum (the Krein grade), not a convention, and it is
  NOT freely chosen: it is forced positive IF records are C-positive and otherwise two-sided. So the
  honest statement is "no coefficient is FITTED; one coefficient's MAGNITUDE is undetermined-by-
  normalization and its SIGN is gated on question #1." That is materially better than debit-2 (a
  genuinely fitted epoch) and I decline to conflate the two.
- **Is "branch-3" well-defined here, or just a label?** Partly a label, and I will not overclaim. The
  action realizes the Cycle-1 Branch-3 FIRST-ORDER PARENT schema at its ULTRALOCAL order: the kinetic
  / gradient-stiffness term `Z_U |D_A U|^2` of the full parent action is NOT built -- W180/W203 use an
  algebraic (zero-gradient) kernel. The full branch-3 action has a second coefficient `Z_U` (the
  theta gradient stiffness), which is the nonlocal completion and is where the eta-MAGNITUDE lives.
  So "branch-3 source action with fixed coefficients" is honestly "the ultralocal truncation of the
  branch-3 parent action, with its one algebraic coefficient forced and its one gradient coefficient
  un-built." The NEXT concrete blocker is `Z_U` (the induced-YM kernel `D_A* F`, nonlocal), and it is
  the SAME already-named eta-magnitude bridge (W151), not a fifth object.

## 6. Synthesis -- the return data

| Field | Result |
|---|---|
| the branch-3 source action | `S = Re<Psi, K_S c(A) Psi> + (1/2kappa)<theta, M theta> - <theta, J[Psi]>`; `theta` Gaussian; Legendre-eliminated: `theta* = kappa M^{-1} J`, `S_eff = -(kappa/2)<J, M^{-1} J>`, connection law `D_A* F = J`. Realizes the Cycle-1 Branch-3 first-order parent schema at ULTRALOCAL order. |
| swing result | **BRANCH-3-ACTION-BUILT / COEFFICIENTS-FIXED-UP-TO-ONE-NORMALIZATION-WHOSE-SIGN-IS-#1.** |
| what fixes the coefficients | the kernel `M` by Schur / equivariance (`M ~ eta`, INDEFINITE, unique up to scale -- correcting W180's positive Gram); the source coupling by the C3 identity `J = delta S_D/delta A`; the potential is absent (`theta` Gaussian). All RELATIVE coefficients forced. |
| any coefficient still fitted? | NO coefficient is fitted-to-data. Exactly ONE (`kappa` = the eta-from-gimmel-area, W151) is undetermined-BY-NORMALIZATION (Newton `G`); its MAGNITUDE is convention, its SIGN is question #1. The gradient stiffness `Z_U` is un-built (the nonlocal completion). |
| does it remove the W154 dependence? | NO. The linear coupling `-<theta, J[Psi]>` hard-codes "the branch-3 IG current = the record current" = the reverse-engineered marble/wood identification. LOCALIZED (all `Psi`-dependence through `J`), not removed. |
| effect on debit-3 | W180's DISCHARGED-CONDITIONAL is FIRMED: the conditional bridge is now a written branch-3 action with forced coefficients; the residue narrows to one normalization whose sign is #1. Still conditional on W154. No canon movement. |
| effect on bar (b) | UNCHANGED. The `kappa` sign here is the attractive-Einstein / reservoir-Krein object; the scalaron `c_R` sign of W167 is a distinct object; no conflation. |
| tests + exit codes | `tests/W203_branch3_source_action_fixed_coefficients.py` -- 32/32 PASS, exit 0 (four positive controls first). |

## 7. What this does NOT do (the honest limits)

- Exploration grade; conditional register throughout. Nothing asserts GU, a vacuum, or that the DESI
  wiggle is real. The discharge remains CONDITIONAL on the W154 identification (a hypothesis).
- The induced-gravity / equation-of-state stance (`theta` has no fundamental kinetic term), the
  Legendre elimination, and the everpresent-`Lambda` sign are PORTED and labelled (Sakharov;
  Jacobson; Wands; Sorkin; reused from W122/W167). The GU-specific content is the Schur uniqueness of
  the kernel on the Krein-anti-self-adjoint gauge action, the `(9,5)` indefinite signature, and the
  C-positivity sign pin.
- The action is the ULTRALOCAL truncation of the branch-3 parent action. The gradient stiffness `Z_U`
  (the nonlocal induced-YM kernel `D_A* F`) is NOT built; it is where the eta-MAGNITUDE lives and it
  is the SAME already-named route-beta bridge, not a fifth object.
- The C-positive-subspace pin uses a PROXY (the `+1` eigenspace of the Hermitian Krein form `K_S`) for
  the actual interacting C-operator. Whether the interacting C-operator grades the record current
  positive is exactly question #1 and is NOT resolved here.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; H41 unbuilt (narrowed:
  coefficients pinned, sign reduced to #1, magnitude on the eta-bridge); H59 OPEN; the count stays
  `{1,3}`. Tri-repo gating STRICT: the signature and the fiber Krein grade are computed GU-side;
  whether the grading is physically operative is TaF-owned; no cross-repo identity asserted. No
  forbidden target assumed or inserted. Zero em dashes in paper-facing text.

## What this teaches toward the Krein-sign question (#1)

Question #1 is the reservoir Krein SIGN: whether `[P_ghost, S] = 0` / the interacting C-operator
exists / the Krein grading is physically operative -- the single cross-repo bit that clears or
re-poses bar (b). W203 sharpens the map from the source-action side, four ways.

1. **The branch-3 self-energy sign REDUCES to #1, by an independent route.** Building the action with
   the equivariance-FORCED kernel `M ~ eta` (indefinite) makes the induced record-current self-energy
   sign a genuine `(9,5)` Krein readout, `sign(S_eff) = -sign(kappa) * sign(J . eta . J)`. This is not
   an analogy: the sign literally IS the Krein grade of the record current. So the coefficient-pinning
   route lands on the SAME decisive bit as W167 (direct reduction) and W168 (Krein signature) -- three
   independent routes converging on one object.

2. **The C-positive subspace is the concrete operator #1 asks about.** `J . eta . J` is two-sided on
   generic `Psi` but FORCED positive on the C-positive subspace (test `SGN3`, 100%). So "is the Krein
   grading physically operative on records?" = "is the record current confined to the C-positive
   subspace?" is precisely the bit that fixes `kappa`'s sign. #1 is not an abstract sign convention
   here; it is the question of whether the interacting C-operator grades `J` -- and if it does, the
   branch-3 induced Einstein term is attractive/healthy.

3. **The (9,5) = (3,1) + (6,4) decomposition ties this to W168/W202's fiber bit.** `J . eta . J`
   splits additively into a base `(3,1)` part and a DeWitt-fiber `(6,4)` part (test `FIB3`). The
   fiber part is the `(6,4)` Krein grade on which W168 computes the reservoir sign and which W202
   proves is INVARIANT under the `(9,5)`/`(7,7)` signature crux. So the branch-3 sign's fiber
   component is exactly the signature-robust #1 bit: **fixing the branch-3 action does not require
   resolving the signature crux, and resolving #1 settles the branch-3 sign.** The two sibling swings
   and this one are looking at one bit from three sides, and it is robust to the ambiguities each
   found.

4. **A caution W203 removes from the record.** W180's "structural Legendre minus => `S_eff < 0`" was a
   kernel artifact (the positive Gram, `SGN2`), NOT evidence that the reservoir sign is settled. With
   the correct indefinite kernel the sign is openly two-sided and openly gated on #1. So one should
   NOT count W180's self-energy negativity as an independent argument that bar (b) clears; it collapses
   onto #1 like everything else. The net teaching: **the branch-3 source action is built and its
   coefficients are fixed up to a single normalization whose SIGN is #1 -- so the decisive question is
   now cleanly isolated as the one bit that, once known, turns the built action healthy or tachyonic.**
   If the interacting C-operator exists and grades records positive, the branch-3 induced gravity is
   attractive and bar (b)'s record-sector obstruction clears; if the grading is not physically
   operative, the sign is undetermined and bar (b) is re-posed -- the same fork W167/W168/W202 reach,
   now reached from the fully-built source action.

*Filed 2026-07-14 by Team BRANCH3-COEFFICIENTS (W203). Route: Legendre / auxiliary-field (extend
W167). Five personas inline in one worker (variational-calculus specialist; geometer; coefficient-
pinning specialist; coherence-first synthesizer; skeptic); no sub-agents. Reproducible:
`python -u tests/W203_branch3_source_action_fixed_coefficients.py` (32/32, exit 0; four positive
controls first). Exploration grade; conditional register; RUTHLESS self-verification; no canon
movement; tri-repo gating strict; the debit count stays {1,3}; H41 unbuilt (narrowed); bar (b)
UNCHANGED. E1: the branch-3 source action is built and its coefficients pinned to one normalization
whose sign is the reservoir Krein bit #1; the residue is the SAME already-named eta-magnitude bridge
(the nonlocal Z_U completion) plus the interacting C-operator (#1), not a fifth object.*
