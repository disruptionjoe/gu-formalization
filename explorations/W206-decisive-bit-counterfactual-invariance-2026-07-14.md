---
artifact_type: exploration
status: exploration (R16 COUNTERFACTUAL-INVARIANCE / metric-from-stabilizer; one worker, viewpoints inline, no sub-agents; deterministic test 29/29 exit 0, positive controls first)
created: 2026-07-14
wave: W206
label: W206
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (a clean RESIDUAL-BIT negative is exactly as valuable as FORCED; no rooting); RUTHLESS self-verification; tri-repo gating STRICT
route: R16 -- COUNTERFACTUAL-INVARIANCE / METRIC-FROM-ITS-STABILIZER (Klein / Erlangen). One of five parallel siblings attacking the SAME decisive bit by different methods (R16 here; R9 BRST, R7 Lawvere, R12 topos, R1 Helmholtz siblings).
n_number: "W206 chosen. W205 is the current max on origin/main (0dae045). W206 is the next free number and this note says so; concurrent siblings take W207..W210. If a staging race collides, rebase absorbs and the next free number above W210 is used."
title: "W206 -- the decisive Krein-grading-SIGN bit via COUNTERFACTUAL-INVARIANCE (R16, the W205 Condorcet winner). QUESTION: take the good-stable fixed point as GIVEN; its STABILIZER G* = counterfactual/gauge deformations preserving ALL good-stable properties (real total spectrum + positive-definite TOTAL metric eta_+ + C-operator operative); compute the space of G*-invariant symmetric bilinear forms; is it dim 1 with generator eta_+ = eta.C (FORCED: grading sign derived-as-invariant, #1 closes, bar (b) clears) or dim>1 (RESIDUAL-BIT-STANDS: the grading sign is Godel-independent, must be posited). VERDICT: RESIDUAL-BIT-STANDS. Preserving BOTH the indefinite eta AND the positive-definite eta_+ = |eta| forces a deformation to commute with C = sign(eta), so G* = so(9,5) cap o(14) = so(9)+so(5) = the MAXIMAL COMPACT subgroup (W203's SO(9)xSO(5)). Under G* the 14-frame vector rep is REDUCIBLE R^9 (+) R^5 (two INEQUIVALENT irreducibles), so by Schur the invariant symmetric forms are block-diagonal-and-scalar: dim EXACTLY 2, basis {eta_+, eta} = {I_14, C}. BOTH the C-graded positive-definite eta_+ (record block +) AND W203's ungraded indefinite eta (record block -) are G*-invariant; the relative sign of the record-carrying block versus the geometric block is a FREE Z/2 bit -- exactly the C-grading SIGN. Positivity (good-stable property) restricts to the open cone c1>0,c2>0, a 2-parameter family, NOT the single ray eta_+, so it does not pin the sign either. KEY STRUCTURAL FINDING: going from the FULL gauge group so(9,5) (under which the vector rep is IRREDUCIBLE, Schur => the UNIQUE eta, W203 nulldim=1) DOWN to the good-stable stabilizer REDUCES the symmetry, which INCREASES the invariant-form space from 1 to 2 -- the stabilizer does NOT force the grading sign, it LIBERATES it. This is the opposite of a false positive: R16 recovers eta up-to-grading and the grading sign stays free. Refining the good-stable structure (respecting base (3,1), fiber (6,4), and the fiber geometric(6)/record-count(4) Krein split) only ENLARGES the residual (dim grows to 4), never shrinks it to 1. So the answer is derivable-in-FORM, posited-in-SIGN. No canon movement; H59 OPEN; bar (b) UNCHANGED; count {1,3}; no forbidden target inserted. Zero em dashes."
grade: "exploration / STRONG on the decisive linear algebra (all dimensions are exact null-space computations on the (9,5)=(3,1)+(6,4) frame, machine-checked, deterministic seed 20260714, 29/29 exit 0, positive controls first). COMPUTED: PC_FULL the full-group Schur pin (dim so(9,5)=91; invariant symmetric forms = 1 = eta; eta_+ NOT invariant under the full group) -- this REPRODUCES W203's nulldim=1 as the guardrail control, so we recover, not inflate, W203; PC_GRAD the graded objects (C^2=I, C=sign(eta), eta (9,5) indefinite, eta_+=eta.C signature (14,0) positive-definite); PC_STAB the C-commutant inside so(9,5) is dim 46, the 45 boosts lie outside it; G* the good-stable stabilizer {X in so(9,5): X preserves eta_+ too} = dim 46 = the C-commutant = block-diagonal on R^9(+)R^5; INV the DECISIVE number dim{G*-invariant symmetric forms}=2, basis {eta_+, eta}={I,C}, eta and eta_+ distinct (not proportional) so the generator is NOT unique; RES both block projectors invariant (independent Schur scale per block), eta_+=P9+P5 and eta=P9-P5 both invariant (the free Z/2), the positive-definite invariants form a 2-parameter cone not the single ray eta_+; FINER the finer good-stable split gives 4 independent block scales, residual grows; E1 RESIDUAL-BIT-STANDS. CITED (not re-derived): W203 (Schur forced the UNGRADED kernel to eta, indefinite (9,5), nulldim=1 -- the thing we must NOT re-derive as the answer, and here reproduce only as PC_FULL), W205 (R16 the Condorcet winner; its honest prediction 'recovers the form up to scale AND one branch bit; does not close #1' -- W206 makes that precise: the residual is exactly the extra dimension), W186 (the bistable good-stable fixed point; positive total metric; both basins self-consistent = the model-theoretic signature of the free bit), W168 (the (6,4) DeWitt-fiber Krein split: geometric +, record-count -), W202 (#1 signature-robust under the (9,5)/(7,7) crux), W173 (mirror = record = BRST closed-not-exact = the counterfactual invariant), W180/W151 (the eta-from-gimmel-area scale). No canon / RESEARCH-STATUS / claim-status / verdict / posture change; H59 OPEN; H41 unbuilt; bar (b) UNCHANGED."
construction: "program-native where the objects are GU's (the (9,5) Clifford metric eta = W203's forced kernel, the C-operator grading C = sign(eta), the total metric eta_+ = eta.C, the (9,5)=(3,1)+(6,4) base/DeWitt-fiber split, the (6,4)-fiber geometric/record-count Krein split, the good-stable fixed point W186). Standard-field where the machinery binds any construction (Klein's Erlangen 'metric from its isometry group'; Schur's lemma fixing the invariant bilinear form on an irreducible rep and giving one scale per irreducible block on a reducible rep; the elementary fact that the common isometry group of an indefinite form and its positive-definite absolute-value is the maximal compact subgroup). Every method PORTED and labelled; none asserts GU beyond the cited source. The C-positive-vs-C-negative grading sign is carried and named as EXACTLY question #1; the eta-magnitude scale stays a GATED temporal-issuance / time-as-finality object (one-way rule respected); no cross-repo identity asserted."
depends_on:
  - explorations/W205-source-action-reverse-engineering-condorcet-2026-07-14.md
  - explorations/W203-branch3-source-action-fixed-coefficients-2026-07-14.md
  - explorations/W202-signature-crux-bach-branch-2026-07-14.md
  - explorations/W201-count-external-datum-characterization-2026-07-14.md
scripts:
  - tests/W206_decisive_bit_counterfactual_invariance.py
---

# W206 -- the decisive bit via counterfactual-invariance (R16, metric-from-its-stabilizer)

Test: `tests/W206_decisive_bit_counterfactual_invariance.py` (29/29, exit 0, positive controls first).
Deterministic (seed 20260714), on the repo's `(9,5) = (3,1) + (6,4)` frame -- the same level at which
W203 forced `eta`, W168 read the fiber Krein sign, and W202 proved signature-robustness. Viewpoints
(invariant-theory specialist; skeptic/guardrail; coherence synthesizer) ran INLINE in this one worker,
no sub-agents. Zero em dashes. One of five parallel siblings attacking the SAME bit by different
methods; this is R16, the W205 Condorcet winner.

## 0. The question, sharpened, and the guardrail

Take the good-stable fixed point as GIVEN (W186: real total spectrum; positive-definite total metric
`eta_+`, unitary to `6e-14`; C-operator operative, `eta_+ = eta.C`; mirror = record not redundancy,
W173). Its STABILIZER `G*` is the group of counterfactual / gauge deformations that preserve ALL of
those properties at once. Klein's Erlangen program: a geometry IS its invariants under its symmetry
group, so "the metric" of the good stable should be recoverable as the invariant symmetric bilinear
form of `G*`. The decisive question:

> Is the space of `G*`-invariant symmetric bilinear forms ONE-dimensional with generator
> `eta_+ = eta.C` (=> FORCED: the C-grading SIGN is derived-as-invariant, #1 resolves inside GU, bar
> (b) clears), or is it dim > 1 (=> RESIDUAL-BIT-STANDS: the grading sign is Godel-independent, must be
> posited or supplied by an external TI/TaF datum)?

The GUARDRAIL, stated up front so the finding cannot be a false positive. W203 ALREADY proved by Schur
that under the FULL gauge group `so(9,5)` the UNGRADED invariant symmetric form is UNIQUE (`nulldim=1`)
and equals `eta` (indefinite `(9,5)`). That is NOT the open bit. The open bit is whether the good-stable
STABILIZER additionally forces the C-grading SIGN (`eta_+ = eta.C`, the record-count mode Krein-negative
on the `(6,4)` fiber). R16's job is to settle the SIGN, not to re-derive `eta`. If R16 only recovers
`eta` up-to-grading, the honest verdict is RESIDUAL-BIT-STANDS -- and a clean residual negative is
exactly as valuable as FORCED.

## 1. Constructing the stabilizer `G*` (invariant-theory viewpoint)

The good-stable data the deformations must preserve reduce, on the frame, to two symmetric forms and one
involution:

- `eta = diag(+1 x9, -1 x5)`, the Clifford metric, signature `(9,5)` -- carries "real total spectrum"
  (the Krein structure whose self-adjointness gives a real spectrum). This is W203's forced kernel.
- `eta_+ = eta.C = |eta| = I_14`, the positive-definite TOTAL metric (good-stable property).
- `C = sign(eta) = diag(+1 x9, -1 x5)`, the C-operator grading: it flips the `eta`-negative directions
  so that `eta_+ = eta.C` is positive-definite. On the `(6,4)` fiber the `eta`-positive `6` are the
  geometric modes and the `eta`-negative `4` are the record-count modes (W168), so `C` is exactly the
  physical-vs-record grading, W173's closed-not-exact class.

A counterfactual deformation `X` (an element of the gauge Lie algebra `so(9,5)`) keeps the system at the
good stable iff it preserves BOTH forms:

```
X in so(9,5):   X^T eta   + eta   X = 0     (real total spectrum / Clifford metric)
X preserves eta_+:   X^T eta_+ + eta_+ X = 0     (positive total metric)
```

Preserving the indefinite `eta` AND its positive-definite absolute value `eta_+` forces `X` to commute
with their ratio `C = eta_+^{-1} eta = sign(eta)` -- so `X` is block-diagonal on the C-eigenspaces
`R^9 (+) R^5`, and antisymmetric within each block. Hence

```
g* = so(9,5) cap o(14) = so(9) (+) so(5) = the MAXIMAL COMPACT subgroup,   dim = 36 + 10 = 46.
```

The test computes this as a null space, not by hand: block `[G*]` finds `dim g* = 46`, verifies it
equals the C-commutant of `so(9,5)` (block `[PC_STAB]`, dim `46`, with the `45` boosts lying OUTSIDE
it), and verifies every generator is block-diagonal on `R^9 (+) R^5`. This is W203's own observation
("the Hilbert Gram is invariant only under the maximal compact subgroup `SO(9)xSO(5)`") read as the
stabilizer of the good stable.

## 2. The decisive computation -- the invariant-form space (Schur on the reduced rep)

Under the full gauge group `so(9,5)` the 14-frame vector rep is IRREDUCIBLE, so Schur gives ONE
invariant symmetric form = `eta` (block `[PC_FULL]`: `dim = 1`, generator `eta`, and `eta_+` is NOT
invariant under the full group). That reproduces W203's `nulldim = 1` -- the guardrail control passes,
so we are recovering W203, not inflating it.

Now restrict to `G* = so(9) (+) so(5)`. The vector rep is REDUCIBLE: `R^9 (+) R^5`, two INEQUIVALENT
irreducibles (different dimensions, different factors act). By Schur:

- no invariant can couple the `9`-block to the `5`-block (inequivalent irreps have no intertwiner), so
  invariant forms are block-diagonal;
- on each block the invariant symmetric form of the vector rep of `O(n)` is unique up to scale (the
  identity), so each block gets ONE independent scale.

Therefore the invariant symmetric forms are `B = c1 * I_9 (+) c2 * I_5`, a space of dimension EXACTLY
`2`. Block `[INV]` computes this directly:

```
dim{ g*-invariant symmetric forms } = 2       <-- the DECISIVE number
basis = { eta_+ , eta } = { I_14 , C }
eta_+ = P9 + P5   (c = (+,+))   the C-graded positive-definite metric
eta   = P9 - P5   (c = (+,-))   W203's ungraded indefinite kernel
```

Both `eta_+` and `eta` are `G*`-invariant, and they are DISTINCT (not scalar multiples). So the
generator is NOT unique. The two Schur scales `c1` (geometric block) and `c2` (record-carrying block)
are INDEPENDENT; the relative sign `sign(c2/c1)` is a FREE bit -- and that bit is precisely the
C-grading SIGN (record-count block weighted `+` gives the good-stable `eta_+`; weighted `-` gives the
indefinite `eta`).

## 3. Does positivity or spectrum-reality rescue FORCED? (skeptic / guardrail viewpoint)

Two honest attempts to shrink the `2` to `1`, both fail.

- POSITIVITY. The good stable requires `eta_+` positive-definite. Does demanding a positive-definite
  invariant single out `eta_+`? No. `c1 * I_9 (+) c2 * I_5` is positive-definite iff `c1 > 0` AND
  `c2 > 0` -- a 2-parameter OPEN CONE, not a single ray. Block `[RES]` verifies `(1,1), (1,2), (2,1),
  (0.3, 5.0)` are all positive-definite invariants and `(1,2)` is not proportional to `eta_+`. Positivity
  restricts the sign of each block to `+` but leaves the two magnitudes free; it does not pick a unique
  metric. It DOES kill the indefinite branch `c2 < 0` (that is `eta`), but "which side of the wall" is
  still a posited choice, not a derived one -- the wall does not tell you you are on the positive side.
- SPECTRUM REALITY. Real total spectrum is a self-adjointness property of the dynamics with respect to
  `eta_+`; it constrains the OPERATOR, not the FORM space, and adds no equation to the invariant-form
  computation. It does not reduce `dim = 2`.

So neither good-stable property beyond the stabilizer forces the sign. The `2` stands.

## 4. The structural moral -- the stabilizer LIBERATES the sign, it does not force it

The load-bearing insight, and the reason this is a real negative and not a false positive:

> Going from the FULL gauge group `so(9,5)` (vector rep IRREDUCIBLE, Schur => the UNIQUE `eta`,
> W203 `nulldim = 1`) DOWN to the good-stable stabilizer `so(9)+so(5)` REDUCES the symmetry, which
> INCREASES the invariant-form space from `1` to `2`. Less symmetry means MORE invariants. The extra
> dimension IS the C-grading sign. The stabilizer does not fix the sign -- it frees it.

This is the exact opposite of what a FORCED verdict would need (a stabilizer large enough to glue the
two blocks so only `eta_+` survives). No such gluing exists, because the very act of standing at the
good stable -- preserving both `eta` and `eta_+` -- is what SPLITS the rep into the two C-blocks. R16
converts W203's result into its sharpest form: the metric is derivable up to ONE relative-block sign,
and that sign is the open bit.

Refining does not help the FORCED case. Block `[FINER]`: if the good-stable stabilizer also respects the
base `(3,1)`, the fiber `(6,4)`, and the fiber geometric`(6)`/record-count`(4)` Krein split (all
good-stable structure), the rep splits into 4 irreducible `O(n)` blocks and the invariant-form space
grows to dimension `4`. Refining the structure only ENLARGES the residual; it never shrinks it to `1`.
RESIDUAL-BIT-STANDS is robust to how finely you resolve the good stable.

## 5. Verdict and the residual group

VERDICT: **RESIDUAL-BIT-STANDS.**

- Invariant-graded-form-space dimension = `2` (not `1`).
- Generators: `{eta_+ = eta.C, eta}` = `{I_14, C}`. `eta_+` is A distinguished generator (equal block
  weights, the good-stable positive-definite metric) but NOT the UNIQUE invariant; W203's indefinite
  `eta` is equally invariant.
- Residual group / freedom: independent scaling of the two C-blocks, `GL(1) x GL(1)` on the diagonal
  (a 2-parameter family; restricted to positive-definite forms, a 2-parameter open cone). At the level
  of "which grading SIGN", a `Z/2`: the record-count block weighted `+` (`eta_+`, good-stable) versus
  `-` (`eta`, W203) -- both `G*`-invariant, so the sign is unforced.
- Reading: the C-grading is **derivable-in-FORM, posited-in-SIGN**. `#1` (whether the interacting
  C-operator grades records positive) is NOT closed by counterfactual invariance; it is the
  Godel-independent branch bit W205's R13 named, and R16 localizes it to exactly this one relative-sign
  dimension.

H59 stays OPEN; bar (b) UNCHANGED; count `{1,3}`; no canon movement; no forbidden target
`{3,8,24,chi(K3),Ahat}` assumed or inserted. Exploration grade throughout; nothing asserts GU, a
vacuum, or that the DESI wiggle is real. The `eta`-magnitude scale stays TaF/temporal-issuance-gated.

## Convergence note

Five siblings attack this one bit by different methods: R16 (this note, counterfactual-invariance),
R9 (Dirac-BRST cohomology), R7 (Lawvere categorical fixed point), R12 (topos internal logic), R1
(Helmholtz inverse-variational). W205 found the top three (R16, R9, R7) all recover the SAME object
`eta_+ = eta.C` by three routes (invariance, cohomology, consistency), so a shared object is expected;
the live question is whether any route makes the SIGN a theorem.

- AGREEMENT (the expected, and I judge most likely, outcome). If R9's BRST charge only fixes the grading
  UP TO the physical-vs-ghost sign (needing an external positivity input to say which class is
  physical), if R7's Lawvere fixed point exists for BOTH branches (both basins are weakly-point-
  surjective fixed points, as W186's bistability says), if R12's subobject classifier admits both
  complementary characteristic maps, and if R1's Helmholtz self-adjointness is itself the `eta`-vs-`eta_+`
  choice -- then all five land on RESIDUAL-BIT-STANDS. That five-fold convergence on a NEGATIVE would be
  a strong, method-independent result: the C-grading sign is Godel-independent of GU's current axioms
  and must be posited or supplied by the external TI/TaF datum (the W187 `r(N)` crossing), not derived.
  It would also cross-check the SHAPE of the residual: R16 says it is exactly 1 relative-block-sign bit
  (a `Z/2` inside a 2-dim form space), and the other four should localize the SAME single bit if they
  agree.

- DISAGREEMENT (the informative surprise). If any sibling returns FORCED -- e.g. R9 shows `Q^2 = 0`
  plus first-class closure at the good stable admits only ONE grading, or R14-style reflection positivity
  (an OS input) forces the physical class -- then that method is using a good-stable input that R16's
  stabilizer does NOT encode (a dynamical / cohomological / positivity constraint beyond preserving the
  three forms). The disagreement would be diagnostic, not a contradiction: it would pinpoint WHICH extra
  good-stable datum breaks the `Z/2`, and that datum, not counterfactual invariance, would be the thing
  that closes `#1`. R16's contribution either way is the precise residual: dimension `2`, one free
  relative-block sign, robust to finer splitting -- the target the other four either confirm or must
  explicitly overrun.

*Filed 2026-07-14. Route: R16 counterfactual-invariance / metric-from-its-stabilizer (Klein/Erlangen),
the W205 Condorcet winner. Viewpoints inline in one worker (invariant-theory specialist; skeptic/
guardrail; coherence synthesizer); no sub-agents. Reproducible:
`python -u tests/W206_decisive_bit_counterfactual_invariance.py` (29/29, exit 0; positive controls
first, including the W203 `nulldim=1` guardrail control). VERDICT: RESIDUAL-BIT-STANDS (invariant-form
dimension 2; residual = a Z/2 grading sign plus block scales; `eta_+` is a generator, not the unique
one). No canon movement; H59 OPEN; bar (b) UNCHANGED; promotion stays Joe-gated.*
