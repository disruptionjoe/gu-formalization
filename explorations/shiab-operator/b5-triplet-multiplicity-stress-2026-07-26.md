---
title: "B5 target-blind provenance-multiplicity stress"
status: active_research
doc_type: exploration
run_id: RUN-20260726-0408-gu-formalization-progress
lane_id: "1"
claim_status_change: none
---

# B5 Target-Blind Provenance-Multiplicity Stress

## Question

Does the already-frozen complexified B5 principal-symbol class select the
independently located triplet merely because it contains three expanded
provenance copies?

## Construction and boundary

The test stays on the program-native observer-subgroup carrier used by the B5
matrix certificate.  It varies only the number `n` of explicitly labeled
provenance copies while retaining the paired `E_+`, `E_-`, and full `X` ledger.
It does not select any unresolved native phase, coflip, formal-adjoint sign,
Green form, common domain, lower-order term, source action, or physical
quotient.

The standard-field alternative is not substituted: this is an algebraic
principal-symbol census on the frozen GU carrier, not a positive-Hilbert
spectral model.

## Result

`tests/shiab_b5_triplet_multiplicity_stress.py` reconstructs the ordered
symbol-cell count for `n = 1, 2, 3, 4` without a target-coded rule:

| provenance copies `n` | ordered symbol cells |
| ---: | ---: |
| 1 | 40 |
| 2 | 80 |
| 3 | 136 |
| 4 | 208 |

The exact law on this declared range is

\[
C(n) = 8n^2 + 16n + 16.
\]

Thus the three-copy ledger reproduces the frozen 136-cell result but has no
unique algebraic event at `n=3`.  The finite symbol class is sensitive to
provenance multiplicity (it does not silently quotient it away), but it does
not select that multiplicity.  This is a scoped negative result about this
algebraic stage, not a no-go for a source-owned operator/domain or boundary
mechanism.

## Consequence

The active conditional source-action program's division of labor is now
sharper: Rung-1 coefficient enumeration may constrain a proposed selector,
but the triplet cannot be promoted from an algebraic cell count.  A positive
selection result must introduce and test a source-coordinate, operator/domain,
boundary, or index mechanism with the target-blind controls already specified
by `SRC-TOY-01`.

## Validation

The new certificate reproduces the existing three-copy count, verifies the
four finite target-blind controls, and rejects the silent-provenance-collapse
reading by showing each multiplicity has a distinct census.
