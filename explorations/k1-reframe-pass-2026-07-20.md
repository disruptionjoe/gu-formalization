---
title: "Phase-1 close: K1 (Node 4) KILLED on its own pre-declared condition -- conservation locates the coset Sp(1)_comm, never a map -- and the mandatory REFRAME PASS is executed: the one live door (the grade-3 / self-dual record bridge) is REAL (full-rank, exact co-flip deck, guaranteed odd degree) but NOT NATIVE (exact degree fixture/pin-dependent with |c| in {1,3,5}; c = 3 members are the ZERO class; a THIRD exact identity, K_S-parity, collapses the bridge to rank 2 on the confined habitat); no reframe-as-mechanism qualifies, the sharpened two-gate demand is adopted, and all three legs of program-kill clause 6(a) are satisfied at exploration tier"
doc_type: phase1_kill_and_reframe_pass
status: exploration tier; K1 KILLED per its pre-declared condition; reframe pass executed per the tree's Section-4 exhaustion clause (probe exit 0)
created: 2026-07-20
directed_by: "Joe direct chat, 2026-07-20 (Phase 1 close: K1 + mandatory reframe pass)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
target: "The two outstanding obligations of explorations/torsor-k-sequence-2026-07-20.md (commit 6a285c0) before any program-level conclusion is permitted: (1) formal execution of Node 4 (K1, the record-current conservation transport) against its OWN pre-declared kill/survive conditions; (2) the pre-committed REFRAME PASS of the Phase-0 tree's Section 4 exhaustion clause (kill-gets-a-defense-attorney), with the kill sequence's own defense input -- commutant-DETECTING kernels exist in the algebra -- given a real test, not a gesture."
probe: tests/channel-swings/k1_reframe_probe.py
related:
  - explorations/torsor-k-sequence-2026-07-20.md
  - explorations/phase0-torsor-identification-tree-2026-07-20.md
  - explorations/torsion-generation-arena-2026-07-20.md
  - explorations/verify-torsion-arena-2026-07-20.md
  - explorations/operator-grade-end-2026-07-20.md
  - explorations/sig-b5-habitat-verification-2026-07-20.md
  - explorations/d1-coperator-build-2026-07-19.md
  - explorations/global-anomaly-leg-2026-07-20.md
  - tests/channel-swings/torsor_k_sequence_probe.py
  - tests/channel-swings/phase0_torsor_checks.py
sources:
  - "Borsuk-Ulam / odd-map degree theorem: an antipode-equivariant (odd) map S^n -> S^n has odd degree -- used to type what the deck restriction buys FOR FREE (existence of some odd class), sharpening K6's 'existence was never the question'."
  - "Steenrod; Adams J(X) IV; Bott (1959) -- carried unchanged from the Phase-0 tree."
---

# Phase-1 close: K1 formal execution + the mandatory reframe pass

Both outstanding obligations of the kill sequence are discharged here.
Everything sits on the framed-reading fork (R0_COND) and inherits the (9,5)
H-class conditionality of the frozen spine. Probe:
`python tests/channel-swings/k1_reframe_probe.py` -- ALL PASS, 13 [E] + 2 [F]
(setup [T] = 3), exit 0, ~67 s, deterministic. phase0_torsor_checks re-runs
live inside it (Test N power receipt: R1 bit-exact, R2 and the plant killed).
The degree machinery is new (signed preimage counting on an exactly-evaluated
cubic) and is power-validated INSIDE the probe on maps of known degree +1 and
+3 before any kill is read from it.

## 1. K1 (Node 4) -- formally executed, KILLED on its own condition

K1's row: v |-> the commutant unitary that conserves/intertwines the record
current J^a = Re<Psi, K_S e_a Psi> along the core family. Executed against
Node 4's own pre-declared conditions (kill: "transport forced non-commutant
(residual O(1)) or degree 0"), on K1's own family states (the frozen
spin-lift family on both habitat pins, exact deck oddness) -- NOT by
importing K4's verdict:

- **The rule is non-vacuous.** The spin lift moves the current at O(0.2-0.5)
  at generic v (both pins): there is a real conservation equation to solve.
- **K1's own test fires.** ON K1's family states, every commutant unitary
  moves J^a by < 1e-12 (120 random u across pins and sample points). So the
  conservation equation J^a(u Psi_v) = J^a(Psi_0) is INFEASIBLE wherever the
  current moved (the defect is unreachable by the entire group) and is
  solved by ALL of Sp(1)_comm wherever it did not (v = +-v0). The rule
  locates the coset Sp(1)_comm, never a map. This is the K4 invariance
  identity, run here AS K1's decisive test, exactly as the kill sequence's
  shared-source note predicted -- shown, not imported.
- **The intertwining reading dies by the same exact relations.** Commutant
  conjugation fixes every kernel K_S e_a exactly (twisted-commute defect
  0.0 for J; e^{i theta} trivially), while the family rotates the kernels
  at O(1) (defect 1.50): no commutant unitary intertwines.
- **The only other in-row variant is vacuous.** "Conserve the H-valued
  pairing h(Psi, Psi)": its Im H part is IDENTICALLY zero (iI component
  = Re(-i|Psi|^2) = 0; J/iJ components are quadratic forms of the
  antisymmetric kernel C, |C + C^T| = 0.0). The quaternionic norm form is
  real; conserving it selects nothing.
- **Verdict: KILLED, the "degree 0" horn.** The only posit-free section of
  the solution correspondence is the constant u = I: deck restriction +I
  (not the verified co-flip -I), degree c = 0, k = 0, order(J(0)) = 1. Any
  gauge-fixed selection pays the row's own +1 posit -- import >= 1 on this
  route, exactly the exhaustion clause's demand.

**Does any K1 variant survive that the identity does not cover?** Precisely
one, and it is out-of-row: replacing the current's kernel by a
non-H-self-adjoint one is a NEW object, not the frozen record current. That
variant IS reframe (a) below, where it received the pass's full budget.

## 2. The reframe pass (the heart of the assignment)

### 2(a) Higher-grade transport -- the one live door, advocated, then tested

**The defense's case, made honestly at full strength.** The kill sequence
proved the record current is Sp(1)_comm-blind and SIMULTANEOUSLY proved the
blindness is grade-1-specific: its own detectability control (the
K_S-dressed grade-3 kernel) reads the commutant at O(0.1). The defense is
entitled to ask: can a native transport ride the grade-3 sector instead?

**Provenance (named, with receipts).** The candidate dressing is supplied by
the pin-plane Hodge duals of the record legs: for each frozen habitat pin
leg e_i, the product e_hat_i of the other three pin legs -- canonical given
the frozen pin (sig-b5 habitat receipts) up to orientation (the honest +-).
This is the frozen home of the kill sequence's own control kernel
(K_S e_0 e_1 e_9 is a pin-1 dual up to reordering), and operator-grade-end
Discovery 1 receipts grade-3 torsion-type terms as the K-honest J-odd
sector. Neither the bicomplex connection (K3: commutant projection exactly
0, algebra-side) nor the co-flip composite (a Z/2 datum, not a dressing)
supplies it; the dual construction is the honest answer to "which frozen
structure," and its two imports are declared: the pin-plane orientation
(Z/2) and the grade-mixing coefficient lambda below.

**Structural findings (all machine-checked, both pins):**

1. The K4 protection identities FAIL AT MAXIMAL DEFECT (2.0) on every
   pin-dual kernel K_S e_hat, whose commutant columns respond at O(0.1):
   the protection theorem is grade-1-specific. The defense's premise is
   TRUE and now fully receipted.
2. Exact type split: every K_S e_hat is ANTI-Hermitian at 0.0. So a pure
   grade-3 bridge has identically zero real column (rank <= 3; it misses
   the identity direction of the commutant), the mirror image of grade-1's
   zero commutant columns (rank 1; K4). The only full-quaternion bridge is
   the SELF-DUAL mix K_S(e_i + lambda e_hat_i).
3. The self-dual bridge is REAL: full-rank at generic states (min |det|
   5.8e-6, cond <= 215 over 4 draws x 2 pins) with EXACT co-flip deck
   restriction q(-v) = -q(v) (quadratic in Psi, odd in v -- the only
   deck-compatible contraction shape; the one-basepoint-slot variant is
   exactly deck-EVEN and excluded). By deck-oddness, Borsuk-Ulam GUARANTEES
   odd degree: MS clauses (a)-shape, (b), and "some odd c" all land. This
   is the closest any construction has come to MS.

**The decisive computation (exact integer degrees, signed preimage count on
the exactly-evaluated cubic; machinery power-validated on known degree +1
and +3 first):**

- KILL 1 -- the class is NOT native. The degree is target-consistent and
  odd on every fixture, but SCATTERS with the fixture state and the pin:
  pin 1 gives -1, -1, +3, +5 across four generic draws; pin 2 gives +3,
  +3, +1 on the same states; lambda in {0.5, 1, 2} leaves it unchanged
  within a draw. The instability belongs to the state posit. No
  Psi_0-independent class exists; any selection costs import >= 1.
  Corroboration at the frozen-variant level: sign(det B(Psi_0)) is a coin
  flip (18/22 and 15/25 over 40 draws per pin) -- which also kills the
  tree's Section-5 residue hope ("the bridge might natively pin an
  orientation"): it does not; the {8nu vs 16nu} honest +- stays honest.
- KILL 2 -- Test W fires INSIDE the family: the witnessed |c| = 3 members
  give k = 192 = 0 mod 24, the ZERO class (order 1). The family straddles
  order-3-delivering and order-killing classes, and selection between them
  is exactly what nativity cannot supply.
- KILL 3 -- a THIRD exact identity (K_S-parity), sibling to K4's two:
  every pin-dual carrying the single negative-signature pin leg
  ANTICOMMUTES with K_S at 0.0 (the dual OF the negative leg commutes).
  Consequence: on K_S-confined states the bridge tensor has exact
  structural zeros (4.2e-17) and rank EXACTLY 2, both pins. The
  record-confined habitat itself refuses the grade-3 completion: the
  record sector protects the commutant at grade 3 by PARITY, as it does
  at grade 1 by H-reality.

**Verdict on (a): KILLED as a mechanism, on three independent receipts.**
Import price if forced through anyway: >= 2 (fixture state/class posit +
lambda-and-orientation normalization), against the house rule's ceiling of
"a real reframe explains more"; a construction whose class is fixture noise
explains less.

### 2(b) The reading-level reframe -- typed: reframe-as-sharpened-demand

The K4 structural theorem does have positive content, and it is now bigger
than K4: the frozen kernel inventory splits EXACTLY into commutant-blind
(H-self-adjoint: all 14 grade-1 record kernels at 0.0; the Sigma dressing
with commutant projection exactly 0) and commutant-reaching but
habitat-collapsing (the pin-duals). This is a DERIVATION, not a cope, and
it makes new testable statements -- the two-gate admission test any future
ID-1 occupant must pass, machine-checkable as run in this probe:

> **Gate 1 (reach):** the candidate's K_S-dressed kernel must be
> non-H-self-adjoint (commutant columns != 0). **Gate 2 (habitat):** its
> bridge must remain nondegenerate on the K_S-confined sector. Plus the
> standing gates: exact co-flip deck restriction, Psi_0-independent class,
> 3 not | c. NO kernel in the frozen inventory passes both Gate 1 and
> Gate 2 (probe, final [E] check).

Typed honestly: this is reframe-as-sharpened-demand (import 0; it posits
nothing) and NOT a priced alternative mechanism -- it does not reopen MS,
it tells the B.5 ledger exactly what a native framing datum would have to
look like and certifies the current frozen inventory contains no such
object. It explains MORE than the original framing: one sector statement
(quaternionic H-reality at grade 1, K_S-parity at grade 3) now accounts
for all four node kills, the K1 coset degeneracy, AND the detectability
controls' O(0.1) responses.

### 2(c) Survivor-bias reframes anticipated by the tree -- enumerated

- **Non-commutant nonzero transports** (grade-3 winding as a DIFFERENT
  arena mechanism): still disclosed, still out of ID-1's scope; note that
  KILL 3's parity identity now also constrains any such bilinear reading
  on the confined sector. Not cheap to test beyond that; not tested.
- **Physics-side constructions** (e.g., instanton number of a dynamical
  gauge field): the GEOMETER-VS-PHYSICS fork stands; not testable in
  frozen material; the sharpened demand is exactly the specification such
  an occupant would have to meet.
- **Mixed candidates killed while carrying commutant parts:** the guard
  was honored throughout (projections computed before every kill; the
  pin-duals' commutant content was measured, not assumed, and the kill is
  about nativity, not reach).
- **C-04 rep/geometric retyping:** unchanged; would kill all branches at
  once; nothing here touches it.

No candidate in (c) is both cheap and in-scope; none was tested beyond the
parity note.

## 3. The program-kill ledger (clause 6(a)) -- state after this pass

- **Leg A (naturality exhaustion, Nodes 1-4):** SATISFIED. K3 (Test N,
  commutant projection exactly 0), K2 (symmetric-space shape, orbit a
  point), K4 (bridge rank 1, two exact identities), and now K1 (coset not
  map; degree-0 horn) -- all four by their pre-declared conditions.
- **Leg B (the reframe pass fails to produce a priced alternative):**
  SATISFIED. The pass was executed with the defense's best input given a
  real computation; the one live door is killed on three independent
  receipts; the qualifying output is a sharpened DEMAND (import 0), not an
  alternative mechanism.
- **Leg C (import >= 1 on every route):** SATISFIED at enumeration grade.
  Record bilinears: exactly blind (K4's identities). Conservation and
  intertwining rules: coset-degenerate (K1). Two-insertion pairing:
  vacuous (Im H part identically zero). Sigma/connection: commutant
  projection 0 (K3). C-weld: no free orbit (K2). Grade-3/self-dual
  completion: fixture-dependent class + parity collapse (this pass) --
  class selection is a posit. Any other kernel family lacks a native
  model-index assignment (the pins supply the only frozen model-indexing),
  so its indexing is itself a posit. CAVEAT, disclosed: Leg C is
  enumeration-complete over the tree's candidate discipline plus this
  pass's extension, not a universally quantified algebraic theorem over
  all conceivable constructions -- the same epistemic grade as the tree's
  own survivor-bias section.

**Consequence (exploration tier, conditional on R0_COND and the (9,5)
H-class):** clause 6(a)'s legs are now formally satisfied. Within the
current frozen inventory, ID-1 is an obstruction-spec (the tree's outcome
shape ii), never a mechanism: the order-3 line reads "located, not
forced" permanently, and the occupant conditional persists. The
line-closure VERDICT itself is a Joe-gated flip and is NOT executed here
(claim/canon/posture: none). Clauses 6(b) and 6(c) remain Scout-B /
operator-grade property and are untouched; the framed-reading fork is a
separate question and is not conflated with this closure.

**What this leaves:** RF-1 (Scout B's tree) stays BLOCKED -- it waited on
an MS positive that the completed tree now excludes at native grade; no
further note is warranted. The two revival routes are exactly the
disclosed blind spots: new frozen structure passing both admission gates,
or a physics-side occupant across the GEOMETER-VS-PHYSICS fork.

## 4. Five-lens council (answered in writing)

**Defense advocate (required lens -- the strongest case for the door).**
The pass was owed a real fight and got one. My case: (1) the kill
sequence's own control proved commutant-detecting kernels exist; (2) the
self-dual bridge I was given is not a strawman -- it is full-rank, its
deck restriction is EXACTLY the verified co-flip, and deck-oddness buys
odd degree by theorem: every SHAPE the MS statement demands is exhibited
simultaneously for the first time in this program; (3) the kills of
nativity all route through one posit -- the fixture state Psi_0. If the
program ever supplies a canonical state, the class stabilizes and the
door reopens; a kill that depends on "no canonical state exists TODAY" is
a kill about the inventory, not about the mechanism. I ask the record to
show the door was closed for want of a native state supply, not for want
of a transport. And I claim one victory outright: the pass's output is a
two-gate specification that did not exist before -- the defense turned
three kills into a search warrant.

**Adversarial referee (answering, both sides on the record).** The
defense's facts are admitted; the conclusion is not. (1) The program DOES
supply canonical states -- the record-confined habitat sector is exactly
its native state supply -- and KILL 3 shows the bridge collapses to rank
2 THERE, by an exact identity, not by numerical accident. The defense's
hoped-for future state must therefore be unconfined, i.e., outside the
record habitat the arena result lives in: that is a new N-item by
definition, import >= 1, which is precisely Leg C. (2) Even off the
confined sector, the class scatters over {+-1, +3, +5} with the draw and
the pin, and the family straddles the zero class (c = 3): there is no
fact of the matter for a selection rule to recover. (3) The Section-5
residue hope died in the same computation (sign is a coin flip). The
door was closed on the tree's own naturality standard, three independent
ways. What would reopen it is named and machine-checkable (the two
gates) -- that is the defense's real victory, and it is recorded as the
pass's output rather than as a survival. Verdict stands: no
reframe-as-mechanism qualifies.

**Representation theorist.** The pass completes the protection theorem
into a dichotomy of the quaternionic Hermitian form: grade-1 record
kernels are H-self-adjoint (real-valued form; Sp(1) acts trivially),
pin-dual kernels are anti-Hermitian with maximal identity defects
(Im-H-reaching) BUT K_S-odd exactly when they carry the single
negative-signature leg -- so the confined sector annihilates their
matrix elements. Reach and habitat-compatibility are complementary
obstructions in the frozen inventory: what reaches the commutant cannot
live where the record lives. That complementarity IS the sharpened
demand, stated representation-theoretically.

**Bundle/torsor geometer.** K6 said existence is free; this pass makes
the point quantitative: deck-oddness alone forces SOME odd degree
(Borsuk-Ulam), so exhibiting "a transport with the right deck and odd c"
was never evidence of nativity -- only a Psi_0-independent class would
have been, and the class is fixture noise (and pin-dependent, so not
even a stable invariant of the cover). The failure mode is exactly the
one the tree pre-declared for the whole program: at existence-of-native-
map, never at existence-of-map. The arena stays "located, not forced."

**Numerical analyst.** Every load-bearing kill is either exact (parity
defects 0.0; structural zeros 4.2e-17; deck oddness 0.0; pairing Im part
2.1e-17; twisted-commute 0.0) or an integer computed by a method
power-validated in the same run (preimage counter certifies known degree
+1 and +3 on two regular values each; every candidate degree is
target-consistent across two independent regular values). The one
numerically delicate object -- the WZW quadrature -- was NOT used for any
verdict: scouting showed it non-convergent near the bridge's small-|q|
regions, so it was replaced by the exact-cubic preimage count. No
tolerance is load-bearing within ten orders of magnitude. Seeds fixed;
imported battery re-runs exit 0; ~67 s.

## 5. Boundary (what this pass cannot see)

- Leg C's universality is enumeration-complete, not algebraically
  quantified over all conceivable constructions (disclosed above).
- The degree study samples generic fixture states (4 draws x 2 pins, plus
  lambda and target replications); the WITNESSED instability {-1, +3, +5}
  suffices for the kill, but the full degree distribution is not mapped.
- Whether the habitat's OWN preferred states (beyond K_S-confinement) add
  further structure is untested; KILL 3 covers the confined sector, which
  is where the record receipts live.
- The physics-side fork, C-04 retyping, and the framed-reading fork
  (R0_COND) bound everything here exactly as they bound the tree and the
  kill sequence.

## Receipts

- Probe: tests/channel-swings/k1_reframe_probe.py -- ALL PASS, 13 [E] +
  2 [F] = 15 (setup [T] = 3 excluded), exit 0, ~67 s, deterministic.
- HEADLINE: K1 KILLED on its own pre-declared condition (conservation
  locates the coset Sp(1)_comm, never a map; posit-free section = deck
  +I, degree 0; intertwining and pairing variants die by exact
  identities). REFRAME PASS EXECUTED: the grade-3/self-dual record bridge
  is REAL but NOT NATIVE (degree fixture/pin-dependent, |c| = 1 and 3
  witnessed; sign a coin flip; c = 3 members are the ZERO class; K_S-
  parity collapses the bridge to rank 2 on the confined habitat). No
  reframe-as-mechanism qualifies; the sharpened two-gate demand is the
  pass's output. All four node legs of clause 6(a) formally dead.
- Imported battery re-run live, exit 0: phase0_torsor_checks (0.1 s; Test
  N power: R1 bit-exact, R2 2.00 killed, plant 2.00 killed with Z/2
  shadow 1.2e-16 passed).
- New machinery power receipts: signed-preimage degree counter certifies
  identity map +1 and quaternion-cube +3 (two regular values each);
  grade-1 kernels through the same bridge pipeline read < 1e-13
  (kill-direction control).
