---
artifact_type: exploration
label: W216
status: "exploration (W216 / TEAM SPECTRAL-CONDENSATE; one of FIVE independent teams BUILDING the record-condensed true vacuum by a different method to test convergence; five personas inline in one worker, no sub-agents; one deterministic test 28/28 exit 0, positive controls first)"
created: 2026-07-14
wave: W216
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (a condensate with an un-real / unbounded spectrum is a REAL negative result, not a failure to hide); RUTHLESS skeptic; no rooting for GU; tri-repo gating STRICT; do NOT fix the reservoir Krein sign (proven Godel-independent, W211); bar(b)/H59 stay OPEN
route: "BUILD the record-condensed true vacuum as a SPECTRAL REARRANGEMENT -- the mirror/record sector condensing, BCS/gap-equation style, expressed as a spectral shift of the C-operator / Krein spectrum. Solve the self-consistent gap equation; diagonalise the Bogoliubov-de Gennes Hamiltonian in the Krein metric on BOTH the operative-C good branch and the pathological branch; read off existence, gap scale, spectrum reality/boundedness, Krein positivity, the arrow-of-time reading, and validity."
n_number: "W216 chosen. On origin/main the current max exploration is W211 (W212-W215 free); W216 is free and this note takes it. If a staging race collides, rebase absorbs and the next free number above W218 is used, and this line says so."
title: "W216 -- the record-condensed TRUE VACUUM of GU, BUILT as a SPECTRAL / BCS CONDENSATE, and its spectrum. VERDICT: EXISTS-SENSIBLE (conditional on the operative-C good branch) / EXISTS-PATHOLOGICAL (opposite branch). A GENUINE condensate EXISTS: the tachyon m0^2 = -1/4 (W130) is a Cooper-attractive channel in the record-count / conformal-scale mode (W153/W166), the mirror/record sector (W173: 96 hyperbolic null pairs in ker(Gamma), K = Cartan involution) pairs, and the self-consistent gap equation has a NONZERO solution Delta* with condensation energy E_cond = -1/2 N0 Delta*^2 < 0 -- the condensed state is the TRUE (lower) vacuum, below the empty false vacuum W163 named but could not build. ITS NATURE is EXACTLY branch-dependent, and that is the whole result: on the GOOD branch (interacting C-operator OPERATIVE, the paired partners like-signed under eta_+ = eta.C) the Bogoliubov-de Gennes spectrum is +/- sqrt(xi^2 + Delta*^2), REAL, BOUNDED below by the gap Delta*, with a POSITIVE Krein metric on the quasiparticle vacuum -- a SENSIBLE gapped condensate; on the OPPOSITE branch (C not operative, the naive null-pair metric eta = diag(+1,-1)) the pairing couples a Krein-positive to a Krein-negative partner, the BdG spectrum is +/- sqrt(xi^2 - Delta*^2), which is COMPLEX for every |xi| < Delta* (hence down to xi=0, ALWAYS present) -- an opposite-type eigenvalue collision (Krein/Bognar), a self-accelerating / non-real spectrum: PATHOLOGICAL. The selector between the two is ONE Krein sign, which W211 PROVED is Godel-INDEPENDENT of GU's good-stable structure; W216 does NOT fix it (reports both branches). THE ARROW OF TIME (W166) HOLDS at the condensate on the good branch and only there: condensing GAPS the tachyon (record-count mode mass^2 flips from -1/4 to +Delta*^2 > 0), so the ongoing instability is replaced by the CONDENSATE PHASE (the Goldstone of the broken record-count U(1)) winding at a real, monotone, bounded (Josephson) rate = W166's steady de-Sitter-rate clock, now a stationary object rather than a runaway; on the pathological branch the complex spectrum makes the winding rate complex and the clock ill-defined. VALIDITY converges EXACTLY with W175: the spectrum is real and the C-operator BOUNDED (regular critical point, Langer/Curgus-Najman) IFF the gap stays inside the essential-spectrum gap, Delta* < mu_c = 9/(2 R_s) = 4.5/R_s, i.e. Delta* R_s < 9/2 -- the SAME 9/2 threshold W175 found for m2 R_s; strong pairing drives Delta* above mu_c and the condensate mode EMBEDS in the continuum (singular critical point, C unbounded). NET: the condensate is REAL (built, not named), the true-vacuum question #1 is answered YES, but whether it is a sensible vacuum is EXACTLY the one external Krein-sign bit W211 located and GU cannot force. bar(b)/H59 UNCHANGED / OPEN."
grade: "exploration / conditional register throughout. EXACT (finite-dim, machine-verified, tests/W216_true_vacuum_spectral_condensate.py 28/28 exit 0, positive controls first): the BdG dichotomy (good branch xi tau3 + Delta tau1 -> real spectrum +/- sqrt(xi^2+Delta^2), Krein-self-adjoint w.r.t. eta_+ = I, both quasiparticle Krein types +1; pathological branch xi tau3 + i Delta tau2 -> +/- sqrt(xi^2 - Delta^2), complex for |xi|<Delta, Krein-self-adjoint w.r.t. naive tau3); the closed-form BCS gap Delta = W/sinh(1/(gN0)) matching the numerical root of the gap equation to 5e-4; condensation energy E_cond < 0; the Delta < mu_c = 9/(2R_s) validity threshold and the embed-when-Delta>=mu_c singular limit; the arrow-as-phase-winding sign on both branches. POSITIVE CONTROLS run FIRST: the tachyon seed m0^2 = -1/4 -> imaginary normal-mode frequency omega = i/2 (W130); mu_c = 4.5/R_s (W175); an attractive channel condenses with a real gapped BdG spectrum (standard Hilbert BCS). MATCHED NEGATIVE CONTROLS: a repulsive channel does NOT condense (no gap); a positive-definite metric shows NO complex collision (the pathology needs the Krein indefiniteness). CONVERGENCE re-encoded as live assertions: W186's same-sign-real / opposite-sign-exceptional-point structure; the W211 statement that the selector is one Godel-independent bit reported on both branches. STRUCTURAL / MODEL for the lift to GU: the model is a mean-field BdG / two-mode-per-pair stand-in for the full RS Krein D coupled through the (unbuilt) source action -- it DECIDES the STRUCTURE (existence of a genuine condensate; the real-vs-complex spectrum dichotomy; the Delta R_s < 9/2 validity gate; the arrow-as-phase reading) and CANNOT fix the coupling magnitude (g, N0, R_s are FIT / reconstruction-grade, exactly as W175's m2, R_s) nor the Krein sign that selects the branch (Godel-independent, W211). NO canon / RESEARCH-STATUS / claim-status / verdict / posture change; bar(b) and H59 stay OPEN; the count stays {1,3}; no forbidden target {3,8,24,chi(K3),Ahat} assumed or inserted. Zero em dashes."
construction: "program-native where the objects are GU's (the native tachyonic pole m0^2 = -1/4 and c_R = -4/9, W130; the record-count / conformal-scale mode carrying the tachyon, W153/W166; the mirror = closed-not-exact BRST record as 96 hyperbolic null pairs in ker(Gamma) of Cl(9,5)=M(64,H), K = the Cartan involution, W173; the essential-spectrum gap mu_c = 9/(2R_s) of D on the non-compact Y14, W175; the C-operator / eta_+ = eta.C physical metric, W132/W173; the bistable good/pathological fork, W186). Standard-field where the machinery binds any construction and is PORTED and CITED (BCS / Cooper pairing and the gap equation; the Bogoliubov-de Gennes mean-field Hamiltonian; Krein-space eigenvalues of positive/negative type and the opposite-type collision producing complex pairs, Bognar; Langer / Curgus-Najman regular-vs-singular critical points for the bounded C-operator; the Josephson / superfluid phase winding as the Goldstone clock). Every analogy labelled; none asserted of GU beyond its cited source. The reservoir Krein sign stays a GATED temporal-issuance / time-as-finality object (one-way rule respected; no cross-repo identity asserted)."
depends_on:
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W163-lens-nonperturbative-vacuum-2026-07-14.md
  - explorations/W166-lens-tachyon-is-the-engine-2026-07-14.md
  - explorations/W173-brst-cohomology-mirror-sector-2026-07-14.md
  - explorations/W175-build-analytic-fredholm-y14-2026-07-14.md
  - explorations/W186-source-content-reservoir-krein-type-2026-07-14.md
  - explorations/W211-krein-sign-godel-independent-five-method-synthesis-2026-07-14.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W216_true_vacuum_spectral_condensate.py
external_refs:
  - "Bardeen, Cooper & Schrieffer, Phys. Rev. 108 (1957) 1175 -- BCS gap equation; Cooper, Phys. Rev. 104 (1956) 1189 -- any attractive channel condenses (the existence engine)"
  - "de Gennes, Superconductivity of Metals and Alloys (1966) -- the Bogoliubov-de Gennes mean-field Hamiltonian and quasiparticle spectrum"
  - "Bognar, Indefinite Inner Product Spaces, Springer 1974 -- eigenvalues of positive/negative Krein type; an opposite-type collision produces complex conjugate pairs (the pathology decider)"
  - "Langer, Spectral functions of definitizable operators (1982); Curgus & Najman -- regular vs singular critical points; bounded C-operator iff the critical point is isolated (the validity gate, shared with W175)"
  - "Josephson, Phys. Lett. 1 (1962) 251 -- the condensate phase winds at a rate set by the chemical potential (the arrow-as-Goldstone-clock reading)"
cross_repo:
  - "temporal-issuance + time-as-finality: the reservoir Krein sign that selects the good (sensible) vs pathological branch is the finality-reservoir metric signature at the TI/time-as-finality boundary -- owned there, kept as a GATED conjecture. W211 proved it Godel-independent of GU's structure; W216 respects that and reports BOTH branches. The condensate coupling magnitude / gap scale (g, N0, R_s = the eta-from-gimmel-area) is the same FIT-gated object as W175/W180/W151. One-way rule respected; no GU claim moves, no TI/TaF claim moves, no identity asserted."
---

# W216 -- the record-condensed true vacuum, built as a spectral / BCS condensate

Test / build certificate: `tests/W216_true_vacuum_spectral_condensate.py` (28/28, exit 0, positive
controls first). This is one of FIVE independent teams each BUILDING the record-condensed true vacuum
by a DIFFERENT method, to test convergence. This team's method is SPECTRAL / CONDENSATE. Five personas
ran INLINE in one worker (spectral analyst; condensate / gap-equation (BCS) specialist; Krein /
C-operator specialist; mirror-record sector specialist; ruthless skeptic); no sub-agents. Zero em
dashes. This is a BIG SWING: it actually BUILDS the object W163 named PLAUSIBLE-BUT-UNBUILT, and grades
the build honestly.

## 0. The open question, and why the spectral method is the right cut

The native scalaron is tachyonic: `m0^2 = -1/4` (W122/W126/W130), a FALSE-vacuum instability. W163
computed the tell-tale Coleman-Weinberg false-vacuum signature `Im V_eff = m0^4/(64 pi) != 0` and named
the natural true vacuum -- the record-condensed phase -- but could not EXHIBIT it: out of EFT validity
by 4x, sourced by the unbuilt promotion-gate boundary term, and not shown stable (monotone-rolling, not
stationary). W163's honest residual is exactly "an un-built non-perturbative mechanism." We do NOT know
what GU rolls to. This wave builds it.

The spectral / condensate cut is the right one because a tachyon is, in the many-body language, a
**Cooper instability**: a negative `m^2` in a channel is an ATTRACTIVE interaction, and by Cooper's
theorem an attractive channel is ALWAYS unstable to forming a paired condensate with a nonzero gap.
The record-count / conformal-scale mode carries the tachyon (W153/W166), and the mirror/record sector
supplies the pairing partners: W173 built the mirror as a closed-not-exact BRST record sitting in
`ker(Gamma)` of `Cl(9,5) = M(64,H)` as **96 hyperbolic NULL pairs** `(generation, mirror)`, Krein
signature `(+96, -96, 0)`, with the Krein form `K` implementing the Cartan involution. A null pair is
precisely a particle-hole pairing channel. So the record-condensed vacuum IS a BCS condensate of the
mirror sector, and building it means solving the gap equation and diagonalising the Bogoliubov-de Gennes
(BdG) Hamiltonian in the Krein metric. The gap is a **spectral shift of the C-operator / Krein
spectrum** (W130 propagator; W175 essential-spectrum gap), which is exactly the object the other four
teams also target.

## 1. Persona 1 -- spectral analyst: the normal vacuum is unstable, the gap is a spectral shift

The uncondensed ("normal") record-count vacuum carries the tachyon literally: the homogeneous mode
frequency is `omega = sqrt(m0^2) = sqrt(-1/4) = i/2`, a purely imaginary (growing) frequency (PC1). An
imaginary frequency in the single-particle spectrum is the spectral fingerprint of a false vacuum, and
it is the same object W166 read as the arrow-of-time drive and W163 read as the Coleman-Weinberg
`Im V_eff`. The spectral question is whether a rearrangement of the spectrum removes it.

The rearrangement is the condensate. In BdG language the pairing gap `Delta` opens a spectral gap: the
single-particle level at `xi` and its Krein partner at `-xi` hybridise into quasiparticles at
`+/- sqrt(xi^2 + Delta^2)` (good branch). The imaginary frequency at `xi = 0` is lifted to the real
value `Delta`. The condensate is thus a genuine spectral shift of the Krein spectrum, and the whole
verdict is whether that shifted spectrum is real and bounded (sensible) or acquires complex poles
(pathological). The natural ceiling for the shift is the W175 essential-spectrum gap
`mu_c = 9/(2 R_s) = 4.5/R_s` (PC2): the condensate lives INSIDE the gap, and the pairing band is capped
by it.

## 2. Persona 2 -- condensate / gap-equation (BCS) specialist: the condensate EXISTS

Solve the self-consistent gap equation on the symmetric band `[-W, W]` capped by the essential-spectrum
gap (`W = mu_c(R_s)`), with a flat density of states `N0` and pairing coupling `g`:

```
1 = g N0 integral_0^W dxi / sqrt(xi^2 + Delta^2) = g N0 arcsinh(W/Delta)
   =>  Delta* = W / sinh(1/(g N0))     (closed form; matches the numerical root to 5e-4, PC3)
```

For any `g > 0` this has a NONZERO solution `Delta* > 0` (PC3, and the Cooper robustness check: every
attractive coupling condenses -- existence needs no fine-tuning). At `R_s = 2`, `g = 0.5`, `N0 = 1`
(reconstruction-grade illustrative values), `W = mu_c = 2.25` and `Delta* = 0.620`. The condensation
energy relative to the uncondensed false vacuum is

```
E_cond = -(1/2) N0 Delta*^2 = -0.192 < 0,
```

so the condensed state lies BELOW the false vacuum: it is the TRUE vacuum. **This is the answer to
question (1): a genuine condensate EXISTS, as a nonzero gap-equation solution of lower energy than the
empty vacuum W163 could only name.** The magnitude of `Delta*` is FIT-gated (through `g`, `N0`, `R_s`),
exactly as W175's `m2` and `R_s` are; the EXISTENCE and the sign of `E_cond` are structural (Cooper),
not fit-dependent.

## 3. Persona 3 -- Krein / C-operator specialist: the spectrum, on both branches (the crux)

This is the decisive computation, and it is exact (Block 2). Per null pair the BdG Hamiltonian is a
`2x2` block. The branch is set by the relative Krein sign of the paired partners, which is the operative
question W186/W211 isolated.

- **GOOD branch (interacting C-operator OPERATIVE).** Under the C-metric `eta_+ = eta.C` both partners
  of a mirror null pair are POSITIVE-norm (W173: the mirror is a positive-norm record under the
  C-metric). The BdG block is `H = xi tau3 + Delta tau1`, Krein-self-adjoint w.r.t. `eta_+ = I`, with
  eigenvalues
  ```
  E = +/- sqrt(xi^2 + Delta*^2)   --   REAL, BOUNDED below by |Delta*|, gapped.
  ```
  The quasiparticle vacuum has POSITIVE Krein metric (both eigenvectors of Krein type `+1`). This is a
  SENSIBLE gapped condensate: real spectrum, positive metric, a genuine mass gap `Delta*`.

- **PATHOLOGICAL branch (C NOT operative, naive metric).** Under the naive Krein metric
  `eta = diag(+1, -1)` the pair is a null / opposite pair: the gap couples a Krein-POSITIVE mode to a
  Krein-NEGATIVE one. The BdG block is `H = xi tau3 + i Delta tau2`, Krein-self-adjoint w.r.t. the naive
  `eta = tau3`, with eigenvalues
  ```
  E = +/- sqrt(xi^2 - Delta*^2)   --   COMPLEX for every |xi| < Delta*.
  ```
  Because the condensate band runs down to `xi = 0`, complex quasiparticle pairs are ALWAYS present
  (at `xi = 0`, `E = +/- i Delta*`). This is the opposite-type eigenvalue collision of Krein-space
  theory (Bognar): two eigenvalues of OPPOSITE type colliding produce a complex-conjugate pair. The
  condensed vacuum is itself dynamically unstable; the spectrum is not real. This is PATHOLOGICAL, and
  per the mandate it is a REAL (negative) result: a condensate with an un-real spectrum, exhibited, not
  hidden.

The negative control confirms the structure is Krein-driven: under a POSITIVE-DEFINITE metric the
opposite-pair coupling is Hermitian and CANNOT produce a complex collision (NC2). The pathology needs
the indefiniteness. And the same-sign-real / opposite-sign-complex fork reproduces W186's bistable
exceptional-point structure exactly (the CONV checks).

**The selector is one Krein sign, and it is not GU's to set.** Which branch the condensate realises is
whether the paired partners are like-signed under the operative metric -- the reservoir Krein sign. W211
proved (five independent methods, unanimous) that this bit is Godel-INDEPENDENT of GU's good-stable
structure. W216 therefore does NOT fix it; it reports the spectrum on both branches. This is the honest
core: the condensate EXISTS regardless, but its NATURE (sensible vs pathological) is exactly the one
external bit.

## 4. Persona 4 -- mirror-record-sector specialist: the arrow of time at the condensate (W166)

Does condensing the tachyon KILL the arrow of time? W166 read `m^2 < 0` as the arrow (record-count `N`
grows) and warned that a STABLE mode is a dead, record-less universe. Condensing gaps the tachyon
(`mass^2` flips from `-1/4` to `+Delta*^2 > 0`), so naively the arrow would die. It does not, and the
resolution is the Goldstone.

The condensate spontaneously breaks the record-count `U(1)` (the conformal / scale mode). Its Goldstone
is a phase `theta` that WINDS at a rate set by the record chemical potential (Josephson / superfluid):
`dtheta/dtau = 2 mu_rec`. On the GOOD branch the spectrum is real, so this rate is real, positive, and
bounded -- a monotone phase winding. **The arrow of time survives at the condensate as the winding of
the condensate phase, not as an ongoing instability.** This is precisely W166's "steady de-Sitter-rate"
clock, but now realised as a STATIONARY object (a gapped condensate with a winding phase) rather than a
runaway -- which answers W163's "monotone-rolling, not stationary" objection: the rolling becomes the
steady phase winding of a genuine stationary vacuum. The DBI-clock reads the phase. On the PATHOLOGICAL
branch the spectrum is complex, so the winding rate is complex and the clock reading is ill-defined: the
arrow FAILS there. So the arrow-of-time reading (W166) HOLDS at the condensate on the good branch and
ONLY there. Graded PLAUSIBLE: the Goldstone-clock identification is a coherent structural reading, not a
first-principles derivation of the winding rate (the record chemical potential is the same FIT-gated
magnitude).

## 5. Persona 5 -- ruthless skeptic: is this a real build or a dressed-up assumption?

**Steelman that nothing was built.** (a) BCS existence is a THEOREM about attractive channels; feeding
in "the tachyon is attractive" and getting "a condensate exists" is circular -- you assumed the
instability condenses and it condensed. (b) The good branch's real spectrum is bought by assuming the
C-metric, whose operativity is the entire open question (the same circularity W186 flagged). (c) The
gap scale `Delta*` is set by free parameters `g, N0, R_s`; nothing is predicted. (d) The mean-field BdG
model is a `2x2`-per-pair toy, not the full RS Krein operator on `Y14`.

**Answer, and precisely how much the wave earns.** (a) is conceded as the ENGINE and is exactly why the
existence result is robust rather than fragile: Cooper's theorem is what makes "a genuine condensate
exists" a structural fact, not a fit -- and W216's addition is that the existence is CAPPED and the
spectrum DECIDED by GU-specific objects (the W175 gap `mu_c`, the W173 null-pair Krein structure), not
by generic BCS. (b) is the real limit and is handled by reporting BOTH branches and refusing to fix the
sign (W211): the wave does NOT claim the good branch; it claims the DICHOTOMY, and locates the sensible
condensate exactly on the operative-C bit that five methods already proved external. (c) is conceded:
the gap MAGNITUDE is FIT-gated, precisely as W175's `m2, R_s` are; what is NOT fit-gated is the
existence, the real-vs-complex dichotomy, and the `Delta R_s < 9/2` validity gate. (d) is conceded as
STRUCTURAL vs full-operator: the model decides the structure and cannot replace the unbuilt source
action. **Skeptic verdict: a GENUINE build of the condensate's EXISTENCE and its spectral dichotomy,
NOT a fixing of the branch or the scale; the negative (pathological) branch is exhibited as a real
result, not hidden.**

## 6. Validity and the convergence with W175

The condensate is bounded and its spectrum real IFF the gap stays INSIDE the essential-spectrum gap:
`Delta* < mu_c = 9/(2 R_s)`, i.e. `Delta* R_s < 9/2` (Block 4). At the illustrative point
`Delta* R_s = 1.24 < 4.5`: the condensate mode is isolated from the continuum (regular critical point,
Langer/Curgus-Najman), so the C-operator is BOUNDED and the spectrum real. Strong pairing drives `Delta*`
ABOVE `mu_c`; the condensate mode then EMBEDS in the continuum (singular critical point) and the
C-operator becomes UNBOUNDED. This is EXACTLY W175's dichotomy, reached by an independent route: W175
found the C-operator bounded IFF `m2 R_s < 9/2` (the ghost mass shell isolated from the essential
spectrum); W216 finds the CONDENSATE bounded IFF `Delta* R_s < 9/2` (the condensate mode isolated from
the same essential spectrum). The `9/2` half-sum-of-roots threshold governs both. This is a genuine
convergence: two methods (analytic Fredholm; spectral condensate) put the validity gate at the same
number.

## 7. Verdict

**EXISTS-SENSIBLE (conditional on the operative-C good branch) / EXISTS-PATHOLOGICAL (opposite
branch).**

- **(1) Does a genuine condensate exist?** YES. The tachyon `m0^2 = -1/4` is a Cooper-attractive channel;
  the gap equation has a nonzero solution `Delta*` with condensation energy `E_cond < 0`. The condensed
  state is the TRUE (lower) vacuum W163 named but could not exhibit. Existence is structural (Cooper),
  the scale is FIT-gated.
- **(2) Its nature.** Branch-dependent, exactly. GOOD branch (C operative): BdG spectrum
  `+/- sqrt(xi^2 + Delta*^2)`, REAL, BOUNDED below by the gap `Delta*`, POSITIVE Krein metric -- a
  sensible gapped condensate. OPPOSITE branch (C not operative): BdG spectrum
  `+/- sqrt(xi^2 - Delta*^2)`, COMPLEX for all `|xi| < Delta*` (down to `xi = 0`), an opposite-type
  Krein collision -- PATHOLOGICAL. The selector is one Krein sign, Godel-independent (W211), NOT fixed
  here.
- **(3) The arrow-of-time / DBI-clock reading (W166).** HOLDS at the condensate on the good branch and
  only there: condensing gaps the tachyon, and the arrow survives as the condensate phase (Goldstone)
  winding at a real, monotone, bounded (Josephson) rate = W166's steady de-Sitter-rate clock, now a
  stationary object. On the pathological branch the winding rate is complex and the clock ill-defined.
- **(4) Validity / needed extension.** The spectrum is real and the C-operator bounded IFF
  `Delta* < mu_c = 9/(2 R_s)`, i.e. `Delta* R_s < 9/2` -- the SAME threshold as W175's `m2 R_s < 9/2`.
  The needed extension is to pin `R_s` (the fiber curvature radius) and the pairing magnitude so the gap
  stays inside the essential-spectrum gap; strong coupling embeds the mode and voids the build. This is
  the same FIT-gated `eta`-from-gimmel-area magnitude the whole arc reduces to.

**On bar(b) and H59: UNCHANGED / OPEN.** The condensate is now BUILT (existence, spectrum dichotomy,
validity gate), which is more than W163 had, but the sensible-vs-pathological branch is exactly the
external Krein sign W211 proved GU cannot force. A condensate with an un-real spectrum is a real
negative result and it is precisely the pathological branch; W216 exhibits it rather than hiding it.

## Convergence note

W216 is the SPECTRAL / CONDENSATE build, one of five independent methods. As of filing this note the
other four teams' notes were not yet on `origin/main` for me to read (the sibling builds run in
parallel), so this convergence note records the STRUCTURAL agreements W216 can already establish against
the shared upstream arc (W175, W186, W211), which every method must reproduce, and flags where a genuine
disagreement would show up.

- **Agreement on EXISTENCE.** W216 finds a genuine condensate exists (nonzero gap, `E_cond < 0`). Any
  method that builds the true vacuum should agree the condensate EXISTS; W163 already computed the
  false-vacuum signature (`Im V_eff != 0`) that guarantees a true vacuum elsewhere, so a method that
  found NO condensate would be the surprising outlier and should be scrutinised first.
- **Agreement on the DICHOTOMY and its selector.** W216's central result -- the spectrum is
  real/bounded/Krein-positive on the operative-C branch and complex/pathological on the opposite branch,
  with the selector being one external Krein sign -- is forced to agree with W186 (the bistable fixed
  point: two self-consistent solutions, selection external) and W211 (the selector is one
  Godel-independent bit). A method that claimed a UNIQUE, GU-forced sensible vacuum would CONTRADICT
  W211's five-method unanimity and should be treated as either a breakthrough (it found the missing
  datum inside GU) or an error (it smuggled in the C-metric). W216 predicts the former is ruled out.
- **Agreement on the VALIDITY number.** W216's `Delta* R_s < 9/2` for a bounded C-operator coincides
  exactly with W175's `m2 R_s < 9/2`. A method that builds the vacuum through the essential spectrum of
  `D` on `Y14` should land on the same `9/2` half-sum-of-roots threshold; a different threshold would be
  a real disagreement worth resolving (likely a different fiber-multiplicity or `rho`-shift input).
- **Where a genuine disagreement could surface.** (i) The gap SCALE `Delta*`: W216 leaves it FIT-gated
  (through `g, N0, R_s`); a method that predicts an absolute condensate scale without an external
  magnitude would disagree and should be checked for a hidden normalization. (ii) The arrow reading: W216
  says the arrow survives as the Goldstone phase winding (stationary), reconciling W163's
  "monotone-rolling, not stationary" objection; a method that finds the true vacuum genuinely
  non-stationary (eternally rolling) would disagree on (3) and the difference would be physical, not
  cosmetic. (iii) Whether the condensate is the ONLY true vacuum or one of several (W186 bistability
  suggests at least two: the sensible and the pathological); a method finding a THIRD branch would
  extend, not contradict, W216.

Net convergence claim: on the three load-bearing bits W216 can check against the shared arc
(existence; the branch dichotomy with an external Godel-independent selector; the `9/2` validity
threshold) W216 AGREES with W175/W186/W211, and it PREDICTS the other four methods agree on these or
else have found/smuggled the external bit. The one place all five should be watched for real
disagreement is the gap SCALE and the stationary-vs-rolling character of the arrow.

## What this does NOT do

- No claim that GU realises the good branch: the sensible condensate is CONDITIONAL on the operative-C
  Krein sign, which is Godel-independent (W211) and not fixed here. Both branches are reported.
- No construction of the full RS Krein operator on `Y14` or the source action: the BdG model is a
  mean-field stand-in that decides the structure, not the full propagator (W130/W175 own that).
- No new normalization for the gap scale or the record chemical potential: `g, N0, R_s, mu_rec` are
  FIT / reconstruction-grade, exactly as W175's `m2, R_s`.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count stays `{1,3}`;
  `bar(b)` and H59 stay OPEN; no forbidden target `{3,8,24,chi(K3),Ahat}` assumed or inserted. No
  cross-repo identity asserted; the reservoir Krein sign stays a gated temporal-issuance /
  time-as-finality object. Zero em dashes in paper-facing text.

**Artifacts:** this file + `tests/W216_true_vacuum_spectral_condensate.py` (28/28, exit 0; positive
controls first).

*Filed 2026-07-14 by TEAM SPECTRAL-CONDENSATE (W216), one of five independent build teams. Five
personas inline in one worker (spectral analyst; condensate / gap-equation (BCS) specialist; Krein /
C-operator specialist; mirror-record-sector specialist; ruthless skeptic); no sub-agents. Reproducible:
`python -u tests/W216_true_vacuum_spectral_condensate.py` (28/28, exit 0). Exploration grade;
conditional register; no canon movement; bar(b)/H59 OPEN.*
