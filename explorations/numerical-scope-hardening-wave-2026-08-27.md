---
title: "Numerical and scope hardening wave"
status: active_research
doc_type: process
created: "2026-08-27"
grade: "REPRODUCED NUMERICAL AND SCOPE CUSTODY; NO SCIENTIFIC VERDICT CHANGE"
target_claim: "NONE-NOT-A-KILL; two numerical implementation repairs and two semantic fences"
canon_verdict_change: none
ledger_edit: none
manifest: lab/process/numerical-scope-hardening-wave.json
probe: tests/channel-swings/numerical_scope_hardening_wave_probe.py
---

# Numerical and scope hardening wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: this wave specifies one review-gated complex-branching comparator fence,
one fixed-point stopping certificate, one calibrated scalar root and one
boundary-assumption disclosure. It does not choose an ambient signature,
construct a real bridge, change a likelihood, supply a boundary action or move
a scientific verdict.

```gu-typed-objects
result: three unprotected defects repaired and one protected scope correction specified but not applied
carrier: complex Spin(10) branching comparator; H44 dimensionless fixed-point residual; W129 calibrated scalar Delta-chi2 function; M-M20 conditional boundary-identification diagram LAYER=UNTYPED CHIRALITY=N/A
pairing: source-real-form versus ambient-signature custody; iterate-to-iterate scaled norm; bracketed scalar root evaluation; claimed consequence versus owned maps ON=four-distinct-hardening-arcs
real_structure: split Cl(7,7) source algebra remains distinct from the open (9,5)/(7,7) ambient fork; numerical carriers are real scalars; no boundary reality convention selected
grading: comparator-only verification, numerical convergence, statistical root isolation and conditional semantic routing remain distinct
action_owner: UNTYPED -- none newly supplied; the boundary and source-native continuations remain owner-gated
target: P-M25, P-L12, P-L13 and M-M32 hardening dispositions MAP-TYPE=evaluation
```

## Preflight bookend

The source-to-proof route remains authority-gated at SC-SIG-55, all 91 ledger
rows remain terminal, and the named B2 action-root set is empty. The global eta,
DESI/Planck and boundary-action routes still lack their owned packets, while
protected corrections remain outside unattended Progress. Mechanism-level
retrieval nevertheless exposed three compatible unprotected defects and one
protected correction. The latter is preserved as an exact reviewed reopener
rather than applied by unattended Progress.

## P-M25 — protected comparator and real-form scope

The required Pati--Salam fence must distinguish three statements that are easy
to collapse. The source algebra is split `Cl(7,7)` / `Spin(7,7)`. The ambient
signature remains an open fork between the repository's `(9,5)` reconstruction
and source `(7,7)` presentation. The verified finite-dimensional complex
`Spin(10)` branching is insensitive to that real-form choice and must be
classified as a conventional comparator, not an ambient-signature decision or
real equivariant bridge. Because the target lives under `lab/active-research/`,
the protected-surface guard rejected unattended application. The verifier and
its group-theory verdict remain unchanged pending a separately reviewed pass.

## P-L12 — scale-aware fixed-point convergence

H44 formerly compared a dimensionful absolute update with `1e-12` and reached
roundoff. Its stop now uses the maximum componentwise relative update with a
unit floor and retains the absolute update as a separate diagnostic. Exact
reproduction passes with relative residual `1.8e-16` and absolute update
`1.8e-15`; the global-minimum, LCDM-control and distance conclusions do not
change.

## P-L13 — direct calibrated crossing

W129 formerly log-interpolated the first `Delta chi2=9` crossing across a
factor-2.5 amplitude gap. It now calls the calibrated likelihood inside a
bracketed Brent solve. The direct roots are `f0_9=0.0302`, `0.0418` and `0.2213`
for `M2=8`, `7` and `3`. The verdict remains `HOLDS-BAND-WIDE`; the minimum
CPL-matched penalty remains `Delta chi2=+33.46`.

## M-M32 — assumption-fenced boundary route

M-M20's proposed 13-dimensional defect action schedules a consequence across
two unproved identifications: count boundary equals firewall adapter, and
firewall adapter equals the boundary-value-problem boundary. The register now
states both assumptions and requires D3's boundary operator/domain, D4's
source/action ownership and an exhibited map for each identification. This is
not a claim that the identifications are false; it prevents an unowned
conditional from being read as an established boundary object.

## Postflight bookend

- **Strongest overclaim:** the source ambient signature is settled, the GU
  model is statistically rescued or excluded, or a physical boundary action
  exists. Rejected: none follows from these repairs.
- **Strongest contrary route:** an explicit real equivariant bridge, changed
  numerical model/likelihood, or owned D3/D4 boundary packet could reopen the
  corresponding arc. Each remains preserved as an exact condition.
- **Weakest reproducibility seam:** the numerical conclusions depend on two
  full executable reproductions, while the scope conclusions depend on exact
  semantic tokens and a preserved protection boundary. The focused gate pins
  both code paths, the boundary disposition and M-M32 fence and catches seven
  planted mutations.

H44 and W129 pass after the repairs. The focused custody gate passes all nine
checks and catches all seven mutations. No source claim, ledger row, action
owner, SG4 phase, prediction, confirmation, canon, protected paper, release
state or public posture moves.
