---
title: "V2.1 Meta-Reframed Parallel Card Synthesis"
status: exploration
doc_type: synthesis
updated_at: "2026-05-31"
---

# V2.1 Meta-Reframed Parallel Card Synthesis

Generated: 2026-05-31
Scope: WRK-388, WRK-389, WRK-390 rerun after the later meta reframing.

## Executive Verdict

The v2.1 rerun does not remove the v1 results. It preserves them as provenance and
supersedes several strategic conclusions.

The new center is:

```text
monotone provenance + non-monotone signed/readout projection
```

not:

```text
CALM equals GW
anomaly-free iff CALM-monotone
direct C_CALM <-> C_GW adjunction
universal shadow/source meta-theorem already proven
```

## WRK-388

Artifact:

```text
CapacityOS/work/drafts/wrk-388-anomaly-iff/v2.1-meta-reframed-signed-readout-verdict-2026-05-31.md
```

V2.1 verdict:

```text
FAILS as an iff.
```

The anomaly-iff theorem is too broad. The primitive theorem is the signed
readout monotonicity criterion:

```text
R : E -> G is monotone iff every generator weight is nonnegative.
```

Anomaly/index structure is a physics mechanism that produces signed
cancellation, not the general mathematical iff condition.

## WRK-389

Artifact:

```text
CapacityOS/work/drafts/wrk-389-adjunction-lift/v2.1-meta-reframed-adjunction-rerun-2026-05-31.md
```

V2.1 verdict:

```text
OBSTRUCTED-AS-STATED / CONSTRUCTED-AS-CORRECTED.
```

The direct adjunction `C_CALM <-> C_GW` remains obstructed. But the categorical
path is not closed. The corrected category is monotone provenance with signed
readout:

```text
C_GW_loc -> C_MPR -> C_Shadow
```

CALM is the monotone/readout-preserving subcase inside the larger
provenance/readout architecture.

## WRK-390

Artifact:

```text
CapacityOS/work/drafts/wrk-390-meta-template/meta-reframed-v2.1-shadow-source-theorem-rerun-2026-05-31.md
```

V2.1 verdict:

```text
PARTIAL.
```

The shadow/source/failure template is a powerful rigor filter, not yet a
universal meta-theorem. It becomes theorem-grade only after the source category,
projection/readout, observer protocol, provenance state, and assumption-
reflection relation are explicitly typed.

## Combined Synthesis

The three cards now converge:

```text
source/local-rule dynamics
  -> monotone provenance/certified contribution state
  -> signed or phase-sensitive semantic readout
  -> observer/shadow protocol
  -> no-go assumptions may fail to reflect
```

This is the Hegelian synthesis:

- Thesis: CALM/GW might identify the same class of observables.
- Antithesis: signed/index-bearing GW observables break CALM monotonicity; direct
  categorical equivalence is obstructed.
- Synthesis: CALM describes the monotone provenance/observer-coherence layer,
  while GW/anomaly/local-rule systems expose a broader source/readout structure.

## Recommended WRK-386 Pivot

WRK-386 should pivot to a theorem-first paper:

```text
Monotone Provenance, Non-Monotone Readout:
A Boundary Theorem for the CALM / Ginsparg-Wilson Analogy
```

Core package:

1. Signed readout monotonicity criterion.
2. PN/Jordan monotone provenance factorization.
3. GW axial charge as a corollary.
4. `C_MPR` categorical bridge as corrected structure.
5. Shadow/source assumption-reflection theorem as meta-frame.
6. Wolfram/local-rule systems as the broader superframe.

## Next Agent Path

The best next bounded task is not another broad synthesis. It is a rigorous
theorem-core drafting pass:

```text
Definition 1: evidence/provenance monoid
Definition 2: ordered signed codomain
Theorem 1: signed readout monotonicity criterion
Theorem 2: PN/Jordan provenance factorization
Corollary 1: GW axial charge non-monotonicity
Definition 3: C_MPR
Proposition 1: CALM is the monotone readout subcategory
Open Conjecture: source/shadow assumption-reflection boundary
```
