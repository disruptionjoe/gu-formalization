---
title: "Portfolio correction wave: claims, quotients, defects and durable priorities"
artifact_type: exploration_result
created: 2026-08-12
status: PORTFOLIO_CORRECTED__FOUR_EXACT_CHECKS_SCOPED__CONSTRUCTION_FRONTIERS_RETAINED
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# Portfolio correction wave

## Result first

One coordinated pass validated the ten proposed portfolio corrections, ran four
bounded calculations, and separated inexpensive interpretation debt from the
remaining construction programs.  It does **not** solve the source action,
physical carrier, emergent chirality, interacting positivity, or the generation
count.

The durable changes are:

1. `SC-GEN-54` is now typed as a live **dynamical generation mechanism**.  The
   repo has an exact graded inhomogeneous-gauge extension, but no map from that
   extension to exactly three physical families.
2. `SC-GEN-53`, not `SC-GEN-57`, is the untyped two-versus-one high-energy
   prediction.  It becomes a Lane-2 seed, not a prediction packet: the scale,
   invariant discriminator and observable are still absent.
3. Weinstein's equation (12.19) is filed as a named **source resolver** for the
   unordered ambient signature `{7,7}`.  `SIGNATURE-AMBIENT` remains open: the
   equation assigns the horn but does not derive the relative block sign from
   the GU action.
4. The compatible-`J` orbit has zero-dimensional local quotient **if** the full
   `q`-stabilizer is an action-, observation- and BV-owned gauge symmetry.  That
   premise is not yet constructed, so no 20-coordinate residue reduction is
   booked.
5. The ten rank-128 transverse leaks are not one repeated subspace.  They are
   pairwise disjoint and span rank 1280.  They form an exact
   `so(6) + so(4)`-equivariant candidate normal-covector/spinor package, but the
   first full `so(6,4)` intertwiner convention fails all 24 mixed boosts.
6. The M-H17 free-comparator instrument returns descended `H^0` inertia
   `(96,96,0)`.  The comparator therefore fails positive definiteness.  This is
   not interacting K77 physical positivity.
7. The B5 five-field blocker is retyped field by field.  The absolute coflip
   value is a globally mistyped stored value; only its unordered pair is
   invariant.  Failure of a canonical section-domain selector does not imply
   failure of the one common domain the B5 contract asks for.
8. Canonicity distance is now a typed tuple.  Continuous coordinates,
   function-valued slots, discrete forks, conditional quotients and proven
   nonselectors are not subtracted into one misleading scalar.
9. The inherited eleven-file root signature/reality inventory gap is closed by
   an explicit sector; no file is forced into a false pre-existing category.

## Adaptive preflight

| lens | basis | confidence | decisive concern |
|---|---|---:|---|
| source criticism | direct | very high | preserve `SC-GEN-53`, `54`, and `57` as distinct claims |
| Lie superalgebra | direct | high | algebraic closure is not a count map |
| anomaly matching | direct | high | W224 is necessary-condition tooling, not emergent-chirality construction |
| homogeneous spaces | direct | very high | orbit transitivity is not a physical gauge quotient |
| representation theory | direct | high | equal rank is not module equivalence |
| Krein/BV operator theory | direct | high | comparator cohomology is not interacting physical cohomology |
| topology/descent | direct | high | a `Z/3` receptacle may act as a torsor; it does not additively read out three |
| symplectic geometry | principle | high | finite DeWitt inertia and Green eligibility are below reduced phase-space grade |
| phenomenology | principle | high | a source sentence without scale and observable is a seed, not a prediction |
| measurement architecture | principle | high | heterogeneous freedom types require a vector-valued meter |

Lens output selected tests; it is not scientific evidence.  Every result below
rests on an exact certificate or a cited source row.

## Packet 1 — claim and dependency correction

### `SC-GEN-54`: supercharges and the count

The source asserts that extending the inhomogeneous gauge group by supercharges
fixes the effective count at three.  The repo's exact graded extension has
odd-odd bracket in `Omega^1(ad)`, but no computation maps it to exactly three
physical families.  The older H54 super-Poincare branch is a different object
and also leaves its multiplicity `N` free.

> **FRAME-SENSITIVE:** The repo has an exact graded inhomogeneous-gauge
> extension whose odd-odd bracket lands in `Omega^1(ad)`, but no computation
> maps that extension to exactly three physical families; the existing
> super-Poincare H54 branch is a different object and likewise leaves `N`
> unfixed.

This earns PARTIAL adherence: the mechanism is now named and its existing
algebraic ingredient linked, while the claimed count remains unconstructed.

### `SC-GEN-53`: the two-versus-one tripwire

`SC-GEN-53` says that at higher energy two families remain equivalent up to
mass while the imposter does not. `SC-GEN-57` is the already-adhered rolled-up
2+1 claim.  The former has no source-supplied scale, observable or discriminant,
so prediction research owns a seed with three admission burdens rather than a frozen packet.

### Anomaly, carrier and B5 scope

The W224 certificate reruns green on its old `(9,5)` content, but a K77 port
must first construct the K77 16-content map and remains only a necessary anomaly
condition.  `PH-K1-PHYSICAL` and `OQ-RK1` still block any promotion from the
kinematic carrier to physical families.

For B5, the field-by-field update replaces the stale five-blank-field summary:

- the coflip support and relative transport exist;
- an absolute ordered phase is globally ill-posed, with only the unordered
  pair invariant under its nontrivial `Z/2` local system;
- the formal-adjoint sign/pairing table is the genuine bounded computation;
- a formal Green packet exists below packet grade;
- the section-domain moduli have no canonical selector at filed symmetry, but
  B5 asks for one common compatible domain and the ambient domain remains open;
- RB6's exact-derivative recertification remains a prerequisite to resuming an
  interior verdict.

## Packet 2 — signature and `J` quotient

### Signature Wave A

The exact DeWitt-family check reproduces the fibre inertias and proves base-sign
independence.  Weinstein's equation (12.19) directly displays

```text
g*(TY^(7,7)) = TX^(1,3) + N_g^(6,4).
```

This gives a convention-invariant authorial assignment of `{7,7}`.  It does
not supply the missing metric/action formula selecting the relative sign.

> **FRAME-SENSITIVE:** `SIGNATURE-AMBIENT` earns a named source resolver:
> Weinstein's eq. (12.19) authorially assigns the ambient pullback the unordered
> signature `{7,7}` in one uniform display. It does not earn settlement,
> because no allowed receipt derives that relative block sign from the GU action
> or proves the resulting physical analytic structure.

`REAL-CLIFFORD-FORM` remains distinct.  The two complex `C^(32,32)` halves,
their `U(32,32) x U(32,32)` block subgroup, and the full `U(64,64)` parent are
not signature selectors.

### Compatible-`J` orbit

The exact infinitesimal dimensions are

```text
dim so(6,4)=45,  dim u(3,2)=25,
dim stab(q)=36, dim stab(q,J)=16.
```

Both orbit differences are 20.  Hence all 20 local `J` directions are gauge
orientation if the relevant physical gauge algebra is

```text
im(d_BV,-1) intersect stab(P_obs) intersect stab(S_selected) intersect stab(q,H_q)
```

and its map `X -> [X,J]` has rank 20.  That intersection, its global
components, and descent are unbuilt.  The canonicity meter records a
**conditional possible reduction of 20**, not a booked quotient.

## Packet 3 — defect module and comparator positivity

### Ten rank-128 leaks

On the exact v0.184 fixture over `GF(1009)`:

```text
rank D_i = 128                       for all 10 directions
rank(D_i + D_j) = 256                for all 45 pairs
dim(D_i intersect D_j) = 0           for all 45 pairs
rank(sum_i D_i) = 1280
```

The naive single-common-subspace hypothesis is falsified.  A candidate
`N* tensor S` map is exact for all 21 same-sign `so(6)+so(4)` generators and
fails all 24 mixed boosts in the first convention.  The next bounded check is
to solve for ten nonzero covector normalizations and a possible common spinor
endomorphism across all 45 generators, then repeat over a second prime.  Only
after that may the ten bookkeeping entries become one full normal-module
extension problem.

### M-H17 comparator

The new exact `H^0` instrument composes the filed free-complex incidence with
the filed 96-pair Krein bank:

```text
rank Q[-1] = 192, dim H^0 = 192,
descended inertia = (96,96,0).
```

The pairing descends and is nondegenerate but indefinite, so comparator PC-3
fails.  Quartet, non-radical, sign-flip, positive and symplectic-type controls
all fire.  The interacting K77 BRST differential, physical carrier, descended
pairing and domain remain open.

## Canonicity distance v0.1

```text
raw coordinates: continuous prequotient 84; action-parent range 84..86
function slots: >=19, typed by gauge/boundary/domain/source/unclassified
discrete: 9 open forks; horn product 1152; external bit/integer separate
reductions: applied global 0; booked conditional 5
provisional: J local orbit may remove 20 iff the physical BV stabilizer has rank 20
status vocabulary: FORCED | GAUGE_EQUIVALENT | CHOICE_INDEPENDENT |
                   EXTERNAL_DATUM | PROVEN_UNSELECTABLE |
                   OPEN_UNRANKED | CONDITIONAL
physical residue: UNCOMPUTED until scopes compose
```

No headline row count, residue, fork, quotient, P1/P2/P3, canon or public
posture changes in this wave.

## Highest-value construction programs remaining

1. **Action-owned tangent/BV reduction:** construct the physical gauge algebra,
   test the `J` orbit rank, and apply it to the twelve-cell and transverse
   residual problems.
2. **Physical carrier and emergent chirality:** build `Pi_RS^phys`, type the K77
   family content, then port the W224 anomaly ledger as a necessary check.
3. **Supercharge count map:** connect the graded IG extension to physical
   family cohomology and ask whether three is selected rather than hosted.
4. **B5 completion:** exact RB6 recertification, pairing-table/formal-adjoint
   sign, Green form and verdict-stability across one common-domain family.
5. **Rank-128 normal module:** settle the mixed-boost intertwiner before any
   extension-class construction.
6. **Lane-2 tripwire:** give `SC-GEN-53` an energy scale, representation
   invariant and observable before packaging it.

The old W224 regression is preserved as prior-art validation, not counted as a
new K77 calculation.  `fork_depth_audit.py` scans eleven fork-declaring
dispositions and finds one historical declaration using the invalid process ID
`SIGNATURE_AMBIENT_K77`; this wave reports it and does not silently rewrite what
that wave said it assumed.

## Receipts

- `tests/channel-swings/portfolio_signature_ambient_wave_a_probe.py`
  — `20/20` exact/source checks.
- `tests/channel-swings/portfolio_j_orbit_local_quotient_probe.py`
  — `10/10` exact checks.
- `tests/channel-swings/portfolio_rank128_defect_module_probe.py`
  — all 45 pairwise ranks, rank-1280 span, 21 same-sign and 24 mixed-boost
  intertwiners checked exactly over `GF(1009)`.
- `tests/channel-swings/portfolio_mh17_comparator_h0_inertia_probe.py`
  — `10/10` exact checks.
- `tests/channel-swings/conditional_physics_ledger_v0227_probe.py`
  — `15/15` migration/accounting/plant checks.
- `lab/process/hostile-reviews/2026-08-12-portfolio-correction-wave-review.md`

The v0.226 ledger regression, W224 anomaly and dynamical-vacuum controls,
signature/Clifford regressions, learning-transport, lane freshness, kill-target,
root-test inventory, process-gate inventory, JSON/YAML uniqueness and whitespace
checks all pass. `fork_depth_audit.py` retains its one pre-existing red:
historical `SIGNATURE_AMBIENT_K77` is not a registry ID and is not silently
rewritten here.
