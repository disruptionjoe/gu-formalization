---
artifact_type: exploration
status: exploration (torsion generation arena made precise; J-homomorphism/framing route computed on the frozen rep; five personas inline, one worker, no sub-agents; deterministic probe, exit 0; outcome shapes pre-declared before computation)
created: 2026-07-20
directed_by: "Joe direct chat, 2026-07-20 (summit wave: torsion generation arena)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
title: "The generation-count torsion arena (Z/3 in Z/24 = pi_3^s) made precise, and the J-homomorphism/framing-twist route computed on the frozen material. ARENA: pi_3^s = Z/24 is generated by nu (the quaternionic Hopf class) and is EXACTLY the image of J : pi_3(SO) = Z -> Z/24 (Adams), so a framing-twist integer k lands at k mod 24 -- the arena is concretely computable the moment the construction pins a k. ROUTES ENUMERATED AND COMPUTED (fixture grade, on the verified Cl(9,5) = M(64,H) rep): R0 (the fiber-core deck family (I-2vv^T)(I-2v0v0^T) in SO(4)) winds ZERO (stably trivial, measured); R2 (its exact spin lift Lambda(v) = v0 I + sum v^k G_b0 G_bk on the 128-dim module, which reproduces the machine-verified K_S co-flip as the deck monodromy Lambda(-v0) = -I) ALSO winds ZERO (both-chirality cancellation, measured, two base-leg choices); R1 (the commutant right-Sp(1) twist on the frozen quaternionic carrier H^64) winds EXACTLY 64 control blocks (Autonne-Takagi block reduction from the bit-exact antisymmetry of C = e1 e3 e5 e7 e10 e12, corroborated by direct 256-dim quadrature, ratio 64.000) giving the twist integer k = +-64 and the class J(k) in {8 nu, 16 nu}: NONZERO, PURELY 3-primary, ORDER EXACTLY 3. ROBUSTNESS: every native quaternionic carrier (H-multiplicity 64m, m in {1,7,13,14,91}) gives order exactly 3, because 8 | 64m kills the 2-primary part (the SAME quaternionic evenness that powers the Kramers no-go) and 3 never divides the native dimension ledger (C-04's '3 absent from the prime spectrum') so the 3-primary part cannot vanish -- the two established obstructions jointly BECOME the purity mechanism. THE MISSING DATUM (outcome shape ii): a native Sp(1)-torsor identification of the S^3 double cover of the fiber core with the unit quaternions of the commutant; its Z/2 restriction (deck = {+-1} in Sp(1) = the K_S co-flip) is already machine-verified, the pi_3-level lift is the demand. It MERGES with the existing B.5/Element-1 co-flip payload item (a strengthening, not a new N item) and sits on the already-flagged framed-reading fork (global-anomaly leg, 2026-07-20). Z/3 SUBTLETY SETTLED: 'the count = 3' cannot mean the element 3 of Z/24 (3 is not in the 3-Sylow {0,8,16}) and cannot mean the residue 3 mod 3 = 0 (trivial class); the only well-typed nontrivial reading is count = ORDER of the class, and the computed class has order exactly 3. The order-3-class -> integer-3 dictionary gate (H6 limit b) stays OPEN and is not claimed."
grade: "exploration / strong on the machine content, table-grade on the stable-homotopy inputs. COMPUTED (tests/channel-swings/torsion_arena_probe.py, exit 0, 8 [T] + 11 [E] + 5 [F], 201 s): rep anchors reproduced; J_quat facts bit-exact (C Cbar = -I, antisymmetry ||C + C^T|| = 0, per-generator commutation 0); WZW winding quadrature engine with the SU(2) identity-map control (+1.0029 at the working grid); the 1-quaternion control W_C = -2.006 => stable class |k| = 1 matching the Hopf-bundle table |p_1| = 2; the R1 commutant twist = 64 identical control blocks (structural, via Autonne-Takagi) AND direct 256-dim quadrature ratio 64.000; the R2 spin lift exact (Ad residual 3.3e-16, deck monodromy -I exact, winding 0.0000, two disjoint base-leg choices); the R0 metric-side winding 0.0000; the carrier-ledger sweep (all order 3); the Z/24 CRT / 3-Sylow / order arithmetic; falsifier controls showing 8|n and 3-not-|n are load-bearing (n=4 -> order 6; n=24 -> zero class). TABLE INPUTS (flagged, not re-derived): pi_3^s = Z/24 = im J with nu the generator and e_KO(nu) = 1/24 (Adams, J(X) IV; Toda), the complexification c : pi_3(O) -> pi_3(U) is multiplication by 2 (Bott), Pontryagin-Thom, Autonne-Takagi factorization of antisymmetric unitaries. No claim/canon/verdict/posture movement. Zero em dashes."
construction: "Fork-typed per GEOMETER-VS-PHYSICS-OBJECTS.md. Program-native objects: the verified Cl(9,5) = M(64,H) rep and its exact J_quat; the metric-fiber core RP^3 and its S^3 double cover (machine-verified retraction, sig_b5_habitat_probe.py); the K_S co-flip holonomy (machine-verified, re-derived here as spin-lift deck monodromy); the native carrier dimension ledger (C-04 prime spectrum {2,7,13}). Standard machinery binding the construction (every port named): Pontryagin-Thom, the J-homomorphism and Adams' im J = Z/24, Bott's c = x2, the WZW winding-number formula for pi_3 of unitary groups, Autonne-Takagi. This exploration lives on the FRAMED-READING side of the fork flagged in explorations/global-anomaly-leg-2026-07-20.md ('a string/framed reading would put the dim-3 Z/24 receptacle live'); everything below is R0_COND on that fork."
probe: tests/channel-swings/torsion_arena_probe.py
related:
  - GEOMETER-VS-PHYSICS-OBJECTS.md
  - explorations/wave33/H6-family-puzzle-theorem-2026-07-11.md
  - tests/wave33/H6_family_puzzle_theorem.py
  - docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md
  - canon/no-go-quaternionic-parity-generation-sector.md
  - canon/three-generations-locate-not-force-CRT-RESULTS.md
  - explorations/global-anomaly-leg-2026-07-20.md
  - explorations/dk-chirality-fork-2026-07-20.md
  - explorations/blockbuster-p4-generation-doors-2026-07-19.md
  - tests/channel-swings/sig_b5_habitat_probe.py
sources:
  - "Adams, J.F., On the groups J(X) IV, Topology 5 (1966) 21-71 -- im J in pi_3^s = Z/24; e_KO(nu) = 1/24."
  - "Toda, H., Composition Methods in Homotopy Groups of Spheres, Ann. Math. Studies 49 (1962) -- pi_3^s = Z/24."
  - "Bott, R., The stable homotopy of the classical groups, Ann. Math. 70 (1959) -- pi_3(O) = pi_3(U) = Z; c = x2 in this degree."
  - "Pontryagin-Thom: framed bordism of S^3 with a twist f : S^3 -> SO(N) shifts the framed class by J([f])."
  - "Autonne (1915) / Takagi (1925) factorization: an antisymmetric unitary C equals V eps V^T for the standard symplectic eps -- the exact block reduction used for R1."
---

# The torsion generation arena, pinned and computed

## 0. Question, discipline, pre-declared outcomes

The program's native location for the generation count is a torsion class in
`Z/3 subset pi_3^s = Z/24` (GEOMETER-VS-PHYSICS-OBJECTS, generation-count row). H6 proved
that no Z-valued index and no 2-primary selector can ever reach it (`Hom(Z/3, Z) = 0`;
receipt: `explorations/wave33/H6-family-puzzle-theorem-2026-07-11.md`, test exit 0), and the
2026-07-20 global-anomaly leg proved the whole spin Dai-Freed ledger is DISJOINT from this
arena (no odd torsion anywhere in it). The arena is therefore clean: nothing already-computed
contaminates it, and nothing already-killed can be smuggled back in.

Pre-declared outcome shapes: (i) MECHANISM -- a named GU-native framing/twist integer k with
k mod 24 computed; (ii) OBSTRUCTION SPEC -- the exact missing datum, with its N-accounting;
(iii) NO-GO -- native material provably cannot reach the arena. Overselling is the only loss.

**Outcome fired: (ii), with the conditional (i) payload fully computed.** The twist integer
exists and is computed (`k = +-64`, class of order exactly 3); exactly ONE named identification
separates it from an unconditional mechanism, and that identification's Z/2 shadow is already
machine-verified. Sections 2-4 make this precise.

## 1. The arena pinned (Stage 1)

`pi_3^s = Z/24`, generated by `nu`, the quaternionic Hopf class `S^7 -> S^4 = HP^1` (Toda).
The decisive structural fact for this exploration: **`Z/24` is exactly the image of the
J-homomorphism** `J : pi_3(SO) = Z -> pi_3^s` (Adams; `|im J| = den(B_2/4) = 24`). So the
arena is not an abstract receptacle: any construction that pins a framing-twist INTEGER k
lands at `k mod 24`, computably. H6's census already identified the J/e-invariant tool as the
canonical 3-primary-reaching selector; this exploration uses that allowed tool and no other.

The H6 receipt, read in full (not from the table one-liner): H6 is the selector-type theorem
(a selector forcing a 3-primary count must have nonzero 3-Sylow image), PROVEN and
GU-independent, with two honest limits that bind everything below: (a) "odd" is the wrong
prime -- the rigorous statement is mod-3, not parity; (b) **the theorem does not derive 3**:
the order-3-class -> integer-3 gate is OPEN. Nothing in this exploration closes gate (b), and
nothing here claims to.

### Route enumeration (typed)

A framed 3-manifold reaches `pi_3^s` by Pontryagin-Thom. The construction's candidate
3-manifold is RIGHT THERE: the metric-fiber core `RP^3` (the fiber `F = GL(4,R)/O(3,1)` of
`Met(X4)` deformation-retracts onto `{I - 2P : P rank-1}`, machine-verified in
`sig_b5_habitat_probe.py`), with `S^3` its double cover (unit directions v, Euclidean-normalized,
in the native 4-dim model space). Framing bookkeeping on `S^3`: framings form a torsor over
`[S^3, SO] = pi_3(SO) = Z` (simply connected, so only pi_3 contributes); the ambient-bounding
framing (from the machine-verified linear model embedding, where the unit ball is present) is
the natural origin; a twist by `f : S^3 -> SO(N)` shifts the framed class by `J([f])`.

| Route | What it is | Type | Computed result |
|---|---|---|---|
| R0 | the native deck family `F_v = (I-2vv^T)(I-2v0v0^T)` in SO(4) (fiber-core points ARE reflections; the composite with the basepoint reflection is a native closed S^3-family) | NATIVE-PROVABLE (it is the core model itself) | winding 0: **stably trivial** |
| R2 | the exact spin lift `Lambda(v) = v0 I + sum_k v^k G_b0 G_bk` of R0 on the frozen 128-dim module (Ad residual 3.3e-16; identity off the base legs; deck monodromy `Lambda(-v0) = -I` = the K_S co-flip's Z/2, re-derived) | NATIVE-PROVABLE as the lift of R0 (Euclideanized compact lift, the same move the habitat probe makes at frame level) | winding 0: **both-chirality cancellation** (two disjoint base-leg choices) |
| R1 | the commutant right-Sp(1) twist: unit quaternions of the verified H-structure (`i_R = i`, `j_R = C conj`) acting on the frozen carrier `H^64` | NATIVE-PROVABLE as a structure; its attachment to the S^3 cover is DEMANDED (Section 4) | `k = +-64`; `J(k) in {8 nu, 16 nu}`, **order exactly 3** |
| (b) | the Sp(1)/quaternionic bundle through the Hopf class over an S^4 | IMPORT (GU has no native S^4; the Hopf class enters only as the im-J generator, i.e. as R1's value, not as a native bundle) | not pursued |
| (c) | the record-law / co-flip structure as a stable map | NATIVE-PROVABLE as a Z/2 datum only | it is the pi_1-level shadow: on S^3 (simply connected) it contributes nothing to pi_3; on RP^3-descent it is the `H^1(RP^3; Z/2)` framing bit. It selects, it does not wind. |

The sharp negative content of R0 = R2 = 0 (both measured, not argued): **the fiber-core
geometry alone, even spin-lifted through the full frozen module, does not reach the arena.**
The only nonzero native twist source is the quaternionic commutant. Everything therefore
hinges on whether the commutant's Sp(1) is natively attached to the S^3 cover -- Section 4.

## 2. The computation (Stage 2)

Machinery: the winding number of `g : S^3 -> U(n)` (the WZW integral
`1/(24 pi^2) int tr (g^{-1} dg)^3`), midpoint quadrature, calibrated on the SU(2) identity map
(+1.0029 at the working grid). Complexification bookkeeping `c : pi_3(O) -> pi_3(U) = x2`
(Bott) converts real-linear windings to stable classes.

- **Control (one quaternion).** Right multiplication by unit `v` on `H = C^2` (with
  `j = eps conj`), complexified: `W_C = -2.006 => |k| = 1`. Matches the table: the stable class
  of the H-line clutching has `|p_1| = 2|k| = 2` (quaternionic Hopf bundle). The sign is the
  orientation convention of the S^3 parametrization; it is carried, not chosen.
- **R1 structurally.** `C = e1 e3 e5 e7 e10 e12` is antisymmetric unitary to the bit
  (`||C + C^T|| = 0.0`). Autonne-Takagi then gives a constant unitary V with `C = V eps V^T`,
  and in those coordinates the commutant right-Sp(1) action on the frozen module is EXACTLY 64
  identical copies of the control block. Hence `W_C(R1) = 64 x W_C(control)` and
  **`k = +-64`**. Direct quadrature on the actual 256-dim complexification corroborates:
  ratio to control = **64.000**.
- **R1's class.** `J(k) = k nu mod 24`. `64 mod 24 = 16` (or 8 for the opposite orientation):
  in CRT coordinates `Z/24 = Z/8 (+) Z/3` this is `(0 mod 8, nonzero mod 3)` -- **purely
  3-primary, order exactly 3** (`order = 24/gcd(64,24) = 3`). Its e-invariant is `1/3` or
  `2/3` in `(Q/Z)_(3)`: precisely H6's positive-control locator values.
- **R2 exactly.** `Lambda(v)` is unitary, spin-lifts the native deck family exactly
  (`Ad_Lambda = F_v^{-1}`, i.e. transport = `Lambda^dag`; residual 3.3e-16; identity off the
  base legs, 0.0), carries the deck monodromy `Lambda(-v0) = -I` (the co-flip's Z/2, now
  re-derived at the spin level), and winds **0.0000**. Verified for base legs (9,0,1,2) and
  (13,3,7,8): leg-choice independent.
- **R0 exactly.** The SO(4) family itself winds 0.0000 (it is `x -> vxv`, the `f_{1,1}`
  clutching, stably trivial -- the TS^4 pattern).

### Why the class has order exactly 3: the two walls become the mechanism

`order(J(64m)) = 3` decomposes into two facts, each an ESTABLISHED obstruction of this
program now working in reverse:

1. **The 2-primary part dies because `8 | 64m`.** The quaternionic evenness of every native
   carrier (the same `Cl(9,5) = M(64,H)` structure that powers the Kramers/quaternionic-parity
   no-go against odd indices) forces `v_2(multiplicity) >= 3`, annihilating the `Z/8` component.
2. **The 3-primary part cannot die because `3` never divides the native dimension ledger**
   (C-04: prime spectrum `{2, 7, 13}`, "the prime 3 is ABSENT from the rep"). What was recorded
   as evidence against a native 3 is exactly what makes the class nonzero mod 3.

Falsifier controls confirm both facts are load-bearing, not free: multiplicity 4 would give
order 6 (2-primary contamination); multiplicity 24 would give the zero class. And the sweep
over the ENTIRE native carrier ledger (`H^64, H^448, H^832, H^896, H^5824`; m = 1, 7, 13, 14,
91) gives order exactly 3 in every case. The rank-dependence that killed the index route
("index r, choosing r = the forbidden import") inverts here: the ORDER of the class is
**carrier-independent**. Only the mod-3 residue (8 vs 16) varies with carrier and orientation,
and it is honestly reported as NOT pinned.

## 3. The Z/3 subtlety (Stage 3)

What could "the count = 3" mean as a statement about `Z/24`? Three candidate readings, two
rejected by arithmetic, machine-checked:

1. **The element `3 in Z/24`?** No: `3 mod 8 = 3 != 0`, so 3 is NOT in the 3-Sylow
   `{0, 8, 16}`; it has order 8, a 2-primary-contaminated element. The native claim
   "the count is a Z/3-torsion class" cannot mean the element 3. Any argument producing the
   element 3 would in fact CONTRADICT the torsion-arena location.
2. **The residue `3 mod 3`?** Self-defeating: `3 = 0 mod 3` is the TRIVIAL class. A theory
   whose count-class is the mod-3 residue of 3 would put three generations at zero -- the
   reading kills itself.
3. **The ORDER of the class.** The only well-typed nontrivial reading: a nonzero class in the
   3-Sylow has order exactly 3, and `{0, 8, 16} = Z/3` is canonically "the arena whose
   nontrivial classes have order 3". The computed R1 class has order exactly 3, robustly.

The adversarial residue: even under reading 3, "order-3 class" -> "three generations" is a
dictionary entry, not a derivation -- this is exactly H6's limit (b), untouched. What the
mechanism would deliver, if its one missing datum lands, is: **a canonical GU-native framed
invariant whose order is exactly 3, living in precisely the arena the program located the
count in, with the 3 forced by von Staudt-Clausen arithmetic (the 24) plus the frozen
dimension ledger (8 | multiplicity, 3 never divides it) -- and with every killed route
(Z-index, eta, 2-primary) provably unable to see it.** What it would NOT deliver is the
integer 3 as a cardinality. Located, not forced -- now with a candidate canonical occupant
of the location.

## 4. The missing datum and its N-accounting (outcome shape ii)

**The demand, stated exactly.** A native Sp(1)-torsor identification of the S^3 double cover
of the fiber core with the unit quaternions of the commutant: equivalently, a native rule
transporting the frozen carrier around the fiber core whose induced twist is the commutant
action (R1) rather than only the spin lift (R2). What is already supplied by frozen material:

- the 3-manifold and its cover (machine-verified retraction; covering step typed
  IMPORTED-standard in the habitat probe);
- the base framing origin (ambient-bounding, from the machine-verified model embedding --
  NATIVE-PLAUSIBLE; a referee may demand the embedding's canonicity be argued once more, but
  the retraction is spectral, hence canonical);
- **the Z/2 restriction of the demanded identification**: deck = `{+-1} subset Sp(1)` IS the
  K_S co-flip holonomy, machine-verified in the habitat probe (Part C) and re-derived here as
  `Lambda(-v0) = -I`. The pi_1 shadow of the demand is already a theorem of the fixtures.

What is missing is exactly the pi_3-level lift of that verified pi_1 shadow. **N-accounting:
this MERGES with the existing B.5/Element-1 payload item** (the co-flip/anchor-exchange
identification staked in the P5 instance dossier and verified at the Z/2 level by the habitat
probe): it is the same identification asked one homotopy level higher, a strengthening of an
existing item, NOT a new item. Separately, the whole route sits on the framed-reading fork
already flagged by the global-anomaly leg (2026-07-20): also existing, not new. Net new
demand items: **zero**. Net strengthened: one (Element-1, pi_1 -> pi_3).

## 5. Killed items: an explicit non-resurrection audit

- **No Z-index for the count** (H6): untouched. The value group here is `Z/24`, reached
  through im J -- the exact tool H6's census marks as the allowed 3-primary-reaching selector.
  Nothing Z-valued is read as the count; the integer k enters only through its mod-24 class.
- **No eta-count from GU-native operators** (C-01/C2-is-not-an-index; rs-boundary-eta):
  untouched. No spectral eta appears anywhere in this route.
- **The Kramers/quaternionic-parity wall** (C-07): untouched and USED. R1 is a unitary family
  inside the commutant, not a Hermitian carrier index ([F] checks: the R1 family commutes with
  the algebra; the R2 twist generators are honestly flagged NOT H-linear, J-residual 2.0 --
  they implement the non-isometry congruence move, as in the habitat probe). The wall's
  evenness is one of the two legs of the order-3 purity (Section 2).
- **Quaternionic-parity contingency**: the (9,5)-vs-(7,7) signature caveat of the no-go canon
  carries over verbatim: everything here is H-class ((9,5)) conditional, like the rest of the
  frozen spine.

Forks crossed (named): the geometer-side arena row (settled, this is its native pursuit); the
framed-reading fork (EXISTING, flagged 2026-07-20; this doc is R0_COND on it); the
standard-machinery ports (Adams im J, Bott c = x2, Pontryagin-Thom, Autonne-Takagi -- all
IMPORTED-standard, binding native objects, per the construction-fork discipline); the
Euclideanized compact lift (the habitat probe's own move, reused at spin level).

## 6. Five-lens council (inline, answered in writing)

**Stable-homotopy theorist.** Identifications audited: `[S^3, SO] = pi_3(SO)` needs simple
connectivity (holds); the twist class `J([f])` is base-framing-independent as a DIFFERENCE
class (the torsor difference is canonical; only the absolute Pontryagin-Thom class needs the
bounding origin, and that dependence is stated, not hidden); im J is ALL of pi_3^s in this
dimension, so no cokernel is being ignored; the nu-vs-(-nu) generator convention is absorbed
into the honest +-. The winding was computed twice (structural block count and direct
quadrature) and calibrated against a known class (the H-line, |p_1| = 2). No sloppy
identification found standing.

**Quaternionic geometer.** The Sp(1) is the right-multiplication commutant of the verified
`M(64,H)` -- basis-free, and its H-multiplicity on any native carrier is forced by the frozen
algebra, not chosen. Left-vs-right and conjugation conventions flip only the sign of k. The
deck family R0 and its lift R2 are exactly the reflection-composite structure of the core
model (`I - 2vv^T` IS a reflection; the composite is `x -> vxv`), and the fixtures confirm the
lift to machine precision. The quaternionic content of the arena (nu = the quaternionic Hopf
class) and of the mechanism (the commutant) match; that match is the route's motivation, not
its proof -- the proof obligation is the torsor demand of Section 4.

**Index theorist.** The boundary is clean: index-type invariants (Z-valued, eta, mod-2) are
all H6-census BLIND to this arena, and none is used. The invariant here is a framing holonomy
class in im J, detected by e_KO in `(Q/Z)_(3)` -- the census's allowed locator. The killed
routes stay killed; the audit of Section 5 is explicit. One warning issued and recorded: do
not let the integer k = 64 be quoted as "an index 64" -- it is a clutching degree, and only
its mod-24 class is invariant content.

**GU-native geometer.** Supplied by frozen material: the core RP^3 + cover, the co-flip Z/2
(twice-verified), the H-structure and carrier ledger, the ambient-bounding origin. Demanded:
the pi_3 torsor lift (merged, Section 4) and the framed-reading fork commitment. R0 = R2 = 0
is the honest native-side news: the metric sector alone is stably trivial; whatever winds must
come through the commutant, i.e. through exactly the structure the source-action/B.5 program
is already obligated to build transport for. The demand lands where the program already dug.

**Adversarial referee (every 3 and every 24 attacked).** The 24: `|im J| = den(B_2/4)`,
von Staudt-Clausen -- imported, cited, and not adjustable; no GU input touches it. The 3s, one
by one: the 3-Sylow arithmetic (CRT, computed); `3 - not - | 64m` (C-04, established 2026-06,
predates this question's present form; not fitted); the ORDER 3 (= 24/gcd, computed); the
element-3 and residue-3 readings (REJECTED in writing, Section 3). The suspicious coincidence
"the count the world shows is 3 and the order comes out 3" is answered: the order-3 output is
forced by two frozen facts (8 | multiplicity, 3 absent from the ledger) that were both
recorded as OBSTRUCTIONS long before this route existed; the route was not tuned to produce
3 -- it could have produced order 1, 2, 4, 6, 8, 12 or 24 (falsifier controls show nearby
multiplicities do), and landing at 3 was the pre-declared extraordinary branch of outcome (i).
The residue (8 vs 16) is NOT pinned and is said so. The dictionary gate (order-3 -> the
integer 3) is OPEN and is said so. Verdict: no numerology import found; the conditional
statement as written survives.

## 7. Bottom line

The arena is pinned: `Z/24 = im J`, concretely computable from a framing-twist integer. The
native computation is done: the metric-fiber core supplies the 3-manifold and (twice-verified)
the Z/2 co-flip datum, but winds nothing itself (R0 = R2 = 0, measured); the quaternionic
commutant supplies the one nonzero native twist, `k = +-64` on the irreducible carrier, and
`J(k)` is a purely 3-primary class of ORDER EXACTLY 3 -- for every carrier in the frozen
ledger, with the order forced jointly by the Kramers evenness (kills the Z/8 part) and C-04's
missing-3 (protects the Z/3 part). One named datum separates this from an unconditional
mechanism: the Sp(1)-torsor identification whose pi_1 shadow is already machine-verified; it
merges with the existing B.5/Element-1 payload item (no new N). The Z/3 subtlety is settled:
count-as-ORDER is the only well-typed nontrivial reading; the order-3-class -> integer-3 gate
(H6 limit b) remains open and unclaimed. No claim, canon, or posture movement.
