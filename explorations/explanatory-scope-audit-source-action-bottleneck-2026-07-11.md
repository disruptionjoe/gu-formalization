# GU explanatory-scope audit: the coherent-whole story bottlenecks on ONE object (the source action), geometry-agnostically

2026-07-11. Adversarial audit (5 claim-auditors reading the actual canon + 1 adjudicator; grading
discipline = no compatibility-as-derivation, no vindication). Question: does GU coherently HOUSE the
physics (SM, QM, forces, gravity, dark energy) so that "GU houses everything + located-not-forced" is an
honest, profound, publishable story? **Answer: the naive story is NOT tellable -- but the audit found a
stronger, geometry-agnostic result.**

## The coherence map (grades from the repo's own canon)

| Claim | Grade | Why |
|---|---|---|
| Standard Model (group + content) | **IMPORTED** | textbook SO(10) > PS > SM rep theory verified for internal consistency, not derived; the SM-selecting vacuum `v_PSB` is open; the Connes derivation belongs to CC and GU's bridge is OBSTRUCTED (J^2 = -1 vs +1 mismatch, inner-fluctuation failure) |
| Quantum mechanics (Krein) | **IMPORTED** | the Krein signature `(+96,-96,0)` is an exact machine-checked KINEMATIC feature (genuine), but the QM content -- Born rule, positivity, unitarity -- is borrowed from Bateman-Turok; the chirality bridge is refuted; the "inside-class enrichment" framing was retracted (A0 audit) |
| Forces (gauge-from-connection / IG) | **PARTIAL** | one genuine GU-native derivation survives: the ambient group `Sp(64)` is forced by `Cl(9,5)=M(64,H)` (defuses Nguyen's `U(128)` critique); but the SM force content is imported and the selection is open |
| Gravity (Einstein eqs / exact solutions) | **REFUTED / OBSTRUCTED** | the weakest link -- Einstein equations are never derived from a GU action; the only concrete dynamical branch (Willmore) FAILS exact Schwarzschild; exact Schwarzschild/Kerr is BLOCKED |
| Dark energy (equivariance -> divergence-free) | **PARTIAL** | dynamical equivariant `theta` replacing static `Lambda` is genuine reconstruction-grade; but "IG-equivariance -> divergence-free" is Noether's second theorem dressed up; the specific prediction is DESI-open |

**Housing verdict:** 3 of 5 (SM, QM, gravity) do NOT clear the bar; 2 (forces, dark energy) are PARTIAL
and house structural SCAFFOLDING, not the physics content. And located-not-forced itself overstates: only
ONE generation is verified; three is a reconstruction-grade conjecture GU's own "2+1 effective" argues
against; the selection bridge is refuted. Honest phrasing is "one generation housed, count OPEN."

## The finding: all five gaps are the SAME gap (the missing source action)

The adjudicator's load-bearing result: **every "imported" or "open" or "refuted" verdict roots to a
single shared cause -- GU has no built SOURCE ACTION (variational dynamics / S-matrix).** Without it:
- no dynamics to SELECT the SM subgroup -> `v_PSB` open (caps SM + forces);
- nothing to connect the Krein kinematics to Bateman-Turok QM or to test `[P_ghost, S]=0` (caps QM);
- no way to prove the dark-energy assumption `theta ~ D_A* F_A` that would turn the DESI fit into a
  prediction (caps dark energy);
- no derivation of the Einstein equations, no pinned shiab, no exact Schwarzschild/Kerr (caps gravity).

**The entire geometry->physics program bottlenecks on ONE unbuilt object.** And that object is exactly the
one located-not-forced already precisely characterized: SG4, the source action, whose field-space
declaration is a rigid, finite, 2-bit residual (see `sg4-forcing-rubric-complete-2026-07-11.md`).

## The geometry-agnostic reframe (Joe, 2026-07-11): it does not have to be GU

The audit's failures are NOT "GU's geometry is wrong" -- the geometric SCAFFOLD (the `(9,5)` Clifford /
rep-theory spine, `Sp(64)`, the equivariant structures) is genuine and largely survives. The failures are
"GU has no DYNAMICS." So a *different* geometry that "fits everything" would face the SAME requirement: it
must supply the source action GU lacks. The bottleneck is geometry-AGNOSTIC. And the located-not-forced
characterization of the missing object (the generation count is a boundary quantity in the odd-primary
summand, rigidly located to a 2-bit source-action declaration) holds for ANY geometry that fits -- it is a
structural constraint on the missing dynamics, not a GU claim.

## The honest, profound, tellable story (what to publish instead of the overclaim)

NOT "GU houses everything" (false; 3/5 fail). Instead: **"The program of deriving all of physics from a
geometric structure bottlenecks on a single, precisely-located missing object -- the source action -- which
is the shared root of the Standard-Model-selection, quantum-dynamics, dark-energy, and gravity gaps; and
that object is tightly constrained (its generation-sector declaration is a rigid finite 2-bit residual, and
the count it would fix is provably a boundary quantity in the odd-primary summand)."** This is
truth-seeking, geometry-agnostic, credible (no GU vindication), and it is a genuine result about the
STRUCTURE of the geometry->physics problem, not a claim that any particular theory succeeds.

## Highest-leverage next step (from the audit)

**Build the source action** (the geometry's variational dynamics). It converts the most legs from
hosted/open to derived at once. But: a FREE build p-hacks (located-not-forced established this), so the
honest form is a FORCED construction -- which the SG4 forcing rubric + out-of-rubric hunt found no internal
mechanism for. So the honest status is: the whole program's bottleneck is a single object that is (a)
shared across all five domains, (b) precisely characterized, and (c) not forceable from the armchair -- a
construction problem, sharply posed.

## Grade / boundary

The per-claim grades are read from the repo's own canon (with its corrections/obstructions); the
source-action bottleneck is the adjudicator's synthesis. No claim/canon-ledger movement. The durable
output is the coherence map + the single-bottleneck finding + the geometry-agnostic reframe. Feeds
JoeOps WI-068 (#6).
