---
artifact_type: exploration
label: W231
status: "exploration (W231 / lane A3 of the GAP-CLOSURE wave; CLOSE the dynamical SMG realization for GU's SPECIFIC SO(10)-16 mirror by EXTENDING W225; four personas inline in one worker, no sub-agents; one deterministic test 26/26 exit 0, positive controls first)"
created: 2026-07-14
wave: W231
posture: "coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (a genuine STILL-OPEN with a sharply-named X is a real result, a fake COMPLETED is not); NON-NAIVE (grant the good Krein branch / W216 condensate, grant anomaly-freedom / W224); RUTHLESS skeptic; do NOT re-run W225's literature survey (EXTEND it); do NOT re-litigate the reservoir Krein sign (Godel-independent, W211) nor the W216 branch selector; tri-repo gating STRICT; bar(b)/H59 stay OPEN"
route: "SETTLE GU's SPECIFIC case that W225 left OPEN-IN-THE-FIELD. Grant existence (Wang-Wen cobordism, W225), grant no-beyond-anomaly-obstruction (W225), grant the good Krein branch condensate (W216). Then use the REPRESENTATION THEORY of the exact SO(10) 16 (a SYMMETRY argument) plus a strong-coupling / mean-field TOY of GU's actual mirror content to decide whether the symmetric gapped phase is DYNAMICALLY realized. Key move W225 did not make: read GU's OWN built condensate (W216: BCS pairing of the (generation, mirror) null pairs) as a channel in the SO(10) rep decomposition, exposing a THIRD basin (the trivial vectorlike Dirac gap) that W225's two-basin S-vs-B toy missed."
n_number: "W231 chosen. On origin/main at filing the max exploration is W230 (W229, W231+ free); W231 is free and this note takes it. If a staging race collides, rebase absorbs and the next free number above W230 is used, and this line says so."
title: "W231 (lane A3) -- CLOSE the dynamical SMG realization for GU's SPECIFIC SO(10)-16 mirror. VERDICT: STILL-OPEN-NEEDS-X, where X is NAMED EXACTLY and is NOT a new object: X = the mirror being a CONSERVED RECORD (the Cartan-involution / ghost-parity Z2 of W173 operative as a symmetry), which is the SAME unbuilt Y14 connection-curvature / C2 spectral-section datum that governs bar(b) (W173) and W216's good branch. LEANING REALIZES on GU's determined free-BV content. The extension past W225: W225 left the realization coupling-ratio-dependent between two basins, S (symmetric multi-fermion SMG) and B (Eichten-Preskill symmetry breaking), and could not decide. W231 settles the STRUCTURE exactly by the rep theory of the SO(10) 16 and finds THREE channels, not two: (B) MIRROR-only bilinear 16bar.16bar in {10,120,126} has NO SO(10) singlet (the 16 is COMPLEX) so channel B can only BREAK SO(10) spontaneously (disfavored, no symmetric order parameter) -- this PLACES GU in the FAVORABLE Butt-Catterall class where the generic E-P failure has no explicit driver; (D) GEN-MIRROR Dirac 16.16bar DOES contain the SO(10) singlet, so a symmetric vectorlike Dirac mass EXISTS -- but it gaps the GENERATION together with the mirror, so it is CHIRALITY-KILLING (the trivial vectorlike trap), and it is EXACTLY the channel GU's OWN W216 condensate (the BCS pairing of the null partners generation-xi <-> mirror--xi) STRUCTURALLY sits in -- a real, previously-unflagged danger; (S) MIRROR-only four-fermion (16bar)^4 has TWO SO(10) singlets (from the REAL subreps 10 and 120; the complex 126 contributes none) so the SMG operator EXISTS and gaps ONLY the mirror, keeping the generation chiral. The DISCRIMINANT between the good outcome S and the trap D is ONE bit: whether the Z2 grading (generation +, mirror -; W173: K = Cartan involution of so(9,5)) is a CONSERVED symmetry, which FORBIDS the Z2-odd Dirac mass D and protects the massless chiral generation, leaving S as the only SYMMETRIC gap (B being disfavored). That bit is precisely W173's operative-C / spectral-section datum: GU's DETERMINED content (the built, nilpotent free BV bicomplex) makes the mirror BRST-cohomology-NONTRIVIAL = a RECORD = Z2 operative = D forbidden = LEANS REALIZES; the demotion to redundancy (Z2 broken, D allowed, chirality lost) requires the UNBUILT Y14 curvature and is NOT GU-forced. So A3 does NOT add an open object: it UNIFIES the SMG-realization question with the pre-existing central one (bar(b) / records-vs-redundancy / the W216 branch) and gives it sharp SO(10)-representation content. Not COMPLETED-REALIZES (the record reading is unforced, W173), not COMPLETED-BROKEN (channel D is not forced either). Test tests/W231_close_a3_smg_realization_gu_mirror.py 26/26 exit 0, positive controls first."
grade: "exploration / STILL-OPEN-NEEDS-X (honest; the field question W225 posed is settled for GU's SPECIFIC case up to ONE named, pre-existing bit). EXACT / machine-verified (tests/W231_close_a3_smg_realization_gu_mirror.py, 26/26, exit 0, positive controls first; pure-integer weight arithmetic on the 5 SO(10) Cartan charges, no floats in the rep theory, no hardcoded rep answers): the 16 is COMPLEX (even/odd minus-sign halves of (+-1/2)^5, disjoint, self-duality False); channel B has ZERO SO(10) singlets in 16(x)16 and 16bar(x)16bar (no symmetric bilinear); channel D has EXACTLY ONE singlet in 16(x)16bar (zero-weight support 16, Schur mult 1, cross-checked by 1+45+210 zero-weight bookkeeping 1+5+10=16); channel S has EXACTLY TWO singlets in 16^(x)4 (from the real 10 and 120; the complex 126 contributes none, witnessed by 16(x)16 not being self-dual); the Z2 discriminant table (D Z2-odd -> forbidden when Z2 conserved; S, B Z2-even); the mean-field trichotomy whose verdict FLIPS on the single Z2-conservation bit (S wins when conserved, D wins when broken); the teeth (a counterfactual SYMMETRIC bilinear would let B break SO(10), and the guard that the real 16 forbids it). STANDARD-FIELD / reconstruction grade (flagged, NOT re-derived; same honesty boundary as W224/W225): the Wang-Wen cobordism existence, the Eichten-Preskill failure mode, the Razamat-Tong s-confinement construction, the Butt-Catterall lattice class where an absent symmetric bilinear yields a direct SMG transition. STRUCTURAL / GRANTED per the non-naive method: the W216 good-branch condensate (its EXISTENCE and real spectrum), which A3 re-reads as channel D; W173's free-BV record reading (the Z2-operative lean); the Krein sign / branch selector (Godel-independent, W211), NOT re-litigated. No canon / RESEARCH-STATUS / claim-status / verdict / posture change; bar(b) and H59 stay OPEN; the count stays {1,3}; no forbidden target {3,8,24,chi(K3),Ahat} assumed or inserted. Zero em dashes."
construction: "program-native where the objects are GU's (the mirror 16bar as CP conjugate of the physical SO(10) 16, W222/W224; the (generation, mirror) hyperbolic null pairs in ker(Gamma) of Cl(9,5), signature (+96,-96,0), K = Cartan involution, W173; the good-branch BCS record condensate, W216; the good-vs-pathological branch selector, W186/W211). Standard-field where the machinery binds any construction and is PORTED and CITED (SO(10) spinor rep theory; the SMG-realization literature: Eichten-Preskill, Wang-Wen, Razamat-Tong, Butt-Catterall-Toga, the Wang-You DQC-SMG picture). Every analogy labelled; none asserted of GU beyond its cited source. The reservoir Krein sign / branch selector and the Z2-operative datum stay GATED temporal-issuance / time-as-finality objects (one-way rule respected; no cross-repo identity asserted; the sign gate is NOT re-litigated)."
depends_on:
  - explorations/W225-falsify-smg-dynamical-realization-2026-07-14.md
  - explorations/W224-falsify-nielsen-ninomiya-chirality-2026-07-14.md
  - explorations/W222-falsify-sm-emergence-anomaly-hypercharge-2026-07-14.md
  - explorations/W216-true-vacuum-spectral-condensate-2026-07-14.md
  - explorations/W173-brst-cohomology-mirror-sector-2026-07-14.md
  - explorations/W211-krein-sign-godel-independent-five-method-synthesis-2026-07-14.md
scripts:
  - tests/W231_close_a3_smg_realization_gu_mirror.py
external_refs:
  - "Eichten & Preskill, Nucl. Phys. B268 (1986) 179 -- the mirror-fermion strong-coupling program; the original attempt FAILED into a symmetry-breaking / gapless phase (channel B here, the failure mode this leg tests for)"
  - "Wang & Wen, Phys. Rev. Research 2 (2020) 023356, arXiv:1809.11171 -- the anomaly-free SO(10) 16n admits a symmetric gapped mirror boundary (EXISTENCE via cobordism); granted from W225, not re-derived"
  - "Razamat & Tong, Phys. Rev. X 11 (2021) 011063, arXiv:2009.05037 -- Gapped Chiral Fermions: one SM generation / the SO(10) 16 CAN be gapped preserving the chiral symmetry, but via ADDED auxiliary SU(2) gauge dynamics + vectorlike partners and SUSY s-confinement (confinement without chiral symmetry breaking); a CONSTRUCTED existence, phase stability / competing phases NOT analysed"
  - "Butt, Catterall & Toga; Catterall et al, arXiv:2111.01001 (Phys. Rev. D) -- 3+1D SMG in a SU(2)xSU(2) bifundamental reduced-staggered lattice gauge theory: single-site bilinear masses VANISH IDENTICALLY by the rep, and the fermions gap symmetrically by a four-fermion condensate through a DIRECT transition with NO intervening broken phase (the favorable class GU's rep theory lands in)"
  - "You, BenTov & Xu; Wang & You, Phys. Rev. X 8 (2018) 011026, arXiv:1705.09313 -- SMG as deconfined quantum criticality; the single continuous transition"
  - "Wang, You, Zheng et al, 'Symmetric Mass Generation', Symmetry 14 (2022) 1475 -- review; the open status of the exact 16 in 3+1D and the intervening-symmetry-breaking caveat (the S-vs-B fork W225/W231 sharpen)"
cross_repo:
  - "temporal-issuance + time-as-finality: the bit that decides SMG-realizes (S) vs chirality-killing trivial-vectorlike (D) is whether the Cartan-involution / ghost-parity Z2 is a CONSERVED symmetry -- identically W173's operative-C / Y14-spectral-section datum, which W216's good branch also rides and which the reservoir Krein sign (Godel-independent, W211) gates. W231 UNIFIES the SMG question with that pre-existing object and does NOT fix or re-litigate it. One-way rule respected; no GU claim moves, no TI/TaF claim moves, no identity asserted."
---

# W231 (lane A3) -- Close the dynamical SMG realization for GU's specific SO(10)-16 mirror

Test / closure certificate: `tests/W231_close_a3_smg_realization_gu_mirror.py` (26/26, exit 0,
positive controls first). This is lane A3 of the GAP-CLOSURE wave. Four personas ran INLINE in one
worker (SMG / lattice-chiral-fermion specialist; deconfined-criticality / competing-phases specialist;
GU-mirror-content specialist; ruthless skeptic); no sub-agents. Zero em dashes. It EXTENDS W225 (it does
NOT re-run W225's literature survey) to settle GU's SPECIFIC case.

## 0. What W225 left, and the one move that settles GU's case

W225 established, for the exact 3+1D SO(10) `16`: existence of the symmetric gapped phase is GUARANTEED
(Wang-Wen cobordism), NO beyond-anomaly obstruction fires, but the DYNAMICAL flow is unsettled IN THE
FIELD, and W225's strong-coupling toy left it coupling-ratio-dependent between two basins, `S` (symmetric
multi-fermion SMG gap) and `B` (Eichten-Preskill symmetry breaking). W225's own honest close: GU's
mirror couplings are the unbuilt condensate, so the toy cannot decide GU.

The move W225 did not make, and the one that settles GU's SPECIFIC case, is to stop treating GU's mirror
as a generic anomaly-free `16` and instead read GU's OWN built condensate in the SO(10) representation
decomposition. GU's mirror is not a bare `16bar` sitting in a vacuum. It is the Krein-negative partner of
a hyperbolic NULL PAIR `(generation, mirror)`, signature `(+96, -96, 0)`, inside `ker(Gamma)` of
`Cl(9,5)`, with the Krein form `K` the Cartan involution (W173); and the condensate GU actually built
(W216, granting the good branch) is the BCS pairing of those null partners, `Delta` coupling the
generation mode `xi` to its mirror partner `-xi`. Put in SO(10) language, that pairing is a
`<16 . 16bar>` bilinear. Once you ask which SO(10) representation each candidate gap operator lives in,
the two-basin picture becomes a THREE-basin picture, and the third basin is exactly where GU's own
condensate sits.

## 1. Persona 1 -- SMG / lattice-chiral-fermion specialist: three channels, by the rep theory of the 16

Everything below is machine-checked by pure-integer weight arithmetic on the five SO(10) Cartan charges
(the `16` weights are `(+-1/2)^5` with an even number of minus signs; `16bar` the odd half; scaled by 2
in the test). No rep answer is hardcoded; the singlet counts are computed from weights.

- **The `16` is COMPLEX.** `16` and `16bar` are disjoint weight sets (negating a weight flips all five
  signs, flipping the minus-count parity), so `16 !~ 16bar`; self-duality test returns False (contrast
  the vector `10`, real, True). Frobenius-Schur `= 0`. This one fact drives the whole trichotomy.

- **Channel B (mirror-only bilinear `<16bar . 16bar>`):** `16 (x) 16 = 10 + 120 + 126`, and it contains
  **NO SO(10) singlet** (machine: zero-weight multiplicity of `16(x)16` and of `16bar(x)16bar` is `0`,
  because no weight `w` of `16` has `-w` in `16`). A gauge-invariant symmetric mirror MASS therefore does
  NOT exist. Channel B can gap the mirror only by a NON-singlet condensate in `{10, 120, 126}`, i.e. by
  SPONTANEOUSLY BREAKING SO(10). This is the Eichten-Preskill failure mode, and the point is that here it
  has **no explicit driver** and needs a symmetry to break itself.

- **Channel D (gen-mirror Dirac `<16 . 16bar>`):** `16 (x) 16bar = 1 + 45 + 210` contains **EXACTLY ONE
  SO(10) singlet** (machine: zero-weight support `16`, Schur multiplicity `1`, cross-checked by the
  `1 + 45 + 210` zero-weight bookkeeping `1 + 5 + 10 = 16`). So a SO(10)-SYMMETRIC vectorlike Dirac mass
  DOES exist. But it pairs the generation with the mirror, gapping BOTH: it is the TRIVIAL VECTORLIKE
  gap, and it destroys the chiral generation. This is the channel GU's W216 condensate structurally sits
  in.

- **Channel S (mirror-only four-fermion `<(16bar)^4>`):** `16^(x)4` contains **EXACTLY TWO SO(10)
  singlets** (machine: from the REAL subreps `10` and `120` of `16(x)16`, each pairing with itself; the
  COMPLEX `126` contributes NONE because it needs `126bar`, absent in `16(x)16` -- witnessed by
  `16(x)16` not being self-dual). So a SO(10)-SYMMETRIC four-fermion operator on the mirror EXISTS. It
  gaps ONLY the mirror and leaves the generation massless and chiral. This is the SMG SUCCESS channel,
  and by the complexity of the `16` it is necessarily NON-PERTURBATIVE (no bilinear route), exactly as
  Wang-Wen / Razamat-Tong require.

So the realization question is not "S versus B" (W225) but "which of B, D, S does GU's dynamics choose,"
and the decisive new fact is that GU's own built condensate is in the chirality-killing D, not in S.

## 2. Persona 2 -- competing-phases specialist: which basin, and the one discriminant

Rank the three by the standard mean-field / Landau reasoning, all granted the good Krein branch (real
gap, W216):

- **B is disfavored.** It is a non-singlet condensate: turning it on costs SO(10)-breaking free energy
  and there is NO symmetric bilinear order parameter to seed it (Persona 1). This is precisely the
  Butt-Catterall lattice situation (`arXiv:2111.01001`): when single-site bilinears vanish identically by
  the representation, the fermions gap through a DIRECT symmetric transition with NO intervening broken
  phase. GU's `16` is in that favorable class. So the generic Eichten-Preskill breaking is NOT the
  default here. This is a genuine positive result and it is where the symmetry argument bites.

- **D is the deepest well if it is available.** A bilinear (Dirac) condensate generically beats a
  four-fermion (quartic) condensate in mean field: fewer fermions to correlate, lower condensation energy
  per unit coupling. So IF the generation-mirror Dirac mass is allowed, mean-field dynamics falls into D,
  the mirror and the generation gap TOGETHER, and the chiral generation is LOST. This is a NEW failure
  mode, distinct from Eichten-Preskill: it does NOT break SO(10) (D is a singlet), it silently removes
  the chiral fermion by pairing it to its mirror. W225's two-basin toy could not see it because it did
  not resolve the SO(10) content of GU's own condensate.

- **S wins only if D is forbidden.** With D projected out and B disfavored, the only remaining SYMMETRIC
  gap is the mirror-only SMG channel S, and the mirror gaps while the generation stays chiral.

The discriminant between the trap D and the success S is ONE bit: **is the `generation(+) / mirror(-)`
grading a CONSERVED symmetry?** If yes, the Dirac mass D (which is Z2-ODD: it carries one generation and
one mirror) is FORBIDDEN, the generation is protected massless, and S is forced among symmetric options.
If no, D is available and mean-field takes it. The mean-field trichotomy in the test (Block E) flips its
winner on exactly this bit: S when the Z2 is conserved, D when it is broken, with the teeth that a
COUNTERFACTUAL symmetric bilinear (a real rep, not the actual `16`) would additionally open B and break
SO(10) -- and the guard that the real `16` forbids that branch.

## 3. Persona 3 -- GU-mirror-content specialist: the discriminant IS W173's datum

The grading in Persona 2 is not a free label. It is GU's Cartan-involution / ghost-parity `Z2`: W173
established that on the `(generation, mirror)` triplet the Krein form `K` IS the Cartan involution of
`so(9,5)` (residual `0.0e+00`), and it equals the ghost parity `u <-> v`. So the bit "is the Z2
conserved" is identically the bit W173 spent its whole computation on: is the mirror a genuine RECORD
(BRST-cohomology-nontrivial, the Cartan `Z2` a global superselection grading, OPERATIVE) or a gauge
REDUNDANCY (the mirror paired away into a BRST doublet, the `Z2` not operative)?

W173's determined-content verdict: at every level GU ACTUALLY computes (the built, nilpotent free BV
bicomplex on `M(64,H)`), the mirror is BRST-CLOSED-NOT-EXACT -> a RECORD -> the Cartan `Z2` OPERATIVE.
A conserved global `Z2` under which generation and mirror carry OPPOSITE charge FORBIDS the
generation-mirror Dirac mass D (a superselection rule between the two sectors), protects the chiral
generation, and forces the mirror to gap by the mirror-only SMG channel S. **So GU's DETERMINED content
LEANS REALIZES.** The ONLY thing that demotes the mirror to redundancy (Z2 broken, D allowed, chirality
lost) is the UNBUILT Y14 connection-curvature 2-form that closes the secondary constraint C2 and pairs
`(generation, mirror)` into a doublet, and W173 proved GU's native cores cannot supply it (spectrally
sign-blind; C non-unique at the three-generation degeneracy).

This is the closure A3 can honestly claim: the SMG-realization question for GU's SPECIFIC mirror does NOT
introduce a new open object. It REDUCES to, and UNIFIES with, the single unbuilt datum that already
governs `bar(b)` (W173) and W216's good branch: the Y14 spectral section / operative-C / mirror-as-record
bit. And it gives that bit sharp SO(10)-representation content: the source action must (i) preserve the
Cartan `Z2` as a conserved symmetry (forbidding the chirality-killing Dirac mass D), and (ii) condense the
mirror-only `(16bar)^4` SO(10)-singlet (channel S), NOT the generation-mirror singlet (channel D) that
W216 built.

## 4. Persona 4 -- ruthless skeptic: is this a closure or a relabeled punt, and does it push to a verdict?

**Steelman COMPLETED-REALIZES.** GU's determined content leans record (W173), the record reading forbids
D, the rep theory forbids the symmetric bilinear that drives B, and the Butt-Catterall class realizes S
by a direct transition. Is that not enough to call it realized? **Answer: no, and honestly so.** W173
itself graded the record reading QUANTIZATION-DEPENDENT and UNFORCED: the demotion to redundancy is
possible, needs the unbuilt Y14 datum, and neither branch is certified. Worse, GU's OWN built condensate
(W216) is in channel D, not S: on the good Krein branch the BCS pairing couples the generation to the
mirror. If that is the literal content, GU's "gap exists" is the chirality-killing trivial gap, not SMG.
Whether the good-branch condensate is really D (chirality lost) or reorganizes into S (chirality kept)
depends on whether the C-operator that defines the good branch also respects the Cartan `Z2` as a
conserved charge, and that is again the unbuilt spectral section. Granting the good Krein branch fixes the
SPECTRUM (real, gapped); it does NOT fix the CHANNEL (D vs S). So COMPLETED-REALIZES would overstate what
GU has built.

**Steelman COMPLETED-BROKEN (falsifies chirality).** The mean-field default, if D is available, is the
Dirac well: chirality lost. And W216 built exactly D. Is that a kill? **Answer: no.** D being available
requires the Z2 broken (redundancy), which is NOT GU-forced (W173: native cores cannot supply the
demoting datum; determined content leans the other way). A live failure mode that requires an unbuilt
demotion is not a proof of failure. So COMPLETED-BROKEN is not earned either.

**Is this a relabeled punt?** No, and precisely because it does three things W225 did not: (a) it proves,
by machine-checked rep theory, that the generic Eichten-Preskill symmetry-breaking channel B has no
explicit driver for the `16` (the symmetric bilinear does not exist), placing GU in the favorable direct-
transition class -- a real narrowing of the open region; (b) it EXHIBITS a new, previously-unflagged
failure mode, the chirality-killing trivial vectorlike Dirac gap D, and identifies it with GU's OWN built
condensate (W216) -- a substantive, somewhat uncomfortable finding, not a punt; (c) it reduces the whole
residual to a SINGLE pre-existing bit (mirror-as-conserved-record = W173's operative-C) with sharp SO(10)
content, rather than leaving a generic coupling-ratio dependence. That is a closure of the STRUCTURE with
one honestly-named external bit, which is the correct non-naive outcome.

**Skeptic verdict: STILL-OPEN-NEEDS-X, leaning REALIZES.** The lean is STRONGER than W225's generic
"leaning survives" because it is anchored to GU's determined free-BV content (W173's record reading),
not to generic literature; but it is not a certification.

## 5. The toy, its controls, and what it does and does not decide

`tests/W231_close_a3_smg_realization_gu_mirror.py`, 26/26, exit 0, positive controls FIRST. The rep
theory (Blocks A-C) is EXACT: complexity of the `16`, zero singlets in `16(x)16` (channel B), one singlet
in `16(x)16bar` (channel D), two singlets in `16^(x)4` (channel S). The discriminant (Block D) is the Z2
charge algebra: D is Z2-odd (forbidden when the grading is conserved), S and B are Z2-even. The mean-field
trichotomy (Block E) is a schematic Landau comparison, reconstruction-grade illustrative exactly as
W225's toy: it DEMONSTRATES that the winner flips on the single Z2-conservation bit (S when conserved, D
when broken) and it CANNOT fix GU's couplings. Positive control: Z2 conserved -> S realizes. Negative
control: Z2 broken -> D, chirality lost. Teeth: a counterfactual symmetric bilinear opens B and breaks
SO(10), with a guard that the real `16` forbids it. The toy decides the STRUCTURE and the discriminant; it
does not, and does not claim to, replace the unbuilt source action.

## 6. Verdict

**STILL-OPEN-NEEDS-X, leaning REALIZES.** Granting anomaly-freedom (W224), existence (Wang-Wen, W225),
no beyond-anomaly obstruction (W225), and the good Krein branch condensate (W216):

- **The symmetry argument CLOSES the structure.** By the complexity of the SO(10) `16`: channel B (the
  Eichten-Preskill symmetry-breaking mirror bilinear) has NO SO(10) singlet, so it needs spontaneous
  breaking and has no explicit driver -- GU sits in the favorable Butt-Catterall direct-transition class.
  Channel S (the mirror-only `(16bar)^4` SMG singlet) EXISTS and keeps the generation chiral. Channel D
  (the gen-mirror Dirac singlet) EXISTS, is SO(10)-symmetric, but gaps the generation too (chirality
  lost) -- and it is the channel GU's OWN W216 condensate structurally sits in.

- **The residual is ONE named, pre-existing bit.** The discriminant between the success S and the trap D
  is whether the Cartan-involution / ghost-parity `Z2` (generation +, mirror -) is a CONSERVED symmetry,
  which is identically W173's operative-C / Y14-spectral-section datum (the mirror being a conserved
  RECORD, not a redundancy). GU's DETERMINED free-BV content makes the mirror a record -> `Z2` operative
  -> D forbidden -> S realized -> LEANS REALIZES. The demotion (Z2 broken, D allowed, chirality lost)
  needs the UNBUILT Y14 curvature and is NOT GU-forced (W173).

- **X, stated exactly.** Build the Y14 connection-curvature 2-form / close C2 / fix the spectral section
  so that the source action (i) preserves the Cartan `Z2` as a conserved symmetry (forbidding the
  chirality-killing generation-mirror Dirac mass D) and (ii) condenses the mirror-only `(16bar)^4`
  SO(10)-singlet (channel S) rather than the generation-mirror singlet (channel D) that W216 built. This
  is the SAME object that clears `bar(b)` and selects W216's good branch; A3 does not add an open object,
  it unifies the SMG question into that one and sharpens its SO(10) content.

Not **COMPLETED-REALIZES** (the record reading is unforced, W173; W216's built condensate is in the trap
channel D, and granting the good branch fixes the spectrum but not the channel). Not **COMPLETED-BROKEN**
(channel D is not GU-forced; its availability requires the unbuilt demotion). The honest verdict is the
sharpened open one, with the lean toward realization coming from GU's own determined content.

## 7. What this changes

Nothing in canon moves. Exploration grade; `bar(b)` and `H59` stay OPEN; the count stays `{1,3}`; no
forbidden target inserted; the reservoir Krein sign is a SEPARATE gate and is NOT re-litigated. What the
note DOES, relative to W225:

- W225 left the realization as a two-basin (S vs B) coupling-ratio-dependent open problem. W231 resolves
  the STRUCTURE to three channels by exact rep theory, PROVES the Eichten-Preskill channel B has no
  explicit driver (favorable class), and reduces the whole residual to one pre-existing bit.
- W231 EXHIBITS a new failure mode W225 and W216 did not flag: the chirality-killing trivial vectorlike
  Dirac gap D, which is SO(10)-symmetric yet removes the chiral generation, and which is the channel GU's
  OWN good-branch condensate (W216) structurally realizes. This is a real caution on reading W216's "gap
  exists" as "chirality survives."
- W231 UNIFIES the SMG-realization question with W173's records-vs-redundancy / operative-C datum and
  W216's branch, giving that single unbuilt object sharp SO(10)-representation content.

**On `bar(b)` and `H59`: UNCHANGED / OPEN.** No canon / verdict / posture flip.

**Artifacts:** this file + `tests/W231_close_a3_smg_realization_gu_mirror.py` (26/26, exit 0; positive
controls first).

*Filed 2026-07-14 by lane A3 (W231) of the GAP-CLOSURE wave. Four personas inline in one worker (SMG /
lattice-chiral-fermion specialist; deconfined-criticality / competing-phases specialist; GU-mirror-content
specialist; ruthless skeptic); no sub-agents. Reproducible:
`python -u tests/W231_close_a3_smg_realization_gu_mirror.py` (26/26, exit 0). Exploration grade;
STILL-OPEN-NEEDS-X leaning REALIZES; RUTHLESS skeptic; no canon movement; bar(b)/H59 OPEN. Zero em dashes.*
