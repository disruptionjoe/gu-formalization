---
title: "Eric/Curt Wave 3D-B2C6: two-connection transgression, candidate odd maps, and the conditional local-reading gate"
status: exploration
doc_type: construction_result
updated_at: "2026-08-01"
branch: agent/null-clifford-omega1-repair
run: RUN-20260801-085119-gu-formalization-ecw3d-b2c6-direct
predecessor: explorations/eric-curt-wave3d-b2c5-covariant-action-green-ward-2026-08-01.md
registry: lab/process/eric-curt-wave3d-b2c6-fermion-boson-euler-factorization.json
probe: tests/channel-swings/eric_curt_wave3d_b2c6_fermion_boson_euler_factorization_probe.py
grade: "SPLIT CONSTRUCTION AND CONDITIONAL LOCAL-READING RESULT. A mandatory primary-source collision corrects the first target: Weinstein supports two physical somatic complexes with a shared on-shell Einstein obstruction, but does not supply odd transformations of the connection, reduction, or metric. The repo reconstructs the written G2 source-curvature polynomial exactly as the affine-connection-segment average integral_0^1 F_(B+tT) dt and, equivalently, the equal AA/BB/symmetrized-AB route average; Weinstein does not state this interpolation or its weights. The associated-bundle mixed operator H_AB^E is kept distinct from its adjoint-curvature representative Hhat_AB. The mixed AB composition cancels arbitrary first and symmetric second jets. A nonzero symmetric complex-symplectic mixed-carrier bracket and a nonzero active projected-endomorphism space are exact separately; their actual intersection is uncomputed. The G3 graph return for mu_T is exact. After correcting a split-signature index placement, the right Levi-Civita composition is Einstein-gamma and the left is its vector/Krein adjoint on positive-index, tracefree mixed-sign, and off-diagonal Ricci fixtures; both vanish on pure Weyl, while a non-admitted internal comparator exposes an independent grade-three slot. At any admissible bosonic solution carrying the verified generically invertible B2C5 principal symbol, that Hessian cannot be the ungauge-fixed Hessian of a finite-order local polynomial odd gauge symmetry. The probe's formal flat intermediate-field jet does not itself construct such an admissible Levi-Civita/DeWitt point. A larger degenerate/gauge-fixed parent, rigid operation, changed carrier, nonlocal operation, no odd symmetry, and the unreleased on-shell two-connection complex remain open. P1/P2/P3 are unused."
---

# Wave 3D-B2C6 — two-connection transgression and the conditional local-reading gate

## Plain-English result

This swing found a real positive construction and a sharp boundary.

The positive construction is that the bosonic curvature expression already
written in G2 is not an arbitrary fitted polynomial. If

\[
B=A_{\rm LC}(\epsilon_{\rm red},g_{\rm DW}),
\qquad T=A-B,
\qquad A_t=B+tT,
\]

then its curvature is

\[
F_{A_t}=F_B+tD_BT+t^2q(T,T),
\]

and therefore

\[
\boxed{
\int_0^1F_{A_t}\,dt
=F_B+\frac12D_BT+\frac13q(T,T).}
\]

That is exactly the curvature polynomial inside the G2 source action. The
same object is also the equal average of three routes: curvature from `A`,
curvature from `B`, and the symmetrized high-road/low-road `AB` composition.
The exact jet calculation shows why Weinstein's two derivatives matter: the
first and second derivatives cancel in the symmetrized mixed route, leaving a
zero-order curvature/distortion obstruction.

The boundary is equally useful but conditional. The repaired B2C5 fermion
operator is invertible off the characteristic cone. At any admissible bosonic
solution carrying that principal symbol, no finite-order local polynomial odd
gauge generator can lie in its kernel. The probe's `F_B=T=0` fixture is only a
formal intermediate-field jet: it verifies the zero source-curvature seed but
does not construct an admissible point of the full Levi--Civita/DeWitt graph or
execute the complete returned bosonic Euler tuple there.

So the present B2C5 operator can still be a good **gauge-fixed** physical
operator, a rigid/Killing-spinor system, or the descendant of a larger
two-connection parent. It cannot simultaneously be treated as the
ungauge-fixed operator of a local odd gauge symmetry.

That is not a failure of the source-action program. It identifies the next
construction: first build Weinstein's two physical somatic route operators and
test their claimed shared Einstein obstruction. A degenerate parent and gauge
fixing become the next step only if that construction independently generates
a local odd-gauge requirement.

## 1. Layer 0: four different structures

The source review requires permanent separation of:

| object | type | role |
|---|---|---|
| `Psi=(zeta,nu)` | physical `Omega1(S) direct-sum Omega0(S)` field | fermionic matter/operator carrier |
| ordinary gauge parameter | `Omega0(ad P)` | even `Gau(P)` generator already owned by G3 |
| `q_odd` | independent hypothetical odd-generator parameter | input to a future super-IG transformation |
| somatic composition | composition of physical Bose/Fermi route operators | on-shell obstruction/complex question, not automatically Noether II |

For BV bookkeeping, the ghost of an **odd** generator is parity even. Thus
“odd ghost” is unsafe shorthand: `q_odd` refers to the generator's parity,
not the parity of its BV ghost. Physical `nu` is neither object.

Also keep distinct:

- ordinary gauge Ward closure;
- the two-connection somatic obstruction;
- an on-shell complex modulo bosonic equations; and
- an off-shell local odd action symmetry.

None implies another.

## 2. Mandatory source collision changes the order

The B2C5 plan asked first for `mu_A`, `mu_epsilon`, and `mu_g`. The required
source recheck changes the efficient order.

### `SOURCE-CONFIRMS`

- Portal `01:48:26--01:54:09` says the Bose and Fermi somatic complexes are
  intended to share a generalized-Einstein obstruction.
- Portal `01:48:59--01:51:50` explicitly uses two derivative operators from
  two connections. Their difference is said to be zero-order augmented
  torsion; composing the high and low routes is intended to remove the
  differential operators and leave an Einstein-shaped obstruction.
- Portal `01:55:03--01:58:27` types `zeta in Omega1(S)` and `nu in Omega0(S)`
  as two separate physical fields obeying coupled Dirac-role equations. This
  is why neither may be silently relabeled as an odd gauge parameter or ghost.
- TOE `01:41:12--01:42:56` and UCSD `00:46:02`, `00:49:16` support an
  algebraic odd extension whose products land in connection translations.
- TOE `02:18:17--02:20:33`, UCSD `00:20:57--00:22:26`, and Portal
  `01:41:26--01:41:58` support displacement relative to the gauge-rotated
  Levi--Civita connection.
- TOE `02:38:12--02:39:51` identifies curvature as the obstruction to the
  covariant fermion complex.

### `SOURCE-CORRECTS`

The primary source-guided calculation is the two-connection on-shell somatic
factorization, not an assumed odd Noether identity. A single-connection
curved sequence is only a control. “Square root of connections” names the
algebraic bracket target; it is not a supplied analytic square root, action,
or BV differential.

### `SOURCE-SILENT`

At TOE `01:43:43--01:44:14`, when asked for the supersymmetric action
structure, Weinstein does not provide it. The sources give no formulas for

\[
\delta_qA,\qquad\delta_q\epsilon_{\rm red},\qquad
\delta_qg_{\rm DW},\qquad\delta_q\Psi.
\]

They also do not give the active-real-form odd bracket, Jacobi/BV
completion, Velo--Zwanziger cure, or the exact curved cancellations. Portal
`02:03:07` explicitly says the stress-energy “up-and-back” and Dirac
“up-and-over versus over-and-up” zero-order invariant cancellations were
still unfinished.

The TOE `02:44:06--02:45:43` cyclic/acyclic wording is transcription-uncertain
and the operator is unreleased. No period, signs, or generation inference are
imported from it.

## 3. Exact affine-segment and route construction

This subsection is **repo-derived from source-motivated two-connection
geometry, not author-stated**. The source motivates the two connections,
their zero-order difference, and a shared obstruction; it does not state the
affine interpolation, equal route weights, or the `1/2,1/3` derivation below.

Freeze the G2/G3 graph

\[
B=A_{\rm LC}(\epsilon_{\rm red},g_{\rm DW}),
\qquad T=A-B.
\]

The endpoint curvature identity is

\[
F_A=F_B+D_BT+q(T,T).
\]

For the whole affine connection segment `A_t=B+tT`,

\[
F_{A_t}=F_B+tD_BT+t^2q(T,T).
\]

Coefficientwise integration gives

\[
Q_{\rm seg}
:=\int_0^1F_{A_t}\,dt
=F_B+\frac12D_BT+\frac13q(T,T).
\]

This is the written G2 source-curvature polynomial before the moving Shiab
and leading `T` pairing are applied.

There is a second, source-facing expression. For an associated bundle `E`
with representation `rho`, define the symmetrized mixed-route operator

\[
H^E_{AB}
=\frac12\left(d_A^Ed_B^E+d_B^Ed_A^E\right)
\big|_{\Omega^0(E)}.
\]

The exact product-rule computation cancels all second derivatives and all
first derivatives of the test field:

\[
H^E_{AB}=\rho(\widehat H_{AB}),\qquad
\widehat H_{AB}:=F_B+\frac12D_BT
=\frac12\left(F_A+F_B-q(T,T)\right).
\]

Consequently

\[
\boxed{
Q_{\rm seg}
=\frac13\left(F_A+F_B+\widehat H_{AB}\right).}
\]

After applying `rho`, the corresponding identity is

\[
\rho(Q_{\rm seg})
=\frac13\left(\rho(F_A)+\rho(F_B)+H^E_{AB}\right).
\]

The probe verifies this twice:

1. by exact rational connection/curvature algebra; and
2. by replaying two unrelated first-jet and symmetric-second-jet assignments
   while holding the zero-jet field fixed.

Both replays return the same zero-order `H^E_AB`. An endpoint-only average
`(F_A+F_B)/2` is a planted failure because it gives the wrong quadratic
weight.

### What this does and does not mean

This is a concrete origin for the action's `1/2` and `1/3` geometry. It also
makes Weinstein's two-connection direction operational without inventing a
third connection.

It does **not** prove that `Q_seg` is the Euler equation. The action varies in
all three `T` slots and through the moving Shiab, Hodge, density, trace line,
Krein dual, and Levi--Civita graph. The actual owner remains

\[
E_T
=S_\epsilon(F_B)
+\frac12(L+L^\ddagger)T
+M_\epsilon(T,T)
+\kappa_1\flat_1T.
\]

The inherited exact G2 plant has `q(y,z)=0` while the noncentral
slot-symmetrized Euler response is nonzero. Thus `S(F_A)+kappa T` remains an
invalid compressed replacement.

## 4. The smallest candidate odd maps survive algebra

Let `Omega` be the complex symplectic spinor form and define

\[
\mu_\Omega(u,v)w
=\Omega(u,w)v+\Omega(v,w)u.
\]

On the mixed carrier `Q_mix=S direct-sum (T*Y tensor S)`, the alpha-free
candidate is

\[
\boxed{
\beta((u,\psi),(v,\chi))(X)
=\mu_\Omega(u,\chi(X))+\mu_\Omega(v,\psi(X)).}
\]

An exact finite symplectic control proves that `beta` is symmetric, nonzero,
adjoint-valued, and equivariant. With one entry equal to `(q_odd,0)` and the
other to the physical rolled field, its connection leg is

\[
(\mu_A^{(0)})_a
=\Pi_{\rm ad,real,H}\mu_\Omega(q_{\rm odd},\zeta_a).
\]

The active `Cl(9,5)=M(64,H)` calculation then shows:

- representative `C+` Clifford bilinears at every degree `0--14` obey their
  exact degree-dependent transpose laws;
- even Clifford degree is cross-chiral and odd degree is same-chiral;
- a nonzero rank-one endomorphism survives the explicit `C+`, right-`H`, and
  Krein-skew projections; and
- a nonzero real symmetric metric bilinear

  \[
  h_{ab}=\operatorname{Re}_K
  \langle q_{\rm odd},\gamma_{(a}\zeta_{b)}\rangle
  \]

  survives.

The complex mixed moment-map image and this active projected-endomorphism
space are separately nonzero. Their actual intersection has not been
computed, so the active connection transformation remains a type-level gate.
Krein reality and right-`H` constrain rather than automatically kill the
idea.

The smallest live reconstruction basis is:

\[
\begin{aligned}
(\mu_A)_a={}&
b_1\Pi_{\rm ad}\mu_\Omega(q_{\rm odd},\zeta_a)
+b_2\Pi_{\rm ad}\mu_\Omega(\gamma_aq_{\rm odd},\nu)+\cdots,\\
\mu_\epsilon={}&
\Pi_{\mathfrak m_\epsilon}
\left[c_1\mu_\Omega(q_{\rm odd},\nu)
+c_2\mu_\Omega(q_{\rm odd},\Gamma\zeta)\right]+\cdots,\\
\mu_g={}&
P_{T_g\mathcal G_{\rm DW}}
\left[a_1\operatorname{Re}_K
\langle q_{\rm odd},\gamma_{(a}\zeta_{b)}\rangle
+a_2g_{ab}\operatorname{Re}_K
\langle q_{\rm odd},\nu\rangle+\cdots\right].
\end{aligned}
\]

The ellipses are not fitted directions. The actual active-adjoint,
reductive-tangent, and induced-DeWitt-tangent projectors must be constructed
before those channels become bundle transformations.

## 5. The G3 graph law is compulsory

Only `A`, `epsilon_red`, and `g_DW` are free. Therefore

\[
\boxed{
\mu_T
=\mu_A-(D_\epsilon B)\mu_\epsilon-(D_gB)\mu_g.}
\]

Choosing `mu_T` independently double-counts the field graph.

The exact rational control includes live `B`, `S`, and pseudo-musical
responses. It verifies that the primitive variation

\[
\langle E_T,\mu_T\rangle
+\langle E_B^\circ,\delta B\rangle
+E_S[\delta S]+E_\flat[\delta\flat]
\]

returns exactly to

\[
\langle E_A,\mu_A\rangle
+\langle E_\epsilon,\mu_\epsilon\rangle
+\langle E_g,\mu_g\rangle.
\]

An independently assigned `mu_T` is a planted rejection.

## 6. Both torsion-free curved compositions

B2C5 executed only the right Levi--Civita composition. B2C6 now executes the
left vector-spinor commutator as well:

\[
C_gA_w\zeta
=\frac12\gamma_{abc}
\left(F_S^{ab}\zeta^c
+R^c{}_{d}{}^{ab}\zeta^d
+F_{\rm int}^{ab}\zeta^c\right)
\]

in a normal, torsion-free, parallel-gamma frame. The vector index and spinor
index both carry curvature.

After correcting the split-signature index placement, exact fixtures give:

- the right composition equals one-half Einstein-gamma on a traceless-Ricci
  fixture;
- the left composition is the vector-metric/Krein adjoint row of the right
  Einstein-gamma map on the original positive-index fixture, a tracefree
  mixed-sign diagonal fixture, and an off-diagonal mixed-sign fixture;
- a nonzero algebraic pure-Weyl fixture has zero Ricci and vanishes in both
  compositions; and
- a commuting scalar-endomorphism curvature gives an independent left
  grade-three slot.

The last item is only a type-level control. The scalar identity is not in the
active symplectic adjoint, so it cannot be reported as actual GU internal
curvature.

The actual-adjoint internal curvature, `A/A` and mixed `A/B` compositions,
torsion derived from `T`, moving Clifford/projector/weight jets, density
derivatives, and boundary formal adjoints remain unexecuted. They become
relevant after a viable typed two-connection somatic operator exists.

## 7. Conditional local-syzygy classifier

Assume an ordinary local odd symmetry begins at lowest fermion order as

\[
\delta_q\Psi=R_\Phi q_{\rm odd}+O(\Psi^2),
\qquad
\delta_q\Phi=\mu_\Phi(q_{\rm odd},\Psi)+O(\Psi^3),
\quad \Phi=(A,\epsilon,g).
\]

The probe uses the formal intermediate-field jet

\[
F_B=0,\qquad T=0,\qquad\Psi=0.
\]

At this fixture, the G2 source-curvature seed vanishes. The probe does **not**
construct `B=A_LC(epsilon_red,g_DW)` on the actual induced trace-reversed
DeWitt field graph or execute the complete
`E_T,E_B,E_S,E_flat -> E_A,E_epsilon,E_g` return there. Its admissibility as a
bosonic solution is therefore open.

The exact general statement is conditional: at any admissible bosonic
solution on which the B2C5 Hessian carries its verified generically invertible
principal symbol, a proposed local Noether identity would require

\[
D_0R_0=0
\]

and, in the independent-dual formulation, its paired left condition.

The natural first-order basis is

\[
R_1(k)=\binom{k\otimes1}{0},\qquad
R_2(k)=\binom{\gamma_ac(k)}{0},\qquad
R_3(k)=\binom{0}{c(k)}.
\]

On a timelike and a generic covector, the exact active-matrix probe finds
rank three and nullity zero for their images under the completed B2C5
operator. The minimum Gram eigenvalues are about `52.654` and `492.335`.
Fixing the exterior-gradient coefficient to one is inconsistent.

More strongly, B2C5 already proved that the full symbol `D_0(k)` is
invertible on an open covector set. Hence any finite-order local polynomial
`R_0(k)` satisfying `D_0(k)R_0(k)=0` vanishes on an open set and therefore
identically.

The positive control still recognizes

\[
A_w(k)K(k)=0.
\]

The failure comes from completing the physical rolled equations: the lower
codifferential-after-gradient route is nonzero off the null cone, and the
full repaired system has no polynomial gauge kernel.

The verdict at this scope is

\[
\boxed{\texttt{CURRENT\_B2C5\_HESSIAN\_CANNOT\_BE\_AN\_UNGAUGE\_FIXED}}
\]

local polynomial odd gauge Hessian, conditional on an admissible solution
carrying that symbol. This does not make a degenerate parent mandatory; it
only removes one reading of the current Hessian.

### Scope of the classifier

It assumes:

- an admissible bosonic solution carrying the B2C5 principal symbol;
- the B2C5 square is the ungauge-fixed fermion Hessian;
- the odd parameter is arbitrary and local;
- the generator is finite-order differential/polynomial in the symbol;
- bosonic variations are bilinear in parameter and physical fermion; and
- no additional compensator or fermion tadpole exists.

It does not kill:

1. a larger degenerate parent whose gauge fixing gives B2C5;
2. a rigid or Killing-spinor operation;
3. an enlarged fermion carrier or changed action;
4. a priced nonlocal/pseudodifferential transformation;
5. the abstract mixed-carrier super-IG bracket; or
6. Weinstein's unreleased on-shell two-connection somatic complex.

## 8. Coefficients, surplus, and external datum

The two-connection identity adds no free parameter. It explains the already
written `1/2` and `1/3` coefficients as affine-segment moments and route
weights. It does not select B2C5's remaining source scale or shape

\[
p=\frac{w_+w_-}{r^2}.
\]

Because the present operator cannot carry that local odd symmetry under the
stated admissible-solution condition, no odd-Ward constraint may be counted
against those parameters. Doing so would inflate constraint surplus.

P1/P2/P3 remain unused. In particular:

- a missing odd bracket is not external datum;
- a missing gauge parent or gauge fixing is not external datum;
- a coefficient of the fermion action is not external datum; and
- the route average has no generation-count or chirality content.

## 9. Seven-axis disposition

| layer | result |
|---|---|
| Layer 0 | physical fields, even gauge, odd generator, on-shell complex, and Noether identity separated |
| L1 geometry | active trace-reversed `(3,1)+(6,4)=(9,5)` retained; Curt `(7,7)` remains rival |
| L2 fields | `A,epsilon,g` free; `B,T,S,flat` derived graph retained |
| L3 symmetry | ordinary G3 gauge symmetry retained; mixed super-IG bracket is a candidate, not a source-derived local symmetry |
| L4 dynamics | repo-derived affine-segment/two-route source-curvature construction; conditionally, the current B2C5 Hessian is not an ungauge-fixed local polynomial odd gauge Hessian |
| L5 quantum | complex mixed bracket and active projected-endomorphism space are separately nonzero; their intersection, constrained-real BV, and domain remain open |
| L6 physics | right Einstein-gamma and left vector/Krein-adjoint relations pass three Ricci fixtures; pure Weyl vanishes; no SM, mass, count, or cosmology recovery claimed |
| L7 evidence | `33 exact + 4 type-level + 14 planted = 51 PASS`; source timestamps and nonclaims recorded |

## 10. Highest-information next gate

The primary next gate is

`ECW3D-B2C7-TWO-CONNECTION-SOMATIC-OPERATORS-AND-SHARED-EINSTEIN-OBSTRUCTION`.

It should:

1. transcribe and type the source-described Bose and Fermi somatic route
   operators using the two owned connections, with `zeta` and `nu` retained as
   physical fields;
2. compose high-road/low-road operators and test whether both route systems
   have one shared generalized-Einstein obstruction, using `Q_seg` only as a
   repo-derived coefficient candidate rather than an author-stated formula;
3. vary the moving Shiab, trace line, Hodge, pseudo-musical, density, and
   Levi--Civita graph so the obstruction is the actual G2/G3 differential
   Euler module, not only `Q_seg`;
4. decide whether the result is an on-shell complex, a rigid operation, or
   genuinely generates a local odd gauge redundancy;
5. only in the third case, construct the smallest degenerate parent with a
   compensator distinct from physical `nu`, prove BV closure, and test whether
   gauge fixing returns the B2C5 repaired Hessian; and
6. replay left/right curvature, active-adjoint, torsion, moving-weight, and
   boundary controls on the resulting somatic construction and, only if one
   was introduced, its parent.

The cheap Stueckelberg completion of an arbitrary invertible operator is only
a positive control. No parent should be introduced unless the typed somatic
construction generates the need for one.

The analytic boundary gate remains separately runnable:

`ECW3D-B2C6B-ACTION-GREEN-ENERGY-DOMAIN-AND-BOUNDARY-COMPLETION`.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE` and
`TG-1 AND TG-2 AND TG-3` remains false.

## Validation and boundary

The executable probe reports:

```text
ECW3D-B2C6:
33 exact + 4 type-level + 14 planted = 51 PASS
```

The type-level controls are explicitly not promoted to exact active-GU
results: the complex mixed bracket still needs native real projection, its
actual intersection with the separately nonzero active projected-endomorphism
space is uncomputed, the metric bilinear needs the induced DeWitt tangent
projector, and the scalar internal-curvature comparator is not in the active
adjoint.

No complete covariant fermion action, odd BV master equation, global bundle
transformation, active-adjoint curved factorization, analytic domain,
stationary nonflat vacuum, Standard Model recovery, anomaly/index, generation
count, cosmological amplitude, or PP3 prediction is claimed.
