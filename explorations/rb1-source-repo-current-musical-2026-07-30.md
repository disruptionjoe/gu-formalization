---
title: "RB1 source/repository register, varied-root dependency ledger, and native connection-current pseudo-musical"
status: active_research
doc_type: construction_result
created: 2026-07-30
lane: "1"
work_item: RB1-SOURCE-REPO-DICTIONARY-DEPENDENCY-DAG-CURRENT-MUSICAL
run: lab/process/runs/GUH-20260730T191725Z-rb1-source-repo-current-musical/run-plan.md
probe: tests/channel-swings/rb1_source_repo_current_musical_probe.py
grade: "FORMULA-BUILT / TYPED CONSTRUCTION WITH HOSTILE-REVIEW CORRECTIONS. The frozen Layer-0 arrow register is complete but source/repository alignment remains unresolved. A one-parameter formal affine comparison family is exposed; lambda=1 uniquely preserves the source B_omega epsilon-only dependency, while lambda!=1 is a reference-gauge-orbit-constrained control family. The N1 varied-primal Euler-root graph plus coefficient/map/background owner ledger, fixed-geometry A-variation of all nine full-20 blocks, graded J_D/Qhat_F/Jhat_F Green split, and N3's native indefinite G/kappa_g pseudo-musical are explicit. Three candidate action schemas plus one control family are emitted to RB2 on the Spin/stabilizer arena. No field-space pullback, source/repository identity, source-forced Shiab selector, preferred action, full-Sp lift, stationarity, CME, global domain, physical mass, index, count, canon verdict, or public-posture change is claimed."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# RB1 source/repository register, dependency ledger, and current musical

## Result first

RB1 returns:

```text
RB1-GO
LAYER-0-REGISTER-COMPLETE; SOURCE/REPOSITORY-ALIGNMENT-UNRESOLVED
FORMAL-AFFINE-LAMBDA-FAMILY; LAMBDA=1-DEPENDENCY-PRESERVING
N1-VARIED-ROOT-DAG+OWNER-LEDGER-BUILT; PHANTOM/DUPLICATE-EDGES-REMOVED
FIXED-GEOMETRY-A-VARIATION; J_D/QHAT_F/JHAT_F-GREEN-SPLIT-BUILT
N3-G/KAPPA_CONNECTION-PSEUDO-MUSICAL-INSTANTIATED-POINTWISE/WEAKLY
THREE-RB2-CANDIDATES+ONE-REFERENCE-ORBIT-CONTROL-FAMILY
RB2-SPIN/STABILIZER-FROZEN; MOVING-FULL-SP-HELD-RB3
INDEPENDENT-SOLDERED-CURRENT-ABSENT
```

The highest-value positive result is that RB1 has instantiated N3's
connection-current musical placeholder at local algebraic and compact-core
weak grade. On the native real form, a nondegenerate gimmel metric \(G\) and
a nondegenerate real invariant adjoint pairing
\(\kappa_{\mathfrak g}\) convert an adjoint-valued connection equation
covector into an adjoint-valued one-form. Positivity is neither used nor
available. The spinor Krein form is upstream in the fermion current; it is
not this musical.

The highest-value Layer-0 result is that Weinstein's source notation does
not identify the repository objects by substitution. The checked receipts
display \(B_\omega\) and \(T_\omega\), but do not locally source the equality
\(A_\omega=B_\omega+T_\omega\). Treating their algebraic sum as the
separately appearing \(A_\omega\) is therefore a reconstruction. That
reconstruction admits the whole formal family

\[
A_\omega\dashmapsto A-\lambda U,\qquad
B_\omega\dashmapsto\Gamma_{\rm conn}+(1-\lambda)U.
\]

Only \(\lambda=1\) preserves the displayed source dependency that
\(B_\omega\) is derived from \(\epsilon\) alone. Every
\(\lambda\ne1\) charges one continuous parameter and additionally constrains
the independent \(U\)-piece to the gauge orbit of the reference connection.

The most important corrections to N3 are:

1. the literal N1 bilinear
   \(J_{\rm literal}(Z)=\operatorname{Re}[Z,Kc(e_a)Z]\) has no
   \(A\)-dependence;
2. the independent-parent term has no
   \(\epsilon_{\rm IG}\)-dependence on the written \(V_{\rm src}=0\)
   stratum;
3. \(\Gamma_{\rm conn}(\epsilon_{\rm IG})\) and the full-20 Clifford trace
   \(\Gamma_{\rm tr}\) are different maps;
4. \(g_s=s^*G\) is derived from \(s\), not an independent Euler root, and
   the literal induced metric depends on \(ds\), so its density/Hodge/norm
   chains are first order in the section variation;
5. \(F_{s^*A}=s^*F_A\) and the direct-\(II\)/Gauss forms are alternative
   presentations, not additional terms; and
6. all \(\tau=0\) BV owners inherit Lie-derivative Green obligations, while
   the open-BV Hom grammar remains uninstantiated; and
7. moving pullback and differentiating \(s_*\) are two presentations of one
   shape derivative and cannot be added twice.

The output is therefore a finite RB2 shootout, not a verdict for or against
GU and not a return to “the source action is missing.”

## Plain English

We can now put the connection equation on the same side of the equals sign
as the distortion field without pretending a covector is already a vector.
The conversion uses the fourteen-dimensional metric and the gauge algebra's
own invariant bilinear form. It works even though both structures are
indefinite.

We also learned exactly how N1's extra \(U\) enters the comparison. There is
not a two-way choice but a one-parameter affine family. The source
dependency graph selects \(\lambda=1\) as the only unconstrained comparator:
its reference connection is \(\Gamma(\epsilon_{\rm IG})\) and its matter
connection is \(A-U\). Other values remain useful hostile controls, but
they must make the extra \(U\)-piece part of the reference-connection gauge
orbit. No bundle map or pullback universal property is claimed.

Finally, we differentiated what the action actually contains rather than
what its symbols suggest. Several dependencies that looked plausible are
exactly absent. That matters because a false dependency creates a fake
Euler term, and a fake Euler term can make a Noether identity appear to
close or fail for the wrong reason.

## Frozen inputs and evidence boundary

This swing inherits without modification:

- the N1 bulk-plus-defect action and P1/P2/P3 datum ledger from
  [`unified-source-datum-packet-v0-2026-07-30.md`](unified-source-datum-packet-v0-2026-07-30.md);
- the N2a actual-\(\operatorname{Sym}^2T^*X\) odd-bilinear screen;
- the N4a curvature-irrep/open-BV screen;
- the N3 term-to-equation ledger from
  [`unified-source-datum-variational-emission-map-2026-07-30.md`](unified-source-datum-variational-emission-map-2026-07-30.md);
- the primary-source packet
  [`weinstein-gu-primary-source-pack-2026-07-30.md`](../lab/sources/weinstein-gu-primary-source-pack-2026-07-30.md);
- the source action locator in
  [`hourly-20260626-1003-cycle3-tau-source-locator-packet.md`](hourly-cycles/hourly-20260626-1003-cycle3-tau-source-locator-packet.md);
  and
- the rendered equations (9.4)--(10.9) ledger in
  [`hourly-20260625-0301-cycle3-rendered-dgu01-identity-transcription.md`](hourly-cycles/hourly-20260625-0301-cycle3-rendered-dgu01-identity-transcription.md).

The checked source receipts explicitly supply

\[
\begin{aligned}
B_\omega&=\nabla_0+\epsilon^{-1}d_0\epsilon,\\
T_\omega&=\varpi-\epsilon^{-1}d_0\epsilon,
\end{aligned}
\]

so their algebraic sum is \(B_\omega+T_\omega=\nabla_0+\varpi\).
Identifying that sum with the separately appearing \(A_\omega\) is a
`DERIVED-RECONSTRUCTION`: no checked local source locator for
\(A_\omega=B_\omega+T_\omega\) was found in this swing.

The source also supplies the first-order bosonic action with
\(\frac12d_BT+\frac13[T,T]\), and the residual packet
\(\Upsilon^B+\Upsilon^F\). It does not supply, in the checked source, a
\(Y^{7,7}/U(64,64)\)-type to
\(Y^{9,5}/\operatorname{Sp}(32,32;\mathbb H)\) bundle map, a native Shiab,
the source-to-repository residual map, or a global field-space declaration
for every \(\epsilon,\varpi\) variation. Source resemblance is therefore
evidence for a comparator, not an identity.

## 1. Layer 0: required-arrow register; alignment unresolved

### 1.1 Global fork

| feature | source surface used here | active repository construction | disposition |
| --- | --- | --- | --- |
| ambient signature | \(Y^{7,7}\)-type presentation | \(Y^{9,5}=\operatorname{Met}_{3,1}(X)\) | different until mapped |
| gauge/real form | \(U(64,64)\)-type complex notation; \(U(128)\) appears in spoken Euclidean framing | \(\operatorname{Sp}(32,32;\mathbb H)\), right-\(\mathbb H\) native real form | typed fork |
| metric fibre | source presentation not used here to override the repo | \(\operatorname{Sym}^2T^*X\) | native branch retained |
| state pairing | source fermion pairing requires translation | indefinite native spinor Krein form | map absent |
| connection duality | source Hodge/adjoint notation | explicit \(G,\star_G,\kappa_{\mathfrak g}\) | constructed below |

No row below is `SAME`. The executable vocabulary is deliberately
restricted to `DIFFERENT`, `CANDIDATE`, and `ABSENT`; the compact table
expands the last label to `ABSENT IN CHECKED SOURCE` where useful. `SAME`
remains available only as a planted failure.

### 1.2 Object dictionary

The executable register carries domain, codomain, real form, degree/density,
pairing, gauge law, varied/fixed status, and formal-adjoint metadata for
every required arrow. The compact view is:

| proposed arrow | decisive mismatch or condition | Layer-0 result |
| --- | --- | --- |
| \(\epsilon\to\epsilon_{\rm IG}\) | source gauge coordinate versus varied native soldering/IG section; bundle and real-form map absent | `DIFFERENT` |
| \(\varpi\to\) full N1 tuple | one source one-form versus a heterogeneous tuple of independently varied fields | `DIFFERENT` |
| \(\varpi\to A-A_0\) | needs an intertwining source/native background split | `CANDIDATE` |
| \(B_\omega\to A\) | source is \(\epsilon\)-derived; repository \(A\) is independently varied | `DIFFERENT` |
| \(B_\omega\to\Gamma_{\rm conn}(\epsilon_{\rm IG})+U\) | one source coordinate would have to reproduce two independent repository fields | `CANDIDATE` |
| \(T_\omega\to U\) | same one-form shape, different derived/independent role | `DIFFERENT` |
| \(T_\omega\to\theta\) | requires the reconstructed \(A_\omega=B_\omega+T_\omega\) relation and a native bundle map | `CANDIDATE` |
| \(\nabla_0\to A_0\) | both are fixed reference connections, but the common bundle map is absent | `CANDIDATE` |
| \(A_\omega\to A-\lambda U\) | formal affine comparator; source dependency and native descent remain unproved | `CANDIDATE` |
| \(B_\omega\to\Gamma_{\rm conn}(\epsilon_{\rm IG})\) | preserves the displayed \(\epsilon\)-only dependency at \(\lambda=1\) | `CANDIDATE` |
| \(\epsilon^{-1}d_0\epsilon\to\Gamma_{\rm conn}+(1-\lambda)U-A_0\) | at \(\lambda\ne1\), the independent \(U\) is constrained to the same reference gauge orbit | `CANDIDATE` |
| \(\varpi\to A-\lambda U-A_0\) | formal affine translation; \((\delta A-\lambda\delta U)^!\) is required | `CANDIDATE` |
| \(\Upsilon^B\to\mathcal E_A\) | Euler outputs of different actions and field spaces | `DIFFERENT` |
| \(\Upsilon^B\to\mathcal E_A^{\rm bos,restricted}\) | needs native Shiab, real restriction, and complete pulled-back variation | `CANDIDATE` |
| \(\Upsilon^F_{\rm full}\to J_D\) or \(J_F\) | mixed spinor/adjoint Euler packet cannot be one connection current | `DIFFERENT` |
| \((\Upsilon^F)_{\rm ad}\to J_D\) | native operator and real-form map absent | `CANDIDATE` |
| \((\Upsilon^F)_{\rm ad}\to J_F\) | no \(12\)-form precursor or Green split appears in the checked source | `ABSENT IN CHECKED SOURCE` |
| \((\Upsilon^F)_{\rm ad}\to J_D+J_F\) | typed comparator; source identity and moving-full-\(Sp\) covariance unproved | `CANDIDATE` |
| \(\Upsilon^B+(\Upsilon^F)_{\rm ad}\to\mathcal E_A^{\rm source-shaped}\) | complete native action/residual translation is absent | `CANDIDATE` |
| \(\Xi\to\) Noether identity | a covariant residual is not an identically vanishing relation | `DIFFERENT` |
| \(\Xi-D_\omega\Upsilon\to R_{\rm gauge}^{!}\mathcal E\) | requires the complete native differential and field dictionary | `CANDIDATE` |
| source spin zero \(\to\) VEV/full-20 mass | gauge-potential component, zero-order operator, solved VEV, and physical mass are different objects | `DIFFERENT` |
| source musical \(\to\sharp_{\rm conn}\) | no checked source display of the native \(Sp(32,32;\mathbb H)\) map | `ABSENT IN CHECKED SOURCE` |

The source's true-spin-zero gauge-potential component, the stylized
\(R/4\) scalar, the future full-20 zero-order operator, its VEV, and a
physical mass eigenvalue remain separate objects. RB1 does not use any of
them to infer a count.

## 2. Formal affine comparison family

Because \(A_\omega=B_\omega+T_\omega\) is reconstructed rather than checked
source data, the comparison arrows are dashed and are not a pullback square.
For any \(\lambda\in\mathbb R\), define the formal ansatz

\[
\begin{aligned}
A_\lambda&=A-\lambda U,\\
B_\lambda&=\Gamma_{\rm conn}(\epsilon_{\rm IG})+(1-\lambda)U,\\
T_\omega&\dashmapsto\theta
=A-\Gamma_{\rm conn}(\epsilon_{\rm IG})-U,\\
\varpi&\dashmapsto A-\lambda U-A_0,\\
\epsilon^{-1}d_0\epsilon
&\dashmapsto\Gamma_{\rm conn}(\epsilon_{\rm IG})
 +(1-\lambda)U-A_0.
\end{aligned}
\]

Algebraically, \(A_\lambda-B_\lambda=\theta\) for every \(\lambda\).
That identity alone does not choose a source dictionary. The displayed
source definition makes \(B_\omega\) a function of \(\epsilon\) and the
reference connection only. Exactly \(\lambda=1\) preserves that dependency:

\[
A_1=A-U,\qquad B_1=\Gamma_{\rm conn}(\epsilon_{\rm IG}).
\]

Every \(\lambda\ne1\) is retained as a hostile control family, not counted
as another candidate. It charges a continuous parameter and requires
\(\Gamma_{\rm conn}+(1-\lambda)U-A_0\) to lie in the gauge orbit of the
reference connection. This is a reference-gauge-orbit constraint; it does
not say that the connection or its curvature is pure gauge. The earlier
connection-anchored presentation is merely the \(\lambda=0\) endpoint.

RB2 must determine whether \(\lambda\) disappears after a legitimate
Euler-system quotient or elimination. It may not fit \(\lambda\) to a
desired phenomenological answer.

## 3. N1 varied-primal Euler-root DAG and owner ledger

The varied primal roots are exactly

\[
\{A,U,P_{\rm IG},\epsilon_{\rm IG},s,Z,
c_g,\gamma,\bar\gamma,b_\gamma,\xi\}.
\]

\(A_0,\widehat Z,\Sigma\) and topology are supplied or comparator data;
\(\theta\) and \(g_s\) are derived. Every fixed coefficient, projector,
restriction, pushforward, Green operator, supplied field, and background
response appearing below also has a named owner in the executable ledger.
This is a complete dependency-accounting claim, not a claim that every
open map has been constructed. In particular, the open-BV paths are owners,
not instantiated Hom maps.

| action family / branch | actual varied roots and paths | order, adjoint, and Green ownership | grade |
| --- | --- | --- | --- |
| ambient Yang--Mills | \(A\to F_A\) | order 1; \(D_A^!\); \(G_{\rm YM}\) | exact weak |
| induced-YM parent | \(A\to D_AU\), \(U\to D_AU\), \(P_{\rm IG}\to\) linear/quadratic parent | \(A\) algebraic \(B_U^!\), no Green; \(U\) order 1 \(D_A^!\), Green; \(P_{\rm IG}\) algebraic \(1-Z_U^{-1}\) | exact |
| literal N1 bridge | \(A,U\to\theta\); \(\epsilon_{\rm IG}\to\Gamma_{\rm conn}\to\theta\); \(Z\to J_{\rm literal}\) | \(A/U/Z\) algebraic; \(\epsilon\) uses \((D_\epsilon\Gamma)^!\) and an unbuilt Green term if differential | exact except \(\epsilon\) branch |
| full-20 quadratic | \(A\to D_h\); \(A\to F_A\to V_{\rm GT}\); \(Z\to\) ordered operator | \(\widehat J_D\) algebraic, no \(A\)-Green; \(\widehat J_F=D_A^{\rm coad}\widehat Q_F\), with Green flux; \(Z\)-operator adjoint partial | exact for fixed geometry/projectors under \(A\)-variation; partial in \(Z\) |
| end selector \(V\) | \(Z\to v_{\rm tr}\)-profile selector | algebraic; the profile still needs an ambient extension on \(Y_{\rm end}\) | branch conditional |
| end selector \(B\) | \(A\to\widehat D\to\) auxiliary selector | order 1 auxiliary adjoint and Green term | comparator |
| end selector \(0\) | \(Z\to\) vectorlike selector | algebraic | exact |
| induced section gravity | \(s\to II_s,II_s^0\); \(s\to g_s=s^*G\to\) projector/norm/density | \(II\) branch is order 2/Jacobi; the literal induced-metric, Hodge, density, and norm branches are order 1 in \(s\), all with Green data | partial |
| SW defect | parallel \(s\)-paths through metric/Hodge/\(P_+\), moving \(A\)-pullback, moving \(Z\), algebraic or induced \(P_0\), and moving support | ordinary shape velocity is order 0; differentiated pushforward support is distribution order 1; Green and distributional owners are separate | partial |
| \(K\)-Yukawa defect | \(A-A_0\to\operatorname{res}^V_s\to c_\rho\), plus separate moving restriction, \(Z/P_0\), metric/pairing, and support paths | \(A\) algebraic; moving geometric branches own their transposes/Green terms; exact total-\(K\) algebraic adjoint | partial |
| \(C\)-Yukawa defect | same separate \(A,s,Z\) path classes | \(C\)-natural alternating transpose; reality completion partial | partial |
| spurion interface | same paths plus supplied \(\Sigma\) and its moving carrier | \(C\)-natural transpose; moving carrier/maps partial | partial |
| minimal BV, \(\tau=0\) skeleton | all eleven roots through BRST, Lie-derivative, and nonminimal owners | every root is differential order 1 because of Lie derivatives and owns a named Green form; open Hom maps and mixed-parity signs remain uninstantiated | partial |
| orientation cocycle \(T_9\) | no ordinary local varied root at fixed topology | no local Euler covector | exact at fixed topology |

### 3.1 Exact negative edges

The following plausible-looking arrows are false in the written packet:

\[
\begin{aligned}
\epsilon_{\rm IG}&\not\to D_AU,\\
A&\not\to J_{\rm literal}(Z),\\
\epsilon_{\rm IG}&\not\to\Gamma_{\rm tr}\to P_R\to D_h,\\
s&\not\to G_Y,\\
A&\not\to v_{\rm tr}\text{ in the physical end-selector branches}.
\end{aligned}
\]

In particular, the phrase in N3 that a later \(A\)-dependent bridge current
would create \(-(D_AJ)^!\theta\) was conditional. It is not a term in the
literal N1 bridge. The new \(J_D\) and \(J_D+J_F\) architectures have their
own explicitly declared dependencies and must not be back-projected onto
\(J_{\rm literal}\). The absent
\(\epsilon_{\rm IG}\to\Gamma_{\rm tr}\) edge is a statement about the
fixed-plane RB2 arena. A future moving-full-\(Sp\) lift may add a soldering
path, but it cannot be smuggled into this graph.

### 3.2 Aliases that cannot be added twice

The executable graph rejects simultaneous use of:

- the \(P_{\rm IG}\) parent and its eliminated \(Z_U\lVert D_AU\rVert^2/2\)
  presentation;
- the direct \(II\)-action and its Gauss rewrite;
- \(F_{s^*A}\) and \(s^*F_A\) as two curvature contributions;
- the full-20 quadratic and one displayed vertical expansion as separate
  action terms;
- the \(K\)- and \(C\)-Yukawa branches as though one repeated odd field used
  both without a branch declaration;
- rival end selectors \(V,B,0\) simultaneously; and
- intrinsic moving-pullback and ambient differentiated-pushforward
  descriptions of the same shape derivative.

### 3.3 Moving-section chain

For section velocity \(V=\dot s\),

\[
\delta_s(s^*A)
=s^*(\iota_VF_A)+D_{A_X}(s^*\iota_VA).
\]

Both terms are required. A rotating vertical space also gives
\(D_sP_0\ne0\) and \(D_s\operatorname{res}^V_s\ne0\) generically. Hence

\[
D_sc_\rho
\]

is one composite derivative through the moving restriction and intrinsic
Clifford/gauge maps; it is not a second independent current. A fixed
Spin/stabilizer control can make the projector derivative vanish, but that
result cannot be assigned to a moving full-\(Sp\) orbit.

There is an additional fork that must stay visible. RB1 uses the literal
induced metric \(g_s=s^*G\), so metric, Hodge, density, and norm variations
depend on \(ds\) and are first order in the section variation. A
tautologically supplied four-metric would be a different architecture. The
ambient \(P_0\) branch can vary algebraically as the vertical plane rotates;
only an induced-metric reconstruction of \(P_0\) inherits the first-order
metric path.

Finally, moving pullback and differentiating the support pushforward are
two coordinate presentations of one shape derivative. The ledger keeps
their ordinary velocity order \(0\) distinct from the pushforward's
distribution order \(1\), and a planted simultaneous insertion produces
the expected factor of two.

## 4. Fixed-geometry/fixed-projector full-20 \(A\)-variation

Let \(a=\delta A\) and hold the geometric soldering, Clifford trace,
projectors, and \(h\)-coefficients fixed on this local variation stratum.
The allowed symmetry is the corresponding Spin/stabilizer. Define

\[
C_{V\otimes S}(a)
=(1_V\otimes c^\mu)\rho_{V\otimes S}(a_\mu),
\qquad
\rho_{V\otimes S}=\rho_V\otimes1+1\otimes\rho_S.
\]

Then

\[
\begin{aligned}
\dot D_S[a]&=c\!\cdot\!\rho_S(a),\\
\dot D_V[a]&=C_{V\otimes S}(a),\\
\dot T[a]&=P_R\rho(a),\\
\dot\delta_R[a]&=-\operatorname{tr}_G\rho(a),\\
\dot Q[a]&=P_R\dot D_V[a]P_R.
\end{aligned}
\]

Inserted into the nine displayed \(S/I/R\) blocks:

| block | \(\dot D_h[a]\) |
| --- | --- |
| \(SS\) | \(h_a\,c\!\cdot\!\rho_S(a)\) |
| \(SI\) | \(h_{SI}\,c\!\cdot\!\rho_S(a)\Gamma_{\rm tr}\) |
| \(SR\) | \(-h_{SR}\operatorname{tr}_G\rho(a)\) |
| \(IS\) | \(14\overline{h}_{SI}\,j\,c\!\cdot\!\rho_S(a)\) |
| \(II\) | \(h_dP_IC_{V\otimes S}(a)P_I\) |
| \(IR\) | \(h_{IR}P_IC_{V\otimes S}(a)P_R\) |
| \(RS\) | \(-\overline h_{SR}P_R\rho(a)\) |
| \(RI\) | \(\overline h_{IR}P_RC_{V\otimes S}(a)P_I\) |
| \(RR\) | \(P_RC_{V\otimes S}(a)P_R\) |

Define density-dual forms by

\[
a\wedge\widehat J_D
=\frac12\operatorname{Re}
[Z,K_E\dot D_h[a]Z]\,\mu_G,
\]

and

\[
b\wedge\widehat Q_F
=\frac{\lambda_F}{2}\operatorname{Re}
\left[
Z,
K_E P_R
\left(\operatorname{contr}b-\frac16\operatorname{wedge}b\right)
P_RZ
\right]\mu_G.
\]

Their carriers are

\[
\widehat J_D\in\Omega^{13}(Y,\operatorname{ad}^*P),
\qquad
\widehat Q_F\in\Omega^{12}(Y,\operatorname{ad}^*P).
\]

With the frozen wedge convention and \(D_A^{\rm coad}\) denoting the
covariant derivative on the density-dual adjoint bundle,

\[
\begin{aligned}
\delta_AS_{20}[a]
&=\int_Ya\wedge\widehat J_D
  +\int_YD_Aa\wedge\widehat Q_F\\
&=\int_Ya\wedge
  \left(\widehat J_D+D_A^{\rm coad}\widehat Q_F\right)
  +\int_{\partial Y}\iota^*(a\wedge\widehat Q_F).
\end{aligned}
\]

Thus

\[
\widehat J_F
=D_A^{\rm coad}\widehat Q_F
\in\Omega^{13}(Y,\operatorname{ad}^*P).
\]

If

\[
q_F=\sharp_2\widehat Q_F,\qquad
j_F=\sharp_1\widehat J_F,
\]

then the primal-form statement is

\[
j_F=D_A^\dagger q_F.
\]

Because \(*_G^2=-1\) on degrees \(2\) and \(12\),
\(\sharp_2=-*_G\kappa_{\mathfrak g}^{\sharp}\) in the convention used here.
The sign is not optional. A genuine \(14\)-dimensional exterior-algebra
fixture checks

\[
d(a_1\wedge Q_{12})
=D_Aa_1\wedge Q_{12}
-a_1\wedge D_A^{\rm coad}Q_{12},
\]

and therefore

\[
\int_YD_Aa\wedge Q
=\int_{\partial Y}\iota^*(a\wedge Q)
{}+\int_Ya\wedge D_A^{\rm coad}Q.
\]

\(J_D\) is algebraic in the \(A\)-variation and owns no \(A\)-Green term.
\(Q_F\) is the \(12\)-form precursor; \(J_F\) owns both the differential
adjoint and the boundary flux. Dropping that flux changes the weak equation.

The executable fixture also finite-differences all nine \(S/I/R\) blocks,
including controls that catch the \(SR\) sign and a falsely zero \(RR\)
block. This is a fixed-geometry, fixed-projector \(A\)-variation on the
Spin/stabilizer arena. Extending it to a moving full-\(Sp\) soldering is
held to RB3.

This is a local/compact-core statement. A global end domain, BFV boundary
completion, and the ordered \(Z\)-operator adjoint remain RB8 work.

## 5. Native connection-current pseudo-musical

Let

\[
\mathfrak g=\mathfrak{sp}(32,32;\mathbb H)
\]

and choose a nonzero nondegenerate real
\(\operatorname{Ad}\)-invariant symmetric form
\(\kappa_{\mathfrak g}\). A normalized real Killing form is one available
choice; its normalization remains a declared coupling convention.

For
\(j\in\Omega^1(Y,\operatorname{ad}P)\), define

\[
\widehat{\flat_{\rm conn}j}
=\kappa_{\mathfrak g}^{\flat}(*_Gj)
\in\Omega^{13}(Y,\operatorname{ad}^*P).
\]

For
\(\widehat\eta\in\Omega^{13}(Y,\operatorname{ad}^*P)\), define

\[
\sharp_{\rm conn}\widehat\eta
=*_G^{-1}\kappa_{\mathfrak g}^{\sharp}(\widehat\eta).
\]

In dimension \(14\) with five negative directions,

\[
*_G^2\big|_{\Omega^k}
=(-1)^{k(14-k)+5}.
\]

Therefore \(*^2=+1\) on degrees \(1,13\) and \(*^2=-1\) on degrees
\(2,12\). The connection musical has no extra Hodge sign; the
\(Q_F\)-to-two-form conversion does.

In a coordinate-density convention with
\(\rho_G=|\det G|^{1/2}\),

\[
(\flat_{\rm conn}j)_B{}^\nu
=\rho_G\,G^{\nu\mu}\kappa_{BA}j_\mu{}^A,
\]

\[
(\sharp_{\rm conn}\eta)_\mu{}^A
=\rho_G^{-1}G_{\mu\nu}
(\kappa^{-1})^{AB}\eta_B{}^\nu.
\]

The defining identity is

\[
\mathcal B_{\rm conn}
\left(a,\sharp_{\rm conn}\widehat\eta\right)
=\widehat\eta[a].
\]

The map is real and gauge-equivariant because \(G\) is geometric and
\(\kappa_{\mathfrak g}\) is invariant. It is an algebraic bundle
isomorphism pointwise and a weak compact-core identification after
integration. It is not:

- a positive Riesz theorem;
- a global Hilbert-space isomorphism;
- a boundedness, closure, or Fredholm result;
- the spinor Krein operator \(K_E\); or
- a license to replace the native group by \(U(128)\).

This instantiates the pseudo-musical placeholder already identified in N3;
it is not a newly discovered physical structure. The executable inversion
and pairing fixture uses a dimension-reduced indefinite matrix control.
It tests the algebra and the signature dependence, but it is not a native
Lie-algebra computation of
\(\mathfrak{sp}(32,32;\mathbb H)\). The native metadata check instead
requires the quaternionic real form and rejects a positive \(U(128)\)
substitution structurally.

This closes the local carrier mismatch that RB1 was assigned to close.

## 6. Internal-gauge and diffeomorphism Ward scopes

The two Noether contracts must not be conflated. On the frozen
Spin/stabilizer arena, the internal \(Sp\) identity has the schematic
off-shell form

\[
\begin{aligned}
D_A^\top\mathcal E_A
{}+{\rm ad}_U^\top\mathcal E_U
{}+{\rm ad}_{P_{\rm IG}}^\top\mathcal E_{P_{\rm IG}}
{}+\rho_\epsilon^\top\mathcal E_\epsilon
{}+\rho_Z^\top\mathcal E_Z
{}+(\text{ghost/nonminimal terms})\\
{}+R_{A_0}^\top\frac{\delta S}{\delta A_0}
{}+R_\Sigma^\top\frac{\delta S}{\delta\Sigma}
=\text{boundary flux or }0.
\end{aligned}
\]

The internally neutral section \(s\) and Diff ghost \(\xi\) do not enter
that contraction. Backgrounds \(A_0\) and \(\Sigma\) are fixed when taking
ordinary Euler variations but transform in the Ward test, so their response
terms remain.

The Diff identity is instead

\[
\sum_{\Phi\in
\{A,U,P_{\rm IG},\epsilon_{\rm IG},s,Z,c_g,\gamma,\bar\gamma,b_\gamma,\xi\}}
(\mathcal L_\xi\Phi)^\top\mathcal E_\Phi
+R_{A_0,\xi}^\top\frac{\delta S}{\delta A_0}
+R_{\Sigma,\xi}^\top\frac{\delta S}{\delta\Sigma}
=\text{boundary flux or }0.
\]

Both contracts are off shell, graded, and boundary-aware. Their active
terms are architecture-specific; the owner sets above prevent an omitted
field from being silently mistaken for a proved cancellation. Neither
contract implies \(D_A^\top J_D=0\),
\(D_A^\top(J_D+J_F)=0\), or
\(\Xi=D_\omega\Upsilon\) by itself.

## 7. Three RB2 candidates plus one control family

The literal Clifford-vector bridge is excluded because it has the wrong
carrier. The independent-soldered-current branch is excluded because no
such map was constructed; it may re-enter only through a preregistered
geometry-return mutation.

| record | role | explicit action schema | retained / replaced | RB1 grade |
| --- | --- | --- | --- | --- |
| `N1_JD_bridge` | candidate | \(I_{\rm N1}-I_{\rm literal\ bridge}+\frac1{2\kappa}\lVert\theta\rVert^2-\langle\theta,\sharp_{\rm conn}J_D\rangle\) | retains ambient YM, \(P_{\rm IG}\) parent, full-20, defects, gravity, BV, \(T_9\); replaces only the literal bridge | `LOCAL-WEAK-ADMISSIBLE` |
| `N1_total_current_bridge` | candidate | \(I_{\rm N1}-I_{\rm literal\ bridge}+\frac1{2\kappa}\lVert\theta\rVert^2-\langle\theta,\sharp_{\rm conn}(J_D+J_F)\rangle\) | same N1 terms; the \(Q_F/J_F\) Green flux travels with the bridge | `LOCAL-BOUNDARY-CONDITIONAL` |
| `source_reference_lambda1` | candidate | \(I^{\rm native}_{B1}[\theta,B_1,\odot_c]+S_{20}[A_1,Z]+I_{s_*(\mathrm{defects+gravity})}+I_{\rm BV,source}+T_9\) | \(A_1=A-U,\ B_1=\Gamma_{\rm conn}\); replaces ambient YM, \(P_{\rm IG}\) parent, and literal bridge | `NATIVE-SHIAB-SELECTOR-AND-REAL-FORM-CONDITIONAL` |
| `source_orbit_constrained_lambda_not1` | control family | the same finite source-shaped schema at \(A_\lambda,B_\lambda\) | \(\lambda\in\mathbb R\setminus\{1\}\) is a charged one-dimensional debit plus the reference-orbit constraint | `REFERENCE-GAUGE-ORBIT-CONSTRAINT-CONTROL` |

Here \(I^{\rm native}_{B1}\) carries the source eddy coefficients
\((\frac12,\frac13)\) explicitly:

\[
I^{\rm native}_{B1}[\theta,B_\lambda,\odot_c]
=\int_Y
\left\langle
\theta,\,
*_G\odot_c\!\left(
F_{B_\lambda}
+\frac12D_{B_\lambda}\theta
+\frac13[\theta,\theta]
\right)
+\frac{\kappa_1}{2}*_G\theta
\right\rangle_{\kappa_{\mathfrak g}}.
\]

The product \(\odot_c\) ranges over the
four-real-dimensional native equivariant Shiab family already proved in
canon. The source's written point is preregistered, but no checked receipt
forces a unique selector \(c\). Likewise, \(S_{20}[A_\lambda,Z]\) is a typed
repository fermion comparator, not an asserted identity with the source
fermion action.

The source-shaped schema retains the end selector, moving-section
gravity, SW and \(K/C\) defect terms, spurion interface, minimal BV
extension, action-domain pushforward infrastructure, and \(T_9\). P3 stays
external to the action. The three candidates and the control record freeze
the Spin/stabilizer arena:
a moving-full-\(Sp\) soldering lift is held to RB3. All also hold out
zero-order selection, VEV, stationarity, a curved solution, CME, global
domain, physical reduction and mass, P3 pushforward, index, and count.

The control family is not a fourth scientific candidate. Killing it is
evidence that the dependency-preserving \(\lambda=1\) point is meaningful;
it is not counted as candidate attrition.

## 8. RB2 experiment and efficient ordering

RB2 should use one symbolic variation kernel and proceed in this order:

1. freeze the candidate/control census and Spin/stabilizer arena;
2. test the affine/orbit map, native real restriction, and Shiab selector
   before expensive action comparison;
3. freeze each action's term ledger, five-leg owners, alias exclusions, and
   held-out wall;
4. instantiate the three candidate actions and the orbit-constrained
   control family term by term;
5. compute the shared \(A,U,P_{\rm IG},\epsilon_{\rm IG}\) variations and
   every formal adjoint, Green form, and boundary flux;
6. derive the separate internal-\(Sp\) and Diff Ward contractions;
7. compare complete Euler systems after only proved parent elimination or
   quotienting, with source coefficient and connection controls; and
8. retain an equivalence class if genuinely indistinguishable, otherwise
   select by preregistered failures or return the minimal inconsistent
   equations.

The first cheap kill is the native real-form/Shiab gate on the
source-shaped candidate. The next is its gauge contraction. The two N1
bridges then reuse the built \(J_D/\widehat Q_F/\widehat J_F\) kernel; the
total-current branch must carry its boundary condition rather than winning
by an algebraic cancellation alone.

Kill only the failing record:

- wrong primal/dual carrier kills that bridge;
- failure of the source real-form/Shiab map kills the source-shaped
  candidate, not the N1 pair;
- failure of the reference-orbit constraint kills the
  \(\lambda\ne1\) control family, not the \(\lambda=1\) candidate;
- duplicate bridge/current insertion kills that presentation;
- an uncancelled Ward contraction kills the corresponding Euler system;
  and
- if all three candidates die, emit the minimal inconsistent equations and
  a bounded adjacent connection/action geometry, then re-enter RB1.

## 9. Five-leg constraint carry

Every architecture record carries concrete owners for all five legs:

| campaign leg | common owner and architecture-specific condition |
| --- | --- |
| Standard Model / Yukawa | separate \(K/C\) defects plus spurion interface; no universal mass or free flavor matrix |
| quantum / Krein / BV | native Krein pairing and minimal BV extension; source-shaped branch still owes fermion/source translation |
| gravity / dark energy / cosmology | action-domain \(s_!\) and \(s_*\) infrastructure and induced-section gravity; VEV remains held out |
| index / count | \(T_9\) plus external P3 relative-\(KO\) comparator; no decomposition or action choice is read as generations |
| UV / causality | full-20 curvature vertex, Green data, and future common domain; source-shaped translation remains conditional |

The external-datum ledger is unchanged and contains only:

\[
\text{one P1/P2 orientation line}
\quad+\quad
P3=(\nu,\widehat e_n,n),
\]

where P3 is the separate target-blind relative-\(KO\) comparator. The
\(s_!\), \(s_*\) weld and stratified measure are action-domain
infrastructure, not another external datum. RB1 neither merges these
objects nor adds a new datum.

## 10. Layer-0 plus L1--L7 disposition

Layer 0 moved substantially: the current, connection, residual, gamma,
metric, selector, and count homonyms are now typed. The seven-axis
substrate candidate did not change. RB1 changes neither the smooth
gimmel-bundle substrate class, observer class, bulk/defect pairing, causal
class, emergence claim, coordination loop, nor indefinite state-space
signature. It also makes no new chirality bridge claim. Re-running L1--L7
as though an action-internal musical were a new substrate candidate would
be a category error.

## Controls

The executable contract requires rejection of:

- a source formula promoted to `SAME` by authority;
- omission of any required Layer-0 arrow or adjoint owner;
- promotion of the reconstructed \(A_\omega=B_\omega+T_\omega\) relation
  to checked source fact;
- hiding the affine \(\lambda\)-continuum behind two chosen endpoints;
- silent \(Y^{7,7}/U(64,64)\) or \(U(128)\) substitution;
- direct \(A\to J_{\rm literal}\);
- \(\epsilon_{\rm IG}\to D_AU\);
- \(\epsilon_{\rm IG}\to\Gamma_{\rm tr}\);
- \(s\to G_Y\);
- an independent \(g_s\) Euler root;
- form pullback substituted for vertical coefficient restriction;
- a dropped or invented Green term;
- degree \(13\) assigned to \(Q_F\);
- the wrong \(2/12\)-form Hodge sign or graded integration-by-parts sign;
- an \(SR\) sign error or falsely zero \(RR\) full-20 derivative;
- a positive optimizer/majorant metric substituted for
  \(G/\kappa_{\mathfrak g}\);
- the spinor Krein form used as the connection musical;
- a duplicate typed DAG path or mutation of order, adjoint, or Green owner;
- duplicate parent/Gauss/curvature/selector/shape presentations;
- dropped moving-pullback, moving-projector, or moving-restriction terms;
- a BV root stripped of its Lie-derivative Green owner;
- a literal Clifford-vector or unbuilt soldered bridge admitted to RB2;
- an architecture missing a five-leg owner, held-out item, source-action
  term, frozen symmetry, or candidate/control role;
- conflation or incompleteness of the internal-\(Sp\) and Diff Ward
  contracts; and
- any early P3, index, or count inference.

All controls pass in
[`rb1_source_repo_current_musical_probe.py`](../tests/channel-swings/rb1_source_repo_current_musical_probe.py).

## Honest endpoint

RB1 made three real pieces of the build:

1. a complete required-arrow register that refuses false source identities
   and exposes the formal affine \(\lambda\)-family;
2. a varied-root DAG plus coefficient/map/background owner and Green ledger
   that removes phantom and duplicate Euler terms; and
3. the native indefinite connection-current musical needed to place
   \(J_D\) and \(J_D+J_F\) in the distortion arena.

It has therefore earned `RB1-GO`. It has not selected an action. The next
scientific question is now precise: which, if any, of the three frozen
candidates produces a real, gauge-compatible, nonduplicated Euler system
when the source eddy and every moving-\(\epsilon\) and boundary term are
included, and does the \(\lambda\ne1\) control family fail for the predicted
dependency/orbit reason?

## Append-only RB3 return — \(A_0\)-induced moving-\(\Gamma\) candidate

RB3 constructed the moving Clifford-plane orbit and found that the frozen
RB1 owner

\[
\Gamma_{\rm conn}(\epsilon_{\rm IG})
\]

does not determine a connection once the orbit moves. A bare orbit
representative does not define a lift-independent connection. Using the
already supplied \(A_0\) and the reductive
\(\mathfrak{sp}=\mathfrak{spin}(9,5)\oplus\mathfrak m\) split gives

\[
B_0=g^{-1}A_0g+g^{-1}dg,
\]

\[
\Gamma_{\epsilon,A_0}
=g\operatorname{pr}_{\mathfrak{spin}}(B_0)g^{-1}
-dg\,g^{-1}
=A_0-g\operatorname{pr}_{\mathfrak m}(B_0)g^{-1}.
\]

This gives one economical candidate overlay:

```text
Gamma_conn^A0
  inputs: epsilon_IG, d epsilon_IG, A0, pr_spin
  epsilon order: 1
  Green owner: G_Gamma_REQUIRED_UNBUILT
  A0 role: homogeneous background-response proxy
  identity with abstract N1 Gamma_conn: UNRESOLVED
  global grade: conditional on Spin(9,5) reduction
```

This is an append-only candidate branch. The original owner/path record
remains the frozen RB1 slice and its digest is not rewritten. A
Levi--Civita/spin connection constructed from the soldering jet and an
independent \(H\)-connection remain rival branches. The executable probe
checks the returned \(A_0\) overlay only at homogeneous background-response
grade and continues to keep
\(\Gamma_{\rm conn}\) distinct from the Clifford trace
\(\Gamma_{\rm tr}:V\otimes S\to S\).

The candidate adds no local external datum because \(A_0\) was already
supplied. Globally, either the \(Sp(32,32;\mathbb H)\) bundle is constructed
by extension of the inherited spin frame bundle or reduction existence is
an obstruction of the supplied bundle. A separate reduction sector is
charged only if the variational or boundary problem independently fixes
one.
