---
title: "Pre-registration: the ORIENTED-SHARD-CYCLE swing — is σ the orientation class of a canonical observerse 1-cycle, is record-issuance a coherent (Gorard/Wolfram) rewriting, and does the model leave a falsifiable observable shadow?"
status: preregistration
doc_type: scoping_decision_tree
created: 2026-07-21
directed_by: "Joe direct chat, 2026-07-21 (orchestrate a swing at the falsifiable viable questions of the shard-cycle model; add the dynamical issuance rule + Gorard/Wolfram CA layer)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
external_actions: none
kill_conditions_declared_before_computation: true
---

# The oriented-shard-cycle swing

## The model under test (Joe direct chat, co-developed 2026-07-21) — all [PROPOSED]

The three shards — **subjective / intersubjective / objective** (observer / a
consensus-region "becoming hard to undo" / the frame-independent global, itself
possibly galaxy-LOCAL with a bigger outside) — form an **oriented 1-cycle**
(subjective→intersubjective→objective→subjective). Claims:
- **σ (the Z/2 bit) = the ORIENTATION of that cycle** (Z/2 = the two orientations
  of S¹ — which would EXPLAIN why the bit is Z/2). σ is the glue/transmission
  between shards; sign(K_S) is its value. It is a **circle, not a directed acyclic
  graph** — third-person-directed, but with NO first-person backward causality.
- The cycle **closes through the q<0 / null-K_S stratum** (Prong 0's ~8%): there
  K_S is null ⇒ σ undefined ⇒ no readable direction at the join ⇒ no backward-
  causal edge; and the operator is non-constructible ⇒ the closure is invisible
  ("never seen or known").
- First-person = the **orientation-quotient** (σ-blind ⇒ sees the directed Z/3
  cycle as the unoriented S₃ generation trit; τ = the graded pieces).
- The external input sets BOTH the **initial orientation** AND the **record-
  issuance rule** ("the dynamical next states") — i.e. an externally-seeded
  update law; TI's issuance domain. Two externalities: inner closure (σ, through
  the null stratum) + outer boundary (the bigger outside).

## Discipline (binding — three clean fits died on hostile verify TODAY)
Every prong returns a definite VERDICT; planted controls mandatory; any positive
("constructed"/"coherent") is HOSTILE-VERIFIED before banking; everything stays
[PROPOSED] until verified; **the numerology gate: if we cannot CONSTRUCT the object
from canonical GU structure, we say "numerology" and drop it**; no claim/canon/
verdict/posture moves.

## Prong 1 — GEOMETRY (make-or-break): is σ a canonical circle's orientation class?
The one-bit dossier already types σ as a **holonomy over F = GL(4,ℝ)/O(3,1)**
(σ ↔ record-direction co-flip, holonomy over F). Test whether that holonomy is
literally the **orientation class of a canonical 1-cycle**: compute π₁(F) and the
relevant Z/2 (orientation / first Stiefel–Whitney-type) class; is the K_S-sign
monodromy `U_h K_S U_h⁻¹ = −K_S` the orientation double-cover of a CANONICAL loop
(not a hand-picked one)? Does that loop necessarily pass through the q<0 null-K_S
stratum (Prong 0)?
- **G-CONSTRUCTED** (win, hostile-verify): σ = orientation class of a canonical
  observerse 1-cycle that closes through the null stratum. Explains the "2".
- **G-PARTIAL**: the holonomy exists but is not an orientation class, or the loop
  need not pass through the null stratum — state which.
- **G-NUMEROLOGY** (kill): no canonical 1-cycle carries σ as its orientation ⇒
  σ-as-circle-orientation is FALSIFIED; say so plainly.
- Control: a planted "any Z/2 bit is some circle's orientation" MUST be rejected —
  the cycle must be canonical and its orientation class computed, not asserted.

## Prong 2 — DYNAMICS (Gorard/Wolfram/CA): does the issuance logic close coherently?
Model "the continuing issuance of records / the shared dynamical next states" as a
discrete **hypergraph-rewriting / Wolfram-model** system (read
`lab/sources/transcripts/toe-gorard-wolfram-physics.md`,
`lab/sources/gorard-grok-source-pack-2026-07-20.md`). Test: can a **causal-invariant**
rewriting rule, externally seeded (orientation IC + update law), generate the
oriented shard-cycle **without a backward-causality paradox**? Does the structural
parallel hold — **third-person = the multiway/causal graph, first-person = a single
branch, σ-blindness = invisibility of the branchial orientation**?
- **D-COHERENT** (hostile-verify): a causal-invariant rule produces the oriented
  cycle and the first/third split maps to branch/multiway; the logic closes.
- **D-INCOHERENT** (kill): no consistent causal-invariant rule produces it, or it
  requires a paradox ⇒ the dynamical layer fails.
- **D-ANALOGY**: the CA/multiway picture is illustrative but not structurally
  forced by GU — state the disanalogy (do NOT inflate a rhyme to a mechanism).
- Control: a planted NON-causal-invariant rule must be caught (it should produce
  the paradox / branch-divergence the real rule avoids).

## Prong 3 — FALSIFIER (observable contact): does it leave a shadow, and does it match?
The model is science only if it leaves a third-person shadow that could fail.
Two tests: (a) the three graded pieces' **α-even magnitude ordering** = the record-
issuance RATES — does it match the generation **mass hierarchy** (ordered, identical
quantum numbers)? (b) does the **seed↔realization consistency** (per-observer bit =
external seed) forbid anything (a nonvacuous constraint)?
- **F-FALSIFIABLE-CONSISTENT**: leaves a nonvacuous shadow AND is compatible with
  the observed mass pattern ⇒ the model is science and survives this test.
- **F-FALSIFIED** (kill): the forced magnitude ordering is incompatible with the
  generation masses ⇒ the model is wrong.
- **F-UNFALSIFIABLE** (kill): the shadow is provably empty ⇒ retire the reach-around
  as unfalsifiable, honestly.
- Control: exhibit something the shadow FORBIDS, or concede it forbids nothing.

## Synthesis
Prong 1 is the make-or-break (no canonical cycle ⇒ the geometric story is
numerology, and Prongs 2–3 become at most suggestive). Prong 2 tests whether the
dynamical/issuance layer is coherent independent of Prong 1. Prong 3 decides whether
the whole thing is falsifiable at all. Run in parallel; synthesize; hostile-verify
every positive.
