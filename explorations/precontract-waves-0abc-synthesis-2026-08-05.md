---
artifact_type: campaign_synthesis
created: 2026-08-05
title: "Pre-contract Waves 0B/0A/0C: point Build at the observed equation and make composition measurable"
route_disposition: COMPOSE_PLUS_SOURCE_TEMPORARILY_LEAD__BUILD_RESUMES_ON_TYPED_FULL_DOMAIN_OBSERVED_TARGET
fork_assumed: none
search_space_dim: "78 frozen conditional targets; 83 real parameters, at least 19 function-valued slots and 10 discrete forks before quotienting"
free_object_delta: 0
residue_touched:
  - "LT-GR1b:T2"
  - "LT-SM3b:T3"
canon_verdict_change: none
---

# Pre-contract Waves 0B, 0A and 0C: synthesis

## Outcome first

The expensive construction channel was solving a real ambient problem, but it
was not yet connected to the four-dimensional equation that the conditional
build needs. Wave 0B proved that the vertical Frobenius trace reversal, the
ambient fourteen-dimensional Einstein contraction and the observed
four-dimensional Einstein equation are three different typed operations. The
vertical metric is already active; the ambient selector kill remains exact;
and naive restriction to four dimensions fails by incompatible exact factors
`26` and `6`.

Wave 0C then found the constructive opening. The gamma-traceless T3 spinor
vertex is exactly the same projective line as the canon spinor reconstruction,
but neither is Eric's written adjoint-valued Shiab. On genuine algebraic
Riemann curvature, trace reversal followed by `-2` is an exact adapter from the
spinor Ricci response `(13,1)` to the selected adjoint response `(156,-2)`.
Extending that adapter to the full action domain, then composing it with the
observed equation receiver required by 0B, is now the sharpest next target.

This changes ordering, not the program objective: **Compose plus Source leads
briefly so that Build receives the right object; Build then resumes.** No
fixed resource percentages are ratified here.

## The first honest progress surface

Wave 0A froze a v0.1 denominator from the three existing conditional-build
axes. There are 86 source rows, eight explicit aliases and 78 canonical
targets:

```text
Ledger v0.1 — 78/78 frozen target rows typed
32 SAME · 18 DIFFERS · 22 NEEDS · 6 OVER-DETERMINED
Residue — 83 continuous real + >=19 function-valued + 10 open discrete forks
Quotients ranked: 0
```

The `78/78` is mapping coverage, not completion of GU. Construction converges
when residue or distance falls; mapping coverage without residue movement is
visible circling rather than manufactured progress.

Each row carries a verdict, an extensible reason kind, the one condition that
would improve its class, a revival trigger, evidence and a mapping grade.
Unknown cases create a new kind instead of being forced into a misleading
bucket. A reader can therefore distinguish a one-bit decision from a
proven-unsupplyable route, or an imported agreement from a derived one,
without reopening every source artifact.

## Six adverse rows, not one adverse bucket

The exact denominator confirms six over-determined rows: one representation,
three Lagrangian and two anomaly/consistency requirements. The finder may only
escalate them. Independent review has so far adjudicated two:

- `LT-GR1b` is a scope error if the exact ambient-ansatz kill is advertised as
  observed gravity; the local ambient result itself is retained.
- `LT-SM3b` is a stale-premise conflict because the canon spinor
  reconstruction was treated as Eric's written adjoint Shiab. The exact
  failure of the pure spinor contraction remains; its attribution changes.

The other four remain escalated, not explained away.

## Proposed later operating contract—not ratified in this Run

Name channels by function rather than embodiment:

1. **Build:** construct new objects against a predeclared typed ledger row.
2. **Compose:** own the denominator, typings, residue, contradiction discovery
   and cross-composition of results.
3. **Source:** return `SOURCE-CONFIRMS`, `SOURCE-CORRECTS` or `SOURCE-SILENT`
   before any decisive Eric-lane disposition.
4. **Verify:** attack new or high-fanout claims; do not spend capacity replaying
   unchanged packets merely because Build is occupied.

Dispatch is condition-based. Every Build or Compose wave names its ledger rows
and reports their after-state. Compose runs after three material Build outputs,
or immediately when a verdict, residue count, fork or high-fanout premise
changes. A wave that changes no row must say `ledger_row_changes: none` and
why. Over-determined rows receive an independent owner before propagation.

## Efficient next gates

1. Construct the full-domain Shiab action adapter and the observed equation
   receiver as one commuting variational square.
2. Compute the native `R^2` running sign for `LT-GR4`.
3. Propagate the resolved real-Clifford fork through the anomaly surface for
   `AC-G1`.
4. Decide the high-fanout norm/stabilizer rows that move several targets at
   once.
5. Construct or exclude a non-inflow count bridge without rediscovering P3.
6. Append genuinely missing targets into ledger v0.2 and start ranking the
   currently unranked quotients.

P1/P2/P3, canon status, public posture and observed physics are unchanged.

## Subsequent ratification — 2026-08-05

Joe subsequently ratified the proposed functional-channel method in direct
chat and requested that it become durable before the next scientific wave.
The ratified human and machine contracts are:

- `lab/methods/research-evidence-contract-v1.0.md`;
- `lab/methods/research-evidence-contract-v1.0.json`.

The historical statement above that the contract was not ratified **in this
Run** remains true and is preserved for provenance. Ratification does not alter
0A/0B/0C's scientific results, the six adverse rows, P1/P2/P3, canon or public
posture. It adds the functional dispatch, ledger freshness, independent
adjudication and two-sided hostile-review rules, plus the binding
`GU-COSMO-DYNAMIC-01` Layer-0 split hold on the Einstein/cosmological mapping.
