---
title: "PW2F-R2B2B2I2 resumable second-uncovered size-six full-evaluator certificate"
status: reconstruction
doc_type: exploration
updated_at: "2026-08-05"
---

# PW2F-R2B2B2I2 resumable second-uncovered size-six certificate

## Outcome

The append-only finite S3 evaluator chain now covers the lexicographically
first canonical representative outside the durable `4/380` predecessor:

```text
SECOND_UNCOVERED_SIZE6_CERTIFIED__DURABLE_5_OF_380__UNIVERSAL_375_OPEN__DENSE_HELDOUTS_0_OF_6_EXECUTED
```

The selected representative is `(owners 0,0; quartic point 0,0,2,2)`. Its
complete orbit has six labels. All twelve directed generator edges and every
mixed evaluator layer pass exactly. Durable representative coverage is now
`5/380`; the other `375` remain open. The six frozen dense heldout inputs are
still `0/6` executed, the reduction engine remains unpromoted, and the
unconditional 1,925-cell fallback stays live.

## Layer 0 and provenance

The public `(7,7)` source presentation remains distinct from the active
trace-reversed `(9,5)` repository reconstruction. Canonical-lattice coverage
is not dense-heldout confirmation; freezing a heldout input is not executing
it; one orbit is not universal coverage; and `I1 A4` remains a separate bank
from `I2B C4`. The finite evaluator and reduction are repository-derived and
public-source silent at this scope.

The successor byte-pins both the durable predecessor probe and the exact
ledger bytes at owner revision `40e93a9`. Its covered set is reconstructed
from that committed ledger before selection. The new ledger entry retains the
predecessor revision and SHA-256, so the chain is append-only and auditable.

## Exact selected-orbit certificate

The selected orbit, in canonical label order, has exact mixed actions:

| owner pair | quartic point | mixed action |
| --- | --- | ---: |
| `(0,0)` | `(0,0,2,2)` | `-31/144` |
| `(0,0)` | `(0,2,0,2)` | `-697/144` |
| `(4,4)` | `(0,0,2,2)` | `-31/144` |
| `(4,4)` | `(2,0,0,2)` | `-409/144` |
| `(7,7)` | `(0,2,0,2)` | `871/72` |
| `(7,7)` | `(2,0,0,2)` | `1015/72` |

The frozen exact replay passes:

- dependency hashes: `2/2`;
- directed generator edges: `12/12`, all non-self;
- geometry, `Phi1`, `Phi2`, Hodge, residual, moving primalizer, and action:
  `48/48` slots each;
- mixed residual, primalizer, action, and moving-Hodge controls: `6/6` each;
- obsolete forward lift: rejected; and
- exact mixed-action fingerprint: matched.

The final probe total is:

```text
26 exact + 2 source + 9 type + 7 planted = 44 PASS
```

## Constraint, non-regression, and scope

No parameter is fitted after the S3 action, sparse-curvature fixture,
predecessor coverage, representative ordering, and heldout inputs are frozen.
The shard checks 336 raw transport equalities. Their independent rank is not
computed, so no constraint-surplus or physics-surplus claim is made.

Gravity, gauge, odd matter, Higgs/Yukawa, quantum/domain, cosmology, and
P1/P2/P3 are unchanged; P1/P2/P3 remain unused. No coefficient, support,
`kappa1`, Green/Helmholtz, C3, domain, observation, datum, or physics verdict
changes here.

The seven planted promotions reject engine promotion from `5/380`, counting
frozen heldouts as executed, replacing the fallback before universal
coverage, assembling or merging the two banks, beginning Green/Helmholtz,
spending P1/P2/P3, or merging Curt/promoting a third lane.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.
`TG-1 AND TG-2 AND TG-3` remains `NOT_PROMOTED`.

## Next gate

```text
PW2F-R2B2B2I2-RESUMABLE-REMAINING-375-REPRESENTATIVE-FULL-EVALUATOR-COVERAGE-PLUS-EXECUTE-DENSE-HELDOUTS-THEN-SEPARATE-C4-BANKS
```

Continue one complete output-blind canonical orbit per serialized shard over
the other 375 representatives, then execute the frozen dense heldout bank.
Only complete `380/380` coverage plus executed heldouts may promote the engine
or replace the fallback. Assemble `I1 A4` and `I2B C4` separately afterward.
