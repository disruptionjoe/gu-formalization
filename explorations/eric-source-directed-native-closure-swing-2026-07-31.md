---
title: "Eric source-directed native closure swing"
status: active_research
doc_type: exploration
created: 2026-07-31
branch: agent/weinstein-guided-source-action
run: archived private execution record
specification: lab/specifications/eric-source-directed-native-closure-packet-2026-07-31.md
certificate: lab/process/eric-source-directed-native-closure-certificate.json
probe: tests/channel-swings/eric_source_directed_native_closure_probe.py
---

# Eric source-directed native closure swing

## Result

The ten Weinstein-guided physics routes now close into one conditional native
system. They no longer terminate at ten separate notes saying “look in the
geometry,” and they do not require ten familiar equations to be copied into
the action.

The closure uses five shared objects:

1. one observation lift/retract, equation-dual, leakage, and domain packet;
2. one stationary native orbit, its stabilizer, and one reduced bosonic
   Hessian;
3. one minimal Krein-paired odd action on
   `Omega^0(Y,S) + Omega^1(Y,S)` and its action-derived current
   (**[CN-2 S-TYPING: S-CHIRALITY-UNTYPED]** — `S` unsubscripted; which Weyl
   halves the two slots carry is not fixed here and the route does not choose,
   but the same-half reading `S-HALF-SAME` is stated by neither primary and is
   `REFUTED-AS-FILED` in `canon/escape-corners-campaign-RESULTS.md`);
4. one Frobenius-fibre trace-reversed metric readout; and
5. one action-derived preboundary quotient, positive majorant, and eventual
   observed-time Hamiltonian split.

Every requested physical name now has a precise conditional readout from
those objects:

- Maxwell, weak, and strong are different structural classifications of the
  same connection Hessian and action-selected stabilizer;
- Yang--Mills is the reduced exact G2/G3 connection Euler/Hessian block, with
  its matter current obtained by varying the same odd action;
- Einstein is the transverse-traceless quotient of the **trace-reversed**
  equation-dual metric Euler map;
- Higgs is a heavy scalar orbit of the shared Hessian whose incidence is also
  the odd zero-order bilinear;
- dark energy is a candidate second, distinct light scalar/trace mode whose
  observation stress remains to be derived;
- Dirac is the physical-domain `Omega^0` readout of the shared odd action;
- dark matter is a candidate physical complement of the luminous sector
  **inside** the admitted domain, separate from off-observation leakage; the
  control keeps its projector fixed while planting high-curvature mixing; and
- Schrodinger evolution is the observed-time Hamiltonian form of the reduced
  Krein dynamics, not a separate quantum postulate inserted upstairs.

The exact probe passes **1,954 exact checks and 13 planted failures, 1,967 in
all**. It demonstrates that these arrows are mutually compatible in one
finite exact parent model.

That is not a claim that the model has emerged on the actual metric bundle
`Y^14`. The gauge algebra, background, Hessian blocks, and finite operators in
the executable are deliberately planted positive controls. Every physical
row remains `OPEN` at the `Y^14` emergence gate.

## Paired-source correction: this is a compatibility control

The later paired Curt--Eric reconstruction supersedes the “one reduced
bosonic Hessian” reading as Weinstein's final action architecture. Eric says
the Einstein--Dirac layer comes first and a second Lagrangian/action supplies
Yang--Mills--Higgs as a square or second-order companion. This swing's finite
one-Hessian model remains valuable: it proves that the proposed readout arrows
can coexist and catches projector, pairing, and mode-naming errors. It does
not decide how the two source layers are coupled.

The next source-faithful object is the forked staged packet

\[
(\Upsilon_B,Q_B,I_2^B;
 I_{\rm ED},\Upsilon_{\rm ED},Q_{\rm ED},I_2^{\rm ED};
 \Pi_{\rm Higgs}^{\varpi},\Pi_{\rm Higgs}^{T},K,C),
\]

where the total-residual square is conjectural and the `varpi`/`T` Higgs
carriers must be related before either is shared across the first-layer odd
bilinear and second-layer gauge/scalar Hessian. See
[`paired-curt-eric-gu-axiom-and-argument-reconstruction-2026-07-31.md`](../lab/sources/paired-curt-eric-gu-axiom-and-argument-reconstruction-2026-07-31.md).

## What was actually filled in

The earlier atlas said where to look. This swing adds the map by which an
upstairs object would become a downstairs equation.

For fields,

\[
L_d:V_X\to V_Y,\qquad R_d:V_Y\to V_X,\qquad R_dL_d=1.
\]

For Euler covectors, the map is instead

\[
L_d^\vee:V_Y^\vee\to V_X^\vee,
\qquad
E_X(\phi)=L_d^\vee E_Y(L_d\phi).
\]

The Hessian is likewise first a covector-valued map

\[
H_Y:V_Y\to V_Y^\vee\otimes\operatorname{Dens},
\qquad H_X=L_d^\vee H_YL_d.
\]

Only after explicit Hodge/DeWitt/Krein Riesz maps may we define the primal
adjoint and operators

\[
L_d^!=\sharp_XL_d^\vee\flat_Y,
\qquad
\sharp_XE_X=L_d^!\sharp_YE_Y,
\qquad
K_X=L_d^!K_YL_d,
\]

where `K_Y=sharp_Y H_Y`, and apply the field-space leakage projector
`Q_off=1-LR`.

The exact control exhibits both cases that matter:

- the algebraic dual `L^vee`, not `R`, gives the action chain rule;
- with generic Euclidean pairings, the matrix representing `L^vee` is also
  the Euclidean Riesz adjoint `L^!`, and it differs from `R`;
- with a deliberately compatible native pairing, `L^!=R` holds, but now as a
  theorem of the pairing rather than a type mistake.

It also plants an operator for which

\[
R D_YL=D_X
\]

passes while

\[
(1-LR)D_YL\ne0.
\]

That catches the common false closure in which the decoded equation looks
right even though the native evolution immediately leaves the observation
image.

## The conditional parent action

The parent is not reset. It extends the already-surviving G2/G3 source action:

\[
I_{\rm cl}[d]
=I_{G2}[A,\epsilon,g;d]
 +I_{\rm odd}[A,\epsilon,g,\Psi,\bar\Psi;d],
\]

with

\[
v=\Pi_{\rm vert}(A-A_{\rm LC}(\epsilon,g))
\]

and the least-inserted odd term

\[
I_{\rm odd}
=\operatorname{Re}_{\mathbb H}
\int_Y\bar\Psi
(\mathcal D_{\rm sym}+M_v^{\rm phys})\Psi\,d\mu_G.
\]

The minimal operator preserves the `Omega^0/Omega^1` form degree. It inserts
no flavour matrix, generation label, SW moment map, or independent mass
matrix. The vertical coefficient of the already-present distortion is the
only provisional zero-order source. Its physical mass is the complete
Krein-paired bilinear, not the bare operator.

The connection current is

\[
\langle J_A,a\rangle
=\left.\frac d{dt}\right|_0I_{\rm odd}[A+ta].
\]

This resolves the N3 type question conditionally: the action emits a
density-dual connection covector. It still needs the explicit
Hodge/DeWitt/Krein Riesz map to become the primal one-form used by the bridge.
No `J_F` is claimed until a curvature-dependent odd vertex is actually
admitted by the naturality census.

## Trace reversal is load-bearing

The user reminder about trace reversal on the Frobenius fibre changes the
metric readout, not merely its notation. On the actual ten-dimensional
symmetric metric fibre,

\[
\tau_F(h)=h-\frac12\operatorname{tr}_g(h)g.
\]

The exact probe verifies that `tau_F` is Frobenius self-adjoint and involutive
in four dimensions: it negates the trace line and fixes all traceless
symmetric tensors. The gravitational/cosmological Euler covector uses the
dual trace reversal

\[
E_{g,X}=\tau_F^\vee L_g^\vee E_{g,Y},
\]

before transverse-traceless or homogeneous projection. This stays on
`Sym^2 T^*X`; it is not replaced by the numerically equal but structurally
different `Lambda^2 + Lambda^3` ten.

## The one-Hessian control

The repaired exact finite fixture derives one 19-dimensional Hessian from one
explicitly coupled action. It contains:

- one planted compact `u(1)+su(2)+su(3)` connection algebra;
- one represented `U(2)` complex doublet and vacuum whose rank-three orbit
  incidence generates the gauge mass matrix and has one abelian stabilizer;
- eight massless colour directions;
- two massless spin-two controls;
- a quartic scalar orbit with three Goldstones and one heavy radial mode; and
- one separate positive light scalar mode.

The compact algebra passes Jacobi, centre, derived-rank, Killing-sign,
anti-Hermitian, cross-ideal, and dense exact basis-change checks. The represented
vacuum orbit is **exactly** the scalar Hessian's Goldstone kernel; the same
orbit incidence generates the gauge mass matrix. An explicit equivariant map
`M_h:C_trivial -> C^2_fundamental` has the same infinitesimal orbit incidence,
so the fixture now tests local Higgs/gauge/Yukawa compatibility rather than
merely matching dimensions. The gauge-scalar mixed Hessian vanishes as a
derived fact at the stationary zero-connection background, not because that
block was manually deleted. The light and heavy scalar eigenvalues are exact
and unequal.

Only the planted `U(2)` gauge--doublet portion is nontrivially coupled in this
finite action. Colour, spin two, and the light scalar remain deliberately
decoupled controls. The Yukawa test is representation-level: it has not yet
been installed as a Krein-adjoint-completed odd block with realistic left/right
hypercharges. Those limits are why the result is compatibility, not dynamical
unification.

This answers the compatibility question: one Hessian can carry all those
classifiers without assigning a different parent action to each sector. It
does not answer the emergence question because the fixture starts with the
desired compact algebra and sector projectors. On `Y^14`, the background,
stabilizer, projectors, and representations must be frozen from native
properties before the spectrum is read.

## The odd/Krein/dark/evolution control

The finite odd operator uses an indefinite exchange pairing `K` and satisfies

\[
D^\times=K^{-1}D^\dagger K=D.
\]

With the fundamental symmetry `Theta=K`,

\[
K\Theta=1>0,
\qquad
H_{\rm form}=\Theta D=H_{\rm form}^\dagger.
\]

The zero-order operator `M` commutes with chirality, while the physical
bilinear kernel `KM` anticommutes with it. The planted no-`K` test fails. This
reproduces the earlier repository correction in the closure packet itself.

A fixed physical luminous projector `P_lum` and complement
`Q_dark=1-P_lum` decouple at the planted background. Both live **inside** the
admitted finite dynamics; `Q_dark` is not the off-observation projector
`Q_off=1-LR`. A declared off-diagonal curvature parameter turns on
`P_lum D Q_dark` without moving either projector. That is only the minimal
mathematical shape of Weinstein's high-curvature recoupling claim. It is not
yet dark matter: the actual construction still owes common gravitational
stress, a gap, positive norm, stability, abundance, and a source-derived
threshold.

Finally, `Theta D` generates unitary algebraic flow in the finite control, but
the specialist audit prevents a false promotion. For a real time evolution,
the reduced operator must first be written

\[
D_X=C_t\nabla_t+D_\Sigma+M_X,
\]

after which the covariant equation is

\[
H_{\rm cov}=-iC_t^{-1}(D_\Sigma+M_X),
\qquad i\nabla_t\Psi=H_{\rm cov}\Psi.
\]

If `nabla_t=partial_t+Gamma_t`, the ordinary-time Hamiltonian also contains
the connection transport term `-i Gamma_t`, plus the measure and
time-dependent-majorant corrections needed to preserve the inner product.
Ambient `(9,5)` geometry supplies neither the time nor the domain for free.

## Specialist council: what each lens added

The specialists were asked to attack the concrete closure, not to rediscover
that an action or datum was needed.

### Affine, symplectic, and group-cohomological geometry

This lens forced the complete observation packet

\[
(L,R,L^\vee,L^!,\sharp_X,\sharp_Y,
 P,Q,L_\partial,R_\partial,\Omega_Y,\Omega_X)
\]

and distinguished a decoder from a variational transpose, an Euler Hessian
from its primalized operator, and off-observation leakage from a physical dark
sector. It also required a staged bulk gauge reduction and separate boundary
characteristic quotient; those are not one ordinary “physical Hessian
quotient.” Its strongest plant is an `RDL` success with nonzero leakage.

### Krein/operator and hyperbolic PDE

This lens supplied the minimal odd action, Krein symmetrization, exact current
variation, moving-owner Ward identity, and domain gates. It confirmed
`Theta D` as the Hilbert Riesz representative of the Krein form while
rejecting its premature identification with a time Hamiltonian. It also
requires a common closed domain, maximal-isotropic Green boundary condition,
right-quaternionic compatibility, a 4D Dirac principal symbol, and no retained
Velo--Zwanziger characteristic.

### Standard Model representation theory and cosmology

This lens made the readout tests target-blind. A photon needs a unique compact
abelian ideal and a physical transverse pole; weak and strong need compact
simple `A1` and `A2` ideals; spin two needs two TT modes plus nonlinear
constraint closure; the Higgs needs a doublet orbit and common Yukawa
incidence. Most importantly, the cosmological scalar must be a second
protected mode, and the dark complement must remain dynamical rather than
being discarded by the observation projector.

The lenses agree on the construction order. Their cautions are not averaged
away: the finite control covers algebraic compatibility, while global bundle,
domain, nonlinear, quantum, and empirical gates remain separately open.

## Constraint-surplus posture

This swing does not dismiss a constructed fit as uninformative, but it also
does not compute a positive surplus for the planted fixture. The fixture has
target-coded algebra and projectors, so its undeclared choices consume the
apparent constraints. Its value is as a compatibility and matcher control.

The informative surplus calculation begins only after the actual G3.5/G4
naturality census freezes the admissible maps, coefficients, background
family, domain, and classifiers without Standard Model labels. At that point
the shared-Hessian fan-out can produce genuine surplus: one frozen parent must
simultaneously clear many independent gauge, gravity, scalar, odd, and dark
tests.

## What is now closed and what remains open

Closed in this branch:

- every one of the ten source directives has a typed native readout;
- the readouts share five objects rather than ten imported equations;
- the observation/equation-dual/leakage distinctions are exact;
- trace reversal is installed in the metric route;
- the minimal odd action, bilinear, current type, and Hamiltonian distinction
  are written;
- one exact model proves mutual compatibility and catches the main shortcuts.

Open on the actual theory:

- global observation maps and a common closed `(9,5) -> (3,1)` domain;
- nonlinear leakage closure for the exact G3 Euler map;
- action-selected stationary backgrounds and stabilizer;
- the actual reduced bosonic Hessian and all physical poles/signs;
- global odd operator, constraint propagation, mirror gap, anomaly, and
  physical chiral cohomology;
- source-selected positive majorant/time; the current finite result is only a
  presymplectic characteristic quotient, with BFV charge/master closure open;
- Higgs representation/VEV, protected light scalar, dark gap/threshold;
- Standard Model, gravity, cosmological, abundance, confinement, PP3/DESI,
  index, and P3 tests.

No generation count, anomaly cancellation, mass spectrum, cosmological
prediction, or empirical recovery is claimed.

## Next highest-information swing

Do not build another finite Standard Model-shaped fixture. The later Curt
reconciliation inserts C0 before this report's C1 lift: adjudicate the
chimeric/gimmel carrier, build only the invariant primitives required for a
target-blind G3.5 census, ablate the candidate terms, and port only the
surviving G2/G3 realization if the carriers are inequivalent. Then lift C1
into the surviving actual bundle/Euler packet:

1. construct local bundle/differential-operator candidates for
   `L_d,R_d,L_d^vee,L_d^!`
   from the source-directed metric section and tilted gauge geometry;
2. prove patch descent, right-quaternionic/Krein compatibility, and preservation
   of one declared domain;
3. construct `sharp_Y`, then compute the full nonlinear primalized leakage
   `Q_off sharp_Y E_Y(L_d phi)` and retain every cross-sector block;
4. transport the coupled G3 Ward identity through `L_d^vee`, using `L_d^!`
   only after primalization, including moving
   reduction, matter, and boundary owners;
5. reduce the action-derived preboundary kernel and test whether a source-owned
   polarization/positive majorant exists.

If that survives, solve a one-parameter stationary native background family
and compute the actual stabilizer, bosonic Hessian, and odd decomposition
together. That is the first swing in which the planted compatibility picture
can become—or decisively fail to become—emergent physics.
