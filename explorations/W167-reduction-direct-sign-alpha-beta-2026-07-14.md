---
artifact_type: exploration
status: exploration (TEAM REDUCTION-DIRECT, W167; five personas inline, one worker, no sub-agents; deterministic test with W126/W130/W159/W165 positive controls, 26/26 exit 0)
created: 2026-07-14
wave: W167
label: W167
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; honest grading; RUTHLESS self-verification; tri-repo gating STRICT
route: DIRECT REDUCTION FROM GU'S ACTUAL (LINEAR) ACTION
sibling: W168 (independent Krein-signature route computing the same bit)
title: "W167 -- the DIRECT reduction of GU's actual action, and the decisive bit sign(alpha+beta). VERDICT (two levels): at GU's LAW level alpha+beta = 0 EXACTLY (c_R=0, linear action, W161) -- the |II|^2 tachyon is SPURIOUS as a fundamental-law property because |II|^2 is a SEPARATE geometric bending self-energy (Willmore, Weyl-carrying), NOT the pullback of GU's dynamical linear law (shiab-Einstein, Ricci-class, Weyl-annihilated); the two are DIFFERENT covariant points (c_R=-4/9 vs -1/6). At the INDUCED-SHADOW level sign(alpha+beta) is UNDETERMINED: the actual first-order integrate-out of GU's linear action gives S_eff = -(1/2)<shiab F, shiab F> (a structural Legendre MINUS the +|II|^2 default omitted), which under a positive-definite inner product lands POSITIVE (alpha+beta = +3/8 > 0, c_R < 0, TACHYONIC) ROBUSTLY across the entire shiab trace-parameter family (c_R(t) = -2t^2+t-1/6 < 0 for all real t, discriminant -1/3 < 0); the ONLY lever to NEGATIVE (healthy) is the Krein signature of the record-count (trace) mode on the (9,5) q=5 finality frontier -- the SAME gated, TaF-owned object W165 reached independently. This CORRECTS W161's optimistic 'the reduction lands healthy': performed, the induced shadow lands tachyonic-or-gated, not healthy-by-default. Bar (b) UNCHANGED / conditional -- consistent with W159/W161/W165."
grade: "exploration / conditional register throughout. COMPUTED (deterministic, tests/W167_reduction_direct_sign_alpha_beta.py, 26/26 exit 0, W126/W130/W159/W165 positive controls first): PC1-PC6 reproduce W126 (2,1/3,8/9,-4), W130 c_R=-4/9, W159/W165 |H|^2=(-1,4/3,-4/9,0), the shape law c_R=-(4/9)(alpha+beta), the healthy point (-2,1), and the covariant map controls (GB null, +R^2 healthy, -Ric^2 tachyonic, linear c_R=0). NEW-DERIVED: R0/R1 the Legendre integrate-out T*=-shiab(F), S_eff=-(1/2)<shiab F,shiab F> (structural minus); R2a/R2b the Ricci-class shadow |shiab_t(F)|^2 = Ric^2+(4t^2-2t)R^2 with c_R(t)=-2t^2+t-1/6; R2c the shadow is a DIFFERENT covariant point than the geometric |II|^2; R3a/R3b the shiab-family robustness (c_R(t)<0 for all real t, discriminant -1/3, max -1/24); R3c the Einstein-point map alpha+beta=+3/8; R4 shadow-of-linear-action is tachyonic (same sign as |II|^2, different point); R9a/R9b POINT (3): |II|^2 is a separate geometric self-energy so the law-level alpha+beta=0 stands; R6 the steelman-healthy reading refuted (shiab kills Weyl); R7 the Krein-indefiniteness escape (w2=-64<0); R8 the UNDETERMINED headline; NC1 the readings bracket zero; NC2 the shiab non-uniqueness is not a sign-lever. CITED (not re-derived): W126/W130 (induced |II|^2 slice, covariant c_R), W159 (|H|^2, det=4/9, w2=-64), W161 (linear action, c_R=0 at law, the Palatini/E-C integrate-out toy), W154 (the |II|^2 = integrated-out shadow claim, T0, the unbuilt eta-from-gimmel-area bridge), W165 (the shape law c_R=-(4/9)(alpha+beta), the gated (9,5) q=5 Krein-signature object), W132/W137 (records = C-positive subspace), W150 (the q=5 finality frontier). No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count stays {1,3}; H41 unbuilt; H59 OPEN; the shiab non-uniqueness and the shadow-vs-law fork are load-bearing SOURCE facts, not asserted of GU beyond the April 2021 draft. No forbidden target {3,8,24,chi(K3),Ahat} assumed/inserted/hardcoded/divided-by."
construction: "program-native where the objects are GU's (Y14, the displaced torsion T, the shiab-Einstein projection, the induced |II|^2/|H|^2 slice, the covariant c_R channel, the (9,5) Krein ambient, the q=5 finality frontier). Standard-field where the machinery binds any construction (the Legendre/auxiliary-field elimination of an algebraic field; the covariant quartic basis map c_R=a+b/3+c/3 with GB in its kernel; the 4D identity |Ric - t R g|^2 = Ric^2 + (4t^2-2t)R^2; the Willmore-vs-Einstein functional-class distinction; the discriminant test for sign-definiteness). Every analogy PORTED and labelled; none asserted of GU beyond the source. Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md; the shiab-choice fork (the source's own non-uniqueness) is CARRIED and SWEPT (sign-robust); the inner-product-signature fork (positive-definite vs Krein) is CARRIED and named as the sign-deciding datum; tri-repo gating STRICT (the finality/capability bridge is a GATED conjecture, no cross-repo identity asserted)."
depends_on:
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W154-reverse-engineered-source-action-2026-07-14.md
  - explorations/W159-tachyon-escapes-2026-07-14.md
  - explorations/W161-lens-foundational-action-2026-07-14.md
  - explorations/W165-lens-abstraction-level-2026-07-14.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W167_reduction_direct_sign_alpha_beta.py
primary_source:
  - "Geometric_UnityDraftApril1st2021.pdf (E. Weinstein), sec 9.1 eq (9.4) (the first-order Bosonic action, LINEAR in curvature), eq (9.3) (the shiab annihilates the Weyl curvature), eq (9.7)-(9.10) (reduction to the second-order Einstein equation), sec 9.1 fn 10 (the shiab is non-unique)"
external_refs:
  - "Stelle, PRD 16 (1977) 953 -- fourth-order gravity spectrum (the R^2 = spin-0 scalaron whose sign c_R sets tachyonicity)"
  - "Starobinsky, PLB 91 (1980) 99 -- +R^2 scalaron (healthy), the positive control on the c_R map"
  - "Sakharov 1967 / induced gravity -- higher-curvature terms are integrated-out coefficients, not fundamental law"
---

# W167 -- the DIRECT reduction of GU's actual action: sign(alpha+beta)

## 0. The one bit, and the route

The whole substrate/record bar-(b) verdict has been reduced (six-lens wave W161-W166) to ONE
BIT: the sign of `(alpha+beta)` in GU's EFFECTIVE 4D gravitational action, where the induced
spin-0 (scalaron) coupling is `c_R = -(4/9)(alpha+beta)` (W165, shape-blind / exact), so
`sign(c_R) = -sign(alpha+beta)`:

- `alpha+beta < 0  ->  c_R > 0`  HEALTHY  -> the tachyon is SPURIOUS -> bar (b) clears.
- `alpha+beta > 0  ->  c_R < 0`  TACHYONIC -> the false-vacuum record-accretion engine (W163/W166).
- UNDETERMINED -> the sign is a free datum of GU's current formulation; name what fixes it.

W168 (sibling) computes the same bit by an independent Krein-signature route. THIS team is the
DIRECT reduction route: build the reduction from GU's ACTUAL action (linear, shiab-Einstein,
April 2021 eq 9.4) to the effective 4D action and READ OFF the induced `(alpha,beta)` -- rather
than defaulting to the postulated pure `|II|^2` (`alpha=1, beta=0`, tachyonic) the session
inherited.

Five personas inline, one worker, no sub-agents. Deterministic test
`tests/W167_reduction_direct_sign_alpha_beta.py`, 26/26 exit 0, W126/W130/W159/W165 positive
controls first. Led with the STRUCTURAL route (the auxiliary-field / Legendre trick and the
targeted pullback comparison), not a 14-dimensional brute-force integrate-out (Joe's redirect,
2026-07-14).

## 1. Persona 1 -- GU-action specialist: the law is linear; the shadow is a Legendre transform

GU's actual fundamental action is (April 2021, eq 9.4):

```
I1B = < T ,  star_shiab( F_B + (1/2) d_B T + (1/3)[T,T] )  +  (1/2) T >_{Y14}
```

with `T = omega - g.omega0` the DISPLACED TORSION (a contorsion / second-fundamental-form-like
object), `star_shiab` the gauge-covariant Einstein (Ricci minus trace) projection that
"annihilates the Weyl curvature" (eq 9.3). It is LINEAR in the Ehresmann curvature and reduces
(eq 9.10) to the SECOND-ORDER Einstein equation `S = T`. In the covariant quartic basis
`gamma R + a R^2 + b Ric^2 + c Riem^2` that is `a=b=c=0`, so at LAW level `c_R = a + b/3 + c/3 = 0`
EXACTLY: **no propagating scalaron, tachyonic or otherwise** (test R0; W161).

The tachyon lives one level down, in the INDUCED / integrated-out SHADOW. The decisive structural
move (the same auxiliary-field trick that turned the W122 spin-0 grind into a two-line result):
`T` is ALGEBRAIC in eq 9.4's Gaussian part `< T, J > + (1/2) < T, T >` with `J = star_shiab(F)`
and the `(1/2)` from GU's own `+(1/2)T` term. Integrating it out is exact:

```
delta/delta T = 0  ->  T* = -J  ->  S_eff = -(1/2) < J, J > = -(1/2) | star_shiab(F) |^2
```

**The induced shadow carries a structural LEGENDRE MINUS SIGN** (test R1). This is a Legendre
transform, not a convention. The `+|II|^2` default the session inherited OMITTED it: it read the
shadow as a POSITIVE norm-square. The direct reduction says the shadow is MINUS one-half a
norm-square of the Einstein-projected curvature.

## 2. Persona 2 -- differential geometer: the shadow is Ricci-class (Weyl-annihilated)

Because `star_shiab` annihilates the Weyl curvature (source eq 9.3), `J = star_shiab(F)` is a pure
Ricci-class tensor: `J = Ric - t R g` for the shiab trace-parameter `t` (Einstein value `t = 1/2`).
Its covariant norm-square in 4D (using `g:Ric = R`, `g:g = 4`) is

```
| Ric - t R g |^2 = Ric^2 + (4 t^2 - 2 t) R^2       (test R2a; NO Riem^2 / Weyl content, c = 0)
```

So the induced shadow `-(1/2)|J|^2` sits at the covariant point `(a,b,c) = -(1/2)(4t^2-2t, 1, 0)`,
and its scalaron coupling is

```
c_R(t) = a + b/3 + c/3 = -(2 t^2 - t) - 1/6 = -2 t^2 + t - 1/6            (test R2b)
```

**This is a DIFFERENT covariant point than the geometric `|II|^2`** (test R2c). The geometric
`|II|^2` (W126) is a WILLMORE bending energy: quadratic in the EXTRINSIC curvature, and it CARRIES
Weyl content (slice `a3s = -4`), giving `c_R = -4/9`. The shiab shadow (Ricci-class, Weyl killed)
gives `c_R(1/2) = -1/6`. The shiab projection removes exactly the Weyl content the geometric
`|II|^2` keeps. The two induced objects are NOT equal.

## 3. Persona 3 -- higher-derivative theorist: the shiab-family sign sweep is ROBUST

The source leaves the shiab non-unique (sec 9.1, fn 10). Sweep the whole trace-parameter family:

```
c_R(t) = -(2 t^2 - t + 1/6) .
```

The inner quadratic `2 t^2 - t + 1/6` has discriminant `1 - 4*2*(1/6) = 1 - 4/3 = -1/3 < 0` and
positive leading coefficient, so it is strictly POSITIVE for EVERY real `t`. Therefore
`c_R(t) < 0` for the WHOLE shiab family (maximum `-1/24` at `t = 1/4`; test R3a, with explicit
members `t = 0, 1/4, 1/2, 1, -1` all tachyonic in R3b). **The shiab non-uniqueness does NOT flip
the sign.** Mapped into the shape family via `c_R = -(4/9)(alpha+beta)`, the Einstein-point shadow
`c_R = -1/6` corresponds to `alpha+beta = +3/8 > 0` (test R3c): POSITIVE total weight, TACHYONIC.

So under a positive-definite inner product the DIRECT reduction of GU's linear action lands at
`alpha+beta > 0`, TACHYONIC, and it is ROBUST across the entire admissible shiab family. The sign
does not live in the shiab choice.

## 4. Is the shadow of the LINEAR action the tachyonic |II|^2 point or the healthy region?

Under a positive-definite inner product: **the tachyonic region** (test R4). The shiab-Einstein
shadow (`c_R = -1/6`) and the geometric `|II|^2` (`c_R = -4/9`) are DIFFERENT covariant points but
SHARE the tachyonic sign. So the induced shadow of GU's actual linear action is tachyonic,
CONVERGING with W126/W130/W165 via a genuinely independent route (the first-order integrate-out,
not the geometric Willmore norm). W130 called `|II|^2` "the integrated-out shadow"; the honest
first-order integrate-out of the LINEAR action is a nearby but distinct Ricci-class point of the
same sign.

## 5. POINT (3) -- the cheapest deepest check: is |II|^2 even GU's effective action?

Joe's redirect named the decisive cheap check: is `|II|^2` the TRUE effective action of GU's
dynamical linear theory, or an INDEPENDENTLY-computed geometric self-energy (the section's
Willmore / bending energy from the H21/H25/W126 induced-metric machinery)?

**Answer (test R9a): they are DIFFERENT objects.** The geometric `|II|^2` is a Willmore bending
self-energy, quadratic in the extrinsic curvature, computed independently by the H21/H25 machinery,
and it CARRIES Weyl (`a3s = -4`). GU's dynamical-law pullback (the shiab-Einstein integrate-out)
ANNIHILATES Weyl (`c = 0`). They are not the same tensor and not the same covariant point. So
`|II|^2` is NOT proven to be GU's effective action; it is a geometric self-energy that W154 T0
IDENTIFIES with the effective action via the UNBUILT eta-from-gimmel-area bridge (W151/W154).

**Consequence (test R9b): at GU's LAW level `alpha+beta = 0` EXACTLY** (`c_R = 0`, linear action,
second-order field equations, W161). The tachyon (`c_R = -4/9`) is a property of the geometric
`|II|^2` self-energy, NOT of GU's dynamical law. So the tachyon is **SPURIOUS at tree/law level**;
it can re-enter ONLY as an induced/radiative coefficient, whose sign is the gated Krein datum
(Section 6). This is the cheap structural resolution: at the level of GU's actual law the tachyon
is not there.

## 6. Persona 5 -- adversarial skeptic (RUTHLESS): steelman all three outcomes

The main risk is ASSUMING the answer. All three outcomes are steelmanned; none is defaulted.

- **HEALTHY steelman, REFUTED (test R6).** "Apply the Legendre minus to W126's geometric `|II|^2`:
  `-(1/2)(-4/9) = +2/9 > 0`, healthy." This REQUIRES `< star_shiab F, star_shiab F > = |II|^2_W126`.
  But `star_shiab` kills Weyl (Section 2) while the geometric `|II|^2` carries Weyl (`a3s = -4`).
  The identification is FALSE; the healthy reading rests on a wrong map. The correct shiab shadow is
  Ricci-class, `c_R = -1/6 < 0`. The tempting "the minus sign saves it" story does NOT survive.
- **TACHYONIC-determined steelman, TEMPERED (test R7).** "The positive-definite reduction is
  robustly tachyonic (Section 3), so the answer is just TACHYONIC." But the Y14 inner product is
  INDEFINITE (W159/W165: `w2 = < II_1, II_1 > = -64 < 0`). On a Krein-NEGATIVE trace mode the
  `-(1/2)` flips the induced coefficient's sign, reaching `alpha+beta < 0` (health). So TACHYONIC is
  the positive-definite DEFAULT, not a theorem.
- **UNDETERMINED, the honest headline (test R8).** The sign-deciding free datum is the Krein
  signature of the record-count (trace) mode on the (9,5) `q=5` finality frontier -- the SAME gated,
  TaF-owned object W165 reached by the universal-property route. GU-internal C-positivity (W132/W137:
  records = C-positive subspace) is a CANDIDATE pin, but the bridge from the C-metric on `Psi` to the
  trace-mode Krein sign in the `II` inner product is the unbuilt eta-from-gimmel-area bridge
  (W151/W154). Do NOT default: neither sign is forced by GU's current formulation.

**Negative controls.** The admissible readings BRACKET zero (`-4/9`, `-1/6` tachyonic; a
Krein-negative trace mode `+1/6` healthy), so the sign is genuinely two-sided (test NC1); and the
shiab non-uniqueness is NOT a sign-lever (`c_R(t) < 0` for all real `t`, test NC2) -- the one and
only sign-lever is the inner-product Krein signature.

## 7. Synthesis -- the return data (two levels)

**LAW LEVEL: `alpha+beta = 0` EXACTLY** (`c_R = 0`, W161). GU's actual linear action has no
tree-level scalaron. The `|II|^2` tachyon is SPURIOUS as a fundamental-law property, because
`|II|^2` is a separate geometric bending self-energy (Willmore, Weyl-carrying), a DIFFERENT object
than GU's dynamical-law pullback (shiab-Einstein, Ricci-class, Weyl-annihilated) -- Point (3),
tests R9a/R9b.

**INDUCED-SHADOW LEVEL: sign(alpha+beta) UNDETERMINED.** The actual first-order integrate-out of
GU's linear action gives `S_eff = -(1/2) < star_shiab F, star_shiab F >` -- a structural Legendre
minus the `+|II|^2` default omitted -- which under a positive-definite inner product lands POSITIVE
(`alpha+beta = +3/8 > 0`, `c_R < 0`, TACHYONIC) ROBUSTLY across the entire shiab trace family
(`c_R(t) = -2t^2 + t - 1/6 < 0` for all real `t`). The ONLY lever to NEGATIVE (healthy) is the
Krein signature of the record-count (trace) mode on the (9,5) `q=5` finality frontier -- the SAME
gated, TaF-owned object W165 reached independently. **Two independent routes (W165 universal
property; W167 direct reduction) converge on the SAME missing input.**

This CORRECTS W161's optimistic "the reduction lands healthy at the fundamental level": performed,
the induced shadow lands tachyonic-or-gated, NOT healthy-by-default. The shiab non-uniqueness (a
plausible sign-lever) is SWEPT and does not flip the sign.

**Return fields (raw data):**

| Field | Result |
|---|---|
| sign(alpha+beta) VERDICT | LAW LEVEL: `= 0` EXACTLY (tachyon SPURIOUS as a law property). INDUCED-SHADOW LEVEL: UNDETERMINED, POSITIVE (tachyonic) under a positive-definite inner product, flipping to NEGATIVE (healthy) only via the gated (9,5) `q=5` Krein signature. |
| effective (alpha,beta) the linear-action reduction produces | `S_eff = -(1/2)|star_shiab F|^2`, Ricci-class, `c_R(t) = -2t^2+t-1/6`; at Einstein `t=1/2`, `c_R = -1/6` <=> `alpha+beta = +3/8` (positive-definite). A DIFFERENT point than the geometric `|II|^2` (`c_R=-4/9`, `alpha+beta=+1`). |
| shiab-family robustness | SIGN ROBUST: `c_R(t) < 0` for ALL real `t` (discriminant `-1/3 < 0`, max `-1/24` at `t=1/4`). The sign does NOT flip within the shiab family; it is not a sign-lever. |
| induced shadow of the LINEAR action: tachyonic |II|^2 point or healthy region? | The TACHYONIC region under a positive-definite inner product (a Ricci-class point, `c_R=-1/6`, distinct from but same-sign as the geometric `|II|^2`). Healthy only if the trace mode is Krein-negative. |
| effect on bar (b) | UNCHANGED / conditional. Consistent with W159 (SIGN-FREE), W161 (conditional-on-shadow-as-law), W165 (relocation to the finality frontier). The count stays {1,3}. |
| tests + exit codes | `tests/W167_reduction_direct_sign_alpha_beta.py` -- 26/26 PASS, exit 0. |

## 8. Gates and honest limits

- Exploration grade; conditional register throughout. Nothing asserts GU, asserts a vacuum, or
  changes any verdict. The primary source is read as DATA; every "eq (9.x) says X" is a quotation.
  The completion is a two-level STATUS reading (law: `alpha+beta=0`; shadow: undetermined-gated),
  established by exact computation with W126/W130/W159/W165 positive controls, not asserted.
- W138 battery honored: the verdict binds to GU-specific objects (eq 9.4, the shiab, the displaced
  torsion, the induced `|II|^2` / `|H|^2` slice, the covariant `c_R` channel, the shape family, the
  (9,5) `q=5` frontier). The tempting overclaims the analysis FORBIDS are recorded: we do NOT claim
  GU's shadow lands healthy (the Legendre minus on the geometric `|II|^2` reading is refuted, R6); we
  do NOT claim it is determinately tachyonic (the Krein indefiniteness tempers it, R7); we do NOT
  claim the shiab choice fixes the sign (swept, R3/NC2).
- Tri-repo gating STRICT: the finality/capability bridge is a GATED CONJECTURE; no cross-repo
  identity is asserted. "record / finality / capability" stay pointers to
  temporal-issuance / time-as-finality / TaF; GU owns the induced-action and Krein math only. The
  sign-deciding object being the SAME as W165's is a convergence of two GU-internal routes onto the
  SAME gated input, not a cross-repo claim.
- Honest residuals (what would move UNDETERMINED to a sign): (1) the Krein signature of the
  trace/record-count mode on the `q=5` frontier [gated, TaF capability measure]; (2) the exact
  reduction map identifying `< star_shiab F, star_shiab F >_{Y14}` with a specific covariant
  quadratic invariant (the eta-from-gimmel-area bridge, W151/W154, unbuilt) -- this is also what
  would settle whether the induced shadow is literally the Ricci-class point or the geometric
  `|II|^2`; (3) whether the higher-than-Gaussian `T`-terms in eq 9.4 (the `(1/2)d_B T`, `(1/3)[T,T]`
  inside the shiab and the torsion self-coupling) shift the leading Gaussian coefficient.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; H41 unbuilt; H59 OPEN; the
  count stays {1,3}. No forbidden target assumed or inserted. Zero em dashes in paper-facing text.

*Filed 2026-07-14 by Team REDUCTION-DIRECT (W167). Route: DIRECT REDUCTION FROM GU'S ACTUAL ACTION.
Five personas inline in one worker (GU-action specialist; differential geometer; higher-derivative-
gravity theorist; symbolic engineer; adversarial skeptic); no sub-agents. Primary source read as
data: `Geometric_UnityDraftApril1st2021.pdf` sec 9. Reproducible:
`python -u tests/W167_reduction_direct_sign_alpha_beta.py` (26/26, exit 0; W126/W130/W159/W165
positive controls first). Sibling W168 computes the same bit by the independent Krein-signature
route. Exploration grade; conditional register; RUTHLESS self-verification; no canon movement;
tri-repo gating strict; bar (b) UNCHANGED.*
