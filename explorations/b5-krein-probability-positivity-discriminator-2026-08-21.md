---
title: "B5 Krein-to-probability discriminator: positivity is extension- and metric-underdetermined"
status: active_research
doc_type: exact_current_packet_positivity_nonselection_result
created: "2026-08-21"
registry: lab/process/b5-krein-probability-positivity-discriminator.json
probe: tests/channel-swings/b5_krein_probability_positivity_probe.py
grade: "EXACT WITT-PLANE, CLOSED REDUCED-CLASS AND CURRENT ACTION/SOURCE NONSELECTION GRADE ON THE NAMED FLAT B5 HALF-CYLINDER"
target_claim: internal target B5-KREIN-TO-PROBABILITY-POSITIVITY-DISCRIMINATOR; verdict native marked class is Green-null, positive completions require unselected data, and physical positivity remains extension-underdetermined
canon_verdict_change: none
---

# B5 Krein-to-probability positivity discriminator

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: SOURCE_NATIVE_ROUTE`

Scope: the repository-constructed strict massless four-stage B5 complex and
its exact marked reduced middle-stage class on the flat complexified `(9,5)`
half-cylinder. It is not the graph-mixing Stage-B Euler family, the conditional
98D balanced RSAP, Weinstein's unreleased source action, a quantum BRST
measure, a Fock space, or the global geometry of `Y=Met(X)`.

```gu-typed-objects
result: the exact marked reduced hit class is null for the native Green form, every compatible positive fundamental symmetry leaves its hit trace line, and auxiliary positive quotient metrics remain unselected
carrier: marked line C[u] inside ker K_hit over closure im A_hit, controlled by the real Witt plane span(v,w) in the U1 boundary quotient LAYER=ambient CHIRALITY=S-FULL-DIRAC
pairing: program-native middle Green Krein form G with G(v,v)=G(w,w)=0 and G(v,w)=1, distinguished from the auxiliary positive L2 quotient topology ON=independent-B5-curved-strict-carrier
real_structure: relative Gamma-natural antilinear coflip fixing the real Witt vectors and the marked decaying Fourier mode
grading: linear abelian gauge/BV stage grading; positivity target is a physical Hermitian form on reduced U1 cohomology
action_owner: repository-construction strict massless quadratic Rarita--Schwinger action; hit and miss extensions and every positive metric completion remain action/source-unselected
target: whether the available closed domain, action or source selects a positive probability pairing on the marked reduced class MAP-TYPE=evaluation
```

## Result first

The hit complex contains the exact nonzero reduced class `[u]`; the miss
middle domain excludes the same section. The program-native middle Green form
does not make the hit class positive. On the normalized Witt plane

```text
G(v,v)=0,          G(w,w)=0,          G(v,w)=1,
```

the marked trace is the null line `C v`. Its native squared norm is therefore
zero.

Positive metrics are mathematically available, but the current packet does
not select one. For every rational `r>0`, the exact fundamental symmetry

```text
J_r = [[0,1/r],[r,0]],       J_r^2=1,
G J_r = diag(r,1/r)>0
```

gives a positive Hilbert majorant on the Witt plane. It also sends
`v` to `r w`. Thus it does not preserve the hit trace line. More generally,
if a fundamental symmetry preserved `C v`, then `Jv=lambda v` and

```text
G(v,Jv)=lambda G(v,v)=0,
```

so the induced form could not be positive on `v`. A positive Krein completion
must mix the hit line with its excluded opposite Witt partner or change the
domain data.

The auxiliary positive `L2` topology used to prove graph closedness gives the
nonzero reduced hit quotient an ordinary positive quotient norm. On the
one-dimensional marked line, however, every

```text
<z[u],z[u]>_alpha = alpha |z|^2,       alpha>0,
```

is equally compatible with the presently established linear quotient. No
bulk-action identity, boundary selector, source statement, conservation law,
observable algebra, or quantum measure chooses `alpha` or identifies this
auxiliary norm as physical probability.

Therefore the correct verdict is:

```text
positive mathematical norm: available
native Green norm of the marked class: zero
domain-preserving positive fundamental symmetry: absent on the marked Witt line
action/source-selected physical pairing: not supplied
physical presence of the marked class: extension-sensitive
```

Positivity is extension- and metric-underdetermined at the current exact
packet, not disproved in every possible completion.

## Why the quotient does not manufacture probability

The bounded Fourier/transverse separator from the prior result proves
`[u] != 0` in the reduced quotient. It does not choose a metric. The positive
Hilbert topology is analytic scaffolding for closed operators and quotient
completion; the native Green form is the action-linked indefinite boundary
form. Equating them would silently replace the program-native pairing with an
auxiliary one.

Conversely, the zero native norm is not a proof that no physical positive
space can ever exist. A new action-owned fundamental symmetry, constrained
reduction, contour, boundary law, observable construction, or quantum BRST
measure could supply additional data. It would have to state its carrier map,
preserve or deliberately replace the closed domain, descend through gauge,
give a positive quotient form, and prove dynamical conservation.

## Ownership and claim ceiling

The strict quadratic bulk action admits both hit and miss trace choices and
selects neither. The filed primary source remains silent on endpoint,
asymptotic, real/Krein, fundamental-symmetry and probability selectors. The
miss choice removes the marked state; the hit choice retains a native-null
class; a positive completion adds an unowned metric choice. These are three
separate decisions.

This result proves current-packet nonselection. It does not prove a universal
Krein-space no-go, compare total hit/miss cohomology, establish Hilbert
self-adjointness or Fredholmness, construct a quantum BRST measure or Fock
space, select the global geometry, identify particles, move canon, or decide
GU.

## Reproduction and continuation

`tests/channel-swings/b5_krein_probability_positivity_probe.py` certifies the
exact Witt form, the rational family `J_r`, positivity of `GJ_r`, failure of
hit-line preservation, nonuniqueness of auxiliary quotient norms, extension
sensitivity, planted hostile controls, and the claim ceiling.

Reopen this positivity branch only with
`B5-PHYSICAL-PAIRING-OWNER-PACKET`: a typed action/source-owned fundamental
symmetry, constrained reduction, contour or boundary law that names the
carrier map, selects a closed domain, descends through gauge, produces a
positive quotient pairing, and proves its dynamical conservation. Until such
a packet exists, reconstruct the broader research frontier rather than
retesting free positive metrics.
