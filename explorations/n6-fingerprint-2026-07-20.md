---
artifact_type: exploration
doc_type: results_of_preregistered_node
status: "Phase-1 execution of pre-registered node N6 of the dictionary gate tree (the fingerprint hunt). OUTCOME: ABSENT -- the prediction is WELL-POSED (the canonical Z/3 from the N4 data exists, unique up to exactly the admissible inversion) and its equivariant decomposition of ker(Gamma) CONTRADICTS the pre-bound cyclic-permutation pattern: the commutant Z/3 acts fixed-point-freely (division-algebra theorem), trivial-character multiplicity 0, vs the predicted regular pattern. The order-reading's second statement fails as bound; the dictionary gate stays OPEN; no program-kill."
created: 2026-07-20
directed_by: "Joe direct chat, 2026-07-20 (N6 fired: 'We are seeking truth here')"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
preregistration: "explorations/phase0-dictionary-gate-tree-2026-07-20.md, node N6; prediction armed by explorations/n4-two-z3s-2026-07-20.md Section 4.2 (order-3 cyclic permutation of the triplet's 3 (generation, mirror) pairs per 16-unit in ker(Gamma), characters pinned via Phi onto {0, 8nu, 16nu}, determinate up to exactly the inversion {Phi_D, Phi_B}); executed as bound, no mid-flight scope change"
probe: tests/channel-swings/n6_fingerprint_probe.py
grade: "COMPUTED (float64 with exact structural anchors; several certificates land at literal 0.0; 28 checks = 8 [T] + 12 [E] + 8 [F], exit 0, deterministic seeded). The load-bearing negative (trivial-character multiplicity 0) is theorem-grade: H is a division algebra, so the frozen commutant acts freely. No claim/canon/verdict/posture movement; the dictionary gate (H6 limit b) stays OPEN."
related:
  - explorations/phase0-dictionary-gate-tree-2026-07-20.md
  - explorations/n4-two-z3s-2026-07-20.md
  - explorations/torsion-generation-arena-2026-07-20.md
  - explorations/verify-torsion-arena-2026-07-20.md
  - explorations/hardening-h3-triplet-lift-2026-07-19.md
  - canon/order3-equivariant-rho-RESULTS.md
  - canon/gamma-traceless-38-adjudication-RESULTS.md
  - canon/no-go-quaternionic-parity-generation-sector.md
  - canon/ghost-parity-krein-synthesis.md
  - explorations/blockbuster-p4-generation-doors-2026-07-19.md
  - lab/sources/claim-mining-toe-weinstein-2026-07-20.md
---

# N6: the fingerprint hunt (pre-registered node, executed)

## 0. What was pre-bound, and what fired

The prediction, armed before this node ran and not modifiable by it (N4 doc Section 4.2 +
tree node N6): the order-reading's second statement predicts an ORDER-3 CYCLIC FINGERPRINT
in ker(Gamma) -- an order-3 action cyclically permuting the triplet's 3 (generation,
mirror) pairs per 16-unit, with the character assignment pinned by the N4 identification
(rho characters (0,1,2)/3 Dirac and (0,2,1)/3 carrier-B, mapped through Phi onto
`<J(k)> = {0, 8nu, 16nu}`), determinate up to EXACTLY the admissible inversion
`{Phi_D, Phi_B} = Aut(Z/3)` and nothing else. Any further choice = failure as bound.

Pre-declared outcomes: FOUND / ABSENT / ILL-POSED / PARTIAL.

**Outcome fired: ABSENT.** The prediction is well-posed -- the canonical Z/3 action built
from the N4-identified data exists and is unique up to exactly the admissible ambiguity
(Section 1) -- and its equivariant decomposition contradicts the pattern uniformly: the
canonical action has trivial-character multiplicity ZERO on all of ker(Gamma) (it is
fixed-point-free, a division-algebra theorem), while a cyclic permutation of 3 pair-slots
must carry the trivial character with full multiplicity (the invariant diagonal). Every
pair-slot-triple reads (0, 6, 0) against the predicted regular (2, 2, 2); no block
matches, so this is ABSENT, not PARTIAL. Probe: 28 checks, exit 0.

## 1. Well-posedness: the canonical Z/3, constructed (not invented)

The N4-identified data lives on the frozen commutant. Frozen inputs, all verified exactly
in the probe: the Cl(9,5) = M(64,H) rep (gammas exact, `Gamma Gamma^dag = 14 I` at literal
0.0); `C = e1 e3 e5 e7 e10 e12` with `J_quat = C . conj`, `J^2 = -1` exact, ALL 14 gammas
J-commuting exactly (canon C07 convention); the real commutant is therefore the frozen
quaternion algebra `H = span{1, i, J, iJ}` (relations machine-checked), whose unit group
is the commutant Sp(1) -- the same object whose twist integer k = +-64 generated the arena
class in the first place. The C-linear commutant is scalars only (`Cl(9,5) x C = M(128,C)`
is full; monomial trace-orthogonality sampled exactly).

**The canonical Z/3.** Order-3 subgroups of the commutant Sp(1) are generated by
`u = cos(2pi/3) + sin(2pi/3) xi`, `xi` a unit imaginary quaternion. Three exact facts make
the induced action on ker(Gamma) canonical:

1. Every representative is in the commutant (commutes with all 14 gammas, hence with
   Gamma, Pi_RS, the SU(2)+ generators, and the Casimir): the action restricts to
   ker(Gamma) and to the 192-dim triplet with no leak. Verified for the scalar and three
   generic representatives.
2. All order-3 elements of Sp(1) form ONE conjugacy class (complexified spinor-factor
   trace = -128 for every representative, computed) -- the equivariant decomposition is
   representative-independent.
3. The residual labeling freedom is exactly `u <-> u^2` = Aut(Z/3), realized by the
   C-linear representative pair `{omega I, omega^2 I}` -- precisely the admissible
   inversion `{Phi_D, Phi_B}` and nothing else.

So ILL-POSED does not fire: the order-reading's second statement WAS well-formed. The
canonical action exists at exactly the ambiguity level the binding allows.

## 2. The decomposition

| space | predicted (regular / permutation) | canonical action (computed) |
|---|---|---|
| ker(Gamma), complexified (dim 3328) | trivial character present with full multiplicity | `(m_1, m_w, m_w2) = (0, 1664, 1664)` exact |
| triplet (192), complexified | (128, 128, 128) | `(0, 192, 192)` exact |
| triplet (192), C-linear representative | (64, 64, 64) | `(0, 192, 0)` (all-omega; inversion partner `(0, 0, 192)`) |
| per pair-slot-triple (3 pairs, 6 dims) | (2, 2, 2) | (0, 6, 0) -- every one of the 32 triples, uniformly |
| weight-space character assignment | bijection onto {1, w, w2} | constant (w, w, w): not bijective |

The trivial-character zero is not numerics; it is structure. `H` is a division algebra:
`x u = x` with `u != 1` forces `x = 0` in `H^64`. Every nontrivial element of the frozen
commutant acts FREELY on the carrier, hence on ker(Gamma). A cyclic permutation of three
pair-slots always fixes the diagonal (trivial character, multiplicity = slot dimension).
**Freeness and the permutation fingerprint are mutually exclusive** -- the canonical
commutant Z/3 rotates the quaternionic fiber; it provably cannot permute slots. No
recomputation on these fixtures can flip this: flipping requires new frozen structure.

## 3. Match verdict: ABSENT (and what ABSENT does not say)

The match test (two tiers, both pre-implied by the bound wording -- tier-1: regular
multiplicities (64,64,64) on the triplet; tier-2: the three frozen SU(2)+ weight-64
pair-slot spaces preserved, scalar per slot, characters a bijection onto Z/3-hat up to
global inversion only) fails at tier-1 AND tier-2 for the canonical action, identically
under inversion. ABSENT as pre-declared: the decomposition contradicts the pattern; the
order-reading loses its anchor.

**The boundary finding (recorded as boundary, not result).** The regular pattern IS
realizable inside ker(Gamma): `g = exp((2pi/3) J_3)` (an SU(2)+ rotation in the frozen
factor-2 normalization) is order 3 on ALL of C^1792, preserves ker(Gamma) and the triplet,
and matches BOTH tiers (multiplicities (64,64,64); weight characters bijective; the
`g -> g^2` swap is exactly Aut(Z/3)). But: (i) g requires an SU(2)+ axis choice -- an S^2
family, two members exhibited with identical match -- so the matching actions are NOT
determinate up to the inversion alone; (ii) g is not in the commutant
(`||[g, gamma]|| = O(10)`) and is not built from the N4-identified data. Per the binding
("if the match requires any further choice, that is a FAILURE of the prediction as
bound"), this is failure, doubly: the canonical action mismatches, and the matching
actions are non-canonical. What the finding types precisely: the fingerprint fails not
because ker(Gamma) cannot carry the pattern, but because no frozen datum converts the
commutant's fiber rotation into a slot permutation -- the missing bridge is a
transport/torsor-shaped object, the same demand shape as ID-1/B.5. That is reframe fuel
for a FUTURE pre-registered node, not a rescue of this one.

**Separation discipline (claim-mining Section 1).** Weinstein's spoken kernel lead is
arena-level only: ker(Gamma) as author-stated generation home, same map, six-slot
verified. The fingerprint prediction is repo-originated. ABSENT refutes the repo's
order-reading second statement; it neither confirms nor refutes his whole-kernel labeling,
which never mentioned an order-3 permutation.

## 4. Controls (all four, demonstrated power)

1. **Scrambled characters**: the weighted scramble (128,64,0) fails against the planted
   positive's data; the non-homomorphic assignment (0, 1/3, 1/3) is rejected by the
   bijection gate; Z/9 characters (0, 1/9, 2/9) are rejected (not cube roots); a
   blockwise-INCONSISTENT inversion (omega on both weight +-2 spaces -- not a global
   inversion) fails the match. Only the global inversion is admissible, as bound.
2. **Specificity**: five seeded random order-3 unitary actions on the triplet -- none
   matches. The adversarial case, an equal-multiplicity Haar-rotated action, passes
   tier-1 but fails tier-2 (weight-space leak 4.6): right character content alone does
   NOT reproduce the fingerprint; the frozen-slot alignment is load-bearing.
3. **Inversion check**: `g -> g^2` maps the weight-character assignment through exactly
   `k -> -k mod 3` with the verdict unchanged -- the `Phi_D <-> Phi_B` swap of N4, on the
   nose; likewise the canonical pair `{omega I, omega^2 I}` swaps (0,192,0) <-> (0,0,192).
4. **Power validation**: a corrupted gamma fixture is flagged by the structural gate;
   a corrupted planted fixture (one weight-0 eigenvalue surgically moved to omega) is
   flagged by the match test ((63,65,64)); an order-5 imposter is rejected at the order
   gate. And the planted positive itself proves the test CAN return FOUND on these exact
   fixtures -- ABSENT was not baked in.

## 5. Null expectation (computed, though outcome is not FOUND)

Under a uniform-composition null on the complex triplet (multiplicity vectors of 192 over
3 characters, `C(194,2) = 18721` compositions): P(regular (64,64,64)) = 1/18721 = 5.3e-5;
P(trivial multiplicity exactly 0) = 193/18721 = 1.03e-2. Per-slot refinement: independent
uniform triples give (1/28)^32 ~ 1e-46 for the full slot-aligned pattern; weight-space
preservation is measure-zero for Haar actions. Two consequences: (i) had FOUND fired, it
would not have been dimension-forced -- the same 192 dimensions demonstrably carry both a
matching and a non-matching order-3 action, and the equal-multiplicity Haar control shows
character content alone does not match; (ii) the observed canonical outcome (trivial
multiplicity 0) is itself non-generic under the null -- it is not noise, it is the
division-algebra theorem showing through.

## 6. Five-lens council (inline, answered in writing)

**Representation theorist.** The computation is the isotypic decomposition of ker(Gamma)
under the canonical Z/3, and the two shapes in play are the extreme opposites available on
the same dimension: the regular/permutation shape (induced from the trivial rep of the
trivial subgroup; trivial isotype = the invariant diagonal, always present) versus the
free/fiber shape (single-character-pair, trivial isotype empty). The bound prediction was
the first; the frozen commutant delivers the second, and MUST: freeness of a division
algebra's unit action is basis- and representative-independent. The well-posedness
construction is honest -- conjugacy-invariance of multiplicities was computed, not
assumed, and the C-linear-commutant-is-scalars step rests on the sampled exact
monomial-orthogonality certificate plus the standard Cl_14(C) = M(128,C) fact. One
discipline note honored: tier-2's "bijective weight characters" is the correct
operationalization of "cyclic permutation up to equivalence" (diag(1,w,w2) in the slot
basis IS the 3-cycle's spectrum); no stronger literal-permutation demand was smuggled in.
Passed.

**Nikulin/K3 specialist.** Instructive geometric contrast: the monodromy Z/3 on the K3
benchmark is a symplectic automorphism WITH fixed points (six -- the adjudicated canon
input), and its rho data is precisely fixed-point/asymmetry data. The commutant Z/3 acts
freely. The N4 character-set identification could not see this -- character sets of Z/3
in Q/Z carry no fixed-point information -- and N6 is exactly where the difference bites:
the transported prediction needed invariant content in ker(Gamma) and the free action has
none. This does not retro-invalidate N4 (the identification stands as computed, at
character-set level); it shows the two Z/3s differ at the next structural level down,
which is what N4's own referee flagged as risk (f). The benchmark caveat is unchanged: no
geometric map between the K3 side and ker(Gamma) exists in frozen content, and this node
neither found nor needed one. Passed.

**GU-native geometer.** What the program keeps: the arena class (order 3, hostile-
verified), the N4 character identification, the full triplet/Krein structure, and now a
sharp new type fact -- the frozen commutant's Z/3 is a FIBER symmetry (free, scalar-type
on slots), and the generation-permuting symmetry the order-reading wanted must be a BASE/
slot symmetry (SU(2)+-axis-type), and no frozen object links the two. The missing link is
transport-shaped: exactly the ID-1/B.5 demand silhouette, now visible from a second
independent direction. What the program loses: the order-reading's only receipt-backed
second statement in frozen content. Honest native accounting: the S^2 of matching
SU(2)+ rotations means the pattern's home exists but its selection is unpinned -- the
same "external datum" shape the program keeps meeting (SG4, the one-Z/2, the gap import).
Passed.

**Statistician.** The null expectation is computed (Section 5) and the design has
demonstrated two-sided power: the pipeline returns FOUND on the planted positive and
flags every corruption, scramble, and imposter; ABSENT is therefore a discriminating
verdict, not a null machine. Specificity is quantified: tier-1 alone would pass 1-in-19k
random compositions and the deliberately-adversarial equal-multiplicity action; tier-2's
frozen-slot alignment reduces the accidental-match set to measure zero. The single
caveat to record: the uniform-composition null is a convenience model; its role here is
only to show FOUND would not have been cheap and ABSENT's specific shape (trivial = 0) is
itself extreme -- both conclusions are robust to reasonable null swaps because the
load-bearing facts are exact. Passed.

**Adversarial referee.** Attacks run: (a) *convenient action?* -- no freedom existed: the
C-linear commutant is scalars, the real commutant is the frozen H, order-3 elements form
one conjugacy class; every representative computed identically. (b) *ABSENT an artifact
of C-linearity conventions?* -- no: the complexified R-linear decomposition (0,1664,1664)
lacks the trivial character exactly as the C-linear one does; freeness is convention-
independent. (c) *fingerprint elsewhere in ker(Gamma), outside the triplet?* -- excluded:
freeness kills the trivial character on ALL of ker(Gamma), and any permutation fingerprint
anywhere requires it; the absence is global. (d) *is the planted positive a backdoor
FOUND?* -- no: it fails the binding on the exhibited S^2 degeneracy and non-commutant
provenance; it is boundary data. (e) *does ABSENT contradict N4?* -- no: N4's SURVIVE was
character-set identification plus a prediction; N6 tested the prediction and it failed;
both nodes stand as computed, and N4's recorded live risk (f) ("two unrelated Z/3s read
on one dial") is now STRENGTHENED at the action level -- the skeptic's position gains
ground and the file says so plainly. (f) *strongest surviving pro-reading objection*: the
binding was tight, and a looser prediction (allowing one new axis datum) would have
matched -- recorded, but that is exactly what "failure of the prediction as bound" means;
a loosened prediction is a NEW node with its own pre-registration, not this one. Passed
as declared.

## 7. What ABSENT does to the tree

- **Node N6 (this node): the KILL branch fired** as pre-declared -- "fingerprint provably
  absent from ker(Gamma) structure" (here: provably absent for the canonical action, with
  the absence theorem-grade). The dictionary line does NOT graduate to a claim candidate.
- **The order-reading** loses its discriminating second statement. Per the tree's own
  logic it drops from "physics-grade candidate" toward the convention side UNLESS a new
  frozen structure supplies a different second statement; its remaining distinguisher
  over the sylow-reading is order-sensitivity only.
- **Program-kill does NOT trigger.** The tree's Section 6 requires the N4 kill AND the N5
  kill; N4 SURVIVED (character identification stands) and N5 (RF-1, the return-period
  functional, gated on ID-1) is untested and now carries the dictionary line's remaining
  physics-grade hope essentially alone.
- **Conditional-forcing line (sibling work, their file):** stated plainly for their
  intake -- any forcing argument that leaned on "the order-reading has a receipt-backed
  second statement in frozen content" must reroute through N5/RF-1 or through a NEW
  pre-registered import (a transport datum tying the commutant Z/3 to an SU(2)+ axis).
  The arena class itself and the N4 identification are untouched inputs.
- **The e_KO/rho character math is untouched**; what died is specifically the claim that
  the frozen content already contains the order-3 action on generation slots.

Since the outcome is not FOUND, the pre-committed inline adversarial section for FOUND
and the mandatory hostile-verification dispatch for a positive do not apply; the
orchestrator may of course still hostile-verify this ABSENT (the load-bearing steps are
cheap to re-derive: freeness is one line of division-algebra arithmetic).

## 8. Receipts

- `tests/channel-swings/n6_fingerprint_probe.py`: 28 checks = 8 [T] + 12 [E] + 8 [F],
  ALL PASS, exit 0, 23.8 s, deterministic (seeded), float64 with exact structural anchors
  (`Gamma Gamma^dag = 14 I`, `C Cbar = -I`, all-14 J-commutation at literal 0.0;
  multiplicities land on exact integers). HEADLINE emitted by the probe names the outcome.
- Frozen inputs read in full this session: the tree (node N6 and Sections 1-2, 5-7), the
  N4 doc, the torsion-arena hostile verification, the H3 triplet-lift doc and
  ghost-parity fixtures, claim-mining Section 1, blockbuster-p4 Door A ledger lines.
- Pre-registration honored: the bound prediction was not modified; the four controls ran;
  the outcome is one of the four pre-declared branches; the boundary finding is filed as
  boundary, not smuggled into the verdict.

## 9. Boundary

Exploration-grade execution of a pre-registered node. No claim, canon, verdict, or public
posture movement; no edits to any existing file; no commits. The dictionary gate (H6
limit b) remains OPEN. ABSENT is scoped exactly: the canonical Z/3 built from the
N4-identified frozen data does not carry -- and provably cannot carry -- the pre-bound
permutation fingerprint on ker(Gamma); the pattern itself is realizable there only via
actions requiring an axis datum the binding forbids and frozen content does not supply.
Everything remains conditional on the framed-reading fork and SG4, as the parent docs
already were. Pauses for Joe; any loosened/reframed fingerprint prediction is a new
Joe-gated node.
