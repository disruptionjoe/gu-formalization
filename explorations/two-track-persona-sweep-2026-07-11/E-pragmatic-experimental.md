---
artifact_type: exploration
status: exploration
created: 2026-07-12
title: "Two-track persona sweep, slice E: the pragmatic / commercial / experimental / phenomenology family (Track-2 owners)"
depends_on:
  - explorations/science-advisory-council-full-picture-2026-07-11.md
  - explorations/four-legs-resolved-gu-live-candidate-2026-07-11.md
  - explorations/disproof-hunt-given-working-action-2026-07-11.md
  - explorations/predictive-boundary-audit-2026-07-12.md
  - explorations/non-lnf-open-frontier-prioritized-2026-07-11.md
  - explorations/wave20/H43-de-shape-falsifier-2026-07-11.md
  - explorations/wave25/H44-de-backreacted-background-2026-07-11.md
---

# Slice E: the pragmatic / commercial / experimental / phenomenology family

The Track-2 owners. This family does not adjudicate whether the source action can be built; it asks what
NUMBER the program can put on the table this week that a hostile experimenter could try to kill. Personas are
inline (one worker), instantiated from the repo rosters (science-advisory Commercial scientist; the persona
sweeps). Ground truth for the state assessed: DE `(w0,wa)` CPL comparison is FALSIFIED-FULL-STOP vs DESI DR2
(H43/H44, 3.2 sigma floor, every rescue route spent) BUT the raw non-CPL `H(z)` still mimics DESI distances to
`~1.3%` and is UNTESTED against the actual DESI DR2 BAO likelihood; gravity is a conditional theorem (PPN
passes, gated on `mu_DW`, loop unitarity open); count located-not-forced at `{1,3}`; `|II|^2` favored-but-gated;
the Bianconi rival (arXiv:2408.14391) is empirically distinguishable in the spin-2/Weyl sector; H6 is a
GU-independent family-puzzle theorem candidate; ZERO parameter-free predictions; everything routes to the one
unforceable source action.

---

## The voices (each on Q1-Q4, one liner apiece)

### 1. Commercial scientist (science-advisory roster) -- "you keep buying maps when you could buy a measurement"
- **Q1:** The local minimum is not "narrowing onto a keystone"; it is that the program's default deliverable is a
  RESIDUAL-FREEDOM MAP, and number-extraction only happens as a special adversarial event (the disproof-hunt).
  Refinement is cheaper than measurement, so the program keeps buying refinement.
- **Q2:** Cheapest decisive item on the board is already built and unspent: the DE raw-`H(z)` against the actual
  DESI DR2 BAO likelihood. The `H(z)` exists (H44 backreacted), the likelihood is public, it is a chi-squared
  eval, and it can KILL. Run it before anything else.
- **Q3:** After DE, the next thing a referee reaches for is another DATA confrontation, not an internal proof:
  strong-field gravity (does GU host an exact isolated black hole).
- **Q4:** Yes. Track-2 hands the North Star a shopping list of which postulates the real action must reproduce,
  and kills the ones the data already excludes so the build never chases them.

### 2. Observational cosmologist, DESI/CMB (BAO distances) -- "the CPL contour is a projection; you have not met the data yet"
- **Q1:** The program mistook a projection-comparison (CPL `(w0,wa)`) for the data. Falsifying the projection is
  real, but the program has still never confronted the actual BAO+CMB likelihood, so it has neither a prediction
  nor a clean death at the distance level.
- **Q2:** THE test: fit the backreacted GU `H(z)` to the DESI DR2 BAO table (`D_M/r_d`, `D_H/r_d`, `D_V/r_d` per
  redshift bin) with a CMB `Omega_m`/`r_d` prior. The `~1.3%` distance match either survives the covariance or
  it does not. Cheap (public data, built `H(z)`), and it can kill.
- **Q3:** If the full-likelihood fit passes only by making GU LCDM-degenerate, the referee's next kill is that
  GU DE emits no distinguishing coefficient -- an "unfalsifiable at the distance level" verdict is its own death.
- **Q4:** Track-2 returns a hard number: a chi-squared and a p-value against DESI DR2. That is a falsifier the
  North Star can carry whether or not the source action is ever written.

### 3. Gravitational-wave phenomenologist -- "the spin-2/Weyl sector is where GU and Bianconi actually diverge"
- **Q1:** The program has a fourth-order (Bach/conformal) graviton and has never asked it for a DISPERSION
  RELATION. That is a number-shaped question being left as a structural label.
- **Q2:** Derive the GW dispersion and graviton propagator pole structure of the conditional theory (declare the
  `|II|^2` lean and `mu_DW` as postulates). Fourth-order gravity generically adds a massive spin-2 pole ->
  modified `v_g(f)` and extra polarizations. This is the cleanest observational FORK vs Bianconi's information-
  first gravity, testable against LIGO/Virgo/LISA bounds. Cheap to derive; decisiveness gated on where `mu_DW`
  puts the mass.
- **Q3:** The single cleanest kill in my sector: if `mu_DW` forces a massive-graviton / dispersion scale already
  excluded by GW170817 `|v_g/c - 1| < 10^-15`, GU dies the same way DE did -- on data, no source action needed.
- **Q4:** Track-2 delivers a `mu_DW`-parametrized dispersion prediction. The North Star inherits either a first
  genuine parameter-linked GU prediction or a clean observational exclusion band on `mu_DW`.

### 4. Collider / particle phenomenologist -- "a vectorlike mirror at 1.3-1.5 TeV is a prediction you are not stating"
- **Q1:** The program has a forced vectorlike mirror generation (SM leg) whose mass is a free modulus, and it
  files this as "consistent, incomplete" rather than as a mass-window prediction. It refuses to name the number
  because the number is not forced -- but a declared-postulate branch could name it.
- **Q2:** Under the declared vacuum-selection postulate, emit the mirror-sector mass window and its collider
  signature (vectorlike quarks/leptons, `16 + 16bar`). Medium cheapness, low near-term decisiveness: it credits
  more than it kills, and the lane is crowded. Rank below the cosmology and GW forks.
- **Q3:** Not my sector's kill; collider absence of a free-mass modulus never falsifies (you raise the mass).
  This is why the mirror is not the referee's next shot.
- **Q4:** Track-2 can return a "if GU, then vectorlike matter below X TeV" conditional -- useful as a
  deprioritized standing bet, not a keystone deliverable.

### 5. Precision-tests experimentalist (solar-system PPN, fifth force, EP) -- "the Stelle-Yukawa scale is a live table lookup"
- **Q1:** Weak-field PPN already PASSES -- that is the program's one quiet real contact with data, and it is
  buried under keystone-narrowing prose. The local minimum is that a passing conditional theorem is treated as
  less interesting than another map.
- **Q2:** A Bach/conformal graviton yields a Stelle-Yukawa modification `~ (1/r)(1 + a e^{-m r})` of the
  Newtonian potential. Declare `mu_DW` as a postulate, derive the Yukawa range and strength, and check it
  against Eot-Wash / lunar-laser-ranging / planetary-ephemeris fifth-force bounds. This is the CHEAPEST possible
  decisive test: a table lookup once `m(mu_DW)` is in hand, and existing bounds can exclude whole `mu_DW` bands.
- **Q3:** The referee's cleanest kill co-owned with GW: if the Yukawa range from `mu_DW` sits in the exclusion
  window of existing fifth-force experiments, GU is dead on the bench, no source action required.
- **Q4:** Track-2 returns an EXCLUSION BAND on `mu_DW` from lab + solar-system data. That directly constrains the
  North Star's source-action build before a line of it is written.

### 6. Model-builder (BSM) -- "declare the leans as postulates and you have a theory tonight"
- **Q1:** The program conflates "not forced from first principles" with "not usable." A model-builder declares
  the leans (`|II|^2`, the soldering `A = spin-lift(grad gimmel)`, `mu_DW`, the vacuum) as POSTULATES and builds
  the conditional theory. The local minimum is the refusal to postulate, which is what keeps predictions at zero.
- **Q2:** The conditional build's output menu, in order: DE full-likelihood fit; GW/Yukawa `mu_DW` prediction;
  count 1-vs-3 under the declared field-space postulate; then Yukawa/flavor. The first three are cheap because
  the objects exist; only they need declaring, not building.
- **Q3:** Internally, the count: if the declared field-space postulate still yields `{1,3}` and cannot pin `3`,
  that is a structural exposure -- the postulate the program most wants is the one that under-determines.
- **Q4:** Yes -- the conditional build IS the two-track bridge. It returns to the North Star a labeled list:
  which postulates, once assumed, produce data-contact, and which produce only more residue.

### 7. Forecaster / Bayesian model-comparison -- "you have no likelihood, so you have no evidence, only stories"
- **Q1:** The deepest local minimum in Bayesian terms: the program computes no LIKELIHOOD, so it can never update.
  It accumulates consistency narratives (unnormalized, unfalsifiable) instead of evidence ratios. DE was the one
  place a likelihood-shaped object appeared, and it promptly falsified -- proving the machinery works and is
  simply not being run elsewhere.
- **Q2:** Ranked by decisiveness-per-cost, the ONLY item that yields a proper evidence ratio now is the DE
  raw-`H(z)` vs DESI DR2 BAO likelihood (plus CMB prior): it gives `p(data|GU)` vs `p(data|LCDM)`, a real Bayes
  factor. The GW/Yukawa fork gives a second likelihood once `mu_DW` is declared. Everything else is prior-only.
- **Q3:** After DE, the cleanest falsifier is whichever sector next admits a likelihood: strong-field gravity
  (black-hole shadow residual is binary) edges out loop unitarity because the latter risks an inconclusive
  Stelle-Mannheim verdict -- a referee wants a clean kill, not a decades-old debate.
- **Q4:** Track-2 delivers Bayes factors and exclusion bands; the North Star keeps the model prior. The two-track
  split is exactly a prior (North Star) / likelihood (Track-2) division of labor. Run both.

### 8. Science-strategy / portfolio lens -- "you are 100% invested in one illiquid asset"
- **Q1:** The portfolio is fully concentrated in a single unforceable asset (the source action) with zero yield
  (zero predictions). The local minimum is a concentration risk the program keeps DOUBLING DOWN on with each
  narrowing pass, because narrowing feels like progress while paying no dividend.
- **Q2:** Diversify into the cheap-and-decisive quadrant: DE full-likelihood (liquid, public data), GW/Yukawa
  `mu_DW` prediction (one derivation), H6 family-puzzle theorem (GU-independent, uncorrelated with GU's fate).
  These are the only positions that pay regardless of the keystone.
- **Q3:** The single highest-Sharpe kill: strong-field gravity, because it is binary and near-term. Loop
  unitarity is high-variance; DE is spent at the CPL level.
- **Q4:** Two-track is correct portfolio construction: keep a long position on the North Star (high-upside,
  illiquid) while running Track-2 as the liquid sleeve that generates cash-flow falsifiers. The North Star gets
  de-risked by every Track-2 verdict.

### 9. Referee for a flagship journal (PRD/PRL) -- "show me one number you predicted before you saw it"
- **Q1:** As a referee I read "located-not-forced" and "one-residual compression" as an admission that the paper
  has NO prediction. The local minimum, stated plainly: the program has confused a beautiful residual-freedom
  map for a result. A compression is not a prediction.
- **Q2:** I would accept exactly one of these as a real result: (a) a full DESI DR2 BAO likelihood fit with a
  stated Bayes factor, or (b) a `mu_DW`-parametrized Stelle-Yukawa / GW-dispersion prediction with an exclusion
  plot. Both are cheap and both can kill. The count and Yukawa work is not yet paper-grade.
- **Q3:** My first kill shot after DE: the strong-field / exact-Schwarzschild section. "This candidate theory of
  gravity cannot host an observed isolated black hole" is a one-line desk-reject and it is BINARY. That is where
  I aim first. Loop unitarity is my second shot, held in reserve because it may not resolve cleanly.
- **Q4:** Keep the North Star as your discussion section; put Track-2 in your results section. A referee needs
  results. The conditional-build branch is what makes the paper publishable at all.

### 10. Data analyst / likelihood specialist -- "the 1.3% is a headline number, not a fit"
- **Q1:** The `~1.3%` RMS distance agreement is an eyeball statistic computed against a CPL proxy `H(z)`, not a
  fit to binned data with a covariance. The local minimum here is stopping at the headline number instead of the
  covariance-weighted chi-squared.
- **Q2:** The decisive object: take the H44 backreacted `H(z)`, compute `D_M/r_d`, `D_H/r_d`, `D_V/r_d` at the
  DESI DR2 effective redshifts, contract with the published covariance, add the CMB `theta_star` constraint,
  report chi-squared / dof and the Bayes factor vs LCDM. This is a day of work and it is the sharpest DE kill or
  survival available.
- **Q3:** If that fit passes at LCDM-degenerate quality, the honest verdict is "indistinguishable from LCDM at
  DESI DR2 precision" -- which for a theory claiming to DERIVE dark energy is a soft kill (no predictive content).
- **Q4:** Track-2 returns the actual `chi^2`, dof, covariance-weighted residuals, and Bayes factor. That is the
  most citeable falsifier the program could hold, and it needs no source action.

### 11. Numerical / strong-field relativist -- "the black-hole test is the one binary you have"
- **Q1:** The program has an isolated, sharply-posed strong-field question (ELProjectedGRShadow: does the
  projected section residual vanish on exact Schwarzschild/Kerr) and keeps it BLOCKED behind source-action prose
  rather than running it on the surviving conservative IG branch. That deferral is the local minimum in my sector.
- **Q2:** Run the projected residual `R = alpha_W W_s + E_s^YM + E_s^theta + E_s^Phi + E_s^cross` on an imported
  exact Schwarzschild section using the surviving IG branch. Cheap (import a known metric, evaluate a residual),
  and BINARY: vanishes -> gravity clears; provably nonzero for every admissible config -> GU cannot host a black
  hole -> DEAD. It is gated on `mu_DW`/the branch, so not fully postulate-free.
- **Q3:** This IS the single cleanest Track-1 kill next in line. Weak-field PPN passed; the next data-adjacent
  binary is strong-field, and its failure is a whole-GU death (observed black holes exist).
- **Q4:** Track-2 declares `mu_DW` and runs it now instead of waiting for the action. Either outcome (clear or
  kill) is a hard deliverable back to the North Star.

### 12. Neutrino / flavor phenomenologist (family-puzzle owner) -- "H6 is the one result that survives GU being wrong"
- **Q1:** The program under-invests in its only GU-INDEPENDENT asset. H6 ("a theory that forces an odd family
  count must have nonzero 3-Sylow image") bears on a real open problem (why three families) and stands whether
  or not GU is right, yet it is queued behind keystone work. That misallocation is the local minimum here.
- **Q2:** Sharpen H6 into a precise proposition with the group-theory census verified against primary sources
  (the primary-partition lemma is already computed, exit 0). Cheap (finite group theory), decisive AS A THEOREM,
  but it does NOT test GU -- it is a credibility deliverable, not a GU falsifier. Rank high for portfolio value,
  outside the GU kill-chain.
- **Q3:** Not a GU kill (by construction it is GU-independent). Its role is to make the program citeable even in
  the world where GU dies.
- **Q4:** Track-2 ships H6 as a standalone result. The North Star benefits indirectly: a credible GU-independent
  theorem buys the program the standing to keep the high-risk North Star alive.

---

## Slice synthesis (pragmatic / experimental family)

### The local minimum, named
**The theory-refinement treadmill: the program's DEFAULT deliverable is a residual-freedom MAP, and
number-extraction is treated as a rare adversarial event rather than the standing mode.** The working diagnosis
("narrowing onto an unforceable keystone -> zero predictions") is the SYMPTOM. The disease, from the
pragmatic/experimental view, is deeper and more curable: every pass is rewarded for producing a better map of
where the freedom lives (the one-residual compression), and none is rewarded for emitting a covariance-weighted
number. The tell is that the ONE time the program did emit numbers under pressure -- the DE waves -- the
machinery worked perfectly and produced a clean falsification (H43/H44). The program CAN measure; it simply
routes every question back to the unbuildable source action instead of declaring a postulate and measuring.
Concentration risk (one illiquid asset, zero yield) is the portfolio name for the same thing. The cure is not a
better map. It is to declare the leans as postulates and extract numbers.

### Track-2 testable deliverables, RANKED by cheap-and-decisive (a cheap test that can KILL outranks an expensive one that can only credit)

1. **DE raw-`H(z)` vs the actual DESI DR2 BAO likelihood (+CMB `Omega_m`/`r_d` prior).** CHEAPEST DECISIVE
   KILL-SHOT ON THE BOARD. The backreacted `H(z)` already exists (H44); the DESI DR2 BAO table and covariance are
   public; the work is contracting `D_M/r_d, D_H/r_d, D_V/r_d` with the covariance plus a CMB prior and reporting
   `chi^2`/dof and a Bayes factor vs LCDM. It is THE comparison the CPL falsification explicitly did NOT settle
   (H43/H44 name it as the single next object). Outcome is decisive both ways: the `~1.3%` distance match either
   fails the covariance (DE dead at the distance level, not just the CPL projection) or passes at
   LCDM-degenerate quality (a soft kill: GU DE has no distinguishing predictive content). One day of work.

2. **Spin-2/Weyl discriminator vs Bianconi: the `mu_DW`-parametrized Stelle-Yukawa potential + GW dispersion
   (+ conformal lensing).** Declare `|II|^2` and `mu_DW` as postulates and derive the massive-spin-2 pole ->
   (a) Stelle-Yukawa `(1/r)(1 + a e^{-mr})` checked against Eot-Wash / lunar-laser / ephemeris fifth-force
   bounds (a near-literal table lookup once `m(mu_DW)` is in hand), and (b) GW dispersion `v_g(f)` checked
   against GW170817's `|v_g/c-1| < 10^-15`. Cheap to DERIVE; produces the program's FIRST genuine parameter-
   linked number; and it is the real observational FORK between GU-geometry and Bianconi-information. Decisiveness
   is gated on where `mu_DW` lands: if it falls in an already-excluded band, this KILLS GU on the bench; if it
   falls in an open window, it becomes GU's first standing prediction. Ranked second only because its kill-power
   depends on the scale, where item 1 can kill unconditionally.

3. **H6 GU-independent family-puzzle theorem.** Cheap (finite group theory; primary-partition lemma already
   computed), decisive AS A THEOREM, and uncorrelated with GU's fate. It does NOT test GU (by construction), so
   it is a portfolio/credibility deliverable, not a link in the kill-chain -- but on cheap-and-decisive it ranks
   above the internal count work because it is a publishable result that survives GU being wrong.

4. **Count 1-vs-3 under the declared field-space postulate.** Cheap-ish and internally decisive for the count
   leg: declare the source-action field-space postulate and check whether it FORCES `3` or leaves `{1,3}`. Not
   observational; its most likely honest output ("the postulate we most want still under-determines") is itself
   informative. Below H6 because it credits/exposes internally rather than confronting data.

5. **Fermion / Yukawa structure (mirror mass window, mass ratios, mixing).** Expensive, GU-conditional, crowded
   lane, and it credits far more than it kills (a free-mass modulus never falsifies). Deprioritize to a standing
   conditional bet ("if GU, vectorlike matter below X TeV").

### The single cleanest Track-1 falsifier next in line after DE
**The strong-field / exact-Schwarzschild(-Kerr) section residual (ELProjectedGRShadowTheorem), run on the
surviving conservative IG branch.** It is the referee's first kill shot because it is (a) BINARY -- the projected
residual either vanishes on an imported exact black-hole metric or it does not; (b) a WHOLE-GU death on failure
-- "GU cannot host an observed isolated black hole" is a one-line desk-reject, and observed black holes exist;
and (c) the natural next DATA confrontation now that weak-field PPN has passed and DE has fallen -- a referee
who just watched the program die on cosmological data reaches for the next observational binary, not an internal
proof. Honest caveat: it is gated on `mu_DW` / the branch-fixed IG configuration, so it is not fully
postulate-free; declaring `mu_DW` (Track-2 discipline) is what lets it run now. The class-level runner-up is
LOOP-LEVEL GHOST UNITARITY of the fourth-order/Bach graviton (if the observer-geometry class carries a
propagating Ostrogradsky ghost the Krein repair cannot remove beyond tree level, the whole class is
non-unitary, GU-independent of the source action). It is held in reserve, not first, precisely because a
Stelle-Mannheim loop verdict risks being inconclusive, and a referee wants a clean kill.

### Two-track verdict
**Yes -- keep the North Star as posture and run the Track-2 conditional-build branch in parallel; this family
votes to run both, and to run Track-2 FIRST as the cheaper information source.** The North Star (find/derive the
true source action) is the model prior: high-upside, illiquid, unforceable. Track-2 is the likelihood sleeve:
declare the leans (`|II|^2`, soldering, `mu_DW`, the vacuum) as postulates, build the conditional theory, and
extract covariance-weighted numbers. What Track-2 delivers back to the North Star is concrete and
source-action-free: (1) a DESI DR2 full-BAO `chi^2`/Bayes factor on dark energy; (2) a `mu_DW`-parametrized
Stelle-Yukawa / GW-dispersion prediction that is either GU's first real prediction or an exclusion band on
`mu_DW`; (3) a binary strong-field gravity verdict; (4) a forced-or-under-determined verdict on the count under
the declared postulate; and (5) H6 as an uncorrelated GU-independent theorem. Each of these either KILLS a leg
(making the source-action build for that leg moot -- do not build what the data already excludes) or hands the
North Star a fixed number/falsifier the eventual action must reproduce. That is the entire pragmatic case: a
cheap test that can kill outranks an expensive build that can only credit, and Track-2 is where the cheap
kill-shots live.

### Grade / provenance
Interpretive strategic assessment; no new computation. Personas inline, grounded in the repo's actual state
(H43/H44 DE falsification and its named raw-`H(z)`/BAO scope; the predictive-boundary audit; the disproof-hunt;
the non-LNF frontier register). All proposed deliverables are research advancement of THIS repo (a
raw-likelihood FIT, a `mu_DW`-linked dispersion/Yukawa PREDICTION, a binary strong-field residual, a
finite-group theorem); none is an external/shipping item. No claim-ledger or canon movement asserted.
