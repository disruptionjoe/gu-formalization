# D. Wild-frontier / exotic-structure family: two-track persona sweep (2026-07-11/12)

One slice of the full-roster two-track sweep. This family carries the exotic-structure lenses:
homotopy/tmf, K-theory/index, exotic real-forms, Weinstein-native geometry, double-copy/amplitudes,
noncommutative geometry, derived/higher-category, integrable/hidden-symmetry, quantum-topology.
Personas run INLINE (one worker). Grounded in the actual repo state: Waves 1-26, the seven-axis count
map (`seven-axis-count-map-L0-L7`), the SG4 forcing rubric (`sg4-forcing-rubric-complete`), the
framed-bordism / image-of-J closures (`big-swing-2026-07-07/FB-F2`, `FB-F4`), and the conformal-Bach /
double-copy turn (`science-advisory-council-full-picture-2026-07-11`, the Weinstein transcript).

**Discipline (the H20 trap).** A forcing candidate must plausibly REDUCE the unforced-choice count, not
relabel it. H20 (even/odd unification) was a beautiful object that removed zero of four unforced choices;
this family will not repeat that trade. Every candidate below is graded FORCES / RELABELS / UNKNOWN
against the actual open freedoms: (i) the (9,5)-vs-(7,7) signature [2-primary], (ii) the SG4 carrier
A-vs-B K-class declaration [the count residual, {1,3}], (iii) the |II|^2 norm gate, (iv) the source
action itself [terminal object; the causal-cure / non-minimal RS coupling]. The count lives in the Z/3
(3-primary) arena; `gcd(2,3)=1`, so no 2-primary geometry move can reach it. That congruence is the hard
floor every persona here is tested against.

---

## Per-persona: Q1 (local minimum) / Q2 (Track-2 submit) / Q3 (Track-1 forcing object + one test) / Q4 (two-track meta)

**1. Wild frontier mathematician (science-advisory native).**
Q1: Agree the shortcut is dead, but the diagnosis under-names the register error. Every narrowing move
lives in the SAME register as the keystone (geometry/field-space). The untouched object is in a DIFFERENT
register -- the on-shell/kinematic one (Weinstein's "2nd order = square of 1st order"). That is the missed
object, not a deeper geometric minimum.
Q2: Submitting a purely conditional theory buries the one falsifiable thing (the conformal-Bach
identification) under four declared leans. Build it, but only as scaffold for the kinematic test.
Q3: The double-copy square (gravity = gauge^2). ONE test: is the GU source action the L-infinity/BV double
copy of a first-order gauge datum, and does color-kinematics FORCE carrier B?
Q4: Yes -- the build writes the fixed-coefficient action the double-copy test cannot run without.

**2. Homotopy / tmf theorist.**
Q1: Agree the shortcut is dead. The tempting "missed object" is tmf as the finer invariant that CAN
co-vary (floated in the double-major sweep against "e_R=1/12 is homotopy-fixed"). HONEST: tmf is an H20
trap. `pi_3 tmf` is still all torsion; refining `Im J = Z/24` to a tmf class (f-invariant, one chromatic
level up) hits the SAME `Hom(torsion, Z)=0` wall FB-F2 already proved at the framed-bordism level. tmf
relabels the count's arena finer; it removes zero freedoms.
Q2: A conditional build gains nothing from tmf. Skip tmf; it is a beautiful non-lever.
Q3: NOT tmf. If anything homotopical forces, it is not a spectrum-level refinement of the same torsion
class. The one honest homotopical contribution: certify that the count-as-class reading is dead at EVERY
chromatic height (it is), which frees effort for the integer-by-construction (index) route.
Q4: tmf does not feed either track; naming it dead is the contribution.

**3. K-theory / index theorist.**
Q1: Agree. The count's only well-typed home is an integer-by-construction relative/equivariant twisted-RS
index (FB-F2's surviving GATED route). The local minimum is real: the index EXISTS as an integer, but its
mod-3 value is `m^2 d' + sigma` and both imports are free (FB-F4). So the index is a home, not a forcer.
Q2: Building the conditional theory forces you to declare the boundary datum (frame: tangential-vs-gauge,
e=1/12 vs 3/8) the index needs. Useful.
Q3: A KO-theoretic / Bismut-Cheeger family index whose bulk is FIXED by the source action's positivity
(reflection positivity + normalizability), so `ind = bulk - e_R` is forced integer with forced residue.
ONE test: does the source-action positivity constraint pin the geometry-dependent bulk to a UNIQUE integer
(not just mod 1)? If yes, the count is forced by the index; if the bulk stays a free modulus, it relabels.
Q4: Yes -- the conditional build supplies the fixed-coefficient action whose bulk the index reads.

**4. Exotic-signature / real-form specialist.**
Q1: Agree, and add the sharp negative result: the signature axis (L7, (9,5) vs (7,7)) is a 2-primary
datum (`p-q mod 8 in Z/8`). Wave 14 proved (7,7) lifts the Kramers veto but is STRUCTURALLY INCAPABLE of
supplying the count -- `|Hom(Z/8,Z/3)| = 1`. So resolving the signature homotopically CANNOT force the
count. The signature is a red herring FOR THE COUNT (it still matters for gravity/conformal-nativeness).
Q2: The build must declare (9,5) as a lean; fine, it is the H-class the conformal turn favors.
Q3: NOT the signature. A homotopical invariant resolving (7,7)-vs-(9,5) is 2-primary-blind to the count by
construction. Do not spend the North Star here.
Q4: The signature declaration feeds gravity (conformal-native (6,4)), not the count. Keep the two legs
separate.

**5. Weinstein-native geometer (the GU-source geometry).**
Q1: Agree the built-side narrowing is near-terminal, but the transcript's OWN mechanism is untouched: the
generation count as 2+1 with the third an "imposter" from the Rarita-Schwinger spinor product rule
(`Spin(V+W) = Spin(V) (x) Spin(W)` + RS cross-term). We have never matched this against carrier A/B. This
is a concrete, distinguishing mechanism (third generation reunifies at high energy), NOT a relabel -- it
could pick which carrier the source action names.
Q2: The conditional build should encode the observerse 4+10 and the shiab operator with fixed coefficients;
that is exactly the object needed to test the RS 2+1 mechanism.
Q3: The RS product-rule cross-term as the FORCING structure for carrier B (the index-changing carrier). ONE
test: does the `Spin(V) (x) Spin(W)` + RS cross-term decomposition force the 192 = 3x64 occupancy onto
B's index -38 (order-3 rho=(0,2,1)) rather than A? If the cross-term is 3-primary AND index-changing, it
is the operator SG4 says is missing.
Q4: Yes -- Weinstein's mechanism is the physics content the abstract "source action" is a placeholder for;
the build instantiates it, the North Star tests whether it forces.

**6. Double-copy / amplitudes theorist.**
Q1: Strongly disagree that the minimum is purely a keystone-narrowing. The missed object is the double
copy. H-class GU gravity = Bach/conformal = `box^2 h = -4 Bach` is literally a fourth-order SQUARED
structure, and the transcript says 2nd-order GU = square of 1st-order. That is BCJ/double-copy language:
gravity = (gauge)^2. The count `3 = dim Lambda^2_+(R^4)` is the SELF-DUAL sector -- exactly what the
self-dual double copy (Chalmers-Siegel, Monteiro-O'Connell-White) squares. Three currently-independent
declarations (count = dim Lambda^2_+, norm = |II|^2 = "the square", K-class = index-changing) may be ONE
color-kinematics constraint.
Q2: Submitting a conditional theory WITHOUT the double-copy structure wastes the highest-upside object,
because the |II|^2 norm is exactly a double-copy square you would be declaring by hand instead of deriving.
Q3: THE forcing object. The self-dual-sector homotopy (L-infinity/BV) double copy. ONE test: construct the
GU source action as the BV double copy of a first-order self-dual gauge datum on Lambda^2_+, and check
whether the color-kinematics (kinematic-Jacobi) constraint is satisfiable ONLY on carrier B's
gamma-trace-constrained field space. If the kinematic algebra's structure constants force B, the A/B bit
is removed AND the norm |II|^2 is derived as the square -- two freedoms collapse to one algebraic identity.
Q4: This IS the two-track hinge: North Star = the double-copy forcing test; the branch = build the
fixed-coefficient action it squares.

**7. Noncommutative geometer (Connes / type-II1).**
Q1: Agree. The type-II1 spectral-triple program already hit a selector no-go
(`type-ii1-selector-or-nogo-theorem`): the required fundamental symmetry the Krein sector lacks is the
SAME object SG4 rubric #6/#10 (boundary well-posedness / Takesaki) relocate to. NCG re-houses the keystone;
it does not force it.
Q2: The build should declare the KO-dimension and the real structure J; the spectral action then has fixed
coefficients. Useful discipline, not a forcer.
Q3: A Takesaki-modular / KMS uniqueness argument that forces the positive state (the self-adjoint boundary
condition) the Krein sector currently lacks. ONE test: does the modular flow of the GU spectral triple
select a UNIQUE positive cone (hence a unique corner of the equivariant Grassmannian)? If yes it forces
carrier B; SG4 rubric #10 says the required positive state is absent, so the honest prior is RELABELS.
Q4: Feeds the build (fixes the spectral action) more than the North Star.

**8. Higher-category / derived-geometry lens.**
Q1: Agree, and sharpen the trap warning: a derived / shifted-symplectic (PTVV) reformulation of the source
action makes the field space a derived critical locus, but Wave 16/17 already show "full field space + BRST
-> A; gamma-trace-constrained -> B" are TWO valid BV data. Derived geometry does not pick between them
unless there is a rigidity theorem. Bare derived geometry RELABELS.
Q2: The BV/BRST data IS what a conditional build must write down; derived geometry is the right language
for it. Useful as scaffold.
Q3: A (-1)-shifted-symplectic RIGIDITY / uniqueness theorem for the GU source action's derived critical
locus -- OR, better, the L-infinity double copy (persona 6) as the derived structure that forces the BV
data. ONE test: is the GU BV complex the homotopy-algebraic double copy of a gauge BV complex, and does
that force a unique (B) field-space declaration? This is persona 6's test in derived-geometric language --
the same top object.
Q4: Yes -- derived geometry is the register in which "build the action" (branch) and "does its structure
force B" (North Star) are the same question asked twice.

**9. Integrable-systems / hidden-symmetry lens.**
Q1: Agree the built side is narrow, but flag an untouched high-symmetry object: self-dual Yang-Mills and
self-dual gravity are INTEGRABLE (infinite hidden symmetry, twistor/Kac-Moody). The count sits in
Lambda^2_+ = the self-dual sector = the integrable sector. If the source action's self-dual truncation is
integrable, its hidden symmetry algebra may FORCE the triplet occupancy. Untouched.
Q2: A conditional build that ignores the self-dual integrable structure leaves the strongest constraint
(an infinite-dimensional symmetry) on the table.
Q3: The self-dual hidden-symmetry (Kac-Moody/twistor) algebra as the forcing structure for the count. ONE
test: does the self-dual truncation of the GU source action carry a hidden loop/Kac-Moody symmetry whose
representation content forces rank-3 (not 1 or 5) on Lambda^2_+? This is the same self-dual sector as the
double copy (persona 6/9 converge); the integrable structure is WHY the double copy is rigid there.
Q4: Yes -- the build writes the truncation; the North Star tests its hidden symmetry.

**10. Quantum-topology / Jones-subfactor lens.**
Q1: Agree, with a caution flag. The Jones index `4cos^2(pi/n) = 3` at `n=6` and the SM's own
`Z/6 = Z/2 x Z/3` global-gauge quotient (double-major sweep) are seductive integer-3 arenas. HONEST: these
are analytic/arithmetic coincidences unless wired to GU's actual frame group; unwired, they transport the
token "3" (the quarantined move). RELABELS unless a subfactor is exhibited inside GU's operator algebra.
Q2: No contribution to the conditional build.
Q3: If anything: is the GU type-II1 factor's principal graph a subfactor of index exactly 3? ONE test:
compute the GU spectral triple's Jones index. But the prior is low -- this is a coincidence-hunt, not a
forcing key. Do not spend the North Star here.
Q4: Does not feed either track load-bearingly.

**11. Spin(8)-triality lens.**
Q1: Agree, and name the one genuinely new BRIDGE candidate from the double-major sweep: a Spin(8) whose
outer-automorphism (triality, order 3) acts DIAGONALLY on tangent and fiber is a SYMMETRY-bridge, not a
coprime pairing, so it is not killed by the `gcd(8,3)=1` linking-form vanishing -- it is the natural source
of an equivariant Lefschetz integer in the Z/3 arena. This is the one 2-primary-blindness-evading object
besides the double copy. BUT the SG4 rubric already checked triality: "triality commutes with Dirac ->
LABELS generations, never SELECTS the corner." So on current evidence it RELABELS (labels 3, does not force
occupancy 3).
Q2: The build must declare whether GU's frame group contains such a diagonal Spin(8); a sharp structural
question worth resolving in the conditional theory.
Q3: The Z/3-equivariant Lefschetz number of a diagonal triality on the source-action field space. ONE test:
does GU's frame group contain a Spin(8) with triality acting diagonally on tangent+fiber, and is its
equivariant Lefschetz integer nonzero/3? If the triality is only a labeling automorphism (rubric result),
it relabels; if it acts on the source-action critical locus it could force. UNKNOWN, worth one probe.
Q4: Feeds the build (frame-group question); the North Star tests the equivariant integer.

**12. Chromatic-homotopy / image-of-J lens.**
Q1: Agree, and deliver the family's cleanest negative result: FB-F2/F4 already closed the image-of-J route.
`pi_3^s = Z/24` is all torsion; the e-invariant is `Q/Z`-valued by construction; both external imports
(`3|m`, `3|sigma`) are FREE. So the entire chromatic/framed-bordism escape is a proven non-forcer for the
count. The local minimum here is DONE and honest -- do not reopen it.
Q2: No contribution.
Q3: NONE from chromatic homotopy directly. The family's chromatic verdict is a KILL that redirects effort
OFF the class-as-count reading and ONTO the integer-by-construction index (persona 3) and the kinematic
double copy (persona 6). That redirection is the contribution.
Q4: Its value is negative-space: it tells the North Star where NOT to look (any torsion-class refinement).

---

## Slice synthesis (wild-frontier family)

### The deepest local minimum
We AGREE with the working diagnosis (narrowing onto an unforceable keystone; H20 dead), but we sharpen it
to a REGISTER statement, which is deeper than "one more geometric object is missing." Every built-side
narrowing move -- signature (L7), K-class (SG4), boundary condition (rubric #6), spectral selector (NCG),
derived critical locus -- lives in the SAME register: geometry / field-space, and is 2-primary or
declaration-valued. The count lives in the orthogonal Z/3 (3-primary, self-dual `dim Lambda^2_+ = 3`)
arena, and `gcd(2,3)=1` is a hard floor: **no move in the geometric-signature register can reach the count,
and the family has now closed the two registers that people HOPED could (the chromatic/image-of-J torsion
refinement -- FB-F2/F4 KILL -- and the signature flip -- Wave 14 structural-incapacity).** So the deepest
local minimum is not "we have not found the deep geometric object"; it is "**we keep searching the
2-primary geometric register for a 3-primary self-dual answer.**" The only registers NOT yet exhausted are
the two that natively live in the self-dual/Z/3 arena: the kinematic/amplitude (double-copy) register and
the equivariant-symmetry (diagonal triality) register. Everything else relabels.

### Top Track-1 FORCING object (+ the one test)
**The self-dual homotopy (L-infinity/BV) double copy.** It is the single object that (a) lives natively in
the self-dual `Lambda^2_+` sector where the count 3 actually sits (so it is NOT 2-primary-blind), (b) is
grounded in Weinstein's own transcript claim ("2nd order = square of 1st order") and independently
corroborated by the machine-checked conformal-Bach identification (`box^2 h = -4 Bach`, a fourth-order
square), and (c) could REDUCE THREE currently-independent freedoms to ONE algebraic constraint: the count
(`3 = dim Lambda^2_+`), the norm (`|II|^2` = the double-copy square), and the K-class (index-changing =
carrier B) would all be fixed by whether the color-kinematics / kinematic-Jacobi constraint is satisfiable.
Personas 6, 8, and 9 converge on it from amplitudes, derived geometry, and integrable systems respectively;
the self-dual sector is integrable, which is WHY the double copy is rigid there.

THE ONE COMPUTATION: **construct the GU source action as the L-infinity/BV double copy of a first-order
self-dual gauge datum on `Lambda^2_+`, and test whether the kinematic-Jacobi (color-kinematics) constraint
is satisfiable ONLY on carrier B's gamma-trace-constrained field space (index-changing, order-3
rho=(0,2,1)), not on carrier A's full BRST field space.** Outcome semantics, stated in advance to avoid the
H20 trap:
- If the kinematic algebra closes only on B: the A/B bit is REMOVED and `|II|^2` is DERIVED as the square.
  Two-to-three freedoms collapse. FORCES. The located-not-forced count becomes forced-modulo-nothing on
  this leg, and the conditional theorem turns unconditional.
- If it closes on both A and B: it RELABELS (another beautiful non-lever), and we have honestly spent one
  branch to learn the double copy is off-shell-underdetermined -- still worth knowing.

This is the ONLY candidate the family will call "top forcing object," precisely because it is the only one
whose failure mode is a genuine freedom-reduction rather than a relabel, and it is testable against the
already-built carrier-B structure (Wave 16 index -38).

### Track-2 (SUBMIT / conditional-build) verdict
**Worth one branch, but NOT as an end in itself -- as scaffold.** From the frontier view, submitting a
purely conditional theory (declare leans: (9,5), carrier B, `|II|^2`; derive+test predictions) buries the
one falsifiable asset (conformal-Bach, with its Ostrogradsky-ghost and rotation-curve exposure, and the
DESI dark-energy handle) under four hand-declared choices, and it declares `|II|^2` by fiat exactly where
the double copy could DERIVE it. So a conditional build submitted alone is a partial waste of the
highest-upside object. BUT the conditional build produces the one thing Track-1 cannot run without: a
source action written with FIXED COEFFICIENTS (the branch-3 action). The double-copy test, the index-bulk
test, and the RS-2+1 mechanism all require a written action to interrogate. Verdict: **build it as the
forcing scaffold for the North Star, not as the deliverable.**

### Two-track meta verdict
**Yes -- they compose, and in this family they are the SAME question asked in two registers.** North Star =
force-or-falsify the coherent picture via the self-dual double-copy test (does the kinematic constraint
force carrier B and derive `|II|^2`?). Branch = write the fixed-coefficient conditional source action. The
feedback is tight and non-optional: the branch SURFACES the invariants the North Star consumes (the written
action, its BV complex, its self-dual truncation, its norm), and the North Star tells the branch whether
its declarations were forced or free (if the kinematic algebra closes only on B, the branch's carrier-B
lean stops being a lean). The build does not compete with the North Star; it is the North Star's input
tape. Run the conditional build as one branch specifically to hand its BV data to the double-copy test --
and route ZERO North-Star effort into the two exhausted registers (chromatic torsion refinement, signature
flip) that the family has now closed.

---

## Grade / provenance
Interpretive strategic synthesis; no new computation. Personas instantiated inline from the repo rosters
(`science-advisory-council-full-picture-2026-07-11`, `canon/double-major-persona-sweep-RESULTS`) and
grounded in the actual current state (seven-axis count map L0-L7 with Wave 14-17 updates, SG4 forcing
rubric, FB-F2/F4 image-of-J closures, the conformal-Bach/double-copy turn and the Weinstein transcript).
The two closed negatives (chromatic/image-of-J KILL; signature 2-primary-blindness) are cited canon, not
new claims. The one proposed forcing object (self-dual BV double copy) is a CANDIDATE with an explicit
forces-or-relabels test, not an asserted result -- offered under the H20 discipline (a forcing candidate
must reduce a real freedom, and the test is designed so that its failure mode is honest). No claim/canon
movement; the generation count stays located-not-forced; the source action stays the terminal unbuilt
object. Not committed.
