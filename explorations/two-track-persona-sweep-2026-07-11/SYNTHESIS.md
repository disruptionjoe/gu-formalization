# Full-roster persona sweep: the two-track synthesis (2026-07-11)

Five discipline-family slices (A orthodox/rigor, B geometric/structural, C foundations/observer, D wild
frontier, E pragmatic/experimental), ~60 personas total, each run inline, on four questions: the local
minimum, the Track-2 (submit) escape, the Track-1 (North Star) escape, and whether both tracks can run.
Slice files: `A-orthodox-rigor.md` ... `E-pragmatic-experimental.md`. This is the synthesis.

## 1. The local minimum (unanimous, three convergent statements)

The keystone-narrowing ("everything routes to an unforceable source action") is a SYMPTOM. The disease, named
from five directions:
- **Register mismatch (D, B).** Every built-side narrowing move lives in the 2-primary geometric/field-space
  register; the count lives in the orthogonal 3-primary self-dual arena (`dim Lambda^2_+ = 3`), and
  `gcd(2,3)=1` is a hard floor. The two registers people hoped could cross it are now BOTH closed: chromatic /
  image-of-J torsion (KILL) and the signature flip (KILL). We keep searching the 2-primary geometric register
  for a 3-primary self-dual answer.
- **One abstraction level too low (B).** The program DECLARES a source action where a UNIVERSAL PROPERTY would
  force it, and treats the signature that sets the grading as a convention rather than the forcing lever.
- **Coherent conflated with forced; maps rewarded over numbers (C, E).** The four "cleared" legs are
  existence/consistency clears a simpler rival (Bianconi) also clears; the default deliverable is a
  residual-freedom MAP; number-extraction is treated as a rare event. The one time numbers were forced (the DE
  waves) the machinery worked and cleanly falsified.

## 2. The two registers to STOP (D, decisive)

Route ZERO North-Star effort into chromatic-torsion refinement or the `(9,5)`-vs-`(7,7)` signature flip. Both
are proven structurally incapable of supplying the 3-primary count. `tmf` is an H20-style trap (same torsion
wall one chromatic level up, zero freedoms removed). This retires signature-chasing as a North-Star object.

## 3. The North Star, FORCE route: the self-dual square (B + D converge on ONE object)

The single genuinely new forcing candidate, reached independently by the geometric and wild-frontier families:
**the source action as the SELF-DUAL SQUARE.** Two faces of one principle:
- **B (Willmore/GJMS uniqueness):** the source action is the UNIQUE conformally-invariant Willmore/GJMS
  functional of the soldering distortion (= moment-map norm-square = spectral action), pinned up to scale by
  conformal-weight + IG-invariance + derivative-order. A uniqueness statement, which is exactly what
  signature/causality/grading each failed to be.
- **D (self-dual L-infinity/BV double copy):** construct the source action as the double copy of a first-order
  self-dual gauge datum on `Lambda^2_+`. It lives NATIVELY in the 3-primary self-dual arena (not 2-primary
  blind), and could collapse THREE freedoms to one: the count (`3 = dim Lambda^2_+`), the norm (`|II|^2` = the
  square), and the K-class (index-changing = carrier B).

Both are Weinstein's "2nd order = square of 1st order" made into a forcing test, grounded in the machine-checked
`box^2 h = -4 Bach` (a fourth-order square). **THE ONE TEST:** does the color-kinematics / Willmore-first-
variation constraint close ONLY on carrier B's gamma-trace-constrained field space? Closes only on B -> the
A/B bit is removed and `|II|^2` is DERIVED as the square (FORCES; the source-action postulate becomes a
theorem and the branch collapses into the North Star). Closes on both -> RELABELS. Unlike H20, its failure
mode is a genuine freedom-reduction, not a relabel, and it is testable against the built Wave-16 carrier-B
structure.

## 4. The North Star, FALSIFY route: the Bach/Weyl graviton sector (A + C + E converge)

The load-bearing falsification object is the same sector, from the other side:
- **The class discriminator (C):** geometry-first-vs-information-first is ILL-POSED (both frames smuggle the
  metric). The decidable, empirically-real, signature-binary-ROBUST axis is the spin-2 action ORDER:
  GU's `box^2 h = -4 Bach` (4th-order, extra GW polarizations / propagator pole / GW dispersion / massive
  spin-2 companion) vs Bianconi's Einstein (2nd-order). PPN does NOT discriminate (both pass); the radiative
  content does.
- **The Bach-identification (A):** if H-class GU-gravity simply IS conformal/Bach gravity, known mainstream
  refutations transfer as a whole-theory kill (Stelle-Mannheim loop non-unitarity, Ostrogradsky ghost,
  Horne/Hobson-Lasenby no-flat-rotation-curves).
- **The class-level no-goes:** the Occam-kill (if the graviton is exactly 2nd-order GR and Lambda is set by
  orthogonal data, the two-metric apparatus is superfluous) and the Lambda-magnitude no-go (neither GU nor
  Bianconi pins ~`10^-122`; if the magnitude needs information the g-vs-G action cannot contain, ALL instances
  die at once). Also A's positivity/boundedness-below no-go and B's Gauss-Codazzi integrability off umbilic
  sections (Schwarzschild is non-umbilic) -- each a whole-class killer.
- **Next data confrontation (E):** the strong-field exact-Schwarzschild/Kerr section residual (whole-GU death
  on failure: "cannot host an observed black hole"), gated on mu_DW (so declaring it lets it run).

## 5. Track 2, the submit branch: extract NUMBERS, ranked cheap-and-decisive (E, with C)

The FIRST product must be a number, NOT the count (the count is decoupled, source-action-gated, and likely
"still under-determined" even under a declared postulate -- the worst Track-2 deliverable).
1. **DE raw-`H(z)` vs the actual DESI DR2 BAO likelihood (+ CMB prior).** Cheapest decisive kill-shot on the
   board (~1 day): the backreacted `H(z)` already exists, the data is public, it is the exact comparison the
   CPL falsification did NOT settle. Kills both ways.
2. **The spin-2/Weyl discriminator to concrete numbers:** mu_DW-parametrized Stelle-Yukawa + GW dispersion
   (+ conformal lensing). The program's first parameter-linked number AND the class fork vs Bianconi -- the
   same object as Section 4, so it is simultaneously a Track-2 number and the Track-1 class discriminator.
3. **H6 the GU-independent family-puzzle theorem:** cheap, decisive as a theorem, survives GU being wrong.
4. Count 1-vs-3 under the declared postulate (likely under-determines). 5. Fermion/Yukawa (expensive; defer).

## 6. The two-track verdict (unanimous YES) and the firewall

Both run without a stance change. The firewall is the repo's existing **conditional-theorem form** (A): a
conditional theorem never asserts its antecedent, so Track 2 declares the leans as explicit postulates and
ships conditional predictions ("count = 3 given S" is a prediction of GU-given-S, never of GU) without
flipping the repo's unconditional North-Star stance. The tracks are NOT parallel silos -- they are the same
questions in two registers (B, C, D, E all say this): Track 2's deliverables ARE Track 1's tests. The
fixed-coefficient conditional action Track 2 builds is exactly the object Track 1's self-dual-square forcing
test consumes; the spin-2 numbers Track 2 emits ARE the Track-1 class discriminator; and a Track-1 forcing
win COLLAPSES the branch back into the North Star. E's framing: North Star = the model prior (illiquid,
unforceable); Track 2 = the likelihood sleeve. Run Track 2 first as the cheaper information source.

## 7. Net reprioritization

- North Star headline object = the **Bach/Weyl self-dual-square graviton sector** -- it FORCES (Section 3),
  FALSIFIES (Section 4), DECIDES the class, and needs neither the source action nor the signature settled.
- The **count/signature** is the separate upstream question; STOP chasing it in the 2-primary register.
- Track 2's first products are the **DE BAO likelihood** and the **spin-2 discriminator numbers**, not the count.
- Corrects the prior framing where "declare-and-build the conditional theory, derive the count" (H47) was the
  headline: the count is the WORST first deliverable; the graviton numbers are the best, and they double as the
  North-Star class test.
