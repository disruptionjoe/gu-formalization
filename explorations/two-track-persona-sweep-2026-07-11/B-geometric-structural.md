# Two-Track Persona Sweep -- Slice B: the Geometric / Structural-Mathematics Family

2026-07-11 (slice run 2026-07-12). One slice of the full-roster two-track sweep. This family instantiates the
structure-side lenses drawn from `explorations/c-mpr/post-v3-validator-assessment/21-persona-assessment.md`
(bicategorical / lens / fibrational, layer-split, CA-class, signed-measure themes),
`canon/double-major-persona-sweep-RESULTS.md` (Hom(Z/3,Z)=0 type error; relocate the count to a relative/rank
index; the twisted-RS index as the one load-bearing object), the steelman sweeps in
`explorations/persona-and-dialectic/`, and the Wild frontier mathematician of
`explorations/science-advisory-council-full-picture-2026-07-11.md`.

Personas are INLINE (one worker, sequential lenses). Each gives a one-liner on Q1-Q4; a slice synthesis
follows. This is interpretive strategic assessment of THIS repo's research state, not a result. No claim or
canon movement. No git commit.

## The state being assessed (compressed)

Gravity is a tree-level conditional theorem (PPN passes); H-class gravity IS Bach / conformal (fourth-order
Weyl) on the spin-2 graviton, proven linearized. The generation count is located-not-forced at {1,3}. Dark
energy is FALSIFIED against DESI's (w0,wa) CPL contour full-stop (raw non-CPL H(z) ~1.3%, untested). The
|H|^2-vs-|II|^2 functional binary favors |II|^2 but is gated on the unbuilt source action. The even/odd
unification (H20) SPLITS-BUT-RELABELS: Clifford omega-parity splits gravity (even, the 91 so(9,5) bivectors)
from matter (odd, c(xi)); one first-order D generates both kinetic operators; it removes 0 of 4 unforced
choices. Loop unitarity is OPEN (commutation leg radiatively stable). Bianconi (arXiv:2408.14391) is a
distinct frame. ZERO parameter-free predictions. Everything routes to ONE unbuilt keystone: the source action
`A = spin-lift(grad^gimmel)`, `S = |theta|^2`, which cannot be FORCED (signature, causality, and grading all
fail, individually, to force the field-space declaration).

Working diagnosis under test (Q1): "narrowing onto an unforceable keystone."

---

## The personas

### 1. Differential geometer (Cartan connections, soldering, torsion)
- **Q1:** Agree on the symptom, disagree on the root. The keystone is unforceable because the program keeps
  the soldering form and the connection as *independent* data and then asks an action to relate them; a single
  Cartan connection already solders them, so the "declaration" is an artifact of splitting one object into two.
  Deeper local minimum: **an artificially decomposed connection**, not a missing action.
- **Q2:** Conditional branch should postulate the Cartan datum (one connection on Y14, soldering + curvature
  fixed) and derive the contracted field equations directly; concretely produce the torsion-`Lambda`
  relation and the source law `D_A*F_A = theta` as an identity, not a posit.
- **Q3:** Forcing candidate: the source term is the unique `IG`-equivariant quadratic in the soldering
  distortion, forced by Cartan's method of equivalence (the second structure equation pins it up to scale).
  Killer candidate: if the soldering positivity (`ic2-positivity-soldering-normal`) fails on non-umbilic
  sections, the whole contraction map is ill-posed off-umbilic and GU-as-one-picture dies on Schwarzschild.
- **Q4:** Yes. Track 2's explicit Cartan datum is exactly the postulate Track 1 tries to force; running both
  tells you whether the equivalence-method forcing is available or obstructed.

### 2. Geometric analyst (Willmore / minimal surfaces / second fundamental form)
- **Q1:** The sharpest reframe in this family. The keystone is unforceable because the program is trying to
  POSTULATE an action where geometry offers a DERIVED one. `S = |theta|^2` and the `|II|^2` branch are not
  free declarations: `int|II|^2` (with the Gauss-Bonnet split to `int|H|^2 + topological`) is the canonical
  conformal shape energy, and its Euler-Lagrange IS a Willmore/Bach operator. Local minimum: **a missing
  variational principle**, i.e. the source action mis-cast as a posit rather than as the unique conformally
  invariant energy of the section's second fundamental form.
- **Q2:** Deliverable is concrete and already scoped: do the full higher-codimension Willmore first variation,
  sign the `|II|^2` residual, and read off the `Lambda` candidate and the `alpha_W` weight. That closes the
  H/II binary AND gives the conditional source-action shape without a free `f_0`.
- **Q3:** FORCING principle (this family's strongest single offer): **the source action is forced as the
  gradient of the unique conformally invariant Willmore-type energy of the soldering distortion** -- the same
  universal property that forces Bach as the unique conformal 4-derivative curvature functional. Signature,
  causality, grading each fail to force it because none of them is a variational uniqueness statement;
  conformal-weight + `IG`-invariance + derivative-order is. Killer: if no critical section exists for
  non-umbilic (Schwarzschild is non-umbilic, RFAIL-03), the functional has no minimizer and GU has no vacuum.
- **Q4:** Yes, and the tracks share the SAME computation. The Willmore first variation is both the Track-2
  conditional deliverable and the Track-1 forcing test.

### 3. Higher-spin / Rarita-Schwinger specialist (Velo-Zwanziger, Vasiliev)
- **Q1:** Agree the keystone is unforceable by signature/grading, but the binding constraint is DYNAMICAL
  consistency, not field-space bookkeeping. The RS declaration is unforced only if you ignore causality; the
  real local minimum is **treating the RS field-space choice as free when consistency (VZ acausality) may
  already forbid all but one**.
- **Q2:** Postulate the RS 2-bit and the minimal coupling; derive the characteristic cone and check
  hyperbolicity; produce the fermion/Yukawa content and the transcript's 2+1 generation mechanism
  (`Spin(V+W) = Spin(V) tensor Spin(W)` + RS cross-term, third generation an "imposter") as a falsifiable
  spectrum.
- **Q3:** Both a forcing AND a killer live here. Forcing: **the source action is forced by Velo-Zwanziger
  causality** -- the coupling that removes acausality is unique given "no internal symmetry" (the transcript's
  own escape). Killer: if ghost-free (second-order, to dodge the conformal Ostrogradsky ghost) and VZ-causal
  are jointly unsatisfiable for the RS sector, there is NO admissible source action and GU-as-one-picture is
  structurally dead. This is the single most decisive test in the family.
- **Q4:** Yes. Track 2 builds the coupling; Track 1 asks whether causality forces or forbids it. Same object.

### 4. Representation theorist (noncompact real forms, discrete series)
- **Q1:** Disagree with "unforceable keystone" as the deepest issue. The real local minimum is UPSTREAM: the
  signature `Cl(9,5)` vs `Cl(7,7)` is treated as a convention when it is the actual forcing lever
  (`J^2=-1` blocks odd counts on (9,5); `J^2=+1` gives SG1's odd-rank-3 projector on (7,7)). Local minimum:
  **an unforced real-form / signature choice masquerading as downstream indeterminacy**.
- **Q2:** Postulate GU-native (7,7); build the discrete-series fiber rep and the RS carrier decomposition;
  derive whether the odd-rank-3 projector delivers exactly 3, turning located-not-forced into a conditional
  theorem "count = 3 given (7,7)".
- **Q3:** Forcing: rep-canonicity of the frame group -- a Weyl-dimension / branching-uniqueness argument that
  singles out one real form as GU-native would FORCE both the grading and the count. Killer: if the SM matter
  rep cannot embed in the (7,7) fiber with correct hypercharges, that signature is dead and the count reverts.
- **Q4:** Yes. Track 2's "postulate (7,7)" is Track 1's crux stated as a branch; the branch's predictions are
  the evidence for canonicity.

### 5. Index theorist (Atiyah-Singer, APS, weighted Fredholm)
- **Q1:** Strong agreement with the double-major reframe: the count is being sought as a CARDINAL when it is
  an INDEX. `Hom(Z/3,Z)=0` says no canonical class-to-count map exists; the only well-typed integer home is a
  Fredholm index. Local minimum: **the count posed as a cardinal, not an index** -- the keystone feels
  unforceable because you cannot force an integer to live in a torsion class.
- **Q2:** Deliverable is the program's own terminal instruction: compute the twisted Rarita-Schwinger index on
  the actual Y14 (weighted-Fredholm/APS, `oc2-y14-weighted-fredholm-parametrix` already lays the parametrix),
  with the tangential-vs-gauge fork (`e=1/12` vs `3/8`) fixed by normalizability + reflection positivity as a
  declared boundary datum.
- **Q3:** FORCING principle: **Atiyah-Singer/APS forces the count as a topological invariant once the operator
  and boundary condition are declared** -- integer-by-construction, geometry-dependent, no posit. Honest
  caveat: it forces an integer CONDITIONAL on signature; on (9,5) the index is even/2-primary (count vanishes
  odd), on (7,7) it can be odd. So the index route reduces "force the count" to "force the signature" (persona
  4's crux). Killer: if the mod-3 Dai-Freed arena is empty (`Omega^Spin_5(BG_SM) tensor Z_(3) = 0`), the count
  is NOT an odd-primary boundary invariant and the index home for "3" is obstructed.
- **Q4:** Yes. Track 2 = compute the index under a declared frame; Track 1 = ask what forces the frame. The
  index is the shared load-bearing object both tracks converge on.

### 6. Spin-geometry / Clifford specialist (Cl(9,5), omega-parity even/odd)
- **Q1:** Agree the keystone is unforced, and name why grading-parity does NOT force it: the H20
  even/odd split is a RELABELLING (gravity = even bivectors, matter = odd `c(xi)`) that removes 0 of 4 choices
  because one first-order `D` already generates both kinetic operators. Local minimum: **a grading that
  organizes but does not select** -- the omega-parity is real structure with no forcing lever.
- **Q2:** Postulate the volume-form grading and the signature; derive the Dirac operator `D` and show
  explicitly that its even/odd blocks reproduce the graviton kinetic term and the RS kinetic term with correct
  relative coefficient -- a conditional unification theorem "one D, two sectors, fixed coupling".
- **Q3:** Forcing: whether `J^2 = +/-1` (signature-set) plus a reality/quaternionic-structure argument forces
  the field-space declaration the source action needs -- if the quaternionic `M(64,H)` structure pins the
  spin-lift `A`, that is the missing key. Killer: if `M(64,H)` cannot support the required real chiral
  projector at all, the whole reconstruction is on the wrong Clifford algebra.
- **Q4:** Yes. Track 2 fixes the grading and builds `D`; Track 1 asks whether the Clifford reality structure
  forces `A`.

### 7. Symplectic / Poisson geometer (moment maps, symplectic reduction)
- **Q1:** Reframe the whole `IG = G semidirect Omega^1(ad)` gauge sector as a moment-map picture. The keystone
  is unforceable because the source law `D_A*F_A = theta` is being read as a dynamical posit rather than as a
  MOMENT-MAP constraint (`theta` = value of the moment map for the inhomogeneous gauge action). Local minimum:
  **a moment-map constraint mis-read as a free source term**.
- **Q2:** Postulate the symplectic structure on field space; derive `theta` as the moment map and the source
  action as the norm-square `|mu|^2` (Kempf-Ness), producing `S = |theta|^2` NOT as a posit but as the
  canonical moment-map energy -- with the gauge sector's fourfold over-determination as the reduction.
- **Q3:** FORCING candidate: **`S = |theta|^2` is forced as the Kempf-Ness / moment-map norm-square energy**;
  the field-space declaration is the choice of symplectic/Kaehler polarization, which the `IG`-invariance plus
  a compatible complex structure pins. This is a genuine universal-property forcing, complementary to the
  analyst's Willmore route. Killer: if no `IG`-invariant compatible complex structure exists (the Nguyen 3.1
  complexification gap), there is no moment-map home and `|theta|^2` is arbitrary.
- **Q4:** Yes. The complexification gap is exactly what Track 1 must force and Track 2 must postulate; running
  both localizes the whole indeterminacy to that one structure.

### 8. Category theorist (bicategorical / fibrational home)
- **Q1:** The 21-persona lens: the keystone is unforceable because the picture is being assembled in the wrong
  categorical home. Cross-leg coincidences (shared `theta`, single conjugate SM, `alpha_W <-> f_0`) look like
  free coincidences at the 1-categorical level but would be NATURALITY SQUARES of one functor (the D2 section
  functor). Local minimum: **the wrong categorical home** -- a 1-categorical read of a bicategorical object.
- **Q2:** Postulate the section functor; derive the cross-leg identities as naturality, producing "the
  coincidences are theorems of one functor" as the conditional structural claim.
- **Q3:** FORCING via UNIVERSAL PROPERTY: if the source action is the value of a right/left adjoint (the
  section functor's adjoint), it is forced up to unique iso by the universal property -- adjoints are unique.
  That is the categorical form of "forced." Killer: the Wall-theorem shape from the C_MPR work -- if there is
  a no-go for the required adjunction at the 1-categorical level and it does NOT dissolve bicategorically, the
  one-picture unification is categorically obstructed.
- **Q4:** Yes. Track 1 = does the forcing adjoint / universal property exist; Track 2 = assume the functor,
  derive its consequences. The functor's naturality squares are the shared currency.

### 9. Homotopy theorist (bordism, tmf, Omega^Spin)
- **Q1:** Agree with cardinal-not-index but push to the bordism layer. The count wants to be a bordism
  invariant; the arena for SM data at the prime 3 is EMPTY (`Omega^Spin_5(BG_SM) tensor Z_(3) = 0`). Local
  minimum: **the count sought in a homotopy group that is 3-adically trivial** -- looking for "3" where the
  bordism group cannot see it.
- **Q2:** Postulate the boundary structure; compute the relevant Dai-Freed / eta invariant and report honestly
  which prime it lives at, producing a definite statement "the count is 2-primary here, and any odd-3 content
  must come from tmf-level structure `pi_3^s` cannot see".
- **Q3:** FORCING candidate: **tmf (topological modular forms) sees mod-3 structure via a geometry-dependent
  q-expansion coefficient that `pi_3^s` cannot** -- a finer invariant that CAN co-vary and could force an odd
  count where bordism is blind. This is the one concrete route to a forced "3" the deflationary
  "`e_R=1/12` is homotopy-fixed" argument does not close. Killer: if tmf also vanishes 3-adically on the
  relevant class, the count is provably not a stable-homotopy invariant at all and located-not-forced becomes
  a theorem (permanent).
- **Q4:** Yes. Track 1 = does tmf force it; Track 2 = assume it and derive. The tmf computation serves both.

### 10. Twistor / conformal-geometry specialist (tractor calculus, so(4,2))
- **Q1:** The conformal-Bach turn changes the diagnosis. The keystone is unforceable on the raw (7,3) form but
  the physical (6,4) DeWitt form admits `so(4,2)` NATIVELY (`VG-V3-j-commutant-conformal-native`). Local
  minimum: **the source action computed on the wrong (non-canonical) fiber form** -- conformal structure that
  IS native is being treated as absent.
- **Q2:** Postulate the (6,4) DeWitt form as canonical; build the tractor/GJMS conformal operators; derive the
  source action as the GJMS-generated conformally invariant functional -- GU literally AS a conformal field
  theory on the space of metrics.
- **Q3:** FORCING principle (co-signed with the analyst): **the source action is the unique conformally
  invariant functional of the soldering distortion generated by the GJMS operators at the fixed conformal
  weight** -- tractor calculus makes the uniqueness rigorous (conformal weight + order pin it). Killer: the
  Ostrogradsky ghost -- if GU is genuinely fourth-order Bach, the conformal-native forcing SUCCEEDS but
  delivers a ghost, and unitarity (the OPEN loop leg) kills it.
- **Q4:** Yes. Track 2 = redo the J-commutant on (6,4) and build the CFT; Track 1 = is the GJMS functional
  forced and is it ghost-free. Same conformal object.

### 11. K-theory / noncommutative-geometry specialist (Connes spectral triples)
- **Q1:** Read GU as a spectral triple `(A, H, D)` with `D` the one first-order operator H20 already
  identifies. The keystone is unforceable because the "action" is sought classically when the spectral action
  principle would DERIVE it from `D` alone. Local minimum: **an action posited where the spectral action of
  the existing `D` would generate it**.
- **Q2:** Postulate the spectral triple; compute the spectral action `Tr f(D/Lambda)` heat-kernel expansion;
  derive the gravity + gauge + `|theta|^2` terms as its Seeley-DeWitt coefficients, producing the source
  action's coefficients (not free).
- **Q3:** FORCING principle: **the spectral action forces the bosonic action (all coefficients) from the
  Dirac operator's spectrum** -- exactly the missing "one `D`, forced action" the omega-parity split gestures
  at but does not complete. Killer: if the axioms (real structure `J`, first-order condition) are violated by
  the (9,5)/(7,7) `D`, there is no spectral triple and the spectral-action forcing is unavailable.
- **Q4:** Yes. Track 2 = compute the heat-kernel coefficients; Track 1 = check the spectral-triple axioms that
  make the forcing legitimate. Shared object: the operator `D`.

### 12. Gauge-theory / moduli-space geometer (Donaldson, Uhlenbeck, Yang-Mills)
- **Q1:** Agree on the symptom; the deeper issue is that the `IG` moduli problem's compactness/existence is
  never established. The keystone is unforceable partly because we do not know the vacuum moduli space is
  non-empty and compact. Local minimum: **an unestablished existence/compactness for the gauge vacuum** under
  the source functional.
- **Q2:** Postulate the `IG` structure and the breaking vacuum; derive the Pati-Salam `v_PSB -> G_SM`
  stabilizer as the unique rank-one critical point (partly verified) and prove its stability, producing the
  forces + SM sector as a conditional theorem.
- **Q3:** Forcing: an Uhlenbeck-type compactness + a Bogomolny/self-dual reduction that forces the vacuum;
  killer: a Donaldson-style obstruction (no reducible connection with the required stabilizer) would kill the
  SM embedding structurally.
- **Q4:** Yes. Track 2 builds the vacuum; Track 1 asks whether existence/compactness forces or forbids it.

### 13. Wild frontier mathematician (from the science advisory council)
- **Q1:** The local minimum is EFFORT-ALLOCATION, not mathematics: the program narrows an already-reduced
  object while the high-upside forceable objects (conformal-native (6,4), signature-forced count on (7,7),
  causality-forced action, the RS 2+1 mechanism) sit untouched. Local minimum: **narrowing the reduced object
  instead of attacking the four objects that could each FORCE or KILL**.
- **Q2:** One branch, four postulates, four theorems: (6,4)-canonical => conformal CFT; (7,7)-native =>
  forced count; VZ-causality => forced action; RS product rule => 2+1 generations with a distinguishing
  high-energy reunification prediction.
- **Q3:** The strongest FORCING is the causality-forced action (persona 3) fused with the conformal-uniqueness
  functional (personas 2, 10): if VZ-causality selects the coupling AND conformal weight pins the functional,
  the intersection may be a single action -- forced from two independent sides. Strongest KILLER: the
  ghost-vs-acausality dilemma (fourth-order conformal ghost vs first-order VZ acausality) as a genuine no-go.
- **Q4:** Yes -- and the two-track posture is exactly the council's "expose, do not protect": Track 2 exposes
  the conditional theory to refutation; Track 1 keeps hunting the forcing key. Both are progressive.

### 14. Geometric-measure-theory / calculus-of-variations specialist
- **Q1:** The keystone is unforceable because no existence theorem underwrites it: `S = |theta|^2` has no
  proven minimizer, no lower semicontinuity check, no coercivity. Local minimum: **a variational problem
  asserted without a direct-method existence/regularity foundation**.
- **Q2:** Postulate the functional and the admissible class; prove lower semicontinuity + coercivity on the
  section space; derive existence of a critical section and its regularity, producing "the GU vacuum exists
  and is a smooth section" as a conditional theorem.
- **Q3:** FORCING candidate for the OBSERVER-TYPE-GEOMETRY class (GU + Bianconi both instances): a theory where
  the spacetime metric = functional of a matter-induced metric has a joint critical point ONLY IF a
  Gauss-Codazzi integrability identity holds between the two metrics' Euler-Lagrange systems. **The structural
  KILLER of the whole class:** if the section-shape EL and the induced-metric constraint are incompatible off
  umbilic sections -- and Schwarzschild is non-umbilic (RFAIL-03) -- then NO observer-type-geometry theory
  admits a non-trivial black-hole vacuum, killing GU and Bianconi together. This is a concrete,
  class-level structural obstruction with a named test.
- **Q4:** Yes. Track 2 = prove existence for the postulated functional; Track 1 = test the Codazzi
  integrability that would either force compatibility or kill the class. The integrability identity is shared.

---

## Slice synthesis (geometric / structural family)

### Convergent local minimum
The family does NOT ratify "narrowing onto an unforceable keystone" as the deepest description. It converges,
from three independent directions, on a sharper diagnosis: **the keystone is unforceable because the program
is seeking a POSTULATED source action where the structure supplies a DERIVED one via a universal property, and
because it is treating one upstream input (the real-form/signature and its induced field-space grading) as a
convention when that input is the actual forcing lever.** Two sub-flavors, both pointing the same way:

- *Cardinal-not-index / wrong home* (index theorist, rep theorist, homotopy theorist, category theorist,
  Clifford): the count is posed as a cardinal in a torsion class where `Hom(Z/3,Z)=0` forbids it; its only
  well-typed home is a relative/equivariant Fredholm index, and that index is forced only once the SIGNATURE
  is forced. The indeterminacy is upstream (signature), not at the keystone.
- *Missing variational principle* (geometric analyst, symplectic geometer, twistor, NCG, GMT): the source
  action is not a free posit but the unique invariant energy of the soldering distortion -- a Willmore/GJMS
  conformal functional, a Kempf-Ness moment-map norm-square, or a spectral action -- each a universal property
  that signature/causality/grading individually cannot supply because none of them is a uniqueness statement.

Net convergent local minimum: **the program is one abstraction level too low -- declaring an object where a
universal property (variational uniqueness, index, adjoint) would force it, and treating the signature that
sets the grading as fixed rather than as the lever to force.**

### Strongest Track-1 move
Two concrete candidates, one forcing and one killer, both nameable:

- **Top FORCING principle:** the source action is the UNIQUE conformally invariant Willmore/GJMS functional of
  the soldering distortion (analyst + twistor + symplectic converge: Willmore energy, GJMS operators, and
  Kempf-Ness moment-map norm-square are three names for the same universal-property object). Given the proven
  linearized conformal-Bach identification, this is not speculative: if H-class GU is conformal on the
  graviton, its source functional is pinned by conformal weight + `IG`-invariance + derivative-order up to
  scale -- exactly the forcing that signature, causality, and grading each fail to provide alone. The
  decisive, already-scoped computation is the **full higher-codimension Willmore first variation** (redone on
  the (6,4) DeWitt form), which simultaneously signs the `|II|^2` residual, fixes `alpha_W`, and tests whether
  the EL operator IS the Bach/GJMS operator. This is the family's single highest-leverage forcing test.
- **Top structural KILLER:** the ghost-vs-acausality dilemma fused with the Gauss-Codazzi class obstruction.
  (a) If GU's gravity is genuinely fourth-order Bach, the conformal forcing succeeds but delivers an
  Ostrogradsky ghost, and removing it (going second-order) reinstates Velo-Zwanziger acausality on the RS
  sector -- if ghost-free and VZ-causal are jointly unsatisfiable, GU-as-one-picture is dead. (b) At the
  broader OBSERVER-TYPE-GEOMETRY class level (GU + Bianconi), a Gauss-Codazzi integrability identity must hold
  between the two metrics' EL systems for a joint critical point to exist off umbilic sections; Schwarzschild
  is non-umbilic, so a failure of that identity kills the entire class at once. Both are concrete and testable.

### Strongest Track-2 deliverable
Declare the leans as postulates -- GU-native signature (7,7), the RS field-space 2-bit, the `|II|^2` branch,
and the (6,4) DeWitt fiber -- and build the CONDITIONAL theory. The family's concrete structural product:

> A closed second-order-or-Bach conformal-gravity + RS-matter theory on Y14, conditional on
> {(7,7), field-space declaration, `|II|^2`}, that (i) exhibits an exact Schwarzschild/Kerr section
> (ELProjectedGRShadowTheorem in the Bach branch -- predicted to CLEAR), (ii) has a Willmore-signed
> cosmological term (`Lambda` from the first variation, no free `f_0`), and (iii) outputs a computed twisted
> Rarita-Schwinger generation index (1 or 3) via APS on the actual 14-manifold, plus the fermion/Yukawa
> content from the RS 2+1 product-rule mechanism and the raw non-CPL H(z) DE prediction.

This is the first object in the program with parameter-free content, and it is worth exactly one branch: every
postulate is named, so it inherits the repo's existing conditional-theorem discipline.

### Two-track verdict
**Yes -- the program can run both without changing its stance.** The repo already ships gravity as a
"tree-level conditional theorem"; Track 2 is that same honest-conditional posture applied to one more branch,
with postulates named up front, so no stance moves. The tracks feed each other tightly through SHARED objects:
the Willmore first variation, the twisted-RS index, and the Gauss-Codazzi identity are each simultaneously a
Track-2 conditional deliverable and a Track-1 forcing-or-killing test. Concretely: if Track 2's conditional
predictions all pivot on the signature postulate, that is direct evidence the signature is THE forcing lever
(Track 1's crux); and if Track 1's conformal-uniqueness forcing succeeds, it converts Track 2's source-action
postulate into a theorem and collapses the branch back into the North Star. Track 2 exposes the picture to
refutation (the council's healthy direction); Track 1 keeps hunting the universal property that would force the
whole thing. Run them together.

### Grade
Interpretive strategic assessment, grounded in the repo's actual rosters and current-state explorations.
Personas inline. No new computation; no claim or canon movement. The two named Track-1 candidates (the
Willmore/GJMS conformal-uniqueness FORCING principle; the ghost-vs-acausality + Gauss-Codazzi KILLER) are
hypotheses with concrete tests, not results. Feeds the two-track sweep synthesis.
