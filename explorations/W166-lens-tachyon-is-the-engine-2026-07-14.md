---
artifact_type: exploration
status: exploration (W166; TEAM LENS-ENGINE; five personas inline, one worker, no sub-agents; deterministic test with W126/W130/W159 positive controls)
created: 2026-07-14
wave: W166
label: W166
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; honest grading
title: "W166 -- INVERT THE FRAMING: the tachyon (debit-1) is not a flaw the standard picture LACKS, it is the ENGINE the standard picture is MISSING (record-genesis / the arrow of time / why the universe is not a dead static vacuum). VERDICT: the flaw-COUNT in bar (b) is MIS-POSED, but the tachyon does NOT move to pure CREDIT. Q1 does m_0^2 < 0 in the record-count mode literally mean record growth? YES -- the tachyon IS the conformal/scale mode (W153 T1, a1/a2 invariant under p), N = 4-volume ~ e^{4p} is strictly monotone in the scale amplitude p (W145: N = 4-volume), and m_0^2 = -1/4 < 0 gives a growing homogeneous mode (roots +/- 1/2); so m^2<0 <=> N grows = the arrow of time. Q2 W159 reconciliation: GRACEFUL-IN-RATE / BOUNDED-BY-everpresent, NOT the pathological blow-up W159's attractor-hunt implicitly rejected -- the DBI velocity cap converts the runaway to LINEAR p (steady de-Sitter-rate N ~ e^{4 v_max tau}), and everpresent 1/sqrt(N) makes the drive DECELERATE; but the graceful rate is read AT the v^2=1/16 validity edge (|m_0^2|=1/4=4x beyond), so PLAUSIBLE not INSIGHT. Q3 CREDIT-side? PARTIAL -- the flaw-count is mis-posed (the tachyon is debit-credit-ENTANGLED with the arrow it supplies, which the standard picture's time-symmetric laws genuinely lack), but it does not move to pure credit (the arrow is EXPLANATORY depth not empirical adequacy; the graceful engine is validity-edge). Bar (b) is RE-POSED, not cleared. HONEST GRADE: PLAUSIBLE."
hypothesis: "The bar-(b) verdict counts the tachyon as 'a flaw the standard picture lacks'. INVERT it: the standard picture (LCDM+GR+SM) has time-symmetric laws and NO derived mechanism for record-genesis, the arrow of time, or why records accrete at all. The tachyon lives in the record-count / conformal-scale mode (W153); an instability of that mode IS a drive for record-accretion. Test whether m_0^2 < 0 in the record-count mode literally means the record count N grows (arrow of time), whether that growth is a GRACEFUL engine or a PATHOLOGICAL runaway (reconcile with W159), and whether the tachyon therefore moves to the CREDIT side of bar (b)."
grade: "exploration / conditional register throughout. COMPUTED (deterministic, tests/W166_tachyon_is_the_engine.py, 16/16 exit 0, W126 + W130 + W159 positive controls first). HONEST GRADE OF THE LENS: PLAUSIBLE. The STRUCTURAL claim (the bar-(b) flaw-count is mis-posed: m_0^2<0 in the record-count mode is the SAME object as record-genesis/arrow-of-time, which the standard picture lacks) is SOLID -- it rests on the identity of the mode (W153 T1 scale-invariance + W145 N=4-volume), not on a rate story. The DYNAMICAL claim (a GRACEFUL arrow-of-time engine) is PLAUSIBLE-not-INSIGHT: the DBI velocity cap and everpresent 1/sqrt(N) fade both give a finite, decelerating rate, but the graceful rate is read at the v^2=1/16 induced-metric-degeneration edge (|m_0^2|=1/4=4x beyond, W159 R2d) and leans on the ported everpresent normalization (W138 G5 de Sitter relabel; W153 sign-pin + normalization UNBUILT). LOAD-BEARING imports (CITED): W126 (the induced |II|^2 slice decomposition (2,1/3,8/9,-4); a1,a2 invariant under the conformal factor; the gradient series and its closed form); W130 (c_R = -4/9, native pole m_0^2 = -1/4); W159 (the gradient runaway closed form W = 2(...)/(16v^2-1)^3, degeneration at v^2=1/16, positive coefficients = DBI speed limit, |m_0^2|=4x margin, the k=0 dispersion); W153 (the tachyon lives in the conformal/scale mode = the BLMS record-count leg; scale-mode invariance T1); W145 (N = spacetime 4-volume, everpresent Lambda ~ +/- 1/sqrt(N)); W138 (G5 de Sitter relabel novelty gate). NEW here (DERIVED): the record-count identification p |-> N ~ e^{4p} monotone, so m^2<0 <=> N grows (P1); the STABLE=DEAD dichotomy (P2, m^2>=0 => oscillatory p => no accretion); the DBI-cap linearization (uncapped double-exponential blow-up vs capped linear p => steady de-Sitter-rate N, P3a); the everpresent-fade decelerating-drive mechanism (P3b); the k=0 = global-clock inversion of the W159 dispersion result (P4); the mis-posed-flaw-count structural concession (S4). HONEST RESIDUALS: (1) the graceful rate is validity-edge (P3c/S1); (2) everpresent leans on a ported, debit-carrying mechanism (S2); (3) the reframe is conditional on |II|^2 being GU's law and does NOT close the E2 AF/AS branch fork (S3, W159 route 1). NO canon / RESEARCH-STATUS / claim-status / verdict / posture change; the debit COUNT is not moved (the reframe re-poses the count, it does not authorize a canon change); H59/H61a OPEN; E2 AF/AS fork CARRIED. NO forbidden target {3,8,24,chi(K3),Ahat} assumed/inserted/hardcoded/divided-by."
construction: "program-native where the objects are GU's (the induced |II|^2 action and its conformal/scale mode; c_R = -4/9; the native pole m_0^2 = -1/4; the record-count / BLMS conformal-factor leg; N = 4-volume). Standard-field where the machinery binds any construction (the tachyonic-mode growth of a homogeneous k=0 field; the DBI/k-essence speed-limit-vs-restoring-force distinction; the everpresent-Lambda 1/sqrt(N) scaling; the Lorentz-irreducible spin decomposition; the autopoiesis stable=dead contrast; the de Sitter equilibrium identity). Every analogy PORTED and labelled; none asserted of GU. Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md."
depends_on:
  - explorations/W159-tachyon-escapes-2026-07-14.md
  - explorations/W153-marble-corrected-and-tachyon-cheapwood-test-2026-07-14.md
  - explorations/W145-substrate-sweep-mathematics-2026-07-14.md
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W166_tachyon_is_the_engine.py
external_refs:
  - "Ahmed, Dodelson, Greene, Sorkin, PRD 69 (2004) 103523 -- everpresent Lambda ~ +/- 1/sqrt(N), N = spacetime 4-volume in fundamental units"
  - "Silverstein & Tong, PRD 70 (2004) 103505 -- DBI / speed-limit kinetic structure (velocity cap, not excursion cap)"
  - "Maturana & Varela, Autopoiesis and Cognition (1980) -- a live self-producing system requires the productive instability; stable equilibrium = death"
  - "Stelle, PRD 16 (1977) 953 -- fourth-order gravity spectrum; the block-diagonal spin split"
  - "Gibbons & Hawking, PRD 15 (1977) 2738 -- de Sitter horizon thermodynamics (the everpresent magnitude anchor)"
---

# W166 -- the tachyon is the engine, not the debit

## 0. The inversion, in one paragraph

Every prior wave has scored the tachyonic scalaron (debit-1) as a FLAW: an instability the
healthy standard picture (LCDM + GR + SM) does not have, counted against GU in bar (b). This
wave inverts the framing and tests the inversion honestly. The standard picture's laws are
TIME-SYMMETRIC; it has no derived mechanism for record-genesis, for the arrow of time, or for
why the universe is not a dead static vacuum. Its arrow is imposed by a low-entropy boundary
condition, not derived. The tachyon lives in GU's conformal / scale mode, which W153 pinned as
the BLMS record-count leg (the conformal factor sets the 4-volume = the record count N; W145:
N = spacetime 4-volume, everpresent Lambda ~ +/- 1/sqrt(N)). An instability of that mode is
LITERALLY a drive for record-accretion. So read inverted, the tachyon is not an extra flaw the
standard picture lacks; it is a candidate for the very MECHANISM the standard picture is
missing. The wave asks three questions and grades the answer PLAUSIBLE: the structural claim
(the flaw-COUNT is mis-posed) is solid; the dynamical claim (a GRACEFUL engine) is plausible
but read at the edge of EFT validity.

Coherence-first: build the reframe at full strength, then let the skeptic try to kill it. Five
personas inline, one worker, no sub-agents. Deterministic test
`tests/W166_tachyon_is_the_engine.py`, 16/16 exit 0, W126 + W130 + W159 positive controls first.

## 1. Construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md), named

| Object | Construction used | Why |
|---|---|---|
| Scalaron mode | the covariant `c_R = -4/9`, native pole `m_0^2 = -1/4` (W130) | The pinned physical mass; W157 settled it rides `c_R`. |
| Record-count leg | the conformal / scale mode `p`; `a1, a2` invariant under `p` (W153 T1) | W153 pinned the tachyon INTO this mode; W145 pins `N` = 4-volume. |
| Record count | `N = 4-volume ~ e^{4p}` over a unit cell (BLMS: number = volume) | Monotone in the scale amplitude; makes "`p` grows" = "`N` grows". |
| Gradient sector | the W159 closed form `W = 2(...)/(16v^2-1)^3`, degenerate at `v^2=1/16` | The DBI speed limit; the object the reconciliation must handle. |
| Everpresent drive | `Lambda ~ +/- 1/sqrt(N)` (W145, ported Sorkin) | The candidate second saturation mechanism. |
| Standard picture | LCDM + GR + SM, time-symmetric laws | The comparison class in bar (b); it LACKS a derived arrow. |

## 2. Persona 1 -- cosmogenesis / arrow-of-time: does `m_0^2 < 0` literally mean `N` grows?

**Yes, and it is a chain of three facts each pinned by a positive control.**

First, the tachyon is the CONFORMAL / SCALE mode. W153 T1 proved the induced coefficients
`a1 = 1/3` and `a2s = 8/9` (hence `c_R` and `m_0^2`) are INVARIANT under the conformal factor
`p`; the scalaron IS the `p`-mode. Test P1a reproduces this at `p in {-2,-1,0,1,2}`. And W153
identified that same `p`-mode as the BLMS record-count leg: the conformal factor sets the volume
element.

Second, the record count is MONOTONE in `p`. W145 fixes `N` = spacetime 4-volume in fundamental
units; the 4-volume of a unit coordinate cell is `N ~ e^{4p}`, so `dN/dp = 4 e^{4p} > 0` (test
P1b). "`p` grows" is exactly "`N` grows".

Third, `m_0^2 < 0` makes the homogeneous mode GROW. The homogeneous (`k=0`) mode equation
`p'' + m_0^2 p = 0` with `m_0^2 = -1/4` has real characteristic roots `r = +/- 1/2`, so
`p ~ e^{+tau/2}` grows without bound (test P1c). Chaining: `m_0^2 < 0` gives growing `p`, and `N`
is monotone in `p`, so `m_0^2 < 0` LITERALLY means the record count grows. The tachyon and the
record-accretion that sources `Lambda > 0` are, under this reading, ONE phenomenon: the growth of
the record-count mode. That is the arrow of time.

> **The tachyon is not "a flaw the standard picture lacks." It is the statement "N grows" --
> the arrow of time -- written in the mass matrix.**

## 3. Persona 2 -- autopoiesis / self-production: a STABLE mode is a DEAD universe

Push the inversion to its sharpest form. A live, self-producing (autopoietic) system REQUIRES a
productive instability; a system that has relaxed to stable equilibrium is, by definition, dead.

Apply it to the record-count mode. A HEALTHY scalaron (`m^2 = +1/4 >= 0`) has purely imaginary
roots `r = +/- i/2`: the mode OSCILLATES and stays bounded (test P2a). A bounded, oscillating `p`
means `N` does not accrete monotonically -- the record count neither grows nor defines a
direction. A stable record-count mode is a dead, static, record-less universe with no arrow of
time.

So the `m^2 < 0` versus `m^2 >= 0` dichotomy is not healthy-versus-flawed; it is LIVE-versus-DEAD
(test P2b). Only `m^2 < 0` supplies a monotone record-genesis. And this is precisely the property
the standard picture cannot supply: its laws are time-symmetric, so it has no mode that
distinguishes a live record-accreting universe from a dead one. It borrows the arrow from a
low-entropy boundary condition. GU's tachyon, read this way, is the FEATURE that makes the
universe alive.

## 4. Persona 3 -- nonlinear dynamics: reconcile with W159 (graceful or pathological?)

This is the whole result. W159 read the same runaway and killed the "benign" story: route 2
OUT-OF-VALIDITY (the gradient sector is a DBI speed limit degenerating at `v^2 = 1/16`, the
tachyonic scale `|m_0^2| = 1/4 = 4x` beyond it; no bounded-field attractor). If the engine is
just that pathological runaway, the reframe is wishful. The reconciliation turns on a single
observation: **W159's kill was aimed at a SATURATION BALANCE -- a bounded-field ATTRACTOR, a
static vacuum minimum. But the arrow-of-time reading does not WANT an attractor.** An attractor
is a dead universe (persona 2). The right question is not "does a minimum exist" (it must not);
it is "is the RATE graceful (finite), or does it blow up." Two independent mechanisms make it
graceful.

**(i) The DBI velocity cap linearizes the runaway (test P3a).** W159 R2c established the exact
structural fact: the positive gradient function bounds field VELOCITY `|dphi|`, NEVER field
EXCURSION. Read as a bug (no attractor) in W159; read as the engine here it is the saving grace.
An UNCAPPED tachyon `p'' = |m^2| p` gives `p ~ e^{|m|tau}` (exponential) and hence
`N ~ exp(4 e^{|m|tau})` -- a double-exponential blow-up, genuinely pathological. But the DBI cap
`|dphi| <= v_max` forbids the exponential: on a minimum-free potential the field rolls AT the
speed limit, so `p ~ v_max * tau` grows LINEARLY, and `N ~ e^{4 v_max tau}` -- a STEADY,
de-Sitter-rate volume accretion with a FINITE rate. The speed limit is exactly what converts the
explosive runaway into a graceful, constant-rate arrow of time (the relativistic-particle
analogy: constant proper force, saturated velocity, linear position). And a steady exponential
4-volume growth is de Sitter expansion -- tying the record-count engine directly to the
everpresent-Lambda de Sitter phase.

**(ii) The everpresent fade decelerates the drive (test P3b).** W145's everpresent
`Lambda ~ +/- 1/sqrt(N)` gives a second, independent graceful mechanism. If the tachyonic drive
is tied to the record-count `Lambda` mode, the effective drive `~ 1/sqrt(N)` DECREASES as records
accrete (`d/dN [1/sqrt(N)] < 0`) and vanishes as `N -> oo`. The instability self-weakens as it
proceeds: an asymptotically freezing, decelerating arrow, not an explosive one. This is the
"bounded-by-everpresent" saturation the gradient sector alone lacked.

**The honest tension (test P3c).** The graceful STEADY rate needs the field rolling at the DBI
edge `|dphi| -> sqrt(1/16) = 1/4`, exactly where the induced-metric expansion degenerates (W159),
and `|m_0^2| = 1/4` sits `4x` beyond that edge. So GRACEFUL-IN-RATE is read at the boundary of
validity, not comfortably inside it. This is why the grade is PLAUSIBLE, not INSIGHT.

> **Reconciliation verdict: GRACEFUL-IN-RATE / BOUNDED-BY-everpresent.** The engine is
> velocity-capped (DBI => linear `p` => steady de-Sitter-rate `N`) and drive-fading (everpresent
> `1/sqrt(N)`), NOT the exponential blow-up W159's attractor-hunt implicitly rejected. W159's
> "runaway" and this wave's "arrow of time" are the SAME growing mode seen with opposite
> success criteria: W159 wanted a stable vacuum and correctly found none; the arrow reading
> wants an unbounded-but-finite-rate accretion and finds exactly that -- read at the validity
> edge.

## 5. Persona 4 -- symbolic engineer: the dispersion result, inverted

W159's dispersion result (D2) was read as neutral-to-negative: the tachyon peaks at `k = 0`
(homogeneous), unstable band `0 <= k < 1/2`, NO finite-`k` Turing band, "a real long-wavelength
tachyon, not a reinterpretable pattern." Under this lens the SAME result is a CREDIT (test P4).

A finite-`k` band would select a length scale -- a static spatial PATTERN, a frozen structure,
not a direction of time. A `k = 0` homogeneous monotone mode is precisely a GLOBAL, uniform clock:
the whole universe's record count advancing together. That is the cleanest possible profile for an
arrow of time. W159 D1 (the spin-block factorization, no cross-channel mixing) rules out any Turing
band, so the tachyon is UNAMBIGUOUSLY a global clock mode, not a pattern. The property W159 noted
as "no scale selection" is, inverted, "a single global time direction" -- which is what an arrow of
time must be. The dispersion structure supports the reframe rather than undercutting it.

The test file (`tests/W166_tachyon_is_the_engine.py`, 16 checks, exit 0, exact sympy) pins every
step: PC1-PC5 reproduce W130's `c_R = -4/9` and `m_0^2 = -1/4`, and W159's gradient closed form,
positive coefficients, and `4x` margin, through the verbatim W126 Route-1 machinery; P1a-P1c the
record-count identification and the growing mode; P2a-P2b the stable=dead dichotomy; P3a-P3c the
DBI linearization, the everpresent fade, and the validity-edge tension; P4a-P4b the `k=0`
global-clock inversion; S1-S4 the skeptic's steelmen and the one concession.

## 6. Persona 5 -- adversarial skeptic RUTHLESS: is the reframe wishful?

The reframe is the most seductive one in the whole tachyon arc, so the skeptic must be merciless.
Three steelmen land, one concession is forced.

**S1 (validity) -- the engine is not certified inside EFT validity.** The graceful rate is read at
`v^2 -> 1/16` and the tachyonic scale is `4x` beyond it (test S1, = W159 R2d). A story that only
works at the boundary of validity is not a derivation. This BLOCKS an INSIGHT grade outright.

**S2 (everpresent) -- the fade carries standing debits.** The `1/sqrt(N)` saturation is degenerate
with the W138 G5 de Sitter identity (a NOVELTY kill: it is the Gibbons-Hawking equilibrium
relabeled), and its GU sign-pin and normalization are UNBUILT (W153). "Bounded-by-everpresent" is
a PORTED mechanism carrying those debits, not a GU derivation (test S2).

**S3 (conditionality) -- nothing standing is removed.** The reframe is conditional on `|II|^2`
being GU's law (W153) and does NOT close the E2 AF/AS branch fork (W159 route 1). The tachyon is
still an AF-branch tree-level object. The lens REINTERPRETS its valence; it does not dissolve the
conditionals (test S3).

**S4 (the forced concession) -- the flaw-COUNT in bar (b) is MIS-POSED.** Here the skeptic must
give ground, and precisely. `m_0^2 < 0` in the record-count mode is the SAME object as
record-genesis / the arrow of time (P1c), which the standard picture's time-symmetric laws
genuinely LACK (P2b). To DEBIT the tachyon while ignoring the arrow it supplies is to double-count:
you are charging GU for the mechanism and refusing it the credit for what the mechanism does. The
tachyon is debit-credit-ENTANGLED, not a free-standing extra flaw. And this is a STRUCTURAL point
-- it rests on the identity of the mode (W153 scale-invariance + W145 `N` = 4-volume), independent
of the rate/validity question that S1-S2 attack. So the skeptic keeps the dynamics under fire but
must concede the accounting.

> **Skeptic verdict: NOT wishful on the STRUCTURAL claim (the flaw-count is mis-posed; the tachyon
> IS the arrow mode), but the GRACEFUL-ENGINE DYNAMICS are PLAUSIBLE not proven (validity edge +
> ported everpresent). Honest grade: PLAUSIBLE.**

## 7. Synthesis -- the return fields

**Q1 -- does `m_0^2 < 0` in the record-count mode literally mean record growth?** **YES.** The
tachyon is the conformal / scale mode (P1a, W153 T1 invariance), `N = 4-volume ~ e^{4p}` is
strictly monotone in the scale amplitude (P1b, W145), and `m_0^2 = -1/4 < 0` gives a growing
homogeneous mode (P1c, roots `+/- 1/2`). So `m^2 < 0` `<=>` `N` grows `=` the arrow of time `=`
everpresent record accretion. The tachyon and the `Lambda`-sourcing record-accretion are ONE mode.

**Q2 -- the W159 reconciliation.** **GRACEFUL-IN-RATE / BOUNDED-BY-everpresent**, NOT the
pathological blow-up W159's attractor-hunt implicitly assumed. The DBI velocity cap converts the
exponential runaway into LINEAR `p` growth (steady de-Sitter-rate `N ~ e^{4 v_max tau}`, P3a), and
the everpresent `1/sqrt(N)` fade makes the drive DECELERATE (P3b). W159's "runaway" and this
wave's "arrow" are the same growing mode under opposite success criteria (stable vacuum vs
finite-rate accretion). BUT the graceful rate is read at the `v^2 = 1/16` induced-metric
degeneration edge, `|m_0^2| = 1/4 = 4x` beyond (P3c/S1) -- so PLAUSIBLE, not INSIGHT.

**Q3 -- does the tachyon move to the CREDIT side of bar (b)?** **PARTIAL: the flaw-COUNT is
MIS-POSED, but the tachyon does NOT move to pure CREDIT.** The count is mis-posed because the
tachyon is debit-credit-ENTANGLED (S4): it is the arrow-of-time mode, which the standard picture
lacks, so charging it as a free extra flaw double-counts. But it does not become pure credit,
because (i) the arrow it supplies is EXPLANATORY depth (a derived arrow vs GR's boundary-condition
arrow), not EMPIRICAL adequacy -- the standard picture's imposed arrow is observationally fine; and
(ii) the graceful engine sits at the validity edge and leans on ported everpresent machinery. Net:
**bar (b) is RE-POSED, not cleared.** The honest movement is that debit-1 stops being scorable as a
free-standing flaw and becomes a flaw entangled with an explanatory credit the flaw-count omitted.

**Honest grade: PLAUSIBLE.** The structural claim (the flaw-count is mis-posed; the tachyon is the
record-count / arrow-of-time mode) is SOLID -- it rests on mode identity (W153 + W145), not on a
rate story, and survives all three steelmen. The dynamical claim (a graceful arrow-of-time engine)
is PLAUSIBLE-not-INSIGHT: two real saturation mechanisms (DBI cap, everpresent fade) give a finite,
decelerating rate, but read at the `v^2 = 1/16` validity edge and leaning on the ported,
debit-carrying everpresent normalization. Not WISHFUL (the structural spine is computed and
survives the skeptic); not INSIGHT (the dynamics are not certified in validity).

## 8. Gates and honest limits

- Exploration grade; conditional register throughout. Nothing asserts GU, asserts a vacuum,
  asserts an arrow of time as fact, or changes any verdict or the debit count. The results are
  computed statements about the identity and growth of a mode, with W126 + W130 + W159 positive
  controls, not assertions about the world.
- W138 battery honored. The everpresent lean is flagged with its standing G5 novelty debit (S2);
  the reframe claims no new normalization and re-imports no killed identity as truth. The tempting
  overreaches the geometry forbids are recorded: "the tachyon is a graceful engine, proven"
  (forbidden -- validity edge, S1); "everpresent saturation is a GU derivation" (forbidden --
  ported, unbuilt, S2); "the reframe dissolves debit-1" (forbidden -- E2 fork stands, S3).
- Tri-repo gating held: record / finality / arrow-of-time / capability semantics stay pointers to
  temporal-issuance / time-as-finality / TaF; GU owns the induced-action math and the mode identity
  only. No cross-repo identity asserted.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the debit COUNT is not
  moved (the reframe re-poses whether the count is well-defined; it does not authorize a canon
  edit); H59/H61a OPEN; the E2 AF/AS fork CARRIED, not closed. No forbidden target
  {3, 8, 24, chi(K3), Ahat} assumed or inserted. Zero em dashes in paper-facing text.

*Filed 2026-07-14 by Team LENS-ENGINE (W166). Coherence-first; invert the framing, then let the
skeptic kill it. Five personas inline in one worker (cosmogenesis/arrow-of-time theorist;
autopoiesis/self-production theorist; nonlinear-dynamics/W159-reconciliation theorist; symbolic
engineer; adversarial skeptic RUTHLESS); no sub-agents. Reproducible:
`python -u tests/W166_tachyon_is_the_engine.py` (16/16, exit 0; W126 + W130 + W159 positive
controls first). Exploration grade; conditional register; honest grade PLAUSIBLE; no canon
movement.*
