---
title: "Eric/Curt Wave 3D-B2C9 — First-Jet Euler, Fermion-Current, and Preboundary Comparators"
status: active-research
doc_type: exploration
updated_at: "2026-08-01"
---

# Eric/Curt Wave 3D-B2C9 — First-Jet Euler, Fermion-Current, and Preboundary Comparators

## Result

B2C9 makes four construction advances and exposes one sharp fork.

First, the bosonic connection Euler object is now computed as a genuine local
first-jet Euler covector, not as the older twelve-coordinate algebraic partial
derivative.  It obeys the exact coadjoint identity

\[
D E_T[R_\chi](u)+E_T([\chi,u])=0
\]

on a noncentral rational fixture, including nonzero \(d\chi\) and the moving
Shiab insertion.  A pure-\(d\chi\) control vanishes for the completed Euler
covector and fails for the old zero-jet reconstruction.  This is a correction
of object, not a sign tweak.

Second, an independent-dual two-connection fermion action is varied in every
finite slot.  It emits separate connection currents

\[
J_A=(r\bar\zeta+\ell\bar\nu)\otimes\nu,
\qquad
J_B=(w\bar\zeta+s\bar\nu)\otimes\zeta.
\]

In a scalarized shared-connection, zero-southeast comparator

\[
w=2\ \text{(standing for one scalarized }\mathscr S\text{ weight)},
\qquad \ell=0,\qquad r=s=1,
\]

their sum exactly matches the three bilinear monomials in the draft's equation
`(9.18)` current alphabet:

\[
\bar\nu\otimes\zeta
+\bar\zeta\otimes\nu
+2\,\bar\zeta\otimes\zeta.
\]

The B2C5 nonzero-southeast comparator instead adds the monomial

\[
\boxed{\ell\,\bar\nu\otimes\nu}.
\]

That is the decisive new finite discriminator.  It does **not** yet prove an
identity or mismatch of the typed draft current, which also contains the
Hodge/density dual, moving \(\mathscr S_\omega\), representation action, and
adjoint projection.  It shows that the repaired operator does not preserve the
written current's bilinear alphabet in this comparator without an additional
source/action mechanism.  The active typed current identity remains open.

Third, if a zero-order block is built tensorially from \(T=A-B\), the finite
action returns

\[
E_A^{\rm F}=J_A+J_T,
\qquad
J_B^\circ-J_T
\quad\text{as the primitive intermediate }B\text{-covector}.
\]

If \(B=B(\epsilon,g)\), the owner equations must then receive
\((D_\epsilon B)^!(J_B^\circ-J_T)\) and
\((D_gB)^!(J_B^\circ-J_T)\).  The finite model executes the formal chain rule
for a supplied arbitrary \(\delta B\); it does not construct
\(D_\epsilon B\), \(D_gB\), their formal transposes, or the actual
trace-reversed DeWitt/Hodge/Krein metric stress on \(Y^{14}\).

Fourth, the independent density-dual action and a generic same-field
moving-lowerer architecture control are kept apart.  The independent row
already contains its lowering and receives no second Krein factor.  The
two-vector same-field control shows that a moving generic indefinite lowerer
contributes exactly once.  Its inverse obeys

\[
\delta R=-R(\delta b)R.
\]

An additive one-dimensional G3-shaped plus **unsymmetrized** independent-dual
fermion Green comparator closes with a nonzero boundary contribution.  It is
not yet the B2C5 symmetrized action's total preboundary form, which requires
both fermion boundary terms and the moving \(D\) response.  It does not select
a Green domain, polarization, Poisson anchor, or prequantum lift.

The probe passes

```text
22 computational exact + 4 source receipts + 17 type-level + 11 planted = 54 PASS
```

The source receipts verify recorded text, and the type-level rows are scope
declarations; neither is additional mathematical computation.
No P1, P2, or P3 datum is consumed or changed.

## Layer 0 — the objects before the equations

The following distinctions are load-bearing.

| term | object used here | explicitly not identified with |
|---|---|---|
| \(\zeta\) | physical spinor-valued one-form | gauge ghost or odd parameter |
| \(\nu\) | physical spinor-valued zero-form | ghost or a claim about one neutrino species |
| \(\widetilde{\bar\Psi}\) | independently varied density-dual row | automatically \(\Psi^\dagger b_F\) before restriction |
| constrained-real field | future pullback to a moving symplectic-real subbundle | the independent-dual field space or the finite same-field control |
| adjoint Euler current | \(\delta_A I_F\) in the connection Euler dual | Noether, Green, or observed Maxwell current |
| Noether current | term in the coupled ordinary-gauge Ward identity | isolated connection conservation |
| Green current | integration-by-parts boundary flux | a field-space Poisson inverse |
| completed \(E_T\) | \(\partial L/\partial T-\partial_j(\partial L/\partial T_{|j})\) | the earlier constant-direction algebraic partial derivative |
| trace-reversed fibre pairing | raw `Sym2` Frobenius `(7,3)` reversed to `(6,4)` | a positive Frobenius norm |
| total carrier | fibre `(6,4)` plus base `(3,1)`, hence `(9,5)` | Curt's literal `(7,7)` rival carrier |

This clears the exact failure mode described in the session handoff: a control
can verify a computation while the computation still concerns the wrong
object.

## Primary-source collision

### Source grades

- The rendered 2021 draft, especially pages 46–49, is an author working draft
  and primary source.
- The 2020 Portal material is an official-host, community-edited transcript;
  video should be checked for a load-bearing verbatim quotation.
- The 2025 UCSD/Into the Impossible material records author speech in raw and
  edited transcript forms.
- The 2025 Theories of Everything transcript is a third-party automatic
  transcript.  Its recalled token ordering and signs remain tentative.

### `SOURCE-CONFIRMS`

Draft equations `(9.16)–(9.20)` use

\[
\zeta,\bar\zeta\in\Omega^1(Y,S),
\qquad
\nu,\bar\nu\in\Omega^0(Y,S),
\]

and treat barred and unbarred variables as an independent classical
variational packet.  Equation `(9.18)` packages

\[
\Upsilon^F_\omega
=*\left(
d_{A_\omega}\nu+\mathscr S_\omega d_{A_\omega}\zeta,
d_{A_\omega}^*\zeta,
\bar\nu\zeta+\bar\zeta\nu+\mathscr S_\omega\bar\zeta\zeta
\right).
\]

Equation `(9.20)` gives two alternatives:

\[
\Upsilon^B_\omega+\Upsilon^F_\omega=0,
\qquad\text{or}\qquad
D_\omega^*\Upsilon^B_\omega=\Upsilon^F_\omega.
\]

They are rival coupling architectures.  The source does not license adding
both bosonic actions plus the fermion term indiscriminately.

Portal `01:47:01–01:54:09` supports the intended Bose/Fermi somatic routes and
one Einstein-shaped obstruction.  Portal `01:55:03–01:58:27` supports the
physical \(\zeta/\nu\) carrier.  Portal `02:03:07` also says the stress-energy
and Dirac route cancellations were still being assembled.

TOE `02:44:06–02:45:13` states an unreleased two-connection on-shell program.
It does not publish the matrix, define “on shell,” or select \(A=B\).

### `SOURCE-CORRECTS`

The source-compatible shifted cones constructed in B2C8 are repo
reconstructions.  The remaining B2C8 wording “the 2025 cone is confirmed” must
be read, and is corrected here, as:

> the repo-constructed, source-compatible 2025-shaped cone is proved to be a
> connection-level equality complex in the tested realization.

This is not confirmation of a released Weinstein formula.

Likewise, “shared obstruction” does not mean equality of the raw Bose and
Fermi matrices.  It means one Euler owner seen through typed carrier maps.

### `SOURCE-SILENT`

No checked source supplies:

- the complete active `(9,5)` right-`H`, Krein, constrained Grassmann action;
- the full fermionic return into \((E_A,E_\epsilon,E_g)\);
- the moving Hodge, density, charge, projector, and trace-reversed DeWitt
  derivative formulas;
- a GU Green form/domain, characteristic kernel, BFV quotient, Poisson anchor,
  polarization, or prequantum line bundle.

These are construction obligations, not no-go results.

## L1 — algebra and representations

The active parent family remains

\[
D_{\rm pr}(k)=
\begin{pmatrix}
A_w(k)W & K(k)B\\
C_g(k)D & c(k)L
\end{pmatrix}+M_0,
\]

with

\[
W=w_+P_++w_-P_-,
\qquad
L=\ell_+P_++\ell_-P_-.
\]

B2C5 executed the tied off-diagonal stratum \(B=D=r1\).  The repaired
semisimple stratum obeys

\[
12w_+\ell_-+11r^2=0,
\qquad
12w_-\ell_++11r^2=0.
\]

The exact fixture uses

\[
w_+=2,\quad w_-=3,\quad r=\frac32,
\quad \ell_+=-\frac{11}{16},
\quad \ell_-=-\frac{33}{32},
\]

and retains

\[
p=\frac{w_+w_-}{r^2}=\frac83.
\]

After the known chiral-boost equivalence the overall source coupling and
\(p\) remain.  They are ordinary construction parameters, not external
datum P1/P2/P3.

The strata must remain separate:

1. strict zero principal and zero-order southeast corner;
2. zero principal southeast with an allowed zero-order \(M_{00}\);
3. nonzero first-order southeast repair;
4. degenerate coefficient strata.

The zero-order completion is a typed block operator

\[
M_0=
\begin{pmatrix}
M_{11}&M_{10}\\
M_{01}&M_{00}
\end{pmatrix}.
\]

Krein and charge reality relate \(M_{10}\) and \(M_{01}\); they do not allow
one to be selected in isolation.  Completeness of a natural equivariant basis
built from \(T\), contracted curvature, and the soldering/reduction data is
open.

## L2 — differential complex and the first-jet correction

The bosonic finite model uses

\[
L_B=
\left\langle T,\mathscr S_H\left(
F_B+\frac12D_BT+\frac13q(T,T)
\right)\right\rangle
+\frac{\kappa m}{2}\langle T,*T\rangle.
\]

Here \(B_i,B_{i|j}\) are arbitrary rational connection jets carrying the
correct prolonged gauge law.  B2C9 does not reconstruct them from jets of
\(u,\omega_{\rm LC}(g)\) or execute the actual
\(B=A_{\rm LC}(\epsilon,g)\) graph; that ownership remains inherited as a
conditional input from G1.

The previous reconstruction varied twelve zero-jet \(T\) coordinates with
\(d(\delta T)=0\).  It therefore reconstructed only the algebraic partial
derivative.

B2C9 carries full local first jets

\[
(B_i,T_i,H;B_{i|j},T_{i|j},H_{|j})
\]

and computes the Euler functional operationally:

\[
E_T(u)=
\frac{\partial L}{\partial T}(u)
-\sum_j\partial_j\left(
\frac{\partial L}{\partial T_{|j}}(u)
\right).
\]

The prolonged local gauge tangent is

\[
\begin{aligned}
\delta B_i&=[\chi,B_i]-\chi_i,\\
\delta B_{i|j}&=[\chi,B_{i|j}]+[\chi_j,B_i]-\chi_{ij},\\
\delta T_i&=[\chi,T_i],\\
\delta T_{i|j}&=[\chi,T_{i|j}]+[\chi_j,T_i],\\
\delta H&=[\chi,H],\\
\delta H_{|j}&=[\chi,H_{|j}]+[\chi_j,H].
\end{aligned}
\]

On the exact fixture:

- the invariant coadjoint sign passes;
- a pure \(d\chi\) tangent produces zero completed Euler response;
- freezing \(H_{|j}\) fails;
- omitting the prolonged \(B\)-jet law fails;
- the opposite coadjoint sign fails;
- the old zero-jet reconstruction fails the pure-\(d\chi\) control.

This supplies a connection-component deformation arrow on the finite first-jet
architecture.  It does not yet prove the full-tuple Hessian identity

\[
D\mathfrak G[R_\chi]+(D R_\chi)^!\mathfrak G=0
\]

on the active bundle.

## L3 — action and Euler equations

In the finite two-sector truncation, write

\[
\begin{aligned}
R_\zeta&=wD_B\zeta+rD_A\nu+M_{11}\zeta+M_{10}\nu,\\
R_\nu&=sD_B\zeta+\ell D_A\nu+M_{01}\zeta+M_{00}\nu,
\end{aligned}
\]

and

\[
I_F=\widetilde{\bar\zeta}R_\zeta+
\widetilde{\bar\nu}R_\nu.
\]

The exact all-slot variation moves:

- \(\zeta,\nu\) and their first jets;
- the independent barred rows;
- both connections;
- all four \(M_0\) blocks;
- the density.

It equals the exact directional derivative.  The full first-jet ordinary gauge
Ward identity also closes.  Dropping \(d\chi\) from the matter-jet law or
freezing a noncentral \(M_0\) fails.

For a tensorial placement \(M_{10}=T=A-B\), variation derives

\[
E_A^F=J_A+J_T,
\qquad
J_B^\circ-J_T\quad\text{as an intermediate }B\text{-covector}.
\]

This is a formal owner-chain theorem for a supplied \(\delta B\), not an
independent \(B\) Euler equation.  The active \(Y^{14}\) version must construct
the actual Levi-Civita/reduction graph and its transposes and include every
dependence of \(D^a,M_0,b_F,C_+\), the density, and the observation graph.

## L4 — constraints and gauge identities

The completed finite bosonic Euler covariance is not isolated current
conservation.
The durable total Ward identity must be

\[
\langle E_A,-D_A\chi\rangle
+\langle E_\epsilon,\chi\epsilon\rangle
+\langle E_\Psi,\rho(\chi)\Psi\rangle
+\langle E_{\bar\Psi},-\bar\Psi\rho(\chi)\rangle
+\text{boundary}=0.
\]

On the future constrained-real pullback the two fermion variations must be
related.  B2C9 does not yet construct that symplectic-Majorana/Grassmann
restriction.

No external datum is allowed to choose \(p\), a southeast block, an \(M_0\)
term, a current, or a domain.  Those are action/symmetry/reality/domain
construction debts.

## L5 — Green and preboundary data

The unsymmetrized one-dimensional fermion fork verifies the moving-pairing
identity

\[
\int \bar\Psi\,b(x)\,\partial_x\Psi
=-\int \left(
\partial_x\bar\Psi\,b
+\bar\Psi\,\partial_x b
\right)\Psi
+[\bar\Psi b\Psi]_{\partial}.
\]

Freezing \(\partial_xb\) fails.  Adding this flux to a G3-shaped
one-dimensional bosonic comparator gives an exact additive identity

\[
\Theta_{\rm cmp}=\Theta_{\rm G3\text{-}shaped}+\Theta_{F,\rm unsym},
\]

with nonzero fermion contribution and a nonzero antisymmetric comparator
two-form.  The B2C5 symmetrized action instead has both
\(\bar\Psi D(n)\delta\Psi\) and
\(-\delta\bar\Psi D(n)\Psi\) boundary terms.  Its moving-\(D\) total form
remains open.

This is still preboundary data.  It does not choose:

- a common Green-Lagrangian domain;
- a characteristic-kernel quotient;
- a polarization;
- an invertible reduced symplectic form;
- a Poisson or prequantum map.

## L6 — global and analytic domain

Open:

- actual \(Y^{14}\) patch descent of the completed fermion action;
- closed active right-\(\mathbb H\)/Krein domain;
- constrained Grassmann/symplectic-Majorana functional analysis;
- common boson/fermion Green domain for either draft coupling architecture;
- nonlinear constraint propagation and maximal dissipativity;
- observation/retract compatibility of the total preboundary packet.

B2C5 already showed that its positive-energy incoming half is not isotropic for
the frozen action Green form.  The B2C9 unsymmetrized comparator shows that a
fermion fork can change the boundary form, so the old domain verdict cannot be
reused before the actual symmetrized total form is recomputed.

## L7 — physics recovery and claims not made

This swing does not claim:

- a stationary solution;
- a Standard Model spectrum, hypercharge, Yukawa coefficient, or Higgs VEV;
- a Maxwell/Yang–Mills observed current;
- unitarity or a quantum theory;
- an index, generation count, or chiral count;
- a cosmological constant, dark-energy, or dark-matter prediction;
- a Poisson/prequantum/BFV completion.

The current result is valuable one layer earlier: it identifies which action
forks produce which Euler currents and which moving geometric responses must
be present before those physics comparisons are meaningful.

## Constraint-surplus ledger

The earlier statement that the semisimple tied family is fully selected remains
false.  The exact executed relations leave:

- one overall source coupling;
- one dimensionless shape \(p\).

The draft current suggests a new compatibility condition: its written bilinear
alphabet has no \(\bar\nu\otimes\nu\) term.  In the scalarized
shared-connection comparator, the nonzero-southeast derivative repair adds
exactly that monomial.  Whether the full typed current preserves, absorbs, or
cancels it requires at least one further typed mechanism.

This is not yet a global constraint-surplus count.  The available choices
include different connection ownership, an \(M_0\) or cyclic completion, or a
corrected current in a source-admitted alternate version.  Each must be written
and varied before counting independent constraints and equivalences.

## Specialist audit

Three independent specialist lenses were used before construction:

1. affine/cohomological geometry detected that the old Euler reconstruction
   omitted the horizontal divergence and designed the pure-\(d\chi\) kill;
2. Krein/PDE analysis separated the coefficient strata, independent-dual and
   future constrained-real variations, generic moving inverse response, and
   the symmetrized total-Green burden;
3. a source/axiom audit typed the 2021 current, the two rival coupling
   architectures, the unreleased 2025 object, and the current/stress/Green
   homonyms.

Their common correction was to compute the action-owned current and full
first-jet Euler object before asking for factorization or a domain.

## Next gate

The highest-information successor is

```text
ECW3D-B2C10-ACTIVE-9_5-SOURCE-CURRENT-REPAIR-AND-FULL-TUPLE-HESSIAN
```

It should:

1. lift the exact finite bilinear-current comparator to the active
   \(Cl(9,5)=M(64,\mathbb H)\)
   rolled carrier;
2. enumerate natural equivariant \(M_0\) blocks from \(T\), contracted
   curvature, and soldering data;
3. determine whether connection ownership, a cyclic/two-connection completion,
   or a source-admitted current correction can reconcile the nonzero-southeast
   semisimple repair with the typed draft current;
4. execute the constrained Grassmann/symplectic-real variation and all moving
   Hodge, density, Krein, charge, projector, and trace-reversed DeWitt returns;
5. verify the full-tuple differentiated Ward/Hessian identity.

Only after that should

```text
ECW3D-B2C6B-FINAL-TOTAL-ACTION-COMMON-GREEN-DOMAIN-REPLAY
```

compare the two source-explicit coupling architectures on the recomputed total
boundary form.

## Artifacts

- Probe:
  `tests/channel-swings/eric_curt_wave3d_b2c9_offdiagonal_total_current_preboundary_probe.py`
- Registry:
  `lab/process/eric-curt-wave3d-b2c9-offdiagonal-total-current-preboundary.json`
- Predecessor:
  `explorations/eric-curt-wave3d-b2c8-source-forked-two-connection-square-euler-map-2026-08-01.md`
