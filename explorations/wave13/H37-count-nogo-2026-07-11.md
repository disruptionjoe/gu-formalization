---
title: "H37 (Wave 13) -- THE COUNT ENDGAME: the generation count is PROVABLY located-not-forced within the built (9,5)+positivity+no-import structure. The fermion analog of H27. The H29 mutual-exclusion is a genuine NO-GO."
artifact_type: exploration
status: exploration
updated_at: "2026-07-11"
wave: 13
depends_on:
  - "tests/wave13/H37_count_nogo.py"
  - "tests/wave12/H29_fermion_c2_wall.py"
  - "explorations/wave12/H29-fermion-c2-wall-2026-07-11.md"
  - "tests/wave10/H27_soldering_palatini.py"
  - "explorations/wave10/H27-soldering-palatini-2026-07-11.md"
  - "tests/big-swing/sg1_signature_carrier_parity_77.py"
  - "explorations/sequential-goals-2026-07-09/SG1-signature-carrier-parity-77.md"
  - "canon/no-go-quaternionic-parity-generation-sector.md"
  - "canon/carrier-dirac-mass-capstone-RESULTS.md"
  - "canon/two-arena-rep-theory-core-RESULTS.md"
  - "absorbed/gu-source-action/DEAD-ENDS.md"
verdict: "NO-GO (high confidence, structural), CONDITIONAL on (9,5)+positivity+no-import. The H29 mutual exclusion is a GENUINE EXHAUSTION at the symmetry-class level: an odd generation count needs a boundary Dirac D_Sigma with a count-invariant != 0; on (9,5) BOTH count-invariants are theorem-forced to 0 -- the Z-valued APS eta by the anticommuting chiral grading G ({G,H_bd}=0, spectrum +/-symmetric, eta=0 EXACT), and the Z/2 nullity-parity by Kramers (J^2=-1, even multiplicity). Forcing eta!=0 requires breaking G; breaking G additively is LOCKED OUT by the positive Koszul-Tate Hessian ((H_bd+V)^2=H_bd^2 forces V^2=0 => V=0), and the only symmetry escape (the antilinear chiralizer J_quat.G) is FRAME-TRIVIAL (tangent-frame charge 0.0) -- the forbidden import. These two are mutually exclusive with (preserve positivity AND no import). => the count is PROVABLY located-not-forced within the built structure: the fermion analog of H27. POSITIVE CONTROL FIRES: on (7,7) J^2=+1 the SAME nullity-parity test detects an admissible odd rank-3 (obstruction lifts) -- the method is not rigged. Signature-contingent: dissolves under (7,7), which is precisely the H19 signature question."
---

# H37 -- THE COUNT ENDGAME (fermion analog of H27)

**Discipline.** This is a PRESERVING verdict, so the risk is LAZY closure (fabricating an
unearned no-go), not just over-eager forcing. Every positive claim below is an exact matrix
identity on the verified `Cl(9,5) = M(64,H)` representation (residuals `0.0` or `~1e-14`), an exact
eigenvalue-count, or an explicit ratio. **Nothing imported** (no `24/8`, no `chi(K3)`, no assumed
antilinear chiralizer, no fitting to 3). The required **positive control** is built and **fires**:
on `(7,7)` the same machinery detects a count-forcing invariant `!= 0`, so the method is not rigged
to answer "no-go." Reproducible: `python tests/wave13/H37_count_nogo.py` (exit 0, 12/12 PASS).

---

## 0. The question (H29's precisely-named residual)

H29 sharpened the fermion count-carrier `C2 = 155.36` to the Hermitian part of a constraint
leakage `O = Q M_D Pi_RS + Pi_RS M_D Q` that is simultaneously **particle-hole-odd**
(`{G, O} = 0`, `G = Pi_RS - Q`), **Kramers** (`J^2 = -1`), and **Krein-self-adjoint** -- symmetry
class **CII** -- so its index/eta is theorem-forced to zero. To force an odd generation count from
the fermion index you need a grading-**breaking** boundary Dirac `D_Sigma` with `eta != 0`. H29's
claim, now tested:

> `eta != 0` (count forced) `<=>` break the CII grading `<=>` **EITHER** break the positive-Hessian
> Koszul-Tate/BV grading (destroys the source-action positivity) **OR** import the frame-trivial
> antilinear chiralizer (a forbidden import). On current data these are **mutually exclusive** --
> that exclusion IS the wall.

H37 asks: is that mutual exclusion a **genuine NO-GO** (=> the count is provably located-not-forced,
the fermion analog of H27's soldering-postulate result), a real **admissible ROUTE**, or **OPEN**?

---

## 1. The three properties, formalized on the actual `Cl(9,5)` rep

### (a) An odd count requires a boundary object with a count-invariant `!= 0` -- and on `(9,5)` BOTH vanish

The generation count, read as a fermion index, is the spectral asymmetry of a boundary Dirac
`D_Sigma`. There are two count-invariants and **both are theorem-forced to zero on `(9,5)`**:

- **Z-valued APS `eta`.** Build the Hermitian boundary-Dirac proxy `H_bd = (O + O^dag)/2` and the
  grading `G = Pi_RS - Q` (`G^2 = I`, residual `1.3e-14`). `{G, H_bd} = 0` (residual `5.5e-14`) is a
  pure `Q Pi_RS = 0` algebraic identity, so the spectrum is `+/-`-symmetric and **`eta(H_bd) = 0`
  EXACTLY** (integer, computed). *(Part A1.)*
- **Z/2 nullity-parity (mod-2 index).** With `C = e1 e3 e5 e7 e10 e12` (`C^2 = -I`, residual `0.0`),
  the Kramers structure `J^2 = -1` forces **even eigenvalue multiplicity** on every J-commuting
  Hermitian (all-even-mult `True` over samples) -> even nullity -> **mod-2 index `= 0`**. *(Part A2.)*

So the count is forbidden from the fermion index on `(9,5)` **twice over** -- a Z lock and a Z/2
lock -- both freshly computed, not merely cited.

### (b) `eta != 0` requires breaking the anticommuting chiral grading `G` **specifically**

Not Kramers, not Krein-self-adjointness -- the grading `G` is the exact `eta = 0` forcer:

- **B1.** A grading-**preserving** (G-odd) perturbation keeps the spectrum `+/-`-symmetric:
  `eta(H_bd + t V_odd) = 0`.
- **B2.** A grading-**breaking** (G-even) perturbation drives `eta != 0`: `eta(V_even) = -2`,
  `eta(H_bd + t V_even) = -2`.
- **B3.** Kramers alone does **not** kill `eta`: a J-commuting (Kramers-symmetric) but **G-even**
  Hermitian carries `eta = 28 != 0`. So the eta-forcer is the **grading**, not `J`. *(This cleanly
  separates the two locks: grading <-> Z-eta, Kramers <-> Z/2 nullity-parity.)*

### (c) Breaking `G` => break the positive Hessian **OR** import the frame-trivial chiralizer

- **C1 (positivity locks additive breakers).** The grading is inherited from
  `D_Sigma^2 = M_KT` (the positive Koszul-Tate/BV Hessian, C-01). Requiring `(H_bd + V)^2 = H_bd^2`
  for a G-even Hermitian `V`: split by G-parity, `{H_bd, V}` is G-odd and `V^2` is G-even, so both
  must vanish; `V^2 = 0` with `V = V^dag` forces `V = 0`. Computed: a normalized G-even `V` has
  `||V^2|| = 0.034 > 0` and the G-even part of `(H_bd+V)^2 - H_bd^2` equals `V^2` exactly
  (residual `4.8e-14`). **Positivity locks out every additive grading-breaker.**
- **C2 (the only symmetry escape is the import).** The one move that dodges the positive-square lock
  is the antilinear chiralizer `C = J_quat . G` (a symmetry/re-grading, `C^2 = -1`, class-CII
  particle-hole), which revives a nonzero index. But its antilinear factor `J_quat = id_14 (x) C`
  is **frame-trivial**: `max || [J_quat, L_ij] || = 0.0` over all 91 tangent-frame `so(9,5)`
  rotations `L_ij = (E_ij - E_ji) (x) I_128` (exact, by the kron split). A pure internal
  `M(64,H)`-fiber re-grading -- the capstone's selector-side, 2-primary import.
- **C3 (classifier -- the decisive line, H27-style).** A grading-breaking `V` is **EITHER** a
  Hessian-changer (C1: breaks `D^2 = M_KT > 0`) **OR** the frame-trivial chiralizer (C2: import).
  No third admissible option at the symmetry-class level. **Mutually exclusive** with
  (preserve positivity **AND** no import).

---

## 2. The positive control (`(7,7)`, `J^2 = +1`): the method is not rigged

The required control -- exhibit a setting where the **same** machinery correctly detects a
count-forcing invariant `!= 0` -- **fires**, and it fires on the honest leg:

- **D1. The grading leg is signature-INDEPENDENT.** `{G, O} = 0` is the `Q Pi = 0` identity, so it
  holds on `(7,7)` too (`{G, O} = 5.9e-14`). The **Z-eta lock does not lift** under signature
  change. This is an honest, precise finding: the escape is **not** via the Z-eta/grading leg.
- **D2. The Kramers/mod-2 leg is signature-SPECIFIC and it LIFTS.** On `(9,5)` (`J^2 = -1`) an
  odd-rank J-commuting projector is **unreachable** (odd nullity forbidden -> mod-2 index `= 0`). On
  `(7,7)` (`J^2 = +1`) a genuine **rank-3** J-commuting projector **exists** (`odd-reachable = True`,
  `[P] = P`, `J P = P J` to `< 1e-9`). The **same** nullity-parity test flips from "forced 0" to
  "route exists." The count-forcing invariant is **nonzero-admissible** on `(7,7)`.

This reproduces and unifies SG1's both-signature control under the H37 count-endgame frame: the
`(9,5)` no-go is not a rigged "always-no." Flip the signature and the machinery reports an
admissible odd count. **The method finds a real route when one exists.**

---

## 3. Verdict: NO-GO (high confidence, structural) -- the fermion analog of H27

**NO-GO.** The H29 mutual exclusion is a **genuine exhaustion** at the symmetry-class level: to
force an odd count you must break the chiral grading `G`; the only two ways to break it are
(i) an additive Hessian-changer, **locked out** by the positive Koszul-Tate Hessian
(`V^2 = 0 => V = 0`), and (ii) the antilinear chiralizer, which is **frame-trivial** (tangent-frame
charge `0.0`) -- the forbidden import. No third admissible class survives. Therefore, **within the
built `(9,5)` + positive-Hessian/BV + no-import structure, the generation count is PROVABLY
located-not-forced** -- the fermion analog of H27's "the soldering is a genuine postulate; gravity
does not go unconditional."

The parallel to H27 is exact in form:

| | H27 (gravity, even sector) | H37 (fermion, odd sector) |
|---|---|---|
| Positive control | linear Palatini toy -> FORCED (detects real forcing) | `(7,7)` `J^2=+1` -> admissible odd rank-3 (detects real route) |
| The built object | `S = |II|^2` = SQUARE of a distortion | `D_Sigma^2 = M_KT` = positive KT Hessian |
| Why the forcing/route fails | square (not linear-in-curvature) => trap `theta=0` or YM family | positive square => grading locked; only breaker is frame-trivial import |
| The would-be escape | `theta = 0` is acausal AND circular (imports `pi_ref`) | chiralizer revives `eta` but is frame-trivial/selector-side (imports) |
| Verdict | NOT FORCED: soldering is a genuine postulate (conditional theorem stands) | NO-GO: count located-not-forced within the built structure (conditional on `(9,5)`) |

### Adversarial self-check

- **Is the "no-go" a genuine exhaustion or did I miss an admissible class?** The exhaustion is at
  the symmetry-class level and rests on: `eta = 0 <=>` grading `G` (freshly computed, B1-B3);
  additive grading-breakers locked by positivity (freshly computed, C1); the only symmetry-level
  grading-breaker being frame-trivial (frame charge freshly computed `= 0`, C2). The one gap I do
  **not** close by fresh computation is whether some *non-additive, non-symmetry* GU-native
  operation could break `G` while preserving both `D^2 = M_KT` and frame-nontriviality -- the
  capstone's "chiralizer is the UNIQUE breaker" (item 3) is **campaign-asserted, not
  capstone-computed**, and I inherit that softness. So the honest grade is a **high-confidence
  structural no-go**, not a closed-form theorem over the full operator space.
- **Is any "route" secretly the import?** The only `(9,5)` route (the chiralizer) has tangent-frame
  charge exactly `0.0` -- it **is** the import. The only genuine route (`(7,7)` odd rank-3) is a
  **signature change**, i.e. the separate H19 question, not an admissible move within built `(9,5)`.

---

## 4. What it means for located-not-forced, and for H19

**Located-not-forced is now PROVABLE within the built structure**, in the precise conditional sense:
given the `(9,5)` signature, the positive Koszul-Tate/BV Hessian, and the no-import rule, no boundary
Dirac `D_Sigma` can carry an odd generation count -- both count-invariants are theorem-forced to zero
and every escape is either a positivity-break or a frame-trivial import. This upgrades the capstone's
"located, not forced" from a converged synthesis to a **structural no-go with a positive control**,
matching H27's status for gravity. The count is located by the geometry (the 3-primary
`Lambda^2_+` selector arena), not forced by the fermion index.

**For H19 (the signature question), the no-go sharpens the stakes rather than settling them.** The
wall is **signature-contingent**: the Z-eta/grading lock is signature-independent (holds on `(7,7)`
too), but the Z/2 Kramers lock -- the one that forbids the odd nullity -- **dissolves on `(7,7)`
(`J^2 = +1`)**, where an odd count becomes parity-admissible. So the entire located-not-forced
conclusion for the fermion count reduces to **one antecedent: is GU's `Y^14` the `(9,5)`/H-class or
the `(7,7)`/R-class signature?** On `(9,5)` the count is provably located-not-forced; on `(7,7)` the
parity obstruction lifts (though under-determination -- the rank stays free -- is untouched, per
SG1/C-06). H19 is now the **single load-bearing hinge** for the fermion count leg.

### Honest caveats (what is canon-cited vs freshly computed)

1. **Freshly computed (this test, exact on the 128/1792-dim rep):** the grading `{G, H_bd} = 0` and
   `eta(H_bd) = 0`; Kramers even-multiplicity on `(9,5)`; grading-preserving vs grading-breaking
   perturbations (`eta` `0` vs `-2`); Kramers-alone does not kill `eta` (`eta = 28`); positivity
   locks additive breakers (`V^2 = 0 => V = 0`, residual `4.8e-14`); the chiralizer's tangent-frame
   charge `= 0.0`; the signature-independence of the grading leg on `(7,7)`; the `(7,7)` odd-rank-3
   J-projector (positive control).
2. **Canon-cited / campaign-asserted (NOT re-proved here):** "the positive Koszul-Tate Hessian
   FORCES the grading `G`" is C-01's obstruction theorem (`step1..step5`); "the antilinear chiralizer
   `C = J_quat.G` is the UNIQUE GU-native grading-breaker" is the capstone's item-3 uniqueness,
   flagged there as the **softest link** (campaign-asserted). The H37 no-go is a genuine exhaustion
   *conditional on* these two canon legs; it does not independently close the full operator-space
   uniqueness.
3. **No p-hacking.** No `3` is fit anywhere on `(9,5)+positivity`. `C2 = 155.3625` is unchanged
   (not driven down -- the acausal Velo-Zwanziger trap in `DEAD-ENDS.md` is avoided); `C2/bare =
   sqrt(7)` (irrational). The only odd count that appears is on `(7,7)` (a signature change) or via
   the frame-charge-0 chiralizer (the import) -- both on the wrong side of the located/forced line.
4. **Grade of the numerics.** Exact finite-dim matrix identities and integer eigenvalue-counts on
   the explicit rep, not a symbolic proof over the abstract algebra. For these finite-dim identities
   the explicit rep is decisive; the symmetry-class inferences (eta from spectral symmetry, mod-2
   from Kramers) are standard.

---

*Filed 2026-07-11. Wave 13, the count-endgame swing. Reproducible:
`python tests/wave13/H37_count_nogo.py` (exit 0, 12/12 PASS). Exploration-grade; not promoted to
canon. Adversarially graded: NO-GO earned by symmetry-class exhaustion, positive control fires on
`(7,7)`, no forcing manufactured, no target imported, `C2` not driven down. The fermion analog of
H27: the generation count is provably located-not-forced within the built `(9,5)`+positivity+no-import
structure, conditional on the `(9,5)` signature -- which makes H19 the single hinge.*
