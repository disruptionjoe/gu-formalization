---
title: "PW2F-R2B2B2I separate C4-bank universal-coverage gate"
status: reconstruction
doc_type: exploration
updated_at: "2026-08-04"
---

# PW2F-R2B2B2I separate C4-bank universal-coverage gate

## Question

H4 closed the public-source attribution blocker and admitted two later objects:
the conditional-active `I1 A4` bank and the distinct conditional-active `I2B
C4` bank. It did not assemble either bank. This swing asks whether the accepted
H/H2/H3/H4 evidence is already universal enough to promote those banks, or
whether an exact owner/conormal coverage dependency remains.

The answer is decisive: **neither complete bank is promoted**. The accepted
mixed operator and residual/primalizer formulas survive as parameterized
constructors, but their reviewed executable receipts cover one owner/conormal
pair. A complete bank requires all symmetric owner pairs and the complete
quartic conormal basis, or a proved symmetry reduction that supplies the same
information.

Earned result:

```text
SEPARATE_CONDITIONAL_ACTIVE_C4_BANK_PROMOTION_BLOCKED_ON_UNIVERSAL_OWNER_CONORMAL_COVERAGE__PARAMETRIC_H_H2_FORMULAS_RETAINED
```

## Exact coverage arithmetic

The active metric graph has ten symmetric owners. A symmetric action Hessian
therefore has

```text
10 * 11 / 2 = 55
```

independent owner-pair entries. Four observed-base conormal variables have

```text
binomial(4 + 4 - 1, 4) = 35
```

homogeneous quartic monomials. A direct uncompressed bank consequently has

```text
55 * 35 = 1,925
```

owner-pair/lattice cells per action, or 3,850 cells for the two separately
custodied actions.

This is not a new convention. The accepted normal `kappa1` subbank provides
the bank-grade precedent: it evaluates the full 35-point simplex lattice,
reconstructs `10 x 10` owner matrices, verifies owner symmetry, and checks
three dense held-out conormals. The earlier quartic-basis gate also proves the
35-dimensional Vandermonde rank and demonstrates why a five-point panel is
insufficient.

## Accepted coverage actually present

The byte-pinned H operator receipt hard-codes owner pair `(3,7)` with conormals
`(-1,2,0,1)` and `(1,0,-2,2)`. It proves a valid, live mixed
trace/Phi/Hodge/Shiab response and retains a parameterized `shiab_jet`
constructor. It contains no 35-point lattice loop or all-owner loop.

The byte-pinned H2 residual/primalizer receipt exercises the same `(3,7)` pair
and returns one mixed `I2B` scalar, `-103/256`, with the exact five-family
reassembly. It retains parameterized moving-trace, Shiab, and pairing helpers,
but it likewise contains no 35-point lattice or all-owner reconstruction.

H3 closes a conditional local source orbit. It explicitly rejects promotion
of that orbit to either complete bank. H4 admits separate conditional-active
banks after source attribution exits scope, while stating that neither bank is
source-derived or assembled.

Thus the accepted bank-grade coverage is at most

```text
owner pairs:       1 / 55   (gap 54)
quartic settings:  1 / 35   (gap 34)
```

for the H/H2 dependencies. Parameterization makes a universal successor
possible; it is not evidence that the successor was already executed.

## Serialized resource attempt

The prescribed H2 replay was launched serially with Python bytecode disabled
and the pinned SymPy `1.14.0` environment. The source and Layer-0 prechecks
passed. After more than five minutes, the first exact `shiab_jet` kernel was
still simplifying the mixed outer wedge; the investigation was interrupted rather than
launching an unbounded 1,925-cell grid. No owner bytes other than this declared
packet were produced by that attempt, and no cache files were admitted.

This timing is a resource signal, not a mathematical obstruction. A future
swing can still close coverage by proving an exact owner-pair symmetry/orbit
reduction, introducing a validated sparse exact evaluator, or executing a
bounded serialized full grid with resumable evidence. The present result does
not estimate a theorem from wall-clock time.

## Layer 0 and source boundary

The following objects remain distinct:

- public-source `I1` and its `(7,7)` presentation;
- the conditional-active repository `I1 A4` coefficient bank;
- the separately custodied manuscript `I2B` residual-square glyph;
- the conditional-active repository `I2B C4` coefficient bank;
- raw action-density Hessians, Euler coefficients, Green concomitants, and
  Helmholtz quotients; and
- Curt's rival action/carrier track inside the Eric lane.

H4's source-attribution exit does not make either active bank source-derived.
Shared owners and conormal variables do not merge the two actions. Insufficient
coverage does not prove either bank zero, inconsistent, or nonexistent.

## Hostile boundary

Six planted claims are rejected:

1. a parameterized constructor proves all 55 owner pairs were evaluated;
2. one symmetric mixed pair determines the full ten-owner quartic bank;
3. H4's source-scope exit makes the active banks source-derived;
4. common owner/conormal coordinates merge `I1 A4` and `I2B C4`;
5. Green/Helmholtz may start before both complete banks close; and
6. a coverage audit merges Curt or promotes a third lane.

No multi-index Green/Helmholtz reduction, live-C3 return, projective `kappa1`
classification, domain, quotient, observation, characteristic, or physics
result is licensed here.

## Next gate

Resume at

```text
PW2F-R2B2B2I1-UNIVERSAL-OWNER-CONORMAL-COVERAGE-ENGINE-THEN-SEPARATE-C4-BANKS
```

First prove a bank-preserving symmetry reduction or build a validated exact
sparse/resumable evaluator that covers all 55 owner pairs and 35 quartic
monomials with held-outs. Then assemble `I1 A4` and `I2B C4` separately. Only
after both close may the campaign enter multi-index Green/Helmholtz, the live
C3 return, or projective classification.

P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`. `TG-1 AND TG-2 AND TG-3` remains
`NOT_PROMOTED`.
