---
artifact_type: exploration
created: 2026-08-04
title: "K77 Wave 2: one action-first current/pseudo-Riesz/even-Ward architecture, with the odd super-IG action boundary isolated"
grade: "Formula-built local conditional architecture plus exact rational matrix and real symplectic certificates. The source first-order action, total residual, and redundancy passages are rechecked; the action's actual Euler derivative replaces the unearned advertised full-domain endpoint; JD+JF is emitted once by the fermion action; the indefinite connection pseudo-musical and complete even local-IG Ward contraction are explicit. A real pointwise mixed rolled super-IG bracket is constructed, but source-group/Krein compatibility, the odd action, odd Ward/BV closure, global descent, domain, observation, vacuum, and physics remain open."
named_gate: RENDEZVOUS-ACTION-CURRENT-RIESZ-SUPERIG-WARD
gate_before: ZERO_ORDER_LINEAR_FULL_DOMAIN_EINSTEIN_CYCLIC_INTERSECTION_ZERO__FOUR_ACTION_REBUILDS_OPEN
gate_after: FROZEN_LOCAL_ACTION_FIRST_NO_BRIDGE_EULER_CURRENT_PSEUDORIESZ_EVEN_WARD__REAL_POINTWISE_MIXED_SUPERIG_BRACKET__ODD_ACTION_AND_WARD_OPEN
route_disposition: PARTIAL_GATE_MOVEMENT__KEEP_WAVE2_FRONTIER_ON_TG2_TG3
source_collision: SOURCE-CONFIRMS-FIRST_ORDER_ACTION_TOTAL_RESIDUAL_AND_NO_SEPARATE_BRIDGE__SOURCE-CORRECTS-XI_AS_REDUNDANCY_NOT_NOETHER__SOURCE-SILENT-ODD-ACTION-WARD-AND-DOMAIN
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
third_lane_promoted: false
---

# K77 Wave 2: action/current/Riesz/Ward rendezvous

## Result first

Wave 2 now has one frozen **local action architecture**, but it has not earned
the full odd super-IG/BV claim.

The selected primary architecture is

```text
ACTION FIRST
+ USE THE ACTION'S ACTUAL SYMMETRIZED EULER DERIVATIVE
+ EMIT JD+JF ONCE FROM THE VARIED FERMION ACTION
+ DO NOT INSERT A SECOND MATTER-CURRENT BRIDGE
+ USE THE KREIN/HODGE/ADJOINT PAIRING ONLY AS AN INDEFINITE PSEUDO-MUSICAL
+ INCLUDE THE MOVING EPSILON/SHIAB RESPONSE IN E_EPSILON AND THE EVEN IG WARD IDENTITY
```

This is the most source-faithful surviving architecture after K77-B3. The
2021 draft puts `Upsilon^B+Upsilon^F` in one Euler-residual arena and does not
display a second term `-<T,J_matter>` beside the fermion action. The
architecture therefore uses

\[
 \widehat J_{20}
 =\widehat J_D+\widehat J_F,
 \qquad
 \widehat J_F=D_A^{\rm coad}\widehat Q_F,
\]

exactly once in the connection/translation Euler equation. The native
connection pseudo-musical from RB1 converts this density-dual covector into a
primal one-form when needed; it is not inserted as another interaction.

The exact finite certificate passes

```text
25 exact + 6 source + 14 type + 7 planted = 52 PASS.
```

It establishes all of the following simultaneously:

- the source coefficients `1/2,1/3` are uniquely selected in a cyclic
  transgression control;
- on a noncyclic Shiab comparator, the action's actual translation derivative
  is `-211/21`, while the advertised endpoint pairing is `-68/7`, leaving the
  exact defect `-1/3`;
- the moving-Shiab response is separately live (`-58/3`) and required by the
  even Ward identity, but is zero in a fixed-epsilon translation variation;
- a regular parent-field elimination reproduces the actual symmetrized Euler
  derivative, not the advertised endpoint;
- the no-bridge action emits `J_D+J_F` once; the `J_D` bridge cancels `J_D`,
  and the total bridge cancels the direct current but leaves its Hessian;
- the indefinite flat/sharp maps invert and satisfy their defining pairing;
  a negative-norm control rules out a hidden positive Riesz reading; and
- the full finite even local-IG Ward contraction is zero only when both the
  moving compensator response and the inhomogeneous connection direction are
  retained.

The same Ward test does **not** select the bridge policy: both bridge terms are
separately gauge invariant. The no-bridge architecture is selected as the
source-faithful, nonduplicating primary construction, not proved to be the
unique gauge-invariant action.

The super-IG result is narrower but real. Using the K77 real spinor's
alternating form `Omega`, the standard symplectic moment map gives a mixed
rolled bracket

\[
 \beta((u,\psi),(v,\chi))(X)
 =\mu_\Omega(u,\chi(X))+\mu_\Omega(v,\psi(X))
 \in V^*\otimes\mathfrak{sp}(S,\Omega).
\]

It is symmetric, nonzero, real, and equivariant; because the translation
ideal acts trivially on the odd module, the odd-odd-odd Jacobi identity closes
in the two-step model. This constructs `TG-1` only at real pointwise
`Sp(S,Omega)` grade. It does not identify the source group with
`Sp(S,Omega)`, prove simultaneous preservation of the action's Krein pairing,
turn the rolled fermion fields into odd gauge parameters, or supply their
action on the full field space. Therefore:

```text
TG-1 = PARTIAL: REAL POINTWISE MIXED ROLLED BRACKET CONSTRUCTED
TG-2 = OPEN: FULL-FIELD ODD ACTION
TG-3 = OPEN: ODD WARD/BV CLOSURE
TG-1 AND TG-2 AND TG-3 = NOT PROMOTED
```

Wave 2 moves from `NOT_STARTED` to a frozen local even-Ward architecture, but
the campaign frontier remains on Wave 2 until `TG-2/TG-3` are constructed or
the campaign formally proves that the odd extension is not an action
requirement. It does not advance to the observation/datum wave by relabeling
an even Ward identity as super-IG.

## Plain English

Eric's source material answers more than “there should be an action,” but less
than a complete theory.

It says the bosonic and fermionic equations belong in one combined residual.
That is a strong reason not to write the fermion action and then separately
couple the distortion to the current derived from that same action. The latter
either cancels part of its own source or leaves extra Hessian terms. We now
have all three possibilities written and differentiated; the no-bridge
version is the clean Eric-guided primary route.

The source's printed bosonic equation still cannot simply be copied. K77-B3
proved that the advertised full-domain endpoint is not the derivative of the
written cubic term for a nonzero ambient-Einstein Shiab. The honest repair is
to keep the written action and use the equation it actually produces. Later
physics tests decide whether that symmetrized equation still yields the
needed Einstein shadow. A moving epsilon matters to its own equation and to
gauge covariance, but it does not change what varying `T` at fixed epsilon
means.

Finally, we can now square the rolled spinor pair into a real symplectic
connection translation. What we still cannot do is make that odd generator a
symmetry of the complete action. The missing object is no longer vaguely
“super-IG”: it is the full-field odd transformation and its Ward/BV identity,
compatible with the same K77 real/Krein action.

## 0. Layer 0

| phrase | object used here | object kept distinct |
|---|---|---|
| source action | draft first-order `I_1^B` plus a repository fermion/defect/gravity reconstruction | the earlier N1 sum or a complete published GU action |
| Euler residual | actual Fréchet derivative of the written action | the advertised translated-curvature expression unless Helmholtz holds |
| current | density-dual `delta_A S_20=J_D+J_F` | Clifford-vector observable or a primal connection form |
| Riesz | real indefinite Hodge/invariant-pairing pseudo-musical | positive Hilbert-space Riesz theorem or analytic domain |
| Noether identity | off-shell contraction of all Euler covectors with the even local-IG gauge generator | `Xi=D_omega Upsilon`, an on-residual redundancy/deformation equation |
| super-IG bracket | real pointwise two-step bracket on `S plus V* tensor S` | odd transformation of the full action or a VZ guardian |
| fermion | rolled field in `Omega0(S)+Omega1(S)` | odd gauge parameter; the identification is open |
| physical recovery | held out | carrier support or source resemblance |

The action variables are taken to be `(epsilon,T,Z,s,ghosts,...)`, with
`B=B(epsilon)` and `A_epsilon=B(epsilon)+T`. The finite certificate splits
`B` and the compensator only to audit their two derivative paths. It does not
promote `B` to an extra independent physical Euler root.

## 1. Primary-source collision

The underlying source rows were reopened before selecting the architecture.

| source | classification | Wave-2 consequence |
|---|---|---|
| draft 9.1, equations 9.1--9.7 | `SOURCE-CONFIRMS` | use the first-order transgression action and its `1/2,1/3` completion |
| draft 9.18--9.20 | `SOURCE-CONFIRMS` | compare one total residual `Upsilon^B+Upsilon^F`; do not assume a second current bridge |
| Portal/Oxford 01:43:32--01:45:25 and 02:35:10 | `SOURCE-CONFIRMS` | curvature alone is not exact; retain the eddy completion |
| draft 9.5--9.6, 9.21--10.9 | `SOURCE-CORRECTS` | `Xi=D Upsilon` is a redundancy/deformation target, not the required Noether identity |
| Portal/Oxford 02:41:16 and draft 9.16 | `SOURCE-SILENT/OPEN` | complete Dirac and zero-order placement are reconstruction work |
| checked source corpus | `SOURCE-SILENT` | no native K77 current musical, admissible domain, full odd action, or odd Ward/BV identity is supplied |

Thus the source guides the architecture and prevents a duplicate current. It
does not close the missing mathematics by authority.

## 2. Inline specialist pre-assessment

| lens | preregistered efficient route and kill condition |
|---|---|
| differential geometry | treat `B(epsilon)` and the moving Shiab as dependent geometry; kill any chain that counts them as independent physical fields |
| representation/invariant theory | reuse K77-B3's full-domain theorem; do not resume a superseded zero-order expression census |
| variational bicomplex | compute the actual Euler derivative first; kill any architecture that substitutes the advertised endpoint without Helmholtz |
| hyperbolic/operator theory | carry `Q_F` preboundary ownership and the indefinite pseudo-musical; make no domain or positivity inference |
| symplectic/BV geometry | derive Ward from the same action; reject `Xi=D Upsilon` as a replacement and keep odd BV closure distinct |
| fermion phenomenology | emit `J_D+J_F` once and retain zero-order placement as open; do not infer particles from the current carrier |
| gravity/cosmology | preserve ambient versus observed versus trace-reversed Frobenius-fibre contractions for Wave 4 |
| topology/anomaly | keep P1/P2/P3 outside the action until a receiving arrow exists |
| exact algebra/ML engineering | use exact active fixtures; no surrogate model is warranted for this small candidate family |
| proof/distributed systems | monotone-propagate B3 and prior current results; reject both summary overreach and defense of a superseded bridge object |

Pre-registered primary failures were: wrong current carrier, current inserted
twice, missing Hessian, missing moving-epsilon response, missing inhomogeneous
connection term, positive-Riesz substitution, bridge selection inferred from
Ward alone, or mixed bracket promoted to an odd action.

## 3. The frozen local action architecture

Write the K77 reconstruction schematically as

\[
\begin{aligned}
S_{W2}[\epsilon,T,Z,s,\Phi_{\rm BV}]={}&
I^B_{\rm act}[B(\epsilon),T,\mathscr S_\epsilon]
+S_{20}[A_\epsilon,Z]\\
&+I_{\rm defects}[s^*A_\epsilon,s^*Z,P_0,\rho,Y_K,Y_C]
+I_{|II|^2}[s]
+S_{\rm BV,even},\\
A_\epsilon={}&B(\epsilon)+T.
\end{aligned}
\]

This freezes ownership, not completion of every later physical coefficient.
The orientation term is not inserted before Wave 3 constructs a receiving
arrow. P3 remains external. Rival `K/C` zero-order branches remain separately
declared rather than inserted simultaneously.

For the bosonic source piece, define `Q_T` by

\[
 \langle T,\widehat{\mathscr S}_\epsilon X\rangle
 =\langle X,Q_T\rangle.
\]

At the source coefficients, the actual translation Euler covector has the
structural form

\[
\begin{aligned}
E_T^{B,\rm act}={}&
\widehat{\mathscr S}_\epsilon
\left(F_B+\tfrac12D_BT+\tfrac13q(T)\right)
+\tfrac12D_B^{\rm coad}Q_T\\
&+\tfrac13(d_{\mathcal F,T}q)^!Q_T
+\kappa_1\flat T.
\end{aligned}
\]

It equals the advertised

\[
\widehat{\mathscr S}_\epsilon(F_B+D_BT+q(T))+\kappa_1\flat T
\]

only on a domain where the two transgression-adjoint identities hold. K77-B3
rules that equality out on the full written translation tangent for a nonzero
ambient-Einstein restriction. Wave 2 therefore freezes `E_T^{B,act}`.

The total translation equation begins

\[
E_T=E_T^{B,\rm act}+\widehat J_D+\widehat J_F
+E_T^{\rm defects}+E_T^{\rm BV},
\]

with no separately inserted `-<T,J>` term. The epsilon equation owns both
the `B(epsilon)` chain and the moving Shiab:

\[
E_\epsilon=(D_\epsilon B)^!E_B
+(D_\epsilon\widehat{\mathscr S}_\epsilon)^!
  (T,\mathcal C)
+E_\epsilon^{20,\rm moving}
+E_\epsilon^{\rm defects/BV}.
\]

The finite split-field model verifies those paths independently and rejects
omitting either during the Ward test.

## 4. Four bosonic rebuilds after K77-B3

| rebuild | Wave-2 result |
|---|---|
| actual symmetrized Euler | **selected primary**; action-derived by construction and nonzero, with its physical Einstein shadow held to Wave 4 |
| moving Shiab/epsilon/soldering/Hodge | **required in `E_epsilon` and Ward**, but cannot repair a fixed-epsilon `T` derivative |
| independent parent curvature/soldering | **regular parent does not repair**; eliminating `R,P` returns the same symmetrized Euler. A distinct parent needs new nonregular geometry or constraints |
| action/BV-derived restricted domain | **live reopener**; a planted one-dimensional cyclic subdomain passes, but the source supplies no lawful physical restriction |

This comparison prevents the phrase “moving geometry may fix it” from hiding
which Euler equation the movement actually enters.

## 5. Current and pseudo-Riesz

RB1 already constructs the actual density-dual split and Green owner:

\[
\delta_A S_{20}[a]
=\int_Y a\wedge\widehat J_D
+\int_YD_Aa\wedge\widehat Q_F,
\qquad
\widehat J_F=D_A^{\rm coad}\widehat Q_F.
\]

Wave 2 adopts that split without replacing its differential `J_F` by the
finite model's algebraic curvature comparator. The latter is used only to
audit single versus duplicate current ownership and Hessian response.

The pointwise carrier map is

\[
\sharp_{\rm conn}widehat\eta
=*_G^{-1}\kappa_{\mathfrak g}^{\sharp}\widehat\eta,
\qquad
\flat_{\rm conn}j=\kappa_{\mathfrak g}^{\flat}(*_Gj).
\]

It is real, invertible, and indefinite. It does not turn the current into a
positive norm or construct a closed operator domain.

The bridge comparison remains useful as a negative-test bank:

| presentation | exact consequence |
|---|---|
| no separate bridge | `J_D+J_F` occurs once in `E_T` |
| `J_D` bridge | direct `J_D` cancels; `J_F` remains |
| total-current bridge | direct `J_D+J_F` cancels; `A/Z` Hessians and `Q_F` boundary data remain |

All three can be gauge invariant. Source fidelity and no duplication select
the primary; Ward covariance alone does not.

## 6. Even Ward and BV boundary

For the complete even local-IG transformation generator, the off-shell
identity has the form

\[
R_\epsilon^!E_\epsilon
+{\rm ad}_T^!E_T
+\rho_Z^!E_Z
+\rho_s^!E_s
+R_{\rm ghost}^!E_{\rm ghost}
+R_{\rm bg}^!E_{\rm bg}
=d\Theta_{\rm Ward}.
\]

The finite exact core gives zero. Omitting the compensator/Shiab response or
the inhomogeneous connection direction gives a nonzero planted residual. The
full field-theoretic preboundary includes RB1's `Q_F` flux and the other
already named Green owners; no common analytic boundary condition is selected
here.

This identity supports the standard minimal **even** BV skeleton, conditional
on the full transformation/owner ledger. The current artifact does not claim
a complete master equation with moving support, defects, backgrounds, and
boundary fields. In particular,

\[
\Xi_\omega=D_\omega\Upsilon_\omega
\]

is not substituted for the off-shell contraction above.

## 7. Mixed rolled super-IG boundary

Let `S` be the real K77 spinor carrier with invariant alternating form
`Omega`. For `u,v in S`, define

\[
\mu_\Omega(u,v)=u v^T\Omega+v u^T\Omega
\in\mathfrak{sp}(S,\Omega).
\]

For

\[
Q_{\rm mix}=S\oplus(V^*\otimes S),
\]

the bracket printed at the top of this report is symmetric and equivariant.
With the connection-translation ideal acting trivially on `Q_mix`, it defines
a two-step Lie superalgebra extension of the even inhomogeneous gauge algebra.
The exact probe checks nonzero squaring, symplectic landing, infinitesimal
equivariance, symmetry, and Jacobi on independent rational fixtures; the
identities themselves are dimension-generic symplectic linear algebra.

Three missing identifications remain load-bearing:

1. the source/K77 gauge group must admit this `Sp(S,Omega)` connection target
   or a proved projection to its actual adjoint;
2. the bracket must be compatible with the simultaneous Krein pairing used
   by the action, not merely the alternating form; and
3. `Q_mix` must be specified as an odd **parameter** module acting on all
   fields, not inferred from the similarly shaped fermion field carrier.

Until those maps and the odd action are written, no odd Ward identity or BV
master equation exists to test. This is `TG-2/TG-3`, not a reopened search for
the source action or external datum.

## 8. Hostile post-review disposition

The post-review returns `PASS_WITH_MATERIAL_SCOPE_REPAIRS__GATE_PARTIAL`.
Its main repairs were:

- downgrade no-bridge from mathematically unique to source-faithful primary;
- keep `B(epsilon)` dependent even though the finite audit splits its paths;
- keep RB1's differential `J_F/Q_F` as the physical current owner rather than
  silently replacing it with the algebraic finite comparator;
- call the connection map a pseudo-musical, not a positive Riesz theorem;
- report the bracket at `Sp(S,Omega)` pointwise grade, not as source-group or
  simultaneous-Krein closure; and
- leave the named gate partial because an even Ward identity is not an odd
  super-IG action.

The artifact is not defending the superseded printed endpoint or N1 bridge
family merely because those objects were previously hardened. It uses them as
negative tests and advances the action-first architecture.

## 9. Status and next build

Earned status:

```text
WAVE2 = PARTIAL_FROZEN_LOCAL_ACTION_EVEN_WARD
CURRENT = JD+JF ONCE; NO SEPARATE BRIDGE PRIMARY
Riesz = INDEFINITE POINTWISE PSEUDO-MUSICAL
TG1 = REAL POINTWISE MIXED BRACKET, SOURCE-GROUP/KREIN CONDITIONAL
TG2/TG3 = OPEN
```

The campaign stays on the same named Wave-2 gate. Its next bundled build is:

1. choose or derive the actual K77/source gauge algebra preserved by both
   `Omega` and the Krein action pairing;
2. define how an odd parameter `(u,psi)` acts on
   `(epsilon,T,Z,s,ghosts/backgrounds)`;
3. check closure against the mixed bracket and gamma/projector constraints;
4. vary the frozen action under that odd transformation;
5. construct the odd BV generator and exact master/Ward contraction, including
   preboundary terms; and
6. either complete `TG-2/TG-3` or prove the odd extension is not an action
   symmetry and formally rebase the campaign before Wave 3.

Observation, vacuum, global/analytic domain, particles, local physics
equations, masses, chirality, anomaly, index, generation count, cosmology, and
P1/P2/P3 use remain held out.

## Reproduction

```bash
uv run --with sympy==1.14.0 python \
  tests/channel-swings/k77_wave2_action_current_riesz_superig_ward_probe.py
python3 process_gates/k77_wave2_action_ward_scope_audit.py
```
