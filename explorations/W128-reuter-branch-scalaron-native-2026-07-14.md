---
artifact_type: exploration
status: exploration (5-persona inline team; deterministic test; H65 Condorcet-winner swing)
created: 2026-07-14
hypothesis: H65 -- the AS/Reuter-branch scalaron, computed at the repo's native truncation grade
branch: "W128 -- the tachyon's last perturbative escape and the referee's ported-loop-blocks vulnerability, one computation: at the Reuter-family fixed point W119 catalogued (g* = 2/A_tot, GU content g* = 0.674), what is the R^2-sector sign, the de-slaved f_0^2 root's sign and m_0^2, the scalaron's fate on IR trajectories from the FP, and does anything in GU's structure select AF vs AS?"
title: "VERDICT: AS-RESCUES-CONDITIONAL (PERMITTED-NOT-FORCED); E2 FORK-STANDS. (1) SIGN-LOCK THEOREM (derived, sympy): the de-slaved Reuter-family f_0^2 root is f_0^2* = eta0 g*/(kappa Phi c_C), so sign(de-slaved root) = sign(eta0) = sign(the RELEVANCE of R^2 at the interacting FP) for EVERY admissible regulator dressing -- the whole Reuter-branch scalaron question reduces to ONE sign, and that sign is PORTED (Lauscher-Reuter, Codello-Percacci-Rahmede, Benedetti-Machado-Saueressig, AS-Starobinsky: RELEVANT => root POSITIVE => m_0^2 >= 0 at BOTH Reuter-family FPs; main family f_0^2* = 0, scalaron massless at the scale-invariant point). (2) IR TRAJECTORIES: the scalaron's fate is a FREE relevant boundary condition -- healthy (f_0^2 -> +0.128) AND tachyonic (f_0^2 -> -0.128) trajectories BOTH emanate UV-complete from the Reuter FP (both orientations of the same relevant eigendirection; forward RK4 confirms the UV basin contains both signs with no tuning). The AS branch PERMITS the rescue that AF FORBIDS (W123's monotonicity theorem reproduced as the contrast); it does not FORCE it -- rescue additionally requires the IR-consistency boundary condition (W83's D2). (3) BRANCH SELECTION: FORK-STANDS -- no GU-native selector found: the Krein keep-and-grade is COMPATIBLE with the interacting FP (f_2^2 obeys the same exact positive law on AS trajectories, grading clear at all finite scales on BOTH ghost-mass fork branches, fork again confined to the endpoint), the induced-action IR data (gamma > 0, Lambda > 0) and the mu_DW fixed-scale construction are branch-blind, and BOTH branches host UV-complete trajectories. W83's satisfiability filter remains the only selector and it is a consistency principle, conditional, not GU structure. E2 CARRIED. (4) BOTTOM LINE: the tachyon does NOT follow GU onto the AS branch as a forced property -- the forcing mechanism (fixed-ratio slaving) provably disappears at g* != 0 -- but the rescue is conditional on (i) the Reuter FP being genuine and (ii) the ported R^2-relevance sign; the negative control shows (ii) flipped gives TACHYON-FOLLOWS, so the verdict is not hardcoded and the hinge is exactly one uncomputed native number."
grade: "DERIVED-on-PORTED, exploration-grade, truncation-dependence explicit. DERIVED here (exact sympy / deterministic numerics): the sign-lock theorem (de-slaved root = eta0 g*/(kappa Phi c_C), sign locked to eta0 for every positive dressing); the relevance <=> root-sign <=> tachyon-status equivalence (eigenvalues -eta0 g* at the main FP, +eta0 g* at the de-slaved root, exact); the eta_N* = -2 identity at any finite-g* FP (dimensional, magnitude-independent); the both-signs IR-trajectory and UV-basin computations; the f_2^2 positivity law on AS trajectories (same exact solution as W119's, since beta_{f_2^2} is g-independent in this truncation) and the grading-locus conclusions on both ghost-mass fork branches; the AF contrast (healthy IR data Landau-poles at g = 0, W123 reproduced); the basin boundary (large f_0^2 diverges on AS too). PORTED (cited, not re-derived, THE HINGE): the SIGN of the de-slaving coefficient eta0 > 0, equivalently the relevance of the R^2 direction at the interacting FP -- Lauscher-Reuter 2002, Codello-Percacci-Rahmede 2009 (f(R) polynomial: 3 relevant), Benedetti-Machado-Saueressig 2009 (Weyl^2+R^2: 3 relevant + 1 irrelevant, R^2 relevant), AS-Starobinsky 1311.0881 (R^2 relevant, IR value free, healthy branch realized); its magnitude is SCHEMATIC (0.3 in W119, 0.9 in W83, both swept). SCHEMATIC (mirrored from W83/W119, reproduced as positive controls): the EH-sector magnitudes (A_tot budget, B0, B1; pure-gravity g* = 0.700, g* lambda* = 0.109; GU g* = 0.674, lambda* = 0.151). PORTED (W45, inherited): the one-loop marginal coefficients 5/3, 5, 5/6, b_2 = 133/10 + 17/12. Convention: f_0^2 is the agravity coupling here; W123's audit certifies the reciprocal wobble against the direct-R^2-coefficient convention is sign-harmless. Deterministic test tests/W128_h65_reuter_branch_scalaron.py, 20/20, exit 0, with 4 positive controls + 1 negative control. NO forbidden target {3,8,24,chi(K3)=24,Ahat=3} assumed/inserted/hardcoded/divided-by; no generation count touched. NO canon / RESEARCH-STATUS / claim-status / verdict / posture changed. H59/H61a remain OPEN; the E2 fork is CARRIED, not closed."
depends_on:
  - explorations/h59-frg-minimal-truncation-krein-negative-ratio-2026-07-13.md
  - explorations/W122-spin0-gauge-vs-physical-auxfield-2026-07-13.md
  - explorations/W123-native-r2-running-sign-convention-audit-2026-07-13.md
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/frg-fr-weyl-af-as-fork-2026-07-11.md
  - explorations/true-fixed-point-f0-sign-2026-07-11.md
  - explorations/E2-asymptotic-safety-2026-07-11.md
  - explorations/landscape-assessment-post-three-waves-2026-07-14.md
  - tests/W119_h59_frg_krein_negative_ratio.py
  - tests/W83_frg_fr_weyl_af_as.py
  - tests/W123_native_r2_sign_convention_audit.py
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W128_h65_reuter_branch_scalaron.py
external_refs:
  - "Reuter, Nonperturbative evolution equation for quantum gravity, PRD 57 (1998) 971, hep-th/9605030 -- the Reuter fixed point"
  - "Lauscher & Reuter, Flow equation of quantum Einstein gravity in a higher-derivative truncation, PRD 66 (2002) 025026, hep-th/0205062 -- first R^2 truncation at the Reuter FP: R^2 direction found (near-marginally) RELEVANT, FP value small"
  - "Codello, Percacci & Rahmede, Ann. Phys. 324 (2009) 414, arXiv:0805.2909 -- polynomial f(R): 3 relevant directions including R^2"
  - "Benedetti, Machado & Saueressig, MPLA 24 (2009) 2233, arXiv:0901.2984 -- Weyl^2 + R^2 Reuter FP: 3 relevant + 1 irrelevant, R^2 relevant, Weyl coupling stays irrelevant"
  - "Falls, Litim, Nikolakopoulos & Rahmede, arXiv:1301.4191 -- bootstrap: near-canonical scaling at the Reuter FP in high-order f(R)"
  - "Copeland, Rahmede & Saltas, Asymptotically Safe Starobinsky Inflation, arXiv:1311.0881 -- R^2 relevant, IR value a free boundary condition, healthy (non-tachyonic) branch observationally realized"
  - "Dona, Eichhorn & Percacci, PRD 89 (2014) 084035, arXiv:1311.2898 -- matter bounds; spin-3/2 anti-screens (the Reuter-FP preservation input, via W83)"
  - "Salvio & Strumia, Agravity, JHEP 06 (2014) 080, arXiv:1403.4226 -- the AF-branch forced-negative f_0^2 and M_0^2 = f_0^2 Mbar_Pl^2/2 (the contrast object)"
---

# W128 -- The AS/Reuter-branch scalaron: sign-lock, IR fate, and the fork

**Role.** The 2026-07-14 landscape assessment made H65 the Condorcet winner: the AF-branch tachyon
(W122 positive-norm, W123 native-robust, W126 tree-unliftable) has exactly one perturbative escape
left, the asymptotic-safety branch through the Reuter fixed point that W119 catalogued and W83
conditionally selected -- and the flagship's referee ledger states the vulnerability plainly: on AS,
*none of this is yet computed*. W128 computes what the repo's truncation machinery can honestly say
about the Reuter-branch scalaron, separates it cleanly from what remains ported, and locates the
single number the whole branch now hinges on.

Five personas inline, one context, no sub-agents. Deterministic test:
`tests/W128_h65_reuter_branch_scalaron.py` (20/20, exit 0; 4 positive controls, 1 negative control).

## 0. Construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md discipline), named

| Fork | Standard-physics side | GU program-native side | Handling here |
|---|---|---|---|
| **UV completion** | asymptotic freedom (Gaussian FP) | asymptotic safety (Reuter FP) | THE object under study. Not defaulted; both branches computed; the fork is CARRIED (Section 5). |
| **Ghost mass** | agravity `m2^2 ~ f_2^2 M_Pl^2` | fixed-scale `m2 = sqrt(m2_eff) mu_DW`, `m2_eff in [5/6, 5/4]` | BOTH branches computed on the AS trajectory (Section 4). Fork decides only the FP-endpoint behavior, exactly as W119 found on AF. |
| **Which f_0^2** | agravity coupling (R^2 coefficient `1/(6 f_0^2)`) -- this file and the W45/W83/W119 systems | direct R^2 coefficient (W79/W122) | W123's audit certifies the reciprocal wobble is sign-harmless: `sign(m_0^2) = sign(f_0^2)` in both conventions. Only signs are asserted here. |
| **Positivity object** | positive Hilbert space | Krein keep-and-grade `[P,S] = 0` | Grading-locus distance tracked along the AS trajectory (Section 4); loop positivity itself out of scope (W48 gate, unchanged). |

## 1. Persona 1 -- asymptotic-safety specialist: what the AS literature already knows (PORTED, labeled)

The scalaron at the Reuter fixed point is charted territory in standard AS gravity, and porting it
honestly is the first duty:

- **Lauscher-Reuter (2002)**, the first `R^2`-extended truncation of QEG: the FP survives the `R^2`
  enlargement, the FP value of the `R^2` coupling is small, and the `R^2` direction comes out
  (near-marginally) **relevant**. PORTED.
- **Codello-Percacci-Rahmede (2009)**, polynomial `f(R)` to high order: the FP persists with
  **3 relevant directions**, the `R^2` direction among them; higher powers irrelevant. PORTED.
- **Benedetti-Machado-Saueressig (2009)**, `Weyl^2 + R^2`: 3 relevant + 1 irrelevant, `R^2`
  **relevant**, the Weyl coupling stays (marginally) irrelevant -- the truncation family closest to
  GU's induced 4th-order action. PORTED. (Falls-Litim et al.'s bootstrap corroborates near-canonical
  scaling in high-order `f(R)`.)
- **AS-Starobinsky (Copeland-Rahmede-Saltas 2013)**: because the `R^2` direction is relevant, its IR
  value is a **free boundary condition** on the Reuter FP's critical surface, and the positive
  (healthy, non-tachyonic) branch is the one observationally realized in Starobinsky-type inflation.
  PORTED.

The uniform message of four independent truncation lineages: at the interacting FP the `R^2`
direction is **relevant**, its FP value is small (zero in the repo's minimal truncation), and its IR
value -- hence the scalaron's mass sign -- is a boundary condition, not a prediction. In standard AS
gravity the scalaron is **not** forced tachyonic; it is the AF branch that forces the wrong sign
(Salvio-Strumia's own statement, W123's audit). What none of this supplies is a GU-native derivation
of the relevance sign with GU's own content and conventions -- that is exactly where the port line
sits, and Section 2 reduces the entire branch verdict to that one sign.

## 2. Persona 2 -- FRG engineer: the native increment (the sign-lock theorem and the numbers)

Setup: the W83/W119 4-coupling system `(g, lambda, f_2^2, f_0^2)` with the W45 marginal quadratic
(PORTED coefficients `5/3, 5, 5/6`; `b_2 = 133/10 + 17/12`), the schematic EH sector reproduced as
positive controls (pure gravity `g* = 0.700`, `g* lambda* = 0.109`; GU content `g* = 0.674`,
`lambda* = 0.151`), the W119 threshold-dressing band `[0.50, 2.47]` **rebuilt from scratch by
quadrature** (Litim `1/n!`; exponential `Phi^2_2(0) = 1`, `Phi^1_1(0) = pi^2/6` -- PC4), and the
de-slaving linear term `-eta0 g f_0^2` whose sign is the ported relevance and whose magnitude is
schematic (0.3 in W119, 0.9 in W83; both swept).

**The sign-lock theorem (derived, sympy).** At the Reuter family (`g = g*`, `f_2^2 = 0`) the
`f_0^2` beta collapses to `beta_y = kappa Phi c_C y^2 - eta0 g* y`, whose nonzero root is

    f_0^2* (de-slaved) = eta0 g* / (kappa Phi c_C).

Every factor except `eta0` is strictly positive for every admissible regulator (threshold positivity
is W119's sympy identity), so **sign(de-slaved root) = sign(eta0), with no regulator freedom to
break the lock**. And the two eigenvalues are exact: `-eta0 g*` at the main root `y = 0`,
`+eta0 g*` at the de-slaved root. Hence the chain of equivalences:

    R^2 RELEVANT at the Reuter FP  <=>  eta0 > 0  <=>  de-slaved root POSITIVE  <=>  non-tachyonic
    R^2 IRRELEVANT                 <=>  eta0 < 0  <=>  de-slaved root NEGATIVE  <=>  tachyon follows

**The entire Reuter-branch scalaron question is one sign.** With the ported sign (four lineages,
Section 1): the main Reuter family has `f_0^2* = 0` (the scalaron is massless at the scale-invariant
point -- mass is a trajectory statement, not an FP statement) and the de-slaved root is **positive**
(`f_0^2* in [+15, +230]` across the schematic-magnitude and dressing bands; magnitudes schematic and
explicitly not load-bearing), giving `m_0^2 = f_0^2* Mbar^2/2 > 0`. **Neither Reuter-family fixed
point carries a tachyonic R^2 sector, given the ported relevance sign.**

**IR trajectories (question 2).** Flowing toward the IR from the main Reuter FP along the relevant
`f_0^2` eigendirection: the positive orientation reaches `f_0^2 = +0.128` (healthy, `m_0^2 > 0`,
reproducing W83's C2) -- and the **opposite orientation of the same eigendirection reaches
`f_0^2 = -0.128`, equally UV-complete**. The forward check confirms it without eigendirection
tuning: generic IR data with `f_0^2 = -0.4` AND with `f_0^2 = +0.3` both flow into the Reuter FP
toward the UV under RK4 on the full system. The contrast and the boundary are both real: the same
healthy IR data on the AF route (`g = 0`) Landau-poles (W123's monotonicity theorem reproduced), and
even on AS the basin is bounded (`f_0^2 = +300`, above the de-slaved root, diverges).

**So the AS branch PERMITS what AF FORBIDS, and forces nothing.** The scalaron's fate on the Reuter
branch is a free relevant boundary condition. The tachyon is not excluded by the branch; it is
de-FORCED -- the fixed-ratio slaving that makes `f_0^2 < 0` compulsory on AF provably disappears at
`g* != 0` (the linear term dominates the quadratic near `f_0^2 = 0`). Selecting the healthy
trajectory requires the IR-consistency condition (a tachyonic IR is not a consistent theory --
W83's D2), which is a physics boundary condition, not branch structure.

One exact structural bonus: at ANY fixed point with finite `g* = 2/A != 0`, `eta_N* = -2` exactly
(dimensional identity `beta_g = (2 + eta_N) g`), independent of every schematic magnitude -- the
truncation respects the exact anomalous-dimension structure of the real Reuter FP.

## 3. Persona 3 -- Krein specialist: is keep-and-grade compatible with an interacting FP?

W119 proved the spin-2 grading RG-stable at all finite scales on the AF trajectory. Does that cover
the AS trajectory? Within this truncation, **yes, by the same exact law**: `beta_{f_2^2}` is
`g`-independent (the Weyl coupling's marginal flow does not couple to the EH sector at this order),
so along ANY Reuter-basin trajectory `1/f_2^2(t) = 1/f_2^2(0) + kappa Phi b_2 t` verbatim --
`f_2^2 > 0` at every finite scale (verified numerically against the analytic law to 1e-3 over
t = 400). Consequences, per ghost-mass fork branch:

- **Branch A (agravity `d_locus = f_2^2/2`):** grading clear at all finite scales; pinches onto the
  exceptional locus only at the FP endpoint `f_2^2* = 0` -- the SAME boundary behavior as the
  Gaussian endpoint (W53/W119). The interacting FP does not create a new degeneration; it relocates
  the same endpoint pinch.
- **Branch B (fixed-scale `m2 = sqrt(m2_eff) mu_DW`):** `d_locus = m2_eff in [5/6, 5/4]`,
  scale-independent, positive including at the Reuter FP. STAYS-CLEAR.

**Named caveat (the honest hole):** a `g`-dependent dressing of `beta_{f_2^2}` at finite `g` -- an
`eta(g) f_2^2` term analogous to the de-slaving term -- is outside this truncation and could in
principle move `f_2^2` off its AF law near the FP. The PORTED support that this is not an artifact:
BMS find the Weyl coupling stays (marginally) irrelevant at their higher-derivative Reuter FP, i.e.
the interacting FP does not destabilize the Weyl direction. Within the truncation the verdict is:
**keep-and-grade is COMPATIBLE with the interacting FP; the grading question adds no obstruction to
the AS branch and no selector between branches.**

## 4. Persona 4 -- numerical engineer: the reproducible system

`tests/W128_h65_reuter_branch_scalaron.py`, 20/20 checks, exit 0. Structure:

- **Positive controls.** PC1 pure-gravity Reuter FP `g* = 0.700`, `g* lambda* = 0.1089` (calibrated
  literature values, mirrored from W83/W119); PC2 GU-content FP `g* = 0.6740`, `lambda* = 0.1512`
  (the W119 PC4 / W83 A1 catalogued values); PC3 the W46 AF fixed-ratio roots `-23.575, -0.0848`
  (the contrast object); PC4 threshold closed forms by fresh quadrature, band rebuilt
  `[0.500, 2.467]` (matches W119's `[0.50, 2.47]`, not copied).
- **Derived core.** B1 sign-lock (sympy, `y*/eta0` manifestly positive under positivity
  assumptions); B2 the exact eigenvalue pair `(-eta0 g*, +eta0 g*)`; B3 root positivity across the
  dressing x schematic-magnitude grid; B4 the two FP-level `m_0^2` verdicts; C0-C4 the IR/UV
  trajectory suite (RK4, both signs, basin boundary, AF contrast); D1-D2 the grading extension
  (numeric vs analytic `f_2^2` law; both fork branches); A2 the `eta_N* = -2` sympy identity.
- **Negative control.** NC1: `eta0 = -0.9` flips the de-slaved root to `-115.0` and the main-FP
  eigenvalue to `+0.607` (irrelevant) -- the machinery detects the flip; the AS-side verdict is not
  vacuous or hardcoded.
- **Verdict encoding.** E1: all three conditional branches encoded and checked (Reuter artifact ->
  AF-NOGO; relevance flipped -> TACHYON-FOLLOWS; both as ported -> AS-RESCUES-CONDITIONAL).
  E2: FORK-STANDS checked, not assumed (both branches host UV-complete trajectories; grading and IR
  data branch-blind).

## 5. Persona 5 -- adversarial skeptic: steelmanning TACHYON-FOLLOWS, attacking selection claims

**Steelman 1: "the generic expectation is that the negative-definite structure follows GU onto every
branch."** Examined natively and it FAILS as stated: the AF forcing is not a negative-definite
coupling structure (the quadratic-block coefficients `5/3, 5, 5/6` are all POSITIVE); it is the
fixed-ratio slaving of `f_0^2` to `f_2^2` on the AF trajectory. At `g* != 0` the linear de-slaving
term dominates near `f_0^2 = 0` and the slaving mechanism is gone -- exactly the eigenvalue
computation (`~0` at Gaussian, `-eta0 g*` at Reuter, W83's B3 re-derived here symbolically). The
generic-expectation steelman survives only in its sharpened form: **"eta0's sign is ported; flip it
and the tachyon follows."** Conceded loudly -- that is the negative control, and it is why the
verdict carries CONDITIONAL in its name. The flipped world is however not free: it requires the
`R^2` direction to be IRRELEVANT at the interacting FP, against four independent truncation
lineages. The skeptic's residual is real but it is one specific, falsifiable, literature-opposed
sign, not a generic doom.

**Steelman 2: "even granting the sign, AS does not rescue anything -- the tachyonic trajectory is
UV-complete too, so GU may simply sit on it."** CORRECT, and adopted into the verdict: this is
exactly why the verdict is PERMITTED-NOT-FORCED rather than AS-RESCUES outright. The branch removes
the compulsion, not the possibility. The healthy trajectory must be selected by the IR-consistency
requirement (a background-independent tachyonic IR is not a consistent theory -- W79/W83 D2), which
is a boundary condition GU's observed IR (attractive gravity, stable vacuum) supplies physically but
which nothing in the UV structure derives.

**Attack on branch selection: "any claim that GU selects AS is truncation-contingent."** SUSTAINED.
W128 checked the three GU-native candidate selectors and none selects: the Krein grading is
compatible with both branches (Section 3); the induced-action IR data (`gamma > 0`, `Lambda > 0`)
feeds both; the `mu_DW` fixed-scale construction is branch-blind. Both branches host UV-complete
trajectories within the truncation. **E2 FORK-STANDS**; W83's satisfiability filter
(UV-complete AND non-tachyonic) remains the only selector, it is conditional, and it is a
consistency principle rather than GU structure. No selection claim is made here, so none can be
attacked further.

**Attack on the truncation: "the de-slaving term was inserted, not derived -- the whole Reuter
sector is schematic."** PARTLY SUSTAINED, and it is the named residual: the EH magnitudes are
schematic (W83's calibration, reproduced as controls), and `eta0`'s magnitude is schematic. What the
sign-lock theorem changes is the epistemic shape: the branch verdict no longer rests on the
schematic apparatus but on ONE sign with four independent literature witnesses, and the single
computation that would replace the port is precisely named -- the native Hessian/threshold
projection of `beta_{f_0^2}` at finite `(g, lambda)` with GU's content and conventions, i.e. the
H66 graviton block. That is the referee-vulnerability discharge path.

## 6. Verdict

| Question | Answer | Grade |
|---|---|---|
| (1) Reuter-FP R^2 sector sign | Main family `f_0^2* = 0` (massless at the FP); de-slaved root `f_0^2* = eta0 g*/(kappa Phi c_C)` **SIGN-LOCKED positive** given the ported relevance; `m_0^2 >= 0` at both Reuter-family FPs. Numbers: `g* = 0.674`, `lambda* = 0.151`; root `[+15, +230]` schematic-magnitude band. | DERIVED lock on PORTED sign |
| (2) IR-trajectory scalaron fate | FREE relevant boundary condition: healthy (`f_0^2 -> +0.128`, `m_0^2 > 0`) AND tachyonic (`-0.128`) trajectories both emanate UV-complete. Healthy is PERMITTED (forbidden on AF); tachyon is POSSIBLE but not forced. | DERIVED within truncation |
| (3) Branch selection | **FORK-STANDS.** No GU-native selector (grading compatible with both; IR data and `mu_DW` branch-blind; both branches UV-complete). W83's filter remains the only, conditional, selector. E2 CARRIED. | DERIVED (absence checked) |
| (4) Bottom line | **AS-RESCUES-CONDITIONAL (PERMITTED-NOT-FORCED).** The tachyon does NOT follow GU onto the AS branch as a forced property; the forcing mechanism is AF-specific and provably absent at `g* != 0`. Conditional on (i) Reuter FP genuine, (ii) the ported R^2-relevance sign; (ii) flipped gives TACHYON-FOLLOWS (negative control). | DERIVED-on-PORTED |

**What would settle it (in order of decisiveness):**
1. **The native de-slaving sign** -- the Hessian/threshold projection of `beta_{f_0^2}` at finite
   `(g, lambda)` on GU's own content and conventions (= the H66 GU-native graviton one-loop block,
   paper-scale). This replaces the one ported sign the whole branch hinges on; it is the referee
   vulnerability's discharge.
2. **Reuter-FP genuineness for GU's exact content** -- a second regulator / higher polynomial
   `f(R)` truncation with GU's true `(N_S, N_D, N_V, N_RS)` (W83's next-swing 2, still open).
3. **A GU-native branch selector** -- any UV boundary condition derived from GU structure (the
   residual-arity audit H72 is the adjacent question). Until one exists, E2 is a carried fork and
   the honest flagship statement remains per-branch.

## What this does NOT do

No native computation of the de-slaving sign (THE hinge stays PORTED, four-lineage-witnessed); no
proof of asymptotic safety; no closure of the E2 fork (CARRIED); no loop amplitude, no positivity
claim (W48 gate); no resolution of the ghost-mass or eta_C forks (both carried, endpoint-confined on
AS exactly as on AF); no canon / RESEARCH-STATUS / claim-status / verdict / posture change.
H59/H61a remain OPEN.
