---
artifact_type: exploration
status: exploration (W140; full-roster steelman sweep, THEORETICAL PHYSICS family; assumption-first by design; 14 personas inline, 28 stories, internal scoring pass against the W138 battery; mid-wave framing correction from Joe applied before finalizing, Section 0)
created: 2026-07-14
wave: W140
hypothesis: "Joe's framing, steelmanned, with the mid-wave correction applied: science ALREADY KNOWS the dark-energy rate -- W135 (16e476f) measured it in repo units: rho_Lambda ~ (2.24 meV)^4; bookkeeping issuance Q_tot = 3 H0 rho_L V_H = (3/2) Omega_L c^5/G = 1.027 Planck luminosities per Hubble volume (to 3%); dimensionless q_B/(H0^3 M_Pl,red^2) = 9 Omega_L = 6.16. The steelman question per persona: IF that known, measured number is DECLARED as the issuance input to the source action, distributed through the observers, what OTHER information can be backed out (which spec FITs, scales, relations), to be rigorously checked later. Exotic schedules are OUT OF SCOPE by the correction (and independently dead in W135's own taxonomy: drip, holographic, halving). NOT the falsified naive claim that record-creation consumes positive issuance (W137 killed that pointwise; only the signed ledger survives)."
title: "W140 -- theoretical-physics-family steelman sweep of the issuance-distribution function. 14 personas (Family A, orthodox/rigor/mainstream-physics partition of the 2026-07-11 roster), 2 stories each = 28 candidate distribution functions F, each with an assumption leg (which spec FIT or open object it pins) and a concrete test, scored against the W138 kill battery (G1-G6) and the standing physics (W122/W123/W126 tachyon chain, W128 AS branch, W130 native tree point, W132 C-operator equivalence, W133 family-fork declaration, W136 issuance propagation, W137 signed ledger). 11 stories KILLED-AT-GENERATION with computed or cited reasons; 3 reclassified OUT-OF-SCOPE by the mid-wave framing correction (schedule-drift stories A1-1/A7-1/A12-1: the declared input is the KNOWN MEASURED rate, not a schedule freedom); 14 in-scope survivors; top tier = (1) the Krein-excess distribution (issuance = the pseudo-unitarity excess the C-metric re-books; SET collectively, DISTRIBUTED per positive-subspace observer; positive per observer where the |II|^2 ledger was signed), (2) the C-compression trace distribution (per-observer share = trace density of the compressed C-metric; cheapest test = W137 K2), (3) the two-scale-band uniform issuance (q = 3 H c mu_emb^4; the escape band and the H52 floor envelope intersect almost exactly, computed; falsifiable by a ~1.4x sub-mm floor improvement), (4) the Gibbs-measure AS selector (the distribution exists only if the scalaron is non-tachyonic, so the issuance reading REQUIRES the AS branch: the fork's first issuance-side selector), (5) the observer-dilution exact-LCDM function (total set, per-observer share = total / N_obs(a); pins B_i = 0, f0 = 0 as the prediction). Deterministic scoring arithmetic in tests/W140_steelman_scoring_checks.py (16/16, exit 0; the W135 measured-rate identities Q_tot/L_Planck = (3/2) Omega_L = 1.027 and 9 Omega_L = 6.16 reproduced as anchors N7/N8)."
grade: "exploration / hypothesis-generation, conditional register throughout. Every statement of the form 'the issuance is/does X' is to be read 'under the issuance declaration (W136 sense), X'; nothing here asserts the declaration. COMPUTED (this wave, deterministic): the two-scale band vs floor-envelope intersection, the SFH-schedule mimic-gate kill factor (~9x), the curvature-invariant dimensional kill (~6e120), the halo-weighting G1b margin (~4e12 headroom), the pinned-point Gibbs sign (f0^2 = -1/8 < 0), plus four positive controls reproducing W138 anchors and two reproducing the W135 measured rate. CITED (not re-derived): the entire W135/W136/W137/W138 evidence pack and the standing-physics chain. No canon / RESEARCH-STATUS / claim-status / verdict / posture change; no FIT row moves; H41 stays unbuilt; H59 stays OPEN."
construction: "fork named per GEOMETER-VS-PHYSICS-OBJECTS.md, per story below. The observer object is program-native throughout (section sigma: X4 -> Y14, the W137 skeleton); the cosmological gates are standard-field (FRW continuity, ephemeris, Landauer) exactly as W138 built them so they bind any construction. Stories state which side their F lives on."
depends_on:
  - explorations/W135-issuance-structure-taxonomy-2026-07-14.md
  - explorations/W136-issuance-declaration-propagation-2026-07-14.md
  - explorations/W137-observer-slice-structure-2026-07-14.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - explorations/two-track-persona-sweep-2026-07-11/A-orthodox-rigor.md
  - explorations/two-track-persona-sweep-2026-07-11/SYNTHESIS.md
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W132-graded-optical-theorem-physical-subspace-2026-07-14.md
  - explorations/source-action-requirements-spec-2026-07-13.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W140_steelman_scoring_checks.py
---

# W140 -- Theoretical-physics-family steelman sweep: the issuance-distribution function F

**Charge.** An earlier team tested a strawman of this framing (the positive-ledger reading,
already falsified pointwise by W137). This sweep steelmans the actual claim: the cosmological
constant is a SET issuance rate; the issuance is DISTRIBUTED THROUGH THE OBSERVERS in some way;
the deliverable is the candidate FUNCTION F for that rate/distribution, specific enough that
declaring it as an assumption leg lets the program back into the rest of the story (a spec FIT,
an open object, a branch of a standing fork). Assumption-first by design; the rigorous kills run
inside this artifact, against the pre-committed W138 battery.

## Section 0: mid-wave framing correction (from Joe, applied before finalizing)

The correction, received while the sweep was executing: drop ALL exotic-schedule
characteristics (halving, difficulty adjustment, schedule exotica) from the steelman target.
The corrected claim is minimal: science ALREADY KNOWS the dark-energy rate. W135 (commit
16e476f, landed mid-wave) measured it in repo units:

- rho_Lambda ~ (2.24 meV)^4 (Planck-anchored; reproduced here as control C1);
- bookkeeping issuance per Hubble volume Q_tot = 3 H0 rho_L V_H = (3/2) Omega_L c^5/G
  = **1.027 Planck luminosities** (exact identity + O(1) coincidence flag, reproduced here as
  check N7, agreement to 3%);
- the dimensionless ladder q_B/(H0^3 M_Pl,red^2) = **9 Omega_L = 6.16** (THE O(1) ratio per
  W135; q_B/H0^4 ~ 1e121 is not; reproduced as check N8).

The steelman question per persona is exactly: IF that known measured number is DECLARED as the
issuance input to the source action, what OTHER information can be backed out (which spec
FITs, scales, relations), to be rigorously checked later. Consequences applied to this sweep:

1. **Schedule-drift stories are out of scope.** A1-1 (unimodular drift), A7-1 (running-Lambda
   schedule), and A12-1 (pre-registered schedule test) are RECLASSIFIED OUT-OF-SCOPE (not
   killed: their gate sheets were clean, but their content is a schedule freedom the corrected
   framing does not have). A7-1's branch-selector residue is folded into A3-1, where it
   belongs without any schedule. W135's own taxonomy independently killed the exotic schedules
   (per-comoving-time drip squeezed from both sides, b2-pure dead by the parameter-free floor
   |w0+1| >= 0.35, the holographic c-family dead, halving steps provably BAO-invisible in
   shape but dead by secular drift), so the reclassification loses nothing alive.
2. **The declared input is now a MEASURED number, not a free postulate value.** Every
   surviving story's Q_tot is the W135 rate above; the W136 boundary declaration and the W135
   measurement are the same number in two registers (declaration-side and data-side). This
   STRENGTHENS the surviving stories: the assumption leg starts from a number science already
   owns, and everything the declaration pins (SA-G5 = 2, B_i = 0, f0 = 0, the two-scale band,
   the branch preference, the C-operator clauses) is backed out of a measurement, not a guess.
3. **W135's null-structure theorem sharpens the whole sweep:** for rho = const the covariant
   source Q = 0 IDENTICALLY and the bookkeeping rate is pure pressure work; only w != -1
   deviations evidence genuine issuance. So under the corrected framing the "set rate" is a
   bookkeeping rate of the known background, and the entire physical content of the framing
   lives in the DISTRIBUTION leg (which observers, weighted how) and in what the declared
   number pins -- exactly where this sweep's surviving stories already put it. The A5-1 kill
   (schedules tracking observer activity die 9x outside G2) stands as the boundary marker.

Scoring below is re-framed accordingly; kills and gate sheets are unchanged (the battery is
schedule-agnostic); the survivor count is restated as 14 in-scope.

**Roster.** Family A of the 2026-07-11 two-track persona sweep (orthodox / rigor /
mainstream-physics gatekeeper) is the theoretical-physics partition of the roster; all 14
personas are taken, run inline, sequentially, one context, no sub-agents:
(1) orthodox gravity theorist, (2) heterodox-rigorous professor (signature crux),
(3) constructive/lattice QFT theorist, (4) anomaly/index theorist, (5) numerical
relativist/observational phenomenologist, (6) particle phenomenologist, (7) renormalization/EFT
theorist, (8) mathematical-physics referee, (9) falsifiability gatekeeper, (10) rigor gatekeeper,
(11) historical-priority lens, (12) advanced-statistics expert, (13) operator algebraist,
(14) index/K-theory rigorist.

**The shape every story must fill.** (i) F, mathematically specific: issuance rate as a function
of WHAT (Hubble volume, horizon area, section count/density, C-operator subspace structure,
matter content, curvature invariants); (ii) the assumption leg: which spec FIT or open object
declaring F pins (candidates: mu_DW, B_i, the two-scale escape band, m2_eff, the AF-vs-AS
branch, the C-operator's structure); (iii) the persona's concrete test.

**Binding constraints honored throughout** (violations are kills, not caveats):

- W138 battery: G1 conservation (ledger named; local enhancement < ~1e19; H10/H52 channels on
  re-entry), G2 mimic (|d ln rho/d ln a| < 0.3 per e-fold; must NOT claim the DESI wiggle),
  G3 H36 non-reimport (>= 4.775x), G4 B2 non-reimport (no structural constant read as a rate;
  schedule/filtration readings alive), G5 de Sitter relabeling (normalization derivable from
  E_Lambda = T_dS S_dS / 1.46 alone = novelty-fail), G6 record cost (Landauer budget numbers).
- W137: the positive ledger is FALSIFIED pointwise; only the SIGNED bookkeeping survives. No
  story below re-asserts "records consume positive issuance."
- Standing physics: AF-branch scalaron tachyonic (W122/W123/W126); AS branch conditionally
  healthy (W128); native tree point off the AF branch (W130); keep-and-grade unitarity =
  C-operator existence (W132); family fork = permanent declaration empirically (W133);
  W136 pins beta/alpha* = 2, natural point B_i = 0 / f0 = 0, two-scale band named-not-taken.
- Tri-repo gate: "issuance" is a local postulate label (W136 sense); the issuance concept is
  owned by the temporal-issuance repo; no cross-repo identity claim (R5 kill stands). Capability
  MEASURE belongs to TaF. One-way rule enforced.
- W135 (landed mid-wave, cited per the framing correction): the measured rate is the declared
  input; the null-structure separator (Q = 0 identically at w = -1) and the W135 schedule
  kills (drip, holographic, halving) are honored; the surviving-transducer requirement (one
  scalar: Q = 0, Q = const, or a feedback term) is noted where stories touch it.

Scoring arithmetic: `tests/W140_steelman_scoring_checks.py` (16/16, exit 0; four positive
controls reproduce the W138 anchors -- DE scale 2.239 meV, H36 factor 4.775, dS ratio 1.4605,
Landauer stellar gap 1.32e6 -- and two reproduce the W135 measured rate (N7: Q_tot/L_Planck =
(3/2) Omega_L = 1.027; N8: 9 Omega_L = 6.16) before any new number is extracted).

---

## Part 1: the 28 stories (per persona, two each)

Notation: q = issuance rate density (energy per volume per time); the SET total at background is
q_tot = 3 H rho_L per comoving volume (the fixed-rho_L bookkeeping rate, W138 G1; per the
framing correction this is the MEASURED W135 number, Q_tot = (3/2) Omega_L c^5/G = 1.027
Planck luminosities per Hubble volume, dimensionless 9 Omega_L = 6.16); sigma = an
observer slice = a section X4 -> Y14 (W137 skeleton); H_C+ = the C-operator's maximal positive
subspace (W132); P_sigma = the sigma-localized compression (W137 C1).

### Persona 1: orthodox gravity theorist (Bach / Stelle / Ostrogradsky lens)

**A1-1 (unimodular ledger drift).** F: work in unimodular gravity where Lambda is the
integration constant; the issuance is its slow drift sourced by a comoving current,
dLambda/d ln a = -8 pi G q(a)/H with q(a) = 3 H rho_L epsilon(a), epsilon the drift fraction.
The Bianchi ledger sector is named by construction (G1a pass): the unimodular constant absorbs
the non-conservation. Distribution through observers: epsilon(a) is sourced by the comoving
observer current density (each observer contributes a fixed increment per unit proper time).
Assumption leg: pins B_i -- the W136 natural point B_i = 0 is epsilon = 0; any nonzero epsilon
IS the B_i deviation, bounded by G2 at |3 epsilon| < 0.3. Test: the W129 likelihood machinery
rerun with the one-parameter epsilon schedule, pre-registered (existing machinery; the G2
translation is the analytic cross-check). Status: RECLASSIFIED OUT-OF-SCOPE by the framing
correction (Section 0): its content is a schedule freedom (epsilon != 0) the corrected framing
does not have; the surviving residue -- the unimodular constant as the named Bianchi ledger
sector for the DECLARED constant rate -- is absorbed as the G1a mechanism every in-scope story
may cite. Gate sheet was clean; nothing killed.

**A1-2 (Weyl-channel / Bach-invariant sourcing).** F: q(x) = c_B |Bach|(x) with dimensionless
c_B -- issuance density proportional to the Bach-tensor invariant, so it vanishes exactly where
the geometry is conformally Einstein and concentrates at structure. KILLED-AT-GENERATION:
FRW is conformally flat, so Weyl = 0 and Bach = 0 identically at background for ANY matter
content; the candidate emits exactly zero background issuance and cannot source the uniform
rho_L it was declared to be. (Secondary kill: Schwarzschild is an Einstein space, Bach = 0, so
the function is also blind to every vacuum strong-field region.) One line, no computation
needed beyond the classical identities.

### Persona 2: heterodox-rigorous professor (signature crux)

**A2-1 (causally graded distribution; the signature as the distribution law).** F: the
per-deformation issuance exchange of an observer slice is the W137 signed form itself, read as
the distribution law: q(sigma; deformation) = the exact signed density with time-space mixing
paying (+32 w^2), spatial traceless and conformal trace releasing (-32 w^2, -64 u^2), gradient
sector 16 eta(d phi, d phi) (timelike releases, spacelike pays). The claim steelmanned: the
issuance is not distributed per observer COUNT but per observer ACTIVITY, graded by the causal
type of the activity; the Lorentzian signature IS the distribution function. Assumption leg:
pins the physical meaning of the (9,5) signature choice (the grading exists only in Lorentzian
signature) and gives the W137 signed ledger its role: the sign structure is the distribution,
not a bug. Test: verify the covariant form 16 eta(d phi, d phi) beyond the two computed axes
(named, tractable extension of tests/W137_observer_slice_deformation_cost.py). Status:
SURVIVES (it is the steelman of the signed ledger rather than a denial of it).

**A2-2 ((7,7) three-channel equipartition).** F: under the (7,7) signature the odd-rank-3
projector exists and the issuance distributes in three equal family channels, q_i = q_tot/3.
KILLED-AT-GENERATION: W133 established the family fork is empirically a PERMANENT DECLARATION
(no observable discriminates), so the story's only content is an unobservable partition label;
it pins nothing (no FIT moves; the count is source-action-gated) and names no test that could
distinguish q_i = q_tot/3 from no partition at all. Fails the sweep's own sharpness bar.

### Persona 3: constructive / lattice QFT theorist

**A3-1 (the Gibbs measure over sections; the AS-branch selector). TOP-TIER.** F: the issuance
distributes over observer slices as a Gibbs measure, d mu(sigma) = Z^{-1} exp(-S_ind[sigma])
D sigma with S_ind the induced |II|^2 action of the pinned construction, and per-observer
issuance q(sigma) = q_tot * dmu-weight of sigma's section class. Constructive requirement: the
measure must EXIST, i.e. the action must be bounded below in the fluctuation directions.
COMPUTED (this wave, N5): at the W136 pinned point beta/alpha* = 2 the family scalar channel is
c_R = -4/3, so f0^2 = 1/(6 c_R) = -1/8 < 0 -- the tree scalaron stays tachyonic and the Gibbs
measure over sections is UNBOUNDED BELOW at tree on the AF/native side. W128: on the AS/Reuter
branch the de-slaved root carries the healthy sign (+0.128) on UV-complete trajectories. So the
distribution function exists ONLY on the AS branch. Assumption leg: the AF-vs-AS fork acquires
its FIRST issuance-side selector -- declaring the issuance-distribution reading forces the AS
branch (previously PERMITTED-NOT-FORCED, W128). This is exactly the "story that prefers a
branch" the evidence pack ranks highest. Test: the named W128 settling computation (the native
beta_{f0^2} Hessian projection at finite (g, lambda), = the H66/W130 graviton-block lane)
becomes the story's life-or-death check: if the native Hessian keeps the tachyonic sign on ALL
UV-complete trajectories, the measure never exists and the story dies with the branch. Status:
SURVIVES, conditional on AS.

**A3-2 (block-spin entropy schedule).** F: q = T d S_block/dt, the issuance as the free-energy
release of one e-fold of block-spin coarse-graining of the observer's record lattice.
KILLED-AT-GENERATION: at the only temperature where the numbers close (T_dS) the capacity IS
the horizon entropy (W138 G6: 2.96 S_dS, the same factor 1.46), so the normalization reduces to
the de Sitter identity with no new degree of freedom -- a G5 novelty kill; at any higher T the
schedule is unanchored (no rule picks the blocking rate) and the story is not mathematically
specific enough to score.

### Persona 4: anomaly / index theorist

**A4-1 (spectral-flow issuance).** F: q(sigma) = epsilon_0 * (spectral-flow rate through zero
of the compressed operator P_sigma D P_sigma), records = eigenvalue crossings of the observer's
compressed Dirac-type operator, epsilon_0 a declared quantum. The issuance is quantized per
crossing; the distribution is the crossing density per slice. Assumption leg: pins the
C-operator's structure at the boundary: the compression needs the W103 tail-quotient slot (the
positive invertible metric at infinity) as the APS-type boundary condition, so declaring F
pins the one typed interface the wall analysis left standing. Test: in the W54 free model,
drive a one-parameter family of section deformations and count spectral flow of the compressed
operator (machinery: partial; the compression exists, the flow count is a new but finite
computation). Status: SURVIVES (mid-rank: eta/spectral flow is deformation-sensitive, so the
rigidity kill of A14-2 does not apply to it; but epsilon_0 is a free quantum, weakening the leg).

**A4-2 (topological quantization in the 3-primary channel).** F: issuance quantized per unit of
a mod-3 boundary invariant tied to the generation count. KILLED-AT-GENERATION: the repo's own
near-theorem (Hom(Z/3, Z) = 0; the mod-3 Dai-Freed arena is empty, Omega^Spin_5(BG_SM) tensor
Z_(3) = 0, roster file Section synthesis) says no canonical integer-valued invariant lives
there; the story asks the issuance to be counted by an invariant that provably does not exist
in that arena.

### Persona 5: numerical relativist / observational phenomenologist

**A5-1 (record-tracking schedule; SFH-weighted).** F: rho_iss(a) tracks the record-creation
rate of the observer population, proxied by the cosmic star-formation history:
d ln rho_iss/d ln a = d ln psi_SFR/d ln a (Madau-Dickinson form). KILLED-AT-GENERATION,
COMPUTED (N2): the SFH slope reaches |d ln rho/d ln a| = 2.686 per e-fold at z ~ 0 (exactly
where w0 is measured), 8.95x outside the G2 mimic gate (0.3). Any issuance whose SCHEDULE
tracks observer record-creation activity is dead by an order of magnitude; the surviving
versions must decouple the schedule (set) from the distribution (through observers). This kill
is the sweep's sharpest internal lesson: Joe's framing survives ONLY in the form "total set,
distribution varies," never "total tracks the observers."

**A5-2 (observer-dilution exact-LCDM function). TOP-TIER.** F: the total is SET at the
MEASURED W135 rate, Q_tot = 3 H0 rho_L V_H = (3/2) Omega_L c^5/G = 1.027 Planck luminosities
per Hubble volume exactly (w = -1 exact; W135's null structure: Q = 0 covariantly, the rate
is bookkeeping); the per-observer
share is F(sigma, a) = Q_tot / N_obs(a), the issuance divided evenly over the admissible
observer slices, so each observer's share falls as structure forms while the total never moves.
The distribution carries all the observer-dependence; the schedule carries none. Assumption
leg: pins the spec FIT pair B_i = 0 and f0 = 0 EXACTLY -- the W136 natural point becomes the
PREDICTION of the distribution reading (exact LCDM in the DE channel, strictly inside every
W129 band bound), rather than a choice. Test: already running -- W129 containment is the
current pass; any future detection of |w0 + 1| > 0.1 at signal level (DESI DR3+, W129
machinery) kills the story outright, and the story is forbidden from claiming the present DESI
wiggle (G2 discipline: it predicts the wiggle is NOT real). Status: SURVIVES (cleanest gates
sheet of the sweep; its weakness is that the background prediction is null, so its novelty
lives entirely in the distribution leg, which needs A13-1/A13-2 to be measurable).

### Persona 6: particle phenomenologist

**A6-1 (Majorana-channel transduction).** F: the issuance enters the matter sector solely
through the one dimensionful channel W136 found reachable -- the Majorana mass (SA-Y8):
q distributed to observers via the cosmic neutrino background, with rho_L^{1/4} = 2.24 meV
sitting one OOM inside the neutrino mass band and the seesaw conversion v^2/rho^{1/4} =
1.3e16 GeV. Assumption leg: pins SA-Y8 (the Majorana FIT) and the seesaw scale as the
transduction scale. Test: the OOM ledger against the tightening sum-m_nu bounds (DESI
cosmology); a sum-m_nu detection at the band edge sharpens the click, a bound pushing the
lightest mass far from 2 meV degrades it. Status: SURVIVES at OOM tier only (comparison-only,
exactly as W136 graded the click; the leg is real but one order of magnitude wide).

**A6-2 (three-generation equipartition via 3:2:1).** F: q_i in ratio 3:2:1 across the three
generations, imported from the W130 Einstein-sector split. KILLED-AT-GENERATION: type error --
the 3:2:1 split is gamma_TT : gamma_phi : gamma_slice, a DEFORMATION-SECTOR split at one
section (W130, W137 C3), not a family split; no repo object connects deformation sectors to
generations, and W133 makes the family fork empirically a permanent declaration anyway.

### Persona 7: renormalization / EFT theorist

**A7-1 (running-Lambda schedule on the AS branch).** F: rho_iss(a) = rho_L(k(a)) with
k = xi H(a), the RG-improved running cosmological constant near the Reuter fixed point;
near-Gaussian IR expansion rho_iss(a) = rho_0 (1 + nu ln a + ...), so the schedule is
d ln rho/d ln a = nu to leading order. G2 bounds |nu| < 0.3 today (and the story must not
claim the DESI wiggle: |nu| = 0.744 is 2.48x excluded, N6). Assumption leg: pins the AF-vs-AS
branch from the schedule side, complementing A3-1: on the AF/native side the W130 direct-chart
Weyl flow crosses c_W = 0 (strong coupling) after ~43-48 e-folds, so an AF-side RG-improved
schedule hits a strong-coupling wall within the post-BBN e-fold budget and cannot stay a
mimic; the AS side has the fixed point to anchor nu small. Test: compute nu on the W128
UV-complete trajectories at the IR end (the W128 trajectory machinery exists; extracting the
IR slope is bookkeeping) and check |nu| < 0.3. Status: RECLASSIFIED OUT-OF-SCOPE as a
schedule story (framing correction, Section 0): the nu-drift is schedule exotica. The
branch-selector RESIDUE survives and is folded into A3-1: even at nu = 0 (the declared
constant rate), asking WHERE the declared constant can consistently live still discriminates
-- the AF/native side hits the W130 strong-coupling crossing (c_W = 0 after ~43-48 e-folds)
so the constant cannot be RG-anchored there, while the AS side has the fixed point. That
residue needs no schedule and is now part of A3-1's case.

**A7-2 (coupling-as-rate: alpha_W or f0 as the issuance rate).** KILLED-AT-GENERATION: this is
verbatim the killed B2 form (W138 G4: f0, c_L, alpha_W all failed exactly this way; a rate is
the quotiented Connes fiber and cannot appear in rate-invariant structural content). Cited,
no new computation; included so the family's most tempting move is on the record as dead.

### Persona 8: mathematical-physics referee

**A8-1 (Q-curvature / conformally covariant density).** F: q(x) = c_Q Q_g(x), the issuance
density as the Q-curvature (the unique conformally covariant scalar of the right weight,
resonant with the repo's Willmore/GJMS lane), c_Q dimensionless. KILLED-AT-GENERATION,
COMPUTED (N3): on the observed background every curvature invariant of dimension 4 scales as
H^4 ~ (1.44e-33 eV)^4 while the required density is (2.24 meV)^4; the shortfall is a factor
5.9e120. A dimensionless-coefficient curvature-invariant issuance underpays dark energy by
~121 orders of magnitude; rescuing c_Q ~ 1e121 is the cosmological-constant problem restated,
not a distribution function.

**A8-2 (Noether-charge normalization; the deliberate G5 control).** F: issuance = the flux of
the conformal Killing-time Noether charge through the de Sitter horizon, distributed per
observer horizon patch. KILLED-AT-GENERATION (novelty, by design): the normalization reduces
identically to E_Lambda = T_dS S_dS / 1.46 with no new degree of freedom -- the Gibbons-Hawking
equilibrium relabeled, the exact case W138 G5 was built to catch (control confirming the gate
fires: C3 reproduces the 1.4605). Included as the sweep's positive control on the novelty gate.

### Persona 9: falsifiability gatekeeper

**A9-1 (two-scale-band uniform issuance). TOP-TIER.** F: q = 3 H c mu_emb^4, uniform comoving
issuance whose scale is the EMBEDDING scale mu_emb (the second scale of W136's named-not-taken
escape), distinct from the operator scale mu_DW, with the O(1) geometric ratio
mu_emb/mu_DW in [0.41, 0.57] (c = 2 chart) or [0.63, 0.85] (c = 3/8 chart). This is the one
story that takes the two-scale escape rather than naming it. Assumption leg: pins the two-scale
escape band as a real FIT (plus the counted H24-revision cost), and CONDITIONALLY pins mu_DW:
COMPUTED (N1), the implied mu_DW range is [3.386, 4.707] meV (c = 2) and [3.459, 4.667] meV
(c = 3/8) against the H52 floor envelope [3.4, 4.7] meV -- the intersection is nearly the
entire envelope, i.e. the escape band and the sub-mm floor agree almost exactly, which is
either a coincidence or the first two-sided localization of mu_DW the program has. Both central
m2_eff floors (3.71 and 4.54 meV) sit strictly inside both charts' bands, so m2_eff is NOT yet
pinned; but the pin sharpens mechanically with the floor. G3 is passed by construction (the
issuance scale is mu_emb, not mu_DW; the implied c_L = (2.24/mu_DW)^4 stays under every W138
cap). Falsifier, computed (N1e): a resolved sub-mm floor above 4.71 meV (a ~1.4x improvement
on the envelope minimum) kills the c = 2 chart outright; above 4.67 meV kills c = 3/8. Test:
exactly that -- the next-generation sub-mm inverse-square experiments move the floor; the story
dies or localizes mu_DW to a shrinking two-sided band. Status: SURVIVES (sharpest quantitative
assumption leg in the sweep).

**A9-2 (horizon-patch distribution with a named double-counting falsifier).** F: per-observer
issuance proportional to the observer's causal-diamond horizon area,
q(obs) = q_tot * A_diamond(obs) / sum A, the Bousso-style covariant partition. Assumption leg:
thin (it pins no FIT; the area weights are kinematic). Named falsifier: summing over a
uniformly distributed de Sitter congruence must return q_tot without double counting; if the
overlap correction cannot be made exact the partition is ill-defined. G5 risk flagged: if the
weights are uniform the normalization collapses to the horizon identity (relabel). Status:
SURVIVES provisionally, low rank (well-posed falsifier, weak leg).

### Persona 10: rigor gatekeeper ([speculation] discipline)

**A10-1 (two-postulate factorization).** F: q(sigma, a) = q_0(a) * w(sigma) with the SET
schedule q_0 [POSTULATE 1, the W136 declaration] and the distribution weight w, integral 1,
[POSTULATE 2, new]; the sweep's discipline is that every surviving story above is an instance
of this factorization and must tag BOTH postulates separately (A5-1 died precisely by welding
them). Assumption leg: pins the DECLARATION STRUCTURE itself -- on the spec's keep-branch
(where W136's entire beta/alpha* = 2 selection lives) postulate 1 already pins SA-G5 = 2 and
the natural point B_i = 0/f0 = 0; postulate 2 is the only new freedom, and it is
observer-side. Test: the keep-vs-subtract fork is the named check -- on the subtract branch
the flat constant vanishes for every (alpha, beta) and postulate 1 loses its SA-G5 leverage,
so the factorized story is fork-dependent and must say so. Status: SURVIVES as the sweep's
normal form (meta-story; ranks by the stories it disciplines rather than by itself).

**A10-2 (kill-extension: the boundary-balance no-go rerun).** F candidate: the balancing
supply for signed record cost enters through the boundary term of the integrated action (W137
C2's reading), with the retreat landing on the W103 tail-quotient slot. The rigor move: run the
C2 integration-by-parts on Y14 sections NOW, because for compactly supported deformations the
boundary term vanishes identically, and if the bulk signed bookkeeping does not integrate to
zero against the deformation family then "issuance enters at the boundary" is FALSE for
compact records and every boundary-supplied F must retreat to asymptotic deformations (W103
slot) or die. Assumption leg: pins C2 and the W103 slot as the unique boundary home. Test: the
named integration-by-parts computation (tractable next wave, per W137). Status: SURVIVES as a
decisive-computation proposal (it is a test-of-the-others more than a rival F).

### Persona 11: historical-priority lens

**A11-1 (the Padmanabhan collision; a new novelty sub-gate).** F: the emergent-gravity
equipartition law dV/dt = l_p^2 (N_surf - N_bulk) H, issuance distributed per bulk degree of
freedom -- this is Padmanabhan's holographic equipartition, in the literature since 2010. The
persona's contribution is a G5-extension: any candidate F whose normalization OR distribution
reduces to (N_surf - N_bulk) equipartition (or to Verlinde's entropic force bookkeeping) is
prior-art relabeled even if it clears the strict dS-identity form of G5. Applied to the sweep:
A9-2 in its uniform-weight limit fails this sub-gate; A13-1/A13-2 pass it (the C-operator
structure has no equipartition analogue in that literature). Assumption leg: none (it is a
gate, not a proposal). Status: gate contributed; as a story KILLED-AT-GENERATION by its own
criterion (it IS the prior art).

**A11-2 (the Hoyle-Narlikar boundary).** F: continuous-creation C-field, q sourcing matter
(w = 0) rather than the Lambda channel. KILLED-AT-GENERATION as a rival but kept as a
boundary marker: steady-state creation is historically dead (CMB), and the persona's verdict
is that the issuance framing is NOT the C-field precisely when the issuance stays in the
Lambda channel with w = -1 (+/- the G2 band) -- a useful demarcation line: any story that lets
issuance source MATTER re-enters the killed steady-state lane and inherits its falsifiers.

### Persona 12: advanced-statistics expert

**A12-1 (pre-registered one-parameter schedule test).** F: rho_iss(a) = rho_0 exp(integral of
nu(a) d ln a) with nu pre-registered from whichever surviving story is being tested (A1-1's
epsilon, A7-1's Reuter-IR nu), then confronted with the actual W129 band-sweep likelihood --
never fit. Assumption leg: pins B_i as the schedule moments (the statistician's reading of the
same FIT A1-1 pins). Test: the W129 machinery IS the test; the persona's requirement is
pre-registration discipline (record nu, then run). Status: RECLASSIFIED OUT-OF-SCOPE
(framing correction, Section 0): under the corrected framing there is no schedule parameter
to pre-register (the rate is the known measured constant). The methodological residue --
pre-register any claimed consequence before confronting data -- remains binding on every
in-scope survivor's test.

**A12-2 (halo-density-weighted distribution; DE clustering).** F: q(x, a) = 3 H rho_L *
[rho_m(x)/rho_m_mean], total fixed (the weight integrates to 1 over the comoving volume),
issuance delivered preferentially where observers actually are. The naive objection is the
solar-system gate; COMPUTED (N4): the local (solar-neighborhood) enhancement is
rho_local/rho_mean ~ 2.5e6, which clears the G1b threshold of ~1e19 with ~4e12 headroom --
locally invisible issuance is fine, exactly as the evidence pack says. G2: the volume-averaged
schedule is untouched (total fixed), so background cosmology stays exact-LCDM. Assumption leg:
pins the distribution weight as measurable: a matter-weighted issuance predicts a specific
late-time DE clustering / ISW-type signature at perturbation level (DE is no longer perfectly
smooth), which is the first OBSERVABLE the distribution leg (as opposed to the schedule leg)
has produced in this sweep. Test: perturbation-level -- the story requires the full W129
likelihood extended with a DE-clustering parameter (named work, machinery partially exists);
the background test passes by construction. Status: SURVIVES (the distribution leg's best
observable; upgrade path named).

### Persona 13: operator algebraist

**A13-1 (the C-compression trace distribution). TOP-TIER.** F: q(sigma) = q_tot *
Tr(P_sigma eta_+ P_sigma) / Tr(eta_+), the per-observer issuance share as the trace density of
the compressed positive C-metric (eta_+ = eta C, W132) on the observer's slice, made
finite-per-volume by the W54 exponential localization (~1/m at one loop) of the C-operator.
The collective object is C (one section-independent operator, W137 C1 clause 2); the single
observer's share is its compression -- the SET/DISTRIBUTED split lands exactly on Joe's
framing in the repo's own operators. Assumption leg: pins the C-operator's structure, three
clauses: (i) C exists (K1; = keep-and-grade unitarity itself by W132), (ii) C is
section-independent (K3), (iii) the compressed metric is positive, invertible, and
trace-class-per-volume (K2 sharpened: the distribution needs a finite trace DENSITY, which is
precisely what exponential localization supplies if it holds interactingly). Declaring F backs
into C1 with the distribution as the physical payload. Test: the W137 K2 computation, already
graded cheapest -- compute P_sigma eta_+ P_sigma on a half-space region in the W54 free model
and check positivity + bounded invertibility, EXTENDED one step: also compute the trace
density and check finiteness/locality (the new requirement F adds to K2). Existing machinery.
Status: SURVIVES (gives the C-operator the physical role; the brief's highest-rank criterion).

**A13-2 (the Krein-excess distribution). TOP-TIER, ranked first.** F: the issuance an observer
registers is the pseudo-unitarity excess on its positive subspace: per scattering process,
q_obs = (A^dag A - P_+) = B^dag B >= 0 (the exact W132 expansion identity), summed over open
odd-ghost channels. Read as distribution: the COLLECTIVE bookkeeping (the C-metric inner
product) is exactly conserved -- the total is SET; the SINGLE observer, working on its
compressed positive subspace, sees a strictly positive influx (anti-damping) whenever odd
channels are open -- the issuance is DISTRIBUTED through the observers as the difference
between C-metric conservation and positive-subspace bookkeeping. This inverts the sign problem
that killed the strawman: the |II|^2 ledger is signed (W137), but B^dag B is positive
SEMIDEFINITE BY CONSTRUCTION, so the per-observer issuance is non-negative exactly where the
naive record-cost ledger failed to be. It also re-reads the program's standing wound: the
odd-cut leak (W120/W124/W134, parity-survives) stops being a unitarity sickness and becomes
the issuance channel itself; the W132 result "physical channels carry probability excess"
becomes the mechanism of distribution. Threshold structure inherited exactly: the toy model's
excess turns on at s > 2 M^2 (crossover computed in W132), maximal deficit magnitude 1/4 at
s = 4 M^2, tail M^4/s^2 -- the issuance switches on only above a record-formation threshold.
Assumption leg: pins the C-operator's existence AND gives it the physical role (the collective
ledger IS the C-metric; without C there is no "set" side at all); jointly with A13-1 it makes
the C-operator the load-bearing object of the whole framing. Gates: G4 pass (B^dag B is a
per-process structural object, not a structural constant read as a rate; the schedule mapping
to cosmic time is OPEN and named); G5 pass (no de Sitter identity anywhere in the
normalization; the total is the W136 declaration); G1/G2 not yet engaged (no cosmological
embedding exists -- honest gap, named as the story's missing half). Test: two-step. (1)
Structural, existing machinery: verify on the W132 toy that the per-channel excess is
non-negative for every open channel configuration and that C-metric conservation books it
exactly (a re-slicing of the 18/18 W132 test, cheap). (2) The bridge computation, new: map the
per-process excess to a cosmological schedule (excess rate per comoving volume given a channel
density) and confront G2 -- the story's life-or-death check, honestly not yet runnable because
the channel density needs the source-action normalization (H41-adjacent; named blocker).
Status: SURVIVES, ranked first on novelty + use of standing physics + sign correctness; its
missing cosmological half is the named price.

### Persona 14: index / K-theory rigorist

**A14-1 (eta-invariant flow at the W103 boundary).** F: q(sigma) = epsilon_0 *
d/dt [eta_APS(P_sigma D P_sigma)] with the APS boundary condition supplied by the W103
tail-quotient metric; the issuance is the flow of the boundary spectral asymmetry, records =
crossings. Distinct from A4-1 by WHERE the object lives: on the asymptotic boundary (the one
surviving typed interface), not the bulk slice. Assumption leg: pins the W103 slot as the
issuance port (the same landing spot C2's retreat needs, so A10-2 and A14-1 test each other).
Test: the same K2/W54 half-space machinery, computing the boundary eta of the compressed
operator under deformation (finite, new, tractable after A13-1's trace-density step). Status:
SURVIVES (mid-rank; shares A4-1's free-quantum weakness).

**A14-2 (the index-rigidity kill).** F candidate: "issuance rate = index density of the
observer's operator." KILLED-AT-GENERATION: a Fredholm index is deformation-invariant --
rigid under every continuous change of the section, the background, and the coupling -- so it
cannot flow, and a nonzero continuous issuance RATE can never be an index. Only the
NON-topological part of the spectral asymmetry (eta, spectral flow) can carry a rate, which is
why A4-1/A14-1 are phrased in eta and spectral flow. One-line theorem-grade kill that
disciplines the whole index-side lane.

---

## Part 2: internal scoring pass

### 2.1 Killed at generation (11 of 28), one line each

| story | kill | type |
|---|---|---|
| A1-2 Bach-invariant sourcing | FRW conformally flat: Bach = 0 at background, zero issuance where all of it is needed | truth (structural) |
| A2-2 (7,7) equipartition | W133: family fork empirically a permanent declaration; no test, no FIT pinned | sharpness |
| A3-2 block-spin schedule | normalization closes only at T_dS = the horizon identity (G5); unanchored elsewhere | novelty (G5) |
| A4-2 mod-3 quantization | the mod-3 Dai-Freed arena is empty (repo near-theorem); the counting invariant does not exist | truth (cited) |
| A5-1 SFH-tracking schedule | COMPUTED: max schedule slope 2.686 per e-fold at z~0 = 8.95x outside G2 | truth (G2, computed) |
| A6-2 3:2:1 generation split | type error: 3:2:1 is a deformation-sector split (W130/W137 C3), not a family split | truth (structural) |
| A7-2 coupling-as-rate | verbatim the killed B2 form (G4); f0/c_L/alpha_W all died exactly this way | truth (G4, cited) |
| A8-1 Q-curvature density | COMPUTED: curvature invariants scale as H^4; shortfall 5.9e120 | truth (dimensional, computed) |
| A8-2 Noether-charge normalization | reduces exactly to E_Lambda = T_dS S_dS/1.46; the G5 control, gate fires as designed | novelty (G5, control) |
| A11-1 equipartition law | it IS Padmanabhan 2010 (prior art); contributed as a new novelty sub-gate instead | novelty (prior art) |
| A11-2 C-field creation | matter-channel issuance re-enters the dead steady-state lane; kept as a demarcation marker | truth (historical) |

### 2.2 Survivor ranking (14 in-scope stories after the Section 0 reclassification of A1-1/A7-1/A12-1; ranked by sharpness of the assumption leg, testability, novelty vs the dS-relabeling line, and use of standing physics)

Tier 1 (the sweep's products):

1. **A13-2 Krein-excess distribution.** Novelty maximal (no dS identity anywhere; converts the
   odd-cut leak from sickness to mechanism); uses W132/W134 standing physics as load-bearing
   structure; the per-observer issuance is positive by construction where the strawman's
   ledger was signed; pins the C-operator's existence and physical role. Price: no
   cosmological embedding yet (G1/G2 unengaged; bridge computation named and blocked on the
   source-action normalization).
2. **A13-1 C-compression trace distribution.** The same collective/single split with a
   RUNNABLE cheapest test (W137 K2 + trace density, W54 machinery); pins three clauses of the
   C-operator's structure. Together with A13-2 it makes the C-operator the single object the
   whole framing backs into -- the strongest assumption-leg convergence in the sweep.
3. **A9-1 two-scale-band uniform issuance.** The sharpest NUMERICAL leg: the escape band and
   the H52 floor envelope intersect almost exactly (computed, both charts), giving a two-sided
   mu_DW localization [3.40, 4.71] meV and a mechanical falsifier (floor improvement ~1.4x
   kills the c = 2 chart). m2_eff not yet pinned (both central floors sit inside both bands)
   but pins mechanically as the floor moves. The only story that TAKES the two-scale escape.
4. **A3-1 Gibbs-measure AS selector** (carrying A7-1's schedule-free residue). Two independent
   legs force the same branch: the distribution measure exists only where the scalaron is
   healthy (computed sign at the pinned point: f0^2 = -1/8 < 0 on the native side), and the
   declared constant cannot be RG-anchored on the AF side (W130 strong-coupling crossing).
   Declaring the measured rate as the issuance input therefore PREFERS THE AS BRANCH -- the
   AF-vs-AS fork's first issuance-side selector, exactly the evidence-pack ranking criterion,
   and a pure back-out (no schedule anywhere). Test = the named W128 settling computation,
   which now carries this story's life as well.
5. **A5-2 observer-dilution exact-LCDM** (with A12-2 as its measurable refinement). Cleanest
   gates sheet; pins B_i = 0, f0 = 0 as the PREDICTION (the W136 natural point becomes
   forced); A12-2 supplies the missing observable (matter-weighted issuance implies DE
   clustering at perturbation level, G1b cleared with 4e12 headroom, computed).

Tier 2 (alive, mid-rank): A2-1 (causally graded distribution: the steelman of the signed
ledger; pins the signature's physical meaning; named covariance test),
A4-1 / A14-1 (spectral-flow and boundary-eta issuance: pin the W103 slot; free-quantum
weakness), A10-2 (the C2 boundary-balance rerun: the decisive computation that adjudicates
every boundary-supplied F), A10-1 (the two-postulate normal form all survivors instantiate),
A9-2 (horizon-patch partition: well-posed falsifier, weak leg, G5-adjacent in its uniform
limit).

Out of scope (Section 0 reclassification, not killed): A1-1 unimodular drift, A7-1
running-Lambda schedule (branch residue folded into A3-1), A12-1 pre-registered schedule
(pre-registration discipline retained as a binding requirement on all in-scope tests).

### 2.3 What the sweep converges on (the assumption leg, stated once)

Three independent personas' top stories back into the SAME object: **the C-operator.** A13-2
needs it to exist (the collective ledger IS the C-metric); A13-1 needs its compression to have
finite positive trace density (the distribution weight); A3-1 needs the branch on which its
positive subspace supports a measure (AS). The steelmanned framing, made maximally specific,
is therefore:

> DECLARED (never asserted): the issuance total is the KNOWN MEASURED rate,
> Q_tot = 3 H0 rho_L V_H = (3/2) Omega_L c^5/G = 1.027 Planck luminosities per Hubble volume
> (dimensionless 9 Omega_L = 6.16; W135 measurement = W136 boundary declaration, one number in
> two registers; w = -1 exact, B_i = 0, f0 = 0); its distribution through observers is
> F(sigma) = Q_tot * Tr(P_sigma eta_+ P_sigma)/Tr(eta_+), the compressed-C-metric trace
> density, with the per-process face q_obs = B^dag B (positive, threshold s > 2 M^2), existing
> on the AS branch.

Declaring THAT backs into: the C-operator's existence and section-independence (C1 clauses),
the trace-class/positivity of its compression (K2 sharpened), the AS branch of the E2 fork,
the spec pair B_i = 0 / f0 = 0 as predictions, and (via the independent A9-1 leg) the
two-scale band with its two-sided mu_DW localization. The cheapest next computation across the
whole sweep is the W137 K2 + trace-density check in the W54 free model (A13-1's test);
the sharpest external falsifier is the sub-mm floor (A9-1's, mechanical); the deepest is the
W128 native Hessian projection (A3-1's, already the repo's named settling computation).

### 2.4 Gates cleared, summary table (survivors, tier 1)

| story | G1 | G2 | G3 | G4 | G5 | G6 | standing physics |
|---|---|---|---|---|---|---|---|
| A13-2 Krein excess | not engaged (named gap) | not engaged (named gap) | pass (no mu_DW) | pass (per-process, not a constant-as-rate) | pass (no dS identity) | pass (no positive-ledger claim) | uses W132/W134 load-bearing; odd-cut leak re-read |
| A13-1 C-compression | pass (metric channel, uniform total) | pass (schedule untouched) | pass | pass (weight = structural distribution, allowed) | pass (weights are new dof) | pass | uses W132/W54/W137 C1 |
| A9-1 two-scale band | pass (uniform comoving) | pass (constant rho_iss) | pass BY CONSTRUCTION (scale = mu_emb; implied c_L under all caps) | pass (scale, not rate) | pass (mu_emb is a new dof) | n/a | takes W136's named escape; H24 cost counted |
| A3-1 Gibbs/AS | pass | pass | pass | pass (measure weight, not rate) | pass | n/a | W122/W123/W126 tachyon chain + W128 + W136 pinned point, all load-bearing |
| A5-2 dilution | pass | pass (exact LCDM = the prediction) | pass | pass | pass (N_obs(a) is the new dof) | pass (no Landauer claim) | W129 containment is the live test; W136 natural point becomes forced |

---

## Part 3: honesty section

- **Conditional register.** Every story is a conditional structure under the W136-style
  declaration. Nothing in this artifact asserts that dark energy is an issuance, that
  observers receive anything, or that any F is true. The deliverable is which declaration
  would be sharpest to make, and what making it pins.
- **The strawman is not rebuilt.** No story claims record-creation consumes positive issuance
  in the |II|^2 ledger; A13-2's positivity lives in B^dag B (a different object, positive by
  the W132 identity), and A2-1 embraces the signed ledger as the distribution law rather than
  denying it.
- **Named gaps.** A13-2 has no cosmological embedding (its G1/G2 confrontation is blocked on
  the source-action normalization; H41-adjacent). A13-1's trace density is proven finite only
  if the W54 exponential localization survives interactingly (open). A9-1 carries the H24
  one-scale revision as a counted cost. A3-1 dies if the W128 settling computation keeps
  the tachyonic sign on all UV-complete trajectories. A5-2's background prediction is null;
  its content is the distribution leg. The Planck-luminosity coincidence (1.027) carries
  W135's own O(1)-flag caveats and is used here as an anchor label, never as a derivation.
- **Tri-repo gating.** "Issuance" is used throughout as the local postulate label only (W136
  terminology gate); the issuance concept is owned by the temporal-issuance repo; no
  cross-repo identity claim is made (the R5 kill stands). No capability-measure claim is made
  (TaF owns measure). One-way rule respected: this artifact supplies GU-side structure that a
  TI-side story would have to survive, never support for TI.
- **W135** landed mid-wave and is cited per the framing correction (Section 0): its measured
  rate is the declared input, its null-structure separator and schedule kills are honored, and
  its two rate identities are reproduced as anchors N7/N8. Scoring of the W135/W136/W137
  proposals against the W138 battery remains the sibling wave's job; this sweep scored only
  its own 28 stories.
- **No canon / RESEARCH-STATUS / claim-status / verdict / posture change.** No FIT row moves
  in the spec (all pins are conditional on declarations). H41 stays unbuilt; H59 stays OPEN;
  the E2 AF-vs-AS fork stays CARRIED (A3-1 gives it an issuance-side selector, it does
  not resolve it).

**Artifacts:** this file + `tests/W140_steelman_scoring_checks.py` (16/16, exit 0; controls
first, W135 rate anchors N7/N8 included).
