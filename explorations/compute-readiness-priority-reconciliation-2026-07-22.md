---
title: "Compute readiness and priority reconciliation"
status: exploration
doc_type: run_receipt
date: "2026-07-22"
run_id: GUD-20260723T022801Z-compute-readiness-priority-reconciliation
---

# Compute readiness and priority reconciliation

## Bottom line

This Lane-A integrity cleanup removes two sources of artificial work: Lane 1's declared difficulty protection
is now executable, and `DE-AMP-DIAGNOSTIC` no longer requests an already-completed computation. No claim,
canon, verdict, publication posture, or GU-native construction grade moves.

## Selection repair

The ordinary portfolio score remains recorded for audit comparability. Lane 1 now uses a protected rule:

1. eligibility and dependency state are hard gates;
2. an explicit Joe or steward lead signal applies first among eligible Lane-1 items;
3. the Lane-1 scientific score omits effort and wall-risk deductions;
4. effort and wall risk remain capacity-planning metadata only.

This prevents a safe enumeration task from winning merely because the theorem work is hard. It does not make
`OPERATOR-END-PENCIL`, `B5-MIDDLE-DIFFERENTIAL`, or `CONSTRUCTION-SPACE-EXPLORATION` runnable while their
source-owned packets are absent. The latter's stale `hourly_eligible: true` flag is corrected to `false`.

## Completed computation reconciliation

The exact `DE-AMP-DIAGNOSTIC` swing requested by the portfolio was already executed in
`de-amplitude-audit-2026-07-20.md`. On 2026-07-22 it was reproduced from scratch with the shared research
environment using NumPy 2.5.1 and SciPy 1.18.0:

- 7 setup checks passed;
- 14 load-bearing computation checks passed;
- 4 confrontation checks passed;
- the two high-redshift-tail treatments differed by `3.29e-5` relatively;
- the canonical point returned `chi2_GU = 66.47`, `chi2_LCDM = 30.68`, and `Delta AIC = +35.79`;
- the certificate exited 0.

The item is therefore `RESOLVED_NO_GO` operationally and non-hourly. This records the already-banked
exploration-grade result; it does not promote it. Reopen only on corrected official likelihood data, materially
changed frozen GU-native structure, or a demonstrated defect in the certificate.

## Construction fork

This cleanup creates no new scientific object. The DE certificate continues to compare the program-native GU
two-component background against the standard Planck/DESI likelihood apparatus, with the absolute scale
explicitly imported. The operator channel remains program-native `D_GU` and source-gated; no Callias,
finite-grid, conventional boundary-condition, or other surrogate is promoted into the missing object.

## Validation

- `python -u tests/channel-swings/de_amplitude_audit_probe.py`: PASS, 7 setup plus 18 substantive checks.
- `python process_gates/research_portfolio_contract_audit.py`: required after the portfolio edit.
