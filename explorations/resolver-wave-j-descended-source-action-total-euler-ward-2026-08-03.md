---
artifact_type: exploration
created: 2026-08-03
title: "Wave J: local source-density and translation comparators expose the coset-curvature burden"
grade: "Exact local three-chart transport for a pointwise source-shaped scalar-density comparator; exact rational cyclic translation-coefficient, finite Green, and GL(2) covariance comparators; exact degree-typed quadratic-eddy and public/native coset-curvature witnesses; exact auxiliary Wave-I metric/Theta/density and Wave-H chosen-J/projector response witnesses. The actual native Shiab, monolithic B1 functional and Euler map, degree-correct port placement, native Ward/Green/domain, fermionic residual, total tangency, and physical no-leakage remain open."
named_gate: RESOLVER-WAVE-J-DESCENDED-SOURCE-ACTION-TOTAL-EULER-AND-WARD
gate_before: LOCAL_NONLINEAR_METX_THETA_RECONSTRUCTION_AND_RIESZ_PORTED_SPIN_FIXTURE
gate_after: LOCAL_SOURCE_DENSITY_AND_TRANSLATION_COMPARATORS_WITH_COSET_CURVATURE_OBSTRUCTION
route_disposition: REBASE
source_collision: SOURCE-CONFIRMS-B1-COEFFICIENTS-AND-TRANSLATION-DIRECTION; SOURCE-SILENT-NATIVE-SHIAB-PORT-ORDER-AND-TYPED-FERMION-RESIDUAL
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
third_lane_promoted: false
---

# Resolver Wave J: comparator layer and the unresolved port-order fork

## Result first

Wave J did **not** complete its advertised total-Euler/Ward gate. Hostile
review caught an invalid attempt to bracket two isolated components of one
projected one-form as though each component were independently in the
projector image. That inference has been retracted.

The repaired result is:

```text
POINTWISE SOURCE-SHAPED DENSITY TRANSPORT: EXACT LOCAL COMPARATOR
SOURCE 1/2,1/3 TRANSLATION ARITHMETIC: EXACT CYCLIC COMPARATOR
GREEN OWNER: LIVE FINITE COMPARATOR
WARD OWNER: GL(2) LOCAL-COVARIANCE COMPARATOR ONLY
QUADRATIC EDDY: LIVE OMEGA2 GRADE-TWO COEFFICIENT
PUBLIC COSET CURVATURE: LIVE NATIVE GRADE-SIX COEFFICIENT
PROJECT-BEFORE / PROJECT-AFTER VARIATION: OPEN
BOSONIC AND TOTAL PORT TANGENCY: OPEN / NOT TESTED
NATIVE SHIAB / MONOLITHIC B1 / NATIVE WARD-GREEN-DOMAIN: OPEN
```

The strongest new exact obstruction is degree-correct. For public connection
directions

\[
 X=i e_0,\qquad Y=i e_{45678},
\]

the chosen local Reynolds map satisfies

\[
 R_J(X)=R_J(Y)=0,
 \qquad R_J([X,Y])=-2e_{045678}\ne0.
\]

Thus the shortcut

\[
 F_{R_J\widetilde A}=R_J(F_{\widetilde A})
\]

is false unless the reduced construction retains the coset-curvature term
`R_J(m wedge m)`. This refutes a naive projection of a public connection
followed by ordinary curvature formation. It does **not** decide between a
corrected reduced `(a,m)` action and a full-public action followed by a
degree-correct Euler port.

The gate therefore rebases to the actual missing object:

```text
RESOLVER-WAVE-K-NATIVE-SHIAB-MONOLITHIC-B1-VARIATION-AND-PORT-PLACEMENT
```

## Plain English

We have tested the plumbing around the action, not yet installed the real
action.

The source's one-half and one-third coefficients pass an exact
noncommutative differentiation test. A pointwise source-shaped density obeys
the three-chart transport rules built in Wave I. Separate small models show
that a boundary term and an inhomogeneous gauge response cannot be ignored.
Those are useful construction requirements, but they are not the native GU
Shiab, Euler equation, Ward identity, or analytic domain.

Wave H already established that the public-to-native reduction is not a Lie
homomorphism and that erased directions can return as native curvature. Wave
J refines that warning with an exact returned grade-six coefficient aligned
with the reference `Psrc` coefficient support. Any source action we build must
carry that nonlinear information. What remains unknown is where the rank-252
port belongs. Only a degree-correct `Omega2 -> Omega13` Shiab and a monolithic
variation can answer that.

## 0. Layer 0: objects that must stay separate

| phrase | Wave-J object | not identified with it |
|---|---|---|
| source action | Weinstein's displayed bosonic `I_B^1` target | N1's bulk-plus-defect action or a complete published GU action |
| descended comparator | a pointwise scalar-density fixture assembled from already-composed one-form channels | the displayed `I_B^1` or a native top-form action |
| source Shiab | an unbuilt density-dual `Omega2(adP) -> Omega13(ad*P)` map | the rank-252 `Psrc:Omega1 -> Omega1` port |
| translation comparator | exact cyclic differentiation with geometry fixed | the native B1 Euler covector |
| Ward comparator | an infinitesimal `GL(2)` cyclic-trace covariance fixture | the native tilted-Spin Ward identity or `Xi=D_omega Upsilon` |
| Green comparator | a two-component first-order integration-by-parts fixture | the B1 preboundary form or common analytic domain |
| `Psrc` | a candidate native output/tangency port | a connection homomorphism or Shiab operator |
| moving responses | separate Wave-I and Wave-H auxiliary first jets | one joined derivative of the displayed source action |

The raw source first leg remains in `C*` with transition `O^-T`; the inherited
projector acts after the Riesz map in `C` with transition `O`. `A0` is
Levi-Civita-derived in the source, but its exact observer/Zorro/Y-side owner
remains uncertain. P1/P2/P3 are unchanged and unused.

## 1. Specialist preregistration and hostile correction

Three lenses were used before and after construction:

1. Clifford/representation theory required public/native carrier typing,
   the `R_J(m wedge m)` correction, and a degree audit.
2. Source/differential geometry required that `source action` mean the
   displayed bosonic `I_B^1`, not an invented completion.
3. Variational/PDE analysis required Green, Ward, moving-background, and
   project-before/project-after branches to be tested separately.

The hostile pass found the decisive defect in the first draft: the whole
one-form `Psrc(T)` was projector-fixed, but its isolated coindex components
were not separately projector-fixed. Their bracket could not establish
nonclosure of `im Psrc`. The corresponding claims of rank-252 nonclosure,
bosonic transverse Euler failure, and forced post-variation projection were
removed. This is exactly the two-sided review rule in action: the probe had
to type-check both the summary's inference and the object being defended.

## 2. Local pointwise density transport

On Wave I's three-chart fixture, the probe transports a raw source one-form
and three already-composed one-form channels. Pointwise it evaluates

\[
 \ell_i=\langle T_i,F_i+\tfrac12D_i+\tfrac13Q_i\rangle_\eta
       +\tfrac15\langle T_i,T_i\rangle_\eta,
\]

where in this comparator `F= Psrc(T)`, `D=2Psrc(T)`, and `Q=3Psrc(T)`.
These names do not denote the native curvature, covariant derivative, or
quadratic eddy of `I_B^1`.

The common scalar is

\[
 -\frac{1404431087}{642555375},
\]

and the absolute densities are `8`, `128`, and `10368`. Pairwise and triple
transport obey the fourteen-dimensional Jacobian law. Dropping the
metric-bundle Jacobian fails, as does applying the raised `O` law to the raw
`C*` source.

This proves a conditional local transport lemma for an already-composed
scalar-density fixture. It does not construct or descend `I_B^1`.

## 3. Exact coefficient/transgression comparator

For exact rational noncommuting matrices, define

\[
 I_{a,b}(T)=\operatorname{tr}\left[T\{F+a(BT+TB)+bT^2\}\right]
  +\frac{\kappa_1}{2}\operatorname{tr}(T^2).
\]

Cyclicity gives

\[
 \delta I_{a,b}[\alpha]
 =\operatorname{tr}\left[\alpha\{F+2a(BT+TB)+3bT^2+\kappa_1T\}\right].
\]

At `(a,b)=(1/2,1/3)`, the linear and quadratic translated-curvature weights
become one. The exact derivative is `157/7`, and the two channels are
independent. The planted points `(1,1/3)` and `(1/2,1/2)` fail.

This independently verifies the source coefficient arithmetic conditional
on the cyclic/transgression identities. It does not include the native
Shiab, coadjoint/Krein adjoints, or full B1 Euler operator.

## 4. Green and covariance comparators

A separate two-component first-order fixture yields

```text
direct derivative = 2387/360
bulk Euler         = 2567/360
boundary           = -1/2
```

so a preboundary owner is genuinely live. This is not B1's Green form.

A separate nonconstant infinitesimal `GL(2)` fixture has zero cyclic-trace
variation when

\[
 \delta B=[B,\xi]+d\xi
\]

and all other fields move covariantly. Omitting `d xi` gives `-7/30`. Wrong
eddy coefficients remain covariant, so covariance cannot select `1/2,1/3`.
This is a local gauge-covariance proxy, not a native Ward or Noether theorem.

## 5. Degree-correct nonlinear burdens

### 5.1 The quadratic eddy changes degree

For one actual `Psrc`-fixed one-form, the coindex-`01` quadratic coefficient
is

\[
 [T_0,T_1]=-\frac{2}{81}e_{01}\ne0.
\]

It is an `Omega2` grade-two coefficient. The probe never feeds it back into
the `Omega1` projector. Its meaning is a construction burden: a real
curvature/Shiab/output map must say how this degree-two term contributes to
the degree-thirteen Euler dual before closure or tangency can be judged.

### 5.2 The public coset returns native curvature

The exact pair `X=i e0`, `Y=i e45678` proves that the local Reynolds map is
not a Lie-algebra homomorphism. The mandatory reduced curvature has the form

\[
 \mathcal F_J=F_a+R_J(m\wedge m)=R_J(F_{\widetilde A})
\]

when the remaining hypotheses are met. The returned grade-six coefficient
intersects the reference coefficient support used by the rank-252 port, but
no degree-correct Euler map has yet been applied. Therefore:

- `F_(R_J A)` without the coset correction is killed;
- a corrected reduced `(a,m)` action remains possible;
- full-public action followed by a typed residual port remains possible;
- restrict-before versus project-after variation remains open; and
- bosonic and total port tangency remain open and untested.

## 6. Moving geometry is live but not joined

The actual Wave-I metric fixture satisfies

\[
 \dot G_Y=\dot\Theta^T C\Theta+\Theta^T\dot C\Theta
           +\Theta^T C\dot\Theta.
\]

Its density response is `-99/32`, and the normalized auxiliary response is
`-72357157/10653696`. Freezing `Theta` or density changes the answer. This
varies tautological vertical `h` while observer `Gamma` is frozen; it is not
an observer-metric or `D_g Gamma` variation.

Wave H's separate chosen-`J/Psrc` first jet also remains live. A frozen
auxiliary has zero derivative while the moving auxiliary has derivative
`-4`. That number is not a source-action derivative.

These fixtures show which dependencies a future monolithic variation must
own. They do not assemble those dependencies in one B1 functional.

## 7. Seven-axis register

| candidate | L0 | L1 | L2 | L3 | L4 | L5 | L6 | L7 | first falsification |
|---|---|---|---|---|---|---|---|---|---|
| naive `F_(R_J A)` reduced action | connection reduction distinct from curvature reduction | local Wave-H carrier | local chosen J | no native Shiab | none | exact coset pair | none | indefinite | **killed:** omits `R_J(m wedge m)` |
| corrected reduced `(a,m)` action | connection/coset/curvature typed | local | open | native Shiab open | open | candidate | open | open | construct monolithic B1 and vary |
| full-public B1 then typed Euler port | Shiab and Psrc distinct | local | open | native Shiab open | open | candidate | open | open | construct monolithic B1 and vary |

No candidate is promoted merely because the naive shortcut failed.

## 8. Source collision

| claim | collision |
|---|---|
| displayed `I_B^1`, `1/2,1/3`, translation-direction variation | `SOURCE-CONFIRMS` — 2021 draft equations 9.1--9.7 |
| bosonic and fermionic residuals share an Euler arena | `SOURCE-CONFIRMS` — equations 9.18--9.20 |
| `Xi=D Upsilon` is the Ward identity | `SOURCE-CORRECTS` — it is a redundant/cohomological equation, not the identity tested here |
| `Psrc` is the source Shiab | `SOURCE-SILENT / TYPE-REFUTED` — domains and degrees differ |
| source selects restrict-before or project-after variation | `SOURCE-SILENT` in checked material |
| complete native fermion action and adjoint residual | `SOURCE-SILENT/OPEN` in checked material |

## 9. Validation and claim boundary

The repaired probe passes:

```text
18 exact + 5 source + 19 type + 10 planted = 52 PASS
```

It does not build or claim:

- the native density-dual Shiab;
- the displayed monolithic `I_B^1` on the Wave-I carrier;
- a B1 Euler covector or total geometry Euler covector;
- bosonic or total rank-252 tangency;
- a native Ward/Noether identity or B1 Green form;
- a typed source fermion action or adjoint residual;
- a common closed/right-quaternionic/Krein domain;
- physical observation/no-leakage; or
- a VEV, mass, stationarity, anomaly, index, generation count, or cosmology.

Curt remains formally separate. `TG-1 AND TG-2 AND TG-3` remains not
promoted. No protected claim, canon verdict, public posture, or lane changes.

## 10. Next gate

```text
RESOLVER-WAVE-K-NATIVE-SHIAB-MONOLITHIC-B1-VARIATION-AND-PORT-PLACEMENT
```

In order:

1. construct the actual native density-dual `Omega2 -> Omega13` Shiab on the
   same Wave-I cocycle;
2. build one monolithic bosonic `I_B^1` with actual `F_B`, `D_B T`,
   `[T,T]`, and source coefficients `1/2,1/3`;
3. compare its direct and owner-by-owner first variations, including its
   same-action metric/Theta/J/density dependencies;
4. test corrected reduced `(a,m)` and full-public/projected-residual routes
   with degree-correct port placement;
5. derive the native Ward/Noether contraction and preboundary form; and
6. only after those pass, construct the typed fermionic residual and test
   total tangency on one common domain.
