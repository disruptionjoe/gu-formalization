---
artifact_type: exploration
status: exploration (TEAM EMERGENCE, W151; 5 personas inline, one context, no sub-agents; deterministic test)
created: 2026-07-14
wave: W151
hypothesis: "W151 -- Joe's strongest structural substrate conjecture: records are held in the Y14 substrate and COMMUNICATED THROUGH LIGHT (causal order); TIME is the growing set of mutually-consistent shared records across the global/regional/individual capability tiers (the number/volume leg); and GENERAL RELATIVITY and the SPEED OF LIGHT should POP OUT of this record/light/consistency structure. CRUX: does GU make the emergence NATIVE (forced by Y14 = Met(X4)) rather than a generic import, and does the record-thermodynamic route reproduce GU's already-computed native Einstein coefficient (W126/W130 a1 = 1/3, split 3:2:1)?"
title: "VERDICT: HALF-NATIVE, with a sharp two-route split. (1) The causal/CONFORMAL leg is GU-NATIVE: 'records communicated by light' = the causal order, and Malament's theorem (causal order -> conformal metric + topology + differentiable structure + a maximal signal speed) is realized IN GU by the W131 gimmel null cone, whose (9,5) = base (3,1) + DeWitt fiber (6,4) split makes the observed X4 light cone the (3,1) base factor -- so c-emergence is THEOREM-BACKED and GU-LOCALIZED (though the VALUE of c is not a substrate prediction; c = 1 by convention, the content is cone existence + Lorentzian signature). (2) The number/VOLUME leg (BLMS 'order + number = geometry') IS Joe's 'time = consistent additional records': counting = volume = the conformal factor Malament drops; positive control reproduces order->[g]-only and number->volume on a toy causal diamond. Lorentz invariance survives via sprinkling (no preferred frame), at the price of the standing LIV bound. (3) The DYNAMICS leg (Jacobson equation-of-state on the measurement-gated capability-tier boundary as a LOCAL causal horizon) is a GENERIC IMPORT that AGREES with GU's own geometric route (alpha, W126/W130) on the SIGN of the Einstein term (both attractive: alpha a1 = +1/3 > 0, beta entropy density eta = +1/4 > 0) but does NOT reproduce the MAGNITUDE 1/3 (beta FIXES the Einstein coefficient by DEFINING G via eta, so the number is normalization, not a prediction: NUMBER-MATCH NOT-YET-COMPUTABLE) and CANNOT produce the 3:2:1 sector split (a GU-specific literal-graph object with a longitudinal residue the covariant record route lacks). (4) R^2-SECTOR: genuine CONFLICT, informative -- the record/thermodynamic route famously gives PURE EINSTEIN (Raychaudhuri focusing is LINEAR in R_ab; area-entropy Clausius yields G_ab + Lambda g_ab with NO R^2; f(R) needs a non-equilibrium entropy-production term, Eling-Guedens-Jacobson 2006), whereas GU's |II|^2 geometry FORCES the tachyonic R^2 scalaron (a2 = -1/9, W126/W130). The record route would truncate GU to Einstein and explain the tachyon away; GU's own geometry does not permit that. The departure LOCALIZES exactly there: the record route is an equilibrium/area-entropy (leading-order) statement blind to the higher-derivative structure GU's exact induced functional carries. (5) NOVELTY: at the GR-emergence level the bare chain is a RELABEL-OF-{Malament 1977 + BLMS 1987 + Jacobson 1995}, and Padmanabhan equipartition + the W138 G5 de Sitter relabel gate bite; the ONE GU-SPECIFIC PIN is structural not numerical -- the measurement-gated Y14 -> X4 projection identifies the Jacobson horizon with the capability-tier/firewall boundary AND forces the counting dimension to collapse 14 -> 4 (the W146 shadow-map constraint) AND localizes the cone as the (3,1) base of the (9,5) gimmel cone; the only numerical hook (everpresent-Lambda subleading structure) is the W145-W149 siblings' object and is G5-degenerate at leading order."
grade: "exploration / conditional register throughout. METHOD AND ANALYSIS plus positive-control computation, NOT evidence. The real-physics chain (Malament / BLMS / Jacobson / Padmanabhan / Sorkin everpresent-Lambda) is PORTED and labelled as such; the native GU Einstein data (W126 F(R) = 2 + R/3 - R^2/9; W130 split 3:2:1, c_R = -4/9) is RECORDED in-repo and regression-pinned here, not re-derived. The two-route comparison is stated as sign-MATCH / R^2-MISMATCH / magnitude-NOT-YET-COMPUTABLE with the exact reason for each. Deterministic test tests/W151_gr_and_c_emergence_from_records.py (25 checks, exit 0), positive controls first: Malament order->[g]-only + number->volume on a toy 1+1 causal diamond; the Jacobson null-vector lemma checked numerically in d=4; the W126/W130 coefficients regression-pinned. NO forbidden target assumed/inserted. NO canon / RESEARCH-STATUS / claim-status / verdict / posture change. Tri-repo gating honored (capability measure = TaF; record/finality/time semantics = temporal-issuance + time-as-finality; GU owns the emergent-metric MATH only; no cross-repo identity asserted). Zero em dashes in paper-facing text."
depends_on:
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W131-covariant-operator-y14-2026-07-14.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - explorations/W148-substrate-sweep-philosophy-2026-07-14.md
  - explorations/sorkin-causal-set/sorkin-causal-set-axis-note.md
  - canon/firewall-boundary-hypothesis.md
  - tests/W146_everpresent_lambda_substrate_checks.py
scripts:
  - tests/W151_gr_and_c_emergence_from_records.py
external_refs:
  - "Malament, J. Math. Phys. 18 (1977) 1399 -- the causal order determines the conformal metric, topology, and differentiable structure (not the conformal factor)"
  - "Bombelli, Lee, Meyer, Sorkin, Phys. Rev. Lett. 59 (1987) 521 -- causal sets; order + number = geometry"
  - "Jacobson, Phys. Rev. Lett. 75 (1995) 1260, gr-qc/9504004 -- Einstein equations as an equation of state, delta Q = T delta S"
  - "Eling, Guedens, Jacobson, Phys. Rev. Lett. 96 (2006) 121301 -- non-equilibrium thermodynamics of spacetime; f(R) needs an entropy-production term"
  - "Padmanabhan, Rep. Prog. Phys. 73 (2010) 046901 -- emergent gravity, equipartition (PRIOR ART / novelty sub-gate)"
  - "Verlinde, JHEP 04 (2011) 029 -- entropic gravity (more contested; flagged)"
  - "Ahmed, Dodelson, Greene, Sorkin, Phys. Rev. D 69 (2004) 103523 -- everpresent Lambda ~ 1/sqrt(N) (the siblings' wire)"
  - "Hawking, King, McCarthy, J. Math. Phys. 17 (1976) 174 -- causal reconstruction (Malament companion)"
---

# W151 -- GR and c emergence from records, light, and consistency

**Role.** Joe's strongest structural conjecture in the substrate arc: records are held in the
Y14 substrate and communicated through light; time is the growth of the mutually-consistent
shared record set across the global, regional, and individual capability tiers; and general
relativity and the speed of light should fall out. The substrate arc's concurrent families
(W145-W149) take the number-to-Lambda leg (everpresent-Lambda, `delta Lambda ~ 1/sqrt(N)`).
W151 takes the complementary GEOMETRY and DYNAMICS leg: the Malament -> causal-set -> Jacobson
chain, and the sharp GU-native test against GU's own already-computed native Einstein
coefficient (W126/W130). Five personas inline, one context, no sub-agents. Deterministic test
`tests/W151_gr_and_c_emergence_from_records.py` (25 checks, exit 0).

Two things are kept strictly separate throughout, following the W138 discipline: whether the
emergence WORKS (does GR pop out) and whether it is NATIVE (forced by `Y14 = Met(X4)`, versus a
generic import that any causal substrate would supply). The headline is that they split cleanly
across the three legs.

## 0. The chain, stated with what each step gives

| step | input | gives | does NOT give |
|---|---|---|---|
| **Malament 1977** (PORTED) | the causal order (which records light can reach) | conformal metric `[g]` + topology + differentiable structure + a maximal signal speed | the conformal factor `Omega` (scale/volume/proper time) |
| **BLMS 1987** (PORTED) | order + NUMBER (element/record count = volume) | the conformal factor `Omega` -> the full Lorentzian `g` | dynamics (which `g` is realized) |
| **Jacobson 1995** (PORTED) | area-entropy `S = eta A` + Unruh `T` + Clausius on a local causal horizon | Einstein's equations `G_ab + Lambda g_ab = 8 pi G T_ab` (the DYNAMICS) | higher-derivative (`R^2`) terms at equilibrium order |

Joe's conjecture maps onto this chain exactly: light-communication = the causal order (Malament
leg); `time = consistent additional records across observers` = the number/volume leg (BLMS);
the measurement-gated capability-tier boundary = the local causal horizon (Jacobson leg). The
rest of this note assesses each leg for NATIVITY and runs the two-route Einstein test.

---

## 1. Persona 1 -- causal-structure / GR theorist: what the causal order gives, exactly

**Malament, precisely.** For a past- and future-distinguishing spacetime, a bijection preserving
the causal precedence relation is a smooth conformal isometry. So the causal order fixes the
manifold topology, its differentiable structure, and the metric UP TO a positive conformal factor
`g -> Omega^2 g`. The light cone is the boundary of the causal order, so a maximal signal speed is
theorem-backed: `c` is the causal-order boundary, not an added postulate.

**What it does not give.** The conformal factor. Proper time and proper distance, the volume
element, the scale at which the geometry sits, are all invisible to the order alone. This is the
precise sense in which "records communicated by light" gives you the light cones and nothing
metric beyond them.

**Positive control (test PC1, M1).** On a toy 1+1 causal diamond with null coordinates
`u = t - x, v = t + x`, precedence is `u_q > u_p AND v_q > v_p`. Applying any monotone increasing
reparametrisation `u -> f(u), v -> g(v)` (exactly the conformal transformations of
`ds^2 = -du dv`) leaves the causal-order relation matrix IDENTICALLY unchanged (checked on 500
sprinkled points, 62261 relations, exact array equality). So the order sees only `[g]`. This is
Malament's statement made concrete.

**The GU-native question for this leg.** Is the causal order in GU the gimmel structure, or an
imported causal set laid on top? W131 answers: the covariant operator's characteristic variety at
every point of curved Y14 is exactly the gimmel null cone (signature (9,5) verified pointwise on
the Lorentzian locus; frame residual 1.8e-15). So the "light" in `Y14 = Met(X4)` propagates on the
gimmel null cone natively, and the causal order that Malament turns into conformal geometry is the
gimmel order, not a generic sprinkle. **This leg is GU-NATIVE**: the conformal-structure output is
the (9,5) gimmel conformal structure, computed in W131, not an import.

## 2. Persona 2 -- causal-set theorist: number = volume = Joe's consistent-record-time

**BLMS, precisely.** Order gives `[g]` (Malament). NUMBER gives the missing factor: in a Poisson
sprinkling at density `rho`, the expected count in a region equals `rho x Vol`, so counting
elements recovers the spacetime volume, hence the conformal factor. Order + number = the full
Lorentzian metric. This is the "order + number = geometry" slogan (Sorkin 1991), and the repo
already carries the causal-set axis as a scoped program (`sorkin-causal-set-axis-note.md`).

**Joe's time IS the number leg.** `time = the consistent additional records across the global,
regional, and all individual observers` = the growth of the count = the volume element. Joe's
addition beyond bare BLMS is the word CONSISTENT: not any count, but the mutually-agreed count
across the capability tiers. That agreement condition is what Persona 4 examines.

**Positive control (test PC1, M2/M3).** With density defined per unit Minkowski volume
(`dt dx = (1/2) du dv`), an Alexandrov interval's cardinality divided by the density recovers its
Minkowski volume (`count = 3941` vs `rho_mink x Vol = 4000`, inside 4 sigma Poisson). Combined with
M1: the same order is shared by `g` and `Omega^2 g` (M1) while the count differs (M2), so order
alone fixes `[g]` and order+number fixes `g`. The toy diamond reproduces the reconstruction.

**Lorentz invariance and the hard constraint.** Poisson sprinkling has no preferred frame: the
process is statistically Lorentz-invariant, a genuine advantage over lattice discretisations. The
price is the standing LIV bound (swerve/diffusion and photon-timing constraints push the
discreteness scale to near Planck). The Hauptvermutung (that a causal set has an essentially unique
smooth approximant) is proven only in cases and conjectural in general, so the number->geometry
reconstruction is faithful-in-the-manifold-like-regime, not a theorem in full. Stated, not hidden.

**Native or imported?** BLMS counting is a GENERIC construction: any causal substrate supplies it.
`Y14 = Met(X4)` does not force the Poisson sprinkling. What GU adds is WHAT is counted: records in
Y14 over a base point `x` are values of the fiber metric at `x`, so the count is a count of metric
records, and the sibling W146 finds the counting DIMENSION must collapse from 14 to 4 for the
everpresent amplitude to land (the shadow-map constraint). That dimension-collapse is a GU-specific
pin on the number leg; the counting mechanism itself is imported. **This leg is IMPORTED, with one
GU-specific rider (14 -> 4).**

## 3. Persona 3 -- thermodynamic / emergent-gravity theorist: the Jacobson leg and the two-route test

**Jacobson, precisely.** On a local Rindler causal horizon through any point, take heat
`delta Q = integral T_ab chi^a dSigma^b` (matter energy flux across the horizon), entropy
`S = eta A` with `A` the horizon area, and Unruh temperature `T = hbar kappa / 2 pi`. Demanding the
Clausius relation `delta Q = T delta S` for ALL local Rindler horizons forces
`G_ab + Lambda g_ab = 8 pi G T_ab` with `eta = 1 / (4 G hbar)`. GR pops out of the thermodynamics of
records crossing a local causal horizon.

**Joe's measurement-gated boundary IS the local horizon.** The capability hierarchy GLOBAL ->
REGIONAL -> INDIVIDUAL, where an individual must OBSERVE a record to promote it into the regional
measured component, is precisely a local causal horizon: unpromoted Y14 records sit behind the
observer's capability boundary, and promotion is the heat flux across it. In canon terms this is the
firewall boundary (`firewall-boundary-hypothesis.md`), read as the interface where individual
observation promotes hidden Y14 records into shared regional reality. The Jacobson horizon and the
firewall/capability boundary are the same object under this reading.

**Positive control (test J).** The load-bearing step of Jacobson 1995 is the null-vector lemma:
if `S_ab k^a k^b = 0` for all null `k`, then `S_ab = phi g_ab`. Checked numerically in d=4: the
kernel of the null-null contraction map on symmetric tensors is exactly one-dimensional and equals
`span{eta}` (residual < 1e-8). The Raychaudhuri focusing term that feeds `delta A` is LINEAR in
`R_ab k^a k^b` (curvature power 1), and `eta = +1/4 > 0` gives an ATTRACTIVE Einstein term.

### The GU-native test (the sharpest deliverable)

GU already has a FIRST route to emergent GR, from GEOMETRY: the induced `|II|^2` action
(W126) reduces on the scalar slice to `F(R) = 2 + R/3 - R^2/9`, and the native quadratic operator
(W130) has an attractive Einstein term with `a1 = +1/3`, sector split
`gamma_TT : gamma_phi : gamma_slice = 3 : 2 : 1` (all positive), and `c_R = -4/9`. Call this route
alpha. Joe's conjecture proposes route beta: Malament conformal + record-count conformal factor +
Jacobson equation-of-state. The test: does beta reproduce alpha's `a1 = 1/3` (or the 3:2:1 split)?

Result, decomposed by what is comparable (test T):

- **SIGN: MATCH.** Route alpha gives `a1 = +1/3 > 0` (attractive, H25-consistent); route beta gives
  `eta = +1/4 > 0` (attractive). Both routes agree the Einstein term is attractive. This is not
  vacuous: a negative entropy density would give repulsive gravity, and a wrong-sign induced `a1`
  would too. The two independent routes agree on the sign.
- **MAGNITUDE: NOT-YET-COMPUTABLE.** Route beta does NOT independently predict the number `1/3`. It
  fixes the Einstein coefficient by DEFINING Newton's constant through the horizon entropy density,
  `1/(16 pi G) <- eta`. The "1/3" of route alpha lives in the induced-functional (DeWitt/`mu_DW`)
  normalisation, which is exactly the normalisation Jacobson's route sets by hand rather than
  derives. The two routes normalise the Einstein coefficient by different conventions with no
  bridge computed. The bridge that WOULD make the number comparable is a native computation of `eta`
  from the Y14 record-count entropy = the gimmel horizon area of W131; that is not done here. So:
  MATCH-ON-SIGN, MAGNITUDE-MISMATCH-BY-CONVENTION, honestly labelled NOT-YET-COMPUTABLE, with the
  exact missing object named (native `eta` from the gimmel area).
- **3:2:1 split: GU-SPECIFIC, absent from beta.** Route beta produces the covariant Einstein tensor,
  whose linearisation has the standard graviton channel structure. GU's 3:2:1 split includes a
  non-covariant LONGITUDINAL residue confined to the diffeo-gauge direction (W130, a computed
  property of the literal-graph construction). The record route matches GU's COVARIANT projection
  and is blind to the longitudinal residue. So the 3:2:1 object is GU-specific; beta cannot supply
  it.

**Padmanabhan prior-art honesty.** Padmanabhan's equipartition / emergent-gravity program derives
the field equations from horizon degrees of freedom and equipartition, and the prior substrate wave
already flagged it as PRIOR ART and a novelty sub-gate. The Jacobson leg here inherits that: a bare
record-thermodynamic re-derivation of Einstein is Padmanabhan/Jacobson prior art, not new. Verlinde's
entropic-gravity form is noted and left flagged as more contested.

### The R^2-sector verdict (a real bonus, honest either way)

The record/thermodynamic route famously gives PURE EINSTEIN, not higher-derivative gravity. The
mechanism is in the test: the Raychaudhuri area change is LINEAR in `R_ab`, so area-entropy Clausius
yields `G_ab + Lambda g_ab` with NO `R^2` source (test J2, J4: `a2_beta = 0`). Getting `f(R)` out of
the equation of state requires a curvature-dependent Wald entropy AND an internal entropy-production
term, i.e. it becomes NON-equilibrium (Eling-Guedens-Jacobson 2006). GU's route alpha, by contrast,
FORCES the `R^2` sector: `a2 = -1/9`, the tachyonic scalaron (W126/W130), an exact feature of the
`|II|^2` induced functional. So (test T2) the two routes MISMATCH on the `R^2` sector:
`alpha a2 = -1/9 != 0`, `beta a2 = 0`.

This is informative, and honest either way:

- **Reading A (no real conflict).** The record route is an equilibrium/area-entropy (leading-order)
  statement, coarser than GU's exact induced functional; the `R^2` sector is subleading structure
  the record route does not resolve. The routes agree where they overlap (attractive Einstein) and
  the record route is simply blind above that order.
- **Reading B (a would-be conflict, which loses).** If one took the record route as FUNDAMENTAL, it
  PREDICTS pure Einstein and hence predicts GU's tachyon is an artifact. But W126/W130 already showed
  the tachyon is a robust native feature (exact at tree level, hardened by W123). So reading B would
  have the record route contradicting GU's own geometry, and GU's geometry is the computed side.
  Reading A is the honest one.

Either way the localisation is the same: the record/thermodynamic route does NOT generate the
tachyon; GU's geometry does. The departure between "records give GR" and "GU's own geometry gives GR"
lives exactly at the `R^2` sector, and it is an order-of-approximation departure, not a sign or
structural contradiction in the Einstein term.

## 4. Persona 4 -- relational-time theorist: consistent record accretion as an emergent time

**The construction.** Joe's time is not a coordinate; it is the growth of the mutually-consistent
shared record set across the capability tiers. In relational terms (Rovelli, Barbour), this is a
correlation-defined time: the ordering parameter is "how many records have been promoted and agreed",
and the "agreed" clause is a consensus condition across the tiers GLOBAL -> REGIONAL -> INDIVIDUAL.
Two features make this a well-defined emergent time parameter at the level of this analysis:

1. **Monotone growth.** Promotion is one-way (a measured record does not un-measure), so the
   consistent-record count is monotone, giving an arrow. This is the number leg of Section 2 read as
   a clock: the volume element IS the tick.
2. **Consensus = section-consistency in GU.** In `Y14 = Met(X4)`, the promoted records must assemble
   into a single X4 metric (one section, single-valued). "Consistent across all observers" is
   "consistent across all sections restricted to what has been promoted". So Joe's consistency
   condition has a native GU reading: the promoted sub-bundle must be a genuine partial section.

**The honest boundary (tri-repo GATED).** "Record", "finality", and "consistent accretion" are the
semantics owned by the temporal-issuance and time-as-finality repos; the capability MEASURE is TaF's.
This note does NOT claim any TaF or time-as-finality quantity IS a GU quantity. The relational-time
reading is stated as a GU-local postulate label only, and the cross-repo identity (does the
consistent-record clock EQUAL the TaF finality measure) is left gated, exactly as W148 left it.

**Does consistency FORCE Einstein?** Here is the load-bearing negative. Record-consistency is a
KINEMATIC constraint: it forces the promoted metric to be a well-defined single-valued section. It
does NOT by itself force that section to SOLVE Einstein's equations. The dynamics (which section is
realized) comes from the action, route alpha (the `|II|^2` EL equations) or route beta (the Jacobson
equation of state), not from consistency. So the honest statement: consistency across sections forces
a well-defined emergent metric and a monotone emergent time, but the field EQUATION on that metric is
supplied by the dynamics, which GU already has natively (alpha) and which the record route (beta)
re-supplies generically. Consistency is the arena, not the law.

## 5. Persona 5 -- adversarial skeptic: is this a GU-specific pin, or a relabel?

**Steelman IT-IS-A-RELABEL, at full strength.** Emergent gravity is non-unique and notoriously hard
to falsify. The entire chain here is prior art: Malament 1977 (order -> conformal), BLMS 1987 (order +
number -> metric), Jacobson 1995 (thermodynamics -> Einstein), Padmanabhan (equipartition),
everpresent-Lambda (Sorkin). "Records communicated by light, counted into volume, thermalised on a
horizon into Einstein" is a re-narration of that literature in Joe's vocabulary. At the GR-emergence
level, W151 delivers NO number the standard program does not already have: the Einstein sign is
Jacobson's, the volume-from-count is BLMS's, the cone is Malament's. The W138 gates confirm the
danger: G5 (any normalisation reducing to `rho_L V_H ~ T_dS S_dS` is a de Sitter relabel; the test
reproduces `1.46 = 1/Omega_L`), and the Padmanabhan equipartition sub-gate. On the strongest reading,
**the record route re-derives known GR in new words.** This must be reported, and it is the honest
verdict for the bare chain.

**Where the relabel charge does NOT fully land -- the one GU-specific pin.** The steelman is answered
only if GU pins an object the bare program lacks. There is exactly one, and it is STRUCTURAL not
numerical:

- The measurement-gated `Y14 -> X4` projection IDENTIFIES the Jacobson horizon with the
  capability-tier / firewall boundary (a canon object, `firewall-boundary-hypothesis.md`), rather
  than positing a generic Rindler horizon.
- It FORCES the counting dimension to collapse `14 -> 4` (the W146 shadow-map constraint): a naive
  14-dim Y14 Planck sprinkle catastrophically mispredicts, so the projection must reduce the
  effective sprinkling dimension to 4. That is a GU-specific constraint on the number leg absent from
  bare BLMS.
- It LOCALIZES the observed light cone as the (3,1) base factor of the (9,5) gimmel null cone
  (W131), rather than an unstructured Lorentzian cone.

None of these pins a NUMBER the standard program cannot produce; they pin STRUCTURE
(which horizon, which dimension collapse, which cone). The only numerical hook is the everpresent
subleading correction (a non-Poisson `{2,7,13}`-smooth record spectrum rescaling the amplitude by
`sqrt(c)`, per sibling W145), and that is the siblings' object, G5-degenerate at leading order. So the
skeptic's honest scorecard: **NOVELTY = RELABEL-OF-{Malament + BLMS + Jacobson + Padmanabhan} at the
GR-emergence level, with ONE GU-SPECIFIC STRUCTURAL PIN (horizon = firewall/capability boundary; 14 ->
4 dimension collapse; cone = (3,1) base of (9,5)), and NO GU-specific NUMBER yet, the numerical hook
being delegated to the W145-W149 siblings.**

---

## 6. Synthesis and the return fields

**The Malament / causal-set / Jacobson chain, with what each step gives.** Malament (PORTED): causal
order -> conformal `[g]` + topology + differentiable structure + maximal speed; NOT the conformal
factor. BLMS (PORTED): number/record-count -> the conformal factor -> full `g`; Joe's
consistent-record-time IS this number leg; Lorentz invariance via sprinkling, LIV-bounded,
Hauptvermutung case-proven. Jacobson (PORTED): area-entropy Clausius on the measurement-gated
horizon -> `G_ab + Lambda g_ab = 8 pi G T_ab` (the dynamics); NOT the `R^2` sector.

**GU-NATIVE test result.** MATCH-ON-SIGN (both routes attractive: alpha `a1 = +1/3 > 0`, beta
`eta = +1/4 > 0`); MISMATCH-BY-the-`R^2`-sector (alpha `a2 = -1/9 != 0`, beta `a2 = 0` at
equilibrium); MAGNITUDE-NOT-YET-COMPUTABLE-because beta fixes the Einstein coefficient by DEFINING G
via `eta` rather than predicting a dimensionless ratio, the missing bridge being a native `eta` from
the W131 gimmel horizon area; the 3:2:1 split is GU-specific (a longitudinal-residue object) and
absent from the covariant record route.

**c-emergence verdict.** THEOREM-BACKED (Malament: the causal order has a maximal-signal-speed cone)
and GU-LOCALIZED (the observed X4 light cone = the (3,1) base factor of the (9,5) gimmel null cone,
W131; signature arithmetic (3,1)+(6,4)=(9,5) pinned). The VALUE of `c` is NOT a GU-specific
prediction: no substrate predicts the number `c` (it is 1 by convention); the content is cone
existence + Lorentzian signature + the base/fiber localisation. So GU pins WHERE the light cone comes
from, not its numerical value.

**R^2-sector verdict.** CONFLICT, informative. The record route gives PURE EINSTEIN (Raychaudhuri
focusing linear in `R_ab`; `f(R)` needs non-equilibrium entropy production, Eling-Guedens-Jacobson
2006); GU's `|II|^2` geometry FORCES the tachyonic `R^2` scalaron (`a2 = -1/9`, W126/W130). The record
route would truncate GU to Einstein and explain the tachyon away; GU's own geometry does not permit
that. The departure is an order-of-approximation departure (equilibrium/area-entropy vs exact induced
functional), not a sign or structural contradiction.

**Novelty verdict.** RELABEL-OF-{Malament 1977 + BLMS 1987 + Jacobson 1995 + Padmanabhan
equipartition} at the GR-emergence level; the W138 G5 de Sitter relabel gate and the Padmanabhan
prior-art sub-gate both bite. ONE GU-SPECIFIC PIN, structural: the measurement-gated `Y14 -> X4`
projection = the Jacobson horizon = the capability-tier/firewall boundary, forcing the `14 -> 4`
counting collapse and the (3,1)-base cone localisation. No GU-specific NUMBER yet; the numerical hook
is the siblings' everpresent-subleading object.

**Native-or-import summary (the crux).** HALF-NATIVE. The causal/conformal leg is GU-NATIVE (the
gimmel null cone, W131, forced by Y14's (9,5) DeWitt metric). The number/volume leg is IMPORTED with
one GU rider (14 -> 4). The dynamics leg is a GENERIC IMPORT that AGREES with GU's own geometric route
alpha on the Einstein SIGN but is not forced by `Y14 = Met(X4)`, and diverges from alpha at the `R^2`
sector. So "GR pops out" is HALF-demonstrated twice over: once geometrically (route alpha, W126/W130,
already in the repo) and once thermodynamically (route beta, this note), agreeing on the attractive
Einstein term and disagreeing exactly at the higher-derivative sector.

## 7. Tests, exit codes, files

- `tests/W151_gr_and_c_emergence_from_records.py` -- 25 checks, exit 0. Positive controls first:
  PC0 (de Sitter relabel `1.46 = 1/Omega_L`, W135 ladder); PC1 (Malament order->[g]-only under
  conformal reparam, exact; number->volume on the causal diamond, 4-sigma Poisson); PC2 (the recorded
  W126/W130 native Einstein data regression-pinned: `a0=2, a1=1/3, a2=-1/9, a2/a1=-1/3`, split 3:2:1,
  `c_R=-4/9`); J (Jacobson null-vector lemma in d=4, kernel dim 1 = span{eta}; area-entropy linear in
  `R_ab`; attractive `eta`; `a2_beta=0`); T (two-route: sign MATCH, `R^2` MISMATCH, magnitude
  NOT-YET-COMPUTABLE, 3:2:1 GU-specific); C ((9,5)=(3,1)+(6,4), base cone = observed `c`); E
  (everpresent leading term G5-degenerate, delegated to siblings).

## What this does NOT do

No canon / RESEARCH-STATUS / claim-status / verdict / posture change. No cross-repo identity asserted
(record/finality/time semantics belong to temporal-issuance + time-as-finality; capability measure is
TaF's; GU owns the emergent-metric math only). The everpresent-Lambda number leg is the concurrent
W145-W149 siblings' object, cited not duplicated. No native `eta`-from-gimmel-area computation
attempted (named as the missing bridge for the magnitude match). The Hauptvermutung and LIV bounds are
stated as standing constraints, not resolved. Exploration grade; conditional register throughout.

*Filed 2026-07-14. Wave W151 (TEAM EMERGENCE), five personas inline in one session. Reproducible:
`python -u tests/W151_gr_and_c_emergence_from_records.py` (25 checks, exit 0).*
