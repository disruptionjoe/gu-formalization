---
title: "Source-owned reduction and transport packet: the W177 background is not Yang--Mills stationary, and W131 is only a proper suboperator of the 20-slot B5 complex"
status: active_research
doc_type: result
created: 2026-07-29
run_id: GUH-20260729T231156Z-source-owned-reduction-transport-packet
lane_id: "1"
work_item: SOURCE-OWNED-REDUCTION-TRANSPORT-PACKET
code:
  - tests/channel-swings/w177_ym_residual_and_mode_closure_probe.py
  - tests/channel-swings/b5_normalized_transport_from_differential_probe.py
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Source-owned reduction and transport packet

## Result

`TWO-GATES-FAIL; ONE-CONSTRUCTION-TARGET-SHARPENED`.

The integrated packet returns two independent results:

1. **`W177-AMBIENT-YM-NONSTATIONARY`.** Under the explicitly conditional
   identification

   ```text
   A0 = spin-lift(nabla^gimmel)
   S_YM[A] = alpha integral_Y14 <F_A,F_A>,
   ```

   the W177 connection is not a critical point of the Yang--Mills action at
   the deterministic W177 point. The orthonormal-frame norm of
   `D_A0^* F_A0` is approximately `3.19904`, stable to about three parts per
   million over three nested derivative scales and about `859` times the
   numerical/control floor. Therefore the previously available W177
   quadratic curvature form is **not a physical fluctuation Hessian at this
   background**. The mode-closure calculation correctly does not run.

2. **`B5-NORMALIZED-TRANSPORT-NOT-DETERMINED-BY-W131-DIFFERENTIAL`.**
   Layer 0 shows that the written W131 operator and the B5 middle complex are
   not the same object. W131 acts on `ker Gamma` only: 12 observer slots and
   dimension `1664`. B5's frozen ledger is
   `S + im Gamma + ker Gamma`: 20 slots and dimension `1920`.
   Even granting W131 the most favorable possible coefficient support on its
   own carrier, `2^4` relative endpoint-sign classes survive on the absent
   `S`/`im Gamma` sectors. A planted noncentral return commutes with that
   maximal envelope.

The negative B5 result is not a no-go. The complete 136-cell observer-symbol
graph is connected. A genuinely written coefficient-level differential with
a connected nonzero graph would force a slot-diagonal endpoint return to be
central. Starting from the W131 carrier, the exact support lower bound is
**four additional formal-adjoint/mirror symbol orbits** connecting the
`S` and `im Gamma` provenance sectors to the RS/`X` core. This is the
construction target generated by the attempt.

No source-action row, physical index, generation count, B5 phase, vacuum,
canon verdict, or public posture moves.

## Layer 0: the two shared-name failures

### The ambient action branch

The connection `A` in W131/W177 is the metric-compatible
`so(9,5)` frame/spin/RS connection of the gimmel metric. The `A` varied in the
displayed source-action Yang--Mills term is an IG gauge connection. They are
homonyms unless one conditionally identifies them. This packet made that
identification only for the ambient candidate above.

The result therefore says:

- that fixed ambient candidate is not stationary;
- its curvature remains a real geometric carrier;
- its algebraic vertical--Krein channel remains real; and
- nothing transfers automatically to the separately written `X4`
  Seiberg--Witten action.

### The differential branch

The two first-order objects are:

```text
W131:
  D_RS = Pi_RS c(nabla) Pi_RS + m^2 Pi_RS
  acting on ker Gamma.

B5:
  an unreleased/native or independently reconstructed middle complex
  acting on the complete observer ledger
  S + im Gamma + ker Gamma.
```

W131 supplies the RS block of a possible B5 construction. It does not supply
the `S` and `im Gamma` rows, the off-diagonal provenance maps, a square-zero or
acyclic arrangement, or the full Green/domain packet. Calling both objects
“the written RS differential” would hide precisely the missing construction.

## A. The stationarity gate

For a Levi--Civita connection, the Yang--Mills equation is the harmonic
curvature condition. Contracted Bianchi identifies its residual, up to an
irrelevant overall sign, with the Codazzi defect of Ricci:

\[
(D_{A_0}^{*}F_{A_0})_{MJ,L}
=\nabla_M\operatorname{Ric}_{LJ}
-\nabla_J\operatorname{Ric}_{LM}.
\]

The probe evaluates this tensor at the exact seeded W177 point using the
actual

\[
TY^{14}=TX^4\oplus\operatorname{Sym}^2T^*X
\]

gimmel metric, not the exterior `6+4` numerical ten.

### Numerical receipt

| item | result |
| --- | ---: |
| gimmel signature | `(9,5)` |
| curvature norm, three scales | `5.170299, 5.170299, 5.170299` |
| residual norm, scales `0.75, 1, 1.25` | `3.19904935, 3.19904137, 3.19903939` |
| residual relative spread | `3.11e-6` |
| maximum Ricci-symmetry defect | `5.60e-6` |
| metric-compatibility defect | `2.19e-11` |
| direct-divergence / contracted-Bianchi defect | `3.73e-3` |
| planted `Ric=lambda*g` residual | `2.51e-11` |
| planted non-Codazzi residual | `21.6839` |
| signal / numerical floor | `858.6` |

The preregistered nonstationarity classifier required a residual above
`1e-6` at every scale, relative spread below `12%`, and signal above twenty
times the larger control floor. It passes by wide margins.

This is strong local numerical/construction evidence, not a symbolic theorem
about every point of `Y14`. It is nevertheless sufficient to reject this
specific background as a global critical point: a Yang--Mills solution must
satisfy the Euler--Lagrange equation at every point.

### Consequence for reduction

A restricted quadratic form at a non-solution contains a tadpole. Its
eigenvalues are not masses of fluctuations about a vacuum. Therefore:

```text
physical Hessian at W177 background: KILLED
retained-mode closure at that background: NOT RUN
literal X4 descent: still unbuilt
```

The constructive repair has to precede any more spectral reading. There are
two honest branches:

1. vary the IG connection independently and solve
   `D_A^*F_A=0` (with a declared gauge/domain) before linearizing; or
2. if the connection must remain Levi--Civita, solve the harmonic-curvature
   / Codazzi-Ricci equation in the allowed gimmel-metric family.

Choosing an Einstein or locally symmetric metric merely because it makes the
residual vanish would be a fit unless the source construction selects it.

## B. The transport-normalization gate

### Exact carrier and support census

| object | slots | complex dimension | allowed ordered cells |
| --- | ---: | ---: | ---: |
| complete B5 observer ledger | 20 | 1920 | 136 |
| optimistic W131 carrier envelope | 12 | 1664 | 40 |
| absent `S + im Gamma` sector | 8 | 256 | n/a |

“Optimistic envelope” is deliberate. The probe treats every allowed
observer-natural cell internal to the W131 carrier as if its actual
coefficient were nonzero. The true projected RS symbol can impose no more
slot-diagonal phase equations than this envelope.

The forty-cell envelope connects all twelve RS/`X` slots. On the full ledger,
however, it leaves the eight absent slots isolated. Enumerating the ten
mirror-pair signs gives:

```text
support/mirror ledger alone:       2^10 absolute signs
maximal W131 envelope:             2^5 absolute signs
modulo one common global sign:     2^4 relative classes
complete connected B5 graph:       2 absolute signs
modulo one common global sign:     1 central class
```

The planted control flips one absent `S` mirror pair while leaving every W131
slot fixed. It commutes with all forty optimistic W131 cells, preserves the
mirror pairing, and is noncentral. The full graph rejects it. Thus the test
distinguishes a real full-ledger constraint from a permissive support match.

### The smallest missing support packet

Under formal adjoint and the declared mirror support action, a brute-force
exact search over the 39 joint orbits finds that no set of one, two, or three
additional orbits can connect the full ledger to the W131 core. Four can.

The probe prints one lexicographically first four-orbit skeleton. It is a
**support target**, not a selected differential. Its scientific use is the
lower bound:

> The next source-action term must supply at least four appropriately placed
> adjoint/mirror principal-symbol orbits, or an equivalent connected
> coefficient graph, before the normalized 20-slot transport can possibly be
> forced by intertwining.

This sharpens the role of the placeholder `T4` compensator. The projected
kinetic term `T2` cannot do the job. The missing object is an explicit
off-diagonal chimeric/BV differential connecting `S`, `im Gamma`, and
`ker Gamma`, naturally built from the already named spin-Dirac,
twistor/divergence, projected-RS, and compensator channels.

Connectivity is only the first gate. A candidate must then pass:

1. coefficient normalization from the action rather than a favorable table;
2. formal Krein adjoint with the physical pairing composed in;
3. square-zero/deformation-retract or the declared B5 acyclicity predicate;
4. a Green boundary form and common closed domain; and
5. transport around the actual DeWitt fibre loop, producing the twenty
   endpoint mismatches rather than assuming them.

## Constraint-surplus reading

The failed routes are informative because neither was tuned.

| test | fitted freedom | independent demand | outcome |
| --- | ---: | ---: | --- |
| W177 stationarity | 0 | full local Yang--Mills equation | robust failure |
| W131-only centrality | 0 phase choices admitted | every written nonzero W131 block intertwines | at least 4 relative bits survive |
| full connected differential, conditional target | no endpoint phases fitted | connected coefficient graph | centrality would follow |

The last row is why the result does not license the orthodox “a construction
would be shaped to fit and teach nothing” response. If the four-orbit-plus
coefficient packet is written independently of the desired endpoint sign,
then centrality is a surplus consequence. The work is to write and test that
packet, not to decline it.

## What moved and what did not

### Moved

- The ambient W177 Levi--Civita background is removed from the list of
  admissible Yang--Mills vacua for this conditional branch.
- The W177 quadratic form is fenced from physical mass language at that
  background.
- The B5 transport residual is no longer “some normalized 20-slot map.” It is
  localized to a full-ledger differential extension with an exact
  four-orbit support lower bound.
- The W131/B5 shared-name ambiguity is closed at Layer 0:
  **proper suboperator**, not same differential.

### Unchanged

- The actual-fibre Krein-line monodromy remains exact `-1`.
- `C_perp=KJ_obs` remains the conditional algebraic P1--P2 weld candidate.
- P1, P2, and P3 remain separately accounted data; no merger or count follows.
- The vertical four-dimensional scalar Krein channel remains built
  algebraically.
- The displayed `X4` action still has no built ambient mode reduction.
- No claim, canon, source-action row, physical index, count, or public posture
  changes.

## Next construction order

1. **Write the full coefficient-level chimeric/BV first-order differential**
   on all twenty slots. Use the four-orbit result as a minimum connectivity
   test, not as a coefficient selector.
2. **Demand acyclicity and Krein adjointness from that written object.** Let
   those equations determine which support skeletons survive.
3. **Only then compute normalized DeWitt-loop transport** and the twenty
   endpoint mismatches. This is the earliest point at which the P1--P2 merger
   can pass or fail.
4. In parallel, **replace the W177 background by an actual stationary
   connection/metric** before any ambient Hessian or retained-mode spectrum is
   read.

Construction remains the target. The packet has identified exactly which
construction must now be written.

## Validation

Both probes pass all predeclared and planted controls:

```text
python3 tests/channel-swings/w177_ym_residual_and_mode_closure_probe.py
python3 tests/channel-swings/b5_normalized_transport_from_differential_probe.py
```

The run also re-executes the W177 curvature carrier, vertical-reduction,
actual-fibre naturality, and native-packet fail-closed controls before close.
