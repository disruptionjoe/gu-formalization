---
title: "Reproduction and status integrity wave"
status: exploration
operational_state: complete
doc_type: integrity_result
created: "2026-08-27"
grade: "MACHINE-CHECKABLE REPRODUCTION AND STATUS CURRENCY; NO SCIENTIFIC RESULT"
target_claim: NONE-NOT-A-KILL; this artifact repairs repository integrity contracts only
canon_verdict_change: none
---

# Reproduction and status integrity wave

Scope: the tracked-Markdown status population and public certificate discovery
contract. This result binds repository-integrity machinery only. It does not
bind a GU mathematical object, physical construction, source assertion,
ledger row, prediction or paper lifecycle.

```gu-typed-objects
result: two current repository-integrity contracts are restored and coupled to fail-closed checks
carrier: repository-native YAML and Python integrity surfaces LAYER=UNTYPED CHIRALITY=N/A
pairing: declared status population and certificate roots against live tracked paths ON=repository-integrity-surfaces
real_structure: N/A -- this process result assigns no mathematical real form
grading: machine-checkable repository-integrity currency only
action_owner: N/A -- no scientific action owner is created or transferred
target: preserve status semantics and public reproduction coverage MAP-TYPE=evaluation
```

## Preflight bookend

A1 remains terminal at `ADHERED 106 / PARTIAL 4 / UNTYPED 1`; the disposition
register remains 91 terminal and zero open against ledger v0.263; the B2
action-root set remains empty. A serialized sweep of 423 process gates found
269 green and 154 red. Most reds are historical scientific regression fixtures.
Two current integrity defects are independently actionable: the prior wave
added two correctly typed Markdown files after the status population was
frozen, and the reproduction scope audit rejected a legitimate nested test
directory solely because its basename matched the root governance directory.

## Result

1. The frontmatter population ratchet now covers the complete final tracked
   Markdown population, including this wave's exploration and hostile review.
   Document role remains separate from operational state; no legacy status is
   normalized.
2. The reproduction scope audit now compares absolute root identity. Root
   `process_gates/` remains excluded while `tests/process_gates/` is explicitly
   required to stay in certificate discovery.
3. The coupled certificate verifies both clean baselines and catches planted
   count, digest and directory-pruning mutations. The channel-swing inventory
   advances by exactly one Python certificate. The complete 423-gate census
   moves from 269 green / 154 red to 271 green / 152 red; no historical or
   authority-bounded red was normalized.

## Postflight bookend

- **Strongest overclaim:** a green discovery contract proves coverage of the
  declared certificate roots, not correctness of every certificate or claim.
- **Strongest contrary case:** repeated directory names are legitimate in a
  hierarchy; a basename exclusion is not equivalent to excluding one root.
- **Weakest reproducibility seam:** status mapping is Git-tracked-path based,
  so every new Markdown artifact must be staged before the final population is
  frozen. The coupled probe checks the final staged population.

No source status, action owner, ledger row, SG4 phase, prediction,
confirmation, canon verdict, protected paper, release state or public posture
changes.
