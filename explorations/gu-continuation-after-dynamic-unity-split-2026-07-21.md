---
title: "GU continuation after the Dynamic Unity split — the non-dynamic work GU keeps and drives to completion once the dynamic frontier (tachyon-transducer, DE-amplitude via Λ~1/√N, record-accretion, finality/issuance bridge) moves to the new Dynamic Unity repo. Three continuation threads (paper hardening; non-dynamic prediction extraction; harden the static results to completion) plus the hand-off (what moves to DU, what is shared with P2C). GU remains the STATIC SUBSTRATE Dynamic Unity consumes. Per-item: concrete PROPOSED update + current state + recommended next step, with a stay/move/shared tag for every portfolio node. Grades are CONSUMED, not moved."
status: proposal
doc_type: roadmap
created: 2026-07-21
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
directed_by: "Joe direct chat via coordinator, 2026-07-21 (produce the LIST of GU updates for the non-dynamic continuation after the Dynamic Unity split; LIST/proposal only — do NOT edit research-portfolio.json / LANES.yaml / NEXT-STEPS, the hourly cycle co-owns those)"
inputs:
  - LANES.yaml
  - lab/process/research-portfolio.json (all work_items)
  - NEXT-STEPS.md (front-door operational block)
  - RESEARCH-PROGRAM.md, RESEARCH-STATUS.md, VERIFICATION.md
  - explorations/proposed-dynamic-unity-repo-scaffolding-2026-07-21.md (the DU charter, approved)
  - explorations/de-packet-lane-structure-clarification-2026-07-21.md
  - explorations/uncontestability-audit-math-spine-2026-07-21.md (the 5 must-fix list)
  - explorations/pin14-anomaly-number-2026-07-21.md + explorations/anomaly-rigor-approach-scoping-2026-07-21.md + explorations/anomaly-rigor-track-a-sw-number-2026-07-21.md (the Track B engine build)
  - explorations/per-leg-recovery-state-2026-07-21.md (hosts-not-derives / type wall)
  - explorations/parsimony-unexplained-joints-ledger-2026-07-21.md + comparative-tensions-ledger-* (the ledgers)
  - explorations/more-predictions-hunt-2026-07-21.md (the parity leg + the collapsed candidates)
  - explorations/b5-independent-reconstruction-scope-2026-07-21.md (the shared spine)
  - papers/candidates/located-not-forced/HARDENING-QUEUE.md, VERIFICATION.md
scope: read_only_proposal
external_actions: none
---

# GU continuation after the Dynamic Unity split

**What this is.** A proposal LIST of what to UPDATE in `gu-formalization` so it continues its
**non-dynamic** work now that the dynamic frontier is standing up its own repo (Dynamic Unity,
"DU"; charter `explorations/proposed-dynamic-unity-repo-scaffolding-2026-07-21.md`, approved by
Joe 2026-07-21, not yet instantiated). This is a **list/proposal only.** Nothing here edits
`research-portfolio.json`, `LANES.yaml`, or `NEXT-STEPS.md` — the hourly/steward cycle co-owns
those; every portfolio state change below is written as a **PROPOSED** update for the daily
steward / Joe to apply. No claim, canon verdict, paper status, or public posture moves; every
grade is **consumed as found, not moved.**

## The split (the frame)

- **Moves to Dynamic Unity (the DYNAMIC frontier):** the tachyon-transducer (source action →
  tachyonic record-count roll `N = e^{4p}` → record-accretion → DE sign+magnitude+arrow as one
  object), the **DE amplitude via `Λ ~ 1/√N`** (native derivation of `φ = 1/(3·Ω_Λ)²`),
  **record-accretion / volume→Λ**, the **finality/issuance bridge** (issuance grounding of the
  DE magnitude, the `N`-growth "does anything cross" reading drawn from TaF/TI), and the
  **dynamic tension-reframe sweep** (make each GU tension dynamic). These are DU Lane 1
  (channels 1.1/1.2) and DU Lane 2.1.
- **GU keeps (the STATIC / non-dynamic substrate):** paper hardening, non-dynamic prediction
  extraction, and hardening the static GU results to completion. **GU is the static substrate
  Dynamic Unity consumes** (`σ`/`τ` data, the type wall, hosts-not-derives, the Rung-2 results,
  the record-count mode `p`, the frozen DE-sign packets). DU re-verifies everything it draws;
  GU does not chase the dynamic questions.
- **Shared with P2C (the blocked spine):** the B5 source action — P2C constructs the
  transferred frontier, GU runs the independent recovering-action / native-vs-forced
  adjudication, DU consumes the result. Home coordinated with P2C (the signed-graph / one-Z/2
  home lives at P2C).

**How to read the tags.** Every portfolio node is tagged **STAYS** (GU non-dynamic work,
continues here), **MOVES→DU** (forward work relocates; GU keeps the frozen artifact as consumed
substrate), or **SHARED** (GU keeps running it AND it feeds DU/P2C; home coordinated). Almost
everything STAYS — GU *is* the static substrate, so the split removes very few current nodes
and mostly adds a hand-off/seam duty. The items that "move" are dynamic **exploration threads**,
not existing portfolio work_items (there is no `TACHYON-TRANSDUCER` node — the dynamic frontier
lived as Lane-1 exploration content, which is exactly what DU is being stood up to house).

---

## THREAD 1 — Paper hardening / publication-readiness (per paper)

*Lane 3 + the Drafting-Factory seam. Joe always publishes; the terminal agent state is a
Joe-review draft. All items STAY in GU. The uncontestability audit
(`uncontestability-audit-math-spine-2026-07-21.md`) supplies the concrete fix list; the spine
is honestly graded (Lean flagships kernel-check with no axioms; `reproduce_all.py` 31/31), so
the contestable joints are FRAMING + VERIFICATION-TIER, not mathematics.*

### 1.1 PP3 — DE curve-family pre-registration note (the drafted paper)
- **Current state:** **REGISTRATION-READY awaiting Joe's per-action GO.** Frozen packet GU-side
  (`explorations/prediction-package-pp3-de-curve-family-2026-07-20.md`,
  `FROZEN_CONDITIONAL_CURVE_FAMILY`); drafted manuscript DF-side
  (`drafting-factory/production/pp3-preregistration-note/`, all six Joe decisions cascaded). PP3
  packages the DE *shape* (one-parameter thawing locus, frozen slope, curvature `S2`), **not the
  amplitude** — so it is **non-dynamic and stays**.
- **Concrete update:** none inside GU (the GU repo by design does not surface factory release
  states). PROPOSED steward note only: the `DECISIONS.md` ~2-day READY-escalation clock is the
  intended remedy for "it's ready but nobody flagged it"; DF's own `PAPER-SEEDS.md`/
  `LANE-STATE.yaml` are slightly behind `DECISIONS.md` and should be synced by the DF steward.
- **Next step:** hold at REGISTRATION-READY; Joe's GO is the single external gate. No GU work.

### 1.2 Located, Not Forced — the flagship generation-count paper
- **Current state:** internally judged **arXiv-ready**, publication-deferred pending Joe's
  decision + outside validation (portfolio `PAPER-EXTERNAL-EYES = NEEDS_JOE`). Spine
  Lean-verified, `reproduce_all.py` 31/31, `HARDENING-QUEUE.md` H1/H8/H9 done.
- **Concrete hardening updates (all STAY; from the audit + queue):**
  1. **Antilinear-leg → Lean (queue H2/H7; audit item 1c).** The finite-dim core of Theorem 2
     is kernel-checked, but the antilinear index-nullity leg is not — port it so the *whole*
     Theorem-2 conclusion is kernel-checked, not just the linear leg. Next hardening step.
  2. **Math-spine audit fixes (audit items 5, 6, 9):** change Theorem 2's "machine-verified"
     status line to "Lean-checked implication; premises computed (numpy, internal tier)" — or
     discharge the K-nullity / (+96,−96) premises with the exact `Cl(9,5)` rep theory the repo
     already runs elsewhere (MOVE-4 pattern, exact integers); and fix `VERIFICATION.md` flagship
     (c) (see 1.5). Optional: homotopy-theorist confirmation of the inverse-reading novelty.
  3. **The ENDORSER note is Joe's lane.** `ENDORSER-REQUEST-DRAFT.md` exists; getting one named
     specialist to sign off on the core is the single highest-leverage action (audit item 1),
     but it crosses the internal→external tier boundary and is **Joe-gated** — GU stages it, Joe
     acts.
- **Next step:** work H2/H7 (Lean) as Lane-3 hourly hardening; stage the ENDORSER packet for Joe;
  do not edit the frozen submission body without maintainer GO.

### 1.3 Observer Value Selection Theorem
- **Current state:** locally **READY FOR ZENODO** (portfolio `PAPER-EXTERNAL-EYES`); the abstract
  theorem is the durable externally-attackable result (`VERIFICATION.md` flagship (a), L1, Lean
  `ResidualSelection.lean` no-axioms). Self-graded NOVEL-PACKAGING (Cantor–Lawvere), stated
  honestly.
- **Concrete update:** none needed to the math; hold at READY. Joe-gated for release.
- **Next step:** preserve readiness; no routine polish. Joe authorization or a substantive defect
  only.

### 1.4 Six-axis testability white paper (paper seed)
- **Current state:** staged in `papers/candidates/six-axis-testability/`; **stale** — the drafting
  note says it is now "seven axes + Layer-0," title/body unreconciled, and its central empirical
  claim (H1–H6 benchmark) is **entirely unrun** (audit item 7).
- **Concrete update:** retitle/refold to "Seven-Axis + Layer-0" and relabel explicitly as a
  **protocol pre-registration (methods; benchmark unrun)**, not a candidate *result*. No physics
  claim moves — tier/title only.
- **Next step:** the retitle+relabel edit (cheap, Lane 3); decide whether to run the benchmark or
  leave it as a pre-registration.

### 1.5 VERIFICATION.md — the outsider honesty map (root)
- **Current state:** billed as the surface outsiders trust instead of adjectives, but flagship
  (c) is **stale/over-precise** — it reads "the count is forced to {1,3} and 3-primary (a
  class-wide no-go theorem)," which the current paper does **not** prove (audit item 6).
- **Concrete update:** rewrite flagship (c) to match the paper's status table — "multiplicity
  natively 3 (a representation dimension); net chiral count interior-even / external-by-structure;
  only unconditionally computable integer = 1; three not derived." The honesty map must be the
  *most* conservative surface, never a looser one.
- **Next step:** the one-line rewrite (Lane 3). Load-bearing because it is the document outsiders
  are told to trust.

### 1.6 Other paper seeds (carried, not activated)
- `PAPER-UV-GRAVITY` (stationary-vacuum gravity survivor) — **PARKED**; compatibility, not
  distinctiveness. Stays; activate only after an explicit overlap/novelty gate (see Thread 3).
- The remaining candidates (`generation-number-boundary-odd-primary`, `keep-and-grade-loop-cost`,
  `one-residual-complete-picture`, `observer-value-selection`) are staged seeds; route via
  `PAPER-SEED-ROUTING` (Lane A) to Drafting Factory, do not open a parallel GU drafting queue.

---

## THREAD 2 — Finding more predictions (non-dynamic)

*Lane 2. The static-`σ`-based prediction surface. Adversarial finding (parsimony ledger +
more-predictions hunt): GU's evidential surface is **one bit with currently one observable
readout** (the DE no-phantom-crossing). The honest yield of the hunt is one new observable
window, not a zoo. All items STAY.*

### 2.1 The frozen DE-sign packets — PP1 (sign) and PP3 (shape)
- **Current state:** **frozen, stay.** PP1 (`blockbuster-p1-de-sign-covariance-2026-07-19.md`,
  `w(z) ≥ −1` pointwise, R0_COND) and PP3 (the curve family). Owner node
  `PRED-CANDIDATE-PACKETS` (BLOCKED_SOURCE_GAP for the *mirror* candidate; PP1/PP2/PP3 unchanged).
  **The DE SPLITS at the sign/amplitude line:** sign+shape = static (stay); the **amplitude**
  (`φ = 1/(3·Ω_Λ)²` native derivation) = dynamic → **MOVES→DU Lane 2.1**.
- **Concrete update:** none to the packets. PROPOSED clarity note in the node: PP1/PP3 stay GU
  Lane 2; the amplitude-derivation route leaves GU (was never a GU node — it is DU's distinctness
  bank). Public mentions should foreground the **joint rigidity surface** (sign + no-crossing +
  amplitude-ceiling + one-bit co-flip), never the bare sign (audit item 3).
- **Next step:** route PP1/PP3 observational confrontation via `P-OBS-LEG`/`DE-F1-TRIPWIRE` on
  official releases; no retuning.

### 2.2 The parity leg (anomaly-based) — the ONE genuine new observable window
- **Current state:** the single shortlist survivor of the more-predictions hunt
  (`more-predictions-hunt-2026-07-21.md`, Candidate A): a `w1` reflection/T anomaly generically
  forces **parity-odd cosmological response** (CMB TB/EB, cosmic birefringence `β` — measured,
  ~3σ isotropic hint — chiral primordial GW), whose sign **co-varies** with the DE sign
  (`LCDM`-forbidden cross-domain correlation). Grade: **FORCED only at the proposal grade of the
  anomaly ID + one UNBUILT descent map with a definite sign.** Lives as target **T3** of
  `ANOMALY-DESCENT-HARDENING` (PARTIAL_ACTIVE). Non-dynamic (anomaly/`σ`-based) — **stays.**
- **Concrete update:** PROPOSED — keep T3 in GU Lane 1/2 as the prediction-extraction candidate;
  its two gates are (1) bank the anomaly ID at operator grade, (2) build the fiber-`w1`→spacetime
  descent-with-signs. Gate 2 discharges via `SRC-COH-1`, which `B5-INDEPENDENT-RECONSTRUCTION`
  (SHARED) addresses. One descent map lights up four faces at once (parity A, `θ_QCD`/neutron-EDM
  G, ν Majorana/Dirac B via Pin+ Z/16, protected-mode→`ΔN_eff` C).
- **Next step:** if it freezes, it becomes a second frozen packet (the parity leg) → seed to
  Drafting Factory. Until the descent is built, hold as proposal-grade; do NOT report an absolute
  parity sign as internally forced (it is external = `σ`); the forced content is the CO-VARIANCE.

### 2.3 The recovered TeV mirror state (blocked on the ghost-fork)
- **Current state:** the unnumbered mirror-sector candidate is **BLOCKED_PRODUCIBILITY** — the
  K-negative modes must be producible asymptotic states while the same dependency removes them as
  negative-norm ghosts (`PRED-CANDIDATE-PACKETS` assessment; `MIRROR-NATIVE-ROUTE =
  RESOLVED_NO_GO` for every native order-parameter class). The ghost-vs-physical fork needs the
  L7 Born-rule/superselection rule (source-gated). DU Lane 2.2 carries "the TeV mirror state
  (ghost-vs-physical fork + reopen conditions)" as a live candidate with blockers.
- **Concrete update:** PROPOSED — GU keeps the frozen candidate + its four reopen conditions;
  tag the ghost-fork resolution as **SHARED** (it rides the source/dynamics work DU/P2C own). Do
  not promote the no-go into a mirror-particle prediction.
- **Next step:** reopen only on owner-native evidence resolving the producible-vs-ghost fork;
  otherwise monitor.

### 2.4 The FALSIFICATION-BATTERY
- **Current state:** `FALSIFICATION-BATTERY = MONITOR` (51-entry council registry). Internal
  structural track **largely exhausted** on current inputs (T-E2 → `w1`; T-B3/T-B1 no-fourth
  failed as a GU falsifier — REMOVE from the experimental track, universality survives not a
  count-cap; T-C2 already computed; T-D1 qualitative, T-D2 sampler-dependent, T-D3 underdefined).
  Experimental track routes to the existing machinery (T-H2 DESI/Euclid, etc.). Non-dynamic —
  **stays.**
- **Concrete update:** none structurally; the two-track contract stands. PROPOSED: keep routing
  experimental falsifiers to `DE-AMP-DIAGNOSTIC`/`DE-F1-TRIPWIRE`/`PRED-CANDIDATE-PACKETS` — **no
  second monitoring mechanism** (Joe standing rule).
- **Next step:** reopen only on a genuinely distinct structural test, an independent concurrency
  predicate/map + common measure for T-D3, or a fired experimental falsifier.

### 2.5 The static prediction GATES (carried, structurally blocked)
- `FIXED-NATIVE-QUANTITY` (GATED_NEW_STRUCTURE), `BLIND-QUANTITATIVE-CONFRONTATION`
  (GATED_FIXED_QUANTITY), `COSMO-CROSS-OBSERVABLE` (PARKED) — all **stay**; each waits on new
  native structure or native normalization. Honest note: the "new native structure" that would
  fill them is largely the **dynamic source action** — i.e. the unlock route lives in DU, but the
  GATE stays in GU. `PRED-FLAVOR-RANK`, `PRED-NORM-RANK`, `PRED-FLAVOR-OBS` are `RESOLVED_NO_GO`
  and stay banked; `PRED-NORM-RANK` (no GU-native absolute scale) is precisely the **static
  no-go DU consumes** as the starting point for its dynamic amplitude derivation.
- **Concrete update:** none; preserve residual dimensions and reopen conditions verbatim.

---

## THREAD 3 — Harden the non-dynamic parts to completion

*Lanes 1 + 3. Drive the static results to their hardening end. All STAY (with two SHARED
spine items called out). The uncontestability audit is the master fix list; the honest ceiling
throughout is the internal→external tier boundary, which only an outside human can cross.*

### 3.1 Located, Not Forced → completion
- Covered in Thread 1.2. The static-result hardening residuals: antilinear-leg→Lean, the exact
  `Cl(9,5)` premise discharge, the class-C boundary table (H3), the premise-flag consistency
  audit (H4), the per-claim GU-dependency table (H5), the prior-art delta table (H6). **STAYS.**

### 3.2 The `σ = w₁` / anomaly / Pin cluster → the Track B Ω^{Pin+}_14 engine build
- **Current state:** the anomaly-protection of `σ`'s reflection-anomaly identification is
  **PROPOSAL grade**. Track A (the `σ`-free SW-number survival check,
  `anomaly-rigor-track-a-sw-number-2026-07-21.md`) **VANISHED** → ANOMALY-TRIVIAL **inconclusive**
  (`σ` reaches only degree 3 on its `RP^3` base; every firing monomial is T2-gated). The T1 shot
  (`pin14-anomaly-number-2026-07-21.md`) left `|Ω^{Pin+}_14|` **BLOCKED**, but reconstructed the
  instrument (ABP reduction, the A(1)-module `N`, Margolis homologies, generators at 0/4/8/12) and
  **sharpened the residual to three named sub-steps**. Node: `ANOMALY-DESCENT-HARDENING` T1
  (RECONSTRUCTION-GRADE, hourly-eligible); `OPERATOR-END-PENCIL` residual. Non-dynamic — **stays.**
- **Concrete update:** PROPOSED — the "harden to completion" step is the **Track B minimal-A(1)-
  resolution engine build** (`anomaly-rigor-approach-scoping-2026-07-21.md`): ~600–950 lines
  extending the existing pin14 probe (resolution loop → `h_0`-tower truncation lengths; ABP
  correction summands `Σ^8 ko<2>`, `Σ^10 ko`, `Σ^12 ko<2>`; differentials/extensions), cross-
  validated against Bruner `ext`. **Be honest about the ceiling:** the group order is **neither
  necessary nor sufficient** to settle ANOMALY-TRIVIAL — the deliverable is `σ`'s class nonzero,
  which is **T2-gated (σ-circular at operator grade)**. So Track B can rigorously bank the GROUP
  order (a genuine reproducible computation, used as a post-hoc engine control) but **cannot** bank
  `σ`'s class without breaking T2 non-circularly. Recommend running the engine build for the group
  order + T1 rigor, and stating plainly that class-nonvanishing stays gated.
- **Next step:** dedicated Track B engine-build pass (one focused session); report BLOCKED-vs-
  computed honestly; do not recite a published `|Ω^{Pin+}_14|` as computed.

### 3.3 The type wall + hosts-not-derives (per-leg recovery)
- **Current state:** `per-leg-recovery-state-2026-07-21.md` grades every sector Rung-0/Rung-1
  given `σ,τ` supplied; **nothing reaches Rung 2 (native forcing).** "Most of physics *fits*
  given the bit, but GU does not yet *force* any of it given the bit — and the bit itself is
  supplied, not derived." The type wall (native/imported/fitted/free separation) and
  hosts-not-derives are the static discipline DU consumes. Non-dynamic — **stays.**
- **Concrete update:** none to grades. PROPOSED: keep `per-leg-recovery` as the canonical
  hosts-not-derives surface; DU re-verifies rather than re-asserts (charter §3 self-check duty).
- **Next step:** update per-leg grades only when `B5-INDEPENDENT-RECONSTRUCTION` (SHARED) lifts a
  leg from `R0_COND`/`R1` to `R1`-forced.

### 3.4 The parsimony + tension ledgers
- **Current state:** the parsimony/unexplained-joints ledger
  (`parsimony-unexplained-joints-ledger-2026-07-21.md`) favors GU **narrowly, conditionally, on
  the sign/orientation cluster only** (4 posits → 1 external bit `σ`; one observable leg carries
  the entire IBE license); a rigged control is rejected ~800× (the method has teeth). The
  comparative-tensions ledgers (`comparative-tensions-ledger-MASTER` + cosmo-gravity + particle-qm
  + qg-toe, `tension-ledger-successful-theories`, `council-inherited-tension-resolution`) are the
  static IBE substrate. Non-dynamic — **stays**, but SHARED at the consumption seam: DU Channel
  1.2 (the dynamic tension-reframe sweep) takes the GU tension ledger and tests which tensions
  reframe when made DYNAMIC.
- **Concrete update:** PROPOSED — GU keeps hardening the STATIC ledger (fairness discipline,
  equal-coverage guard, the rigged-control); tag the tension ledger **SHARED** (DU reads it as the
  input to its dynamic sweep, re-verifying rather than importing). Do not let DU's dynamic reframe
  move the static grades back into GU without a frozen packet.
- **Next step:** maintain the ledgers as GU's honest comparison surface; no dynamic reframe here.

### 3.5 The NCG-comparison, the math-spine uncontestability fixes, README charter
- **Current state:** the load-bearing math spine is honestly graded (audit verdict: over-hedged
  in the deep files), but the **README charter compresses past its own hedges on three words** —
  "result" (σ externality), "forces" (DE sign), "complete modulo σ,τ" (audit item 2). After the
  split, the README also needs to state the GU↔DU boundary (GU = static substrate).
- **Concrete update (PROPOSED, README is not a shared-surface lock — but this is a proposal, so
  it is staged, not executed):**
  1. Sync the three charter words to their existing downstream grades — "reconstruction cannot
     close from committed structure (reconstruction-grade for GU's unbuilt operator)"; "derives,
     at toy grade conditional on SRC-COH-1 + an unbuilt action, a (non-distinctive) DE sign";
     "hosts conditionally, gated on the blocked source action" (audit item 2).
  2. **De-bank the "exactly two external data" ledger line** — mark `σ`'s and `τ`'s cardinalities
     proposal-grade *in the sentence*, drop "provably" for `τ`, name the single closing number
     (the ABP-exact `Ω^{Pin+}_14` order + operator-grade deck action) (audit item 4).
  3. Add the **GU/DU split statement**: GU is the static substrate; the dynamic frontier
     (transducer, amplitude, accretion, issuance) now lives in Dynamic Unity, consumed-and-
     re-verified, not owned here.
  4. Write **one consolidated `σ`-dossier** (+ `sigma_dossier_reproduce.py`) linearizing the chain
     operator → `q<0` crossing → `K_S`-null → no self-adjoint realization → external Z/2 → `=w₁`
     → Pin receptacle → protection, each link with grade + probe, ending on the one open number
     (audit item 8). The flagship reproduces in one command; the charter's centerpiece should too.
- **Next step:** stage these edits as a Lane-3 hardening batch for maintainer/Joe review (the
  README/VERIFICATION edits are content, not the frozen paper body).

### 3.6 The static recovery no-gos and construction space (Lane 1, banked)
- `RECOVERY-CERTIFICATION` (+ GR/SM/QM/COSMO internal items, mostly `BOUNDED_NO_GO` /
  `COMPLETE_CONDITIONAL_FAIL`), `CONSTRUCTION-SPACE-EXPLORATION` (WAITING_EXTERNAL; the lattice is
  source-gapped at C1's L7 Krein-probability slot), `OPERATOR-END-PENCIL` (`RESOLVED_NO_GO`,
  LC-SELECTOR; residuals: DeWitt-as-J-symmetrizing-measure, exact Pin+_14/eta),
  `PHYSICAL-C-CONDITIONAL/BOUNDARY`, `TRIT-INTERPRETATION`, `MIRROR-NATIVE-ROUTE` — all **STAY**
  as banked static results. Preserve their reopen conditions verbatim; they are boundary data DU
  consumes, not verdicts to re-run.

---

## THREAD 4 — Hand-off to Dynamic Unity (moves + shared)

### 4.1 What MOVES to DU (forward work relocates; GU keeps the frozen artifact as substrate)
These are **dynamic exploration threads**, not existing portfolio work_items — which is exactly
why DU is being stood up. For each, GU retains its frozen docs read-only as consumed substrate;
the forward work is DU's.

| Dynamic thread (current GU exploration home) | DU destination | GU-side grade CONSUMED (not moved) |
|---|---|---|
| **Tachyon-transducer** (source action → tachyonic `N=e^{4p}` roll → record-accretion → DE sign+magnitude+arrow as one object; roll↔accretion computation; the mirror-sector vacuum W224 gap) | DU **Channel 1.1** | unbuilt / B5-gated; W157 centrality = coincidence-grade; W224 singlet input-failure |
| **DE amplitude via `Λ ~ 1/√N`** (native derivation of `φ = 1/(3·Ω_Λ)²`) | DU **Lane 2.1** (the distinctness bank) | DE amplitude currently a **pure import**; `PRED-NORM-RANK = RESOLVED_NO_GO` (no GU-native absolute scale) |
| **Record-accretion / volume→Λ link** | DU **Channel 1.1** | roll↔accretion identification = **conjecture** |
| **Finality / issuance bridge** (issuance grounding of the DE magnitude; `N`-growth "does anything cross", drawn from TaF/TI) | DU (draws TaF `λ_max=1/t_obs` + TI issuance) | GU↔TaF↔TI identity is a **hypothesis to test in DU**, not a granted premise |
| **Dynamic tension-reframe sweep** (make each GU tension dynamic) | DU **Channel 1.2** | consumes GU's static tension ledger (§3.4), re-verified |

**Concrete update (PROPOSED):** GU does NOT create nodes for these; it adds a one-line pointer in
its front-door / steward log that the dynamic frontier is owned by DU, and that any hourly
"dynamic" idea (making a static result dynamic) is **out of GU scope** — route to DU Discovery.
GU's `PACKET-AND-MAILBOX-INTAKE` (Lane A) accepts frozen DU packets on the same seam it uses for
P2C; DU re-verifies GU substrate rather than importing GU grades on say-so.

### 4.2 What is SHARED (GU keeps running it; it feeds DU/P2C; home coordinated with P2C)

| Node | Current state | Why shared / home |
|---|---|---|
| **B5-INDEPENDENT-RECONSTRUCTION** | **READY**, Lane-1 lead, `hourly_eligible` | **The shared blocked spine.** GU runs the operator-grade acyclization swing + native-vs-forced adjudication; **P2C** supplies the discrete-GfE / boundary-action `σ`-habitat ingredient (transferred source-action lane); **DU** consumes the result (its Channel 1.3). Honest ceiling: operator-grade **recovering** action (lifts six legs to R1-forced), **NOT** native/unique (external-`σ` theorem bars uniqueness). Discharging SRC-COH-1 here forces the parity co-variance prediction (§2.2). Home coordinated with P2C. |
| **B5-MIDDLE-DIFFERENTIAL** | **BLOCKED_SOURCE_GAP** (parked) | The *native-class* question (delimit Weinstein's exact class) — blocked on unreleased material; stays parked in GU. Runs alongside B5-INDEPENDENT-RECONSTRUCTION, not substituted. Shared spine. |
| **DEP-NATIVE-SOURCE-DATUM** | **GATED_P2C** | The Y14 `F_A` C2 spectral-section / source-action datum + native normalization — deepest open dependency; **P2C constructs**, GU retains native-vs-forced + grade authority, **DU** consumes. Shared. |
| **DEP-PROP1** | GATED_P2C | The positive-majorant "good-stable" definition; P2C owns the firewall alternative, GU retains scope wording. Shared awareness. |
| **DEP-W235-RECORD** | MONITOR | The favorable record-conserved bit — the **record-count mode `p`** DU consumes (charter §3). GU owns the conditional scope; cross-repo identity stays gated. Shared. |
| **Parsimony + tension ledgers** (§3.4) | active | GU owns the STATIC ledger; DU's Channel 1.2 consumes it for the dynamic reframe sweep. Shared at the consumption seam. |
| **The Krein-sign / signed-graph home** | Godel-independent one Z/2 posit | Per prior canon the `GU=TaF=TI` one-Z/2 signed-graph **home = P2C**; GU exports `σ` as the external bit. Shared; home coordinated with P2C. |

### 4.3 New Lane-A stewardship duty (the seam)
- **Concrete update (PROPOSED):** extend `PACKET-AND-MAILBOX-INTAKE` / `DAILY-PORTFOLIO-
  RECONCILIATION` to cover the **GU↔DU seam** on the existing frozen-packet channel (same
  provenance/ownership/native-vs-forced guards as the P2C seam): accept DU frozen returns, share
  GU static substrate to DU as consumed-and-re-verified (never as a granted premise), and reject
  any DU packet that converts a dynamic reframe into a GU grade change without a frozen packet.
  No new lane or channel (mirror the anti-duplication guard already in the canon).

### 4.4 Lane-1 lead re-designation after the split (steward reconciliation, pending)
- **Current state:** the dynamic frontier was part of what Lane 1 chased; with it gone, GU's
  Lane-1 **static** leads are `B5-INDEPENDENT-RECONSTRUCTION` (SHARED, `lane_one_lead: true`),
  the `OPERATOR-END-PENCIL` residuals + `ANOMALY-DESCENT-HARDENING` T1 (static hardening), and
  `FALSIFICATION-BATTERY` (fallback). The **`difficulty_demotion_reconciliation_flag`** is still
  open: the score formula subtracts effort+wall_risk and demotes Lane-1 North-Star items below the
  safe lattice item, contradicting `lane_one_is_protected_from_difficulty_demotion`.
- **Concrete update (PROPOSED, steward-only):** reconcile the flag (exempt Lane-1 North-Star items
  from the effort/wall-risk subtraction OR gate difficulty-driven demotion behind a steward note)
  so the static Lane-1 leads are actually pickable; confirm no Lane-1 node still points its
  "reopen" at the departed dynamic frontier (they point at source packets / p2c returns, which is
  correct). **No formula edit autonomously** — Lane-A steward reconciles.

---

## Consolidated stay / move / shared table (every portfolio node)

| Node | Lane | Current state | Disposition |
|---|---|---|---|
| RECOVERY-CERTIFICATION (+ NO-GO-SCOPE-CHALLENGE, RECOVERY-CONTRACT, SM-CONSISTENT-SECTOR, GR-DYNAMICAL-BENCHMARKS, QM-PHYSICAL-SECTOR, COSMO-PERTURBATIONS, ADAPTER-RETURN-CERTIFICATION) | 1 | ACTIVE (internals BOUNDED_NO_GO / CONDITIONAL_FAIL / GATED_P2C) | **STAYS** |
| CONSTRUCTION-SPACE-EXPLORATION (+ CH-QM/GR/COSMO/SM/REC/SRC/SIG-95/SIG-77) | 1 | WAITING_EXTERNAL | **STAYS** (CH-REC static co-flip stays; dynamic record-accretion → DU) |
| DEP-NATIVE-SOURCE-DATUM | 1 | GATED_P2C | **SHARED** (P2C builds, GU grades, DU consumes) |
| DEP-PROP1 | 1 | GATED_P2C | **SHARED** (GU keeps scope wording) |
| DEP-W235-RECORD | 1 | MONITOR | **SHARED** (record mode `p` consumed by DU) |
| PHYSICAL-C-CONDITIONAL | 1 | PARKED | **STAYS** |
| PHYSICAL-C-BOUNDARY | 1 | GATED_P2C | **STAYS** |
| MIRROR-NATIVE-ROUTE | 1 | RESOLVED_NO_GO | **STAYS** (banked no-go; DU consumes) |
| B5-MIDDLE-DIFFERENTIAL | 1 | BLOCKED_SOURCE_GAP | **SHARED** (native-class; parked) |
| B5-INDEPENDENT-RECONSTRUCTION | 1 | READY (lead) | **SHARED** (GU runs; P2C ingredient; DU consumes) |
| OPERATOR-END-PENCIL | 1 | RESOLVED_NO_GO | **STAYS** (σ external; residuals static) |
| ANOMALY-DESCENT-HARDENING | 1 | PARTIAL_ACTIVE | **STAYS** (T1 number = static hardening §3.2; T3 parity leg = static prediction §2.2) |
| TRIT-INTERPRETATION | 1 | RESOLVED_INTERPRETATION_SHAPE | **STAYS** |
| FALSIFICATION-BATTERY | 1 | MONITOR | **STAYS** |
| PRED-FLAVOR-RANK | 2 | RESOLVED_NO_GO | **STAYS** |
| PRED-NORM-RANK | 2 | RESOLVED_NO_GO | **STAYS** (the static DE-amplitude no-go DU consumes) |
| PRED-FLAVOR-OBS | 2 | RESOLVED_NO_GO | **STAYS** |
| FIXED-NATIVE-QUANTITY | 2 | GATED_NEW_STRUCTURE | **STAYS** (dynamic unlock route = DU) |
| BLIND-QUANTITATIVE-CONFRONTATION | 2 | GATED_FIXED_QUANTITY | **STAYS** |
| P-OBS-LEG | 2 | MONITOR | **STAYS** |
| PRED-CANDIDATE-PACKETS (PP1/PP2/PP3) | 2 | BLOCKED_SOURCE_GAP | **STAYS** (PP1/PP3 static); mirror candidate + DE/Planck-ratio & amplitude → **SHARED/→DU 2.2** |
| DE-AMP-DIAGNOSTIC | 2 | RESOLVED_NO_GO | **STAYS** (closed audit; DU consumes) |
| DE-F1-TRIPWIRE | 2 | MONITOR | **STAYS** |
| COSMO-CROSS-OBSERVABLE | 2 | PARKED | **STAYS** (dynamics route = DU) |
| PROOF-STABLE-KERNELS | 3 | READY | **STAYS** |
| PAPER-EXTERNAL-EYES | 3 | NEEDS_JOE | **STAYS** |
| PAPER-UV-GRAVITY | 3 | PARKED | **STAYS** |
| DAILY-PORTFOLIO-RECONCILIATION / PACKET-AND-MAILBOX-INTAKE / INTEGRITY-AND-NAVIGATION-MAINTENANCE / PAPER-SEED-ROUTING | A | ACTIVE/READY | **STAYS** (+ new GU↔DU seam duty §4.3) |
| *(dynamic threads — not portfolio nodes)* tachyon-transducer, DE-amp via 1/√N, record-accretion, finality/issuance bridge, dynamic tension-reframe sweep | — | exploration content | **MOVES→DU** (§4.1) |

**One-line summary.** GU keeps essentially its entire current portfolio — it *is* the static
substrate. The split **moves out** the dynamic exploration frontier (which had no portfolio
nodes), **shares** the B5 source-action spine + the source datum + the record bit + the tension
ledger + the signed-graph home with P2C/DU, and **adds** a GU↔DU packet-seam stewardship duty.
The concrete continuation work is: finish the paper hardening (Thread 1), keep the non-dynamic
prediction surface honest and hunt the parity leg (Thread 2), and drive the static results —
located-not-forced, the `σ`/anomaly/Pin cluster via the Track B engine build, the type wall,
the ledgers, the README/VERIFICATION uncontestability fixes — to their honest completion
(Thread 3).

---

## Boundary

Proposal/roadmap tier. **One new file written — this document.** GU otherwise read-only: no edit
to `research-portfolio.json`, `LANES.yaml`, `NEXT-STEPS.md`, `CANON.md`, any paper body,
`VERIFICATION.md`, or any other agent's artifact; **no commit, no push, no external action.**
Every portfolio state change is a **PROPOSED** update for the daily steward / Joe, applied to
nothing here. All physics grades are **consumed as found, not moved** — `σ`/`τ` external
(LC-SELECTOR), `PRED-NORM-RANK = RESOLVED_NO_GO`, the DE amplitude as pure import, the DE-sign
packets frozen/conditional, `|Ω^{Pin+}_14|` BLOCKED, ANOMALY-TRIVIAL inconclusive, the mirror
`BLOCKED_PRODUCIBILITY` fork, the parsimony lean narrow/conditional, per-leg hosts-not-derives,
B5-MIDDLE-SOURCE-GAP. The Dynamic Unity repo is **not instantiated** (name, creation, and
Weinstein-attribution are Joe's alone); this list assumes only the approved charter. No
claim-status, canon-verdict, paper-status, or public-posture change. Joe alone publishes.
