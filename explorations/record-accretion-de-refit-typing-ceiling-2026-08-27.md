---
title: "The proposed record-accretion dark-energy refit is a homonym until three owners are supplied"
status: exploration
doc_type: determination
created: "2026-08-27"
grade: "OWNER-SOURCE SEMANTIC ADJUDICATION; NO BACKGROUND REFIT OR COSMOLOGICAL VERDICT"
scripts:
  - tests/channel-swings/record_accretion_de_refit_typing_probe.py
target_claim: "M-H13"
target_claim_verdict: "HOMONYM_REFIT_NOT_LICENSED"
comparator_classification: INTERNAL_STRUCTURAL_ONLY
canon_verdict_change: none
---

# M-H13 record/DE typing ceiling

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: semantic adjudication of the proposed r(N(z)) dark-energy refit
carrier: W187 open-system self-energy ratio and W154 record-sourced FLRW mean LAYER=UNTYPED CHIRALITY=N/A
pairing: typed comparison of source object, map and target ON=current-owner-evidence
real_structure: real scalar coefficients with distinct physical roles
grading: premise correction; no likelihood or background result
action_owner: UNTYPED
target: eligibility of an M-H13 cosmology refit MAP-TYPE=not-a-map
```

Scope: this result decides whether the registered computation is presently
well typed. It does not decide whether some future record-coupled completion
can improve the dark-energy shape, and it changes no source, prediction, canon
or public verdict.

## The objects do not compose

W187 defines

```text
r = |Sigma_ext| / |Sigma_internal|
```

inside a finite Fano--Anderson/open-self-energy stand-in for whether an
opposite-Krein pole moves between basins. It explicitly says that only
`dr/dN > 0` is directionally native; `r(N)=kappa_0 sqrt(N)` is a model, and
`r` does not occur in the FLRW `H(z)` equations.

The actual record-sourced dark-energy mean in W154 is a different map,
`Lambda_mean=c/sqrt(N)`. With one-way accretion, `N` grows and this mean
decreases, giving monotone withdrawal and no zero crossing. W215's reciprocal
identity `r Lambda = const` does not identify the two codomains; it reinforces
that they carry opposite monotonicity.

There is one named conditional bridge: W230 proves that the connection
distortion aligns with `M^{-1}J` if and only if the fundamental kinetic
coefficient `c_kin` vanishes. That posit is unowned. Even after granting it,
the repository has no owned backreacted `N(z)` and no decision whether the
count changes the theta amplitude, mass, kinetic term or another coefficient.

Therefore inserting W187's `r(N)` directly into the existing background would
be a homonym refit, not a test of GU-owned dynamics.

## Disposition and reopen condition

M-H13 is `PREMISE CORRECTED` at the current evidence ceiling. A numerical
background/`theta_star` refit becomes licensed only when one packet supplies:

1. an owned bridge from the record current/self-energy object to the FLRW
   dark-energy field;
2. an owned `N(z)` definition on the backreacted cosmology, including whether
   it is bulk four-volume or a boundary/confirmed count; and
3. the exact coupling target and normalization in the background equations.

The existing DES/DESI comparator penalties remain unchanged. The correction
removes an invalid rescue computation; it does not claim that every
record-coupled completion has the wrong shape.

`python3 tests/channel-swings/record_accretion_de_refit_typing_probe.py --selftest`
checks the source-object, model-grade, monotonicity, bridge and reopen fields and
catches five planted reference mutations.
