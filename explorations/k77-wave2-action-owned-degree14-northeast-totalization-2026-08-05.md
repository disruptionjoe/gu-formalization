# K77 Wave 2: action-owned degree-14 totalization and raw northeast rendezvous packet

Status: `PARTIAL CONSTRUCTION / TWO CANDIDATE KILLS / NEXT CLOSURE BURDEN SHARPENED`

This wave advances the recorded gate without claiming Wave-2 completion.  It
constructs the exact formal adjoint of the selected degree-two Shiab on the
complete `Omega^2 tensor Cl^2` carrier; types the action-owned degree-14
quantity as the full even Noether totalization; proves that the fermion
current cannot own the raw cyclic northeast block; and tests the simplest
source-natural degree-three Shiab that could move that block into degree 14.
The simple degree-three candidate fails on the complete rank-91 Riemann
principal carriers because it erases traceless Ricci.  The source does not
publish a preferred degree-three Shiab, so the general rendezvous remains
open.

No external datum, physics row, canon verdict, lane count, or public posture
moves.

## 1. Layer 0: which objects are being compared?

| name | typed object | role | not the same as |
| --- | --- | --- | --- |
| selected Shiab `S` | `Omega^2(Y,Cl^2) -> Omega^13(Y,Cl^1+Cl^5)` on the tested K77 carrier | degree-two curvature contraction selected by principal Bianchi plus Einstein response | a degree-three Shiab or a scalar action |
| formal adjoint `S^!` | `Omega^1(Y,Cl^1+Cl^5) -> Omega^12(Y,Cl^2)` under the top-form/Clifford-scalar pairing | the map required in the action Euler derivative | a positive Hilbert adjoint or a closed physical Krein operator |
| action Euler `E_act` | an `Omega^13(ad^*)` connection/torsion Euler density | derivative of the source-shaped action | the printed endpoint `Upsilon`, its redundancy `Xi`, or a Noether identity |
| even Noether totalization `N_g` | `Omega^14(ad^*)` after integration by parts against a zero-form gauge parameter | full off-shell gauge identity of the action | `D_B E_act` alone |
| raw northeast `N_NE` | `-T wedge F_B in Omega^3(ad)` in the unreleased cyclic two-connection square | a square/complex obstruction component | `J_D+J_F in Omega^13(ad^*)` |
| degree-three Shiab candidate `S_3` | `Omega^3(ad) -> Omega^14(ad)` | possible route from `N_NE` into the degree-14 arena | the already-selected degree-two Shiab |

The word *owner* therefore has two different meanings that must not be
collapsed: action ownership means “appears from varying an action,” while
complex ownership means “appears as a block of the rolled cyclic square.”
The wave asks whether a source-natural map connects those arenas; it does not
assume that sharing degree 14 makes the resulting objects equal.

## 2. Inline divergent preassessment

Ten lightweight lenses were run before computation.

| lens | highest-information demand |
| --- | --- |
| differential geometry | derive the full gauge-parameter coefficient after integration by parts; do not call `D E` the Noether identity |
| Clifford representation theory | decompose the selected Shiab image by Clifford grade before defining its adjoint domain |
| variational PDE | construct only a formal compact-core adjoint first and retain the Green/domain burden |
| gauge/BV engineering | include every transforming field, dependent moving Shiab, and background response in the Ward owner |
| source archaeology | check the general Shiab degree law and whether a degree-three member is actually supplied |
| exact algebra | use the complete finite carrier, independent arithmetic, and scalar/Ricci/Weyl plants |
| symplectic/BFV geometry | keep an ambient top-form identity distinct from a physical preboundary phase space |
| statistics/ML | do not fit a surrogate: the candidate family is finite and exact arithmetic is cheaper and more informative |
| topology/external datum | do not spend P1/P2/P3 to repair a missing local operator |
| epistemic systems | propagate candidate kills and residual burdens to owner surfaces without promoting the exciting reading |

The preassessment selected one sequence: source collision, exact selected
adjoint, total Noether typing, raw-map rank, degree-three candidate test, then
hostile review.

## 3. Mandatory primary-source collision

The primary material both supports and limits the construction.

1. Portal `01:34:34-01:38:38` supplies pure-trace `Phi_i` for all grades and
   types the general suite as
   `Omega^i(ad) -> Omega^(d-3+i)(ad)`.  Thus in `d=14`, a degree-three Shiab
   has exactly the required `Omega^3 -> Omega^14` type.
2. Portal `01:36:44` allows contraction or wedge on form degree and
   commutator or `i` times anticommutator on coefficients.
3. Portal `02:01:28-02:02:01` explicitly invokes `d_A^*`, the adjoint of the
   Shiab, and a Shiab/adjoint-Shiab second-order combination.  This confirms
   that constructing `S^!` is source-directed rather than an orthodox
   substitute.
4. Portal `02:03:07` says stress energy *should* be an up-and-back term while
   the required cancellations were still being assembled.  It does not
   identify the later raw northeast block with stress energy.
5. Portal `02:33:43` says there are many Shiabs and that the desired target
   determines a bespoke contraction.  It supplies no preferred degree-three
   formula.
6. TOE 2025 `02:38:12-02:45:43` describes the rolled `0 -> 1 -> 13 -> 14`
   picture and an unreleased cyclic square for two connections.  Weinstein
   explicitly says that construction had not been released.

Disposition: `SOURCE-CONFIRMS` the adjoint and degree-three search arena;
`SOURCE-SILENT` on the degree-three selector and the identity of the raw
northeast owner.  The source therefore authorizes a conditional search, not a
retrospective attribution.

## 4. Exact selected Shiab and its formal adjoint

The selected displayed product is

`SELECTED = (comm, symi, symi)`.

On the complete spin-curvature carrier

`Omega^2 tensor Cl^2`, of dimension `91 * 91 = 8,281`, the exact sparse map
has:

| quantity | exact result |
| --- | ---: |
| nonzero input columns | `8,281 / 8,281` |
| nonzero matrix entries | `63,336` |
| distinct output coordinates | `10,206` |
| total rank | `1,197` |
| `Cl^1` projection rank | `196` |
| `Cl^5` projection rank | `1,001` |

The `Cl^1` rank is exactly the entire `Omega^13 tensor Cl^1` receiver
dimension, `14 * 14 = 196`.  But it is not the whole image: a generic `Cl^2`
curvature also forces a rank-1001 `Cl^5` sector.  On the algebraic Riemann
slice the predecessor sees the Einstein response and kills Weyl; away from
that slice, an Einstein-only receiver is not closed under the selected map.

Use the nondegenerate bilinear pairing

`B_p(alpha,beta) = [alpha wedge beta]_top * Sc(alpha_Cl beta_Cl)`.

If `A_qp` is a selected-Shiab matrix entry, with diagonal basis-pairing
weights `w_in(p)` and `w_out(q)`, define

`(S^!)_pq = A_qp * w_out(q) / w_in(p)`.

All weights are `+1` or `-1` in the K77 basis, hence invertible.  The probe
checks this identity for all `63,336` nonzero entries:

`B_13(S e_p, e_q) = B_2(e_p, S^! e_q)`.

Therefore `S^!` has the same exact rank `1,197` by invertible diagonal
transpose.  This is a finite real-bilinear/formal adjoint.  It is not yet a
positive Riesz map, an analytic closure, a boundary-condition theorem, or the
full physical Krein adjoint.

## 5. The action-owned degree-14 object

For a gauge-invariant action with transforming fields
`(B,T,epsilon,Z,...)`, the action-owned top-degree relation is schematically

`N_g = D_B^coad E_B + ad_T^top E_T + rho_epsilon^top E_epsilon + rho_Z^top E_Z + backgrounds + ghosts = 0`,

up to fixed left/right and Green-sign conventions.  Every term is the
coefficient of the same infinitesimal gauge parameter after integration by
parts.  The previous exact even-Ward fixture already verifies the simultaneous
`B`, `T`, moving-Shiab/epsilon and current response, including `J_D+J_F`
exactly once.

Two consequences follow.

1. `D_B E_act` is only one summand and cannot be relabeled as the full
   Noether identity.
2. `Xi=D Upsilon`, a source-displayed redundant Euler relation, is still not
   automatically this action-derived off-shell identity.

This is a typing and ownership result.  A native global analytic Ward/BV
closure remains open.

## 6. Raw northeast and the fermion-current kill

The raw cyclic northeast map with tautological `T=Phi_1` is

`F |-> -Phi_1 wedge F`.

On the same complete `Omega^2 tensor Cl^2` carrier it has:

| quantity | exact rank |
| --- | ---: |
| total | `8,281` |
| `Cl^1` projection | `5,096` |
| `Cl^3` projection | `8,281` |

It is therefore injective, not a disposable zero block.  On every positive,
negative, and null rank-91 principal-Riemann bank it retains rank 91.

`J_D+J_F` cannot universally own this block:

- it is an `Omega^13(ad^*)` connection Euler density, while the raw block is
  an `Omega^3(ad)` square component;
- the raw block remains nonzero when the fermion is set to zero, while the
  fermion current vanishes.

This kills the direct identification.  It does not kill a later
action-derived bosonic map that sends the raw block into the Ward arena.

## 7. Minimal degree-three Shiab test

The cheapest source-natural candidate uses

`Phi_3 = (1/6) Phi_1 wedge Phi_1 wedge Phi_1`

and the two coefficient products allowed by the source:

`S_3^comm(U) = Phi_3 [product_comm] *U`,

`S_3^symi(U) = Phi_3 [product_i-anticomm] *U`.

These have the correct `Omega^3 -> Omega^14` degree.  On the generic raw
northeast image their exact ranks are `1,092` and `1,093`, so neither is a
vacuous zero map.  The decisive Riemann controls are different:

| carrier | raw rank | comm rank | symi rank |
| --- | ---: | ---: | ---: |
| positive principal bank | `91` | `0` | `1` |
| negative principal bank | `91` | `0` | `1` |
| null principal bank | `91` | `0` | `1` |
| scalar fixture | live | `0` | `1` |
| traceless-Ricci fixture | live | `0` | `0` |
| Weyl fixture | live | `0` | `0` |

The commutator candidate erases the complete Riemann carrier.  The
`i`-anticommutator retains only a scalar trace and erases traceless Ricci.  It
therefore cannot be a universal northeast-to-Einstein/stress-energy owner.

This is a candidate-map kill, not an ultimate GU kill.  Portal explicitly
says there are many bespoke Shiabs, and the released sources do not identify
the relevant degree-three member.

## 8. Independent arithmetic and controls

Sage 10.9 independently reconstructs the exterior and `Cl(7,7)` products
without importing the Python probe.  It verifies:

- full raw-map rank `8,281` over `GF(1,000,003)`, which proves rational
  injectivity because the rank equals the number of columns;
- `(91,0,1)` raw/comm/symi ranks on all three principal-Riemann orbit banks;
- on a 91-column Gaussian-rational selected-Shiab slice, both `Cl^1` and
  `Cl^5` output grades and every formal-adjoint entry identity.

Planted controls reject:

- support substituted for rank;
- `D E` substituted for a full Ward identity;
- a degree-13 current substituted for a degree-3 square block;
- a scalar-only degree-three response called an Einstein/stress-energy map;
- the formal adjoint promoted to a positive closed physical operator;
- external datum used to manufacture missing local geometry.

## 9. Constraint surplus

There are no fitted continuous parameters in this wave.  The selected
degree-two product was inherited from the prior eight-row source grammar and
principal-Bianchi selection.  The tested degree-three family contains exactly
the two source-permitted coefficient products for the minimal pure-trace
`Phi_3` ansatz.  It faced three covector-orbit banks plus scalar,
traceless-Ricci, Weyl, degree, zero-fermion, and independent-arithmetic
controls.  Both candidates fail before any parameter fitting is available.

## 10. What moved and what remains open

Moved:

1. the selected degree-two Shiab now has an exact full-image formal adjoint;
2. the hidden rank-1001 `Cl^5` closure burden is explicit;
3. action-owned degree 14 is correctly assigned to the full even Noether
   totalization;
4. direct `J_D+J_F` ownership of the raw northeast is killed;
5. the raw northeast is proved injective on the complete carrier;
6. the minimal pure-trace degree-three owner is killed on the Riemann sector.

Still open:

- the full odd-Clifford action closure generated by the selected Shiab;
- a source-natural degree-three Shiab basis beyond the minimal `Phi_3` trace;
- a common map making the raw northeast and action Noether systems commute;
- the exact moving-epsilon, Green, background and boundary terms on the native
  global domain;
- observation/no-leakage and every physical row.

Next gate:

`K77_FULL_ODD_CLIFFORD_ACTION_CLOSURE_AND_SOURCE_NATURAL_DEGREE3_SHIAB_NORTHEAST_NOETHER_RENDEZVOUS`

Wave 3 remains closed.  P1/P2/P3 remain unchanged and unused.  Curt remains
formally separate inside the Eric lane, and `TG-1 AND TG-2 AND TG-3` remains
not promoted.
