---
title: "Eric/Curt Wave 3D-B2C15P: source-epsilon tangent, distortion reduction, and exact Zorro/DeWitt jet"
status: active_research
doc_type: construction_result
created: 2026-08-02
branch: agent/null-clifford-omega1-repair
run: historical-investigation
registry: lab/process/eric-curt-wave3d-b2c15p-source-epsilon-tangent-zorro-dewitt.json
probe: tests/channel-swings/eric_curt_wave3d_b2c15p_source_epsilon_tangent_zorro_dewitt_probe.py
grade: "B2C15P PARTIAL CONSTRUCTION PASS. The direct source-epsilon to repository-reduction tangent bridge is exactly absent: Hom_H(Lambda2, Lambda3+Lambda6+Lambda7+Lambda10+Lambda11+Lambda14)=0. A stronger source-shaped route survives through the homogeneous distortion delta T=alpha-D_A zeta: Alt(delta T) plus its Hodge dual gives a two-parameter, H-equivariant, tilted-descended rank-364 candidate for every nonzero coefficient pair into quotient grades 3 and 11, with a 7801-dimensional cokernel and no selected coefficient ratio. The literal odot_epsilon branch remains separate from a conditional genuine odot_omega branch; any nonzero D_varpi odot contributes to the existing varpi Euler equation and an exact zero-order bulk-plus-preboundary witness passes. The old blockdiag(h,D_h) gimmel comparator is upgraded to an LC-horizontal connection metric G_Y=h+D_h(theta,theta) at an exact constant-curvature normal base three-jet. The full local G,dG,d2G, Levi-Civita, Riemann, and spin-curvature jets pass metricity, torsion, Riemann, Bianchi, observation-pullback, trace-reversal, and exact active reality checks. The product surrogate fails 40 second-pullback-jet components. The reconstructed split/trace/spin-curvature tuple has diagonal-Spin stabilizer 6 inside 91. The coordinate formula is reconstruction-grade, not source-unique; the full Shiab/action coefficient, source real-form family, atlas, BV, domain, vacuum, and physics remain open."
canon_verdict_change: none
---

# B2C15P source tangent, distortion reduction, and Zorro/DeWitt jet

## Result first

B2C15P closes one false route, opens one narrower constructive route, and
replaces the old total-space metric surrogate with an exact curved local
connection-metric jet.

The false route was the most direct reading of the shared epsilon notation:

\[
\delta\epsilon\in\mathfrak h
\quad\longrightarrow\quad
\delta\epsilon_{\rm red}\in\mathfrak m=\mathfrak g/\mathfrak h.
\]

In the active Clifford decomposition,

\[
\mathfrak h=\Lambda^2V,
\qquad
\mathfrak m=
\Lambda^3V\oplus\Lambda^6V\oplus\Lambda^7V
\oplus\Lambda^{10}V\oplus\Lambda^{11}V\oplus\Lambda^{14}V.
\]

The exact quadratic Casimirs are `24` on \(\mathfrak h\) and
`33,48,49,40,33,0` on the six quotient summands. Therefore

\[
\boxed{\operatorname{Hom}_H(\mathfrak h,\mathfrak m)=0.}
\]

There is no canonical nonzero equivariant linear zero-jet bridge. This is not
a no-go for every source-derived reduction. It says the construction must use
more than epsilon alone.

The source already supplies exactly such a larger object:

\[
T_\omega=\varpi-\epsilon^{-1}D_0\epsilon,
\qquad
\delta T=\alpha-D_A\zeta.
\]

This is the right-trivialized local formula

\[
\zeta=\epsilon^{-1}\delta\epsilon,
\qquad
A=q_g(\epsilon)=\epsilon^{-1}D_0\epsilon.
\]

It is not a global formula for an arbitrary independently chosen \(A\).

Unlike \(\zeta\) alone, \(\delta T\) annihilates the tilted graph. Alternation
and Hodge duality then give the concrete candidate

\[
\boxed{
\mathcal L_{c_3,c_{11}}(\delta T)
=c_3\operatorname{Alt}(\delta T)
+c_{11}*\operatorname{Alt}(\delta T)
\in\Lambda^3V\oplus\Lambda^{11}V\subset\mathfrak m.
}
\]

This candidate is exact, source-shaped, and nontrivial. It is also sharply
bounded: for every `(c3,c11) != (0,0)` it has rank `364`, kernel `910`, and
cokernel `7801` in the full `8165`-dimensional quotient. The zero pair has
rank zero and cokernel `8165`. Symmetry permits both independent coefficients
and does not choose their ratio.

The geometry result is similarly constructive. The previous product metric

\[
G_{\rm prod}=\operatorname{diag}(h,D_h)
\]

is a valid algebraic/product-gauge comparator but is not the curved Zorro
metric. The LC-horizontal candidate is

\[
C_{i,ab}
=\Gamma^c{}_{ia}(g)h_{cb}+\Gamma^c{}_{ib}(g)h_{ac},
\qquad
\theta_{ab}=dh_{ab}-C_{i,ab}dx^i,
\]

\[
\boxed{
G_Y(g)=h_{ij}dx^idx^j+D_h(\theta,\theta).
}
\]

At an exact constant-curvature normal base three-jet, B2C15P constructs its
complete pointwise metric two-jet, Levi--Civita connection, Riemann tensor,
and spin curvature. The defining discriminator passes:

\[
\boxed{s_g^*G_Y(g)=g}
\]

through second order. The product surrogate fails in `40` second-jet
components on the same curved input.

## Plain English

Eric's epsilon and the repo's reduction field were being asked to do two
different jobs. Eric's epsilon moves inside the gauge group. The repo's field
moves the chosen fourteen-plane through a much larger quotient. There is no
natural linear rule that turns the first motion into the second.

But epsilon does not occur alone in Eric's action. It occurs together with
the connection potential in their difference, the distortion. That
difference can produce part of the missing reduction motion in a natural way.
It reaches a real `364`-dimensional slice, rather than all `8165` directions.
So the useful conclusion is not “the source cannot build the reduction.” It
is “the first source-native piece is grades 3 and 11, and the remaining
directions and the coefficient ratio now have exact burdens.”

The metric correction is equally important. A block metric has the right
signature but forgets that Levi--Civita tells us how the metric fibres are
carried horizontally over spacetime. Putting that horizontal connection back
in restores the exact observation identity. This is the first local curved
Zorro/DeWitt jet in this campaign rather than another compatible metric
surrogate.

## Layer 0

| object | type | disposition |
| --- | --- | --- |
| source `epsilon` | local `H`-valued gauge transformation | source explicit |
| source tangent `zeta` | `Omega^0(ad P_H)`, conditionally `h` after the real-form port | source/reconstruction typed |
| tilted tangent | `(zeta,D_A zeta)` in the tilted-subgroup graph | source pattern and repo exact |
| repository `epsilon_red` | section of `P_G/H` | repository construction |
| reduction tangent | associated `m=g/h` section | repository exact |
| source distortion | `T=varpi-q_g(epsilon)` | source explicit |
| distortion tangent | `delta T=alpha-D_A zeta` with `zeta=epsilon^-1 delta epsilon` and `A=q_g(epsilon)` | tilted-descended local source chart |
| direct epsilon bridge | `h -> m` | exactly zero at equivariant zero-jet level |
| distortion bridge | `V* tensor Lambda2 -> Lambda3 plus Lambda11` | constructed candidate; partial and unselected |
| product gimmel | `blockdiag(h,D_h)` | exact product-gauge comparator |
| Zorro connection metric | `h+D_h(theta,theta)` | canonical local reconstruction |
| selected curvature reality port | exact `(9,5)` spin-curvature tuple | passes all active word identities |
| full source/action real-form port | complete `(7,7)` family to active right-H/Krein action | open |

The tilted inhomogeneous-gauge quotient and the finite Clifford-plane quotient
are not the same quotient. Their shared use of epsilon or quotient language
does not identify their tangents.

## Source collision

- `SOURCE-CONFIRMS`: draft pp.43--44 and pp.56--57 own
  \(\omega=(\epsilon,\varpi)\), the source coordinates, and
  \(T=\varpi-\epsilon^{-1}D_0\epsilon\).
- `SOURCE-CONFIRMS`: TOE `02:19:49` states the tilted homomorphism and
  double-coset construction.
- `SOURCE-CONFIRMS`: equations 9.2--9.3 display epsilon-only Shiab syntax.
  Direct inspection then `REPOSITORY-DERIVES`
  \(D_\varpi\odot_\epsilon=0\) only for that literal formula.
- `SOURCE-SILENT`: whether equation 9.4's abbreviated
  \(\odot_\omega\) is that literal family or a genuinely
  varpi-dependent family.
- `SOURCE-CONFIRMS`: Portal/Oxford `02:23:30--02:23:52` states the chain
  metric \(\to\) LC \(\to\) gimmel \(\to\) spin connection.
- `SOURCE-CONFIRMS`: TOE `00:26:28--00:29:16` requires trace reversal in
  the Frobenius fibre.
- `SOURCE-CORRECTS`: the tilted source tangent and the finite
  Clifford-plane reduction tangent are homonyms.
- `SOURCE-SILENT`: the distortion alternation bridge, its coefficient ratio,
  the coordinate normalization or uniqueness of the connection metric, and
  the full native Shiab/action port.

## Direct epsilon bridge: exact obstruction

### Equivariant Hom gate

For \(n=14\), the exterior-power quadratic Casimir is \(k(14-k)\). Thus

```text
Lambda2:   24
Lambda3:   33
Lambda6:   48
Lambda7:   49
Lambda10:  40
Lambda11:  33
Lambda14:   0
```

No quotient summand has the \(\Lambda^2\) eigenvalue. The exact active
representation therefore contains no copy of \(\mathfrak h\) in
\(\mathfrak m\). Every one of the `91` direct grade projections is zero.

### Supplied-background plant

The executable `SO(3)/SO(2)` plant uses

\[
\mathfrak h=\langle J_{12}\rangle,
\qquad
\mathfrak m=\langle J_{13},J_{23}\rangle.
\]

At the canonical coset basepoint,

\[
\operatorname{pr}_{\mathfrak m}(J_{12})=0.
\]

After supplying a quarter-turn \(u\) in the `13` plane,

\[
\operatorname{pr}_{\mathfrak m}
(\operatorname{Ad}_{u^{-1}}J_{12})
=\pm J_{23}\ne0.
\]

The plant proves that the nonzero tangent came from the supplied reduction
background. It cannot be used to derive that background without circularity.

Separately, Whitehead's lemma rules out a nontrivial
\(H^1(\mathfrak h,\mathfrak m)\) class for semisimple \(\mathfrak h\). This
corroborates the need for supplied background data but is not the proof of the
Hom gate. The `SO(3)/SO(2)` plant also does not instantiate Whitehead's lemma,
because \(\mathfrak{so}(2)\) is not semisimple.

## Distortion-derived partial reduction

Alternation is the natural map

\[
\operatorname{Alt}:V^*\otimes\Lambda^2V\longrightarrow\Lambda^3V.
\]

The probe checks `91 x 1274` generator/basis combinations for both
\(\operatorname{Alt}\) and \(*\operatorname{Alt}\). Every equivariance
identity passes exactly.

The rank ledger is:

| map | rank | kernel | cokernel in full `m` |
| --- | ---: | ---: | ---: |
| pointwise `Alt + star Alt`, `(c3,c11) != (0,0)` | 364 | 910 | 7801 |
| zero coefficient pair | 0 | 1274 | 8165 |
| fixed nonzero conormal symbol and nonzero coefficient pair | 78 | 13 | 8087 |

Both coefficient choices `(c3,c11)=(1,0)` and `(0,1)` obey the symmetry.
Consequently symmetry alone supplies a two-parameter family, not a selected
map. A future surplus calculation must constrain the ratio on held-out action
and observation identities rather than choose it to fit a desired reduction.

The strongest current local candidate is

\[
\epsilon_{\rm red}(T)
=\exp\!\left(
c_3\operatorname{Alt}(T)+c_{11}*\operatorname{Alt}(T)
\right)H.
\]

This formula is conditional on the active real-form port and only describes a
`364`-dimensional submanifold. It is not stated by Weinstein and is not yet a
full reduction.

## Literal versus genuine omega contraction

B2C15O selected the literal equations-9.2-to-9.3 branch:

\[
D_\varpi\odot_\epsilon=0,
\qquad
E_\varpi=E_T.
\]

B2C15P retains that branch and separately prices a genuinely
omega-dependent family. If

\[
\Sigma_\varpi=D_\varpi\mathscr P,
\qquad
\Pi_{\mathscr P}[\dot{\mathscr P}]
=\langle T,\dot{\mathscr P}C\rangle,
\]

then

\[
\boxed{
E_\varpi^\Omega
=E_T+\Sigma_\varpi^!\Pi_{\mathscr P}.
}
\]

Here \({}^!\) is the formal adjoint for the action's actual density/Krein
pairing. The displayed one-slot formula assumes every other varpi-dependent
coefficient is fixed. In general,

\[
\boxed{
E_\varpi
=E_T+\sum_r(D_\varpi r)^!\Pi_r,
}
\]

where `r` runs over every moving Shiab, density, Krein pairing, Hodge, and
lowerer slot.

This is a contribution to the existing varpi equation, not a second owner.
The preregistered noncentral rational insertion is zero-order: it has a
nonzero coefficient return, a zero-control on which the branches coincide,
and an exact bulk-plus-preboundary identity, but its isolated coefficient
Green term is exactly zero. The surrounding action supplies the nonzero
boundary in that fixture.

A second exact plant prices the differential case without identifying it with
equation 9.4:

\[
\Sigma=D_x,
\qquad
\Sigma^!=-D_x,
\qquad
\Theta_\Sigma=\Pi\,\delta\varpi.
\]

For \(\Pi=1+x^2\) and \(\delta\varpi=1+x\) on `[0,1]`,

```text
direct   =  4/3
bulk     = -5/3
boundary =  3
```

Omitting the isolated Green term or using the raw positive derivative fails.
Neither plant selects the actual equation-9.4 operator.

## Exact Zorro/DeWitt connection-metric jet

### Input

The fixture fixes:

- \(g_0=\operatorname{diag}(1,1,1,-1)\);
- normal base coordinates, so \(dg_0=0\);
- unit constant sectional curvature;
- \(\nabla R=0\), which supplies the bounded base three-jet needed to fix
  the second Christoffel jet;
- the trace-reversed DeWitt form

  \[
  D_h(p,q)
  =\operatorname{tr}(h^{-1}ph^{-1}q)
  -\frac12\operatorname{tr}(h^{-1}p)
            \operatorname{tr}(h^{-1}q).
  \]

The exact derivative formulas are

\[
D(h^{-1})[k]=-h^{-1}kh^{-1},
\]

\[
D^2(h^{-1})[k,l]
=h^{-1}kh^{-1}lh^{-1}+h^{-1}lh^{-1}kh^{-1}.
\]

They generate all `10` first and `100` second vertical DeWitt derivatives.

### Total-space result

The total metric has fibre inertia `(6,4)`, total inertia `(9,5)`, and trace
norm `-4`. The probe constructs:

- \(G_0\);
- all \(14\) coordinate derivatives \(D_AG\);
- all \(14\times14\) second derivatives \(D_AD_BG\);
- all pointwise Levi--Civita coefficients;
- the full pointwise Riemann curvature;
- its exact orthonormal-frame Clifford spin lift.

The controls pass:

1. all second DeWitt derivatives are symmetric in owner and fibre slots;
2. \(D_kC_i(g_0)=g_{,ik}\), the infinitesimal metricity identity;
3. \(j^2(s_g^*G_Y)=j^2g\);
4. curvature reconstructed from the pulled-back metric jet is the exact
   preregistered constant-curvature tensor;
5. total LC is torsion-free and metric-compatible;
6. Riemann internal skew, pair exchange, and first Bianchi all hold;
7. horizontal curvature is nonzero and differs from the product comparator;
8. the spin curvature has `71` nonzero external legs, all in Clifford grade
   two;
9. every one of those `71` legs satisfies the exact active right-quaternionic,
   Krein-skew, and `C+` word identities.

The last result is a real advance in the source-to-active port: this selected
curvature tuple survives. It is not yet a port of every source connection,
Shiab coefficient, Hodge/lowerer, variation, or action field.

### Product-surrogate kill

On the same curved base input,

\[
s_g^*G_{\rm prod}
=g+D_g(\partial g,\partial g).
\]

Although the extra term vanishes at the normal point and at first order, its
second jet is live. The planted product construction fails `40` exact
second-jet components. Signature agreement and point-metric agreement were
therefore insufficient.

## Actual selected stabilizer

The exact tuple consisting of

- the observed `4+10` split;
- the negative trace line; and
- the reconstructed spin-curvature two-form

has stabilizer dimension

```text
6 inside diagonal spin(9,5), orbit rank 85 inside 91.
```

The external two-form pullback action is essential: rotating only the
Clifford coefficients gives a different, hostile result. This `6` is not the
stabilizer of the full `8256`-dimensional `sp(32,32;H)` action jet, a conormal
family, a global field, or a stationary vacuum.

## External datum consequence

P1/P2/P3 remain unchanged and unused. P1/P3 and the currently instantiated
P2 packet supply no continuous tangent map, alternation coefficient, Zorro
jet, Shiab port, BV differential, or domain. Because P2's ultimate type is
still unknown, it cannot yet be ruled in or out by final type.

There is nevertheless a concrete opportunity for the datum ledger. P2 is the
still-unknown X-sector datum. If source distortion and its nonlinear
concomitants eventually generate the full reduction while the action fixes
the `(c3,c11)` ratio, part or all of P2's current burden may disappear. That
ledger change is not made here: the linear candidate still leaves a
`7801`-dimensional cokernel for every nonzero coefficient pair.

## What this does not claim

B2C15P does not claim:

- a direct epsilon-to-reduction identification;
- a selected distortion coefficient ratio;
- a full `8165`-dimensional reduction;
- identity of `odot_epsilon` and `odot_omega`;
- source uniqueness of the connection-metric formula;
- a complete source `(7,7)` to active `(9,5)` action port;
- the actual Shiab/Hodge/lowerer owner coefficient on the new curvature;
- a full ambient/action-jet stabilizer;
- a global atlas, BV quotient, Green domain, stationary vacuum, Standard
  Model recovery, generation count, or dark-energy prediction.

Curt remains formally separate inside the Eric lane. `TG-1 AND TG-2 AND
TG-3` remains `NOT_PROMOTED`.

## Next gate

The highest-information successor is

`ECW3D-B2C15Q-DISTORTION-REDUCTION-SUBSTITUTION-AND-NATIVE-ZORRO-SHIAB-OWNER-RETURN`:

1. substitute the two-parameter
   \(\epsilon_{\rm red}(T)\) candidate into the fixed-varpi source-coordinate
   owner system;
2. derive every new chain-rule Euler and Green return;
3. apply the actual Shiab/Hodge/lowerer chain to the reconstructed Zorro spin
   curvature;
4. pre-register a finite equivariant ansatz for `(c3,c11)` and any genuine
   \(\Sigma_\varpi\), including its free-parameter count, then use the
   compact source Euler expression and observation identities to compute the
   independent constraint-Jacobian rank and surplus; require a planted
   matcher plus held-out tests before fitting;
5. only if the linear candidate remains consistent, construct nonlinear
   distortion concomitants for the missing quotient grades and measure the
   residual datum burden.

The kill condition is explicit: do not call a nonzero partial rank-364 map a full
reduction, do not fit its coefficients to a desired answer, and do not reuse
the product gimmel or fixed-A metric return.
