# Cross-Repo Roster Synthesis: what lives BETWEEN the repos (2026-07-11)

Executed 2026-07-13 session. One worker, full roster inline (hard repo rule: no per-persona agents).
Roster: the five-archetype science advisory council (`science-advisory-council-full-picture-2026-07-11.md`)
+ the five-family two-track sweep (`two-track-persona-sweep-2026-07-11/`: A orthodox/rigor x14,
B geometric/structural x14, C foundations/observer x13, D wild-frontier x12, E pragmatic/experimental x12).
Inputs: `cross-repo-survey-taf-ti-2026-07-11.md`, `cross-repo-survey-others-2026-07-11.md`,
`VERIFICATION.md`, `observer-structure-theorem-assembly-2026-07-11.md` (incl. W110-W114 hardening),
`CONJECTURE-source-action-is-the-observer-2026-07-11.md`.

**Scope:** content only — bigger stories, connections, potential papers BETWEEN GU x TaF x TI (primary)
and ai-epistemology / architecture-of-legitimacy / church-of-ai (secondary). Not methodology, not
external-review process. **No canon/verdict/claim-status change anywhere; nothing here promotes anything.**

**Standing discipline carried throughout (non-negotiable):**
- The identity claim ("one object, three shadows") is NOT made by any repo and is not made here.
- Cross-repo material is stress-test input, never support (one-way rule, both directions).
- Same-designer confound (ai-epistemology C002: UNDEFEATED) is named wherever it applies.
- The tri-repo adapter record is **0-for-1** (T421 E3 swing: logged DISANALOGY — TaF operational
  recoverability and GU metric/grading selection "did not type-check as one functor").
- A connection is claimable only as a COMPUTED shared structure, never a narrative echo.

---

## 1. Per-family findings (condensed; each family's unique catch)

### A — Orthodox / rigor / gatekeeper family (14 voices)

- **Publishability triage.** Of everything on the candidate list, exactly two items clear a referee bar
  today: (i) the residual-selection two-instance package (GU's Lawvere arena/value theorem, L1, already a
  paper candidate self-graded NOVEL-PACKAGING + TI's kernel-checked Lean diagonal suite — both
  machine-checked NOW, no new construction needed); (ii) nothing else — every other candidate needs its
  earning computation first. The CMP/LMP referee (A8): the filtration→section functor finite instance
  (candidate b) is the only item a mathematics journal would accept as a *construction*; the rest are
  programs.
- **The "Lawvere in costume" objection, and its exact answer.** A referee will say applications of
  Lawvere's fixed-point theorem are cheap (Yanofsky catalogued dozens). The defensible surplus is the
  RESIDUAL-SELECTION content: not merely "no closure" but "the forced residual is typed and located"
  (GU: value = symmetry-breaking selection, non-circular partition W73; TI: the escaping diagonal is a
  concrete issuance object, `diagSem_escapes`). Yanofsky's catalog does not contain the typed-residual
  half. Modest but real.
- **Same-designer confound, partially defused for (a) only.** Because both instances are machine-checked
  against EXTERNAL mathematics (Lawvere/Yanofsky; Lean kernel), the shared core is Lawvere's, not the
  designer's. This defense works ONLY where a kernel or an external theorem anchors both sides; it does
  NOT defuse the confound for the sheaf-condition candidates (d), the mu candidate (e), or anything in
  the social-layer family (f) — there C002 stands undefeated and must be stated in any paper.
- **Operator algebraist (A13):** the mu/strip-cost identification (e) lives on the W98 Krein tower class
  and nowhere else yet. Every sentence about it must carry "model class." The Weinberg-transfer and
  HORN-K conditionalities of the Observer Structure Theorem propagate into any cross-repo use of it.
- **Statistics rigorist (A12):** none of the between-repo candidates has data contact except the
  Petermann-factor dictionary (W113), which is confidence-grounding, not evidence.

### B — Geometric / structural family (14 voices)

- **THE B-FAMILY UNIQUE CATCH — the two H^1's are different cohomologies.** Candidate (d)'s H1 (TI
  driving hypothesis: shared reality iff Cech H1 of the finality sheaf over the OBSERVER cover vanishes;
  GU W110: cocycle-on-triple-overlaps over the REGION cover) is *horizontal* — descent/gluing over an
  observer/region site. Candidate (b)'s `H^1(X, F_tau)` (the transient witness in the reciprocal bridge
  contract) is *vertical* — indexed by filtration stage. TaF's bridge contract EXPLICITLY forbids
  collapsing these ("double-diagram separation: horizontal = observer-domain descent/gluing, vertical =
  record/source filtration"). Any synthesis that treats (b) and (d) as one H^1 story commits precisely
  the error the contract was written to prevent. They are two computations, two candidate papers, and
  must stay separated.
- **Candidate (b) is the most buildable object on the board** because the three repos hold complementary
  pieces of one contractually pre-specified construction: TI holds the domain (`Compat_G^MLTT`, its
  strongest formal candidate, with kernel-checked stage structure E120/E121); TaF holds the machinery and
  the witness spec (T26 restriction systems, T16 finite gluing + obstruction witnesses, the
  transient-H^1 requirement, plus its own E054/E059-E061/E143 `FiltSh(C)` work on the TI side); GU holds
  the codomain-side identification (the selection-level map `{F_tau} <-> Sect` IS the Connes
  Radon-Nikodym cocycle, all-orders rigorous, positivity-free, W91/W97 — with rate-independence in
  theorem form, so the falsified "issuance rate" reading provably does not re-enter). No repo knew the
  other two pieces had landed. This is a genuine "closer than either repo alone knows."
- **Sheaf-theorist warning imported from TaF (T59):** a coefficient-blind scalar encoding produced a
  FALSE global section at the Möbius boundary. GU's W110 vanishing is coefficient-typed on its own model;
  any adapter to TI's finality sheaf must transport the coefficient typing or it will manufacture
  spurious "shared reality." This is the named failure mode of candidate (d) and the reason its earning
  computation must include the nonvanishing control (W110's 0.403-rad holonomy case) alongside the
  vanishing instance.
- **Category theorist (B8):** candidate (d)'s theorem-shape is a two-sheaf schema — a morphism of
  presheaves `F_op -> F_class` over the same site with `H^0(F_class) != 0` (global class exists) while
  `F_op` admits no global section (no global operator). Write THAT down abstractly and GU W110, TaF T21
  (Bell/CHSH local finality without global section), and TI-C022 (quorum continuity exceeding BFT
  integrity) become instantiation checks rather than analogies.

### C — Foundations / observer family (13 voices)

- **THE C-FAMILY UNIQUE CATCH — the level-split refinement flows GU -> TI as a stress test.** TI's
  central condition ("shared reality iff Cech H1 vanishes") is LEVEL-AMBIGUOUS as stated. GU's W110
  computed both halves on one instance: H1 vanishes for the interface CLASS presheaf (global class
  exists, coboundary identity < 1e-12) while no global OPERATOR exists at any level (clause 3,
  factorization-invariant per W111). So the answer to "does shared reality exist?" depends on the level
  at which the finality sheaf is valued: sections-of-classes YES, sections-of-operators NO — on the same
  world. Under the one-way rule this is not support for TI; it is a named refinement obligation: TI's
  driving hypothesis must type its H1 criterion by level before mu can make it computable. This is the
  sharpest single between-repo finding of the session.
- **Relational/RQM persona:** "facts relative, comparisons consistent, no God's-eye state" is Rovelli's
  program; the class-level global section without a global operator is structurally an RQM-shaped
  result. Cite as prior art in any paper — it deflates novelty and simultaneously grounds the claim in
  an existing literature (the novelty is the COMPUTED cohomological form, not the philosophy).
- **Quantum-foundations persona:** TaF already holds the quantum instance of the same split (T21 —
  Bell/CHSH local finality without global section, bounded), so the two-sheaf schema has a quantum leg
  computed independently of GU. Three computed instances (GU model tower, TaF Bell fixture, TI
  fork-discriminator) of one schema would be a genuine cross-layer theorem — IF the schema is written
  and the instantiations are checked, not narrated.
- **Philosopher of science:** the diagonal triptych (a) must be demoted honestly — TaF's T110 is an
  orbit/recurrence obstruction (edgewise nondecrease on a finite cycle forces constancy), NOT a
  diagonal/point-surjectivity argument. Same impossibility GENUS (self-maps + recurrence kill global
  grading), different theorem family. Forcing it into a Lawvere triptych would be exactly the narrative
  echo the discipline forbids. Honest shape: a PAIR (TI diagonal + GU Lawvere, same categorical core)
  plus a structurally distinct companion negative (T110), stated as such.

### D — Wild-frontier family (12 voices)

- **THE D-FAMILY UNIQUE CATCH — mu may already have a candidate density inside GU, with laboratory
  contact.** TI names mu (the reversal-cost issuance measure) "the central missing object," domain
  unspecified, decomposition hypothesized as Landauer + computational-irreducibility. GU's W109 adversary
  (b) computed a canonical COST FUNCTIONAL on value-selections: the Connes cocycle is unitary at every
  real t (zero cost on all states), and its -i/2 continuation has per-mode norm = sqrt(cond) = the strip
  cost EXACTLY, finite on exactly the sharp state class `D(|k|^{(p+1)/4})`. That is: GU already prices
  selection-finalization, per state, with a sharp finite/divergent boundary. Candidate identification:
  mu's domain = GU's selection space (arena/value theorem gives it structure: symmetry-breaking
  selections), mu's density ~ strip cost / Araki relative entropy (clause 1(iv): needs only
  `log Delta_rel` — and relative entropy is the information-theoretic side of Landauer, TI's one
  un-killed energy route, E140). And via W113 the conditioning IS the Petermann factor of EP photonics —
  measured physics. If mu is strip cost, TI's issuance measure has a laboratory-contacted instance. Wild,
  but every step named above is already computed on the GU side; only the identification is new.
- **The falsifiable fork inside (e):** TI's E043 proved SAX (strict distribution-free size-concavity) is
  formally distinct and logically independent from Shannon/entropy subadditivity. So if the mu-candidate
  IS relative entropy, either the identification fails E043's own discriminator, or the strip-cost
  functional has SAX-type behavior BEYOND entropy — a computable, three-outcome question on existing W98
  arrays. TI's discriminator can be run ON GU's object. All outcomes informative.
- **Protocol-stack resonance (kept subordinate):** TaF's observerse notes predict SG4 is a protocol
  STACK with S1 as consensus layer; GU's W110 is literally consensus-without-a-global-validator
  (agreement on the class ledger, no God's-eye operator). This folds into candidate (d)'s two-sheaf
  schema rather than standing alone; flagged so the observerse notes' ILLUSTRATION grade is not
  laundered upward through GU.
- **Regime-pinning consistency check:** TI pins SAX as the FTS-regime signature; TI's driving hypothesis
  makes GU the LAYER-1 fixed substrate. A fixed substrate is FTS-side, so SAX SHOULD hold on GU's
  selection space if the pipeline is coherent — the (e) computation doubles as a consistency test of the
  three-layer driving hypothesis itself.

### E — Pragmatic / experimental family (12 voices)

- **Ranked by cheap-and-decisive** (a cheap test that can kill outranks an expensive one that can only
  credit): (1) the E043-on-strip-cost run (candidate e) — days of numpy on EXISTING W98/W109 arrays,
  three-outcome, kills or narrows a central object of another repo; (2) the (d) adapter — cheap-medium,
  finite fixtures on both sides exist (W110 arrays; TaF T16/T59 fixtures), the T59 coefficient control is
  reusable; (3) the (b) finite-instance functor build — medium, but contractually pre-specified so
  scope-creep-resistant; (4) the (a) Lean lemma — medium, and the only one producing an immediately
  submittable artifact.
- **Portfolio note:** these four are uncorrelated with GU's own Track-1/Track-2 frontier (Bach/self-dual
  square, DE BAO likelihood) — cross-repo work adds a genuinely different risk book, not more exposure
  to the same illiquid asset.
- **Referee-for-PRD voice:** no between-repo candidate produces a physics NUMBER. Do not pitch any of
  this as phenomenology; the publishable venues are foundations / mathematical-physics / FoM.
- **The one external-data thread** is the W113 Petermann dictionary inside (e) — keep it as
  confidence-grounding language, never as a claim.

---

## 2. Candidate-by-candidate verdicts (the six posed + what the roster added)

| # | Candidate | Verdict | The one earning computation |
|---|---|---|---|
| (d) | Shared reality iff H1 vanishes (TI) vs W110 global-section computation (GU), refined by the class/operator level split | **THEOREM-SHAPED target** (currently productive-bridge grade) — the sharpest single connection; GU computed a model-grade instance of the FORM of TI's central condition, plus a refinement (level-typing) TI's statement lacks | Coefficient-typed adapter: express GU's W110 interface presheaf as an instance of the TI/TaF finality-sheaf schema (two-sheaf form `F_op -> F_class`), transport BOTH the vanishing proof AND the nonvanishing holonomy control, honoring T59's coefficient caution and the double-diagram separation. Success = the FIRST proven adapter contract (identity gate needs two). |
| (b) | Filtration<->section functor: GU's Connes-cocycle identification + TaF's `S: Compat_G^MLTT -> FiltSh(C)` contract + TI's kernel-checked domain | **PRODUCTIVE-BRIDGE** — three complementary pieces of one pre-agreed construction; closer than any repo knows | Build S on ONE finite instance: a small kernel-checked `Compat_G^MLTT` stage tower (TI) -> finite-site filtration sheaf (TaF T26/T16) -> a computed transient `H^1(X, F_tau)` class that R depends on and the final sheaf does not determine (the contract's own witness clause). Keep it VERTICAL — distinct from (d)'s horizontal H1. |
| (e) | TI's mu vs GU's value-selection space + strip cost | **PRODUCTIVE-BRIDGE**, highest upside — candidate domain AND density for TI's "central missing object," with a Landauer leg (relative entropy) meeting TI's one un-killed energy route | Run TI's own E043 SAX-vs-entropy discriminator ON GU's strip-cost functional over composed selections/modes of the W98 tower. Three outcomes: SAX holds beyond entropy (mu candidate survives, regime-consistency confirmed); reduces to entropy subadditivity (identification killed by TI's own discriminator); fails concavity (killed). |
| (a) | The diagonal pair/triptych | **THEOREM-SHAPED as a PAIR** (TI `diagSem_escapes` + GU Lawvere no-closure = same categorical core, both machine-checked); the T110 third leg is a DIFFERENT impossibility genus (recurrence, not diagonal) — triptych form is RHYME, drop it | One Lean lemma: a parametrized residual-selection form of Lawvere (point-surjectivity + fixpoint-free involution => no total symmetric valuation; forced typed residual), with BOTH instantiations derived from it in the same development (TI's suite is already in Lean; GU has a Lean-checked core to extend). T110 cited as companion negative, not instance. |
| (f) | Legitimacy-as-selection (ai-e T12-U/T13/T15 + AoL S7 monad + church-of-ai claim ladder) as social-layer arena/value | **RHYME at theorem level; salvageable core collapses into (a)** — the genuinely proved social-layer object is E038 (canonical founder-independent measure impossible; declared measure + robustness margin sufficient), which has the residual-selection SHAPE | Derive E038's impossibility as a literal instance of the (a) lemma (invariance argument over the acting group on valuations), or show it is not one. If it derives, (a) gains a third, proved-conditional instance at the epistemics layer — with C002 named as an undefeated limitation. If not, (f) drops entirely to essay grade. Note both TI and ai-epistemology carry a `lambda*(s)` optimum — same symbol, same designer; flag, do not use. |
| (c) | Shared boundary residue identity (GU typed slot at infinity / TI no-hidden-future-oracle / TaF projection-obstruction) | **RHYME-DROP as identity** — the adapter record is 0-for-1 here (T421 disanalogy), and GU's own W114 two-slots result shows even two INTRA-GU walls that rhymed are distinct slots (payload mismatch, only Kramers-parity meta-type shared). Keep only the classificatory program | Extend W114's payload-type invariant (filler admitted/forbidden; Z_2 vs 3; positive-metric-at-infinity payload) to TI's oracle residue and TaF's capability residue; compute pairwise match/mismatch. Expected outcome per precedent: mismatches — which is honest content (a taxonomy of typed boundary slots), and would upgrade the "shared residue" language across all three repos from shape-talk to typed classification. |
| (g) | [roster-added, folded into (d)] Consensus-without-validator: GU W110 + TaF T21 (Bell local finality, no global section) + TI-C022 (quorum surplus over BFT) as three computed instances of the two-sheaf schema | **Absorbed into (d)** — it is (d)'s instantiation set | Same as (d), plus the T21 fixture check as the quantum leg. |

---

## 3. Chairman synthesis

### (a) Ranked between-repo findings

1. **The H1 level-split (d).** GU's W110 computed a model-grade instance of the *form* of TI's central
   shared-reality condition — and refined it: Cech H1 vanishes at CLASS level while the OPERATOR-level
   global object provably fails to exist, on the same instance. TI's driving hypothesis is
   level-ambiguous as stated; this is a named, computable refinement obligation flowing GU->TI as
   stress-test input (one-way rule respected). THEOREM-SHAPED target; earning computation: the
   coefficient-typed adapter with both the vanishing instance and the holonomy control.
2. **The filtration<->section functor (b).** Contractually pre-agreed across all three repos; the pieces
   (TI domain, TaF machinery+witness spec, GU's Connes-cocycle identification of the selection quotient)
   have all independently landed. PRODUCTIVE-BRIDGE; earning computation: the finite-instance S with a
   transient vertical-H^1 witness. Strictly distinct from (d)'s horizontal H1 (double-diagram clause).
3. **mu = strip cost on the selection space (e).** GU already prices selection-finalization (sqrt(cond)
   = strip cost, per state, sharp class boundary, relative-entropy/Landauer leg, Petermann laboratory
   dictionary); TI's central missing object wants exactly a reversal-cost measure with a Landauer
   component. PRODUCTIVE-BRIDGE, highest upside; earning computation: E043's SAX discriminator run on
   the strip-cost functional. Doubles as a consistency test of TI's three-layer driving hypothesis
   (FTS regime-pinning of the GU layer).
4. **The diagonal PAIR (a).** TI's kernel-checked diagonal and GU's Lawvere no-closure are the same
   categorical core at two layers, both machine-checked TODAY — the only candidate with zero
   construction debt. THEOREM-SHAPED; earning computation: the single Lean residual-selection lemma with
   both instantiations. The T110 triptych extension is demoted (different impossibility genus).
5. **E038 as a possible third instance of (a) (the surviving core of (f)).** PRODUCTIVE-BRIDGE
   conditional on the derivation; otherwise the whole social-layer family stays a rhyme with C002
   undefeated.
6. **Boundary-residue classification (the surviving core of (c)).** Identity form RHYME-DROP; the
   payload-type taxonomy is the honest residue, with an expected-mismatch outcome that is itself
   content.

### (b) The single best cross-repo STORY/PAPER

**Best publishable paper today: the residual-selection pair** — "Self-referential admissibility forces
residual selection: one categorical core, two kernel-checked instances" (GU arena/value + TI diagonal
suite), upgrading GU's existing `papers/candidates/observer-value-selection-theorem/` from one instance
to two, at two layers (physics-model admissibility; issuance/enumeration). Honest grade: **sound (L1 on
both legs, machine-checked), modestly novel (novel-packaging conceded; the surplus is the typed-residual
content absent from the Yanofsky catalog), publishable in a foundations/mathematical-philosophy venue,
useful as the program's first genuinely cross-repo artifact.** The same-designer confound is partially
defused here and only here (both instances anchor to external mathematics through a proof kernel). Not a
blockbuster and should not be dressed as one.

**Best story pending one computation: the H1 level-split (d).** "Shared reality without a God's-eye
view: agreement is a cohomological condition, and it holds at the class level while provably failing at
the operator level" — with GU (model tower), TaF (Bell fixture T21), and TI (fork discriminator E044) as
computed instances of one two-sheaf schema. That is a genuinely big story IF the adapter computes
through.

### (c) Blockbuster assessment (deflation discipline applied)

The only blockbuster-class object is (d)'s story above. Deflators, all standing: (i) same research
process built all three repos — convergence is weak evidence, C002 undefeated, and unlike (a) there is
no external-kernel defense here; (ii) GU's H1 lives on the W98 model tower with its own coefficients —
site, sheaf, and coefficients all differ from TI's finality sheaf; (iii) TaF's T59 documents that
exactly this move (coefficient-blind section-gluing) manufactures FALSE global sections; (iv) the
tri-repo adapter record is 0-for-1 (T421 disanalogy); (v) RQM is prior art for the philosophical shape.
**Verdict: NOT claimable today. It is a well-posed adapter target with a bounded computation and the
best story-to-computation ratio on the board — a potential blockbuster only conditional on the (d)
adapter proving out, and per the standing gate, identity language would additionally require a second
proven adapter (which (b) or (e) could supply).**

### (d) Recommended next cross-repo move

**Run (e) and (d) first, in that order, as one short wave.** (e) is the cheapest decisive swing on the
board (days, existing arrays, TI's own discriminator turned on GU's object, three informative outcomes)
and simultaneously stress-tests TI's driving hypothesis regime-pinning. (d) is the sharpest connection
and, if the coefficient-typed adapter transports, becomes the first proven adapter contract of the two
the identity gate requires — while a partial failure (the likely outcome per T59/T421 precedent) sharpens
the coefficient-typing requirements that the (b) functor build needs anyway. (a)'s Lean lemma runs in
parallel as the submit-track artifact since it has zero construction debt. All moves respect the one-way
rule: every result is stress-test input to the receiving repo, never support.

---

*Grade of this file: synthesis/advisory over already-recorded results; no new computation; no claim,
verdict, canon, or posture change in this or any repo. Not committed by this run. Durable in-repo per
the dispatch spec.*
