---
artifact_type: exploration
status: exploration (W143; full-roster steelman sweep, family 5 of 5: APPLIED / COMPUTATIONAL / FRONTIER-WILD; all personas inline, one worker; one deterministic test)
created: 2026-07-14
wave: W143
title: "W143 -- steelman sweep, applied/computational/frontier family, on the corrected core claim: science ALREADY KNOWS the rate (W135: rho_Lambda ~ (2.24 meV)^4; q_B = 3 H0 rho_Lambda = (3/2) Omega_L c^5/G = 1.027 Planck luminosities per Hubble volume; dimensionless ladder 9 Omega_L = 6.16). Question per persona: IF that known measured number is DECLARED as the issuance input to the source action, what OTHER information backs out (spec FITs, scales, relations, mechanism constraints) for later rigorous checking. 28 personas, 52 scored stories, 6 survivors. Headline computed lines (tests/W143_controller_distribution_scoring.py, 24/24 exit 0): (1) the whole O(1) coincidence board DECOMPOSES through Omega_Lambda -- q_B/L_Planck = 3 Omega_L/2 = 1.027 (W135 re-derived), the W138 G5 factor 1.46 = 1/Omega_L exactly (T_dS S_dS = E_crit(V_H) = c^5/(2GH) is an identity), the W138 G6 saturation 2.96 = 3 Omega_L/ln2 exactly: under the known-rate declaration every de Sitter coincidence collapses into the SINGLE residual input Omega_Lambda; (2) an architecture separator inside W135's surviving PID-setpoint structure (computed): integral action forces zero steady-state error exactly, so setpoint-=-Lambda loops (W135 (e)) tolerate an I-term while residual-Lambda loops (setpoint = bulk-flatness beta/alpha* = 2, Lambda_obs = the proportional residual = the W136 1e-60 offset) CUT the I-term -- the two architectures differ in whether Lambda_obs is an input or an output; (3) the mimic gate as a (gain, acquisition-epoch) exclusion complementing W135's offset-not-gain finding: O(3) offsets acquired inside the DESI window are excluded for g in [0.11, 3.1] per e-fold, and acquisition before N* = 3.68 e-folds (z >~ 39) is gain-independent -- the regulator's sensor PREDATES structure and records, so it senses an ambient invariant, not observer demand; (4) fresh G1b kill: issuance physically DELIVERED proportional to record-writing power (luminosity-tracking at the known budget) drifts solar GM by 8.9e-8/yr, 8.9e5x over ephemeris and 1.05e25x over uniform -- demand-tracking GRAVITATING delivery is DEAD, so 'distributed through the observers' survives only at the signed-ledger / interface-class level; (5) the |II|^2-weighted (innovation) allocation gravitates EXACTLY uniformly on Ricci-flat vacuum (W = a0 = 2 at Ric = 0, symbolic) with FRW schedule deviation ~2e-61. Surviving family shape: the known rate enters as a uniform boundary supply; if regulated, regulated on an ambient sensor with the offset (not the gain) as the physical unknown; observer distribution is ledger-level (C-operator compression credits, signed as spectral flow); allocation weights 3:2:1 (W130) pending C3, already discriminating against a triality equal split at the one computed section."
grade: "exploration / conditional register throughout. Every story is a steelman of a DECLARED reading (the W135/W136 lineage: the measured rate declared as the issuance input); nothing asserts the issuance, a DE mechanism, or any canon movement. COMPUTED: the 24-check scoring script (exact sympy where symbolic, floats vs cited anchors elsewhere). CITED: W135 (16e476f), W136 (cc227b0), W137 (9cb9f4c), W138 (ec00fbd), W129/W130/W131/W132, H10/H52 floors. BINDING kills honored: H36 never re-imported; B2 rate-identity FALSE (no story puts a rate in a structural equation; the declared object is the measured bookkeeping NUMBER and schedule, per W135's separator); W135's own kills honored (no halving/stepped schedule, no holographic c-family, no per-comoving-time drip); Krein-TT physical leg untouched; the naive positive record-cost ledger treated as FALSIFIED (W137), only signed-ledger stories scored. Tri-repo gating: 'issuance' is a local postulate label only (TI owns the concept; the R5 bridge kill stands); capability MEASURE statements are TaF's (none made). NO canon / RESEARCH-STATUS / claim-status / verdict / posture change."
construction: "standard-field for all controller/FRW/thermodynamic machinery (continuity equation, linear control theory, Landauer, ephemeris bounds) -- deliberately, so the gates bind any construction (same posture as W135/W138); program-native only as cited repo objects (the W126 slice decomposition, the W130 3:2:1 split, the W132 C-operator, the W136 bulk-flatness point, the W137 signed bookkeeping). Forks carried, not resolved: the beta/alpha* = 2 setpoint story lives on the KEEP branch of the slice-reference fork exactly as W136 flagged; C1/C2/C3 remain conjectures with their kills."
depends_on:
  - explorations/W135-issuance-structure-taxonomy-2026-07-14.md
  - explorations/W136-issuance-declaration-propagation-2026-07-14.md
  - explorations/W137-observer-slice-structure-2026-07-14.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - explorations/two-track-persona-sweep-2026-07-11/D-wild-frontier.md
  - explorations/two-track-persona-sweep-2026-07-11/E-pragmatic-experimental.md
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W132-graded-optical-theorem-physical-subspace-2026-07-14.md
  - canon/firewall-boundary-hypothesis.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W143_controller_distribution_scoring.py
---

# W143 -- steelman sweep, applied / computational / frontier-wild family

**The corrected core claim steelmanned (Joe, direct chat; assumption-first, kills
after).** Science ALREADY KNOWS the rate. W135 measured it on the record:
rho_Lambda ~ (2.24 meV)^4; the bookkeeping rate q_B = 3 H0 rho_Lambda, which per Hubble
volume is (3/2) Omega_L c^5/G = 1.027 Planck luminosities, and in natural units is the
O(1) ladder q_B/(H0^3 M_Pl_red^2) = 9 Omega_L = 6.16. The steelman question this family
answers per persona: **IF that known measured number is DECLARED as the issuance input
to the source action, what OTHER information can be backed out** -- spec FITs, scales,
relations, mechanism constraints -- for later rigorous checking. Controller stories
survive ONLY in the minimal form W135 already vetted: the observed constant rate may be
a regulated setpoint (W135 structure (e), dAIC -8.6, alive; the data constrain the
OFFSET, not the gain). No schedule exotica: W135 itself killed the stepped/halving
family, the per-comoving-time drip, and the holographic c-family; none is rebuilt here.
This is NOT the falsified naive record-cost claim (the positive ledger died pointwise in
W137; only the signed ledger survives; W143 honors that throughout).

**Evidence pack (binding):** W135 (16e476f): the taxonomy and the measured rate; the
null-structure separator (rho = const has Q = 0 identically); survivors = null,
stock+drip (s in [-0.116, +0.446] H0 rho_crit), PID setpoint (offset-constrained);
the transducer requirement reduced to one scalar (Q = 0, Q = const, or a feedback term,
with feedback-expressibility the named GU-side object). W136 (cc227b0): beta/alpha* = 2
pinned under the declaration, (B_i, f0) = (0, 0) determined, a0-as-physical dead both
forks, two-scale escape band mu_emb/mu_DW in [0.41, 0.57] / [0.63, 0.85], Majorana OOM
click, Yukawa dimensional no-go. W137 (9cb9f4c): C1 (record space = section compression
of the C-operator positive subspace), signed ledger, C3 (3:2:1 per-slice question).
W138 (ec00fbd): the six-gate battery (G1 conservation with the 1.2e19 solar margin,
G2 mimic 0.3 per e-fold, G3 H36 non-reimport >= 4.78x, G4 B2 non-reimport, G5 dS
relabeling at 1.46, G6 Landauer with the 1.3e6 over-pay). Standing ingredients: the
C-operator (W132: unitarity = its existence), the branch fork (W128/W130), the
boundary/firewall hypothesis. Sibling steelman teams (W139-W142) landed during this
run; their proposals are unread beyond commit level by design (cross-team scoring is
next-wave work); the one commit-level convergence is noted in Section 3.4.

**Roster.** Family 5 of the five-team full-roster sweep = the applied / computational /
frontier-wild partition of the 2026-07-11 sweep: ALL of slice D (wild frontier, 12
personas) + ALL of slice E (pragmatic / experimental, 12 personas), plus the four types
the family brief names that the 2026-07-11 roster did not carry literally, instantiated
here: control/systems engineer, information theorist, complexity/computation theorist,
economic/mechanism designer. 28 personas, all inline in one worker, two stories each
where the persona has two; 52 stories total (four personas honestly returned a single
story or pure negatives). Scoring: the W138 battery + W135's own kills as
generation-time filters + the brief's two extra gates (novelty vs the dS line;
mechanism concreteness).

All numbers cited as computed below are in `tests/W143_controller_distribution_scoring.py`
(24/24, exit 0; blocks A-E as referenced).

---

## 1. The computed backbone (what the known-rate declaration backs out before any story is graded)

1. **The O(1) board decomposes through Omega_Lambda (blocks B0a, B0b, B1, B2, B4; new
   exact lines).** Symbolically, T_dS S_dS = c^5/(2 G H) = rho_crit c^2 V_H EXACTLY:
   the Gibbons-Hawking product IS the critical energy of a Hubble volume. Therefore:
   q_B/L_Planck = 3 Omega_L / 2 = 1.027 (the W135 anchor re-derived); the W138 G5
   factor **1.46 = 1/Omega_Lambda exactly**; the W138 G6 saturation factor
   **2.96 = 3 Omega_L / ln 2 exactly**; the W135 ladder 6.16 = 9 Omega_L. Backed-out
   information: under the known-rate declaration, every O(1) de Sitter coincidence on
   the board is (a Planck-scale identity) x (a power of Omega_Lambda). **The
   declaration compresses the entire coincidence stack into the single residual input
   Omega_Lambda** -- equivalently, into the offset of the observed universe from the
   critical/Planck identity line. What remains to derive is one number, and it is the
   same number in every register. This also makes the W138 G5 novelty gate mechanical:
   a normalization story earns novelty iff it derives (a function of) Omega_L, and
   relabels iff it lands on the Planck identity side of the factorization.

2. **The architecture separator inside W135's surviving setpoint structure (blocks C1,
   C2; exact).** W135 established: the PID-setpoint structure is alive, the data
   constrain the offset (positive-offset corner eps >= +0.5 excluded at any Kp in
   [0.1, 10] H0), not the gain, and Ki > 0 removes the steady-state error. Computed
   here, the complementary fork: a P-only loop keeps a NONZERO steady-state residual
   d/g under a constant disturbance, while any integral action forces the residual to
   zero exactly. That separates two architectures of the SAME surviving structure:
   - **Setpoint-=-Lambda (W135 (e) as stated):** Lambda_obs is the INPUT (the setpoint
     itself); the I-term is allowed; the loop explains nothing about Lambda's value and
     the offset eps is the only measurable.
   - **Residual-Lambda (this family's refinement):** the setpoint is BULK-FLATNESS
     (beta/alpha* = 2, the W136 selection; tadpole = 0, i.e. Lambda_set = 0), and
     Lambda_obs is the OUTPUT: the proportional residual d/g, numerically the W136
     ~1e-60 offset from the bulk-flat point. Here the observed nonzero Lambda CUTS the
     I-term (integral action would erase it), so the architecture is falsifiably
     P-type; the D-term is unconstrained at steady state.
   Backed-out information: whether Lambda_obs is an input or an output of the loop is a
   decidable architecture bit, and the residual reading is the only one in which the
   declaration returns leverage on the VALUE (Lambda = disturbance/gain, with the
   disturbance the boundary-bulk mismatch the W136 pin quantifies).

3. **The transient gate is an acquisition-epoch theorem (blocks C4-C6), the complement
   of offset-not-gain.** W135: the data cannot see the gain. Computed here: what the
   mimic gate DOES see is the (offset, acquisition-epoch) pair. For an O(3) initial
   offset, setpoint acquisition inside the DESI window (z <= 2) violates the schedule
   gate for every gain g in [0.11, 3.1] per e-fold (peak deviation 1.005 = 3.4x the
   gate); acquisition earlier than N* = x0/(0.3 e) = 3.68 e-folds ago (z >~ 39) passes
   at ALL gains. **Substrate constraint backed out: the regulator, if there is one,
   acquired its setpoint before structure formation, hence before observers and
   records. The sensor is an ambient geometric invariant; it cannot be record demand.**
   And a DESI-like w(z) feature, if ever confirmed, would have to be loop RINGING
   (underdamped response, period 2 pi/sqrt(g_i - g_p^2/4) e-folds, block C3, matching
   the W135 transfer-function poles at Kd = 0) -- which in the residual architecture
   requires the I-term item 2 just cut. The residual-architecture family therefore
   PREDICTS the DESI CPL pull regresses (sharpening W136 K5 into this family's one
   data forecast; W135's measured best-fit +0.10 q_B is honestly the same DESI pull
   and its SNe integration is the named killer, cited not rerun).

4. **The demand-delivery kill (blocks D1-D3; fresh quantitative kill via W138 G1b).**
   If the known budget is physically DELIVERED (as stress-energy) proportional to
   record-writing power, with luminosity as the record-power proxy (the W138 G6
   ceiling), the Sun's share is the 1.3e6 over-pay times L_sun = 5.0e32 W landing in
   the inner solar system: GM drifts at 8.9e-8 per year, **8.9e5x over the ephemeris
   bound and 1.05e25x over uniform, far beyond the 1e19 G1b gate. DEAD.** The same
   arithmetic from the Planck-luminosity side: the known rate is ~1 L_Planck per
   Hubble volume; concentrating any O(1) fraction of a Planck luminosity into a
   bound system is gravitationally absurd, so **the actuator must be maximally
   delocalized** (boundary/horizon-scale), and "distributed through the observers"
   survives only as accounting -- the LEDGER level (who is credited, in the signed
   bookkeeping / interface-class sense), never a local energy pipe.

5. **Innovation-weighted allocation is gravitationally invisible (blocks E1, E2).** The
   one repo-native pointwise allocation base, the cost density W = |II|^2 = 2 + R/3 +
   (8/9)R^2 - 4 Ric^2 (W126), equals a0 = 2 EXACTLY on Ricci-flat vacuum (symbolic):
   an allocation proportional to W is exactly uniform across the solar system
   (Schwarzschild is Ricci-flat), and its FRW schedule deviation is (H0/mu_DW)^2 ~
   1.8e-61. Curvature/innovation-tracking allocation passes G1b and G2 with
   astronomically trivial margins -- a viable gravitating F, unlike demand tracking.

6. **Allocation weights are already discriminating (block E3).** The W130
   Einstein-sector split 3:2:1 gives fractions (1/2, 1/3, 1/6); a Z/3-triality
   equivariant allocation predicts (1/3, 1/3, 1/3). These differ by 1/6 at the computed
   section, so the triality equal split is ALREADY disfavored at the one point
   computed, and W137's C3 decision computation (rerun W130 Stage A on a curved
   background) doubles as the allocation-weight discriminator.

---

## 2. The roster: 28 personas

Per-persona question (uniform): given the DECLARED known rate as the issuance input,
what other information backs out. Format per story: **F / mechanism** (sensor /
actuator / setpoint / carrier, or allocation base) | **backed out** (the assumption
leg: what it pins) | **test**. Verdicts applied in Section 3.

### New instantiations (the brief's named types)

**P1. Control / systems engineer.**
- **P1-S1 (the residual-Lambda architecture).** F: q^nu = q(t) u^nu, minimal regulated
  form (W135 (e) vetted); sensor = the bulk flat constant / tadpole a0(alpha, beta) =
  2 alpha - beta (plant sensitivity = the W137 first-order exchange dW/du = -8 at the
  constant section); actuator = the boundary flux (W136 wrapper, delocalized per
  Section 1 item 4); setpoint = bulk-flatness (Lambda_set = 0); error carried by the
  causally graded conformal/gradient mode (W137 D2). Backed out: SA-G5 = 2 acquires a
  MECHANISM (the setpoint), (B_i, f0) = (0, 0) re-derives as "loop at steady state",
  Lambda_obs = the P-residual d/g (the W136 1e-60 pin read as disturbance/gain), and
  the I-term is cut (block C2) -- the first frame in which the smallness of Lambda is
  a generic loop property rather than a tuning. Test: blocks C1-C2 computed; forward
  falsifier = W136 K5 + the K4 setpoint falsifiers, inherited unchanged.
- **P1-S2 (the acquisition-epoch bound).** F: same loop, transient regime. Backed out:
  the (gain, acquisition-epoch) exclusion of Section 1 item 3; sensor is ambient, not
  demand; the complement of W135's offset-not-gain. Test: blocks C4-C6 computed; data
  forecast = DESI regression to w = -1.

**P2. Information theorist.**
- **P2-S1 (capacity allocation on the C-compression).** F: the known total credited per
  observer proportional to dim(P_sigma H_C+)/dim(H_C+) -- the section-compressed
  dimension of the C-operator's maximal positive subspace (W132/W137 C1); ledger-level
  only (Section 1 item 4). Backed out: C1 becomes the allocation carrier; clause 2 of
  C1 (one collective C) IS "a set rate distributed to all compressions"; the
  C-operator becomes the distribution ledger. Test: K2's compressed-metric positivity
  check (W54 half-space model), the cheapest discriminator; C3 for sector weights.
- **P2-S2 (rate-distortion).** F: records = lossy compression of the ambient state; the
  known rate pays the RATE side of an R(D) tradeoff; C1 is literally the codebook
  statement ("section-compressed"). Backed out: the W138 G6 over-pay becomes expected
  supply-side structure (a capacity-targeted code always over-pays early demand), i.e.
  the declaration PREDICTS the absence of budget/demand fine-tuning, which is exactly
  what W138 G6 found. Test: none sharper than K2; below S1 on testability.

**P3. Complexity / computation theorist.**
- **P3-S1 (reversible/irreversible split = the signed ledger).** F: only IRREVERSIBLE
  record operations draw on the known budget (Landauer); reversible dynamics recycle.
  The W137 indefinite cost form (+32/-32 in the traceless sector, -16/+16 causally
  graded) read as the reversible sector showing through: releases = uncomputation.
  Backed out: a structural explanation of WHY the positive ledger failed pointwise
  while the signed ledger survives. Test: proposal-grade -- map the releasing
  directions to a measure-preserving subsector of deformation space; not run.
- **P3-S2 (Landauer capacity accounting).** F: the known budget at ambient temperature
  T buys budget/(k_B T ln 2) bits per Hubble time; as T falls the same budget buys
  more, reaching exact horizon saturation at T_dS with bits/S_dS = 3 Omega_L/ln 2 =
  2.9634 (block B4, decomposed -- the earned half of the W138 G5/G6 gate). Backed out:
  the saturation factor is not a new coincidence, it is Omega_L again (Section 1
  item 1); the T_CMB over-pay 1.3e6 is epoch structure, not tuning. Test: blocks
  B3-B4, D1 computed. (No protocol vocabulary; the stepped/halving schedule family is
  DEAD by W135 and is not rebuilt.)

**P4. Economic / mechanism designer.**
- **P4-S1 (supply-side vs demand-side, the one lawful separation).** F: the known rate
  is SUPPLY-SIDE (set independently of record demand); the mechanism-design content is
  the matching layer between a fixed supply and heterogeneous observers, which after
  Section 1 item 4 must clear at ledger level. Backed out: the sensor cannot be
  demand (item 3), the delivery cannot be demand-weighted (item 4), so the only
  mechanism-design freedom left is the CREDIT RULE -- exactly the C1/C3 objects; the
  economics collapses onto the same two conjectures the geometry produced. Test: K2 +
  C3 (shared).
- **P4-S2 (price-stability baseline).** F: q = 3 H rho_L exactly, w = -1 (the W135
  null structure, where Q = 0 identically and the bookkeeping rate is pure pressure
  work). Backed out: nothing beyond the declaration itself; W135's separator says this
  structure evidences NO genuine issuance. Kept as the honest null, not ranked.

### Slice E personas (pragmatic / experimental family, all 12)

**E1. Commercial scientist.**
- **E1-S1.** F: P1-S1 bought as a MEASUREMENT: the family's falsifiable number is the
  forecast w(z) = -1 at all future precision (residual architecture). Test: DESI DR3 /
  joint likelihood; W135's named SNe integration is the same purchase.
- **E1-S2.** F: the (g, N_acq) exclusion chart as a standing data product (block C5b).
  Kept as the deliverable form of P1-S2.

**E2. Observational cosmologist (DESI/CMB).**
- **E2-S1 (the flatness reading of the ladder).** F: the regulated quantity is
  FLATNESS (total = critical); the known rate's Planck-identity factor is exact for
  the critical density, and Omega_L is the DE share of it (block B2: 1.46 = 1/Omega_L
  exactly). Backed out: the dS "coincidence" stops being evidence about DE specifically
  and becomes the flatness identity plus one number; consistency track T_dS(z) S_dS(z)
  /E_Lambda(z) = 1/Omega_L(z). HONEST FLAG: in flat FRW "total = critical" is the
  Friedmann constraint, so this is tautology-adjacent unless the actuator is H itself;
  carried flagged.
- **E2-S2.** F: any surviving F must be an LCDM mimic (G2), so issuance constancy is
  the family prediction. Merged into the forecast row.

**E3. Gravitational-wave phenomenologist.**
- **E3-S1 (feedback stabilization of the tachyonic scalaron).** F: the open-loop plant
  is UNSTABLE (tree scalaron tachyonic at the issuance-selected point, W136: c_R =
  -4/3, f_0^2 = -1/8); the loop is the stabilizer and the known rate is the control
  effort. Sensor/actuator at the mu_DW (sub-mm) scale; cosmological signature = the
  averaged residual. Backed out: reframes the standing tachyon (the One Residual
  pressure point) as the open-loop instability the regulation exists to close --
  and W135's named GU-side object ("can the source action express a feedback term")
  becomes the whole question. Test: none until the family c_W at beta/alpha = 2
  exists (W136's named settling computation); the family's highest-upside UNKNOWN.
- **E3-S2 (TT-pole guard).** Constraint row all survivors inherit: no dynamical DE
  degree of freedom may touch the massless TT pole (W136 K7). Not a story.

**E4. Collider / particle phenomenologist.**
- **E4-S1 (the seesaw tap).** F: the only fermion-sector channel the known datum can
  dimensionally reach is the Majorana spurion (W136 SA-Y8); allocation to matter runs
  through v^2/rho^{1/4} = 1.3e16 GeV. Backed out: SA-Y8 as the unique matter-side tap
  (all other Yukawa rows closed by the W136 dimensional no-go). Test: W136 K8
  (quasi-degenerate spectrum >~ 100 meV kills). OOM-tier; alive, low.
- **E4-S2.** Mirror-sector window: no F derivable (free modulus). Discarded.

**E5. Precision-tests experimentalist.**
- **E5-S1 (the demand-delivery kill).** The persona's contribution IS Section 1 item 4
  (blocks D2, D3): luminosity-tracking gravitating delivery dead at 8.9e5x over
  ephemeris, 1.05e25x over uniform. Backed out (negative, structural): every surviving
  F is forced to ledger-level distribution. Test: computed.
- **E5-S2 (curvature-tracking passes exactly).** The complementary positive: the
  |II|^2-weighted allocation is exactly uniform on Ricci-flat vacuum (block E1) -- the
  one gravitating allocation base that survives the same gate with zero margin
  consumed. Backed out: a0 = 2 as the per-point allocation constant. Test: computed.

**E6. Model-builder (BSM).**
- **E6-S1 (the declared spec row).** F: Q^nu = 3 H rho_L u^nu at background with the
  Bianchi ledger named (unimodular integration constant as reservoir; G1a done
  properly). Backed out: (B_i, f0) = (0, 0) as the declared model's natural point.
  Subsumed by P1-S1 in ranking.
- **E6-S2 (the two-scale band as dynamic range).** F: sensor scale mu_emb, plant scale
  mu_DW; the W136 escape band mu_emb/mu_DW in [0.41, 0.57] / [0.63, 0.85] read as the
  loop's required O(1) scale separation, to be SUPPLIED by a geometric ratio. Backed
  out: the band becomes an architecture datum rather than a tuning. Test: W136 K3
  unchanged.

**E7. Bayesian forecaster.**
- **E7-S1 (the mimic as evidence).** F: the regulated-setpoint class is a proper
  likelihood concentrated at w = -1 up to transients; W129's "everything allowed is a
  mimic" converts to support, now with W135's dAIC table behind it (PID -8.6 = the
  DESI pull, SNe the named killer). Test: the Bayes factor vs CPL from standing W129
  machinery (named, not run).
- **E7-S2 (allocation model comparison).** F: 3:2:1 vs 1:1:1 as two priors over
  weights; block E3 shows the computed section already separates them. Test: C3.
  Merged with D11.

**E8. Science-strategy / portfolio.**
- **E8-S1.** Routing note: the declaration buys three determined spec rows (W136) plus
  this wave's architecture bit and substrate bounds for one input; cheapest next test
  on the whole board = K2. Not ranked.
- **E8-S2.** Hold residual-architecture and flatness-reading as separate bets (they
  fail differently: K4 forks vs the tautology flag). Not ranked.

**E9. Journal referee.**
- **E9-S1.** The admissible RESULT is the pre-registered triple: w = -1 exactly, no
  fifth force above standing floors, DESI CPL regresses. Test: data.
- **E9-S2 (novelty audit demonstration).** A dS-equilibrium-setpoint story (regulate E
  to T_dS S_dS) reduces to the G5 identity and FAILS novelty; the Omega_L
  factorization (Section 1 item 1) shows exactly what it misses. The scoring pass's
  worked negative control.

**E10. Data analyst / likelihood specialist.**
- **E10-S1 (transient inversion).** F: if a w(z) feature is ever confirmed, invert it
  to (g_p, g_i, N_acq) via the block-C3 ringing formulas (= the W135 transfer function
  at Kd = 0): a DE anomaly becomes a three-parameter loop fit. Dormant until a
  non-mimic signal exists.
- **E10-S2.** The 1/Omega_L(z) consistency track of E2-S1 as a standing sanity row.
  Merged.

**E11. Numerical / strong-field relativist.**
- **E11-S1 (black holes are allocation-neutral).** From block E1: W = 2 exactly on
  Ricci-flat vacuum, so an |II|^2-weighted allocation assigns a black-hole exterior
  the uniform credit -- no issuance concentration at horizons, consistent with the
  solar-system gate open by 1e19. Test: computed (symbolic).
- **E11-S2 (boundary settlement).** F: the signed ledger SETTLES at boundaries: C2's
  boundary-balance conjecture + the firewall/boundary hypothesis as the settlement
  layer (the W103 tail-quotient slot as the retreat landing). Backed out: C2 as the
  settlement mechanism of the distribution. Test: C2's named integration-by-parts on
  Y14 sections (tractable next wave, still unrun).

**E12. Neutrino / flavor phenomenologist.**
- **E12-S1.** The E4-S1 tap from the neutrino side: 2.3 meV lands x3.7-22 from the
  oscillation band; the absolute spectrum scale is the falsifier (K8). Merged with
  E4-S1.
- **E12-S2.** Hierarchy distribution: dead by the W136 dimensional no-go. Recorded
  discard.

### Slice D personas (wild frontier family, all 12)

**D1. Wild frontier mathematician.**
- **D1-S1 (three channels because dim Lambda^2_+ = 3).** F: the known rate splits
  across the three self-dual channels; generations = issuance channels. Backed out:
  would tie the count leg to the distribution leg. Test: none computable;
  conjecture-only. Fails testability; flagged as the family's wildest live idea.
- **D1-S2.** Kinematic-register restatement of D6-S1. Merged.

**D2. Homotopy / tmf theorist.** Both stories honestly NEGATIVE: no torsion class can
carry the continuous declared datum (the 1e-60 residual is Q-valued, torsion is not).
Two recorded discards; negative-space contribution.

**D3. K-theory / index theorist.**
- **D3-S1 (index-quantized credits).** F: allocation proportional to the local index
  density of the compressed operator; records = integer index jumps; carrier B (the
  index-CHANGING carrier, order-3 rho = (0,2,1)) is the only carrier that can RECEIVE
  credits. Backed out: the K-class / carrier-B branch selection from the distribution
  side (a ledger needs an integer account; only B's index moves). Test: whether the
  C-compression shifts the index by an integer per admissible record, runnable on the
  K2/W54 model (named).
- **D3-S2 (the signed ledger IS spectral flow).** F: record events = spectral flow
  through zero; spectral flow is naturally SIGNED, never positive -- a mechanism class
  for W137's signed-bookkeeping survival (the +32/-32 pair as crossing directions).
  Test: map the W137 exact witnesses to mode crossings of the section-compressed
  operator; the cheapest D-family proposal.

**D4. Exotic-signature / real-form specialist.**
- **D4-S1 (the error signal's carrier field).** F: the error signal rides the
  gradient/conformal sector, causally graded 16 eta(dphi, dphi) (W137): timelike error
  RELEASES, spacelike PAYS -- the loop's sign convention is the Lorentzian signature
  itself; the DeWitt-negative pure-trace direction (h0 = -1, W136) is the actuator's
  line of action. Backed out: the named carrier field the brief asks for (the
  conformal mode on the (6,4) DeWitt fiber). Test: the covariant form of the
  gradient-sector cost (W137 flags it as two-axis observation, not theorem) -- a named
  small computation.
- **D4-S2.** Signature as allocation selector: dead by the 2-primary/3-primary floor.
  Recorded discard.

**D5. Weinstein-native geometer.**
- **D5-S1 (the shiab transducer with a 2+1 valve).** F: the source action's shiab
  coupling as the transducer distributing boundary flux into carriers; the RS
  cross-term splits allocation 2+1 across generations (third an imposter), predicting
  unequal per-generation coupling reunifying at high energy. Backed out: which carrier
  the valve feeds = which carrier the source action names. Test: the RS product-rule
  decomposition vs carrier A/B occupancy (the sweep's named unmatched mechanism); not
  run.
- **D5-S2 (the observer's own share is 1/6).** F: the 3:2:1 split as base/fiber/slice
  allocation -- the slice channel (the observer's own deformation direction) carries
  exactly 1/6 of the Einstein-sector weight. Backed out: C3 becomes the direct test of
  "distributed through the observers": per-slice split = each observer's share a
  computable invariant of its section. Test: C3 (the family's cheapest decisive item).

**D6. Double-copy / amplitudes theorist.**
- **D6-S1 (the known constant as the square of a first-order flux).** F: the
  Lambda-channel constant a0 = 2 as |flux|^2 of a first-order self-dual gauge datum;
  the gravity-side distribution is the SQUARE of a gauge-side charge allocation
  (color-kinematics maps charge accounting to energy accounting). Backed out: would
  derive |II|^2 as the square (the standing North-Star object) AND hand the issuance a
  gauge-side ledger in one move; adds the prediction that the gauge datum carries a
  conserved charge whose square is 2. Test: the SYNTHESIS one-test (kinematic Jacobi
  closes only on carrier B), inherited unchanged.
- **D6-S2.** KLT-weighted channel allocation: relabel of D6-S1. Merged.

**D7. Noncommutative geometer (Connes).**
- **D7-S1 (the cocycle-clean form).** F: by the Path-5 theorem the RATE is the
  quotiented fiber of the Connes cocycle bijection {F_tau} <-> Sect, so the ONLY
  G4-safe form of "a set rate" is: the rate label lives on the fiber and the
  distribution function is the SECTION (rate-invariant content); F = the section map
  assigning each observer its filtration stage. Backed out: the unique story
  G4-compliant BY THEOREM; pins the filtration/schedule reading as the lawful home of
  "set rate" (consistent with W135 declaring the NUMBER, not a rate-identity). Test:
  exhibit the cocycle for the C-metric dynamics (W54-priced; expensive, named).
- **D7-S2 (spectral-action boundary term as actuator).** F: the issuance enters as the
  boundary/eta term of the spectral action. Backed out: a concrete functional for C2's
  boundary balance. Test: shared with E11-S2.

**D8. Higher-category / derived lens.**
- **D8-S1 (credits descend, operators do not).** F: the allocation is a section of the
  interface-class sheaf (W107): credits are well-defined exactly at the level that
  descends, and W94/W98 non-gluing is WHY the distribution cannot be an operator pipe
  -- the categorical face of the delivery kill. Backed out: the LEVEL of the
  distribution function (class level), making C1's fallback the primary statement.
  Test: extend the standing W107 coherence controls to the credit assignment; cheap,
  named.
- **D8-S2.** Bare derived-geometry reformulation: relabel. Recorded discard.

**D9. Integrable-systems lens.**
- **D9-S1 (conserved-charge allocation).** F: distribution along the self-dual
  sector's hidden-symmetry charge tower. No test short of the hidden-symmetry
  computation; conjecture-only.
- **D9-S2.** Releasing directions as the integrable (non-dissipative) sector: same
  content as P3-S1. Merged.

**D10. Quantum-topology / Jones lens.** Index-3 subfactor as allocation branching:
coincidence-hunt by the roster's own standing discipline; both stories recorded
discards (unwired token-3 transport).

**D11. Spin(8)-triality lens.**
- **D11-S1 (equal-split allocation).** F: a diagonal-triality-equivariant allocation
  forces (1/3, 1/3, 1/3). ALREADY DISFAVORED at the computed section: W130 says 3:2:1
  (block E3). Wake condition: C3 moving the ratio toward equality on curved
  backgrounds.
- **D11-S2.** Triality as ledger-account labeling (labels, never selects): relabel.
  Recorded discard.

**D12. Chromatic / image-of-J lens.** Same negative as D2 one level up; both stories
recorded discards; the redirection (a continuous residual needs index/flow objects,
not torsion classes) feeds D3.

---

## 3. Scoring pass (W138 G1-G6 + W135 generation-time kills + novelty and mechanism gates)

### 3.1 Discards (one line each)

| story | reason |
|---|---|
| P4-S2 price-stability baseline | the W135 null structure: Q = 0 identically, evidences no issuance (kept as null control, not ranked) |
| any stepped/halving or holographic or drip schedule | DEAD in W135 before this wave; not rebuilt (generation-time filter) |
| E4-S2 mirror window | free modulus, no F derivable |
| E12-S2 hierarchy distribution | dead by the W136 dimensional no-go |
| E9-S2 dS-equilibrium setpoint | reduces to G5; the worked negative control (the Omega_L factorization shows the miss) |
| D1-S1 three-channel generations | no test; conjecture-only (flagged wild-live, not scored) |
| D2 both | torsion cannot carry the continuous 1e-60 residual |
| D4-S2 signature selector | 2-primary/3-primary floor |
| D6-S2 KLT weights | relabel of D6-S1 |
| D8-S2 bare derived | relabel |
| D9-S1 charge tower | no test short of the hidden-symmetry computation |
| D9-S2 dissipative split | duplicate of P3-S1 |
| D10 both | unwired token-3 transport (standing quarantine) |
| D11-S1 equal split | KILLED at the computed section by W130's 3:2:1 (wake: C3) |
| D11-S2 account labels | labels, never selects |
| D12 both | as D2, one chromatic level up |
| luminosity-tracking gravitating delivery (any persona's demand variant) | FRESH KILL, blocks D2-D3: 8.9e5x over ephemeris, 1.05e25x over uniform (G1b) |

Merged (not discarded): E1 pair into P1; E2-S2/E7-S1/E9-S1/E10-S2 into the family
forecast row; E12-S1 into E4-S1; D1-S2 into D6-S1; E6-S1 into P1-S1; P3-S2's Landauer
accounting absorbed into survivor 5.

### 3.2 Survivors, ranked (mechanism concreteness / leg sharpness / testability / novelty)

**1. THE RESIDUAL-LAMBDA ARCHITECTURE (P1-S1 + P1-S2 + E1, with D4-S1's carrier).**
The minimal-form controller story, refining W135's surviving structure (e) with a
decidable architecture bit. Sensor: the bulk flat constant (tadpole), plant
sensitivity -8u. Actuator: the delocalized boundary flux. Setpoint: bulk-flatness
beta/alpha* = 2 (Lambda_set = 0), so Lambda_obs = the P-residual d/g and the I-term is
CUT by the observed nonzero offset (computed, C1-C2; W135's setpoint-=-Lambda form
keeps the I-term and treats Lambda as input -- the fork is decidable in principle by
whether Lambda's value carries loop information). Acquisition predates z ~ 39 at O(3)
offsets (computed, C6), the complement of W135's offset-not-gain: the sensor is
ambient, not demand. Backed out: SA-G5 = 2 with a mechanism; (B_i, f0) = (0, 0) as
steady state; the 1e-60 pin as disturbance/gain; the substrate bound. Gates: G1a pass
(ledger = boundary flux, Q = q(t) u^nu), G1b pass (uniform), G2 pass at setpoint with
transient content in the exclusion chart, G3 pass, G4 pass (the declared object is the
measured number and the loop parameters live in the wrapper dynamics, never in a
structural constant; f0 stays structural and equals zero), G5 EARNED (setpoint is
bulk-flatness, repo-native), G6 n/a. Test: blocks C1-C6 computed; forward falsifier =
family forecast (DESI CPL regresses; W136 K5; W135's SNe integration).

**2. THE LEDGER-LEVEL DISTRIBUTION THEOREM (E5-S1 + E5-S2 + D8-S1 + D3-S2).** The
structural result of the sweep, negative + positive halves: demand-tracking
GRAVITATING delivery is dead by 25 orders of enhancement (fresh G1b kill, computed;
Planck-luminosity form: the actuator must be maximally delocalized), while the
|II|^2/innovation-weighted allocation gravitates exactly uniformly on Ricci-flat
vacuum (computed, symbolic) with schedule deviation 2e-61. Therefore "distributed
through the observers" lives at the signed-ledger / interface-class level (W107
descent fixes the categorical level; spectral flow supplies the signed mechanism
class). Backed out: the observer-slice structure carries the distribution (C1
compression credits, class-level), decoupled from the stress tensor -- an accounting
1-form on sections, not an energy pipe. Gates: all pass by construction. Test:
computed (D2/D3/E1/E2); next decisive: K2, then D3-S2's crossing map.

**3. THE OMEGA_LAMBDA FACTORIZATION OF THE O(1) BOARD (E2-S1 + E10 + block B).**
Backed out of the declaration alone: q_B/L_Planck = 3 Omega_L/2, the G5 factor
1.46 = 1/Omega_L (via T_dS S_dS = E_crit(V_H) = c^5/(2GH), exact), the G6 saturation
2.96 = 3 Omega_L/ln 2, the W135 ladder 6.16 = 9 Omega_L. Every de Sitter coincidence
on the board is (Planck identity) x (power of Omega_L): the declaration compresses
the coincidence stack into the single residual input Omega_L, and makes the W138 G5
novelty gate mechanical (novelty = deriving Omega_L; relabeling = landing on the
identity side). Honest flag: the flatness reading of WHY the factorization exists is
tautology-adjacent in flat FRW; carried flagged, ranked for the factorization itself
(computed) not the reading. Test: blocks B0-B4 computed; consistency track
1/Omega_L(z).

**4. THE C-COMPRESSION CAPACITY ALLOCATION (P2-S1 + P2-S2 + D3-S1 + D5-S2 + P4-S1).**
F = credits proportional to the section-compressed C-operator positive subspace
(dimension / index density), weights 3:2:1 across deformation sectors pending C3; the
observer's own channel carries 1/6. Control-theoretic teeth (the Kalman reading): a
per-slice state estimator REQUIRES a positive innovation metric, the Krein space
refuses one, and the C-operator's positive subspace is the unique supply -- so W132's
"unitarity = C exists" re-reads as "observers can run estimators iff the collective
ledger exists", which is C1's collective/single matching clause. The mechanism-design
face (P4-S1): with sensor and delivery both barred from demand, the only design
freedom is the credit rule, which IS C1/C3. Backed out: C1 as allocation carrier,
carrier B as the creditable account, the C-operator as the distribution ledger.
Gates: G4 pass (fractions and dimensions are structural, not rates), G1 pass
(ledger-level), G5 pass. Test: K2 compressed-metric positivity (cheapest, machinery
exists); C3 for the weights (already discriminating vs equal split, block E3).

**5. LANDAUER CAPACITY ACCOUNTING (P3-S2, absorbing P2-S2's supply-side reading).**
The known budget's bit-capacity runs from 6.5e92 (T_CMB) to exact horizon saturation
at T_dS with bits/S_dS = 3 Omega_L/ln 2 = 2.9634 (computed, decomposed); the 1.3e6
T_CMB over-pay is epoch structure of a supply-set budget, so the declaration PREDICTS
the absence of budget/demand fine-tuning at every temperature -- precisely what W138
G6 found and could not read as a kill. Backed out: the G6 numbers become consequences
of the declaration rather than free observations. Gates: G6 matched; G5 exposure
partial (the saturation statement is G5-adjacent; the earned part is the 2.96
decomposition and the no-fine-tuning prediction) -- ranked last of the survivors for
that residual exposure. Test: blocks B3/B4/D1 computed.

**Unranked live flags:** E3-S1 (the loop as stabilizer of the tachyonic scalaron;
highest upside, zero computation until the family c_W exists; note it gives W135's
feedback-expressibility question its physical stakes), E11-S2 + D7-S2 (boundary
settlement via C2 + the spectral-action boundary term; C2's integration-by-parts is a
named cheap next computation), D6-S1 (the constant as a square; inherits the
North-Star one-test unchanged), D7-S1 (the cocycle-clean form: the only theorem-grade
G4 compliance in the family; expensive test), E6-S2 (the two-scale band as loop
dynamic range; W136 K3 unchanged).

### 3.3 The assumption leg, assembled (what to declare to back into the rest)

Declare, in the conditional register, the four-clause leg:

1. **Input clause (the corrected core):** the measured rate is the issuance input:
   rho_iss = rho_Lambda,obs, q_B = 3 H0 rho_Lambda = (3/2) Omega_L L_Planck per Hubble
   volume, ladder 9 Omega_L = 6.16 (W135 numbers, declared not asserted). Immediate
   yield: the O(1) coincidence board factorizes through Omega_L; the single number
   left to derive is Omega_L / the offset.
2. **Regulation clause (minimal form):** IF regulated, the loop is at setpoint since
   z >~ 39 with the offset (not the gain) as the physical unknown => (B_i, f0) =
   (0, 0), exact LCDM, DESI CPL regresses (the family's one data forecast); the
   residual-vs-setpoint architecture bit decides whether Lambda_obs is an output
   (then SA-G5 = 2 is the setpoint and the I-term is cut) or an input.
3. **Ledger clause:** distribution through observers is signed-ledger / class-level
   (never gravitating demand delivery) => C1 is the allocation carrier, carrier B the
   creditable account, W107-level descent the collective/single matching.
4. **Weight clause:** allocation weights are the Einstein-sector split 3:2:1
   (observer's own channel = 1/6) pending C3; a C3 ratio shift is the first
   per-observer-slice coupling effect, a C3 null retires the weight clause to ambient.

Backs into: the source action (boundary flux term + transducer; W135's one-scalar
transducer requirement with feedback-expressibility the named GU-side object), the
spec FITs (SA-G5/G3/G4 as in W136, now with mechanism), and the observer-slice
structure (C1/C2/C3 as the distribution's carrier, settlement, and weights). Kills
after, per posture: the family forecast failing (a confirmed non-mimic signal), K4's
setpoint falsifiers, K2's compressed-metric degeneration, C3's null, and the standing
W135/W136 kill lists inherited unchanged.

### 3.4 Commit-level convergence note (siblings unread beyond commit messages)

W139, W140, and W142's commit messages independently rank a C-compression /
C-operator-based distribution in their top tiers, and W141's S7 names C1's first
quantitative job; this family's survivors 2 and 4 land on the same object from the
control/information side. The cross-team scored synthesis is next-wave work; the
convergence is recorded here only as a pointer.

## 4. Honest limits

- Everything is conditional on declared postulates; nothing here asserts the issuance,
  moves a FIT row, or touches canon/verdicts. The declared VALUE (the W135 measured
  rate) is an input everywhere; survivor 3 shows what it compresses, not where it
  comes from.
- The controller mathematics is standard-field linear control on the FRW background;
  no GU-native loop dynamics exist (W135's feedback-expressibility question is open;
  survivor 1 gives it a target shape, not a construction; the transduction clause
  stays pre-mathematical exactly as W137 conceded).
- The demand-delivery kill uses the stellar proxy for record power at CITED-ORDER
  grade (W138's honest limit iii); a sharper record model rescales the 8.9e5 factor
  but cannot close 25 orders of enhancement.
- The acquisition-epoch theorem assumes O(3) natural initial offsets; a tuned
  x0 << 1 evades it (that tuning is itself a counted cost). It complements, and
  nowhere contradicts, W135's grid result (W135 scanned offsets at fixed recent t_i
  inside z <= 30 and found the positive corner excluded; this wave scans the
  acquisition epoch).
- The Omega_L factorization lines (1.46 = 1/Omega_L, 2.96 = 3 Omega_L/ln 2, 1.027 =
  3 Omega_L/2) are exact identities of standard cosmology, new to the repo's ledger
  but not to physics; their role is to make the W138 G5/G6 novelty gate mechanical
  and to expose the declaration's single residual input, not to claim discovery.
- Sibling proposals (W139-W142) unread beyond commit level by design; cross-team
  scoring is next-wave work.

*Filed 2026-07-14, W143. 28 personas inline (slices D + E complete, plus the four
brief-named instantiations), one worker, no sub-agents. Reproducible:
`python -u tests/W143_controller_distribution_scoring.py` (24/24, exit 0). Exploration
grade; no canon movement.*
