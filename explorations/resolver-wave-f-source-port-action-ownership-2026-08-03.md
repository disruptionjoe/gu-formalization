---
artifact_type: exploration
label: "Resolver Wave F: fixed-signature source port and action ownership"
created: 2026-08-03
status: exploration
posture: adversarial; Layer-0-first; construction-first; source-collision required
title: "A rank-252 projector exists after grade-six typing; the actual source port and downstream weight selection remain open"
grade: "Exact sparse exterior algebra on all 252 internal five-blades, exhaustive inherited native 210/252 K/right-H matrices, constant signed-permutation split-transport/composition and one projector-chain fixture, independent Sage D5/projective-ideal certificate, and finite auxiliary/KO controls; q6, the actual source epsilon/Theta transport, public U-to-Sp reduction, tilted descent, total Euler tangency/transverse closure, full G2/Y/P0 placement, VEV, mass, quotient, domain, and no-leakage remain open"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
route_disposition: REBASE
hostile_review_status: PASS_AFTER_REPAIRS
depends_on:
  - lab/specifications/six-axis/six-axis-template.md
  - explorations/resolver-wave-e-source-owned-moving-252-full20-placement-2026-08-03.md
  - explorations/rb3-moving-soldering-spinzero-placement-2026-07-30.md
  - explorations/rb5-epsilon-flag-ownership-spectral-hessian-2026-07-30.md
  - explorations/unified-source-datum-packet-v0-2026-07-30.md
  - lab/sources/weinstein-gu-primary-source-pack-2026-07-30.md
scripts:
  - tests/channel-swings/resolver_wave_f_source_port_action_ownership_probe.py
---

# Resolver Wave F: fixed-signature source port and action ownership

## Outcome first

Wave F constructs a fixed-split rank-252 projector **after** its input has
already been typed as a grade-six exterior symbol. It does not yet construct
the adjoint-to-grade-six map `q6`, the source `epsilon_src` action, the Zorro
transport, or the public source-to-active port.

At a fixed trace-reversed split

```text
C_0 = H_(3,1) + V_(6,4),
```

the exterior-symbol projector is

```text
Pext^0 = j5 (1/9) pi_(Lambda5 V*) delta.
```

Here the input is already in `C* tensor Lambda6(C*)`; `q6` is still only the
formula-level map that would have to produce that input from a generic native
adjoint value. `delta` contracts the one-form coindex against the grade-six
form, and `j5` is its signed exterior adjoint. Exact signed exterior algebra
on all 252 internal blades gives

```text
delta j5 = (4 horizontal + 5 vertical) I = 9 I.
```

Thus `Pext^0` is an algebraic rank-252 image projector with a forced `1/9`.
The executable checks every image blade and one explicit off-image input. It
does not independently certify the full Krein self-adjointness of `Pext^0`.

The eventual locally moved source candidate would be

```text
Pext^(Theta,epsilon) = U_(Theta,epsilon) Pext^0 U_(Theta,epsilon)^(-1),
```

where `U` would have to transport the covector leg through the Zorro/splitting
connection and the adjoint value through the promoted source gauge field. The
executable does **not** instantiate this `U`. It constructs a constant
signed-permutation model that moves the coindex and exterior-form legs,
verifies composition, checks one split-preserving permutation, and verifies
`P_moved=U_perm Pext^0 U_perm^-1`. A one-generator infinitesimal fixture gives
the internal-five-form projector chain rule `dot P=[X,P]`; freezing it fails.

This is an **exterior-symbol projector plus a split-transport schema**, not a
conditional construction of the public source port. Four obligations remain
unbuilt:

1. the Clifford-symbol projection `q6` on a generic native adjoint input,
   including cross-grade annihilation;
2. a compatible quaternionic `Sp(32,32;H)` reduction of the public source's
   complex notation, with the source Euler field tangent to it;
3. the actual tilted left/right law proving that `epsilon_src` descends to the
   required moving `4+10` split rather than only a coarse Clifford plane; and
4. actual `Y14` Zorro/metric/Hodge/Krein Riesz overlap descent.

The downstream full-20 horizontal/vertical placement is a different object.
Under only the split stabilizer it has two complex-type support branches.
Because `star^2=-1` on the real 252, each also admits composition with the
Hodge star. The declared real exterior Hom space is therefore four-
dimensional. `[a:b]` covers only the two-dimensional star-even subansatz.
Independent Sage character algebra confirms the D5 branch multiplicities and
exclusions. For Wave E's one-simple-blade witness, the clean star-even ratio
is `[a:b]=[2:1]`, equivalently `lambda=b/a=1/2`; representation-wide extension
remains open.

The displayed source kappa term cannot select that downstream ratio. It is
written upstream of the rectangular placement and contains no `[a:b]` field.
Even the hostile collapsed model in which kappa is restricted through
`j_(a,b)` sees only

```text
5 a^2 + 4 b^2.
```

It is nonzero for every nonzero real projective placement; if `a,b` are instead
varied as raw fields, its only stationary point is the forbidden zero map.
Because `[a:b]` is absent from the source field registry, the zero ideal used
in the certificate records **absence/action blindness**, not a varied Euler
flat direction. The displayed kappa term therefore does not directly imply
`a-2b=0`.

One isolated algebraic multiplier can force `a-2b=0` in the star-even
one-blade comparator, with rank-two algebraic Hessian. An explicit zero-jet
variation has no integration-by-parts boundary term in that isolated sector.
This does not prove coupled nonpropagation. It prices a possible fallback:
one projective placement field plus one multiplier, or equivalently a new
constrained coupling sector. It is visibly shaped to the representative clean
ratio, is source-silent, and is **not yet required**.

The terminal ownership result is therefore

```text
FIXED-SPLIT PEXT^0 AFTER GRADE-SIX TYPING: CONSTRUCTED;
ADJOINT-TO-GRADE-SIX q6: OPEN / FORMULA ONLY;
MOVED PROJECTOR: SIGNED-PERMUTATION SCHEMA ONLY;
COARSE EPSILON_PLANE OWNS INTERNAL 252: REFUTED;
EPSILON_SRC + THETA_Z AS GLOBAL OWNER: OPEN / SERIOUS CANDIDATE;
PUBLIC COMPLEX-TO-NATIVE-SP PORT: OPEN;
DOWNSTREAM REAL EXTERIOR HOM: DIMENSION 4;
STAR-EVEN [a:b] SUBANSATZ: SOURCE-SILENT / UNSELECTED;
DISPLAYED KAPPA DIRECTLY SELECTS LAMBDA=1/2: REFUTED;
COMPLETE SHIAB+FERMION SELECTION: NOT EVALUABLE;
AUXILIARY: CAN FORCE ONE-BLADE STAR-EVEN RATIO ONLY;
VECTORLIKE CHI=0 PHYSICAL BASEPOINT: NOT CANONICAL.
```

The actual-source Wave-F gate remains open but is now rebased around the
missing `q6` and source transport:

```text
OPEN -> OPEN_REBASED_COMPONENT_PROJECTOR_BUILT.
```

The route decision is **REBASE**. P1/P2/P3 remain unchanged and unused.

## 0. Layer-0 object table

| phrase | typed object | Wave-F ruling |
|---|---|---|
| source gauge field | `epsilon_src`, the group-valued field in `omega=(epsilon,varpi)` | promoted source field and serious moving-compensator candidate; not identical to `epsilon_IG` by name |
| source connection difference | `T_omega=varpi-epsilon_src^-1 d_0 epsilon_src in Omega1(Y,ad P)` | tensorial difference of two affine connections; adjoint-valued source one-form before projection |
| Zorro data | `Theta_Z`, a connection/splitting giving the chimeric-to-`TY` identification | must transport coindices, metric, Hodge and Riesz data; not N1's distortion `theta_dist` |
| coarse soldering | `epsilon_plane in G/Spin(9,5)` | moves a Clifford plane but forgets the internal `4+10` split |
| moving split | candidate `q_split(epsilon_src,Theta_Z)` into a split reduction | formula-grade local candidate; tilted/global descent open |
| active connection component | `Q6 in C* tensor Lambda6(C*) subset C* tensor sp` | K-anti and right-H; genuine connection-grade object |
| effective real 252 | `phi252 in Lambda5(V_(6,4)*)` | K-self/right-H effective kernel; not itself a connection |
| complex 126 halves | `star=+i` and `star=-i` halves of the real 252 | conjugate comparators; not two independent native real fields |
| exterior-symbol component projector | `Pext^0=j5(1/9)pi_V5 delta` on an already grade-six input | constructed with forced normalization; `q6` from the generic native adjoint is open |
| downstream placement weight | `[a:b]` or `lambda=b/a` in the distinct full-20 rectangular map | star-even projective subfamily of the real four-dimensional exterior Hom-space; not a coefficient of `Pext^0` |
| projected Euler equation | candidate `(P_source)^! E=0` after `q6`, Riesz, and source transport are built | currently open; even then insufficient unless the transverse equation also vanishes |
| stationary VEV / mass | nonzero source-owned solution and observed zero-order operator | unbuilt |
| auxiliary KO zero | `e_hat_0=[H_0]-[R4]=0` inside the supplied comparison family | canonical zero of that auxiliary family only |
| vectorlike `chi=0` completion | an object in a zero-index fibre | not automatically the KO zero or an origin of a spectral-section torsor |

This table corrects one tempting conflation introduced by the Wave-E wording:
the forced `1/9` source component extractor and the source-silent `lambda=1/2`
full-20 placement are different maps.

## 1. Specialist preregistration

Three divergent read-only specialists reviewed the source and repo before the
construction.

1. **Source/differential geometry** proposed the promoted `epsilon_src` plus
   separate `Theta_Z` connection as the strongest source-faithful mover. It
   required the eventual formula `Pext^(Theta,epsilon)=U Pext^0 U^-1`, native-real tangency, right
   split-lift invariance, the actual tilted law, and the transverse Euler
   equation.
2. **Variational/PDE** preregistered the expected verdict
   `COMPATIBLE_UNSELECTED`; required an exact projective Euler ideal rather
   than a fitted state; and separated `AUXILIARY_CAN_FORCE` from
   `AUXILIARY_REQUIRED`.
3. **Full-20/Krein/index** required the density-to-primal Riesz map before
   projection, full K/right-H and conjugate-126 reality, true vector/covector
   transport, and a separate audit of `chi=0` versus the P3 comparison zero.

The expected verdict was registered before computation:

```text
GRADE-SIX EXTERIOR COMPONENT PROJECTOR: CONSTRUCTIBLE;
GLOBAL PUBLIC SOURCE PORT: NOT YET EVALUABLE;
DOWNSTREAM WEIGHT: COMPATIBLE BUT UNSELECTED;
AUXILIARY: POSSIBLE, NOT REQUIRED;
NO-PORT: NOT EARNED.
```

## 2. The exterior projector after grade-six typing

Let `C0=H+V` have signature `(3,1)+(6,4)=(9,5)`. At the native quaternionic
real form, the adjoint algebra has the Clifford grades

```text
2, 3, 6, 7, 10, 11, 14
```

with total dimension

```text
91 + 364 + 3003 + 3432 + 1001 + 364 + 1 = 8256.
```

Grade six is K-anti-self-adjoint and right-H linear. The inherited native
matrix fixture exhaustively rechecks all 210 internal grade-six blades and
all 252 real internal grade-five blades. The general grade-six K sign is the
Clifford reversal sign

```text
(-1)^(6*5/2) = -1.
```

The executable starts with an input already typed in
`C* tensor Lambda6(C*)`; it does not implement `q6` from a generic native
adjoint value. For `phi in Lambda5(V*)`, define

```text
j5(phi)_a = eta_a e^a tensor (e^a wedge phi).
```

The signed pairing adjoint is contraction `delta`. Every internal five-blade
has four horizontal and five vertical complement legs, so the all-blade check
gives `delta j5=9I`. Therefore

```text
Pext^0 = j5 (1/9) pi_V5 delta.
```

This is idempotent on all 252 image blades and on an explicit off-image input.
The observer-first `1/5` normalization fails as a projector on the full
grade-six exterior carrier. Raw Euclidean musical identification also fails
on the five negative directions; the fixed-signature trace-reversed Riesz
comparator is load-bearing. Full Krein self-adjointness of `Pext^0` is not
independently certified.

The real `Lambda5(V*)` has `star^2=-1`. Its two complex 126 eigenspaces must
travel as a conjugate pair. Multiplication of a real grade-five word by `i`
can change K parity only by breaking right-H, reproducing the prior native
real-form obstruction.

## 3. What moves the projector

The source-guided eventual local formula is

```text
U_(Theta,epsilon)
  = (Zorro/Theta coindex transport) tensor Ad_(rho(epsilon_src)),

Pext^(Theta,epsilon)
  = U_(Theta,epsilon) Pext^0 U_(Theta,epsilon)^(-1).
```

The executable does not instantiate `U_(Theta,epsilon)`. Its exact
signed-permutation fixture moves both tensor legs, verifies constant
composition, checks one split-preserving signed permutation that commutes
with `Pext^0`, and verifies conjugation naturality for this model. A live
signed permutation that exchanges one horizontal and one vertical direction
moves the internal split. With the split frozen, a vertical five-form leaves
the 252; with the split transported, the model naturality is restored. This
reproduces RB5's obstruction to the coarse `G/Spin(9,5)` field owning the
internal `4+10` split, but it is neither the tilted source action nor a
`Y14` overlap theorem.

For one infinitesimal internal five-form projector fixture, differentiation
gives

```text
dot P = [dot U U^-1, P],
dot(P T) = P dot T + [dot U U^-1,P]T.
```

The finite first-derivative plant is nonzero and the frozen-projector route
fails. Any complete action must return the corresponding chain term to the
`epsilon_src` and `Theta_Z` Euler equations and to the Green boundary owner;
Wave F has not yet constructed that actual source derivative.

The public/global port is still conditional because the repo has not shown
that the public complex field space reduces consistently to the native
quaternionic fixed locus, that the total Euler field is tangent to that
reduction, or that the actual tilted double-coset law makes `q_split` descend.

## 4. The separate downstream weight question

Once only the `H+V` split stabilizer is demanded, the exterior ansatz admits
two maps from `Lambda5(V*)`:

1. the vertical coindex branch inside `V* tensor Lambda6(V*)`; and
2. the horizontal scalar branch inside `H* tensor H* tensor Lambda5(V*)`.

Their supports are disjoint. The independent Sage certificate verifies the
D5 multiplicities and excludes the only other SO(4)-invariant horizontal
degrees: `Lambda3(V*)` and `V* tensor Lambda2(V*)` contain no 126 halves. Each
of the two real support branches also permits composition with the internal
Hodge complex structure. Thus the full declared real exterior Hom-space has
dimension four. The star-even restriction is the two-dimensional ansatz with
one projective ratio `[a:b]` used below.

This is the downstream placement freedom behind Wave E's

```text
B_lambda = B_V + lambda B_H,
lambda=b/a.
```

For Wave E's one-simple-blade comparator, the exact low-`R` polynomial is

```text
(160/7)(a-2b)^2,
```

so its clean projective point is `[2:1]`. This is not yet a
representation-wide statement and remains a placement fact, not an action
selection.

## 5. Action ownership and the auxiliary price

Weinstein's bosonic action owns `T_omega` and its kappa residual. The displayed
field registry does not contain the downstream rectangular map or `[a:b]`.
The corresponding written selector ideal in `Q[a,b]` is therefore the zero
ideal: this records absence/action-blindness, not a varied Euler flat
direction. Sage confirms that this ideal does not contain `a-2b`.

Even under the intentionally hostile collapse that pushes the kappa norm
through `j_(a,b)`, the pulled Hessian is proportional only to

```text
5a^2+4b^2.
```

It neither distinguishes `[2:1]` from another nonzero real ratio nor gives a
nonzero projective stationary point when `a,b` are themselves varied. A toy
linear comparator `aX+bY` likewise varies to the equations `X=Y=0`; this is
not the unbuilt N1 bridge and does not select a universal structural ratio.

The actual moving Shiab term, total source fermion residual, N1 bridge
cancellation, and complete `G2/Y/P0` placement are not assembled. Their
columns are `NOT EVALUABLE`, not zero. A toy transverse plant also records the
consistency condition:

```text
P^!E=0 does not imply (1-P)^!E=0.
```

The algebraic comparator

```text
S_aux = integral mu (a-2b)
```

has directly checked algebraic Euler equations enforcing `[a:b]=[2:1]` and
`mu=0`, with algebraic Hessian rank two. The isolated zero-jet sector has no
integration-by-parts boundary term. It adds a projective placement field and
a multiplier (or a new constrained coupling sector), and leaves an overall
scale unless it is projectivized or separately normalized. It proves only
`AUXILIARY_CAN_FORCE` for the one-blade star-even comparator; coupled
nonpropagation is open. A topological datum could select a discrete reduction
class, but cannot by itself supply the continuous number `1/2`.

## 6. The `chi=0` side audit

The supplied auxiliary KO family has

```text
e_hat_n = [H_n]-[R4],  n=-1,0,+1.
```

All three classes have virtual rank zero, but

```text
p1(H_n) = -2n u.
```

Thus `n=+/-1` are exact counterexamples to the inference “rank zero or
vectorlike implies KO zero.” `e_hat_0` is the canonical zero of this auxiliary
comparison family because the same trivial bundle and connection are
subtracted from themselves. An arbitrary physical vectorlike `chi=0`
completion is only an object in a zero-index fibre. It is not a canonical
basepoint until common symbol, right-H/K/C carrier, domain, boundary
trivialization, admissible homotopy, reduced KO/eta/spectral-flow residue, and
observer/gauge naturality are proved.

This audit does not reprove vectorlikeness and does not spend P3 or infer a
count.

## 7. Primary-source collision

| source | disposition | consequence |
|---|---|---|
| 2021 draft Sec. 9.1, eqs. 9.1–9.7 | `SOURCE-CONFIRMS` `T_omega`, the bosonic action, and the kappa residual | the port starts from a displayed connection difference |
| 2021 draft eqs. 9.18–9.20 | `SOURCE-CONFIRMS` one total bosonic-plus-fermionic Euler arena | projected bosonic success cannot ignore the fermion/transverse equations |
| Portal/Oxford `01:13:00–01:16:36`, `02:23:30` | `SOURCE-CONFIRMS-ROLE` a varying connection supplies the chimeric-to-`TY` identification | `Theta_Z` must move the covector/metric/Hodge/Riesz data |
| Portal/Oxford `01:22:54–01:25:18` | `SOURCE-CONFIRMS-ROLE` gauge transformations are promoted to field content so projection and gauge motion can commute | `epsilon_src` is a serious moving-compensator candidate, not passive bookkeeping |
| TOE `01:35:23–01:36:08` | `SOURCE-CONFIRMS` the Higgs-like owner is an ad-valued one-form | the connection difference, not a free 126, is the starting type |
| TOE `02:18:17–02:20:33`; UCSD `00:17:01–00:25:03` | `SOURCE-CONFIRMS` gauge-rotated distinguished-connection/distortion architecture | supports the `T_omega` route |
| UCSD `00:42:42–00:43:47` | `SOURCE-CONFIRMS` trace-reversed `(6,4)` fibre | raw positive Frobenius is a planted failure |
| inspected sources | `SOURCE-SILENT` on native `Sp` reduction, `q6`, internal 252, `1/9`, exact `q_split`, total K/C placement, and `lambda=1/2` | these are reconstruction results or open candidates |
| source pack WGS-07 | `SOURCE-CONFIRMS_OPEN` preferred Shiab projection, VEV selection, Dirac piece, and zero-order cancellations | no complete action selection, VEV, or mass is imported |

## 8. Evidence and controls

The direct Wave-F executable passes

```text
29 exact/analytic or deterministic-native checks
 1 independent Sage character/ideal certificate
 4 source-receipt checks
12 Layer-0/type checks
13 live planted rejections
59 total
```

The controls reject a full-14 five-form as the internal 252, observer-first
`1/5`, raw Euclidean musical, an isolated complex 126, a fixed split under an
ambient mover, a frozen moving projector, kappa-to-half-weight inference,
state-fitted toy ratio selection, projected-Euler-to-consistent-truncation
promotion, auxiliary-to-source promotion, rank-zero-to-KO-zero, and
`chi=0`-to-count promotion.

## 9. Gate movement and next gate

Wave F records

```text
RESOLVER-WAVE-F-ACTUAL-SOURCE-SHIAB-FERMION-EULER-WEIGHT-SELECTION
OPEN -> OPEN_REBASED_COMPONENT_PROJECTOR_BUILT.
```

It constructs `Pext^0` only after grade-six exterior typing, a constant
signed-permutation transport schema, and an exact negative result for direct
selection of the downstream half-weight by the displayed kappa term. It does
not construct `q6`, the actual source `U_(Theta,epsilon)`, the public/native
global port, or complete total-Euler selection. The route is therefore
**REBASE**, not a partial construction of the actual-source gate.

The next named gate is
**`RESOLVER-WAVE-G-Q6-NATIVE-SP-TILTED-SOURCE-PORT-AND-TRANSVERSE-EULER`**:

1. construct `q6` on the generic native adjoint field, including exact
   cross-grade annihilation and its density/Krein adjoint;
2. construct or kill the public complex-to-native quaternionic reduction and
   prove total source Euler tangency;
3. construct the actual tilted `epsilon_src -> G/L_split` law and `Theta_Z`
   overlap descent, including Lorentz-component preservation;
4. pull the complete bosonic plus fermionic residual through the Riesz/port;
5. compute both active and transverse Euler equations with the moving chain
   rule and Green boundary owner;
6. only if that truncation closes, assemble the full `G2/Y/P0` and N1
   `J_D/J_F` bridge system and revisit the downstream placement ratio.

## 10. Boundaries

- The exact signed-permutation fixture is local splitting functoriality, not
  an actual `Y14` atlas theorem.
- `epsilon_src` is a stronger source candidate than `epsilon_plane`, but the
  exact tilted quotient map is unbuilt; no identity with `epsilon_IG` is made.
- The fixed local Riesz comparator is not the global Hodge/density/Krein map.
- The forced `1/9` exterior projector after grade-six typing is distinct from the downstream
  source-silent `lambda=1/2` placement.
- The displayed kappa term alone fails to select the half-weight; complete
  Shiab/fermion selection remains not evaluable, not refuted.
- `AUXILIARY_CAN_FORCE` does not mean required, natural, or source-derived.
- `e_hat_0` is not a physical canonical basepoint without same-domain data.
- No stationary VEV, mass, quotient, analytic domain, no-leakage theorem,
  anomaly cancellation, index, generation count, or cosmological prediction
  is claimed.
- P1/P2/P3 remain unchanged and unused.
- No claim, canon, protected verdict, public posture, count, lane,
  Eric/Curt-separation, or third-lane status changes.
