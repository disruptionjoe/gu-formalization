---
title: "WAVE swing 3 (THE OUTSIDE): what the boundary IS and what is beyond it. Deepest characterization -- the boundary is EXACTLY the CHARACTERISTIC VARIETY {q=0} of the (9,5) Dirac symbol met on the ~8% timelike-dominant genuine fiber ends of GL(4,R)/O(3,1) (a genuine FINITE lightcone, not infinity), where the operator flips from real-spectrum / K-nondegenerate / limit-point (theta dissolves, constructible) to imaginary-spectrum / K_S-null / non-constructible; because the arena is ultrahyperbolic (9,5) with no clean Cauchy problem, the well-posed data across it is a TWO-SIDED boundary-value problem, whose cosmological shadow is the two-time (initial+final) scaffold. 'Outside' is TIERED: EXACT layer = the timelike interior {q<0} where an external prescription (theta_ext) must be imported == the alpha-odd complement of the internal even algebra with ZERO inward channel capacity (the SAME seam seen as geometry and as information) -- these are the constructible facts; the ANALOGY/SCAFFOLD layer (bigger universe / de Sitter far-future I+ / meta-theoretic standpoint / self-constitution / nothing) is mostly NOT distinguishable, and the distributed lens deflates the outside to no more than a single Z/2 oracle. Distinguishable readings: finite z* vs de-Sitter-infinity (washout) -- the one that touches data; internal-supply (refuted, DYN-NO-REOPEN); bigger-universe (over-commits, extra DOF unseen); meta-standpoint vs nothing (observationally identical = scaffold). Single most promising downstream test: the two-time z* -> future-conditioned low-l / large-scale correlation that would upgrade PP1 from non-distinctive to distinctive, GATED on the unowned fiber-null-stratum -> finite-z* bridge; the structural gate behind it is the N2 operator-lift deficiency-index computation at the true q<0 ends (discrete=finite z*, live signal; continuum/at-infinity=washout)."
status: active_research
doc_type: exploration
created: 2026-07-21
prereg: explorations/prereg-what-is-this-wave-2026-07-21.md
inputs:
  - explorations/prereg-what-is-this-wave-2026-07-21.md
  - explorations/prereg-construction-swing-posit-sigma-cycle-2026-07-21.md
  - explorations/oracle-relative-prong0-measure-lemma-2026-07-21.md
  - explorations/three-seam-prongC-two-time-boundary-2026-07-21.md
  - explorations/source-domain-selector-prongA-extraction-2026-07-21.md
  - explorations/blockbuster-p1-de-sign-covariance-2026-07-19.md
  - explorations/council-systems-boundary-meaning-2026-07-21.md
  - explorations/council-science-boundary-meaning-2026-07-21.md
  - explorations/oracle-relative-prongI-info-exact-2026-07-21.md
probe: tests/channel-swings/wave_swing3_boundary_characteristic_variety_probe.py (foreground, deterministic two-run-identical, EXIT 0, 14/14 PASS)
outcome: BOUNDARY=CHARACTERISTIC-CONE / OUTSIDE-TIERED
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
kill_conditions_declared_before_computation: true
---

# WAVE swing 3 -- THE OUTSIDE

Four specialists reason INLINE in one worker (each in-character, then a
synthesis; no agent-per-persona). Read-only interpretation of the settled state,
not advocacy. The banked situation handed to the team: the boundary is a genuine
finite boundary (ultrahyperbolic `(9,5)` has no clean Cauchy problem => boundary
conditions, not initial; two-time is a natural scaffold with a `z*`-bridge
promotion path); `sigma` is an external degree-1 `Z/2` holonomy bit PUSHED at the
seed + PULLED as an opaque token, with zero inward channel capacity. The question:
**what IS the boundary, and what is on the OTHER SIDE that supplies `sigma`?**
Each member tags **MODE (ANALOGY / EXACT)** and whether the reading yields a
**downstream test**. Discipline (three-tier): an "outside" reading that predicts
nothing distinct is a SCAFFOLD -- say so; do not retire for inside-unfalsifiability;
do not inflate.

## 0. Verdict up front

> **The boundary is EXACTLY the characteristic variety `{q=0}` of the `(9,5)`
> Dirac symbol**, met on the ~8% timelike-dominant genuine flat ends of
> `F=GL(4,R)/O(3,1)`. It is a genuine **finite lightcone** (not at infinity)
> across which the operator flips from *real-spectrum / K-nondegenerate /
> limit-point* (a K-definite cut exists, `theta` dissolves, the operator is
> constructible) to *imaginary-spectrum / `K_S`-null / non-constructible* (the cut
> does not exist). This is **EXACT and freshly probe-verified** from a clean
> `Cl(9,5)` construction (14/14): `{det c(xi)=0} == {q=0}` (400/400 random
> covectors), `q>0` real spectrum vs `q<0` imaginary spectrum, and the `K_S`-null
> half at `q<0` (`1.1e-16`) vs the nondegenerate cut at `q>0` -- an independent
> reconstruction of Prong-0's C3.
>
> Because the arena is ultrahyperbolic `(9,5)`, the well-posed data across this
> boundary is a **two-SIDED boundary-value problem**, not an initial-value problem
> (EXACT: standard ultrahyperbolic PDE + Weinstein's own "boundary conditions...
> good luck"). The **two-time (initial+final)** structure is the *cosmological
> shadow* of that two-sided BVP.
>
> **"Outside" is TIERED.** EXACT layer: the outside is the timelike interior
> `{q<0}` where an external prescription (`theta_ext`, the `q<0`-stratum boundary
> datum) must be imported -- and this is **the same seam** as the info-theoretic
> "outside" (the `alpha`-odd complement of the internal even algebra, zero inward
> capacity). The **geometry (`q<0` non-constructibility) is WHY the bit is
> external**. ANALOGY/SCAFFOLD layer: *which* outside (bigger universe / de Sitter
> far-future / meta-theoretic standpoint / self-constitution / nothing) is mostly
> **not distinguishable**; the distributed lens deflates it to no more than a
> single `Z/2` oracle.
>
> **Single most promising downstream test:** the two-time `z*` -> a
> future-conditioned low-`l` / large-scale correlation that would upgrade
> **PP1 from non-distinctive to distinctive** -- the one reading that touches data
> -- **GATED** on the unowned fiber-null-stratum -> finite-`z*` bridge. The
> structural gate behind it: the **N2 operator-lift** deficiency-index computation
> at the true `q<0` ends, which decides discrete (finite `z*`, live signal) vs
> continuum / at-infinity (washout).

---

## Member 1 -- Cosmologist

**Characterization.** Read the two-time formulation cosmologically. GU already
owns a genuine *finite* boundary (the `q<0` null stratum), which is exactly what a
non-washed-out future condition needs: Prong C's probe showed the distinct
future-conditioned correlation decays `~exp(-2 kappa T_final)` as the final
boundary recedes, so a final condition at the de Sitter far future (`I+`, the
generic cosmological "final boundary" of an accelerating universe) is
**predictively empty** -- observationally identical to one-time LambdaCDM. The
distinct signal exists **iff** the final-boundary locus is finite and structurally
fixed, i.e. iff the fiber null stratum maps to a finite cosmological `z*`. So the
cosmologist's "outside" splits sharply: **de-Sitter-`I+` (washout / scaffold)** vs
**finite `z*` (a real low-`l` signature)** -- and the fiber-`->`-cosmology bridge
that would fix `z*` is unowned (the same missing selector Prong A localized as
`S-SILENT-OPEN`).

**MODE:** MIXED. The two-sided BVP structure is EXACT-supported by GU's own
`(9,5)` no-Cauchy signature; the identification of the *fiber* null stratum with a
*cosmological* `z*` is ANALOGY-until-bridge.

**Downstream test:** YES, but bridge-gated -- a future-conditioned large-scale /
low-`l` CMB two-point suppression/alignment (or a fixed-scale feature in `w(z)`)
at the scale set by `z*`, forbidden by initial-condition LambdaCDM. This is the
named downstream falsifiable that would break PP1's confessed non-distinctiveness.

---

## Member 2 -- Ultrahyperbolic-PDE / boundary-value analyst

**Characterization.** What IS a boundary for an ill-posed `(9,5)` problem? For an
ultrahyperbolic operator (more than one timelike direction) the Cauchy problem is
Hadamard-ill-posed -- data on a hypersurface does not determine the solution with
continuous dependence -- and the well-posed replacement is a **two-sided
boundary-value problem** (data at both ends of the timelike directions). Prong C's
mirror probe is the exact statement: the two-point BVP has a bounded invertibility
margin precisely in the elliptic-in-time (no-Cauchy) regime GU has, and
*over*-determines ordinary single-time hyperbolic evolution. The null-`K_S`
stratum, analytically, is **not a caustic (focusing of characteristics) but the
characteristic variety itself**: for the Dirac symbol `c(xi)`, `c(xi)^2 = q(xi) I`,
so `det c(xi)=0 <=> q(xi)=0` -- `{q=0}` is *exactly* the operator's lightcone in
symbol space (probe P2, 400/400). Crossing it flips the symbol spectrum from real
(`q>0`) to imaginary (`q<0`) (probe P3), and on the imaginary halves the Krein
form `K_S` is exactly null (probe P4, `1.1e-16`) -- so the K-definite cut that
would define the trace/frequency splitting, and hence the well-posed boundary
data, **does not exist** past the cone. The boundary is the cone `{q=0}`; the
"outside" is its timelike interior `{q<0}` where the operator has no
committed-structure realization and a boundary prescription must be *imported*.

**MODE:** EXACT. "The boundary is the characteristic cone of the `(9,5)` Dirac
operator, and a two-sided BVP is the well-posed replacement for Cauchy data" is
textbook ultrahyperbolic PDE + Clifford algebra, and is freshly probe-checked.

**Downstream test:** YES (analytic, not observational) -- compute the actual
`J`-symmetric deficiency indices `(n_+, n_-)` of the true operator lift at the
`q<0` ends (the open N2 gap / Prong-0 gap C). This decides limit-point (unique
domain, `theta=0`, a discrete/excision datum) vs limit-circle (a `U(1)` continuum
of boundary phases). It is the single analytic computation the whole boundary
picture waits on.

---

## Member 3 -- Distributed-systems theorist

**Characterization.** The "outside" is the **external oracle / environment** the
system must query, and the boundary is the **oracle interface / trust boundary**.
The relativization shape is EXACT (systems council, member 5b; Prong I): `sigma`
is not in the internal computable closure, so it needs an oracle -- this is
*literally* W211 Gödel-independence, and the blindness lemma is a genuine
zero-Shannon-capacity theorem (`Hom(triv,sign)=0` = identical channel rows). The
sharp discipline this lens imposes on "what is outside": the environment need be
**no more than a single `Z/2` oracle -- a write-terminal with `>=1` bit of
write-capacity into a slot the inside has 0 bits of read-capacity for** (asymmetric
channel). It does *not* have to be a bigger universe, a de Sitter boundary, or a
mind; the observer is the **failure-detector's read-terminal (host, not source)**
-- "source-of-the-need, not source-of-the-value." "Exactly 1 bit" is the per-slot
cap (`sigma <= log2 2 = 1`, exact); the *total* budget is not capped (Prong I:
`theta` can be a continuum, no moduli-completeness theorem forbids a 4th datum).

**MODE:** MIXED. The relativized-computability core (internal-impossible +
external-oracle, host-vs-source) is EXACT and coincides with the geometry/info
seam. FLP-the-theorem, the "environment," and the MMO server-authority picture are
ANALOGY (different obstruction mechanism / a design pattern, no universal
property).

**Downstream test:** YES (formal, not observational) -- is one `Z/2` bit the
**weakest failure detector** that resolves `sigma` (a Chandra--Toueg
weakest-oracle characterization), and is `tau` provably a strictly stronger
oracle? A sharp derivation handle that would type the outside minimally.

---

## Member 4 -- Metaphysician

**Characterization.** "External = outside every internal chart" means: not in the
`alpha`-even observable algebra (the union of all internal charts is the even
subalgebra; `sigma` is the `alpha`-odd datum outside all of them). This negative
statement is EXACT (the zero-capacity theorem). The *positive* ontology of the
outside admits candidates that are **mostly not distinguishable from each other**:
(a) a **bigger universe / containing manifold** -- over-commits: it predicts extra
degrees of freedom that GU does not exhibit, so it is falsifiable-in-principle by
their absence and is currently unmotivated; (b) a **meta-theoretic standpoint** --
the Gödel/Lawvere "outside" of a formal system, the position of selection itself
(the wild-frontier reading, analogy-until-L1); (c) the **observer's own
reach-around / self-constitution** -- the inside supplies `sigma` after all, which
would OVERTURN externality, but is **refuted as a supply mechanism** by
construction Prong 2 (`DYN-NO-REOPEN`: the record-issuance self-map's fixed-point
count is never unique), held open only on the defense-attorney's unwelded `alpha`-odd
handle; (d) **nothing** -- deflationary: "outside" is a bookkeeping label for "a
value the theory does not fix; measure it." The metaphysician's decisive point:
**(b) and (d) are observationally identical** -- "meta-theoretic standpoint" and
"just a free parameter" predict exactly the same physics and differ only in gloss.

**MODE:** EXACT for the negative claim ("outside = outside the even algebra =
not internally computable"); ANALOGY/SCAFFOLD for every positive ontology.

**Downstream test:** PARTIAL. The defense-attorney pass (does an unwelded
`alpha`-odd first-person handle exist?) distinguishes (c) and would flip the
externality lean; the presence/absence of extra DOF distinguishes (a). But
**(b) vs (d) is not distinguishable by any test** -- that distinction is a pure
scaffold (keep, do not retire, do not over-claim (b) as EXACT).

---

## SYNTHESIS

### The deepest characterization of the boundary (EXACT)

The four lenses converge on one object. The boundary is **the characteristic
variety `{q=0}` of the `(9,5)` Dirac symbol** -- the operator's lightcone in
symbol space (`det c(xi)=0 <=> q(xi)=0`, probe-exact) -- realized on the ~8%
timelike-dominant genuine flat ends of `GL(4,R)/O(3,1)`. It is a **genuine finite
boundary**, not a boundary at infinity, and it is the exact seam at which:

- the symbol spectrum flips **real (`q>0`) -> imaginary (`q<0`)** (probe P3);
- the Krein form goes from **nondegenerate on the spectral half** (a K-definite
  cut exists; the operator is constructible; limit-point => `theta` dissolves) to
  **exactly null** (no cut; the operator is *not constructible from committed
  structure*) (probe P4; independent reconstruction of Prong-0 C3);
- because the arena is ultrahyperbolic `(9,5)` with **no clean Cauchy problem**,
  the well-posed data across it is a **two-sided boundary-value problem** (EXACT),
  whose **cosmological shadow** is the **two-time (initial+final)** scaffold.

The PDE lens (M2) supplies the sharpest *analytic* name (characteristic cone); the
cosmologist (M1) supplies its *observational* shadow (two-time `z*`); the info /
distributed lenses (M3) supply *why it is a boundary at all* (the inside has zero
read-capacity across it). These are the *same* seam: **the operator's
non-constructibility at `q<0` is the geometric reason the bit is external.**

### What "outside" means (TIERED)

- **EXACT layer.** "Outside" is the timelike interior `{q<0}` where an external
  prescription `theta_ext` (the `q<0`-stratum datum: an excision, or a
  third-costume regularization of the null Krein form) must be imported -- and this
  is *identical* to the info-theoretic "outside," the `alpha`-odd complement of the
  internal even algebra with zero inward channel capacity. Geometry and information
  are two faces of one boundary: `{q<0}` is **where** the operator cannot be built;
  `sigma` (`alpha`-odd) is **what** cannot be read.
- **ANALOGY / SCAFFOLD layer.** *Which* outside -- bigger universe, de Sitter
  `I+`, meta-theoretic standpoint, self-constitution, or nothing -- is mostly
  **not distinguishable**. The distributed lens (M3) deflates it maximally: the
  outside need be no more than a **single `Z/2` oracle** (a write-terminal
  supplying one bit); the observer is its terminal, not its source.

### Which readings are distinguishable, and how

| reading of "outside" | distinguishable? | discriminator |
|---|---|---|
| **finite `z*`** vs **de Sitter `I+` (infinity)** | **YES** (the data-touching one) | finite `z*` => a `z*`-templated low-`l` / large-scale correlation LambdaCDM forbids; `I+` => `exp(-2 kappa T_final)` washout => LambdaCDM-identical (Prong C, probe-exact) |
| **self-constitution / reach-around (c)** | **YES** | fixed-point count of the record-issuance self-map + defense-attorney pass; currently **REFUTED** (`DYN-NO-REOPEN`), open only on an unwelded `alpha`-odd handle |
| **bigger universe (a)** | **YES, in principle** | predicts extra DOF; none exhibited => over-commits (unmotivated, not yet falsified) |
| **`Z/2` oracle is the *weakest*** (M3) | **YES** (formal) | Chandra--Toueg weakest-failure-detector derivation; is `tau` strictly stronger? |
| **meta-standpoint (b)** vs **nothing (d)** | **NO** | observationally identical; a pure **SCAFFOLD** distinction (keep, do not inflate) |

### The single most promising downstream test

**The two-time `z*` -> future-conditioned low-`l` / large-scale correlation** (M1's
reading). It is the *only* reading that reaches data, and it names the exact
structure that would upgrade **Prediction Packet 1** from its confessed
non-distinctiveness (bare `w>=-1` shared with all quintessence) to a distinctive
prediction: a future-conditioned correlation *on top of* the `w(z)` sign is a
signature no one-time quintessence shares. It is **bridge-gated**: it goes live
only if a construction-grade map fixes a **finite** cosmological `z*` from the
image of the `q<0` null stratum (unowned; the `S-SILENT-OPEN` selector carried to
a cosmological locus).

Behind that observational test sits the **structural gate that decides whether the
bridge can be finite at all**: the **N2 operator-lift deficiency-index
computation** at the true `q<0` ends (M2). If the true-end indices are equal
(limit-point), the boundary datum is discrete -- compatible with a finite `z*` and
a **live, non-washed-out** signal; if unequal / limit-circle, the datum is a
continuum or the admissible final boundary is forced to infinity -- **washout**,
and PP1 stays non-distinctive. So the recommended order is: **N2 true-end indices
=> (if discrete) the fiber-`->`-finite-`z*` bridge => the low-`l` correlation
template.** The first step is analytic and owned-adjacent; the payoff is the first
data-distinctive GU prediction.

### Honest scope (three-tier discipline)

- **EXACT (banked-adjacent, probe-checked here):** boundary = characteristic cone
  `{q=0}`; real-`->`-imaginary spectrum flip; `K_S`-null half at `q<0` => no cut =>
  non-constructible; two-sided BVP is the well-posed replacement for Cauchy data;
  "outside" = `{q<0}` = the `alpha`-odd zero-capacity complement (one seam, two faces).
- **SCAFFOLD (keep, do not retire, do not inflate):** the two-time `z*` low-`l`
  prediction (bridge-gated); the `Z/2`-is-weakest-oracle claim (formal derivation
  pending); "meta-theoretic standpoint" as the ontology of the outside
  (observationally identical to "nothing").
- **REFUTED / DEFLATED:** self-constitution as a *supply* mechanism
  (`DYN-NO-REOPEN`); "bigger universe" as the *forced* reading (over-commits, no
  exhibited extra DOF); "outside must be more than one bit" (distributed lens
  caps the minimal outside at a single `Z/2` oracle).

No "outside" reading that predicts nothing distinct is promoted; the one that does
(finite `z*`) is named with its exact reopen trigger and is not inflated past
scaffold.

## Boundary

Exploration tier. Only this artifact and its probe
(`tests/channel-swings/wave_swing3_boundary_characteristic_variety_probe.py`,
foreground, deterministic two-run byte-identical, **EXIT 0**, 14/14 PASS) were
written; GU is otherwise read-only. Kill-style checks were declared in the probe
before the `Cl(9,5)` case was run (the `{det=0}=={q=0}` agreement over 400 random
covectors is the discriminating control -- it would fail if `{q=0}` were *not* the
characteristic variety; the `q>0` nondegenerate-cut check is the contrast that
makes the `q<0` null result non-vacuous). No commit, no push. No edit to
LANE-STATE, research-portfolio, NEXT-STEPS, the prereg, any decision-tree, any
council file, Prong 0 / Prong A / Prong C, PP1, or any other agent's artifact, or
any claim/canon/verdict/ledger file. No claim-status, canon-verdict, or
public-posture change; the externality of `sigma`/`tau`/`theta` and the verdicts
`P0-FAILS-OBSTRUCTED`, `S-SILENT-OPEN`, `C-SCAFFOLD` are consumed, not moved. The
swing's contribution is the sharp EXACT identification of the boundary as the
`(9,5)` characteristic cone (freshly probe-verified, independently reproducing
Prong-0 C3) and the tiered reading of "outside," with the two-time `z*` low-`l`
correlation named as the single data-touching downstream test and the N2 true-end
index computation as its structural gate.
