---
artifact_type: exploration
created: 2026-08-04
title: "Wave K: Curt's (7,7) is source-coherent and the normalized-trace (9,5) B1 remains only a comparator"
grade: "Exact local conditional-active (9,5) normalized-trace candidate with live monolithic variation and a candidate-local q_wedge mismatch. Exact source-signature ledger and primary-source effective-chirality receipts. Source [T,T] normalization, a real Cl(7,7) matter carrier/action, a decoupling theorem, global lowering/port/Ward/domain, and physics remain open."
named_gate: RESOLVER-WAVE-K-NATIVE-SHIAB-MONOLITHIC-B1-VARIATION-AND-PORT-PLACEMENT
gate_before: LOCAL_SOURCE_DENSITY_AND_TRANSLATION_COMPARATORS_WITH_COSET_CURVATURE_OBSTRUCTION
gate_after: NORMALIZED_TRACE_FIXED_ACTIVE_95_QWEDGE_B1_CANDIDATE_MISMATCH
route_disposition: REBASE
source_collision: SOURCE-CONFIRMS-B1-GRAMMAR-77-SIGNATURE-SPINOR-MATTER-AND-EFFECTIVE-CHIRALITY; SOURCE-SILENT-BRACKET-NORMALIZATION-DECOUPLING-THEOREM-AND-ACTIVE-95-PORT
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
third_lane_promoted: false
---

# Resolver Wave K: source `(7,7)` becomes the primary reconstruction burden

## Result first

Wave K produced a real, nonvacuous local action candidate and then hostile
review materially narrowed what it means.

On the conditional-active `(9,5)` branch, using the **normalized DeWitt trace
direction** rather than an arbitrary vertical blade, the executable constructs

\[
 I^{q\wedge}_{K95}(B,T)=\int T\wedge\left[
 \widehat\odot_{95}\left(
 F_B+\frac12D_BT+\frac13q_\wedge(T)
 \right)+\frac{\kappa_1}{2}*T\right],
 \qquad q_\wedge(T):=T\wedge T.
\]

All four channels are live. Direct differentiation of the whole action agrees
with a separately assembled owner derivative:

```text
delta_B direct = delta_B owner = -3/4
delta_T direct = delta_T owner = 13*kappa_1/4 + 221/36
delta_(B+T) direct = delta_(B+T) owner = 13*kappa_1/4 + 97/18
```

The candidate's selected zeroth-order Euler coefficient differs from the
repository comparator
`widehat-odot_95(F_B + D_B T + q_wedge(T)) + * kappa_1 T` by

\[
 \Delta_K(x,y)=-x^2+\frac32xy^2+\frac32xy-
 \frac54x+\frac32y-\frac14\ne0.
\]

This is an exact mismatch for **this fixed, normalized-trace, native-grade,
`q_wedge` candidate**. It is not a refutation of Weinstein's displayed source
equation, because the source writes `[T,T]` and the checked source material
does not fix its graded-bracket normalization relative to the probe's
`T wedge T`. A common convention would introduce a factor of two. The source
identity is therefore `NOT_TESTED`, not killed.

The more important result is upstream: Curt and Eric's `(7,7)` is internally
coherent and is tied to their proposed matter carrier. It should now be the
primary source-aligned reconstruction route. The existing `(9,5)` construction
is retained as a strong conditional comparator or possible alternative real
form, not silently treated as the source geometry.

## Plain English

Curt's explanation is:

1. The ten directions that change the four-dimensional metric have raw
   Frobenius signature `(7,3)`.
2. GU trace-reverses that fibre metric, changing it to `(6,4)`.
3. Curt and Eric use the spacetime convention `(1,3)`.
4. Adding the two pieces gives `(1,3)+(6,4)=(7,7)`.

The repo's active construction used the opposite horizontal convention
`(3,1)`, giving `(9,5)`. At the level of quadratic forms this starts as a sign
choice. At the level of real spinors it is a genuine fork:

\[
 \mathrm{Cl}(7,7)\cong M_{128}(\mathbb R),\qquad
 \mathrm{Cl}(9,5)\cong M_{64}(\mathbb H).
\]

That matters because GU places fermionic matter in chimeric spinors and
spinor-valued forms. A real 128-component carrier and a quaternionic
64-component carrier may have the same complexification, but their reality,
pairing, chirality, and allowed reductions are not automatically the same.

Eric also explicitly says GU is **not fundamentally chiral**. His proposed
mechanism is instead:

```text
non-chiral Dirac/Rarita-Schwinger parent
  -> a curvature-linked VEV couples two chiral halves
  -> sufficiently low curvature/mass makes a Dirac-type operator decouple
  -> effective Weyl sectors give the observed chiral world
  -> a hidden/dark partner restores parity in the larger theory
```

So GU does not need to derive fundamental chirality. It still must derive the
observed chiral weak theory as an effective sector. The sources state the idea
but provide no decoupling theorem, quantitative scale, anomaly calculation, or
proof that the surviving Weyl sector has the Standard Model representations.

## 0. Layer-0 objects

| object | Wave-K meaning | forbidden identification |
|---|---|---|
| public bosonic Shiab | source candidate `Omega2(ad)->Omega13(ad*)` on source `(7,7)` | canon spinor Shiab |
| canon spinor Shiab | `Omega2 tensor S -> Omega1 tensor S` | bosonic source operator |
| `widehat odot_95` | fixed-frame normalized-trace, native-grade repository candidate | source `(7,7)` Shiab |
| `R_J` | map defined only on typed public `u(K)` in the chosen `(9,5)` real structure | arbitrary projection of a raw Shiab word |
| native-grade filter | fixed-frame projection onto the exercised real native carrier | source-to-active real-form bridge |
| `q_wedge(T)` | probe's explicit `T wedge T` comparator | source `[T,T]` before normalization is proved |
| exercised lowerer | `Dual13` representative tested on a three-bivector pairing panel | global `ad -> ad*` isomorphism or coadjoint theorem |
| `Psrc` | rank-252 `Omega1->Omega1` source/output candidate | the `Omega2->Omega13` Shiab |
| Hodge-conjugated port | one fixed-background fixture | global rank-252 descent or moving port theorem |
| effective chirality | proposed low-curvature Dirac-to-Weyl decoupling | absence of an obligation to recover chiral SM couplings |
| `(7,7)` | source `(1,3)` plus trace-reversed fibre `(6,4)` | relabeled quaternionic `(9,5)` branch |

## 1. Exact signature and matter-source ledger

The executable independently verifies

\[
 \operatorname{Sym}^2(T^*X):(7,3)
 \xrightarrow{\text{trace reversal}}(6,4),
\]

then both totals

\[
 (1,3)+(6,4)=(7,7),\qquad (3,1)+(6,4)=(9,5).
\]

Dualization `H -> H*` preserves inertia; it does not explain the difference.
The spoken “four metrics, two viable” selector remains underived and is not
used.

Primary-source locators now carried by the gate are:

- TOE `02:36:02–02:37:37`: fundamental nonchirality, effective chirality,
  VEV coupling, and Dirac-to-Weyl decoupling;
- TOE `02:41:12–02:43:30`: chimeric spinors, the rolled fermionic complex,
  and the `U(64,64)` presentation;
- TOE `02:50:38–02:52:38`: observed spinors plus additional conjugate and
  higher-spin matter sectors;
- Portal/Oxford `00:22:37`: emergent chirality and a missing sector that would
  restore parity.

These are source claims, not completed mathematics.

## 2. The repaired `(9,5)` candidate

Hostile review found that the first draft inserted `e8`, an arbitrary positive
vertical direction. Wave I and the adjacent moving-trace machinery locate the
normalized DeWitt trace at index `10`, a negative unit direction. Wave K was
rebuilt with `TAU=e10` and trace-carrying fixtures `e0,10` and `e2,10`.

The fixed map has exterior type `Omega2 -> Omega13`. Both displayed Shiab
terms are live, it is linear in curvature, and its entire exercised output is
simultaneously public-`u(K)` typed and in the selected native grades. The raw
word is still rejected by strict `R_J`, so the grade filter is explicitly a
repository candidate—not a source port.

The scalar-trace lowering is wrapped as `Dual13`, and the kappa/Hodge term is
lowered through the same exercised interface before addition. Nondegeneracy is
proved only on the exercised three-bivector panel, not on every native
coefficient direction or under global coadjoint transport.

## 3. Variation, calibration, and mismatch

`F_B`, `D_B T`, and `q_wedge(T)` are live and linearly independent. The
source-shaped part remains nonzero at `kappa_1=0`, so this is not a mass-only
false positive. Direct and owner variation agree independently in `B`, `T`,
and the simultaneous direction.

Wave J's independent cyclic matrix comparator remains a positive calibration:
there, coefficients `(1/2,1/3)` exactly produce unit linear and quadratic
weights. Wave K's nonzero defect therefore does not arise from a matcher that
can only report failure. It is candidate-local and survives the corrected
trace selector.

The selected field channel has no derivative on its scalar test amplitude:
`p_x=p_y=boundary=0`. It proves a zeroth-order Euler comparison only, not a
live Green concomitant or analytic domain.

## 4. Scoped covariance and port witnesses

One infinitesimal active fixture has two nonzero owner contributions—field
motion and moving `Phi/trace` motion—which cancel exactly. Freezing the
operator or omitting the inhomogeneous `d xi` response fails. This is a
nonvacuous owner-cancellation witness, not an independently differentiated
Ward theorem, global `Spin(9,5)` covariance result, or source epsilon orbit.

At fixed metric and chosen `J`, one selected image fixture satisfies
idempotence and the Hodge-conjugated top-form adjoint identity. The form
adapter correctly rejects feeding an `Omega13` object directly to the
`Omega1` port. No global rank, descent, moving-background compatibility, or
Euler tangency follows.

## 5. Route decision

The route is rebased as follows:

1. **K77-A — source real matter carrier.** Build `Cl(7,7)`, its real spinor
   module, invariant bilinears/Krein structure if present, and the observation
   branching to `Spin(1,3) x Spin(6,4)`. Check the claimed visible and missing
   fermion sectors without importing the `(9,5)` right-H structure.
2. **K77-B — source bosonic action.** Resolve the `[T,T]` graded-bracket
   normalization, construct the displayed degree-correct Shiab/B1 action on
   the `(7,7)` carrier, and vary it.
3. **K77-C — effective chirality.** Construct a VEV-dependent Dirac/Rarita-
   Schwinger block and test an actual low-curvature Dirac-to-Weyl decoupling,
   the visible SM chiral representation, its hidden parity partner, and anomaly
   consistency.
4. **K95 comparison.** Only afterward decide whether `(9,5)` independently
   realizes the same physics or is connected through a justified complex-parent
   relationship. A shared complexification alone is insufficient.

This ordering uses the geometry in which the source says matter lives as the
primary hypothesis while preserving the exact `(9,5)` work as discriminating
evidence.

## 6. Required atomic physics crosswalk

The existing Eric-native atlas has the correct sector skeleton but is too
coarse for the new route. It groups photon/weak/strong and treats Dirac matter
at the carrier level. K77 must revise it into an atomic ledger with at least
these separately scored rows:

- left- and right-handed charged leptons, neutrinos, and any `nu_R`;
- left- and right-handed up/down quark multiplets with color and hypercharge;
- the photon, `W+`, `W-`, `Z`, and eight gluon modes;
- the Higgs doublet's four real components, eaten Goldstone directions, and
  surviving scalar mode;
- graviton physical modes and their universal stress-current incidence;
- each additional conjugate, spin-3/2, 144-dimensional, or dark parity-partner
  sector claimed by the source;
- the cosmological/distortion scalar separately from the Higgs scalar.

Every row should carry:

| field | required information |
|---|---|
| observed identity | Lorentz spin, `SU(3)xSU(2)xU(1)` representation, charge, chirality, and generation label |
| GU parent | exact `Cl(7,7)` real module, `Omega^p(Y,S)` slot, connection/curvature/distortion component, and stabilizer representation |
| observation map | lift, pullback/equation dual, projector, normalization, quotient, and no-leakage condition |
| dynamics | parent action term, kinetic symbol/domain, Euler equation, current, mass/VEV/Yukawa incidence, and mixing owner |
| nonchiral completion | opposite-parity partner, coupling/decoupling block, curvature scale, and why the visible sector is effectively chiral |
| consistency | anomaly contribution, reality/Krein pairing, positivity/unitarity gate, and datum debit |
| evidence | exact source locator, repo certificate, status grade, and planted kill condition |

Rows are frozen monotonically: a later parent candidate may be accepted only
if it preserves every already-earned row. Unmapped components are then genuine
construction targets or predictions. They are not permission to relabel a
different representation after the fact.

## 7. Honest boundary

Closed here:

- Curt/Eric's exact `(7,7)` arithmetic and its distinction from `(9,5)`;
- primary-source typing of spinors as matter and chirality as emergent;
- a corrected normalized-trace fixed `(9,5)` `Omega2->Omega13` candidate;
- a live monolithic `q_wedge` B1 candidate and exact direct/owner variations;
- a nonzero candidate-local transgression mismatch;
- one scoped gauge-owner cancellation and one fixed-background port witness.

Open:

- source bracket normalization and the actual source `(7,7)` Shiab/action;
- the real `(7,7)` spinor branching, pairing, fermionic operator, VEV block,
  effective-Weyl decoupling, hidden parity sector, and anomalies;
- global lowering, covariance, port descent, domain, BV quotient, observation,
  and no-leakage;
- every VEV value, mass prediction, Standard Model equation, generation count,
  dark-matter claim, or cosmological prediction;
- every use of external datum P1/P2/P3.

No protected claim, canon verdict, public posture, or third-lane status moves.
