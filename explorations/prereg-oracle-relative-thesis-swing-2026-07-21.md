---
title: "Pre-registration: the ORACLE-RELATIVE THESIS swing — make the councils' convergent read a theorem or break it (input-gated on the Condorcet aggregation + the Q1a θ verdict)"
status: preregistration
doc_type: scoping_decision_tree
created: 2026-07-21
directed_by: "Joe direct chat, 2026-07-21 (orchestrate the next big swing to follow up on the two councils)"
inputs:
  - explorations/council-science-boundary-meaning-2026-07-21.md
  - explorations/council-systems-boundary-meaning-2026-07-21.md
  - explorations/council-aggregation-condorcet-2026-07-21.md (PENDING — picks the target prongs)
  - explorations/decision-tree-Q1a-hostile-verify-2026-07-21.md (PENDING — sets the θ bit-budget term)
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
external_actions: none
kill_conditions_declared_before_computation: true
---

# The ORACLE-RELATIVE THESIS swing

## The convergent thesis both councils landed on (the thing to harden or break)

> **GU is an ORACLE-RELATIVE theory: complete modulo a finite external
> information budget the inside provably has ZERO capacity to read.** The budget
> is `σ` (1 bit, DE sign, FREE — Q2 robust) `+ τ` (≈log₂3 ≈ 2.58 bits, generations,
> independent — Q3) `+ θ` (0 bits if the boundary is forced-unique, or a continuum
> if free — Q1a, pending HV). The boundary is a **channel with asymmetric
> capacity**: ≈0 inward, ≥1 outward. The observer is the **read-blind write-carrier**
> — the source of the *need* for the datum, not of its value.

Both councils' EXACT-graded claims point here: info-theorist (`I(internal;σ)=0`,
EXACT 80%), FLP (oracle-relativization = W211 Gödel-independence, 72%), orthodox
(superselection + self-adjoint-extension + moduli, EXACT 80%), philosopher (the
blindness lemma is literally true). The rest (metastable tie, hashgraph order,
MMO authority, crypto hiding) are ANALOGY. So the swing targets the EXACT layer:
**promote the thesis from "strong lean + analogies" to a theorem, at the exact
seams the councils flagged as the promotion-gates.**

## The four candidate prongs (fire the Condorcet-winning subset)

### Prong I — INFO-EXACT (formalize the bit-budget)
Is the information accounting EXACT, not metaphor? Prove/refute: inward capacity
`I(internal;σ)=0` is a genuine zero-capacity theorem (the blindness lemma
`Hom(triv,sign)=∅` IS zero mutual information, not a rhyme); `σ`=1 bit, `τ`=log₂3
exactly; and whether "hosting σ" imposes a **write-ceiling** (no more than σ+τ+θ
importable). Deliver the external budget as a well-typed quantity. Control: a
planted "GU secretly imports a 4th bit" must be caught or admitted.

### Prong II — FLP-EXACT (is the obstruction literally FLP?)
Is "GU cannot mint σ internally" a genuine relativization/impossibility theorem
or an analogy? Test: is one Z/2 the **weakest failure-detector** that resolves σ
(Chandra–Toueg), is τ strictly stronger, and is FLP's faulty process **literally
the observer's causal future = TaF T19**? Separate the EXACT shape
(internal-impossible + external-oracle) from the ANALOGY mechanism (valency vs
equivariance proof). Cross-repo READ of TaF/TI.

### Prong III — EXHAUSTIVENESS → THEOREM (promote the first-person reading)
The Q2 defense-attorney made the standpoint dichotomy exhaustive at *fixture*
grade (α-parity is grade-independent). Promote it to operator/Lean grade: land
**L1** (the assembly that makes physical σ-blindness a bona fide Lawvere
fixed-point instance) and the **operator-grade base-uniting map** (Q3's named
residue). This is the single promotion-gate the science council said the whole
"the bit IS the first person" reading is analogy-until.

### Prong IV — RETRACTION REFRAME (the wild-frontier's high-interest recast)
Since the time-face is a causal-past **retraction** (strictly more general than an
involution, TaF), is "outside" better modeled as a **finality/closure horizon**
than a Z/2 mirror — recasting the missing piece from a bit to a *direction of
closure*? This routes to the Time-as-Finality program and may re-type the whole
budget. Highest reach, lowest current grade — run only if it ranks.

## Input-gating (why this is staged, not yet fired)

- **The Condorcet aggregation picks the prongs.** Fire the top-ranked-across-lenses
  prongs (Joe's consensus criterion), not all four. The winning *questions* become
  each prong's precise kill-conditioned target.
- **The Q1a hostile-verify set θ — RESOLVED `HV-REFUTE` → `Q1a-BLOCKED-SHARPENED`.**
  The boundary does NOT force-close from committed structure (the FORCED lean planted
  a toy; true-end coefficients blow up; GL(4,R)/O(3,1) has no invariant Riemannian
  metric). θ is neither 0 nor a proven continuum — it is **conditional on one exactly
  named measure-lemma**: *a GU-owned measure `w(s)ds` making `A~` J-self-adjoint with
  EQUAL Krein deficiency indices, with bounded measure-normalized `W~`,`φ'` and all
  fiber ends spacelike-gapped.* Under it θ=0 and the pencil theorem closes; without it
  θ is undetermined. The one theorem-grade survivor: `B` is non-degenerate wherever
  `P>0`. **This promotes a concrete Prong 0 below.**

### Prong 0 — THE MEASURE-LEMMA (new, highest-value concrete target from Q1a HV)
Attack the exact reopen lemma: does GU own a measure `w(s)ds` making `A~`
J-self-adjoint with EQUAL Krein deficiency indices, bounded normalized `W~`,`φ'`,
and spacelike-gapped ends? If YES → θ=0, `OPERATOR-END-PENCIL` closes from
structure (a paper), the bit-budget is exactly σ+τ. If NO / the ends generically
cross into the K-null sector → θ genuinely external or the operator has no
self-adjoint realization at all (a different, sharper obstruction). This is the
single most decision-relevant open target and does not depend on the aggregation.

## Anti-circling + discipline (binding)
Every prong returns a VERDICT (EXACT / ANALOGY / REFUTED), never a
re-characterization; externality stays a closed premise; each EXACT claim is
hostile-verified before banking; planted controls mandatory; no claim/canon/
verdict/posture moves without Joe. Prongs run in parallel (independent seams);
each writes one durable artifact + a foreground probe.

## SWING RESULTS (3 of 4 in; Prong III pending)

- **Prong 0 — `P0-FAILS-OBSTRUCTED`** (`oracle-relative-prong0-measure-lemma-2026-07-21.md`).
  The pencil theorem does NOT close from structure. GU DOES own a native measure
  (DeWitt (9,5) volume `w=√|det G|`), but the sign of `q=P−T` is measure-INVARIANT
  (built from the symbol + `K_S`), so no measure fixes clause (c). Clause (c) is
  FALSE: the genuine ends are the diagonal FLAT geodesics (NOT boosts — boosts are
  isometries), and ~8% of 2000 generic ends cross into `q<0`, where `K_S` is
  exactly NULL on the spectral halves and the operator `A~` is **not constructible
  from committed structure at all** (its K-definite cut doesn't exist). This is
  UPSTREAM of `B` degeneracy (B-non-degeneracy survives). Gapped ends: equal Krein
  indices ⇒ limit-point ⇒ θ dissolves there. Crossed ends: index genuinely open
  (needs the N2 operator lift; agent refused to plant). **θ is external not as a
  phase but as a required PRESCRIPTION for the `q<0` stratum (excision/
  regularization).** Sharper than "θ external."
- **Prong I — `I-MIXED`** (`oracle-relative-prongI-info-exact-2026-07-21.md`).
  (1) **Zero inward capacity = EXACT, THEOREM-GRADE:** `Hom(triv,sign)=0` is Schur ⇒
  the σ→internal channel has IDENTICAL ROWS ⇒ Shannon capacity exactly 0, prior-FREE;
  non-vacuous (same α-even algebra carries 2 bits about the orbit label, 0 about σ).
  Rests on the α-even-algebra model of "the inside" — the one HV target, and exactly
  what Prong III tests. (2) **Hard import ceiling I2 = ANALOGY:** no ceiling theorem;
  total bit-count uncapped because θ can be a continuum; the "finite budget" thesis
  WEAKENS (rests on θ=0, which Prong 0 just denied, + unproven enumeration-
  completeness). (3) **Bit-count invariant = MIXED:** coarse digital-vs-analog
  ranking is cut-robust; the fine cross-theory integer is cut-relative.
- **Prong II — `II-MIXED`** (`oracle-relative-prongII-flp-exact-2026-07-21.md`).
  GU IS oracle-relative in the **logic/independence sense** — EXACT: σ internally
  undecidable (W211 Gödel-independence, proof-grade) + one Z/2 axiom (Krein-
  positivity) resolves it = the standard model-theoretic meaning of oracle-relative.
  NOT oracle-relative in the complexity sense (no relativized TM). The FLP
  MECHANISM is ANALOGY: GU's obstruction is `Hom_G(triv,sign)=0` (Schur equivariant-
  vanishing), FLP's is bivalence/π₀-connectivity — same Lawvere/diagonal SCHEMA,
  different invariant, no identifying functor. Chandra–Toueg ordering = bit-
  cardinality only; FLP-faulty-process = TaF T19 = three distinct engines (already
  certified LEG-deep not mechanism-deep). Planted Byzantine control rejected.

### Consolidated (pending Prong III)
The EXACT theorem-grade core that SURVIVES is ONE fact from two angles: **GU's
inside has provably ZERO capacity to read σ** (Prong I.1, Schur/Shannon) = **σ is
Gödel-independent and GU is oracle-relative in the logic sense** (Prong II, W211).
The councils' top-2 (INFO + FLP) both reduce to this, and it is a theorem — but as
a logic/information fact, NOT an FLP mechanism, NOT a complexity oracle. What is
ANALOGY / WEAKENED: the FLP machinery, the hard import ceiling, and the tidy
"finite bit-budget" framing — because Prong 0 shows θ is not a clean bit but a
prescription for a stratum where the operator isn't even constructible, and the
total budget is not provably finite. Honest headline: σ-unreadability is EXACT and
proven; "GU = complete modulo a finite external budget" is too clean.
