---
title: "Blockbuster P4: the two generation doors -- the (9,5) rank fold attacked candidate-by-candidate, and the (7,7) eta pinning converted into a typed p2c demand"
status: active_research
doc_type: exploration
created: 2026-07-19
directed_by: "Parent dispatch, 2026-07-19 (Blockbuster swing, Pathway 4; inline five-lens council mandated)"
axiom: lab/process/boundary-adapter-standing-axiom.md
extends:
  - explorations/p77-real-index-W1-builder-2026-07-19.md
  - explorations/p77-real-index-W2-twin-2026-07-19.md
  - explorations/channel-swing-CH-SM-2026-07-19.md
inputs:
  - canon/no-go-quaternionic-parity-generation-sector.md
  - canon/ghost-parity-krein-synthesis.md
  - docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md
  - explorations/channel-swing-CH-QM-2026-07-19.md
  - explorations/channel-swing-CH-SRC-2026-07-19.md
  - tests/channel-swings/ch_sm_chain_sweep.py
  - tests/channel-swings/p77_real_index_builder.py
  - tests/channel-swings/p77_real_index_twin.py
  - "DERIVATION-PROGRESS.md (generation-count-rank3-resolution entry)"
tests: tests/channel-swings/bb_p4_generation_doors_probe.py
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Blockbuster P4: the two generation doors

Mission: attack BOTH doors through which the generation integer could stop
being free. Door A (the rank fold, (9,5)): can the integer DERIVE from
finite data already in the construction? Door B (the eta pinning, (7,7)):
what geometric datum would pin the boundary flow value, turning the
FREE-INTEGER "reachable" into "predicted"? Then the devil's advocate closes
with the strongest case that both doors fail.

Signature purity (K6): Section 2 (Door A) contains only (9,5)-side facts
and signature-NEUTRAL internal Spin(10)/base-SU(2) group theory, each
labeled; Section 3 (Door B) contains only (7,7)-side facts plus one
class-generic Altland-Zirnbauer control, labeled. The companion probe
(`tests/channel-swings/bb_p4_generation_doors_probe.py`, 27 checks, ALL
PASS) is split into Part A / Part B with no shared computations. The two
doors are compared only in the computation-free closing sections (4-5).

## 1. The five-lens council (inline, run before execution)

**Index theorist.** Approach: treat both doors as index problems. Door A
asks whether the count is the dimension/multiplicity of a FORCED subobject
or the index of a FORCED operator; enumerate the possible pinning shapes
and check each against the standing index results (all natural connections
give 0; the families index is 2-torsion; the flow quanta are known in both
classes). Door B asks for the boundary-condition datum -- a spectral
section -- that makes eta well-defined at the noncompact Y14 end; state
its required properties as theorems-to-be, not vibes. Trap: index
formalism makes free data look derived -- an index "computed" relative to
a chosen section or connection has the choice hidden in the boundary
condition; every integer must be tracked to WHERE it enters.

**GUT model-builder.** Approach: run the question through standard SO(10)
machinery. Multiplicity is a flavor-space statement; check whether ANY
datum on the table (54-lock, matter parity, chain choice) carries a flavor
index. The 126's Majorana matrix is the standard home of generation
structure in SO(10) models -- test whether the VEV structure constrains
its rank at all. Trap: imported habit. In ordinary GUTs N_gen is an input
BY DESIGN, so the model-builder's instinct ("of course it's free") could
blind him to a GU-specific forcing structure; the Lambda^2_+ triplet is
exactly the kind of object standard GUT practice has no analog for and
would walk past.

**KO-topologist.** Approach: the real class is where the arithmetic
lives. Track KO-grade data -- Z/2 kernel parities, section charges, mod-2
indices invisible to Chern characters -- and formulate Door B as a Real
(J'-equivariant) spectral-section problem in the Melrose-Piazza sense,
with the uniqueness and charge conditions stated exactly. Trap: KO
language can dignify a free choice. "The section's charge" is still a
choice unless connectivity of the admissible-section space is PROVEN; a
typed demand must not be allowed to pass as progress on the value.

**Arithmetic geometer.** Approach: forensic number theory on every
integer in reach. Factor the native dimension ledger; trace the
provenance of every 3 on the board (Hirzebruch's p1 = 3 sigma, the color
3, the Lambda^2_+ 3, the SU(2)+ triplet multiplicity); separate universal
representation-theory 3s from GU-derived 3s; demand a provenance CHAIN
for any claimed derivation. Trap: numerology. Matching 3s is cheap (three
payload items, three colors, three spatial dimensions); only a
machine-checked "this 3 IS that dimension" chain counts.

**Devil's advocate** (brief: argue the integer is irreducibly external).
Approach: assemble the complete kill list; show that every "derivation"
so far is a RELOCATION of the integer, not a reduction; pattern-match to
the Krein-sign terminus (five methods, unanimous: one external Z/2
posit); state what the external reading would mean, in landscape/anthropic
form, without adopting it. Trap (for the advocate himself):
survivor-blindness -- "everything failed so far" is induction, not proof,
and exhaustion has not been pre-declared. The advocate must engage the
two live residues (the Turok-Bateman x2-collapse question, which needs S;
the end-geometry rigidity question, which needs p2c) rather than declare
them dead by fiat.

**Chair synthesis (the plan).** (1) Door A is run candidate-by-candidate
in the brief's order -- (i) the 126/condensate rank, (ii) the
integer-as-dimension question with the ghost-parity synthesis as the
strongest entry (the council flags NOW: the triplet 3 is NOT an input --
this must be handled honestly in both directions), (iii) the shape
enumeration for a-priori C2/boundary pinning -- each typed
DERIVES / EXCLUDED / NEEDS-OBJECT, with the computable legs (weight
arithmetic, parity scalars, constraint-set emptiness, triplet ledger,
Hodge-star split) machine-checked in probe Part A. (2) Door B is executed
as a demand-sharpening: state the FOUR properties (existence/type,
rigidity, odd certificate, value) a p2c spectral section must have to pin
eta, plus the T4 base-typing clause; corroborate the class arithmetic
(crossing quanta, section-charge additivity, the 8Z wall) at minimal
scale in probe Part B. (3) The devil's advocate closes on both doors,
required to name the two live residues explicitly. Purity is enforced
structurally: one section per door, one probe part per door, comparison
only in computation-free prose. Guard adopted from the index theorist:
every integer that appears below carries an entry-point annotation
(FORCED / INPUT / CHOICE / OPEN).

---

## 2. DOOR A -- the rank fold [(9,5) side + signature-neutral internal group theory]

Purity note: everything computed in this section is either signature-blind
internal Spin(10)/D5 weight arithmetic, base-4-manifold arithmetic
(Hodge star on Lambda^2(R^4)), or exact integer bookkeeping; every
(9,5)-specific fact (Kramers walls, flow quanta, isospectrality theorems)
is CITED from its receipt, never recomputed. No (7,7) fact appears.

### 2.1 Candidate (i): the 126/spinorial condensate route

Question: does the rank of the record-condensate carrier get pinned by the
VEV structure (the 54-lock) plus matter parity?

**Computed (probe A1-A3, exact rational arithmetic):**

- **A1 -- the entire VEV menu is generation-blind.** Every weight of the
  45, the 54 (inside Sym^2(10) = 1 + 54), and the 210 is an integer vector
  of EVEN coordinate sum (root lattice); the rank-drop direction
  u = (1,1,1,-1,1) (odd sum) is absent from all of them, and u/2 sits in
  the 16 with u in Sym^2(16) -- the 126's nu_c.nu_c direction. This
  extends CH-SM's lemma S5 from the rank-drop step to the full VEV menu:
  no tensor VEV anywhere in the chain table carries spinor-coset data,
  and NONE of them carries a flavor (generation-space) index at all.
- **A2 -- matter parity is a SCALAR on the matter module.** 3(B-L) is an
  odd integer on every one of the 16 states (computed: 2(w1+w2+w3) is odd
  for all 16 weights), so (-1)^(3(B-L)) = -1 UNIFORMLY on the 16 and +1 on
  every VEV weight. A uniform scalar grades nothing: matter parity cannot
  project, split, or count generation space. Its physical Z/2 content
  (stability of the lightest matter state) is real but flavor-blind.
- **A3 -- the constraint set on the Majorana rank is EMPTY.** The
  126-condensate in generation space is a symmetric matrix M^{ij} (the
  nu_c Majorana matrix). Every G_SM charge of the nu_c.nu_c direction is
  exactly 0 (su3 pairing, T3L, Y -- the S5a fact, re-verified), and the
  matter-parity factor is (+1); so the invariance equations on M reduce to
  the ZERO system, and symmetric matrices of rank 1, 2, 3 all pass with
  residual exactly 0 (verified by exact elimination). The 54-lock forces
  the FIRST step of the chain (Spin(10) -> PS, D-even) and says nothing
  about M: the lock and the rank live in different tensor factors.

**Cited (9,5) walls (not recomputed):** even if one tried to source M
from GU-native structure, every GU-native Hermitian carrier is
J_quat-commuting with even signature (canon C-07, per-generator exact),
and the V1 condensate scan sharpened this to: every GU-native
ghost-parity-even condensate channel is exactly ISOSPECTRAL across the
parity -- mirrors and generations gap together
(`canon/ghost-parity-krein-synthesis.md`, 2026-07-06 update). A rank-3
condensate carrier is itself the forbidden import.

**VERDICT (i): EXCLUDED as a derivation from the VEV structure**
(kinematically: zero equations on the rank -- probe A1-A3), **with the
residue typed NEEDS-OBJECT**: a dynamically determined Majorana rank is
exactly spec item B.5/B.6 business (the source action), and in (9,5) the
object would have to supply non-quaternionic structure on top of the rank
(canon C-07 + V1), i.e. this candidate cannot even in principle close
with an object of "integer" type alone.

### 2.2 Candidate (ii): is the integer the DIMENSION of anything already present?

This is where the brief's direct question lands -- "the triplet's 3
generation/mirror pairs in CH-QM's toy: was that 3 an input or could it
be forced?" -- and the answer is the sharpest single finding of Door A:

**The 3 was NOT an input. It is forced -- and it is already in canon.**
The CH-QM toy's three generation/mirror pairs miniaturize the self-dual
triplet of `canon/ghost-parity-krein-synthesis.md` (H1): ker(Gamma) = 1664
decomposes under the self-dual SU(2)+ as 640 singlets + 416 doublets +
64 TRIPLETS (probe A4c re-verifies the ledger exactly:
640*1 + 416*2 + 64*3 = 1664; 64*3 = 192; 96 = 3 x 2 x 16 per chirality).
The multiplicity 3 is the dimension of the SU(2)+ triplet =
dim Lambda^2_+ of the 4-base, and probe A4a/A4b machine-checks the
provenance chain at its root: star^2 = +1 on Lambda^2(R^4) exactly, with
eigenspace split exactly 3 + 3. Entry-point annotation: **FORCED,
conditional on d = 4** -- and d = 4 is a declared structural INPUT, not a
derived invariant (canon C-04 caveat: the signature 9 - 5 = 4 is an
input). So the provenance chain of the integer is:

    d = 4 (INPUT) -> dim Lambda^2_+ = 3 (FORCED) -> SU(2)+ triplet
    multiplicity 3 in ker(Gamma) (FORCED, H1) -> 3 generation/mirror
    pairs per (SU(2)- x 16)-unit (FORCED, exact signature (+96,-96,0))

This is why the H1 result was recorded as killing the earlier "a count of
3 must be imported" thesis: the INTEGER has native provenance.

**What does NOT derive (cited, each with its receipt):**

1. **The chiralization.** The triplet is exactly vectorlike -- net chiral
   asymmetry 0 in every signature (H1d); every GU-native parity carrier
   is even (C-07); the self-dual connection carrying this very geometric
   3 gives generation index ZERO (C-05); and the 2026-07-06 V1/R3
   theorems fence the strongest reading: {K, chi} = 0 forces
   Re tr(chi Pi_+) = 0 for every admissible C, and native condensate
   channels are isospectral across the parity. The 3+3 split into
   physical + ghost survives as a CONSISTENCY statement only; the
   mirror-selective datum is an import. Entry-point: **OPEN /
   NEEDS-OBJECT** (a ghost-parity-preserving dynamics with
   [P_ghost, S] = 0 -- the unbuilt S_IG -- plus a mirror-selective
   structure the native algebra provably lacks).
2. **The x2 debt.** Probe A5: the physical half of the triplet counts
   96/16 = SIX 16-units, not three -- the SU(2)- doublet doubling. The
   synthesis says this explicitly ("extracting exactly three ... and not
   6 ... is not yet shown"). Even a successful mirror selection delivers
   3 x (SU(2)- doublet); collapsing the extra 2 is unshown and belongs to
   the same missing dynamics. Entry-point: **OPEN.**

**Numerology candidates, named and killed** (arithmetic-geometer lens):
the payload-item count (orientation + scale + chain = 3 items) is
bookkeeping of the reconstruction with no map to flavor space -- category
error, EXCLUDED; Hirzebruch's 3 in -5376 = -(2^8 * 3 * 7) is
p1 = 3 sigma, not generation content (W2 forensics, cited); the color 3
is gauge, not flavor. The pre-H1 native dimension ledger
{128, 14, 1792, 1664, 91} is 3-free (prime spectrum {2, 7, 13}, C-04);
every 3-carrying native dimension (192, 96) descends from the ONE
Lambda^2_+ triplet -- there is exactly one native 3, and candidate (ii)
found it.

**VERDICT (ii): PARTIAL-DERIVES / NEEDS-OBJECT.** The integer 3 IS the
dimension of something already present (dim Lambda^2_+ of the 4-base,
conditional on the d = 4 input), machine-checked end to end -- but its
PHYSICALITY (chiralization + the x2 collapse) does not derive and is
theorem-fenced as non-native in (9,5). Door A's free import therefore
RELOCATES: not "an integer" but "a mirror-selective datum plus a factor-2
collapse," both living in the unbuilt dynamics.

### 2.3 Candidate (iii): the C2/boundary data -- what SHAPE of a-priori pinning is possible without import?

Section E of the standing doc demands the source action "pin the free rank
a-priori." Enumerating every shape such a pinning could take in (9,5),
against receipts:

| shape | status in (9,5) | receipt |
|---|---|---|
| S-A: dimension/multiplicity of a forced subobject | exactly ONE native 3 exists (the Lambda^2_+ triplet) and it needs the selection dynamics -> collapses onto candidate (ii)'s residue | H1, C-04, probe A4 |
| S-B: index of a forced operator | EXCLUDED for the value 3: every natural metric so(9,5) connection gives index 0 (C-05); the revived index is connection-dependent (C-02/C-03); and the (9,5) boundary flow moves in quanta of 4 (step6, cited), so under the half-index reading boundary-revived counts move in EVEN steps -- 3 is unreachable from the (9,5) boundary; under the literal reading Kramers forces even outright (C-07) | C-02/03/05, C-07, step6 |
| S-C: torsion invariant | EXCLUDED: the families index over the metric fiber retract is 2-torsion -- Z/2-valued data cannot output 3 | C-05 |
| S-D: characteristic number | EXCLUDED: the honest number is ch2(S_X)[K3] = -5376, and its extractable 3 is Hirzebruch's coefficient | capstone doc, W2 forensics |

**VERDICT (iii): EXCLUDED for every quantitative shape; the one surviving
shape is qualitative.** In (9,5) there is no import-free route by which
boundary/C2 data pin an integer to 3. What survives is the
multiplicity-plus-selection shape: the source action does not need to
supply an INTEGER at all -- it needs to supply a SELECTION PRINCIPLE
([P_ghost, S] = 0, mirror-selective, x2-collapsing). This sharpens spec
B.5/B.6: the (9,5) door's missing object is qualitative, not
quantitative. (Whether Turok-Bateman projector consistency itself forces
the x2 collapse is the one uncomputed question inside this shape; it
needs S and is typed NEEDS-OBJECT, not EXCLUDED.)

### 2.4 Door A verdict table

| candidate | verdict | ground |
|---|---|---|
| (i) rank from 54-lock + matter parity | **EXCLUDED** (zero equations on the rank; probe A1-A3) ; residue **NEEDS-OBJECT** (dynamical rank, and in (9,5) necessarily non-quaternionic on top) | A1-A3; canon C-07; V1 |
| (ii) integer as dimension-of-present | **PARTIAL-DERIVES** (3 = dim Lambda^2_+, forced conditional on d = 4 input; the toy's 3 was a faithful miniature, not a fixture choice) ; **NEEDS-OBJECT** for physicality (chiralization theorem-fenced; x2 debt open) | A4, A5; H1, H1d, V1/R3, C-05 |
| (iii) a-priori pinning shapes from C2/boundary | **EXCLUDED** (all quantitative shapes: index routes even/zero, torsion is Z/2, char numbers wrong) ; collapses onto (ii)'s qualitative residue | shape table; C-02/03/05/07, step6 cited |

Net Door A: the generation integer does not stand as free as the
FREE-INTEGER label suggests -- its VALUE has a native provenance chain --
but what the (9,5) construction cannot supply is the SELECTION that makes
the value physical. The import is real and irreducible on current
receipts; it is just not integer-typed.

---

## 3. DOOR B -- the eta pinning [(7,7) side; separate section]

Purity note: everything in this section is (7,7)-side or class-generic.
The standing (7,7) facts are cited from W1/W2 (never recomputed); the
probe's Part B computes only minimal-scale class arithmetic; the Kramers
family in B1c is a class-generic Altland-Zirnbauer control (abstract
J^2 = -1), labeled as such, not a (9,5) GU recomputation. No (9,5)
quantity appears in this section.

### 3.1 Standing (7,7) facts this door builds on (cited)

- Bare eta(D_Sigma) = 0 by the anticommuting chiral grading; the
  mechanism is signature-blind and has a J-free route (W1 C1; W2 Section
  4). What died with the class change is the perturbation-stability
  argument, not the vanishing.
- Symmetry class drops to BDI: T'^2 = +1, C'^2 = +1, no Kramers pairing
  anywhere in the class (W1 C2).
- The revived boundary flow has quantum 2 (values 0, +-2, -4, -6
  observed), odd counts are boundary-reachable, and the value is
  CONNECTION-DEPENDENT across admissible breakers; the natural
  M_D-diagonal breaker stays at 0 (W1 C3). The boundary is the SOURCE of
  odd counts but does not pin a value -- the missing selector is the Y14
  boundary holonomy / spectral section, spec B.5(ii).
- The Z/2 kernel-parity invariant is LIVE in the real class (odd kernels
  exhibited, W1 Part B) -- KO-grade data that Chern-character inputs
  cannot see (W2 trap T5: ch2 underdetermines the real-class index; the
  mod-2 components are invisible to it).
- The relocated wall T4: in the (7,7)-inducing mostly-minus convention
  the BASE algebra is Cl(1,3) = M(2,H); base-Clifford-linear carriers
  have signature in 8Z, so literal count 3 is unreachable under
  spacetime-scalar typing (W2 T4).
- The hyperbolic (generation, mirror) pairing itself survives in (7,7):
  the triplet-sector Krein signature is exactly (+96, -96, 0) in (7,7)
  too (`tests/generation-sector/ghost_parity_krein.py`, cited via the
  synthesis) -- so the structure Door B would chiralize is present in
  the real class.

**Probe corroborations at minimal scale (Part B, all PASS):** a BDI
(real-symmetric) family shows every eta jump = 2 with a SIMPLE crossing
of kernel dimension 1 (odd), reaching both eta classes mod 4 -- odd and
even counts (B1a/B1b); the class-generic Kramers control (J^2 = -1) shows
doubly degenerate spectrum (pair gap < 1e-9), jumps in 4Z, even kernels,
count parity FROZEN (B1c/B1d). Section-charge additivity: two sections
differing by a rank-r reassignment shift eta by exactly 2r -- pinning the
count IS pinning the section charge (B2). On the cited flow set, count
odd <=> eta = 2 mod 4, and |count| = 3 exactly at |eta| = 6 (B3). The T4
wall shape at minimal scale: commutant of an explicit real 8-dim Cl(1,3)
rep has real dimension 4 (quaternionic), base-linear symmetric carriers
on R^8 (x) R^3 have every eigenvalue multiplicity divisible by 8 and
signature in 8Z (B4).

### 3.2 The precise property that would pin eta: the typed p2c demand

The object and its four clauses, stated as the demand this door proposes
(proposal only; no ledger write -- routing is the parent's move).
Proposed id: **D-77-ETA**.

**The object.** A spectral section P_end for the (7,7) boundary Dirac
family over the Y14 end -- a continuous choice, in the Melrose-Piazza
sense, of self-adjoint boundary projection interpolating the positive
spectral projection outside a compact spectral window -- equivalently,
the global boundary holonomy datum of spec B.5(ii), delivered as a
boundary condition rather than as a connection choice.

**Clause E (existence / type).** P_end must be:
(a) **J'-real** -- commuting with T' = J'_RS (T'^2 = +1): a Real/KO
spectral section compatible with class BDI, so that its relative indices
are KO-valued, carrying both a Z and the live Z/2;
(b) **ghost-sector compatible** -- commuting with the ghost grading K, so
that PER-SECTOR counts are defined: the physical count is the per-sector
count (one ghost-parity sector survives the projection), and per-sector
parity freedom is the Gram-robust fact (W1 Section 6 D4/D5);
(c) **declaredly non-base-scalar** -- NOT base-Clifford-linear. This is
the T4 clause: a spacetime-scalar section has its relative signature
forced into 8Z (probe B4 at minimal scale; W2 T4 at full scale), making
count 3 unreachable by typing alone. The base-typing is a weld
convention datum and must be stated, exactly as W2 demanded.

**Clause R (rigidity -- THE pinning property).** The space of admissible
sections (J'-real, K-compatible, correctly base-typed, finite relative
charge) must be **CONNECTED**: any two admissible sections homotopic
through admissible sections. Equivalent formulations: (i) the end
geometry trivializes the KO^0-indexed deck of section choices; (ii) the
boundary holonomy datum has NO residual integer moduli. Under R -- and
only under R -- eta(D_Sigma, P_end) is an invariant of the end geometry
rather than of a breaker choice: the flow value stops being
connection-dependent. **This single property is the conversion of W1's
"reachable" into "predicted."** Without R, any claimed value is a section
choice wearing geometric clothes (the KO-topologist's trap, named in
council).

**Clause O (odd certificate).** The mod-2 index of the interpolation from
the canonical graded (eta = 0) operator to (D, P_end) must be
NONTRIVIAL: an odd number of simple crossings -- equivalently, the
section's kernel-parity class (the live Z/2 of W1 Part B) = 1. By probe
B1/B3 arithmetic this forces the count ODD (eta = 2 mod 4). This clause
is QUALITATIVE and separable: it is checkable on a built p2c object
before any value computation, it is exactly the datum ch2-type inputs
cannot see (T5), and it is exactly what the class-generic Kramers control
shows is IMPOSSIBLE in a quaternionic-protected class -- so clause O is
also a signature-class discriminator: satisfying it certifies the
construction genuinely lives in the real class.

**Clause V (value).** The relative KO-charge of the section against the
graded reference, [P_end] - [P_G] = +-3, giving eta = +-6 and count 3
under the half-index reading (probe B2/B3: eta shifts by exactly twice
the section charge; |count| = 3 <=> |eta| = 6). **Debt flag, stated
plainly:** the D6 reading fork (literal vs half-index) is still unpinned
in (7,7) (W1 Section 8) -- Kramers no longer justifies the /2, and
nothing yet pins the literal reading either. A pinned eta = 6 therefore
delivers "3 under half-index, 6 under literal": Door B, like Door A,
carries a dangling factor-2 debt, and discharging D6 is an argument-level
obligation attached to this demand.

**Conversion statement.** With E + R, the generation integer stops being
a free import and becomes a topological invariant of ONE named geometric
object (the end's spectral section / boundary holonomy). With O, the
construction would make a falsifiable QUALITATIVE claim (odd count,
real-class certificate) in advance of any value. V is then a computation
ON the built object, not a posit. What would supply R: a concrete end
geometry whose boundary family collapses the section moduli -- e.g. the
S^3-fiber end data rebuilt in the real class (W1 Section 8: the eta of a
geometric (7,7) boundary operator on a concrete end is UNCOMPUTED; that
computation is the next rung of this door).

### 3.3 What Door B does NOT claim

No claim that clause R holds -- no candidate end geometry has been
computed, and R is precisely the kind of statement that could fail (the
devil's advocate's best target, below). No claim that O or V hold. No
change to the FREE-INTEGER verdicts of W1/W2: on current receipts the
flow value remains connection-dependent. Door B's output is a
demand-sharpening: the import is downgraded from "choose the integer" to
"build the object; oddness (O) and typing (E(c)) become checkable
properties; the value (V) becomes a computation gated on rigidity (R)."

---

## 4. The devil's advocate closes: the case that both doors fail

(Computation-free; both doors cited by verdict only.)

**The kill list is long and unanimous.** Every attempted derivation of
the integer has died by computation: ch2 = -5376 (not 24); the discrete
series is empty (rank criterion, theorem-grade); the tau-twist
rank-correction falsified on four criteria; every natural connection
gives index 0; the families index is 2-torsion; the 34-agent tournament
found no trap-free GU-native odd break; the V1 scan found every native
condensate channel isospectral across the parity; and both independent
(7,7) workers, by different functors, landed FREE-INTEGER with steering
menus that produce 3 AND not-3 at will. The probability-weighted lesson
of that record is not subtle.

**Every "derivation" so far is a relocation.** Door A relocated the
integer from "a chosen rank" to "a forced multiplicity plus a
mirror-selective datum plus a x2 collapse" -- the multiplicity is real,
but the selection datum is exactly as external as the rank was, and it
must choose 3-of-6 per 16-unit, which is integer-flavored data with a
new name. Door B relocated it from "a chosen connection" to "a section
charge plus a rigidity assumption" -- and clause R begs the question in
the precise sense that the components of the admissible-section space
ARE the integers: asserting connectivity is asserting that the choice
has been made by geometry not yet exhibited. The external bit-count has
never decreased below {one selection/charge integer + one factor-2
reading}. Compare the program's own terminus for the Krein sign: five
methods, unanimous, ONE external Z/2 posit that no internal structure
supplies. The generation integer is converging on the same shape: one
external integer posit that no internal structure supplies.

**The residue has the SHAPE of boundary-condition data.** GU's surviving
free data are: one Z/2 orientation, one absolute scale, one
R0-equivalent chain choice, one generation integer. That is exactly the
signature of initial/boundary-condition data, not law data. On this
reading GU is a FRAMEWORK: it forces the shape of the Standard Model
(chain funnel, unique hypercharge line, sin^2 theta_W = 3/8 at the
boundary -- all machine-checked) and PERMITS its multiplicity, the way
general relativity forces the form of the field equations and permits
the matter content.

**The landscape/anthropic reading, stated without adoption.** If the p2c
end charge of Door B exists but is a topological SECTOR label -- a
winding the end geometry supports at many values -- then generation
number is a vacuum coordinate: sectors with 1, 2, 3, ... generations all
exist, and 3 is selected environmentally (minimal CKM CP violation
requires N >= 3, so a selection effect is even mildly natural) or
historically, not predicted. Under this reading the two doors' outputs
are still valuable but change meaning: Door A's Lambda^2_+ provenance
becomes "why 3 is AVAILABLE," and Door B's demand becomes "where the
sector label lives." What it would mean for the program: the North Star
claim "GU predicts 3" demotes to "GU predicts the SM's shape and
permits 3"; the honest publishable statement becomes a
conditional-framework claim. That is a coherent, receipt-consistent
terminus -- and it is the null hypothesis both doors must beat.

**The advocate's honest limit (chair-enforced).** Two residues are alive
and uncomputed, and "everything failed" does not cover them: (1) whether
Turok-Bateman projector CONSISTENCY itself forces the x2 collapse and
the mirror selection once an S exists (needs the source action -- Door
A's qualitative residue); (2) whether a concrete real-class end geometry
collapses the section moduli (needs the p2c end computation -- Door B's
clause R). Neither has been run to a verdict; exhaustion has not been
pre-declared. The external reading is therefore the leading hypothesis,
not a verdict -- and by the program's own posture, only falsification of
these residues would license adopting it.

## 5. Honest odds per door

Stated as this worker's calibrated betting odds, not measurements:

| proposition | odds | ground |
|---|---|---|
| Door A: the integer's VALUE has native provenance (3 = dim Lambda^2_+ chain) | already established conditional on d = 4 input | H1 + probe A4; this is not at stake |
| Door A closes as a DERIVATION from existing finite data alone (no new object) | ~3% | all three candidates EXCLUDED at that strength; V1/R3 are theorems |
| Door A closes conditional on a built S_IG with TB structure (selection + x2 both forced by consistency) | ~10-15% conditional; ~5% joint with S_IG existing | the residue is genuinely unexplored, but the native algebra's walls are exact |
| Door B: clause R (rigidity) holds for the actual Y14 end, conditional on the p2c object being built | ~10-15% | section moduli generically DO persist; but concrete ends sometimes collapse them, and nothing computed excludes it |
| Door B: full success (R + O + V = 3, D6 discharged), conditional on p2c built | ~3-4% | two independent conditions on top of R, one of them the exact value |
| Door B unconditional (object built AND pins an odd value) | ~2% | compounded with p2c construction risk |
| Both doors fail; the integer is honest external data (the Section 4 reading) | ~75-80% | the kill record + the relocation pattern; held as leading hypothesis, not verdict, per the two live residues |

The two doors are not symmetric: Door A has already yielded a real
partial result (the integer's provenance) and its remaining question is
qualitative; Door B is a well-posed demand whose pinning property is
stated but unevidenced. The highest-value next computations, in order:
(1) the geometric (7,7) boundary operator on a concrete end (tests
clause R directly -- W1 Section 8's named gap); (2) the TB-consistency
x2-collapse question, the moment any candidate S exists.

## 6. Receipts

- Probe: `tests/channel-swings/bb_p4_generation_doors_probe.py` -- run
  2026-07-19, exit 0, **27 checks ALL PASS** (two fixes during
  development, both to the probe's own scaffolding before final run: the
  su(3) root list had wrongly included +-(ei+ej) [so(6) roots], and the
  eta scans initially sampled points inside the crossing tolerance;
  no check was weakened after seeing results). Part A exact (Fractions):
  A1a-A1d VEV-menu generation-blindness; A2a/A2b matter-parity scalars;
  A3a-A3c empty constraint system on the Majorana rank (ranks 1/2/3 all
  residual 0); A4a/A4b star^2 = +1 and the 3+3 split; A4c triplet ledger
  640+832+192 = 1664; A5 the 96/16 = 6 debt. Part B numeric (seeded,
  tolerances stated in-script): B1a/B1b BDI quantum 2, kernel dim 1,
  both mod-4 classes; B1c/B1d class-generic Kramers control (pair gap
  1.6e-14, jumps 4Z, counts even); B2a/B2b section-charge additivity;
  B3a/B3b odd-count arithmetic on the cited flow set; B4a-B4e Cl(1,3)
  rep exact, commutant dims 4 and 36, multiplicities all 8, signature
  in 8Z.
- Door A citations: `canon/no-go-quaternionic-parity-generation-sector.md`
  (C-01..C-07); `canon/ghost-parity-krein-synthesis.md` (H1, H1d, V1,
  R3, V2, the 2026-07-06 update); `explorations/channel-swing-CH-SM-2026-07-19.md`
  + `tests/channel-swings/ch_sm_chain_sweep.py` (S5, 54-lock, matter
  parity, per-16 orthogonality of multiplicity);
  `explorations/channel-swing-CH-QM-2026-07-19.md` (toy design; J_quat
  verdict); DERIVATION-PROGRESS generation-count-rank3-resolution (the
  24 = 16+8 packaging and Candidate A/B); step6 (9,5 flow quantum,
  cited); `docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md`
  Section E.
- Door B citations: `explorations/p77-real-index-W1-builder-2026-07-19.md`
  (C1-C3 flow table, Part B kernel parity, Section 6 per-sector facts,
  Section 8 gaps); `explorations/p77-real-index-W2-twin-2026-07-19.md`
  (Section 4 eta skepticism; traps T4/T5);
  `tests/generation-sector/ghost_parity_krein.py` ((7,7) triplet
  signature, cited via the synthesis);
  `explorations/channel-swing-CH-SRC-2026-07-19.md` (ghost-sector
  context).
- Killed routes honored, none re-run: ch2 = 24, discrete series,
  tau-twist, shiab selectors, the eleven dead selector routes.

## Boundary

Conditional work under the standing axiom
(`lab/process/boundary-adapter-standing-axiom.md`); exploration grade.
The council was run inline in this worker (five lenses, no fan-out). K6
purity held: Door A and Door B occupy disjoint sections consuming
disjoint probe parts; (9,5) walls and (7,7) flow facts were cited, never
recomputed, and the one Kramers computation in Part B is a labeled
class-generic AZ control. The D-77-ETA demand is a PROPOSAL routed via
this doc -- no ledger, scorecard, interface-contract, claim-status,
canon-verdict, map, or public-posture mutation; no cross-owner or
external action. The odds in Section 5 are this worker's calibration,
not program verdicts; the comparative (9,5)-vs-(7,7) adjudication stays
Joe-gated at the integration barrier.
