---
title: PW2F-R2B2B2B — active local source-grammar tangent, full residual carrier, and leading-symbol triage
date: 2026-08-03
status: ACTIVE_LOCAL_METRIC_TANGENT_AND_FULL_RESIDUAL_PORT_PASS_LC_J3_ZERO_I1_C5_AND_NORMAL_I2B_C6_J3J2_C5_ZERO_OFFSHELL_I2B_C5_OPEN
run: historical-investigation
---

# Result

R2B2B2B corrects the predecessor's next-gate order ledger and closes three
construction prerequisites before any new quartic bank.

First, a local active-reconstruction principal-`Z1` metric--metric tangent
shaped by the source grammar is now built rather than left as an independent
`q` fixture. A chosen
constant noncentral exact Spin(9,5) point conditionally occupies the epsilon
slot while epsilon and varpi are held fixed:

\[
\delta B=\operatorname{Ad}_{\epsilon^{-1}}\delta\Gamma,
\qquad
\delta q=(\operatorname{Ad}_{\epsilon^{-1}}-1)\delta\Gamma,
\]

\[
\delta T=-\delta q,
\qquad
\delta A=\delta B+\delta T=\delta\Gamma.
\]

All ten reconstructed induced-`Y14` metric owners satisfy these identities.
The conditional noncentral active point makes all ten source-grammar `q/T`
motions live. Identity epsilon
annihilates `q/T` motion while retaining `delta A=delta Gamma`. The old
shortcut `delta T=-delta B` fails by exactly the live `delta Gamma` return.

Here `B=Gamma+q` and `A=Gamma+varpi` are repository-derived coordinate
reparameterizations of the source distortion grammar. This is not a completed
source identity: source epsilon versus the active rotor is
`UNCERTAIN/HOMONYM-RISK`; source `nabla^g` versus the reconstructed induced-Y14
`Gamma` is `UNCERTAIN`; and source `Upsilon/I2B` versus the active residual and
primalizer is only a `SAME-GRAMMAR-CANDIDATE`. The construction is not a global
source-to-active bundle morphism, a source-selected epsilon, an identification
with repository `h=exp(u)`, or a construction of the background
Maurer--Cartan value. In the chosen local trivialization fixed epsilon makes
`d epsilon` metric-independent, but its background and general
epsilon/varpi variations remain omitted and can affect C4.

Second, the sparse active Shiab residual is explicitly ported into the full
residual carrier already constructed by B2C15. The actual background fixture
has thirteen terms. Each is a 13-form carrying one grade-two Clifford blade,
and each maps to the dual coordinate

\[
(\widehat\mu,m),
\qquad
e_{\widehat\mu,m}
=\operatorname{sgn}(\widehat\mu,\mu_1,\ldots,\mu_{13})
\,\kappa(m,m)\,E_{\mu_1\cdots\mu_{13},m}.
\]

The coordinate lies in the exact 115,584-dimensional active carrier. Its
primalizer has inertia `(57664,57920)`. Direct top-form contraction after
primalization equals the coordinate calculation exactly and gives

\[
\langle E,R_{\rm res}E\rangle=\frac{981}{64}.
\]

A one-coordinate control has norm `+1`. The earlier 28-dimensional
two-generator slice cannot represent the thirteen distinct internal masks;
the full B2C15 carrier is essential. An independent B2C14 `basis_pair` check
reproduces every orientation/internal sign, and lowering after primalization
returns all thirteen raw density coordinates exactly.

# Leading-symbol result

The prerequisite homogeneous monomial-bank burdens in four conormal variables
are, per symbol entry,

| homogeneous degree | monomials |
| ---: | ---: |
| C4 | 35 |
| C5 | 56 |
| C6 | 84 |

The 35-point quartic bank therefore could not have certified C5 or C6. The
correct move was to compute the leading maps before interpolation.

Let `delta Gamma_i^(2)` denote the order-two Levi--Civita principal jet for
owner `i`. The possible order-three residual map is

\[
J_{3,i}(\xi)
=\operatorname{Shiab}\bigl(\xi\wedge\delta\Gamma_i^{(2)}(\xi)\bigr).
\]

The executable computes this over
`Q[eta0,eta1,eta2,eta3]`, not just on a finite panel, and finds for every
observed-base conormal

\[
\boxed{J_{3,i}(\eta)\equiv0\quad\text{for all ten owners}.}
\]

In fact the stronger incidence identity
`eta wedge delta Gamma_i^(2)(eta)=0` holds for all ten owners before Shiab is
applied. The cancellation is therefore carried by this Levi--Civita
principal-symbol incidence, not manufactured by the residual projector.

This is not an all-`T*Y`-conormal result: vertical/mixed conormals,
partial-`Z1`, and section-tangent directions remain open. The independent
universal Euler comparator also verifies that the odd
first-action coefficient is `C-C^T`; the probe does not obtain that formula by
definition alone. Consequently, on this reconstructed Levi--Civita principal
branch:

- the source-directed `I1` `2+3` coefficient has `C5` rank zero;
- the residual-zero normal `I2B` symbol
  `J3^vee R_res J3` has `C6` rank zero; and
- its normal `J3/J2` C5 cross also vanishes.

No 84-monomial normal C6 or 56-monomial `J3/J2` C5 interpolation is needed
after the map itself is proved identically zero. This does **not** discharge
the full off-shell `I2B` C5: stationarity of an indefinite residual square does
not imply `E=0`, and the `E R D2E`, moving-primalizer, and moving-pairing
returns still need an order-ceiling port on this graph. The zero is
branch-specific, not a generic statement about residual-square theories. A
deterministic non-Levi--Civita connection plant survives the same Shiab and
full residual-pairing pipeline with norm `-13/2`. That planted control prevents
a permissive zero matcher from earning the result.

# Layer 0 and source collision

- `SOURCE-CONFIRMS`: the first bosonic action, the
  `T=varpi-epsilon^-1 d_0 epsilon` grammar, and `kappa1` with its placement
  (author draft Sec. 9.1/9.2, pp. 43--45 and 56--57; pinned source-pack SHA-256
  `5b50adabf067959654073f7e5c6665e8ac1e3e52ae36ae22ae9754bc9db23b5f`).
- `REPOSITORY-DERIVES`: the local `delta T=alpha-D_A zeta` formula from that
  grammar; the `B/q/T/A` reparameterization; the conditional active rotor and
  ten-owner principal-`Z1` tangent; the sparse-to-full residual injection and
  norm `981/64`;
  and the polynomial `J3=0` theorem.
- `SOURCE-SILENT`: the source-to-active Spin(9,5) embedding, active residual
  injection/primalizer, selected epsilon, global bundle map, complete induced
  coefficient tensors, `kappa1` value/active normalization, and the
  leading/lower-order symbol results.

Source epsilon, repository `h/u`, and reduction epsilon remain distinct.
`I1` and `I2B` remain distinct actions. The residual primalizer is a
repository construction, not a manuscript formula.

# What this earns and what it does not

Earned:

1. an exact conditional local fixed-(epsilon,varpi) active principal-`Z1`
   metric--metric tangent rather than an independent-q toy chart;
2. an explicit full-carrier residual-pairing interface;
3. exact observed-base-conormal elimination of `I1` C5 and the residual-zero
   normal `I2B` C6 plus `J3/J2` C5 blocks on the exercised LC principal-`Z1`
   branch; and
4. a corrected polynomial-order ledger.

Still open:

- the background and global source-to-active epsilon/real-form bundle port,
  full `(delta g,zeta,alpha)` tangent, vertical/mixed conormals, partial-`Z1`,
  and section tangents;
- the actual induced density, Krein, input/output lowerer, outer-pairing, and
  moving-Shiab coefficient tensors;
- the off-shell `I2B` C5 order ceiling, including `E R D2E` and moving-pairing
  returns;
- complete C4 assembly for `I1` and, after that ceiling, for `I2B`;
- independent reverse/formal-adjoint and native Green reconciliation;
- C3/C2, characteristic, analytic domain, BV quotient, observation, and
  physics.

P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`. The conjunctive
`TG-1 AND TG-2 AND TG-3` gate remains `NOT_PROMOTED`. No canon, scientific
status, public posture, scheduler, publication, or physics claim changes.

# Hostile reviews

- Exact representation/differential geometry: `PASS_AFTER_REPAIR`; added the
  independent B2C14 pairing, lowerer round-trip, observed-base scope, and
  pre-Shiab incidence control.
- Variational/PDE: `PASS_AFTER_REPAIR`; restricted the square-action result to
  the residual-zero normal block and left the full off-shell C5 ceiling open.
- Source/Layer 0: `PASS_AFTER_SCOPE`; source epsilon, the conditional active
  point, and repository-derived structures remain distinct.

# Next gate

Resume at
`PW2F-R2B2B2C-I1-C4-AND-I2B-OFFSHELL-C5-CEILING-THEN-C4-GREEN`.
Build the actual induced coefficient tensors and the complete 35-monomial
`I1` C4 bank. In parallel, port the full off-shell `I2B` Hessian order ledger;
only if all surviving returns stop at C4 should its complete C4 bank run.
Reproduce every admitted bulk symbol by an independent formal-adjoint/Green
route before any C3 or physics step.

# Executable evidence

- Main probe after hostile repairs: `25 exact + 3 source + 19 type + 11 planted
  = 58 PASS`.
- Conditional active source-grammar principal-`Z1` tangent: ten owners,
  noncentral live and identity-zero controls.
- Full residual port: 13 coordinates, dimension `115584`, inertia
  `(57664,57920)`, norm `981/64`.
- Leading symbols: symbolic pre-Shiab incidence zero and `J3=0`, `rank(I1 C5)=0`,
  `rank(normal I2B C6)=0`; non-LC control norm `-13/2`; off-shell `I2B` C5
  remains open.
