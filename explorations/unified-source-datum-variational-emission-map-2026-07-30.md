---
title: "Unified source-action variational emission map: six interfaces, one typed-current fork, and a dynamical-soldering target"
status: active_research
doc_type: construction_result
created: 2026-07-30
lane: "1"
work_item: SOURCE-OWNED-CHIMERIC-BV-CAMPAIGN-N3
run: lab/process/runs/GUH-20260730T145219Z-n3-variational-emission-map/run-plan.md
probe: tests/channel-swings/unified_source_variation_probe.py
grade: "FORMULA-BUILT PARTIAL. Every frozen N1 term has a first-variation owner; the total K/C projections, moving-section current derivative, P_IG parent coefficient, same-Dirac-current cancellation, metric-trace Ricci0 symbol, fixed-plane full-Sp obstruction under stated hypotheses, an even canonical-pair sign plant, and virtual P3 symbol pair are explicit. Zero-order full-20 placement and eight named carrier/adjoint/domain maps remain. No stationary solution, CME, domain, mass, index, or count is claimed."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Unified source-action variational emission map

## Result first

Swing N3 returns:

```text
N3-TERM-BY-TERM-FIRST-VARIATION-LEDGER-COMPLETE
TOTAL-K/C-PROJECTIONS-EXPLICIT; ZERO-ORDER-PLACEMENT-DEFERRED
CONDITIONAL-FIRST-ORDER-SYMBOL-SUPPORT-COMPARATOR-EXPLICIT
MOVING-SECTION-CURRENT-DERIVATIVE-CONSTRUCTED
P_IG-PARENT-ELIMINATION-COEFFICIENT-PLUS-Z_U/2
SAME-DIRAC-CURRENT-A-COUPLING-CANCELS-ON-A-LINEAR-STRATUM
KINEMATIC-METRIC-TRACE-TO-RICCI0-HESSIAN-CHANNEL
FIXED-PLANE-FULL-SP-BRANCH-OBSTRUCTED-UNDER-STATED-HYPOTHESES
SIX-TYPED-N3-INTERFACES; PARTIAL-WITH-NAMED-MISSING-ADJOINTS
```

Every frozen N1 term now has a typed or formal first-variation owner. This is
a real advance: the \(K/C\) algebraic projection identities are exact, the
still-missing \(C\)-reality completion is named, and the bulk/defect weld has
an explicit moving-section derivative. The calculation also finds a more
informative obstruction than “the source is missing.”

N3 imports W192's already-recorded distinction between a vector observable
and an \(Sp\)-connection current. Applied to the frozen N1 action, the
literal bridge current

\[
J(Z)_a=\operatorname{Re}[Z,\mathbb K_{\mathbf G}c(e_a)Z]
\]

is a Clifford-vector current. The bridge term tries to pair it with
\(\theta\in\Omega^1(Y,\operatorname{ad}P)\), an adjoint-valued connection
one-form. Those are different carrier types.

Split the connection variation of the full-20 matter term into its
connection-linear Dirac part and curvature-vertex part:

\[
\delta_A S_{20}[a]
=
\langle J_D(Z),a\rangle
+\langle Q_F(Z),D_Aa\rangle,
\qquad
J_F:=D_A^!Q_F .
\]

These are density-dual covectors. A nondegenerate adjoint/Hodge/Krein Riesz
map must still be declared to obtain the primal adjoint-valued one-form used
inside \(\langle\theta,*J\rangle\).

This opens a sharp Layer-0 fork. If the bridge current is the **same object**
as the Riesz-lowered \(J_D\), then on the \(A\)-linear stratum
\(+J_D-J_D\) cancels while \(J_F\) remains. If the bridge is the whole
\(J_D+J_F\), both direct pieces cancel. If it is a **different object**, its
independent soldering/intertwining map has not yet been constructed. This
same-current cancellation inside N1 is the new consequence; the type
distinction itself is not new.

The full N3 verdict is deliberately refused. Eight explicit maps still
separate this packet from a closed Euler system. No stationary solution,
CME, global domain, physical mass, index, or count follows.

## Plain English

We took the candidate action seriously enough to ask what equations it
actually produces.

Most of it differentiates coherently. The indefinite Krein pairing is doing
real work; it selects a precise part of the complete ordered operator rather
than merely decorating a conventional mass term. The four-dimensional
defect really does push a moving source into the fourteen-dimensional
equations. A varying metric trace can generate a traceless-Ricci-shaped
second-derivative term, so the earlier trace-versus-traceless mismatch is
not automatically fatal.

The most valuable new consequence is what happens when the W192 type repair
is inserted into N1. The written fermion current knows about a spacetime
direction but not a gauge-algebra generator. The action derivative supplies
a connection covector; the action's pairing supplies a separate
dual-to-primal map. If the bridge uses that same Dirac current, the action
subtracts the very current its Dirac term adds, while the curvature current
survives. That is now a small, decisive construction problem rather than an
open-ended complaint.

There is a second conditional decision. Assume the standard irreducible
\(Sp(32,32;\mathbb H)\) action, a fixed nonzero Clifford fourteen-plane,
and a metric-preserving induced action on that plane. Full covariance would
induce a nonzero Lie-algebra map
\(\mathfrak{sp}(32,32;\mathbb H)\to\mathfrak{so}(9,5)\). The dimensions
obstruct such a map:

\[
\dim\mathfrak{sp}(32,32;\mathbb H)=8256,
\qquad
\dim\mathfrak{so}(9,5)=91.
\]

The fixed gamma/\(P_R\) map remains Spin-compatible; that statement does not
establish covariance of the unresolved total \(P_0/\rho/Y/C\) kernel. The
viable choices are to restrict the packet to the corresponding stabilizer or
let \(\epsilon_{\rm IG}\) dynamically move the Clifford plane. Since N1
already contains \(\epsilon_{\rm IG}\), building that orbit and its
covariant derivative is a construction of the existing packet, not an added
rescue field.

### Prior ownership and N3 delta

- [W192](W192-explicit-carrier-kernel-spectral-gate-2026-07-14.md) owns the
  vector-observable versus \(Sp\)-connection-current distinction. N3 newly
  applies it to the frozen N1 bridge and derives the cancellation fork.
- [W229](W229-close-a2-source-action-znu-completion-2026-07-14.md) owns the
  inherited parent; N3 records the coefficient sign forced by its displayed
  first-order action.
- [H15](wave3/H15-gravity-fork-2026-07-11.md) and
  [H25](wave7/H25-II-first-variation-2026-07-11.md) own the curved-ambient
  Gauss structure. N3 keeps \(R^Y_{\rm tan}\) in the section residual.
- [H23](wave8/H23-source-action-construction-2026-07-11.md) and
  [W131](W131-covariant-operator-y14-2026-07-14.md) own the
  soldering/Spin-compatible construction. N3 adds the fixed-plane
  full-\(Sp\) dimension fork under stated hypotheses.
- [N2a](actual-sym2-c14-odd-orbit-knockout-2026-07-30.md) owns the boundary
  between first-order \(68\)-cell support and unresolved zero-order
  placement. N3 preserves that boundary.

## 1. Layer-0 decisions

| phrase | object used in N3 | object not identified with it |
| --- | --- | --- |
| connection current | density-dual pair \(J_D+J_F=\delta_A S_{20}\), followed by a declared Hodge/Krein Riesz map | the vector current \(\operatorname{Re}[Z,Kc(e_a)Z]\), or a covector silently equated with a primal one-form |
| total \(K\) kernel | Krein-self-adjoint projection of the complete ordered operator \(c_\rho(v)Y_K\) | separate Hermitian projections of \(c_\rho(v)\) and \(Y_K\) |
| total \(C\) kernel | \(C\)-twisted-transpose anti-fixed projection for one repeated odd field | the \(K\)-adjoint projection |
| metric trace | a section variation \(h=(\phi/4)g\) or its native DeWitt inverse | the N2 vertical connection coefficient \(v_{\rm tr}=\tau_g\otimes\Phi_{\rm tr}\) |
| fixed gamma covariance | preservation of one fixed Clifford-vector plane | covariance of a dynamical soldering orbit |
| parent coefficient | the algebraic coefficient \(+Z_U/2\) obtained by eliminating the displayed \(P_{\rm IG}\) parent | positivity of an indefinite functional or a sign assumption for charged \(Z_U\) |
| P3 operator | a virtual comparator pair on two different bundles | one literal tensor-product operator on an identified bundle |
| BV obstruction | a master-equation term after all carrier maps and the Euler ideal exist | a nonzero Hessian incidence or an antifield-linear skeleton |

The current fork and the trace fork are both Layer-0 forks. Controls can
verify either calculation but cannot decide which carriers the construction
means to identify.

## 2. Exact total odd-bilinear projections

Let \(P=P_0\), \(A_K=\mathfrak c_\rho(v)Y_K\), and
\(K=\mathbb K_{\mathbf G}\). With

\[
A_K^\times=K^{-1}A_K^\dagger K,
\]

the real \(K\)-sesquilinear quadratic form selects

\[
\operatorname{Herm}\!\left(P^\dagger K A_KP\right)
=
P^\dagger K\,\frac{A_K+A_K^\times}{2}\,P.
\]

Thus the killed stratum is the Krein-skew part of the **total ordered
operator**. This does not authorize commuting or separately projecting its
Clifford, gauge, and provenance factors.

For \(C^T=\tau C\), define

\[
A_C^\natural=\tau C^{-1}A_C^TC.
\]

The diagonal Grassmann bilinear selects

\[
\operatorname{Alt}\!\left(P^TCA_CP\right)
=
P^TC\,\frac{A_C-A_C^\natural}{2}\,P.
\]

The \(C\)-natural fixed part is killed for one identical odd field. Dagger
and transpose are different operations, so neither branch's survival result
transfers to the other.

As a deliberately separate comparator, the inherited
actual-\(\operatorname{Sym}^2T^*X\) **first-order observer-symbol** support
has these conditional \(P_0\)-restricted ceilings:

| restriction | first-order symbol cells |
| --- | ---: |
| \(P_0=1\) | 68 |
| \(P_0=P_S\) | 4 |
| \(P_0=P_I\) | 4 |
| \(P_0=P_R\) | 20 |

N2a explicitly forbids promoting these cells to the zero-order
\(\mathfrak c_\rho(v)\) Yukawa insertion. The required
\(\operatorname{End}(S_{\mathbb C})\to\operatorname{End}(E_{20})\) lift,
\(P_0\), \(\rho(\Phi)\), provenance, covariance, and \(C\)-reality
placement remain unbuilt. Zero-order full-20 Yukawa incidence is therefore
**UNRESOLVED**, not \(68/4/4/20\).

## 3. Complete term-to-equation ledger

The executable ledger covers every frozen N1 term and every field it
declares varied.

| N1 term | emitted covectors | N3 status |
| --- | --- | --- |
| ambient Yang--Mills | \(A\) | \(D_A^!F_A\), exact weak form |
| induced-YM parent | \(A,U,P_{\rm IG},\epsilon_{\rm IG}\) | \(D_AU-Z_U^{-1}P_{\rm IG}\), \(D_A^!P_{\rm IG}\), \(B_U^!P_{\rm IG}\); exact zero in the independent-parent \(\epsilon\) slot |
| distortion/source bridge | \(A,U,\epsilon_{\rm IG},Z\) | formal covectors; literal \(J\) type blocked and \(D_\epsilon\Gamma\) deferred |
| full-20 Krein quadratic | \(A,Z\) | density-dual \(J_D+J_F\) split defined; projector/gamma/\(K\)/curvature-vertex adjoints deferred |
| end selector | \(A,Z\) | branch-typed: physical fixed-profile rivals vary \(Z\); the auxiliary boundary rival owns its separate \(A,\widehat Z\) dependence |
| induced section gravity | \(s\) | \(D_sII\), \(D_sR^Y_{\rm tan}\), normal connection, and Green map deferred |
| SW defect | \(s,A,Z\) | exact residual pattern; \(P_+\) and moment-map adjoints deferred |
| \(K\)-Yukawa defect | \(s,A,Z\) | total \(K\) projection exact; moving bundle maps deferred |
| \(C\)-Yukawa defect | \(s,A,Z\) | Grassmann projection exact; reality completion deferred |
| spurion interface | \(s,A,Z\) | Grassmann projection exact; moving bundle maps deferred |
| minimal BV extension | all declared BV fields | antifield-linear owners explicit; mixed-parity left/right derivative signs and open Hom maps deferred |
| orientation holonomy \(T_9\) | none at fixed topology | no ordinary local Euler covector |

The exact algebraic parent equation is

\[
P_{\rm IG}=Z_UD_AU.
\]

For the frozen displayed parent

\[
\langle P_{\rm IG},D_AU\rangle
-\frac1{2Z_U}\langle P_{\rm IG},P_{\rm IG}\rangle,
\]

eliminating \(P_{\rm IG}\) gives

\[
+\frac{Z_U}{2}\lVert D_AU\rVert^2.
\]

This fixes the coefficient to **plus \(Z_U/2\)**. It does not prove
positivity of the native indefinite functional, and N1 does not constrain
the sign of charged \(Z_U\). The inherited “negative gradient” wording in
W229 does not match this displayed action. This correction changes no N1
term; it records the coefficient forced by the term already written.

## 4. The typed-current fork

Write the full-20 connection covector through the action itself and keep its
two derivative structures separate:

\[
\left.\frac{d}{dt}\right|_{0}S_{20}[A+ta,Z]
=
\langle J_D(Z),a\rangle
+\langle Q_F(Z),D_Aa\rangle
=
\langle J_D+D_A^!Q_F,a\rangle
+\mathfrak g_{D_A}(Q_F,a).
\]

Here \(J_D\) is the connection-linear Dirac/operator current and
\(J_F=D_A^!Q_F\) is the curvature-vertex Euler current. This automatically
supplies the missing gauge generator but initially lands in the
density-dual connection space. Let

\[
\mathcal R_{\kappa,G}:
\Omega^1(Y,\operatorname{ad}P)^\vee\otimes\operatorname{Dens}(Y)
\longrightarrow
\Omega^1(Y,\operatorname{ad}P)
\]

denote the nondegenerate Hodge/Krein/adjoint-pairing Riesz map used by the
bridge. Its convention and sign are part of the missing typed map.

On the minimal \(A\)-linear stratum, first suppose the bridge's \(J\) is
SAME-OBJECT as \(\mathcal R_{\kappa,G}J_D\). Since
\(\theta=A-\Gamma(\epsilon_{\rm IG})-U\),

\[
\delta_A S_{20}=+J_D+J_F,
\qquad
\delta_A[-\langle\theta,J_D\rangle]=-J_D.
\]

The \(J_D\) pair cancels in \(\mathcal E_A\), leaving
\(\kappa^{-1}\theta\), Yang--Mills/parent, and \(J_F\). On this frozen
connection-linear stratum \(J_D\) is \(A\)-independent. If a later typed
identification makes \(J_D\) \(A\)-dependent, it also leaves
\(-(D_AJ_D)^!\theta\). The bridge's \(J_D\) does **not** vanish from the
\(U\)- or \(\epsilon_{\rm IG}\)-equations.

If the bridge instead means the total
\(\mathcal R_{\kappa,G}(J_D+J_F)\), both direct matter currents cancel on
the corresponding linear stratum and only their \(A\)-dependence/Hessian
terms survive. Those two SAME-OBJECT branches must not be conflated.

If the bridge's \(J\) is DIFFERENT-OBJECT, no cancellation follows, but the
packet owes a new *derived map*, not a new fitted coefficient:

\[
\mathfrak j_{\epsilon}:
\text{Clifford-vector current}
\longrightarrow
\Omega^1(Y,\operatorname{ad}P)^\vee\otimes\operatorname{Dens}(Y)
\xrightarrow{\mathcal R_{\kappa,G}}
\Omega^1(Y,\operatorname{ad}P).
\]

Its gauge covariance, soldering dependence, and Noether identity must be
written before either interpretation is admitted.

## 5. Moving defect and trace-to-\(\operatorname{Ric}^0\)

For \(s_t\), velocity \(V=\dot s_0\), density \(\ell_t\), and test scalar
\(f\),

\[
\left.\frac d{dt}\right|_0
\langle (s_t)_*\ell_t,f\rangle
=
\int_X
\left[
df_{s(x)}(V)\,\ell
+f(s(x))\,\dot\ell
\right].
\]

For a bundle-valued density and a compatible connection this becomes

\[
\int_X
\left[
\langle D_t\ell,q\circ s\rangle
+\langle\ell,\nabla_Vq\circ s\rangle
\right].
\]

The first term varies the intrinsic defect expression; the second moves its
support. Neither can be dropped. For a pulled-back connection,

\[
\delta_s(s^*A)
=
s^*(\iota_VF_A)+D_{A_X}(s^*\iota_VA).
\]

The full section equation must also carry the variations of the volume,
\(P_0\), \(P_0^!\), \(c_\rho\), \(K/C\), \(\Sigma\), and
\(\operatorname{res}_s^V\), without counting the density variation twice.

For a genuine metric variation \(h=(\phi/4)g\) on a four-dimensional
Einstein background,

\[
D\operatorname{Ric}^0[h]
=
-\frac14(\operatorname{Hess}\phi)^0.
\]

The constant mode vanishes; generic nonconstant Fourier modes, including
null modes, survive. If \(\phi\) multiplies the N2 native DeWitt trace vector
\(h_{\rm tr}=-g/4\), the sign reverses.

This proves only

```text
KINEMATIC-METRIC-TRACE -> RICCI0-HESSIAN-CHANNEL.
```

It does not prove that N2's vertical connection trace is the same metric
variation. That identification requires \(D_sv_s\) and the mixed section
Hessian.

The curved-ambient Gauss rewrite, already owned by H15/H25, prevents an
isolated Einstein--Hilbert overclaim:

\[
\alpha_{II}|II|^2+\beta_0|II_0|^2
=
-(\alpha_{II}+\beta_0)R
+\left(\alpha_{II}+\frac34\beta_0\right)|H|^2
+(\alpha_{II}+\beta_0)R^Y_{\rm tan}.
\]

The coefficient of the isolated intrinsic \(R\) subchannel is
\(-(\alpha_{II}+\beta_0)\), but the \(H^2\) and ambient tangential-curvature
variations can alter or cancel its contribution to the full equation. Only
the complete \(D_sII\) and \(D_sR^Y_{\rm tan}\) maps can decide the native
geometry channel.

## 6. IG covariance, BV, and P3

Under the stated hypotheses—standard irreducible \(Sp(32,32;\mathbb H)\)
action, one fixed nonzero Clifford fourteen-plane, and a metric-preserving
induced vector action—equivariance would induce a nonzero homomorphism
\(\mathfrak{sp}(32,32;\mathbb H)\to\mathfrak{so}(9,5)\). The source algebra
is simple, so a nonzero homomorphism is injective; the dimension mismatch
\(8256>91\) forbids it. A trivial induced action would put the Clifford
image in the commutant of the standard irreducible \(Sp\) action. In the
right-\(\mathbb H\)-linear category that commutant is \(\mathbb R\); even in
the real irreducible category it has real dimension at most four. It cannot
contain an injective fourteen-dimensional Clifford-vector plane.

Therefore:

- the fixed gamma/\(P_R\) map is Spin-compatible, as in H23/W131, without
  proving covariance of the total \(P_0/\rho/Y/C\) kernel;
- fixed-plane full-\(Sp\) covariance is obstructed under those hypotheses;
  and
- the constructive full-\(Sp\) route is a moving Clifford plane soldered by
  the already-present \(\epsilon_{\rm IG}\).

The executable BV control is only an **even canonical-pair sign plant**:

\[
X_{\Phi}=\frac{\delta S}{\delta\Phi^+},
\qquad
X_{\Phi^+}=-\frac{\delta S}{\delta\Phi}.
\]

It does not verify the left/right graded signs of N1's mixed-parity field
space. N1 supplies an antifield-linear ownership skeleton, not a CME
solution. The antifield-number-one master-equation term already depends on
the unresolved typed bridge, graded conventions, and symmetry action, so
N4b factorization must wait for those maps rather than treating a Hessian
defect as a CME obstruction.

P3 is carried as the virtual pair

\[
\left(
D_{\mathcal E\otimes\nu^*H_n},
D_{\mathcal E\otimes\underline{\mathbb R}^4}
\right),
\]

not as one operator \(D\otimes\widehat e_n\) on an identified bundle. Their
local principal matrices agree as \(\sigma(D)\otimes1_4\). For nontrivial
\(n=\pm1\), their lower connection and curvature data differ; for \(n=0\)
with the trivial connection, the comparator pair agrees. No domain,
pushforward, index, or count has been taken.

## 7. Six-interface status

| interface | emitted status |
| --- | --- |
| total \(K/C\) kernels | projections explicit; zero-order full-20 placement deferred; first-order support ceiling kept only as a comparator |
| fermion/vertical Euler maps | formal covectors; literal \(J\)-bridge blocked |
| section trace split | moving current explicit; \(D_sII\), \(D_sR^Y_{\rm tan}\), and placement deferred |
| IG parent/compatibility | parent coefficient exact; fixed-plane full-\(Sp\) branch obstructed under stated hypotheses; dynamical soldering deferred |
| physical-\(R\) defect | LC piece exact; IG/compat/source Euler quotient deferred |
| P3 virtual symbol pair | principal pair explicit; nontrivial-\(n\) lower connection carried; \(n=0\) trivial; no pushforward |

The eight named missing maps are:

1. typed disposition of \(J_{\rm bridge}\): Riesz-lowered \(J_D\),
   Riesz-lowered \(J_D+J_F\), or an independent soldered current;
2. the full \(D_A\) matter covector and Hodge/Krein Riesz map, including
   projector, gamma, \(K\), and \(F_A\)-vertex adjoints;
3. the \(C\)-branch reality completion and exact
   \(P_0/\rho(\Phi)\)/provenance placement;
4. \(D_sII\), \(D_sR^Y_{\rm tan}\), the normal connection, and the
   geometric Green operator;
5. \(D_sP_0\), \(D_s\operatorname{res}_s^V\), \(D_sc_\rho\), and the
   bundle-valued moving equation dual;
6. the \(\epsilon_{\rm IG}\) soldering orbit and its full-\(Sp\) covariant
   derivative;
7. native-real-form \(\Omega^{IG}\)/open-BV Hom maps and the Euler-ideal
   quotient; and
8. the twisted subprincipal convention, closed domain, and pushforward.

## 8. One packet across all five legs

| leg | what N3 preserves | next decisive test |
| --- | --- | --- |
| Standard Model/Yukawa | separate total \(K/C\) projections; zero-order placement explicitly held open | build the \(\operatorname{End}(S)\to\operatorname{End}(E_{20})\), \(P_0,\rho(\Phi),Y_{K/C}\), reality, and gauge-generator packet |
| Krein/BV | total Krein adjoint, an even-pair sign control, and an explicit Euler ledger | decide the current/Riesz fork and its Noether identity before a graded Euler-ideal/CME test |
| gravity/dark energy | moving defect current, curved-ambient Gauss coefficients, and the kinematic trace-Hessian channel | construct \(D_sII\), \(D_sR^Y_{\rm tan}\), and the mixed vertical-trace/metric-section map |
| index/count | virtual P3 comparator pair retained on distinct bundles | fix the subprincipal/domain data before any pushforward |
| UV/causality | the frozen principal packet and gamma-traceless branch retained | choose Spin restriction or construct moving soldering, then retest the native symbol |

No successful row is bought by dropping another leg or adding a fitted
parameter.

## 9. Highest-information next swing

The next swing should construct the **typed connection-current and dynamical
soldering packet** in this order:

1. expand \(\delta_A S_{20}\) into \(J_D\), projector/gamma/\(K\), and
   \(J_F=D_A^!Q_F\) contributions;
2. construct the adjoint/Hodge/Krein Riesz map and decide whether the bridge
   means \(J_D\), \(J_D+J_F\), or an independent soldered current;
3. in the same calculation, derive the current's gauge transformation and Noether
   identity, so a merely type-correct but noncovariant repair fails;
4. build the minimal zero-order placement packet
   \(\operatorname{End}(S)\to\operatorname{End}(E_{20})\), \(P_0\),
   \(\rho(\Phi)\), \(Y_K/Y_C\), and \(C\)-reality completion;
5. construct the \(\epsilon_{\rm IG}\) orbit map that moves the Clifford
   fourteen-plane, or formally restrict the packet to its Spin stabilizer;
6. verify that the resulting placement and soldering preserve right-
   \(\mathbb H\) compatibility of the P3 virtual pair, without taking an
   index;
7. recompute \(\mathcal E_A,\mathcal E_U,\mathcal E_{\epsilon_{\rm IG}}\)
   and the source part of \(\Delta_R\);
8. only then run the Euler-ideal factorization needed by N4b.

This blocker-first sequence principally advances the Krein/BV,
source-gravity, and UV/soldering legs. The explicit placement step gives the
Yukawa leg a real new test, while the right-\(\mathbb H\) check preserves the
index leg without pretending to advance its analytic pushforward. The
sequence does not close all five legs; it prevents progress on one from
silently breaking the others.

## Nonclaims

N3 does not claim stationarity, anomaly cancellation, nonlinear CME closure,
a closed/global operator domain, a physical mass eigenvalue, an index
pushforward, a cosmological prediction, or a generation count. It does not
identify three provenance summands with three generations, identify the N2
vertical trace with a metric variation, or treat a fixed Spin-compatible
matrix as full-\(Sp\)-covariant.

## RB4 continuation: the dynamical soldering target is a flag

RB4 executes the finite moving-family part of missing map 6 and sharpens
what remains.

The base-induced construction

\[
u\longmapsto
P_W(u),\qquad
W_4(u)=\mathbb Rt\oplus(u^\flat\odot u^\perp)
\]

is exactly covariant when the Cartan split, orientations, Clifford frame,
volumes, and conditional full-20 field move together. This repairs the
frozen-frame obstruction at pointwise finite grade.

It does not complete the \(\epsilon_{\rm IG}\) orbit. The full target is not
merely an observer \(u\). The source-indicated complex structure \(J\)
cannot be derived equivariantly from \(u\), and the common reduction target
is now typed as a compatible complex--Cartan flag

\[
\mathfrak f=(J,\Theta_\chi;t,\Omega_{\mathbb C}),
\]

where \(\Omega_{\mathbb C}\) denotes the still-unbuilt
determinant-one/complex-volume condition.

Consequently missing map 6 becomes the following explicit packet:

\[
\mathcal O_{\epsilon_{\rm IG}}
\longrightarrow
(P_W,J,t,\Omega_{\mathbb C}),
\]

together with its stabilizer descent, covariant derivative, gauge quotient,
and chain-rule contribution

\[
\mathcal E_\epsilon
\mapsto
\mathcal E_\epsilon
+(D_\epsilon P_W)^!\mathcal E_P
+(D_\epsilon J)^!\mathcal E_J
+(D_\epsilon\Omega_{\mathbb C})^!\mathcal E_\Omega.
\]

The displayed N1/N3 variation contains none of these terms because no such
map was previously defined. A frozen flag is therefore external at the
current grade; a zero-new-datum claim requires either a complete
compensator quotient or a source-derived flag Euler/Hessian receipt.

This continuation does not alter the current/Riesz fork, P3, or any
physical count. See
[`rb4-observer-cartan-moving-family-2026-07-30.md`](rb4-observer-cartan-moving-family-2026-07-30.md).
