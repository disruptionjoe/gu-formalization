---
title: "Semantic-integrity typed-object custody closure"
status: active_research
doc_type: semantic_integrity_certificate
created: "2026-08-25"
grade: "SEMANTIC CUSTODY AND FAIL-CLOSED CONTROL CLOSURE; NO NEW SCIENTIFIC RESULT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "Repairs metadata and detector custody; it kills no source claim or scientific route."
canon_verdict_change: none
---

# Semantic-integrity typed-object custody closure

Scope: existing artifact declarations, negative-result target custody, and the
branch-relative frontier certificate. This result does not rederive, strengthen
or weaken any source, action, ledger, SG4, prediction, paper or canon claim.

```gu-typed-objects
result: forty-six typed-object declaration reds, three kill-target scope reds and one stale current-frontier digest are closed without scientific movement
carrier: repository-native artifact declarations and semantic-integrity controls LAYER=UNTYPED CHIRALITY=N/A
pairing: declarations paired with their artifact-local prose and controls ON=governed-artifact-set
real_structure: N/A -- the closure spans process, specification and mathematical artifacts without assigning one shared real form
grading: semantic custody and control closure only
action_owner: N/A -- no scientific action owner is introduced or changed
target: typed-object, kill-target and current-frontier control state MAP-TYPE=evaluation
```

## Preflight bookend

The scientific frontier remains unchanged: `SC-SIG-55` is authority-limited,
ledger v0.263 remains terminal on all 91 rows, the named B2 action-root set is
empty, and prediction/SG4 work retains its exact dependencies. The fresh
result-hardening frontier contained three independent semantic-integrity
defects rather than another scientific gate.

The typed-object audit reported 46 reds across twelve malformed blocks and
three missing blocks. The kill-target audit reported three inherited negative-
result surfaces with no declared target. The current-frontier certificate
reported one stale research-agenda digest, and the agenda's own hardening
summary still stopped at a historical authorship count. The five comparator-
routing gaps were not selected: four have no author-declared type and one is
self-contradictory, so later classification would be invented attribution.

## Typed-object custody

All fifteen affected artifacts were read in their own scope before repair.
Twelve existing blocks now use only the closed `LAYER`, `CHIRALITY`, owner and
`MAP-TYPE` vocabularies. Process pathways and demand specifications declare
`LAYER=UNTYPED` or `MAP-TYPE=not-a-map` where no mathematical carrier or arrow
exists. Observed benchmark demands stay at the observed layer. The D4 rank
certificate retains its exact toy contraction. Three missing blocks now type:

- BP2's conditional family-incidence homomorphism, with spinor chirality left
  explicitly untyped and no physical decoupling owner claimed;
- SA2's source-package restrictions under the block and endogenous Lorentz
  embeddings, with no physical embedding selection; and
- the three-composition hostile review as an evaluation over a heterogeneous
  artifact set rather than a fabricated common carrier.

The audit moves from 46 reds across 177 blocks to zero reds across 181 blocks
after this certificate is included. Declared ambiguity rises visibly rather
than being hidden: every `UNTYPED` slot remains inspectable and no block is
all-untyped.

## Negative-result target custody

The three inherited surfaces now declare `NONE-NOT-A-KILL` with exact notes:

- the Zorro/DeWitt curvature result excludes two frozen repository-
  reconstructed nonzero branches while leaving source-global completion open;
- the regular-Cartan hostile review preserves only the untwisted-product
  obstruction and explicitly rejects a universal no-go; and
- the opposite-edge hostile review preserves a charge-only equivariant-
  selector obstruction while independent-edge and physical routes stay open.

The scope ratchet therefore moves from three inherited reds to zero. All
kill-bearing ledger rows remain typed, and source-claim coverage is unchanged.

## Frontier currency

The research agenda now records the actual 107/983 probe-authorship state and
the zero-red semantic-integrity controls. Its digest-bound frontier registry is
recomputed from those live bytes. The live next condition retains the same
selection order and exact source/action/dependency gates while retiring stale
historical counts from the instruction surface.

## Postflight and hostile review

- **Strongest overclaim:** a green declaration gate proves grammatical custody,
  not that every declared object is physically correct or fully typed.
- **Strongest contrary case:** process and combined-review artifacts genuinely
  lack one mathematical carrier; forcing an ambient/observed token would be
  less truthful than `UNTYPED`.
- **Weakest reproducibility seam:** the kill-target control bounds only its
  registered claim and frontmatter-trigger classes; its emitted coverage
  ceiling remains visible.
- **Hostile controls:** bad layer, map, owner and pairing tokens; a missing
  declaration; an untyped negative target; and a stale agenda digest all drive
  their controls red. Native self-tests remain green.

No source claim, source adherence, scientific verdict, action owner, ledger
row, SG4 phase, prediction, confirmation, paper, canon verdict, release
readiness or public posture changed. The next selection must rebuild the live
substantial frontier; semantic maintenance reopens only on a fresh concrete
defect.
