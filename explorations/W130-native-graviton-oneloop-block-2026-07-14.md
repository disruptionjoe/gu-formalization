---
artifact_type: exploration
status: exploration (5-persona inline team; deterministic test)
created: 2026-07-14
hypothesis: "W123 residual / flagship referee vulnerability 2 -- the repo has NO GU-native computation of the graviton one-loop block; the UV/tachyon chain's pure-gravity coefficients are ported"
branch: "W130 -- the GU-native graviton one-loop block, staged honestly: (A) the quadratic fluctuation operator of GU's induced |II|^2 gravity around flat space, computed natively in the pinned Convention-B construction, with the exact basis map onto the literature's fourth-order coupling space; (B) the one-loop divergent/log coefficients of GU's operator -- fourth-order route evaluated at GU's ACTUAL coefficient point, plus an exact second-order (Gilkey) route on every block it can reach; (C) does anything in the tachyon-chain beta system change at the native point?"
title: "VERDICT: Stage A COMPLETE-NATIVE, Stage B TWO-ROUTE (fourth-order DERIVED-on-PORTED at the native point + second-order EXACT blocks), Stage C NO-CHANGE-AT-NATIVE-POINT plus one NEW fork datum. (A) GU's native quadratic operator around flat space is computed for the first time INCLUDING the Weyl channel the conformal family provably cannot see: c_W = +2, c_R = -4/9, Lambda = -1, with Einstein-channel sector split gamma_TT : gamma_phi : gamma_slice = 3 : 2 : 1 (all positive = attractive); c_R = -4/9 lands EXACTLY on the value the W126 slice coefficients predict through the derived basis map c_R = a + b/3 + c/3 (the a2/a3 slice-vs-4D ambiguity provably cancels out of the scalar channel -- that flag DISCHARGES), and c_W = +2 has the OPPOSITE SIGN from the naive slice-literal reading (-2): the missing Weyl-channel number was not just unmeasured, its naive guess was wrong. The operator's gauge-invariant (TT + conformal) content is EXACTLY covariant fourth-order; a genuine NON-COVARIANT residue exists and is CONFINED to the diffeo-gauge (longitudinal) direction -- a computed property of the literal-graph construction, named as a residual. Sign maps DERIVED from the Stelle poles + SS mass anchors: f_0^2 = +1/(6 c_R), f_2^2 = MINUS 1/(2 c_W) (the C^2-channel sign joint W123 did not need). (B) In the direct-coefficient basis the Weyl-channel one-loop coefficient dc_W/dt = -kappa b_2/2 is POINT-INDEPENDENT (exact identity): evaluating at the native point CANNOT move the C^2 divergence -- the wrong-point risk in that channel is exactly zero; the R^2-channel coefficient depends only on rho = f_2^2/f_0^2 and at the native point (rho = 2/3) is dc_R/dt = -(265/324) kappa. Second-order route, EXACT: the Christensen-Duff c-trio 3:9:36 is DERIVED from Gilkey a4 (validating the engine), the repo's ported /180 b_2-shift rule is DERIVED, the W123 matter-scalar perfect square is DERIVED WITH ITS COEFFICIENT (w_a = 3 per real scalar), and the scalaron's own block is +1/72, mass-independent. (C) NO beta-system coefficient, sign, or O(1) factor changes at the native point: A = 5/6 + d_RS + 3 sum(xi+1/6)^2, b_2, and the c_RS_weyl band edges stand; the chain's tree-level sign inputs are natively CONFIRMED at operator level (m_0^2 < 0 both measures). NEW: the native tree point maps to f_0^2 = -3/8 AND f_2^2 = -1/4 -- BOTH negative -- so GU's native tree data lie OFF the AF branch (outside the W123 basin, failing its f_2^2 > 0 gate outright): the AF-branch tachyon verdict is untouched, and the AF-vs-AS fork (E2/W128) acquires its first native tree-side datum."
grade: "exploration. Stage A: DERIVED-NATIVE, exact sympy (the plane-wave quadratic forms of the Convention-B |II|^2 functional, both measure forks; the evaluator is regression-pinned to the verbatim W126 Route-1 code and the W126 slice results). Stage B route 1: DERIVED-on-PORTED (the FT/AB/SS one-loop beta polynomials are NOT re-derived; they are evaluated at the now-native point, with the point-independence of the Weyl channel proven as an exact identity of the derived coupling map). Stage B route 2: DERIVED (Gilkey a4 from E/Omega data on symmetric-space curvature arrays; validated on the anomaly trio before use). Stage C: assembly of the above. HONEST RESIDUALS: (1) the fourth-order gravity-multiplet block values (133/10; 5/3, 5, 5/6) remain PORTED -- this wave locates and evaluates them at the right point and proves the Weyl one cannot move, but does not re-derive them (a native fourth-order tensor heat kernel, Gusynin/Barvinsky-Vilkovisky class, was NOT attempted: bluff-free PARTIAL boundary); (2) the non-covariant longitudinal residue of the native operator is exhibited and confined but its own loop contribution is UNCOMPUTED -- the ported blocks assume the covariant Stelle class, so the loop statement applies to the operator's covariant (gauge-invariant-sector) projection; (3) magnitudes (m_0^2 values, Landau distances) remain normalization-gated exactly as H25 graded them -- the 16/3 inter-chain flag, the measure fork, and the new 3:2:1 Einstein split are proven sign/ratio-harmless and remain magnitude-live. Deterministic test tests/W130_native_graviton_oneloop_block.py, exit 0, positive controls first (W126 Part-0 curvature pin; verbatim-code regression; MSS interpolant; Starobinsky scalaron; GB null; anomaly trio; W46 roots). NO forbidden target {3,8,24,chi(K3),Ahat} assumed/inserted/hardcoded/divided-by. NO canon / RESEARCH-STATUS / claim-status / verdict / posture changed. H59/H61a remain OPEN."
depends_on:
  - explorations/W123-native-r2-running-sign-convention-audit-2026-07-13.md
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/W122-spin0-gauge-vs-physical-auxfield-2026-07-13.md
  - explorations/geometry-curvature-emergence/ii-s-coordinate-formula-2026-06-23.md
  - explorations/landscape-assessment-post-three-waves-2026-07-14.md
  - tests/W126_beyond4th_conformal_iisq.py
  - tests/W45_H57_stage1_beta_system.py
  - tests/W46_H57_stage2_fixed_point.py
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W130_native_graviton_oneloop_block.py
external_refs:
  - "Stelle, PRD 16 (1977) 953 -- fourth-order gravity spectrum and poles"
  - "Fradkin & Tseytlin, Nucl. Phys. B201 (1982) 469 -- one-loop quadratic gravity (ported blocks)"
  - "Avramidi & Barvinsky, Phys. Lett. B159 (1985) 269 -- omega beta (ported blocks)"
  - "Salvio & Strumia, JHEP 06 (2014) 080, arXiv:1403.4226 -- agravity couplings and mass anchors M_i^2 = f_i^2 Mbar^2/2"
  - "Gilkey, J. Diff. Geom. 10 (1975) 601; Vassilevich, Phys. Rept. 388 (2003) 279 -- the a4 heat-kernel coefficient used in the second-order route"
  - "Christensen & Duff, Nucl. Phys. B154 (1979) 301 -- the anomaly trio 3:9:36 used as the engine validation target"
  - "Gusynin, Phys. Lett. B225 (1989) 233; Barvinsky & Vilkovisky, Phys. Rept. 119 (1985) 1 -- the fourth-order/non-minimal heat-kernel machinery this wave explicitly did NOT attempt (named PARTIAL boundary)"
---

# W130 -- The GU-native graviton one-loop block: the operator, the point, the verdict

**Role.** The tachyon chain (W79/W122/W123/W126) rests, at exactly one place, on numbers no wave
ever computed natively: the pure-gravity one-loop blocks, PORTED from Fradkin-Tseytlin /
Avramidi-Barvinsky / Salvio-Strumia. W123 proved the PORT is faithful (convention chain pinned end
to end) and left as its own residual: *"no GU-native graviton loop is computed here or anywhere in
the repo."* The flagship referee names the same gap as vulnerability 2. W128 (same day) reduced the
AS/Reuter-branch rescue to ported relevance signs and named the native graviton block as the
settling computation. W130 attacks the gap in the staged, honest order: first the OPERATOR (Stage
A), then the COEFFICIENTS at GU's actual point (Stage B), then the VERDICT on the chain (Stage C).

Five personas inline, one context, no sub-agents. Deterministic test:
`tests/W130_native_graviton_oneloop_block.py` (exact sympy throughout, exit 0).

## 0. Construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md), named

| Object | Construction used | Why |
|---|---|---|
| **II of the section** | ii-s **Convention B** literal-graph, vertical representative + normal lift -- the SAME code path as W126 Route 1, generalized to arbitrary metric 2-jets and regression-pinned to the verbatim W126 implementation at a dphi != 0 jet | The repo's pinned construction (H15/H24/H25/W126 all sit on it). The generalization is forced: plane-wave TT jets are NOT conformal, and the Weyl channel is invisible on the conformal family (Weyl = 0 there, exact -- W122). |
| **Measure** | BOTH: sqrt(det gbar) (induced) and sqrt(det g) (section) | The W126 fork, carried. Computed result: the k^4 couplings (c_W, c_R) are IDENTICAL in both measures; the Einstein/Lambda channels differ in magnitude only, signs robust. The fork is now quantified instead of open-ended. |
| **Background** | flat eta with the native tadpole (Lambda = -1) kept, coefficients read off by k-grading | Flat space is not a stationary point of the induced functional (the DeWitt-Lambda constant a0 = 2). k-grading separates the channels exactly: Lambda touches only k^0, gamma only k^2, (c_W, c_R) only k^4. |
| **Which f_0^2 / f_2^2** | agravity couplings DERIVED from the operator poles + the SS mass anchors: f_0^2 = +1/(6 c_R), f_2^2 = -1/(2 c_W) | Not defaulted: the R^2 joint reproduces W123's B3 pin; the C^2-channel MINUS sign is a new joint that no previous wave needed (W123's audit never had a native c_W to map). |
| **UV completion** | none selected | Stage C produces a fork DATUM (the native tree point lies off the AF branch), not a fork selection. The AF-vs-AS fork (E2, W82/W128) stands. |

## 1. Persona 1 -- differential geometer: Stage A, the native operator

**Method.** Plane-wave metric 2-jets `g = eta + eps1 A e^{ikx} + eps2 B e^{-ikx}`; the quadratic
Lagrangian is the `eps1*eps2` coefficient of the density, which is x-independent (the mixed
monomial always pairs `e^{ikx}` with `e^{-ikx}`), so a pointwise 2-jet evaluation IS the action
density and total derivatives drop automatically. Two sectors: TT (`h_12`) and spin-0
(`phi eta + beta k(x)k` -- the two covariant scalars, W122's polarization basis). The SAME jets are
fed to (i) a from-scratch curvature engine (reference class
`-2 Lambda + gamma R + a R^2 + b Ric^2 + c Riem^2`) and (ii) the generalized Convention-B `|II|^2`
evaluator. Regressions: the evaluator equals the verbatim W126 code on a conformal `dphi != 0` jet
(exact rationals), reproduces the MSS interpolant `-64u^2 - 8u + 2`, and reproduces the full
10-component slice decomposition `2 + R/3 + (8/9)R^2 - 4 Ric^2`.

**The native operator (the Stage-A deliverable).** In the pinned convention, both measures:

```
TT:      Q = a12^2 [ 2 c_W k^4  -  gamma_TT k^2  +  2 Lambda ]
spin-0:  Q = phi^2 [ 18 c_R k^4 +  3 gamma_phi k^2 ] + Lambda-block + (gauge-direction residue)

c_W   = +2        (both measures; the number the conformal family cannot see)
c_R   = -4/9      (both measures)
Lambda = -1       (i.e. -2 Lambda = +2 = the W126 flat constant; internally consistent)
gamma_TT : gamma_phi : gamma_slice = 3 : 2 : 1   (induced measure: 1, 2/3, 1/3; all POSITIVE)
```

**Covariance status, stated exactly.** The gauge-invariant content (TT block and the phi block) is
EXACTLY a covariant fourth-order operator: after subtracting the reference at
`(Lambda, gamma_phi, c_R, c_W) = (-1, 2/3, -4/9, 2)` the entire spin-0 residual is proportional to
`beta` -- the diffeo-orbit direction -- and the covariant class provably has NO beta-sector
4-derivative terms (the reference `beta beta k^8` and `beta phi k^6` coefficients vanish
identically in ALL of `(Lambda, gamma, a, b, c)`; the native operator has them with coefficients 1
and -2). The exact residual (induced measure, `(k^2)`-graded, symmetric completion in
`(phi_1, beta_1) x (phi_2, beta_2)`):

```
Q_native - Q_covariant(-1, 2/3, -4/9, 2) =
    beta1 beta2 [ (k^2)^4 + (k^2)^3/2 ]  -  2 (beta1 phi2 + beta2 phi1) [ (k^2)^3 - (k^2)^2 ]
```

(section measure: the same with `(k^2)^3/2 -> -(k^2)^3/2` and `-(k^2)^2 -> -2(k^2)^2`). So the
literal-graph functional's quadratic form is NOT diffeo-gauge-invariant beyond the Lambda-tadpole
structure: a computed property of the pinned construction, confined to the longitudinal sector,
named as a residual (Section 5). The Einstein-channel sector split (3 : 2 : 1,
identical ratio in both measures) is the same phenomenon one derivative down -- and it is exactly
the "c_L background-vs-TT normalization" item the landscape carries as open: now COMPUTED. Every
member of the split is positive (attractive, H25-consistent), so no sign statement anywhere in the
chain feels it.

## 2. Persona 2 -- higher-derivative-gravity expert: the basis map, and where the factors live

Derived, not recalled (Part A of the test, from-scratch curvature):

- **Basis map:** the operator sees `(a, b, c)` ONLY through `c_W = 2c + b/2` and
  `c_R = a + b/3 + c/3`; Gauss-Bonnet `(1, -4, 1)` is in the kernel (identically zero quadratic
  form, both sectors); the Weyl combination `(1/3, -2, 1)` has zero spin-0 form (W122 re-derived in
  momentum space).
- **Poles:** `m_0^2 = gamma/(6 c_R)` (W122's Legendre formula re-derived at operator level) and
  `m_2^2 = -gamma/(2 c_W)` (Stelle), with the Starobinsky control fixing the mass-sign convention.
- **Sign maps:** via the SS anchors `M_i^2 = f_i^2 Mbar^2/2`: `f_0^2 = +1/(6 c_R)` (W123's B3 pin,
  re-derived) and `f_2^2 = -1/(2 c_W)` (NEW: the direct Weyl-squared coefficient in this convention
  is MINUS `1/(2 f_2^2)`; the AF branch `f_2^2 > 0` corresponds to `c_W < 0`).

**Where the flagged factors live -- the discharge ledger:**

| Flag | Status after W130 |
|---|---|
| a2/a3 slice-vs-4D split (W126) | **DISCHARGED for the scalar channel:** `c_R = a2 + a3/3` is invariant under the full `(GB, C^2)` representation freedom -- the ambiguity lives entirely in the `(c_W, GB)` plane, and the operator computation confirms `c_R = -4/9` exactly. **RESOLVED for the Weyl channel by computation:** `c_W = +2`. The naive slice-literal reading (`g4 = w4 = 0` => `c_W = -2`) had the WRONG SIGN -- the flag was hiding a sign, exactly what the skeptic brief feared, and it is now measured. |
| 16/3 (flat 2 vs H50 c_L = 3/8) | **DISCHARGED at sign/ratio level:** an overall rescaling Omega > 0 of the functional leaves `m_0^2 = gamma/(6 c_R)`, `r = -c_W/(3 c_R)`, `omega = -(3/2) c_R/c_W`, and every coupling sign invariant (Omega < 0 is excluded by the H25 attractive calibration). Inter-chain ratio remains a MAGNITUDE item only, exactly as H25 graded magnitudes. Internally, this chain is consistent: the operator's Lambda channel (-2 Lambda = 2) equals the functional's flat value. |
| c_L background-vs-TT normalization | **COMPUTED:** the Einstein channel genuinely splits by sector, `gamma_TT : gamma_phi : gamma_slice = 3 : 2 : 1`, measure-fork-stable as a ratio. Signs uniform positive; magnitude-level consequences only (which gamma calibrates `mu_DW`-unit masses). |
| measure fork (W126) | **QUANTIFIED:** `(c_W, c_R)` identical in both measures (the k^4 grade cannot feel a 2-derivative measure correction -- proven by computation); the kinetic-sector gammas differ by a common factor 5, the ratio 3:2 and all signs are fork-stable. |

## 3. Persona 3 -- heat-kernel specialist: Stage B, the coefficients, two routes

**Route 1 (fourth-order, DERIVED-on-PORTED, at the native point).** In the direct-coefficient
basis the ported beta system transforms into:

```
dc_W/dt = -kappa b_2/2                                  (EXACT CONSTANT: point-independent)
dc_R/dt = -(kappa/6) [ (5/3) rho^2 + 5 rho + A ],       rho = f_2^2/f_0^2
```

The first line is the sharpest possible answer to "does the ported number sit at the wrong point":
the Weyl-channel one-loop log coefficient of a renormalizable fourth-order theory is
point-independent in this chart, so GU's native point CANNOT move it -- the wrong-point risk in
the C^2 channel is exactly zero, as an identity of the derived map, not as an estimate. At the
native point `rho = f_2^2/f_0^2 = (-1/4)/(-3/8) = 2/3`:

```
dc_W/dt|_GU = -(133/20) kappa            (pure gravity; -(kappa/2)(133/10 + 17/12) with RS anchor)
dc_R/dt|_GU = -(265/324) kappa           (pure-gravity content, d_RS_R2 = 0 (W82))
beta_{f_0^2}|_GU = +(265/384) kappa      (f_0^2 rises from -3/8, the W123 direction)
```

Route labels: the polynomial COEFFICIENTS are ported (FT/AB/SS, verified faithful by W123); the
POINT, the chart, and the point-(in)dependence statements are native/derived.

**Route 2 (second-order Gilkey, EXACT method).** Engine: the a4 density for `Delta = -(box + E)`
with bundle curvature Omega, evaluated on symmetric-space curvature arrays and fitted on the
`{W^2, E4, R^2, m^2 R, m^4}` basis (three families, overdetermined). Validation BEFORE use, per the
brief: the Christensen-Duff c-trio (3, 9, 36)/360 and a-trio (1, 11/2, 62)/360 come out of E/Omega
data alone (scalar; Dirac via explicit gamma matrices; vector in Feynman gauge minus two ghosts),
and the conformal R^2 nulls (Dirac, vector, xi = 1/6 scalar) land. Then:

- **The repo's /180 rule is DERIVED.** W45 ported "b_2 shift = anomaly coefficient/180" as a rule;
  with the Part-A sign map `c_W = -1/(2 f_2^2)`, the Gilkey W^2 coefficients reproduce the scalar
  +1/60, fermion +1/20, vector +1/5 shifts exactly, fixing the single channel rule
  `d(direct coefficient)/dt = -kappa (a4 coefficient)`.
- **The W123 matter block is DERIVED with its coefficient.** The scalar R^2 block is
  `(1/2)(xi - 1/6)^2` (perfect square, exact, xi symbolic); pushed through the channel rule and the
  `f_0^2` map it gives `Delta beta_{f_0^2} = +3 kappa (xi + 1/6)_SS^2 f_0^4` per real scalar:
  the perfect-square structure AND `w_a = 3`, previously PORTED-structure in W123's A coefficient,
  now derived by an independent exact method. The A-flip impossibility argument now rests on a
  computed square, not a cited one.
- **The scalaron's own block:** xi = 0 gives `+1/72` in the R^2 channel -- positive-definite and
  MASS-INDEPENDENT (the tachyonic `m_0^2` cannot enter the R^2 log coefficient; it enters the
  `m^2 R` Einstein-channel divergence as `-m_0^2/6` and the `m^4/2` Lambda-channel one). This is
  the exact-method cross-check leg the brief asked for, at its honest scope: it is ONE block of the
  fourth-order `A = 5/6` (the graviton and mixing blocks are gauge-dependent off shell in the
  second-order frame and were NOT reassembled -- see the PARTIAL boundary).

**What Route 2 does NOT do (bluff-free boundary).** No fourth-order/non-minimal tensor heat kernel
(Gusynin, Barvinsky-Vilkovisky) was attempted; the gravity-multiplet values 133/10, 5/3, 5, 5/6
remain PORTED. W130's contribution to them is: the faithful-port verification (W123) + the
point-independence theorem (the Weyl one cannot move) + the native point at which the R^2-channel
polynomial is now evaluated + the exact derivation of every matter-side block the chain uses.

## 4. Persona 4 -- symbolic engineer: what the test pins down

`tests/W130_native_graviton_oneloop_block.py`, exact sympy, deterministic, no RNG, 51 checks,
exit 0 (~10 min runtime; `reproduce_all --timeout 900` to include it in the sweep).
Structure (positive controls before every extraction):

- **A0** curvature convention pin: the from-scratch engine reproduces the W126 Part-0 conformal
  curvature (`R = -6 tr_eta(s)`) exactly.
- **A1-A4** reference operator: Lorentz covariance in k^2; the basis map (c_W, c_R); GB null;
  Weyl spin-0 null; diffeo invariance at Lambda = 0 (beta-independence); poles
  `m_0^2 = gamma/(6 c_R)` (Starobinsky control +1/6), `m_2^2 = -gamma/(2 c_W)`; the sign maps.
- **B1-B2** evaluator regressions: verbatim-W126-code equality at a `dphi != 0` conformal jet
  (exact rational, 4409421/120050); MSS interpolant; full 10-component slice decomposition.
- **B3-B6** the native forms (both measures), coupling extraction, the covariance audit
  (reference beta-sector k^4-grade identically zero vs native coefficients 1 and -2; residue
  confined to beta), native poles (`m_0^2 = -1/4 / -5/4` induced/section; `m_2^2` likewise).
- **C0-C5** Gilkey engine: gamma algebra; the anomaly trios; conformal nulls; the /180 rule;
  `w_a = 3`; the scalaron block (1/72, `-m^2/6`, `m^4/2`).
- **D1-D8** the chart identities (point-independence; ratio-dependence), W46 roots control,
  the native point in the agravity chart, basin membership, the native-point coefficient values,
  the c_W-chart crossing observation, the NO-CHANGE audit, the Omega-rescaling invariance.

## 5. Persona 5 -- adversarial skeptic: the error hunt, and what survived it

The brief's steelman pair was: *"the ported point was already right"* vs *"a factor hides in the
map."* Both landed, in an instructive split:

1. **The port was right (W123 confirmed, W130 re-confirms).** Every joint W130 re-derives
   independently -- the `f_0^2 = 1/(6 c_R)` map, `m_0^2 = gamma/(6 c_R)`, the W46 roots, the /180
   rule, the matter square -- lands on the ported chain's values. No sign error was found in any
   previously used joint.
2. **A factor WAS hiding -- in the unmeasured channel.** The naive slice-literal reading of W126's
   `a3 = -4` would give `c_W = -2`; the computed native value is `c_W = +2`. Had any wave quietly
   used the slice-literal guess, GU's native tree point would have been placed INSIDE the AF basin
   (`f_2^2 = +1/4 > 0, r = -3/2 < r_1`); the computed point is OUTSIDE it (`f_2^2 = -1/4 < 0`).
   The construction fork W126 flagged instead of defaulting was concealing a sign that flips a
   fork-level conclusion. This is the wave's sharpest vindication of the flag-do-not-default rule.
3. **The skeptic's residual bites where it should.** The native operator is not exactly a Stelle
   operator: it carries a longitudinal non-covariant residue (and the 3:2:1 Einstein split). The
   ported one-loop blocks are computed for the covariant class, so Stage B's statements apply to
   the operator's gauge-invariant projection; whether the longitudinal residue contributes to
   physical running (it sits in the diffeo-orbit direction, where a properly quantized theory
   cancels it against the diffeo ghosts/measure -- W78's constraint analysis is the repo's
   standing treatment of that sector) is NAMED as the residual this wave leaves, not silently
   assumed away.
4. **Where the skeptic must stop.** The tachyon chain does not currently stand on `c_W`'s sign or
   magnitude anywhere: W122/W123/W126's verdicts are conditional on the AF branch, within which
   `f_2^2 > 0` is an assumption of the branch, not a native claim. W130's `f_2^2 < 0` does not
   contradict any prior wave; it says the AF branch's assumption is not natively realized at tree
   level around flat space, which RAISES the fork W128 just mapped (AS permits the rescue AF
   forbids) and adds: the native tree data do not start on the AF side. In the direct-coefficient
   chart the one-loop Weyl flow is globally linear (`c_W(t) = 2 - (b_2/2) kappa Delta t`, crossing
   zero after ~43-48 e-folds), so the W123 finite-t blow-up of negative `f_2^2` is a chart
   singularity at `c_W = 0` -- but the crossing is `|f_2^2| -> infinity` = strong Weyl coupling,
   where one loop adjudicates nothing. FORK DATUM, not fork resolution.

## 6. Verdict (the brief's return fields)

- **Stage A: COMPLETE.** Native operator: `c_W = +2`, `c_R = -4/9`, `Lambda = -1`,
  `gamma_TT : gamma_phi : gamma_slice = 3 : 2 : 1` (all positive), both measures; basis map
  `c_W = 2c + b/2`, `c_R = a + b/3 + c/3`; sign maps `f_0^2 = +1/(6 c_R)`, `f_2^2 = -1/(2 c_W)`.
  The 16/3 and c_L flags discharge at sign/ratio level (the c_L item is now a computed 3:2:1
  split; the 16/3 stays magnitude-only); the a2/a3 ambiguity discharges for the scalar channel
  and is resolved by computation for the Weyl channel.
- **Stage B (route-labeled):** Weyl channel `dc_W/dt = -kappa b_2/2` [DERIVED-map on PORTED b_2;
  point-independent, hence NATIVE-POINT-PROOF]; R^2 channel `dc_R/dt|_native = -(265/324) kappa`
  at `rho = 2/3` [DERIVED-on-PORTED at the native point]; matter scalar block `w_a = 3` and
  scalaron block `+1/72` [EXACT, second-order Gilkey route, engine validated on 3:9:36 first].
  Fourth-order tensor blocks NOT re-derived [honest PARTIAL boundary].
- **Stage C: NO-CHANGE-AT-NATIVE-POINT.** No sign, no O(1) factor in the beta system feeding the
  tachyon chain moves: `A = 5/6 + d_RS_R2 + 3 sum (xi + 1/6)^2` (the square now derived), `b_2`
  and the `c_RS_weyl` band [1.02, 1.82] untouched, W123's monotonicity/Vieta/basin results stand
  verbatim. The chain's tree sign inputs are natively confirmed at operator level
  (`m_0^2 = -1/4` induced / `-5/4` section, both negative). The referee's vulnerability-2
  "ported point might be the wrong point" is discharged in the strongest available form for the
  Weyl channel (cannot move) and evaluated-at-the-point for the R^2 channel.
- **NEW (fork datum):** the native tree point is `(f_2^2, f_0^2) = (-1/4, -3/8)` -- off the AF
  branch. Together with W128 (AS permits the healthy scalaron), the weight of the UV question
  moves further onto the AF-vs-AS fork, now with a native tree-side input.

## 7. What would settle the remainder

1. **The longitudinal residue at loop level:** quantize the native operator with the diffeo
   ghosts/measure of the pinned construction and show the gauge-direction residue cancels in the
   one-loop determinant (or compute its physical remnant). This is the precise gap between "the
   covariant projection runs as computed" and "the native functional runs as computed."
2. **The native fourth-order tensor heat kernel** (Gusynin/BV machinery on the full 10-component
   operator including the residue): would upgrade 133/10, 5/3, 5, 5/6 from PORTED to DERIVED.
   After W123 + W130 the expected yield in the covariant sector is confirmation; the residue
   sector is where a surprise could still live.
3. **The fork:** W128's named settling computation (the native beta_{f_0^2} Hessian projection at
   finite (g, lambda)) now inherits W130's native tree point as its initial data; and the strong
   coupling crossing (`c_W = 0`) is the concrete obstruction any AF-branch matching story must
   address.

## What this does NOT do

No canon / RESEARCH-STATUS / claim-status / verdict / posture change. No AF-vs-AS fork selection.
No re-derivation of the ported fourth-order gravity-multiplet block values (named PARTIAL
boundary). No loop amplitude (W48 gate). No magnitude claims beyond the normalization gates H25
set. H59/H61a remain OPEN; the W79/W122 no-go remains conditional on the AF branch exactly as
W123 left it -- with the added native datum that tree-level GU does not start on that branch.
