---
artifact_type: exploration
label: W227
status: exploration (STEERING REGISTER; additive; no new computation; no canon/verdict/claim-status/posture movement)
created: 2026-07-14
wave: W227
title: "W227 -- GAP-ANALYSIS-TO-UNCONDITIONAL register. GOAL: move GU from 'more-unified but CONDITIONAL / incomplete' to UNCONDITIONAL + ONE CONFIRMED NOVEL PREDICTION -- the two things needed to honestly clear the two-bar higher-order-truth test (bar a: fewer flaws than every current explanation; bar b: no more flaws but MORE unification). TRUTH-SEEKING and explicit: GU is NOT proven. It survived a FIRST non-naive falsification gauntlet (W219-W226) with its structure intact and its gaps now PRECISELY located. The flaw-ledger cannot be scored clean while pieces are unbuilt -- which is exactly why closing the conditionals matters. This register is the machine-readable INDEX of what remains: Track A (six conditionals to close, ordered by load-bearing weight + dependency) and Track B (extract + confront GU's single most distinctive checkable novel prediction). It designates the current hourly work queue. It moves NOTHING: bar(b) and H59 stay OPEN, the count stays {1,3}."
posture: coherence-first (Joe 2026-07-14); exploration grade; truth-seeking (report what is unbuilt as unbuilt; a located gap honestly named is as valuable as a survival); RUTHLESS about what SURVIVES the gauntlet does and does not license; sovereign / exploration-tier / steering
method: "This is a STEERING REGISTER, not a computation. It reads the gauntlet corpus (W219-W226) and the pre-gauntlet conditional spine (W201/W202/W203/W211/W154 and the paper-hardening report) and records, in one place and in dependency order, the precise remaining work to make GU UNCONDITIONAL plus ONE confirmed novel prediction. Each item carries WHAT-CLOSING-IT-REQUIRES + DEPENDENCIES. Two standing monitors (empirical / dynamical) are noted as MONITORS, not hourly work. One bounded item per hourly run; receipt; promotion Joe-gated."
depends_on:
  - explorations/W223-falsification-scorecard-synthesis-2026-07-14.md
  - explorations/W224-falsify-nielsen-ninomiya-chirality-2026-07-14.md
  - explorations/W225-falsify-smg-dynamical-realization-2026-07-14.md
  - explorations/W226-harden-de-tripwire-squeeze-data-2026-07-14.md
  - explorations/W203-branch3-source-action-fixed-coefficients-2026-07-14.md
  - explorations/W202-signature-crux-bach-branch-2026-07-14.md
  - explorations/W201-count-external-datum-characterization-2026-07-14.md
  - explorations/W154-reverse-engineered-source-action-2026-07-14.md
  - papers/drafts/structurally-forced-internally-undecidable/HARDENING-REPORT.md
scripts:
  - tests/W227_gap_analysis_register.py
---

# W227 -- Gap-analysis-to-unconditional register

This note is an **additive steering register**. It computes nothing new, imports nothing external,
and moves no canon, verdict, claim-status, or posture. It names, in dependency order, the precise
remaining work that would move Geometric Unity from **more-unified-but-conditional** to
**unconditional plus one confirmed novel prediction**, and it designates the current hourly work
queue over that list. `bar(b)` and `H59` stay OPEN; the count stays `{1,3}`; exploration grade
throughout.

## 0. Where GU actually stands (truth-seeking, not vindication)

GU is **NOT proven.** What happened over 2026-07-14 is narrower and worth stating exactly: GU
survived a **first non-naive falsification gauntlet** -- W219 through W226 -- with its structure
intact and its gaps now **precisely located**. The gauntlet ran a strict discipline (grant every
unbuilt piece; "unbuilt" is a GAP not a falsifier; only a wrong definite output or a forced
inconsistency kills; every leg pre-declared a discriminating failure condition backed by a control
that provably fires on a real falsifier). Under that discipline:

- PPN / weak-field gravity: **SURVIVES** (W220-ppn).
- Generation count = 3: **SURVIVES-WITH-A-GAP** (W221; the count VALUE rests on an external `Z/3`).
- SM emergence (anomaly + hypercharge): **SURVIVES** (W222; chirality displaced onto an unbuilt condensate).
- Dark energy vs DESI DR2: **SURVIVES-WITH-TENSION** (W220-de, hardened W226).
- Nielsen-Ninomiya chirality no-go: **SURVIVES-BY-SMG** (W224; the mirror `16bar` is 't Hooft anomaly-free).
- SMG dynamical realization: **OPEN-IN-THE-FIELD** (W225; leaning survives, gated on an open lattice-field-theory problem).

Survival under this method is **genuine, hard-won credibility, not confirmation.** The crucial
honest point for this register: **the flaw-ledger cannot be evaluated as clean while pieces are
unbuilt.** GU is currently *more-unified but conditional and incomplete*. That is precisely why the
two things that would let it honestly clear the two-bar higher-order-truth test are (1) closing the
conditionals to make the picture **UNCONDITIONAL**, and (2) producing **ONE CONFIRMED NOVEL
PREDICTION.** This register indexes exactly that work.

### The two-bar test being targeted

- **Bar (a):** GU has strictly **fewer flaws** than every current explanation of the same facts.
- **Bar (b):** GU has **no more flaws** than the best current explanation but achieves **more
  unification.** (`bar(b)` and `H59` remain OPEN; this register does not touch that verdict.)

Neither bar can be adjudicated against an incomplete construction. **Track A** removes the "and-if
these unbuilt pieces resolve as hoped" qualifier from the flaw-ledger. **Track B** supplies the one
thing a merely-consistent unification cannot claim: a distinctive checkable prediction that
confronts data.

### Decisive under either outcome

Every item below is **decisive whichever way it resolves.** Closing a conditional either succeeds
(GU becomes unconditional on that axis) or **exposes a real hidden flaw** the gauntlet's grant-based
method deliberately deferred. The novel prediction either **confirms** (GU earns a discriminating
success) or **falsifies** (GU is wrong and we learn it cheaply). There is no wasted branch.

---

## TRACK A -- close the conditionals

Ordered by load-bearing weight and dependency. **Dependency read:** A1 gates A3 and the true-vacuum
health; A2 + A4 are the constructive spine and are the **live hourly work** while A1 finishes on the
hardening track.

### A1 (KEYSTONE) -- the corrected reservoir-sign theorem

**Status:** the "one undecidable bit" (Godel) framing was **KILLED this week** -- it conflated the
cone of positive metrics with the admissible `C`-gradings. The corrected theorem is **pending on the
paper-hardening track**
(`papers/drafts/structurally-forced-internally-undecidable/HARDENING-REPORT.md`).

**The question the corrected theorem must answer:** does **positivity FORCE the grading sign to be
unique** -- so the Krein-sign conditional resolves **INTERNALLY** -- or is the admissible residual a
**continuum** / genuinely **external**? For a compact stabilizer with the same irreducible type on
both signs the residual is normally a **continuous homogeneous space**, not one bit; whether GU's
actual physical stabilizer shares a type on both signs is the deciding computation.

**What closing it requires:** the derived physical state / stabilizer / observable algebra, the real
isotypic decomposition under that stabilizer, and a uniqueness-or-moduli read (no shared type -> unique
admissible grading; shared type -> exact continuous residual dimension `sum dim_R(D_lambda) a_lambda b_lambda`).

**Gates:** true-vacuum health, tachyon-spuriousness, and the SMG branch (A3). **Ownership now:**
mostly the hardening agent's job; hourly runs take **follow-ons**, not the keystone itself.
**Hygiene:** no hourly run may use the killed Godel / one-bit language as a premise.

### A2 -- source-action nonlocal (`Z_U`) completion

**Status:** W203 built only the **ultralocal** source kernel; the **nonlocal gradient-stiffness**
completion is **unbuilt**, so there are **no full field equations** yet.

**What closing it requires:** construct the nonlocal `Z_U` gradient-stiffness term, fix its
coefficients (not a free build -- a free build p-hacks), and derive the complete field equations.

**Dependencies:** feeds on **A4** (removing the reverse-engineered `W154` identification makes the
source action unconditional). **Ownership:** constructive core; **W203's named next blocker**;
**LIVE hourly work.**

### A4 -- derive or replace the `W154` identification

**Status:** several results rest on the **reverse-engineered** `W154` source-action identification
(`W154-reverse-engineered-source-action`). Reverse-engineering is a conditional: it fits the answer
rather than deriving it.

**What closing it requires:** either **derive** the `W154` identification from GU-native structure or
**replace** it with a derived object. Removing the reverse-engineering makes the source action
unconditional.

**Dependencies:** **feeds A2** (the source-action spine). **Ownership:** constructive; **LIVE hourly
work**, paired with A2.

### A3 -- dynamical SMG realization for GU's SO(10)-16 mirror

**Status:** chirality is **conditional on the symmetric gapped phase actually flowing**;
**OPEN-IN-THE-FIELD** per **W225**. The necessary (anomaly) condition is met (W224: the mirror
`16bar` is 't Hooft anomaly-free -- perturbative cubic, Witten `SU(2)` mod-2, and the mod-16
cobordism anomaly all vanish, the `nu_R` completion load-bearing). The **sufficient (dynamical)**
condition -- that the symmetric strong-coupling gapped phase realizes in 3+1D rather than collapsing
to a symmetry-breaking / gapless phase -- is unsettled for the exact SO(10) 16.

**What closing it requires:** chip the open lattice-field-theory problem via a **symmetry argument**
or a **targeted toy model for GU's specific mirror** (GU's mirror interaction is the unbuilt record
condensate, W216, so a faithful Monte Carlo cannot yet be run). **Dependencies:** gated by **A1**
(the good Krein branch / grading sign). **Ownership:** hourly-tractable in toy/symmetry form; the
full 3+1D realization is a standing monitor (see MONITORS).

### A5 -- pin the (9,5) signature via the open 2-primary Witten `Z/2` anomaly

**Status:** **W202** left the native signature `(9,5)`-vs-`(7,7)` **under-determined**; the one lever
is the **open 2-primary Witten `Z/2` anomaly.**

**What closing it requires:** compute the 2-primary Witten `Z/2` anomaly and either **pin** the
signature to `(9,5)` or **prove it is a genuine choice** (a free rep-canonicity input). The signature
decides whether the count is FORCED (`(7,7)` -> odd-rank-3 projector) or merely located (`(9,5)`), and
which functional fixes the gravity coefficient. **Dependencies:** independent of A2/A4; informs the
count characterization (A6). **Ownership:** targeted anomaly computation; hourly-tractable.

### A6 -- characterize the count-import dependence honestly

**Status:** several count results rest on an **external `Z/3` datum** (W201/W177/W218). Whether that
datum is a **FREE input** (-> the count is an **input, not a prediction** -- an honest downgrade) or
**structurally constrained** is the open question.

**What closing it requires:** prove which it is. **This may already be at its honest terminal state**
(W201's characterization + W218's vectorlike `R_src` + the W177 connection): if so, the task is to
**say so precisely** and record the count as an honest input rather than leaving it ambiguous.
**Dependencies:** informed by A5 (signature decides forced-vs-located). **Ownership:** characterization
/ honesty pass; low-compute, hourly-tractable.

---

## TRACK B -- one confirmed novel prediction

### B1 -- extract GU's single most distinctive checkable novel prediction, then confront it

**The job in one line:** find GU's sharpest **distinctive** prediction -- something **not used to
build GU** -- and confront it with data, **prioritizing one checkable TODAY over one gated to 2027.**

**Honest caveat (why the obvious answer does not count):** "GU predicts dynamical dark energy" is
**TOO GENERIC** -- a phantom crossing is not uniquely GU, and many models produce one. A generic
feature confirmed is not a discriminating success.

**Priority target:** a **mass / coupling (Yukawa) relation in the SM sector** -- a relation among
measured Standard-Model parameters that GU forces and that was **not used to construct GU** -- checkable
**NOW** against measured values. This is the strongest possible outcome: a today-checkable
discriminating prediction.

**Near-term backup (if no today-checkable SM relation survives the scan):** a **distinctive dark-energy
feature** -- e.g. the `w(z)` feature-width `~ one e-fold` from the clock-coupling, or an `f sigma8` sign
-- confrontable against **DESI DR3 / Euclid (~2027).**

**B1's first job is the SCAN:** enumerate GU's candidate distinctive predictions, rank by
(distinctiveness x checkability-now), and select the sharpest. **What closing it requires:** a
selected prediction with a pre-declared confirm/falsify condition, then the confrontation.
**Decisive under either outcome:** confirmation is a discriminating success toward bar (a)/(b);
falsification kills GU cheaply.

---

## MONITORS (standing; NOT hourly work)

These are tracked auto-firing / standing watch-items, not items an hourly run advances:

- **DE amplitude tripwire (empirical, near-term).** W226 hardened FC-d: current combinations span
  central ratios `-3.02 .. -3.83`, but **no current combination excludes `ratio > -3.5` at 2-sigma**;
  the tightest 2-sigma edge is `-2.39` (DESY5), a **live falsification margin of +1.11.** (W220's
  `+0.032` central-value figure was a DESY5-central artifact and is superseded by the 2-sigma edge.)
  **Resolving release:** DESI DR3 (full 5-yr, ~2027) / Euclid DR1 (~mid-2027). Encoded as a standing
  monitor in `tests/W226_de_tripwire.py`.
- **SMG dynamical realization (internal, open).** W225: **OPEN-IN-THE-FIELD** -- GU's chirality is
  conditional on an open 3+1D lattice-field-theory problem for the exact SO(10) 16. A3 chips it in
  toy/symmetry form; the full realization is a monitor, not an hourly deliverable.

---

## The current hourly work queue (designation)

Hourly runs work this register. **Priority order:**

1. **A2 / A4** -- the constructive source-action spine (nonlocal `Z_U` completion + derive/replace
   `W154`). **LIVE hourly work.**
2. **A3** -- chip the SMG dynamical realization via a symmetry argument or a GU-specific toy model.
3. **A5** -- pin `(9,5)` via the 2-primary Witten `Z/2` anomaly (or prove it a genuine choice).
4. **A6** -- state the count-import dependence at its honest terminal (likely a characterization pass).
5. **B1** -- run the novel-prediction SCAN, prioritizing a today-checkable SM Yukawa relation.

**A1 (keystone)** advances on the **paper-hardening track**, not the hourly loop; hourly runs take
**follow-ons** to it. **One bounded item per run, leave a receipt, promotion is Joe-gated.** No hourly
run may move canon, a verdict, claim-status, or posture; `bar(b)` and `H59` stay OPEN; the count stays
`{1,3}`.

---

## 2026-07-14 SIX-LANE GAP-CLOSURE WAVE RESULTS

The six Track-A conditionals (A1-A6) were each worked to a verdict in a single wave (W228-W233).
The register above is preserved verbatim as the pre-wave index; this section records the outcomes
and the consolidation they force. **Additive; exploration grade; no canon/verdict/claim-status/posture
movement. `bar(b)` and `H59` stay OPEN; the count stays `{1,3}`.**

### A1 (W228) -- COMPLETED-FORCED-INTERNAL (kinematic grade)

GU's actual good-stable stabilizer is the **canonical forced-unique case.** Non-coincidence of the
irreducible types across the two grading signs holds at `SO(9)xSO(5)`, at the refined
`SO(3)xSO(6)xSO(4)`, and in the `Sp`/`Spin` frames; the grading-sign moduli are **0**; positivity
**forces `C = eta`.** This **corrects** W206/W211's free-`Z/2` reading, which was exactly the
cone-vs-grading conflation the paper-hardening pass killed -- there is no free bit at the kinematic
level. **RESIDUAL:** the **DYNAMICAL** good-stable stabilizer (the actual vacuum isotropy plus the
observable algebra) is **unbuilt**; forcing is proven only at the **kinematic** level. See
`explorations/W228-close-a1-corrected-sign-gu-instance-2026-07-14.md`.

### A2 (W229) -- ADVANCED

The `Z_U` **nonlocal completion is BUILT** and the **full field equations are written**
(screened-Poisson / induced-YM form). The construction **reduces to W203's ultralocal source in the
`Z_U -> 0` limit**, and **all tensor coefficients are forced** (not a free build). It remains
**conditional** on: W154, two normalization scales `{kappa, Z_U}` (both the `eta`-from-gimmel-area
bridge -- undetermined-by-normalization, **not data-fitted**), and the `K_IG` selector, which is
**narrowed but not closed**. See
`explorations/W229-close-a2-source-action-znu-completion-2026-07-14.md`.

### A4 (W230) -- COMPLETED-POSIT

W154 (`theta = record current`) is a **required independent assumption**, now **reduced to ONE clean
named axiom: `c_kin = 0`** -- the marble/wood emergence axiom. The identification holds **iff the
connection distortion has no fundamental kinetic term**; only the **coupling sign** is forced. Fork:
A2 built the `Z_U != 0` side (full dynamics, softened nonlocal identity); `c_kin = 0` recovers the
clean identity. So W154 is honestly a **posit** -- accept-as-axiom or seek a deeper derivation. See
`explorations/W230-close-a4-derive-w154-2026-07-14.md`.

### A5 (W232) -- COMPLETED-GENUINE-CHOICE

The Witten `Z/2` anomaly does **NOT force `(7,7)`.** `(9,5)` is **provably the clean side**: dead
`KO`/`KSp` at dim 15; the reduced spin-bordism `Omega~15(BSp(64)) = 0` is empty; and the even
quaternionic multiplicity kills `pi_4(Sp(64)) = Z/2`. The **signature is a free convention**, and the
`(7,7)`/count-dissolution falsifier is therefore **DEAD.** See
`explorations/W232-close-a5-signature-witten-anomaly-2026-07-14.md`.

### A6 (W233) -- COMPLETED-CONSTRAINED

Separates **multiplicity** from **chiral actuation.** Multiplicity is **predicted**: a unique **odd
multiplicity = 3 = dim `Lambda^2_+(R^4)`** (arena, parity, operator-type). Chiral actuation is
**imported free**: no anomaly / Dai-Freed / modular requirement forces it. **Honest downgrade:** GU
**predicts a three-fold multiplicity** and **ACCOMMODATES** three chiral generations via a free `Z/3`
import; it does **NOT predict three chiral generations.** Terminal state: **located, not forced.** See
`explorations/W233-close-a6-count-import-characterization-2026-07-14.md`.

### A3 (W231) -- STILL-OPEN-NEEDS-X (leans REALIZES)

The SMG realization for GU's `SO(10)-16` mirror **reduces to ONE bit:** is the Cartan-involution `Z2`
(mirror = conserved record; W173) **operative?** If yes, the chirality-killing Dirac channel `D` is
**forbidden** and the phase **REALIZES.** **Caution:** GU's own W216 good-branch condensate
**structurally sits in the chirality-killing channel `D`** unless the `Z2` protects it. That one bit
**is** W173's operative-`C` / `Y14`-spectral-section datum. See
`explorations/W231-close-a3-smg-realization-gu-mirror-2026-07-14.md`.

### CONSOLIDATION (the headline)

The six lanes **collapse GU's conditionality onto essentially ONE central object:** whether the
**`C`-operator / Krein grading is operative at the DYNAMICAL (quantization) level.** Equivalently, and
these are the same object: *is the mirror a conserved RECORD; is the Cartan-involution `Z2` operative;
build the DYNAMICAL good-stable stabilizer and show it non-coincident.* **A1's residual, A3's bit,
`bar-b` (loop unitarity), the true-vacuum health, and W216's good-branch selection are ALL THIS ONE
OBJECT.**

It **LEANS FAVORABLE** -- A1 forced it **kinematically**, every derivable candidate stabilizer is
**non-coincident**, and A3 **leans realizes** -- but it is **UNBUILT at the dynamical level.** And it
is the **same object that took the Godel-kill this morning in a different framing**, so hold the honest
line: **NOT proven-modulo-one-bit.**

**Residuals beyond the central object:**
- The two normalization scales `{kappa, Z_U}` (W187's fade **dynamically discharges the magnitude**).
- W154 as a named posit (A4): accept-as-axiom or seek a deeper derivation (`c_kin = 0`).
- Track B B1 was subsequently scanned in W239. **No current candidate passes** the forced, quantitative,
  calibration-independent, distinctive, observable, confrontable, and robust gates. The priority Yukawa route
  emits support but no values. The H36 DeWitt-to-spin-2 relation is the sharpest quantitative conditional, but
  it imports the observed dark-energy density and is already excluded. Track B therefore remains unsatisfied;
  its scan is complete, and its exact reopen burden is a dimensionless flavor invariant, a GU-native scale
  relation without target data, or a complete physical mirror observable.

**The count is now honestly an ACCOMMODATION, not a prediction** (A6). No canon or verdict moves:
`bar(b)` and `H59` stay OPEN; the count stays `{1,3}`.

## 2026-07-15 TRACK B B1 RESULT (W239)

The candidate scan is complete. Its honest result is
`NO_CURRENT_DISTINCTIVE_CONFIRMED_PREDICTION`. This is a completed negative search result, not a
completed prediction. The full seven-gate census, conditional H36 confrontation, construction forks,
and machine-checkable reopen conditions are in
`explorations/W239-track-b-distinctive-prediction-scan-2026-07-15.md` and
`tests/W239_track_b_distinctive_prediction_scan.py`. No higher-order-truth bar moves. `bar(b)` and
`H59` remain OPEN.

---

*Filed 2026-07-14 by the W227 gap-analysis-to-unconditional register. Additive; exploration grade;
steering; truth-seeking. Indexes Track A (A1-A6 conditionals) + Track B (B1 novel prediction) over the
W219-W226 gauntlet corpus and the W201/W202/W203/W211/W154 + paper-hardening conditional spine.
Machine-checkable index: `python -u tests/W227_gap_analysis_register.py` (exit 0). No canon movement;
count stays `{1,3}`; `H59` and `bar(b)` OPEN.*
