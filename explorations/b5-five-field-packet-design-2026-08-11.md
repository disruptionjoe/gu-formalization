---
title: "Design packet: reopening the B5 five-field native operator/domain packet, one field at a time"
status: active_research
doc_type: design_packet
artifact_type: design_packet
created: 2026-08-11
target: "B5 five-field native packet (slot-pairing phases, coflip linearity/phases, formal-adjoint sign, program-native Green form, common closed domain)"
work_item: B5-INDEPENDENT-RECONSTRUCTION
reviewed_at_revision: bdd2c934335e6e534d4e0f9e7f55d7678eb566d8 (agent/operator-anomaly-big-swing, 2026-08-11)
relates_to:
  - archived private execution record (the blocking audit)
  - explorations/shiab-operator/b5-native-phase-domain-packet-contract-2026-07-25.md (+ tests/shiab_b5_native_packet_contract.py)
  - explorations/shiab-operator/b5-observer-symbol-multiplicity-matrix-2026-07-24.md
  - explorations/shiab-operator/b5-krein-mirror-orbit-reduction-2026-07-25.md
  - explorations/b5-phase-sum-forcing-audit-2026-07-29.md
  - explorations/b5-chirality-orientation-audit-2026-07-29.md
  - explorations/b5-constraint-surplus-audit-2026-07-29.md
  - explorations/sa-y8-majorana-layer0-and-vertical-krein-weld-2026-07-29.md
  - explorations/source-owned-reduction-transport-packet-2026-07-29.md
  - explorations/full20-chimeric-bv-first-write-2026-07-29.md
  - explorations/full20-native-polarization-closure-wave-2026-07-30.md
  - explorations/full20-dewitt-loop-transport-wave-2026-07-30.md
  - explorations/dc-h1-orbit-signs-monodromy-check-2026-08-04.md
  - explorations/time-orientation-home-for-the-orientation-datum-2026-08-04.md
  - explorations/c1-domain-moduli-result-2026-08-08.md
  - explorations/five-lens-analytic-council-2026-08-08.md
  - explorations/imposed-wall-triplet-comparator-2026-07-26.md
  - explorations/rb7-stationary-nonmetric-order-parameter-2026-07-30.md (+ its 2026-08-03 exact-derivative re-verdict)
  - explorations/rb6-target-blind-spectral-grammar-2026-07-30.md (M-C2 debt)
  - explorations/campaign-altitude-route-review-2026-08-11.md
  - explorations/signature-ambient-relative-sign-resolver-design-packet-2026-08-11.md (sibling packet, distinct fork)
binding: >-
  Design input only. This packet binds no wave and makes no disposition: it
  changes no claim status, canon verdict, registry row, fork, residue, packet
  field, priority, or public posture. Any executing wave owns every
  disposition under the full pre-flight / hostile-review checking contract,
  implements the decisive checks independently, and certifies or refutes
  them. Lens outputs below are planning evidence, never scientific evidence.
hostile_review: lab/process/hostile-reviews/2026-08-11-b5-five-field-packet-design-review.md
canon_verdict_change: none
---

# Design packet: reopening the B5 five-field native packet, one field at a time

## Execution update — native Rarita--Schwinger BV/Hessian lift completed 2026-08-20

The strict branch now reaches the actual complexified
`128 -> 1792 -> 1792 -> 128` carrier at principal-symbol grade. The natural
maps `A_xi(s)=xi tensor s`,
`(K_xi psi)_a=gamma_[a b c]xi^b psi^c`, and `A_xi^vee` obey both Noether
compositions identically. Off the null cone the transverse Clifford inverse
gives exact ranks `(128,1664,128)`. Under the native `(9,5)` Krein pairing the
middle symbol is formally anti-adjoint, its `RR` restriction is exactly the
W131-normalized `P_R c(xi) P_R` block with `q=1`, and all eight strict coarse
blocks are live.

A gauge-inert `S` spectator and cross row `B_xi=Gamma K_xi` produce a
full-nine graded Euler family with free coefficients `a,t` and the same
master/Noether identity. The CME still normalizes neither coefficient. At a
null covector the middle symbol has an exact nongauge kernel witness, and
covariant closure still carries the prior curvature obstruction. Thus strict
field (iii) is now `ANTI-PRINCIPAL-SYMBOL` on the native carrier, while the
current separate full-nine Euler family remains `EXTERNAL-VIA-GRAM` and the
five-field packet remains fail-closed. Next construct or obstruct
`B5-CURVED-RS-BV-COMPLETION`; coflip and domain work remain dependent.

## Execution update — strict BV action bridge completed 2026-08-20

The strict coarse complex now has an exact quadratic abelian
BV/Koszul--Tate owner. With `A=(1,1)^T`,
`K=(6/7)[[1,-1],[-1,1]]` and terminal map `A^T`, the canonical
antibracket gives `(S_BV,S_BV)=2 x^T K A c`; the genuine master-equation
condition is therefore `K A=0`, not Hessian symmetry. The complex is exact,
its fold has all eight eligible blocks, normalized `q=1`, and is
`SELF-CANONICAL-COARSE` for the declared stage evaluation.

Adding one gauge-inert `S` Euler spectator yields the full-nine family
`H(a,t)=[[a,t,-t],[t,6/7,-6/7],[-t,-6/7,6/7]]`, with live `SS=a` and the
same strict generator `(0,1,1)`. The CME leaves `a` and `t` free. The current
Stage-B Hessian is not in this family: it closes only for graph-mixing kernel
`(1,-2,1)`. Thus the strict action branch is nonempty, while the current
witness belongs to the alternative filtered route at coarse grade.

The five-field packet remains fail-closed. The strict field (iii) result is
only conditional coarse `SELF`; the native multiplicity Gram and full-20
formal adjoint remain open, and the current Euler family remains
`EXTERNAL-VIA-GRAM`. Next lift the strict BV/Hessian relations to the actual
20-slot carrier through `B5-NATIVE-BV-HESSIAN-LIFT` before coflip or domain
work.

## Execution update — strict-eight-block plus Euler separation completed 2026-08-20

The strict route is now typed as a pair rather than one nine-block operator.
The four-stage differential `d` and its stage-preserving fold `Q8` retain
eight-block support and strict exactness. A separate quadratic Euler/Hessian
`H9` may carry the live `aD_S` / `S->S` term. Exact witnesses prove that
changing that term or the multiplicity Gram leaves `d^2` and strict exactness
unchanged. Therefore acyclicity supplies no normalization equation for `a` or
`G`, and ordinary Hessian symmetry is not the BV master equation.

The packet ingress target is restated as `(d/Q8, H9, action/BV bridge)`. The
all-Gram `EXTERNAL-VIA-GRAM` result binds the current full-nine Euler family;
it does not compute the formal-adjoint sign of the strict eight-block
differential without a bridge identifying the objects. The packet remains
fail-closed. Next construct `B5-STRICT-DIFFERENTIAL-ACTION-BRIDGE` before
coflip, domain, or another normalization solve.

## Execution update — strict four-stage roll support completed 2026-08-20

The typed `0->1->13->14` owner has now been tested at strict
stage-preserving fold grade. Its three degree-`+1` arrows populate exactly
eight coarse blocks and structurally exclude `S->S`; an exact acyclic
`1->2->2->1` control retains all eight blocks and normalized W131 `q=1`.
Therefore the current full-nine-block Stage-B family is not the roll of that
strict differential.

This does not kill the action-complex route. Before another normalization
attempt, the owner must either construct the filtered field/antifield graph
roll that generates `S->S` while preserving BV degree/master compatibility,
or separate the strict eight-block differential from the quadratic `a D_S`
Euler/Dirac term and restate the five-field target. Field (iii) remains
`EXTERNAL-VIA-GRAM` for the current nine-block family; the packet remains
fail-closed.

## Execution update — Stage-B owner test completed 2026-08-20

The current Stage-B equation set is now executed at
`explorations/b5-stage-b-action-normalization-underdetermination-2026-08-20.md`.
At the coarse formal compact-core grade, the quadratic-action symmetry and
formal-adjoint conditions are the same equation `H^T=H`, and the linear
BV/Noether condition duplicates the coarse kernel equation. Two inequivalent
allowed Grams admit exact full-support `q=1` rank-two packets with one common
symmetric Hessian and all-grade kernel. The current equations therefore do not
normalize the Gram or coefficients.

Field (iii) remains `EXTERNAL-VIA-GRAM` and the five-field packet remains
fail-closed. Before another normalization attempt, the missing
`ACTION-OWNED-FOUR-STAGE-DIFFERENTIAL` packet must serialize the Hodge/Krein
roll maps and actual `0->1->13->14` differential, exposing an independent
nilpotence/acyclicity or nonlinear BV relation. This update does not exclude a
future selector and chooses no Gram, action, domain, quotient or GU verdict.

## Execution update — Wave One completed 2026-08-20

P-H29 Step 0 and the formal-adjoint computation are now executed at
`explorations/b5-full20-gram-adjoint-universality-2026-08-20.md`. Exact
gimmel derivatives recertify RB6's five geometry-owned commutators as
structurally zero. The full allowed `S/imGamma` multiplicity-Gram family then
forces every cross-grade coefficient of a Gram-uniform adjoint expression to
zero, which is incompatible with full nine-block support. The canonical `G3`
candidate remains a pairing-specific positive control.

Per K2, field (iii) became `EXTERNAL-VIA-GRAM` and opened the Stage-B owner
test now recorded in the update above. The five-field ingress contract remains
fail-closed; no Gram, action, Green form, common closed domain, packet
admission, source-preferred operator, or GU verdict is selected. The coflip
rider and domain-stability wave remain dependent on later owned data.

The interior program's `B5-INDEPENDENT-RECONSTRUCTION` has been BLOCKED since
the 2026-07-29 source audit at "the five-field native operator/domain packet"
(`CURRENT-STATE.yaml:12-14`), under the standing decision default
(`CURRENT-STATE.yaml:19-24`): require the five-field native packet before
selecting any B5 coefficient space; no FD-read null or kill is citable until
certified with exact derivatives or interval arithmetic (P-H29); no imposed
positive-Hilbert wall substitutes for a target-blind dynamical selector. The
audit found that **none** of the five fields was fixed by a repository-owned
construction (run-plan, "Execution and result": "`BLOCKED` — no
repository-owned native construction currently fixes any of the five required
packet fields").

This packet scaffolds, for each field, the derivation route that would fix it
natively, or the exactly-typed external datum that would (with
constraint-surplus accounting), or the honest under-determination verdict.
The central design finding, developed below: the audit's verdict was correct
on 2026-07-29 **and is stale in per-field detail** — a chain of repo-owned
constructions executed 2026-07-29 through 2026-08-08 (vertical–Krein weld,
full-20 chimeric-BV S1–S4 waves, DC-H1, the time-orientation relocation, the
C1 domain-moduli closure) has since fixed, partially fixed, retyped, or
provably dissolved large parts of fields (i), (ii), (iv), and (v) at stated
grades, while **nothing has been carried into the fail-closed packet
certificates**, which still reject every packet. The reopening design is
therefore mostly absorption-plus-one-computation, not construction from
scratch — with one genuinely open computation (the formal-adjoint sign /
pairing-table wave) named as Wave One.

## Pre-flight assessment

Failure modes this packet could commit, and the mitigations applied:

1. **Treating lens output as evidence.** All six lenses below are planning
   evidence. Nothing here is citable as a result; every scientific statement
   is a citation to an existing artifact at its own grade. Mitigation: the
   binding note, plus the verify-status manifest grading every claim.
2. **Silently importing the boundary campaign's constructions.** The live
   K77 operator-anomaly campaign (v0.163–v0.174) is building its own
   anti-linear reality adjoint and global Green/domain for the *corrected
   K77 wedge/southeast operator* — a different operator on a different fork.
   Its Green identities, graded-trace boundary triple
   (`tests/channel-swings/selected_k77_common_graded_trace_boundary_triple_probe.py`),
   and Cauchy-domain gates are **contrast objects only** here. The audit's
   construction fork is explicit: "the program-native observer-restricted
   `(9,5)` Rarita–Schwinger carrier, formal Krein adjoint, and
   normal-chirality coflip" (run-plan, "Construction fork"). Mitigation: the
   Layer-0 table separates the three same-named domain/Green objects.
3. **False novelty.** `python3 lab/process/novelty-check.py` was run on the
   candidate new terms ("pairing-table wave", "moduli-uniform",
   "verdict-stability", "20-slot pairing table", "B5 packet skeleton",
   "five-field design packet"). "20-slot pairing table" has one exact prior
   hit — `sa-y8-...-2026-07-29.md:358` names the pairing-table-plus-adjoint-
   sign computation as the next step. Wave One below is therefore the
   **named-but-unexecuted** in-repo step, not a new idea; this packet's
   contribution is its bounded specification, controls, kill conditions,
   and sequencing. Everything else is cited to its origin.
4. **Staleness in both directions.** The audit is 13 days old and the
   frontier moved; conversely, the loop-transport results are at "exact
   finite associated-bundle grade" and must not be read as packet-grade.
   Mitigation: the per-field state table distinguishes AUDIT-STATE from
   TREE-STATE, each with grade; the fail-closed certificates' rejection is
   reported as the current unconditional repo state throughout.
5. **P-H29 violation by scheduling.** P-H29 (AGENTS.md:44; register row 75)
   forbids a new interior wave until the prior wave's null is certified with
   exact derivatives. M-C2 (register row 145) is only **partially
   executed**: RB7 Track-A and W177 carry 2026-08-03 exact-derivative
   re-verdict addenda (both kills survive, evidence upgraded to exact); RB6's
   "no nonzero source-derived Q resolved" null
   (`explorations/rb6-target-blind-spectral-grammar-2026-07-30.md:75-76`,
   read "comparable to the finite-difference floor") has **no re-verdict
   addendum and no verify script** (`tests/channel-swings/verify/` holds only
   the rb7 and w177 re-verdicts). Mitigation: Step 0 below sequences RB6
   recertification before or with Wave One; the route review's F4 already
   names M-C2 "the standing debt with the widest blast radius if the
   interior program resumes."
6. **Homonym risk, rated HIGH.** The five field names are repo-native terms
   of art; the letter `C` alone names four distinct objects
   (`lab/process/NAMES.md`, "the parity" row); "B5" has three same-letter
   neighbors. Mitigation: the Layer-0 object table below is mandatory
   reading for the executing wave and re-run as its Layer-0 precondition.
7. **The orthodox reflex.** Two of the five fields end in datum-or-choice
   territory. Mitigation: the surplus lens computes (or reports
   SURPLUS-UNCOMPUTABLE with the named bridge) rather than arguing; a datum
   with surplus <= 0 is typed uninformative-as-fit, per AGENTS.md's standing
   correction.

## Layer-0 object table (run before any wave; six-axis precondition)

| term | object meant HERE | nearest homonyms (do not substitute) |
|---|---|---|
| **B5 / B.5** | the source-action spine's **middle differential**: the physicalization map on the frozen observer ledger `3E+ + 3E- + X` whose job is to acyclize `3E- + X` while retaining the three `E+` lines (`explorations/b5-independent-reconstruction-scope-2026-07-21.md` §1a); `B5-INDEPENDENT-RECONSTRUCTION` is the Joe-authorized (2026-07-21) lane building a RECOVERING action from an independently enumerated symbol class, running alongside the parked native-class question `B5-MIDDLE-DIFFERENTIAL` | "Branch 5" (gauge-group/super-IG results in `GEOMETER-VS-PHYSICS-OBJECTS.md`); "RB5" (spectral-ownership gate); "M-H9, the B5 signature test" (a falsified fork-resolver that consumed B5 objects) |
| **the B5 carrier** | the frozen independent-B5 ledger: 20 labeled irreducible slots, complex dimension 1920 = `S + im Gamma + ker Gamma` = 128+128+1664, under the observer restriction `H_C = Spin(4,C) x Spin(10,C)` of the `(9,5)`/RS carrier (`b5-observer-symbol-multiplicity-matrix-2026-07-24.md`) | **W131** `D_RS = Pi_RS c(nabla) Pi_RS + m^2 Pi_RS`, which acts on `ker Gamma` ONLY (12 slots, 1664) and is a **proper suboperator**, not the B5 differential (`source-owned-reduction-transport-packet-2026-07-29.md`, Layer 0) |
| **slot-pairing phases** (field i) | the phase-normalized invariant sesquilinear Krein pairing on every one of the 20 observer summands, including the S/I multiplicity Gram across the three provenance copies | the support-grade phases `epsilon_ij` already absorbed into the 39-orbit reduction (`b5-krein-mirror-orbit-reduction-2026-07-25.md`); the bare-spinor `C_14` transpose components of N2a (`actual-sym2-c14-odd-orbit-knockout-2026-07-30.md` — placement intertwiners to the 20 slots missing) |
| **coflip** (field ii) | NAMES.md sense (d): the monodromy coflip `C_perp = K J_obs`, antilinear involution with the exact B5 dual-slot support action | (a) the ghost-parity Krein form `K`; (b) the PT/Krein C-operator; (c) the antilinear chiralizer `J_quat . G` (whose `0.00e+00` frame charge does NOT transfer); the LINEAR normal-chirality coflip (same support permutation, different coefficient reduction) |
| **coflip "phases"** | the ten special-edge invariants `delta_e in {+1,-1}` of the antilinear fork; their signed sum is the sole dimension-relevant residual (`b5-phase-sum-forcing-audit-2026-07-29.md`) | the three orientation characters, which are DIFFERENT: `sigma_J(J_obs)=det` (spacetime), `sigma_K(K_S)=det*orth` (space), `chi(C_perp)=orth` (TIME) — any weld of "the arrow" to a Krein sign must name which (`time-orientation-home-for-the-orientation-datum-2026-08-04.md`) |
| **formal-adjoint sign** (field iii) | the sign of the actual assembled first-order differential expression under the formal KREIN adjoint with the physical pairing composed in | the positive-Hilbert adjoint sign (the contract's explicitly rejected substitution); "NINE-BLOCK-FORMAL-EXPRESSION-FAMILY-ADJOINT-CLOSED" (`full20-native-polarization-closure-wave-2026-07-30.md`) — closure of the family under adjoint is a weaker statement than one sign for one assembled operator |
| **program-native Green form** (field iv) | the Green boundary form of the B5 differential on the native carrier, emitted by K-integration-by-parts of the assembled expression | the K77 campaign's moving-density Green identity for the draft-9.16 operator (`k77-wave2-*` files — different operator, different fork); rb3c's planted abelianized 1D polynomial Green fixture (a toy); the S3 "FORMAL-GREEN-PACKET-BUILT" (the nearest owned object, formal grade) |
| **common closed domain** (field v) | one common closed, symmetry-compatible operator domain for the B5 packet | (b) the SECTION-restricted domain objects: ECW3D-A's `(832,832)` Green trace on an admitted flat Lorentz section and C1's `Gr(k,832)` moduli — these live on `ker Gamma` restricted to a section, not on the full packet; (c) the K77 selected-operator Cauchy/graded-trace domains (boundary campaign; do not import); (d) the 1D pencil/graph-domain certificates (existence pattern only) |
| **"imposed wall"** | the standard-field positive-Hilbert domain-wall comparator that HOSTS `N=3` but does not select it (`imposed-wall-triplet-comparator-2026-07-26.md`: return `IMPOSED_BOUNDARY_HOSTING / EFFECTIVE_ACCESS_N`) | a target-blind dynamical selector (what the decision default demands instead); a generation derivation (explicitly not) |

## State of the problem, compressed (every line cited)

- The blocking verdict: no repo-owned construction fixes any field; the
  finite algebra does not license choosing them; the fail-closed contract
  rejects the all-unfrozen packet and both hostile substitutions
  (positive-Hilbert; unspecified Green form)
  (`historical-investigation/run-plan.md`, "Execution and result").
- The certified algebra beneath it: 136 ordered complex cells on 20 slots
  (1920 dims); 68 adjoint edges; 39 joint `<T,M>` orbits = 29 four-cell + 10
  special two-cell; linear coflip gives (68,68); antilinear gives
  `(58+2k, 78-2k)`, `k` = number of `+1` phases among ten — eleven pairs
  (`b5-krein-mirror-orbit-reduction-2026-07-25.md`; certificates
  `tests/shiab_b5_*.py`, re-run green in the audit).
- The residual is ONE integer, not ten bits: `even = 68 + sum(signs)`,
  verified exhaustively over 1024 assignments; chirality distinguishes 6 of
  10 special orbits, every other certified invariant is blind to all ten;
  the four X-sector orbits are blind to everything certified
  (`b5-phase-sum-forcing-audit-2026-07-29.md`).
- Chirality distinguishes but cannot ORIENT: the ledger is invariant under
  global `E+ <-> E-`, so the six addressable orbits need an external `Z/2`
  orientation; verdict `DISTINGUISHED-NOT-ORIENTED`; residual stays eleven
  pairs (`b5-chirality-orientation-audit-2026-07-29.md`).
- Surplus meter: `SURPLUS-UNCOMPUTABLE` — zero of the eight FORCED rows
  share a distinctive certified object with the phase residual; the blocker
  on positing is a missing BRIDGE, not epistemics; one bridge (SA-C2 is the
  named candidate) makes the surplus computable
  (`b5-constraint-surplus-audit-2026-07-29.md`).
- Post-audit movement, same day: all four X-sector special orbits are
  vertical-only projected RS-symbol edges; `C_perp = K J_obs` has exactly
  the B5 dual-slot action and gives one uniform relative parity to all ten
  special edges; P1/P2 conditionally weld to one bit
  (`sa-y8-majorana-layer0-and-vertical-krein-weld-2026-07-29.md`).
- Same day, the Layer-0 wall: W131 is a proper suboperator (12/20 slots);
  its optimistic envelope leaves `2^4` relative sign classes; the full
  136-cell graph is connected, so a coefficient-complete written
  differential would force centrality; exact lower bound: four additional
  adjoint/mirror orbits must connect `S`/`im Gamma` to the RS/X core
  (`source-owned-reduction-transport-packet-2026-07-29.md`).
- 2026-07-29/30, the chimeric-BV campaign wrote it: a nine-block first-order
  operator family on all 20 slots (spin Dirac, gamma splitting, twistor,
  divergence, projected RS, inherited W131 block; eight off-core complex
  coefficients) (`full20-chimeric-bv-first-write-2026-07-29.md`); the native
  spinor Krein form is FIXED UP TO SCALE, the induced I/R pairing is
  orthogonal, the nine-block family is adjoint-closed, a FORMAL Green packet
  is built, the S/I multiplicity Gram is NOT forced, global native domain
  OPEN (`full20-native-polarization-closure-wave-2026-07-30.md`, result
  block).
- 2026-07-30, the transport theorem at finite associated-bundle grade:
  Gamma-naturality forces the vector-spinor coflip extension
  `C_VS = (N eta) tensor C_perp`, `N = diag(+1_TX, -1_Sym2)`, fourteen
  relative phases fixed by fourteen intertwining equations; the 136 written
  coefficient intertwiners reject any relative mirror-pair phase (planted
  phase fails 28 intertwiners, residual 2); DeWitt-loop return is uniformly
  `-1` on all twenty slots; doubled loop `+I`; P1/P2 one-bit weld CLOSED at
  that grade; Green/domain and nonlinear completion still open; "No absolute
  formal-adjoint special-edge sum is promoted without the complete
  pairing/Green convention" (`full20-dewitt-loop-transport-wave-2026-07-30.md`).
- 2026-08-04, the absolute bit is typed: the ten-edge common sign is the
  holonomy of a `Z/2` LOCAL SYSTEM on the metric fibre `F = GL(4,R)/O(3,1)`
  with class `w != 0` in `H^1(F;Z/2)` — no global section; only the
  unordered pair `{58,78}` is loop-invariant; the acting `Z/2` is the
  ORTHOCHRONOUS (time-reflection) character, and the spin double cover is
  provably inert (`dc-h1-orbit-signs-monodromy-check-2026-08-04.md`); the
  datum's home is a time-orientation of the Lorentzian base `X^4`,
  fibrewise-nontrivial (not in the image of `H^1(X^4;Z/2)`), and all seven
  real candidates PRESUPPOSE rather than fix it
  (`time-orientation-home-for-the-orientation-datum-2026-08-04.md`).
  Independently re-executed 2026-08-09 with a five-seat hostile review
  (`run-monodromy-frame-charge-preflight-hostile-2026-08-09.md`).
- 2026-08-08, the domain is a moduli, not a bit: at filed symmetry (Krein,
  right-H, deck) on the filed `(832,832)` section Green trace, the
  deck-fixed admissible set is `union_k Gr(k,832)`, maximal real dimension
  346,112; only the two definite sectors are isolated and BOTH are
  admissible; verdict `NO-CANONICAL-SELECTOR`
  (`c1-domain-moduli-result-2026-08-08.md`, answering register M-M23).
  Named there and nowhere earlier: **M-H10's "Bär–Ballmann does this
  generically" does not cover ultrahyperbolic signature**, which is what the
  ambient `Y^14` operator is.
- 2026-08-08, M-H9 (the register's route that would have derived fields
  (i)–(iii) natively from a signature discriminator) was FALSIFIED at Tier 1:
  `C_perp`'s involution character is `+1` on both horns; and its post-mortem
  records that the loop-transport probe had already forced all ten `delta_e`
  EQUAL, so `k in {0,10}` — endpoints only
  (`lab/process/improvement-register-2026-08-03.md:161`).
- The last interior positive control remains the 2026-07-26 imposed-wall
  comparator (`CURRENT-STATE.yaml:55-57`): a standard-field, positive-Hilbert
  wall HOSTS a supplied `N=3` and does not select it; the return is typed
  `IMPOSED_BOUNDARY_HOSTING`, and target-coding is detected
  (`imposed-wall-triplet-comparator-2026-07-26.md`).
- The kill inventory constraining reopening: RB7's saddle kill (isotropic
  Hessian `{-m^2,-m^2,2m^2}`; every generic branch a saddle; W177 vertical
  Euler response structurally zero) stands on exact-derivative evidence
  since 2026-08-03; W177-ambient-YM-nonstationary likewise; RB6's null is
  the remaining FD-band read with no exact recertification
  (`rb7-...-2026-07-30.md` addendum; `source-owned-reduction-transport-
  packet-2026-07-29.md` addendum; `rb6-...-2026-07-30.md:75-76`).

## Per-field scaffolding

Each field gets: what the audit found (lens 1); what "repository-owned"
would mean here; the nearest owned results; the specialty lens (variational
for iii–iv, Clifford slot/phase for i–ii, analytic-domain for v); surplus
accounting where a datum enters (lens 5); and the verdict class —
DERIVATION-ROUTE / TYPED-EXTERNAL-DATUM / UNDER-DETERMINED / DISSOLVES.

### Field (i) — slot-pairing phases

**Audit finding.** The support algebra cannot supply "the phase-normalized
invariant pairing on every observer summand"; declaring it from convenience
"would silently replace the program-native fork"
(`b5-observer-symbol-multiplicity-matrix-2026-07-24.md`, mirror section; the
orbit reduction lists it as unfrozen item 1).

**Ownership would mean.** A construction already in the repository (action,
naturality demand, or written differential) that outputs the normalized
K-pairing per slot with zero per-slot choices, so that the `epsilon_ij` in
the adjoint condition `c_ji = epsilon_ij conj(c_ij)` are computed, not
absorbed.

**Nearest owned results (Clifford slot/phase lens).** Three pieces exist at
stated grades: (1) `NATIVE-SPINOR-KREIN-FORM-FIXED-UP-TO-SCALE` and
`INDUCED-I/R-PAIRING-ORTHOGONAL` (S3 wave) — the spinor-factor pairing is
owned up to one overall scale, and the gamma-trace splitting is orthogonal
for it; (2) the 136 written coefficient intertwiners fix every RELATIVE
mirror-pair phase at finite associated-bundle grade (S4); (3) N2a built two
native charge-conjugation components `(epsilon_C, tau_C) = (-1,+1), (+1,-1)`
with `(C Gamma(alpha))^T = -C Gamma(alpha)` on the bare 128-spinor kernel —
but "the still-missing intertwiner placing these matrices on every
factorized 4+10, full-20 carrier slot" is explicitly absent.

**The genuine residual.** `S/I-MULTIPLICITY-GRAM-NOT-FORCED` (S3): the
pairing normalization ACROSS the three provenance copies (the multiplicity
Gram on the repeated E-type slots) is the one piece of field (i) no owned
structure currently computes. This is exactly where the 07-29 packet's gate
(1) bites: "coefficient normalization from the action rather than a
favorable table."

**Verdict class: DERIVATION-ROUTE, two-stage.** Stage A (Wave One) determined
that the formal-adjoint sign is Gram-dependent. Stage B then tested the
equations actually serialized by S1/S3. They underdetermine the Gram because
quadratic-action symmetry duplicates formal adjointness and linear BV closure
duplicates the coarse kernel equation; exact inequivalent-Gram full-support
witnesses survive with `q=1`. The residual is therefore typed external for the
current action packet. Another normalization attempt must first serialize the
missing four-stage differential/roll maps and expose an independent
nilpotence/acyclicity or nonlinear BV relation. The surplus meter for any
posited Gram still runs through M-M20; until that bridge exists it remains
SURPLUS-UNCOMPUTABLE.

### Field (ii) — coflip linearity and phases

**Audit finding.** "the antilinear fork still has ten unselected phase
invariants and eleven possible real parity-dimension pairs" (run-plan,
result bullet 2); the orbit reduction leaves open whether the physical
coflip is linear or antilinear (unfrozen item 2).

**Ownership status now (Clifford slot/phase lens).** Three-part answer, all
three parts already in the tree:

1. **Linearity: OWNED — antilinear.** `C_perp = K J_obs` is an exact
   antilinear involution with exactly the B5 dual-slot support action and
   contragredient covariance (`sa-y8` §5); its unique Gamma-natural
   vector-spinor extension is `(N eta) tensor C_perp` with the normal
   grading `N` forced (S4). The linear coflip remains a certificate
   comparator, not the physical candidate.
2. **Relative phases: OWNED-EQUAL at finite associated-bundle grade.** The
   136 written coefficient intertwiners force all ten `delta_e` equal (S4;
   independently re-run green 2026-08-04 inside DC-H1). Static support
   admitted `2^10`; the written differential admits 2 absolute assignments
   = 1 relative class.
3. **The absolute bit: DISSOLVES AS A STORED VALUE.** This is the packet's
   highest-value finding and it is already computed, twice: the bit is the
   holonomy of a nontrivial `Z/2` local system on `F` (class `w != 0`,
   `H^1(F;Z/2)`), the acting `Z/2` is the time-orientation character, only
   the unordered pair `{58,78}` is loop-invariant, and "the absolute
   ordered pair is loop-monodromy dependent" (DC-H1; M-H9 post-mortem's
   dc-h1 citation). A field slot that demands ten stored phase VALUES is
   demanding an object the repo has proven does not exist globally. The
   correctly-typed field content is: kind = antilinear; relative phases =
   all-equal (computed); absolute datum = the monodromy class plus, for any
   trivialized computation, one declared basepoint/path convention. The
   external-datum ledger entry this touches is D1 (the joint P1/P2
   orientation), whose home is a time-orientation of `X^4` that everything
   presupposes and nothing fixes.

**Surplus accounting for the residual datum.** Free parameters: one `Z/2`
(one bit). Independent constraints expressible on it today: zero (the 07-29
surplus audit's result, unchanged — no FORCED row shares a certified object
with the phase residual). Surplus: UNCOMPUTABLE, not low; the named bridge
remains SA-C2. A bare posit of the bit is uninformative-as-fit (surplus <= 0
even if one constraint appears) and must be typed as a convention/datum, not
a result.

**Verdict class: PARTIAL-DISSOLUTION + TYPED-EXTERNAL-DATUM.** The cheap
wave (rider on Wave One, or Wave Two): execute DC-H1's reopener 1 — certify
the holonomy consequence ON the packet objects (the even/breaking split as a
non-global function on `F`; the invariant statement is the unordered pair)
— "finite linear algebra on objects that already exist" — and amend the
packet-contract SCHEMA so `coflip_linearity_and_phases` accepts
`{kind: antilinear, relative: all-equal-cited, absolute: monodromy-class
w != 0 + declared trivialization}` instead of ten stored values. The schema
change is a contract edit on a process artifact, not a scientific
disposition; it still fails closed on everything else.

### Field (iii) — formal-adjoint sign

**Audit finding.** Unfrozen item 4 of the orbit reduction ("the
formal-adjoint sign of the actual differential expression"); reaffirmed at
S4: "No absolute formal-adjoint special-edge sum is promoted without the
complete pairing/Green convention."

**Ownership would mean.** One computed sign for the assembled first-order
expression under the formal Krein adjoint — i.e., whether the written B5
differential is K-symmetric or K-skew (blockwise table plus one global
statement), derived from the owned pairing and the owned nine-block family
with no per-block choices.

**Variational lens — what existing structure already forces.** The selected
first-order family is adjoint-CLOSED: "every formal adjoint of the nine
written blocks stays inside the nine-block family" (S3). The spinor K-form
is fixed up to one overall scale, and a formal Green packet exists at
formal first-order-expression grade (S3). The formal-adjoint sign is
scale-invariant (an overall rescaling of K cancels between the two sides of
the adjoint identity), loop-invariant (endpoint transport acts by
conjugation), and can therefore only depend on: the block coefficients, the
I/R-orthogonal structure, and possibly the unfixed S/I Gram. Nothing in the
tree has computed that table. This is the one field that is plausibly
FULLY repo-owned after one bounded exact computation — and the same
integration-by-parts that fixes it emits field (iv)'s Green form as its
boundary defect. The computation is specified exactly in "First decisive
steps."

**Verdict class: DERIVATION-ROUTE (Wave One).** Possible honest outcomes,
all informative: sign forced Gram-independently (field owned); sign
Gram-dependent (field typed external-via-Gram; surplus via M-M20 bridge);
sign scale- or path-dependent (the absolute sign is ill-posed — a
convention artifact; the field dissolves into a relative statement, the
same shape as field (ii)'s dissolution; this outcome must be reported, not
repaired).

### Field (iv) — program-native Green boundary form

**Audit finding.** Unfrozen item 5; the fail-closed contract rejects both a
positive-Hilbert and an unspecified Green-form replacement (run-plan,
result bullet 3; `tests/shiab_b5_native_packet_contract.py` asserts
`green_boundary_form["construction"] == "program_native"`).

**Ownership status.** `FORMAL-GREEN-PACKET-BUILT` (S3) at formal
first-order-expression grade on the native carrier — the nearest owned
object, never carried into the packet certificates. RB3c's Green/Q join
used "a planted abelianized 1D Green/chain-rule fixture" (its own grade
line) — a toy, not the native form. The K77 campaign's moving-density
Green identity and epsilon-Ward/Green-domain gates are for the draft-9.16
/ corrected K77 operator on the other fork — contrast only, per the
pre-flight.

**Variational lens — the bounded derivation.** The program-native Green
form at formal grade is the boundary defect of Wave One's
K-integration-by-parts of the assembled nine-block expression:
`<D u, v>_K - (sign) <u, D v>_K = boundary form`, computed blockwise on
the 20-slot carrier with the fixed-up-to-scale K-form and the I/R-orthogonal
splitting. The wave must (a) verify it against the S3 formal Green packet
(agreement is a control, not a source), and (b) type its grade honestly:
FORMAL (fibrewise/first-order-expression) versus GLOBAL on `Y^14`. The
global grade inherits the field-(v) analytic gap: the ambient operator is
first-order **ultrahyperbolic on a non-compact `(9,5)` manifold**, and the
register's named route (M-H10, "Bär–Ballmann does this generically") has a
recorded scope hole — "Bär–Ballmann does not cover ultrahyperbolic
signature" (`c1-domain-moduli-result-2026-08-08.md`, "What it does not
show"). The packet contract's field can be satisfied at formal grade with
the grade declared; the global form is a separate, harder object.

**Verdict class: DERIVATION-ROUTE (emitted by Wave One at formal grade);
UNDER-DETERMINED at global `Y^14` grade** pending the analytic program in
field (v).

### Field (v) — common closed symmetry-compatible domain

**Audit finding.** Unfrozen item 6; the orbit reduction already recorded
the shape of the truth: "admissible domains can exist after a Green form is
frozen, while ... existence does not canonically select one" (citing the
graph-domain work).

**What the interior program actually needs (analytic-domain lens).** Three
distinct objects hide under "domain"; the executing wave must not let them
merge:

- (v-a) the packet's contract field: ONE common closed symmetry-compatible
  domain on which the assembled differential and its formal adjoint act,
  compatible with `K`, `C_perp`, and the slot structure. Note the contract
  tests only `symmetry_compatible is True` — it demands A domain, not a
  canonical one.
- (v-b) the section-restricted boundary objects, where the repo's exact
  results live: ECW3D-A's balanced `(832,832)` Green trace with BOTH
  maximal-definite sectors admissible and right-H invariant; C1's
  deck-fixed admissible moduli `union_k Gr(k,832)` with no canonical
  selector at filed symmetry; the LP/LC computation's `LC-SELECTOR` verdict
  at the true fibre ends (no canonical J-self-adjoint realization; an
  external `+-i0` orientation bit required), with its 2026-07-22 scope
  correction. The five-lens council's Lens 1: the boundary-triple skeleton
  "exists in all but name," and canonical-selection failure was
  demonstrated three independent times without ever being recorded as a
  closure — until C1.
- (v-c) the ambient `Y^14` closed operator — ultrahyperbolic, non-compact,
  outside the cited BVP literature's hypotheses (the named M-H10 gap), and
  outside every current exact result.

**What the audit's grade assumed vs the weakest sufficient statement.** The
audit demanded the field be "fixed by a repository-owned independent
construction." For (v-a) at filed symmetry the repo has since PROVEN the
canonical version of that demand unsatisfiable: the admissible set is a
positive-dimensional moduli (346,112 at the maximal stratum), and C1's own
reading is that in standard BVP theory this is the EXPECTED outcome —
"self-adjoint extensions are classified by unitaries between deficiency
subspaces and the choice is never canonical." Two weaker grades are
available, and the design recommends the second:

1. **Declared-point grade.** Supply one moduli point (e.g., one definite
   sector) as an explicit convention. Satisfies the contract literally;
   consumes surplus (one declared choice with up to 346,112 continuous
   parameters at the big stratum — any downstream fit through it is
   uninformative unless the choice is priced); honest but weak.
2. **Verdict-stability grade (the weakest sufficient statement).** What the
   downstream B5 gates (mirror obstruction, symbol exactness, physical
   quotient) actually need is not one domain but the INVARIANCE of their
   verdicts over the admissible set. Novelty check: "verdict-stability",
   "moduli-uniform", "stratum-independent" — zero exact and zero
   co-occurrence hits; this reframe appears to be new at the naming level
   (the ingredients are all C1/ECW3D-A). If the gate verdicts are constant
   across `union_k Gr(k,832)` (or constant per stratum with the stratum
   dependence typed), the domain CHOICE drops out of the science and field
   (v) dissolves into a finitely-checkable uniformity statement plus a
   convention. If a gate verdict varies over the moduli, the domain choice
   is physical, and it is then a TYPED-EXTERNAL-DATUM with the moduli as
   its exactly-known parameter space.

**Enlarged-symmetry caveat.** C1 notes a larger supplied symmetry group
could shrink the fixed set; the deck action on trace data is a recorded
missing field, and banking the operator deck action was separately proven
sigma-CIRCULAR at operator grade (portfolio, T2-closed note). So the
enlarged-symmetry escape is not currently available without importing the
very datum at issue. State this; do not chase it silently.

**Surplus accounting.** A declared moduli point: free parameters up to
346,112 (stratum-dependent); expressible independent constraints: none
currently; surplus deeply negative — uninformative as a fit, admissible
only as priced convention. The verdict-stability statement consumes zero
parameters and is therefore the informative object.

**Verdict class: UNDER-DETERMINED as audited (canonical selection provably
fails at filed symmetry, section grade); DERIVATION-ROUTE for the
verdict-stability reframe; the ambient (v-c) object remains OPEN with its
analytic gap named.** Do not import (v-b)'s K77 siblings; do not cite
Bär–Ballmann generically for (v-c).

## First decisive steps (bounded, independently implementable)

**Step 0 — clear the P-H29 gate (not an interior wave).** Complete M-C2 for
RB6: re-derive the "no nonzero source-derived Q resolved" null with the
existing exact-derivative library (`tests/lib/exact_gimmel_derivatives.py`),
producing `tests/channel-swings/verify/rb6_exact_derivative_reverdict.py` on
the pattern of the rb7/w177 re-verdicts. Until this exists, P-H29 forbids
admitting any new interior wave on the strength of the RB-chain nulls. The
route review's F4 independently ranks this "the standing debt with the
widest blast radius if the interior program resumes." Expected outcome by
analogy with RB7/W177 (both kills survived, evidence upgraded): survival —
but that is an expectation, not a result, and the recertification can fire
the other way.

**Wave One — the pairing-table / formal-adjoint-sign wave (field iii, with
field iv emitted and field i's residual sharpened).** The
named-but-unexecuted step from `sa-y8` ("written first-order B5 differential
-> induced 20-slot pairing table + one formal-adjoint sign") and S4's
withheld promotion. Specification:

- **Objects (all existing):** the nine-block first-order family (S1/S3);
  the native spinor K-form fixed up to scale and the I/R-orthogonal
  splitting (S3); the projectors `P_I, P_R`, maps `Gamma, j`; the coflip
  `C_perp` and its Gamma-natural extension (S4); the certified 136-cell
  matrix and 39-orbit structure (`tests/shiab_b5_*.py`).
- **Computation:** (1) blockwise K-adjoint of each of the nine written
  blocks on the 20-slot carrier — the induced 20-slot pairing table; (2)
  the sign table `D_block^[K-adjoint] = s_block D_block'` and whether one
  global sign `s` is consistent for the assembled expression; (3) the
  dependence audit: is `s` invariant under (a) the free overall K-scale,
  (b) an arbitrary invertible S/I multiplicity Gram, (c) DeWitt-loop
  endpoint conjugation; (4) the boundary defect of the same
  integration-by-parts = the formal-grade native Green form, compared
  against the S3 formal Green packet; (5) with `s` in hand, the absolute
  special-edge `delta_e` semantics: confirm the relative all-equal result
  and state the absolute assignment ONLY as (monodromy class + declared
  trivialization), per field (ii).
- **Discipline:** exact arithmetic only — Fraction / Gaussian-integer /
  sympy closed forms, in the house pattern of the DC-H1 and
  time-orientation probes. No finite differences anywhere; nothing for
  P-H29 to object to. Any float appearing in a load-bearing comparison
  voids the run.
- **Planted controls (all must fire or hold as stated):** (a) a planted
  K-scale change leaves every sign invariant; (b) a planted skew
  (sign-flipped) pairing flips the computed signs — proves the computation
  can detect the thing it measures; (c) a planted Gram twist on one
  repeated E-type: record whether any sign moves (this is a measurement,
  not a pass/fail); (d) a planted relative mirror-pair phase must break
  the intertwiner set (reproducing S4's 28-residual rejection); (e) the
  fail-closed packet contract must STILL reject any packet the wave
  assembles until all five fields are jointly frozen — the wave freezes at
  most (iii) and formal (iv).
- **Kill conditions (declared before computation):** K1 — `s` varies with
  the free K-scale: the absolute formal-adjoint sign is ill-posed;
  report DISSOLVED-AS-CONVENTION; do not normalize it away silently. K2 —
  `s` depends on the S/I Gram: field (iii) is EXTERNAL-VIA-GRAM; route
  through the M-M20 bridge and the surplus meter; do not pick the
  favorable Gram. K3 — the assembled expression's adjoint leaves the
  nine-block family (contradicting S3's family closure at coefficient
  level): stop and report; S3's closure was then support-grade only, and
  that correction outranks this packet's design. K4 — the emitted Green
  form disagrees with the S3 formal packet: one of the two is wrong;
  adjudicate before any use.
- **Unblocking power if `s` is forced:** fields (iii) and formal-(iv)
  become repo-owned; the eleven-pair residual's semantics reduce to the
  monodromy statement (field ii, certify-on-packet rider); the packet's
  remaining open matter includes field (i)'s Gram owner and field (v)'s
  stability program. The completed Stage-B owner test shows the current
  quadratic equations do not close the first item; an action-owned four-stage
  differential/roll packet is required before retrying it.

**Wave Two (field ii rider) and Wave Three (field v stability)** are
specified in their field sections; neither may start before Wave One
reports, because both consume its outputs (the sign semantics; the frozen
formal Green form).

## Outcome table (graded; every disposition wave-owned)

| outcome | condition | packet consequence (proposed grade) |
|---|---|---|
| (a) sign forced, Gram-independent | Wave One (1)-(4) green; controls (a)-(e) as stated | fields (iii) + (iv-formal) fixed by repo-owned construction; contract fields fillable at declared grade; LANE-STATE lane-1 findings sentence due a refresh; proceed to Wave Two/Three |
| (b) sign Gram-dependent | K2 fires | field (iii) typed EXTERNAL-VIA-GRAM; completed Stage-B owner test shows current equations underdetermine it; four-stage differential/roll owner next; surplus via M-M20 bridge; no sign selected |
| (c) sign scale/path-dependent | K1 fires | absolute sign DISSOLVES-AS-CONVENTION; packet contract schema amended (relative statement + declared convention), mirroring field (ii)'s type; a finding, not a failure |
| (d) family closure breaks at coefficient level | K3 fires | S3 result corrected first; this packet's field-(iii) route re-derived after; escalate per finder-escalates |
| (e) RB6 recertification flips the null | Step 0 | the RB-chain kill inventory changes; the interior reopening design is re-priced before any wave runs |
| (f) verdict-stability holds over the moduli | Wave Three | field (v) dissolves into uniformity-plus-convention; the audit's strongest field becomes the cheapest |
| (g) verdict-stability fails | Wave Three | the domain choice is physical: TYPED-EXTERNAL-DATUM with parameter space `union_k Gr(k,832)` exactly known; surplus accounting mandatory on any use |

## Why the program parked here (cited)

- The recorded reason: BLOCKED at the five-field packet (2026-07-29 audit)
  plus the RB7 kill, with the new acceptance rules holding the door shut —
  "do not cite an FD-read null or kill until it is certified ... (P-H29)"
  (`CURRENT-STATE.yaml:19-24`); "no interior claim was manufactured to keep
  the lane green" (`CURRENT-STATE.yaml:52-54`); recommendation on record:
  build the packet via M-H10 and recertify RB6/RB7/W177 (M-C2) "before
  admitting any new interior wave (P-H29)" (`CURRENT-STATE.yaml:62-65`).
- The structural reason: the wave-scheduling rule (P-H28) makes prerequisite
  fields batch rather than spawn runs, and the cadence's queue is
  re-authored every wave from the latest gate — so when the frontier moved
  to the K77 operator-anomaly big swing (one wave per hour for two days,
  v0.163–v0.174), no queue rank ever came back to the parked interior
  (`campaign-altitude-route-review-2026-08-11.md`, F1's mechanism, F3's
  observation that LANE-STATE still narrates the 2026-08-03 B5/RB7 picture).
- The route reason: the register's two named B5-adjacent resolvers went
  dead or gapped — M-H9 FALSIFIED at Tier 1 (2026-08-08), and M-H10's
  literature premise has a recorded ultrahyperbolic hole (C1) — leaving the
  five-field wall with no live named route until this packet's Wave One.
- The debt reason: M-C2 half-done (RB7/W177 recertified 2026-08-03; RB6
  not), so P-H29 blocks the reopening even where design exists (route
  review F4).

## Prior art

**In-repo (enumerated; this packet claims none of it):** the B.5 scope and
feasibility ("yes for the build; no for the escape from ambiguity",
`b5-independent-reconstruction-scope-2026-07-21.md`); the symbol-class
enumeration contract (07-23); the multiplicity matrix (07-24); the orbit
reduction (07-25); the fail-closed contract (07-25); the triplet stress
(07-26); the imposed-wall comparator (07-26); the audit (07-29); the
phase-sum, chirality-orientation, and surplus audits (07-29); the
vertical–Krein weld (07-29); the reduction/transport Layer-0 packet with
the four-orbit bound (07-29); the chimeric-BV S1–S4 waves (07-29/30); RB6
and RB7 (07-30); DC-H1 and the time-orientation relocation (08-04); the C1
domain moduli (08-08); the five-lens council (08-08); M-H9's falsification
(08-08); the monodromy run receipt (08-09); the route review and the
sibling SIGNATURE-AMBIENT design packet (08-11). What is new here relative
to those, per the novelty checks: the per-field TREE-STATE regrade of the
audit's five-field list; the Wave-One specification (the step itself was
named in `sa-y8:358`); the verdict-stability reframe of field (v) at naming
level; and the Step-0 sequencing of RB6 under P-H29.

**Literature (flagged for the wave's own independent check; nothing here is
verified against the sources):** Bär–Ballmann boundary value problems
(arXiv:1101.1196), non-self-adjoint extensions (1906.08581), non-compact
boundary (2401.17784) — all three carried zero repo cites when M-H10 was
filed (register row 162), and the C1 caveat that this machinery does not
cover ultrahyperbolic signature must be checked against the sources before
any wave cites them either way; DeWitt 1967 for the supermetric trace-sector
flip (already flagged in the sibling packet); standard Krein-space extension
theory (deficiency/unitary classification) as the background for C1's
"never canonical" reading. Confirm citations independently before writing
them into any disposition.

## What this packet does not do

No registry row, fork disposition, verdict, residue, claim status, canon,
packet field, LANE-STATE edit, contract-schema edit, banner (the owed Q1a
banner is noted, not applied), priority, or posture changes. It runs no
probe and certifies nothing. It supplies design, per-field regrade with
citations, a bounded Wave-One specification with controls and kill
conditions, and an outcome-graded sequencing. The executing waves own every
disposition under the full pre-flight / hostile-review contract; anything
approaching canon goes through the two-phase rule. External review is not
proposed anywhere in this packet.

## Verify status manifest

- HEAD pin, branch, and every file/line citation above: **CONFIRMED** —
  each cited file was opened at revision `bdd2c93...` during drafting; the
  LANE-STATE quotes, the audit result text, the register rows (75, 145,
  161-162), the contract test's five REQUIRED keys and its
  linear/antilinear + program_native + symmetry_compatible asserts, and the
  RB6 no-reverdict state (`tests/channel-swings/verify/` contents) were
  read directly.
- Per-field TREE-STATE regrades (i, ii, iv, v): **CONFIRMED as citations**
  of the named artifacts at their own stated grades; **SCOPED as a
  synthesis** — the joint reading "mostly absorption-plus-one-computation"
  is this packet's judgment, and the fail-closed certificates' continued
  rejection of every packet is the controlling unconditional repo state.
- Field (ii) dissolution and field (v) no-canonical-selector: **CONFIRMED**
  as existing computed results (DC-H1, time-orientation, C1); the
  contract-schema amendment they motivate is **PROPOSED**.
- Wave One's claim that the adjoint sign is scale- and loop-invariant:
  **SCOPED** — an elementary argument stated here, deliberately assigned to
  the wave to verify or refute as its first check (K1's subject), not
  assumed by the design.
- Step 0 (RB6 debt) and the P-H29 gating: **CONFIRMED** (absence of an rb6
  re-verdict artifact; register M-C2 text; AGENTS.md:44) — with the
  expectation of survival explicitly **PROPOSED**, not predicted as fact.
- Sequencing, outcome table, and all "packet consequence" columns:
  **PROPOSED**; the cadence adopts, amends, or declines per the absorption
  protocol.

## Self-hostile review

Filed separately per repository convention: `lab/process/hostile-reviews/2026-08-11-b5-five-field-packet-design-review.md` (the three standing charges, same-run).
