---
artifact_type: exploration
label: W224
status: exploration
created: 2026-07-14
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (a failed falsification honestly reported is as valuable as a kill); RUTHLESS skeptic; NON-NAIVE falsification (grant every unbuilt piece); tri-repo gating STRICT; bar(b)/H59 stay OPEN; do NOT fix the reservoir Krein sign (Godel-independent, W211)
title: "W224 FALSIFICATION probe (NON-NAIVE) of the sharpest INTERNAL falsifier of GU: the NIELSEN-NINOMIYA chirality no-go. GRANTING the mirror-gapping record condensate (W216), can GU's GLOBALLY VECTORLIKE Dirac operator (16 + 16bar, the Sp(64) pseudoreal zero, W222/W218) yield an on-shell CHIRAL, anomaly-consistent, LOCAL single SM generation, or does it PROVABLY hit NN doubling (a surviving mirror) without breaking a symmetry / locality GU requires? VERDICT: SURVIVES-BY-SMG (symmetric mass generation / mirror-gapping). The entire NN falsification reduces to ONE checkable ledger -- are ALL 't Hooft anomalies of the would-be-gapped mirror 16bar zero? -- because Eichten-Preskill / Wang-Wen SMG can gap a vectorlike mirror while keeping the light sector chiral IFF the mirror is 't Hooft anomaly-free (perturbative cubic + Witten SU(2) mod-2 + the discrete mod-16 cobordism anomaly of Omega^Spin_5(B G_SM), Garcia-Etxebarria & Montero / Wang & Wen). GU's carrier delivers the FULL SO(10) 16 (W222), whose CP-conjugate mirror 16bar has ALL these anomalies vanishing -- perturbative (no Spin(10) cubic Casimir), Witten (4 doublets, even), AND mod-16 (16 = 0 mod 16, the nu_R / SO(10) completion is exactly what makes it vanish). So the SMG NECESSARY condition is MET: the mirror is symmetrically gappable, leaving a chiral, anomaly-consistent, LOCAL 16, and the gapping breaks NO gauge symmetry (gauge-singlet condensate) and NO locality -- only the GLOBAL record-count U(1) (W166 arrow), which GU welcomes. Pre-declared FAILURE CONDITION -- (a) a FORCED surviving mirror, or (b) breaking a GU-required symmetry/locality -- is NOT triggered. The falsifier has genuine TEETH: removing the 16th state (nu_R) gives a 15-mirror with mod-16 = 15 != 0, SMG is obstructed, NN doubling survives, and the verdict FLIPS to FALSIFIED. LOAD-BEARING CAVEAT: the necessary (anomaly) condition is CHECKED and real; the SUFFICIENT (dynamical) condition -- that the symmetric strong-coupling GAPPED phase is realized, not a symmetry-breaking/gapless one -- is the GRANTED condensate (unproven in 3+1D for the full SM). Test tests/W224_falsify_nielsen_ninomiya_chirality.py exit 0, positive controls first."
grade: "exploration / strong-negative (a FAILED falsification, honestly reported). EXACT / machine-verified (tests/W224_falsify_nielsen_ninomiya_chirality.py, exit 0, positive controls first): NN FIRES on a naive local translation-invariant lattice fermion (1D doubling, +1 at k=0 and -1 at k=pi, net 0); NN realized INSIDE GU -- an exactly cross-chirality operator D=[[0,B],[B',0]] with {D,omega}=0 (to 0e0) has net chiral index tr(omega sign(D)) = 0 (to 1e-15), and a GW/overlap operator BREAKS that anticommutation (||{D_GW,omega}|| ~ 19.5), so GU's exact-anticommuting D is NOT of Ginsparg-Wilson type; the mirror 16bar perturbative anomalies (U(1)^3, grav, SU(2)^2-U(1), SU(3)^3) all vanish as exact rationals; the Witten SU(2) mod-2 anomaly is absent (4 doublets); the mod-16 count = 16 mod 16 = 0; and the falsification-teeth control (drop nu_R -> 15) gives perturbative-still-zero but mod-16 = 15 != 0, correctly FLIPPING the verdict to FALSIFIED. STANDARD-FIELD / reconstruction grade (flagged, NOT re-derived here): the SMG-iff-anomaly-free theorem (Eichten-Preskill 1986; Wang-Wen 2018-2020; the modern symmetric-mass-generation literature); the mod-16 cobordism anomaly value Omega^Spin_5(B G_SM) = Z/16 and its vanishing at 16 Weyl fermions (Garcia-Etxebarria & Montero 2018) -- applied to GU's content, NOT a fresh Dai-Freed computation (same honesty boundary as W222); the reality/rep-theory anomaly-freedom of the Spin(10) 16 (standard GUT physics). STRUCTURAL / GRANTED per the non-naive method: the existence and good-branch real/bounded spectrum of the mirror-gapping condensate (W216), i.e. the SMG SUFFICIENT (dynamical) condition; the selecting Krein sign (Godel-independent, W211). No canon / RESEARCH-STATUS / claim-status / verdict / posture change; bar(b) and H59 stay OPEN; the count stays {1,3}; no forbidden target inserted. Zero em dashes."
construction: "program-native where the objects are GU's (the globally VECTORLIKE 16+16bar carrier, Sp(64)-pseudoreal, W222/W218; the exact cross-chirality {R_src, omega}=0 / purely-off-diagonal Krein structure, W218 / swing / H2; the mirror as 96 hyperbolic null pairs in ker(Gamma) of Cl(9,5)=M(64,H), W173; the mirror-gapping record condensate on the good branch, W216; the SO(10) 16 hypercharge/anomaly content, W222). Standard-field where the machinery binds any construction and is PORTED and CITED (the Nielsen-Ninomiya doubling theorem; the Ginsparg-Wilson / overlap modified-chirality relation; Kaplan domain-wall localization; the Eichten-Preskill mirror-fermion program and its modern SMG resolution, Wang-Wen; the 't Hooft anomaly matching and the mod-16 cobordism anomaly of Garcia-Etxebarria & Montero). Every analogy labelled; none asserted of GU beyond its cited source. The reservoir Krein sign / branch selector stays a GATED temporal-issuance / time-as-finality object (one-way rule respected; no cross-repo identity asserted)."
depends_on:
  - explorations/W222-falsify-sm-emergence-anomaly-hypercharge-2026-07-14.md
  - explorations/W218-lean-Rsrc-unification-check-2026-07-14.md
  - explorations/W216-true-vacuum-spectral-condensate-2026-07-14.md
  - explorations/W177-build-connection-curvature-c2-2026-07-14.md
  - canon/no-go-class-relative-map.md
  - canon/swing-ghost-parity-no-chiral-selection.md
  - canon/h2-base-index-chirality.md
scripts:
  - tests/W224_falsify_nielsen_ninomiya_chirality.py
external_refs:
  - "Nielsen & Ninomiya, Nucl. Phys. B185 (1981) 20; B193 (1981) 173 -- the lattice fermion doubling / chirality no-go"
  - "Ginsparg & Wilson, Phys. Rev. D25 (1982) 2649; Neuberger, Phys. Lett. B417 (1998) 141; Luscher, Phys. Lett. B428 (1998) 342 -- overlap / Ginsparg-Wilson modified chiral symmetry (the GW evasion)"
  - "Kaplan, Phys. Lett. B288 (1992) 342 -- domain-wall chiral fermions (the domain-wall evasion)"
  - "Eichten & Preskill, Nucl. Phys. B268 (1986) 179 -- mirror-fermion / interacting lattice chiral gauge program (the SMG progenitor)"
  - "Wang & Wen, Phys. Rev. Res. 2 (2020) 023356 and arXiv:1809.11171 -- solution to the 3+1D chiral fermion problem via symmetric mass generation; the 16 vs 15 mod-16 obstruction"
  - "Garcia-Etxebarria & Montero, JHEP 08 (2019) 003, arXiv:1808.00009 -- global/cobordism anomalies of the Standard Model, Omega^Spin_5(B G_SM) and the Z/16"
  - "Razamat & Tong, Phys. Rev. X11 (2021) 011063 -- gapping mirror fermions and SMG for anomaly-free chiral matter"
cross_repo:
  - "temporal-issuance + time-as-finality: the good-branch (sensible, real-spectrum) realization of the mirror-gapping condensate that SMG requires is exactly W216's operative-C good branch, selected by the reservoir Krein sign that W211 proved Godel-independent of GU's structure. W224 GRANTS that branch per the non-naive method (it is the SMG sufficient/dynamical condition) and does NOT fix the sign. One-way rule respected; no GU claim moves, no TI/TaF claim moves, no identity asserted."
---

# W224 -- Falsification probe: the Nielsen-Ninomiya chirality no-go against GU

Test / falsification certificate: `tests/W224_falsify_nielsen_ninomiya_chirality.py` (exit 0,
positive controls first). Five personas ran INLINE in one worker (lattice / Nielsen-Ninomiya
specialist; index-theory / overlap-Ginsparg-Wilson specialist; domain-wall / Y14-projection
specialist; symmetric-mass-generation / Eichten-Preskill / 't-Hooft-anomaly specialist; ruthless
skeptic); no sub-agents. Zero em dashes. Builds on W222, W218, W216, W177.

## 0. The leg and why it is the sharpest remaining internal falsifier

Three independent lines this session converge on ONE object, GU's fermion sector being globally
VECTORLIKE and chiralizing only through the mirror-gapping record condensate:

- **W222 (SM emergence).** GU's carrier delivers a VECTORLIKE `16 + 16bar` (the trivial
  `Sp(64)`-pseudoreal zero); the observed SM chirality is produced ONLY by the unbuilt condensate.
- **W218 (lean check).** The source re-grading `R_src` is K-null / index-conserving / vectorlike:
  `{R_src, omega} = 0` exactly, so `tr(omega sign(R_src)) = 0` is FORCED (core-theorems Theorem 3).
- **W216 (true vacuum).** That condensate is built as a BCS / spectral gap of the mirror null pairs,
  a fading de Sitter attractor on the good branch.

So GU's fermion operator is a local, translation-invariant, (Krein-)self-adjoint, globally
vectorlike Dirac operator, and the observed single chiral generation must be manufactured by
condensing the mirror. That is EXACTLY the situation the Nielsen-Ninomiya theorem governs. NN
(Nucl. Phys. B185 (1981) 20): a local, translation-invariant, hermitian lattice Dirac operator with
an on-site chiral symmetry has an equal number of left- and right-handed modes -- net chirality 0,
a compensating mirror is unavoidable. If GU cannot remove the mirror without breaking a symmetry or
locality it requires, GU is falsified on chirality even granting the condensate.

### Pre-declared FAILURE CONDITION

GU is **FALSIFIED on this leg** iff its fermion operator PROVABLY CANNOT produce a single chiral SM
generation without either

- **(a)** a surviving **mirror generation** (NN doubling, contradicting observation), OR
- **(b)** breaking a **symmetry / locality** that GU structurally requires.

GU **SURVIVES** iff there is an ADMISSIBLE evasion by which GU chiralizes, granting the condensate.

The method is strict NON-NAIVE: ASSUME GU is correct and GRANT the mirror-gapping condensate exists
and works (its existence and the good-branch real/bounded spectrum, W216; the selecting Krein sign,
Godel-independent, W211). "The condensate is unbuilt / dynamically ungated" is a GAP, GRANTED, NOT a
falsification. Only a PROVABLE obstruction that survives EVEN granting the condensate counts.

## 1. Persona 1 -- lattice / Nielsen-Ninomiya specialist: NN FIRES, on the naive operator and inside GU

NN is not evaded by fiat. First, positive control that it has power: the 1D naive lattice fermion
`s(k) = sin(k)` has two zero modes (doublers) at `k = 0` and `k = pi` with OPPOSITE chirality
(`sign(cos 0) = +1`, `sign(cos pi) = -1`), net `0` (CONTROL A). The doubler is real.

Second, and decisively, NN FIRES on GU's OWN operator. GU's built Dirac / source operator is exactly
cross-chirality: `D = [[0, B],[B', 0]]`, `{D, omega} = 0` (W218: `8e-15`; the swing / H2 canon: the
Krein form is purely cross-chirality `||K(+,+)|| = ||K(-,-)|| ~ 1e-14`). For any such operator
`tr(omega sign(D)) = 0` is FORCED, not measured (CONTROL B: `{D,omega} = 0` to `0e0`,
`tr(omega sign(D)) = -1.1e-15`). This is the Nielsen-Ninomiya statement realized INSIDE GU: the naive
/ kinematic GU operator has net chirality zero and a full mirror. The doubling is not hypothetical for
GU; it is a theorem on GU's own carrier (H2, swing, W218 all agree). So the question is strictly
whether the CONDENSATE (a non-kinematic, interacting deformation) removes the mirror admissibly.

## 2. Persona 2 -- overlap / Ginsparg-Wilson specialist: GU's operator is NOT the GW evasion

The cleanest NN evasion (Neuberger; Luscher) keeps a net index by realizing a MODIFIED chiral
symmetry: the overlap operator satisfies the Ginsparg-Wilson relation `{D, gamma5} = a D gamma5 D`,
whose right side is a chirality-DIAGONAL `O(a)` term. That term is exactly what lets `index =
tr(gamma5 (1 - a D/2)) != 0`. GU's operator has NO such term: `{D, omega} = 0` EXACTLY (W218,
`8e-15`), so there is no GW deformation and the index is pinned at `0`. A genuine GW/overlap operator
BREAKS the exact anticommutation (CONTROL B: `||{D_GW, omega}|| ~ 19.5`). **Conclusion: GU's
exact-anticommuting Dirac operator sits on the NN-DOUBLED side of the Ginsparg-Wilson dichotomy, not
the overlap side. The GW/overlap route does NOT rescue GU; if anything it confirms the doubling.** GU
does not chiralize by a modified chiral symmetry.

## 3. Persona 3 -- domain-wall / Y14-projection specialist: the fiber is not a chiralizing domain wall

Kaplan's domain-wall evasion localizes a chiral zero mode where a mass profile crosses zero, exiling
the mirror to a distant wall. Two candidate walls in GU:

- **The geometric `Y14 -> X4` fiber.** `Y14 = Met(X4)` has non-compact fiber `GL(4,R)/O(3,1)`. Could
  the section `s: X4 -> Y14` be a domain wall localizing a net chiral mode? The repo's own result says
  NO: `canon/no-go-class-relative-map.md` (2.1) records that the generation-count analytic mechanism
  via the non-compact fiber is CLOSED as FAILED (rank-3 Atiyah-Schmid empty; the BC1 Jacobi route a
  GENUINE_OBSTRUCTION), and `canon/h2-base-index-chirality.md` (C-05/step9) that ALL metric `so(9,5)`
  connections give a particle-hole-symmetric spectrum, `sig = 0`, over the fiber whose `H^2(RP^3) =
  Z_2` is 3-free. The fiber carries NO net chiral index by itself. So the geometric projection is not
  a chiralizing domain wall.
- **The condensate interface.** The record condensate (W216) does supply a position-dependent mass in
  the conformal / record-count mode (the tachyon `m0^2 = -1/4` gapped to `+Delta*^2`). A domain wall in
  THAT profile is a genuine wall, but it is the condensate, not the geometry. So the domain-wall route
  collapses into the SMG / condensate reading of Persona 4; it is not an independent geometric evasion.

**Net: domain-wall-via-fiber is NOT supported (fiber index empty, by the repo's own canon);
domain-wall-via-condensate is the SMG route, handled below.**

## 4. Persona 4 -- SMG / Eichten-Preskill / 't-Hooft-anomaly specialist: the one operative evasion

The remaining route is symmetric mass generation (SMG): gap the vectorlike MIRROR by strong dynamics
while keeping the LIGHT sector chiral, without a bilinear Dirac mass (which would pair physical to
mirror and remove BOTH). Eichten-Preskill (1986) first attempted this; the naive attempt famously
FAILED (the mirror stayed light, or the gauge symmetry broke). The modern resolution
(Wang-Wen 2018-2020; Razamat-Tong; the SMG literature) is sharp and gives the operative criterion:

> A vectorlike mirror can be gapped symmetrically (light sector left chiral) IF AND ONLY IF ALL of
> its 't Hooft anomalies vanish -- perturbative cubic, the Witten SU(2) mod-2, AND the discrete /
> global (cobordism) anomalies, in particular the mod-16 anomaly `Omega^Spin_5(B G_SM) = Z/16`
> (Garcia-Etxebarria & Montero 2018). If any mirror anomaly is nonzero, the mirror CANNOT be
> symmetrically gapped: the doubler survives, or a symmetry must break.

This turns the ENTIRE Nielsen-Ninomiya falsification into ONE checkable ledger on GU's mirror `16bar`
(the CP conjugate of the physical `16`, triality and `Y` flipped). The ledger (test, printed):

- **Perturbative cubic:** `U(1)^3 = grav = SU(2)^2-U(1) = SU(3)^3 = 0` (exact rationals). Real
  `Spin(10)` rep theory -- the `16` has no cubic Casimir, so the mirror inherits the exact zero
  (W222). Power control: a lone chiral `3` reads `SU(3)^3 = 1`, `U(1)^3 = 3`.
- **Witten SU(2) mod-2:** 4 `SU(2)_L` doublets in the mirror, even -- absent.
- **Discrete mod-16 (cobordism):** the mirror has `#Weyl = 16`, and `16 mod 16 = 0` -- the Z/16
  anomaly vanishes. This is the load-bearing datum, and it vanishes PRECISELY because GU's carrier
  delivers the FULL `16` including `nu_R` (the SO(10) completion, W222's 16th state).

**All mirror anomalies vanish. The SMG NECESSARY condition is MET: the mirror is symmetrically
gappable.** Wang-Wen's celebrated statement is exactly this: one SM generation with 16 Weyl fermions
(the SO(10) `16`, including `nu_R`) admits SMG mirror-gapping because it is anomaly-free including the
mod-16 anomaly; with 15 (no `nu_R`) it does NOT. GU delivers the `16`, so it lands on the gappable
side. And in W216's language this is exactly the good-branch condensate: the mirror null pairs gap to
`+/- sqrt(xi^2 + Delta*^2)`, real and bounded, leaving the physical chiral `16` gapless.

### The falsifier's TEETH (this is where it could have died)

Remove `nu_R` (the 16th state): the mirror is now the 15 (SU(5) `5bar + 10` conjugate). The
perturbative anomalies STILL vanish (SU(5) is perturbatively anomaly-free), but `#Weyl = 15` and
`15 mod 16 = 15 != 0`: the mod-16 anomaly is NONZERO, SMG is OBSTRUCTED, the doubler CANNOT be
symmetrically gapped, and pre-declared **FAILURE CONDITION (a) WOULD trigger**. The test prints this
flip. So the probe is not toothless: it has a specific, physical way to falsify GU, and GU escapes it
by exactly one datum -- delivering the full `16`, not the `15`. The `nu_R` / SO(10) completion is
load-bearing on the NN leg, not just the anomaly leg.

## 5. Persona 5 -- ruthless skeptic: is this a real survival, and does the Krein / non-compact structure change NN's premises?

**Steelman the kill.** (a) The whole survival rides on granting the condensate, which is unbuilt --
so nothing was shown. (b) SMG's mirror-gapping is only PROVEN as a necessary (anomaly) condition; the
SUFFICIENT dynamical condition (that the symmetric GAPPED phase, not a symmetry-breaking or gapless
phase, is realized) is unproven in 3+1D for the full SM -- Eichten-Preskill's original attempt failed
on exactly this. (c) The Krein / `(9,5)` non-compact structure might change NN's premises
(hermiticity, compactness, locality) enough that the whole framing is wrong.

**Answers, and precisely how much the probe earns.** (a) is the METHOD, not a defect: the non-naive
mandate grants the condensate; the probe's job is to test whether, GRANTING it, an admissible chiral
outcome exists, and it does. The probe does NOT claim GU builds the condensate. (b) is conceded and is
the honest residual: the NECESSARY (anomaly) condition is CHECKED and real (and is the non-trivial
part -- it is what fails for 15 fermions); the SUFFICIENT (dynamical) condition is the granted
condensate. The probe REDUCES the entire NN falsification to that one dynamical assumption the method
already grants, and confirms the one non-dynamical obstruction (anomaly) is absent. That is a
SURVIVES, not a vindication. (c) is the sharpest point and cuts BOTH ways:

- **Hermiticity.** GU's `D` is K-self-adjoint (Krein), not Hilbert-hermitian. But on the good branch
  (C operative, W216) the spectrum is REAL, so `D` is hermitian in the physical C-inner-product, and
  NN's hermiticity premise HOLDS there -- the doubling applies, which is why the mirror is real and
  must be gapped. On the pathological branch the spectrum is complex (non-physical). So the Krein
  structure does NOT cleanly evade NN: on the physical branch it reduces to the hermitian case.
- **Non-compactness.** `Y14` is non-compact, so the index is APS / L^2 / Fredholm, not the compact
  Atiyah-Singer index NN assumes over a compact Brillouin zone. This is the ONE genuinely modified
  premise. But the repo already found (H2, C-05/step9) that all metric `so(9,5)` connections give
  `sig = 0` on the non-compact fiber -- the non-compactness supplies NO net chirality either. So it
  does not help GU chiralize; it only relocates where the (still-zero) index is computed.
- **Locality.** The SMG condensate is a LOCAL four-fermion / record condensate (W216 BCS pairing of
  the `ker(Gamma)` null pairs); no non-locality is introduced.

**Skeptic verdict: a GENUINE, honestly-bounded SURVIVAL. The necessary anomaly condition is real and
checked (with teeth: 15 fermions falsifies); the sufficient dynamical condition is the granted
condensate; the Krein / non-compact structure does not evade NN but also does not add a doubler beyond
the one the anomaly-free mirror can absorb by SMG.**

## 6. Admissibility -- does gapping the mirror break a symmetry / locality GU requires?

Failure condition (b) asks whether the ONLY way to remove the mirror breaks something GU needs. It
does not:

- **Gauge symmetry.** The SMG condensate is a `G_SM` GAUGE SINGLET: it supplies no bilinear Dirac mass
  linking gauge-charged physical and mirror states (that would be the naive vectorlike mass that
  removes both). It gaps the mirror via symmetric multi-fermion dynamics, leaving the light `16`'s
  gauge anomalies intact and zero (test: light sector is the chiral, anomaly-free `16`). No gauge
  symmetry is broken.
- **Locality.** The condensate is local (W216).
- **The one thing broken is a GLOBAL symmetry GU WELCOMES.** The condensate spontaneously breaks the
  global record-count `U(1)` (W216), whose Goldstone is W166's arrow-of-time clock. That is a global,
  not gauge, breaking, and it is a FEATURE (the arrow), not a symmetry GU requires to keep. Breaking a
  global symmetry to gap a vectorlike mirror is exactly what SMG is.

So (b) is NOT triggered.

## 7. Verdict

**SURVIVES-BY-SMG (symmetric mass generation / mirror-gapping condensate).** Granting the condensate,
the pre-declared FAILURE CONDITION is NOT triggered:

- **(a)** No mirror is FORCED to survive. GU's operator does hit Nielsen-Ninomiya doubling as built
  (net chiral index 0, exactly cross-chirality, confirmed on GU's own carrier and NOT rescued by
  Ginsparg-Wilson or by the geometric fiber), BUT the would-be-gapped mirror `16bar` is 't Hooft
  ANOMALY-FREE -- perturbative (no `Spin(10)` cubic Casimir), Witten SU(2) (even), and the discrete
  mod-16 cobordism anomaly (`16 = 0 mod 16`, the `nu_R` / SO(10) completion) -- so SMG can gap it
  symmetrically, leaving a chiral, anomaly-consistent, LOCAL single `16`.
- **(b)** No GU-required symmetry or locality is broken: the condensate is a gauge singlet, local, and
  breaks only the global record-count `U(1)` (the W166 arrow), which GU welcomes.

This is an honest FAILED falsification: the sharpest internal falsifier (NN doubling) does not fall
GU, because GU delivers exactly the anomaly content (the full SO(10) `16`) for which symmetric mass
generation is admissible. The probe's contribution is to REDUCE the entire NN chirality question to a
single checkable ledger (all mirror anomalies zero) and to show that ledger is satisfied precisely
because of W222's `16`, with the `nu_R` state load-bearing.

**Load-bearing caveat (not a kill, per method).** The result is CONDITIONAL: the NECESSARY (anomaly)
condition is CHECKED and real, but the SUFFICIENT (dynamical) condition of SMG -- that the symmetric
strong-coupling GAPPED phase is actually realized, rather than a competing symmetry-breaking or
gapless phase (the very failure mode of the original Eichten-Preskill mirror program) -- is the
GRANTED condensate (unproven in 3+1D for the full SM, an active lattice question). The genuine open
falsification RISK on chirality is therefore no longer the anomaly ledger (settled and real, with
teeth) but whether GU's dynamics DELIVERS the symmetric gapped phase, i.e. W216's good branch -- the
same one Godel-independent Krein-sign bit (W211) that the whole arc reduces to. Any future non-naive
attack on GU's chirality should target that dynamical realization, not the anomaly arithmetic.

**On bar(b) and H59: UNCHANGED / OPEN.** No canon / verdict / posture flip; the count stays `{1,3}`;
no forbidden target inserted.

**Artifacts:** this file + `tests/W224_falsify_nielsen_ninomiya_chirality.py` (exit 0; positive
controls first).

*Filed 2026-07-14. NON-NAIVE FALSIFICATION probe. Five personas inline in one worker (lattice /
Nielsen-Ninomiya; overlap / Ginsparg-Wilson; domain-wall / Y14-projection; symmetric-mass-generation
/ Eichten-Preskill / 't-Hooft-anomaly; ruthless skeptic); no sub-agents. Reproducible:
`python -u tests/W224_falsify_nielsen_ninomiya_chirality.py` (exit 0). Exploration grade; conditional
register; RUTHLESS skeptic; no canon movement; bar(b)/H59 OPEN. Zero em dashes.*
