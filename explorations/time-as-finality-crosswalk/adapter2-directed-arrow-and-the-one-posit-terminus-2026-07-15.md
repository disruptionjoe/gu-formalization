---
title: "SUPERSEDED: directed-arrow adapter and one-posit terminus"
status: exploration
doc_type: superseded-cross-repo-adapter-contract
created: 2026-07-15
superseded_by: explorations/time-as-finality-crosswalk/adapter2-correction-polarity-fiber-not-axis-2026-07-15.md
verdict: "ADAPTER #2 NOT PROVED; MONOTONE FORGETFUL FUNCTOR ONLY"
---

# Adapter #2 corrected status

The earlier version of this note claimed that TI `Ext_S` and TaF T18 had been
identified as one directed object and that the remaining `Z/2` was the polarity
of that axis. Correction ADAPTER2-01 withdraws both statements.

The shared-DAG script establishes only that an acyclic directed graph and its
reverse are both acyclic. Direction enters after T18's D1-monotone admissibility
rule is imposed. TaF itself grades the physical reading of that rule as
conditional and weakened.

The attempted hardening produces a valid monotone cardinality functor, but it
maps both source fork states to the same target profile. A field-valued
enrichment preserves their proposition values as extra data, not as finality
direction.

Current result:

- `SURVIVES`: a common acyclic-monotone toy shape and a monotone forgetful
  profile functor;
- `FAILS`: branch preservation in the profile target;
- `OPEN`: a native cross-repo functor and any identification of its fiber with
  the GU Krein sign;
- `NOT EARNED`: the one-posit terminus and the two-adapter closure.

See `adapter2-correction-polarity-fiber-not-axis-2026-07-15.md` and run
`adapter2_repair_audit.py`.
