# Swing KILLED: generation count 3 = consensus fault-tolerance 2f+1 is numerology

2026-07-10, adversarial swing (build -> hostile-kill -> adjudicate; 3 agents; verdict NUMEROLOGY,
does NOT change located-not-forced). Records the killed hypothesis so it is not re-derived.

## The thesis (tie-it-together)

The session's protocol-stack synthesis (SG4 = an observerse stack; S1 = its consensus layer;
Nielsen-Ninomiya/chirality = its boundary layer) suggested: the generation count 3 = the consensus
layer's minimal fault-tolerance multiplicity, 2f+1 with f=1 = "2 honest replicas + 1 byzantine/imposter"
= GU's stated "2+1 effective, third is an imposter." Strongest build tried to pin f=1 from the Z/2 Clifford
chirality grading and locate the imposter in the RS/spin-3/2 (1-form (x) Delta-) sector.

## Why it died (decisive killshots)

1. **No agreement predicate.** 2f+1 is the replica count for f faulty nodes to still AGREE ON ONE SHARED
   VALUE. In GU/SM every generation is INDEPENDENTLY anomaly-free -- no cross-generation shared variable
   exists for a consensus theorem to solve. Invoking 2f+1 is a category error, not a derivation.
2. **The spinor machinery yields 2 and 1, never 3.** N-N doubling gives mode+mirror = 2 (net-zero
   chirality); the domain-wall escape gives 1 surviving boundary chirality. The integer 3 is manufactured
   solely by 2f+1|_{f=1} and welded onto a sector whose own machinery produces 2 and 1 -- the
   disjoint-framework coincidence in its sharpest form.
3. **f is a free dial disguised as fixed.** No theorem links the rank of a Clifford grading to a consensus
   fault parameter; f=1 is chosen to hit 3.

The builder conceded #1-2 as the honest weakest link. This is the exact failure mode the campaign has
killed repeatedly ("2+1 is numerology across disjoint frameworks").

## The one surviving lead (and it is largely already closed)

The only way a consensus reading could do real work: a genuine CROSS-generation invariant the sectors
must JOINTLY satisfy -- a global/discrete (cobordism / Dai-Freed) anomaly that cancels only for a specific
generation count, not per-generation (cf. Garcia-Etxebarria-Montero Z/9 forcing N_gen in 3Z). But the
paper's own big-swing R2 already checked this: for actual SM boundary data the mod-3 arena is EMPTY
(`Omega^Spin_5(B G_SM) (x) Z_(3) = 0` by color triality), so no 3-torsion global anomaly pins the count.
So the surviving lead re-points at already-explored, already-empty territory.

## Net

Numerology; killed. Located-not-forced is unchanged -- the count still rides SG4, and the consensus/stack
reframe does not force it. Value: the discipline held (a seductive tie-it-together thesis was refuted by
its own adversarial referee), and the failure sharpens WHY -- no cross-generation agreement predicate
exists, which is a clean structural reason the consensus layer cannot supply the count. No canon/claim/
verdict movement.
