---
title: "Eric/Curt Wave 3D-B2C15: all-grade bare quotient primalizer and induced LC graph"
status: exploration
doc_type: research_report
updated_at: "2026-08-01"
---

# Eric/Curt Wave 3D-B2C15: all-grade bare quotient primalizer and induced LC graph

## Verdict

The local principal construction is substantially tighter, but it does not
yet select a physical domain.

B2C15 completes the all-grade **bare, fixed-Shiab graph coefficient of the
compressed residual-square comparator** that B2C14R had executed only on
grade three. Every one of the six quotient-owner grades has
full rank at the named positive coordinate representative. On the named null
representative the coefficient remains nonzero
with total rank `4136`, but its entire indefinite `R_res` Gram vanishes. That
is a nonzero isotropic image, not a constructed gauge quotient.

The physical metric return is now the actual ten-owner derivative
`D_g G_Y`, followed by the fourteen-dimensional Levi--Civita spin graph. Its
graph has rank `10`; the old observed-four-dimensional proxy has rank `9`, and
their difference has rank `10`. After the active Shiab coefficient, the metric
block has rank `7` on two non-null representatives and rank `8` on a null
representative. These ranks equal their Gram ranks, so the remaining metric
radicals are genuine coefficient kernels rather than nonzero isotropic images.

The bare quotient term and metric block are decoupled in the computed
comparator. Hostile review caught that this is not yet the full reduction
graph. A splitting-dependent return
`r_xi(chi_m) in T*Y tensor h` is still unconstructed, and its image can pair
with the metric response in residual grade two. Therefore no full owner-block
rank, inertia, radical, or characteristic conclusion is admitted.

This is reconstruction grade. It does not construct `r_xi`, classify
continuous trace-line rank strata, identify the isolated metric kernels in
the coupled graph, add the all-grade moving-Shiab/formal-adjoint terms, return
the distinct exact-G2 Euler branch, construct the full nonlinear active
preboundary quotient, select an analytic domain, or recover a Standard Model
equation.

## Layer 0

| shared term | construction used here | object kept distinct |
| --- | --- | --- |
| reduction field | `[u] in P/H`, tangent `m=g/h` | local lift `u` and vertical `h` directions |
| metric variation | ten `Sym2(T*X)` owners mapped by `D_g G_Y` | arbitrary `Sym2(T*Y)` data |
| Levi--Civita graph | LC spin response of the full induced Gimmel metric | the four-dimensional truncated LC proxy |
| residual square | density-dual residual primalized by `R_res=b_res^{-1}` | a positive Hilbert norm |
| kernel | `ker K` in an explicitly named bare or isolated block | radical of `K^top R_res K` and the future full characteristic kernel |
| null result | a statement at one null covector orbit representative | a global hyperbolicity or physical-domain theorem |

The last distinction is load-bearing. The bare reduction coefficient has a
rank-`4136` null image whose Gram is zero. The metric coefficient instead has
rank `8` and Gram rank `8` on the same null representative. Calling both
objects “the null constraint” would conflate distinct blocks; the full-block
verdict additionally awaits `r_xi`.

## Source collision

This gate is `SOURCE-CONSTRAINED__REPOSITORY-CONSTRUCTED`.

`SOURCE-CONFIRMS`:

- The 2021 draft's Section 6 Zorro chain sends the metric on `X` through the
  induced metric on `Y` to its distinguished Levi--Civita/reference
  connection. The repository source-location table records this as the
  section-level source constraint; it does not prove the derivative used here.
- TOE local `02:19:17–02:20:33` places displacement from a gauge-rotated
  Levi--Civita reference in the contorsion slot.
- The 2021 draft, p.44 eq.9.4, supplies the completed bosonic residual grammar
  with `F_B`, `1/2 d_B T`, and `1/3[T,T]` rather than a curvature-only square.
- TOE local `00:20:51–00:29:16` and `01:34:49–01:36:56` supply trace reversal
  and contraction-not-projection.

`SOURCE-CORRECTS`:

- naked torsion is not the active object;
- a curvature-only square omits written terms; and
- the observed four-dimensional LC response cannot substitute for the
  induced `Y14` graph.

`SOURCE-SILENT`:

- the active grade set and its quadratic grade-preservation theorem;
- the all-grade residual inverse;
- the Frechet `D_g G_Y` graph;
- the exact isolated metric kernels and Hessian inertias;
- the reduction-induced `h` return `r_xi`;
- the all-grade moving-Shiab/formal-adjoint completion;
- the exact-G2 Euler return on this all-grade coupled graph;
- a source-derived tangent/BV differential; and
- the preboundary quotient and analytic domain.

The unreleased 2025 two-connection `D^2` discussion remains watch-only and is
not used to repair the 2021 action.

## Complete pointwise residual pairing

The active algebra is

```text
g = Lambda2 + Lambda3 + Lambda6 + Lambda7 + Lambda10 + Lambda11 + Lambda14,
dim g = 8256,
h = Lambda2, dim h = 91,
m = g/h, dim m = 8165.
```

The residual carrier is `T*Y tensor g`, dimension `14*8256=115584`. In the
orthonormal trace-reversed frame,

```text
b_res = rho G_Y^-1 tensor kappa,
R_res = rho^-1 G_Y tensor kappa^-1.
```

Both the inverse identity and
`D R_res = -R_res (D b_res) R_res` pass on the actual ten-dimensional metric
variation. The total residual inertia is `(57664,57920,0)`. `R_res` is an
indefinite primalizer, not a positive Hilbert Riesz map.

| internal grade | positive | negative |
| ---: | ---: | ---: |
| 2 | 45 | 46 |
| 3 | 190 | 174 |
| 6 | 1479 | 1524 |
| 7 | 1716 | 1716 |
| 10 | 491 | 510 |
| 11 | 174 | 190 |
| 14 | 1 | 0 |

## Why cross-grade leakage really cancels

Raw ordered terms do change grade. The executable plant finds an exact
grade-3-to-grade-7 ordered coefficient `1/2`. It becomes exactly zero only in
the symmetric quadratic covector assembly.

The proof certificate evaluates three diagonal coordinate types—positive,
negative non-trace, and the distinguished trace line—and five unordered mixed
orbits. Signed coordinate permutations preserving the `(9,5)` metric and the
trace line carry these representatives to every quadratic monomial
`xi_a xi_b`. Every assembled block is same-grade, while the union of the
coordinate types activates all six quotient grades. Thus the cancellation is a property
of the assembled second-order tensor, not an imposed output projection.

This support theorem does not imply constant rank on continuous non-null
orbits. The trace line breaks full `O(9,5)` symmetry: `q(xi)` and the trace
component are independent orbit data. The named non-null trace representative
already has no grade-14 support.

## Reduction-owner census

| grade | named positive bare rank | named positive-negative null bare rank |
| ---: | ---: | ---: |
| 3 | 364 | 132 |
| 6 | 3003 | 1584 |
| 7 | 3432 | 1848 |
| 10 | 1001 | 440 |
| 11 | 364 | 132 |
| 14 | 1 | 0 |
| **total** | **8165** | **4136** |

The named positive-negative null formula is `2*binomial(12,r-1)`. At the named non-null positive
representative, the bare reduction Hessian is diagonal and has inertia
`(4114,4051,0)`. At the named null representative, bare `K` has rank `4136` but
`K^top R_res K` is identically zero. The residual image is totally isotropic.
No gauge differential, quotient, or boundary condition follows from that
fact.

## The actual induced metric graph

For each of the ten basis variations `h in Sym2(T*X)`, the probe differentiates

```text
G_Y(g) = g + G_DW(g)
```

and converts `D_g G_Y[h]` to the fixed `(9,5)` orthonormal frame. At a normal
frame the LC spin response is

```text
delta Omega_mu = (1/8) sum_ab
  (xi_b H_{mu a} - xi_a H_{mu b}) gamma^a gamma^b.
```

It is therefore `h=Lambda2` valued, as required, but it contains the vertical
DeWitt response that the prior four-dimensional proxy omitted.

| covector representative | rank LC graph | rank `K_g` | rank Gram | Gram inertia |
| --- | ---: | ---: | ---: | ---: |
| positive non-null | 10 | 7 | 7 | `(4,3,3)` |
| positive+positive non-null | 10 | 7 | 7 | `(4,3,3)` |
| positive+negative null | 10 | 8 | 8 | `(1,7,2)` |

The observed-four-dimensional proxy has rank `9`; its difference from the
induced graph has rank `10`. The active metric coefficient lands only in
residual grade two. Because its coefficient rank equals its Gram rank, its
radicals are actual isolated `K_g` kernels.

The bare reduction term lands only in grades `3,6,7,10,11,14`. Since `R_res`
is grade diagonal, its comparator with the metric block is exactly zero on all
three executed representatives. The missing `h`-valued return
`r_xi(chi_m)` can land in grade two, so this zero does not transfer to the
full reduction--metric cross-Hessian.

## Why the coupled principal block remains open

The correct local graph has the schematic form

```text
sigma(DB)_xi(chi_m,h_X)
  = -xi tensor chi_m + r_xi(chi_m) + Omega_xi(D_gG_Y[h_X]).
```

This gate computes the first and third terms, not the second. Both
`C_xi r_xi` and `C_xi Omega_xi` may land in residual grade two. Until a
connection/splitting of `0 -> h -> g -> m -> 0` supplies `r_xi` with its
overlap law, its contribution can change the reduction Gram, mixed block,
total inertia, and radicals. The isolated null metric Gram rank `8` therefore
does not close a full owner-symbol verdict.

There are two further noncommuting pieces. B2C14R proved that the total
quotient-grade moving-Shiab response is nonzero even where its selected
six-slot `DM` component vanishes. B2C13 also proved that the compressed 2021
residual and the exact G2 action Euler covector have different symbols. This
gate contains neither completion. Consequently it is a coefficient theorem
for one comparator, not the principal symbol of the complete source action.

## Preboundary result

On the selected exact `3/4` reduction-coefficient subchannel, the complete scalarized
second-jet variation satisfies

```text
delta I = bulk + [delta q' P2 - delta q (P2)']_boundary,
P2 = a R E.
```

The endpoint is exactly `-3075/8`; reversing its sign fails. This earns a
two-trace principal preboundary packet for the residual-square subchannel. It
does not supply the full nonlinear active preboundary form, a boundary
quotient, or a closed domain.

## External datum boundary

P1, P2, and P3 remain unused and unchanged. Their present types do not supply
a tangent/BV differential or an analytic boundary condition. A newly declared
external datum could in principle select among independently admissible
domains, but it cannot silently convert a nonzero isotropic image into gauge
or replace a source-owned differential. That would be a new datum with a new
constraint-surplus burden.

## Next gate

Run
`ECW3D-B2C15R-REDUCTION-H-RETURN-RANK-STRATA-AND-COUPLED-BLOCK`:

1. choose or derive the connection/splitting of `0 -> h -> g -> m -> 0`;
2. construct `r_xi:m -> T*Y tensor h`, its overlap law, and all 91 vertical
   cancellations;
3. classify rank strata using both `q(xi)` and the trace component;
4. add the all-grade moving-Shiab/formal-adjoint terms while keeping the
   compressed residual and exact G2 Euler branches distinct;
5. recompute the genuinely coupled reduction--metric Gram, inertia, and
   radicals;
6. only then identify metric kernels and derive or kill a source-owned
   tangent/BV differential;
7. return the exact G2 Euler residual—not the compressed source residual—through
   the complete nonlinear second-jet variation; and
8. quotient the resulting preboundary form and construct the weighted coupled
   Euler symbol, including lower G2/G3 layers where the fourth-order block
   drops, before any analytic-domain claim.

Only after its gauge/constraint quotient has constant-rank data on the
inequivalent trace-line-preserving normal strata should the campaign replay
the mixed boson--fermion common Green domain. Curt's literal `(7,7)` carrier
remains formally separate, and `TG-1 AND TG-2 AND TG-3` remains false.

## Reproduction

```bash
uv run --with sympy --with numpy python \
  tests/channel-swings/eric_curt_wave3d_b2c15_full_quotient_primalizer_lc_graph_probe.py
```

Registry:
`lab/process/eric-curt-wave3d-b2c15-full-quotient-primalizer-lc-graph.json`.
