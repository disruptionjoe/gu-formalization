---
artifact_type: exploration
status: exploration (W214; TEAM RG-FLOW / RUNNING; five personas inline, one worker, no sub-agents; deterministic test with W130/W119/W128/W126/W165 positive controls)
created: 2026-07-14
wave: W214
label: W214
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (a pathological / runaway / non-existent vacuum is a REAL result); tri-repo gating STRICT
method: RG-FLOW / RUNNING (flow the couplings to the IR; the true vacuum = the IR condensate / endpoint of the flow)
title: "W214 -- BUILD the record-condensed true vacuum by the RG-FLOW route: flow the native tree point to the IR, locate the fixed points, read the endpoint spectrum. VERDICT: RUNAWAY-NO-VACUUM on the NATIVE basin (EXISTS-CONDITIONAL off-basin, unforced / rolling). The native tree point (f_2^2,f_0^2)=(-1/4,-3/8) (W130) flows, in the g-INDEPENDENT spin-2 sector (W128 D1, exact law 1/f_2^2(t)=1/f_2^2(0)+kappa Phi B2 t), to f_2^2 -> 0^- in the IR (Gaussian spin-2 corner, NEGATIVE at every finite scale) and to the c_W=0 STRONG-COUPLING WALL in the UV (1/f_2^2 crosses zero at finite t~+42.9 -> f_2^2 -> +/-inf, a Landau-type divergence one loop cannot adjudicate; W159). The scalaron mass m_0^2(mu)=gamma_phi f_0^2(mu) is SIGN-LOCKED NEGATIVE at every finite scale on the native basin (W119 two negative fixed-ratio roots; W163 NO-SIGN-FLIP), so the IR endpoint spectrum is TACHYONIC (k=0 growing mode, unbounded-below), NOT a real-bounded stationary condensate. The native tree point is OFF BOTH sensible-vacuum basins (AF gates f_2^2>0, W123; every AS/Reuter trajectory keeps f_2^2>0, W128) -- the RG-flow statement of E2 branch-UNSELECTED. A real-bounded vacuum EXISTS only OFF-basin: the AS/Reuter de-slaved endpoint f_0^2*=eta0 g*/(kappa Phi c_C)>0 (PERMITTED-NOT-FORCED, sign-locked to the ported eta0>0; flip it and TACHYON-FOLLOWS -- NC1), and the record-condensed de Sitter attractor which is ROLLING (Lambda~1/sqrt(N), dLambda/dN<0), an attractor NOT a stationary minimum (W163 NPV5c / W166). The DBI-clock / arrow-of-time reading (W166) HOLDS at the endpoint: the persistent k=0 growing scale mode IS the monotone global record-count clock -- the SAME feature that denies a stationary vacuum supplies the arrow -- but read at the v^2=1/16 validity edge (|m_0^2|=1/4=4x beyond, W126/W159), so PLAUSIBLE not certified. Debit-1 STANDS; bar (b) UNCHANGED; count {1,3}; E2 CARRIED."
hypothesis: "The native scalaron is tachyonic (m_0^2=-1/4, a false-vacuum instability). W163 named a record-condensed TRUE vacuum that EXISTS but is out-of-validity and unbuilt. BUILD it by the RG-flow route: treat the true vacuum as the IR condensate / endpoint of the coupling flow. Flow the native tree point toward the IR in the W128/W119 FRG truncation, locate the fixed point(s) and the basin it flows into, and read the IR spectrum. Does the running REACH a sensible condensed vacuum (finite vev, real bounded spectrum), or run away / hit a Landau-type pathology? Does the arrow-of-time / DBI-clock reading (W166) HOLD there? What validity / extension is needed? Grade honestly; a runaway / non-existent vacuum is a REAL result."
grade: "exploration / conditional register throughout. Every 'the flow reaches X' reads 'within the W128/W119 truncation, the running lands at X'; nothing asserts GU, asserts a vacuum, or changes any verdict. COMPUTED (deterministic, tests/W214_true_vacuum_rg_flow.py, 21/21 exit 0, W130+W119+W128+W126+W165 positive controls first): the exact g-independent spin-2 law and its IR->0^- endpoint / UV c_W=0 Landau wall (B1-B3); the native point off both basins (B4); the NO-SIGN-FLIP scalaron mass -> tachyonic k=0 endpoint spectrum (C1-C3); the off-basin AS de-slaved root sign-lock and PERMITTED-NOT-FORCED both-signs UV completeness (D1-D2); the rolling (non-stationary) record de Sitter attractor (D3); the arrow-of-time / global-clock reading at the endpoint (E1-E2); the 4x validity margin (E3); three negative controls (NC1 eta0-flip TACHYON-FOLLOWS; NC2 positive-f_2^2 in-basin completes; NC3 healthy M^2 kills Im V_eff). LOAD-BEARING imports (CITED, not re-derived): W130 (native (f_2^2,f_0^2)=(-1/4,-3/8), c_R=-4/9, c_W=+2, m_0^2=-1/4, the sign maps); W119 (the two NEGATIVE AF fixed-ratio roots, sign-lock, Landau pole at g=0); W128 (the 4-coupling FRG system, the Reuter FP g*=0.674 lambda*=0.151, the sign-lock theorem f_0^2*=eta0 g*/(kappa Phi c_C), PERMITTED-NOT-FORCED, the g-independent 1/f_2^2 law, E2 fork); W123 (AF monotonicity, f_2^2>0 basin gate); W126 (potential exactly quadratic; the gradient closed form and v^2=1/16 degeneration); W159 (the c_W=0 spin-2 wall shared by both branches; the DBI speed-limit; the k=0 dispersion); W163 (the CW Im V_eff false-vacuum signature; NO-SIGN-FLIP-ON-AF; the record condensate candidate and its two kills); W165 (shape-blind c_R=-(4/9)(alpha+beta)); W166 (the tachyon = record-count / arrow-of-time engine; DBI velocity cap; everpresent 1/sqrt(N) fade); W145 (N=4-volume ~ e^{4p}); W203 (the Z_U nonlocal completion / Krein kernel). NEW here (DERIVED within the truncation): the EXPLICIT flow of the native tree point (not just its off-branch status) -- IR endpoint f_2^2->0^-, UV c_W=0 Landau wall as a flow statement; the identification of the native basin's IR endpoint spectrum as tachyonic (no zero crossing, no CW transmutation); the RG-flow reading of E2 (off both basins); the location of the only real-bounded endpoints strictly off-basin; the arrow-holds-at-the-endpoint cross-check. HONEST RESIDUALS: (1) the EH-sector magnitudes (A_tot, B0, B1) and eta0's magnitude are SCHEMATIC (W83/W119 calibration, reproduced as controls); the load-bearing content is SIGNS and the g-independent spin-2 law, not magnitudes; (2) the f_0^2(mu) sign-definite-negative trajectory in Part C is illustrative, matching W119's PROVEN sign-lock -- the load-bearing content is the SIGN, imported, not the curve (same honesty as W163 NPV1b); (3) the c_W=0 strong-coupling passage is NAMED not computed (the strong-coupling problem one loop cannot adjudicate, unchanged from W130); (4) the E2 AF/AS fork is CARRIED, not closed; (5) the record-condensate endpoint is UNBUILT (W154/W158/W160) and out of validity. NO canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count stays {1,3}; H59/H61a OPEN; E2 carried. NO forbidden target {3,8,24,chi(K3),Ahat} assumed/inserted/hardcoded/divided-by."
construction: "program-native where the objects are GU's (the induced |II|^2 action, the covariant c_R / c_W channel split, the native tree point, the record field / record-count conformal mode, the (9,5) ambient). Standard-field where the machinery binds any construction (the FRG Reuter fixed point and R^2 relevance; the agravity 4-coupling flow and its g-independent Weyl sector; the Coleman-Weinberg effective potential and its Im-part false-vacuum reading; DBI kinetic saturation; the everpresent-Lambda 1/sqrt(N) scaling; the Lorentz-irreducible spin decomposition). Every analogy PORTED and labelled; none asserted of GU. Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md. Tri-repo gating STRICT: record / finality / arrow-of-time semantics stay pointers to temporal-issuance / time-as-finality / TaF; GU owns the induced-action and flow math only; no cross-repo identity asserted."
depends_on:
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W128-reuter-branch-scalaron-native-2026-07-14.md
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/W159-tachyon-escapes-2026-07-14.md
  - explorations/W163-lens-nonperturbative-vacuum-2026-07-14.md
  - explorations/W165-lens-abstraction-level-2026-07-14.md
  - explorations/W166-lens-tachyon-is-the-engine-2026-07-14.md
  - explorations/W203-branch3-source-action-fixed-coefficients-2026-07-14.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W214_true_vacuum_rg_flow.py
external_refs:
  - "Reuter, PRD 57 (1998) 971 -- the Reuter (asymptotic-safety) fixed point"
  - "Codello, Percacci & Rahmede, Ann. Phys. 324 (2009) 414 -- polynomial f(R): R^2 relevant at the Reuter FP"
  - "Benedetti, Machado & Saueressig, MPLA 24 (2009) 2233 -- Weyl^2 + R^2 Reuter FP; Weyl coupling stays (marginally) irrelevant"
  - "Copeland, Rahmede & Saltas, arXiv:1311.0881 -- AS-Starobinsky: R^2 relevant, IR value a free boundary condition, healthy branch realized"
  - "Salvio & Strumia, JHEP 06 (2014) 080 -- agravity: the AF-branch forced-negative f_0^2; M_i^2 = f_i^2 Mbar^2/2"
  - "Coleman & Weinberg, PRD 7 (1973) 1888; Callan & Coleman, PRD 16 (1977) 1762 -- the one-loop effective potential; Im V_eff as a false-vacuum decay width"
  - "Silverstein & Tong, PRD 70 (2004) 103505 -- DBI / speed-limit kinetic structure"
  - "Ahmed, Dodelson, Greene, Sorkin, PRD 69 (2004) 103523 -- everpresent Lambda ~ +/- 1/sqrt(N), N = spacetime 4-volume"
---

# W214 -- the true vacuum as the IR endpoint of the RG flow

## 0. The charge, and the one-paragraph answer

Five teams are each BUILDING the record-condensed true vacuum of GU by a different method, to test
convergence. This is team RG-FLOW / RUNNING. The genuinely open question: the native scalaron is
tachyonic (`m_0^2 = -1/4 < 0`; W122/W126/W130), a false-vacuum instability; W163 argued a
record-condensed TRUE vacuum EXISTS but is out-of-validity and unbuilt; we do not know what GU rolls
to. Our route is to treat the true vacuum as the IR condensate / endpoint of the coupling flow, use
the W128/W119 Reuter asymptotic-safety structure, and ask whether the running of GU's native tree
point reaches a sensible condensed vacuum (finite vev, real bounded spectrum) or runs away / hits a
Landau-type pathology. The answer, computed: on the NATIVE basin the flow reaches NO sensible
stationary condensate. The native tree point flows in the IR to `f_2^2 -> 0^-` (the Gaussian spin-2
corner, tachyonic scalaron intact) and in the UV to the `c_W = 0` strong-coupling wall (a Landau-type
divergence, not a fixed point); it sits off BOTH sensible-vacuum basins. A real-bounded vacuum exists
only OFF-basin -- the AS/Reuter endpoint (PERMITTED-NOT-FORCED, an unforced boundary datum) and the
record-condensed de Sitter attractor (which is ROLLING, not a stationary minimum). So the RG-flow
verdict is **RUNAWAY-NO-VACUUM on the native basin, EXISTS-CONDITIONAL off-basin.** The arrow-of-time
/ DBI-clock reading (W166) HOLDS at the endpoint, at the edge of validity. This CONVERGES with the
other methods (Section 8): none independently BUILDS a stable stationary true vacuum; all locate one
only as an unforced, out-of-validity, or rolling object.

Five personas inline, one worker, no sub-agents. Deterministic test
`tests/W214_true_vacuum_rg_flow.py`, 21/21 exit 0, W130 + W119 + W128 + W126 + W165 positive controls
first.

## 1. Construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md), named

| Object | Construction used | Why |
|---|---|---|
| Scalaron / ghost couplings | the agravity `(g, lambda, f_2^2, f_0^2)` 4-coupling flow (W128/W119) | The pinned RG object; `c_R = 1/(6 f_0^2)`, `c_W = -1/(2 f_2^2)` map to W130's covariant couplings. |
| Native tree point | `(f_2^2, f_0^2) = (-1/4, -3/8)` (W130) | The IR seed the flow starts from; the thing we flow. |
| UV completion | none selected; both branches carried | Route is the fork itself; E2 stands (W128/W159). |
| True vacuum | the IR condensate / endpoint of the flow | The method's operational definition of "true vacuum". |
| Record condensate | the de Sitter attractor `Lambda ~ 1/sqrt(N)` (W145/W163/W166) | The candidate off-basin IR attractor; ported everpresent normalization. |

## 2. Persona 1 -- functional-RG specialist: the exact spin-2 flow of the native point

The load-bearing simplification of this truncation is that the Weyl (spin-2) coupling flows
`g`-INDEPENDENTLY (W128 Section 3 / D1): `beta_{f_2^2} = -kappa Phi b_2 f_2^4`-type, so
`1/f_2^2(t) = 1/f_2^2(0) + kappa Phi b_2 t` holds VERBATIM on every trajectory, AF or AS, with
`t = ln(mu/mu_0)`. This lets us flow GU's native `f_2^2(0) = -1/4` in closed form, both ways
(test B1 checks the closed form against direct beta-function integration).

**IR (`t -> -inf`):** `1/f_2^2` decreases without bound, so `f_2^2 -> 0^-` -- the Gaussian spin-2
corner, approached FROM BELOW, staying NEGATIVE at every finite scale (test B2). The ghost coupling
`c_W = -1/(2 f_2^2) -> +infinity`. The native flow's IR endpoint in the spin-2 coordinate is the
origin, reached WITHOUT ever entering a positive-`f_2^2` region.

**UV (`t` increasing):** `1/f_2^2` rises from `-4` and CROSSES ZERO at finite
`t = -1/(f_2^2(0) kappa Phi b_2) ~ +42.9`, i.e. `f_2^2 -> +/- infinity`, which is exactly `c_W = 0`
-- the strong Weyl-coupling wall W159 identified (test B3). This is a Landau-type DIVERGENCE, not a
fixed point; the flow cannot be continued perturbatively through it. The native tree point has NO
controlled UV completion on its own flow.

## 3. Persona 2 -- asymptotic-safety analyst: the native point is off both basins

Both known UV completions live at `f_2^2 > 0`: the AF basin gates on `f_2^2 > 0` by construction
(W123), and every AS/Reuter trajectory keeps `f_2^2 > 0` at all finite scales (the same exact
`1/f_2^2` law, W128). GU's native `f_2^2 = -1/4 < 0` is therefore OFF BOTH sensible-vacuum basins
(test B4). This is the RG-flow statement of W159's "branch-UNSELECTED": reaching either branch from
the native point requires crossing the same `c_W = 0` wall (Section 2), and the branch distinction
is entirely a scalar-sector (`f_0^2`) story orthogonal to that spin-2 crossing. The asymptotic-safety
structure does not deliver a UV completion for GU's native content; it delivers two basins GU is not
in, separated from GU by a strong-coupling passage one loop cannot adjudicate. E2 is CARRIED, now as
an explicit basin fact rather than an abstract fork.

Where the AS branch DOES supply a sensible endpoint is strictly off-basin, and only conditionally:
the de-slaved Reuter root `f_0^2* = eta0 g*/(kappa Phi c_C)` is SIGN-LOCKED to `eta0` (the ported
`R^2`-relevance), positive for `eta0 > 0`, giving `m_0^2 >= 0` (test D1) -- but PERMITTED-NOT-FORCED:
both a healthy and a tachyonic IR datum flow UV-complete into the Reuter FP (test D2), so nothing
native selects health, and flipping `eta0` makes the tachyon follow onto AS too (NC1).

## 4. Persona 3 -- IR-condensate specialist: the endpoint spectrum, and the rolling record attractor

The scalaron mass rides the running `R^2` coupling: `m_0^2(mu) = gamma_phi f_0^2(mu)` with
`gamma_phi = 2/3 > 0` (W163 NPV1a, test PC2). On the native (AF-type) basin `f_0^2(mu)` is sign-locked
NEGATIVE at every finite scale (W119's two NEGATIVE fixed-ratio roots, flip impossible for admissible
regulators; W163 NO-SIGN-FLIP), so `m_0^2(mu) < 0` all the way to the IR endpoint, with NO real zero
crossing and NO Coleman-Weinberg minimum to dimensionally transmute (test C1). The IR endpoint
spectrum is therefore TACHYONIC: the homogeneous `k = 0` mode `p'' + m_0^2 p = 0` with `m_0^2 = -1/4`
has REAL roots `+/- 1/2` -- a growing (runaway) mode, unbounded-below, NOT a bounded oscillation
around a stationary minimum (test C2). A healthy `m_0^2 > 0` would give imaginary roots `+/- i/2`
(bounded oscillation = a stationary real bounded spectrum, test C3); the native flow endpoint is on
the wrong side of that dichotomy. **The running does NOT reach a sensible condensed vacuum on the
native basin: it reaches the Gaussian spin-2 corner carrying a persistent tachyonic scalaron.**

The one genuinely appealing IR condensate is the record-condensed de Sitter phase (`Lambda > 0`,
records accreting; W163 NPV5 / W166). As an RG-flow endpoint it is an IR ATTRACTOR, but a ROLLING
one: `Lambda ~ 1/sqrt(N)` with `N` monotone increasing (the arrow) gives `dLambda/dN < 0` always
(test D3), so the mean rolls and never settles -- a de Sitter attractor, NOT a stationary minimum
(W163 NPV5c). Even off-basin, the sensible-looking IR endpoint is not a STABLE stationary vacuum.

## 5. Persona 4 -- fixed-point / flow-topology checker: the map, and the arrow at the endpoint

The `(f_2^2, f_0^2)` flow topology: the Gaussian point `(0,0)` (spin-2 IR endpoint of the native
flow, tachyonic scalaron); the AF fixed-ratio family at `f_2^2 > 0` (both ratio roots negative, W119
PC3, Landau-poling at `g = 0`); the interacting Reuter FP at `g* != 0`, `f_2^2* = 0`, de-slaved
`f_0^2* > 0` (off-basin, unforced). The native tree point at `(-1/4, -3/8)` is in the basin of NONE
of the real-bounded endpoints; its own basin flows to the tachyonic Gaussian corner (IR) or the
`c_W = 0` wall (UV). NC2 confirms the topology is real, not an integrator artifact: a start WITH
`f_2^2 > 0` flows to a finite Reuter neighbourhood without a Landau blow-up.

The arrow-of-time / DBI-clock reading (W166) is a genuine cross-check at the endpoint, and it HOLDS.
The persistent `k = 0` growing scale mode IS the record-count engine: `N = 4-volume ~ e^{4p}`,
`dN/dp = 4 e^{4p} > 0`, and `m_0^2 < 0` grows `p` (test E1) -- the SAME feature that denies a
stationary vacuum (Section 4) supplies the monotone global clock. The growth rate
`omega^2 = -(k^2 + m_0^2)` peaks at `k = 0` (test E2), a single global time direction, no finite-`k`
band -- the cleanest arrow-of-time profile. But the graceful-rate reading needs the field at the
`v^2 = 1/16` degeneration edge and `|m_0^2| = 1/4 = 4x` beyond it (test E3): out of EFT validity, read
at the edge, so PLAUSIBLE not certified -- exactly W166's grade, reached here from the flow side.

## 6. Persona 5 -- ruthless skeptic: is this a real result or a re-labelling?

**Steelman "the flow DOES reach a vacuum".** The sharpest pro-vacuum case: the IR endpoint
`f_2^2 -> 0` IS a fixed point (Gaussian), so the flow terminates cleanly, and one could call that the
vacuum. Refuted on the SPECTRUM: a fixed point of the couplings is not a stationary vacuum of the
field; the scalar mass at that endpoint is `m_0^2 = -1/4 < 0`, a growing mode (test C2). "The
couplings stop running" is not "the field sits in a stable minimum". The endpoint is a runaway, not a
vacuum.

**Steelman "AS rescues it".** The AS de-slaved endpoint has `m_0^2 >= 0` (test D1). Conceded as an
EXISTENCE statement -- but off-basin and PERMITTED-NOT-FORCED (test D2, NC1): the native point is not
in that basin, reaching it needs the `c_W = 0` wall, and even there health is a free boundary datum
sign-locked to a PORTED number. This is not GU rolling to a sensible vacuum; it is a sensible vacuum
existing somewhere GU's native flow does not go, if one ports a sign. Honest EXISTS-CONDITIONAL, not
EXISTS-SENSIBLE.

**Steelman "the record condensate is the true vacuum".** It is the right candidate (W163) and it is
an IR attractor -- but ROLLING, not stationary (test D3), out of validity (test E3), and UNBUILT at
the source (W154/W158/W160/W203). Naming it does not build it.

**Where the skeptic must stop.** This is NOT a kill of GU; it is a truth-seeking result about what
the running does. The native flow does not reach a sensible stationary condensate; a real-bounded one
exists only off-basin and unforced; a rolling record attractor exists but is not stationary. Debit-1
STANDS; bar (b) UNCHANGED. The result is a RUNAWAY-NO-VACUUM on the native basin, which is a real
finding, not a re-labelling of the tachyon.

## 7. Synthesis -- the return data

| Question | RG-flow answer | Grade |
|---|---|---|
| (1) Does a true vacuum EXIST? | Native basin: NO sensible stationary condensate -- the flow reaches `f_2^2 -> 0^-` (tachyonic scalaron) / the `c_W=0` Landau wall; off both basins. Real-bounded EXISTS only off-basin (AS/Reuter, unforced) or as a rolling record attractor. | DERIVED (native RUNAWAY); EXISTS-CONDITIONAL off-basin |
| (2) Its NATURE (scale; spectrum real+bounded or pathological?) | Condensate scale `\|m_0^2\| = 1/4`, 4x beyond `v^2 = 1/16` (out of validity). Native endpoint spectrum TACHYONIC (`k=0` growing, unbounded-below), NOT real-bounded. Real-bounded only on the unforced AS endpoint. | DERIVED |
| (3) Arrow-of-time / DBI-clock (W166) at the endpoint? | HOLDS -- the persistent `k=0` growing scale mode is the monotone global record-count clock; the same feature that denies a stationary vacuum supplies the arrow. Read at the validity edge. | DERIVED-on-PORTED, PLAUSIBLE |
| (4) Validity / needed extension | Truncated-FRG + derivative expansion, out of validity at the condensate scale. Needed: native de-slaving sign `eta0` (H66) to SELECT AF vs AS; nonperturbative completion past `v^2=1/16` (record / `Z_U` kernel, W203). | HONEST |

**Overall verdict: RUNAWAY-NO-VACUUM on the native basin (EXISTS-CONDITIONAL off-basin, unforced /
rolling).** The running of GU's native tree point does not, by itself, reach a sensible stationary
condensed true vacuum. A real-bounded vacuum exists only off the native basin (the AS/Reuter endpoint,
PERMITTED-NOT-FORCED) or as a rolling, out-of-validity, unbuilt record-de-Sitter attractor. The
tachyon persists to the IR endpoint, where the DBI-clock / arrow-of-time reading holds at the edge of
validity. Debit-1 STANDS; bar (b) UNCHANGED; count `{1,3}`; E2 CARRIED.

## 8. Convergence note

Agreement and disagreement with the other four methods (the five-method convergence test):

- **vs W163 (Coleman-Weinberg / nonperturbative-vacuum, "PLAUSIBLE-BUT-UNBUILT"):** STRONG AGREEMENT,
  by an independent mechanism. W163 found the tachyon is a false vacuum (`Im V_eff != 0`, reproduced
  here as PC7) and named the record condensate as the true vacuum but could not exhibit it (out of
  validity, unbuilt, non-stationary). The RG-flow route reaches the SAME place from the running: the
  native flow reaches no sensible stationary condensate, and the only real-bounded endpoints are
  off-basin / rolling / out-of-validity. Both methods agree the true vacuum is a CANDIDATE, not a
  built object.

- **vs W128/W159 (branch-selection, "PERMITTED-NOT-FORCED / branch-UNSELECTED"):** AGREEMENT, and the
  RG-flow route SHARPENS it. W159 said the native point is off both branches and reaching either needs
  the `c_W=0` wall; W128 said AS PERMITS but does not FORCE health. This wave shows the SAME facts as
  an explicit flow: the native `f_2^2 = -1/4` flow Landau-poles at the `c_W=0` wall in the UV and
  reaches the tachyonic Gaussian corner in the IR (B2-B4), and the AS endpoint is off-basin and
  unforced (D1-D2, NC1). No disagreement; the flow picture is the dynamical version of the static
  branch statement.

- **vs W166 (arrow-of-time / tachyon-is-the-engine, "PLAUSIBLE"):** AGREEMENT. W166's DBI-clock arrow
  reading is exactly what the RG-flow endpoint carries: the persistent `k=0` growing mode is the
  monotone global clock (E1-E2), read at the `v^2=1/16` validity edge (E3). The RG-flow route adds
  that this arrow is present precisely BECAUSE the native basin has no stationary vacuum -- the two
  readings are the same growing endpoint mode under opposite success criteria.

- **vs W165 (universal-property / shape-blind `c_R`, "NO-FORCING / relocation to the finality
  frontier"):** CONSISTENT. W165's `c_R = -(4/9)(alpha+beta)` (reproduced here as PC6) puts every
  positive-total-weight norm-square on the tachyonic side; the RG-flow route confirms the running does
  not cure that sign (NO-SIGN-FLIP, C1). Both locate health off the native object -- W165 off the
  norm-square cone (relative Krein signature), this wave off the native basin. No disagreement.

**Net convergence:** all five routes agree on the negative core -- GU's native content does NOT, by
itself, roll to a demonstrable stable stationary real-bounded true vacuum; the tachyon persists and
the record-condensed true vacuum is a candidate that is unforced / out-of-validity / rolling /
unbuilt. No method independently BUILDS the vacuum. The five-method convergence is therefore on
**RUNAWAY-NO-VACUUM (native) with EXISTS-CONDITIONAL (off-basin)** -- a genuine, truth-seeking result:
a pathological / conditional vacuum is a real answer, and the debit stands. The one lever every method
names is the same paper-scale item: the native de-slaving sign `eta0` (H66) to SELECT AF vs AS, plus
a nonperturbative completion past the `v^2 = 1/16` induced-metric degeneration.

## 9. Gates and honest limits

- Exploration grade; conditional register throughout. Nothing asserts GU, asserts a vacuum, or
  changes any verdict. The one existence result (the AS off-basin endpoint) is CONDITIONAL and
  unforced; the primary finding is native RUNAWAY-NO-VACUUM.
- The EH-sector magnitudes and `eta0`'s magnitude are SCHEMATIC (W83/W119 calibration, reproduced as
  positive controls); the load-bearing content is SIGNS and the exact `g`-independent spin-2 law, not
  magnitudes. The `f_0^2(mu)` curve in Part C is illustrative, matching W119's PROVEN sign-lock.
- The `c_W = 0` strong-coupling passage is NAMED, not computed (the strong-coupling problem one loop
  cannot adjudicate). The E2 AF/AS fork is CARRIED, not closed. The record-condensate endpoint is
  UNBUILT (W154/W158/W160/W203) and out of validity.
- Tri-repo gating held: record / finality / arrow-of-time semantics stay pointers to
  temporal-issuance / time-as-finality / TaF; GU owns the induced-action and flow math only. No
  cross-repo identity asserted.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count stays `{1,3}`;
  H59/H61a OPEN; E2 carried. NO forbidden target `{3,8,24,chi(K3),Ahat}` assumed or inserted. Zero em
  dashes in paper-facing text.

*Filed 2026-07-14 by Team RG-FLOW / RUNNING (W214). One of five independent methods building the
record-condensed true vacuum, to test convergence. Coherence-first; flow the native tree point to the
IR, locate the fixed points, read the endpoint spectrum. Five personas inline in one worker
(functional-RG specialist; asymptotic-safety analyst; IR-condensate specialist; fixed-point /
flow-topology checker; ruthless skeptic); no sub-agents. Reproducible:
`python -u tests/W214_true_vacuum_rg_flow.py` (21/21, exit 0; W130 + W119 + W128 + W126 + W165
positive controls first). Exploration grade; conditional register; no canon movement.*
