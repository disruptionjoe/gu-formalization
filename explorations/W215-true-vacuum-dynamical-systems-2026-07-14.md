---
artifact_type: exploration
status: exploration (W215; TEAM DYNAMICAL-SYSTEMS / ROLLING; one of five independent convergence teams; five personas inline, one worker, no sub-agents; deterministic test with W126/W130/W159/W166 positive controls)
created: 2026-07-14
wave: W215
label: W215
posture: coherence-first (Joe 2026-07-14); build the true vacuum, do not propose it; exploration grade; conditional register; honest grading; truth-seeking (a runaway is a real negative result)
title: "W215 -- BUILD the record-condensed true vacuum by DYNAMICAL SYSTEMS / ROLLING: treat the tachyonic scalaron roll as a flow on (p, v) and find the ATTRACTOR. VERDICT: RUNAWAY-NO-VACUUM (graceful de Sitter ROLLING attractor; arrow holds; validity-edge). The only finite fixed point is the origin (the empty/no-record false vacuum), a hyperbolic SADDLE for every drag Gamma>=0 (Jacobian det = m_0^2 = -1/4 < 0; eigenvalues +/- 1/2 at Gamma=0, reproducing the W166 growing mode). NO stable fixed-point true vacuum exists: on the physical everpresent branch (Lambda>0, c>0) V_tot'(p)<0 for all p>=0 (pure runaway), and on the attractive branch (Lambda<0, c<0) the one finite fixed point p*~0.38 is a MAXIMUM (V_tot''(p*)=-0.44<0), because W126 exact-quadraticity locks V''(p)=m_0^2=-1/4 EVERYWHERE. The genuine attractor is the DBI TERMINAL-VELOCITY surface v -> v_max = 1/4: the roll asymptotes to a bounded velocity (verified by integration), so p is LINEAR in tau and N = e^{4p} grows at a steady de-Sitter rate dN/dtau = 4 v_max N -- a de Sitter ROLLING attractor, NOT a static minimum, and NOT a double-exponential blow-up. No limit cycle (N is a strict monotone Lyapunov function increasing along the whole flow, which forbids closed orbits). The arrow-of-time / DBI-clock reading (W166) HOLDS: N monotone from saddle to attractor, clock graceful-in-rate, everpresent drive fades (dLambda/dN<0). Validity: the attractor sits AT the v^2 = 1/16 induced-metric degeneration edge, the drive scale |m_0^2| = 1/4 sitting 4x beyond -- out of certified EFT validity; a trustworthy endpoint needs the DBI/induced completion past v^2=1/16 AND the unbuilt promotion-gate source T4 (W154/W158/W160). Debit-1 STANDS; bar (b) UNCHANGED; H59 OPEN; count unchanged."
hypothesis: "W163 named the record-condensed TRUE vacuum as a CANDIDATE that is out-of-validity, unbuilt at the source, and monotone-rolling rather than stationary, but did not BUILD the roll as a dynamical system. Build it: treat the homogeneous (k=0) scalaron amplitude p (= the conformal/scale/record-count mode, W153/W166) as a dynamical variable with velocity v = dp/dtau, on the EXACTLY-quadratic tachyonic hilltop (W126), with the DBI speed limit at v^2=1/16 (W126/W159) capping velocity, and the everpresent/accretion drive (W145/W164/W185/W187) sourcing the roll. Find the attractor / endpoint: is it a STABLE FIXED POINT (a genuine true vacuum), a LIMIT CYCLE, or an UNBOUNDED RUNAWAY? Characterize the flow, the fixed points, their eigenvalue stability, the basin of the native initial condition, the endpoint scale and its spectrum, and whether the arrow / DBI-clock holds there."
grade: "exploration / conditional register throughout. Every 'the roll ends on X' reads 'under the reduced homogeneous DBI-capped conditional flow, X'; nothing asserts GU, asserts a vacuum, or changes any verdict. COMPUTED (deterministic, tests/W215_true_vacuum_dynamical_systems.py, 22/22 exit 0, W126+W130+W159+W166 positive controls first): the origin as the unique finite fixed point of the free flow; the Jacobian det = m_0^2 = -1/4 and the +/- 1/2 eigenvalues at Gamma=0 (saddle, matching W166); the eigenvalue product = det < 0 for all Gamma (hyperbolic saddle); the numerically-integrated terminal-velocity attractor (v -> v_max = 1/4, p linear, velocity bounded by the cap); v_max as an attracting fixed point of the reduced velocity flow (rate 0 at the cap, negative slope just inside); p (hence N=e^{4p}) strictly monotone along the roll (Lyapunov, no limit cycle); V''(p) = m_0^2 = -1/4 everywhere (endpoint spectrum stays tachyonic); the de Sitter rate dN/dtau = 4 v_max N; the everpresent fade dLambda/dN < 0; the accretion r(N)=kappa0 sqrt(N) monotone with r*Lambda = kappa0 const (monotone drive, no restoring component); the no-stable-vacuum-on-either-branch result (physical branch pure runaway; attractive branch fixed point is a maximum); the v^2=1/16 / 4x validity margin. LOAD-BEARING imports (CITED, not re-derived): W126 (potential EXACTLY quadratic so V''=const; the gradient rational and v^2=1/16 degeneration); W130 (c_R=-4/9, native pole m_0^2=-1/4); W159 (gradient sector is a DBI SPEED LIMIT, all coefficients positive, bounds velocity not excursion, |m_0^2|=4x margin, bounded-velocity runaway); W166 (m^2<0 <=> N grows = arrow; N=e^{4p} monotone; DBI cap linearizes the runaway to steady de-Sitter-rate N; everpresent 1/sqrt(N) fade; graceful-in-rate at the validity edge); W145 (N=4-volume, everpresent Lambda~+/-1/sqrt(N)); W153 (the tachyon lives in the conformal/scale = record-count mode); W164 (record accretion is a MONOTONE destabilizing drive, not a restoring feedback); W163 (record-condensed phase is a true-vacuum CANDIDATE, out-of-validity, unbuilt T4, monotone-rolling not stationary); W185/W187 (accretion r(N)=kappa0 sqrt(N)). NEW here (DERIVED): the reduced (p,v) phase portrait with the DBI inverse-mass factor; the saddle classification via the Jacobian determinant for all Gamma; the numerically-verified terminal-velocity attractor as a de Sitter ROLLING attractor (fixed point at infinity); the no-limit-cycle Lyapunov argument (N monotone); the no-stable-vacuum-on-either-everpresent-branch computation. HONEST RESIDUALS: (1) the reduced model uses the standard relativistic DBI inverse-mass factor (1-(v/v_max)^2)^{3/2} PORTED, not a first-principles native induced-metric derivation of the (p,v) inertia; the load-bearing content is the SPEED-LIMIT structure (W159, all-positive coefficients) and the exact-quadratic potential (W126), both imported and reproduced as positive controls; (2) the attractor sits at the v^2=1/16 edge, so the endpoint is OUT-OF-VALIDITY exactly as W159/W163 found; (3) the everpresent/accretion drive is the ported, debit-carrying W145/W185 mechanism (W138 G5), not a GU derivation; (4) the promotion-gate source T4 that would build the condensate is UNBUILT (W154/W158/W160). NO canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count stays unchanged; H59/H61a OPEN; the E2 AF/AS fork carried, not closed. NO forbidden target {3,8,24,chi(K3),Ahat} assumed/inserted/hardcoded/divided-by."
construction: "program-native where the objects are GU's (the induced |II|^2 scalaron; the conformal/scale = record-count mode p; the covariant native pole m_0^2 = -1/4; the exact-quadratic potential; the gradient rational and its v^2=1/16 degeneration; N = 4-volume; the record-condensed phase / promotion-gate source; the (9,5) ambient). Standard-field where the machinery binds any construction (the (p,v) phase-portrait / fixed-point / Jacobian-eigenvalue apparatus; the relativistic DBI / k-essence terminal-velocity 'inverse-mass' factor; the Lyapunov-monotone-function no-limit-cycle argument; the de Sitter constant-rate e-folding; the false-vacuum-hilltop / rolling-attractor cosmology). Every analogy PORTED and labelled; none asserted of GU. Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md."
depends_on:
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W159-tachyon-escapes-2026-07-14.md
  - explorations/W163-lens-nonperturbative-vacuum-2026-07-14.md
  - explorations/W164-lens-left-out-ingredient-2026-07-14.md
  - explorations/W166-lens-tachyon-is-the-engine-2026-07-14.md
  - explorations/W145-substrate-sweep-mathematics-2026-07-14.md
  - explorations/W153-marble-corrected-and-tachyon-cheapwood-test-2026-07-14.md
  - explorations/W185-record-energy-economy-toy-model-2026-07-14.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W215_true_vacuum_dynamical_systems.py
external_refs:
  - "Silverstein & Tong, PRD 70 (2004) 103505 -- DBI / speed-limit kinetic structure (the terminal-velocity inverse-mass factor, PORTED)"
  - "Strogatz, Nonlinear Dynamics and Chaos (1994) -- fixed points, Jacobian linearization, saddles, Lyapunov functions, Bendixson (the phase-portrait apparatus, PORTED)"
  - "Ahmed, Dodelson, Greene, Sorkin, PRD 69 (2004) 103523 -- everpresent Lambda ~ +/- 1/sqrt(N) (the drive, PORTED via W145)"
  - "Gibbons & Hawking, PRD 15 (1977) 2738 -- de Sitter horizon (the steady-rate volume-accretion anchor)"
---

# W215 -- the true vacuum by dynamical systems: where does the roll end?

## 0. The build, in one paragraph

W163 named GU's record-condensed TRUE vacuum as a CANDIDATE that is out of EFT validity,
unbuilt at the source, and monotone-rolling rather than stationary. It did not BUILD the
roll as a dynamical system and read the endpoint off the flow. This wave does. Treat the
homogeneous (`k = 0`, the mode that peaks, W159/W166) scalaron amplitude `p` -- the
conformal / scale / record-count mode (W153/W166), with `N = 4-volume ~ e^{4p}` (W145) --
as a dynamical variable with velocity `v = dp/dtau`. The flow lives on three pinned
ingredients: the tachyonic hilltop potential, EXACTLY quadratic (W126, `V = (1/2) m_0^2 p^2`,
`m_0^2 = -1/4`, W130), which is a runaway with no minimum; the DBI gradient sector (W126/W159),
a SPEED LIMIT at `v^2 = 1/16` (`v_max = 1/4`) that caps VELOCITY and never EXCURSION; and the
everpresent / accretion drive (`Lambda ~ +/- 1/sqrt(N)`, `r(N) = kappa0 sqrt(N)`, W145/W164/W185/W187),
a MONOTONE destabilizing drive, not a restoring force. Build the phase portrait, find the fixed
points, classify them by eigenvalue, integrate the roll from the native initial condition, and
read the endpoint. The answer is a clean, computed, honest NEGATIVE result: **no stable
fixed-point true vacuum exists; the roll ends on a graceful de Sitter ROLLING attractor at the
validity edge; the arrow of time holds all the way.**

Five personas ran inline in one worker, sequentially, no sub-agents: (1) phase-portrait
analyst, (2) DBI-clock specialist, (3) attractor / Lyapunov-stability specialist, (4)
record-accretion-roll specialist, (5) ruthless skeptic. Deterministic test
`tests/W215_true_vacuum_dynamical_systems.py`, 22/22 exit 0, W126 + W130 + W159 + W166
positive controls first.

## 1. Construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md), named

| Object | Construction used | Why |
|---|---|---|
| Scalaron field | the conformal / scale / record-count mode `p`; `N = e^{4p}` (W153/W166/W145) | The tachyon lives here; the roll IS record-genesis. |
| Potential | EXACTLY quadratic `V = (1/2) m_0^2 p^2`, `m_0^2 = -1/4` (W126/W130) | The pinned hilltop; no minimum; `V'' = m_0^2` constant. |
| Kinetic cap | the DBI inverse-mass factor `(1 - (v/v_max)^2)^{3/2}`, `v_max^2 = 1/16` (W126/W159) | The speed limit; PORTED relativistic form on the W159 all-positive-coefficient fact. |
| Drive | everpresent `Lambda = c e^{-2p}` and accretion `r(N) = kappa0 sqrt(N)` (W145/W185) | The monotone record-accretion drive (W164), not a restoring force. |
| Native IC | small `p > 0`, `v = 0`: the empty / no-record FALSE vacuum near the saddle | Where the physical roll starts (W163's false vacuum). |

The dynamical-systems apparatus (fixed points, Jacobian linearization, saddle
classification, Lyapunov functions, terminal velocity) is STANDARD-FIELD, PORTED and
labelled; the objects it acts on are GU's.

## 2. Persona 1 -- phase-portrait analyst: the fixed points and their eigenvalues

Write the reduced homogeneous flow on `(p, v)`:

```
p' = v
v' = (1 - (v/v_max)^2)^{3/2} * [ F_drive(p) - Gamma v ]
```

`F_drive(p) = -V'(p) = -m_0^2 p + 2 c e^{-2p} = +p/4 + 2 c e^{-2p}` is the outward tachyonic
drive plus the everpresent force; `Gamma >= 0` is Hubble / everpresent drag; the DBI factor
`(1 - (v/v_max)^2)^{3/2}` is the relativistic inverse effective mass (it equals 1 at `v = 0`
and vanishes as `v -> v_max`, so it caps the velocity -- the W159 speed-limit fact, PORTED as
the standard DBI form).

**The origin is the only finite fixed point of the free flow (`c = 0`), and it is a SADDLE
(DS1).** A fixed point needs `v = 0` and `F_drive(p) = 0`, i.e. `-m_0^2 p = 0`, so `p = 0`:
the empty no-record false vacuum, and nothing else (DS1a). At `v = 0` the DBI factor is 1, so
the Jacobian is

```
J(0,0) = [[0, 1], [ -m_0^2, -Gamma ]],   det J = m_0^2 = -1/4 < 0.
```

A negative determinant is a hyperbolic SADDLE for EVERY `Gamma >= 0` (DS1b). The eigenvalues
are `lambda = (-Gamma +/- sqrt(Gamma^2 + 1))/2`; at `Gamma = 0` they are exactly `+/- 1/2`
(DS1c) -- reproducing the W166 growing-mode roots and pinning the linearization to the known
physics -- and their product is `det = -1/4 < 0` for all `Gamma`, so there is always one
growing and one decaying direction (DS1d). The native initial condition sits on the saddle;
its unstable manifold IS the roll. The field leaves the hilltop and `N = e^{4p}` grows.

> **There is no stable finite fixed point in the free flow. The one that exists is the
> false-vacuum saddle the universe rolls OUT of, exactly as W163 read it.**

## 3. Persona 2 -- DBI-clock specialist: the endpoint is a terminal-velocity attractor

On a minimum-free runaway hilltop, the drive `F_drive > 0` never vanishes as `p` grows (the
physical `c >= 0` branch), so an uncapped tachyon would give `p ~ e^{|m| tau}` and hence
`N ~ exp(4 e^{|m| tau})`, a genuinely pathological double-exponential blow-up. The DBI cap
forbids it. Integrating the flow from the native IC (`p = 10^{-3}`, `v = 0`, RK4):

- the velocity asymptotes to the cap, `v -> v_max = 1/4` (DS2a, `v_end = 0.250000`);
- late-time `p` is LINEAR in `tau`, `dp/dtau -> v_max` (DS2b, tail slope `0.24988`), so
  `N = e^{4p}` grows at a steady de-Sitter rate `N ~ e^{4 v_max tau}`;
- the velocity NEVER exceeds the cap for any `tau` (DS2c): a BOUNDED-velocity runaway, not a
  blow-up.

The endpoint of the roll is therefore a "fixed point at infinity": `p -> +inf`, `v = v_max`.
It is a de Sitter ROLLING attractor -- bounded VELOCITY, unbounded FIELD and VOLUME. The
speed limit is exactly what converts the explosive tachyonic runaway into a graceful,
constant-rate arrow of time (the relativistic-particle picture: constant proper force,
saturated velocity, linear position).

## 4. Persona 3 -- attractor / Lyapunov-stability specialist: attractor, no cycle, endpoint spectrum

**`v_max` is a genuine attractor of the reduced velocity flow (DS3a).** For fixed drive
`F > 0`, `dv/dtau = (1 - (v/v_max)^2)^{3/2} F`; the rate vanishes at `v = v_max` (approach
from below) and its slope just inside (`v = 0.2`) is `-144 F / 25 < 0`. So `v_max` is a stable
fixed point of the velocity dynamics: the terminal-velocity surface attracts.

**No limit cycle (DS3b).** `N = e^{4p}` (equivalently `p`, since `N` is monotone in `p`) is a
strict Lyapunov function: it increases along every forward trajectory with `v > 0`. A function
that strictly increases along the flow cannot return to its starting value, so no closed orbit
exists -- the roll has no oscillatory / limit-cycle endpoint. Verified: `p` is nondecreasing
over the whole integrated trajectory (`p: 0.001 -> 996.7`, min step `1.25e-8 >= 0`).

**The endpoint spectrum is NOT a stable-minimum spectrum (DS3c).** Because W126 proved the
potential is EXACTLY quadratic, `V''(p) = m_0^2 = -1/4` EVERYWHERE, including at the endpoint.
The curvature stays tachyonic all along the roll; there is no point where the spectrum becomes
that of a stable, real, bounded minimum. The endpoint is a rolling attractor, not a static
vacuum -- so "is the spectrum around it real and bounded or pathological" has the honest
answer: **there is no static point to expand around; the potential curvature is tachyonic
throughout, but the DBI cap makes the physical roll graceful (finite rate), not a blow-up.**

## 5. Persona 4 -- record-accretion-roll specialist: the arrow / DBI clock holds

**The arrow holds from saddle to attractor (DS4a).** `m^2 < 0 <=> N grows` (W166): `N = e^{4p}`
is strictly monotone along the ENTIRE flow (DS3b), so the record count advances everywhere and
the DBI clock reads forward -- it never stalls or reverses. At the attractor the rate is
`dN/dtau = 4 v_max N`: steady de Sitter e-folding, finite per e-fold, graceful (no
double-exponential).

**The drive decelerates (DS4b).** The everpresent `Lambda ~ 1/sqrt(N)` has `dLambda/dN < 0`,
so as records accrete the drive fades and vanishes as `N -> inf`: an asymptotically freezing,
self-weakening arrow, not an explosive one.

**The accretion law is a monotone drive, not an oscillator (DS4c).** `r(N) = kappa0 sqrt(N)`
(W185/W187) is monotone increasing, and `r * Lambda = kappa0` is constant -- the accretion and
the everpresent fade are the same object seen two ways. A monotone one-way process (W164) has
NO oscillatory restoring component at the pole frequency, which is precisely why the flow rolls
and never settles into a vacuum. This is the dynamical-systems restatement of the W164 result:
the coupled record dynamics is a destabilizing drive, not a stabilizing feedback.

## 6. Persona 5 -- ruthless skeptic: is there ANY stable fixed-point true vacuum?

The seductive move is to use the everpresent term to manufacture a finite stable minimum. Try
both branches; both fail.

**Physical branch `c > 0` (Lambda > 0, records accreting) -- pure runaway (DS5a).**
`V_tot'(p) = m_0^2 p - 2 c e^{-2p} < 0` for all `p >= 0` (the tachyonic term is already outward,
and `-2 c e^{-2p} < 0` adds to it). No fixed point at `p >= 0`; the field runs away
monotonically. The everpresent term is a fading correction, not a minimum-maker.

**Attractive branch `c < 0` (Lambda < 0) -- the fixed point is a MAXIMUM (DS5b).** Here a
finite fixed point DOES exist (`p* e^{2 p*} = 8|c|`, numerically `p* ~ 0.377`), which is the
tempting "true vacuum". But it is unstable: `V_tot''(p*) = m_0^2 + 4 c e^{-2p*}`, and at the
root `4 c e^{-2p*} = -p*/2`, so `V_tot''(p*) = -1/4 - p*/2 < 0`. It is a hilltop, not a well.
The reason is structural and un-evadable: W126 exact-quadraticity locks the bare curvature at
`m_0^2 = -1/4`, and a single decaying exponential cannot out-curve a parabola to make a
minimum. No stable vacuum exists on either branch.

**And the graceful attractor is validity-edge (DS5c).** The terminal velocity is `v_max`, so
`v^2 = 1/16` -- exactly the induced-metric degeneration point where the gradient rational
`W(v) = 2(...)/(16 v^2 - 1)^3` blows up (W126/W159). The drive scale `|m_0^2| = 1/4 = 4 x (1/16)`
sits four times beyond it. The attractor is read AT the boundary of EFT validity, not
comfortably inside. This is the SAME `4x` margin W159 route-2 and W163 found, now as the
location of the dynamical attractor itself.

> **Skeptic verdict: NO stable fixed-point true vacuum exists (neither everpresent branch). The
> endpoint is a graceful de Sitter ROLLING attractor sitting at the validity edge, not a static
> minimum. This is a REAL (negative) result, not a construction failure -- the flow is fully
> characterized; it simply has no vacuum to end on.**

## 7. Synthesis -- the four return fields

**Q1 -- does a true vacuum EXIST (a genuine attractor / fixed point)?** **A stable FIXED-POINT
vacuum: NO.** The only finite fixed point is the origin, a hyperbolic saddle (DS1); neither
everpresent branch produces a stable minimum (DS5a physical = runaway; DS5b attractive = a
maximum). **A genuine ATTRACTOR of the flow: YES**, but it is the DBI terminal-velocity surface
`v = v_max = 1/4` (DS2) -- a de Sitter ROLLING attractor, a "fixed point at infinity", not a
static vacuum. No limit cycle (DS3b: `N` is a strict monotone Lyapunov function).

**Q2 -- NATURE: endpoint scale + spectrum?** Endpoint: `p -> +inf`, `v -> v_max = 1/4`,
`N -> +inf` at steady de-Sitter rate `dN/dtau = 4 v_max N` (DS4a). Spectrum: `V''(p) = m_0^2 =
-1/4` EVERYWHERE (exact-quadratic, DS3c), so the curvature stays tachyonic; there is NO real,
bounded, stable-minimum spectrum to exhibit. But the roll is GRACEFUL (bounded velocity,
decelerating drive), not a pathological blow-up. So the honest reading of "real and bounded or
pathological" is a THIRD option: **neither a healthy static spectrum nor a blow-up -- a
graceful bounded-velocity de Sitter roll with a persistently tachyonic potential curvature.**

**Q3 -- does the arrow-of-time / DBI-clock reading (W166) HOLD there?** **YES.** `N = e^{4p}` is
strictly monotone along the entire flow from the saddle to the attractor (DS3b); the DBI clock
reads forward and is graceful-in-rate (DS4a); the everpresent drive fades (DS4b). The arrow
holds and the clock neither stalls nor reverses -- the W166 reading survives being built as a
flow.

**Q4 -- validity / needed extension?** The attractor sits AT the `v^2 = 1/16` induced-metric
degeneration edge (DS5c); the drive scale `|m_0^2| = 1/4` is `4x` beyond. A trustworthy endpoint
needs (i) the induced geometry / DBI kinetic sector BUILT past `v^2 = 1/16` (the completion the
derivative expansion cannot reach), and (ii) the promotion-gate source `T4` BUILT (W154/W158/W160,
the object that would source the record condensate). Both are UNBUILT. The dynamical-systems
build does not extend validity; it locates precisely where the extension is needed.

**Overall verdict: RUNAWAY-NO-VACUUM (graceful de Sitter rolling attractor; arrow holds;
validity-edge).** The roll has no stable fixed-point true vacuum. It ends on a bounded-velocity
de Sitter attractor -- a genuine attractor of the flow, graceful and arrow-preserving, but a
ROLLING endpoint, not a static minimum, and read at the edge of validity. This is the
truth-seeking negative result the charge anticipated: not "an unbounded blow-up with no
attractor" (the DBI cap prevents that), and not "a healthy static vacuum" (exact-quadraticity
prevents that), but the honest middle -- **a graceful runaway to a de Sitter rolling attractor
outside certified validity.**

**Effect on bar (b): UNCHANGED.** The flaw count does not drop and no debit is added. This wave
BUILDS the object W163 named a candidate (the record-condensed true vacuum) and confirms, as a
flow, that it is not a stable vacuum: the endpoint is a rolling attractor, out of validity, and
the source that would anchor it is unbuilt. Debit-1 stands as the tachyonic scalaron whose roll
ends on a graceful de Sitter attractor rather than a static true vacuum.

## 8. Convergence note (agreement / disagreement with the other four methods)

This is one of five independent teams building the same record-condensed true vacuum by a
different method, to test convergence. Reading this dynamical-systems build against the
established results the sibling methods share:

- **Agreement with the non-perturbative / effective-potential method (W163).** W163's
  Coleman-Weinberg build found the perturbative point is a FALSE vacuum (`Im V_eff != 0`) whose
  true-vacuum candidate is out-of-validity (`4x` past `v^2 = 1/16`), unbuilt at the source (T4),
  and MONOTONE-ROLLING rather than stationary. This method converges exactly: the false vacuum
  is the origin saddle (DS1), the true-vacuum candidate is out of validity at the same `4x`
  margin (DS5c), and "monotone-rolling not stationary" is precisely the de Sitter rolling
  attractor (DS2/DS4). Two different builds, same endpoint character.

- **Agreement with the coupled matter/record (backreaction) method (W164).** W164 found the
  record-accretion promotion is a MONOTONE destabilizing drive, not a restoring feedback, and
  cannot stabilize the tachyon. Here that is the reason the flow has no vacuum: `r(N) = kappa0
  sqrt(N)` is monotone with no oscillatory restoring component (DS4c), so the roll never settles.

- **Agreement with the arrow-of-time / engine method (W166).** W166 argued the DBI cap
  linearizes the runaway to a steady de-Sitter-rate `N` and the everpresent fade decelerates the
  drive, graceful-in-rate but validity-edge. Built as a flow, that is exactly the terminal-
  velocity attractor (DS2), the `dN/dtau = 4 v_max N` rate (DS4a), the fading drive (DS4b), and
  the validity-edge caveat (DS5c). The eigenvalues `+/- 1/2` (DS1c) reproduce W166's growing mode.
  The dynamical-systems method upgrades W166's "graceful engine, PLAUSIBLE" to a computed flow
  statement: graceful, YES, and now with the attractor located and classified -- and still
  validity-edge, so still not a certified vacuum.

- **Expected agreement with the remaining sibling method (thermodynamic / free-energy or
  RG-flow).** A free-energy method should find no stable minimum (the potential is unbounded
  below with `V'' < 0` everywhere, DS3c/DS5b); an RG method should find no sign-flip that opens a
  well (W119/W163 NO-SIGN-FLIP-ON-AF). Both are consistent with RUNAWAY-NO-VACUUM.

**Convergence verdict: CONVERGENT on the negative result.** All methods that have been built
agree that GU has NO stable fixed-point true vacuum within validity; the honest positive content
is that the roll is GRACEFUL (a bounded-velocity de Sitter attractor with the arrow intact), not
a blow-up. The single point where a method could DISAGREE and change the verdict is the
extension past `v^2 = 1/16` (the DBI / induced completion) together with the built promotion-gate
source T4 -- the same unbuilt paper-scale objects every method reduces to. No method exhibits a
stable vacuum; the dynamical-systems method adds that even the attractor that DOES exist is a
rolling one, at the edge, not a minimum.

## 9. Gates and honest limits

- Exploration grade; conditional register throughout. Nothing asserts GU, asserts a vacuum, or
  changes any verdict. The result is a computed characterization of a reduced flow, with
  W126 + W130 + W159 + W166 positive controls, not an assertion about the world.
- The DBI inverse-mass factor `(1 - (v/v_max)^2)^{3/2}` is the STANDARD relativistic form,
  PORTED; the phase-portrait / Jacobian / Lyapunov apparatus is standard-field, PORTED. The
  load-bearing GU content is the exact-quadratic potential (W126, `V'' = m_0^2` everywhere) and
  the all-positive-coefficient speed-limit structure (W159), both imported and reproduced as
  positive controls. The reduced model is not a first-principles native induced-metric
  derivation of the `(p, v)` inertia; the qualitative attractor structure (saddle + terminal-
  velocity attractor + no cycle + no stable minimum) rests on the imported facts, not on the
  specific porting.
- W138 battery honored: the everpresent / accretion drive routes through the ported
  `Lambda ~ 1/sqrt(N)` (W145) and `r(N) = kappa0 sqrt(N)` (W185), carrying their standing G5 de
  Sitter debit; the reframe claims no new normalization and re-imports no killed identity as
  truth. The tempting overreaches the geometry forbids are recorded: "the roll ends in a stable
  true vacuum" (forbidden -- DS5a/DS5b, no stable minimum on either branch); "the everpresent
  term makes a minimum" (forbidden -- DS5b, it makes a maximum); "the graceful attractor is
  certified" (forbidden -- DS5c, validity edge).
- Tri-repo gating held: record / finality / arrow-of-time / count / promotion semantics stay
  pointers to temporal-issuance / time-as-finality / TaF; GU owns the induced-action math and the
  flow structure only. No cross-repo identity asserted.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count is unchanged;
  H59/H61a OPEN; the E2 AF/AS fork carried, not closed. No forbidden target
  {3, 8, 24, chi(K3), Ahat} assumed or inserted. Zero em dashes in paper-facing text.

*Filed 2026-07-14 by Team DYNAMICAL-SYSTEMS / ROLLING (W215), one of five independent
convergence teams. Coherence-first: BUILD the true vacuum as a flow, then let the skeptic read
the endpoint honestly. Five personas inline in one worker (phase-portrait analyst; DBI-clock
specialist; attractor / Lyapunov-stability specialist; record-accretion-roll specialist;
ruthless skeptic); no sub-agents. Reproducible:
`python -u tests/W215_true_vacuum_dynamical_systems.py` (22/22, exit 0; W126 + W130 + W159 + W166
positive controls first). Exploration grade; conditional register; no canon movement. Verdict:
RUNAWAY-NO-VACUUM (graceful de Sitter rolling attractor; arrow holds; validity-edge).*
