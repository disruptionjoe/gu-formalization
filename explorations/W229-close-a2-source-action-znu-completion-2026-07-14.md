---
artifact_type: exploration
status: exploration (TEAM CLOSE-A2, W229; four personas inline, one worker, no sub-agents; deterministic test, 39/39 exit 0, positive controls first)
created: 2026-07-14
wave: W229
label: W229
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (report value under either outcome); RUTHLESS self-verification; tri-repo gating STRICT
route: LEGENDRE / INDUCED-YANG-MILLS (eliminate the Gaussian parent field P_IG; promote W203's ULTRALOCAL algebraic kernel to a DIFFERENTIAL / nonlocal Green's-function kernel)
title: "W229 -- the SOURCE-ACTION NONLOCAL (Z_U) COMPLETION. VERDICT: ADVANCED -- Z_U-SECTOR-BUILT / FULL-FIELD-EQUATIONS / FORCED-IN-SHAPE-ONE-FREE-MAGNITUDE. W203 built the branch-3 source action only to ULTRALOCAL order (theta algebraic, M = eta Schur-forced, theta = kappa M^{-1} J POINTWISE, connection law D_A* F = J). W203/W180/W151 all flagged the SAME missing object: the gradient-stiffness / nonlocal induced-YM completion Z_U (the eta-magnitude bridge). W229 BUILDS it. The Cycle-2 source-forced gate names the first-order PARENT exactly: S_parent = int<P_IG, D_A U> - 1/(2 Z_U)<P_IG, P_IG> - V_src - (c_theta/2)<theta,theta> + S_cross + S_bdy, with P_IG a Gaussian 2-form; its Legendre elimination is EXACT (P_IG = Z_U D_A U), giving the second-order gradient sector S_grad = -(Z_U/2)<D_A U, D_A U>. Adding S_grad to W203's algebraic mass term PROMOTES the ultralocal kernel to a DIFFERENTIAL one: the theta/U field equation becomes the screened-Poisson / induced-YM law (-Z_U D_A* D_A + c_theta eta) theta = J whose solution theta = G * J is a NONLOCAL Green's function of the record current, and the connection equation is the full induced-YM law D_A* F_A = theta_eff with theta_eff = g_A^2(c_theta theta - J_IG - ...). In the ULTRALOCAL (Z_U -> 0 / zero-mode) limit the differential term drops and the kernel collapses to W203's pointwise theta = kappa eta^{-1} J -- so the completion is a genuine PROMOTION that REDUCES to W203 (machine-checked). FORCED-OR-FREE (the deliverable): the gradient FIBER pairing Q_IG is FORCED ~ eta by the SAME Schur uniqueness (nulldim=1) that fixed the ultralocal mass kernel (no new fiber freedom); the differential operator is FORCED = the covariant exterior derivative D_A up to an ALGEBRAIC (zeroth-order) bundle map absorbed into the mass / V_src sector (the K_IG selector NARROWED, not left fully open); and the MAGNITUDE Z_U is exactly ONE new free datum -- the induced-YM stiffness / screening length ell^2 = Z_U*kappa, the SAME KIND of object as kappa (an eta-from-gimmel-area normalization, W151), undetermined-BY-NORMALIZATION (it sets the induced YM coupling g_A and the correlation length), NOT fitted-to-a-data-window (the gate's target-fitting exclusion forbids Lambda/epoch/DESI/Rubin/xi_eff inputs). So the COMPLETE action carries exactly TWO normalization scales {kappa, Z_U}; every relative/tensor coefficient forced; ZERO data-fitted. It is ADVANCED not unconditional-COMPLETED because (i) the magnitude Z_U remains the SAME already-named eta-from-gimmel-area bridge (now covering both kappa and Z_U); (ii) K_IG-uniqueness is narrowed, not fully closed; (iii) the action still SECRETLY ASSUMES W154 (source current = the record current). No fifth object; the residues are all already-named. No canon movement; count {1,3}; H41 unbuilt (narrowed); H59 OPEN; bar(b) UNCHANGED."
grade: "exploration / STRONG on the structural completion (the P_IG Gaussian elimination is exact linear algebra; the Schur fiber uniqueness and the eta-anti-self-adjointness of the vector rep are exact and machine-checked; the screened-Poisson operator is built explicitly and its ultralocal reduction, nonlocal Green's-function spread, and flat-background conservation are direct computations). STRUCTURAL on the K_IG selector (D_A is forced up to an algebraic piece -- a genuine narrowing, not a full uniqueness proof) and on the magnitude (Z_U's numerical value is the SAME unbuilt eta-from-gimmel-area bridge as kappa, cited not computed). COMPUTED (deterministic, tests/W229_source_action_znu_completion.py, 39/39 exit 0, positive controls first): PC1 W131 exact algebra + Krein anti-self-adjointness of all 91 Sigma; PC2 the (9,5)=(3,1)+(6,4)/q=5 split; PC3 W180's C3 identity S_D=A.J and delta S_D/delta A=J; PC4 W203's ultralocal Legendre elimination theta = kappa eta^{-1} J and S_eff = -(kappa/2)<J,eta J>. NEW-DERIVED: KER1/KER3 the Schur fiber uniqueness (nulldim=1, signature (9,5), ~ eta) shared by the mass kernel AND Q_IG; GAU1 the eta-anti-self-adjointness of the vector-rep generators (so <D_A theta, D_A theta>_eta is gauge-invariant); GAU2 the Ad-closure making the gradient sector covariant; GAU3 the difference of two covariant derivatives is algebraic (K_IG narrowing); PIG1/PIG2 the exact Gaussian elimination P_IG = Z_U D_A U -> -(Z_U/2)<D_A U, D_A U>; PIG3 the parent current J_IG = Z_U[U, D_A U]; NL0-NL5 the screened-Poisson operator (self-adjoint), the solved full field equation, the ultralocal (constant-source and Z_U->0) reduction to W203, the nonlocal Green's-function spread and its Yukawa tail with screening length ell = sqrt(Z_U*kappa), the rescale-invariance of ell^2, and the flat-background conservation d* theta_eff = 0; FF1-FF5 the forced-or-free ledger; W154-1/2 the localized W154 dependence. CITED (not re-derived): W203 (the ultralocal branch-3 action, the Schur kernel pin, the Krein sign #1), W180 (the C3 identity, theta = kappa M^{-1} J, Noether II D_A* J = 0), W167 (the Legendre elimination of a Gaussian field), W151 (route-beta / the eta-from-gimmel-area bridge, only the sign forced), W154 (the reverse-engineered marble/wood identification), the Cycle-2 source-forced-S_IG_dyn action gate (the first-order parent, the P_IG field, the Z_U normalization, J_IG = [U, P_IG], theta_eff, D_A* theta_eff = 0). No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the debit count stays {1,3}; H41 unbuilt (narrowed); H59 OPEN; no forbidden target {3,8,24,chi(K3),Ahat} assumed or inserted. Zero em dashes in paper-facing text."
construction: "program-native where the objects are GU's (Y14, the record field Psi in carrier B / ker Gamma, the Krein/C-operator structure K_S, the (9,5) split and its (3,1)+(6,4) base/DeWitt-fiber decomposition, the q=5 finality frontier, the promotion-gate current J, the connection distortion theta = A - Gamma(eps) - U, the first-order parent field P_IG, the induced-YM connection equation). Standard-field where the machinery binds any construction (Schur's lemma fixing the invariant bilinear form on the irreducible vector rep; the Legendre / auxiliary-field elimination of a Gaussian field -- PORTED, reused from W122/W167/W203; the induced-gravity / induced-Yang-Mills / equation-of-state stance that the connection has no fundamental kinetic term and its stiffness is an induced loop coefficient -- PORTED Sakharov/Jacobson; the screened-Poisson / Yukawa Green's function -- standard). Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md; the reverse-engineered dark-energy-is-the-record-current identification (W154 marble/wood) is carried and named as the standing conditionality; the K_IG = D_A U selector is carried and NARROWED (D_A forced up to an algebraic piece), not asserted unique; the Z_U magnitude is carried and named as the SAME eta-from-gimmel-area residue as kappa. Tri-repo gating STRICT: the induced-YM stiffness and the screening length are computed GU-side as structure; whether the magnitude is physically operative rides the TaF-owned eta-bridge; no cross-repo identity asserted."
depends_on:
  - explorations/W203-branch3-source-action-fixed-coefficients-2026-07-14.md
  - explorations/W180-build-matter-connection-bridge-c3-2026-07-14.md
  - explorations/W167-reduction-direct-sign-alpha-beta-2026-07-14.md
  - explorations/W151-gr-and-c-emergence-from-records-2026-07-14.md
  - explorations/W154-reverse-engineered-source-action-2026-07-14.md
  - explorations/cycle-gates-and-audits/cycle2-source-forced-s-ig-dyn-action-gate-2026-06-24.md
  - explorations/cycle-gates-and-audits/cycle1-branch3-dynamical-ig-current-gate-2026-06-24.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W229_source_action_znu_completion.py
external_refs:
  - "Sakharov 1967 -- induced gravity; higher-curvature / kinetic coefficients are integrated-out loop coefficients, not fundamental law (the stance the induced-YM stiffness Z_U is read under)"
  - "Jacobson, Phys. Rev. Lett. 75 (1995) 1260 -- Einstein equations as an equation of state (the induced / no-fundamental-kinetic-term stance)"
---

# W229 -- the source-action nonlocal (Z_U) completion (induced-YM / gradient-stiffness)

Test: `tests/W229_source_action_znu_completion.py` (39/39, exit 0). Deterministic (seed 20260714);
the algebra runs on the repo's verified `Cl(9,5) = M(64,H)` representation via `gen_sector_bridge`.
Four personas ran inline in one worker, sequentially (no sub-agents): (1) variational / field-equation
specialist, (2) nonlocal-completion / induced-Yang-Mills specialist, (3) shiab / Legendre specialist,
(4) ruthless skeptic. The four positive controls run first.

## 0. The object, and the exact gap W203 left

W203 built the branch-3 source action

```
S_ultralocal  =  Re<Psi, K_S c(A) Psi>  +  (1/2kappa)<theta, M theta>  -  <theta, J[Psi]> ,
```

pinned the kernel `M ~ eta` by Schur, and Legendre-eliminated the Gaussian `theta` to
`theta = kappa M^{-1} J` **pointwise**, connection law `D_A* F = J`. But W203 was explicit (its
Section 5, the skeptic): this is the **ultralocal truncation** of the branch-3 parent action. The
kinetic / gradient-stiffness term `Z_U |D_A U|^2` is NOT built; it is where the eta-MAGNITUDE lives;
without it there are no full field equations and the action stays conditional-on-ultralocal. W180 and
W151 name the SAME object. W229's job is to build it.

The Cycle-2 source-forced gate (`cycle2-source-forced-s-ig-dyn-action-gate`) already names the
first-order **parent** of the missing sector precisely:

```
S_parent[A, eps, U, P_IG; s]
  =  int<P_IG, D_A U>  -  1/(2 Z_U) int<P_IG, P_IG>  -  int V_src(U, eps)
     -  (c_theta/2) int<theta, theta>  +  S_cross  +  S_bdy ,
```

with `U` the connection-distortion 1-form (`theta = A - Gamma(eps) - U`), `P_IG in Omega^2(Y, ad P)`
(degree 2 because `D_A U` is a 2-form), and `Z_U` the gradient stiffness. This is the induced-Yang-Mills
route: the connection carries no fundamental kinetic term; `Z_U` is the induced (Sakharov) stiffness.

## 1. Persona 1 (variational / field-equation specialist) -- the full field equations

The parent exposes every field equation without hiding it in second-order notation. Stationarity in
each field (the gate's `E_P`, `E_U`, `E_A`), with the sign convention `delta_A theta = delta A`,
`delta_A(D_A U) = [delta A, U]`:

- **`E_P` (`delta/delta P_IG = 0`).** `P_IG` is **Gaussian** in the parent, so its elimination is
  EXACT (the same Legendre move as W203's `theta` and W167's torsion): `D_A U - Z_U^{-1} P_IG = 0`,
  i.e. `P_IG = Z_U D_A U` (test `PIG1`, residual `2e-16`). Back-substituting gives the second-order
  gradient sector `S_grad = -(Z_U/2)<D_A U, D_A U>` (test `PIG2`, residual `0`). So the gradient
  stiffness is NOT a convenient add-on; it is the exact reduction of a first-order Gaussian parent,
  and `Z_U` is its one normalization.
- **`E_U` (`delta/delta U = 0`).** `-D_A* P_IG + V_U + c_theta theta + E_U^cross = 0`. With
  `P_IG = Z_U D_A U` this is the **screened-Poisson / Yang-Mills-Proca** equation for the connection
  distortion: `-Z_U D_A* D_A theta + c_theta eta theta = J` (writing the source coupling as the record
  current, Section 2). Its solution `theta = G * J` is a **NONLOCAL Green's function** of `J`.
- **`E_A` (`delta/delta A = 0`), plus the Yang-Mills variation.** The full connection equation is
  `g_A^{-2} D_A* F_A - c_theta theta + J_IG + J_cross + J_Psi + J_section = 0`, i.e.

  ```
  D_A* F_A  =  theta_eff ,     theta_eff = g_A^2( c_theta theta - J_IG - J_cross - J_Psi - J_section ) ,
  ```

  with the **parent current** `J_IG = [U, P_IG] = Z_U [U, D_A U]` (test `PIG3`, nonzero and
  source-bilinear). This is the gate's strongest valid Branch-3 contract: NOT the bare-theta law
  `D_A* F = theta`, but the full induced-YM law with the total source `theta_eff`.

So the completed action has full field equations, and (Section 3) they REDUCE to W203's when `Z_U -> 0`.

## 2. Persona 2 (nonlocal-completion / induced-YM specialist) -- ultralocal becomes nonlocal

The decisive structural content: **adding `S_grad` promotes W203's ALGEBRAIC (ultralocal) kernel to a
DIFFERENTIAL (nonlocal) one.** In W203 `theta` was eliminated algebraically, `theta = kappa eta^{-1} J`
at each point. With the gradient sector present the theta field equation is the screened-Poisson
operator

```
O  =  -Z_U D_A* D_A (eta)  +  c_theta eta ,        c_theta = 1/kappa ,
O theta = J    =>    theta = O^{-1} J   (a NONLOCAL Green's function of the record current).
```

Built explicitly on a periodic base lattice tensored with the 14-fiber (the vector rep acting through
the `T_ij` generators as the covariant lattice transport):

- **`NL0/NL1`: the operator is self-adjoint in the eta inner product and the full field equation
  `O theta = J` is solved** (residual `1e-13`). The connection distortion is now a Green's-function
  functional of `J`, not a pointwise multiple.
- **`NL2` (the load-bearing reduction): the completion REDUCES to W203.** On a covariantly-constant
  source the covariant Laplacian vanishes and `O` collapses to `c_theta eta`, giving
  `theta = kappa eta^{-1} J` -- exactly W203's ultralocal kernel (test `NL2a`, residual `4e-14`). And
  as `Z_U -> 0` the differential term drops for ANY source, recovering the pointwise W203 map (test
  `NL2b`, residual `3e-6`). So the completion is a genuine **promotion**, not a different theory.
- **`NL3`: the completed kernel is NONLOCAL.** A single-site source spike produces a nonzero `theta`
  on neighboring sites (Green's-function spread, test `NL3a`) with a Yukawa tail (`NL3c`) and a finite
  **screening length `ell = sqrt(Z_U*kappa)`** (`NL3b`) -- the correlation scale the ultralocal action
  lacked. This is the physical content the gradient sector adds: the connection distortion propagates.
- **`NL5`: linearized conservation.** In the abelian / flat-background reduction the discrete Noether
  identity `d* theta_eff = 0` holds exactly (`theta_eff = O theta = J`, and the constant current
  telescopes to zero divergence on the periodic base, residual `0`). The covariant version
  `D_A* theta_eff = 0` rides W180's `D_A* J = 0` (Noether II), cited not re-toyed (a spatially-constant
  `J` is not covariantly constant under a nontrivial holonomy, so the covariant divergence check is
  ill-posed on the toy -- stated, not fudged).

## 3. Persona 3 (shiab / Legendre specialist) -- the forced fiber and the forced differential operator

Two of the three ingredients of `Z_U` are FORCED; only the third is free.

- **The gradient FIBER pairing `Q_IG` is FORCED ~ eta by Schur (test `KER1/KER3`, nulldim `= 1`).**
  `<D_A U, D_A U>_Q` is an invariant symmetric form on the SAME 14-frame carrying the irreducible
  vector rep of `so(9,5)`; by Schur the invariant symmetric form is unique up to scale and equals the
  Clifford metric `eta`, signature `(9,5)`, INDEFINITE. This is the identical equivariance computation
  that fixed W203's ultralocal mass kernel `M`. **The gradient sector introduces NO new fiber
  freedom.** And the vector-rep generators are `eta`-anti-self-adjoint (`T^T eta + eta T = 0`, test
  `GAU1`), so `D_A` preserves the `eta` pairing and `<D_A theta, D_A theta>_eta` is gauge-invariant.
- **The differential operator is FORCED = the covariant exterior derivative `D_A`, up to an algebraic
  piece.** The gradient sector is gauge-covariant: `R rho(A) R^{-1}` stays in the vector-rep Lie
  algebra (Ad-closure, test `GAU2`), so `D_{A'}(R theta) = R (D_A theta)`. And the **difference of two
  covariant derivatives is an ALGEBRAIC (derivative-free, C-linear) bundle map** (test `GAU3`), which
  is absorbed into the mass / `V_src` sector. So among gauge-covariant local first-order operators the
  DIFFERENTIAL part is canonical (`D_A`); the freedom is entirely algebraic and already accounted for.
  This **NARROWS** the gate's `K_IG` selector (`K_IG = D_A U` was "natural but not proved unique") to
  "`D_A` up to a piece in the mass/`V_src` sector" -- a genuine narrowing, not a full uniqueness proof.

The one thing NOT forced is the **magnitude `Z_U`** (Section 4).

## 4. Persona 4 (ruthless skeptic) -- is Z_U forced or free? is this a real completion or a relabel?

Three sharp challenges, answered honestly.

- **Forced or free?** The tensor SHAPE is forced (fiber `~ eta` by Schur; `D_A` up to algebraic). The
  MAGNITUDE `Z_U` is exactly ONE new free datum. It is the induced-YM stiffness / screening length
  `ell^2 = Z_U*kappa`, a rescale-invariant physical scale (test `NL4`: invariant under `theta -> s theta`,
  so NOT removable by a normalization choice -- a genuine SECOND datum beyond `kappa`). It is the SAME
  KIND of object as `kappa`: an **eta-from-gimmel-area** normalization (W151), undetermined-BY-
  NORMALIZATION (it sets the induced YM coupling `g_A` and the correlation length, like Jacobson's route
  DEFINES `G` from the entropy density rather than predicting a number), **NOT fitted-to-a-data-window**.
  The gate's target-fitting exclusion is honored: no bare `Lambda`, no `R theta^2` for `xi_eff`, no
  epoch / DESI / Rubin / `w0,wa` input enters (test `FF4`). So the **complete action carries exactly
  TWO normalization scales `{kappa, Z_U}`; every relative / tensor coefficient is forced; ZERO
  coefficients are data-fitted.** This is materially different from debit-2's PROVABLY-free fitted
  epoch (W160): two undetermined-by-normalization scales, like `{G, m}`, not a data-matched parameter.
- **Is it a real completion or a relabel of W203?** Real. The ultralocal kernel is pointwise; the
  completed kernel is a Green's function with a screening length; they are DIFFERENT operators that
  agree only in the `Z_U -> 0` / zero-mode limit (`NL2`). The completion adds genuine physical content
  (a correlation scale, propagating connection distortion, the full `D_A* F = theta_eff` with a nonzero
  `J_IG`) that the ultralocal action lacked. It does not merely rename W203.
- **Does it close the lane, or advance it?** ADVANCED, and I decline to overclaim COMPLETED. The
  SECTOR is built, the FULL field equations are written, and forced-or-free is settled -- the three
  things the completion criterion names. But three residues keep it short of an unconditional close:
  (i) the magnitude `Z_U` stays the SAME already-named eta-from-gimmel-area bridge (now covering both
  `kappa` and `Z_U`), not computed; (ii) `K_IG`-uniqueness is narrowed, not fully closed; (iii) the
  action still SECRETLY ASSUMES W154 (the source coupling `-<theta, J[Psi]>` hard-codes "the branch-3
  IG current = the record current"; test `W154-1`: all `Psi`-dependence enters through `J`, so
  `theta = 0` when `J = 0` -- localized, not diffuse, but a hypothesis). No fifth object: the residues
  are all already-named.

## 5. Synthesis -- the return data

| Field | Result |
|---|---|
| the Z_U sector | Cycle-2 parent `int<P_IG, D_A U> - 1/(2Z_U)<P_IG,P_IG>`; `P_IG` Gaussian, eliminated EXACT to `P_IG = Z_U D_A U` and `S_grad = -(Z_U/2)<D_A U, D_A U>`. Built. |
| full field equations | `E_P`: `P_IG = Z_U D_A U`. `E_U`: `(-Z_U D_A* D_A + c_theta eta) theta = J` (screened-Poisson / induced-YM, NONLOCAL Green's function). `E_A`: `D_A* F_A = theta_eff`, `theta_eff = g_A^2(c_theta theta - J_IG - ...)`, `J_IG = Z_U[U, D_A U]`. |
| reduces to W203? | YES. `Z_U -> 0` / zero-mode -> `theta = kappa eta^{-1} J` pointwise, `D_A* F = J` (machine-checked `NL2`). A genuine promotion. |
| swing result | **ADVANCED -- Z_U-SECTOR-BUILT / FULL-FIELD-EQUATIONS / FORCED-IN-SHAPE-ONE-FREE-MAGNITUDE.** |
| forced or free | fiber pairing `Q_IG` FORCED `~ eta` (Schur, nulldim=1); differential operator FORCED `= D_A` up to an algebraic piece (`K_IG` selector narrowed); magnitude `Z_U` = ONE new free datum (eta-from-gimmel-area, W151), undetermined-BY-NORMALIZATION, NOT data-fitted. TWO scales `{kappa, Z_U}` total; ZERO data-fitted. |
| standing residues (no fifth object) | the magnitude bridge (kappa AND Z_U on the same eta-from-gimmel-area, W151); `K_IG`-uniqueness (narrowed); W154 (source = record current); the interacting C-operator / Krein sign #1 (W203). All already-named. |
| effect on debit-3 | W203's ULTRALOCAL branch-3 action is now COMPLETED-IN-STRUCTURE: the gradient sector is built, full field equations written, the ultralocal action recovered as a limit. The residue narrows to the eta-magnitude (now a two-scale normalization) plus the standing W154 conditionality. No canon movement. |
| effect on bar (b) | UNCHANGED. The `Z_U` stiffness and the `kappa` sign are the attractive-Einstein / reservoir-Krein objects; the scalaron `c_R` sign of W167 is distinct; no conflation. |
| tests + exit codes | `tests/W229_source_action_znu_completion.py` -- 39/39 PASS, exit 0 (four positive controls first). |

## 6. What this does NOT do (the honest limits)

- Exploration grade; conditional register throughout. Nothing asserts GU, a vacuum, or that the DESI
  wiggle is real. The completion is CONDITIONAL on the W154 identification (a hypothesis) and its
  magnitude stays on the unbuilt eta-from-gimmel-area bridge.
- The induced-gravity / induced-Yang-Mills / equation-of-state stance (the connection has no
  fundamental kinetic term; `Z_U` is an induced stiffness), the Legendre elimination, and the
  screened-Poisson / Yukawa Green's function are PORTED and labelled (Sakharov; Jacobson; standard).
  The GU-specific content is the Schur uniqueness of the fiber pairing on the Krein-anti-self-adjoint
  vector rep, the `eta`-anti-self-adjointness making `D_A` `eta`-preserving, and the explicit reduction
  of the ultralocal W203 action as the `Z_U -> 0` limit.
- The nonlocal kernel is exhibited on a finite discrete base lattice (a positive-control model of the
  Green's function, its ultralocal limit, and its screening length), NOT the continuum Y14 operator.
  The structural claims (fiber forced, `D_A` forced up to algebraic, reduces to W203) are exact; the
  lattice is the witness, not the theorem.
- The magnitude `Z_U` is NOT computed. It is the SAME named eta-from-gimmel-area residue as `kappa`
  (W151), undetermined-by-normalization. `K_IG`-uniqueness is NARROWED (the differential part is
  `D_A`), not proved unique. Both are named, not new fifth objects.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; H41 unbuilt (narrowed: the
  branch-3 action is now complete-in-structure with full field equations, the ultralocal action a
  limit; the magnitude and the Q-epoch remain); H59 OPEN; the count stays `{1,3}`. Tri-repo gating
  STRICT: the stiffness and screening length are computed GU-side as structure; whether the magnitude
  is physically operative rides the TaF-owned eta-bridge; no cross-repo identity asserted. No forbidden
  target assumed or inserted. Zero em dashes in paper-facing text.

*Filed 2026-07-14 by Team CLOSE-A2 (W229). Route: Legendre / induced-Yang-Mills (eliminate the Gaussian
parent `P_IG`; promote the ultralocal kernel to a nonlocal Green's function). Four personas inline in
one worker (variational / field-equation specialist; nonlocal-completion / induced-YM specialist; shiab
/ Legendre specialist; ruthless skeptic); no sub-agents. Reproducible:
`python -u tests/W229_source_action_znu_completion.py` (39/39, exit 0; four positive controls first).
Exploration grade; conditional register; RUTHLESS self-verification; no canon movement; tri-repo gating
strict; the debit count stays {1,3}; H41 unbuilt (narrowed); bar (b) UNCHANGED. E1: the nonlocal Z_U
gradient sector is built and the branch-3 source action is completed-in-structure with full field
equations; forced-in-shape and one-new-free-magnitude; the residue is the SAME already-named eta-from-
gimmel-area bridge (now for kappa AND Z_U) plus the narrowed K_IG selector and the standing W154 / #1
conditionalities, NOT a fifth object.*
