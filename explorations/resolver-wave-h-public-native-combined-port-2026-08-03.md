---
artifact_type: exploration
created: 2026-08-03
title: "A chosen-J moving source-port fixture exists; ownership and actual Theta_Z descent are next"
grade: "Exact sparse Gaussian-rational Cl(9,5) algebra, numerical 128x128 Krein/right-H controls, independent Sage dimension arithmetic, one combined tilted-source/projector fixture, and an exact first-jet moving-projector derivative. The chosen J is an extra local U/Sp-type reduction field across a source real-form fork; its ownership is source-silent. Actual Met(X) Theta_Z overlaps, global lift, source-action variation, density/Krein domain, complete Euler/Ward/Green system, and observation no-leakage remain open."
named_gate: RESOLVER-WAVE-H-PUBLIC-NATIVE-REDUCTION-THETA-Z-DESCENT-AND-TOTAL-EULER
gate_before: PARTIAL_NATIVE_Q6_AND_LOCAL_TILTED_SCHEMA_CONSTRUCTED
gate_after: LOCAL_CHOSEN_J_MOVING_REDUCTION_AND_COMBINED_PORT_FIXTURE
route_disposition: CONTINUE
source_collision: SOURCE-CONFIRMS-PUBLIC-U-AND-TRACE-REVERSED-NATIVE-INSTRUCTIONS-SOURCE-SILENT-ON-THE-BRIDGE
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
third_lane_promoted: false
---

# Resolver Wave H: chosen-J moving source-port fixture

## Result first

Wave H constructs the local composite that Wave G left formula-only. It does
**not** construct the missing bundle-level or source-owned reduction. On one
fixed complex spinor carrier `(S_C,K,J_red)`, choose

\[
 \rho_J(A)=J\overline A J^{-1},
 \qquad
 R_J(A)=\frac12(A+\rho_J(A)).
\]

Restricted to the explicitly typed real public Krein algebra

\[
 \mathfrak u_K=\{A:A^\times=-A\},
\]

the intersection of the fixed locus of `rho_J` with that public algebra is
the chosen native right-quaternionic algebra

\[
 \mathfrak{sp}_J=\mathfrak u_K^{\rho_J}.
\]

The real dimensions match in two independent ways:

\[
 \dim_{\mathbb R}\mathfrak u(64,64)=128^2=16384,
\]

\[
 \dim_{\mathbb R}\mathfrak{sp}(32,32;\mathbb H)
 =64(2\cdot64+1)=8256,
\]

and the complementary public directions have dimension `8128`. In the
Clifford basis, the native K-anti grades are

\[
 \{2,3,6,7,10,11,14\},
\]

while the public complement is `i` times the K-self grades

\[
 \{0,1,4,5,8,9,12,13\}.
\]

Consequently `R_J` keeps the former and removes the latter. Coefficient
conjugation on the unrestricted complex Clifford algebra has a much larger
fixed set; a real complementary blade is rejected as ill-typed. The
128-by-128 numerical controls verify `J_red conjugate(J_red)=-1`, Krein
compatibility, inertia `(64,64)`, the phase typing of all 15 grade
representatives, `rho_J^2=1`, `R_J^2=R_J`, K-anti-adjointness, and zero
right-`H` defect on the image.

This closes a **chosen local reduction fixture**, not the Layer-0 identity between
the 2021 public `U(64,64)` presentation and the repo-native
`Sp(32,32;H)` geometry. The source ledger already calls those objects a
`REAL-FORM-FORK`, and the bounded source audit does not construct or assign
dynamics to `J_red`. It is an extra local `U/Sp`-type reduction field. It is
not P1/P2/P3, `epsilon_src`, `epsilon_IG`, or `Theta_Z`.

At a fixed chimeric coindex split, the complete projector is

\[
 \Pi_{252}^0
 =\iota_6\,j_5\frac19\pi_{\Lambda^5V^*}\delta\,q_6\,R_J.
\]

Here `iota_6` is explicit Chevalley reinclusion into the adjoint-valued
one-form. Its public one-form domain has real dimension `229376`, its native
intermediate has dimension `115584`, and its final rank/kernel are

\[
 \operatorname{rank}\Pi_{252}^0=252,
 \qquad
 \dim\ker\Pi_{252}^0=229124.
\]

The executable feeds one mixed public source through the whole map at once.
The input contains:

- a live `j5` image;
- an off-image native grade-six component;
- a native grade-ten equivariant near-miss;
- a native grade-three component; and
- an imaginary public-complement component.

The composite is nonzero and idempotent. On the declared real fixed-density
trace pairing, the Reynolds block is self-adjoint, the `q6` grade blocks are
orthogonal, and the exterior image/kernel are orthogonal; hence the composite
is locally self-adjoint. It retains exactly the selected 252 image and kills both the
public complement and grade-ten near-miss.

Most importantly, the same executable now feeds a source-shaped tilted
distortion in the chosen local `A0=0` jet convention

\[
 T_\omega=\varpi-\epsilon^{-1}d\epsilon
\]

through the same projector. Let `F=epsilon^{-1}s_ref` be a candidate paired
Clifford-frame variable and

\[
 \Pi^F=\operatorname{Ad}_F\Pi_{252}^0\operatorname{Ad}_{F^{-1}}.
\]

The rule `s_ref' = h s_ref` is stipulated and tested in this local fixture;
it is not derived from `Met(X)`, Zorro, `Theta_Z`, or a source-owned frame
bundle. The fixture uses both:

- Wave G's genuine native non-Spin mover (its full-`Sp` membership is
  inherited from that immutable predecessor; Wave H rechecks its inverse and
  combined covariance),
  `1 + ((e0+e3)e4e5)/2`; and
- a public K-unitary mover `h=1+i(e0+e3)/2` outside the right-`H` fixed
  locus.

In the chosen local `A0=0` first-jet convention, exact algebra gives

\[
 T_{\tau(h)\omega}=T_\omega,
 \qquad
 F_{\tau(h)\omega}=F_\omega,
\]

and

\[
 T_{\omega\tau(h)}=\operatorname{Ad}_{h^{-1}}T_\omega,
 \qquad
 F_{\omega\tau(h)}=h^{-1}F_\omega.
\]

The general `tau_A0` sign/convention bridge remains open.

Therefore the **same composite** satisfies

\[
 \Pi^{F'}(T')=\Pi^F(T)
\]

on the left and

\[
 \Pi^{F'}(T')=\operatorname{Ad}_{h^{-1}}\Pi^F(T)
\]

on the right. Freezing `J`/the frame under the public mover fails as a
planted control. Moving the quaternionic structure by

\[
 J^u=uJ\overline{u}^{-1}
\]

repairs covariance of the **family of chosen reductions**:

\[
 R_{J^u}(\operatorname{Ad}_uA)
 =\operatorname{Ad}_uR_J(A).
\]

The codomain moves from `sp(K,J)` to `sp(K,J^u)`; the result does not land
back in one frozen native `Sp`. The fixed-`J` failure is a planted control.

The gate moves

```text
PARTIAL_NATIVE_Q6_AND_LOCAL_TILTED_SCHEMA_CONSTRUCTED
  -> LOCAL_CHOSEN_J_MOVING_REDUCTION_AND_COMBINED_PORT_FIXTURE
```

with route decision `CONTINUE`. The next swing is not another projector. It
is the actual nonconstant `Met(X)`/Zorro overlap construction for `Theta_Z`.

## Layer 0: what was and was not identified

| object | type | Wave H status |
|---|---|---|
| public source coefficient | `u(64,64)`-valued one-form in the 2021 presentation | source-confirmed |
| native coefficient | `sp(32,32;H)`-valued one-form in trace-reversed `(9,5)` geometry | repo-native |
| `J_red` | chosen anti-linear quaternionic reduction field | local fixture; ownership/dynamics source-silent; distinct from P1/P2/P3, both epsilons, and `Theta_Z` |
| `R_J` | `u(K)->sp(K,J_red)` on the explicitly phase-typed real carrier | locally constructed chosen reduction; not defined on arbitrary complex Clifford elements |
| `T_omega` | tensorial difference of two connection transformations | source-confirmed; not a connection |
| `Pi252^F(T_omega)` | moving End(S)-valued one-form at fixed coindex | locally constructed |
| `Theta_Z` | connection-dependent chimeric-to-`TY` coindex transport | source role confirmed; formula/descent open |
| source `epsilon` | public tilted-group field | identity with repo `epsilon_IG` remains uncertain |
| connection/curvature projection | would require coset correction | explicitly not supplied by `R_J` |
| local trace adjoint | fixed fibre pairing | constructed |
| global density/Krein adjoint | density-dual plus varying Hodge/Krein lowerer | open |
| projector image/kernel split | fixed-pairing linear-algebra control | locally controlled; not an Euler covector |
| total physical Euler/Ward/domain | all action owners on one domain | open |

The public and native objects are not called the same object merely because
the dimensions fit. The local reduction is conditional on a supplied
compatible quaternionic structure `J`. Globalizing it is a section problem
for the associated `U/Sp` reduction bundle, not a free bit.

## The new obstruction exposed by writing the map

`R_J` is real-linear but it is **not** a Lie-algebra homomorphism. For the
exact public coset generators

\[
 X=i e_0,\qquad Y=i e_1,
\]

the fixed-locus map gives

\[
 R_J(X)=R_J(Y)=0,
\]

but

\[
 R_J([X,Y])=[X,Y]\ne0,
\]

because the commutator returns to native grade two. Thus

\[
 R_J(F_A)\ne F_{R_J(A)}
\]

without the usual coset `[m,m]` contribution. This does not obstruct applying
`R_J` to the tensorial connection difference `T_omega`; it forbids silently
using the same averaging map as a group projection or as a curvature functor.
The actual source action must decide how the complement is constrained or
varied.

## First jet of the whole moving port

For

\[
 \Pi^F(T)=F\Pi^0(F^{-1}TF)F^{-1},
 \qquad \delta F=Fu,
\]

put `t=F^-1 T F`. Exact symbolic differentiation gives

\[
 F^{-1}\delta\Pi^F(T)F
 =[u,\Pi^0t]+\Pi^0([t,u])+\Pi^0(F^{-1}\delta T F).
\]

For fixed `T`, the executable verifies this formula coefficient by
coefficient and verifies differentiated idempotence

\[
 (\delta\Pi)\Pi+\Pi(\delta\Pi)=\delta\Pi.
\]

The derivative is nonzero on the mixed public/native fixture. For the
auxiliary local quadratic projector-chain comparator

\[
 S_{\rm loc}=\frac12\langle\Pi^F T,\Pi^F T\rangle_{\rm tr},
\]

an exact non-null first jet gives

\[
 \delta S_{\rm loc}=-4.
\]

The directly differentiated auxiliary equals the moving-projector chain
term. Dropping that term incorrectly returns zero. This is a local
projector-chain witness, not a variation of the displayed `I1B+IF` source
action, an actual Euler covector, or a global Euler theorem.

The three adjoint layers remain separated:

1. coefficient transpose;
2. density-dual formal adjoint; and
3. primal Krein adjoint after nondegenerate lowerers.

Only the fixed local trace pairing is instantiated here. A varying
`Theta_Z`, Hodge/density, or Krein lowerer can add terms and Green returns.

## Projector-kernel and no-leakage controls

The probe constructs a nonzero kernel vector annihilated by the projector.
This is only a fixed-pairing linear-algebra control. It is not an assembled
active/transverse Euler covector and does not prove consistent truncation of
the full source action.

It also plants exact maps with `RL=1` and transported observed equation zero
but

\[
 (1-LR)E_YL\ne0.
\]

Thus the local port does not settle observation no-leakage. The actual
observation lift/retract, overlaps, common operator, and domain remain
required.

## Source collision

The source result is deliberately mixed rather than forced into one verdict.
The bounded audit covers the draft reconstruction's public-group sections,
Portal `01:12:17-01:13:55` and `02:25:46-02:33:13`, TOE `00:20:51`,
`00:26:28-00:29:16`, and `02:41:57`, plus the primary-pack
`REAL-FORM-FORK` ledger. The draft reconstruction is not treated as primary
authority and its stale dimension inconsistency is not imported.

| statement | status | use |
|---|---|---|
| 2021 `Spin(7,7)->SO(64,64)->U(64,64)` presentation | `SOURCE-CONFIRMS` | public side of the fork |
| modern trace-reversed Frobenius correction and `Spin(6,4)` real form | `SOURCE-CONFIRMS/CORRECTS-RAW-FROBENIUS` | native direction |
| intrinsic `C=V10+pi*T*X4` | `SOURCE-CONFIRMS` | owns the 4+10 split |
| `C<->TY` requires and varies with a connection | `SOURCE-CONFIRMS` | makes `Theta_Z` load-bearing |
| exterior/Clifford grading plus moving `Ad(epsilon^-1)` pattern | `SOURCE-CONFIRMS` | guides moving family |
| IG, bi-connection, tilted subgroup, connection difference | `SOURCE-CONFIRMS` | owns `T_omega` law |
| explicit U-to-Sp/right-H reduction | `SOURCE-SILENT` | Wave H chosen local fixture only |
| ownership/dynamics of `J_red` | `SOURCE-SILENT` | extra local reduction field, not external datum |
| q6/Pext/Psrc formula | `SOURCE-SILENT` | repo construction |
| actual `Theta_Z` formula and nonconstant cocycle | `SOURCE-SILENT` | next gate |
| source `epsilon` equals `epsilon_IG` | `LAYER-0-UNCERTAIN` | no alias allowed |
| source variation domain and complete total Euler | `SOURCE-SILENT/UNDECLARED` | blocks global promotion |

Source silence is not used as a no-go. Curt remains a formally separated
comparator inside the Eric lane; nothing here promotes a third lane.

## What remains open

1. **Actual `Met(X)` coindex.** Use an integrable nonconstant base coordinate
   change, its `Sym^2` metric-fibre transition, a real base connection, and
   the connection-dependent horizontal/contact coframe. Verify cancellation
   of Christoffel inhomogeneous terms and preservation of the trace-reversed
   fibre metric.
2. **Three-overlap descent.** Verify vector, Clifford, Spin sign,
   quaternionic-`J`, tilted-source, and `Psrc` cocycles on the same three
   patches. Retain the `+-1` Spin triple cocycle as an obstruction rather
   than erasing it.
3. **Source ownership of the complement.** Since `R_J` is not a Lie
   homomorphism, construct the coset curvature/current terms and determine
   whether the displayed action makes the native fixed locus dynamically
   invariant.
4. **Global adjoints and total variation.** Construct density-dual and Krein
   adjoints, differentiate `Theta_Z` and all lowerers, and join the port to
   every bosonic and fermionic source-action owner.
5. **Euler/Ward/Green/domain.** Derive both active and transverse Euler
   equations, the off-shell Ward identity, preboundary/Green form, justified
   quotient, and common closed right-`H`/Krein domain.
6. **Observation.** Build the actual lift/retract and prove both equation
   transport and no-leakage.

## External datum ledger

P1/P2/P3 remain unchanged and unused. A future datum might select among
already existing global `U/Sp` reductions or admissible domains. It cannot
manufacture `J`, the reduction section, `Theta_Z`, the source-derived BV
differential, Euler tangency, or the analytic domain.

## Next gate

**`RESOLVER-WAVE-I-ACTUAL-METX-ZORRO-THETA-DESCENT`**:

1. build the actual trace-reversed `Met(X)` horizontal/contact coframe under
   two explicit nonlinear coordinate shears and their composition;
2. verify pairwise and triple-overlap `Theta_Z` naturality;
3. lift the resulting `SO^+(9,5)` transitions and retain the Spin cocycle;
4. transport `J`, the tilted source, and the entire rank-252 `Psrc` on those
   same overlaps; and
5. only on success attempt total Euler tangency, density/Krein adjoints,
   Ward/Green data, domain, and no-leakage.

No stationarity, VEV, mass, anomaly solution, index, generation count,
cosmological prediction, global port, domain, or public-posture change is
claimed.
