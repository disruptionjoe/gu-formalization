---
title: "PW2E finite active Shiab descent and mixed metric-Ward readiness"
status: active_research
doc_type: construction_result
created: 2026-08-02
branch: agent/null-clifford-omega1-repair
run: private orchestration runtime#meta/runs/historical-investigation/run-plan.md
registries:
  - lab/process/pw2e-finite-native-shiab-descent.json
  - lab/process/pw2e-mixed-metric-frechet-native-ward-registry.json
probes:
  - tests/channel-swings/pw2e_finite_native_shiab_descent_probe.py
  - tests/channel-swings/pw2e_mixed_metric_frechet_native_ward_probe.py
grade: "PW2E PARTIAL PASS. The active finite grade-3/11 Shiab operator, right-H/Krein/C-plus structure, nonconstant three-patch active descent, affine B+K law, transported action pairing, and grade-two contragredient covector law pass. The active projector is corrected to invariant rank 8256, with bivector dimension 91. The mixed inventory independently verifies a structural fixed-varpi Levi-Civita adjoint/Green certificate, all eight live native moving-Shiab slots, rank ten across ten metric owners, and a live actual induced-Y14 LC graph. The actual native top-order composition, action Euler dual, and diffeomorphism Ward remain unassembled. A comparator shows possible fourth-jet sensitivity, while the inherited structural fixture exhibits exact top-order cancellation. PW2F must assemble the actual top-order coefficient first and build a fourth-order family only if a nonzero term survives."
canon_verdict_change: none
---

# PW2E finite active Shiab descent and mixed metric-Ward readiness

## Result first

PW2E closes the finite local active-geometry part of the prior gate and turns
the metric problem into a precise composition and cancellation audit.

The bridge generator on the synthetic source-shaped fixture is

\[
u=c_3\operatorname{Alt}(T_{\rm syn})+c_{11}\star\operatorname{Alt}(T_{\rm syn}),
\qquad
u^2=c_{11}^2-c_3^2=-\Delta.
\]

Consequently its finite exponential lives in the two-dimensional algebra
spanned by `1` and `u`. Exact rational points on the elliptic and hyperbolic
exponential conics, together with the exact null branch `h=1+u`, verify the
finite construction without floating point or expensive trigonometric
simplification.

The finite Shiab is assembled by moving the trace line, `Phi1`, `Phi2`, Hodge
composition, and projector independently. It then agrees with the conjugated
operator identity on elliptic, hyperbolic, and null witnesses. On the actual
B2C15P curvature it moves nontrivially, and its tangent agrees with PW2D.
Every finite branch preserves the active right-`H`, Krein, and `C+`
structures.

The first important correction is:

> The active projector is not a moving rank-91 projector.

It retains Clifford grades `2,3,6,7,10,11,14` and therefore has rank `8256`.
The bivectors form a `91`-dimensional subspace. Under the tested
source-generated elliptic, hyperbolic, and null finite family the projector
is invariant. The Shiab motion comes from the moving
trace/Phi/Hodge family, not from projector motion. This corrects the
specialist pre-assessment rather than fitting the probe to it.

## Three-patch descent

A square-zero native bivector supplies nonconstant transitions with patch
functions

\[
0,\qquad x,\qquad 2x+x^2.
\]

The exact Čech cocycle closes. On all overlaps:

- the finite bridge and independently assembled local Shiab coefficient
  descend;
- literal `K=h^{-1}D_Bh` is tensorial;
- `B+K` obeys the affine connection law, including the nonzero derivative
  term;
- the transported thirteen-form coefficient has invariant scalar pairing,
  with value `9/64`; and
- the invariant grade-two pairing converts coefficient covariance into an
  independent contragredient representation-covector law.

This is an active-principal-bundle algebraic atlas with external form
coordinates fixed. It is not the public `U/(7,7)` real-form port, a coordinate
atlas of arbitrary `Y14`, or an analytic global-domain theorem.

## The metric pieces that still require composition

The inherited B2C15O structural one-dimensional coordinate-chain certificate
is exact. With

\[
A=\Gamma(g)+\varpi,
\]

varying the metric at fixed source `varpi` gives

\[
E_g\big|_{\varpi}
=E_g\big|_A+(D_g\Gamma)^!E_T.
\]

The correction is nonzero and carries its own nonzero Green companion. Thus
the fixed-`A` equation cannot be reused after changing source coordinates in
that fixture. This two-component certificate is not the actual `1274x10`
native `Y14` Levi-Civita composition.

Independently, the actual native metric bank remains fully live:

- all eight moving-Shiab slots contribute;
- the coefficient matrix has `544` rows and rank `10` across the ten
  trace-reversed metric owners;
- four of ten frozen action pairings are nonzero; and
- the actual induced-`Y14` Levi-Civita spin graph has rank `10` on both a
  nonnull and a null covector and differs from the observed-four-dimensional
  truncation by rank `10` in both cases.

These are the pieces needed for the full metric derivative. They have not yet
been composed into that derivative.

## The open top-order cancellation

The current native background supplies

```text
G0, DG, D2G
```

for the total-space metric, induced from a base metric three-jet. A generic
moving-coefficient formal-adjoint route can see a total-space metric third
jet. A first-order Zorro graph can therefore make a comparator depend on the
base metric fourth jet.

The executable comparator is variational rather than asserted: set `G=g'`,
`F=G'`, and `L=F^2/2`. Direct variation gives `E_g=g''''`; the graph route
gives `E_G=-G''` and `(D_gG)^!E_G=g''''`. Two base metrics `g_a` and `g_b`
agree through derivative order three but return `a` and `b`. Therefore the
existing fixture does not exclude fourth-jet sensitivity. It does not prove
that the actual native coefficient is nonzero.

In fact, the inherited structural fixed-`varpi` fixture supplies an exact
cancellation control. Its fixed-`A` return and `(D_g Gamma)^!E_T` correction
each contain live third derivatives of the metric, with opposite
coefficients; their fixed-`varpi` sum has no such term. The control also
replays the general same-order danger:

\[
A_2 Z_0\ne0
\quad\text{while}\quad
A_2Z_0+A_1Z_1=0
\]

on an exact planted comparator. The prior live `A2 Z0` rank cannot be read as
the complete effective `C2` rank. PW2F must therefore assemble the actual
top-order coefficient first. A fourth-order background family becomes
necessary only if that coefficient survives.

## Ward boundary

The predecessor `2x2` residual active internal-gauge Ward comparator remains
exact and off shell,

\[
D_CE_C+[T,E_T]+[Q,E_Q]=0.
\]

It fixes its scalar weight. A gauge-invariant scalar weight can have a live
Euler derivative while leaving that internal gauge variation zero. The
native metric equation therefore cannot be erased or inferred from this
internal Ward comparator. A formal diffeomorphism Noether schema can be
written before the native coefficient is evaluated; its native rank and
content wait for the full action composition.

## Source collision and Layer 0

- `SOURCE-CONFIRMS`: source roots, `B_omega/T_omega`, one-half/one-third first
  action, bosonic distortion-norm slot, tilted grammar, trace reversal, and gauge-rotated
  Levi-Civita in the contorsion slot.
- `SOURCE-NEGATIVE`: the public preferred Shiab projection sheet/calculation
  is unavailable. It is not the repository's active `Cl(9,5)/Sp` operator.
- `SOURCE-SILENT/REPOSITORY-DERIVED`: the finite active exponential, the
  rank-8256 invariant projector, the active three-patch descent, the complete
  mixed metric Frechet coefficient, the fourth-order background family, and
  the native diffeomorphism Ward identity.

The source silence is scoped to the reviewed corpus. It does not reject the
construction; it prevents attributing the repository completion to
Weinstein. The synthetic `build_source_t` and finite epsilon surrogate remain
unidentified with Weinstein's `T_omega` and H-valued epsilon.

The following objects remain distinct:

- fixed total `A`, fixed source `varpi`, and fixed `B` metric variations;
- internal gauge Ward, diffeomorphism Ward, `Xi=D Upsilon`, and Euler equation;
- public source bundle, active native bundle, and reduced Spin bundle;
- rank-8256 active projector and its 91-dimensional bivector sector; and
- local algebraic descent, analytic global domain, BV quotient, observation
  descent, and physical equation.

## External datum and lanes

P1/P2/P3 remain unchanged and unused. None has the type of a continuous
metric jet, a Frechet coefficient, a Green term, or a Ward generator.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.
`TG-1 AND TG-2 AND TG-3` remains `NOT_PROMOTED`.

## Next gate

`PW2F-COMPLETE-NATIVE-TOP-ORDER-COMPOSITION-AND-METRIC-WARD` should:

1. compose the top-order parts of `D_g Gamma`, `D_g h`, and literal `D_g K`
   at fixed `varpi`;
2. include all eight moving-Shiab slots, the bosonic distortion-norm Hodge
   star, density, Krein lowerer, pairing, and every generated Green term;
3. verify or refute cancellation of the complete total-third/base-fourth
   coefficient;
4. in parallel, derive the formal diffeomorphism Noether schema;
5. only if the top-order coefficient survives, construct `D3G` from an
   explicit fourth-order native base-metric family and evaluate the schema's
   native coefficient and rank; and
6. only then compute the complete effective order/rank, observation descent,
   and first physics-facing equation.

The finite active construction is now strong enough to support that swing.
PW3 remains blocked until the complete action derives its own BV tangent.
