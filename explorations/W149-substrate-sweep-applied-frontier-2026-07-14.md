---
title: "W149: substrate-reframe steelman sweep -- APPLIED / COMPUTATIONAL / FRONTIER-WILD family. If the fundamental object is Y14 as a GROWING RECORD DAG (records held in the 14-dim observerse, dark energy a SHADOW of record accretion, the Y14->X4 projection MEASUREMENT-GATED through a GLOBAL->REGIONAL->INDIVIDUAL finality hierarchy), which concrete growth-graph mechanisms survive the W138 kill battery and the novelty gate?"
status: exploration
grade: "exploration / hypothesis-generation. Every steelman scored against the W138 battery + the new novelty gate (distinct from bare causal-set relabeling AND bare Wolfram relabeling; must pin something GU-specific). COMPUTED: tests/W149_substrate_record_shadow.py (26/26, exit 0) -- the record-count-to-Lambda mechanics for the bulk (Sorkin) and boundary (finality-frontier) readings, their exact relation, and the Omega_L-factorization that caps novelty. No canon / RESEARCH-STATUS / claim / verdict / posture change."
date: 2026-07-14
family: "APPLIED / COMPUTATIONAL / FRONTIER-WILD (systems/control engineers, information theorists, complexity/computation, distributed-systems/DAG-ledger, stochastic-process/percolation, network science, quantum-information/holography)"
label: W149
test: tests/W149_substrate_record_shadow.py
depends_on:
  - explorations/W135-issuance-structure-taxonomy-2026-07-14.md
  - explorations/W137-observer-slice-structure-2026-07-14.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - explorations/W132-graded-optical-theorem-physical-subspace-2026-07-14.md
  - explorations/W131-covariant-operator-y14-2026-07-14.md
  - explorations/sorkin-causal-set/sorkin-causal-set-axis-note.md
  - explorations/threads/six-axis-examination-of-the-records-as-rows-reframe-2026-07-10.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
binding_constraints:
  - "B2 rate-identity FALSE: no structural constant is read as a rate (path4-branchC). Growth reading uses a FILTRATION/frontier, not a rate coupling."
  - "H36 mu_DW identification FALSIFIED (H50/H52): no DE magnitude pinned from a DeWitt scale."
  - "W129: every band-allowed amplitude is an LCDM mimic |w0+1| < ~0.1."
  - "W143: the O(1) board factorizes through Omega_L; predicting an O(1) normalization ALONE is a W138-G5 novelty relabel."
  - "Tri-repo: capability MEASURE -> TaF; issuance/finality TYPING -> temporal-issuance + time-as-finality; GU owns the Y14 substrate math (the (9,5) DAG, C+ subspace, the section projection, the record-count-to-Lambda shadow)."
---

# W149: substrate-reframe steelman sweep (applied / computational / frontier-wild)

## 0. The reframe, and what my family owns

Joe's reframe REVERSES the prior issuance arrow. The fundamental object is not an energy
issuance; it is a **substrate where records are held**. Three load-bearing clarifications
(direct chat, 2026-07-14) pin it GU-natively:

1. **The substrate is Y14 itself** -- the 14-dim observerse, not an imported causal set or
   Wolfram model. Records live in Y14; we cannot see them until they are projected into X4.
2. **The shadow map is GU's Y14 -> X4 projection.** Dark energy / Lambda is the shadow of
   record accretion in Y14, not a source term. The projection integrates out the DeWitt
   fiber (6,4) and keeps the base (3,1) (W131).
3. **The projection is MEASUREMENT-GATED through a capability hierarchy** GLOBAL -> REGIONAL
   -> INDIVIDUAL. An individual observer must OBSERVE a record to promote it from the global
   Y14 substrate into a regional/confirmed shard. The "boundary" is not fundamental: it is
   the **confirmation frontier** of a finality/consensus process (uncommitted -> observed/
   attested -> final). Capability-bounded-by-tier = the reachable/confirmable record set per
   observer (its light-cone in the Y14 DAG).

My family owns the **graph / computational / information-substrate mechanism**: the concrete
growing graph (nodes/edges = records/precedence), the named growth rule, the named resource
"energy to continue", and the capability bound as a graph-theoretic bound. Comparative
advantage: make the mechanism CONCRETE. The Rideout-Sorkin classical sequential growth (CSG)
process is the gold-standard concrete record-accretion dynamics on a DAG; the Sorkin
everpresent Lambda (`Lambda ~ 1/sqrt(N)`) is the gold-standard shadow-map template. Both are
ported ON Y14, not imported as rival substrates.

## 1. What the test computed (tests/W149_substrate_record_shadow.py, 26/26, exit 0)

The whole family shares one shadow-map arithmetic; the test settles it once, so persona
scoring is against numbers, not adjectives.

- **The everpresent shadow WORKS on Y14->X4.** Sorkin bulk count `N_bulk = (R_H/l_p)^4 ~
  5.2e243`; `1/sqrt(N_bulk) = 1.39e-122`, and `Lambda_Pl = 3 Omega_L (l_p/R_H)^2 = 2.85e-122`.
  They agree to an O(1) factor. The **substrate reframe INHERITS the everpresent-Lambda
  success**: `Lambda ~ 10^-122` because there are `~10^244` records, with no fine-tuning.
  This is the click the issuance reading never had -- issuance had to normalize Lambda by
  hand; record-accretion PREDICTS its order.
- **The O(1) offset is a function of Omega_L.** `Lambda_Pl / (1/sqrt(N_bulk)) = 3 Omega_L =
  2.054` (computed). This is the W143 cap made mechanical: the normalization carries no
  GU-specific number. Confirmed on the whole dS board: `T_dS S_dS / E_Lambda = 1.460 =
  1/Omega_L` exactly; `bits/S_dS = 2.963 = 3 Omega_L / ln2` exactly; `Q_tot = 1.027 =
  (3/2) Omega_L`. **Predicting an O(1) normalization alone is a G5 novelty relabel.**
- **Measurement-gating selects the BOUNDARY (holographic) count as the physical one.** The
  confirmed-frontier count `N_conf = S_dS ~ 2.27e122` (= the W138 Landauer bit count, up to
  the same Omega_L factor: `bits_at_T_dS = 6.72e122 = 2.96 S_dS`). Under measurement-gating,
  `Lambda ~ 1/N_conf` (LINEAR), and this coincides with the Sorkin bulk `1/sqrt(N_bulk)`
  because **`N_bulk = N_conf^2 / pi^2`** (computed to 0.05 dex). So the two record counts the
  repo already has -- the Landauer boundary count (`~S_dS`) and the implied Sorkin bulk count
  (`~S_dS^2`) -- are the SAME substrate viewed confirmed-frontier vs total, and
  `N_confirmed = pi / Lambda_Pl` (up to `3 Omega_L`): **the number of confirmed records is the
  inverse cosmological constant in Planck units.**
- **The shadow exponent 1/2 is dimension-independent (Poisson), so the DeWitt fiber is a
  SPECTATOR to it.** `14 = 4 + 10` does not enter the everpresent exponent. Honest
  consequence: GU-specific novelty CANNOT come from the shadow normalization or exponent; it
  must come from (a) the admissible-record SET (the C-operator positive subspace, W132/W137)
  and (b) the confirmed-vs-bulk SELECTION rule (measurement-gating). This is the sweep's
  sharpest negative result and it disciplines every survivor below.

## 2. Family roster and coverage

Ten personas, each run INLINE (one worker, no sub-agents), two steelmen apiece = 20 steelmen.
Nearest-partition to the D-wild-frontier + E-pragmatic slices of the 2026-07-11 roster, plus
the four brief-named applied types. Coverage list: (1) causal-set dynamicist (Rideout-Sorkin
CSG), (2) distributed-systems / DAG-ledger engineer, (3) consensus / finality theorist,
(4) information theorist (Landauer-Bekenstein), (5) control / systems engineer,
(6) complexity theorist (Wolfram / ruliad), (7) network scientist (Bianconi growing graphs),
(8) stochastic-process / percolation theorist, (9) quantum-information / holography (tensor
networks), (10) large-deviations / statistical mechanic.

Each steelman is stated as the brief requires: (i) the growing-graph object + growth rule,
(ii) the energy-to-continue + capability bound as a graph bound, (iii) the shadow map,
(iv) the assumption leg, (v) the persona's test. Scored G1-G6 (W138) + NOVELTY.

## 3. Top survivors (ranked by mechanism concreteness, assumption-leg sharpness, testability, novelty)

### S1 -- Consensus / finality theorist: the measurement-gated promotion DAG on the C+ subspace
**(persona 3, fused with 2 and 1). RANK 1.**

- **Growing graph + growth rule.** Nodes = admissible records = elements of the C-operator
  positive subspace `H_C+` over Y14 (W132's "what counts as physical"). Edges = Y14 (9,5)
  causal precedence. Growth rule = **promotion/confirmation dynamics**: records exist
  "uncommitted" in the global Y14 substrate; a record is PROMOTED to regional-confirmed when
  an observer-section's X4 light-cone reaches it and commits it (observation = a commit
  advancing the confirmed frontier). Rideout-Sorkin CSG is the template for the underlying
  substrate order; the promotion frontier is a monotone advancing cut (a finality frontier),
  never a rate.
- **Energy to continue + capability bound.** Resource consumed per promotion = the a0 = 2
  DeWitt-Lambda-channel constant per confirmed record (W137's signed deformation-cost
  bookkeeping; honest: signed, not a positive ledger). Capability bound is the tier ladder as
  a graph bound: **individual** = the reachable set of the section's X4 light-cone preimage in
  the Y14 DAG (a sub-poset); **regional** = the promoted/confirmed shard (a larger down-set);
  **global** = the full Y14 ledger. This is exactly the sharding/consistency hierarchy of a
  growing record substrate.
- **Shadow map.** The confirmed-frontier count `N_conf = S_dS`; the observable
  `Lambda ~ 1/N_conf` (measurement-gated boundary reading), which coincides with Sorkin
  `1/sqrt(N_bulk)` because `N_bulk = N_conf^2/pi^2` (computed). **Measurement-gating is what
  makes the HOLOGRAPHIC/area count -- not the bulk 4-volume count -- the physical record
  count.** It adjudicates the standing bulk-vs-boundary tension in favour of the boundary,
  and gives the clean statement `N_confirmed = pi/Lambda_Pl`.
- **Assumption leg (spec FIT / open object pinned).** The interacting C-operator exists over
  Y14 (W137 kill K1: open, branch B order-by-order only). This is the SAME single gate the
  whole program rides -- the unbuilt source action / the C-operator existence. The reframe
  does not add a new gate; it re-reads W137's static C1 conjecture (admissible records =
  C-compression) as a DYNAMICS (record accretion INTO H_C+ = the growth process).
- **Test.** Extend W137's K2 compressed-metric test to a MONOTONE sequence of nested sections
  (a promotion sequence): compute `P_{sigma_k} eta_+ P_{sigma_k}` on a growing down-set and
  check that positivity is preserved as the frontier advances (a confirmed record stays
  confirmed = finality is monotone). Positivity-preserved -> the promotion dynamics is
  well-posed on `H_C+`; positivity-lost at some advance -> finality is not monotone on the
  C+ subspace and the reading retreats to class-level coherence (W107). Tractable on the W54
  free model.
- **Gates.** G1-G4: PASS (no rate -- a frontier is a filtration, explicitly B2-allowed; no
  mu_DW scale; LCDM-mimic since Lambda is a shadow, not a tuned amplitude). G5: the
  normalization is Omega_L-functional (relabel-capped) -- but the STRUCTURE (measurement-
  gating selects the boundary count; finality frontier = horizon) is NOT a dS relabel.
  G6: emits structure -- the promotion rate `dN_conf/dt = 2 S_dS H0 ~ 9.9e104/s` has a
  Landauer cost that saturates the DE budget at T_dS (W138 G6 resurfacing, now READ as "the
  DE budget pays exactly for confirmed-frontier promotion"). **NOVELTY: CLEARS.** Distinct
  from bare causal-set (which uses the BULK count and has no C+ admissible set and no
  measurement-gate); distinct from bare Wolfram (a poset/DAG with causal order and a
  finality frontier, not hypergraph rewriting). Pins two GU-specific objects: `H_C+` (W132)
  and the Y14->X4 section projection (W131).

### S2 -- Information theorist (Landauer-Bekenstein): the confirmed-record count IS the entropy
**(persona 4). RANK 2.**

- **Object + rule.** Nodes = promoted records; the graph is the confirmed sub-DAG. Growth
  rule = one Landauer-costed bit per promotion. Resource = `k_B T_dS ln2` per confirmed
  record; capability bound = an observer can confirm at most `S_dS` records (its horizon is
  its memory bound -- Bekenstein bound as the individual-tier cap).
- **Shadow map.** `N_confirmed = S_dS = pi/Lambda_Pl`: the horizon entropy is literally the
  count of confirmed records, and Lambda is its inverse. The Landauer numbers (6.72e122 at
  T_dS = 2.96 S_dS; 6.5e92 at T_CMB) are now a **count of PROMOTED records**, not a generic
  bit budget.
- **Assumption leg.** That the confirmed count saturates the horizon entropy (holographic
  saturation) rather than under-filling it -- i.e. the substrate is a maximally-packed
  holographic code. Pinned as the "T_dS reading" of W138 G6.
- **Test.** The G6 no-fine-tuning result becomes a PREDICTION: at any temperature between
  T_dS and T_CMB, confirmed-record creation must sit below the budget; W138 found the budget
  over-pays stars by 1.3e6 at T_CMB. Forecast: if a record model with a sharper creation rate
  is supplied (TI-side), recompute the gap; a model REQUIRING promotion above budget is dead.
- **Gates.** G1-G4 PASS; G5 the 2.96 = `3 Omega_L/ln2` is a relabel (conceded, flagged);
  G6 turned from annotation into a bounded prediction. **NOVELTY: CLEARS at STRUCTURE level**
  (confirmed-count = entropy is the finality reading of a known identity; the GU-specific pin
  is that the count is of Y14 records projected through the section). Weaker than S1 because
  it does not use `H_C+` -- it rides only the boundary count.

### S3 -- Causal-set dynamicist (Rideout-Sorkin CSG on Y14): the substrate growth process
**(persona 1). RANK 3.**

- **Object + rule.** The literal Rideout-Sorkin CSG stochastic process, ON Y14: a growing
  past-finite poset, one element ("record") added at a time to the future of a randomly
  chosen down-set, governed by CSG coupling constants `t_k` (transitive percolation). The
  Y14 (9,5) causal cone constrains the admissible additions (a record must respect the
  observerse light-cone). This is the gold-standard CONCRETE record-accretion dynamics.
- **Energy + capability.** Resource = the a0 = 2 constant per element. Capability = an
  observer's past-set (its reachable down-set) -- exactly the CSG "stem" cardinality.
- **Shadow map.** Number -> volume -> `Lambda ~ 1/sqrt(N_bulk)`, the everpresent Lambda,
  reproduced by the test at `10^-122`.
- **Assumption leg.** That Y14's (9,5) precedence gives a manifold-faithful growth (Section 5
  of the Sorkin axis-note: only the manifold-faithful subclass has a smooth shadow). Pinned.
- **Test.** Simulate small CSG on a (9,5)-cone-restricted poset and measure the Myrheim-Meyer
  dimension of the confirmed sub-DAG: does the fiber-restricted growth recover base dimension
  4 (the X4 shadow) from a 14-substrate? A runnable growth simulation -- ranks high on
  testability.
- **Gates.** G1-G4 PASS; G5 normalization Omega_L-capped. **NOVELTY: PARTIAL.** The bare CSG
  is exactly "bare causal-set relabeling" the novelty gate forbids UNLESS the Y14 restriction
  or the C+ admissible set is load-bearing. It clears only when fused with S1's `H_C+`
  restriction; standalone it is the causal-set control, kept as the mechanism template and the
  simulation harness, not as an independent survivor.

### S4 -- Control / systems engineer: finality as a monotone advancing setpoint
**(persona 5). RANK 4.**

- **Object + rule.** The confirmed frontier as the controlled variable; the promotion process
  as a monotone (integral-only, no overshoot) advance toward the global ledger. Distinct from
  W135's PID-setpoint DE structure: here the controller does not hold a DENSITY setpoint, it
  advances a FRONTIER (a count), so it is a monotone accumulator, not a regulator.
- **Energy + capability.** Resource = per-commit cost; capability = the controller's authority
  = the observer's reachable set (individual-tier gain-limited).
- **Shadow map.** `Lambda ~ 1/N_conf` with `N_conf` monotone increasing -> Lambda monotone
  DECREASING (records accrete, the shadow dims). Prediction shape: Lambda was larger in the
  past. Honest caveat: W129/W135 bound any such drift to the LCDM-mimic band; the monotone
  decrease must be sub-0.3/e-fold to survive, i.e. the frontier advance is slow on the
  cosmological clock.
- **Assumption leg.** Finality is MONOTONE (a confirmed record is never un-confirmed) -- the
  integral-only, no-overshoot property. Pinned; this is the finality axiom.
- **Test.** Reuse W135's continuity-equation machinery: a monotone `N_conf(a)` gives
  `d ln rho / d ln a`; check it against the |0.3/e-fold| mimic bound. Cheap; decides whether a
  visibly-advancing frontier is already excluded.
- **Gates.** G1-G4: PASS only if the advance is slow (G2 mimic bound is the live constraint);
  G5 relabel-capped; NOVELTY: CLEARS at structure level (monotone-finality control is not a dS
  relabel), but the reading credits more than it kills. Ranks below S1-S3 because its content
  is the drift bound, which W135 already owns.

### S5 -- Quantum-information / holography: the confirmed frontier as a holographic code
**(persona 9). RANK 5.**

- **Object + rule.** The confirmed sub-DAG as a tensor-network / holographic error-correcting
  code; promotion = adding a bulk record protected by boundary (X4) data. Growth = code-
  distance-preserving tensor addition.
- **Energy + capability.** Resource per logical record = the code's redundancy cost;
  capability = an observer decodes only records within its light-cone's causal wedge
  (entanglement-wedge reconstruction = the individual tier).
- **Shadow map.** Bulk-boundary: `N_bulk = N_conf^2` is the code's bulk-to-boundary ratio;
  `Lambda ~ 1/N_conf` is the boundary (code-space) reading.
- **Assumption leg.** The Y14->X4 projection is an isometry onto a code subspace = W132's
  `A^dag A = P_+ + B^dag B` expansion read as the code's isometry condition. Pinned to W132.
- **Test.** Check whether the W132 physical-subspace expansion has the complementary-recovery
  property of a code on a bipartition of a Y14 section. Structural; shares S1's cheapest
  discriminator.
- **Gates.** G1-G4 PASS; G5 capped; NOVELTY: CLEARS (holographic-code reading of W132 is
  GU-specific), but the mechanism is less concrete than S1's promotion dynamics (no named
  growth rule beyond "add a protected tensor"). Ranks last of the survivors.

## 4. Discards (one line each)

- **Complexity theorist / Wolfram ruliad (persona 6), both steelmen.** A Y14 hypergraph-
  rewriting substrate RELABELS: the novelty gate explicitly forbids bare-Wolfram relabeling,
  and Y14 carries a causal ORDER (a poset/DAG), not an un-oriented hypergraph; the rewriting
  metaphysics adds no computable GU-specific object. DISCARD (kept only as vocabulary).
- **Network scientist / Bianconi growing graph (persona 7), both.** Bianconi's growing-network
  gravity is the RIVAL, not the steelman; a preferential-attachment record graph reproduces
  Lambda only by fitting the attachment exponent to Omega_L -- a G5 relabel with an extra free
  exponent. DISCARD (it is the empirical fork the E-family already tests in the spin-2 sector).
- **Stochastic-process / percolation (persona 8), steelman-A (bulk transitive percolation).**
  Identical to S3's bare CSG; folded into S3, not independent.
- **Stochastic-process (persona 8), steelman-B (a percolation THRESHOLD as Lambda).** Reads a
  critical occupation probability as the DE amplitude -- a structural constant read as a
  rate/amplitude, dead on arrival by B2/G4. DISCARD.
- **Control engineer (persona 5), steelman-B (PID density regulator).** Duplicates W135's (e)
  PID-setpoint DE structure exactly; already scored there (ALIVE-bounded). Not re-litigated;
  only the monotone-frontier variant (S4) is new.
- **Large-deviations / statistical mechanic (persona 10), both.** The everpresent Lambda IS a
  large-deviation/fluctuation statement; the persona re-derives `1/sqrt(N)` and the
  `N_bulk = N_conf^2` relation -- valuable as the WHY behind the test, but adds no independent
  mechanism. Folded into Section 1 as the fluctuation account, not a standalone survivor.
- **DAG-ledger engineer (persona 2), steelman-B ("issuance rate = block reward schedule").**
  The dead prior-wave reading; a coin-schedule rate is exactly the B2/G4 kill. DISCARD. Its
  steelman-A (append-only confirmed DAG, finality frontier) is the load-bearing half and is
  fused into S1.

## 5. Requested verdicts

### The C-operator-positive-subspace-as-substrate-element-set mechanism: VERDICT
**Sharpest available mechanism; CLEARS the novelty gate; rides the single standing gate.**
Making `H_C+` the admissible-record set is the sharpest possible substrate mechanism because
it is the one object that is (a) GU-specific (W132: unitarity = C-operator existence over
Y14), (b) already the repo's answer to "what counts as physical", and (c) the exact static
shadow of Joe's "admissible records" (W137 C1). W149's contribution is to make it a DYNAMICS:
**record accretion = adding elements to `H_C+`; promotion/confirmation = the section-
compression `P_sigma H_C+` advancing as the observer's light-cone grows.** This upgrades W137
C1 from a static identity to a growth process without adding a gate -- it rides W137's K1
(does the interacting C-operator exist) and K2 (does the compressed metric stay positive),
which are the SAME gate the whole program rides (the unbuilt source action). Honest limit: the
mechanism's NUMBER (the Lambda normalization) is Omega_L-functional and cannot be a novelty
click; its STRUCTURE (measurement-gated promotion selects the boundary count, finality frontier
= horizon, admissible set = `H_C+`) is genuinely GU-specific and new relative to both bare
causal-set (bulk count, no C+) and bare Wolfram (hypergraph, no order).

### The Landauer / record-count click: YES, a structural click (novelty-capped number)
The two record counts the repo already computed are ONE substrate under measurement-gating:
- **Boundary / confirmed:** `N_conf = S_dS = 2.27e122` = the W138 Landauer bit count
  (`6.72e122 = 2.96 S_dS` at T_dS). These are **promoted records**.
- **Bulk / uncommitted:** `N_bulk = (R_H/l_p)^4 = 5.2e243`, the Sorkin 4-volume count.
- **Relation (computed):** `N_bulk = N_conf^2 / pi^2`, so the Sorkin `1/sqrt(N_bulk)` shadow
  and the holographic `1/N_conf` shadow COINCIDE, and **`N_confirmed = pi / Lambda_Pl`**: the
  number of confirmed records is the inverse cosmological constant in Planck units.
- **Promotion rate:** `dN_conf/dt = 2 S_dS H0 ~ 9.9e104/s`, whose Landauer cost at T_dS
  saturates the DE budget (W138 G6). Read forward: **the dark-energy budget pays exactly for
  the promotion of the confirmed frontier** -- the reframe's cleanest sentence, and the sense
  in which "energy to continue" is literally the resource that advances finality.
The click is genuine (it unifies the boundary and bulk counts and adjudicates which is
physical) but the NUMBER stays Omega_L-functional (W143); it is a structural click, not a new
constant. Reported as such.

## 6. Counts, gates, provenance

- Personas: 10 (family roster in Section 2), all inline, 20 steelmen; 5 survivors (S1-S5),
  ~11 discards/folds (Section 4).
- Test: `tests/W149_substrate_record_shadow.py`, 26/26, exit 0 (~1 s, pure-python
  deterministic; 5 positive controls first: l_p, rho_L^(1/4)=2.24 meV, Q_tot=1.027=(3/2)Omega_L,
  ladder 9 Omega_L=6.16).
- Battery: every survivor scored G1-G6 (W138) + NOVELTY. No survivor claims a rate (B2/G4
  honored via the filtration/frontier reading), a mu_DW scale (H36 honored), or a
  DESI-signal amplitude (W129 honored: Lambda is a shadow, the drift-visible variant S4 is the
  only one exposed to G2 and it must stay sub-0.3/e-fold).
- Tri-repo gates HELD: capability MEASURE -> TaF (this file makes no measure/ranking claim);
  finality/issuance TYPING (promotion = commit, uncommitted->confirmed->final) -> temporal-
  issuance + time-as-finality (used as the reading label only); GU owns the Y14 DAG structure,
  the C+ admissible set, the section projection, and the record-count-to-Lambda math.
- Conditional register throughout; zero em dashes; no canon / RESEARCH-STATUS / claim /
  verdict / posture change. C-operator convergence with W135/W137/W138 noted; siblings
  W139-W148 not read (cited at commit level only per the wave protocol); W144 (running) not
  cited.

## 7. What this does NOT do

No claim that the C-operator exists (K1 open), that the promotion dynamics is well-posed
(K2/S1's test unrun), or that any number here is GU-specific beyond Omega_L. The everpresent
shadow's SUCCESS (Lambda ~ 10^-122 from record count) is a real inherited click; its
NORMALIZATION is not a prediction. The measurement-gated-promotion / finality-frontier reading
is the family's genuinely new structural contribution and is offered under the novelty gate,
with S1's monotone-positivity test named as the cheapest discriminator.
