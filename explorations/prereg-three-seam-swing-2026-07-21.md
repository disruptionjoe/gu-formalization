---
title: "Pre-registration: the THREE-SEAM swing — (A) define the class-relative circle frame, (B) prove no-fourth-generation, (C) adjudicate the two-time boundary posit"
status: preregistration
doc_type: scoping_decision_tree
created: 2026-07-21
directed_by: "Joe direct chat, 2026-07-21 (orchestrate all three open seams into one swing)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
external_actions: none
kill_conditions_declared_before_computation: true
---

# The three-seam swing

Three independent prongs, one per open seam from the 22-construction coherence
assessment. Discipline (three clean fits died on hostile verify today): numerology
gate on constructions; hostile-verify every positive; three-tier falsifiability
(directly-falsifiable / SCAFFOLD-keep / vacuous-retire — do NOT retire for
inside-unfalsifiability, which self-referential structures predict of themselves);
planted controls; everything [PROPOSED] until verified; no claim/canon/verdict move.

## Prong A — CLASS-RELATIVE CIRCLE FRAME (the #1 contested seam)
Reading-A (σ = w₁(L_time), the arrow-of-time line bundle over F≃RP³) is solid.
Reading-B (σ = the orientation of a first-person/consensus/quotient CIRCLE in a
GU-NATIVE structure group) is open. The hypergraph/category/sheaf/intersubjectivity
constructions claim A and B are the SAME Z/2 in two structure-classes. TEST: is there
a CANONICAL construction — the shard-cycle as a coequalizer/quotient of the record
helix, with GU's deck/Krein transition structure group (NOT the standard tangent
group) — under which σ = its orientation obstruction, nontrivial, and mapping to
w₁(L_time) under realization to standard topology?
- **A-RECONCILED** (hostile-verify): a canonical GU-native frame gives σ = cycle-
  orientation = w₁(L_time) — reading-A and reading-B are one object; the "class-
  relative no-go" is dissolved (Prong 1's w₁(TS¹)=0 was the wrong structure group).
- **A-DISTINCT**: a canonical cycle-orientation exists but is a DIFFERENT Z/2 from
  w₁(L_time) — two genuine external data, not one (state the second datum + its test).
- **A-NUMEROLOGY** (kill): no canonical GU-native frame carries σ as a cycle-
  orientation ⇒ reading-B is numerology; reading-A stands alone. Say so.
- Control: a planted "any quotient gives the orientation" must be rejected — the
  frame must be canonical (GU's deck/Krein group), the class computed, the
  realization map to w₁(L_time) exhibited, not asserted.

## Prong B — NO-FOURTH-GENERATION THEOREM (the strongest falsifiable-now prediction)
Two council members derived it independently: topologist (the generation Z/3 lives in
the π₃ˢ=Z/24 stem, which has NO Z/4 to host a fourth) and Clifford geometer (the three
generations are the weights of the j=1 su(2)₊ triplet in ker Γ, with NO j=3/2 room).
TEST: fuse these into ONE rigorous theorem that GU FORBIDS a fourth same-signature
generation — or find where it fails.
- **B-THEOREM** (hostile-verify — it's a positive, and contacts experiment): a
  grade-honest proof that no fourth generation with identical Krein signature
  (+32,−32,0) is admissible. State exactly what forbids it and at what grade.
- **B-CONDITIONAL**: it holds only under a stated extra assumption — name it.
- **B-FAILS** (kill): a fourth same-signature generation IS constructible in the
  framework ⇒ the prediction is false; state the construction.
- Control: a planted "a fifth generation is also forbidden by the same argument" must
  be checked (does the argument forbid exactly three, or something else?); and confirm
  the argument does NOT also forbid the observed three.

## Prong C — TWO-TIME BOUNDARY POSIT (the boldest, most-contested construction)
The wild-frontier construction: reality writes its own boundary condition through a
future-conditioned (two-time: initial + final) structure; the ~8% null stratum is the
genesis seam. Grounded in Weinstein's own "on the 14-manifold you have BOUNDARY
conditions, not initial conditions" + Prong 0's null stratum. TEST + CLASSIFY (three
tiers, do NOT retire for inside-unfalsifiability):
- **C-FALSIFIABLE**: GU's boundary structure supports a two-time formulation that
  makes a DISTINCT third-person-visible prediction (e.g. a small future-conditioned
  cosmological correlation ΛCDM forbids) ⇒ a real new direction with a named test.
- **C-SCAFFOLD** (keep): coherent, coherence-improving, not-yet-directly-falsifiable
  but points at a downstream falsifiable — name it; do NOT retire.
- **C-VACUOUS** (retire): adds no coherence AND generates no downstream test.
- Control: distinguish "makes a distinct prediction" from "observationally identical
  to one-time ΛCDM" — a two-time story that predicts nothing new is C-SCAFFOLD at
  best, not C-FALSIFIABLE.

## Synthesis
Prongs are independent (a structure group / a counting theorem / a boundary
formulation) — run in parallel; hostile-verify A-RECONCILED and B-THEOREM before
banking; classify C honestly by tier. A-RECONCILED would dissolve the top open seam;
B-THEOREM is the hardest experimental-contact prediction; C decides whether the
boldest bet is a direction or a scaffold.

## RESULTS (Prong C in; A & B pending)

- **Prong C — `C-SCAFFOLD` (keep)** (`three-seam-prongC-two-time-boundary-2026-07-21.md`,
  probe exit 0). GU SUPPORTS two-time boundary structure, and it is NATURAL not an
  added posit: three independent facts converge — the ultrahyperbolic (9,5) arena has
  NO clean Cauchy problem (two-sided boundary data is the well-posed alternative),
  Weinstein says exactly this ("no initial conditions... boundary conditions"), and
  Prong 0's ~8% `q<0`/K_S-null stratum is a genuine FINITE boundary (the wild-frontier
  "genesis seam" = the orthodox APS boundary, same fact). A two-boundary field carries
  a future-conditioned two-point correlation a one-boundary (ΛCDM-analog) field forbids
  (`max|C2−C1|=2.7e-2` at a finite final boundary) — BUT it WASHES OUT as the boundary
  recedes (`~exp(−2κ·T_final)`); at an infinite (de Sitter far-future) boundary it is
  observationally IDENTICAL to one-time ΛCDM. So a distinct prediction exists IFF a
  finite, structurally-fixed final-boundary locus is supplied. Controls: PLANT-1
  ("two-time natural for any signature") and PLANT-2 ("always observably distinct")
  both rejected. **NAMED DOWNSTREAM FALSIFIABLE (the scaffold's target):** a
  construction-grade bridge fixing a finite cosmological `z*` from the image of the
  fiber null-stratum ⇒ the two-time formulation predicts a future-conditioned
  large-scale / low-ℓ cosmological correlation at the scale set by `z*` that ΛCDM
  forbids. This is exactly what would UPGRADE Prediction Packet 1 from its confessed
  non-distinctiveness (bare `w≥−1`, shared with all quintessence) to a genuinely
  distinctive prediction. **Promotion condition SCAFFOLD→FALSIFIABLE:** a
  fiber-null-stratum → finite-`z*` map — the SAME unowned selector Prong A (source-
  domain-selector) localized as `S-SILENT-OPEN`, carried to a cosmological locus. Tier
  honesty: not vacuous (real coherence; inside-invisibility of the genesis seam is a
  predicted feature per the three-tier rule), not falsifiable (washes out / scale
  unfixed without the bridge). Registry: this is entry H-H1, now with a named promotion
  path.

- **Prong A — `A-NUMEROLOGY` (resolves the #1 seam: reading-B is numerology; reading-A stands)**
  (`three-seam-prongA-class-relative-frame-2026-07-21.md`, probe exit 0). CRITICAL: this tested the
  class-relative frame in GU's OWN deck/Krein group (the exact escape hatch that saved reading-B
  from Prong 1's standard-topology no-go) — so it is NOT a standard-topology artifact. All four
  reading-B constructions (MTH-CAT twisted coequalizer, MTH-SHEAF H1(S1_SIO;or_deck), EXT-HYPER
  incidence-parity, EXT-INTER consensus coequalizer) reduce to ONE object: the shard-cycle equipped
  with the real line bundle L_deck whose band is sign(K_S), class w1(L_deck). It is NOT sigma, at
  four un-forced links: (1) the bare cycle is orientable (w1(TS1)=0); the record-arrow's two
  orderings are a trivial-class Z/2 TORSOR (a choice), not a nontrivial holonomy; (2) the deck band
  is not a function of shard-type — two helices with different sign(K_S) give the identical
  type-quotient, so the band must be IMPORTED (no functor, per Prong 2); (3) accumulation is
  realization-dependent (K_S,-K_S isospectral (+64,-64) => both flip and no-flip closures are
  valid), and the fresh-token seam sign IS the free coin sigma — so "cycle-orientation = sigma" is a
  TAUTOLOGY OF INSERTION, not a derivation; (4) the realization map f:S1_SIO->F is not canonically
  pi1-nontrivial (GU forces neither degree). Controls PLANT-1/PLANT-2 rejected; power demonstrated
  (forced value for the reading-A metric-fiber loop, realization-split for the cycle). NOT A-DISTINCT:
  no canonical NONTRIVIAL cycle-orientation exists to be a second datum. VERDICT: reading-B (sigma =
  orientation of a first-person/consensus/quotient circle) is RETIRED as numerology; exactly reading-A
  survives — sigma = w1(L_time), an orientation class of a canonical LINE BUNDLE, not of the shard
  cycle. The council members' OTHER legs (confluence<=>H1, generation counting, the descent/cocycle
  relative-orientation rule) are untouched. Registry T-E2 = RESOLVED.
