---
title: "Eric/Curt Wave 3D-B2C7: typed somatic route core and graph-complete obstruction target"
status: exploration
doc_type: construction_result
updated_at: "2026-08-01"
branch: agent/null-clifford-omega1-repair
run: historical-investigation
predecessor: explorations/eric-curt-wave3d-b2c6-fermion-boson-euler-factorization-2026-08-01.md
registry: lab/process/eric-curt-wave3d-b2c7-two-connection-somatic-obstruction.json
probe: tests/channel-swings/eric_curt_wave3d_b2c7_two_connection_somatic_obstruction_probe.py
grade: "PARTIAL CONSTRUCTION PASS WITH 2021/2025 SOURCE-FORK CORRECTION. Direct inspection of the official 2021 draft confirms concrete fermion-Euler and bosonic deformation arrows but also finds the author-facing warning that the combined diagram is inherited from an older version and may contain inconsistencies. The 2025 TOE conversation definitively corrects 'projection' to 'contraction,' supplies the shortened 0-to-1-to-13-to-14 spine, and announces an unreleased two-connection new D-squared with tentatively spoken D_A/F_B/id/D_B entries and unstable signs. It does not repair the 2021 diagram: the stable 2021 F_A/d_B arrow and tentative 2025 F_B entry remain rivals. The durable common-obstruction target is one graph-complete variational residual represented by separately typed Bose and Fermi intertwiners, not equality of raw operators. An exact rational model proves that the symmetrized A/B mixed route cancels arbitrary first and symmetric-second jets to the same Hhat_AB in both fundamental/spinor and adjoint representations. Reconstructing the exact noncyclic G2 Euler covector from all twelve variation directions then forces a nonzero, parameter-free correction Z_var=E_T-[S(Q_seg)+kappa flat(T)]. Neither Q_seg, Hhat_AB, nor the draft-compressed S(F_A)+kappa flat(T) is the graph-complete owner. Carrier linearity shows that core plus correction is a well-typed necessary target in both representations, but it does not prove either somatic square independently emits Z_var; that factorization remains open. The 2025 geometric-quantization passage motivates a downstream reduced-phase-space route but does not replace the local Krein/Hodge primalizer: G3 has only an unreduced preboundary presymplectic current with unclassified kernel and no proved inverse, not an invertible Poisson anchor. The actual active (9,5) maps, source-forked square, off-diagonal zeta/nu map, total current/stress return, moving observation data, and Green/BFV domain remain open. P1/P2/P3 are unused."
---

# Wave 3D-B2C7 - typed somatic route core and obstruction target

## Result first

This swing turns Eric's “two complexes, one Einstein obstruction” statement
into a construction that can be tested without pretending the Bose and Fermi
operators have the same type.

The 2025 Curt/Weinstein conversation sharpens the architecture. It confirms
that the GU curvature operation is a **contraction, not a projection**, but it
does not stabilize the caveated 2021 square. Instead it announces a new,
unreleased two-connection `D^2`. It also discusses geometric quantization,
where an observable is differentiated, sent through a symplectic sharp, and
then lifted through a prequantum connection. Those are useful instructions,
but they are not one map. The local Euler primalizer, the GU form-degree
contraction, the reduced phase-space Poisson anchor, the prequantum lift, and
the observation pullback must remain separate.

The candidate shared object is not a square matrix. It is the graph-complete
variational residual emitted by the source action. In the
connection/distortion slot it is

\[
G:=E_A=E_T
=\mathscr S_\epsilon(F_B)
+\frac12(L+L^\ddagger)T
+M_\epsilon(T,T)
+\kappa_1\flat_1T.
\]

The Bose and Fermi systems may see this same `G` through different maps,

\[
J_{\rm B}(G),\qquad J_{\rm F}(G),
\]

because the adjoint field carrier, spinor field carrier, and their
density-valued equation carriers are different bundles. “Common” therefore
means **one owner with two typed representations**, not equality of raw
operators.

The exact probe constructs the common two-connection core in both
representations:

\[
H^E_{AB}
=\frac12(d_A^Ed_B^E+d_B^Ed_A^E)|_{\Omega^0(E)}
=\rho_E(\widehat H_{AB}),
\qquad
\widehat H_{AB}=F_B+\frac12D_BT.
\]

For both the fundamental/spinor control and the adjoint/Bose control,
arbitrary first and symmetric-second jets cancel exactly. That gives a real
shared route core.

But it is not the full obstruction. Reconstructing the exact G2 Euler
covector from a basis of all twelve rational variation directions gives the
forced correction

\[
\boxed{
Z_{\rm var}
:=E_T-
\left[\mathscr S_\epsilon(Q_{\rm seg})
+\kappa_1\flat_1T\right]\ne0,}
\]

where

\[
Q_{\rm seg}=F_B+\frac12D_BT+\frac13q(T,T).
\]

This correction adds no coefficient and uses no external datum. It is the
formal-adjoint, noncyclic slot-polarization, moving-geometry, and graph-return
content already forced by varying the action. In the finite control,

\[
J_E(G)=J_E(G-Z_{\rm var})+J_E(Z_{\rm var})
\]

holds exactly for both representations and is nonvacuous in both.

So B2C7 does not finish the somatic complex. It gives it a much tighter
architecture: **the action-derived correction must be inside the square, not
added later as a fitted repair.**

## 1. Layer 0

| phrase | object used here | not identified with |
|---|---|---|
| Fermi field | physical `Psi=(zeta,nu)` in `Omega1(S_H) direct-sum Omega0(S_H)` | odd gauge parameter or BV ghost |
| Bose complex | linearized ordinary-gauge deformation complex of the free bosonic field graph | the physical fermion operator |
| common obstruction | one Euler owner with carrier-dependent intertwiners | equality of raw Bose/Fermi matrices |
| on-shell complex | Hessian kills the gauge tangent at a stationary background | off-shell nilpotent BRST differential |
| mixed A/B route | associated-bundle connection composition producing `Hhat_AB` | graph-complete Euler covector |
| action residual | density-valued variational covector `E_T` | curvature 2-form before Shiab/Riesz conversion |
| GU contraction | equivariant curvature/form-degree map, with Hodge/density conversion as typed | idempotent projection or field-space Poisson sharp |
| local Euler primalizer | moving trace-reversed Krein/Hodge/density map from an equation dual to a field carrier | Hamiltonian vector field on reduced phase space |
| G3 preboundary form | `omega_G3 in Omega^(13,2)` before integration and reduction | invertible symplectic form or Poisson tensor |
| reduced Poisson anchor | future inverse of a nondegenerate reduced field-space two-form | Shiab, fermion Riesz, or raw bulk Euler map |
| prequantum lift | future covariant derivative along a Hamiltonian vector field | map that turns a bulk Euler equation into a gauge connection |
| observation | `L_s,R_s,L_s^vee,L_s^!` with separate field/equation/pairing roles | symplectic sharp or local Riesz map |
| augmented torsion | homogeneous distortion `T=A-B` in the selected graph | ordinary torsion without the source dictionary |

The complete free-field Euler owner is really

\[
\mathfrak G=(E_A,E_\epsilon,E_g),
\]

with `E_A=E_T` and the reduction/metric components receiving all G3 graph
returns. The exact finite probe executes the `E_T` owner and ordinary-gauge
covariance. It does not yet construct an active map from the whole tuple into
the fermion equation carrier.

## 2. Primary-source collision

The transcript was insufficient because its route diagram is visual. We
therefore inspected the official 2021 draft directly at pages 46-49. The PDF
has SHA-256
`3f28d742234a9841fc8e51ff172053200aa3eddf3ece38154a3328b9ebd186d4`,
matching the repository's earlier rendered-display receipt.

### `SOURCE-CONFIRMS`

- Portal `01:47:01--01:54:09` gives the `Omega0(S) direct-sum Omega1(S)`
  carrier, Hodge-dual copies, two connection derivatives, zero-order
  augmented-torsion difference, high/low cancellation target, and two Bose/
  Fermi somatic complexes with one generalized-Einstein obstruction.
- Portal `01:55:03--01:58:27` makes `zeta in Omega1(S)` and
  `nu in Omega0(S)` two separate physical fields in coupled Dirac-role
  equations.
- Draft p.46, eqs. `(9.16)--(9.17)`, says to begin with operators “like” the
  displayed fermion block and retains alternate lower-right blocks. That
  block is `MANUSCRIPT-CANDIDATE / UNSTABLE`, not source-selected.
- Draft p.46, eqs. `(9.18)--(9.20)`, explicitly gives the fermion Euler packet

  \[
  \Upsilon_\omega^F
  =*\left(
  d_{A_\omega}\nu+\mathscr S_\omega d_{A_\omega}\zeta,
  d_{A_\omega}^*\zeta,
  \bar\nu\zeta+\bar\zeta\nu+\mathscr S_\omega\bar\zeta\zeta
  \right).
  \]

- Draft pp.47-48, eqs. `(10.1)--(10.9)`, types the bosonic deformation
  complex and supplies

  \[
  \delta_1^\omega=(d_{A_\omega},DL_{\epsilon_\omega}),
  \]

  \[
  \delta_{2,a}^\omega
  =\mathscr S_\omega d_{A_\omega}+\kappa_1*,
  \qquad
  \delta_{2,b}^\omega
  =F_{A_\omega}\wedge\mathscr S_\omega-\kappa_1*d_{B_\omega}.
  \]

### `SOURCE-NEGATIVE` and repo-required implications

Draft p.49, eq. `(10.10)`, overlays the Bose/Fermi arrows but immediately
warns that the diagram was carried over from an older version and may contain
inconsistencies until stabilized. Therefore:

1. the typed formula rows above are construction inputs;
2. the exact combined arrow topology and signs are not stable source law; and
3. a repo reconstruction must prove its commutative diagram rather than cite
   the old diagram as proof.

As a repo-required Layer-0 implication, the bosonic `delta_1` starts from
`Omega0(ad)` and ordinary `H` gauge symmetry. It is not a spinorial minus-one
map. Likewise, the common obstruction must be reconstructed through typed
carrier maps rather than assumed to be equality of raw Bose/Fermi matrices.
These are necessary consequences of the displayed types, not corrections
Weinstein states in the interview.

### `SOURCE-CORRECTS`

At `01:36:35--01:36:56`, Weinstein expressly says Curt was right and he was
wrong: the Einstein/Shiab-like operation is a **contraction**, not a
projection. This is the definite 2025 author correction.

### Within `SOURCE-CONFIRMS`: a new unreleased successor

The 2025 TOE conversation also adds useful source material that is not itself
an erratum:

- At `00:13:44--00:18:17`, Weinstein gives a deliberately approximate
  geometric-quantization chain

  \[
  f\longmapsto df\longmapsto X_f
  \longmapsto \nabla^{\rm preq}_{X_f}.
  \]

  This is general phase-space/prequantum mathematics. It is not stated as a
  GU map from a bulk Euler covector to a physical connection.
- At `01:36:08--01:36:56`, he describes Chern--Simons/star and
  Einstein-contraction branches whose
  Euler--Lagrange equations contain curvature and whose contraction sends
  two-form data toward a one-form-shaped equation. In fourteen dimensions
  the Hodge/density degree conversion must still be typed explicitly.
- At `02:38:12--02:42:55`, he gives the stable spine
  `d_A^2=F_A`, rolls `d_A+d_A^*`, and shortens the sequence to
  `0 -> 1 -> 13 -> 14` by contraction and Hodge star.
- At `02:44:06--02:45:13`, he announces an unreleased two-connection “new
  `D^2`,” tentatively reciting entries resembling `D_A,F_B,id,D_B` and then
  correcting two signs in the second column. It is said to become a complex
  on shell, making the Einstein condition cohomological. The domains,
  degree shifts, matrix order, curvature action, and signs are not supplied.

This does **not** repair draft `(10.10)`. It creates a live source fork:

1. the explicit 2021 bosonic arrow uses `F_A` together with `d_B`;
2. the tentative 2025 spoken successor names `F_B` and `D_B`;
3. neither may be silently blended into a source-exact square.

The 2025 statement therefore corrects the construction grammar and opens a
new candidate family. It does not prove

\[
\Delta_{A,B}^2=\rho(E_{\rm GU}),
\]

nor identify that square with a Noether/BV differential, `Q_seg`, or the G3
preboundary form.

### `SOURCE-SILENT`

The sources do not provide:

- the active trace-reversed `(9,5)` native-real-form port;
- the two distinct local primalizers: the connection-density Euler
  pseudo-musical used by `J_B/J_F`, and the `C_+`/Krein/right-`H` Riesz map
  from fermion Euler covectors back to the physical field carrier;
- the moving Hodge, density, Clifford projector, Shiab, and observation jets;
- a stable off-diagonal `zeta/nu` operator completing p.49;
- the map carrying the fermionic composition/current/stress back into the
  full graph-complete bosonic Euler tuple; or
- a Green domain, local odd symmetry, or BV completion.

The 2025 source is also silent on a GU prequantum line bundle, polarization,
integrality condition, reduced phase space, Poisson bracket, and a map from
bulk `E_T` to the differential of a reduced observable. G3 supplies a nonzero
preboundary two-form, but its domain, kernel quotient, boundary charges, and
nondegeneracy remain open.

Portal `02:03:07` expressly says the stress-energy up-and-back and Dirac
up-and-over versus over-and-up cancellations were still being assembled.

Disposition:
`SOURCE-CONFIRMS_PLUS_SOURCE-CORRECTS_PLUS_SOURCE-SILENT`.

## 3. Typed route registry

### Bose route

Use the free-field tangent, not the draft's compressed two-slot notation:

\[
Q_B^{-1}=\Omega^0(Y,\operatorname{ad}P),
\]

\[
Q_B^0
=\Omega^1(Y,\operatorname{ad}P)
\oplus T_\epsilon\mathcal R
\oplus T_g\mathcal G_{\rm DW}.
\]

The first arrow is the ordinary gauge tangent

\[
\delta_1^B(\gamma)=(-D_A\gamma,\gamma\epsilon,0)
\]

up to the already declared sign convention. The second arrow must be the
linearization of the whole G2/G3 Euler map, including `B=A_LC(epsilon,g)`,
moving Shiab/flat/Hodge/density, and graph returns.

At a genuine stationary background,

\[
\delta_2^B\delta_1^B=0.
\]

Off shell it is the infinitesimal covariance of `mathfrak G`, generically
nonzero. The exact control verifies a nonzero commutator orbit and its
on-shell zero specialization.

### Fermi route

The physical carrier is

\[
Q_F=\Omega^1(Y,S_{\mathbb H})\oplus\Omega^0(Y,S_{\mathbb H}),
\qquad \Psi=(\zeta,\nu).
\]

Its variational equation naturally lands in

\[
Q_F^\vee
\simeq
\Omega^{13}(Y,S_{\mathbb H}^*)
\oplus\Omega^{14}(Y,S_{\mathbb H}^*),
\]

not canonically back in `Q_F`. A same-bundle operator requires a moving
Riesz map built from the `(9,5)` Hodge star, density, Krein form, one-form
metric, pseudo-musical maps, and right-`H` structure.

Thus a correct full route has the form

\[
Q_F\xrightarrow{D_{F,1}}Q_F^\vee
\xrightarrow{\mathcal R_F}Q_F
\]

or another explicitly typed equation carrier. Writing `D_F^2` without
`mathcal R_F` is a bundle-type error; omitting a Green domain leaves the
unbounded operator without an analytic realization.

The 2025 geometric-quantization passage does not remove this requirement.
Its symplectic map has a different domain and symmetry class:

\[
dH\in T^*\mathcal P_{\rm red}
\xrightarrow{\Pi_{\rm red}^{\sharp}}
X_H\in T\mathcal P_{\rm red}
\xrightarrow{\nabla^{\rm preq}_{X_H}}
\operatorname{Diff}^1(\mathcal L).
\]

By contrast, G3 currently supplies only

\[
\Theta_{G3}\in\Omega^{13,1},\qquad
\omega_{G3}=\delta_{\mathcal F}\Theta_{G3}\in\Omega^{13,2}.
\]

The current is not yet a field-space flat. The missing stages are

\[
\omega_{G3}
\xrightarrow{\int_\Sigma}
\Omega_\Sigma\in\Omega^2(\mathcal F_\Sigma)
\xrightarrow{\Omega_\Sigma^\flat}
T^*\mathcal F_\Sigma.
\]

They require an admitted thirteen-chain, boundary field space, and domain.
Before those are selected and any characteristic kernel is classified and
quotiented, there is no inverse Poisson sharp. Moreover, the bulk Euler
covector `E_T` vanishes on the solution space and is not automatically the
differential of a nontrivial boundary observable. A Hamiltonian/prequantum
route therefore remains a separate downstream construction, not a
replacement for the local Krein/Hodge primalizer.

The active Hodge signs are

\[
*^2|_{\Omega^p}=(-1)^{p(14-p)+5},
\]

so degrees `0,1,2,13,14` have signs `-1,+1,-1,+1,-1`. Euclidean signs are a
planted failure.

## 4. Exact two-representation route core

For any associated bundle `E` on which both `A` and `B` preserve the required
structure, define

\[
H^E_{AB}
=\frac12(d_A^Ed_B^E+d_B^Ed_A^E)|_{\Omega^0(E)}.
\]

The exact rational probe executes this twice:

1. `E` in the fundamental representation, the finite Fermi/spinor control;
2. `E=ad`, the Bose control.

On two independent first-/second-jet assignments in each representation,

\[
H^E_{AB}=\rho_E(\widehat H_{AB}),
\qquad
\widehat H_{AB}=F_B+\frac12D_BT.
\]

Each asymmetric route retains jet dependence and is rejected. The
symmetrization is load-bearing.

This establishes one shared adjoint-valued curvature **core** before the
variational completion. It does not establish the full common-owner
factorization and does not identify `Hhat_AB` with `E_T`.

## 5. The forced graph-complete correction

The probe reconstructs `E_T` rather than assuming it. In the exact G2
noncentral fixture, it evaluates the action derivative on a basis of all
twelve independent one-form/matrix variation directions. It then inverts the
nondegenerate wedge-pairing matrix over the rationals to recover the unique
density-dual Euler form.

Every basis variation pairs back to the original derivative exactly.

Three compressed candidates then fail:

\[
\mathscr S(Q_{\rm seg})+\kappa\flat T\ne E_T,
\]

\[
\mathscr S(F_A)+\kappa\flat T\ne E_T,
\]

\[
\mathscr S(\widehat H_{AB})\ne E_T.
\]

The difference `Z_var` is not an optional new term in the action. It is the
part of the derivative already generated by varying all slots of the existing
action. The old G2 plant remains decisive: there are `y,z` with

\[
q(y,z)=0
\]

but nonzero polarized Euler response. Therefore no completion depending only
on one linear Shiab of `q(y,z)` can carry the whole obstruction.

This is the main construction result. It tells the next somatic operator
exactly what must be factorized.

## 6. What “shared” now means

Let `J_B` be the adjoint/Bose action and `J_F` the active spinorial action
after the two missing local primalizers are built. One converts the
connection Euler density into the adjoint-valued one-form carrier used by the
representation action; the other converts the fermion equation dual into the
physical `Q_F` carrier. The target diagram is

\[
\begin{array}{ccc}
\text{Bose route} & \longrightarrow & J_B(G)\\
 & G=E_T & \\
\text{Fermi route} & \longrightarrow & J_F(G).
\end{array}
\]

The exact finite control verifies only that this target is carrier-compatible
under the preregistered linear representation maps:

\[
J_E(G)
=J_E(\mathscr S(Q_{\rm seg})+\kappa\flat T)
+J_E(Z_{\rm var})
\]

component by component. Both images are nonzero, and the physical `zeta/nu`
carrier admits the same blockwise target before the unfinished source-silent
off-diagonal couplings are added.

This equality is definitional at the tested level: `Z_var` was defined as
`E_T-core`, and representation linearity carries that identity to both
carriers. It does **not** prove either independently constructed Bose or Fermi
somatic composition emits `Z_var`. The substantive results are the independent
Euler reconstruction, the shared mixed-curvature core, `Z_var != 0`, and the
failure of the compressed curvature owners. The common-owner factorization is
therefore a necessary, nonvacuous B2C8 target, not a B2C7 theorem.

No equality `J_stat=Pi_red^sharp` is claimed or expected. A future reduced
Hamiltonian map may coexist with the static somatic intertwiner, but relating
them would require its own theorem.

## 7. External datum and surplus

B2C7 uses no P1, P2, or P3.

The forced correction has zero new coefficient freedom:

- `E_T` is fixed by the chosen action;
- the two-connection core is fixed by `A,B,T`;
- `Z_var` is their difference.

The remaining choices are construction debts, not datum:

- active `J_F`/Riesz map;
- connection-density Euler pseudo-musical;
- off-diagonal `zeta/nu` route;
- fermion-current/stress map to `mathfrak G`;
- boundary domain and observation descent.

The future presymplectic reduction, Poisson anchor, and prequantum lift are
also construction debts, not datum. A nonzero two-form does not price or
select its kernel quotient, domain, polarization, or observable.

The global constraint surplus remains `UNCOMPUTABLE`, because the ranks and
allowed coefficients of those active maps are not yet known. Calling them
external datum would hide continuous local construction freedom inside P1/
P2/P3 and is rejected.

## 8. Seven-axis disposition

| layer | result |
|---|---|
| Layer 0 | physical fields, gauge deformation, same-owner intertwiners, curvature contraction, Euler primalizers, preboundary form, Poisson anchor, prequantum lift, and observation maps separated |
| L1 geometry | active trace-reversed `(9,5)` retained; Curt literal `(7,7)` remains a rival carrier |
| L2 fields | `A,epsilon,g` free; `B,T,S,flat` derived; `zeta,nu` physical |
| L3 symmetry | ordinary gauge deformation is on-shell complex/off-shell Euler-covariant; no local odd symmetry claimed |
| L4 dynamics | exact common two-representation `Hhat_AB` core plus nonzero action-forced `Z_var`; actual active somatic square open |
| L5 quantum | geometric-quantization passage retained as a downstream construction guide; no Poisson/prequantum or BV promotion; constrained-real Riesz/domain open |
| L6 physics | no SM, mass, count, cosmology, or generation recovery claimed |
| L7 evidence | `28 exact + 5 type-level + 17 planted = 50 PASS`; official PDF pages plus 2025 transcript collision inspected |

## 9. Highest-information next gate

The 2025 source collision makes a narrower gate more efficient than fitting
the off-diagonal fermion square immediately:

`ECW3D-B2C8-SOURCE-FORKED-TWO-CONNECTION-SQUARE-AND-ACTIVE-EULER-MAP-DISCRIMINATOR`.

It should run two local construction tracks and one analytic track:

1. Enumerate and type every two-by-two realization compatible with the
   explicit 2021 `F_A/d_B` arrow and, separately, the tentative 2025 spoken
   `D_A,F_B,id,D_B` list and two second-column minus signs. Do not canonize a
   row-major matrix or merge `F_A` and `F_B` by attribution. Declare every
   source and target carrier before composition.
2. Construct the two active local maps separately:
   the connection-density Euler pseudo-musical used inside `J_B/J_F`, and
   `R_F:Q_F^vee->Q_F` from the `(9,5)` Hodge star, density, active Krein form,
   one-form pseudo-musical, trace-reversed DeWitt fibre form, `C_+`, right-`H`,
   moving projectors, and observation-dual data.
3. Only after those maps are typed, square every candidate off shell before
   imposing `E_T=0`. Reject any candidate that retains unowned first/second
   jets or fails to factor through the same graph-complete
   `mathfrak G=(E_A,E_epsilon,E_g)` with independently action-emitted
   corrections. The definitional identity `Z_var=E_T-core` is not an emission
   certificate.
4. In parallel, continue B2C6B/G4 only as **provisional** domain analysis on
   the existing bosonic and repaired-fermion sector packets: test candidate
   thirteen-chains, Green domains, boundary-charge retention, possible
   kernels, and observation descent. After B2C9 completes the coupled fermion
   action and boundary terms, recompute the final kernel, charges, domain,
   quotient, and any anchor on `Theta_tot,omega_tot`.
5. Do not invert `omega_G3` or call its image gauge-reduced. First construct
   `Omega_Sigma` by integration on the admitted chain. A Poisson anchor on the
   full cotangent is eligible only if the reduced flat is a strong symplectic
   topological isomorphism. Otherwise declare an admissible cotangent
   subbundle `im Omega_red^flat`, a partial inverse there, and Hamiltonian/
   Jacobi closure. It acts on differentials of reduced observables/charges,
   not raw bulk `E_T`. A later prequantum lift additionally needs a bundle,
   connection, curvature/integrality, and polarization.
6. Compare the local Riesz and reduced Hamiltonian routes only after both
   exist. Neither equality nor substitution is assumed.

The following B2C9 gate may then build the stable independent-dual
off-diagonal `zeta/nu` operator, total fermion action/current/stress return,
and completed preboundary packet against the surviving active square. Final
G4/B2C6B observation/domain/kernel reduction must follow B2C9. A still later
gate may attempt a Poisson anchor and prequantum lift. This avoids fitting
coefficients to a source fork or to a nonexistent inverse.

The guided B2C2C mixed-carrier super-IG action route remains separate.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; `TG-1 AND TG-2 AND TG-3`
remains false.

## Validation boundary

The executable probe reports:

```text
ECW3D-B2C7: 28 exact + 5 type-level + 17 planted = 50 PASS
```

The type-level items are not promoted: the two active local primalizers, the
source-forked two-connection square, the off-diagonal physical `zeta/nu`
completion, the total current/stress return, and a reduced
Poisson/prequantum route remain unconstructed.

No complete active `Y^14` somatic operator, full graph-tuple intertwiner,
fermion action, odd BV master equation, global domain, observation descent,
stationary nonflat vacuum, Standard Model recovery, anomaly/index,
generation count, cosmological amplitude, or PP3 prediction is claimed.
