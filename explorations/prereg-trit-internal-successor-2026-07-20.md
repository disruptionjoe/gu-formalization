---
title: "PRE-REGISTRATION: the trit-internal successor check — does N4's frozen Z/3 supply the trit half of the minimal Z/6 input, collapsing the external requirement to the payload bit alone?"
status: pre_registration
doc_type: pre_registration
created: 2026-07-20
directed_by: "Joe direct chat, 2026-07-20 (council-unanimous: pre-register before looking; the kernel is epistemically hot)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
binding: >
  This registration BINDS the successor check before any computation
  looks. Commit timestamp = the notary. The executor may not modify the
  hypothesis, the test, the allowed freedom, or the outcome conditions.
---

# Pre-registration: the trit-internal check

## Hypothesis (bound)

The minimal external input for conditional forcing is Z/6 = Z/2 x Z/3
(commit f513fcf): the payload bit (deck admissibility) times a trit
anchor (mod-3 phase matching to the canonical sixth roots of unity in
the commutant scalars). HYPOTHESIS: the trit half is ALREADY SUPPLIED
by frozen structure — specifically by the N4-identified Z/3 (the
canonical identification of the Nikulin monodromy characters with
<J(k)>, commit 4f5a44c, unique up to the admissible inversion). If
true, the minimal EXTERNAL input collapses to the payload bit alone:
GU + one bit forces the order-3 count.

## The test (bound)

1. Construct, from the N4 identification data ONLY (the rho characters,
   the Phi maps, the admissible inversion — no other choices), the
   candidate trit anchor in the role the conditional-forcing probe
   requires (the mod-3 matching on the commutant scalars).
2. Run the EXACT sufficiency battery the Z/6 oracle passed (commit
   f513fcf's probe, imported unmodified): both admission gates, deck
   admissibility via the bit, degree computation on generic members —
   sufficiency = all members degree == 1 mod 6 with order-3 forced.
3. Allowed freedom: the N4 admissible inversion {Phi_D, Phi_B} ONLY.
   Any additional choice required = outcome K-a. No axis scanning; no
   convention shopping (the N6 lesson binds).
4. Controls (power-demonstrated): scrambled-character trit must FAIL
   the battery; the X1.5 generic-reader must still scatter; the
   f513fcf planted weight-blind oracle must still be rejected.

## Outcome conditions (bound; report which fired)

- K-a (NOT CANONICAL): the N4 Z/3 cannot occupy the trit-anchor role
  without a new choice beyond the inversion -> the trit remains
  external; minimal input stays Z/6; the N4 identification is
  arena-side only.
- K-b (CANONICAL BUT INSUFFICIENT): transports canonically but fails
  gates/degree -> the two Z/3s are identified yet the anchor needs
  MORE than identification; characterize the residue exactly.
- K-c (CONFIRMS): bit + frozen-N4-trit passes the full battery ->
  the minimal external input = ONE BIT; "GU + the arrow choice forces
  three generations" at fixture grade, conditional on the framed fork
  as always. MANDATORY next step regardless of excitement: independent
  hostile verification before any packaging, posture, or paper motion.

## Severity note

This check was named as the successor question in f513fcf's doc BEFORE
this registration; no computation has touched the question between the
naming and this binding. The N6 precedent governs: if the outcome is
adverse, it stays adverse; near-misses are new gated nodes, not saves.
