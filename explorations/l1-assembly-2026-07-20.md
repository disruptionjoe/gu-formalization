---
title: "L1 ASSEMBLY swing: the diagonal-boundary law's first missing lemma LANDS at fixture grade -- outcome L-c: the six reading classes assemble into one category of Z/2-sets over ONE Z/2 (finite products, terminal object, equivariant diagonal), the THREE senses of fixpoint-free reconcile as the three graded pieces of one implementation group G (extension 1 -> Sp(1)_comm -> G -> Z/2_deck -> 1: label = coset, ray = the wholly-Kramers antilinear part, free = kernel), the Lawvere parent APPLIES to the assembly, and the planted C-04 prime-3 no-go is excluded STRUCTURALLY (Hom_{Z/2}(triv, B) = empty); STAGE-0 warm-up: the section-classification Z/2 and the payload-bit Z/2 are ONE class -- the seam U_h = -(e0 e9)(e3 e11)(e4 e12) is a habitat word, one character sgn_K, no separating element among 91 planes + random words"
status: active_research
doc_type: exploration
created: 2026-07-20
directed_by: "Joe direct chat, 2026-07-20 (L1 assembly + the Z/2-identity warm-up)"
extends:
  - explorations/diagonal-boundary-unification-2026-07-20.md
inputs:
  - explorations/diagonal-boundary-unification-2026-07-20.md
  - explorations/sector-relative-section-theory-2026-07-20.md
  - explorations/sig-b5-habitat-verification-2026-07-20.md
  - explorations/m1-third-reading-2026-07-20.md
  - explorations/torsor-k-sequence-2026-07-20.md
  - explorations/araki-scale-route-2026-07-20.md
  - explorations/smatrix-sector-face-2026-07-20.md
  - canon/no-go-quaternionic-parity-generation-sector.md
  - tests/channel-swings/diagonal_boundary_probe.py
  - tests/channel-swings/sig_b5_habitat_probe.py
  - tests/channel-swings/sector_relative_section_probe.py
  - tests/channel-swings/torsor_k_sequence_probe.py
runnable:
  - tests/channel-swings/l1_assembly_probe.py
claim: none
canon: none
posture: none
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# L1 assembly: the missing lemma, run

The diagonal-boundary swing (aadad6b) ended with a named first missing
lemma: **L1 (Branch B)** -- assemble the per-no-go reading classes into
one category with finite products and a diagonal; until then the
re-typing of the class-relative no-gos as the third-person/first-person
boundary "is a TYPING, not a theorem." It also flagged the obligation
that must be discharged at L1 time: THREE distinct senses of
"fixpoint-free" -- label-involution (Araki w, S-matrix V, section deck),
ray-fixpoint-free (Kramers, J^2 = -1 on rays), free group action
(record-sector Sp(1)) -- must be reconciled or the assembly is a pun.
This swing runs the lemma, with the 63-panel Sheaf Theorist's question
#4 as the bounded warm-up.

Receipt: `tests/channel-swings/l1_assembly_probe.py` -- deterministic
(two runs byte-identical), numpy only, seeded 20260720, exit 0 --
HEADLINE `24 [E] + 5 [F] = 29 (setup [T] = 2 excluded) ALL PASS`.

**Verdict up front: outcome L-c fired.** Stage 0: the two Z/2s are ONE
class. Stage 1: the category assembles; the three fixpoint-free senses
reconcile as the three graded pieces of one implementation group; the
Lawvere parent applies to the assembly; the planted C-04 prime-3 no-go
is excluded structurally, not by fiat. The boundary law is stated in
Section 4 at fixture grade. What that grade does and does not buy is
stated in Section 5 and Boundary.

## 1. Stage 0 -- the two Z/2s are one class

The question (Sheaf Theorist #4): is the section-classification Z/2
(the deck twist classifying sector-relative sections) the SAME
cohomology class as the payload-bit Z/2 (the twisted-bundle class of
sig-b5), or two isomorphic-but-distinct classes? Answer: **the same
class**, in the strongest sense available at fixture grade, with the
identifying map exhibited rather than posited:

- **The identifying map is an algebra identity.** The section-theory
  seam operator is EXACTLY a habitat word:
  `U_h = -(e0 e9)(e3 e11)(e4 e12)` (machine-exact, defect 0.0), i.e.
  (minus) the spin lift of the composite of THREE mixed-plane
  pi-rotations -- three generator loops of the habitat fiber
  F = GL(4,R)/O(3,1)-type twist, an ODD mixed count. Conjugation by
  U_h flips exactly the six legs {0,3,4,9,11,12}: the seam IS a loop
  of frames, and its class in the habitat pi_1 is the generator.
- **One character.** Define sgn_K(W) by W K_S W^-1 = sgn_K(W) K_S.
  Machine-checked: sgn_K(spin lift) = habitat frame-transport sign
  (the sig-b5 kprod) = mixedness parity, on each seam plane, on ALL 91
  coordinate-plane loops (45 mixed -> -1, 46 same-sign -> +1), and on
  20 random words (the homomorphism law holds). There is one Z/2
  datum, {+K_S, -K_S}, and one character reading it.
- **Two readings, same value.** The SECTION reading (the symbol
  M = Ku D: deck-oddness `M(U_h D U_h^-1) = -U_h M(D) U_h^-1` is
  DERIVED in the probe from `U_h K_S U_h^-1 = -K_S` alone, for random
  K_S-self-adjoint D; M carries exactly one K_S factor, so M(-K_S) =
  -M while P is blind) and the PAYLOAD reading (anchor exchange
  P_+(-K_S) = P_-(K_S), Krein-charge flip -- the sig-b5 Part-C chain)
  agree with sgn_K on EVERY exhibited element. [F] teeth: a second,
  independent Z/2 would need an exhibited element separating the two
  readings; the sweep (91 plane lifts + 20 random words) finds ZERO
  separators. One disagreement would have refuted the identity.

Honest form of "same cohomology class": the two classes live over
different bases (the end-family base loop; the habitat fiber loop
space), so the precise fixture-grade statement is: both are readings
of the one K_S-sign double cover under the one character sgn_K, and
the exhibited spin-lift map carries the seam loop to a generator-class
habitat word, identifying the two classes as pullbacks of one datum --
not merely abstractly isomorphic. This is the L1 ingredient the
mission named: **one Z/2 object, many reading classes.**

## 2. The reconciliation -- three senses, one structure

The three senses are honestly DISTINCT at operator level ([E] B3: the
linear flip implementation U_h has exhibited fixed rays, so
label-fixpoint-freeness does not give ray-fixpoint-freeness; [E] B1:
J_quat FIXES K_S, so the Kramers involution is label-even and is NOT
the flip). The reconciliation is not an identification of operators; it
is the discovery that the three senses are the three graded pieces of
ONE finite structure, all machine-exact in the verified Cl(9,5) rep:

There is one group G of exhibited symmetries with an exact sequence

    1  ->  Sp(1)_comm  ->  G  ->  Z/2_deck  ->  1

(U_h normalizes the commutant: `U_h J_quat U_h^-1 = J_quat` exactly;
the coset map is sgn_K), carrying a second grading: linear/antilinear.

- **[label] = the COSET grading.** G/Sp(1)_comm = Z/2 acts on the
  label object B = {+K_S, -K_S} by the fixpoint-free swap. The Araki
  w, the S-matrix V, and the section deck U_h all cite this piece:
  each is a linear involution flipping its label object (w K w = -K,
  {V, eps} = 0, U_h K_S U_h^-1 = -K_S).
- **[ray] = the ANTILINEAR grading.** EVERY antilinear element of G
  squares to -1: (zJ)^2 = -1 on the kernel side and (U_h zJ)^2 = -1 on
  the deck side, random unit z, with <A psi, psi> = 0 identically on
  both cosets -- the antilinear part of G is WHOLLY Kramers-type; no
  antilinear element has a fixed ray. This is the quaternionic class
  of the commutant showing up as a property of the whole group; the
  ghost-Kramers composite S_VJ of the operator-grade swing is exactly
  the deck-side antilinear element U_h J_quat, now located inside G.
  The Kramers no-go cites this piece.
- **[free] = the KERNEL.** Sp(1)_comm acts freely on nonzero states,
  every U_h u (u a unit quaternion) implements the SAME flip, and the
  ratio of any two implementations lies in the kernel: the
  implementation set of the categorical involution is a free
  Sp(1)_comm-torsor. The record-sector protection cites this piece --
  and this is WHY record readings must be Sp(1)-invariant: readings
  well-defined on reading CLASSES (independent of the implementation
  choice) are exactly the kernel-invariant ones.

**The dissolution dial survives reconciliation.** Real-type antilinears
(sigma^2 = +1) have fixed rays and no multiplicity forcing (the (7,7)
miniature, re-run). And -- new, structural -- m1's conjugation swap
u_+ <-> u_- is ITSELF real-type (square +1, exhibited fixed mode): the
universal-null lemma sits on the DISSOLUTION side of the ray dial.
That is a mechanism-level explanation of the parent doc's grading of
universal-null as ANALOGOUS-not-instance: its involution is the wrong
antilinear type to force blindness; what it forces instead is
indefiniteness -- the supply line, exactly as typed.

**The Kramers embedding closes the loop.** [E] C4: an isometric
Phi : C^4 -> C^128 with `Phi(J_m conj v) = J_quat Phi(v) =
(U_h J_quat) Phi(v)` (image inside the U_h = +1 eigenspace): the
Kramers fixture's involution is the RESTRICTION of the flip's
antilinear implementation. The parent doc's Krein-analyst caveat
("reading the Kramers pairing as the alpha of the parent ... should be
attacked at L1 time") is hereby adjudicated: the identification
survives, in corrected form -- J is not the flip (B1), it is the
antilinear DRESS of the flip (C4), and ray-fixpoint-freeness is the
statement that every antilinear dress is Kramers-type (B4).

## 3. The category C_read, defined and machine-realized

**Objects.** The six reading classes, realized at fixture grade as
Z/2-sets (orbit-closed fixture carriers with their exhibited
involutions and even/odd reading batteries), plus the label object
B = ({+,-}, swap):

| object | carrier | involution | even (excluded) class -- verified | odd datum | external cure -- verified |
|---|---|---|---|---|---|
| X_ar entropy-class | Krein toy states | cut-swap w | rel-entropy vs invariant ref, purity: BLIND | k = Tr(rho K) | eps-tilted reference reads (~eps) |
| X_sm S-matrix | 2-channel S | V (ghost conj) | traced graded functionals = 0 exactly | per-channel graded T | graded preparation reads |
| X_kr J-commuting probes | C^4 quaternionic | J_m conj | all-even multiplicities | odd signature/rank | rank-3 non-J import reads |
| X_m1 conserved pairings | 2-mode space | conj swap (real-type) | conserved pairings off-diagonal, self-null | NONE (positivity is not a B-datum) | none: supply line |
| X_rec record bilinears | C^128 (W229) | kernel Sp(1) / coset U_h | record current Sp(1)-invariant | Krein charge (coset-odd) | anchor exchange = holonomy (sig-b5) |
| X_sec sections/K_S-linear | C^128 symbols | deck U_h | q, P, margin: deck-blind | M-sign / k-reading | orientation datum (f5/araki chain) |

**Morphisms.** The exhibited structure-preserving maps, every one
machine-checked equivariant (the naturality squares of the ONE
involution commute): the label maps lambda_ar, lambda_sm, lambda_rec,
lambda_sec to B (each object involution covers the swap); the Kramers
embedding Phi into X_rec (Section 2); the Stage-0 identity as the
X_sec <-> X_rec arrow (two readings of one B); parity transfer as the
S-matrix face's own bit-conversion (cited from the smatrix receipts,
its label map re-verified here). X_m1 joins with its involution but
carries NO map to B -- recorded, and harmless: an object without an
odd label map obstructs nothing in a category of Z/2-sets.

**L1 structure, verified rather than assumed.** Finite products with
the universal property (existence AND uniqueness of the pairing map,
exhaustive over equivariant maps at label level), equivariant
projections and diagonal (`Delta . alpha = (alpha x alpha) . Delta`),
terminal object (the point; unique equivariant map from each object,
enumerated). The one categorical involution alpha is the K_S-sign flip
of Stage 0; its per-object components are the exhibited involutions
(linear on X_ar/X_sm/X_sec, antilinear-dress on X_kr via C4, torsor of
implementations on X_rec via B5).

**The "exactly" clause is an identity, not an aspiration.** For any
functional F and any alpha-orbit, F(x) - F(alpha x) = 2 F_odd(x)
(machine-verified on random functionals): F separates an orbit iff its
odd part is nonzero there. So "excluded class = alpha-equivariant
class" is EXACT by construction once the native classes are verified
even -- and they are, per object (the C1a-e receipts re-run the parent
probe's C3 legs in compact form). The per-sector CONTENT remains what
the parent doc said it was: that the native/internal class IS the even
class, computed per fixture, not the tautology.

## 4. The parent applies: the boundary law at fixture grade

With L1 exhibited (Section 3) and L2 the one fixpoint-free swap
(Section 2), the parent skeleton runs on the assembly and both
conclusions land (exhaustive, with the alpha = id dissolution
control):

> **THE DIAGONAL-BOUNDARY LAW (fixture grade).** Let C_read be the
> assembled category of reading classes (six objects + B, morphisms
> the exhibited equivariant maps), alpha its single involution -- the
> K_S-sign flip, one Z/2 datum by Stage 0. Then:
> (i) [L1] C_read has a terminal object, finite products, and
> equivariant diagonals; admissibility predicates are the maps to B.
> (ii) [L2] alpha is fixpoint-free on B, and its three fixpoint-free
> senses are the three graded pieces of the one implementation group
> G (label = coset, ray = wholly-Kramers antilinear part, free =
> kernel torsor).
> (iii) [conclusion a] No weakly point-surjective T : A x A -> B
> exists over the assembled label object: the twisted diagonal
> d = alpha . T . Delta escapes EVERY T (exhaustive, |A| <= 3), and
> the escape dissolves at alpha = id.
> (iv) [conclusion b] No alpha-invariant valuation exists: every
> definite value of the bit is an external, symmetry-breaking
> selection.
> (v) [the exactly clause] Per object, the excluded reading class
> equals the alpha-equivariant class EXACTLY (separation iff odd part
> nonzero; native classes verified even; external cures verified per
> object).
> (vi) [scope] The five orientation no-gos (Kramers, selection rule,
> transparency/ghost/parity, record protection, sections) are
> instances through their label maps / embeddings. The universal-null
> object lies IN the category but its exclusion (positivity) is not a
> B-datum: it is the supply line, and its involution is real-type --
> the dissolution side of the ray dial (Section 2).
> (vii) [non-vacuity] The planted C-04 prime-3 no-go lies OUTSIDE
> C_read structurally: every involution of G fixes its datum
> (dim mod 3), so its Z/2-action is trivial, and
> Hom_{Z/2}(trivial, B) = EMPTY (all 32 candidate label maps
> enumerated and rejected: equivariance would need a fixed point of
> the swap, and there is none); no 2^a label refinement reaches
> divisibility by 3. The law excludes it structurally.

What was a TYPING this morning is, at fixture grade, a THEOREM about
the assembled category: the class-relative no-gos are one law --
excluded classes are the equivariant classes of one Z/2, exactly, and
the bit's value is external to every internal reading class.

## 5. Which outcome fired, and what it does not claim

**L-c fired** (assembles + reconciles + parent applies + plant
outside), against two pre-declared alternatives that did not: no
assembly obstruction appeared (L-a), and the senses did not fail to
reconcile (L-b) -- though B1/B3 show the reconciliation had real
content to earn: the senses are genuinely distinct at operator level
and merge only at the level of the graded implementation group.

NOT claimed: any claim/canon/posture movement; GU-uniqueness (the
parent's Curie/genericity caveat carries); any movement of bar-b, H59,
W172, or the bit's externally-posited VALUE; any operator-grade or
Lean-grade statement -- fixture grade means finite Z/2-sets and the
verified 128-dim rep, and the operator lift (the {F_tau} <-> Sect
map's L^2 version, N2's residual spec) remains the open gate it was;
the +-i0 orientation bit of the section theory is a DIFFERENT Z/2
(typed scheme at matrix grade there) and is deliberately not absorbed
into the assembled class; the TaF involution-typing lemma (the parent
doc's second missing lemma, cross-repo face) is untouched here.

## 6. Council pass (inline, five lenses)

- **Categorical logician (load-bearing; attack the assembly for
  pun-collapse and the products for freeness):** two attacks pressed.
  (1) "Z/2-Set trivially has products; you proved nothing." Answer:
  the content of L1 was never bare existence of products -- it is that
  all six classes are objects OVER ONE Z/2 (Stage 0 + the label maps,
  each equivariance machine-checked and each refutable: A9 hunted for
  a separating element and found none in 111 tries) and that the
  per-object involutions are components of one natural involution
  (C3/C4). The category is Z/2-Set only AFTER those identifications
  are earned; the plant shows the membership bar is real -- C-04 has
  no object structure over this Z/2 at all. (2) "The parent run
  (C6) is the same finite skeleton as W70/W75; what is new?" Answer:
  the hypothesis DISCHARGE is new. W70/W75 ran on an abstract (A, B,
  swap); today the A and B are the assembled objects of an exhibited
  category whose predicates pull back to six machine-verified reading
  classes. That is the difference between a schema and an instance --
  exactly the difference L1 was named to close. Remaining honest gap:
  weak point-surjectivity is exercised on the finite label object,
  not on an infinite function object; the operator lift owns that.
- **Krein analyst:** the identifications consumed are the receipts'
  own (w, V, U_h, J_quat, the commutant basis), and the one
  identification the parent doc flagged for attack at L1 time -- the
  Kramers pairing as parent-alpha -- is resolved rather than assumed:
  J is label-EVEN (B1, a fact the parent table did not state), so the
  correct statement is C4's: J is the antilinear dress of the flip,
  and Kramers-ness is a property of the WHOLE antilinear part of G
  (B4). The (9,5)/(7,7) dial survives in reconciled form (B6). One
  warning kept: sgn_K is defined on K_S-normalizing elements; the
  sweep never met a non-normalizing exhibited symmetry, but the
  character's domain should be named if one appears.
- **Sheaf theorist (question #4 answered):** the answer is the strong
  one: not two isomorphic torsors but one double cover {+-K_S} with
  two section-functors, and the identifying map is an ALGEBRA IDENTITY
  (the seam is a habitat word, odd mixed count) -- the kind of answer
  that survives base change. The honest caveat is recorded in Section
  1: over different bases, "same class" means "pullbacks of one datum
  along exhibited maps"; the fixture cannot see a global base uniting
  the two loop families, and the operator/KR-grade computation (the
  section theory's Section 7.4 residue) is where that would live.
- **TI-native reader (the Lean-shape mirror):** the L1 package built
  here is shape-for-shape the TI threshold's hypothesis side as E117
  carries it -- self-encoding admissibility (predicates = maps to B
  over an exhibited category) + diagonal -- and C6's discharge mirrors
  what G1/G2 did in Lean for the diagonal leg: convert a visible
  hypothesis into a derived fact for a concrete instance. The one-way
  rule holds: nothing here imports GU physics into TI or asserts
  TI-side movement; the mirror is cited as shape, not as shared
  theorem. If a future pass wants theorem-prover grade for THIS
  assembly, the finite Z/2-set skeleton (C5/C6) is Lean-transcribable
  as stated -- that is a rung, not a claim.
- **Adversarial referee (in writing):** (i) Charge: the assembly is
  curve-fitting -- six fixtures were BUILT to share K_S, so finding
  one Z/2 is circular. Answer: the fixtures share K_S by construction,
  but the IDENTITIES are not free: the seam being exactly a habitat
  word (A2, defect 0.0), J being label-even while U_h J is
  Kramers-and-label-odd (B1/B4), and the zero-separator sweep (A9)
  are facts that could each have failed; and the plant DID fail
  membership on the same machinery (F2). (ii) Charge: "excluded =
  equivariant exactly" is a tautology once stated as F_odd. Answer:
  conceded as algebra, denied as content -- the theorem-weight sits in
  the verified evenness of each NATIVE class (C1a-e), which is
  per-sector physics, not bookkeeping; the tautology is what makes
  the clause EXACT once that physics is in. (iii) Charge: L-c was
  preordained; the pre-declared outcomes were theater. Answer: B3 is
  the leg that kept L-b live (the senses are provably distinct at
  operator level); reconciliation needed the extension + grading
  structure to exist EXACTLY (B2's normalization identity has defect
  0.0; a nonzero defect kills the coset map), and (U_h zJ)^2 = -1
  was an open sign until run. (iv) Charge: fixture grade is doing all
  the work in "theorem." Answer: stated in the law's own name and in
  Boundary; the claim is calibrated -- the parent doc said "until L1
  lands, the re-typing is a typing"; L1 has now landed at the same
  grade at which the family's instances were proven, no higher.

## 7. Receipts

- Probe: `tests/channel-swings/l1_assembly_probe.py` -- HEADLINE
  `24 [E] + 5 [F] = 29 (setup [T] = 2 excluded) ALL PASS`, exit 0,
  deterministic (double-run byte-identical), numpy only, seeded
  20260720. Stage 0: A1-A8 + [F] A9 (zero separators in 91 planes +
  20 words). Reconciliation: B1-B6 (extension exact; antilinear part
  wholly Kramers; kernel free + torsor; dissolution dial incl. m1
  real-type). Category: C1a-C1e (objects + gradings + cures), C2
  (the exactly-identity), C3 (label-map naturality), C4 (Kramers
  embedding), C5 (products/terminal/diagonal, exhaustive at label
  level), C6 (parent skeleton, exhaustive |A| <= 3 + dissolution).
  Plant: F1-F4 (datum fixed by every involution; all 32 label maps
  rejected; no 2^a cure; teeth).
- Fixtures consumed, not rebuilt: the Cl(9,5) = M(64,H) rep via
  `tests/generation-sector/gen_sector_bridge.py`; K_S/U_h/J_quat
  conventions identical to sig_b5_habitat_probe.py,
  sector_relative_section_probe.py, torsor_k_sequence_probe.py; the
  per-no-go fixture shapes of diagonal_boundary_probe.py (Kramers C^4,
  Araki toy, 2-channel, C-04 plant) re-run in compact form.
- Parent receipts: the diagonal-boundary doc's certificate C1-C5 and
  per-instance table; W70/W75 skeleton (re-reproduced in C6); the
  flagged residues (three fixpoint-free senses; Kramers-as-alpha to
  be attacked at L1 time) -- both discharged above.

## 8. Boundary

Exploration tier; claim/canon/posture: none; no external actions; no
commits or pushes (hourly cadence owns the working tree); no edits to
any existing file -- new files only (`tests/channel-swings/
l1_assembly_probe.py`, this document). Fixture grade throughout:
finite Z/2-sets, the verified 128-dim rep, matrix identities; the
probe checks the ASSEMBLY and the reconciliation on fixtures -- the
original per-instance theorems have their own receipts, cited not
re-proven (except the W70/W75 skeleton and the parent probe's C3-C5
legs, re-run compactly). Named residues: (i) the operator lift of the
assembly (weak point-surjectivity on a genuine function object; the
N2/L^2 gate) -- untouched; (ii) the +-i0 orientation bit stays a
separate Z/2, scheme-typed, outside the assembled class; (iii) the
TaF involution-typing lemma (parent doc Section 4) -- not begun;
(iv) sgn_K's domain is the K_S-normalizing exhibited symmetries --
named, per the Krein analyst; (v) a Lean transcription of the finite
skeleton is a possible rung, not started. Nothing here moves bar-b,
H59, W172, N-accounting, scorecard rows, or public posture; the bit's
VALUE remains externally posited (p2c-owned).
