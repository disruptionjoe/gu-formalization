---
title: "Index: five frontier design packets (2026-08-11)"
status: active_research
doc_type: design_packet_index
created: 2026-08-11
run_id: RUN-20260811-234500-gu-five-frontier-design-packets
lane: "1 (packets 1-4) and 2 (packet 5)"
reviewed_at_revision: bdd2c934 (agent/operator-anomaly-big-swing; all five packets pinned there — later waves supersede by default)
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
binding: >-
  Discovery-adjacent design input. The five packets bind no wave, make no
  disposition, and grant no execution authority; every disposition is
  wave-owned under the full checking contract.
---

# Index: five frontier design packets

Joe-directed (direct chat, 2026-08-11): five parallel design passes on the
hardest open problems off the hourly campaign's path, each produced
read-only with problem-specific lenses run inline, each carrying a
same-run self-hostile-review (filed separately in
`lab/process/hostile-reviews/`), a verify-status manifest, and bounded
first decisive steps a wave can implement independently.

## The packets

1. **Falsification frontier — the Nguyen pincer on the settled real form
   (AC-G1a).** `explorations/nguyen-pincer-real-form-design-packet-2026-08-11.md`.
   Core: the pincer re-arms sharpest at content level ("chiral generations
   vs 14d consistency"; the CB-C rank-5 lattice makes the chiral branch
   12/12-fatal), secondarily at group level against the source's own
   U(64,64); the source-owned vectorlike defusal carries surplus. First
   step C1: exact integer 128x128 Cl(7,7) gamma construction and the
   invariant bilinear's type/signature, with a source-contradiction
   tripwire. FRAMING CORRECTION (Joe direct chat, 2026-08-11, post-draft;
   see the cross-packet note below): the packet's "concedes the generation
   ambition" line measures the vectorlike branch against the
   physics-default ambition (three fundamental chiral generations) — a
   claim the 2021 draft explicitly disavows. The draft's own §12.10 title
   REPLACES three generations with "2+1 ... two True Generations and one
   Effective Imposter Generation"; its p.52 text asserts "a non-chiral
   total theory splits at the emergent level into two separate chiral
   theories"; p.51 says nature has not "simply repeated herself three
   times" (lab/sources/gu-2021-draft-s11-s12-extraction-2026-08-03.md,
   verbatim). The executing wave should therefore carry a source-claim
   column: the content-level kill applies to the unclaimed physics-default
   reading; the source-native reading (non-chiral total theory, emergent
   chirality, 2+1 with a dark mirror sector) is untouched by that kill and
   is blocked instead on the OPEN construction burdens the repo already
   fences (PH-K1-PHYSICAL; the Witten-1983 exit for any chiral use of the
   384). Kills must name which claim they kill.
2. **Count receptacle — dim-13 Z/3.**
   `explorations/z3-receptacle-design-packet-2026-08-11.md`. Core: after
   exclusions exactly three candidate Z/3 homes survive; the standing
   anchor fact is stale (spin inflow dead in BOTH columns since cb-c F3,
   2026-08-05). First step D1+D2: referee the normal-bundle decomposition
   and compute the orientation bit `w` (register M-H5; hours-scale), which
   alone decides the mod-3 top-degree structure.
3. **Positivity exit criteria (M-H17).**
   `explorations/positivity-exit-criteria-design-packet-2026-08-11.md`.
   Core: the decisive computation — the descended H0 Gram inertia — has
   never been run anywhere; the packet supplies a criteria ladder
   (PC-0..PC-6) quantified over an interface so it cannot collide with the
   live K77 BV construction. First step C1: the criteria checker on the
   existing free bicomplex and triplet bank with quartet planted controls.
4. **B5 five-field native packet.**
   `explorations/b5-five-field-packet-design-2026-08-11.md`. Core: the
   07-29 audit's operative verdict stands, but its per-field narrative is
   stale — post-audit results own or re-type most fields at stated grades,
   and two fields partially dissolve as audited (coflip phases are a
   computed Z/2 monodromy class; the "canonical domain" is a proven
   positive-dimensional moduli whose informative object is verdict
   stability). First steps: RB6 exact-derivative recertification (M-C2's
   unfinished half), then the named-but-never-executed
   pairing-table/formal-adjoint-sign wave.
5. **Lane 2 prediction packet + XS-S typing.**
   `explorations/lane2-prediction-packet-design-2026-08-11.md`. Core: the
   anchor's XS-S question is stale — in-tree results already reduced the
   residue to one dimensionful scale and excluded the symmetry/adjointness
   condition class; the decision is typed as four horns with a one-wave,
   zero-data decisive step (directed source extraction plus one exact
   block computation under a hard indefinite-kernel Layer-0 gate). Counted
   finding: the r(N(z)) refit is not packet-worthy on any horn (surplus
   <= -4); the lane's strongest banked surplus is the hardened FC-d
   tripwire.

## Cross-packet findings (planning evidence)

- **The anchor-facts layer lags the artifact layer.** Three of five passes
  independently found a standing context-pack anchor stale against
  in-tree results (the COUNT inflow columns; the XS-S wording; the B5
  per-field narrative), and a fourth found a prior probe aimed at the
  wrong group candidate (W232). This extends the recorded
  "artifacts honest, dashboards stale-optimistic" meta-pattern to the
  anchor-facts block. PROPOSED (Lane A): a bounded anchor-facts
  re-verification pass over the context pack, artifact-cited line by line.
- **Never-run decisive computations cluster.** Each pass found at least
  one cheap, decisive, never-executed computation already implied by the
  repo's own records (the H0 inertia; the orientation bit; the
  pairing-table wave; the ladder bilinear; the XS-S block computation).
  The packets exist to make exactly these schedulable.

## Absorption guidance

All five packets are pinned at `bdd2c934`; anything a later wave
supersedes, it supersedes by default. The cadence absorbs, schedules a
packet's first step as a wave, or declines with reasons per the absorption
protocol; canon-adjacent consequences stay under the two-phase rule. The
packets' literature-grade facts are flagged for independent citation-check
and must not enter any disposition unchecked.
