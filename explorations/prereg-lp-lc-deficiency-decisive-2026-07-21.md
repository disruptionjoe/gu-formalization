---
title: "Pre-registration: the DECISIVE LP/LC deficiency-index computation — the single-carrier reduction of O-b (the base-uniting/product-uniformity theorem everything gates on)"
status: preregistration
doc_type: scoping_decision_tree
created: 2026-07-21
directed_by: "Joe direct chat, 2026-07-21 (fire the high-leverage limit-point/limit-circle move)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
external_actions: none
kill_conditions_declared_before_computation: true
---

# The decisive LP/LC deficiency-index computation

## Target
Compute the **J-symmetric / Krein (Sims-type) deficiency indices** of the single-carrier
first-order operator `A~ = -i K_u(s) G ∂_s + W~(s)` (`B=-iK_uG`) at the TRUE noncompact
fiber ends of `F = GL(4,R)/O(3,1)`, driven by the wall degeneration `C₀ = q^{1/2} → 0`.
Per Swing 1, this IS O-b's single-carrier reduction (product-uniformity is FREE via
product = Krein direct sum); per Swing 3 the boundary is the characteristic variety `{q=0}`
and the genuine flat-geodesic ends are the locus in question.

## ANTI-TOY DISCIPLINE (binding — a prior "limit-point" verdict was a planted toy, Q1a-HV)
1. Use the ACTUAL flat-geodesic TRUE-END asymptotics of `GL(4,R)/O(3,1)` (the diagonal
   `exp(s·diag(α))·o` ends), NOT bounded-coefficient collar toys. Q1a's FORCED died for
   planting `P=1+0.2sin` etc.
2. Use the GENUINE J-symmetric / Krein (Sims) deficiency count, NOT the Hermitian reduction
   (Q1a-HV: the Hermitian probe is blind to unequal Krein indices).
3. CONFRONT the K_S-null issue honestly: Prong 0 found ~8% of genuine ends cross into
   `{q<0}` where `K_S` is null and the K-definite cut does NOT exist — there the operator
   may not be J-self-adjoint at all (a THIRD possibility, not just LP/LC).
4. PLANTED CONTROL: a bounded-coefficient collar toy MUST give a DIFFERENT answer than the
   true ends (demonstrating the true-end computation is not itself a toy); and a known
   limit-point Dirac end vs a known limit-circle degenerate end must be distinguished.

## Pre-declared outcomes
- **LP-FORCED (summit win — MUST be hostile-verified before ANY banking):** the genuine ends
  are spacelike-gapped / limit-point at both ends ⇒ `A~` essentially self-adjoint ⇒ unique,
  derivable domain ⇒ Mourre delivers ⇒ **O-b proved ⇒ the pencil theorem (two-paper summit)
  closes ⇒ finite z* ⇒ PP1 distinctive.** Must reconcile with Prong 0's ~8% `{q<0}` crossing
  (are those genuine ends, or excised?). Flag LOUDLY; do not bank without hostile verify.
- **LC-SELECTOR (the coherent negative horn — the likely one):** genuine ends cross into
  `{q<0}` / limit-circle / the K_S-null makes the operator non-constructible without an
  external prescription ⇒ the required selector IS `σ = w₁` (the phase `T_θ=e^{iθ}S(b)` =
  a spin-lift/holonomy) ⇒ O-b does NOT close from structure; **the bit gating the lemma is
  the posited σ (the loop closes)** ⇒ z* continuum/washout ⇒ PP1 stays non-distinctive.
- **BLOCKED-D2 (honest):** the true-end asymptotics genuinely cannot be pinned at current
  grade ⇒ the D2 datum is the exact named missing input (matches Q1a-HV's reopen packet).

## Deliverable
Write `explorations/lp-lc-deficiency-decisive-2026-07-21.md` (house frontmatter; outcome one
of the three) with the true-end asymptotic derivation, the Krein deficiency count, the
`{q<0}` confrontation, the planted controls, and the verdict + grade. Probe under
tests/channel-swings/ (foreground, note exit). No claim/canon/verdict moves; a summit-closing
LP-FORCED is a PROPOSAL pending hostile verification, never a banked theorem.
