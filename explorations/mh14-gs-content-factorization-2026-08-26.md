---
title: "M-H14 Green-Schwarz signed-content constraint"
status: active_research
doc_type: construction_result
created: "2026-08-26"
grade: "EXACT DEGREE-16 ANOMALY-POLYNOMIAL CONSTRAINT; NO SOURCE SPECTRUM OR COUNTERTERM"
target_claim: "INTERNAL:M-H14 verdict=CONSTRAINT_DERIVED"
canon_verdict_change: none
ledger_edit: none
scripts:
  - tests/anomaly/mh14_gs_content_factorization.py
---

# M-H14 Green-Schwarz signed-content constraint

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: this result binds the universal fourteen-dimensional degree-16
gravitational anomaly polynomial for a signed physical spectrum. It does not
construct GU's source spectrum, BRST quotient, Green-Schwarz field,
counterterm, boundary law, low-energy matter content, generation count, or
anomaly-free physical theory.

```gu-typed-objects
result: M-H14 quotient-aware irreducible gravitational-anomaly constraint
carrier: physical or BRST-quotient chiral spin-1/2, Rarita-Schwinger and self-dual tensor sectors LAYER=ambient CHIRALITY=S-CHIRALITY-UNTYPED
pairing: index-theoretic anomaly density integrated on a closed sixteen-dimensional test manifold ON=quotient-level-field-content
real_structure: real gravitational Pontryagin classes with chiral complex index signs
grading: chirality sign and ghost-subtracted physical field grade
action_owner: source-action absent; a future source action must supply the quotient-level signed multiplicities
target: primitive p4 coefficient and decomposable residual MAP-TYPE=evaluation
```

## Preflight and route

The source-fidelity lens forbids reading raw `nu`/`zeta` component counts as
physical anomaly multiplicities. The index-theory lens identifies the
primitive (p_4) coefficient as the cheapest decisive condition. The anomaly
factorization lens distinguishes removal of that primitive generator from
construction of a particular counterterm. Exact algebra dominates a numerical
or bounded census, so the certificate reuses the repository's fourteen-
dimensional `Fraction` genus engine and independently checks the self-dual
coefficient.

## Exact result

Let (n_{1/2},n_{3/2},n_{m SD}\in\mathbb Z) be **signed physical/quotient
multiplicities**: changing chirality or duality changes the sign. They are not
raw off-shell component counts. With common denominator (D=2,419,200), the
primitive degree-16 coefficients are

\[
[I_{1/2}]_{p_4}=-\frac{1}{D},\qquad
[I_{3/2}^{(14)}]_{p_4}=-\frac{493}{D},\qquad
[I_{\rm SD}]_{p_4}=-\frac{8128}{D}.
\]

The middle number is dimension-sensitive. In fourteen dimensions

\[
I_{3/2}^{(14)}=\widehat A\bigl(\operatorname{ch}(T_{\mathbb C})-1\bigr),
\]

and the exact tangent-twisted engine gives (D_0-D_1=493/D), the corrected
C0 anchor. The older `487` coefficient comes from evaluating the vector
character with only four root pairs, i.e. the eight-dimensional constant term;
it is not the fourteen-dimensional gravitino density.

Therefore cancellation of the irreducible `tr R^8` / (p_4) channel requires
exactly

\[
\boxed{n_{1/2}+493n_{3/2}+8128n_{\rm SD}=0.}
\]

The integer solution lattice is

\[
(n_{1/2},n_{3/2},n_{\rm SD})
=a(-493,1,0)+b(-8128,0,1),\qquad a,b\in\mathbb Z.
\]

Because all three weights are positive, a spectrum whose three species all
have the same chirality/duality sign has no nonzero solution. For example, one
positive-chirality gravitino requires 493 opposite-chirality spin-1/2 units if
no self-dual tensor participates. This is a source-action **constraint**, not a
prediction that GU contains those fields.

When the boxed equation holds, the remaining degree-16 gravitational
polynomial is in the decomposable span

\[
\langle p_1^4,\ p_1^2p_2,\ p_2^2,\ p_1p_3\rangle.
\]

Both primitive kernel generators have nonzero decomposable residuals in the
certificate. Thus `p4=0` removes the irreducible obstruction; it does **not**
prove full anomaly cancellation and does not construct a source-owned
Green-Schwarz term. A physical factorization still has to name the available
form field, its gauge transformation, normalization, quotient-level spectrum,
and the exact allowed product channel. The already balanced draft-literal
full-tower branch remains governed by its stronger exact cancellation result;
the imported C0 truncation retains its corrected nonzero 493-density.

## Postflight hostile review

The strongest overclaim is “the Diophantine equation proves GU anomaly-free.”
It does not: GU has not supplied the signed physical quotient to which the
integers refer, and primitive cancellation leaves a nonzero decomposable
polynomial on generic lattice generators. The strongest contrary construction
is a mixed-chirality spectrum on the displayed integer lattice; it clears only
the primitive condition until an allowed counterterm or complete cancellation
is proved. The weakest reproducibility seam was the dimension-dependent `487`
versus `493` coefficient, so the probe plants that mutation explicitly and
rejects raw off-shell counts, the old rank-only `13`, full-cancellation
inflation, and a nonzero same-chirality solution.

M-H14 is executed at this exact constraint ceiling. No source claim,
scientific ledger row, canon verdict, prediction, confirmation, paper,
priority or public posture moves.
