---
artifact_type: exploration
status: complete
doc_type: control-integration-result
created: 2026-08-24
title: "GU control-semantics integrity wave: order-sensitive discharge, warn-only claim affinity, and v_PSB kind adjudication"
grade: "EXACT repository-control integration. CT-1's new statement quotes the existing LA-5 AC-A1/AC-F3 witness and asserts no unproved functor or G3/G6 identification. Claim affinity is a warn-only output with known, unknown and untyped planted controls; it cannot change gate exit status. v_PSB is adjudicated from the register's own kind definitions and remains homonym. Native probes and a composite baseline-first hostile harness pass. NOT: physics evidence, source adjudication, a ledger mint, canon promotion, prediction, confirmation or public-posture movement."
target_claim: NONE-NOT-A-KILL
bears_on: SC-CHI-01
canon_verdict_change: none
steering_effect: unchanged
canonical_effect: none
scripts:
  - tests/channel-swings/control_semantics_integrity_wave.py
depends_on:
  - lab/active-research/joe-directed/lens-digs/ldb-bit2-direction-and-krein-parity-2026-08-17.md
  - lab/active-research/joe-directed/bit1-price/b1p1-three-corner-support-prices-the-chiral-grant-2026-08-17.md
  - lab/active-research/joe-directed/composition/cp1-three-live-pairs-adjudicated-2026-08-17.md
---

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
>
> Classification: `INTERNAL_STRUCTURAL_ONLY`.

# Control-semantics integrity wave

## Scope and result

This result binds only the repository's control surfaces. It changes no
scientific object, action, source claim, ledger row, prediction, confirmation,
canon statement, paper or public posture.

Three due packets are closed:

1. **Grant discharge is order-sensitive.** CT-1 now propagates LA-5's concrete
   witness: discharging `AC-A1` is what kills `AC-F3`, so the grant-to-row map
   is not monotone. No Layer arrow is presumed to commute with discharge
   absent a dated migration proving that square. G3 and G6 remain incomparable;
   no functor `L -> G` is asserted and no row moves.
2. **Claim affinity is visible without becoming a verdict.** Dated artifacts
   may declare `bears_on: SC-AREA-NN`. The existing kill-target gate now emits
   typed per-claim affinity plus WARN-only defects for missing or unknown IDs.
   This hook runs before kill-language selection and leaves the gate's exit
   status determined solely by its existing red baseline. Historical source-
   mechanism artifacts were not retrofitted with guessed targets; this result
   declares `bears_on: SC-CHI-01` as the integration pointer for the known
   SRC-1..4 mechanism arc.
3. **`v_PSB` remains `homonym`.** The register defines `homonym` as one token
   with multiple unrelated live senses and `near_collision` as two spellings
   one token apart. The `(10bar,1,3)` and `(4,1,2)` objects share exactly one
   token while denoting different representations, so the existing `homonym`
   kind is correct. The receipt and FX-3 certificate now pin that adjudication.

## Preflight bookend

The rebuilt frontier checked source-to-proof, ledger, prediction,
construction-space, B5, proof and integrity arcs. B2 remained root-empty;
DS1 remained source-reality-gated; P-OBS, C1 and B5 retained their named
release, source or bridge conditions; and no evidence licensed a ledger mint.
The selected three controls were independent, due, nonduplicative, and shared
only the final integration artifact/register closeout.

Route-changing lenses included Layer-0 semantics, order/category theory,
source attribution, control observability, homonym taxonomy, propagation,
vacuity and hostile overclaim. The selected route wrote each fact into its
existing native owner. Retrofitting old authors' `target_claim` fields was
rejected because that would invent attribution; canon and ledger proposal
application was rejected because their independent authority was absent.

## Verification and hostile review

The CT-1 probe pins the dated order-sensitive marker, the LA-5 witness and the
explicit no-G3/G6-identification ceiling. The kill-target selftest plants a
known affinity, an unknown ID and an untyped affinity, proves two WARNs and one
typed link, and proves all three leave exit status green. FX-3 pins the exact
`v_PSB` kind rationale. The composite probe reruns these structural invariants,
pins protected science hashes and catches targeted hostile mutations only
after a clean baseline.

Standing hostile charges:

- **Overclaim:** the order witness does not prove every discharge order differs;
  it forbids assuming commutation without a recorded migration.
- **Contrary construction:** a future migration may prove a particular square
  commutes; the reference explicitly admits that route.
- **Reproducibility seam:** affinity is advisory and useful only when producers
  declare `bears_on`; its typed/warn counts are printed so zero coverage cannot
  masquerade as completeness.

No hostile review finding requires narrowing beyond these ceilings.

## Postflight bookend

Intended and actual effect match: three integrity packets close, no science
moves, and no compatible selected packet remains. The strongest scientific
arcs retain their exact dependency predicates. The next run must reconstruct
the frontier from fresh truth rather than repeating this control wave.

```gu-typed-objects
result: the integrated order-sensitivity, claim-affinity and homonym-kind control semantics
carrier: repository control records and probes LAYER=UNTYPED CHIRALITY=N/A
pairing: NONE
real_structure: N/A
grading: N/A
action_owner: repository-construction
target: the three due control packets MAP-TYPE=not-a-map
```
