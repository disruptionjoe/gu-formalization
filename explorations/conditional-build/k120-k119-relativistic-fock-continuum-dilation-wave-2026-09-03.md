---
title: "K120 K119 relativistic Fock continuum dilation boundary"
status: active_research
doc_type: conditional_relativistic_fock_continuum_dilation_result
created: 2026-09-03
date: 2026-09-03
claim_ceiling: exact repository-owned massless right-moving Klein-Gordon characteristic lift, edge-channel Fock factorization and shift, Hudson-Parthasarathy dilation of the K115 generator, K119 time-bin-to-white-noise reduced limit, causal output counting and mathematical state-effect owner only; the complex structure, Fock vacuum, channel multiplicity and coupling are supplied repository constructions rather than K118-selected, source-owned or GU-native data, and no finite-energy KMS or Hadamard state, full AQFT net, nontrivial gauge BV theory, Born derivation, prediction or confirmation follows
manifest: lab/process/k120-k119-relativistic-fock-continuum-dilation-wave.json
probe: tests/channel-swings/k120_k119_relativistic_fock_continuum_dilation_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K120 K119 relativistic Fock continuum dilation boundary

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: this packet joins K119's edge-labelled repeated interactions to the
massless right-moving characteristic sector of K118's Klein--Gordon host. It
constructs the continuum Fock shift, incoming vacuum, adapted local factors,
Hudson--Parthasarathy interaction and diagonal state/effect algebra on one
rigged finite-particle Sobolev domain. These are repository constructions. The
packet does not show that K118's original real scalar selects this
quantization, that the vacuum is a physically authenticated reservoir, or
that the declared diagonal effects are a derived probability postulate.

```gu-typed-objects
result: a massless right-moving Klein-Gordon characteristic/Fock continuum whose adapted unitary vacuum dilation restricts exactly to the K115 generator and is the controlled white-noise target of K119 time-bin collisions
carrier: C9 K115 base-record system tensored with symmetric Fock space over L2 of the incoming null coordinate with one channel for each directed K115 edge, on a finite-particle H1-to-L2-to-Hminus1 rigged domain LAYER=observed CHIRALITY=N/A
pairing: finite system Hilbert pairing, one-particle L2 null-coordinate pairing, Fock vacuum expectation, diagonal system effects and input-output counting pairing ON=repository_relativistic_Fock_dilation
real_structure: K118 real massless right-moving solutions are complexified before Fock construction; computational-basis conjugation acts on the finite system and edge multiplicity
grading: degree-zero system and Bose field sectors with particle-number grading; minimal nongauge antifields only for finite smooth regularizations and no ghost or BRST grading
action_owner: repository-construction -- smooth finite-band chiral boundary actions scale to the declared quantum stochastic cocycle; not Weinstein's source action or a GU action
target: relativistic continuum interaction, incoming-state/shift owner, K115 reduced dynamics and causal output record MAP-TYPE=evaluation
```

## Inline preflight bookend

The rebuilt frontier contained five serious routes: massive Klein--Gordon
wavepackets, the massless left/right characteristic split, a Davies weak-
coupling limit, a chiral input-output field, and a generic Fock dilation. A
massive packet disperses and does not give exact fresh interval factors. A
generic dilation hides the spacetime carrier. The massless right-moving
sector is selected because it is already a characteristic sector of K118's
hyperbolic equation and transports disjoint incoming intervals without
dispersion; its Fock functor turns orthogonal interval decomposition into an
exact tensor-product factorization.

The route-changing lenses were hyperbolic PDE, characteristic propagation,
Sobolev traces, microlocal spectrum, bosonic Fock factorization, repeated
interactions, quantum stochastic calculus, Lindblad dilation, causal
nondemolition measurement, open-system thermodynamics, BV-BFV regularization,
source custody, probability semantics and hostile philosophy of science. The
cheapest kill is failure of the Hudson--Parthasarathy coefficient identity or
failure of the diagonal reduced generator to equal K115's `Q`. The strongest
contrary route is a massive finite-temperature Hadamard/KMS reservoir with a
proved weak-coupling limit; it would improve physical-state ownership but is
not supplied by current target truth.

## 1. K118's massless characteristic sector

Set the K118 mass to zero and work first on a cylinder of circumference `R`.
Every right-moving solution has

```text
psi(t,x)=f(x-t),                 (partial_t^2-partial_x^2)psi=0.       (1)
```

Its nonzero wavefront covectors are proportional to `(-1,1)`, hence lie on
the Klein--Gordon characteristic set. If a finite observation horizon uses
incoming intervals of total length less than `R`, none wraps around the
cylinder. Sending `R` to infinity after fixing the horizon gives the incoming
null line and removes recurrence in exactly the same local-prefix sense used
by K119's tape limit.

For `E`, the set of directed K115 edges, take the complex one-particle space

```text
h = L2(R_+; C^E),        D = H1_c(R_+; C^E) subset h subset H^-1.      (2)
```

The right shift `S_t f(s)=f(s-t)` (zero before the incoming boundary) is the
characteristic translation. Equation (2) is the common rigged domain: smooth
finite-band actions live on the finite-particle Fock core over `D`; sharp
white-noise increments live in the `L2` middle space and their distributional
boundary values in `H^-1`. K120 does not claim that sharp indicator bins are
themselves `H1` functions.

## 2. Fock factorization owns freshness and the shift

Let `Gamma(h)` be symmetric Fock space and `Omega` its vacuum. Orthogonal
decomposition by null-time intervals gives the canonical factorization

```text
Gamma(h_[0,t+s]) = Gamma(h_[0,t]) tensor Gamma(h_[t,t+s]).             (3)
```

The vacuum factorizes under (3), and second quantization `Gamma(S_t)` shifts
future input toward the detector. Thus disjoint increments are fresh because
they are different tensor factors, not because a used finite cell is reset.
The infinite incoming line and its vacuum boundary condition are the resource.
They are now mathematical owners rather than unnamed tape operations, but
they remain supplied rather than source-selected or thermodynamically free.

For a mesh `h`, each interval `I_n=[nh,(n+1)h)` contains the normalized edge
mode `h^(-1/2) 1_I_n tensor e_(j<-i)`. Its vacuum-plus-one-particle subspace is
exactly K119's edge-labelled cell. The K119 star rotation therefore embeds in
the local algebra of each Fock interval. Reusing a past factor is excluded by
adaptedness, not erased by fiat.

## 3. The continuum detector interaction

On the nine-state system define one operator per directed edge,

```text
L_(j<-i)=sqrt(q_ij) |j><i|,
K=-1/2 sum_e L_e^* L_e=-1/2 sum_i q_i |i><i|.             (4)
```

The adapted Hudson--Parthasarathy equation is

```text
dU_t = {sum_e L_e dA_e^*(t)-L_e^* dA_e(t)+K dt} U_t,
U_0=I.                                                     (5)
```

Because

```text
K+K^*+sum_e L_e^*L_e=0,                                   (6)
```

(5) has a unitary adapted cocycle on the exponential domain. Its vacuum
reduced generator is

```text
L(rho)=sum_e L_e rho L_e^*
       -1/2 {sum_e L_e^*L_e,rho}.                          (7)
```

If `rho=diag(p)`, then `L(rho)=diag(pQ)`. Thus (7) is a
continuum unitary dilation whose declared diagonal state algebra is exactly
K115's Markov semigroup, not merely a fitted rate approximation. K115's
stationary `nu` remains stationary on that algebra.

The coupling is local at the incoming null boundary in the input-output
sense: `U_t` acts only on the system and `Gamma(h_[0,t])`. It commutes with
future input observables. Outputs in disjoint intervals commute, and future
input remains nondemolished by past dynamics.

## 4. K119 time bins converge to the white-noise cocycle

On each interval retain the vacuum and one-particle edge modes and apply
K119's exact star rotation. Its angle satisfies

```text
sin(theta_i(h))=sqrt(h q_i),
theta_i(h)/h = sqrt(q_i/h)+O(sqrt(h)).                     (8)
```

Equation (8) is the usual weak-collision scaling. The local vacuum matrix
element is `1-hq_i/2+O(h^2)`, the one-particle creation amplitude is
`sqrt(h q_ij)`, and all different interval modes are orthogonal. These are
exactly the coefficients in (5).

For the reduced diagonal system no appeal to an unproved joint-limit norm is
needed: every K119 time-bin step is exactly `I+hQ`, and K119 already proves

```text
d_TV(delta_i (I+Q/m)^(mt), delta_i exp(tQ))
 <= t Lambda^2/m.                                          (9)
```

The vacuum reduction of (5) is exactly `exp(tQ)` on the diagonal algebra, so
(9) is a quantitative collision-to-white-noise convergence theorem for the
claimed reduced observable sector. K120 does not promote it to operator-norm
convergence of the complete interacting fields.

## 5. Output counts and state/effect ownership

Let `N_e^out(t)=U_t^* N_e(t) U_t` be the output count for edge `e=(j<-i)`.
For a diagonal system state with population `p_i`, vacuum input gives

```text
d E[N_(j<-i)^out(t)]/dt = p_i(t) q_ij.                    (10)
```

At stationarity the edge intensities are `nu_i q_ij`; summing them gives
K119's activity, and the forward/reverse log-intensity pairing gives the same
positive entropy-production rate. Counts before time `t` commute with future
input effects because of adapted factorization.

The packet now owns a precise mathematical state/effect tuple: the Fock
vacuum functional, diagonal density matrices, diagonal record projections,
and commuting output-count spectral effects. Positivity and normalization
follow from the Hilbert/Fock construction. This is not a Born derivation: the
expectation functional and effect interpretation are declared as part of the
repository model. Nor is the vacuum physically authenticated as KMS,
Hadamard, finite-energy, source-selected or unique.

## 6. Regularized action and BV-BFV boundary

For smooth compactly supported interval approximants in `D`, project the
chiral field to finitely many modes and use the first-order action

```text
S_[h,R] = integral [ i/2(<Psi,dot Psi>-<dot Psi,Psi>)
                     - <Psi,H_free+H_int^[h,R] Psi> ] dt.  (11)
```

Here `H_free` is characteristic translation and `H_int^[h,R]` is the direct
sum of the local K119 star generators on the admitted smooth time-bin modes.
Variation owns the regularized joint unitary and endpoint symplectic form.
The minimal shifted-cotangent extension is nongauge, so its BV master equation
is trivial and the ordinary endpoint BV-BFV identity holds. Taking the mesh
and decompactification limits gives (5) at the coefficient/reduced-semigroup
level established above.

Equation (11) is not asserted to converge as an ordinary finite-energy local
action: the coupling scales as `h^(-1/2)` and the white-noise vacuum is a
singular boundary idealization. No nontrivial ghost complex or interacting GU
BV-BFV action has been constructed.

## 7. Composition boundary

K120 closes K119's named mathematical common-host seam. The tape cells are
local Fock interval factors of a massless right-moving Klein--Gordon
characteristic field; the shift is characteristic translation; the vacuum is
the factorized incoming boundary state; and the continuum cocycle reduces
exactly to K115.

It does not close the physical/source seam. K118 did not select the massless
limit, complex polarization, Bose Fock functor, edge multiplicity, vacuum or
coupling coefficients. Their successful composition establishes a
repository-owned conditional relativistic dilation, not Weinstein's action,
a GU-native quantum theory, a finite equilibrium bath, a Hadamard state, a
complete AQFT net or a derived probability law.

## Inline postflight bookend

- **Strongest overclaim:** “The Klein--Gordon field derives the detector and
  Born rule.” Refused. The edge channels, coupling, complex structure, vacuum
  and diagonal effect semantics are declared construction data.
- **Strongest contrary construction:** a finite-temperature massive Hadamard
  field could be more physical. It does not inherit exact null-interval
  factorization or the K115 generator without a new weak-coupling theorem.
- **Strongest mistyping risk:** treating the singular white-noise field as an
  `H1` vector. Refused. Smooth regularizations use the finite-particle `H1`
  core; sharp increments live in `L2` and boundary distributions in `H^-1`.
- **Weakest reproducibility seam:** the full joint repeated-interaction-to-HP
  convergence is not certified in operator norm. The claim is restricted to
  coefficient convergence and the exact reduced diagonal bound (9).

The certificate runs a clean exact baseline before every hostile mutation.
No source/GU action, finite-energy KMS/Hadamard state, full AQFT net,
nontrivial gauge BV theory, Born derivation, held-out score, prediction,
confirmation, canon, paper, release or public-posture surface moves.

## Reproduction

```bash
python3 tests/channel-swings/k120_k119_relativistic_fock_continuum_dilation_probe.py
python3 tests/channel-swings/k120_k119_relativistic_fock_continuum_dilation_probe.py --selftest
```
