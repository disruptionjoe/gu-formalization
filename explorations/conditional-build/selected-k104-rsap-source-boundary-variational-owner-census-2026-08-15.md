---
title: "Selected-K104 RSAP source boundary-variational owner census"
status: active_research
doc_type: exact_equivariant_boundary_selector_obstruction_and_owner_factorization
created: "2026-08-15"
registry: lab/process/selected-k104-rsap-source-boundary-variational-owner-census.json
probe: tests/channel-swings/selected_k104_rsap_source_boundary_variational_owner_census_probe.py
grade: "RELEASED SOURCE NAMES THE ULTRAHYPERBOLIC BOUNDARY PROBLEM BUT SUPPLIES NO PHYSICAL BOUNDARY LAW; FULL-G EQUIVARIANCE CANNOT SELECT THE FIXED 42D BALANCED ZERO LEVEL; MOVING-R COVARIANCE REQUIRES THE UNOWNED BALANCED SEED"
target_claim: K103_NEXT_GATE__RELEASED_SOURCE_OR_ACTION_OWNS_A_BOUNDARY_VARIATIONAL_PRINCIPLE_SELECTING_THE_98D_BALANCED_ZERO_CHARGE_HORN
target_verdict: NO_AT_RELEASED_SOURCE_AND_FULL_G_EQUIVARIANT_LOCAL_GRADE__THREE_OWNER_FACTORIZATION_EXACT
canon_verdict_change: none
---

# Selected-K104 RSAP source boundary-variational owner census

> **GU-COMPARATOR-ROUTING — scope before inference.** This is a source-native
> boundary-variation and symmetry-reduction question. Ordinary Higgs/VEV,
> family-index, net-chirality, anomaly, symmetry-breaking and familiar
> four-dimensional gauge-model conclusions do not substitute for Weinstein's
> objects. Read `lab/methods/source-native-comparator-routing.md` before
> importing any such comparator.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: SOURCE_NATIVE_ROUTE`

Scope: the checked 2021 draft action grammar, the 2025 source admission about
the upstairs ultrahyperbolic boundary problem, K100's balanced-owner theorem,
and K103's exact finite preboundary route census. This is not a universal
statement about unreleased actions or boundary principles that explicitly
break the full source gauge group.

## Result first

The released source does not choose the `98D` reverse-RSAP boundary theory.
It does something more specific than merely omitting a favored formula:

1. the draft displays the bulk `I1B`, `I2B`, and total fermionic residual but
   no balanced boundary functional, zero-flux law, right-`H_bal` multiplier,
   edge action, gauge declaration, or physical polarization;
2. the later source explicitly calls the multiple-time upstairs problem a
   boundary-condition problem and says how to handle it is not known; and
3. any boundary principle constructed from the current fields while retaining
   full `G=Spin_0(7,7)` equivariance cannot select the fixed condition
   `lambda_h=0` unless a balanced order parameter moves with the fields.

The third point is exact. Identify `g*` with `g=so(7,7)` using the invariant
pairing and write the balanced symmetric pair as

```text
g = h_bal + p_bal,
dim h_bal = 42,  dim p_bal = 49.
```

The desired zero level is `lambda_h=0`, equivalently `lambda in p_bal`.
But `p_bal` is not invariant under the full adjoint action because

```text
[p_bal,p_bal] contains nonzero h_bal.
```

The exact probe gives an explicit pair of mixed bivectors whose bracket is a
nonzero balanced-stabilizer bivector. Therefore the zero set of the fixed
42-component condition is not full-`G` invariant. Since the Euler zero set of
a full-`G`-invariant boundary functional must be invariant, such a functional
cannot produce precisely this fixed balanced constraint.

This leaves an exact three-owner factorization:

```text
balanced orbit owner R_0
+ boundary equation selecting lambda_h=0
+ declaration that right H_bal is gauge
= 98D reverse-RSAP boundary theory.
```

The current released source/action owns none of those three selections. It
owns the `182D` formal epsilon cotangent parent and the diagonal full-`G`
Gauss law adjacent to them.

## Layer 0: four different statements

| statement | exact object | not the same as |
|---|---|---|
| source says “boundary conditions” | admission that multiple-time evolution needs boundary data | a boundary condition being supplied |
| bulk gauge invariance | diagonal full-`G` action and K102 Gauss law | fixed right-`H_bal` zero level |
| balanced boundary equation | `P_h(R)lambda=0` | ownership of `R` or declaration of right gauge |
| scalar boundary functional | shifts the preboundary potential by an exact field-space one-form and may select a graph | construction of the full 98D coisotropic quotient |

The collar normal remains one conditional codimension-one normal in `Y14`,
not the ten-dimensional normal bundle of the observation section.

## Source return

The checked source/action inventory is finite at this grade:

- `SC-ACT-01` displays the first bosonic `I1B` action;
- `SC-ACT-04` displays `I2B=||Upsilon_B||^2`;
- `SC-ACT-05` packages bosonic and fermionic contributions into one total
  Euler residual; and
- `SC-META-52` says that upstairs there are boundary rather than ordinary
  initial conditions and records the author's uncertainty about handling the
  ultrahyperbolic problem.

None supplies a boundary density or boundary-domain law selecting zero
balanced flux, `lambda_h=0`, an `H_bal` edge/multiplier sector, or the status
of unrestricted right-`H_bal` transformations as gauge. This agrees with the
source packet's existing `SOURCE-SILENT` classification for physical boundary
selection and common domain.

```text
SOURCE-CONFIRMS:
  I1B, I2B, the total Euler-residual grammar, and that the upstairs problem
  requires boundary rather than ordinary initial data.

REPOSITORY-DERIVES:
  the action preboundary cotangent parent, K102 diagonal Gauss law, K103
  route census, and the fixed-balanced equivariance obstruction here.

SOURCE-SILENT:
  balanced seed, balanced boundary equation, right-H_bal gauge declaration,
  edge/multiplier action, physical polarization and closed analytic domain.
```

This is source silence plus a structural obstruction on one precisely named
class. It is not a claim that Weinstein selected the wrong boundary condition.

## Why full-G covariance forbids the fixed balanced selector

Let `R_0` be the conditional balanced involution. Its adjoint involution

```text
sigma_R(Y)=R Y R
```

has `+1` eigenspace `h_bal` and `-1` eigenspace `p_bal`. With

```text
P_h^R(Y)=(Y+R Y R)/2,
```

the target condition is `P_h^R(lambda)=0`.

Holding `R_0` fixed while applying all of `G` fails covariance. The probe's
explicit `X,lambda in p_bal` obey

```text
P_h^R(lambda)=0,
P_h^R([X,lambda])=[X,lambda] != 0.
```

So a full-`G` motion takes an allowed charge out of the fixed zero set. The
condition is invariant under `H_bal`, as expected, but that is already the
symmetry reduction the reverse scaffold is trying to own.

The full zero condition `lambda=0` is `G` invariant, but it imposes 91 rather
than 42 moment constraints. If the free right full-`G` action is also declared
gauge, the formal cotangent count is `182-2(91)=0`, not `98`. The source
physical `4|10` stabilizer is another distinct option: its dimension is `51`
and its cotangent reduction is `80D`.

## The moving-R repair and what it costs

Covariance is restored if `R` moves:

```text
delta_X R=[X,R],
delta_X lambda=[X,lambda],
delta_X(P_h^R lambda)=[X,P_h^R lambda].
```

The probe checks this identity exactly and fires the fixed-`R` control. K100
already proves that the existing epsilon field can carry
`R_epsilon=epsilon R_0 epsilon^-1` with a 49-dimensional orbit and 42-
dimensional stabilizer. It also proves that epsilon transports rather than
derives `R_0`; homogeneous source-metric data and a global charge-only map
cannot choose the balanced real orbit.

Thus moving `R` does not evade the ownership problem. It exposes it cleanly:
either a source/action term must derive the balanced orbit, or `R_0` remains
Variancer's explicit reverse conditional.

## Why a scalar boundary term is still insufficient

Even after `R_0` is supplied, an ordinary scalar boundary functional does not
by itself build the 98D reduced phase space. Its exact field-space shift has
zero presymplectic curl, and a right-invariant generating function supplies
at most a 49D reduced Lagrangian graph inside the 98D quotient. The complete
boundary theory still needs the zero-level equation and the right-`H_bal`
gauge disposition. K103's edge extension solves gauge compatibility while
retaining charge, so it remains 182D rather than selecting the zero level.

## Hostile boundary

- A boundary may legitimately break full `G` to `H_bal`; the theorem does
  not forbid that. It says the breaking datum is then part of the boundary
  principle and cannot be credited to the unbroken source action.
- A nonhomogeneous curvature concomitant may derive a moving balanced `R`.
  K100 deliberately leaves that route open. It must select a gapped `7|7`
  real-signature split without a fitted spectral cut and satisfy the actual
  action stationarity equations.
- The source admission that boundary conditions are needed is not evidence
  for zero flux, Dirichlet, Neumann, edge, BFV, or any other particular horn.
- No global domain, positivity, quantization, spectrum, particle content,
  chirality or generation count follows from this finite local theorem.

## Disposition and next gate

The released-source boundary-owner census is closed at its stated grade. Do
not search the same bulk action formulas for an implicit fixed balanced
projector, and do not treat the word “boundary” as a selection.

The strongest live source-native successor is the one class not killed by
the homogeneous/equivariant census: test whether a stationary,
nonhomogeneous, action-owned curvature or connection concomitant constructs a
smooth gapped `7|7` involution of balanced real type. It must remain defined
at the zero-charge stratum, avoid a fitted spectral threshold, and make the
boundary Euler/Noether law select zero balanced flux and right gauge. Failing
that, the 98D theory remains conditional on `R_0` plus an explicit new
boundary multiplier/domain law.

No ledger, datum, quotient, canon claim or public posture changes. Reproduce:

```bash
python3 tests/channel-swings/selected_k104_rsap_source_boundary_variational_owner_census_probe.py
```

> **Successor closure (K105).** The curvature horn is mathematically nonempty:
> spectral sign sends any natural, real-semisimple, gapped curvature
> endomorphism with the balanced eigenspace signatures to the required moving
> involution. An exact algebraic Riemann fixture realizes it. Current action
> ownership nevertheless fails upstream: SR-1H's five serialized point-
> carrier classes have zero eligible stationary survivors. Reopen only with a
> genuinely new stationary background or action term; do not repeat spectral
> searches over those same five carriers.
