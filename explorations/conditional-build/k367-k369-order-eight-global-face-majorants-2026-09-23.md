---
title: "K367--K369 order-eight global face majorants"
status: exploration
claim_verdict: internal_structural_only
date: 2026-09-23
claim_ceiling: exact zero-inclusive confluent determinant envelopes, global scaled Bessel derivative envelopes through order eight, and finite whole-radial bounds for every reachable face program; the disjoint recursive owner cover, complete hybrids and order-eight remainder remain open
manifests:
  - lab/process/k367-order-eight-zero-inclusive-determinant-envelope.json
  - lab/process/k368-order-eight-global-scaled-bessel-envelope.json
  - lab/process/k369-order-eight-whole-radial-face-majorants.json
producers:
  - tests/channel-swings/k367_order_eight_zero_inclusive_determinant_envelope.py
  - tests/channel-swings/k368_order_eight_global_scaled_bessel_envelope.py
  - tests/channel-swings/k369_order_eight_whole_radial_face_majorants.py
probes:
  - tests/channel-swings/k367_order_eight_zero_inclusive_determinant_envelope_probe.py
  - tests/channel-swings/k368_order_eight_global_scaled_bessel_envelope_probe.py
  - tests/channel-swings/k369_order_eight_whole_radial_face_majorants_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: GLOBAL_FACE_PROGRAM_MAJORANTS_CLOSED__DISJOINT_RECURSIVE_HYBRID_SUM_OPEN
canon_verdict_change: none
---

# K367--K369 order-eight global face majorants

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: these results bind only the repository-owned conditional order-eight
positive-Fock coefficient family and its K348 cubature remainder. They do not
construct a GU physical state, interacting quotient, source-owned action,
observable or K152 decision.

```gu-typed-objects
result: exact zero-inclusive confluent two-scale determinant envelopes, global scaled 2*K1 derivative bounds through order eight, and finite whole-radial majorants for all 517 reachable face programs
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), restricted to the K162 zero-bath seed orbits LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Gram pairing after complete normalized specieswise exterior projection PAIRING-TYPE=hilbert
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering and real Bessel kernels
grading: conserved incidence charges, impurity seed, bath species, bath number eight, K179 contraction identity and ordered left/right momentum provenance
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W and K179 fixes the coefficient family; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: bound every reachable order-eight face program over its complete radial half-line without scalarizing determinant transitions, then expose the exact remaining disjoint-owner stitching obligation MAP-TYPE=intertwiner
```

## Preflight bookend

K366 retains every determinant permutation's outer and inner singular exponent
but supplies no coefficient enclosure. K352's row/column confluence can demand
three derivatives on each side, and the active Peano derivative adds two more.
The first gate is therefore a determinant-level interval formed only after the
confluent operations. Independently, K365 stops at compact argument width four,
so a complete radial face bound also needs a certified positive-argument tail.

The cheapest contrary route is a common scalar exponent. K364 already rejects
it on twelve template pairs: the repair invents one inverse inner power against
K353's zero margin. K367 therefore keeps the permutation index through every
coefficient calculation, while K368 derives a global primitive bound without
using the determinant at all.

## K367: zero-inclusive confluent determinant envelopes

K367 enumerates every rank-compatible combination of K366's `146` comparable
singular-template pairs and K352's `38` confluent templates. The result is
`2,633` exact rational envelopes. For derivative orders zero, one and two it
enumerates every Leibniz allocation, applies the exact row and column
factorials, and aggregates coefficients only after preserving the determinant
permutation's coupled exponent pair. All twelve K364 obstruction pairs are
represented, and no primitive order above eight occurs.

The compact contract is

```text
0 <= tau <= rho/64,
0 <= rho <= 1/64,
every cumulative Bessel argument <= 1.
```

The original determinant's extracted Vandermonde factors remain symbolic;
they are never divided out after absolute enclosure.

## K368: global scaled Bessel envelopes

For orders `m<=8`, the exact derivative recurrence contains only integer
Bessel orders at most nine. The positive integral representation makes
`K_nu(w)` increasing in `nu>=0`, hence every term is bounded by `K_(19/2)`.
The explicit half-integer formula then gives

```text
abs((2*K1)^(m)(w)) <= 2*K_(19/2)(w),  w>=1.
```

Using `sqrt(pi/2)<2` and an exact rational bound on each
`w^(m+1/2-k) exp(-w)` produces a rational tail constant. Joining it to K365's
width-one row yields global zero-safe scaled bounds through order eight. All
`27` Arb controls at arguments one, four and sixteen are contained.

## K369: whole-radial face-program majorants

K369 recomputes the `2,633` determinant envelopes with K368's global primitive
constants. It then composes two old-kernel factors and every species determinant
for all `2,400` ordered descriptors, keeping all `23` coherent groups through
the final absolute sum.

For a codimension-`c` K353 face with net radial power `p>=0`, the exact angular
mass is `1/(c-1)!` and the Laguerre radial mass is `p!/256^(p+1)`. If the active
Peano axis is normal, the combined Peano/Laguerre weight obeys

```text
K_256(t) exp(-256 sum(other normals))
  <= (3/2) t^2 exp(-256 rho).
```

Otherwise K369 uses K348's exact kernel mass `1/(2*256^3)`. Every one of the
`517` face programs therefore receives a finite exact rational whole-radial
upper.

## Postflight bookend

K367 passes `9/9` controls and rejects `10/10` hostile mutations. K368 passes
`9/9` and rejects `10/10`; K369 passes `10/10` and rejects `11/11`. The
strongest overclaim is to sum K369's 517 rows as though they were disjoint.
They overlap by construction. K363 gives the unique owner rule, but its
recursive numerical stitching to these constants and a positive-interior cell
cover remains open. No complete K348 hybrid, order-eight remainder, K345 join,
source register, physics ledger, canon, paper, public or physical posture moves.

SC-META-53 remains `UNCERTAIN`; RA-F1, LT-SM8, LT-GR6b and AC-F1 remain
`NEEDS`.
