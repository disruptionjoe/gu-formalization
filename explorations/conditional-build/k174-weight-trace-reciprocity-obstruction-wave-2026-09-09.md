---
title: "K174 weight-trace reciprocity obstruction wave"
document_role: active_research
doc_type: conditional_native_K139_K156_diagonal_weight_graph_reciprocity_obstruction_and_orbit_tail_interface_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned diagonal-weight route obstruction for the K139--K173 signed point control showing that the quarter-energy plus number graph makes the boundary map strictly contractive but cannot bound a nonzero K156 exchange block, and that no positive diagonal one-particle multiplication weight can simultaneously contain the dressed boundary profile and make the unsmeared point trace continuous; correlated, non-diagonal, cancellation-adapted and coefficient-specific routes remain open, while the complete action column, R_ref residual, complement or flux floor and scalar-center left floor remain absent, so no native K152 interval, physical/source selection, Born derivation, prediction or confirmation follows
manifest: lab/process/k174-weight-trace-reciprocity-obstruction-wave.json
solver: tests/channel-swings/k174_weight_trace_reciprocity_obstruction.py
probe: tests/channel-swings/k174_weight_trace_reciprocity_obstruction_probe.py
target_claim: INTERNAL_TARGET:K173_SINGLE_DIAGONAL_WEIGHT_GRAPH_TAIL_REPLACEMENT
target_claim_verdict: ROUTE_KILLED_COEFFICIENT_SPECIFIC_OR_CANCELLATION_ADAPTED_TAIL_REQUIRED
canon_verdict_change: none
---

# K174 weight-trace reciprocity obstruction wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: this packet stays on the repository-supplied K139--K173 equal-coupling
two-edge positive particle/hole point control at auxiliary chart `256`. It
tests diagonal multiplication-weight graphs for the one-particle free-energy
variable. It does not rule out correlated, non-diagonal, cancellation-adapted,
rigged, boundary/Weyl or coefficient-specific orbit domains, and it does not
select a physical extension, polarization, state or scalar center.

```gu-typed-objects
result: the quarter-energy plus number graph makes the fixed boundary chart strictly contractive but leaves each isolated nonzero normal-ordered exchange point trace unbounded; a Cauchy--Schwarz reciprocity theorem extends the incompatibility to every positive diagonal one-particle multiplication weight
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), in q=(0,0),(1,0),(0,1), with fixed K139 chart and K162 seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing transported by S=(1-G_256)^-1 to M=S* S; the tested graph is generated by a positive diagonal one-particle weight and N+1 ON=repository_signed_point_control
real_structure: CAR adjoint, momentum conjugation, Hermitian normal-ordered bath blocks and the K168 real flavor-symmetric reference shape
grading: conserved incidence charges, hard-core impurity degree, bath-particle number, free-energy weight, Neumann word order and K162 level
action_owner: repository-construction -- K156 fixes the complete renormalized normal-ordered core, but separate exchange factors are singular and no source/GU action selects a graph, extension, domain, state or scalar center
target: decide the full diagonal-weight graph family, preserve the exact route ceiling and compile the remaining coefficient-specific K171 tail interface MAP-TYPE=intertwiner
```

## Inline preflight bookend

K173 leaves the quarter-energy plus number graph as the smallest candidate
because the dressed point profile has every exponent below one half and the
named diagonal multiplier grows only logarithmically. Retrieval against K139
and K156 changes the work list: the four exchange blocks retain an unsmeared
point-annihilation trace. A complete `W:D->H` estimate therefore has to control
both the dressed creation coefficient and that trace on the same domain.

The route census compared power and logarithmic weights, general positive
diagonal weights, graph-of-core and cancellation-adapted domains, rigged
form-dual estimates, coefficient-specific seed orbits, boundary/Weyl
denominators, Kato residuals, Feshbach complements and Lehmann--Goerisch flux
bounds. The cheapest route-changing discriminator is weighted duality on one
isolated nonzero exchange channel. It decides the entire diagonal family
before a large all-Fock computation.

`SC-META-53` remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain
`NEEDS`. The source register and v0.263 physics ledger do not move.

## 1. The quarter graph does make the boundary map contractive

Set

```text
A_(1/4)=(dGamma(omega))^(1/4)+N+1,
||psi||_(1/4)=||A_(1/4) psi||.                         (1)
```

For one dressed creation channel, fractional pull-through and
`(x+y)^(1/4)<=x^(1/4)+y^(1/4)` give the conservative graph bound

```text
||A_(1/4) a*(h) psi||
 <=(3||h||+||omega^(1/4)h||)||A_(1/4)psi||.           (2)
```

Here `h=(2 pi)^(-1/2)/(omega+256)`. Elementary integral bounds give

```text
||h||<1/27,
||omega^(1/4)h||<9/50.                                (3)
```

For the second estimate use `omega(p)<=p+1` and `omega(p)>=p` on the
positive half-line, substitute `u=sqrt(p+1)`, and bound the resulting integral
by `1/(2 sqrt(255))<81/2500`. The two-edge C3 control has at most two mutually
orthogonal outgoing impurity/species channels. Since `sqrt(2)<3/2`,

```text
||G_256||_(D_(1/4)->D_(1/4))
 <(3/2)(3/27+9/50)=131/300<1.                         (4)
```

Thus K173's fractional candidate is invariant and strictly contractive in the
explicit norm (1). The obstruction lies in `W`, not in `G`.

## 2. One exchange channel forces the unsmeared point trace

Each nonzero K156 exchange block contains one resolvent-dressed creation
coefficient and one point annihilation trace. Restrict to an impurity endpoint
and edge/polarity species for which only that block reaches its output. The
other blocks have orthogonal impurity or CAR output, so cancellation cannot
hide the trace on this subspace. Up to a fixed nonzero output vector the
necessary functional is

```text
L(f)=int_R f(p) dp.                                    (5)
```

On the one-particle sector of (1), continuity of (5) requires
`(omega^(1/4)+2)^(-1)` to be square integrable. Its square decays only as
`|p|^(-1/2)`, so it is not. Consequently a nonzero exchange block is
unbounded from the quarter graph to Hilbert space even though the diagonal
K172 self-energy subblock is bounded:

```text
||W||_(D_(1/4)->H)=infinity.                           (6)
```

This is not a claim that the complete renormalized core fails to exist.
K156 constructs it after matched cancellation. It says that its singular
exchange factors cannot all be bounded separately on K173's proposed graph.

## 3. No positive diagonal multiplication weight can satisfy both sides

Let `a(p)>0` be the effective one-particle graph weight, including the harmless
`N+1` contribution. Boundary invariance on the vacuum requires

```text
H=integral a(p)^2/(omega(p)+256)^2 dp < infinity,      (7)
```

while continuity of the isolated point trace requires

```text
T=integral a(p)^(-2) dp < infinity.                    (8)
```

For `p>=257`, `omega(p)+256<=2p`. Therefore on `[257,R]`,
Cauchy--Schwarz gives

```text
log(R/257)
 <=2 [H_R T_R]^(1/2),
H_R T_R >=(1/4)log(R/257)^2.                          (9)
```

The right side diverges. No positive diagonal multiplication weight has both
properties (7)--(8). The result is not limited to powers:

- for `a~omega^s`, boundary membership requires `s<1/2`, trace continuity
  requires `s>1/2`, and both integrals diverge logarithmically at `s=1/2`;
- finite logarithmic weights preserve the boundary profile but never make the
  trace continuous.

Thus free-energy, particle-number, quarter-energy and logarithmic candidates
are instances of one reciprocity obstruction. A stronger diagonal weight fixes
the trace only by excluding the boundary image.

## 4. The surviving route is coefficient-specific or cancellation-adapted

K171 does not require a global `W:D->H` theorem if the complete renormalized
vectors themselves admit a summable bound. A valid direct certificate now
pins the seed, operator and coefficient-family identities, the first unresolved
order, complete matched cancellation, and an all-order estimate

```text
||W_n G^n phi|| <= beta r^n,       0<=r<1.             (10)
```

Together with the K153 Hilbert contraction `q_H<1`, the post-adjoint K171 tail
from order `N+1` is

```text
beta r^(N+1)/[(1-r)(1-q_H)].                           (11)
```

The compiler accepts an abstract positive control with `beta=1/64`,
`r=q_H=3/8`, `N=1`, returning `9/1600`. It rejects the native packet because
K172 supplies only the orders zero and one; no all-order coefficient estimate
or proof that the singular exchange pieces cancel in every complete vector is
serialized. Bounding the separate factors forbidden by (9) is not a valid
substitute.

## 5. Native release replay

The single diagonal-weight graph branch is now closed, including the quarter
candidate. The coefficient-specific or cancellation-adapted branch remains
open. Until (10) is proved on the exact K162 vacuum and one-impurity orbits,
K171's complete base action column, the `R_ref` form-dual residual, a positive
complete `M`-orthogonal complement or flux floor, and the K152 interval remain
absent. K169's `5/2` threshold member is still only a cap, and scalar-center
selection remains separate.

## Inline postflight bookend

- **Strongest construction:** the quarter graph has the explicit strict
  contraction `q_(1/4)<131/300` for the K139 boundary map.
- **Strongest negative result:** equations (7)--(9) rule out every positive
  diagonal one-particle multiplication weight as a common boundary/exchange
  graph.
- **Strongest scope control:** the theorem binds an isolated nonzero exchange
  channel and does not rule out correlated, non-diagonal or cancellation-
  adapted domains.
- **Strongest continuation:** compute complete renormalized
  coefficient-specific orbit vectors and prove (10), rather than attempting
  to bound their singular factors separately.
- **Strongest overclaim:** “the K156 regular core does not exist.” Refused;
  K156 owns the matched normal-ordered core, while K174 kills only this graph-
  bounded factorization route.
- **Weakest reproducibility seam:** native all-order cancellations on the seed
  orbit remain unexpanded beyond K172's first two blocks.

All five compatible mathematical arcs were attempted. Quarter contraction,
exchange unboundedness, the general reciprocity theorem, power/log controls
and the fail-closed orbit-tail interface closed. The native all-order orbit
tail, residual and complement arcs remain dependent on complete coefficient
estimates. Physical extension and scalar-center selection remain authority-
excluded. No source, ledger, Born, prediction, confirmation, canon, paper,
release or public-posture truth moves.

## Next condition

Leave the impossible single diagonal-weight graph route. Expand the complete
renormalized `W_nG^n phi` vectors on the exact K162 vacuum and one-impurity
seed orbits, preserving matched cancellation before taking limits, and prove
an all-order summable majorant of the form (10) or a sharper sequence bound.
Only then assemble the K171 action column and `R_ref` residual before testing
the complete complement or flux floor. Keep scalar-center selection separate.

## Reproduction

```bash
python3 tests/channel-swings/k174_weight_trace_reciprocity_obstruction.py --demo
python3 tests/channel-swings/k174_weight_trace_reciprocity_obstruction_probe.py
python3 tests/channel-swings/k174_weight_trace_reciprocity_obstruction_probe.py --selftest
```
