---
artifact_type: conditional_physics_ledger_view
created: 2026-08-05
version: "0.9"
machine_source: lab/process/conditional-physics-ledger-v0.9.json
predecessor: lab/process/conditional-physics-ledger-v0.8.json
status: APPEND_ONLY_FIRST_JET_SECTION_GERM_NO_LEAKAGE_EXACT__FINITE_JET_GLOBAL_SHELL_AND_STANDARD_K77_CODIM1_GLOBALLY_HYPERBOLIC_ROUTES_KILLED__CONDITIONAL_OBSERVED_CURVATURE_DISTORTION_EQUATION_TYPED__CONSTRAINED_DOMAIN_UP_AND_BACK_STRESS_AND_VACUUM_OPEN
---

# Conditional physics ledger v0.9

## Progress meter

```text
Ledger v0.9 — 82/82 active target rows mapped (100% of current denominator)
32 SAME · 19 DIFFERS · 25 NEEDS · 6 OVER-DETERMINED
Global K77 gamma_epsilon and rank-ten gravitational receiver: exact
Selected moving Shiab + primitive epsilon + complete even Ward owner: composed
Formal minimal homogeneous even CME: pass at algebraic/compact-core grade
Null coupled kernel: exact 10 -> 6 -> 2 constraint/gauge dimension filtration
Defect Green complex: conditional on flat globally hyperbolic X and harmonic gauge
Complete first-jet section-germ observation/no-leakage: exact
Finite-jet global bulk-shell faithfulness: killed
Standard codimension-one globally-hyperbolic K77 Y14 domain: sharply obstructed
Conditional observed curvature/distortion equation: typed
Constrained ambient domain + physical up-and-back stress + nonzero vacuum: open
Residue — 84 continuous real before quotient + >=19 function-valued
          + 10 open discrete forks
Quotients ranked: 2 local/defect symbol quotients; no global residue reduction
```

Coverage, verdict counts and residue do not change. The new information is in
four row distances. The selected action cannot descend through value-only
pullback because all 85 mixed-normal directions are live, but its complete
first jet along the section is an exact block-triangular observation map. The
inverse-transpose carries every first-order Euler monopole and normal dipole,
so section-germ no-leakage is exact without a datum.

No finite section jet is faithful on the global bulk shell, and the standard
ambient Cauchy horn is now sharply obstructed: every thirteen-dimensional
hypersurface in signature `(7,7)` is indefinite or degenerate. The live
analytic options are observation-first normal-jet constraint propagation, a
constrained ultrahyperbolic boundary theory, or a source-derived Lorentzian
reduction. The two defect graviton polarizations remain intact.

The action-owned observed equation now has the typed form

```text
C_s + kappa_1 v + J_X^conn + E_X^(defect/BV) = 0.
```

`J_X^conn` is a projected connection current, not yet the physical stress
tensor. The Hilbert tensor from an independent `X` matter action and
Weinstein's proposed up-and-back tensor remain separately typed. The trace of
`v` is a variable cosmological-like field, but its nonzero VEV, magnitude and
screening are not selected.

## Row movements

| row | v0.9 disposition | reason |
| --- | --- | --- |
| `LT-GR1b` | no change | the selected post-Shiab factor route remains killed; the conditional pre-Shiab action is a distinct horn |
| `LT-GR2b` | migrated, still `SAME/DERIVED_PARTIAL` | the variable distortion has an exact first-jet observed equation placement; nonzero vacuum selection remains |
| `LT-GR2c` | migrated, still `NEEDS/MISSING_CONSTRUCTION` | section-germ no-leakage is exact and the standard ambient Cauchy horn is obstructed; constrained domain and physical stress identity remain |
| `LT-GR2d` | no change | no magnitude, screening or radiative-stability rule is built |
| `LT-GR5` | migrated, still `DIFFERS/STRUCTURAL_DIFFERENCE` | observed augmented-torsion jet equation is typed; extra-mode constraint propagation remains |
| `LT-GR6` | migrated, still `DIFFERS/STRUCTURAL_DIFFERENCE` | observed receiver/current is typed; symmetric conserved up-and-back stress map remains |
| `LT-SM8` | no change | a formal homogeneous even CME is not a positive physical BV/BRST cohomology |

## Exact null result

For `k=(1,1,0,0)`, the repaired coupled `(h,v)` Hessian has rank 10 and a
10-dimensional kernel. The de Donder symbol

```text
C_k(h)_nu = k^mu h_mu,nu - (1/2) k_nu tr(h)
```

has rank four both on the metric carrier and when restricted to the coupled
kernel. The constraint-compatible kernel is therefore six-dimensional. Null
diffeomorphisms have rank four, lie in the coupled kernel, and preserve the
constraint because `C_k D_0(k)=k^2 I=0`. Hence

```text
dim[(ker J cap ker C_k) / im D_0(k)] = 10 - 4 - 4 = 2.
```

Explicit plus and cross tensors are independent representatives modulo gauge.
This supersedes the earlier wording “six non-gauge characteristic directions
remain” as a sufficient physical diagnosis. Four are constraint violations;
two are physical null modes.

## Formal even BV and analytic boundary

The global primitive chain is

```text
gamma_epsilon = Ad(epsilon^-1) gamma_0
delta Phi_i = [Phi_i, chi]
delta B = D_B chi
delta T = -D_B chi
E_epsilon = D_B^!(E_B-E_T) + (D_epsilon Shiab_epsilon)^! K_S.
```

Together with all separately typed metric, section, density, projector,
background and boundary owners, this gives the complete homogeneous even Ward
identity. Ordinary gauge-algebra closure and Jacobi then give the standard
minimal formal even BV action and classical master equation.

That statement is not the odd square-root-of-connections algebra, a quantum
master equation, a global analytic BV phase space or positive cohomology.

On the flat globally hyperbolic `X` background tested here, harmonic gauge
turns the defect Einstein operator into an invertible trace-reversal
automorphism times a wave operator. The advanced/retarded Green complex and
its operator composition are therefore available conditionally. A curved
background requires a separate normally-hyperbolic lower-order completion.
The full noncompact ultrahyperbolic `Y14` coupled domain remains open. What is
new in v0.9 is that its **ordinary Lorentzian Cauchy horn is closed**: signature
`(7,7)` admits no spacelike codimension-one hypersurface. This is consistent
with Weinstein's own multiple-time technical-debt warning. Constrained
ultrahyperbolic boundary data and observation-first propagation remain live.

## Residue movement

| quantity | v0.8 | v0.9 |
| --- | ---: | ---: |
| continuous reals before quotient | 84 | 84 |
| function-valued slots | at least 19 | at least 19 |
| discrete forks | 10 | 10 |
| horn product | 2304 | 2304 |
| ranked quotients | 2 | 2 |

The section-germ jet interface is not booked as a new physical quotient. It is
an exact observation coordinate/dual theorem. Neither existing physical-symbol
quotient is booked as a reduction of the global parameter residue.

## Next work

1. `LT-GR2c/GR5/GR6`: on the primary observation-first horn, prove normal-jet
   constraint propagation on the Lorentzian defect domain and construct the
   source-directed symmetric conserved up-and-back stress map.
2. `LT-GR2c/GR5/SM8`: independently construct a constrained ultrahyperbolic
   `Y14` boundary/Krein/BFV domain or prove a sharper obstruction to that
   class. This is a rival, not a hidden prerequisite for the observation horn.
3. `LT-GR2b/d`: after one domain horn and the stress map close, construct and
   select a nonzero variable-distortion vacuum and rerun the
   independent vacuum-shift test without retuning `lambda_def` or `kappa_1`.
4. `LT-GR2e`: only then derive FLRW and perturbations.
5. `LT-SM8`: extend the formal even result to the actual odd super-IG/Clifford
   closure and test positive physical cohomology.

Evidence and controls:

- `k77-moving-observation-y14-domain-obstruction-2026-08-05.md`;
- `k77_moving_observation_y14_domain_obstruction_probe.py`; and
- `k77_moving_observation_y14_domain_obstruction_independent.sage`.

The decisive source return is `SOURCE-CORRECTS`. The source confirms the
observation and dynamic-field direction, explicitly acknowledges the
multiple-time analytic debt, and states that the up-and-back stress
construction was unfinished. The jet theorem, signature obstruction and
conditional observed equations are this repo's constructions.
