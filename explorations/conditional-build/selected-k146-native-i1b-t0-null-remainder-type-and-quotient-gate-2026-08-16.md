---
title: "Selected-K146 native I1B T=0 null-remainder type and quotient gate"
status: active_research
doc_type: exact_carrier_typing_and_polynomial_schur_basicness_gate
created: "2026-08-16"
registry: lab/process/selected-k146-native-i1b-t0-null-remainder-type-and-quotient-gate.json
probe: tests/channel-swings/selected_k146_native_i1b_t0_null_remainder_type_and_quotient_gate_probe.py
grade: "K146 CORRECTS THE PROPOSED DIRECT H_N/G_N TEST: P^5 IS A DISTORTION-CARRIER OPERATOR AND DOES NOT ACT ON THE METRIC RADICAL H_N OR GAUGE IMAGE G_N. THE FIRST OWNED METRIC OPERATOR BUILT FROM K135'S POLYNOMIAL IS S_4=-A^*R_4A. IT KILLS G_N EXACTLY BECAUSE A G=0, SO GAUGE PRESERVATION IS POSITIVE WITHOUT AN INVERSE. PRESERVATION OF H_N IS A SEPARATE CURVED COMPOSITION AND REMAINS UNEVALUATED. THE UNIVERSAL CONTROL P_S=M_S D, M_S^2=0 AND P_S^2=S P_S, GIVES P_S^5=S^4 P_S: IDENTICAL NILPOTENT FROZEN PRINCIPAL DATA CAN CARRY ARBITRARY NONZERO FIRST-ORDER FIFTH-POWER REMAINDER. THE CURRENT SERIALIZED GU EVALUATOR OWNS THE ALL-GRADE FROZEN SELECTED-SHIAB COEFFICIENT AND THE BRINKMANN CURVATURE SUBSTRATE, BUT NOT THEIR COMPLETE VARIABLE-COEFFICIENT FIVEFOLD COMPOSITION. THE LOWER REMAINDER IS THEREFORE NOT MATERIALIZED, NOT ZERO. NO ENDOMORPHISM OF H_N/G_N EXISTS UNTIL S_4(H_N) SUBSET H_N IS PROVED."
target_claim: K145_NEXT_GATE__SELECTED_SHIAB_ORDER_AT_MOST_FOUR_P5_REMAINDER_AND_H_N_G_N_PRESERVATION
target_verdict: DIRECT_P5_QUOTIENT_TEST_ILL_TYPED__POLYNOMIAL_SCHUR_S4_FIRST_OWNED_METRIC_MAP__S4_G_ZERO_EXACT__H_N_PRESERVATION_UNEVALUATED__FULL_SELECTED_SHIAB_CURVED_COMPOSITION_NOT_MATERIALIZED
canon_verdict_change: none
---

# Selected-K146 native I1B T=0 null-remainder type and quotient gate

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K146 binds the selected displayed `comm/symi/symi` source-native `I1B`
Hessian at the Ricci-flat `T=0` germs of K127/K138, fixed nonzero `kappa_1`,
K145's exact polynomial remainder, and the K138 null metric quotient. It
classifies the carriers and the first well-typed polynomial metric
composition. It does not evaluate the complete curved selected-Shiab
fivefold composition, select an inverse or parametrix, or define a physical
propagation law.

## Result in plain English

K145 proposed evaluating the lower part of `P^5` and then testing whether it
preserves `H_n` and `G_n`. The two clauses do not compose directly:

```text
P^5 : Omega1(Cl(7,7)) -> Omega1(Cl(7,7)),
H_n,G_n subset Sym2(T*),
dim Omega1(Cl(7,7))=229376,  dim Sym2(T*)=10.          (1)
```

The action owns carrier bridges
`A:Sym2(T*)->Omega1(Cl(7,7))` and `A*` in the reverse direction. Therefore
the first metric operator associated with K135's polynomial is

```text
R_4=kappa_1^-1 sum_(j=0)^4(-kappa_1^-1 P)^j K,
S_4=-A* R_4 A : Sym2(T*) -> Sym2(T*).                 (2)
```

This is a local differential polynomial and requires no inverse. K145's
Noether identity gives the exact positive result

```text
S_4 G=-A*R_4 A G=0.                                  (3)
```

Thus `S_4` preserves the diffeomorphism image, in fact annihilates it. But an
endomorphism on `H_n/G_n` additionally requires `S_4(H_n) subset H_n`.
Neither `L(n)^5=0`, nonzero Brinkmann curvature, nor (3) proves that separate
curved composition.

The lower distortion remainder is genuinely data-sensitive. K144's exact
control has

```text
u_s=(1,sx)^T, v_s=(-sx,1)^T, M_s=u_s v_s^T,
P_s=M_s d/dx, M_s^2=0, P_s^2=sP_s, P_s^5=s^4P_s.     (4)
```

Every slope has the same square-zero frozen matrix at `x=0`, while the
fifth-power differential remainder ranges from zero to nonzero first order.
This is an exact lower-order control, not an `I1B` coefficient.

The repository serializes the complete all-grade frozen selected-Shiab
coefficient and separately reconstructs the Brinkmann curvature and first
jet. It does not serialize one executable total variable-coefficient operator
carrying the moving frame, form connection, Clifford-adjoint connection,
Shiab, pairing and formal-Euler adjoint through five compositions. Therefore
the selected lower `P^5` remainder is
`NOT_MATERIALIZED_FROM_CURRENT_SERIALIZED_EVALUATOR`, not zero and not
nonzero by inference.

```text
direct P^5 test on H_n or G_n:          ILL_TYPED_DIFFERENT_CARRIERS;
polynomial metric operator S_4:         ACTION_OWNED_LOCAL_FORMAL_OPERATOR;
S_4 preservation of G_n:                PASS_BY_A_G_ZERO;
S_4 preservation of H_n:                UNEVALUATED_CURVED_COMPOSITION;
endomorphism on H_n/G_n:                UNDEFINED_H_PRESERVATION_OPEN;
selected lower P^5 value:               NOT_MATERIALIZED_FROM_CURRENT_SERIALIZED_EVALUATOR.
```

## 0. Layer-0 packet and route selection

| object | carrier | role |
| --- | --- | --- |
| `P`, `P^5`, `R_4` | distortion | generalized Euler polynomial |
| `A`, `A*` | metric/distortion cross | action-owned carrier bridge |
| `S_4=-A*R_4A` | metric | first typed polynomial Schur candidate |
| `H_n`, `G_n`, `H_n/G_n` | metric | radical, gauge image, quotient |

The route-changing type audit dominated a direct large computation: a value
of `P^5` alone could never answer the stated quotient question. The exact
matrix control then proves that frozen nilpotence cannot fill the missing
lower coefficient. The fallback is a future sparse covariant evaluator for
`S_4` itself, with radical preservation as its cheapest decisive condition.

## 1. Exact polynomial metric composition

Equation (2) is well typed using only action-owned differential operators. It
is not the exact Schur complement: K145 proves `R_4 C` and `C R_4` leave
fifth-power remainders. It is the exact local operator obtained from the
frozen-polynomial candidate.

Gauge basicness is nevertheless complete. Since `A G=0` as a differential
identity, every defined operator between `A` and `A*` gives zero on `G`. No
boundary condition or representative choice enters. This is narrower than a
quotient endomorphism because a map can kill `G_n` and still leak `H_n` out of
the radical.

## 2. Why the Brinkmann packet does not yet close the value

The Brinkmann metric is an exact background family, not merely a point
curvature fixture. The missing item is computational serialization, not a
claim that the mathematical action is ambiguous. Existing probes expose the
frozen all-grade coefficient, the algebraic Hodge map, principal metric-
curvature columns, and the central curvature/first jet. They do not expose
one common variable-coefficient `E(D_B)` routine whose connection acts on the
one-form and Clifford indices and whose formal-Euler transpose is propagated
through five factors. Combining separate outputs by hand would guess omitted
moving-coefficient terms—the failure class corrected by K125/K126.

## 3. Hostile controls and claim ceiling

- **Carrier:** a distortion endomorphism cannot act on the metric radical
  without `A/A*` bridges.
- **Frozen/lower:** (4) gives identical frozen nilpotent data and slope-
  dependent fifth powers.
- **Gauge/radical:** `A G=0` proves gauge preservation; a planted metric map
  can preserve `G` while leaking `H`.
- **Curvature/projection:** nonzero curvature is an input, not the selected
  projected value.
- **Formal/local:** `S_4` is a formal local differential operator, not a
  closed-domain inverse or physical Green operator.

No canon, ledger, claim status, particle, phenomenology, published paper, or
GU-wide verdict changes.

## 4. Reverse scaffold and next gate

```text
R0 K138: H_n/G_n is a covariant five-class metric quotient.
R1 K145: the distortion polynomial leaves exact P^5 remainders.
R2 K146: P^5 does not act on H_n or G_n.
R3 K146: S_4=-A*R_4A is the first typed local metric composition.
R4 K146: S_4 G=0 exactly; H_n preservation remains open.
R5 K147: serialize one sparse covariant selected-Shiab E(D_B) evaluator and
   compute ell_n S_4 h for a basis of H_n on the Brinkmann family.
R6 only after R5 passes: descend S_4 to H_n/G_n.
```

Joe input is not required. K147 is an implementation/evaluator gate, not a
new action, inverse, gauge, boundary or physical-state choice.

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k146_native_i1b_t0_null_remainder_type_and_quotient_gate_probe.py
```
