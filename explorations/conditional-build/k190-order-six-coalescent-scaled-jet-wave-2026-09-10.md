---
title: "K190 order-six coalescent scaled-jet wave"
document_role: active_research
doc_type: conditional_native_K139_K190_order_six_coalescent_scaled_jet_result
created: 2026-09-10
date: 2026-09-10
claim_ceiling: exact repository-owned cancellation-preserving coalescent normal forms for the K186 size-two and size-three regularizers in the scaled jets a_n=x^n q^(n)(x) of q(x)=x*K1(x), with directed-Arb point balls at all 199 dyadic radii from 2^-200 through 1/4 and all-53-pattern high-precision finite-gap convergence controls; this resolves the complete-coalescence conditioning singularity and selects scaled q jets as the finite-gap coordinates, but supplies no outward nonzero-gap enclosure, Duffy/Jacobi chain-rule remainder, complete positive-core error, outward order-six total, accurate prefix, action column, residual, complement or flux floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation
manifest: lab/process/k190-order-six-coalescent-scaled-jet-wave.json
solver: tests/channel-swings/k190_order_six_coalescent_scaled_jet.py
probe: tests/channel-swings/k190_order_six_coalescent_scaled_jet_probe.py
target_claim: INTERNAL_TARGET:K189_CANCELLATION_PRESERVING_SCALED_KERNEL_OR_REGULARIZER_TAYLOR_GATE
target_claim_verdict: COMPLETE_COALESCENCE_HAS_AN_EXACT_WELL_CONDITIONED_SCALED_Q_JET_NORMAL_FORM_FOR_R2_AND_R3__FINITE_NONZERO_GAPS_REQUIRE_BIVARIATE_CONFLUENT_DIVIDED_DIFFERENCE_TAYLOR_ENCLOSURES_BEFORE_DUFFY_JACOBI
canon_verdict_change: none
---

# K190 order-six coalescent scaled-jet wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: K186's fixed K139/K156 hard-core `C3` impurity control, auxiliary chart
`lambda=256`, equal couplings one, three normalized K162 zero-bath seed orbits,
and complete K184--K189 order-six determinant family. K190 changes no operator,
extension, state, domain, polarization or scalar center. It resolves only the
fully coalescent conditioning spine of the K189 positive-radius core.

```gu-typed-objects
result: the K186 size-two and size-three regularizers have exact complete-coalescence normal forms in a_n=x^n q^(n)(x), q=x*K1(x), with every negative power of x cancelled before evaluation; directed Arb balls at all 199 dyadic radii 2^-200 through 1/4 are positive and overlap the original confluent Hankel formula, while all 53 pattern controls converge to the matching scaled-jet limit
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on the order-six output sector; K184 coherent path-pair assembly and K186 specieswise determinant factorization remain unchanged ON=repository_signed_point_control
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, positive Laplace times, exact K0/K1 recurrences, confluent mixed divided differences, dimensionless scaled jets and python-flint 0.9.0 / FLINT 3.6.0 Arb balls
grading: K186 exact Cauchy--Vandermonde skeleton and coalescent divided differences, K188 four-region split, K189 raw-kernel global-envelope rejection, exact R2/R3 scaled-jet normal forms, 199-radius directed-Arb point sweep, 53-pattern near-collision controls and withheld finite-gap/cubature release
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W, K179 fixes the coefficient family, K184 fixes the time Grams, K185/K188 fix boundary majorants and K186/K187/K189 fix factorization, endpoint class and global raw-kernel failure; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: remove the complete-coalescence singularity from the interval coordinates, select the exact scaled q-jet spine for the finite-gap model, and state the remaining bivariate confluent divided-difference enclosure without claiming that it is already built MAP-TYPE=intertwiner
```

## Inline preflight bookend

K189 leaves one conditioning question before finite-gap cubature: can the
Cauchy cancellation be performed symbolically at complete row and column
coalescence, where direct determinant division is formally `0/0`? If the
scaled form remained broad or singular there, a large stratified solver would
be the wrong next investment.

The strongest independently framed source-to-physics challenger remains the
positive physical quotient/count/chirality bridge. `SC-META-53` is
`UNCERTAIN`; current ledger rows `LT-SM8`, `RA-F1` and `AC-F1` remain `NEEDS`,
with no action-owned stationary background, functional BV/BFV domain,
positive physical pairing, nontrivial physical cohomology or typed count/chiral
observable. K190 therefore pursues an independent reusable determinantal
method; it implies no GU or physical verdict.

The route comparison rejected raw determinant intervals and arbitrary-
precision subtraction as the interval object, and found no held comparison
theorem proving `R_m<=1`. Direct confluent derivatives of `2 K1` are exact but
carry large positive and negative powers of the common argument. The selected
route rewrites the whole quotient in dimensionless scaled jets before Arb sees
it.

## 1. Exact confluent determinant

Let all canonical row times coalesce to `T` and all canonical column times to
`U`, and put `x=T+U`. Successive mixed Newton differences give

```text
R_m(x) = x^(m^2) / 2^m
         det[ f^(i+j)(x)/(i! j!) ]_(i,j=0,...,m-1),
f(x) = 2 K1(x).
```

This is the exact confluent extension already licensed qualitatively by K186.
It is not yet a finite-gap enclosure.

## 2. Dimensionless scaled-jet normal form

Set

```text
q(x) = x K1(x),       a_n(x) = x^n q^(n)(x).
```

The Bessel identities give a cancellation-preserving basis through the order
needed by size three:

```text
a0 = q = x K1(x)
a1 = -x^2 K0(x)
a2 = x^2 (q-K0)
a3 = x^2 q-x^4 K0
a4 = (x^4-x^2)q-2x^4 K0.
```

Substitution into the confluent determinant cancels every negative power of
`x` algebraically. The exact size-two result is

```text
R2 = q^2 + q a2 - a1^2.
```

The exact size-three result is

```text
R3 = q^3 + 3q^2 a2 - 3q a1^2
   + q^2 a3 - 3q a1 a2 + 2a1^3
   + (q^2 a4 - 4q a1 a3 + 3q a2^2)/4
   + (q a2 a4 - q a3^2 - a1^2 a4
      + 2a1 a2 a3 - a2^3)/4.
```

The independent SymPy probe expands the original derivative Hankel
determinants and proves both polynomial identities exactly.

## 3. Directed-Arb coalescent spine

The solver evaluates both normal forms and the original confluent Hankel
formula at every exact dyadic radius `x=2^-k`, `k=2,...,200`, with 200 decimal
digits and one thread. Every Arb ball has nonzero width, every scaled `R2` and
`R3` ball has a strictly positive lower endpoint, and every original Hankel
ball overlaps its scaled counterpart.

Across those certified point balls, the midpoint ranges are

| regularizer | midpoint range |
| --- | ---: |
| `R2` | `0.8328240456730885` to `1.0` |
| `R3` | `0.7217700981526607` to `1.0` |

The scaled form is at least `3.62` times narrower than the direct Hankel form
for size two and `35.37` times narrower for size three over the sweep. These
are conditioning comparisons at exact radii, not outward ranges over the
interval between radii. Samples below one do not establish a global `R_m<=1`
theorem.

## 4. Complete-pattern near-collision control

For every one of K186's 53 distinct canonical position patterns, the solver
constructs size-matched row and column times with gaps `10^-10` and `10^-24`,
evaluates K186's independent 260-digit mixed divided differences, and compares
them with the scaled-jet coalescent value at the common argument. The maximum
finest-gap relative difference is `3.16e-24`; all 53 are below `1e-20`.

This preserves the pattern IDs and canonical positions and keeps all 468
nontrivial occurrences, 234 signed time-Gram entries and 18 coherent groups in
scope. It is an independent convergence control only. Decimal divided
differences at nonzero gaps are not promoted to outward finite-gap intervals.

## 5. Route decision and release boundary

The complete-coalescence singularity is resolved: the regularizer has a small,
positive, cancellation-preserving point representation all the way down to the
K188 primitive floor. Scaled `q` jets are therefore the right coordinates for
the next finite-gap model.

The remaining object is genuinely bivariate. It must enclose confluent divided
differences away from zero gap, retain correlations among the four or nine
kernel entries, and bound the defect from the coalescent spine on dyadic radial
strata and gap-ratio boxes. Entrywise `q` boxes alone are not declared
sufficient. Only after those outward finite-gap enclosures exist may the
Duffy/Jacobi chain rule and weighted cubature remainder be composed.

K190 does not serialize a finite-gap regularizer interval, core cubature error
or complete outward order-six total. The accurate order-six prefix remains
through order five. No K171/K168 action column, `R_ref` residual, complement or
flux floor, scalar-center floor or K152 interval is claimed. `SC-META-53`,
`LT-SM8`, `RA-F1` and `AC-F1` remain unchanged.

## Inline postflight bookend

- **Strongest advance:** exact dimensionless `R2/R3` coalescent normal forms cancel every negative power before evaluation and stay rigorously positive at 199 dyadic radii.
- **Strongest route decision:** the scaled-jet coordinates succeed at the singular spine, so the next work is a finite-gap bivariate confluent Taylor enclosure rather than another raw-kernel subdivision.
- **Strongest overclaim:** “positive dyadic point balls bound the whole core.” Refused; no between-radius or nonzero-gap outward coverage exists.
- **Strongest contrary route:** a global total-positivity comparison theorem could dominate the finite-gap build, but no applicable determinant ordering theorem is established in current owner evidence.
- **Weakest reproducibility seam:** the 53-pattern finite-gap controls converge at high precision but are not interval certificates; the next solver must turn the gap defect into an outward bound.

The exact algebra, Arb sweep, all-pattern controls, symbolic identity check,
hostile release surface and successor specification form one coherent result.

## Reproduction

```bash
_local/cas-venv/bin/python tests/channel-swings/k190_order_six_coalescent_scaled_jet.py --summary
_local/cas-venv/bin/python tests/channel-swings/k190_order_six_coalescent_scaled_jet.py --write --summary
_local/cas-venv/bin/python tests/channel-swings/k190_order_six_coalescent_scaled_jet_probe.py
_local/cas-venv/bin/python tests/channel-swings/k190_order_six_coalescent_scaled_jet_probe.py --selftest
```
