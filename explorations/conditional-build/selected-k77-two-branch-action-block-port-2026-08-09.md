---
artifact_type: construction_and_composition_result
created: 2026-08-09
status: BOTH_BRANCHES_RETAIN_OWNED_ACTION_BLOCKS__COMMON_PRINCIPAL_SYMBOL__DISTINCT_LOWER_ORDER_RESPONSE__COMPLETE_PORT_OPEN
channels: [SOURCE, COMPOSE, BUILD, VERIFY]
ledger_rows: [LT-GR1, LT-GR2b, LT-GR2c, LT-GR3, LT-GR5, LT-GR6]
canon_verdict_change: none
---

# Selected K77 two-branch action-block port

## Plain English result

The two surviving exact K77 backgrounds have now been tested against the
action blocks the repository had already built. Both pass. This eliminates the
shortcut of treating those blocks as belonging only to the earlier rational
fixture.

The useful distinction is now exact:

- the first action's moving-Shiab `epsilon`/Clifford-one cross remains rank 91
  on both branches, but with unequal positive conjugate coefficients;
- the residual equation's full low-grade zero-jet `varpi` map remains
  injective, rank 1470, on both branches, again with unequal conjugate
  lower-order coefficients;
- the selected 125-field causal principal bank is genuinely common to the two
  branches: its timelike/spacelike/null Gram ranks remain `110/110/16`.

So the leading propagation geometry is common while some lower-order dynamics
is branch-dependent. Neither branch is selected, and neither may stand in for
the other in the unfinished full operator.

## Exact data

Relative to the independently reconstructed common branch, the first-action
cross scales by

```text
(9/16)(2-sqrt(3)),  (9/16)(2+sqrt(3)).
```

Both numbers are positive and nonzero, so rank 91, 182 nonzero entries, two
receivers per epsilon column and Clifford-grade-one support all persist.

For the raw residual, the zero-jet amplitudes are

```text
(-3+sqrt(3))/624,  (-3-sqrt(3))/624,
```

and the scalar-line derivatives are `-2+sqrt(3)` and `-2-sqrt(3)`. Direct
exact elimination over `Q(zeta_12)` gives rank 1470 on each full low-grade
`varpi` tangent.

## What is and is not ported

This wave ports two actual, already-owned blocks:

1. the moving-Shiab epsilon/Cl1 cross of the first transgression action;
2. the zero-jet low-grade varpi map and the selected metric/varpi/epsilon
   principal bank of the distinct residual-square route.

It does **not** construct the branch-dependent lower-order metric and epsilon
blocks, the complete first-action Hessian, off-slice expanded-parent
Hessians, gauge fixing, ghosts, a Riesz map, a closed domain or coupled BV-BFV.
Principal-symbol equality is not complete Frechet equality.

The selected Spin carrier, the two separate `U(32,32)` Weyl halves and the
full `U(64,64)` comparator remain three distinct parent scopes. Nothing here
selects among them or licenses combining the two action candidates with an
invented relative coefficient.

## Accounting and next gate

```text
new fields/coefficients/selectors/bundle classes/quotients: 0
P1/P2/P3 consumed: 0
conditions closed/opened/remaining: 2 / 0 / 4
```

Primary certificate: `49/49 PASS`. Independent Sage/FLINT port: `14/14 PASS`,
on top of its independently replayed predecessor banks.

Next build the missing branch-dependent lower-order metric/epsilon blocks and
the expanded-parent pairings. Keep the first and residual-square actions
separate; only after a complete parent-indexed operator exists should gauge,
ghost, `Dmax/Dmin`, Green/Krein and coupled BV-BFV tests run.
