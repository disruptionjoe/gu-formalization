---
artifact_type: exploration
cluster: A-representation-content
created: 2026-08-05
title: "CB-A: the three-column representation-content ledger — 41 rows built conditionally on an unbuilt source action, of which 18 are exactly-same, 11 need no unknown at all, 1 is OVER-DETERMINED (the source's own stated effective-chirality mechanism is excluded by Schur, not merely unbuilt [title clause CORRECTED 2026-08-17: the Schur exclusion binds a G_SM-preserving MASS mechanism on the conjugate-pair comparator carrier, not the source's stated VEV-conditional decoupling — see CORRECTION V1 at end of file]), and the entire rank-two internal tensor class is excluded as a Higgs parent in one theorem"
grade: "EXACT finite weight-lattice representation theory over the complexified chain so(14) >= so(4)+so(10) >= su(4)+su(2)+su(2) >= su(3)+su(2)+u(1), 45/45 checks, no float load-bearing anywhere (P-H29 satisfied by construction). Plus a typed conditional ledger over a symbolically-carried unknown source action. NOT: any source action, vacuum, pole, residue, physical domain, effective chirality, generation count, or claim-status movement."
named_gate: CONDITIONAL-BUILD-CLUSTER-A-REPRESENTATION-CONTENT
gate_before: K77A_ATOMIC_LEDGER_ONE_COLUMN_NO_EXTERNAL_DATUM_JOIN
gate_after: CB_A_THREE_COLUMN_REPRESENTATION_LEDGER_FILED__RANK_TWO_HIGGS_CLASS_EXCLUDED__MASS_TYPE_EFFECTIVE_CHIRALITY_OVER_DETERMINED
fork_assumed:
  - SIGNATURE-AMBIENT
  - CARRIER-SPLIT
fork_stack_acknowledged:
  SIGNATURE-AMBIENT: >-
    Declared voluntarily. This artifact sorts at depth 2 in the date-ordered
    counter and so does not trip the threshold-3 gate, but the live stack on
    SIGNATURE-AMBIENT is 10 deep and every peer disposition acknowledges it;
    escaping on sort order would be exactly the invisible cost the gate exists
    to surface. Proceeding is cheap here and the reason is computed, not
    asserted: 37 of this ledger's 41 rows are complexified branching
    multiplicities, which do not see the real form at all, and 2 of the 4
    fork-sensitive rows are separately certified fork-robust (PH-K1-KINEMATIC
    holds under both real forms; the M-M1 lemma holds for every (p,q) with
    p+q = 2 mod 4). Net exposure to this open horn is two rows, D3 and D4,
    both flagged in place.
  CARRIER-SPLIT: >-
    Same argument, and weaker still: no row in this cluster distinguishes the
    Lorentzian (3,1)+(6,4) horn from the compact (4,0)+(5,5) horn or from the
    author-declared third option, because every representation-content row is
    computed after complexification, where Spin(6,4)^C = Spin(10)^C. The horn
    is declared because the observation split is quoted from K77-A, not
    because any row depends on it.
search_space_dim: "100 = dim(V10 (x) V10) = 45 + 55, the complete rank-two internal tensor class, excluded wholesale as an SM-Higgs parent by one weight-lattice theorem rather than candidate-by-candidate; the fermion rows search a 0-dimensional space (the branching is unique once U1 is fixed)"
free_object_delta: 0
residue_touched:
  - {id: CB-A-U1-STABILIZER-SELECTION, grade: T3}
  - {id: CB-A-U3-CHIRALITY-MECHANISM, grade: T3}
  - {id: CB-A-U4-HIGGS-CHANNEL, grade: T3}
  - {id: SU4C-SEESAW-MR, grade: T2}
  - {id: M-H7-GENERATION-SLOT, grade: T4}
route_disposition: CONTINUE_CONDITIONAL_BUILD__MASS_TYPE_EFFECTIVE_CHIRALITY_EXCLUDED__HIGGS_CHANNEL_CLASS_NARROWED
source_collision: SOURCE-CONFIRMS-PATI-SALAM-ONE16-AND-BIDOUBLET-CHANNEL; ~~SOURCE-CONTRADICTED-ON-VEV-INDUCED-EFFECTIVE-CHIRALITY~~ [second token WITHDRAWN 2026-08-17 per CORRECTION V1 at end of file -- the row contradicts a comparator-carrier mass mechanism, not the source's stated mechanism]
canon_verdict_change: none
priority_change: none
third_lane_promoted: false
deposit_gate: "PRE-DEPOSIT, J5-gated. Row D2 (OVER-DETERMINED) would, if adopted, move the effective-chirality axis from `NARRATIVE / under-specified` to `excluded in the stated class`. That is a verdict-adjacent consequence and it is NOT enacted here. It moves only via a filed hostile field-specialist review (two-sided charge: overclaim AND over-fencing). Nothing in this file moves a claim, canon entry, verdict, bar, count, LANE-STATE, fork registry row, or public posture."
---

# CB-A — representation content, three columns, built on an unknown

## Result first

The Standard Model's representation content decomposes into **41 rows**. Every
row is stated three ways: what the SM requires, what GU determines *conditional
on the unbuilt source action carried symbolically*, and the verdict.

| verdict class | rows |
|---|---:|
| **SAME** — exactly the same mathematics, zero residual freedom | **18** |
| **DIFFERS(term)** — differs by a named term, with the geometric reason | **9** |
| **NEEDS-U_k** — determined once a named piece of the unknown is fixed | **12** |
| **OVER-DETERMINED** — no admissible choice of the unknown satisfies the row | **1** |
| **DEAD ROUTE** — recorded so it is not re-derived | **1** |

Independently of that split: **11 of the 41 rows depend on no unknown at all** —
their GU-side content is fixed with the source action still absent.

Three findings carry the artifact.

**1. Eighteen rows match the SM exactly, and the hypercharge/anomaly block has
zero residual freedom.** All six hypercharge assignments, all five anomaly
conditions, and charge quantization follow from one formula, `Y = T3R + (B−L)/2`,
with **zero free parameters** once `U1` (the stabilizer) is fixed — and the
hypercharge line itself is *unique mod the Spin(10) Weyl group*, so `U1` does not
even get to choose it. Constraint-surplus on this block: **11 independent
constraints, 0 free parameters.** This is the lane's positive column and it is
large.

**2. The entire rank-two internal tensor class is excluded as a Higgs parent, in
one theorem, without building anything.** `Λ²V₁₀` (= 45, the `so(10)` adjoint)
and `Sym²V₁₀` (= 1 + 54, the metric / trace-reversed Frobenius slot) between them
span all 100 complex dimensions of `V₁₀ ⊗ V₁₀`, and both contain **exactly zero**
colour-singlet weak doublets — at any hypercharge. The SM Higgs cannot live in
either. This is a Method-A class exclusion in the sense of the 2026-08-04
council's §2.1, and it kills the metric-slot Higgs route a *second*, independent
time (the repo's `P-54-WELD` failure was a different argument).

**3. One row is OVER-DETERMINED, and it is the source's own stated mechanism.**
Reported in full in §5 below and flagged loudly, because the program has had no
way to fail and this is one.

---

## 0. PRE-WAVE (binding — answered before any construction)

### PRE-1. Which fork is this wave assuming?

Two, declared: **`SIGNATURE-AMBIENT`** (the `(7,7)` horn) and **`CARRIER-SPLIT`**
(the Lorentzian `(3,1)+(6,4)` horn), both `status: open` in
`lab/process/layer0-fork-registry.yaml:41,60`.

**What it costs if the other horn is right: almost nothing, and this is a
computed statement, not a hope.** Every row in §2–§4 and §6 is a *complexified*
branching multiplicity. Complexification does not see the real form
(`layer0-fork-registry.yaml (REF REPAIRED 2026-08-08: this cited lines 94-96, which a 2026-08-07 insertion into the SIGNATURE-AMBIENT row displaced; resolve by content, and note the nearest matching text is now MORE guarded than the use made of it here -- "complexification does not carry the real pairing or the right-H structure, so nothing built on the other horn transfers", which cautions against step 2 rather than licensing it)`), and `Spin(6,4)^C ≅ Spin(10)^C`, so the
representation-content rows transfer across both horns unchanged. Exactly
**four** rows are fork-sensitive, and all four are in the chirality/reality
sub-cluster (§5): D1, D2, D3, D4. Of those, D1 and D2 are *independently*
fork-robust for a second reason — `PH-K1-KINEMATIC` is confirmed under **both**
real forms and all five allocations
(`explorations/chirality-grading-and-77-rerun-2026-08-03.md:190`), and the M-M1
scalar-vanishing lemma holds for **every** `(p,q)` with `p+q ≡ 2 mod 4`
(`explorations/signature-independent-scalar-vanishing-lemma-2026-08-03.md:35-43`).

So: **37 of 41 rows are fork-free; 4 are fork-declared and 2 of those 4 are
separately certified fork-robust.** Net exposure to the two open horns: two rows
(D3, D4).

This wave does **not** assume `KINEMATIC-VS-PHYSICAL-CARRIER`. Everything below
is kinematic/representation-theoretic. `Π_RS^phys` does not exist
(`OQ-RK1 BLOCKED_NEEDS_SPEC`, `layer0-fork-registry.yaml:169`) and no row here
reads a kinematic result as a physical one.

### PRE-2. What is the dimension of the space being searched, and can it be excluded wholesale?

**Yes, and it was.** The load-bearing question — *where can the SM Higgs doublet
live?* — was not attacked candidate-by-candidate. The multiplicity function

```text
    m(R) = # colour-singlet SU(2)_L doublets with Y = ±1/2 inside R
```

was computed for the whole rank-two internal tensor class at once:

```text
    dim(V10 (x) V10) = 100 = dim Λ²V10 (45) + dim Sym²V10 (55)
    m(Λ²V10)  = 0        m(Sym²V10) = 0        m over the whole class = 0
```

The sharp sub-form the council specified — *is the target a coordinate on the
orbits of a group under which all candidates are invariant?* — has an exact
analogue here and it is what makes the exclusion airtight rather than
enumerative. A colour-singlet weak doublet needs an `so(10)` weight whose
`su(4)` part is `(0,0,0)` or `(n,n,n)` and whose `(h₄,h₅)` part has
`h₄+h₅ = ±1`. Rank-two tensors of the vector have **at most two nonzero
entries**; `(n,n,n)` needs three, and `(0,0,0)` forces both nonzero entries into
the `(h₄,h₅)` plane, giving `T3L ∈ {0,±1}` and never `±1/2`. The exclusion is a
statement about the weight *lattice*, so no choice of coefficient, basis, VEV,
normalization, or source action can evade it. **One theorem, 100 dimensions, no
enumeration.**

The fermion rows search a **0-dimensional** space: once `U1` fixes a
`G_SM`-shaped stabilizer, the branching of the `16` is unique (the hypercharge
line is unique mod Weyl — `explorations/channel-swing-CH-SM-2026-07-19.md:69-76,240`,
"**ONE physical hypercharge line** … 0 bits").

### PRE-3. Does success introduce a new un-owned object?

**No. `free_object_delta = 0`.** The symbols `U1 … U7` introduced in §1 are
*labels for unknowns the program already carries* — the source action, its
vacuum, its stabilizer selection — not new objects. Naming an existing unknown
is not introducing one; that is the whole method of this lane.

A hostile grader could book **−1**, on the ground that §5 excludes the mass-type
realization of `M-EFFECTIVE-CHIRALITY`
(`lab/process/resolver-wave-k77a-atomic-particle-crosswalk.json`, mechanism
`M-EFFECTIVE-CHIRALITY`, status `SOURCE_GUIDED_CANDIDATE`), retiring a live
un-owned object. **That reading is available and is deliberately not claimed
here.** `free_object_delta` is the one number the council said cannot be improved
by writing more artifacts (`science-council-program-efficiency-2026-08-04.md:81`),
so it is graded conservatively.

---

## 1. The unknown, carried symbolically

The source action is not built. This lane does not wait for it. It is carried as
seven named unknowns, and every row declares which it depends on.

| id | what it is | who would own it | current in-repo status |
|---|---|---|---|
| **U1** | the **action-selected stabilizer**: which subgroup of the ambient structure group the vacuum leaves unbroken | source action + vacuum | `C1_EXACT_CONTAINMENT__SELECTION_OPEN`; "containment is not a physical unbroken gauge group" (crosswalk JSON, `M-GAUGE-SELECTION`) |
| **U2** | the **light-fermion carrier**: which sub-bundle of `Ω⁰ ⊕ Ω¹(Y,S)` carries physical light fermions (which ambient half; which of `F/Q/Z`) | source action | K77 `F/Q/Z` invariant subspaces **unbuilt** (`k77a:255-256`) |
| **U3** | the **zero-order coefficient and its VEV**: the `X` in `Ψ*KXΨ` the vacuum selects — i.e. the mass operator | source action + vacuum | "no coefficient, VEV, action, or spectrum selected" (`M-K77-BILINEAR-INCIDENCE` boundary) |
| **U4** | the **Higgs channel**: which Lorentz-scalar component of the connection/one-form propagates, and its potential | source action | `SOURCE_GUIDED_CANDIDATE` (`k77a:149`); `hosted_open` (`matter-gauge-source-geometry-selector-gate-2026-06-24.md:255`) |
| **U5** | the **multiplicity mechanism**: whatever supplies the integer 3 | unowned | receptacle **T4**, "the slot is empty" (`science-council:193`) |
| **U6** | the **global form**: the central/discrete data (`μ₆`) of the unbroken group | source action | `failed` as a derivation (`sm-gauge-higgs-finite-control-extraction-ledger-2026-06-24.md:67`) |
| **U7** | the **imported absolute scale** | external, banked | `PRED-NORM-RANK = RESOLVED_NO_GO`; touches no representation-content row |

**Dependency census across the 41 rows:**

| unknown | rows depending on it | which |
|---|---:|---|
| `U1` | **19** | A1–A6, A8, B1–B6, C1–C6, G4 |
| `U4` | 5 | E1, E2, E5, E6, E7 |
| `U3` | 4 | D2, D4, G1, G3 |
| `U2` | 4 | D2, D4, F1, G1 |
| `U5` | 2 | F1, F2 |
| `U6` | 1 | A6 |
| `U7` | 0 | — |
| **none** | **11** | A7, B7, B8, B9, D1, D3, E3, E4, F3, G1*, G2 |

(*G1's *existence* claim needs nothing; its *decoupling* obligation needs `U3`.)

`U1` is by a wide margin the highest-fan-out unknown in this cluster. That is a
schedulable finding rather than an observation: **fixing `U1` alone converts 19
of 41 rows from conditional to determined**, and 12 of those 19 land in the
`SAME` column with zero residual freedom. No other unknown in the cluster comes
close.

---

## 2. Gauge group and its factors (8 rows)

| # | SM REQUIRES | GU DETERMINES (conditional) | VERDICT |
|---|---|---|---|
| **A1** | `SU(3)_C` colour, exactly one factor, fundamental = **3** | `su(3) ⊂ su(4) ⊂ so(6) ⊂ so(6,4)`; the `A1`-lock says that for a regular `su(3)_c` on any coordinate triple, the **only** commuting root-`su(2)`s are the Pati–Salam `SU(2)_L`, `SU(2)_R` (`channel-swing-CH-SM-2026-07-19.md:62-68`) | **NEEDS-U1** (containment exact; selection open) |
| **A2** | `SU(2)_L` weak isospin, doublets are genuine `2`s | `su(2)_L = ±(e₄+e₅)`, forced by the `A1`-lock once `su(3)_c` is fixed | **NEEDS-U1** |
| **A3** | `U(1)_Y`, **compact**, so charge is quantized | `u(1)_Y ⊂ so(10)`; compactness is inherited, not assumed — charge quantization is a *consequence* | **SAME** (given `U1`; the compactness is not `U1`'s to choose) |
| **A4** | gauge algebra has dim **12**, rank **4** | `so(10)` adjoint `Λ²V₁₀` = **45**, rank **5**. Exactly **2** SM-singlet `(1,1,0)` directions survive (computed) — `U(1)_Y` and `U(1)_X` | **DIFFERS(one extra U(1))**: rank 5 vs 4; the extra `U(1)_{B−L}` is forced by the rank of the carrier, not chosen |
| **A5** | no gauge bosons beyond the 12 | internal adjoint 45 − 12 = **33** extra directions; ambient `Λ²V₁₄` = 91 = 6 (Lorentz) + 40 (mixed) + 45 (internal), so **73** non-SM ambient gauge directions total | **DIFFERS(33 / 73 extra gauge bosons)**: geometric reason — the SM adjoint is a proper subspace of a curvature 2-form on a 14-dimensional carrier; all must be lifted by `U1`'s VEV |
| **A6** | global form `G_SM = (SU(3)×SU(2)×U(1))/Z₆` | the `μ₆` kernel is *computed*, conditionally: `μ₆ = ⟨(ω₃I₃, −I₂, e^{iπ/3})⟩` and the quotient `(SU(3)×SU(2)_L×U(1)_Q)/μ₆` (`cycle1-source-selected-pati-salam-stabilizer-gate-2026-06-24.md:275-283`) — **conditional on a source-selected rank-one `v_PSB ∈ (10bar,1,3)`**, which is not selected | **NEEDS-U6** (filed `failed` as a derivation, `:67`) |
| **A7** | Lorentz `Spin(1,3)` acts, commuting with the gauge group | observation image is `(Spin(1,3) × Spin(6,4))/diag(Z₂)`, exact (`k77a:203-205`) | **SAME** |
| **A8** | the gauge group is the **stabilizer of the vacuum**, not merely a contained subgroup | conditional stabilizer theorem exists: given a rank-one `v_PSB`, the identity-component stabilizer has Lie algebra `su(3) ⊕ su(2)_L ⊕ u(1)` (`cycle1:28-34`) | **NEEDS-U1** — this is the row that gates A1, A2, A6 |

**Sub-cluster reading.** Nothing here is over-determined; the gauge sector is
*under*-determined in a precisely measurable way: one unknown (`U1`) and one
extra `U(1)`, with the containment arithmetic exact and the selection absent.
The 33/73 counts are new here and are the honest size of the "extra gauge boson"
obligation.

---

## 3. Fermion content per generation (9 rows)

All nine computed from the `16` of `so(10)` by exact weight arithmetic. The
computed `(Y, T3L, Q)` multiset reproduces the K77-A all-left charge dictionary
(`lab/process/resolver-wave-k77a-atomic-particle-crosswalk.json`,
`fermion_charge_dictionary`) **exactly**, as an equality of multisets.

| # | SM REQUIRES | GU DETERMINES | VERDICT |
|---|---|---|---|
| **B1** | `Q_L = (3,2,1/6)` | 6 states, `Y = 1/6`, `T3L = ±1/2`, `Q = 2/3, −1/3` | **SAME** |
| **B2** | `u^c = (3̄,1,−2/3)` | 3 states, `Y = −2/3`, `Q = −2/3` | **SAME** |
| **B3** | `d^c = (3̄,1,1/3)` | 3 states, `Y = 1/3`, `Q = 1/3` | **SAME** |
| **B4** | `L_L = (1,2,−1/2)` | 2 states, `Y = −1/2`, `Q = 0, −1` | **SAME** |
| **B5** | `e^c = (1,1,1)` | 1 state, `Y = 1`, `Q = 1` | **SAME** |
| **B6** | *nothing* — `ν_R` is optional in the SM | 1 state, `(1,1,0)`, and it is **forced**: the `16` has no room to omit it ("forced by the Pati–Salam structure; it is not inserted by hand", `canonical-structures-14d-metric-geometry-2026-06-22.md:274`) | **DIFFERS(ν_R forced)** — GU predicts a right-handed neutrino the SM does not require. Geometric reason: `16` is an irreducible spinor; you cannot delete one weight from it |
| **B7** | fermions are in **fundamental-type** reps, not the adjoint | fermions live in the spinor `S`, gauge fields in `Λ²V`. Structurally distinct carriers; the `16` is not a sub-rep of the 45 | **SAME** |
| **B8** | matter fills exactly `15` (or `16`) Weyl states per family, no more | one all-left `16`, and the six SM irreps each appear with **multiplicity exactly one** (computed) | **SAME**, and this multiplicity-one fact is load-bearing for §5 |
| **B9** | the family is a **single** irreducible unit under the unifying group | the `16` is irreducible under `so(10)`; the SM's six unrelated multiplets are one object | **SAME** — this is the strongest structural agreement in the cluster |

**Fence, carried, not softened.** These are *carrier-support* results
(`C2_EXACT_K77_CARRIER_SUPPORT`), not emitted particles. "`u_L` and `d_L` reach
`C2_EXACT_K77_CARRIER_SUPPORT`; they are not yet emitted particles"
(`k77a:301-302`). And "containment ≠ action-selected"
(`science-council-program-efficiency-2026-08-04.md:345`). The rows above are
about representation content only, which is exactly this cluster's remit.

**Second fence.** The `pati-salam-chain-verification.md` Step-0 `R128 → C64 →
Weyl C32` story is superseded (`k77a:280-283`). This ledger imports **only** the
internal `D5`/PS/SM branching — checks 9–19 of that file's 19 — and re-derives
all of it independently from weights, so none of the three superseded checks is
load-bearing here.

---

## 4. Hypercharge and anomaly conditions (6 rows) — the constraint-surplus block

| # | SM REQUIRES | GU DETERMINES | VERDICT |
|---|---|---|---|
| **C1** | six hypercharge values `{1/6, −2/3, 1/3, −1/2, 1, 0}`, fitted by experiment, derived by nobody | all six from `Y = T3R + (B−L)/2` — **one formula, zero free parameters**. And the line is unique: `Y = a·T3R + b·(B−L)` reproduces the SM multiset for exactly two `(a,b)`, which the `SU(2)_R` Weyl element conjugates into each other → **ONE physical hypercharge line, 0 bits** (`channel-swing-CH-SM:69-76,240`) | **SAME** |
| **C2** | `grav²·U(1)`: `Tr Y = 0` | `Tr Y = 0`, exact | **SAME** |
| **C3** | `[U(1)]³`: `Tr Y³ = 0` | `Tr Y³ = 0`, exact | **SAME** |
| **C4** | `[SU(3)]²·U(1)`: `Σ Y` over coloured states `= 0` | `= 0`, exact | **SAME** |
| **C5** | `[SU(2)]²·U(1)`: `Σ Y` over doublets `= 0` | `= 0`, exact | **SAME** |
| **C6** | Witten global `SU(2)` anomaly: even number of doublets | **4** doublets per family, even | **SAME** |

**Constraint surplus, counted rather than argued** (the doctrine, verbatim, is
"posits with more independent constraints than free parameters are informative;
count, don't argue", `lab/process/CURRENT-RESEARCH-CONTEXT.md:24-26`):

```text
    independent constraints satisfied : 11   (6 hypercharges + 5 anomaly conditions)
    free parameters available         :  0   (the hypercharge line is unique mod Weyl)
    surplus                           : +11
```

**Two honesty notes, both required.**

*(i)* This is the **standard** `SO(10) ⊃ PS ⊃ SM` embedding. It is textbook
(`pati-salam-chain-verification.md:212-218`: "the paper has correctly re-derived
the well-known … embedding … that geometric origin is *not* what is tested
here"). The surplus is real and it is not GU's alone — it is the surplus that
"is most of why anyone takes grand unification seriously" (`AGENTS.md:86-88`).
What is GU-specific is only that the `16` arrives from a metric-bundle
observation split rather than being posited.

*(ii)* The **absolute** normalization `Tr(Y²)/Tr(T3L²) = 5/3`, i.e.
`sin²θ_W = 3/8` at the unification boundary, is filed **BOUGHT**
(`per-leg-recovery-state-2026-07-21.md:55`;
`comparative-tensions-ledger-particle-qm-2026-07-21.md:263`). Rows C1–C6 concern
*relative* charge assignments, which are free of that purchase. The `3/8` is not
counted in the surplus above.

---

## 5. Chirality (4 rows) — including the OVER-DETERMINED row

### 5.1 The rows

| # | SM REQUIRES | GU DETERMINES | VERDICT |
|---|---|---|---|
| **D1** | the light spectrum is **chiral**: no gauge-invariant Dirac mass exists for it | the K77 parent is **exactly vectorlike**. `PH-K1-KINEMATIC = CONFIRMED`, `64+64`, both real forms, all five allocations, joint grading `32/32/32/32` (`chirality-grading-and-77-rerun-2026-08-03.md:190,194-201`). Net 4D chiral asymmetry **0** | **DIFFERS(net chirality 0 vs 3)** — stated at kinematic grade only; `PH-K1-PHYSICAL` stays `OPEN/BLOCKED` |
| **D2** | a mechanism producing chirality at low energy | ~~the source's stated mechanism — VEV/low-curvature mass decoupling into chiral sectors — **cannot work**. See §5.2~~ **[CORRECTED 2026-08-17, see CORRECTION V1 at end of file:** the exclusion binds a `G_SM`-preserving **mass** mechanism on the conjugate-pair (`16 ⊕ 16bar`) comparator carrier; the source's stated mechanism is the VEV-conditional **decoupling** typed as SG4 bit 2 (ST-1), on the four-corner carrier (CR-B), with dark-sector partner placement (SC-FER-03), and is **NOT excluded by this row**. The Schur argument of §5.2 survives intact at comparator-carrier scope. See §5.2.**]** | **OVER-DETERMINED** |
| **D3** | no same-chirality Majorana mass for charged states | `dim Hom(S⁺⊗S⁺, Λ⁰) = 0` for **every** `(p,q)` with `p+q ≡ 2 mod 4` (M-M1, `signature-independent-scalar-vanishing-lemma-2026-08-03.md:35-43`). Independently reconfirmed here at `so(10)` level: `16 ⊗ 16` contains **zero** singlets (computed; `16×16 = 10+120+126`, singlet-free) | **SAME** — GU forbids exactly what the SM forbids, for a structural reason, fork-independently |
| **D4** | no light mirror partners | the invariant form `B` anticommutes with `ω₁₄`, so same-half pairings **vanish** and opposite-half pairings are **perfect** (`k77a:193-201`). One ambient half carries an all-left `16`, the other an all-left `16bar` — an exact mirror | **NEEDS-U3** — and §5.2 shows `U3` cannot discharge it by mass |

### 5.2 Row D2, in full — the OVER-DETERMINED row

**The claim, stated precisely and narrowly.**

> No `G_SM`-preserving vacuum, in any channel, at any magnitude, can produce a
> chiral low-energy spectrum from the K77 vectorlike parent by a **mass**
> mechanism.

**The proof.** Four facts, each already in the repo, composed for the first time.

1. The two ambient halves carry, all-left, exactly `16` and `16bar` — conjugate
   reps. (From the K77-A observation branching `k77a:41-50`: `S^C_{14,+} =
   (S_{4,+}⊗S_{10,+}) ⊕ (S_{4,-}⊗S_{10,-})`; a right-handed Weyl in `16bar` is a
   left-handed Weyl in `16`; the Majorana–Weyl reality condition halves the
   complexification. Checked.)
2. The invariant pairing `B` is **perfect** between the halves and **zero**
   within a half (`k77a:193-201`). So every zero-order mass term is a
   **cross-half Dirac** pairing `16 ⊗ 16bar → singlet`, of multiplicity one.
   (Checked: `16 ⊗ 16bar` zero-weight multiplicity 16 = 1 + 5 + 10, i.e. the
   singlet is present; `16 ⊗ 16` zero-weight multiplicity 0, i.e. absent — the
   `so(10)`-level shadow of M-M1.)
3. Under `G_SM`, the `16` decomposes into six irreps **each of multiplicity
   exactly one** (row B8, computed). Therefore a `G_SM`-equivariant mass
   operator `M : 16 → (16bar)*` is Schur-diagonal with exactly **six** complex
   parameters `m_Q, m_u, m_d, m_L, m_e, m_ν`.
4. Each `m_i` is the coefficient of a **Dirac** mass term. A Dirac mass gives
   mass to *both* ends of the pair. There is no parameter that lifts the mirror
   state while leaving its visible partner massless, **because they are the same
   Dirac fermion.**

Hence: `m_i = 0` leaves the visible state *and* its mirror massless (vectorlike,
no chirality); `m_i ≠ 0` lifts *both* (nothing light at all). The six-parameter
family contains no chiral point. There is no third option, and enlarging the
channel does not help — the argument is about the *kernel structure* of a
`G_SM`-equivariant map between conjugate reps, not about which VEV was used.

**Why this is OVER-DETERMINED rather than merely a kill.** The lane's test is:
do the SM's requirements, propagated back onto the unknown, admit *any*
admissible value? For `U3` restricted to the source's declared mechanism, the
answer is **no** — not "not yet", but *no value exists*. The SM's chirality
requirement and GU's stated chirality mechanism are jointly unsatisfiable.

**Exactly what dies, and exactly what does not.** Using the K77-A kill ladder
(`k77a:126-131`), this is a **`MECHANISM_KILL`**, not a lane or program kill:

| killed | surviving |
|---|---|
| chirality from a `G_SM`-preserving **mass/VEV** decoupling — the mechanism at `M-EFFECTIVE-CHIRALITY`, `SOURCE_GUIDED_CANDIDATE`, and at TOE `02:36:02-02:38:12` | an **index / zero-mode** mechanism (a Dirac operator of nonzero index; not a mass matrix) |
| the reading of "in regions of low scalar curvature the non-chiral theory decouples into chiral sectors" as a *mass* statement | a vacuum that **breaks** `G_SM` at the relevant scale (but then the light states are not SM reps, and the row must be re-asked) |
| | a parent in which the two halves are **not** conjugate reps — i.e. a different `U2` |

**Where this lands against what is already filed.** The repo currently grades the
effective-chirality claim `NARRATIVE`, "the *weakest* of GU's proposed exits …
stated narratively, without a decoupling theorem, without an energy scale
estimate" (`papers/drafts/no-go-class-relative-survey.md:148`, `:381`, `:384`).
Row D2 says something strictly stronger and different in kind: **there cannot be
a decoupling theorem of the mass type.** "Under-specified" becomes "excluded in
the stated class."

It is also consistent with — and sharpens — three banked statements:
`README.md:41-45` ("intrinsically vectorlike and cannot force its own chirality
… from inside; on present evidence the count is **external**");
`canon/six-axis-candidate-krein-positivity-dg.md:137` ("net chiral asymmetry
`0`"); `canon/ghost-parity-krein-synthesis.md:57` ("three generations plus three
mirrors"). D2 upgrades "cannot force from inside" to "the specific stated escape
route is closed", and it explains *why* the count must be external: not because
nobody has computed it, but because the only remaining carrier of chirality is
an index, and an index is a topological datum the interior provably cannot
supply (`M-C4`: spin Dai–Freed inflow has no 3-primary column;
`improvement-register-2026-08-03.md:147`).

**Gate discipline, non-negotiable.** This row is **PRE-DEPOSIT** and
**J5-gated**. It moves no claim, verdict, bar, canon entry, count, LANE-STATE
row, or fork-registry row today. Adopting it as a status change on the
effective-chirality axis requires a filed hostile field-specialist review with
the two-sided charge — **overclaim and over-fencing both**. The over-fencing
charge is real and should be pressed: a reviewer should test whether step 3's
`G_SM`-equivariance assumption is doing more work than stated, and whether a
vacuum that breaks and then restores `G_SM` at different scales evades the
Schur argument.

---

## 6. Higgs sector (7 rows)

| # | SM REQUIRES | GU DETERMINES | VERDICT |
|---|---|---|---|
| **E1** | Higgs is `(1,2,1/2)`, a colour-singlet weak doublet | present in `V₁₀` (exactly one `(1,2,+1/2)` and one `(1,2,−1/2)`), and in `Sym²(S) = 10 ⊕ 126` (two of each) — the repo's stated offer, the PS bidoublet `(1,2,2)` (`sm-gauge-higgs-...-2026-06-24.md:122-131`) | **NEEDS-U4** (`hosted_open`) |
| **E2** | Higgs is a **Lorentz scalar** | in an adjoint-valued one-form `Ω¹(Y, ad)`, ~~the Lorentz-scalar components are exactly those with **both** legs vertical:~~ **[CORRECTED 2026-08-17, see CORRECTION V2 at end of file: no component descends to a 4D scalar at all — the source-declared observation reduction is a CONTRACTION sending every ad-valued one-form to exactly ONE 4D one-form (MD-1, VZ-4, canon-integrated), and "vertical legs ⇒ 4D scalars" is the Kaluza-Klein reading the source disavows; the tensor decomposition and its multiplicities here survive as exact algebra on Y]** `V*₁₀ ⊗ Λ²V₁₀ = 10 ⊗ 45`, which hosts **6** `(1,2,+1/2)` and 6 `(1,2,−1/2)` (computed). Any component with a spacetime form leg is a **vector**, not a scalar | **NEEDS-U4**~~, with an exact shape constraint: the Higgs must consume the vertical form leg. This is a tightening act of type (iii)~~ **[shape constraint WITHDRAWN 2026-08-17, see CORRECTION V2 at end of file — under the corrected observation map there is no vertical-leg 4D scalar to consume, so the constraint and its type-(iii) tightening claim are withdrawn together]** |
| **E3** | *(the exclusion)* — | **`Λ²V₁₀` (45) and `Sym²V₁₀` (1+54) contain ZERO colour-singlet weak doublets, at any `Y`.** The whole 100-dimensional rank-two internal tensor class is excluded as a Higgs parent | **DIFFERS(class exclusion)** — see §6.1 |
| **E4** | exactly **one** complex doublet: 4 real dof → 3 eaten + 1 physical | every surviving GU channel supplies an **even number ≥ 2**: `V₁₀` → 2, `Sym²(S)` → 4, `10⊗45` → 12. A single doublet is representation-theoretically **unavailable**, because the underlying real/self-conjugate structure pairs `Y=+1/2` with `Y=−1/2` | **DIFFERS(multi-doublet forced)** — GU is a ≥2HDM at minimum; `U4` must give large mass to all but one combination |
| **E5** | **no** light colour-triplet scalar (proton decay) | the `(1,2,±1/2)` in `V₁₀` sits in the **same irreducible 10** as a `(3,1,−1/3)+(3̄,1,1/3)` pair (the remaining 6 states, computed) | **DIFFERS(doublet–triplet splitting)**, inherited in full; and **conditionally OVER-DETERMINED**: if `U4`'s scalar mass term is exactly `so(10)`-invariant, doublet and triplet are **degenerate by Schur**, and a Higgs-light doublet forces a Higgs-light triplet — excluded by proton decay by ~13 orders of magnitude. Named trigger, not asserted |
| **E6** | Higgs breaks `SU(2)×U(1) → U(1)_em` leaving a massless photon | the stationary orbit, the `4 → 3+1` mode split, and the photon kernel are all open (`k77a:328`; `OPEN_PHOTON_KERNEL`) | **NEEDS-U4** |
| **E7** | Yukawa couplings exist: `16 · 16 · H` | `16 ⊗ 16 = 10 ⊕ 120 ⊕ 126`. The `10` is present, so the Yukawa channel exists — and note it is the **same 10** that carries the doublet in E1 | **NEEDS-U4** (channel exact, coupling unselected) |

### 6.1 Row E3 — the class exclusion, and a Layer-0 homonym it exposes

**The theorem.** For every rep `R` in the rank-two internal tensor class,
`m(R) = 0`:

```text
    Λ²V10  = 45   (the so(10) adjoint)         (1,2,±1/2) x 0
    Sym²V10 = 55  = 1 + 54  (the metric slot)  (1,2,±1/2) x 0
    ------------------------------------------------------------
    V10 (x) V10 = 100 complex dimensions       (1,2,±1/2) x 0
```

Contrast, same computation, same conventions:

```text
    V10          = 10    (1,2,+1/2) x 1     Λ³V10 = 120  x 2
    Sym²(S16)    = 136   (1,2,+1/2) x 2     Λ⁴V10 = 210  x 1
    10 (x) 45    = 450   (1,2,+1/2) x 6     Λ⁵V10 = 252  x 2
```

**Consequence 1 — the metric-slot Higgs route dies a second, independent time.**
The repo already records `P-54-WELD` as **failed** — "the direct native `θ` (one
vertical 10) → conventional `Sym²₀(10)=54` weld fails"
(`per-leg-recovery-state-2026-07-21.md:55`). Row E3 shows that **even if the weld
had succeeded**, the `54` could not have hosted the Higgs: it has zero doublets.
Two independent kills of one route is a genuine class exclusion, not a repetition.

**Consequence 2 — "the adjoint" is a Layer-0 homonym, and the Higgs row is
exactly where it bites.** Two different objects in this repo are called *the
adjoint*:

| object | dimension | restricted to `so(10)` | `(1,2,+1/2)` content |
|---|---|---|---|
| `ad(so(14)) = Λ²V₁₄` — the curvature/metric-connection adjoint | 91 → 45 internal | `45` | **0** |
| `ad(Sp(64))` — the spinor-bundle gauge adjoint (`Sp(64)` is "the natural gauge group", `canon/shiab-existence-cl95.md:74`) | → `Sym²(16) = 136` | `10 ⊕ 126` | **2** |

They **differ on the Higgs row**. This is a load-bearing homonym: a sentence of
the form "the Higgs comes from the adjoint-valued one-form" is *true* for one and
*false* for the other. Typed here as a **UNCERTAIN → now DISTINGUISHED** pair;
the distinguishing observable is `m(R)`.

**Cross-validation, independent and exact.** The computation reproduces, from
scratch, a detail the repo had already noticed and flagged as an open
disambiguation: the ledger at
`sm-gauge-higgs-finite-control-extraction-ledger-2026-06-24.md:133-140` asks for
"a disambiguation of the extra SM-level `(1,2,+1/2)` contribution coming from the
`SU(3)`-singlet inside `(15,2,2)`". This probe finds `Sym²(16)` hosts **exactly
2**, splitting as **1 from the `10` and 1 from the `126`** — and the `126`'s one
doublet is precisely the `SU(3)`-singlet of `(15,2,2)`. The open disambiguation
is answered as an arithmetic fact: **the two are one from each irreducible
summand.**

---

## 7. Generations and beyond-SM content (7 rows)

| # | SM REQUIRES | GU DETERMINES | VERDICT |
|---|---|---|---|
| **F1** | exactly **3** generations | not derived. "three is NOT derived (the external index is any integer, nothing privileges 3)" (`CANON.md:176`). Receptacle exhibited at **T4**; **the slot is empty** (`Im J₁₃ = 0`) | **NEEDS-U5** |
| **F2** | generations are exact **replicas** — identical rep content, differing only in mass | if multiplicity arises by repeating the `16`, replication is automatic and exact. ~~GU has no mechanism that could make generation 2 differ in rep content~~ **[CORRECTED 2026-08-17, see CORRECTION V5 at end of file: the source's own construction names exactly such a mechanism — the third family is an imposter "for representation theoretic reasons", and HE-1 makes the partition FORCED and SUBTRACTIVE with the family-shaped block inside the 144 a MIRROR; generations are not exact replicas at the fundamental layer and look the same only at low energy. The corrections also falsify this row's antecedent at the fundamental layer; the verdict survives at the effective/low-energy layer]** | **SAME** (conditionally on `U5` acting by multiplicity) |
| **F3** | *(the killed route)* | "three kinematic blocks ⇒ three observed generations" is **killed at Layer 0** — a `HOMONYM`, false-escape direction (`layer0-pass-on-the-2plus1-count-claim-2026-07-29.md:19`; `k77a:297`) | **DEAD ROUTE**, recorded so it is not re-derived. **This ledger does not count 16-blocks and compare to 3.** |
| **G1** | no spin-3/2 fields | the DRS carrier forces them: per ambient half `F=64, Q=192, Z=576`, sum 832 (`k77a:239-246`). `Q`/`Z` are RS-shaped. Any chiral use owes the **Witten-1983 exit** (`layer0-fork-registry.yaml:130-131`) | **DIFFERS(spin-3/2 sectors forced)** — geometric reason: the carrier is `Ω⁰ ⊕ Ω¹(Y,S)`, and `ker Γ` is not optional. **NEEDS-U3** to decouple |
| **G2** | no mirror sector | forced by `B`'s perfect cross-half pairing (row D4) | **DIFFERS(mirror forced)**; and by §5.2 `U3` **cannot** remove it by mass [scope re-set 2026-08-17 per CORRECTION V1 at end of file — "cannot remove by mass" binds the conjugate-pair comparator carrier; the source's stated exit is not a mass mechanism but the SG4-bit-2 decoupling with partner placement, a scope correction, not a refutation] |
| **G3** | Majorana `M_R` for `ν_R` (if seesaw) | no `Λ⁰` channel exists (M-M1, row D3), so `M_R` **must** come from an odd-form channel. `Λ⁵ = 252` contains exactly **2** SM singlets `(1,1,0)` — one in `126`, one in `126bar` (computed), matching `126⁻ ⊃ (10,1,3)` (`resolver-wave-c-rebased-q5-q6-mh7-2026-08-03.md:190-196`) | **NEEDS-U3**, at grade **T2**, unmoved. Channel located, VEV not earned. `K2` open, `M-M22` open |
| **G4** | no extra light `U(1)` beyond `U(1)_Y` | the 45 has exactly **2** SM-singlet directions (row A4); the second must be broken | **NEEDS-U1** |

---

## 8. What this lane did that a forward wave could not

Not one row below required the source action to be built. All were obtained by
taking the SM as *given*, the source action as *unknown*, and propagating
backwards.

| tightening act (council §1 taxonomy) | instance |
|---|---|
| **(ii)** excludes a **class**, not a candidate | the 100-dimensional rank-two internal tensor class, excluded as a Higgs parent by one weight-lattice theorem (E3) |
| **(ii)** again | the mass-type realization of effective chirality, excluded by Schur (D2) |
| **(iii)** adds a **shape constraint** the supplier must satisfy | ~~the Higgs must consume the vertical form leg (E2)~~ **[withdrawn 2026-08-17 with E2's shape constraint, see CORRECTION V2 at end of file]**; `M_R` must come from an odd-form channel (G3) |
| **(v)** converts an assumption into a **derived** fact | the `(15,2,2)` doublet disambiguation, open since 2026-06-24, answered as arithmetic (§6.1) |
| **(i)** reduces free-parameter count | `U3` restricted to `G_SM`-equivariant masses has exactly **6** parameters, and the chiral subset of that 6-parameter family is **empty** (D2) |

**Residue tuple for this wave**, in the council's §5.5 form:

```yaml
residue_count: 5                       # U1, U3, U4, U5, and the banked M-H7 slot
residue_tightness: {T4: 1, T3: 3, T2: 1, T1: 0, T0: 0}
free_object_delta: 0
```

`CB-A-U3-CHIRALITY-MECHANISM` moved **T1 → T3** (previously "a decoupling
theorem", named and untyped; now typed *and* carrying an exact class exclusion).
`CB-A-U4-HIGGS-CHANNEL` enters at **T3** (type + dimension fixed + a
100-dimensional class excluded). `SU4C-SEESAW-MR` is **unmoved at T2**, as filed.
`M-H7` is **unmoved at T4**.

---

## 9. Honest limits of this artifact

- **This is representation theory, not dynamics.** No action, vacuum, pole,
  residue, propagator, positivity statement, or physical domain is constructed
  or claimed. Every "SAME" verdict is a statement about *representation content*
  and inherits the standing `containment ≠ action-selected` fence.
- **The `16` is imported, not derived from the real carrier.** K77-A imports the
  `D5`/PS packet rather than deriving its weights from `Cl(7,7)`
  (`k77a:55-63`). This ledger re-derives the branching independently but does
  not close that gap.
- **Row D2's scope is the mass mechanism only.** It does not kill the `(7,7)`
  lane, GU, or effective chirality by any other route. Read §5.2's kill table
  before citing it.
- **Row E5's over-determination is conditional** on a trigger that is not
  currently known to hold. It is named so it can be tested, not asserted.
- **The three-column axis is terms + representations + anomaly conditions**, per
  the council's ruling (`science-council-program-efficiency-2026-08-04.md:337-339`);
  no row here is a particle, and no row here has a pole. The K77-A 37-row
  particle census remains the separate downstream *acceptance* surface and is
  not duplicated.
- **No claim-status movement.** `claim_status_change: none`. Pre-deposit,
  J5-gated.

---

## Reproduction

```bash
python3 tests/channel-swings/cb_a_representation_content_probe.py
```

`45 exact checks, 45 passed, 0 failed.` Exact `Fraction` arithmetic throughout;
no float is load-bearing on any asserted claim, so P-H29 is satisfied by
construction. Colour-singlet multiplicities are extracted by the `su(3)` Weyl
alternating sum, not by weight inspection. The probe reproduces, as internal
controls, three results filed independently elsewhere in the repo: the K77-A
all-left charge dictionary (exact multiset equality), the singlet-free
`16 × 16 = 10 + 120 + 126` decomposition, and the `126 ⊃` one-SM-singlet
Majorana slot.

---

*Filed 2026-08-05. Conditional-build lane, Cluster A. Pre-deposit, J5-gated.
Creates no claim, verdict, grade, priority, lane movement, count, bar, canon
entry, external-datum entry, or public posture. Nothing is committed. Row D2's
verdict-adjacent consequence is gated on a filed hostile field-specialist review
with a two-sided charge.*

---

# CORRECTIONS V1, V2, V5 (2026-08-17, applied from the SCUR-1 source-currency audit)

**What forced this.** The systematic currency audit SCUR-1
(`lab/active-research/joe-directed/source-currency/scur1-source-currency-audit-2026-08-17.md`,
every quote byte-matched by
`tests/channel-swings/joe_directed_scur1_source_currency_audit.py`, 52/52)
found that this file's three steps from algebra into *source attribution* —
written 2026-08-05, a week before the source layer was pinned — violate the
2026-08-14..17 correction register. The algebra needed zero repair anywhere;
these blocks correct attribution and typing sentences only, append-only,
original text struck in place, matching CB-E's E3/E9 pattern. Application
record: `lab/active-research/joe-directed/source-currency/fixa-cb-corrections-applied-2026-08-17.md`.

**Kill-typing carried, not re-minted.** These corrections quote SCUR-1's
claim-indexed typing (its `target_claim`: INTERNAL — the exact sentences below,
no registered GU source claim attacked; its `target_claim_verdict`: SPLIT — the
five sentences violate corrections, the mathematics under them is banked). No
new kill is asserted here and no verdict cell token moves; D2 stays
OVER-DETERMINED at its corrected scope, F2 stays SAME under its conditional,
E2 stays NEEDS-U4.

## CORRECTION V1 (2026-08-17) — the excluded-mechanism attribution (title clause, `source_collision`, row D2; G2 re-scoped)

- **Withdrawn** (struck in place above): the title clause "the source's own
  stated effective-chirality mechanism is excluded by Schur, not merely
  unbuilt"; the frontmatter token
  `SOURCE-CONTRADICTED-ON-VEV-INDUCED-EFFECTIVE-CHIRALITY`; row D2's middle
  column "the source's stated mechanism — VEV/low-curvature mass decoupling
  into chiral sectors — **cannot work**".
- **Corrected by** register items 6, 3 and 8 of SCUR-1 §1: the source's stated
  mechanism is a VEV-conditional DECOUPLING — "exactly three families of chiral
  fermions if you have a decreased VEV in the total space taking a Dirac
  equation into two [Weyl] equations because the mass is actually a variable"
  (`papers/drafts/Transcript into the impossible.md:158`) — on the four-corner
  carrier (CR-B,
  `lab/active-research/joe-directed/carrier/crb-carrier-is-four-corners-not-one-weyl-2026-08-15.md`),
  with partner sectors the source names as dark (SC-CHI-01, SC-FER-03), and
  with the selector typed as SG4 bit 2 (ST-1,
  `lab/active-research/joe-directed/seesaw-tradeoff/st1-tradeoff-dissolves-into-sg4-bit-2-2026-08-16.md`),
  OPEN by design. D2's proof models a `G_SM`-preserving mirror-LIFTING mass on
  a conjugate-pair (`16 ⊕ 16bar`) carrier and rejects the m→0 branch as
  "vectorlike, no chirality" — but the m→0 branch IS the source's claimed
  phase, with the mirror handled by partner placement that register 8 types as
  an obligation, not an established defect. No corpus locus states a
  mirror-lifting mass mechanism (SCUR-1 checked SC-CHI-01, SC-CHI-02, and the
  TOE 02:36–02:38 decoupling sentence: all are decoupling statements). The
  source's stated mechanism is therefore **NOT excluded by this row**.
- **What survives, explicitly banked:** the §5.2 Schur argument itself (the
  six-parameter `G_SM`-equivariant mass family on `16 ⊕ 16bar` has no chiral
  point) is exact and stands as a comparator-carrier theorem; D2's own kill
  table already lists the exits the corrections instantiate ("a parent in which
  the two halves are **not** conjugate reps — i.e. a different `U2`"). G2's
  "cannot remove it by mass" inherits this scope correction, not a refutation.
- **Established by** SCUR-1 §3 V1 (severity HIGH), with the register owners
  named above; underlying gate evidence byte-matched by SCUR-1's probe.
- **Consumers named for the mint (Arc-1), not edited here:** the residue claim
  "`CB-A-U3-CHIRALITY-MECHANISM` moved T1 → T3" (its T3 rests on the class
  exclusion binding the source mechanism, which it no longer does at the
  source layer); the `deposit_gate` framing ("would move the effective-chirality
  axis to 'excluded in the stated class'"); "Result first" item 3 and §5.2's
  kill-table row naming `M-EFFECTIVE-CHIRALITY` / TOE 02:36:02–02:38:12, which
  now read at comparator-carrier scope; any v0.258 ledger row grounded on
  D2/G2 (SCUR-1 §6 item 1).

## CORRECTION V2 (2026-08-17) — the vertical-form-leg = Lorentz-scalar identification (row E2; §8 row (iii))

- **Withdrawn** (struck in place above): E2's "the Lorentz-scalar components
  are exactly those with **both** legs vertical" typing, E2's verdict-cell
  shape constraint "the Higgs must consume the vertical form leg" with its
  type-(iii) tightening claim, and §8 row (iii)'s restatement of that
  constraint.
- **Corrected by** register item 7 of SCUR-1 §1: the source-declared reduction
  is a CONTRACTION that sends every ad-valued one-form on Y¹⁴ to exactly ONE
  4D one-form — no component descends to a 4D scalar at all — and the
  geometric fibre `Sym²(T*X⁴)` is not Lorentz-inert (MD-1,
  `lab/active-research/joe-directed/four-d-mode-decomposition/md1-form-leg-survives-ad-leg-is-untyped-2026-08-14.md`;
  VZ-4, `lab/active-research/joe-directed/vz-repair/vz4-pullback-is-a-contraction-2026-08-15.md`,
  canon-integrated, V3c killed in every gauge). "Vertical form leg ⇒ observed
  Lorentz scalar" is the Kaluza-Klein reading the source disavows ("It's not
  Kaluza Klein.", drafts L29). The contraction formula
  `(s*ψ)_μ = ψ_μ + ψ_(ab) ∂_μ g_ab` feeds vertical content into the 4D
  ONE-FORM slot, so the withdrawn constraint was arguably backwards.
- **What survives, explicitly banked:** the branching multiplicities (6
  doublets in `10 ⊗ 45`, zero in the whole rank-two class — row E3 is
  untouched) are exact algebra on Y. Fork nuance recorded by SCUR-1: on MD-1's
  INERT-AD horn the ad-leg inertness could be restored, but E2's scalar typing
  rides the FORM leg, whose contraction answer is horn-independent.
- **Established by** SCUR-1 §3 V2 (severity HIGH).
- **Consumers named for the mint, not edited here:**
  `CB-A-U4-HIGGS-CHANNEL`'s T3 grade ("type + dimension fixed"); CB-B SM-6's
  bridge setting (corrected as V3 in that file); any Higgs-channel search that
  filters candidates by vertical-leg consumption (SCUR-1 §6 item 2).

## CORRECTION V5 (2026-08-17) — "GU has no mechanism that could make generation 2 differ in rep content" (row F2)

- **Withdrawn** (struck in place above): the quoted absolute clause.
- **Corrected by** register item 5 of SCUR-1 §1: the source's own construction
  names exactly such a mechanism — the third family is an imposter "for
  representation theoretic reasons" (drafts L119), it is "really two plus one"
  (L128), and HE-1
  (`lab/active-research/joe-directed/high-energy-two-plus-one/he1-imposter-separation-invariant-2026-08-14.md`)
  makes the partition FORCED and SUBTRACTIVE (n_g → n_g − 1, unlabelled, the
  distinguished family REMOVED), with the family-shaped block inside the 144 a
  MIRROR. Generations are not exact replicas at the fundamental layer; they
  look the same only at low energy.
- **What survives, with SCUR-1's two-sided caveat quoted rather than
  smoothed:** the row's verdict "SAME (conditionally on `U5` acting by
  multiplicity)" survives at the effective/low-energy layer; the corrections
  falsify the row's ANTECEDENT ("multiplicity arises by repeating the `16`")
  at the fundamental layer rather than only the clause. SCUR-1 typed this
  VIOLATES on the plain reading because the clause is written as an absolute
  GU-side fact, and records it as its closest call: "a reviewer who weighs the
  row-context fence more heavily than the plain reading would downgrade it to
  PREDATES-BUT-CONSISTENT-WITH-SUPERSEDED-ANTECEDENT" (SCUR-1 §4). Severity
  LOW.
- **Established by** SCUR-1 §3 V5.
- **Consumers named for the mint, not edited here:** any replica-exactness
  citation from F2 (flavour-universality arguments are the natural consumers);
  ledger generation rows citing CB-A §7 (SCUR-1 §6 item 4).

*Corrections applied 2026-08-17 by FIX-A (application record and probe:
`lab/active-research/joe-directed/source-currency/fixa-cb-corrections-applied-2026-08-17.md`,
`tests/channel-swings/joe_directed_fixa_cb_corrections.py`). Append-only: no
original sentence deleted, no verdict cell token, grade, count, bar, canon
entry, registry row, or ledger row moved. The struck sentences remain readable
above so that SCUR-1's byte-matched evidence probe remains verifiable.*
