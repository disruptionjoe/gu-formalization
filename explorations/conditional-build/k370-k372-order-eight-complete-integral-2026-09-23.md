---
title: "K370--K372 order-eight complete integral enclosure"
status: exploration
claim_verdict: internal_structural_only
date: 2026-09-23
claim_ceiling: exact disjoint owner and positive-interior hybrid majorants, complete eighteen-axis Peano remainder bound, and rigorous complete order-eight conditional integral enclosure; sign, order nine, action-column, residual and K152 consequences remain open
manifests:
  - lab/process/k370-order-eight-disjoint-owner-hybrid-majorants.json
  - lab/process/k371-order-eight-complete-peano-remainder.json
  - lab/process/k372-order-eight-complete-integral-enclosure.json
producers:
  - tests/channel-swings/k370_order_eight_disjoint_owner_hybrid_majorants.py
  - tests/channel-swings/k371_order_eight_complete_peano_remainder.py
  - tests/channel-swings/k372_order_eight_complete_integral_enclosure.py
probes:
  - tests/channel-swings/k370_order_eight_disjoint_owner_hybrid_majorants_probe.py
  - tests/channel-swings/k371_order_eight_complete_peano_remainder_probe.py
  - tests/channel-swings/k372_order_eight_complete_integral_enclosure_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: COMPLETE_ORDER_EIGHT_CONDITIONAL_INTEGRAL_FINITE__SIGN_AND_DOWNSTREAM_ACTION_COLUMN_OPEN
canon_verdict_change: none
---

# K370--K372 order-eight complete integral enclosure

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: these results bind only the repository-owned conditional order-eight
positive-Fock coefficient integral. They do not construct a GU physical state,
interacting quotient, source-owned action, observable or K152 decision.

```gu-typed-objects
result: disjoint owner and positive-interior bounds for all eighteen order-eight Peano hybrids, their complete remainder sum, and a rigorous complete conditional integral enclosure
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), restricted to the K162 zero-bath seed orbits LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Gram pairing after complete normalized specieswise exterior projection ON=conditional K179 coefficient family
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering and real Bessel kernels
grading: conserved incidence charges, impurity seed, bath species, bath number eight, K179 contraction identity and ordered left/right momentum provenance
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W and K179 fixes the coefficient family; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: complete the order-eight conditional integral before constructing order-specific higher-rank calculus MAP-TYPE=evaluation
```

## Preflight bookend

K369 gives a finite whole-radial majorant for every reachable face program but
forbids summing the overlapping rows. K363 already partitions every positive
moving-time vector by its low-coordinate set and assigns a unique maximal
reachable face, or `INTERIOR`. The cheapest complete route is therefore to
restrict one K369 row to each owner union and bound the remaining interior
union analytically. Summing all 517 rows without the owner rule is rejected;
recursive raw Arb boxes are unnecessary once the scaled global constants and
per-hybrid singular degrees are composed.

## K370: disjoint owner and interior hybrid majorants

K370 replays all `524,286` low-coordinate subsets and every K363 owner digest.
The `517` distinct face owners each resolve to exactly one K369 row. A row is
charged once for the union of all cells carrying that owner, never once per
subset.

For a moving dimension `m`, let `M` be the cell maximum, `rho` the radial sum,
`epsilon=1/64`, and `S` the maximum second-order singular degree. On an interior
owner cell every reachable singular support meets a coordinate greater than
`epsilon*M`; since `rho<=mM`, its inverse scale costs at most
`(m/epsilon)^S rho^(-S)`. The active Peano factor contributes `rho^2`, and the
simplex Jacobian contributes `rho^(m-1)`, leaving radial power `m+1-S`. The
minimum is zero, so all gamma masses are finite. For the face-free terminal
hybrids `v8,v9`, every singular support meets a preceding coordinate fixed at
`1/256`; K370 therefore charges K349's complete-second scaled degree twelve by
`256^12`, leaves zero moving radial singular degree, and obtains radial powers
three and two. The finite first-maximum/low-high recursion has `271,491`
nonempty interior max cells.

## K371: complete Peano remainder

K371 follows K348's tensor identity in native axis order. It sums exactly one
complete coherent absolute hybrid upper for each of the eighteen axes; no
mixed derivatives and no node-jet substitution enter. The resulting raw
remainder upper is exact rational. It is deliberately enormous because each
owner union receives a global scaled majorant, but finiteness—not sign—is the
gate being closed.

## K372: complete integral join

K372 takes K345's outward one-node interval

```text
[3.1047016409619726e-35, 3.1047016409619737e-35]
```

and applies the outward native `(2*pi)^-10` upper exactly once to K371's raw
remainder. The complete conditional order-eight integral lies in

```text
[-5.90747806585151869e+680, 5.90747806585151869e+680].
```

This interval is rigorous but sign-indefinite. It closes the complete
order-eight finiteness obligation and releases construction of the rank-five
order-nine calculus; it supplies no useful K152 numerical margin.

## Postflight bookend

The strongest overclaim is that completing the integral also determines its
sign or action-column consequence; it does neither. The strongest contrary
seam is the terminal face-free pair, so K370 uses the scaled old-kernel fallback
from K349's degree twelve and the fixed-node lower bound rather than applying
the moving-support rule where K350 records no reachable face. The weakest numerical
seam is width: full owner-union bounds discard nearly all interior correlation.
That affects sharpness, not coverage or finiteness.

K370 passes `10/10` controls and rejects `11/11` hostile mutations; K371 passes
`9/9` and rejects `9/9`; K372 passes `10/10` and rejects `11/11`. Deterministic
regeneration and the focused predecessor regressions are byte-stable. No
source-register, physics-ledger, canon, paper, public or physical posture moves.
SC-META-53 remains `UNCERTAIN`; RA-F1, LT-SM8, LT-GR6b and AC-F1 remain
`NEEDS`.
