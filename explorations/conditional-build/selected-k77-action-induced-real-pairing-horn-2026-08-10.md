---
artifact_type: exact_conditional_action_pairing_and_surplus_result
created: 2026-08-10
status: SELECTED_SCALAR_TRACE_CLOSES_R_AND_HORN_COST_CONDITIONALLY__REALITY_OR_ORIENTATION_ALONE_DO_NOT__STRICT_SURPLUS_ZERO__P3_BRIDGE_PRIMARY
source_return: SOURCE_CONFIRMS_NORM_SQUARE_ARENA_AND_TWO_WEYL_HALVES__SOURCE_SILENT_SCALAR_TRACE_DESCENT_BRE_HORN_AND_P3_BRIDGE__SOURCE_FORK_ACTION_PARENT
ledger_rows: [LT-GR1, LT-GR2b, LT-GR2c, LT-GR2d, LT-GR3, LT-GR6]
canon_verdict_change: none
---

# Selected K77 action-induced real pairing and chiral horn

## Result in plain English

The selected action removes one of the two external choices exposed by
v0.152, but it still does not complete the external datum.

The observed Lorentz chiral algebra has two real invariant pairings,

```text
B_Re(X,Y) = Re Tr(XY),
B_Im(X,Y) = Im Tr(XY).
```

Both produce real numbers. Reality by itself therefore does **not** choose
between them. But the scalar Clifford trace already used by the selected K77
action acts on the two conjugate Weyl halves by addition. Its exact restriction
is

```text
Tr_full(XY) = Tr(XY) + conjugate(Tr(XY)) = 2 B_Re(X,Y).
```

The other line requires a different action insertion,

```text
-i Tr_full(chi XY) = 2 B_Im(X,Y).
```

Thus, **inside the selected scalar-trace action parent**, the projective
pairing ratio is not an external coordinate: `r=0`. The selected line is also
unchanged when the two conjugate chiral horns are exchanged, so the horn label
adds no independent datum in this parent.

This is conditional action ownership, not source derivation. Weinstein directs
that `Upsilon` be norm-squared but does not publish this real K77 pairing or
select the Spin-native parent over the Weyl-block/full-unitary rivals.

The constraint surplus improves but does not become positive. One integer
boundary component remains and the characteristic equation supplies one
independent amplitude constraint:

```text
1 independent fit condition - 1 supplied integer = 0.
```

Small-gauge/BFV compatibility is an admissibility test, not a second empirical
or same-object fit equation, so v0.152's favorable double count is retired.
The next decisive task is now singular: build or kill a typed map from this
same relative boundary index into P3. If it exists, the same integer would
close two independent rows and the surplus would become `+1`.

## Layer 0

| phrase | object here | not the same as |
| --- | --- | --- |
| real functional | either `B_Re` or `B_Im`, both real-valued on `sl(2,C)_R` | conjugation invariance |
| conjugation | exchange of the two Lorentz Weyl halves | boundary orientation reversal |
| parity | chiral exchange plus reversal of the oriented boundary integral | realness or a fixed orientation |
| selected scalar trace | sum of the two conjugate block traces | source selection of the action parent |
| chirality insertion | relative difference of the two block traces | the scalar trace already in `K_loc` |
| horn label | which conjugate chiral description is called plus | an independent datum after `B_Re` is fixed |
| BFV basicness | admissibility of the boundary functional under small gauge | another fit constraint |
| P3 bridge | same-object index theorem into the realized right-`H`/relative-`KO` interface | reusing the printed integer `n` |

## Exact pairing calculation

Use the real basis `(h,e,f,ih,ie,if)` of `sl(2,C)_R`. The two matrices
`B_Re` and `B_Im` are symmetric, nondegenerate and linearly independent.
Complex conjugation is

```text
C = diag(1,1,1,-1,-1,-1).
```

Exactly,

```text
C^T B_Re C =  B_Re,
C^T B_Im C = -B_Im.
```

Therefore conjugation invariance selects the `B_Re` line. Real-valuedness
alone leaves both lines. A combined orientation reversal and chiral exchange
adds the sign of the boundary integral and instead selects `B_Im` if parity is
imposed as a symmetry. GU fixes an oriented observation sector and the
Standard Model is parity-violating, so orientation cannot silently be promoted
to that symmetry requirement. It classifies the rival; it does not choose it.

## Action-parent scope

The result closes `r` only because v0.92 chose the Hodge-times-scalar-Clifford
trace as the selected local K77 residual pairing. Its transgressed restriction
to the observed chiral algebra is `B_Re`. A separately chosen complex
Chern--Simons action still has a two-real-dimensional coupling space, and a
chirality-inserted parent selects the other line.

This distinction also preserves the two `C^(32,32)` versus `U(64,64)` warning.
The current theorem does not enlarge the carrier or settle which principal
symmetry is ultimately operative.

## Constraint accounting

| item | coordinate/constraint count |
| --- | ---: |
| projective pairing ratio inside selected parent | `0` |
| independent chiral-horn choice inside `B_Re` parent | `0` |
| supplied boundary integer `n` | `1` coordinate |
| characteristic-amplitude equation | `1` independent constraint |
| small-gauge/BFV basicness | admissibility, not counted |
| typed P3/index bridge | absent |

Current strict surplus is exactly `0`. If an independently verified P3 bridge
uses the same `n`, the conditional surplus is `2-1=+1`. Merely calling both
integers `n` does not count.

## Ten efficient specialist lenses

1. **Complex Lie algebra — ACTUAL MATH, very high.** The two invariant real forms are exact and independent; real-valuedness is not selection.
2. **Real Clifford algebra — ACTUAL MATH, very high.** The ordinary full real scalar trace sums conjugate blocks and gives `2B_Re`; `B_Im` requires chirality insertion.
3. **Lorentzian spin geometry — ACTUAL MATH, high.** Horn exchange is conjugation; the selected even line is horn-blind.
4. **Chern--Simons theory — ACTUAL MATH, high.** Different invariant polynomials define different boundary actions, not normalizations of one action.
5. **Orientation/parity — ACTUAL MATH, high.** Combined parity would select the rival line; fixed orientation alone selects neither.
6. **Krein/operator theory — ACTUAL MATH, high.** This fixes a fibre bilinear line, not positivity, a fundamental symmetry or a closed domain.
7. **Variational bicomplex — ACTUAL MATH, high.** Transgression preserves the invariant polynomial; changing to the chirality-inserted line changes the action.
8. **Symplectic/BV--BFV — ACTUAL MATH, very high.** Basicness is an admissibility gate and cannot be double-counted as a fit equation.
9. **Constraint-rank geometry — ACTUAL MATH, very high.** Removing `r` raises strict surplus from `-1` to `0`; only a new independent same-object constraint makes it positive.
10. **Source criticism — ACTUAL MATH, high.** The source motivates the arena but remains silent on this conditional restriction and the parent choice.

## Progress and next gate

```text
Ledger v0.153 — 82/82 mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue 84; conditional parent range remains 84..86
Scoped quotients 5

headline_delta: none
frontier_conditions_closed: 2
frontier_conditions_opened: 0
remaining_named_conditions: 1
```

Closed conditionally: the projective pairing coordinate and independent horn
cost inside the selected scalar-trace parent. Remaining: a typed
relative-boundary index/P3 interface that would make the external integer
positive-surplus.

No residue, quotient, P1/P2/P3 assignment, canon verdict or public posture
moves. Exact pinned-SymPy probe: `30/30 PASS`.
