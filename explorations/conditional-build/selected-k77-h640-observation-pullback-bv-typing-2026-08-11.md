---
title: "Selected K77 H640 observation-pullback and BV typing"
artifact_type: exploration_result
created: 2026-08-11
status: ACTION_DERIVED_RANK128_GRAPH_LIFT_EXACT__LOWER_ORDER_GRAPH_RICCATI_AND_FULL_BV_OPEN
grade: "exact rational flat-section principal graph theorem plus two-prime complete coordinate-frame transport classes and finite lower-order/BV typing controls; no complete sixteen-cell lower-order closure, equation-dual, BV/Koszul--Tate cohomology, global section or analytic domain"
canon_verdict_change: none
---

# Selected K77 H640 observation-pullback and BV typing

## Result first

`H640` is not the naive coordinate subspace consisting of four observed
one-form slots plus the zero-form slot. The two rank-640 spaces intersect in
rank 512 and span rank 768. The difference is an exact rank-128 off-slice
graph component.

The observation retract nevertheless has full rank 640 on `H640`. It therefore
defines a canonical action-derived graph lift

\[
  L_H=H(RH)^{-1},\qquad RL_H=1,\qquad P_H=L_HR.
\]

All three conditional spatial evolutions preserve this graph exactly and
intertwine with their observed compressions:

\[
  (1-P_H)E_iL_H=0,
  \qquad E_iL_H=L_H(RE_iL_H).
\]

The coordinate lift fails this same principal no-leakage test. Thus the
previous `512+128` fingerprint was physically suggestive but not a subspace
identification; the action itself supplies the missing rank-128 graph at
principal grade.

## Lower-order result

The graph is not yet a full operator domain. Exact finite-field witnesses give:

| lower-order class | fixed-graph leakage |
| --- | ---: |
| noncentral internal spin action | 128 |
| observed zero-to-one connection port | 128 |
| transverse zero-to-one connection port | 128 |
| generic ambient connection port | 128 |
| mixed gauge-frame witness | 256 |

Graph compression is always defined, but compression is not ambient
no-leakage. The complete sixteen-cell source operator could still cancel these
individual residuals through its lower-left, barred-adjoint and connection
cells; that coupled Riccati condition has not been solved.

Pure moving-frame transport is exact. All 51 stabilizer generators preserve
the coordinate observation carrier, all 40 mixed generators leak rank 128
when it is frozen, and all 40 satisfy the moving-projector chain rule over
`GF(1009)` and `GF(1013)`. The same covariance identity holds for the moving
action graph. This proves tensorial transport, not the physical compatibility
condition `D_varpi P_H=0`.

## BV and symplectic typing

The source keeps barred and unbarred fields independent. Applying the graph
lift to both gives a split rank-1280 field carrier. Its formal field-antifield
doubling has non-ghost rank 2560. The exact K77 spin pairing restricts
nondegenerately to `H640`, and ordinary-gauge BRST transports the moving graph
covariantly.

These are eligibility and typing results. No antifield equations,
Koszul--Tate differential, master equation, BFV reduction or physical
cohomology is claimed.

## Layer 0

- `H640` and the coordinate observed carrier have equal dimension but are not
  equal subspaces.
- An action-derived graph lift is not a source quotation or a selected global
  observation section.
- Literal form pullback is not the equation-dual map and need not commute with
  variation.
- Moving-frame covariance is not the physical equation `D_varpi P_H=0`.
- Graph compression is not complement cancellation.
- A paired field-antifield carrier is not a BV differential or its cohomology.

The source owns observation pullback and the independent
`Omega^0(Y,S)+Omega^1(Y,S)` barred/unbarred grammar. It is silent on this
principal graph, the coupled lower-order Riccati condition, the equation-dual,
full BV/KT and physical cohomology.

## Consequence and next gate

Do not freeze the coordinate four-vector-plus-zero-form subspace and do not
discard `H640` merely because it differs from that coordinate lift. The next
gate is now precise:

1. insert `L_H` into all sixteen lower-order source cells;
2. solve or kill the coupled graph/Riccati and barred-adjoint conditions;
3. retain the full rank-1920 ambient carrier as the mandatory no-leakage
   control;
4. only if the coupled condition closes, build the observation-pulled
   Euler/BV/Koszul--Tate complex and its equation-dual.

Real K77, the observation section, both pairing horns and the source-action
parent remain conditional. P1/P2/P3, verdicts, residue, quotients, canon and
public posture do not move.

