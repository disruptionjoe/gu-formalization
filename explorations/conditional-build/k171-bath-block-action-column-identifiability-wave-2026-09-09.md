---
title: "K171 bath-block action-column identifiability wave"
status: active_research
doc_type: conditional_native_K139_bath_number_block_action_column_formula_scalar_data_nonidentifiability_and_vector_tail_interface_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned bath-number block formula and finite algebraic identifiability theorem showing that K170 scalar Neumann-word norms, seed base-form/Rayleigh data and reference-shape data do not determine the complete K156 base action column or matched residual; a fail-closed vector-block and sum-norm-tail interface is supplied, but the native continuum vectors, complete R_ref residual, complement or flux floor and scalar-center left floor remain absent, so no native K152 interval, physical/source selection, Born derivation, prediction or confirmation follows
manifest: lab/process/k171-bath-block-action-column-identifiability-wave.json
solver: tests/channel-swings/k171_bath_block_action_column_identifiability.py
probe: tests/channel-swings/k171_bath_block_action_column_identifiability_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K171 bath-block action-column identifiability wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: this packet stays on the repository-supplied K139--K170 positive
particle/hole signed point control. It asks whether the scalar information
banked by K170 determines the coefficient-complete K156 base action column on
the same K162 seeds. It derives the exact block formula and the missing-data
interface; its exact finite matrices are countercontrols, not native continuum
action data.

```gu-typed-objects
result: the K139 creation map raises bath number and the K156 normal-ordered base regular core preserves it, but the adjoint Neumann series lowers bath number, so every base-action component depends on vector blocks W_n G^n phi that K170 scalar word norms and shape parity do not determine; an exact same-Gram/same-form countercontrol changes the complete action column and matched residual
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), in q=(0,0),(1,0),(0,1), with fixed K139 regular chart and K162 zero-bath seeds; a four-dimensional graded rational carrier is a non-native algebraic countercontrol LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing transported by S=(1-G_256)^-1 to M=S* S; the complete matched residual uses the exact M^-1 dual norm ON=repository_signed_point_control
real_structure: CAR adjoint, momentum conjugation, Hermitian bath-number blocks and the K168 real flavor-symmetric reference shape
grading: conserved incidence charges, hard-core impurity degree, bath-particle number, Neumann word order, K162 level, generalized Rayleigh quotient and action-column versus scalar-form data
action_owner: repository-construction -- K156 owns the limiting normal-ordered base core abstractly, but no serialized continuum block vectors or complete all-word action tail, response datum, Weinstein source or GU action selects the physical extension, scalar center, domain or state
target: decide the action-column identifiability gate, compile the exact vector-valued input needed by K152 and refuse scalar or finite-control substitution MAP-TYPE=intertwiner
```

## Inline preflight bookend

K170 proves that the vectors `G^n phi` are mutually orthogonal, which exactly
diagonalizes the Gram and the two-valued K168 shape variance. Its next gate
asks for the coefficient-complete base action column. Retrieval rechecked
K139's creation chart, K156's normal-ordered remainder, K162's conforming
seeds, K165's generalized complement, K168's reference shape, K169's `5/2`
cap and K170's scalar data. No artifact serializes the limiting vectors
`W_n G^n phi` or their complete sum-norm tail.

The route census compared direct continuum normal ordering, graded block
recursion, form-only Rayleigh evaluation, Kato--Temple residuals,
Feshbach/Birman--Schwinger complements, Lehmann--Goerisch fluxes, finite
regulator scouting and exact identifiability countermodels. The block formula
is the cheapest route-changing discriminator: if scalar word data do not
determine the action column, another scalar integral cannot close the native
residual.

`SC-META-53` remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain
`NEEDS`. The source register and v0.263 physics ledger do not move.

## 1. The exact bath-number action-column formula

Write the K139/K156 base regular representative as

```text
R_0=A+S* W S,       A=H_0+256,       S=(1-G)^-1,       (1)
```

where `W=-256 I-X` and `X` is the normal-ordered Pauli/spectator plus four-
polarity exchange remainder. Let `P_n` project onto bath-particle number `n`.
K170 proves

```text
P_(n+1) G P_n=G P_n.                                  (2)
```

Every term of `X` has one bath creation and one bath annihilation, so

```text
P_n W P_n=W P_n.                                      (3)
```

Put `g_n=G^n phi` for a zero-bath K162 seed. Expanding the two Neumann factors
and applying (2)--(3) gives the exact component identity

```text
P_k R_0 phi
 =P_k A phi+sum_(n>=k) P_k (G*)^(n-k) W_n g_n.         (4)
```

This is a limiting common-carrier identity. It explains both why K170 works
for the Gram and why it does not yet give the action column. The Gram contains
only `sum_n ||g_n||^2`; equation (4) contains the directions and phases of
every `W_n g_n`, followed by every compatible adjoint-lowering word. Terms
with different `n` can land in the same output sector `k` and interfere.
Bath-number orthogonality therefore does **not** diagonalize the column.

## 2. Same Gram and form, different complete column

The solver constructs an exact rational four-dimensional positive Hilbert
control with sectors `(0,1,1,2)`. Its boundary map has the chain

```text
e_0 --1/4--> e_1 --1/5--> e_3,                        (5)
```

and two Hermitian number-preserving base cores differ only by an off-diagonal
coupling between `e_1` and the unused same-number direction `e_2`. The two
controls have identical

```text
||g_n||^2=(1,1/16,1/400),
M=S* S,
<e_0,R_0 e_0>=227/200,
rho_0=227/213,                                        (6)
```

and can carry the same K168 reference-shape data. Yet their base action
columns are

```text
(227/200,27/50,0,1/5),
(227/200,27/50,1/4,1/5),                              (7)
```

and their matched `M^-1` residual squares are respectively

```text
137/1704,       487/3408.                              (8)
```

Thus even the base Rayleigh value, in addition to K170's Gram and shape data,
does not identify the complete residual. This is an identifiability theorem
about the currently serialized data, not freedom to alter K156's fixed native
operator. The native task is to evaluate its missing vector blocks.

## 3. Minimum vector-valued completion interface

Suppose the vectors `w_n=W_n g_n` are explicitly evaluated through order `N`
and a complete native proof supplies

```text
sum_(n>N)||w_n|| <= beta_N.                            (9)
```

Since `||S*||<=1/(1-q)` and K153 fixes `q=3/8`, the unresolved complete-column
tail obeys

```text
||S* sum_(n>N) w_n|| <= beta_N/(1-q)=8 beta_N/5.      (10)
```

Equations (4), (9) and (10) are sufficient to turn outward approximations of
the resolved vector blocks into a complete Hilbert action column. A shifted
form-dual route may replace Hilbert vectors by bounded functionals, but it must
serialize the analogous block functionals and complete dual tail. Scalar
expectations `<g_n,W_ng_n>` or word norms do not fill either interface.

The compiler rejects a native claim without both an explicit vector-block
reference and a complete tail reference. Its positive control returns the
exact `8/5` adjoint-Neumann multiplier and a `1/40` tail from a supplied
`1/64` unresolved block sum. This proves the interface is nonvacuous without
calling the control native.

## 4. Native release replay

K170 supplies scalar word norms and reference-shape parity. K156 proves the
limiting normal-ordered `W` exists, but its current artifact and executable
serialize neither `W_n g_n` on these seeds nor a numerical all-`n` sum-norm
tail. The finite K155/K168 matrices cannot substitute because K163 proves the
independently rebuilt finite pullback does not commute with K162 refinement.

Therefore the coefficient-complete native `R_0 phi` column remains
unevaluated. The complete `R_ref` form-dual residual, a positive complete
`M`-orthogonal complement or flux floor below `E_ref(q)+5/2`, and the scalar-
center left floor remain closed. K169's threshold membership still does not
identify the first threshold, and K152 emits no native interval.

## Inline postflight bookend

- **Strongest structural advance:** equation (4) is the exact bath-number
  component formula for the limiting base action column.
- **Strongest negative result:** K170 word norms, physical Gram, base form and
  reference-shape data can all agree while the complete action column and
  matched residual differ.
- **Strongest execution advance:** equations (9)--(10) give the minimal
  fail-closed vector-block/tail interface for completing the native column.
- **Strongest overclaim:** “orthogonal Neumann words make the action residual
  a scalar variance.” Refused; adjoint lowering makes distinct input words
  interfere in the same output bath sector.
- **Strongest contrary route:** a complete shifted form-dual block-functional
  calculation could bypass Hilbert action vectors. It still needs its own
  complete dual tail and complement/flux certificate.
- **Weakest reproducibility seam:** a finite exact action column is not native
  without K162-compatible cofinal transfer; K163's noncommuting-square control
  remains active.

All five compatible mathematical arcs were attempted. The formula,
counterexample and certificate interface closed. The native release replay
failed on the missing vector blocks and tail, so the dependent complete
residual and complement/flux floor remain open. Physical extension and center
selection remain authority-excluded. No source, ledger, Born, prediction,
confirmation, canon, paper, release or public-posture truth moves.

## Next condition

Serialize the coefficient-specific continuum vectors `W_n G^n phi` for the
K162 vacuum and one-impurity seeds, beginning with the exact `n=0` and `n=1`
normal-ordered blocks, and prove a summable all-`n` norm tail. Feed the
resulting complete `R_0` column to K170's Gram/reference-shape packet. Then
compute the complete `R_ref` form-dual residual and prove a positive complete
`M`-orthogonal complement or flux floor below `E_ref(q)+5/2`. Select the
scalar center separately before asking for the native left floor.

## Reproduction

```bash
python3 tests/channel-swings/k171_bath_block_action_column_identifiability.py --demo
python3 tests/channel-swings/k171_bath_block_action_column_identifiability_probe.py
python3 tests/channel-swings/k171_bath_block_action_column_identifiability_probe.py --selftest
```
