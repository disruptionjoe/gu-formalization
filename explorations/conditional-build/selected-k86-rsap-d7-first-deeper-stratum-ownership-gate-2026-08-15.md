---
title: "Selected-K86 RSAP D7 first deeper stratum ownership gate"
status: active_research
doc_type: exact_one_root_extension_census_rank_ceiling_and_owner_gate
created: "2026-08-15"
registry: lab/process/selected-k86-rsap-d7-first-deeper-stratum-ownership-gate.json
probe: tests/channel-swings/selected_k86_rsap_d7_first_deeper_stratum_ownership_gate_probe.py
grade: "THREE ABSTRACT ONE-ROOT EXTENSIONS CLASSIFIED; A3+A1 NEAREST BY RANK; ACTUAL AMBIENT SUCCESSOR TYPE-MISSING"
canon_verdict_change: none
---

# Selected-K86 RSAP `D7` first deeper stratum ownership gate

## Result first

The completed local `A3` atlases do not determine one first deeper ambient
`so(7,7)` stratum. They end at a smooth local model whose `72D` symplectic
leaf is held fixed. Making that leaf itself singular requires new ambient
support and path data which the current construction does not serialize.

The exact abstract `D7` calculation nevertheless closes completely. Fix the
standard `A3` chain

```text
e1-e2, e2-e3, e3-e4.
```

There are `72` `D7` roots outside its twelve-root subsystem. Adding any one
of them gives exactly one of three rank-four semisimple root systems within
this fixed-Cartan one-root extension class:

| one-root extension | number of extra roots producing it | centralizer dimension | ambient target rank | `98D` map-rank ceiling |
|---|---:|---:|---:|---:|
| `A3+A1` | `12` | `7+14=21` | `91-21=70` | `84` |
| `A4` | `48` | `7+20=27` | `91-27=64` | `81` |
| `D4` | `12` | `7+24=31` | `91-31=60` | `79` |

The prior `A3` origin row is target/map rank `72/85`. Thus the three abstract
continuations force minimum map-rank losses of `1`, `4`, and `6` respectively.
`A3+A1` is the nearest candidate only in this numerical partial order. It is
not selected by the source, the action, the five local real-form atlases, or
the existing ambient-incidence data.

The honest answer to “what is the first deeper ambient stratum reached?” is
therefore:

```text
candidate semisimple one-root extensions: A3+A1, A4, D4
nearest by rank:                           A3+A1
actual selected type:                      TYPE_MISSING
```

This census does not exhaust nonsemisimple paths, simultaneous multi-root
jumps, other Cartan embeddings, or non-root-parametrized ambient
degenerations. Those routes remain unclassified until an owner supplies the
actual ambient object and path.

## Why the local atlases stop before this question

Every completed local real-`A3` carrier has the shape

```text
S72 x T*(A3 principal factor) x T*R4.
```

The local factor degenerates all the way to its `A3` origin, but `S72` remains
an abstract common leaf. The `T*R4` term supplies four zero-target transverse
pairs; it does not label four ambient roots. None of the local registries
contains an ambient `A3` support map, an extra `D7` root, or a coadjoint path
on which the `72D` leaf loses rank.

This is exactly the ownership boundary found at K81. Abstract root
combinatorics does not select a real-form support, and independently completed
real-form factors are alternative local horns rather than proved charts of
one ambient atlas. The new census narrows the missing type to three abstract
possibilities but does not supply the missing map.

## Enforced ambient-discriminator decision

The reusable contract in the registry binds the owner tuple, candidate
formulae, K80 contrary placements, transfer disposition, exact bound effect,
and independent successor admission. The synchronized decision view is:

<!-- RSAP_AMBIENT_STRATUM_DECISION_V1_BEGIN -->
```json
{
  "ambient_owner": "NOT_PROVIDED",
  "selected_ambient_successor": "TYPE_MISSING",
  "candidate_scope": "FIXED_CARTAN_SEMISIMPLE_ONE_ROOT_ONLY",
  "candidate_types": [
    "A3+A1",
    "A4",
    "D4"
  ],
  "contrary_controls": [
    "TRANSVERSE",
    "INTERSECTION_2_2",
    "INTERSECTION_3_2"
  ],
  "rank_schedule_transfer": "NOT_LICENSED",
  "bound_interval_before": [
    98,
    182
  ],
  "bound_interval_after": [
    98,
    182
  ],
  "bound_tightened": false,
  "zero_charge_gate_at_k86": "ADMITTED_LOCAL_ONLY_WITH_RANK_CEILING_49",
  "global_glue": "PROHIBITED"
}
```
<!-- RSAP_AMBIENT_STRATUM_DECISION_V1_END -->

K80's transverse, `(2,2)`, and `(3,2)` placements are the contrary control:
the same individual local factor classes admit inequivalent joint ambient
objects. K81 supplies no selected support maps or relative orbit. Therefore a
rank row cannot select an ambient object, and the `[98,182]` existence interval
does not tighten.

## Conditional `A3+A1` revival template

If a later source/action-owned ambient path selects an orthogonal `A1`, the
dimensionally natural local horn would be

```text
S70 x T*(A3 principal factor) x T*(A1 principal factor) x T*R3.
```

Its dimension arithmetic is `70+18+4+6=98`, and at the joint factor origin
its formal map-rank arithmetic is `70+9+2+3=84`, saturating the rank-`70`
pointwise bound. This is a **conditional dimensional template only**. No such
horn is constructed or attached here. In particular, the pointwise ceiling
is not evidence that a smooth onto moment map with that rank exists, and
“nearest by rank” is not an ownership rule.

## Layer 0 and comparator routing

This is a classical root-system, coadjoint-centralizer and symplectic-
realization ownership calculation. The ordinary Higgs model, the ordinary
three-family index route, and a net-chirality target are irrelevant to this
lane. They neither type the missing ambient support nor choose among
`A3+A1`, `A4`, and `D4`; importing them would be a category error. Use the
repository's GU-specific comparator routing before discussing those separate
physics burdens.

## Claim ceiling and next gate

- The exact abstract one-root extension census is closed: `12/48/12` roots
  produce `A3+A1/A4/D4`.
- The corresponding target ranks are `70/64/60`; the `98D` pointwise map-rank
  ceilings are `84/81/79`.
- None of those ceiling values is promoted to an achieved construction.
- `A3+A1` is nearest by rank but is not selected.
- The actual first deeper ambient stratum remains repository-scoped
  `TYPE_MISSING` until an explicit source/action-owned ambient `A3` embedding
  plus extra root, or equivalent coadjoint degeneration path, is supplied.
- No ambient or cross-real-form atlas edge is added. All five local `A3`
  atlases remain unchanged at their completed grades.
- Global all-strata RSAP remains open; the `182D` cotangent parent remains the
  all-charge fallback.
- No canon, ledger, residue, quotient datum, physical interpretation or public
  posture changes.

The next independent local swing is the zero-charge gate. At the zero
coadjoint value, a `98D` RSAP map must have differential rank at most `49`.
Attempt a local construction or obstruction there without pretending that it
glues across the still-type-missing ambient successor.

Subsequent K87 work executed that independent gate: a maximal-unipotent
cotangent horn achieves rank `49` at zero but fails zero-neighborhood coverage.
That later result resolves K86's issued successor without retroactively adding
an ambient `A3` edge or tightening `[98,182]`.

Reproduce with:

```bash
python3 tests/channel-swings/selected_k86_rsap_d7_first_deeper_stratum_ownership_gate_probe.py
```

The certificate uses the Python standard library and exact integer/rational
arithmetic only.
