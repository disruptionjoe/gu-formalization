---
title: "Selected-K77 CBRS-1U conformal-coframe primitive integrability obstruction"
status: active_research
doc_type: exact_local_integrability_obstruction
created: "2026-08-22"
registry: lab/process/selected-k77-cbrs1u-conformal-coframe-local-solution.json
probe: tests/channel-swings/selected_k77_cbrs1u_conformal_coframe_local_solution_probe.py
grade: "EXACT RECONSTRUCTION-GRADE KILL OF THE MINIMAL HOMOTHETIC PARALLEL-J4 LOCAL LIFT; NOT A CLASS-WIDE LOCAL-SOLUTION NO-GO"
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_ACTION_PRIMITIVE_EPSILON_AND_METX_GRAMMAR__REPOSITORY_DERIVES_THE_HOMOTHETIC_RAY_PRIMITIVE_INTEGRABILITY_OBSTRUCTION__SOURCE_SILENT_ON_THE_MULTIPLET_CLASS_AND_OBSTRUCTION
canon_verdict_change: none
---

# Selected-K77 CBRS-1U conformal-coframe primitive integrability obstruction

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: CBRS-1U exact primitive-epsilon obstruction to the minimal homothetic conformal-coframe local lift
carrier: complete selected-first-action owner plus four repository-constructed real Grassmann-even base scalars on the parallel moving J4 ray LAYER=toy CHIRALITY=N/A
pairing: selected K77 action-density pairing plus fixed internal Lorentz eta_AB scalar kinetic term and unit radial potential ON=parallel_homothetic_base_J4_conformal_coframe_candidate
real_structure: real base-J4 radical pair with fixed same-signature internal Lorentz form
grading: inherited Clifford and form grades with an unrestricted grade-one/three primitive momentum distinct from the Spin-grade-two connection restriction
action_owner: repository-construction
target: common local complete-Euler lift or exact integrability obstruction MAP-TYPE=evaluation
```

## Result first

The most economical lift of the two CBRS-1T pointwise coframe bodies fails a
complete Euler equation that a reduced matter-plus-metric calculation omits.

The `T`, scalar and intrinsic `MET(X)` subsystem forces the nonzero J4 ray

```text
T = rho T0,
U(rho) = I_base rho^3 + (rho-1)^2/4,
g = eta/[-U(rho)],
Phi^A(y)=y^A.
```

This formal conformal candidate exists on the same-signature component
`rho>rho_star`, where `rho_star=0.604489705474125...`. It reproduces the
CBRS-1T scale at `rho=1` and closes the displayed reduced equations.

It is **not** an actual solution of the selected first action. The primitive
owner is

```text
E_epsilon = D_B^!(E_B-E_T) + (D_epsilon S)^! K_S.
```

At either base-J4 unit body, the unrestricted momentum
`M0=E_B-E_T` has exactly 18 nonzero cells in Clifford grades one and three.
Only its Spin-grade-two restriction vanishes. Along the action-weighted
homothetic ray, every cubic momentum term is quadratic in `T`, while the
quadratic-action momentum is `rho` times linear in `T`. Therefore

```text
M(rho)=rho^2 M0.
```

In the frozen parallel moving J4 frame,

```text
D_B^! M(rho) = 2 rho d(rho) tensor M0.
```

At the unit-spacelike orbit, invertible `dPhi` gives
`d(rho)=2 eta_AB Phi^A dPhi^B != 0`. The moving-Shiab return vanishes
identically on the same scaled J4 ray, so it cannot cancel this first-jet
term. The complete primitive equation is nonzero for both radical signs.

CBRS-1U therefore returns the requested exact integrability obstruction. It
does not establish a local vacuum, and no stabilizer or coupled spectrum is
admissible for the failed candidate.

## Reduced subsystem and why it is still useful

Homogeneity at either normalized base-J4 point gives

```text
C3(T0)=-2 I_base,       Q2(T0)=3 I_base,
F(s,rho)=I_base(-2s^3+3rho s^2),
partial_s F=6 I_base s(rho-s).
```

Thus the nonzero reduced `T` equation uniquely fixes `s=rho`. Eliminating it
produces `U`. In scalar coordinates, `B_mu_nu=eta_AB d_mu Phi^A d_nu Phi^B`
equals `c g_mu_nu` for `c=-U`, so the intrinsic metric row vanishes. The
conformal d'Alembertian also matches the scalar source because

```text
box_g Phi^A = 2U'(rho) Phi^A,
2U'(rho)=6 I_base rho^2+rho-1.
```

These identities are exact, but they prove only a subsystem solution. The
primitive equation is an independent owner, not a Noether consequence of the
three displayed rows.

The cubic `U` is strictly decreasing and has one simple real root. Hence the
candidate's domain is action-defined rather than a fitted boundary. That
makes it a useful planted control for the next completion even though it is
not on shell.

## Why the primitive return cannot be hidden

The base-J4 point computation distinguishes three objects:

| object | exact unit-body result | role |
| --- | --- | --- |
| complete translation covector | zero | `T` equation |
| Spin-grade-two momentum restriction | zero | independent connection and metric graph |
| unrestricted momentum | 18 nonzero grade-one/three cells | primitive divergence |

Confusing the second and third rows is the failed shortcut. A constant point
body has zero divergence even when its unrestricted momentum is nonzero. A
nonconstant radial lift does not: the action makes its magnitude `rho^2`, and
a Spin connection can transport the J4 frame but cannot erase the explicit
radial derivative in the declared parallel-frame class.

The algebraic moving-Shiab term was already checked to vanish for both
base-J4 signs. Scaling the whole J4 body preserves that homogeneous zero, so
there is no second primitive term available in this class.

## Hostile return and claim ceiling

- **Reduced-Euler overclaim:** solving `T`, scalar and `MET(X)` rows is not a
  solution of the complete selected action while `E_epsilon` is nonzero.
- **Wrong-owner shortcut:** zero Spin-grade-two momentum does not mean zero
  unrestricted momentum; the latter owns the primitive divergence.
- **Constant-point shortcut:** the unit body has zero momentum divergence only
  because its jet is zero. The coframe lift requires `d(rho) != 0`.
- **Connection overclaim:** this result kills the frozen parallel moving-frame
  completion. It does not prove that every materially distinct connection or
  primitive carrier fails.
- **Spectrum trap:** curvature, stabilizer and characteristic calculations on
  the reduced candidate are off-shell diagnostics, not a physical spectrum.
- **Target leakage:** no fitted coefficient, observed frame, boundary, sector,
  particle datum or ledger value selects the obstruction.

No ledger verdict, canon, source ownership, residue, particle assignment,
prediction, confirmation or public posture changes.

## Reverse-scaffold consequence

Continue with `CBRS-1V`: freeze the smallest target-blind connection/primitive
completion capable of contributing in the same 18-cell grade-one/three
carrier. Solve the complete `T`, independent connection, primitive-epsilon,
scalar and intrinsic `MET(X)` equations together. If the completion is rigid
or requires a fitted coefficient, frame, boundary or sector, close it and move
to the next materially distinct owner. Do not compute a stabilizer or spectrum
until an actual local solution passes every Euler owner.

Reproduce with:

```bash
sage -python \
  tests/channel-swings/selected_k77_cbrs1u_conformal_coframe_local_solution_probe.py
```

The exact probe passes `54/54` after native propagation.
