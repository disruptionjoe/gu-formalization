---
title: "K236 invariant native I1B mixed-grade normal-symbol star"
status: active_research
doc_type: exact_local_selected_i1b_mixed_grade_symbol_and_null_classification
created: "2026-09-18"
registry: lab/process/k236-native-i1b-invariant-mixed-grade-symbol.json
solver: tests/channel-swings/k236_native_i1b_invariant_mixed_grade_symbol.py
probe: tests/channel-swings/k236_native_i1b_invariant_mixed_grade_symbol_probe.py
grade: "EXACT RANK-TWO TWELVE-SPOKE SELECTED-I1B NORMAL-SYMBOL STAR INSIDE ONE RANK-SIX LABEL BLOCK; ELEVEN EVEN NULL DIFFERENCES; NO TANGENTIAL/LOWER-ORDER OR PHYSICAL CLOSED MODE"
canon_verdict_change: none
---

# K236 invariant mixed-grade normal-symbol star

> **GU-COMPARATOR-ROUTING — scope before inference.** This is the selected
> source-native first-action local distortion symbol and one formal displayed
> four-field candidate insertion, not a conventional Einstein/Dirac or
> particle-spectrum comparator. Read the source-native routing method first.

Classification: `SOURCE_NATIVE_ROUTE`.

```gu-typed-objects
result: the minimum invariant selected-I1B frozen-normal subspace containing K235's odd direction is a rank-two twelve-spoke Cl1/Cl2 star with eleven even kernel differences
carrier: Omega1(Cl(7,7)) selected local distortion label block at flat T=0; candidate full-Dirac source insertion is a separate formal bridge LAYER=ambient+source-print BRIDGE=local_clifford_action CHIRALITY=S-FULL-DIRAC
pairing: formal selected-Shiab Euler/Green coefficient after skew integration by parts; no positive physical norm ON=local_28_basis_normal_symbol
real_structure: real exact Clifford/one-form coefficients; global barred-field reality and physical adjoint UNTYPED
grading: one Cl1 odd spoke hub, twelve Cl2 even spokes; source-global spinor grading remains UNTYPED
action_owner: repository-construction for the selected comm/symi/symi coefficient; source-print for the separate displayed fermion candidate
target: exact invariant frozen-normal symbol block and its kernel, not a global operator or physical quotient MAP-TYPE=not-a-map
```

## The completed K235 follow-through

K235's single pair `o=dx^0⊗gamma_1` and
`e_2=dx^2⊗gamma_1 gamma_2` has a nonzero cross coefficient but is **not**
an invariant two-dimensional subsystem. In the complete 28-basis invariant
label block for normal `n=dx^0`, define

```text
e_j = dx^j tensor gamma_1 gamma_j,   j=2,...,13,
E = sum_(j=2)^13 e_j.
```

The selected `comm/symi/symi` formal Euler coefficient `N` has the exact
columns

```text
N(o) = -sum_(j=2)^13 e_j = -E,
N(e_j) = o                 for every j=2,...,13.
```

These are **complete columns in the 28-basis block**, not just selected
matrix entries. Each ordered raw pair is `(2,0)` before skew
antisymmetrization. The 13-dimensional span of `o,e_2,...,e_13` is
invariant for this one frozen normal. Its two-dimensional subspace
`span{o,E}` is the smallest invariant subspace containing `o`, with matrix
`[[0,12],[-1,0]]` in that ordered basis, square `-12 I` and rank two.
The eleven independent differences `e_j-e_2` for `j=3,...,13` are exact
normal-symbol kernel elements, **not** eleven gauge directions. The full
28-label coefficient has rank six; this star accounts for only two of those
six ranks. Its formal complex roots `±i√12` are eigenvalues of a frozen
finite symbol matrix, not frequencies, an instability, propagation or a
closed physical spectrum. Normalization of `E` is algebraic, not a positive
physical-state pairing.

The producer reads K235's full-basis matrix and verifies the complete
support, star action, rank and independent kernel. A second probe directly
replays raw source-Shiab wedge contractions against all 28 output directions
for each of the twelve spokes. A wrong first product channel kills each
cross, and omission of eleven spokes fails invariance. This checks the
source-native algebra, not the missing global operator seam.

By linearity, K235's twelve formal potentially nonzero entries in the
**displayed** draft four-field candidate now take the collective even
insertion `sum_j dx^j gamma_1 gamma_j` together with the odd hub. This is
the first actual invariant *normal-symbol* mixed-grade candidate rather
than an arbitrary pair. It does not make the full fermion matrix, its star,
bar, domain or nonzero southeast variant uniquely source-selected. Even
though each individual Clifford bivector is invertible on full Dirac spinors,
their one-form sum is not thereby an invertible global operator.

## Boundary and rerank

K132 already shows that only eleven of twenty-four normal-null rows in a
different exact all-grade 56-block remain tangential-null. K236 therefore
cannot promote this one normal invariant star or its kernel to a covariant
invariant subbundle, constraint propagation, closed common domain or BV
quotient. The algebraic `kappa_1 K` and metric-curvature coupling may also
mix it at lower order. At zero fermion, the complete quadratic Hessian is
still bosonic ⊕ fermionic; the formal fermion insertion is a cubic response
vertex, not a mixed boson–single-fermion quadratic eigenmode.

Strongest overclaim is a physical helicity/mass or instability reading of
`±i√12`. Strongest contrary extension is an admissible different Shiab,
fermion southeast branch or action-owned lower-order/tangential coupling.
The weakest seam is preserving this star under those actual operators and a
stationary common closed domain. The next source-facing gate is to supply
and test that complete operator/domain or prove a structural obstruction
without silently choosing it. Repeating another frozen-normal spoke is no
longer useful. The independent signed third-shell region and a/c atlas remain
separate alternatives.

SC-ACT-01/02 (`ASSERTS`), SC-META-53 (`UNCERTAIN`), LT-GR6b/LT-SM8
(`NEEDS`), K234's fixed ray, K224/K229's remaining `1.7933e-22` budget and
81.83-times selected upper, K215, boundaries, source/ledger/canon and
public posture are unchanged. No external novelty or specialist acceptance
is claimed.

```sh
PYTHONDONTWRITEBYTECODE=1 _local/cas-venv/bin/python tests/channel-swings/k236_native_i1b_invariant_mixed_grade_symbol.py
PYTHONDONTWRITEBYTECODE=1 _local/cas-venv/bin/python tests/channel-swings/k236_native_i1b_invariant_mixed_grade_symbol_probe.py
```
