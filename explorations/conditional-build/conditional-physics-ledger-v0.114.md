---
title: "Conditional physics ledger v0.114"
status: current
doc_type: ledger_summary
created: "2026-08-09"
---

# Conditional physics ledger v0.114

`82/82` targets remain mapped: `32 SAME`, `19 DIFFERS`, `26 NEEDS`, and
`5 OVER-DETERMINED`. Residue remains `84..86` continuous parameters, at least
19 function-valued slots and nine discrete forks. Five scoped quotients remain
booked.

The recorded v0.113 successor partly duplicated v0.103: the compact-boundary
`H7 x H-7` strong cotangent space and vertical polarization already existed.
Composing the two new branch amplitudes with that object proves that all
nonzero scalar multiples are symplectomorphic by momentum rescaling. The two
opposite-sign Galois branches therefore cannot be selected or normalized by
strongness, the vertical polarization or minimal-edge horizontality.

On every declared nonempty compact-boundary edge-torsor stratum, the
minimal-edge horn now carries the classical BFV charge
`Q=c^a mu_a-1/2 f_ab^c c^a c^b b_c`. Its master-equation coefficients vanish
exactly by first-class closure and Jacobi. The cubic ghost term is necessary:
omitting it fires the nonabelian planted control. The charged horn remains a
physical current-algebra rival rather than a gauge quotient.

This is not a global or quantum BFV theorem. Edge-torsor nonemptiness/topology,
a common bulk Green/Krein domain preserving physical and ghost traces, coupled
bulk-boundary BV-BFV, contour, measure, anomaly, physical horn and action
parent all remain open. The selected Spin-native parent, two `U(32,32)` halves
and full `U(64,64)` remain distinct.

Seven rows migrate in distance, frontier grade and evidence only: `LT-GR1`,
`LT-GR2b`, `LT-GR2c`, `LT-GR2d`, `LT-GR3`, `LT-GR5`, and `LT-GR6`. No verdict,
reason kind, residue, booked quotient, datum, canon statement or public posture
changes. P1/P2/P3 remain unused.

Next: prove global edge-torsor nonemptiness/topology and construct one bulk
Green/Krein domain preserving `H7/H-7` and `H8/H-8` traces; then test coupled
bulk-boundary BV-BFV compatibility while keeping physical-horn and
action-parent selection separate.

Machine ledger: `lab/process/conditional-physics-ledger-v0.114.json`.
