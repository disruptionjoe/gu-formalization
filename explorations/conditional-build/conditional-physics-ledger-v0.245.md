---
title: "Conditional Physics Ledger v0.245"
status: research
doc_type: ledger-summary
created: 2026-08-14
---

# Conditional Physics Ledger v0.245

Ledger v0.245 preserves all 82 verdicts and records one distance-only migration
on `LT-SM8`, the requirement that BV/BRST reduction yield a positive physical
space. It also registers the moving-twistor coherence proposal as a conditional
mechanism hypothesis rather than allowing it to drift into the result ledger as
established physics.

The exact support is local and typed. The base `Gr(2,C^4)` twistor
correspondence and normal `O(6,4)/U(3,2)` complex-structure fibre construct
separately. The minimal longitudinal moving-`J` differential is nilpotent and
preserves the defining `J` constraints. In the universal twistor
superconnection, BRST-mixed and ghost-two curvature terms cancel. Existing GU
horizontal principal symbols are compatible with the tested `Jhat`.

None of that supplies the actual lifted GU `(0,1)` connection, its
`F^(0,2)` or base-normal mixed curvature, a full BV master/BFV complex, a
Penrose pushforward, a Lorentzian positive-energy closed domain, a positive
physical pairing, interacting physical cohomology, or an observable
decoherence law.

```text
Ledger v0.245 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Tightness: T4x1 T3x3 T2x1 · scoped quotients: 5
Headline delta: none
```

Only `LT-SM8` receives a distance migration. No verdict, kind, residue,
quotient, datum, canon, or public-posture change follows.

## Conditional hypothesis retained

`HYP-TW-COHERENCE-01` states:

> If the action-selected GU connection lifts to an integrable moving twistor
> complex, descends through complete BV/BFV reduction, and admits a Lorentzian
> positive physical pairing, then physical superposition may be represented
> by the resulting complex physical cohomology. If the action-owned normalized
> mixed curvature is the Willmore second-fundamental-form sector, failure of
> parallel transport may provide a geometric coherence obstruction.

This is deliberately stronger than the computed result. It is not a claim
that every spacetime point is either ambient `C^(32,32)` half, that `J_N` is
the spinorial `J10`, or that pointwise complex structure is already quantum
superposition. Base twistor two-planes, normal `J_N`, spinorial `J10`, the two
ambient Weyl halves, and a positive physical polarization remain distinct
objects until explicit intertwining maps are built.

## Kill sequence

1. Compute the actual lifted GU `F^(0,2)` and base-normal mixed curvature.
   An unavoidable nonzero obstruction kills integrable twistor closure.
2. Push the complex to spacetime. Failure to reproduce the owned GU
   Weyl/Dirac/Rarita--Schwinger principal symbols kills the carrier proposal.
3. Construct the Lorentzian real structure, positive-energy cone and closed
   domain. Failure here kills the physical reading while leaving the complex
   geometry intact.
4. Construct the positive pairing and physical BV/BFV cohomology. A trivial,
   indefinite or wrong cohomology kills the superposition mechanism.
5. Derive an interaction- and observable-dependent coherence functional.
   Failure to obtain one kills the decoherence interpretation and any rate
   claim.

The next move for this route is therefore not another interpretation. Build
the program-native rank-four twistor carrier, the base-normal
incidence/soldering map and the actual lifted GU `(0,1)` connection; compute
its `F^(0,2)` and mixed curvature before attempting the Penrose transform or
positive-state claim.
