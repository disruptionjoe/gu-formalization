---
artifact_type: conditional_physics_ledger_migration
created: 2026-08-11
status: active_research
predecessor: lab/process/conditional-physics-ledger-v0.160.json
machine_ledger: lab/process/conditional-physics-ledger-v0.161.json
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Conditional physics ledger v0.161

## Progress meter

```text
Ledger v0.161 — 82/82 mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous; function-valued slots >=19
Discrete forks 9 · scoped quotients 5
```

The headline meter is unchanged. Six rows migrate in distance/evidence after
the exact fixed-common receiver computation and the prior-art selector typing
check.

## What changed

- Each timelike, spacelike and null minimal receiver remains rank `256`.
- Any pair intersects at rank `128` and joins at rank `384`.
- All three give the same tested fixed common rank-`384` hull.
- The row-Pin and column-Pin common hulls coincide: join and intersection are
  both rank `384`.
- The action-dual hull therefore requires `256` paired left additions, not the
  per-stratum `128` additions.
- `P_epsilon` and `D_varpi chi_epsilon` remain bosonic parent constraints and
  cannot directly discriminate the distinct fermion receiver/carriers.

This kills the bounded **fixed rank-256** reading, not the graph, the source
action program, or the unrestricted four-field operator wholesale.

## Six migrations

- `RA-D4`: mirror removal now waits on four-field action-dual ownership of the
  rank-`384` hull and induced fermion/BV cohomology.
- `RA-F1`: P3 still waits on that operator, a common domain and a typed index.
- `RA-F2`: fixed-W replication stays conditional; the common receiver is
  rank `384`, not a selected rank-`256` subbundle.
- `RA-G2`: graph/mirror discrimination waits on the induced operator rather
  than a direct bosonic projector.
- `LT-SM3`: the next test is equality with the unrestricted four-field Euler
  image/action dual or return to the unrestricted source operator.
- `AC-F1`: observed chirality remains downstream of four-field action/BV/domain
  descent and a 14D-to-4D index.

Verdicts, reason kinds, residue, function-valued slots, forks, quotients,
P1/P2/P3, canon and public posture do not move.

## Frontier

```text
headline_delta: none
frontier_conditions_closed: 2
frontier_conditions_opened: 1
remaining_named_conditions: 3
```

Closed: the exact tested common-hull rank and the direct bosonic-selector
shortcut. Opened: compare the common hull with the unrestricted four-field
Euler image and its action dual. Only an induced fermion intertwiner may then
run graph/mirror/random-192/640/832 controls.

Evidence:

- `selected-k77-fixed-common-receiver-hull-2026-08-11.md`;
- `lab/process/selected-k77-fixed-common-receiver-hull.json`;
- `tests/channel-swings/selected_k77_fixed_common_receiver_hull_probe.py`;
- `lab/process/hostile-reviews/2026-08-11-selected-k77-fixed-common-receiver-hull-review.md`;
- `lab/sources/selected-k77-fixed-common-receiver-hull-source-return-2026-08-11.md`.
