---
artifact_type: compose_source_verify_result
created: 2026-08-08
status: K77_AUTHOR_ASSERTED_CONDITIONAL__K95_GEOMETRY_DERIVED__NEXT_BUILD_BRANCH_AWARE
source_return: SOURCE-CORRECTS
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Signature rationale and Build-branch retype

## Plain-English result

The K77 work was not wasted, but the reason for calling it primary was too
strong.

Eric/Curt's released material explicitly **chooses and uses** the `(7,7)`
carrier because of the spinor and matter structure they want.  However, the
metric blocks they display, interpreted with their own signature convention,
add to `(9,5)`.  Thus K77 is the author-asserted conditional construction; K95
is the branch derived by the currently written metric-bundle arithmetic.

This preserves both scientific programs without pretending they are the same:

- continue K77 when asking whether Eric's asserted carrier can make the whole
  physics picture cohere;
- retain K95 as the geometry-derived comparator and negative-control bank;
- whenever a theorem is tensor-natural and independent of the real form,
  prove it once at the signature-generic level;
- whenever Hodge, Clifford reality, Krein pairing, adjoint or domain enters,
  specialize separately.  No silent port is allowed.

## What changed

The `REAL-CLIFFORD-FORM` settled side remains K77 because the question is
which algebra the source actually uses, and the draft repeatedly prints
`Spin(7,7)`.  Its `settled_how` changes: the source **asserts** K77; its
displayed arithmetic does not derive it.

`SIGNATURE-AMBIENT` remains open.  The newly falsified declared-base resolver
does not select K95 as physical truth; it shows only that the advertised K77
derivation is missing a geometric step.

The current K77 exact artifacts remain valid on their declared conditional
metric.  Their mathematics is not retracted.  Claims calling that metric
“derived from the source's block arithmetic” are retyped.

## Frontier movement

Three named uncertainties close:

1. the source's three displayed signature pairs use a consistently
   negative-first convention;
2. the source explicitly computes its fermionic construction on K77; and
3. the previous `REAL-CLIFFORD-FORM` rationale overstated an assertion as a
   derivation.

One sharper condition opens: construct the missing source-native bilinear or
sign map that would derive K77 from the displayed blocks, or leave K77
conditional.  The three remaining Build conditions are the signature-generic
nonhomogeneous Ward identity, its K77 branch-native specialization and the K95
branch-native control.

## Constraint accounting

No field, coefficient, function, quotient, datum or P1/P2/P3 component is
added.  Verdict counts and residue do not move.  The construction queue is
redirected rather than expanded:

1. express the upcoming nonhomogeneous Ward calculation at a
   signature-generic tensor-natural layer;
2. specialize its Hodge/Clifford/Krein/adjoint parts independently on K77 and
   K95 only where those structures are actually used;
3. do not redo an entire branch when the proof depends only on nondegeneracy
   and naturality.

## Seven-axis disposition

- **Layer 0:** source use, source arithmetic, derived inertia and conditional
  branch choice are distinct.
- **L1:** both signature horns and the corrected selection rationale are explicit.
- **L2:** K77 and K95 real Clifford/Krein data are not identified.
- **L3:** the three mirrored pairs and the direct-sum inertia are exact.
- **L4:** K95 is derived from the displayed metric blocks; a K77 geometric
  bilinear remains missing.
- **L5:** no action, Euler, Green or symplectic result changes.
- **L6:** no common domain or positivity transfer is inferred.
- **L7:** no Standard Model, chirality, gravity or cosmology verdict changes.

## Next gate

Construct the nonhomogeneous primitive-epsilon and field-Lie Ward packet first
as a signature-generic naturality identity.  Then test all selected-action
coefficients on K77 and run the K95 specialization as a branch-native control
where Hodge, Clifford, Krein or adjoint data enter.  Only exact coefficientwise
`J R=0` opens `K*`, formal adjoint and Green/symplectic descent.

## Evidence

- `tests/source_signature_notation_is_mirrored.py`
- `tests/source_signature_branch_rationale_retype.py`
- `lab/sources/signature-rationale-build-branch-source-reinspection-2026-08-08.md`

Validation passed the 5/5 mirrored-notation certificate, the 6/6 branch-retype
gate, the 28/28 v0.89 append-only ledger gate, the 44/44 functional-channel
behavior gate and the 170/170 predecessor K77 physical-diffeomorphism gate.
The broad functional-contract scope audit retains one inherited failure: it
expects `ACTION_OWNED_DEGREE14_GREEN_PRIMARY`, while clean HEAD already carries
`ACTION_OWNED_DEGREE14_EPSILON_COMPANION_EXACT`.  This wave neither introduces
nor repairs that baseline naming debt.
