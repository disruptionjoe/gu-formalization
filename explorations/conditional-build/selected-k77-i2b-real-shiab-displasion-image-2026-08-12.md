---
artifact_type: construction_result
created: 2026-08-12
source_claims: [SC-ACT-02, SC-ACT-04]
source_return: SOURCE_CONFIRMS_SWERVATURE_DISPLASION_SHELL_TWO_CONNECTIONS_AND_RESIDUAL_SQUARE__SOURCE_SILENT_ON_FIXED_HQ_REAL_SHIAB_CANCELLATION_AND_OPPOSITE_PHASE_BACKGROUND
verdict: DIRECT_FIXED_HQ_REAL_SHIAB_CANCELLATION_KILLED__COMPLEX_PREIMAGE_REJECTED__MOVING_FULL_UNITARY_DIFFERENT_SELECTOR_AND_JET_ROUTES_OPEN
---

# Selected K77 I2B real-Shiab displasion image

## Outcome

The v0.201 branch exposed the right next question.  Its restricted
`SC-ACT-04` potential vanishes at

```text
r^2=-3 rho,
```

but the raw first-shell residual is the nonzero, Krein-null grade-one
thirteen-form

```text
Upsilon = kappa_1 r H_q.
```

This wave asked whether a curvature background in the selected-Shiab image can
cancel `H_q`.  Over the unrestricted complex algebra the answer is yes.  Over
the operative fixed-`H_q` real unitary algebra the answer is no.

That is a route kill, not a theory kill.  It removes the tempting instruction
“choose a fixed-`H_q` curvature background that cancels the residual.”  It does
not decide a moving Hermitian form, the source-full unitary connection,
derivative jets, a different source-typed Shiab, or a global connection.

## Pre-wave typing

The following predicates were kept separate:

1. `Upsilon=0`, the first-shell source equation;
2. `<Upsilon,Upsilon>=0`, the second-action value;
3. stationarity of the second action;
4. membership in a pointwise curvature-value image;
5. realization by a connection satisfying Bianchi and descent;
6. unrestricted complex membership versus membership in the real form.

The fixed-`H_q` bank is an operative candidate inherited from the current
construction.  It is not identified with the full source `U(64,64)` parent or
with the two possible `U(32,32)` half-connections.

## Exact result 1: a zero-fit negative curvature ray

Write the current radial augmented-torsion field as

```text
T0 = e^13 gamma_13 + i e^12 gamma_12
```

and use the source-available second connection to form the opposite-phase cell

```text
B0 = e^13 gamma_13 - i e^12 gamma_12.
```

The exact algebra gives

```text
T0^2 = E,
B0^2 = -E,
B0 T0 + T0 B0 = 0.
```

Thus the two-connection geometry itself supplies the sign and ray needed for a
negative co-moving curvature contribution.  No new field direction or
external datum was fitted.  Its amplitude remains free.

The full `T` first variation was then assembled in the two-cell family.  Its
`14 x 4` exact stationarity matrix has rank two, and every vector in its
two-dimensional nullspace has zero `kappa_1` component.  Therefore this
natural background cannot cancel nonzero displasion.  It geometrizes the
negative ray but does not solve the source shell.

## Exact result 2: complex preimage

As a positive control, all two-form values with complex Clifford-bivector
coefficients were sent through the selected Shiab.  The resulting exact
Gaussian-rational image has rank `1197` and contains `H_q`.

This proves that failure is not caused by a broken target, empty source bank,
or vacuous matcher.  It also makes complexification dangerous here: the answer
changes when the reality condition is restored.

## Exact result 3: complete fixed-Hq real image

The selected Shiab multiplies a curvature Clifford coefficient by one
generator in its first term and by three in its second.  It flips Clifford
parity and changes grade by at most three.  Therefore a Clifford-grade-one
target can receive contributions only from even input grades `0`, `2`, and
`4`; odd inputs produce even output, and even grades `6+` cannot contract down
far enough.

The complete relevant Clifford bank contains

```text
C(14,0)+C(14,2)+C(14,4) = 1+91+1001 = 1093
```

blades.  Exact fixed-`H_q` unitarity assigns one allowed real phase to every
blade:

```text
364 real-phase blades + 729 imaginary-phase blades = 1093.
```

Across all `C(14,2)=91` curvature form slots this gives

```text
91 * 1093 = 99,463
```

real source columns.  Their Clifford-grade-one projection has exact real rank
`364`.  Adjoining the displasion target raises the rank to `365`.

Hence

```text
H_q is not in Im(Shiab_selected | fixed-H_q real Clifford source bank).
```

The exclusion is complete for this real Clifford parent and this selected
Shiab at the pointwise curvature-value level.

## Constraint accounting

- New fields: `0`.
- New parameters: `0`.
- New data: `0`.
- New selectors: `0`.
- P1/P2/P3: unchanged and unused.
- Negative curvature direction: source-two-connection-owned.
- Background magnitude: still unsupplied.
- Full source-unitary complement: not constructed.

## What died and what survived

**Died:**

- direct fixed-`H_q` real selected-Shiab curvature cancellation;
- treating the unrestricted complex preimage as a physical real-form answer;
- the simplest opposite-phase two-cell background as a solution with
  `kappa_1 != 0`.

**Survived:**

- the restricted `SC-ACT-04` Mexican-hat polynomial as a conditional action
  restriction;
- a moving-`H_q` derivative of Shiab/Hodge;
- the source-full `U(64,64)` parent or two `U(32,32)` half-parent;
- derivative-jet and Bianchi-compatible connection completion;
- a different source-typed Shiab/adjoint-Shiab channel;
- physical reduction, kinetic normalization, Yukawa placement, BV and domain.

## Next gate

Construct the derivative of the Hermitian form and selected Shiab/Hodge along
the moving doublet reduction, while separately typing the complement between
the finite real Clifford bank and the source-full unitary parent.  Recompute
the first-shell residual and second-action first variation before trying to
select `J`, `rho`, a Higgs spectrum, or an external datum.
