---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-06
status: CURRENT_APPEND_ONLY_LEDGER
machine_ledger: lab/process/conditional-physics-ledger-v0.22.json
predecessor: explorations/conditional-build/conditional-physics-ledger-v0.21.md
---

# Conditional physics ledger v0.22

> **K122 native-coordinate correction (2026-08-15):** the diagonal kernel
> computed below is exactly where the native metric column lies:
> `(delta A,delta B)=(DB_LC[H],DB_LC[H])`, hence `delta T=0`. The nonzero
> `14/3` anti-diagonal/raw relative-connection kernel remains a carrier
> control, not a native metric interaction. Follow the K122 artifact before
> using this historical ledger in a cubic assembly.

## Meter

```text
Ledger v0.22 — 82/82 active target rows mapped (100%)
33 SAME · 19 DIFFERS · 24 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

The denominator, verdicts, reason kinds, residue and quotient count do not
change. Five distances migrate after the rank-five principal gauge obstruction
of v0.21 was retyped on the source's actual two-connection object.

## What moved

GU's augmented torsion is a difference of two connections,

```text
T = A - B.
```

On the complete `24+24` Levi-Civita connection carrier, its tangent map

```text
W(delta A, delta B) = delta A - delta B
```

has exact rank 24 and kernel equal to the complete 24-dimensional diagonal.
Consequently simultaneous inhomogeneous principal connection-gauge motion
`(D chi,D chi)` is killed exactly. The six-dimensional gauge/gauge block that
had rank five when one connection was tested in isolation has rank zero after
the source-owned difference is assembled.

This correction does not erase the raw anti-diagonal first-jet carrier
calculation. The exact mixed helicity-two kernel on that non-native carrier
remains

```text
(14/3) (p.q) (h0:hm).
```

Within the linear family `alpha delta A + beta delta B`, diagonal annihilation
and normalization at the first endpoint uniquely give `(alpha,beta)=(1,-1)`.
This is the source-owned connection difference, not a fitted counterterm; it
adds zero coefficients and consumes no external datum.

The old rank-five computation remains correct for its isolated one-connection
scope. What is retired is only its use as a final obstruction to the
two-connection principal quotient.

## Rows moved

`LT-GR1`, `LT-GR2b`, `LT-GR5`, `LT-GR6` and `LT-SM8` receive
distance-only migrations. Their verdicts and reason kinds remain frozen.
`LT-GR3` remains open because direct curvature, full `II` and defect third
derivatives were not assembled.

## Current highest-information gate

Assemble the lower-order homogeneous adjoint orbit at nonzero `T`, the moving
Shiab/Hodge/DeWitt/Krein pairing and observation Ward terms, direct
curvature/full-`II`/defect `D3`, and the preboundary current. Principal
diagonal descent is exact; only the resulting complete reduced class may
advance to the Q1 pole test.

P1/P2/P3 remain unused. Curt remains formally separate and no third lane is
promoted.
