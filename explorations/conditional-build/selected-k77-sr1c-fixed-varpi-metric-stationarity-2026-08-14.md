---
title: "Selected-K77 SR-1C fixed-varpi metric stationarity"
status: active_research
doc_type: construction_result
created: "2026-08-14"
registry: lab/process/selected-k77-sr1c-fixed-varpi-metric-stationarity.json
probe: tests/channel-swings/selected_k77_sr1c_fixed_varpi_metric_stationarity_probe.py
grade: "EXACT LOCAL FORMAL TWO-JET METRIC KILL; BRANCH- AND ACTION-SCOPED"
canon_verdict_change: none
---

# Selected-K77 SR-1C fixed-varpi metric stationarity

## Result first

The compatible parallel two-jet is killed as a stationary background for the
selected action on both algebraic roots.

The preceding gate proved

```text
E_T=0,  j1(E_B-E_T)=0,  E_epsilon=0
```

on the declared parallel extension.  Composing that result with the exact
all-ten K77 naturality bank reduces the intrinsic fixed-`varpi` metric first
variation to

```text
E_g = rho L_1 + (D_g B_Z)^! (E_B-E_T).
```

The second term vanishes because the covariant Levi-Civita graph is first
order and the complete common-basis `j1(E_B-E_T)` is zero.  The first term
does not vanish.  In canonical

```text
(00,01,02,03,11,12,13,22,23,33)
```

order, reduction modulo

```text
28392t^2+91t-351
```

gives the normalized total metric covector

```text
(33703t/468-3/52) (1,0,0,0,-1,0,0,-1,0,-1).
```

Its scalar coefficient is coprime to the branch polynomial.  It is therefore
nonzero on both real roots.  The row has rank one and a nine-dimensional
traceless kernel; it is one trace obstruction, not ten independent failures.

Primitive epsilon remains exactly zero on this extension.  That success is
not retracted, but it no longer advances the extension toward a background.
`SR-1` remains `BACKGROUND-MISSING`, `SR-2` remains blocked, and VRS-6 gains
no background premise.

## Why the six-name moving queue is not six action owners

The prior queue named moving Shiab, Hodge, frame, density, lowerer and
observation returns.  Existing exact receipts force a sharper decomposition:

- metric, Hodge, Clifford/Krein pairing, frame, Phi and Shiab transport are
  one functorial packet.  In a co-moving K77 frame their coefficient motion is
  stationary; splitting them into separate action contributions depends on a
  trivialization;
- density retains the physical gimmel-volume response
  `rho=(-2,0,0,0,2,0,0,2,0,2)`;
- fixed `varpi` gives `delta T=-delta B_Z`, `delta A=delta F_A=0`, so field
  motion contributes through the single source-graph momentum
  `p=E_B-E_T`; and
- lowerer and observation act after variation, converting a density Euler
  covector to a field-like complete-germ equation.  Their derivatives are
  live in the Hessian/Green receiver, but they are not extra summands in the
  metric Euler covector.

Thus the intrinsic first variation is density plus the source-graph formal
adjoint, independent of how the fused naturality packet is split in
coordinates.  This is the object required for stationarity.

## Exact source-graph closure

In covariant symmetric-coframe coordinates,

```text
delta omega_(mu ab)
  = 1/2 (nabla_b h_(mu a)-nabla_a h_(mu b)).
```

The coefficient map from the forty components of `nabla h` has exact rank
twenty.  It has no additional free zero-order metric owner in this covariant
presentation.  Formal adjunction therefore differentiates the momentum.
Since the compatible extension has all `14 x 196` entries of `j1p` equal to
zero, the metric graph return is zero independently of the adjoint sign.

This is not a vacuous zero: a planted nonparallel momentum derivative fires
the transpose of the same rank-twenty coefficient map.

## Exact density row

The branch first-action scalar density is

```text
L_1 = -t(27+728t^2).
```

In the quadratic root algebra it reduces to

```text
-33703t/936 + 3/104.
```

Multiplying by the exact trace-reversed gimmel density covector gives the row
displayed above.  The coordinate-volume convention multiplies every entry by
the already-owned factor eight and does not alter its zero locus.

The printed residual is zero on the branch family, so the residual-square
second action has zero first variation.  It cannot cancel the nonzero
first-action trace at this same point.

## Hostile boundaries

- This kills the declared compatible parallel extension, not every possible
  second jet over the two point roots.
- It does not kill every canonical-Zorro reconstruction or a genuinely
  distinct action-owned trace sector.
- Ordinary observation pullback has a rank-ten conormal kernel and may not be
  used to hide the metric row.  Complete-germ equation transport preserves
  the variational stationarity question.
- A new adjustable counterterm is not inferred or authorized.  Any trace
  cancellation must already be action-owned and must preserve the translation
  and primitive-epsilon equations on the same branch.
- No open solution, total deformation complex, positive physical cohomology,
  superposition law, Born rule, spectrum or empirical prediction follows.

No ledger, canon, residue, quotient datum or public posture changes.

## Reverse-scaffold consequence

VRS-5 does not proceed to higher prolongation on this extension.  Its next
move is to rerank the honest background reopeners:

1. a genuinely distinct canonical-`B_Z` branch or compatible nonparallel jet
   whose complete source rows close; or
2. an already action-owned trace sector that cancels the exact rank-one row
   without a fitted coefficient and without reopening a failed translation or
   primitive-epsilon equation.

Until one constructs, VRS-6 and VRS-7 remain conditional only and the null
`H0` is strengthened without being proved.

The exact Sage probe passes `38/38`.
