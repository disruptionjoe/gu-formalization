---
title: "K192 order-six radially stratified wide-gap wave"
document_role: active_research
doc_type: conditional_native_K139_K192_order_six_radially_stratified_wide_gap_result
created: 2026-09-10
date: 2026-09-10
claim_ceiling: rigorous repository-owned outward positivity boxes for every K186 size-two and size-three regularizer on the union of minimum cross argument 2^-200 through 1/8 with row and column spread at most 1/48 of the local cell base, and 1/8 through 1/4 with K191 spread at most 1/512; no arbitrary gap-ratio cover, noncoalescent face atlas, Duffy/Jacobi chain-rule remainder, complete positive-core error, outward order-six total, accurate prefix, action column, residual, complement or flux floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation
manifest: lab/process/k192-order-six-radially-stratified-wide-gap-wave.json
solver: tests/channel-swings/k192_order_six_radially_stratified_wide_gap.py
probe: tests/channel-swings/k192_order_six_radially_stratified_wide_gap_probe.py
target_claim: INTERNAL_TARGET:K191_RESIDUAL_GAP_RATIO_STRATIFICATION_GATE
target_claim_verdict: LOW_RADIAL_CORE_CERTIFIED_TO_1_OVER_48_RELATIVE_SPREAD__HIGH_RADIAL_K191_CAP_RETAINED__ARBITRARY_GAPS_AND_DUFFY_JACOBI_COMPOSITION_REMAIN_OPEN
canon_verdict_change: none
---

# K192 order-six radially stratified wide-gap wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: K186's fixed K139/K156 hard-core `C3` impurity control, auxiliary chart
`lambda=256`, equal couplings one, three normalized K162 zero-bath seed orbits,
and complete K184--K191 order-six determinant family. K192 changes no operator,
extension, state, domain, polarization or scalar center. It stratifies K191's
single uniform gap rule by radial scale.

```gu-typed-objects
result: all 53 K186 size-two and size-three regularizer patterns are outwardly positive on the union of 1,584 new low-radial cells covering minimum cross argument 2^-200 through 1/8 with row and column spread at most 1/48 of the local cell base, and 64 retained K191 high-radial cells covering 1/8 through 1/4 with spread at most 1/512; low-radial minimum directed-Arb lower bounds are 0.903917060000822 and 0.06683241948485374 for sizes two and three
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on the order-six output sector; K184 coherent path-pair assembly and K186 specieswise determinant factorization remain unchanged ON=repository_signed_point_control
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, positive Laplace times, sequential confluent divided differences, normalized Bessel K1 kernel, exact Cauchy determinant cofactors, order-16 Taylor arithmetic and python-flint 0.9.0 / FLINT 3.6.0 Arb balls
grading: K186 exact Cauchy--Vandermonde skeleton, K188 four-region split, K190 exact coalescent scaled-q spine, K191 uniform near-coalescent cover, K192 radial-gap stratification with a 32/3 low-radial spread gain, complete 53-pattern and 468-occurrence propagation, independent 200-digit controls and withheld residual-gap/cubature release
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W, K179 fixes the coefficient family, K184 fixes the time Grams, K185/K188 fix boundary majorants and K186/K187/K189/K190/K191 fix factorization, endpoint class, raw-kernel failure, coalescent coordinates and the normalized Taylor theorem; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: prove the first substantial radial stratification of K191's residual gap domain, retain the complete high-radial certificate and expose the exact noncoalescent-face atlas still needed before Duffy/Jacobi composition MAP-TYPE=intertwiner
```

## Inline preflight bookend

K191 uses one `1/512` row/column-spread radius over its whole radial range.
That radius is valid but need not be uniform: the Bessel correction approaches
the exact Cauchy core as the base argument decreases. K192 first asks whether
radial stratification removes a material part of the residual simplex before
introducing a new noncoalescent chart.

The strongest independently framed source-to-physics challenger remains a
positive physical state/domain and quotient construction. `SC-META-53` stays
`UNCERTAIN`; `LT-SM8`, `LT-GR6b`, `RA-F1` and `AC-F1` stay `NEEDS`. K192 does
not supply their action-owned background, BV/BFV domain, positive cohomology,
count observable or chiral carrier.

The route comparison rejected raw determinant boxes because they repeat K189's
correlation loss, and rejected Taylor-order escalation because the remaining
failure is centering rather than a missing derivative. A full face atlas
remains the long route. The selected discriminator keeps K191's exact theorem
and searches the largest cheap radially stratified outward domain first.

## 1. Unchanged normalized theorem

K192 keeps K191's exact identity

```text
F_x(y)=x K1(x(1+y)),
R_m=det([r_0,...,r_i][c_0,...,c_j]F_x(w+r+c))
    product_(a,b)(1+w+r_a+c_b).
```

The pure Cauchy part `G(y)=(1+y)^-1` still cancels the normalizing product
symbolically. Only the Bessel correction is enclosed. On the new low-radial
stratum the order-16 tail uses the actual maximum offset, cell radial width
plus twice `1/48`; its global maximum is `1/6`.

## 2. Radially stratified outward certificate

The new low-radial domain is

```text
2^-200 <= x <= 1/8,
row spread <= x/48,
column spread <= x/48.
```

Adaptive radial subdivision uses eight cells on every dyadic band through
`[2^-5,2^-4]` and sixteen on `[2^-4,2^-3]`, giving 1,584 contiguous cells.
Every size-two and size-three cell is strictly positive.

| Size | minimum `R_m` lower | maximum `R_m` upper | maximum entry-tail radius |
| --- | ---: | ---: | ---: |
| 2 | `0.903917060000822` | `1.096082939999178` | `1.1569950859349202e-9` |
| 3 | `0.06683241948485374` | `1.9331675805151463` | `2.186720712416999e-6` |

On `1/8<=x<=1/4`, K192 reuses exactly the 64 K191 cells for each size and
their `1/512` spread rule. The two strata meet at `x=1/8`, so the union has no
radial gap. The low-radial relative-spread radius increases by `32/3`.

## 3. Family propagation and controls

The enclosure depends only on determinant size and the stated stratum. It
therefore propagates to all 45 size-two and eight size-three K186 patterns,
all 468 nontrivial occurrences, all 234 entries and all 18 coherent groups.

Independent 200-digit divided-difference values at `x=2/25` and gap `1/96`
are `0.9686848420292676069268591` and `0.9404341263109263859305443` for
sizes two and three. They lie inside the corresponding outward intervals.
They are controls, not the proof.

A pre-registered size-three cell at base `31/256`, upper endpoint `1/8` and
candidate radius `1/32` fails strict positivity under the same enclosure. This
rejects that coarse candidate for the current method. It does not prove that
`1/48` is mathematically maximal or that a recentered chart cannot cover it.

## 4. Honest boundary and continuation

The remaining domain is spread greater than `1/48` below `x=1/8`, or greater
than `1/512` above it. K192 does not cover arbitrary gap ratios and does not
serialize a noncoalescent face atlas, Duffy/Jacobi chain-rule envelope or
weighted-cubature remainder. The accurate certified prefix remains through
order five.

The next exact work is to partition the residual by its maximal row/column
gap, recenter the complete normalized divided-difference matrix on each
noncoalescent face, and derive outward local tails there. Only a complete union
over every K186 time-node pattern can release the core remainder and later
K171/K168 action-column work.

## Reproduction

```bash
_local/cas-venv/bin/python tests/channel-swings/k192_order_six_radially_stratified_wide_gap.py --summary
_local/cas-venv/bin/python tests/channel-swings/k192_order_six_radially_stratified_wide_gap.py --write --summary
_local/cas-venv/bin/python tests/channel-swings/k192_order_six_radially_stratified_wide_gap_probe.py
_local/cas-venv/bin/python tests/channel-swings/k192_order_six_radially_stratified_wide_gap_probe.py --no-replay --selftest
```

The probe checks the complete union topology, exact domain boundary, all
pattern/occurrence propagation, independent controls, the rejected `1/32`
candidate and every withheld-release fence. Its baseline has 20 checks with
deterministic replay and the hostile selftest catches 19/19 mutations.

## Inline postflight bookend

K192 establishes that K191's global `1/512` radius was controlled by the
high-radial cap, not by the small-argument core. Radial stratification alone
certifies a more than tenfold wider gap neighborhood on nearly the entire
logarithmic range. It also localizes the remaining obstruction: further
coverage requires noncoalescent recentering, not more uniform coalescent
subdivision. No physical, source, canon, paper or publication effect follows.
