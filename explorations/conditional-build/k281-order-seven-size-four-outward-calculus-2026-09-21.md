---
title: "K281 size-four scaled-jet theorem and outward-calculus boundary"
status: internal_structural_result
claim_verdict: internal_structural_result
date: 2026-09-21
claim_ceiling: exact R4 scaled-jet normal form, directed coalescent certificate and complete 63-mask size-four face topology; the tested zero-centered Taylor transfer fails to certify a finite-gap cell and no arbitrary-gap enclosure, mixed-Duffy derivative, Jacobi cubature error, action-column value, residual, exterior gap, native K152 interval, physical state, source claim, ledger row, canon, paper or public posture is changed
manifest: lab/process/k281-order-seven-size-four-outward-calculus.json
producer: tests/channel-swings/k281_order_seven_size_four_outward_calculus.py
probe: tests/channel-swings/k281_order_seven_size_four_outward_calculus_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: SIZE_FOUR_COALESCENT_NORMAL_FORM_CLOSED__SHIFTED_FACE_TAIL_OPEN
canon_verdict_change: none
---

# K281 size-four scaled-jet theorem and outward-calculus boundary

> **GU-COMPARATOR-ROUTING:** This artifact tests a repository-supplied
> conditional Fock construction. Read
> `lab/methods/source-native-comparator-routing.md` and the current source and
> physics ledgers before transferring it to a GU-native physical state.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

```gu-typed-objects
result: the unique order-seven size-four Bessel regularizer has an exact cancellation-free scaled-jet Hankel normal form, a directed positive coalescent spine and a complete six-gap face topology; the inherited zero-centered Taylor enclosure does not certify a finite-gap cell
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), restricted to the K162 zero-bath seed orbits LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Gram pairing after complete normalized specieswise exterior projection PAIRING-TYPE=hilbert
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, real Bessel K1 kernel and mixed Newton divided differences
grading: conserved incidence charges, impurity seed, bath species, order seven, K179 contraction identity, cumulative-time position, primitive face support and six active size-four row/column gaps
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W, and K179 fixes the coefficient family; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: provide the size-four outward value calculus needed before determinant-preserving Jacobi composition MAP-TYPE=intertwiner
```

## Preflight bookend

K280 exposes one genuine size-four canonical position pattern, occurring 24
times in the complete order-seven atlas. Its high-precision divided-difference
controls are positive, but they are not outward intervals. Direct raw
determinants are especially unreliable near a face because they subtract the
same Vandermonde zero that the normalized regularizer divides out.

The selected route therefore keeps the divided-difference geometry and first
removes the radial singularity algebraically. Source and physics status remain
unchanged: `SC-META-53` is `UNCERTAIN`; `LT-SM8`, `LT-GR6b`, `RA-F1` and
`AC-F1` remain `NEEDS`.

## Exact size-four scaled-jet normal form

Let `q(x)=x K1(x)`, `s=x^2`, and `a_n=x^n q^(n)(x)`. The exact recurrence

```text
a_(n+1) = x d(a_n)/dx - n a_n,
x dq/dx = -s K0,
x dK0/dx = -q
```

eliminates every negative radial power before interval evaluation. K281
serializes `a_0,...,a_6`, which are sufficient for the size-four coalescent
Hankel determinant. With

```text
b_n = x^(n+1) K1^(n)(x)
    = n! sum_(k=0)^n (-1)^(n-k) a_k/k!,
R4 = det[b_(i+j)/(i! j!)]_(i,j=0..3),
```

the limit matrix is the signed Pascal matrix and has determinant exactly one.
Directed Arb evaluation at all 199 dyadic radii `x=2^-2,...,2^-200` proves
every scaled `R4` ball strictly positive. Every raw derivative-Hankel ball
overlaps its scaled counterpart; the scaled route is at least 1.40004 times
narrower in the tested width comparison. This is a certified coalescent spine,
not coverage of nonzero gap ratios.

## Complete face topology and failed transfer

The size-four pattern has six row/column gap coordinates. K281 serializes all
`2^6-1=63` nonempty active-gap masks and checks the exact rational
Cauchy--Vandermonde identity on an interiorization of every mask. Continuity
therefore fixes the normalized Cauchy value as one on every named face
closure. This closes the combinatorial face atlas that K280 left implicit.

K281 also executes the actual K191/K192-style zero-centered Taylor transfer.
Mixed divided differences of each monomial are bounded by exact multinomial
and complete-homogeneous formulas; order-16 tails use complete monotonicity,
and the Cauchy entry is enclosed directly by Hermite--Genocchi. The search
tests the low radial bands from `2^-200` to `2^-3`, the high band through
`1/4`, gap radii down to `1/512` and `1/2048`, and radial subdivisions through
256. No finite-gap size-four cell survives the determinant perturbation
bound.

That result is a method failure, not a theorem of nonpositivity. A separate
420-digit finite-gap control at `x=2/25` and relative gap `1/1024` remains
strictly positive. The failed enclosure becomes insensitive to finer radial
subdivision near a radius of two because it takes absolute values of residual
entries about a zero-centered Cauchy matrix. More subdivisions do not repair
that lost correlation.

The exact successor is a shifted Hermite--Genocchi face-center tail operator
on the 63 size-four masks, together with the residual size-two and size-three
faces. Only after those outward values and mixed Duffy derivatives propagate
across all 98 patterns and 996 occurrences may the exact face weights be
composed with a determinant-preserving Jacobi remainder.

## Postflight bookend

The producer freezes the exact jets, all 199 directed coalescent rows, every
tested failed finite-gap cell family and all 63 face masks. The independent
probe reconstructs the Pascal limit, checks the manifest and face power set,
replays the producer, and rejects hostile mutations to a jet, radius, mask,
transfer verdict, action-column ceiling and ledger posture.

Strongest overclaim: the positive dyadic spine or finite-gap decimal control
is an arbitrary-gap outward interval. Neither is. Strongest contrary case:
the failed zero-centered transfer proves the regularizer becomes nonpositive.
It does not; only that enclosure fails. Weakest reproducibility seam: the next
implementation must center its remainder on each face chart and retain the
shared determinant correlation rather than bounding every matrix entry
independently.

K281 closes the exact size-four coalescent normal form and complete face
topology. Shifted face-center outward values, mixed Duffy derivatives,
determinant-preserving Jacobi error, the coefficient-complete action column,
complete residual, exterior gap and native K152 interval remain open. There is
no action-column value and no source, ledger, physical, canon, paper or public
posture change.
