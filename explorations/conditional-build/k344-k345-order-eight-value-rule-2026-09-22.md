---
title: "K344--K345 order-eight native value rule"
status: internal_numerical_control
claim_verdict: internal_numerical_control
date: 2026-09-22
claim_ceiling: exact order-eight product Gauss--Laguerre rule, cumulative-time zero/coalescence face atlas and complete 1,296-entry coherent node evaluator; the eighteen global Peano remainder integrals, complete order-eight integral, action column, R_ref, K152 interval, source/ledger move, canon, paper and public posture remain open
manifests:
  - lab/process/k344-order-eight-gauss-laguerre-face-atlas.json
  - lab/process/k345-order-eight-group-interval-evaluator.json
producers:
  - tests/channel-swings/k344_order_eight_gauss_laguerre_face_atlas.py
  - tests/channel-swings/k345_order_eight_group_interval_evaluator.py
probes:
  - tests/channel-swings/k344_order_eight_gauss_laguerre_face_atlas_probe.py
  - tests/channel-swings/k345_order_eight_group_interval_evaluator_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: ORDER_EIGHT_NATIVE_NODE_VALUE_CLOSED__GLOBAL_PEANO_REMAINDER_OPEN
canon_verdict_change: none
---

# K344--K345 order-eight native value rule

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY` for K344 and
`INTERNAL_NUMERICAL_CONTROL_ONLY` for K345.

```gu-typed-objects
result: the complete order-eight K179 coherent family has one exact positive product Gauss--Laguerre node rule, a support-mask face atlas, and a rigorous group-level value at that node; its global second-derivative remainder is not yet bounded
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), restricted to the K162 zero-bath seed orbits LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Gram pairing after complete normalized specieswise exterior projection PAIRING-TYPE=hilbert
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering and real Bessel kernels
grading: conserved incidence charges, impurity seed, bath species, bath number eight, K179 contraction identity and ordered momentum provenance
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W and K179 fixes the coefficient family; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: evaluate the exact order-eight positive value rule without losing any coefficient, contracted position, determinant zero or coherent cross term MAP-TYPE=intertwiner
```

## Preflight bookend

K343 closes the representation fork: order eight is the last unresolved order
whose species determinants have rank at most four, but none of K299's weights,
K305's four-group border, K334's cover, K340's split atlas or K341's
normalization is order-independent. The cheapest native route is therefore to
return to K279's eighteen positive Laplace times themselves.

Each axis has measure `exp(-256*x) dx`. Its one-node Gauss--Laguerre rule has
node and weight `1/256`; the remainder has the positive Peano kernel

```text
K(t) = exp(-256t)/256^2 - (1/256-t)/256,  0 <= t <= 1/256,
K(t) = exp(-256t)/256^2,                  t >= 1/256.
```

This dominates a literal order-seven port because it derives order eight's
measure and remainder directly. An accurate K218 value remains dependency-
limited by the absent instantiated K152 margin, and the rank-five order-nine
calculus depends on first validating this rank-four consumer.

## K344: rule and face atlas

The tensor rule samples all eighteen time variables at `1/256` with total
weight `256^-18`. Radializing the same node gives
`rho=18/256=9/128`, `theta=1/2`, both nine-coordinate simplex barycenters,
radial weight `17!/256^18` and angular weight `1/17!`; their product exactly
replays `256^-18`.

Every cumulative time is explicit:

```text
T_j = s_j + ... + s_9,     U_j = v_j + ... + v_9.
```

K344 audits all `1,296` upper-triangle entries and records `49` distinct
Bessel-argument zero masks plus `16` row- and `16` column-coalescence masks.
The atlas covers `2,592` old-position kernel uses and `24,048` determinant-
entry kernel uses. A recorded suffix mask states exactly which axes must vanish
before an argument reaches zero; an intervening-axis mask states exactly when
two cumulative nodes coalesce and determinant rows or columns become equal.
The atlas digest is
`sha256:2cb58a2ea7e0de1f4433c0c5da46922988e73e9ad5aa46d2373d4341e32420dd`.

## K345: complete coherent node value

At the native node, K345 evaluates every Gram entry as

```text
[2 K1(T_old)] [2 K1(U_old)] product_species det[2 K1(T_i+U_j)].
```

It then forms each complete `c_G^T K_G c_G`, using every diagonal once and
every off-diagonal twice. All `1,296` Gram entries are strictly positive at the
node, all transpose checks pass, and a separate full ordered replay of all
`2,400` coefficient terms agrees with the upper-triangle assembly. The raw
complete quadratic form is enclosed by

```text
[6.63953906349498e16, 6.6395390634949816e16].
```

Applying `256^-18` and the native `(2*pi)^-10` factor exactly once gives the
strictly positive one-node rule value

```text
[3.1047016409619726e-35, 3.1047016409619737e-35].
```

This is a rigorous interval for the cubature rule value, not for the complete
order-eight integral.

## Postflight bookend

K344's producer and probe replay the complete K343 interface, normalization
and face atlas (`15/15` controls; `17/17` hostile mutations rejected). K345's
Arb calculation is independently replayed with a SciPy double-precision
determinant backend and the full ordered term sum (`16/16` controls; `19/19`
hostile mutations rejected). The
strongest overclaim is that a tiny node value makes the order-eight integral
tiny; it does not, because none of the eighteen Peano derivative integrals has
yet been bounded. The strongest contrary construction is literal K341 reuse;
K344 uses different native variables, weight and prefactor and imports none of
the order-seven cubature data. The weakest seam is now the global face
derivative bound: exact zero/coalescence ownership is known, but the scaled
second derivatives must still be integrated.

Next differentiate the complete group quadratic forms twice along each of the
eighteen Gauss--Laguerre axes, take scaled limits on the exact K344 face atlas,
and integrate the positive Peano kernels. Only after that remainder is joined
may the complete order-eight integral be enclosed or the rank-five order-nine
calculus begin. No source claim, physics-ledger row, canon, paper, public or
physical posture changes.
