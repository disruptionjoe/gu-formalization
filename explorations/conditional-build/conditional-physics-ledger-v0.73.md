# Conditional physics ledger v0.73

## Meter

```text
Ledger v0.73 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 5 scoped
```

## Migration from v0.72

The K77 group owner is no longer the open part of the edge problem. The
global chimeric-spin construction already builds
`P_H = P_Spin(C) x U(64,64)`, containing the real `Spin(7,7)` carrier and
its invariant split form. No K95 `Sp(32,32;H)` or right-H machinery is
imported.

For a connection holonomy with independent source/target gauge action, the
exact cotangent dressing

```text
X  -> h_s^-1 X h_t
P  -> h_s^T P h_t^-T
u_s -> u_s h_s,  u_t -> u_t h_t
q  = u_s X u_t^-1
pi = u_s^-T P u_t^T
```

is invariant. On a generic noncommuting rational fixture, the pulled-back
canonical form has rank eight on sixteen dimensions and its eight-dimensional
kernel equals exactly the source-and-target gauge orbit. The nonlinear
groupoid quotient is therefore exact and K77-compatible.

The decisive comparison is negative. Its identity tangent is

```text
delta q = (delta g3 - delta phi3) - (delta g0 - delta phi0).
```

Matching the holonomy potential to the v0.70 two-endpoint potential forces
`p0=p2=-P`. A single holonomy therefore retains only the Gauss-diagonal
`20/20` subspace of the full ten-normal v0.70 quotient `40/40`. It cannot be
the full nonlinear globalization.

The successor must derive two continuum endpoint evaluation maps and their
primitive epsilon preboundary momenta from the selected action, then dress
both copies without holonomy compression. Source epsilon is a typed candidate
for the endpoint frames but is not yet the action-derived BFV owner.

Five rows migrate in distance, evidence and mapping grade: `LT-GR1`,
`LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`. Verdicts, coverage, residue,
forks, five scoped quotients, P1/P2/P3, canon and public posture are unchanged.

## Frontier

```text
headline_delta: NONE
frontier_conditions_closed: 2
frontier_conditions_opened: 1
remaining_named_conditions: 2
```

Next derive the two continuum endpoint evaluation maps and primitive epsilon
preboundary pair from the action. Apply direct-sum K77 edge dressing without
the killed single-holonomy compression, then prove full `tau_A0`, global
moment-map/BFV and common-domain descent.

Machine truth: `lab/process/conditional-physics-ledger-v0.73.json`.
