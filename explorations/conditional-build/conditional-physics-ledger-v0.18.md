---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-05
status: CURRENT_APPEND_ONLY_LEDGER
machine_ledger: lab/process/conditional-physics-ledger-v0.18.json
predecessor: explorations/conditional-build/conditional-physics-ledger-v0.17.md
---

# Conditional physics ledger v0.18

## Meter

```text
Ledger v0.18 — 82/82 active target rows mapped (100%)
33 SAME · 19 DIFFERS · 24 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

The denominator, verdict counts, residue and quotient count are unchanged.
Four row distances migrate after the selected-cubic numerator gate.

## What moved

The inherited constant-`theta` `hh` block does not determine the moving cubic
on the physical shell.

- `theta-q0-q0`: the compact-core bulk numerator is exactly zero because it
  is proportional to the two massless external inverse propagators.
- `theta-q0-qm`: an `hh`-only completion is nonzero, while a full-pencil
  field-redefinition completion has the same inherited `hh` block and is zero
  on shell.
- unrestricted preboundary/BFV charge, `Q1`, physical sheet and native `Y14`
  quantum state space remain open.

Rows `LT-GR2b`, `LT-GR3`, `LT-GR5` and `LT-SM8` receive explicit migration
edges. `LT-SM8` stays `NEEDS` but its reason kind changes from
`PROVEN_UNSUPPLYABLE` to `MISSING_CONSTRUCTION`; the old kinematic shell is not
a positivity no-go once the diagonal numerator cancels and the mixed numerator
is unselected.

## Current highest-information gate

Construct the complete moving third derivative of the selected action on the
mixed and massive shells, including `h-v`, observation, Shiab/Hodge/pairing,
soldering, compensator and preboundary responses. Then restrict its reduced
Hamiltonian class to the exact shells before asking about `Q1`.

P1/P2/P3 remain unused. Curt remains formally separate and no third Lane is
promoted.
