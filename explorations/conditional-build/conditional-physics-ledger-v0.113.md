---
title: "Conditional physics ledger v0.113"
status: current
doc_type: ledger_summary
created: "2026-08-09"
---

# Conditional physics ledger v0.113

`82/82` targets remain mapped: `32 SAME`, `19 DIFFERS`, `26 NEEDS`, and
`5 OVER-DETERMINED`. Residue remains `84..86` continuous parameters, at least
19 function-valued slots and nine discrete forks. Five scoped quotients remain
booked.

The exact nonzero endpoint momentum has now been evaluated on the actual named
boundary orbits. On both algebraic branches,

```text
Theta = t Phi1,
P = (-312 b^2-t) Phi1,
[Theta,P] = 0.
```

Thus the residual right-`tau_A0` adjoint moment map vanishes for every one of
the 16,384 Clifford generators. The primitive epsilon endpoint orbit is a
different object: its two endpoint values are independent and the same
nonzero coefficient gives rank 14 at each endpoint, rank 28 jointly.

The endpoint coefficients are the Galois conjugates
`(-3+2 sqrt(3))/416` and `(-3-2 sqrt(3))/416`, with opposite real signs. Both
survive as charged sectors or as dressed cotangent values under the unique
minimal edge completion `(-1,+1)`. A zero-charge horn excludes both. None of
these local horns selects or normalizes a branch.

This is a local orbit and boundary-horn classification, not a global BFV phase
space. The selected Spin-native, two-half `U(32,32)` product and full
`U(64,64)` remain distinct action parents. The global trace space,
polarization, complete functional tangent and analytic domain remain open.

Seven rows migrate in distance, frontier grade and evidence only: `LT-GR1`,
`LT-GR2b`, `LT-GR2c`, `LT-GR2d`, `LT-GR3`, `LT-GR5`, and `LT-GR6`. No verdict,
reason kind, residue, booked quotient, datum, canon statement or public posture
changes. P1/P2/P3 remain unused.

Next: construct the global functional trace space and BFV polarization for the
primitive-epsilon endpoint orbit, comparing the charged and minimal-edge
horns. Keep parent selection and complete global tangent separate before
Hessian/BV/common-domain work.

Machine ledger: `lab/process/conditional-physics-ledger-v0.113.json`.
