---
title: "Selected-K138 native I1B T=0 null-stratum covariant quotient transport"
status: active_research
doc_type: exact_null_schur_covariance_ricci_flat_threejet_hamilton_stratum_and_subprincipal_type_gate
created: "2026-08-16"
registry: lab/process/selected-k138-native-i1b-t0-null-stratum-covariant-transport.json
probe: tests/channel-swings/selected_k138_native_i1b_t0_null_stratum_covariant_transport_probe.py
grade: "K138 PROVES THAT K136'S FIVE GAUGE-REDUCED NULL CLASSES DO FORM A COVARIANT VECTOR BUNDLE ON ONE SMOOTH NULL STRATUM, EVEN THOUGH K137 CORRECTLY PROVED THAT THEY DO NOT EXTEND AS ONE CONSTANT-RANK BUNDLE THROUGH NONNULL AND SPACELIKE-SHELL STRATA. AT THE REFERENCE NULL COVECTOR AND AN EXACT RATIONALLY ROTATED NULL COVECTOR, THE COMPLETE ACTION-DERIVED CLIFFORD PACKET GIVES THE SAME METRIC SCHUR LAW S_NULL=-48 ELL_N ELL_N^T. ITS RADICAL HAS DIMENSION NINE, THE DIFFEOMORPHISM IMAGE HAS DIMENSION FOUR, AND THE QUOTIENT HAS DIMENSION FIVE. LORENTZ TRANSPORT CARRIES THE RADICAL AND GAUGE IMAGE TO THEMSELVES. H_Q CONSERVES Q, SO A NULL BICHARACTERISTIC DOES NOT DRIFT ONTO THE DISTINCT SPACELIKE SHELLS. AN EXACT RICCI-FLAT BRINKMANN FAMILY SUPPLIES TWO INDEPENDENT CURVATURE-DERIVATIVE THREE-JET PARAMETERS WHILE LEAVING THE NULL PROJECTOR AND QUOTIENT UNCHANGED. THIS CLOSES THE GEOMETRIC PROJECTOR/FRAME PART OF STRATUMWISE TRANSPORT AND CORRECTS K137'S IMPLICATION THAT A THREE-JET IS NEEDED FOR THAT PART. IT DOES NOT YET CONSTRUCT THE ACTION-SPECIFIC DENCKER ENDOMORPHISM: THE COUPLED HESSIAN IS MIXED ORDER AND THE REPOSITORY HAS NOT DECLARED A COVARIANT FIRST-ORDER OR DOUGLIS-NIRENBERG REDUCTION WHOSE INVARIANT SUBPRINCIPAL SYMBOL CAN BE TESTED. K139 OWNS THAT REDUCTION AND THE FIVE-BY-FIVE LEAKAGE TEST."
target_claim: K137_NEXT_GATE__GENERIC_RICCI_FLAT_THREEJET_COVARIANT_SUBPRINCIPAL_EVALUATOR_AND_FIVE_CLASS_NULL_STRATUM_DENCKER_INVARIANCE
target_verdict: NULL_STRATUM_SCHUR_OUTER_SQUARE_AND_FIVE_QUOTIENT_COVARIANT__NULL_HAMILTON_FLOW_CANNOT_REACH_SPACELIKE_SHELL__THREEJET_NOT_NEEDED_FOR_GEOMETRIC_PROJECTOR__FULL_ACTION_DENCKER_ENDOMORPHISM_REQUIRES_TYPED_MIXED_ORDER_REDUCTION_K139
canon_verdict_change: none
---

# Selected-K138 native I1B T=0 null-stratum covariant quotient transport

> **GU-COMPARATOR-ROUTING — scope before inference.** This is a source-native
> first-transgression, real `Cl(7,7)`, null-characteristic, Ricci-flat-jet and
> mixed-order microlocal calculation. Ordinary Einstein, Higgs/VEV,
> family-index, chirality, anomaly, symmetry-breaking and familiar
> particle-spectrum constructions do not adjudicate it without an explicit
> typed bridge. Read `lab/methods/source-native-comparator-routing.md` before
> reuse.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K138 binds the selected displayed `comm/symi/symi` `I1B` Hessian at
`T=0`, nonzero normalized `kappa_1`, the K135 exact distortion inverse and the
K136 null metric quotient. It proves covariance and parallel transport of the
geometric quotient data on one smooth null stratum. It does not construct a
canonical mixed-order reduction, the complete action-specific invariant
subprincipal endomorphism, a crossing law, a closed domain, BFV cohomology,
positivity or physical states.

## Result in plain English

K137 combined two correct warnings—a rank-five null fibre cannot be continued
as the same bundle through fibres of dimensions zero or `46481`, and a frozen
point symbol is not a complete amplitude law. K138 separates the part that can
already be decided.

On the null cone the exact effective metric symbol is not an arbitrary
rank-one matrix. It has the covariant formula

```text
S_null(n) = -48 ell_n ell_n^T,
ell_n(h)  = h(n_sharp,n_sharp).                       (1)
```

The probe derives (1) twice from the full Clifford packet: first at
`n=(1,0,0,1)`, then at the rationally rotated null covector
`n=(1,3/5,0,4/5)`. The second calculation closes in `224` exact distortion
coordinates; its generalized coefficient has power ranks
`118/63/8/4/0`, its finite inverse is exact, and its Schur complement is again
precisely (1), not merely the same rank.

Equation (1) makes the null geometry transparent. Its radical is the
nine-plane `ell_n=0`. The four action-owned diffeomorphisms
`h=n odot xi` lie inside that radical. Lorentz transport carries both spaces
covariantly, so

```text
Q_n = ker(ell_n) / im(n odot -)                       (2)
```

is a rank-five vector bundle on every smooth null patch on which these ranks
remain fixed. This is compatible with K137: (2) is stratumwise and does not
extend across the nonnull or spacelike-shell rank jumps.

There is also no ordinary null-to-spacelike-shell encounter along the
Hamilton flow generated by `q=g^{-1}(n,n)`. Exactly,

```text
H_q q = {q,q} = 0.                                    (3)
```

A null bicharacteristic stays null. The spacelike shells matter for the full
Fourier/domain problem and for other characteristic branches, but they are not
points that the `q=0` Hamilton ray reaches by ordinary propagation.

Finally, an exact Ricci-flat Brinkmann family gives freely variable Weyl and
curvature-derivative data along a central null geodesic. Two independent
third-jet parameters change `nabla R` while the connection vanishes in the
parallel central frame and (1)-(2) remain unchanged. Thus a generic
Ricci-flat three-jet is not needed to define the geometric null projector or
its frame transport. It may enter lower transport coefficients, but it cannot
change the principal quotient just constructed.

The remaining gap is now narrower and better typed. The source Hessian is a
mixed-order block `[[0,A*],[A,C]]`, with second-order metric-curvature map
`A`, first-order distortion map `C`, and zero-order Hodge mass. A Dencker
endomorphism is not determined until one declares a covariant
Douglis--Nirenberg or first-order reduction, its left inverse/principal-type
certificate, and its invariant subprincipal convention. K138 therefore books
the geometric quotient transport and leaves the action-specific five-by-five
amplitude matrix to K139.

## 0. Pre-wave answers

1. **Which fork?** Real `Cl(7,7)`, selected `comm/symi/symi` Shiab, nonzero
   normalized `kappa_1`, `T=0`, and the source-native K127--K137 `I1B` branch.
   No path-dependency chain in `lab/process/path-dependencies.md` covers this
   exact microlocal horn. If a different real form, Shiab, or action branch is
   operative, every coefficient in (1) must be recomputed, though the abstract
   quotient test survives.
2. **Search-space dimension?** The metric shadow is ten-dimensional. The
   null Schur radical is nine, the gauge image four, and the target quotient
   five. The decisive computation is the outer-square identity (1), which
   excludes basis-by-basis transport testing. The rotated action packet has
   `224` distortion coordinates but reduces exactly to the same ten-dimensional
   identity.
3. **New un-owned object?** The geometric quotient introduces none: it is
   built from the action-derived Schur form and action-owned diffeomorphisms.
   The still-missing object is explicitly named and not smuggled in:
   `E_sub^I1B`, the action-specific invariant subprincipal endomorphism after a
   declared mixed-order reduction. Its owner must be the second variation of
   the displayed `I1B` action in that reduction. `free_object_delta=0` for the
   proved result and `1 unconstructed/not booked` for the K139 target.
4. **What dies if this succeeds?** K137's cross-stratum obstruction does not
   die. Its weaker implication that a Ricci-flat three-jet is needed before
   the null projector/frame transport can be evaluated is re-scoped in the
   K137 artifact, `CURRENT-STATE.yaml`, `NEXT-STEPS.md`, and
   `lab/process/CURRENT-RESEARCH-CONTEXT.md`. The assertion that a null
   bicharacteristic must be transported through the spacelike shells is also
   retired by (3). No canon, ledger, particle, phenomenology or GU-wide
   verdict changes.

## 1. Exact covariant null Schur law

For a null covector `n`, raise its index with the horizontal Lorentz metric and
define the metric functional

```text
ell_n(h)=h_mu_nu n^mu n^nu.
```

At the K136 reference direction, exact factorization gives

```text
S_null=-48 ell_n ell_n^T,
rank S_null=1,
dim ker S_null=9.                                     (4)
```

The second direction is not a ten-dimensional covariance assumption. The
probe rebuilds the complete local distortion packet from the K135 action
maps. For `n=(1,3/5,0,4/5)`, the exact first-order generalized coefficient is
nilpotent of index five. Hence

```text
(i C_1+K)^-1 = sum_(r=0)^4 (-i K C_1)^r K,            (5)
```

and direct multiplication certifies (5). Substitution in
`-A* (i C_1+K)^-1 A` returns the same outer-square law (4) with the rotated
`ell_n`. This is an action-derived exact covariance check, not a numerical
rank comparison.

## 2. Gauge quotient and frame transport

For null `n`, every diffeomorphism column `n odot xi` lies in `ker ell_n`
because

```text
ell_n(n odot xi)=2 g^-1(n,n) xi(n_sharp)=0.           (6)
```

The map has rank four. A rational Lorentz rotation carrying the reference
covector to the second covector carries the complete K136 nine-plane radical
to the second radical and the four-plane gauge image to the second gauge
image. The Schur forms obey the exact congruence law. Therefore the quotient
in (2) has rank five and is geometrically parallel under simultaneous
covector/tensor frame transport.

This result does not assert that all five amplitudes are constant or physical.
It says the carrier on which an action-specific amplitude connection would act
is now a legitimate vector bundle on a smooth null patch.

## 3. Ricci-flat three-jet evaluator and order correction

Use Brinkmann coordinates `(u,v,x,y)` and

```text
g = 2 du dv + dx^2 + dy^2 + H du^2,
H = (a0+a1 u)(x^2-y^2)+2(b0+b1 u)xy.                 (7)
```

The probe computes the complete Levi-Civita and Ricci tensors of (7). Ricci
vanishes identically. Along `x=y=0`, all connection coefficients vanish, the
transverse Weyl profile is trace-free, and its `u` derivative contains the two
independent parameters `a1,b1`. These are exact compatible Ricci-flat
three-jets, not an arbitrary algebraic `nabla R` tensor.

Neither `a1` nor `b1` occurs in `ell_n`, the gauge map, or the quotient rank.
They therefore cannot alter the principal null projector or its natural frame
transport. K137 was right that point two-jet data do not select every lower
amplitude coefficient along a neighborhood; K138 corrects the stronger
ordering claim. The three-jet is downstream of the principal quotient, not a
prerequisite for constructing it.

## 4. Hamilton stratum and first switch

Equation (3) means the null Hamilton trajectory stays on `q=0`. The first
transport switch is therefore not “arrival at one of the 27 spacelike
shells.” The honest local switch condition is

```text
STOP before the first s at which
  rank S_null(x(s),n(s)) != 1,
  rank G_diff(x(s),n(s)) != 4,
  det(i C_1(n(s))+kappa_1 K)=0,
  or the chosen reduced real-principal-type certificate fails.             (8)
```

The first three conditions stay clear on the two exact null controls. The
fourth is not yet evaluable because no covariant mixed-order reduction has
been declared. Equation (8) is a switch, not a gluing law.

## 5. Twenty-lens reassessment

The current hypotheses were:

```text
H_A the five classes do not even form a bundle on the null stratum;
H_B they form a covariant null bundle and the full Dencker law is already fixed;
H_C they form a covariant null bundle, but the action-specific Dencker
    endomorphism still requires a typed mixed-order reduction;
H_D a generic Ricci-flat three-jet changes the principal null projector.
```

| lens | strongest hypothesis | decisive reason |
| --- | --- | --- |
| source criticism | `H_C` | the displayed action owns the blocks, not a chosen reduction |
| Layer-0 semantics | `H_C` | quotient transport and amplitude transport are different objects |
| action custody | `H_C` | `E_sub^I1B` must come from the Hessian reduction |
| exact Clifford algebra | `H_C` | both null packets give (1) |
| Lorentz covariance | `H_C` | exact congruence carries radical and gauge image |
| differential geometry | `H_C` | natural frame transport preserves (2) |
| Hamiltonian flow | `H_C` | `{q,q}=0` keeps null rays off spacelike shells |
| microlocal analysis | `H_C` | a Dencker matrix needs principal-type reduction data |
| Douglis--Nirenberg theory | `H_C` | the Hessian has orders `2/1/0`, not one declared order |
| pseudodifferential calculus | `H_C` | Schur elimination needs an invariant symbol convention |
| metric-jet realization | `H_C` | (7) supplies exact free three-jets without changing (2) |
| curvature sensitivity | `H_C` | `nabla R` is absent from the principal quotient |
| Noether theory | `H_C` | exactly four owned gauge columns are removed |
| BV/BFV | `H_C` | no physical quotient follows from a symbol quotient |
| domain theory | `H_C` | K136's shell/Fredholm obstruction remains |
| hostile falsification | `H_C` | the rotated full packet defeats a frame-artifact attack |
| identifiability | `H_C` | the missing endomorphism is named, not fitted |
| model selection | `H_C` | one outer-square theorem replaces five basis tests |
| superposition strategy | `H_C` | a carrier exists, positivity and states do not |
| program sequencing | `H_C` | K139 now has one exact missing matrix |

Vote: `H_A=0`, `H_B=0`, `H_C=20`, `H_D=0`.

Highest conviction:

1. the five-class quotient is a real covariant bundle on a smooth null patch;
2. K137's cross-stratum rank obstruction remains exact but does not interrupt
   a `q=0` Hamilton ray with a spacelike-shell crossing;
3. generic Ricci-flat curvature-gradient freedom does not change the principal
   quotient; and
4. full Dencker/amplitude transport remains unbooked until the mixed-order
   action operator is reduced and its invariant subprincipal leakage is
   computed.

## 6. Reverse scaffold and next gate

Retaining Variancer's reverse conditional from the superposition hypothesis:

```text
R0 superposition hypothesis:
   observable superposition, if GU derives it, lives in positive physical
   cohomology of one source-native linearized theory.

R1 analytic prerequisite:
   that theory needs a closed propagated characteristic carrier.

R2 K136-K138 carrier result:
   on a smooth null patch, Q_n=ker ell_n/im(n odot -) is an exact covariant
   rank-five bundle; it does not extend as the same bundle across all strata.

R3 K138 base transport:
   H_q preserves q=0 and natural frame transport preserves Q_n.

R4 K139 action transport:
   construct a covariant first-order/Douglis--Nirenberg reduction of
   [[0,A*],[A,C]], prove real principal type modulo diffeomorphisms, and compute
   the exact five-by-five action-specific subprincipal leakage/endomorphism.

R5 only after R4:
   choose a closed domain or action-owned boundary law and form KT/BFV.

R6 only after R5:
   construct a positive conserved pairing, complex structure and states.

R7 only then:
   test superposition and any typed attachment to the wider RSAP carrier.
```

K139 must not call the natural frame connection the full Dencker connection.
It must state the mixed orders, reduction map, left inverse, principal scalar,
subprincipal convention and the exact leakage map from the five quotient
classes to their complement. If two admissible reductions give inequivalent
leakage, that nonuniqueness is the result. Joe input is not required.

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k138_native_i1b_t0_null_stratum_covariant_transport_probe.py
```
