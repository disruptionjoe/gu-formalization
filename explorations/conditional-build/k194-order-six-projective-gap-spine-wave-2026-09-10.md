---
title: "K194 order-six projective gap-spine wave"
document_role: active_research
doc_type: conditional_native_K139_K194_order_six_projective_gap_spine_result
created: 2026-09-10
date: 2026-09-10
claim_ceiling: rigorous repository-owned correlated projective-gap spine for the K186 size-two and size-three regularizers on the K193 radial cell 31/256<=x<=1/8, with one shared scale t, exact translated-interval evaluation, 5,120 contiguous directed-Arb cells on 1<=t<=8, an exact join to the K193 fully active face center, and independent 200-digit controls; no transverse ordered-gap atlas, arbitrary-gap coverage, Duffy/Jacobi derivative envelope, complete positive-core error, outward order-six total, accurate prefix, action column, residual, complement or flux floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation
manifest: lab/process/k194-order-six-projective-gap-spine-wave.json
solver: tests/channel-swings/k194_order_six_projective_gap_spine.py
probe: tests/channel-swings/k194_order_six_projective_gap_spine_probe.py
target_claim: INTERNAL_TARGET:K193_ORDERED_GAP_ATLAS_GATE
target_claim_verdict: CORRELATED_PROJECTIVE_SPINE_CERTIFIED_AND_JOINED_TO_K193__TRANSVERSE_ORDERED_SHAPE_ATLAS_AND_DUFFY_JACOBI_COMPOSITION_REMAIN_OPEN
canon_verdict_change: none
---

# K194 order-six projective gap-spine wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: K186's fixed K139/K156 hard-core `C3` impurity control, auxiliary chart
`lambda=256`, equal couplings one, three normalized K162 zero-bath seed orbits,
and complete K184--K193 order-six determinant family. K194 changes no
operator, extension, state, domain, polarization or scalar center. It
constructs one correlated ordered-gap spine inside the atlas K193 withheld.

```gu-typed-objects
result: the K193 shifted Hermite--Genocchi entry operator can retain all four ordered gaps through one shared projective scale t; 1,024 size-two and 4,096 size-three contiguous directed-Arb cells are strictly positive on 1<=t<=8 and 31/256<=x<=1/8, with minimum lower bounds 0.881830322982048 and 0.15617474532762968
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on the order-six output sector; K184 coherent path-pair assembly and K186 specieswise determinant factorization remain unchanged ON=repository_signed_point_control
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, positive Laplace times, sequential confluent divided differences, normalized Bessel K1 kernel, exact translated rational monomial divided differences, one common projective-scale cell supplying all entries before an outward complete-determinant enclosure, order-16 Taylor arithmetic and python-flint 0.9.0 / FLINT 3.6.0 Arb balls
grading: K186 exact Cauchy--Vandermonde skeleton, K188 four-region split, K190 exact coalescent scaled-q spine, K191/K192 outward radial/coalescent union, K193 exact shifted-entry identity and 18 local face charts, K194 correlated scale ray and dependency-loss control, complete 53-pattern and 468-occurrence formula propagation, six independent 200-digit controls and withheld transverse-atlas/cubature release
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W, K179 fixes the coefficient family, K184 fixes the time Grams, K185/K188 fix boundary majorants and K186--K193 fix factorization, endpoint class, coalescent coordinates, normalized Taylor theorem and shifted local operator; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: preserve K193's shifted entry operator while coupling the physically ordered gap coordinates through a shared projective scale, certify a contiguous spine and its exact K193 join, and expose the transverse shape variables as the remaining atlas prerequisite MAP-TYPE=intertwiner
```

## Inline preflight bookend

K193 proves strict outward neighborhoods at every banked face center but does
not retain correlations when different gap coordinates are ranged over a
large common hull. A direct baseline scout showed that the Taylor tail is not
the first loss: separately ranging the gaps and determinant entries destroys
the cancellation even when point values are positive.

The strongest independently framed source-to-physics challenger remains a
positive physical state/domain and quotient construction. `SC-META-53` stays
`UNCERTAIN`; `LT-SM8`, `LT-GR6b`, `RA-F1` and `AC-F1` stay `NEEDS`. K194 does
not supply their action-owned background, coupled BV/BFV complex, positive
cohomology, count observable or chiral carrier.

The selected route keeps the K193 coefficient and tail theorem but substitutes
one shared projective variable before interval evaluation. This is the
cheapest structural discriminator between a genuinely difficult regularizer
and dependency loss in the enclosure.

## 1. Exact translated-interval evaluator

K193 forms every shifted monomial divided difference by the exact substitution
`w -> w-a`. K194 uses the algebraically identical evaluation rule

```text
evaluate(P(w-a,r,c), I_w, I_r, I_c)
  = evaluate(P(w,r,c), I_w-a, I_r, I_c).
```

The second form avoids a new symbolic expansion for every chart. Exact SymPy
identities and overlapping directed-Arb evaluations pass for all 221
size-two/three matrix entries through order 16 at the independent shift
`7/64`. The order-17 complete-monotonicity tail is unchanged.

## 2. Correlated projective spine

For the fully active ordered shape, K194 sets

```text
r0=t/32,   r1=t/64,   c0=t/40,   c1=t/80,
1 <= t <= 8.
```

Thus `r0>r1>0` and `c0>c1>0` everywhere. One common interval cell for `t`
supplies every divided-difference entry and the Cauchy normalization before the
complete interval determinant is evaluated. The entry balls are then combined
outwardly; no affine cross-entry dependency is claimed. The size-two
restriction keeps `r0=t/32` and `c0=t/40`.

The scale range is divided into 64 size-two and 256 size-three cells; each is
crossed with 16 contiguous radial subcells on `0<=w<=1/31`.

| Size | outward cells | minimum `R_m` lower | maximum `R_m` upper | maximum entry tail |
| --- | ---: | ---: | ---: | ---: |
| 2 | 1,024 | `0.881830322982048` | `0.9914652806461046` | `1.11549e-7` |
| 3 | 4,096 | `0.15617474532762968` | `1.6214312314156671` | `1.14651e-4` |

All 5,120 cells are strictly positive and adjacent cell endpoints agree
exactly. At `t=1`, the size-three coordinates are exactly K193's fully active
face center and the radial cell is identical, proving the join rather than
inferring it from nearby decimal values.

Independent 200-digit divided-difference controls at `t=1,4,8` lie inside the
global outward ranges. The size-three values decrease from
`0.8864999310162916342882860` to `0.8554958710559274445603315`; these are
controls only, while the Arb cells carry the proof.

## 3. Dependency-loss control

The independent axis-aligned hull of the same endpoint coordinates ranges

```text
1/32<=r0<=1/4,  1/64<=r1<=1/8,
1/40<=c0<=1/5,  1/80<=c1<=1/10.
```

It produces the nonpositive enclosure
`-365.5575474315812 <= R_3 <= 367.74575437963705`. This hull contains shapes
outside the correlated ray. Its failure is therefore a dependency-loss
control, not a counterexample to positivity and not a statement about the
physical regularizer. K194 proves that preserving one shared projective scale
recovers a substantial continuous domain without raising Taylor order.

## 4. Honest boundary and continuation

The certified ray is one-dimensional in the four-dimensional gap coordinates.
It does not cover transverse ordered shapes and therefore cannot be composed
into the complete positive-radius Duffy/Jacobi remainder. The formula remains
available to all 53 K186 patterns and all 468 occurrences, preserving all 234
entries and 18 coherent groups, but an occurrence inherits K194's new outward
bound only when its normalized gap tuple lies on the declared ray.

The next exact object is a projective shape atlas: introduce ordered transverse
variables such as `r1/r0`, `c1/c0` and `c0/r0`, supply every entry from the
same compact scale-and-shape cell before the outward complete-determinant
enclosure, and tile that compact shape domain. Only
after this atlas closes should the common-chart model be differentiated for
Duffy/Jacobi chain-rule and weighted-cubature bounds.

## Reproduction

```bash
_local/cas-venv/bin/python tests/channel-swings/k194_order_six_projective_gap_spine.py --summary
_local/cas-venv/bin/python tests/channel-swings/k194_order_six_projective_gap_spine.py --write --summary
_local/cas-venv/bin/python tests/channel-swings/k194_order_six_projective_gap_spine_probe.py
_local/cas-venv/bin/python tests/channel-swings/k194_order_six_projective_gap_spine_probe.py --no-replay --selftest
```

The probe checks the exact translated evaluator, complete cell counts and
strict lower bounds, adjacency, the exact K193 join, the dependency-loss
control, six independent controls, complete family propagation, deterministic
replay and every withheld-release fence.

## Inline postflight bookend

K194 turns K193's isolated fully active face center into a rigorously joined
multiscale ordered-gap spine and localizes the next obstruction to transverse
shape coverage. It does not convert that spine into a global atlas. No
physical, source, ledger, canon, paper, release or public-posture effect
follows.
