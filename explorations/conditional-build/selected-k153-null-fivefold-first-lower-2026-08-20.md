---
title: "Selected-K153 null fivefold first-lower coefficient"
status: active_research
doc_type: exact_first_jet_closed_null_action_and_restricted_fivefold_gate
created: "2026-08-20"
registry: lab/process/selected-k153-null-fivefold-first-lower.json
probe: tests/channel-swings/selected_k153_null_fivefold_first_lower_probe.py
grade: "K153 CLOSES THE K152 BRIDGE SEED UNDER THE FROZEN AND FIRST-JET SELECTED CONDITIONAL SHIAB ACTION. THE MINIMAL REFERENCE AND RATIONALLY ROTATED NULL PACKETS EACH HAVE 32 CLIFFORD LABELS AND 448 ONE-FORM/CLIFFORD DIRECTIONS WITH CL(7,7) LOWERER INERTIA (260,188). THE NATIVE GENERALIZED SYMBOL P=K C HAS EXACT POWER RANKS (234,125,16,8,0), REPLAYING FIFTH-STEP NULL NILPOTENCE. VARIABLE COEFFICIENTS PRODUCE A LIVE RANK-FOUR ORDER-FOUR COEFFICIENT IN P^5 AT BOTH NULL COVECTORS, WHILE FREEZING THE COEFFICIENT ERASES IT. AFTER THE EXACT K152 BRIDGE EMBEDDING, THE ORDER-SIX COEFFICIENT OF K P^5 K A IS ZERO AT BOTH COVECTORS: THE LIVE DISTORTION LOWER TERM ANNIHILATES THE RANK-FOUR BRIDGE IMAGE. THIS IS NO FIRST-LOWER RESTRICTED LEAKAGE, NOT A ZERO FULL CURVED REMAINDER. K154 OWNS THE SECOND RESTRICTED LOWER COEFFICIENT WITH THE REQUIRED HIGHER JETS."
target_claim: K152_NEXT_GATE__NULL_FIVEFOLD_FIRST_LOWER_AND_RESTRICTED_BRIDGE_ACTION
target_verdict: NULL_ACTION_PACKET_448D_FIRST_JET_CLOSED__P_POWER_RANKS_234_125_16_8_0__P5_FIRST_LOWER_RANK4_LIVE__RESTRICTED_ORDER6_ZERO_AT_REFERENCE_AND_ROTATED_NULL__LATER_LOWER_ORDERS_OPEN
canon_verdict_change: none
---

# Selected-K153 null fivefold first-lower coefficient

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K153 binds the repository-selected conditional `comm/symi/symi`
distortion operator and K152 mixed bridge on the minimal finite packets closed
under the frozen and first-coordinate-jet action at
`n0=(1,0,0,1)` and `n1=(1,3/5,0,4/5)`. It computes only the top and first-lower
coefficients of `P^5` and `K P^5 K A`. It does not recover Weinstein's
preferred historical Shiab or compute later lower orders, a quotient, inverse,
domain, BFV class, physical mode, positivity, or propagator.

```gu-typed-objects
result: K153 exact null fivefold first-lower coefficient
carrier: LAYER=ambient CHIRALITY=N/A metric 10-packet through a closed Omega1(Cl(7,7)) 448-packet
pairing: DeWitt ON=metric-10 and Hodge/scalar-Clifford ON=distortion-448
real_structure: real Cl(7,7) with exact complex-coordinate bookkeeping
grading: one-form/Clifford grading with finite first-jet action closure
action_owner: repository-construction
target: first-lower restricted fivefold coefficient MAP-TYPE=homomorphism
```

## 0. Route, correction currency, and typed formula

K132--K135 own the frozen selected Euler coefficient and its all-grade null
nilpotence. K147 owns the frozen restricted identity

```text
K P(n)^5 K A(n) = 0,
```

and K148 corrects the metric-side contraction by applying the DeWitt musical
map. K149--K152 supply the missing moving operator, pairing, formal adjoint and
curved bridge. Mechanism-level retrieval found no artifact composing them on
one finite action packet.

Those older inputs predate the 2026-08-17 source-correction registry. Their
load-bearing content is repository-derived Clifford algebra, variational
typing and corrected metric index raising, not a superseded source reading.
K153 uses the K148-corrected chain as-is.

The objects remain distinct:

```text
C = lowered formal-Euler coefficient,
P = K C,
P(n) = frozen generalized null symbol,
P^5 = variable-coefficient differential fifth power,
R = K P^5 K A = restricted metric-to-distortion remainder.
```

## 1. The closed null action packet

K152's bridge image uses eight Clifford labels and 112 directions. That set is
not an invariant distortion carrier: at the reference null covector its
projected frozen raw block is identically zero even though the full action is
live. K153 therefore starts from those eight labels and repeatedly adds every
label reached by the frozen and first-jet moving-Shiab action. At both exact
null covectors the closure stabilizes on

```text
labels = 0,1,...,31,
dimension = 32 * 14 = 448.
```

The native Hodge/scalar-Clifford lowerer is nondegenerate with inertia
`(260,188)`. No output is projected back to the 112-dimensional bridge seed;
the bridge is instead embedded exactly into the closed packet.

## 2. Frozen fifth-step nilpotence and live first lower term

For `P=A(t)D+B(t)`, K153 propagates the top coefficient `T_m` and first-lower
coefficient `L_m` by the exact one-variable Leibniz recursion

```text
T_(m+1) = A T_m,
L_(m+1) = A L_m + A (T_m)' + B T_m.
```

Only `A(0)`, `A'(0)` and `B(0)` can enter this horizon. K151 fixes
`B(0)=-K C_raw'(0)^T/2`; it is not a plain or frozen transpose shortcut.

At both `n0` and `n1`, the frozen principal powers have exact ranks

```text
rank P(n)^j = (234,125,16,8,0),  j=1,...,5.
```

Thus the full 448-dimensional packet reproduces fifth-step terminal
nilpotence. The first variable-coefficient lower term is nevertheless live:

```text
rank [P^5]_4 = 4.
```

Freezing both the principal coefficient jet and the formal-Euler lower term
makes `[P^5]_4` zero. Replacing the native `K` by a positive identity also
destroys the frozen fifth-step law. Frozen symbol data therefore do not
determine this lower coefficient.

## 3. Exact bridge restriction

K152's rank-four null symbol is embedded into the 448-dimensional packet, and
K153 evaluates the first possible lower coefficient after the vanished
order-seven term:

```text
[K P^5 K A]_6 = K [P^5]_4 K A_2.
```

The other nominal order-six contribution contains `P(n)^5` and therefore
vanishes exactly. At both the reference and rationally rotated null covectors,

```text
rank [P^5]_4 = 4,
[K P^5 K A]_6 = 0.
```

The live distortion lower term annihilates the K152 bridge image. Hence there
is no restricted metric-radical or gauge leakage at this first lower order.
This is strictly weaker than a zero curved remainder: coefficients of orders
five and below in `K P^5 K A` require second and higher coefficient jets and
remain unevaluated.

## 4. Claim ceiling and next gate

K153 closes the first lower null coefficient and resolves it as zero only
after exact action closure and bridge restriction. It does not prove
`P^5=0`, `K P^5 K A=0`, preservation at every lower order, or an endomorphism
of `H_n/G_n`.

K154 should compute the order-five coefficient of `K P^5 K A` with the
minimal required second jets, retaining the same reference/rotated closure,
frozen and positive-pairing plants. Only a later nonzero restricted coefficient
can be tested for metric-radical leakage; another zero advances the exact
lower-order boundary without licensing a quotient.

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k153_null_fivefold_first_lower_probe.py
```
