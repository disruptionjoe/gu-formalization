---
artifact_type: construction_result
created: 2026-08-13
status: ACTION_OWNER_FORK_EXPOSED__ENDPOINT_TWO_JET_REPAIR_DOES_NOT_TRANSFER_TO_FIXED_BANK_E_ACT
run_id: RUN-20260813-175109-gu-i2b-action-euler-principal-owner
lane: "1"
functional_channels: [BUILD, COMPOSE, SOURCE, VERIFY]
channels: [Build, Compose, Source, Verify]
ledger_rows: [RA-E1, RA-E3, LT-SM6]
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_PRINTED_ENDPOINT_AND_DISTINCT_FIRST_ACTION_EULER__SOURCE_SILENT_OPERATIVE_SECOND_ACTION_AND_FULL_MOVING_PRINCIPAL_OWNER
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
fork_assumed: none
search_space_dim: "four observed covector directions on the exact 196-real selected connection bank; decided wholesale"
free_object_delta: 0
residue_touched: [RA-E1:T2_DISTANCE_ONLY, RA-E3:T2_DISTANCE_ONLY, LT-SM6:T2_DISTANCE_ONLY]
---

# Selected K77 I2B action-Euler principal owner comparison

## Result in plain English

The previous endpoint calculation was correct for the equation it actually
studied. Its printed residual has `182` live connection responses in each of
the four observed directions, and the ten symmetric second-jet blocks span
the full `196`-dimensional connection equation.

That result does **not** transfer to the action-derived Euler equation. On the
same selected bank, pairing every raw endpoint response against every
connection test using the released first action's own wedge-Hodge pairing
gives the zero `196 x 196` matrix in all four observed directions. The
integration-by-parts principal Euler covector and its unique action-Riesz
representative are therefore also zero on this fixed bank.

The two candidates are distinct PDEs here:

```text
printed endpoint principal rank:       182 in one timelike block
printed endpoint full jet image rank:  196
fixed-bank E_act principal rank:          0 in all four directions
```

Thus the v0.236 two-jet repair remains valid for Weinstein's literal printed
endpoint, but it cannot be reused as a repair for the repository-composed
`||E_act||^2` rival. The program must derive which second-action owner is
operative, or construct the missing moving/full-field terms before deepening
the Spencer campaign.

## Layer 0: the owner split

The following objects are now kept separate:

1. the path-average curvature in the released first action;
2. the literal printed endpoint
   `Upsilon_print = S(F_A) + * kappa T`;
3. the actual first-action Euler covector
   `E_act = S(Fbar) + L_T^! S^! T + * kappa T`;
4. the repository-composed rival `1/2 ||E_act||^2_(Q_u)`;
5. the source-unprinted bosonic primalizer `Q_B` versus the conditional
   observer pairing `Q_u`.

The source carrier's two `C^(32,32)` halves, the block subgroup
`U(32,32) x U(32,32)`, the full `U(64,64)` parent, and the selected K77
connection bank also remain distinct layers.

## Exact comparison

For each observed covector `q_mu`, define the printed-endpoint directional
response

```text
L_mu(delta) = S(q_mu wedge delta).
```

On the ordered 196-real bank, every direction has exactly 182 nonzero raw
responses. Define the first-action pairing matrix

```text
B_mu(test,delta) = <test, L_mu(delta)>_(I1).
```

The exact certificate gives

```text
rank(B_0),...,rank(B_3) = 0,0,0,0
support(B_0),...,support(B_3) = 0,0,0,0.
```

Formal integration by parts yields the principal action covector
`(B_mu-B_mu^T)/2`, which is therefore zero. The action Riesz Gram is diagonal,
nondegenerate, and has inertia `(98,98)`, so zero covector implies zero
representative at this grade; no positivity assumption is used.

At the common radial branch, the repository-composed `E_act` square still has
the twelve nonzero algebraic Euler cells proved in v0.226. A zero selected
second-jet image cannot cancel that nonzero target. This is a fixed-bank
obstruction to transferring the endpoint repair, not a global no-go.

## Why equality at v0.226 was insufficient

V0.226 proved that the printed-endpoint square and the repo-composed
`E_act` square have equal Euler covectors after restricting to one constant
trace-`H_q` background. It also recorded that their Frechet maps differ.

Equality of values at a background does not identify derivatives, principal
symbols, compatibility complexes, or solution jets. The present comparison
computes that missing derivative ownership directly. This is now recorded as
`PD-I2B-ACTION-OWNER` because transferring a Spencer result between those
owners would have deepened the wrong PDE.

## What survives and what remains open

Survives:

- the v0.236 endpoint holonomic-jet theorem, at literal `SC-ACT-04` endpoint
  grade;
- the v0.226 exact algebraic action-Euler square and its twelve-cell branch;
- the source distinction between printed endpoint and first-action Euler;
- every moving/full-field and physical-tangent route not represented by this
  selected fixed bank.

Does not transfer:

- endpoint principal rank `182` to the action-derived Euler owner;
- endpoint full-rank-`196` two-jet reachability to the `E_act` square;
- endpoint Spencer compatibility or its 16-support witness to `E_act`.

Still open:

- whether GU's operative second action is the literal endpoint square, an
  action-consistent `E_act` square, or a fuller source completion;
- the actual moving metric, section, Shiab and `Q_B` principal coefficients;
- source-unitary connection descent and the physical tangent/BV graph;
- nonlinear/Bianchi/atlas closure for whichever operator owns the equation;
- preboundary, domain, positivity, spectrum and physical vacuum.

## Source return and hostile review

The source confirms the printed endpoint, the separate first action and the
actual Euler-covector formula. It does not print the repository-composed
`E_act` norm square, select `Q_u` as `Q_B`, or decide the full moving principal
owner.

The hostile verdict is
`SCOPED_SURVIVAL__ACTION_OWNER_FORK_EXPOSED`. It rejects both overreaches:
calling the endpoint theorem an `E_act` theorem, and calling the selected
zero projection a no-derivative theorem for the full GU action.

## Progress and next gate

```text
Ledger v0.237 — 82/82 mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Headline delta: none
Frontier: 1 named owner-transfer condition closed · 1 owner decision exposed · 3 construction conditions remain
```

Before another nonlinear endpoint prolongation becomes primary, derive or
declare with source grade which second-action owner the Lane-1 construction is
solving. In parallel, construct the full moving `E_act` coefficient and the
physical tangent/BV graph; either can decide whether the selected zero block
persists in the actual theory.
