# Conditional physics ledger v0.123

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.123.json`.

## Progress

`Ledger v0.123 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Six rows move in distance/evidence only. No verdict, residue, quotient, datum,
canon or posture changes.

## What v0.123 adds

The complete primitive-epsilon first-action response to every `1,274`
grade-two equation covector is now exact on the selected real Spin77 parent.
The lower Cartan term in `D_B eta` and every omitted moving-Shiab derivative
vanish coefficientwise on grade two.  The total epsilon block therefore equals
the fixed principal block on both exact branches and timelike, spacelike and
null representatives:

```text
full rank 91 · horizontal rank 6 · off-slice rank 88.
```

The selected-Spin 321-field tangent is not Hessian-closed.  Metric source
columns cannot cancel distinct epsilon source columns.  The construction must
expand the tangent or derive a source/action-owned quotient of the off-slice
equations.  This does not automatically promote all 1,571 low-grade
coordinates, and it is not ported to the two `U(32,32)` halves or full
`U(64,64)` parent.

## Frontier

Conditions closed: `3`; conditions opened: `0`; remaining named: `2`.

Before another heavy Hessian extension, build a durable versioned exact K77
coefficient-bank API with source/construction hashes, stale-cache rejection
and a bounded equivalence replay.  Then complete the ten metric source-Hessian
columns and choose expanded tangent versus a source/action-owned equation
quotient.  Keep first and residual-square actions and all action parents
separate; only afterward add gauge fixing, ghosts, `Dmax/Dmin`, Green/Krein and
coupled BV-BFV.

P1/P2/P3 remain unused.
