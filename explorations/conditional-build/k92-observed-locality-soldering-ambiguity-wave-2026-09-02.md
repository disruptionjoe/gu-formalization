---
title: "K92 observed locality-soldering ambiguity wave"
status: active_research
doc_type: reverse_scaffold_locality_soldering_nonselection_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K91_ABSTRACT_DATA_SELECT_SPATIAL_LOCALITY_NET
claim_ceiling: exact finite nonselection of a spatial tensor net and local detector embedding by the frozen K91 quotient plus the named diagonal generator, state, Z-covariance and fixed-state record weights; no theorem against source-selected locality, continuum AQFT or microcausality, microlocal/Hadamard theory, derived Born law, prediction, confirmation, held-out score or verdict
manifest: lab/process/k92-observed-locality-soldering-ambiguity-wave.json
probe: tests/channel-swings/k92_observed_locality_soldering_ambiguity_probe.py
---

# K92 observed locality-soldering ambiguity wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: exact nonselection of a spatial tensor net and detector embedding by fixed abstract quotient, diagonal generator, state, Z-covariance and record weights
carrier: the same selected P8 subspace of the K91 physical l2 term with two CZ-related three-qubit tensor solderings LAYER=observed CHIRALITY=N/A
pairing: imported finite trace state-effect pairing, held fixed across both spatial nets ON=conditional_control_only
real_structure: computational-basis conjugation, preserved by the real diagonal controlled-Z soldering
grading: three binary site labels in each soldering; no source BV, BFV or ghost grading
action_owner: repository-construction owns both tensor embeddings and their comparison; no GU source action or causal net is supplied
target: whether frozen K91 abstract data plus the K92 diagonal state and covariance select a unique spatial local algebra and detector map MAP-TYPE=evaluation
```

Scope: this result binds only the two finite spatial nets and frozen data below.
It proves nonselection by those data, not that a source action, causal geometry,
interaction law or stronger local-algebra axiom cannot select a net.

## Inline preflight bookend

The problem-matched lens census covered tensor-product structures, finite
factors, algebraic nets, entangling automorphisms, spectral generators,
covariance tomography, detector instruments, operational distinguishability,
gauge quotient descent, source ownership and no-go scope. The decisive route
was an entangling conjugation that fixes all named diagonal data while moving
the transverse local generators. This tests selection directly; another fitted
local model would only show availability.

Retrieval found many abstract commuting-algebra and local-detector controls,
but no packet holding the K91 quotient, a positive gapped diagonal generator,
the K92 state, all named `Z` covariances and the fixed-state record weights
constant while changing the spatial tensor net. Positive controls check two
commuting factor nets, fixed generator/state/covariance and equal frozen record
weights. A separate `|+++>` witness distinguishes the detector maps. The first
kill condition is failure of the two nets to be distinct or failure to hold the
frozen data fixed.

## Two exact tensor solderings

On `P8=(C2) tensor (C2) tensor (C2)`, let the standard site-zero and site-one
algebras be generated by

```text
A0 = alg(X0,Z0),                A1 = alg(X1,Z1).           (1)
```

They commute elementwise. Let

```text
W = CZ_01                                                       (2)
```

and define the primed net by conjugation. Its first generators are

```text
A0' = alg(X0 Z1,Z0),           A1' = alg(Z0 X1,Z1).       (3)
```

The primed algebras also commute elementwise: the two apparent cross-site
anticommutations cancel. But `A0` and `A0'` are distinct. In particular `X0`
is local at site zero in (1) and not in (3), while `X0 Z1` is local at site
zero in (3) and not in (1).

Map computational bit strings to the first eight K91 modes by
`n=s0+2s1+4s2`. The restricted K91 generator is

```text
Omega8 = (9/2) I - (1/2) Z0 - Z1 - 2 Z2,                 (4)
```

with spectrum `{1,2,3,4,5,6,7,8}` and unit gap. Since `W` fixes every `Zi`, it
fixes (4). It also fixes the complete K92 rational Ising state and the named
`Z`-covariance family because both are diagonal. Thus dimension, quotient,
gap, full restricted spectrum, state and the covariances `1/3,1/3,1/9` are
identical across the two nets.

## Equal frozen weights, distinct detector maps

Measure the standard local transverse observable `X0` in the first net and the
corresponding primed local observable `X0 Z1` in the second. Each measurement
has projections `(I +/- O)/2` and an exact controlled-record dilation. On the
fixed diagonal K92 state,

```text
<X0>_rho = 0 = <X0 Z1>_rho,       p_+=p_-=1/2             (5)
```

for both instruments. The frozen state and its one record-weight table
therefore do not distinguish the spatial assignments.

The maps are nevertheless different. On the independent product witness
`|+++>`,

```text
Pr_+(X0)    = 1,
Pr_+(X0 Z1) = 1/2.                                         (6)
```

Equation (6) is not a new physical prediction; it is an exact discriminator
between two repository-selected detector embeddings. Zero extension over the
K91 gauge summand works for either net and therefore selects neither.

## Maximum licensed conclusion

The K91 abstract split quotient and its gapped diagonal generator, even when
augmented by the K92 diagonal state, named spatial covariance data and one
record-weight table, do not uniquely select a tensor-factor net or transverse
local detector observable. Spatial soldering is an additional owner.

This is not a theorem that GU or any source action cannot select locality. A
source causal geometry, interaction density, propagation law, local covariance
functor or net axiom may distinguish the solderings. Those objects are absent
from the frozen control, so the result says nothing about their eventual
selection power.

## Inline postflight bookend

- Strongest overclaim: turning finite nonselection by diagonal data into a
  universal no-locality theorem. The control deliberately excludes source
  action, causal geometry and interaction ownership.
- Strongest contrary construction: a source-owned transverse coupling could
  privilege `X0` over `X0 Z1`; equivalently, adding one observable outside the
  fixed diagonal family can distinguish the nets, as (6) explicitly shows.
- Weakest reproducibility seam: Pauli commutation and the full finite algebras
  are exhausted exactly, but the control is only eight-dimensional. It tests a
  logical ownership gap, not continuum existence or uniqueness.

The exact probe passes `25/25`; its hostile selftest catches `15/15`
soldering, commutation, fixed-data, discriminator, custody, no-go and promotion
mutations. No source, continuum, microlocal, derived-Born, prediction,
confirmation, canon, paper or held-out status moves.

## Next condition

Add one authenticated source object capable of selecting a net: a causal
geometry with local regions, a source interaction density, or a functor from
the source constraint complex to physical local algebras. Test whether it is
invariant under `W` or distinguishes the two solderings. Only a distinguished
owner may convert availability into physical locality.
