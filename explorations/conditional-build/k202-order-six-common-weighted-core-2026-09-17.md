---
title: "K202 order-six common-weighted coherent Gram rule"
document_role: active_research
doc_type: conditional_native_K139_K202_order_six_common_weighted_time_gram_rule
created: 2026-09-17
date: 2026-09-17
claim_ceiling: exact common integrable Jacobi reference weight and a rigorous but non-decision-grade one-node absolute error for all eighteen complete signed K184 order-six time-Gram groups; an archived normalized R3 derivative pilot is rechecked only on one fixed slice and cannot bound the remaining factors or the complete positive core; no accurate order-six prefix, action column, residual, spectral floor, K152 interval, physical/source selection, prediction or confirmation follows
manifest: lab/process/k202-order-six-common-weighted-core.json
solver: tests/channel-swings/k202_order_six_common_weighted_core.py
probe: tests/channel-swings/k202_order_six_common_weighted_core_probe.py
target_claim: INTERNAL_TARGET:K184_ORDER_SIX_COMMON_WEIGHTED_RULE_AND_ERROR_GATE
target_claim_verdict: ONE_COMMON_WEIGHT_AND_COMPLETE_SIGNED_ONE_NODE_ERROR_CERTIFIED__ERROR_TOO_BROAD_FOR_ACCURATE_PREFIX__NORMALIZED_LOCAL_R3_CANNOT_TRANSFER_TO_FULL_INTEGRAND_YET
canon_verdict_change: none
---

# K202 common-weighted coherent-Gram rule

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: the fixed K139/K156 hard-core `C3` impurity and four positive-Fock bath
species on `L2(R)`, auxiliary chart `lambda=256`, equal couplings one, three
K162 zero-bath seed orbits, K184's complete order-six time Grams, K185's exact
positive support allocations, and no selected physical extension or state.

```gu-typed-objects
result: every K185 support allocation embeds into the same 14-coordinate Dirichlet(1/3,...,1/3) reference weight, yielding one deterministic one-node quadrature for each of K184's 18 complete signed coherent groups with a rigorous but broad rational absolute error; one size-three normalized R3 local beta-weighted derivative contribution remains a fixed-slice candidate, not a complete error
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), q=(0,0),(1,0),(0,1), K139 chart, K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on K184's order-six output; specieswise Andreief determinants and full signed coherent path-pair assembly are retained ON=repository_signed_point_control
real_structure: CAR adjoint, real Bessel K1 kernels, positive Laplace times, rational AM--GM allocation and outward Arb one-node evaluation
grading: eighteen coherent seed/output groups, 234 unordered Gram entries, 1,864 support terms, 2,928 ordered terms, fourteen primitive times, left/right old positions and species exterior parity
action_owner: repository-construction -- K139/K156/K179/K184 select the conditional Fock calculation; neither the GU source nor a physical action selects its extension, domain, state, scalar center or observed quotient
target: construct a single weighted rule and a complete rigorous error contribution without mistaking it for a decision-grade order-six action-column prefix MAP-TYPE=intertwiner
```

## Opening and route comparison

K201's third enlarged square is certified, but farther shape shells and the
weighted core error remain open. The September 16 archived review pilot made
the next calculation specific: do not automatically extend another shell;
choose the K184 consumer and compose normalized derivatives with its weights,
Jacobian, remaining factors and signs. K185 already allocates every positive
support term but describes term-specific Dirichlet weights, not a single rule
for a complete signed Gram group. K189's raw global derivative interval is
valid yet hundreds of decimal orders too broad. K186/K191 remove the Cauchy
coalescence cancellation before interval arithmetic. The first structural
route is therefore a common reference measure for the *complete* integrand;
the second is a conditional local regularizer derivative contribution.

The source-facing alternative is the native I1B non-gauge mixed-grade mode
and full fermion insertion on the same background. `SC-ACT-01/02` assert the
source first action/equation, but the September 16 central-Maxwell witness is
a reverse-selected different bosonic model. The native nonzero-kappa
grade-zero equation forbids its pure-central input, and a nonzero metric mode
changes the fermion insertion. That input cannot be borrowed to upgrade this
Fock calculation. `SC-META-53` remains source-`UNCERTAIN`; ledger-v0.263
`LT-SM8`, `LT-GR6b`, `RA-F1` and `AC-F1` remain `NEEDS`. This is independent
mathematics with no physical-recovery vote.

Analytic/probability review chooses exact Dirichlet aggregation before a
fourteen-dimensional grid; orthodox review keeps K171/K168/K152 as separate
endpoint gates; numerical review demands a rule and error for signed groups;
source and philosopher review withhold transfer from a positive-Fock control
to an interacting GU quotient. A raw determinant rule would reintroduce
coalescence cancellation and is not used. The common rule is worth banking
as a reproducible integration instrument even if its first error is broad.

## Exact common-weight theorem

K184's six exterior determinants plus the two old-position factors give
eight `2 K1` factors for each Leibniz support term. K185's exact rational
allocation of each reciprocal support gives loads `alpha_i<=2/3`, hence
`beta_i=1-alpha_i>=1/3` for every one of fourteen primitive coordinates.
Set `x_i=rho z_i`, `sum z_i=1`, and **retain the signed determinants** in
the numerical integrand. Every positive support term obeys

```text
|term measure| <= pi^-8 C_w rho^5 exp(-256 rho)
                  product_i z_i^(beta_i-1) d rho dDelta_13,
C_w=product_(factor,i) w_(factor,i)^w_(factor,i) <= 1.
```

Since `0<=z_i<=1` and `beta_i>=1/3`, each angular monomial is no larger than
`W(z)=product_i z_i^(-2/3)`. This is a common *reference* weight, not a claim
that the physical or original integrand has a Dirichlet distribution. It is
integrable: `int_Delta W=Gamma(1/3)^14/Gamma(14/3)`. The radial reference is
`rho^5 exp(-256 rho)` with integral `120/256^6`. The usual repeated-beta
(Duffy/Jacobi) factorization now supplies one rule for every complete signed
entry; the quotient is the original complete Bessel determinants, two old
factors, coefficient signs and Jacobian divided by this positive reference.
No individual determinant is replaced by its Leibniz expansion during point
evaluation.

The native solver independently replays all 1,276 allocation records, exact
factor weights/support masks and fourteen loads; matches all 234 K184
entries, their group/old-position/species data, the 1,864 term permutations
and signs; and proves the coordinatewise beta minimum is exactly `1/3`.
Ordered cross terms contribute twice, making 2,928 positive support terms
behind the eighteen *signed* group sums. This accounts for every coherent
path pair without interpreting the positive majorant as its true value.

## One actual weighted rule and outward error

Take the radial Gamma(6,256) mean `rho*=3/128` and the symmetric Dirichlet
angular mean `z_i*=1/14`. For each coherent group evaluate the original
K184 complete species determinants and signed path-pair sum at this single
node, then multiply by the exact reference normalizer

```text
Z = (120/256^6) Gamma(1/3)^14/Gamma(14/3).
```

The signed one-node values are directed 100-digit Arb balls in the manifest.
An independent 90-digit mpmath path evaluates all eighteen groups through a
separate determinant implementation and lies within `1e-70` of their Arb
values; this is a control, not the outward proof. No factorials are inserted
after K184's Andréief cancellation.

Here is a fully rational, intentionally broad error. `pi>3`, `C_w<=1`,
`Gamma(1/3)<27/8`, and `Gamma(2/3)>9/10` (integrate
`t^(-1/3)(1-t)` on `[0,1]`) imply
`Gamma(14/3)>792/81`. Thus the integral majorant per ordered support term is

```text
E = (27/8)^14 * (81/792) * (120/256^6) / 3^8
  = 2251419529454986815 / 13617340432139183023890366464.
```

The quotient at the rule node has magnitude at most `n/pi^8` for a group
with `n` ordered support terms. The true integral and the one-node value
are each bounded by `n E`; hence `|I_group-Q_group|<=2nE`, without assuming
a smooth derivative at `rho=0`. The exact eighteen errors range from roughly
`1.19e-8` to `1.08e-7`; their sum is
`412009773890262587145/425541888504349469496573952 < 9.683e-7`.
The node values are on the order of the K184 numerical controls around
`1e-21`, so this certificate is many orders too broad to release an accurate
order-six prefix. It is an **actual full-domain weighted cubature/error
contribution**, not a completed useful precision budget. K185/K188 face,
tail and small-radius bounds remain compatible and are not double-counted
into a spurious tighter bound.

## Normalized derivative pilot: precise conditional transfer

The archived September 16 K191 pilot was re-executed read-only on three
width-`1/1024` `(a,c)` boxes at fixed `x=31/256`, `w=0`, `t=8`, `q=1`.
Its exact Cauchy baseline and 42 nonbaseline determinant terms enclose the
normalized `R3` and both second derivatives with Taylor/Hermite–Genocchi
remainder. The fresh conservative outward ceiling is
`|R_aa|,|R_cc|<11/1000` on all three boxes. The stored fine endpoints differ
in their last displayed digits from the fresh read-only execution, so only
the common coarse ceiling is used here; no false byte-identical replay claim.

This is connected to a *real* K184/K185 occurrence: the `1+` size-three
species in Gram `seed-1-order-6-path-3-contraction-3` against
`seed-1-order-6-path-5-contraction-2` has row positions `(1,3,7)` and column
positions `(1,5,7)`. K185's `entry-166-term-00`, allocation
`501c0f91d3e2c129af28`, gives beta parameters for the grouped ratios

```text
a=(s3+s4+s5+s6)/(s1+...+s6): Beta(8/5,4/5),
c=(v5+v6)/(v1+...+v6):       Beta(4/5,2).
```

On the fixed pilot slice, each box has positive primitive interiors; the
confluent endpoints are continuous boundary limits. Conditional on the two
six-primitive totals and truncating the beta laws to a box, let `m_a,m_c` be
their exact weighted centroids. Tensor Taylor with vanishing first moments
and Popoviciu's variance bound gives a valid local normalized average error

```text
|E_beta R3 - R3(m_a,m_c)|
 <= (sup|R_aa| Var(a) + sup|R_cc| Var(c))/2
 < 11/(4000*1024^2) < 2.623e-9.
```

This is only a factor-level conditional rule under **one majorant reference
allocation**. The actual signed coherent-Gram quotient has other Bessel
factors, species regularizers, old-position factors and nonconstant residual
weight; its a/c conditional distribution is not simply the above beta law.
The archived derivative implementation has not been migrated as a standalone
native reproduction file. Thus the local numeric factor bound is useful
method evidence, **not** an accepted full fourteen-dimensional error or a
global derivative theorem. The next calculation must carry product/chain-rule
derivatives of the complete quotient and a realistic group-level error budget,
or switch to a stronger analytic reduction if the resulting bound stays broad.

## Hostile postflight and release fence

Five mutations (missing Gram entry, flipped permutation sign, flipped path
coefficient, forged allocation loads, missing species factor) are rejected.
The independent mpmath values and direct rational normalizer containment pass.
The strongest overclaim would call the reference weight an exact distribution
or call `9.683e-7` a useful bound at `1e-21` scale; neither is true. The
closest contrary route is an adaptive determinant-preserving product rule
with normalized jets; K202 supplies its shared base weight but not its
required full gradient/Hessian or a decision-grade budget. Farther K201
shape shells remain unproved. No source claim, ledger verdict, canon, paper,
release or public posture changes.

Reproduce from the repository root:

```bash
PYTHONDONTWRITEBYTECODE=1 _local/cas-venv/bin/python tests/channel-swings/k202_order_six_common_weighted_core.py --write
PYTHONDONTWRITEBYTECODE=1 _local/cas-venv/bin/python tests/channel-swings/k202_order_six_common_weighted_core_probe.py
```
