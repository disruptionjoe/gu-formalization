---
title: "Residual phenomenology disposition: the complete 91-row register now derives B2 selectability"
status: active_research
doc_type: result
created: "2026-08-24"
registry: lab/process/residual-phenomenology-disposition-exhaustion-wave.json
probe: tests/channel-swings/residual_phenomenology_disposition_exhaustion_wave_probe.py
grade: "EXACT CORPUS AND TYPE DISPOSITION; TEN B2_NAMED_REQUIREMENT TERMINALS AND ONE PRECISE_IMPOSSIBILITY TERMINAL; 91/91 REGISTER EXHAUSTION DERIVED; NO LEDGER, SOURCE, CANON OR PUBLIC-POSTURE MOVEMENT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "The sole impossibility binds only the superseded unsplit SM-1 encoding. Every named source-action, external-scale, cosmology, thermodynamic, bordism and count owner remains unexecuted and open."
canon_verdict_change: none
---

# Residual phenomenology disposition and exhaustion wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: terminal disposition of the residual normalization, gauge-coupling, corrected cosmology, theta, thermodynamic, twisted-bordism and external-count rows
carrier: K77 ambient and observed gravitational carriers; source-action Yang--Mills and topological sectors; observed cosmological likelihood carriers; K77 twisted-bordism receptacle; framed/String corner and integer-count output LAYER=ambient+observed BRIDGE=named-disposition-seams CHIRALITY=S-CHIRALITY-UNTYPED
pairing: normalized action/observer functional; branching and threshold map; causal-boundary entropy/flux pairing; reduced spin-bordism/eta pairing; framed-corner count detector ON=declared-carriers
real_structure: settled K77 real horn retained; K95 BSp64 calculation fenced; ordinary Spin, framed and String tangential structures kept distinct
grading: dimensional scale orbit, reduction-node topological degree, cosmological background/perturbation/likelihood, BFV physical reduction, bordism degree fifteen and framed stable-homotopy count
action_owner: repository-construction; exact requirements retain source-action, external absolute-scale, observation, cosmology, thermodynamic, bordism and external-P3 owners; no owner is executed here
target: LT-GR2d LT-GR2e LT-GR7 LT-GR8 LT-SM1 LT-SM1a LT-SM1b LT-SM2 LT-SM7 AC-B2 AC-F5 MAP-TYPE=evaluation
```

## Result first

The final eleven `NEEDS` rows now have evidence-backed terminal dispositions
without treating their ledger labels or completed B3 subdispositions as
evidence and without changing conditional-physics ledger v0.263.

Ten rows carry exact B2 requirements. The normalization packet names the one
external absolute scale, the source-owned Yang--Mills horn, the one-to-three
branching, relative kinetic normalization and threshold computation. The
corrected cosmology packet names the normalized observer functional and
vacuum-shift Ward response, one action-owned background/perturbation forward
model, direct held-out likelihood evaluation, a selected QCD topological
sector, and the complete K77-to-observed Jacobson bridge. The anomaly/count
packet names the settled-K77 gauge twist and degree-fifteen bordism/eta
calculation, plus an actual nonzero framed/String corner and a typed integer
count detector paired with external `P3`.

Superseded `LT-SM1` alone closes at B4. It is precisely impossible to treat the
fundamental-versus-induced Yang--Mills horn and the unbuilt relative
normalization/branching as one finite-choice object. Its two successors remain
the exact escape and each closes separately through named B2 requirements.

No B2 owner was executed. The live denominator is now **91 terminal and 0 open
rows** of 91. With ledger currency, exact coverage, split parentage and all six
B3 subdispositions independently passing, the executable register derives
`exhausted=true` and `b2_selectable=true`. This is a work-selection transition,
not a scientific verdict and not evidence that any B2 requirement is solved.

## Preflight bookend: typing, currency and route choice

Layer 0 separated dark-energy connection distortion from the QCD vacuum angle,
the external absolute scale from dimensionless ratios, a Yang--Mills horn from
its relative normalization, an empirical cosmology carrier from its
action-owned forward model, Jacobson's conditional theorem from an observed
GU realization, the K95 `BSp(64)` receptacle from the settled K77 real horn,
and a framed torsion/input class from an integer output count. The correction-
currency pass retained the B3 Hubble, dark-energy and Jacobson scope repairs,
the K77 real-horn correction and the subtractive `2+1` count correction.

The route census rejected three shortcuts: execute B2 before the admitted gate
permits it, call every row one generic source-action debt, or use a B3
literature disposition as its parent construction's terminal evidence. The
selected route names one exact owner, coefficient or datum for each remaining
obligation and uses a bounded impossibility only where the parent encoding is
itself typed incorrectly.

## Normalization and gauge-coupling packet

| requirements | exact owner object |
| --- | --- |
| `ABS-1` | one declared external absolute scale and its dimensionally correct relation to the normalized source action; no invariant, Ward or symmetry condition may be relabelled as its supplier |
| `YM-HORN-1` | a source-action declaration and completeness proof selecting fundamental `zeta_F=1`, induced `zeta_F=0`, or a genuinely distinct third horn |
| `YM-BRANCH-1` | the operative K77-to-observed one-to-three gauge branching with representation, reality and normalization maps |
| `YM-NORM-1` | the source-action-computed inverse-squared gauge normalization on that branching |
| `YM-THRESH-1` | symmetry-breaking and threshold running from the frozen branching to the observed coupling triple |

`LT-GR7` requires `ABS-1`; `LT-SM1a` requires `YM-HORN-1`;
`LT-SM1b` requires `YM-BRANCH-1` and `YM-NORM-1`; and `LT-SM2`
requires the latter pair plus `YM-THRESH-1`.

`PI-SUPERSEDED-SM1-UNSPLIT-1` binds only the parent claim that horn selection
and normalization are one finite choice. Horn cardinality is discrete; a
branching-dependent inverse-squared normalization is a distinct construction
and real coefficient. The successor split is the resurrection path.

## Corrected cosmology, theta and thermodynamic packet

| requirements | exact owner object |
| --- | --- |
| `COS-NORM-1` | a native or explicitly external normalized observer functional inserted into the selected action with fixed units |
| `COS-NORM-2` | the exact zero-mode Ward/constraint and vacuum-shift response on one common physical domain |
| `COS-NORM-3` | a radiatively stable magnitude map with sign and normalization propagated to observables |
| `COS-FWD-1` | an action-owned matter/radiation background and perturbation solution with fixed initial data |
| `COS-FWD-2` | sound horizon, CMB spectra/angle, BAO ratios and luminosity-distance observables derived from that same solution |
| `COS-FWD-3` | preregistered held-out evaluation against the named DESI/CMB/BAO/supernova likelihoods without retuning |
| `TOP-1` | the operative reduction node and its physical degree-four topological basis |
| `TOP-2` | the complete periodic angle vector and source-action coefficients on that node |
| `TOP-3` | a descent/symmetry mechanism and hadronic observable map that fixes or suppresses the physical QCD angle |
| `JAC-1` | the typed K77 action and observation map to one observed 3+1 Lorentzian physical system |
| `JAC-2` | a named causal boundary/local horizon and observable algebra |
| `JAC-3` | action-derived constraints, canonical/BFV reduction and physical state space |
| `JAC-4` | frozen entropy density, temperature/KMS state and stress-energy flux normalization |
| `JAC-5` | an equilibrium Clausius or explicit entropy-production law with the resulting field equation and higher-curvature terms |
| `JAC-6` | one independently frozen held-out consequence evaluated without retuning |

The B3 scope work is preserved: no row receives relief from Hubble tension,
CPL compression, microscopic vacuum decomposition, the neutron-EDM mapping or
Jacobson's theorem status. Those source checks narrow the exact owners above;
they do not themselves terminate the rows.

## Twisted-bordism and external-count packet

| requirements | exact owner object |
| --- | --- |
| `BORD-1` | the operative settled-K77 boundary gauge group, full fermion content and corresponding real twist |
| `BORD-2` | the exact reduced degree-fifteen Spin-bordism/eta calculation on that twist, with trivialization or a nonzero obstruction stated |
| `CNT-1` | an actual nonzero framed or String corner/end object on the target geometry rather than an empty stable-homotopy slot |
| `CNT-2` | a typed nonzero `f`-invariant or other count detector with all framing and tangential-structure conventions fixed |
| `CNT-3` | a bridge from that detector's output to an observed integer count paired with external `P3`, without identifying a torsion input class with the output integer |

The K95 `BSp(64)` computation does not discharge `AC-B2`, and ordinary Spin
torsion remains 2-primary. `AC-F5` is not a claim that a framed class already
equals the generation count; the nonzero object, detector and integer bridge
are each separately required.

## Postflight bookend and hostile review

- **Strongest overclaim:** “the source action is now complete or GU fits all
  phenomenology.” Refused. The register only proves that every non-B2
  disposition has reached a terminal bucket and that B2 may now be selected.
- **Strongest contrary route:** any named owner may fail when constructed; the
  complete B2 register is a falsifiable specification, not evidence of success.
- **Weakest reproducibility seam:** the wave composes existing exact and scoped
  evidence rather than recomputing the owners. The native probe therefore
  checks source resolution, row/requirement ownership, superseded-parent
  assumptions and escape, protected effects, register derivation and ledger
  identity with planted failures.
- **Propagation seam:** the next legal work family changes from disposition to
  B2 construction. Ledger v0.263, source ownership, predictions,
  confirmations, claims, canon and public posture remain byte-identical.

The next Progress run must rebuild the substantial B2 frontier from the exact
requirement graph rather than assuming the previous pre-exhaustion ranking.
No Joe intervention is required.

Reproduce with:

```bash
python3 tests/channel-swings/residual_phenomenology_disposition_exhaustion_wave_probe.py --selftest
```
