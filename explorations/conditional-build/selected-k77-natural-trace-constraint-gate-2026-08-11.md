---
title: "Selected K77 natural trace-constraint gate"
artifact_type: exploration_result
created: 2026-08-11
status: NATURAL_ZERO_ORDER_CONSTRAINT_ROUTE_KILLED__DERIVATIVE_NONLOCAL_BV_OR_OPERATOR_COMPLETION_OPEN
grade: "exact rational real-Cl(7,7) representation and evolution theorem at Spin-natural zero-order scope; no action derivation, BV quotient, global domain, index or physics recovery"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Selected K77 natural trace-constraint gate

## Result first

There is one—and only one—Spin-natural zero-order trace equation propagated by
the actual real-K77 source-shaped `Omega1(S) plus Omega0(S)` evolution:

\[
C(\zeta,\nu)=2\Gamma\zeta-\nu=0.
\]

For every observed spatial direction its constraint variable obeys the
ordinary semisimple Dirac evolution `gamma(dt) gamma(dx_j)`. That is a genuine
piece of structure. It does **not** cure the PDE.

The complete rank-128 square-zero Jordan image lies inside `ker C`, and the
Jordan quotient map remains rank 128 when restricted to `ker C`. Therefore the
constrained evolution is still non-diagonalizable. The entire natural
zero-order constraint route is killed wholesale.

In plain English: the geometry supplies a trace relation that travels
consistently with the equations, but the bad generalized modes also satisfy
that relation. Imposing it does not remove them.

## Why the family is complete at this scope

At zero order, a Spin-natural map from `V tensor S plus S` to `S` has only two
channels: Clifford gamma trace on `V tensor S`, and the identity on `S`.
Consequently every candidate is

\[
C_{a,b}(\zeta,\nu)=a\Gamma\zeta+b\nu.
\]

For `b != 0`, set `t=a/b`. Exact coefficientwise propagation residuals in all
three observed spatial directions have polynomial gcd `t+2`. The `b=0` point
fails separately. Thus the unique projective line is `[a:b]=[2:-1]`.

This is not a scan over selected ratios. It decides the complete projective
family.

### Relation to prior art

B2C1 already showed, on the older projected W131 object, that two
root-dependent rank-64 halves span a rank-128 Jordan image and that the
smallest fixed invariant quotient erases the observer carrier. The present
increment is different: it works on the current full dimension-1920 real-K77
source-shaped evolution and classifies every common Spin-natural zero-order
constraint before any quotient. Its new relative result is the unique
covariant propagated line and the proof that this line itself retains the
complete Jordan defect.

## Why propagation is not removal

The preceding wave factors the Jordan remainder as

\[
N_j=E_j^2-1=U_jQ_j,
\qquad Q_jU_j=0,
\qquad \operatorname{rank}N_j=128.
\]

Exact calculation now gives

\[
\Gamma U_j=0,\qquad \pi_\nu U_j=0,
\]

so every natural trace constraint—not only the propagated one—annihilates the
Jordan endpoint inclusion. For the propagated line,

\[
\operatorname{im}N_j\subset\ker C,
\qquad
\operatorname{rank}(Q_j|_{\ker C})=128.
\]

The nonzero square-zero remainder therefore survives intact on the constrained
carrier.

## Layer 0 and controls

- `nu` and `zeta` are physical source fields. A relation between them is not a
  ghost or BV differential.
- Restricting to `ker C` is not quotienting by a gauge image.
- A direction-fitted condition `Q_j=0` would algebraically kill `N_j`, but
  `Q_x`, `Q_y`, and `Q_z` are different and are not the common covariant trace
  equation. That is the planted fitted-projector control.
- The result is about an observed one-time constraint. It does not construct
  an ambient `(7,7)` ultrahyperbolic domain.

## Consequence

The next domain attempt must leave this family. The live routes are:

1. a derivative or nonlocal constraint for the actual first-order operator;
2. an independently action-derived BV differential; or
3. an action-owned modification of the source-shaped operator, including the
   source-admitted Shiab/southeast completion class, rerun in real K77.

The Craig–Weinstein-style nonlocal constraint is now the highest-information
domain route. In parallel, the old source-admitted nonzero-southeast repair
should be reconciled with the current selected action because it changes the
operator instead of deleting physical modes.

P1/P2/P3 remain unused. No ledger verdict, residue, quotient, canon verdict or
public posture moves.

## Validation

`tests/channel-swings/selected_k77_natural_trace_constraint_gate_probe.py`
passes `51/51` exact, representation, analytic, Layer-0, symplectic and planted
checks after replaying the complete v0.168 predecessor.
