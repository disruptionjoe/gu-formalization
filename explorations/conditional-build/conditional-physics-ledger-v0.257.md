---
title: "Conditional Physics Ledger v0.257"
status: research
doc_type: ledger-summary
created: 2026-08-14
---

# Conditional Physics Ledger v0.257

Ledger v0.257 preserves all 82 verdicts and the canonicity-distance meter. It
migrates four distances after a structural construction closes the live
selected-stratum edge-carrier gate.

For the exact regular endpoint of Cartan type `(split,compact)=(5,2)`, the
restriction of the canonical cotangent form to

```text
M_C = Spin_0(7,7) x C
```

is globally symplectic over the selected regular chamber stratum. Its exact
dimension and rank are `91+7=98`, its moment-map differential has rank `91`,
and its seven-dimensional fibre is the right-Cartan tangent. This attains the
regular Poisson lower bound, so the selected-stratum global minimum is exactly
98. Singular walls, other real Cartan types and the all-strata minimum remain
open; `T*Spin(7,7)` remains the 182-dimensional all-charge fallback.

The opposite chamber `M_-C` composes with the selected endpoint:

```text
base moment components cancelled:             91/91
first-variation components cancelled:         91/91
edge-lift ambiguity:                                7
diagonal algebraic BFV compatibility:          exact
```

This removes the old fixed-orbit invariant-locking burden at mathematical
Hamiltonian grade. It does not supply a source-owned edge field, an
action-owned boundary kinetic term, a stationarity law, proper functional
BFV/Koszul--Tate, an analytic domain, positivity or physical cohomology.
Charged boundary symmetry remains the zero-import rival.

Two parallel packets changed no ledger row. The tautological `b±,t±` branches
are exact total-residual-zero candidates only at frozen-frame,
fixed-boundary/compact-support grade; their native `B(epsilon)/Y` curvature
legality is still missing and the bare free-edge horn excludes them. The ten
rank-128 leaks were already the exact full `N* tensor S` observation-kernel
module in v0.228; the apparent mixed-boost failure was a vector/covector
convention plus a genuinely compact-natural selected graph, not ten carrier
defects.

```text
Ledger v0.257 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Tightness: T4x1 T3x3 T2x1 · scoped quotients: 5
Headline: selected regular-stratum global minimum 98; edge cancellation exact
```

Next compare source/action ownership of the Cartan-slice boundary carrier
against charged boundary symmetry. In parallel, test the tautological
background's curvature orbit under the actual distinguished `B(epsilon)`
connection, and pursue one action/BV-owned moving graph correction rather
than rebuilding ten rank-128 repairs.
