---
title: "Ledger advance wave: zero of thirty-eight upgrades survived, and the four things that finding is worth"
status: active_research
doc_type: multi_agent_wave_result_and_ranking_correction
created: "2026-08-23"
directed_by: "Joe direct chat, 2026-08-23 (orchestrate a big wave that maxes ledger advances)"
registry: lab/process/ledger-advance-wave-result.json
probe: tests/channel-swings/ledger_advance_wave_result_probe.py
grade: "EXACT SURVEY OVER 35 LEDGER ROWS WITH ADVERSARIAL VERIFICATION; ZERO VERIFIED UPGRADES; NO VERDICT, REASON_KIND, MAPPING_GRADE, DISTANCE OR REVIVAL_TRIGGER MOVED"
target_claim: NONE-NOT-A-KILL
target_claim_note: "surveys and adjudicates repository-internal ledger typing; kills no source claim and no research route"
canon_verdict_change: none
---

# Ledger advance wave: the result

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: INTERNAL_STRUCTURAL_ONLY`

Thirteen agents surveyed 35 ledger rows across six families plus the
source-claim register, and every proposed upgrade was pipelined into an
independent adversarial verifier whose instruction was to refute it. This
artifact records the outcome, corrects the ranking that commissioned the wave,
and states what is actually worth doing.

```gu-typed-objects
result: zero verified ledger upgrades from thirty-eight proposals; a five-class failure taxonomy; a verified grant partition with two unintegrated collapses; three verified structural blockers; two corrections to the commissioning ranking
carrier: the canonical ledger v0.262 and the source-claim register LAYER=UNTYPED CHIRALITY=N/A
pairing: proposed upgrades paired against independent refutation ON=recorded-artifacts
real_structure: N/A
grading: survey and adjudication at wave grade; every claim reproduced below was re-verified against the live ledger by the author
action_owner: N/A -- no action, coefficient or physics object is introduced
target: ledger upgradeability MAP-TYPE=evaluation
```

## The headline

**Zero of thirty-eight proposals survived adversarial verification.** Rows that
can move today: **zero**. Rows that can receive an annotation: **at most
eight**. No verdict, `reason_kind`, `mapping_grade`, `distance` or
`revival_trigger` changes, and `progress.verdict_counts` is unchanged.

That is the correct outcome for a wave commissioned as "cheap upgrades," and
it says the cheap upgrades were already taken. The wave's value is entirely in
what the zero exposed.

## Author-verified facts

Every load-bearing claim below was re-checked against the live ledger rather
than taken from agent output:

| claim | verified |
| --- | --- |
| ledger head is **v0.262**, 91 rows | yes |
| `AC-F3` is now **`DIFFERS / ROUTE_KILLED`** | yes — the kill-typing delta filed earlier today was integrated |
| `OVER_DETERMINED` count is now **2** (was 5) | yes |
| `AC-G1` is `row_status: SUPERSEDED` | yes — so the live `STALE_PREMISE` population is **one**, not two |
| `la1-`, `la3-`, `la4-`, `la9-`, `phi1-`, `phi2-`, `sa1-` each occur **0 times** in the ledger | yes, all seven |
| `prediction-package`, `pv2-`, `CKM` each occur **0 times** in the ledger | yes |
| every one of the 91 rows is sourced from CB-A (35), CB-B (25), CB-C (27) or EXT-* (4) — **zero from a source claim** | yes |

## Finding 1 — the grant partition, and the collapse that would move twelve rows

The 15 `DERIVED_CONDITIONAL` rows partition into exactly three named grants
plus one singleton:

| grant | rows | n |
| --- | --- | ---: |
| **GRANT-U1** — action-selected, `G_SM`-shaped, maximal-compact-contained identity-component vacuum stabilizer | RA-A3, RA-B1..RA-B5 | 6 |
| **{U4, EMB}** collapsing to `v ∈ L` — the chiral-16-shadow / observed internal SM content | AC-D1..AC-D5 | 5 |
| **GRANT-ACA1-C1** — draft-literal Sec 9.3 full-`/S` reading, branch C1, `x = 0` | AC-A1, AC-A2, AC-A3 | 3 |
| in-row conditional, unnamed | RA-F2 | 1 |

Two **collapses** are already filed and would compose: LA-1 proves at 36/36
with zero floats that given a `G_SM`-shaped stabilizer the hypercharge line is
unique modulo the Spin(10) Weyl group — one physical line, **zero bits** — so
the embedding is not a second selection event; and PHI-1 computes that the
14→4 reduction sends 14D anomaly cancellation to zero bits in the 4D verdict,
reducing that criterion to `v ∈ L`, a datum the first group already owns.

**If both collapses hold, one discharge moves twelve of the fifteen rows.**
That is the highest-leverage object in the ledger, and it is why this wave was
worth running despite the zero.

Four cautions, all recorded and all verified: neither collapse is integrated;
PHI-1 is fork-1 comparator-classified and self-declares `rows_advanced: 0`, so
collapse 2 is a hypothesis about the condition graph rather than recorded
structure; the two halves are **severed today**, so discharging `AC-A1` moves
`AC-D1..D5` by exactly nothing and anyone relaying "the anomaly axis is the
strongest sector" as one fact is compounding two conditions that provably do
not compose; and the grant itself is undischarged, with RA-A1, RA-A2 and RA-A8
all still `NEEDS / MISSING_CONSTRUCTION`.

## Finding 2 — an eight-day integration backlog

`la1-`, `la3-`, `la4-`, `la5-`, `la9-`, `phi1-`, `phi2-` and `sa1-` occur
**zero times** in the canonical ledger. These are the 2026-08-15/16 artifacts
that computed the grant partition, the hypercharge-line uniqueness, the
incidence optimum and the 14→4 reduction. Each self-files `rows_advanced: 0`,
correctly — but their *structural* content was never wired into the rows they
describe. **The wave re-derived an eight-day-old backlog and then tried to
convert it into movement it never claimed.**

The work item is therefore not "find upgrades." It is **integrate the backlog**:
attach the named condition to each row, so that a future discharge of
GRANT-U1 is mechanically known to move six rows rather than being rediscovered.

## Finding 3 — the prediction rows are not wired to the prediction surface

`prediction-package`, `pv1-`, `pv2-`, `mj5-`, `sn1-`, `bd1-`, `ac1-` and
`mirror-sector-prediction` each occur **zero times** in the ledger. Every
`DIFFERS / PREDICTION` row still points at the 2026-08-05 CB-A/CB-C wave,
while the 2026-08-14..17 results that sharpen exactly those differences are
unlinked. **The lowest-cost, highest-value action in this lane is the wiring,
not the packaging.**

Readiness ranking, sharpest first: `RA-A4` needs one number (a mass for the
second singlet direction) and can otherwise freeze on the PP2 scale-free
precedent; `RA-A5` needs the same mass for nine states; `RA-B6`'s
Dirac/Majorana bit is *undefined* rather than unmeasured, awaiting a reality
map on the source-native carrier. At the other end, `RA-E5` is
**PP-inadmissible** — its only quantitative content compares against an
already-measured bound, which the standing rule bars without exception — and
`RA-E3` and `AC-A5` have no observable at all. Recording those blocks is the
useful move.

## Finding 4 — the two inventories were never joined

Every one of the 91 rows carries a `source_row` from CB-A, CB-B, CB-C or an
accepted external benchmark. **Zero rows are sourced from a source claim.** The
`denominator.inclusion_rule` admits requirements enumerated by CB-A/B/C plus
externally accepted benchmarks only. Source claims enter this ledger as
`target_claim` typing, kill targets and trigger conjuncts — never as the
enumerated requirement.

So the register-minus-ledger gaps are not admissible today by any channel
action, and the strongest of them are real: the `SC-COS-01` + `SC-MAS-03`
identity between the cosmological constant and the fermion-mass VEV (the
entire anti-fine-tuning payoff, with the LT-GR2b..2e split covering four
adjacent objects without ever stating the identification); `SC-ACT-06`'s
moduli and elliptic deformation complex, logically upstream of every
LAGRANGIAN row and recorded simply as *untested*; and CKM accommodation, which
is self-witnessing — the ledger accepted `EXT-SM-STRUCTURE`, which names
mixing, then linked eleven rows none of which owns it.

**The one thing worth shipping from this lane is not a row.** The row set was
enumerated on 2026-08-05 from the SM-plus-GR side; the source register was
built on 2026-08-11 from the source side; the two were never joined and
`inclusion_rule` still names only CB-A/B/C. Amending it to admit the register
as a second source of targets is **Joe's call, not a channel's** — the EXT-*
route carries a recorded acceptance artifact every time it is used.

## Finding 5 — how ledger upgrades fail, in five classes

The refutations are reusable, which makes them the wave's most transferable
output:

- **A — grant laundering through a non-verdict field** (7). The verdict stays
  `DERIVED_CONDITIONAL` while the `distance` string quietly stops naming the
  open grants, so the row *reads* discharged. LA-3 anticipated this exactly,
  requiring the grant be carried "rather than laundering it."
- **B — comparator transported without a typed bridge** (6). Including one
  proposal that cited an artifact with zero entries in the routing registry.
- **C — false novelty from a substring search** (4). Every one searched a
  token, got zero hits, and concluded absence; in each case the object was
  filed under different words. This is the repository's recorded failure mode
  and it recurred four times in one wave.
- **D — the cited artifact's own text refuses the use** (5). One proposal
  quoted a source one line above that source's own sentence reading "Zero rows
  advance. No ledger edit."
- **E — direction inversion** (2). Both proposed `EXTERNAL_DATUM →
  MISSING_CONSTRUCTION` as a move *away* from dischargeability; the recorded
  semantics run the other way, since `MISSING_CONSTRUCTION` grants
  availability.

## Finding 6 — a real defect the recorded fix does not fix

LA-9's replacement revival trigger for `AC-D1..D5` — endorsed verbatim by four
of five corrected proposals — is itself mode `NR` on all five rows against
LA-3's own recorded functional table. **The trigger defect on `AC-D1..D5` is
real, and the filed repair does not repair it.** This is the single most useful
positive finding of the wave and it is a defect report, not an upgrade.

## Correcting the ranking that commissioned this wave

`lab/process/ledger-kill-typing-and-upgrade-ranking.json` was written earlier
today and the wave falsified two of its five items:

- **Rank 2 was wrong.** It asserted "nobody has checked whether those
  conditions are shared, discharged or narrowed." They had been checked, on
  2026-08-15/16, by LA-1, LA-3, LA-4, LA-5, LA-9, PHI-1, PHI-2 and SA-1. The
  correct item was never "audit the conditions" but **"integrate the audit that
  already exists."**
- **Rank 4 was wrong on its count.** It commissioned a re-check of "the 2
  `OVER_DETERMINED / STALE_PREMISE` rows." The live population is **one**:
  `AC-G1` is `SUPERSEDED` and outside the denominator, and its repair was made
  and propagated on 2026-08-07.

Both errors have the same root and it is the one this session keeps finding:
**a ranking built by reading the ledger without reading what had already been
written about the ledger.** The ranking is corrected in place rather than
withdrawn, because ranks 1, 3 and 5 stand and rank 1 was executed successfully.

## Method note — half the wave was never tested

Of 38 proposals, 19 carry a binding refutation, 14 were never verified, and 5
carry an assessment computed against a *different* proposal on the same row —
which, under the wave's own claim-indexed rule, means those five are
**unverified, not refuted**. They are not endorsed here. A future wave should
key verifier output to a proposal identifier rather than a row identifier,
since a row can carry several proposals.

## Claim ceiling

No ledger verdict, `reason_kind`, `mapping_grade`, `distance` or
`revival_trigger` moves. No canon, source ownership, prediction credit or
public posture changes. The grant collapses are recorded as *filed and
unintegrated*, not as established structure. The `inclusion_rule` amendment is
recorded as a decision for Joe, not a proposal this channel may execute.
