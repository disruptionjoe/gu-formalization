---
title: "Selected K77 zero-seed H640 action-closure controls"
artifact_type: exploration_result
created: 2026-08-11
status: ZERO_FORM_GENERATES_SPECIAL_CONDITIONAL_H640__FULL_BV_AND_SELECTION_OPEN
grade: "exact characteristic-zero zero-seed module plus two-prime exact natural and planted controls; no lower-order BV, physical cohomology, global domain, chirality, index or count"
canon_verdict_change: none
---

# Selected K77 zero-seed H640 action-closure controls

## Result first

The conditional principal action does not need a supplied `W` or mirror seed
to produce `H640`. The source-owned zero-form spinor `Omega^0(S)` alone
generates, under the three spatial evolution matrices, the exact invariant
module

```text
H640 = 512 one-form directions + 128 zero-form directions.
```

The load-bearing `640=512+128` result is exact over `QQ`. Every natural and
planted comparator was independently repeated over `GF(1009)` and `GF(1013)`.
`W+Omega0`, mirror-plus-`Omega0`, and their union all generate precisely the
same `H640`.

This is selective, not a dimension-only closure. Three preregistered generic
rank-192 planes in `ker Gamma`, each extended by the same zero-form field,
generate ranks `1920`, `1916`, and `1908`. The prior one-form 640 and the 832
each generate a rank-1280 module. Those two rank-1280 modules are distinct,
intersect exactly in `H640`, and together span the full rank-1920 carrier.

## Layer 0

`H640` is the minimal module generated by the zero-form source component under
the complete algebra of the three conditional spatial evolution matrices. It
is not yet:

- a source quotation or a source-selected physical carrier;
- the previous rank-640 one-form Lorentz representation sector;
- the complete barred/unbarred action and antifield domain;
- BV/Koszul--Tate cohomology or a BFV reduced phase space;
- a global closed-domain spectrum, chirality, index, or generation count.

The source supplies independent `Omega^0(S)` and `Omega^1(S)` fields. The
repository constructs their principal spatial action. Their composition is a
repository derivation, not an attribution to Weinstein.

**[CN-2 S-TYPING: S-FULL-DIRAC]** `S` is unsubscripted in the source and this
file reads it as the **full** Dirac bundle, not one Weyl half. That is not a
convention: the probe's `spin = 128` and the `128 zero-form directions` of the
`640 = 512 + 128` split are the full complex Dirac dimension of `Cl(7,7)`. Under
a Weyl-half reading the seed would be `64`-dimensional and the reported module
would not be this one, so the arithmetic here **depends** on `S-FULL-DIRAC` and
is stated to depend on it. This fixes the dimension only; it decides nothing
about whether the built theory's operative content is the total declaration or
one of its two class-homogeneous halves.

## Why the eight-word closure is complete

The three spatial evolution matrices square to the identity and pairwise
anticommute. Their generated algebra therefore reduces to the eight words

```text
1, E1, E2, E3, E1E2, E1E3, E2E3, E1E2E3.
```

The eight operators act independently in the exact fixtures, and every
reported generated module is invariant under all three generators. The
calculation is the full finite spatial-action closure, not a bounded-depth
sampling.

## Constraint surplus

The zero-form seed adds no parameter: it is already one of the source fields.
`W`, mirror, 640, 832, and the random planes are controls, not candidate
data. Thus the result removes the need to *supply* a `W` or mirror seed at
principal grade without booking a new external datum.

It does not remove the external-datum problem. Full BV cohomology may still
require a horn, global domain, observation class, or P3 input. None is used
here.

## Source return

- `SOURCE-CONFIRMS`: independent `Omega^0(S)` and `Omega^1(S)` fermion fields
  and the four-field covariance grammar.
- `SOURCE-CORRECTS`: none.
- `SOURCE-SILENT`: `H640`, its physical selection, lower-order invariance,
  BV/Koszul--Tate cohomology, the global domain, chirality, and count.

## Consequence and next gate

The preregistered first horn fires. The next full BV/Koszul--Tate construction
should use `H640` as the candidate action module and rank 1,920 as a mandatory
control. It must first test whether the lower-order `varpi`, reality pairing,
barred/dual variables, observation map, and boundary differential preserve
`H640`. If any fails, the physical complex returns to the full carrier.

Selected Spin, Curt's two `U(32,32)` halves, and full `U(64,64)` remain
distinct. Real K77 and both action-pairing horns remain conditional. P1/P2/P3,
verdicts, residue, quotients, canon, and public posture do not move.
