---
artifact_type: conditional_physics_ledger_release
created: 2026-08-12
ledger_version: "0.192"
run_id: RUN-20260812-024031-gu-moving-split-structure-action-selection
---

# Conditional physics ledger v0.192

Ledger v0.192 — 82/82 mapped; 32 SAME · 19 DIFFERS · 26 NEEDS · 5
OVER-DETERMINED. Headline counts and residue do not move. The moving
two-half/finer-split connection decomposition is now exact.

The covariant derivatives of the moving ambient chirality `omega` and the
split-native real complex structure `J4` reconstruct the complete connection
pieces that break the two nested reductions. The canonical projector-reduced
K77 connection preserves both. A block connection preserves `omega` but may
break `J4`; the full endomorphism connection additionally carries a
half-exchanging tensor.

The existing nonzero-branch pointwise action Hessian has no radical on either
breaking bank, so the current selected action does not dynamically select the
compatible locus. Those pieces remain live fields. The source's assignment of
Higgs-like and Yukawa functions to `varpi` makes this a concrete arena to test,
but no physical block identification or full-unitary-to-K77 compatibility map
has been derived.

Residue stays at 84 booked continuous real parameters, at least 19
function-valued slots and nine discrete forks, with five scoped quotients.
No field, datum, coefficient, projector or quotient was added. P1/P2/P3 remain
unchanged and unused. Six rows migrate in distance/evidence only.

Machine-readable ledger:
`lab/process/conditional-physics-ledger-v0.192.json`.
