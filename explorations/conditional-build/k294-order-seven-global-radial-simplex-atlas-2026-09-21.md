---
title: "K294 Order-Seven Global Radial-Simplex Atlas"
document_role: active_research
operational_state: internal_structural_result
date: "2026-09-21"
claim_ceiling: "Exact whole-positive-orthant radial-simplex chart and bare native-measure normalization; no coherent exterior-integrand bound, action-column value, residual, K152 interval, or physical claim."
manifest: lab/process/k294-order-seven-global-radial-simplex-atlas.json
producer: tests/channel-swings/k294_order_seven_global_radial_simplex_atlas.py
probe: tests/channel-swings/k294_order_seven_global_radial_simplex_atlas_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: GLOBAL_NATIVE_MEASURE_ATLAS_SERIALIZED__COHERENT_EXTERIOR_INTEGRAND_OPEN
canon_verdict_change: none
---

# K294 Order-Seven Global Radial-Simplex Atlas

## GU-COMPARATOR-ROUTING

This artifact concerns the repository-supplied conditional Fock construction.
It neither identifies a source-selected action nor changes a physics-ledger
row.

```gu-typed-objects
result: exact global radial-simplex chart and bare-measure normalization for the six positive K288 gap ratios
carrier: complete order-seven K179 coherent Gram occurrence family on sixteen positive primitive cumulative-time increments
measure: K288 native cumulative-time measure before the fixed point prefactor
action_owner: repository construction from K139/K156/K179; no source-selected GU action or physical state is supplied
target: expose the full radial/projective exterior geometry needed before a complete order-seven action-column enclosure
```

## Exact global chart

Let `g=(r0,r1,r2,c0,c1,c2)` be the six positive K288 gap ratios. Set

```text
q = sum_i g_i,       p_i = g_i/q.
```

Then `q>0`, `p` lies in the open five-simplex, the inverse is `g_i=q p_i`,
and the six-dimensional Jacobian is exactly `q^5`. Since
`product(g_i)=q^6 product(p_i)`, K288's projective factor becomes

```text
exp(-256 x (1+q)) x^15 y(1-y) q^11 product(p_i).
```

The simplex integral is `1/11!`; the q integral is
`11!/(256x)^12`. Their factorials cancel, leaving `(256x)^-12`.
Together with the y integral `1/6` and the remaining gamma integral in x,
the complete bare mass is exactly `256^-16`, independently replaying K288.

## Radial exterior

After integrating q, p, y and the eight split coordinates, normalized x has
the Gamma(shape 4, rate 256) density. The K290 interval corresponds to
`31 <= 256x <= 32`. Exact incomplete-gamma formulas show that its bare-mass
fraction is about `1.1250222140e-10`; both `x<31/256` and `x>1/8` are now
explicit atlas strata rather than an unnamed exterior.

This is a measure atlas, not a bound on the determinant integrand. K295 tests
the first legal composition question: whether K290's pointwise derivative
majorant remains integrable at the simplex faces.
