---
title: "Selected-K105 RSAP curvature-sign owner qualification"
status: active_research
doc_type: exact_conditional_curvature_sign_construction_and_current_stationary_owner_obstruction
created: "2026-08-15"
registry: lab/process/selected-k105-rsap-curvature-sign-owner-qualification.json
probe: tests/channel-swings/selected_k105_rsap_curvature_sign_owner_qualification_probe.py
grade: "A GAPPED CURVATURE ENDOMORPHISM CAN CANONICALLY PRODUCE THE BALANCED INVOLUTION, BUT THE CURRENT SERIALIZED ACTION HAS ZERO ELIGIBLE STATIONARY CARRIERS ON WHICH TO EVALUATE IT"
target_claim: K104_NEXT_GATE__CURRENT_ACTION_OWNED_NONHOMOGENEOUS_CURVATURE_OR_CONNECTION_CONCOMITANT_CONSTRUCTS_A_SMOOTH_GAPPED_BALANCED_7_PLUS_7_INVOLUTION_AT_ZERO_CHARGE
target_verdict: NO_AT_CURRENT_SERIALIZED_STATIONARY_CARRIER_GRADE__CONDITIONAL_GAPPED_CURVATURE_SIGN_CONSTRUCTION_EXACT
canon_verdict_change: none
---

# Selected-K105 RSAP curvature-sign owner qualification

> **GU-COMPARATOR-ROUTING — scope before inference.** This is a source-native
> curvature, stationary-background and boundary-reduction question. Ordinary
> Higgs/VEV, family-index, net-chirality, anomaly, symmetry-breaking and
> familiar four-dimensional gauge-model conclusions do not adjudicate it.
> Read `lab/methods/source-native-comparator-routing.md` before importing any
> such comparator.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K104's last live balanced-order-parameter owner class, K100's complete
real-orbit warning, and the SR-1H census of currently serialized action-owned
stationary point carriers. This is not a no-go theorem for unreleased actions,
new stationary solutions, or a source-global reconstruction not yet built.

## Result in plain English

Curvature can do the mathematical job. If a natural curvature endomorphism
has seven positive and seven negative real eigenvalues, never touches zero,
and its two eigenspaces have the required real `Q` signatures, then taking its
spectral sign produces exactly the balanced involution needed by the `98D`
reverse RSAP.

The present action does not yet do that job. The repository's exhaustive
SR-1H census contains five currently serialized action-owned point-carrier
classes and seven instances. None survives as an eligible stationary
background. A curvature formula therefore has no current action-stationary
input on which it can be evaluated. This failure is upstream of spectral
calculation.

The precise disposition is:

```text
curvature-sign mechanism:       exact conditional construction
current action-stationary input: absent across the serialized census
global persistent gap/inertia:  unproved
balanced boundary equation:     unowned
right-H_bal gauge declaration:  unowned
```

So the curvature-owner lane is parked, not declared impossible. It reopens
only when a genuinely new action-owned stationary background or action term
is constructed. Until then Variancer's `R_0` remains an explicit reverse-
scaffold conditional; it is not silently promoted to a source result.

## 1. The conditional curvature-sign construction

Let `Q` have signature `(7,7)` and let `S` be a `Q`-self-adjoint curvature
endomorphism. Assume on the region under study that `S` is real semisimple,
invertible, and has exactly seven positive and seven negative eigenvalues.
Then functional calculus defines

```text
R_S = sign(S) = S (S^2)^(-1/2).
```

It follows that

```text
R_S^2=1,       R_S is Q-self-adjoint,       tr(R_S)=0.
```

The construction is natural under `O(7,7)` conjugation and varies smoothly
as long as the gap at zero stays open and the inertia is constant. It is
independent of endpoint charge, so it can remain defined on the zero-charge
stratum. Zero charge does **not** imply zero curvature.

The eigenvalue count is not enough. K100 proves that all eight real `7|7`
involution types have the same `+1/-1` spectrum. The desired unordered real
type additionally requires

```text
signature_Q(E_+)=(3,4),   signature_Q(E_-)=(4,3)
```

or the reversed labeling. A `(7,0)|(0,7)` split has the same spectrum and is
the exact hostile control.

## 2. Exact positive algebraic-curvature witness

The probe constructs a pointwise algebraic curvature tensor, rather than
merely asserting that one could exist. Choose the balanced diagonal
involution `R_0`, put

```text
S=R_0,                 r=Q S,
Rm=(r Kulkarni-Nomizu Q)/(14-2).
```

Because `tr_Q(r)=tr(S)=0`, exact rational contraction gives

```text
Ric(Rm)=r,             Q^(-1) Ric(Rm)=S=R_0.
```

The probe checks both pair antisymmetries, pair exchange, the first Bianchi
identity and the Ricci contraction over all indices. The spectral gap is one,
the eigenspace signatures are `(3,4)|(4,3)`, and the sign construction returns
`R_0` exactly.

This witness proves that the curvature-sign horn is algebraically nonempty.
It is not a metric germ, a solution of the GU Euler equations, a global
background, or an action-selection theorem.

## 3. The zero-curvature and gap boundary

At the zero tensor tuple, any natural output must commute with the full
`O(7,7)` stabilizer. K100's exact commutant calculation leaves only scalar
endomorphisms. The only scalar involutions are `+1` and `-1`; their traces are
`+14` and `-14`, never zero. Equivalently, the spectral-sign formula is
undefined when the gap closes at zero.

A smoothing such as `S(S^2+delta^2)^(-1/2)` is defined at zero but ceases to
be an exact involution there. Choosing a balanced limit at zero imports a
seven-plane—the same missing order parameter in another notation. Thus the
construction is canonical on a gapped stratum, not across arbitrary gap
closure without extra data.

## 4. The current stationary-carrier obstruction

SR-1H already performed the required current-action census. Its five classes
are:

| class | current disposition |
|---|---|
| canonical `T=0` Zorro points | killed by the action two-jet |
| two homogeneous nonzero-`T` source branches | current canonical intersection empty |
| two canonical-`B_Z` nonzero-`T` roots | killed by the complete affine jet fibre |
| rank-450 source-instability carrier | killed by checked action boundedness |
| scalar-curvature/VEV trace branch | canonical realization open, hence ineligible |

The census contains zero full-local-stationarity survivors and zero eligible
`VRS-5` backgrounds. Consequently no currently serialized action-owned
curvature or full-`II` concomitant can yet be evaluated on an eligible
stationary carrier. Computing more spectral polynomials on those same five
classes cannot repair the missing input.

This is a scoped exhaustion statement. The SR-1H reopeners remain real:
a derived rival Zorro connection/metric, a nonzero-fermion coupled saddle, a
new primalizer or domain, a higher action, or unreleased source material.
They are not counted as present achievements.

## 5. Owner accounting after K105

The positive fixture settles only the first implication below:

```text
gapped balanced-real curvature endomorphism
    => canonical moving R
    => balanced zero-level boundary equation
    => right-H_bal gauge reduction
    => 98D physical boundary theory.
```

The last three arrows remain ownership questions. Even a future stationary
curvature background must also prove a global persistent gap, balanced real
inertia, the boundary Euler/Noether equation `lambda_h=0`, and the gauge
status of right `H_bal`. K104's three-owner factorization therefore survives.

## 6. Disposition and successor rule

Do not run another curvature-polynomial search over the same five dead or
ineligible carriers. Owner work reopens only with a genuinely new stationary
background—most concretely an explicit source-global rival Zorro
reconstruction or a nonzero-fermion saddle—or with a new action term.

Otherwise continue the reverse RSAP only with both imports visibly labeled:
the balanced real-orbit seed `R_0` and the K99 boundary multiplier/gauge law.
That conditional mathematics may be tested for global domain, positivity and
physical cohomology, but it must not be described as selected by the current
source action.

No ledger, datum, canon claim or public posture changes. Reproduce:

```bash
python3 tests/channel-swings/selected_k105_rsap_curvature_sign_owner_qualification_probe.py
```
