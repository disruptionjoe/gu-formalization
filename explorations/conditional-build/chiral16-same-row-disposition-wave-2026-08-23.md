---
title: "Chiral-16 SAME-row disposition: eleven exact B2 terminals and one comparator-selector impossibility"
status: active_research
doc_type: result
created: "2026-08-23"
registry: lab/process/chiral16-same-row-disposition-wave.json
probe: tests/channel-swings/chiral16_same_row_disposition_wave_probe.py
grade: "EXACT CORPUS AND METHOD DISPOSITION; ELEVEN B2_NAMED_REQUIREMENT TERMINALS; ONE BOUNDED PRECISE IMPOSSIBILITY; NO LEDGER, SOURCE, CANON OR PUBLIC-POSTURE MOVEMENT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "The bounded impossibility targets an internal inference from conventional anomaly cancellation to chirality selection; no source claim, physical carrier or GU theory class is killed."
canon_verdict_change: none
---

# Chiral-16 SAME-row disposition wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: terminal four-bucket disposition of eleven SAME rows sharing the conventional chiral-16 comparator
carrier: complex Spin(10) Weyl 16 and its six signed Standard-Model constituent multiplicities LAYER=observed CHIRALITY=S-CHIRALITY-UNTYPED
pairing: representation branching and five linear four-dimensional anomaly functionals ON=signed-multiplicity-lattice-Z6
real_structure: UNTYPED for the source-native physical carrier; complexified comparator only
grading: all-left four-dimensional Weyl comparator versus non-chiral ambient source carrier
action_owner: UNTYPED; exact named successor is source-action plus observation owner
target: RA-A3 RA-B1 RA-B2 RA-B3 RA-B4 RA-B5 AC-D1 AC-D2 AC-D3 AC-D4 AC-D5 MAP-TYPE=evaluation
```

## Result first

Eleven ledger rows now have evidence-backed terminal dispositions, and none
moves in ledger v0.263.

- `RA-A3` and `RA-B1`--`RA-B5` are **B2_NAMED_REQUIREMENT**. Their exact
  branching and hypercharge arithmetic remain valid, but they are conditional
  on one unbuilt action-selected vacuum stabilizer. The embedding inside a
  `G_SM`-shaped stabilizer costs zero additional bits; the stabilizer itself is
  not built.
- `AC-D1`--`AC-D5` are **B2_NAMED_REQUIREMENT**. Their exact anomaly zeros on a
  conventional chiral 16 remain valid, but the source-native physical 4D
  carrier, action invariance and observation map are unbuilt.
- The proposed shortcut from those five zeros to a selected chiral 16 is
  **precisely impossible in the frozen comparator arena**. The five functionals
  have rank four, their common zero set is the saturated rank-two lattice
  `Z·15 + Z·nu^c`, and it contains zero and is closed under negation. Anomaly
  cancellation therefore cannot detect net chirality or uniquely select the
  complete 16 there.

The bounded impossibility is not the terminal token for the anomaly rows. It
removes a false B1/B4 shortcut. The terminal token is B2, with the physical
carrier and its owners named exactly.

## Layer 0 and the two objects

The shared words “16,” “chirality” and “anomaly cancellation” denote two
different objects.

1. The **conventional comparator** is one complex `Spin(10)` Weyl 16, written
   as six all-left Standard-Model irreps with signed multiplicities. Its
   hypercharges and five 4D anomaly functionals are exact.
2. The **source-native object** is a non-chiral ambient fermion carrier whose
   low-curvature observed sector is claimed to look chiral after an action- and
   VEV-owned decoupling. Its physical projection, pairing, domain and action
   invariance are not supplied by the comparator.

The relating map is precisely the missing object. A favorable calculation on
the first carrier is neither evidence for nor evidence against the second.

## Packet A — the one stabilizer grant

The six rows are one condition class:

```text
GRANT-U1:
  a stationary source action selects a vacuum whose identity-component
  stabilizer is G_SM-shaped, lies in the source's maximal-compact reduction,
  and contains a regular su(3)_c embedding.
```

LA-1 proves that, after this grant, the compatible hypercharge line is unique
modulo the `SU(2)_R` Weyl action. That is a zero-bit **consequence of the
grant**, not a construction of the grant.

| rows | exact terminal requirements |
| --- | --- |
| `RA-A3`, `RA-B1`--`RA-B5` | `U1-1`: OWNER-A supplies a stationary source action and vacuum. `U1-2`: OWNER-A proves its identity-component stabilizer is `G_SM`-shaped and maximal-compact-contained. `U1-3`: OWNER-D transports that selected stabilizer and the normal-bundle spinor carrier to the observed 4D section without changing the branching fingerprint. |

### FC-1--FC-7 audit

- **FC-1 — failed.** Exact weight arithmetic builds the *conditional
  branching*, but no action-selected vacuum or observed physical carrier is
  built. The full action-owned fingerprint is absent.
- **FC-2 — met at observed representation rung R4.** The conditional object
  supplies representation and hypercharge content upward and depends on the
  selected stabilizer below.
- **FC-3 — checked.** Retrieval covered CB-A, LA-1, LA-4, LA-9, the
  chiral-16/physical-carrier fences, `PD-STRUCTURE-TRANSPORT`, the correction
  registry and ledger v0.263. The arithmetic is preserved; the grant remains
  open.
- **FC-4 — checked.** `SC-GRP-03`, `SC-FER-07` and `SC-GEN-55` were checked.
  They assert the Standard-Model/Pati-Salam locus and one-generation Weyl-
  spinor reading; none supplies the stationary selector or observed transport.
- **FC-5 — met as a pathway, not as a build.** Execute `U1-1` through `U1-3`,
  then rerun the existing exact branching probe on the transported carrier.
- **FC-6 — met.** These matches are not a source derivation, prediction or
  confirmation. They demote if the selected stabilizer is not `G_SM`-shaped or
  if transport changes the representation fingerprint.
- **FC-7 — failed for admission.** The exclusions are inherited from a
  conventional 16 chosen against already-known Standard-Model content. No
  target-blind nontriviality witness belongs to the unbuilt source-owned
  selector.

The target-before-evaluation rule also fails for FC admission: the Standard
Model representation target predates the conditional comparator. The six rows
therefore terminate at B2 rather than being retroactively relabeled B1.

## Packet B — anomaly cancellation cannot select chirality

The five rows evaluate the conventional anomaly vector

```text
D = (SU(3)^3, SU(2)^2 U(1), SU(3)^2 U(1), U(1)^3, grav^2 U(1))
```

on the signed multiplicity lattice of the 16's six Standard-Model
constituents. LA-3's exact certificate proves

```text
rank(D) = 4,
2 D1 - 27 D2 - 36 D3 - 9 D4 + 9 D5 = 0,
ker_Z(D) = Z·(15 of SU(5)) + Z·(nu^c),
```

with saturation verified exhaustively over `[-3,3]^6`. Because this kernel is
a subgroup, `v in ker(D)` iff `-v in ker(D)`, and `0 in ker(D)`. The five
channels cannot distinguish a chiral 16 from its conjugate and cannot make the
complete 16 necessary. This is a bounded `PRECISE_IMPOSSIBILITY` for the
**anomaly-selector inference**, not for anomaly cancellation or for a future
source-native physical carrier outside the frozen arena.

| rows | exact terminal requirements |
| --- | --- |
| `AC-D1`--`AC-D5` | `AD4-1`: OWNER-A and OWNER-D construct one source-native observed 4D physical carrier from the non-chiral ambient field, with observation map and chirality-decoupling mechanism explicit. `AD4-2`: OWNER-A proves the selected carrier and reduction are invariant under the actual source action, including the open soldered-adjoint choice. `AD4-3`: OWNER-B evaluates each anomaly functional separately on that carrier, with its own responsive revival trigger and arena-extension controls. |

### FC-1--FC-7 audit

- **FC-1 — failed.** The ledger itself types the carrier and layer as
  `UNTYPED`; the conventional signed-multiplicity lattice supplies no physical
  pairing, real structure, domain, observation map or action owner.
- **FC-2 — met at observed anomaly-consistency rung R4.** The comparator
  supplies exact conditional zeros; it does not supply the source-native
  descent.
- **FC-3 — checked.** Retrieval covered W222, CB-C, AC-1, LA-3, PHI-1/2,
  SA-1, the per-row trigger corrections, comparator routing, the correction
  registry and ledger v0.263.
- **FC-4 — checked.** `SC-FER-05`, `SC-FER-07`, `SC-CHI-01`, `SC-CHI-50`,
  `SC-CHI-51` and `SC-CHI-54` were checked. The source asserts a non-chiral
  total carrier and emergent chiral-looking decoupling. It does not assert a
  net-chiral comparator as the source-native mechanism.
- **FC-5 — met as an exact pathway.** Execute `AD4-1` through `AD4-3`; a
  generic “select a chiral 16” is explicitly rejected because the exact
  functionals prove it cannot do the claimed selecting work.
- **FC-6 — met.** The anomaly zeros remain comparator facts only and demote as
  physical evidence if the transported carrier or charges differ.
- **FC-7 — failed for admission.** On the frozen arena the zeros are inherited
  and nondiscriminating: the empty carrier, a 15 and arbitrary right-handed-
  neutrino multiplicity are all anomaly-free. The calculation forbids no
  source-native mechanism and cannot select the target.

These five rows therefore terminate at B2. The precise impossibility prevents
the comparator from being misfiled as a B1 fitting construction or a B4
source-native route.

## Hostile review

- **Strongest overclaim:** “GU derives one anomaly-free Standard-Model family.”
  Refused. The exact results bind a conventional comparator and an unbuilt
  stabilizer/physical-carrier pathway.
- **Strongest contrary route:** a future source-native observed carrier may
  lie outside the six-coordinate comparator arena or carry additional
  observables. Preserved as the resurrection trigger; the impossibility is not
  class-wide.
- **Weakest reproducibility seam:** the five anomaly triggers were historically
  grouped despite having only four independent directions. The new registry
  preserves per-row identities and requires per-row responsive reevaluation.
- **Propagation seam:** terminal disposition must not rewrite ledger verdict,
  distance, revival trigger, mapping grade, claim status, source ownership,
  prediction credit, canon or public posture. All stay byte-identical or
  explicitly `none`.

Reproduce with:

```bash
python tests/channel-swings/chiral16_same_row_disposition_wave_probe.py --selftest
```
