---
title: "K193 order-six shifted face-entry wave"
document_role: active_research
doc_type: conditional_native_K139_K193_order_six_shifted_face_entry_result
created: 2026-09-10
date: 2026-09-10
claim_ceiling: rigorous repository-owned shifted Hermite--Genocchi divided-difference Taylor-entry operator for the K186 size-two and size-three regularizers, with shared rational face-box variables, determinant-level Cauchy cofactors, exact translation checks and strict directed-Arb neighborhoods around all 3 size-two and 15 size-three K192 face centers on 31/256<=x<=1/8; no adaptive tiling of the ordered physical gap-ratio domain, overlap proof with the K192 coalescent union, Duffy/Jacobi derivative envelope, complete positive-core error, outward order-six total, accurate prefix, action column, residual, complement or flux floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation
manifest: lab/process/k193-order-six-shifted-face-entry-wave.json
solver: tests/channel-swings/k193_order_six_shifted_face_entry.py
probe: tests/channel-swings/k193_order_six_shifted_face_entry_probe.py
target_claim: INTERNAL_TARGET:K192_SHIFTED_NONCOALESCENT_FACE_ENTRY_GATE
target_claim_verdict: SHIFTED_ENTRY_OPERATOR_AND_ALL_BANKED_FACE_CENTER_NEIGHBORHOODS_CERTIFIED__ORDERED_GAP_DOMAIN_TILING_AND_DUFFY_JACOBI_COMPOSITION_REMAIN_OPEN
canon_verdict_change: none
---

# K193 order-six shifted face-entry wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: K186's fixed K139/K156 hard-core `C3` impurity control, auxiliary chart
`lambda=256`, equal couplings one, three normalized K162 zero-bath seed orbits,
and complete K184--K192 order-six determinant family. K193 changes no operator,
extension, state, domain, polarization or scalar center. It constructs the
shifted local face operator that K192 explicitly withheld.

```gu-typed-objects
result: the exact K191 normalized divided-difference model admits a shifted Hermite--Genocchi Taylor form on one common rational face box; all 3 size-two and 15 size-three K192 face centers have strict directed-Arb neighborhoods on the previously rejected high-radial cell 31/256<=x<=1/8, with minimum regularizer lower bounds 0.90992808167357 and 0.44423198886215687
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on the order-six output sector; K184 coherent path-pair assembly and K186 specieswise determinant factorization remain unchanged ON=repository_signed_point_control
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, positive Laplace times, sequential confluent divided differences, normalized Bessel K1 kernel, exact translated rational monomial divided differences, complete Cauchy minor determinants with a separately outward normalization-product bound, order-16 Taylor arithmetic and python-flint 0.9.0 / FLINT 3.6.0 Arb balls
grading: K186 exact Cauchy--Vandermonde skeleton, K188 four-region split, K190 exact coalescent scaled-q spine, K191/K192 coalescent-centered outward union, K193 exact shifted-entry identity and 18 local face charts, complete 53-pattern and 468-occurrence formula propagation, two independent 200-digit face-center controls and withheld global tiling/cubature release
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W, K179 fixes the coefficient family, K184 fixes the time Grams, K185/K188 fix boundary majorants and K186/K187/K189/K190/K191/K192 fix factorization, endpoint class, raw-kernel failure, coalescent coordinates, normalized Taylor theorem and radial/face scaffold; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: serialize the shifted noncoalescent face-entry operator with shared chart variables and determinant-level Cauchy cofactors, certify neighborhoods of every banked face center, and expose adaptive ordered-domain tiling as the remaining prerequisite before Duffy/Jacobi composition MAP-TYPE=intertwiner
```

## Inline preflight bookend

K192 proves a radially stratified coalescent-centered union and banks every
nonempty active-gap mask. Its `1/32` size-three control fails on the cell from
`31/256` to `1/8`; raising Taylor order cannot fix a poor expansion center,
and raw determinant intervals would repeat K189's cancellation loss.

The strongest independently framed source-to-physics challenger remains a
positive physical state/domain and quotient construction. `SC-META-53` stays
`UNCERTAIN`; `LT-SM8`, `LT-GR6b`, `RA-F1` and `AC-F1` stay `NEEDS`. K193 does
not supply their action-owned background, coupled BV/BFV complex, positive
cohomology, count observable or chiral carrier.

The selected route therefore translates the complete K191 divided-difference
polynomial, recenters every entry on the argument range induced by one common
face box, and retains the exact normalized Cauchy cofactors at determinant
level. This directly tests the operator K192 named as missing.

## 1. Exact shifted entry identity

Let

```text
F_x(y)=x K1(x(1+y)),       G(y)=(1+y)^-1,
H_x(y)=F_x(y)-G(y).
```

For matrix entry `(i,j)` and rational shift `a`, K193 uses

```text
[r_0,...,r_i][c_0,...,c_j] H_x(w+r+c)
 = sum_n (F_x^(n)(a)-G^(n)(a))/n!
         [r_0,...,r_i][c_0,...,c_j](w+r+c-a)^n.
```

The shifted monomial divided differences are obtained by the exact
substitution `w -> w-a` in K191's symbolic polynomial. A binomial translation
reconstructs all original monomial matrices through order 16: 221 matrix
entries pass exact rational identity checks at the independent shift `7/64`.

For each entry, `a` is the midpoint of its Hermite--Genocchi argument range
induced by the common chart. The remainder uses the half-width of that range.
Complete monotonicity bounds the order-17 derivative at the range's lower
endpoint, so recentering reduces the tail without sampling or assuming
symmetry.

## 2. Shared face boxes and determinant cofactors

K193 uses K192's centers

```text
r0=1/32, r1=1/64, c0=1/40, c1=1/80
```

whenever the corresponding mask coordinate is active. Each active coordinate
has half-width `1/512`; each inactive coordinate ranges from zero to `1/512`.
The same rational intervals feed every entry in the matrix. Thus entrywise
centers differ only as deterministic functions of one shared chart; the
matrix does not receive independently fitted centers.

The pure Cauchy part still satisfies

```text
det(A) product_ab(1+w+r_a+c_b)=1.
```

For the linear Bessel correction, K193 evaluates the whole Cauchy minor before
absolute bounding, then multiplies by a separately outward bound for the
common normalizing product. Quadratic and cubic correction terms retain
complete determinants. This is the same
cancellation boundary K191 required, now on shifted face boxes.

## 3. Outward face-center certificate

On the fixed radial cell

```text
31/256 <= x <= 1/8,       0 <= w <= 1/31,
```

all three size-two and all 15 size-three face charts are strictly positive.

| Size | charts | minimum `R_m` lower | maximum `R_m` upper |
| --- | ---: | ---: | ---: |
| 2 | 3 | `0.90992808167357` | `1.09007191832643` |
| 3 | 15 | `0.44423198886215687` | `1.5557680111378431` |

Independent 200-digit values at the fully active centers and minimum cross
argument `63/512` are `0.9371451860725112284384350` and
`0.8864999310162916342882860` for sizes two and three. Both lie inside their
outward chart intervals. The Decimal values are controls; Arb carries the
proof.

The operator formula applies to all 53 K186 patterns and all 468 nontrivial
occurrences, preserving all 234 entries and 18 coherent groups. An occurrence
inherits an outward bound only when its normalized gap tuple lies in a
declared chart.

## 4. Honest boundary and continuation

The 18 local charts do not yet tile the ordered physical gap-ratio domain.
They also do not prove overlap with every boundary of the K192 coalescent
union. K193 therefore releases neither arbitrary-gap coverage nor the
Duffy/Jacobi chain-rule derivatives, positive-core cubature remainder,
complete order-six total or accurate prefix. The prefix remains through order
five.

The next exact work is adaptive tiling in the ordered gap variables: reuse the
shifted operator on rational chart centers, certify overlap with K192, and
prove a gapless finite union for every K186 time-node pattern. Only then should
the same common-chart model be differentiated for Duffy/Jacobi remainder
bounds and composed with K185/K188.

## Reproduction

```bash
_local/cas-venv/bin/python tests/channel-swings/k193_order_six_shifted_face_entry.py --summary
_local/cas-venv/bin/python tests/channel-swings/k193_order_six_shifted_face_entry.py --write --summary
_local/cas-venv/bin/python tests/channel-swings/k193_order_six_shifted_face_entry_probe.py
_local/cas-venv/bin/python tests/channel-swings/k193_order_six_shifted_face_entry_probe.py --no-replay --selftest
```

The probe checks the translated identity, common chart widths, all 18 masks
and strict lower bounds, entry geometry, two independent controls, complete
family formula propagation, deterministic replay and every withheld-release
fence.

## Inline postflight bookend

K193 closes the operator-level dependency isolated by K192 and proves that
the formerly rejected high-radial cell has nonzero outward neighborhoods at
every banked face center. It does not convert those neighborhoods into a
global atlas. No physical, source, ledger, canon, paper, release or public-
posture effect follows.
