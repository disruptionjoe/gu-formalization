---
title: "Selected K77 unreduced hyperbolic-domain gate"
artifact_type: exploration_result
created: 2026-08-11
status: UNREDUCED_POSITIVE_SYMMETRIZER_CONE_EMPTY__STANDARD_CAUCHY_ROUTE_KILLED__NONLOCAL_OR_REDUCED_DOMAIN_OPEN
grade: "exact rational real-Cl(7,7) principal-symbol theorem; no global domain, quotient, spectrum, index or physics recovery"
canon_verdict_change: none
---

# Selected K77 unreduced hyperbolic-domain gate

## Result first

The actual real-K77 source-shaped `Omega1(S) plus Omega0(S)` evolution fails a
necessary condition for the ordinary one-time maximal-dissipative Cauchy
route. In each of the three observed spatial directions its exact evolution
generator `E_j=D(dt)^{-1}D(dx_j)` obeys

```text
E_j^2 - 1 = U_j Q_j
rank(U_j Q_j) = 128
(U_j Q_j)^2 = 0.
```

Thus `E_j` is not diagonalizable. If a positive definite symmetrizer `H`
satisfied `H E_j=E_j^T H`, then `H^(1/2) E_j H^(-1/2)` would be symmetric and
`E_j` would be diagonalizable. Contradiction. The positive simultaneous-
symmetrizer cone is empty before any right-H or boundary-condition demand.

This is a scoped adverse result. It kills the **unreduced positive-energy,
strong/symmetric-hyperbolic, standard maximal-dissipative one-time route**. It
does not kill every closed graph realization, a nonlocal constrained
ultrahyperbolic theory, a source-derived constraint/BV reduction, or a weak
distributional evolution.

## Layer 0 and the domain trap

Three distinctions carry the result:

1. The operator is the current dimension-`1920` real-K77 source-shaped rolled
   operator, not the earlier gamma-traceless W131 carrier.
2. Positive Hilbert symmetrizers are not indefinite Krein/Green pairings. The
   latter may remain nondegenerate without supplying positive energy.
3. A one-time Lorentz-section Cauchy problem is not the ambient `(7,7)` Y14
   problem. `PD-ULTRAHYPERBOLIC-DOMAIN` applies: ordinary Lorentzian Cauchy,
   APS, or Baer--Ballmann theory cannot be ported to the ambient operator.

The section-level failure is nevertheless decisive because it kills even the
necessary ordinary route proposed by v0.167. The ambient successor must own a
nonlocal ultrahyperbolic constraint or another domain theory; it cannot inherit
one from the noncharacteristic symbol inverse.

## Same-object transfer, now exact

Prior Jordan calculations did not by themselves settle this gate because the
old W131 carrier was a different object. The present probe constructs the
current K77 source symbol directly and verifies on time plus all three observed
spatial axes that its displayed-sign predecessor satisfies

```text
D_current(k) = -P D_prior-minus(k) P,
P = diag(-1_{Omega1(S)}, 1_{Omega0(S)}).
```

The overall sign and constant conjugacy cancel in `D(dt)^{-1}D(dx_j)`. This is
the missing same-object bridge to the source-shaped B2C3 calculation, while the
older W131 result remains only prior-art guidance.

## Exact factorization

Let `T=gamma(dt)`, `X=gamma(dx_j)`, and let `w` run over the twelve spectator
directions. On an input `(zeta_t,zeta_x,zeta_w,nu)`, define

```text
Q_j(zeta,nu) = sum_w gamma(w) zeta_w - nu,
U_j q = (T q, X q, 0_w, 0).
```

Direct block multiplication over `QQ` on the full real `Cl(7,7)` module gives
`D(dt)E_j=D(dx_j)`, `E_j^2-1=U_jQ_j`, and `Q_jU_j=0`. The `-nu` block makes
`Q_j` onto and `T` makes `U_j` injective, so the remainder has rank exactly
the real spin dimension `128`, and square zero. This is an exact identity, not
a finite-difference, eigenvalue-tolerance, or complexified-real-form claim.

The contrary pure `1 tensor gamma` Omega1 completion is semisimple and squares
to one. Substituting it would delete the source-shaped `k tensor Gamma` term
and manufacture hyperbolicity for a different operator; the planted control
rejects that substitution.

## Source return

- `SOURCE-CONFIRMS`: the first-order four-field/operator grammar, and the
  distinction between ordinary one-time dynamics and the ambient
  ultrahyperbolic technical debt.
- `SOURCE-SILENT`: a positive symmetrizer, removal of the rank-128 generalized
  characteristic chains, a source-derived constraint/BV reduction, and an
  ambient nonlocal domain.
- `SOURCE-CORRECTS`: none.

## Consequence for construction

The next high-information object is not a fitted boundary graph. It is one of:

1. a **source/action-derived invariant constraint or BV differential** whose
   quotient removes the generalized characteristic chains and preserves the
   observed carrier; or
2. an **explicit nonlocal ultrahyperbolic constraint** for the ambient
   source-shaped first-order operator, tested for action/Green/BFV compatibility.

The old conditional characteristic quotient is not promoted: prior work shows
the source `d` annihilates only on the characteristic cone and is not an
off-shell BV differential. The graph family remains transported and
unselected with at least 120 coordinates. P1/P2/P3 remain unused.

## Validation

`selected_k77_unreduced_hyperbolic_domain_gate_probe.py` passes `52/52` exact,
typed, analytic and planted checks on the full rational `1920 x 1920`
construction. No spectrum, physical positivity, chirality, mirror removal,
index, generation count, mass, canon verdict or public posture is claimed.
