# Conditional physics ledger v0.95

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.95.json`.

## Progress

`Ledger v0.95 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.95 adds

The source-coordinate metric partial is now explicit. With independent
`varpi` and `epsilon` held fixed,

```text
T = varpi - B_LC(g,epsilon),  A = B_LC + T = varpi,
delta T = -delta B_LC,        delta A = delta F_A = 0.
```

The curvature zero is an exact cancellation among three separately nonzero
derivatives of `F_B`, `d_B T`, and `T wedge T`. Differentiating only the
stationary shorthand `F_A*=T* wedge T*` gives a false live term.

The complete covariant spin-Levi-Civita first-jet map has rank `20` inside the
unrestricted rank-`24` Lorentz-connection carrier. At each timelike,
spacelike and null covector, its physical transverse restriction has rank
`6`. The dependent observation-germ derivative multiplies the raw residual
`Upsilon*=0`, so it adds no term and preserves that rank.

This closes the local fixed-`varpi` metric block of `D_g Upsilon`. It does not
close the common-field action operator.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `4`; conditions opened: `0`.

Closed:

1. fixed-`varpi` component-normal `delta T`;
2. expanded fixed-`varpi` `delta F_A=0`;
3. complete rank-`20` covariant Levi-Civita first-jet image;
4. moving observation chain rule at raw residual zero.

Remaining named condition: `1`.

1. Compose the complete local metric block with the owned `D_varpi` and
   `D_epsilon` blocks and v0.92 `K_loc`, then derive the action-density formal
   adjoint and Green concomitant before analytic/symplectic domain work.

P1/P2/P3 remain unchanged and unused. No residue, quotient, fork, canon,
claim-status, public-posture, Curt-track or third-lane status changes.

## Evidence

- `explorations/conditional-build/selected-k77-fixed-varpi-normal-frechet-closure-2026-08-08.md`
- `lab/process/selected-k77-fixed-varpi-normal-frechet-closure.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-fixed-varpi-normal-frechet-closure-review.md`
