# Conditional physics ledger v0.91

Ledger v0.91 remains fully mapped at `82/82`: `32 SAME`, `19 DIFFERS`,
`26 NEEDS`, and `5 OVER-DETERMINED`. Residue remains `84` continuous real
parameters before quotient, at least `19` function-valued slots, `9` open
discrete forks, and five scoped conditional quotients. P1/P2/P3 are unchanged
and unused.

This migration corrects a queue-level object conflation. The v0.77 complete
first-action Euler bank is retained as complete at its declared pointwise
grade. It is not the residual Jacobian `J=D Upsilon`. Stationary residual
naturality determines the four physical Ward-orbit columns, while exact
transverse plants prove that these columns leave six metric directions of `J`
undetermined in every causal class.

Two named conditions close:

1. the duplicate request to rebuild the first-action bank is retired;
2. the stationary Ward-orbit theorem is separated from full-J construction.

Two named conditions remain:

1. the six transverse physical K77 `D_g Upsilon` columns plus residual `K`;
2. formal adjoint, Green, symplectic/BFV and common-domain descent.

K77 is the unique ordinary Majorana--Weyl horn among K77/K95 only conditional
on requiring a real chiral carrier without extra symplectic-doublet
structure. This changes neither a ledger verdict nor an
external datum.
