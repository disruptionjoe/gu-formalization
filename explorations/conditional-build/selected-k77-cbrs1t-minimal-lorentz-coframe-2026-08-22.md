---
title: "Selected-K77 CBRS-1T minimal Lorentz-coframe pointwise reopener"
status: active_research
doc_type: exact_minimal_indefinite_coframe_pointwise_reopener
created: "2026-08-22"
registry: lab/process/selected-k77-cbrs1t-minimal-lorentz-coframe.json
probe: tests/channel-swings/selected_k77_cbrs1t_minimal_lorentz_coframe_probe.py
grade: "EXACT RECONSTRUCTION-GRADE POINTWISE FORMAL COFRAME BODY AND COMPLETE FIXED-METRIC SYMBOL; NO SOURCE COFRAME, LOCAL OR GLOBAL VACUUM, OR SPECTRUM"
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_ACTION_PRIMITIVE_EPSILON_AND_METX_GRAMMAR__REPOSITORY_FREEZES_THE_MINIMAL_FIXED_LORENTZ_COFRAME_OWNER_AND_DERIVES_THE_POINTWISE_REOPENER__SOURCE_SILENT_ON_THE_MULTIPLET_AND_CLASS
canon_verdict_change: none
---

# Selected-K77 CBRS-1T minimal Lorentz-coframe pointwise reopener

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: CBRS-1T exact minimal fixed-Lorentz coframe pointwise body and complete fixed-metric symbol
carrier: complete 230650-dimensional real CBRS-1P T-plus-independent-Spin-connection carrier extended by four repository-constructed real Grassmann-even base scalars and their rank-four Lorentzian first-jet matrix LAYER=toy CHIRALITY=N/A
pairing: selected K77 action-density pairing plus fixed internal Lorentz eta_AB contracted with the Lorentzian inverse base metric and unit radial potential ON=pointwise_four_scalar_jet_extended_carrier
real_structure: normalized real K77 base-J4 radical pair with real scalar values first covectors and d'Alembertian jets; internal eta has the same fixed inertia as the base
grading: Clifford and form grades inherited unchanged; all Phi^A are Z2-even and their first jets are not a K77 Spin connection dynamical tetrad or Grassmann-odd fermion
action_owner: repository-construction
target: real pointwise formal two-jet body stationarity complete fixed-metric Hessian symbol gauge quotient and full intrinsic MET(X) tensor MAP-TYPE=evaluation
```

## Result first

CBRS-1T decides the derivative-owner fork before reading a J4 density. A
four-dimensional top form has an isotropic Hilbert tensor, but its Euler
equation leaves a conserved continuous Hodge-dual flux. Without an
action-owned quantization or selector, choosing that flux to match a J4
density is exactly the prohibited post-target sector fit. The top-form route
is therefore not selected; this is not a universal top-form no-go.

The smallest non-flux alternative has four real Grassmann-even base scalars
`Phi^A`. Fewer than four gradients produce a pullback Gram tensor of rank at
most three and cannot equal a nonzero multiple of a rank-four metric. Freeze
the internal form `eta_AB` with the same fixed Lorentz inertia as the base,
write `rho=eta_AB Phi^A Phi^B`, and use the target-blind action

```text
L_T = C3(T) + rho Q2(T)
      + (1/2) g^{mu nu} eta_AB d_mu(Phi^A) d_nu(Phi^B)
      + (rho-1)^2/4.
```

The internal form, unit radial orbit, kinetic coefficient and potential are
fixed for the whole class. No J4 radical, action density, desired zero,
internal frame or signature sector sets them.

On the base-J4 radical pair, whose common selected-action density `I_base` is
negative, there are two exact real pointwise formal coframe bodies:

```text
T = T0,                 rho = 1,
eta_AB d_mu Phi^A d_nu Phi^B = lambda^2 g_mu nu,
lambda^2 = -I_base > 0,
box(Phi^A) = 6 I_base Phi^A.
```

The same unit-spacelike internal orbit, frame class and exact scale serve both
base-J4 signs. All four multiplet Euler rows and all ten intrinsic metric rows
vanish. The normal-J4 density is positive, so the same fixed-signature real
coframe equation would require `lambda^2=-I_normal<0`; neither normal branch
continues in this class. No signature or frame is flipped to recover it.

The complete fixed-metric `T` plus independent connection plus multiplet fiber
has dimension `230654`. At zero covector, the three transverse scalar modes
have exact Schur value `6I_base` and the radial mode has
`30I_base+2`. Both are nonzero. Each licensed base branch therefore has rank
`230614`, nullity `40`, and kernel exactly the inherited broken
diagonal-Spin gauge orbit. The real Lorentzian transverse characteristic is
threefold and raises nullity to `43`; the distinct radial characteristic
raises it to `41`. These extra directions are scalar characteristics, not
gauge.

This reopens CBRS-1 at pointwise formal-body grade. It does not yet supply a
common local or global solution, a source-selected coframe, full coupled
spacetime stabilizer, spectrum, physical quotient or observed 3+1 bridge.

## Structural fork and minimality

For a four-form `F=f vol_g`, the local field equation makes `f` constant and
the Hilbert tensor is proportional to `g`. That removes the trace-free rank
problem but does not select its magnitude. The two J4 radical families have
different nonzero densities of opposite signs. A fixed kinetic sign and one
continuous flux cannot cover both, and choosing a matching flux only after
reading one family is target fitting. A future quantized or action-selected
flux owner would be materially new and is not excluded here.

For `N` one-form gradients assembled into a `4 by N` matrix `P`, every
internal-bilinear pullback has rank at most `N`. Nonzero isotropy requires

```text
P eta P^T = c g,        c != 0,
```

whose right side has rank four. Thus `N>=4`. At `N=4`, `P` is invertible and
Sylvester inertia fixes the pullback signature to the internal signature. A
same-signature internal Lorentz form admits `+lambda^2 g` and cannot silently
be turned into `-lambda^2 g` by a real internal frame. This makes four the
smallest fully typed owner and prevents the normal-J4 failure from being
repaired by a disguised branch-selected signature change.

The representative `Phi=e_1`, `dPhi=lambda I` only fixes coordinates on the
single invariant unit-spacelike orbit and coframe class. It is not a physical
choice of one preferred internal frame. Both licensed J4 signs use the same
orbit and scale.

## Exact body and metric stationarity

At `rho=1`, the `T` Euler row is exactly that of `C3+Q2` and vanishes on every
J4 body. Homogeneity again gives

```text
Q2(T0)=3I0,             C3(T0)=-2I0.
```

The raised multiplet equation is

```text
-box(Phi^A) + (2Q2 + rho - 1) Phi^A = 0,
```

so the displayed real second jets close it. Prescribing those finite jets at
a point is formal local data, not a solved PDE.

Let

```text
B_mu nu = eta_AB d_mu Phi^A d_nu Phi^B.
```

With inherited graph-visible connection momentum zero, the intrinsic metric
equation, after a harmless factor of two, is

```text
E_mu nu = B_mu nu - g_mu nu L_T.
```

For `B=lambda^2 g`, its kinetic contraction is `4lambda^2` and

```text
E_mu nu = (-I0-lambda^2) g_mu nu.
```

The base family therefore closes exactly at `lambda^2=-I_base`. Numerically,

```text
lambda^2 = 0.177047119426730... .
```

The trace-free part vanishes because the four gradients form a full Lorentz
coframe, while the remaining trace equation fixes the solution scale rather
than an action coefficient or internal frame. Both base-J4 signs share the
same density and scale.

## Complete fixed-metric symbol and quotient

At fixed intrinsic metric, the multiplet decomposes into the radial direction
through `Phi` and three `eta`-orthogonal directions. The radial mixed column is
the CBRS-1S column, so its exact quotient Schur symbol remains

```text
g^{-1}(xi,xi) + 30 I_base + 2.
```

The transverse directions have no `T` mixing and carry

```text
g^{-1}(xi,xi) + 6 I_base
```

with multiplicity three. At zero covector all four values are nonzero. Adding
them to the inherited exact rank `230610` gives rank `230614` and leaves only
the 40 broken diagonal-Spin columns in the kernel. The multiplet is a
Spin(7,7) scalar, and its internal Lorentz invariance is global rather than an
extra gauge group in this frozen action.

Because `I_base<0`, both characteristic norms are positive and have real
spacelike covectors. The transverse shell has three physical scalar
directions, while the radial shell has one. Neither shell changes the
zero-covector gauge quotient.

## Retrieval, controls and hostile review

Mechanism-level retrieval found CBRS-1S, earlier conventional Higgs/VEV
comparators, scalaron auxiliaries, coframe/soldering discussions, flux-index
results and prior metric-stress reconstructions. None freezes this minimal
four-scalar internal-Lorentz owner, derives its rank lower bound, selects the
top-form fork before target evaluation and composes the exact J4 metric and
complete-symbol result. Older objects retain their recorded types and scopes.

- **Strongest overclaim:** two pointwise formal coframe bodies are not two
  local or global vacua. Exactness of four first gradients does not solve the
  coupled PDE or boundary problem.
- **Strongest contrary construction:** a quantized or action-selected
  top-form flux can become target blind. This result excludes only an
  unselected continuous flux sector.
- **Strongest real-form trap:** reversing the internal Lorentz inertia after
  reading the normal-J4 sign defines a different owner; it is not a frame
  change within this class.
- **Strongest tuning trap:** the metric equation solves for a dynamical first-
  jet scale on one radical pair. It does not tune an action coefficient, and
  the same scale serves both signs of that pair.
- **Strongest gauge trap:** global internal rotations and scalar
  characteristics are not added to the inherited diagonal-Spin gauge orbit.
- **Strongest completeness trap:** the `230654` symbol holds the intrinsic
  metric fixed and checks its full Euler row separately. It is not yet the
  coupled spacetime metric/coframe stabilizer or spectrum.
- **Weakest reproducibility seam:** the large inherited rank imports CBRS-1P.
  The exact probe separately checks the four-gradient rank bound, both radical
  density signs, metric tensor cancellation, both Schur shells and all hostile
  plants.

No ledger verdict, canon, source ownership, residue, particle assignment,
prediction, confirmation or public posture changes.

## Reverse-scaffold consequence

CBRS-1T is the first owner in the reverse scaffold to produce a nonconstant
pointwise formal J4 body that is fully stationary under the complete intrinsic
metric equation while retaining an exact non-gauge scalar characteristic.
The result is partial across the radical class: it licenses the base-J4 pair
and rejects the normal pair in the same fixed-signature owner.

Continue with `CBRS-1U`: lift the two base-J4 pointwise coframe bodies to one
common local formal or actual solution, or prove the exact integrability
obstruction, then derive the full coupled stabilizer and spectrum. Preserve
the fixed action, internal signature, unit orbit and common scale; do not
choose a global coordinate chart, boundary condition, flux, frame or sector
after a desired result. Do not advance to CBRS-2 until that local/global and
complete-spectrum gate closes.

Reproduce with:

```bash
sage -python \
  tests/channel-swings/selected_k77_cbrs1t_minimal_lorentz_coframe_probe.py
```

The exact probe passes `60/60` after native propagation.
