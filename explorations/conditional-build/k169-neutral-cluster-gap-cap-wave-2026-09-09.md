---
title: "K169 neutral-cluster gap-cap wave"
status: active_research
doc_type: conditional_native_neutral_cluster_HVZ_threshold_and_finite_trial_complete_complement_gap_cap_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned fixed-charge theorem that a same-flavor particle-hole neutral cluster places E_ref(q)+5/2 in the K143 HVZ threshold set and caps every finite K162 complete-complement margin above a variational ground trial by 5/2; this kills K168's three-unit base-to-reference transfer route but leaves direct reference-specific K152 enclosure at a smaller positive gap open; the first threshold, native ground, form, Gram, residual, complement floor and left floor remain unevaluated, and no physical or source selection, Born derivation, prediction or confirmation follows
manifest: lab/process/k169-neutral-cluster-gap-cap-wave.json
solver: tests/channel-swings/k169_neutral_cluster_gap_cap.py
probe: tests/channel-swings/k169_neutral_cluster_gap_cap_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K169 neutral-cluster gap-cap wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: this packet stays on the repository-supplied K139--K168 massive signed
particle/hole point control. It uses K143's cluster formula and K162's finite
trial spaces to decide whether K168's three-unit transfer criterion can ever
hold. It neither selects the reference operator nor computes its ground state.

```gu-typed-objects
result: a same-flavor particle-hole escape cluster has zero charge and energy infimum 2 tau=5/2, so every finite K162 trial has complete M-orthogonal complement margin at most 5/2; K168's direct three-unit transfer condition is therefore unattainable, while direct reference-specific K152 certification remains open
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), in a fixed conserved charge sector, with K139 inverse chart and finite K162 dyadic trial ranges LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: physical Fock Hilbert pairing transported by S=(1-G_256)^-1 to the regular-coordinate metric M=S* S; physical orthogonality to S P is exactly M-orthogonality to P ON=repository_signed_point_control
real_structure: CAR adjoint, momentum conjugation and the Hermitian reference extension; the escaping particle and hole are adjoint excitations of the same flavor
grading: conserved incidence charges q_i=n_i+N_i+-N_i-, hard-core impurity degree, particle/hole number, dyadic level, HVZ cluster channel and generalized-pencil inertia
action_owner: repository-construction -- no complete response datum, Weinstein source or GU action selects the extension, scalar center, polarization, couplings, domain or state
target: decide the K168 transfer route from the native cluster geometry, retain direct R_ref computation as the next path, and refuse any native K152, physical, source, Born, prediction or confirmation promotion MAP-TYPE=intertwiner
```

## Inline preflight bookend

K168 supplied the complete trace-zero flavor-symmetric reference
`W_ref=diag(-2,1,1)` and an exact three-unit shape-oscillation budget, but its
next gate asked for a base complete margin strictly greater than three. The
object-level route census reloaded K139's chart, K143's HVZ formula, K148's
charge bookkeeping, K152's enclosure contract and K162's finite conforming
trials. The decisive route is the lowest explicit charge-neutral escape
cluster, because finite-dimensional projection cannot remove a Weyl sequence.

No serialized limiting action column, complete-carrier flux estimate or native
reference-specific form packet was found, so direct `R_ref` evaluation is
outside this wave. `SC-META-53` remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and
`AC-F1` remain `NEEDS`. The source register and v0.263 physics ledger do not
move.

## 1. The neutral escape cluster

K148's conserved charges have the signed form

```text
q_i = n_i + N_i+ - N_i- .
```

Choose one escaping particle and one escaping hole of the same flavor. Their
charges are `e_i` and `-e_i`, so their sum is zero and the fixed total charge
sector is preserved. K143's massive dispersion has one-particle infimum
`tau=5/4`. Hence this compatible two-particle cluster has energy infimum

```text
2 tau = 5/2.                                                (1)
```

For every fixed sector `q`, the K143 HVZ threshold set therefore contains

```text
E_ref(q) + 5/2.                                             (2)
```

Equation (2) is threshold membership, not identification of the first
threshold. The bounded local impurity extension changes neither the remote
particle mass nor the escape cost.

## 2. Every finite trial complement inherits the cap

If `E_ref(q)` is an isolated ground value, tensor an approximating ground
vector with a same-flavor particle-hole pair translated to infinity. This is a
Weyl sequence at (2). If no isolated ground exists, the desired positive
complete ground gap is already absent.

Let `P` be any finite K162 trial space and `Q=1-P`. A Weyl sequence escaping to
infinity converges weakly to zero, so its projection into `P` vanishes. After
normalizing `Q psi_n`, its Rayleigh values still converge to (2). Thus, for a
trial Rayleigh value `rho >= E_ref(q)`, the complete physical-complement
margin satisfies

```text
inf spec(H_ref | (P_phys)^perp) - rho
    <= E_ref(q) + 5/2 - rho
    <= 5/2.                                                  (3)
```

This is an upper cap only; it does not identify the actual complement floor.

## 3. K139 transports the cap without changing it

For the fixed chart `S=(1-G_256)^-1`, set `M=S* S`. If `P` is a regular-
coordinate trial space, then

```text
v in P^(perp_M)  iff  S v in (S P)^perp.                    (4)
```

Consequently (3) is exactly the cap on the complete `M`-orthogonal complement
used by K152/K165. Substituting the free Hilbert complement would be a category
error; no such substitution is made here.

## 4. K168's direct transfer route is impossible

K168's direct base-to-reference argument needs a base complete margin strictly
greater than the reference shape oscillation `3`. Equations (1)--(4) instead
give the universal finite-trial cap

```text
complete margin <= 5/2 < 3.                                 (5)
```

Therefore no evaluation of the K139 base form or Gram can make that transfer
condition true. The direct base-to-reference gap and complement transfer route
is killed by the native neutral cluster.

This does not kill direct computation on `R_ref`. K152 needs only a positive
reference-specific gap below the native threshold, not a gap greater than
three.

## 5. Exact positive control below three

The nonidentity metric and form

```text
M = [[ 1, 1, 0],       R = [[-1,-1,0],
     [ 1, 2, 0],            [-1, 0,0],
     [ 0, 0, 1]]            [ 0, 0,3]]
```

have generalized spectrum `(-1,1,3)`. The trial `e_1` has exact value `-1`
and zero residual. At threshold zero, `R` restricted to the `M`-orthogonal
complement has inertia `(0,0,2)`, while the full pencil has inertia `(1,0,2)`.
Thus there is exactly one value below zero and a valid gap `2<3`. This is an
abstract exact control, not native K139 data.

## 6. Route verdict and next condition

The three-unit K168 transfer gate is permanently closed by (5). The next
coherent route is to evaluate the same limiting reference-specific form
`R_ref` and physical Gram `M` on a small K162 trial, compute the complete
form-dual residual, and prove a positive `M`-orthogonal complement or flux
floor below `E_ref(q)+5/2`. A native left floor still requires a separately
selected scalar center.

Claim ceiling: K169 proves threshold membership and a universal complement-
margin cap. It does not identify the first threshold, ground value, native
form, Gram, residual, complement floor or left floor. It does not supply a
native K152 interval, physical or source selection, Born derivation,
prediction or confirmation.

## Inline postflight bookend

The K169 exact solver serializes the neutral cluster, finite-codimension
projection, K139 chart transport, transfer disposition and a nonidentity-
metric direct-route control. The independent probe checks 30 invariants and
its baseline-first hostile selftest catches 18 mutations. The machine manifest
records the route verdict and preserves every source and publication fence.

Reproduce with:

```bash
python3 tests/channel-swings/k169_neutral_cluster_gap_cap.py --demo
python3 tests/channel-swings/k169_neutral_cluster_gap_cap_probe.py
python3 tests/channel-swings/k169_neutral_cluster_gap_cap_probe.py --selftest
```
