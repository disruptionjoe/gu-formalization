# Conditional physics ledger v0.81

Status: append-only successor to v0.80.

```text
Ledger v0.81 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Tightness — T4×1 · T3×3 · T2×1
Scoped quotients ranked — 5
```

## What moved

Five gravity/source-action rows move only in distance, evidence and mapping
grade: `LT-GR1`, `LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`.

v0.80 correctly retained the ten metric-section Euler coordinates but then
named a closed ten-dimensional selected-action Euler complex as the next
object.  v0.81 retracts that closure wording after composing existing exact
results:

- the completed first layer is a `34`-variable coupled source symbol after
  elimination of a nondegenerate `196`-dimensional grade-one bank;
- it is Ward-basic with rank `30` and gauge radical `4` on all three causal
  orbits, hence has generic physical symbol cohomology zero;
- its exceptional two-mode `N2` locus is helicity one, not helicity two;
- the separate second-layer metric block retains the exact TT helicity-two
  polynomial but has full Ward-defect rank four;
- naively adding that block to the first layer preserves the defect; and
- formal symmetric Ward completions form a 21-dimensional affine family, so
  a fit is not an informative construction.

The ten metric equations remain necessary.  The primary gate is now the full
selected two-layer Hessian on one common field/jet bundle and stationary
background, with action-derived metric--connection, grade-one, matter and
observation cross blocks and the coupled Ward complex.

## What did not move

Coverage, verdict counts, residue, tightness, five scoped quotients,
P1/P2/P3, canon, claim status and public posture are unchanged.  No new field,
coefficient, datum or quotient is booked.  No hyperbolicity, Green/Krein,
BFV, graviton, Einstein or cosmological claim is promoted.

Machine truth:
`lab/process/conditional-physics-ledger-v0.81.json`.

Construction result:
`selected-k77-coupled-euler-complex-scope-2026-08-08.md`.
