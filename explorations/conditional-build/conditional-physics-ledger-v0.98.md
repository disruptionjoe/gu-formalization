# Conditional physics ledger v0.98

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.98.json`.

## Progress

`Ledger v0.98 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.98 adds

The exact physical four-dimensional Ward graph now closes on the same
raw-`Upsilon` carrier in all three causal classes:

```text
delta_g T              = -q eta,
delta varpi             = q eta + [T,eta],
delta_epsilon Shiab     = moving-Phi response,
delta Upsilon           = 0 at Upsilon*=0.
```

The physical Jacobian has rank four, while its spin/Cartan raw-residual image
has rank three. The remaining longitudinal metric direction has zero output;
the grade-one gamma construction is not needed to manufacture a fourth Ward
column.

The v0.97 rank-four defect is therefore retyped as a composition artifact. It
froze the timelike response operator under spacelike and null labels and used
the conditional grade-one gamma orbit instead of the matched source-native
bivector/Kosmann response. The exact negative controls leave rank three if the
operator is frozen, the lower Cartan commutator is omitted, or moving Shiab is
omitted.

This closes the dependent physical four-column orbit. It does not construct
arbitrary primitive `D_epsilon Upsilon`, action Euler/Noether,
presymplectic/BFV descent, a global common domain, or a field-space Riesz map.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `3`; conditions opened: `0`.

Closed:

1. matched-q physical metric--Cartan--epsilon `J R=0`;
2. the need for grade-one gamma soldering to supply a fourth physical Ward
   direction;
3. the v0.97 rank-four defect as a physical-diffeomorphism burden.

Remaining named conditions: `3`.

1. Extend `K_loc` equation-dual/formal-adjoint/Green to the physical
   metric--`varpi`--epsilon graph.
2. Derive action Euler/Noether and the presymplectic/preboundary class.
3. Derive a geometrically owned field-space Riesz map, or retain explicitly
   covector-valued adjoint language; construct arbitrary primitive
   `D_epsilon` only if these gates require it.

P1/P2/P3 remain unchanged and unused. No residue, quotient, fork, canon,
claim-status, public-posture, Curt-track or third-lane status changes. The
selected Spin-native parent, product of the two `U(32,32)` Weyl halves and full
`U(64,64)` comparator remain distinct action-parent questions.

## Evidence

- `explorations/conditional-build/selected-k77-source-native-diffeomorphism-ward-closure-2026-08-08.md`
- `lab/process/selected-k77-source-native-diffeomorphism-ward-closure.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-source-native-diffeomorphism-ward-closure-review.md`
