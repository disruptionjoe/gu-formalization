---
title: "K183 order-five coherent Gram and compression wave"
document_role: active_research
doc_type: conditional_native_K139_K183_order_five_coherent_gram_compression_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned canonical exterior-coordinate assembly for all 32 K179 order-five exchange paths, rigorous finite global outward intervals for all 64 self/cross Gram entries, localized analytic-contraction interval witnesses for all 12 coherent group norms, and an exact Laplace-simplex species-determinant compression identity on the fixed equal-coupling point control; no order-six-through-twelve numerical prefix, complete action column, residual, complement or flux floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation is evaluated
manifest: lab/process/k183-order-five-coherent-gram-compression-wave.json
solver: tests/channel-swings/k183_order_five_coherent_gram.py
probe: tests/channel-swings/k183_order_five_coherent_gram_probe.py
target_claim: INTERNAL_TARGET:K179_ORDER_FIVE_COHERENT_GRAM_AND_COMPRESSION_GATE
target_claim_verdict: ALL_TWELVE_GROUPS_ASSEMBLED__ALL_64_GRAM_ENTRIES_OUTWARDLY_ENCLOSED_AFTER_EXTERIOR_PROJECTION__ALL_GROUP_NORMS_CERTIFIED_NONZERO__HYBRID_LITERAL_TO_DETERMINANT_DAG_SWITCH_SELECTED_AT_ORDER_EIGHT__ORDER_SIX_TIME_INTEGRATION_GATE_OPEN
canon_verdict_change: none
---

# K183 order-five coherent Gram and compression wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: the fixed K139/K156 hard-core `C3` impurity coupled to four
particle/hole bath species on `L2(R)`, auxiliary chart `lambda=256`, equal
couplings one, and the three normalized K162 zero-bath seed orbits. K183
consumes K179's coefficient-complete exchange family and K182's canonical
exterior-coordinate convention. It changes no operator, extension, state,
polarization, domain or scalar center.

```gu-typed-objects
result: all 32 coefficient-complete order-five paths map to 12 coherent output groups; every path is normalized and antisymmetrized specieswise before any interval operation, all 64 unique self/cross Gram entries are outwardly enclosed, all 12 group norms have positive localized witnesses, and the higher-order exterior representation switches from literal sums to exact species determinants at order eight
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on the order-five output sector; distinct seed/output signatures are orthogonal and every common-signature path pair is paired only after complete normalized specieswise exterior projection ON=repository_signed_point_control
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, positive contracted resolvent kernels and real species determinants
grading: conserved incidence charges, hard-core impurity state, bath species, bath number five, K179 path/contraction identity, contracted position, ordered momentum provenance and specieswise exterior parity
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W=(E_R-256)I-X, and K179 fixes the coefficient-complete family; no source/GU action selects the physical extension, state, domain, polarization or scalar center
target: close the complete order-five coherent Gram gate and select an exact nonliteral exterior representation without claiming a higher-order action column or physical state MAP-TYPE=intertwiner
```

## Inline preflight bookend

K182 leaves exactly 32 order-five paths. Retrieval against K179 groups them
by `(seed_impurity, output_signature)` into 12 orthogonal outputs. Every group
is coherent: six contain two paths and six contain four paths, for 64 unique
self/cross Gram entries. The records use contracted positions two or four and
retain every K179 coefficient, old position, output-letter order and surviving
momentum provenance. Thus all 32 coefficient-complete paths are present.

The incumbent question is the next complete exchange-vector gate required by
the K171/K168 action columns. The strongest independently framed physics
challenger remains the state/positivity bridge (`SC-META-53`, `LT-SM8`), but
its action-owned physical domain and state selector are absent. The K179
order-five internal gate is executable without inventing them. The source
register and v0.263 physics ledger therefore remain unchanged:
`SC-META-53` is `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain `NEEDS`.

The route census compared direct six-dimensional cubature, raw pathwise
interval subtraction, analytic contracted-resolvent divided differences,
complete canonical exterior coordinates, weighted AM--GM majorants,
localized complete-sum intervals, transformed numerical controls and K177's
Laplace-simplex determinant form. Raw per-cell subtraction was rejected after
it honestly failed to separate five high-cancellation groups. The selected
proof route integrates the contracted momentum analytically as one positive
multi-resolvent divided difference, forms every complete normalized exterior
sum, and only then applies intervals. A bounded transformed numerical control
remains separate from proof.

## 1. Complete order-five exterior family

For one K179 path `t`, let `L_t=(ell_1,...,ell_5)` be its ordered output
species, `nu_t=(j_1,...,j_5)` its surviving momentum provenance, and
`c_t in {+1,-1}` its exact W coefficient. K183 defines

```text
Psi_t(x) = c_t (2*pi)^(-7/2) / sqrt(product_s m_s!)
           * sum_(pi in product_s S_(m_s)) sgn(pi)
             int_R dp_old product_(r=1)^6 D_r(p)^(-1),
D_r = 256 + sum_(j<=r) sqrt(1+p_j^2).
```

The permutation sends canonical momentum slots specieswise into `L_t`.
Within each seed/output group `G`, `Psi_G=sum_(t in G)Psi_t` and every Gram
entry is formed in this common basis. Thus coefficients, exterior signs and
cross-path interference exist before any interval operation.

## 2. Position-two and position-four global majorants

Each cumulative denominator is allocated by weighted AM--GM among `256` and
energies already present in it. For contracted position two the six energy
exponents are

```text
(alpha_1,...,alpha_6) = (0.9,1.2,0.6,0.6,0.6,0.6).
```

For contracted position four they are

```text
(alpha_1,...,alpha_6) = (0.6,0.6,0.6,1.1,0.6,0.6).
```

The contracted exponent is strictly above one in both classes, and every
exterior squared exponent is strictly above one. Hence the exact factor

```text
int_R (1+p^2)^(-a/2) dp
  = sqrt(pi) Gamma((a-1)/2) / Gamma(a/2)
```

is finite wherever used. The complete exterior projection is bounded first;
only then does Cauchy--Schwarz enclose cross-path terms. All 64 unique Gram
entries are outwardly enclosed. The resulting broad coherent upper endpoints
range from `5.420150379341184e-10` to `1.6260451138023544e-9`. Their lower
endpoints are zero, so they are not nonzero certificates.

## 3. Analytic contracted integration and localized witnesses

For contracted position `r`, the denominators before `r` are an independent
positive prefactor. The remaining denominators have the form

```text
product_k (sqrt(1+p_r^2)+a_k)^(-1),  a_1 < ... < a_m.
```

K183 integrates this positive kernel through recursive divided differences,
starting from

```text
I_2(a,b) = 2*(F(b)-F(a))/(b-a),
F(a) = a*acosh(a)/sqrt(a^2-1),
I_m(a_1,...,a_m)
  = (I_(m-1)(a_1,...,a_(m-1))
     - I_(m-1)(a_1,...,a_(m-2),a_m))/(a_m-a_(m-1)).
```

Position two uses five positive resolvents after one independent denominator;
position four uses three after three independent denominators. Independent
full-line quadrature at asinh span 20 and order 120 agrees with both analytic
classes within `1.19e-12` relatively.

On each explicit positive canonical-momentum box, monotonic energy endpoints
now bound the analytic contracted value directly. Every exterior permutation
and every signed path is accumulated before zero separation. All 12 complete
coordinates exclude zero on their boxes, giving rigorous local norm-square
floors from `8.537574903258709e-77` to
`3.8287135182577294e-69`. These deliberately tiny witnesses prove existence;
in particular, all 12 coherent groups are nonzero. They do not sharpen the
global normalization enough for an action column.

## 4. Independent numerical reconstruction

The independent control uses positive bounded asinh coordinates through span
12, explicit momentum reflection, scrambled Sobol outer samples and full-line
Gauss--Legendre contracted integration. Outer sample counts `2^13` and `2^14`
with contracted orders 48 and 56 agree within `1.93e-2` groupwise, and every
fine value lies inside its rigorous global interval. The fine seedwise
order-five squared norms are

```text
seed 0: 7.780845531593745e-18
seed 1: 4.199052792986050e-18
seed 2: 4.204522946276353e-18.
```

The seed-one/seed-two flavor-swap controls agree within `1.31e-3`; the finite
scrambled sample is not forced to preserve the symmetry exactly. These values
check normalization, signs and interference. They are not the outward proof.

## 5. Exact compression identity and scale switch

Introduce one positive Laplace parameter `s_r` for each denominator and set

```text
T_j = sum_(r>=j) s_r.
```

The contracted momentum then contributes the separate positive factor
`2*K_1(T_old)`. For a fixed species `s`, the complete Leibniz permutation sum
is exactly

```text
det[ exp(-T_(j_a) E(p_b)) ]_(a,b=1,...,m_s).
```

Therefore every path exterior coordinate is the product of its species
determinants, and a memoized cumulative-time DAG shares the `T_j` nodes across
paths. This is an algebraic identity, not an approximation. The solver checks
all 32 order-five paths at three distinct controls—96 literal/determinant
comparisons—with maximum absolute discrepancy `2.62e-21` in floating
evaluation.

The selected compressed object is the product of Laplace-simplex species determinants.

A dense determinant cubic-operation proxy is initially costlier than the
small literal sums, so the selected evaluator is hybrid: keep canonical
literal exterior sums through order seven and switch to Laplace-simplex
species determinants with the shared time DAG at order eight. At order twelve,
the literal representation needs `19,609,920` exterior summands per time
point; the dense determinant proxy is `186,624`, a factor of `105.08` smaller,
with `48,384` matrix entries. This closes the representation decision but not
the time integral. The next numerical gate is a certified positive-orthant
time quadrature or low-rank error bound for the complete order-six family.

No order-six-through-twelve numerical prefix, complete K171/K168 action
columns, `R_ref` residual, complement or flux floor, scalar-center floor or
K152 interval is claimed.

## Inline postflight bookend

The strongest overclaim would be to call the bounded Sobol control a rigorous
tail certificate or to treat the selected determinant representation as a
completed time integrator. The strongest mistyping would be to use a product
of raw determinant-entry intervals instead of the complete species
determinants. The exact remaining numerical problem is the high-dimensional
positive-time integration error, not exterior permutation growth.

The exact K179 pin, all 32 path identities, 12 groups, 64 Gram entries, both
integrable contracted-position classes, 12 localized witnesses, independent
control, determinant identity and through-order-twelve operation census are
machine checked. This is a repository-internal conditional construction, not
a source result or physical state. `SC-META-53` remains `UNCERTAIN`;
`LT-SM8`, `RA-F1` and `AC-F1` remain unchanged at `NEEDS`. Repository-wide
priority and unrelated channel order are unchanged.

## Reproduction

```bash
python3 tests/channel-swings/k183_order_five_coherent_gram.py --quick
python3 tests/channel-swings/k183_order_five_coherent_gram.py --demo
python3 tests/channel-swings/k183_order_five_coherent_gram_probe.py
python3 tests/channel-swings/k183_order_five_coherent_gram_probe.py --selftest
```
