---
title: "Selected K77 nonlocal ultrahyperbolic polarization gate"
artifact_type: exploration_result
created: 2026-08-11
status: CONDITIONAL_STRICT_CENTER_PRINCIPAL_DOMAIN_INGREDIENT_BUILT__PHYSICAL_CLOSED_DOMAIN_OPEN
grade: "exact rational flat real-Cl(7,7) principal-symbol theorem plus primary scalar ultrahyperbolic donor theorem; no selected-action, curved overlap, Green/BFV, nonlinear or index result"
canon_verdict_change: none
---

# Selected K77 nonlocal ultrahyperbolic polarization gate

## Result first

The Craig--Weinstein remedy is necessary but not sufficient for the actual GU
operator.

Their scalar theorem supplies a nonlocal restriction on tangential Fourier
data. For two-sided global evolution, data are supported in the strict center
cone

\[
\rho(k)^2=|\xi_{\rm space}|^2-|\eta_{\rm extra\ time}|^2>0.
\]

On the actual dimension-1920 real-K77 source-shaped matrix operator, that
support restriction still leaves the exact rank-128 square-zero Jordan
remainder. A scalar cone cannot see or remove a matrix polarization defect.

The operator itself nevertheless supplies a minimal additional condition:

\[
N(k)\widehat u(k)=0,
\qquad
N(k)=E(k)^2-\rho(k)^2 I.
\]

On the strict center cone, `ker N(k)` is invariant under evolution and the
restricted evolution obeys `E(k)^2=rho(k)^2 I`. Its minimal polynomial has
distinct roots, so the generalized chains disappear. The condition has
pointwise rank 128, leaving a 1792-dimensional kernel, and the complete rank-640
observed four-vector-plus-`nu` carrier remains reachable.

This is a **conditional flat principal-domain ingredient**, not a physical
closed domain.

## What was computed

The exact source-shaped normal symbol was inverted directly. Five tangential
frequency types were then tested:

- one ordinary observed spatial direction;
- two mixed extra-time/spatial points in the strict center cone;
- one null-boundary point;
- one extra-time-dominant point.

For all three center representatives, `N(k)` has rank 128, is nonzero and
square-zero, and commutes with `E(k)`. It scales quadratically with frequency.
The null and extra-time-dominant points are excluded from the two-sided center
domain before the matrix polarization is imposed.

The prior v0.168/B2B work already owned the Jordan remainder and its
direction-dependent factorization. The new increment is the typed composition
with the primary ultrahyperbolic theorem, the mixed extra-time test, the
minimal pointwise polarization, and the exact observation-rank check.

## Layer 0

- Walter Craig and Steven Weinstein prove a theorem for a scalar
  ultrahyperbolic equation. Eric Weinstein is the GU source author. These are
  different claims by different people.
- Fourier support in the center cone is not matrix polarization.
- `ker N(k)` is a restriction on physical data, not a gauge orbit or BV image.
- `N(k)` is polynomial in the flat symbol; the **combined center-support
  projector** is nonlocal. Neither is yet a curved pseudodifferential bundle
  construction.
- Principal propagation is not selected-action, Green, BFV or nonlinear
  propagation.

## What remains

The next gate is no longer "find any possible constraint." It is to decide
whether this specific strict-center-plus-`ker N` domain ingredient is compatible
with:

1. the action-owned Green form (isotropy/coisotropy and boundary flux);
2. patch overlap and a curved pseudodifferential completion;
3. the selected action and BFV boundary reduction;
4. nonlinear constraint propagation.

Failure at any one of those gates kills this candidate without killing every
nonlocal ultrahyperbolic domain. The source-admitted wedge-Shiab/nonzero-
southeast operator completion remains a separate route because it changes the
operator instead of restricting its data.

P1/P2/P3 remain unused. No ledger verdict, residue, quotient, canon verdict or
public posture moves.

## Validation

`tests/channel-swings/selected_k77_nonlocal_ultrahyperbolic_polarization_gate_probe.py`
passes 47 exact, analytic, source, Layer-0, microlocal, observation,
symplectic and planted checks after replaying the complete v0.169 predecessor.
