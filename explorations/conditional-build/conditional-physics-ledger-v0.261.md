---
title: "Conditional Physics Ledger v0.261"
status: research
doc_type: ledger-summary
created: 2026-08-23
predecessor: lab/process/conditional-physics-ledger-v0.260.json
ledger: lab/process/conditional-physics-ledger-v0.261.json
canon_verdict_change: none
---

# Conditional Physics Ledger v0.261

Ledger v0.261 preserves every v0.260 scientific verdict, appends three
target-centered gravitational requirements, and links two accepted external
benchmarks to existing rows without minting duplicates.

> **GU-COMPARATOR-ROUTING — scope before inference.** These are observed-side
> external targets. They do not select a source-native mechanism and do not
> adjudicate the source's non-chiral total-theory claim, generation mechanism,
> dark-matter position, or cosmological prediction packages.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: EXTERNAL_BENCHMARK_TARGETS`

```gu-typed-objects
result: three external gravitational targets appended and two external benchmark anchors linked to existing realization rows
carrier: append-only conditional-physics ledger and observed-side comparator records LAYER=UNTYPED CHIRALITY=N/A
pairing: each external benchmark paired only with its explicitly linked GU realization obligations ON=ledger-rows
real_structure: N/A
grading: scoped benchmark integration; native realizations retain their prior grades
action_owner: repository-construction -- no source action or coefficient is introduced
target: ledger target/register consistency MAP-TYPE=evaluation
```

## Appended targets

| row | accepted external target | existing evidence ceiling | GU realization |
| --- | --- | --- | --- |
| `LT-GR9` | strong-field Schwarzschild/Kerr phenomenology | W225 clears the imported-Schwarzschild cheap half; `E_s^theta` and `alpha_W` remain open; W229 is branch-local | `NEEDS / MISSING_CONSTRUCTION` |
| `LT-GR10` | recorded PPN solar-system signature | W220 is conditional on the granted shiab-Einstein reduction | `NEEDS / MISSING_CONSTRUCTION` |
| `LT-GR11` | rotation-curve functional-form consistency | H49 kills pure conformal behavior and retains induced `|II|^2` with its Einstein term | `NEEDS / MISSING_CONSTRUCTION` |

All three rows declare `mechanism_commitment: NONE`,
`confirmation_credit: NONE`, observed-side `L2/C2` context, and an untyped
grant because the current Grant poset has no node for the complete benchmark
assumption sets. A future packet failure is scoped to its named realization
bridge; the rows are targets, not kills.

## Linked anchors

`EXT-SM-STRUCTURE` links `LT-SM1..LT-SM8`. It demands accommodation of the
observed low-energy gauge structure without turning an ordinary family-index
or net-chirality control into an adjudication of the source's total-theory
claim. `EXT-COSMO-BACKGROUND` links `LT-GR1` and the `LT-GR2` successor family;
all dark-energy confrontation remains confined to frozen PP1/PP3 through
P-OBS.

The two context-only notes preserve the consolidated owner ledger:

- `LT-SM8` records OWNER-F as jointly demanded with LT-GR8 D3 and types its
  grants as `G5 + G7`; its exact physical carrier remains unbuilt.
- superseded aggregate `LT-GR2` records OWNER-C as demanded independently by
  PPN, rotation, LT-GR8, and cosmology. Independent demand does not imply one
  shared number or joint discharge.

## Ceiling

No predecessor verdict, reason kind, scientific meaning, mechanism,
prediction, confirmation, canon status, or public posture changes. Direct
owner packets at `093c5ff6` and `2ce482d3` completed the kill-target typing
ratchet before closeout; v0.261 preserves that non-verdict typing exactly.
Their separate verdict-retyping proposal remains a later pending delta. The
`0e3650de` velocity-council packet is disjoint.

```text
Ledger v0.261 — 88/88 active targets mapped
33 SAME · 19 DIFFERS · 31 NEEDS · 5 OVER-DETERMINED
three targets appended · two anchors linked without duplicate rows
zero predecessor verdict changes · zero mechanism imports · zero confirmation launders
```
