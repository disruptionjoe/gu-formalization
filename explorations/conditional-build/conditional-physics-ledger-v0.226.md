---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-12
ledger: lab/process/conditional-physics-ledger-v0.226.json
status: CURRENT_APPEND_ONLY_LEDGER_V0_226
---

# Conditional physics ledger v0.226

```text
Ledger v0.226 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Tightness: T4x1 T3x3 T2x1 · scoped quotients: 5
Headline delta: none
Frontier: 1 named condition closed · 0 opened · 1 named condition remains
```

## What changed

The corrected first-action Euler covector has now been represented through the
exact action Riesz map and squared with the conditional observer `Q_u` on the
same `196`-real fixed-background connection bank.

The action Riesz matrix is diagonal and nondegenerate with inertia `(98,98)`.
On the constant trace-`H_q` locus the Fréchet-adjoint companion is exactly
`2 S_q`, so the corrected square reduces to

```text
80 (rho+r^2)^2 + kappa^2 r^2.
```

It therefore has the same radial branch and the same complete fixed-background
Euler covector as the literal printed-endpoint square.  The twelve transverse
cells and determinant-`80` incompatibility survive.

## What did not collapse

The endpoint and corrected Fréchet maps are different.  Their agreement occurs
only after restriction to this constant locus and pairing through `Q_u`.
Accordingly the full-domain endpoint/action-Euler distinction, source ownership
fence, moving-background terms, and physical tangent/BV problem remain open.

## Append-only migration

`RA-E1`, `RA-E3`, and `LT-SM6` move in distance/evidence only.  Verdicts,
reason kinds, residue, forks, five quotients, P1/P2/P3, canon and public posture
do not move.

## Next gate

Construct the smallest source/action-owned tangent or BV differential and test
whether it legitimately removes or balances the twelve transverse cells.  In
parallel, build the actual moving reference/metric/section/Shiab derivatives
excluded by the fixed-background bank.  Do not fit a restriction to the desired
branch or identify the corrected square with literal `SC-ACT-04` globally.
