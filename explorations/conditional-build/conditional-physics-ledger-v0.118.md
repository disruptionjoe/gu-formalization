# Conditional physics ledger v0.118

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.118.json`.

## Progress

`Ledger v0.118 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Six rows move in distance/evidence only. No verdict, residue, quotient, datum,
canon or posture changes.

## What v0.118 adds

The cheapest possible branch-Hessian selector has been tested before building
the complete operator. The displayed two-coordinate reconstruction matrices
really do have different inertias, but neither branch is critical in the
independent `B` direction and `B` at fixed `T` is not a source tangent. An
exact local coordinate change can force either displayed determinant to zero,
so the apparent Morse distinction is not invariant.

On the scalar `varpi` line the source actually owns, both first-action
coefficients are negative. For the distinct residual-square action, both
coefficients are positive rank one, up to parent pairing orientation. This
does not make the coefficients equal and does not license transferring a full
operator by Galois conjugacy. It says only that neither scalar restriction
selects a branch. Both branches and all three parent scopes remain live.

## Frontier

Conditions closed: `1`; conditions opened: `0`; remaining named: `6`.

Next port the actual first-action epsilon/Cl1 cross and residual-square
metric/varpi/epsilon Jacobian separately to both branches and each retained
parent. Then complete the Hessian, add gauge fixing/ghosts and test the
`Dmax/Dmin` exact sequence.

P1/P2/P3 remain unused. The selected Spin parent, two `U(32,32)` halves and
full `U(64,64)` comparator remain distinct.
