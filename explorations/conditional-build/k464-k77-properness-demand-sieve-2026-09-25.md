---
title: "K464 K77 properness demand sieve"
status: working_draft_verified
doc_type: conditional_build
created: "2026-09-25"
result_id: K464-K77-PROPERNESS-DEMAND-SIEVE
direction: observed_to_native
classification: BRIDGE_OR_SEMANTIC_BOUNDARY
---

# K464 K77 properness demand sieve

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains
> or borders a conventional particle-physics comparator. A result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar
> vector-mass route binds only that named model. It is not evidence for or
> against Weinstein's source-native mechanism without an explicit typed
> bridge. Read `lab/methods/source-native-comparator-routing.md` and its
> source-native pointers before reuse.

## Result

K464 composes the five backward quantum-demand rows `QD-R1-1` through
`QD-R1-5` with the exact structural boundary left by K444--K446. A future K77
degree-changing action packet is admitted only when every schema field is
present and the packet carries:

1. complete demand lineage for coherent/composite states, normalized event
   pairing, causal-domain instruments, phase/decoherence dynamics and gauge
   descent;
2. coefficient-complete action-owned arrows `D2:C2->C1` and `D1:C1->C0`;
3. both typed squares `Pi1 D2 = D2 Pi2` and `Pi0 D1 = D1 Pi1`;
4. total nilpotence `D1 D2=0`;
5. preservation of the closed corrected trace domain; and
6. exact ranks `10752/35840` or an adapted contraction that excludes the
   K446 contrary cohomology.

The three outputs are deliberately narrow:

- `rejected` for a missing field, broken structural obligation, partial
  action custody/coefficient packet, or a K446-type properness failure;
- `structurally_admissible_but_unselected` for a complete structural control
  such as K445 that has no action owner or coefficients; and
- `action_complete_ready_for_properness` only for a fully populated action
  packet shape. The repository currently has zero actual packets in this
  third class.

## Why the rank or contraction gate is load-bearing

K444 proves boundary descent, not properness. K445 gives a nontrivial
chain-conjugate family with ranks `10752/35840`, an adapted contraction and
zero finite cohomology. K446 gives two equally boundary-compatible and
nilpotent controls with cohomology `(0,70,70)` and `(21,21,0)`. Therefore the
typed squares and nilpotence cannot choose the action and cannot identify
physical states. A properness witness must come from the submitted action
packet itself.

## Exact control dispositions

| packet | classification | reason |
|---|---|---|
| K445 chain-conjugate control | `structurally_admissible_but_unselected` | exact ranks and contraction, but no action custody or coefficients |
| K446 low-arrow defect | `rejected` | rank `35770` and cohomology `(0,70,70)` |
| K446 high-arrow defect | `rejected` | rank `10731` and cohomology `(21,21,0)` |
| current native action gap | `rejected` | coefficient references are absent |
| abstract action-complete shape | `action_complete_ready_for_properness` | schema fixture only; not an actual K77 action |

The abstract shape proves only that the classifier's final state is reachable.
Its references are fixtures and it contains no coefficient matrix.

## Bookends

The strongest alternative is to test a coefficient-complete owner-native K77
action directly. None is serialized. Fitting coefficients to K445 would hide
the missing owner, while treating K446 as physical cohomology would convert a
logical counterexample into a false state claim. The fail-closed answer is to
reject the current gap and preserve the exact admission interface.

The weakest reproducibility seam is also explicit: finite ranks and an
adapted contraction decide only this homogeneous-orbit corrected-boundary
complex. They do not prove nonlinear/global BV/KT properness, a global
Calderon domain, or physical cohomology.

## Verification and claim ceiling

The producer serializes the three-way classifier and exact controls. The
independent probe checks 27 invariants and rejects 27 hostile mutations; the
producer self-test checks two positive classes and rejects 23 incomplete or
structurally defective packets.

This is an exact demand-lineaged certificate interface only. It supplies no
actual action coefficients, selects no K77 action, proves no physical
cohomology, and changes no source claim, conditional-physics ledger row,
canon, paper, public posture, prediction, confirmation or physical GU verdict.
