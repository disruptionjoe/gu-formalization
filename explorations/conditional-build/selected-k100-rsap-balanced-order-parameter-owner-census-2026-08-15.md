---
title: "Selected-K100 balanced RSAP order-parameter owner census"
status: active_research
doc_type: exact_equivariant_order_parameter_construction_and_owner_obstruction
created: "2026-08-15"
registry: lab/process/selected-k100-rsap-balanced-order-parameter-owner-census.json
probe: tests/channel-swings/selected_k100_rsap_balanced_order_parameter_owner_census_probe.py
grade: "EPSILON DRESSES A BALANCED SEED WITH EXACT 49D ORBIT; SOURCE METRIC AND GLOBAL CHARGE DATA CANNOT SUPPLY THE SEED"
target_claim: K99_NEXT_GATE__CURRENT_ACTION_CONSTRUCTS_BALANCED_INVOLUTION_OR_ORDER_PARAMETER
target_verdict: CURRENT_ACTION_NO__MINIMAL_CONDITIONAL_BALANCED_ORBIT_DATUM_EXACT
canon_verdict_change: none
---

# Selected-K100 balanced RSAP order-parameter owner census

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: SOURCE_NATIVE_ROUTE`

Scope: algebraic zero-order and equivariant constructions from the currently
owned K77 metric/observation data, epsilon, its cotangent charge, and the
typed connection/spinor structures already serialized by the repository. The
result does not exclude nonhomogeneous curvature data, an unreleased action
term or a newly supplied reduction field.

## Result first

The existing epsilon field can carry the balanced order parameter with no new
continuous local field, but it cannot choose which order parameter to carry.
Given one fixed `Q`-self-adjoint involution `R_0` with eigenspace signatures

```text
(3,4) | (4,3),
```

define

```text
R_epsilon = rho(epsilon) R_0 rho(epsilon)^(-1).
```

Then `R_epsilon^2=1`, its two eigenspaces retain those signatures, and

```text
delta_eta R_epsilon = [eta,R_epsilon].
```

The derivative has rank `49`; its kernel is exactly the `42D` stabilizer
`h_bal`. Thus the moving order parameter is precisely the homogeneous orbit

```text
G/H_bal,
```

and epsilon already supplies its 49 local orbit coordinates once right
`H_bal` is declared gauge. This is the exact conditional owner shape required
by the reverse scaffold.

But `R_0` is not derived. Epsilon only transports the value chosen at
`epsilon=e`; every equivariant map from the free left `G`-torsor is determined
by that seed. The current source/action record supplies no balanced seed and
no right-`H_bal` gauge declaration.

## Why the source-native metric data cannot choose the seed

The physical metric-bundle geometry carries the Lorentz representation

```text
TY|_X = TX + Sym^2(T*X)
       = 4 + 1_trace + 9_traceless.
```

The exact commutant of the six `so(1,3)` generators on this 14D carrier has
dimension three. It is generated by the projectors onto the `4`, `1` and `9`
blocks. Therefore every Lorentz-equivariant involution built naturally from
the homogeneous metric/observation data is a choice of signs on those three
blocks. Its eigenspace dimensions are subset sums of `{4,1,9}`:

```text
0, 1, 4, 5, 9, 10, 13, 14.
```

Seven never occurs. This extends the earlier exact trace/traceless commutant
result from the 10D fibre to the full physical 14D carrier and directly
answers the balanced-owner question. Producing a seven-plane from the source
physical split requires extra symmetry-breaking data; it cannot be done by a
Lorentz-natural zero-order concomitant on the current homogeneous background.

The physical `(1,3)|(6,4)` involution also has eigenvalue multiplicities
`4|10` and trace `-6`. Conjugation by epsilon preserves both, so dressing that
involution never produces the required balanced `7|7`, trace-zero object.

## Why the charge cannot choose it globally

One might instead define `R` equivariantly from the cotangent charge
`lambda in so(7,7)*`. That route fails globally at the zero charge which the
surjective 98D RSAP must include.

If `F(lambda)` is adjoint-equivariant, then `F(0)` is fixed by all of
`SO_0(7,7)`. The exact commutant calculation on the 14D vector representation
has dimension one: only scalar endomorphisms commute with all 91 generators.
A scalar involution is `+I` or `-I`, with trace `+14` or `-14`, never a
balanced trace-zero involution. Hence:

```text
no globally defined adjoint-equivariant charge-only map
so(7,7)* -> balanced-involution orbit.
```

Spectral constructions away from zero may remain local, stratum-dependent
comparators. They cannot globally own the all-charge RSAP order parameter.

## The real-orbit datum is invisible to trace-polynomial selection

There are eight `O(7,7)` orbit types of `Q`-self-adjoint involutions with
seven `+1` and seven `-1` eigenvalues, indexed by the positive signature
`a=0,...,7` of the `+1` seven-plane:

```text
(a,7-a) | (7-a,a).
```

All eight have the same characteristic polynomial
`(t-1)^7(t+1)^7` and the same traces of every power. The balanced RSAP type is
the unordered pair `a=3/4`. Consequently an invariant polynomial built only
from traces of the self-adjoint involution cannot distinguish the balanced
real orbit from the other seven real-signature allocations. Selecting its
inertia is a discrete real-domain/orbit choice, not a consequence of the
common algebraic spectrum.

Within the selected orbit a choice of `R_0` is homogeneous placement data and
epsilon transports it. Modulo the intended `G` covariance, the minimal new
input is therefore the **balanced real-orbit type** (plus any finite connected-
component/orientation refinement), not a new 49-component continuous bulk
field. Until right `H_bal` is actually declared gauge, however, one may not
erase the placement as gauge.

## Current-owner census

| candidate | disposition |
|---|---|
| ambient metric `Q`, orientations and scalar coefficients | Full-`G` commutant is scalar; no balanced involution. |
| physical `4+10` metric/observation split | Lorentz commutant is exactly the `4+1+9` block algebra; no invariant seven-plane. |
| epsilon dressing of the physical split | Conjugation preserves `4|10` multiplicities and trace `-6`; wrong orbit. |
| charge-only spectral construction | No global equivariant value at `lambda=0`; local regular-stratum constructions do not globalize. |
| epsilon with a balanced `R_0` | Exact 49D `G/H_bal` construction; seed and right-gauge law are external to the current action. |
| spinor `omega`, `J4` or Weyl-half projector | Different carrier and grading. No vector-14 intertwiner to the balanced involution is owned. |
| connection/curvature holonomy | A parallel `R` is equivalent to an `H_bal` holonomy reduction. This is a valid future owner criterion, but the current action selects no such reduction. |
| new independent balanced `R` field | Mathematically available 49D coset/order-parameter completion; new field/action and real-orbit domain. |

This census is exhaustive for the current primitive zero-order routes. The
earlier nonhomogeneous curvature successor remains open because curvature can
break the homogeneous Lorentz stabilizer. It must produce the balanced orbit
without a fitted spectral cut and pass action stationarity before changing
this verdict.

## Next gate

Variancer's reverse conditional can now proceed with its exact irreducible
assumption stated honestly:

```text
CONDITIONAL DATUM: one balanced real-orbit seed R_0,
not 49 new continuous coordinates.
```

Using that datum, form `R_epsilon` and the dressed source connection

```text
B(epsilon)=epsilon^(-1) B epsilon + epsilon^(-1)d epsilon.
```

Project it by `R_0` into `h_bal+p_bal`, then perform the actual boundary
Hamiltonian/collar decomposition. Geometry alone is no longer the gate; the
variational role is.

> **Successor correction (K101).** A zero coordinate self-Hessian in the
> covariant action is not a necessary multiplier test. A normal connection
> component can have nonzero coordinate Hessian yet no normal velocity and
> become linear after the Legendre transform. The corrected gate is an exact
> normal-velocity Legendre kernel, followed by the canonical Gauss coupling
> and its preboundary identification with `J_R,H_bal`. K101 proves the
> conditional `42+49` projection but finds the source-owned collar, Legendre
> map and endpoint bridge type-missing.

No ledger, datum booking, quotient booking, canon claim, public posture,
W/mirror choice, chirality or generation count changes.

Reproduce with:

```bash
python3 tests/channel-swings/selected_k100_rsap_balanced_order_parameter_owner_census_probe.py
```
