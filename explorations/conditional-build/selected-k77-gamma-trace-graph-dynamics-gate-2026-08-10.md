---
artifact_type: construction_result
created: 2026-08-10
run_id: RUN-20260810-211634-gu-k77-graph-dynamics-gate
status: exact_conditional_result
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Selected K77 gamma-trace graph dynamics gate

## Result

The exact 128-dimensional `Omega0`--gamma-trace graph from v0.157 is
dynamically active but is **not a closed full-Euler sector** for either
canonical Pin placement.

After treating the barred and unbarred fields as independent, composing the
local degree-one/degree-zero Krein pairing, and constructing the complete
left and right stationary kernels, both candidates give the same exact
fingerprint:

```text
                               timelike   spacelike   null
induced Green/kinetic rank        128        128       128
raw symbol-on-graph rank          128        128       128
full-Euler receiver leak rank     128        128       128
```

Thus the restricted action sees a nondegenerate first-order kinetic matrix,
including at the tested observed null covector, but the full field equation
produces an equally large component outside the equations tested by graph
variations. A reduced action exists at this finite principal grade; it is not
a consistent truncation of the full displayed-southeast-zero operator.

The action-derived connection current is also live. Each of the fourteen
natural diagonal soldering directions `delta V_i=gamma_i` gives a rank-128
bilinear current matrix for both candidates. Their radial sum is exactly zero:

```text
sum_i J_i = L (sum_i d_i D) G = L D G = 0.
```

This is cancellation along the one-dimensional tautological scale direction,
not vanishing of the transverse current. Consequently, the zero-fermion
bosonic stationary branches do not automatically extend to generic nonzero
graph amplitudes at fixed bosonic fields. Special isotropic barred/unbarred
amplitudes are not excluded by a matrix-rank statement.

The preregistered `GRAPH_ACTION_LIVE` and `FERMION_CURRENT_LIVE` horns fire.
`FINITE_BV_ADMITTED` does not: full-Euler receiver closure fails at maximal
rank in all three tested observed causal strata. The result therefore stops
before manufacturing a BV differential on a non-closed carrier.

## Layer 0

Seven objects remain distinct:

1. the right zero-order algebraic kernel `G`;
2. the independent barred left kernel `L`;
3. the Krein-dual barred vector `K^{-T}L^T`;
4. the restricted action symbol `L sigma(D) G`;
5. closure of the full Euler residual in the graph equation receiver;
6. BV cohomology;
7. a closed-domain physical mode or Fredholm index.

The source writes four independent barred/unbarred fields. No reality
condition is inserted here. The local nondegenerate pairing

```text
K = diag(eta_1 B, ..., eta_14 B, B)
```

is used only to turn the barred carrier into the action covector. It is not a
positive Hilbert Riesz map or a source-selected global reality/domain.

The full-Euler receiver is `im(L^T)`. Receiver closure asks whether
`sigma(D)(xi)G` lies in that image. For each tested covector, the joined image
has rank `256`, so the leak is exactly `256-128=128`.

## Exactness

All ranks are computed over the good prime `1000033`. The predecessor proves
the zero-order operator has exact characteristic-zero rank/nullity
`1792/128`. The left and right kernels therefore both have characteristic-zero
dimension 128. Each induced 128-square Green/current matrix has nonzero
determinant modulo the good prime, which proves its characteristic-zero
determinant is nonzero. The joined receiver-plus-residual rank is 256 modulo
the prime and cannot exceed 256, so the characteristic-zero receiver leak is
also exactly 128. Radial cancellation is the field-independent identity
`L D G=0`, not a modular coincidence.

The observed causal representatives are plus-first K77 covectors

```text
timelike:  e0
spacelike: e7
null:      e0+e7.
```

These are the selected `(1,3)` observation-plane strata. This does not prove a
global characteristic theorem for every covector after the Pin reduction.

## Interpretation

The good news is that the v0.157 graph is not an inert algebraic accident: the
action supplies it a full-rank kinetic pairing and nonzero connection current.
The bad news is more decisive for the current route: the graph alone omits
half of the principal response. It cannot be promoted directly to the
physical carrier or to a BV complex.

This points to a sharply bounded successor rather than another free carrier
search. Construct or kill an **action/source-owned equation receiver or
constraint completion** that contains the missing rank-128 response while
preserving the graph's 128 physical coordinates. Compare exactly three
options:

1. the source-admitted southeast-nonzero map;
2. the smallest action-owned extension generated by the leaked image;
3. a genuine gauge/BV differential whose image is the leak.

Reject any option that merely projects away the full Euler equations, adds an
unowned rank-128 selector, destroys the induced Green form, or worsens datum
surplus without independent constraints. Only a passing completion advances
to BV/Green domains and then observed representation content.

`W`, mirror, full `U(64,64)`, moving Spin and Curt's two `U(32,32)` halves
remain separate comparators. The source-admitted southeast-nonzero map remains
unconstructed. No physical-mode, chirality, particle, index, generation,
datum, residue, quotient, canon or public-posture inference is made.

## Evidence

- `tests/channel-swings/selected_k77_gamma_trace_graph_dynamics_probe.py`;
- `lab/process/selected-k77-gamma-trace-graph-dynamics-gate.json`;
- `lab/process/hostile-reviews/2026-08-10-selected-k77-gamma-trace-graph-dynamics-review.md`;
- `lab/sources/selected-k77-gamma-trace-graph-dynamics-source-return-2026-08-10.md`.
