---
title: "K77 global quotient and domain descent wave"
status: active_research
doc_type: reverse_scaffold_globalization_and_physical_quotient_boundary_result
date: 2026-09-01
claim_ceiling: exact chart-transition descent, coorientation and representative-independence conditions, plus an I1B holonomy obstruction; no actual GU physical quotient, state space, action, prediction, confirmation, or verdict
manifest: lab/process/k77-global-quotient-domain-descent-wave.json
probe: tests/channel-swings/k77_global_quotient_domain_descent_probe.py
---

# K77 global quotient and domain descent wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This is a conditional
> bundle-descent, boundary-coorientation, constraint-quotient and symplectic-
> holonomy result. Ordinary Hilbert-space quotient, BRST/BV, symmetric-
> hyperbolic or quantum-instrument language does not adjudicate GU without the
> explicit typed bridges stated below. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.

Classification: `SOURCE_NATIVE_ROUTE`

```gu-typed-objects
result: observed-projector globalizability criteria and independent I1B holonomy obstruction
carrier: observed real rank-1920 incoming boundary bundle and I1B fixed-stratum symplectic quotients of dimensions 24 24 22 LAYER=conditional CHIRALITY=N/A
pairing: transported observed positive principal energy versus I1B alternating Green form ON=packet_local_carriers
real_structure: real doubled-Majorana observed boundary data and real I1B distortion/metric fluctuations
grading: observed fermionic Clifford grading remains distinct from I1B mixed differential order
action_owner: conditional observed principal coefficient pair; no constraint/BV differential or global domain owner; I1B Hessian owns only the Green class
target: K77-PSX-1 through K77-PSX-4 and K77-PSX-7 release conditions without cross-packet composition MAP-TYPE=classification
```

## Result

The observed packet's chart-local scalar unit has no ordinary chart-holonomy
problem. If the associated carrier changes frame by `U`, then

```text
Pi_i  = U Pi_j U^-1,
H_i   = U^-T H_j U^-1,
rho_i = U rho_j U^T,
```

and cyclicity gives `Tr(H_i rho_i)=Tr(H_j rho_j)`. The projector, positive
quadratic density and scalar energy therefore glue across
**coorientation-preserving** chart transitions.

Two separate obstructions remain. First, normal reversal exchanges
`Pi_in` with `Pi_out=I-Pi_in`. The incoming half is not invariant under the
deck transformation of an uncooriented boundary. It globalizes on the
coorientation double cover, or after a global outward coorientation is
supplied, but the action does not select such a boundary or domain.

Second, the raw chart energy does not descend unchanged through a nontrivial
gauge quotient. For an action-owned complex

```text
G --d0--> ker(d1) subset V_in,
```

the incoming projector acts on cohomology only when it preserves cycles and
`im(d0)`. A positive `H` then gives quotient classes unique `H`-orthogonal
representatives and the quotient norm

```text
q_Q([v]) = min_g H(v+d0 g, v+d0 g).
```

That norm is representative-independent, but it generally differs from the
raw normalization. In the exact control, `v=(3/5,2/5)` has raw energy `1`,
while quotienting the first coordinate gives `q_Q([v])=16/25`; the class must
be renormalized after reduction. The prior chart-local deterministic unit and
effects therefore cannot be copied unchanged onto physical classes.

Independently, I1B now has a sharp global majorant obstruction. The
symplectic loop `A=diag(2,1/2)` preserves the standard Green form but fixes no
positive symmetric majorant. For `G=[[a,b],[b,c]]`, invariance forces
`a=c=0`, leaving determinant `-b^2`. A global compatible majorant therefore
requires a compact/unitary reduction, or equivalent owner data, rather than
the Green quotient alone. Rotational holonomy is the firing positive control.

The exact probe passes its focused controls and hostile selftest. No actual
GU constraint/BV complex, physical quotient or global causal domain is
constructed.

## 1. Chart descent is not domain selection

The associated-bundle formulas already present in the observed packet are
strong enough for exact overlap descent. They make the scalar unit immune to
ordinary changes of spin frame because the energy and quadratic state
transform contragrediently. This closes the chart-representative seam.

It does not choose a global spacetime region, a boundary hypersurface, an
outward normal, a time orientation, coefficient regularity, bounded geometry,
constraint propagation or a closed operator domain. Those are distinct
global analytic objects rather than failures of coordinate covariance.

## 2. Normal reversal is a coorientation obstruction

For the action-derived involution `E_n=D_t^-1 D_n`,

```text
Pi_in(n)  = (I-E_n)/2,
Pi_in(-n) = (I+E_n)/2 = Pi_out(n).
```

On the coorientation double cover one may select one sheet and obtain a
global incoming family. Descending it to an uncooriented base would require
the incoming projector to equal its complement. That is impossible for the
nonzero half-rank split. This is an exact obstruction to an **unoriented
incoming label**, not a no-go for every global boundary problem.

## 3. Physical quotient changes the order unit

Boundary projection and gauge reduction commute only after the action-owned
complex proves

```text
Pi(ker d1) subset ker d1,
Pi(im d0)  subset im d0.
```

Even then, positive raw energy is not constant on representatives `v+d0 g`.
The canonical finite-dimensional repair is the `H`-orthogonal representative
or, equivalently, the quotient norm. In functional form the same statement
requires a closed gauge image and compatible closed domains; neither may be
silently inferred from the finite control.

The exact rational witness uses `H=diag(1,4)` and
`im(d0)=span(e_1)`. The raw unit of `(3/5,2/5)` is `1`; its orthogonal quotient
representative `(0,2/5)` has norm `16/25`. The physical class can be
renormalized by `(0,1/2)`, but that is a new quotient normalization after the
gauge owner is supplied. It is not the old chart-local unit descending by
fiat.

## 4. I1B needs a polarization reduction

I1B's fixed-stratum Green quotient has structure group contained in a
symplectic group. A compatible positive majorant is equivalent to reducing
that structure to a unitary subgroup, or supplying a compatible complex
structure/polarization with the same effect.

The hyperbolic symplectic mapping-torus control proves that such a reduction
does not follow from the Green form. The loop preserves `omega` yet cannot be
orthogonal for any positive form because it has eigenvalue `2`. A compact
rotation preserves both `omega` and the Euclidean majorant, so the detector
distinguishes possible from obstructed holonomy. Separately, the `24/24/22`
null-stratum jump still prevents one constant-rank quotient bundle across a
connected crossing.

## 5. Hostile close and next frontier

The strongest overclaim is that the observed chart-local state interface is
now global. It is not: only its transition law is globalizable, while domain,
coorientation, constraint propagation and physical quotient ownership remain
open. The strongest contrary route is a named globally cooriented
noncharacteristic domain with an action-derived closed BV complex; it would
release the quotient-norm construction. The weakest propagation seam is the
functional closed-range step: finite-dimensional orthogonal representatives
do not prove Sobolev closedness or nonlinear propagation.

The incumbent continuation is therefore to construct one named cooriented
causal domain and an action-owned complex satisfying the frozen projector and
quotient-energy conditions. The independent I1B alternative is to derive a
unitary holonomy reduction or prove that its actual holonomy contains a
hyperbolic element. Constraint propagation and closed-range completion should
precede normalized instruments or composites.

The observed and I1B packets remain noncomposable. No global GU-native
physical quotient, state space, Born rule, composite system, instrument
algebra, source action, prediction, confirmation, held-out success, canon
change, paper status, public-posture change or GU verdict is created.
Delayed-choice entanglement swapping remains reserved and unscored.
