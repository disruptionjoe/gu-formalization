---
artifact_type: exact_action_restriction_and_owner_split
created: 2026-08-12
grade: EXACT_SCOPED_MOVING_FAMILY_ACTION_RESTRICTION__CONDITIONAL_EDDY_SQUARE_MEXICAN_HAT__ACTION_OWNER_AND_BACKGROUND_MAGNITUDE_OPEN
canon_verdict_change: none
---

# Selected K77 moving-Hq action-owner and potential gate

## Result first

The quartic carrier from v0.199 survives, but the physical-action question
splits more sharply than its next-step sentence suggested.

The released first-order bosonic action `I^B_1` (`SC-ACT-01`) pairs augmented
torsion `T` with the Shiab image of its path-average curvature and adds the
`kappa_1` Hodge term. On the exact moving-`H_q` weak-doublet family, both the
mass scalar and selected-Shiab cubic scalar vanish. Thus this first action is
blind to the radial amplitude on the tested family. It cannot by itself own a
quadratic-plus-quartic Higgs potential there.

The source separately says the Higgs quartic comes from **squaring the
quadratic eddy**. That is a distinct action grammar. For the same moving family
the exact Hodge square of the eddy is `4 r^4`, independently on four coordinate
representatives. With a co-moving curvature background

\[
F_0=\rho E_q,\qquad \bar F=(\rho+r^2/3)E_q,
\]

the corresponding norm-square rival restricts to

\[
V_2(r)=2(\rho+r^2/3)^2.
\]

For `rho<0` it has a conditional nonzero branch

\[
r_*^2=-3\rho,
\]

with positive radial Hessian `-16 rho/3`. Moving-reduction covariance makes
the three orientation directions orbit-flat. This is the exact algebraic
shape of a Mexican hat—conditional on the eddy-square action and the co-moving
background—but it does not select that action, `J`, `rho`, or its magnitude.

## Plain English

The previous wave found a geometric object whose self-interaction has the
right quartic shape. This wave asked whether the action already written in the
2021 draft actually turns that object into a Higgs potential. On the moving
family, it does not: the written first action evaluates to zero.

But Weinstein's separate verbal clue points to another operation: square the
eddy itself. That operation does produce the familiar bowl plus a
curvature-controlled quadratic term. If the curvature background has the
right sign, the bottom moves away from zero and gives one radial mode plus
three flat symmetry directions. The route therefore fits conditionally, but
the missing burden is now precise: establish that GU's full action really
contains this square and derive the background `rho` rather than choosing it.

## Layer 0

| object | exact status |
| --- | --- |
| fixed-`q` tangent coordinates | local linearization; not the moving-family potential |
| moving point `(r,q)` | amplitude plus unit vector on the compact weak orbit |
| first-order action `I^B_1` | released and claim-indexed as `SC-ACT-01` |
| eddy coefficient `E_q=T_q wedge T_q` | exact, nonzero |
| eddy Hodge square | exact finite scalar `4 r^4` |
| co-moving background `rho E_q` | typed construction hypothesis, magnitude unselected |
| physical GU Higgs action | open action-owner identification |
| physical vacuum/spectrum | open beyond the conditional radial calculation |

The preregistered frozen-frame trap fired. Holding `q`, `Jq`, Hodge and Shiab
fixed while varying four tangent coefficients produces a split mass form
`diag(-2,-2,0,0)`, a non-radial cubic, and a split quartic. Those numbers are
exact, but they evaluate the wrong object. When `q`, `Jq`, both form legs and
both Clifford coefficients move together, all four coordinate representatives
give the same scalars: mass `0`, first-action cubic `0`, eddy square `4`.

## What is forced and what remains free

```text
new fields: 0
new fitted soldering-kernel coordinates: 0
new external datum consumed: 0
first-action radial coefficient on moving family: 0
eddy-square quartic coefficient: 2/9 in V_2(r)
curvature cross quadratic coefficient: 4 rho/3
conditional nonzero amplitude: r^2 = -3 rho
unselected: J (20-dimensional family), action owner, rho and its magnitude
```

The construction has positive constraint surplus inside the declared moving
family: one eddy-square grammar produces one radial polynomial, four
coordinate representatives agree, the wrong frozen-frame evaluation fails,
and no free kernel coordinate is introduced. It remains a construction
hypothesis because the eddy-square action is not published at complete grade.

## Symplectic and analytic boundary

The three orbit-flat directions are group-orbit tangents, not yet proven
Goldstone modes in a reduced phase space. The calculation supplies no kinetic
normalization, presymplectic current, gauge moment map, photon kernel, BV
quotient, closed Krein domain or spectral mass. The positive radial second
derivative is a finite potential Hessian conditional on `rho<0`; it is not a
Hamiltonian-positivity or global stability theorem.

## Source and prior-art return

- `SC-ACT-01` confirms the released first-order action and its `1/3[T,T]`
  path-average eddy.
- The Portal transcript says the quartic Higgs piece comes from squaring the
  quadratic eddy.
- The source does not publish the complete eddy-square/Dirac-square action,
  its overall coefficient, the co-moving background identification, or the
  value/sign of `rho`.
- V0.199's Frobenius quartic remains a carrier diagnostic. This wave replaces
  it with the exact Hodge scalar on the moving family, without identifying the
  result as the selected physical action.

## Ledger effect and next gate

`RA-E1`, `RA-E3` and `LT-SM6` move in distance/evidence only. Verdict counts,
residue, forks, five quotients, P1/P2/P3, canon and public posture do not move.

The next gate is:

1. construct the source-owned Dirac-square/second norm-square action and show
   whether its weak restriction is exactly `V_2` with no extra coefficient;
2. derive the co-moving background `rho E_q` from curvature/observation rather
   than posit it, and connect its sign to the dynamic cosmological sector;
3. derive the kinetic term and reduced Hessian, requiring three genuine gauge
   orbit/Goldstone directions, one radial scalar and the photon kernel; and
4. only then attach the zero-order Yukawa/reality blocks.

The exact probe passes with zero failures.
