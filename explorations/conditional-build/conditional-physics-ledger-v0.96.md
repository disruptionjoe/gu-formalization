# Conditional physics ledger v0.96

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.96.json`.

## Progress

`Ledger v0.96 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.96 adds

The actual 24-dimensional horizontal source-`varpi` operator is now emitted
as a first-order coefficient bank on the residual carrier:

```text
J_varpi = A^mu partial_mu + B,
rank(A^mu) = 13 for mu=0,1,2,3,
rank(B) = 24.
```

With the conditional v0.92 residual bilinear `K_loc`, the canonical formal
object is the field covector

```text
J_K^! v = -partial_mu((A^mu)^T K_loc v) + B^T K_loc v,
G^mu(u,v) = u^T (A^mu)^T K_loc v.
```

The Green identity is exact and nonzero in all four directions. A field-valued
operator adjoint is not canonical until a nondegenerate field-space Riesz map
is supplied; two exact choices give different vectors for the same covector.

The attempted full common-field assembly fails closed. The local `D_g`
geometry/ranks from v0.95 have not been emitted on this common residual
coordinate bank. The epsilon predecessor owns a primitive Euler covector and
a four-column principal Ward orbit, while its own registry leaves the full
lower-order/nonlinear `D_epsilon Upsilon` field bank open.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `4`; conditions opened: `2`.

Closed:

1. actual four-direction `D_varpi` principal coefficient bank;
2. actual rank-24 `D_varpi` zero-order bank;
3. exact covector-valued `K_loc` formal adjoint and nonzero Green current;
4. exact field-Riesz nonuniqueness fence.

Remaining named conditions: `3`.

1. Emit the complete local `D_g Upsilon` on the same residual-coordinate
   coefficient bank.
2. Construct the full lower-order/nonlinear primitive `D_epsilon Upsilon`
   bank and verify common-field `J R=0`.
3. Derive a geometrically owned field-space Riesz map, or retain explicitly
   covector-valued adjoint language.

P1/P2/P3 remain unchanged and unused. No residue, quotient, fork, canon,
claim-status, public-posture, Curt-track or third-lane status changes. The
selected Spin-native parent, the product of two `U(32,32)` Weyl halves and the
full `U(64,64)` comparator remain distinct action-parent questions.

## Evidence

- `explorations/conditional-build/selected-k77-common-field-formal-adjoint-green-2026-08-08.md`
- `lab/process/selected-k77-common-field-formal-adjoint-green.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-common-field-formal-adjoint-green-review.md`
