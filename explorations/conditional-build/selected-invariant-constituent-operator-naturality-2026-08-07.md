---
artifact_type: conditional_build_result
created: 2026-08-07
status: SELECTED_FA_STAR_T_STAR_EXACT__INVARIANT_BRANCH_TANGENT_OPERATOR_PACKET_ZERO__AMBIENT_FIELD_NORMAL_JET_OPEN
source_return: SOURCE-CONFIRMS__RAW_RESIDUAL_CONSTITUENTS__SOURCE-SILENT__SELECTED_BRANCH_TANGENT_OPERATOR_NATURALITY
ledger: lab/process/conditional-physics-ledger-v0.54.json
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Selected invariant constituent operator naturality

## Result in plain English

The selected stationary background is now explicit in the source raw-residual
variables:

\[
T_*=-\frac{\kappa_1}{312}\Phi_1,
\qquad F_{A*}=T_*\wedge T_*.
\]

Both constituents are nonzero. Their selected outputs are also separately
nonzero, but cancel coefficientwise:

\[
\operatorname{Shiab}(F_{A*})+*\kappa_1T_*=0.
\]

Along the fused moving-gimmel/frame packet, `Phi1` is the tautological identity
and is invariant; so is its square. The two operator derivatives are therefore
the same natural target transport applied to opposite vectors. Their sum is
zero on every one of the four graph columns. This branch-tangent operator
packet supplies none of the transverse 117.

That does **not** set `delta F_A` or `delta T` to zero. The independent ambient
field/connection/soldering/observation normal jet is now the remaining owner
candidate.

## Layer 0

| phrase | object computed | object kept distinct |
| --- | --- | --- |
| source curvature `F_A*` | full raw connection curvature `T* wedge T*` on the flat constant branch | action path-average `bar F=(1/3)T* squared` |
| operator response | branch-tangent natural movement of Shiab/Hodge on invariant inputs | independent input derivatives `delta F_A`, `delta T` |
| metric/frame packet | actual trace-reversed gimmel TT variation plus exact co-moving frame | arbitrary off-branch physical field motion |
| zero response | zero fused operator column in the raw residual target | full action, Euler equation, quotient or spectrum |
| transverse comparison | zero intersection with the nonzero 117 family | construction of the 117's actual owner |

This is the same distinction that v0.53 restored: zero total residual is not
constituent zero. The new result adds a separate reason the *branch-tangent
natural operator sum* vanishes—both nonzero constituents transform in the same
target representation and are exact negatives.

## Exact construction

The probe reuses the exact `Cl(7,7)` selected `comm/symi/symi` backend. At
`kappa_1=1` it constructs:

```text
T* support       = 14
F_A*=T* wedge T* = 91
Shiab(F_A*)      = 14 nonzero target coefficients
*T*              = 14 nonzero target coefficients
sum              = 0 coefficientwise
```

The one-third action path-average is a planted wrong-object control and does
not solve the source raw endpoint.

For the actual TT gimmel variation, the total metric derivative has rank eight
and its co-moving compensator is exact. The fixed-coordinate degree-one Hodge
derivative and the induced thirteen-form target transport are both nonzero.
One constituent's target transport is therefore live, but the two transported
constituents cancel exactly. The result is not a vacuous “everything moves to
zero” matcher.

## Source return

The primary source confirms the raw residual has separate Shiab-curvature and
Hodge-torsion constituents. It does not publish this selected stationary
coefficient, the `comm/symi/symi` choice as Weinstein's recovered historical
selector, or the branch-tangent naturality theorem.

```text
SOURCE-CONFIRMS: raw residual constituent types and common target.
SOURCE-SILENT: selected branch-tangent operator cancellation and transverse-117 comparison.
```

## Specialist and hostile review

- **Differential geometry:** naturality applies to the invariant branch; it
  does not erase independent connection, soldering or observation jets.
- **Representation theory:** the 14 and 91 supports are carrier counts, not
  particle multiplicities or generations.
- **Variational PDE:** a zero coefficient/operator column is not the total
  principal symbol until input jets and constraints are assembled.
- **Symplectic geometry:** no Euler, presymplectic, BV or BFV class follows;
  the next input-jet wave must carry the symplectic review again.
- **Krein/operator theory:** this exact finite naturality identity says nothing
  about positivity, a common closed domain or Green operators.
- **Source criticism:** the source fixes the constituent grammar but is silent
  on this selected response.
- **Repo archaeology:** v0.53's correction, the exact stationary branch and
  the moving-gimmel/frame theorem were composed; no predecessor replay was
  counted as the scientific result.

Both hostile charges were applied. The summary may not turn branch-tangent
zero into a full moving-geometry kill. Conversely, rigor may not keep the now-
closed selected operator packet on the queue as if its constituents were still
unknown.

## Progress and fences

```text
Ledger v0.54 — 82/82 mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped

headline_delta: none
conditions_closed: 4
  - T* constructed in raw-residual variables
  - F_A*=T* wedge T* constructed
  - nonzero constituent cancellation checked coefficientwise
  - branch-tangent operator response compared with transverse 117 and found zero
conditions_opened: 0
remaining_named_conditions: 4
  - independent ambient field/connection/soldering/observation normal jet
  - null screen and total raw-Upsilon/Bianchi naturality
  - scalar and massless constraint quotient
  - coupled fermion Hessian and common domain
```

No verdict, residue, quotient, scalar pole, physical spectrum, P1/P2/P3,
canon verdict or public posture changes. Curt remains formally separate and no
third lane is promoted.

## Next gate

Construct `delta F_A` and `delta T` induced by the independent ambient field,
gauge-rotated Levi-Civita, soldering and observation normal jets on the same
four graph directions. Compose them with the fixed selected Shiab/Hodge maps
and compare coefficientwise with the transverse 117. Then construct the null
screen and test total Bianchi/raw-`Upsilon` naturality. Do not reuse the
branch-tangent naturality cancellation as an input-jet cancellation.

The executable probe passes `35/35` with live Hodge/target-transport controls
and planted fences against path-average substitution, full-action promotion,
datum substitution and symplectic inflation.
