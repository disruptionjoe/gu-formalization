---
title: "F1 rides the existing pipeline: the D6 cross-sector confrontation is bound to P-OBS and the frozen PP1/PP3 kill surfaces"
status: active_research
doc_type: preregistration_pipeline_binding
created: "2026-08-23"
directed_by: "Joe direct chat, 2026-08-23 (preregister the dark-energy data confrontation; awareness-note canon path)"
registry: lab/process/selected-k77-ltgr8-heldout-consequence-reservation-protocol.json
probe: tests/channel-swings/selected_k77_ltgr8_heldout_consequence_reservation_protocol_probe.py
grade: "PIPELINE BINDING AND RELEASE CHECKLIST ONLY; NO NEW STATISTIC, KILL SURFACE, DATASET READ, PACKET, PREDICTION OR VERDICT; NOTHING ALREADY FROZEN IS RESTATED OR RETUNED"
target_claim: "the D6/F1 external confrontation executes through the already-frozen P-OBS/PP1/PP3 machinery; no second observation mechanism is created; LT-GR8 remains NEEDS"
canon_verdict_change: none
---

# F1 rides the existing pipeline

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: a duplicate-check against the destination found the dark-energy
confrontation already substantially preregistered — PP1 (sign, frozen
2026-07-19) and PP3 (one-parameter thawing curve family, frozen 2026-07-22)
carry frozen kill surfaces, and `P-OBS-LEG`'s standing instruction is to
apply them without retuning on an official future release while creating no
second observation mechanism. This artifact therefore adds only what is
genuinely missing: the binding of yesterday's D6 family F1 to that existing
machinery, and a release-handling checklist. It freezes no new statistic and
reads no data.

```gu-typed-objects
result: F1's confrontation is bound to the P-OBS wake and the frozen PP1/PP3 kill surfaces as its sole execution machinery, with a five-step release checklist
carrier: none constructed LAYER=protocol CHIRALITY=N/A
pairing: none constructed
real_structure: N/A at protocol grade
grading: N/A at protocol grade
action_owner: none -- no statistic, surface, dataset, packet or observable is created or modified; PP1/PP3 remain frozen exactly as recorded
target: the D6/F1 execution pathway MAP-TYPE=binding
```

## The binding

1. **One pipeline.** F1's confrontation data ("BAO or BAO-like
   amplitude-calibration data released after the packet freeze") is evaluated
   only through the `P-OBS-LEG` wake, applying the already-frozen PP1/PP3
   kill surfaces without retuning. F1 creates no second observation
   mechanism, no new statistic, and no re-derivation of any frozen number.
2. **The packet hook.** If a completed K77 packet exists when an applicable
   release arrives, its F1 consequence (the derived entropy-normalization to
   dark-energy-amplitude relation, reserved under the five-step D6 protocol)
   is evaluated under the same release, the same provenance verification, and
   the same no-retuning rule, alongside — never instead of — the PP1/PP3
   surfaces. If no packet exists, the confrontation is PP1/PP3-only and earns
   no LT-GR8 credit in either direction.
3. **Custody unchanged.** Consumed DR2 inputs are not rescored (the P-OBS
   rule) and remain inadmissible as held-out data (the F1 rule). These are
   the same discipline stated once each; this binding adds no third custody
   regime.

## Release checklist (frozen)

On an official release applicable to PP1/PP3:

1. verify provenance per the `P-OBS-LEG` standing instruction before any
   value is read;
2. record the release identity and date in the confrontation record before
   computing anything;
3. apply the frozen PP1/PP3 kill surfaces exactly as recorded, without
   retuning, and report per their own frozen grammar;
4. if and only if a reserved F1 consequence exists, evaluate it under the
   same release per the D6 protocol's commit-before-read step;
5. report outcomes with the standing grammar: a surface firing kills its
   scoped family; survival is compatibility; F1 success is at most a
   prediction candidate pending independent verification.

## Claim ceiling

Binding and checklist only. PP1/PP3 stay frozen as recorded and are not
restated here; no dataset is read; no packet exists; LT-GR8 remains
`NEEDS`/`MISSING_CONSTRUCTION`. Awareness note: this is a
process-surface change in Joe's lane; scheduled `P-OBS-LEG` behavior is
unchanged — the wake it already watches now also serves F1 when a packet
exists. No scientific ledger verdict, canon, source ownership, prediction
credit, or public posture changes.
