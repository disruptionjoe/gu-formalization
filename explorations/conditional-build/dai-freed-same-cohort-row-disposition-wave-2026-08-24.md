---
title: "Dai--Freed and compact SAME-cohort disposition: three fitting constructions and one typed aggregate impossibility"
status: active_research
doc_type: result
created: "2026-08-24"
registry: lab/process/dai-freed-same-cohort-row-disposition-wave.json
probe: tests/channel-swings/dai_freed_same_cohort_row_disposition_wave_probe.py
grade: "EXACT GF(2) AHSS RECOMPUTATION AND CORPUS/METHOD DISPOSITION; THREE FITTING_CONSTRUCTION TERMINALS AND ONE TYPED AGGREGATE IMPOSSIBILITY; NO LEDGER, SOURCE, CANON OR PUBLIC-POSTURE MOVEMENT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "The precise impossibility binds only the already-superseded LT-GR2 aggregate as one construction; it kills no cosmological-sign, variable-theta, source-native Yukawa or GU route."
canon_verdict_change: none
---

# Dai--Freed and compact SAME-cohort row-disposition wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: terminal disposition of AC-E1 LT-GR2 LT-GR2b and LT-SM5 after exact Dai-Freed recomputation and carrier-local FC audit
carrier: observed SM Weyl content on four Spin global forms; separate Spin-times-Z4 control; superseded GR2 aggregate; K77 variable-distortion relative-KO interface; K77 ambient Weyl-half exchanger LAYER=observed CHIRALITY=S-CHIRALITY-UNTYPED
pairing: reduced-eta bordism homomorphism; semantic carrier comparison; real-KO clutching pairing; ambient Clifford half forms ON=declared-carriers
real_structure: complex observed Weyl content; real K77 relative-KO carrier; real Clifford module with complex half restrictions
grading: chirality and bordism degree; relative-KO degree; ambient Weyl-half and derivative grading
action_owner: repository-construction; OWNER-A and OWNER-D remain named pathway owners; no action or physical projection is selected here
target: AC-E1 LT-GR2 LT-GR2b LT-SM5 MAP-TYPE=evaluation
```

## Result first

The final four `SAME` rows now have evidence-backed terminal dispositions while
conditional-physics ledger v0.263 remains byte-identical.

- `AC-E1` is banked as `FC-ANOMALY-SM-DAI-FREED-SHADOW-1`. The plain-Spin
  groups are independently recomputed as `Z/2, 0, Z/2, 0` for
  `Gamma_n`, `n=1,2,3,6`; complete Standard-Model content evaluates to zero
  on both surviving Witten classes. The separate `Spin x_(Z2) Z4`/`Z16`
  object is labelled rather than folded into the plain-Spin receptacle.
- `LT-GR2` closes as `PI-LT-GR2-UNSPLIT-AGGREGATE-1`. The obsolete row cannot
  be one construction because it identifies a sign, a variable field,
  curvature covariation, a magnitude/radiative burden and observable
  cosmology. Its existing successors `LT-GR2a`--`LT-GR2e` are the escape.
- `LT-GR2b` is banked as
  `FC-GR-VARIABLE-THETA-RELATIVE-KO-INTERFACE-1`: an action-owned local
  distortion carrier and injective boundary-winding-to-relative-KO input map,
  not an output equation, VEV selection, index or count.
- `LT-SM5` is banked as `FC-SM-AMBIENT-WEYL-HALF-EXCHANGER-1`: the exact K77
  trace-radial zero-order half exchanger and derivative carrier, not a
  physical Yukawa texture, chirality selection, potential or mass.

No ledger verdict, distance, revival trigger, source owner, prediction,
confirmation, claim status, canon or public-posture field moves.

## Preflight bookend: frontier, typing and route choice

The live register, not the prior handoff, rebuilt the frontier. Its only
remaining `SAME` cohort was exactly these four rows. Layer 0 separated:

1. the plain-Spin bordism receptacle from the anomaly homomorphism carried by
   the fermion content;
2. the plain-Spin gauge object from the separately assumed discrete `Z4`
   tangential structure;
3. cosmological sign, field carrier, coefficient/equation placement and
   observable background;
4. a relative-KO input twist from a Fredholm index or physical count; and
5. an ambient Weyl-half exchanger from observed Yukawa texture and chirality.

The route census considered citation-only closure, headline-only
recomputation, blanket B1 admission from `SAME`, and exact carrier-local audit.
The last route dominates: it reproduces the deciding bordism values, tests the
content rather than declaring the receptacle empty, and makes each incomplete
physical owner visible. The fallback was nonterminal narrowing if any FC
criterion failed. No large search was admitted: exact GF(2) linear algebra and
existing exact carrier certificates decide the wave.

The corpus search covered the AC-E1 evidence chain back to its 3-local root,
the 2026-08-12 pre-deposit derivation, CB-C, W222, the relative-boundary/P3
interface, the radial-half exchange gate, the corrected representation and
Morita records, path dependencies, correction registry and ledger v0.263.
The source audit covered `SC-GRP-01`, `SC-GRP-02`, `SC-FER-03`, `SC-META-57`
and `SC-GEO-58`. No matching canonical-source correction supersedes the
objects used here; the old K95 right-`H` comparator remains fenced from K77.

## Packet A: independent Dai--Freed reconstruction

For a connected global form `G`, the degree-five Spin AHSS is

```text
E2_(p,q) = H_p(BG; Omega_q^Spin(pt)) => Omega_(p+q)^Spin(BG).
```

Even, torsion-free integral cohomology and
`Omega_3^Spin = Omega_5^Spin = 0` leave only
`E2_(4,1) = H_4(BG; Z/2)`. Hence

```text
Omega_5^Spin(BG)
  = ker[d2 : E_(4,1) -> E_(2,2)]
    / im[d2 : E_(6,0) -> E_(4,1)].
```

Both `d2` maps are the appropriate dual `Sq2` maps. In the degree-four dual
basis `(t^2, c2_3, c2_2)`, the outgoing map kills `t^2`. The incoming image
always kills `c2_3`; it also kills `c2_2` exactly when the `SU(2)` factor is
absorbed into `U(2)`, namely `n=2,6`. The exact quotient is therefore:

| global form | recomputed group | published control |
| --- | --- | --- |
| `G_SM/Gamma_1` | `Z/2` | match |
| `G_SM/Gamma_2` | `0` | match |
| `G_SM/Gamma_3` | `Z/2` | match |
| `G_SM/Gamma_6` | `0` | match |

The same implementation returns `Omega_5^Spin(BSU(2))=Z/2`,
`Omega_5^Spin(BU(1))=0`, and `Omega_5^Spin(BSU(3))=0`. These mixed positive
and negative controls prevent an unconditional-zero or hard-coded pass.

For `n=1,3`, the survivor is the Witten `SU(2)` class. One complete SM
generation contains three coloured quark doublets plus one lepton doublet,
so its evaluation is `4 mod 2 = 0`; any integer number of complete generations
also evaluates to zero. For `n=2,6`, the receptacle is already zero. Thus
the content anomaly homomorphism is zero for all four forms even though two
receptacles are nonzero.

This matches Davighi--Gripaios--Lohitsiri, arXiv:1910.11277, and the simple-
group controls of Garcia-Etxebarria--Montero, arXiv:1808.00009. The second
paper's `16` belongs to another object: with an additionally assumed anomaly-
free discrete `Z4`, the `Spin x_(Z2) Z4` group is `Z/16` and sixteen odd-
charged Weyl fermions, including a right-handed neutrino, evaluate trivially.
That theorem-assisted control is labelled separately. GU does not thereby
select the discrete symmetry, global form or physical reduction.

### FC-1--FC-7 ceiling

The full fingerprint and criterion audit are serialized in the registry. The
construction lives at R4. Its pathway is a frozen OWNER-D observed reduction
followed by OWNER-B evaluation on this exact object. It demotes if physical
content is not a union of anomaly-balanced blocks, the tangential structure
changes, or a checked differential disagrees. It forbids odd doublet content
on `n=1,3` and forbids identifying the `Z/2` and `Z/16` receptacles.

## Packet B: why `LT-GR2` cannot remain one object

`LT-GR2` is already marked `SUPERSEDED`, with five successor rows. The
impossibility is semantic and exact at that frozen schema: one sign-exact
statement cannot simultaneously construct a variable field, its curvature
covariation, the coefficient/radiative placement and an observable cosmology.
Those obligations have different carriers and quantifiers. Compatibility of
their signs supplies no typed isomorphism.

The escape is not speculative: use `LT-GR2a`--`LT-GR2e`, which the ledger has
already minted. The aggregate could resurrect only if one frozen action-owned
construction came with explicit commuting bridges proving all five successors
were its functorial images without fitted identifications. This closes only
the obsolete aggregate and kills no sign or variable-theta route.

## Packet C: variable-theta relative-KO interface

`FC-GR-VARIABLE-THETA-RELATIVE-KO-INTERFACE-1` composes two built objects at
R4: the local action-owned olive/varpi distortion amplitude, and the boundary
winding map through `SU(2)=Sp(1)` clutching to a pulled-back real rank-four
relative-KO class. The normalizations `c2=n`, `p1_fund=-2n`, and
`p1_adj=-4n` are explicit. The live K77 carrier is real; the older K95
right-quaternionic comparator is not ported.

Its pathway is specific: OWNER-B must build the K77 relative operator and
Fredholm domain on this input class, then OWNER-D supplies the physical count
readout. The construction forbids treating winding, characteristic-class
normalization and count as the same object, and its injective pullback forbids
a nonzero winding coordinate from silently becoming zero. It earns no VEV,
index, count, cosmological magnitude or dynamics claim.

## Packet D: ambient Weyl-half exchanger

`FC-SM-AMBIENT-WEYL-HALF-EXCHANGER-1` lives at R4 on the settled real K77
Clifford carrier. The trace-radial cell satisfies the exact zero-order
half-exchange identities, has rank 64 in each direction, and owns a rank-four
observed derivative incidence. Its isolated self-wedge vanishes.

The pathway is OWNER-A's complete moving `U(3,2)` doublet connection bank,
curvature action and Hessian, followed by OWNER-D's physical
`P0/rho(Phi)/Y_K/Y_C/C` placement and observed mass descent. The object
forbids same-half zero-order action by this cell and forbids an isolated radial
Mexican-hat potential. It is source-compatible with the registered
connection-cell Higgs/Yukawa claims and the disavowal of a separately added
fundamental Higgs, but the trace-radial realization remains reconstructed,
not source-owned.

## Postflight bookend and hostile review

- **Strongest overclaim:** “GU is globally anomaly-free and has built dark
  energy and Yukawa physics.” Refused. These are observed-shadow/topological
  and ambient-carrier fitting constructions; physical reduction, action,
  domain, VEV, texture, mass and observable dynamics remain open.
- **Strongest contrary route:** a different tangential structure or physical
  projection can reopen AC-E1; a failed K77 Fredholm domain can demote the
  relative-KO interface; the full moving bank can reject the radial cell.
  Each is an explicit demotion condition.
- **Weakest reproducibility seam:** the AHSS depends on the standard dual-
  `Sq2` identification. The native probe exposes the exact matrices, checks
  three independent controls and plants both group-value and content-parity
  mutations.
- **Weakest source seam:** neither K77 carrier is source-action-selected. The
  source register was checked with polarity respected and the owner pathway is
  explicit.
- **Propagation seam:** only disposition evidence changes. Ledger v0.263 and
  all protected scientific fields remain unchanged.

The measurable effect is three fitting constructions plus one bounded typed
impossibility. The live denominator becomes 49 terminal and 42 open rows of
91, with `exhausted=false` and `b2_selectable=false`. The next frontier is the
boundary/anomaly `DIFFERS` cohort and the highest-fanout remaining `NEEDS`
rows; B2 source-action work remains ineligible. No Joe intervention is
required.

Reproduce with:

```bash
python3 tests/channel-swings/dai_freed_same_cohort_row_disposition_wave_probe.py --selftest
```
