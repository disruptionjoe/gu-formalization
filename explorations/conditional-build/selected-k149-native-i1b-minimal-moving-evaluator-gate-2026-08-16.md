---
title: "Selected-K149 native I1B minimal moving evaluator gate"
status: active_research
doc_type: exact_sparse_differential_algebra_and_target_adapter_closure_gate
created: "2026-08-16"
registry: lab/process/selected-k149-native-i1b-minimal-moving-evaluator-gate.json
probe: tests/channel-swings/selected_k149_native_i1b_minimal_moving_evaluator_gate_probe.py
grade: "K149 SERIALIZES THE FIRST REUSABLE MOVING-EVALUATOR LAYER: AN EXACT MULTIVARIATE SPARSE DIFFERENTIAL-OPERATOR ALGEBRA WITH COMPLETE LEIBNIZ COMPOSITION, EXPLICIT TOTAL-CONNECTION HOOKS, AND A MECHANICAL MOVING-DENSITY/MOVING-PAIRING FORMAL ADJOINT. ALL 25 EXACT CONTROLS PASS, INCLUDING ORDINARY, ONE-FORM, CLIFFORD-ADJOINT, PLAIN-TRANSPOSE, AND SIXTH-PROFILE-JET PLANTS. EXECUTABLE CLOSURE THEN LOCATES THE FIRST TARGET-SPECIFIC GAP: NO CURRENT SOURCE FILE EXPORTS THE MOVING SELECTED-SHIAB COEFFICIENT AS A COORDINATE OPERATOR. THEREFORE THE CURVED RESTRICTED RESIDUAL AND NULL LOWER LEAKAGE REMAIN UNDEFINED, NOT ZERO. K150 OWNS THAT ONE ADAPTER BEFORE THE CURVED BRIDGE OR FIVEFOLD COMPOSITION."
target_claim: K148_NEXT_GATE__MINIMAL_MOVING_EVALUATOR_AND_CURVED_NULL_LOWER_LEAKAGE
target_verdict: GENERIC_SPARSE_DIFFERENTIAL_JET_ENGINE_EXACT__MOVING_WEIGHTED_FORMAL_ADJOINT_EXACT__TARGET_PACKET_STOPS_AT_FIRST_ABSENT_MOVING_SELECTED_SHIAB_COORDINATE_ADAPTER__CURVED_RESTRICTED_RESIDUAL_UNDEFINED
canon_verdict_change: none
---

# Selected-K149 native I1B minimal moving evaluator gate

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K149 binds the evaluator infrastructure needed by the selected
conditional `comm/symi/symi` local `I1B` branch at `T=0`. It does not recover
the preferred historical Shiab, materialize an action coefficient that is not
present in executable source, or compute a curved residual by substituting a
toy coefficient.

## Result

K147 identified six missing executable primitives. K149 closes the generic
differential-algebra part with
`tests/channel-swings/k149_sparse_differential_jet_api.py`:

```text
operator:              sum_alpha A_alpha(x) partial^alpha;
composition:           complete multivariate Leibniz rule;
formal transpose:      (-partial)^alpha A_alpha(x)^T;
weighted adjoint:      B_in^-1 rho^-1 L^T_formal rho B_out;
covariant derivative:  partial_mu + Gamma_mu on a flattened carrier.
```

The implementation keeps coefficient differentiation exact in SymPy. A
moving-density/moving-pairing Green test gives the expected total divergence;
the plain formal transpose fails the same test. Removing the ordinary,
one-form, or Clifford-adjoint connection separately changes the evaluated
section. Generic two-profile Brinkmann controls retain independent sixth jets
while remaining transversely harmonic.

## Exact closure boundary

The engine does not choose physics. To evaluate K148's restricted residual it
still needs target adapters in this dependency order:

```text
moving_selected_shiab_coefficient
  -> moving_distortion_pairing
  -> curved_metric_bridge_A
  -> curved_null_restricted_residual.
```

The K149 probe parses the reusable K77, K132, K135, K138, and K149 executable
sources. Frozen `raw_block`, principal `coupled_local`, and Brinkmann curvature
fixtures exist. None exports the first target adapter
`moving_selected_shiab_coefficient`, so none of the dependent adapters can be
honestly composed. This is a serialization boundary, not a claim that the
conditional tensor formula is mathematically unowned. The formula remains in
the research record; its coordinate operator lift has not been written and
tested.

Consequently:

```text
generic sparse differential-jet engine:          EXACT;
Leibniz-complete composition:                    EXACT;
moving weighted formal adjoint:                  EXACT;
three connection channels:                       EXPLICIT_AND_PLANTED;
moving selected-Shiab coordinate adapter:        NOT_SERIALIZED;
curved restricted K P^5 K A residual:            UNDEFINED;
first curved null lower leakage:                  UNDEFINED;
frozen K148 quotient principal map:               ZERO_EXACT_UNCHANGED.
```

## Why no toy coefficient is substituted

A planted matrix can test the engine but cannot decide the action. Importing
K132's frozen matrix as a moving coefficient would discard connection and
coefficient-derivative terms. Importing an algebraic transpose would repeat
the precise K144 error class. Guessing the selected Shiab lift would silently
choose index placement, pairing, density, and connection conventions. K149
therefore stops at the first executable dependency rather than reporting a
fabricated zero or leakage.

## Route selection and hostile controls

The route census compared a dense 229376-carrier evaluator, sparse dual
composition, a metric-only toy, and an immediate missing-owner return. Sparse
composition dominates the dense route and is reusable. The toy route is
scientifically invalid. The immediate return would have left generic
Leibniz/adjoint uncertainty unresolved. Hostile review confirms that the
engine's passing controls do not license the absent target adapter or a
curved result.

## Reverse scaffold

```text
K148: frozen null quotient principal descent is exact and zero.
K149: generic sparse differential algebra, connections and weighted adjoint
      are executable; target closure stops at the first absent moving-Shiab
      coordinate adapter.
K150: serialize that one adapter from the owned conditional tensor formula,
      replay K132 frozen blocks and rotated covariance, then attempt the
      moving pairing and curved bridge composition.
Only after target closure: compute the curved restricted residual and lower
      leakage; only after descent may a quotient endomorphism be named.
```

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k149_native_i1b_minimal_moving_evaluator_gate_probe.py
```

## K150 successor classification

K150 serializes `moving_selected_shiab_coefficient` from the selected
conditional two-term tensor formula as an exact coordinate-jet multiplication
operator. Frozen K132 blocks and the K138 rationally rotated null control pass.
The next executable dependency is `moving_distortion_pairing`; the curved
metric bridge and restricted residual remain undefined until that pairing is
serialized and its weighted adjoint is checked.
