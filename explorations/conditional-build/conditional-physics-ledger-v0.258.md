---
title: "Conditional Physics Ledger v0.258"
status: research
doc_type: ledger-summary
created: 2026-08-14
---

# Conditional Physics Ledger v0.258

Ledger v0.258 preserves all 82 verdicts and the canonicity-distance meter. It
migrates four distances after separating three boundary objects that had been
allowed to blur together:

1. the one source-owned moving `epsilon` field;
2. the exact 98-dimensional Cartan restriction of its cotangent geometry;
3. a distinct opposite-sign compensating edge system.

The unrestricted source preboundary potential

```text
<lambda, epsilon^-1 delta epsilon>
```

is exactly the canonical left-trivialized potential on
`T*Spin_0(7,7)`. Its 182-dimensional two-form is nondegenerate, and the
previous 98-dimensional Cartan-slice form is exactly its pullback. This closes
formal source/action ownership of the full cotangent parent. It does not make
the Cartan restriction action-selected or physical.

The opposite Cartan chamber still cancels the endpoint charge and its first
variation as an independent mathematical system. It cannot be manufactured
from the existing source epsilon. The actual epsilon carries the original
endpoint sign, so counting it twice gives `2 mu`, not zero. Moreover, the
principal Cartan bundle has a seven-dimensional stabilizer: local gauge-fixed
sections exist, but a full-`G`-equivariant section depending only on the charge
cannot. Its canonical connection has curvature spanning all seven Cartan
directions and transports the ambiguity rather than selecting a point.

The result is a useful negative classification. Charged boundary symmetry is
the only current zero-import horn. An independent opposite edge completion
remains mathematically consistent but source/action-unowned. Functional BFV,
analytic domain, positivity and physical cohomology remain open.

```text
Ledger v0.258 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Tightness: T4x1 T3x3 T2x1 · scoped quotients: 5
Headline: source epsilon owns the formal T*G parent; no equivariant
          charge-only opposite selector exists
```

The next primary gate is therefore native background legality: test the exact
residual-zero candidates against the distinguished `B(epsilon)/Y` curvature
and moving geometry. In parallel, construct the smallest action-stationary
nonhomogeneous vacuum. Reopen the edge-completion branch only if new source or
action evidence supplies a second boundary owner or an opposite boundary
term.
