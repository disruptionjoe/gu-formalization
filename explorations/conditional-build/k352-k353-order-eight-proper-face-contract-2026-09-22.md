---
title: "K352--K353 order-eight proper-face contract"
status: internal_structural_only
claim_verdict: internal_structural_only
date: 2026-09-22
claim_ceiling: exact determinant-preserving mask-native preconditioner bank and local face-normal integrability for every reachable K350 face; recursive positive-interior coverage, analytic tails, numerical Peano integrals and every downstream scientific claim remain open
manifests:
  - lab/process/k352-order-eight-mask-native-preconditioner-compiler.json
  - lab/process/k353-order-eight-face-normal-integrability-atlas.json
producers:
  - tests/channel-swings/k352_order_eight_mask_native_preconditioner_compiler.py
  - tests/channel-swings/k353_order_eight_face_normal_integrability_atlas.py
probes:
  - tests/channel-swings/k352_order_eight_mask_native_preconditioner_compiler_probe.py
  - tests/channel-swings/k353_order_eight_face_normal_integrability_atlas_probe.py
---

# K352--K353 order-eight proper-face contract

Classification: `INTERNAL_STRUCTURAL_ONLY`.

This result advances the K348 remainder calculation from a list of reachable
faces to an executable zero-safe scaling interface. It does not integrate a
Peano term or turn K351's reference cells into a global bound.

## K352: determinant-preserving mask compiler

For each K350 face, a primitive entry is marked exactly when its cumulative-
time argument mask is contained in the zeroed face coordinates. Within a
rank-`r` species determinant, the largest number of marked entries in any
Leibniz term is a size-`r` assignment problem. K352 compiles both:

- a primal permutation attaining that maximum; and
- binary row/column scaling powers whose sum equals the same maximum and
  dominates every marked entry.

Strong assignment duality therefore gives a determinant-preserving
preconditioner: scale rows and columns before interval determinant assembly.
No numerical result takes absolute values permutation by permutation.

Across all 2,400 ordered descriptors and all 517 reachable face instances,
4,876,344 determinant-matrix/face uses reduce to 34 exact singular templates:
two of rank one, five of rank two, ten of rank three and seventeen of rank
four. A separate bank of 38 confluent templates pins the repeated-row/column
clusters, Vandermonde orders and divided-difference orders through three.
Every primal, dual and confluent certificate replays exactly. K349 supplies the separate
primitive derivative scaling `w^(d+1)*(2*K1)^(d)` for `d=0,1,2`, so no raw
Bessel function is evaluated at zero.

## K353: complete face-normal product rule

K353 introduces the exact normal chart for a codimension-`c` mask:

`t_a = rho*q_a` for zeroed axes, with `q` on the positive `(c-1)`-simplex.

The Jacobian contributes `rho^(c-1)`. Each selected zero primitive contributes
one inverse power, an active derivative contributes one additional inverse
power, and the K348 Peano kernel restores two powers when the active axis is
normal to the face. K353 performs the exact max-plus product-rule convolution
through derivative order two over the two old-position kernels and every
species determinant in all 2,400 ordered descriptors.

All 1,240,800 descriptor-face replays are locally integrable. The global
minimum face-normal exponent is zero, while the all-eighteen-axis `s1` face
replays K349's degree seven with value/first/second singular powers
`10/11/12`. Determinant coalescence cancellation is not needed merely to prove
integrability, but K352 preconditioning remains necessary for interval
contraction and faithful determinant assembly.

## Boundary and next dependency

K352--K353 release the mask-native evaluator interface. They do not supply the
recursive positive-interior cells, finite annuli, analytic radial tails or any
of the eighteen numerical Peano integrals. The next calculation must
instantiate these scalings inside the complete coherent Arb evaluator, join
accepted face cells to a recursive interior cover and tails, and only then
integrate K348 and join K345.

SC-META-53 remains `UNCERTAIN`; RA-F1, LT-SM8, LT-GR6b and AC-F1 remain
`NEEDS`. No source, ledger, canon, paper, public or physical posture changes.

The K352 probe passes `11/11` controls and rejects `12/12` hostile mutations;
the K353 probe passes `10/10` and rejects `11/11`.
