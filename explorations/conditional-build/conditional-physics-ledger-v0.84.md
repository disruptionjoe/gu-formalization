# Conditional physics ledger v0.84

Status: append-only successor to v0.83.

```text
Ledger v0.84 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Tightness — T4×1 · T3×3 · T2×1
Scoped quotients ranked — 5
```

## What moved

Five gravity/source-action rows move only in distance, evidence and mapping
grade: `LT-GR1`, `LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`.

v0.84 resolves v0.83's source-epsilon principal-orbit fork:

- the ordinary grade-two Kosmann compensator is exactly minus the spin
  Levi-Civita lift, remains rank three and has the same longitudinal kernel;
- the already-owned grade-one map
  `gamma_epsilon:C -> ad(P_H)` supplies a conditional epsilon tangent
  `eta=gamma_epsilon(xi-flat)` of rank four;
- the injective all-grade raw-residual response preserves those four columns
  and is nonzero on the longitudinal direction missed by source `varpi`;
- source-varpi plus gamma-epsilon admits exact four-column principal
  `J R=0` in timelike, spacelike and null classes; and
- no field, fitted coefficient or external datum is added.

This removes the fixed-epsilon rank obstruction to the old rank-four metric
diagnostic, but does not identify it coefficientwise with the physical Gram
block. Six transverse physical `D_g Upsilon` columns, lower-order/nonlinear
`D_epsilon Upsilon`, `K*`, the formal adjoint and Green concomitant remain.

Source `epsilon`, dependent `gamma_epsilon`, and the conditional soldering
identity remain three different objects. The source confirms the first two
and is silent on the third.

## What did not move

Coverage, verdict counts, residue, tightness, five scoped quotients,
P1/P2/P3, canon, claim status and public posture are unchanged. No physical
kernel, hyperbolicity, Krein positivity, contour, path-integral measure, BFV
or particle claim is promoted.

Machine truth: `lab/process/conditional-physics-ledger-v0.84.json`.

Construction result:
`selected-k77-gamma-soldered-epsilon-dupsilon-orbit-2026-08-08.md`.
