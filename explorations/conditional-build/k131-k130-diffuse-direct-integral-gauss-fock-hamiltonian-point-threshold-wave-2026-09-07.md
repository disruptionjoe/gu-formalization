---
title: "K131 K130 diffuse direct-integral Gauss/Fock Hamiltonian and point threshold wave"
status: active_research
doc_type: reverse_scaffold_diffuse_direct_integral_gauss_fock_hamiltonian_result
created: 2026-09-07
date: 2026-09-07
target_claim: INTERNAL_TARGET:K130_FOCK_COMPATIBLE_RIGGING_DIRECT_INTEGRAL_HAMILTONIAN_POINT_SUCCESSOR
claim_ceiling: exact repository-owned Lebesgue direct-integral completion of the compact circle Gauss fibres over neutral finite-particle configurations, with a nonzero positive diffuse physical pairing, measurable unitary cumulative-flux equivalence to the K128 axial carrier enlarged by eight integer global-flux sectors, exact position/local-gauge/holonomy covariance, and one transported positive closed Dirac-Coulomb form carrying the bounded cylindrical-regulator-independent fixed-width defect; the delta interaction is not uniformly form-controlled at the critical H1/2 Dirac-energy threshold, so no singular point-Fock Hamiltonian, thermodynamic scattering, interacting NESS/current, source/GU ownership, Born derivation, prediction, confirmation or holdout credit follows
manifest: lab/process/k131-k130-diffuse-direct-integral-gauss-fock-hamiltonian-point-threshold-wave.json
probe: tests/channel-swings/k131_k130_diffuse_direct_integral_gauss_fock_hamiltonian_point_threshold_probe.py
---

# K131 K130 diffuse direct-integral Gauss/Fock Hamiltonian and point threshold wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: Lebesgue direct integral of compact physical U(1)^8 Gauss fibres over neutral finite-particle configurations, its measurable axial trivialization, transported fixed-width Dirac-Coulomb Hamiltonian and critical point-trace obstruction
carrier: direct sum over neutral impurity and antisymmetric finite-particle sectors of L2 configuration wavefunctions valued in l2(Z8 global flux), with collision diagonals removed only as null sets LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Lebesgue configuration measure times counting measure on eight integer global-flux labels, carrying the transported positive Dirac-Coulomb quadratic form ON=repository_owned_diffuse_circle_physical_control
real_structure: CAR adjoint, complex spinor conjugation, Hermitian Clifford-Klein generators, integer charge conjugation and fibrewise Hilbert adjoint
grading: total fermion parity and total Z8 charge; antisymmetry is alternating equivariance under simultaneous position/species/fibre relabelling
action_owner: repository-construction
target: Fock-compatible physical pairing and cylindrical-regulator-independent fixed-width Hamiltonian, with atomic-embedding, smooth-connection, singular-point, thermodynamic and source boundaries MAP-TYPE=intertwiner
```

Scope: this packet constructs a new continuum representation on a finite
spatial circle after choosing an orientation, a cut at the impurity and one
self-adjoint one-particle Dirac boundary condition. It integrates the compact
physical Gauss fibres over Lebesgue-distributed matter configurations. It is
not a subrepresentation or Hilbert completion of K130's counting-measure
point-network carrier, not an unreduced smooth connection representation, and
not an infinite-volume reservoir theory.

## Inline preflight bookend

The rebuilt frontier contained seven substantial arcs: diffuse configuration
measure, measurable physical fibres, the axial/K128 unitary, position and gauge
covariance, global flux/holonomy, the common Hamiltonian form, the singular
point threshold, thermodynamic scattering and source ownership. The first,
covariance and point-threshold arcs were independently ready. The axial
equivalence and Hamiltonian depended on the diffuse carrier. Infinite-lead
NESS lacked an asymptotic dynamics and a uniform singular limit; source credit
lacked an authenticated coefficient-complete action owner.

Mechanism retrieval separates this result from K130's atomic cylindrical
inductive limit, K129's regular unreduced zero-kernel representation, K128's
selected trivial-open-holonomy interval quotient and K127's fixed external
connection. The route changes the base measure before group reduction rather
than pretending atomic position networks converge in counting norm. No
canonical correction entry supersedes the charge, gauge, parity, measure,
domain or representation premises used here.

The route-changing lens census covered direct integrals and measurable Hilbert
fields, Abelian lattice Gauss law, charge-network refinement, antisymmetric
Fock configuration spaces, axial/Coulomb reduction, local gauge covariance,
global electric flux and holonomy, closed quadratic forms, KLMN control,
Sobolev trace thresholds, interior-boundary/counterterm alternatives,
scattering and NESS, source fidelity and hostile philosophy of science. The
diffuse direct integral dominates another atomic embedding attempt because it
matches the measure class K130 proved necessary and has an exact fibrewise
Gauss solution.

## 1. A diffuse direct integral of compact physical fibres

Let `Sigma=S1_L`, orient it, choose a cut and impurity point `x0`, and let the
n-particle labelled configuration space be

```text
C_n={(x_1,e_1;...;x_n,e_n): x_j in Sigma, x_i != x_j},
```

with Lebesgue measure in the positions and counting measure in the eighteen
species labels. Collision diagonals have measure zero. In the impurity sector
`u`, retain exactly the neutral configurations

```text
q_u + sum_j c_(e_j)=0 in Z^8.                              (1)
```

For almost every configuration `X`, insert its points and `x0` as vertices of
the oriented circle graph. The compact physical fibre is

```text
H_X^phys = l2{ n in (Z^8)^E(X) : div n + Q_X = 0 }.        (2)
```

Equation (1) is necessary and sufficient for (2). After the cut, choose the
flux `m in Z^8` on the root edge. Cumulative charge then gives the unique
solution on every other edge. Thus the solution set is an affine `Z^8` torsor
and the root-flux map gives a fibrewise unitary

```text
T_X : H_X^phys -> l2(Z^8).                                (3)
```

The edge ordering changes only across collision diagonals. Hence `X -> T_X`
is measurable on the full-measure union of ordering chambers. The physical
space

```text
H_phys = direct-sum_(n,u) integral^direct_(C_n,neutral)
         H_X^phys dX                                     (4)
```

has the positive pairing `integral ||Psi(X)||^2 dX` and is nonzero: the root
impurity/CAR vacuum with any square-summable global-flux amplitude is a vector.
Unlike K130's point-network direct sum, singleton position projections vanish
in (4). Position multiplication is therefore diffuse and has exactly the
Lebesgue measure class required by K128.

This is a change of representation, not a repair internal to the K130
Ashtekar-Lewandowski matter carrier. There is no isometric inclusion sending a
point delta to an `L2` vector, and no claim that (4) is cylindrically embedded
in K130's atomic inductive limit.

## 2. Antisymmetry, position and gauge covariance survive

Use alternating equivariance under a simultaneous permutation of particle
positions, species and incident charge labels. The graph relabelling carries
Gauss solutions unitarily, so the antisymmetric sector of (4) is well defined;
collision diagonals require no boundary value at the Hilbert-space level.
For every bounded Borel `F(X)`, fibrewise multiplication satisfies

```text
T M_F T^-1 = M_F                                           (5)
```

on `L2(C_n) tensor l2(Z^8)`. This is the position-algebra intertwiner that the
atomic K130 carrier could not possess.

Before reduction, a local `U(1)^8` gauge function `alpha` multiplies the matter
configuration by

```text
exp(i[q_u.alpha(x0)+sum_j c_(e_j).alpha(x_j)]).            (6)
```

The compact edge character in (2) transforms by the inverse endpoint phase
because `div n+Q_X=0`. Their product is one. Thus every physical fibre is
gauge invariant and (3) intertwines the physical action with the identity,
exactly as axial reduction requires. Equation (6) is a nontrivial covariant
multiplication law on the kinematic bundle; its cancellation on (4) must not
be advertised as a nontrivial local-gauge action on the reduced space or as a
Haar measure on the smooth continuum gauge group.

Circle holonomy characters add one common `k in Z^8` to every edge flux and
therefore shift `m -> m+k` in (3). The eight global electric-flux directions
survive as `l2(Z^8)`. A large gauge transformation changes the chosen
trivialization by the corresponding integer spectral-flow relabelling; it does
not choose a theta vacuum or a physical superselection state.

## 3. The K128 carrier and fixed-width defect lift exactly

Taking the direct sum over all neutral particle numbers and species turns the
right side of (3) into the charge-neutral K128 antisymmetric CAR carrier
tensored with `l2(Z^8)`. The cumulative flux is the same electric string as
K128 equation (1), now with the global root flux retained. Therefore `T`
intertwines every bounded position multiplier and every smeared electric
multiplication operator.

For a neutral transition `e:u->v`, the K127 fixed-width CAR smearing creates
or annihilates charge at `y` while the Wilson character shifts precisely the
edges between `x0` and `y`. Under `T` this is K128's electric-string update.
The graph relabelling is an isometry, each fermionic creator/annihilator with
`f_e in L2(Sigma)` is bounded with norm `||f_e||_2`, and the finite
Clifford-Klein/impurity coefficients are bounded. Hence the finite sum `V_f`
is a bounded symmetric even operator on (4). It is genuinely continuum and
independent of a cylindrical graph regulator at every fixed nonzero smearing
width. The width remains an ultraviolet regularization, so this is not a
point field.

## 4. A transported common Dirac-Coulomb Hamiltonian

On the axial side use K128's positive one-particle operator
`h_D=(1+D*D)^(1/2)` and cumulative electric field, now allowing root flux
`m`. Define

```text
q_0[Psi] = <Psi,dGamma(h_D)Psi>
           +(1/2) sum_a g_a^2 integral ||E_a(x)Psi||^2 dx. (7)
```

For fixed `m`, K128's Pauli-counting bound controls `(N+1)^2` by the positive
Dirac form. The root-flux part is the positive multiplication form in
`|m|^2`, and the mixed term is controlled by
`2|m||Q| <= epsilon|m|^2+epsilon^-1|Q|^2`. Consequently (7) is a positive
closed form on the intersection of the Dirac and global-flux form domains.
Transporting it by `T` defines a closed physical form directly on (4), without
differentiating the chamberwise graph trivialization.

Since `V_f` is bounded and symmetric,

```text
q_f = q_0 + <Psi,V_f Psi>                                  (8)
```

is closed and semibounded on exactly `Q(q_0)`. The first representation
theorem gives a unique self-adjoint Hamiltonian. Neutrality, total fermion
parity and the global-flux/holonomy covariance reduce it. This supplies the
cylindrical-regulator-independent gauge-covariant fixed-width Hamiltonian interface left
open by K130. It does not construct a pointwise connection, prove equality
with a signed Dirac operator domain, or select the cut, boundary condition,
couplings, global sector or state from a source action.

## 5. The singular point limit fails at the critical form threshold

The form domain of the order-one positive Dirac energy is `H^(1/2)(S1)` in
each one-particle channel. In the normalized Fourier basis, evaluation at
`x0` has cutoff dual norm

```text
||delta_x0^(N)||^2_(H^-1/2)
  = (1/L) sum_(|k|<=N) (1+k^2)^(-1/2).                    (9)
```

The sum diverges logarithmically. Indeed, for `k>=1`,
`1/(sqrt(2)k) <= (1+k^2)^(-1/2) <= 1/k`; harmonic-number bounds make the
divergence explicit. Choosing Fourier coefficients proportional to the dual
weights saturates the cutoff norm, so this is not merely failure of one
mollifier: point evaluation is not continuous on `H^(1/2)`.

Accordingly a fixed nonzero coefficient multiplying the number-changing
point field has no cutoff-independent relative form bound against (7) by the
direct KLMN route. Electric-string energy tends to zero with string length and
does not cancel (9). For `H^s`, the same series converges exactly when
`s>1/2`, so the obstruction is critical and scoped: it does not exclude a
stronger ultraviolet kinetic form, a running counterterm/coupling, an
interior-boundary condition, or a separately proved norm/strong-resolvent
limit. None is constructed here.

## 6. Thermodynamic and source boundaries remain

The construction is on a compact circle and has no incoming/outgoing
reservoir morphisms. It supplies neither an infinite-lead Moller/Ruelle map nor
return to an interacting NESS. K115's reduced affinity `6561/256` therefore
remains a finite-state thermodynamic target, not a microscopic field current.
The gauge group, cut, charges, carrier, Dirac boundary condition, smearing,
couplings, global sector and state are repository-selected inputs. No actual
Weinstein/source/GU action owns them.

## Inline postflight bookend

The diffuse pairing, measurable fibre, axial unitary, covariance/global-sector
and common fixed-width Hamiltonian arcs completed. The point arc returned an
exact logarithmic `H^-1/2` divergence, narrowing the next construction to an
IBC, renormalized resolvent/counterterm route or stronger ultraviolet form.
The thermodynamic and source arcs remained outside the licensed conclusion.

- **Strongest construction:** a nonzero positive Lebesgue direct integral of
  compact Gauss fibres, unitarily equivalent to the neutral K128 axial carrier
  enlarged by eight global integer-flux directions, with a transported
  self-adjoint semibounded fixed-width Hamiltonian.
- **Strongest overclaim caught:** matching the K128 measure class does not make
  the new space a subspace or completion of K130's atomic cylindrical matter
  representation, and physical gauge invariance is not an unreduced smooth
  local-gauge representation.
- **Strongest contrary route:** an IBC or renormalized resolvent family may
  define a nontrivial point interaction despite failure of uniform KLMN form
  control; a genuinely unreduced continuum connection representation may
  encode the same quotient without the chosen measurable trivialization.
- **Weakest reproducibility seam:** the common Hamiltonian is transported from
  the axial representation; it does not prove that the chamberwise graph
  trivialization preserves a globally differentiable connection-domain
  description across collision strata.

No source action, physical preparation, detector effect, Born rule, held-out
score, prediction, confirmation, canon, paper, release or public-posture status
changes.

## Next condition

Construct an explicit singular number-changing point interaction by an
interior-boundary condition or a renormalized cutoff/resolvent limit that
survives the logarithmic `H^-1/2` divergence, and prove its compatibility with
the diffuse Gauss fibres and global-flux domain. Independently build
infinite-lead dynamics and a finite-coupling Moller/Ruelle or return-to-NESS
theorem before identifying a field current. A genuinely unreduced smooth
connection/BRST parent and an actual source/GU action selecting the imported
data remain separate requirements.

## Reproduction

```bash
python3 tests/channel-swings/k131_k130_diffuse_direct_integral_gauss_fock_hamiltonian_point_threshold_probe.py
python3 tests/channel-swings/k131_k130_diffuse_direct_integral_gauss_fock_hamiltonian_point_threshold_probe.py --selftest
```
