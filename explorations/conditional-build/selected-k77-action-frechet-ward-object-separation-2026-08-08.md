---
artifact_type: construction-result
date: 2026-08-08
status: FIRST_ACTION_BANK_ALREADY_COMPLETE__STATIONARY_WARD_THEOREM_EXACT__TRANSVERSE_J_AND_K_OPEN
source_return: SOURCE-CONFIRMS
canon_verdict_change: none
---

# The next operator was two different operators

## Result in plain English

The queue said to build the “selected-action Frechet bank.” That phrase had
collapsed two different calculations.

The first one is already done: v0.77 computed the complete pointwise first
variation of the selected K77 action over all 16,384 real `u(64,64)`
coefficient directions. Its row rank is 14, normal rank is 10, and its live
Clifford grades are 1, 2 and 5.

The second one is not done: the residual Jacobian

`J = D Upsilon`

needed by the norm-square action. At a zero-residual background its stationary
Hessian is `J^! K J`. The first-action bank is not `J`, and neither is the old
ten-by-ten metric diagnostic.

This wave removes the duplicate first calculation and narrows the remaining
build.

## What the Ward identity does and does not give

For an equivariant residual,

`D Upsilon · R_xi = rho_*(xi) Upsilon`.

At `Upsilon*=0`, the four physical diffeomorphism columns vanish. An exact
nonzero-residual plant makes the right-hand side live, so the zero is a
stationarity consequence rather than a frozen representation.

But the physical metric tangent is ten-dimensional. The already-constructed
diffeomorphism map has rank four in the timelike, spacelike and null causal
classes. Its exact transverse projector has rank six. For every class this
wave constructs nonzero rational additions `Delta J` supported on that
six-plane with

`Delta J · R = 0`.

Therefore many different full Jacobians have the same Ward columns. Proving
`J R=0` cannot construct the six transverse columns and cannot justify a
stationary Gram operator by itself.

The actual selected residual still needs a coefficientwise naturality proof;
the present theorem states what follows once that hypothesis is met. It does
not relabel representation-level naturality as the completed action
calculation.

## Corrected dependency

```text
v0.77 dI1 bank (complete)
          │ distinct from
          ▼
J = D Upsilon
  ├─ four Ward-orbit columns: fixed by naturality at Upsilon*=0
  └─ six transverse metric columns: still must be constructed
          │
          ▼
residual K + formal adjoint + Green concomitant
          │
          ▼
H2 = J^! K J and presymplectic/BFV/domain tests
```

## Signature condition absorbed without overreach

Among the two 4+10 horns, K77 has `p-q=0 mod 8` and real
ordinary Majorana--Weyl halves `64+64`; K95 has `p-q=4 mod 8`, is
quaternionic, and has no ordinary real Majorana--Weyl half. K95 can support a
symplectic-MW condition only with additional doublet structure. This selects
K77 **conditional on GU requiring an ordinary real chiral carrier without
that extra doublet**. The 2021 authorial equation
`TY^(7,7)=TX^(1,3)+N^(6,4)` supports that arena. Curt remains an expositor,
not an authorial source, and his sign-order arithmetic is not used.

The full real Clifford modules also do not have the same size:
`Cl(7,7)` acts on `R^128`, while `Cl(9,5)=M(64,H)` acts on `H^64`, real
dimension 256. What matches is the underlying complex Dirac dimension 128.
K95 remains a useful pseudoreal control. Its right-H/Clifford/Krein
objects are not imported into the K77 construction.

## Constraint accounting

- no new field, coefficient, function slot, quotient or datum;
- no residue, verdict or coverage change;
- P1/P2/P3 remain unused;
- no Einstein, spectrum, positivity, hyperbolicity, path-integral, Green,
  symplectic or BFV claim.

## Next gate

Construct the six transverse physical K77 `D_g Upsilon` columns and the
residual pairing `K`. Then derive the formal adjoint and Green concomitant
before forming the stationary Gram Hessian or making symplectic/domain claims.
