---
title: "K112 K105 equivariant stochastic branch-record and descent boundary"
status: active_research
doc_type: conditional_stochastic_branch_record_and_finite_descent_result
created: 2026-09-03
date: 2026-09-03
claim_ceiling: exact finite probability, Markov-generator, classical-record and direct-sum algebra/domain theorem on the K111 256-point vacuum orbit; a symmetric stochastic law uniformly distributes representatives and a realized eight-bit record conditionally recovers one embedded K105/K91 retract, but no source/GU stochastic law, physical environment or measurement, continuous nonlinear stochastic dynamics, spacetime/BV-BFV ownership, Born derivation, prediction or confirmation follows
manifest: lab/process/k112-k105-equivariant-stochastic-branch-record-descent-wave.json
probe: tests/channel-swings/k112_k105_equivariant_stochastic_branch_record_descent_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K112 K105 equivariant stochastic branch-record and descent boundary

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: this packet is an exact finite stochastic and classical-record control
on K111's 256-point repository-constructed vacuum orbit. It tests operational
branch distribution, record-conditioned K105/K91 descent and the invariant
observable algebra. It is not Weinstein's source action, a physical collapse
or measurement model, or a spacetime BV-BFV construction.

```gu-typed-objects
result: one S_256-equivariant stochastic law uniquely distributes K111 vacuum representatives uniformly; a simultaneous-equivariant diagonal classical record conditionally recovers one embedded K105/K91 retract while the unrecorded quotient remains branch blind
carrier: K111's transitive 256-point vacuum orbit inside the normalized K105 positive coefficient-blind seed simplex, together with a 256-valued classical record LAYER=observed CHIRALITY=N/A
pairing: classical probability pairing on the finite orbit and record, followed branchwise by K110's explicitly imported finite trace pairing ON=repository_stochastic_boundary_control
real_structure: real classical probabilities, real K105/K91 histories and real finite operational interface
grading: degree-zero branch and record variables with the inherited minimal K91 classical BV grading on each direct-sum sector
action_owner: repository-construction -- K111 quartic plus a new finite classical Markov/quench-and-record law; not authenticated as Weinstein's source action, physical noise law or measurement postulate
target: stochastic vacuum distribution, record-conditioned retract selection, direct-sum domain descent and invariant observable algebra MAP-TYPE=evaluation
```

## Inline preflight bookend

K111 closes the deterministic symmetric route: its nonlinear action has a
transitive orbit of strict minima, no invariant section and no distinguished
embedded rank-one projector. The next reverse edge is therefore not another
potential. It is the branch datum itself: can symmetry-respecting stochastic
dynamics operationally produce representatives, what must be recorded to use
one, and what descends before versus after conditioning?

The route census compared deterministic equivariant selection, a coordinate-
biased field, continuous Langevin dynamics, a finite orbit Markov law,
superselection by fiat and a quench with a correlated classical record. The
first two repeat K111 and K110. Continuous stochastic field dynamics requires
an unowned noise covariance, analytic domain and boundary law before the
finite information question is settled. The finite complete-graph generator
and diagonal record decide that question exactly. The route switches only if
the stated law is not equivariant, the uniform law is not uniquely stationary,
or conditioning fails to recover the branch.

## 1. The unique symmetric one-shot branch law

Let `M={0,...,255}` label K111's 256 strict vacua and let `S_256` act
transitively. A probability kernel from a symmetry-fixed input to `M` is
equivariant exactly when its output law is invariant. Transitivity forces all
256 point masses to agree, so normalization gives

```text
q(j)=1/256.                                                 (1)
```

There is no deterministic equivariant kernel: a point mass would require a
vacuum fixed by the whole group, and K111 proves none exists. A symmetric
stochastic law may therefore *distribute* representatives, but it cannot
prefer one before a realized sample exists.

## 2. Exact Markov dynamics on the vacuum orbit

For `kappa>0`, define the complete-graph continuous-time generator

```text
(L f)(j)=kappa sum_(k != j) (f(k)-f(j)).                    (2)
```

It commutes with every coordinate permutation. Its rows sum to zero and the
uniform law obeys detailed balance. If `f` has zero mean, then

```text
(L f)(j)=-256 kappa f(j),                                  (3)
```

so the uniform stationary law is unique and the exact spectral gap is
`256 kappa`. The dynamics erases a pre-existing branch bias rather than
creating a preferred label. Sampling at a quench time and then freezing the
label gives the symmetric one-shot law (1); freezing is an additional boundary
operation, not a consequence of (2).

## 3. The diagonal record supplies exactly the usable branch datum

Introduce a classical record `R` with the same 256 values and the joint law

```text
mu(J=j,R=r)=delta_(j,r)/256.                                (4)
```

The joint law is invariant under simultaneous permutation of branch and
record. Both marginals remain uniform, but

```text
Pr[J=j | R=r]=delta_(j,r).                                 (5)
```

Thus no coordinate is written into the law before sampling, while each
realized record conditionally identifies one representative. The exact
classical information in a perfect record is

```text
I(J;R)=log_2(256)=8 bits.                                  (6)
```

Equation (6) localizes the selection invoice. Symmetry can own the uniform
distribution; a readable outcome record owns the branch-relative embedding.
The packet constructs the conditional instrument but does not derive a
physical environment, detector, measurement or collapse law.

## 4. Conditioned projector and domain descent

Transport K111's equivariant projectors `P_j=e_j e_j^T`. Before reading the
record,

```text
E[P_J]=I_256/256,                                          (7)
```

which has rank 256. After conditioning on `R=r`,

```text
E[P_J | R=r]=P_r,                                         (8)
```

which has rank one and selects the existing K110/K91 action, Euler, domain,
Green and boundary retract for branch `r`.

Let `D=direct_sum_r D_r` be the direct sum of the already-proved branch
domains. All inherited branch operators are permutation transports of one
operator `B`, while the label generator is `L tensor I`. Therefore

```text
(L tensor I)(I tensor B)=(I tensor B)(L tensor I).          (9)
```

Constant sections descend to the abstract quotient domain; record evaluation
selects `D_r`. This is exact finite direct-sum domain compatibility, not an
analytic stochastic field domain or physical BV-BFV boundary theory.

## 5. Observable algebra and superselection record

Write the finite branch algebra as

```text
A=product_r A_0,
```

where `A_0` is K110's finite real operational interface. Permutations act by
relabeling factors. The invariant subalgebra consists exactly of constant
tuples and is canonically isomorphic to `A_0`; this recovers K111's abstract
unrecorded descent. The branch center is `R^256`, with primitive idempotents
`z_r`. Conditioning on the record is evaluation `ev_r:A->A_0`.

For any branch state `phi`, the unrecorded invariant state is

```text
bar(phi)=(1/256) sum_r phi after ev_r,
```

whereas record conditioning gives `phi after ev_r`. K110's finite interface
and calibration statistics transport identically in every factor, so reading
`R` changes the embedding but produces no new interference or Bell statistic.
The trace/state/effect and tensor rules remain K110's explicit imports. The
uniform classical group law is not a Born derivation.

## 6. Ownership and exact boundary

K112 supplies the first explicit branch mechanism after K111 that does not
pre-encode a preferred coordinate: a symmetric random law produces a
representative and a covariant classical record makes that representative
operationally usable. It also proves the exact split between unrecorded
quotient descent and recorded sector descent.

The remaining scientific debt moves to the origin of the stochastic and
record channels. No source/GU action, boundary environment, physical noise
covariance, detector, continuous nonlinear stochastic evolution, causal
locality law or spacetime BV-BFV structure owns them here. No Born rule,
held-out result, prediction, confirmation, canon or public posture follows.

## Inline postflight bookend

- **Strongest overclaim:** “symmetry breaking now derives a physical K105
  branch.” Refused. A finite classical sample and record conditionally select
  a repository branch; their physical origin is not derived.
- **Strongest contrary route:** a source-owned asymmetric boundary condition
  could select deterministically, or a physical environment could derive a
  nonuniform law. Both remain exact reopeners and would supersede the finite
  uniform control only after ownership and domain checks.
- **Strongest mistyping risk:** calling the record center a BV-BFV boundary
  algebra or calling (1) Born probability. Refused. Both are finite classical
  constructions.
- **Weakest reproducibility seam:** checking one sampled label would confuse a
  realization with covariance. The certificate checks normalization,
  stationarity, detailed balance, mean-zero spectrum, diagonal conditioning,
  projector ranks, invariant observables and hostile ownership mutations.

The standard-library certificate passes its clean baseline before every
hostile mutation. No source/GU action, physical branch law, environment,
measurement, continuous stochastic dynamics, spacetime/BV-BFV quotient, Born
derivation, holdout score, prediction, confirmation, canon, paper or public
posture moves.

## Reproduction

```bash
python3 tests/channel-swings/k112_k105_equivariant_stochastic_branch_record_descent_probe.py
python3 tests/channel-swings/k112_k105_equivariant_stochastic_branch_record_descent_probe.py --selftest
```
