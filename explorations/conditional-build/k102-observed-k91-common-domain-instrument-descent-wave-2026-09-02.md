---
title: "K102 observed K91 common-domain instrument-descent wave"
status: active_research
doc_type: reverse_scaffold_quotient_instrument_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K102_K91_COMMON_DOMAIN_KMS_INSTRUMENT_DESCENT
claim_ceiling: exact repository-owned K91 split/common-domain descent of the finite-prefix K99 reduced instrument only; no source complex, source interaction, spatial locality, tail-locality, Born, prediction, confirmation, held-out score, promotion or verdict
manifest: lab/process/k102-observed-k91-common-domain-instrument-descent-wave.json
probe: tests/channel-swings/k102_observed_k91_common_domain_instrument_descent_probe.py
---

# K102 observed K91 common-domain instrument-descent wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: K91 split-quotient/common-domain descent of the K99 finite-prefix reduced instrument
carrier: G_N direct-sum P_N with G_N=G tensor Q tensor A_N and P_N=P tensor Q tensor A_N, G=P=l2(N), finite apparatus A_N LAYER=observed CHIRALITY=N/A
pairing: l2 graph pairing tensored with imported trace/Born state-effect pairing ON=repository_owned_quotient_instrument_control
real_structure: coefficientwise conjugation and finite matrix adjoint
grading: K91 gauge/physical split, binary q label and cell number; no source BV, BFV or ghost grading
action_owner: repository-construction
target: common-domain quotient instrument descent MAP-TYPE=quotient
```

Scope: this packet composes the repository-owned K91 split quotient with each
finite K99 apparatus prefix. It does not select a source complex, interaction,
spatial net or Born pairing.

## Inline preflight bookend

The route census covered split exact sequences, closed graph domains,
invariant nuclear cores, bounded tensor interactions, CP instruments,
trace-norm convergence and gauge basicness. Retrieval found the two ingredients
separately but no typed common carrier. The selected route leaves K91's
unbounded diagonal generator on a spectator `l2` factor; this is the strongest
composition that can prove a common domain without inventing a coupling to the
mode operator.

## Split tensor quotient

For an odd finite prefix apparatus `A_N=(C^2)^tensor N` and label space `Q`,
put

```text
G_N=G tensor Q tensor A_N,       P_N=P tensor Q tensor A_N,
H_N=G_N direct-sum P_N,
d0_N(g)=(g,0),                   q_N(g,p)=p.                (1)
```

The section `j_N(p)=(0,p)` proves that (1) is split short exact and identifies
the quotient with `P_N`.

On the physical term define

```text
Omega_N=Omega tensor I_Q tensor I_A_N.                     (2)
```

Finite amplification preserves self-adjointness and closedness, with domain
`D(Omega_N)=D(Omega) tensor Q tensor A_N` in the natural finite direct-sum
sense. Its invariant nuclear core is `s_N=s tensor Q tensor A_N`.

Every K99 controlled flip acts only on `Q tensor A_N`, and every majority
effect only on `A_N`. They strongly commute with (2), preserve its closed
domain, and preserve `s_N`. This is the common-domain owner; an interaction
that acted on the `l2` mode factor would require a new commutator/domain proof.

## Cochain and instrument descent

Lift a physical prefix unitary by

```text
U_tilde_N=I_G_N direct-sum U_N.
```

Then `q_N U_tilde_N=U_N q_N` and `U_tilde_N d0_N=d0_N`. Lift each physical
effect by zero on the gauge term. It annihilates `im d0_N`, so outcome
probabilities and post-measurement physical states are representative
independent.

Appending the common Gibbs prefix, applying the controlled flips, measuring
majority and tracing `A_N` gives

```text
Phi_y,N = id_P tensor I_y,N^Q.                             (3)
```

Therefore the K99 error `e_N` is also the induced trace-map norm error on the
fixed quotient system `P tensor Q`; spectator amplification does not change
it. The maps converge to `id_P` tensored with the projective q instrument.

## Owner accounting and maximum conclusion

Repository-owned: split amplification, common closed domain/core, cochain
lift, basic effects, factorization (3) and uniform reduced convergence.
Imported: the tensor split, K91 generator, K99 stream and flips, majority
effect, apparatus trace, q prior, trace/Born pairing and record semantics.

The maximum conclusion is compatibility: the reduced stream instrument can be
made exactly K91-basic on one common repository domain. The construction does
not select its interaction, and K101 proves that faithful modular data alone
cannot provide that missing selector under the stated naturality type.

## Inline postflight bookend

- Strongest overclaim: calling the spectator tensor construction a source
  functional complex or local causal measurement.
- Strongest contrary construction: a mode-dependent interaction can fail to
  preserve `D(Omega)` or `s`.
- Weakest reproducibility seam: finite controls certify algebraic identities;
  analytic closedness/core claims use finite amplification of K91's proof.

The probe checks exactness, domain commutation, descent, instrument formulas
and all scope fences. Delayed choice remains reserved and unscored.

## Next condition

Replace the spectator construction with a source/action-selected interaction
that preserves a source quotient/domain and carries source causal support.
