# Conditional physics ledger v0.117

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.117.json`.

## Progress

`Ledger v0.117 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Six rows move in distance/evidence only. No verdict, residue, quotient, datum,
canon or posture changes.

## What v0.117 adds

The v0.116 boundary carrier is now protected by an operator-admission gate.
The source action has a safe covariantly reduced derivative grammar
`(g,varpi,epsilon)=(2,1,1)`, with Euler-block upper bound
`[[4,3,3],[3,2,2],[3,2,2]]`. This is the unique componentwise-minimal
symmetric Douglis--Nirenberg weight, not an actual principal-symbol theorem.

The complete action-owned gauge-fixed operator remains unbuilt. In particular,
the earlier rank-91 epsilon cross and 34-field Gram must be recomputed on both
current stationary branches and across the retained parent family. Gauge
fixing, bulk ghosts, field-Riesz/covector graph calculus and a closed
ultrahyperbolic realization remain absent. H7/H8 stays a strong kinematic
boundary target rather than an operator-derived graph trace.

## Frontier

Conditions closed: `2`; conditions opened: `0`; remaining named: `6`.

Next construct the branch- and parent-indexed full source-variable Hessian
principal coefficients. Only then add gauge fixing/ghosts and test the
`Dmax/Dmin` exact sequence.

P1/P2/P3 remain unused. The selected Spin parent, two `U(32,32)` halves and
full `U(64,64)` comparator remain distinct.
