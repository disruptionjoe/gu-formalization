---
artifact_type: conditional_physics_ledger_release
created: 2026-08-12
ledger_version: "0.191"
run_id: RUN-20260812-020740-gu-split-layer-commutant-action-parent
---

# Conditional physics ledger v0.191

Ledger v0.191 — 82/82 mapped; 32 SAME · 19 DIFFERS · 26 NEEDS · 5
OVER-DETERMINED. Headline counts and residue do not move. The split-layer
complex structure and the exact relation between the real and complex Weyl
halves are now decided.

The complete real commutant of `Spin(1,3) x Spin(6,4)` on the exact real-128
Clifford carrier has dimension four and is `C + C`. Each real Weyl half has
dimension 64 and a native square-minus-one endomorphism, hence complex
dimension 32; complexification produces conjugate `32+32` pieces. This is the
exact dimensional content behind the source's two `C^(32,32)` halves.

The result does not derive the same-half Hermitian `(32,32)` form and does not
show that the action selects the full `U(64,64)` parent, its block-preserving
`U(32,32) x U(32,32)` subgroup, or the finer `J`-preserving observation
reduction. Those three conditions are now typed separately. Nonhomogeneous
reduced curvature and full second-fundamental data also remain live.

Residue stays at 84 booked continuous real parameters, at least 19
function-valued slots and nine discrete forks, with five scoped quotients.
No field, datum, frame, flag or quotient was added. P1/P2/P3 remain unchanged
and unused. Five rows migrate in distance/evidence only.

Machine-readable ledger:
`lab/process/conditional-physics-ledger-v0.191.json`.
