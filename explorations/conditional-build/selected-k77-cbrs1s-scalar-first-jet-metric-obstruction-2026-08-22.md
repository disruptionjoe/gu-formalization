---
title: "Selected-K77 CBRS-1S scalar first-jet metric obstruction"
status: active_research
doc_type: exact_covariant_scalar_first_jet_obstruction
created: "2026-08-22"
registry: lab/process/selected-k77-cbrs1s-scalar-first-jet-metric-obstruction.json
probe: tests/channel-swings/selected_k77_cbrs1s_scalar_first_jet_metric_obstruction_probe.py
grade: "EXACT RECONSTRUCTION-GRADE POINTWISE FORMAL-JET AND COMPLETE-SYMBOL OBSTRUCTION; NO SOURCE SCALAR, GLOBAL VACUUM OR SPECTRUM"
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_ACTION_PRIMITIVE_EPSILON_AND_METX_GRAMMAR__REPOSITORY_FREEZES_THE_COVARIANT_EVEN_SCALAR_FIRST_JET_OWNER_AND_DERIVES_THE_OBSTRUCTION__SOURCE_SILENT_ON_THE_SCALAR_AND_CLASS
canon_verdict_change: none
---

# Selected-K77 CBRS-1S scalar first-jet metric obstruction

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: CBRS-1S exact covariant even-scalar formal-jet and complete-symbol metric obstruction
carrier: complete 230650-dimensional real CBRS-1P T-plus-independent-Spin-connection carrier extended by one repository-constructed real Grassmann-even scalar field and its Lorentzian base jets LAYER=toy CHIRALITY=N/A
pairing: selected K77 action-density pairing plus the Lorentzian inverse-base-metric scalar kinetic pairing and unit symmetric double-well potential ON=pointwise_scalar_jet_extended_carrier
real_structure: normalized real K77 J4 radical branches with a real Lorentzian scalar value first covector and d'Alembertian jet
grading: Clifford and form grades inherited unchanged; phi is Z2-even and its base first jet is not a K77 Spin connection or Grassmann-odd fermion
action_owner: repository-construction
target: real formal two-jet body stationarity complete Hessian symbol and full intrinsic MET(X) tensor MAP-TYPE=evaluation
```

## Result first

CBRS-1S freezes the minimal covariant derivative extension of the CBRS-1R
even owner before evaluating any J4 density:

```text
L_S = C3(T) + phi^2 Q2(T)
      + (1/2) g^{mu nu} partial_mu(phi) partial_nu(phi)
      + (phi^2-1)^2/4.
```

Here `g` is the Lorentzian base metric and `phi` is one real Grassmann-even
base scalar. The unit kinetic coefficient, `phi^2 Q2` coupling and unit
double-well potential are fixed for the whole class. No J4 radical, action
density, desired Hessian zero or metric component sets a coefficient.

All four J4 bodies have exact real pointwise formal two-jet continuations at

```text
T = T0,       phi = 1,       p = d phi != 0,
box(phi) = 6 I0,
```

where `I0=C3(T0)+Q2(T0)`. The field and scalar Euler rows vanish. The complete
`T` plus independent Spin-connection Hessian is inherited, and adding the
scalar gives the exact Schur symbol

```text
g^{-1}(xi,xi) + 30 I0 + 2.
```

At zero covector it is nonzero on both radical families, so every complete
`230651`-fiber has rank `230611`, nullity `40`, and kernel exactly equal to the
broken diagonal-Spin gauge orbit. Lorentzian signature also gives one real
scalar characteristic quadric per branch, where nullity rises to `41`; that
extra direction is a scalar characteristic, not gauge.

The full intrinsic metric equation kills the nonhomogeneous class before a
global PDE, stabilizer or spectrum is licensed. Its trace-free scalar-gradient
term is

```text
p_mu p_nu - (p^2/4) g_mu nu.
```

This tensor is nonzero for every nonzero covector `p` in four dimensions. In
the frozen class the inherited J4 graph-visible connection momentum is zero,
while the action density and potential change only the trace, so no other
trace-free owner is present to cancel it. Thus the four formal
field-plus-scalar jets are not four metric-stationary backgrounds: this
one-new-gradient extension of the inherited J4 body yields zero fully
metric-stationary bodies with nonzero scalar gradient.

## Frozen construction and Layer 0

The construction is the ordinary covariant real-scalar kinetic owner on the
base, attached to the repository-selected K77 first-action split
`C3+Q2`. It is deliberately not attributed to Weinstein. In particular:

- `phi` is not the source's `ad`-valued connection perturbation;
- `d phi` is a base covector, not an independent Spin-grade-two K77
  connection coefficient;
- the double well is a repository-frozen potential, not evidence for a
  source-native Higgs mechanism; and
- a pointwise formal jet is not a global section, analytic solution, vacuum
  state or observed scalar.

The scope is the selected K77 pointwise J4 action bodies extended by this one
repository-owned scalar jet. The result binds that class only.

## Exact formal body stationarity

For every nonzero J4 critical body `T0`, Euler homogeneity gives

```text
Q2(T0)=3 I0,       C3(T0)=-2 I0.
```

With `phi=1`, the complete `T` Euler covector is exactly the original
`C3+Q2` covector and therefore vanishes. The scalar Euler equation is

```text
-box(phi) + 2 phi Q2(T) + phi(phi^2-1) = 0.
```

It closes at `box(phi)=6I0`. For the two radical families,

```text
normal J4: box(phi) = 0.1988951311129...,
base J4:   box(phi) = -1.0622827165603....
```

Both are real. A prescribed finite real two-jet is locally realizable in
normal coordinates; the d'Alembertian value is the scalar Euler completion,
not an independently varied multiplier. This certifies local formal body
stationarity only. It neither supplies global integrability nor evades the
metric equation below.

## Complete tangent symbol

At `T=T0`, `phi=1`, the complete `T`/connection block is the CBRS-1P Hessian.
It has dimension `230650`, rank `230610`, nullity `40`, and kernel exactly the
broken diagonal-Spin orbit. The mixed scalar column is `2 grad Q2(T0)`.
Using

```text
H_B(T0) T0 = -grad Q2(T0),
Q2(T0)=3I0,
```

the quotient Schur term from the mixed column is exact, and the full scalar
symbol becomes

```text
Sigma_phi(xi) = g^{-1}(xi,xi) + 30 I0 + 2.
```

At `xi=0`, its values are

```text
normal J4:  2.9944756555645...,
base J4:   -3.3114135828019...,
```

both nonzero. Hence the zero-covector rank is `230611` and nullity is `40` on
all four bodies. Because a Lorentzian quadratic form represents every real
number, `g^{-1}(xi,xi)=-(30I0+2)` has a real covector solution in both
families. On that scalar characteristic, rank returns to `230610` and nullity
is `41`. The additional null direction is the expected differential scalar
characteristic. It does not enlarge the gauge orbit or create a
metric-admissible background by itself.

## Full intrinsic metric obstruction

The scalar Hilbert variation contains the gradient dyad and an isotropic
density term. At the inherited J4 body the pre-existing graph-visible
connection momentum is zero. The remaining trace-free part is independent of
`C3`, `Q2`, the potential, and `box(phi)`:

```text
E_g^TF = p tensor p - (p^2/4) g.
```

Suppose this vanished for nonzero `p`. If `p^2` were nonzero, the left dyad
would have rank one while `(p^2/4)g` would have rank four. Therefore `p^2`
would have to vanish, but then the equation would reduce to `p tensor p=0`,
which forces `p=0`. Contradiction. The argument covers timelike, spacelike and
null gradients and is independent of the J4 action density.

The exact probe plants `p=(1,0,0,0)` in signature `(1,3)`. The dyad has rank
one, while its trace-free projection is traceless, full-rank and nonzero. All
four formal two-jets therefore fail the complete intrinsic metric equation
before its trace component is even considered.

## Retrieval, controls and hostile review

Mechanism-level retrieval found the CBRS-1R ultralocal condensate, older
conventional Higgs/VEV comparators, scalaron auxiliaries, nonhomogeneous K77
first-jet campaigns and stress-tensor reconstructions. None contains this
frozen single-scalar extension together with the exact inherited complete
Schur symbol and the dimension-four trace-free rank obstruction. The older
objects retain their recorded types and scopes.

- **Strongest overclaim:** four real formal two-jets are not four local
  metric-stationary solutions. Their first-jet stress already violates
  `MET(X)`.
- **Strongest contrary construction:** an action-owned nonzero T/connection
  metric graph, a top form, or a sufficiently typed internally indefinite
  coframe can add a distinct trace-free or isotropic stress image. This result
  freezes those owners out and does not exclude their jointly stationary
  classes.
- **Strongest trace trap:** checking only the metric trace reports zero for
  `E_g^TF` by definition and misses the entire obstruction.
- **Strongest collapse trap:** setting `d phi=0` removes the materially new
  derivative owner and returns to an ultralocal class.
- **Strongest Euler trap:** choosing `box(phi)` closes the scalar Euler row but
  cannot change the first-jet Hilbert stress.
- **Strongest real-form trap:** Euclideanizing the base changes the frozen
  Lorentzian owner; the exact Lorentzian symbol and obstruction must not be
  reported as a Euclidean computation.
- **Weakest reproducibility seam:** the large tangent result imports the
  exact CBRS-1P kernel theorem. The probe separately checks the homogeneity
  identity, both radical Schur values, the real scalar characteristics and
  the full metric tensor plant.

No ledger verdict, canon, source ownership, residue, particle assignment,
prediction, confirmation or public posture changes.

## Reverse-scaffold consequence

Close the single-scalar first-jet owner. It genuinely introduces a differential
non-orbit characteristic, and all four J4 bodies admit real pointwise formal
two-jets, but every nonzero scalar gradient has an unavoidable trace-free
metric image. The class cannot supply CBRS-1's fully stationary
nonhomogeneous background.

Continue with `CBRS-1T`: before solving, freeze the smallest target-blind
**isotropizing** even derivative owner. The route fork is between an action-
owned top-form/flux class and a fully typed internally indefinite coframe-like
owner; select it structurally before reading a J4 density. Require real body
stationarity, complete gauge quotient and `MET(X)` variation, and do not tune
a continuous flux, internal frame, potential coefficient or sector separately
to any J4 branch. Do not advance to CBRS-2.

Reproduce with:

```bash
sage -python \
  tests/channel-swings/selected_k77_cbrs1s_scalar_first_jet_metric_obstruction_probe.py
```

The exact probe passes `53/53` after native propagation.
