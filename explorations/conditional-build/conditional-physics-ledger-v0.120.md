# Conditional physics ledger v0.120

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.120.json`.

## Progress

`Ledger v0.120 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Six rows move in distance/evidence only. No verdict, residue, quotient, datum,
canon or posture changes.

## What v0.120 adds

The lower-order primitive-epsilon derivative of the raw residual is now exact
on both K77 source-stationary branches.  On `B=b Phi1`, `T=t Phi1`, it is

```text
[-b+360(b+t)^2] [Phi1,eta].
```

Its two positive conjugate branch coefficients retain rank `91` on all 91
selected Spin directions.  Residual zero does not mean its Jacobian is zero.

The fixed-`varpi` metric block was already owned by v0.95.  Both branches
satisfy its `Upsilon*=0` premise, so its transverse rank `6` ports for
timelike, spacelike and null symbols.  The earlier "missing metric block"
label was composition debt, not missing mathematics.

The raw residual, first transgression packet, integrated first-action epsilon
Euler and live endpoint momentum remain distinct.  The selected Spin carrier,
two `U(32,32)` halves and full `U(64,64)` comparator also remain distinct.

## Frontier

Conditions closed: `2`; conditions opened: `0`; remaining named: `2`.

Next construct the complete first-action Hessian and expanded-parent pairings,
keeping the first and residual-square actions separate.  Then complete gauge
fixing and ghosts before testing `Dmax/Dmin`, Green/Krein realization and
coupled BV-BFV.

P1/P2/P3 remain unused.
