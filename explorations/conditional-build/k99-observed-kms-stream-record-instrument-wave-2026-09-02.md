---
title: "K99 observed KMS-stream record-instrument wave"
status: active_research
doc_type: reverse_scaffold_kms_stream_record_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K98_POSITIVE_TEMPERATURE_KMS_INPUT_ASYMPTOTIC_RECORD_INSTRUMENT
claim_ceiling: exact positive-temperature product-KMS repeated-interaction control whose odd-block majority instrument converges to the projective q record; tensor stream, controlled flips, energy scale, readout, record meaning and trace/Born pairing remain supplied, with no autonomous equilibrium measurement, source, prediction, confirmation, held-out score, promotion or verdict
manifest: lab/process/k99-observed-kms-stream-record-instrument-wave.json
probe: tests/channel-swings/k99_observed_kms_stream_record_instrument_probe.py
---

# K99 observed KMS-stream record-instrument wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: odd-block majority readout on a repeated-interaction thermal spin stream converges to a projective q instrument
carrier: C2_q tensor infinite tensor product of C2 thermal cells, used through finite prefixes LAYER=observed CHIRALITY=N/A
pairing: imported trace/Born state-effect pairing on q and diagonal cell readout ON=repository_owned_kms_stream_control
real_structure: occupation-basis conjugation and matrix adjoint
grading: q label and cell excitation number; no source BV, BFV or ghost grading
action_owner: repository-construction
target: positive-temperature KMS-input asymptotic record instrument MAP-TYPE=evaluation
```

Scope: this result binds a commuting on-site spin stream and a supplied
sequential controlled-flip interaction. It does not bind a source field,
continuum bath, autonomous Hamiltonian apparatus or physical Born owner.

## Inline preflight bookend

The route census covered product KMS states, repeated interactions, binary
hypothesis testing, likelihood ratios, majority effects, exact binomial tails,
Hoeffding bounds, instrument coherences, work cost, tail algebras, autonomous
versus driven dynamics, source custody and Born semantics. Retrieval found
K98's common vacuum input and its finite-temperature reference-state packet,
but no instrument whose actual common input is a positive-temperature KMS
state. The decisive route is a thermal stream: accept noisy cells and derive
asymptotic exactness by repeated independent records rather than reusing the
vacuum half-line.

## One common positive-temperature KMS input

Let one cell have Hamiltonian `h=epsilon |1><1|` and choose
`beta epsilon=log 3`. Its Gibbs state is

```text
tau_beta = diag(3/4,1/4).                                   (1)
```

For the commuting stream Hamiltonian, every finite restriction of
`Tau_beta=tau_beta^(tensor infinity)` is Gibbs and the compatible product
state is the algebraic beta-KMS state. Both `q` branches receive this same
state. For cells `j=1,...,N`, apply the supplied interactions

```text
U_j=P0 tensor I_j + P1 tensor X_j.                          (2)
```

They commute on distinct cells. Conditional on `q=0`, an observed excitation
bit is Bernoulli `1/4`; conditional on `q=1`, it is Bernoulli `3/4`.

## Exact majority instrument

Take `N=2m+1` and let outcome one mean more than `m` excited cells. The common
error probability is

```text
e_N = sum_{k=m+1}^N C(N,k)(1/4)^k(3/4)^(N-k).              (3)
```

The diagonal majority projection makes every cross-label coefficient vanish:
`tr(M tau_beta^N X^N)=0`. Hence the two operations on an arbitrary label state
are

```text
I0_N(rho)=(1-e_N)P0 rho P0 + e_N P1 rho P1,
I1_N(rho)=e_N P0 rho P0 + (1-e_N)P1 rho P1.                (4)
```

They are completely positive and trace preserving in sum. Hoeffding gives
`e_N <= exp(-N/8)`, so (4) converges in finite-dimensional map norm to the
projective `q` instrument. Exact first values are
`e_1=1/4`, `e_3=5/32`, and `e_5=53/512`. For the imported benchmark weights
`(2/3,1/3)`, mismatch is still `e_N` and the outcome-one weight is
`1/3+e_N/3`.

The nonselective map dephases `q` after the first cell. This is a property of
the supplied controlled flip and diagonal thermal state, not a derivation of
physical collapse. The limit uses an unbounded number of fresh cells and
driven interactions; it is not a stationary finite apparatus.

## Owner accounting and maximum conclusion

Repository-owned: the exact finite-prefix Gibbs calculation, the binomial
instrument, its off-diagonal cancellation, CP/trace identities, exact errors
and exponential convergence. Imported: the tensor stream, energy scale,
fresh-cell preparation, interaction schedule, controlled flip, majority
readout, record semantics, `q` prior and trace/Born pairing. Source-selected
owner count is zero.

The maximum conclusion is an exact constructed counterexample to the claim
that positive temperature itself prevents an asymptotically sharp record:
one genuine KMS input plus an unlimited driven stream can realize it. This
does not show that equilibrium, KMS, detailed balance or GU selects the stream
or pays its cost.

## Inline postflight bookend

- Strongest overclaim: calling the product-KMS input an autonomous thermal
  measurement. The repeated flips, fresh stream and readout are supplied.
- Strongest contrary construction: at `beta epsilon=0`, both hypotheses are
  Bernoulli `1/2` and no block size reduces error below `1/2`.
- Weakest reproducibility seam: finite binomial samples do not prove the
  asymptotic statement; the analytic Hoeffding bound carries that burden.

The exact probe runs positive controls first, certifies the finite identities
and analytic bound, and mutation-tests the ownership fences. Delayed-choice
entanglement swapping remains reserved and unscored. No source, continuum,
microlocal, derived-Born, prediction, confirmation, canon, paper or promotion
status moves.

## Next condition

Derive the stream factorization, repeated coupling, energy supply and record
effect from one authenticated source-owned causal action, or replace the
driven stream by an autonomous finite-work construction with an identified
tail algebra and physical probability owner.
