---
title: "Complete DIFFERS-cohort disposition: five named requirements and five bounded impossibilities"
status: active_research
doc_type: result
created: "2026-08-24"
registry: lab/process/differs-cohort-row-disposition-wave.json
probe: tests/channel-swings/differs_cohort_row_disposition_wave_probe.py
grade: "EXACT CORPUS AND TYPE DISPOSITION; FIVE B2_NAMED_REQUIREMENT TERMINALS AND FIVE PRECISE_IMPOSSIBILITY TERMINALS; NO LEDGER, SOURCE, CANON OR PUBLIC-POSTURE MOVEMENT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "The five impossibilities bind only named comparator or realization classes. SC-CHI-51 and every source-native escape remain open."
canon_verdict_change: none
---

# Complete DIFFERS-cohort row-disposition wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: terminal disposition of the ten open DIFFERS rows at their exact comparator realization or missing physical-owner ceiling
carrier: conditional C1 gauge and fermion content; observed C2 spectrum; fixed equivariant mass-splitting route; local anomaly lattice; inflow and Spin-bordism count comparators LAYER=ambient+observed BRIDGE=named-disposition-seams CHIRALITY=S-CHIRALITY-UNTYPED
pairing: representation branching and physical quotient; degree-16 anomaly functional; free and torsion inflow; Spin bordism ON=declared-carriers
real_structure: K77 real total carrier with separately typed observed complex chiral shadow; comparator Spin carrier
grading: representation degree chirality anomaly degree and primary torsion grading
action_owner: repository-construction; named requirements retain action stabilizer mass BV/BFV domain reality spectrum and observation owners; no owner is executed here
target: RA-A4 RA-A5 RA-B6 RA-D2 RA-G1 RA-G2 AC-A5 AC-F3 AC-F4 AC-G2 MAP-TYPE=evaluation
```

## Result first

All ten open `DIFFERS` rows now have evidence-backed terminal dispositions,
without treating `DIFFERS` itself as evidence and without changing
conditional-physics ledger v0.263.

- Five rows become exact `B2_NAMED_REQUIREMENT` terminals: `RA-A4` and
  `RA-A5` name the post-Higgs stabilizer, mass-operator and physical-spectrum
  owners; `RA-B6` names a common neutral operator, reality/charge map, spectrum
  and mixing evaluation; `RA-G1` names the RS physical quotient, domain and
  spectrum; `RA-G2` names the SG4-bit-2 action owner, BFV domain and positive
  mirror-free cohomology.
- Five rows become bounded `PRECISE_IMPOSSIBILITY` terminals. `RA-D2` excludes
  only the fixed equivariant mass-splitting realization. `AC-A5` excludes net
  chirality zero as a sufficient local-anomaly condition. `AC-F3` excludes a
  locally vanishing bulk anomaly as a nonzero 3-primary inflow carrier.
  `AC-F4` excludes ordinary Spin-bordism torsion as a 3-primary count carrier.
  `AC-G2` excludes the old gauge-octic horn as a necessary premise for the
  fork-independent local conclusion.

The source-native `2+1`, boundary/BV, replacement Shiab, coupled-functional,
framed/String and physical-decoupling routes are preserved. No impossibility
is a GU verdict.

## Preflight bookend: typing and route choice

Layer 0 separated raw representation content from the observed physical
spectrum, the balanced total carrier from an effectively chiral quotient, the
free anomaly polynomial from torsion bordism, and the old K95 gauge-octic horn
from the settled K77 local rank theorem. The route census rejected three
shortcuts: declaring every `DIFFERS` row B4, reconstructing source-action
owners while B2 is ineligible, and auditing only the already-killed anomaly
rows. The selected route covers the complete cohort and accepts only a typed
impossibility or an exact named owner.

The input-currency pass checked the correction registry. In particular,
`RA-G2` consumes the corrected four-corner/SG4-bit-2 reading rather than the
superseded bare-mass claim; `RA-D2` retains the correction that the source
claim survives named realization failures; and `AC-F3` is fenced from the
source's explicitly non-chiral `2+1` construction.

## Named-requirement packet

| row | exact requirements | what is not claimed |
| --- | --- | --- |
| `RA-A4` | `XU1-1` action-selected post-Higgs stabilizer; `XU1-2` extra-U(1) mass operator; `XU1-3` observed BRST spectrum | raw branching is not an observed extra photon |
| `RA-A5` | `EXG-1` mass operator on all 106 extra directions; `EXG-2` gauge/BFV quotient; `EXG-3` observed spectrum | a dimension census is not a physical exotic spectrum |
| `RA-B6` | `NU-1` common neutral operator and reality/charge map; `NU-2` spectrum; `NU-3` mixing/decoupling map | a forced neutral representation is not an observed sterile state |
| `RA-G1` | `RS-1` Witten/BRST exit; `RS-2` common closed physical domain; `RS-3` quotient spectrum/decoupling | pullback content is not the physical spin-3/2 spectrum |
| `RA-G2` | `MIR-1` source/action SG4-bit-2 selector owner; `MIR-2` proper BFV/domain; `MIR-3` positive physical cohomology without mirror states | the four-corner carrier neither proves nor disproves physical mirrors |

Each requirement is a terminal disposition of the row, not execution of the
named owner. The six completed neutrino literature subclaims remain
parent-linked evidence; this packet closes `RA-B6` only by naming what the
construction still requires.

## Precise-impossibility packet

`PI-EQUIVARIANT-MASS-SPLIT-CHIRALITY-1` binds the fixed equivariant
mass-splitting class on the K77 balanced carrier. Bare `q` leaks outside the
RS image, Pin completion exchanges the candidate half with its mirror, and
both degree-duality primalizers miss the ordinary port. Its escape is a
genuinely different source-owned Shiab/restricted port or coupled functional;
`SC-CHI-51` remains `NOT_KILLED`.

`PI-NET-CHIRALITY-ALONE-LOCAL-ANOMALY-1` binds integer content vectors in the
15-slot conditional C1 arena. The exact degree-16 system has rank five and a
rank-ten kernel: `W=0` supplies one condition and four independent anomaly
rows remain. A witness satisfies `W=0` and violates the remainder. The escape
is a selected content vector in the full kernel.

`PI-LOCAL-ZERO-ANOMALY-INFLOW-Z3-BRIDGE-1` binds the free inflow column of a
locally consistent bulk. Its anomaly polynomial is identically zero, so it
cannot carry a nonzero 3-primary count. A non-inflow framed/String/boundary
bridge remains open.

`PI-SPIN-BORDISM-3PRIMARY-COUNT-1` binds ordinary Spin torsion: it is
2-primary, so no homomorphism from `Z/3` can land nontrivially in it. Changing
the tangential structure and constructing a nonzero class is the explicit
escape.

`PI-GAUGE-OCTIC-PREMISE-NECESSITY-1` binds only the claim that the old
K95/right-H gauge-octic flip is necessary for local cancellation. The premise
does not transfer to K77, while the independent local rank theorem already
rescues the conclusion. A source/action change making weights slot-dependent
is the resurrection condition.

## Postflight bookend and hostile review

- **Strongest overclaim:** “the DIFFERS rows are false predictions of GU.”
  Refused. Five rows merely name missing physical owners; five impossibilities
  exclude exact comparator or realization classes with explicit escapes.
- **Strongest contrary route:** the source-native observation/BV/SG4-bit-2
  route may remove raw extra content or separate observed halves. Preserved.
- **Weakest reproducibility seam:** the packet relies on corpus-backed exact
  results rather than rerunning every historical computation. The new probe
  therefore checks every source pointer, class, assumption, escape, named
  requirement and protected hash and plants mutations across all five
  disposition types.
- **Propagation seam:** only disposition evidence and branch steering move.
  Ledger v0.263, source ownership, prediction, confirmation, claims, canon and
  public posture remain byte-identical.

The measurable effect is ten additional row terminals. The live denominator
becomes 59 terminal and 32 open rows of 91, with `exhausted=false` and
`b2_selectable=false`. The next frontier is the three `OVER_DETERMINED` stale-
premise forks and the highest-fanout remaining `NEEDS` rows. No Joe
intervention is required.

Reproduce with:

```bash
python3 tests/channel-swings/differs_cohort_row_disposition_wave_probe.py --selftest
```
