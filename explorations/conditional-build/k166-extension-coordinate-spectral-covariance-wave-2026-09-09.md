---
title: "K166 extension-coordinate spectral covariance wave"
status: active_research
doc_type: conditional_scalar_extension_generalized_spectral_residual_gap_and_absolute_floor_covariance_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned extension-coordinate covariance theorem for the supplied K139 signed point control; the scalar finite extension direction gives R_E=R_0+E M and translates the complete generalized spectrum, Rayleigh values and absolute floors while preserving matched residual covectors, spectral gaps, the M-orthogonal complement and counts at translated thresholds; the theorem does not select E or the non-scalar extension, and the K155 control is finite only, so no native absolute K152 interval, physical/source selection, Born derivation, prediction or confirmation follows
manifest: lab/process/k166-extension-coordinate-spectral-covariance-wave.json
solver: tests/channel-swings/k166_extension_coordinate_spectral_covariance.py
probe: tests/channel-swings/k166_extension_coordinate_spectral_covariance_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K166 extension-coordinate spectral covariance wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: this packet stays on K139--K165's repository-supplied positive
particle/hole signed point control at fixed polarization, couplings,
non-scalar Hermitian extension, chart, domain and state. It classifies the
remaining scalar finite extension coordinate. It does not select any physical
input or identify K155's finite matrices with K162's continuum anchors.

```gu-typed-objects
result: the scalar finite extension coordinate acts as R_E=R_0+E M on the exact K139 generalized pencil, translating absolute generalized spectral locations while preserving matched residuals, gaps, the M-orthogonal complement and count certificates at translated thresholds
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), with K162 dyadic cylinder ranges in the K139 regular chart and K155 finite charge blocks used only as exact controls LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing; in regular coordinates M=S* S is the physical Gram and the scalar physical identity pulls back to E M ON=repository_signed_point_control
real_structure: CAR adjoint, real scalar extension, momentum conjugation and the complete K162 signed flavor intertwiner; exact controls use real symmetric generalized forms
grading: conserved incidence charges q_i=n_i+N_i+-N_i-, hard-core impurity degree, dyadic level, generalized-pencil inertia, relative spectral gaps and absolute energy coordinate
action_owner: repository-construction -- neither a measured complete response nor Weinstein's source or a GU action selects E, the non-scalar extension, polarization, couplings, domain or state
target: separate coordinate-invariant relative form residual and gap data from absolute coercivity count-threshold and left-floor placement before K164 and K152 MAP-TYPE=intertwiner
```

## Inline preflight bookend

K165 requests evaluation of a combined fixed K139 form on a small K162 trial
space, including a native floor above `-5`. Mechanism retrieval changes the
work list. K138 already proves that finite Hermitian boundary subtractions act
affinely on `W`; K140 proves that a complete full-row-rank complex response
would identify `W`, but supplies no response datum; K155 writes the scalar
part explicitly as `E_R I`. No prior packet propagates that scalar direction
through K165's corrected generalized pencil or separates its invariant and
absolute spectral consequences.

The route census compared direct K162 quadrature, generalized Ritz/Schur,
boundary-triple Weyl functions, response inversion, min--max translation,
form-dual residuals, Lehmann--Goerisch and source/action selection. The affine
identity is the cheapest route-changing discriminator. A precise numerical
anchor at the convention `E_R=0` would still not place a physical spectrum on
an absolute energy axis. Conversely, the same identity may preserve enough
relative information to keep residual and gap construction executable.

`SC-META-53` remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain
`NEEDS`. The v0.263 ledger remains 88/88 mapped with `33 SAME / 22 DIFFERS /
31 NEEDS / 2 OVER_DETERMINED`. No source or ledger row changes.

## 1. The scalar extension pulls back to the physical metric

Fix every non-scalar K139 input and let the scalar finite extension coordinate
enter the physical operator as

```text
H_E=H_0+E I.                                             (1)
```

Write `S=U^-1` for the fixed boundary chart. Its regular form and physical
metric are

```text
R_E=S* H_E S,               M=S* S.                     (2)
```

Therefore, before any finite compression,

```text
R_E=R_0+E M,
R_E-zM=R_0-(z-E)M.                                      (3)
```

This is not ordinary isospectrality of the nonunitary congruence. It is exact
covariance of the generalized pencil. The complete generalized spectrum obeys

```text
spec(R_E,M)=E+spec(R_0,M).                              (4)
```

The scalar coordinate is a physical identity shift in the supplied
realization. Calling `E=0` a convenient reference does not select that
reference physically.

## 2. Counts and complements transform with the threshold

For every threshold `b`, equation (3) gives

```text
inertia(R_E-(b+E)M)=inertia(R_0-bM).                   (5)
```

Thus counts are invariant only when the threshold is translated with the
coordinate. Holding the numerical threshold fixed asks a different physical
question.

The physical complement of a trial space depends only on `M`, so its
`M`-orthogonal subspace is unchanged. On that same complement,

```text
Q_M*(R_E-(b+E)M)Q_M=Q_M*(R_0-bM)Q_M.                  (6)
```

K165's generalized Schur certificate therefore transports exactly between
coordinates, including nonzero trial/complement coupling, but only at the
translated threshold.

The exact rational K165 control has one value below `b=0`, complement inertia
`(0,0,2)` and full inertia `(1,0,2)`. At `E=7`, the identical statement holds
below `b=7`; the matrices in (5)--(6) are literally unchanged.

## 3. Relative residuals and gaps survive

For a fixed trial vector `u`, put

```text
rho_E=<u,R_Eu>/<u,Mu>=rho_0+E.                         (7)
```

Then the matched residual covector is exactly invariant:

```text
(R_E-rho_E M)u=(R_0-rho_0 M)u.                        (8)
```

Any form-dual norm using the same coordinate-independent reference is
therefore unchanged. Differences of generalized eigenvalues are also
unchanged by (4). K162 form/Gram evaluation, a matched K152 residual and a
relative next-distinct spectral gap remain useful work at a declared
reference coordinate.

This does not make all K152 data coordinate free. An absolute coercivity
statement with fixed shift, an absolute count threshold and a native left
floor translate with `E`. If a reference-coordinate floor is `ell`, the
physical floor is `ell+E`. For the exact control `ell=-4`, the equally allowed
coordinates `E=-2` and `E=0` place the floor at `-6` and `-4`, on opposite
sides of the requested `-5` threshold. No argument about the relative form can
choose between them.

## 4. Exact complete finite control

The solver evaluates K155's entire `q=(0,0)` two-mode charge block, dimension
`84`, at extension coordinates `0` and `7`. With its exact rational inverse
chart it verifies entry by entry

```text
R_7-R_0=7 U^(-*)U^-1=7M.                              (9)
```

The trial control has physical Gram `2`, Rayleigh values `-1` and `6`, and
the same residual covector `(0,2,1)` in both coordinates. This is an exact
algebraic control for (3), (7) and (8). It remains K155's arbitrary finite
regulator matrix, not a native K162 continuum anchor.

## 5. K152 replay

The scalar-coordinate split sharpens rather than closes the next task:

- evaluate the fixed-reference K139/K162 form and physical Gram;
- certify the matched form-dual residual and relative generalized spectral
  gap;
- state every count certificate at a covariantly translated threshold; and
- obtain a complete response datum or an action-owned full Hermitian
  extension before asserting any absolute physical interval or floor.

K140's response formula is a valid selection interface when the channel map
has full row rank, but the repository supplies no measured response. A source
or GU action also does not select the extension. Therefore K152 remains closed
to an **absolute native** interval, while its relative form/residual work
remains executable.

## Inline postflight bookend

- **Strongest advance:** the unselected scalar extension is now propagated
  exactly through the generalized form, spectrum, counts and floors.
- **Strongest preserved route:** matched residual covectors, relative gaps and
  `M`-orthogonal complement certificates survive unchanged at translated
  thresholds, so the numerical/form construction is narrowed rather than
  discarded.
- **Strongest overclaim:** “choose `E_R=0` and prove a native floor above
  `-5`.” Refused; that is a coordinate convention without physical selection.
- **Strongest contrary route:** an actual complete full-rank response datum or
  an action-owned extension would select `E` and restore absolute placement.
  Neither is present.
- **Weakest reproducibility seam:** a count may look unchanged if its threshold
  translation is omitted. The solver compares the full pencil matrices, not
  only reported eigenvalue lists.

All five admitted mathematical arcs were attempted inline. The affine
identity, pencil/count covariance, complement and residual invariance,
absolute-floor nonidentifiability and finite K155 control closed. No external
response acquisition, source adjudication, or second writer was used.

## Next condition

Evaluate the K139/K162 combined form and Gram at a declared reference scalar
coordinate, certify the matched form-dual residual and relative generalized
spectral gap, and carry K165's complete `M`-orthogonal complement certificate
at a translated threshold. In parallel, obtain a complete full-rank response
datum or an action-owned full Hermitian extension before placing the result on
an absolute physical energy axis. Do not treat `E_R=0`, a finite K155/K157
matrix, an essential edge, or a K161 tail as the missing native selection or
absolute floor.

## Reproduction

```bash
python3 tests/channel-swings/k166_extension_coordinate_spectral_covariance.py --demo
python3 tests/channel-swings/k166_extension_coordinate_spectral_covariance_probe.py
python3 tests/channel-swings/k166_extension_coordinate_spectral_covariance_probe.py --selftest
```
