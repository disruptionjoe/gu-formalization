# Conditional physics ledger v0.76

## Meter

```text
Ledger v0.76 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 5 scoped
```

## Migration from v0.75

The actual local selected-action coefficient owner has now been constructed.
Exact differentiation of the same `comm/symi/symi` action on the admitted
real-K77 `Cl1 + Cl2` tangent produces a rank-fourteen `E_B-E_T` bank.  Its ten
metric-fibre normal rows are all nonzero and independent.  A complete `4+10`
observation equation dual has an explicit inverse, preserves the full bank,
and retains rank ten on the observed normal image; naive pullback still loses
the normal receiver.

The program-native scalar Clifford coefficient form is nondegenerate on both
the raw and observed normal images, with exact inertias `(4,6,0)` and
`(5,5,0)`.  Opposite endpoint orientations preserve rank and the pairing, so
the existing independent two-endpoint dressing can accept the selected action
covector locally.  No fitted `K`, current, field or external datum was added.

This result is deliberately narrow.  The 105-dimensional low-grade tangent is
not the full `U(64,64)` coefficient algebra; the scalar Clifford pairing is
not yet the full analytic Krein domain; and the complete local graph receiver
is not the global physical observation section.  Full coefficient/bundle
extension, global `tau_A0`/BFV moment map and common Green/Krein domain remain
open.

Five rows migrate in distance, evidence and mapping grade: `LT-GR1`,
`LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`.  Verdicts, coverage, residue,
forks, five scoped quotients, P1/P2/P3, canon and public posture are unchanged.

## Frontier

```text
headline_delta: NONE
frontier_conditions_closed: 3
frontier_conditions_opened: 1
remaining_named_conditions: 2
```

Next extend the exact selected action bank to the full coefficient and bundle
carrier, prove overlap naturality for the physical observation section, and
then assemble the global `tau_A0`/BFV moment map and common Green/Krein domain.
Keep the coupled nonzero-fermion residual and the separate
`I2B <-> ||II||^2` map out of this local bank claim.

Machine truth: `lab/process/conditional-physics-ledger-v0.76.json`.

