---
title: "K168 flavor-symmetric reference-extension wave"
status: active_research
doc_type: conditional_complete_C3_non_scalar_reference_extension_form_order_residual_gap_and_complement_margin_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned declaration W_ref=diag(-2,1,1) on the native C3 hard-core impurity, with fixed K139 chart, charge/domain/flavor compatibility and complete order R_0-2M <= R_ref <= R_0+M; every generalized eigenvalue moves within [-2,1], while relative gaps, matched residuals and complement margins carry a three-unit shape-oscillation budget; the missing native base form, Gram, residual, gap, complement and left floor are not evaluated, finite matrices remain controls, and no physical extension selection, K152 interval, Born derivation, prediction or confirmation follows
manifest: lab/process/k168-flavor-symmetric-reference-extension-wave.json
solver: tests/channel-swings/k168_flavor_symmetric_reference_extension.py
probe: tests/channel-swings/k168_flavor_symmetric_reference_extension_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K168 flavor-symmetric reference-extension wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: this packet stays on K139--K167's repository-supplied positive
particle/hole signed point control and native hard-core C3 impurity. It
declares one complete conditional non-scalar extension and proves what follows
from that declaration before the still-missing limiting base-form evaluation.
Its exact finite matrices are controls, not native K162 anchors.

```gu-typed-objects
result: the primitive trace-zero flavor-symmetric reference W_ref=diag(-2,1,1) is a complete bounded extension on the native hard-core C3 impurity; it preserves both charges, K162 domains and signed flavor transport, and under the fixed K139 chart obeys R_0-2M <= R_ref <= R_0+M, giving sharp three-unit shape-oscillation budgets without supplying the missing base form or complete K152 data
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), with K139 inverse-chart domain and K162 dyadic cylinder ranges in q=(0,0),(1,0),(0,1) LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing pulled back by the fixed K139 chart S=(1-G_256)^-1 to M=S* S; finite rational K155 charge-block matrices are controls only ON=repository_signed_point_control
real_structure: CAR adjoint, momentum conjugation and the real diagonal Hermitian reference W_ref; the two occupied impurity flavors have equal extension value
grading: conserved incidence charges q_i=n_i+N_i+-N_i-, hard-core impurity degree, dyadic level, generalized-pencil order, residual covectors and M-orthogonal complement margins
action_owner: repository-construction -- the unit shape normalization, scalar center, polarization, couplings, domain and state are not selected by complete response data, Weinstein's source or a GU action
target: close the complete-reference-extension input and derive exact chart-independent stability budgets while refusing to relabel a finite control or form-order bound as the unevaluated native K162/K152 packet MAP-TYPE=intertwiner
```

## Inline preflight bookend

K167 proves that K166's covariance is restricted to the scalar center and
requires one complete declared non-scalar extension before any relative K162
certificate. Object-level retrieval found K138's affine Hermitian extension
torsor, K139's fixed inverse chart, K151's native C3 charge blocks, K153's
physical Gram, K162's dyadic domains and K165's complete-complement contract.
It found no declared complete C3 reference shape or evaluated native limiting
base-form matrix. K140 supplies a response-identifiability interface but no
response datum. No correction-registry entry changes those inputs.

The route census compared unrestricted Hermitian choice, flavor-equivariant
commutants, charge-preserving diagonal shapes, response inversion, direct
quadrature, K139 chart pullback, min--max form order, Kato--Temple residuals,
generalized Schur/Feshbach complements, finite regulator controls and
form-level Lehmann--Goerisch. A primitive symmetry-preserving declaration and
its exact order consequences come first: they close a necessary input and
quantify the margin later computation must beat without pretending to supply
the absent complete form.

`SC-META-53` remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain
`NEEDS`. The source register and v0.263 physics ledger do not move.

## 1. One complete native C3 reference shape

Use the hard-core impurity basis

```text
|0> = impurity vacuum,       |1>=d_1*|0>,       |2>=d_2*|0>.
```

Declare

```text
W_ref = diag(-2,1,1).                                      (1)
```

Equation (1) is complete, Hermitian, non-scalar and trace zero. It is the
primitive integer normalization of the one-dimensional trace-zero diagonal
subspace fixed by flavor exchange `|1><->|2>`. The word *primitive* fixes a
repository reference unit; it does not claim nature selects its magnitude.
Multiplying (1) by another real shape coordinate would be another conditional
reference.

Because (1) is diagonal in impurity occupation, it preserves the hard-core
subspace and commutes with both conserved charges

```text
q_i=n_i+N_(i,+)-N_(i,-).                                  (2)
```

Its equal values on `|1>` and `|2>` make it commute with the complete signed
flavor swap. It is bounded, so it preserves the K139 form domain and every
K162 dyadic trial domain. Thus the declaration closes exactly the extension,
domain, charge and flavor-transport inputs; it does not evaluate the rest of
the operator.

## 2. Fixed-chart form order

Fix K139/K153's chart and metric

```text
S=(1-G_256)^-1,          M=S* S.                         (3)
```

Let `R_0` denote the same limiting regular form at zero extension. The
declared reference contributes

```text
Delta R = S* W_ref S,    R_ref=R_0+Delta R.             (4)
```

Since the physical eigenvalues of `W_ref` are `-2,1,1`, congruence by the
boundedly invertible `S` gives the exact complete-form inequalities

```text
-2 M <= Delta R <= M,
R_0-2M <= R_ref <= R_0+M.                               (5)
```

Equation (5) holds on the complete K139 form domain and after compression to
every K162 trial space. It does not depend on a finite rediscretization.
Generalized min--max therefore gives, for every ordered discrete value to
which the comparison applies,

```text
lambda_k(R_0,M)-2 <= lambda_k(R_ref,M)
                    <= lambda_k(R_0,M)+1.              (6)
```

The interval is sharp from the two eigenspaces of (1). A scalar center may be
added later through K166's exact translation; it is deliberately zero in the
reference shape so shape and center remain separate.

## 3. Residual, gap and complement budgets

For an `M`-normalized regular-coordinate trial `u`, put `psi=Su` and let
`theta=<psi,W_ref psi>/<psi,psi>`. Then `theta` lies in `[-2,1]` and

```text
||(Delta R-theta M)u||_(M^-1)
 =||(W_ref-theta)psi|| <=3 ||u||_M.                    (7)
```

Thus three is a rigorous matched shape-residual increment budget, not a
computed residual. From (6), a relative adjacent gap can change by at most
three. Likewise, on any `M`-orthogonal complement, subtracting a threshold
that follows the trial Rayleigh value can reduce the shifted complement
margin by as much as the spectral oscillation

```text
osc(W_ref)=1-(-2)=3.                                   (8)
```

Consequently a base relative gap or complete complement margin strictly
larger than three survives this reference perturbation by a direct order
argument. A margin at most three does not certify positivity and must be
recomputed on `R_ref`. K167's shape-dependence obstruction is therefore not
evaded; K168 turns it into a quantitative acceptance threshold.

## 4. Bare seeds are not dressed K139 values

On the bare impurity seeds, (1) gives

```text
<Omega,W_ref Omega>=-2,
<d_i*Omega,W_ref d_i*Omega>=1.                         (9)
```

The native K153 trials are instead `psi=S phi`. The non-scalar reference does
not commute with the K139 boundary map `G_256`, which changes impurity
occupation together with bath occupation. Therefore the dressed extension
Rayleigh values are not generally the bare numbers in (9); only the rigorous
interval `[-2,1]` follows before the chart matrix elements are evaluated.

The exact finite K155 `q=(0,0)` block of dimension 84 verifies
`R_ref=R_0+S*W_ref S`, both semidefinite slacks in (5), noncommutation with the
boundary map and the signed flavor intertwiner. This is an algebraic hostile
control. K163 already proves why independently rebuilt finite matrices cannot
be promoted to the same limiting K162 form.

## 5. Native K152 replay

Two K167 fields are now complete:

1. the complete reference extension (1); and
2. the fixed chart (3).

Six native fields remain absent:

1. evaluation of the same limiting base form `R_0` on a small K162 space;
2. evaluation of its physical Gram `M` there;
3. a complete form-dual residual for `R_ref`;
4. a relative generalized gap;
5. positivity on the complete `M`-orthogonal complement; and
6. a native left floor.

The form order (5) constrains those values but does not manufacture them. In
particular, (7) is an upper budget on the shape contribution, not the complete
residual, and (8) is a sufficient margin rule, not a proved complement floor.
No ground count or K152 interval is emitted.

## Inline postflight bookend

- **Strongest construction:** one complete, bounded, charge-preserving and
  flavor-symmetric non-scalar C3 extension is now declared at fixed unit shape.
- **Strongest exact consequence:** its full K139 pullback lies between `-2M`
  and `M`, so every generalized value moves within `[-2,1]`.
- **Strongest certification threshold:** relative gaps, matched residuals and
  trial-following complement margins carry the exact three-unit oscillation
  budget.
- **Strongest overclaim:** “the bare vacuum value `-2` is the dressed K153
  trial value.” Refused; `W_ref` does not commute with the boundary chart.
- **Strongest contrary route:** complete full-rank response data or an
  action-owned extension could select a different shape and normalization.
  Neither is supplied.
- **Weakest reproducibility seam:** a semidefinite form-order control can be
  mistaken for evaluation of `R_0`, `M` or the complete complement. Those six
  missing fields remain explicit.

All five admitted mathematical arcs were attempted inline: declaration,
charge/domain/flavor compatibility, exact form order and spectral budgets,
bare-versus-dressed seed separation, and the finite hostile compiler. The
complete base-form certificate remains dependency-blocked, while physical
selection remains authority-blocked. No source or external action was used.

## Next condition

Evaluate the same limiting K139 base form `R_0` and physical Gram `M` on a
small K162 dyadic trial space. Add the K168 reference contribution, compute a
complete form-dual residual and relative generalized gap, and prove positivity
on the complete `M`-orthogonal complement plus a native left floor. Use the
three-unit oscillation threshold when transferring a base gap or complement
margin. Keep every result conditional until complete response or action-owned
evidence selects both extension shape and scalar center.

## Reproduction

```bash
python3 tests/channel-swings/k168_flavor_symmetric_reference_extension.py --demo
python3 tests/channel-swings/k168_flavor_symmetric_reference_extension_probe.py
python3 tests/channel-swings/k168_flavor_symmetric_reference_extension_probe.py --selftest
```
