---
title: "K99 observed KMS tail-work accounting wave"
status: active_research
doc_type: reverse_scaffold_kms_tail_work_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K99_KMS_STREAM_TAIL_RECORD_WORK_OWNER
claim_ceiling: exact work, free-energy, relative-entropy and tail-magnetization accounting for the K99 product-KMS stream; the limiting record requires an infinite fresh-cell resource and supplied tensor/time/readout structure, with no finite-work autonomous measurement, source, Born, prediction, confirmation, held-out score, promotion or verdict
manifest: lab/process/k99-observed-kms-tail-work-accounting-wave.json
probe: tests/channel-swings/k99_observed_kms_tail_work_accounting_probe.py
---

# K99 observed KMS tail-work accounting wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact thermodynamic and tail-algebra ownership boundary for the K99 positive-temperature record stream
carrier: infinite product of two-level Gibbs cells with finite-prefix Hamiltonians LAYER=observed CHIRALITY=N/A
pairing: imported product-state expectation and tail empirical-average pairing ON=repository_owned_kms_stream_control
real_structure: occupation-basis conjugation and matrix adjoint
grading: cell excitation number and q-conditioned product sector; no source BV, BFV or ghost grading
action_owner: repository-construction
target: KMS-stream work cost, relative entropy and asymptotic tail record MAP-TYPE=evaluation
```

Scope: this packet accounts for the same supplied stream as the companion K99
instrument packet. It isolates which resource becomes unbounded when the
finite-temperature record becomes exact.

## Inline preflight bookend

The route census covered Gibbs free energy, passivity, relative entropy,
Landauer-style accounting, infinite product states, strong laws, tail events,
quasilocal versus GNS observables, finite-support unitaries and disjointness.
Retrieval found K98 fixed-local forgetting but no exact tail event or work law
for a thermal record. The selected structural route derives a per-cell identity
first and then lets additivity expose the asymptotic resource; a numerical
entropy simulation was rejected as weaker.

## Exact per-cell identity

With `tau_0=diag(3/4,1/4)` and `tau_1=X tau_0 X=diag(1/4,3/4)`, the controlled
flip changes one cell's mean energy by

```text
Delta E = epsilon(3/4-1/4)=epsilon/2.                       (1)
```

The two states have the same entropy. Therefore `Delta F=Delta E`, and

```text
D(tau_1||tau_0)
 = (1/4)log(1/3)+(3/4)log 3
 = (1/2)log 3
 = beta Delta E.                                           (2)
```

For `N` cells, energy, free-energy difference and relative entropy all add:
`Delta E_N=N epsilon/2` and
`D(tau_1^N||tau_0^N)=N log(3)/2=beta Delta E_N`. Thus the same
limit that drives the majority error to zero consumes unbounded supplied work
in the `q=1` branch. This is accounting, not a universal minimum-work theorem
for every measurement architecture.

## Tail record and topology

Let `Z=|0><0|-|1><1|` and
`M_N=N^{-1} sum_{j=1}^N Z_j`. The product strong law gives

```text
M_N -> +1/2  under Tau_0,
M_N -> -1/2  under Tau_1.                                  (3)
```

The tail event `lim M_N<0` therefore has probabilities zero and one in the two
infinite product sectors. It is the asymptotic record selected by the supplied
cell tensor structure and empirical observable. It is not an element of any
fixed finite local algebra. A finite-support unitary changes only finitely many
summands and cannot change either limit in (3); consequently it cannot create
the exact tail record from the equilibrium sector. The two product states are
globally disjoint even though every finite-prefix experiment has nonzero error.

## Owner accounting and maximum conclusion

Repository-owned: equations (1)--(3), additivity, finite-support invariance of
the empirical limit and the explicit connection between vanishing error and
diverging resource. Imported: the product decomposition, fresh-cell supply,
Hamiltonian, controlled flip, time order, empirical readout, tail semantics and
state-effect/Born pairing. No source owner is acquired.

The maximum conclusion is an exact topology-and-resource boundary for this
model: the positive-temperature record lives in a growing/tail algebra and its
perfect limit carries linearly divergent work and relative entropy. It is not
an autonomous KMS return, global thermalization theorem, universal Landauer
bound or GU-native measurement.

## Inline postflight bookend

- Strongest overclaim: calling `beta Delta E=D` a universal measurement-cost
  theorem. It is exact only for the named flipped Gibbs cell.
- Strongest contrary construction: finite temperature at `beta epsilon=0`
  makes `Delta E=D=0` and the two product states identical.
- Weakest reproducibility seam: tail separation uses the product strong law;
  finite samples merely corroborate its exact mean/variance premises.

The exact probe certifies rational moment, energy and additivity identities and
mutation-tests all ownership fences. Delayed-choice remains reserved and
unscored. No source, Born, prediction, confirmation, canon, paper or promotion
status moves.

## Next condition

Produce an autonomous local dynamics that creates a stable record from a
single admitted finite-temperature reservoir with finite supplied work, or
derive the growing/tail observable and its probability pairing from source
causal structure rather than importing them.
