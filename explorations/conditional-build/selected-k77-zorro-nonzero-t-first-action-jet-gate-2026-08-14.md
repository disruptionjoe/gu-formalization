---
artifact_type: exact_nonzero_t_first_action_jet_admission
created: 2026-08-14
status: SELECTED_K77_CANONICAL_ZORRO_NONZERO_T_FIRST_ACTION_BIANCHI_JET_ADMITTED__SOURCE_METRIC_EPSILON_COMPLETION_OPEN
lane_id: SRC-RES-COH-01
source_claims: [SC-ACT-01, SC-ACT-04, SC-ACT-05]
probe: tests/channel-swings/selected_k77_zorro_nonzero_t_first_action_jet_probe.py
registry: lab/process/selected-k77-zorro-nonzero-t-first-action-jet-gate.json
hostile_review: lab/process/hostile-reviews/2026-08-14-selected-k77-zorro-nonzero-t-first-action-jet-gate-review.md
canon_verdict_change: none
ledger_row_changes: none
---

# Selected-K77 canonical-Zorro nonzero-T first-action jet gate

## Result first

A genuinely new canonical-`B_Z`, nonzero-`T` formal branch passes the printed
residual, the true selected transgression-action translation Euler row and all
inherited Bianchi rows exactly.

Let

```text
C = Phi1 wedge Phi1,
T = t Phi1,
DT = -F_BZ + q(t) C + Q,
q(t) = -t/312-t^2,
```

where `Q_(r;k)^ij=Q_(k;r)^ij` is the symmetric grade-two first-jet freedom.
Since `S(C)=312 Hodge(Phi1)`, the endpoint curvature is

```text
F_varpi = F_BZ+DT+T^2 = -(t/312)C,
```

and the source-printed translation residual vanishes identically.

The prior symmetric-`DT` action map has `196` rows and rank `195`. Thus its
cokernel is **one-dimensional**; the exact certificate happens to have support
on fourteen invariant cells. Projecting the complete nonzero-`T` action Euler
target onto that quotient gives

```text
28392 t^2+91 t-351 = 0.
```

Its discriminant is `39,870,649`, positive and nonsquare, so it has two real
nonzero amplitudes

```text
t_+ = (-91+sqrt(39870649))/56784,
t_- = (-91-sqrt(39870649))/56784.
```

At either root, the target reduces to the same rational vector. One explicit
solution of the complete `196+5,096` row system uses only thirteen of the
`9,555` symmetric variables:

```text
Q_(0,a)^0a = -9/56,  a in {1,2,3,10},
Q_(0,a)^0a =  5/56,  a in {4,5,6,7,8,9,11,12,13},
all other Q = 0.
```

Every true action Euler row and every inherited Bianchi row then vanishes
exactly. This is the first canonical-Zorro nonzero-`T` candidate to pass the
true action/Bianchi jet gate. It supersedes neither the exclusion of the two
old homogeneous `B=b Phi1` branches nor the zero-`T` obstruction.

## Exact derivation

Write the true translation Euler at this grade as

```text
E_T = E_F
    + q(t) [ (1/2)S(C)+Comp(C) ]
    + t^2 S(C)+t Hodge(Phi1),
```

where `E_F` is the already-certified canonical contribution with
`DT=-F_BZ`, and `Comp(C)` is the formal-adjoint derivative term generated by
the antisymmetric representative `partial_r T_k=C_rk/2`.

If `lambda` spans the action cokernel, the exact predecessor normalization is

```text
lambda(E_F)                = -117/4,
lambda(linear t term)      = 91/12,
lambda(quadratic t^2 term) = 2366.
```

Multiplication by twelve gives the displayed branch polynomial. Modulo that
polynomial the target has no coefficient of `t`; therefore the same rational
`Q` works for both algebraic embeddings. Direct multiplication certifies

```text
A Q + E_T = 0,
Bianchi(Q) = 0.
```

No numerical rank threshold, floating root or fitted tolerance is used.

## Direct metric partial and honest stop

The branch is not yet a complete stationary source background. Three exact
density contractions are

```text
<Phi1,S(F_BZ)> = -54,
<Phi1,S(C)>    = 4368,
<Phi1,*Phi1>   = 14.
```

Consequently the first-action density on either branch is

```text
I1 = -t(27+728 t^2).
```

This polynomial is coprime to the branch polynomial, so the direct rank-one
gimmel-volume partial is nonzero at both roots. That is not yet the total
fixed-`varpi` metric Euler. Only `E_T` has been closed; away from independent
`B` criticality, metric lift terms can contain `E_B-E_T`, moving Shiab,
Hodge/frame and observation graph derivatives.

The same distinction applies to primitive epsilon. The source-owned identity

```text
E_epsilon = D_B^!(E_B-E_T)+(D_epsilon S)^!K_S
```

types the next calculation, but neither term has yet been evaluated on this
nonhomogeneous algebraic jet. Calling the live direct volume partial a metric
no-go, or calling action/Bianchi closure a full stationary background, would
skip the required source graph.

## Disposition and next gate

The branch is `NOT-YET-FALSIFIED` at canonical-Zorro local formal
action/Bianchi grade. `SR-1` remains `BACKGROUND-MISSING`, and `SR-2` remains
blocked.

Next compute `E_B` and the moving-Shiab primitive-epsilon plus fixed-`varpi`
metric/observation graph on this exact two-root witness. If those rows close,
continue to the full formal-integrability/Spencer tower on the same carrier.
If they do not, this branch is killed at the first failing owned row.

The source owns the field and action grammar, not the algebraic amplitudes or
the thirteen-cell correction. No open solution, full deformation complex,
positive physical cohomology, superposition rule, Born rule, spectrum or
empirical prediction follows. No ledger, canon, residue, quotient, datum,
scheduled-priority or public-posture change is licensed.

Reproduce with:

```bash
sage -python tests/channel-swings/selected_k77_zorro_nonzero_t_first_action_jet_probe.py
```

The exact probe passes `40/40`.
