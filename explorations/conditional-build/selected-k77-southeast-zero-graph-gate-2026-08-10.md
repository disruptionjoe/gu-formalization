---
title: "Selected K77 southeast-zero graph and lower-left gate"
status: exact_scoped_negative_result
date: 2026-08-10
canon_verdict_change: none
---

# Selected K77 southeast-zero graph and lower-left gate

## Outcome

The zero-form port found in v0.138 supplies exactly one candidate graph map,
but the complete action-tied graph equation rejects it.

On each of the moving-Spin, two-`U(32,32)`-half and full-`U(64,64)` witness
classes, and for both the proposed `W` and its ASD mirror:

```text
projected port rank             128
unique graph-map rank            64
upper graph residual rank         0
induced W/mirror action rank       0
lower-left restriction rank       64
complete lower residual rank       64
```

The result repeats exactly over `GF(1000033)` and `QQ(i)`. Flipping the
displayed lower-left sign leaves rank `64`; suppressing the lower-left block
fakes closure and is rejected because equation 9.16 ties that block to the
port through the fermion bilinear.

Thus the restricted-parent adapter hope revived by v0.138 dies for the
**current q-repaired conditional rival**. W and its mirror remain exactly
indistinguishable.

## Layer 0 and source scope

Write the reduced rolled operator as

\[
  D=\begin{pmatrix}A&B\\ C&0\end{pmatrix}
\]

on `Omega1(S) + Omega0(S)`, and let `P` project onto W or its mirror. The
source's two one-form half-spinors sum to `Omega1(S)` and its two zero-form
half-spinors sum to `Omega0(S)`, so this reduced two-by-two carrier is the
direct-sum assembly of the four unbarred fields.

But the tested `A` remains the repository's q-repaired total-grading rival.
The primary source labels the fields by ambient half-spinor, while the current
construction reproduces the derivative cells only after a one-form relabel.
That source-sign collision remains open. Therefore this result is not a
refutation of equation 9.16, GU, or every member of the Shiab family.

Source return:

- `SOURCE-CONFIRMS`: the displayed candidate has southeast zero, the
  lower-left cells are minus formal-adjoint partners of the port cells, and
  the source admits an unspecified nonzero southeast rival.
- `SOURCE-CORRECTS`: none.
- `SOURCE-SILENT`: a W graph adapter, a source-faithful resolution of the
  ambient-half-sign collision, BV cohomology and a physical domain.

## Exact graph theorem

For a graph `Gamma_G={(w,Gw):w in W}`, invariance requires

\[
 (1-P)(A+BG)|_W=0,
 \qquad
 C|_W = G H,
 \qquad
 H=P(A+BG)|_W.
\]

At the unique v0.138 ratio, `(1-P)B` has full column rank `128`, so the upper
equation has at most one solution. Exact row-minor inversion constructs it;
its rank is `64`, and the upper residual vanishes. Direct substitution then
gives `H=0`. The complete K77 form-index adjoint gives `rank(C|_W)=64`, hence
the lower equation cannot hold.

There is a stronger same-carrier byproduct. For a lower-right endomorphism
`D_0`, the lower equation would be `C|_W + D_0 G = 0`. But

```text
rank(G) = 64,
rank([G ; C|W]) = 128.
```

So the row space of `C|_W` is not contained in the row space of `G`; no
endomorphism acting only after this same graph can repair the fixed
`A/B/C` construction. This scopes the source-admitted southeast rival: it
must change more than inserting a postcomposition on the present carrier, or
it fails here too.

## Efficient specialist pre-assessment

1. **Layer-0 semantics — ACTUAL MATH, very high.** Preserve the distinction
   between the q-repaired rival and a source-faithful ambient-half operator.
2. **Prior-art archaeology — ACTUAL MATH, very high.** Reuse v0.138's exact
   port and the August 4 complete K77 adjoint.
3. **Clifford/representation theory — ACTUAL MATH, high.** Keep W, mirror and
   the three parent witness classes separate.
4. **Linear-systems/control theory — ACTUAL MATH, high.** Treat the graph as
   a coupled Riccati problem; upper image inclusion is only its first row.
5. **Variational bicomplex — ACTUAL MATH, high.** The lower-left block is
   action-tied and cannot be deleted to fit the graph.
6. **Symplectic/BV-BFV — ACTUAL MATH, high.** Graph invariance is still not
   cohomology or a reduced phase space.
7. **Krein/operator theory — ACTUAL MATH, high.** Use the full form-index
   adjoint, not Euclidean transpose, and infer no domain or spectrum.
8. **Source history — ACTUAL MATH, high.** A negative on the conditional rival
   cannot be promoted across the unresolved page-46/page-51 sign collision.
9. **Exact-computation engineering — ACTUAL MATH, high.** Repeat over a finite
   field and Gaussian rationals with sign and suppressed-block plants.
10. **Adversarial scope — ACTUAL MATH, very high.** The highest-risk overclaim
    is "equation 9.16 fails"; the artifact proves a narrower statement.

## Constraint surplus

The upper equation has zero freedom: its projected port is injective. The
lower equation adds a rank-64 obstruction, so the displayed southeast-zero
graph is over-constrained rather than under-fitted. An arbitrary same-carrier
lower-right postcomposition also fails because its reachable row space is
disjoint at the required grade.

No external datum can repair this without owning a new arrow. A bit or scalar
cannot turn a rank-64 missing map into the required lower-left identity.

## What moves

Closed negatively:

> The existing q-repaired equation-9.16 rival can preserve W or its mirror by
> adding the v0.138 zero-form graph on a restricted parent.

Still open:

- a source-faithful parity/degree-duality repair of the derivative grammar;
- a different source-family Shiab contraction or additional moving odd tensor;
- an enlarged carrier/adapter whose lower row is not a postcomposition of the
  present graph;
- BV cohomology and domains only after an algebraic carrier survives.

Ledger v0.139 moves six distance/evidence fields only. Coverage, verdicts,
residue, five scoped quotients, P1/P2/P3, canon verdicts and public posture do
not move.

## Next gate

Resolve or kill the source-faithful ambient-half-sign and degree-duality
collision. Classify the admissible native Shiab parities and degree-dependent
row dualities without relabeling the source fields. Only a surviving operator
returns to W/mirror graph, BV or domain work.
