# Conditional physics ledger v0.93

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.93.json`.

## Progress

`Ledger v0.93 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient, with a newly explicit
  `84..86` range by action parent; `>=19` function-valued slots and `9` open
  discrete forks
- scoped quotients ranked: `5`

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.93 adds

The selected K77 residual carrier `Cl1 + Cl2 + Cl5` has complex dimension
`2,107` and is exactly closed under `Spin(7,7)`. It is not closed under either
large-group reading raised by the source:

| Parent symmetry | smallest complex invariant carrier | pairing coordinates |
|---|---:|---:|
| Spin-native selected action | 2,107 | 3 |
| `U(32,32) x U(32,32)` preserving two Weyl halves | 16,382 | 3 |
| same plus a separately justified chirality exchange | 16,382 | 2 |
| full `U(64,64)` mixing the halves | 16,383 | 1 |

Three explicit block-even generators send the selected grades to `3`, `4`
and `7`. Thus full-group weight uniqueness never applied to the selected
truncation itself; it belongs to an expanded parent action.

The source statements “two `C^(32,32)` Weyl halves” and “principal group
`U(64,64)`” are compatible but do not select which residual action parent is
operative after observation. The primary build therefore proceeds
conditionally on the Spin-native selected carrier at the canonical scalar-
Clifford-trace point, while the 16,382/16,383 parents remain explicit rivals.

## Residue typing

The already-booked overall source normalization remains one of the 84. The
new exact parent-dependent relative-weight burden is:

- full-`U(64,64)` parent: `+0`;
- block product plus chirality exchange: `+1`;
- block product or intrinsic Spin-native parent: `+2`.

No parent is source-selected, so one number is not falsely booked. The ledger
now reports the honest `84..86` range. Fixing the equal-weight trace point is a
conditional construction, not a fitted parameter and not a derivation.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `2`; conditions opened: `1`.

Closed:

1. exact invariant carrier dimensions under Spin, Weyl-block and full-U;
2. exact invariant-pairing coordinate counts for all three parents.

Opened but typed: which action parent and observation/symmetry-breaking map the
source intends.

Remaining named conditions on the primary selected route: `2`.

1. Construct lower-order complete physical K77 `D_g Upsilon` and verify
   coefficientwise naturality.
2. Construct the formal adjoint, Green concomitant and common analytic/
   symplectic domain.

P1/P2/P3 remain unchanged and unused. No canon verdict, claim status, public
posture, Curt-track status or third-lane status changes.

## Evidence

- `explorations/conditional-build/selected-k77-operative-pairing-symmetry-closure-2026-08-08.md`
- `lab/process/selected-k77-operative-pairing-symmetry-closure.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-operative-pairing-symmetry-closure-review.md`
