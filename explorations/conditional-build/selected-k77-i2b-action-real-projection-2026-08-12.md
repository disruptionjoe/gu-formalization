---
artifact_type: exact_conditional_variational_result
created: 2026-08-12
status: PPLUS_ACTION_OWNED_AT_FIXED_REAL_EULER_GRADE__NONLINEAR_REPLACEMENT_REFUTED
lane: "1"
functional_channels: [BUILD, COMPOSE, SOURCE, VERIFY]
source_return: SOURCE_CONFIRMS_RESIDUAL_NORM_SQUARE_AND_ADJOINT_GRAMMAR__SOURCE_SILENT_ON_HQ_AND_PPLUS__REPO_DERIVES_PPLUS_AS_FIXED_REAL_EULER_PRIMALIZER__NONLINEAR_RESIDUAL_REPLACEMENT_REFUTED
ledger_rows: [RA-E1, RA-E3, LT-SM6]
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Selected K77 I2B action-real projection

## Result in plain English

The fixed-output projection is no longer wholly inserted.  The source-owned
residual-square grammar, together with the already-selected real K77 pairing,
forces exactly that projection in the first variation about the physical
`H_q`-real residual.

This is a scoped construction, not a rewrite of the field equation.  Away
from the fixed-real residual stratum, the unprojected action still sees the
anti-fixed sector.  Therefore the action owns `P_+` as an Euler primalizer at
this stratum, but does not own `P_+ A` as a globally substituted nonlinear
residual.

## Layer 0

| Object | Meaning | Not established |
| --- | --- | --- |
| `A` | selected pointwise Shiab residual map | real-equivariant morphism |
| `P_+ A` | post-composed fixed-output map | source-published residual |
| `I_R(U)=1/2 Re B(U,U)` | selected conditional real action | positive Hilbert norm |
| `dI_y` | first variation at residual `y` | global Euler operator |
| `P_+` in `dI_y` | action-self-adjoint primalizer when `tau y=y` | nonlinear replacement |
| formal `D^* Upsilon` | section-level adjoint equation | closed Green domain here |

The distinction matters because image membership and stationarity are not the
same question.  V0.205 established that `P_+ A` is a natural pointwise map.
This wave establishes why the same `P_+` occurs in the action derivative at a
fixed-real background.

## Exact theorem

Let `tau` be the moving `H_q` target involution and

```text
P_+ = (1+tau)/2,    P_- = (1-tau)/2.
```

On the selected real bilinear `B_R=Re B`, the involution is an isometry and
`P_+` is self-adjoint.  The fixed and anti-fixed sectors are orthogonal.  Thus
for every fixed residual `y`,

```text
B_R(A delta x,y)
  = B_R(P_+ A delta x,y) + B_R(P_- A delta x,y)
  = B_R(P_+ A delta x,y).
```

The executable certificate checks this on every one of the `99,463`
target-relevant columns.  The coordinate real action agrees exactly with the
existing symmetrized Hodge-times-Clifford-trace residual pairing.  There are
zero factorization failures and zero anti-fixed couplings to the target;
`90` columns have nonzero first derivative.

| Exact quantity | Value |
| --- | ---: |
| live columns | `99,463` |
| target complex coordinates | `196` |
| fixed-output image rank | `170` |
| anti-fixed image rank | `195` |
| fixed/anti basis pair tests | `33,150` |
| fixed/anti orthogonality failures | `0` |
| nonzero first derivatives | `90` |
| anti-sector action witness | `-11` |

## Why this does not modify the nonlinear residual

The real quadratic action splits as

```text
B_R(U,U)=B_R(P_+U,P_+U)+B_R(P_-U,P_-U).
```

The second term is not zero as a bilinear form: the certificate finds the
exact anti-fixed witness `-11`.  Adding that anti-fixed witness to the residual
background makes the full first variation differ from the projected one.
This planted contrary background is the control that prevents the theorem
from becoming a tautological projection rule.

The correct disposition is therefore:

- **derived:** `P_+` as the action primalizer at an `H_q`-fixed residual;
- **not derived:** replacement of `A` by `P_+A` in the nonlinear source
  residual;
- **still open:** a chosen Spin/source-`epsilon` lift and global real residual
  bundle, moving `dH_q`, `dHodge`, `dShiab`, formal adjoint/Green return,
  Euler/preboundary and physical vacuum.

## Source return and constraint accounting

The draft states the bosonic residual norm square and the adjoint equation
`D^*_omega Upsilon_omega=0`.  No inspected source publishes the repository
`H_q` involution or `P_+`.  The first-variation factorization is therefore a
repository theorem inside the selected conditional action parent, not a
quotation from Weinstein.

No field, parameter, datum, function-valued slot, quotient or selector is
added.  P1/P2/P3 remain unchanged and unused.  Three ledger rows migrate only
in distance and evidence.

## Adaptive specialist review

- **Variational bicomplex:** required the derivative of the action, not image
  admission alone.  This supplied the theorem's correct layer.
- **Clifford/Krein geometry:** required the indefinite real pairing and
  involution to be tested exactly; positivity is not claimed.
- **Real-form geometry:** predicted fixed/anti orthogonality and demanded a
  non-fixed control.
- **Symplectic geometry:** refused to identify a fibre Euler primalizer with a
  presymplectic potential or reduced phase space.
- **Analytic PDE:** retained the formal adjoint, Green identity and domain as
  open.
- **Source criticism:** separated source-owned norm-square grammar from the
  repository-derived `H_q/P_+` realization.
- **Contrary path:** the anti-fixed witness refuted global nonlinear
  replacement while preserving the scoped Euler result.

## Next gate

Construct the action-owned global real associated residual bundle and chosen
Spin/source-`epsilon` lift.  Then differentiate the moving projector, Hodge
and Shiab and assemble the complete Euler and presymplectic preboundary
classes.  The derivative calculation must retain both motion of the residual
and motion of the primalizer.
