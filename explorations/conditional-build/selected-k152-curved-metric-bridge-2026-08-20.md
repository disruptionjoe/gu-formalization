---
title: "Selected-K152 curved metric bridge"
status: active_research
doc_type: exact_normal_coordinate_curved_metric_bridge_and_weighted_adjoint_gate
created: "2026-08-20"
registry: lab/process/selected-k152-curved-metric-bridge.json
probe: tests/channel-swings/selected_k152_curved_metric_bridge_probe.py
grade: "K152 SERIALIZES THE K127/K129 CURVED METRIC-TO-DISTORTION BRIDGE AS THE EXACT NORMAL-COORDINATE MINUS-TWO LINEARIZED EINSTEIN ROW FOLLOWED BY THE SELECTED CONDITIONAL SHIAB. THE CLOSED 112-DIMENSIONAL OUTPUT PACKET HAS CL(7,7) LOWERER INERTIA (58,54). REFERENCE AND RATIONALLY ROTATED NULL SYMBOLS EXACTLY REPLAY THE HELD K135 MATRICES WITH RANK FOUR AND ANNIHILATE THE DIFFEOMORPHISM IMAGE. AN ALIGNED RICCI-FLAT WEYL FIXTURE GIVES A LIVE RANK-NINE ZERO-ORDER BRIDGE; THE GENERIC K127 FIXTURE RETAINS OFF-TT OUTPUT. K149/K151 PRODUCE FOUR LIVE RANK-ONE FIRST-ORDER FORMAL-ADJOINT COEFFICIENTS THAT A FROZEN-BRIDGE PLANT ERASES. FIVEFOLD COMPOSITION AND NULL LOWER LEAKAGE REMAIN K153."
target_claim: K151_NEXT_GATE__CURVED_METRIC_BRIDGE_AND_FROZEN_ROTATED_CONTROLS
target_verdict: CURVED_METRIC_BRIDGE_EXACT__NULL_SYMBOL_RANK4_REPLAY__WEYL_ZERO_ORDER_LIVE__MOVING_ADJOINT_FIRST_ORDER_LIVE__FIVEFOLD_REMAINS_K153
canon_verdict_change: none
---

# Selected-K152 curved metric bridge

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K152 binds the repository-selected conditional `comm/symi/symi`
metric-to-distortion bridge at one Ricci-flat normal-coordinate germ, on an
exact 112-dimensional output packet through the bridge's second-order and
Weyl-dependent zero-order coefficients and the formal adjoint's first lower
coefficient. It does not recover Weinstein's preferred historical Shiab,
compose the distortion operator five times, or define a quotient/domain.

```gu-typed-objects
result: K152 exact curved metric bridge and weighted formal adjoint
carrier: LAYER=ambient CHIRALITY=N/A real symmetric horizontal metric 10-packet to Omega1(Cl(7,7)) 112-packet
pairing: DeWitt ON=metric-10 and Hodge/scalar-Clifford ON=distortion-112
real_structure: real Cl(7,7) with exact complex-coordinate bookkeeping
grading: symmetric metric degree to one-form/Clifford grading
action_owner: repository-construction
target: curved selected-Shiab density-dual differential bridge MAP-TYPE=homomorphism
```

## 0. Route and currency

K129 owns

```text
A = D_g[S_g(F_B)]|Ricci-flat = -2 delta G
```

and K127 owns exact Ricci-flat normal-coordinate Weyl two-jets. K135 retains
only frozen `A(n)` matrices. K149--K151 now supply the sparse operator,
selected moving Shiab and indefinite distortion pairing, but no prior module
joins those owners.

The K127/K129 artifacts predate the 2026-08-17 canonical source-correction
register. Their load-bearing content is repository-derived local curvature
and variational typing rather than a superseded source reading; K152 uses
them as-is with that currency check explicit.

The route comparison rejected importing frozen matrices as coefficients,
stopping at a flat principal map, and attempting the dense fivefold product
before the bridge was typed. The chosen route differentiates the metric germ
itself, contracts to the owned `-2 delta G`, selects one algebraic
zero-Weyl curvature representative for that Einstein row, and applies the
selected Shiab. The representative adds no background claim: on the
horizontal packet the selected response factors through the Einstein
contraction.

## 1. Normal-coordinate bridge

At a normal-coordinate point,

```text
g_mn,ab = -(R_manb + R_mbna)/3.
```

K152 differentiates the all-lowered Riemann tensor with respect to the metric
zero-jet and every commuting second derivative. It also differentiates the
inverse metric used in the Ricci contraction. This second contribution is
load-bearing: applying only the frozen Shiab to `delta Riemann` drops the
variation of the contraction and incorrectly erases the curved coefficient.

The resulting Einstein row is multiplied by the K129 normalization `-2` and
then sent through the selected conditional Shiab. The output closes on eight
Clifford labels and 112 one-form/Clifford coordinates. Its exact pairings are

```text
det(B_DeWitt) = 64,
rank(B_distortion) = 112,
inertia(B_distortion) = (58,54).
```

The density-dual bridge is order two. Primalizing the distortion output and
applying K149's complete DeWitt/Clifford weighted adjoint gives an order-two
reverse bridge. After lowering its metric output, it agrees coefficientwise
with the complete formal transpose of the density-dual bridge.

## 2. Curved and moving lower coefficients

For K127's aligned pure-electric Weyl fixture
`E=diag(q/2,q/2,-q)`, the zero-order `-2 delta G` coefficient preserves the
plus/cross plane with weight `-2q`. Its full metric coefficient and selected
density-dual bridge both have rank nine at nonzero `q`.

The generic K127 fixture `E_13=E_31=1` retains the hostile control:

```text
H_plus  -> -2 (dx1 odot dx3),
H_cross -> -2 (dx2 odot dx3).
```

Both outputs leave the selected plus/cross plane. A zero two-by-two
compression therefore still does not establish an invariant TT subsystem.

The chosen inner-Spin frame jet leaves the pairings constant but moves the
selected coefficient. The complete formal adjoint consequently has four live
first-order coefficients, each of rank one at the jet origin. Freezing the
bridge coefficients makes all four vanish. These terms belong to `A*`; they
are not yet a fivefold distortion residual.

## 3. Frozen and rotated controls

At

```text
n0=(1,0,0,1),
n1=(1,3/5,0,4/5),
```

the serialized second-order symbol exactly equals the independently rebuilt
K135 selected-curvature matrix, entry for entry. Both matrices have rank four
and annihilate the four-dimensional metric diffeomorphism image. The replay
tests values, not only ranks, so an overall sign, factor, index order or
packet-coordinate mismatch fires.

## 4. Claim ceiling and next gate

K152 closes the curved metric bridge and its operative formal adjoint on the
declared packet. It does not yet compose

```text
K P^5 K A
```

as a variable-coefficient operator. The curved restricted residual and first
null lower leakage therefore remain
`UNDEFINED_FIVEFOLD_NOT_YET_COMPOSED`, not zero or nonzero.

K153 should compose the already serialized K149--K152 operators on the
smallest closed null action packet, replay the frozen zero, and extract the
first curved null lower coefficient before any quotient, inverse, domain,
BFV, physical-mode, positivity or propagator claim.

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k152_curved_metric_bridge_probe.py
```

## K153 successor classification

K153 closes the bridge seed under the frozen and first-jet selected action on
an exact 448-dimensional packet. The generalized null symbol has power ranks
`[234,125,16,8,0]` at the reference and rationally rotated covectors. Its
variable fifth power has a live rank-four first-lower coefficient, but the
order-six coefficient of `K P^5 K A` is exactly zero at both covectors because
that term annihilates the K152 bridge image. This is no first-lower restricted
leakage, not a zero complete curved remainder. K154 owns the order-five
restricted coefficient with the required second jets.
