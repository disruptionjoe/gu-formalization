---
title: "K178 exchange-prefix numerical-sufficiency wave"
document_role: active_research
doc_type: conditional_native_K139_K178_exchange_prefix_structural_compiler_and_coefficient_tensor_admission_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned sufficiency result for the K139--K177 equal-coupling two-edge positive particle/hole point control; K177's path automaton, contraction census, ordered-simplex identity and heat-kernel determinant representation remain exact, while a complete recompile shows that its emitted data do not identify the signed coefficient-level integral family because operator-monomial identities, exact coefficients, contracted resolvent incidences, output kernels and antisymmetrizer normalization are not serialized; a fail-closed numerical-admission contract and complete 633-path/2,958-contraction structural skeleton are now executable, but no outward prefix integral, action column, residual, complement floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation is evaluated
manifest: lab/process/k178-exchange-prefix-numerical-sufficiency-wave.json
solver: tests/channel-swings/k178_exchange_prefix_numerical_sufficiency.py
probe: tests/channel-swings/k178_exchange_prefix_numerical_sufficiency_probe.py
target_claim: INTERNAL_TARGET:K177_COEFFICIENT_COMPLETE_INTEGRAL_FAMILY
target_claim_verdict: REPRESENTATION_CLASS_EXACT__COEFFICIENT_TENSOR_NOT_SERIALIZED__NUMERICAL_ADMISSION_REJECTED
canon_verdict_change: none
---

# K178 exchange-prefix numerical-sufficiency wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: the fixed K139/K156 hard-core `C3` impurity coupled to four
particle/hole bath species on `L2(R)`, auxiliary chart `lambda=256`, and the
three K162 zero-bath seed lines. The result audits whether the K177
representation is sufficiently explicit for certified numerical evaluation.
It does not alter the operator, replace the continuum by a regulator, or
select a physical extension, state, domain, polarization or scalar center.

```gu-typed-objects
result: K177's exact path automaton, matched-contraction census, ordered Laplace-simplex identity and specieswise heat-kernel determinant representation are preserved, but the emitted certificate and executable output do not determine a coefficient-complete integral family; K178 compiles the complete structural skeleton and installs a fail-closed coefficient-tensor admission contract before outward quadrature
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing transported by S=(1-G_256)^-1 to M=S* S; coefficient-level scalar products use the exact specieswise exterior pairing rather than path-label orthogonality ON=repository_signed_point_control
real_structure: CAR adjoint, momentum conjugation, Hermitian matched finite-cutoff normal ordering and the K168 real flavor-symmetric reference shape
grading: conserved incidence charges, hard-core impurity state, four bath species, bath number, word order, path identity, contraction identity, ordered energy-variable provenance and occupation signature
action_owner: repository-construction -- K156 owns the matched finite-cutoff normal-order coefficient, K176 owns its all-order tail and K177 owns the representation class; no source/GU action selects the physical extension, state, domain, polarization or scalar center
target: decide whether the K177 output is a numerically identified finite integral family, preserve its exact positive content, and expose the minimum coefficient-level input required before determinant-aware interval evaluation MAP-TYPE=intertwiner
```

## Inline preflight bookend

K177 correctly turns cumulative resolvents into ordered-simplex integrals and
correctly reduces fixed exterior scalar products to determinants of
`kappa(t)=K_1(t)/pi`. Its next condition says to evaluate *the* resulting
order-two-through-twelve family. The route-changing question is whether that
family is actually emitted term by term.

The route census compared immediate interval quadrature, determinant-level
boxes, divided differences, sparse K162 compression, low-order direct normal
ordering and coefficient serialization. Numerical method choice is downstream:
an outward evaluator cannot operate until the exact signed integrand family is
identified. The cheapest decisive step is therefore a sufficiency audit.

The source register and v0.263 physics ledger do not move. `SC-META-53`
remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain `NEEDS`.

## 1. What K177 does determine

K178 imports K177's native automaton and recompiles every deterministic
structural record. For each path it now emits a stable identity, seed, order,
ordered bath letters, output impurity, occupation signature, global-CAR sign,
ordered energy variables and all cumulative denominator factors.

Across orders zero through twelve this gives exactly

```text
633 path records in 198 seed/order/occupation-signature blocks.       (1)
```

For each surviving older-letter contraction it additionally emits a stable
identity, source path, newest letter, contracted old position, output letters,
output impurity, output signature, exact K177 CAR sign and output-variable
provenance. This gives

```text
2,958 contraction records in 201 seed/order/output-signature blocks.  (2)
```

At the first nonzero order there are six order-two contraction records and six
signature blocks. At order twelve there are 1,152 records and only 33 blocks.
Thus the structural compression is real, but it is not yet coefficient
compression.

## 2. The stronger numerical family is not serialized

Every contraction record still lacks five fields needed to turn it into a
signed integral term:

1. the K156 exchange operator-monomial identity;
2. its exact operator coefficient;
3. the affine energy forms in both contracted resolvents;
4. the resulting output-kernel formula after the old variable is integrated;
5. the precise specieswise antisymmetrizer normalization and permutation action.

The K177 manifest records the count `2,958`, a generic cumulative denominator,
the generic statement that a contraction adds a Schwinger parameter and the
generic determinant formula. It does not bind these five fields to individual
contractions. The executable reproduces the same boundary: its public result
is a census plus a generic simplex certificate, not a term list. K178 finds
`2,958 x 5 = 14,790` unresolved coefficient-field instances.

This is a correction of ceiling, not a rejection of K177's mathematics.
K177's representation theorem remains exact. Its output proves that any fully
specified term has the advertised determinant representation; it does not serialize a coefficient-complete integral family.

## 3. Why counts and signatures cannot supply the missing data

Occupation signature determines an orthogonality *eligibility* block: terms
with different final impurity or species counts do not pair. It does not
determine the kernel inside a block. K178 finds 173 multi-record contraction
signature blocks whose members retain different source paths, contracted
positions or ordered variable provenance. Summing their CAR signs at the block
label would erase those distinct functions before the determinant pairing is
formed.

An exact one-coordinate countercontrol makes the logical gap minimal. Fix the
same nonzero structural kernel and the same one-by-one heat-kernel Gram
`[1]`. Coefficient assignments `1` and `2` satisfy every coefficient-free
K177 structural assertion, but their quadratic values are respectively `1`
and `4`. This does not choose or vary the native K156 coefficient. It proves
that the current serialized assertions do not identify its numerical value.

The order-two family does not escape this issue. Its six structural records
are singleton signature blocks, so no same-signature path cancellation is
available to reconstruct the missing coefficient or contracted resolvent.
Consequently even the first nonzero exchange order is not admitted for
outward evaluation from the current artifact set.

## 4. Fail-closed numerical-admission contract

The K178 compiler admits a contraction family only when every term supplies
all five fields above. It rejects the complete K177-derived structural list and
accepts a fully populated exact positive control. Hostile tests mutate the
counts, required fields, admission polarities, nonidentifiability control and
downstream fences.

The next native producer is not the interval quadrature layer. It is K156's
matched finite-cutoff normal ordering, specialized to the K162 seed paths. It
must emit one coefficient-complete term record per surviving exchange
contribution, after the adjacent endpoint contraction is cancelled and before
the regulator limit is taken. The record must preserve the K176 two-resolvent
structure and map its old/new variables into the K177 ordered-simplex
coordinates.

Once those records exist, a determinant-aware evaluator can group terms only
after proving they share the same output kernel and permutation convention.
Determinant-level or divided-difference interval bounds remain viable; K178
does not select between them.

## 5. Downstream replay

The coefficient-complete exchange prefix is not yet a numerical vector. K176's
tail below `1/250`, K175's scalar/diagonal tails and K170's Gram/reference
shape therefore cannot be assembled into a complete K171 action column.
Without that column there is no complete `M`-dual `R_ref` residual. The
positive complete `M`-orthogonal complement or flux floor, scalar-center left
floor and K152 interval remain downstream.

No source claim, physics-ledger row, canon verdict, paper, release or public
posture changes. No physical/source selection, Born derivation, prediction or
confirmation follows.

## Hostile result review

- **Strongest overclaim:** “K177 is wrong.” Refused. Its automaton, CAR census,
  simplex identity and heat-kernel determinant representation remain exact.
- **Strongest contrary construction:** the missing five fields might be
  recoverable from K156 prose by hand. That would be a new normal-order
  derivation; until it is serialized and checked term by term, it is not input
  to an outward certificate.
- **Weakest analytic seam:** a complete term list still does not by itself
  solve small-time determinant cancellation. Numerical admission precedes,
  but does not replace, determinant-level error analysis.
- **Weakest reproducibility seam:** structural compilation imports K177 code.
  The hostile probe therefore pins both predecessor totals and K178's stronger
  missing-field rejection so a changed predecessor cannot silently turn the
  rejection into admission.

## Inline postflight bookend

The Run preserves K177's exact representation result while removing an
unjustified implication from its next condition. The complete available
structural family is now executable at record level, and the exact missing
normal-order coefficient tensor is named and machine-enforced. Immediate
quadrature is rejected before it can emit a false outward bound.

The next condition is to specialize K156's finite-cutoff matched normal order
to every K178 contraction record and populate the five required fields. Only
then evaluate order two first, verify the coefficient and determinant
normalization against the exact K172 first-block fence, and extend through
order twelve before assembling K171.

## Reproduction

```bash
python3 tests/channel-swings/k178_exchange_prefix_numerical_sufficiency.py --demo
python3 tests/channel-swings/k178_exchange_prefix_numerical_sufficiency_probe.py
python3 tests/channel-swings/k178_exchange_prefix_numerical_sufficiency_probe.py --selftest
```

Expected: `43/43` exact controls and `29/29` hostile mutations caught.
