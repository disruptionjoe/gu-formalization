---
title: "Conditional physics ledger v0.110"
status: current
doc_type: ledger_summary
created: "2026-08-09"
---

# Conditional physics ledger v0.110

`82/82` targets remain mapped: `32 SAME`, `19 DIFFERS`, `26 NEEDS`, and
`5 OVER-DETERMINED`. Residue remains `84..86` continuous parameters, at least
19 function-valued slots and nine discrete forks. Five scoped quotients remain
booked.

This version closes the two immediate v0.109 gates at the grade actually
proved. First, a faithful local matrix model with nonconstant, noncommuting
transition functions verifies the affine connection law

```text
B^g = g^{-1} B g + g^{-1} d g
```

on direct and sequential triple overlaps. Recomputed `F_B`, `D_B T`, the raw
source residual and its formal covariant derivative descend exactly. Removing
the affine term or reversing the cocycle order fires planted failures. This is
generic affine descent, not yet the moving selected-K77 `Phi`/Shiab/Hodge/
density/observation packet on `Y^14`.

Second, the primary source itself prints

```text
dI_1^B = (Upsilon_omega, Xi_omega),
Xi_omega = D_omega Upsilon_omega.
```

It explicitly calls the second equation redundant once `Upsilon_omega=0`.
Therefore `Xi` is not an independent source equation that can select the
remaining amplitude. In the frozen homogeneous scalar receiver it factors as
`Xi=2(b+t)Upsilon`, so the joint Jacobian rank remains two.

The homogeneous frozen-coefficient ansatz now gives two exact nonzero
open-set witnesses, in addition to the zero branch:

```text
t_+ = (-2+sqrt(3))/208,  b_+ = 1/208-sqrt(3)/312,
t_- = (-2-sqrt(3))/208,  b_- = 1/208+sqrt(3)/312.
```

Both lie on the v0.109 invariant family. Their amplitudes are selected by the
extra homogeneous ansatz, not by the source, so neither is a GU magnitude
prediction.

Seven rows migrate in distance, frontier grade and evidence only: `LT-GR1`,
`LT-GR2b`, `LT-GR2c`, `LT-GR2d`, `LT-GR3`, `LT-GR5`, and `LT-GR6`. No verdict,
reason kind, global residue, booked quotient, datum, canon statement or public
posture changes. The selected Spin-native, two `U(32,32)`-half and full
`U(64,64)` parents remain distinct; P1/P2/P3 remain unused.

Next: port both algebraic branches to the actual selected-K77 moving
`Phi`/Shiab/Hodge/density/observation packet, derive the complete primitive
epsilon Euler and off-shell Ward factorization, and test branch survival and
amplitude ownership. Only then choose the 321-versus-1,571 tangent and resume
Hessian/BV/domain work.

Machine ledger: `lab/process/conditional-physics-ledger-v0.110.json`.
