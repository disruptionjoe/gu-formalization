---
title: "Selected-K150 moving selected-Shiab coordinate adapter"
status: active_research
doc_type: exact_coordinate_jet_adapter_and_frozen_rotated_control_gate
created: "2026-08-16"
registry: lab/process/selected-k150-moving-selected-shiab-coordinate-adapter.json
probe: tests/channel-swings/selected_k150_moving_selected_shiab_coordinate_adapter_probe.py
grade: "K150 SERIALIZES THE FIRST TARGET-SPECIFIC K149 DEPENDENCY: THE SELECTED CONDITIONAL COMM/SYMI/SYMI CL(7,7) SHIAB COEFFICIENT AS AN EXACT COORDINATE-JET MULTIPLICATION OPERATOR. THE FIRST TWO COORDINATE JETS ARE LIVE; THE FIRST AGREES WITH THE INDEPENDENT K77 MOVING-PHI DERIVATIVE. FROZEN K132 BLOCKS REPLAY AT THE REFERENCE AND K138 RATIONALLY ROTATED NULL COVECTORS. THE MOVING DISTORTION PAIRING AND CURVED METRIC BRIDGE REMAIN UNSERIALIZED, SO NO CURVED RESIDUAL OR LOWER NULL LEAKAGE IS YET DEFINED."
target_claim: K149_NEXT_GATE__MOVING_SELECTED_SHIAB_COEFFICIENT
target_verdict: MOVING_SELECTED_SHIAB_COORDINATE_ADAPTER_EXACT__FROZEN_AND_ROTATED_CONTROLS_PASS__PAIRING_AND_CURVED_BRIDGE_REMAIN_NEXT
canon_verdict_change: none
---

# Selected-K150 moving selected-Shiab coordinate adapter

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K150 binds only the selected conditional `comm/symi/symi` Shiab on the
settled real `Cl(7,7)` branch as a coordinate coefficient compatible with the
K149 sparse operator algebra. It does not recover the preferred historical
Shiab, import the Eric-channel `(9,5)` metric port, choose a distortion pairing,
or compute a curved residual.

## 0. Pre-wave answers

1. **Object and fork.** The object is the coefficient map from curvature-valued
   two-forms to the selected Shiab output, not its formal Euler pairing. The
   selected conditional sheet remains distinct from the missing preferred
   historical selector.
2. **Owned motion.** Coordinate motion is generated by the repository-recorded
   source law `delta Phi_i=[Phi_i,chi]`; the two-term Shiab formula is rebuilt
   from those moving tensors at every jet order.
3. **Retrieval collision.** A nearby exact moving-metric implementation exists
   in the Eric channel, but it declares the distinct `(9,5)` port. Importing it
   would silently change real form and workstream owner.
4. **Cheapest kill.** Failure to reproduce the frozen K77/K132 formula or its
   independent first derivative kills the adapter. Both controls pass.
5. **Claim ceiling.** A coordinate coefficient is not a pairing, metric bridge,
   fivefold remainder, quotient endomorphism, domain, or physical mode.

## Result

`tests/channel-swings/k150_moving_selected_shiab_coordinate_adapter.py`
reimplements the source-displayed selected two-term formula over sparse exact
exterior and Clifford dictionaries. It constructs finite coordinate jets of
`Phi_1` by the conjugation series and recomputes
`Phi_2=(1/2) Phi_1 wedge Phi_1`; it never differentiates or substitutes a stored
K132 matrix. The resulting coefficient matrix is wrapped as a zero-order
`SparseDifferentialOperator` in K149's algebra.

Three independent curvature columns reproduce the settled K77 formula at the
frozen coordinate. The first coefficient jet equals the separately implemented
K77 `d_shiab` derivative column by column. Both first and second jets are
nonzero, while the frozen-substitution plant erases the first jet.

For an exact 56-dimensional invariant packet, the adapter's frozen raw and
formal-Euler blocks reproduce the independent K132 construction at both

```text
n0 = (1,0,0,1),
n1 = (1,3/5,0,4/5).
```

Both selected Euler blocks have rank 30. The K138 rational Lorentz matrix
preserves `diag(1,-1,-1,-1)` and carries `n0` to `n1`, so the coordinate
adapter passes the required frozen and rotated replay without claiming that a
fixed component matrix is itself a covariant moving coefficient.

## Exact closure boundary

```text
moving_selected_shiab_coefficient:  SERIALIZED_EXACT_COORDINATE_JET_ADAPTER;
moving_distortion_pairing:           NOT_YET_SERIALIZED;
curved_metric_bridge_A:              NOT_YET_SERIALIZED;
curved restricted residual:          UNDEFINED_DEPENDENT_ADAPTERS_NOT_SERIALIZED;
first curved null lower leakage:      UNDEFINED_DEPENDENT_ADAPTERS_NOT_SERIALIZED;
frozen K148 quotient principal map:   ZERO_EXACT_UNCHANGED.
```

K151 should serialize the moving distortion pairing, including its density and
Clifford/form lowerers, and prove that its K149 mechanical weighted adjoint is
compatible with the K150 coefficient. Only then may the curved metric bridge
be composed. A later gate, not K150, owns the restricted fivefold residual.

## K151 successor classification

K151 now serializes the exact indefinite Hodge/scalar-Clifford lowerer on a
nontrivial 56-dimensional null packet, reproduces K132's frozen rank-30 Euler
principal coefficient through K149's complete weighted formal adjoint, and
extracts the live rank-27 lower term forced by K150 coefficient motion. The
curved metric bridge remains the first missing dependency; no curved residual
or null leakage is yet defined.

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k150_moving_selected_shiab_coordinate_adapter_probe.py
```
