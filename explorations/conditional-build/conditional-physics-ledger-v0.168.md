---
artifact_type: conditional_physics_ledger
schema_version: "0.168"
created: 2026-08-11
predecessor: conditional-physics-ledger-v0.167.md
status: CURRENT_APPEND_ONLY_LEDGER_V0_168
---

# Conditional physics ledger v0.168

## One-line meter

```text
Ledger v0.168 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue 84 continuous + >=19 function-valued + 9 discrete forks
Five scoped conditional quotients ranked
```

## What moved

The actual real-K77 source-shaped dimension-`1920` evolution was reconstructed
on the admitted one-time observation section. In each of its three spatial
directions,

```text
rank(E_j^2 - 1) = 128,
(E_j^2 - 1)^2 = 0.
```

Therefore every `E_j` is non-diagonalizable. A positive symmetrizer would make
each generator similar to a symmetric matrix, so the positive simultaneous-
symmetrizer cone is empty. This kills the standard **unreduced** strong/
symmetric-hyperbolic maximal-dissipative Cauchy route.

**Conditioning disclosure.** The exact nonzero square-zero remainder proves
that each `E_j` is defective, so an eigenvector-basis condition number is
infinite or undefined rather than a finite numerical estimate. No numerical
pseudospectral radius is computed here; the conclusion uses the exact Jordan
identity and rank, not a tolerance-sensitive eigendecomposition.

It does not kill every closed graph realization. The ambient Y14 operator is
ultrahyperbolic, so ordinary Lorentzian Cauchy theory was never an ambient
domain theorem. A source-derived invariant reduction or an explicit nonlocal
ultrahyperbolic constraint remains open. The globally descended normal symbol
and Darboux atlas survive, and the graph family remains unselected.

## Six append-only row migrations

- `RA-D4`: mirror removal now waits for a derived reduction or nonlocal ambient
  domain rather than the killed unreduced Cauchy route.
- `RA-F1`: P3 remains downstream of that physical domain and its index.
- `RA-F2`: the at-least-120-coordinate replica graph remains unselected.
- `RA-G2`: neither the no-go nor the surviving domain routes distinguish W
  from mirror.
- `LT-SM3`: the selected action owes a reduced/nonlocal Dirac domain without a
  fitted projector.
- `AC-F1`: chirality still needs that domain, observation and a 14D-to-4D
  index.

Verdict classes and reason kinds do not change.

## Layer-0 result

- the current source-shaped real-K77 rolled operator is not the isolated W131
  carrier, although an exact constant-conjugacy bridge now transfers the
  source-shaped Jordan calculation;
- positive Hilbert symmetrizers are not indefinite Krein/Green pairings;
- a one-time Lorentz-section evolution is not the ambient ultrahyperbolic
  domain;
- failure of strong hyperbolicity is not nonexistence of every closed graph;
- rank-128 generalized characteristic chains are not an off-shell BV
  differential or physical quotient; and
- a transported Lagrangian graph is not an action-selected domain.

## Frontier delta

```text
headline_delta: none
frontier_conditions_closed: 1
  - standard unreduced positive-symmetrizer/maximal-dissipative Cauchy route decided and killed
frontier_conditions_opened: 1
  - source-derived invariant reduction versus explicit nonlocal ultrahyperbolic constraint
remaining_named_conditions: 3
  - construct or kill the source/action-derived constraint or BV differential
  - type and test the nonlocal ambient ultrahyperbolic constraint
  - observation, chirality, mirror, index/count and physics rendezvous
```

## Source return

`SOURCE_CONFIRMS_FIRST_ORDER_FOUR_FIELD_GRAMMAR_AND_ONE_TIME_VERSUS_ULTRAHYPERBOLIC_DISTINCTION__SOURCE_CORRECTS_NONE__SOURCE_SILENT_ON_POSITIVE_SYMMETRIZER_JORDAN_REDUCTION_BV_DIFFERENTIAL_AND_NONLOCAL_DOMAIN`

## Unchanged boundary

Coverage, verdict counts, residue, forks, five scoped quotients, P1/P2/P3,
canon verdicts and public posture remain unchanged. The 120 graph coordinates
remain unbooked. No global ambient domain, physical state, positivity,
chirality, particle, family, index, count, spectrum or prediction is claimed.

Machine ledger: `lab/process/conditional-physics-ledger-v0.168.json`.
