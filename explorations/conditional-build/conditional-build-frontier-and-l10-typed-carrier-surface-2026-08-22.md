---
title: "Post-LT-GR8 frontier and L10 typed-carrier certificate-surface reconciliation"
status: active_research
doc_type: integrity_result
created: "2026-08-22"
registry: lab/process/conditional-build-frontier-and-l10-typed-carrier-surface.json
scripts:
  - tests/channel-swings/conditional_build_frontier_l10_typed_carrier_surface_probe.py
grade: "EXACT PROCESS-CERTIFICATE RECONCILIATION; NO SCIENTIFIC VERDICT, SOURCE OWNERSHIP, LEDGER, CANON, PREDICTION OR PUBLIC-POSTURE CHANGE"
target_claim: NONE-NOT-A-KILL; this artifact repairs an internal certificate boundary and does not adjudicate a source claim
canon_verdict_change: none
---

# Post-LT-GR8 frontier and L10 typed-carrier certificate-surface reconciliation

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: this result binds only GU's internal typed-carrier process certificate
and the already-declared LT-GR8 typing block. It does not type a new scientific
object or move LT-GR8 beyond `NEEDS` / `MISSING_CONSTRUCTION`.

```gu-typed-objects
result: repository-rooted typed-carrier audit and reconciliation of the already-declared LT-GR8 composite typing block
carrier: GU markdown result corpus plus the LT-GR8 ambient-to-observed section split LAYER=ambient+observed BRIDGE=section-restriction CHIRALITY=N/A
pairing: NONE
real_structure: N/A
grading: process-integrity only
action_owner: N/A -- no scientific action owner is supplied or changed
target: internal typed-carrier certificate surface MAP-TYPE=not-a-map
```

## Result

The post-LT-GR8 frontier has no newly enabled scientific arc. CBRS-1 still
lacks a complete target-blind owner packet; CBRS-2 still lacks a local
solution; C1 lacks seven source axes; B5 lacks a physical boundary owner; and
P-OBS has no applicable release or finite-redshift bridge. The source-to-proof
delta is unchanged. Stable-proof and integrity work therefore remain
maintenance, not scientific advance.

L10's reopening predicate is nevertheless concrete. The newest LT-GR8 result
carried a typed-objects block, but three fields used prose categories outside
the gate's closed vocabularies: `LAYER=typing`, an unregistered `none` owner,
and `MAP-TYPE=typing`. The repair does not invent a missing type. It records the
author's explicit ambient/observed section restriction, uses `N/A` for the
explicitly absent action owner, and declares the composite target map
`UNTYPED` because the artifact contains restriction, pullback, conormal lift
and non-lift statements rather than one arrow type.

The audit also discovered its scan set from the caller's current directory.
That made a CapacityOS-root invocation traverse unrelated repositories and
eventually crash on non-text content, while a GU-root invocation produced the
intended bounded verdict. The scan now roots at the gate's own repository and
continues to emit repository-relative paths. Both invocation locations produce
the same result. At the 2026-08-22 checkpoint the three new LT-GR8 reds were
gone while three older author-owned artifacts without declarations remained
visible. Their authors later supplied the missing declarations. Current-
currency custody therefore requires the live gate to remain green at zero
red rather than preserving that historical three-red population.

The LT-GR8 conditional evidence delta remains indexed exactly once and
dispositioned `deferred` with no priority effect. The global integration cursor
may advance to later valid deltas; it is not an immutable LT-GR8 field. The
metric-only result is already present in branch-relative state and the
canonical ledger verdict is unchanged, so minting a new ledger version solely
to restate `NEEDS` would create churn without changing a scientific decision.

## Hostile review and ceiling

The strongest overclaim is that a green typed-carrier surface proves the
scientific objects are fully typed. It does not: declared `UNTYPED` slots stay
visible and the trigger ceiling remains explicit. The strongest contrary
route is to infer one composite LT-GR8 map type from its prose; the repair
refuses and uses `UNTYPED`. The weakest reproducibility seam is caller working-
directory drift;
the propagation probe invokes the same gate from both GU and CapacityOS roots
and requires byte-identical output.

No theorem, scientific ledger row, canon, source attribution, prediction
credit, or public posture changes.
