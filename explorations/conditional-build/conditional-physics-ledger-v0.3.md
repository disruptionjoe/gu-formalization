---
artifact_type: conditional_physics_ledger_view
created: 2026-08-05
version: "0.3"
machine_source: lab/process/conditional-physics-ledger-v0.3.json
predecessor: lab/process/conditional-physics-ledger-v0.2.json
status: APPEND_ONLY_DYNAMIC_COSMOLOGICAL_SECTOR_SPLIT__ACTION_OWNERSHIP_OPEN
---

# Conditional physics ledger v0.3

## Progress meter

```text
Ledger v0.3 — 82/82 active target rows mapped (100% of current denominator)
32 SAME · 19 DIFFERS · 25 NEEDS · 6 OVER-DETERMINED
LT-GR2 split into five typed successors; one historical row retained
Residue — 83 continuous real + >=19 function-valued + 10 open discrete forks
Quotients ranked: 0; no reduction booked from the source-level two-field claim
```

Coverage rose because the previous cosmological row was not granular enough,
not because the construction became complete. The source argument and exact
rank classifier identify a promising mechanism, but no native action equation
or parameter reduction has yet been built.

## What changed

`LT-GR2` remains in the machine ledger as a superseded historical record. Its
single “positive cosmological-term sign” row had collapsed five different
objects and opposite prognoses:

| successor | verdict/kind | current result | distance |
| --- | --- | --- | --- |
| `LT-GR2a` literal `Lambda g` comparator | `DIFFERS/STRUCTURAL_DIFFERENCE` | fixed term is not the intended GU object | keep it typed as comparator |
| `LT-GR2b` variable connection-distortion VEV | `SAME/DERIVED_PARTIAL` | equivariance and dynamism exist | derive the nonzero vacuum branch and complete Euler placement |
| `LT-GR2c` curvature/VEV covariation | `NEEDS/MISSING_CONSTRUCTION` | an independent equality is abstractly rank one | identify the curvature object and prove quotient-independent Euler rank |
| `LT-GR2d` sign, magnitude, normalization and radiative stability | `NEEDS/MISSING_CONSTRUCTION` | sign is partial; scale burden remains | build vacuum selection stable under independent vacuum shifts |
| `LT-GR2e` action-owned `w(z)` and cosmology | `NEEDS/MISSING_CONSTRUCTION` | proxy FLRW work exists | derive the cosmology from the selected action and predict held-out data |

The active denominator is now `35` representation targets, `21` Lagrangian or
equation targets and `26` anomaly/consistency targets. The JSON contains `83`
row records because superseded `LT-GR2` is retained for provenance but excluded
from active counts.

## Why the source contact matters

The official 2025 Keating interview confirms Weinstein's actual magnitude
argument: a movable dark-energy VEV is to covary with a curvature-side field so
that two puzzles become one. The repo's earlier divergence-free refutation was
correct mathematics attached to the wrong argument.

The source also creates a hard Layer-0 burden. It moves verbally from spatial
flatness to a near-zero curvature field, but spatially flat de Sitter has
nonzero four-dimensional curvature. Until the native curvature object and
observation map are written, `LT-GR2c` stays uncertain at the decisive locus.

## Current-build collision

The current W229/W236 action identifies the distortion with a fermionic
record-current source. In matter-free vacuum it forces `theta=0` under its
positive screened-domain assumptions. It therefore does not yet implement the
nonzero curvature-covarying vacuum mechanism described in the interview.

This is a construction fork, not a global kill. The next job is to vary the
bare geometric distortion and a named curvature-side field in one action,
then compute the new rank modulo Ward, Bianchi and BV/gauge rows. The ledger
forbids defending the existing branch by merely renaming its record current.

## Next work

1. `LT-GR2b/c/d`: construct the source-native two-field Euler coupling and
   quotient-rank it, with a vacuum-shift control.
2. `LT-GR1/GR1b`: continue source-action ownership of the surviving pre-Shiab
   Einstein receiver.
3. `LT-SM3b`: extend the typed Shiab adapter through the action domain.

The full evidence, E10 exact rank table, E11 class-relative Weinberg check and
hostile reviews are in
`dynamic-cosmological-sector-constraint-rank-2026-08-05.md`.
