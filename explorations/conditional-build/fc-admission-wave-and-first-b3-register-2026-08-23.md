---
title: "FC admission wave: zero of four admitted, FC-7 overturned every first-pass verdict, and the first B3 register"
status: active_research
doc_type: admission_review_and_external_tension_register
created: "2026-08-23"
directed_by: "Joe direct chat, 2026-08-23"
registry: lab/process/fc-admission-wave-and-first-b3-register.json
probe: tests/channel-swings/fc_admission_wave_and_b3_register_probe.py
grade: "ADMISSION REVIEW AND EXTERNAL-TENSION REGISTER; ZERO CONSTRUCTIONS ADMITTED; NO VERDICT, CANON OR LEDGER MOVEMENT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "reviews admissions against an internal grade and records external tensions; kills no source claim and no research route"
canon_verdict_change: none
---

# FC admission wave, and the first B3 register

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders conventional particle-physics comparators throughout — DESI/CPL, the
> neutron EDM, neutrino mass bounds, the distance ladder. Every such result
> binds only the named model. Read
> `lab/methods/source-native-comparator-routing.md` before reusing any of it.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: BRIDGE_OR_SEMANTIC_BOUNDARY`

## Part 1 — the grade did its job by refusing everything

**Zero of four admitted.** The four surviving objects from the observed-side
construction wave were each reviewed against FC-1..FC-7 and each refused.

**The finding that matters: FC-7 was scored MET on first pass in all four
packets and overturned to NOT MET in all four.** That is the direct evidence
that the criterion earned its place — it was added the same day the grade was
installed, before anything had been banked, precisely because FC-1 through
FC-6 test whether a construction is *consistent* and none tests whether it is
*contentful*. Four reviewers, working the other six criteria, passed
prohibitions that do not hold.

FC-1 and FC-2 also failed 4/4: not one object was built with a complete
structure fingerprint, and not one sat at a named descent rung.

### The FC-7 refusal taxonomy

The refusals fall into reusable patterns, and this is the wave's most
transferable output:

- **CIRCULAR** — the prohibition restates the construction's own definition.
  "It forbids objects that do not satisfy it" is not content.
- **INHERITED** — the prohibition is a property of the *carrier*, true with or
  without GU, so the construction adds nothing. (The vertical-density line is
  a line bundle fact for any 4-manifold; what the construction supplies is one
  constant, and that constant is basis-convention dependent.)
- **EMPTY EXTENSION** — no possible violator. Every candidate anyone has
  proposed already satisfies it.
- **ANALYTIC** — a rewording no measurement could falsify. The "perfect fluid
  is thermodynamically visible iff `w != -1`" sharpening is exactly
  `T_ab k^a k^b = (rho+p)(u·k)^2` vanishing on all null `k` iff `rho + p = 0`.
- **DOUBLE-COUNTED** — the same fact booked as the FC-6 demotion condition and
  again as the FC-7 content. A scope boundary cannot also be the content.
- **PRIOR-ART INSTRUMENT** — the prohibition's witness is already the
  repository's own standing control.

### The structural distinction that separates a pass from a fail

The grade already had **one** banked entry when this wave ran, filed by a
concurrent lane about an hour after the method was installed:
`FC-GR-ROTATION-EINSTEIN-WEYL-STATIC-1`, rung R5, row LT-GR11, anchor
EXT-GR-ROTATION. (The wave brief's claim that these would be the program's
first entries was wrong, and the correction is recorded here.)

That entry carries `construction_created_before_anchor: true` — its
construction evidence is dated 2026-07-11 against anchor acceptance on
2026-08-23. **It was built six weeks before the target it was later measured
against.** All four of this wave's objects were assembled against targets
already in hand. The grade's own text makes that decisive: *"every choice must
be frozen before evaluation against a target, inheriting the CBRS
owner-before-evaluation discipline."*

That is the cleanest available statement of what admission requires, and it is
not a criterion anyone can satisfy retroactively.

### A marking irregularity, and the method fix it forces

One packet's FC-4 was substantively clean — no `ASSERTS` claim violated, no
`DISAVOWS` reading relied on — but the packet named **zero** `SC-` ids. The
reviewer supplied the search and scored the criterion met.

That destroys the criterion. FC-3 and FC-4 exist so that *an unchecked claim is
visibly unchecked*; a reviewer who performs the missing search converts an
unchecked claim into a checked one and erases the signal. **Rule, now added to
the grade: a reviewer who supplies a missing FC-3 or FC-4 search records it as
reviewer-supplied and scores the packet as failed.**

## Part 2 — the first B3 register

Bucket B3 has never been used. Six entries now exist, **all surviving the
citation standard, none softening an obligation without relocating it**: two
enlarge GU's obligation, two are two-sided, one withdraws a target GU could
accidentally hit while keeping the one it already fails, one narrows a
comparator-side premise while adding a joint-evaluation constraint. **Zero are
net-softening.**

The initial register classified two entries as enlarging the obligation. One
of those first-pass classifications has since been source-corrected; the
historical count remains useful evidence about the filing pass, not current
scientific direction.

- **B3-J95-03 (LT-GR8, EXT-J95) — corrected by
  `B3-J95-SCOPE-20260823`.** What is measured remains **nothing**: Jacobson
  1995 is a conditional theorem and its premises are hypotheses. The proposed
  further inference is withdrawn. Eling--Guedens--Jacobson 2006 proves a
  nonequilibrium requirement for Ricci-scalar entropy corrections, not every
  curvature correction. Guedens--Jacobson--Sarkar 2012 retains a restricted
  equilibrium Clausius construction for Lagrangians algebraic in the metric
  and Riemann tensor, subject to horizon, approximate-Killing and integrability
  conditions. The equilibrium branch remains open. Ledger v0.263 already
  states the correct two-branch obligation, so this entry is disposed with no
  ledger movement. See
  `explorations/conditional-build/b3-j95-primary-scope-disposition-2026-08-23.md`.
- **B3-H0-06 (EXT-COSMO-BACKGROUND, LT-GR2e) — corrected by
  `B3-H0-SCOPE-20260823`.** `H_0` is an inferred parameter in every route, but
  “nothing is measured” is too coarse and understates the local ladder's
  empirical content. SH0ES is a geometrically anchored empirical calibration
  through Cepheids, SNe Ia and Hubble-flow redshifts. Planck measures the CMB
  spectra and acoustic angle, then infers `H_0` under a cosmological model.
  DESI measures BAO peak positions and distance ratios in units of `r_d`; in
  flat ΛCDM it constrains `H_0 r_d`, not `H_0` alone. The source-faithful GU
  burden is one action-owned forward model for the background, perturbations,
  sound horizons, CMB angle/spectra, BAO ratios and low-redshift
  luminosity-distance relation. LT-GR2e v0.263 already requires the
  action-owned solution and held-out CMB/BAO predictions, so the entry is
  disposed with no ledger movement. **The Hubble tension supplies GU no
  relief.** See
  `explorations/conditional-build/b3-h0-observable-scope-disposition-2026-08-23.md`.

**B3-DE-01 discloses that it cuts both ways**, which is the discipline working.
Withdrawing the CPL central pair as a target also withdraws the CPL-based
argument *against* GU recorded in the comparative-tensions ledger. Undisclosed
that reads as one-sided relief; disclosed, it is exactly the relocation B3 is
for — the lean-against moves onto the pointwise object where the recorded kill
already lives.

### Corrections the register applied to itself

Re-checking citations at filing time changed four statements across two
entries, all against `CORRECTION DARK-ENERGY-07`: the `+5.7 sigma_A` overshoot
may not be quoted bare, since ΛCDM overshoots `+4.0` on the same metric and
the honest mechanism is **shape, not amplitude**; the `f_0` bounds are quoted
at fixed `omega_m h^2` and are roughly three times looser profiled; a claim
about GU sitting further from every measurement than the disputants sit from
each other is **false on `Omega_m`**, where GU's calibrated value sits `0.01
sigma` from DES-Y5's; and the status line must read *"excluded as the DESI
dark-energy signal; unconstrained-but-null as a family."*

### The maintenance rule the register must carry

`sigma_8`/`S_8` was refused as an entry, and it is the cautionary case:
KiDS-Legacy now agrees with Planck at `0.73 sigma`. **A register built a year
ago would today be holding `S_8` as a live excuse.** Entries are therefore
re-checked **at citation time, not filing time** — a rule this deliverable
demonstrated on itself.

Also refused, with reasons: muon `g-2` HVP (no row, and the tension has
*closed* — the 2025 Theory Initiative lattice-based value agrees with the
final Fermilab measurement), the `W` mass (no row; the CDF outlier is refuted
by CMS), the proton radius (resolved), and rotation curves (a row exists, but
mass-to-light has converged to ~0.1 dex against a discrepancy of a factor of
several, so a tension claim would be special pleading).

## Ledger bearing — candidates only

No verdict moves and nothing moves toward `SAME`; the grade is barred from it
and every FC object was refused. The FC wave produces **zero** ledger bearing.
The still-pending B3 entries propose row-field candidates only — scope notes
on the LT-GR2 family and LT-SM7, and a framing note plus joint-evaluation
constraint on RA-G3/RA-B6 — all of which go through ordinary evidence-delta
process. The proposed LT-GR8 `revival_trigger` narrowing is refused by
`B3-J95-SCOPE-20260823`; the existing row already carries the source-faithful
equilibrium-or-entropy-production fork. The proposed H0 measured-versus-derived
binary is refused by `B3-H0-SCOPE-20260823`; the existing LT-GR2e burden
already requires an action-owned cosmological solution with held-out CMB/BAO
predictions, and the accepted EXT-COSMO-BACKGROUND link covers the observed
expansion history without a duplicate row.

One independent confirmation worth recording: the wave ran
`tests/channel-swings/d4_contraction_rank_j_independence_probe.py` and it
passed 33/33, confirming the `J`-independence result from the same day.

## Honest assessment

The grade refused everything put to it, which is the correct behaviour for a
standard whose failure mode is laundering. The evidence that FC-7 was worth
adding is direct and quantitative: four packets, four first-pass MET verdicts,
four overturned. The evidence that the *rest* of the grade is calibrated is
the standing entry, which passes for a reason no later object can retrofit —
it was built before its target existed.

B3, by contrast, produced six usable entries on first attempt, none of them
softening. That asymmetry is itself informative: the program's untapped work
is on the phenomenology side, not in re-banking constructions it has already
made.
