---
artifact_type: exact_construction_and_scope_result
created: 2026-08-09
status: SELECTED_SPIN_LOCAL_PRINCIPAL_MINIMAL_COMMON_TANGENT_915__GRADE2_EXTENSION594__NO_DERIVED_CONSTRAINT_OWNED
ledger_rows: [LT-GR1, LT-GR2b, LT-GR2c, LT-GR3, LT-GR5, LT-GR6]
canon_verdict_change: none
---

# Selected-K77 minimal Hessian tangent closure

## Result in plain English

The previous wave proved that the selected `321` fields send `89` independent
equations outside their own equation space. This wave asks the constructive
question: what is the **smallest part of the already-existing source
connection** that must be restored so the first-action Hessian closes?

The answer at local principal-symbol grade is neither `89` nor all `1,250`
omitted grade-two directions.

- For one fixed symbol, the rank-`89` seed closes at rank `174`.
- Combining only the three stored causal representatives closes at rank `464`.
- A field tangent must work for every observed covector, not just three sample
  representatives. Exact spatial covariance supplies the missing fourth basis
  covector. The full `X^4` symbol family has seed rank `344` and closes at
  **rank `594`**.
- The same `594`-dimensional subspace works on both exact Galois-conjugate
  stationary branches and is already invariant under both branch Hessians.

Thus the least currently action-owned local principal tangent is

```text
321 existing selected fields + 594 derived grade-two directions = 915.
```

This is a proper subspace of the known `1,571` low-grade source coordinates.
The calculation therefore replaces the false binary “`321` or all `1,571`”
with an exact intermediate construction. It adds no fitted coefficient and no
external datum: all `594` directions already live in Weinstein's full
adjoint-valued connection one-form and are selected by the Hessian-closure
algorithm.

No existing action-derived constraint differential removes the leakage. The
owned matched-`q` Noether result is a four-parameter **kernel identity**, not a
map whose image is the `594`-dimensional sector. Unrestricted primitive-epsilon
boundary transformations also retain a live moment map, so they cannot be
quotiented by fiat. A future BV differential remains possible but unbuilt.

## Layer 0

| phrase | object constructed here | object kept distinct |
|---|---|---|
| off-slice response | rank-89 Euler-covector image for a fixed symbol | a source-field tangent |
| K-lift | field representative obtained from the nondegenerate grade-two `K_loc` pairing | a positive Hilbert Riesz map |
| symbolwise closure | rank-174 invariant subspace for one `q` | one field space valid for every `q` |
| common tangent | least rank-594 subspace closing the full four-dimensional symbol family on both branches | coordinate support of all 1,250 omitted fields |
| Noether identity | `H R=0` on the four-column matched physical graph | an image differential authorizing an equation quotient |
| selected parent | real-Spin(7,7) first-transgression action | two `U(32,32)` halves or full `U(64,64)` |

## Exact construction

The grade-two `K_loc` pairing is diagonal and nondegenerate with inertia
`(637,637)`. The grade-two first-action self Hessian has `5,642` directed
nonzero entries in an exact sparse stencil. Its construction uses the same
first-action second variation as the already-verified grade-one block, not the
raw-residual Jacobian or residual-square action.

Let `W(q)` be the K-lift of the complete metric/epsilon off-slice covector
image and add the off-slice response of the already-selected horizontal
grade-two fields. For branch Hessian `A`, compute

```text
S(q) = span{W(q), A W(q), A^2 W(q), ...} modulo the retained horizontal fields.
```

The exact ranks are:

| scope | seed | invariant closure |
|---|---:|---:|
| timelike, either branch | 89 | 174 |
| spacelike, either branch | 89 | 174 |
| null, either branch | 89 | 174 via `89 -> 164 -> 174` |
| three stored representatives, either branch | 259 | 464 |
| full `X^4` symbol basis, either branch | 344 | 594 |
| full `X^4` basis, both branches jointly | 594 | 594 |

The null image lies in the exact span of the `e_0` and `e_3` symbol images.
The missing `e_2` image is generated by the exact spatial `1 <-> 2`
permutation; the grade-two Hessian commutes with the `1 <-> 2` and `1 <-> 3`
generators. Since the principal response is linear in the observed covector,
the four basis images span the whole `X^4` symbol family.

The strongest planted control fires against the tempting wrong answer:
promoting the three-representative rank `464` as the field tangent fails once
the missing fourth symbol direction is included. A second control distinguishes
the rank-89 image from its rank-174 invariant closure, and exhaustive held-out
columns find no nonzero coefficient outside the sparse Hessian stencil.

## Source return

```text
SOURCE-CONFIRMS:
  the full adjoint-valued connection one-form and first-action grammar.

REPO-DERIVES:
  the K-lifted least invariant grade-two closure, its rank 594 common
  full-X4/both-branch subspace, and total local principal tangent 915.

SOURCE-SILENT:
  321 versus 915 versus 1571, the closure ranks, a derived equation quotient,
  BV completion, action-parent selection and physical interpretation.
```

## Efficient inline specialist assessment

Every lens below is **ACTUAL MATH**.

1. **Layer-0 semantics — very high confidence.** Object: covector image,
   K-lift, invariant tangent and Noether image. Strongest attack: treating any
   two as the same. Cheapest test: the `89 -> 174 -> 594` rank separation.
2. **Prior art — very high.** Object: composition of v0.100, v0.121, v0.124
   and v0.125. Attack: rebuilding old coefficients. Test: hash-verified bank
   consumption and typed registry checks.
3. **Differential geometry — high.** Object: subspace of the source-owned
   adjoint one-form. Attack: a symbol-dependent subspace is not a field bundle.
   Test: span the full four-dimensional symbol basis and both branches.
4. **Representation/Clifford — high.** Object: selected real-Spin grade-two
   carrier. Attack: spatial relabelling may not intertwine the Hessian. Test:
   exact commutation with two generating spatial swaps.
5. **Variational bicomplex — high.** Object: symmetric Hessian of the first
   action. Attack: a residual Jacobian could give a different closure. Test:
   use the first-action second-variation packet only.
6. **Symplectic/BV-BFV — very high.** Object: pre-quotient cotangent lift.
   Attack: declaring off-slice equations constrained from an algebraic
   projector. Test: demand an action-owned differential and preserve the live
   epsilon boundary moment map.
7. **Operator/Krein — high.** Object: finite indefinite K-lift. Attack:
   calling it a positive Riesz construction. Test: retain exact balanced
   inertia `(637,637)` and make no positivity claim.
8. **Exact sparse algebra — very high.** Object: `5,642`-entry Hessian and
   Krylov closure over `QQ(sqrt(3))`. Attack: stencil omission. Test: symmetry,
   exhaustive held-out columns and independent Sage/FLINT reconstruction.
9. **Invariant theory — high.** Object: one tangent valid for all `q` and both
   branches. Attack: three orbit representatives are not a linear basis.
   Test: explicitly add the fourth `X^4` basis direction.
10. **PDE/microlocal — high.** Object: full observed principal-symbol family.
    Attack: fixed-symbol closure masquerading as operator closure. Test: use
    the four-symbol span; keep lower order and domains open.
11. **Adversarial scope — very high.** Object: a proper intermediate tangent.
    Attack: `915` could be advertised as the complete GU tangent. Test: retain
    derivative jets, lower order, gauge/ghost, parents and global domain as
    open.

## Hostile disposition

Verdict:
`CANDIDATE_SURVIVES__EXACT_LOCAL_PRINCIPAL_MINIMAL_COMMON_EXTENSION594_TOTAL915`.

The review killed the first attractive disposition (`174`) and the second
(`464`) because neither was a common field tangent for the full observed
symbol family. The rank-`594` result survives the full-`X^4`, both-branch,
independent exact replay. It remains local principal and selected-Spin only.

## Progress and next gate

```text
Ledger v0.126 — 82/82 mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue 84; conditional action-parent range 84..86
Scoped quotients 5

headline_delta: none
conditions_closed: 2
  - least common local-principal source tangent is rank 594, total 915
  - no currently owned Noether/BV differential authorizes the rival quotient
conditions_opened: 0
remaining_named_conditions: 2
```

Next build the lower-order and derivative-jet first-action Hessian on the
derived `915` tangent and test whether it remains closed. In parallel, seek a
source-natural representation-theoretic description of the rank-`594`
subspace; its exact algorithmic construction is sufficient for this gate but
not yet a global subbundle theorem. Only an independently constructed BV
differential can revive the equation-quotient route.

Evidence:

- primary exact probe: `48/48 PASS`;
- independent Sage/FLINT: `19/19 PASS`;
- P1/P2/P3 unchanged and unused.
