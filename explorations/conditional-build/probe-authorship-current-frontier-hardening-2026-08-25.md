---
title: "Probe-authorship hardening: current-frontier detectors fail closed"
status: active_research
doc_type: internal_certificate_hardening
created: "2026-08-25"
grade: "MECHANICAL FAILURE-DETECTION HARDENING; NO NEW SCIENTIFIC RESULT"
target_claim: internal target current-frontier probe failure detection; verdict 20 authored detector defects are repaired and the repository-wide lint ratchet is lowered from 254 to 233
canon_verdict_change: none
---

# Probe-authorship current-frontier hardening

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: INTERNAL_STRUCTURAL_ONLY`

Scope: mechanical failure detection in the branch-relative certificate corpus.
This artifact does not rederive, strengthen or weaken any GU, comparator,
source, ledger, SG4, prediction, paper or canon claim.

```gu-typed-objects
result: twenty probe-authorship defects are repaired and the global ratchet now fails on any increase above 233
carrier: repository-native Python propagation probes and the probe-authorship lint corpus LAYER=UNTYPED CHIRALITY=N/A
pairing: NONE
real_structure: N/A -- detector syntax has no scientific real-form content
grading: mechanical certificate hardening only
action_owner: N/A -- no scientific action owner is created or changed
target: make missing objects, prose reflow, logical negation and baseline regression fail closed MAP-TYPE=evaluation
```

## Preflight bookend

The current source-to-proof residual is intentionally `UNTYPED`, the
phenomenology disposition register is terminal on all 91 rows, and the named B2
action-root set is empty. Prediction confrontation and the independent B5
branch retain exact external or owner-structure dependencies. The strongest
concrete result-hardening defect was therefore the current-frontier certificate
cohort itself.

Before the repair, the authorship lint reported 253 violations across 971
probes while its ratchet still allowed 254. The admitted cohort contained:

- missing-row lookups that could raise `StopIteration` instead of returning a
  controlled failure;
- multiword assertions against raw wrapped prose;
- two predicates whose positive token was a substring of its own negation; and
- historical propagation checks that had become stale against current owner
  state even though their underlying results remained valid.

The selected route was mechanical and meaning-preserving: supply lookup
defaults, flatten whitespace only at prose-comparison boundaries, require word
boundaries for the two logical tokens, reconcile historical/current assertions,
and then lower the ratchet only to the count actually reproduced. Broad
scientific recomputation was rejected because it would reopen completed or
unowned gates rather than test this defect.

## Result

The repaired cohort now distinguishes historical result custody from current
agenda state. The Pin14 probe validates an ancestral, at-most-one-commit status
basis instead of pinning a 2026-08-23 hash forever. The L7--L10, T3 and LT-GR8
propagation probes preserve their original result checks while following the
current empty-B2-root and proof-kernel completion surfaces. The two source/
boundary predicates require standalone `conditional`, `formal` and `physical`
tokens, so `unconditional` or `informal` cannot satisfy them.

The coupled certificate records 233 residual violations across the resulting
972-probe corpus, down 20 from the entering 253/971 state. The lint baseline is
ratcheted from 254 to 233; a one-defect regression is now red instead of being
absorbed by stale headroom. Residual debt remains explicit and is not presented
as a zero-defect corpus.

## Postflight and hostile review

- **Strongest overclaim:** detector hardening does not independently verify the
  scientific claims those probes guard; it improves how regressions are caught.
- **Strongest contrary case:** flattening prose could accidentally widen a
  phrase match. The repair flattens whitespace only, preserves exact words and
  punctuation, and replays every touched baseline and available planted control.
- **Weakest reproducibility seam:** the two exact algebraic probes use their
  native SymPy/Sage environments. Both were replayed after the word-boundary
  change, including the 35/35 Lie-closure and 24/24 cotangent-parent results.
- **Hostile controls:** the new coupled probe catches four of four planted
  regressions: missing-default lookup, raw wrapped prose, negation-satisfiable
  token, and lint/registry ratchet mismatch.

No source adherence or polarity, scientific verdict, action owner, ledger row,
SG4 phase, prediction, confirmation, paper, canon verdict, release readiness or
public posture changes. The next run rebuilds the substantial frontier and does
not repeat this hardening without a new concrete detector defect or a fresh
measured baseline decrease.
