---
title: "Conditional Physics Ledger v0.256"
status: research
doc_type: ledger-summary
created: 2026-08-14
---

# Conditional Physics Ledger v0.256

Ledger v0.256 preserves all 82 verdicts and records one distance-only migration
on `LT-SM8`. It sharpens, but does not promote,
`HYP-TW-COHERENCE-01`. Ledger v0.255's local-twistor connection, `Pi4`
carrier adapter and Bach-flat detour classification remain unchanged. Ledger
v0.254's endpoint coadjoint result and four boundary/BFV migrations also remain
unchanged.

The universal K77-typed adapted-connection current at the actual `4+10`
dimensions now constructs at exact algebraic grade. For

```text
A = [ a   -b^dagger ]
    [ b    c        ],
```

with actual `4+10` blocks and explicit seven-plus/seven-minus metric, the
curvature splits into Gauss, Codazzi and Ricci blocks and the total
Yang--Mills current splits into tangent, normal and mixed equations. The mixed
coefficient `b` is raw `II`; its Codazzi curvature and the total current are
different objects.

Two exact controls reject the previous equivalence in both directions:

```text
nonzero symmetric II + flat total connection  => total YM with II != 0,
II=0 + non-YM diagonal connection              => total YM fails.
```

Thus `II=0` is neither necessary nor sufficient for total Yang--Mills. A
reference-normalized replacement `II^H=II-II_ref` also changes the connection:

```text
F(A-K_ref)=F(A)-D_A K_ref+K_ref wedge K_ref.
```

The action cannot insert normalized `II^H` into the detour current without
constructing the corresponding normalized connection.

The surviving geometry separates two variational fields. A moving reduction
involution `R=diag(+I4,-I10)` satisfies

```text
D_A R=[A,R],
```

and exactly measures the mixed connection block. Its bending energy
`|D_A R|^2` yields a harmonic-reduction equation and contributes the connection
current `[R,D_A R]`. For the coupled functional

```text
S[A,R]=(1/4)||F_A||^2+(lambda/2)||D_A R||^2,
```

the connection equation balances the Yang--Mills current against the reduction
current. It is not generally source-free Yang--Mills. Therefore the ordinary
Bach/Yang--Mills detour need not close on the coupled shell.

The best geometric hypothesis is now a coupled moving-reduction detour/BV
complex. The tangent-plane Gauss/reduction field owns bending; the connection
owns curvature and the detour. A coupled complex would have to absorb the
reduction current in its next differential. This complex, its action owner,
normalized connection, factorization through the GU rolled operator, endpoint,
domain, positive pairing and physical cohomology remain open.

```text
Ledger v0.256 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Tightness: T4x1 T3x3 T2x1 · scoped quotients: 5
Headline delta: none
```

Next: freeze the action-owned `(A,R)` pair and normalized connection; linearize
the coupled Euler--Lagrange and moving-reduction gauge maps; build the first
coupled BV/detour compositions; and test whether the reduction-current map
cancels the ordinary detour obstruction on the coupled shell. Only afterward
attempt the `Pi4 -> ker Gamma14` push and rolled-operator factorization. Preserve
the independent endpoint-invariant gate and do not infer superposition or a
decoherence rate from the coupled classical identities.
