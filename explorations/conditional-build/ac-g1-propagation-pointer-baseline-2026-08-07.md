---
artifact_type: propagation_result
created: 2026-08-07
title: "AC-G1 propagation: scope Sp(64) to Cl(9,5), preserve the settled Cl(7,7) anomaly construction gap, reconcile the live pointer, and deposit inherited gate debt"
status: verified_scoped_correction
canon_verdict_change: "scope correction only; no anomaly-cancellation promotion"
---

# AC-G1 propagation, pointer reconciliation and inherited baseline

## Outcome

The unfinished propagation wave is now complete.

1. The `Sp(64)` response to Nguyen's `U(128)` pincer is explicitly scoped to
   the conditional quaternionic `Cl(9,5)=M(64,H)` horn on current canon and
   research-status surfaces.
2. The settled real `Cl(7,7)=M(128,R)` reconstruction is not credited with
   that result. Its native replacement group, local `I_16`, and global
   Dai-Freed/bordism receptacle remain open constructions.
3. Historical ledger row `AC-G1` is retained as superseded. Successor
   `AC-G1a` is `NEEDS/MISSING_CONSTRUCTION`; this removes one stale-premise row
   from the adverse bucket without claiming anomaly cancellation.
4. The shared Eric/Curt campaign pointer now defers to the current functional-
   channel contract instead of naming the historical PW2F successor as the
   active cross-campaign gate.
5. The 49 branch-integration gate failures are deposited as historical
   inherited baseline debt with zero regressions and zero fixes at merge time.
   They are not asserted to be the live failure count at current `HEAD`.

## Layer 0

Four objects had been collapsed:

- a real Clifford horn;
- the automorphism/gauge group available on that horn;
- defusal of one specific critique;
- full local and global anomaly cancellation.

`Sp(64)` is tied to the right-quaternionic `Cl(9,5)` carrier. The settled
`Cl(7,7)` carrier is real and does not inherit that group or its pincer-defusal
argument. Conversely, removing the stale sentence does not prove that the
settled horn is anomalous; it exposes a missing horn-native computation.

The baseline has a second Layer-0 fence: the 49 failures characterize the
August 6 integration comparison, not today's repository state. The durable
record preserves the historical count, the two parent revisions, the
`44+5` provenance split, and the fact that the integration introduced no
regression. It deliberately does not manufacture an exact per-gate manifest
that was not deposited by the integrating agent.

## Evidence

- `CB-C:G1` independently identified the disconnect and preregistered the
  minimal repair.
- `lab/process/layer0-fork-registry.yaml` settles the active carrier at
  `Cl(7,7)` and prohibits silent transfer of right-`H` machinery.
- `4968884e5978874fca8c3e9a5df4209cb77473f1` records the branch integration,
  its two parents and a 49-failure differential sweep with no regression or
  fix.
- `private continuity record` preserves the integrating agent's finer
  `44` common-line plus `5` branch-tip classification. That private note is
  cited as provenance, not as a scientific certificate.

## Ledger movement

```text
Ledger v0.50 — 82/82 active targets mapped
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous real + >=19 function-valued + 9 open forks
```

No target was deleted. `AC-G1` remains in history with a supersession edge to
`AC-G1a`. The active denominator stays 82; record count rises from 83 to 84.

## What this does not claim

- no `Cl(7,7)` anomaly cancellation or anomaly no-go;
- no transfer of `Sp(64)`, right-`H`, or `BSp(64)` bordism to the settled horn;
- no change to the current odd-curvature/soldering Build frontier;
- no change to P1/P2/P3, residue, quotient count, Curt separation, third-lane
  status or public posture;
- no claim that the current process-gate sweep has exactly 49 failures.

## Next

Resume the authoritative Build frontier: construct a source-native odd
augmented-torsion/translation-curvature packet or richer moving
epsilon/soldering owner, then construct the null screen and test total Bianchi
and raw-`Upsilon` naturality. The bounded D2 source-locus/survivor/index-transfer
wave remains a separate rank-three task and has not been silently counted as
done here.
