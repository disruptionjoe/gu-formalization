---
title: "K84 local-frame factorization universal-coupling wave"
status: active_research
doc_type: reverse_scaffold_local_frame_factorization_result
date: 2026-09-01
claim_ceiling: exact local-Lorentz invariant-tensor factorization and preferred-vector countermodel for the repository-owned first-derivative observed-quotient scalar coframe class; no source full-carrier action, dynamical coframe Euler equation, physical total stress, nonlinear gravity, prediction, confirmation, or verdict
manifest: lab/process/k84-local-frame-factorization-universal-coupling-wave.json
probe: tests/channel-swings/k84_local_frame_factorization_universal_coupling_probe.py
---

# K84 local-frame factorization universal-coupling wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact local-frame invariant-tensor factorization theorem and preferred-timelike-vector nonselection control
carrier: first jets of the repository-owned real rank-960 observed quotient W, represented as a real scalar multiplet with coframe index a and internal index A LAYER=conditional CHIRALITY=N/A
pairing: an arbitrary symmetric internal form H, positive only when the universal-cone corollary is invoked ON=repository_owned_candidate_family
real_structure: real scalar multiplet, real 1+2 Lorentz coframe, with the invariant classification extending to 1+d for d at least 2
grading: the existing K-to-V-to-W short exact sequence and minimal abelian BV grading; neither supplies the local frame symmetry or removes preferred tensors
action_owner: repository-owned reverse-scaffold first-derivative coframe-index action class; not source-selected or attributed to Weinstein
target: local-Lorentz factorization, universal characteristic cone and preferred-vector spurion boundary MAP-TYPE=classification
```

## Local-frame invariant-tensor theorem

Let the principal part of a real scalar multiplet on a local coframe be

```text
L_pr = 1/2 C^{ab}_{AB} (e_a psi^A)(e_b psi^B),
```

with `C^{ab}_{AB}` symmetric in the coframe indices and in the internal
indices. In `1+2` dimensions, infinitesimal local Lorentz invariance is the
linear system

```text
X C + C X^T = 0                 for X in so(1,2).
```

Solving it simultaneously for the spatial rotation and two boosts gives a
one-dimensional spacetime-tensor nullspace, spanned by
`eta=diag(1,-1,-1)`. Applying the same system to every internal coefficient
therefore gives the exact factorization

```text
C^{ab}_{AB} = eta^{ab} H_{AB}
```

for one arbitrary symmetric internal form `H`. Conversely every tensor of
this form is invariant. This is the coframe-index version of K83's boost
identity: full local frame invariance eliminates every second spacetime
principal tensor. The argument is invariant theory, not another modal scan,
and extends to `1+d`, `d>=2`, where the only invariant symmetric rank-two
tensor in the defining Lorentz representation is the metric line.

If `H` is positive on the complete observed quotient, the principal symbol is
`H eta^{ab} k_a k_b`; every internal mode therefore has the same unit Lorentz
characteristic cone. The internal eigenvalues rescale the equations but cannot
split the cone.

## Preferred-vector control: covariance is weaker

Diffeomorphism covariance alone does not impose the local-frame invariant
tensor line. If an independent unit timelike vector `u` is supplied, the
tensorial family

```text
C^{ab} = eta^{ab} H + u^a u^b J
```

is covariant and retains the spatial rotation stabilizer of `u`, but a boost
exposes the extra internal form `J`. With `u=(1,0,0)`, `H=I` and
`J=diag(0,3)`, the temporal form is `H+J=diag(1,4)` while the spatial form is
`H=I`. Both are positive, yet the two squared characteristic speeds are

```text
1 and 1/4.
```

Thus a coframe, tensorial covariance, translation conservation and positive
quadratic energy can coexist with multiple cones. Universal coupling follows
from the absence or dynamical elimination of preferred-tensor spurions plus
the full local frame symmetry, not from diffeomorphism covariance by itself.

## Hostile boundary

The strongest overclaim would be to identify the invariant metric line with a
GU source action. This theorem binds only a repository-owned first-derivative
scalar coframe class. The strongest contrary construction is the positive
preferred-vector family: it is covariant, has a common coframe and remains
multi-cone. The weakest propagation seam is the absent source/action-owned
full-carrier principal tensor, dynamical coframe equation and physical
total-stress descent.

Spin currents, torsion, metric-affine terms, derivative-dependent
improvements, nonlinear gravity, gauge reduction and emergent coframes lie
outside the classified scalar principal class. Local Lorentz factorization
does not make the coframe dynamical, prove its Euler equation, or show that the
actual GU carrier realizes the invariant line.

This is not a source-owned GU coframe or stress-tensor theorem. It supplies no
prediction, confirmation or held-out credit.

## Next condition

Identify the actual source/action-owned full-carrier coframe-index principal
tensor, prove that preferred-tensor spurions are absent or dynamically
eliminated, derive the coframe Euler and total-stress Ward identity, and
descend the metric factorization to physical cohomology.
