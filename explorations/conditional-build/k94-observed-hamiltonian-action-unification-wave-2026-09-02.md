---
title: "K94 observed Hamiltonian-action unification wave"
status: active_research
doc_type: reverse_scaffold_hamiltonian_action_unification_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K93_SINGLE_ACTION_STATE_SOLDERING_PROPAGATION_DETECTOR_OWNER
claim_ceiling: exact finite piecewise-Hamiltonian ownership of the K92 Gibbs density and K93 selector, propagation and detector dilation; no source-selected action or net, autonomous physical clock, continuum AQFT or microcausality, microlocal or Hadamard state, derived Born or temperature principle, prediction, confirmation, held-out score or verdict
manifest: lab/process/k94-observed-hamiltonian-action-unification-wave.json
probe: tests/channel-swings/k94_observed_hamiltonian_action_unification_probe.py
---

# K94 observed Hamiltonian-action unification wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: one finite Hamiltonian control action deriving the K92 Gibbs density and generating the K93 soldering selector, propagation and detector dilations
carrier: selected P8=(C2) tensor (C2) tensor (C2) subspace of the K91 physical l2 term, plus one C2 detector LAYER=observed CHIRALITY=N/A
pairing: imported finite trace state-effect pairing, with beta=1 Gibbs selection ON=repository_owned_hamiltonian_control_action
real_structure: computational-basis conjugation, preserved by the real generators up to exact unitary phases
grading: three site labels, one detector label and piecewise control time; no source BV, BFV or ghost grading
action_owner: repository-construction
target: common finite action ownership of state, soldering discriminator, propagation and record dilation MAP-TYPE=evaluation
```

Scope: this result binds only the selected finite tensor factor, one explicitly
scheduled repository control action and one detector. It does not bind a GU
source carrier, a source action, an autonomous physical clock, a continuum
local net, a microlocal state, a measurement postulate, a prediction or a
verdict.

## Inline preflight bookend

The problem-matched lens census covered Gibbs variational selection, finite
Hamiltonian control, tensor-factor interaction order, quantum-control
schedules, causal support, Stinespring dilation, exact rational states,
simultaneous action/net conjugation, gauge-basic descent, AQFT/microlocal
ownership, source custody and counterexamples. The decisive route was to ask
whether the K92 state and the K93 gates are exponentials or equilibrium data of
one declared action, rather than to add another fitted state or gate table.

Object-level retrieval found K92's rational Ising state and local record,
K93's fixed Hadamard pulse and phase-free CNOT control, K91's finite unitary
dilation, and earlier stationary-state controls. It found no packet that owns
all four finite objects through one control action. Gibbs distributions,
Pauli exponentials and controlled rotations receive no novelty claim.
Positive controls check normalization, exact Gibbs weights, generator
exponentiation, interaction order, propagation, effects, weights, endpoint
invariance and quotient descent. Negative controls change temperature,
exponents, interaction order, action/net conjugation, causal support, detector
completeness, gauge basicness and every promotion fence.

## One finite control action

On K92's three-site factor, define the equilibrium segment

```text
H_eq=-(ln 2)/2 (Z0 Z1+Z1 Z2),       beta=1.               (1)
```

The Gibbs density selected by the imported finite trace and equilibrium
principle is

```text
rho_eq=exp(-H_eq)/Tr exp(-H_eq),
p(s)=2^(1[s0=s1]+1[s1=s2])/18.                              (2)
```

Thus the previously supplied K92 rational state is exactly the beta-one Gibbs
state of (1). Its three distinct basis weights are `1/18`, `1/9` and `2/9`;
it is full support, stationary under (1), and invariant under the global bit
flip. Equation (2) derives the density *conditional on* the Gibbs principle,
the finite trace and beta. It does not derive those owners.

The same control-action specification contains the selector segment

```text
H_sel=pi/(2 sqrt(2)) (X0+Z0).                               (3)
```

Because `((X0+Z0)/sqrt(2))^2=I`,

```text
exp(-i H_sel)=-i (X0+Z0)/sqrt(2)=-i H0.                    (4)
```

The global phase is irrelevant to conjugation, so (4) generates K93's
Hadamard selector instead of stipulating it as a gate. Relative to the
standard K92 net, both terms in (3) are site-zero terms. Relative to the
`CZ_01`-primed net, `Z0` remains site zero but

```text
X0=(X0 Z1) Z1                                                (5)
```

is a two-site product. The fixed action therefore selects the standard net by
one-site implementability of its selector segment. Conjugating both the net
and the action restores one-site order, so the conclusion still belongs to an
action-net pair rather than the carrier alone.

## Hamiltonian-generated propagation and detector

Let `P1_j=(I-Zj)/2`. The propagation segment is

```text
H_prop=(pi/2) P1_0 X1,
U_prop=exp(-i H_prop)=P0_0 I1-i P1_0 X1.                    (6)
```

This is CNOT decorated by a control-sector phase. It has the same basis
permutation and the same pulled-back diagonal effect:

```text
(s0,s1,s2) -> (s0,s1 xor s0,s2),
U_prop* Z1 U_prop=Z0 Z1.                                    (7)
```

Add a detector initialized in `|0_D>` and the interaction segment

```text
H_det=(pi/2) P1_1 X_D,
U_det=P0_1 I_D-i P1_1 X_D.                                  (8)
```

Taking detector matrix elements gives

```text
M0=P0_1,          M1=-i P1_1,
M0* M0+M1* M1=I.                                            (9)
```

Composing (6) and (8) with (2) therefore gives the same input record
observable `Z0 Z1` and exact weights

```text
(Pr(0),Pr(1))=(2/3,1/3).                                   (10)
```

The complete endpoint marginal is unchanged, and zero extension over K91's
gauge summand commutes with every segment. The pulse, propagation and Kraus
maps are now generated by named action terms; the schedule, detector
initialization, trace/Born pairing and record interpretation are still inputs.

## Maximum licensed conclusion

One finite repository-owned piecewise Hamiltonian control action owns the K92
rational density at beta one and generates the K93 soldering pulse,
phase-decorated propagation and detector dilation. This replaces four separate
finite stipulations by one action specification and exposes the remaining
owners exactly.

It is not Weinstein's action. It is not autonomous: the segment order and
durations are supplied. Its Gibbs state is not a continuum Hadamard state, its
finite interaction order is not physical microcausality, and Hamiltonian-
generated Kraus maps do not derive the trace/Born pairing.

## Inline postflight bookend

- Strongest overclaim: calling a piecewise laboratory-style control action a
  source-selected autonomous dynamics or its Gibbs density a physical vacuum.
  The schedule, beta and equilibrium/Born principles remain supplied.
- Strongest contrary construction: simultaneous `CZ_01` conjugation of the
  action and net restores the primed one-site selector. The standard net is
  preferred only for the fixed action as written.
- Weakest reproducibility seam: the finite basis, weights, Pauli exponentials
  and effects are exhaustive and exact, but a continuous-time source action
  and physical clock are absent.

The exact probe passes `24/24`; its hostile selftest catches `20/20`
mutations. No source, continuum, microlocal, Born, prediction, confirmation,
canon, paper or held-out status moves.

## Next condition

Replace the supplied control schedule by an autonomous action or causal
functor whose clock, equilibrium state, net and detector coupling are selected
on the physical quotient. Independently authenticate the source functional
complex before awarding any GU-native action, locality or state credit.
