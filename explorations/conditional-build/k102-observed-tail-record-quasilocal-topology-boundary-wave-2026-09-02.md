---
title: "K102 observed tail-record quasilocal topology-boundary wave"
status: active_research
doc_type: reverse_scaffold_tail_topology_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K102_MAJORITY_TAIL_QUASILOCAL_GNS_TOPOLOGY
claim_ceiling: exact quasilocal-norm nonconvergence and direct-sum product-GNS strong tail limit for the K99 majority effects only; no source net, local record, universal tail, Born, prediction, confirmation, held-out score, promotion or verdict
manifest: lab/process/k102-observed-tail-record-quasilocal-topology-boundary-wave.json
probe: tests/channel-swings/k102_observed_tail_record_quasilocal_topology_boundary_probe.py
---

# K102 observed tail-record quasilocal topology-boundary wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: majority-effect quasilocal nonconvergence and sectorwise GNS tail projection
carrier: two-level UHF quasilocal algebra with two disjoint product-state GNS sectors and their direct sum LAYER=observed CHIRALITY=N/A
pairing: product-state GNS vector pairing and imported trace/Born pairing ON=repository_owned_tail_topology_control
real_structure: occupation-basis conjugation and C-star adjoint
grading: finite-prefix support and two product tail sectors; no source BV, BFV or ghost grading
action_owner: repository-construction
target: quasilocal versus GNS record limit MAP-TYPE=inclusion
```

Scope: this packet types the K99 majority effects in the quasilocal UHF algebra
and in the direct sum of their two product-state GNS representations. It does
not construct a source local net or derive classicality/Born semantics.

## Inline preflight bookend

The route census covered projection norm, UHF inductive limits, product strong
laws, GNS representations, strong operator limits, tail events and gauge
basicness versus locality. K99 proves expectation/channel convergence and
names the tail event, but does not determine whether the internal majority
effects converge as observables. The selected route compares consecutive odd
prefix projections exactly.

## No quasilocal norm limit

Let `M_N` be the projection onto strict majority-one words in the first odd
`N` cells, embedded into longer prefixes by tensoring the identity. Choose a
word whose first `N` cells contain exactly `(N+1)/2` ones and append two zeros.
Then `M_N` evaluates to one while `M_(N+2)` evaluates to zero. Since both are
commuting projections,

```text
||M_N-M_(N+2)||=1.                                        (1)
```

Thus the sequence is not norm Cauchy and has no limit in the quasilocal
C-star algebra. This remains true even though every `M_N` is itself local.

## Sectorwise strong tail limit

In the Bernoulli product sector with one-probability `1/4`,
`||M_N Omega_0||^2` is the K99 error `e_N` and tends to zero. In the sector
with one-probability `3/4`,
`||(I-M_N)Omega_1||^2=e_N`. Finite changes do not alter the empirical limit,
so the same convergence holds on dense local-vector sets; boundedness of the
projections extends it strongly to each full GNS space. Hence in the direct-sum
representation,

```text
M_N  ->  T = 0 direct-sum I                               (2)
```

in the strong operator topology. `T` is the central sector/tail projection in
that represented von Neumann completion. Equation (1) proves that it is not a
quasilocal C-star element.

## Reduced map versus internal record

The K99/K102 reduced q operations still converge in norm after the product
state is appended and the apparatus is traced out. The internal effects do not
converge in quasilocal norm. State insertion and representation choice are
therefore load-bearing owners of the exact tail record.

Zero-extending `T` across the K91 gauge term can make it basic and
representative-independent. That does not make it spatially local, give it
finite causal support, place it in a source net or derive its Born meaning.

## Owner accounting and maximum conclusion

Repository-owned: (1)--(2), the topology separation and the basicness/locality
distinction. Imported: the UHF tensor net, product states, GNS direct sum,
majority readout, tail semantics and state-effect/Born pairing.

The maximum conclusion is a precise admission boundary: the three-wave
conditional record stack descends through K91 at the reduced-map level, but
its exact persistent internal record requires a representation-dependent tail
completion not selected by K91, modular flow or GU source material.

## Inline postflight bookend

- Strongest overclaim: calling `T` a quasilocal/source-local observable.
- Strongest contrary construction: a phase-coexistence or dissipative model
  may contain a genuine local order parameter or source-selected asymptotic
  algebra.
- Weakest reproducibility seam: the full strong-limit proof uses density of
  local vectors plus the product strong law; the probe certifies exact finite
  witnesses and cyclic-vector norms.

Delayed choice remains reserved and unscored. No source, Born, prediction,
confirmation, canon, paper or public posture moves.

## Next condition

Derive the representation/asymptotic algebra from a source causal action or
construct an autonomous infinite/open reservoir whose finite-work dynamics
selects it without importing the tail sector.
