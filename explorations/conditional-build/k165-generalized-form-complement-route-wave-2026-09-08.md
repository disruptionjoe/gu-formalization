---
title: "K165 generalized-form complement route wave"
status: active_research
doc_type: conditional_fixed_form_generalized_metric_complement_and_count_route_result
created: 2026-09-08
date: 2026-09-08
claim_ceiling: exact repository-owned route theorem for the supplied K139--K164 equal-coupling two-edge positive-Fock control that K162 dyadic spaces are conforming but not reducing for the physical free dispersion, that the K139 chart turns the physical count problem into the generalized pencil R-zM with an M-orthogonal complement, and that separate auxiliary-lambda norm bounds destroy the cancellation needed for coercivity; an exact generalized Schur control closes when the complete same-form complement is positive, but the native absolute anchor form, coercivity, residual and spectral floors remain absent, so no native count, K152 interval, physical/source selection, Born derivation, prediction or confirmation follows
manifest: lab/process/k165-generalized-form-complement-route-wave.json
solver: tests/channel-swings/k165_generalized_form_complement_route.py
probe: tests/channel-swings/k165_generalized_form_complement_route_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K165 generalized-form complement route wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: this packet stays on K139--K164's repository-supplied equal-coupling
two-edge hard-core signed point control and its positive particle/hole carrier.
It tests the finite-to-complete spectral route after K164; it does not select
the operator, extension, polarization, couplings, renormalized boundary
coordinate, domain, trial anchor or physical state.

```gu-typed-objects
result: K162 cell spaces are conforming but nonreducing for the physical free dispersion; the K139 inverse chart makes R-zM with M=S* S the exact fixed-form pencil, so complete counts require an M-orthogonal same-form complement or equivalent Lehmann--Goerisch flux certificate, while separate lambda norm bounds and K161 tails cannot supply absolute anchor coercivity
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), with K162 dyadic cylinder ranges and K139 conforming inverse-chart images in q=(0,0),(1,0),(0,1) LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing; in regular coordinates the Gram is M=S* S with exact K153 bounds 64/121 I <= M <= 64/25 I ON=repository_signed_point_control
real_structure: real dyadic indicators, CAR adjoint, momentum conjugation and the complete K162 signed flavor intertwiner; all count matrices are exact real symmetric forms
grading: conserved incidence charges q_i=n_i+N_i+-N_i-, hard-core n1*n2=0, dyadic level, exterior degree and generalized-pencil inertia
action_owner: repository-construction -- K139 fixes the conditional mathematical form, but Weinstein's source and a GU action do not select its extension, polarization, couplings, boundary coordinate, domain, anchor or state
target: correct same-form complement, cancellation-safe coercivity and generalized Schur or Lehmann--Goerisch data needed before K164 can feed a complete native packet to K152 MAP-TYPE=intertwiner
```

## Inline preflight bookend

K164 closes the self-adjoint half-weight topology and asks for one physical
K162 anchor with coercivity, residual, next-spectrum and left-floor data. The
obvious next step is a finite Ritz diagonalization plus a free-energy exterior
gap. Before paying for that spectrum, the reduction and metric must be checked.

Mechanism retrieval found K153's nonunitary chart and Gram interval, K156's
rank-one exterior criterion, K158's generalized pencil, K162's literal
dyadic carrier, K163's noncommuting rediscretization result and K164's exact
tail transfer. No held result proves that the K162 range reduces the free
operator, identifies the physical complement with the free Hilbert
complement, or turns K161's cutoff error into an absolute anchor bound.

The route census compared high-cutoff spectra, ordinary Ritz/min--max,
free-gap and Schur complements, generalized pencils, Kato--Temple residuals,
Lehmann--Goerisch, boundary-triple determinants, operator-valued
Birman--Schwinger counting and positivity cones. The reduction/metric test is
the cheapest route-changing condition. If it fails, a numerically precise
ordinary Ritz matrix still cannot certify the complete count.

`SC-META-53` remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain
`NEEDS`. The v0.263 ledger remains 88/88 mapped with `33 SAME / 22 DIFFERS /
31 NEEDS / 2 OVER_DETERMINED`. No source or ledger row changes.

## 1. The dyadic spaces are conforming, not reducing

K162's one-particle cell range consists of functions constant on each
positive-width dyadic cell. The physical one-particle dispersion is

```text
omega(p)=sqrt(1+p^2)+1/4.                              (1)
```

Its second derivative is `(1+p^2)^(-3/2)>0`; hence it is strictly convex and
nonconstant on every interval of positive width. For a cell indicator
`chi_I`, `omega chi_I` is not constant on `I`. Therefore

```text
Q_j H0 P_j != 0                                         (2)
```

at every finite K162 level. The spaces remain literal nested form cores and
their Gram congruence is exact, but they are not reducing spectral subspaces.
Their Ritz values supply conforming upper data only. A free complement floor
cannot simply be pasted beside the compressed matrix as if the cross block
vanished.

This is not an objection to Galerkin convergence. It identifies the residual
or Schur data a rigorous lower/count argument must include.

## 2. The physical complement is M-orthogonal

Write K139's inverse chart as `S=(1-G)^-1`. K153 gives the exact physical
Gram in regular coordinates,

```text
M=S* S,       64/121 I <= M <= 64/25 I,                (3)
```

and K158 gives the exact generalized pencil

```text
T(z)=R-zM.                                               (4)
```

Thus a physical trial space `S P_j` corresponds in regular coordinates to
`P_j` with metric `M`. Its physical orthogonal complement is

```text
Q_(M,j)={v : <u,Mv>=0 for every u in P_j}.              (5)
```

The free Hilbert complement `P_j^perp` is generally a different subspace.
K153 already proves `M` is not the identity, and the dressed boundary vector
has non-piecewise-constant tails. A complete exterior statement for K152 must
therefore bind `Q_(M,j)^*(R-bM)Q_(M,j)`, not `Q_j H0 Q_j` alone.

## 3. Separate auxiliary bounds destroy the needed cancellation

K156 writes the regular representative schematically as

```text
R=(H0+lambda)+S*[-lambda I-X]S.                         (6)
```

The large `+lambda` and `-lambda` are chart coordinates and cancel only in
the combined fixed form. If they are norm-bounded separately using K153's
`||S||<=8/5`, then even before the unknown `X` contribution the shifted
identity-piece budget at `lambda=256`, physical shift `6`, is

```text
256+6-256(8/5)^2 = -9834/25.                            (7)
```

Equation (7) is not a physical lower bound and does not prove the operator is
unbounded below. It proves that this separated estimate cannot certify
`H+6>=kappa(1+S)` with positive `kappa`. A cancellation-safe proof must bound
the combined fixed form or its Schur/Birman--Schwinger representation.

K161 supplies `|a-a_Lambda|<=epsilon A`, a cutoff-to-limit difference. It
does not supply `|a_Lambda|<=B A` or an absolute lower bound on the anchor.
Using the tail as the missing `B` is a type error.

## 4. Exact generalized Schur count

Let `u` be a trial vector, `P` its line in regular coordinates, and `Q_M` its
`M`-orthogonal complement. If

```text
<u,Ru>/<u,Mu> < b,
Q_M^*(R-bM)Q_M > 0,                                    (8)
```

then exact block congruence gives

```text
inertia(R-bM)=(1,0,n-1).                               (9)
```

Therefore the generalized pencil has exactly one eigenvalue below `b`, even
when the trial/complement coupling is nonzero. The solver checks (8)--(9) on
the exact rational control

```text
R=[[-2,1,1],[1,6,0],[1,0,5]],
M=[[2,1,0],[1,2,0],[0,0,1]], u=e1, b=0.                (10)
```

The trial quotient is `-1`, the `M`-orthogonal complement inertia is
`(0,0,2)`, and the full pencil inertia is `(1,0,2)`. This is a nonvacuous
certificate compiler, not native K139 spectral data.

Form-level Lehmann--Goerisch is an equivalent allowed route only if its flux
or auxiliary solve certifies the same complete fixed-form complement
information. A finite Ritz spectrum, K161 tail, essential edge or free gap
alone does not fill that field.

## 5. Native K152 replay

The chain now fixes:

- the K139/K156 fixed self-adjoint normal-ordered form;
- the physical generalized metric `M=S* S` and the correct complement type;
- the K162 conforming family and complete signed flavor transport; and
- the K164 cutoff-to-limit form transfer.

Native admission remains closed because no packet evaluates the anchor form
and Gram on a selected trial, proves positive coercivity for the combined
form, supplies the complete form-dual residual, or proves (8) and the native
left floor on `Q_M`. No native count or K152 interval is emitted.

## Inline postflight bookend

- **Strongest correction:** conforming dyadic ranges are not reducing for the
  physical dispersion, so ordinary Ritz plus a free gap is not a complete
  spectral decomposition.
- **Strongest typing result:** the physical complement is `M`-orthogonal for
  `M=S* S`; this is the exact K139 generalized-pencil geometry.
- **Strongest quantitative obstruction:** separately bounding the auxiliary
  `lambda=256` terms yields only `-9834/25` after shift `6`, so that method
  cannot prove positive coercivity.
- **Strongest positive result:** exact generalized Schur congruence closes the
  one-below-threshold count once the complete `M`-orthogonal complement is
  positive, including nonzero coupling.
- **Strongest overclaim:** “the negative separated budget proves an unbounded
  physical Hamiltonian.” Refused; it diagnoses a lossy estimate only.
- **Strongest contrary route:** a direct operator-valued Birman--Schwinger or
  boundary-triple determinant could certify the same complete count without
  a finite `Q_M` basis. It must still prove global monotonicity/count and the
  native left floor.
- **Weakest reproducibility seam:** any Lehmann--Goerisch packet must name the
  fixed `R,M` form, the auxiliary/flux solve and its complete-carrier error;
  “form level” alone is not a certificate.

All five admitted mathematical arcs were attempted inline. The free-gap
route was rejected at its reduction and metric prerequisites, the auxiliary
cancellation loss was quantified, the generalized count compiler closed on
an exact positive control, and native replay isolated the remaining same-form
data. No external retrieval or second writer was used.

## Next condition

Construct the first native `R,M` anchor on a small K162 trial space by
evaluating the **combined** fixed K139 normal-ordered form, not its separated
`+/-lambda` pieces. Compute the physical Gram and form-dual residual, then
prove positivity of `R-bM` on the complete `M`-orthogonal complement using
either an operator-valued Feshbach/Birman--Schwinger monotonicity theorem or a
form-level Lehmann--Goerisch auxiliary solve with an outward complete-carrier
error. Prove the native floor above `-5` in the same representation, apply
K164 only after `epsilon<kappa`, and transport `q=(0,1)` only through K162.
Do not substitute an essential edge, free complement, bounded contour island,
K155/K157 regulator matrix or K161 cutoff tail for those absolute data.

## Reproduction

```bash
python3 tests/channel-swings/k165_generalized_form_complement_route.py --demo
python3 tests/channel-swings/k165_generalized_form_complement_route_probe.py
python3 tests/channel-swings/k165_generalized_form_complement_route_probe.py --selftest
```
