---
title: "K129 K128 unreduced circle CCR CAR Gauss kernel wave"
status: active_research
doc_type: reverse_scaffold_unreduced_gauge_kinematics_result
created: 2026-09-07
date: 2026-09-07
target_claim: INTERNAL_TARGET:K128_UNREDUCED_CONNECTION_GAUGE_DOMAIN_EQUIVALENCE_SUCCESSOR
claim_ceiling: exact repository-owned regular continuum circle CCR-CAR kinematic representation of U(1)^8 with smeared connection/electric canonical pairs, exact nonprojective small and large gauge implementers, integral Wilson/charge covariance, gauge-invariant circle holonomy and a common invariant analytic core for the smeared fields and Gauss generators; the normalizable Dirac kernel is zero for every nonconstant gauge direction, so this regular kinematic Hilbert representation is not unitarily equivalent to K128's nonempty reduced physical Hilbert space, and no gauge-invariant interacting Hamiltonian domain, continuum Haar projector, rigging map, singular point-Fock extension, finite-coupling NESS/current, source/GU ownership, Born derivation, prediction, confirmation or holdout credit follows
manifest: lab/process/k129-k128-unreduced-circle-ccr-car-gauss-kernel-wave.json
probe: tests/channel-swings/k129_k128_unreduced_circle_ccr_car_gauss_kernel_probe.py
---

# K129 K128 unreduced-circle CCR/CAR Gauss-kernel wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: regular unreduced continuum circle U(1)^8 CCR-CAR kinematics with exact gauge implementers and a zero normalizable nonconstant-Gauss kernel
carrier: bosonic symmetric Fock space over L2(S1;C8) tensor C9 impurity tensor irreducible Cl_18(C) module C512 tensor antisymmetric Fock space over eighteen full Dirac species LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Fock pairings with Segal connection and momentum fields, CAR adjoint and Hermitian Clifford-Klein generators ON=repository_owned_regular_circle_gauge_kinematics
real_structure: real L2 connection/electric test space, complex Fock conjugation, CAR adjoint and integral U(1)^8 charge lattice
grading: total fermion parity and total integral U(1)^8 charge; local gauge symmetry is represented nontrivially before reduction
action_owner: repository-construction
target: gauge-group homomorphism on the unreduced regular kinematic carrier and Dirac-kernel comparison with K128 MAP-TYPE=homomorphism
```

Scope: this packet constructs one regular continuum kinematic representation on
the spatial circle. It proves exact covariance and domain statements for
smeared fields, Wilson exponentials, matter charges and gauge generators. It
also proves that this particular representation has no nonzero normalizable
vector satisfying even one nonconstant Gauss constraint. It does not exclude
cylindrical, polymer, compact-link, rigged, BRST-distributional or reduced
quantizations.

## Inline preflight bookend

The rebuilt frontier contained seven substantial arcs. Regular continuum CCR
kinematics and the Gauss spectral discriminator are independently executable;
local/large gauge composition, Wilson covariance, holonomy and the K128
comparison depend only on their outputs. A gauge-invariant minimally coupled
Hamiltonian is a further regularity gate, while singular point-Fock and
finite-coupling NESS work remain downstream. No qualified source action selects
the imported gauge group, representation, vacuum, charge lattice or state.

Mechanism retrieval separates the result from K124's finite lattice Haar
projector, K125's conditional vectorlike anomaly cancellation, K126's finite-
regulator Wilson/Klein action, K127's external smooth connection, and K128's
already-reduced axial/Coulomb theory. The route-changing lens census covered
CCR and CAR representation theory, loop groups, large gauge topology,
constraint spectra, direct integrals, analytic vectors, Wilson covariance,
constructive Hamiltonians, rigged Hilbert reduction, point interactions,
scattering/NESS, source fidelity and hostile philosophy of science.

The regular representation is selected because it answers the kinematic
question directly and makes the physical-kernel question decidable. The
fallback is a compact cylindrical or rigged representation if the regular
Dirac kernel vanishes. Brute-force mode enlargement is rejected: one
nonconstant gauge direction already decides normalizable invariance.

## 1. A regular unreduced connection/electric representation

Let `K_B=L2(S1;R8)` and let `F_B=Gamma_s(K_B^C)`. On the coherent-polynomial
analytic domain `D_B`, take Segal fields

```text
A(f)=Phi(f),                 E(g)=Phi(i g),
[A(f),E(g)]=i <f,g> I,       [A(f),A(g)]=[E(f),E(g)]=0.    (1)
```

Together with the K127 matter space

```text
H_M=C9 tensor C512 tensor Gamma_a(L2(S1;C36)),             (2)
```

this gives the unreduced kinematic Hilbert space
`H_kin=F_B tensor H_M`. The coherent-polynomial bosonic vectors with arbitrary
`L2` labels tensored with finite-particle CAR vectors form one dense invariant
analytic core `D` for the gauge algebra.
Every smeared `A(f)`, `E(g)`, bounded Wilson exponential, finite impurity
charge and smeared second-quantized matter charge is defined there.

This is a regular representation of the continuum smeared canonical algebra.
It does not define a pointwise connection operator: evaluation at one point is
not an `L2` test function.

## 2. Small and large gauge transformations are exact, not projective

For a periodic `alpha in H1(S1;R8)`, define

```text
U(alpha)=W(d alpha) tensor R_imp(alpha(x0))
                         tensor Gamma_a(M_alpha),          (3)
```

with signs chosen so `A` shifts by `d alpha`, an impurity state of charge
`q_v` receives its integral phase, and CAR species `e` receives the opposite
transition-charge phase. The Weyl displacements `d alpha` are real, so their
mutual symplectic pairing is zero. The impurity and CAR multiplication
representations are exact. Hence

```text
U(alpha)U(beta)=U(alpha+beta)                              (4)
```

with no Weyl or fermionic cocycle in this empty-Fock representation.

For `n in Z8`, the winding map `g_n(x)=exp(i n x)` is single-valued. Its
implementer uses the constant displacement `d alpha_n=n dx` and integral
matter phases. It is nontrivial on `H_kin`. The circle holonomy

```text
Hol_a=exp(i A_a(1_[0,2 pi]))                               (5)
```

is invariant because a winding shift changes its exponent by `2 pi n_a`.
Thus K128's missing local/large gauge and holonomy kinematics genuinely exist
here. The result is anomaly-free at the represented level because both the
real Weyl subgroup and ordinary second quantization carry exact group laws.
K125's vanishing left-minus-right charge matrix remains a compatibility check
for a polarized Dirac-sea representation, not a derivation from the source.

For an oriented arc not crossing the chosen cut,
`W_b(x0,y)=exp(i A(b 1_[x0,y]))` gains the endpoint phase
`exp(i b.(alpha(y)-alpha(x0)))`. The K124 charge identity
`b_e=q_v-q_u` and the opposite CAR charge cancel that phase term by term in
the K126/K127 defect. Wilson covariance therefore composes with the unreduced
representation at fixed nonzero smearing width.

## 3. Gauss generators share the analytic core

Differentiating (3) along a periodic small-gauge direction gives

```text
G(alpha)=E(d alpha)+Q_imp(alpha(x0))+dGamma(Q alpha),       (6)
```

up to the consistent convention signs already fixed in the neutral Wilson
term. The three summands strongly commute because they act on separate tensor
factors, and their sum is essentially self-adjoint on `D`. The domain `D` is
stable under every `U(alpha)` and under coherent-polynomial differentiation.
This is a common invariant analytic **core** for the smeared fields and Gauss
generators. It is not yet a common operator domain for a locally coupled Dirac
Hamiltonian.

Constant `alpha` tests total charge. Nonconstant `alpha` additionally moves the
bosonic connection coordinate and is decisive for the Hilbert kernel.

## 4. The normalizable Dirac kernel is zero

Fix any periodic nonconstant `alpha`; then `h=d alpha` is a nonzero vector in
`K_B`. The one-mode factor of `E(h)` is unitarily equivalent to ordinary
momentum `-i d/dx` on `L2(R)`, hence has purely absolutely continuous spectrum
and no eigenvectors. The remaining matter charge operator in (6) is
self-adjoint on the other tensor factor. In the joint direct-integral
representation, `G(alpha)` is multiplication by `p+q`. For every `q`, the
level set `p=-q` is a Lebesgue singleton, so Fubini gives

```text
ker G(alpha)={0}.                                          (7)
```

Therefore the intersection of all small-gauge kernels in `H_kin` is also
zero, and there is no normalized Haar projector onto a nonzero invariant
sector in this regular representation. Exact nontrivial gauge implementers do
not imply normalizable physical states.

K128's reduced neutral Hilbert space is nonempty because it has already
divided out the connection translations and solved Gauss law. Since one side
is nonzero and the regular Dirac kernel is zero, there is no unitary
equivalence between K128 and the normalizable kernel of this K129 parent.
Recovering the reduced theory requires an explicitly constructed rigging map,
distributional group averaging, compact cylindrical/polymer representation,
or direct gauge-fixing quotient. None is supplied here.

## 5. The interacting-domain and point arcs remain open

The bosonic Fock connection is an operator-valued distribution. `A(f)` exists
for `f in L2`, and Wilson exponentials are bounded, but `A(x)` does not exist as
an operator. Consequently the formal minimally coupled Dirac expression
`-i partial_x+Q.A(x)` and its square are not defined on `D` without additional
smearing, renormalization or a different representation. The free positive
matter/electric form plus the fixed-width Wilson defect can be written, but it
is not locally gauge invariant unless the covariant kinetic term is built.
K129 therefore owns no gauge-invariant interacting Hamiltonian or common
self-adjoint Hamiltonian domain.

This failure also does not repair K125's point-CAR divergence. No IBC,
counterterm, closed singular form or resolvent limit is constructed. Without
that interacting dynamics and an infinite-lead scattering or return-to-NESS
theorem, K115's `6561/256` reduced affinity remains distinct from a microscopic
field current.

## Inline postflight bookend

The regular CCR representation, exact local/large gauge group law, circle
holonomy, Wilson/charge covariance and common analytic Gauss core complete.
The physical-kernel discriminator is negative and corrects the naive lift:
normalizable Dirac invariants vanish for one nonconstant gauge direction, so
the nonempty K128 quotient is not the normalizable invariant subspace of this
regular parent.

- **Strongest construction:** a genuine regular unreduced continuum
  connection/electric CCR-CAR representation with exact nonprojective gauge
  implementers and integral winding covariance.
- **Strongest overclaim caught:** anomaly-free unitary gauge covariance does
  not imply a nonzero Dirac physical Hilbert kernel or a Haar projector.
- **Strongest contrary route:** compact cylindrical/polymer kinematics or a
  rigged distributional group average may recover K128, but must construct its
  physical pairing and quotient map explicitly.
- **Weakest reproducibility seam caught and repaired:** an initial draft called
  the CAR factor of the common core smooth. Multiplication by an `H1` gauge
  phase need not preserve `C-infinity`; the final core uses unrestricted
  finite-particle `L2` vectors, which are preserved because every `H1(S1)`
  phase is bounded in one dimension. The no-kernel theorem still uses the
  regular one-mode momentum spectral type and does not transfer to nonregular
  compact representations.

No source action, physical preparation, detector effect, Born rule, held-out
score, prediction, confirmation, canon, paper, release or public-posture
status changes.

## Next condition

Construct one compact cylindrical/polymer or rigged continuum `U(1)^8`
representation whose local Gauss reduction has a nonzero positive physical
pairing, carries the circle holonomy sectors and Wilson/Klein defect, and prove
its reduction equivalent to or a correction of K128. Independently define a
renormalized gauge-covariant Dirac Hamiltonian and the singular number-changing
Fock interaction by an IBC, counterterm, closed form or resolvent limit. Only
after an infinite-lead finite-coupling Moller/Ruelle or return-to-NESS theorem
may the reduced affinity be compared with a field current. A source/GU action
must still select every imported datum.

## Reproduction

```bash
python3 tests/channel-swings/k129_k128_unreduced_circle_ccr_car_gauss_kernel_probe.py
python3 tests/channel-swings/k129_k128_unreduced_circle_ccr_car_gauss_kernel_probe.py --selftest
```
