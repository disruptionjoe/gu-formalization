---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-06
status: CURRENT_APPEND_ONLY_LEDGER__GRADE1_SCHUR_LIVE__GENERIC_GRAPH_EINSTEIN_MATCH_RETRACTED__DISCRETE_TWO_MODE_CAUSAL_CANDIDATE_OPEN
machine_ledger: lab/process/conditional-physics-ledger-v0.36.json
predecessor: explorations/conditional-build/conditional-physics-ledger-v0.35.md
---

# Conditional physics ledger v0.36

## Meter

```text
Ledger v0.36 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 25 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

One verdict and five distances move.  Residue, quotient count and P1/P2/P3
do not.

## What moved

The source-required 196-dimensional grade-one bank has an exact invertible
selected Hessian with positive-`kappa_1` inertia `(97,99)`.  Including both
the curvature and formal `d_B T` source couplings gives full cross ranks
`13/15/15` and Schur ranks `13/15/14` on timelike/spacelike/null covectors.
The paired horizontal-plus-vertical observation receiver preserves this
cross.

At `kappa_1^2=1`, all three corrected quotient symbols have only the
rank-four gauge kernel.  Thus the graph-only pair of null tensor modes does
not survive generic full-source completion.  Exact Sage factorization finds
one positive coefficient locus with two additional null source modes,
`N2(kappa_1^2)=0`, positive root `3.175378...`, and proves it is disjoint from
all timelike and spacelike factors.  Its metric projection is not the old
graph TT plane modulo gauge, so physical spin-two and Green/domain typing
remain open.

`LT-GR1` moves from `SAME/DERIVED_CONDITIONAL` to
`NEEDS/MISSING_CONSTRUCTION`.  `LT-GR2b`, `LT-GR5`, `LT-GR6` and `LT-SM8`
receive distance-only migrations.  No value of `kappa_1` is selected and no
continuous residue is reduced.

## Current highest-information gates

1. **Causal mode typing:** type the positive `N2` two-mode kernel under the
   null little group and its Green form; decide whether it is the physical
   graviton or a mixed source characteristic.
2. **Physical reduction:** construct the common right-`H`/Krein domain and
   odd BV/BFV totalization on that coefficient locus.
3. **Second-layer owner map:** separately construct or refute
   `I2B <-> ||II||^2` for `LT-GR3` before Q1.

The graph theorem remains exact on its declared constant-torsion subspace,
but it is no longer used as evidence for the completed source action.
