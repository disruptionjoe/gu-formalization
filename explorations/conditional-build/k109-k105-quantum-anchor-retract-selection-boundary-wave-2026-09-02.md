---
title: "K109 K105 quantum-anchor retract-selection boundary"
status: active_research
doc_type: conditional_state_observable_retract_nonselection
created: 2026-09-02
date: 2026-09-02
claim_ceiling: exact equivariant boundary transporting the minimal commutative BRST/CHSH result and one explicitly imported real-quantum calibration interface across all 256 K105 positive retracts; no physical quotient, Born, source-action, prediction or confirmation result
manifest: lab/process/k109-k105-quantum-anchor-retract-selection-boundary-wave.json
probe: tests/channel-swings/k109_k105_quantum_anchor_retract_selection_boundary_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K109 K105 quantum-anchor retract-selection boundary

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: this packet asks whether the admitted matter-wave-interference and
Bell/no-signalling calibration anchors select one of the 256 positive K91
action/domain/Green retracts inside the frozen K105 carrier. It transports the
owned minimal BRST boundary and then adds a real-quantum interface as an
explicit comparator import. It does not attribute that import to GU.

```gu-typed-objects
result: every K105 positive retract carries the same commutative BRST CHSH ceiling and the same explicitly imported real-quantum anchor interface, so the frozen action plus calibration anchors select no retract
carrier: two K91 modes over each positive coefficient-blind K105 distortion line LAYER=observed CHIRALITY=N/A
pairing: K105 lowerer for retract transport; imported trace state/effect pairing for the rebit control ON=conditional_comparator
real_structure: real K91 histories and real symmetric rebit matrices with transpose star
grading: BRST ghost degree plus bipartite calibration tensor grading
action_owner: repository-construction; quantum state/effect tensor and Born data explicitly imported
target: positive-retract and physical-interface selection from frozen action plus calibration anchors MAP-TYPE=evaluation
```

## Inline preflight bookend

The frontier contains a precise unresolved junction. K105 has 256 positive
coordinate lines in the coefficient-blind sector, each supporting the exact
K91 action/domain/Green retract. K85's minimal off-shell BRST model has
commutative degree-zero cohomology and sharp CHSH ceiling 2. K108 separately
shows that a real-quantum interface realizes both calibration anchors without
selecting complex scalars or local tomography. None of those packets tests
whether the anchors distinguish the K105 retracts.

The cheapest route-changing discriminator is symmetry. Coordinate
permutations of the 256-dimensional positive blind block lie inside K106's
proved `O(256)` action symmetry. If both the owned classical boundary and the
imported quantum statistics are covariant under those permutations, the
anchors cannot select a retract from the frozen data. Another free-standing
quantum model, source search or generated operator would duplicate K82--K108
or cross an explicit ownership boundary.

## The owned classical boundary transports to every retract

For each positive coefficient-blind seed `v_i`, the K105/K91 construction has
a split injection and retraction preserving the free action, Euler operator,
closed domain/core, retarded/advanced Green maps and boundary form. The
minimal BRST differential is therefore the same on every transported copy:

```text
s g = c,     s p = 0,     s c = 0.
```

Its degree-zero cohomology is the commutative real star algebra of functions
of the physical `p` variables. On the complete binary joint-assignment
control, every pointwise CHSH value is `+2` or `-2`; hence every normalized
positive state has `|S| <= 2`. This boundary is exact on all 256 retracts and
cannot itself realize the Bell calibration value `2 sqrt(2)`.

That failure identifies an imported denominator. A noncommutative
quantization/deformation, positive state, tensor rule, domain and measurement
pairing are not supplied by the minimal cohomology merely because the free
action and Green maps transport.

## The imported quantum interface also transports—and selects nothing

Choose two K91 modes over any positive seed `v_i`. Because
`<v_i,v_i>_K=1`, the two-mode Gram matrix is `I_2` for every seed. Now add the
K108 rebit interface explicitly:

- local states and effects are real symmetric `2 x 2` matrices;
- probabilities use the imported trace pairing;
- composition uses the imported real Hilbert tensor product; and
- the Bell and interference states/observables are the K108 controls.

This gives the exact two-path probabilities `1,0,1/2`, CHSH `2 sqrt(2)`, and
maximally mixed Bell marginals. It also retains K108's non-local-tomography
boundary: local symmetric products span 9 dimensions inside the 10-dimensional
global symmetric space, with hidden direction `J tensor J`.

For any two positive seeds, a coordinate permutation sends one seed to the
other while preserving the K105 lowerer, Weyl coefficient, action, domain,
Green and boundary data. Transporting the rebit matrices along that
permutation leaves every displayed trace statistic unchanged. Thus the
calibration anchors break none of the frozen `S_256` symmetry and select no
positive retract.

There are 256 conditional interfaces in this coordinate family, not 256
physical GU states. Their multiplicity is a nonselection result because the
state/effect algebra, tensor product and Born pairing were added rather than
derived.

## Inline postflight bookend

- **Strongest overclaim:** “quantum observations cannot select a GU physical
  state.” Refused. This result binds the frozen K105/K91 carrier and the stated
  symmetry-equivariant interface class. A new owned coupling with nonzero
  blind-sector symmetry defect can reopen selection.
- **Strongest contrary route:** a source/action/boundary/observable packet that
  chooses one positive line or higher-dimensional positive subspace and owns
  the quotient, pairing, tensor rule, domain and dynamics. Preserved as the
  exact reopener; no such packet is currently supplied.
- **Weakest reproducibility seam:** merely counting 256 lines would not prove
  equivalent physics. The certificate separately checks the action-preserving
  `e_0/e_1` swap, classical CHSH face, positive two-mode Gram control,
  interference, Bell value, marginal and hidden composite direction before
  accepting the manifest ceiling.

The delayed-choice holdout remains reserved and unscored. No source action,
physical quotient/state, noncommutative observable owner, Born derivation,
prediction, confirmation, canon, paper, ledger or public posture moves.

## Reproduction

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-gu-formalization uv run --with sympy==1.14.0 python \
  tests/channel-swings/k109_k105_quantum_anchor_retract_selection_boundary_probe.py
UV_CACHE_DIR=/private/tmp/uv-cache-gu-formalization uv run --with sympy==1.14.0 python \
  tests/channel-swings/k109_k105_quantum_anchor_retract_selection_boundary_probe.py --selftest
```
