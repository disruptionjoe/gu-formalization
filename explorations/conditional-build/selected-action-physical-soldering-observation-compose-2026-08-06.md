---
artifact_type: construction_result
created: 2026-08-06
status: LOCAL_PRINCIPAL_PHYSICAL_SOLDERING_OBSERVATION_EULER_PREBOUNDARY_EXACT__FULL_NONLINEAR_SECOND_JET_OPEN
lane: "1"
functional_channels: [BUILD, SOURCE, COMPOSE, VERIFY]
source_return: SOURCE-CONFIRMS_AND_SOURCE-SILENT
ledger_rows: [LT-GR1, LT-GR2b, LT-GR5, LT-GR6, LT-SM8]
scripts:
  - tests/channel-swings/selected_action_physical_soldering_observation_compose_probe.py
registry: lab/process/selected-action-physical-soldering-observation-compose.json
---

# Selected-action physical soldering and observation composition

## Result first

The physical first-order chain now closes at its exact local principal grade.
This is the step that the preceding co-moving-frame theorem deliberately left
open.

The already-constructed gauge-rotated Levi-Civita derivative has rank ten on
the null Lorentz orbit. Its formal adjoint therefore reaches every symmetric
metric direction. The complete first-jet observation/equation dual is an
invertible block-triangular map, so it preserves all ten soldering directions.
Consequently a nonzero connection current produces a nonzero observed metric
Euler row; neither co-moving-frame naturality nor observation deletes it.

The derivative-bearing soldering map also contributes an exact preboundary
potential under integration by parts. That potential is nonzero for
unrestricted boundary variations and vanishes under compact Dirichlet data.
This is a presymplectic owner, not yet a reduced BFV phase space.

No new field, coefficient, selector or external datum was introduced. The
full nonlinear Euler problem still needs the second observation and soldering
jets. Diffeomorphism/odd super-IG BV, global Krein/Green closure and
unrestricted BFV remain open.

## Layer 0

| phrase | exact object here | not identified with |
| --- | --- | --- |
| frame transport | simultaneous change of metric, coframe, Hodge, Phi and fields | physical metric/soldering variation |
| Levi-Civita soldering | `D_g(Ad_epsilon Gamma_LC)[h]` modulo connection gauge | the full nonlinear ambient connection |
| observation | complete value plus tangential/vertical first section germ | naive form pullback alone |
| equation dual | inverse transpose of the complete germ map | a second defect action |
| metric Euler receiver | formal adjoint of the derivative-bearing soldering map applied to the connection Euler row | connection current by itself equals Hilbert stress |
| preboundary potential | boundary term generated by Green integration by parts | a reduced BFV phase space or physical charge |

The result is therefore about a local principal first-order variational chain.
It is not a claim of nonlinear stationarity, global bulk-shell faithfulness,
Einstein recovery, cosmology or unitarity.

## Source collision

The source inspection records three relevant Weinstein claims:

1. augmented torsion is a difference of two connections;
2. the gauge-rotated Levi-Civita connection occupies the contorsion slot; and
3. observation is a richer package than naive pullback of forms.

Those claims confirm the objects and route used here. The source does not give
the inverse-transpose equation dual, the exact rank-preservation theorem, the
formal-adjoint metric Euler row or the preboundary identity. Those are
repository-derived.

```text
SOURCE-CONFIRMS: gauge-rotated Levi-Civita, connection difference and richer
                 observation package
SOURCE-SILENT:   exact local chain, Euler receiver, preboundary potential,
                 nonlinear second jets and global BV/BFV/domain closure
```

## Exact construction

On a flat observed Lorentz background, for a symmetric metric variation `h`,
the Levi-Civita symbol is

\[
 L(k)h^\rho{}_{\mu\nu}
 =\frac12\eta^{\rho\sigma}
 (k_\mu h_{\nu\sigma}+k_\nu h_{\mu\sigma}
 -k_\sigma h_{\mu\nu}). \tag{1}
\]

The exact `64 x 10` matrix has rank ten for the null covector used in the
physical characteristic calculation. Gauge rotation is invertible and a
moving epsilon contributes only a connection-gauge image, so the quotient
rank stays ten. If `E_A` is the selected action's connection Euler covector,
the soldered metric contribution is

\[
 E_g^{\rm sold}=L(k)^T E_A. \tag{2}
\]

The probe gives a concrete rational `E_A` for which (2) is nonzero. More
strongly, `rank L(k)^T=10`, so the receiver spans all symmetric metric
directions at this grade.

For a section with first jet `J`, the complete derivative-direction map is

\[
 M_1(J)=\begin{pmatrix}I&J^T\\0&I\end{pmatrix},\qquad
 O_E=M_1(J)^{-T},\qquad L_E=M_1(J)^T. \tag{3}
\]

Both composites are the identity. Observation acts on derivative direction
and field carrier independently, hence

\[
 (O_E\otimes I_{64})L(k)
\]

still has rank ten, and lifting it returns `L(k)` exactly. This is the precise
local no-leakage statement. The tangential-only row retains a conormal kernel
and fails the planted control.

When the observation section moves,

\[
 \delta(M_1a)=M_1\delta a+(\delta M_1)a. \tag{4}
\]

The second term is nonzero on the exact rational witness. It is a response of
the already-present observation field, not a new external datum.

## Symplectic and variational consequence

Because (1) contains a derivative of `h`, inserting it into the first
variation cannot produce only the bulk formal adjoint. Green integration by
parts has the schematic form

\[
 \langle E_A,L(\nabla\delta g)\rangle
 =\langle L^!E_A,\delta g\rangle+d\Theta_{\rm sold}. \tag{5}
\]

The exact four-vertex difference complex verifies both terms:

\[
 \sum_i(h_{i+1}-h_i)e_i
 =\sum_{j=1}^2h_j(e_{j-1}-e_j)+h_3e_2-h_0e_0. \tag{6}
\]

The first sum is the finite formal-adjoint Euler row; the last two terms are
the preboundary potential. The unrestricted witness is nonzero. Dirichlet
endpoint variations kill the boundary term but do not prove that its
unrestricted BFV reduction is zero.

Thus the local selected-action chain now has the typed form

\[
 E_g^{\rm phys}
 =E_g^{\rm direct}+L^!E_A+E_g^{\rm obs},
 \qquad
 \Theta^{\rm phys}=\Theta^{\rm direct}+\Theta_{\rm sold}+\Theta_{\rm obs}.
 \tag{7}
\]

At principal first-order grade, every arrow in (7) is now owned and the
soldered/observation part is nonzero. The full nonlinear coefficients of (7)
still require second section and soldering jets.

## What this changes

Five ledger distances move. Nothing changes verdict, reason kind, residue,
quotient count or external datum:

```text
Ledger v0.28 — 82/82 active target rows mapped (100%)
33 SAME · 19 DIFFERS · 24 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

The former open item “insert the physical Levi-Civita/soldering and first-jet
observation response” is closed at local principal grade. The next gate is
strictly narrower: construct the second observation/soldering jets and derive
the full nonlinear Euler/presymplectic class before diffeomorphism/odd BV,
global domain and unrestricted BFV.

## Seven-axis disposition

- **Layer 0:** frame, soldering, observation, equation dual, Euler and
  preboundary objects are separated.
- **L1 syntactic:** every composed arrow is located in an existing owner.
- **L2 type:** derivative direction and field carrier are tensored, not
  conflated; current and stress remain distinct.
- **L3 algebraic:** rank ten, inverse-transpose no-leakage and Green identity
  are exact over rationals.
- **L4 geometric:** local observed Lorentz section germ only; global ambient
  reduction excluded.
- **L5 variational:** local principal Euler and preboundary owners close;
  nonlinear second jets and BV/BFV remain open.
- **L6 analytic:** no global Krein/Green or maximal domain claim.
- **L7 physical:** no Einstein, cosmology, particle, Q1 or unitarity claim.

## Constraint fence

```text
new fields: 0
new coefficients: 0
new selectors: 0
new quotients: 0
P1/P2/P3 consumed: 0
```

Curt remains formally separate inside the Eric lane. No third lane, canon
verdict, claim status or public posture is promoted.
