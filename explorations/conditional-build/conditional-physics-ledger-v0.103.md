# Conditional physics ledger v0.103

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.103.json`.

## Progress

`Ledger v0.103 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`; the fifth is upgraded from finite local to
  conditional compact-boundary functional scope

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.103 adds

The v0.102 edge geometry now has a strong functional completion on every
declared nonempty edge-torsor stratum over a compact 13-dimensional boundary.

The analytic typing is load-bearing:

```text
gauge and edge frames: H^8
connection/distortion: H^7
cotangent momentum: H^-7
```

The canonical form on `H^7 x H^-7` has a bounded inverse uniformly across
modes. By contrast, the tempting same-regularity `H^7 x H^7` form is weak:
its finite-cutoff singular values are nonzero but decay as `(1+n^2)^-7`, so
the inverse blows up in the continuum.

On the strong completion the edge dressing is a split submersion and its
kernel is exactly the residual gauge orbit. The quotient is the dressed
cotangent pair with its real vertical polarization. The charged horn also
completes strongly; its classical nonabelian current algebra closes with zero
central remainder for the selected canonical form. The same polarization
exists on both horns and therefore selects neither.

This is an ordinary even presymplectic reduction, not full BFV. Edge-torsor
nonemptiness/topology, common bulk Green/Krein-domain compatibility, ghosts,
BFV charge, CME, quantum measure/anomaly and physical horn selection remain
open. The selected Spin-native and expanded unitary action parents remain
distinct.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `4`; conditions opened: `1`.

Closed:

1. strong-versus-weak Sobolev typing of the boundary canonical form;
2. conditional compact-boundary strong edge reduction and kernel theorem;
3. classical charged current-algebra closure and zero central remainder;
4. real vertical polarization on both horns, proving it is not a selector.

Opened:

1. compatibility of edge-torsor topology and the strong trace spaces with the
   selected bulk operator/domain and odd BFV construction.

Remaining named conditions: `3`.

1. prove nonemptiness/classify the global edge-torsor strata, including any
   obstruction from nontrivial boundary bundles;
2. construct a common Green/Krein domain preserving the `H^7/H^-7` boundary
   traces and action evolution;
3. build the actual nonabelian odd BFV/BRST charge and CME, retaining the
   charged horn until a physical selector exists.

P1/P2/P3 remain unchanged and unused. No residue, independent quotient count,
fork-registry, canon, claim-status, public-posture, Curt-track or third-lane
status changes.

## Evidence

- `explorations/conditional-build/selected-k77-sobolev-edge-current-algebra-2026-08-08.md`
- `lab/process/selected-k77-sobolev-edge-current-algebra.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-sobolev-edge-current-algebra-review.md`
