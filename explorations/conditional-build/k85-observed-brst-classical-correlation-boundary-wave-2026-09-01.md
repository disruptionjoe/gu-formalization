---
title: "K85 observed BRST classical-correlation boundary wave"
status: active_research
doc_type: reverse_scaffold_repository_owned_brst_classical_boundary_result
created: 2026-09-01
date: 2026-09-01
claim_ceiling: exact minimal off-shell degree-zero BRST cohomology and sharp classical CHSH boundary for the repository-owned K77 interacting quotient candidate, with a normalized positive finite observation state; no nontrivial Koszul-Tate/BFV boundary ideal, noncommutative physical algebra, Born rule, source-selected quantization, Bell prediction, confirmation, or verdict
manifest: lab/process/k85-observed-brst-classical-correlation-boundary-wave.json
probe: tests/channel-swings/k85_observed_brst_classical_correlation_boundary_probe.py
---

# K85 observed BRST classical-correlation boundary wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: exact minimal degree-zero BRST cohomology and sharp classical CHSH boundary for the repository-owned K77 interacting quotient candidate
carrier: minimal off-shell BRST algebra on Phi=(psi,k) in W960+K960 and odd ghost c in K960, with a finite 16-point binary observation quotient LAYER=observed CHIRALITY=N/A
pairing: normalized positive probability functional on the commutative finite observation algebra ON=repository_owned_classical_quotient
real_structure: real functions with identity star involution
grading: psi and k have degree zero, c has degree plus one, and the minimal off-shell algebra has no negative-degree generator
action_owner: repository-construction
target: degree-zero BRST cohomology, positive-state descent and classical-versus-Tsirelson correlation boundary MAP-TYPE=quotient
```

## The concrete K77 minimal cohomology

Write the K77 ambient field as `Phi=(psi,k)` in `W960 direct_sum K960` and keep
the already-owned minimal abelian differential

```text
s psi = 0,       s k = c,       s c = 0.               (1)
```

On the polynomial off-shell algebra, (1) is `s=c partial_k`. A degree-zero
function is closed exactly when it is independent of `k`, so

```text
Z0 = functions of psi.
```

The minimal off-shell algebra has no negative-degree generators; hence its
degree-zero boundary space is `B0=0` and

```text
H0_s = Z0 = the commutative real star-algebra of functions of psi.   (2)
```

K84's state-annihilation criterion is therefore satisfied, but vacuously at
this minimal level: every normalized positive state on (2) annihilates the
zero boundary space. A nonzero equation-of-motion/Koszul--Tate or boundary
BFV ideal would require antifields, constraints, a domain and a fuller
differential not owned by this candidate. Calling (2) the complete physical
cohomology would overstate it.

## Sharp correlation boundary of the owned algebra

Take the finite binary observation quotient of (2) generated by four
commuting self-adjoint involutions `A0,A1,B0,B1`. It is the real function
algebra on the sixteen joint sign assignments. Every normalized positive
state is a probability weight on those sixteen points. Pointwise,

```text
C = A0 B0 + A0 B1 + A1 B0 - A1 B1
  = A0(B0+B1)+A1(B0-B1) in {-2,+2}.                    (3)
```

Consequently every descended positive state obeys the sharp bound

```text
|omega(C)| <= 2.                                      (4)
```

Deterministic assignments attain 2. The uniform state is normalized,
positive and gives zero. Thus the K84/Tsirelson ceiling is not saturated by
the algebra K77 currently owns: its strict upper slack is
`2 sqrt(2)-2>0`. A PR value of 4 cannot extend to a positive state on one
commutative joint-assignment algebra, because every point in its support
already satisfies (3).

## Scientific effect and boundary

The minimal BV/BRST completion closes the classical gauge quotient but does
not generate quantum noncommutativity. This is independent of `m2` and
`lambda`; both coefficients leave (1)--(4) unchanged. Any route to the
Tsirelson face now needs a new owner: an action-derived Poisson/BV bracket and
quantization/deformation, a noncommutative product on reduced observables, a
selected analytic domain, and a normalized positive state. Noncommutativity
alone would still not select the Born rule, tensor composition or measurement
dynamics.

The strongest overclaim would call the finite probability state a GU Born
state. The strongest contrary construction is K84's noncommutative matrix
quotient, which reaches `2 sqrt(2)` under stronger owned relations. The
weakest seam is the absent nontrivial Koszul--Tate/BFV boundary ideal and
action-owned quantization map. Delayed-choice entanglement swapping remains
reserved and unscored.

## Next condition

Construct an action-owned Poisson/BV bracket and quantization map or star
product on the K77 reduced observables, together with a selected domain and
normalized positive state. Then test whether the sharp ceiling remains `2`,
reaches `2 sqrt(2)`, or is strictly intermediate.
