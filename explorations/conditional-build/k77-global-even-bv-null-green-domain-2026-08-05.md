---
title: "K77 global even BV owner ledger, null 4+4+2 split, and conditional defect Green domain"
date: 2026-08-05
status: GLOBAL_GAMMA_EPSILON_SELECTED_SHIAB_PRIMITIVE_OWNER_AND_FORMAL_MINIMAL_EVEN_CME_COMPOSED__NULL_KERNEL_SPLIT_4_CONSTRAINT_4_GAUGE_2_PHYSICAL_EXACT__DEFECT_GREEN_DOMAIN_CONDITIONAL__LAMBDA_DEF_CHARGED_AS_84TH_PREQUOTIENT_REAL__GLOBAL_Y14_DOMAIN_OBSERVATION_AND_PHYSICS_OPEN
lane: 1
functional_channels: [BUILD, COMPOSE, SOURCE, VERIFY]
ledger_rows: [LT-GR1b, LT-GR2b, LT-GR2c, LT-GR2d, LT-SM8]
source_return: SOURCE-SILENT
---

# K77 global even BV owner ledger, null split and defect Green domain

## 1. Outcome

This wave closes three construction ambiguities and refuses one tempting
overpromotion.

1. The global K77 Clifford frame, selected moving Shiab, primitive `epsilon`
   derivative and complete homogeneous even Noether identity now form one
   typed owner chain. For the closed ordinary **homogeneous gauge** Lie
   algebra, that chain admits the standard formal minimal even BV action, so
   the gauge-sector classical master equation holds at formal
   algebraic/compact-core grade. The diffeomorphism and odd algebras are not
   included in that statement.
2. The predecessor's six non-gauge null directions are not six defects. The
   exact coupled null kernel has the following dimension filtration, not a
   canonical direct-sum decomposition:

   ```text
   10 characteristic directions
   - 4 harmonic-constraint violations
   = 6 constraint-compatible directions
   - 4 residual diffeomorphism gauge directions
   = 2 physical null polarizations.
   ```

   The two survivors have explicit plus and cross representatives.
3. On the flat globally hyperbolic observation background tested here,
   harmonic gauge gives a trace-reversed wave operator. More generally the
   same conclusion is conditional on the completed gauge-fixed defect
   operator being normally hyperbolic. Its auxiliary-field
   square/composition is then Green-hyperbolic.
   This constructs a four-dimensional defect Green complex, not the global
   coupled noncompact ultrahyperbolic `Y14` domain.
4. `lambda_def` is neither `kappa_1` nor a coordinate already present in the
   83-real packet inventory. It is the relative coefficient of the
   independently typed `X` action. The honest continuous prequotient count is
   therefore **84**, with a future normalization quotient still unranked.

The result advances `LT-GR2c` substantially, but it does not yet recover the
physical Einstein/cosmological equation, prove no-leakage under observation,
construct odd super-IG/BV closure, establish positivity, or build a vacuum.

## 2. Why this is composition rather than a restart

Four exact inputs already existed in separate artifacts:

| input | existing result | role here |
| --- | --- | --- |
| global frame | `gamma_epsilon=Ad(epsilon^-1)gamma_0` is global on the admitted oriented/time-oriented K77 branch | makes every local moving-Clifford owner bundle-level |
| moving Shiab | all displayed channels and `delta Phi_i=[Phi_i,chi]`; primitive `epsilon` Euler row exact | supplies the actual chain rule |
| selected product | `(comm,symi,symi)` is the unique displayed row satisfying principal Bianchi plus nonzero Riemann response | fixes the displayed product horn, without claiming universal Shiab uniqueness |
| full even totalization | `N_g=D_B^coad E_B+ad_T^top E_T+rho_epsilon^top E_epsilon+rho_Z^top E_Z+backgrounds+ghosts` | prevents a partial `D E=0` slogan from masquerading as the Ward identity |

The new work is the joint theorem, the null quotient, the coefficient
adjudication and the domain fence. No global Clifford or selected-Shiab rank
calculation is repeated.

## 3. Layer 0: objects that cannot be collapsed

| name | object meant here | common wrong identification |
| --- | --- | --- |
| primitive `epsilon` derivative | independent variation of the field `epsilon` through `B`, `T` and moving `Phi_i` | simultaneous infinitesimal gauge action |
| `E_epsilon` | Euler covector for that primitive variation | the Ward identity |
| homogeneous even Ward identity | coefficient of an arbitrary ordinary gauge parameter after every owner is varied and integrated by parts | odd supercharge algebra or CME |
| formal minimal even BV action | antifield completion for that closed even gauge algebra | full GU super-IG BV theory |
| characteristic kernel | kernel of the principal symbol at a null covector | gauge image or physical state space |
| harmonic constraint | four de Donder equations on the metric perturbation | gauge quotient |
| residual gauge | null diffeomorphisms preserving harmonic gauge | constraint violation |
| physical quotient | constraint-compatible characteristic kernel modulo residual gauge | every non-gauge null direction |
| defect Green complex | advanced/retarded propagation on globally hyperbolic `X` after gauge fixing | global `Y14` Hamiltonian or boundary theory |
| `lambda_def` | relative `S_Y`/`S_X` action coefficient | internal `T^2` gain `kappa_1` or abstract `source_norm` scaling |

This Layer-0 split is why the null result reverses the apparent verdict. A
control can verify “six non-gauge directions remain” and still ask the wrong
question. Four of those six fail the physical constraint; two are precisely
the waves a viable theory must retain.

## 4. Global primitive-owner chain

On the global K77 branch, write

\[
 \gamma_\epsilon=\operatorname{Ad}(\epsilon^{-1})\gamma_0,
 \qquad
 \Phi_i(\epsilon)=\operatorname{Ad}(\epsilon^{-1})\Phi_i^0,
\]

and use the selected displayed Shiab product

\[
 \mathscr S_\epsilon(F)=
 \Phi_1(\epsilon)\mathbin{\mathrm{comm}}*F
 -\frac12 *\Bigl(
  \Phi_1(\epsilon)\mathbin{\mathrm{symi}}
  *\bigl(\Phi_2(\epsilon)\mathbin{\mathrm{symi}}*F\bigr)
 \Bigr).
\]

For `chi=epsilon^-1 delta epsilon`,

\[
 \delta\Phi_i=[\Phi_i,\chi],\qquad
 \delta B=D_B\chi,\qquad
 \delta T=-D_B\chi
\]

when `varpi`, metric, density and section are held fixed. Therefore the
primitive row is

\[
 \boxed{
 E_\epsilon=D_B^!(E_B-E_T)
 +(D_\epsilon\mathscr S_\epsilon)^!K_{\mathscr S}
 } . \tag{1}
\]

Metric/Hodge, density, observation-section, `q`, moving-projector, boundary
and background variations remain their own primitive rows. They do not become
functions of `epsilon` merely because the total Ward identity contains them.

## 5. Complete homogeneous even Ward owner

Let `rho_a` denote the infinitesimal homogeneous gauge action on every
primitive field/background owner. After compact-core integration by parts,
the complete even identity has the typed form

\[
 \boxed{
 \mathcal N_a=
 D_B^{\mathrm{coad}}E_B
 +\operatorname{ad}_T^\top E_T
 +\rho_{\epsilon,a}^\top E_\epsilon
 +\rho_{Z,a}^\top E_Z
 +\rho_{g,a}^\top E_g
 +\rho_{s,a}^\top E_s
 +\rho_{q,a}^\top E_q
 +\rho_{P,a}^\top E_P
 +\rho_{\mathrm{bg},a}^\top E_{\mathrm{bg}}
 =0 . } \tag{2}
\]

Some displayed terms can vanish because their objects are gauge invariant or
dependent, but they must first be typed and discharged. Removing them by
silence would reproduce the repo's former owner-ledger failure.

Equation (2) is the actual invariant-action result. `D_B E_B=0` alone is not.

## 6. Formal minimal homogeneous-gauge BV completion

For the closed ordinary gauge Lie algebra `g_even`, introduce ghost `c` and
antifields for every primitive owner `phi`. The minimal action is

\[
 S_{\mathrm{BV}}^{\mathrm{even}}
 =S_{\mathrm{cl}}[\phi]
 +\langle\phi^*,\rho(c)\phi\rangle
 +\frac12\langle c^*,[c,c]\rangle . \tag{3}
\]

The classical master equation

\[
 (S_{\mathrm{BV}}^{\mathrm{even}},S_{\mathrm{BV}}^{\mathrm{even}})=0
\]

reduces term by term to:

1. invariance of `S_cl`, supplied by (2);
2. representation closure
   `[rho(a),rho(b)]=rho([a,b])`; and
3. the Jacobi identity of `g_even`.

The executable certificate verifies these identities on a nonabelian
`so(3)` control rather than hiding behind an abelian fixture. This establishes
the standard formal theorem's hypotheses and composes them with the actual
full-owner Ward identity.

The grade is deliberately narrow:

- classical and formal, not quantum;
- homogeneous even gauge algebra only, not the diffeomorphism BV algebra or
  the proposed odd square-root-of-connections extension;
- compact-core/local variational algebra, not a global analytic BV phase
  space; and
- no positivity or physical cohomology claim.

## 7. Exact null split

The repaired defect action has quadratic symbol

\[
 J(k)=
 \begin{pmatrix}
 0 & G(k)^\top W_{\mathrm{DW}}\\
 W_{\mathrm{DW}}G(k)&2W_{\mathrm{DW}}
 \end{pmatrix}, \tag{4}
\]

on `(h,v)`, with diffeomorphism tangent

\[
 D_0(k)\xi=(k_{(\mu}\xi_{\nu)},0).
\]

For the null representative `k=(1,1,0,0)`, exact rational arithmetic gives

\[
 \operatorname{rank}G=4,
 \quad \operatorname{rank}J=10,
 \quad \dim\ker J=10,
 \quad \operatorname{rank}D_0=4.
\]

Introduce the de Donder symbol

\[
 C_k(h)_\nu=k^\mu h_{\mu\nu}
 -\frac12 k_\nu\operatorname{tr}_\eta h . \tag{5}
\]

Then

\[
 \operatorname{rank}C_k=4,
 \qquad
 \operatorname{rank}(C_k|_{\ker J})=4,
 \qquad
 C_kD_0(k)=k^2\operatorname{id}=0 . \tag{6}
\]

Consequently,

\[
 \dim(\ker J\cap\ker C_k)=10-4=6,
\]

and the residual-gauge quotient is

\[
 \boxed{
 \dim\frac{\ker J\cap\ker C_k}{\operatorname{im}D_0(k)}
 =6-4=2 . } \tag{7}
\]

Explicit representatives are

\[
 h_+=dx^2\odot dx^2-dx^3\odot dx^3,
 \qquad
 h_\times=dx^2\odot dx^3,
\]

with their auxiliary lift `v=-G(k)h/2=0`. Both lie in `ker J cap ker C_k`,
and adjoining them to the four residual gauge columns raises rank from four
to six. They are therefore independent modulo gauge.

This is the desired result. The null cone is characteristic because it
carries two physical waves; exactness should not erase them.

## 8. Gauge-fixed defect Green complex

For non-null `k`, the same exact symbol gives

\[
 C_kD_0(k)=k^2\operatorname{id}_{4}, \tag{8}
\]

so harmonic gauge fixes the gauge direction away from the characteristic
cone. On the gauge-fixed metric field, the Einstein symbol is the wave symbol
times the four-dimensional trace-reversal automorphism

\[
 R(h)=h-\frac12\eta\operatorname{tr}_\eta h,
 \qquad R^2=1 . \tag{9}
\]

`R` is invertible and the DeWitt/Krein fibre pairing is nondegenerate. On the
flat globally hyperbolic Lorentz observation background used by the exact
symbol certificate, the gauge-fixed defect operator is therefore
Green-hyperbolic with advanced and retarded Green operators on compactly
supported sections. On a curved background this statement additionally
assumes that the complete lower-order curvature terms yield the standard
normally-hyperbolic gauge-fixed operator; that completion was not derived
here. Eliminating the auxiliary `v` then produces a composition of the
gauge-fixed operator with its formal adjoint, and the Green-hyperbolic class
is closed under composition.

The composition fact is supported independently by Christian Bär,
[*Green-hyperbolic operators on globally hyperbolic spacetimes*](https://arxiv.org/abs/1310.0738),
not attributed to GU.

The domain statement earned here is therefore:

```text
flat globally hyperbolic X, or a separately proved normally-hyperbolic completion
+ harmonic gauge
+ compactly supported test sections / spacelike-compact Green images
+ nondegenerate trace-reversed DeWitt pairing
=> conditional advanced/retarded defect Green complex,
   with two physical null characteristics preserved.
```

For a compact region with boundary, the predecessor's
`H^10 cap H^1_0 -> H^9` graph supplies the zero-Dirichlet-flux closure; without
that boundary condition the Green form is preboundary data. Neither choice is
a global coupled domain for `Y14`.

## 9. `lambda_def`: 84, not an 83/84 alias fork

CB-D's 83-real total is explicit:

| charged source | real count |
| --- | ---: |
| unified packet subtotal | 82 |
| previously uncharged gimmel block ratio | +1 |
| prior total before any quotient | 83 |

The selected support horn later writes a genuinely new relative coefficient

\[
 S=S_Y+\lambda_{\mathrm{def}}S_X . \tag{10}
\]

Layer 0 rules out the proposed aliases:

- `kappa_1` is the internal quadratic gain in the `T` sector. It changes the
  auxiliary equation inside `S_Y`; it is not the relative weight of a
  separately supported `X` action.
- `source_norm` is an abstract rescaling direction in PRED-NORM-RANK. That
  certificate explicitly says no invariant basis element contains it, and it
  is not one of CB-D's charged 83 coordinates.

An explicit same-source map could later prove that a normalization quotient
removes one combination. No such quotient has been ranked. Prequotient
accounting must therefore charge the written coefficient:

\[
 \boxed{83+\lambda_{\mathrm{def}}=84\text{ continuous reals before quotient}.}
 \tag{11}
\]

This retires the extra alias fork rather than pretending the physical freedom
has vanished. The discrete fork count returns from 11 to 10; the continuous
count becomes 84.

## 10. Constraint surplus and what actually moved

| item | before | after |
| --- | --- | --- |
| global full K77 Clifford frame | built | consumed, not recomputed |
| displayed Shiab product horn | selected in separate artifact | composed into global owner chain |
| primitive/full even owner ledger | separate partial surfaces | one typed global formal chain |
| formal minimal even CME | open | constructed for the closed homogeneous gauge algebra only |
| null non-gauge directions | six unresolved | four constraint violations plus two physical modes |
| defect Green domain | compact-core fragment | globally-hyperbolic gauge-fixed Green complex, conditional |
| full coupled `Y14` domain | open | open |
| continuous reals, prequotient | 83 lower / 84 upper | 84 |
| discrete forks | 11 | 10 |
| ranked physical quotients | one non-null local symbol quotient | plus one exact null constraint/gauge quotient; neither booked as residue reduction |

No new external datum is used. The result is information-gaining because it
turns an undifferentiated null obstruction into the correct physical two-mode
quotient and removes a bookkeeping ambiguity.

## 11. Seven-axis protocol

| layer | result |
| --- | --- |
| Layer 0 | primitive/gauge epsilon, Ward/CME, constraint/gauge/physical null directions, defect/Y14 domains and coefficient/rescaling are separated |
| L1 typed substrate | global K77 frame plus independently supported `X` defect action retained |
| L2 algebra | selected displayed Shiab and closed homogeneous even Lie action composed; minimal formal BV algebra typed |
| L3 geometry | global `gamma_epsilon` owns the moving Clifford/Shiab family on the admitted branch |
| L4 variation | primitive epsilon row and full Euler-owner Ward totalization assembled; moving metric/section/density/preboundary owners remain explicit |
| L5 quotient | exact null `10 -> 6 -> 2` dimension filtration and constraint/gauge quotient constructed; no canonical direct-sum claim; physical two retained |
| L6 analytic | conditional flat-background defect Green complex constructed; curved completion and global coupled noncompact `Y14` domain open |
| L7 physics | no observed Einstein/cosmological equation, positivity, vacuum, screening, magnitude or `w(z)` claim |

## 12. Source disposition

Decisive return: **`SOURCE-SILENT`**.

The primary GU material supplies the moving-frame/connection ingredients and
explicitly marks the upstairs multiple-time boundary problem as technical
debt. It does not print the formal BV completion, harmonic null split,
conditional defect Green complex, or relative-normalization identification.
The construction is therefore repo-native and source-compatible, not an
attribution to Weinstein.

## 13. Two-sided hostile boundary

### Summary-outruns-artifact charge

The exciting reading would say “the nonlinear BV and domain are solved.” The
artifact supports only formal homogeneous even CME plus a conditional
four-dimensional gauge-fixed Green complex. Odd super-IG closure, analytic BV
phase space and global `Y14` domain are excluded.

### Defends-superseded-object charge

The narrow-work failure would keep trying to eliminate all six null directions
because the predecessor called them non-gauge. That object is superseded by
the harmonic-constraint split: four fail the constraint and two are the
physical characteristic quotient. A future domain that kills the latter two
fails this gate.

## 14. Held-open boundary and next gate

Still unconstructed:

- a global coupled Krein/Green/BFV domain on the noncompact `Y14` geometry;
- odd Clifford/super-IG action closure and its master equation;
- moving observation-section no-leakage for the complete action and domain;
- a physical positive cohomology carrying the Standard Model spectrum;
- the nonzero vacuum branch and stress-energy/cosmological identification;
- screening or fixing of the common dark-energy amplitude; and
- any use of P1, P2 or P3.

The next gate is

```text
CONSTRUCT_MOVING_OBSERVATION_NO_LEAKAGE_AND_GLOBAL_COUPLED_Y14_KREIN_GREEN_BFV_DOMAIN_OR_SHARP_OBSTRUCTION__THEN_DERIVE_THE_OBSERVED_EINSTEIN_STRESS_ENERGY_AND_DYNAMIC_COSMOLOGICAL_EQUATIONS
```

That sequence keeps construction ahead of hardening. The domain/no-leakage
gate decides whether the assembled action actually reaches physics; only then
should the vacuum and cosmological equations be varied and compared.
