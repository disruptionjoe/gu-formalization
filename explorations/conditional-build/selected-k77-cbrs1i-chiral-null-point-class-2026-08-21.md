---
title: "Selected-K77 CBRS-1I chiral null point class"
status: active_research
doc_type: exact_stationary_point_class
created: "2026-08-21"
registry: lab/process/selected-k77-cbrs1i-chiral-null-point-class.json
probe: tests/channel-swings/selected_k77_cbrs1i_chiral_null_point_class_probe.py
grade: "EXACT RECONSTRUCTION-GRADE POINTWISE METRIC-STATIONARY CLASS OF THE SELECTED FIRST ACTION"
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_ACTION_PRIMITIVE_EPSILON_AND_METX_GRAMMAR__REPOSITORY_DERIVES_THE_CHIRAL_NULL_POINT_CLASS__SOURCE_SILENT_ON_THE_CLASS
canon_verdict_change: none
---

# Selected-K77 CBRS-1I chiral null point class

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: CBRS-1I two-sign chiral null pointwise metric-stationary class
carrier: Spin_equivariant_span{gamma_i,gamma_i_Omega} inside real u(64,64) point coefficients with volume sign not observed chirality LAYER=ambient CHIRALITY=N/A
pairing: selected K77 Clifford scalar-density pairing ON=real grade1_plus_grade13 point carrier
real_structure: real B-skew Clifford grades one and thirteen with Omega squared equal to one
grading: exterior form degree and Clifford grade
action_owner: repository-construction
target: complete point translation Spin-connection primitive-epsilon and intrinsic metric Euler covectors MAP-TYPE=evaluation
```

## Result first

The selected K77 first action has two nonzero, target-blind, exact point classes
that pass the complete point translation equation, the independent
Spin-connection equation, primitive epsilon, and intrinsic `MET(X)`
stationarity.

Let `Omega` be the real `Cl(7,7)` volume element.  It satisfies
`Omega^2=1` and anticommutes with every vector.  Freeze the smallest
Spin-equivariant extension of the homogeneous vector map:

```text
T_i(a,b) = a gamma_i + b gamma_i Omega.
```

Direct Clifford evaluation gives

```text
I(a,b) = 7 (a-b)(a+b)(208 a+1).
```

Its complete reduced critical set is

```text
(0,0),
(-1/312,0),
(-1/208,-1/208),
(-1/208,+1/208).
```

The older homogeneous point has density `7/292032`.  The two new points lie on
the exact null lines of the quadratic pairing

```text
Q(a,b)=7(a-b)(a+b)
```

and have density zero.  They can be written as the two Clifford-volume signs

```text
T_i = -(1/208) gamma_i (1 plus-or-minus Omega).
```

Every coefficient is nonzero and square-zero.  Both signs are retained; the
calculation does not select one by reference to observed chirality.

## Complete point equations

The two-parameter gradient is not the stationarity certificate.  The symbolic
Clifford adjoint evaluates every real pointwise translation direction:

```text
14 x 16,384 = 229,376 directions.
```

For each sign, the complete translation covector has support zero.  The
full-comparator momentum `E_B-E_T` is not zero as an unrestricted
`u(64,64)` covector: it has exactly two entries per form slot, `28` total, in
Clifford grades one and thirteen.  That is a type control, not a failure.  The
independent connection owner is real Spin grade two, and the fixed-`varpi`
Levi-Civita graph also lands in grade two.  The restriction of the momentum to
that owner is exactly zero for both signs.

Primitive epsilon closes separately.  The constant grade-one/thirteen
momentum has zero divergence at this zero jet, and direct moving-Shiab
evaluation vanishes for all `91` real Spin generators for each sign.

## Why the metric equation passes

CC-01 remains binding: `MET(X)` is the second action argument, not frozen
furniture.  At fixed `varpi`, the intrinsic metric equation has the owned form

```text
E_g = rho I + (D_g B_Z)^! (E_B-E_T).
```

On both new points, `I=0`.  The graph-visible grade-two momentum is also zero.
Therefore both terms vanish and the complete ten-cell intrinsic metric row is
zero.  No cosmological constant, trace counterterm, deleted field owner, or
target-facing coefficient was added.

This is the first CBRS-1 class admitted beyond the intrinsic metric gate.  It
is still a constant, pointwise, Spin-equivariant reconstruction.  It is not the
requested spacetime-nonhomogeneous global solution, and metric stationarity
does not by itself supply a global associated-bundle section, analytic domain,
full Hessian, stabilizer, spectrum, BV quotient, or observed physics.

## Reduced Hessian seed

The exact reduced Hessians at the two signs are

```text
b=-1/208:  [[-28,  14], [ 14, 0]],
b=+1/208:  [[-28, -14], [-14, 0]].
```

Both have determinant `-196`, so neither branch has a flat direction inside
the frozen Spin-equivariant two-plane.  This does not determine the complete
coupled Hessian.  Because the background mixes Clifford grades one and
thirteen, CBRS-1G's anisotropic grade-diagonal census cannot be transferred.
The next gate must freeze the complementary-grade orbit and the coupled
Spin-connection/gauge carrier before computing ranks or a spectrum.

## Prior-art and correction fence

CBRS-1A found the coefficient-anisotropic root and the older homogeneous root.
CBRS-1B through CBRS-1H closed the anisotropic point/action class for every
compatible formal jet.  None contains the exact value `-1/208` or this
vector-plus-volume-dual action class.  Earlier repository artifacts use
Clifford chirality projectors for different mirror, condensate, or representation
questions; they neither evaluate this first action on `T_i(a,b)` nor establish
these point equations.  Their existence is acknowledged as algebraic prior art
and not mistaken for this result.

All decisive CBRS inputs are dated after the canonical-source corrections.
CC-01 is applied directly.  The result does not consume the corrected
particle-chirality, observed-positivity, normal-bundle, or comparator readings.

## Hostile return and exact successor

- **Strongest overclaim:** pointwise intrinsic metric stationarity is not a
  source-owned or global physical vacuum.
- **Strongest contrary construction:** the complete Hessian may contain
  nongauge kernels outside the reduced two-plane; one may kill both branches
  before any spectrum is meaningful.
- **Strongest mistyping:** treating the full `u(64,64)` momentum as the
  connection equation falsely rejects the class.  The actual connection owner
  and metric graph are Spin grade two, where the momentum is zero.
- **Strongest target leak:** the two volume signs are both retained.  No
  observed handedness, particle rank, mass, charge, or ledger row selects one.
- **Weakest reproducibility seam:** the result depends on symbolic adjoints
  inherited from the exact full-action bank.  The probe replays that bank,
  checks held-out action samples, and fires off-null, homogeneous-density,
  owner-mistyping, and sign-deletion controls.

`CBRS-1J` must build the complete complementary-grade `T/T` Hessian together
with every Spin-grade-two connection equation, the pointwise Spin orbit and
stabilizer, and the primitive-epsilon quotient at both signs.  Only a surviving
complete field kernel may advance to the first symbol, global stabilizer,
`mu6`, `J`/Higgs, photon, extra-`U(1)`, or gravitational-spectrum questions.

No ledger verdict, canon, source ownership, residue, quotient datum, particle
assignment, prediction, confirmation, or public posture changes.

Reproduce with:

```bash
sage -python \
  tests/channel-swings/selected_k77_cbrs1i_chiral_null_point_class_probe.py
```

The exact probe passes `47/47`.
