# Conditional physics ledger v0.121

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.121.json`.

## Progress

`Ledger v0.121 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Six rows move in distance/evidence only. No verdict, residue, quotient, datum,
canon or posture changes.

## What v0.121 adds

The complete first-transgression-action connection Hessian has now been tested
from all `196` grade-one inputs to every `1,274` grade-two output covector on
both exact K77 branches.  The cross block is coefficientwise zero, including
the actual horizontal `24` and off-slice `1,250`.  The grade-one self block is
nondegenerate on both branches: rank `196`, exact inertia `(97,99,0)`, with the
two matrices unequal but Galois conjugate.

This means the minimum-known `321 = 125+196` selected tangent survives this
one connection-block closure test.  It does **not** mean `321` is the complete
field tangent or that the source selects it.  The known `1,571` low-grade
coordinates are likewise not a complete functional tangent, and the two
`U(32,32)` halves and full `U(64,64)` parent remain separate questions.

A planted control caught a potentially serious indexing error: the low-grade
basis is interleaved by form slot, so its first `196` entries contain only `28`
grade-one elements and `168` grade-two elements.  Grade labels, not positions,
must select Clifford banks.

## Frontier

Conditions closed: `2`; conditions opened: `0`; remaining named: `2`.

Next complete the metric/epsilon and epsilon/epsilon Hessian blocks and test
the remaining off-slice leakage routes, then construct the expanded-parent
Hessians.  Keep the first and residual-square actions separate.  Only after
operator completion add gauge fixing and ghosts and test `Dmax/Dmin`,
Green/Krein realization and coupled BV-BFV.

P1/P2/P3 remain unused.
