# Conditional physics ledger v0.79

Status: append-only successor to v0.78.

```text
Ledger v0.79 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Tightness — T4×1 · T3×3 · T2×1
Scoped quotients ranked — 5
```

## What moved

Five gravity/source-action rows move only in distance, evidence and mapping
grade: `LT-GR1`, `LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`.

The old successor asked for an integrable physical observation section on
arbitrary `X` whose ordinary pullback was faithful.  v0.79 separates and
decides those burdens:

- local holonomic first/second section jets exist exactly;
- spin does not imply a global Lorentz section on arbitrary `X` (`S^4` is the
  counterexample);
- ordinary pullback always has rank four and a ten-dimensional conormal
  kernel;
- the selected augmented-torsion action emits a nonzero Euler covector into
  that kernel; and
- the exact complete `4+10` equation receiver detects it.

The new rank-one gate is therefore a construction fork: retain and physically
type the complete `4+10` equation system, or derive a source-owned conormal
constraint/BV differential and quotient.  Global `tau_A0`/BFV and a common
Green/Krein domain remain downstream.

## What did not move

Coverage, verdict counts, residue, tightness, five scoped quotients,
P1/P2/P3, canon, claim status and public posture are unchanged.  No sixth
quotient is booked by naming the BV horn.

Machine truth:
`lab/process/conditional-physics-ledger-v0.79.json`.

Construction result:
`selected-k77-physical-section-faithfulness-gate-2026-08-08.md`.
