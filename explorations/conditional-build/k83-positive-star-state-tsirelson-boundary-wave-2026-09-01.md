---
title: "K83 positive star-state Tsirelson boundary wave"
status: active_research
doc_type: reverse_scaffold_quantum_positive_state_extension_result
date: 2026-09-01
claim_ceiling: exact conditional CHSH sum-of-squares theorem, saturation-null relations and PR non-extension certificate for commuting binary observables in a positive star-state composite; no GU-native observable algebra, physical state, Born rule, unique quantum selection, prediction, confirmation, or verdict
manifest: lab/process/k83-positive-star-state-tsirelson-boundary-wave.json
probe: tests/channel-swings/k83_positive_star_state_tsirelson_boundary_probe.py
---

# K83 positive star-state Tsirelson boundary wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact positive-star-state sum-of-squares boundary for Tsirelson and exact PR-box non-extension certificate
carrier: one unital star-algebra generated by binary self-adjoint involutions A0,A1,B0,B1 with commuting Alice/Bob subalgebras LAYER=conditional CHIRALITY=N/A
pairing: normalized positive linear functional omega with omega(X*X) nonnegative ON=repository_owned_control_algebra
real_structure: real coefficients and self-adjoint generators suffice; complex structure is not selected
grading: N/A; quotient and minimal abelian BV gradings do not supply the composite star product or state
action_owner: none for the quantum structure; the algebra and state are repository-owned downstream-demand controls and are not attributed to Weinstein
target: Tsirelson SOS bound, saturation-null relations and obstruction to extending PR correlations as one positive composite state MAP-TYPE=classification
```

## Positive-state sum-of-squares theorem

Let `A0,A1,B0,B1` be self-adjoint involutions in one unital star-algebra and
assume each Alice generator commutes with each Bob generator. Define

```text
C = A0(B0+B1) + A1(B0-B1),
X = A0 - (B0+B1)/sqrt(2),
Y = A1 - (B0-B1)/sqrt(2).
```

Direct expansion gives the exact identity

```text
2 sqrt(2) 1 - C = (X^2+Y^2)/sqrt(2).
```

Therefore every normalized positive linear functional `omega` on the complete
algebra obeys `omega(C)<=2sqrt(2)`. Applying the same identity after replacing
both Alice observables by their negatives gives the lower bound, so

```text
|omega(C)| <= 2 sqrt(2).
```

No Hilbert tensor factorization or complex scalar structure is needed as a
premise for this inequality. The decisive owned object can be stated more
minimally: one composite star product, cross-local commutation, binary
involution relations and a positive normalized state on the complete algebra.
K82's Hilbert representation is one realization of this packet, not the only
form of the demand.

Saturation has an exact state-relative meaning. Equality at the upper bound
holds if and only if both nonnegative terms have zero expectation:

```text
omega(X^2)=omega(Y^2)=0.
```

In the GNS quotient these are the state-null relations
`X Omega=Y Omega=0`. They are constraints on the composite state and
observable product, not consequences of no-signalling marginals alone.

## Exact PR non-extension certificate

The PR correlations from K82 give `omega(C)=4` if represented as the four
cross-correlators `omega(Ax By)`. Substitution into the SOS identity would
require

```text
omega((X^2+Y^2)/sqrt(2)) = 2 sqrt(2) - 4 < 0,
```

because `8<16`. That is impossible for a positive state. Hence the PR box is
not merely non-Hilbert in the previously tested representation: its
correlations admit no extension to a normalized positive functional on any
single commuting binary-observable star-algebra satisfying these relations.

This pinpoints the missing physical owner. Positivity of a table of conditional
probabilities and no-signalling of its marginals are not positivity of a state
on the complete observable product algebra. The latter, together with the
composition and commutation relations, carries the Tsirelson ceiling.

## Hostile boundary

The strongest overclaim would be to call this algebra and positive state a GU
derivation. They are imported conditional demands. The strongest contrary
control remains the positive normalized no-signalling PR table; it is excluded
only after asking for extension to this stronger composite algebra. The weakest
propagation seam is the absent source/action-selected physical quotient,
observable product and state/Born pairing.

The result does not select real, complex or quaternionic quantum theory,
derive a C-star norm, prove locality in curved spacetime, build completely
positive dynamics, or export a Bell experiment. Delayed-choice entanglement
swapping remains reserved and unscored.

This is not a GU-native observable algebra, positive physical state or Born
rule. It supplies no prediction, confirmation or held-out credit.

## Next condition

Derive or independently supply the actual source/action-owned physical
cohomology, local observable product and normalized positive state. Then test
whether the SOS factors are well-defined and positive on that quotient and
whether their saturation or strict slack yields a held-out structural
consequence before any observable confrontation.
