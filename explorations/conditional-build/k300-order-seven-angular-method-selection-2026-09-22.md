---
title: "K300 Order-Seven Angular Method Selection"
document_role: active_research
operational_state: internal_structural_result
date: "2026-09-22"
claim_ceiling: "Exact face-integrability transfer and structural selection of the positive Peano route; no numerical global derivative norm or radial gamma composition."
manifest: lab/process/k300-order-seven-angular-method-selection.json
producer: tests/channel-swings/k300_order_seven_angular_method_selection.py
probe: tests/channel-swings/k300_order_seven_angular_method_selection_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: POSITIVE_PEANO_REMAINDER_FINITE__QUANTITATIVE_GLOBAL_NORMS_OPEN
canon_verdict_change: none
---

# K300 Order-Seven Angular Method Selection

## GU-COMPARATOR-ROUTING

This is an `INTERNAL_STRUCTURAL_ONLY` result about the repository-supplied
conditional Fock construction. It does not identify a source-selected action
or move a physics-ledger row.

```gu-typed-objects
result: complete face-integrability audit of the K299 tensor Peano remainder and comparison with terminal analytic subtraction
carrier: all 24 K288 occurrences and four ordered coherent groups on the K294 projective simplex LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Gram pairing with coherent signs retained before absolute Peano enclosure PAIRING-TYPE=hilbert
real_structure: CAR adjoint, momentum reflection and real Bessel K1 kernel
grading: K296 face support, K297 old position, K298 coherent terminal coefficient and Duffy axis
action_owner: repository construction from K139/K156/K179; no source-selected GU action or physical state is supplied
target: select a legal complete angular remainder method before K294 radial composition MAP-TYPE=intertwiner
```

## Face transfer

The Duffy order is `(r0,r1,r2,c0,c1,c2)`. The left terminal gap is
`r2=0`, represented by `t2=0`. After the earlier tensor means are fixed, the
right terminal gap is `c2=0`, represented by `t4=1`. The endpoint coordinate
`y` is last in the Peano telescope, so every gap error retains its paired
`y` or `1-y` integration. Both terminal Peano kernels add a second-order
zero. The Duffy Jacobian adds only nonnegative polynomial face powers.

The six pure one-gap projective faces have order-two margin `1`; every
codimension-two projective face has margin at least `4`. K297's nested
endpoint margins are `14`, `7` and `2` for old positions two, four and six.
In particular,

```text
d_g^2[g^2 y/(y+a g)] = 2 y^3/(y+a g)^3,
```

which has homogeneous degree zero at fixed positive split parameter. The six
K299 Peano terms are therefore locally absolutely integrable on every K296
face and K297 nested endpoint corner. The interior is smooth, so a finite
face cover proves a finite qualitative global angular remainder.

## Method choice and remaining gate

The positive route needs six complete coherent second-directional norms, one
positive node, and no cutoffs. Analytic subtraction would need at least eight
terminal coefficient functions—four coherent groups times two endpoint
corners—plus a compatible partition of unity, exact model integrals and a
fourth-derivative proof for the overlap residual. The positive Peano route is
therefore selected; subtraction remains a legal fallback rather than a
globally killed method.

The result is qualitative, not yet numerical. The next gate is a
determinant-preserving interval enclosure of the six complete Peano norms,
first on the projective/q domain at fixed `x`, then with explicit `q` and `x`
scaling. Only that bound may release K294's low/middle/high gamma join. No
exterior action-column value, residual or native K152 interval follows.
