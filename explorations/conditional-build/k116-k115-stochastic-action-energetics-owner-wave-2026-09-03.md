---
title: "K116 K115 stochastic-action and energetics-owner boundary"
status: active_research
doc_type: conditional_stochastic_action_energetics_owner_result
created: 2026-09-03
date: 2026-09-03
claim_ceiling: exact conditional marked-Poisson stochastic-path-action, detector local-detailed-balance, compensating-controller, stationary entropy-production and information-contraction theorem for the K115 finite-dimensional diffusion-refresh carrier; the bath, energy convention, clock, preparation and controller are declared repository constructions; no Weinstein/source/GU action, autonomous microscopic environment, spacetime causal BV-BFV descent, Born rule, prediction or confirmation follows
manifest: lab/process/k116-k115-stochastic-action-energetics-owner-wave.json
probe: tests/channel-swings/k116_k115_stochastic_action_energetics_owner_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K116 K115 stochastic-action and energetics-owner boundary

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: this packet closes the conditional stochastic-action and ideal
energetics denominator left by K115. It gives the K115 marked paths an exact
probability action, supplies a detector energy and isothermal refresh law,
identifies the controller force required to preserve the K113 base, proves a
stationary entropy-production theorem and prices the record as information.
Every physical ingredient is declared rather than source-derived. This is not
Weinstein's action, a microscopic reservoir or a spacetime detector theory.

```gu-typed-objects
result: one conditional marked-Poisson path action owns K115's refresh law; an ideal detector energy gives exact local detailed balance, base autonomy requires an explicitly supplied canceling controller, the stationary diffusion-jump entropy production is nonnegative and strictly positive for informative readout, and the resolvent contracts event-time record information
carrier: K113 reflected paths on the closed normalized nonnegative weight simplex Delta_255 together with a 256-state detector and marked Poisson event stream LAYER=observed CHIRALITY=N/A
pairing: K113 Gibbs path law composed with the conditional marked-Poisson likelihood, stationary density nu(dw,r)=mu(dw)h_r(w), and classical relative-entropy pairing ON=repository_stochastic_energetics_control
real_structure: real reflected diffusion coordinates, positive real detector probabilities, real state-dependent energies and a classical real-valued path likelihood
grading: degree-zero classical stochastic process; inherited K91 BRST grading appears only after record-center evaluation and is not used by the energetics theorem
action_owner: repository-construction -- K113 base path law plus declared marked clock, conditional preparation, ideal detector bath and canceling controller; not Weinstein's source action or an autonomous microscopic environment
target: exact stochastic-action ownership, local-detailed-balance, controller-invoice, stationary-production and information-contraction boundary MAP-TYPE=evaluation
```

## Inline preflight bookend

K115 proves that an informative one-way record can leave the complete K113
base path law unchanged, but it deliberately imports the clock, refresh
kernel, erasure/preparation operation and any thermodynamic meaning. The
strongest executable reverse edge is therefore not another detector kernel.
It is to own that same law as a conditional stochastic action, make its ideal
energetic realization explicit and ask exactly what must be supplied to cancel
backreaction.

The route census separated six possibilities: marked-Poisson action,
equilibrium detector energy alone, reversible enlargement, finite autonomous
fuel register, spacetime Schwinger--Keldysh/BV-BFV theory and source
authentication. An equilibrium-only model cannot explain K115's global
nonequilibrium; reversible enlargement changes the carrier; finite fuel cannot
sustain a stationary process indefinitely; and the spacetime/source routes
lack owned inputs. The marked-Poisson plus ideal stochastic-energetics route is
selected. Its cheapest kill is failure to recover K115's generator or failure
of nonnegative production. Its strongest contrary route is an autonomous
microscopic dilation that supplies the controller and fuel rather than
declaring them; that remains open.

## 1. Exact marked-Poisson path action

Let `P_A` be the K113 law of the reflected simplex path `W_t`, with generator
`A` and invariant Gibbs probability `mu`. Conditional on a realized base path,
let event times be a Poisson process of rate `lambda`. At event `t_k`, draw a
mark `R_k` from K115's kernel

```text
a_r(w)=(epsilon+w_r)^m,
p_r(w)=a_r(w)/sum_s a_s(w),
epsilon>0, m>=1.                                         (1)
```

The mark overwrites the detector state; a mark equal to the old state is a
real clock event but a null state jump. Relative to `P_A`, unit-rate Poisson
time measure and counting measure on marks, the negative log density on
`[0,T]` is, up to reference constants,

```text
I_T[W,t_1,R_1,...,t_N,R_N]
 = I_A[W]+lambda T-N log(lambda)
   -sum_(k=1)^N log p_(R_k)(W_(t_k)).                    (2)
```

Here `I_A` names the already-owned K113 path factor; equation (2) does not
claim a coordinate-independent Onsager--Machlup density against nonexistent
flat Wiener measure. Its conditional marked-process factor is an exact
Radon--Nikodym likelihood. Over a short interval `dt`, one mark `s` occurs
with probability `lambda p_s(w)dt+o(dt)`, while no mark occurs with
probability `1-lambda dt+o(dt)`. Consequently (2) recovers exactly

```text
Gf(w,r)=A[f_r](w)
       +lambda(sum_s p_s(w)f(w,s)-f(w,r)).                (3)
```

Thus the action owns the K115 law rather than merely fitting its stationary
measure. Simultaneous permutation of `w`, detector labels and marks preserves
(1)--(3), and the base factor remains literally `P_A` for every detector
history.

## 2. Ideal detector bath and exact local detailed balance

For a declared detector inverse temperature absorbed into dimensionless
units, define

```text
U_r(w)=-log p_r(w)+c(w),                                 (4)
q_(r->s)(w)=lambda p_s(w), r!=s.                         (5)
```

The common gauge `c(w)` cancels from differences. For every pair of distinct
record states,

```text
log(q_(r->s)/q_(s->r))
 =log(p_s/p_r)=-(U_s-U_r).                               (6)
```

Equation (6) is exact local detailed balance for an ideal isothermal detector
bath at fixed `w`. At each clock mark, the record is conditionally prepared in
the Gibbs distribution proportional to `exp(-U_r(w))=p_r(w)exp(-c(w))`.
This supplies the bath convention, reset clock and preparation law that K115
left unnamed.

It does not construct a Hamiltonian reservoir, a finite fuel store, a
metastable memory barrier or a laboratory reset device. In particular, a
fixed finite autonomous fuel register cannot maintain nonzero stationary
production forever without depletion or replenishment. The ideal bath and
clock are parts of this repository control.

## 3. Exact price of base autonomy: a canceling controller

For informative `p`, the detector energy (4) depends on `w`. If the detector
were included in an ordinary joint overdamped energy, its dimensionless force
on the base in record sector `r` would contribute

```text
F_det,r=-D grad U_r,                                     (7)
```

where `D` is the base diffusion tensor on the simplex tangent space. K115's
generator contains no such term. Exact base autonomy therefore requires an
additional controller contribution

```text
F_ctrl,r=+D grad U_r,
F_det,r+F_ctrl,r=0.                                      (8)
```

The sum leaves the K113 generator `A` in every record sector. This is not a
new interaction hidden in notation: (8) is the explicit backreaction invoice
for the one-way idealization. A physical successor must own the sensor,
controller and energy supply that realize this cancellation. Without them,
the energy assignment (4) would reinstate record-conditioned drift of the
kind K114 exposed.

## 4. Stationary entropy production is exact under the declared convention

Retain K115's unique stationary density

```text
nu(dw,r)=mu(dw)h_r(w),
h_r=lambda(lambda-A)^(-1)p_r.                            (9)
```

For K113's diffusion coefficient `beta^(-1)` on the simplex tangent space,
define the stationary diffusion and refresh productions

```text
sigma_diff
 =sum_r integral mu(dw) beta^(-1)|grad h_r|^2/h_r,       (10)

sigma_jump
 =1/2 sum_(r,s) integral mu(dw) lambda
   (h_r p_s-h_s p_r) log((h_r p_s)/(h_s p_r)).           (11)
```

Every term in (10) is nonnegative. Every pair contribution in (11) has the
form `(x-y)log(x/y)>=0`. Hence

```text
sigma=sigma_diff+sigma_jump>=0.                          (12)
```

If (12) vanishes, (10) makes every `h_r` constant under K113
irreducibility. Stationarity in (9) then gives `p_r=h_r`, so the readout is
state-independent. Conversely a constant kernel has `h=p` and zero currents.
Therefore every informative strictly positive K115 kernel has

```text
sigma>0.                                                 (13)
```

This is a thermodynamic entropy-production theorem for the explicitly
declared ideal overdamped diffusion-jump convention. It is stronger than
K115's bare current statement, but it is not a derived GU energy budget,
cosmological dissipation law or microscopic fluctuation theorem. Those claims
would require the autonomous reservoir/controller realization still missing
from section 3.

## 5. Record information is a preparation resource

Equivariance gives uniform event and stationary detector marginals. Define

```text
I_event
 =integral mu(dw) sum_r p_r(w)log(256 p_r(w)),            (14)

I_stat
 =integral mu(dw) sum_r h_r(w)log(256 h_r(w)).            (15)
```

The resolvent representation is an exponential average of the K113 Markov
semigroup:

```text
h_r=integral_0^infinity lambda exp(-lambda t)P_t p_r dt. (16)
```

Convexity of `x log x`, Jensen's inequality and invariance of `mu` give

```text
0<=I_stat<=I_event.                                      (17)
```

For an informative kernel, nontrivial base motion and finite positive clock
rate, the contraction is strict. At a K111 vacuum with
`epsilon=1/257`, event accuracy remains

```text
p_correct=3^m/(3^m+255*2^m)<1                            (18)
```

at every finite `m`. The record therefore costs conditional preparation
information while retaining nonzero error, and finite clock response loses
some of that information before a stationary observation.

Equations (14)--(17) do not assert a universal work equality. Converting the
information resource into a Landauer work bound requires a specified initial
memory distribution, cyclic protocol, temperature, Hamiltonian and reset
implementation. None is silently supplied here. Classical mutual information
is not Born probability or a quantum measurement theorem.

## 6. Exact finite control

The certificate uses a three-state complete-graph base with rate `2/5`, clock
rate `7/11`, and equivariant event kernel with correct probability `81/113`
and each wrong probability `16/113`. The centered-mode resolvent attenuation
is exactly

```text
alpha=lambda/(lambda+3 kappa)=35/101.                    (19)
```

It checks the stationary equation and every current divergence with rational
arithmetic, verifies all local-detailed-balance ratios exactly, and then
evaluates the standard edge-production formula to show positive base and
refresh contributions. Event mutual information is positive and strictly
exceeds stationary mutual information. The hostile harness first verifies a
clean baseline, then attacks the rates, normalization, informativeness,
ownership, controller, production, information and claim ceilings.

## 7. Remaining autonomous and spacetime invoice

K116 now owns one complete conditional stochastic-law/energetics packet, but
the word *conditional* is load-bearing. A physical successor must provide at
least one of the following without imposed cancellation:

1. an autonomous microscopic dilation with reservoir, controller, fuel,
   reset/preparation cycle and an appropriate long-time or replenishment law;
2. a source-authenticated action whose reduction owns those same structures;
3. a genuine causal spacetime stochastic or Schwinger--Keldysh/BV-BFV domain,
   boundary traces and local detector-observable descent; and
4. a state/probability owner before any Born, measurement, prediction or
   confirmation claim.

An autonomous dilation may reproduce K116 and remain nonequilibrium. It is not
required to be reversible. It must, however, expose where the sustaining
resource lives instead of treating the canceling controller as costless.

## Inline postflight bookend

- **Strongest overclaim:** “K116 derives the physical source action behind
  measurement.” Refused. It derives an exact conditional stochastic action
  whose bath, controller and preparation are declared controls.
- **Strongest contrary construction:** an autonomous Hamiltonian or
  field-theoretic detector with a replenished reservoir could realize the same
  coarse law and derive its fluctuation relation. K116 does not exclude it;
  it states the fields that construction must own.
- **Strongest mistyping risk:** identifying local detailed balance at frozen
  `w` with global equilibrium. Refused. State-dependent detector energy plus
  exact base autonomy requires the supplied controller, and informative
  stationary production is strictly positive.
- **Weakest reproducibility seam:** a simulation can report stationary balance
  while hiding circulating edge currents. The exact control checks every node
  divergence separately and then verifies positive base and refresh
  production.

The certificate runs a clean exact baseline before every hostile mutation.
No Weinstein/source/GU action, autonomous microscopic bath or fuel,
spacetime causal BV-BFV quotient, Born derivation, held-out score, prediction,
confirmation, canon, paper or public-posture surface moves.

## Reproduction

```bash
python3 tests/channel-swings/k116_k115_stochastic_action_energetics_owner_probe.py
python3 tests/channel-swings/k116_k115_stochastic_action_energetics_owner_probe.py --selftest
```
