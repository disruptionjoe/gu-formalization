---
artifact_type: exact_construction_and_scope_result
created: 2026-08-10
status: GRADE125_NOT_EULER_CLOSED__SPIN_GRADE_SATURATED113893__UNITARY_PARENT_FULL229477__PARENT_FORK_OPEN
ledger_rows: [LT-GR1, LT-GR2b, LT-GR2c, LT-GR3, LT-GR5, LT-GR6]
canon_verdict_change: none
---

# Selected-K77 grade-five and unitary-parent Euler closure

## Result in plain English

The grade-`1+2+5` field carrier is not closed under the first-order source
equation. Grade five emits grade six. Repeating the exact computation for all
`16,384` real Clifford directions gives the complete transition graph

```text
0 -> 3        5 -> 2,6       10 -> 9,13
1 -> 2        6 -> 5,9       11 -> 8,12
2 -> 1,5      7 -> 4,8       12 -> 11
3 -> 0,4      8 -> 7,11      13 -> 10,14
4 -> 3,7      9 -> 6,10      14 -> none.
```

Starting from grades `1+2+5`, the smallest closure made from **whole Clifford
grade spaces** is

```text
{1,2,5,6,9,10,13,14},
```

the `B`-adjoint-skew real sector. It has `8,128` coefficient directions,
`14*8,128=113,792` connection directions, and `113,893` total directions after
the ten metric and ninety-one epsilon coordinates are restored.

That smaller sector is not invariant under either advertised unitary parent.
An allowed block-even unitary generator sends a skew grade-one direction to an
`i`-times-self grade-three direction. Requiring the two-`U(32,32)` symmetry or
the full `U(64,64)` symmetry together with Euler closure therefore forces all
`16,384` real coefficient directions: `229,376` connection directions and
`229,477` total directions.

This does **not** select full `U(64,64)`. The two-`U(32,32)` and full-`U(64,64)`
parents now require the same completed field carrier, but retain different
symmetry groups and different invariant-pairing freedom: three weights for the
two-half parent versus one for full `U(64,64)`. The Spin-native skew sector is
also a proper Euler-closed rival unless unitary covariance is imposed.

## Layer 0

| phrase | object established | kept distinct |
|---|---|---|
| `B`-adjoint parity | skew versus `i`-times-self real-form sectors | Weyl chirality parity |
| field carrier | admissible connection coefficients | residual/action-pairing carrier generated by an adjoint orbit |
| grade closure | minimum union of complete Clifford grades | minimum arbitrary invariant linear subspace |
| Euler closure | invariance under `D_varpi Upsilon` | unitary covariance; parent selection |
| two-half completion | full coefficient field required by Euler plus `U(32,32)^2` covariance | equality with full-`U(64,64)` symmetry or pairing coordinates |

The finite calculation owns the fixed-epsilon first-order value-plus-`Y^14`
jet symbol. It does not yet construct either global associated bundle, prove
moving-epsilon equivariance, descend through the observation map, or provide a
gauge/BV quotient, closed domain, positive majorant, spectrum or quantum
measure.

## Exact composition with prior work

The pointwise v0.77 bank already found live grades `1`, `2`, and `5`, but did
not compute their first-order Euler closure. The v0.93 pairing calculation
found residual carriers of dimensions `16,382` for the two-half parent and
`16,383` for full `U(64,64)`. Those were action-invariant residual carriers,
not complete connection tangents. The Euler graph adds the missing scalar and
chirality centers to the two-half carrier and the missing scalar center to the
full-unitary carrier.

The v0.112 pointwise stationarity theorem survives: both parent branches can be
stationary while their functional tangent still needs the full carrier.
Stationarity and first-order closure answer different questions.

## Source return

```text
SOURCE-CONFIRMS:
  two C^(32,32) Weyl halves and, separately, a later U(64,64) group.

REPO-DERIVES:
  the exact grade graph, the grade-saturated Spin total 113,893, and the
  unitary-covariant full-field total 229,477.

SOURCE-SILENT:
  which symmetry is operative in the action, why either unitary parent should
  be selected over the Spin-skew sector, and the global reduction.
```

Canonical return code:

```text
SOURCE_CONFIRMS_TWO_C32_32_HALVES_AND_SEPARATE_U64_64_GROUP__SOURCE_SILENT_OPERATIVE_ACTION_PARENT_AND_REDUCTION
```

## Efficient specialist assessment

1. **Layer-0 semantics — ACTUAL MATH, very high confidence.** Clifford-adjoint
   parity and Weyl chirality are different gradings; identifying them would
   turn the exact split into a false chirality claim.
2. **Representation theory — ACTUAL MATH, very high.** The grade graph is
   reconstructed on every basis direction and four signature-orbit
   representatives; no sampled multiplicity argument is used.
3. **Variational/PDE — ACTUAL MATH, high.** The unsymmetrized first-order Euler
   symbol owns the graph. Pointwise stationary densities do not close it.
4. **Symplectic/BV-BFV — ACTUAL MATH, high.** Enlarging a carrier neither
   deletes endpoint momentum nor constructs a characteristic quotient.
5. **Krein/operator theory — ACTUAL MATH, high.** Real-form preservation is
   exact, but it supplies no positive majorant or closed operator domain.
6. **Source fidelity — ACTUAL MATH, high.** Curt's two halves and later full
   group are preserved as a fork; the source does not choose the action parent.
7. **Analytic/path-integral — ACTUAL MATH, medium.** The complexified full
   carrier is signature-blind and cannot choose a contour, measure, or physical
   spectrum.
8. **Systems/provenance — ANALOGY, high.** Equal payload size does not make two
   protocols identical; symmetry authority and pairing coordinates remain
   first-class identifiers.

## Accounting and next gate

No coefficient, quotient, external datum, or P1/P2/P3 status changes. Ledger
verdicts, residue `84` with conditional range `84..86`, and five scoped
quotients remain unchanged.

```text
headline_delta: none
conditions_closed: 2
  - grade-five/complete-grade Euler closure is decided wholesale
  - the two advertised unitary parents' complete field-carrier burden is typed
conditions_opened: 0
remaining_named_conditions: 1
```

Next globalize both surviving carrier horns: the Spin-skew `113,893` bundle and
the unitary-covariant `229,477` bundle. Test moving-epsilon equivariance and
observation reduction before selecting a parent or beginning gauge/ghost/domain
work. Give the source-native unitary route priority without erasing the proper
Spin rival.

Evidence:

- primary exact Python probe: `39/39 PASS`, including all `65,536` K lifts;
- independent Sage/FLINT implementation: `17/17 PASS`.

