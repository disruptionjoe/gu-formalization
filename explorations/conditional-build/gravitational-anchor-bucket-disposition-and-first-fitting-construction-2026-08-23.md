---
title: "Gravitational anchor disposition: one fitting construction, two exact B2 non-admissions"
status: active_research
doc_type: result
created: "2026-08-23"
registry: lab/process/gravitational-anchor-bucket-disposition-and-first-fitting-construction.json
probe: tests/channel-swings/gravitational_anchor_bucket_disposition_and_first_fitting_construction_probe.py
grade: "EXACT CORPUS AND METHOD DISPOSITION; ONE FITTING_CONSTRUCTION AT OBSERVED R5; TWO PRECISE B2 NON-ADMISSIONS; NO LEDGER OR SOURCE VERDICT MOVEMENT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "The result classifies three repository-internal realization routes; it kills no source claim, accepted benchmark, or GU theory class."
canon_verdict_change: none
---

# Gravitational anchor disposition and the first fitting construction

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: first admitted fitting construction for the rotation-functional anchor plus exact B2 non-admissions for the strong-field and PPN anchors
carrier: real Lorentzian transverse-traceless symmetric-two-tensor sector on observed X4 with an Einstein-Weyl static Green-function family LAYER=observed CHIRALITY=N/A
pairing: quadratic Einstein-Weyl action bilinear form ON=observed-TT-metric-sector
real_structure: real Lorentzian coefficients and real static Green function
grading: spin two with second-order Einstein and fourth-order Bach differential pieces
action_owner: repository-construction
target: LT-GR9 LT-GR10 and LT-GR11 realization eligibility under the four-bucket method MAP-TYPE=evaluation
```

## Result first

The three newly accepted gravitational anchors do not have one common
disposition.

| ledger row | bucket | result |
| --- | --- | --- |
| `LT-GR9` strong field | `B2` | W225 imports Schwarzschild and leaves the native equation, coupling, observed reduction, stationary background and perturbation owner unbuilt. It is not a construction of the benchmark. |
| `LT-GR10` PPN | `B2` | W220 grants the shiab-Einstein equation before computing the ten target-facing parameters. It is a conditional comparator pass, not a forward construction. |
| `LT-GR11` rotation functional | `B1` | The pre-existing observed Einstein-Weyl linear sector and its static Green-function form pass FC-1 through FC-7 at `FITTING_CONSTRUCTION` grade, with the K77-to-observed bridge still open. |

This is the first object banked under the new grade. It changes no ledger row:
all three remain `NEEDS / MISSING_CONSTRUCTION` because a fitting construction
is below conditional derivation and cannot discharge a benchmark.

## The B1 object: FC-GR-ROTATION-EINSTEIN-WEYL-STATIC-1

The object is the real observed-four-dimensional linear Einstein-Weyl metric
sector

```text
L_EW(h) = h [a box + b box^2] h,
a > 0, b != 0,
```

restricted to transverse-traceless spin-two perturbations, together with the
bounded-source static Green-function family

```text
V(r) = -(G M/r) [1 + (1/3) exp(-m_2 r)],
m_2^2 = a/b > 0.
```

Only the functional form is banked. No value of `m_2`, `G`, `a`, `b`, or a
galactic matter model is fitted or claimed source-owned. H45 constructed the
full-norm `|II|^2 = |H|^2 - R^X` branch and its two-pole operator on
2026-07-11; H49 independently computed the Bach factor, residues and static
Yukawa form on the same date. `EXT-GR-ROTATION` was accepted on 2026-08-23.
The object therefore existed before this benchmark was installed and was not
chosen after reading its target.

### FC-1 — built, not gestured

- **Carrier:** real TT symmetric two-tensors on observed Lorentzian `X^4`.
- **Pairing/form:** the quadratic Einstein-Weyl action form on that carrier.
- **Real structure:** real Lorentzian fields and coefficients.
- **Grading:** spin two; differential orders two and four.
- **Signature horn:** observed `(3,1)`; the object makes no claim that the
  missing ambient K77 observation adapter has been built.
- **Ambient embedding:** the target interface is the observed section of the
  `Y^14 = Met(X)` observerse. Transport from the K77 source carrier is an
  explicit missing pathway item, not silently inferred.

### FC-2 — rung named

The construction lives at **R5**, static potential accounting for the
`EXT-GR-ROTATION` descent. It supplies the Newtonian-plus-massive-Yukawa Green
form upward. It remains conditional below on the R4 coefficient hierarchy and
the R3 elliptic observed reduction.

### FC-3 — non-conflicting, checked

The retrieval and hostile check covered H45, H49, H23, H26, W225, the August
UV-gravity hardening result, `PD-STRUCTURE-TRANSPORT`, the three-anchor descent,
and ledger v0.263. The scoped object does not claim the interacting physical
quotient or loop unitarity that H26 and the UV audit leave open. It does not
claim a stationary strong-field solution, so W225/RFAIL-03 and the W229
branch-local obstruction do not conflict. H45 leaves the full-norm branch open
but favored; a constructed open branch is eligible at fitting grade precisely
because the grade does not assert derivation.

The input-currency check found no canonical-source correction whose topic
signature supersedes this spin-two/static-Green object. RFAIL-03 is separately
acknowledged and fenced to the imported-Schwarzschild strong-field route.

### FC-4 — source-compatible, checked

The source register check covered `SC-OP-03`, `SC-ACT-02`, `SC-ACT-04`, and
`SC-ACT-05`. The source asserts a Weyl-annihilating Shiab candidate, a
first-order Swervature/Displasion recovery sketch, a norm-square second-order
action and a combined bosonic/fermionic Euler residual. It does not assert the
observed Einstein-Weyl coefficient ratio or static Green function, but it also
does not disavow this observed branch. The construction is therefore
source-compatible, not source-derived.

### FC-5 — pathway named

The route has two named owners and an executable next step:

1. **OWNER-A, action/reduction:** decide H45's P2 norm fork by deriving whether
   the registered `I2B = ||Upsilon||^2` source action transports through H21's
   full `theta = II_s` identification to the full `|II|^2` observed operator.
2. **OWNER-D/OWNER-E, observation and source coupling:** construct the K77-to-
   observed elliptic static reduction and bounded-source matter coupling, then
   recompute the Green function on that derived operator.

The next executable gate is the first item: a source/action-owned P2 transport
certificate with carrier, norm, coefficient ratio and observation map frozen
before any rotation comparison. This is more specific than “the source action
must exist.”

### FC-6 — ceiling and demotion

This is not a GU derivation, prediction, fit, source-owned mechanism or
benchmark discharge and may not be cited as one. It demotes to `speculative`
if P2 resolves to trace-square `|H|^2`, if the K77 observation transport cannot
produce the observed Einstein term, or if the derived static reduction does
not retain the stated Green-function form. A proved conflict demotes it to the
appropriate negative grade.

### FC-7 — nontriviality witness

The object forbids a dominant long-range linear potential `gamma r` and the
pure conformal `a = 0` branch inside this construction. A derived observed
static Green function with a nonzero dominant `gamma r` term, or no Einstein
pole, falsifies this fitting construction. The witness is functional-form
content only; it says nothing about dark matter or a successful galactic fit.

## The two exact B2 non-admissions

### LT-GR9 strong field

W225 is an imported-metric residual check. It proves exact GR-shadow zero on
the supplied Schwarzschild metric and locates the first built section residual,
but it does not construct a native stationary family. The row is B2 with five
named requirements, not the generic phrase “needs the source action”:

- `SF-1`: OWNER-A must derive the observed vacuum equation, `E_s^theta`, and
  `alpha_W` at tested orders.
- `SF-2`: OWNER-E must supply minimal matter/geodesic coupling.
- `SF-3`: OWNER-D must supply a hyperbolic observed reduction.
- `SF-4`: OWNER-B must supply a stationary horizon-bearing background.
- `SF-5`: OWNER-B plus OWNER-A must supply its perturbation operator.

The precise non-admission statement is: no current artifact jointly owns even
`SF-1 + SF-4`, so the imported Schwarzschild control cannot satisfy FC-1,
FC-3, or the target-before-evaluation rule.

### LT-GR10 PPN

W220 begins by granting `G_mn = T_mn`, no extra long-range mode and the exact
Schwarzschild vacuum; the GR PPN signature follows from those grants. The row
is B2 with four named requirements:

- `PPN-1`: OWNER-A derives the shiab-Einstein reduction after observation.
- `PPN-2`: OWNER-C owns `G_eff` normalization.
- `PPN-3`: OWNER-E supplies perfect-fluid-compatible matter coupling.
- `PPN-4`: OWNER-A proves extra long-range fields decouple at 1PN.

The precise non-admission statement is: removing the granted reduction removes
the object whose PPN parameters were computed, so W220 fails FC-1 and FC-5 as
a native construction. Its conditional pass remains valid at its recorded
scope.

## Hostile review and claim ceiling

The strongest overclaim is “GU explains rotation curves.” Refused: the result
only banks an observed linear functional form and expressly excludes a matter
model or fit. The strongest contrary construction is H45's still-open pure
`|H|^2` horn; it is the explicit demotion condition, not hidden evidence. The
weakest reproducibility seam is the step from the linear TT operator to a
bounded-source static reduction; this is why the R3 observed reduction and
matter coupling stay in the pathway rather than being declared built.

No ledger verdict, distance, revival trigger, mapping grade, mechanism
commitment, confirmation credit, source claim, canon verdict, prediction or
public posture changes. Reproduce with:

```bash
python tests/channel-swings/gravitational_anchor_bucket_disposition_and_first_fitting_construction_probe.py --selftest
```
