---
artifact_type: source_compose_layer0_reconciliation
created: 2026-08-06
status: TWO_LAYER_QUEUE_SPLIT_CONFIRMED__LT_GR3_OWNER_MAP_OPEN
channels: [SOURCE, COMPOSE, VERIFY]
ledger_rows: [LT-GR3]
canon_verdict_change: none
---

# Two-layer action / selected-cubic owner retype

## Result in one sentence

The current rank-one queue combines three objects that the source and the
repository do not identify: the first-order selected transgression action
`I1B`, the second-layer norm square `I2B = ||Upsilon_B||^2/2`, and the observer
section's extrinsic `||II||^2` functional. The queue must split before another
large mixed-cubic assembly. `LT-GR3` does not move in verdict or residue; its
distance is retyped to constructing the missing owner map first.

## 1. Layer 0: the three owners

| name | typed object | what is established | what is not established |
| --- | --- | --- | --- |
| first layer `I1B` | a scalar transgression/Chern--Simons-like action in the connection difference `T`, whose translation derivative is `Upsilon_B` | 2021 draft equations 9.1--9.7; selected intrinsic `D3`, two-connection principal descent, and homogeneous Ward closure are now exact in their scoped packages | that its third derivative is the Weyl/Bach/full-II cubic |
| second layer `I2B` | a norm square of a first-layer residual, schematically `1/2 <Upsilon_B,G Upsilon_B>` | Portal `01:59:12--02:02:01`, 2021 draft 9.11--9.15, UCSD and the 2025 TOE correction to Curt all state the two-layer/square architecture | the actual moving metric, residual target, adjoint, observed reduction, or equality with `||II||^2` |
| observer `I_II` | the norm of the second fundamental form of a section `X -> Y`, whose TT reduction contains Weyl/Bach and Einstein pieces | conditional ledger row `CB-B:GR-3` and its H45/H49 evidence | a source-native map `I2B -> I_II`, or equality of their Euler and preboundary classes |

The word **curvature-squared** is therefore not an owner. It describes physics
that can arise from several inequivalent functionals. The phrase “direct
curvature/full-II/defect D3” in ledger v0.23 silently collapsed those owners.

## 2. Source collision

The source is clear on the distinction and silent on the missing
identification:

- `WGS-01` gives the first-order bosonic action and its residual.
- Portal `01:59:12--02:00:49` says to norm-square the first-order residual to
  obtain a **new** Lagrangian whose first-order solutions are redundant
  solutions of the second.
- Portal `02:01:28--02:02:01` describes the second operator as containing a
  Shiab/adjoint-Shiab Yang--Mills generalization and a zeroth-order
  Einstein-like term. It says “replacement,” “generalizing,” and “looks like,”
  not equality with the observer `||II||^2` action.
- The 2025 TOE exchange at `00:41:50--00:43:38` explicitly corrects Curt's
  one-layer reading: Einstein--Dirac is first; Yang--Mills--Higgs is added at a
  second layer.
- The selected-cubic source reinspection already fenced `D3 I` as a
  construction object not supplied by the source.

Scoped return:

```text
SOURCE-CONFIRMS: two distinct action layers and one-way first-layer-solution
                 redundancy under the norm square
SOURCE-CORRECTS: the current queue's single-owner wording
SOURCE-SILENT:   I2B = I_II, equality of their cubic tensors, Euler systems,
                 preboundary currents, or reduced physical classes
```

## 3. Exact finite theorem

Let a first-layer action near a background be

```text
I1(x) = 1/2 <x,Lx> + 1/6 C(x,x,x),
Upsilon(x) = dI1 = Lx + 1/2 C(x,x,-),
I2(x) = 1/2 <Upsilon(x), G Upsilon(x)>.
```

For diagonal `L=diag(lambda_i)` and `G=1`, the exact background cubic tensors
satisfy

```text
D3 I1[e_i,e_j,e_k] = C_ijk,
D3 I2[e_i,e_j,e_k] = (lambda_i+lambda_j+lambda_k) C_ijk.
```

With two unequal eigenvalues and nonzero `C111,C112,C122,C222`, the four
ratios are `3,4,5,6`; no common scale identifies the cubics. In one dimension
the ratio is accidentally constant. That planted degeneracy proves why a
single slice or one amplitude cannot establish an action identity.

The same exact probe also distinguishes solution sets. `Upsilon=0` always
implies `dI2=0`, but the converse can fail: for
`I1=x^3/3-x`, `Upsilon=x^2-1`, the second action has
`dI2=2x(x^2-1)`, so `x=0` is a second-layer critical point with
`Upsilon=-1`. The source's redundancy is one-way unless the linearized
residual/adjoint is injective on the selected domain.

This is a typing theorem, not a no-go. A constructed owner map, special
geometric identity, quotient, or on-shell restriction can still relate the
layers.

## 4. Queue correction

The old single rank-one instruction is replaced by two ordered gates:

1. **First-order completion (`I1B`)** — continue the exact selected
   augmented-torsion construction with moving metric/Hodge/DeWitt/Krein
   pairing, observation response, primitive epsilon, diffeomorphism/odd BV and
   preboundary reduction. This owns the current `LT-GR5`/`LT-SM8` progress and
   may proceed without pretending to compute `LT-GR3`.
2. **Second-layer/observer owner map (`I2B <-> I_II`)** — write the actual
   residual target metric and adjoint, derive `D3 I2B`, write `D3 ||II||^2`,
   and construct or refute an observation/pullback/boundary equivalence. Only
   then evaluate the Weyl/Bach `LT-GR3` cubic and Q1 burden.

These packages have different primary files and tests, so they are legitimate
non-conflicting channels when two automations are available. Their results
must reconcile before a combined Q1 or physics claim.

## 5. Ledger v0.24

```text
Ledger v0.24 — 82/82 active target rows mapped (100%)
33 SAME · 19 DIFFERS · 24 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

Only `LT-GR3` receives a distance/evidence/mapping-grade correction. Its
verdict `DIFFERS/STRUCTURAL_DIFFERENCE`, revival trigger, residue and physical
claim are frozen. P1/P2/P3 remain unused.

## 6. Seven-axis disposition

- **Layer 0:** three owners are distinct; the two missing maps are named.
- **L1 syntactic:** source formulas for `I1B` and `I2B` and repo formula for
  `I_II` are located.
- **L2 type:** action, residual, adjoint Euler operator and observer functional
  are separated.
- **L3 algebraic:** generic cubic independence and accidental 1D coincidence
  are exact.
- **L4 geometric:** the `I2B <-> I_II` observation map is open.
- **L5 variational:** one-way redundancy is exact; converse and preboundary
  equivalence are open.
- **L6 analytic:** injectivity, domains, Green operators and positivity are
  open.
- **L7 physical:** no Q1, Weyl/Bach amplitude, unitarity or prediction moves.

## 7. Constraint and lane fence

```text
new fields: 0
new coefficients: 0
new selectors: 0
residue reduction: 0
new quotients: 0
P1/P2/P3 consumed: 0
```

Curt remains formally separate inside the Eric lane. No third lane, canon
verdict, claim status or public posture is promoted.

## K118 successor closure — 2026-08-15

K118 confirms that this layer split is an active prerequisite, not merely a
queue annotation.  The observed `I_sc`, primitive `I1B`, residual-square
`I2B`, and observer `I_II` channels do not determine one common moving cubic.
Before any further coefficient assembly, K119 must select the primitive action
layer and construct the stationary observed-to-native two-jet.  See
`selected-k118-rsap-tt-full-moving-d3-owner-sufficiency-and-action-layer-gate-2026-08-15.md`.

## Evidence

- `tests/channel-swings/two_layer_action_selected_cubic_owner_retype_probe.py`
- `tests/channel-swings/conditional_physics_ledger_v024_probe.py`
- `lab/process/two-layer-action-selected-cubic-owner-retype.json`
- `lab/process/hostile-reviews/2026-08-06-two-layer-action-selected-cubic-owner-retype-review.md`
