---
title: "Selected-K139 native I1B T=0 DN principal-type obstruction"
status: active_research
doc_type: exact_homogeneous_dn_kernel_finite_schur_quotient_and_dencker_type_gate
created: "2026-08-16"
registry: lab/process/selected-k139-native-i1b-t0-dn-principal-type-obstruction.json
probe: tests/channel-swings/selected_k139_native_i1b_t0_dn_principal_type_obstruction_probe.py
grade: "K139 PROVES THAT K138'S COVARIANT FIVE-CLASS NULL QUOTIENT IS NOT THE CHARACTERISTIC KERNEL OF THE ACTION'S CURRENT HOMOGENEOUS DOUGLIS--NIRENBERG PRINCIPAL SYMBOL. THE COMPLETE 229386-DIMENSIONAL COUPLED DN SYMBOL HAS NULL RANK 122748 AND NULLITY 106638; AFTER REMOVING THE ONLY FOUR ACTION-OWNED DIFFEOMORPHISMS, ITS PRINCIPAL QUOTIENT HAS DIMENSION 106634, NOT FIVE. THE FIVE-CLASS K138 OBJECT EXISTS ONLY AFTER THE NONHOMOGENEOUS ZERO-ORDER HODGE MASS MAKES I C_1(N)+K FINITE-FREQUENCY INVERTIBLE AND THE DISTORTION VARIABLES ARE SCHUR-ELIMINATED. K IS LOWER ORDER AND CANNOT ENTER THE ORDINARY DN PRINCIPAL SYMBOL; C_1(N) ITSELF HAS NULLITY 106630. AN EQUIVALENT HOMOGENEOUS FIRST-ORDER PROLONGATION CANNOT SILENTLY REMOVE THOSE MODES, AND THE ACTION OWNS NO DISTORTION GAUGE OR CONSTRAINT COMPLEX THAT QUOTIENTS THEM. THEREFORE NO ACTION-OWNED REAL-PRINCIPAL-TYPE FIVE-CARRIER, LEFT INVERSE, INVARIANT SUBPRINCIPAL CONVENTION, OR FIVE-BY-FIVE DENCKER LEAKAGE MATRIX IS CURRENTLY TYPED. K138'S FINITE-SYMBOL GEOMETRIC BUNDLE REMAINS EXACT. A NEW SEMICLASSICAL/ANISOTROPIC CALCULUS OR AN ACTION-DERIVED CONSTRAINT REDUCTION COULD REOPEN TRANSPORT BUT MUST BE DECLARED AND PROVED EQUIVALENT FIRST."
target_claim: K138_NEXT_GATE__COVARIANT_MIXED_ORDER_FIRST_ORDER_OR_DN_REDUCTION_REAL_PRINCIPAL_TYPE_AND_FIVE_BY_FIVE_LEAKAGE
target_verdict: FIVE_CLASS_FINITE_SCHUR_QUOTIENT_NOT_CURRENT_DN_PRINCIPAL_KERNEL__DN_NULL_QUOTIENT_DIMENSION_106634__ZERO_ORDER_K_ESSENTIAL_AND_NONHOMOGENEOUS__NO_OWNED_DISTORTION_CONSTRAINT_REDUCTION__FIVE_BY_FIVE_DENCKER_MATRIX_ILL_TYPED__K140_CALCULUS_OR_CONSTRAINT_OWNER_GATE
canon_verdict_change: none
---

# Selected-K139 native I1B T=0 DN principal-type obstruction

> **GU-COMPARATOR-ROUTING — scope before inference.** This is a source-native
> first-transgression, real `Cl(7,7)`, mixed-order principal-symbol,
> finite-frequency Schur and microlocal-reduction calculation. Ordinary
> Einstein, Higgs/VEV, family-index, chirality, anomaly, symmetry-breaking and
> familiar particle-spectrum constructions do not adjudicate it without an
> explicit typed bridge. Read `lab/methods/source-native-comparator-routing.md`
> before reuse.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K139 binds the selected displayed `comm/symi/symi` `I1B` Hessian at
`T=0`, nonzero normalized `kappa_1`, the complete all-grade real
`Omega1(Cl(7,7))` carrier, K132's formal DN symbol and K138's smooth-null
finite-symbol quotient. It proves that the latter is not the former's current
principal characteristic module. It does not rule out a newly declared
gauge-fixed, constrained, semiclassical, anisotropic or pseudodifferential
reduction with its own equivalence proof.

## Result in plain English

K138 built a legitimate five-dimensional geometric bundle, but it also left
one typing question: is that bundle the kernel on which the action's Dencker
connection acts? K139 answers no for the currently declared operator calculus.

The action's relative Douglis--Nirenberg weights make

```text
H_DN(n) = [[0,A(n)^*],[A(n),C_1(n)]]                 (1)
```

the homogeneous principal symbol. On the complete `229386`-dimensional
carrier, its exact null rank is `122748`, so

```text
dim ker H_DN(n)                 = 106638,
dim(ker H_DN(n)/im G_diff(n))   = 106634.             (2)
```

Only the four metric diffeomorphisms in (2) are action-owned gauge. K132
already proved that the distortion radical is not an owned gauge or
Koszul--Tate generator list. The principal quotient is therefore not K138's
five-dimensional quotient.

K138's five classes arise from a different, nonhomogeneous object. At a fixed
null frequency the zero-order Hodge mass makes

```text
C_full(n)=i C_1(n)+kappa_1 K                            (3)
```

invertible. Eliminating distortion from the full coefficient gives the
rank-one metric Schur form `-48 ell_n ell_n^T`, whose nine-plane radical modulo
four diffeomorphisms has dimension five. But `K` has order zero while `C_1`
has order one. It is absent from (1); indeed `C_1(n)` alone has nullity
`106630`. Thus the operation that creates the five-class object is not an
ordinary principal-symbol elimination.

This does not make K138 wrong. It makes its object a covariant
finite-frequency Schur quotient rather than the currently declared DN
principal characteristic bundle. Before a five-by-five Dencker endomorphism
can be computed, the repository needs an additional action-compatible owner:
either constraints that remove the other `106629` non-gauge principal
directions, or a declared parameter-dependent calculus in which (3) is the
leading block and the Schur elimination is uniform and covariant. Neither is
present now.

## 0. Pre-wave answers

1. **Fork.** Real `Cl(7,7)`, selected `comm/symi/symi` Shiab, nonzero
   normalized `kappa_1`, `T=0`, and the K127--K138 source-native branch remain
   fixed.
2. **Cheapest decisive condition.** A Dencker carrier must be the constant-
   rank kernel of the declared principal system, modulo proved gauge. Exact
   dimensions (2) decide this before any connection coefficient is fitted.
3. **Positive route.** K138's covariant quotient survives as finite-symbol
   geometry. Transport can reopen after an action-derived constraint complex
   or a uniform semiclassical/anisotropic Schur calculus is declared.
4. **Claim ceiling.** The current ordinary DN/undeclared-first-order route to
   a five-by-five action connection is ill typed. No universal reduction or
   propagation no-go follows.

## 1. Exact homogeneous DN characteristic module

K131 determined the relative weight family

```text
(s_g,s_T)=(1+a,a),   (t_g,t_T)=(2-a,1-a).             (4)
```

Every value of `a` preserves the same relative orders: the metric-distortion
blocks `A,A*` are order two, `C_1` is order one and `K` is lower order. K132
then exhaustively totalized (1). The exact causal ranks and nullities are

| conormal | rank `H_DN` | nullity | quotient after four diffeomorphisms |
| --- | ---: | ---: | ---: |
| timelike | 130912 | 98474 | 98470 |
| spacelike | 130912 | 98474 | 98470 |
| null | 122748 | 106638 | 106634 |

The null jump after gauge is `8164`. Consequently the complete principal
system neither has a five-dimensional characteristic kernel on the null cone
nor one constant-rank characteristic module across causal strata.

An equivalent first-order prolongation may introduce derivative variables and
constraint equations, but equivalence requires explicit maps between solution
modules. Invertible homogeneous row/column operations preserve kernel
dimension. A prolongation that deletes `106629` additional quotient directions
is therefore not merely a change of presentation: it supplies new constraints
whose action/Noether ownership must be proved.

## 2. Why the finite Schur quotient is not principal

For `kappa_1 != 0`, `K` is a nondegenerate involution. On a null covector,
`K C_1(n)` is nilpotent of index five and the full inverse is a finite
polynomial through frequency degree four. K135's exact composition proves all
positive powers cancel between `A*` and `A`, leaving

```text
-A* C_full(n)^(-1) A = -48 ell_n ell_n^T.             (5)
```

Equation (5) is exact and covariant at fixed normalization; it is not an
ordinary classical Schur principal symbol. The decisive distinctions are:

- the mass `K` is essential to invertibility but is lower order;
- the homogeneous coefficient `C_1(n)` is singular on a `106630`-plane;
- the full inverse is not a uniform order-minus-one symbol on null rays—it
  grows through fourth frequency degree before metric projection cancels it;
- the cancellation in (5) does not remove the full operator's distortion
  principal radical or create action-owned constraints on it.

Multiplying the system by powers of frequency to promote `K` changes the
principal filtration and does not by itself produce a classical homogeneous
symbol. Treating `kappa_1` as a semiclassical parameter of the same weight as
frequency is possible only after declaring that two-parameter calculus,
uniform estimates and its relation to the original operator.

## 3. Real-principal-type and leakage verdict

For a system of real principal type, a declared matrix symbol, scalar
Hamiltonian, left multiplier and constant-rank characteristic kernel define
the polarization carrier. The current action data supply the scalar null
Hamiltonian and K138 supplies a five-bundle, but the declared DN matrix acts on
the much larger module (2). There is no action-owned projector from that
module to the five classes.

Therefore these requested K139 objects are not presently typed:

```text
five-class DN kernel,                         absent;
action-owned left inverse on that kernel,     absent;
invariant subprincipal convention for it,     absent;
five-by-five complement-leakage map,           undefined.       (6)
```

`undefined` in (6) is a stronger and more useful answer than a guessed zero or
nonzero matrix. Any numerical five-by-five result would depend on an
unowned projection of `106634` principal classes onto five.

## 4. Twenty-lens reassessment

The live hypotheses were:

```text
H_A K138's five quotient is already the DN principal kernel;
H_B an equivalent first-order rewriting canonically makes it so;
H_C the five quotient is finite-symbol geometry but not the current DN
    principal module, so its Dencker matrix is ill typed;
H_D the mismatch invalidates K138's null-bundle theorem.
```

| lens | strongest hypothesis | decisive reason |
| --- | --- | --- |
| source criticism | `H_C` | the displayed action owns no added reduction |
| Layer-0 semantics | `H_C` | finite coefficient and principal symbol differ |
| exact Clifford algebra | `H_C` | exact ranks are `106638` versus nine |
| DN theory | `H_C` | lower-order `K` is absent from (1) |
| microlocal systems | `H_C` | the carrier must be the declared kernel |
| real-principal-type analysis | `H_C` | no five-carrier multiplier is typed |
| first-order reduction | `H_C` | equivalence cannot silently delete modes |
| pseudodifferential calculus | `H_C` | null inverse lacks ordinary order `-1` |
| semiclassical analysis | `H_C` | a new two-parameter filtration could reopen |
| Noether theory | `H_C` | only four gauge columns are owned |
| BV/KT theory | `H_C` | the distortion radical is not a generator list |
| Hamiltonian flow | `H_C` | `q=0` flow survives but does not select fibres |
| differential geometry | `H_C` | K138's covariant five-bundle remains exact |
| representation theory | `H_C` | no covariant `106634 -> 5` projector is owned |
| domain theory | `H_C` | a closed reduced realization remains absent |
| identifiability | `H_C` | any five-matrix would encode a fitted projection |
| hostile falsification | `H_C` | rank mismatch defeats the advertised carrier |
| philosopher of science | `H_C` | undefined is preferable to false precision |
| pragmatic sequencing | `H_C` | next gate is one explicit owner/equivalence test |
| wild frontier | `H_C` | anisotropic/semiclassical calculus stays open |

Vote: `H_A=0`, `H_B=0`, `H_C=20`, `H_D=0`.

## 5. Reverse scaffold and next gate

```text
R0 physical propagation needs a closed action-owned reduced operator.
R1 K138: Q_n is a covariant five-dimensional finite-symbol bundle.
R2 K139: Q_n is not ker(H_DN)/G_diff; that principal quotient is 106634D.
R3 K139: lower-order K is essential to the Schur elimination and cannot be
   promoted into the ordinary DN principal symbol without a new calculus.
R4 K140: test whether the full Euler/constraint system derives an invariant
   action-owned reduction to Q_n; in parallel specify the minimal
   semiclassical/anisotropic alternative and its equivalence obligations.
R5 only after R4: compute the correctly typed subprincipal leakage.
R6 only after a propagated closed domain: construct KT/BFV and positivity.
```

K140 must name the projector or constraint map, prove its action ownership,
show preservation under tangential/subprincipal evolution, and give uniform
equivalence to the original mixed-order equations. If it instead selects a
semiclassical or anisotropic calculus, it must state the parameter weights,
symbol class, uniform inverse estimates and the limit in which the original
operator is recovered. Joe input is not required.

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k139_native_i1b_t0_dn_principal_type_obstruction_probe.py
```
