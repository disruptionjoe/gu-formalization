---
title: "Selected-K77 CBRS-1F complete real-form grade-three extension"
status: active_research
doc_type: exact_class_obstruction
created: "2026-08-21"
registry: lab/process/selected-k77-cbrs1f-complete-grade3-extension.json
probe: tests/channel-swings/selected_k77_cbrs1f_complete_grade3_extension_probe.py
grade: "EXACT COMPLETE REAL B2 PLUS T2 PLUS iT3 FIRST-JET RESTRICTED CLASS KILL; NOT AN ALL-GRADE FIRST-JET, FULL CLIFFORD HESSIAN, OR PHYSICAL GAUGE-QUOTIENT THEOREM"
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_TWO_CONNECTION_MOVING_SHIAB_PRIMITIVE_EPSILON_AND_METX_GRAMMAR__REPO_DERIVES_AND_KILLS_THIS_FROZEN_B2_T2_IT3_RESTRICTED_FIRST_JET_CLASS__SOURCE_SILENT_ON_THE_CLASS
canon_verdict_change: none
---

# Selected-K77 CBRS-1F complete real-form grade-three extension

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: CBRS-1F complete real B2 plus T2 plus iT3 first-jet rigidity and metric-trace restricted-class kill
carrier: Omega1_base_tensor{Spin_grade2_connection plus real_Cl77_grade2_T plus i_real_Cl77_grade3_T} at the K77 anisotropic point LAYER=ambient CHIRALITY=N/A
pairing: K77 Clifford scalar-density pairing ON=Omega1_Cl77
real_structure: K77 real Spin(7,7) grade-two connection plus real B-skew grade-two and i-times-B-self grade-three T directions inside the u(64,64) comparator
grading: exterior form degree and Clifford grade
action_owner: repository-construction
target: complete retained field prolongation primitive-epsilon restriction and intrinsic metric covector MAP-TYPE=evaluation
```

## Result first

CBRS-1F extends the frozen anisotropic point

```text
(a,b)=(-13/96,1/48)
```

through the complete real-form grade-three `T` module.  The retained carrier is

```text
1,274  real Spin grade-two connection directions
1,274  real grade-two T directions
5,096  i-times-B-self grade-three T directions
-----
7,644  total first-jet directions.
```

The two cross-grade Hessian blocks vanish entry by entry:

```text
H(B2,iT3) = 0,       H(T2,iT3) = 0.
```

This is grade-block separation, not a diagonal-coordinate theorem.  The pure
`iT3` block has `24,024` off-diagonal nonzero entries.  Its six diagonal
classes are

| diagonal value | count |
| --- | ---: |
| `-1` | 588 |
| `-31/36` | 1,400 |
| `-4/9` | 560 |
| `1` | 504 |
| `31/36` | 1,460 |
| `4/9` | 584 |

The exact symmetric `5,096 x 5,096` grade-three block has rank `5,096` and
nullity zero.  Together with CBRS-1E's retained rank-`2,548` block, the complete
restricted Hessian has

```text
rank = 7,644,       nullity = 0.
```

Thus every first jet in this complete `B2 + T2 + iT3` carrier vanishes.  No
field kernel or primitive-epsilon quotient class survives.

## Real-form and gauge typing

Grade two is B-skew and enters the real `u(64,64)` comparator with real
coefficients.  Grade three is B-self and enters only as `i` times a real blade.
The exact probe plants the wrong real grade-three phase and rejects it.

A pointwise Spin generator still moves the grade-one background `T` in
Clifford grade one.  Its principal gauge jet is therefore outside the tested
`B2 + T2 + iT3` carrier.  Zero kernel, rather than a gauge quotient convention,
is what removes the restricted field jets.  The moving-Shiab base return still
vanishes on all `91` Spin generators.

## Held-out metric return

The unique on-shell restricted jet is zero, so its fixed-`varpi` source-graph
adjoint return is zero.  The inherited action density is still `221/55296`,
and the normalized intrinsic `MET(X)` row is

```text
(-221/27648,0,0,0,221/27648,0,0,221/27648,0,221/27648).
```

It is nonzero.  The complete `B2 + T2 + iT3` restricted first-jet branch is
therefore killed at intrinsic metric stationarity before any second jet,
global stabilizer, `mu6`, `J`/Higgs, photon, extra-`U(1)`, or gravitational
spectrum is admissible on this carrier.

## Prior-art, source fence, and hostile return

CBRS-1E owns the complete grade-two block and is replayed inside the new probe.
The new calculation retains it rather than sampling connection equations.  The
released source supplies the two-connection action, moving Shiab,
primitive-epsilon, and `MET(X)` grammar.  It does not supply the anisotropic
carrier, these exact blocks, their ranks, or the restricted-class verdict.

- **Strongest overclaim:** rank `7,644` is not the all-grade `230,650`-direction
  first-jet Hessian.
- **Strongest contrary construction:** higher real-form Clifford grades or a
  carrier containing the actual grade-one gauge tangent may couple differently.
- **Strongest mistyping:** the cross-grade blocks vanish, but the `iT3` block is
  not coordinate-diagonal; deleting its 24,024 internal incidences is invalid.
- **Weakest reproducibility seam:** final replay takes roughly thirty minutes
  because it rebuilds the complete predecessor block and all 5,096 new columns.

CBRS-1 remains active.  The next gate is `CBRS-1G`: rebuild the remaining
real-form first-jet grade frontier using whole-grade selection rules before
admitting another large matrix.  Identify the cheapest next complete carrier,
including whether the grade-one gauge tangent must enter with it, and admit a
second jet only after the next complete first-jet carrier decision.  A
class-wide negative remains valid.

No ledger verdict, source ownership, canon, residue, quotient datum, particle
assignment, prediction, confirmation, or public posture changes.

Reproduce with:

```bash
sage -python \
  tests/channel-swings/selected_k77_cbrs1f_complete_grade3_extension_probe.py
```

The exact probe passes `37/37` on final replay.
