# Conditional physics ledger v0.60

Machine source: `lab/process/conditional-physics-ledger-v0.60.json`
Predecessor: `lab/process/conditional-physics-ledger-v0.59.json`

```text
Ledger v0.60 — 82/82 active rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 open discrete forks
Quotients ranked — 4 scoped
```

## What moved

Five gravity rows—`LT-GR1`, `LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`—move
in distance, mapping grade and evidence only.

The parity-complete K77 source tangent satisfies the full linearized
superconnection Bianchi identity on all four columns. A labelled reciprocal
null leg also supplies an exact ambient rank-12 screen of signature `(6,6)`
without dividing by `q^2`.

The source return sharpens the object being tested: raw
`Upsilon_B=Shiab(F_A)+kappa_1 T`. The previous curvature-only grade-two fit
still cancels exactly, but the mandatory `kappa_1 T` term leaves a rank-four
residual with live Clifford grades one and two. The current graph therefore
fails total raw-`Upsilon` naturality and must be repaired as a coupled
all-grade graph.

## What did not move

Coverage, verdict counts, residue, quotient count, P1/P2/P3, Curt separation,
third-lane status, canon verdict and public posture remain unchanged. Bianchi
and null-homotopy identities are not counted as independent constraint
surplus. The ambient screen is not booked as the already-existing 4D physical
`10 -> 6 -> 2` quotient.

## Next

Solve the coupled all-grade raw-`Upsilon` graph including the mandatory
`kappa_1 T` term. Only a survivor advances to observation Euler,
preboundary, symplectic, common-domain and nonlinear BV/BFV descent on the
labelled null screen.
