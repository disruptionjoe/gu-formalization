# Selected K77 fixed-operator metric/epsilon leakage

Date: 2026-08-09
Disposition: `FIXED_OPERATOR_321_LEAKS__MOVING_COMPLETION_REQUIRED`

## Question

The preceding wave showed that the 196-dimensional grade-one connection bank
has no first-action cross into the 1,274-dimensional grade-two equation dual.
Does the remaining selected metric/epsilon source bank also close if Hodge,
Phi, Shiab, the frame and the observation section are held fixed?

Layer 0 is load-bearing: this frozen derivative is only one summand of the
source-coordinate Hessian.  GU's source grammar makes the omitted geometric
operators move.  Nonzero frozen leakage can therefore kill the shortcut, but
cannot kill the total moving construction.

## Exact construction

For each timelike, spacelike and null symbol representative, the probe pulls
back the ten metric and 91 epsilon principal directions to the two connection
slots as `(delta B,delta T)=(-u,u)`.  It differentiates the first-action
`E_T` covector coefficientwise in `1,b,t`, evaluates both exact stationary
branches in `QQ(sqrt(3))`, and tests every one of the 1,274 grade-two equation
covectors.  Grade labels select the 24 observed horizontal receivers and the
1,250 off-slice receivers.

The rank pattern is identical for all three causal representatives and both
branches:

| receiver | metric 10 | epsilon 91 | combined 101 |
|---|---:|---:|---:|
| all grade-two 1,274 | 9 | 91 | 97 |
| horizontal 24 | 9 | 6 | 12 |
| off-slice 1,250 | 4 | 88 | 89 |

The support is not identical: timelike has 487 nonzero entries, spacelike 489,
and null 976.  Their ranks nevertheless agree exactly.  Off-slice nonzero
counts are 445, 445 and 890 respectively.

An independent Sage/FLINT route reconstructs the sparse matrices over
`QQ(sqrt(3))` and checks the metric, epsilon, combined, horizontal and
off-slice ranks for all six causal/branch cases.

## Consequence

The frozen-Hodge/Shiab 321-field shortcut is not closed: its metric/epsilon
response reaches an 89-dimensional off-slice equation subspace.  This is a
useful construction target, not a GU no-go.  The full moving source Hessian
must contribute exactly the opposing metric rank-4, epsilon rank-88 and
combined rank-89 off-slice image if the 321-field candidate is to survive.

No field, coefficient, selector, quotient or datum is added.  The known 1,571
low-grade coordinates are not thereby promoted, and the first action remains
distinct from the raw residual and residual-square action.  Selected Spin,
the two `U(32,32)` halves and full `U(64,64)` also remain distinct.

## Next gate

Construct the moving metric, density, Hodge, Phi, Shiab, frame and observation
contributions to the same source-coordinate Hessian and test their exact sum
against the rank-89 off-slice target.  Only that total can decide whether 321
survives or whether the tangent must expand.  Expanded parents, gauge/ghost,
`Dmax/Dmin`, Green/Krein and BV--BFV remain downstream.

P1/P2/P3 are unused.
