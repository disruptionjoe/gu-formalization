---
title: "K121 K120 thermal Araki--Woods KMS boundary-net wave"
status: active_research
doc_type: reverse_scaffold_thermal_boundary_reservoir_result
created: 2026-09-03
date: 2026-09-03
target_claim: INTERNAL_TARGET:K120_PHYSICAL_OR_SOURCE_AUTHENTICATION_OF_STATE_CHANNELS_COUPLING_AND_EFFECTS
claim_ceiling: exact repository-owned single-equilibrium obstruction and multi-reservoir chiral KMS Araki--Woods realization of K115 with a locally Hadamard null-boundary CCR net, finite local energy density, exact thermal HP diagonal generator and output accounting; temperatures, pairing, couplings, work supply and effects remain selected, so no source/GU owner, one closed equilibrium bath, full spacetime AQFT, nontrivial gauge BV, Born derivation, prediction, confirmation or holdout credit follows
manifest: lab/process/k121-k120-thermal-araki-woods-kms-boundary-net-wave.json
probe: tests/channel-swings/k121_k120_thermal_araki_woods_kms_boundary_net_probe.py
---

# K121 K120 thermal Araki--Woods KMS boundary-net wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: single-equilibrium obstruction and exact multi-reservoir thermal Araki-Woods realization of the K115 continuum detector
carrier: nine-state K115 system tensor interval-local right-moving chiral-current CCR algebras in thermal or vacuum representations LAYER=observed CHIRALITY=N/A
pairing: quasifree KMS functional, Hilbert/Fock inner product and imported system trace/effect pairing ON=repository_owned_thermal_boundary_control
real_structure: complex conjugation of current test functions, Fock adjoint and system matrix adjoint
grading: system transition labels and bosonic occupation number; no source BV, BFV or ghost grading
action_owner: repository-construction
target: thermal incoming state, channel multiplicity, coupling, local observable net and K115 reduced dynamics MAP-TYPE=evaluation
```

Scope: this result binds one repository-selected chiral boundary reservoir and
the finite K115 generator. It does not bind a source/GU quantization, one
closed equilibrium bath, a full spacetime field net or a physical Born owner.

## Inline preflight bookend

The rebuilt frontier compared five serious arcs: one equilibrium Davies/KMS
bath, a grand-canonical chemical-potential bath, pairwise thermal channels,
a generic quasifree nonequilibrium state, and a source-action search. K117's
exact nonunit cycle is the cheapest route-changing discriminator: it kills a
faithful *single-equilibrium* detailed-balance owner before any spectral fit.
The pairwise thermal route survives and uses K120's own chiral carrier. It is
preferred to a generic fitted covariance because the KMS relation fixes each
asymmetric forward/reverse ratio and Araki--Woods doubling explains rather
than merely recounts the 36 noise coefficients.

Retrieval found K90's one-mode leading response, K97's CAR KMS state that does
not prepare its record, K99's finite product-KMS stream and three-level Davies
graph, K117's cycle obstruction and K120's vacuum continuum dilation. Those
facts are inputs, not novelty claims. No prior packet joins them into an exact
all-edge K115 thermal continuum owner. All inputs postdate the current
correction registry's relevant semantic boundaries; no stale correction is
being bypassed.

The route-changing lenses were open quantum systems, KMS spectral balance,
Kolmogorov reversibility, Araki--Woods representations, chiral CCR nets,
Hadamard/microlocal singularity, thermodynamic limits, HP calculus, boundary
actions, resource accounting, state/effect semantics and hostile philosophy
of science. The cheapest kills are a unit K115 cycle, failed rate
factorization, failed KMS ratio, a non-smooth local thermal correction, or an
attempt to hide the multi-reservoir work supply. The strongest contrary route
is a source-owned GU action deriving the same state and coefficients; none is
currently admitted.

## 1. One equilibrium KMS bath is impossible for exact K115

K117 exhibits the four-edge cycle with forward/reverse product

```text
R_cycle = 256/6561 != 1.                                    (1)
```

A faithful single-temperature KMS weak-coupling generator satisfying quantum
detailed balance is symmetric in its Gibbs GNS pairing. If its diagonal
algebra is invariant, the restricted classical generator is reversible and
obeys every Kolmogorov cycle identity. Equation (1) therefore excludes one
such equilibrium owner of exact K115. Equivalently, K115's strictly positive
stationary entropy production cannot be the diagonal restriction of a single
equilibrium detailed-balanced semigroup.

The quantifiers matter. This does not exclude multiple KMS components,
external work, chemical affinities, nonfaithful zero-temperature channels,
non-Markovian reductions or nonequilibrium quasifree states. It selects the
next construction class rather than ending the program.

## 2. Pair census and exact thermal decomposition

K115 has nine states and 36 directed edges, paired into 18 undirected
transitions. Nine symmetric pairs move the base state at rate `2/5`. Three
more symmetric pairs move between the two wrong records at rate

```text
q_equal = (7/11)(16/113)=112/1243.                         (2)
```

The remaining six record pairs are asymmetric. Orient each toward the correct
record. Their rates are

```text
q_down=(7/11)(81/113)=567/1243,
q_up  =(7/11)(16/113)=112/1243.                            (3)
```

For each asymmetric pair use one bosonic thermal channel with

```text
gamma=q_down-q_up=455/1243,
n=16/65,                 n+1=81/65,
exp(-beta omega)=n/(n+1)=16/81.                            (4)
```

Then exactly

```text
gamma(n+1)=567/1243=q_down,
gamma n    =112/1243=q_up.                                 (5)
```

Each symmetric pair uses two independent zero-temperature directed channels,
one in each direction. Thus there are six physical positive-temperature
channels and 24 directed vacuum channels. The Araki--Woods representation of
each thermal channel uses two vacuum noises, giving

```text
6*2+24=36                                                   (6)
```

vacuum noise modes—exactly K120's directed-edge multiplicity. Equation (6)
does not show that GU selected 36. It shows that K120's count is the minimal
standard declared doubled-noise bookkeeping for this particular pairwise thermal
realization of K115.

## 3. Null-boundary CCR net and KMS/Hadamard state

Use the derivative/current version of K120's right-moving massless
characteristic field, avoiding the scalar zero-mode infrared ambiguity. For a
bounded null interval `I`, let `A(I)` be the Weyl CCR algebra generated by
compactly supported current test functions in `I`. Support inclusion gives
isotony. Disjoint supports have zero symplectic pairing and hence commuting
Weyl algebras. Characteristic translation gives

```text
alpha_t(A(I))=A(I+t).                                      (7)
```

For each of the six thermal channels choose

```text
n_beta(omega)=1/(exp(beta omega)-1),
beta omega_0=log(81/16).                                   (8)
```

Its positive/negative frequency weights obey the KMS relation. At the
selected Bohr frequency `omega_0`, (8) gives exactly (4). In the doubled vacuum
representation the physical annihilator is

```text
b_beta=sqrt(n_beta+1) a_1 + sqrt(n_beta) a_2^*.             (9)
```

The current two-point function carries an extra frequency factor. Its thermal
minus vacuum part is proportional to
`omega/(exp(beta omega)-1)`, whose moments are finite at zero and decay
exponentially at high frequency. The difference is therefore smooth on every
bounded interval, so the thermal state has the same local positive-frequency
singularity class as the boundary vacuum. This is a null-boundary
Hadamard-class statement, not a theorem for the full two-dimensional scalar
field.

The renormalized chiral energy density is finite:

```text
epsilon_beta=pi/(12 beta^2) per thermal current,
epsilon_total=6 epsilon_beta=pi/(2 beta^2).                 (10)
```

The energy on the entire stationary half-line is infinite. The KMS state is
algebraic and locally normal, not a normal finite-total-energy Gibbs density
in K120's vacuum Fock representation.

## 4. Thermal HP generator and output law

For an asymmetric pair `i<->j`, let `V=|j><i|` point in the high-rate
direction. Equation (9) gives the two Lindblad coefficients

```text
L_down=sqrt(gamma(n+1)) V,
L_up  =sqrt(gamma n) V^*.                                  (11)
```

For a symmetric pair use `sqrt(q)|j><i|` and
`sqrt(q)|i><j|` on independent vacuum channels. Summing (11) and the symmetric
coefficients gives one operator for every directed K115 edge, with squared
norm exactly `q_ij`. The usual HP drift

```text
K=-1/2 sum_e L_e^*L_e                                      (12)
```

satisfies the coefficient identity. On diagonal density matrices the reduced
generator is exactly `pQ`, hence it preserves K115's stationary law. Adapted
output counts have intensity `p_i q_ij`; their stationary sum and
forward/reverse logarithmic pairing recover K119/K120 activity and K117
entropy production.

The six unequal KMS ratios plus the 24 directed vacuum inputs are an explicit
nonequilibrium resource. Coupling them to one nine-state system requires
channel routing and switching/work. That owner is declared, not made free by
calling every component thermal.

## 5. Regularized action and state/effect boundary

For compact frequency bands and smooth interval functions, double the six
thermal one-particle channels as in (9), retain the 24 vacuum channels, and use
K120's first-order finite-mode action with the interaction (11). Variation
owns the regularized joint unitary and endpoint symplectic form on the same
finite-particle `H1 subset L2 subset H^-1` rigging. The shifted-cotangent
extension gives the same minimal nongauge BV--BFV identity.

The KMS covariance is a boundary/state condition; classical variation does
not select `beta`. Output spectral projections and the quasifree expectation
define a positive normalized state/effect model. Their probability reading
uses the imported trace/Born pairing. No Born rule is derived.

## Inline postflight bookend

- **Strongest overclaim:** calling the construction one physical equilibrium
  bath. Refused: (1) forbids that, while the admitted model uses independently
  routed KMS/vacuum channels and work.
- **Strongest contrary construction:** a source-owned interacting GU action
  might derive a different polarization, state, channel organization and
  coupling while producing the same diagonal generator. K121 neither excludes
  nor selects that route.
- **Strongest mistyping risk:** calling local chiral-current Hadamard class a
  full scalar spacetime Hadamard theorem or calling a null-boundary net a full
  AQFT net. Both promotions are explicitly refused.
- **Weakest reproducibility seam:** the exact finite rate arithmetic does not
  itself prove the analytic KMS and smooth-difference theorems. The packet
  states the spectral formulas and their integrability argument; the probe
  checks the selected-frequency relation, moment convergence witnesses,
  interval locality and every finite generator coefficient.

All five admitted arcs completed. The exact certificate runs a clean baseline
before hostile mutations. No source/GU action, autonomous closed bath, finite
total reservoir energy, full spacetime AQFT, nontrivial gauge BV, Born
derivation, held-out score, prediction, confirmation, canon, paper, release or
public-posture surface moves.

## Next condition

Derive the chiral current polarization, temperatures, pair decomposition,
work source, coupling and state-effect meaning from one source-owned GU action
or a microscopic autonomous nonequilibrium field theory. Then extend the
null-boundary net through the bulk and gauge quotient before testing physical
state selection, Born or export claims.

## Reproduction

```bash
python3 tests/channel-swings/k121_k120_thermal_araki_woods_kms_boundary_net_probe.py
python3 tests/channel-swings/k121_k120_thermal_araki_woods_kms_boundary_net_probe.py --selftest
```
