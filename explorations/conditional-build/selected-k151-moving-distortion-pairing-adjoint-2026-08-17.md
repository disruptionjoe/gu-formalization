---
title: "Selected-K151 moving distortion pairing and weighted adjoint"
status: active_research
doc_type: exact_distortion_lowerer_weighted_formal_adjoint_and_first_lower_term_gate
created: "2026-08-17"
registry: lab/process/selected-k151-moving-distortion-pairing-adjoint.json
probe: tests/channel-swings/selected_k151_moving_distortion_pairing_adjoint_probe.py
grade: "K151 SERIALIZES THE SELECTED CL(7,7) DISTORTION PAIRING AS AN EXACT HODGE/SCALAR-CLIFFORD DENSITY-DUAL LOWERER, WITH INERTIA (18,38) ON A NONTRIVIAL 56-DIMENSIONAL NULL PACKET. PRIMALIZING K150 AND APPLYING K149'S COMPLETE WEIGHTED FORMAL ADJOINT REPLAYS K132'S FROZEN RANK-30 PRINCIPAL EULER BLOCK AT THE REFERENCE AND RATIONALLY ROTATED NULL COVECTORS. THE LIVE K150 COEFFICIENT JET FORCES A NONZERO RANK-27 ORDER-ZERO EULER TERM THAT PLAIN TRANSPOSE AND FROZEN-COEFFICIENT PLANTS MISS. THE CURVED METRIC BRIDGE IS STILL UNSERIALIZED, SO NO CURVED RESIDUAL OR NULL LEAKAGE IS DEFINED."
target_claim: K150_NEXT_GATE__MOVING_DISTORTION_PAIRING_AND_WEIGHTED_ADJOINT_COMPATIBILITY
target_verdict: MOVING_DISTORTION_PAIRING_EXACT__K132_FROZEN_WEIGHTED_ADJOINT_REPLAY__LIVE_RANK27_LOWER_TERM__CURVED_BRIDGE_REMAINS_NEXT
canon_verdict_change: none
---

# Selected-K151 moving distortion pairing and weighted adjoint

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `SOURCE_NATIVE_ROUTE`.

Scope: K151 binds the selected conditional `comm/symi/symi` distortion
coefficient on one exact 56-dimensional null packet in
`Omega1(Cl(7,7))`, its Hodge/scalar-Clifford density-dual lowerer, and the
resulting formal Euler operator through first coordinate jet. It does not
recover the preferred historical Shiab, compose the curved metric bridge, or
compute a curved restricted residual.

```gu-typed-objects
result: K151 moving distortion pairing and weighted formal adjoint
carrier: LAYER=ambient CHIRALITY=N/A Omega1(Cl(7,7)) selected 56-packet
pairing: Hodge/scalar-Clifford ON=distortion-56
real_structure: real Cl(7,7) with exact complex-coordinate bookkeeping
grading: one-form degree and Clifford blade grade
action_owner: repository-construction
target: density-dual formal Euler operator MAP-TYPE=homomorphism
```

## 0. Pre-wave and route-selection answers

1. **Typed object.** K150's matrix is a density-dual coefficient. It becomes a
   field-like first-order operator only after the indefinite lowerer's inverse
   is applied. The field operator, its formal adjoint, and the lowered Euler
   covector are different maps.
2. **Prior-art collision.** K149 already owns the generic formula
   `B_in^-1 rho^-1 L^T_formal rho B_out`; K132 owns only the frozen shortcut
   `(raw-raw.T)/2`; K77 owns the degree-one `(7,7)` pairing bank. Retrieval
   found no artifact composing those owners with K150.
3. **Cheapest kill.** A singular lowerer or failure to recover K132's frozen
   principal Euler block would kill this adapter. Neither occurs.
4. **Selected route.** The exact 56-dimensional packet dominates a dense
   229376-dimensional build: it is already large enough to retain a live
   coefficient jet, indefinite pairing, K132 rank, rotated control, and Green
   identity.
5. **Fallback.** A failed coordinate identification would return to K77's
   degree-one compound-metric lowerer before any propagation. That switch was
   not needed.
6. **Claim ceiling.** A formal lower coefficient is not yet the curved metric
   bridge, fivefold residual, null quotient transport, domain, or propagator.

The inline council included real Clifford/Krein algebra, exterior/Hodge
geometry, variational bicomplex, sparse differential operators, Lorentz/frame
covariance, microlocal typing, source custody, exact computation,
reproducibility, falsification, and hostile claim review. It rejected plain
transpose, dense all-carrier composition, and a synthetic matrix-only pairing
because each drops a load-bearing target object.

## 1. Exact pairing and lowerer

On the selected packet, let `e_a` be an explicit one-form/Clifford basis and
define

```text
B_ab = top_scalar(e_a wedge *(e_b)).
```

The resulting matrix is diagonal, symmetric, nondegenerate, and indefinite:

```text
dimension = 56,
inertia(B) = (18,38),
B^-1 B = B B^-1 = I.
```

The packet is selected for a live K150 coefficient jet, not for balanced
inertia. It remains an exact restriction of the ambient `(7,7)`
Hodge/scalar-Clifford pairing and is not replaced by a positive identity.

The coordinate motion is generated by the inner Spin element used in K150.
Pairing each moving basis vector with its moving Hodge dual gives

```text
B(0)=B,
dB/dt at t=0 = 0,
rho(0)=1,
drho/dt at t=0 = 0.
```

This is first-jet naturality, not a claim that a truncated exponential is an
exact global finite transformation.

## 2. Density-dual coefficient, primalized operator, and Euler owner

Let `C(t)` be K150's density-dual raw coefficient and `D_t` the selected
coordinate derivative. K151 keeps the maps typed as

```text
L(t) = B^-1 C(t) D_t,                 field-like raw operator
L^dagger = B^-1 rho^-1 L^T_formal rho B,
E(t) = (L+L^dagger)/2,                field-like formal Euler operator
E_dual(t) = B E(t).                   density-dual Euler covector
```

On the reference null packet,

```text
rank C(0) = 30,
rank C'(0) = 27.
```

The lowered principal coefficient reproduces K132 exactly:

```text
[E_dual]_(D_t) at t=0 = (C(0)-C(0)^T)/2,
rank = 30.
```

The complete formal transpose also differentiates the moving coefficient.
Because the selected Spin chart has constant first-jet pairing and density,
the first lower coefficient is

```text
[E_dual]_0 at t=0 = -C'(0)^T/2,
rank = 27.
```

This term is forced before any curved metric bridge is added. Freezing `C`
sets it to zero; using a plain field-coordinate transpose fails both the
indefinite principal coefficient and the exact Green identity.

## 3. Controls and rotated replay

The exact certificate checks:

- the K149 moving-density/pairing adjoint owner;
- the K150 next-gate boundary;
- K77's degree-one indefinite pairing bank;
- the 56-dimensional nondegenerate lowerer and its inverse;
- first-jet Spin naturality of pairing and density;
- K132's rank-30 frozen principal Euler coefficient;
- the nonzero rank-27 moving lower term;
- an exact weighted Green total-divergence identity;
- frozen-coefficient, positive-pairing, and plain-transpose plants; and
- K138's rationally rotated null covector, where frozen rank 30, weighted
  principal replay, and a live lower term all persist.

## 4. Hostile postflight

- **Strongest overclaim:** calling `-C'(0)^T/2` the curved residual. It is only
  the coefficient-motion part of the formal Euler operator.
- **Strongest contrary construction:** a positive identity pairing makes a
  different field adjoint and fails the native Green identity.
- **Weakest reproducibility seam:** the packet is finite and selected. K151
  proves adapter compatibility, not the complete 229376-carrier curved
  fivefold result.
- **Source ceiling:** the selected branch is repository-derived and remains
  distinct from the unrecovered preferred historical Shiab.

The result is exact at its declared packet and first-jet grade. No inverse,
closed domain, BFV class, physical mode, positivity, propagator, or GU-wide
verdict follows.

## 5. Closure and next gate

K151 closes the moving distortion pairing and weighted-adjoint dependency.
The curved metric bridge `A(g)` is still absent, so the selected restricted
fivefold residual and first null lower leakage remain
`UNDEFINED_CURVED_METRIC_BRIDGE_NOT_SERIALIZED`.

K152 should serialize the curved metric bridge from K127/K129's owned
selected-Shiab curvature linearization into the K149/K150/K151 operator
algebra, replay the frozen and rationally rotated metric packets, and stop
before fivefold composition unless the bridge passes those typed controls.

## K152 successor classification

K152 closes that bridge on an exact 112-dimensional distortion packet. It
serializes the normal-coordinate `-2 delta G` selected-Shiab operator,
replays the held K135 reference and rationally rotated null matrices
entrywise at rank four, retains K127's aligned and off-TT Weyl controls, and
constructs the moving weighted formal adjoint through its first lower
coefficient. The restricted residual remains
`UNDEFINED_FIVEFOLD_NOT_YET_COMPOSED`; K153 owns the smallest closed
variable-coefficient fivefold composition and first null lower leakage.
