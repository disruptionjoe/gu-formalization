# Conditional physics ledger v0.77

## Meter

```text
Ledger v0.77 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 5 scoped
```

## Migration from v0.76

The selected `comm/symi/symi` action covector has now been evaluated exactly
on all 16,384 real directions of the **pointwise** K77 `u(64,64)` comparator.
The full bank remains rank fourteen and its ten metric-normal rows remain
independent.  It has 549 live coefficient coordinates on the seed fixture,
distributed across Clifford grades `1,2,5` as `14,59,476`.

This changes a real conclusion from the low-grade calculation.  Although the
v0.76 `Cl1+Cl2` bank already had full row rank, its omitted grade-5 support
changes the observed coefficient-image inertia from `(5,5,0)` to `(4,6,0)`.
Rank was therefore not a sufficient completeness test.  A second exact
background retains ranks `14/10` and live grades `1,2,5` with a distinct
628-coordinate union.

The extension is complete at a point, not globally.  It does not patch the
covector over an actual K77 atlas, construct the physical observation section,
select Weinstein's preferred Shiab, or produce the global `tau_A0`/BFV moment
map and common Green/Krein domain.

Five rows migrate in distance, evidence and mapping grade: `LT-GR1`,
`LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`.  Verdicts, coverage, residue,
forks, five scoped quotients, P1/P2/P3, canon and public posture are unchanged.

## Frontier

```text
headline_delta: NONE
frontier_conditions_closed: 2
frontier_conditions_opened: 1
remaining_named_conditions: 2
```

Closed are the complete pointwise real-comparator action bank and the
full-support pairing correction.  Next patch that covector as a global
adjoint-bundle section and prove overlap naturality for the physical
observation section.  Then assemble the global `tau_A0`/BFV moment map and
common Green/Krein domain.  Keep the coupled nonzero-fermion residual and the
separate `I2B <-> ||II||^2` map out of this claim.

Machine truth: `lab/process/conditional-physics-ledger-v0.77.json`.
