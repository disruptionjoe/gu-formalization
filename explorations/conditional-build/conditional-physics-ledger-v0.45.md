---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-07
status: CURRENT_APPEND_ONLY_LEDGER__OBSERVATION_OWNER_RETYPED__SOURCE_NORMAL_JET_PRIMARY
machine_ledger: lab/process/conditional-physics-ledger-v0.45.json
predecessor: explorations/conditional-build/conditional-physics-ledger-v0.44.md
---

# Conditional physics ledger v0.45

## Meter

```text
Ledger v0.45 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 25 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

Five row distances/priorities move. Verdicts, residue, quotient count and
P1/P2/P3 do not.

```text
headline_delta: none
frontier_conditions_closed: 3
frontier_conditions_opened: 1
remaining_named_conditions: 4
source_return: SOURCE-CORRECTS + SOURCE-SILENT
collision_disposition: no collision; central claim acquired
```

## What moved

v0.44 correctly killed connection-only cancellation on the time generator,
but its next-step phrase could be read as adding a second observation field.
That reading is now rejected. The graph section `s_g` moves with the same
metric tangent `delta g`; stacking the two copies adds no rank. Complete
observation is an invertible equation receiver and transports rather than
cancels a Ward defect.

A narrower route remains live. Moving evaluation along `s_g` contains the
normal first jet of the ambient source residual. That term can be nonzero even
at a residual-zero point, but the existing on-section full-`II` pullback does
not determine it. The missing object is therefore a dependent chain-rule term
inside the total metric derivative, not a new field or datum.

## Current highest-information gates

1. Construct the source-native first normal jet of
   `Upsilon^B=odot F_A+*kappa_1 T` along the metric section.
2. Assemble the total metric-section derivative on the time generator and
   then all four diffeomorphism generators.
3. Compare that derivative with the conditional selected full-`II` owner map.
4. Only after a pass, extend through transverse source variables and derive the
   scalar/massless constraints, then the nonzero-fermion/common-domain stack.

No scalar pole, coefficient, external datum or new quotient was added. Curt
remains formally separate and no third lane is promoted.
