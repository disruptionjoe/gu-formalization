---
title: "Selected K77 induced-fermion principal discriminator"
artifact_type: exploration_result
created: 2026-08-10
status: PRINCIPAL_PARTIAL__W_EQUALS_ASD_MIRROR__LOWER_ORDER_SELECTION_OPEN
grade: "exact finite-field and Gaussian-rational principal-symbol result; no physical kernel, cohomology, domain, spectrum, chirality or count"
canon_verdict_change: none
---

# Selected K77 induced-fermion principal discriminator

## Result first

The released-source-guided K77 Dirac--Rarita--Schwinger principal operator is
informative, but it is not the physical carrier selector.

At an observed base-null covector, the exact coupled symbol on
`Q subset Omega1(S)` together with the source `Omega0(S)` field has:

| exact K77 one-form sector | one-form dim | coupled rank | characteristic kernel |
| --- | ---: | ---: | ---: |
| proposed self-dual `W` | 192 | 224 | 96 |
| anti-self-dual mirror | 192 | 224 | 96 |
| Lorentz doublet sector | 832 | 544 | 416 |
| Lorentz singlet sector | 640 | 448 | 320 |

All four natural representation submodules retain exactly half their one-form
dimension. Three planted generic rank-192 subspaces of `ker Gamma` instead
have characteristic kernels `1,0,0`. Thus the operator sees natural Lorentz
module structure and rejects a generic dimension-only fit, but it gives `W`
and its mirror the **same exact answer**. The critical `224/96` equality was
computed over the Gaussian rationals; it is not a floating-rank inference.

The complete symbol also reproduces Wave 2: rank `1920` off the null cone and
rank `1024` with kernel `896` on it.

## Layer 0

The computed object is the first-order characteristic symbol on
`Omega1(S)+Omega0(S)`. Its kernel at a null covector is propagation data. It
is not a physical zero-mode space, BRST cohomology, global spectrum, boundary
condition, or generation count.

The observation split is authorial plus-first
`(1,3)+(6,4)=(7,7)`. Exact Gaussian rationals are used only to resolve the two
Lorentz `sl2` factors inside that fixed real K77 module. No complexified
calculation is being asked to choose between K77 and K95.

## Why the three parents coincide here

The full `U(64,64)` source parent, the moving-Spin restriction, and the two
`U(32,32)`-half reduction constrain the **connection potential**. A connection
enters a Dirac-type operator at zero order. Consequently all three ablations
share the same derivative coefficient and the same principal fingerprint:

```text
nonnull rank 1920
null rank    1024
```

This is a typing result, not a finding that the three parents are equivalent.
Their first possible discrimination is in the zero-order `varpi` cells,
reality/pairing completion, BV differential, observation descent, or domain.

## Source return

- `SOURCE-CONFIRMS`: the `0 -> 1 -> 13 -> 14` carrier, the
  contraction-plus-Hodge middle grammar, and the southeast-zero candidate.
- `SOURCE-SILENT`: a globally selected operator, a carrier selector, exact
  reality/BV completion, physical domain, and family index.
- `SOURCE-CORRECTS`: none in this wave.

The draft's sixteen-cell equation 9.16 remains the lower-order construction
surface. Its known ambient-half-sign versus derivative-parity collision must
travel into the successor rather than being repaired by relabeling.

## Constraint surplus and interpretation

There is no unique selector whose surplus can honestly be ranked. Principal
order supplies **zero independent condition distinguishing `W` from its
mirror**, so at least a binary selector burden survives. That burden is not
booked as a new external datum: the next construction may still derive it
from zero-order action, reality, BV or domain structure.

The preregistered `PRINCIPAL_PARTIAL` horn fires. The strong negative would
have been “every rank-192 plane passes”; that is false. The strong positive
would have been “only `W` passes”; that is also false.

## Inline specialist pre-assessment

1. **Representation theory:** require `W`, mirror, 640, 832 and random
   controls; a rank-192 result alone has no selectivity.
2. **Clifford/reality:** execute on real K77 and declare the exact role of
   Gaussian rational branching.
3. **Hyperbolic PDE:** use null-symbol kernels only as characteristics.
4. **Operator theory:** separate principal, zero-order, global-domain and
   spectral questions.
5. **Variational analysis:** connection-parent restrictions cannot alter the
   principal derivative coefficient without changing the operator class.
6. **Symplectic geometry:** no characteristic kernel becomes a reduced phase
   space without a presymplectic current and coisotropic quotient.
7. **BV/BRST:** the physical discriminator may live in cohomology, but no
   differential is supplied here.
8. **Standard Model physics:** mirror equality is the decisive failure; the
   natural half-kernel by itself is not observed chirality.
9. **Source archaeology:** use equation 9.16 as a candidate grammar, not a
   unique global operator.
10. **Proof engineering:** use polynomial projectors, exact Gaussian ranks,
    finite-field whole-space checks and planted generic planes.

## Next gate

Assemble the complete draft-9.16 zero-order `varpi` and reality pairing on the
same K77 common carrier. Run the full-unitary, moving-Spin and two-half
restrictions as ablations and ask whether the resulting operator/BV/domain
structure distinguishes `W` from its mirror while rejecting random, 640 and
832 controls. Only a surviving physical kernel/cohomology may reopen index or
generation-count work.

No verdict, residue, quotient, datum, P1/P2/P3, canon verdict or public posture
changes.
