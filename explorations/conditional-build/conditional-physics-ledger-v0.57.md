# Conditional physics ledger v0.57

The complete four-column chain has been rebuilt on the settled K77 metric.
v0.46/v0.55 called their executed metric `(7,7)`, but its signs had inertia
`(9,5)`. Correcting the raw targets, selected-Shiab solve, Koszul inverse and
source-`varpi` composition changes every target and lift coefficient packet.
The pointwise theorem survives: all 117 transverse coefficients are still
reproduced with supports `57,34,34,34`, rank four and zero coefficient freedom.
The old coefficient values are superseded.

```text
Ledger v0.57 — 82/82 active rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

Five gravity rows migrate in mapping grade and evidence only; their distances
do not move. The next gate is not another carrier search: use only the
corrected K77 columns to construct a covariant four-column graph morphism,
count constraint surplus, and test Spencer/atlas integrability before total
raw-`Upsilon` Bianchi/naturality and survivor-only Euler/preboundary descent.

See the machine-readable [v0.57 ledger](../../lab/process/conditional-physics-ledger-v0.57.json)
and the [signature correction](selected-k77-cartan-spencer-signature-correction-2026-08-07.md).
