# Conditional physics ledger v0.59

Machine source: `lab/process/conditional-physics-ledger-v0.59.json`
Predecessor: `lab/process/conditional-physics-ledger-v0.58.json`

```text
Ledger v0.59 — 82/82 active rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 open discrete forks
Quotients ranked — 4 scoped
```

## What moved

Five gravity rows—`LT-GR1`, `LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`—move
in distance, mapping grade and evidence only.

The source-owned full labelled Clifford reduction resolves the primary
basicness fork. Its stabilizer is scalar `U(1)`, which acts trivially on the
adjoint lift, so the paired `(gamma_epsilon,L_epsilon)` object descends. The
v0.58 failure remains exact after forgetting that reduction to the horizontal
four-plane. Spencer uniqueness and an exact target-span rank test also close
the block-invariant replacement horn for the same targets.

The source return is corrected from silent to confirming/correcting ownership
of the full reduction. It remains silent on the observation section and
physical Euler descent.

## What did not move

Coverage, verdict counts, residue, quotient count, P1/P2/P3, Curt separation,
third-lane status, canon verdict and public posture remain unchanged. Orbit
transport identities are consequences of the construction and are not counted
as independent constraint surplus. No physical quotient is booked.

## Next

Compose the full-reduction graph with total raw-`Upsilon` Bianchi/naturality
and construct the null characteristic screen without non-null normalization.
Only a survivor advances to observation-section Euler, preboundary,
symplectic, common-domain and nonlinear BV/BFV descent.
