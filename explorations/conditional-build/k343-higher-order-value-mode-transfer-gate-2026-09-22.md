---
title: "K343 higher-order value-mode transfer gate"
status: internal_structural_result
claim_verdict: internal_structural_result
date: 2026-09-22
claim_ceiling: exact transfer gate and coherent quadratic-form interface for every K179 order-eight-through-twelve group; direct literal reuse of the order-seven K341 evaluator is rejected, while no higher-order numerical value, complete action column, residual, K152 interval, source/ledger move, canon, paper or public posture is emitted
manifest: lab/process/k343-higher-order-value-mode-transfer-gate.json
producer: tests/channel-swings/k343_higher_order_value_mode_transfer_gate.py
probe: tests/channel-swings/k343_higher_order_value_mode_transfer_gate_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: HIGHER_ORDER_COHERENCE_INTERFACE_CLOSED__NUMERICAL_CUBATURE_OPEN
canon_verdict_change: none
---

# K343 higher-order value-mode transfer gate

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

```gu-typed-objects
result: all K179 order-eight-through-twelve coherent groups have one deterministic coefficient/position/kernel interface, while literal K341 reuse is rejected at the first changed determinant-rank and native-cubature assumptions
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), restricted to the K162 zero-bath seed orbits LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Gram pairing after complete normalized specieswise exterior projection PAIRING-TYPE=hilbert
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering and real Bessel kernels
grading: conserved incidence charges, impurity seed, bath species, bath number eight through twelve, K179 contraction identity and ordered momentum provenance
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W and K179 fixes the coefficient family; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: construct the exact higher-order numerical interface without losing any K179 coefficient, contracted position or coherent cross term MAP-TYPE=intertwiner
```

## Preflight bookend

K340--K342 close the complete order-seven positive-cubature enclosure and name
orders eight through twelve as the next dependency. The route choice is not
whether to continue that action-column arc, but whether K341 is actually a
dimension-free integrator. Retrieval finds that K341 hard-codes four coherent
groups, a D4 times bordered-B5 functional, six-axis K299 weights, eight split
variables, twelve K334 radial/projective cells and the `(2*pi)^-9` prefactor.
Those are proved order-seven inputs, not generic parameters.

The structural route audits the complete K179 family first. It dominates a
blind numerical port because a wrong determinant rank or dropped cross term
would contaminate every later interval. An accurate K218 value remains a
separate alternative but has no instantiated K152 consumer margin. SC-ACT-06
remains separately dependency-limited. Source claims and physics-ledger rows
therefore remain unchanged.

## Exact coherence interface

For every coherent group `G`, K343 serializes the coefficient vector, every
contracted-position support, species multiplicities and a canonical digest of
all paths. The numerical consumer must evaluate

```text
                 c_G^T K_G c_G
```

with `K_G` the factorial-free species-determinant Gram kernel from K279. The
upper-triangle form uses each diagonal once and each off-diagonal twice. No
entry may be absolutized before the complete coherent group is formed.

| order | paths | groups | upper-triangle entries | ordered terms | maximum determinant rank |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 8 | 192 | 23 | 1,296 | 2,400 | 4 |
| 9 | 256 | 20 | 2,368 | 4,480 | 5 |
| 10 | 480 | 28 | 6,890 | 13,300 | 5 |
| 11 | 640 | 24 | 12,920 | 25,200 | 6 |
| 12 | 1,152 | 33 | 35,352 | 69,552 | 6 |

The combined interface covers `2,720` paths, `128` coherent groups, `58,826`
upper-triangle Gram entries and `114,932` ordered quadratic terms. Its digest
is `sha256:53cfc9cd0fa36fbe0ac8d8521fba21fe366cb6911829507327a4f0f082a37995`.

## Transfer decision

The factorial-free K279 kernel, delayed absolute value, radial-tail/face-error
separation and zero-safe determinant calculus through rank four transfer.
K305's four-group border identity, K299's weights, K340's terminal atlas,
K334's cover and K341's normalization do not transfer as generic data.

Order eight remains algebraically inside the existing rank-four Bessel
calculus, but it still needs its own native cubature/face atlas and group-level
interval evaluator. Order nine is the first unavoidable rank-five case.
Orders eleven and twelve require rank six. A literal loop changing only the
order number in K341 would therefore omit both new determinant ranks and
order-specific measure data; that route is rejected before it can emit a
number.

## Postflight bookend

The producer deterministically replays all higher-order K179 records. The
independent probe passes `18/18` controls and rejects `17/17` hostile mutations,
including dropped paths/groups/entries, contracted-position loss, determinant-
rank demotion, occurrencewise absolute values and false action-column or K152
claims.

Strongest overclaim: the exact interface evaluates order eight. It does not;
the native order-eight cubature and face-error proof remain absent. Strongest
contrary route: the order-seven rank-four atlas might cover every later order;
the exact multiplicity census refutes that at order nine and reaches rank six
at order eleven. Weakest reproducibility seam: group digests make dropped
algebra detectable, but they do not certify an interval backend or quadrature.

The next exact construction is an order-eight native value cubature and
group-level interval evaluator checked against all `1,296` order-eight Gram
entries. Only after that validation should the rank-five calculus for order
nine be built. No complete base action column, `R_ref` residual or native K152
interval follows from K343.
