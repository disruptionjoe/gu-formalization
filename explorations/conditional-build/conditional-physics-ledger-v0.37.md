---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-06
status: CURRENT_APPEND_ONLY_LEDGER__N2_HELICITY_ONE_NOT_SPIN_TWO__SECOND_LAYER_OWNER_MAP_PRIMARY
machine_ledger: lab/process/conditional-physics-ledger-v0.37.json
predecessor: explorations/conditional-build/conditional-physics-ledger-v0.36.md
---

# Conditional physics ledger v0.37

## Meter

```text
Ledger v0.37 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 25 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

Six distances and the priority order move. Verdict counts, residue, quotient
count and P1/P2/P3 do not.

## What moved

The unique positive two-extra-mode locus of the completed first-layer
grade-one source symbol is no longer a graviton candidate. On the exact
six-dimensional null kernel, the compact null-stabilizer rotation descends
through gauge to

```text
[ 0 -1]
[ 1  0]
```

so its characteristic polynomial is `x^2+1`: a real helicity-one doublet.
The helicity-two graviton module would have `x^2+4`. The multiplicity was two,
but the representation is wrong.

The differentiated full Schur pencil supplies a rank-two local principal
Green flux. It descends through the gauge radical and is definite at the
positive `N2` embedding. That is useful—it says the two modes are not merely
an isotropic algebraic accident—but it cannot repair their helicity and it is
not a global right-`H`/Krein, BV or BFV domain.

`LT-GR1`, `LT-GR2b`, `LT-GR3`, `LT-GR5`, `LT-GR6` and `LT-SM8` receive
distance migrations. The first-layer graph theorem remains exact on its
declared constant-`T` subspace, while the completed first-layer action has no
positive spin-two coefficient locus among the factors already found.

## Current highest-information gates

1. **Second-layer owner map:** construct or refute the source-native map from
   `I2B=||Upsilon_B||^2/2` to the observer `||II||^2` functional, including
   moving target metric, adjoint, cubic, Euler, helicity and preboundary
   classes.
2. **Physical carrier test:** ask whether that distinct action layer restores
   precisely two helicity-two null classes without nonnull characteristics.
3. **Domain only after type:** construct a common right-`H`/Krein and odd
   BV/BFV domain only after a correctly typed carrier survives.

No `kappa_1` value is selected. N2 is a scoped route kill, not a falsification
of every source-action completion or of GU.
