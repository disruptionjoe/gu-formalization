# Conditional physics ledger v0.74

## Meter

```text
Ledger v0.74 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 5 scoped
```

## Migration from v0.73

Two parts of the endpoint successor close.

The v0.25 primitive-epsilon Green identity already supplies two continuum
endpoint evaluations.  On a collar their trace map is locally surjective:
`eta(t)=(1-t)eta0+t eta3` realizes arbitrary tangent values at both ends.
This was composition debt, not a missing construction.

Two independent copies of the exact K77 endpoint dressing then recover the
full v0.70 local quotient without the killed single-holonomy compression:

```text
extended dimension: 60
two-form rank: 40
endpoint-gauge kernel: 20
quotient dimension/rank: 40/40
```

The v0.73 `20/20` single-holonomy theorem remains exact as a compression
no-go.  The direct-sum construction retains independent endpoint momenta.

Hostile review blocks the stronger epsilon-ownership inference.  The v0.25
Green coefficient is `i_n(E_B-E_T)`; the v0.69 contact momentum is `p=KT`.
Their potentials match if and only if `e0=p0` and `e2=p2`, but no current
artifact computes that coefficientwise weld on the selected K77 action.  The
remaining local gate is therefore one explicit equality, not another edge
field or another endpoint map.

Five rows migrate in distance, evidence and mapping grade: `LT-GR1`,
`LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`.  Verdicts, coverage, residue,
forks, five scoped quotients, P1/P2/P3, canon and public posture are unchanged.

## Frontier

```text
headline_delta: NONE
frontier_conditions_closed: 2
frontier_conditions_opened: 1
remaining_named_conditions: 2
```

Next compute `i_n(E_B-E_T)=p_KT` on both oriented endpoint cells and all ten
selected K77 normal/contact directions with the same invariant trace and
observation receiver.  Only a pass retypes the v0.70 edge coordinates as
boundary traces of existing epsilon; then full `tau_A0`, global BFV and the
common domain become the successor.

Machine truth: `lab/process/conditional-physics-ledger-v0.74.json`.
