# Conditional physics ledger v0.66

## Meter

```text
Ledger v0.66 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

## Migration from v0.65

The selected action's normal Euler jet is now constructed at exact
formal-variational grade as the mixed action Hessian

```text
D_normal E_field = D_normal D_field I.
```

All connection and epsilon directions pass on a noncyclic rational fixture.
Seven independently live owner classes—`B` jet, `T` jet, density, target/Krein
pairing, two Shiab legs, and Hodge/mass pairing—sum exactly to the full normal
packet. The printed residual normal jet differs and cannot be substituted.
Dependent ambient jets are not new fields or external data.

The exact packet inserts losslessly into the v0.65 complete-germ receiver.
The coefficientwise full-K77 seven-owner bank remains unassembled, so the
Green potential is not yet antisymmetrized.

Five rows migrate in distance, evidence and mapping grade only: `LT-GR1`,
`LT-GR2b`, `LT-GR3`, `LT-GR5`, `LT-GR6`. Verdicts, residue, quotient count,
P1/P2/P3, canon and public posture are unchanged.

## Frontier

```text
headline_delta: none
frontier_conditions_closed: 3
frontier_conditions_opened: 0
remaining_named_conditions: 2
```

Next assemble the seven owner classes coefficientwise on all ten normal
directions of the full real-K77 selected action, then antisymmetrize the
complete Green potential and test basic/common-domain/BFV descent.

Machine truth: `lab/process/conditional-physics-ledger-v0.66.json`.
