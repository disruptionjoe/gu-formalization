---
title: "Three global falsification labels corrected to bounded route kills"
status: active_research
doc_type: conditional_evidence_integration
created: 2026-08-23
registry: lab/process/conditional-ledger-verdict-context-integration.json
ledger: lab/process/conditional-physics-ledger-v0.262.json
probe: tests/channel-swings/conditional_ledger_verdict_context_integration_probe.py
grade: "EXACT LEDGER VERDICT-KIND AND APPEND-ONLY CONTEXT-HISTORY INTEGRATION; NO PHYSICS RECOMPUTED"
target_claim: NONE-NOT-A-KILL
target_claim_note: "The integration corrects repository-internal verdict kinds; no source claim or surviving route is killed."
canon_verdict_change: none
---

# Ledger verdict and CT-2 history integration

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: three ledger verdict kinds corrected from global falsification to bounded route kill, plus a digest-bound append-only repair of eight CT-2 history defects
carrier: ledger rows, migration records, context projections and the historical repair registry LAYER=toy CHIRALITY=N/A
pairing: each negative computation paired to its exact target claim, surviving alternative and predecessor row ON=repository-surfaces
real_structure: N/A
grading: exact repository-integrity integration; no physics recomputed
action_owner: N/A -- no action, coefficient, background or physical carrier is introduced
target: internal ledger verdict and context-history consistency MAP-TYPE=evaluation
```

## Disposition

The pending `GU-LEDGER-KILL-TYPING-2026-08-23` delta is incorporated into
v0.262. `AC-F3`, `LT-GR1b`, and `RA-D2` move from
`OVER_DETERMINED / GENUINE_FALSIFICATION` to
`DIFFERS / ROUTE_KILLED` because each row's own exact record bounds the result
to a route:

- `AC-F3` kills the conventional bulk-anomaly-inflow carrier for a
  source-disavowed repeated-generation framing; `AC-F4` already carries the
  same target class as `DIFFERS / ROUTE_KILLED`.
- `LT-GR1b` kills the repository-selected post-Shiab factorization while the
  source-aligned pre-Shiab Gauss/II route is explicitly named as open.
- `RA-D2` excludes fixed-W and degree-duality realizations, while its mapping
  grade ends `REPLACEMENT_SHIAB_OR_FUNCTIONAL_BUILD_OPEN` and its registered
  `SC-CHI-51` verdict remains `NOT_KILLED`.

Every computation, distance, revival trigger, evidence field, mapping grade,
and target-claim ceiling is byte-preserved. No row moves toward `SAME`; the
three remain negative results in the ledger's bounded-route column.

## Append-only CT-2 repair

Direct target-claim typing had changed eight v0.260 rows after that version was
minted, leaving the live CT-2 gate red because those touched rows lacked
context. v0.260 remains byte-identical. The repair has two additive parts:

1. v0.262 accretes reviewed `(layer, grant, carrier)` context on the eight
   rows; and
2. `lab/process/mint-context-history-repairs.json` pins the complete v0.260
   SHA-256, the canonical JSON-line SHA-256 of each source row, the named
   successor, and the exact successor context.

The gate accepts a historical repair only when every pin matches. Source-byte,
row-byte, successor-row, or successor-context drift is red. Unused registry
entries are red. CT-2's baseline remains zero, and no new touched row can use a
repair implicitly.

## Result and ceiling

Ledger v0.262 has 88 canonical targets: 33 `SAME`, 22 `DIFFERS`, 31 `NEEDS`,
and 2 `OVER_DETERMINED`. The change supplies no new physics, source reading,
action, background, physical quotient, prediction, confirmation, canon, or
public-posture movement.

## Hostile review

The strongest overclaim would be that retyping the rows makes the theory look
better. It does not: all three computations and all three open debts remain,
and none moves toward `SAME`. The strongest contrary reading is that `RA-D2`
really falsifies `SC-CHI-51`; its own registered target verdict says the claim
is not killed and its mapping grade names open replacement constructions, so
global falsification is strictly broader than the evidence. The weakest seam
is historical repair becoming a tolerance; exact source-ledger, source-row,
successor-row and context pins plus unused-entry failure keep it fail-closed.
