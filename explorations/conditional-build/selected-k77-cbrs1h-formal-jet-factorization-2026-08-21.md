---
title: "Selected-K77 CBRS-1H formal-jet field/graph factorization"
status: active_research
doc_type: exact_class_obstruction
created: "2026-08-21"
registry: lab/process/selected-k77-cbrs1h-formal-jet-factorization.json
probe: tests/channel-swings/selected_k77_cbrs1h_formal_jet_factorization_probe.py
grade: "EXACT FORMAL-JET METRIC OBSTRUCTION OVER ONE REPOSITORY-CONSTRUCTED POINT/ACTION CLASS"
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_TWO_CONNECTION_PRIMITIVE_EPSILON_AND_METX_GRAMMAR__REPOSITORY_DERIVES_THE_FIELD_GRAPH_FACTORIZATION_AND_CLASS_OBSTRUCTION__SOURCE_SILENT_ON_THE_CLASS
canon_verdict_change: none
---

# Selected-K77 CBRS-1H formal-jet field/graph factorization

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: CBRS-1H formal-jet field/graph factorization obstruction
carrier: real u(64,64) formal jets at the frozen anisotropic point LAYER=ambient CHIRALITY=N/A
pairing: selected K77 Clifford scalar-density pairing ON=real u(64,64) first-action carrier
real_structure: real B-skew Clifford grades plus i times B-self Clifford grades
grading: Clifford grades 0 through 14 with independent Spin-grade-two connection owner
action_owner: repository-construction
target: fixed-varpi intrinsic metric Euler covector MAP-TYPE=projection
```

## Result first

The frozen target-blind anisotropic point/action class is closed for **every
compatible formal jet**, not only for the first-jet carriers tested in
CBRS-1G.

The fixed-`varpi` Levi-Civita graph lands in the Spin-grade-two connection
owner.  On that same owner the action momentum is, by the selected first
action's independent-owner variation,

```text
p_2 = E_{B,2} - E_{T,2}.
```

Formal jet prolongation is linear.  For every multi-index `alpha`,

```text
j_alpha p_2 = j_alpha E_{B,2} - j_alpha E_{T,2}.
```

A compatible formal field jet satisfies both prolonged field-owner equations.
Therefore every graph-visible momentum jet vanishes.  The covariant
Levi-Civita coefficient map is still live: its horizontal matrix is `24 x 40`
with exact rank `20`, and those 24 rows embed injectively in all `1,274`
Spin-grade-two connection coordinates.  A planted off-shell momentum cell
fires its transpose.  The zero constrained graph image is thus caused by the
field equations, not by a dead source map.

The inherited selected-action density is

```text
221/55296,
```

so the normalized intrinsic metric row remains

```text
(-221/27648,0,0,0,221/27648,0,0,221/27648,0,221/27648).
```

The graph adjoint cannot cancel that nonzero four-cell trace.  No second jet,
higher formal prolongation, or uncomputed isolated Clifford-grade kernel over
this frozen point can make the class intrinsically metric-stationary.

## Actual owner check and dropped-owner controls

The theorem is not inferred only from notation.  On CBRS-1D's first actual
coupled `B2/T2` incidence, the independent-connection and translation rows are

```text
H = [[0,-1/24],[-1/24,17/18]],
```

and the grade-two momentum row is their exact difference,

```text
p_2 = [1/24,-71/72].
```

The matrix has determinant `-1/576`.  Two hostile plants show why both owners
are load-bearing:

- imposing only `E_T=0` admits the nonzero vector `(68,3)`, on which `p_2` is
  nonzero;
- imposing only `E_B=0` admits `(1,0)`, again with nonzero `p_2`.

The factorization closes only after the actual selected action's independent
Spin-connection equation and complete translation equation are both imposed.
It would not survive silently deleting either owner.

## Why a large second-jet matrix is dominated

At higher order the individual prolonged equations can contain inhomogeneous
lower-jet forcing.  That does not change the result.  On a formal solution
each complete owner equation still vanishes, and their difference still
vanishes coefficientwise.  Spencer, Bianchi, holonomicity and
primitive-epsilon constraints may shrink the compatible field-jet fibre; they
cannot give a nonzero image to a momentum map that is already in the field
ideal.

This also removes the vacuum relevance of the uncomputed grade-four through
grade-eleven Hessian ranks.  CBRS-1G had already proved that the complete
`T/T` Hessian is grade diagonal and that only `T2` couples to the connection
owner.  CBRS-1H is stronger for the metric question: regardless of which
isolated field kernels those ranks might reveal, every compatible formal
prolongation has zero graph-visible `p_2`.

## Primitive epsilon remains a separate equation

The primitive equation has the owned form

```text
E_epsilon = D_B^!(E_B-E_T) + (D_epsilon S)^! K_S.
```

The first summand vanishes on the compatible formal carrier by the same
factorization.  CBRS-1G's moving-Shiab base return is also exact zero.  This
result does **not** silently claim that every higher moving-Shiab prolongation
vanishes.  Any live higher term is an additional constraint and can only
shrink the carrier.  It cannot generate a metric source-graph return or cancel
the intrinsic density trace.

## Scope and hostile return

The result binds the repository-selected K77 conditional first action at

```text
T(a,b)=a e^0 gamma_0+b sum_(i=1)^13 e^i gamma_i,
(a,b)=(-13/96,1/48),
```

with independent real Spin-grade-two `B`, complete real-form `T`, and the
covariant fixed-`varpi` Levi-Civita graph.  It does not bind a different point,
action owner, connection reconstruction, metric source map, or source-owned
vacuum.

- **Strongest overclaim:** this is not a GU-wide no-go and does not exclude a
  materially distinct point or action class.
- **Strongest contrary construction:** a new action could carry a metric owner
  outside `p_2=E_{B,2}-E_{T,2}`, or a new point could have zero on-shell
  density.  Neither object is supplied by this class.
- **Strongest mistyping:** an unconstrained second jet can have nonzero
  graph-visible momentum; it is not a compatible formal field jet.
- **Weakest reproducibility seam:** the arbitrary-order conclusion is a
  formal-ideal induction, not a coordinate enumeration of infinitely many
  jets.  The actual owner identity, horizontal embedding, rank-20 graph and
  dropped-owner controls are executable.

The class is closed before full Hessian, stabilizer, `mu6`, `J`/Higgs, photon,
extra-`U(1)`, or gravitational-spectrum work.  No ledger verdict, canon,
source ownership, residue, quotient datum, particle assignment, prediction,
confirmation, or public posture changes.

## Next exact gate

`CBRS-1I` must freeze a materially distinct action-owned point or action class
before starting another jet tower.  The first discriminator is structural:
require either zero on-shell intrinsic density or a metric owner that does not
factor through the simultaneously imposed field equations.  A fitted
counterterm, a deleted field owner, or another prolongation of this closed
class is inadmissible.

Reproduce with:

```bash
sage -python \
  tests/channel-swings/selected_k77_cbrs1h_formal_jet_factorization_probe.py
```

The exact probe passes `56/56`.
