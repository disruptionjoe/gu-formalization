---
title: "K77 observed full-incoming operator-system wave"
status: active_research
doc_type: reverse_scaffold_full_incoming_ordered_operator_result
date: 2026-09-01
claim_ceiling: exact basis-independent ordered-operator and algebraic two-copy extension of the complete rank-960 incoming energy space; no physical gauge quotient, action-selected composite, prediction, confirmation, or verdict
manifest: lab/process/k77-observed-full-incoming-operator-system-wave.json
probe: tests/channel-swings/k77_observed_full_incoming_operator_system_probe.py
---

# K77 observed full-incoming operator-system wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: basis-independent full-incoming ordered-operator and conditional two-copy algebraic composite extension
carrier: H-self-adjoint endomorphisms of the complete rank-960 incoming energy space and of its rank-921600 labelled tensor square LAYER=conditional CHIRALITY=N/A
pairing: H-trace state-effect pairing ON=conditional_operator_extension
real_structure: real rank-960 incoming carrier with real symmetric H-self-adjoint density operators
grading: N/A; the incoming projector is not a constraint or BV quotient
action_owner: none; the incoming projector and positive principal energy are packet data, while quadratic-density and tensor-copy physical interpretations remain conditional
target: K77-PSX-3 through K77-PSX-7 on the full incoming energy space MAP-TYPE=construction
```

## Construction

Let `W_in` be the complete rank-960 range of the observed packet's incoming
projector on one supplied cooriented chart, and let `H` be its transported
positive principal energy. The pair `(W_in,H)` determines, without choosing an
`H`-orthonormal basis, the real ordered vector space of `H`-self-adjoint
endomorphisms:

```text
A(W_in,H) = {A : W_in -> W_in | A is H-self-adjoint},
A_+         = {A | H(v,Av) >= 0 for every v},
u(A)        = Tr_H(A).
```

Its real dimension is `960*961/2 = 461280`. Amplitudes map quadratically to
rank-one positive operators, so `v` and `-v` give the same state. Normalized
positive operators have `Tr_H=1`; effects are the interval `0 <= E <= I`.
These objects are canonical up to an `H`-isometry, rather than artifacts of a
selected coordinate frame.

Two labelled copies have amplitude carrier

```text
W_in(A) tensor W_in(B),       rank 960^2 = 921600,
```

and a real-symmetric density carrier of exact dimension `424673740800`.
The tensor trace supplies two algebraic partial traces. On one energy-
orthonormal two-plane in each complete copy, the predecessor Bell state embeds
as a sparse rank-one density with marginals `I_2/2`. A rank-one projector on
the first local basis ray together with its rank-959 complement defines a
full-space two-outcome instrument. Its nonselective sum preserves trace and
the remote marginal. An `H`-orthogonal quarter turn and half-dephasing extend
by identity outside the witness plane and preserve the same remote marginal.

The exact probe never allocates a dense `921600 x 921600` matrix. It verifies
the dimension identities and basis-free formulas with sparse rational states,
including full-space complement branches, product marginals, Bell marginals,
sign-ray identification, energy-orthogonal covariance, phase and decoherence.
It passes `20/20`; its hostile selftest catches `13/13` mutations.

## What advanced

The earlier result attached an imported two-coordinate control to two labelled
copies of a chart-local interface. This result removes that dimensional
bottleneck: the ordered carrier, trace, effects, tensor carrier and partial
traces now exist on the *complete* rank-960 incoming energy space and are
canonical up to energy isometry. Thus `K77-PSX-3` through `K77-PSX-7` have a
full-incoming conditional algebraic realization rather than only a two-level
adapter.

The advancement is mathematical ownership by the packet's supplied principal
data, not physical selection. Declaring two tensor copies to be physical
subsystems, declaring quadratic operators to be states, and interpreting the
trace as a Born rule still require the missing action, quotient and causal
domain.

## Boundary and next condition

`W_in` is a boundary-projector range, not an action-owned gauge quotient. The
rank-921600 tensor square is a composite of two labelled rank-960 copies; it is
not a factorization, quotient or dimension reduction of the ambient rank-1920
carrier. No global causal domain proves that the local instruments descend or
that the labelled-copy tensor product is the physical composition law.

The next release condition is therefore action/domain descent, not another
finite quantum control: construct an action-owned physical constraint quotient
on a named global causal domain, prove that the positive energy, quadratic
states, trace, composite and instruments descend representative-independently,
and only then ask for native state or observable-export credit. Delayed-choice
entanglement swapping remains reserved and unscored.
