---
artifact_type: conditional_build_result
created: 2026-08-12
status: LOCAL_FIRST_ORDER_COMPOSITION_PASSES__FULL_EULER_PREBOUNDARY_OPEN
---

# Selected K77 I2B radial Levi-Civita and section-q-row composition

## Result

The two packets left open by v0.209 close at **local first-order selected-action
grade**, for different reasons.

First, an exact signature-preserving coordinate permutation identifies the
four v0.209 base-fibre motions with the q-row of the previously certified
forty-dimensional observation-section Cartan lift.  These four directions are
therefore not new free ambient variables: they are the local moving-frame
response to a possible physical section jet.  The result does **not** select
the coefficients of the physical section, identify all forty directions with
q-motion, or construct the nonlinear observation receiver.

Second, the conformal/radial metric trace `delta g = f g` restricts the
covariant Levi-Civita first-jet map from rank `20` to an exact rank-`4`
subspace, one column for each base derivative of `f`.  At fixed `varpi`, the
curvature response cancels by the two-connection identity.  Every radial
Levi-Civita column changes the live nonzero I2B residual, but the change has
Clifford grade two while the branch residual has grade one.  Their exact
selected action pairing is therefore zero in all four directions:

```text
rank(delta B_LC | radial first jets) = 4
delta Upsilon_radial != 0  in 4/4 directions
delta I2B_radial = 0       in 4/4 directions
```

A grade-one connection perturbation gives the exact nonzero comparator `8/3`.
The zero is thus a typed grade-orthogonality result, not a permissive matcher,
a null residual, or an inherited residual-zero argument.

## Layer 0

Five objects remain separate:

1. one radial metric-trace value and its four base first jets;
2. nine normalized vertical-fibre motions;
3. four base-fibre q-row section/soldering motions;
4. Higgs amplitude `r`; and
5. the complete arbitrary-field Euler/preboundary variation.

The pointwise changing-gimmel trace compensator is symmetric and is not a
fixed-gimmel Cartan generator.  Conversely, the four q-row directions are
fixed-metric Cartan/Spin frame motions and are not metric trace variations.

## Exact receipts

- v0.209 ambient/fibre correction, v0.201 nonzero I2B branch and v0.95
  fixed-varpi Frechet packet replay;
- old and current signature orders are related by an exact permutation;
- the q-row has rank `4` inside the prior rank-`40` section-Cartan family;
- full spin Levi-Civita first jet retains rank `20`, radial restriction rank
  `4`;
- radial residual derivatives are nonzero `4/4`, action derivatives zero
  `4/4`;
- the grade-one plant fires at `8/3`;
- identity-coordinate, q-row-equals-forty, zero-residual and frozen-torsion
  plants reject.

## Constraint accounting

No field, scalar, quotient, selector or external datum is added.  P1/P2/P3
remain unused.  This removes the *local first-order* demand for a new datum in
these five directions; it does not reduce the ledger residue because the
physical section, nonlinear `epsilon_IG`, full observation receiver, arbitrary
field Euler map, second variations and presymplectic preboundary remain
unconstructed.

## Next gate

Construct the nonlinear `epsilon_IG`/observation receiver and vary the
remaining arbitrary fields inside the selected action, then derive the Euler
and presymplectic preboundary classes.  Preserve the distinction among source
`C^(32,32)+C^(32,32)`, its derived block subgroup, full `U(64,64)` and
independent connection fields.
