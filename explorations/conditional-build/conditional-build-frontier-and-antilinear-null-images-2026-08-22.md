---
title: "Conditional-build frontier replay and T3 antilinear null-image kernel"
status: active_research
doc_type: exact_frontier_revalidation_and_corrected_finite_proof_kernel_result
created: "2026-08-22"
registry: lab/process/conditional-build-frontier-and-antilinear-null-images.json
probe: tests/channel-swings/conditional_build_frontier_antilinear_null_images_probe.py
grade: "EXACT REGISTERED-CANDIDATE ADMISSION REVALIDATION AND LEAN-CHECKED FINITE COMPLEX HERMITIAN NULL-IMAGE TRANSVERSALITY; NO PHYSICAL-REAL-FORM, FREDHOLM, FUNCTION-SPACE, OBSERVED-HANDEDNESS OR GENERATION-SELECTION THEOREM"
target_claim: internal target T3-ANTILINEAR-NULL-IMAGE-TRANSVERSALITY; verdict corrected finite star-semilinear image theorem is Lean-verified from explicit positivity and image-isotropy premises
source_return: CORRECTION_AWARE_USE_OF_V15_1_AND_CARRIER_20260810__FINITE_COMPLEX_HERMITIAN_INTERSECTION_DIFFERENCE_ONLY
canon_verdict_change: none
---

# Conditional-build frontier replay and T3 antilinear null-image kernel

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: INTERNAL_STRUCTURAL_ONLY`

Scope: this result binds the repository's corrected finite-dimensional T3
null-image theorem and the substantial frontier after the L9 close. It is not a
theorem about the K-null Lorentzian half, a Fredholm or analytic index, observed
handedness, the GU-native `2+1` mechanism, a physical carrier, or generation
selection.

```gu-typed-objects
result: zero intersectionDifference for a positive complex subspace against two explicitly null images of a supplied conjugate-linear map
carrier: arbitrary finite-dimensional complex module with a supplied nondegenerate Hermitian sesquilinear form LAYER=toy CHIRALITY=N/A
pairing: supplied finite Krein form; positivity and total isotropy are independent explicit premises ON=complex-submodules
real_structure: complex conjugation appears only through Mathlib star-semilinearity; no physical Lorentzian real-form transfer is inferred
grading: two supplied source subspaces mapped by one conjugate-linear map; no ordinary eigenspace construction for the antilinear map
action_owner: repository-construction -- no source action, coefficient, stationary point, or physical selector is introduced
target: corrected finite Hermitian intersection-dimension statement MAP-TYPE=evaluation
```

## Result first

The stable theorem queue still contained T3 at `SYMPY-DERIVED`, even though L9
had closed the numbered F/G sequence. A correction-aware frontier replay makes
T3 the strongest executable proof gate before conditional old-file triage.
Lean now types the operator as a complex star-semilinear map, constructs the
image of each source subspace as a complex submodule, and proves that explicit
pointwise nullness of those images is equivalent to total isotropy of the
mapped submodules. A strictly positive subspace meets each such image only in
zero, so the difference of the two intersection dimensions is zero.

The map type does not imply nullness. `ImageTotallyIsotropic` is a separate
premise, and a negative control proves that one mapped vector with strictly
negative quadratic value refutes it. The formal statement therefore records
the exact delimited class instead of treating conjugate-linearity, equivariance,
or carrier provenance as a hidden admissibility theorem.

## Correction-aware input custody

The July symbolic owner artifacts predate the correction registry. V15-1
requires the result to be read as finite complex Hermitian transversality for
`intersectionDifference`, not physical handedness or a Fredholm index, and
forbids unchanged transfer to the K-null physical Lorentzian half.
CARRIER-20260810 additionally keeps the fixed carrier as a supplied input
rather than a derivation. The Lean statement adopts both corrections:

- it uses no physical, observed-chirality, family-index, or Fredholm identifier;
- it does not encode the `192/96/96` carrier as selected or faithful;
- it uses mapped subspaces rather than ordinary eigenspaces of an antilinear map;
- it makes image isotropy explicit; and
- it proves no function-space, domain, boundary, or source-native `2+1` result.

## Lean kernel

`Lean/GUFormalization/LocatedNotForcedLegs.lean` now contains:

- `AntilinearImage`, using `V →ₗ⋆[ℂ] V` and `Submodule.map`;
- `ImageTotallyIsotropic`, the explicit pointwise Hermitian premise;
- `imageTotallyIsotropic_iff`, relating the premise to the mapped submodule;
- `positive_inf_antilinearImage_eq_bot`;
- `antilinearImage_intersectionDifference_eq_zero`; and
- `negative_value_not_imageTotallyIsotropic`, the firing contrary control.

The proof reuses the already checked positive/null transversality theorem. Its
new content is the corrected conjugate-linear image typing and the auditable
separation between map structure and null-image admissibility.

## Reranked substantial frontier

1. **T3 antilinear null-image transversality — closed here.** The corrected
   finite theorem is Lean-verified without physical-transfer language.
2. **CBRS-1 new action class — parked.** No registered candidate now supplies
   all owner-before-evaluation fields.
3. **CBRS-2 observed `3+1` bridge — dependency-blocked.** It still requires an
   actual complete CBRS-1 local solution.
4. **C1 source packet — parked.** Seven owner-grade axes remain absent.
5. **B5 physical action/boundary reopener — parked.** No new metric/coframe
   action or physical BFV owner packet is registered.
6. **P-OBS held-out confrontation — monitor only.** No applicable official
   release or construction-grade finite-redshift bridge has arrived.

L10 old-file triage is now eligible only as its existing conditional
maintenance fallback: the next Progress run still rebuilds the substantial
frontier and selects L10 only if no newly enabled higher-value scientific or
stable-proof arc appears. Triage must repair actual certificate confusion, not
manufacture activity.

## Route-selection and hostile review

The route-changing census considered Layer-0 semantic typing, current
corrections, finite Krein geometry, complex semilinear algebra, proof-system
design, manuscript wording, source-native/comparator separation, and program
counterfactual effect. Four routes were compared: arbitrary null subspaces
(already proved but does not type T3), a star-semilinear image theorem
(selected), a fixed 192-dimensional matrix theorem (too narrow), and a
Lorentzian/Fredholm/function-space theorem (unsupported and prohibited by the
corrections).

The strongest overclaim is to say Lean proves an antilinear operator is
admissible or physically relevant. It does not: null-image admissibility is an
assumption. The strongest contrary construction is any conjugate-linear map
whose image contains a negative-norm vector; the formal negative control rejects
that case. The weakest propagation seam is stale “no antilinear content” or
physical-index wording; the exact probe checks the Lean map, ledger, agenda,
current state, and front door together.

No scientific ledger verdict, canon, source ownership, prediction credit, or
public posture changes.
