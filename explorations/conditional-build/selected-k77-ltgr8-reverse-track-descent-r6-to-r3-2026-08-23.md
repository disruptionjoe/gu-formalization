---
title: "LT-GR8 reverse track, executed as a descent: rungs R6 to R3 frozen, with the R2/R1 demand interface"
status: active_research
doc_type: frozen_reverse_track_specification_and_demand_interface
created: "2026-08-23"
directed_by: "Joe direct chat, 2026-08-23 (execute the reverse track top-down: start at the known-physics rung and descend; stop treating action-side work as the reverse scaffold)"
registry: lab/process/selected-k77-ltgr8-reverse-track-descent-r6-to-r3.json
probe: tests/channel-swings/selected_k77_ltgr8_reverse_track_descent_r6_to_r3_probe.py
grade: "FROZEN COMPARATOR-DRIVEN SPECIFICATION ONLY; DEMANDS, NOT CONSTRUCTIONS; NO GU OBJECT, STATE, BOUNDARY, ENTROPY, TEMPERATURE, CONSTRAINT OR VERDICT IS BUILT OR CHANGED"
target_claim: ledger row LT-GR8 (verdict NEEDS unchanged); this artifact freezes the reverse track's target-side rungs R5, R4, R3 and types the terminal demand interface on R2/R1
canon_verdict_change: none
---

# LT-GR8 reverse track, executed as a descent: R6 to R3

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: every object in this artifact is a comparator-driven DEMAND chosen
because of the frozen external benchmark, per the reverse track of the 8-1-1
council protocol. Nothing here is a GU construction, a source attribution, or
evidence in either direction. Mechanism commitment and confirmation credit
remain NONE; reverse-only success remains compatibility.

```gu-typed-objects
result: the reverse track's target-side rungs frozen as typed demand packets (R5 accounting, R4 boundary/algebra, R3 constraints/state space) plus the terminal R2/R1 demand interface
carrier: demands over a hypothesized observed 3+1 Lorentzian system LAYER=observed CHIRALITY=N/A
pairing: demanded positive or Krein-superselected form, not constructed ON=future-physical-quotient
real_structure: N/A at specification grade
grading: N/A at specification grade
action_owner: N/A -- no action term, boundary law, state, ensemble, contour or measure is selected; every dependency is labeled comparator-driven and frozen before any target-facing comparison
target: LT-GR8's realization obligation, approached from the benchmark side MAP-TYPE=not-a-map
```

## Rung registry and execution order

The council's reverse track is registered as descent rungs `R6..R1`. The `R`
vocabulary is deliberate: the repository's `L1..L7` names the six-axis legs
and `Layer 0` names semantic alignment, and reusing "layer" here would plant
the exact homonym trap Layer-0 exists to catch.

| rung | object | status |
| --- | --- | --- |
| R6 | scoped external benchmark phenomena (EXT-J95: local-horizon entropy, first law, Einstein-like dynamics) | FROZEN 2026-08-21 (council, 8-1-1) |
| R5 | entropy and energy-flux accounting | FROZEN HERE |
| R4 | physical causal boundary and observable algebra | FROZEN HERE |
| R3 | canonical constraints and reduced state space | FROZEN HERE |
| R2 | observed 3+1 system and observation map | typed 2026-08-22 at metric-only ceiling; receives the demand interface |
| R1 | K77 source action and external datum | CBRS-1 lane; receives the demand interface |

**Execution-order correction (Joe direct chat, 2026-08-23).** Reverse-track
work descends `R6 -> R5 -> R4 -> R3` and hands `R2/R1` a typed demand
interface. Action-side candidate work (CBRS-1) is forward/R1-side work: it
retains its own exact reopening predicate but no longer stands in for, or
gates, the descent. Only *instantiation* of descent demands on an actual
background remains CBRS-blocked.

## R5 — entropy and energy-flux accounting (frozen, comparator-driven)

1. **Entropy functional.** Leading term `S = eta * Area` with `eta` frozen as
   ONE undetermined normalization constant. `eta` is not a prediction target:
   the council recorded that area entropy density may define the coupling
   rather than predict it, so any future claim of predicting `eta` requires a
   separately owned normalization argument. Higher-curvature corrections are
   admissible only in Wald--Noether charge form and must be declared before
   any target-facing comparison.
2. **Energy flux.** `deltaQ = integral over the horizon of T_ab chi^a dSigma^b`,
   with `chi` the R4 wedge's local boost vector — the Rindler flux definition,
   frozen now.
3. **Equilibrium law.** Leading-order Clausius `deltaQ = T deltaS`. Any
   entropy-production or nonequilibrium term is a NEW declared dependency and
   must be frozen before use, never fitted after a mismatch.
4. **Comparison observable.** Recovery of Einstein-like dynamics at leading
   order; the discriminating observable is the form of the first
   higher-curvature correction (Wald-consistent versus not).

## R4 — physical causal boundary and observable algebra (frozen)

1. **Boundary identity.** The local Rindler wedge family through each point
   of the observed system — frozen because the R6 theorem quantifies over all
   local Rindler horizons. Not the cosmological horizon, not a York boundary,
   not the analytic operator domain, not a BV--BFV boundary, not the
   capability boundary: the 2026-08-22 six-way census keeps these types
   distinct, and switching boundary type after a result is prohibited.
2. **Causal prerequisites.** A two-component causal orientation and a local
   approximate boost flow. Both exist only post-observation: the 2026-08-22
   T-4 certificate proves the ambient `(7,7)` metric supplies no orientation,
   so every R4 object lives strictly on the observed side of the R2 map.
3. **Observable algebra.** An isotone, boost-covariant assignment of algebras
   to wedges, with temperature DEFINED as the KMS parameter `kappa/2pi` of
   the wedge state class under the boost flow — a definition, not a claim
   that such a state class exists.
4. **The in-repo wall, imported at its grade.** On the W98 Krein tower class
   the repository's own result is that no coherent net of bounded modular
   conjugations exists (the sectorial-closure retraction, `tests/W98`,
   `tests/W100_obj2`), while the one-sided per-state structure survives
   (Observer Structure Theorem, `tests/W109`). The R4 demand is therefore
   typed ONE-SIDED: a state class whose wedge restrictions satisfy the KMS
   condition for the boost flow, WITHOUT demanding a global modular
   conjugation net. A future construction that needs the two-sided object
   must first overturn that recorded wall.

## R3 — canonical constraints and reduced state space (frozen)

1. **Constraint structure.** An even scalar Hamiltonian constraint with a
   lapse-type multiplier plus spatial diffeomorphism constraints on the
   observed system. The banked method ports bind here: constraint-reduced
   state meaning precedes any covariant state count, and a named physical
   boundary/ensemble precedes any contour, convergence, entropy or sign
   interpretation (Jacobson councils, 2026-08-21).
2. **Reduced state space.** A physical pairing that is positive, or Krein
   with a ghost-parity `Z2` superselection and generalized Born rule (the
   six-axis L7 menu's indefinite option). Any candidate must meet the K109
   entry gate: selector, pairing and analytic domain typed on the actual
   carrier — no invariant-linear repair, which K107 closed.
3. **Imported typed negatives.** The strict B5 `(9,5)` odd spinor normal
   multiplier is not an ADM lapse (`WRONG_TYPE`); the released source names
   the ultrahyperbolic boundary problem but supplies no physical boundary law
   (K104); the invariant balanced fibre Hamiltonian is unbounded with
   nonelliptic symbol (K106). These are inherited constraints on what can
   discharge the R3 demands, not new results.

## The R2/R1 demand interface (terminal deliverable)

What the observation map and source action must eventually supply, stated as
six typed demands:

1. an even scalar Hamiltonian constraint and lapse-type multiplier arising
   after observation (the B5 `WRONG_TYPE` gap, now a demand rather than a
   comparator failure);
2. a physical boundary law / boundary variational owner (the K104 gap);
3. a state class realizing the one-sided wedge-KMS structure on a positive or
   Krein-superselected physical pairing (the K106--K111 gap, K109 entry
   gate);
4. the observed-side composition of the native Hilbert stress through the
   contraction `s^*` (the one PARTIAL component of the 2026-08-22 packet
   map);
5. post-observation causal-orientation selection data (the T-4 certificate
   makes this a required import or construction, not a default);
6. one held-out consequence reserved at packet time (the no-retuning rule).

Descent progress is measured against this interface: an R1-side result that
discharges none of the six demands is not reverse-track progress, whatever
else it is.

## Route selection and hostile review

The census ran a protocol-fidelity lens (the council's reverse track is
executed in its stated order for the first time, rather than paraphrased), a
semantic-alignment lens (the `R` vocabulary avoids the `L1..L7`/`Layer 0`
homonym; temperature and entropy are introduced as definitions with owners,
not as facts), a modular-theory lens (the KMS demand is reconciled with the
recorded no-bounded-conjugation wall instead of silently contradicting it), a
no-retuning auditor (every R5/R4/R3 choice is frozen here, before any
target-facing comparison exists to leak back), and a hostile claim-ceiling
lens. The hostile pass pressed: "frozen demands are just prose." They are
prose with teeth: each demand names the in-repo gap it lands on
(WRONG_TYPE, K104, K109, PARTIAL, T-4, no-retuning), the probe pins the
frozen fields against silent drift, and the interface defines what counts as
descent progress. The second attack — "this reopens CBRS-2 by the back
door" — fails: nothing observed-side is constructed; the observed system
remains hypothesized, and instantiation stays CBRS-blocked.

The strongest overclaim would be reading any frozen demand as a GU property.
No demand asserts existence; three of the six land on gaps the repository has
proved are presently undischarged. No scientific ledger verdict, canon,
source ownership, prediction credit, or public posture changes; LT-GR8
remains `NEEDS`/`MISSING_CONSTRUCTION`.
