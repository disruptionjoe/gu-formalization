# Conditional physics ledger v0.170

```text
Ledger v0.170 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Tightness: T4×1 T3×3 T2×1 · scoped quotients: 5
Frontier: 2 conditions closed · 4 opened · 5 named conditions remain
```

## What changed

Craig--Weinstein's scalar nonlocal support condition was typed against the
actual real-K77 dimension-1920 source-shaped operator. Scalar center support
alone retains the exact rank-128 square-zero Jordan defect.

The operator supplies a minimal additional frequency polarization,
`N(k) u_hat(k)=0` with `N(k)=E(k)^2-rho(k)^2 I`. On the strict center cone this
removes the generalized chains at flat principal grade, leaves a
1792-dimensional kernel and preserves the complete rank-640 observed
four-vector-plus-`nu` carrier.

Six rows migrate in distance and evidence only: `RA-D4`, `RA-F1`, `RA-F2`,
`RA-G2`, `LT-SM3`, and `AC-F1`. Verdicts, residue, forks, quotients, P1/P2/P3,
canon verdicts and public posture do not move.

## Why the frontier grew

This is a candidate domain ingredient, not a closed domain. Construction made
four compatibility demands explicit: selected-action/Green, BFV, curved
overlap/pseudodifferential completion, and nonlinear propagation. That is
positive information: the program has moved from "find some constraint" to a
specific object with four killable gates.

## Ranked successor

1. Test strict-center plus `ker N` against the action-owned Green form and
   selected boundary coefficient.
2. If it survives, construct or kill curved patch-overlap/pseudodifferential
   and BFV compatibility; only then test nonlinear propagation.
3. Keep the source-admitted wedge-Shiab/nonzero-southeast semisimple operator
   completion separate and compare it if the restriction route fails.

Machine-readable truth remains
`lab/process/conditional-physics-ledger-v0.170.json`.
