---
title: "LT-GR8 D6: the held-out consequence family and reservation protocol, frozen at packet grade"
status: active_research
doc_type: frozen_preregistration_family_and_protocol
created: "2026-08-23"
directed_by: "Joe direct chat, 2026-08-23 (continue the descent; execute the next demand)"
registry: lab/process/selected-k77-ltgr8-heldout-consequence-reservation-protocol.json
probe: tests/channel-swings/selected_k77_ltgr8_heldout_consequence_reservation_protocol_probe.py
grade: "FROZEN PREREGISTRATION FAMILY, SELECTION RULE AND RESERVATION PROTOCOL ONLY; NO CONSEQUENCE VALUE, PACKET, DERIVATION, CONFRONTATION OR VERDICT EXISTS OR IS CONSTRUCTED"
target_claim: "LT-GR8 demand D6 at protocol grade; the reservation itself executes only at packet time; LT-GR8 remains NEEDS / MISSING_CONSTRUCTION"
canon_verdict_change: none
---

# LT-GR8 D6: the held-out consequence family and reservation protocol

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: demand D6 of the descent interface asks that one held-out consequence
be "reserved at packet time." Because no packet exists, what CAN be frozen now
— and what the no-retuning rule requires to be frozen now — is the
preregistration FAMILY the consequence will be drawn from, the SELECTION RULE
that picks within the family, and the RESERVATION PROTOCOL that executes at
packet time. Freezing these before any packet exists removes the freedom to
choose a flattering consequence after seeing what a packet predicts. No
consequence value exists yet; mechanism commitment and confirmation credit
remain NONE.

```gu-typed-objects
result: the held-out consequence family (primary and fallback), its selection rule, and the packet-time reservation protocol, frozen before any packet exists
carrier: none constructed -- preregistration objects only LAYER=protocol CHIRALITY=N/A
pairing: none constructed
real_structure: N/A at protocol grade
grading: N/A at protocol grade
action_owner: none -- no action, packet, consequence value, dataset read, or comparison statistic is constructed; recorded W113 dark-energy numbers are cited at their recorded grade as ALREADY-READ and therefore inadmissible as held-out data
target: LT-GR8 demand D6 MAP-TYPE=preregistration
```

## Result first

Three objects are frozen:

1. **Family F1 (primary) — cross-sector normalization consistency.** A
   completed K77-to-observed-3+1 packet derives its entropy normalization
   `eta` from the action. The same action already carries the repository's
   dark-energy sector at its recorded grade. F1's consequence is the packet's
   implied relation between `eta` and the dark-energy amplitude, confronted
   against BAO/BAO-like amplitude-calibration data **released after the
   packet freeze**. The in-repo DESI DR2 numbers (marginalized `A* = 30.8059`,
   own-theta_star `A_GU = 31.9715`, canonical `f_0 = 0.125` shape) are
   already read and recorded, and are therefore INADMISSIBLE as held-out
   data; they may seed the derivation, never the confrontation. The
   pre-recorded exclusion observable already stands in `VERIFICATION.md`: a
   future BAO amplitude calibration confirming the Planck value at
   sub-percent precision excludes the family. F1's data wake is the existing
   `P-OBS` monitor lane.
2. **Family F2 (fallback) — the nonequilibrium correction.** The R5 packet
   froze the reverse build to the leading-order equilibrium Clausius law, so
   a packet's derived boundary law implies a nonequilibrium/entropy-production
   correction the reverse build never used. F2's consequence is that
   correction's functional form, confronted against entries of the
   2022--2025 model-scoped Jacobson-family test library that were not used in
   the reverse build.
3. **Selection rule (frozen).** F1 is selected unless the packet contains a
   typed derivation that its action leaves the dark-energy amplitude
   undetermined — a non-coupling proof, not a convenience claim. In that case
   F2. If neither family is realizable from the packet, the packet earns
   compatibility only. Substituting any third family requires a new council
   decision and VOIDS prediction credit for that packet.

## The reservation protocol (executes at packet time)

At packet completion, in order, before any target-facing comparison is read:

1. Apply the frozen selection rule and state the consequence with numeric
   content: a value, interval, or explicit functional form, plus the
   comparison statistic.
2. Record it in a dedicated registry file naming the packet's commit hash and
   the base conditional-physics-ledger digest.
3. Commit that record. Only after the record is committed may any
   target-facing comparison be read or computed.
4. The confrontation may use only data released after the freeze commit, or a
   blind subset pre-registered by name inside the record itself.
5. Any deviation — retroactive edit, family swap, statistic change, or
   early reading of target-facing results — voids prediction credit: the
   packet's ceiling drops to reverse-only compatibility.

Credit grammar, frozen with the protocol: a successful held-out confrontation
makes the packet a **prediction candidate** pending independent verification.
It is never summarized as GU confirmed, and a failed confrontation kills only
the scoped GU--Jacobson bridge per LT-GR8's recorded kill scope.

## Descent-interface accounting

With D6 at protocol grade, the descent-side executable queue is empty:

- D1--D3 are R1-side demands (constraint/lapse after observation, boundary
  law, one-sided wedge-KMS state class) — they wait on source/action owners.
- D4 closed 2026-08-23 as an exact type correction: the existing Hilbert
  stress is already observed; only a hypothetical ambient tensor route is
  typed, and its input, density conversion, section-chain variation and
  causal flux are unowned.
- D5 is blocked on a new orientation owner (the T-4 certificate prices it).
- D6 is protocol-frozen here; the reservation itself executes at packet time.

Descent progress therefore now depends entirely on R1-side owners, measured
against the interface. Selection returns to the substantial frontier.

## Route selection and hostile review

The census ran a preregistration-methodology lens (family, selection rule and
protocol frozen before any packet can leak a preferred consequence), a
data-custody lens (already-read DESI DR2 numbers typed inadmissible; only
post-freeze releases or pre-named blind subsets count), a cross-sector
physics lens (F1 exploits the one structural fact that makes a GU packet
falsifiable beyond its build inputs: one action must normalize both the
entropy constant and the dark-energy amplitude), a scoped-fallback lens (F2
stays inside the benchmark's own model library so the fallback cannot smuggle
a new comparator), and a hostile claim-ceiling lens. The hostile pass
pressed: "a family chosen now can still be chosen flatteringly." True but
bounded — the choice is made before any packet exists, against gaps the
repository has proved open, and the selection rule's only escape hatch (the
non-coupling proof) must itself be a typed derivation inside the packet. The
second attack — "this manufactures prediction credit" — inverts the content:
the protocol only ever REMOVES credit (void conditions), and its success
ceiling is "prediction candidate pending independent verification."

No scientific ledger verdict, canon, source ownership, prediction credit, or
public posture changes. LT-GR8 remains `NEEDS`/`MISSING_CONSTRUCTION`.
