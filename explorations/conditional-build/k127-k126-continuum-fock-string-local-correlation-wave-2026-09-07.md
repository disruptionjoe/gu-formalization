---
title: "K127 K126 continuum Fock string-local correlation wave"
status: active_research
doc_type: reverse_scaffold_continuum_fock_string_local_correlation_result
created: 2026-09-07
date: 2026-09-07
target_claim: INTERNAL_TARGET:K126_CONTINUUM_FOCK_GAUSS_LOCALITY_POINT_NESS_SUCCESSOR
claim_ceiling: exact repository-owned continuum CAR Fock representation for the fixed-width K126 defect in a smooth bounded external U(1)^8 background on the spatial line, with a common self-adjoint domain, even string-local causal-hull relative dynamics, compact-C2 free reservoir correlations in L1 and exact K115 on-shell Davies rates; no dynamical quantum gauge/Gauss Hilbert theory, singular point-Fock extension, finite-coupling scattering, interacting NESS/current, source/GU ownership, Born derivation, prediction, confirmation or holdout credit follows
manifest: lab/process/k127-k126-continuum-fock-string-local-correlation-wave.json
probe: tests/channel-swings/k127_k126_continuum_fock_string_local_correlation_probe.py
---

# K127 K126 continuum Fock/string-local/correlation wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`

```gu-typed-objects
result: fixed-width continuum background-gauge CAR Fock Hamiltonian with common domain, string-local causal-hull relative dynamics and integrable free-reservoir correlations realizing the K115 Davies rates
carrier: C9 impurity tensor irreducible Cl_18(C) module C512 tensor antisymmetric Fock space over eighteen full Dirac L2 species LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: Hilbert/Fock adjoint and one-particle L2 pairing with quasifree thermal two-point functions ON=repository_owned_continuum_background_gauge_control
real_structure: CAR adjoint, complex spinor conjugation, Hermitian Clifford-Klein generators and eight real smooth background connection components
grading: total fermion parity and the free Dirac finite-particle grading
action_owner: repository-construction
target: common fixed-width Fock domain, background-gauge covariance, causal string support and free correlation readiness, with dynamical Gauss, point-Fock and interacting-NESS boundaries MAP-TYPE=not-a-map
```

Scope: this packet quantizes the CAR reservoirs and impurity/Klein carrier in
one continuum representation while keeping the spatial-line `U(1)^8`
connection smooth, bounded and external. It proves a fixed-width relative-locality theorem and an integrable
free-correlation theorem. It does not quantize the connection, impose a local
Gauss law, construct a point defect or identify a finite-coupling field NESS.

## Inline preflight bookend

The rebuilt frontier contained seven substantial arcs. Two were immediately
constructive and independent: the continuum CAR Fock/common-domain packet and
the smooth thermal-correlation packet. A positive common-domain result releases
the causal-hull/string-local packet. The dynamical-Gauss packet is an
independent discriminator because background covariance and a quantum Gauss
law are different objects. The singular point-Fock and finite-coupling NESS
packets are conditional successors. Source/action ownership remains excluded
because no qualified source packet selects the imported data.

Mechanism retrieval distinguishes K127 from K120's symmetric-Fock HP white-
noise dilation, K122's CAR on-shell factorization and free observable net,
K123's undressed bounded interaction, K124's finite spatial lattice Gauss
representation, K125's linear point-mollifier obstruction and K126's formal
fixed-width action plus finite-regulator quantum theorem. K127's new content is
the simultaneous continuum CAR domain, Wilson/Klein even localization and an
explicit correlation-regular spectral family. No correction newer than those
inputs changes the relevant charges, parity, gauge or source typing.

The route-changing lens census covered CAR/Fock operator theory, continuum
Dirac propagation, background and dynamical Abelian gauge theory, Gauss
reduction, Wilson-line geometry, graded-local nets, causal perturbations,
self-adjoint extension/IBC and resolvent theory, thermal spectral analysis,
Davies limits, scattering/NESS, source fidelity and hostile philosophy of
science. Bounded CAR perturbation is the primary route because it gives an
exact shared domain. A compact `C^2` spectral family is preferred to a numeric
fit because two integrations by parts certify `L^1` decay. Polymer gauge
quantization, a singular boundary Hamiltonian and finite-coupling scattering
remain fallback routes, not consequences of the successful fixed-width model.

## 1. One genuine continuum CAR Fock carrier

For each of the eighteen undirected K115 edges on the spatial line let

```text
h_e = L2(M, C2),
h = direct-sum_e h_e,
F = Gamma_-(h),
K = C9 tensor C512.
```

Here `M=R` is the spatial line, `C2` is the
full left/right Dirac carrier and `C512` is K126's irreducible complex
`Cl_18` module. Let `D_A` be the self-adjoint free Dirac operator for one fixed
smooth bounded external `U(1)^8` connection `A`, and let

```text
H0(A) = 1_K tensor dGamma(D_A).
```

The smooth finite-particle vectors over a Dirac core form a core `C_fin` for
`H0(A)`. For `f_e in C_c^infinity(I)` and the selected path in the defect
interval `I`, K126 supplies

```text
f_e^A(x) = f_e(x) exp(i integral_[0,x] b_e dot A),
B_e = |v><u| tensor kappa_e,
V_A = sum_e g_e (B_e tensor a_e(f_e^A)
                 + B_e^* tensor a_e^*(f_e^A)).                  (1)
```

Wilson multiplication is unitary, hence `||f_e^A||_2=||f_e||_2`. CAR creation
and annihilation satisfy `||a_e^(#)(f)||=||f||_2`, while
`||B_e||=1`. Therefore

```text
||V_A|| <= 2 sum_e |g_e| ||f_e||_2.                            (2)
```

The adjoint-paired sum is bounded and self-adjoint. The bounded-perturbation
theorem now gives

```text
H(A)=H0(A)+V_A self-adjoint on Dom(H0(A)),
C_fin remains a core.                                           (3)
```

This closes K126's continuum **CAR Fock** representation and common-domain
condition at fixed nonzero width. Unlike an ordinary bosonic field coupling,
no particle-number estimate is needed: smeared fermionic creation and
annihilation operators are bounded on the complete antisymmetric Fock space.
The interaction changes reservoir occupation and therefore is not K126's
number-preserving one-particle boundary extension.

## 2. Background-gauge covariance is not a quantum Gauss law

For a smooth gauge parameter `alpha`, multiplication by
`exp(-i b_e dot alpha(x))` is unitary on `h_e` and its second quantization acts
on `F`. The Wilson endpoint identity gives

```text
f_e^(A+d alpha) -> exp(-i b_e dot alpha(0)) f_e^A,
|v><u| -> exp(+i b_e dot alpha(0)) |v><u|.
```

Thus the two phases cancel in every term of (1). K126's neutral odd
`kappa_e` also makes each `B_e a_e` even, so background covariance and total
parity are independently satisfied.

This representation deliberately stops there. `A` labels the family `H(A)`;
it is not an operator on an additional gauge Hilbert space. No conjugate
electric field, local Gauss generator, group-averaged physical kernel or
quantum gauge vacuum has been constructed. K124 owns those objects only on its
finite lattice. Calling (3) a continuum gauge/Gauss Hilbert theory would erase
the central missing owner.

## 3. Finite-width string support has an exact causal hull

Let `F(O)` be the graded CAR algebra generated by spinor test functions in a
double cone `O`, enlarged by the impurity/Klein algebra when `O` contains the
defect worldline. If all smearings and selected Wilson paths lie in the fixed
spatial interval `I`, then the complete `V_A` is an **even** element of the
time-zero algebra of `I` with the impurity. Graded locality therefore gives

```text
[V_A, C] = 0
```

for every field or observable `C` localized at disjoint support: even elements
commute with both parities across a graded tensor split.

Free Dirac propagation has unit speed. In the interaction picture,
`V_A(t)=alpha_t^0(V_A)` is supported in the causal translate of `I`. Every
Dyson monomial lies in the union of the iterated causal hulls, so the norm-
convergent bounded Dyson cocycle

```text
U_A(t) = 1 + sum_n (-i)^n integral_{0<t_n<...<t_1<t}
                   V_A(t_1)...V_A(t_n) dt_1...dt_n             (4)
```

commutes with any algebra spacelike separated from that hull. Relative
interacting evolution therefore equals free evolution on the spacelike
complement. This is an exact finite-width causal-hull theorem and an honestly
typed string-local relative net. It is not point locality: the path and
smearing interval remain part of the localization region, distinct circle
arcs may differ by holonomy, and no path-independence theorem is supplied.

## 4. Smooth spectra make the free correlations integrable

For each K122 undirected pair choose a positive Bohr frequency `Omega_e`, a
width `0<Lambda_e<Omega_e`, inverse temperature `beta_e>0`, and the K122 total
spectral rate `kappa_e` and occupation `n_e` at the Bohr frequency. Set

```text
x=(omega-Omega_e)/Lambda_e,
p(x)=(1-x^2)^3  for |x|<1, and p(x)=0 otherwise,
J_e(omega)=kappa_e p(x),
mu_e=Omega_e-beta_e^(-1) log((1-n_e)/n_e),
N_e(omega)=1/(1+exp(beta_e(omega-mu_e))).                       (5)
```

The polynomial, its first derivative and its second derivative vanish at
`x=+-1`. Its zero extension is therefore `C^2` with compact support. The
particle/hole spectral functions

```text
h_e^+(omega)=J_e(omega) N_e(omega),
h_e^-(omega)=J_e(omega) (1-N_e(omega))                          (6)
```

are also compactly supported `C^2`. Two integrations by parts give

```text
|C_e^+-(t)| <= ||(h_e^+-)''||_1 / t^2  for t != 0,              (7)
```

while the Fourier integral is bounded near zero. Hence every particle and
hole two-point correlation lies in `L^1(R,dt)`. This is an analytic
correlation-decay result, not a numerical tail fit.

At `omega=Omega_e`, `p(0)=1`, so `J_e=kappa_e` and `N_e=n_e`. The six biased
pairs use K122's `kappa=679/1243` and occupations `112/679` or `567/679`; the
nine symmetric base pairs use `kappa=4/5`, and the three symmetric wrong-record
pairs use `kappa=224/1243`, all at occupation `1/2`. Thus the on-shell particle
and hole weights exactly reproduce all thirty-six K115 directed rates. K127
does not reprove the analytic Davies theorem; it supplies its usual integrable
free-correlation hypothesis and exact coefficient data.

## 5. What the point and NESS routes still require

Equation (2) depends on `f_e in L2`. For a delta-normalized box,
`||f_epsilon||_2=epsilon^(-1/2)`, so fixed nonzero coupling leaves the bounded-
perturbation class. Scaling the coupling as `O(sqrt(epsilon))` keeps the norm
bounded but sends its distributional strength to zero. K127 therefore does not
turn its common fixed-width Fock domain into a point domain. A genuine
number-changing point successor still needs an IBC, counterterm, closed
quadratic form or norm/strong-resolvent construction on the full Fock carrier.

Likewise, (7) is a free-reservoir input for weak-coupling theory. It does not
construct a finite-coupling Moller/Ruelle morphism, return to a unique
stationary state, or identify the microscopic current. The reduced K115 ratio
`6561/256` remains exact reduced-model truth and is not promoted to a field
current.

## Inline postflight bookend

The continuum Fock packet and compact-`C^2` spectral packet completed
independently. The first released the causal-hull packet. The dynamical-Gauss
discriminator returned a sharp type boundary: background covariance has no
electric/Gauss operator or physical projection. The singular point packet
reached K125's exact norm obstruction but no IBC/form/resolvent successor. The
NESS packet gained `L^1` free correlations and exact on-shell coefficients but
did not cross finite coupling.

- **Strongest overclaim caught:** “continuum gauge representation” can mean
  either a covariant family over external connections or a Hilbert
  representation of dynamical gauge fields and constraints. K127 owns only the
  former and says so in its title, typed block and claim ceiling.
- **Strongest contrary construction:** an anomaly-free loop-group or reduced
  Coulomb-gauge representation with renormalized charge density could add the
  missing electric/Gauss sector; a Chebotarev--Gregoratti/IBC or resolvent
  boundary construction could replace the fixed-width interaction. Neither is
  excluded.
- **Weakest reproducibility seam:** correlation decay can be overclaimed from
  a smooth-looking cutoff whose boundary derivatives jump. The certificate
  checks `p`, `p'` and `p''` at both endpoints before using the two-integration-
  by-parts bound, and hostile mutations break each seam separately.

The result changes the physics map by closing the continuum CAR/common-domain,
finite-width causal-hull and free-correlation-readiness cells. It clarifies
rather than reduces the distance to a dynamical gauge/Gauss theory, point
interaction and finite-coupling NESS. No source action, physical state,
detector effect, Born rule, held-out score, prediction, confirmation, canon,
paper, release or public-posture status changes.

## Next condition

Construct the dynamical `U(1)^8` connection/electric representation, anomaly-
free local gauge implementers, Gauss generators, nonempty physical sector and
one self-adjoint domain carrying the K127 Wilson/Klein defect. Independently,
construct a singular number-changing Fock IBC, counterterm, quadratic form or
resolvent limit. Only after a finite-coupling scattering or return-to-NESS
theorem compare the microscopic current with K115's affinity. Separately
require an actual source/GU action selecting the gauge group, charges, paths,
Klein carrier, spectral functions, couplings, domain, boundary sector and
state.

## Reproduction

```bash
python3 tests/channel-swings/k127_k126_continuum_fock_string_local_correlation_probe.py
python3 tests/channel-swings/k127_k126_continuum_fock_string_local_correlation_probe.py --selftest
```
