---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-06
status: CURRENT_APPEND_ONLY_LEDGER__GRAPH_CURVATURE_SIX_VERSUS_FOUR_EXACT__OFF_GRAPH_DBT_OPEN
machine_ledger: lab/process/conditional-physics-ledger-v0.34.json
predecessor: explorations/conditional-build/conditional-physics-ledger-v0.33.md
---

# Conditional physics ledger v0.34

## Meter

```text
Ledger v0.34 — 82/82 active target rows mapped (100%)
33 SAME · 19 DIFFERS · 24 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

The denominator, verdicts, reason kinds, revival triggers, residue and quotient
count do not change. Five distances migrate.

## What moved

On the source-native constant-torsion graph, the selected `I1B` curvature
term is a nonzero `-1/26` multiple of the Fierz--Pauli symbol. It lifts all six
v0.33 nongauge directions for timelike and spacelike covectors, leaving
exactly the four gauge directions. On the null cone it leaves gauge four plus
the two transverse tensor characteristics. The total graph ranks are `30/30/28`.

The full independent-source carrier still needs the off-graph `d_B T` torsion
principal block, observation receiver and common domain. No characteristic
direction is booked as a parameter or new quotient. The `I2B` to observer-
full-`II` map remains the separate `LT-GR3` gate.

`LT-GR1`, `LT-GR2b`, `LT-GR5`, `LT-GR6`, and `LT-SM8` receive distance-only
migrations.

## Current highest-information gates

1. **Off-graph torsion/domain completion:** construct the `d_B T` principal
   block and cross terms on the full independent `(g,varpi)` carrier, compose
   the observation receiver and common domain, and preserve the exact
   nonnull gauge kernel plus two null tensor characteristics.
2. **Physical quotient and domain:** only after the action totalizes, build
   diffeomorphism/odd BV, the global Krein/Green domain and BFV.
3. **Second-layer/observer owner map:** separately construct or refute
   `I2B <-> ||II||^2` for `LT-GR3` before Q1.

P1/P2/P3 remain unused. Curt remains formally separate and no third lane is
promoted.
