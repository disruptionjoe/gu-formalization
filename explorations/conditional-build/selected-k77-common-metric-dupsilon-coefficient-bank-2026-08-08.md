---
artifact_type: conditional_build_result
created: 2026-08-08
status: COMMON_DG_BANK_EXACT__PRIOR_WARD_METRIC_OWNER_REJECTED__FULL_DEPSILON_DIFFEO_TRANSPORT_OPEN
source_return: SOURCE-CONFIRMS_TWO_CONNECTION_RAW_UPSILON_GRAMMAR__SOURCE-SILENT_COMMON_METRIC_BANK_AND_COMPLETE_DIFFEO_EPSILON_TRANSPORT
ledger: lab/process/conditional-physics-ledger-v0.97.json
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Selected K77 common metric `D-Upsilon` coefficient bank

## Result in plain English

The missing metric part of the local source equation is now written in the
same coordinates as the already-built connection part.

It is exact and nontrivial.  Across the four derivative directions, each
metric coefficient bank has rank nine.  Together they span the expected
rank-twenty torsion-free Levi-Civita image.  After removing the four
diffeomorphism directions, six physical transverse metric directions remain
live for timelike, spacelike and null symbols.

This exposed a useful correction.  An earlier wave made the Ward identity
close by *defining* four metric-orbit values to cancel the connection and
epsilon responses.  Those values were a diagnostic target, not the physical
Levi-Civita derivative.  Direct comparison now shows:

```text
actual physical metric orbit:       rank 3
identity-defined metric completion: rank 4
difference:                         rank 4
```

Consequently the current `g + varpi + gamma-epsilon` principal assembly has a
rank-four Ward defect in all three causal classes.  This does not invalidate
the metric bank or the source action.  It says that the principal
gamma-epsilon orbit is not yet the complete physical epsilon/diffeomorphism
transport.  That missing construction is now the next gate.

## Layer 0

| Phrase | Object used here | Object kept distinct |
| --- | --- | --- |
| fixed-`varpi` metric derivative | `delta T=-D_gB_LC`, `delta A=delta F_A=0` | applying the full `D_varpi Upsilon` response to `-D_gB_LC` |
| physical metric bank | covariant spin--Levi-Civita first jet on the common raw-residual carrier | four orbit values solved from `JR=0` |
| gamma-epsilon response | exact principal internal gauge-orbit derivative | full primitive lower-order/nonlinear `D_epsilon Upsilon` |
| Ward defect | raw-residual principal defect of the current partial assembly | action Euler/Noether failure or presymplectic non-basicness |
| equation dual | field-covector-valued `J_K^!` | field-valued adjoint requiring a Riesz map |

The source coordinates are

```text
T = varpi - B_LC(g,epsilon),
A = B_LC + T = varpi.
```

Therefore at fixed independent `varpi`,

```text
delta_g T = -delta_g B_LC,
delta_g A = 0,
delta_g F_A = 0.
```

The fixed-`varpi` metric derivative is not obtained by feeding
`-delta B_LC` into the independent-connection response.  Doing that would
move `A` and manufacture a curvature term; the planted control rejects it.

## Exact common-coordinate bank

In symmetric coframe gauge,

```text
delta omega_(mu ab)
  = 1/2 (nabla_b h_(mu a) - nabla_a h_(mu b)).
```

Emitting the augmented-torsion term on the all-grade residual carrier gives
four principal maps

```text
G^mu : Sym^2(T*X) -> Omega^1(X,Lambda^2),
G^mu h = -(D_g B_LC)^mu[h].
```

Exact ranks are

```text
rank(G^0,G^1,G^2,G^3) separately = 9,9,9,9,
rank of their complete covariant first-jet family = 20,
output support per derivative direction = 12.
```

In covariant `nabla h` coordinates there is no separate zero-order metric
owner: background connection coefficients are already part of the covariant
derivative.  This statement is presentation-relative and is not exported to
arbitrary fixed-coordinate formulas.

For each canonical causal covector, the metric symbol has rank nine and its
restriction to the transverse metric complement has rank six.  On the
diffeomorphism graph, the physical metric torsion response cancels the
independent-`varpi` torsion displacement exactly:

```text
D_g Upsilon[D_q xi] + delta-varpi_torsion[C_q xi] = 0.
```

This is the common-coordinate closure that v0.96 lacked.

## Why the complete Ward identity remains open

The independent-`varpi` response also contains its curvature derivative, and
the conditional gamma-epsilon response contains a separate grade-one orbit.
The previous v0.84 construction chose metric-orbit values as minus their sum;
that necessarily made `JR=0`, but did not show those values came from the
physical Levi-Civita derivative.

The direct comparison gives the following exact packets:

| causal class | source orbit rank | physical metric orbit | Ward-defined metric orbit | physical Ward defect |
| --- | ---: | ---: | ---: | ---: |
| timelike | 4 | 3 | 4 | 4 |
| spacelike | 4 | 3 | 4 | 4 |
| null | 4 | 3 | 4 | 4 |

The defect supports are respectively
`[14,2,2,2]`, `[15,15,4,4]`, and `[17,7,7,17]`.  These are coefficient-support
diagnostics, not particle or generation counts.

The old orbit completion survives as a precise target: a complete primitive
epsilon/diffeomorphism construction must supply exactly the missing rank-four
response.  Its promotion as the physical metric owner is rejected.

## Specialist assessment

- **Differential geometry:** the common metric bank is the correct
  torsion-free Levi-Civita image; the remaining defect belongs to the
  gauge/diffeomorphism transport, not to an arbitrary replacement of `D_g`.
- **Symplectic geometry:** raw `JR=0` is prerequisite structure only.  No
  presymplectic current, basic quotient or BFV class is inferred.
- **Variational PDE:** the local metric block is now a genuine coefficient
  bank.  The common operator may not be formally adjointed until the primitive
  epsilon block is complete.
- **Real Clifford/Krein:** the calculation stays on the real K77 carrier and
  uses no positive fundamental symmetry or closed domain.
- **Complex/path-integral:** no contour, measure, determinant, saddle, Wick
  rotation or reflection-positivity statement is made.
- **Source criticism:** Weinstein confirms the two-connection/raw-`Upsilon`
  grammar.  The common metric realization and physical gamma-epsilon
  soldering remain source-silent repository constructions.
- **Constraint accounting:** zero new fields, coefficients, quotients or
  external datum pieces.  P1/P2/P3 remain unused.

## Hostile verdict and boundary

The hostile verdict is
`METRIC_BANK_SURVIVES__PRIOR_WARD_ORBIT_PROMOTION_FALSIFIED`.

It rejects a wrong-object substitution without escalating the local defect to
a theory-wide no-go.  The selected Spin-native parent, the product of the two `U(32,32)` Weyl halves, and the full `U(64,64)` comparator remain distinct.  No
Einstein equation, cosmology, Standard Model sector, mass, chirality,
generation count, global solution, positive domain, quantum theory, canon or
public-posture claim moves.

## Frontier delta

Conditions closed: `3`; opened: `1`.

Closed:

1. common residual-coordinate `D_g Upsilon` coefficient bank;
2. exact rank-twenty/rank-nine/rank-six metric geometry on that carrier;
3. exact `g-varpi` augmented-torsion graph cancellation.

Opened/refined:

1. the prior Ward-defined metric orbit is not physical `D_g Upsilon`; the
   full primitive epsilon/diffeomorphism transport must supply a rank-four
   correction.

Remaining named conditions: `3`.

1. Construct complete primitive `D_epsilon Upsilon` and the physical
   diffeomorphism transport, then retest common `JR=0`.
2. Extend the `K_loc` equation dual and Green identity only after that closure.
3. Derive a field-space Riesz map or retain covector-valued adjoint language.

## Evidence

- `tests/channel-swings/selected_k77_common_metric_dupsilon_coefficient_bank_probe.py`
  — `54/54 PASS`.
- `tests/channel-swings/selected_k77_common_metric_dupsilon_coefficient_bank_independent.sage`
  — `19/19 PASS`.
- `lab/process/selected-k77-common-metric-dupsilon-coefficient-bank.json`.
- `lab/process/hostile-reviews/2026-08-08-selected-k77-common-metric-dupsilon-coefficient-bank-review.md`.

## Next gate

`CONSTRUCT_COMPLETE_PRIMITIVE_DEPSILON_AND_PHYSICAL_DIFFEO_TRANSPORT__RETEST_COMMON_JR_ZERO__THEN_EXTEND_KLOC_FORMAL_ADJOINT_GREEN`.
