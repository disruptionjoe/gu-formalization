---
title: "Conditional benchmark delta integration"
status: active_research
doc_type: conditional_evidence_integration
created: 2026-08-23
registry: lab/process/conditional-benchmark-delta-integration.json
ledger: lab/process/conditional-physics-ledger-v0.261.json
probe: tests/channel-swings/conditional_benchmark_delta_integration_probe.py
grade: "EXACT APPEND-ONLY LEDGER INTEGRATION; EXTERNAL TARGETS ONLY; NATIVE REALIZATIONS UNCHANGED"
target_claim: NONE-NOT-A-KILL
target_claim_note: "This integration registers accepted targets and owner joins. It kills no source claim or route."
canon_verdict_change: none
---

# Conditional benchmark delta integration

> **GU-COMPARATOR-ROUTING — scope before inference.** The integrated anchors
> are observed-side external targets. No conventional comparator selects or
> adjudicates a source-native mechanism without an explicit typed bridge.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: EXTERNAL_BENCHMARK_TARGETS`

```gu-typed-objects
result: two cursor-ordered conditional evidence deltas incorporated into ledger v0.261
carrier: append-only conditional-physics ledger and benchmark registries LAYER=UNTYPED CHIRALITY=N/A
pairing: scoped external targets paired only with their named realization rows ON=ledger-rows
real_structure: N/A
grading: ledger integration; no new physics computation
action_owner: repository-construction -- no source action or parameter is introduced
target: delta-to-ledger consistency MAP-TYPE=evaluation
```

## Disposition

`GU-EXT-GR-BENCH-WIDENING-2026-08-23` is incorporated by appending exactly
`LT-GR9`, `LT-GR10`, and `LT-GR11`. Each row is `NEEDS /
MISSING_CONSTRUCTION`, has no mechanism commitment or confirmation credit,
and preserves the cited W225, W220, W229, and H49 ceilings.

`GU-EXT-SM-COSMO-BENCH-LINKING-2026-08-23` is incorporated by registering
`EXT-SM-STRUCTURE` and `EXT-COSMO-BACKGROUND` against existing rows. It mints
no duplicate target. Context-only accretions on `LT-SM8` and superseded
aggregate `LT-GR2` record OWNER-F and OWNER-C demand joins; neither changes a
verdict.

The predecessor advanced through direct packets `093c5ff6` and `2ce482d3`
before closeout. Their complete kill-target typing ratchet is compatible and
preserved. The associated proposed verdict re-typings are later in the index
and remain pending for separate canonical Progress disposition. The later
`0e3650de` velocity-council packet is disjoint.

## Result and ceiling

The ledger moves from 85 to 88 canonical targets and from 28 to 31 `NEEDS`.
Six external benchmarks are now registered. All 85 predecessor verdicts and
scientific meanings remain fixed; the only predecessor-row additions are the
two requested context projections.

This is a ledger-target integration, not construction progress. It supplies
no source action, background, observed reduction, physical quotient,
normalization, prediction, confirmation, canon change, or public-posture
change. Reverse-only recovery remains compatibility.

## Validation residual

The v0.261 integration probe, both source-registry probes, delta gate, JSON
Schema validation, and CT-2 self-test pass. The live CT-2 repository scan is
still RED on eight rows changed in immutable v0.260 without context; v0.261's
three touched rows are typed `3/3`. The older v0.260 reverse-scaffold probe
also exposes the same historical mismatch because v0.260 changed target-claim
typing on predecessor rows while its summary claimed no predecessor-row
changes. This run does not rewrite the immutable predecessor, raise CT-2's
zero baseline, or hide either defect. They remain explicit append-only repair
work.
