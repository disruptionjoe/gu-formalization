---
artifact_type: exploration
status: exploration
doc_type: conditional-build-ledger
created: 2026-08-05
cluster: "B — LAGRANGIAN TERMS (SM sector + Einstein sector, term by term)"
title: "Cluster B conditional build: the Lagrangian term by term, with the source action carried SYMBOLICALLY as U1..U13. Sixteen term-slots, eighteen graded rows. THREE OVER-DETERMINED rows are reported loudly (GR-1b Einstein-Hilbert-via-Shiab; GR-4 the R^2 scalaron; SM-3b constraint-preservation versus the written shiab), three rows are DETERMINED without guessing (GR-2's cosmological-term SIGN exact at +1/2 g_mn; SM-5's Yukawa CHANNEL unique at dim Hom = 1; SM-9's perturbative anomaly conditions), and the single largest structural finding is that every dimensionful row in the cluster dies to ONE theorem already in the repo (DC-H2's congruence-orbit argument), so no symmetry condition of any kind can ever supply Newton's constant, Lambda's magnitude, or any mass scale."
grade: "LEDGER PASS at filed grade over existing results. No new computation is performed and no coefficient is derived here. Every row's content is a citation of an in-repo artifact plus a typed verdict; three items are explicitly labelled REFEREE_CONJECTURE with the certifying check named. Nothing here moves a claim, canon entry, verdict, bar, count, priority, LANE-STATE row, external-datum entry, or public posture."
fork_assumed:
  - SECTION-VS-OBSERVERSE
  - VERTICAL-FROBENIUS-TRACE
  - SIGNATURE-AMBIENT
  - KINEMATIC-VS-PHYSICAL-CARRIER
fork_stack_acknowledged: true
search_space_dim: 16
search_space_dim_reason: "PRE-2 is answered in the affirmative and WHOLESALE, not by enumeration. The space this lane searches is the set of Lagrangian TERM-SLOTS of SM+GR, which is a fixed finite census, so it is enumerated exhaustively (16 slots, 18 graded rows after two slots split) rather than sampled. The sharp sub-form is answered too, and it is the cluster's most valuable output: DC-H2 proved the target of every dimensionful row IS a coordinate on the orbits of the blockwise-congruence group under which every symmetry-type selector is invariant. Therefore the ENTIRE CLASS of symmetry/self-adjointness/reciprocity selectors is excluded, in one theorem, from ever supplying Newton's constant, the cosmological-term magnitude, the RS mass, or any Yukawa eigenvalue. Six of the sixteen slots are decided wholesale by that single exclusion and no candidate-by-candidate search of them is warranted."
free_object_delta: 0
free_object_delta_justification: "U1..U13 are LABELS for coefficients and forks that already appear, charged and owned, in the packet's own Section 7.2 parameter ledger and in the Layer-0 fork registry. No new un-owned object is introduced. PRE-3 answered: this lane introduces zero new objects by construction, because its output is a re-typing of material already in custody."
residue_touched:
  - "GR-R2-RUNNING-SIGN: T4"
  - "GR-LAMBDA-MAGNITUDE: T3"
  - "GR-NEWTON-CONSTANT: T3"
  - "A4-ZU-BLOCK-RATIO: T3"
  - "SM-BRST-POSITIVITY-MH17: T3"
  - "SM-CHIRALITY-FROM-NONCHIRAL-PARENT: T2"
  - "SM-MAJORANA-SPURION: T2"
  - "SM-YUKAWA-TEXTURE: T1"
  - "SM-HIGGS-POTENTIAL: T1"
  - "SM-ANOMALY-I16-LOCAL-LEG: T1"
  - "SM-THETA-QCD: T0"
route_disposition: "CONDITIONAL_LEDGER_BUILT_SIXTEEN_ROWS__TWO_OVER_DETERMINED_ROWS_REPORTED__TWELVE_UNKNOWNS_NAMED__ALL_DIMENSIONFUL_ROWS_EXCLUDED_WHOLESALE_BY_DC_H2"
canon_verdict_change: none
priority_change: none
verdict_gate: "Pre-deposit, J5-gated. No bar, verdict, canon claim, count, H59, register row, or LANE-STATE entry moves here. The three OVER-DETERMINED rows are REPORTS of results already filed by K77-B2, W79 and DERIVATION-PROGRESS with their existing scopes intact; this artifact widens none of them and files no falsification."
depends_on:
  - lab/process/CURRENT-RESEARCH-CONTEXT.md
  - lab/process/layer0-fork-registry.yaml
  - lab/process/science-council-program-efficiency-2026-08-04.md
  - explorations/unified-source-datum-packet-v0-2026-07-30.md
  - explorations/source-action-term-by-term-against-the-spec-2026-07-29.md
  - explorations/wave24/H45-H2-vs-II2-binary-2026-07-11.md
  - explorations/scalaron-normsign-and-vacuum-2026-07-11.md
  - explorations/resolver-wave-k77b2-shiab-family-curvature-selector-transgression-2026-08-04.md
  - explorations/k77-wave2-principal-bianchi-product-selector-2026-08-05.md
  - explorations/k77-wave2-full-adjoint-shiab-bianchi-two-connection-target-2026-08-05.md
  - explorations/dc-h2-reciprocity-and-the-zu-block-ratio-2026-08-04.md
  - explorations/de-certification-redo-2026-08-03.md
  - canon/schwarzschild-weak-field-rfail.md
  - canon/shiab-existence-cl95.md
  - canon/theta-field-flrw-dark-energy-eos.md
---

# Cluster B: the Lagrangian, term by term, with the source action carried symbolically

## 0. The reframe this lane runs on

Every prior wave built FORWARD — geometry, then construction, then a collision
with the missing source action, then "blocked." This lane inverts the arrow.
The source action is treated as an **unknown**, its choices are named `U1..U13`,
the downstream physics is built **conditionally**, matched against the Standard
Model and General Relativity as physics actually writes them, and the
constraints are propagated **backwards** onto the unknown.

The consequence for reporting is strict and is worth stating before any row: a
row never reads "blocked." It reads **determined given `U_k`**, or
**under-determined with the freedom quantified**, or **over-determined**. The
third is the most valuable outcome available and it is never softened here. A
program that cannot fail is not doing science; this cluster returns **two rows
where GU is currently over-determined**, at exactly the scope its own artifacts
established.

## 1. PRE-WAVE (binding; answered before any construction)

### PRE-1 — which fork does this lane assume without deciding?

Four open horns, declared, with the cost of the other branch priced.

| fork id | horn assumed | cost if the other horn is right |
|---|---|---|
| `SECTION-VS-OBSERVERSE` | that the SM-sector rows live on the **defect** `s(X^4)` and the gravity/gauge-kinetic rows live on the **bulk** `Y^14`, as the packet's own bulk/defect split writes them (`unified-source-datum-packet-v0-2026-07-30.md:101-102, 340-352`) | if the physics runs on the full observerse rather than a section, the SM rows are not defect rows and the obstruction each of them reports may be **invisible** rather than absent. This is the fork the time-orientation run flagged as producing "a missing answer, not a wrong one." The K77 stack is several waves deep on it; this lane makes that five |
| `VERTICAL-FROBENIUS-TRACE` | the **ambient** trace reversal, because both the K77-B2 kill and the 2026-08-05 principal-Bianchi selector are stated against the ambient fourteen-dimensional Einstein contraction | K77-B2 explicitly does NOT decide this fork. Under the observed-4D or vertical-Frobenius reversal the over-determination in row **GR-1b** may dissolve entirely. This is the single largest fence on this artifact's loudest finding and it is restated at the row |
| `SIGNATURE-AMBIENT` | that `Cl(9,5)`-computed results (`SHIAB-01/02/03/04/05/06`, the anomaly audit) may be **cited as typed inputs** while `REAL-CLIFFORD-FORM` is settled to `Cl(7,7) = M128(R)` | Wave K's import ban is explicit: "do not import the `(9,5)` right-H / chosen-J machinery into `Cl(7,7)`." Section 6 of this artifact is the price: **the anomaly row and part of the shiab family chain are fork-stranded**, and this lane types that rather than silently transporting it |
| `KINEMATIC-VS-PHYSICAL-CARRIER` | nothing. Every row that would need a physical reading of a kinematic block result is marked `REQUIRES-UNKNOWN` at that step rather than assumed through | `Pi_RS^phys` does not exist (`OQ-RK1 BLOCKED_NEEDS_SPEC`). Rows **SM-3** and **SM-4** name this explicitly instead of leaning on it |

`REAL-CLIFFORD-FORM` is **settled** (`Cl(7,7) = M128(R)`, 2026-08-04) and this
lane stands on the settled side; it is not counted as an assumed open horn.

**Fork-stack acknowledgment.** `SECTION-VS-OBSERVERSE` is now declared by more
than three consecutive dispositions. The threshold is not a refusal threshold —
proceeding is allowed — but the acknowledgment is mandatory and this is it: the
cost of that fork is now carried by the K77 construction stack **and** by this
entire ledger, and the number of artifacts that would need re-typing if it flips
is growing monotonically.

### PRE-2 — what is the dimension of the space, and can it be excluded wholesale?

**Yes, and this is the cluster's most valuable single output.**

The space is not a candidate space. It is the **fixed finite census of
Lagrangian term-slots** that SM+GR occupies, and it is enumerated exhaustively:
**16 term-slots** — 9 on the SM side and 7 on the Einstein side — which split
into **18 graded rows** because two slots carry two distinct routes each. There is
no enumeration-versus-theorem tradeoff at the level of the ledger itself.

But the sharp sub-form — *is the target quantity a coordinate on the orbits of a
group under which all candidate selectors are invariant?* — **fires, and it
fires on six rows at once.**

DC-H2 proved that the `Z_U` block ratio **is** a coordinate on the orbits of the
blockwise-congruence group, and that every symmetry-of-the-pairing and every
self-adjointness demand is invariant under exactly that group
(`dc-h2-reciprocity-and-the-zu-block-ratio-2026-08-04.md:229-236`). Its own
words: "no condition of this *type* — any condition invariant under blockwise
congruence, which includes every symmetry-of-the-pairing and every
self-adjointness demand — can ever supply `(c_b : c_f)`. What A4 needs is a
**scale**, and scale-blindness is exactly the defining property of the
conditions H2 proposed."

**Generalized to this cluster, that theorem excludes a class from every
dimensionful row simultaneously.** The rows whose target is a *scale* rather
than a *ratio* are:

- `GR-2` cosmological-term magnitude
- `GR-7` Newton's constant / the gravitational scale
- `SM-2` the gauge-coupling normalization
- `SM-4` the RS mass `m_R`
- `SM-5` the Yukawa eigenvalues (as opposed to their ratios and angles)
- `SM-6` the Higgs VEV

For **all six**, the following is now a stated consequence and not a
row-by-row discovery: *no equivariance condition, no reciprocity condition, no
self-adjointness condition, no Ward identity, and no reality condition can ever
supply the answer*, because each such condition is invariant under a group whose
orbits the answer parameterizes. The repo's own independent confirmations of the
same shape are `PRED-NORM-RANK` (rank-3 log-monomial rescaling certificate,
`RESOLVED_NO_GO`, contract-locked) and the `H49` Lambda-magnitude no-go
(`tests/wave28/H49_bach_weyl_sector.py:456-476`: "a dimensionful `Lambda =
(dimensionless number) * mu_DW^2` requires an IMPORTED scale").

This is the difference between a lane that reports six blockers and a lane that
reports **one theorem with six corollaries**. It also means that any future wave
proposing a symmetry-type selector for any of those six rows is **wasted before
it starts**, and this ledger says so in advance.

**Three further class-exclusions the cluster inherits, all already banked, all
of the same wholesale shape, and none of them previously collected in one
place.** Each kills a family of candidate moves rather than a candidate:

| exclusion | what it kills | source |
|---|---|---|
| **the norm-square is coefficient-blind** | every argument of the form "the second-layer norm square selects the coefficient." A norm square is automatically defined for *every* coefficient, so it supplies **no** independent constraint | `lab/sources/gu-two-layer-action-source-reinspection-2026-08-04.md:51-55`; confirmed by `k77-wave2-common-two-layer-action-euler-coefficient-selection-2026-08-04.md:2` ("its redundancy cannot select the trace-q coefficient") |
| **CHANNEL-FORBIDDEN** | every **non-form** Higgs carrier. `dim Hom = 0` **exactly** for all of them, so `SA-Y1` FORCES the Higgs carrier to be a form carrier `Lambda^k(V_14)` | `yukawa-scoping-2026-07-13.md:59-75`; `source-action-requirements-spec-2026-07-13.md:68` |
| **FN-sterility** | mod-3 Froggatt-Nielsen charges as a hierarchy mechanism — provably **sterile for all 27 assignments** | `source-action-requirements-spec-2026-07-13.md:72` (`SA-Y5`) |

Together with DC-H2, the cluster's honest posture is that **four independent
wholesale exclusions are already in hand**, and that the program's remaining
selector work should be measured against them rather than repeating members of
the excluded classes. The 2026-08-04 council's §2.1 finding — three artifacts in
one day returning three non-selections of one quantity where one theorem would
have decided the class — is the failure mode these four exclusions exist to
prevent.

### PRE-3 — does success introduce a new un-owned object?

**No. `free_object_delta = 0`, and the reason is structural rather than
disciplinary.**

`U1..U13` are not new objects. Every one is a label for a coefficient or fork
that already appears — charged and owned — in the packet's own Section 7.2
parameter ledger (`unified-source-datum-packet-v0-2026-07-30.md:1104-1122`) or
in `lab/process/layer0-fork-registry.yaml`. The mapping is given explicitly in
Section 3 so the identification can be audited rather than asserted. A ledger
that renamed existing debts as new unknowns would be the coverage-theatre
failure mode the 2026-08-04 council priced; the U-list is checkable against the
packet line by line precisely so that it cannot be.

## 2. Layer-0 typing (before use; the standing rule)

Five load-bearing homonyms are live in this cluster. Getting any of them wrong
silently produces a fake row.

| phrase | typing | ruling for this ledger |
|---|---|---|
| **"theta"** | **HOMONYM, four senses — and the (a)-vs-(b) pair is typed HERE FOR THE FIRST TIME** | (a) GU's connection distortion `theta = pi - eps^{-1} B eps in Omega^1(Y, ad P)` (`dark-energy-theta-divergence-free.md:21, 36`), PROVEN equal to the second fundamental form `II_s` under pullback (H21); this is the **dark-energy** field. (b) The **QCD vacuum angle** `theta_QCD` multiplying `tr F ^ F`, a dimensionless periodic parameter. (c) The packet's `T_9`, a **Z/2** holonomy phase of the orientation line `L_sigma`. (d) The **Cartan involution** `theta` of `so(9,5)` (`VG-V2-fourth-seat-gauge-sector.md:5`). Four different objects. Rows GR-2/GR-6 use (a); row SM-7 is about (b) and (c); (d) appears in SM-8. **No result transfers between them.** The repo has no file that types (a) against (b); the closest precedent is the canon's explicit quarantine of the *torsion* pun (`two-geometries-and-the-adapter-2026-06-29.md:29, 41`), and this row applies the same treatment. |
| **"torsion"** | **HOMONYM, two senses** | (a) *Geometric* torsion of a connection — GU's **augmented torsion** `T`, an actual field appearing in the action's Euler derivative. (b) *Algebraic/homotopy* torsion — the `Z/3` inside `pi_3^s = Z/24` of the generation-count arena (`torsion-generation-arena-2026-07-20.md`). Row GR-5 is about (a) ONLY. The generation-count arena is not a torsion term in any Lagrangian and does not appear in this cluster. |
| **"Einstein contraction"** | **HOMONYM, three senses** | ambient-14D vs observed-4D vs vertical-Frobenius trace reversal. Fork `VERTICAL-FROBENIUS-TRACE` is **open**. Rows GR-1b and GR-6 are stated against the **ambient** reading and are void under the other two until re-run. |
| **`Z_U`** | **HOMONYM (flagged upstream, carried)** | ledger sense = the whole gradient term `\|D_A U\|^2` (**NOT BUILT**); W229 sense = the scalar coefficient multiplying it (`normalization_unbuilt`). `U4` below is the ledger sense. |
| **"the Higgs"** | **HOMONYM-WITH-NAMED-BRIDGE (adjudicated upstream, carried)** | `layer0-pass-on-the-two-higgs-objects-2026-07-29.md:18, 60-68`: `SA-Y1`'s Higgs is the `Lambda^0` carrier (`dim Hom = 1`, **OPPOSITE** chirality); Weinstein's Higgs is the IG connection perturbation `a in Omega^1(ad)` = `Lambda^1` (`dim Hom = 1`, **SAME** chirality, and not a Lorentz scalar). **"Weinstein's mechanism does not satisfy `SA-Y1` as stated."** The named, unadjudicated bridge (`:70-82`) is whether a vertical vev of `a` converts the 14D same-chirality `Lambda^1` channel into a 4D opposite-chirality Dirac mass channel — recorded **UNCERTAIN**. Row SM-6 carries both objects and does not merge them. |
| **"square"** | **HOMONYM (load-bearing, and it kills a class)** | `gu-two-layer-action-source-reinspection-2026-08-04.md:51-55`: a **norm square is automatically defined for every coefficient** and therefore supplies **no independent coefficient constraint**. Any argument of the form "the second-layer norm square selects the coefficient" is dead on arrival. See PRE-2. |

**Construction fork.** Rows are marked `[N]` where the object is program-native,
`[S]` where a standard-field object is imported for comparison, and `[N|S]`
where the row is precisely a comparison between the two.

## 3. The unknowns: `U1..U13`

Each is checked against the packet's own charged-coefficient ledger. "Rows" is
the dependency list that makes each downstream row read *determined given `U_k`*
rather than *blocked*.

| id | the unknown | where it is already charged | rows that depend on it |
|---|---|---|---|
| **U1** | **the action-NORM premise (`P2`)**: does the action norm the full `\|theta\|^2 = \|II\|^2` or the trace-square `\|H\|^2`? | `H45` Q1; the residual freedom is explicitly named as `P2`, one facet of the unbuilt source action | GR-1, GR-2, GR-3, GR-4, GR-6, GR-7 |
| **U2** | **`zeta_F in {0,1}`**, the fundamental-versus-induced Yang-Mills fork | `packet:498` | SM-1, SM-2 |
| **U3** | **the Shiab selector** on the source-natural full-domain extension | `SHIAB-02/-04/-06`; `k77b2:305-315` | GR-1b, SM-9 |
| **U4** | **`Z_U`'s horizontal:vertical block ratio**, reduced by DC-H2 to exactly **one length-squared** | `dc-h2:241,250`; `de-certification-redo:A4` | GR-2, GR-7, SM-1 |
| **U5** | **the overall imported absolute scale** (`mu_DW`, and the rank-3 normalization group `{kappa, Z_U, source_norm}`) | `packet:1106`; `PRED-NORM-RANK` | GR-2, GR-7, SM-2, SM-4, SM-5, SM-6 |
| **U6** | **`Lambda_bare`**, the bare cosmological coefficient | `packet:575-586` | GR-2 |
| **U7** | **`alpha_II`** and the ratio **`beta_0/alpha_II`** (full vs traceless `II`) | `packet:585`, `SA-G5/G6` | GR-1, GR-3, GR-4, GR-7 |
| **U8** | **the odd-polarization coefficients** `h = (h_a, h_d, h_SI, h_SR, h_IR, h_mu)` of the full-20 Dirac-type operator, with `\|h_mu\| = m_R = sqrt(m_2,eff) mu_DW` | `packet:388-420, 1108` | SM-3, SM-4 |
| **U9** | **the Yukawa data** `Y_K, Y_C in M_3(C)` on the three **provenance** summands, plus the charge-conjugation branch `(eps_C, tau_C) in {+-1}^2` | `packet:620-665, 1112` | SM-5 |
| **U10** | **the spurion sector**: twelve real `sigma_A` on a supplied `F_2` flavour test space; **absent** on the minimal no-same-chirality branch | `packet:668-695, 1112` | SM-5 (Majorana/seesaw sub-row) |
| **U11** | **the BV/gauge-fixing package**: gauge-fixing fermion, ghost-mass convention (`SA-U2`), guardian fork (`SA-U5`), and whether the BV master equation closes at all (`T4`, "written, closure unverified") | `packet:696-780, 1110`; `source-action-term-by-term:89, 137, 139` | SM-8, SM-9 |
| **U12** | **`lambda_SW`** (the SW-shaped defect coupling) and **`lambda_F`** (the gamma-traceless curvature vertex, whose internal ratio `-1/6` is **fixed and inherited** while the overall coupling stays charged) | `packet:452-462, 600` | SM-1, SM-3, SM-3b, SM-5 |
| **U13** | **the sign of the background curvature `F_{A_0}`** — the condition "*if your curvature is negative*" that turns the `\|\|F_A\|\|^2` expansion into a Mexican hat. It is a **condition on the background, not a free choice**, and `layer0-pass-on-the-two-higgs-objects-2026-07-29.md:96-100` records it as *"a new requirement-row-shaped obligation with no current row"* — FORCED/DECLARATION/FIT unknown | `packet:1114` charges `A_0` as a section-valued slot | SM-6 |

Thirteen unknowns. **Honest reading of the U-list against the packet.** The packet's Section 7.2 is
substantially **larger** than `U1..U13`: it also charges the multiplicity pairing
(4 real), the gauge generators `r_S, r_I, r_R in C` (6 real), section-valued
background slots, an **open-BV row whose syntactic rank is bounded only by
233,100 with the actual rank uncomputed**, and a topology integer. The packet
states plainly: **"No constraint surplus is reported at N1"**
(`packet:1124`). `U1..U13` is the sub-list this cluster's eighteen rows actually
consume; it is not a claim that the residue is twelve. The Standard Model plus
GR is 19 measured parameters (26 with neutrino masses and mixings) plus Newton's
constant and Lambda. **A ledger claiming a residue smaller than that would be
the number to check first, and this cluster does not produce one.**

---

## 4. The Einstein sector, term by term

Joe's stated target for this half: *"mostly the same except for one little thing
here or there, which are explained by the geometry and would explain other
things."* Where a difference exists, the row states what it would predict.

### GR-1 — the Einstein-Hilbert term `[N|S]`

**Physics writes:** `S_EH = (1/16 pi G) INT R sqrt(-g) d^4x`.

**GU produces, given `U1`:** the traced-twice Gauss identity is **exact and
computed**, `\|II\|^2 = \|H\|^2 - R^X`, verified in dims 2/3/4 from the shape
operator with `R^X = 2 e_2(k)`
(`H45-H2-vs-II2-binary-2026-07-11.md:72-73, 207`). So on the `U1 = \|II\|^2`
horn, the induced Einstein-Hilbert scalar `R^X` appears in the defect action
`S_geom = INT_X [alpha_II <II_s,II_s> + beta_0 <II_s^0,II_s^0> - 2 Lambda_bare]`
(`packet:575-583`) **with its coefficient tied to `alpha_II` and to nothing
else**. `H21` PROVED premise `P1` (`s*(theta) = II_s`, off-shell, full tensor,
convention route to `\|H\|^2` closed); `P2` — the norm choice — is `U1` and is
open.

`R^X` is genuinely **dynamical in 4D** (linearized Einstein on a TT graviton,
`G^(1)_yy = -1/2 box h_yy != 0`) and merely topological in 2D — which is exactly
why 4D forces a real Einstein-Hilbert term where 2D Willmore is scale-free
(`H45:78-81`).

**Verdict: DETERMINED GIVEN `U1`, and exactly-same in form with one structural
difference.** The difference is not a new term: it is that `1/16 pi G` and the
Weyl-squared coefficient are **not independent**. Both are `alpha_II`. Physics
carries them as two unrelated numbers; GU carries one.

**What that predicts.** A Stelle mass fixed relative to the Planck scale rather
than free, hence a specific massive spin-2 partner at `m^2 = +1/2` in `mu_DW`
units (`H45:153-155`, `H15-E`) — and, at natural `mu_DW ~ meV`, a **sub-mm
Yukawa deviation window**, which `H49` calls a knife-edge but explicitly a
*live* window (`tests/wave28/H49_bach_weyl_sector.py:440-452`). That is a
falsifiable consequence of the tie, not of the term.

**Under the other horn of `U1`** (`\|H\|^2`, pure Willmore) there is **no
Einstein-Hilbert term at all** — pure conformal Bach, no Einstein graviton
(`H45:110-118`). The fork is real, not cosmetic: on `Sym^2(R^4)` the full norm is
a **rank-10** quadratic form and the trace-square is **rank-1**, a
nine-dimensional difference (`H45:74-76`).

### GR-1b — the Einstein-Hilbert term produced *through the Shiab* — **OVER-DETERMINED**

This is a separate row from GR-1 because it is a different route to the same
term: not the Gauss identity on the defect, but the source's own Shiab operator
acting on ambient curvature.

**The three conditions, each independently motivated by the source:**

1. **Principal Bianchi.** Among the eight fixed product assignments in the
   displayed degree-two Shiab formula, exactly **one** is both
   principal-Bianchi-compatible and nonzero on algebraic Riemann curvature:
   `(comm, symi, symi)`. Four pass Bianchi; three of those four pass only by
   erasing the entire Riemann sector
   (`k77-wave2-principal-bianchi-product-selector-2026-08-05.md:116-133`). The
   test is complete on the rank-91 principal Riemann/Bianchi carrier across all
   three nonzero `Spin(7,7)` covector orbits (`:88-104`), not a
   representative-jet heuristic.
2. **The ambient Einstein ratio.** That same row, computed separately, equals
   **`-2` times the fourteen-dimensional Einstein contraction** on the scalar and
   traceless-Ricci pieces and kills Weyl exactly (`:144-155`). Weyl-killing is
   automatic rather than selecting — the exact complexified `D7` arithmetic gives
   `3185 = 1 + 104 + 3080` with target multiplicities `2, 2, 0`
   (`k77b2-disposition:16-20`).
3. **The same-action cubic transgression.** For the source cubic
   `I_3(T) = (1/3)<T, S(T ^ T)>` to have the proposed unit-weight endpoint, an
   exact identity must hold for every `T, alpha`
   (`k77b2:252-262`).

**The collision.** K77-B2 evaluated (2) and (3) jointly on the displayed
factorized family. For `comm/symi/symi` the Einstein conditions are `ac = a` and
`bc = b`, while the transgression witnesses give `base = (1024/3)a - (512/3)ac`
and `dual = (512/3)bc` (`k77b2:240-246, 264-276`). Substituting the Einstein
conditions into the transgression conditions **forces `a = b = 0`**, and since
every displayed term contains the same `Phi1`, factorization then makes the map
**identically zero** (`k77b2:276-281`). The certificate is exact: reduced
Groebner basis `<a, b>`, **nonzero map survivors = 0**, and it is stated as an
exact counterexample-based family obstruction, not a sampled pass promoted to an
identity (`k77b2:330-338`).

> **VERDICT: OVER-DETERMINED. On this row the displayed-family GU construction
> is FALSIFIED.** The unique product assignment that the source's own Bianchi
> requirement selects, and which does reproduce the ambient Einstein contraction
> exactly, is the same assignment the source's own cubic transgression identity
> annihilates. Three source-motivated conditions on one object admit only the
> zero map.

**The scope, stated at full strength and not one inch wider.** This is a
`DISPLAYED_ANSATZ_KILL` under the **joint** ambient-Einstein plus same-action
reading (`k77b2-disposition:35-40`). It is **not** a K77 mechanism kill, it is
**not** a lane kill, and it does **not** decide the `VERTICAL-FROBENIUS-TRACE`
fork — under the observed-4D or vertical-Frobenius trace reversal the Einstein
conditions change and the collision may dissolve. The surviving construction
target is a source-natural **full-domain, non-factorized** extension
`Lambda^2 V* (x) u(64,64) -> Lambda^13 V* (x) u(64,64)`, which restriction does
not determine uniquely (`k77b2:305-317`).

**What this lane adds that neither prior artifact states.** K77-B2 (2026-08-04)
established the kill over the family; the principal-Bianchi wave (2026-08-05)
established that Bianchi selects **exactly one** member of that family and that
it is Einstein-matching. **Neither file composes the two.** Composed, the
statement sharpens from "no member of the family survives both conditions" to
**"the unique Bianchi-selected, Einstein-matching member is precisely the member
the transgression identity kills."** That is a three-way collision on a single
named row, and it is the sharpest adverse result currently available anywhere in
the program. It should be scheduled as such: the cheapest way to learn whether
GU survives it is to re-run condition (3) under the other two trace reversals,
which is finite exact arithmetic on objects that already exist.

**Dependency:** `U3`. **Grade:** the collision is `VERIFIED_REPO_DISCONNECT`
between two artifacts filed one day apart; each half is exact and machine-tied.

### GR-2 — the cosmological term `[N|S]`

**Physics writes:** `-(2 Lambda / 16 pi G) sqrt(-g)`, with
`Lambda / M_Pl^2 ~ 10^-122`.

**GU produces, split into two parts that must not be conflated.**

*The sign — DETERMINED, without guessing.* The constant-section `O(M^0)`
vertical second fundamental form of the gimmel metric,
`B_{mn,ab} = -(1/2)(g_{a(m} g_{n)b} - (1/2) g_{ab} g_{mn})`, has fibre-trace
**exactly `+1/2 g_{mn}`** — computed, dim 4, flat background
(`H45:127-131, 211`; source formula `ii-s-coordinate-formula-2026-06-23.md`
sec 6.1). That is a **positive**, de-Sitter-signed cosmological signature, and it
is a zero-derivative term entering the operator symbol at `s^0` only, so it fixes
the vacuum sign **without touching** the `s^1` Einstein or `s^2` Weyl
coefficients. Cross-check: `C_RY > 0` by two independent methods (`H25`), giving
`m^2_eff = 1/2 + C_RY > 1/2 > 0`.

**This is the one row in the cluster that is DETERMINED rather than conditional
in its qualitative content, and it should be said plainly: GU derives the SIGN of
the cosmological constant.** The honest fences, both already on the record: the
result is conditional on `U1 = \|II\|^2` (under `\|H\|^2` thread-B assigns the
opposite sign), and the curved-ambient *horizontal* `R^Y` contribution is
Krein-indefinite with both signs present, so the **net** `O(M^0)` sign remains
gated on `mu_DW` and the source normalization (`H45:139-147`).

*The magnitude — REQUIRES-UNKNOWN `U5`, and provably so.* `H49` fires as a
class-level scope-kill: a scale-free (conformally invariant / dimensionless)
action's Euler-Lagrange system fixes only dimensionless `O(1)` ratios, while the
observed `Lambda` is a `~10^-120` hierarchy in Planck units, so the magnitude
**requires an imported scale the action provably cannot contain**
(`tests/wave28/H49_bach_weyl_sector.py:456-476`). DC-H2's congruence-orbit
theorem independently excludes the entire class of symmetry conditions from
supplying it. `Lambda_bare` (`U6`) sits in `S_geom` as an explicitly charged
coefficient (`packet:575-586`) and the packet states "the cosmological response
is not read at N1" (`:586-587`).

**Verdict: sign DETERMINED given `U1`; magnitude REQUIRES-UNKNOWN `U5`, with the
class of possible suppliers exactly excluded (T3).** The correct public shape,
matching the council's ruling on the DE amplitude, is: **GU predicts the sign and
a dimensionless coefficient; it does not predict the scale.** Saying more than
that is the "we proved GU requires an imported scale" conversion the 2026-08-04
council named as the most expensive credibility mistake available, and it is
declined here as it was there.

*Separately, and not to be merged with the above:* the `theta`-sector dynamical
dark energy (canon `theta-field-flrw-dark-energy-eos.md`) is a **second**
component with `w != -1`. Its derived content is the dimensionless coefficient
`lambda_{N,1} = (9/2)^2 - (7/2)^2 = 8` exactly, hence `M_KK = 2 sqrt(2) H_0`; its
scale is imported from `R_s = c/H_0`. Signal-level exclusion stands
(`DARK-ENERGY-07`), and the honest one-liner is "excluded AS the DESI dark-energy
signal; unconstrained-but-null as a family" (`:309-313`).

### GR-3 — curvature-squared, Weyl/Bach sector `[N]`

**Physics writes:** nothing. GR has no `C^2` term; it is a higher-derivative
addition.

**GU produces it, under BOTH horns of `U1`, with the coefficient not free.** On a
TT graviton the `\|H\|^2` Willmore-EL operator is `box^2`, exactly the
conformal-gravity **Bach** operator (`box^2 h = -4 Bach^(1)`); since
`\|II\|^2 = \|H\|^2 - R^X`, the `\|II\|^2` operator is
`box(box + m^2) = box^2 + m^2 box`. So `c_weyl(\|H\|^2) = c_weyl(\|II\|^2) = 1`
and `c_weyl(Einstein) = 0` (`H45:92-101`). The `U1` binary gates **only** whether
GU *also* has an Einstein graviton; it does not gate the Weyl sector.

**Verdict: DIFFERS BY A NAMED TERM, geometric origin stated.** The origin is
exact: the gravitational functional is the norm of the second fundamental form of
the section `X^4 -> Y^14 = Met(X^4)`, and the conformally-invariant Willmore part
of that norm **is** the Bach sector. GR has no such term because GR has no
embedding.

**What it predicts, three things.**

1. **A massive spin-2 partner, and a Krein structure that is native rather than
   posited.** `\|II\|^2 -> s(s + m^2)`: two distinct poles, TT residues
   `(+1/m^2, -1/m^2)` — a healthy graviton plus a massive ghost in the correct
   Stelle ordering, with `m^2 = +1/2 > 0` real and non-tachyonic
   (`H45:151-158`). `H16` records the asset precisely: where generic Stelle must
   *posit* a ghost parity, GU supplies one natively
   (`H16-stelle-viability-2026-07-11.md:134`). Loop-level unitarity remains
   **OPEN**, inherited from generic Stelle and explicitly fenced
   (`H16:102, 124-125, 158`).
2. **Bach-flat exact vacua.** `div-Weyl = 0` identically on exact Schwarzschild
   at all orders in `M`, and Kerr is Ricci-flat, so GU's exact vacua coincide with
   GR's on both — a genuine strong-field cancellation, not a weak-field accident
   (`H45:102-105`, `tests/wave1/H1_bach_flat_exact_vacua.py`).
3. **A clean discriminator against entropic gravity.** Bianconi's second-order
   entropic gravity has no `box^2` term at all; GU carries one regardless of how
   `U1` resolves (`H45:99-101`).

This is the row that best matches Joe's description: one extra term, geometric
origin fully stated, and it explains three other things.

### GR-4 — curvature-squared, `R^2` / scalaron sector — **OVER-DETERMINED**

**Physics writes:** nothing.

**GU produces it, and the induced construction fixes both of its signs.** The
GU-native gravity functional lands (`H49`, `H57`-stage1) in the fourth-order class
`f(R) = gamma R + beta R^2 - 2 Lambda`, where `gamma` is the Gauss-fixed induced
Einstein coefficient and `beta = f_0^2` is the induced `R^2` coefficient coming
from `\|H\|^2` (`scalaron-normsign-and-vacuum-2026-07-11.md:42, 53-56`).
Critically, this is **not** a freely chosen `R^2` Lagrangian: on a free one you
may tune `gamma < 0` and manufacture a ghost; GU cannot, because the Gauss
identity fixes the `-R^X` Einstein term and `H25` computed its sign **positive**
(`:42`).

Two computed consequences, two independent derivations each, exact sympy
(`tests/W79_scalaron_normsign_and_vacuum.py`, exit 0):

- **Norm: POSITIVE.** The scalaron norm is `sign(f'(R_bg))`, set by `gamma`, not
  by `beta` — provably `beta`-independent (`:64-79`). The `R^2` scalaron is a
  *physical, positive-norm* propagating scalar. Loop-positivity **closes** on this
  leg.
- **Mass: TACHYONIC, at every background.** `M_0^2 = gamma/(6 f_0^2)` with
  `d M_0^2 / dR = 0` — background-independence is **exact**, a pure-`R^2` feature
  since `f'' = 2 f_0^2` is constant (`:90, 117, 175-179`). With `f_0^2 < 0` the
  scalaron is tachyonic at **every** constant-curvature background, including GU's
  own de Sitter true vacuum `R_vac = 4 Lambda / gamma`. The Einstein-frame check
  agrees: `phi_* = 0.92`, `V_E'' = -0.321`, a tachyonic **hilltop**, not a minimum
  (`:158`).

> **VERDICT: OVER-DETERMINED, conditional on exactly one sign.** GU is a
> "wrong-sign Starobinsky": positive-norm like Starobinsky, but tachyonic unlike
> it, and the tachyon is background-independent so **vacuum selection cannot lift
> it**. W79's own words: this is "the program's **first genuine no-go**, located
> precisely at the scalaron potential" (`:187-191`).

**The scope and the single escape, both exact.** The no-go is rigorous *within
the fourth-order truncation* and is conditional on **one number**: `f_0^2 < 0` on
GU's asymptotically-free trajectory, which is **ported one-loop betas**
(W45-47/W78), **not** a native GU computation (`:121, 193-197, 207-210`). If GU's
*native* `R^2` running gives `f_0^2 > 0`, the tachyon was a porting artifact and
the row flips to healthy Starobinsky. The second escape, named and priced: if
higher-derivative terms beyond the truncation contribute a non-constant `f''(R)`,
background-independence — the thing that makes the no-go airtight — weakens
(`:234`).

**Residue grade: T4.** This is the tightest item in the entire cluster and the
only `T4` this lane produces. The requirement is fully typed, dimensionless, a
single sign; every alternative rescue route (vacuum selection, rolling
background, Coleman-Weinberg-style lifting) is **exactly excluded** by
background-independence (`:133-142`); and it is handable to an outside specialist
as a self-contained one-number computation. Per the council's ordering rule
(residue-first, fan-out override), **this is the highest-value unscheduled
computation the Einstein sector contains**, and it stands between "GU's first
genuine no-go" and "a porting artifact."

### GR-5 — torsion terms `[N|S]`

**Physics writes:** nothing in GR (Levi-Civita, torsion `= 0`). Einstein-Cartan
adds torsion sourced algebraically by spin, with no propagating torsion.

**GU produces a torsion term natively, and it is not the Einstein-Cartan one.**
The augmented torsion `T` appears in the action's actual Euler derivative:

```
E_act = S(F_bar) + (D_T F_bar)^! S^! T + *kappa T
```

(`k77-wave2-eddy-augmented-torsion-euler-prolongation-2026-08-05.md:28-35`). The
`*kappa T` piece is the linear torsion term; the middle piece is the
Frechet/formal-adjoint companion, which vanishes or absorbs into the endpoint
**only** for a genuinely cyclic `S` or a lawful restricted domain. The
source-printed unit-weight endpoint `Upsilon_print = S(F_A) + *kappa T` is
**already proved not to be** the variation of the same action for the selected
nonzero full-domain Shiab (K77-B3) — an initial draft of that wave accidentally
revived the killed endpoint and the supersession reviewer caught it
(`:20-27`).

Two structural facts that are real gains: the quadratic "eddy" correction has an
**exact geometric role** — it is the average curvature along the path between two
connections — and it satisfies an **integrated Bianchi identity**
`D_B F_bar + [T, M1] = 0` in the connection-path DGA, which the two-connection
square independently reconstructs from its curvature and torsion blocks
(`k77-wave2-full-adjoint-...-2026-08-05.md:124-128, 139`).

**Verdict: DIFFERS BY A NAMED TERM, geometric origin stated, but the term's
coefficient normalization requires `U3` and its variational status is
`REQUIRES-UNKNOWN`.** The geometric origin is stated exactly: torsion in GU is
the defect of a **two-connection comparison** (gauge connection versus the
metric-induced one), which is the same structural shape as MacDowell-Mansouri /
Cartan first-order gravity — the adjacent-theory skeleton the 2026-08-04 council
named as the closest structural analogue (`science-council:270-277`).

**Layer-0 fence restated because it is easy to get wrong:** this row has **no**
relationship to the `Z/3`-in-`Z/24` generation-count "torsion arena." Those are
different senses of the word and no result transfers.

### GR-6 — the Einstein field equation itself `[N|S]`

This is Joe's headline row: *are the Einstein equations mostly the same, except
for one little thing?*

**Physics writes:** `G_{mu nu} = 8 pi G T_{mu nu} + Lambda g_{mu nu}`.

**GU produces the Gauss identity, which holds as a differential-geometric
tautology for any section `s: X^4 -> Y^14`:**

```
G^X_{mu nu} = G^Y_T_{mu nu} + Q_{mu nu}(B) + E^Psi_{mu nu}
```

(`canon/schwarzschild-weak-field-rfail.md:56`, with the term typings at `:59`).
`G^X` is the 4D Einstein tensor on the induced metric `g_s = s*(g)`; `G^Y_T` is
the tangential projection of the 14D Einstein tensor; `Q(B)` is the extrinsic
stress, **quadratic** in the second fundamental form `B = II_s^H`; and `E^Psi`
is the gauge-curvature contribution.

**Answer: yes, and the "little things" are exactly two, both named, both with
stated geometric origin.** `Q(B)`, from the extrinsic curvature of the observer
section, and `E^Psi`, from the gauge field. Everything else is the Einstein
tensor.

**What the difference predicts — and this is the part that is COMPUTED rather
than argued.** On linearized Schwarzschild the true mean-curvature vector is
`H^(1)_ab = (M/r) eta_ab`, and `M/r` is **harmonic**, so the entire linear-in-`M`
Willmore-EL residual `Delta H^(1)` vanishes **identically** — verified exactly by
sympy (`rfail-schwarzschild:89-101`, `tests/chase/MOVE-3/willmore_el_order.py`).
The leading residual is therefore `O(M^2/r^4)`, **quadratic in `M`**, the same
object as `Q(B)`. Consequences:

- Solar-system deviations are **quadratically** suppressed, safe a fortiori:
  `O(M_sun/r_Mercury) ~ 3 x 10^-8` as an upper bound against a `~10^-5` Cassini
  precision, with the true term smaller still (`:140-146`).
- The correction becomes `O(1)` at `r ~ 2M` — the **strong-field regime is where
  GU and GR must differ**, and that is `OQ2-D`, unbuilt (`:173`).
- Two competing prior estimates, `O(M/r^3)` (falsifying) and `O(M/r^4)` (safe),
  are **both RETRACTED** as artifacts of misidentifying mean curvature with the
  connection/distortion (`RFAIL-03`).

**Verdict: DIFFERS BY TWO NAMED TERMS, geometric origins stated, differences
COMPUTED at compatibility grade — and `REQUIRES-UNKNOWN` for the derivation.**
The essential fence, which the canon file states in its own headline and which
this ledger repeats rather than softens: this is a compatibility check on an
**IMPORTED** Schwarzschild metric, not a derivation of weak-field GR from GU. The
`R_fail^GR = 0` leg is trivial (true of any vacuum metric, zero GU content), and
the genuine open object is `OQ2-A`, the full gimmel Willmore-EL from GU's own
action. **"GU passes solar-system tests" is explicitly RETRACTED and must not be
cited** (`:18`, `RFAIL-02`).

### GR-7 — Newton's constant / the gravitational scale `[N|S]`

**Physics writes:** `G = 6.674 x 10^-11`, one measured number.

**GU produces:** `1/16 pi G` proportional to `alpha_II` times a power of
`mu_DW`, i.e. `U7 x U5`. `alpha_II > 0` is a live stratum, `beta_0/alpha_II`,
`Lambda_bare`, `mu_DW` and the background amplitude are all **charged**
(`packet:585-586`).

**Verdict: REQUIRES-UNKNOWN `U5`, with the class of suppliers exactly excluded
(T3).** Same theorem as GR-2's magnitude: `PRED-NORM-RANK`'s rank-3 log-monomial
certificate contains no invariant basis element with `source_norm`, so any route
depending on source normalization must wait for a native rule
(`pred-norm-rank-2026-07-15.md:71-72`), and DC-H2 excludes every symmetry-type
condition wholesale.

**The unasked count question, restated here because this row is where it bites.**
The 2026-08-04 council named it as the cheapest high-value move in the program
and it is **still unrun**: is DC-H2's horizontal:vertical **length-squared** the
same object as `PRED-NORM-RANK`'s `l^2 = Z_U kappa`, or independent of the rank-3
rescaling group? Same object ⇒ the certificate covers it and the imported-scale
count is unchanged at one. Independent ⇒ **the contract-locked rank-3 certificate
is INCOMPLETE and the program's imported-scale count is at least two**
(`science-council:495-508`). Until it is run, **this ledger cannot honestly state
how many independent scales GU must import**, which is the first number an
outside physicist asks for. Cost: hours.

---

## 5. The Standard Model sector, term by term

### SM-1 — the gauge kinetic term `[N|S]`

**Physics writes:** `-(1/4) SUM_i (1/g_i^2) F^{i,a}_{mu nu} F^{i,a mu nu}` over
`SU(3) x SU(2) x U(1)`, **three independent couplings**.

**GU produces:** `(zeta_F / 2 g_A^2) INT_Y kappa_g(F_A, *_G F_A)`
(`packet:465-470`) — the Yang-Mills kinetic term in exactly the standard form
(invariant adjoint pairing, Hodge star), with two structural differences:

- **One coupling, not three**, for the whole ambient structure group, and it
  lives on `Y^14` rather than `X^4`. The three SM couplings must therefore come
  from branching plus dimensional reduction of a single `g_A`. That is
  GUT-shaped, and it is a genuine constraint the SM does not have — but **the
  reduction is unbuilt**, so no ratio prediction is currently extractable.
- **`zeta_F in {0,1}` is a declared fork (`U2`)**: `zeta_F = 1` makes Yang-Mills
  fundamental; `zeta_F = 0` makes it induced, in which case the gauge kinetic
  term is not a written term at all but a consequence of the fermion determinant
  and the `\|II\|^2` sector (`packet:498`).

**Verdict: exactly-same in FORM, DETERMINED GIVEN `U2` in status, and
UNDER-DETERMINED in normalization with the freedom EXACTLY QUANTIFIED — and this
is the strongest positive coefficient result in the repo, so it is stated
precisely.** `W203` proves by **Schur's lemma** that the equivariant symmetric
kernel space is **1-dimensional**, generated by the Clifford metric `eta` (of
signature `(9,5)`, indefinite), so **every relative coefficient in that sector is
FORCED** and **exactly one** coefficient is undetermined: the overall scale
`kappa`, which sets Newton's `G`
(`W203-branch3-source-action-fixed-coefficients-2026-07-14.md:121-131`). It is
explicitly *not* fitted to data (`:130-139`). **Freedom quantified: 1.** Scope
fence, which the same file states: `Z_U`, the gradient stiffness, is **NOT
BUILT** (`:127`), and W180/W203 are ultralocal (`:199-202`), so the forcing holds
on the ultralocal branch-3 sector and not on the gradient sector.

**The source-fidelity fence, which must travel with this row.** The Portal source
says the second-layer operator contains a "Shiab / adjoint-Shiab **generalization**
of the Yang-Mills term plus a zeroth-order Einstein-like piece," and the
reinspection grades the wording exactly: *"'replacement,' 'generalizing,' and
'looks like,' **not an identity with the observed Standard Model action**"*
(`lab/sources/gu-two-layer-action-source-reinspection-2026-08-04.md:30, 44-46`).
This row therefore claims *the same functional form*, not *the same theory*.

**The row's acceptance blocker, stated because it is the one physics will check
first.** The K77-A atomic census carries this row's observable consequence at
status `OPEN_PHOTON_KERNEL`: no extracted weak doublet, no vacuum orbit and **no
massless-photon kernel exists** (`resolver-wave-k77a-...-2026-08-04.md:307, 328`;
`lab/process/resolver-wave-k77a-atomic-particle-crosswalk.json:679, 735, 948`).
A gauge kinetic term with the right form but no massless photon in its spectrum
is not yet the Standard Model's gauge sector.

### SM-2 — gauge-coupling unification / normalization `[N|S]`

**Physics writes:** three measured numbers `g_1, g_2, g_3` (or `alpha_em`,
`sin^2 theta_W`, `alpha_s`).

**GU produces:** one `g_A^{-2}` (charged) plus a branching pattern. The
containment side is genuinely strong — the Pati-Salam / `SO(10)` representation
content verifies 19/19 with an independent Clifford cross-check — but it carries
a standing fence the council states explicitly: **containment is not
action-selection** (`science-council:344`).

**Verdict: REQUIRES-UNKNOWN `U2, U5`; UNDER-DETERMINED with the freedom NOT yet
quantified**, because the branching that would turn one coupling into three
ratios has not been computed. **This is the row `M-M4` would move.** The
branching dictionary is rated the highest-fan-out unbuilt item in the program —
one Sage/Racah-Speiser module producing `Lambda^k V (x) S^+-` decompositions, PS
branching and reality types, discharging five registered open items **from one
build** — and its stated `J2`/Sage gate is **de facto already satisfied**, since
Sage is in production use across Wave G, K77-B, K77-B2 and K77-B3
(`science-council:362-374`). It is unbuilt anyway. This ledger's contribution is
to record that `M-M4` is a **direct dependency of two SM rows** (SM-2 and SM-5),
which was not previously joined.

### SM-3 — the fermion kinetic term `[N|S]`

**Physics writes:** `SUM psi-bar i gamma^mu D_mu psi` over three generations of
15 (or 16) Weyl fermions — and the defining feature is that it is **chiral**.

**GU produces:** `(1/2) Re INT_Y [Z, K_G (D_h + lambda_F V_GT(F_A)) Z]`
(`packet:472-478`), a Krein-paired first-order Dirac-type operator on the full-20
carrier `E_20 = S (+) I (+) R` with `I = im Gamma`, `R = ker Gamma`, carrying the
already-closed odd-polarization family `D_h` (`packet:388-408`).

Three differences from the SM kinetic term, each with a stated origin:

1. **A curvature vertex inside the kinetic operator.** The gamma-traceless
   curvature vertex `V_GT(F_A) = P_R(contract(F_A) - (1/6) wedge(F_A)) P_R` is an
   explicit summand of the quadratic form, with the internal ratio `-1/6`
   **fixed and inherited** while only the overall coupling `lambda_F` stays
   charged (`packet:452-462`). The SM has no such term; it is a Pauli-type
   non-minimal coupling forced by the gamma-traceless carrier. **DIFFERS BY A
   NAMED TERM whose geometric origin is stated, with a fixed internal ratio** —
   this is one of only two places in the cluster where GU supplies a
   *dimensionless number* rather than charging a coefficient.
2. **The pairing is Krein, not positive-definite.** `F = empty` on the kinematic
   carrier and this is **signature-robust** — the boost witness transfers to
   `(7,7)` (`lab/process/CURRENT-RESEARCH-CONTEXT.md:130-136`). This is normal for covariant
   indefinite theories (Gupta-Bleuler) and its only exit is the BRST/quotient
   route, register-tracked as `M-H17`. See SM-8.
3. **The parent is NON-CHIRAL.** Wave K reframed the gate from "force chirality"
   to "derive effective chirality from a non-chiral parent"
   (`science-council:113`). Under the settled reading A, the imposter 128
   `S(V)(x)S(W)` block is kinematically **VECTORLIKE** — `64 + 64`, joint
   `32/32/32/32` — with `PH-K1-PHYSICAL` OPEN/BLOCKED and any chiral use of the
   384 still owing the Witten-1983 exit (`lab/process/CURRENT-RESEARCH-CONTEXT.md:76-82`).

**Verdict: DIFFERS BY A NAMED TERM (`lambda_F V_GT`, ratio `-1/6` fixed) AND
REQUIRES-UNKNOWN `U8` for the polarization coefficients AND `REQUIRES-UNKNOWN`
for chirality itself.** The chirality item is **under-determined, not
over-determined** — the mechanism is unbuilt, not contradicted — and this
distinction is stated explicitly because the temptation to file a non-chiral
parent as a falsification is exactly the "blocker acquires a story" failure the
near-miss council warned against. **T2:** typed, no class exclusion.

**One nuance that removes a naive objection, and one that does not.** K77-A's §3
shows that operator incidence is **not** bilinear incidence: the mass/source
object is a bilinear `Psi^* K X Psi`, not the bare operator `X`, and the exact
parity census gives the vertical Clifford row as `(preserve, flip)` bare but
`(flip, preserve)` for `KX` — i.e. **cross-Weyl in 4D** while preserving the
ambient half (`k77a:211-231`). That removes a naive chirality objection. It does
**not** identify the coefficient with the Higgs, select a vacuum, prove a mass,
or close `SA-Y1` (`:228-231`).

### SM-3b — the constraint-preserving vertex versus the written Shiab — **OVER-DETERMINED**

This is a sub-row of SM-3 and SM-8 jointly, and it is the third place in this
cluster where GU is currently over-determined.

**The two demands.**

1. **Constraint preservation.** The gamma-traceless `ker Gamma` carrier is a
   *constrained* system; for the BV/ghost sector to close, the compensator
   `sigma_c` must make `(I - Pi_RS)(M_D + sigma_c) Pi_RS` vanish. The measured
   defects are nonzero: `||[Pi_RS, M_D]|| = 58.72` and
   `||(I - Pi_RS) M_D Pi_RS(gauge)|| = 169.19` (`DERIVATION-PROGRESS.md:1859`).
   The **unique** gamma-traceless constraint-preserving coupling is
   `wedge - 6 . contract`, i.e. `t* = -1/6` (`:1861`).
2. **GU's written Shiab.** The canon shiab is the Clifford-trace channel, real
   coordinates `(1,0,1,0)` — and it is gamma-trace**ful**, measured norm
   `215.85` (`:1861`). `SHIAB-04` states the same collision from the other side:
   the gamma-trace/RS selector "pins to 1 but **EXCLUDES** GU's Clifford-trace
   shiab" (`canon/shiab-existence-cl95.md:81`).

> **VERDICT: OVER-DETERMINED.** `DERIVATION-PROGRESS.md:1861` states it without
> hedging: **constraint-preservation and GU's written Shiab are jointly
> unsatisfiable.** If one operator must serve both roles, there is no solution.

**How the candidate action resolves it — by abandoning the written Shiab at this
slot, and this is worth seeing explicitly.** The packet's gamma-traceless
curvature vertex is

```
V_GT(F_A) = P_R ( contract(F_A) - (1/6) wedge(F_A) ) P_R
```

with the note that "the ratio is **inherited and fixed**; its overall coupling
`lambda_F` remains charged" (`packet:452-462`). That is `-1/6` times the
constraint-preserving combination `wedge - 6 . contract`. **The packet has taken
the constraint-preserving operator, not the canon Shiab.** Whether this is a
deliberate choice or a silent divergence is not recorded anywhere; either way the
consequence is the same and it is the cleanest instance in the cluster of this
lane's method working in reverse:

> **Constraint propagated BACKWARDS onto the unknown: `U3` (the Shiab selector)
> is constrained by the ghost/constraint sector to be the gamma-traceless
> operator, and that choice EXCLUDES GU's own written formula.** No forward
> construction was needed to learn this.

**Scope.** The over-determination is real *if and only if* the same operator must
occupy both slots — the degree-two Shiab `Omega^2 (x) S -> Omega^1 (x) S` and the
curvature vertex inside the full-20 Dirac-type operator. `DERIVATION-PROGRESS`
treats them as competing for the `sigma_c`-closure role; the packet treats the
vertex as its own object. **Deciding that typing is finite and cheap, and it is
the whole content of the row.** If they are different slots, the row downgrades
from OVER-DETERMINED to a `Layer-0` distinction that should be written down; if
they are the same slot, GU's written Shiab formula does not survive its own ghost
sector.

### SM-4 — the fermion mass term `[N|S]` — a term physics does not have

**Physics writes:** *nothing.* Gauge invariance forbids a bare fermion mass in the
SM; every fermion mass comes from a Yukawa times a VEV. This is a structural
feature of the SM, not an accident.

**GU FORCES one.** `T6 = m_RS <Psi, Psi>_K` is **FORCED** by `SA-U4`
(`source-action-term-by-term-2026-07-29.md:88, 138`), and the packet writes it
with a bounded magnitude:

```
|h_mu| = m_R = sqrt(m_2,eff) * mu_DW > 0,   m_2,eff in [5/6, 5/4]
```

(`packet:414-420`). Its origin is stated: the `g = 1` `ker Gamma` cure (`T5`) plus
the subprincipal-causality requirement on the gamma-traceless carrier — the mass
is what makes the constrained Rarita-Schwinger system propagate causally.

**Verdict: DIFFERS BY A NAMED TERM whose geometric origin is stated.** And it
carries a real, if scale-relative, prediction: a heavy **vectorlike** RS sector at
`m_R = sqrt(m_2,eff) mu_DW` with `m_2,eff` bounded in `[5/6, 5/4]` — a
dimensionless quantity constrained to a **1.5x band**, which is the second of the
two places in this cluster where GU supplies a number rather than charging one.

**Fences.** The absolute scale is `REQUIRES-UNKNOWN U5` (DC-H2's exclusion again).
`Pi_RS^phys` does not exist, so a *physical* reading of the RS block is
`BLOCKED_NEEDS_SPEC` and the mass is currently a statement about the kinematic
carrier.

### SM-5 — the Yukawa couplings `[N|S]`

**Physics writes:** `-(Y_u)_{ij} Q-bar_i H-tilde u_{Rj} + (down) + (lepton) + h.c.`
— three `3x3` complex matrices, reducing after field redefinitions to **13
physical parameters** (9 masses, 3 angles, 1 phase).

**GU produces two distinct vertical defect bilinears**, `S_Yuk,K` (Hermitian,
`K`-branch) and `S_Yuk,C` (charge-conjugation branch), with
`Y_K, Y_C in M_3(C)` acting on the **three provenance summands**
(`packet:620-650`). The packet's Layer-0 fence is explicit and load-bearing:
**they are not generation matrices**.

**The CHANNEL is DETERMINED — exactly, uniquely, and by computation. This is the
row's one genuine derivation and it should not be buried under the texture
problem.** The complete equivariant channel table
(`yukawa-scoping-2026-07-13.md:59-65`) gives:

| carrier | `dim Hom` | chirality | role |
|---|---:|---|---|
| `Lambda^0` (scalar) | **1** | **OPPOSITE only** | **the unique Dirac-Yukawa channel** |
| `Lambda^1` (vector, 14) | 1 | SAME | not a Lorentz scalar |
| `Lambda^2` | 1 | OPPOSITE | — |
| `Lambda^0` on `S^+ x S^+` | **0** | — | Majorana scalar: **absent** (SHIAB-05 reproduced as control) |

Two wholesale exclusions come with it: **CHANNEL-FORBIDDEN** — no *non-form*
Higgs carrier has any Yukawa channel at all, `dim Hom = 0` exactly, which is why
`SA-Y1` is FORCED rather than declared (`:70-75`) — and **FN-sterility**, mod-3
Froggatt-Nielsen charges are provably sterile for **all 27** assignments
(`spec:72`). So the Yukawa *slot* is not open: its carrier type is forced, its
multiplicity is exactly one, and its chirality structure is fixed. What is open
is everything numerical: `yukawa-scoping`'s own verdict is "**SILENT on
hierarchy** ... NO built object grades magnitudes — the three surviving couplings
are free" (`:6`).

Four further things that are settled and worth stating precisely:

- **The carrier exists.** The vertical `(1,10)` part of the existing `T1 + T2`
  connection coupling already gives a nonzero, Hermitian, cross-chirality 4D
  Lorentz-scalar Krein bilinear, so `T10` is **not required merely to create the
  Yukawa carrier** (`source-action-term-by-term:42-51`). The channel is real.
- **The old texture is RETRACTED.** `SA-Y4`'s `9 -> 3` reduction "by the derived
  `Z/3`" and `SA-Y7a`'s `Z/3`-doublet spurion both assumed a symmetric triplet the
  product rule does not supply; the packet states the older `Z/3` texture
  **cannot be imported** after the multiplicity/index Layer-0 correction, so the
  **full `3x3` matrices are charged** (`packet:648-652, 1142-1145`).
- **`SA-Y6` narrows but does not close.** Under `2+1` the third family is
  *identifiable* — it is the `S(V)(x)S(W)` term and it is not of Leibniz form — so
  sector-to-flavour is no longer a free assignment among three symmetric sectors.
  One of three is distinguishable **before** any physics input; which of the
  remaining two is which stays free (`source-action-term-by-term:155-161`).
- **The Majorana channel is provably ABSENT from the equivariant family.**
  `dim Hom_{Spin(9,5)}(S^+ (x) S^+, Lambda^0) = 0` exactly — the scalar
  charge-conjugation bilinear exists only off-diagonally on `S^+ <-> S^-` — with
  the hard checksum `16384 = 128^2` and all component errors `0.00e+00`
  (`shiab-existence-cl95.md:83-84`, `SHIAB-05`). So a same-chirality Majorana
  scalar mass **must** be supplied by an external source-action spurion, which is
  `U10`, the twelve `sigma_A` — an **optional** branch, absent entirely on the
  minimal no-same-chirality branch (`packet:668-695`).

> **Verdict: CHANNEL DETERMINED (unique, `dim Hom = 1`, cross-chirality, with two
> class-exclusions banked); TEXTURE AND VALUES REQUIRE-UNKNOWN `U9` (and `U10`
> for the Majorana sub-row). On the numerical side the accounting must be stated
> plainly: GU does NOT currently reduce the Yukawa parameter count. It relocates
> it.** The SM
> charges 13 physical parameters here; GU charges `2 x 18 = 36` real parameters in
> `Y_K, Y_C`, plus 12 `sigma_A`, plus a 4-fold discrete `(eps_C, tau_C)` branch,
> with **no texture derived**. A ledger that reported this row as progress would
> be measuring artifacts rather than residue.

**T1** (named, untyped) for the texture; **T2** for the Majorana spurion (typed —
a same-chirality scalar bilinear of specified rep-theoretic type — with the
equivariant class exactly excluded, but no magnitude). The seesaw leg's own
retrodiction gives `M_R in 1.1-6.0 x 10^14 GeV`, squarely inside the canonical
GUT-seesaw window, but its external requirement is a GU-native `M_PS` (`M-M22`,
unbuilt) plus a VEV in the `(10,1,3)`-type component of `Lambda^5` (channel
located, VEV **not** earned) — **T2, and it has not tightened since filing**
(`science-council:195, 205-211`). `M-M4` would move it to T3.

### SM-6 — the Higgs kinetic term and potential `[N|S]`

**Physics writes:** `|D_mu H|^2 - mu^2 |H|^2 - lambda |H|^4`, with `mu^2 < 0`,
`v = 246 GeV`, `m_H = 125 GeV`, `lambda ~ 0.13`.

**GU writes no separate Higgs term — and the source says that is the point.** The
candidate action selects the **minimal `V_src = 0` stratum** of the `W229` parent
"rather than leaving an unspecified potential in the action" (`packet:462-465`).
There is no scalar potential term written anywhere in the candidate. But that is
not the whole row, because the mechanism the source proposes **is not a separate
term at all**.

**The mechanism, and it sits entirely inside `T1`.** The primary source is
explicit: *"there's no Higgs. **The Higgs is an illusion.** ... They both have a
Klein Gordon kinetic term. They both have a quartic term ... **if your curvature
is negative, now you start to get a Mexican hat potential. Minimal coupling and
Yukawa coupling are the same thing**"*
(`layer0-pass-on-the-two-higgs-objects-2026-07-29.md:28-39`, transcript lines
145-149; the same directive is graded `SOURCE-CONFIRMS-CONSTRUCTION-DIRECTIVE` at
`gu-two-layer-action-source-reinspection-2026-08-04.md:33`). Writing
`A = A_0 + a` in the gauge kinetic term gives, term by term (`:41-49`):

```
||F_A||^2  =  ||d_{A_0} a||^2        <- Klein-Gordon KINETIC term for a
            + 2 <F_{A_0}, a ^ a>     <- QUADRATIC, sign set by BACKGROUND CURVATURE
            + ||a ^ a||^2            <- QUARTIC
```

*"Negative background curvature flips the quadratic's sign and produces a Mexican
hat **with no separate Higgs field and no separate Higgs potential**."*

**Verdict: DIFFERS STRUCTURALLY — the Higgs kinetic term, the quartic, and the
symmetry-breaking are not three inputs but three pieces of ONE already-present
term, with the geometric origin fully stated. DETERMINED GIVEN `U2` (that the
Yang-Mills term is fundamental) AND `U13` (that the background curvature is
negative), with one named unadjudicated bridge.**

**What this predicts, and it is the sharpest parameter reduction in the
cluster.** In the Standard Model the quartic `lambda` and the gauge coupling `g`
are **independent** measured numbers, and `m_H` is therefore an input. Here they
are **the same coefficient** — both are the expansion of `||F_A||^2` — so
`lambda` is not free: it is fixed by `g_A` up to the reduction. That is a
relation the SM does not have and it is exactly the shape Joe described ("one
little thing, explained by the geometry, that would explain other things"): it
would make `m_H / m_W` a **prediction** rather than an input. **This is not
claimed as a computed prediction.** The reduction that would turn it into a
number does not exist, and the row below says why.

**The named unadjudicated bridge, which is why the row is not DETERMINED
outright.** The adjudicated Layer-0 verdict is
**`HOMONYM-WITH-NAMED-BRIDGE`** (`:18`): `SA-Y1`'s Higgs is the `Lambda^0`
carrier (`dim Hom = 1`, **OPPOSITE** chirality); Weinstein's is the connection
perturbation `a in Omega^1(ad)` = `Lambda^1` (`dim Hom = 1`, **SAME** chirality,
and not a Lorentz scalar). The file's own conclusion is blunt: **"Weinstein's
mechanism does not satisfy `SA-Y1` as stated"** (`:60-68`). The bridge is stated
exactly and is recorded **UNCERTAIN** (`:70-82`): under `14D -> 4D`,
`Lambda^1(V_14)` gives `Lambda^1(V_4)` plus **ten 4D scalars** — does a vertical
vev of `a` convert the 14D same-chirality `Lambda^1` channel into a 4D
opposite-chirality Dirac mass channel? That single question decides whether the
"no Higgs" mechanism and the forced Yukawa channel are the same physics or two
different ones.

**`U13` is a genuinely new requirement-row shape, and this ledger is where it
lands.** *"If your curvature is negative"* is a **condition on the background,
not a free choice**; the Layer-0 pass records it as "a new requirement-row-shaped
obligation with no current row," with its class (FORCED / DECLARATION / FIT)
**unknown** (`:96-100`). Something must supply or declare `sign(F_{A_0})`, and
nothing currently does. **Recommendation: this belongs in the `SA-*` requirements
spec as a new row.** It is the only place in this cluster where the pass
generates an obligation the spec does not already carry.

**The honest fences, none softened.** All four K77-A Higgs-sector rows are
**OPEN**: `higgs_doublet_four_real` is `SOURCE_GUIDED_CANDIDATE` /
`PARENT_TYPE_ONLY`; both Goldstones are `OPEN_GOLDSTONE_ORBIT` / `TARGET_TYPED`;
and `higgs_scalar` is `OPEN_STATIONARY_SCALAR` with the debt written out —
*"Derive kinetic normalization, stable quartic potential, nonzero VEV, scalar
Hessian, and physical pole"* (`crosswalk.json:687-754`). The census scope note is
equally plain: "no extracted doublet, vacuum orbit, or photon kernel exists"
(`:948`), and the JSON records
`higgs_vs_vertical_trace_vs_cosmological_scalar: THREE_DISTINCT_TARGETS` (`:976+`)
— so the three candidate scalars must not be merged. **No Higgs potential is
anywhere written down, minimized, or varied in this repo.** `V_src = 0` is a
declared `N1` branch choice, not a derivation.

**T1**, and the row's status is the most interesting in the SM sector: GU has a
*mechanism* with a real parameter reduction attached and **no built object at
any step of it**.

### SM-7 — the theta terms `[N|S]`

**LAYER-0 FIRST, because this row is a homonym minefield.** The QCD vacuum angle
`theta_QCD` and GU's field `theta = pi - eps^{-1} B eps` are **different objects**
and nothing transfers between them. GU's `theta` is a 1-form equal to the second
fundamental form; the QCD `theta` is a dimensionless periodic parameter
multiplying a characteristic class.

**Physics writes:** `theta_QCD (g^2 / 32 pi^2) F^a_{mu nu} F-tilde^{a mu nu}`,
allowed by every symmetry, and measured to be `|theta-bar| < 10^-10` — the strong
CP problem.

**GU's `theta`-sector terms are NOT this.** The ambient action carries
`(1/2 kappa) INT_Y kappa_g(theta, *_G theta) - INT_Y kappa_g(theta, *_G J(Z))`
(`packet:470-472`) — a **mass-type quadratic** plus a **linear source coupling**,
both contracted with the Hodge star `*_G`, hence **metric-dependent and not
topological**. There is no `INT F ^ F` in the written candidate.

**Verdict on the QCD theta slot: REQUIRES-UNKNOWN, currently T0 — the requirement
is met by an unexamined convention (the term is simply not written).** Naming
this T0 rather than "absent" is the honest grading: a term allowed by all
symmetries that a candidate action omits without argument is a convention, not a
prediction.

**But there is a real and specific candidate for the slot, and it is worth
recording precisely because it is exactly the shape Joe asked for.** The packet's
classical/global action is

```
S_packet,cl = S_Y + s_! S_X + T_9[L_sigma],    T_9 circle-valued mod 2 pi
```

(`packet:340-352`), and `T_9` is defined by

```
exp(i T_9(l)) = Hol_{L_sigma}(l) = (-1)^{<w_1(L_sigma), [l]>}
```

(`packet:890-896`), where `L_sigma` is the flat real orientation line over the
DeWitt circle with `w_1(L_sigma) = 1` and holonomy `-1` on the primitive DeWitt
loop (`packet:875-879`). **`T_9` is GU's only written topological term, and it is
`Z/2`-quantized: `theta in {0, pi}`, not a continuous angle.** It is also the same
object as `P1/P2`, the orientation datum, and as the ghost-parity local system
(`packet:137, 854`) — and it is precisely the term `T9` that the 2026-07-29
term-by-term pass listed as **"not yet written"**
(`source-action-term-by-term:91`), now written in the packet.

> **REFEREE_CONJECTURE, filed as a conjecture and not as a finding.** *If* the
> QCD theta slot descends from `T_9`, then `theta-bar` is **quantized to
> `{0, pi}` by the `Z/2` orientation class** rather than being a free continuous
> parameter — which is the shape of a strong-CP resolution, with the geometric
> origin being `w_1` of the DeWitt orientation line.
>
> **This is NOT claimed, and the repo has already run the nearest neighbour of
> this idea and it FAILED.** The "more-predictions" hunt filed candidate G
> (strong CP) and the anomaly-grade banking pass returned:
> *"(G) strong-CP ... **no**, and it does NOT pin `theta_QCD -> 0`: an ANOMALOUS
> T does not enforce the T-symmetric vacuum; `theta` tracks `sigma`"*
> (`operator-grade-anomaly-banking-2026-07-21.md:302`; see also
> `council-inherited-tension-resolution-2026-07-21.md:225-234`,
> "proposal-grade adjacency, not a partial"). The standing banked posture is
> `INHERITS`: `theta_QCD` is **not forced in GU** and the reflection-anomaly
> descent fix is unbuilt (`tension-ledger-successful-theories-2026-07-21.md:165`).
> A `Z/2` holonomy is a *different* proposal from an anomalous-`T` descent, so
> that result does not close this one — but it is the correct prior, and it is
> why this is filed as a conjecture with a low prior rather than as a lead.
>
> Two further reasons for restraint: no descent map from `T_9`'s `Z/2` class on
> the fibre DeWitt circle to the 4D gauge instanton number exists, and the
> `EPS-CHARACTER` fork — whether `eps` attaches to the linear Krein involution or
> the antilinear coflip — is **open and could make this a Layer-0 error in a
> banked identity**.
>
> **Deciding check, cheap and named:** does `T_9`'s class push forward to
> `INT_X tr F ^ F` under the section, and does `P1`'s orientation datum couple to
> the gauge Pontryagin class at all? If no, the QCD theta slot is simply **empty**
> and stays T0. Both outcomes are informative; the negative outcome retires a
> tempting story, which — given the banked `INHERITS` posture — is the more likely
> and still the more useful result.

### SM-8 — gauge-fixing and ghosts `[N|S]`

**Physics writes:** `-(1/2 xi)(partial . A)^2 + c-bar partial^mu D_mu c`, with
`xi` a free gauge parameter, plus diffeomorphism ghosts on the gravity side.

**GU produces a BRST structure that is not a Yang-Mills ghost sector.** The
candidate semidirect BRST skeleton is

```
s_0 A = D_A c_g + L_xi-tilde A,     s_0 U = [c_g, U] + L_xi-tilde U,
s_0 Z = rho(c_g) Z + L_xi-tilde Z + R_r gamma,
s_0 c_g = (1/2)[c_g, c_g] + L_xi-tilde c_g,   ...
```

(`packet:696-720`) — carrying **both** a gauge ghost `c_g` **and** a
diffeomorphism-lift ghost `xi-tilde`, because the inhomogeneous gauge group is a
**semidirect product**. The lifted `Diff(X)` action is included explicitly in the
BV skeleton and is "not represented by the `s^+` term alone" (`packet:324-328`).

**Verdict: DIFFERS STRUCTURALLY (a semidirect gauge-times-diffeomorphism BRST
complex rather than a plain YM ghost sector), and REQUIRES-UNKNOWN `U11`.** The
same structural shape as MacDowell-Mansouri / Cartan first-order gravity, which is
the borrowed skeleton the council recommended.

**Why this row is load-bearing rather than bookkeeping — and this is the single
most important thing to understand about it.** GU's ghost sector is not optional
technology. `F = empty` on the kinematic carrier is **signature-robust**, and the
**only** exit to a positive physical subspace is the BRST/quotient route,
register-tracked as `M-H17` (`lab/process/CURRENT-RESEARCH-CONTEXT.md:130-134`). So the object `U11`
must supply is sharply typed: **a BV/BRST differential whose ghost-number-zero
cohomology is positive-definite on the Krein carrier.** That is a T3 requirement —
type and role fixed, and a whole class of alternatives (any attempt to find
positivity on the kinematic carrier directly) exactly excluded by the
signature-robust `F = empty` result.

**Current status of the pieces:** `T4 = S_compensator[sigma_c]` is "written,
**closure unverified**" (`source-action-term-by-term:89`); `SA-U2`, the
ghost-mass convention, is a charged fork; `SA-U5`, the guardian-versus-finite-
cutoff-EFT branch, is a charged fork; and the open-BV row of the parameter ledger
has **syntactic rank at most 233,100 with actual rank to be computed**
(`packet:1110`). The master equation is not known to close.

### SM-9 — anomaly cancellation conditions `[N|S]`

**Physics writes:** these are not terms but consistency conditions on the
representation content: `SUM Y^3 = 0`, `SUM Y = 0`, `SU(2)^2 U(1)`,
`SU(3)^2 U(1)`, `grav^2 U(1)`, and Witten's global `SU(2)` condition. Their
non-trivial satisfaction by one SM generation is one of the strongest structural
facts in particle physics.

**GU's status splits cleanly into three legs, and only one of them is open.**

**(i) The SM-shadow perturbative conditions: SATISFIED, computed, not by
analogy.** All four coefficients of the chiral 16 vanish — `U(1)^3 = 0`,
`grav^2-U(1) = 0`, `SU(2)^2-U(1) = 0`, `SU(3)^3 = 0` — **by real representation
theory**, because `Spin(10)` has no symmetric cubic Casimir; Witten's `SU(2)`
condition is satisfied (four doublets, even); and the
`Omega^Spin_5(B G_SM)` shadow carries no obstruction
(`W222-falsify-sm-emergence-anomaly-hypercharge-2026-07-14.md:5`). **Verdict on
this leg: exactly-same as physics.** The caveat travels with it and is the same
one as SM-3: the carrier delivers a **vectorlike `16 + 16bar`**, so chirality is
produced only by an unbuilt mirror-gapping condensate; and the mirror `16bar` is
itself anomaly-free on all three axes, with the teeth being that a 15-mirror
(dropping `nu_R`) gives `mod-16 = 15 != 0` and flips the verdict to FALSIFIED
(`W224:143, 227`).

**(ii) The global / Dai-Freed leg: DETERMINED, and fork-robust.** On balanced
(`W = 0`) contents every torsion receptacle is **empty**
(`Omega^spin_15 = 0`, also 13 and 14; `Omega~^spin_15(BSp(64)) = 0`;
`KO_15 = KSp_15 = 0`) and the free part vanishes **exactly**, so
`eta-bar = 0 mod 1` exactly on every closed 15-spin background
(`global-anomaly-leg-2026-07-20.md:9`), hostile-verified DRY with no material
revision (`verify-anomaly-closure-2026-07-20.md:10-11`). The Witten leg is
explicitly computed on **both** signatures: it vanishes by three independent
mechanisms on `(9,5)`, and *"the `(7,7)`/real side has no candidate at all
(`pi_4(SO(128)) = 0`)"* (`W232-close-a5-signature-witten-anomaly-2026-07-14.md:10-11`).
And there is **no 3-primary column** in the spin Dai-Freed ledger in any
dimension, which is why the `Z/3` generation route stays dead
(`global-anomaly-leg:9`; `lab/process/CURRENT-RESEARCH-CONTEXT.md:126-129`).

**(iii) The 14D local `I_16` leg: REQUIRES-UNKNOWN, and it is the piece that is
fork-exposed.** `CANON.md:170` holds the position open pending an explicit
`I_16 = [Ahat(TY) ch_R(F)]_16` for the actual chiral field content, and
`CORRECTION MOVE1-01` already retracted the "irreducible `Sp(64)` gauge octic"
reading as a gauge-reading artifact: under the genuine Clifford commutant the
octic is Green-Schwarz **reducible**, and the surviving non-factorizability is
**pure-gravitational / net-chirality**, nonzero solely because the assumed
truncated content has `n_+ - n_- = 1 - 14 = -13` — explicitly conditional on that
truncation.

> **FINDING (this lane), narrowed and labelled `VERIFIED_REPO_DISCONNECT`: leg
> (iii) is FORK-EXPOSED, legs (i) and (ii) are NOT.** `Sp(64) = U(64,H)` arises
> **only** from `Cl(9,5) = M(64,H)` (`shiab-existence-cl95.md:56-58`), while
> `REAL-CLIFFORD-FORM` was **settled 2026-08-04** to `Cl(7,7) = M128(R)` with an
> explicit import ban on the `(9,5)` right-H / chosen-J machinery
> (`layer0-fork-registry.yaml:88-96`). So the `Sp(64)`-specific results —
> `Omega~^spin_15(BSp(64)) = 0`, the `Sp(64)` octic analysis, and `SHIAB-04/-06`'s
> `Sp(64)`-equivariant dim-4 endpoint (which survives **if and only if** full
> `Sp(64)`-equivariance is the imposed constraint, `shiab-existence-cl95.md:86`)
> — are stated on the demoted comparator horn. **The correction to the version of
> this claim a careless reader would make: the Witten leg is NOT stranded**, since
> W232 computed both signatures on purpose, and **the SM-shadow leg is NOT
> stranded**, since W222's argument is real rep theory that does not use `Sp(64)`
> at all.

**The `SHIAB-05` transfer: already computed on the settled horn, and the canon
file does not cite it.** K77-A §2 constructs the exact real `Cl(7,7) = M128(R)`
carrier — a real `Spin`-invariant symmetric form `B = gamma_{t1}...gamma_{t7}`
with `B^2 = 1`, trace 0, signature `(64,64)`, anticommuting with `omega_14` — and
records that **same-half restrictions vanish while opposite-half pairings are
perfect** (`resolver-wave-k77a-...-2026-08-04.md:171-209`). That **is** the
`(7,7)` statement of `SHIAB-05`: no invariant scalar bilinear on
`S^+ (x) S^+`, the pairing exists only off-diagonally. The structural reason is
signature-independent — for `so(2n)` with `n = 7` **odd** the half-spin
representations are dual to *each other* rather than self-dual, so the
complexification contains no degree-zero form and the real invariant injects into
it.

> **Consequence, and it is good news for the ledger: the `SHIAB-05` exclusion is
> FORK-ROBUST.** A same-chirality Majorana scalar mass must come from an external
> spurion on **both** horns, so `SM-5`'s Majorana sub-row and the seesaw's
> external requirement survive Wave K unchanged. **`VERIFIED_REPO_DISCONNECT`:**
> `canon/shiab-existence-cl95.md`'s `SHIAB-05`/`SHIAB-06` discussion does not cite
> K77-A §2, so the canon still reads as if the exclusion were `(9,5)`-only. That
> is a one-line canon repair, not a computation, and it is reported to the register
> owner rather than made here.

**Verdict: legs (i) and (ii) exactly-same / DETERMINED; leg (iii)
REQUIRES-UNKNOWN `U11` (BV, counterterm, regulator, parity and inclusive
interfaces, all frozen at `SA-U1`), plus one fork-exposure to repair. T1** on leg
(iii) — the requirement is named and the receptacle partially typed, but no
`I_16` computation for GU's actual content exists. **Do not cite**
`papers/drafts/canonical-structures-14d-metric-geometry-2026-06-22.md:214` ("no
global Witten anomaly" from `pi_15(Sp) = Z`): that is precisely the free-versus-
torsion inference `CANON.md:170` and `ANOMALY-01` forbid.

---

## 6. What the cluster returns

### 6.1 Row count and verdict distribution — 16 term-slots, 18 graded rows

Two slots split: GR-1 / GR-1b (the Einstein-Hilbert term by two different routes)
and SM-3 / SM-3b (the fermion kinetic operator and the vertex it must carry).

| verdict class | count | rows |
|---|---:|---|
| **OVER-DETERMINED** | **3** | **GR-1b** (Einstein-Hilbert via Shiab: Bianchi + ambient-Einstein + same-action transgression admit only the zero map); **GR-4** (`R^2` scalaron: positive-norm but background-independently tachyonic, no vacuum lifts it); **SM-3b** (constraint-preservation and GU's written Shiab are jointly unsatisfiable) |
| **DETERMINED** (derived, not guessed) | **3** | **GR-2 sign** (`+1/2 g_mn`, exact); **SM-5 channel** (`Lambda^0`, `dim Hom = 1` exactly, cross-chirality, with two class-exclusions banked); **SM-9 legs (i)+(ii)** (SM-shadow perturbative conditions satisfied by real rep theory; Dai-Freed / Witten legs vanish exactly and fork-robustly) |
| **DIFFERS BY A NAMED TERM / STRUCTURALLY**, geometric origin stated | **6** | **GR-3** (Weyl/Bach), **GR-5** (augmented torsion), **GR-6** (`Q(B)` and `E^Psi` in the Einstein equation), **SM-3** (the `lambda_F` gamma-traceless vertex, ratio `-1/6` fixed), **SM-4** (the forced RS mass, `m_2,eff in [5/6, 5/4]`), **SM-6** (no separate Higgs field or potential; kinetic + quartic + breaking all inside `\|\|F_A\|\|^2`) |
| **DETERMINED GIVEN `U_k`** (same as physics in form) | **2** | **GR-1** (Einstein-Hilbert, given `U1`), **SM-1** (Yang-Mills, given `U2`, with relative coefficients FORCED by Schur and freedom exactly 1) |
| **REQUIRES-UNKNOWN `U_k`** | **4** | **GR-7** (Newton's constant), **SM-2** (coupling unification), **SM-7** (QCD theta, T0), **SM-8** (gauge-fixing / ghosts / BRST positivity) |

Six of the sixteen slots additionally carry the **wholesale** exclusion of
PRE-2: their target is a scale, and no symmetry-type condition can ever supply
it. Three further wholesale exclusions (norm-square coefficient-blindness,
CHANNEL-FORBIDDEN, FN-sterility) are collected in PRE-2.

**A note on how this distribution changed during the pass, because it is the
honest measure of the method.** Four rows moved materially once the SM-side
inventory was complete: **SM-6** moved from "the emptiest row in the cluster,
REQUIRES-UNKNOWN" to "DIFFERS STRUCTURALLY, with a parameter reduction attached"
once the `\|\|F_A\|\|^2` expansion was located; **SM-5** and **SM-9** each moved from
REQUIRES-UNKNOWN to partially DETERMINED once the forced Yukawa channel and the
W222/W232/global-anomaly-leg results were joined to them; and **SM-3b** appeared
as a third over-determination that no forward-building wave had collected.
**Three of those four moves were toward GU and one was against it**, which is the
distribution a ledger should produce if it is measuring rather than advocating.

### 6.2 The three OVER-DETERMINED rows, restated loudly

**These are the most valuable outputs of this cluster and none is softened.**

**GR-1b.** The unique product assignment that the source's own principal-Bianchi
requirement selects — `(comm, symi, symi)`, the sole nonvacuous survivor of eight
— is exactly the assignment that the source's own cubic transgression identity
forces to zero once the ambient Einstein ratio is imposed. **Three
source-motivated conditions on one object admit only the zero map.** Scope: the
displayed factorized ansatz, the joint ambient-Einstein / same-action reading, and
the **ambient** trace reversal. The `VERTICAL-FROBENIUS-TRACE` fork is
undecided, and deciding it is finite exact arithmetic on existing objects. **That
is the single cheapest way to learn whether this row is a falsification or a
fork error, and it is not currently scheduled.**

**GR-4.** GU's induced fourth-order gravity `f(R) = gamma R + beta R^2 - 2 Lambda`
has a positive-norm scalaron whose mass `M_0^2 = gamma/(6 f_0^2)` is
**background-independent by exact computation**, so with `f_0^2 < 0` it is
tachyonic at *every* background including GU's own de Sitter vacuum, and **no
vacuum selection can lift it**. Every rescue route is exactly excluded. The
escape is a **single sign**: whether GU's *native* `R^2` running gives
`f_0^2 < 0` — currently **ported**, not derived. **T4, the tightest residue item
in the cluster, and the highest-value unscheduled computation on the Einstein
side.**

**SM-3b.** The unique gamma-traceless **constraint-preserving** coupling is
`wedge - 6 . contract` (`t* = -1/6`); GU's written canon Shiab is the
Clifford-trace channel `(1,0,1,0)` and is gamma-trace**ful** (measured norm
`215.85`). `DERIVATION-PROGRESS.md:1861` states the collision without hedging:
**constraint-preservation and GU's written Shiab are jointly unsatisfiable**, and
`SHIAB-04` says the same from the other side. The candidate action has already
chosen a side — the packet's vertex carries the constraint-preserving ratio, not
the canon Shiab's — **without that choice being recorded anywhere as a choice**.
Scope: the over-determination binds only if one operator must occupy both slots,
and settling that typing is finite and cheap.

**What the three have in common, and it is the lane's method working.** None
required building the source action. Each was obtained by imposing a downstream
requirement — Bianchi, vacuum stability, constraint preservation — and
propagating it backwards onto `U3`, `U1`/`U7`, and `U3` again. **The source
action was never built and it was constrained three times.**

### 6.3 The distinct unknowns this cluster needs

**Thirteen**, `U1..U13`, all pre-existing and charged; the full table with
row-dependencies and packet line references is Section 3. Ranked by how many rows
they gate:

| rank | unknown | rows gated |
|---:|---|---:|
| 1 | **`U5`** overall imported absolute scale | 6 |
| 1 | **`U1`** the action-NORM premise `\|II\|^2` vs `\|H\|^2` | 6 |
| 3 | **`U7`** `alpha_II`, `beta_0/alpha_II` | 4 |
| 4 | **`U4`** `Z_U` block ratio (one length-squared) | 3 |
| 4 | **`U12`** `lambda_SW`, `lambda_F` | 3 |
| 4 | **`U3`** Shiab selector (now gates GR-1b, SM-3b, SM-9) | 3 |
| 7 | **`U2`** `zeta_F` YM fork (now gates SM-1, SM-2, SM-6) | 3 |
| 8 | **`U11`** BV / gauge-fixing / closure | 2 |
| 8 | **`U8`** odd-polarization coefficients | 2 |
| 10 | **`U6`**, **`U9`**, **`U10`**, **`U13`** | 1 each |

`U1` and `U5` together gate ten distinct rows of the eighteen (six each, overlapping on GR-2 and GR-7). `U1` is a **binary** — and per
`H45` it is favored-`\|II\|^2` with premise `P1` already proven and the
convention-trace route closed, leaving only the norm choice `P2`. **Resolving one
binary would move ten rows.** That is the highest fan-out item this cluster
identifies and it is not on any current queue.

### 6.4 Six cheap, decidable questions this pass generated

None introduces a new object; each is finite work on objects already in custody,
and each has an informative negative outcome.

1. **Re-run the same-action transgression condition under the observed-4D and
   vertical-Frobenius trace reversals.** Decides whether **GR-1b** is a
   falsification or a fork error. Exact finite arithmetic on existing objects.
2. **Compute GU's native `R^2` running sign.** Decides whether **GR-4** is "the
   program's first genuine no-go" or a porting artifact. **One number. T4 — the
   tightest item in the cluster.**
3. **Type the Shiab slot against the `V_GT` vertex slot.** Decides whether
   **SM-3b**'s over-determination binds. Pure Layer-0 work, hours.
4. **Compute the `14D -> 4D` bridge for the Higgs mechanism:** does a vertical
   vev of `a` convert the same-chirality `Lambda^1` channel into a 4D
   opposite-chirality Dirac mass channel? Decides whether **SM-6**'s "no Higgs"
   mechanism and **SM-5**'s forced `Lambda^0` Yukawa channel are the same physics.
   Already named as the actionable item that replaces `T10`
   (`layer0-pass-on-the-two-higgs-objects-2026-07-29.md:90-96`).
5. **File `sign(F_{A_0})` as a new `SA-*` requirements row** and classify it
   FORCED / DECLARATION / FIT. The only obligation this pass generated that the
   spec does not already carry.
6. **Repair the `SHIAB-05` canon citation** to point at K77-A §2's `(7,7)`
   carrier, so the canon stops reading as if the Majorana exclusion were
   `(9,5)`-only. One line, no computation.

Plus the one the 2026-08-04 council already named and nobody has run: **is
DC-H2's horizontal:vertical length-squared the same object as
`PRED-NORM-RANK`'s `l^2 = Z_U kappa`?** Until it is answered this ledger cannot
state how many independent scales GU imports, and that is the first number an
outside physicist will ask for.

### 6.5 Honest status

This is a ledger pass, not a build. **No term is derived here and no coefficient
is computed.** Every row is a citation plus a typed verdict; the single item
labelled `REFEREE_CONJECTURE` (the `T_9` / strong-CP conjecture in SM-7) is a
conjecture with a named certifying check and an explicitly stated adverse prior,
not a result. What the pass earns is the thing the 2026-08-04 council said was
missing and that Joe specified: a **term-level** SM-and-Einstein ledger in which
no row reads "blocked", the residue is typed rather than counted, the three rows
where GU is currently over-determined are named at full strength with their
scopes intact, and the six-row fan-out of a single open binary (`U1`) is visible
for the first time.

**Where GU looks best, said plainly because a ledger that only reports debts is
also miscalibrated.** Four rows carry real derived content: the cosmological
term's **sign** (`+1/2 g_mn`, exact); the Yukawa **channel** (unique,
`dim Hom = 1`, with two class-exclusions banked); the **relative** coefficients of
the ultralocal sector (FORCED by Schur, freedom exactly 1); and the perturbative
**anomaly** conditions (satisfied by real rep theory). Three rows carry named
differences with stated geometric origins and attached predictions — the Bach
sector, the `Q(B) + E^Psi` correction to the Einstein equation, and the Higgs
mechanism's collapse of `lambda` and `g` into one coefficient. That is not
nothing, and none of it is a fit.

**What it does not earn, stated as plainly:** GU's Yukawa sector does not reduce
the parameter count, it relocates it; its Higgs mechanism has no built object at
any step; its QCD theta slot is met by an unexamined convention against a banked
`INHERITS` posture; its 14D `I_16` leg sits on a demoted fork; and its
gauge-fixing sector, which is the *only* route to a positive physical subspace,
does not close. A residue of eight would have been a real result a competitor must
match. **This cluster's residue is not eight**, and the packet's own words are the
right summary of why: *"No constraint surplus is reported at N1."*

---

*Filed 2026-08-05. Pre-deposit, J5-gated, conditional-build lane. Creates no
claim, verdict, canon entry, count, bar, priority, lane movement, external-datum
entry, or public posture. Nothing is committed by this artifact. The three
OVER-DETERMINED rows are reports of results already filed by K77-B2, W79, and
`DERIVATION-PROGRESS` with their existing scopes unchanged; this artifact widens
none of them and files no falsification. Two `VERIFIED_REPO_DISCONNECT` items and
one new requirement-row obligation are reported to the register owner, not edited
in.*
