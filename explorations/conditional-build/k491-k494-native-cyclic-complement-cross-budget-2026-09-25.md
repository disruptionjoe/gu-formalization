---
title: "K491--K494 native cyclic-complement cross budget"
status: active_research
status_axis: operational_state
doc_type: conditional_native_k139_k162_cyclic_cross_and_full_complement_boundary
created: 2026-09-25
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K491--K494 native cyclic-complement cross budget

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders conventional particle-physics comparators. Any result about a
> standard Higgs/VEV, family index, anomaly selector or familiar vector-mass
> route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` before reuse.

Classification: INTERNAL_STRUCTURAL_ONLY.

```gu-typed-objects
result: exact K139 cyclic tail kernels, exhaustive cyclic M-orthogonal contrasts, native signed cross budgets, and the cyclic-to-full-complement extension boundary
carrier: one K162 q00 or q10 charge sector with K139 Neumann cyclic closure C generated by a zero-bath seed phi LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: physical Gram M=S* S with S=(1-G_256)^-1 ON=repository_signed_point_control
real_structure: CAR adjoint, momentum conjugation and q10/q01 signed flavor transport
grading: charge, bath-particle number, Neumann-word order and impurity parity
action_owner: repository-construction; no source-native GU action selects the polarization, couplings, extension, scalar center, state or domain
target: signed same-form cyclic cross and the full K473 complement packet MAP-TYPE=not-a-map
```

## K491: cancellation-safe tail kernels

Put `e_i=G^i phi`, `a_n=||G^n phi||^2` and
`B_j=sum_(n>=j)a_n`. Bath-number orthogonality gives

```text
<e_i,M e_j> = B_max(i,j).
```

Because the K139 base has a bath-number-preserving normal operator, writing
`E_j=sum_(n>=j)<G^n phi,W_n G^n phi>` gives

```text
R0_ij = delta_ij alpha_i + E_max(i,j).
```

The seed identity `alpha_0+w_0=0` then reduces K489's corrected-line base
cross to `E_1/B_0`. This is retained with its sign and added to K490's shape
cross; separate absolute values would destroy a possible cancellation.

## K492: exhaustive cyclic contrasts

The exact contrasts

```text
u_j=e_(j-1)-(B_(j-1)/B_j)e_j
```

are orthogonal to the seed and mutually M-orthogonal, with

```text
<u_j,M u_j>=a_(j-1)B_(j-1)/B_j.
```

Finite prefixes span the cyclic trial complement, and their closure is total
in `C intersect phi^perp_M`. K489's corrected first line is proportional to
`u_1`. This is an exhaustive result inside `C`, not inside the full K162
sector.

## K493: actual first block and rigorous tails

The native q00/q10 calculation combines four pieces on the corrected line:

1. the signed scalar base cross from K491;
2. K490's signed K168 shape cross;
3. K172's actual first normal block; and
4. K175/K176's rigorous later-order bound.

The cross is strictly negative in both sectors through the first normal block.
The available all-order estimate after order one is nevertheless too broad to
exclude zero. The exact next calculation is the signed K179/K456 normal cross
through orders 2--12, not the full 59,586-entry residual Gram. After that
finite sum, the remaining absolute cross radii are below approximately
`2.02e-4` in q00 and `1.54e-4` in q10.

## K494: cyclic is not complete

Let `N=C^perp_M`. Since `phi` belongs to `C`,

```text
phi^perp_M = (C intersect phi^perp_M) direct-sum_M N.
```

K492 coordinates the first block only. A complete K473 packet still needs a
same-form floor on `N` and an upper bound on the same-form cross between the
cyclic and noncyclic blocks. The sharp release test remains
`alpha>b`, `gamma>b`, `mu^2<(alpha-b)(gamma-b)`.

## Hostile review and boundary

- A signed cross that is negative through the first block is not proved
  nonzero after an inconclusive remainder is added.
- Cyclic exhaustiveness is not full K162 exhaustiveness.
- The K168 shape component and K491 base scalar cannot be absolutized
  separately without losing the cancellation the calculation is designed to
  test.
- A nonzero cyclic cross does not itself establish or obstruct a complete
  complement floor.

No K473 floor, K152 interval, source, ledger, canon, paper, public-posture or
physical conclusion moves.

## Reproduction

```bash
python3 tests/channel-swings/k491_k139_cyclic_tail_kernel_form.py --write
python3 tests/channel-swings/k491_k139_cyclic_tail_kernel_form_probe.py --selftest
python3 tests/channel-swings/k492_k162_cyclic_m_orthogonal_basis.py --write
python3 tests/channel-swings/k492_k162_cyclic_m_orthogonal_basis_probe.py --selftest
python3 tests/channel-swings/k493_k152_base_cross_prefix_tail_budget.py --write
python3 tests/channel-swings/k493_k152_base_cross_prefix_tail_budget_probe.py --selftest
python3 tests/channel-swings/k494_k162_full_complement_extension_boundary.py --write
python3 tests/channel-swings/k494_k162_full_complement_extension_boundary_probe.py --selftest
```
