---
title: "K91 observed unitary detector-instrument wave"
status: active_research
doc_type: reverse_scaffold_interaction_to_kraus_dilation_ownership_result
created: 2026-09-01
date: 2026-09-01
claim_ceiling: exact repository-stipulated finite system-detector unitary dilation with detector zero initialization, derived Kraus branches, effects, completeness, complete positivity, weights, conditional records and spectator-marginal invariance; no source-selected detector dynamics, derived Born rule, continuum local coupling, microlocal or Hadamard theory, prediction, confirmation, or verdict
manifest: lab/process/k91-observed-unitary-detector-instrument-wave.json
probe: tests/channel-swings/k91_observed_unitary_detector_instrument_probe.py
---

# K91 observed unitary detector-instrument wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact finite interaction-to-Kraus detector-instrument ownership control extending the K90 response-weight packet
carrier: repository-stipulated real two-level system tensor real two-level detector with detector initialized in zero LAYER=observed CHIRALITY=N/A
pairing: imported trace state-effect pairing used after the unitary-derived branch maps ON=conditional_control_only
real_structure: transpose on the exact real rational subcontrol of the standard Hermitian interface
grading: system excitation and detector record labels zero and one
action_owner: repository-construction owns only the stipulated finite unitary dilation and detector preparation
target: stipulated system-detector interaction to Kraus maps, effects, weights, normalized records and spectator-marginal law MAP-TYPE=evaluation
```

Scope: this result binds only the exact finite dilation and ownership boundary recorded in `lab/process/k91-observed-unitary-detector-instrument-wave.json`; it constructs no source-selected detector dynamics, continuum coupling, physical GU state, probability postulate, causal-local net, prediction, confirmation, export, or verdict.

## Result first

Let the system `S` and detector `D` each have basis `|0>,|1>`, order the joint
basis as `|00>,|01>,|10>,|11>`, prepare `D` in `|0>`, and stipulate the exact
joint interaction

```text
U|00> = |00>,
U|01> = (3/5)|01> - (4/5)|10>,
U|10> = (4/5)|01> + (3/5)|10>,
U|11> = |11>.                                           (1)
```

The two nontrivial columns have unit norm because `3^2+4^2=5^2` and are
orthogonal, so `U^T U=I4`. This is a stipulated finite interaction, not a
source-selected Hamiltonian or time evolution.

Reading the detector in its named basis after the interaction gives, by
`K_r=<r|_D U |0>_D`,

```text
K_0 = [[1,0],[0,3/5]],        K_1 = [[0,4/5],[0,0]],      (2)
E_0 = K_0^T K_0 = diag(1,9/25),
E_1 = K_1^T K_1 = diag(0,16/25),
E_0+E_1 = I2.                                             (3)
```

Thus the stipulated `U` plus the stipulated detector preparation derives the
two record-branch maps `Phi_r(rho)=K_r rho K_r^T`, their effects, and their
completeness. Each branch is completely positive because, for any spectator
dimension and positive `X`,
`(K_r tensor I) X (K_r^T tensor I)` is positive. Equation (3) gives
`tr(sum_r Phi_r(rho))=tr(rho)` for every trace-class matrix in this finite
control.

For the imported exact system density

```text
rho = [[9/25,12/25],[12/25,16/25]],                       (4)
```

the unnormalized branches and weights are

```text
Phi_0(rho) = (1/625)[[225,180],[180,144]],  p_0=369/625,
Phi_1(rho) = (1/625)[[256,  0],[  0,  0]],  p_1=256/625.  (5)
```

Using the imported trace/Born state-effect pairing and classical-record
interpretation, the normalized conditional records are therefore

```text
rho_0 = (1/41)[[25,20],[20,16]],
rho_1 = [[1,0],[0,0]].                                    (6)
```

Both are positive and trace one. On the exact Bell spectator control,
applying the two branches locally to `S` and forgetting the detector record
leaves the remote marginal exactly `I2/2`. More generally,
`tr_S sum_r[(K_r tensor I) rho_SR (K_r^T tensor I)] = tr_S(rho_SR)` follows
from (3). This is spectator remote-marginal invariance for the stipulated
tensor control; it does not construct source locality or a continuum causal
domain.

## Ownership accounting and retrieval boundary

The packet derives more than K90's leading response weights: the exact
unitary and detector `|0>` preparation now own the interaction-to-Kraus step,
the branch effects, their sum to identity, and the resulting finite
instrument maps. It still imports the initial system density, positivity and
trace-one state convention, the trace/Born state-effect pairing, the meaning
of a detector basis label as a classical record, and the source interaction
and locality semantics.

Retrieval found the generic dephasing/Lueders controls in
`quantum-anchor-reverse-scaffold-interface-wave` and the packet-local composite
control in `k77-observed-composite-instrument-extension-wave`. Those artifacts
already own imported instrument, marginal, dephasing and no-signalling
controls. This K91 packet is new only in repository ownership of one explicit
system-detector unitary dilation and the derivation of its Kraus operators from
that interaction plus ancilla preparation. It neither repeats their generic
claims nor promotes their imported controls into GU or source ownership.

## Inline preflight bookend

The problem-matched lens census covered unitary dilation, Kraus/Stinespring
representation, finite operator algebra, exact rational computation,
open-system instruments, state-effect semantics, compositional spectators,
operational records, causal locality, source provenance and reverse-scaffold
ownership. Exact finite dilation was selected because it closes the missing
K90 interaction-to-record seam with no numerical tolerance. The retrieval and
object-level duplicate check distinguished this object from the nearby
generic dephasing/Lueders controls: those begin with imported instruments,
whereas (1) begins with a stipulated joint unitary and derives (2)--(3).

Positive controls check unitary normalization, both Kraus extractions,
complete positivity by displayed Kraus form, completeness, exact weights,
record normalization and spectator invariance. Negative controls plant a
nonunitary rotation, omit a Kraus branch, misnormalize a conditional record,
insert a negative state or outcome, alter the spectator marginal, and promote
source, Born or held-out status. The packet has no `target_claim`, so it makes
no target-level exclusion language.

## Inline postflight bookend

- Strongest overclaim: treating a repository-stipulated finite dilation as a
  source-selected detector interaction or as a derivation of the Born rule.
- Strongest contrary construction: keep the same imported initial density but
  change the rational rotation, ancilla preparation or detector record basis;
  the Kraus maps and outcome weights change while state and trace conventions
  stay fixed.
- Weakest reproducibility seam: the exact probe certifies one real rational
  two-qubit dilation and one tensor spectator witness. It does not certify an
  arbitrary complex instrument, a Hamiltonian generator, continuum locality,
  microlocal spectrum/Hadamard conditions, or a physical GU coupling.

The narrow probe passes `24/24`; its hostile selftest catches `11/11`
interaction, completeness, state, outcome, record, spectator and promotion
mutations. The held-out delayed-choice entanglement-swapping family remains
reserved and unscored.

## Next condition

Construct a source-authenticated detector interaction and initial physical
state on the same named causal/analytic domain as the source field dynamics.
Derive its unitary or channel, record algebra and state-effect pairing there,
then test whether the finite instrument descends without importing its Born
or locality semantics.
