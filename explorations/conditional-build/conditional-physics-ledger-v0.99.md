# Conditional physics ledger v0.99

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.99.json`.

## Progress

`Ledger v0.99 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.99 adds

The two independently certified common-coordinate coefficient banks now form
one exact first-order operator:

```text
metric domain 10 direct-sum varpi domain 24 = common domain 34.
```

Under the conditional real K77 residual pairing `K_loc`, the common operator
has a canonical covector-valued formal equation dual and a nonzero local Green
concomitant. The integration-by-parts identity closes coefficientwise over
exact rationals, with a firing wrong-sign plant.

Pulling that equation dual back through the v0.98 source-native matched-q
physical metric--`varpi`--epsilon graph gives zero in timelike, spacelike and
null classes. The cancellation remains nonvacuous: deleting moving Shiab or
the lower Cartan commutator restores a rank-three defect in every class.

This closes the common raw-residual equation-dual/Green gate. It does not
construct an arbitrary primitive `D_epsilon Upsilon`, a field-valued Riesz
representative, the complete selected-action Euler/Noether identity, an
antisymmetrized presymplectic class, BFV phase space or global Green domain.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `3`; conditions opened: `0`.

Closed:

1. common 34-field metric--`varpi` covector-valued equation dual;
2. nonzero common local Green concomitant;
3. exact physical matched-q equation-dual pullback.

Remaining named conditions: `2`.

1. Compose moving action pairing/density with the already-owned physical
   epsilon Euler and preboundary owners on the matched-q graph, then derive the
   selected-action Euler/Noether identity.
2. Antisymmetrize the complete action-owned Green potential and test
   physical basicness, polarization, global domain and BFV descent.

Arbitrary primitive `D_epsilon Upsilon` remains an independent-field question
but is no longer a prerequisite for the dependent physical pullback. A
field-space Riesz map is required only if field-valued operator-adjoint
language is demanded; the covector-valued equation dual is already canonical.

P1/P2/P3 remain unchanged and unused. No residue, quotient, fork, canon,
claim-status, public-posture, Curt-track or third-lane status changes. The
selected Spin-native parent, product of the two `U(32,32)` Weyl halves and full
`U(64,64)` comparator remain distinct action-parent questions.

## Evidence

- `explorations/conditional-build/selected-k77-common-physical-equation-dual-green-2026-08-08.md`
- `lab/process/selected-k77-common-physical-equation-dual-green.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-common-physical-equation-dual-green-review.md`
