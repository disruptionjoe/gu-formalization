---
title: "K77 observed action-owned global quotient wave"
status: active_research
doc_type: reverse_scaffold_repository_owned_action_quotient_result
date: 2026-09-01
claim_ceiling: exact complete repository-owned conditional action on a named global causal slab with closed rank-1920-to-rank-960 quotient and representative-independent ordered/composite/instrument descent; no source-selected GU action, unique physical selection, prediction, confirmation, or verdict
manifest: lab/process/k77-observed-action-owned-global-quotient-wave.json
probe: tests/channel-swings/k77_observed_action_owned_global_quotient_probe.py
---

# K77 observed action-owned global quotient wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: repository-owned rank-1920 quadratic action with a closed global constraint quotient and descended full-incoming operator interface
carrier: H1 sections of the observed rank-1920 real carrier over [0,1]xT3, split by the supplied rank-960 incoming projector LAYER=conditional CHIRALITY=N/A
pairing: transported positive incoming energy H on the quotient ON=repository_owned_candidate_action
real_structure: real observed carrier and real Sobolev section spaces
grading: exact short sequence 0 to K to V to W to 0; not a nonlinear BV master complex
action_owner: repository-owned reverse-scaffold candidate built from the observed projector and downstream demands; not source-selected and not attributed to Weinstein
target: K77-PSX-1 through K77-PSX-7 representative-independent action/domain descent MAP-TYPE=construction
```

## One packet owns the full conjunction

Let `V` be the observed real rank-1920 carrier, `P:V->W` the supplied
incoming projector with `rank(W)=960`, and `K=ker(P)`, also of rank 960. On the
cooriented ultrastatic globally hyperbolic slab

```text
M = [0,1]_t x T^3,       g = -dt^2 + g_T3,
```

the direct-sum splitting gives an exact sequence on `H^1` sections:

```text
0 -> H^1(M;K) -> H^1(M;V) --P--> H^1(M;W) -> 0.
```

Write `psi=P Phi` and use the transported positive incoming energy `H`. The
repository-owned reverse-scaffold candidate action is

```text
S_1[Phi] = 1/2 integral_M (
    H(dt psi,dt psi) - sum_j H(dj psi,dj psi) - H(psi,psi)
).
```

This is a complete constant-coefficient quadratic action for this candidate,
not a placeholder for an unwritten operator. It is invariant under every
gauge shift `Phi -> Phi+kappa` with `kappa` an `H^1(M;K)` section because
`P kappa=0`. Its Euler equation is the massive wave equation on `psi`, pulled
back by `P*`.

The gauge range is closed without a compactness inference: its inclusion is
isometric and `I-P` is a bounded projection onto it. Hence

```text
H^1(M;V) / H^1(M;K)  ~=  H^1(M;W)
```

is a genuine functional quotient for this candidate. The Cauchy energy

```text
E_t = 1/2 integral_T3 (
    |dt psi|_H^2 + |grad psi|_H^2 + |psi|_H^2
)
```

is positive and conserved by the quotient evolution. This closes in one
packet the domain, coefficients, rank-1920 map, gauge complex, closed-range
estimate, propagation and positive-energy obligations that previous controls
kept separate.

## Representative-independent operator and instrument descent

Every predecessor object depends only on `P Phi`, so it is gauge-basic:

```text
rho(Phi+kappa) = |P Phi><P Phi|_H = rho(Phi),
Tr_H rho(Phi+kappa) = Tr_H rho(Phi),
```

and likewise for effects, the two labelled quotient copies, both partial
traces, the rank-one/complement local instrument, the energy-orthogonal phase,
and half-dephasing. The exact sparse certificate checks these identities on
the complete rank-960 quotient without allocating dense rank-921600 matrices.
It passes `22/22`; its hostile selftest catches `14/14` mutations.

This is the first packet in the reverse scaffold where a written action, a
named global causal domain, the full rank-1920 carrier, a closed functional
gauge image, and the full-incoming ordered interface coexist. The result is
stronger than a finite planted quotient or a conventional Maxwell control.

## The action is owned but not selected

Action ownership here is repository ownership, not historical-source
attribution. The construction deliberately uses the observed projector and
the downstream quantum demands as reverse-engineering inputs. It therefore
proves consistency of the demanded conjunction, not that Weinstein wrote or
selected it.

Nonselection is exact. Replacing the mass coefficient `1` by `4` leaves the
same domain, short exact sequence, closed gauge range, quotient carrier,
ordered state space, composites and instruments, while changing the spatial
zero-mode frequency from `1` to `2`. Thus the present downstream demands do
not select one action coefficient packet even after action/domain descent.

No nonlinear gauge algebra, BV master equation, interacting quantum field
theory, source-native coupling, unique physical quotient, observable export,
prediction or confirmation follows. Delayed-choice entanglement swapping
remains reserved and unscored.

## Next condition

The reverse edge has moved from *no complete action/quotient packet exists* to
*a complete repository-owned candidate exists but is nonunique and source-
silent*. The next decisive gate is therefore selection or falsification among
complete candidates: add one source/action-owned coefficient, nonlinear gauge
relation, interacting coupling, or independent held-out structural demand
that distinguishes the mass-1 and mass-2 packets without changing the frozen
rank-1920 quotient construction. Native certification still requires source-
owned action closure before observable export.
