---
title: "K184 order-six certified low-rank and time-Gram wave"
document_role: active_research
doc_type: conditional_native_K139_K184_order_six_finite_rank_error_and_andreief_time_gram_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned assembly of all 72 K179 order-six exchange paths into 18 coherent groups, deterministic finite-rank intervals for all 234 self/cross Gram entries, localized nonzero witnesses for every group, and an exact factorial-free Andreief reduction plus integrable rho-radialization of the remaining time Gram family on the fixed equal-coupling point control; the rank-729 pathwise-tail projection is not decision-grade, so no accurate certified order-six numerical prefix, complete action column, residual, complement or flux floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation is obtained
manifest: lab/process/k184-order-six-certified-low-rank-wave.json
solver: tests/channel-swings/k184_order_six_certified_low_rank.py
probe: tests/channel-swings/k184_order_six_certified_low_rank_probe.py
target_claim: INTERNAL_TARGET:K179_ORDER_SIX_CERTIFIED_TIME_GRAM_GATE
target_claim_verdict: ALL_72_PATHS_AND_234_GRAM_ENTRIES_ASSEMBLED__FINITE_RANK_REMAINDER_CERTIFIED_BUT_NOT_DECISION_GRADE__ANDREIEF_TIME_GRAM_REDUCTION_AND_RHO5_ORIGIN_CONTROL_PROVED__WEIGHTED_SIMPLEX_FACE_ERROR_OPEN
canon_verdict_change: none
---

# K184 order-six certified low-rank and time-Gram wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: the fixed K139/K156 hard-core `C3` impurity coupled to four
particle/hole bath species on `L2(R)`, auxiliary chart `lambda=256`, equal
couplings one, and the three normalized K162 zero-bath seed orbits. K184
consumes K179's coefficient-complete order-six family and K183's canonical
exterior-coordinate and Laplace-simplex determinant conventions. It changes
no operator, extension, state, polarization, domain or scalar center.

```gu-typed-objects
result: all 72 order-six paths form 18 coherent seed/output groups; exact old-momentum contraction and complete exterior-coordinate intervals give positive local witnesses and a certified rank-729 projection/remainder for every group and all 234 Gram entries, while specieswise Andreief integration reduces those entries to factorial-free Bessel determinant time integrals whose joint radial origin has a rho^5 majorant; the pathwise tail is too broad for action-column use and the weighted simplex-face error remains open
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on the order-six output sector; common-signature paths are paired only after complete normalized specieswise exterior projection, and the time-Gram reduction integrates the same complete exterior determinants ON=repository_signed_point_control
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, positive contracted resolvents and real Bessel determinant kernels
grading: conserved incidence charges, hard-core impurity state, bath species, bath number six, K179 path/contraction identity, contracted position, ordered momentum provenance, specieswise exterior parity and two-copy Laplace time variables
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W=(E_R-256)I-X, and K179 fixes the coefficient family; no source/GU action selects the physical extension, state, domain, polarization or scalar center
target: decide whether the first certified low-rank/time quadrature route is accurate enough for the order-six action-column prefix and leave the exact factorial-free next integrator when it is not MAP-TYPE=intertwiner
```

## Inline preflight bookend

K183 leaves a precise gate rather than an open-ended numerical task: certify
the high-dimensional integration error beginning at order six. Retrieval from
K179 gives exactly 72 paths. They occupy 18 seed/output groups with sizes
`1,2,3,4,5,7,9`; contracted positions one, three and five each occur 24 times,
and the groups require 234 unique self/cross Gram entries.

The incumbent is the next complete internal input to the K171/K168 action
columns. The strongest independently framed physics challenger remains the
state/positivity bridge (`SC-META-53`, `LT-SM8`), but it still lacks an
action-owned physical domain and state selector. `RA-F1` and `AC-F1` likewise
remain downstream of an owned observable/domain construction. The source
register and v0.263 ledger therefore remain unchanged. This internal result
cannot be transferred into physical positivity.

The route census compared direct six-dimensional momentum cubature, raw
fourteen-dimensional time cubature, tensor Gauss--Laguerre, sparse grids,
scrambled QMC, analytic contracted-resolvent coordinates, finite-rank cell
projection, species-determinant Gram integration and radialized time
coordinates. Sample agreement cannot certify an error. The first selected
proof route is a piecewise-constant exterior-momentum projection because it
can reuse exact contraction and bound both cell oscillation and the complete
tail. The predeclared switch route is an Andréief reduction of all exterior
momenta followed by a weighted radial/simplex rule.

## 1. Complete order-six family and analytic contraction

The 18 coherent group sizes have histogram

```text
size 1: 2 groups    size 2: 4    size 3: 4    size 4: 2
size 5: 2           size 7: 2    size 9: 2.
```

Every path retains its K179 coefficient, old position, output letters and
momentum provenance. For old position `r in {1,3,5}`, the remaining contracted
kernel is the positive multi-resolvent integral

In total, 72 coefficient-complete paths occupy 18 coherent output groups; all 18 coherent groups are nonzero. These statements do not yet supply the
K171/K168 action columns.

```text
int_R dp product_k (sqrt(1+p^2)+a_k)^(-1),
```

evaluated by the same recursive divided differences as K183. Independent
full-line asinh/Gauss--Legendre controls agree for all three position classes
within `1.20e-12` relatively.

Nested weighted AM--GM transports put exponent `1.05` on the contracted
energy and exponents between `0.90` and `0.95` on every exterior energy. The
contracted power exceeds one and every squared exterior power exceeds one.
Complete normalized exterior sums are formed before any interval operation;
path-norm majorants and post-projection Cauchy bounds then enclose all 234 Gram
entries. Local complete-sum boxes certify that all 18 coherent groups are
nonzero, with full-space squared-norm floors from `2.98e-94` to `3.53e-81`.

## 2. A certified finite-rank projection—and its honest failure

On the positive orthant, partition every canonical momentum axis at
`0, 0.5, 4, 32`. Reflection across the six even momentum axes supplies the
factor 64. Each group is projected onto the 729 tensor-cell indicators.
Within a cell, exact monotone endpoints of the analytically contracted kernel
give every path interval only after its complete antisymmetrizer. If `c_Q` is
the interval midpoint and `r_Q` its radius, then

```text
||P f||^2 = 64 sum_Q |Q| c_Q^2,
||1_box (f-Pf)||^2 <= 64 sum_Q |Q| r_Q^2.
```

The complement outside `[-32,32]^6` is bounded from the AM--GM product
majorant, coordinate by coordinate. For two paths `f=p+e` and `g=q+d`,

```text
|<f,g>-<p,q>| <= ||p|| ||d|| + ||q|| ||e|| + ||e|| ||d||.
```

Thus every one of the 234 Gram entries has a deterministic interval and every
group has a complete finite-rank remainder certificate.

This certificate is not decision-grade. The projected group norms range from
`2.93e-44` to `9.87e-30`, while the certified remainder squares range from
`3.20e-4` to `1.34e-2`: the pathwise tail majorant overwhelms the projection
by at least `1.36e27` and as much as `1.23e40`. The resulting group upper
endpoints are `3.20e-4` to `1.34e-2`; the positive lower endpoints come only
from the localized witnesses. This is a valid low-rank error certificate, but
it does not accurately evaluate the order-six action-column scale. Refining
interior cells cannot repair an error dominated by cancellation-destroying
pathwise tails.

The separate bounded-asinh scrambled-Sobol control gives seedwise order-six
squared norms

```text
seed 0: 7.044399344407909e-21
seed 1: 3.171384975415895e-21
seed 2: 3.120265703163414e-21.
```

Coarse/fine group differences reach `0.572`, and every fine value lies inside
the certified intervals. These values locate the likely scale and preserve
the seed-one/seed-two flavor control. They are not the outward proof and are
not used to narrow the deterministic remainder.

## 3. Exact Andréief time-Gram switch

Let `s_1,...,s_7` and `v_1,...,v_7` be the two positive Laplace parameter
sets, with cumulative times `T_i=sum_(k>=i)s_k` and
`U_i=sum_(k>=i)v_k`. For a path pair in one coherent group, integrate the six
canonical exterior momenta specieswise. Andréief's identity gives

```text
int product_a dp_a det[exp(-T_i E(p_a))] det[exp(-U_j E(p_a))]
  = m! det[2 K_1(T_i+U_j)].
```

The product of species factorials cancels the two normalized wedge factors
exactly. Therefore every Gram entry is

```text
c_t c_u (2*pi)^(-8) int_(R_+^14)
  exp(-256(sum s+sum v))
  [2K_1(T_old(t))] [2K_1(U_old(u))]
  product_species det[2K_1(T_i+U_j)] ds dv.
```

All 234 path-pair formulas are serialized. Independent literal discrete
exterior sums for multiplicities one, two and three agree with the separately
formed kernel determinants within `4.91e-14` relatively.

For the remaining time integral, put `r=sum s`, `u=sum v`,
`rho=r+u`, and `theta=r/rho`, retaining two `Delta_6` simplex coordinates.
The measure becomes

```text
rho^13 theta^6 (1-theta)^6 exp(-256 rho) d rho d theta dDelta_6 dDelta_6.
```

Each Leibniz term has eight Bessel factors. The crude small-argument bound
`K_1(z)=O(1/z)` therefore leaves `rho^13 rho^-8 = rho^5`, already integrable
without spending determinant cancellation. The radial origin is not the
blocker. The remaining proof obligation is a Duffy or explicitly weighted
rule controlling the integrable `theta` and simplex-face singularities plus
the large-variable tail. That route preserves the determinants whose
cancellation the pathwise tail erased.

## 4. Release replay

K184 certifies existence and finite Gram enclosures for the complete order-six
family, but it does not produce narrow certified values. Consequently the
accurate numerical prefix remains through order five. The K171/K168 base and
reference action columns, complete `R_ref` form-dual residual, positive
complete `M`-orthogonal complement or flux floor, separately selected scalar
center and K152 interval remain unevaluated.

No source or physics-ledger row moves. `SC-META-53` remains `UNCERTAIN`;
`LT-SM8`, `RA-F1` and `AC-F1` remain `NEEDS`. No physical/source selection,
Born derivation, prediction, confirmation, canon, paper, release or public-
posture conclusion follows.

## Inline postflight bookend

- **Strongest advance:** every order-six path and Gram entry now has both a
  deterministic finite-rank/remainder certificate and an exact factorial-free
  time-determinant representation.
- **Strongest negative result:** the cancellation-blind AM--GM exterior tail
  is 27–40 orders larger than the projection and cannot support action-column
  use, regardless of interior refinement.
- **Strongest structural switch:** Andréief integration removes all six
  exterior momenta and every wedge factorial, while radialization proves a
  `rho^5` origin majorant before determinant cancellation.
- **Strongest overclaim:** “the order-six numerical prefix is certified at the
  QMC scale.” Refused; the QMC spread is large and the deterministic interval
  is intentionally broad.
- **Strongest contrary route:** a cancellation-aware exterior tail might
  rescue the momentum projection, but the already exact time determinant is
  the more direct object because it preserves cancellation globally.
- **Weakest reproducibility seam:** the next Duffy-weighted rule must certify
  every theta/simplex face rather than merely sample away from it.

All seven admitted arcs were attempted. The census, analytic contraction,
majorants, finite-rank error, nonzero witnesses, independent control,
Andréief reduction, radial origin analysis and release replay closed. The
weighted simplex-face error remains the exact next gate. No compatible
decision-grade action-column work remains until it closes.

## Reproduction

```bash
python3 tests/channel-swings/k184_order_six_certified_low_rank.py --quick
python3 tests/channel-swings/k184_order_six_certified_low_rank.py --demo
python3 tests/channel-swings/k184_order_six_certified_low_rank_probe.py
python3 tests/channel-swings/k184_order_six_certified_low_rank_probe.py --selftest
```
