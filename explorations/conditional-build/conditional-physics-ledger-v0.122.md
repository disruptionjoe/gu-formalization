# Conditional physics ledger v0.122

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.122.json`.

## Progress

`Ledger v0.122 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Six rows move in distance/evidence only. No verdict, residue, quotient, datum,
canon or posture changes.

## What v0.122 adds

The fixed-operator first-action response from all ten metric and 91 epsilon
source directions to every `1,274` grade-two equation covector has now been
computed for timelike, spacelike and null representatives on both exact K77
branches.  Every case has the same ranks: full metric/epsilon/combined
`9/91/97`, horizontal `9/6/12`, and off-slice `4/88/89`.

Thus the frozen-Hodge/Phi/Shiab/frame/observation version of the minimum-known
`321 = 125+196` tangent leaks.  This kills the shortcut, not the total moving
construction: source geometry makes those omitted operators move, and their
derivatives may cancel the rank-89 off-slice image.  The known `1,571`
low-grade coordinates are not promoted.  First and residual-square actions,
the selected Spin parent, two `U(32,32)` halves and full `U(64,64)` remain
distinct.

## Frontier

Conditions closed: `1`; conditions opened: `1`; remaining named: `2`.

Next construct the moving metric, density, Hodge, Phi, Shiab, frame and
observation Hessian terms and test their exact sum against the rank-89
off-slice target.  Only then decide whether `321` survives or the tangent must
expand; after that construct expanded-parent Hessians.  Keep the first and
residual-square actions separate.  Only after operator completion add gauge
fixing and ghosts and test `Dmax/Dmin`, Green/Krein and coupled BV-BFV.

P1/P2/P3 remain unused.
