---
title: "K77 I1B action-owned mixed-residue independence wave"
status: active_research
doc_type: reverse_scaffold_i1b_action_owned_mixed_residue_independence_result
date: 2026-09-01
claim_ceiling: exact repository-owned punctured regular-singular covariant action and evidential-independence audit; no source-owned coupled Hessian, independent matching datum, physical cross-null bundle, prediction, confirmation or verdict
manifest: lab/process/k77-i1b-action-owned-mixed-residue-independence-wave.json
probe: tests/channel-swings/k77_i1b_action_owned_mixed_residue_independence_probe.py
---

# K77 I1B action-owned mixed-residue independence wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: action-owned regular-singular I1B connection and evidential-independence classification for its mixed-curvature selector
carrier: real degenerating Darboux two-plane on a punctured cross-null collar, embedded in the native rank-24 to rank-22 quotient jump LAYER=conditional CHIRALITY=N/A
pairing: J_u=uJ_2 with a logarithmic compatible normal connection ON=repository_owned_punctured_action
real_structure: real two-dimensional regular-singular covariant field
grading: normal logarithmic residue plus tangential symplectic generator; not a physical BV-BFV grading
action_owner: the complete local covariant derivative is repository-owned; native I1B data supply only the rank jump
target: whether joint action ownership of H,C,M supplies independent decision power between positive tangential coefficients MAP-TYPE=classification
```

## Put the residue tensors into one action

On the punctured collar `epsilon<u<1`, use

```text
J_u=uJ_2,
R=-I_2/2+C,             tr(C)=0,
A_u=R/u,
A_y=aH,                 H=diag(1,-1).              (1)
```

Write a repository-owned quadratic covariant action from
`D_u=partial_u+A_u` and `D_y=partial_y+A_y`, for example

```text
S_[a,C](z)=1/2 integral (|D_u z|^2+|D_y z|^2+|z|^2). (2)
```

The Euclidean density in (2) is a declared candidate trivialization, not a
source-selected positive physical pairing. Its role is to make every
connection coefficient part of one action/Hessian rather than supplied after
the fact.

For every two-by-two matrix `R`,

```text
R^T J_2+J_2 R=tr(R)J_2.
```

Since `tr(R)=-1`, (1) obeys the varying-Green compatibility equation

```text
partial_u J_u+A_u^T J_u+J_u A_u=0.                (3)
```

This is the predecessor's complete logarithmic residue family, now written
inside a covariant action on the punctured domain.

## The mixed residue is owned but not independent

Because the coefficients are constant in `y` and `aH` is constant in `u`,

```text
F_uy=[A_u,A_y]=(a/u)[C,H],
M=res(u F_uy)=a[C,H].                               (4)
```

In the nonnilpotent horn the invariant selector therefore returns

```text
tr(M^2)/tr([C,H]^2)=a^2.                            (5)
```

Equations (4)-(5) are exact, gauge covariant and action-owned. They are not an
independent selection of `a`: `M` is derived from the same free coefficient
whose value the ratio reports. The selector has become a consistency identity,
not a confrontation datum.

This distinction is constructive. Both `a=log(2)` and `a=log(3)` define
different repository-owned actions satisfying the same native rank jump,
Green compatibility, forced trace residue, regular-singular domain and tensor
ownership requirements. Each action returns its own inserted coefficient.
Joint ownership alone does not choose between them.

## Degenerate horns and hostile review

If `[C,H]` is nilpotent, full tensor proportionality in (4) still recovers the
inserted `a`, but trace squares vanish. If `[C,H]=0`, then `M=0` for every `a`
and even definitional recovery disappears. These are the same invariant horns
as the predecessor; action ownership does not remove them.

The strongest overclaim would say that an action-owned `M` is automatically an
independent observable. The explicit construction refutes that inference. The
strongest contrary route is an independently owned boundary, matching or
source Hessian datum `M_*` whose derivation does not contain the tested `a`;
then comparison with `[C,H]` can select or reject the candidate. The weakest
reproducibility seam is hiding the coefficient lineage when reporting the
trace ratio.

This packet does not cross `u=0`, construct a source-native coupled Hessian,
select a physical domain or positive state space, or produce an observable.
No held-out, prediction, confirmation or GU-verdict credit follows.

## Next condition

Obtain `M_*` from a source-owned coupled Hessian term or an independently
derived boundary/matching law. Record its coefficient lineage. Only then apply
the nonnilpotent trace ratio, nilpotent tensor proportionality or commuting-horn
nonidentifiability test to `log(2)` and `log(3)`.
