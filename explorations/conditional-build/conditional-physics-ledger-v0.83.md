# Conditional physics ledger v0.83

Status: append-only successor to v0.82.

```text
Ledger v0.83 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Tightness — T4×1 · T3×3 · T2×1
Scoped quotients ranked — 5
```

## What moved

Five gravity/source-action rows move only in distance, evidence and mapping
grade: `LT-GR1`, `LT-GR2b`, `LT-GR3`, `LT-GR5`, and `LT-GR6`.

v0.83 assembles the first actual source-owned block requested by v0.82:

- the horizontal `D_varpi Upsilon` block has domain/rank `24`;
- its 56 output coordinates split across Clifford grades `1/2/5` as
  `22/24/10`;
- its diffeomorphism response has rank three on timelike, spacelike and null
  representatives;
- a diagnostic metric orbit completion exists but leaves six transverse
  metric columns unselected; and
- the older rank-four metric diagnostic cannot be imported as the stationary
  Gram `g-g` block on a fixed-`epsilon` `(g,varpi)` horn.

The source action already contains `epsilon`, so the last point is a fork, not
an absolute kill. The next Build constructs the physical metric block
(including Shiab/Hodge and dependent observation-normal-jet motion) together
with the source-`epsilon` Fréchet block and proves the complete `J R=0`. Only
then should it derive `K*`, the formal adjoint and Green concomitant and form
the stationary Gram Hessian.

The printed `Xi=D_omega Upsilon` does not fill the epsilon block: it is an
exterior-covariant prolongation, not `D_epsilon Upsilon`.

## What did not move

Coverage, verdict counts, residue, tightness, five scoped quotients,
P1/P2/P3, canon, claim status and public posture are unchanged. No new field,
coefficient, datum or quotient is booked. No physical kernel, hyperbolicity,
Krein positivity, path-integral contour, BFV or particle claim is promoted.

Machine truth: `lab/process/conditional-physics-ledger-v0.83.json`.

Construction result:
`selected-k77-common-field-dupsilon-varpi-block-2026-08-08.md`.
