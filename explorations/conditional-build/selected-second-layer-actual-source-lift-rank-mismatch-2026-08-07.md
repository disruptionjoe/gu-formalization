---
artifact_type: conditional_build_correction
created: 2026-08-07
status: CONNECTION_ONLY_RANK_MISMATCH__V043_PROXY_RETRACTED_AS_ACTION_TARGET
source_return: SOURCE-CORRECTS__USE_G_VARPI_LD_NOT_COVECTOR_SLOT_PROXY__SOURCE-SILENT__SECTION_OBSERVATION_WELD
ledger: lab/process/conditional-physics-ledger-v0.44.json
canon_verdict_change: none
---

# Selected second-layer actual source-lift rank mismatch

## Result in plain English

The four-column action test from v0.43 fails before any expensive
differentiation.

The rank-four connection object used there was the old v0.32 proxy obtained by
moving only the covector slot of the tautological one-form. The repository had
already corrected that object in v0.33: when both tautological slots co-move,
their response is zero, and the independently moving source connection
`varpi` has principal diffeomorphism component `L D` of rank three, not four.

That distinction is decisive. The rank-three connection component has a
one-dimensional kernel generated by time reparametrization. The current
metric Ward load is nonzero on exactly that kernel for every nonzero rest
momentum. Every possible connection derivative annihilates the kernel input,
so no coefficients and no choice of connection action can make it cancel the
metric load.

Therefore the v0.43 diagnostic weld remains valid only on its deliberately
reduced proxy carrier. It is retracted as the target for the actual selected
action. The complete construction now needs a nonconnection part of the
diffeomorphism orbit—section/observation participation, or a separately typed
full-orbit correction—already on the missing time direction. This does not
kill the full action.

## Layer 0

| phrase | object proved here | object kept distinct |
| --- | --- | --- |
| v0.43 connection orbit | covector-slot-only `G_T` proxy, rank four | source-variable connection lift `L D` |
| tautological response | both covector and internal slots co-moving, response zero | frozen-internal-slot proxy |
| source connection | independent `varpi` in `T=varpi-B_LC(g)` | augmented torsion supplied independently |
| four columns | four diffeomorphism parameters | rank of the connection component, which is three |
| route kill | no connection-only cancellation at current principal grade | failure of the full section/observation action |
| Ward obstruction | unreduced Hessian not yet basic on one generator | BV cohomology, BFV phase space or physical quotient |

This correction also demonstrates the two-sided Layer-0 process failure: v0.43
computed a valid theorem on a proxy that a later source-corrected artifact had
already superseded as the construction target.

## Source return

The primary source assigns the first action to metric plus inhomogeneous gauge
data and types augmented torsion as an independent connection minus the
gauge-rotated Levi-Civita reference connection. The v0.33 source reinspection
therefore controls the action tangent:

```text
SOURCE-CORRECTS:
  use source variables (g,varpi), T=varpi-B_LC(g), and the complete lift;
  do not use the covector-slot-only tautological proxy as the action orbit.

SOURCE-SILENT:
  the coefficientwise section/observation response on the missing generator,
  the full nonlinear Ward identity, BV/BFV and global domain.
```

## Exact kernel obstruction

At the timelike rest covector, let

```text
D : diffeomorphism parameter -> metric variation,          rank 4
C = L D : diffeomorphism parameter -> delta varpi,         rank 3
W = H_gg D : diffeomorphism parameter -> metric Ward load, rank 4.
```

The exact kernel is

```text
ker C = span{e_0},  e_0=(1,0,0,0)^T.
```

With `s` the rest momentum-square variable,

```text
W e_0 = (
  4s(3589s-9355)/13689,
  0,0,0,
  4s(3589s-255)/13689,
  0,0,
  4s(3589s-255)/13689,
  0,
  4s(3589s-255)/13689
).
```

The greatest common divisor of the nonzero entries is `s`, so `W e_0` never
vanishes for `s != 0`. For every possible residual derivative `J_varpi`,

```text
(J_varpi C)e_0 = 0 != -W e_0.
```

Equivalently, stacking the row spaces raises rank from `rank C=3` to
`rank(C;W)=4`. Thus the equation `W+J_varpi C=0` is inconsistent without
fitting or assuming any pairing sign.

## What v0.43 still proves

The v0.43 rational left-inverse construction is correct for its rank-four
proxy `G_T`. It proves a diagnostic completion exists on that reduced carrier.
It does not prove that the actual independent connection realizes those four
directions. v0.44 appends this disposition; it does not rewrite v0.43.

## Specialist and hostile review

- **Differential geometry:** the complete source-variable lift replaces the
  frozen tautological slot; only the principal timelike result is claimed.
- **Representation theory:** the kernel action, not the nominal four-column
  count, decides solvability.
- **Variational PDE:** this is a principal-symbol route kill; nonlinear and
  lower-order full-orbit terms remain separately typed rivals.
- **Symplectic geometry:** one failed radical generator blocks reduction, but
  does not construct or refute a presymplectic/BFV quotient.
- **Krein/operator theory:** the kernel contradiction is independent of
  positivity, energy sign and analytic domain.
- **Source criticism:** source architecture selects `(g,varpi)` and corrects
  the proxy; it is silent on the missing nonconnection coefficients.
- **Repo archaeology:** the decisive v0.33 correction already existed and was
  not propagated into v0.43's queue.

Both hostile charges fired. The summary may not call all of
section/observation false, and the lane may not continue computing four
obsolete proxy columns merely because their diagnostic weld exists.

## Progress and next gate

```text
Ledger v0.44 — 82/82 active rows mapped (100%)
32 SAME · 19 DIFFERS · 25 NEEDS · 6 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped

headline_delta: none
frontier_conditions_closed: 3
  - actual independent-connection diffeomorphism rank = 3
  - missing connection kernel direction = time reparametrization
  - connection-only cancellation is impossible on that direction
frontier_conditions_opened: 0
remaining_named_conditions: 4
  - actual section/observation diffeomorphism response on e_0
  - combined selected-Upsilon naturality on the complete orbit
  - scalar and massless constraint quotient
  - coupled fermion Hessian and common domain
```

Next construct the actual section/observation diffeomorphism tangent on `e_0`
and test whether its selected-`Upsilon` response supplies the missing nonzero
column. A failure there either requires an explicitly typed lower-order/full-
orbit term or kills this selected second-action owner more broadly.

No scalar pole, coefficient, external datum or fifth quotient is added.
P1/P2/P3 remain unused. Curt remains formally separate and no third lane is
promoted.

## Verification

`tests/channel-swings/selected_second_layer_actual_source_lift_rank_mismatch_probe.py`
passes `34/34`, including exact predecessor replays and planted failures
against proxy reuse, full-action overreach and external-datum substitution.
