---
title: "K354--K355 order-eight face-program instantiation"
status: exploration
claim_verdict: internal_numerical_control_only
date: 2026-09-23
claim_ceiling: complete exact approach-program map for all reachable order-eight faces plus rigorous hardest-boundary/interior Arb controls in all eighteen hybrids; preconditioned face intervals, recursive coverage, analytic tails, Peano integrals and every downstream scientific claim remain open
manifests:
  - lab/process/k354-order-eight-face-program-compiler.json
  - lab/process/k355-order-eight-hardest-face-arb-bank.json
producers:
  - tests/channel-swings/k354_order_eight_face_program_compiler.py
  - tests/channel-swings/k355_order_eight_hardest_face_arb_bank.py
probes:
  - tests/channel-swings/k354_order_eight_face_program_compiler_probe.py
  - tests/channel-swings/k355_order_eight_hardest_face_arb_bank_probe.py
---

# K354--K355 order-eight face-program instantiation

Classification: `INTERNAL_NUMERICAL_CONTROL_ONLY`.

K354 converts K352--K353 from a template and power-counting interface into an
executable program bank. Every one of K350's 517 reachable face instances
retains all 2,400 ordered descriptors. Their 1,240,800 descriptor-face programs
contain 4,876,344 determinant-matrix uses; every use resolves to one of K352's
34 singular assignment-dual templates and one of its 38 confluent divided-
difference templates. The compiler also records K353's exact normal power,
three positive face-local approach levels and a deterministic tangential
anchor. K350 has no reachable zero/coalescence face for `v8` or `v9`, so those
two hybrids correctly receive positive-interior fallbacks rather than invented
face programs.

K355 executes the complete coherent 180-digit Arb second-directional evaluator
on the hardest reachable face in each of the first sixteen hybrids and the two
positive-interior fallbacks. At `rho=1/1024,1/2048,1/4096`, all 54 controls
have strictly positive cumulative-argument floors and finite raw, K353-scaled
and K349 Peano-majorized intervals. Every control contains all 2,400 ordered
descriptors and a digest of all 23 coherent groups. Normal scaling and the
Peano majorant are applied only after complete assembly.

The first attempted approach levels (`1/64,1/128,1/256`) were rejected before
publication because they were not face-local relative to the fixed `1/256`
tangential anchor. The accepted levels are all strictly below that scale. The
accepted bank is still a control bank, not a limit proof: one hardest-face
sequence can pass while another face or a positive-width interval fails.

## Boundary and continuation

The next exact dependency is a real K352 row/column-scaled and confluent Arb
interval evaluator on positive-width face cells. It must extend from the
eighteen selected control programs to all 517 reachable faces, then join them
to a recursive positive-interior cover and analytic radial tails. Only after
all eighteen K348 Peano integrals close may their remainder join K345.

SC-META-53 remains `UNCERTAIN`; RA-F1, LT-SM8, LT-GR6b and AC-F1 remain
`NEEDS`. No source, ledger, canon, paper, public or physical posture changes.

K354 passes `11/11` controls and rejects `12/12` hostile mutations. K355 passes
`10/10` controls and rejects `11/11` hostile mutations.
