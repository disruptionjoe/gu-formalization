---
title: "K364--K366 order-eight multiscale face transition"
status: exploration
claim_verdict: internal_structural_only
date: 2026-09-23
claim_ceiling: exact scalar-nesting obstruction, zero-safe scaled Bessel envelopes through derivative order eight, and bivariate determinant Newton fronts; the zero-inclusive interval determinant, uniform strip majorant, recursive cover, tails and complete hybrid integrals remain open
manifests:
  - lab/process/k364-order-eight-nested-face-dual-obstruction.json
  - lab/process/k365-order-eight-confluent-bessel-envelope-bank.json
  - lab/process/k366-order-eight-bivariate-determinant-newton-fronts.json
producers:
  - tests/channel-swings/k364_order_eight_nested_face_dual_obstruction.py
  - tests/channel-swings/k365_order_eight_confluent_bessel_envelope_bank.py
  - tests/channel-swings/k366_order_eight_bivariate_determinant_newton_fronts.py
probes:
  - tests/channel-swings/k364_order_eight_nested_face_dual_obstruction_probe.py
  - tests/channel-swings/k365_order_eight_confluent_bessel_envelope_bank_probe.py
  - tests/channel-swings/k366_order_eight_bivariate_determinant_newton_fronts_probe.py
target_claim: INTERNAL_TARGET:K152_COEFFICIENT_COMPLETE_BASE_ACTION_COLUMN
target_claim_verdict: MULTISCALE_FACE_INTERFACE_CLOSED__ZERO_INCLUSIVE_INTERVAL_DETERMINANT_OPEN
canon_verdict_change: none
---

# K364--K366 order-eight multiscale face transition

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: these results bind only the repository-owned conditional order-eight
positive-Fock coefficient family and its K348 cubature remainder. They do not
construct a GU physical state, interacting quotient, source-owned action,
observable or K152 decision.

```gu-typed-objects
result: twelve of 146 comparable K352 singular-template pairs obstruct monotone optimal scalar-dual nesting; zero-safe scaled 2*K1 derivative envelopes now reach order eight; exact bivariate determinant Newton fronts retain the required two-scale coupling for every pair
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), restricted to the K162 zero-bath seed orbits LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive Hilbert Gram pairing after complete normalized specieswise exterior projection PAIRING-TYPE=hilbert
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering and real Bessel kernels
grading: conserved incidence charges, impurity seed, bath species, bath number eight, K179 contraction identity and ordered left/right momentum provenance
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W and K179 fixes the coefficient family; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: construct the multiscale determinant interface required for a zero-inclusive interval bound on K363-owned face strips without discarding determinant or coherent cancellation MAP-TYPE=intertwiner
```

## Preflight bookend

K363 supplies exact disjoint ownership for every low-coordinate pattern, but
the accepted K352 optimal scalar duals are face-local. Reusing one scalar dual
down a nested face flag is legal only if optimal duals can be chosen
coordinatewise monotonically. The alternative is to retain the determinant's
permutation-level exponents at both scales. K364 exhausts that discriminator
before a numerical boundary evaluator is attempted.

The same nested-face composition can differentiate a row divided difference
three times, a column divided difference three times and the active Peano axis
twice. The zero-safe primitive bank must therefore reach order eight; K349's
orders zero through two are insufficient for this gate.

## K364: scalar nesting obstruction

K364 enumerates all `146` same-rank entrywise-comparable pairs among K352's
`34` singular templates. `134` pairs admit coordinatewise monotone optimal
binary scalar duals. The remaining `12` admit neither binary nor integral-
gauge monotone optimal duals. Every forced scalar repair overextracts exactly
one inner power. K353's minimum face-normal exponent is zero, so that invented
inverse power is unsafe. This rejects naive scalar-dual nesting, not the
determinant, face integral or order-eight route.

K364 also fixes the maximum scaled-kernel derivative demand:

```text
maximum row divided-difference order       3
maximum column divided-difference order    3
active Peano derivative order              2
required scaled 2*K1 derivative order      8
```

## K365: zero-safe order-eight derivative bank

For `m=0,...,8`, K365 gives exact rational envelopes for

```text
Phi_m(w) = w^(m+1) * abs((2*K1)^(m)(w))
```

at cumulative-argument widths `1/64`, `1/16`, `1` and `4`. The continuous
zero limits are `2*m!`, namely
`2, 2, 4, 12, 48, 240, 1440, 10080, 80640`. All `36` rational bounds contain
their `72` positive 180-digit Arb controls, and orders zero through two replay
K349 exactly. No raw Bessel function is evaluated at zero. This is a compact-
width primitive bank; analytic unbounded tails remain separate.

## K366: bivariate determinant Newton fronts

K366 retains, for every determinant permutation, its coupled outer singular
exponent and inner increment. It compiles exact bivariate exponent supports
and Pareto-maximal Newton fronts for all `146` comparable pairs, including all
`12` K364 obstructions. The front-size histogram is
`{1: 60, 2: 81, 3: 5}`; the largest front has three points and the bank has
`22` unique signatures. No occurrencewise absolute value or false common
scalar exponent is introduced.

## Postflight bookend

K364, K365 and K366 each pass `9/9` controls and reject `10/10` hostile
mutations. The strongest overclaim is that the bivariate fronts and primitive
Bessel bounds already form a uniform strip majorant. They do not: the next
gate must combine each K366 front point with K365 and K352's confluent factors
inside one zero-inclusive closed-strip interval determinant while preserving
same-permutation two-scale coupling. Only then may the recursive positive
interior, analytic radial tails and all eighteen K348 hybrid integrals close.

SC-META-53 remains `UNCERTAIN`; RA-F1, LT-SM8, LT-GR6b and AC-F1 remain
`NEEDS`. No source claim, physics-ledger row, canon, paper, public or physical
posture changes.
