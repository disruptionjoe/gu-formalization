---
title: "K477 — K152 Multilevel Complete-Complement Tree"
status: active_research
status_axis: operational_state
doc_type: conditional_multilevel_same_form_complement_floor
created: 2026-09-25
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K477 — K152 multilevel complete-complement tree

Classification: INTERNAL_STRUCTURAL_ONLY.

```gu-typed-objects
result: recursive K473 composition on a complete binary block tree
carrier: one complete K162 complement partitioned into M-orthogonal leaves LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Gram M=S* S on the same fixed K139/K162 form ON=k162_complete_complement
real_structure: CAR adjoint, momentum conjugation and K162 signed flavor transport
grading: binary tree depth and exhaustive block ownership
action_owner: repository-construction; native leaf and cross certificates absent
target: K463 complete M-orthogonal-complement floor beta MAP-TYPE=not-a-map
```

## Opening bookend

K473 proves one two-block lower theorem. A practical complete complement may
need several finite slices and one analytic tail, so the theorem must compose
without losing completeness or silently changing forms. K477 makes that
recursive certificate explicit.

## Theorem and control

At every internal node, use the certified lower floors of its two children
and that node's cross-norm upper bound in K473. Require the same fixed form,
an exhaustive M-orthogonal partition and the strict target margin at every
node. Induction then certifies the root floor.

The four-leaf rational control has leaf floors `3,6,4,7`, child cross norms
`2,2`, and root cross norm `1/2`. Its two child floors are exactly `2` and
`3`; the root has strict margin product `2`, cross square `1/4`, and an
outward lower floor above target `1`. All three internal nodes pass.

## Closing bookend

- Strongest advance: an arbitrary finite hierarchy can preserve the exact
  same-form K473 logic and expose every completeness dependency.
- Strongest overclaim: a certified tree manufactures its leaf data. Refused;
  every native leaf floor, cross norm and exhaustive partition remains input.
- Strongest contrary construction: failure at any internal strict margin
  invalidates the root release even if all leaves individually exceed target.
- Weakest seam: one missing block or a form change breaks the induction.

No native K162 packet or K152 interval is emitted.

## Reproduction

```bash
python3 tests/channel-swings/k477_k152_multilevel_complement_tree.py
python3 tests/channel-swings/k477_k152_multilevel_complement_tree_probe.py
```
