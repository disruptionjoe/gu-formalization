---
artifact_type: construction_correction
created: 2026-08-13
status: ENDPOINT_FROZEN_OBSTRUCTION_RANK_ROBUST__COEFFICIENTS_CORRECTED__PURE_FRAME_DPPLUS_NOT_CONNECTION_COMPLETION
lane: "1"
functional_channels: [BUILD, COMPOSE, SOURCE, VERIFY]
source_return: SOURCE_CORRECTS_RESIDUAL_LOCUS__SOURCE_SILENT_QB_AND_FIELD_TO_FRAME_ADAPTER
ledger_change: none
target_claim: NONE-NOT-A-KILL
scripts:
  - tests/channel-swings/selected_k77_i2b_endpoint_frozen_compatibility_adapter_probe.py
---

# Selected K77 I2B endpoint frozen-compatibility and adapter gate

## Result first

The preceding frozen-compatibility theorem was exact for the object it
computed, but that object was the one-third path-average residual inside the
released first action—not Weinstein's separately printed endpoint residual.
This wave ports the entire frozen Hessian to the printed endpoint while
holding the same conditional fixed-`H_q` pairing and selected K77 carrier.

The correction changes the mathematics substantially without changing the
rank-level verdict:

```text
endpoint H0:                           symmetric rank 196, support 558
endpoint H1_mu:                        0,0,0,0
endpoint minus surrogate H0:           rank 193, support 544
endpoint degree-one defect ranks:      14,14,14,14
endpoint combined defect:              rank 56, support 160
endpoint minus surrogate defect:       rank 56, support 156
```

Thus the frozen rank-56 obstruction is robust under the source-residual
correction, but almost none of its coefficients may be inherited from the
surrogate. The endpoint result remains conditional on the repository's fixed
pairing. The source-owned bosonic primalizer `Q_B` is still unconstructed, so
this is not the complete source second action.

The second result prevents a tempting false repair. The already-built moving
projector `dot P_+` also has rank 56, but it is a pure metric-trace/frame
derivative acting on a 392-real residual carrier. The compatibility defect is
a `56 x 196` map on independent connection variations. Since

```text
q_g = g/2,
delta_A g = 0,
```

on the source's independent connection translation, `D_A q_g=0`; the chain-
rule pullback of the known `dot P_+` family into this connection block is
zero. Equal rank is not an adapter. A nonzero contribution would require a
genuine `A/T`-dependent coefficient or an action-derived tangent graph such
as `delta g=L delta A`, neither of which presently exists.

## Source-locus correction

Three nearby objects must remain separate:

```text
Fbar      = F_B + 1/2 D_B T + 1/3 T wedge T        (inside I1B)
Upsilon_p = Shiab(F_A) + * kappa T                 (printed endpoint)
E_act     = actual Frechet-adjoint derivative of I1B
```

The old frozen probe inherited the first object's one-third derivative. The
source-collision pass had already withdrawn its attribution as the literal
`SC-ACT-04` residual. This port uses the second object's unit endpoint
coefficient. The third object's complete second Hessian remains a separate
repo-composed rival.

Within the inherited fixed pairing, at `r=kappa=1` the endpoint restricted
branch is `rho=-1`, so the residual is the same nonzero null `H_q` as in the
surrogate comparator. Its arbitrary-connection derivative is different:

```text
D Upsilon_p[delta A]
 = Shiab(delta A wedge T + T wedge delta A) + * delta A.
```

The resulting endpoint gradient has fourteen diagonal cells: value `8` on
indices `0,...,11`, then `+1,-1` on `12,13`. This differs from the surrogate's
`8/3,...,8/3,+1,-1` before the Hessian is even formed.

## Exact endpoint compatibility

The principal ten-block symbol is residual-independent. Its `(00)` and `(01)`
blocks still have cumulative ranks `182,196`, so the degree-two completion
equations force the constant correction `C0=0`.

The endpoint first/lower cross blocks vanish exactly. The degree-one equations
are consequently the fourteen row blocks `C_mu H0_endpoint`. Each has rank
fourteen and forty nonzero coefficients; their direct sum has rank fifty-six.
The endpoint and surrogate defect maps differ in 156 coefficients with a
rank-56 difference. This is rank-profile robustness, not equality of actions
or operators.

## The rank-56 adapter trap

The two rank statements live on different structure fingerprints:

| object | domain/codomain | variational altitude | owner |
| --- | --- | --- | --- |
| endpoint compatibility defect | `F_A,196 -> T*X tensor C^14`, displayed as `56 x 196` | frozen connection Hessian | conditional fixed pairing |
| `dot P_+` | endomorphism of a 392-real residual carrier | pure normalized-trace/frame orbit | metric-owned `q_g=g/2` |

The current field tangent is a direct sum of connection and metric/section
directions. On the independent translation `delta B=0, delta T=alpha`, the
metric and its tautological trace do not move. Therefore every one of the 196
connection columns has zero coefficient in the thirteen-dimensional trace
orbit. The existing `dot P_+` can still contribute on metric/section
variations or after a separately derived field-to-metric graph; it simply
cannot be inserted here because its rank happens to match.

## Layer 0 and structure fingerprint

| field | inherited value | open alternative |
| --- | --- | --- |
| carrier | ordered 196-real connection tangent; 196-real equation dual | physical carrier/projector |
| pairing | repository fixed-`H_q` Hodge/Clifford-trace comparator | source `Q_B` |
| real structure | trace-`H_q`, selected real K77 | another action-owned real form |
| grading | connection one-form to degree-thirteen residual | full unitary/odd parent |
| signature horn | conditional source-aligned `(7,7)` | none selected here |
| embedding | selected K77 fixture | full `U(64,64)` or block-preserving parent |
| variational altitude | local frozen arbitrary-connection Hessian | complete moving stationary Euler/BV complex |
| globalization | pointwise/local exact | atlas descent, global domain, observation quotient |

The source carrier `C^(32,32)+C^(32,32)`, its block-preserving subgroup, full
`U(64,64)` parent and the independent connection fields remain distinct.

## Source return

The source confirms the one-third path-average inside the first action, the
separately printed unit endpoint, augmented torsion as a connection
difference, and the residual-square/adjoint grammar. It does not supply the
repository fixed pairing as `Q_B`, the endpoint Hessian above, a field-to-
frame tangent graph, or a completed nonlinear Noether/BV identity.

```text
SOURCE-CORRECTS: residual locus used by the previous frozen chain
REPO-DERIVES:    exact endpoint port under the inherited fixed pairing
SOURCE-SILENT:   Q_B, field-to-frame adapter and covariant compatibility
```

## Hostile scope and accounting

The hostile review returns `SCOPED_SURVIVAL`. It rejects three overreads:

- the endpoint port is not the full source action because `Q_B` is open;
- rank-profile agreement is not coefficientwise equality;
- the pure-frame rank-56 projector derivative is not a connection-field
  completion without an adapter.

No field, coefficient, selector, quotient, external datum, P1/P2/P3 use,
ledger verdict, residue count, canon verdict or public posture changes.

## Next gate

Do not spend the next wave composing the two rank-56 objects directly. Build
one of the only typed exits:

1. a genuine `A/T`-dependent moving `Q_B`/Shiab/source-connection coefficient;
2. an action-owned field-to-metric tangent graph, then pull back every moving
   coefficient through it; or
3. the corrected `E_act` second Hessian as a separate action-consistent rival.

Only then apply the fourteen compatibility rows coefficientwise. A surviving
defect after all appropriate owners are present would become the first genuine
local obstruction.
