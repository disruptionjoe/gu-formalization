---
title: "K93 observed action-soldering selection wave"
status: active_research
doc_type: reverse_scaffold_action_soldering_selection_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K92_ACTION_OWNED_SPATIAL_SOLDERING_DISCRIMINATOR
claim_ceiling: exact finite normalization discrimination for one fixed action-net pairing and covariance under simultaneous action/net conjugation; no source-selected action or net, unique physical locality, continuum AQFT or microcausality, microlocal or Hadamard theory, derived Born law, prediction, confirmation, held-out score or verdict
manifest: lab/process/k93-observed-action-soldering-selection-wave.json
probe: tests/channel-swings/k93_observed_action_soldering_selection_probe.py
---

# K93 observed action-soldering selection wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact site-factor normalization discriminator for one fixed action pulse across the standard and K92 CZ-primed tensor solderings
carrier: selected P8=(C2) tensor (C2) tensor (C2) subspace of the K91 physical l2 term LAYER=observed CHIRALITY=N/A
pairing: Pauli multiplication and finite factor membership ON=repository_owned_action_soldering_control
real_structure: computational-basis conjugation, with real Hadamard and controlled-Z pulses
grading: three site labels; no source BV, BFV or ghost grading
action_owner: repository-construction
target: selection of a site-algebra soldering by exact normalization under a fixed action MAP-TYPE=evaluation
```

Scope: this result binds only the two finite K92 tensor nets and the discrete
action pulses below. It proves that a fixed action can distinguish those nets;
it does not prove that this pulse is physical, source-selected or uniquely
selective among all spatial nets.

## Inline preflight bookend

The problem-matched lens census covered finite factor nets, Clifford
normalizers, discrete quantum dynamics, Hamiltonian versus step-unitary
ownership, algebraic covariance, causal-net selection, source custody,
counterexamples and simultaneous-conjugation controls. The decisive route was
exact Pauli normalization: it asks directly whether one fixed action preserves
each candidate local factor, rather than fitting another state or record
table.

Object-level retrieval found K88's discrete causal action, K91's functional
causal control, and both K92 nets. It found no packet testing a fixed action
against both solderings while also conjugating the action as a covariance
control. Hadamard, controlled-Z and finite Pauli-normalizer facts receive no
novelty claim. Positive controls check both nets, fixed-action normalization and
simultaneous conjugation. Negative controls collapse the nets, replace the
pulse, omit conjugation and corrupt the membership witness.

## Two nets and one fixed action pulse

Retain K92's standard factors

```text
A0=alg(X0,Z0),   A1=alg(X1,Z1),   A2=alg(X2,Z2),          (1)
```

and its `W=CZ_01`-conjugated factors

```text
A0'=alg(X0 Z1,Z0),   A1'=alg(Z0 X1,Z1),   A2'=A2.        (2)
```

Now add one discrete-time action owner, held fixed across the comparison:

```text
R=H0 tensor I1 tensor I2.                                  (3)
```

The pulse is a product unitary and normalizes every standard factor. On site
zero it exchanges `X0` and `Z0`; it fixes the other site algebras. Thus

```text
R Ai R*=Ai                                                   (4)
```

for `i=0,1,2`.

The same fixed pulse does not normalize the primed net. The shortest exact
witness is

```text
Z0 in A0',       R Z0 R*=X0,       X0 not in A0'.          (5)
```

The last exclusion is exact: the Pauli basis of `A0'` is
`{I,Z0,X0 Z1,Y0 Z1}`. Equation (5) is therefore a genuine action/net
discriminator, not a comparison of names.

## The simultaneous-conjugation control

Define the conjugated pulse

```text
R'=W R W*.                                                   (6)
```

Then, for every `a` in `Ai`,

```text
R' (W a W*) R'^* = W (R a R*) W* in W Ai W*=Ai'.           (7)
```

So `R'` normalizes the entire primed net. The two covariant pairs

```text
(A,R)       and       (A',R')                                (8)
```

are equally valid finite controls. What fails is the cross-pair `(A',R)`.
Hence the selected datum is the relation between a fixed action and a net, not
the eight-dimensional Hilbert carrier by itself.

## Maximum licensed conclusion

One action-owned discrete pulse distinguishes the two K92 tensor solderings by
exact local-factor normalization. This closes K92's specific question whether
additional dynamics can discriminate the frozen nets: yes, once a fixed action
is supplied. Simultaneous conjugation of both action and net restores
covariance, so the result neither chooses the action nor proves unique physical
locality.

The pulse is repository data. It is not Weinstein's action, a Hamiltonian
density derived from GU, a continuum causal automorphism, an AQFT covariance
law or a microlocal state selector.

## Inline postflight bookend

- Strongest overclaim: calling normalization under one chosen pulse a unique
  physical-locality theorem. Many actions and nets remain untested, and the
  simultaneous-conjugation control leaves an equivalent primed pair.
- Strongest contrary construction: `R'=W R W*` normalizes the primed net
  exactly. This proves that action custody, not abstract Hilbert data, is the
  load-bearing selector.
- Weakest reproducibility seam: the complete three-qubit Pauli basis is
  exhausted exactly, but the step unitary is stipulated and finite. No
  continuum generator or source variational principle is constructed.

The exact probe passes its complete control set and its hostile selftest
mutates net distinction, pulse ownership, covariance, source custody and every
promotion fence. No source, continuum, microlocal, Born, prediction,
confirmation, canon, paper or held-out status moves.

## Next condition

Replace the stipulated pulse by an authenticated source interaction density,
causal evolution or functor to local algebras. Test its normalization and
covariance on the physical quotient. Only that owner can turn this finite
availability result into source-selected locality.
