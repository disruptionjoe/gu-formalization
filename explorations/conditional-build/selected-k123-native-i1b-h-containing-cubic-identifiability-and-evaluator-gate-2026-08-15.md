---
title: "Selected-K123 native I1B h-containing cubic identifiability and evaluator gate"
status: active_research
doc_type: exact_serialization_identifiability_obstruction_and_evaluator_contract
created: "2026-08-15"
registry: lab/process/selected-k123-native-i1b-h-containing-cubic-identifiability-and-evaluator-gate.json
probe: tests/channel-swings/selected_k123_native_i1b_h_containing_cubic_identifiability_and_evaluator_gate_probe.py
grade: "THE WRITTEN I1B ACTION WOULD FIX C_THH AND C_THV, BUT THE CURRENT SERIALIZED BANK DOES NOT. EXACT PLANTED SAME-ACTION MOVING GERMS PRESERVE STATIONARITY, THE COMPLETE QUADRATIC GERM, D3_TTT=8736, C_TVV=-(56/3)<V,*V>, AND THE NATIVE COORDINATE CANCELLATION WHILE SHIFTING C_THH AND C_THV BY AN ARBITRARY R2 PAIR. THE MAP FROM THE TWO MISSING MOVING JETS TO THOSE TWO NATIVE COEFFICIENTS HAS RANK TWO. THE THREE K122 SOURCE-PRIMITIVE SLOTS ARE ALSO COORDINATE-SPLIT PIECES: LINEAR AND QUADRATIC CONNECTION REPARAMETRIZATIONS REDISTRIBUTE THEM WHILE LEAVING THE TWO NATIVE SUMS EXACTLY INVARIANT. NUMERICAL EVALUATION THEREFORE REQUIRES ONE COMMON FULL-14D I1B MOVING-COEFFICIENT EVALUATOR, NOT ANOTHER PARTIAL LC INSERTION OR WARD FIT."
target_claim: K122_NEXT_GATE__EVALUATE_THREE_PRIMITIVE_I1B_TENSOR_SLOTS_COEFFICIENTWISE
target_verdict: CURRENT_SERIALIZED_BANK_HAS_RANK_TWO_NATIVE_COEFFICIENT_DEFICIT__COMMON_FULL_14D_I1B_EVALUATOR_REQUIRED
canon_verdict_change: none
---

# Selected-K123 native I1B h-containing cubic identifiability and evaluator gate

> **GU-COMPARATOR-ROUTING — scope before inference.** This is a source-native
> parameterized-torsion, moving-gimmel, variational and preboundary question.
> Ordinary Higgs/VEV, family-index, net-chirality, anomaly, symmetry-breaking
> and familiar four-dimensional gauge-model constructions do not adjudicate
> it. Read `lab/methods/source-native-comparator-routing.md` before reuse.

Classification: `SOURCE_NATIVE_ROUTE`.

## Result in plain English

K123 attempted the three coefficient evaluations requested by K122. The
action formula is not the problem. The repository has not yet put every
metric-dependent ingredient of that formula into one executable 14D tensor
contract.

This matters in a provable way. Let the two still-open native coefficients be

```text
A = C_t_h_h,
B = C_t_h_v.                                          (1)
```

The current exact bank fixes stationarity, the entire quadratic germ, the
native coordinate map, `D3_ttt=8736`, and
`C_t_v_v=-(56/3)<V,*V>`. It nevertheless permits the planted missing moving
germ

```text
Delta I = (a/2)t h^2 + b t h v.                       (2)
```

Equation (2) changes no already-certified datum and sends

```text
(Delta A,Delta B)=(a,b).                              (3)
```

The exact deficit therefore has rank two. This does **not** say the source
action contains two free coefficients. It says the source action has not yet
been completely instantiated in the repository, so its missing tensor jets
cannot be replaced by zero, by the old `14/3` partial, or by a Ward-compatible
fit.

K123 also corrects the work target. The three K122 primitive source-coordinate
evaluations are useful implementation pieces, but they are not individually
native invariants. Connection-coordinate changes redistribute those three
pieces while preserving the two native sums (1). The next evaluator must
compute the native sums directly and expose its primitive split only as a
checksum.

## 1. Layer-0 packet

| name | exact object | not identified with |
| --- | --- | --- |
| `I1B` | the printed first transgression action | `I_sc`, `I2B`, or observer `I_II` |
| `A,B` | native bulk cubic coefficients `C_t_h_h,C_t_h_v` | primitive source-coordinate summands |
| planted `a,b` | witnesses for absent serialized moving tensor jets | new physical couplings or action freedom |
| `O_K123` | required common full-14D moving-`I1B` evaluator | a fitted Ward completion |
| preboundary representative | Cartan one-form before domain/reduction | physical BFV charge |

The source-native fork is mandatory because `T=varpi-B_LC(g)` is the action's
actual connection-difference coordinate. The conventional particle routes
are not nearby substitutes.

## 2. Exact rank-two native deficit

Write the already fixed cubic control as

```text
I_known = (8736/6)t^3 -(56/6)N t v^2 + terms of degree <=2,   (4)
```

where `N=<V,*V>`. Add (2). At the stationary origin:

- every first derivative of `Delta I` vanishes;
- every second derivative vanishes, so the complete free/quadratic pencil is
  unchanged;
- `D3_ttt` and `D3_tvv` are unchanged;
- setting `h=0` removes `Delta I` identically; and
- `D3_thh Delta I=a`, `D3_thv Delta I=b`.

The Jacobian from `(a,b)` to `(A,B)` is the `2 x 2` identity. Thus neither
native `h` coefficient is identifiable from the serialized lower-order and
fixed-metric evidence. Two completions can agree on every admitted datum and
disagree on either coefficient.

This is an evidence-sufficiency theorem, not a modification of `I1B`. In the
actual action, the missing derivatives of `F_B`, `D_B T`, Shiab, Hodge,
pairing, density and the chimeric frame determine `a,b`.

## 3. Why the three primitive slots are not the final target

K122's scalar source-coordinate control gives

```text
A = C_rgg + 2 s C_rgw + s^2 C_rww + b2 H_rw,
B = C_rgw + s C_rww,
C = C_rww.                                             (5)
```

Under the linear connection-coordinate change `w'=w+c g`,

```text
s'     = s+c,
C_rgw' = C_rgw-c C_rww,
C_rgg' = C_rgg-2c C_rgw+c^2 C_rww.                    (6)
```

Substitution proves `A'=A`, `B'=B`, `C'=C`, although the two primitive
third-derivative entries in (6) move. Under the nonlinear change
`w'=w+(d/2)g^2`,

```text
b2'    = b2+d,
C_rgg' = C_rgg-d H_rw,                                (7)
```

so the primitive `D3I` and `D2I[D2F]` pieces of `A` trade exactly while their
sum is unchanged.

Therefore K124 should not report three isolated numbers as though each were a
physical interaction. It should report native `A,B`, then show the three
primitive pieces recombine according to (5) in the frozen source chart.

## 4. Serialization inventory

| packet | current exact grade | consequence |
| --- | --- | --- |
| printed `I1B` formula and selected algebraic Shiab | owned | fixes the functional architecture |
| invariant stationary line and fixed-metric cubic | owned | fixes `8736` and `-56/3` |
| abstract source/native map and selected 4D spin-LC first/second jets | owned on their stated slice | proves coordinate cancellation and chain support |
| first moving 14D gimmel/DeWitt metric jet | owned | TT density first derivative is zero; Hodge/frame form one packet |
| full composed 14D spin-LC/curvature two-jet | not serialized in the common action basis | blocks the `F_B` part of `A` |
| derivatives of selected Shiab/Hodge/Krein pairing and frame, composed with the same fields | not serialized as one action evaluator | blocks both `A` and `B` |
| compatible background field jets for `D_B T` | not serialized in the same shell/basis | blocks derivative-term coefficients |
| native Cartan current on a selected Green/domain convention | open | blocks reduced preboundary class |

The existing 24-dimensional LC carrier is the horizontal `4 x Lambda2(4)`
slice. It is an exact slice, not the full 1,274-dimensional
`C* tensor Lambda2 C` connection carrier of the 14D action. K120's abstract
coordinate custody survives; numerical full-action sufficiency does not
follow from that slice.

Observation is not an extra native bulk summand. It is used only after the
native `I1B` coefficients have been computed, to translate or descend them.

## 5. Required evaluator contract `O_K123`

The next construction must instantiate, in one common labelled K77 basis:

1. the induced `DG,D2G` of the full chimeric metric from the TT base metric;
2. the corresponding full 14D spin-Levi-Civita `DB_LC,D2B_LC`, curvature
   and covariant-derivative jets;
3. `D,D2` of Shiab, Hodge, the Krein/pairing primalizer, density and the
   co-moving frame without double-counting Hodge/frame motion;
4. the stationary background `T_*`, TT directions and compatible `D_B T`
   field jets in one momentum/polarization convention;
5. direct scalar-action differentiation, returning native `A,B,C`; and
6. the Cartan/Green current before any BFV reduction.

Acceptance controls are exact:

- reproduce `D3_ttt=8736` and `C_tvv=-(56/3)N`;
- reproduce `delta T=0` on the native metric graph and reject the old `14/3`
  import;
- make the source-coordinate primitive split obey (5)--(7);
- satisfy the same-action Ward columns without choosing the 21-dimensional
  quotient freedom by fit; and
- preserve the K122 preboundary pullback columns before domain reduction.

## 6. Preboundary ambiguity

The K122 pullback columns are exact, but a Cartan representative can shift by
`delta K` when a total derivative `dK` is added to the Lagrangian. For the
control `K=gamma t h v`, the three columns shift by

```text
gamma h v,  gamma t v,  gamma t h,                    (8)
```

while the field-space exterior derivative of `delta K` is zero. Thus the bulk
cubic cannot select an unreduced preboundary representative or a physical
charge. The printed action convention, integrations by parts, Green domain,
boundary conditions and BFV descent must be carried together.

## 7. Route decision and reverse scaffold

| route | disposition |
| --- | --- |
| import the old `14/3` LC value | rejected by K122 native cancellation |
| use Ward solvability to fit `A,B` | rejected; Ward leaves quotient dynamics free |
| import `I_sc`, `I2B` or `I_II` | rejected by action custody |
| infer zero from absent serialized tensors | rejected by the rank-two planted family |
| compute only three primitive source pieces | implementation checksum, not final invariant target |
| build `O_K123` and differentiate the scalar action directly | selected |

```text
R0 target: numerical native C_t_h_h and C_t_h_v plus Cartan current
R1 complete: source/native map and fixed-metric cubic
R2 complete: rank-two evidence deficit; neither h coefficient is selected
R3 complete: primitive three-slot split is source-chart dependent
R4 complete: common evaluator contract O_K123
R5 next: K124 builds O_K123 and returns native A,B with primitive checksums
R6 then: test whether the resulting full first-order pencil is unique
R7 then: spectral owner and common Green/domain gate
R8 later: BFV reduction and 2D-to-98D attachment
```

No ledger, datum, quotient, canon, public posture, particle interpretation,
phenomenology or GU truth-status claim changes. Joe input is not required.

Exact probe: `40/40`.

Reproduce:

```bash
PYTHONDONTWRITEBYTECODE=1 ./_local/cas-venv/bin/python \
  tests/channel-swings/selected_k123_native_i1b_h_containing_cubic_identifiability_and_evaluator_gate_probe.py
```
