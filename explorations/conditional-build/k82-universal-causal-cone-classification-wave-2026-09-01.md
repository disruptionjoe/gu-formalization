---
title: "K82 universal causal-cone classification wave"
status: active_research
doc_type: reverse_scaffold_common_causal_cone_classification_result
date: 2026-09-01
claim_ceiling: exact constant-coefficient common-cone necessity, sufficiency and nonselection theorem on the repository-owned rank-960 observed quotient family; no source full-carrier symbol, dynamical metric, universal stress coupling, prediction, confirmation, or verdict
manifest: lab/process/k82-universal-causal-cone-classification-wave.json
probe: tests/channel-swings/k82_universal_causal_cone_classification_probe.py
---

# K82 universal causal-cone classification wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact common-causal-cone necessity, sufficiency and nonselection theorem for the complete observed quotient action class
carrier: H1 sections of the repository-owned real rank-960 quotient W on [0,1]xT3 LAYER=conditional CHIRALITY=N/A
pairing: positive temporal form H and independently variable positive spatial stiffness K ON=repository_owned_candidate_family
real_structure: real observed quotient and real Sobolev section spaces
grading: the existing K-to-V-to-W short exact sequence and minimal abelian BV grading; neither selects the principal stiffness
action_owner: repository-owned reverse-scaffold candidate class built from the observed quotient and downstream causal demands; not source-selected or attributed to Weinstein
target: common characteristic cone and universal-coupling demand on every quotient mode MAP-TYPE=classification
```

## Exact common-cone theorem

Retain the complete observed quotient `W` and its positive temporal form `H`.
Allow the spatial principal form to be another positive form `K`; equivalently,
let

```text
C = H^(-1) K
```

be positive and `H`-self-adjoint. The constant principal action on the fixed
ultrastatic slab is

```text
S_pr[psi] = 1/2 integral (
  <dt psi,dt psi>_H - sum_i <di psi,C di psi>_H
).
```

Diagonalizing `C` in an `H`-orthonormal basis gives one scalar principal factor
for every eigenvalue `c_j>0`:

```text
p_j(tau,xi) = -tau^2 + c_j |xi|^2.
```

Therefore the mode speed is `sqrt(c_j)`. The complete family has one
universal causal cone if and only if all eigenvalues agree, equivalently

```text
K = c H.
```

If the background slab metric has already fixed the unit light cone, matching
that cone requires the stronger normalization `C=I`. The positive two-mode
control `C=diag(1,4)` has speeds `1` and `2`, so positivity and hyperbolicity
alone permit two distinct cones. This witness is an exact failure of universal
coupling inside the same quotient carrier, not a source-level GU counterexample.

## Existing action structures do not select the cone

The direct-summand gauge complex, closed quotient, abelian BRST differential
and formal minimal classical master equation depend on `P:V->W`, not on `C`.
They survive for every positive `C`. So does the conserved positive energy

```text
E_C = 1/2 integral_T3 (
  |dt psi|_H^2 + sum_i <di psi,C di psi>_H
  + m2 |psi|_H^2 + lambda/2 |psi|_H^4
),
```

for `m2>0` and `lambda>=0`. The mass and quartic terms are lower order and do
not enter the characteristic polynomial. Thus quotient closure, positive
energy, minimal BV closure and genuine interaction do not force `C` to be
scalar and do not select one universal cone.

This is the requested class-level necessity result: any future candidate that
claims universal coupling must own an independent identity tying its spatial
principal form to its temporal form on every physical quotient sector. Merely
reusing the same slab metric in a repository definition builds the answer in.

## Hostile boundary

The strongest overclaim would be to call `K=cH` a GU result. It is only the
necessary and sufficient condition in a frozen constant-coefficient quotient
class. The strongest contrary construction is the positive `diag(1,4)`
two-speed witness. The weakest propagation seam is the absent source-owned
full-carrier symbol and dynamical metric or total-stress map. The result does
not classify variable coefficients, birefringent systems with nontrivial
Jordan structure, nonlinear gravitational backreaction or physical gauge
cohomology.

This is not a source-owned full-carrier GU causal cone or universal stress
coupling. It supplies no prediction, confirmation or held-out credit.

## Next condition

Derive the actual source/action-owned full-carrier principal symbol, its
physical quotient and its dynamical metric or total-stress coupling. Then test
whether the normalized spatial operator is scalar—and unit-normalized—on every
physical sector, rather than stipulating a common cone in the candidate action.
