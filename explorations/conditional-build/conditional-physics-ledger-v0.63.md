# Conditional physics ledger v0.63

## Meter

```text
Ledger v0.63 — 82/82 active target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue — 84 continuous + >=19 function-valued + 9 forks
Quotients ranked — 4 scoped
```

## Migration from v0.62

The exact K77 graph output is now correctly typed.  It is a Hodge/primalized
exterior **one-form**, not the source-printed degree-thirteen `Upsilon`
density.  At the fixed background, applying Hodge restores the degree-thirteen
density.  Only then does the source relation `Xi=D Upsilon` land in degree
fourteen.

The four nonzero source-side `Xi` columns have exact supports `16,15,11,11`
and family rank four.  But after the total `Upsilon` residual cancels, total
`Xi` cancels identically and adds rank zero.  This confirms the source's
printed on-shell redundancy on the exact graph; it does not license importing
that printed pair into the action-owned noncyclic Euler derivative.

An independent invariant-trace covariant Green identity passes with nonzero
unrestricted flux and zero Dirichlet flux.  This locates the formal boundary
owner without yet supplying the moving K77 Hodge/Krein equation dual or an
antisymmetrized reduced symplectic class.

Five rows migrate in distance, evidence and mapping grade only: `LT-GR1`,
`LT-GR2b`, `LT-GR3`, `LT-GR5`, `LT-GR6`.  Verdicts, residue, quotient count,
P1/P2/P3, canon and public posture are unchanged.

## Frontier

```text
headline_delta: none
frontier_conditions_closed: 3
frontier_conditions_opened: 0
remaining_named_conditions: 2
```

Next derive the degree-fourteen companion from the actual Frechet-adjoint
Euler derivative, then move the Hodge/Krein observation map and
antisymmetrize the resulting Green current before any reduced symplectic,
common-domain or BV/BFV claim.

Machine truth: `lab/process/conditional-physics-ledger-v0.63.json`.
