---
title: "K93 observed causal-instrument composition wave"
status: active_research
doc_type: reverse_scaffold_causal_instrument_composition_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K92_CAUSAL_PROPAGATION_RECORD_COMPOSITION
claim_ceiling: exact finite causal propagation/instrument composition on one repository-selected three-site factor; no source-selected dynamics or detector, continuum AQFT or microcausality, curved-spacetime or microlocal or Hadamard theory, derived Born law, prediction, confirmation, held-out score or verdict
manifest: lab/process/k93-observed-causal-instrument-composition-wave.json
probe: tests/channel-swings/k93_observed_causal_instrument_composition_probe.py
---

# K93 observed causal-instrument composition wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact nearest-neighbor propagation composed with the K92 middle-site record instrument, including causal input effects and ordered weights
carrier: selected P8=(C2) tensor (C2) tensor (C2) subspace of the K91 physical l2 term, plus one binary detector LAYER=observed CHIRALITY=N/A
pairing: imported finite trace state-effect pairing on the K92 rational diagonal state ON=repository_owned_causal_instrument_control
real_structure: computational-basis conjugation preserved by the real CNOT propagation and controlled record dilation
grading: three site labels, one time-step order and a binary detector record; no source BV, BFV or ghost grading
action_owner: repository-construction
target: causal past-neighborhood record effect and quotient-basic instrument composition MAP-TYPE=quotient
```

Scope: this result binds only the selected finite tensor factor, K92 rational
state, one nearest-neighbor reversible step and its middle-site record. It does
not bind a GU source carrier, continuum local net, microlocal state,
measurement postulate, prediction or verdict.

## Inline preflight bookend

The problem-matched lens census covered Clifford propagation, reversible
cellular automata, Heisenberg support, causal past neighborhoods, process
ordering, exact rational states, projective instruments, no-signalling,
gauge-basic descent, AQFT locality and source custody. The smallest decisive
composition is one `CNOT_0->1` step followed by the already-defined K92 middle
record. It makes the detector's input support and ordering effect exact without
inventing a continuum cone.

Retrieval found K88's action-pulled detector supports, K91's unitary-to-Kraus
instrument and K92's static local record table. It found no packet composing a
propagator with the K92 state and instrument to derive a pulled-back record
effect and new exact weights. CNOT and projective measurement receive no
novelty claim. Positive controls check bijectivity, radius, effect pullback,
weights, endpoint invariance and quotient descent. Negative controls mutate
each separately.

## A one-edge reversible propagation step

On K92's three-site computational basis, define

```text
V=CNOT_0->1,
V:(s0,s1,s2) -> (s0,s1 xor s0,s2).                         (1)
```

This is an involutive permutation and hence a unitary. Its Heisenberg action
contains

```text
V* X0 V = X0 X1,             V* Z1 V = Z0 Z1.              (2)
```

Thus information from site zero reaches site one in one step, while site two
is fixed. Equation (2) is an exact radius-one finite support law, not continuum
microcausality.

## Compose K92's middle detector after propagation

Let the output detector measure `Z1` with effects `P_b`. The composed input
effects are

```text
E_b=V* P_b V=(I+(-1)^b Z0 Z1)/2.                            (3)
```

The output-local record therefore has the exact input past neighborhood
`{0,1}`. Completeness is preserved:

```text
E_0+E_1=I.                                                  (4)
```

For K92's rational state

```text
p(s)=2^(number of aligned adjacent edges)/18,               (5)
```

the static middle record has weights `(1/2,1/2)`. After (1),

```text
(Pr(0),Pr(1))=(2/3,1/3).                                    (6)
```

The order therefore matters: measuring before propagation and measuring after
propagation are distinct instruments on the same initial state. Equation (6)
is an exact control consequence of the stipulated state and circuit, not a
physical prediction.

The propagated diagonal state additionally has

```text
<Z0>=0,      <Z1>=1/3,      <Z2>=0,
<Z0 Z1>=0,  <Z1 Z2>=0,      <Z0 Z2>=1/9.                    (7)
```

Because (1) changes only the middle bit, the full endpoint joint marginal is
unchanged, not merely the site-two marginal. The K91 zero extension over the
gauge summand commutes with both the propagation and record maps, so the
composition remains representative-independent.

## Maximum licensed conclusion

A finite action-owned nearest-neighbor propagation law can be composed with
the K92 local detector to derive its causal input support, complete record
effects, order-dependent exact weights and remote endpoint invariance. This is
the missing dynamic composition immediately above K92.

The circuit, initial state, trace/Born pairing, detector preparation and record
meaning are supplied. Kraus/effect completeness does not derive any of them.
The control supplies no source interaction, continuum AQFT net,
curved-spacetime Green operator, microlocal spectrum or Hadamard state.

## Inline postflight bookend

- Strongest overclaim: promoting the one-edge support identity to continuum
  microcausality or the exact weights to a GU prediction. Both the state and
  circuit were selected to build the control.
- Strongest contrary construction: reversing the order restores the K92
  `(1/2,1/2)` record table, so the weights are not invariant data of the state
  or detector alone.
- Weakest reproducibility seam: the eight configurations and all marginals are
  exhausted exactly, but the discrete step is stipulated and the physical
  trace/Born interpretation remains imported.

The exact probe passes its complete control set and its hostile selftest
mutates locality, bijectivity, effects, weights, remote invariance, descent,
source custody and promotion fences. No source, continuum, microlocal, Born,
prediction, confirmation, canon, paper or held-out status moves.

## Next condition

Replace the stipulated circuit by an authenticated source causal evolution and
derive the physical state and detector coupling on the same quotient. Test
whether its local-algebra functor selects a K92 soldering and whether the
state-effect pairing is derived or remains an additional owner.
