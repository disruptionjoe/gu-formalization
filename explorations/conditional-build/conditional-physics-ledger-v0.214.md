---
artifact_type: conditional_physics_ledger_summary
created: 2026-08-12
ledger: lab/process/conditional-physics-ledger-v0.214.json
status: CURRENT_APPEND_ONLY_LEDGER_V0_214
---

# Conditional physics ledger v0.214

```text
Ledger v0.214 — 82/82 target rows mapped (100%)
32 SAME · 19 DIFFERS · 26 NEEDS · 5 OVER-DETERMINED
Residue: 84 continuous + >=19 function-valued + 9 discrete forks
Tightness: T4x1 T3x3 T2x1 · scoped quotients: 5
Headline delta: none
Frontier: 4 conditions closed · 1 sharper ownership gate opened · 2 remain
```

## What changed

The v0.213 rank-two kinetic defect is pairing-relative, not an absence of a
four-direction derivative response. On the same exact four-real grade-two
response, a phase-even conjugation-sensitive primalizer gives

```text
8 diag(1,-1,-1,-1) tensor I4,
```

so the principal symbol is rank four off the null cone.

This does not yet complete the source action. The already action-owned
`P_+` and `P_-` sectors stay rank two. Exact traces on the two real Weyl halves
agree for every live grade-two product, so two scalar `C^(32,32)`-half weights
also cannot exceed rank two. Finally, an exact `U(1,1)` plant changes the
phase-even value from `1` to `1681/81`, proving that the candidate is not
automatically invariant under the noncompact block or full unitary parent.

## Scoped migrations

- `RA-E1`: a conditional pairing-only rank-four repair now exists; action-
  owned moving reduction/fundamental symmetry or coupled contact remains open.
- `RA-E3`: the allowed one-form carrier has a full finite principal candidate,
  but action ownership, scalar descent and Yukawa placement remain open.
- `LT-SM6`: the restricted potential and a conditional rank-four kinetic form
  coexist; preboundary, domain and physical spectrum remain open.

No verdict, residue, fork, quotient, parameter, selector, field or external
datum changes. P1/P2/P3 remain unchanged and unused.

## Layer-0 carrier fence

Keep distinct: source `C^(32,32)+C^(32,32)` carrier halves; their derived
`U(32,32)xU(32,32)` subgroup; the full `U(64,64)` parent; and independent
connection fields. The equality of the two half traces on the live grade-two
sector is not a statement that the halves are the same object.

Also keep distinct: source norm glyph, typed `Q_B`, phase-even pairing,
positive Hilbert majorant, moving fundamental symmetry, coupled contact term
and expanded total-residual parent.

## Next gate

Construct or kill an action-owned moving fundamental symmetry/reduction that
transports the phase-even `Q_B` covariantly. Keep the coupled
metric/section/gauge contact parent as the independent repair. Require rank
four plus observation/gauge basicness and an analytic domain before any Higgs
spectrum or physical-vacuum claim.
