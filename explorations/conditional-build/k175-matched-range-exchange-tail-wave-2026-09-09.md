---
title: "K175 matched-range exchange-tail audit wave"
document_role: active_research
doc_type: conditional_native_K139_K175_matched_diagonal_tail_scalar_metric_resummation_and_exchange_lift_obstruction_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: exact repository-owned partial action-tail closure for the K139--K174 signed point control; the K172 matched resolvent difference and K174 quarter-graph contraction give an all-order diagonal Pauli/spectator tail, while the scalar minus-256 core resums through M=S* S with an explicit finite Neumann error, but K174 forbids lifting the same componentwise estimate to the four isolated exchange blocks and the complete cross-exchange cancellation identity and sector-uniform CAR estimate remain absent, so the complete action column, R_ref residual, complement or flux floor, K152 interval, physical/source selection, Born derivation, prediction and confirmation remain open
manifest: lab/process/k175-matched-range-exchange-tail-wave.json
solver: tests/channel-swings/k175_matched_range_exchange_tail.py
probe: tests/channel-swings/k175_matched_range_exchange_tail_probe.py
target_claim: INTERNAL_TARGET:K174_MATCHED_RANGE_COMPLETE_EXCHANGE_TAIL_SHORTCUT
target_claim_verdict: DIAGONAL_AND_SCALAR_TAILS_CLOSED_COMPONENTWISE_EXCHANGE_LIFT_INVALID
canon_verdict_change: none
---

# K175 matched-range exchange-tail audit wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: this packet stays on the repository-supplied K139--K174 equal-coupling
two-edge positive particle/hole point control at auxiliary chart `256` and the
exact normalized K162 zero-bath seed orbits. It separates the matched diagonal
Pauli/spectator coefficient from the four exchange coefficients. It does not
claim a global graph bound for `W`, select a physical extension, polarization,
state or scalar center, or move source truth.

```gu-typed-objects
result: the matched K172 resolvent difference has a range-specific quarter-energy bound and closes the diagonal all-order seed-orbit tail; the scalar core resums exactly through M=S* S; the proposed componentwise four-exchange lift is invalid because its isolated point traces remain unbounded and no complete cross-term cancellation formula is serialized
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space over L2(R;C4), in q=(0,0),(1,0),(0,1), with fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing transported by S=(1-G_256)^-1 to M=S* S; A_(1/4)=(dGamma(omega))^(1/4)+N+1 controls the diagonal matched multiplier only ON=repository_signed_point_control
real_structure: CAR adjoint, momentum conjugation, Hermitian normal-ordered bath blocks and the K168 real flavor-symmetric reference shape
grading: conserved incidence charges, hard-core impurity degree, bath-particle number, free-energy weight, Neumann word order and K162 level
action_owner: repository-construction -- K156 owns the coefficient-complete normal-ordered core, K172 identifies the diagonal first-block multiplier, and K174 proves isolated exchange traces cannot be graph-bounded; no source/GU action selects an extension, state, domain or scalar center
target: close every justified scalar and diagonal action-column tail, audit the attempted matched exchange lift, and expose the exact coefficient-level premises still required by K171 MAP-TYPE=intertwiner
```

## Inline preflight bookend

K174 leaves a deliberately narrow opening: a complete coefficient may cancel
even when each exchange factor is unbounded. The strongest cheap candidate is
to reuse K172's matched resolvent difference at every word order, combine the
four polarities, and attach K174's quarter-graph contraction. Before making
that inference, this wave traces which normal-ordered coefficient K172
actually computed.

The route census included the matched diagonal difference, cross-exchange
cancellation, sector CAR row bounds, scalar-core resummation, the K171
post-left-adjoint tail, the K168 residual and the complete complement/flux
floor. `SC-META-53` remains `UNCERTAIN`; `LT-SM8`, `RA-F1` and `AC-F1` remain
`NEEDS`. The source register and v0.263 physics ledger do not move.

## 1. The matched difference is finite, but it is diagonal

For `a=omega(k)+256` and external energy `e>=0`, subtraction before the
regulator is removed gives

```text
1/(a+e)-1/a=-e/[a(a+e)],                              (1)
D_256(e)=integral_R e/[(omega(k)+256)
                      (omega(k)+256+e)] dk/(2 pi).     (2)
```

K172 proves

```text
0<=D_256(e)<=(1/pi)e^(1/4)<=(1/3)e^(1/4).            (3)
```

This is a genuine range-specific smoothing identity. It belongs to the
diagonal Pauli/spectator contraction remaining after the matched self-energy
subtraction. It is not a formula for each of K156's four polarity exchange
blocks, whose normal ordering leaves a point annihilation trace.

## 2. The diagonal seed-orbit tail now closes

Let

```text
A=A_(1/4)=(dGamma(omega))^(1/4)+N+1,
q_D=131/300,                 q_H=3/8.                 (4)
```

K174 gives `||A G^n phi||<=q_D^n||A phi||`; either normalized K162
zero-bath seed has `||A phi||=1`. The diagonal multiplier (3) occurs with
multiplicity at most two by K172. Therefore its complete order-`n` vector
obeys

```text
||X_diag,n G^n phi|| <= (2/3) q_D^n.                  (5)
```

After resolving through order `N`, K171's left adjoint Neumann sum gives

```text
T_diag(N)<=(2/3) q_D^(N+1)/[(1-q_D)(1-q_H)].          (6)
```

At `N=1`, the exact rational bound is

```text
T_diag(1)<=68644/190125.                              (7)
```

Equations (5)--(7) are all-order native bounds for the named matched diagonal
coefficient, not for the complete `X` or `W`.

## 3. The componentwise four-exchange lift is invalid

The attempted shortcut would assign the bound `1/pi` from (3) to each of four
exchange polarities and conclude

```text
||X_ex,n G^n phi|| <= (4/pi) q_D^n.                   (8, invalid)
```

That inference changes the operator being bounded. K174 isolates a nonzero
exchange output whose coefficient contains the unsmeared point trace
`f -> integral f`. It is unbounded on the quarter graph. Thus no one of the
four exchange factors has the `1/pi` estimate needed for a triangle sum, and
equation (8) is invalid.

This does not prove that the complete coefficient diverges. It proves that a
positive result must first write the complete all-order coefficient after all
matched subtractions, show the exact cross-term cancellation identity, and
derive a sector-uniform CAR sum-norm estimate. K172 supplies only the diagonal
first-block multiplier; K156 supplies qualitative coefficient convergence on
fixed cylinders; neither serializes those all-order identities. The companion
compiler rejects any certificate missing them.

## 4. The scalar core resums exactly through the metric

K156 fixes

```text
W=-256 I-X,                   S=(1-G)^-1,
R=H0+S* W S.                                          (9)
```

The scalar contribution is therefore exactly `-256 M`, where `M=S* S` is the
K168 positive metric. For `S_J=sum_(j=0)^J G^j` and `q_H=3/8`,

```text
||S-S_J|| <= q_H^(J+1)/(1-q_H),
||M-S_J* S_J|| <= 2 q_H^(J+1)/(1-q_H)^2.             (10)
```

Hence the finite scalar action-column compiler has the explicit error

```text
||-256(M-S_J* S_J)||
 <=512 (3/8)^(J+1)/(1-3/8)^2.                         (11)
```

At `J=12`, this is

```text
1594323/419430400 < 1/250.                            (12)
```

This closes the scalar tail without expanding `W_n G^n phi` term by term.
It does not evaluate `M phi` in the native K170 coefficient basis.

## 5. K171 and K168 release replay

The action column now has two convergent certified pieces: the exact scalar
metric contribution (9)--(12) and the matched diagonal tail (5)--(7). The
complete exchange contribution is still missing, so the full K171 column
cannot be assembled numerically. Without that column, K168's complete
`R_ref` form-dual residual and a positive complete `M`-orthogonal complement
or flux floor remain unevaluated. No K152 interval follows.

## Inline postflight bookend

- **Strongest construction:** the diagonal matched coefficient has the exact
  geometric tail (6), while the scalar core is represented exactly by
  `-256 M` with finite error (11).
- **Strongest route correction:** `D_256` is a diagonal self-energy difference,
  not a bound for any isolated exchange trace; the `4/pi` exchange shortcut is
  invalid.
- **Strongest scope control:** K174's isolated exchange obstruction is
  preserved while coefficient-level cancellation remains possible.
- **Strongest continuation:** serialize the complete four-exchange coefficient
  before taking its norm, then prove its cross-term cancellation and a
  sector-uniform CAR estimate.
- **Strongest overclaim:** “the complete K171 action column is convergent.”
  Refused; only its scalar and diagonal pieces have certified tails.
- **Weakest reproducibility seam:** native exchange coefficients beyond the
  first word are not written in a common cancellation-preserving normal form.

All five admitted mathematical arcs were attempted. The matched diagonal and
scalar-resummation arcs closed; the intended complete exchange lift was killed
at its operator-typing step; the complete residual and complement replay
failed closed on that missing exchange column. Physical extension and
scalar-center selection remained authority-excluded. No source, ledger, Born,
prediction, confirmation, canon, paper, release or public-posture truth moves.

## Next condition

Write the complete all-order four-exchange coefficient on each exact K162 seed
orbit after matched subtraction. Prove the cross-term cancellation identity
and a sector-uniform CAR sum-norm estimate before taking any component norm.
Then add that tail to the now-closed scalar and diagonal pieces, evaluate the
complete K171 action column and `R_ref` residual, and only then replay the
complete complement or flux floor. Keep scalar-center selection separate.

## Reproduction

```bash
python3 tests/channel-swings/k175_matched_range_exchange_tail.py --demo
python3 tests/channel-swings/k175_matched_range_exchange_tail_probe.py
python3 tests/channel-swings/k175_matched_range_exchange_tail_probe.py --selftest
```
