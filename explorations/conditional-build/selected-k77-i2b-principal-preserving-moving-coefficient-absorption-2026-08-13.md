---
artifact_type: construction_composition_result
created: 2026-08-13
status: PRINCIPAL_PRESERVING_MOVING_COEFFICIENT_TORSION_UNIVERSALLY_ABSORBED_THROUGH_QUARTIC__PRINCIPAL_CHANGES_AND_PHYSICAL_BV_OPEN
ledger_rows: [RA-E1, RA-E3, LT-SM6]
target_claim: NONE-NOT-A-KILL
source_return: SOURCE_CONFIRMS_SC_ACT_04_RESIDUAL_SQUARE_AND_TWO_CONNECTION_GRAMMAR__SOURCE_SILENT_UNIVERSAL_COEFFICIENT_JET_ABSORPTION
canon_verdict_change: none
fork_assumed: none
free_object_delta: 0
scripts:
  - tests/channel-swings/selected_k77_i2b_principal_preserving_moving_coefficient_absorption_probe.py
---

# Principal-preserving moving-coefficient torsion absorption

## Result first

The first moving-coefficient gate does not require the program to expand every
known coefficient term separately in order to test cubic or quartic formal
torsion.

The preceding exact certificate proved that the higher-jet absorber restricted
to the relevant prolonged-symbol kernel is onto the **entire** compatibility
target at both tested orders:

```text
cubic:   rank([P3; A3]) - rank(P3) = 910 - 770   = 140 = dim T3
quartic: rank([P4; A4]) - rank(P4) = 2184 - 1904 = 280 = dim T4.
```

Therefore every cubic `T3` or quartic `T4` representative has zero class—not
only the particular fixed-coefficient representative already computed. This
includes representatives produced by first spatial jets of algebraic moving
coefficients, provided their base values preserve the same endpoint principal
tableau and compatibility target bundle.

That scope covers the already-built:

- nonzero scalar rescalings along the fixed-natural `Q_B` trace/Hodge line;
- co-moving metric/Hodge/Clifford/Phi/Shiab transport;
- fixed-`varpi` metric Levi-Civita and augmented-torsion lower-order response;
- source-normal real-`u` contact values once they enter as lower-order terms;
- regular invertible observation-receiver coefficient jets.

It follows that none of those packets, by itself, can create a surviving cubic
or quartic formal obstruction on the exact compatible endpoint stratum.

## Exact linear-algebra reason

Let

```text
P : V -> W
A : V -> T
```

be the prolonged principal map and lower-order absorber. The induced map on
allowed higher jets is `A|ker(P)`. Elementary exact linear algebra gives

```text
rank(A|ker(P)) = rank([P; A]) - rank(P).
```

Thus the rank difference equalling `dim(T)` is equivalent to surjectivity of
the absorber on the **constrained** higher-jet kernel. The probe includes an
exact fixture and a deficient planted control. Quotienting by unconstrained
jets is explicitly rejected.

The expensive finite-field ranks are not recomputed in this composition wave;
they are consumed from the pushed predecessor certificate, which reproduced
them at two primes and attained the maximal rational bounds.

## What transfers

The theorem transfers only when all of the following remain fixed:

1. the source-natural printed-endpoint action owner;
2. the nonzero base value of `Q_B` on its natural trace/Hodge line;
3. the endpoint principal tableau and its prolonged-symbol kernels;
4. the regular compatibility target bundle and observation receiver;
5. the unreduced 196-real field/196-real equation domain.

For an algebraic zero-order coefficient field, its first spatial derivatives
enter below the second-order principal symbol at a fixed base value. They may
change the torsion representative, but the onto quotient erases its class at
the tested orders.

## What requires a Layer-0 reset

This result does **not** transfer to:

- a `Q_B` base value leaving the fixed-natural trace/Hodge line;
- coefficient dependence on field derivatives that changes the principal
  symbol;
- the separate `E_act/Q_u` rival, whose selected fixed-bank principal map is
  zero;
- a new or completed source action with a different Euler principal owner;
- a singular observation map that changes the target bundle;
- an action-owned physical tangent, constraint or BV quotient that changes
  the domain;
- higher nonlinear orders, where the next absorber must be checked.

Each is `OBJECT_CHANGED__LAYER0_RESET`, not a coefficient correction to this
tableau.

## Scientific meaning

This is a formal-integrability economy result. It prevents a long sequence of
term-by-term coefficient expansions from being mistaken for independent
obstruction tests when the quotient is already onto the whole target.

It does not say the coefficients are physically irrelevant. They can still
change which stationary solution is selected, observation values, the Green
form, boundary data and the physical reduced theory. It says only that their
first principal-preserving jets cannot supply a cubic or quartic **formal
torsion obstruction** on this stratum.

No convergence, hyperbolicity, positivity, spectrum, presymplectic current,
BV/BFV quotient, global descent, particle count or physics verdict follows.

## Source return

`SC-ACT-04` owns the residual-square/two-connection grammar and the distinct
`Q_B` slot. The universal prolonged-symbol quotient theorem is repository
mathematics.

```text
SOURCE-CONFIRMS: SC-ACT-04 residual-square and two-connection grammar.
REPO-DERIVES:    universal cubic/quartic coefficient-jet absorption at fixed principal grade.
SOURCE-SILENT:   this Spencer theorem, a principal-changing completion and physical BV.
```

No ledger verdict, residue, quotient, datum, P1/P2/P3, canon claim or public
posture moves.

## Next gate

Stop expanding known principal-preserving coefficient jets merely to search
for cubic/quartic torsion. The high-yield alternatives are now:

1. construct and test the first **principal-changing** source-action owner, if
   the source grammar supplies one; or
2. construct the action-owned physical tangent/BV packet and recompute the
   symbol on that changed domain.

Higher fixed-owner nonlinear orders remain a fallback, not the primary path.
