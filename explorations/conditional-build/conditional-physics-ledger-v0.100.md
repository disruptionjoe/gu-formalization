# Conditional physics ledger v0.100

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.100.json`.

## Progress

`Ledger v0.100 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.100 adds

The exact local construction now reaches the selected action rather than only
its raw residual.

The moving K77 residual pairing and density, the action-owned primitive
epsilon Euler/preboundary term and the matched-`q` physical graph compose to a
local selected-action Euler-Noether identity in timelike, spacelike and null
classes. A nonzero-residual exact control makes the density, field and pairing
terms separately live before they cancel, so this is not a zero-background
artifact.

Antisymmetrizing the action-owned endpoint potential produces

```text
i_R Omega = -delta Q_boundary.
```

Boundary-vanishing transformations are characteristic. Unrestricted endpoint
transformations carry a live moment map and cannot be silently quotiented as
gauge. The selected action's exact `E_B-E_T` endpoint bank owns the momentum;
the rejected generic `p=K T` realization is not used.

This is a local proof-level composition of immutable exact K77 coefficient
owners plus a fresh exact universal variational theorem. It is not a new
16,384-direction replay, global adjoint-bundle theorem, BFV phase space,
boundary-domain selection, polarization or common Green/Krein domain.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `2`; conditions opened: `0`.

Closed:

1. local selected-action Euler-Noether identity on the matched-`q` physical
   graph;
2. compact-support presymplectic basicness versus unrestricted boundary
   moment map.

Remaining named condition: `1`.

1. Source- or action-select the physical boundary class, then construct global
   `tau_A0`/BFV or an owned edge-mode completion, polarization and a common
   closed Green/Krein domain.

Arbitrary primitive `D_epsilon Upsilon` remains an independent-field question
but is not a prerequisite for the dependent physical orbit. A field-space
Riesz map is required only for field-valued operator-adjoint language.

P1/P2/P3 remain unchanged and unused. No residue, quotient, fork, canon,
claim-status, public-posture, Curt-track or third-lane status changes. The
selected Spin-native parent, product of two `U(32,32)` Weyl halves and full
`U(64,64)` comparator remain distinct.

## Evidence

- `explorations/conditional-build/selected-k77-action-noether-preboundary-2026-08-08.md`
- `lab/process/selected-k77-action-noether-preboundary.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-action-noether-preboundary-review.md`
