---
title: "K92 observed spatial covariance and local-instrument wave"
status: active_research
doc_type: reverse_scaffold_spatial_covariance_local_instrument_result
created: 2026-09-02
date: 2026-09-02
target_claim: INTERNAL_TARGET:K91_SPATIAL_COVARIANCE_LOCAL_RECORD_DESCENT
claim_ceiling: exact repository-selected finite three-site covariance, local detector record instrument, commuting endpoint coupling and gauge-basic K91 quotient descent; no source-selected factorization, state, interaction or Born law, continuum AQFT or microcausality, curved-spacetime or microlocal/Hadamard theory, prediction, confirmation, held-out score or verdict
manifest: lab/process/k92-observed-spatial-covariance-local-instrument-wave.json
probe: tests/channel-swings/k92_observed_spatial_covariance_local_instrument_probe.py
---

# K92 observed spatial covariance and local-instrument wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: exact three-site rational covariance, local controlled-record instrument and zero-extended K91 quotient descent
carrier: selected P8=(C2) tensor (C2) tensor (C2) subspace of the K91 physical l2 term, plus one two-level detector LAYER=observed CHIRALITY=N/A
pairing: imported finite trace state-effect pairing on the selected physical tensor factor ON=repository_owned_spatial_control
real_structure: computational-basis conjugation, preserved by the diagonal state and real controlled detector unitary
grading: three site labels and binary detector record; no source BV, BFV or ghost grading
action_owner: repository-construction owns the finite state and controlled coupling only; no GU source action or detector Hamiltonian is supplied
target: spatial covariance, local record algebra and complete-positive instrument descent through the K91 split quotient MAP-TYPE=quotient
```

Scope: this result binds only the selected finite tensor soldering, rational
state, controlled detector interaction and zero extension below. It does not
bind a GU source carrier, causal geometry, continuum local net, physical state,
measurement postulate, prediction or verdict.

## Inline preflight bookend

The problem-matched lens census covered finite quantum spin systems,
nearest-neighbor Gibbs/Markov fields, covariance reconstruction, tensor-product
locality, finite operator algebras, controlled-unitary dilations, Kraus
instruments, record algebras, remote marginal invariance, gauge-basic quotient
descent, AQFT net ownership, microlocal state conditions and source custody.
The smallest exact route was a three-site diagonal state: it supplies nonzero
spatial covariance, an exact local record interaction and two disjoint endpoint
couplings without using numerical tolerance or importing a continuum causal
cone.

Object-level retrieval found K88's action-pulled causal detector net, K89's
separate detector/covariance quotient descent, K90's one-mode response weights
and K91's finite unitary-to-Kraus instrument. It found no packet putting a
nontrivial spatial covariance, a named local record algebra and K91
zero-extension on one selected tensor factor. The standard Ising and
controlled-record mechanisms are not claimed as novel. Positive controls are
normalization, covariance composition, unitary involution, Kraus completeness,
conditional correlations, endpoint commutation, remote invariance and
basicness. Negative controls mutate each separately.

## A selected spatial factor inside the K91 physical term

Retain K91's split sequence

```text
0 -> G -> G direct-sum P -> P -> 0,       P=l2(N,C).       (1)
```

Select the span `P8` of the first eight physical modes and choose an isometry

```text
iota : (C2) tensor (C2) tensor (C2) -> P8.                (2)
```

Equation (2) is new repository soldering data. An eight-dimensional Hilbert
subspace does not carry named sites merely from its dimension. Write a
computational basis state as `s=(s0,s1,s2)` and set `z_i=(-1)^(s_i)`.

Define an exact diagonal density by giving a factor two to every aligned
nearest-neighbor edge:

```text
w(s)   = 2^[1(s0=s1)+1(s1=s2)],
Z      = sum_s w(s) = 18,
rho(s) = w(s)/18.                                         (3)
```

All eight weights are positive and sum to one. Global spin flip is a symmetry,
so every one-site mean vanishes. Direct summation gives

```text
Cov(Z0,Z1) = 1/3,
Cov(Z1,Z2) = 1/3,
Cov(Z0,Z2) = 1/9.                                        (4)
```

The endpoint identity `1/9=(1/3)(1/3)` is the exact three-site Markov
composition for this selected state. It is spatial covariance on a finite
chosen tensor factor, not a source two-point function, a Hadamard condition or
a microlocal spectrum theorem.

## Local detector dilation and record algebra

Attach a two-level detector `D` initialized in `|0>`. At the middle site use

```text
U1 = P0(Z1) tensor I_D + P1(Z1) tensor X_D,               (5)
```

where `P_r(Z1)` projects onto computational bit `s1=r`.
Equation (5) permutes the sixteen joint basis states and squares to the
identity, hence it is unitary. Reading the detector in its computational basis
derives

```text
K0=P0(Z1),        K1=P1(Z1),
K0^*K0+K1^*K1=I8.                                        (6)
```

The maps `Phi_r(a)=K_r a K_r` are completely positive by displayed Kraus
form. Their record algebra is the two-dimensional commutative algebra generated
by the detector projections. Using the imported trace/Born pairing with (3),

```text
p0=p1=1/2.                                                (7)
```

Conditioning exposes rather than creates the covariance:

```text
E[Z0 | s1=0] = +1/3,
E[Z0 | s1=1] = -1/3,                                     (8)
```

and the same equations hold at site 2 by reflection. The unitary and detector
preparation derive (6), but they do not derive (3), the trace/Born pairing, or
the claim that a detector basis label is a physical classical record.

## Equal-time locality and remote invariance

The analogous controlled detector unitaries at sites zero and two act on
disjoint tensor factors and commute exactly. They therefore generate commuting
endpoint record algebras in this selected equal-time tensor net. The
nonselective middle instrument leaves the diagonal state (3) unchanged:

```text
Phi_0(rho)+Phi_1(rho)=rho.                                (9)
```

In particular both endpoint marginals, and their joint marginal, are
unchanged when the middle record is forgotten. Conditional endpoint statistics
can change because (3) is correlated; the unconditional remote state cannot.
These are finite tensor-factor statements. They do not prove continuum
microcausality, spacetime separation, finite propagation or a GU local net.

## Gauge-basic descent through the K91 split sequence

Decompose the physical term as `P=P8 direct-sum P_perp`, let (5) act on
`P8 tensor D`, and use the identity on `P_perp tensor D`. On the ambient term
`G direct-sum P`, use the identity on `G tensor D` and the physical unitary on
`P tensor D`. Then

```text
(q tensor id_D) U_tilde = U_P (q tensor id_D).            (10)
```

Site and record observations are extended by zero on `G`; equivalently they
annihilate the image of the gauge injection. Thus their outcome probabilities,
conditional records and remote marginals are representative-independent.
This is exact descent through the repository-owned split quotient. Neither the
embedding (2) nor the site labels are selected by (1).

## What moved and what did not

K91's abstract physical quotient now has one explicit finite spatial
realization in which nonzero covariance, a local unitary coupling, a complete
record instrument, commuting disjoint couplings and gauge-basic descent coexist.
The result closes a compatibility construction, not an ownership claim.

The spatial soldering, finite state, temperature/edge weight, detector
preparation, trace/Born pairing and record meaning were supplied. No source
functional complex, spacetime geometry, source state or local interaction was
found. No continuum AQFT, curved-spacetime Green, microlocal/Hadamard, derived
Born, prediction, confirmation, canon, paper or held-out status moves.

## Inline postflight bookend

- Strongest overclaim: calling the selected `P8` tensor coordinates and
  commuting endpoint algebras source GU locality. The K91 `l2` quotient does
  not name tensor sites, and equal-time commutation is not microcausality.
- Strongest contrary construction: change the edge weight and the exact
  covariances change; omit one record projector and completeness fails; make
  the detector map nonbijective and unitarity fails; add a gauge coefficient
  and representative independence fails.
- Weakest reproducibility seam: the exact probe exhausts all eight classical
  configurations and the finite controlled maps, but it does not approximate
  a continuum field or establish source relevance. Its completeness is a
  virtue only inside the stated finite scope.

The exact probe passes `27/27`; its hostile selftest catches `17/17` state,
covariance, interaction, completeness, signalling, descent, custody and
promotion mutations. The delayed-choice entanglement-swapping family remains
reserved and unscored.

## Next condition

Supply a source-authenticated physical tensor/net assignment, state and local
interaction on the source functional quotient. Show that its causal evolution
preserves the net, its state meets the required analytic or microlocal
condition, and its record instrument descends. Independently test whether the
source data select that spatial soldering rather than merely admit one.
