---
title: "K474 — K152 Hierarchical Accuracy Scheduler"
status: active_research
status_axis: operational_state
doc_type: conditional_recursive_complement_and_residual_budget_composition
created: 2026-09-25
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K474 — K152 hierarchical accuracy scheduler

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

```gu-typed-objects
result: fail-closed composition of the K473 recursive complement floor and K469 direct M-dual ground budget
carrier: the same fixed K162 trial line and complete M-orthogonal complement used by K463/K469 LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical M=S* S metric and its M-dual residual norm ON=k162_trial_and_complete_complement
real_structure: inherited CAR adjoint and K162 signed flavor transport
grading: trial line, finite complement slice, complete tail and coherent residual groups
action_owner: repository-construction; no physical action or state is selected
target: release condition for the 59586-entry K457 finite Gram payload MAP-TYPE=evaluation
```

## Opening bookend

K473 can produce a rigorous complete floor only after its three same-form
inputs arrive. K469 separately turns that floor into a target-deficit residual
budget. Neither test may be skipped, and the K457 computation should not begin
until their conjunction specifies an accuracy.

## Composition

First require

```text
mu^2 < (alpha-b)(gamma-b),
beta=(alpha+gamma-sqrt((alpha-gamma)^2+4 mu^2))/2 > b.
```

Then, for trial Rayleigh value `rho` and chosen target deficit `d>0`, require

```text
eta^2 <= d(beta-rho+d).                                 (1)
```

With K457's certified norm tail `epsilon`, the finite Gram work is released
only to the accuracy

```text
sqrt(Q12) <= sqrt(d(beta-rho+d)) - epsilon.             (2)
```

The exact control uses `rho=-2`, `b=1`, `alpha=3`, `gamma=6`, `mu=2`,
`beta=2`, `d=1/2`, `eta^2=9/4` and K456's exact tail
`3011499/838860800`. Both inequalities saturate or pass, and (2) gives the
finite allowance `1255279701/838860800`. This is a control, not a native
accuracy release.

## Closing bookend

- Strongest advance: the complete complement construction and numerical
  residual evaluation now meet at two explicit fail-closed inequalities.
- Strongest overclaim: the exact control authorizes evaluation of native
  K457 entries. Refused; native block and target packets remain absent.
- Strongest contrary case: either a failed Schur margin or a residual above
  (1) blocks release even when the other half passes.
- Weakest seam: the `eta` packet and K473 blocks must refer to the same fixed
  form and complete carrier.

## Reproduction

```bash
python3 tests/channel-swings/k474_k152_hierarchical_accuracy_scheduler.py
python3 tests/channel-swings/k474_k152_hierarchical_accuracy_scheduler_probe.py
```
