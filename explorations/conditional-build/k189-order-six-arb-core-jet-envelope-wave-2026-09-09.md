---
title: "K189 order-six Arb core jet envelope wave"
document_role: active_research
doc_type: conditional_native_K139_K189_order_six_arb_core_jet_envelope_result
created: 2026-09-09
date: 2026-09-09
claim_ceiling: rigorous repository-owned Arb-backed outward value and primitive-coordinate mixed-derivative envelopes through total order four for all 53 K186 divided-difference regularizer patterns on the K188 positive-radius face-stripped core, derived without time-gap division from complete monotonicity, the exact K1 derivative recurrence, tensor Hermite--Genocchi and the exact Cauchy normalization; the global raw-kernel envelope is finite but reaches 10^533 for size-three values and 10^776 for fourth derivatives, so it is not a decision-grade Duffy/Jacobi remainder and supplies no complete positive-core error, outward order-six total, accurate prefix, action column, residual, complement or flux floor, scalar-center floor, K152 interval, physical/source selection, Born derivation, prediction or confirmation
manifest: lab/process/k189-order-six-arb-core-jet-envelope-wave.json
solver: tests/channel-swings/k189_order_six_arb_core_jet_envelope.py
probe: tests/channel-swings/k189_order_six_arb_core_jet_envelope_probe.py
target_claim: INTERNAL_TARGET:K188_POSITIVE_RADIUS_FACE_STRIPPED_CORE_GLOBAL_RAW_KERNEL_JET_GATE
target_claim_verdict: RIGOROUS_VALUE_AND_PRIMITIVE_MIXED_DERIVATIVE_ENVELOPES_EXIST_FOR_ALL_53_PATTERNS__ONE_UNSPLIT_RAW_KERNEL_CORE_REMAINDER_IS_NOT_DECISION_GRADE__CANCELLATION_PRESERVING_SCALED_KERNEL_OR_REGULARIZER_TAYLOR_MODEL_REQUIRED
canon_verdict_change: none
---

# K189 order-six Arb core jet envelope wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: INTERNAL_STRUCTURAL_ONLY.

Scope: K186's fixed K139/K156 hard-core `C3` impurity control, auxiliary chart
`lambda=256`, equal couplings one, three normalized K162 zero-bath seed orbits,
and complete K184--K188 order-six family. K189 changes no operator, extension,
state, domain, polarization or scalar center. It tests only the global interval
conditioning of the K188 positive-radius face-stripped core.

```gu-typed-objects
result: every one of the 53 K186 nontrivial regularizer patterns has a rigorous positive value envelope and rigorous absolute primitive-coordinate mixed-derivative envelopes through total order four on 2^-20<=rho<=1/4 and z_i>=2^-180; size-three value ceilings reach 10^533 and fourth-derivative ceilings reach 10^776, so the unsplit raw-kernel envelope is not a decision-grade Duffy/Jacobi remainder
carrier: hard-core C3 impurity tensor positive particle/hole antisymmetric Fock space Gamma_-(L2(R;C4)), in q=(0,0),(1,0),(0,1), with the fixed K139 chart and normalized K162 zero-bath seed lines LAYER=observed CHIRALITY=S-FULL-DIRAC
pairing: positive physical Fock Hilbert pairing on the order-six output sector; K184 coherent path-pair assembly and K186 specieswise determinant factorization remain unchanged ON=repository_signed_point_control
real_structure: CAR adjoint, momentum reflection, Hermitian matched finite-cutoff normal ordering, positive Laplace times, exact K1 derivative recurrence, tensor Hermite--Genocchi averages and python-flint 0.9.0 / FLINT 3.6.0 Arb balls
grading: K186 exact Cauchy--Vandermonde skeleton and coalescent divided differences, K188 four-region split, exact dyadic primitive floor, global kernel jets, determinant/product-rule envelope, directed Arb endpoints, finite decimal ceilings, independent binary64 controls and withheld cubature release
action_owner: repository-construction -- K139 fixes C and G, K156 fixes W, K179 fixes the coefficient family, K184 fixes the time Grams, K185/K188 fix the closed boundary majorants and K186/K187 fix the regularizers and endpoint class; no source/GU action selects a physical extension, state, domain, polarization or scalar center
target: test whether one global cancellation-free raw-K1 derivative envelope can support the determinant-preserving positive-radius Duffy/Jacobi remainder, bank every valid outward jet bound, and route the core to a cancellation-preserving scaled-kernel or regularizer Taylor model when the global enclosure is too broad MAP-TYPE=intertwiner
```

## Inline preflight bookend

K188 leaves exactly one integration region:

```text
2^-20 <= rho <= 1/4,
z_i >= 2^-180,
sum_i z_i = 1.
```

K186 proves that all 468 nontrivial determinant occurrences reduce to 53
positive coalescent regularizers. The incumbent question is whether explicit
outward value and derivative bounds on that core can carry a weighted Jacobi
remainder. The strongest independently framed source-to-physics challenger is
still the positive physical quotient and count/chirality bridge.
`SC-META-53` is `UNCERTAIN`; ledger-v0.263 rows `LT-SM8`, `RA-F1` and
`AC-F1` remain `NEEDS`, with absent action-owned stationary background,
functional BV/BFV domain, positive physical pairing and typed count or chiral
carrier. That challenger remains dependency-limited.

The route census compared raw determinant intervals, an assumed theorem
`R_m<=1`, local arbitrary-precision subdivision, direct regularizer Taylor
models and a global Hermite--Genocchi jet envelope. Raw determinants repeat the
coalescence subtraction already removed by K186. No held theorem proves
`R_m<=1`, so the sampled values below one cannot be promoted. A global jet
envelope is the cheapest rigorous feasibility discriminator before building a
large local cubature: it either supplies a useful remainder or identifies
exactly which cancellation must remain inside the interval object.

## 1. Gap-free mixed divided-difference theorem

Let

```text
f(x) = 2 K_1(x)
     = integral_R exp(-x sqrt(1+p^2)) dp.
```

Differentiation under the positive integral proves complete monotonicity:

```text
(-1)^n f^(n)(x) > 0  for x>0.
```

Repeated use of `K_nu'(x)=-(K_(nu-1)(x)+K_(nu+1)(x))/2` gives the exact
finite identity

```text
|f^(n)(x)|
  = 2^(1-n) sum_(k=0)^n binomial(n,k) K_|1-n+2k|(x).
```

For the K186 mixed Newton matrix, the entry in row order `i` and column order
`j` is a tensor Hermite--Genocchi average of
`f^(i+j)/(i! j!)`. This is the crucial conditioning step: the theorem bounds
the divided difference directly and never divides two wide intervals by a
small time gap.

On the K188 core, every primitive time is at least

```text
rho z_i >= 2^-20 2^-180 = 2^-200.
```

For one pattern with canonical positions `a_i,b_j`, every argument contributing
to mixed entry `(i,j)` is therefore at least

```text
(16-a_i-b_j) 2^-200.
```

Because `|f^(n)|` decreases with its argument, the exact dyadic lower endpoint
controls the whole entry.

## 2. Determinant and normalization derivatives

K186's divided-difference factorization gives

```text
R_m(T,U)
  = det(M_f(T,U)) product_(i,j)(T_i+U_j) / 2^m.
```

For any primitive-coordinate mixed partial of total order `r`, the solver
enumerates every Leibniz permutation and every multinomial allocation of the
`r` derivatives among the `m` matrix factors. Hermite--Genocchi bounds each
differentiated matrix entry at the exact pattern-specific argument floor.

The normalization product has `m^2` linear factors, each at most `1/4`. Its
order-`s` derivative is bounded by

```text
(m^2)!/(m^2-s)! (1/4)^(m^2-s).
```

The outer product rule then encloses every primitive mixed partial of `R_m`
through total order four. K186 positivity supplies the value lower endpoint
zero. The result is finite across every time-coalescent face and contains no
division by a primitive gap.

## 3. Directed Arb certificate for the complete family

The tracked solver evaluates every Bessel endpoint with
python-flint `0.9.0`, FLINT `3.6.0`, 100 decimal digits and one thread. Each
transcendental endpoint is a nonzero-width Arb ball. The solver converts the
final positive ball to an elementary decimal ceiling `10^N` only after Arb
proves that the ceiling strictly contains it.

All 53 patterns, all 468 occurrences (each nontrivial), all 234 time-Gram entries and
all 18 coherent groups remain in scope. The per-pattern manifest records the
canonical positions, occurrence count, positive value interval and orders
zero through four. The power ranges are:

| size | value | order 1 | order 2 | order 3 | order 4 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 2 | `10^235`--`10^238` | `10^295`--`10^298` | `10^355`--`10^359` | `10^415`--`10^419` | `10^475`--`10^480` |
| 3 | `10^531`--`10^533` | `10^592`--`10^594` | `10^652`--`10^655` | `10^713`--`10^716` | `10^773`--`10^776` |

These are valid outward envelopes, not approximations to the regularizer.
Their breadth is the result of taking absolute values before the Cauchy
cancellation has acted.

## 4. Global-rule feasibility and route switch

The solver repeats the size-two/three value envelope at combined primitive
floors from `2^-8` through the actual `2^-200`. Even at `2^-8`, the largest
size-three ceiling is `10^13`; at the actual floor it is `10^533`. By contrast,
K186's direct and mixed-divided-difference controls remain in

```text
0.8804248822657249 <= R_m <= 0.9880041699766289.
```

This does not prove the sampled interval globally and does not prove an
`R_m<=1` theorem. It does prove that one unsplit raw-kernel envelope is
not decision-grade: the single global outward values and derivatives are hundreds of decimal
orders broader than the object they are meant to resolve, and no complete
Duffy/Jacobi chain-rule or cubature error follows from it.

The next interval object must preserve the Cauchy cancellation internally.
The selected successor is a scaled-kernel or direct-regularizer Taylor model
using

```text
q(x)=x K_1(x),
```

the exact normalization and coalescent jets together, before stratified
radial/angular subdivision. Merely creating more boxes around the raw `K_1`
kernel does not fix the conditioning defect.

## 5. Independent controls

SciPy binary64 evaluation independently recomputes the finite derivative
recurrence through order eight at `x=1/4`. Its largest relative difference
from the Arb midpoint is `2.15e-16`. The K186 direct and mixed-divided-
difference controls lie inside their matching positive outward envelopes.

Those checks test the recurrence, backend plumbing and scale contrast. Arb
balls, the exact derivative identity and Hermite--Genocchi carry the outward
claim.

## 6. Honest release boundary

K189 banks rigorous value and primitive mixed-derivative envelopes through
order four for every current regularizer pattern. It does not serialize the
Duffy/Jacobi chain rule, a core cubature remainder or the signed coherent
order-six total. The accurate order-six prefix remains through order five.

No K171/K168 action column, `R_ref` residual, complete complement or flux
floor, scalar-center floor or K152 interval is claimed. The source and physics-
ledger statuses remain unchanged: `SC-META-53` remains `UNCERTAIN`; `LT-SM8`,
`RA-F1` and `AC-F1` remain `NEEDS`. No physical/source selection, Born
derivation, prediction, confirmation, canon, paper, release or public-posture
conclusion follows.

## Inline postflight bookend

- **Strongest advance:** all 53 regularizers now have the first directed-Arb outward value and primitive mixed-derivative envelopes, valid across time coalescence without gap division.
- **Strongest negative result:** the size-three global raw-kernel value and fourth-derivative ceilings reach `10^533` and `10^776`; they cannot support a decision-grade unsplit remainder.
- **Strongest structural switch:** interval work must enclose the scaled kernel and Cauchy normalization together, or act directly on regularizer Taylor models, before subdivision.
- **Strongest overclaim:** “finite Arb envelopes close the positive core.” Refused; no Duffy/Jacobi chain rule or cubature error is serialized.
- **Strongest contrary route:** a global theorem such as `R_m<=1` would dominate the present envelope, but no such theorem is proved or found in the owner evidence, and samples below one are controls only.
- **Weakest reproducibility seam:** the next solver must preserve Cauchy cancellation inside every interval object; separately enclosing raw kernel derivatives recreates hundreds of orders of dependency loss.

All seven admitted packets completed. The derivative theorem, determinant and
normalization product rules, Arb endpoint evaluation, complete pattern and
occurrence propagation, scale ladder, independent control and hostile/release
surface are banked. The result advances the core gate by eliminating one
apparently rigorous but numerically unusable remainder class.

## Reproduction

```bash
_local/cas-venv/bin/python tests/channel-swings/k189_order_six_arb_core_jet_envelope.py --summary
_local/cas-venv/bin/python tests/channel-swings/k189_order_six_arb_core_jet_envelope.py --write --summary
_local/cas-venv/bin/python tests/channel-swings/k189_order_six_arb_core_jet_envelope_probe.py
_local/cas-venv/bin/python tests/channel-swings/k189_order_six_arb_core_jet_envelope_probe.py --selftest
```
