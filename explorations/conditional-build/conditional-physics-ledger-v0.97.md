# Conditional physics ledger v0.97

Status: current append-only human view. Machine authority:
`lab/process/conditional-physics-ledger-v0.97.json`.

## Progress

`Ledger v0.97 — 82/82 mapped (100%)`

- `32 SAME`
- `19 DIFFERS`
- `26 NEEDS`
- `5 OVER-DETERMINED`
- residue: `84` booked continuous real before quotient; `84..86` conditional
  range by action parent; `>=19` function-valued slots and `9` open forks
- scoped quotients ranked: `5`

Headline verdicts do not move. Five rows receive append-only distance,
frontier-grade and evidence migrations.

## What v0.97 adds

The fixed-independent-`varpi` metric derivative is now emitted on the same
all-grade residual-coordinate carrier as the actual source-`varpi` operator:

```text
J_g = G^mu nabla_mu,
rank(G^mu) = 9 for mu=0,1,2,3,
rank(G^0,...,G^3) = 20.
```

Every timelike, spacelike and null transverse restriction has rank six.  The
physical metric torsion response and independent-`varpi` torsion displacement
cancel exactly on the diffeomorphism graph.

```text
delta_g T[D_q xi] + delta_varpi T[C_q xi] = 0.
```

Direct comparison falsifies the prior Ward-defined metric orbit as the
physical Levi-Civita derivative.  The actual orbit has rank three; the
identity-defined completion has rank four.  Their discrepancy and the current
physical `g+varpi+gamma-epsilon` principal Ward defect both have rank four in
all three causal classes.

The metric bank survives.  The earlier four-column orbit completion survives
only as the target that a complete primitive epsilon/diffeomorphism transport
must reproduce.  The v0.96 `D_varpi` equation-dual and Green result also
survives, but it may not yet be promoted to a common-field formal adjoint.

## Rows moved

- `LT-GR1`
- `LT-GR2b`
- `LT-GR3`
- `LT-GR5`
- `LT-GR6`

Each retains its verdict.

## Frontier

Conditions closed: `3`; conditions opened: `1`.

Closed:

1. common residual-coordinate `D_g Upsilon` coefficient bank;
2. exact rank-twenty/rank-nine/rank-six metric geometry on that carrier;
3. exact physical `g-varpi` augmented-torsion graph cancellation.

Opened/refined:

1. the prior Ward-defined metric orbit is not physical `D_g Upsilon`; the
   complete primitive epsilon/diffeomorphism transport must supply a measured
   rank-four correction.

Remaining named conditions: `3`.

1. Construct complete primitive `D_epsilon Upsilon` and the physical
   diffeomorphism transport, then retest common `J R=0`.
2. Extend the `K_loc` equation dual and Green identity only after that closure.
3. Derive a geometrically owned field-space Riesz map, or retain explicitly
   covector-valued adjoint language.

P1/P2/P3 remain unchanged and unused. No residue, quotient, fork, canon,
claim-status, public-posture, Curt-track or third-lane status changes. The
selected Spin-native parent, the product of two `U(32,32)` Weyl halves and the
full `U(64,64)` comparator remain distinct action-parent questions.

## Evidence

- `explorations/conditional-build/selected-k77-common-metric-dupsilon-coefficient-bank-2026-08-08.md`
- `lab/process/selected-k77-common-metric-dupsilon-coefficient-bank.json`
- `lab/process/hostile-reviews/2026-08-08-selected-k77-common-metric-dupsilon-coefficient-bank-review.md`
