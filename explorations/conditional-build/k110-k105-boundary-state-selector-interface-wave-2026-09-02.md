---
title: "K110 K105 conditional boundary-state selector and complete finite interface"
status: active_research
doc_type: conditional_boundary_selector_sufficiency_and_selection_ownership_result
created: 2026-09-02
date: 2026-09-02
claim_ceiling: exact conditional theorem for a repository-owned boundary-state packet whose unique rational maximum selects one K105 positive retract and whose declared finite interface owns the quotient, state/effect pairing, tensor rule, domain and dynamics; selection is encoded in the supplied boundary weights, not derived from K105 or GU, and no physical/source ownership, Born derivation, prediction or confirmation follows
manifest: lab/process/k110-k105-boundary-state-selector-interface-wave.json
probe: tests/channel-swings/k110_k105_boundary_state_selector_interface_probe.py
target_claim: NONE-NOT-A-KILL
canon_verdict_change: none
---

# K110 K105 conditional boundary-state selector and complete finite interface

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `INTERNAL_STRUCTURAL_ONLY`.

Scope: this packet supplies one conditional boundary state on K105's exact
256-dimensional positive coefficient-blind seed space. It asks whether that
single datum can own enough structure to select a K91 retract and carry the
complete finite operational denominator missing at K109. The answer is yes at
conditional repository-construction grade, with an exact debit: the supplied
boundary weights already contain the selected coordinate.

```gu-typed-objects
result: one strictly positive normalized boundary weight with a unique rational maximum breaks the K105 positive-seed permutation symmetry, conditionally selects one K91 retract and supports one packet-owned finite real quantum interface, while the selected coordinate remains supplied rather than derived
carrier: K105 distortion-448 with positive coefficient-blind coordinate seed space B_plus of dimension 256, followed by two K91 modes on the selected quotient line LAYER=observed CHIRALITY=N/A
pairing: K105 mixed lowerer for carrier/retract descent; packet-declared trace state/effect pairing on real symmetric two-mode matrices ON=conditional_boundary_packet
real_structure: real K105/K91 histories, real symmetric state/effect matrices, transpose star and real Hilbert tensor composition
grading: inherited minimal K91 BRST grading plus bipartite tensor grading; the boundary weight is degree zero
action_owner: repository-construction -- K105 free action plus an independently declared conditional boundary-state/interface packet; neither is authenticated as a GU source or physical owner
target: positive-retract selection, quotient ownership and complete finite state/effect-tensor-domain-dynamics sufficiency MAP-TYPE=quotient
```

## Inline preflight bookend

K106 proves that the frozen K105 action has an `O(256) x O(183)` blind-sector
symmetry and gives only a planted diagonal-operator positive control. K109
proves that both the minimal commutative cohomology and an imported rebit
calibration interface remain equivariant across all 256 positive seeds. Thus
neither predecessor contains a symmetry defect and a complete operational
owner in the same packet.

The broad route census considered another action coefficient, another
generated boundary operator, source retrieval, a nonuniform boundary state and
an observable-first selector. The first two repeat K106/K107 or violate the
no-invented-source-operator boundary; source retrieval is not an executable
construction method; an observable-only selector still leaves the quotient,
pairing, tensor and dynamics owners split. A conditional boundary-state packet
is the smallest materially distinct route because its stabilizer is directly
classifiable and it can declare every downstream operational field in one
typed owner.

The cheapest switch is whether the boundary weight has a unique maximum.
Equal weights retain `S_256` and select nothing. A unique maximum produces a
nonzero swap defect and a rank-one spectral projector, but the maximizer can
then be read directly from the supplied weight vector. This simultaneously
tests sufficiency and prevents false derivation credit.

## 1. Boundary-weight stabilizer and selection theorem

Let `B_+` be the span of K105's 256 positive coefficient-blind coordinate
seeds `e_0,...,e_255`. Supply the normalized strictly positive boundary weight

```text
w_0 = 2/257,                 w_i = 1/257  (1 <= i <= 255).   (1)
```

Regard `Lambda_w=diag(w)` as a boundary density on `B_+`. The stabilizer of a
diagonal coordinate weight under seed permutations is the product of the
symmetric groups on its equal-weight level sets. Hence

```text
Stab(Lambda_w) = S_1 x S_255 ~= S_255,                       (2)
```

whereas the equal weight `w_i=1/256` has stabilizer `S_256` and no preferred
seed. The `e_0/e_1` swap has nonzero defect because `w_0-w_1=1/257`.

More generally, a coordinate seed is uniquely selected by the top spectral
projector of `Lambda_w` if and only if its weight is the unique strict maximum.
For (1), the selector is

```text
P_0 = e_0 e_0^T,            gap = 1/257.                    (3)
```

Moving the heavy weight from coordinate zero to coordinate `j` moves the
selected line to `e_j` while every frozen K105 datum stays unchanged. Thus the
selected label is recoverable from the boundary packet alone. Equations
(1)--(3) prove conditional selection and, at the same time, prove that the
selection information has been relocated into `Lambda_w`; it has not been
derived from the K105 action or the quantum anchors.

## 2. Boundary-owned quotient and exact K91 retract

Extend `P_0` by zero from `B_+` to the full distortion-448 carrier. It is
idempotent, `K`-self-adjoint and has one-dimensional `K`-positive range. Since
`e_0` lies in the coefficient-blind sector,

```text
C_*^T K P_0 = 0.                                             (4)
```

The boundary packet declares the physical null subspace `N=ker P_0` and owns
the quotient

```text
R^448 / N ~= span(e_0).                                     (5)
```

Composing (5) with K105's previously proved split injection and retraction
selects the `e_0` K91 action/domain/Green retract. The free action, Euler map,
closed domain, rapid core, retarded/advanced Green pair and boundary form
intertwine exactly because (4) is the same coefficient-blind premise used by
K105. The ambient K105 form remains inertia `(260,188,0)`; the packet owns the
quotient that discards its other directions. It does not rewrite the ambient
form as positive or attribute that quotient to K105.

## 3. One packet owns the finite operational denominator

On two K91 modes over `span(e_0)`, the same conditional packet now declares:

- local states: positive semidefinite `rho in Sym_2(R)` with `tr rho=1`;
- effects: `E in Sym_2(R)` with `0 <= E <= I`;
- pairing: `p(E|rho)=tr(rho E)`;
- composition: the real Hilbert tensor product and positive normalized
  `Sym_4(R)` composite states;
- domain: the full finite two-mode space; and
- dynamics: `R_t=exp(tJ)`, with `J^T=-J` and `J^2=-I`, acting by orthogonal
  conjugation locally and by the corresponding tensor action on composites.

This is a complete finite owner, not a derivation of Born's rule. Orthogonal
conjugation preserves the positive cone and trace. The exact controls reproduce
two-path probabilities `1,0,1/2`, Bell CHSH `2 sqrt(2)` and maximally mixed
local Bell marginals. They also retain the rebit boundary: local symmetric
products span dimension 9 while `Sym_4(R)` has dimension 10, with hidden
direction `J tensor J`.

The construction is materially different from K109. There the rebit interface
was imported identically on all 256 retracts and selected none. Here one
boundary packet owns both a nonuniform seed density and the downstream finite
interface, so it conditionally selects exactly one retract. The price is fully
visible: the unique maximum is an independently supplied boundary datum.

## 4. Selection-information accounting

This package closes the finite sufficiency question:

```text
nonzero defect + unique positive top line + quotient + finite operational
owner  ==>  one conditionally selected K91 interface.                       (6)
```

It does not close the origin question. Because `argmax(w)` recovers the chosen
seed and moving the heavy weight moves the result with K105 fixed, the packet
contains at least the coordinate-selection information it outputs. The
calibration anchors verify compatibility only. They neither determine (1) nor
convert the conditional boundary owner into a source-derived GU state.

## Inline postflight bookend

- **Strongest overclaim:** “K105 now selects a physical quantum state.”
  Refused. The frozen K105 data still select no seed; the independently
  declared boundary weights own the quotient and choice.
- **Strongest contrary construction:** a source/action/boundary mechanism may
  derive a nonuniform weight invariantly on the actual nonlinear/global
  carrier. Preserved as the exact reopener.
- **Strongest mistyping risk:** treating the trace pairing as derived Born
  structure. Refused: it is a declared field of the same conditional packet.
- **Weakest reproducibility seam:** a nonzero swap defect alone would not prove
  unique selection. The certificate separately checks normalization,
  positivity, maximum multiplicity, rational gap, stabilizer multiplicities,
  projector identities, quotient rank and the movable-heavy-weight control.

The independent certificate passes its baseline before attempting hostile
mutations. It checks the exact K105 fixture, selection theorem, quotient,
bounded dynamics, interference, Bell value, marginal and hidden composite
direction (`25/25`) and catches all `22/22` hostile mutations. The delayed-
choice holdout remains reserved and unscored. No source
action, GU physical state, nonlinear/global quotient, derived Born rule,
prediction, confirmation, canon, paper or public posture moves.

## Reproduction

```bash
UV_CACHE_DIR=/private/tmp/uv-cache-gu-formalization uv run --with sympy==1.14.0 python \
  tests/channel-swings/k110_k105_boundary_state_selector_interface_probe.py
UV_CACHE_DIR=/private/tmp/uv-cache-gu-formalization uv run --with sympy==1.14.0 python \
  tests/channel-swings/k110_k105_boundary_state_selector_interface_probe.py --selftest
```
