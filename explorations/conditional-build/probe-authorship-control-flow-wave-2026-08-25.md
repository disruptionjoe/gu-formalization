---
title: "Probe-authorship control-flow hardening wave"
status: active_research
doc_type: internal_certificate_hardening
created: "2026-08-25"
grade: "MECHANICAL FAILURE-DETECTION HARDENING; NO NEW SCIENTIFIC RESULT"
target_claim: internal control-flow witness custody; verdict seven crash-not-detection defects are repaired and the repository-wide lint ratchet is lowered from 114 to 107
canon_verdict_change: none
---

# Probe-authorship control-flow hardening wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: INTERNAL_STRUCTURAL_ONLY`

Scope: mechanical failure detection across five repository-native probes.
This artifact does not rederive, strengthen or weaken any GU, comparator,
source, action, ledger, SG4, prediction, paper or canon claim.

```gu-typed-objects
result: seven control-flow witness defects are repaired and the global ratchet now fails on any increase above 107
carrier: repository-native Python probes and the probe-authorship lint corpus LAYER=UNTYPED CHIRALITY=N/A
pairing: NONE
real_structure: N/A -- detector syntax has no scientific real-form content
grading: mechanical certificate hardening only
action_owner: N/A -- no scientific action owner is created or changed
target: make absent strict wall crossings, half-spin representatives, distinct operator inputs and exterior complements reach controlled checks or safe sentinels before use MAP-TYPE=evaluation
```

## Preflight bookend

The source-to-proof class remains authority-gated on `SC-SIG-55`; all 91
v0.263 ledger rows remain terminal; the named B2 action-root set remains empty;
prediction and SG4 retain their exact dependencies. The earlier semantic-
currency wave supplies the route switch required before this fresh Lane-3
cohort.

The entering lint reported 114 residual L1/L2 violations across 981 probes.
Semantic review excluded infinite counters, finite-order searches with a
mathematically guaranteed witness, iterators behind exact cardinality or
homogeneity guards, and matrix inversions whose invertibility is the theorem's
explicit premise. Seven unguarded lookups remained in four compatible packets:
three wall-crossing sentinels, two half-spin fixture sentinels, one distinct-
input operator sentinel and one full-degree exterior sentinel.

## Result

Strict-crossing routines now return `None` when a sampled path reaches zero but
never crosses negative. The CB8 probe records missing plus/minus eigenspace
fixtures before using safe placeholders, so its existing checks fail without a
lookup crash. Every K77 map records whether a second input column exists before
polarization. Coefficient dualization ignores a full-degree key that has no
exterior complement, leaving downstream exact checks to decide sufficiency.

Successful paths retain their selected indices, spinors, sparse-operator
entries and dual coefficients exactly. The lint falls by seven, from 114 to
107, across the resulting 982-probe corpus. The immediately preceding witness
certificate preserves its historical 136-to-114 result monotonically.

## Postflight and hostile review

- **Strongest overclaim:** safe sentinels certify detector authorship, not the
  scientific claims encoded by the probes.
- **Strongest contrary case:** the excluded iterators have local mathematical
  proofs of nonemptiness; inserting defaults there could weaken exact tests.
- **Weakest reproducibility seam:** the operator-grade probe is the heaviest
  touched replay; its existing bounded stage structure and the narrow coupled
  hostile certificate carry the sentinel claim if full execution reaches the
  measured local-resource limit.
- **Hostile controls:** zero-only wall samples, missing half-spin eigenspaces,
  one-input operators and full-degree exterior keys all reach controlled
  states; reintroduced defaultless `next()` remains lint-detectable.

No source adherence or polarity, scientific verdict, action owner, ledger row,
SG4 phase, prediction, confirmation, paper, canon verdict, release readiness or
public posture changes. Further authorship work requires another fresh,
semantically reviewed nonoverlapping cohort.
