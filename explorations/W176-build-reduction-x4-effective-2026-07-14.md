---
artifact_type: exploration
status: exploration (W176 / TEAM BUILD-REDUCE; five personas inline, one worker, no sub-agents; one deterministic test 18/18 exit 0, positive controls first; the effective X4 operator BUILT by section pullback and the dimensional-reduction question answered per decisive content)
created: 2026-07-14
wave: W176
label: W176
posture: coherence-first BUILD sprint (Joe 2026-07-14); exploration grade; conditional register; honest grading; RUTHLESS self-verification; tri-repo gating STRICT
hypothesis: "Do NOT build the full 14-dim source-action operator D on Y14 = Met(X4).  REDUCE it to the effective 4-dim (X4) operator induced by D along a section sigma: X4 -> Y14 (the vertical Hessian of the induced action at the section), and ask which of the three decisive contents the one object is meant to settle -- (i) the interacting C-operator (bar (b), the tachyon sign), (ii) the generation-count declaration (the C2 = 155.3625 fermion-sector obstruction), (iii) Q's character (the source-action promotion flux) -- the effective operator CAPTURES, versus which are genuinely 14-dim (fiber / connection-curvature 2-form) data that the base-4 pullback LOSES."
title: "W176 VERDICT: PARTIAL / SPLIT, decisive per content.  The effective X4 operator is the VERTICAL HESSIAN of the induced action at the section -- exactly W130's native quadratic operator (spin-2 c_W = +2, spin-0 c_R = -4/9), Krein-graded by the POINTWISE DeWitt (6,4) fiber metric (W168).  (1) C-OPERATOR / bar (b): REDUCTION SUFFICES.  The graviton and scalaron ARE the vertical (fiber) fluctuations of the metric around the section, so the (6,4) fiber Krein signature is NOT lost -- it is the field-space metric of the effective 4-dim theory, available pointwise over X4 (W131 verified (9,5) = (3,1) + (6,4) pointwise).  At the induced-coefficient point the effective spectrum is MASS-SPLIT (Einstein channel a1 = 1/3 > 0 massless attractive graviton + a separate massive spin-2 ghost = Stelle, NOT the equal-mass conformal double-pole), so W169's criterion returns secular_Q1 = secular_Q2 = 0 => the interacting C-operator EXISTS => OPERATIVE => bar (b) CLEARS (branch A).  The connection-curvature 2-form enters D^2 only as SUBPRINCIPAL curvature terms (W131), which move the poles continuously and generically stay off the measure-zero resonant lattice, adding NO decisive content to the OPERATIVE bit.  So the C-operator question is a 4-DIM EFFECTIVE-theory question, and the terminal-wave perturbative result (W169/W172) closes it 4-dim-effectively.  (2) GENERATION-COUNT (C2 = 155.36) and (3) Q's CHARACTER: REDUCTION INSUFFICIENT.  C2 = ||Gamma . M_D . Pi_RS|| is a degree-1 escape symbol E(xi) = sum_{a=1}^{14} xi_a E_a, ISOTROPIC over all 14 cotangent directions of Y14, proven GLOBAL / NOT-local / NOT-an-index (W131), a Clifford-module invariant on the full RS bundle (14 x 128 = 1792, ker Gamma = carrier B = 1664) -- a section pulls back only the 4 BASE cotangent directions, capturing 4/14 of the escape and DROPPING the 10 fiber directions that carry the count.  Q's sign-changing character (W154 T4) is a boundary flux across the (9,5) q=5 finality frontier, whose 5 negative directions are 1 base-time + 4 fiber-negatives of (6,4); a base (3,1) section sees only 1 of 5.  So the generation-count and Q are genuinely 14-dim (fiber / connection-curvature) data reduction loses.  NET: reduction SUFFICES for bar (b) and INSUFFICIENT for the count and Q -- the connection-curvature 2-form is subprincipal-only for the C-operator sector and load-bearing (the C2 escape lives on it) for the count/Q sector."
grade: "exploration / conditional register throughout.  COMPUTED (deterministic, tests/W176_build_reduction_x4_effective.py, 18/18 exit 0, positive controls first): PC1 the W130/W154 induced anchors a0=2, a1=1/3, a2=-1/9 and c_R=-4/9, c_W=+2; PC2 the W168 DeWitt fiber signature G(eta,eta)=4-16*lambda=-12<0 at lambda=1 (conformal Krein-NEGATIVE), the (6,4) threshold lambda>1/4, the graviton (5,0) POSITIVE, the OPPOSITE relative signature => c_R_phys=+4/9, and the lambda=0 negative control (SAME => tachyon physical); PC3 the W169 secular map reproduced (equal-mass secular_Q2=125!=0 OBSTRUCTS-at-Q2, 2:1 secular_Q1=20!=0 OBSTRUCTS-at-Q1, incommensurate sqrt(2) secular=0 EXISTS); B1/B2 the effective mass-split spectrum at the induced point => secular_Q1=secular_Q2=0 => C-operator EXISTS/OPERATIVE; I1 the RS integer skeleton 14/128/1792/1664 and the 4/14 captured fraction; I2 the q=5 = 1 base + 4 fiber-negatives decomposition.  CITED (not re-derived): W130 (native operator, c_W=+2, c_R=-4/9, m_0^2/m_2^2 poles), W131 (the covariant operator, Gamma Gamma^dag = 14 I, ker Gamma = 1664, the C2=155.3625 anchor, subprincipal curvature), W154 (the promotion-gate boundary term T4, Q's character), W168 (the (6,4) fiber Krein signature and the c_R heal), W169 (the perturbative C-operator through Q2, the mass-ratio resonance criterion), W172 (the one live no-go is dynamical PT-breaking, itself a 4-dim effective-theory computation).  No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the debit count stays {1,3}; H41 unbuilt; H59/H61a OPEN; the E2 AF/AS fork carried, not closed.  No forbidden target {3,8,24,chi(K3),Ahat} assumed/inserted/hardcoded/divided-by."
construction: "program-native where the objects are GU's (Y14 = Met(X4), the section sigma: X4 -> Y14, the horizontal/vertical split T Y14 = (3,1)_base (+) (6,4)_fiber, the DeWitt vertical fiber metric, the induced |II|^2 action and its native channel couplings, the gamma-trace constraint Gamma and its C2 escape symbol, the (9,5) q=5 finality frontier).  Standard-field where the machinery binds any construction (the horizontal/vertical connection split of a fibre bundle; the DeWitt supermetric conformal-mode sign; the Bender-Brody-Jones / Mostafazadeh order-by-order C = e^Q P construction and its small-denominator/secular obstruction; the Stelle spin-0/spin-2 pole structure of a fourth-order operator).  Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md; the shadow-vs-law (branch A / branch B, W161/W168) and AF/AS (E2) forks are carried, not resolved.  Tri-repo gating STRICT: the reduction and the signature are GU-side; only the finality-grading ACTIVATION (whether the indefinite Krein form is the operative physical inner product) is TaF-owned and flagged; no cross-repo identity is asserted."
depends_on:
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W131-covariant-operator-y14-2026-07-14.md
  - explorations/W154-reverse-engineered-source-action-2026-07-14.md
  - explorations/W168-reduction-krein-signature-2026-07-14.md
  - explorations/W169-c-operator-perturbative-construction-2026-07-14.md
  - explorations/W172-interacting-c-operator-nogo-2026-07-14.md
  - canon/w2-y14-spin-structure.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W176_build_reduction_x4_effective.py
external_refs:
  - "DeWitt, Phys. Rev. 160 (1967) 1113 -- the supermetric on the space of metrics; the conformal-mode (fiber-negative) sign"
  - "Stelle, PRD 16 (1977) 953 -- fourth-order gravity spectrum: massless graviton + massive spin-2 ghost (the mass-split spectrum) block-diagonal from the spin-0 scalaron"
  - "Bender, Brody & Jones, PRL 89 (2002) 270401 -- the C = e^Q P construction whose order-by-order secular obstruction W169 evaluated"
  - "Bender & Mannheim, PRL 100 (2008) 110402 -- Pais-Uhlenbeck: unequal frequency real spectrum + positive metric; equal frequency non-diagonalizable, no positive metric (the conformal double-pole locus)"
  - "Besse, Einstein Manifolds (Springer 1987) Sec 1.G -- the O(p,q) isotypic decomposition of Sym^2(T*X)"
---

# W176 -- the effective X4 operator by section pullback, and the dimensional-reduction test

## 0. The move, and the one-line answer

The one object is `D`, the covariant record/RS operator on non-compact `Y14 = Met(X4)`
(symbol-built W131/W125; the analytic + connection-curvature-2-form layer unbuilt). It is meant to
settle three things at once: the interacting **C-operator** (bar (b), the tachyon sign), the
**generation-count** declaration (the `C2 = 155.3625` fermion-sector obstruction), and **Q's
character** (the source-action promotion flux). Every prior build attacked the full 14-dim object.
This team does the opposite: **reduce** `D` to the effective 4-dim operator induced along a section
`sigma: X4 -> Y14`, and ask, per decisive content, whether the reduction CAPTURES it or LOSES it.

The section `sigma: X4 -> Y14` is a choice of metric field `g(x)` on `X4` (a point in each fiber).
The tangent to `Y14 = Met(X4)` at `sigma(x)` splits, exactly as GU's own verified structure (W131;
canon/w2-y14):

```
T Y14 = HORIZONTAL (base, signature (3,1))  (+)  VERTICAL (fiber Sym^2(T*X4), signature (6,4)),
        (9,5) = (3,1) + (6,4)   pointwise on the Lorentzian locus (W131).
```

**The one-line answer: PARTIAL / SPLIT.** The graviton and scalaron are the VERTICAL (fiber)
fluctuations of the metric around the section, so the C-operator/tachyon content lives in the
vertical Hessian at the section -- a pointwise-over-X4 object -- and **reduction SUFFICES** for
bar (b). The generation-count and Q's character live on the full 14-dim cotangent / Clifford
structure (the escape symbol's 10 fiber directions; the finality frontier's 4 fiber-negative
directions), and **reduction is INSUFFICIENT** for them.

Five personas ran inline, one worker, no sub-agents. Deterministic test
`tests/W176_build_reduction_x4_effective.py`, **18/18, exit 0**, positive controls first.

## 1. Construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md), named

| Object | Construction used | Why |
|---|---|---|
| The reduction | vertical Hessian of the induced action at the section (linearize `D` on the fiber over `sigma`) | the graviton/scalaron ARE the fiber fluctuations; this is the natural KK-style effective operator |
| Effective operator | W130's native quadratic operator (`c_W=+2` spin-2, `c_R=-4/9` spin-0) | the pinned induced `|II|^2` construction's fluctuation operator IS the section's vertical Hessian |
| Field-space metric | the pointwise DeWitt `(6,4)` fiber metric (W168) | the Krein grading of the effective theory is the fiber signature, available pointwise (W131) |
| The C-operator | interacting `C = e^Q P`, order-by-order (W169), OPERATIVE iff the effective spectrum is off the resonant lattice | the existence bit is a spectral property of the effective operator |
| The shadow | branch A (the `|II|^2` shadow IS GU's effective action) / branch B (shadow separate; linear law `c_R=0`) | carried, not resolved (W161/W168) |
| Finality activation | whether the indefinite Krein form is the operative physical inner product | TaF-owned; flagged, not decided here |

## 2. Persona 1 -- KK / dimensional-reduction specialist: the section pullback and the effective operator

A section `sigma: X4 -> Y14` is the graph of a metric `g(x)`. The pullback `sigma^* D` decomposes
along the horizontal/vertical split of `T Y14`. The horizontal directions reproduce the base `(3,1)`
spacetime; the **vertical** directions are the fiber `Sym^2(T*X4)` fluctuations `delta g` of the
metric around `g(x)`. The physical content of bar (b) -- the graviton and the scalaron -- lives
entirely in the vertical sector: the graviton is the spatial transverse-traceless block
`Sym^2_0(R^3)`, the scalaron is the conformal/full-trace mode. So the **effective 4-dim operator is
the vertical Hessian of the induced action at the section**, and that is exactly W130's native
quadratic operator computed around flat space: TT channel `2 c_W k^4 - gamma_TT k^2 + 2 Lambda` and
spin-0 channel `18 c_R k^4 + 3 gamma_phi k^2 + ...`, with `c_W = +2`, `c_R = -4/9` (PC1).

The decisive KK observation: **the fiber Krein signature `(6,4)` is not lost under reduction,
because the fiber IS the effective theory's field space.** The DeWitt supermetric is the metric on
`Sym^2(T*X4)`; a section selects a point `g(x)` in each fiber, and the vertical Hessian at that
point carries the full pointwise `(6,4)` (W131 verified `(9,5) = (3,1) + (6,4)` pointwise on the
Lorentzian locus). This is the difference between "reduce and keep only the horizontal base" (which
WOULD lose `(6,4)`) and "reduce = linearize on the fiber over the section" (which keeps it). The
graviton/scalaron content forces the latter.

## 3. Persona 2 -- effective-field-theorist: the induced Krein QFT and the C-operator bit

The effective 4-dim theory is a Krein QFT: the spin-2 graviton (Krein-positive) and the conformal
scalaron (Krein-negative), graded by the pointwise `(6,4)` field-space metric. W168's translation
rule applies verbatim: the relative signature is OPPOSITE (conformal NEG x graviton POS), so
`alpha + beta < 0`, so `c_R_phys = -(4/9)(alpha+beta) = +4/9 > 0` -- the scalaron is HEALTHY, the
naive tachyon `c_R = -4/9` being the artifact of a positive-definite norm on a Krein-negative mode
(PC2; W130 cross-check `c_W=+2` vs `c_R=-4/9` is the shadow of the fiber signature). The `lambda=0`
negative control (PC2.NC) confirms this rides GU's genuine gimmel flip (`lambda>1/4 <=> (6,4)`), not
a convention.

The C-operator EXISTENCE bit then reduces, by W169, to whether the effective spectrum sits on the
measure-zero resonant lattice. At the **induced-coefficient point** the Einstein channel is
`a1 = 1/3 > 0` (an attractive, massless graviton) and the spin-2 ghost is a SEPARATE massive pole
`m_2^2 = -gamma/(2 c_W)` -- this is the Stelle **mass-split** spectrum, NOT the equal-mass conformal
double-pole (Bender-Mannheim's non-unitary Pais-Uhlenbeck case). Feeding a mass-split representative
into W169's criterion (B1/B2): `secular_Q1 = secular_Q2 = 0` -- **the interacting C-operator EXISTS
at the induced point => OPERATIVE => bar (b) CLEARS (branch A)**. W172's one live no-go (dynamical
PT-breaking, the ghost self-energy pushing the pole to the physical sheet) is itself an
effective-4-dim loop computation, not a fiber datum -- so it too is inside the effective theory. The
C-operator question is a **4-DIM EFFECTIVE-theory question**, and the terminal-wave perturbative
result closes it 4-dim-effectively.

## 4. Persona 3 -- GU-structure specialist: what the 14-dim fiber carries that X4 does not

Two of the three decisive contents do NOT reduce, and the reason is structural: they are not
vertical Hessians, they are invariants of the full cotangent / Clifford bundle.

**Generation-count / C2.** `C2 = ||Gamma . M_D . Pi_RS|| = 155.3625` is a degree-1 **escape symbol**
`E(xi) = sum_{a=1}^{14} xi_a E_a`, ISOTROPIC over all 14 cotangent directions of `Y14`
(W131/DERIVATION-PROGRESS: `C2(xi) = K|xi|`, 0.0% directional spread), proven GLOBAL, NOT-local and
NOT-an-index. It is a Clifford-module invariant on the full RS bundle: `Gamma Gamma^dag = 14 I`
(the gamma-trace tight frame over 14 directions), spinor dim 128 (`Cl(9,5) = M(64,H)`), RS bundle
`14 x 128 = 1792`, `ker Gamma = carrier B = 1664` (I1.1). A section `sigma` pulls back only the **4
base** cotangent directions, capturing `4/14` of the escape and dropping the **10 fiber** directions
that carry the generation count (I1.2). The count is a property of the whole 14-dim bundle, not a
pointwise vertical Hessian -- **GENUINELY 14-DIM, reduction INSUFFICIENT** (I1.3).

**Q's character.** The promotion flux `Q` (W154 T4) is a boundary flux across the `(9,5)` **q=5
finality frontier**. The 5 negative directions of `(9,5)` are `1` base-time `+ 4` fiber-negatives of
`(6,4)`; a section over the base `(3,1)` sees only 1 of the 5, so it is `4/5` blind to the
finality frontier (I2.1). Q's sign-changing character crosses the frontier in exactly the
fiber-negative directions the base-4 pullback drops -- **GENUINELY 14-DIM, reduction INSUFFICIENT**
(I2.2).

The clean statement of the connection-curvature 2-form's role, per sector: it is **subprincipal-only**
for the C-operator sector (W131: the curvature terms `rho(R)` of `D^2` are subprincipal, moving the
poles continuously without changing the OPERATIVE bit), and it is **load-bearing** for the
count / Q sector (the C2 escape symbol lives on the full cotangent bundle the 2-form connects). This
is why the same reduction SUFFICES for one content and LOSES the other two.

## 5. Persona 4 -- symbolic engineer: the test (18/18, exit 0)

`tests/W176_build_reduction_x4_effective.py`, deterministic (numpy + sympy, no RNG), positive
controls first. Structure:

- **PC1** the induced anchors: `F(R) = 2 + R/3 - R^2/9 => a0=2, a1=1/3, a2=-1/9`; `c_R=-4/9`,
  `c_W=+2` (W130/W154).
- **PC2** the DeWitt `(6,4)` fiber signature: `G(eta,eta) = 4-16*lambda = -12 < 0` at `lambda=1`
  (conformal Krein-NEGATIVE); threshold `lambda>1/4 = (6,4)` condition; graviton `(5,0)` POSITIVE;
  relative OPPOSITE `=> c_R_phys=+4/9`; and the `lambda=0` NEGATIVE control (`+4>0`, SAME, tachyon
  physical -- the flip is not a convention).
- **PC3** the W169 secular map REPRODUCED: equal-mass `secular_Q2 = 125 != 0` (obstructs at Q2), 2:1
  `secular_Q1 = 20 != 0` (obstructs at Q1), incommensurate `sqrt(2)` `secular = 0` (exists) -- the
  same two-Krein-mode `build`/`secular`/`solve_comm` machinery as W169.
- **B1/B2** the effective mass-split spectrum at the induced point => `secular_Q1 = secular_Q2 = 0`
  => C-operator EXISTS/OPERATIVE; the subprincipal-curvature argument recorded.
- **I1/I2** the RS integer skeleton `14/128/1792/1664` and the `4/14` captured fraction; the
  `q=5 = 1_base + 4_fiber-negative` decomposition.
- **SPLIT** the per-content verdict.

Positive controls reproduce the two mandated anchors -- W130's `a1=1/3` (PC1) and W169's
generic-operative result at the induced/mass-split point (B2, secular = 0) -- before any new claim.

## 6. Persona 5 -- adversarial skeptic: steelman REDUCTION-INSUFFICIENT for bar (b)

The North Star is high-exploit; the skeptic must attack the SUFFICES side (do not let the team
default to the clean answer that closes bar (b) cheaply).

**Steelman: the C-operator is genuinely 14-dim and the reduction fakes closing it.** The strongest
version: the mass-split-vs-equal-mass fork W169 turns on is set by GU's ACTUAL linearized operator,
which is a 14-dim fiber object; the effective 4-dim masses are only the leading symbol, and the
connection-curvature 2-form (unbuilt) could shift the ghost mass ONTO the equal-mass conformal
double-pole (the NOT-OPERATIVE locus). If the true fiber operator is conformal-type (`Weyl^2`-only,
double pole at `p^2=0`), the reduction's "mass-split" is an artifact of truncating the 2-form.

**Where it lands (honest).** This is a REAL residual, and it is exactly the branch-A caveat. Two
bounds hold it: (i) W131 proved the curvature terms are SUBPRINCIPAL -- they cannot change the
degree-1 principal symbol / characteristic variety, so they move the poles CONTINUOUSLY and the
resonant lattice is measure-zero; landing exactly on the conformal double-pole is non-generic and
would itself be a 14-dim fine-tuning, not a default. (ii) The induced Einstein channel is
`a1 = 1/3 > 0` (attractive, massless graviton) -- a GENUINE Einstein pole at `p^2 = 0`, NOT a
conformal double-pole (which has NO `a1` Einstein term); the induced action is `|II|^2` (Willmore),
not `Weyl^2`-only. So the effective operator is structurally mass-split, and the 2-form would have to
CANCEL the `a1 = 1/3` Einstein term to reach the conformal locus -- which the subprincipal theorem
forbids. **The skeptic concedes SUFFICES for bar (b), conditional on branch A and on the
grading-activation (TaF) gate**, and holds that the genuine 14-dim residual is the count and Q, not
the C-operator.

**The skeptic's win.** The reduction does NOT close the generation-count or Q -- and the temptation
to say "the effective theory sees the fermions too" is FORBIDDEN: the count is the isotropic escape
symbol on all 14 cotangent directions and the finality frontier is `4/5` in the fiber; a base-4
pullback provably drops them. The honest deliverable is the SPLIT, not a blanket SUFFICES.

## 7. Verdict (the return fields)

| Content | Reduction verdict | Basis |
|---|---|---|
| interacting **C-operator** / bar (b) | **4-DIM-EFFECTIVE -- REDUCTION SUFFICES** | vertical Hessian at the section = W130 operator, Krein-graded by pointwise `(6,4)`; mass-split at the induced point => W169 `secular=0` OPERATIVE; 2-form subprincipal-only |
| **generation-count** (C2 = 155.36) | **GENUINELY 14-DIM -- REDUCTION INSUFFICIENT** | isotropic escape symbol on all 14 cotangent directions; base-4 pullback captures `4/14`, drops 10 fiber directions; global/not-local/not-an-index Clifford invariant |
| **Q's character** | **GENUINELY 14-DIM -- REDUCTION INSUFFICIENT** | boundary flux across the `q=5` finality frontier = `1` base + `4` fiber-negatives; base section `4/5`-blind |

**SWING RESULT: PARTIAL.** `REDUCTION-SUFFICES` for the interacting C-operator: the effective 4-dim
vertical-Hessian operator at the induced-coefficient point, Krein-graded by the pointwise `(6,4)`
fiber signature, is mass-split (Einstein `a1=1/3>0` + separate massive ghost = Stelle), so W169's
perturbative result returns `secular_Q1 = secular_Q2 = 0` => the interacting C-operator EXISTS =>
OPERATIVE => bar (b) CLEARS (branch A, conditional on the TaF grading-activation gate). The terminal
wave's `LEANS operative` becomes, under reduction, `operative at the induced point 4-dim-effectively`.
`REDUCTION-INSUFFICIENT` for the generation-count (the `C2 = 155.36` fermion wall) and Q's character:
both are genuinely 14-dim (fiber / connection-curvature) data -- the 10 fiber cotangent directions of
the escape symbol, the 4 fiber-negative directions of the finality frontier -- that the base-4
pullback drops. The connection-curvature 2-form is subprincipal-only for the C-operator sector and
load-bearing for the count / Q sector; **the exact 14-dim piece the reduction needs is the full RS
cotangent bundle carrying `C2` and the finality-frontier fiber-negative directions carrying `Q`.**

## 8. What this does NOT do

- No full 14-dim operator built (by design; that is the object reduction avoids). The analytic layer
  and the connection-curvature 2-form on non-compact `Y14` stay unbuilt (W131 residual).
- No branch A/B selection: SUFFICES for bar (b) is stated on branch A (the `|II|^2` shadow IS GU's
  effective action); on branch B the linear law has `c_R = 0` and the tachyon is absent, so bar (b)
  is untroubled either way, but the A/B selection needs the `I1B -> X4` shadow-vs-law reduction map
  (unbuilt, W161/W168), which this wave does not supply.
- No grading-activation claim: whether the indefinite Krein form (not a positive-definite `H_C+`
  restriction) is the operative physical inner product is the TaF-owned finality reading; the sign
  and the reduction are GU-side, the activation is flagged.
- No resolution of W172's dynamical PT-stability (the physical-sheet-vs-second-sheet fate of the
  ghost pole = H59's W48 object); it is named as a 4-dim effective computation, not done here.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the debit count stays
  `{1,3}`; H41 unbuilt; H59/H61a OPEN; the E2 AF/AS fork carried. Tri-repo gating strict: "record /
  substrate / finality / promotion" stay local postulate labels (W136 sense); MEASURE is
  time-as-finality's, issuance is temporal-issuance's; GU owns the field-equation / emergent-metric
  math only. Zero em dashes in paper-facing text. W138 battery honored (the reduction binds to
  GU-specific objects -- the verified `(9,5)=(3,1)+(6,4)`, the `Gamma Gamma^dag=14I` tight frame, the
  `C2` escape isotropy, the `q=5` frontier; the tempting story it FORBIDS is "the effective theory
  sees the fermions too", forbidden by the 4/14 escape-symbol capture).

*Filed 2026-07-14 by Team BUILD-REDUCE (W176). Coherence-first BUILD sprint; exploration grade;
RUTHLESS self-verification. Five personas inline in one worker (KK/dimensional-reduction specialist,
effective-field-theorist, GU-structure specialist, symbolic engineer, adversarial skeptic); no
sub-agents. Reproducible: `python -u tests/W176_build_reduction_x4_effective.py` (18/18, exit 0;
positive controls first). No canon movement; the debit count unmoved; the swing is a PARTIAL that
names, per decisive content, which side of the 4-dim/14-dim line it sits on.*
