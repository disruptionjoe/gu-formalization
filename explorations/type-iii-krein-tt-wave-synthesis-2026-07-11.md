# Type-III Krein-TT wave -- orchestrator synthesis (decider)

Five branches attacked the frontier that turns the observer conjecture conditional->theorem (develop type-III
Krein Tomita-Takesaki for a non-definitizable ghost, or equivalently flip the horn via eta_C). Tests W89-W93 all
exit 0. This synthesis renders the verdict; the two present-not-decide branches (4, 5) are weighed, not obeyed.

## 1. The frontier is REAL (not dissolved by eta_C)

Branch 1 computed eta_C (the shared-Z_h EH x Weyl off-diagonal TT trace, W88's horn decider) across three
regulator families. Result: sign(eta_C) is fixed by the Z_h-SCHEME, not the regulator family (regulator-robust
WITHIN a scheme). EH-adapted scheme -> eta_C>0 -> f_2^2* lifted (HORN Q); Weyl-adapted scheme -> eta_C=0 ->
f_2^2*=0 (HORN K). The physically-correct scheme is Weyl-adapted -- the graviton is ONE field whose physical Z_h
is set by its leading UV (4th-order Weyl) kinetic term, so eta_h -> eta_Weyl -> 0 -> eta_C=0; the EH-adapted
eta_C>0 is a 2nd-order-cutoff-on-a-4th-order-propagator artifact. **Honest lean: HORN K, the ghost is genuinely
kept, the frontier is real.** eta_C did NOT dissolve it (the cheap ~15-25% dissolution did not fire). The one
computation that would make HORN K unconditional is named: the off-diagonal EH x Weyl TT Hessian with a single
self-consistent graviton Z_h whose eta_h is computed from the leading 4th-order kinetic term.

## 2. THREE independent routes CONVERGE on ONE residual: infinite-rank definitizability

This is the wave's central result -- the pre-registered "decisive map" outcome.

- **Branch 2 (Shulman quasivector, pure math):** a kappa-quasivector extends Shulman's Pi_1 conjugation to every
  FINITE rank (rank-2 J built explicitly, all four modular properties, residuals <1e-15) -- but the method's
  reach is CO-EXTENSIVE with definitizability. At GU's infinite rank the truncated patch norm diverges (the
  metric inverse is unbounded). Reduces with NO REMAINDER to: is GU's Delta definitizable? Closes the quasivector
  route as an independent crossing.
- **Branch 3 (algebraic/relative modular skeleton, highest conceptual value):** positivity enters Tomita-Takesaki
  at EXACTLY ONE place -- the eta-positive square root Delta^{1/2} in the polar decomposition S = J Delta^{1/2},
  i.e. the CONJUGATION J itself. Everything else is positivity-free and SURVIVES: the modular FLOW Delta^{it}
  (Gottschalk), the algebraic KMS relation, the Connes cocycle, and the observer-record<->section map. The
  relative move (J relative to the observer's selected state) removes UNIQUENESS but NOT POSITIVITY -- the
  relative modular operator is still eta-positive, still needs a positive reference weight. So a relative J exists
  IFF Delta is definitizable: relative and absolute J degrade in lockstep. Reduces to the SAME residual.
- **Branch 5 (adversarial no-go):** theorem-grade that any J tied to the flow through a single GLOBAL metric
  operator is UNBOUNDED for a non-definitizable Delta (no eta-positive Delta^{1/2} -> J = S Delta^{-1/2}
  unbounded). Rigorous for the vacuum-polar route AND any global-relative J. INCOMPLETE with a NAMED GAP (its
  own steelman found it): a SECTORIAL/relative J localized to a definitizable Pi_kappa SUB-SECTOR sees a bounded
  metric and admits a bounded J. So the global route is walled; the sectorial route is open.

**Convergence:** all three collapse onto the single decidable question -- **is GU's modular Delta = S^+ S
definitizable at infinite rank (equivalently, does a positive reference weight exist)?** The global modular
conjugation J is RIGOROUSLY WALLED under HORN K (non-definitizable), from three independent directions.

## 3. The single surviving crack, and what genuinely advanced

- **The surviving crack (precise):** a SECTORIAL / relative modular conjugation localized to a definitizable
  sub-sector (Branch 5's named gap = Branch 3's flow/section half + a bounded sub-sector J). The open question is
  whether the observer's firewall needs the WHOLE mode tower (walled) or a SUB-SECTOR (open). This is the sole
  route Front A has left, and it is the next decidable target.
- **What genuinely advanced (Branch 3, positivity-free half):** the modular FLOW + Connes cocycle + the
  observer-record<->section map all survive without a positive state -- so the observer's VALUE-SELECTION is
  realized algebraically even where the conjugation is not. The value-selection half of the conjecture is on
  firmer ground than the firewall-conjugation half.
- **The abstract Lawvere theorem STANDS regardless** (all five branches confirm): it needs only the fixpoint-free
  J^2=1 on the 2-element grading labels, never the flow-tied Delta^{1/2}. GU-independent, unconditional.

## 4. The deflationary fork (Branch 4, weighed)

A graded S-matrix reaches physical positivity ONLY by REMOVING the ghost (fakeon/Lee-Wick) = HORN Q = the firewall
deflates (nothing genuinely indefinite to grade). Save-of-physics XOR genuine-firewall is ONE dichotomy. GU lands
on HORN K (repo-native, Branch 1 lean), so the cheap removal is DISFAVORED for GU -- the firewall is genuine, and
its price is the real (walled-global, sectorial-open) modular frontier. Weighed, not obeyed: the deflationary
route is available only under the artifact-scheme (HORN Q) the physics disfavors.

## 5. Net verdict
- Frontier REAL (HORN K lean, eta_C did not dissolve).
- The GLOBAL type-III Krein modular conjugation is RIGOROUSLY WALLED (three converging routes, under HORN K +
  non-definitizability) -- a genuine open frontier of mathematics, confirmed absent from the literature.
- ONE surviving crack: a SECTORIAL/relative J on a definitizable sub-sector (the next decidable question).
- The observer's VALUE-SELECTION (flow/cocycle/section half) is realized algebraically WITHOUT positivity -- a
  real advance.
- The ABSTRACT observer/symmetry-breaking theorem STANDS unconditionally.
So the conjecture's status tightens: mechanism genuine + abstract theorem proven + value-selection algebraically
realized; the full physical firewall-conjugation is walled globally with one sectorial crack left. The wave
produced the decisive MAP it was scoped to produce; it did not (as honestly pre-registered at ~5-15%) construct
the J.
