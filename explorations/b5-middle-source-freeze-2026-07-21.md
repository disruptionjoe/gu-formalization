---
title: "B.5 middle-differential source freeze: public material does not delimit a complete native first-order class"
status: source_gap
doc_type: exploration_disposition
created: 2026-07-21
run_ref: RUN-20260721-003510-repository-work-cycle-cai-hourly
lane: "1"
outcome: B5-MIDDLE-SOURCE-GAP
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
external_actions: github_versioning_only
---

# B.5 middle-differential source freeze

## Question and stop rule

Can the available source material freeze the rolled B.5 matrix, types,
filtration, lower-order terms, domain and boundary data, real structure, and
super-IG role tightly enough to enumerate a **complete native first-order
class**?

Answer: **no**. The predeclared outcome is `B5-MIDDLE-SOURCE-GAP`. The source
fixes an arena and a degree skeleton, but it does not fix a complete operator
class. The mirror, native-closure, planted-control, symbol-exactness, and
global-cohomology gates therefore do not run.

## Construction fork

This disposition concerns the program-native GU fork: the full Dirac carrier
on the `(9,5)` / indefinite geometry, its Clifford contraction and RS sector,
and the author-described rolled construction. Standard BRST/BV, positive-
Hilbert polarization, Higgs removal, APS selection, replicated families, or a
standalone Rarita-Schwinger completion can define conventional comparison
models. None source-license the missing GU differential, and none can rescue
or kill this frozen native class by substitution.

## Frozen source ledger

| item | public source support | frozen reading | completeness effect |
|---|---|---|---|
| Base and carrier | Draft receipt for Sec. 9.3, eq. 9.16 (PDF p. 46); TOE transcript 02:41:57 | Work is on `Y^14`; the fermionic fields use the full Dirac bundle `S`, with `nu in Omega^0(Y,S)` and `zeta in Omega^1(Y,S)`. The chimeric bundle and indefinite `U(64,64)`-type carrier are part of the native arena. | Fixes types of the two field slots, not their stabilized action. |
| Degree skeleton | Draft receipt for eqs. 9.18-9.19 and diagram 10.10; TOE 02:42:28-02:43:30 | The native skeleton has form degrees `0 -> 1 -> 13 -> 14`. The spoken middle shape is `1 -> 2`, an unnamed contraction back to degree `1`, then Hodge star to degree `13`. | Fixes only a degree-level composition shape. The contraction, coefficients, and connection dependence are not fixed. |
| One-step roll | TOE 02:42:55 | Rolling produces a two-by-two operator shape with a stated zero in the southeast corner and a prospective seesaw interpretation. | The other entries, adjoints, signs, grading convention, and operator domain are not released. |
| Two-connection `D^2` claim | TOE 02:44:06-02:45:43; source-mining Section 3 | A separate spoken fragment names two connections and four tokens: `D_A`, `F_B`, identity, and `D_B`, with two negative signs said to occur in the second column. It is claimed to produce an on-shell cyclic cohomology theory. | The transcription does not support a reliable slot/sign assignment. The author explicitly says the construction was never released. It cannot be merged silently with the one-connection roll. |
| Observer branching target | `observation-to-family-b5-campaign-2026-07-20.md` | The associated-grade target ledger is exactly `3E_+ + 3E_- + X`, `dim_C X = 1536`. | Fixes the output test, not the differential that must acyclicize `3E_- + X`. |

Source grades remain separated:

- manuscript-grounded: the full-Dirac `Omega^0(S) + Omega^1(S)` field arena
  and the four-slot deformation skeleton;
- podcast-level: the generation conclusion, the rolled southeast zero, and
  the cyclic two-connection `D^2` fragment; and
- repo reconstruction: every proposed BV/BRST completion, physical quotient,
  mirror mapping cone, or family pushforward not present in those sources.

## Missing data audit

The public material does **not** determine:

1. a reliable two-by-two matrix with typed source and target for every entry;
2. the exact middle contraction, its normalization, or its connection
   dependence;
3. a BV/ghost/antifield filtration or a square-zero differential off shell;
4. lower-order and curvature terms, including which equations make the
   claimed on-shell square vanish;
5. an admissible operator domain, boundary/asymptotic condition, or spectral
   section;
6. the native real-form projection and Krein adjoint/closure identities;
7. the super-IG action on the rolled complex or its master/Ward identity;
8. a mirror-breaking datum or explicit contractible pairing for
   `3E_- + X`; or
9. a source-owned families pushforward, physical cohomology object, or
   deformation retract.

Because several of these choices change the allowed principal symbols, the
sources do not merely leave coefficients free inside a known finite class.
They fail to delimit the class itself. Enumerating "all compatible" symbols
would therefore enumerate a repo-designed completion rather than the native
GU construction.

## Disposition

`B5-MIDDLE-SOURCE-GAP` fires.

This is not `B5-MIDDLE-OBSTRUCTION`: no complete source-compatible class was
available to exhaust. It is not `B5-MIDDLE-CONSTRUCTION`: the public fragments
do not supply a global differential or deformation retract. It also does not
change any GU claim, canon verdict, paper status, or public posture.

## Reopen condition and handoff

Reopen this item only when a stronger primary packet supplies enough of the
following together to delimit a class: the typed matrix and signs, exact
middle contraction, filtration and lower-order terms, domain/boundary data,
real/Krein closure, and super-IG/BV identity. A released formula matching the
two-connection on-shell signature is the clearest trigger.

Until then, do not invent a favorable differential, merge the two spoken
matrix fragments, import a conventional physics completion, run downstream
`S_3` transport, or treat source absence as a no-go. Lane 1 returns to the
next independently eligible portfolio item after reranking.

## Evidence read

- `lab/sources/claim-mining-toe-weinstein-2026-07-20.md`, especially Section
  3 (`UNRELEASED-CLAIM` and complete stated-property list);
- `lab/sources/transcripts/toe-weinstein-gu-40-years.md`, 02:41:57-02:45:43;
- `explorations/dk-chirality-fork-2026-07-20.md`, manuscript receipts for Sec.
  9.3, eqs. 9.16, 9.18-9.19, and diagram 10.10;
- `explorations/observation-to-family-b5-campaign-2026-07-20.md`; and
- `GEOMETER-VS-PHYSICS-OBJECTS.md`.
