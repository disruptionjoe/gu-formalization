---
title: "Weinstein-guided source action and external datum: executed opening swing and ten-swing scaffold"
status: active_research
doc_type: construction_scaffold
created: 2026-07-31
branch: agent/weinstein-guided-source-action
run: lab/process/runs/GUH-20260731T130016Z-eric-guided-source-action/run-plan.md
probe: tests/channel-swings/weinstein_guided_source_action_probe.py
grade: "GUIDANCE-CONDITIONED SCAFFOLD WITH EXACT FINITE OPENING RECEIPT. The tilted-cocycle, double-coset displacement, invariant source shadow, first variation, chart transport, and Ward identity pass 14 exact rational-matrix checks; four planted false claims fail. No global quotient, native bosonic Shiab, full source action, VEV, Higgs/Yukawa, stationarity, CME, mass, index, count, cosmological amplitude, or prediction is claimed."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Weinstein-guided source action and datum scaffold

## Result first

The source review did not redirect the construction to a different goal. It
identified a more specific candidate grammar for one branch:

\[
(\epsilon,\varpi)\in \mathrm{IG}
\quad\Longrightarrow\quad
T_\omega=\varpi-\epsilon^{-1}d_0\epsilon
\quad\Longrightarrow\quad
I_1^B[T_\omega,F]
\quad\Longrightarrow\quad
(E_\varpi,E_\epsilon).
\]

The first guided swing is executed. In an exact noncommutative finite shadow,
the tilted embedding is a homomorphism, the displacement is invariant under
the left tilted copy and adjoint under the right copy, and the action has the
expected Ward identity. The planted wrong Maurer--Cartan coefficient fails.

The swing also finds the first important correction:

\[
E_T=K(F)+\kappa T,
\qquad
E_a=\operatorname{Ad}_g(E_T).
\]

Thus the displacement field `T` is **not literally the Euler covector** in a
generic curvature-coupled action or generic affine-connection chart. It equals
the Euler representative only in the planted pure-quadratic control. This
sharpens the current conditional in
`canon/dark-energy-theta-divergence-free.md`: the full construction needs the
curvature contraction, pseudo-musical, and chart transport, not the sentence
“theta is the Euler derivative.”

## Executed opening swing: `E0`

The exact fixture uses

\[
\mathrm{IG}=G\ltimes V,
\qquad
(g,a)(h,b)=(gh,a+\operatorname{Ad}_g b),
\]

and a reference-connection coboundary

\[
c(g)=A_0-\operatorname{Ad}_g A_0,
\qquad
\tau(g)=(g,c(g)).
\]

Because

\[
c(gh)=c(g)+\operatorname{Ad}_g c(h),
\]

`tau` is a homomorphism. Define

\[
\Theta(g,a)
=\operatorname{Ad}_{g^{-1}}(a-c(g)).
\]

Then exactly

\[
\Theta(\tau(k)(g,a))=\Theta(g,a),
\qquad
\Theta((g,a)\tau(h))
=\operatorname{Ad}_{h^{-1}}\Theta(g,a).
\]

The coefficient-one subtraction is selected: replacing `c(g)` by `2c(g)`
breaks left invariance on the noncommuting fixture. For

\[
I(\Theta,K)=\langle\Theta,K\rangle
+\frac\kappa2\langle\Theta,\Theta\rangle,
\]

the exact central difference gives

\[
\delta I
=\langle\delta\Theta,K+\kappa\Theta\rangle.
\]

The simultaneous adjoint orbit variation vanishes by trace cyclicity, while
transport from `Theta` to the affine translation coordinate sends the Euler
covector through `Ad_g`. Four planted false claims ensure that the successes
are not produced by a permissive matcher.

Receipt:
`tests/channel-swings/weinstein_guided_source_action_probe.py`,
`14 exact + 4 planted = 18 PASS`.

This is a branch-independent algebraic lemma discovered through guided
candidate selection. Its global interpretation remains guidance-dependent.

## Efficient ten-swing sequence

The order is chosen for information gain. Each swing either closes a required
arrow or kills an entire downstream family before expensive physics is fitted.

**Standing Eric-lane recheck rule.** If a swing finds an unexpected missing
map or apparent failure, it must recheck the verified Weinstein primary set
before treating that result as the guided construction's boundary. A source
route is guidance to build and debit, not evidence that it works. An absent
route earns a timestamped negative receipt. G3's first use of this rule found
that Weinstein repeatedly describes four-dimensional physics by pulling back
ambient fields/equations along a metric section; the checked sources do not
supply the repo's separate bulk-plus-defect action. G4 therefore prioritizes
the pullback/retract branch, with the defect action retained as a repo-originated
comparator.

| swing | build, not diagnosis | decisive output | predeclared kill / information gain |
| --- | --- | --- | --- |
| E1 — global tilted-field packet | Fix one left/right convention for `IG`, `tau_+`, `B_omega`, `A_omega`, and `T_omega` on `Y^14`; prove transition/descent laws and observation-slice reduction. | A global typed field packet matching the draft convention to the repo convention. | Kill a convention if `tau_+` is not a homomorphism, `T_omega` fails descent, or right-`H` reality fails. This prevents every later equation from being coordinate rhetoric. |
| E2 — source action first variation | Write the draft's full first-order bosonic term with its `1/2,1/3` completion and compute `E_varpi`, `E_epsilon`, and boundary terms on the E1 packet. | Exact variational covectors and the genuine gauge identity; replace the canon's “theta = Euler” shortcut by the correct transported relation. | Kill if the action is not differentiable on the declared fields, if the epsilon response is omitted, or if the claimed divergence identity does not follow. Highest-information next swing. |
| E3 — N3 current/Riesz bridge | Put `E_varpi` beside N3's `J_D`, `J_F`, Hodge/Krein pseudo-musical, and the three bridge-current choices. Derive, do not select verbally, which current enters. | One connection equation in common types; a Noether identity deciding whether direct, total, or a soldered current is meant. | Kill any current choice that self-cancels required matter response or violates the Ward identity. Settles the fork N3 actually exposed. |
| E4 — native bosonic Shiab reopener | Continue the moving-`epsilon` full-adjoint grade-flip candidate from RB1b and build the density-dual map needed by E2. | `Omega^2(ad P) -> Omega^13(ad^* P)` with right-`H`, Krein, moving-soldering covariance, and cyclic/transgression tests. | Kill at zero Hom space, failed descent, failed cyclic identity, or nonzero forbidden epsilon response. Run in parallel with E3 after E1; it is the source-native branch's first blocker. |
| E5 — shared spinless VEV channel | Project the same `T_omega` field into the scalar/spinless observation-slice channel and place it in both the cosmological and zero-order fermion terms. Compose the Krein pairing before testing chirality. | A single candidate field, not two fitted fields, feeding mass and dark-energy slots with an explicit VEV/state placeholder. | Kill if the scalar channel is absent, the Krein bilinear misses the physical channel, or two independent amplitudes are needed. This tests the source's strongest “one shared field” suggestion. |
| E6 — Higgs/Yukawa realization | Starting from the ad-valued one-form component, derive its representation after the stabilizer/observation reduction, kinetic term, potential, and `P0/rho/Y_K/Y_C/C`-real fermion bilinear. | Complete `SA-Y1`/zero-order placement candidate, including the actual Standard Model scalar representation. | Kill the slogan-to-Higgs jump if it cannot supply the doublet, Yukawa incidence, Krein reality, or gauge invariance with positive surplus. |
| E7 — external datum attachment and surplus | Attach P1/P2/P3 only at the exact equations that consume them; determine P2's type and test whether P1 serves two slots without hidden duplication. Rank independent constraints and freedoms. | Minimal datum ledger and constraint-surplus certificate for the guided action. | Kill any datum that merely labels the desired answer, any P3 leak from multiplicity/index, or any fit with nonpositive surplus. This is where the construction learns whether shaping was informative. |
| E8 — moving geometry and trace reversal | Restore the moving section/soldering orbit, full-20 curvature, parent fields, and the trace-reversed `Sym^2 T^*X` fibre in the coupled `A,U,P,epsilon,s,g` equations. | Coupled connection, distortion, soldering, and physical-Ricci equations on the native `(9,5)`/`(6,4)` geometry. | Kill a fixed 14-plane pretending to be fully `Sp(32,32;H)` covariant, any raw `(7,3)` Frobenius substitution, or a trace term with the wrong fibre reversal. |
| E9 — cosmology and PP3 confrontation | Derive the observation-slice stress tensor, state/VEV prescription, amplitude owner, and the reduced `w(z)` family from E2/E5/E8. Then compare to frozen PP1/PP3 without refitting their thresholds. | Whether the Eric-guided source actually supplies PP3's missing `f0`/B.5 slot and whether its curve lies on the frozen locus. | Kill if amplitude remains an unowned fit, if the stress is not divergence-compatible, or if a future-eligible prediction lands outside K1--K4. Existing DESI inputs get no confirmation weight. |
| E10 — graded IG and integrated hostile review | Only after the action exists, lift the anchor-scale odd bracket to the derivative-level `tau_+` packet and test action invariance/Jacobi/curvature. Then run the same five-leg council against the independent branch. | A conditional super-IG symmetry of the action or a precise no-go; final surplus and branch comparison. | No generation count from the odd bracket, kernel, or complex length without a new typed observable. Kill on derivative cocycle, curvature obstruction, Ward failure, or count leakage. |

## Dependency and parallelism

```text
E1 -> E2 -> E3 -> E5 -> E6 -> E7 -> E8 -> E9 -> E10
 |          |
 +-> E4 ----+
```

E4 can run beside E3 once E1 freezes the convention. Claim/literature audit is
now background only; it should not displace construction. E10 is deliberately
late because the repo already knows a finite graded algebra can close. What is
unknown is whether the written action realizes it, and the action must exist
before that question is informative.

## DESI/PP3 placement

The TOE episode's DESI statement is qualitative and post-initial-hint:
Weinstein says the new DESI tendency, **if it continues**, would favor variable
dark energy, and presents a responsive distortion/VEV as GU's mechanism. PP3
is the repo's stronger prospective sharpening:

- shared arena: dynamic dark energy rather than exact `Lambda`;
- PP3-only content: non-phantom rail, thawing direction, one-parameter locus,
  slope `[-1.33,-1.00]`, curvature invariant, segment, and K1--K4;
- evidential rule: DESI DR2 was consumed in PP3's construction; only eligible
  post-DR2 data score the frozen packet.

Therefore E9 asks whether the **newly built source action** emits the PP3 family.
It does not use the TOE statement to validate PP3 retroactively.

## Current branch comparison

The independent branch and Eric branch are now doing complementary searches:

| independent lane | Eric-guided lane |
| --- | --- |
| lets the N3/RB equations generate the next candidate | lets verified author guidance choose `IG/tau_+/T_omega/I_1^B` first |
| source resemblance is incidental | source resemblance is an explicit debit and provenance label |
| strongest current fork is current placement plus moving soldering/full-20 geometry | strongest current fork is the transported Euler/Noether relation plus native bosonic Shiab |
| same P1/P2/P3 and five-leg gates | same P1/P2/P3 and five-leg gates |

This is exactly the controlled comparison wanted: the goal and tests stay
fixed while the candidate-generation information differs.

## Boundary

No claim is made that the finite fixture proves the global double-coset
equivalence or source action. No stationarity, CME, vacuum, Higgs, mass,
anomaly, index, count, amplitude, or PP3 result moves. The next full swing is
E1+E2, with E4 allowed in parallel after E1 freezes the field convention.
