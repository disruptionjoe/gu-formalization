---
artifact_type: exploration
status: complete
doc_type: source_claim_adjudication
created: 2026-08-24
work_item: SC-SIG-53-SOURCE-SIGN-UNCERTAINTY
target_claim: SC-SIG-53
target_claim_verdict: ADHERED
grade: SOURCE_UNCERTAINTY_ADHERED__NO_HORN_SELECTION
canon_verdict_change: none
canonical_effect: none
priority_change: none
scripts:
  - tests/channel-swings/source_claim_residual_typing_wave_probe.py
---

# SC-SIG-53: the source owns uncertainty, not a signature horn

## Opening and source custody

The primary Portal transcript at `02:12:34` says: “when we moved from
Euclidean metric to Minkowski metric, we seem to be off by a sign somewhere,
or I could be mistaken.” The same breath separately says that propagation in
fourteen dimensions remains to be worked out. This artifact types the former
sentence only. It does not merge the signature and propagation questions.

The exact source object is an author-voiced uncertainty about a continuation.
It is not a displayed sign equation, a selected real form, an action, a metric
pairing, or a resolver. The sentence therefore supplies a reason the source
cannot be relayed as already settled, but supplies no answer to the fork.

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

## What the existing resolver work does and does not inherit

The 2026-08-11 `SIGNATURE-AMBIENT` resolver design is prior art. It reduces the
well-posed part of the comparison to relative-sign and trace-regime bits, then
proposes exact balance checks over the draft display and spoken blocks. Its
hostile review explicitly leaves the executing disposition to a later wave.

`SC-SIG-53` strengthens that design's source custody: the author himself did
not present the Euclidean-to-Minkowski sign as closed. But the quote cannot
choose among the design packet's outcomes. In particular it cannot:

- locate the suspected sign in the horizontal block, vertical form, total
  convention, Clifford carrier, or an omitted action coefficient;
- select unordered ambient signature `{7,7}` or `{9,5}`;
- resolve `SIGNATURE-AMBIENT` or modify `REAL-CLIFFORD-FORM`;
- transfer a K95 certificate to K77, or the reverse; or
- turn a source uncertainty into evidence against either horn.

Input currency was checked against the correction registry. The design packet
predates the later base-metric and source-layer corrections, so it is consumed
only through its already explicit object table and no-background-metric fence.
Nothing in this packet relies on the corrected readings those entries forbid.

## Adherence disposition

The row moves `UNTYPED -> ADHERED` because the repository now does the exact
thing the uncertain source sentence requires: it cites the uncertainty, keeps
the continuation open, and prevents the sentence from being compressed into a
horn. “Adhered” grades source handling, not the truth of either signature.

The first future condition is unchanged: a resolver must exhibit a typed
source display or action-owned relative-sign construction and pass its planted
mixed-notation controls. Mere repetition of this uncertainty is not progress.

## Hostile review and closing

Strongest overclaim: “the source admits K77 is wrong.” Rejected; no horn is
named. Strongest contrary reading: the later draft's `{7,7}` display could be
read as curing the earlier uncertainty. That is exactly the open
display-versus-construction question the resolver must test, not a reason to
erase the dated uncertainty. Weakest seam: the sentence does not identify the
sign-bearing object, so it has zero resolving power by itself.

No source polarity, signature horn, scientific ledger row, prediction, canon,
paper or public posture changes.

```gu-typed-objects
result: source-owned Euclidean-to-Minkowski sign uncertainty with zero horn-selection power
carrier: author statement about the continuation from Euclidean to Minkowski metric LAYER=source-print CHIRALITY=N/A
pairing: UNTYPED
real_structure: UNTYPED; K77 and K95 real structures remain distinct
grading: source uncertainty versus repository resolver result; not fermion chirality
action_owner: source-print; the claim supplies no action
target: SC-SIG-53 source-adherence row MAP-TYPE=not-a-map
```
