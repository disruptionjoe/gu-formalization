---
title: "Status-contract currency wave"
status: exploration
operational_state: complete
doc_type: integrity_result
created: "2026-08-27"
grade: "MACHINE-CHECKABLE METADATA AND LIVE-TEST CURRENCY; NO SCIENTIFIC RESULT"
target_claim: NONE-NOT-A-KILL; this artifact repairs repository-native metadata and test currency only
canon_verdict_change: none
---

# Status-contract currency wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: INTERNAL_STRUCTURAL_ONLY`

Scope: document-role, operational-state and live propagation-test currency in
repository-native integrity surfaces. No GU object, physical comparator,
source assertion, action, ledger, prediction, paper or canon claim moves.

```gu-typed-objects
result: nine new Markdown artifacts use declared document-role and operational-state axes and the L10 propagation probe requires the live zero-red typed-carrier state
carrier: repository-native Markdown frontmatter, Python probes and JSON/YAML ratchets LAYER=UNTYPED CHIRALITY=N/A
pairing: declared document role to preserved operational state; historical L10 checkpoint to current live audit ON=repository-integrity-surfaces
real_structure: N/A -- this integrity result assigns no mathematical real form
grading: machine-checkable metadata and propagation-test currency only
action_owner: N/A -- no scientific action owner is created or transferred
target: preserve typed metadata axes and current gate truth without modifying protected TWG1 MAP-TYPE=evaluation
```

## Preflight bookend

A1 remains terminal at `ADHERED 106 / PARTIAL 4 / UNTYPED 1`; ledger v0.263
has 91 terminal rows and zero open rows; the B2 action-root set is empty. The
frontmatter audit instead reports twelve failures: nine new non-protected
documents place completion or review state in the document-role field, two
ratchets are consequently stale, and protected TWG1 is separately red. The
typed-carrier audit is live-green at zero red, but its L10 propagation probe
still requires the historical three-red checkpoint.

The selected repair moves only metadata axes on the nine non-protected files,
refreshes the population ratchet after those changes, and makes L10 require
the current live state. TWG1 remains byte-identical and excluded by authority.

## Result

Four source-wave results now declare `status: exploration` and preserve
completion as `operational_state: complete`. Five hostile reviews now declare
`status: process`; eight of the nine total files preserve `complete`, and the
source-dynamics review preserves its original `review_complete` state.

The L10 propagation certificate retains its 2026-08-22 historical checkpoint
but now requires the current typed-carrier audit to return zero red from both
the GU root and the aggregate invocation root. It also proves that the LT-GR8
delta remains indexed once and deferred without freezing the later-moving
global integration cursor. Its scientific ceiling remains unchanged: LT-GR8
is still `NEEDS / MISSING_CONSTRUCTION`, and declared `UNTYPED` slots remain
visible.

## Postflight bookend

- **Strongest overclaim:** green metadata and propagation gates prove only
  machine-readable custody, not physical typing or scientific correctness.
- **Strongest contrary case:** protected TWG1 still uses a legacy status and
  remains comparator-unclassified; the wave records but does not disguise it.
- **Weakest reproducibility seam:** population hashes can be made green by
  ratcheting over bad metadata. The coupled probe first asserts the exact nine
  role/state pairs and protected TWG1 hash, then requires the ratcheted gate.

No source status, action owner, ledger row, SG4 phase, prediction,
confirmation, canon verdict, protected paper, release state or public posture
changes.
