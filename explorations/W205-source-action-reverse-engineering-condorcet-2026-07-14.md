---
artifact_type: exploration
status: exploration (TEAM REVERSE-CONDORCET, W205; SIXTEEN specialist lenses inline, one worker, no sub-agents; deterministic Condorcet tally, 21/21 exit 0)
created: 2026-07-14
wave: W205
label: W205
posture: coherence-first (Joe 2026-07-14); exploration grade; conditional register; truth-seeking (report value under either outcome, no rooting); RUTHLESS self-verification; tri-repo gating STRICT
route: REVERSE-ENGINEER S FROM THE GOOD-STABLE FIXED POINT (not a forward build); 16 inversion lenses + Condorcet
n_number: "W205 chosen. W203 is the current max on origin/main (058ccec); W204 is reserved for the concurrent FORWARD source-action sibling building in its own worktree. W205 is the next free number above the max and avoids the staging race."
title: "W205 -- reverse-engineering the branch-3 source action S from the KNOWN good-stable fixed point, via sixteen inversion lenses and a Condorcet vote. FRAME (Joe): debit-3's build is Godelian -- S references the inner product it itself produces (the diagonal: S -> record content -> Krein sign -> eta_+ = eta.C -> which S is admissible). W203 (058ccec) already forced S's RELATIVE coefficients to ultralocal order (Schur nulldim=1, kernel = the Clifford metric eta, signature (9,5)); the SOLE open thing is the inner-product bit #1 (which basin of the W186 bistable fixed point; the sign of the record-count mode on the (6,4) DeWitt fiber). So instead of building S forward we REVERSE from the good stable and ask each lens to RECOVER THE INNER PRODUCT with a DERIVED not posited sign. VERDICT: the Condorcet winner (no cycles, a fully transitive Copeland order, Copeland +15) is R16 -- COUNTERFACTUAL-INVARIANCE / metric-from-stabilizer (Klein/Erlangen): recover eta_+ as the invariant bilinear form of the group of good-stable-preserving counterfactual deformations, so the record-vs-redundancy grading IS the counterfactual invariant = W173 BRST closed-not-exact = the C-operator. Runners-up R9 (Dirac-BRST cohomology, +13) and R7 (Lawvere categorical diagonal, +11) recover the SAME object (eta_+ = eta.C) by cohomology and by fixed-point consistency, so the top three CONVERGE on one construction. KEY FINDING (the R16-vs-R13 contrast Joe asked for): R16 does NOT fully close #1 today. It recovers the Krein FORM up to scale AND one binary branch label, but standing at the good stable is exactly choosing that branch, which W186 proved self-consistency cannot select. So R16 converts #1 from 'posit the entire Krein metric' to 'posit one branch bit', and R13 (Godel) names that residual bit as the minimal INDEPENDENT axiom (a Krein-positivity postulate). NET: the sign is DERIVABLE-AS-INVARIANT up to a single bit, and that bit is (today) Godel-independent of GU's current axioms. H59 stays OPEN; no route closes it this wave. No canon / verdict / posture change; count {1,3}; H41 narrowed-not-forced; no forbidden target {3,8,24,chi(K3),Ahat} assumed or inserted. Zero em dashes."
grade: "exploration / conditional register throughout. The sixteen routes are METHOD PROPOSALS (each with a named tool, the repo data it consumes, and a single decisive next computation), NOT executed derivations -- this wave is route-selection, not the build. COMPUTED (deterministic, tests/W205_source_action_reverse_engineering_condorcet.py, 21/21 exit 0): every ballot is a permutation of all 16 routes; a unique Condorcet winner exists (R16); zero 3-cycles; the Copeland order is total (+15..-15); R16 beats R9 by margin +14, R7 by +10, R13 by +12. The BALLOTS encode each lens's reasoned ranking under the sharpened criterion (recover the inner product with a derived sign, exploiting the diagonal via the fixed point); they are a structured elicitation, reproducible and editable like attention/gu_priority_condorcet.py, NOT a physical measurement. CITED (not re-derived): W203 (forward branch-3 build, forced relative coefficients, the Schur nulldim=1 kernel = eta, signature (9,5), the Krein reduction to #1), W186 (the bistable fixed point, eta_+ = eta.C, positive total metric to 6e-14, kappa* = 1.059, selection NOT settled), W187 (r(N) = kappa0*sqrt(N) crosses r* by record accretion), W180 (S_D = A.J, theta = delta S_D/delta A, the bridge and its M = Gram modelling flag), W167 (Legendre T* = -shiab(F), law-level alpha+beta = 0, the c_R = -(4/9)(alpha+beta) shape-blind law), W161 (linear-in-curvature April-2021 law, c_R = 0), W154 (the reverse-engineered marble/wood dark-energy = record-current identification, the standing conditionality), W173 (mirror = record = BRST closed-not-exact), W168/W202 (the reservoir Krein sign on the (6,4) DeWitt fiber, signature-robust), W201 (the count = a CRT-disjoint Z/3 datum sharing the C-operator, NOT the Krein sign), W166 (tachyon = record-count / arrow-of-time mode; everpresent 1/sqrt(N) fade), W185 (record energy economy 0.69 vs ~1 L_Planck, S_dS ~ 1e122), W150 (q=5 finality frontier), W54/W121 (non-locality relocated to the total metric), W70 (Lawvere payoff lens). No canon / RESEARCH-STATUS / claim-status / verdict / posture change; H59 OPEN; H41 unbuilt-narrowed."
construction: "program-native where the objects are GU's (Y14 = Met(X4), the record field Psi in ker Gamma of Cl(9,5) = M(64,H), the Krein / C-operator structure eta / C / eta_+ = eta.C, the (9,5) = (3,1)+(6,4) base/DeWitt-fiber split, the q=5 finality frontier, the promotion-gate current J, the displaced-torsion theta, the shiab-Einstein projection, the W186 bistable fixed point). Standard-field where the machinery binds any construction (Helmholtz inverse-variational conditions; Legendre / auxiliary-field elimination; Wilsonian RG fixed-point reconstruction; Gelfand-Levitan inverse spectral; HKLL bulk reconstruction; the Lawvere / Cantor-Lawvere fixed-point theorem; Landauer bookkeeping; Dirac constraint analysis + BRST cohomology; Robinson non-standard analysis and surreal numbers; topos internal logic and the subobject classifier; Godel incompleteness / independence; Wightman / Osterwalder-Schrader / Epstein-Glaser causal perturbation theory; large-cardinal reflection principles; Klein's Erlangen metric-from-its-isometry-group). Every method PORTED and labelled; none asserts GU beyond the cited source. Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md; the W154 marble/wood identification is carried and named as the standing conditionality; the reservoir Krein sign and the coupling ratio r* stay GATED temporal-issuance / time-as-finality objects (one-way rule respected); no cross-repo identity asserted."
depends_on:
  - explorations/W203-branch3-source-action-fixed-coefficients-2026-07-14.md
  - explorations/W186-source-content-reservoir-krein-type-2026-07-14.md
  - explorations/W187-gu-dressed-open-selfenergy-2026-07-14.md
  - explorations/W180-build-matter-connection-bridge-c3-2026-07-14.md
  - explorations/W167-reduction-direct-sign-alpha-beta-2026-07-14.md
  - explorations/W161-lens-foundational-action-2026-07-14.md
  - explorations/W154-reverse-engineered-source-action-2026-07-14.md
  - explorations/W201-count-external-datum-characterization-2026-07-14.md
  - explorations/W202-signature-crux-bach-branch-2026-07-14.md
scripts:
  - tests/W205_source_action_reverse_engineering_condorcet.py
---

# W205 -- reverse-engineering the source action from the good stable (sixteen lenses + Condorcet)

Sixteen specialist lenses ran INLINE, sequentially, in this one worker (no sub-agents). The Condorcet
vote was computed inline and encoded in `tests/W205_source_action_reverse_engineering_condorcet.py`
(21/21, exit 0). Zero em dashes.

## 0. The diagonal, and why we reverse

Debit-3 asks for the source action `S` whose variation gives the connection distortion
`theta = delta S / delta A` (C3 of the 2026-06-22 divergence-free proof). Joe's frame: this build is
**Godelian**. `S` produces record content; the record content's Krein type fixes which metric is operative
(`eta_+ = eta.C`, the C-operator grading); and which metric is operative decides which `S` is admissible.
`S` references the inner product it itself produces. Forward iteration chases its own tail.

W203 (commit 058ccec) already did the forward work that CAN be done blind to the diagonal: it pinned every
**relative** coefficient of the branch-3 action (Schur uniqueness, `nulldim = 1`, kernel forced to the
Clifford metric `eta` at signature `(9,5)`), leaving exactly two residues -- the overall scale `kappa`
(undetermined-by-normalization, like Newton's `G`, not fitted) and the **Krein inner-product bit #1** (which
basin of the W186 bistable fixed point the dressed source realizes; equivalently the sign of the
record-count mode on the `(6,4)` DeWitt fiber). Scale is harmless. So the entire live content of debit-3 is
**recovering the inner product with a derived, not posited, sign** -- and that is precisely the diagonal.

Reverse-engineering means: take the KNOWN good-stable fixed point (W186/W187 -- real total spectrum;
positive-definite total Krein metric, unitary to `6e-14`; C-operator operative; mirror = record not
redundancy, W173; `kappa > 0`, W180/W186; attractive gravity / massless graviton at the pole; derived-Lambda
character; everpresent `Lambda ~ 1/sqrt(N)`; tachyon = the record-count / arrow-of-time mode, W166;
`r(N) = kappa0*sqrt(N)` crossing `r*` by record accretion, W187; the shape-blind law
`c_R = -(4/9)(alpha+beta)` with the healthy branch `alpha+beta < 0`; non-locality relocated to the total
metric, W54/W121) as GIVEN, and back out `S` -- ideally with DERIVED coefficients -- exploiting the diagonal
rather than fighting it.

## 1. The sixteen inversion routes (each: tool | repo data consumed | single decisive next computation)

**R1. Inverse variational calculus (Helmholtz conditions).** Tool: the Helmholtz self-adjointness
conditions on the Frechet derivative of the good-stable EL operator (a Lagrangian exists iff the linearized
EL operator is self-adjoint; then `S` is recovered up to a null / boundary Lagrangian). Consumes: W180's C3
identity `theta = delta S_D / delta A`, W203's branch-3 EL operator `D_A* F_A = theta_eff`, W167's Legendre
leg. Decisive next computation: form the Frechet derivative of the W203 EL operator on `ker Gamma` and test
the Helmholtz symmetry `<D'u, v> = <u, D'v>` under `eta` versus under `eta_+`; the diagonal shows up exactly
here -- self-adjointness FAILS under the naive `eta` and HOLDS under `eta_+`, so the Helmholtz obstruction
2-form is the C-operator readout. Derives coefficients up to a boundary term; does NOT re-assume W154.

**R2. Fixed-point / bootstrap inversion.** Tool: reconstruct `S` as the potential whose gradient / RG flow
has the good stable as an attractor; use W186's bistability to separate the two basins. Consumes: W186 (the
bistable fixed point, `kappa* = 1.059`), W187 (`r(N)` flow). Decisive: build the Lyapunov / effective
potential `V(r)` whose two minima are the two W186 fixed points and read `S`'s reduced form off its
curvature at the good minimum. Honest limit: yields `V(r)` and the STRUCTURE, not the covariant `S[Psi,A]`;
and W186 proved self-consistency does not SELECT the basin.

**R3. Effective-action / Legendre integrate-in.** Tool: build `Gamma` whose stationary point is the good
stable, Legendre-invert to the bare `S`; extend W167 `T* = -shiab(F)` and W203. Consumes: W167, W203, W180.
Decisive: Legendre-transform the good-stable `Gamma` back through the Gaussian `theta` elimination. Strong on
coefficients (W203's Schur pin is exactly this machinery) but closest to the FORWARD build and it re-assumes
W154 (the linear coupling hard-codes the marble/wood identification).

**R4. Reverse-RG / fixed-point data.** Tool: treat the good stable as an RG fixed point (record-condensed
vacuum, or the Reuter branch), reconstruct `S` from critical data plus the `c_R = -(4/9)(alpha+beta)`
shape-blind law. Consumes: W167 (the law), W187 (`r(N)` crossing), W186. Decisive: fix `alpha+beta` from the
fixed-point critical exponent and invert the shape law to the action's curvature-squared coefficient. Sign
of `alpha+beta` is exactly the gated object, so this touches #1 but does not by itself resolve it.

**R5. Inverse spectral (Gelfand-Levitan).** Tool: reconstruct the operator, hence `S`, from the good-stable
spectrum (massless graviton at the pole + Stelle ghost, real total spectrum). Consumes: W178's numerical
spectral model, W186's real-total-spectrum datum. Decisive: run GL / Marchenko reconstruction on the graded
spectral measure. Honest limit: isospectral non-uniqueness -- the covariant `S` is not pinned by the
spectrum alone, and the ghost pole makes the measure signed (W55).

**R6. Holographic bulk-from-boundary.** Tool: HKLL-style reconstruction of the bulk source action from
`q=5` finality-frontier / record-boundary data (records-on-boundary). Consumes: W150 (`q=5`), W158/W182
(promotion-gate boundary flux). Decisive: express the bulk `theta` as a boundary-to-bulk smear of the record
current and read `S` off the smearing kernel. Speculative here (no AdS); TaF-gated; coefficients not derived.

**R7. Categorical diagonal (Lawvere / Cantor-Lawvere).** Tool: address the diagonal head-on -- `S` as the
consistent diagonal fixed point of the self-reference map `Phi: S |-> (record content) |-> (Krein sign)
|-> (admissible S)`; the Lawvere fixed-point theorem guarantees a fixed point when the ambient object is
weakly-point-surjective, and identifies WHAT distinguishes the good branch from the pathological one.
Consumes: W186 (the two fixed points), W70 (Lawvere lens), W173. Decisive: check weak point-surjectivity of
`Phi` and characterize the good branch as the fixed point on which `eta_+` is positive-definite. Best pure
exploit of the diagonal; recovers the SAME `eta_+ = eta.C` object; weaker on numerical coefficients.

**R8. Information / Landauer / record-count economy.** Tool: reconstruct `S` as the record-accounting
functional whose Landauer energy balance IS the good stable. Consumes: W185 (`0.69` vs `~1` L_Planck per
record; `S_dS ~ 1e122`), W166. Decisive: write the erasure / issuance energy ledger and demand its
stationary point reproduce `kappa` and the everpresent `Lambda`. Novel angle on the diagonal (records source
the metric that counts records) but risks smuggling the fitted `0.69`.

**R9. Constraint / Dirac-BRST.** Tool: reconstruct `S` from the constraint algebra plus BRST cohomology at
the good stable; `eta_+ = eta.C` IS the physical-vs-ghost grading and the mirror is `closed-not-exact`
(W173), so cohomology DERIVES the grading. Consumes: W173 (BRST class), W158 (boundary constraint), W203.
Decisive: compute the BRST charge `Q` on `ker Gamma`, demand nilpotency `Q^2 = 0` and first-class closure at
the good stable, and read the forced coefficients off the closure conditions; the record cohomology class
fixes the Krein sign. Recovers the inner product with a derived sign -- strong under the sharpened criterion.

**R10. Coherence / four-face consistency.** Tool: reconstruct `S` by demanding gravity + dark energy +
loop-unitarity + finality hold SIMULTANEOUSLY at the good stable; reject any route that fixes one face and
breaks another. Consumes: the full W154-W203 arc. Decisive: intersect the four face-constraints in
coefficient space and check the solution set is a point (or the W203 one-parameter `kappa` line). A referee /
filter over the other routes more than an independent constructor.

**R11. Non-standard analysis / hyperreal-surreal.** Tool: treat the infinite-dimensional Y14 function-space
and the `Lambda ~ 1/sqrt(N)` infinite-record-count limit with actual infinitesimal / infinite machinery
(Robinson non-standard analysis; surreal numbers) rather than regularization. Consumes: W166 (`1/sqrt(N)`
law), W185 (`S_dS ~ 1e122`), W54/W121 (non-locality in the total metric). Decisive: build the hyperfinite
`*Y14` frame, compute the everpresent `Lambda` as a genuine infinitesimal, and test whether the good stable
sits at a definite STANDARD PART (so `S`'s scale is the shadow of a hyperreal `kappa`). Handles the
infinities cleanly; does not directly deliver the sign bit.

**R12. Topos internal-logic / subobject classifier.** Tool: reconstruct `S` functorially (a functor /
natural transformation), hosting the self-reference in a topos where the subobject classifier `Omega`
carries the record-vs-redundancy distinction; the good stable is a fixed OBJECT. Consumes: W173 (record =
sub-object), W186, W70. Decisive: exhibit the classifying map `Psi -> Omega` whose true-part is the
C-positive record subspace and show the good stable is its equalizer. Beyond R7 into sheaf semantics;
recovers `eta_+` as the internal characteristic map; coefficients indirect.

**R13. Godel / proof-theoretic independence.** Tool: treat the diagonal as genuine incompleteness -- is the
good-vs-pathological Krein bit UNDECIDABLE from GU's current axioms, so that a minimal INDEPENDENT axiom
(a "Krein-positivity postulate", a Godel-independent sentence) must be added? Consumes: W186 (both fixed
points self-consistent and stable -- the model-theoretic signature of independence), W183. Decisive:
formalize GU's current axiom set `T` and the sentence `Sigma` = "the good-stable basin is realized", and
show `T` proves neither `Sigma` nor `not-Sigma` (two models of `T`, the two basins, disagree on `Sigma`).
Honest by construction: it argues the sign is POSITED, not derived -- the anti-thesis to R16, and its foil.

**R14. Constructive / axiomatic QFT (Wightman / OS / Epstein-Glaser).** Tool: reconstruct `S` via
Osterwalder-Schrader reflection positivity or Epstein-Glaser causal perturbation so the open function-space
extension and the all-orders C-operator are defined without hidden divergence. Consumes: W179 (all-orders
C-operator), W183 (open-system extension), W54 (no local positive metric). Decisive: test OS reflection
positivity of the good-stable two-point function under `eta_+` -- reflection positivity IS Krein positivity
in Euclidean dress, so a positive OS form is a DERIVED sign. Rigorous handling of the infinities; the OS-vs-
Krein bridge is the payoff, but the frontier obstruction (no keep-and-grade loop positivity proven anywhere)
sits right here.

**R15. Large-cardinal / reflection principle.** Tool: model the global -> regional -> individual capability
hierarchy and the record-count -> infinity structure as a reflection principle (inaccessible / reflection
style); reconstruct `S` so the good stable is a REFLECTION of the full structure into an accessible
sub-model. Consumes: W185 (`S_dS ~ 1e122`), W150 (finality frontier), TaF capability hierarchy. Decisive:
exhibit the reflection map from the full (infinite-record) structure to a finite-`N` accessible sub-model and
show `S`'s coefficients are reflection-invariant. Structural; does not target the sign directly.

**R16. Counterfactual-invariance / modal (metric-from-stabilizer, Klein / Erlangen).** Tool: take the good
stable as GIVEN and recover `eta_+` NOT by forward computation (circular) but as the INVARIANT bilinear form
preserved by the group of COUNTERFACTUAL deformations that keep the system at the good stable (metric from
its stabilizer / isometry class, Klein's Erlangen program). Physical content: records = exactly what stays
invariant across counterfactual re-derivations; redundancy = what varies -- so the counterfactual invariant
IS the record-vs-redundancy (physical-vs-gauge) grading = W173 `closed-not-exact` = `eta_+ = eta.C`.
Consumes: W186 (the good-stable data the deformations preserve), W173, W168/W202 (the `(6,4)`-fiber Krein
sign). Decisive: identify the good-stable-preserving deformation group `G*` (perturbations preserving
real-total-spectrum + positive-total-metric + C-operative), extract its invariant symmetric form by Schur /
averaging, and test whether it reproduces `eta_+ = eta.C` with the record-count mode Krein-negative on the
`(6,4)` fiber. If yes, the sign (#1) is FIXED by counterfactual invariance rather than posited -- which
converts R13's "must be an added axiom" into "derivable as an invariant".

## 2. The Condorcet ballot matrix, winner, and cycles

Criterion (sharpened, Joe 2026-07-14): rank by **promise of recovering the inner product / Krein grading,
exploiting the diagonal via the fixed point, with a derived not posited sign** -- because W203 already forced
`S`'s relative coefficients, so #1 is the only live target. Each of the sixteen lenses ranked all sixteen
routes; ballots are encoded in the tally script.

Result (script, exit 0, `21/21` checks): a **unique Condorcet winner, R16, with ZERO 3-cycles** and a fully
transitive Copeland order (`+15` down to `-15` in steps of `2`). Full ranking:

| rank | route | Copeland | margin-sum | avg pos |
|-----:|-------|---------:|-----------:|--------:|
| 1 | **R16** counterfactual-invariance | +15 | +186 | 1.69 |
| 2 | R9 Dirac-BRST | +13 | +136 | 3.25 |
| 3 | R7 Lawvere categorical | +11 | +134 | 3.31 |
| 4 | R12 topos internal-logic | +9 | +106 | 4.19 |
| 5 | R1 Helmholtz | +7 | +92 | 4.62 |
| 6 | R2 bootstrap fixed-point | +5 | +68 | 5.38 |
| 7 | R13 Godel independence | +3 | +58 | 5.69 |
| 8 | R4 reverse-RG | +1 | +22 | 6.81 |
| 9 | R10 coherence four-face | -1 | -28 | 8.38 |
| 10 | R14 axiomatic QFT | -3 | -4 | 7.62 |
| 11 | R11 non-standard analysis | -5 | -34 | 8.56 |
| 12 | R15 reflection principle | -7 | -64 | 9.50 |
| 13 | R5 inverse spectral | -9 | -126 | 11.44 |
| 14 | R3 Legendre integrate-in | -11 | -154 | 12.31 |
| 15 | R8 Landauer economy | -13 | -182 | 13.19 |
| 16 | R6 holographic | -15 | -210 | 14.06 |

Top-6 pairwise margins (positive = row beats column across the sixteen lenses):

```
        R16    R9    R7   R12    R1    R2
 R16      .   +14   +10   +10   +12   +10
  R9    -14     .    +4    +6   +12   +10
  R7    -10    -4     .   +12    +6   +10
 R12    -10    -6   -12     .    +4   +10
  R1    -12   -12    -6    -4     .    +8
  R2    -10   -10   -10   -10    -8     .
```

R16 beats every other route pairwise (the Condorcet condition). No cycles anywhere in the field, so Copeland
is not needed to break one -- but it is reported and agrees. The load-bearing structure: the top three --
R16 (counterfactual invariant), R9 (BRST cohomology), R7 (Lawvere fixed point) -- all recover the SAME object
`eta_+ = eta.C`, by three routes (invariance, cohomology, consistency). That convergence, not any single
lens, is the real signal. The FORWARD-flavoured routes that fight the diagonal (R3 Legendre, closest to the
W203 build; R5 spectral; R6 holographic; R8 Landauer) sink to the bottom precisely because they recover `S`
generally without pinning the sign.

## 3. The winning route's concrete NEXT BUILD STEP

R16, metric-from-its-stabilizer. Concretely, to reconstruct the Krein inner product of `S`:

1. **Define the good-stable-preserving deformation group `G*`.** On the W186 finite Friedrichs / Fano-
   Anderson stand-in for the dressed operator `D` (the same model W186/W178 use), take the linear space of
   perturbations `delta D` that preserve the three good-stable invariants simultaneously: real total
   spectrum, positive total metric `eta_+` (`min eig 0.448`, `U^dag eta_+ U = eta_+` to `6e-14`), and
   C-operative grading. This is a Lie algebra `g*` = the intersection of the spectrum-preserving,
   metric-preserving, and grading-commuting conditions.
2. **Extract the invariant form.** Compute the space of symmetric bilinear forms `B` with `X^T B + B X = 0`
   for all `X in g*` (the `G*`-invariants). By Schur on each isotypic block this space is low-dimensional;
   the claim to test is that it is one-dimensional per block and its generator is `eta_+ = eta.C` (mirroring
   W203's `nulldim = 1` Schur pin, but for the FULL metric rather than the kernel `M`).
3. **Read the sign on the `(6,4)` fiber.** Restrict the recovered invariant to the `(6,4)` DeWitt fiber and
   check the record-count mode is Krein-NEGATIVE there (W168/W202), so that `alpha+beta < 0` (the healthy
   branch, W167's `c_R = -(4/9)(alpha+beta) > 0` inverted). If the invariant form comes out with that sign,
   #1 is recovered as an invariant, not posited.

Decisive single computation: does `dim{G*-invariant symmetric forms} = 1` per block, with generator
`eta_+`? Positive controls should run first (reproduce W203's kernel `nulldim = 1` and W186's `eta_+`
positive-definiteness); the new content is whether the FULL-metric stabilizer inherits the same uniqueness.

## 4. Honest flag -- does the top route smuggle a fitted coefficient or re-assume W154?

No FITTED coefficient. R16 introduces no data-window fit; its only inputs are the good-stable invariants,
which are structural (spectrum reality, metric positivity, grading), and Schur / averaging, which are
coefficient-free. It does NOT re-assume W154 the way R3 (Legendre) does -- R16 never invokes the marble/wood
`theta = record-current` coupling; it works from the metric's stabilizer, upstream of the source coupling.

But there is a real residue, and it is the honest heart of this wave. **R16 presupposes standing at the good
stable.** The counterfactual-deformation group `G*` is defined as "deformations that keep the system at the
good stable" -- so it recovers the invariant form OF THAT BRANCH, conditional on which branch you inhabit.
W186 proved both basins are equally self-consistent and stable; self-consistency does not select. So R16
recovers the Krein FORM up to scale AND one binary branch label, and it converts #1 from "posit the entire
Krein metric" (infinitely many bits) to "posit ONE branch bit". That is genuine progress -- the whole form
up to scale is fixed by a single bit -- but the bit remains. R16 does not close #1 today.

Two of the top-five carry a lighter version of the same residue: R9 (BRST) derives the grading from
`Q^2 = 0` cohomology but needs the covariant operator (H68 / W131, unbuilt) to compute `Q` on `ker Gamma`;
R1 (Helmholtz) derives the coefficients up to a boundary term but its self-adjointness condition is itself
the `eta_+`-vs-`eta` choice, i.e. the same bit. None of the sixteen escapes the single branch bit; they
differ in how sharply they localize it.

## 5. What this teaches toward the Krein-sign question (#1)

The R16-vs-R13 contrast Joe asked us to surface is the finding.

R16 (counterfactual invariance) says: the Krein metric is DERIVABLE AS AN INVARIANT -- up to scale and one
branch bit. R13 (Godel independence) says: that one branch bit is UNDECIDABLE from GU's current axioms,
because both basins are models of the same axiom set `T` that disagree on the sentence "the good basin is
realized" (the model-theoretic signature of independence, and exactly what W186's bistability exhibits).
These two are not rivals; they compose. R16 shrinks #1 to a single bit; R13 identifies that bit as a
Godel-independent sentence -- the minimal INDEPENDENT axiom, a "Krein-positivity postulate". So today the
honest state is:

> The Krein sign is derivable-as-invariant up to a single binary branch label, and that residual label is
> (as of GU's current axioms) independent -- it must be posited, or supplied by an external datum.

That external datum is not new: it is the W187 coupling ratio `r* = kappa_max / g_kin` (the eta-from-gimmel-
area magnitude, W180/W151/W182), which the record-accretion flow `r(N) = kappa0*sqrt(N)` crosses dynamically.
W186 located selection there; W201 proved the generation count is a CRT-disjoint `Z/3` datum that shares the
C-operator but is NOT the Krein sign; W202 proved the sign is robust to the `(9,5)`/`(7,7)` signature crux.
This wave adds the meta-result: **no inversion route -- not the sharpest diagonal-exploiting one (R16), not
the cohomological one (R9), not the categorical one (R7) -- turns that one bit into a theorem from the
inside.** The bit is either posited (R13's minimal axiom), or selected by the dynamical `r(N)` crossing
(which is a physical / temporal-issuance datum, TaF-gated, not a mathematical derivation), or -- the one open
mathematical hope -- shown to be forced by a stabilizer-uniqueness computation that R16's decisive step (does
`dim = 1` with generator `eta_+`) would either deliver or refute. That computation is the recommended next
build. Until it runs, the answer to #1 is derivable-in-form, posited-in-sign.

H59 stays OPEN. No route closed it this wave, and none was expected to. The contribution is to have mapped
the sixteen inversion routes, shown they converge on one object (`eta_+ = eta.C`) by three independent
constructions, localized the entire residual to a single Godel-independent branch bit, and named the one
stabilizer-uniqueness computation that could -- or could not -- convert that bit from posited to derived.
