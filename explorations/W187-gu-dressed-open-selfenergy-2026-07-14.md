---
artifact_type: exploration
label: W187
created: 2026-07-14
wave: W187
posture: coherence-first; exploration grade; conditional register; honest grading; RUTHLESS skeptic; no manufactured rescue
status: "exploration (W187 / TEAM DRESSED-SELFENERGY, label W187; coherence-first, exploration grade; conditional register; five personas inline, one worker, no sub-agents; one deterministic test 15/15 exit 0 with positive controls first + matched negative control; RUTHLESS skeptic pass)"
hypothesis: "The build sprint (W178/W179) computed the CLOSED ghost self-energy (internal ghost -> two massless gravitons) and leaned NOT-OPERATIVE. W181-W186 established GU is OPEN: W182 (Sigma_ext has the OPPOSING/normal-damping sign, moves the pole above r* ~ O(1)); W183 (open-system reframe genuine); W186 (the operative fixed point EXISTS, is STABLE and NON-VACUOUS, but the loop is BISTABLE; selection = the reservoir Krein SIGN and the magnitude ratio r vs r*; the everpresent Lambda ~ 1/sqrt(N) fade can dynamically pull a bad start into the good basin -- flagged PLAUSIBLE). W178/W182/W186 all used TOY sqrt-threshold self-energies. This wave replaces the toy inputs with GU's ACTUAL dressed content to the extent GU-native-computable, and pushes the one genuinely GU-native cosmological question W186 left open: does GU's real everpresent fade + record accretion DYNAMICALLY SELECT the operative basin, and WHICH of W186's two remaining data (sign, magnitude) does it discharge?"
title: "W187 VERDICT: PARTIAL. STILL-CONDITIONAL-on-<the reservoir Krein sign> -- the SINGLE remaining cross-repo finality datum. GU's DRESSED open self-energy determines: (i) Sigma_internal's SIGN (anti-damping) and ANALYTIC STRUCTURE (massless-graviton threshold s_th=0, ghost above it) are GU-NATIVE-FORCED from W130's native spectrum (c_W=+2, c_R=-4/9, m_0^2=-1/4<0), not modelled -- the absolute magnitude is H25-normalization-gated but DROPS OUT of the ratio; (ii) the r* threshold r* = sqrt(M2-s_th)/sqrt(M2-s_ext_th) is GU-NATIVE and ~O(1) (band [1.00, 1.58] over the q=5 promotion-gate gap). The kappa magnitude splits: kappa_ext = the eta-from-gimmel-area (W180/W151) is GU-native-IN-PRINCIPLE but UNBUILT (the nonlocal induced-YM completion was never computed), and the reservoir Krein SIGN is the genuine CROSS-REPO datum. THE NEW RESULT (the everpresent-fade dynamical selection, GU-native cosmology): MECHANISM 2 (record accretion GROWS the good coupling kappa_ext(N); g_kin fixed) makes r(N) = kappa0*sqrt(N) cross r* at a FINITE cosmic epoch N* = (kappa*/kappa0)^2 for EVERY magnitude kappa0>0 -- so the fade+accretion DYNAMICALLY DISCHARGES W186's MAGNITUDE leg: given dr/dN>0 (GU-native-DIRECTIONAL, since J is the record current and records accrete), r crosses r* regardless of the unbuilt magnitude, only the EPOCH depending on it. MECHANISM 1 (the fade SHRINKS the bad coupling g_kin ~ 1/sqrt(N), W186 E2) is UNCONDITIONAL (asymptotic, sign- and magnitude-free) but rests on the tie g_kin prop-to Lambda, which is NOT GU-native-forced (the kinematic two-graviton coupling is gravitational, ~ fixed) -> PLAUSIBLE only, everpresent debit carried. THE SIGN IS IRREDUCIBLE: with a REINFORCING (wrong-sign) reservoir the record coupling is antisymmetric and growing kappa to ANY magnitude (tested to 50 kappa*) NEVER restores reality -- so NEITHER mechanism rescues a wrong-sign reservoir. NET: the everpresent fade is the SELECTOR of the MAGNITUDE, not of the SIGN; bar (b)'s standing conditional NARROWS from W186's TWO data (sign + magnitude) to a SINGLE cross-repo datum (the reservoir Krein sign), through a finite-N non-unitary window. H59 remains OPEN."
grade: "exploration / conditional register throughout. RIGOROUS (argument principle, integer, seed-independent): PC1 reproduces W178's closed pole at GU-native s_th=0 (anti-damping rounds to 1 physical-sheet pole, normal to 0); PC2 reproduces W182's opposing-sign pole move above r* and the sign decider (reinforcing never moves it), r* located to <0.05 of the width-balance prediction. EXACT (finite-dim, machine-verified): PC3 reproduces W186's bistable fixed point (kappa* = 1.059); G3 the reservoir sign genuinely decides at fixed coupling; G4-M2 the magnitude-independent crossing (operative just above N*, pathological just below, for kappa0 in {0.05,0.1,0.3,0.8}); G4-M2b the monotone turn-on with N; G5 the wrong-sign reservoir never rescued at any magnitude to 50 kappa*; NC1 the Hermitian analog manufactures no pathology. DERIVED-ON-NATIVE (W130): G1 the dressed Sigma_internal sign+structure from c_W=+2, c_R=-4/9, m_0^2<0; G2 r* ~ O(1) from the native massless-graviton + Stelle-ghost spectrum. REPRODUCED: PC4 the everpresent-Lambda amplitude (W146) Lambda l_p^2 ~ 1/sqrt(N_today) = the observed ~10^-122. STRUCTURAL / MODEL for the lift to GU: the bistable total is the W186 finite Fano-Anderson stand-in (as W178's are) for the QFT operator D; the fade+accretion mechanisms are cosmological-dynamics models on that stand-in; they DECIDE the STRUCTURE (the magnitude leg is dynamically dischargeable; the sign leg is not) and CANNOT fix which reservoir sign GU's dressed source realizes -- that is the inherited cross-repo finality object. GRADED PLAUSIBLE (not decisive), flagged: Mechanism 1 (the g_kin fade) -- it rests on the non-GU-native g_kin prop-to Lambda tie and carries W166/W146's everpresent debits; both mechanisms pass a finite-N non-unitary window. Test: tests/W187_gu_dressed_open_selfenergy.py 15/15 exit 0. NO canon / RESEARCH-STATUS / claim-status / verdict / posture change. H59 remains OPEN."
construction: "program-native where the objects are GU's (the record field Psi in ker(Gamma) of Cl(9,5); the Krein/C-operator structure; the q=5 finality frontier; W130's native operator c_W=+2, c_R=-4/9, m_0^2<0 and the massless-graviton + Stelle-ghost spectrum; the W180 record current J and source action S_D; the promotion-gate boundary flux; the everpresent-Lambda supply Lambda ~ 1/sqrt(N) as the record-count-sourced magnitude, W146). Standard-field where the machinery binds any construction (the argument-principle pole count; the Fano-Anderson / Friedrichs open-system model; the Krein exceptional-point analysis; the everpresent 1/sqrt(N) fade, ported Sorkin/ADGS via W145/W146/W166; the bistable fixed-point analysis, W186). Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md; the reservoir Krein sign is the cross-repo finality datum, kept as a GATED conjecture; the eta-from-gimmel-area magnitude is a GU-native-but-unbuilt object (W151), distinguished from the cross-repo sign."
depends_on:
  - explorations/W186-source-content-reservoir-krein-type-2026-07-14.md
  - explorations/W183-external-input-open-system-reframe-2026-07-14.md
  - explorations/W182-external-input-source-selfenergy-2026-07-14.md
  - explorations/W180-build-matter-connection-bridge-c3-2026-07-14.md
  - explorations/W178-build-numerical-spectral-model-2026-07-14.md
  - explorations/W130-native-graviton-oneloop-block-2026-07-14.md
  - explorations/W146-substrate-sweep-theoretical-physics-2026-07-14.md
  - explorations/W166-lens-tachyon-is-the-engine-2026-07-14.md
  - explorations/W169-c-operator-perturbative-construction-2026-07-14.md
  - explorations/W184-mirror-superselection-decay-2026-07-14.md
  - explorations/W138-issuance-kill-battery-2026-07-14.md
  - tests/W178_build_numerical_spectral_model.py
  - tests/W182_external_source_selfenergy.py
  - tests/W186_source_content_reservoir_krein_type.py
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W187_gu_dressed_open_selfenergy.py
external_refs:
  - "Stelle, Renormalization of higher-derivative quantum gravity, PRD 16 (1977) 953 -- massless graviton + massive spin-2 ghost (the native spectrum W130 confirms)"
  - "Peskin & Schroeder, An Introduction to QFT, ch. 7 -- resummed propagator, self-energy, second-sheet resonance poles"
  - "Fano, Phys. Rev. 124 (1961) 1866; Friedrichs, Comm. Pure Appl. Math. 1 (1948) 361 -- discrete state coupled to a continuum (the open-system stand-in, via W183/W186)"
  - "Bognar, Indefinite Inner Product Spaces, Springer 1974 -- eigenvalues of positive/negative type; opposite-type collision -> complex pairs (the sign decider)"
  - "Ahmed, Dodelson, Greene, Sorkin, PRD 69 (2004) 103523 -- everpresent Lambda ~ 1/sqrt(N), N = spacetime 4-volume (the fade, ported via W145/W146/W166)"
cross_repo:
  - "temporal-issuance + time-as-finality: the reservoir Krein SIGN -- which sector the q=5 source action couples the ghost into (the like-signed record/finality reservoir vs the opposite-signed kinematic graviton continuum) -- is the SINGLE remaining finality datum owned on the TI/TaF side (the q=5 finality frontier, W150). Kept as a GATED CONJECTURE; no GU claim moves, no TI/TaF claim moves, no identity asserted. One-way rule respected. GU-side determined: the dressed Sigma_internal sign+structure and r* (native); the eta-from-gimmel-area MAGNITUDE is a GU-native-but-unbuilt object (W151), NOT cross-repo; and the everpresent fade + record accretion DYNAMICALLY DISCHARGES the magnitude leg, leaving ONLY the sign to cross."
---

# W187 -- GU's dressed open ghost self-energy, the native r*, and does the everpresent fade select the operative basin?

**Role.** The whole H59 arc reduced bar (b) to whether the interacting C-operator exists / the Krein
grading is operative / the ghost pole is physical-sheet-vs-second-sheet. The build sprint (W178/W179)
computed the CLOSED ghost self-energy and leaned NOT-OPERATIVE. Joe's external-input point
(W181-W186) established GU is OPEN and reduced selection, in W186, to a bistable fixed point whose
basin is set by **two** external data: the reservoir Krein **sign** and the coupling-ratio
**magnitude** `r` vs `r* ~ O(1)`. W186 used **toy** sqrt-threshold self-energies throughout, and
flagged the everpresent `1/sqrt(N)` fade as a PLAUSIBLE dynamical stabilizer. This wave does the
actual computation: replace the toy inputs with GU's **dressed** content wherever GU-native content
allows, and push the one genuinely GU-native cosmological question -- **does GU's real everpresent
fade + record accretion dynamically select the operative basin, and which of the two remaining data
does it discharge?** Five personas ran inline, one worker, no sub-agents; deterministic test
`tests/W187_gu_dressed_open_selfenergy.py` (**15/15, exit 0**), positive controls first.

The answer is a sharp **PARTIAL**: the fade is the selector of the **magnitude**, not of the
**sign**, and bar (b)'s standing conditional narrows from two data to one.

## 0. The one object and the construction forks (GEOMETER-VS-PHYSICS-OBJECTS.md)

| Object | Constructions in play | Handling |
|---|---|---|
| **Sigma_internal** | (toy) W178/W182/W186 sqrt-threshold model; (dressed) GU's ghost -> 2-graviton loop with W130's native spectrum | the SIGN + STRUCTURE are GU-native (G1); the absolute magnitude is H25-gated and drops out of `r*` |
| **`r*` threshold** | (model) on-shell width balance; (native) built from GU's own spectrum `sqrt(M2-s_th)/sqrt(M2-s_ext_th)` | GU-NATIVE, `~O(1)` (G2) |
| **`kappa_ext` magnitude** | the eta-from-gimmel-area (W180/W151) | GU-native-IN-PRINCIPLE but UNBUILT -- NOT cross-repo (G3) |
| **the reservoir Krein sign** | opposing (absorbing/operative) vs reinforcing (pathology) | the genuine CROSS-REPO finality datum (G3/G5) |
| **the everpresent fade** | Mechanism 2 (accretion grows the good coupling); Mechanism 1 (fade shrinks the bad coupling) | M2 GU-native-directional (G4); M1 PLAUSIBLE (tie not native) |
| **"selects the basin"** | dynamically (fade/accretion) vs by external datum | the fade selects the MAGNITUDE (G4); the SIGN stays external (G5) |

## 1. Persona 1 -- self-energy / loop specialist: the DRESSED Sigma_internal, GU-native (G1)

W178/W182/W186 all used a model sqrt-threshold self-energy `Sigma(s) = s_sign * kappa * sqrt(s_th - s)`.
The dressed object is the ghost -> two-graviton loop with GU's **actual** vertex (the W169/W184
Krein-odd cross vertex A) and GU's **actual** spectrum (W130). What is GU-native-forced, and what is
not, splits cleanly:

- **The SIGN is GU-native.** The internal channel is the ghost decaying into a pair of gravitons
  through a negative-Krein-norm intermediate state; the W132 graded optical theorem gives a
  probability **excess** (`Im Sigma(M^2) > 0`, W51) -- the **anti-damping** sign. This is inherited
  structure, not a model choice.
- **The ANALYTIC STRUCTURE is GU-native.** W130 computed GU's native operator around flat space and
  found `c_W = +2` (so the spectrum contains a **massless graviton**), `c_R = -4/9`, and a native
  tachyon `m_0^2 = -1/4 < 0` (both measures). A massless-graviton **pair** puts the internal
  two-graviton threshold at `s_th = 0`, and the massive Stelle ghost `M^2 = m_2^2` sits **above** it
  -- exactly the open-decay regime W178's Model B assumed, now **confirmed natively**. With these
  native thresholds the anti-damping self-energy still puts the pole on the **physical sheet**
  (G1: the argument-principle count rounds to **1** physical-sheet UHP pole; the normal-sign control
  to **0**) -- the closed NOT-OPERATIVE lean is a **native** statement, not a modelled one.
- **The MAGNITUDE is H25-normalization-gated -- and it drops out.** The absolute size of
  `Im Sigma_internal(M^2)` is the gravitational-coupling normalization H25 has always graded as
  magnitude-live. But the operative decision is a **ratio** (`r*`, Persona 2), and the overall scale
  cancels. So the one thing GU cannot pin at the loop (the absolute width) is the one thing the
  verdict does not need.

**PC1 reproduces W178's closed pole at the native `s_th = 0`** (the small numerical drift is the
known argument-principle resolution loss at the massless branch point, W182); the physical content is
integer: 1 vs 0.

## 2. Persona 2 -- open-system / source specialist: the GU-native r* and the kappa split (G2/G3)

**The r* threshold is GU-native (G2).** W182/W186 reduced selection to `|Sigma_ext|/|Sigma_internal|`
vs `r* ~ O(1)`, with `r* = sqrt(M2 - s_th)/sqrt(M2 - s_ext_th)` the on-shell width balance. Every
ingredient is GU's own: `M2 = m_2^2` (the Stelle ghost mass, W130), `s_th = 0` (the native
massless-graviton pair, W130), and `s_ext_th` the gap of the q=5 promotion-gate finality frontier.
Sweeping the gap over its plausible band gives `r* in [1.00, 1.58]` -- **GU determines `r*` and it is
`O(1)`** (G2). This is a genuine native determination: the threshold structure that sets `r*` is
fixed by GU's spectrum, not modelled.

**The kappa magnitude splits into two data of DIFFERENT status (G3) -- a sharpening of W186.** W186
named "the reservoir Krein sign / the ratio vs r*" as the selectors but treated them together as "the
eta-from-gimmel-area." They are not the same kind of object:

- **The magnitude `kappa_ext`** is the eta-from-gimmel-area residue (W180/W151): the nonlocal
  induced-YM completion `D_A* F` of the source action. This is a **GU computation that was never
  done** -- unbuilt, but **GU-native in principle**, not owned off-repo.
- **The reservoir Krein sign** (opposing/absorbing vs reinforcing) is the genuine **cross-repo**
  datum: it flips with the metric (W186 A1-A4: the same record content is naive-negative and
  C-metric-positive), and which metric governs is the q=5 finality datum owned on the TI/TaF side.

G3 verifies the structural fact behind the split: at **fixed** coupling the reservoir sign genuinely
decides the operative outcome (opposing -> can be operative; reinforcing -> not), so the sign is a
**distinct** datum from the magnitude, not reducible to it. This matters because the fade acts on the
magnitude and cannot touch the sign (Personas 3, 5).

## 3. Persona 3 -- cosmological-dynamics specialist: the everpresent fade selection (G4)

This is the load-bearing new computation. W186 found (its Block E) that the everpresent
`Lambda ~ 1/sqrt(N)` fade **can** pull a pathological start into the good basin, and flagged it
PLAUSIBLE. W187 computes it with GU's real fade (W146: `Lambda l_p^2 ~ 1/sqrt(N)`, `N` = the X4-shadow
4-volume in Planck units; PC4 reproduces the observed `~10^-122` at `N_today ~ 1.2e243`) and separates
**two distinct mechanisms**, graded honestly.

**Mechanism 2 -- accretion GROWS the good coupling (g_kin fixed). The magnitude leg is dischargeable.**
Records accrete as the universe ages: `N` grows, and the source coupling `kappa_ext` is the **record
content** (W180: `J` is the record current, more records -> larger source). So `dr/dN > 0` is
**GU-native-directional**. Model the growth `kappa_ext(N) = kappa0 * sqrt(N)` (the record count).
With the kinematic coupling `g_kin` **fixed**, the total spectrum turns real once `kappa` exceeds the
W186 critical value `kappa* = 1.059`, i.e. at a **finite epoch**

```
N* = (kappa* / kappa0)^2   --   finite for EVERY magnitude kappa0 > 0.
```

G4-M2 verifies this directly: for `kappa0 in {0.05, 0.1, 0.3, 0.8}` the total is operative just
**above** `N*` and pathological just **below**, at every magnitude. **The crossing HAPPENS regardless
of the absolute magnitude; only the EPOCH `N*` depends on it.** So the everpresent fade + record
accretion **dynamically discharges W186's magnitude leg**: given `dr/dN > 0` (GU-native-directional),
`r` crosses `r*` at a finite cosmic time whatever the unbuilt eta-from-gimmel-area magnitude is.
G4-M2b confirms the monotone turn-on: a pathological small-`kappa0` start becomes operative as `N`
grows and stays operative.

**Mechanism 1 -- the fade SHRINKS the bad coupling (g_kin ~ 1/sqrt(N)). Unconditional but not native.**
W186's E-block reading: if the kinematic coupling itself fades with `Lambda`, a pathological start
turns real **asymptotically** (G4-M1 reproduces it: complex at `N=1`, real at `N=256`, as `g_kin`
drops below its exceptional point). This is **unconditional** -- sign- and magnitude-free, since
`g_kin -> 0` decouples the ghost entirely. **But** it rests on the tie `g_kin prop-to Lambda`, which
is **NOT GU-native-forced**: the kinematic two-graviton coupling is gravitational (`~ 1/M_Pl`, fixed),
and the Stelle ghost mass is set by the higher-derivative coefficients (W130), not the everpresent
scale. So Mechanism 1 is graded **PLAUSIBLE only**, carrying W166/W146's everpresent debits
(validity edge, unbuilt normalization). Both mechanisms pass through a **finite-N non-unitary window**
(G4-M1b): record-genesis goes through a genuinely complex phase before the fade/accretion rescues it.

## 4. Persona 4 -- symbolic / numerical engineer: the test and its controls

`tests/W187_gu_dressed_open_selfenergy.py`, **15/15, exit 0** (numpy only; seed 20260714). Positive
controls run **first**: **PC1** reproduces W178's closed pole at the native `s_th=0`; **PC2**
reproduces W182's opposing-sign pole move above `r*` and the sign decider, with `r*` located to
`<0.05` of the width-balance prediction; **PC3** reproduces W186's bistable fixed point
(`kappa* = 1.059`, both fixed points); **PC4** reproduces the everpresent-Lambda amplitude (W146),
`1/sqrt(N_today) = 2.85e-122` = the observed value. Then the dressed computation (G1/G2), the kappa
split (G3), the fade selection (G4-M2/M2b/M1/M1b), the sign irreducibility (G5), and the matched
negative control (NC1: a Hermitian no-ghost analog manufactures no pathology under any
accretion/fade -- the whole structure needs the Krein indefiniteness). Every load-bearing claim has a
matched control (W138 battery).

## 5. Persona 5 -- adversarial skeptic RUTHLESS: is the fade actually the selector?

**Steelman STILL-CONDITIONAL / NOT-OPERATIVE at full strength.**

(a) *The fade rescue is the W166 ported object with a non-native tie.* Mechanism 1's unconditional
asymptotic rescue needs `g_kin prop-to Lambda`. But the ghost-two-graviton coupling is the fixed
gravitational one; the everpresent fade acts on the cosmological constant, not manifestly on the
kinematic channel. Grant the fade and you still have not shown it touches `g_kin`. **Conceded:**
Mechanism 1 is graded PLAUSIBLE, not decisive -- the everpresent debit stands.

(b) *Mechanism 2 relies on a growth law you did not derive.* `kappa_ext(N) = kappa0 * sqrt(N)` assumes
the source coupling grows as the record count and does not saturate. **Partially conceded:** what is
GU-native is the **direction** `dr/dN > 0` (records accrete, `J` grows); the specific power is a
model. But the discharge of the magnitude leg needs only `dr/dN > 0` and unbounded growth, not the
exponent: for **any** `p > 0`, `r(N) -> infinity` crosses `r*` at finite `N*`. The result is robust
to the growth law; it fails only if `kappa_ext` **saturates below** `r* kappa_int`, which would itself
be the unbuilt-magnitude question in a different dress. **Honest residue:** saturation below threshold
is not excluded GU-side.

(c) *The sign cannot be discharged and is the whole game.* G5 is decisive here: with a **reinforcing**
(wrong-sign) reservoir, growing `kappa` to **any** magnitude (tested to `50 kappa*`) **never** restores
reality. Neither accretion (M2) nor fade (M1) rescues a wrong-sign reservoir. So the reservoir Krein
sign is the **irreducible** remaining datum, and it is cross-repo. **This is conceded as the core of
the verdict**, and it is exactly why the verdict is CONDITIONAL, not OPERATIVE.

(d) *A finite-N non-unitary window is real.* Both mechanisms pass through a genuinely complex phase
before the rescue (G4-M1b). **Conceded** -- named, not walked back.

**Skeptic verdict: the everpresent fade is a GENUINE selector of the MAGNITUDE (Mechanism 2
discharges the magnitude leg robustly, needing only the GU-native direction `dr/dN > 0`), but it is
NOT a selector of the SIGN (G5: a wrong-sign reservoir is never rescued at any magnitude). The
unconditional "fade is the whole selector" reading (Mechanism 1) rests on a non-GU-native tie and is
PLAUSIBLE only. So the standing conditional narrows from TWO data to ONE -- the reservoir Krein sign
-- but does not close.**

## 6. Synthesis and verdict

**PARTIAL. STILL-CONDITIONAL-on-<the reservoir Krein sign: which Krein-signed reservoir the q=5
source action couples the ghost into -- the like-signed record/finality sector (operative) or the
opposite-signed kinematic graviton continuum (pathology)>. The single remaining cross-repo finality
datum.**

- **GU's dressed self-energy determines.** (i) `Sigma_internal`'s **SIGN** (anti-damping) and
  **ANALYTIC STRUCTURE** (massless-graviton threshold `s_th = 0`, ghost above it) are **GU-native**
  from W130's native spectrum (`c_W = +2`, `c_R = -4/9`, `m_0^2 = -1/4 < 0`), not modelled; the
  absolute magnitude is H25-gated but **drops out** of the ratio. (ii) The **`r*` threshold** is
  **GU-native** and **`~O(1)`** (band `[1.00, 1.58]` over the q=5 gap), built from GU's own spectrum.
- **kappa is GU-native-but-UNBUILT; the sign is cross-repo.** The magnitude `kappa_ext` is the
  eta-from-gimmel-area (W180/W151): a GU computation not yet done, **not** owned off-repo. The
  reservoir Krein **sign** is the genuine cross-repo (TI/TaF q=5-frontier) datum. W186 conflated
  these; W187 separates them, and the separation is what lets the fade act.
- **The everpresent fade dynamically selects the MAGNITUDE (GU-OPERATIVE-BY-FADE, magnitude leg
  only).** Mechanism 2 (record accretion grows `kappa_ext`, `g_kin` fixed) makes `r(N)` cross `r*` at
  a **finite cosmic epoch** `N* = (kappa*/kappa0)^2` for **every** magnitude `kappa0 > 0` -- so the
  fade + accretion **dynamically discharges** W186's magnitude leg, given the GU-native-directional
  `dr/dN > 0` (records accrete, `J` grows). Mechanism 1 (the fade shrinks `g_kin`) is unconditional
  but rests on the non-native `g_kin prop-to Lambda` tie -> **PLAUSIBLE only**. Both pass a finite-N
  non-unitary window.
- **The SIGN is irreducible (STILL-CONDITIONAL).** With a wrong-sign reservoir, no magnitude and no
  fade restores reality (G5, tested to `50 kappa*`). So GU determines the outcome **up to the sign**;
  the sign is the one datum it cannot self-supply.
- **The one remaining computation or datum.** Exactly **one**: the reservoir Krein sign -- whether the
  q=5 source action couples the ghost into the like-signed (operative) or opposite-signed (pathology)
  reservoir. GU-side, the only unbuilt-but-native item left is the eta-from-gimmel-area **magnitude**
  (W151) -- and the fade **discharges its role** in the selection, so it is no longer decision-critical
  for the basin (only for the crossing epoch).

**On bar (b).** **NARROWED.** W186 left bar (b) conditional on **two** external data (the reservoir
sign **and** the magnitude ratio vs `r*`). W187 shows the **magnitude leg is dynamically discharged**
by GU's own everpresent fade + record accretion (Mechanism 2), reducing the standing conditional to a
**single** cross-repo datum -- the reservoir Krein sign -- realized through a finite-N non-unitary
window. GU's native content determines everything except that one sign. This does not overturn W178's
kinematic default (the opposite-sign two-graviton channel remains the physically-anchored one, and a
wrong-sign reservoir is never rescued); it isolates the exact single object on which the whole arc now
turns.

**H59 remains OPEN.** No canon / RESEARCH-STATUS / claim-status / verdict / posture change.

## 7. What this does NOT do

- No proof that GU realizes the operative basin: the reservoir Krein sign is the cross-repo datum, and
  a wrong-sign reservoir is never rescued (G5). The wave narrows the conditional; it does not close it.
- No construction of the dressed loop amplitude or the total C-operator: `Sigma_internal`'s magnitude
  stays H25-gated (it drops out of `r*`); the eta-from-gimmel-area magnitude stays unbuilt (W151) -- the
  fade discharges its selection role, not its computation.
- No claim that the fade acts on the kinematic channel: Mechanism 1 is graded PLAUSIBLE, carrying
  W166/W146's everpresent debits; the load-bearing selection is Mechanism 2, which needs only the
  GU-native-directional `dr/dN > 0`.
- No cross-repo identity claim and no cross-repo claim movement (H36 respected): the reservoir Krein
  sign is a TI/TaF finality object, kept as a gated conjecture; the one-way rule is respected.
- W138 battery honored: PC1-PC4 reproduce W178 / W182 / W186 / W146 first; the fade result has the
  matched sign control (G5) and the Hermitian negative control (NC1). No canon movement; no external
  action.

**Artifacts:** this file + `tests/W187_gu_dressed_open_selfenergy.py` (15/15, exit 0).

*Filed 2026-07-14 by Team DRESSED-SELFENERGY (W187). Coherence-first; exploration grade; conditional
register; RUTHLESS skeptic. Five personas inline in one worker (self-energy/loop specialist;
open-system/source specialist; cosmological-dynamics specialist; symbolic/numerical engineer;
adversarial skeptic RUTHLESS); no sub-agents. Reproducible:
`python -u tests/W187_gu_dressed_open_selfenergy.py` (15/15, exit 0; positive controls first).
No canon movement; H59 remains OPEN.*
