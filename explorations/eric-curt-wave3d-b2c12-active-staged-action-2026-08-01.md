---
title: "Eric/Curt Wave 3D-B2C12 — Active Staged Action and Moving Residual Primalizer"
status: active-research
doc_type: exploration
updated_at: "2026-08-01"
---

# Eric/Curt Wave 3D-B2C12 — Active Staged Action and Moving Residual Primalizer

## Result

B2C12 constructs the first honest moving residual pairing in the active Eric
lane and corrects a load-bearing type error in the earlier scaffold.

The manuscript places its bosonic obstruction/residual
\(\Upsilon_B^{\rm src}\) in \(\Omega^{d-1}(\operatorname{ad}P)\).  Reading
that object as the density-dual of a connection variation through an invariant
adjoint pairing is a repository typing step, not an explicit manuscript
declaration.  The map that enters its norm must **primalize** that residual.
Write

\[
b_{\rm res}:\Omega^1(Y,\operatorname{ad}P)
\longrightarrow
\Omega^1(Y,\operatorname{ad}P)^*\otimes\operatorname{Dens}(Y)
\simeq\Omega^{13}(Y,\operatorname{ad}^*P),
\qquad
R_{\rm res}=b_{\rm res}^{-1}.
\]

The typed repository realization of the manuscript glyph
\(I_2^B=\|\Upsilon_B\|^2\) is

\[
\boxed{
I_2^B=\frac12\langle E,R_{\rm res}E\rangle,
\qquad E=\Upsilon_B,
}
\]

not \(\frac12\langle E,b_{\rm res}E\rangle\).  If
\(u=R_{\rm res}E\), the complete local variation is

\[
\boxed{
\delta I_2^B
=\langle DE[\delta\phi],u\rangle
-\frac12\langle u,(Db_{\rm res}[\delta\phi])u\rangle.
}
\]

Equivalently, the second term is
\(+\frac12\langle E,(DR_{\rm res})E\rangle\), with

\[
DR_{\rm res}=-R_{\rm res}(Db_{\rm res})R_{\rm res}.
\]

The invariant adjoint form \(\kappa\) identifies
\(\operatorname{ad}P\) with \(\operatorname{ad}^*P\) only after this
pre-identification type is fixed.  The evaluation convention is
\(\langle E,u\rangle=\int_Y\kappa(u\wedge E)\), with the one-form placed
before the 13-form; reversing the wedge order contributes a minus sign.
Both \(b_{\rm res}\) and \(R_{\rm res}\) are symmetric for this density-dual
evaluation.  Their displayed component matrices already contain
\(\sqrt{|\det G_Y|}\); no second volume density is inserted in the component
action.

The minus sign in lowerer variables is decisive. Frozen-pairing, wrong-sign,
and lowerer-for-primalizer substitutions fail planted controls. A
Euclidean-positive form is not the constructed active residual map; general
positive pairings are not excluded by that check.

The construction is not a renamed rational matrix.  At a Lorentz observation
frame it starts from the actual trace-reversed metric-on-metrics tensor

\[
G_Y(g)=g\oplus G_{\rm DW}(g),
\]

where

\[
G_{\rm DW}(h,k)
=\operatorname{tr}(g^{-1}hg^{-1}k)
-\frac12\operatorname{tr}(g^{-1}h)\operatorname{tr}(g^{-1}k).
\]

It verifies the required inertia

\[
(3,1)+(6,4)=(9,5).
\]

The coefficient carrier is a nondegenerate \((-,+)\) slice of the invariant
trace form on two actual
\(\operatorname{Cl}(9,5)=M(64,\mathbb H)\) connection generators.  Both
generators satisfy the active right-\(\mathbb H\), Krein, and \(C^+\)
conditions.  On this 28-dimensional local residual slice,

\[
b_{\rm res}
=\sqrt{|\det G_Y|}\,G_Y^{-1}\otimes\kappa,
\qquad
R_{\rm res}
=\frac1{\sqrt{|\det G_Y|}}G_Y\otimes\kappa^{-1}.
\]

They are inverse, nondegenerate, and balanced with inertia \((14,14)\).
Their analytic metric derivatives agree with direct finite differences.  The
one-form/thirteen-form Hodge parity is \(+1\) in signature \((9,5)\), and the
four-dimensional trace-reversal map is an involution.

The residual-map construction does not erase the existing G2 fork.  The
source obstruction being squared is

\[
\Upsilon_B^{\rm src}=\mathscr S(F)+*\kappa T,
\]

whereas the exact Euler covector of the selected reconstructed G2 first action
has the slot-symmetrized form

\[
E_T^{\rm var}
=\mathscr S(F_{B_{\rm rot}})
+\frac12(L+L^!)T+M_\epsilon(T,T)+\kappa\flat T.
\]

They are not identified.  G2 refuted their equality for the selected
noncyclic native Shiab.  The map \(R_{\rm res}\) can type either dual
residual after its carrier port is specified; B2C13 must differentiate both
branches rather than reinsert the killed compressed source expression as the
first-layer Euler map.

There is a second carrier fork.  The manuscript norm glyph lives in its
literal \(Y^{7,7}\)/complex \(U(64,64)\)-type arena, whereas this construction
is a source-anchored repository port to the active trace-reversed \((9,5)\),
right-\(\mathbb H\) slice.  No global carrier or real-form map identifying
those objects has been built.

## The staged construction now available

The conservative repository scaffold is now:

1. a reconstructed first Einstein--Dirac layer
   \(I_{\rm ED}=I_1^B+I_F^{\rm candidate}\);
2. the separate source-anchored typed realization of the manuscript bosonic
   norm \(I_2^B\) above;
3. the draft's second-order-sourced equation retained as a rival, not imposed
   simultaneously with the first-layer equation.

This is not claimed to be a source-displayed composite action.  Weinstein's
modern two-layer description supports the staging, while the 2021 draft
supplies the bosonic norm and two alternative coupling equations.  Their
complete functional relation is unreleased.

The B2C12 southeast term is a repository-written finite fermion action term. It
was varied in both directions:

- connection variation emits the same-coefficient nonzero current
  \(J_{\rm SE}\), with \(\|J_{\rm SE}\|=54.8261074\) on the deterministic
  active witness;
- constrained \(\nu\) variation includes both the \(\nu\) term and its
  induced right-\(\mathbb H\)-partner contribution;
- the current returns through the complete local
  \(B_{\rm rot}=u\omega u^{-1}-u_i u^{-1}\) graph, including the
  \(d\epsilon\) term, with nonzero selected graph pairing \(56.4300896\).

This closes the current-only-amendment loophole.  The extra current is the
derivative of the selected lower-right operator block, not a new datum.

An exact rational residual-square first-jet comparator directly differentiates
the composite action and verifies that the \(A_{\rm tr}\), \(\epsilon\), and
metric owners are distinct.  The metric owner receives the nonzero one-half
\(DR_{\rm res}\) return, equivalently minus one-half \(Db_{\rm res}\) in
primal variables. A separate exact direction holds the comparator residual
\(DE_{\rm toy}=0\) while retaining the nonzero \(DR_{\rm res}\) term. The
owner values are

```text
E_A = (47/8, 43/8)
E_epsilon = -73/8
E_g = 23/2
```

## Formal adjoint and Green return

A polynomial one-dimensional differential model uses the active moving
\(R_{\rm res}(g(\phi))\) rather than a fixed comparator.  For

\[
E=A(x)\phi'(x)+B(x)\phi(x),
\]

direct variation agrees with the formal-adjoint bulk plus endpoint identity:

```text
direct variation = -0.01793239330
bulk return      =  0.01101828592
Green endpoint   = -0.02895067922
```

Dropping either the endpoint or the moving-primalizer return fails.  This is a
genuine differential integration-by-parts certificate, but only for the local
one-dimensional model.  It does not establish the actual \(Y^{14}\) formal
adjoint, trace theorem, closed domain, self-adjointness, maximal
dissipativity, or a global ultrahyperbolic Cauchy problem.

The bosonic square raises the Euler order.  Its boundary trace space must
include bosonic field and conormal-momentum data before it can be combined
with the first-order fermion trace space.  Replaying the old first-order
Green matrix alone would therefore compare different objects.

## Current completion and constraint surplus

B2C11's owner theorem survives.  Every distortion-only term lies in the
kernel of the shared pullback:

\[
J_A=qK,
\qquad
J_B=(1-q)K,
\qquad
J_A+J_B=K.
\]

A genuine canceler must introduce a new diagonal/shared action class \(L\),
with cancellation requiring the vector equation \(L=-K\).  Choosing \(q\)
cannot do it.  Any proposed \(L\) must arise from a written action and include
every nonzero companion variation implied by its actual field dependence.  A
displayed current alone does not establish integrability, covariance, or graph
closure; some companion categories may legitimately vanish.

The selected repository action term fixes the coefficient of
\(J_{\rm SE}\) because it is the derivative of the same selected operator
coefficient.  The source does not select a complete compensator alphabet.
One selected current plus two artificial independent ambient-coordinate
directions form a rank-three comparator; a deliberately independent fourth
direction lies outside it.  This checks the rank machinery only and does not
count admissible compensators or free action coefficients.  Therefore:

> the southeast contribution introduces zero new current coefficients, but
> the global current-completion constraint surplus is **uncomputable** until
> the admissible action-term alphabet is constructed.

No additional cyclic or compensating current is forced at this gate.  This is
not evidence that none exists.

## Layer 0

| term | object here | not identified with |
|---|---|---|
| manuscript square | scalar action \(\|\Upsilon_B\|^2\) | spoken inter-layer square/root or unreleased \(D^2\) complex |
| `Upsilon_B_src` | compressed manuscript obstruction/residual | exact slot-symmetrized `E_T_var` of the selected G2 first action |
| residual carrier port | active trace-reversed `(9,5)` right-\(\mathbb H\) slice | literal `(7,7)`/complex `U(64,64)`-type manuscript arena |
| `b_res` | one-form-to-dual/13-form residual lowerer | the map applied directly to a dual residual |
| `R_res` | inverse residual primalizer used in the typed norm | a Poisson sharp or positive Hilbert metric |
| `J_9.18` | source-written adjoint Euler covector | `J_SE` or observed Yang--Mills current |
| `J_SE` | repository action derivative of the selected southeast block | source-mandated term, mass, count, or external datum |
| `A_tr` | translation connection \(A_0+\varpi\) | unstable naked source label `A` |
| `B_rot` | gauge-rotated reference \(A_0\cdot\epsilon\) | unstable naked source label `B` |
| local Green identity | one-dimensional active-coefficient certificate | global closed \(Y^{14}\) domain |

The three uses of “square” remain homonyms until explicit intertwiners are
built:

1. the draft residual-norm action;
2. the spoken first-layer/second-layer Dirac-pair relation;
3. the unreleased two-connection \(D^2\) obstruction complex.

## Primary-source collision

### `SOURCE-CONFIRMS`

- Draft `(9.11)--(9.15)` writes the purely bosonic norm
  \(I_2^B=\|\Upsilon_B\|^2\), its partial `varpi` derivative, and a formal
  adjoint.
- Draft `(9.18)--(9.20)` writes the fermion residual/current tuple and gives
  first-order-total and second-order-sourced equations as alternatives.
- TOE `00:41:50--00:43:38` and ITI/UCSD `00:05:43--00:06:32` distinguish an
  Einstein--Dirac first layer from a Yang--Mills--Higgs second layer.
- TOE `02:44:06--02:45:13` says the modern two-connection on-shell object has
  not been released.

### `SOURCE-CORRECTS`

- `I_2^B[Q_B]` is not itself manuscript-exact.  The manuscript writes only
  the norm glyph; `b_res/R_res` is the repository's typed realization.
- The density-dual interpretation and the active `(9,5)` right-`H` carrier are
  repository ports. No global map identifies them with the literal source
  arena.
- The compressed source residual and the selected G2 action's exact
  slot-symmetrized Euler covector are not the same object.
- The displayed derivative varies `varpi` at fixed other arguments.  It is not
  the complete `epsilon`/metric/graph/domain variation.
- Naked `A/B` labels are unsafe because the draft's equation and prose swap
  them.  Role names `A_tr`, `B_rot`, and `T` are used here.
- The three square objects above cannot be collapsed.

### `SOURCE-SILENT`

The checked sources do not specify:

- the signature, density, invariant adjoint pairing, domain, or boundary data
  behind the norm glyph;
- a complete combined fermion action or its normalization relative to
  \(I_1^B\) and \(I_2^B\);
- the nonzero lower-right block, \(J_{\rm SE}\), or extra compensating terms;
- the full nonlinear \(Y^{14}\) owner graph or moving Shiab/Hodge variation;
- the factorization among the three square objects;
- a common boson--fermion Green domain.

## Seven-axis boundary

- **L1/L2:** the active local bundles, trace-reversed metric, adjoint slice,
  lowerer, and primalizer are explicitly typed.  A global atlas realization
  of the residual maps is open.
- **L3:** the local metric variation, inverse-map return, exact owner chain,
  and one-dimensional differential Green identity close.  The actual
  \(D\Upsilon_B\) symbol and full moving Shiab/graph return are open.
- **L4:** the selected construction is the staged first Einstein--Dirac layer
  plus separate typed bosonic residual norm.  The alternative sourced equation
  remains a rival.
- **L5:** right-\(\mathbb H\), Krein, and \(C^+\) compatibility is retained on
  the active connection/current carriers.  No quantum Hilbert space or
  unitarity follows.
- **L6:** no Standard Model equation, physical current, Higgs potential,
  Yukawa coefficient, generation count, dark-energy value, or dark-matter
  prediction is claimed.
- **L7:** the source boundary is recorded above.  Repository completions are
  not attributed to Weinstein.

P1, P2, and P3 are unchanged and unused.  They supply no pairing, current,
action architecture, adjoint, owner, stress tensor, or domain.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.  `TG-1 AND TG-2 AND TG-3`
remains false; no third lane is promoted.

The probe passes

```text
27 computational + 5 source receipts + 15 type-level + 10 planted = 57 PASS
```

## Next gate

Run

```text
ECW3D-B2C13-ACTUAL-DUPSILON-SYMBOL-AND-STAGED-PREBOUNDARY-JET-FORM
```

Differentiate both the compressed source residual
\(\Upsilon_B^{\rm src}=\mathscr S(F)+*\kappa T\) and the distinct exact
slot-symmetrized G2 Euler covector \(E_T^{\rm var}\) on the active section.
Preserve the fork, return every moving Shiab/Hodge/density/graph term through
\(R_{\rm res}\), and derive each enlarged bosonic field-plus-conormal
preboundary form. Only after those objects exist should the mixed
boson--fermion common-domain search resume.
