---
artifact_type: exploration
created: 2026-08-04
title: "Resolver Wave K77-A: the real (7,7) carrier supports one imported atomic Standard Model representation packet, but no physical particle is yet recovered"
grade: "Exact finite real Clifford carrier, invariant bilinears, observation branching, imported verified D5/Pati-Salam one-family charge checksum, exact K77 gamma image/kernel dimensions, source F/Q/Z branch arithmetic with K77 subspaces open, source-guided atomic target ledger, and typed cross-row coherence debts. No K77 action, vacuum selection, global observation descent, pole/residue, effective chirality, generation count, dark-sector identity, or physical domain."
named_gate: RESOLVER-WAVE-K77A-REAL-SPINOR-OBSERVATION-ATOMIC-CROSSWALK
gate_before: NORMALIZED_TRACE_FIXED_ACTIVE_95_QWEDGE_B1_CANDIDATE_MISMATCH
gate_after: NONCHIRAL_PARENT_AND_OBSERVATION_BLOCKS_CONFIRMED__VEV_BLOCK_CLASSIFIED_NOT_SELECTED__EFFECTIVE_CHIRALITY_NOT_DERIVED
route_disposition: CONTINUE_K77
source_collision: SOURCE-CONFIRMS-REAL77-NONCHIRAL-PARENT-OBSERVATION-BLOCKS-AND-ATOMIC-TARGETS; SOURCE-SILENT-ACTION-SELECTED-VEV-POLES-DOMAIN-AND-COUNT
canon_verdict_change: none
third_lane_promoted: false
---

# Resolver Wave K77-A: real spinors and an atomic physics crosswalk

## Result first

The source-aligned real `(7,7)` route survives its first exact matter-carrier
gate.

The executable constructs

\[
  \mathrm{Cl}(7,7)\cong M_{128}(\mathbb R),\qquad S\cong\mathbb R^{128},
\]

with real Majorana--Weyl halves `S+` and `S-` of dimension 64. Under the
source observation split

\[
  \mathbb R^{7,7}=\mathbb R^{1,3}\oplus\mathbb R^{6,4},
\]

the complexified half-spin modules restrict, up to one harmless global naming
of chirality, as

\[
S_{14,+}^{\mathbb C}
 = (S_{4,+}\otimes S_{10,+})
 \oplus (S_{4,-}\otimes S_{10,-}),
\]

\[
S_{14,-}^{\mathbb C}
 = (S_{4,+}\otimes S_{10,-})
 \oplus (S_{4,-}\otimes S_{10,+}).
\]

Each summand is complex 32-dimensional: a two-component spacetime Weyl
carrier tensored with one complex internal 16. An independent `D7 -> D2+D5`
weight-parity calculation gives the same four multiplicity-one blocks.

An independently verified D5/Pati--Salam chain supplies the internal 16 and
its one-family multiplets; K77-A imports that packet rather than deriving its
weights from the real carrier. The probe computes `Q=T3L+Y` for all eight
all-left atomic states, resolves the all-left/physical charge dictionary, and
checks the local Standard Model anomaly sums. It
also records all twelve abstract SM gauge directions, the four real Higgs
targets, the two graviton helicities, the cosmological/distortion target, the
looking-glass partner, the `F/Q/Z` Rarita--Schwinger sectors, the generation
count, and dark matter.

That is a carrier result, not a physics recovery claim. None of the 37 rows has
yet earned a physical pole, residue, common Euler equation, common observation
map, global domain, or jointly coherent vacuum. Twelve gauge rows are only
abstract Lie-algebra directions; eleven rows have exact `(7,7)` carrier
support; the remainder are open, source-guided, or deliberately killed at the
specific map level. Each target's declared program, SM-shadow, or source-lane
obligation remains live.

The exact verdict is:

```text
NONCHIRAL_PARENT_AND_OBSERVATION_BLOCKS_CONFIRMED
VEV_BLOCK_CLASSIFIED_NOT_SELECTED
EFFECTIVE_CHIRALITY_NOT_DERIVED
```

The next construction gate is K77-B: write and vary the source-typed bosonic
action on this actual real carrier, including the displayed Shiab/B1 grammar
and the unresolved `[T,T]` normalization. K77-C then has to select and test a
VEV-dependent fermion block rather than merely point to an allowed incidence.

## Plain English

The `(7,7)` shape is not yet the Standard Model, but it now passes a meaningful
test that the old coarse table could not express. Its real spinor has four
observation blocks of exactly the size needed for

```text
two-component spacetime Weyl spinor x internal 16-state family packet.
```

The charges of one familiar quark-and-lepton family fit inside that packet.
There is also a precise real pairing joining the two ambient chiral halves.
When that pairing is composed with a vertical Clifford coefficient, it has the
right *kinematic incidence* to couple opposite four-dimensional Weyl blocks
without exchanging the ambient luminous/partner half.

But geometry has only said “this channel exists.” It has not selected the
coefficient, made it stationary, generated masses, removed unwanted states,
or proved that low-energy observers see a chiral weak theory. Those are the
things the source action and its vacuum must do next.

## 0. How this table is allowed to fail

The science council required an obligation scope separate from moving
candidate status on every row:

- `PROGRAM_MANDATORY` marks empirical physics that the conditional program
  must recover; `SM_SHADOW_REQUIRED` marks intermediate Standard Model
  structure; `SOURCE_LANE_CONDITIONAL` marks Eric/Curt-specific predictions
  such as looking-glass and `F/Q/Z` sectors. A source-lane target is mandatory
  only while assessing that lane, not an immutable fact about nature.
- `CANDIDATE_STATUS` says how far the present GU mechanism has actually
  reached.

A failed map therefore does not erase an electron, photon, Higgs, or other
mandatory target. It emits typed reconstruction debt and kills only the
narrowest object proved impossible.

| kill scope | what is rejected | what remains live |
|---|---|---|
| `FIXTURE_FAIL` | one basis, background, normalization, or numerical witness | the typed map and mechanism |
| `CANDIDATE_MAP_KILL` | one exact domain-to-codomain map | other maps serving the same target |
| `MECHANISM_KILL` | one frozen action/map/domain family, including `LOCAL_PASS__JOINT_FAIL` | the `(7,7)` lane through other predeclared mechanisms |
| `LANE_KILL` | the source-faithful `(7,7)` lane after its admissible mechanisms are exhausted | other honest conditional GU lanes |
| `CONDITIONAL_PROGRAM_KILL` | the full declared conditional class | only after Layer 0, all seven axes, and all honest lanes fail a mandatory target |

This prevents two symmetric errors: treating every local obstruction as a
theory refutation, and preserving a locally attractive mapping after it breaks
other physics already locked by the same construction.

## 1. Eric and Curt are search directives, not final authorities

The crosswalk separates source descriptions from mathematical grades. Eric or
Curt can tell a future agent where to look and what role a construction was
intended to play. Exact typing, equivariance, variation, descent, and physics
can confirm, narrow, correct, or kill that proposed mechanism.

| source claim | intended construction | exact locator | present grade |
|---|---|---|---|
| Eric: fundamental theory is nonchiral; curvature/VEV is meant to yield effective Weyl sectors | VEV-dependent Dirac/Rarita--Schwinger block | TOE `02:36:02-02:38:12` | `SOURCE_GUIDED_CANDIDATE` |
| Eric: fermions live in chimeric spinors and the rolled `0-1-13-14` object | real spinor-valued-form carrier | TOE `02:38:12-02:43:30` | carrier confirmed, dynamics open |
| Eric: observed `2 x 16` Weyl matter comes with conjugate/higher-spin sectors | observation branching and partner carrier | TOE `02:50:38-02:54:14`; draft extraction eq. 12.20 | carrier confirmed, physical IDs open |
| Eric: the Higgs parent comes from an adjoint-valued one-form | equivariant weak-doublet extraction from connection data | TOE `01:10:37-01:10:57`, `01:35:23-01:36:08` | `SOURCE_GUIDED_CANDIDATE` |
| Eric: an Einstein--Dirac layer is followed by Yang--Mills--Higgs | common source action | TOE `00:41:50-00:43:38` | K77 action open |
| Eric: trace-reversed fibre and observation reduction expose the one-16/Pati--Salam route | one-family representation packet | *Into the Impossible* lines 149-161; TOE `02:50:38-02:52:38` | exact carrier/charge support |
| Eric/draft: `F/Q/Z` occur in the rolled one-form carrier | proposed DRS kernel decomposition | draft extraction eq. 11.6 | source branch arithmetic only; K77 subspaces/projectors open |
| Eric: cosmological constant is replaced by a distortion/connection VEV | candidate realization of late-time acceleration | TOE `02:14:44-02:23:28`; *Into the Impossible* lines 14-20, 59-86 | unbuilt |
| Curt: trace-line choice is intended to give `(7,7)` and real 128/chiral 64 | source route toward the real Clifford carrier | historical reinspection `00:39:55-00:47:20`, recap `02:58:39-02:59:18` | source-guided; downstream Cl77 carrier independently constructed; Curt's dual-horizontal sign/order completion remains untyped |
| Curt: `Omega0+Omega1` spinors and DRS branching give the fermion/generation picture | carrier and proposed family interpretation | iceberg steps 19-23, `03:00:51-03:02:15` | kernel dimension confirmed; F/Q/Z subspaces and count interpretation remain open/corrected |
| Curt: connection/fibre data supply Higgs, quartic, and Yukawa-like terms | one common connection/action mechanism | iceberg steps 24-28, `03:02:15-03:03:26` | source-guided; no selected weak doublet, potential, or mass matrix |
| Curt: inhomogeneous gauge group, distortion, Shiab, and first-order action recover GR/YM | common bosonic mechanism | iceberg steps 13-18, `02:59:55-03:00:51` | K77-B construction target |

The repository's separate whole-surface source audit found no closed-domain
pole/residue construction for each electron, quark, gluon, electroweak boson,
Higgs mode, or graviton. This is an audit conclusion, not a claim attributed
to Eric or Curt. That silence is an open mathematical burden, not evidence of
impossibility.

The machine-readable registry in
`lab/process/resolver-wave-k77a-atomic-particle-crosswalk.json` links each
atomic target to these claims so a future agent can jump directly from a
failed or incomplete row back to the relevant source passage before inventing
a replacement.

## 2. Exact real carrier and invariant pairings

The probe extends the already-receipted all-real Clifford construction in
`p77_real_index_twin.py`. It verifies all 16,383 nontrivial Clifford monomials
needed for the faithful `M128(R)` carrier and then reorders the generators into
the source split `(1,3)+(6,4)`.

Let `omega4`, `omega10`, and `omega14` be the base, internal, and ambient
volume elements. In the implemented convention,

```text
omega4^2 = omega10^2 = -1
omega14^2 = +1
omega14 = omega4 omega10
```

and after complexification `chi4=i omega4`, `chi10=i omega10` satisfy
`omega14=-chi4 chi10`. All four joint `(chi4,omega14)` projectors have complex
rank 32, are orthogonal, and sum to the identity.

There is a real Spin-invariant symmetric form

\[
  B=\gamma_{t_1}\cdots\gamma_{t_7}
\]

with `B^2=1`, trace zero, and signature `(64,64)`. It anticommutes with
`omega14`; its same-half restrictions vanish and its opposite-half pairings
are perfect by exact inverse/intertwiner identities. `B omega14` is an
invariant alternating form linearly independent of `B`; K77-A does not claim
a complete classification of invariant bilinear forms.

The commuting Lie actions exponentiate to the observation image
`(Spin(1,3) x Spin(6,4))/diag(Z2)`. The probe does not construct the global
group bundle or its descent.

This is not the quaternionic `(9,5)` carrier. K77-A forbids importing
right-`H`, `Sp(32,32;H)`, `R_J`, Kramers degeneracy, or a quaternionic
Fredholm/index conclusion.

## 3. Operator incidence is not bilinear incidence

The mass/source object is a bilinear `Psi^* K X Psi`, not the bare operator
`X`. The exact parity census is:

| coefficient `X` | bare action on `(4D chirality, ambient chirality)` | constructed invariant-Gram `KX` incidence |
|---|---|---|
| identity | `(preserve,preserve)` | `(flip,flip)` |
| horizontal Clifford | `(flip,flip)` | `(preserve,preserve)` |
| vertical Clifford | `(preserve,flip)` | `(flip,preserve)` |
| horizontal x vertical | `(flip,preserve)` | `(preserve,flip)` |

Both constructed invariant Hermitian Gram representatives produce the same
incidence table. No completeness theorem shows that they exhaust every Gram,
and no action or vacuum selects either representative. The
vertical row is therefore a real opportunity: it can be cross-Weyl in four
dimensions while preserving the ambient visible/partner half. This removes a
naive chirality objection, but it does not identify the coefficient with the
Higgs, select a vacuum, prove a mass, or close the old `SA-Y1` scalar-channel
question.

## 4. DRS carrier: useful structure, wrong count inference

For

\[
  \Gamma:T^*Y\otimes S\longrightarrow S,
\]

each ambient half has input dimension 896, image dimension 64, and kernel
dimension 832. The source's proposed branch arithmetic gives

```text
F = 64, Q = 192, Z = 576, F+Q+Z = 832.
```

The identity coefficient has zero tested kernel-to-trace leakage. One
horizontal, one vertical, and one mixed representative each have rank-128
kernel-to-trace leakage. Reverse/K-adjoint leakage and a classification of the
whole coefficient space are open, so this does not establish that a physical
action generally couples the full complex.

K77-A has **not** constructed invariant `F/Q/Z` subspaces or projectors in the
actual K77 kernel. Those rows therefore remain at exact K77 kernel dimension
plus source branch arithmetic, not `C2` K77 carrier support.

What is killed is the inference

```text
three kinematic blocks => three observed generations.
```

Those are different count objects. The target of three physical families
remains mandatory, and P3 remains unspent until an actual physical complex or
index supplies a receiving slot.

An older file,
`explorations/shiab-operator/pc1-spin77-spinor-decomp-2026-06-23.md`, used
left Clifford multiplication as if it were the natural exterior Spin action.
That method is stale. The corrected exterior-action analysis in root
`DERIVATION-PROGRESS.md` finds no spinor in the
relevant exterior carrier. K77-A carries this fence explicitly so later agents
do not resurrect the superseded construction.

Two additional stale surfaces are fenced. In
`explorations/shiab-operator/pc1-spinor-spin77-shiab-2026-06-23.md`, a fixed
`c(v)` is first called Spin-equivariant and later correctly admitted not to be;
an exact commutator plant rejects the former claim. `B` proves
`(S+)^* ~= S-`, not an equivariant identification `S+ ~= S-`. In
`lab/active-research/pati-salam-chain-verification.md`, Step 0's
`R128 -> C64 -> Weyl C32` story is superseded: the real irreducible has Schur
commutant `R`, its complexification is `C128`, and its complex Weyl halves are
`C64`. K77-A imports only that artifact's internal D5/Pati--Salam/SM branching.

## 5. Atomic target census

The ledger contains 37 rows:

| sector | rows | strongest result here |
|---|---:|---|
| fermions | 8 | an imported D5/Pati--Salam packet has exact state-by-state charge/anomaly checks; K77 dynamics remain open |
| gauge bosons | 16 | 12 abstract SM directions; `W+/W-/Z/photon` require mixing, mass, and photon-kernel construction |
| Higgs/Goldstone modes | 4 | acceptance target recorded; connection-to-doublet extraction and `4 -> 3+1` orbit open |
| gravity | 2 | geometric carrier only; helicity poles and universal stress incidence open |
| cosmology/dark matter | 2 | source-guided distortion and partner analogies only; no physical identification |
| DRS/partner exotics | 4 | exact partner carrier plus K77 kernel/source branch arithmetic; F/Q/Z projectors, quotient, spectrum, and decoupling open |
| generation count | 1 | block-to-count map killed at Layer 0; physical target preserved |

Representative row distinctions:

- `u_L` and `d_L` reach `C2_EXACT_K77_CARRIER_SUPPORT`; they are not yet
  emitted particles.
- the eight gluons and four electroweak gauge directions reach only
  `C1_EXACT_ABSTRACT_GAUGE_DIRECTION`; subgroup containment is not an
  action-selected unbroken gauge group.
- `W+`, `W-`, and `Z` remain `OPEN_MIXING_AND_MASS`; the photon remains
  `OPEN_PHOTON_KERNEL`.
- the looking-glass half is an exact partner carrier paired oppositely by `B`;
  ordinary conjugation stays within one ambient half, and neither fact identifies
  it with CP conjugation, an indefinite-metric ghost, or dark matter.
- Higgs, vertical trace, and cosmological/distortion scalar are three distinct
  targets until explicit intertwiners prove otherwise.

## 6. Joint-coherence battery

Local fits are tested against one shared construction. A row cannot advance to
physical recovery by borrowing a different observation map, action, vacuum,
or domain from its neighbors.

| common gate | K77-A status | next burden |
|---|---|---|
| representation census | partial pass | construct K77 F/Q/Z subspaces, then enumerate all multiplicities and extra poles after the actual quotient |
| common reality/chirality | kinematic pass | retain it under the global action/domain |
| common observation/no leakage | open | build equation-dual descent, normalization, quotient, and no-leakage |
| common action/Euler/domain | open | K77 bosonic + fermion action, owner variations, Ward/BV, Green domain |
| gauge closure and selection | partial | action-selected stabilizer, currents, couplings, physical poles |
| anomaly/global consistency | partial | carry anomaly cancellation through the selected physical domain and partner sector |
| Higgs/Goldstone/photon | open | extract weak doublet, stationary orbit, three eaten modes, one scalar, massless photon kernel |
| effective chirality/hidden partner | partial | selected VEV, gap, decoupling, anomaly ledger, partner fate |
| universal gravity | open | common observation derivation of helicity `+/-2` and universal stress coupling |
| parameter budget | open | write the shared parameter space before interpreting any fit |
| signature-lane separation | pass | keep `(7,7)` real mathematics free of `(9,5)` quaternionic imports |

A gate not yet run is `OPEN_NOT_RUN`, not a failure. A mapping that passes
locally and is later observed to fail one of these shared gates is recorded as
`LOCAL_PASS__JOINT_FAIL`. That kills the proposed shared mechanism if the
failure is structural; it does not erase the row or automatically kill the
entire lane.

## 7. What has and has not moved

Confirmed:

- the source `(7,7)` real spinor carrier exists exactly;
- its two real Majorana--Weyl halves and invariant cross-pairing are exact;
- the observation split has four complex `2 x 16` blocks;
- the imported internal D5/Pati--Salam 16 supports one SM-family
  representation/charge packet and cancels the tested local anomalies;
- the K77 gamma image/kernel dimensions, source `F/Q/Z` branch arithmetic,
  and operator-versus-constructed-Gram incidence are exact;
- Eric and Curt's descriptions are now attached as source search directives;
- temporary, mechanism, lane, and program kills are explicitly separated.

Not confirmed:

- a global spinor bundle or observation-section theorem on arbitrary `X`;
- invariant K77 `F/Q/Z` subspaces/projectors;
- the K77 Shiab/B1 source action or its first variation;
- an action-selected gauge group, Higgs doublet, VEV, Yukawa/mass matrix, or
  cosmological scalar;
- a physical pole, residue, propagator, positive/causal domain, or scattering
  statement for any atomic row;
- low-curvature Dirac-to-Weyl decoupling or a hidden-sector identification;
- three generations, dark matter, or a cosmological prediction.

P1/P2/P3 remain unchanged and unused.

## 8. Efficient next sequence

1. **K77-B:** construct the source-displayed bosonic Shiab/B1 action on the
   actual `(7,7)` real carrier; settle `[T,T]` normalization; vary it by
   independent direct and owner routes.
2. **K77-C:** add the fermion action and classify/select the VEV block; test
   low-curvature Weyl decoupling, hidden-partner behavior, anomalies, and a
   common domain.
3. **Atomic emission update:** rerun all 37 rows against the same action,
   observation map, quotient, and vacuum. Advance only rows with typed Euler
   incidence; emit reconstruction debt for the rest.
4. **Higgs/gauge/gravity coherence:** require the same vacuum to produce the
   weak-doublet orbit, photon kernel, gauge currents, and universal gravity.
5. **Physical spectral gate:** only then ask for poles, residues, positivity,
   causal propagation, and empirical labels.

This keeps the source material useful without making it sovereign: when a row
breaks, the source locator tells the next agent where Eric or Curt intended the
missing mechanism to live; the exact failure tells them which part must be
reconstructed rather than copied.

## Reproduction

```bash
python3 tests/channel-swings/resolver_wave_k77a_real_spinor_atomic_crosswalk_probe.py
python3 tests/channel-swings/p77_real_index_twin.py
sage tests/generation-sector/q5_spin10_vector_spinor_product_sage.py
```

The Wave-K `(9,5)` predecessor is campaign sequencing only. K77-A imports no
mathematical result from its quaternionic/right-`H` carrier.
The legacy full Pati--Salam runner is deliberately not a K77-A reproduction
dependency because its Step 0 contains the fenced `R128 -> C64` story; only
the independently checked internal D5/Pati--Salam branching is consumed.

The repaired primary probe passes `60 exact + 11 source + 18 type + 14 planted
= 103` checks. Final review/rerun receipts are recorded in the companion
disposition and hostile-review artifacts.
