---
title: "PW2F-R2B2B2I2 affine first-moving and first size-three full-evaluator certificate"
status: reconstruction
doc_type: exploration
updated_at: "2026-08-05"
---

# PW2F-R2B2B2I2 affine first-moving and first size-three certificate

## Outcome

The admitted finite S3 reduction now has durable exact coverage of every
first-moving owner/conormal direction and the lexicographically first
non-fixed size-three orbit:

```text
AFFINE_FIRST_MOVING_CERTIFIED__FIRST_SIZE3_ORBIT_CERTIFIED__DURABLE_3_OF_380__UNIVERSAL_377_OPEN
```

The predecessor's complete fixed stratum remains certified at `2/380`. This
successor proves the affine first-moving layer for all ten owners and advances
full mixed-evaluator coverage by exactly one non-fixed representative, to
`3/380`. The other `377` representatives and preregistered dense universal
heldouts remain open, the reduction engine remains unpromoted, and the
unconditional 1,925-cell fallback stays live.

## Layer 0

The following remain distinct:

- the public `(7,7)` source presentation and active trace-reversed `(9,5)`
  repository reconstruction;
- affine first-moving closure and mixed second-order universal coverage;
- one complete size-three orbit and all 380 representatives;
- a canonical representative and a complete coefficient-bank cell;
- the separate conditional-active `I1 A4` and `I2B C4` banks; and
- the Eric construction and Curt's formally separated rival track.

The public source is silent on the finite evaluator and its coverage. This is
repository-derived, reconstruction-grade evidence at the executed scope.

## Affine first-moving certificate

The durable successor byte-pins the fixed-stratum probe and registry, then
uses the predecessor's exact S3 action and total exterior/Clifford lift. For
each of the ten metric owners it evaluates the zero conormal and the four
coordinate directions. The zero case is essential: fixed-owner first motion
is affine rather than linear because the moving owner metric supplies a
nonzero origin.

The exact replay under pinned SymPy `1.14.0` plus NumPy `2.5.1` passes:

- owner/conormal affine-basis cases: `50/50`;
- preregistered dense affine-span controls: `2/2`;
- both-generator affine edges: `100/100`;
- geometry, residual, moving primalizer, and action derivative: `100/100`
  each;
- eight accepted moving Shiab families: `800/800`;
- nonzero trace-motion cases: `50/50`;
- nonzero action-derivative cases: `20/50`; and
- order-three obsolete-forward-lift rejection: `PASS`.

This closes every first-moving owner/conormal direction, including the 35
registered quartic interpolation directions. It does not by itself certify a
mixed owner pair or a second-order coefficient cell.

## First non-fixed orbit certificate

Selection is deterministic and output-blind: take the lexicographically first
canonical representative whose orbit size is three. The selected orbit is

```text
(owners 0,0; quartic point 0,0,0,4)
(owners 4,4; quartic point 0,0,0,4)
(owners 7,7; quartic point 0,0,0,4)
```

The resolver remains exact at `1,925` labels, `380` representatives, and `115`
size-three representatives. Both generators close on the orbit. All six
directed generator edges pass, with `5/6` non-self edges. Geometry, `Phi1`,
`Phi2`, Hodge, Shiab residual, moving Hodge-primalizer, and action each pass
`24/24` jet slots. Mixed residual, primalizer, action, and moving-Hodge
product-rule controls are live `3/3`; the obsolete forward lift rejects. Exact
mixed actions are `-727/144`, `-727/144`, and `107/9`.

The total successor probe passes:

```text
33 exact + 2 source + 9 type + 7 planted = 51 PASS
```

## Constraint and parameter ledger

No parameter is fitted after the S3 action, sparse curvature fixture, affine
basis, dense controls, and output-blind orbit selection are pinned. The probe
checks 1,368 raw transport equalities: 1,200 on the affine layer and 168 on the
three-cell full evaluator. Their independent rank is not computed, so no
constraint-surplus number or physics surplus is claimed.

## Non-regression matrix

| family | disposition |
| --- | --- |
| gravity | unchanged |
| gauge | unchanged |
| odd matter | unchanged |
| Higgs/Yukawa | unchanged |
| quantum/domain | unchanged |
| cosmology | unchanged |
| P1/P2/P3 | unchanged and unused |

No coefficient, support, `kappa1`, Green/Helmholtz, C3, domain, observation,
datum, or physics verdict changes here.

## Fail-closed boundary

The seven planted promotions reject treating affine closure as mixed universal
coverage, promoting the engine from `3/380`, replacing the fallback before the
other `377`, assembling or merging the two banks, beginning Green/Helmholtz,
spending P1/P2/P3, or merging Curt/promoting a third lane.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.
`TG-1 AND TG-2 AND TG-3` remains `NOT_PROMOTED`.

## Next gate

Resume at

```text
PW2F-R2B2B2I2-RESUMABLE-REMAINING-377-REPRESENTATIVE-FULL-EVALUATOR-COVERAGE-THEN-SEPARATE-C4-BANKS
```

Build resumable exact coverage over the remaining 377 representatives using
the canonical transport witnesses and preregistered dense universal heldouts.
Only complete `380/380` coverage may promote the engine or replace the
1,925-cell fallback. Assemble `I1 A4` and `I2B C4` separately afterward.
