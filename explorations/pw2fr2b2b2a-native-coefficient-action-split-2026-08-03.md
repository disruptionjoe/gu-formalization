---
title: PW2F-R2B2B2A — active moving-Shiab coefficient slots and staged-action split
date: 2026-08-03
status: SCOPED_ACTIVE_COEFFICIENT_AND_FINITE_ACTION_SPLIT_PREREQUISITE_PASS_COMPLETE_NATIVE_C5_C4_OPEN
lane: Eric-guided Lane 1; active-native and source columns separated
run: RUN-20260803-080313-gu-formalization-pw2fr2b2b2a-coefficient-action-split
---

# Result

R2B2B2A closes two narrower prerequisites without claiming the advertised
complete C5/C4 calculation.

First, the probe reuses the exact B2C15M trace/Phi/Hodge derivative of the
active trace-reversed `(9,5)` Shiab construction. One metric-owner coefficient
survives the same top-form contraction exactly:

\[
D\mathscr S_{\rm active}=-\frac{\sqrt6}{24}.
\]

This answers the old same-pipeline liveness question for one contracted active
coefficient. It is stronger than the R2B2A raw-map-only control and narrower
than a complete action bank.

Second, that exact coefficient is carried through an ordered finite stack with
six factors:

```text
density -> outer pairing -> output lowerer -> Krein -> input lowerer
        -> moving Shiab coefficient
```

Direct mixed differentiation equals an independently enumerated product rule
with six intrinsic mixed terms and thirty ordered cross terms. The complete
contracted return is `5*sqrt(6)/12`. Each named non-Shiab slot has a separately
live cross with the active moving coefficient:

| slot | exact isolated cross |
| --- | ---: |
| density | `-sqrt(6)/6` |
| outer pairing | `5*sqrt(6)/6` |
| output lowerer | `sqrt(6)/12` |
| Krein | `-sqrt(6)/6` |
| input lowerer | `-sqrt(6)/6` |

Freezing all five slots gives zero. Swapping the output-lowerer and Krein
order also gives zero. Both differ from the complete ordered return. Thus the
five slots cannot be hidden in one order-free scalar or frozen merely because
a raw moving-Shiab map is already live.

# Separate staged actions

The same finite graph is differentiated in two distinct action grammars.

The finite source-directed `I1` comparator gives

\[
D_{rs}^2 I_1=-\frac{13\sqrt6}{4}.
\]

The separately built active residual square

\[
I_2^B=\frac12\langle E,R_{\rm res}E\rangle
\]

uses a symmetric indefinite primalizer and gives

\[
D_{rs}^2 I_2^B=6182-\frac{67\sqrt6}{3}.
\]

Its direct derivative equals the independent assembly

\[
J^*R_{\rm res}J
+\langle E,R_{\rm res}D^2E\rangle
+\text{moving-primalizer returns}.
\]

On the selected off-shell fixture, the normal term is zero, the
residual-times-second-graph return is `224`, and the moving-primalizer return
is `5958-67*sqrt(6)/3`. Both corrections are live. A residual-zero control
reduces exactly to the normal term. The two finite Hessians are therefore
inequivalent and neither action may stand silently for the other.

# Hostile scope correction

The exact active datum in this swing is the contracted moving-Shiab
coefficient. The five deterministic noncommuting coefficient matrices are
finite ownership/order fixtures. They are **not** the actual induced-`Y14`
density, Krein, input/output-lowerer, or outer-pairing tensors. Likewise, the
finite `I1` comparator is not a claim that Weinstein released a complete
global first action or its relative normalization.

The result therefore earns:

- one same-pipeline contracted active moving-Shiab liveness certificate;
- complete finite product-rule accounting for all five named slots; and
- a direct finite separation of the `I1` and `I2B` Hessians.

It does not earn the actual epsilon-to-`q/u` active graph, native second
Fréchet tensor, five induced coefficient tensors, 35-monomial C5/C4 banks, or
an independent reverse/presymplectic Green reconciliation.

# Layer 0 and source collision

The primary pack retains first-order-total and second-order-sourced equations
as alternatives and writes the bosonic residual norm. The repository types
that norm through an active primalizer. It does not thereby identify the
source `(7,7)` arena with the active trace-reversed `(9,5)` right-`H`/Krein
port, or identify `I1` with `I2B`.

The active contracted coefficient, ordered product-rule certificate, and
finite action-separation witness are `REPOSITORY-DERIVED`. The actual induced
coefficient tensors, active epsilon graph, complete C5/C4, and staged relative
normalization remain `SOURCE-SILENT`.

# Campaign boundary and next gate

P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; the conjunctive
`TG-1 AND TG-2 AND TG-3` gate remains `NOT_PROMOTED`. No canon, scientific
status, public posture, scheduler, publication, characteristic, domain,
quotient, observation, or physics change occurs.

Resume at
`PW2F-R2B2B2B-ACTUAL-INDUCED-Y14-EPSILON-GRAPH-AND-35-MONOMIAL-C5-C4`:
replace the five finite slot fixtures by the actual induced tensors, port the
R2B2B1 `q/Gamma/Bhat2` dependency into the active epsilon graph, compute
complete C5 before C4 across the 35-monomial bank, and keep `I1` and `I2B`
separate.

# Executable evidence

- Main exact/source/type/control probe:
  `18 exact + 3 source + 17 type + 11 planted = 49 PASS`.
- Representation/geometry hostile review: `PASS_AFTER_SCOPE`.
- Variational/PDE hostile review: `PASS_AFTER_SCOPE`.
- Source/Layer-0 hostile review: `PASS_AFTER_SCOPE`.
