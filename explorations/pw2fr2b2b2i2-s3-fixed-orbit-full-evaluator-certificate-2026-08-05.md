---
title: "PW2F-R2B2B2I2 exact S3-fixed full-evaluator certificate"
status: reconstruction
doc_type: exploration
updated_at: "2026-08-05"
---

# PW2F-R2B2B2I2 exact S3-fixed full-evaluator certificate

## Outcome

The complete one-cell orbit stratum of the admitted finite S3 reduction now
has a durable exact full-evaluator certificate:

```text
FULL_EVALUATOR_CERTIFIED_ON_COMPLETE_S3_FIXED_ORBIT_STRATUM__UNIVERSAL_378_OPEN
```

The durable I1 resolver grid has exactly two S3-fixed labels. Both are
certified under the reflection and three-cycle through every mixed metric,
`Phi1`, `Phi2`, Hodge, Shiab residual, moving Hodge-primalizer, and action
slot. This closes `2/2` fixed representatives but only `2/380` canonical
representatives. The other `378` remain open, the reduction engine remains
unpromoted, and the unconditional `1,925`-cell fallback stays live.

## Layer 0

The following remain distinct:

- the public `(7,7)` source presentation and active trace-reversed `(9,5)`
  repository reconstruction;
- finite geometric transport and complete evaluator transport;
- the two fixed orbits and universal 380-representative coverage;
- a canonical representative and a computed complete coefficient cell;
- the separate conditional-active `I1 A4` and `I2B C4` banks; and
- the Eric construction and Curt's formally separated rival track.

The public source is silent on this active finite reduction. The result is
repository-derived and reconstruction-grade only at the executed scope.

## Exact construction and verification

The durable probe imports only the byte-pinned I1 geometric certificate and H
mixed-Shiab jet. It reconstructs the canonical S3 resolver, exterior/Clifford
total lift, moving Hodge product rule, action jet, and fixed-orbit selection in
one owner-versioned executable.

The fixed representatives are

```text
(owners 9,9; quartic point 0,0,0,4)
(owners 9,9; quartic point 1,1,1,1)
```

Each is stabilized by all six group elements, so the four tested generator
edges are self-transport checks rather than ordinary hops to different labels.
The exact replay under pinned SymPy `1.14.0` plus NumPy passes:

- dependency hashes: `3/3`;
- fixed-label group stabilizers: `12/12`;
- generator edges: `4/4`;
- geometry, `Phi1`, `Phi2`, Hodge, residual, moving primalizer, and action:
  `16/16` slots each;
- mixed residual, primalizer, and action liveness: `2/2` each;
- moving-Hodge product-rule controls: `2/2`;
- old forward-lift rejection: `PASS`; and
- exact mixed actions: `215/8` and `87/16`.

The final probe total is

```text
19 exact + 2 source + 8 type + 6 planted = 35 PASS
```

## Constraint and parameter ledger

No parameter is fitted after the finite action, evaluator convention, and
sparse curvature fixture are pinned. The probe checks `7 x 16 = 112` raw
transport equalities across the seven evaluator layers. Their independent
rank is not computed, so this packet makes no numerical constraint-surplus
claim. No physical surplus is defined at evaluator-transport scope.

## Non-regression matrix

| family | disposition |
| --- | --- |
| gravity | unchanged |
| gauge | unchanged |
| odd matter | unchanged |
| Higgs/Yukawa | unchanged |
| quantum/domain | unchanged |
| cosmology | unchanged |
| P1/P2/P3 | unchanged and unused |

No coefficient, rank, support, `kappa1`, Green/Helmholtz, C3, domain,
observation, characteristic, datum, or physics verdict changes here.

## Fail-closed boundary

The six planted promotions reject:

1. promoting the 380-representative engine from `2/380` coverage;
2. replacing the 1,925-cell fallback before the other 378 representatives;
3. assembling or merging `I1 A4` and `I2B C4`;
4. beginning Green/Helmholtz from the fixed stratum;
5. spending P1/P2/P3 on evaluator symmetry; and
6. merging Curt or promoting a third construction lane.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.
`TG-1 AND TG-2 AND TG-3` remains `NOT_PROMOTED`.

## Next gate

Resume at

```text
PW2F-R2B2B2I2-RESUMABLE-REMAINING-378-REPRESENTATIVE-FULL-EVALUATOR-COVERAGE-THEN-SEPARATE-C4-BANKS
```

Build a resumable exact evaluator over the remaining 378 representatives,
reusing the canonical transport witnesses and durably integrating the
scratch-certified affine first-moving basis. Only complete 380-representative
coverage plus the preregistered dense held-outs may replace the 1,925-cell
fallback. Assemble `I1 A4` and `I2B C4` separately afterward.
