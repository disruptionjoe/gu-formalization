---
artifact_type: exploration
doc_type: results_of_preregistered_node
status: "Phase-1 execution of pre-registered node N4 of the dictionary gate tree (commit 67ea601). OUTCOME: the pre-declared SURVIVE condition fired -- the two frozen Z/3s admit a canonical-up-to-inversion character-set isomorphism, exact on frozen receipts, no new import. The order-reading gains its discriminating second statement AS A PREDICTION (the N6 fingerprint hunt is now armed); no generation count declared."
created: 2026-07-20
directed_by: "Joe direct chat, 2026-07-20 (Phase 1: N4, pre-registered node)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
preregistration: "explorations/phase0-dictionary-gate-tree-2026-07-20.md, node N4 (commit 67ea601); kill/survive conditions executed as declared, no mid-flight scope change"
probe: tests/channel-swings/n4_two_z3s_probe.py
grade: "COMPUTED / exact (pure Fraction arithmetic, 27 checks = 6 [T] + 16 [E] + 5 [F], exit 0, deterministic). All structural inputs are frozen in-repo receipts; the only table inputs are the ones the parent docs already carried (e_KO(nu) = 1/24, Adams). No claim/canon/verdict/posture movement; the dictionary gate (H6 limit b) stays OPEN."
related:
  - explorations/phase0-dictionary-gate-tree-2026-07-20.md
  - explorations/torsion-generation-arena-2026-07-20.md
  - explorations/verify-torsion-arena-2026-07-20.md
  - canon/order3-equivariant-rho-RESULTS.md
  - canon/gamma-traceless-38-adjudication-RESULTS.md
  - canon/families-e-invariant-order3-monodromy-RESULTS.md
  - explorations/blockbuster-p4-generation-doors-2026-07-19.md
  - explorations/wave33/H6-family-puzzle-theorem-2026-07-11.md
sources:
  - "Adams, J.F., On the groups J(X) IV, Topology 5 (1966) -- e_KO(nu) = 1/24, injectivity of e_KO on im J (cited via the arena doc's table inputs, not re-derived)."
  - "Atiyah-Patodi-Singer II, Math. Proc. Camb. Phil. Soc. 78 (1975) -- rho invariants of flat bundles; the e-invariant as an eta-invariant (SUPPORTING CONTEXT ONLY; not load-bearing for any check)."
---

# N4: the two-frozen-Z/3s comparison (pre-registered node, executed)

## 0. What was pre-registered, and what fired

The tree's N4 cell (verbatim intent): compare the character set of `<J(k)>` (e_KO values
1/3, 2/3) with the order-3 Nikulin monodromy's equivariant rho classes ((0,1,2)/3 Dirac;
(0,2,1)/3 carrier B) and determine whether the monodromy action has any functorial map to
the flavor slots (the 16-unit copies of Door A).

- Pre-declared KILL: the two Z/3s provably unrelated (no natural map; rho classes an
  artifact of the K3 benchmark, not the carrier) -- order-reading demoted to convention.
- Pre-declared SURVIVE: the order-reading upgrades -- it PREDICTS an order-3 cyclic
  structure permuting generation slots, a second checkable statement in frozen content.

**Outcome fired: SURVIVE**, at exactly the declared strength and no more. The character-set
identification holds with receipts and no new import (Section 2); both named kill
mechanisms are refuted by exact arithmetic (Section 3); the functorial map to the flavor
slots is NOT exhibited in frozen content and is hereby the sharp PREDICTION that node N6
hunts (Section 4). Probe: 27 checks, exit 0.

## 1. The two frozen structures, as frozen

**Side (i), the arena Z/3.** `A = <J(k)> = {0, 8nu, 16nu} subset pi_3^s = Z/24`, from the
commutant twist `k = +-64` (torsion-arena doc, hostile-verified). Two exact facts the probe
recomputes: `A` is orientation-independent (`<8> = <16> = {0,8,16}`) and `A` is the UNIQUE
order-3 subgroup of Z/24 -- the arena side involves no choice at all. Its canonical
character data: `e_KO|_A = {0, 1/3, 2/3}`, injective (Adams, `e_KO(nu) = 1/24`).

**Side (ii), the monodromy Z/3.** `G = Z/3` = the Nikulin symplectic deck group; its flat
characters `alpha_k` carry the fine equivariant rho: Dirac `(0,1,2)/3`; carrier B
gamma-traceless `(0,2,1)/3`; carrier A ghost-subtracted `(0,0,0)` -- with the fiber-index
formula `rho_k == -(k/3) * ind mod Z` (referee derivation, canon), which the probe verifies
reproduces all three carriers exactly (ind = 2, -38, -42). Decisively for this node, the
canon docs ALREADY freeze both nonzero carriers in Z/24-arena coordinates: Dirac
`(0,16,8)`, B `(0,8,16)` (gamma-traceless-38 doc, "Z/24 arena" line). The shared receptor
is frozen on both sides; nothing here constructs it.

## 2. The identification, constructed

Define `Phi = (e_KO|_A)^{-1} o rho : Ghat -> A`. Existence is a THEOREM of the frozen
values, not a definition-by-fiat, because it requires (and the probe checks exactly):

1. `rho` is an injective HOMOMORPHISM `Ghat -> (Q/Z)_(3)` (all 9 additivity pairs, both
   carriers). Generic rho data is not homomorphic; this is real structure.
2. `image(rho) = image(e_KO|_A) = {0, 1/3, 2/3}` on the nose, equivalently the frozen
   arena columns land exactly on the 3-Sylow `{0,8,16} = <J(k)>` under either sign
   convention (and the two frozen columns share ONE consistent convention,
   `N_k = -24 rho_k mod 24` -- a coherence check on the canon data itself).

Then `Phi` is the UNIQUE intertwining isomorphism per carrier:

| carrier | Phi(alpha_1) | Phi(alpha_2) | status |
|---|---|---|---|
| Dirac (ind 2) | `8nu` | `16nu` | iso onto A, unique |
| B, gamma-traceless (ind -38) | `16nu` | `8nu` | iso onto A, unique |
| A, ghost-subtracted (ind -42) | -- | -- | NO identification (rho = 0): correctly refused |

**Equivariance/ambiguity audit (the naturality content).** Three independent Z/2
ambiguities exist across the two sides: carrier swap (Dirac vs B), generator/character
relabeling (`alpha_k -> alpha_{-k}`, the canon's own "only the 1-vs-2 label moves"
convention sweep), and the arena's unpinned residue (which of `8nu/16nu` is `J(+64)`).
The probe computes the FULL admissible set of intertwiners over all conventions: exactly
`{Phi_Dirac, Phi_B}`, closed under inversion, of size 2 = |Aut(Z/3)|. All three
ambiguities act through the SAME inversion and `Phi` intertwines them. So the
identification is canonical up to `Aut(Z/3) = Z/2` -- and Z/2-up-to-inversion is precisely
the level at which BOTH sides were independently frozen (arena residue not pinned;
monodromy 1-vs-2 label convention-mobile). The identification is as canonical as the two
structures themselves are; no finer identification is possible even in principle, and
none is needed for the order-reading (which consumes the subgroup and its order, both
inversion-invariant).

**Import ledger: zero new imports.** Every load-bearing value is a frozen in-repo receipt
(arena doc; two rho canon docs; their own Z/24 columns). `e_KO(nu) = 1/24` is a table
input those parent docs already carried. APS-II ("the e-invariant IS an eta/rho-type
invariant" -- the reason the two sides share an invariant TYPE, not merely a value group)
is cited as supporting context only; no check depends on it. The tree's kill clause
"identification requires a new import" therefore does NOT trigger.

## 3. The two named kill mechanisms, tested and refuted

- **"No natural map."** Refuted: `Phi` exists, is unique per carrier, and is equivariant
  for the full convention group (Section 2). What would have killed: rho failing
  additivity or injectivity, or its values missing `e_KO(A)`. Controls prove these gates
  have teeth (below).
- **"Rho classes an artifact of the K3 benchmark, not the carrier."** Refuted by the
  canon's own trichotomy, recomputed exactly: over the IDENTICAL K3 monodromy geometry,
  carrier A gives `(0,0,0)`, Dirac gives `(0,1,2)/3`, carrier B gives `(0,2,1)/3` -- the
  class is a function of the carrier K-class (`-ind mod 3` = 0, 1, 2 respectively), not of
  the benchmark geometry. Additionally the SAME monodromy's topological invariant
  (Lefschetz `L = 6 == 0 mod 3`) yields the trivial map: the fine spectral level is
  load-bearing, exactly as the families-e-invariant doc found.

**Controls (demonstrated power, all correctly REJECTED by the probe):**
planted mismatched pairing `(0, 1/3, 1/3)` -- values inside the right subgroup, so
image-membership alone would wave it through, but no injective intertwiner exists (the
gate is not testing mere image-equality); carrier A `(0,0,0)`; Lefschetz `(0,0,0)`;
a Z/9 imposter `(0, 1/9, 2/9)` -- no e_KO preimage in Z/24 exists at all (the gate is
order-sensitive, unlike the sylow-reading's indiscriminateness, tree G5).

**The vacuity objection, answered head-on.** Q/Z has a unique order-3 subgroup, so
image-equality of any two nonzero Z/3-valued invariants is automatic and proves nothing.
The probe's discriminating content is everything BEYOND image-equality: the homomorphism
property (F1 fails it), order-sensitivity (F4), carrier-sensitivity (F2/F3), the shared
frozen Z/24 receptor computed independently on both sides with one consistent sign
convention, and the exact match of the residual ambiguity groups. That is what
"the same Z/3 in the program's own structure" cashes out to at character-set level.

## 4. What is NOT established (the honest boundary of SURVIVE)

1. **No geometric identification of underlying objects.** The fiber-core `S^3` (arena
   side) and the K3 mapping torus `T_phi` (monodromy side) are different manifolds; no
   frozen map connects them. What is identified is the character-set/invariant structure,
   not a geometric equivalence. A referee may say "two detectors reading the same dial";
   the response is Section 3's carrier-sensitivity plus the ambiguity-group match -- but
   the geometric bridge remains open and unclaimed.
2. **The functorial map to the flavor slots is NOT exhibited.** Door A's flavor slots
   (3 (generation, mirror) pairs per 16-unit in ker(Gamma), with the unpaid x2 debt) live
   in the Cl(9,5) rep; the Nikulin action lives on the K3 benchmark. Frozen content
   supplies no map between them. Per the tree's own architecture this is the PREDICTION
   the order-reading now makes -- an order-3 cyclic structure permuting the triplet's
   3 pairs per 16-unit -- and hunting its fingerprint in ker(Gamma) structure is node N6,
   not N4. N6 is now armed with a sharp target.
3. **SG4 conditionality.** Which carrier is GU's (ghost-subtract A vs geometric-complete
   B) is the open SG4 binary. If SG4 lands on carrier A, the monodromy Z/3 vanishes on
   the GU-selected generation-arena carrier and the identification, while still true of
   Dirac/carrier B, loses its GU-carrier anchor. The identification inherits SG4's
   conditionality and the framed-reading fork (both pre-existing, no new N items).
4. **Distinct nonzero-ness mechanisms, stated.** Arena: `3 -not-| 64m` (C-04 ledger,
   carrier-independent). Monodromy: `3 -not-| ind` (carrier-selected). The probe computes
   both. The identification matches the resulting structures; it does not merge the
   mechanisms.
5. **Type discipline (tree obligation 1).** The operational counter is an unsigned
   cardinality; `Hom(Z/3, Z) = 0` is re-verified in the probe. This node establishes a
   structural identification ONLY. No generation count is declared, and the dictionary
   gate (H6 limit b) remains OPEN.

## 5. Five-lens council (inline, answered in writing)

**Representation theorist.** The comparison is correctly posed at the character level:
`Ghat = Hom(Z/3, U(1))` vs the subgroup `A` with its canonical Q/Z-valued character
`e_KO`. The load-bearing rep-theoretic fact is that rho here is a CHARACTER (additive in
k) -- verified exactly, not assumed; generically rho functions on a character group are
not homomorphic, and F1 shows the pipeline kills the non-homomorphic imposter. The
residual `Aut(Z/3) = Z/2` is irreducible: Z/3 has no canonical generator, so demanding a
pinned isomorphism would be demanding structure neither frozen side possesses. Duality
bookkeeping is honest: the identification is `Ghat -> A`, and no leg silently identifies
`G` with `Ghat` (which would need a chosen primitive character). Passed.

**K3-geometry / Nikulin specialist.** The monodromy inputs are the adjudicated canon:
6 fixed points, weights `(zeta, zeta^{-1})`, `(r,s) = (10,12)` after the order-2/order-3
correction; the rho values used are the mod-Z classes, which the canon showed invariant
across spin structure and the 24-combination convention sweep (only the 1-vs-2 label
moves -- exactly the inversion this node quotients by). The geometric caveat stands and
is stated: K3 is a benchmark, not a GU-native object; the specialist's one demanded
sentence is Section 4.1's -- no geometric map between the S^3 and T_phi is claimed. The
carrier trichotomy over fixed geometry (0 / class / inverse class) is the right rebuttal
to "benchmark artifact." Passed with boundary 4.1 mandatory.

**Stable-homotopy theorist.** The arena side is clean: the 3-Sylow of `pi_3^s = Z/24` is
the unique order-3 subgroup (computed), `e_KO` is injective on im J = everything in this
degree (Adams, table), so `A` with `e_KO|_A` is choice-free. The identification factors
through `(Q/Z)_(3)`, the correct common receptor for 3-primary e/rho data; the sign
convention `N = -24 rho` in the frozen columns is an orientation convention absorbed by
the inversion quotient, not a discrepancy. One warning, recorded: do not let "same Z/3"
drift into "same stable homotopy class" -- the monodromy side has not been given a framed
Pontryagin-Thom representative in this repo, and until it is, the identification is at
invariant level. Passed with that phrasing discipline.

**GU-native geometer.** What the program gains: its two independently-built order-3
structures -- one from the frozen carrier's commutant (arena), one from the family
benchmark's fine spectral asymmetry (rho) -- carry literally the same character set in
the same frozen Z/24 coordinates, with ambiguities that match. The order-reading's second
statement is now a live prediction aimed at ker(Gamma)'s triplet structure (N6), which is
native territory (H1 triplet, Door A). The honest native-side accounting: nothing native
was constructed here; the node consumed frozen receipts. The SG4 binary is now visibly
also the binary of WHICH monodromy character class the GU carrier would realize
(`alpha_1 -> 8nu` vs `-> 16nu` vs none) -- a new sharp face on an old open bit. Passed.

**Adversarial referee.** Attacks run: (a) vacuity -- answered in Section 3 (the gates
that do the work are homomorphism, order-sensitivity, carrier-sensitivity; F1/F4 prove
they kill); (b) convention-shopping -- answered by computing the FULL admissible set over
all conventions (size exactly 2, the forced minimum); (c) new-import smuggling -- import
ledger in Section 2, zero new, APS-II demoted to context; (d) numerology-by-24 -- the 24
enters only through the parent docs' frozen coordinates, and this node's arithmetic never
manufactures a value from it; (e) scope creep toward a count -- blocked by the type guard
(F5) and by the deliverable's own language; (f) the strongest surviving objection: the
identification is invariant-level, and a skeptic can coherently hold that two unrelated
Z/3s are being read on one dial until N6 finds (or provably fails to find) the predicted
fingerprint in ker(Gamma). That objection does not meet the pre-declared kill bar
("provably unrelated" / "requires new import" -- neither holds), so SURVIVE stands, with
(f) recorded as the live risk the tree's N6 was designed to discharge. Passed as declared.

## 6. Receipts

- `tests/channel-swings/n4_two_z3s_probe.py`: 27 checks = 6 [T] + 16 [E] + 5 [F], ALL
  PASS, exit 0, deterministic, pure exact Fraction arithmetic, no floats. HEADLINE
  emitted by the probe matches Section 0's outcome.
- Frozen inputs read in full this session: the tree (67ea601, node N4 and Sections 1-2,
  5-7), torsion-arena doc + its hostile-verification correction header, both rho canon
  docs, families-e-invariant doc, Door A sections of blockbuster-p4.
- Pre-registration honored: kill/survive executed as declared; the flavor-slot search was
  NOT performed (it is N6); no mid-flight additions to the tested conditions.

## 7. Boundary

Exploration-grade execution of a pre-registered node. No claim, canon, verdict, or public
posture movement. The dictionary gate (H6 limit b) remains OPEN: what changed is that the
order-reading's candidate second statement is now RECEIPT-BACKED at character-set level
and sharpened into N6's concrete target (an order-3 cyclic permutation of the triplet's
3 (generation, mirror) pairs per 16-unit, up to inversion). Everything is conditional on
the framed-reading fork and SG4, as the parent docs already were. Pauses for Joe; N6 is
Joe-gated per the tree.
