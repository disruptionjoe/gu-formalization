---
artifact_type: exploration
status: exploration
doc_type: protocol_enrollment
created: 2026-08-24
target_claim: NONE-NOT-A-KILL
title: "SG4 bit-2 reconciliation protocol enrollment: different questions, one exact conflict trigger"
grade: "Exact process and Layer-0 question typing against the current source/action record. No action, vacuum, phase or ledger conflict is constructed."
disposition: PROTOCOL_ENROLLED__CONFLICT_NOT_FIRED__PHASE_FORK_OPEN
canon_verdict_change: none
steering_effect: unchanged
canonical_effect: none
depends_on:
  - lab/active-research/joe-directed/lens-digs/ldd-sg4-bit2-selector-governance-2026-08-17.md
  - lab/process/layer0-fork-registry.yaml
  - lab/process/conditional-physics-ledger-v0.263.json
  - lab/sources/source-claim-register.yaml
  - explorations/sg4-observed-epoch-binning-independent-review-2026-08-24.md
scripts:
  - tests/channel-swings/sg4_bit2_reconciliation_protocol_probe.py
---

> [!IMPORTANT]
> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.
> Classification: `INTERNAL_STRUCTURAL_ONLY` — this enrollment governs how a
> future source/action conflict is typed. It is not evidence about either
> phase and does not construct a particle-physics mechanism.

# SG4 bit-2 reconciliation protocol enrollment

Scope: this result binds only the control contract for the still-open
`SG4-BIT-2-PHASE` fork. It selects neither endpoint, changes no source claim,
and creates no conditional-ledger conflict entry.

```gu-typed-objects
result: SG4 bit-2 reconciliation control contract
carrier: open coarse PHASE endpoint declaration LAYER=source-print CHIRALITY=N/A
pairing: NONE
real_structure: UNTYPED
grading: Layer-0 distinction between phase selection and source condition
action_owner: source-action
target: future action/source conflict-routing predicate MAP-TYPE=not-a-map
```

## Preflight and retrieval

LD-D card 15 was re-read against the live fork registry, `SC-CHI-01`, the
immutable v0.263 ledger taxonomy and all six existing
`over_determined_escalations`. The proposal's broad split-brain premise is not
retained: the source and Lane 1 do not compete to answer one question.

Layer 0 separates the two authorized pronouncement surfaces:

1. `LANE-1-STATIONARY-PHASE` may answer which phase is realized, but only from
   an action-owned constructed stationary vacuum.
2. `SC-CHI-01` states the source's condition on that realized phase: the
   relevant `varpi` sub-fields are not pulled significantly above zero.

No third surface is authorized to settle the bit, and neither of these two
overrides the other by rank because they answer different questions.

## Route council

The source-fidelity lens preserves `SC-CHI-01` as a conditional assertion, not
an action solution. The variational lens leaves phase selection to a complete
action and its stationary equations. The Layer-0 lens rejects a generic
source-versus-action disagreement because the questions differ. The ledger
integrity lens rejects creating a row or escalation before a conflict exists.
The fail-closed consumer lens requires the unresolved bit to remain unusable as
a settled input. The hostile contrary route—let either surface win by rank—
would silently erase either source provenance or action ownership.

## Enrolled protocol

Conflict has one exact trigger: Lane 1 constructs an action-owned stationary
vacuum with `varpi` VEV significantly above zero. Prose disagreement,
different emphasis, different hedge strength and the current absence of a
stationary vacuum are explicitly non-conflicts.

If the trigger fires, the integration owner files an
`over_determined_escalations` entry in the next coherent ledger mint, owned by
`sg4-bit-2-selector` and restricted to the standing four dispositions:
`GENUINE_FALSIFICATION`, `FORK_ARTIFACT`, `SCOPE_ERROR` or `STALE_PREMISE`.
Until adjudication, no consumer may cite bit 2 as resolved.

The trigger has not fired. The current repository contains no action-owned
constructed stationary vacuum, so filing an escalation now would invent
scientific state. Immutable ledger v0.263 therefore remains byte-identical and
`SG4-BIT-2-PHASE` remains open.

## Postflight hostile review

Strongest overclaim: “enrolled” could be misread as an adjudication. It means
only that the future conflict boundary and routing are now explicit.

Strongest contrary case: a future action may construct a high-`varpi` vacuum
and a source reinspection may narrow the phrase “significantly above zero.”
That is exactly why the protocol files an escalation rather than preselecting
a disposition.

Weakest seam: the trigger retains qualitative source language. This is
deliberate; a numerical threshold is absent and cannot be manufactured by a
process contract. The certificate therefore pins the phrase and current
non-firing condition, not a number.

No source polarity or adherence, action owner, SG4 phase, ledger row,
prediction, confirmation, paper, priority, canon verdict or public posture
moves.
