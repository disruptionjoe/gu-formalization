---
title: "PW2F-R2B2B2G: exact normalized-trace transport exposes the mixed-Shiab full-C4 blocker"
status: exploration
doc_type: construction_result
updated_at: "2026-08-03"
branch: agent/null-clifford-omega1-repair
run: historical-investigation
grade: "EXACT ACTIVE NORMALIZED-DEWITT-TRACE FIRST-AND-MIXED COFRAME TRANSPORT; FULL I1 A4 BLOCKED ON MIXED SHIAB JET; DISTINCT I2B C4 BLOCKED ON COMPLETE SECOND PRIMALIZER JET"
canon_verdict_change: none
---

# R2B2B2G normalized-trace transport and full-C4 admission

## Result

The full-C4 gate does **not** admit interpolation yet. It advances to the first
exact typed blocker.

R2B2B2F placed the source-explicit

\[
F_B+\frac12D_BT+\frac13[T,T]
\]

transgression on the exact nonlinear Zorro metric and symmetric orthonormal
coframe, but kept Shiab's distinguished normalized DeWitt trace frozen. This
swing constructs the missing geometric trace vector

\[
t(g)=\frac12g\in \operatorname{Sym}^2T^*X
\]

in coordinate components and transports its exact `(1,r,s,rs)` jet through
that same coframe.

For three independent owner/conormal cases—trace-diagonal, off-diagonal, and
mixed—the first slots agree with an independent `moving_phi` derivative when
the **full Zorro metric motion** and coordinate trace motion are supplied.
The norm jet is exactly

\[
(-1,0,0,0),
\]

so the transport preserves the negative-unit normalized trace through mixed
order.

The transported mixed slot is nevertheless nonzero in all three cases. The
nonlinear horizontal split also adds live first-order trace corrections in
five of the six tested directional slots beyond the older pure-metric fixture.
Thus the trace is normalized but not constant in this coframe at Hessian
order.

## Why the complete banks stop here

The currently accepted `moving_metric_shiab_parts(curvature,h,trace_motion)`
constructor is first-order. It has no mixed `(h,k,D2t)` input and therefore
cannot consume the live `rs` trace component just constructed. A complete
metric Hessian of `I1` requires the mixed derivatives of the trace insertion,
`Phi1`, `Phi2`, the nested Hodge maps, projection, density, and their cross
terms on the same nonlinear graph.

Accordingly:

- the complete 35-monomial `I1 A4` tensor is
  `BLOCKED_ON_MIXED_SHIAB_JET`;
- the four-dimensional multi-index/Krein Green and Helmholtz classifier is
  downstream and remains open;
- the distinct off-shell `I2B C4` bank is separately
  `BLOCKED_ON_COMPLETE_SECOND_PRIMALIZER_JET`; and
- no coefficientwise `kappa1` classifier is licensed.

No arbitrary symmetric quartic is used to fill either missing geometric jet.
The R2B2B2F rows `(3/40,-8/25)` and `(-9/50,0)` remain accepted only at their
narrow conditional frozen-Shiab raw-density grade.

## Layer 0 and construction fork

| object | disposition |
|---|---|
| geometric normalized DeWitt trace | `t(g)=g/2`, transported exactly |
| fixed-frame trace coordinates | distinct from moving-coframe components |
| Clifford trace insertion | moving-coframe vector inserted into Shiab |
| pure-metric B2C15M trace derivative | valid first-order fixture; incomplete for the derivative-bearing Zorro horizontal split |
| source `I1` | written transgression grammar; source-silent on active mixed trace/Shiab jets |
| repository `A4` | active derived Euler coefficient, not yet constructed completely |
| manuscript `I2B` | distinct residual-square action and distinct second-primalizer debt |
| raw density / Euler / Green / Helmholtz | four distinct levels; none substituted for another |

The active trace-reversed `(9,5)` geometer-native carrier remains the tested
side. The unported source `(7,7)` presentation is not silently identified with
it. No result about the standard positive-Hilbert comparator is inferred.

## Specialist hostile review

The first executable version made a real typing error: it compared the full
Zorro trace derivative to the earlier pure-metric trace derivative. The
differential-geometric review rejected that equality because the Zorro metric
also moves the horizontal split. The repaired check passes the full metric
jet into `moving_phi`, matches both first slots exactly, and records the
additional horizontal correction separately.

The variational/PDE review then rejects a full-bank or Green claim: a live
mixed trace component plus a first-order-only Shiab API leaves the Hessian
dependency graph open. The source/Layer-0 review confirms that the manuscript
fixes the `1/2,1/3` grammar but supplies no active mixed trace/Shiab or
primalizer jet.

Final hostile verdict: `PASS_AFTER_REPAIR` at the narrowed admission-blocker
grade.

## Validation

- R2B2B2G probe: `19 exact + 2 source + 14 type + 4 planted = 39`, zero
  failures.
- R2B2B2F predecessor replay: `12 exact + 2 source + 17 type + 6 planted =
  37`, zero failures; exact panel and projective rank two reproduced.
- Three owner/conormal trace cases: full first-order moving-`Phi` agreement,
  normalized norm jet, mixed coframe identity, and live mixed trace slot all
  pass.
- Plants reject frozen trace, arbitrary symmetric completion, one-ray Green
  promotion, and `kappa1`/physics inference.

## Remaining boundary and next gate

The next gate is

```text
PW2F-R2B2B2H-MIXED-SHIAB-SECOND-JET-THEN-FULL-A4-I2B-C4
```

Construct the mixed second Frechet jet of the normalized-trace/Phi/Hodge/Shiab
and complete residual primalizer/pairing on the exact nonlinear coframe. Only
then assemble separate 35-monomial `I1` and `I2B` C4 banks, certify the full
multi-index Green/Helmholtz identities, carry the live C3 return, and rerun the
projective classifier.

Vertical/mixed conormals, partial `Z1`, section tangents, global epsilon and
real-form port, domain, quotient, observation, characteristic, and physics
remain open. P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; `TG-1 AND TG-2 AND TG-3` remains
`NOT_PROMOTED`.
