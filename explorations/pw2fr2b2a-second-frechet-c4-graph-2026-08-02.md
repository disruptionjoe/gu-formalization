---
title: PW2F-R2B2A — second-Frechet order audit and partial moving-C4 comparator
date: 2026-08-02
status: PARTIAL_COMPARATOR_PASS_BLOCKS_COMPLETE_C4
run: historical-investigation
---

# Result

R2B2A produced a real exact quartic comparator, but hostile review blocked the
claim that it was the complete active or Eric/source-coordinate `C4` graph.
The decisive issue is Layer 0: the executable used

\[
\delta T=-\operatorname{Ad}_{h^{-1}}\delta B,
\]

which is a fixed-total-connection synthetic tangent. R2B1 had already proved
that fixed source coordinates `(epsilon,varpi)` require

\[
\delta T=-\delta q,\qquad
\delta B_{\rm full}=\delta\Gamma+\delta q.
\]

No executable `q/Gamma` split identifies those two objects. The calculation
therefore survives only as a partial ablation/comparator. It cannot clear the
corrected source-coordinate `C5` or `C4` gate.

Within that narrow comparator the exact result is useful:

- the fixed-`h`, torsion-free LC/Zorro odd `C5` subroute is zero;
- the fixed-Shiab `2+2` action Hessian has ten nonzero quartic blocks;
- the evaluated eight-slot `3+1` moving-Shiab cross is zero on the selected
  synthetic test/`T` background, while an independent hostile fixture proves
  only that the raw eight-slot map is nonzero;
- the `kappa1` mass bank has ten nonzero quartic blocks; and
- the exact `NONE/ANY/UNIQUE(kappa)` classifier returns `NONE` on this partial
  bank, with live `ANY` and `UNIQUE` controls.

The rank-35 quartic interpolation and held-outs are sound for the evaluator
actually coded. The missing graph, not the polynomial extraction, is the
blocker.

# Source and reconstruction columns

## Chartwise source-aligned column

The manuscript literally supplies a connection-difference object

\[
T_\omega=\varpi-\epsilon^{-1}d_{\nabla^g}\epsilon.
\]

The repository's `q`, `B_full=Gamma+q`, and `A_total=Gamma+varpi` are useful
reparameterizations, not the manuscript's displayed triple verbatim. At TOE
`02:19:17`, Weinstein also places the gauge-rotated Levi-Civita connection in
the contorsion slot. That supports taking the connection difference seriously;
it does not identify source `epsilon` with repository `h=exp(u)`.

The public record is unresolved about a global section: at TOE `01:18:06`
Weinstein rejects one, while at `01:19:15` Curt distinguishes a local `iota`
from a global `Gimel`. This packet therefore executes chartwise and records
`SOURCE-UNCERTAIN`; it does not claim that the source selects locality.

## Active reconstruction column

The executable uses the repository's right-`H`/Krein implementation, modern
trace-reversed `(9,5)` arithmetic, the reconstructed B2C15P LC-horizontal
connection metric, induced `Z1`, and an exact Hodge-null finite conjugator.
This is not a source-derived identification. Three distinct surfaces remain:

1. the historical manuscript/Curt `(7,7)/U(64,64)` presentation;
2. modern Eric's trace-reversed fibre `(7,3)->(6,4)`, which points toward total
   `(9,5)` arithmetic; and
3. the repository's right-`H`/Krein port, whose source bridge remains open.

# What the executable actually computes

For each base conormal and ten `Sym2(T*X)` owners, it forms the reconstructed
LC/Zorro connection tangent, conjugates it by fixed `h`, and imposes the
synthetic comparator tangent `t_i=-bhat_i`. It then evaluates:

- the `J* H_action J` fixed-Shiab `2+2` curvature/transgression Hessian;
- the eight explicitly implemented moving-Shiab `1+3` slots;
- the quadratic distortion mass bank; and
- a one-dimensional frozen-principal Green identity.

It reconstructs every ten-by-ten coefficient matrix over the 35 homogeneous
quartic monomials and passes three dense held-out conormals. A separate
two-parameter mixed derivative agrees with the assembled `2+2` entry.

The Green check is a frozen-ray Lagrange identity. Matrix symmetry is imposed
and checked as even-order owner symmetry; no independent reverse
formal-adjoint construction is claimed.

# Why complete C4 is blocked

Four debts remain inside the principal graph:

1. **Correct tangent.** Construct `delta q` independently of
   `delta Gamma`, then recompute fixed-`(epsilon,varpi)` `delta T=-delta q`.
2. **Second connection orbit.** Construct or annihilate
   `delta u`, `theta1=dexp(-u)delta u`, `d theta1`, `Bhat2`, and the off-shell
   `<E,D2 graph>` return. Fixed finite conjugation does not supply it.
3. **One co-moving frame.** The current partial comparator uses conjugated
   `bhat` in core/mass but an unmoved curvature/background in the moving-Shiab
   cross. These banks cannot be promoted until they are pulled into one frame.
4. **Five coefficient slots.** Density, Krein pairing, input/output lowerers,
   and outer pairing are listed in the order ledger but not differentiated.
   Either an exact direct-form equivalence must absorb them or their `1+3`
   crosses must be computed.
5. **Contracted-bank liveness.** The raw moving-Shiab map has a live hostile
   fixture, but the current control does not pass that fixture through
   `top_scalar`, owner/curvature differentiation, symmetrization, and quartic
   reconstruction. The selected contracted zero is exact but not yet paired
   with a same-pipeline nonzero control.

The fixed-total-connection `C5=0` result does not discharge these source or
dynamic-`h` routes.

# Source collision and Layer 0

- `SOURCE-CONFIRMS`: connection-fundamental/metric-emergent architecture;
  moving theta/section grammar; connection difference; trace-reversed fibre;
  Shiab equation-dual degree; upstairs equation followed by pullback; and the
  gauge-rotated Levi-Civita connection in the contorsion slot.
- `SOURCE-CORRECTS`: the named TOE map from projection to contraction and a
  unique preferred Shiab to a family/bespoke choice. This does not outlaw all
  historical source uses of projection language or identify Shiab with the
  active grade projector.
- `SOURCE-UNCERTAIN`: global section existence because the TOE speakers
  disagree.
- `REPOSITORY-DERIVES`: `D2(I1 composed with F)` is not the manuscript's
  separately defined `I2B=||Upsilon_B||^2` residual-square action.
- `SOURCE-SILENT`: the exact `q/Gamma` split, epsilon-to-`h` map, dynamic
  `theta1/Bhat2`, right-`H`/Krein carrier bridge, complete `C5/C4`, and
  `kappa1`.

The next calculation must retain both staged action branches: the
source-directed `I1` Hessian comparator and manuscript `I2B` residual-square
Hessian. One cannot stand silently for the other.

# Boundaries and next gate

This swing does not produce a characteristic, analytic domain, BV quotient,
observation equation, or Einstein/Yang-Mills/Dirac/Higgs/cosmological physics.
P1/P2/P3 remain unchanged and unused. Curt remains formally separate inside
the Eric lane. `TG-1 AND TG-2 AND TG-3` remains not promoted.

The next gate is
`PW2F-R2B2B-SOURCE-COORDINATE-Q-GAMMA-SPLIT-AND-COMOVING-SECOND-GRAPH-C5-C4`.
It must build the `q/Gamma` tangent split, put background and variations in one
co-moving frame, construct the `h/theta1/Bhat2` second graph, account for all
coefficient slots, and compare vary-upstairs-then-observe with
restrict-then-vary for both `I1` and `I2B`.

# Executable evidence

- Main comparator: `22 exact + 16 type + 11 planted = 49 PASS`.
- Primary-source collision: `2 exact + 11 source + 10 type + 6 planted = 29 PASS`.
- Exact/differential-geometry hostile review: initial `BLOCK` at complete-C4
  grade, then `PASS` on the repaired partial-comparator scope.
- Variational/PDE hostile review: initial `BLOCK` at complete-C4 grade, then
  `PASS` on the repaired partial-comparator scope.
- Primary-source/Layer-0 hostile review: `PASS_AFTER_REPAIR`.
