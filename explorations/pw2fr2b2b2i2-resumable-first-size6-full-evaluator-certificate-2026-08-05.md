---
title: "PW2F-R2B2B2I2 resumable first-uncovered size-six full-evaluator certificate"
status: reconstruction
doc_type: exploration
updated_at: "2026-08-05"
run_id: RUN-20260805-085253-gu-formalization-pw2fr2b2b2i2-resumable-first-size6
---

# PW2F-R2B2B2I2 resumable first-uncovered size-six certificate

## Outcome

The finite S3 reduction now has its durable append-only resumption contract
and exact full-evaluator coverage of the lexicographically first canonical
representative not contained in the `3/380` predecessor:

```text
RESUMABLE_LEDGER_CREATED__FIRST_UNCOVERED_SIZE6_CERTIFIED__DURABLE_4_OF_380__UNIVERSAL_376_OPEN__DENSE_HELDOUTS_0_OF_6_EXECUTED
```

The new representative is `(owners 0,0; quartic point 0,0,1,3)`. Its complete
orbit has six labels. All twelve directed generator edges and every mixed
evaluator layer pass exactly. Durable representative coverage is now `4/380`.
The other `376` representatives remain open, the six frozen dense heldout
inputs have not been executed, the reduction engine remains unpromoted, and
the unconditional 1,925-cell fallback stays live.

## Layer 0 and provenance

The following remain distinct:

- the public `(7,7)` source presentation and active trace-reversed `(9,5)`
  repository reconstruction;
- canonical lattice coverage and dense heldout confirmation;
- preregistering a heldout input and executing it;
- one complete six-cell orbit and all 380 representatives;
- a representative certificate and a complete coefficient-bank cell;
- the separate conditional-active `I1 A4` and `I2B C4` banks; and
- the Eric construction and Curt's formally separated rival track.

The public source is silent on the finite evaluator and its coverage. This is
repository-derived reconstruction evidence at the exact executed scope.

## Resumption contract

The ledger starts from the byte-pinned durable `3/380` predecessor. Each new
entry is one complete canonical S3 orbit, selected lexicographically from the
uncovered set before evaluator output. A failed edge stops the chain and keeps
the fallback. Exact work is serialized one heavy process at a time.

Six dense heldout seeds were frozen before their evaluator output. Each lies
outside the 1,925-cell lattice and has a nontrivial closed S3 orbit. They are
input commitments only in this swing: `6/6` preregistered, `0/6` executed.

## Exact selected-orbit certificate

The selected orbit, in canonical label order, has exact mixed actions:

| owner pair | quartic point | mixed action |
| --- | --- | ---: |
| `(0,0)` | `(0,0,1,3)` | `-523/144` |
| `(0,0)` | `(0,1,0,3)` | `-1379/288` |
| `(4,4)` | `(0,0,1,3)` | `-523/144` |
| `(4,4)` | `(1,0,0,3)` | `-1235/288` |
| `(7,7)` | `(0,1,0,3)` | `3499/288` |
| `(7,7)` | `(1,0,0,3)` | `3643/288` |

The pinned exact replay passes:

- dependency hashes: `2/2`;
- directed generator edges: `12/12`, all non-self;
- geometry, `Phi1`, `Phi2`, Hodge, residual, moving primalizer, and action:
  `48/48` slots each;
- mixed residual, primalizer, action, and moving-Hodge controls: `6/6` each;
- obsolete forward lift: rejected; and
- exact mixed-action fingerprint: matched.

The final probe total is:

```text
26 exact + 2 source + 9 type + 7 planted = 44 PASS
```

## Constraint and parameter ledger

No parameter is fitted after the S3 action, sparse curvature fixture,
predecessor coverage, representative ordering, and heldout inputs are frozen.
The shard checks 336 raw transport equalities. Their independent rank is not
computed, so no constraint-surplus number or physics surplus is claimed.

## Non-regression matrix

| family | disposition |
| --- | --- |
| gravity | unchanged |
| gauge | unchanged |
| odd matter | unchanged |
| Higgs/Yukawa | unchanged |
| quantum/domain | unchanged |
| cosmology | unchanged |
| P1/P2/P3 | unchanged and unused |

No coefficient, support, `kappa1`, Green/Helmholtz, C3, domain, observation,
datum, or physics verdict changes here.

## Fail-closed boundary

The seven planted promotions reject promoting the engine from `4/380`,
counting frozen heldout inputs as executed, replacing the fallback before the
other `376`, assembling or merging the two banks, beginning Green/Helmholtz,
spending P1/P2/P3, or merging Curt/promoting a third lane.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`.
`TG-1 AND TG-2 AND TG-3` remains `NOT_PROMOTED`.

## Next gate

Resume at

```text
PW2F-R2B2B2I2-RESUMABLE-REMAINING-376-REPRESENTATIVE-FULL-EVALUATOR-COVERAGE-PLUS-EXECUTE-DENSE-HELDOUTS-THEN-SEPARATE-C4-BANKS
```

Continue the append-only chain over the other 376 canonical representatives,
then execute the frozen dense heldout bank. Only complete `380/380` coverage
plus the heldouts may promote the engine or replace the 1,925-cell fallback.
Assemble `I1 A4` and `I2B C4` separately afterward.
