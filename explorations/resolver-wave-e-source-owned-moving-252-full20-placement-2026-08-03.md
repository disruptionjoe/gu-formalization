---
artifact_type: exploration
label: "Resolver Wave E: native moving 252 and conditional source interface"
created: 2026-08-03
status: exploration
posture: adversarial; Layer-0-first; construction-first; source-collision required
title: "A native moving 252 and a source kappa term are compatible halves; their joining port and the clean half-weight remain open"
grade: "Exact signed exterior identities, one-simple-blade deterministic native 128-spinor/full-20 matrices, a three-frame constant-conjugation fixture, and a coupled affine kappa-sector action comparator; the source-to-active real-form/Zorro port, representation-wide half-weight, actual Y14 descent, full G2/P0/Y placement, moving Shiab/fermion/bridge Euler system, VEV, mass, quotient, domain, and no-leakage remain open"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
route_disposition: CONTINUE
hostile_review_status: PASS_AFTER_REPAIRS
depends_on:
  - lab/specifications/six-axis/six-axis-template.md
  - explorations/resolver-wave-d-native-126-connection-placement-2026-08-03.md
  - explorations/unified-source-datum-packet-v0-2026-07-30.md
  - explorations/unified-source-datum-variational-emission-map-2026-07-30.md
  - lab/sources/weinstein-gu-primary-source-pack-2026-07-30.md
scripts:
  - tests/channel-swings/resolver_wave_e_source_owned_moving_252_full20_probe.py
---

# Resolver Wave E: native moving 252 and conditional source interface

## Outcome first

Wave E constructs two compatible halves but does not yet join them. The native
active `(9,5)` fixture has a moving real-252 direction. Weinstein's displayed
bosonic source action has an adjoint-valued kappa term. The public-source to
active-`Sp(32,32;H)` real-form/Zorro port is still unbuilt, so the source has
not yet been shown to admit that native direction.

The native reconstruction begins with a bulk tensorial adjoint-valued
one-form, not the already-restricted vertical defect coefficient. On the full
fourteen-dimensional chimeric carrier, the pure internal five-form comparator
satisfies

```text
delta j5(phi) = (4 horizontal + 5 vertical) phi = 9 phi.
```

The prior Wave-D `5 phi` result is therefore retained as the exact
observer-stabilizer control. It is not renamed as the full-symmetry answer.

When the same full one-form is kept as the output vector index, the actual
full-20 fixture gives a one-parameter family

```text
B_lambda(phi) = B_V(phi) + lambda B_H(phi).
```

For every one of the four source slots of one unit simple five-blade, rational
reconstruction from three native values plus three held-out values gives

```text
||P_I B_lambda||^2       = (16/7) (5+4 lambda)^2,
||P_low-R P_R B_lambda||^2 = (160/7) (1-2 lambda)^2,
||P_X P_R B_lambda||^2   = 80.
```

Consequently:

- the unweighted reconstruction value `lambda=1` has gamma trace `9 phi` and
  keeps the low-`R` 16 companion;
- the representative's unique real value `lambda=1/2` removes that low-`R`
  companion, leaves a rank-128 gamma-traceless map, and has exactly one X/144
  slot per source;
- the half-weight has gamma trace `7 phi`, not `9 phi`, and no inspected
  primary source or already-written action term selects it.

The half-weight is therefore a real construction opportunity, not a result to
smuggle in because it looks cleaner. The durable verdict is:

```text
NATIVE MOVING REAL 252: LOCAL PARTIAL_CONSTRUCTED;
DISPLAYED SOURCE KAPPA TERM: SOURCE-OWNED;
JOINED SOURCE-TO-ACTIVE 252 ARROW: OPEN;
UNWEIGHTED FULL-20 COMPARATOR: COUPLED TO LOW-R 16;
HALF-WEIGHTED S->X MAP: ONE-SIMPLE-BLADE CANDIDATE, SOURCE-SILENT;
COMPLETE FERMION/BRIDGE EULER SELECTION: OPEN.
```

The named gate moves **`OPEN -> PARTIAL_CONSTRUCTED`**. The route decision is
**CONTINUE**. P1/P2/P3 remain unchanged and unused.

## 0. Layer-0 table

| phrase | typed object | Wave-E ruling |
|---|---|---|
| Eric source distortion | `T_omega=varpi-epsilon_src^{-1}d_0 epsilon_src in Omega1(Y,ad P)` | strongest source-native bulk owner |
| N1 bulk comparator | `Delta A=A-A0 in Omega1(Y,ad P)` | action-owned comparator; identity with `T_omega` unproved |
| defect residue | `v_s=res_s^V Delta A` | vertical `5 phi` carrier only; cannot by itself supply the full `9 phi` component |
| source `epsilon` | gauge transformation in the inhomogeneous source action | not the soldering field |
| `epsilon_IG` | moving Clifford/soldering field | moves the Clifford plane/projectors; not source `epsilon` |
| `Theta_Z` | connection/splitting identifying the chimeric bundle with `TY` | distinct from N1 tensorial distortion `theta_dist` |
| real 252 | `Lambda5(V10*)` with `star^2=-1` | one real carrier with conjugate complex 126 halves |
| full-20 map | a rectangular `S -> R subset V* tensor S` insertion and reciprocal | not the written `c_rho:S->S` kernel |
| displayed source kappa term | `* kappa_1 T_omega` in the source residual | source-owned, but its restriction to the active `j5` direction is conditional on the unbuilt source-to-active `(9,5)` port |
| joined source-to-active Euler coefficient | component of the varied source residual pulled through the actual source/native port by `j5^!` | open; the signed exterior adjoint and an isolated kappa-sector comparator are constructed separately |
| VEV/mass | nonzero stationary background plus observed four-dimensional mass operator | not constructed |

Layer 0 forbids two tempting compressions. `T_omega`, `Delta A`, and `v_s` are
not one object merely because all are connection differences or restrictions.
Likewise, a nonzero source Hessian is not a stationary VEV or mass.

## 1. Specialist preregistration

Three divergent specialists reviewed the repo and primary-source packet before
construction.

1. **Source/differential geometry:** required the full bulk one-form and the
   moving Zorro/soldering map before the vertical restriction; required paired
   `T_omega` and `Delta A` branches rather than a fiat identity.
2. **Full-20/Krein:** required a rectangular `S <-> R` construction, total K/C
   reciprocals, every allowed `P0`, and right-`H` provenance. This lens found
   the candidate half-weight.
3. **Variational/PDE:** required `j5^! E_T`, not a nonzero density; required
   direct-versus-pulled variation, Ward/Green terms, and retention of the
   grade-seven/hook and 16-dimensional complements.

The preregistered kill conditions included: failure of the full `9I`
contraction, failure of moving-projector descent, zero source-root kappa
coefficient, failure of K/C/right-`H`, selection of the half-weight only by
target fit, deletion of companions by an unowned `P_X` or ordinary tau-BRST
image, or silent use of P1/P2/P3.

## 2. The full-14 source carrier

Let `C` be the `4+10` chimeric cotangent carrier and let a moving Clifford
quantization turn a grade-six coefficient of a bulk tensorial one-form into

```text
Q6(t) in C* tensor Lambda6(C*).
```

The canonical contraction and injection obey

```text
delta: C* tensor Lambda6(C*) -> Lambda5(C*),
delta j5 = 9 I,
Pi5=(1/9)j5 delta on im(j5).
```

For an internal five-form, all four horizontal covector legs occur because
none belongs to the five-form; five of the ten vertical legs occur. The split
is therefore `4+5`, not a choice of coefficient.

At the observer stabilizer, the internal carrier still has the exact
decomposition

```text
V10* tensor Lambda6(V10*)
  = Lambda5(V10*) + Lambda7(V10*) + Hook1728,
2100 = 252 + 120 + 1728.
```

Nothing in Wave E deletes the latter two summands. The pure `j5` restriction
is a tested native direction, not yet a proved source direction and not a
proof that every source solution lies in it.

## 3. Full-20 construction fork

### 3.1 Unweighted reconstruction comparator

The unweighted full map `B_1=B_V+B_H` is the direct full-14 reconstruction
output for the rectangular comparator. The sources own the ambient one-form
type, but they do not specify this particular rectangular `S -> VS` map.
Its gamma trace is `9c(phi)`. Its gamma-traceless part has, per source slot,

```text
one low-R 16 + one X/144 component.
```

The four mandatory horizontal legs change only the imGamma amplitude:

```text
vertical: imGamma norm^2 = 400/7,
full-14:  imGamma norm^2 = 1296/7.
```

They do not remove or change the low-`R` and X amplitudes. This closes the
possibility that the missing horizontal part would automatically solve the
Wave-D companion problem.

### 3.2 Half-weighted rival

The exact support polynomial has the unique low-`R` zero

```text
lambda = 1/2.
```

For the native representative,

```text
T_phi=P_R(B_V + 1/2 B_H),
rank T_phi=128,
Gamma T_phi=0,
T_phi^dagger T_phi=(5/2)I.
```

Its only nonzero thin-slot incidences are

```text
L16+ -> X2T+,   R16- -> X1T-,
L16- -> X2T-,   R16+ -> X1T+.
```

The internal five-subset exchange graph connects all 252 blades, but graph
connectivity does not prove that the half-weight ratio is an invariant scalar
on the whole representation. The matrix realization is deterministic
floating-point and the displayed coefficients are exact for the one unit
simple blade tested. Extending the half-weight across all blades, much less
across the moving `Y14` geometry, remains open.

The four named X slots have total codomain dimension 1152 after their Lorentz
factors. “Four 144 slots” is support, not rank or surjectivity: the map has
rank 32 in each slot and rank 128 total.

### 3.3 Why the half-weight is not yet selected

The weight operator distinguishes horizontal and vertical one-form legs.
It is equivariant only when the `4+10` split moves with the soldering/Zorro
data. The deterministic three-frame constant-conjugation fixture passes,
including one explicit out-of-Spin grade-three mover and a live
frozen-projector failure. Its
transition matrices form the expected coboundary fixture. This is not actual
atlas descent: the repo still lacks the `Y14` vector-bundle transitions,
overlap cocycle for `Theta_Z`, and source field-space declaration.

More importantly, Weinstein's source formula, the transcripts, and N1 are
silent on the coefficient `1/2`. It must come from the full Euler/Ward system,
a native contraction identity not yet written, or remain an additional
coefficient. Target cleanliness cannot select it.

## 4. K, C, right-H, provenance, and P0

For `T:S->R`, Wave E constructs the K reverse using the diagonal direct-sum
pairing `K_S direct-sum (eta tensor K_S)`:

```text
T^x = K_S^{-1} T^dagger K_R.
```

Both `T` and `T^x` are right-`H` linear and satisfy the total rectangular
K-adjoint identity. Each invariant C branch also has the explicit reciprocal

```text
R_C = -C_S^{-1} T^T C_R^T,
```

which makes the repeated-Grassmann kernel alternating. The probe also checks
the reverse support and right-`H` identity explicitly. With the implemented
vector-pairing convention, the `C+` reciprocal equals the K reverse while the
`C-` reciprocal differs. This does not merge the K and C action terms: their
total ordered provenance/reality conditions remain different. A generic
off-diagonal `G2` pairing can add an `I`-sector reverse, so the full source
pairing is still open.

In the coarse direct raw `S <-> X` sector-incidence sandwich, only

```text
P0 = 1
```

among N1's declared `{1,P_S,P_I,P_R}` when the same projector sandwiches both
sides. This is a type-level incidence result, not the complete ordered
`G2/Y/P0` placement. The diagonal written `c_rho:S->S` baseline survives `1`
and `P_S`, but it contains no 144 leg. A provenance-mediated `P_R` channel
would be a different `R_low -> X` object and must be named separately.

Under the currently trivial extension of the quaternionic real structure to
the three provenance copies, right-`H` requires `Y=bar(Y)`. Real matrices pass;
a planted generic `M3(C)` matrix fails. Retaining general complex Yukawa
matrices requires a constructed provenance real structure or conjugate
doubling, not a silent exception.

## 5. Source-root Euler result

The 2021 draft supplies the bosonic action and its translation residual

```text
Upsilon_B = odot_omega F_Aomega + * kappa_1 T_omega.
```

Separately, on the isolated kappa-sector comparator and the active restriction
`T=j5(phi)`, the displayed algebra gives

```text
S_kappa = (kappa_1/2)<T,T>,
E_phi = j5^! E_T = 9 kappa_1 phi.
```

The executable constructs the signed exterior pairing on all 2,002 five-form
basis elements and proves `j5^! = delta`, `j5^!j5=9I`, and pairing
self-adjointness of `P5`. It then derives the isolated kappa response twice—
directly after substitution and by pulling the active Euler covector—and the
results agree exactly. This closes the native adjoint calculation. It does
not establish that the public source field restricts to this active direction:
the source-to-active real-form/Zorro port is still open, and the curvature/
Shiab sector has not been set to zero by an actual source solution.

One coupled exact polynomial affine-action comparator `T=varpi-Dh` verifies

```text
D_varpi T[alpha]=alpha,
D_h T[xi]=-Dxi,
D^!E_varpi+E_h=0,
```

and an exact polynomial Green identity with a live interval boundary term.
This is a source-shaped variational comparator, not the nonlinear GU source
Ward identity. The moving-projector chain rule is verified in a separate
structural comparator; the two have not yet been assembled into one moving
source action.

This is not the complete source-action verdict. The following remain
unassembled:

- the actual moving Shiab/curvature contribution;
- the complete fermionic adjoint-valued source residual;
- N1's `J_D/J_F` and same-current bridge cancellation fork;
- moving Hodge/density/Krein and section terms;
- the public-source-to-active-`(9,5)` bundle port;
- a common analytic domain and observation no-leakage theorem.

Accordingly the result is

```text
DISPLAYED SOURCE KAPPA TERM: SOURCE-OWNED;
ACTIVE J5 KAPPA RESTRICTION: NONZERO CONDITIONAL ON KAPPA1 AND THE SOURCE PORT;
JOINED SOURCE-TO-ACTIVE 252 EULER ARROW: OPEN;
COMPLETE TOTAL EULER COEFFICIENT: OPEN.
```

## 6. Primary-source collision

| source | disposition | consequence |
|---|---|---|
| 2021 draft Sec. 9.1, eqs. 9.1–9.7 | `SOURCE-CONFIRMS` `T_omega`, the bosonic action, and the translation residual | the kappa-sector Euler test uses a displayed source term rather than inventing a new action |
| 2021 draft eqs. 9.18–9.20 | `SOURCE-CONFIRMS` bosonic and fermionic terms meet in one total Euler residual | N1's extra current bridge may not be double-counted or ignored |
| Portal/Oxford `01:13:00–01:13:55`, `01:16:36` | `SOURCE-CONFIRMS-ROLE` that the chimeric-to-`TY` identification needs a varying connection and observation section | full descent owes a moving splitting of this kind; the exact `Theta_Z` implementation remains reconstructed/open |
| TOE `01:35:23–01:36:08` | `SOURCE-CONFIRMS` the Higgs-like object is to come from an ad-valued one-form | the bulk source carrier is the correct starting type |
| TOE `02:18:17–02:20:33`; UCSD `00:17:01–00:25:03` | `SOURCE-CONFIRMS` the gauge-rotated distinguished connection/distortion architecture | `T_omega` is the stronger Eric-lane owner than a naked connection-minus-ungauged-LC object |
| UCSD `00:42:42–00:43:47` | `SOURCE-CONFIRMS` the trace-reversed `(6,4)` fibre and minimal/Yukawa relationship | the DeWitt musical and total connection placement are load-bearing |
| inspected sources | `SOURCE-SILENT` on `Pi5`, every rectangular `B_lambda:S->VS` map, the half-weight, K/C reciprocals, and companion removal | those are reconstruction results/candidates, not Weinstein claims |
| TOE `02:52:38–02:54:14` | `SOURCE-CONFIRMS_OPEN` on the 16/144 masses | no mass or VEV is imported |

## 7. Evidence and controls

The direct Wave-E executable passes

```text
24 exact/analytic or deterministic-native checks
 5 source-receipt checks
16 Layer-0/type checks
 8 live planted rejections
53 total
```

The controls reject: vertical-before-full contraction, `1/5` as the full
projector normalization, an unsigned exterior adjoint, unweighted companion
erasure, rank/support conflation, arbitrary complex provenance under trivial
right-`H`, absent C reverse support, frozen projectors, omitted affine-action
terms, dropped Green boundary terms, and density/Hessian-to-mass promotion.

## 8. Gate movement and next gate

Wave E moves

```text
RESOLVER-WAVE-E-SOURCE-OWNED-MOVING-252-FULL20-PLACEMENT
OPEN -> PARTIAL_CONSTRUCTED.
```

It constructs the native moving full-20 algebra and signed exterior adjoint,
the unweighted companion statement, a one-simple-blade clean half-weighted
rival, and a separate source-owned displayed kappa term with a coupled affine
variational comparator. It does not construct the port joining those halves
or close total source selection.

The next named gate is
**`RESOLVER-WAVE-F-ACTUAL-SOURCE-SHIAB-FERMION-EULER-WEIGHT-SELECTION`**:

1. construct the public-source-to-active `(9,5)` real-form and
   `Theta_Z`/moving-soldering port;
2. use that port to establish or reject actual source admission of `j5(phi)`;
3. restrict the complete moving Shiab curvature term and total fermionic
   residual to the grade-six direction;
4. include the N1 `J_D/J_F`/bridge fork without double counting;
5. compute the full Hessian blocks into `Lambda7+Hook1728` and both 16
   companions;
6. determine whether the resulting Euler/Ward equations select `lambda=1/2`,
   retain the unweighted coupled branch, or kill the rectangular route;
7. only after that ask for a stationary nonzero VEV and observed mass.

## 9. Boundaries

- The three-frame constant-conjugation/coboundary check is not the actual
  global `Y14` atlas or vector-bundle overlap theorem.
- The exact one-simple-blade half-weight is source-silent; representation-wide
  extension and action selection are both open.
- The diagonal direct-sum K/C and coarse P0 checks do not construct the full
  `G2/Y/P0` source placement.
- The affine Ward/Green comparator and moving-projector comparator remain
  separate from the nonlinear moving source action.
- The complete total source/N1 Euler coefficient remains open.
- No BV quotient removes the live companions.
- No stationary vacuum, 4D normal mode, Yukawa coefficient, mass, seesaw,
  generation count, common closed domain, or observation no-leakage result is
  claimed.
- P1/P2/P3 remain unchanged and unused.
- No claim, canon, protected verdict, public posture, count, lane,
  Eric/Curt-separation, or third-lane status changes.
