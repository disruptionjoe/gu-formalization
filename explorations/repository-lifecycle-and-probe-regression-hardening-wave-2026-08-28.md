---
title: "Repository lifecycle and probe regression hardening wave"
status: exploration
doc_type: result
created: 2026-08-28
operational_state: complete
target_claim: NONE-NOT-A-KILL
---

# Repository lifecycle and probe regression hardening wave

This wave closes one fresh unprotected regression and localizes two independent
paper-lifecycle defects behind the repository's unattended protected-surface
boundary. It does not move scientific or publication truth.

The immediately preceding comparator-scope certificate had reopened the
probe-authorship L1 category with one defaultless `next()` selection. The live
lint was red at one violation across 1,015 probes even though the prior close
reported zero. The witness selection now carries an explicit `None` default,
asserts that the mutation fixture has a registered comparator, and contains the
missing-witness path before any dereference or set removal. The authorship
ratchet returns to zero L1--L4 findings across 1,015 probes; its baseline is not
raised.

The same census exposed two paper-navigation gates that disagree with the live
filesystem and canonical publication index. The candidate inventory omits the
`observer-value-selection-theorem/` build-residue directory and does not type
one missing staging note. The root lifecycle map still assumes `published/` is
empty and counts published source references as staged entries. Candidate
README repairs were constructed and verified locally, but the scheduled-run
guard correctly classified both README paths under protected `papers/` custody.
Those edits were removed byte-for-byte. The defects therefore remain explicit
reviewed wakes rather than being silently normalized or applied without Joe's
authority.

Scope: this result binds repository-native controlled failure and scheduled
protected-surface custody only. It does not publish, submit, withdraw, move or
reclassify a paper; edit a paper path; change a DOI or posted date; or alter a
scientific claim, release state or public posture.

```gu-typed-objects
result: comparator mutation witness fails closed while protected paper lifecycle defects remain exact reviewed wakes MAP-TYPE=evaluation
carrier: repository-native Python gate state, Git path identity and scheduled protected-surface classification LAYER=UNTYPED CHIRALITY=N/A
pairing: exact controlled-failure and protected-path membership ON=repository-native-filesystem
real_structure: N/A
grading: N/A
action_owner: N/A
target: close the unprotected L1 regression without crossing protected publication custody MAP-TYPE=evaluation
```

## Preflight bookend

The A1 source register remains `ADHERED 106 / PARTIAL 4 / UNTYPED 1`; ledger
v0.263 remains 91 terminal / 0 open; no prediction wake fired; and the named B2
action-root set remains empty. The complete serialized process-gate census was
271 green / 152 inherited reds / 423 total. The fresh delta comprised one L1
regression and two paper-navigation reds.

Deleting or relocating retained PDFs was rejected because it would be
destructive and cross publication custody. Applying documentation repairs was
also rejected once `protected_surface_diff_audit.py` proved that unattended
Progress may not touch either `papers/README.md` path. Only the unprotected
controlled-witness repair remains in the result.

## Exact effect

- `probe_authorship_lint.py`: one L1 finding / 1,015 probes to zero findings /
  1,015 probes.
- Comparator scope certificate: 11/11 checks, including a controlled empty
  registered-fixture branch, exact-content exemption custody and both scope
  horns.
- Protected-surface audit: 3/3 green, with no local `papers/` diff.
- Candidate inventory audit: remains red with three exact failures over the
  omitted directory row and missing-note typing.
- Root paper lifecycle audit: remains red with two exact failures over the
  obsolete empty-published assertion and staged-reference counting.
- No paper path, target payload or protected artifact is changed.

## Postflight bookend

1. **Strongest overclaim or hidden assumption.** Zero authorship findings do
   not independently verify any scientific result; they certify controlled
   probe construction only.
2. **Strongest contrary construction.** A missing registered comparator used
   to escape through `StopIteration`. The explicit default and containment
   checks now turn that absence into a controlled failed certificate.
3. **Weakest reproducibility or propagation seam.** The paper lifecycle defects
   cannot be repaired by unattended Progress. Their exact five failing
   assertions remain the reproducible wake for an explicitly reviewed
   protected-paper navigation pass.

No source status, action owner, ledger row, SG4 phase, prediction,
confirmation, scientific verdict, canon, protected paper, DOI, release state
or public posture changes.
