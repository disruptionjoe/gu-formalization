---
title: "Five escape hatches: plan and decision"
status: exploration
doc_type: plan
created: 2026-06-28
source: "5 parallel design agents (one per hatch, reading the actual machinery) + 1 decision pass"
posture: verdict-agnostic; favored verdict is the KILL; A2/A3/A4-A5 carry the only real chances to overturn it
---

# Generation-Sector No-Go: Five Escape Hatches, Plan and Decision

## 1. What we are deciding

Three sibling repos test one class-relative no-go: GU's Standard-Model generation sits in a native self-dual SU(2)+ triplet (192-dim, machine-checked) whose only so(p,q)-invariant pairing is a purely cross-chirality Krein form K = eta_V (x) beta_S, so every positive-norm subspace is 50/50 and net chirality is exactly 0; every native base-index route is even, and the odd chiral 3 needs an imported unit of flux. The favored verdict across the 18-lens panel is the **KILL**: the chiral 3 is not produced inside the class, it is imported. The five hatches below are the decidable loci that turn "import the prime 3" into one precise statement each. We are deciding which to run, in what order, and which single path to run first. Nothing here presupposes the kill survives; each hatch is graded on its own evidence, and two of the five carry a genuine, low-probability route to overturning the verdict.

## 2. Comparison

| Hatch | Tooling | Difficulty | Expected outcome | Decision value | Asym. upside? |
|---|---|---|---|---|---|
| **A0** Positivity = compactness audit | Documentation-only | Low | Scope-exit confirmed (~0.92): Krein indefiniteness is logically equivalent to negating DG-A3 (internal G compact), by Weyl | Corrective certainty: retract the "first inside-class evasion" overclaim across 3 canon docs, fold the Krein axis into the no-go-map's existing scope-exit verdict, add DG-A3 to the violated list | No (a certain correction, not a chance at a new escape) |
| **A1** Hypercharge-weighted eta | numpy now | Low | KILL: Tr(Y C) and Tr(Y^3 C) both 0 (~1e-15), anomaly-protected (so(10) has no cubic Casimir) | Closes the panel's "we abstracted to a bare count" caveat with the deeper reason (anomaly safety, not coincidence) | Thin (only the family-twisted Y + lambda*J3 sliver is off the protected axis) |
| **A2** KR-equivariant (Atiyah Real) index | numpy now | Low | Reduces-to-known (~85%): sigma is quaternionic (sigma^2 = -I) and grading-even, so the KR-index collapses to the even quaternionic dimension already in canon | Negative hardens the firewall against the full Real refinement; a positive Z2 would be the first GU-native odd unit | Yes |
| **A3** G_SM-only equivariant ghost parity | numpy now | Medium | KILL (~85%): on the triplet C10 = -C exactly and K is cross-chirality on the full 1664-dim bundle, so equivariance group is not the load-bearing hypothesis | Closes the last open native route; a positive (net C10 != 0 with valid 3-generation Born rule) is the first native chiral 3 | Yes |
| **A4/A5** Triality (order 3) + Adams e-invariant (order 24) | Specialist / literature | High | Sharpened negative: triality's 3 is an 8-rep orbit, not the 3-dim Lambda^2_+ triplet (gauge commutant only so(4)); 24/8 = 3 unless /8 is independently pinned | Negative names the two automorphism/homotopy avatars of the same import; a positive forces 3 via Out(Spin(8)) or Im J, the one route the whole panel is blind to | Yes (highest ceiling) |

Honest reconciliation of the "asymmetric upside" label: the genuine discovery hatches (a real chance at a new escape) are **A2, A3, A4/A5**. A0 is corrective certainty and cannot flip the bottom line; A1 is a sharpening whose only escape route is the thin family-twisted sliver. The two paths worth flagging as a special pair are the **bookends of the cost/ceiling spectrum**: A0 (cheapest, most certain, fixes a live integrity risk) and A4/A5 (highest ceiling, the only path that could flip the headline). That is the accurate read; A0's own plan body confirms its upside is not asymmetric.

## 3. The five plans, in priority order

### A0  --  Positivity = compactness honesty audit (run first, free)

**Method (essential).** Fix DG-A3 verbatim from `canon/no-go-class-relative-map.md` lines 231/241 ("G is connected, compact, and centralizes SL(2,C)"). State the Weyl unitarian trick: every finite-dim rep of a compact group admits a G-invariant positive-definite form (average over Haar). Therefore under DG-A3 the matter rep is unitarizable, a Hilbert form exists, and "drop the internal Hilbert-positivity premise" is logically equivalent to "make the internal block non-compact," i.e. negate DG-A3. Leg-3 ("preserves G compact") and Leg-7 ("drop positivity") are the same move read twice and cannot both hold. Source-localize per signature: in (9,5)/(7,7) the base is (4,0) Euclidean, so 100% of the (+96,-96) lives in the internal block (non-compact SO(5,5)); in (14,0) the (+96,-96) comes only from the central chirality operator beta_S = product of all gammas, a non-canonical indefinite invariant form, while the Weyl-guaranteed positive-definite form is available. Either way: not inside-class.

**First step.** Draft the one-paragraph Weyl lemma block and write the corrected-sentence diff against the six pinned loci: `six-axis-candidate-krein-positivity-dg.md` lines 35/73/103/117/149, `ghost-parity-krein-synthesis.md` lines 120/130/136, `swing-ghost-parity-no-chiral-selection.md` lines 30-32, each rewritten to "scope exit at DG-A3," and add DG-A3 to the violated-assumption list at `no-go-class-relative-map.md` line 285 (currently {A1, A2, A6, A7}). Optional ~10-line numpy belt-and-suspenders on `ghost_parity_krein.py`: set beta_S = I in the (14,0) build and verify the triplet form is (+192, 0).

**Positive vs negative.** There is effectively no negative: a true "inside-class" reading would require a compact-internal-G build in which the triplet is hyperbolic under *every* invariant form, which Weyl forbids. The outcome is corrective certainty. This strengthens the kill by removing an overclaim and resolving an internal contradiction between canon docs (the no-go-map already says scope exit; the later six-axis and synthesis docs say inside-class).

### A1  --  Hypercharge-weighted eta (run second, numpy now)

**Method (essential).** Reuse the (9,5) construction from `swing_ghost_parity_chiral_selection.py` (projector Pi, triplet Wt, Krein K, chirality C). Build the SM hypercharge Y in the Pati-Salam embedding Y = T3R + (B-L)/2 from the internal so(10) Cartan, validated against the SM-16 multiset {6Y} = {1:6, 2:3, -4:3, -3:2, 6:1, 0:1} in compact (14,0). Compute Tr_phys(Y^k C) for k = 1, 2, 3 over the K-positive subspace, cross-check Tr_phys(Y^k C) = (1/2)(Tr_{S+}(Y^k) - Tr_{S-}(Y^k)), and sample the 8 gauge-equivariant ghost parities. Then the genuine crack: repeat with the family-twisted weight Y_eff = Y + lambda*J3_family, where the cubic-Casimir protection no longer applies.

**First step.** Harden the working scratchpad scripts (`a1_hypercharge_check.py`, `a1_euclid_spectrum.py`) into a single committed test `tests/generation-sector/a1_hypercharge_weighted_index.py` asserting (1) the SM-16 hypercharge multiset, (2) Tr_phys(Y^k C) < 1e-9 for k = 1, 3 over all 8 ghost parities, (3) Tr_16(T^3) = 0 for random so(10) Cartan elements (the structural reason), (4) the family-twisted extension as the one place an odd value could appear. Then write `canon/a1-hypercharge-weighted-index-kill.md`.

**Positive vs negative.** Negative (forecast, already confirmed at ~1e-15): the Y-weighted index is also 0, and necessarily so because the SM 16 is anomaly-free (so(10) has no independent cubic Casimir). This converts the panel's own caveat into an anomaly-protected no-go: the kill is not a counting artifact. Positive (Tr(Y^3 C) odd): a hypercharge-weighted topological index nonzero where the bare index is zero, a decisive chiral-selection channel. The only live route to positive is the off-axis family-twisted weight.

### A2  --  KR-equivariant (Atiyah Real) index (run third, numpy now)

**Method (essential).** Extend `ghost_parity_krein.py`. Build the antilinear Real structure sigma = (conj on V) (x) (Us . conj on S), Us = product of spacelike-real gammas. Measure the three signs that fix the graded KO-degree: sigma^2 = +/-I (real vs quaternionic), sigma C = +/- C sigma (does the Real structure preserve or shift the grading), and the ABS Clifford degree = number of G-equivariant operators that anticommute with C. Read the KR-index from (p-q mod 8) + (sign sigma^2) + (Clifford degree); only a nonzero grading-odd Clifford degree landing at effective p-q = 1 or 2 mod 8 yields a genuine Z2 mod-2 index.

**First step.** Write `tests/generation-sector/kr_equivariant_index.py`: assert sigma^2 = -I, [sigma, Gt] = 0, sigma C sigma^-1 = +C, sigma K sigma^-1 = -K on the triplet (all confirmed in `scratchpad/kr_probe2.py`), then **exhaustively scan** the G-equivariant commutant (su(2)+, su(2)-, beta_S, C, single timelike-gamma dressings, products) for any operator that anticommutes with C while commuting with Spin(10). That single scan settles the hatch.

**Positive vs negative.** Negative (forecast ~85%): no grading-odd G-equivariant generator exists, the KR-index is a degree-0 quaternionic dimension = 48 - 48 = 0, identical to the swing's complex 0; the firewall is robust against the full Atiyah-Real refinement because the generation-mirror swap is quaternionic (the same J_quat that DG use to certify vectorlike). Positive (~15%): a nonzero Z2 is the first GU-native odd invariant, a single generation mod 2 produced internally by the reality structure, supplying the odd unit H2 and the swing say must be imported. The discriminator is one decidable linear-algebra query.

### A3  --  G_SM-only equivariant ghost parity (run fourth, numpy now)

**Method (essential).** Relax the swing's full-Spin(10)-equivariance to commuting only with the unbroken G_SM = SU(3)xSU(2)xU(1). Fix a complex structure J_int on the 10 internal coords, build U(5) = SU(5)xU(1) ⊂ Spin(10) as its so(10)-commutant, embed G_SM via 5 = 3 + 2, charge-validate U(1)_Y on the 16 against the Pati-Salam charges. Compute the G_SM-commutant on Wt (nullspace of the stacked ad-superoperator) and compare to the Spin(10)-commutant so(4) (dim 6). Enumerate horizontal su(2)/su(3) inside it. Then the decisive loop: scan G_SM-equivariant involutions P, for both the canonical gamma-adjoint K **and** the genuinely-new affine family of G_SM-invariant (Spin(10)-non-invariant) indefinite forms K', keeping those with P K' > 0, and compute net generation chirality C10 (= product of internal gammas, distinct from full C) of the +1 eigenspace plus a 3-generation Born rule.

**First step.** Extend `scratchpad/a3_diag2.py`: build J_int, SU(5)xU(1) ⊂ Spin(10), the G_SM generators, charge-validate U(1)_Y, then compute the G_SM-commutant dimension on the 192-dim triplet and test it for a closed 8-dim su(3). That single output (does a horizontal su(3) live in the G_SM-commutant, and how much larger than so(4) is it) gates whether the full scan is worth running.

**Positive vs negative.** Negative (forecast ~85%): diagnostics already show C10 = -C exactly on the triplet and K cross-chirality on the full 1664-dim bundle for both gamma-adjoint conventions, so equivariance was never the load-bearing hypothesis (the cross-chirality of K is). Read literally on the canonical K, A3 reduces to T1a and is already killed; a negative closes the last open native route, upgrading the kill to "every native route is vectorlike, the chiral 3 is necessarily imported." Positive (the surviving ~15% sliver, a non-gamma-adjoint G_SM-invariant K' plus a horizontal su(3) giving robust net C10 != 0 with valid 3-generation Born probabilities): the first native route to chiral 3, which revives the profound positive and flips the verdict.

### A4/A5  --  Triality + e-invariant, the predict-not-name pair (run last, specialist)

**Method (essential).** A4 (numpy scaffold, specialist decision): build so(8) in the JW basis, construct the explicit order-3 triality tau as a 28x28 adjoint matrix from the octonion Cayley table, verify tau^3 = I, bracket preservation, and outer-ness (permutes 8v/8s/8c). The decisive gate: the Spin(10) commutant is exactly so(4) (dim 6 << 28, probe-confirmed), so no Spin(8) commutes with the gauge group; test whether tau *normalizes* Spin(10) (permutes SM charges within a generation) and compute the triality-twisted chirality Tr(U C|_{Wt}) against the connected metric index 0. A5 (literature-grade, one numpy sub-step): identify the framed-bordism class of the charge-1 self-dual SU(2)+ twist on the GU end RP^3 = S^3/Z2, compute its Adams e_R-invariant (Im J in dim 3 is exactly Z/24, denominator forced by a homotopy theorem), cross-check via the APS eta/rho finite-sum on L(2;1), and show the /8 is independently pinned by KO real-Bott / rank_H(S) = 8, giving 24/8 = 3. Convergence test: are A4's order-3 orbit and A5's framed class the same Lambda^2_+ twist, making 24 = 3 x 8 structural rather than target-fitted.

**First step.** Extend the A4 probe with the explicit tau, assert tau^3 = I, bracket preservation, and outer-ness, then test the decisive gate (does tau or its 128-spinor lift U normalize the Spin(10) generators on indices 4..13). In parallel, draft the one-page A5 specialist ask: the framed class on RP^3, the e_R target (denominator 24 via Adams), and whether the /8 is the forced real-Bott period, to hand to a stable-homotopy specialist.

**Positive vs negative.** Negative (forecast: A4 ~20% gauge-respecting, A5 ~30% surviving /8, convergence ~10%): a sharpened negative naming triality's 3 (an 8-rep orbit length, not the 3-dim Lambda^2_+ triplet, blocked by the so(4) commutant) and the e-invariant denominator 24 (whose /8 is not independently pinned, the coincidence the canon already flags) as the two automorphism/homotopy avatars of the same external import. Positive (asymmetric payoff): if the triality-twisted index is nonzero and integer-3 where the connected index is 0, then 3 = |Out(Spin(8)) orbit|, a count DG's connected-group reality computation cannot see; if A5's denominator 24 is forced by Adams with a real-Bott /8; and if both point at the same SU(2)+ twist, the headline flips, GU forces 3 via the order of an outer automorphism / J-homomorphism, the one route the entire panel (kinematic Krein, even-index, ghost-parity) is structurally blind to.

## 4. Recommended sequencing

Run **decidable-now first**, in ascending order of cost, so that "import the prime 3" becomes one clean statement at each locus before any specialist time is spent:

1. **A0 (free, documentation-only).** Do this immediately and unconditionally. It costs no computation, it is logically certain (Weyl is a theorem), and it removes a propagating overclaim ("first inside-class evasion of Distler-Garibaldi") that currently contradicts the no-go-map across three canon docs. Clearing this integrity risk before building further is the single highest-certainty action available.
2. **A1 (numpy, low).** Already numerically confirmed in scratchpad. Promote to a committed test and canon doc. Closes the panel's explicit "bare count" caveat with the deeper anomaly-protection reason, and runs the family-twisted extension that holds A1's only escape sliver.
3. **A2 (numpy, low).** One exhaustive commutant scan decides it. Cheap, and it carries a real (~15%) route to the first GU-native odd Z2 invariant.
4. **A3 (numpy, medium).** Gate on the horizontal-su(3) commutant-dimension question first; only run the full (P, K') scan if the commutant is genuinely larger than so(4). Closes the last open native route either way.
5. **A4/A5 (specialist, high).** Run only after the numpy hatches report. Highest cost, lowest per-leg probability, but the highest ceiling.

**Which paths can actually move the verdict.** Honestly: only the three discovery hatches A2, A3, A4/A5 carry a chance to overturn the kill, with A4/A5 the highest ceiling and lowest probability and A3 closing the last native route either way. A0 and A1 cannot flip the bottom line; they convert it into sharper, anomaly-protected, internally-consistent statements. The two paths to flag as a special pair are the bookends: A0 (the cheapest certain win, an integrity fix) and A4/A5 (the only path that could flip the headline via a route the panel is blind to). A2 and A3 sit between them as cheap-to-medium numpy discovery checks with genuine slivers.

## 5. The single best first path

**Run A0 first.** It is the only zero-computation hatch, it is the most logically certain (the Weyl unitarian trick is a theorem, and in the (9,5) build the base is (4,0) Euclidean so 100% of the Krein indefiniteness is demonstrably internal non-compactness), and it resolves a live internal contradiction: `no-go-class-relative-map.md` already records "EVASION BY SCOPE EXIT," while the later six-axis and synthesis docs claim a "first inside-class metric-forgetting shadow." Those cannot both stand. Fixing this before anything is published protects the program from shipping a false "first inside-class evasion of Distler-Garibaldi," folds the Krein/positivity axis into the existing scope-exit verdict, and adds DG-A3 to the violated-assumption list. It is the cheapest, most certain, and most integrity-protecting move, and it clears the deck for the compute-bearing hatches.

**The one decision the human needs to make:** authorize editing the three canon docs (`six-axis-candidate-krein-positivity-dg.md`, `ghost-parity-krein-synthesis.md`, `swing-ghost-parity-no-chiral-selection.md`) to retract the "inside-class shadow / first inside-class richer datum" framing and rewrite the six pinned loci to "scope exit at DG-A3," and to add DG-A3 to the violated-assumption list at `no-go-class-relative-map.md` line 285. Approve that retraction and the rest of the sequence (A1 → A2 → A3 → A4/A5) follows as low-risk numpy work plus one specialist hand-off.