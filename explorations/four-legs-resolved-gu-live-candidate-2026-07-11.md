# The four legs resolved: 4 of 5 CLEARED, GU is a live coherent candidate; everything open converges on the source action

2026-07-11. Culminates the posture-corrected North Star (work the legs, not the count). Two adversarial
swings: a disproof-hunt (granting a working action + 3 generations, try to kill every non-count leg) and
a resolution swing (one sub-agent per remaining leg doing REAL computation to CLEAR to a YES or DISPROVE).
**Net: 4 of 5 physics legs are CLEARED to a genuine YES; no leg produced a whole-GU disproof; the 5th
(gravity) is one scalar coefficient from resolution -- and that scalar, the located-not-forced count, and
the vacuum/sub-block selection are all gated on the SAME unwritten object: the source action.**

## The scoreboard (4/5 cleared, 0 disproofs)

| Leg | Status | Load-bearing result (real computation) |
|---|---|---|
| **Dark energy** | **YES** | the "wrong DESI sign" was a code bug (`d ln rho/dz=3`); re-verified `w_0=-0.777, w_a=-0.248` over `z<=2` -- SAME sign as DESI. GU fits the data. |
| **Quantum mechanics** | **YES** | concrete Krein-unitary repair: physical `J=+1` sector positive-definite (min K-eig `+1.000`), a Krein-unitary generator exists (`S^dag K S = K`). Unitary QM recoverable. |
| **Standard Model** | **YES** | the Pati-Salam rank-one breaking vector's stabilizer is EXACTLY `su(3)+su(2)+u(1)` (dim 12, rank 4, one U(1), no extra photon; generic-vector control gives dim 3). Mirror is vectorlike `16(+)16bar`, all four SM anomaly coeffs vanish, gappable at a free modulus above collider bounds. A G_SM-selecting vacuum EXISTS. |
| **Forces** | **YES** | the max compact of `su(3,2)` via the Cartan involution `theta(X)=-X^dag` is EXACTLY `su(3)+su(2)+u(1)` (dim 12, off-block norm `0.00e+00`, zero extra U(1)). The 28-photon catastrophe needs adjoint-only rank-preserving breaking, which GU does NOT use -- it uses the Cartan-involution/maximal-compact selection (the Krein form `K`, machine-verified GU-native). No extra force structurally forced. |
| **Gravity** | **OPEN (progressed)** | exact computation ELIMINATED Branch 2A (`D_A*F_A=theta` bare source: geometric `theta~M/rho^2` forces `F~M/rho` with divergent YM action, mutually exclusive with a finite-action Coulomb field) -> the conservative branch set narrows from `{2A,3}` to `{3}` only. Branch 3 survives, blocked on ONE scalar. |

## What "cleared to YES" means (honest grade)

Each YES is an **existence / consistency** clear at the scoped bar: no disproof, and the required structure
provably EXISTS (a G_SM-selecting vacuum; a Cartan-involution breaking to exactly the SM forces with no
extra boson; a unitary repair; a DESI-consistent dark-energy history). What GU does NOT yet do is DERIVE
*which* vacuum / sub-block / modulus its dynamics selects -- and that selection is **the same
located-not-forced residual**, gated on the source action. So the honest statement is: the legs are
consistent and their required structures exist; the remaining freedom is source-action selection, not a
contradiction with observed physics.

## The convergence: everything open is the SAME object

The resolution swing surfaced that the three remaining open items are one object:
- **Gravity Branch 3** hangs on the sign/coefficient of the ambient-curvature (`R^Y.B`) Willmore-EL term at
  `O(M^2/r^4)` -- which requires the branch-fixed `S_GU` action with fixed coefficients (unwritten).
- **The generation count** is a rigid 2-bit residual = the source action's field-space declaration.
- **The SM/forces selection** (which vacuum, which theta-stable sub-block) is "identical in character to the
  located-not-forced residual" -- source-action-gated.

So after doing the leg work, the WHOLE program bottlenecks on one unwritten object -- the source action --
and no leg falsifies GU. This is the same single-object conclusion the explanatory-scope audit reached,
now with 4 legs positively CLEARED rather than merely not-disproven.

## The bottom line (the profound, honest outcome)

We took a bold, contested, heterodox claim, took it seriously, and drove every leg to a real verdict.
**Result: 4 of 5 legs work; none falsifies GU; the count is located-not-forced; and everything still open
converges on a single, precisely-characterized, unwritten object (the source action).** That is the "we did
everything we could to falsify it, and most of it held up" outcome -- a live, coherent geometric candidate
with one crisp construction problem remaining -- not a proof of GU, but decisively not a failure state, and
honest at every grade. (Whether the geometry that ultimately closes is exactly GU or a nearby corrected
geometry is downstream; the four cleared legs and the single bottleneck hold either way.)

## Next step (highest leverage)

The gravity scalar (`R^Y.B` Willmore-EL coefficient) and the count both require the same thing: a
branch-fixed source action written with fixed coefficients. So the North Star reduces, cleanly, to the
construction problem already isolated -- **write/derive the branch-3 source action**; it simultaneously
decides gravity (CLEAR vs DISPROOF) and pins the count. A free build p-hacks (located-not-forced); the
honest form is a forced/derived construction.

## Grade / provenance

Swings grounded in the repo canon + independent computation (SM `psb_stabilizer.py`, forces `forces_leg.py`,
gravity `el_projected_gr_shadow.py` -- run in sub-agent scratchpads, results returned; the SM and forces
group-theory computations should be re-landed as in-repo tests during hardening). No claim-ledger movement
asserted; the legs' clears are existence/consistency grade, selection remains source-action-gated. Feeds
WI-068 and the 2026-07-11 posture/priority pivot.
