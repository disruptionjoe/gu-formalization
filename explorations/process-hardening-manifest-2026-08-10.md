---
title: "Process-hardening manifest: three propagation gates (baseline-seeded, green), the session-agent card, NAMES.md, the correction registry, the two-phase canon-promotion rule, and the multi-writer/absorption protocols — with the guidance-tier re-triage that rescued six tossed items"
artifact_type: session_manifest
created: 2026-08-10
status: LANDED__GATES_GREEN_WITH_17_BASELINE_NEEDS_RECHECK__TWO_PHASE_PROMOTION_RATIFIED__GUIDANCE_TIER_ADOPTED
grade: "PROCESS INFRASTRUCTURE, no physics content. Design: two councils (process + science) plus a
  keep/toss/adjust round, synthesized under two ratified postures (Joe, direct chat 2026-08-10): the
  TWO-PHASE canon-promotion rule, and the GUIDANCE-TIER principle -- requirement / in-path guidance / omit
  is a three-way call, not a binary; hard-require only what a deterministic gate can enforce."
canon_verdict_change: none
---

# Process-hardening manifest, 2026-08-10

## Landed (per-item status)

| item | what | status |
|---|---|---|
| `process_gates/canon_docs_frontmatter_validity_audit.py` | canon/docs frontmatter must parse | **GREEN** (after the theta-flrw fix below) |
| `process_gates/retracted_claim_citation_audit.py` | no NEW live citation of retracted claims; retraction-context within +/-6 lines exempts; known-open reported | **GREEN**, 1 known-open reported |
| `process_gates/correction_propagation_audit.py` | corrections must reach citing files; walks `lab/process/correction-registry.yaml`; NEW unacknowledged citers fail; baseline reports NEEDS_RECHECK (report-only, ratified) | **GREEN**, **17 baseline gaps now machine-tracked** |
| `lab/process/correction-registry.yaml` | machine-readable correction ledger (V15-1, V15-2, CARRIER-20260810 enforced; 4 inventory entries) | seeded |
| `lab/process/session-agent-card.md` | one-screen quickstart: machinery index, base rates, fired hazards — **guidance-voiced throughout** | new |
| `lab/process/NAMES.md` | homonym disambiguation table, 8 seeded entries with types and per-file senses; grown on-collision, no ritual | new |
| `AGENTS.md` | TWO-PHASE canon promotion (ratified); multi-writer protocol; absorption protocol; guidance pointer | amended |
| `canon/theta-field-flrw-dark-energy-eos.md` | frontmatter YAML fix (line-12 indent) — **mechanical syntax only, zero content change**; 16 keys now parse | fixed |

## The gate's first catch, on its first run

The propagation gate found **12 unacknowledged citers beyond the sweep's known list** (core-theorems,
function-space x2, two-arena, the spec issue register — full list in the registry with dated comments).
The hostile sweep audited the chain's composition; the gate walks **all** edges. Both baselines are now in
the registry as NEEDS_RECHECK: this is the absorption backlog, machine-tracked instead of memory-tracked.

## The guidance-tier re-triage (Joe's principle, applied)

The overbuild prune had operated on a binary — hard requirement vs omit. Joe named the middle tier:
**in-path guidance** ("here's the next step you should take", placed where agents encounter it). Re-triaged
under three tiers, six previously tossed items returned as one-line guidance in the card:

- strength-vocabulary palette (PROVED/COMPUTED/CONDITIONAL/LOCATED/ASSERTED) — guidance, was tossed as a formal vocabulary
- silent-Hermitization hazard — guidance, was tossed as a lint
- machine-tokens-encode-licensed-strength — guidance, was tossed as a standalone rule
- on-touch certificate firing-control — guidance, was tossed as a retrofit requirement
- NAMES check before reusing an overloaded symbol — guidance, was tossed as a ritual
- base-rate calibration line — guidance, was tossed as a standalone file

Still omitted (correctly, even as guidance): the facts cache (staleness risk exceeds guidance value — facts
stay in owning files, horn-tagged), CANON.md table generation (deferred infrastructure), certificate
retrofit audit (work, not guidance).

## A discovery that changed the plan

`lab/process/CURRENT-RESEARCH-CONTEXT.md` is NOT an orphan: it is the **hourly's live campaign memory** (updated
2026-08-10, ~1,370 lines of ledger fences, newest-first). Its title ("one-read distillation") has aged out
of truth, but the file is owned and mid-campaign — so the plan's "rehabilitate the pack" became "leave the
pack to its owner; create the small session-facing card instead." The pack itself is a live exhibit of the
aging problem: append-heavy accumulation with no compaction. **Flagged for the hourly, not acted on:** at
some campaign boundary, the fence stack likely wants compaction into the ledger files it summarizes.

## For the hourly (absorption items)

1. The **17 NEEDS_RECHECK baseline gaps** in the registry are the banner-propagation backlog from the
   sweep + the gate's first run. Applying the owed edits from
   `explorations/canon-spine-hostile-sweep-receipt-2026-08-10.md` and acknowledging the correction ids
   clears them; the gate turns silent as they clear.
2. The **two-phase promotion rule** is now in `AGENTS.md` and binds all writers including this session's
   author. Existing staged owed-edits are effectively Phase-1 proposals.
3. The context-pack compaction flag above — your call, your surface, your timing.
