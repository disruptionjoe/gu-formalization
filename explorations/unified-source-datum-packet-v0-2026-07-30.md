---
title: "Unified source-action and external-datum packet v0: a finite bulk-plus-defect candidate is now written"
status: active_research
doc_type: construction_result
created: 2026-07-30
lane: "1"
work_item: SOURCE-OWNED-CHIMERIC-BV-CAMPAIGN-N1
run: lab/process/runs/GUH-20260730T125227Z-unified-source-datum-packet-v0/run-plan.md
probe: tests/channel-swings/unified_source_datum_packet_v0_probe.py
grade: "FORMULA-BUILT / EXECUTABLE PACKET CONTRACT. The action-domain weld, field/action/datum ledgers, full-20 mapping-torus orientation cocycle, relative KO twist family, BV ghost-number census, search budgets, and planted controls are explicit. No Euler equation, stationary solution, CME, domain, physical reduction, anomaly cancellation, Fredholm index, mass, or count is claimed."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Unified source-action and external-datum packet v0

## Result

Swing N1 returns:

```text
UNIFIED-FINITE-CANDIDATE-WRITTEN
BULK-PLUS-DEFECT-ACTION-DOMAIN-WELD
P1/P2-ORIENTATION-AND-P3-KO-TWIST-SEPARATELY-TYPED
NO-FREE-CELL-FIT
NO-SCIENTIFIC-LEG-YET-SOLVED
```

One finite-grammar source-action/datum family is now written on the same
object. “Finite” here means a finite term, branch, topology, and functional-
slot inventory; it does not pretend that a supplied background section is a
finite-dimensional scalar. Those section-valued slots are charged explicitly
in Section 7. It does not add an \(X^4\) density to a \(Y^{14}\) density as
if they lived on one manifold. It treats the system as a bulk-plus-defect
theory on the stratified pair

\[
\left(Y^{14},s(X^4)\right),
\qquad
Y=\operatorname{Met}_{3,1}(X),
\qquad
s:X\hookrightarrow Y,
\]

with the section-current \(s_*\) as the action-domain map.

The candidate absorbs rather than duplicates the strongest existing partial
builds:

- the full-20 Krein/polarized \(S\oplus\operatorname{im}\Gamma
  \oplus\ker\Gamma\) operator family;
- the first-order induced-Yang--Mills parent and its record-current source
  bridge;
- the \(g=1\) gamma-traceless cure and the
  `contract - (1/6) wedge` curvature vertex;
- the actual-\(\operatorname{Sym}^2T^*X\) vertical connection carrier;
- the induced \(\lvert II\rvert^2/\lvert II_0\rvert^2\) gravity family;
- the SW-shaped two-form moment-map interaction; and
- the finite P1/P2 DeWitt-loop transport result.

The external datum is no longer a placeholder:

\[
\mathfrak d_{\rm ext}^{(0)}
=
\left(
L_\sigma\longrightarrow S^1_{\rm DW},
\quad
\nu:\overline Y\longrightarrow S^4,
\quad
\widehat e_n=[H_n]-[\underline{\mathbb R}^4]
\in\widehat{KO}^0(S^4),
\quad n\in\{-1,0,+1\}
\right).
\]

\(L_\sigma\) has holonomy \(-1\) on the primitive DeWitt loop and \(+1\)
on the doubled loop. \(H_n\) is the real rank-four bundle clutched by
\(q\mapsto L_{q^n}\) on \(S^3\), with the trivial, Hopf, and anti-Hopf
strata \(n=0,+1,-1\). The finite topology set contains no target `3`.
The supplied collapse map \(\nu\) pulls this class into the actual
\(\overline Y\) operator domain and is constant at infinity, so the pullback
has a declared relative boundary trivialization. The orientation and \(KO\)
components are coupled to the same operator packet but are not identified.

This is the N1 object that N2a, N4a, and N3 can now attack. It is not a
stationary theory, a completed source action, or evidence for GU.

## Plain English

We have stopped asking a list of requirements to behave like an action.
There is now an actual candidate functional with fields, terms, integration
domains, gauge laws, ghosts, antifields, global twists, and a finite list of
choices.

The most important new move is the action-domain weld. The older four-
dimensional SW/gravity terms and the fourteen-dimensional operator/curvature
terms were genuinely different objects. The candidate now says:

> the fourteen-dimensional terms are bulk terms; the four-dimensional terms
> are defect terms supported on the observer section \(s(X)\); the total
> action is their sum as a measure/current functional.

That is a real construction, although it has a cost. Varying the section will
generate distributional defect currents and junction equations. N3 must
derive them. If those equations cannot be made gauge covariant or cannot
close on the retained fields, this particular weld dies; the result will be
a typed obstruction to this candidate rather than “the action is missing.”

## 1. Construction fork and Layer 0

### 1.1 Forks used

| load-bearing object | construction used | why |
| --- | --- | --- |
| ambient geometry | program-native \(Y^{14}=\operatorname{Met}_{3,1}(X)\), \(TY=TX\oplus\operatorname{Sym}^2T^*X\) | this is the GU gimmel object; the exterior `6+4` ten is a hostile comparator |
| spinor/gauge structure | program-native `Cl(9,5)` and `Sp(32,32;H)` with the native Krein form | the indefinite form is part of the construction, not a defect to project away |
| RS field | geometric \(\ker\Gamma\) carrier inside the full-20 \(S/I/R\) complex | the standard ghost-subtracted gravitino has a different index and gauge interpretation |
| cure | program-native constant \(g=1\) \(\ker\Gamma\) projector plus the gamma-traceless curvature-vertex line | this is the already-built causal branch; the Porrati--Rahman \(F\)-analytic vertex remains a comparator |
| gravity | induced \(\lvert II\rvert^2+\lvert II_0\rvert^2\) family | this is the surviving geometric gravity branch |
| BV and current calculus | standard BV cotangent pairing and standard embedded-current calculus | these constructions bind either physical interpretation and supply the necessary typing |
| P3 topology | real \(KO^0\) twist acting on the quaternionic family | a real twist preserves the right-\(\mathbb H\) action; a \(KSp\)-by-\(KSp\) tensor would change the reality type |

### 1.2 Layer-0 register

| shared phrase | object in this packet | object it is not |
| --- | --- | --- |
| source action | one bulk-plus-defect functional on \((Y,sX)\) | a claim that Weinstein supplied this functional |
| action-domain weld | the current map \(s_!\) and the measure \(\mu_Y+s_*\mu_X\) | a smooth \(Y\)-density obtained by pretending \(X=Y\) |
| vertical scalar | the coefficient restriction \(\operatorname{res}_s^V(A-A_0)\), followed by a declared trace/orbit projector | the differential-form pullback \(s^*(a_V)\), which is zero or an \(X\)-one-form |
| odd bilinear | either the \(K\)-sesquilinear form or the \(C_{\varepsilon_C}\)-complex bilinear | one interchangeable “Yukawa” object |
| SW Majorana block | a reduced two-form moment-map interaction | the same-Weyl \(\Lambda^0\) complex bilinear excluded by SHIAB-05 |
| mass | a nonzero allowed zero-order action coefficient, later a gauge-quotiented normalizable Hessian eigenvalue | channel support or a provenance-block matrix |
| count | the future Fredholm/family pushforward of the final operator/domain/twist | three provenance summands, \(n\), \(p_1\), a loop sign, or a symbol-kernel dimension |
| stationary | a zero of the complete bulk, defect, section, ghost-free Euler system including boundary terms | W177 or a retained local truncation |
| P1/P2 | the one flat real orientation line \(L_\sigma\) over \(S^1_{\rm DW}\) | the integer/topology datum |
| P3 topology interface | the relative pullback \(\nu^*\widehat e_n\in\widehat{KO}^0(\overline Y,\partial_\infty\overline Y)\) | the orientation bit, the future integer index, or an input integer equal to three |

The action-domain phrase moves from **HOMONYM** to
**SAME BULK-PLUS-DEFECT FUNCTIONAL** at definition grade. The physical
equivalence between the defect vertical interaction and the reduction of the
ambient connection term remains **UNCERTAIN** and is the explicit N2b/N3
intertwiner test.

## 2. Geometry, current map, and field maps

Let \(X\) be an oriented spin four-manifold and work on compact cores until
N8 constructs global domains. Let

\[
\pi:Y=\operatorname{Met}_{3,1}(X)\longrightarrow X
\]

carry the gimmel metric \(G\) of signature \((9,5)\). A section \(s\) is a
Lorentzian metric field on \(X\). Let \(P\to Y\) be the principal
`Sp(32,32;H)` bundle and \(A\) an ambient connection.

The finite P1/P2 transport datum lives over a configuration loop, not over
\(Y\) itself. Accordingly define the family space

\[
\mathcal Y_{\rm DW}=Y\times S^1_{\rm DW},
\qquad
p_Y:\mathcal Y_{\rm DW}\to Y,
\qquad
p_{\rm DW}:\mathcal Y_{\rm DW}\to S^1_{\rm DW}.
\]

Let \(\mathcal E_{20,\rm geom}\to\mathcal Y_{\rm DW}\) be the associated
mapping-torus bundle from the already-built non-scalar full-20 transport
after its common central mismatch is factored. Define

\[
\mathcal E_{20,\rm DW}
=\mathcal E_{20,\rm geom}\otimes p_{\rm DW}^*L_\sigma.
\]

Define \(\mathcal S_{\rm DW}\) analogously for the spinor ghost carrier.

Local \(Y\)-actions below are evaluated on a fixed loop fibre. Global
transport uses the whole mapping torus, including
\(p_{\rm DW}^*L_\sigma\). It is not replaced by the product
\(p_Y^*\mathcal E_{20}\) unless that geometric transport is separately
trivialized. This prevents both the non-scalar irrep motion and a local
system on configuration space from being silently treated as inert
spacetime data.

### 2.1 The action-domain map

For a defect density \(\ell_X\in\operatorname{Dens}(X)\), define the current
\(s_*\ell_X\) by

\[
\left\langle s_*\ell_X,f\right\rangle
=
\int_X f(s(x))\,\ell_X(x)
\]

for compactly supported test scalars \(f\) on \(Y\). Equivalently, use the
stratified measure

\[
\mu_{\rm pair}=\mu_Y+s_*\mu_X,
\]

and write

\[
\int_{(Y,sX)}\mathcal L\,d\mu_{\rm pair}
=
\int_Y\mathcal L_Y\,d\mu_Y
+
\int_X\mathcal L_X\,d\mu_X.
\]

This is the map denoted

\[
\mathcal L_{X\to Y}(S_X)=s_!S_X.
\]

It is a distributional density if forced into a single \(Y\)-density and a
perfectly ordinary functional on the bulk-plus-defect field space. No fibre
profile or hidden normalizing measure is introduced.

### 2.2 Field and equation-dual maps

The defect fields are constructed from ambient fields by

\[
A_X=s^*A,
\qquad
Z_X=P_0\,s^*Z,
\qquad
v_s=\operatorname{res}_s^V(A-A_0).
\]

The last map restricts the **coefficient** of the vertical part:

\[
\operatorname{res}_s^V:
\Omega^1(Y,\operatorname{ad}P)
\longrightarrow
\Gamma\!\left(
s^*V^*Y\otimes\operatorname{ad}P
\right).
\]

It is not differential-form pullback. For the canonical trace candidate,

\[
\tau_g(h)=\frac14\operatorname{tr}_g(h),
\qquad
v_{\rm tr}=\tau_g\otimes\Phi_{\rm tr},
\qquad
\Phi_{\rm tr}\in\Gamma(s^*\operatorname{ad}P).
\]

For
\(v_s=\sum_\alpha\nu^\alpha\otimes\Phi_\alpha\), the typed vertical
Clifford/gauge insertion is

\[
\mathfrak c_\rho(v_s)
=\sum_\alpha c_G(\nu^\alpha)\rho(\Phi_\alpha).
\]

This is the operator meant below; a Lie-algebra-valued covector is never fed
directly into Clifford multiplication without its representation action.

The defect equation dual returns to the ambient equation space through the
distributional adjoint:

\[
\mathcal R_s^!:
\Gamma(E_X^\vee\otimes\operatorname{Dens}_X)
\longrightarrow
\mathcal D'\!\left(
E_Y^\vee\otimes\operatorname{Dens}_Y
\right),
\qquad
\mathcal R_s^!=s_*\circ P_0^!.
\]

N3 must compute the section variation and the normal derivative of \(s_*\);
it may not drop them as total derivatives.

### 2.3 Finite reduction choices at N1

The algebraic projector set is frozen to

\[
\mathcal P_0=\{1,P_S,P_I,P_R\}.
\]

These are algebraic restrictions only. Normalizability, closed equation
hulls, physical gauge quotient, and actual mode projectors remain N8/N9
questions. At most two trial modes per observer slot are allowed in the
later search; that budget is fixed now.

## 3. Fields and transformations

| field | carrier | parity / ghost | varied? | gauge transformation |
| --- | --- | --- | :---: | --- |
| \(s\) | \(\Gamma(X,\operatorname{Met}_{3,1}(X))\) | even / 0 | yes | natural `Diff(X)` action |
| \(A\) | connections on \(P\to Y\) | even / 0 | yes | \(A\mapsto uAu^{-1}-du\,u^{-1}\) |
| \(A_0\) | background connection on \(P\to Y\) | even / 0 | supplied | transforms as a connection |
| \(U\) | \(\Omega^1(Y,\operatorname{ad}P)\) | even / 0 | yes | \(U\mapsto uUu^{-1}\) |
| \(P_{\rm IG}\) | \(\Omega^2(Y,\operatorname{ad}P)\) | even / 0 | yes | \(P_{\rm IG}\mapsto uP_{\rm IG}u^{-1}\) |
| \(\epsilon_{\rm IG}\) | soldering/IG bundle section | even / 0 | yes | the declared IG action |
| \(\theta=A-\Gamma(\epsilon_{\rm IG})-U\) | \(\Omega^1(Y,\operatorname{ad}P)\) | even / 0 | derived | \(\theta\mapsto u\theta u^{-1}\) |
| \(Z\) | \(\Gamma(\mathcal Y_{\rm DW},\mathcal E_{20,\rm DW})\); fixed-loop fibre locally | odd / 0 | yes | \(Z\mapsto\rho(u)Z\) |
| \(\widehat Z_n\) | auxiliary sections over \(\overline Y\times S^1_{\rm DW}\) of \(\mathcal E_{20,\rm DW}\otimes_{\mathbb R}\nu^*(H_n\oplus\underline{\mathbb R}^4)\), \(KO\)-graded | odd / 0 | comparator only | \(\rho(u)\otimes1\) |
| \(\Sigma\) | fixed 12-section span in \(\operatorname{Sym}^2(\Lambda^2_+)\otimes F_2\) | even / 0 | supplied branch | declared spurion representation |
| \(c_g\) | \(\Omega^0(Y,\operatorname{ad}P)\) | odd / +1 | yes | BRST gauge ghost |
| \(\gamma\) | \(\Gamma(\mathcal Y_{\rm DW},\mathcal S_{\rm DW})\) | even / +1 | yes | candidate fermionic-gauge ghost |
| \(\bar\gamma,b_\gamma\) | same spinor carrier | even / -1; odd / 0 | yes | nonminimal doublet |
| \(\xi\) | \(\Gamma(X,TX)\) | odd / +1 | yes | diffeomorphism ghost |

The background split is gauge safe because \(a=A-A_0\) transforms
tensorially. A raw vertical coefficient of \(A\) is never called an adjoint
scalar.

For \(\xi\in\Gamma(TX)\), let \(\widetilde\xi\) be its canonical natural lift
to the metric bundle \(Y\). Every ambient tensor/spinor in the table also
transforms by \(\mathcal L_{\widetilde\xi}\), while defect fields transform by
\(\mathcal L_\xi\). This lifted `Diff(X)` action is included explicitly in
the BV skeleton below; it is not represented by the \(s^+\) term alone.

The carrier declaration \(A\) versus \(B\) remains a charged branch:

- `B`: \(Z_R\) is constrained matter in \(\ker\Gamma\);
- `A`: the full carrier plus the \(\gamma\)-generated BV gauge complex.

The \(g=1\) cure closes on both and therefore does not choose between them.

## 4. The classical/global action

The antifield-free packet is

\[
\boxed{
S_{\rm packet,cl}^{(0)}
=S_Y^{(0)}
+s_!S_X^{(0)}
+T_9[L_\sigma].
}
\]

Here \(T_9\) is circle-valued modulo \(2\pi\). The total BV action is defined
once, in Section 5, as this classical/global packet plus the antifield/ghost
extension. No classical term is counted twice. Every displayed summand below
has its own integration domain.

### 4.1 Native full-20 operator

Let

\[
\mathcal E_{20}=S\oplus I\oplus R,
\qquad
I=\operatorname{im}\Gamma,
\qquad
R=\ker\Gamma,
\]

with

\[
\Gamma^\sharp_g(s)=\sum_a e^a\otimes c(e_a)s,
\quad
j=\frac1{14}\Gamma^\sharp_g,
\quad
P_I=j\Gamma,
\quad
P_R=1-P_I.
\]

Use

\[
D_S=c^a\nabla^S_{A,a},
\quad
D_V=1_V\otimes c^a\nabla^{V\otimes S}_{A,a},
\quad
T=P_R\nabla_A,
\quad
\delta_R=-\operatorname{tr}_G\nabla_A,
\quad
Q=P_RD_VP_R.
\]

The action carries the already-closed odd-polarization family

\[
D_{\mathbf h}=
\begin{pmatrix}
h_aD_S&
h_{SI}D_S\Gamma&
h_{SR}\delta_R\\
14\overline{h_{SI}}\,jD_S&
h_dP_ID_VP_I&
h_{IR}P_ID_VP_R\\
-\overline{h_{SR}}\,T&
\overline{h_{IR}}\,P_RD_VP_I&
Q+h_\mu P_R
\end{pmatrix}.
\]

Here \(h_a,h_d\in\mathbb R\),
\(h_{SI},h_{SR},h_{IR}\in\mathbb C\), and the mass coefficient obeys the
declared Grassmann/reality branch relation. Its nonzero magnitude is written

\[
\lvert h_\mu\rvert=m_R
=\sqrt{m_{2,\rm eff}}\mu_{\rm DW}>0,
\qquad
m_{2,\rm eff}\in[5/6,5/4].
\]

No physical mass phase or eigenvalue is inferred.

The invariant \(S/I\) multiplicity pairing is

\[
G_2=
\begin{pmatrix}
\alpha&\zeta\\
\bar\zeta&\beta/14
\end{pmatrix},
\qquad
\det G_2\ne0,
\]

with the separate native \(R\) pairing. The four real coordinates in \(G_2\)
remain charged. Write

\[
\mathbb K_{\mathbf G}=G_2\oplus K_R
\]

for the resulting full-\(20\) Krein pairing. Thus \(G_2\) is never applied
dimensionally to the \(R\) summand by itself.

The gamma-traceless curvature vertex is

\[
\mathcal V_{\rm GT}(F_A)
=P_R\!\left(
\operatorname{contract}(F_A)
-\frac16\operatorname{wedge}(F_A)
\right)P_R.
\]

The ratio is inherited and fixed; its overall coupling \(\lambda_F\) remains
charged.

### 4.2 Ambient bulk action

Let \(\kappa_{\mathfrak g}\) be the invariant adjoint pairing and use
\(\mathbb K_{\mathbf G}\) for the full-20 Krein pairing. N1 selects the
minimal \(V_{\rm src}=0\) stratum of the W229 parent rather than leaving an
unspecified potential in the action. Define

\[
\begin{aligned}
S_Y^{(0)}
={}&
\frac{\zeta_F}{2g_A^2}
\int_Y\kappa_{\mathfrak g}(F_A,*_GF_A)\\
&+\int_Y
\left[
\kappa_{\mathfrak g}(P_{\rm IG},*_GD_AU)
-\frac1{2Z_U}\kappa_{\mathfrak g}(P_{\rm IG},*_GP_{\rm IG})
\right]\\
&+\frac1{2\kappa}\int_Y
\kappa_{\mathfrak g}(\theta,*_G\theta)
-\int_Y\kappa_{\mathfrak g}(\theta,*_GJ(Z))\\
&+\frac12\operatorname{Re}\int_Y
\left[
Z,
\mathbb K_{\mathbf G}\left(
D_{\mathbf h}
+\lambda_F\mathcal V_{\rm GT}(F_A)
\right)Z
\right]\\
&+\mathbf 1_{\{V,0\}}(r)\,S_{\rm sel}^{r}.
\end{aligned}
\]

The source current is an action derivative, represented locally by

\[
J(Z)_a=\operatorname{Re}[Z,\mathbb K_{\mathbf G}c(e_a)Z].
\]

\(\zeta_F\in\{0,1\}\) carries the fundamental-versus-induced Yang--Mills
fork. \(P_{\rm IG}\) is a genuine Gaussian parent:

\[
\frac{\delta S}{\delta P_{\rm IG}}=0
\quad\Longrightarrow\quad
P_{\rm IG}=Z_UD_AU,
\]

so eliminating it would give the W229 gradient term. N1 does not eliminate
it; N3 varies the first-order action.

In the covariantly constant/\(Z_U\to0\) limit, the \(U\)-equation of the
displayed sign convention reduces to

\[
\theta=\kappa\,\eta^{-1}J,
\]

the W203 ultralocal bridge rather than its negative. N3 must retain the
full \(D_A^*P_{\rm IG}\) and connection-current terms.

The vertical Krein coupling is an explicit summand of the \(A\)-dependent
operator, not another bolted-on term. If

\[
A=A_0+a_H+a_V,
\]

then

\[
S_{K,Y}[a_V]
=
\frac12\operatorname{Re}\int_Y
[Z,\mathbb K_{\mathbf G}\mathfrak c_\rho(a_V)Z].
\]

This is the owned sesquilinear branch and is an expansion component of the
single full-20 quadratic term above, not a second action summand.

### 4.3 Three equal-budget selector rivals

Choose a fixed end cutoff \(\chi(r)\), zero on the compact core and one past
a fixed collar. Each rival has one active real selector coefficient
\(m_{\rm sel}\) and receives 64 solver starts. The physical and auxiliary
carriers are kept distinct:

| rival \(r\) | selector packet | intended discriminator |
| --- | --- | --- |
| vertical | \(S_{\rm sel}^{V}=\frac{m_{\rm sel}}2\operatorname{Re}\int_{Y_{\rm end}}\chi[Z,\mathbb K_{\mathbf G}\mathfrak c_\rho(v_{\rm tr})Z]\) | actual-\(\operatorname{Sym}^2\) source selection |
| boundary/index | \(\widehat D_n\mapsto\widehat D_n+m_{\rm sel}\chi\,\operatorname{diag}(\mathcal V_{\rm GT}(\nu^*F_{H_n}),0)\) on the auxiliary graded comparator | global Hopf/Bott curvature sensitivity |
| vectorlike control | \(S_{\rm sel}^{0}=\frac{m_{\rm sel}}2\operatorname{Re}\int_{Y_{\rm end}}\chi[Z,\mathbb K_{\mathbf G}Z]\) | class-silent decoupling/mass comparator |

For the boundary/index rival, \(S_{\rm sel}^{(B,n)}\) denotes the auxiliary
quadratic functional of that displayed comparator. It is excluded from the
physical Euler system and enters the N10 index test. A nontrivial
\(H_n\to\underline{\mathbb R}^4\) off-diagonal isomorphism is deliberately
not written: such a global isomorphism would trivialize the very \(KO\) class
being tested. The BPST-curvature insertion is global. These are competitors,
not three terms simultaneously fitted to three targets.

### 4.4 Defect action on \(X^4\)

The defect functional is

\[
S_X^{(0)}
=S_{\rm geom}
+S_{\rm SW}
+S_{\rm Yuk}^{(b)}
+S_\Sigma.
\]

The geometry term is

\[
S_{\rm geom}
=
\int_X
\left[
\alpha_{II}\langle II_s,II_s\rangle
+\beta_0\langle II_s^0,II_s^0\rangle
-2\Lambda_{\rm bare}
\right]d\mu_g.
\]

\(\alpha_{II}>0\) is a live stratum; \(\beta_0/\alpha_{II}\),
\(\Lambda_{\rm bare}\), \(\mu_{\rm DW}\), and the background amplitude remain
charged. The cosmological response is not read at N1.

With

\[
A_X=s^*A,\qquad \psi=P_0s^*Z,
\]

the SW-shaped interaction is

\[
S_{\rm SW}
=
\frac{\lambda_{\rm SW}}2
\int_X
\left\lvert
P_+F_{A_X}-\mu(\psi)
\right\rvert^2d\mu_g.
\]

\(\mu\) is the quadratic two-form moment map

\[
\mu:
\Gamma(S_X^+\otimes E_X)
\longrightarrow
\Omega^2_+(X,\operatorname{ad}P_X).
\]

It is not called a same-Weyl scalar Majorana mass.

The two distinct vertical defect bilinears are

\[
S_{{\rm Yuk},K}
=
\operatorname{Re}\int_X
\psi^\dagger K\,
\mathfrak c_\rho(v_s)\,Y_K\psi\,d\mu_g,
\]

and

\[
S_{{\rm Yuk},C}
=
\frac12\operatorname{Re}\int_X
\left[
\psi^TC_{\varepsilon_C}\mathfrak c_\rho(v_s)Y_C\psi
+\text{reality completion}
\right]d\mu_g.
\]

\(Y_K,Y_C\in M_3(\mathbb C)\) act on the three
**provenance summands**. They are not generation matrices. The full
\(3\times3\) matrices are charged because the older `Z/3` texture cannot be
imported after the multiplicity/index Layer-0 correction.

The charge-conjugation candidate is defined by the finite algebraic variety

\[
\mathcal C_{\varepsilon_C,\tau_C}
=
\left\{
C\in GL(S_{\mathbb C}):
C\gamma_aC^{-1}=\varepsilon_C\gamma_a^T,\quad
C^T=\tau_C C,\quad
\varepsilon_C,\tau_C\in\{\pm1\}
\right\}.
\]

N2a constructs or eliminates its branches. No \(K\)-branch result transfers
to it without an explicit map

\[
\mathcal R_{KC}:S_{{\rm Yuk},K}\longrightarrow S_{{\rm Yuk},C}.
\]

The optional spurion interface is

\[
S_\Sigma
=
\operatorname{Re}\int_X
\psi^TC_{\varepsilon_C}\mathfrak c_\rho(v_s)\Sigma\psi\,d\mu_g,
\]

with

\[
\Sigma\in
\operatorname{span}_{\mathbb R}
\left\{\Sigma_A^{(0)}\right\}_{A=1}^{12}
\subset
\Gamma\!\left(
\operatorname{Sym}^2(\Lambda^2_+)
\otimes F_2
\right).
\]

Here \(F_2\) is a supplied two-dimensional flavour test space and
\(\Sigma=\sum_{A=1}^{12}\sigma_A\Sigma_A^{(0)}\). No \(\mathbb Z/3\) action
on \(F_2\) is asserted: the older “forced \(\mathbb Z/3\) doublet” statement
depended on the retracted multiplicity-to-count identification. The twelve
real coefficients are charged. Conditional on its fixed basis, the optional
branch has no coefficient per spacetime point. The basis-section freedom
itself remains debited in Section 7. The branch is absent on the minimal
no-same-chirality branch.

## 5. BV packet

### 5.1 Minimal BV extension

Let \(\widetilde\xi\) be the natural lift introduced above and define the
candidate semidirect BRST skeleton

\[
\begin{array}{ll}
\mathsf s_0A=D_Ac_g+\mathcal L_{\widetilde\xi}A,
&
\mathsf s_0U=[c_g,U]+\mathcal L_{\widetilde\xi}U,
\\
\mathsf s_0P_{\rm IG}=[c_g,P_{\rm IG}]
 +\mathcal L_{\widetilde\xi}P_{\rm IG},
&
\mathsf s_0\epsilon_{\rm IG}
=\rho_{\rm IG}(c_g)\epsilon_{\rm IG}
 +\mathcal L_{\widetilde\xi}\epsilon_{\rm IG},
\\
\mathsf s_0Z=\rho(c_g)Z+\mathcal L_{\widetilde\xi}Z
 +R_{\mathbf r}\gamma,
&
\mathsf s_0c_g=\frac12[c_g,c_g]
 +\mathcal L_{\widetilde\xi}c_g,
\\
\mathsf s_0\gamma=\rho(c_g)\gamma
 +\mathcal L_{\widetilde\xi}\gamma,
&
\mathsf s_0\bar\gamma=b_\gamma+\rho(c_g)\bar\gamma
 +\mathcal L_{\widetilde\xi}\bar\gamma,
\\
\mathsf s_0b_\gamma=\rho(c_g)b_\gamma
 +\mathcal L_{\widetilde\xi}b_\gamma,
&
\mathsf s_0s=\mathcal L_\xi s,\qquad
\mathsf s_0\xi=\frac12[\xi,\xi].
\end{array}
\]

On the underlying real graded space, write the antifield/ghost extension

\[
\begin{aligned}
S_{\rm af/gh}^{(0)}
={}&
\sum_{\Phi\in\{A,U,P_{\rm IG},\epsilon_{\rm IG},Z,c_g,
\gamma,\bar\gamma,b_\gamma\}}
\int_Y\langle\Phi^+,\mathsf s_0\Phi\rangle\\
&+\int_X\langle s^+,\mathcal L_\xi s\rangle
+\frac12\int_X\langle\xi^+,[\xi,\xi]\rangle\\
&+\sum_{\lambda\in\mathscr B_{\rm open}}
\tau_\lambda\mathcal O_\lambda.
\end{aligned}
\]

The total action is therefore

\[
\boxed{
S_{\rm BV,total}^{(0)}
=S_{\rm packet,cl}^{(0)}+S_{\rm af/gh}^{(0)}.
}
\]

The candidate fermionic gauge generator is

\[
R_{\mathbf r}\gamma
=
\left(
r_SD_S\gamma,\;
r_IjD_S\gamma,\;
r_RT\gamma
\right).
\]

The coefficients are not selected by the later Noether defect.

The BV two-form is the canonical density-dual pairing

\[
\Omega_{\rm BV}
=
\int\delta\Phi^A\wedge\delta\Phi_A^+,
\]

not an extension of the physical Krein form to ghosts. Every displayed
minimal monomial has ghost number zero; the probe checks sixteen independent
monomial shapes. Nilpotence beyond this semidirect skeleton is not assumed:
its defect is exactly what the frozen open terms let N4 factor.

### 5.2 Frozen possible open-BV grammar

N4 may fix or eliminate open-algebra terms but may not invent a new grammar
after seeing the curvature obstruction.

The frozen grammar has:

```text
local jets:            J^2(A,G,s) and J^1(Z,U,P_IG)
derivative order:      <= 2
physical-field degree: <= 4
ghost degree:          <= 2
antifield number:      <= 2
composition length:    1, 2, or 3
naturality group:      Spin(4,C) x Spin(10,C), then native-real-form check
quotient:              integration by parts, Bianchi, Clifford,
                       Gamma/j/P_I/P_R identities, formal-adjoint identities
```

The ten insertion families are

\[
\left\{
1,\Omega^{LC},F_A,\Omega^{IG},v,II,
T\delta,\delta T,Q,j\Gamma
\right\}.
\]

For the 20 observer slots, allowing every ordered insertion word of length
one through three gives the syntactic ceiling before Hom-typing and identity
quotient

\[
\binom{21}{2}\times(10+10^2+10^3)=233{,}100.
\]

This is a finite **search grammar**, not 233,100 asserted independent
coefficients. Most words will be ill-typed or identified. The actual nonzero
equivariant Hom basis and its quotient rank are N4 outputs. A generic element
has the form

\[
\mathcal O_\lambda
=
\int_Y
\left\langle
Z_i^+,
\mathcal M_\lambda(\gamma,\gamma)\,Z_j^+
\right\rangle,
\]

with total ghost number zero.

For each tuple \((i,j,w)\), the word \(w\) is composed left-to-right and uses
the first declared invariant pairing; if its types do not compose, that tuple
is zero. No additional contraction multiplicity is hidden behind one word.
Thus 233,100 is the exact ceiling of this **chosen candidate grammar**, not a
claim to enumerate every conceivable local BV functional.

### 5.3 Loop interfaces frozen now

The N1 family carries, without claiming success:

- two regulator families: an even proper-time family and a paired
  Pauli--Villars family;
- the local counterterm grammar above;
- the orientation/ghost-parity local system \(L_\sigma\);
- an inclusive observable resolution parameter \(\Delta E\); and
- the exact \(\ker\Gamma\) projector and full-20 ghost packet.

No regulator is called admissible on the noncompact Krein problem before the
domain and propagator exist.

## 6. The external datum

### 6.1 P1/P2: one orientation line

Let

\[
B_{\rm DW}=S^1_{\rm DW}
\]

parameterize the primitive actual-\(\operatorname{Sym}^2\) DeWitt frame
loop already constructed. Define the flat real line

\[
L_\sigma\longrightarrow S^1_{\rm DW},
\qquad
w_1(L_\sigma)=1,
\qquad
\operatorname{Hol}_{L_\sigma}(S^1_{\rm DW})=-1.
\]

Twist the \(S^1_{\rm DW}\)-families
\(Z,\gamma,\bar\gamma,b_\gamma\) by \(p_{\rm DW}^*L_\sigma\). On every fixed
loop fibre the local action has its ordinary carrier, while continuation
around the family returns the nontrivial sign. Since
\(L_\sigma^{\otimes2}\cong\mathbb R\), every action bilinear is a scalar
family.
The topological action interface is

\[
\boxed{
\exp(iT_9(\ell))
=
\operatorname{Hol}_{L_\sigma}(\ell)
=
(-1)^{\langle w_1(L_\sigma),[\ell]\rangle}.
}
\]

The primitive loop returns \(-1\); its double returns \(+1\). This is the
formal P1 interface N1 required. A common closed domain carrying it is still
an N8 test.

### 6.2 P3: bounded real \(KO\) twist

Use

\[
B_{\rm top}=S^4=D_N^4\cup_{S^3}D_S^4.
\]

For \(n=-1,0,+1\), let \(H_n\to S^4\) be the real rank-four bundle with
clutching map

\[
g_n:S^3\longrightarrow SO(4),
\qquad
g_n(q)=L_{q^n}.
\]

\(H_0\) is trivial, \(H_{+1}\) is the quaternionic Hopf bundle, and
\(H_{-1}\) is its anti-Hopf orientation. Supply the BPST, trivial, or
anti-BPST connection \(\nabla^{H_n}\). It is fixed external data, not a
varied gauge field.

Define the differential refinement candidate

\[
\boxed{
\widehat e_n
=
[H_n,\nabla^{H_n}]
-[\underline{\mathbb R}^4,d]
\in\widehat{KO}^0(S^4),
\qquad n\in\{-1,0,+1\}.
}
\]

With \(u\) the generator satisfying \(\int_{S^4}u=1\),

\[
p_1(H_n)=-2n\,u
\]

in the declared convention. This characteristic number is not an index or a
count.

The P3 support packet also supplies a based collapse map

\[
\nu:
\left(\overline Y,\partial_\infty\overline Y\right)
\longrightarrow
\left(S^4,*\right).
\]

It has degree \(+1\) on the chosen framed four-dimensional normal cycle and
is constant on the rest of the collapsed complement and on
\(\partial_\infty\overline Y\). Thus

\[
\widehat e_{n,Y}
=\nu^*\widehat e_n
\in
\widehat{KO}^0\!\left(
\overline Y,\partial_\infty\overline Y
\right)
\]

has a chosen zero boundary trivialization. The map is fixed external support
data, not varied. Without \(\nu\), an \(S^4\) class and a \(Y\)-action would
again be different objects.

The actual operator comparator uses the equal-rank auxiliary graded pair

\[
\widehat Z_n
=
\Gamma\!\left(
\mathcal E_{20,\rm DW}
\otimes_{\mathbb R}\nu^*H_n
\right)
\oplus
\Gamma\!\left(
\mathcal E_{20,\rm DW}
\otimes_{\mathbb R}\underline{\mathbb R}^4
\right),
\]

and the graded operator

\[
\widehat D_n
=
\left(
D_{\rm packet}^{\,
\nabla^{\mathcal E}\otimes1+1\otimes\nu^*\nabla^{H_n}}
\right)
\ominus
\left(
D_{\rm packet}^{\,
\nabla^{\mathcal E}\otimes1+1\otimes d}
\right).
\]

Tensoring by a real bundle preserves the right-\(\mathbb H\) action. The
virtual difference isolates the reduced \(KO\) class without silently
adding physical Standard Model copies: \(\widehat Z_n\) is an index
comparator, not another physical matter multiplet.

### 6.3 Manifold/end packet

The analytic pair is
\((\overline Y,\partial_\infty\overline Y)\), where \(\overline Y\) is the
radial compactification of the chosen gimmel component. The first end model
contains a supplied framed normal tube

\[
[R,\infty)_r\times S^3_q\times E_6,
\]

with \(E_6\) a fixed complementary control. Compactifying the radial-\(S^3\)
normal slice and collapsing the complement defines the displayed \(\nu\).
The clutching equator carries \(q\mapsto L_{q^n}\). A two-ended
opposite-orientation copy, with the signed degree reversed, is the second
preregistered end model.

The support of \(d\nu\), hence of \(\nu^*F_{H_n}\), is placed in a fixed
clutching annulus where \(\chi=1\), and \(\nu\) becomes constant again before
the ideal boundary. Thus the boundary/index insertion is nonzero on its test
region but compactly supported relative to infinity.

The boundary restriction is explicitly

\[
\widehat e_{n,Y}|_{\partial_\infty\overline Y}
=0
\]

in the supplied trivialization. No spectral section or closed domain is
selected. N8/N10 must prove that one exists and that this relative class
actually enters its index.

### 6.4 Why the two data remain two

The complete external base is

\[
B_{\rm ext}=S^1_{\rm DW}\times S^4_{\rm top}.
\]

\(L_\sigma\) is a degree-one \(\mathbb Z/2\) orientation class.
\(\widehat e_n\) is a reduced degree-zero real \(KO\) class with integer
clutching coordinate \(n\). Their external product can twist one family, but
there is no map in this packet sending the loop sign to the future Fredholm
integer. Three multiplicity blocks remain a third, separately typed object.

This also respects the W201 type boundary. The supplied
\(\widehat e_{n,Y}\) is an input twist to a future
relative/equivariant/rank index; it is not itself the integer count. The
2-primary \(K\)-definite selector and the future odd-torsion-compatible
integer readout remain distinct invariants of the shared operator. N10 must
compute their relation rather than naming \(\widehat e_n\) “three.”

## 7. Finite candidate and search budgets

### 7.1 Operational budgets

| search class | frozen N1 budget |
| --- | ---: |
| background generators | 4 |
| actual-\(\operatorname{Sym}^2\) orbit representatives in N2a | 4 |
| higher-bracket maximum arity | 4 |
| end models | 2 |
| topology strata | 3: \(-1,0,+1\) |
| algebraic reduction projectors | 4 |
| trial modes per observer slot | 2 |
| regulator families | 2 |
| selector rivals | 3 |
| solver starts per rival | 64 |
| open-BV composition depth | 3 |
| fixed optional-spurion basis sections | 12 |

The four background generators are frozen by type:

\[
A=A_0
+t_1B_{\rm tr}
+t_2B_{\rm tf}
+t_3B_{\rm Codazzi}
+t_4B_{\rm IG},
\]

where the entries are respectively trace-vertical, traceless-vertical,
horizontal/Codazzi, and independent IG-curvature directions. Their actual
normalization and stationary coefficients are later outputs.

The four N2a orbit representatives are zero, trace, one spacelike-traceless,
and one null actual-\(\operatorname{Sym}^2\) tensor. They are a screen, not
an exhaustive Lorentzian orbit classification.

### 7.2 Parameter ledger

| category | charged coordinates |
| --- | --- |
| local action coefficients | \(\zeta_F,g_A^{-2},Z_U,\kappa,\alpha_{II},\beta_0,\Lambda_{\rm bare},\lambda_{\rm SW},\lambda_F,m_{\rm sel},\mu_{\rm DW},m_{2,\rm eff}\) |
| multiplicity pairing | \(\alpha,\beta,\operatorname{Re}\zeta,\operatorname{Im}\zeta\) in \(G_2\) |
| native operator | two real diagonal coefficients and three complex cross coefficients; \(h_\mu\) follows the declared reality branch and derived \(m_R\) |
| gauge generator | \(r_S,r_I,r_R\in\mathbb C\) |
| open BV | every \(\tau_\lambda\) in the predeclared Hom/identity quotient, with syntactic rank at most 233,100 and actual rank to be computed by N4 |
| provenance Yukawa/spurion | \(Y_K,Y_C\in M_3(\mathbb C)\), separately charged; twelve real \(\sigma_A\) |
| background/functions | \(t_1,\ldots,t_4,B_{\rm initial}\), plus the charged section-valued slots \(A_0,B_{\rm tr},B_{\rm tf},B_{\rm Codazzi},B_{\rm IG},\Phi_{\rm tr}\), and the twelve supplied \(\Sigma_A^{(0)}\) basis sections |
| topology | \(n\in\{-1,0,+1\}\) |
| domains | end model and future closed-extension choice |
| regulators/observable resolution | even proper-time, paired PV, and \(\Delta E\) |
| reductions | \(1,P_S,P_I,P_R\), then at most two modes per slot |
| discrete forks | bilinear, \(C_{\varepsilon_C,\tau_C}\) component, carrier, mass running, guardian/EFT, YM, selector rival |
| derived, not independently charged | \(m_R=\sqrt{m_{2,\rm eff}}\mu_{\rm DW}\), \(\ell^2=Z_U\kappa\) |

No constraint surplus is reported at N1. Gauge, field redefinition,
normalization, functional, topology, domain, and discrete-search quotients
have not yet been ranked. In particular, “four background generators” is a
finite functional-slot count, not a claim that the section space contributes
only four real degrees of freedom. N3 must select/solve those sections without
reading the held-out wall, and N5 must debit their functional freedom before
reporting surplus. The packet exists so N3/N5 can compute that rank.

## 8. The 27-row disposition

Every source-action row now has a place in the candidate. “Has a place” is
not “is satisfied.”

| row | N1 disposition |
| --- | --- |
| SA-Y1 | both physical bilinear interfaces written; channel identification held |
| SA-Y2 | \(K\) and \(C\) branches remain separate charged choices |
| SA-Y3 | vertical amplitude charged, not selected |
| SA-Y4 | full provenance matrices charged; old texture not imported |
| SA-Y5 | larger-flavour-symmetry branch remains optional and named |
| SA-Y6 | provenance-to-flavour map remains a declaration |
| SA-Y7a | a finite \(F_2\) test-carrier type is frozen for this packet; the older forced-\(\mathbb Z/3\) claim is not imported |
| SA-Y7b | twelve \(\Sigma\)-basis coefficients charged |
| SA-Y8 | same-chirality spurion optional; SW moment map not substituted |
| SA-G1 | soldering field and \(\theta=A-\Gamma(\epsilon_{\rm IG})-U\) written |
| SA-G2 | \(\mu_{\rm DW}\) free bounded coordinate |
| SA-G3 | background initial amplitude charged |
| SA-G4 | \(f_0\) is a later derived readout, not an input |
| SA-G5 | \(\beta_0/\alpha_{II}\) charged |
| SA-G6 | \(\alpha_{II}>0\) live stratum |
| SA-G7 | \(c_L\) held out |
| SA-G8 | \(m_{2,\rm eff}\) band charged |
| SA-G9 | matter/section variation is enabled; linearization not computed |
| SA-C1 | carrier A/B branch charged |
| SA-C2 | \(g=1\) projection and gamma-traceless curvature line written |
| SA-C3 | bounded relative-\(KO\) topology interface written without a count input |
| SA-C4 | curved subprincipal test remains downstream |
| SA-U1 | BV, counterterm, regulator, parity, and inclusive interfaces frozen |
| SA-U2 | fixed-scale/running-mass branch charged |
| SA-U3 | Krein positivity observable held out |
| SA-U4 | \(m_R\ne0\) stratum written |
| SA-U5 | guardian versus finite-cutoff EFT branch charged |

## 9. Held-out wall

Before N2a/N4a/N3 evaluates any consequence, N1 seals these five observable
classes:

1. the first nonzero nonlinear BV obstruction class;
2. one subleading gravity/cosmology response not used in the background
   basis;
3. one background/covector causal test not used in selector design;
4. the primitive twist pushforward integer; and
5. the normalizable physical Hessian/unbroken-group result.

The executable packet hash is

```text
1efdffd34e3ad5358fed16c08cda9ecf681df676e817560bf36b436d79658ffb
```

The hash API includes the action, fields, datum, budgets, rival families,
parameter categories, and held-out **names**, but not held-out values. The
probe mutates all five later readouts, including planting the desired count,
and confirms that the construction hash does not move.

## 10. Seven-axis register

| candidate | L0 | L1 substrate | L2 observer | L3 pairing | L4 causal order | L5 emergence | L6 loop | L7 positivity | first falsification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| unified packet v0 | action domains welded by \(s_*\); bilinears and count objects remain separately typed | smooth principal bundle on bulk-plus-defect pair | geometric section plus finite projector family | smooth adjoint pairing plus separate \(K\) and \(C\) fermion branches | ambient multi-time \((9,5)\), Lorentzian section shadow | specific finite candidate family | no observer feedback; BV gauge loop only | indefinite Krein/gimmel, physical probability rule open | N3 variation must factor as a covariant bulk equation plus \(s_*\)-supported defect/section equations |

### L1--L7 detail

| axis | specification | anchor and assumption signature |
| --- | --- | --- |
| L1 substrate | **Smooth bulk-plus-defect principal-bundle system.** \(P\to Y\), observer section \(s(X)\), and current action \(s_!\). | Standard smooth/current geometry. Preserves smooth-substrate assumptions; claims no substrate no-go escape. |
| L2 observer | **Geometric section plus finite algebraic projector.** \(s\) and \(P_0\in\{1,P_S,P_I,P_R\}\). | Extends the template menu. It must carry rather than erase the operator invariant. |
| L3 pairing | **Smooth gauge pairing.** Bosons use \(G\) and \(\kappa_{\mathfrak g}\); fermions use separately typed \(K\)-sesquilinear or \(C\)-complex-bilinear forms. | Preserves locality/gauge covariance. No result transfers between the two fermion objects without a map. |
| L4 causal order | **Ambient \((9,5)\) pseudo-Riemannian geometry with a \((3,1)\) section shadow.** | Outside single-time global hyperbolicity in the bulk; no global causal order is claimed. |
| L5 emergence | **Specific finite candidate family.** No RG fixed point or universality claim. | Preserves the specific-object premise. |
| L6 coordination | **No observer feedback loop.** BV is a gauge-resolution loop, not an adaptive substrate/observer selection mechanism. | Preserves the no-loop baseline. |
| L7 positivity | **Indefinite Krein/gimmel form.** \(L_\sigma\) supplies orientation transport; a physical probability/superselection rule remains unproved. | Drops positive-Hilbert definiteness for the program-native reason; does not alter the zero-index/count theorem. |

## 11. Executable controls and what they mean

The probe checks:

- the section-current equality on a finite bulk/defect fixture;
- literal pullback versus coefficient restriction;
- the W203 bridge sign, stationary \(\theta=\kappa J\), and Legendre-minus
  effective source term;
- every action term’s domain and field incidence;
- exactly 27 requirement dispositions and all five physics legs;
- rejection of a free observer-cell coefficient;
- rejection of an omitted index leg;
- separation of \(K\) and \(C\) bilinears;
- actual `Sym2` versus exterior-ten typing;
- primitive/doubled orientation holonomy;
- pullback of the orientation line from \(S^1_{\rm DW}\), rather than an
  untyped placement directly on \(Y\);
- separate datum names;
- Hopf/anti-Hopf/trivial topology and rejection of `n=3`;
- the relative support map \(\nu:\overline Y\to S^4\) and rejection of an
  auxiliary \(S^4\) class with no map into \(Y\);
- equal-rank graded \(KO\) representatives;
- use of global pulled-back curvature rather than a nonexistent global
  \(H_n\cong\underline{\mathbb R}^4\) isomorphism;
- equal one-real-coordinate/64-start rival budgets;
- finite 233,100-element syntactic open-BV ceiling;
- a finite twelve-section optional-spurion basis;
- ghost number zero for sixteen BV monomials;
- disjoint parameter-accounting columns; and
- the sealed held-out hash.

The first execution caught a genuinely permissive matcher: the
free-cell regex was over-escaped and admitted `cell_37`. That execution was
discarded, the matcher corrected, and the planted coefficient then failed as
required before the headline verdict was read.

The subsequent hostile type pass rejected a series of draft choices before
sealing this version: an unmapped auxiliary \(S^4\), placement of
\(L_\sigma\) directly on \(Y\), loss of the non-scalar mapping-torus
transport, an impossible global
\(H_n\cong\underline{\mathbb R}^4\) off-diagonal selector, physical use of a
virtual \(KO\) pair, classical/BV double counting, a wrong W203 bridge sign,
an unspecified \(V_{\rm src}\), direct Clifford multiplication of a
Lie-valued covector, collision of the two meanings of \(\epsilon\), hidden
section-valued background freedom, and the false \(10\times3\) open-word
count. The replacements are the typed constructions displayed above; none
is left as an editorial caveat.

## 12. What moved and what did not

### Moved

- The \(X^4/Y^{14}\) action-domain fork now has one explicit
  bulk-plus-defect construction.
- The current adjoint makes the future defect contribution to ambient Euler
  equations explicit.
- The full-20, induced-YM, gravity, SW, vertical-bilinear, cure, and BV pieces
  now inhabit one finite candidate packet.
- P1/P2 has an action-level holonomy cocycle.
- P3 has a concrete, bounded, target-blind relative differential-\(KO\)
  input family with theory, degree, base, support map, clutching, connection,
  characteristic generator, boundary trivialization, and operator coupling
  stated.
- Background, orbit, end, topology, reduction, regulator, higher-bracket,
  open-BV, and rival-search budgets are frozen.

### Did not move

- No Euler equation has been derived from the packet.
- The section-current weld has not passed its full variational/junction test.
- \(C_{\varepsilon_C}\) and its Grassmann symmetry are uncomputed.
- The trace orbit has not survived an actual orbit/Euler-hull screen.
- The physical-\(R\) curvature obstruction has not factored through source
  equations.
- The CME, stationary background, global domain, physical reduction,
  anomaly, positivity, Fredholm index, mass spectrum, and count remain open.
- No source-action row, claim, canon verdict, or public posture changes.

## 13. Exact handoff

Three routes may now start from this exact packet:

1. **N2a:** construct \(C_{\varepsilon_C,\tau_C}\), compute Grassmann symmetry, and
   screen the four actual-\(\operatorname{Sym}^2\) representatives for both
   bilinears.
2. **N4a:** decompose the physical-\(R\) Levi--Civita curvature map and the
   separate IG-curvature map using only the frozen source/open-BV grammar.
3. **N3:** vary the complete bulk-plus-defect action, including the
   distributional section equation, and emit the five-leg residual map.

N2b and N4b wait for N3. No downstream run may silently substitute a
different action, topology family, parameter budget, or held-out wall.

## RB4 Layer-0 and field-register correction

RB4 does not alter the frozen N1 functional. It exposes a missing
\(\epsilon_{\rm IG}\)-dependent field map that must be inserted before the
fixed-Cartan full-20 branch is physical.

The N1 “observer section” \(s:X\to Y\) selects a Lorentzian metric. It does
not select:

- a unit timelike observer \(u\);
- a vertical Cartan involution \(\Theta_\chi\);
- a compatible orthogonal complex structure \(J\); or
- a determinant-one complex volume.

The exact base-induced map

\[
u\mapsto
W_4(u)=\mathbb Rt\oplus(u^\flat\odot u^\perp)
\]

constructs one moving Cartan family, but \(u\mapsto J\) is obstructed by
the fixed-\(u\) \(SO(3)\) stabilizer. The compatible \(J\) is a separate
reduction/order-parameter field.

Therefore the minimal common flag for the Pati--Salam and complex
\(SU(3,2)\)-type chains is provisionally

\[
\mathfrak f=(J,\Theta_\chi;t,\Omega_{\mathbb C}),
\]

with

\[
J^2=-1,\quad
J^{\dagger_G}=-J,\quad
[J,\Theta_\chi]=0,\quad
\Theta_\chi t=-t.
\]

The complex-volume/unimodularity factor is required to reduce the compact
intersection from \(U(3)\times U(2)\) to
\(S(U(3)\times U(2))\); otherwise an extra \(U(1)\) remains.

No new external datum is added to the frozen ledger by this correction.
Instead it defines the pending ownership test:

```text
if the epsilon_IG orbit descends to the complete flag and quotients:
    compensator / no new physical datum
elif the source action derives a stable flag Euler/Hessian orbit:
    dynamical order parameter / no new external datum
else:
    a frozen flag is a new section-valued continuous external spurion
```

P1/P2 remains the one flat real orientation line \(L_\sigma\). P3 remains
the separate relative real-\(KO\) input. Neither is identified with the
continuous flag. Any future discrete flag component or orientation may be
compared with \(L_\sigma\) only by an explicit base map and equality of
\(w_1\)/holonomy.

See
[`rb4-observer-cartan-moving-family-2026-07-30.md`](rb4-observer-cartan-moving-family-2026-07-30.md).
