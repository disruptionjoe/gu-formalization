---
artifact_type: exploration
status: complete
doc_type: source_claim_adjudication
created: 2026-08-24
work_item: SC-SIG-51-FOUR-METRIC-EXPERIMENT-AUDIT
target_claim: SC-SIG-51
target_claim_verdict: PARTIAL
grade: EXACT_ENUMERATION__SOURCE_UNDERDEFINED_EXPERIMENT_SELECTOR
canon_verdict_change: none
canonical_effect: none
priority_change: none
scripts:
  - tests/channel-swings/sc_sig51_four_metric_experiment_audit_probe.py
---

# SC-SIG-51: four normalized forms, no imported experimental selector

## System and target opening bookend

The parent Run admitted this Lane-1 source-adherence packet on clean GU HEAD
`0a1076c9`. The prior July theorem, transcript custody, source row and
signature controls were reopened read-only before execution. This artifact is
the bounded owner effect; it does not acquire an independent claim or widen
the parent's non-movement ceiling.

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

## Claim and scope

`SC-SIG-51` says that, when the trace and traceless coefficients are restricted
to plus or minus one, precisely four metrics are available; the two obvious
ones are ruled out by experiment and the trace-reversed pair remains. This
audit separates the exact algebraic enumeration from the empirical selector.
It changes no ambient-signature fork, action owner, scientific ledger row or
physics claim.

The four-chamber fact is not new. Section 4.1 of
`explorations/source-wave-observer-view-adjudication-2026-07-20.md` already
proved that the invariant DeWitt family is continuous before normalization and
has four sign chambers after the source's explicit plus-or-minus-one freeze.
This artifact closes the narrower source-adherence question and supplies a
mutation-tested certificate; it does not relabel that banked result as a new
theorem.

## Exact normalized enumeration

At a Lorentzian metric `g` on a four-dimensional vector space,

\[
 \operatorname{Sym}^2(V^*)=\mathbb R g\oplus
 \operatorname{Sym}^2_0(V^*).
\]

The Frobenius form has inertia `(7,3)`: the trace line is positive and the
traceless block has inertia `(6,3)`. With normalized block coefficients
`epsilon_0, epsilon_TF` in `{+1,-1}`, define

\[
 B_{\epsilon_{TF},\epsilon_0}
 =\epsilon_{TF}B_F|_{\operatorname{Sym}^2_0}
 \oplus \epsilon_0 B_F|_{\mathbb R g}.
\]

There are exactly four ordered sign pairs and four nondegenerate forms:

| `epsilon_TF` | `epsilon_0` | inertia | role |
|---:|---:|---:|---|
| `+1` | `+1` | `(7,3)` | raw Frobenius |
| `+1` | `-1` | `(6,4)` | trace reversal |
| `-1` | `+1` | `(4,6)` | negative trace reversal |
| `-1` | `-1` | `(3,7)` | negative Frobenius |

Quotienting by unrestricted overall sign would leave two projective rays, but
the spoken claim does not take that quotient: it explicitly retains the two
coefficients as plus or minus one. The exact count is therefore four under the
source's normalization and two only under an additional equivalence relation.

In the continuous convention

\[
 G_\lambda=B_F-\lambda\,\operatorname{tr}_g\otimes\operatorname{tr}_g,
\]

the trace-line norm is `4-16 lambda`. Degeneracy occurs at `lambda=1/4`, and
every `lambda>1/4` lies in the same `(6,4)` chamber. The source-native trace
reversal `lambda=1/2` has trace norm `-4`; a later repository fixture used
`lambda=1`, with trace norm `-12`. Shared inertia preserves signature-only
statements but does not identify their norms, measures, connections,
curvatures or action coefficients.

## The empirical sentence has no owned discriminator

The primary transcript names no experiment, observable, dataset, threshold,
action, kinetic operator, causal domain or map from an inertia pair to a
measured quantity. The adjacent sentence identifies the trace-reversed pair as
the survivors, so the source attribution is clear; its evidential mechanism is
not.

Existing repository facts do not fill that gap:

- `SC-SIG-02` carries the draft's belief that one raw fibre option is
  incompatible with observed Standard Model forces and symmetries. That is a
  source statement, not an independent experiment-to-inertia map.
- The existing Pati--Salam check distinguishes maximal-compact dimensions
  `24` for `(7,3)` and `21` for `(6,4)`, the latter matching
  `Spin(6) x Spin(4)`. This is an authorial structural criterion, not an
  experiment, and it does not select `lambda=1/2` from the whole
  `lambda>1/4` chamber.
- A conventional DeWitt, linearized-GR, positivity or ghost comparator can be
  useful as a control, but no bridge currently proves that its failure
  criterion pulls back to the GU vertical metric and its eventual action.
- The gimmel-signature work may consume a chosen fibre form. It does not derive
  the choice and explicitly left the spoken “four metrics, two viable” selector
  unused.

Therefore the two clauses receive different grades:

1. `FOUR-NORMALIZED-SIGN-FORMS`: **ADHERED / exact**.
2. `TWO-RULED-OUT-BY-EXPERIMENT`: **SOURCE-UNDERDEFINED** at the selector
   layer; it remains an attributed source assertion, not a repository result.

The composite row moves from `UNTYPED` to `PARTIAL`. A future upgrade requires
either a source-owned experimental observable with an explicit map to the four
forms, or an admitted GU action/domain whose exact instability or causal test
selects the same pair without calibration after the fact.

## Hostile review

The strongest overclaim is “experiment proves the `(6,4)/(4,6)` pair.” This
artifact proves no such thing. The strongest contrary reading is that overall
sign should be modded out; that produces two projective rays but contradicts
the source's four-form counting convention and does not create an experiment.
The weakest seam is the source word “experiment”: no primary locator in the
current packet expands it into a reproducible test.

```gu-typed-objects
result: exact four-form normalized enumeration plus a source-underdefined experimental-selector ceiling
carrier: Sym^2(T*X)=R g plus Sym^2_0(T*X), real rank 10 LAYER=source-print CHIRALITY=N/A
pairing: B_F on trace/traceless blocks ON=Sym^2(T*X); normalized coefficients epsilon_0,epsilon_TF in {+1,-1}
real_structure: ordinary real symmetric tensors
grading: trace versus traceless block split; not fermion chirality
action_owner: repository-construction; experimental action or kinetic selector remains UNTYPED
target: SC-SIG-51 source-adherence row MAP-TYPE=not-a-map
```

## Closing bookend

The exact intended effect is achieved: the already-banked enumeration now has
a dedicated certificate, while the empirical selector is prevented from
borrowing conventional authority. No signature horn, scientific ledger row,
prediction, confirmation, paper, canon or public posture moves. The next wake
is an explicit source/experiment carrier or an admitted action-derived
selector.

System postflight returns this owner effect to the parent receipt. No child
Runtime record, external action or additional repository effect is created.
