---
title: "Assembly archaeology: parameters recovered from the 2026-06-27 conditional assembly"
status: active_research
doc_type: exploration
created: 2026-07-19
directed_by: "Joe direct chat, 2026-07-19: find the prior assemblies and check whether they already gave us the parameters"
sources:
  - docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md
  - canon/no-go-quaternionic-parity-generation-sector.md
  - "DERIVATION-PROGRESS.md entries W2-01, SHIAB-01..06, ANOMALY-03/04, DARK-ENERGY-03, RS-BRST, SOURCE-01..04, GHOST-01, BICOMPLEX-01"
  - "mid-July: canon/ghost-parity-krein-synthesis.md + tri-repo Krein-sign reduction (bar-b, one external Z/2)"
claim_status_change: none
---

# Assembly archaeology: the prior work DID give us parameters

Joe's recollection is confirmed. The 2026-06-24..27 campaign assembled the
full conditional picture and then disassembled it into "one missing object"
(the RS/IG source action) with a six-property spec sheet — and its Section D
recommended freezing this repo as audit and moving construction out. That
recommendation is the ancestor of the stop-on-adapter reflex the standing
axiom now bans. The channels inherit the following, immediately.

## Recovered parameters, mapped to channels

### CH-QM (three hard constraints recovered)

1. **The missing differential is Noether-forced, not free** (spec B.2): the
   source action's gauge invariance must FORCE `delta_2 . d_RS,-1 = 0`. So
   the adapter does not transmit `d_RS,-1` as data — it transmits the action
   whose Noether identity produces it. Card language changes from "supply
   the differential" to "supply the action-level identity."
2. **Non-equivariance is mandatory** (spec B.3, GHOST-01): every equivariant
   attempt PROVABLY cannot close; the construction requires a compensator
   ghost sigma_c outside the Spin(9,5) family. Adapter range constraint
   tonight's swing missed: an equivariant payload is dead on arrival.
3. **Cohomological, not decoupled** (spec B.4 + the causality finding): the
   physical sector must be realized cohomologically (full BV bicomplex, both
   legs). A clean decoupled "no-ghost" subspace reinstates Velo-Zwanziger
   acausality — the ghost is causality-required. Constrains the CH-QM toy:
   the wrong-orientation failure mode should present as cohomological
   non-closure, not as a removable subspace.

### CH-SM (the biggest correction to tonight's hypotheses)

4. **The natural generation count is computed and WRONG for the story**:
   `ch2(S_X)[K3] = -5376`, not 24; the lone "24" was a disguised chi-import
   and was rejected. The route is dead, with receipts.
5. **Quaternionic-parity no-go (closed-form)**: every GU-native Hermitian
   carrier commutes with J_quat, so by Kramers its index is EVEN. **GU's own
   building blocks cannot produce an odd count such as 3.** Therefore the
   adapter payload for generations must include either (a) an a-priori rank
   pin (half-index reading) or (b) a **non-quaternionic structure** (literal
   reading). Tonight's subgroup-chain datum is NOT sufficient for
   generations — H-SM's card gains this as a named requirement, and the
   sector-counting sub-hypothesis is now bounded by a computed parity
   obstruction.
6. **The named sharp question this creates (new, high value):** is the
   transmitted Z/2 orientation itself non-quaternionic — i.e., can ONE
   payload item break J_quat and fix the Krein sign at once? If yes, N stays
   <= 4 and the parity no-go is discharged by the same bit that carries the
   arrow (H-REC). If the orientation is J_quat-commuting, generations force
   a FIFTH payload item. This is now the single most consequential joint
   CH-QM/CH-SM probe.

### CH-GR / interface contract (global items with explicit lineage)

7. **The three global objects** (spec B.5) the source must supply:
   (i) a families pushforward/index over the non-convex fiber
   `GL(4,R)/O(3,1)`; (ii) the global boundary holonomy / spectral section of
   the non-compact Y14 end — the direct ancestor of DEP-NATIVE-SOURCE-DATUM,
   now at p2c; (iii) a BV-to-boundary-Dirac map. These belong on the
   interface contract verbatim; they are sharper than tonight's four-item
   sketch and sit UNDER it (the four items must come packaged in an object
   that also supplies these three).
8. **C2 scale law (quantitative)**: the final obstruction C2 is global
   (~94%), NOT an index, and scales exactly as `C2(2xi)/C2(xi) = 2`. Any
   compensator/source structure must reproduce this scale covariance —
   a numeric check every CH-GR candidate can be run against.

### CH-COSMO

9. **The dark-energy sign history**: both prior sign computations came from
   a bug (hard-coded 3 vs real 4.229); the data-facing sign is OPEN. The
   card must not inherit any old sign value; the sign plausibly ties to the
   same Z/2 (H-REC), which would make the DE-sign audit an indirect
   orientation measurement — flag for the Lane 2 DE audit.

### CH-REC

10. **The record-issuance kill, correctly scoped**: the old campaign tested
    "the record/boundary idea = a shiab selector" and killed it (lands on
    the index, not the selector). H-REC does NOT re-tread this: it
    identifies the arrow with the transmitted ORIENTATION, not with a
    selector. The kill is cited so no channel re-runs it.

### Killed-selector ledger (all channels)

11. Do not re-run: shiab=codifferential (false by Clifford parity);
    gamma-trace, seesaw self-adjointness, folded-complex closure, PO1
    forgetful=kernel, conditional-expectation, Kostant cubic Dirac (all
    fail); source-action="observer's slice as one global object with the
    index" (forced analogy; C2 is not an index).

## What this changes right now

- The interface contract gains an addendum: non-equivariant compensator
  required; non-quaternionic structure or rank pin required for
  generations; the three B.5 global objects; the C2 scale-law check.
- The scorecard's CH-SM row sharpens: Q3's "finite chain list" is
  necessary but NOT sufficient — the parity requirement is added.
- New ranked probe: the J_quat status of the transmitted orientation
  (joint CH-QM/CH-SM), decisive for whether N stays <= 4.
- The 2026-06-27 conditional stands as the proof that the channels' work
  was viable all along: the assembly never failed — it was shelved by a
  posture decision the standing axiom has now reversed.

## Boundary

Everything above is recovered audit-grade material with receipts in
DERIVATION-PROGRESS and tests/; no claim status, canon verdict, or public
posture moves. The parity no-go and ch2 = -5376 remain exactly as graded in
their canon homes.
