---
artifact_type: exploration
status: exploration (W213; TEAM EFFECTIVE-POTENTIAL / VARIATIONAL; five personas inline, one worker, no sub-agents; deterministic test 24/24 exit 0, six positive controls first)
created: 2026-07-14
wave: W213
label: W213
posture: coherence-first (Joe 2026-07-14); big-swing BUILD (not a proposal); exploration grade; conditional register; truth-seeking (a runaway / out-of-validity / non-existent vacuum is a REAL result); RUTHLESS self-verification; tri-repo gating STRICT
route: EFFECTIVE-POTENTIAL / VARIATIONAL (build V_eff for the record-count mode from the W203 forced action + the W126 DBI gradient; find v*; compute the spectrum; check boundedness)
title: "W213 -- BUILD the record-condensed true vacuum by the EFFECTIVE-POTENTIAL / VARIATIONAL method (1 of 5 convergence teams). VERDICT: RUNAWAY-NO-VACUUM (in-validity) / EXISTS-BUT-OUT-OF-VALIDITY (true vacuum). The BUILT effective potential for the record-count / scale mode is EXACTLY quadratic and INVERTED -- V_eff(u) = -64 u^2 - 8 u + 2 (machine-checked) -- because the W203 forced action's connection distortion theta is GAUSSIAN and Legendre-eliminated (V(theta) ABSENT), so the whole scale-mode potential comes from the induced |II|^2 geometry (W126) and inherits its exact-quadratic no-minimum structure. dV_eff/du = 0 has EXACTLY ONE stationary point u* = -1/16, and it is a MAXIMUM (V'' = -128 < 0, the tachyonic top); V_eff is unbounded below both ways. The FULL static energy functional E(u,v) = V(u) + K(v) with the DBI kinetic K (the W126/W159 rational, all-positive, diverging at the v^2 = 1/16 speed-limit wall) has that same point as its ONLY critical point, with INDEFINITE Hessian diag(-128, +32): a SADDLE, not a minimum. So Q1: NO true-vacuum minimum exists inside validity; a true vacuum EXISTS (W163's Im V_eff = m_0^4/(64 pi) = 1/(1024 pi) != 0 certifies it) but only OUT-OF-VALIDITY -- the drive scale |m_0^2| = 1/4 = 4 x (1/16) sits a factor 4 BEYOND the DBI wall. Q2 (nature): the vev v* is NOT realized in validity (no minimum); the spectrum at the accessible point is REAL (native poles m_0^2 = m_2^2 = -1/4, block-diagonal, no complex/mixed poles) but TACHYONIC with V_eff unbounded below -> PATHOLOGICAL as a vacuum, and FIELD-INDEPENDENT (W164: exact quadraticity => f'' constant => dm^2/dR = 0) so not liftable by rolling. Q3 (W166 reading): HOLDS as a description of the ROLL -- m^2<0 in the record-count mode is N ~ e^{4p} growing = the arrow of time (roots +/- 1/2), DBI-capped to a steady de-Sitter rate (4 v_max = 1, so the record-count 'Hubble rate' equals the speed limit) -- but there is NO static true vacuum to evaluate it AT: the true 'vacuum' is the rolling de-Sitter attractor itself. Q4: OUT-OF-VALIDITY; the extension needed is the non-perturbative record-condensed completion (W154 source action / promotion-gate T4), UNBUILT and not shown stable. CONVERGES with the effective-potential predecessor W163 and with W159 (DBI out-of-validity), W166 (engine/roll), W164 (field-independent). Bar (b) UNCHANGED; count {1,3}; H59 OPEN; no canon movement."
grade: "exploration / conditional register throughout. STRONG on the built-potential facts (the exact-quadratic inverted V_eff(u) = -64u^2 - 8u + 2, the unique-critical-point + maximum classification, the full E(u,v) saddle with Hessian diag(-128,+32), the DBI wall at v^2=1/16, the 4x out-of-validity margin, the block-diagonal real spectrum m_0^2=m_2^2=-1/4, the field-independence -- all exact sympy, machine-checked, six positive controls first). STRUCTURAL on the identification that the W203 forced action adds no scale-mode self-potential (theta Gaussian / Legendre-eliminated, V(theta) absent) and on the OUT-OF-VALIDITY existence reading (the true vacuum is certified by W163's ported Coleman-Weinberg Im part, not exhibited here). COMPUTED (deterministic, tests/W213_true_vacuum_effective_potential.py, 24/24 exit 0): PC1 W126 slice (2,1/3,8/9,-4); PC2 c_R=-4/9; PC3 native poles m_0^2=m_2^2=-1/4; PC4 the DBI rational denominator (16v^2-1)^3; PC5 the all-positive gradient coefficients (2,16,320,5888) incl. the 320 v^4 quartic; PC6 W163 Im V_eff=1/(1024 pi); EP1a-d the built inverted-quadratic V_eff, unique max, unbounded below; VA1a-c the DBI kinetic minimum at v=0, the E(u,v) saddle, the 4x validity margin; SP1a-c the real-but-tachyonic field-independent spectrum; RC1a-d the record-count monotone growth, the growing homogeneous mode, the de-Sitter rate, and the Q3 answer; SK1-4 the honest verdict, the u*=-1/16 vs v^2=1/16 coincidence flagged as NON-load-bearing, the W203-adds-no-minimum point, and the no-forbidden-target check. LOAD-BEARING imports (CITED, not re-derived): W203 (theta Gaussian, Legendre-eliminated, V(theta) absent; the forced action fixes the source coupling only); W126 (the |II|^2 slice decomposition, the exact-quadratic potential sector, the DBI gradient rational); W130 (c_R=-4/9, c_W=2, native poles m_0^2=m_2^2=-1/4, block-diagonal spins); W159 (the DBI rational, v^2=1/16 degeneration, all-positive = speed limit, |m_0^2|=4x margin); W163 (Coleman-Weinberg Im V_eff != 0 = the true-vacuum existence certificate; out-of-validity; unbuilt/rolling); W164 (field-independent mass; chameleon channel absent); W166 (record-count mode identity, N=4-volume monotone, DBI-capped de-Sitter-rate roll); W145 (N = spacetime 4-volume). HONEST RESIDUALS: (1) the true-vacuum EXISTENCE rests on W163's ported real-scalar Coleman-Weinberg Im part, not on a native GU one-loop measure computation -- the robust content is the SIGN, set by m_0^2 < 0; (2) the variational energy functional E = V + K is the honest static-configuration reading of the induced |II|^2 potential + gradient sectors, not a full covariant stress-tensor minimization on Y14; (3) the record-condensed completion is UNBUILT (W154 T4) and the reading does NOT close the E2 AF/AS branch fork; (4) magnitudes stay normalization-gated exactly as H25/W126 graded them. NO canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count stays {1,3}; H59 OPEN; bar (b) UNCHANGED; E2 carried. NO forbidden target {3,8,24,chi(K3),Ahat} assumed/inserted/hardcoded/divided-by. Zero em dashes."
construction: "program-native where the objects are GU's (the induced |II|^2 action and its conformal / scale = record-count mode; the covariant c_R = -4/9; the native poles m_0^2 = m_2^2 = -1/4; the W203 forced source action with its Legendre-eliminated Gaussian theta; the DBI gradient rational; N = 4-volume). Standard-field where the machinery binds any construction (the effective-potential / variational vacuum search; the Coleman-Weinberg false-vacuum Im-part reading, PORTED via W163; the DBI / k-essence speed-limit-vs-restoring-force distinction; the f(R) constant-f'' field-independent mass; the de Sitter equilibrium relabel). Every analogy PORTED and labelled; none asserted of GU. Forks named per GEOMETER-VS-PHYSICS-OBJECTS.md."
depends_on:
  - explorations/W203-branch3-source-action-fixed-coefficients-2026-07-14.md
  - explorations/W126-beyond4th-vacuum-lift-2026-07-13.md
  - explorations/W159-tachyon-escapes-2026-07-14.md
  - explorations/W163-lens-nonperturbative-vacuum-2026-07-14.md
  - explorations/W164-lens-left-out-ingredient-2026-07-14.md
  - explorations/W166-lens-tachyon-is-the-engine-2026-07-14.md
  - explorations/W185-record-energy-economy-toy-model-2026-07-14.md
  - GEOMETER-VS-PHYSICS-OBJECTS.md
scripts:
  - tests/W213_true_vacuum_effective_potential.py
external_refs:
  - "Coleman & Weinberg, PRD 7 (1973) 1888 -- the one-loop effective potential"
  - "Coleman, PRD 15 (1977) 2929; Callan & Coleman, PRD 16 (1977) 1762 -- false-vacuum decay, Im V_eff as a decay width"
  - "Silverstein & Tong, PRD 70 (2004) 103505 -- DBI / speed-limit kinetic structure (velocity cap, not excursion cap)"
  - "De Felice & Tsujikawa, Living Rev. Rel. 13 (2010) 3 -- f(R) stability f'>0, f''>0; the density-dependent (f'' varying) mass"
  - "Starobinsky, PLB 91 (1980) 99 -- R^2 scalaron (healthy for +R^2)"
---

# W213 -- build the record-condensed true vacuum: effective-potential / variational

**Role.** One of five independent teams building the record-condensed true vacuum of GU by a
DIFFERENT method, to test convergence. Method here: **effective-potential / variational**. The
task is a BIG SWING (actually build it), not a proposal. The open question is genuinely unknown:
the native scalaron / record-count mode is tachyonic (`m_0^2 = -1/4 < 0`, MSS slice
`a2 = -1/9`), a FALSE-vacuum instability; W163 found `Im V_eff != 0` so a record-condensed TRUE
vacuum EXISTS but is out-of-validity and unbuilt. Build `V_eff(phi)` for the record-count mode,
find the true minimum `v*`, compute the spectrum, check boundedness, and answer four questions.

Five personas ran inline in one worker, sequentially (no sub-agents): (1) effective-potential
specialist, (2) variational analyst, (3) spectrum / stability checker, (4) record-count-mode
specialist, (5) ruthless skeptic. Deterministic test
`tests/W213_true_vacuum_effective_potential.py`, **24/24, exit 0, six positive controls first**.

## 0. The object, and what "build V_eff" means here

The record-count mode is the conformal / scale amplitude `p` (W166 / W153): `N = 4-volume ~
e^{4p}`. Its effective potential has two inputs the brief names:

- **The W203 forced-coefficient action.** W203 pinned the branch-3 source action's coefficients.
  Decisively for a vacuum search, its connection distortion `theta` is **Gaussian and
  Legendre-eliminated** (`theta* = kappa M^{-1} J`, kernel `M ~ eta` by Schur), so **`V(theta)`
  is ABSENT** (W203 coefficient ledger, "potential | absent | theta is Gaussian"). The forced
  action fixes the source COUPLING; it adds **no self-potential for the scale mode**. So the
  scale-mode `V_eff` comes ENTIRELY from the induced `|II|^2` geometry (W126). This is the pivotal
  structural input: building the forced action does not manufacture a minimum the naive `|II|^2`
  potential lacked (test `SK3`).
- **The W126 gradient quartic** (`320 v^4`; DBI speed limit at `v^2 = 1/16`): the kinetic sector,
  the exact rational `K(v) = 2(2688 v^6 - 544 v^4 + 40 v^2 - 1)/(16 v^2 - 1)^3`, all-positive, a
  velocity cap.

So the built effective / variational object is `E(u, v) = V(u) + K(v)`: the induced potential
sector `V` on the constant-curvature (record-count) slice `sigma = u * eta`, plus the DBI kinetic
`K`. A true vacuum is an interior MINIMUM of `E`.

## 1. Persona 1 (effective-potential specialist) -- build V_eff, search for v* (Q1)

Building the potential sector directly on the record-count slice (the verbatim W126 Route-1
machinery, regression-pinned by PC1-PC2):

```
V_eff(u) = -64 u^2 - 8 u + 2        (exact, degree 2; c_3 = c_4 = ... = 0)      [EP1a]
```

This is EXACTLY quadratic and **inverted** (leading `-64 u^2`). The variational condition
`dV_eff/du = 0` has **exactly ONE** stationary point `u* = -1/16` (`EP1b`) -- as an inverted
parabola must; there is no second extremum for a true vacuum to sit at. And it is a **MAXIMUM**
(`V_eff'' = -128 < 0`, `EP1c`): the tachyonic top, the FALSE vacuum. `V_eff -> -infinity` as
`u -> +/- infinity` BOTH ways (`EP1d`): a genuine runaway, unbounded below.

**Q1 answer (part 1): the BUILT effective potential has NO true-vacuum minimum.** The record
condensate vev `v*` is not realized as a stationary point of `V_eff`. This is not a truncation
artifact: it is the exact degree-2 structure (W126), and W203 adds nothing to it.

## 2. Persona 2 (variational analyst) -- the full DBI energy functional (Q1, Q4)

Add the kinetic sector and minimize the full static energy `E(u, v) = V(u) + K(v)`. Critical
points factorize: `dE/dv = K'(v) = 0` and `dE/du = V'(u) = 0`.

- `K'(v) = 0` only at `v = 0` (`K` is an even DBI function, minimum `K(0) = 2`, `K''(0) = +32`):
  the kinetic sector prefers ZERO gradient. A static homogeneous condensate with nonzero gradient
  is energetically DISfavoured (`VA1a`).
- So the ONLY critical point of `E` is `(u*, 0)` -- the tachyonic top with zero gradient -- and
  its Hessian is `diag(V''(u*), K''(0)) = diag(-128, +32)`: **INDEFINITE, a SADDLE, not a
  minimum** (`VA1b`). The full potential-plus-DBI-kinetic energy has NO interior true-vacuum
  minimum; `inf E = -infinity` along the potential runaway.

**Where would the true vacuum have to sit?** W163's `Im V_eff = m_0^4/(64 pi) = 1/(1024 pi) != 0`
CERTIFIES a true vacuum exists (PC6). Balancing the tachyonic drive needs a scale `~ |m_0^2| =
1/4`. But the DBI kinetic function DIVERGES (the induced metric degenerates) at `v^2 = 1/16`, and
`|m_0^2| = 1/4 = 4 x (1/16)`: the true vacuum sits a **factor 4 BEYOND the validity wall**
(`VA1c`). It EXISTS but is unreachable inside the derivative expansion.

**Q1 answer (part 2) / Q4:** the true vacuum EXISTS but is **OUT-OF-VALIDITY**. The extension
needed is the non-perturbative record-condensed completion (W154 source action / promotion-gate
`T4`), currently UNBUILT and not shown stable (W163 NPV5). This converges exactly with W159 route
2 and W163 NPV4.

## 3. Persona 3 (spectrum / stability checker) -- real? bounded? (Q2)

There is no true-vacuum minimum to expand around, so the only accessible spectrum is at the
tachyonic top (the false vacuum) -- the native GU poles (W130), block-diagonal in spin:

```
spin-0 (scalaron / record-count):  m_0^2 = -1/4
spin-2 (TT graviton):              m_2^2 = -1/4
```

- **Real?** YES (`SP1a`). Both poles are real; the inverse propagator `(k^2 + m_0^2)(k^2 +
  m_2^2)` is block-diagonal with no complex or cross-mixed poles.
- **Bounded / sensible?** NO (`SP1b`). The spectrum contains a NEGATIVE (tachyonic) eigenvalue in
  the record-count mode, and `V_eff` is unbounded below. It is the FALSE vacuum -- pathological AS
  a vacuum.
- **Liftable?** NO (`SP1c`). The mass is FIELD-INDEPENDENT (`V_eff'' = -128` constant; W164: exact
  quadraticity => `f''` constant => `dm^2/dR = 0`), so the tachyon cannot be healed by rolling to
  higher density / curvature. The chameleon channel is structurally absent.

**Q2 answer:** the vev `v*` is not realized in validity; the accessible spectrum is REAL but
TACHYONIC and unbounded-below -> **PATHOLOGICAL** as a vacuum. There is no in-validity minimum to
host a real, bounded, sensible spectrum.

## 4. Persona 4 (record-count-mode specialist) -- does the W166 reading hold at v*? (Q3)

The record-count mode IS the conformal / scale mode `p`; `N = 4-volume ~ e^{4p}` is strictly
monotone (`dN/dp = 4 e^{4p} > 0`, `RC1a`). `m_0^2 = -1/4 < 0` makes the homogeneous mode
`p ~ e^{+tau/2}` GROW (roots `+/- 1/2`, `RC1b`), so `m^2 < 0` in the record-count mode LITERALLY
means `N` grows = the arrow of time (W166 Q1 reproduced). At the DBI speed limit `|dp| = v_max =
1/4` the runaway is LINEAR in `p`, so `N ~ e^{4 v_max tau} = e^{tau}`: a steady de-Sitter-rate
accretion with rate `4 v_max = 1` -- the record-count "Hubble rate" EQUALS the DBI speed limit
(`RC1c`), a bounded-RATE clock.

**Q3 answer:** the W166 reading **HOLDS as a description of the ROLL** -- the tachyon IS the
record-accretion / arrow-of-time engine, DBI-capped to a de-Sitter rate -- but it **cannot be
evaluated AT a static true vacuum because none exists in validity** (`RC1d`). The true "vacuum"
is the rolling de-Sitter attractor itself, not a stationary minimum with a rest spectrum. This is
consistent with W166 (engine) and W163 NPV5c (monotone rolling, not a stable minimum).

## 5. Persona 5 (ruthless skeptic) -- pin the verdict; do not over- or under-claim

- **Is "RUNAWAY-NO-VACUUM" an overclaim?** Partly -- W163's `Im V_eff != 0` certifies a true
  vacuum EXISTS. Concede and state precisely: a true vacuum EXISTS but is OUT-OF-VALIDITY (4x
  beyond the wall) and UNBUILT; WITHIN the built in-validity effective potential the dynamics is a
  RUNAWAY with no minimum. The honest two-part label is **RUNAWAY-NO-VACUUM (in-validity) ==
  EXISTS-BUT-OUT-OF-VALIDITY (true vacuum)** (`SK1`). Not EXISTS-SENSIBLE; not strictly
  NO-VACUUM.
- **Is the `|u*| = 1/16` (potential-max curvature amplitude) `= v_c^2` (DBI wall gradient)
  coincidence a mechanism?** NO (`SK2`). `u*` is a curvature-slice amplitude, `v_c^2` a gradient
  degeneration -- different objects. Flagged as a numerical curiosity; no claim rests on it.
- **Does the W203 forced action manufacture a minimum the `|II|^2` potential lacks?** NO (`SK3`).
  Its `theta` is Gaussian and Legendre-eliminated (`V(theta)` absent); it fixes the source
  coupling only. The built forced action inherits the SAME inverted-quadratic runaway.
- **No forbidden target** `{3, 8, 24, chi(K3), Ahat}` assumed or inserted (`SK4`).

## 6. Synthesis -- the return data

| Question | Result |
|---|---|
| Q1 -- does a true vacuum EXIST? | NOT inside the BUILT in-validity effective potential: `V_eff(u) = -64u^2 - 8u + 2` is exactly quadratic, inverted, unbounded below; unique stationary point is the tachyonic MAXIMUM; the full `E(u,v)` has it as a SADDLE. A true vacuum EXISTS (W163 `Im V_eff != 0`) but only OUT-OF-VALIDITY, 4x beyond the `v^2=1/16` DBI wall. |
| Q2 -- nature (vev `v*` + spectrum) | `v*` NOT realized in validity; the accessible spectrum is REAL (`m_0^2 = m_2^2 = -1/4`, block-diagonal) but TACHYONIC and unbounded-below -> PATHOLOGICAL; FIELD-INDEPENDENT (W164), so not liftable by rolling. |
| Q3 -- does the W166 reading HOLD at `v*`? | HOLDS as a description of the ROLL (`m^2<0` = `N` grows = arrow of time, DBI-capped to a de-Sitter rate `4 v_max = 1`), but there is NO static true vacuum to evaluate it AT; the true "vacuum" is the rolling de-Sitter attractor. |
| Q4 -- in-validity, or extension? | OUT-OF-VALIDITY. Extension needed: the non-perturbative record-condensed completion (W154 source action / promotion-gate `T4`), UNBUILT and not shown stable. |
| OVERALL VERDICT | **RUNAWAY-NO-VACUUM (in-validity) / EXISTS-BUT-OUT-OF-VALIDITY (true vacuum).** |
| tests + exit code | `tests/W213_true_vacuum_effective_potential.py` -- 24/24 PASS, exit 0 (six positive controls first). |

## 7. What this does NOT do (the honest limits)

- Exploration grade; conditional register throughout. Nothing asserts GU, asserts a vacuum, or
  changes any verdict. A runaway / out-of-validity / non-existent-in-validity vacuum is reported as
  the REAL result it is.
- The true-vacuum EXISTENCE rests on W163's ported real-scalar Coleman-Weinberg `Im` part, not a
  native GU one-loop measure computation; the robust content is the SIGN, fixed by `m_0^2 < 0`.
- The variational energy functional `E = V + K` is the honest static-configuration reading of the
  induced `|II|^2` potential + gradient sectors, not a full covariant stress-tensor minimization on
  Y14. The DBI / k-essence, Coleman-Weinberg, and de Sitter relabel machinery are PORTED and
  labelled.
- The record-condensed completion is UNBUILT (W154 `T4`); the reading does NOT close the E2 AF/AS
  branch fork. Tri-repo gating STRICT: record / finality / arrow-of-time semantics stay pointers to
  temporal-issuance / time-as-finality / TaF; GU owns the effective-potential math only.
- No canon / RESEARCH-STATUS / claim-status / verdict / posture change; the count stays `{1,3}`;
  H59 OPEN; bar (b) UNCHANGED; E2 carried. No forbidden target assumed or inserted. Zero em dashes.

## Convergence note

This team's method is the **effective-potential / variational** one; the other four are RG-flow,
dynamical-systems, spectral-condensate, and geometric-everpresent-Lambda. What agreement or
disagreement would mean:

- **The load-bearing convergence claim of W213 is: there is NO stationary true vacuum inside EFT
  validity; the record-condensed true vacuum is real (Im V_eff != 0) but sits beyond the
  `v^2 = 1/16` DBI wall and is a rolling de-Sitter attractor, not a static minimum.** This already
  agrees with the effective-potential predecessor W163 by construction (same potential sector,
  independently rebuilt here as a full `V + K` variational search with the spectrum and Hessian
  computed explicitly).
- **RG-flow team (W214+):** should independently see the scalaron mass sign-locked negative along
  the forced (AF) trajectory with no zero crossing (W119/W163 NPV1). AGREEMENT = the runaway is not
  an RG artifact; the "no in-validity minimum" is trajectory-robust. DISAGREEMENT (a genuine
  RG-generated zero crossing / dimensional-transmutation minimum inside validity) would mean the
  variational build MISSED a loop-generated confining term -- a real crack, worth chasing, because
  my `E = V + K` is tree + DBI and does not carry the full quantum effective action.
- **Dynamical-systems team:** should see the record-count mode as a monotone growing (`k=0`) mode
  with a de-Sitter-rate attractor under the DBI cap (my RC1). AGREEMENT = the "true vacuum is a
  rolling attractor, not a fixed point" reading is method-independent. DISAGREEMENT (a genuine
  stable fixed point / limit cycle in validity) would contradict my SADDLE result and would have
  to explain what restoring force I missed -- the DBI sector is a speed limit (all-positive), so a
  stable fixed point requires structure outside the built action.
- **Spectral-condensate team:** should locate the condensate scale at `~ |m_0^2| = 1/4`, i.e. at
  `4 x` the `v^2 = 1/16` degeneration, hence OUT of the derivative expansion. AGREEMENT = the "out
  of validity by a factor of 4" margin is not an artifact of the variational framing. DISAGREEMENT
  (a condensate INSIDE validity) would mean either a different (non-`|II|^2`) kinetic normalization
  or a genuine non-perturbative kernel my ultralocal build omits -- exactly W203's unbuilt `Z_U`
  gradient-stiffness completion, the named residue.
- **Geometric / everpresent-Lambda team:** should read the same rolling attractor as the
  everpresent `Lambda ~ 1/sqrt(N)` de Sitter phase with `N` accreting (my RC1c ties the record-count
  Hubble rate to the DBI speed limit). AGREEMENT across all four other methods = the convergent
  picture is: **GU has no static true vacuum; its "true vacuum" is a record-accreting de-Sitter
  attractor, real but out of the current EFT's validity, whose in-validity construction requires
  the unbuilt W154 record-condensed completion.** That is the honest, method-independent landing --
  and the single point where a well-founded DISAGREEMENT from any team (an in-validity minimum, a
  stable fixed point, or an in-validity condensate) would be the most informative possible result,
  because it would localize a confining term the effective-potential build provably does not
  contain.

*Filed 2026-07-14 by Team EFFECTIVE-POTENTIAL / VARIATIONAL (W213), 1 of 5 convergence teams.
Route: build V_eff for the record-count mode from the W203 forced action + the W126 DBI gradient;
find v*; compute the spectrum; check boundedness. Five personas inline in one worker
(effective-potential specialist; variational analyst; spectrum / stability checker;
record-count-mode specialist; ruthless skeptic); no sub-agents. Reproducible:
`python -u tests/W213_true_vacuum_effective_potential.py` (24/24, exit 0; six positive controls
first). Exploration grade; conditional register; RUTHLESS self-verification; no canon movement;
tri-repo gating strict; the debit count stays {1,3}; H59 OPEN; bar (b) UNCHANGED. VERDICT:
RUNAWAY-NO-VACUUM (in-validity) / EXISTS-BUT-OUT-OF-VALIDITY (true vacuum).*
