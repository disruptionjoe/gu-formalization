---
title: PW2F-R corrected induced metric graph and highest-order Euler gate
date: 2026-08-02
status: scoped coefficient result; complete C4 remains open
lane: Eric-guided active-native construction; Curt remains a formal comparator
run: RUN-20260803-002500-gu-formalization-pw2fr-derived-k-c3
grade: >-
  PW2F-R CORRECTION AND PARTIAL PASS. PW2F omitted the derivative-bearing Z1
  part of the induced metric graph. Restoring it makes the ten-owner
  Levi-Civita/spin route exactly rank ten. The induced density can reach jet
  order six. An executable structural q2/q3 Euler comparator gives C6=0, and
  the four-variable C5 polynomial of the exercised principal-Z1 fixed-
  background branch cancels identically. The first live contribution is at
  C4: the principal-Z1 kappa1 distortion-norm subroute has exact rank ten and
  inertia (4,6) off the planted null conormal, and vanishes on e0+e3. This is
  neither the complete kappa1 coefficient nor a physical characteristic. The
  total-swervature contribution, partial-Z1 and section
  tangents, coefficient motion, and native direct/reverse Green return remain
  unassembled. C3 therefore remains blocked behind C4. No datum, physical
  equation, quotient, domain, or observation claim is made.
---

# PW2F-R corrected metric graph and Euler-order gate

## Result in plain English

The previous wave asked the right broad question but differentiated too small
an object. It varied the algebraic dependence of the metric on the ten
four-dimensional metric components, called `Z0`, while omitting the part that
depends on derivatives of those components, called `Z1`.

PW2F-R restores `Z1` and recomputes the top of the action. Three things happen:

1. The missing route is real. It has exact rank ten on every tested base
   conormal, so it cannot be discarded as bookkeeping.
2. It raises the apparent derivative ceiling, but the sixth-order term is
   structurally absent and the fifth-order term cancels as an exact polynomial
   in all four base-conormal variables.
3. The first live exercised subroute is fourth order. Its principal-`Z1` part
   multiplying the still-free distortion coefficient \(\kappa_1\) has exact
   rank ten and signature \((4,6)\) away from the tested null conormal. It
   vanishes on \(e_0+e_3\), but the characteristic set of the complete
   operator remains open.

This is useful progress because it turns a vague "complete the top order"
request into a much smaller target: assemble the other fourth-order pieces and
test whether they cancel this one. It does **not** yet prove that the complete
field equation is fourth order, that a special value of \(\kappa_1\) works, or
that the resulting characteristic is physical.

## Layer 0: the objects kept separate

The calculation uses the induced ten-owner
\(\operatorname{Sym}^2T^*X\) graph. It does not vary all 105 components of an
arbitrary metric on \(Y^{14}\). An upstairs conormal on \(Y\) is not identified
with an observed conormal on \(X\).

The three conditional tangent policies are: hold the source's \(\epsilon\)
fixed; vary that same \(\epsilon\) independently; or replace it with the
distinct repository construction

\[
h=\exp u(T,g)
\]

The inspected corpus supports the class of a connection minus a gauge-rotated
Levi-Civita connection. No inspected span in that declared corpus identifies
Weinstein's \(\epsilon\) with this repository-derived \(h\), selects one of the
three tangent policies, or supplies a metric-dependent selector and its
admissible variation domain.

The executed principal-`Z1` metric branch holds both independent source roots
\(\epsilon\) and \(\varpi\) fixed. Therefore
\(T=\varpi-B\) gives \(\delta T_{\rm top}=-\delta B_{\rm top}\). Holding
\(T\) or \(B\) fixed, or varying \(\varpi\) independently, would be a different
tangent problem and is not covered by the reported coefficient.

## Corrected induced metric graph

At the normal-frame fixture, a base metric variation \(k\) with conormal
\(\eta\) first varies the four-dimensional Levi-Civita connection:

\[
\delta\Gamma^c{}_{ia}
=\tfrac12 g^{cd}
  (\eta_i k_{ad}+\eta_a k_{id}-\eta_d k_{ia}).
\]

That connection motion induces a derivative-bearing cross block in the
connection metric on \(Y=\operatorname{Met}(X)\). This is the missing
\(Z_1(\nabla k)\) term. It is then trace-reversed through the existing
fourteen-frame and sent through the exact Levi-Civita/spin symbol.

On

\[
e_0,\quad e_1,\quad e_3,\quad e_0+e_1,\quad e_0+e_3,
\]

the `Z1` metric, `Z1` spin, and complete `Z0+Z1` spin banks all have rank ten.
Doubling the conormal multiplies the `Z1` spin term by four, as the derivative
count requires. The code also checks the linearized metric-compatibility
identity \(\delta\Gamma_i^Tg+g\delta\Gamma_i=\eta_i k\) on all 200
owner-direction cases. Total alternation kills all 50 complete exercised
Levi-Civita variations, with a planted non-Levi-Civita control retained.

## Complete highest-order derived-connection incidence

For

\[
K=h^{-1}D_Bh,
\]

the exact variation is

\[
\delta K
=D_{B+K}(h^{-1}\delta h)
 +(\operatorname{Ad}_{h^{-1}}-1)\delta B.
\]

At the highest induced base-jet order, torsion-free alternation removes the
`delta-u` route in the selected exact Hodge-null bridge. The remaining
connection incidence recombines exactly:

\[
\delta B+\delta K=\operatorname{Ad}_{h^{-1}}\delta B.
\]

The active conjugation preserves the distortion-norm Gram matrix. Thus the
top distortion result is not an artifact of working in the moved frame.
Partial-`Z1`, the section tangent, and lower coefficient motion are not covered
by this highest-order statement.

## Why the gate returns to C4

The restored `Z1` route makes \(\delta B\) and \(\delta T\) second order in the
base metric. Its curvature input is third order. An executable three-owner
polynomial Euler comparator checks the universal premise used for this route:
a Lagrangian quadratic in a second jet and affine in a third-jet curvature
input has \(C_6=0\), \(C_5=C-C^T\), and zero \(C_5\) when the mixed
coefficient is symmetric. This is a structural theorem under the exercised
principal-`Z1` jet ledger, not a claim that every open native path has already
been assembled.

The only possible \(C_5\) term is the antisymmetric owner block built from the
second-order distortion prefactor and third-order curvature variation. The
finite panel gives rank zero. More importantly, the exact symbolic
four-conormal calculation for the exercised principal-`Z1`, ten-owner,
fixed-background branch has zero nonzero polynomial entries, so that scoped
cancellation is not a five-point interpolation claim.
An exact transported-output/pairing comparator at \(e_0\) additionally shows
that the selected nontrivial null \(h\) preserves this cancellation; it does
not replace the still-open fully moving-Shiab calculation.

The exercised principal-`Z1` distortion-norm subroute then supplies a live
fourth-order contribution.
Its exact ranks are

\[
(10,10,10,10,0),
\]

with inertias

\[
(4,6,0),(4,6,0),(4,6,0),(4,6,0),(0,0,10).
\]

The final zero is the planted null conormal \(e_0+e_3\). It is not yet a
characteristic of the complete operator. FLINT independently verifies rank
ten, a nonzero determinant, and null rank zero over exact rationals.

This is only a principal-`Z1` subroute contribution multiplying
\(\kappa_1\); other \(\kappa_1\) paths may cancel or retain it. The complete
\(C_4\) calculation must also test whether the source-required but unspecified
quadratic-eddy completion of Shiab-contracted curvature has a \(C_4\)
component, together with partial-`Z1`, section-tangent, and motion of the
density/Krein/lowerer data. Direct differentiation and reverse formal adjoints
must agree in the bulk with every Green concomitant retained at the boundary;
the Green terms are validation data, not extra bulk \(C_4\) summands. No
exceptional value of \(\kappa_1\) can be inferred before that assembly exists.

## Source collision

The hashed source replay records:

- `SOURCE-CONFIRMS`: the connection-difference class and the demand that
  Shiab-contracted curvature be completed by an unspecified quadratic eddy to
  form total swervature for variational exactness.
- `SOURCE-CORRECTS`: the projection-versus-contraction reading.
- `SOURCE-SILENT`: identity of source \(\epsilon\) with repository \(h\), a
  metric-dependent selector, the admissible variation domain, the eddy
  formula or whether it contributes to/cancels \(C_4\), and the actual
  \(C_4/C_3\) coefficients.

The silence is an attribution boundary, not a mathematical no-go.

Evidence grades remain explicit: the draft equations use author-PDF custody
(`3f28d742...`) plus the rendered p. 56--57 equations 12.4--12.7 receipt
(`1a847400...`); TOE is a local automated transcript (`f4dfda89...`) at
01:36:35 and 02:19:17; Portal/Oxford is the official transcript
(`bd9f53ab...`) at 02:35:10. The raw and edited UCSD files are two repository
editions of one recording, not independent corroboration.

## Exact evidence and boundary

- Main exact probe: `15 exact + 2 source + 11 type + 9 planted = 37 PASS`.
- Source collision: `3 exact + 7 source + 5 type + 6 planted = 21 PASS`.
- FLINT rational certificate: `4 PASS`; this is an independent exact
  arithmetic backend on the primary geometric matrices, not an independent
  geometric construction.
- A Sage structural comparator is checked in as an independent \(C_6/C_5/C_4\)
  formula, but the current environment has no Sage executable, so this run
  does not claim a Sage execution receipt.

P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; `TG-1 AND TG-2 AND TG-3` remains
`NOT_PROMOTED`. There is no public-bundle port, global \(Y^{14}\) theorem,
analytic domain, BV/BFV quotient, observation theorem, or physics recovery.

## Next gate

`PW2F-R2-COMPLETE-PARTIAL-Z1-TOTAL-SWERVATURE-C4-GREEN-AND-EXCEPTIONAL-KAPPA`

The efficient order is:

1. construct the omitted partial-`Z1` and section tangent;
2. assemble the complete \(\kappa_1\) and total-swervature \(C_4\) with cached
   matrix-free JVP/VJP paths;
3. compare direct differentiation, reverse formal adjoint, and every Green
   concomitant;
4. determine whether any exceptional \(\kappa_1\) cancels the complete
   polynomial and compute the resulting characteristic set;
5. compute \(C_3\) only after \(C_4\) is fixed.
