---
title: "Current-frontier semantic currency: one operative next condition, preserved history"
status: active_research
doc_type: result
created: "2026-08-25"
registry: lab/process/current-frontier-semantic-currency.json
probe: tests/channel-swings/current_frontier_semantic_currency_audit.py
grade: "EXACT ORIENTATION-SURFACE AND POINTER HARDENING; NO SCIENTIFIC, LEDGER, SOURCE, PREDICTION, CANON OR PUBLIC-POSTURE MOVEMENT"
target_claim: NONE-NOT-A-KILL
target_claim_note: "This result binds the branch-relative work-selection surface, not a GU/source claim or physics construction."
canon_verdict_change: none
---

# Current-frontier semantic-currency hardening

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

```gu-typed-objects
result: semantic-currency split between one live GU Progress condition and preserved historical condition custody
carrier: CURRENT-STATE YAML orientation object plus agenda disposition B2-frontier and W154/W229 owner-qualification evidence LAYER=UNTYPED CHIRALITY=S-CHIRALITY-UNTYPED
pairing: exact pointer agreement between the live next condition and owner-authoritative evidence ON=declared-surfaces
real_structure: UNTYPED because no geometric or physical real structure is constructed or selected
grading: current instruction versus historical evidence; scientific grades remain inherited and unchanged
action_owner: UNTYPED because no source or conditional action is executed
target: CURRENT-STATE next_condition and prior_conditions MAP-TYPE=evaluation
```

## Result first

`CURRENT-STATE.yaml` previously held one correct current question and one
`next_condition` scalar containing both the current post-W154 gate and hundreds
of accumulated earlier instructions. Those historical paragraphs include the
now-superseded 25-terminal/66-open register state and
`b2_selectable=false`. They were scientifically honest when written, but a
YAML consumer could not distinguish them from the current work instruction.

The surface now has two roles:

- `next_condition` is the single operative condition. It rebuilds the current
  repository-wide frontier, keeps the named B2 root empty, selects the strongest
  disjoint non-B2 gate, and names the exact action/bridge reopeners.
- `prior_conditions` preserves the complete earlier scalar byte-for-byte except
  for its key. It remains evidence and correction history, not authority for a
  new Run.

The 91/91 disposition result remains true: the non-B2 disposition gate is
exhausted and B2 is selectable in principle. The later W154/W229 qualification
also remains true: the currently named B2 action-root set is empty. Those facts
are sequential, not contradictory. Selectability does not manufacture an
action owner.

## Preflight bookend

Layer 0 separated four objects that had been compressed by the old scalar:

1. historical conditions that were valid at their recorded revisions;
2. the live branch-relative next condition;
3. the derived B2 selectability gate over 91 terminal disposition rows; and
4. the later root-candidate availability result after W154/W229 nonadmission.

The route census considered deleting old conditions, keeping one scalar with a
warning, adding a second competing current pointer, and splitting live from
historical custody. Deletion loses evidence; a warning remains prose-only; a
second live pointer creates dual truth. The selected split preserves history
and leaves exactly one operative key.

Scientific work was challenged first. B2 lacks an owned root; SG4 awaits named
real/action/component/domain inputs or dual receipts; C1 awaits its owner-grade
source packet; prediction work lacks an applicable release or physical bridge;
ledger v0.263 has no new construction or coherent mint activation. The concrete
semantic-currency defect is therefore the strongest admissible Lane-3 swing.

## Certificate

The certificate parses `CURRENT-STATE.yaml`, verifies the live/history key
split, pins the 91/91 register and empty W154/W229 root-candidate set, checks
the research-agenda authority, and verifies that all protected movement fields
remain false. Its self-test mutates live-key identity, stale markers, root
availability, register counts, B2 selectability, history custody and protected
effects; every planted mutation must fail.

## Postflight bookend and hostile review

- **Strongest overclaim:** “B2 is no longer selectable.” Refused. The
  disposition-derived gate stays true; only the current named owner set is
  empty.
- **Strongest contrary route:** a new owner-native K77 action or genuine real
  action bridge can reopen B2 immediately under the recorded conditions.
- **Weakest reproducibility seam:** a future writer could prepend a fresh
  paragraph while leaving a stale condition in the same live scalar. The
  certificate rejects the named stale markers and requires the history key.
- **Weakest propagation seam:** older consumers may expect only
  `next_condition`. That key is preserved and remains a string; the change
  narrows its semantics without breaking its type.

No source claim, source ownership, ledger row, SG4 phase, prediction,
confirmation, claim status, canon verdict, release readiness or public posture
moves. No Joe intervention is required.

Reproduce with:

```bash
python3 tests/channel-swings/current_frontier_semantic_currency_audit.py --selftest
```
