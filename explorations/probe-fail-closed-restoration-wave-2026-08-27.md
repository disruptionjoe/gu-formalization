---
title: "Probe fail-closed restoration wave"
status: active_research
doc_type: internal_certificate_hardening
created: "2026-08-27"
grade: "MECHANICAL FAILURE-DETECTION HARDENING; NO NEW SCIENTIFIC RESULT"
target_claim: internal fail-closed probe custody; verdict three post-ratchet authorship defects are repaired and the global gate returns from 92 to its 89 baseline
canon_verdict_change: none
---

# Probe fail-closed restoration wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: INTERNAL_STRUCTURAL_ONLY`

Scope: mechanical failure detection in one geometry-first program-identity
probe and two source-custody probes. This artifact changes no GU, comparator,
source, action, ledger, SG4, prediction, paper or canon claim.

```gu-typed-objects
result: three post-ratchet probe-authorship findings are repaired and the global gate returns from 92 to its 89 baseline
carrier: repository-native Python probes and their JSON/YAML/Markdown fixtures LAYER=UNTYPED CHIRALITY=N/A
pairing: agenda-row identity to program passport; normalized source-register text to exact historical census assertions ON=declared-probe-fixtures
real_structure: unchanged from each touched probe; this hardening assigns no real form
grading: mechanical certificate hardening only
action_owner: N/A -- no scientific action owner is created or transferred
target: make a missing agenda row and wrapped source-register headline reach controlled assertions MAP-TYPE=evaluation
```

## Preflight bookend

The source register is terminally typed at `ADHERED 106 / PARTIAL 4 / UNTYPED
1`, ledger v0.263 has 91 terminal rows and zero open rows, the named B2 action-
root set is empty, and prediction/data routes remain monitor-only or input-
gated. None supplies an executable source-to-proof, ledger, action or
prediction gate.

The entering probe-authorship lint is red at 92 findings against the 89
ratchet across 1,006 probes. Semantic review identifies exactly three post-
baseline defects: a defaultless agenda lookup in the geometry-first passport
probe and raw multiword source-register headline searches in two source waves.
The same review excludes the infinite `itertools.count()` witness in the
construction-prong probe, historical snapshot pins, proved-nonempty theorem
domains and author-reserved comparator classifications.

## Result

The geometry-first passport probe now returns `None` when the named agenda row
is absent and immediately asserts its presence before dereference. A removed
or renamed program row therefore produces a controlled failure instead of
`StopIteration`.

Both source-custody probes now parse the current wrapped adjudication census
and verify that it preserves each completed historical transition by monotone
bounds. The wave manifests retain their exact before/after values, the named
rows and scientific fixtures are unchanged, and later legitimate adherence
progress no longer makes a historical probe stale.

The coupled certificate replays all three native probes, checks the complete
1,007-probe corpus, and plants the inverse defaultless/raw-text mutations. The
global gate returns to 89 findings at its unchanged baseline of 89.

## Postflight bookend

- **Strongest overclaim:** fail-closed lookup and wrap-stable text matching
  improve detector behavior; they do not validate the geometry-first program,
  establish a source assertion, or increase confidence in any scientific
  result.
- **Strongest contrary case:** a defaultless `next()` over an infinite counter
  has a guaranteed witness, while some exact historical counts intentionally
  freeze a snapshot. Those cases remain untouched.
- **Weakest reproducibility seam:** the global lint recognizes mechanical
  syntax classes, so each touched scientific probe is also replayed with its
  own hostile mutations.
- **Hostile controls:** removing the agenda default/presence assertion,
  restoring raw register searches, or mutating either historical headline is
  detected.

No source adherence or polarity, action owner, ledger row, SG4 phase,
prediction, confirmation, paper, canon verdict, release readiness or public
posture changes. Further probe hardening requires another concrete semantic
defect or a fresh measured baseline decrease after a rebuilt frontier.
