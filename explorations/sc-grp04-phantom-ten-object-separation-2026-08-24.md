---
artifact_type: exploration
status: complete
doc_type: source_claim_adjudication
created: 2026-08-24
work_item: SC-GRP-04-PHANTOM-TEN-OBJECT-SEPARATION
target_claim: SC-GRP-04
target_claim_verdict: PARTIAL__SOURCE_VECTOR_CARRIER_EXACT__GG_MATTER_TEN_EXCLUDED__GR_CARRIER_CONTACT_EXACT__DYNAMICAL_EXPLANATION_UNDERIVED
grade: EXACT_CARRIER_TYPING__DYNAMICAL_EXPLANATION_UNDERIVED
canon_verdict_change: none
canonical_effect: none
priority_change: none
scripts:
  - tests/channel-swings/sc_grp04_phantom_ten_object_separation_probe.py
---

# SC-GRP-04: the phantom ten is a typed carrier, not every object named “10”

## System and target opening bookend

The parent Run admitted this independent Lane-1 packet on clean GU HEAD
`0a1076c9`. Official draft custody, equation (4.9), the source-adherence row
and the existing metric-fibre results were reopened read-only before the
object comparison. This artifact is the bounded owner effect and inherits the
parent claim and non-movement ceiling.

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

## Scope

In scope are draft pages 27--30 and equation (4.9), the defining vector
carrier and its Georgi--Glashow/Pati--Salam restrictions, the conventional
SU(5) matter-`10` collision, the GR symmetric-tensor carrier contact, and the
absence/nonuniqueness of an action-owned explanatory map. Proton decay,
symmetry breaking, physical quotient, scientific ledger rows, predictions,
canon and public posture are out of scope.

## Source reading before comparison

Source custody is the official 69-page working draft pinned in
`lab/sources/weinstein-gu-primary-source-pack-2026-07-30.md` at SHA-256
`3f28d742234a9841fc8e51ff172053200aa3eddf3ece38154a3328b9ebd186d4`.
Printed page 28 names `V^10` as the ten-dimensional normal/vertical
complement before the group diagram; pages 29--30 and equation (4.9) then type
its alternative structure reductions.

Draft equation (4.9) is a diagram of real forms and subgroup reductions. At
the top is `Spin(10,C)`; beneath it sit `Spin(6,4)` and compact `Spin(10)`.
The noncompact branch contains `SU(3,2)` and the Pati--Salam compact subgroup
`Spin(6) x Spin(4)`; the compact branch contains Georgi--Glashow `SU(5)`.
The following prose calls a phantom ten-dimensional representation common to
the Georgi--Glashow and Pati--Salam theories and then says that this strongly
suggests a fundamental explanation. The earlier prose only hopes that the ten
may be tied to the ten coupled equations of General Relativity.

That grammar matters. The claim is about the defining real ten-dimensional
carrier behind the diagram, not automatically the complex Georgi--Glashow
matter irrep conventionally also called the `10`.

## Object table

| token | typed object | scalar dimension | exact relation |
|---|---|---:|---|
| GU vertical ten | `E_g = T_g Met(X) = Sym^2(T_x^*X)` | `10` real | metric-variation carrier |
| noncompact Georgi--Glashow analog | underlying real space of `C^(3,2)` | `10` real | `Re(h_(3,2))` has inertia `(6,4)`, so `SU(3,2)` acts through `SO(6,4)` |
| compact Georgi--Glashow carrier | underlying real space of the standard `C^5` | `10` real | complexification is `5 direct-sum 5bar`; `SU(5)` acts through `SO(10)` |
| Pati--Salam carrier | `(6,1,1) direct-sum (1,2,2)`, i.e. `R^6 direct-sum R^4` | `10` real | restriction of the defining vector to `Spin(6) x Spin(4)` |
| Georgi--Glashow matter “10” | `Lambda^2 C^5` | `10` complex = `20` real | a different representation; the centre acts with weight two rather than one |
| GR equation carrier | `E_mu_nu(g) in Sym^2(T_x^*X)` | `10` real components | same tensor type as the metric-fibre tangent, before any dynamical explanation |

The first four rows are presentations or reductions of the same rank-ten
vector-carrier pattern after changing real structure, metric signature or
compact reduction. Their common complexification is the vector
representation of `Spin(10,C)`. The conventional SU(5) matter `10` is not that
carrier: it is `Lambda^2 C^5`, has real dimension twenty, and the central
element with weight one on `C^5` acts with weight two on `Lambda^2 C^5`.
Equal printed numerals therefore do not permit result transfer.

The centre-weight test is an exact `Hom` obstruction to the most tempting
misidentification. If `zeta` generates the centre of `SU(5)`, it acts with
weight one on `C^5` and weight two on `Lambda^2 C^5`. Hence a complex-linear
`SU(5)` intertwiner
`Lambda^2 C^5 -> 5 direct-sum 5bar` is zero: the target has centre characters
`zeta` and `zeta^(-1)`, neither equal to `zeta^2`. Their unequal complex
dimensions also rule out an isomorphism. This obstruction separates the
conventional matter `10` only. It does not obstruct the draft's defining real
carrier.

## What is exact and what remains a hope

Two pieces are now exact at the object layer:

1. `dim_R Sym^2(R^4*) = 4(4+1)/2 = 10`, and the ten Einstein equations are
   the ten symmetric tensor components of one equation. They are not ten
   independent physical equations: the contracted Bianchi identity supplies
   four differential identities.
2. The draft's Georgi--Glashow and Pati--Salam branches act on real rank-ten
   vector carriers whose complexified spin parent is `Spin(10,C)`; the
   compact and `(6,4)` real forms are precisely distinguished.

This gives a real structural bridge: the tangent to the metric fibre and the
GR equation tensor have the same `Sym^2(T^*X)` carrier type. It does not give
the hoped-for fundamental explanation. No source or repository result derives
the Einstein equation from the branch diagram, derives either internal
reduction from the GR Euler tensor, or proves that a ten-dimensional group
carrier is an action-selected physical sector. A field-space pairing can turn
a variational covector into a tangent vector, but that additional owner cannot
be hidden inside the dimension count.

Under the Lorentz group this carrier splits as the real traceless symmetric
module plus the trace, `Sym^2(T^*X) = (1,1)_R direct-sum (0,0)_R`, of real
dimensions `9+1`. Its equivariant endomorphism algebra therefore contains the
independent trace and traceless scalars. The carrier contact does not select a
unique Euler map or transfer the full internal `Spin(6,4)` action to a Lorentz
equation.

The source claim therefore moves from `UNTYPED` to `PARTIAL`: the common
phantom-ten carrier and the GR tensor contact are typed; the claimed
explanatory force remains an explicitly hopeful, underived bridge.

## Hostile review

The strongest overclaim is “the ten Einstein equations explain SU(5) or
Pati--Salam.” They do not. The strongest numerical trap is to substitute the
complex matter irrep `Lambda^2 C^5` for the real carrier `C^5_R`; the exact
real dimensions and centre weights reject that substitution. The weakest seam
is dynamical: the source supplies a group diagram and a hope, not an action-
owned map from the Einstein Euler tensor to an internal physical quotient.

```gu-typed-objects
result: exact rank-ten carrier separation with a typed GR tensor contact and an underived dynamical bridge
carrier: E_g=Sym^2(T*X), C^5_R, C^(3,2)_R and R^6+R^4; excludes Lambda^2 C^5 as the same object LAYER=source-print CHIRALITY=N/A
pairing: Re Hermitian forms of signatures (10,0) and (6,4) ON=defining-real-vector-carriers; field-space tangent/covector pairing not silently supplied
real_structure: compact Spin(10) vector with SU(5) restriction versus noncompact Spin(6,4) vector with SU(3,2) and maximal-compact restrictions, with common Spin(10,C) complexification
grading: vector-carrier versus exterior-square matter representation
action_owner: source-print; hoped-for dynamical explanation remains UNTYPED
target: SC-GRP-04 source-adherence row MAP-TYPE=not-a-map
```

## Closing bookend

The intended effect is achieved without a numerical-homonym promotion: the
source's common carrier is reconstructed at exact representation type, the
GR contact is limited to its tensor carrier, and the dynamical explanation
remains open. No group horn, action, scientific ledger row, prediction,
confirmation, paper, canon or public posture moves. The next wake is an
action-owned equivariant map that composes the GR Euler carrier with one
branch's physical quotient.

System postflight returns this owner effect to the parent receipt. A central
`10` homonym-register graduation is deliberately deferred: that register's
method requires the paid artifact to be committed first and its count-pinned
certificate to move in the same later act. The parent envelope did not admit
that certificate. No child Runtime record, external action or additional
repository effect is created.
