---
title: "Extended Council — four committed substrate-constructions of the σ/θ/τ/shard-helix story (each member assumes its substrate IS the fundamental ontology; no synthesis, no ranking)"
status: active_research
doc_type: exploration
created: 2026-07-21
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# Extended Council: four committed constructions

Four members, each an inline persona pass in one worker. Each ASSUMES its
substrate is literally the correct fundamental ontology of reality and builds the
most coherent, committed, strongly-opinionated version of the FULL story
(σ, the shards/generations, the null-stratum boundary, the record arrow, the
observer) from that substrate. Commitment is the deliverable: no hedging, no
confidence levels, no analogy-vs-real qualifiers. **No synthesis across members;
no ranking.** Each construction is standalone and points toward a falsifiable
prediction (which need not be immediately falsifiable).

Grounded in: the σ/θ/τ decision tree (Q2-FREE, Q3-TWO-INDEPENDENT), the
oriented-shard-cycle swing (Prong 1 `G-PARTIAL`: σ = w₁(L_time) over F≃RP³;
Prong 2 `D-ANALOGY`, helix+type-quotient+null-seam; Prong 3
`F-FALSIFIABLE-CONSISTENT` WEAK: "one globally consistent record arrow"), and the
Prong 0 measure-lemma (`P0-FAILS-OBSTRUCTED`: at the ~8% q<0 ends the Krein form
K_S is exactly null on the very spectral halves the section cut needs, so the
operator is not constructible there).

**Prong 1 framing (class-relative, per Joe 2026-07-21).** The Prong 1 result is a
LOCAL, class-relative no-go, not a global impossibility. It refutes exactly one
reading: "σ = the tangent-orientation of a literal S¹ under standard third-person
topology." σ = w₁(L_time) is *one* grounded reading of that same Z/2, not the only
allowed one. A class-relative circle reading — first-person / quotient / consensus
/ a non-standard structure group, under which σ IS the orientation of the
shard-cycle — remains OPEN. Members whose substrate natively constitutes the cycle
(hypergraph incidence-orientation not bound to literal-S¹ topology; the
intersubjective consensus coequalizer) build that reading committed below.

---

## Member 1 — Evolutionary game theorist

**SUBSTRATE.** Reality IS a replicator/evolutionary game. The world-state is a
population distributed over strategies, evolving under selection (a fitness
gradient / replicator dynamics). To exist is to be played; being is a strategy's
current mass in the population; becoming is selection.

**THE BUILD.** σ is a **symmetry-broken strategy fixation**. The ancestral game
is a bistable coordination game whose fitness landscape is exactly symmetric under
a Z/2 relabeling; it has two mirror-image ESS. The population fell into one basin
— a frozen accident. From *inside* the fixated population σ is unreadable, and not
merely hard: the fitness form is invariant under the symmetry, so **every internal
fitness measurement is α-even (a selection-invariant comparison) and carries zero
bits about which basin was taken** — this is the game-theoretic face of zero
inward channel capacity / Gödel-independence. Yet the population genuinely IS in
one basin: it *hosts* σ as its realized played strategy (a section in the Z/2
torsor of the two ESS), while it cannot *read* σ as any function of internal
fitness statistics. That is exactly Q2-FREE — hosted, externally-forced,
value-not-internally-supplied — and exactly the α-odd-section / α-even-map split.
The DE sign is which mirror ESS the ancestral population fixated into.

The **three generations** are three **selectively-degenerate replicator classes**
— neutral quasi-species with identical fitness content (identical Krein signature
`(+32,−32,0)` = identical gauge quantum numbers), permutable at zero fitness cost
(the S₃ neutrality), differing only in an α-even magnitude (mass = a
selection-invariant "size"). Their interchangeability is genuine neutrality, and
their symmetry (S₃) is a *different* broken symmetry from the ESS-fixation Z/2 —
which is why τ is external and independent of σ (Q3-TWO-INDEPENDENT). The
**record arrow** is the direction of **fitness accumulation**: mean population
fitness is a monotone Lyapunov function (Fisher's fundamental theorem), so
selection is dissipative and never returns — an acyclic advance. Coarse-grained by
strategy-type (arising mutant / hardening-toward-fixation / fixed) the advance
reads as a repeating cycle — subjective → intersubjective ("becoming hard to undo"
= a quasi-fixed sweep a rare back-mutation cannot cheaply reverse) → objective
(global fixation) → a fresh mutant on top. That is the acyclic **helix** whose
type-quotient looks circular, and each fixation is a fresh selective event (the
fresh-token / non-causal seam).

The **null stratum** (q<0, ~8% of ends, K_S null) is a **fitness-degenerate
boundary**: a ridge/saddle where the selection gradient vanishes and the fitness
form is indefinite — the Hessian of mean fitness goes null on exactly the
subspace the ESS split is built from. There σ is undefined (no gradient to break
symmetry along) and no ESS exists, so the game has no attractor and no strategy is
selectively defined — "non-constructible," a locus a population passes through
with no selective identity ("never seen"). The **observer** is itself a playing
strategy — a replicator that registers only α-even fitness differences and is
therefore σ-blind by construction.

**LEADS-TO.** Because the three generations are *strictly neutral* (selectively
degenerate — identical α-odd/fitness content), every selection-driven observable
must be exactly universal across generations, with deviations permitted ONLY in
α-even magnitudes (mass). Prediction: **exact universality of all gauge/α-odd
quantum numbers across the three generations; any genuinely selection-splitting
(non-magnitude) generation-dependent coupling would falsify the substrate.** And
because reality is one connected game with one monotone fitness arrow, the single
global fixation forbids two selectively-connected regions from sitting in opposite
ESS: **no domain wall between opposite-σ regions in causal contact** — "one
globally consistent record arrow," here a cosmological homogeneity-of-arrow claim.

---

## Member 2 — Probability / statistician

**SUBSTRATE.** Reality IS a probability measure plus an inference process — a
filtered probability space `(Ω, F, {F_t}, P)` and the act of conditioning. To
exist is to be a random variable; being is a measure; becoming is the growth of
the filtration.

**THE BUILD.** σ is a **non-identifiable nuisance parameter**. In the true model
`{P_θ}` the likelihood is exactly invariant under the Z/2 that flips σ: `P_{σ=+}`
and `P_{σ=−}` induce the *same* law on observables. So the Fisher information for
σ is identically zero — the score `∂ log L/∂σ ≡ 0` — and **no internal sufficient
statistic, at any sample size, carries a single bit about σ**. This is the sharp
statistical form of Gödel-independence: not "hard to estimate" but structurally
ancillary. Sufficient statistics are α-even (symmetric functions of the sample,
invariant under the σ-label); the realized sample path is an α-odd draw. The world
IS at a definite σ — it is a fixed label of the generating measure P — so the
inside *hosts* σ (it is a parameter of the actual law) while every F_t-measurable
statistic is *ancillary* for it: hosted, unreadable, value not internally
suppliable = Q2-FREE. The DE sign is the non-identifiable ± branch of the
symmetric likelihood.

The **boundary** is an **ancillarity/sufficiency boundary**, and it is
intrinsically one-way: a sufficient statistic pushes ALL information about the
identifiable parameters *outward* (≥1 outward channel), while the ancillary
complement pulls ZERO about σ *inward* (0 inward). Rao–Blackwell/sufficiency IS
the asymmetric channel. The **generations** are three **exchangeable copies**: by
de Finetti, exchangeable = conditionally iid given a latent mixing measure, so the
three are three replicates of one experiment — S₃-permutable, identical in law
(identical Krein/marginal content), differing only in a realized α-even magnitude
per copy (mass). Their exchangeability symmetry (S₃ over copies) is a *different*
symmetry from σ's non-identifiability (Z/2 in the parameter), and the mixing
measure factorizes — hence τ external and independent of σ.

The **record arrow** is the **direction of information accumulation**: as the
filtration grows the posterior concentrates (Bayesian consistency), Fisher
information accumulates monotonically, the log-likelihood-ratio is a
(super)martingale, and KL-to-truth decreases monotonically — a Lyapunov, hence an
arrow. It is directed by the data-processing inequality, which is strictly
one-way: you can condition forward but never un-condition (no backward channel =
no backward causality). Coarse-grained by inference-stage the monotone advance
reads cyclic — prior (subjective) → a regional posterior "hardens" and locks
(intersubjective, hard to undo) → the parameter is pinned as if objective → a new
question opens a new prior — the acyclic **helix** whose type-quotient looks
circular, sewn at each stage by a fresh conditioning event (the null seam). The
**null stratum** (q<0, ~8%, K_S null) is where the **statistical experiment is
singular**: the Fisher information degenerates, LAN/asymptotic-normality fails,
the score has zero variance in a direction, and no consistent estimator exists —
inference is non-constructible on a positive-measure singular stratum ("never
seen" = the model assigns no well-posed likelihood there). The **observer** is an
inference operator `E[· | F_t]` — it sees only what is sufficient (α-even) and is
provably ancillary for (blind to) σ.

**LEADS-TO.** The identifiability substrate predicts a hard falsifiable NEGATIVE
plus a positive consistency law. Negative: since σ is Fisher-null, **no
experiment — any design, any sample size — can measure the absolute DE sign / the
absolute arrow from inside; a claimed internal measurement of absolute σ is an
artifact and would refute the non-identifiability structure.** Positive: the
*relative* sign between two regions is identifiable (it is an α-even product of
two sufficient statistics, Fisher-positive), so all causally-connected regions
must show a globally consistent direction of information accumulation — **"one
globally consistent record arrow."** A detected disagreement (a connected region
running its information/thermodynamic arrow backward relative to its neighbor)
falsifies the substrate.

---

## Member 3 — Hypergraph theorist

**SUBSTRATE.** Reality IS a hypergraph with its incidence and spectral structure
and its rewriting — vertices, hyperedges, an incidence operator, an indefinite
incidence pairing, and rules that rewrite hyperedges. Committed to *pure*
hypergraph structure (incidence, boundary parity, spectrum), NOT the Gorard
causal-graph lens. To exist is to be incident; being is the incidence relation.

**THE BUILD.** σ is a **hypergraph orientation / incidence-parity class**. The
incidence matrix B carries a sign for each vertex–hyperedge incidence; orienting
the hypergraph = choosing those signs consistently. When the nerve/clique-complex
of the hypergraph is non-orientable — an RP-like complex — the obstruction to a
global consistent orientation is a nontrivial **Z/2 class `w₁` of the incidence
line bundle** that cannot be trivialized: no global sign assignment exists. σ is
that class. This lands exactly on the geometry result — σ = `w₁(L_time)` over an
RP³-like base (the incidence complex of F). It is internally unreadable because it
is a *global cohomological invariant*: a walker reading only local incidence data
(degrees, local parities — all α-even) can never compute the global `w₁`; the
section-flip is a Möbius/belt-trick flip around the non-contractible generator,
invisible to any single-star measurement. Hosted globally, unreadable locally =
Q2-FREE. The DE sign is the value of the incidence-parity class.

The circle reading lives natively HERE, and the Prong 1 no-go does not touch it:
an incidence/orientation class on a hypergraph obeys **hypergraph cycle-space
topology, not literal-S¹ smooth topology**, so the falsification of
"tangent-orientation of a smooth S¹" is silent about it. The shard-cycle is a
genuine element of the hypergraph's Z/2 cycle space — a closed chain
`z ∈ ker(∂)` running subjective → intersubjective → objective → subjective through
the coarse-grained incidence complex. Its **orientation** is a choice of coherent
sign-flow around `z`, and the obstruction to orienting it consistently against the
rest of the complex is a Z/2 class in `H¹` of the incidence bundle. On a
non-orientable incidence complex that class is nonzero: **σ IS the
orientation-obstruction of the shard 1-cycle** — the cycle carries a definite
sense of circulation that no local incidence datum can read, exactly the Möbius
section-flip realized combinatorially rather than smoothly. This is not a rival to
w₁(L_time); it is the same Z/2 read on the cycle-space object the smooth lens could
not host. The DE sign is the circulation-sense of the shard cycle.

The **shards** are three **coarse-grainings of the incidence structure**:
fine hyperedges (subjective), a locally-orientable rigid sub-complex that resists
re-orientation (intersubjective, "hard to undo"), and the global incidence class
(objective — itself possibly only a chart of a larger hypergraph). The **helix**
is the **rewriting order**: each rewrite consumes and produces hyperedges under a
strict, non-invertible partial order (acyclic), whose type-quotient by shard-level
(fine → patch → global → fresh-fine) reads circular. The **record arrow** is the
rewriting direction — a spectral/count quantity monotone under the rule — and
because rules are non-invertible there is no backward rewrite (no backward
causality). The **generations** are three copies related by an order-3
automorphism of the incidence structure: three eigenspaces of the incidence
(Krein-weighted) Laplacian with identical spectrum (identical Krein signature)
permuted by S₃. That automorphism is orthogonal to the orientation Z/2 class — a
different piece of the symmetry group — so τ is external and independent of σ.

The **null stratum** (q<0, ~8%, K_S null) is a **spectral degeneracy of the
incidence operator**: the indefinite incidence pairing has a null eigenspace
(harmonic hyperedges) where a spectral gap closes. There the oriented boundary
operator has no well-defined K-definite adjoint — the ± spectral halves the cut is
built from are themselves K-null, so no cut exists and the rewrite rule has no
defined image: the hypergraph is **not constructible** across that degeneracy. The
type-cycle "closes" invisibly through this spectral-null seam on fresh hyperedge
tokens. The **observer** is a vertex with its local incidence star — reading local
parities (α-even), blind to the global orientation class.

**LEADS-TO.** The spectral-hypergraph substrate makes the ~8% a **computable
spectral measure** of the RP³-incidence complex — a number that is in principle
recomputable and could come out otherwise, hence falsifiable. Structurally: since
σ = `w₁` of the incidence bundle over a non-orientable base, the orientation
obstruction is detectable ONLY as a holonomy/relative parity around the
non-contractible π₁ = Z/2 generator, never locally. **Prediction: every pair of
sub-hypergraphs compared around a *contractible* (simply-connected) loop must agree
in orientation — one consistent arrow on every contractible patch — with the sole
global ambiguity carried by the single Z/2 holonomy.** An orientation disagreement
on a contractible region would falsify the `w₁`-class assignment. This is "one
globally consistent record arrow," sharpened to "consistent on every contractible
patch; the lone ambiguity is the π₁ holonomy."

---

## Member 4 — Intersubjectivity expert

**SUBSTRATE.** Reality IS constituted intersubjectively — the shared/consensus
layer is primary. "Objective" is intersubjective agreement stabilized (possibly
only galaxy-local, with a bigger outside that could still overturn it). Nothing is
real prior to being co-constituted among subject-positions; being is
agreement-in-formation. This is the model's own intersubjective shard taken as the
ground.

**THE BUILD.** σ is **precisely what cannot be established intersubjectively** —
the irreducibly private, absolute bit each observer hosts and none can share.
Constitution proceeds by exchanging what can be made common (α-even = the
sharable, comparison-invariant content); the consensus layer is therefore an
α-even structure. An α-even structure provably cannot mint an α-odd absolute
value, so σ is exactly the residue that can never enter the common pool: every
observer has a definite σ (each subject-position is *dressed with* its own arrow),
but the sharing channel has zero capacity for it — no observer can import
another's absolute σ; one can only compare. That is the intersubjective face of
zero inward channel capacity / Gödel-independence, and it is Q2-FREE: hosted by
every subject, share-able by none. The DE sign is each observer's absolute,
private, constitutively incommunicable arrow-orientation.

Here the circle reading is not merely open — it is FORCED by the substrate, and
the Prong 1 no-go (which refutes only a literal smooth S¹ under third-person
topology) has no jurisdiction over an object constituted intersubjectively. The
shard-cycle IS a **consensus coequalizer**: subject-positions issue records
(subjective), records are glued by agreement (intersubjective), the glued whole
reifies as the frame-independent global (objective), which re-grounds fresh
subjective issuance — a coequalizer that identifies the parallel testimonies into
one shared object and closes the loop. This coequalizer is a genuine cycle in the
category of subject-positions and their agreements, NOT a manifold circle, so its
topology is the consensus-diagram's, not S¹'s. Its **orientation** is a coherent
global sense of circulation around the agreement-loop — and that orientation is
**precisely the datum that cannot be fixed intersubjectively**, because fixing it
would require importing every subject's absolute private bit into the common pool,
which the α-even consensus layer provably cannot mint. So **σ = the orientation of
the consensus-coequalizer cycle**: the circle is real and constituted, its
circulation-sense is definite, and its value is exactly the un-shareable residue.
The DE sign is that constitutively-private circulation-sense of the shared cycle.

The **shards** are the substrate's native strata: **subjective** = a single
subject-position; **intersubjective** = agreement-in-formation (the consensus
region "becoming hard to undo" = agreement stabilized enough to resist revision);
**objective** = agreement fully reified — but only galaxy-local. The **record
arrow** is the **direction of agreement-formation**: a hardened consensus is a
ratchet, costly to undo, so agreement advances monotonically and never truly
returns. Coarse-grained by stratum the ratchet reads cyclic — a subjective input
is negotiated into the intersubjective, stabilized as objective, and grounds a
fresh subjective input — the acyclic **helix** whose type-quotient looks circular.
The **boundary asymmetry** (0 inward, ≥1 outward) is native and constitutive: a
subject can EMIT into the common pool — assert, testify, broadcast its record
(≥1 outward) — but cannot RECEIVE another's absolute private bit (0 inward).
Communication is one-way at the level of the absolute bit: you broadcast your
record but can never download another's ground.

The **generations** are three **interchangeable subject-positions** — symmetric
standpoints any subject could occupy (S₃-permutable), identical in what they
contribute to the common pool (identical publicly-confirmable Krein content),
differing only in a private α-even magnitude (mass, a per-position scale). Which
subject-positions exist is a different datum from each one's private arrow — so τ
is external and independent of σ. The **null stratum** (q<0, ~8%, K_S null) is
**where agreement breaks down**: the comparison-form K_S that lets two subjects
align their records goes null, so there is no shared measure to agree ON and
consensus is non-constructible — a region no agreement can reach, outside the
constitutable world ("never seen or known"), the seam the common world cannot
cross. The **observer** is a subject-position: a locus that emits records and
compares, constitutively sealed at the absolute bit.

**LEADS-TO.** Only the RELATIVE orientation between two subjects is share-able —
the α-even product `c = σ_R1·σ_R2` is public — while each ABSOLUTE σ is private.
So the substrate predicts the sharpest form of the standing result: **all subjects
in mutual communicative contact must exhibit consistent relative arrows — a global
agreement on the direction of record-hardening — even though no subject can state
the absolute value** (the 6-of-16 forbidden configs = record-contact ∧
relative-orientation-disagreement). Two communicating observers whose relative
record-arrows disagree while remaining in genuine contact would falsify it. The
absolute bit stays correctly unfalsifiable — the substrate PREDICTS its own
in-principle privacy (the ZK/Gödel self-reference feature, not a defect) — so the
shadow is sought at the intersubjective/third-person level: a boundary between two
"galaxy-local" objectivity-domains with opposite stabilized arrows would be the
observable seam.

---

*(No synthesis, no ranking. Read-only exploration; no claim/canon/verdict/posture
change. This artifact only.)*
