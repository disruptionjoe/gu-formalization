---
artifact_type: exploration
status: exploration
created: 2026-07-09
title: "SG4 -- boundary-supply-ledger: filled candidate packet + executable classifier"
grade: "COMPUTED (exact Lie-algebra signature backbone + decision procedure). Reconstruction-tier. Internal. No source action built; no verdict/canon/posture change. A CLOSED label would be a promotion and pauses for Joe (never asserted here)."
depends_on:
  - lab/roadmap/boundary-supply-ledger-generation-count-source-action-2026-07-09.md
  - lab/roadmap/boundary-supply-ledger-intake-2026-07-09.md
  - explorations/big-swing-2026-07-03/BIG-SWING-1-source-action-definite-vertical-dirac-BLOCKED.md
  - canon/no-go-quaternionic-parity-generation-sector.md
scripts:
  - tests/big-swing/sg4_source_action_candidate_classifier.py
---

# SG4 -- the boundary-supply-ledger's named #1 next run: candidate packet + classifier

## The swing

Today's ledger row (`lab/roadmap/boundary-supply-ledger-generation-count-source-action-2026-07-09.md`)
names the single most useful next Progress run: **select one proposed source-action term, fill the
minimal candidate packet, and -- if it survives the hard guards -- add a small executable check that
classifies the candidate** as one of `{closed, blocked, target-import fail, eta-zero obstructed,
underdetermined, missing-carrier blocked}`. This goal implements that classifier and applies it.

## What was computed (`tests/big-swing/sg4_source_action_candidate_classifier.py`, exit 0)

**Backbone (real computation).** The `GL(4,R)`-invariant trace form on the tangent space of
`GL(4,R)/O(3,1)` (`~ Sym^2 R^4`, dim 10), `g(S,T) = tr(eta S eta T)`, has signature **(7,3,0)** --
**indefinite**. So there is no GU-native definite (Riemannian) invariant fiber metric, matching
BIG-SWING-1: definiteness must be imported.

**Classifier.** A decision procedure applies the ledger failure modes in order -- target-import fail
-> missing-carrier blocked -> eta-zero obstructed -> underdetermined -> closed (flag only). Each label
is reachable and discriminated (control panel in-script).

**Applied candidate (filled minimal packet).** The Stueckelberg-compensator stabilized RS/IG source
action `S_IG^susy` on `Y14 = Met(X4)`, with the definite vertical fiber Dirac over `GL(4,R)/O(3,1)`
as its integer-extraction carrier -- the object every 2026-07-03..07 route bottoms out on. The
classifier returns:

> **MISSING-CARRIER BLOCKED** -- needs a definite vertical fiber Dirac on `GL(4,R)/O(3,1)`, but the
> only invariant trace form is indefinite `(7,3)`; no GU-native definite metric exists and the packet
> names no non-native import for it.

## Verdict

The candidate is **MISSING-CARRIER BLOCKED** -- the honest current standing, consistent with
BIG-SWING-1 and the ledger. The deliverable is a **reusable classifier**: future source-action
candidates fill the packet and receive a taxonomy label with a computed reason. No source action is
built, no count forced; a `CLOSED` verdict (a promotion) is only ever *flagged*, never asserted, and
would pause for Joe. Generation count stays OPEN (located, not forced).
