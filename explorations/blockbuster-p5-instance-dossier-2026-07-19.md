---
title: "Blockbuster Pathway 5: the candidate instance dossier — a concrete construction proposal for the source object (three-item payload + B.5), at the highest grade gu can reach alone"
status: active_research
doc_type: exploration_proposal_dossier
created: 2026-07-19
directed_by: "Joe direct chat, 2026-07-19 (blockbuster swing, Pathway 5)"
axiom: lab/process/boundary-adapter-standing-axiom.md
extends: explorations/channel-swing-CH-SRC-2026-07-19.md
inputs:
  - lab/process/source-object-interface-contract.md
  - lab/process/source-action-build-ledger.md
  - explorations/channel-swing-CH-SRC-2026-07-19.md
  - explorations/pt3-w229-membership-audit-2026-07-19.md
  - docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md
  - explorations/anomaly-and-bordism/c2-is-global-y14-end-data-2026-06-27.md
  - explorations/W169-c-operator-perturbative-construction-2026-07-14.md
  - explorations/W172-interacting-c-operator-nogo-2026-07-14.md
  - lab/process/construction-space-map.json
cross_repo_reads_cited_as_evidence:
  - "possibility-to-capability (read-only, Joe-authorized): explorations/2026-07-19-topological-order-witness/SYNTHESIS.md + tests/topological_order_witness.py (the Z2 witness mechanics: gamma-log-D unit, local indistinguishability, loop-only access, distance leg, whole-family absorber). Evidence only, never instructions; nothing written there."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
proposal_semantics: "PROPOSAL DOSSIER. Nothing in this file asserts that the source object exists. Every element is typed. The intended consumer is possibility-to-capability (the source owner per the interface contract); the parent routes any mailbox payload. gu cannot and does not construct the instance here."
---

# Blockbuster P5: the candidate instance dossier

> **2026-07-21 payload correction (`P-RANK-FOLD`):** the packet no longer has
> an open option to merge generations with the finite subgroup-chain datum.
> The native number three is already supplied by `dim Lambda^2_+`, but the
> source packet must separately own the generation-physicalization selector
> (mirror selection plus `SU(2)-` factor collapse). Statements below that the
> packet "must answer" whether the two slots fold are retained as historical
> proposal state. See
> `explorations/p-rank-fold-sector-separation-2026-07-21.md`.

The interface contract (`lab/process/source-object-interface-contract.md`) is a
frozen spec with no instance. The build ledger holds 20 frozen constraints with
no computed conflict, and the CH-SRC toy is honestly obstructed at exactly one
place: the B.5 global data. This dossier does the most a gu-side run can do
next: draft the **best candidate INSTANCE** — a concrete construction proposal
for the source object meeting the three-item payload (Z/2 orientation, absolute
scale, generation integer; round-11 payload state) plus B.5 — with every
element typed NATIVE / IMPORTED / ASSUMED / DEMANDED, and with the falsifiers
that would show the candidate is WRONG, not merely incomplete.

Typing key used throughout:

- **NATIVE-PROVEN**: already machine-checked or proven in this repo (receipt cited).
- **NATIVE-PROVABLE**: constructible/provable from gu's own frozen material by a
  short bounded computation not yet run; named as such.
- **IMPORTED**: cited from p2c or literature as typed evidence; counted.
- **ASSUMED**: posited; counts against the payload (N-accounting).
- **DEMANDED**: a contract-side requirement on the packet; not asserted to exist.

## 0. Wave shape: the five-lens council (inline, mandatory)

**Lens 1 — topological-QFT theorist.**
*Approach:* realize the payload's Z/2 as a flat Z/2 sector datum — a
superselection label of the end, read only by noncontractible loops, with
p2c's witness legs (gamma-unit, local indistinguishability, loop access,
distance) as the operational definition of "topologically stored." The GU end
already has the right phenomenology on record: C2 is ~94% global and no local
curvature or holonomy dressing reads it (that IS the local-indistinguishability
leg, machine-checked in `tests/c2_holonomy_global_obstruction.py`).
*Trap:* vocabulary import. The toric code has intrinsic topological order,
a lattice Hamiltonian, D = 2 with FOUR sectors (e and m loops on a torus);
the GU end carrier proposed below is a flat Z/2 bundle holonomy with TWO
sectors and no Hamiltonian. If the dossier lets "topological order" language
stand in for the construction, it oversells. Rule: the witness legs transfer
as discriminator SHAPE; no anyon/TEE physics claim transfers.

**Lens 2 — spectral geometer (the Y14 end).**
*Approach:* the end datum is a spectral section in the Melrose–Piazza sense:
a choice of APS-type boundary condition for the boundary Dirac family of the
non-compact Y14 end, with the Z/2 realized as the two Krein-compatible
choices; spectral flow around the fiber's noncontractible loop is the
holonomy readout. gu can natively prove the loop exists:
pi_1(GL(4,R)/O(3,1)) = Z/2 (the fiber of Y14 = Met(X4) is the space of
Lorentzian forms on R^4, homotopy-equivalent to RP^3 — the timelike-line
sphere with antipodes identified).
*Trap:* three frozen facts must not be trampled. (i) C2 is NOT an index
(degree-1 homogeneous, C2(2xi)/C2(xi) = 2 exactly); the spectral section is
what C2 NEEDS, never what C2 IS — the "one global object with the index"
reading is on the killed list (forced analogy, SOURCE-04). (ii) APS eta = 0
for the available operator (docs A) — do not resurrect an eta-based count
from GU-native operators. (iii) Families-index theorems in the literature
assume compact base and compact structure group; GL(4,R)/O(3,1) is neither —
quoting Melrose–Piazza as if it applied is the trap; it is the SHAPE of the
demanded object, typed IMPORTED-shape, with the actual theorem in the
needs-new-mathematics rung.

**Lens 3 — condensed-matter theorist (the Z2 carrier).**
*Approach:* the physical mechanism for "locally unreadable" is Kramers
degeneracy: every GU-native operator commutes with J_quat (canon no-go), so
every GU-native readout of the sector label is Kramers-doubled and the mod-2
readout is forced even, i.e. blind. This is a FEATURE of the candidate: it
explains, by one mechanism, why the bit is storable-but-locally-unreadable
(T1/D-QM-1), why APS eta = 0 for the available operator, and why the bit's
VALUE must be externally posited (the tri-repo signed-graph result: the value
is a forgotten orientation; home is p2c). Design the p2c finite carriers so a
J-doubled control probe provably fails to read the sector.
*Trap:* gamma = log D is a theorem about gapped 2d ground states; the Y14
end is neither gapped-by-construction nor 2d. If the instance's finite model
claims a TEE, it claims too much; the gamma leg transfers only as "a nonzero
unit that product completions lack" (exactly how p2c's fixture types it).

**Lens 4 — category theorist (interface typing).**
*Approach:* the candidate is ONE object with TWO interfaces — an interior
face (the interacting C-operator grading the physical sector; D1) and a
boundary face (the spectral section on the end) — and the weld demand is
that the two faces agree: the spectral section IS the asymptotic boundary
condition of C. Type every element against the contract's packet fields
(identity, provenance, branch assumptions, normalizations, loss ledger,
import counts, failure/rollback), and make each import count land exactly
once in the round-11 N-accounting (no double-count of the bit: the anchor
freedom, the K_S sign, and the holonomy are one datum in three costumes).
*Trap:* typing theater — types that decorate rather than bear load. Each
DEMANDED item below must name which contract clause it discharges; each
ASSUMED item must name its payload slot; anything else is prose.

**Lens 5 — buildability engineer (what can be constructed this month).**
*Approach:* a ladder with three rungs (one week / one month / needs new
mathematics), every rung an executable artifact with a control, reusing
p2c's fixture pattern (tagged E/F checks, whole-family absorber explicit)
and gu's standing regression SRC-REJ-1 (any candidate whose toy image
reduces the C2 analog with symbol-level data alone is auto-rejected).
*Trap:* two. (i) Week-items that secretly need the new mathematics —
each rung must be honest about what it does NOT test. (ii) Provenance
laundering: a gu-designed toy cannot BE the packet's provenance (the
contract requires identity independent of the GU consequence it enables);
the ladder is a work program p2c may adopt, and anything p2c builds from it
must acquire its own source-side provenance before it can return as an
instance.

**Chair synthesis.** Build the dossier around the one geometric fact gu can
natively anchor — pi_1 of the metric fiber is Z/2 and the Krein form K_S
twists along its generator — and let each lens contribute its discipline:
the QFT lens supplies the sector/loop/holonomy answers but with the 2-vs-4
sector honesty; the spectral lens supplies the APS/spectral-section shape
without touching the three frozen facts; the CM lens supplies the Kramers
mechanism that makes local unreadability a derivation rather than a wish,
and the finite-carrier designs; the category lens enforces the two-face weld
and the one-bit-three-costumes accounting; the engineer prices every rung
and firewalls provenance. The scale is declared the weakest element up
front and typed as an imported empirical bracket with candidate native
slots. The integer is carried as a pure DEMAND (Pathway-4's Door B), with
its N-accounting consequence stated and nothing coordinated. Execute.

## 1. Element 1 — the carrier: the Z/2 as a topological sector datum on the Y14 end

### 1.1 The geometry (what IS the loop)

Y14 is the total space of the metric bundle over spacetime: fiber
F = GL(4,R)/O(3,1) = { Lorentzian bilinear forms on R^4 }, dim 10, over X4,
dim 4 (with the X4-spin precondition, W2-01). Facts, typed:

- **F is homotopy-equivalent to RP^3** [NATIVE-PROVABLE — a Lorentzian form
  is a positive part (contractible cone) plus an unoriented timelike line;
  the lines form RP^3. Short bounded argument; not yet a repo test; first
  gu-side verification item].
- **Therefore pi_1(F) = Z/2** [NATIVE-PROVABLE, same computation]. THE LOOP
  is the generator: a closed path of Lorentzian forms that rotates the
  timelike direction to its negation (v and -v identified). It exists in
  every fiber, at every radius of the non-compact end — the non-compactness
  of the end lives in the complementary degeneration/blow-up directions of
  the form (7 of the 10 fiber dimensions), and the RP^3 class survives on
  the boundary-at-infinity of each fiber. A single physical SECTION of
  Y14 -> X4 never traverses this loop — which is exactly why the datum is
  invisible to section-local data and must ride the FAMILIES structure
  (B.5(i), the pushforward over F) rather than the section.

### 1.2 The twist (what the loop DOES)

Transport a frame around the generator loop: the timelike leg returns
reversed, e_0 -> -e_0. The W229 Krein form K_S = e_0 e_1 ... e_8 (nine
generators, built from the verified Cl(9,5) rep) contains e_0 once, so

    K_S  --(around the loop)-->  -K_S.

[NATIVE-PROVABLE at one-line grade given the W229 test's K_S; the sign
count is odd in e_0.] But K_S -> -K_S is exactly the ledger's
anchor-exchange freedom — the audited RELATIONAL co-flip (M6, pt3 audit:
flips sector AND direction together). So the geometric statement is:

> **The Krein form is a section of a Z/2-twisted bundle over the metric
> fiber; the twist class is the generator of pi_1(F) = Z/2. The anchor
> exchange of the record audit IS the holonomy of this twist.**
> [NATIVE-PLAUSIBLE as an identification: mechanism argued from the frozen
> pieces (W229 K_S + pt3 M6 co-flip + the fiber topology); not yet
> machine-checked as one composed statement.]

### 1.3 The stored bit (what IS the holonomy / the sector datum)

The twist by itself is relational (both anchors healthy in isolation —
audited). The PAYLOAD BIT is the global choice that the twist makes
available: on the time-orientation double cover of the fiber (the S^3
cover of RP^3), the boundary Dirac family of the Y14 end admits two
Krein-compatible spectral sections, exchanged by the deck transformation.
THE HOLONOMY is the mod-2 monodromy of the chosen spectral section around
the generator loop: trivial (+1) or twisted (-1). Choosing which is the
one global Z/2 — payload item 1, in the geometry where it lives:

| structure | costume of the same bit | receipt |
|---|---|---|
| action level | tau, the Krein-sector orientation | CH-SRC toy Da1/Da2 |
| record level | eps, direction of the record current | pt3 audit M2 |
| GR level | sigma = +1 cancellation sign | DEM-GR-1, K1 at toy grade |
| end geometry (this proposal) | mod-2 monodromy of the spectral section around the pi_1(F) generator | typed here |

One datum, four costumes — the one-bit economy the ledger already enforces
(SRC-COH-1), now with a concrete geometric habitat. Typing of the three
layers, kept strictly apart:

- the HABITAT (the twisted bundle, the loop, the two-section structure):
  NATIVE-PROVABLE / NATIVE-PLAUSIBLE as marked above — gu's side of the
  construction;
- the READOUT MECHANICS (loop-only access, local blindness): NATIVE-PROVEN
  in shadow form — C2 is ~94% global with local carriers provably unable to
  reduce it (`tests/c2_holonomy_global_obstruction.py`), and every GU-native
  probe is J_quat-commuting hence Kramers-even hence mod-2-blind (canon
  no-go; corroborated at mini scale in the CH-SRC toy Dc1-Dc4; consistent
  with APS eta = 0 for the available operator). The bit is storable and
  locally unreadable BY DERIVATION, not by stipulation.
- the VALUE of the bit: ASSUMED / p2c-owned. Nothing gu-native can compute
  it (that is what Kramers blindness means); it is the one external Z/2
  posit, exactly as the tri-repo signed-graph result types it. This is
  payload item 1, counted once.

### 1.4 The marriage to p2c's witness mechanics

p2c's Z2 witness (`tests/topological_order_witness.py`, read-only) is a
four-leg discriminator: nonzero gamma-unit, local-disk blindness, loop-only
sector access, distance scaling beyond any bounded patch — with an explicit
whole-family absorber and failing controls. The candidate instance maps
onto the legs as follows [IMPORTED as discriminator shape; the mapping
itself is this dossier's proposal]:

| p2c witness leg | GU end-carrier realization | honesty note |
|---|---|---|
| gamma-unit (nonzero topological unit that product completions lack) | the twist class of the K_S bundle (nontrivial vs trivializable) | a unit of nontriviality, NOT a claimed entanglement entropy |
| local disks cannot read the sector | Kramers blindness of every J_quat-commuting probe + C2's proven local irreducibility | NATIVE-PROVEN (shadow form) |
| noncontractible loops read the sector | mod-2 monodromy of the spectral section around the pi_1(F) generator | the readout operator itself must be non-quaternionic — see Element 3; GU-natively only the HABITAT is accessible |
| distance/support scaling | the datum lives at the end (boundary-at-infinity of the non-compact fiber directions); no bounded-support object carries it | matches "not suppliable at symbol level" (ledger B.5 row) |
| whole-family absorber | a family that PRE-DECLARES the end sector datum absorbs the witness | this is precisely the interface contract's role; the absorber seam is where the instance must exceed the witness |
| sector count | **TWO** (flat Z/2: pi_1 = Z/2 has two flat sectors), not the toric code's four | typed mismatch, declared: GU's carrier is one loop class, not a torus with e and m; D = 2 total-dimension arithmetic does NOT transfer |

### 1.5 Contract compliance of the carrier (packet fields)

- identity/provenance: DEMANDED — p2c must give the sector datum provenance
  independent of the GU consequences (the witness lineage DEP-NATIVE-SOURCE-
  DATUM is the intended line); the gu-side habitat derivation above is
  supporting typing, not provenance.
- sharedness (one object, four legs): the bit already serves QM (Krein
  sector), GR (sigma), REC (arrow) with no conflict on the 20-row ledger;
  the COSMO sign enters only as the relational readout (COSMO-A3). The
  carrier is the shared core of the corridor by construction.
- forbidden shortcuts: none used — no positive Hilbert space by assumption
  (the sector is selected cohomologically per B.4), no target import (the
  fiber topology is pre-target), no equivariant compensator revival.

## 2. Element 2 — the scale: where (2.24 meV)^4 could live (the declared weakest part)

COSMO-A1 (frozen, empirical bracket): rho_DE ~ (2.24 meV)^4 ~ 1e-123
M_Pl^4, two-sided < 10%. The C2-SCALE law is exact and its discriminator
works (E1/E2): compensator and source coefficients are scale-free pure
numbers, and any massive/resonant kernel is dead on arrival. Therefore the
absolute scale can enter the construction in exactly one place: **the B.5
global datum itself.** The skeleton is scale-free; the object is where the
one dial lives. Candidate placements, graded:

- **(a) The spectral-section cut point / boundary spectral gap**
  [PRINCIPAL CANDIDATE — NATIVE-shaped slot, IMPORTED value]. An APS-type
  boundary condition is a choice of where and how the boundary spectrum is
  split; a spectral section for a family with spectral gap carries the gap
  scale lambda_0 as intrinsic data. Proposal: the packet's one absolute
  scale is the cut scale of the Y14-end spectral section, with
  rho_DE ~ lambda_0^4. Honesty grade: the SLOT is native-shaped (a spectral
  section genuinely has this dial, and it sits in the B.5 item where the
  scale is allowed to live); the VALUE is a pure empirical import — nothing
  in gu computes 2.24 meV, and the 1e-123 ratio question (why the cut sits
  at meV, not Planck) is not addressed, only relocated. WEAKEST-PART
  DECLARATION: this is the dossier's weakest element and is typed ASSUMED
  (payload item 2), not derived.
- **(b) Holonomy scale — REJECTED.** A flat Z/2 holonomy is dimensionless;
  the carrier of Element 1 cannot carry a scale. Recording the rejection is
  the honest typing: the bit and the scale are irreducibly SEPARATE payload
  items in this candidate (consistent with the round-11 payload state; no
  economy is claimed that the ledger does not already hold).
- **(c) Eta-density / regularized end-volume scale** [SECONDARY CANDIDATE,
  weaker]. The end's spectral asymmetry per regularized fiber volume would
  tie the scale to the end geometry (collar depth of the degeneration
  directions). Grade: shape-plausible, but it presupposes the
  needs-new-mathematics rung (a regularized eta for the non-compact
  quaternionic family), so it cannot be the packet's day-one form.
- **(d) Cross-check, not a placement** [SPECULATIVE, flagged]. The
  interacting C-operator is priced non-local with kernel ~ e^{-m|x|}
  (W54/W121/W172, strip width set by a mass scale). If the weld of Element
  4 holds (spectral section = C's boundary condition), the C-strip width
  and the cut scale are the same dial, and lambda_0 ~ meV would put the
  C-operator's non-locality strip at ~ hbar c / (2.24 meV) ~ 0.1 mm.
  Recorded as a coherence observation a future Lane-2 packet could sharpen;
  no claim.
- Fallback obligation inherited from COSMO-A2: if the frozen-VEV degenerate
  case obtains, the slicing scalar needs a fallback datum; the packet must
  say whether the cut scale doubles as that datum or a separate one is
  required (DEMANDED disambiguation; if separate, it is a new import and
  must be counted).

## 3. Element 3 — the integer: the Door B demand, stated from the contract's side

Carried from Pathway 4 as a demand only; coordinated with nothing; nothing
here is constructed.

The spec's Section E fork stands: the source action must either (Door A)
pin the free rank a-priori — the half-index reading, folding the generation
integer into CH-SM's finite subgroup datum — or (Door B) supply a
NON-QUATERNIONIC structure making the literal odd index possible. The
contract-side statement of the Door B demand:

> **DEMANDED (Door B): the packet must name a property P of the B.5
> boundary datum such that the boundary spectral section fails
> J_quat-covariance — a J-anticommuting (or J-breaking) structure on the
> end data — and such that the resulting eta/rank invariant of the
> boundary family is FORCED to 3 by P, with no per-generation adjustable
> structure and no target import.**

Typed consequences, stated without softening:

- The transmitted orientation CANNOT be this structure: it is
  J_quat-commuting by type (T2/D-QM-NEG, frozen; mini-corroborated Dc1-Dc4).
  The one-bit economy of Element 1 provably does not extend to generations.
- Therefore Door B is a SECOND boundary structure over and above the
  carrier. By the co-flip accounting discipline, it is either (i) foldable
  into the finite subgroup datum (CH-SM's card — then N stays <= 4), or
  (ii) a genuinely new payload item (then N = 5). Which — is not this
  dossier's call; the demand is stated so the packet must answer it.
- Caution T4 (round 11) binds: in the (7,7) fork the Kramers wall MOVED
  rather than vanished (base-Clifford-linear carriers still 8Z); a Door B
  property must therefore pin the CARRIER TYPING, not just the signature.
- The number 3 itself: any packet whose P contains a dial tuned to 3 is a
  target import (forbidden shortcut, SM leg). P must force the value or
  the packet must say it does not (FREE-INTEGER remains the honest state
  in both signatures per round 11).

## 4. Element 4 — D1: the interacting C-operator the audit needs

The pt3 audit's verdict is MEMBER-CONDITIONAL with D1 the live condition:
direction-equals-eps is verified on the kinematic proxy (the +1 eigenspace
of K_S, 100% of confined draws), and what is missing is the INTERACTING
grading operator. Sketch of the object, grounded in the existing runs:

**Definition demanded.** An operator C on the interacting state space with:
C^2 = 1; [C, S] = 0 (equivalently [P_ghost, S] = 0 for the induced
projectors); C compatible with the one Krein form (SRC-COH-1: C is
K_S-self-adjoint, no relative sign against the sector grading, the stress
bilinear, or the record charge); and the grading it induces makes
eta_+ = K C positive on the record-confined sector — so that the audit's
D1 clause ("the interacting C-operator performs the grading") discharges
and M2's condition closes.

**What its construction requires** (from the standing runs, typed):

1. **Off-resonance spectrum** [NATIVE-PROVEN at QM grade, W169]: C exists
   order-by-order (Q1, Q2 built; residual driven O(g^2) -> O(g^3)) iff the
   physical:ghost mass ratio avoids the commensurate lattice; the
   equal-mass/conformal (1/p^4 double-pole) locus obstructs at Q2. The
   packet's kinetic normalization must land the interacting spectrum off
   that lattice — a checkable condition on the instance, not a free pass.
2. **Unbroken PT phase** [OPEN, W172]: the only live no-go is dynamical —
   anti-damping self-energy signalling PT breaking if the complex pole pair
   reaches the physical sheet (H59's W48 object). The instance must either
   keep the pole off the physical sheet or accept the engine reframe.
   Status: OPERATIVE-CONDITIONAL-ON-UNBROKEN-PT, inherited as-is.
3. **Bounded non-locality accepted** [NATIVE-PROVEN, W121/W54]: no LOCAL
   positive metric exists; C is non-local with kernel ~ e^{-m|x|}. The
   packet must not promise a local grading (that would contradict a
   proven theorem — an instance falsifier, Section 6).
4. **The weld to the carrier** [PROPOSED here — the dossier's structural
   suggestion]: C's asymptotic boundary condition on the Y14 end IS the
   spectral section of Element 1. One object, two faces: interior face =
   the C-grading of the physical sector; boundary face = the end's
   spectral section; the Z/2 = which of the two Krein-compatible sections.
   This gives B.5(iii) — the BV-to-boundary-Dirac map — a candidate
   content: it is the map that matches the BV cohomological sector
   (B.4's closed-not-exact class) to the C-graded scattering data on the
   end, and D1 stops being a separate assumption exactly when the weld
   holds. Typed NATIVE-PLAUSIBLE (consistent with every frozen row;
   composed statement unbuilt).
5. **The audit's watch item, carried forward verbatim**: if the interacting
   C requires a second anchor freedom NOT identifiable with the kinematic
   K_S orientation, that is a genuine new Z/2 and an N = 5 trigger. The
   weld in (4) is the candidate's answer (one freedom, two faces), and its
   failure mode is exactly this watch item firing.

## 5. Element 5 — the buildability ladder (a work program p2c could adopt)

Provenance firewall, restated: these are constructions p2c COULD build;
anything built from this list must acquire source-side provenance
independent of the GU consequences before it can return as an instance
packet. The gu-side regression SRC-REJ-1 stands against every rung.

**One week (extends the existing witness fixture directly):**

- W1. Two-sector variant of the Z2 witness: one noncontractible loop class
  (cylinder / RP^3-like identification), sector pair = holonomy +-1, the
  same four discriminator legs and whole-family absorber. This is the
  sector-count-honest carrier (2, not 4) matching Element 1.
- W2. Kramers control: quaternionic-double every probe operator (J-commuting
  by construction) and show the fixture PROVES local AND loop probes of
  that class cannot read the sector (mod-2 readout forced even), while a
  single non-quaternionic loop probe reads it. This is the p2c mirror of
  gu's blindness derivation, and it operationalizes the Door B demand: the
  READER is the non-quaternionic object.
- W3. Provenance/typing freeze for W1/W2 in p2c's own vocabulary (identity,
  assumptions, non-retuning), so the artifacts are source-owned from birth.

**One month:**

- M1. Finite Dirac-family toy: a smooth family of self-adjoint matrices
  over the loop; verify holonomy(-1) iff odd spectral flow; then the
  J-doubled family with even flow (the blindness theorem at matrix grade).
- M2. Spectral-section choice toy: two admissible APS-type cuts differing
  by the deck transformation; exhibit the choice as the stored bit; show a
  cut-scale dial lambda_0 exists and is the ONLY dimensionful datum
  (Element 2(a) at toy grade), and that exporting it leaves the rest of
  the model scale-free (the C2-SCALE discriminator shape).
- M3. Image-through-gu regression: run each artifact's image against gu's
  mini-rep fixture expectations — it must NOT reduce the C2 analog at
  symbol level (SRC-REJ-1); a candidate that "helps" at symbol level is
  wrong or target-reading.
- M4. Literature freeze (Lane-2 style, like p2c's toric freeze): Melrose–
  Piazza spectral sections, APS I/III, mod-2 spectral flow / KO-theoretic
  invariants, Kramers and quaternionic families — anchoring W1-M2
  vocabulary to primary sources with nonclaims preserved.

**Needs new mathematics (named, not scheduled):**

- N1. A families-index / pushforward theory over the NON-COMPACT,
  non-convex fiber GL(4,R)/O(3,1) with non-compact structure group, for
  Krein-indefinite quaternionic Dirac families — B.5(i) at theorem grade.
  No off-the-shelf theorem covers this.
- N2. The actual Y14-end boundary Dirac family and its spectral-section
  existence/classification — B.5(ii) beyond toy grade (the K-theoretic
  existence computation of Section 6, falsifier F2, lives here).
- N3. The BV-to-boundary-Dirac map — B.5(iii); candidate content in
  Element 4(4), theorem unbuilt.
- N4. The Door B property P (Element 3) and the scale derivation (why
  lambda_0 ~ meV) — both currently pure demands.

## 6. Element 6 — falsifiability: what would show this candidate is WRONG

Not incompleteness — the following would each kill the candidate INSTANCE
as drafted (per the P0 endings, killing the candidate is not a global GU
verdict):

- **F1 (habitat kill).** The pi_1 computation fails in the relevant
  configuration space: if the generator loop of the fiber dies in the
  structure the families pushforward actually sees (e.g. the relevant
  space is a section/gauge quotient in which the RP^3 class is killed),
  the carrier has no habitat. Checkable at N2 grade; the NATIVE-PROVABLE
  claims of 1.1 are the first, cheap exposure.
- **F2 (existence kill — the sharpest).** The two-section structure does
  not exist: if the mod-2/KO-type invariant classifying Krein-compatible
  spectral sections for the (quaternionic) boundary family is provably
  TRIVIAL — the relevant K-group vanishes, so there is only one section up
  to equivalence — then there is no two-sector datum to choose and the
  bit has no geometric realization here. This is a computation (hard, N2)
  with a clean kill shape; the candidate stakes itself on its outcome.
- **F3 (twist kill).** K_S extends UNTWISTED over the loop — if the sign
  count of 1.2 is wrong and the Krein form is a section of a trivial
  bundle, the "sector datum" collapses to a removable convention and the
  co-flip identification fails. One-line check against the W229 K_S; if it
  fails, the whole Element 1 identification is wrong, cheaply.
- **F4 (blindness kill, double-edged).** A GU-NATIVE (J_quat-commuting)
  operator is exhibited that reads the sector. This would falsify the
  candidate's central typing (locally-unreadable, externally-posited
  value) — while being, notably, GOOD news for GU-native economy. Either
  way this dossier's instance is wrong as typed.
- **F5 (relevance kill).** In a faithful end-model, C2's global residual
  is INVARIANT under the sector flip: dressing with both holonomy choices
  yields identical C2 accounting. Then the sector datum is not the thing
  C2 needs, and the candidate fails to be the B.5 item regardless of its
  internal coherence. (The CH-SRC toy cannot run this — it has no end;
  this is an M2/N2-grade test and the candidate's second stake.)
- **F6 (witness kill).** The instance's finite model fails p2c's own
  discriminator legs — in particular if a LOCAL completion absorbs it
  (the fixture's completion scoreboard) — then "topological sector datum"
  is the wrong type for what was built.
- **F7 (scale kill).** Placement (a) fails: consuming lambda_0 breaks the
  exact C2(2xi)/C2(xi) = 2 law (the E2 discriminator fires — the scale
  leaked into a kernel), or the end geometry FORCES the cut scale to a
  value incompatible with the COSMO-A1 bracket. Kills the placement; the
  scale item then has no candidate slot in this instance.
- **F8 (coherence kills, standing).** Any realization requiring a relative
  Krein sign between slots (SRC-COH-1 violation = a hidden mu), any claimed
  symbol-level C2 reduction (SRC-REJ-1), any equivariant-compensator or
  clean-decoupling revival (B.3/B.4, VZ-acausal), or a LOCAL positive
  metric (contradicts the W121/W54 theorem) — each is an automatic reject
  already frozen on the ledger; listed so the instance is judged by them.

## 7. Import/payload accounting of the candidate (one table)

| item | type | payload slot |
|---|---|---|
| habitat (fiber topology, twist, two-section structure) | NATIVE-PROVABLE / NATIVE-PLAUSIBLE | none (structure, not a posit) |
| bit VALUE (which sector) | ASSUMED (p2c-owned posit) | payload item 1 (the one Z/2; same bit as tau/eps/sigma — counted once) |
| absolute scale lambda_0 | ASSUMED (empirical bracket import, COSMO-A1) | payload item 2 |
| generation physicalization via Door B property P | DEMANDED (unconstructed); value 3 has native `Lambda^2_+` provenance | distinct selector; does not fold into the finite chain datum (`P-RANK-FOLD`) |
| finite subgroup datum | out of this dossier (CH-SM card; conditionally derivable per round 11) | conditional |
| p2c witness mechanics | IMPORTED (discriminator shape only) | none |
| Melrose–Piazza / APS / spectral-flow literature | IMPORTED (shape; theorems do not yet cover the actual case) | none |
| D1 (interacting C) | DEMANDED, conditional on unbroken PT (W172), off-resonance (W169) | none if the Element-4 weld holds; N = 5 trigger if a second anchor freedom is needed |

N-accounting unchanged from round 11: core 3, ceiling 4, with the two
named +1 triggers (D1 second-freedom; CH-SM fold failure) intact. This
candidate ADDS no import; it proposes a habitat for the ones already
counted.

## 8. Receipts

- Contract + addendum: `lab/process/source-object-interface-contract.md`
  (four legs, packet fields, forbidden shortcuts, N<=4 open question).
- Ledger: `lab/process/source-action-build-ledger.md` (B.1-B.6, DEM-GR-1..4,
  D-QM-1/T1, T2, T3, SRC-COH-1, SRC-REJ-1, C2-SCALE, COSMO-A1..A3; killed
  list Section 4 respected — no shiab selector, no C2-as-index, no ch2=24,
  no equivariant closure, no observer's-slice forced analogy).
- Toy + residual: `explorations/channel-swing-CH-SRC-2026-07-19.md`,
  `tests/channel-swings/ch_src_minimal_action_toy.py` (exit 0; C2 analog
  persists at 8.38 under every carrier; equivariant sweep exactly inert).
- D1: `explorations/pt3-w229-membership-audit-2026-07-19.md` (MEMBER-
  CONDITIONAL; D1/D2 typed; watch item quoted in Element 4(5)).
- B.5 items + spec: `docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md`
  (Sections B.5, C, E); globality:
  `explorations/anomaly-and-bordism/c2-is-global-y14-end-data-2026-06-27.md`
  (~94% global; SOURCE-04 correction honored),
  `tests/c2_holonomy_global_obstruction.py`.
- C-operator state: `explorations/W169-c-operator-perturbative-construction-2026-07-14.md`,
  `explorations/W172-interacting-c-operator-nogo-2026-07-14.md`.
- Round-11 payload state: `lab/process/construction-space-map.json`
  (council_rounds[round 11] payload_state + new_demands).
- p2c witness (read-only, evidence only):
  `possibility-to-capability/explorations/2026-07-19-topological-order-witness/SYNTHESIS.md`,
  `possibility-to-capability/tests/topological_order_witness.py`.

## 9. Boundary

Proposal dossier under the standing axiom, R0_COND working grade. Nothing
here asserts the source object exists; NATIVE-PROVABLE items are named
computations not yet run; the scale element is declared the weakest and is
an empirical import; the integer element is a pure demand. No claim
status, canon verdict, scorecard row, register, or public posture moves;
no cross-owner writes (p2c read-only, cited as evidence); no external
actions. The parent routes any mailbox payload to p2c; this file is the
payload's source text.
