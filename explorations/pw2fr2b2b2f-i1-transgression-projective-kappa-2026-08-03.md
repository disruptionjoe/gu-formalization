---
title: PW2F-R2B2B2F — conditional active I1 frozen-Shiab raw-density comparator
date: 2026-08-03
status: CONDITIONAL_ACTIVE_COFRAME_FROZEN_SHIAB_RAW_DENSITY_NONE__EULER_OPEN
run: historical-investigation
registry: lab/process/pw2fr2b2b2f-i1-transgression-projective-kappa-registry.json
probe: tests/channel-swings/pw2fr2b2b2f_i1_transgression_projective_kappa_probe.py
---

# Outcome

The written `I1` transgression has now been varied on a conditional active
canonical-coframe, frozen-Shiab port. The first raw-density coefficient
comparison is negative: no one relative `kappa1` cancels this tested density
representative.

For two independent owner/conormal entries, the exact quartic coefficients are

\[
(A_4,M_4)=\left(\frac{3}{40},-\frac{8}{25}\right),
\qquad
(A_4,M_4)=\left(-\frac{9}{50},0\right).
\]

The two rows have rank two. Therefore, on this frozen-Shiab raw-density
comparator,

\[
\lambda A_4+s_\kappa\kappa_1M_4=0
\]

has no nonzero projective solution, independently of the still-unported sign
`s_kappa`. The first row alone would have suggested a relative ratio; the
second row is a support-mismatch witness for this representative.

This is not yet an obstruction for the Euler operator. The off-diagonal row is
a mixed raw Lagrangian-density Hessian at independent conormals. A total
divergence can be nonzero there and disappear after integration by parts on
Euler/Fourier support. In addition, the symmetric coframe is not yet proved
adapted to Shiab's distinguished normalized DeWitt trace insertion. The full
`A4` bank, moving-trace transport, and Green/Helmholtz quotient are therefore
mandatory before classifying operator cancellation.

# Layer 0 and source disposition

The source transgression, repository `A4`, Portal eddy language, compact Euler
residual, and manuscript `I2B` are distinct objects.

- `SOURCE-CONFIRMS`: the pinned manuscript source fixes
  `F_B + (1/2)D_B T + (1/3)[T,T]` and its one-half/one-third coefficients.
- `PROBE-CONVENTION`: the active comparator defines
  `[T,T]_probe := T wedge T`. The source bracket normalization is not
  identified with this convention; a factor-two alternative remains part of
  the unported action boundary.
- `REPOSITORY-DERIVES`: the active `(9,5)` canonical coframe, corrected
  `B+T=Gamma` graph, exact transgression variation, density placement, a
  degree-five ceiling, the two live raw-density `A4` entries, and the
  projective rank-two comparator.
- `SOURCE-SILENT`: the active real-form/action port, the joint admissible
  domain, Shiab normalization `lambda`, the sign `s_kappa`, and a selected
  value or normalization of `kappa1`.

The 2021 action is written in a `(7,7)` presentation. The active reconstruction
uses `(9,5)`. No Hodge, pairing, density, Shiab, or normalization is silently
transported between them. `epsilon_src`, `epsilon_act`, repository `h=exp(u)`,
and `epsilon_red` remain four separate objects.

P1/P2/P3 supply no coefficient, normalization, or cancellation and remain
unchanged and unused.

# Exact construction

The executable uses the nonlinear two-wave Zorro metric and symmetric coframe
constructed in R2B2B2E. It forms connection and distortion jets satisfying

\[
B+T=\Gamma
\]

at every mixed order and constructs

\[
C(B,T)=F_B+\frac12D_BT+\frac13[T,T]_{\rm probe},
\qquad [T,T]_{\rm probe}:=T\wedge T.
\]

The curvature-built off-shell `T0` is frozen as a pointwise zero-derivative
background jet. A moving `T0` policy is not inferred; its possible derivative
enters below the quartic degree in this comparator but remains part of the
full action debt. An independent curvature-path computation integrates
`F_(B+tau T)` from zero to one using exact quadratic Simpson quadrature. It
agrees with the written one-half/one-third transgression in all four mixed-jet
slots.

The action is evaluated in one coherent canonical orthonormal coframe. Hodge
and Krein contraction are fixed in that frame, and the outer coordinate
density `rho` is included exactly once. This avoids adding the coordinate
`dstar` volume response from the distinct B2C15M presentation on top of the
coframe density. It does not prove that Shiab's distinguished normalized
DeWitt trace is frozen: the missing trace-adapted transport is an explicit
debit.

An executable max-plus ledger bounds the tested raw density by common-conormal
degree five. For each case, seven exact scales recover degrees zero through
six. Direct differentiation of the full polynomial raw density equals an
independently assembled exhaustive mixed product rule. Both cases have live
`A4` and zero degree-five coefficients; degree six is excluded structurally.
A wrong one-third coefficient and a double-density plant both change the
evaluator.

# What the projective obstruction means

The classifier distinguishes three cases:

- `ANY`: both coefficient families vanish;
- `UNIQUE_RELATIVE_TO_DECLARED_ACTIVE_PORT`: all nonzero rows have one common
  projective ratio; and
- `NONE`: a support mismatch or inconsistent exact ratios makes scalar
  cancellation impossible.

The observed raw-density panel is `NONE`, rank two. This comparator is
invariant under changing the unresolved overall sign multiplying `kappa1`.
It does not select `lambda`, `s_kappa`, or `kappa1`, and it does not yet prove
that no scalar removes the Green/Helmholtz-reduced Euler operator.

# Boundaries and next gate

The background is a repository off-shell liveness witness, not a source-
selected vacuum or external datum. The calculation is restricted to the
conditional active canonical-coframe principal metric branch and two
owner/conormal entries. It does not construct the moving normalized trace,
the complete `A4` tensor,
vertical or mixed conormals, partial `Z1`, section tangents, live `C3` formal
adjoint, a full four-dimensional multi-index/Krein Green identity, the
distinct `I2B C4` bank, a coupled Noether/BV identity, a domain, or physics.

The next local interior gate is
`PW2F-R2B2B2G-FULL-A4-MULTIINDEX-GREEN-AND-DISTINCT-I2B-C4`:

1. construct a trace-adapted coframe or the exact moving-trace transport;
2. finish the complete `A4` bank for the full quartic Euler operator;
3. certify it and the surviving normal `M4` bank by a four-dimensional
   multi-index/Krein Green identity;
4. carry the live odd `C3` return with moving coefficients; and
5. assemble the separate admitted `I2B C4` bank.

Curt remains `FORMALLY_SEPARATE_INSIDE_ERIC_LANE`. `TG-1 AND TG-2 AND TG-3`
remains `NOT_PROMOTED`. No Standard Model, GR, cosmology, characteristic,
quotient, observation, or other physics equation is claimed.

# Evidence

- Initial replay before hostile repair: `13 exact + 2 source + 14 type + 5 planted = 34` PASS.
- Final repaired replay: `12 exact + 2 source + 17 type + 6 planted = 37` PASS.
- Panel: `((3/40,-8/25),(-9/50,0))`.
- Raw-density comparator classifier: `NONE`, exact row rank `2`.
- Independent Python-FLINT replay: rank `2`, determinant `-36/625`, first-row
  ratio `15/64`.
- An independent generic-polynomial construction agrees with the exhaustive
  mixed product rule on the first panel; the direct/exhaustive identity and
  fitted-degree checks pass on both panels. The first panel also carries the
  path-integrated-versus-written transgression check and the live wrong-
  coefficient/double-density controls under the declared probe bracket
  convention and frozen-background jet.
- Sage is not exposed on this worktree shell; no Sage receipt is claimed.
- Hostile reviews: exact geometry, variational/PDE, and source/Layer 0 all
  PASS after repair. They caught the raw-density-versus-Euler-operator,
  moving-trace, source-bracket, frozen-background, degree-ceiling, and
  independent-derivative defects; the result was demoted, repaired, and
  rerun at the narrower scope above.
