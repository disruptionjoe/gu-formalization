---
title: "K77 Wave 2 advance: a conditional draft-9.16 rival and the exact source-sign obstruction"
status: active_research
doc_type: exploration
created: 2026-08-04
lane: 1
gate: RENDEZVOUS-ACTION-CURRENT-RIESZ-SUPERIG-WARD
result: "PARTIAL__CONDITIONAL_TOTAL_GRADED_D916_RIVAL_BUILT__SOURCE_SIGN_IDENTIFICATION_FULL_CONNECTION_VARIATION_AND_FULL_H_DESCENT_OPEN"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# K77 Wave 2: the actual-carrier draft-9.16 attempt

## Result first

This swing built a mathematically coherent real-`Cl(7,7)` rival for the
four-field operator displayed in draft equation 9.16.  It also found the
smallest exact reason that the rival cannot yet be called the source-faithful
actual operator.

The useful construction is

\[
D_{916}(A,\epsilon)=
\begin{pmatrix}
\Phi_\epsilon d_A^{(1)} & d_A^{(0)}\\
-(d_A^{(0)})^\times & 0
\end{pmatrix}
\]

on `Omega1(S) plus Omega0(S)`, with the exact real 128-spinor K77 carrier,
degree-sensitive Hodge signs, the split spinor pairing, a moving Clifford
orbit, and the source-preferred southeast-zero branch.  The auxiliary grading

```text
G = (-1)^form J
```

makes all three principal blocks odd and reproduces the six derivative-cell
locations of equation 9.16 after a one-form sign relabeling.

That last clause is decisive.  Rendered draft section 11.2 explicitly places
`zeta+/-` and `nu+/-` in the corresponding ambient half-spinor bundles.  The
source glyphs therefore mean ambient `J`, not `G`.  With those source labels,
the selected gamma-contraction `Phi d` flips `J`, whereas exterior `d` and its
adjoint preserve `J`.  The equation-9.16 incidence requires the first two
classes to have the same parity under either natural uniform convention for
the barred rows.  They do not.

The exact probe therefore returns:

```text
8 source + 23 type + 39 exact + 9 planted = 79 PASS
```

and the honest status remains `PARTIAL`.  This is not a no-go for equation
9.16.  It is a typed construction fork among:

1. a different Shiab contraction with the required ambient parity;
2. an explicit degree-dependent duality/reality map;
3. a correction or reinterpretation of the draft's field subscripts; or
4. the present total-graded operator as an acknowledged rival rather than a
   source-identical reconstruction.

## Layer 0: the objects that must not be collapsed

| object | present result |
|---|---|
| source field subscripts | ambient half-spinor labels, identity-grade from section 11.2 |
| auxiliary rolled grading | exact `G=(-1)^form J`, useful for the rival operator |
| source equation-9.16 incidence | all sixteen cells transcribed; six derivative cells exact |
| frozen density/Hodge primalizer | exact K77 coordinate realization in degrees `0,1,13,14` |
| full moving density-dual operator | not constructed on an actual `Y14` atlas |
| formal adjoint | exact for the tested connection wedge; full multi-index `Phi d_A` adjoint open |
| variational core | compact-support candidate only |
| physical domain | downstream Wave 5 |
| family decomposition | kinematic provenance only; not a count or chiral index |

## Primary-source collision

The official 2021 draft supplies four different grades of information.

1. Section 9.3 says fields can “begin with operators like” equation 9.16.
2. Section 8 calls Shiab a family and admits that the calculation selecting
   the preferred operator cannot currently be located.
3. Equation 9.16 supplies the four-by-four row/column grammar and southeast
   zero, while immediately admitting a nonzero southeast rival.
4. Section 11.2 fixes the `zeta+/-` and `nu+/-` subscripts as ambient
   half-spinor labels.

The first three license conditional construction.  The fourth prevents the
successful total-grading fit from being described as a literal extraction of
the source operator.

The exact parity obstruction is short.  Let a barred row represent either the
same ambient half (`kappa=+1`) or the opposite half through the split pairing
(`kappa=-1`).  In the top rows of equation 9.16, both `Phi d` and `d` act on
inputs carrying the same displayed sign and land in rows carrying the same
displayed sign.  Both must therefore have parity `kappa`.  The constructed
blocks have

```text
parity_J(Phi d) = -1
parity_J(d)     = +1.
```

No value of `kappa` fits.  Reversing the one-form labels converts ambient `J`
to total `G` and makes the matrix fit, but section 11.2 shows exactly which
assumption was changed.

## What was constructed on the actual real K77 carrier

### Carrier and principal blocks

The probe uses the real 128-dimensional `Cl(7,7)=M_128(R)` carrier with:

- split symmetric pairing `B`;
- ambient chirality involution `J`;
- cross-half relation `BJ=-JB`;
- balanced half-spinor ranks `64+64`; and
- rolled carrier dimensions `960+960` under the auxiliary total grading.

For a covector `xi`, the selected middle contraction is

\[
(\Phi(\xi\wedge\zeta))_a
=\gamma^b(\xi_b\zeta_a-\xi_a\zeta_b).
\]

It flips ambient `J`.  Wedge and contraction preserve `J` while changing form
parity.  Hence all three are odd for `G`, and `G` is the unique grading
generated from ambient chirality and form parity with that property.

### Hodge/primalizer and frozen adjoint

In signature `(7,7)`,

\[
*^2|_{\Omega^p}=(-1)^{p(14-p)+7},
\]

so the signs in degrees `0,1,13,14` are `(-,+,+,-)`.  The exact coordinate
realization gives:

```text
R13 = + star B^-1
R14 = - star B^-1.
```

The probe also tests a nontrivial even spin-connection generator, rather than
only the identity principal symbol.  Its K77 adjoint is computed with `B`; a
Euclidean-transpose plant fails.  This establishes the frozen wedge/adjoint
pair and the lower-left sign in that tested direction.  It does not yet
derive the full moving-density, multi-index adjoint of every `Phi d_A` cell.

### Moving epsilon orbit

An exact nonconstant three-patch witness uses a square-zero, `B`-skew
generator `N` and transitions `h(x)=1+xN`.  It verifies:

- the cocycle and inverse laws;
- preservation of `B`;
- descent of moving `J`, gamma matrices, and all 196 `Phi` blocks;
- the inhomogeneous connection transformation;
- direct versus two-step descent; and
- failure of fixed-`J`, fixed-`Phi`, and missing-derivative plants.

This is an actual 128-spinor moving-orbit witness and a general associated-
bundle grammar.  It is not an atlas for arbitrary `Y=Met(X)`, a proof that the
moving reduction exists globally, or fixed-full-`H` equivariance.

### Current and common-action boundary

On the 1920-coordinate rolled carrier, one nontrivial real-spin connection
direction differentiates the fermion action to a nonzero current exactly
once.  A duplicate-current bridge fails.  This is compatible with the
predecessor's action-first `JD+JF` architecture.

It is not the complete shared-core rederivation: the predecessor's
differential `JF/QF`, every connection direction, all moving coefficients,
and the full even Ward contraction were not recomputed together in this
probe.  Those items remain part of the Wave-2 gate.

### Corrected algebraic super-IG bracket

Let `Omega=BJ`.  The 91 forms `Omega gamma_ab` are symmetric, so an
opposite-half spinor pair defines a symmetric functional on `spin(7,7)`.
Turning that functional into a spin-algebra element requires the inverse
trace metric:

\[
\beta(u,v)\ \propto\
\sum_{a<b}(-\eta_a\eta_b)
\bigl(u^T\Omega\gamma_{ab}v\bigr)\gamma_{ab}.
\]

The weighted map is nonzero, lands in the simultaneous `B/J` spin algebra,
is infinitesimally `Spin(7,7)`-equivariant on exact compact and noncompact
generator tests, and descends with the moving Clifford orbit.  The unweighted
basis sum used in the first pass fails a noncompact generator and is retained
as a planted negative control.

Pairing an `Omega0(S)` parameter with an `Omega1(S)` parameter places this
bracket in the connection-translation one-form ideal.  Declaring that ideal
to act trivially on the odd module gives a two-step algebraic extension.  The
result is conditional on the moving Spin reduction; it is not a full-`H`
supergroup or an odd symmetry of the action.

## Divergent specialist pre-assessment

Ten lightweight inline lenses set the order before computation:

1. **Primary-source archaeology:** transcribe all sixteen cells and preserve
   the candidate/family language.
2. **Clifford representation theory:** use the actual real K77 carrier and
   calculate every grading parity.
3. **Differential geometry:** treat `rho(epsilon)` as moving associated-bundle
   data, not as proof by conjugation notation.
4. **Krein geometry:** construct the pairing and adjoint before calling a
   block a mass or action term.
5. **Variational bicomplex:** emit the current from the same action and reject
   a duplicate bridge.
6. **Hyperbolic/operator theory:** keep formal core, Green data, and physical
   closed domain distinct.
7. **Lie superalgebra/cohomology:** test equivariance and Jacobi rather than
   inferring a super-extension from a bilinear's shape.
8. **Symplectic geometry:** use `Omega=BJ` and the invariant duality on the
   target Lie algebra.
9. **Phenomenology:** hold particles, chirality, masses, and families outside
   a carrier-level construction.
10. **Proof engineering:** plant wrong grading, Euclidean adjoint, fixed
    comparator, dropped connection derivative, duplicate current, wrong
    pairing, and unweighted bracket.

## Hostile post-review and repairs

The post-review did not accept the provisional Wave-2 completion.  Its six
charges found:

1. the summary identified auxiliary `G` with source field signs despite the
   page-51 ambient-half assignment;
2. the successful six-cell match therefore depended on an unreported
   one-form relabeling;
3. the bracket used a basis sum without the inverse trace metric and failed
   noncompact equivariance;
4. the three-patch witness established a conditional moving reduction, not
   actual `Y14` global existence or full-`H` descent;
5. the current check covered one nontrivial spin-connection direction, not
   the complete common-core `JD+JF`/Ward derivative; and
6. the sixteen-cell ledger typed several zero-order slots but did not supply
   their actual `W_rs` coefficients or the full multi-index adjoint.

All six findings were accepted.  The source extraction, probe, report, and
campaign status were repaired before disposition.  The durable review is
`lab/process/hostile-reviews/2026-08-04-k77-wave2-actual-draft916-blockwise-review.md`.

## Gate disposition and next efficient build

Wave 2 remains:

```text
PARTIAL__CONDITIONAL_TOTAL_GRADED_D916_RIVAL_BUILT__
SOURCE_SIGN_IDENTIFICATION_FULL_CONNECTION_VARIATION_AND_FULL_H_DESCENT_OPEN
```

The next build is narrower than “assemble D916”:

```text
K77_D916_SOURCE_SIGN_DUALITY_SHIAB_PARITY_RECONCILIATION
```

## Successor disposition (2026-08-04)

The named three-branch reconciliation is now executed in
`k77-wave2-source-sign-shiab-duality-reconciliation-2026-08-04.md`.
It does not overturn this result.  It kills a barred-row-only form-degree
duality, but finds two full row-and-column degree-reality sign solutions.  An
exact `D7` Hom calculation shows that naturally realizing either solution
requires the same extra odd vector/covector as an ambient-even middle-symbol
repair.  No released source sign correction was found, and the source-native
invariant `2 -> 1` contraction is necessarily ambient-`J` odd.  The smallest
exact escape supplies one additional moving odd covector.  That escape is a
conditional observation-relative receiver candidate, not yet source-owned and
not yet identified with P1, P2, or P3.  Wave 2 therefore remains partial.

It should enumerate, then construct or kill, the three smallest repairs:

1. an ambient-parity-compatible Shiab middle map;
2. a degree-dependent barred-field duality/reality map; and
3. a source-sign correction/relabeling justified by another primary locator.

For each survivor, substitute the actual zero-order `W_rs` coefficients,
derive the full multi-index adjoint, and rederive the complete connection
variation and even Ward identity on the predecessor's compact-support core.
Only a survivor with moving full-source-group descent closes Wave 2 and admits
the Wave-3 observation/provenance census.

## Held-out wall

No actual `Y14` atlas, arbitrary-`X` moving reduction, closed physical domain,
BFV phase space, observation quotient, Standard Model equation, particle,
vacuum, mass, anomaly, chirality value, generation count, cosmological
prediction, P1/P2/P3 use, lane change, canon change, claim-status change, or
public-posture change is claimed.

## Reproduction

```bash
uv run python \
  tests/channel-swings/k77_wave2_actual_draft916_blockwise_probe.py
python3 \
  process_gates/k77_wave2_actual_draft916_blockwise_scope_audit.py
```
