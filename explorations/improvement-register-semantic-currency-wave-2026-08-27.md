---
title: "Improvement-register semantic-currency wave"
status: active_research
doc_type: process
created: "2026-08-27"
grade: "EVIDENCE-SPECIFIC ROUTE CUSTODY; NO SOURCE OR CANON VERDICT"
target_claim: "NONE-NOT-A-KILL; internal route custody and one conventional-comparator exclusion only"
canon_verdict_change: none
ledger_edit: none
manifest: lab/process/improvement-register-semantic-currency-wave.json
probe: tests/channel-swings/improvement_register_semantic_currency_wave_probe.py
---

# Improvement-register semantic-currency wave

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

Classification: `BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: this wave binds only the executable meaning of improvement-register
rows M-M5, M-M21 and M-M25. It does not construct a source action, choose a
signature, promote a claim, alter the 91-row ledger, or touch a protected
paper.

```gu-typed-objects
result: three stale improvement-register routes reconciled to their already banked evidence ceilings
carrier: M-M5 noncompact induced-module proposal; M-M21 conventional Spin(10) 126 chain and separate boundary/count torsion arena; M-M25 incumbent homogeneous RB7 surrogate LAYER=UNTYPED CHIRALITY=N/A
pairing: Kobayashi asymptotic-support cone test, primary-group-type comparison, and frozen mixed-Gram kernel test ON=three-distinct-register-routes
real_structure: actual SL(4,R)/SO_0(3,1) pair for M-M5; conventional compact Spin(10) comparator for M-M21; real homogeneous amplitude slice for M-M25
grading: analytic admissibility versus physical count; Z/2 matter parity versus Z/3 count arena; trace versus traceless metric directions
action_owner: UNTYPED -- none newly supplied; every broader physical continuation remains input- or action-owner-gated
target: M-M5, M-M21 and M-M25 semantic-currency dispositions MAP-TYPE=evaluation
```

## Preflight bookend

The A1 source-to-proof route remains authority-gated at SC-SIG-55, every
v0.263 ledger row is terminal, and the named B2 action-root set is empty. The
global eta, DESI/Planck and boundary-action routes still lack their actual
owned packets; protected corrections remain outside unattended Progress.

Mechanism-level retrieval nevertheless finds three fresh semantic defects
that the preceding current-frontier custody wave did not touch:

1. M-M5 asks for a Kobayashi/root-system program whose exact June owner
   artifact already returns `FAILS as stated` for the actual symmetric pair.
2. M-M21 asks whether a conventional `126` VEV leaves `Z/3`, while the exact
   chain already returns matter parity `Z/2` and the separate count arena is
   typed as `Z/3` with no owned bridge.
3. M-M25 says a local normal form decides curved continuation, while the exact
   August result proves the incumbent candidate noncoercive on a commuting
   trace witness.

The route-changing lenses were representation classification, coefficient
unitarity, compact branching and moment cones; exact group/charge typing and
source-native comparator isolation; and bifurcation/Fredholm preconditions,
variational ownership and hostile counterfactual review. They converge on
semantic adjudication plus exact reproduction. A new broad representation
search, residual-subgroup computation or bifurcation calculation is dominated
until the recorded reopener changes.

## M-M5 — noncompact admissibility

The banked owner artifact and primary-source recheck agree on four independent
failures:

- `rank(G/H)=3` while `rank(K/(K cap H))=1`, so the scalar relative discrete-
  series gate fails;
- `tau_RS` is a nontrivial finite-dimensional Lorentz-spinor representation
  and is nonunitary, so the ordinary Hilbert induced object is not defined as
  stated;
- `(sl(4,R),so(3,1))` is absent from the Kobayashi--Oshima classification of
  pairs admitting an infinite-dimensional discretely decomposable
  restriction; and
- the compact spinor families approach the same nonzero diagonal cone, so a
  finite twist does not implement `rank_correction(tau_RS)=2`.

M-M5 is executed at that route ceiling. This does not erase or upgrade the
separate reconstruction-grade physical count `C^32 -> C^16 -> dim_H=8`.

## M-M21 — matter parity and the count arena

The exact conventional Spin(10) chain proves that the `126` rank-drop
direction leaves matter parity `(-1)^(3(B-L))`, a physical `Z/2`. The separate
count/boundary arena is `Z/3`; `Hom(Z/2,Z/3)=0`, and the repository owns no
map identifying their carriers, actions, reductions or observables.

Therefore the standard residual-`Z/3` route is excluded and M-M21 is executed
at the conventional-comparator/type ceiling. Weinstein's curvature-induced
connection mechanism remains a different object and receives no positive or
negative evidence from this result.

## M-M25 — bifurcation precondition

The frozen mixed Gram is `(9/32)(I+T_tr)=(9/16)P_traceless`, rank nine, with
the full-trace line as kernel. A nonzero homogeneous `D0=C=0` commuting field
lies on that line, so the necessary coercivity condition fails. No current
source-owned derivative, curvature, parent-action or section term removes the
witness.

M-M25 is premise-shifted. A Crandall--Rabinowitz calculation may become
meaningful only after an owned completion removes the witness and supplies the
Fredholm and transversality hypotheses. Running it now would classify a
noncoercive surrogate, not decide curved continuation.

## Postflight bookend

- **Strongest overclaim:** the complete analytic RS sector, GU Higgs route or
  RB7 family has been falsified. Rejected: each disposition is route- and
  object-specific.
- **Strongest contrary route:** a nonunitary relative-discrete-series theorem,
  a typed source-to-comparator bridge, or an owned coercive action term could
  reopen the respective row. Each is preserved as an exact condition.
- **Weakest reproducibility seam:** the wave consumes older evidence rather
  than producing a new calculation. The focused gate therefore pins the
  decisive tokens and structured kernel data and catches planted changes to
  population, route verdict, group order, precondition and Gram rank.

The existing CH-SM certificate passes all checks and the RB7 certificate
passes `8/8`. The focused custody gate passes `12/12` and catches `6/6`
planted mutations. No source claim, ledger row, action owner, SG4 phase,
prediction, confirmation, canon, protected paper, release state or public
posture moves.
