---
artifact_type: gate_disposition
created: 2026-08-03
gate: RESOLVER-WAVE-F-ACTUAL-SOURCE-SHIAB-FERMION-EULER-WEIGHT-SELECTION
status: OPEN_REBASED_COMPONENT_PROJECTOR_BUILT
route_disposition: REBASE
hostile_review_status: PASS_AFTER_REPAIRS
registry: explorations/cycle-gates-and-audits/resolver-wave-f-source-port-action-ownership-disposition-2026-08-03.json
---

# Resolver Wave F disposition

Wave F constructs an exact rank-252 exterior projector only after the input
has already been typed in `C* tensor Lambda6(C*)`:

```text
Pext^0 = j5 (1/9) pi_(Lambda5 V*) delta,
delta j5 = 9I.
```

It does **not** construct `q6` from a generic native adjoint value. The
eventual moved formula `U_(Theta,epsilon) Pext^0 U_(Theta,epsilon)^-1` is also
not instantiated. Exact constant signed-permutation fixtures verify two-leg
transport, composition, one explicit split-preserving signed permutation
commuting with `Pext^0`, one split-moving naturality example, and one
infinitesimal projector-chain identity. They are
a transport schema—not the actual tilted `epsilon_src` action, `Theta_Z`
overlap descent, or a global source port. The coarse `G/Spin(9,5)` soldering
field still cannot own the internal `4+10` split.

The downstream full-20 placement is distinct. Its full real exterior
Hom-space has dimension four; `[a:b]` is the two-dimensional star-even
subansatz. Wave E's `[2:1]` clean point is proved only for one simple blade.
The displayed upstream kappa field registry contains no `[a:b]`, and the
hostile collapsed quadratic is `5a^2+4b^2`, so the displayed kappa term does
not directly select `lambda=1/2`. Complete Shiab/fermion selection remains
not evaluable. An isolated zero-jet multiplier can force the representative
ratio, but is source-silent, not required, and has no coupled
nonpropagation theorem.

The side audit confirms that `e_hat_0` is the zero of the supplied auxiliary
KO family only. An arbitrary physical vectorlike `chi=0` completion is not a
canonical basepoint without same-symbol/domain/boundary/eta/naturality data.

The actual-source gate records
`OPEN -> OPEN_REBASED_COMPONENT_PROJECTOR_BUILT`; route decision `REBASE` to
`RESOLVER-WAVE-G-Q6-NATIVE-SP-TILTED-SOURCE-PORT-AND-TRANSVERSE-EULER`.
P1/P2/P3 remain unchanged and unused.
