---
title: PW2F-R2B1 — source-coordinate correction, one-graph section JVP, and complete quartic basis
date: 2026-08-02
status: PARTIAL_CONSTRUCTION_PASS_AFTER_HOSTILE_REPAIR
run: historical-investigation
---

# Result

PW2F-R2B1 constructs the exact prerequisite that the full induced-
`Y14` fourth-order coefficient needs. It does **not** compute that coefficient
yet.

The swing made four advances.

1. It corrected the literal source-coordinate tangent dictionary. If
   `q_g(epsilon)` denotes the connection one-form relative to the
   Levi–Civita origin, then

   \[
   B_{\rm full}=\Gamma(g)+q_g(\epsilon),\qquad
   T=\varpi-q_g(\epsilon),\qquad
   A_{\rm total}=\Gamma(g)+\varpi=B_{\rm full}+T.
   \]

   At fixed source `varpi`, therefore,

   \[
   \delta T=-\delta q,\qquad
   \delta B_{\rm full}=\delta\Gamma+\delta q,
   \]

   and not `delta T=-delta B_full`. The latter shortcut deletes the live
   source-coordinate return

   \[
   (D_g\Gamma)^!E_T.
   \]

2. It differentiated the observation section and the Zorro connection as one
   graph. At a normal frame the plane-wave Levi–Civita variation obeys

   \[
   \delta C_{i,ab}
   = (\delta\Gamma_i)^c{}_a g_{cb}
     +(\delta\Gamma_i)^c{}_b g_{ac}
   =\partial_i k_{ab}.
   \]

   Thus `theta=dh-C dx` satisfies

   \[
   \delta(s_g^*\theta)=dk-\delta C=0
   \]

   for all ten `Sym2(T*X)` owners. This prevents section motion, `Z1`, and the
   derivative of `Z1` from being counted as unrelated additions. The existing
   exact reconstructed LC-horizontal B2C15P connection-metric jet independently retains
   `s_g^*G_Y=g` through second base order. Freezing its horizontal Zorro terms
   produces 40 exact second-jet defects; a separate frozen-section plant emits
   the nonzero trace-reversed DeWitt mixed Hessian `12`.

3. It proves that the current first-derivative inventory cannot determine the
   off-shell pulled Hessian. For a graph `F` and action `S`,

   \[
   D^2(S\circ F)
   =J_F^*H_SJ_F+\langle E_S,D^2F\rangle.
   \]

   The probe constructs two exact graphs with the same value and first JVP but
   different `D2F`. Their Hessians differ by the second term whenever `E_S`
   is nonzero; the difference vanishes on shell as the control. B2C15O already
   supplies a live nonzero source-coordinate Euler-return witness. Therefore
   the uncomputed second Fréchet graph is a mathematical dependency for the
   complete off-shell Hessian, not optional hardening. This abstract witness
   does not prove that every candidate owner is nonzero or reaches degree four
   in the actual graph.

4. It installs the complete base-conormal quartic gate. Four variables have
   exactly 35 homogeneous degree-four monomials. The simplex-lattice
   evaluation matrix has exact rational rank 35; three independent owner-pair
   coefficient columns reconstruct exactly and agree at three held-out
   conormals. A nonzero planted quartic `eta_2^4` vanishes on every conormal in
   the predecessor five-point panel, proving that panel cannot decide the
   complete polynomial. A universal `kappa1` is now admissible only when the
   flattened coefficient vectors of

   \[
   C_4(\eta;\kappa_1)=A(\eta)+\kappa_1M(\eta)
   \]

   are coefficientwise proportional across every owner pair and all 35
   monomials.

# Why this prerequisite was necessary

The exact-geometry pre-assessment recommended differentiating the single
graph

\[
g\longmapsto(s_g,G_Y)\longmapsto B\longmapsto
(T,u,h,\widehat B)\longmapsto\text{moving Shiab action}.
\]

That remains the right target. But the repository currently owns first
derivatives for the eight moving-Shiab slots and several metric/pairing
objects, not the full second Fréchet graph. Off shell, first derivatives alone
cannot recover `pairing(E,D2 graph)`. Attempting the full `C4` immediately
could therefore omit an unruled-out fourth-order term or silently invent its
coefficient. The correct first step is to order-filter the candidate owners.

R2B1 converts that danger into an executable dependency and prepares the
exact coefficient extractor that R2B2A will use.

# Layer 0 and source collision

Primary material confirms:

- `omega=(epsilon,varpi)` and the connection-difference grammar;
- `varpi` is measured relative to `nabla^g`, while `T` is measured relative to
  the gauge-rotated Levi–Civita connection;
- the Zorro chain from connection data on `X` to a metric, then
  Levi–Civita/spin data on `Y`;
- the first action's one-half and one-third transgression coefficients.

The repository derives, rather than attributes to Weinstein:

- the distinction between `q` and `B_full` in the executable tangent chart;
- the live `(D_g Gamma)^!E_T` return;
- the exact section-JVP cancellation and rank-35 quartic gate;
- the off-shell second-Fréchet insufficiency theorem.

The leading decisive disposition is `SOURCE-SILENT`: no inspected primary
span supplies repository `h=exp(u)`, `theta1/Bhat2`, the complete second
Fréchet moving-action graph, the actual induced-`Y14` `C4`, or an exceptional
universal `kappa1`. Silence is not a no-go.

# Exact debit for R2B2A

The complete off-shell Hessian must eventually compute or exactly annihilate
the following second Fréchet candidates:

1. `trace_gamma`;
2. `Phi1_first`;
3. `Hodge_first`;
4. `Phi1_outer`;
5. `Phi2`;
6. `Hodge_inner`;
7. `Hodge_middle`;
8. `Hodge_outer`;
9. density;
10. the Krein pairing;
11. the input lowerer;
12. the output lowerer;
13. the outer pairing; and
14. the `h/theta1/Bhat2` rotation branch.

Those candidates must arise from one differentiated graph. R2B2A first
computes their differential-order ceiling and identifies which can reach
`C4`. It then builds or exactly annihilates the `C4`-capable branches—likely
including `h/theta1/Bhat2`—while carrying lower-order second-derivative debt to
the later complete `C3/C2`/Hessian stages. Afterward the code may decompose the
result into `Z0`, `Z1`, partial-`Z1`, section-tangent, and homogeneous `B1/B2`
contributions. It must not add them independently.

R2B2A then extracts all 35 quartic coefficients, verifies owner symmetry and
the even-order Helmholtz condition, and reconciles direct, formal-adjoint, and
presymplectic Green routes. Only then is an exceptional `kappa1` question
well typed.

# Boundaries

This swing does not compute the complete actual-`Y14` `C4`; identify the
repository Euler remainder with Portal's full eddy; identify source `epsilon`
with repository `h`; prove a global `Y14` section; construct an analytic
right-`H`/Krein domain; form a BV quotient; push the Euler equation to the
observation slice; or recover a physics equation.

`C3` and lower `C2` remain blocked behind the complete `C4`. P1/P2/P3 are
unchanged and unused. Curt remains formally separate inside the Eric lane,
and `TG-1 AND TG-2 AND TG-3` remains not promoted.

# Executable evidence

- Main prerequisite probe: `23 exact + 11 type + 10 planted = 44 PASS`.
- Primary-source collision: `2 exact + 6 source + 8 type + 6 planted = 22 PASS`.
- Hostile specialist review: three `PASS_AFTER_REPAIR` verdicts. Repairs typed
  `q` as `ad(P)`-valued rather than `H`-valued; pinned all eight source/control
  hashes; replaced a weak predecessor-status check with exact result-field
  assertions; scoped B2C15P to reconstructed LC-horizontal geometry; demoted
  actual `D2/C4` nonvanishing to uncontrolled; inserted the principal-order
  audit; and split the reusable `kappa1` result into `NONE`, `ANY`, and
  `UNIQUE(kappa)`.

Next gate:
`PW2F-R2B2A-SECOND-FRECHET-PRINCIPAL-ORDER-AUDIT-AND-MOVING-C4-GRAPH`.
