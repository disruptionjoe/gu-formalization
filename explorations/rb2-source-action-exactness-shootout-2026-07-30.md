---
title: "RB2 source-action exactness and current-bridge Euler shootout"
status: active_research
doc_type: construction_result
created: 2026-07-30
lane: "1"
work_item: RB2-SOURCE-ACTION-EXACTNESS-SHOOTOUT
run: lab/process/runs/GUH-20260730T203326Z-rb2-source-action-exactness/run-plan.md
probe: tests/channel-swings/rb2_source_action_exactness_probe.py
grade: "FORMULA-BUILT / CONDITIONAL CONSTRUCTION WITH ONE LAYER-0 CORRECTION. Two classical fixed-geometry antifield-zero A/U/P/epsilon/Z N1 bridge-sector Euler skeletons are built, finite-difference checked, and shown formally distinct on a nondegenerate control. A finite homogeneous adjoint-conjugation covariance proxy passes; no native stabilizer Ward identity is claimed. The total-current bridge cancels the direct full-20 connection current but leaves an A-Hessian bulk response, a Z-Hessian response, the common S20 Q_F flux, and a total-only Z-Hessian flux. The source (1/2,1/3) coefficients are uniquely selected in an independent cyclic noncommutative translation fixture, conditional on the stated transgression identities and independent channels; homogeneous gauge covariance is coefficient- and lambda-blind. The native source-shaped branch is BLOCKED FIRST at the density-dual bosonic Shiab carrier/real-form gate. Two native construction routes return through RB1/RB2; the source-real-form formula remains comparator-only. Complete N1, native Ward, Diff, CME, VEV, stationarity, domain, physical mass, anomaly/index, and count remain held."
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# RB2 source-action exactness and current-bridge Euler shootout

## Result first

RB2 returns:

```text
TWO-N1-FIVE-FIELD-BRIDGE-SLICES-BUILT-AND-FORMALLY-DISTINCT
TOTAL-CURRENT-DIRECT-CANCELLATION-LEAVES-HESSIAN+GREEN-FLUX
SOURCE-(1/2,1/3)-EXACTNESS-CONDITIONAL; WARD-COEFFICIENT-BLIND
BLOCK: SOURCE-FIRST-AT-NATIVE-DENSITY-DUAL-BOSONIC-SHIAB
EMISSION: N1-JD + N1-TOTAL CLASSICAL FIXED-GEOMETRY PAIR TO RB3
SIDE-TRACK: TWO NATIVE SOURCE REOPENERS RETURN THROUGH RB1/RB2
PARTIAL: COMPLETE-DIFF-CLOSURE-HELD-RB4; NO-DIFF-BASED-KILL
NONCLAIM: NO-NATIVE-WARD; NO-CME; NO-VEV; NO-MASS; NO-DOMAIN; NO-INDEX; NO-COUNT
```

This is a construction result, not another diagnosis that an action or
external datum is needed. Two explicit action families now have complete
five-field bridge-sector variations on the classical fixed-geometry
antifield-zero slice and survive to the next geometric swing. Their full
section, ghost, antifield, moving-geometry, and boundary systems are not
closed. A third, source-shaped family has an exact conditional formula and
coefficient selection, but it cannot yet be installed on the native
repository carrier. The bosonic map is its first minimal blocker, not an
exhaustive list of every downstream obligation and not a verdict against the
source formula.

The source formula also teaches something independent of that block. The
coefficients \(1/2\) and \(1/3\) are not decorative fit parameters. Under the
declared cyclic/transgression identities they are the unique coefficients
whose first variation reconstructs the unit coefficients of the translated
curvature. An actual cyclic noncommutative fixture derives the \(2a,3b\)
factors by finite differences, while a genuinely noncyclic insertion breaks
them. Homogeneous gauge covariance cannot see this selection: all three
planted wrong coefficient points pass the same covariance proxy.

## Plain English

There are now two serious classical fixed-geometry slices of the repository
action.

The first couples the distortion field only to the direct Dirac-like matter
current. The second couples it to that current plus the curvature-mediated
matter current. Both are mathematically coherent at the present local level,
but they make different equations. The second does **not** make all matter
backreaction disappear. It cancels the obvious current term and leaves the
less obvious response of that current to changing the connection and odd
field, plus a boundary flux. That residue is exactly the kind of
over-determined structure that can distinguish the candidates at RB3.

The Weinstein-shaped action remains worth pursuing. Its nonlinear
\(1/2,1/3\) completion has real information content. But an earlier step
quietly used a spinor-valued Shiab map where this action needs a bosonic
curvature-to-density-dual map. Those maps have different inputs and outputs.
The source has a bosonic map on its own presentation; the repository has a
proved spinorial family on its native quaternionic presentation. We have not
yet built the bridge between them.

So the next move is not to rediscover the need for a source action or datum.
It is to put the two surviving repository bridge skeletons through moving
geometry and the shared spin-zero full-20 placement. Separately—not as an
RB3 input—one of two tightly bounded native constructions of the missing
bosonic map can return through RB1 and RB2.

## 1. Layer 0 first: correction to the RB1 carrier assignment

RB1 §7 allowed the product \(\odot_c\) in the bosonic source action to range
over a proved four-real-dimensional native Shiab family. That assignment is
retracted at Layer 0.

The proved family is spinorial:

\[
\mathscr S_{\mathrm{spin},c}:
\Lambda^2V^*\otimes S\longrightarrow V^*\otimes S,
\qquad
\mathscr S_{\mathrm{spin},c}
\in
\operatorname{Hom}_{\operatorname{Spin}(9,5),\mathbb H}
(\Lambda^2V^*\otimes S,V^*\otimes S).
\]

Package the Hodge star and adjoint pairing into the density-dual bosonic
map. The action needs a composite

\[
\widehat{\mathscr S}^{\mathrm{bos}}_{\epsilon}:
\Omega^2(Y,\operatorname{ad}P)
\longrightarrow
\Omega^{13}(Y,\operatorname{ad}^*P).
\]

Its epsilon policy must be either explicit
\(\epsilon_{\rm IG}\)-dependence with the complete response retained in
\(E_\epsilon\), or an intrinsic equivariant contraction with a proved zero
epsilon response. The source presentation uses the first policy. A native
Ricci--Einstein-type construction could use the second only after proving
it.

No adjoint-to-spinor and spinor-to-coadjoint adapter has been built. The two
maps therefore cannot fill the same slot merely because both are called
“Shiab.” The source's own \(\odot_\omega\) has the correct source-side
bosonic degree pattern, but no checked map translates its
\(Y^{7,7}/U(64,64)\)-type presentation to the repository's
\(Y^{9,5}/\operatorname{Sp}(32,32;\mathbb H)\) native real form.

The correct disposition is:

| object | carrier | RB2 disposition |
| --- | --- | --- |
| proved native spinorial Shiab family | \(\Lambda^2V^*\otimes S\to V^*\otimes S\) | built, but cannot fill this action slot |
| source bosonic \(\odot_\omega\) | source \(\Omega^2(\operatorname{ad}P)\to\Omega^{13}(\operatorname{ad}P)\) presentation | source-explicit; native translation absent |
| required native density-dual bosonic Shiab | \(\Omega^2(\operatorname{ad}P)\to\Omega^{13}(\operatorname{ad}^*P)\), with Hodge/pairing already included | `UNBUILT / FIRST BLOCKER FOR SOURCE BRANCH` |

This blocks only the native source-shaped branch. It does not refute the
source-side action and does not transfer a failure between real-form forks.

The stabilizer is architecture-specific. For the N1 records,

\[
H_{\rm N1}
=
\operatorname{Stab}_{\operatorname{Sp}(32,32;\mathbb H)}
(\Gamma,P_R,P_0,K\text{ or }C,\text{end branch},
\text{active N1 tensors}).
\]

The conditional source record has the smaller group

\[
H_{\rm src}
=
\operatorname{Stab}_{H_{\rm N1}}
(\widehat{\mathscr S}^{\rm bos}_\epsilon,\text{source tensors}),
\]

which is itself conditional until the bosonic map is built. The executable
covariance control below is not a representation of either group; it is a
real \(4\times4\) homogeneous-conjugation proxy.

A generic fixed-plane full-\(\operatorname{Sp}(32,32;\mathbb H)\) action,
\(U(128)\), a positive-Hilbert replacement, the exterior \(6+4\) ten, or a
ghost-subtracted RS carrier is a hostile comparator, not the native branch.

## 2. Shared N1 action and the two constructed current bridges

Let

\[
\theta=A-\Gamma(\epsilon_{\rm IG})-U
\]

and write the tested five-field current slice as

\[
S_{\widehat C}^{(5)}
=S_{\rm common}^{(5)}+S_{20}
+\frac{1}{2\kappa}B_\theta(\theta,\theta)
-\widehat C[\theta].
\]

The two candidates differ only in

\[
\widehat C_D=\widehat J_D,\qquad
\widehat C_T=\widehat J_D+\widehat J_F,
\qquad
\widehat J_F=D_A^{\rm coad}\widehat Q_F.
\]

The full action records still retain gravity, one end-selector branch, the
SW and one \(K/C\)-Yukawa branch, an explicit spurion on/off choice, the
partial BV skeleton, and \(T_9\). Those owners are not all differentiated
here. The executable slice contains the shared Yang--Mills, parent,
full-20, and a finite retained-\(A\) control needed to test the bridge
calculus. The new bridge atomically replaces the carrier-mismatched literal
bridge.

Use \(\mathrm d_{\mathcal F}\) for a field-space Fréchet derivative so it
cannot be confused with the exterior covariant derivative
\(D_A^{\rm coad}\). Define

\[
\mathsf H_{\widehat C,\Phi}[\delta\Phi]
=
\mathrm d_{\mathcal F,\Phi}\widehat C[\delta\Phi],
\qquad
(\mathsf H_{\widehat C,\Phi}^{!}\theta)[\delta\Phi]
=
\mathsf H_{\widehat C,\Phi}[\delta\Phi][\theta].
\]

Let \(\widehat\theta/\kappa\) be the connection covector produced by the
native indefinite \(G/\kappa_{\mathfrak g}\) pseudo-musical. The complete
\(A/U/P_{\rm IG}/\epsilon/Z\) bridge-parent-full20 skeleton is

\[
\begin{aligned}
\widehat E_A^{(5)}={}&
E_{\rm YM}
+B_U^!P_{\rm IG}
+\widehat J_D+\widehat J_F
+\frac1\kappa\widehat\theta
-\widehat C
-\mathsf H_{\widehat C,A}^{!}\theta
+\widehat R_A^{(5)},\\
\widehat E_U^{(5)}={}&
D_A^!P_{\rm IG}
-\frac1\kappa\widehat\theta
+\widehat C
-\mathsf H_{\widehat C,U}^{!}\theta
+\widehat R_U^{(5)},\\
\widehat E_P^{(5)}={}&
D_AU-Z_U^{-1}P_{\rm IG},\\
\widehat E_\epsilon^{(5)}={}&
-(D_\epsilon\Gamma)^!
\left(\frac1\kappa\widehat\theta-\widehat C\right)
-\mathsf H_{\widehat C,\epsilon}^{!}\theta
+\widehat R_\epsilon^{(5)},\\
\widehat E_Z^{(5)}={}&
\widehat E_Z^{20}
-\mathsf H_{\widehat C,Z}^{!}\theta
+\widehat R_Z^{(5)}.
\end{aligned}
\]

These formulas name covectors. The \(\widehat R_\Phi^{(5)}\) placeholders
are common retained-slice responses, so they cancel in the exact difference
between bridge choices; they are not a claim that the full action has been
varied. \(E_s\), ghost, antifield, moving-map, and distributional equations
remain outside this slice. Applying the native pseudo-musical can represent
a connection covector as a connection-valued one-form, but it does not turn
an indefinite pairing into a positive Riesz theorem.

### 2.1 Direct-current bridge

On the fixed-geometry/projector stratum, \(\widehat J_D\) is independent of
\(A\). Choosing
\(\widehat C=\widehat C_D=\widehat J_D\) therefore cancels the direct
\(\widehat J_D\) contribution in \(\widehat E_A^{(5)}\) but leaves

\[
\widehat J_F
\]

in the connection equation. The current remains in
\(\widehat E_U^{(5)}\), and
\(\mathrm d_{\mathcal F,Z}\widehat J_D\) remains in
\(\widehat E_Z^{(5)}\). This candidate is graded
`SURVIVES-CLASSICAL-FIXED-GEOMETRY-ANTIFIELD-ZERO-LOCAL-WEAK`.

### 2.2 Total-current bridge

Choosing
\(\widehat C=\widehat C_T=\widehat J_D+\widehat J_F\) cancels the direct
\(\widehat J_D+\widehat J_F\) in \(\widehat E_A^{(5)}\), but because
\(\widehat J_F\) depends on \(A\) and \(Z\), the result still contains

\[
-(\mathrm d_{\mathcal F,A}\widehat J_F)^!\theta
\quad\text{and}\quad
-(\mathrm d_{\mathcal F,Z}(\widehat J_D+\widehat J_F))^!\theta.
\]

Moreover, the identity
\(\widehat J_F=D_A^{\rm coad}\widehat Q_F\) is boundary-aware. Its
integration by parts carries the graded \(Q_F\) Green flux. A nonzero
boundary term cannot be discarded as “only a total derivative.”

By contrast,
\[
\mathrm d_{\mathcal F,A}\widehat J_F[a]
=\operatorname{coad}(a)\wedge\widehat Q_F
\]
is algebraic in \(a\). It is an \(A\)-Hessian bulk response, not a new
Green owner.

The total bridge owns an additional \(Z\)-Hessian boundary term:

\[
\begin{aligned}
\mathrm d_{\mathcal F,Z}\widehat J_F[z]
&=
D_A^{\rm coad}
(\mathrm d_{\mathcal F,Z}\widehat Q_F[z]),\\
-(\mathrm d_{\mathcal F,Z}\widehat J_F[z])[\theta]
&=
-\int_YD_A\theta\wedge
\mathrm d_{\mathcal F,Z}\widehat Q_F[z]
+\int_{\partial Y}\theta\wedge
\mathrm d_{\mathcal F,Z}\widehat Q_F[z].
\end{aligned}
\]

Both candidates also inherit common Green owners from Yang--Mills, the
parent \(U\)-variation, differential \(\Gamma(\epsilon)\) when present, the
SW defect, moving support, and partial BV/Lie terms. \(Q_F\) is the
branch-discriminating boundary family, not the entire boundary ledger.

Thus “total current” means cancellation of the direct current, not
cancellation of matter backreaction. The frozen-current plant makes the
latter appear true and fails the finite-difference variation. This candidate
is graded
`SURVIVES-CLASSICAL-FIXED-GEOMETRY-ANTIFIELD-ZERO-HESSIAN-BOUNDARY-CONDITIONAL`.

The two bridge formulas differ exactly by

\[
\begin{aligned}
S_T-S_D
&=-\widehat J_F[\theta]\\
&=-\int_YD_A\theta\wedge\widehat Q_F
+\int_{\partial Y}\theta\wedge\widehat Q_F,\\
\Delta\widehat E_A^{(5)}
&=-\widehat J_F
-(\mathrm d_{\mathcal F,A}\widehat J_F)^!\theta,\\
\Delta\widehat E_U^{(5)}&=+\widehat J_F,\\
\Delta\widehat E_P^{(5)}&=0,\\
\Delta\widehat E_\epsilon^{(5)}
&=+(D_\epsilon\Gamma)^!\widehat J_F,\\
\Delta\widehat E_Z^{(5)}
&=-(\mathrm d_{\mathcal F,Z}\widehat J_F)^!\theta.
\end{aligned}
\]

The relative action is first-order and eddy-like; it is neither a duplicate
nor automatically a boundary. The formulas are formally distinct and a
nondegenerate finite control separates their gradients. This is not yet a
theorem that their native solution spaces differ. They can coincide only on
a controlled \(\widehat J_F=0\) stratum with the corresponding field
derivatives and boundary data also controlled; equality there is not an
equivalence of the actions.

## 3. Parent elimination is a bounded auxiliary-shell solution lift

The retained parent sector is

\[
S_P
=
\langle P_{\rm IG},D_AU\rangle
-\frac1{2Z_U}\langle P_{\rm IG},P_{\rm IG}\rangle .
\]

For \(Z_U\ne0\),

\[
E_P=0
\quad\Longleftrightarrow\quad
P_{\rm IG}=Z_UD_AU,
\]

and substitution gives

\[
S_P^{\rm elim}
=\frac{Z_U}{2}\langle D_AU,D_AU\rangle.
\]

The positive algebraic sign is correct even when the native bilinear form or
\(Z_U\) is indefinite. The sign-flipped shadow fails for both positive and
negative planted \(Z_U\). The probe also checks that the retained
\(A/U\)-gradients on \(E_P=0\) equal the eliminated gradients.

This earns a unique auxiliary-shell solution lift at classical
antifield-zero grade:

- a retained solution satisfying \(E_P=0\) projects to the eliminated
  system; and
- an eliminated solution lifts uniquely by
  \(P_{\rm IG}=Z_UD_AU\).

It is not an off-shell bidirectional Euler equivalence between equal field
spaces. Field-theoretic Green transport, the native Ward map, and the BV
pushforward are argued obligations, not machine-checked results. The
retained and eliminated presentations must never be inserted simultaneously.

The tempting shift \(P_{\rm IG}\mapsto P_{\rm IG}+q_F\) can imitate one
piece of the \(U\) equation but changes the \(P\) equation, \(Z\)-response,
action, and boundary terms. It is not an equivalence.

## 4. Conditional source action and why \(1/2,1/3\) matter

For a density-dual bosonic map
\(\widehat{\mathscr S}_\epsilon^{\rm bos}\) of the required carrier, define

\[
\mathcal C_{a,b}(B,\theta)
=F_B+aD_B\theta+b\,q(\theta)
\]

and the first-order source-shaped functional

\[
I^B_{a,b}
=
\int_Y
\theta\wedge
\left[
\widehat{\mathscr S}_\epsilon^{\rm bos}
\mathcal C_{a,b}(B,\theta)
+\frac{\kappa_1}{2}\flat_1\theta
\right].
\]

Define \(Q_\theta\in\Omega^{12}(\operatorname{ad}^*P)\) by

\[
\int_Y\theta\wedge
\widehat{\mathscr S}_\epsilon^{\rm bos}X
=
\int_YX\wedge Q_\theta.
\]

Before using cyclic identities, the independent Euler covectors have the
structural form

\[
\begin{aligned}
E_\theta={}&
\widehat{\mathscr S}_\epsilon^{\rm bos}\mathcal C_{a,b}
+aD_B^{\rm coad}Q_\theta
+b(\mathrm d_{\mathcal F,\theta}q)^!Q_\theta
+\kappa_1\flat\theta,\\
E_B={}&
D_B^{\rm coad}Q_\theta
+a\,\mathcal A_\theta^!Q_\theta,\\
E_\epsilon^{\mathscr S}={}&
(D_\epsilon\widehat{\mathscr S}_\epsilon^{\rm bos})^!
(\theta,\mathcal C_{a,b}).
\end{aligned}
\]

Freeze the convention

\[
F_{B+\theta}=F_B+D_B\theta+q(\theta).
\]

At fixed \(B,\epsilon,G\), the coefficient result is conditional on the
boundary-aware cyclic/transgression identities

\[
\begin{aligned}
D_B^{\rm coad}Q_\theta
&=
\widehat{\mathscr S}_\epsilon^{\rm bos}(D_B\theta),\\
(\mathrm d_{\mathcal F,\theta}q)^!Q_\theta
&=
2\widehat{\mathscr S}_\epsilon^{\rm bos}q(\theta).
\end{aligned}
\]

When these hold, the \(\theta\)-equation combines as

\[
E_\theta
=
\widehat{\mathscr S}_\epsilon^{\rm bos}
\left(F_B+2aD_B\theta+3b\,q(\theta)\right)
+\kappa_1\flat\theta .
\]

Reproducing the translated-curvature weights therefore requires

\[
2a=1,\qquad 3b=1,
\]

so

\[
(a,b)=\left(\frac12,\frac13\right)
\]

is unique in this family provided the \(D_B\theta\) and \(q(\theta)\)
channels are independent. The executable noncommutative fixture verifies
that independence. The bare point \((0,0)\), the wrong-linear point
\((1,1/3)\), and the wrong-quadratic point \((1/2,1/2)\) all fail this
identity. Finite differences derive the \(2a,3b\) multipliers in an actual
cyclic matrix functional. A noncentral insertion makes the pairing genuinely
noncyclic and breaks the naive identity, so the result is not stipulated
arithmetic.

This “exactness” is translation-sector transgression at fixed
\(B,\epsilon,G\). It is not integrability of the entire source Euler
one-form, de Rham exactness, a Ward identity, the source
\(\Xi=D\Upsilon\) claim, or an equivalence to either N1 action. The
executable endpoint-chain fixture also includes an explicit
\(\epsilon\)-dependent map and rejects omission of its
\(E_\epsilon^{\mathscr S}\) response.

## 5. Affine pullback and the \(\lambda=1\) construction

Retain RB1's formal family

\[
A_\lambda=A-\lambda U,\qquad
B_\lambda=\Gamma(\epsilon)+(1-\lambda)U,\qquad
\theta=A-\Gamma(\epsilon)-U.
\]

Let \(E_\theta,E_B\) be the source-bulk covectors and let \(J_{20}\) be the
connection variation of \(S_{20}[A_\lambda,Z]\). The exact chain rule gives

\[
\begin{aligned}
E_A&=E_\theta+J_{20},\\
E_U&=-E_\theta+(1-\lambda)E_B-\lambda J_{20},\\
E_\epsilon&=(D_\epsilon\Gamma)^!(E_B-E_\theta)
+E_\epsilon^{\mathscr S},\\
E_Z&=E_Z^{20}.
\end{aligned}
\]

At \(\lambda=1\),

\[
E_U=-E_A
\]

for the source bulk plus \(S_{20}\). This is the expected rank-one diagonal
relation because

\[
(A,U)\longmapsto(A_1,B_1)=(A-U,\Gamma)
\]

is singular. It is not evidence that either equation can be deleted
silently.

The conditional source record is an explicitly charged source-bulk/repository
hybrid. Its per-term connection ledger is:

| term family | connection argument |
| --- | --- |
| first-order source bulk | \(B_\lambda,\theta\) |
| full-20 quadratic | \(A_\lambda\) |
| SW and \(K/C\) defects | repository \(A\), charged hybrid |
| optional spurion | repository \(A\) if the branch is on |
| partial BV skeleton | repository \(A\), charged hybrid |
| gravity and \(T_9\) | no local connection argument |

This preserves already-built term arguments pending a per-term native
translation proof. A nondegenerate quadratic repo-\(A\) fixture removes the
**structural guarantee** of the diagonal null, but the actual SW, Yukawa,
and BV responses were not tested. The policy is therefore explicit but not
source-forced:

```text
selected policy: repo A
hostile comparator: A_lambda
source-forced: false
actual retained-defect response tested: false
```

For freely varied \(\lambda\ne1\),

\[
E_A+E_U=(1-\lambda)(E_B+J_{20}),
\]

so an additional equation appears. But RB1 admitted
\(\lambda\ne1\) only with \(B_\lambda\) constrained to a reference gauge
orbit. In endpoint coordinates \((A_\lambda,B_\lambda)\), varying
\(B_\lambda\) at fixed \(A_\lambda\) changes
\(\theta=A_\lambda-B_\lambda\), so the relevant covector is

\[
E_B-E_\theta,
\]

not \(E_B\). Orbit stationarity is schematically

\[
D_{B_\lambda}^{!}(E_B-E_\theta)
+E_{\rm explicit\ map/orbit}=0.
\]

Using \(E_\theta+J_{20}=0\), the same condition can be written
\(D_{B_\lambda}^{!}(E_B+J_{20})+\cdots=0\). The probe verifies the
\(E_B-E_\theta\) endpoint chain and rejects the \(E_B\)-only plant. The
constrained tangent/multiplier system itself remains unbuilt. The entire
\(\lambda\ne1\) family stays one hostile control and is not counted as
candidate attrition.

## 6. Covariance proxy: useful, but not a native Ward identity

The finite fixture uses real \(4\times4\) matrices, homogeneous conjugation,
and a skew generator. It differentiates the complete reduced
\(A/U/\Gamma/Z\) bridge action in that toy. It does **not** instantiate the
native quaternionic stabilizer, local connection inhomogeneous terms,
\(P_{\rm IG}\), defects, supplied-background responses, or BV equivariance.

Both N1 bridge slices pass this finite homogeneous adjoint-covariance proxy.
The checks include the \(A\)- and \(Z\)-responses of the current; omitting
the \(Z\)-response breaks both fixtures.

The source point and every planted wrong coefficient point also pass the
same proxy. Arbitrary tested \(\lambda\) values transform the two affine
connections under the same gauge shift. A fixed nonintertwining map breaks
the source proxy.

Accordingly:

- the finite homogeneous conjugation proxies pass;
- gauge covariance does not select \(a,b\) or \(\lambda\);
- a classical Ward contraction, BV-skeleton equivariance, and the classical
  master equation are different grades; and
- native field-theoretic internal Ward closure remains
  `CONDITIONAL / NOT INSTANTIATED`.

Complete Diff closure is only `PARTIAL`. Moving-\(s\) formulas for
\(D_sII,D_sP_0,D_s\operatorname{res}^V\), density/Hodge responses, and
distributional junctions belong to RB4. RB2 makes no Diff-based kill.

## 7. Action, no-double-count, datum, and five-leg ledgers

Every RB2 record has separate registries for:

1. action terms;
2. supplied backgrounds;
3. external interfaces; and
4. conditional maps.

Referential coverage is not confused with construction status. Each record
also carries a per-leg grade:

| record family | Y | Q | G | I | U |
| --- | --- | --- | --- | --- | --- |
| N1 bridges | built branch | classical built / BV partial | classical built / moving partial | carried, not read | classical built / BV partial |
| source branch/control | repo defects built / source translation conditional | blocked first at map / BV partial | blocked first at map / moving partial | carried, not read | blocked first at map / BV partial |

The validator rejects a source record that relabels these conditional legs
as built merely because an unbuilt conditional map carries their labels.
Every record also charges a spurion `off` or supplied-\(\Sigma\) branch and
the fundamental-YM versus source-replaces-YM choice.

The action ledger rejects:

- the retained parent beside its eliminated shadow;
- both current bridges at once;
- the first-order source action beside a residual square;
- both \(K\)- and \(C\)-Yukawa branches without a declared branch;
- the native source action without the missing bosonic map recorded;
- the source architecture beside the N1 Yang--Mills/parent/bridge terms it
  replaces; and
- an unregistered or missing Yukawa, quantum/Krein, gravity/cosmology,
  index, or unification interface.

P3, the relative-\(KO\) comparator, remains in the **external** registry and
never appears as an action term. The one P1/P2 orientation line continues
through the selected end branch and \(T_9\); RB2 neither splits it nor
derives its return map. The common Green-domain interface is likewise
carried externally until the global-domain swing.

This is how the construction remains connected to the already built
Standard Model/Yukawa, quantum/Krein, gravity/cosmology, index, and
unification legs without using any of their held-out answers to choose an
action. It is a graded interface statement, not a claim that every leg is
built. The exact sealed N1 hash and all five original held-out observable
names are unchanged; anomaly and the positivity rule are additionally
explicit in the expanded held-out wall.

## 8. Candidate disposition and bounded geometry return

| record | role | RB2 disposition | reason |
| --- | --- | --- | --- |
| `N1_JD_bridge` | candidate | `SURVIVES-CLASSICAL-FIXED-GEOMETRY-ANTIFIELD-ZERO-LOCAL-WEAK` | complete five-field bridge slice and homogeneous covariance proxy pass |
| `N1_total_current_bridge` | candidate | `SURVIVES-CLASSICAL-FIXED-GEOMETRY-ANTIFIELD-ZERO-HESSIAN-BOUNDARY-CONDITIONAL` | five-field slice passes with the \(A\)-Hessian bulk response and \(Z\)-Hessian flux retained |
| `source_reference_lambda1` | candidate | `BLOCKED-FIRST-AT-NATIVE-BOSONIC-SHIAB` | source formula, endpoint chain, epsilon-response fixture, and conditional coefficient selection survive; native carrier/real-form map is the first blocker |
| `source_orbit_constrained_lambda_not1` | control family | `BLOCKED-ORBIT-TANGENT-OR-MULTIPLIER-DECLARATION` | dependency-charged reference-orbit control, not a fourth candidate |

The first blocker is not the only downstream obligation. A native source
candidate must still clear selector/cyclic adjoints, epsilon/Hodge policy,
source/native residual translation, every retained-term connection argument,
action reality, and the global variation domain.

There are exactly two native construction routes:

1. construct a native density-dual invariant-tensor bosonic Shiab from
   \(\epsilon_{\rm IG}\) or the soldering data;
2. construct a stabilizer Ricci--Einstein contraction on adjoint curvature;

Either route must clear carrier, native real form, right-\(\mathbb H\)
compatibility, indefinite action reality, stabilizer equivariance, epsilon
policy, and transgression-adjoint certificates. It must then update the RB1
map register and rerun the RB2 source variation.

Retaining the \(U(64,64)\)-type formula is a third **source-fork
comparator**, not a native mutation and not a route back into native RB2.
This is a geometry-return packet, not permission to fit a coefficient.

## 9. RB3 emission

RB3 receives a bounded pair:

```text
N1_JD_bridge
  status: SURVIVES-CLASSICAL-FIXED-GEOMETRY-ANTIFIELD-ZERO-LOCAL-WEAK
  required carry: five-field bridge skeleton, JD Z-response,
                  common Green owners, native pseudo-musical

N1_total_current_bridge
  status: SURVIVES-CLASSICAL-FIXED-GEOMETRY-ANTIFIELD-ZERO-
          HESSIAN-BOUNDARY-CONDITIONAL
  required carry: five-field bridge skeleton,
                  d_F,A J_F, d_F,Z J_F,
                  common S20 Q_F flux, A-Hessian bulk response,
                  and total-only Z-Hessian flux
```

RB3 must now construct, rather than assume:

1. moving soldering or the maximal explicit stabilizer;
2. one shared spin-zero full-20 \(K/C\) placement;
3. the moving-current/projector responses for **both** emitted Euler systems;
4. one discriminator that can separate the two systems without reading a
   VEV, mass, index, or count; and
5. preserve the per-leg grades and full common Green ledger while doing so.

The blocked source branch is **not** consumed by RB3. At most one native
source-map route may run in parallel as an RB1-to-RB2 side track; it can join
later work only after those two gates rerun.

If one N1 branch fails under moving geometry, preserve the other. If both
fail for the same structural reason, emit the minimal obstruction and
return to the earliest geometry whose map changed. Do not terminate at
“source action missing.”

## 10. Executable controls and reproducibility

The probe:

- finite-difference checks every \(A/U/P/\epsilon/Z\) gradient in the
  classical fixed five-field N1 bridge slice;
- checks the parent stationary value and \(A/U\) gradients on the auxiliary
  shell for both signs of \(Z_U\);
- finite-difference checks the source endpoint chain at
  \(\lambda=0,1,2\), including an explicit epsilon-dependent map, and
  rejects an omitted epsilon response;
- derives the \(2a,3b\) factors in an independent cyclic noncommutative
  functional, checks exact rational selection, and breaks the identity with
  a genuinely noncyclic insertion;
- verifies that the constrained reference-orbit endpoint covector is
  \(E_B-E_\theta\), not \(E_B\);
- runs finite homogeneous adjoint-conjugation covariance proxies, including
  omitted-response and nonintertwiner plants, without promoting them to
  native Ward closure;
- checks the graded 14-dimensional Green identity with a nonzero boundary
  flux and assigns the extra total-bridge \(Z\)-Hessian owner;
- rejects the false parent sign and \(P_{\rm IG}+q_F\) field shift;
- rejects independently changed real-form, pairing, fibre, RS, and symmetry
  forks, plus registry, five-leg-status, alias, external-datum, and
  source-map plants;
- checks the hard-coded action-record digest against a validation-legal
  mutation; and
- emits only the two classical fixed-geometry N1 bridge skeletons to RB3.

Reproduce with:

```bash
python3 tests/channel-swings/rb2_source_action_exactness_probe.py
```

The frozen action-registry digest is:

```text
8aa9601a2f1d6ab4ce2b8f25b622ce1217ffb13b00b1c4682e0bf9fbfb4c7d84
```

## Held-out wall

RB2 does not claim a preferred action, a complete N1 Euler system,
source/repository identity, native field-theoretic Ward identity,
moving-full-\(\operatorname{Sp}\) symmetry, Diff closure, CME, zero-order
selector, VEV, stationary or curved solution, global common domain,
physical reduction, positivity rule, mass spectrum, P3 pushforward,
anomaly/index value, generation count, canon verdict, or public-posture
change. Layer 0 changed; L1--L7 remain exactly as inherited from RB1.
