---
artifact_type: exploration
created: 2026-08-04
title: "Resolver Wave K77-B2: the Riemann restriction of Shiab reduces to four coordinates, but the source-inspired repair tier has zero ambient-Einstein/same-action intersection"
grade: "Exact sparse first-Bianchi dimension certificate; explicit spin-curvature injection; exact complexified D7 decomposition and target multiplicities; real Cl(7,7) low/high displayed-family restriction; and two exact same-action transgression counterexamples. Displayed-ansatz kill is conditional on the ambient fourteen-dimensional Einstein trace. Bounded full-domain rival enumeration, differential Bianchi/Green, moving fields, observation, and physics remain open."
named_gate: RESOLVER-WAVE-K77-SHIAB-FAMILY-SELECTOR-AND-TRANSGRESSION-EXACTNESS
gate_before: SOURCE_BRACKET_NORMALIZED__CANONICAL_LOW_GRADE_RAW_SHIAB_CODOMAIN_KILL__LOW_GRADE_REPAIRS_NOT_VARIATIONALLY_CERTIFIED
gate_after: RIEMANN_INJECTION_AND_FOUR_COORDINATE_HOM_CONSTRUCTED__DISPLAYED_AMBIENT_EINSTEIN_SAME_ACTION_INTERSECTION_ZERO__BROADER_EXTENSION_OPEN
route_disposition: CONTINUE_K77_THROUGH_BOUNDED_FULL_DOMAIN_EXTENSION_AND_CYCLIC_EULER_EXISTENCE_SEARCH
source_collision: SOURCE-CONFIRMS-CANDIDATE__SOURCE-SILENT-ON-EXECUTABLE-SELECTOR
canon_verdict_change: none
third_lane_promoted: false
---

# Resolver Wave K77-B2: Riemann-restricted selector and transgression intersection

## Result first

The curvature side is now much smaller and much clearer than it looked after
K77-B.

At one point of the `(7,7)` observerse, the source Shiab accepts the enormous
space

\[
 \Lambda^2V^*\otimes\mathfrak u(64,64),
 \qquad \dim_{\mathbb R}=91\cdot16384=1{,}490{,}944.
\]

Ordinary algebraic Riemann curvature occupies only the explicitly injected
submodule

\[
 \mathcal R(V)=\ker\!\left(
 \operatorname{Alt}:\operatorname{Sym}^2(\Lambda^2V^*)
 \to\Lambda^4V^*\right),
 \qquad \dim=4186-1001=3185.
\]

The pointwise fixed-frame injection used here is

\[
 \iota_\zeta(R)_{ij}
 =\zeta\sum_{a<b}R_{ij}{}^{ab}\gamma_a\gamma_b,
 \qquad \zeta\ne0,
\]

with the internal indices raised by the `(7,7)` metric. Basiswise coefficient
extraction proves injectivity for every nonzero convention factor `zeta`; the
factor is kept explicit rather than silently identifying competing spin-
curvature normalizations. The construction is the pointwise spin differential
after raising the internal curvature pair. The executable verifies the
internal bivector intertwiner on all `91 x 91` basis/generator pairs and checks
nontrivial Ricci and mixed-sign Weyl curvature holdouts. Associated-bundle,
soldering, and atlas descent are not inferred from this fixed-frame result.

Exact `D7` character arithmetic then gives

\[
 \mathcal R(V)=\mathbf1\oplus S^2_0V^*\oplus W,
 \qquad 3185=1+104+3080,
\]

and, in the complexified Shiab target,

```text
scalar multiplicity            2
traceless-Ricci multiplicity   2
Weyl multiplicity              0
```

This reverses one tempting reading of the source. **Weyl-killing cannot select
the Shiab:** every Spin-equivariant zero-order map of this target type already
kills Weyl on the algebraic-curvature submodule. The complete equivariant
Riemann restriction is only four-dimensional—low/high scalar and low/high
traceless-Ricci.

Inside K77-B's source-inspired factorized low/high completion of the printed
raw skeleton, all eight unhalved commutator/`i`-symmetric product patterns and
the complete low/high invariant carrier were then evaluated in lifted
coordinates

\[
 (a,b,ac,ad,bc,bd).
\]

Only two product patterns can reproduce the **ambient fourteen-dimensional**
Einstein trace ratio on nonzero algebraic curvature. Neither is the derivative
of the same displayed cubic action. Exact low and coefficient-volume-dual
field fixtures force `a=b=0`; factorization then makes the entire displayed
map zero for every `c,d`. The zero map is rejected by the nonzero scalar/Ricci
normalization anchor.

The earned disposition is therefore

```text
RIEMANN_INJECTION_CONSTRUCTED
FOUR_DIMENSIONAL_RIEMANN_HOM_CENSUS_COMPLETE
WEYL_KILLING_NOT_A_SELECTOR
DISPLAYED_FACTORIZED_ANSATZ_KILLED_FOR_AMBIENT_EINSTEIN_PLUS_SAME_ACTION
TWO_DIMENSIONAL_AMBIENT_EINSTEIN_RESTRICTION_CONSTRUCTED_POINTWISE
FULL_DOMAIN_SOURCE_NATURAL_EXTENSION_OPEN
```

This is not a K77, GU, gravity, or physics kill. It says the source-inspired
factorized completion, using the source's separately printed unhalved product
operations, cannot
simultaneously be the ambient Einstein contraction and the unit-weight Euler
endpoint of its own cubic term. A broader map remains constructible.

## Plain English

We found the right small arena.

Instead of searching almost 1.5 million curvature coordinates, the Riemann
part splits into just three kinds: scalar curvature, traceless Ricci curvature,
and Weyl curvature. Representation theory says the proposed output cannot see
Weyl at all and has exactly two ways to see each of the other two kinds.

That means Eric's statement that the map kills Weyl is true in this setting,
but it is not special enough to identify his map. Every eligible map does it.

We then gave the displayed formula its best available low/high completion.
Two versions can be tuned to have the right fourteen-dimensional Einstein
trace. But the same versions fail the independent question “is this actually
the derivative of the action you wrote?” The only simultaneous solution is
the zero map.

The constructive escape is precise rather than rhetorical: on true Riemann
curvature there is still an exact two-parameter low/high Einstein family. We
now need to extend that family to the full adjoint-valued curvature space in a
source-natural way and make the extension cyclically variational by
construction. That is a different object from pretending the displayed map
already works.

## 0. Layer 0: the objects kept separate

| object | mathematical type | status here |
|---|---|---|
| ambient source curvature | `Lambda2 V* tensor u(64,64)` | 1,490,944-dimensional pointwise domain |
| algebraic Riemann curvature | `ker(Alt:Sym2 Lambda2 -> Lambda4)` | explicit 3,185-dimensional injected submodule |
| algebraic first Bianchi | `R_[ijk]l=0` | defines the Riemann submodule |
| differential gauge Bianchi | `D_A F_A=0` | unbuilt in this gate |
| Weinstein's historical “Bianchi selected it” | missing rule in the 2021 source | `UNCERTAIN`; not identified with either preceding line |
| ambient Einstein trace | `Ric_14 - (1/2) Scal_14 g_14` | conditional selector used here |
| observed Einstein trace | four-dimensional pullback/quotient equation | unbuilt |
| Frobenius-fibre trace reversal | vertical `(7,3)->(6,4)` metric operation | distinct; not selected by full Spin(7,7) alone |
| Shiab action density | top-degree scalar pairing | locally exercised in the cubic sector |
| Euler covector / Green realization | field derivative / closed-domain operator | cubic obstruction found; derivative/Green remains open for rivals |

The phrase “Einstein-like” is therefore conditional throughout this receipt.
Full Spin `(7,7)` cannot itself distinguish the horizontal four-plane from the
vertical metric fibre; observed and fibre trace maps require the observation/
Zorro split or a stabilizer reduction.

## 1. Primary-source collision

The source collision is `SOURCE-CONFIRMS-CANDIDATE` and
`SOURCE-SILENT-ON-EXECUTABLE-SELECTOR`.

| 2021 draft location | source content | consequence |
|---|---|---|
| p.42 §8.2 | recalls representation/highest-weight work and says Bianchi selected a preferred operator in other circumstances; notes are not located | historical intent, not an executable rule |
| p.43 eqs. (9.2)--(9.3), footnote 10 | types and prints the two-term Shiab; says other Shiabs are possible | candidate family, no uniqueness |
| p.44 eq. (9.4) | uses the candidate inside B1 | action use is not variational proof |
| p.57 eqs. (12.4)--(12.7) | gives the Chern--Simons analogy, Weyl-killing statement, and a differential-Bianchi motivation | does not identify the missing Shiab selector with algebraic Bianchi |
| p.66 eqs. (12.24)--(12.27) | supplies Hodge plus unhalved commutator and `i`-symmetric operations | construction toolkit expands rivals |

The source is silent on the explicit spin/soldering injection, the four
low/high coordinates, the ambient/observed/fibre trace fork, Helmholtz
transgression, Green data, and uniqueness.

## 2. Exact curvature construction

Let `P=Lambda2 V*`, so `dim P=91`. Pair-symmetric curvature has

\[
 \dim\operatorname{Sym}^2P=\frac{91\cdot92}{2}=4186.
\]

For every ordered four-set `i<j<k<l`, the algebraic Bianchi row is

\[
 R_{ij,kl}-R_{ik,jl}+R_{il,jk}=0.
\]

There are `C(14,4)=1001` rows. Their three-coordinate supports are disjoint:
each disjoint pair-of-pairs determines its unique four-set. Hence the rows
have exact rank 1001 without a dense 3185-vector kernel calculation.

The executable constructs three exact representatives:

- constant curvature, with `Ric=13g`, `Scal=182`;
- `R=(S kulkarni-nomizu g)/12` for a traceless `S`, with `Ric=S`;
- a nonzero Ricci-free diagonal Weyl operator on an all-positive four-plane.

It also rejects a pair-symmetric tensor with a live algebraic-Bianchi defect.

The complexified `D7` receipt is

```text
Sym2(Lambda2 V) - Lambda4 V
  = 1 + D7(2,0,0,0,0,0,0) + D7(0,2,0,0,0,0,0)
dimensions: 1 + 104 + 3080
target multiplicities: 2, 2, 0
```

This establishes the four-coordinate Riemann-restriction census. It does not
say two full-domain Shiabs that agree on Riemann curvature are the same.

## 3. Displayed family and ambient Einstein selector

The K77-B source-inspired factorized repair family is

\[
 \Phi_1=a\Phi_1^{(1)}+b\Phi_1^{(13)},\qquad
 \Phi_2=c\Phi_2^{(2)}+d\Phi_2^{(12)}.
\]

Because the first displayed summand is linear in `Phi1` and the second is
bilinear in `Phi1,Phi2`, the correct lifted feature order is

```text
a, b, ac, ad, bc, bd
```

with the rank-one/factorization relations retained. The independent-invariant
fork includes the geometrically narrower generated-Cartan fork
`Phi2=(1/2)Phi1 wedge Phi1`; killing the larger factorized family also kills
that subset for this combined selector.

For constant curvature the fourteen-dimensional Einstein tensor is
`G=-78g`; for the traceless-Ricci representative it is `G=S`. Requiring one
common low/high scale therefore imposes `scalar_response + 78*ricci_response
=0` in each coefficient grade.

Six of the eight product patterns are immediately vacuous or fail that ratio.
The two nonvacuous survivors are:

| channel pattern | low Einstein condition | high Einstein condition |
|---|---|---|
| `comm/symi/comm` | `bd=a` | `ad=b` |
| `comm/symi/symi` | `ac=a` | `bc=b` |

Every viable feature also vanishes on the Weyl representative, as required by
the exact multiplicity-zero result.

## 4. Independent same-action test

For a fixed zero-order map `S`, the source cubic

\[
 I_3(T)=\frac13\langle T,S(T\wedge T)\rangle
\]

has the proposed unit-weight endpoint only if, for every `T,alpha`,

\[
 \langle T,S(\alpha\wedge T+T\wedge\alpha)\rangle
 =2\langle\alpha,S(T\wedge T)\rangle.
\]

This identity is linear in the lifted map features. One exact mixed-grade
fixture and its coefficient-volume dual give:

```text
comm/symi/comm:
  base  = (1024/3)*a + (512/3)*bd
  dual  = (512/3)*ad

comm/symi/symi:
  base  = (1024/3)*a - (512/3)*ac
  dual  = (512/3)*bc
```

Combining these with the Einstein conditions forces `a=b=0` in each channel.
Since every displayed term contains the same `Phi1`, factorization makes the
map zero. This is an exact counterexample-based family obstruction: a single
valid failure would kill a candidate, and these witnesses kill both remaining
families. It is not a sampled pass promoted to a universal identity.

## 5. The constructive object that survives

The representation calculation and explicit real maps supply a useful
replacement target.
On algebraic Riemann curvature, every equivariant zero-order map has the form

\[
 \alpha_L Q_L+\beta_L Z_L+\alpha_H Q_H+\beta_H Z_H,
\]

where `Q` is the traceless-Ricci channel and `Z` the scalar channel. The
ambient Einstein ratio cuts this to

\[
 p\,J_L(G_{14})+q\,J_H(G_{14}).
\]

The executable constructs `J_L(G_14)` and `J_H(G_14)` explicitly and checks
their degree-13 real-adjoint type, independent low/high coefficient grades,
nonzero scalar/Ricci response, zero Weyl response, and nontrivial generator
intertwining. Thus this two-coordinate restriction is exact and nonzero at
the pointwise fixed-frame level. It is not yet a descended associated-bundle
map. What is missing is a full-domain extension

\[
 \Lambda^2V^*\otimes\mathfrak u(64,64)
 \longrightarrow \Lambda^{13}V^*\otimes\mathfrak u(64,64)
\]

that is source-natural, real-adjoint, equivariant, cyclically action-exact,
and compatible with differential Bianchi/Green data. Restriction does not
determine that extension uniquely.

The bounded next grammar `G_0^{<=2}` now has a provisional declared signature:
zero-order, linear in one curvature input, output degree 13, depth at most two
invariant insertions,
generated by Hodge, wedge/contraction, unhalved commutator, `i`-symmetric
product, coefficient volume, real-adjoint projection, and finite sums. The
generator list is defined here; executable AST typing, canonical expression-
DAG enumeration, and fingerprint deduplication remain the next gate. It is not
called the full Shiab family.

## 6. Constraint accounting

For each viable displayed product pattern, the exact certificate records:

```text
lifted linear-row rank                         4
factorized parameter-ideal height              2
zero-map parameter fibre dimension             2
nonzero map survivors                          0
positive constraint surplus                    NOT EARNED
```

The reduced Groebner basis is `<a,b>` for both viable patterns; `c,d` remain
free only because every such point represents the same zero map. Counting
four lifted rows as four independent physical constraints would therefore be
wrong. This is an obstruction, not confirmation from positive surplus. The broader
two-coordinate Riemann restriction has no honest surplus yet because its
full-domain extension, gauge/Bianchi, and Green constraints have not been
constructed.

## 7. What moved and what did not

Moved:

- the 3,185-dimensional Riemann module now has an explicit pointwise fixed-
  frame injection with basiswise inverse and exact infinitesimal intertwiner;
- its exact `1+104+3080` decomposition and four-coordinate target Hom space
  replace a brute-force search;
- Weyl-killing is proved nonselective at this representation level;
- the complete displayed low/high factorized family is killed for the joint
  ambient-Einstein/same-action burden;
- an exact two-coordinate ambient-Einstein restriction survives as the
  constructive target for a broader full-domain extension.

Not moved:

- the missing historical Bianchi selector is still missing;
- the bounded zero-order DAG grammar has only a provisional signature; its
  executable typing and canonical enumeration remain open;
- associated-bundle/soldering descent of the injection and restriction remain
  open;
- no differential Bianchi, derivative/Green, moving-epsilon/Hodge/metric,
  Noether/BV/domain, observation, K77-C, particle, Standard Model, gravity,
  cosmology, dark-sector, generation, or count result advances;
- P1/P2/P3 remain unchanged and unused;
- no canon, protected verdict, lane, public posture, or third-lane status
  changes.

## 8. Next gate

`RESOLVER-WAVE-K77-B3-FULL-DOMAIN-EXTENSION-CYCLIC-EULER-EXISTENCE-AND-GREEN`:

1. canonically enumerate and fingerprint the bounded `G_0^{<=2}` expression
   DAG on both Riemann and off-Riemann fixtures;
2. construct or rule out source-natural full-domain extensions of the two-
   coordinate Riemann restriction;
3. for each surviving extension, test whether its cubic trilinear is cyclic/
   Helmholtz rather than presupposing an Euler completion;
4. distinguish maps agreeing on Riemann curvature but differing on central,
   non-grade-two, and algebraic-Bianchi-defect inputs;
5. impose the principal-symbol formal-adjoint condition for `S o D_B`;
6. establish associated-bundle/soldering/atlas descent and active-`epsilon`
   covariance for survivors;
7. only then construct the live Green boundary concomitant and move Hodge,
   metric, and observation data.

K77-C and the atomic physics table remain blocked until one common bosonic
action member survives these gates.

## Reproduction

```bash
uv run --with sympy==1.14.0 python \
  tests/channel-swings/resolver_wave_k77b2_shiab_family_curvature_selector_transgression_probe.py
```

Initial exact packet: `25 exact + 1 Sage + 6 source + 11 type + 8 planted =
51 PASS`. Final repaired packet: `34 exact + 1 Sage + 6 source + 11 type + 9
planted = 61 PASS`. Final hostile-review status is recorded in the
companion review and disposition artifacts.
