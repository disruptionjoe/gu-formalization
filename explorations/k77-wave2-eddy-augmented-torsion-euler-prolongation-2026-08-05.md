---
title: "K77 Wave 2 eddy-completed augmented-torsion Euler prolongation"
date: 2026-08-05
status: PARTIAL_ACTION_EULER_FUNCTOR_BUILT_PRINTED_ENDPOINT_SUPERSEDED
lane: "1"
fork: "SIGNATURE_AMBIENT_7_7"
grade: "Exact source/repo supersession collision; exact graded-cyclic identity-Shiab control; replay of the selected full-domain K77-B3 printed-endpoint obstruction; exact finite noncyclic Frechet-adjoint action Euler; exact two-connection path-average reconstruction; complete grade-one printed-rival principal ranks on positive, negative and null covectors; independent Sage replay. Full selected-Clifford action adjoint, action-owned degree-14 row, raw northeast owner, total fermion rendezvous, global domain, observation and physics remain open."
---

# K77 Wave 2 eddy-completed augmented-torsion Euler prolongation

## Outcome first

This wave found a more important result than the one it initially sought.

The two-connection geometry really does reconstruct the source's
quadratic-eddy **path-average curvature**.  But the repo had already proved
that the source-printed unit-weight endpoint

\[
\Upsilon_{\rm print}=S(F_A)+*\kappa T
\]

is not the variation of that same action for the selected nonzero
full-domain Shiab.  An initial draft of this wave accidentally revived the
killed endpoint by doing the cyclic transgression calculation as if `S` were
the identity.  The standing archaeology/supersession reviewer caught it.

The corrected construction uses the action's actual Euler derivative:

\[
\boxed{
E_{\rm act}
=S(\bar F)+(D_T\bar F)^!S^!T+*\kappa T.
}
\]

The second term is the missing Fréchet/formal-adjoint companion.  It vanishes
or combines into the endpoint only for a genuinely cyclic `S` or a lawful
restricted domain.  K77-B3 proves that the selected ambient-Einstein Shiab
does not have that property on the full written translation domain.

The source-displayed pair

\[
(\Upsilon_{\rm print},\Xi_{\rm print}=D_\omega\Upsilon_{\rm print})
\]

therefore remains a **rival**, not the action-owned Euler complex.  Its
generic grade-one degree-14 symbol has exact rank 13 on all three nonzero
`Spin(7,7)` covector orbits, while it vanishes on the metric-Riemann
subcarrier.  That rank is useful, but it belongs to the printed rival.  The
degree-14 companion to `E_act` remains to be derived.

The raw cyclic-square northeast block `-T wedge F_B` also remains unowned.
The source's old up-and-back stress-energy remark tells us to investigate it,
not to erase or name it.

## 1. Layer 0: the four objects that were being conflated

| object | formula/type | status |
| --- | --- | --- |
| path-average input | `barF=F_B+1/2 D_BT+1/3 T^2` | reconstructed exactly from two-connection data |
| source-printed row | `Upsilon_print=S(F_A)+*kappa T` | genuinely printed; killed as the selected full-domain action derivative |
| action-owned row | `E_act=S(barF)+(D_T barF)^!S^!T+*kappa T` | live variational construction |
| printed companion | `Xi_print=D_omega Upsilon_print` | first-prolonged rival; not transferable to `E_act` |

Further distinctions remain mandatory:

- rank-91 algebraic-Riemann curvature versus rank-182 complete grade-one
  generic adjoint connection symbols;
- the zero-jet shifted square versus its first prolongation;
- source on-shell redundancy versus Noether or BV identity;
- a spoken up-and-back clue versus a typed stress-energy owner; and
- ambient fourteen-dimensional equations versus observed physics.

## 2. The eddy coefficients are right, but that is not enough

For `A=B+T`, define

\[
\bar F=\int_0^1F_{B+tT}\,dt
=F_B+\frac12D_BT+\frac13T^2.
\]

The coefficients `1/2,1/3` are exactly the path integrals of `t,t^2`.
In the identity-Shiab cyclic control,

\[
\delta_T\langle T,\bar F\rangle[X]
=\langle X,F_A\rangle.
\]

The two derivative terms each contribute `1/2`; the three cubic cyclic
placements each contribute `1/3`.  The kappa term varies to `kappa<T,*X>`.

But for a fixed noncyclic Shiab `S`, the actual variation is

\[
\begin{aligned}
\delta I[X]
=&\;\langle X,S(\bar F)\rangle\\
&+\langle T,S(L_TX)\rangle
+\kappa\langle X,*T\rangle,
\end{aligned}
\]

with

\[
L_TX=D_T\bar F[X]
=\frac12D_BX+\frac13(XT+TX).
\]

Passing the second term through the declared pairing gives

\[
E_{\rm act}=S(\bar F)+L_T^!S^!T+*\kappa T.
\]

The main probe checks this decomposition exactly on a noncyclic matrix
fixture, including

\[
\langle T,S(L_TX)\rangle
=\langle S^!T,L_TX\rangle.
\]

It also replays two independent prior results:

1. the action-first Wave-2 probe, where `E_act` differs from the advertised
   endpoint and regular parent elimination returns `E_act`; and
2. K77-B3, whose exact low/high cyclic-kernel witnesses force both nonzero
   ambient-Einstein coefficients to zero if the same `S` is required to be
   the printed endpoint on the full domain.

So the eddy does not fail.  It supplies the right transgression input.  What
fails is the extra inference that every Shiab can be pulled through the
cyclic variation as though it were the identity.

### Convention fence

`T^2` here uses the normalized product convention already fixed in the local
source pack and predecessor.  A convention with `[T,T]=2T wedge T` must be
converted before coefficients are compared.

## 3. What the two-connection operator now supplies

For

\[
\mathcal D_{A,B}
=\begin{pmatrix}d_A&-L_{F_B}\\1&-d_B\end{pmatrix},
\]

the exact finite noncommutative square is

\[
\mathcal D_{A,B}^2
=\begin{pmatrix}
L_{\Delta F}&-L_{T\wedge F_B}\\
L_T&0
\end{pmatrix},
\qquad
\Delta F=F_A-F_B.
\]

The northwest/southwest blocks and named background reconstruct

\[
F_A=F_B+\Delta F,
\qquad
\bar F=F_B+\frac12\Delta F-\frac16T^2.
\]

The **full unsquared operator** also carries `d_B` and `T`, hence the
linearization `L_T`.  With the selected Shiab, pairing/primalizer and formal
adjoints supplied, the honest comparison is therefore

\[
\boxed{
C_{\rm act}(\mathcal D,\mathcal D^2,S,\langle\ ,\ \rangle)
=S(\bar F)+L_T^!S^!T+*\kappa T.
}
\]

This is the largest currently justified action-owned functor.  The present
probe verifies its calculus and an exact noncyclic realization; it does not
yet assemble `S^!` on the complete real-Clifford field carrier or a closed
global domain.

The simpler map

\[
C_{\rm print}=S(F_B+\Delta F)+*\kappa T
\]

still reconstructs what the draft prints.  It is retained as a rival and a
negative control, not selected as the Euler equation.

## 4. The printed degree-14 rival has a live rank-13 symbol

On the complete grade-one generic connection carrier

\[
F=k\wedge a,
\qquad
a\in T^*Y\otimes\operatorname{Cl}^1(7,7),
\]

the input rank is `(14-1)*14=182`.  The frozen principal symbol of the
**printed rival** is

\[
k\wedge S_{\tt comm/symi/symi}(k\wedge a).
\]

| covector orbit | input rank | printed-rival defect rank | nonzero generating columns |
| --- | ---: | ---: | ---: |
| positive | 182 | 13 | 13 |
| negative | 182 | 13 | 13 |
| null | 182 | 13 | 28 |

The same selected map has defect rank zero on each complete rank-91
algebraic-Riemann differential-Bianchi carrier.  This is a clean Layer-0
separation:

\[
\text{Riemann closure }0
\quad\neq\quad
\text{generic printed-companion image }13.
\]

The nonzero symbol means `Xi_print` cannot be a pointwise zero-order function
of `Upsilon_print` values.  A first prolongation is required.  It does **not**
establish the action-owned degree-14 row, because `Upsilon_print` is not
`E_act`.

Independent Sage rebuilds the exterior/Clifford algebra over `QQ(i)` and
reproduces `(182,13,13)`, `(182,13,13)`, `(182,13,28)` plus the identity-Shiab
transgression coefficients.  It is explicitly a printed-rival/identity
control, not an action-Euler certificate.

## 5. The northeast block still needs an owner

The raw square contains

\[
N_{AB}=-T\wedge F_B.
\]

Hold `F_A,T` fixed and change the splitting `F_A=F_B+DeltaF`.
`C_print` is unchanged, while `N_AB` changes.  The free noncommutative probe
constructs both splittings exactly.  Thus this block is extra information,
not something the endpoint pair determines.

The action-owned `E_act` does use `B,T` and formal adjoints, so it is still
possible that a typed transformation of the northeast block participates in
the companion/current term.  Nothing currently proves that.  The 2020
up-and-back stress-energy remark is a directive to inspect the complete
boson/fermion diagram; the 2025 cyclic square is explicitly unreleased.

An equivalence now needs one of:

- a derived stress-energy/fermion-current owner from the complete action;
- a chain homotopy placing the block in a contractible summand;
- a justified constraint/quotient; or
- an explicit rival-surplus disposition.

## 6. Seven-axis protocol record

| protocol leg | result here | held boundary |
| --- | --- | --- |
| L1 substrate | local real `Cl(7,7)` and two-connection exterior carrier | no arbitrary global `Y14` realization |
| L2 observer | no new observer selection | no observed physics |
| L3 pairing | actual Euler formula names the invariant pairing and formal adjoints | closed Krein/Green domain open |
| L4 causal order | no causal selection | one-time hyperbolic evolution open |
| L5 emergence | action Euler emerges from the eddy path average plus adjoint companion | no vacuum, particles or couplings |
| L6 coordination loop | printed redundancy retained as a rival | action-owned Noether/BV/degree-14 row open |
| L7 positivity | positive/negative/null covector orbits tested exactly | no positive state-space metric |

## 7. Hostile review and the important correction

The paired standing charges did real work:

- **Summary outruns artifact:** the first draft called the identity-Shiab
  cyclic calculation a proof for the selected Shiab.  This was false.
- **Artifact defends a superseded object:** the first draft revived the exact
  printed endpoint that K77-B3 and the action-first architecture had already
  superseded.

The repair makes the action-owned Fréchet-adjoint Euler primary, keeps the
printed pair as a rival, and retypes rank 13 accordingly.  This is the same
compression failure diagnosed elsewhere in the repo: an exciting summary ran
one inference ahead of its artifacts.  The two-sided reviewer caught it before
commit.

## 8. Exact disposition and next gate

```text
EDDY_PATH_AVERAGE_FROM_TWO_CONNECTION_DATA:
  BUILT

ACTION_FRECHET_ADJOINT_EULER_FUNCTOR:
  BUILT_CONDITIONALLY_AT_FORMAL_VARIATIONAL_GRADE

SOURCE_PRINTED_ENDPOINT_AS_SELECTED_FULL_DOMAIN_ACTION_DERIVATIVE:
  KILLED_RETAINED

PRINTED_13_PLUS_14_RIVAL:
  FIRST_PROLONGED_CONDITIONAL_PAIR__DEFECT_RANK_13

ACTION_OWNED_DEGREE14_COMPANION:
  OPEN

RAW_NORTHEAST_OWNER_OR_HOMOTOPY:
  OPEN
```

Main probe: `7 source + 36 type + 34 exact + 10 planted = 87 PASS`.
Independent Sage: `PASS`.

The next construction gate is:

```text
K77_ACTION_OWNED_SELECTED_SHIAB_EULER_AND_DEGREE14_TOTALIZATION
WITH_RAW_NORTHEAST_OWNER
```

It must instantiate the selected Shiab adjoint on the complete real K77
field carrier, derive the action-owned degree-14/Noether row, and collide the
raw northeast block with the already-built `J_D+J_F` total action.  The exit
is either one total boson/fermion Euler complex or a precise block-level rival
incompatibility.

Curt remains formally separate inside the Eric lane.  `TG-1 AND TG-2 AND
TG-3` remains false.  No observation, physical domain, vacuum, particle,
P1/P2/P3, Wave 3, canon, lane-count or public-posture claim moves.
