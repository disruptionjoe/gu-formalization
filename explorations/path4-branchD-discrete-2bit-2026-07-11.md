# Path 4 / Branch D -- the discrete, choice-independent forced fact (a "2-bit prediction")

**Status:** BLIND branch D of the path-4 "forced family-invariant" wave. The continuous predictions of the
source-action family are all scaled by the free `mu_DW` / `alpha` and (H53) decouple or are non-discriminating.
Branch D asks the orthogonal question: **is there a DISCRETE fact -- a small integer, a mod-k selection rule, a
forced correlation between two discrete data -- that is the SAME for every member of the shape-dim-1 family and
its two free scales, is NOT already an input/known, and DISCRIMINATES?** A discrete fact can be forced even when
the continuous residual is free, so this is exactly the crack path-4 should probe.

**Test:** `tests/W64_path4_D_discrete.py` (deterministic; exit 0).
**Construction fork used (per `GEOMETER-VS-PHYSICS-OBJECTS.md`):** the *generation-count* row (count as a
torsion class in the 3-primary arena `Z/3 subset pi_3^s = Z/24`, `3 = dim Lambda^2_+`, NOT a Z-index) and the
*gravity-functional / DOF* row (`|II|^2` -> a genuine 4th-order action, 7 propagating DOF). Both discrete
structures are taken on the geometer's side and each is named where the standard-physics side would differ.

**Honest headline (pre-registered outcome (b), reached cleanly):** *the forced discrete content is REAL but it
is the ALREADY-KNOWN discrete content.* Every discrete fact that is genuinely forced across the whole family is
one the repo has already established (ceiling `3 = dim Lambda^2_+`; `{1,3}` 3-primary; the 2/3-primary arena
split; 7 DOF). Every discrete relation that would be genuinely NEW (the carrier-B `-38` arithmetic; the count
pinned to the integer `3`) is **choice-dependent** -- it rides the carrier bit / C1 / SG4 -- hence NOT forced,
hence not a family-invariant. **Branch D does not deliver a new forced-and-discriminating discrete prediction.**
The single strongest forced-and-arguably-novel discrete statement (the `d=4` <-> ceiling-3 lock, below) is
forced but a ceiling not a value, and not operationally discriminating.

---

## 1. The five personas, inline

### Persona 1 -- representation/topology specialist: enumerate the DISCRETE forced structure

Everything discrete the family forces, listed, with the free-scale dependence checked for each:

| # | discrete fact | value | forced across family? | already known in-repo? |
|---|---|---|---|---|
| D1 | generation **ceiling** = `dim Lambda^2_+(R^4)` | `3` | YES (pure kinematics of the 4-base; no `mu_DW`, `alpha`, shape) | YES (H38 Q1c; branch-D wave-3) |
| D2 | realized count menu (odd, 3-primary) | `{1,3}` | YES (Z/3-invariant subspaces of `R^3`, oddness) | YES (H40, path3-D) |
| D3 | arena of the count vs arena of chirality/ghost | `Z/3` vs `Z/2` | YES (`pi_3^s = Z/8 (+) Z/3`; ghost parity `P^2=I`) | YES (two-primary-lemma; H39 Q3) |
| D4 | propagating gravitational DOF (4th-order `|II|^2`) | `7 = 2 + 5` | YES (`mu_DW`-invariant integer, H53 row 5) | YES (H49/H45; H53) |
| D5 | carrier index on the RS generation slot | `A: -42` / `B: -38` | **NO** -- differs by the carrier bit | YES (H39; -38 adjudication) |
| D6 | order-3 rho class of the count carrier | `A: (0,0,0)` / `B: (0,2,1)/3` | **NO** -- rides the carrier bit | YES (order3-rho canon) |

The **ceiling `3`** deserves a sharper statement, because it is the only place a forced *correlation between two
discrete data* lives. `3 = dim Lambda^2_+(R^4)` is not a free integer: the self-dual eigenspace of 2-forms
EXISTS only when the Hodge star maps `Lambda^2 -> Lambda^2`, i.e. `*: Lambda^k(R^d) -> Lambda^{d-k}`, `k=2`,
`d-k=2`, forcing **`d = 4`**. In `d != 4` there is no self-dual-2-form eigenspace at all (the star lands in a
different degree), so the "generation ceiling = dim Lambda^2_+" mechanism is a **uniquely 4-dimensional**
phenomenon. Hence the family forces a *correlation between two discrete data*:

> **(the small finite generation ceiling is `3`)  <==>  (spacetime is 4-dimensional).**

This is the strongest candidate for a forced discrete RELATION (not just "the count is 3"). Persona 2 grades it.

### Persona 2 -- referee: grade forced-vs-input and novel-vs-known, ruthlessly

- **D1-D4 are FORCED** (choice-independent: none moves with `mu_DW`, `alpha`, or the residual shape `beta/alpha`)
  -- but **all four are ALREADY KNOWN**. "The ceiling is 3", "the count is 3-primary in `{1,3}`", "the count
  arena is coprime to the chirality arena", "7 DOF" are each established in-repo. As *new* forced facts they
  score **zero on novelty** -- exactly the brief's warning ("the count is 3-primary is known/an-input").
- **D5-D6 would be NOVEL discrete relations** (the exact `-38 = 19 sigma/8` index, the `(0,2,1)/3` order-3 class,
  the relation `rho_B = rho_A + 2 rho_Dirac`) -- but they are **NOT FORCED**: they exist only *if the carrier bit
  is B*, and the carrier bit is an arithmetic-undecidable field-space declaration riding SG4 (H39 Q4). A
  quantity that flips with a residual choice is by definition not a family-invariant. Score **fails Q-forced**.
- **The `d=4 <-> ceiling-3` relation (the sharpened D1):** forced YES. Novel? It is GU-native (Weinstein's "3
  from the 4-geometry"), not standard SM lore, so **partially novel** -- but it is *arithmetically the same
  object* as the known anchor `3 = dim Lambda^2_+`, dressed as a correlation. The referee will not credit it as a
  genuinely new fact; it is a re-reading of the known anchor.

**Referee verdict:** the forced discrete facts are the known ones; the novel discrete relations are not forced.
No fact clears BOTH "forced across the family" AND "new beyond the established discrete set". Demand met by
none.

### Persona 3 -- intra-team adversary (PRESENTS, does not veto)

Two attacks, both landing:

**Attack 1 (already-an-input / already-known).** "Every forced discrete fact you list is either an SM input or
already in your own canon. `d=4` is an *input* (you assumed a 4-base). `N_gen <= 3` restates `3 = dim
Lambda^2_+`, which H38 established a year of waves ago. `7 DOF` is H53's row 5, and H53 itself proved it is not
an accessible observable. You have re-derived your own file headers. A forced fact that is already known is a
compression, not a prediction (the H34 trap)."

**Attack 2 (forced-but-not-discriminating / choice-dependent-if-novel).** "Take the `d=4 <-> ceiling-3`
relation, your best card. (i) It is a CEILING, not a value: it is equally satisfied by 1, 2, or 3 generations,
so it does not even predict `3` -- it accommodates the observed 3. (ii) It is not testable: you cannot vary the
spacetime dimension, so 'were `d != 4` the ceiling would differ' is a counterfactual with no experiment. (iii)
Any 4D theory built on self-dual 2-forms (any `Lambda^2_+` gauge sector -- instantons, self-dual GR, Ashtekar,
Plebanski) carries the same `dim = 3`; it does not discriminate GU from a generic 4D self-dual construction. So
the one forced-and-novel-ish relation is not discriminating. And the genuinely sharp discrete data (`-38`,
`(0,2,1)/3`) that *would* discriminate are carrier-choice-dependent -- not forced."

Both attacks are presented, not vetoed. The synthesizer weighs them.

### Persona 4 -- cross-checker: verify the discrete structure independently

- **`dim Lambda^2_+(R^4) = 3` and the `d=4` uniqueness:** verified from the Hodge-star degree map `Lambda^k(R^d)
  -> Lambda^{d-k}`; a self-dual 2-form *eigenspace* requires `d-k = k = 2 => d = 4`. For `d = 4`, `dim Lambda^2 =
  C(4,2) = 6`, `*^2 = +1`, split `3+3`. For `d = 6, 8`, `dim Lambda^2 = 15, 28`, and the star sends `Lambda^2` to
  `Lambda^4, Lambda^6` (no 2-form self-duality). Reproduced exactly in the test.
- **`pi_3^s = Z/24 = Z/8 (+) Z/3`; 3-primary `= Z/3`; `gcd(2,3) = 1`; `|Hom(Z/2, Z/3)| = 1` (zero map only):**
  reproduced. The count arena and the chirality/ghost arena are arithmetically orthogonal -- confirmed
  independently of H39.
- **Oddness -> `{1,3}`:** the order-3 element on `R^3 = V0 (+) V1` (trivial (+) 2-dim rotated) has invariant
  subspaces `{0, V0, V1, R^3}`, dims `{0,1,2,3}`; a real conjugation-stable subspace is odd-dim iff it contains
  the fixed line, so odd ranks `= {1,3}`. Reproduced.
- **Residue trap:** `tr(g | R^3) = 1 + 2 cos(2 pi/3) = 0`; a net count of `3` has `3 = 0 (mod 3)` = the
  trivial/fixed-axis residue. So no order-3 class function separates "3" from "0-mod-3". Reproduced -- this is
  the exact obstruction that keeps the *value* (as opposed to the *menu*) from being a forced discrete datum.
- **Carrier non-invariance:** `ind_A = -42` (residue `0`), `ind_B = -38` (residue `1`) differ; the discrete
  arithmetic that would be novel is literally not a single number across the family. Reproduced.

All five structural facts check out. The cross-checker adds one negative control: there is **no** discrete
quantity in the built content that is (forced AND novel AND discriminating) simultaneously -- searched over the
count, the DOF, the carrier arithmetic, the arena split.

### Persona 5 -- synthesizer: the verdict

The discrete structure is genuinely rich, and every piece of it is real. But it partitions cleanly:

- **Forced ∩ known:** ceiling `3`, `{1,3}` 3-primary, 2/3-primary arena split, 7 DOF. (D1-D4.)
- **Novel ∩ not-forced:** the `-38` carrier index, `(0,2,1)/3` rho, `rho_B = rho_A + 2 rho_Dirac`. (D5-D6.)
- **Forced ∩ arguably-novel ∩ NOT-discriminating:** the `d=4 <-> ceiling-3` lock (sharpened D1).

The intersection **forced ∩ novel ∩ discriminating is EMPTY** in the built content. Branch D's honest 2-bit
statement is the third bullet, and it must be graded honestly as sub-headline.

---

## 2. The single strongest forced-and-novel discrete statement (the "2-bit prediction"), graded honestly

> **Bit A (forced, known):** the generation count is a **3-primary** datum living in `Z/3 subset pi_3^s`, with
> ceiling `dim Lambda^2_+(R^4) = 3`; it is arena-orthogonal (`gcd = 1`, `Hom = 0`) to the 2-primary
> chirality/ghost sector, so no 2-primary invariant (mod-2 index, ghost parity, Rokhlin) can certify it.
>
> **Bit B (forced, arguably-novel, NOT discriminating):** that ceiling equals `3` **only because spacetime is
> 4-dimensional** -- the self-dual-2-form mechanism exists solely at `d = 4` -- so the framework forces the
> correlation *(finite generation ceiling `= 3`)  <==>  (`d = 4`)*, independent of every free scale.

This is forced across the entire family and is not a re-derivation of a *continuous* known result. But it is (i)
a **ceiling, not a value** (it accommodates the observed 3; it does not predict it over 1 or 2), (ii) **not
operationally testable** (spacetime dimension is not a dial), and (iii) **not discriminating** (shared by every
4D self-dual construction). It is, arithmetically, the known anchor `3 = dim Lambda^2_+` re-read as a
correlation. **Honest grade: a real forced discrete structure, not a new forced discrimination.**

## 3. Why the genuinely-new discrete relation is NOT forced (the load-bearing barrier)

The relation that WOULD be a headline -- *the count is exactly the integer `3` (not merely `<= 3` or `in {1,3}`),
carried by the index-changing carrier B with `ind = -38` and order-3 class `(0,2,1)/3`* -- is blocked from being
forced by two established facts:

1. **The residue trap (order-3 class-function no-go, H40 / branch-D wave-3, reproduced here).** A net count of
   `3` sits in residue `0 (mod 3)`, identical to carrier A / the trivial sector; `tr(g|R^3) = 0`. No order-3
   class function certifies the integer `3`. So the *value* is not a forced discrete datum even granting the
   3-primary arena.
2. **The carrier bit is a free declaration (SG4, H39 Q4).** A vs B is arithmetic-undecidable on the field-space
   choice; only B carries the count, and which one the (unbuilt) source action names is exactly the residual
   freedom this wave is trying to see past. So `-38`, `(0,2,1)/3`, and "exactly 3" all ride a choice -- they are
   **choice-dependent, not family-invariant.**

Everything novel is behind the same unbuilt object (the source action / SG4 / C1) that gates the whole program;
nothing discrete escapes to the family-invariant side.

## 4. Graded verdict

| Question | Verdict | Grade |
|---|---|---|
| **Q-forced** | The forced discrete facts are D1-D4 (ceiling `3`, `{1,3}` 3-primary, 2/3-primary arena split, 7 DOF) -- genuinely choice-independent. The novel-looking discrete relations (D5-D6: `-38`, `(0,2,1)/3`, "exactly 3") are **NOT forced** (ride the carrier bit / SG4 / residue trap). | THEOREM for D1-D4 forced; THEOREM for D5-D6 not-forced (carrier-dependent) |
| **Q-novel** | **NO** for the forced set: D1-D4 are all already established in-repo. The one forced-and-arguably-novel item (the `d=4 <-> ceiling-3` correlation) is a re-reading of the known anchor `3 = dim Lambda^2_+`, not a new fact. | ARGUED (strong) |
| **Q-disc** | **NO.** The forced discrete facts are ceilings/menus that accommodate known SM numbers (3 generations, 4D), or are non-observable (7 DOF, H53), or are shared by any 4D self-dual construction. None is a discriminating, testable discrete prediction. | ARGUED (strong) |
| **Overall** | **NO NEW FORCED-AND-DISCRIMINATING DISCRETE FACT.** The discrete content is real and forced but ALREADY KNOWN; the genuinely-new discrete relations are choice-dependent. The honest Lakatos register: the discrete structure is genuine but not a new prediction. | see rows |

**Does it go beyond known discrete facts?** **No.** Beyond the known set (ceiling 3, `{1,3}`, 3-primary,
2/3-primary split, 7 DOF) there is no *forced* discrete relation; the only new relations are carrier-choice-
dependent.

**Does it discriminate?** **No.** The forced discrete facts either match known data (accommodation) or are
non-observable or are generic to 4D self-dual theories.

**The single strongest forced-and-novel discrete statement:** the `d=4 <=> generation-ceiling-3` correlation
(Section 2, Bit B) -- forced and GU-flavored, but a ceiling not a value and not operationally discriminating.

**Load-bearing assumption:** that "the count" = realized rank of a `Z/3`-invariant `V <= Lambda^2_+(R^4)`, and
that the carrier bit (A vs B) is the residual freedom rather than something the family forces. If a future
result forces the carrier to B *and* closes the residue trap (both currently open, SG4-gated), the `-38` /
`(0,2,1)/3` arithmetic would upgrade from choice-dependent to forced -- and D5-D6 could become a genuinely novel
forced discrete relation. Absent that, they stay choice-dependent.

**Confidence:** **HIGH** that no NEW forced-and-discriminating discrete fact exists in the built content (the
forced/known and novel/not-forced partition is elementary and machine-checked). **MEDIUM-HIGH** that this is the
final word rather than a demonstrated-absence -- like H53, it is an absence found after adversarially sweeping
the discrete sectors (count, DOF, carrier arithmetic, arena split), not an impossibility theorem.

## 5. What this changes (and does not)

Confirms the pre-registered outcome (b) for branch D: the honest register is that the discrete content is
genuine but not a new forced prediction. No `CANON.md`, `RESEARCH-STATUS.md`, claim-status, verdict, or public
posture change. The generation count stays OPEN, located-not-forced; the carrier bit stays SG4-gated. Branch D
returns **NO discrete headline**, cleanly -- the value the brief asked for is the honest negative, plus the sharp
localization of *why* (residue trap + carrier-freedom), and the one sub-headline correlation (`d=4 <-> 3`) named
and honestly demoted.
