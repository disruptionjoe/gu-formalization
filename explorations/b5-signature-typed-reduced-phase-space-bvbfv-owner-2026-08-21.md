---
title: "B5 signature-typed reduced phase space/BV--BFV owner: neutral boundary phase data exist, but no gravitational lapse does"
status: active_research
doc_type: exact_current_action_boundary_phase_owner_result
created: "2026-08-21"
registry: lab/process/b5-signature-typed-reduced-phase-space-bvbfv-owner.json
probe: tests/channel-swings/b5_signature_typed_bvbfv_owner_probe.py
grade: "EXACT FIRST-ORDER ANTISYMMETRY, HELD NORMAL-SYMBOL RANK/INERTIA, SPIN CENTRAL-CHARACTER AND CURRENT FIXED-BACKGROUND ACTION-INVENTORY GRADE"
target_claim: internal target B5-SIGNATURE-TYPED-REDUCED-PHASE-SPACE-BVBFV-OWNER-PACKET; verdict strict neutral classical boundary phase precursor exists, but its normal multiplier is an odd spinor and no bosonic scalar lapse, physical boundary ensemble, BFV charge algebra, quantum measure or conserved positive pairing is owned
canon_verdict_change: none
---

# B5 signature-typed reduced phase-space/BV--BFV owner

> **GU-COMPARATOR-ROUTING — scope before inference.** This artifact contains or
> borders a conventional particle-physics comparator. Any result about a
> standard Higgs/VEV, ordinary family index or net chirality, SO(10) `126`
> Majorana mechanism, anomaly selector, VEV-only breaking or familiar vector-
> mass route binds only that named model. It is not evidence for or against
> Weinstein's source-native mechanism without an explicit typed bridge. Read
> `lab/methods/source-native-comparator-routing.md` and follow its source-native
> pointers before reusing this result.

`GU-COMPARATOR-ROUTING-CLASSIFICATION: BRIDGE_OR_SEMANTIC_BOUNDARY`

Scope: the repository-constructed strict massless fixed-background
Rarita--Schwinger action and four-stage linear gauge/BV complex on the named
flat complexified plus-first `(9,5)` half-cylinder. It is not dynamical gravity,
the graph-mixing Stage-B Euler family, Weinstein's unreleased source action, a
complete quantum BV--BFV theory, or the source-aligned `(7,7)` horn.

```gu-typed-objects
result: the strict action supplies a neutral classical linear boundary phase precursor and spinor-valued constraint, but its normal multiplier is odd rank-128 spinor data and not an even scalar ADM/Jacobson lapse
carrier: U1=V* tensor S rank1792 with normal gauge radical n tensor S rank128 and boundary quotient rank1664 over the positive-normal boundary of the flat (9,5) half-cylinder LAYER=ambient CHIRALITY=S-FULL-DIRAC
pairing: program-native Green boundary form descending to neutral inertia (832,832) on the kinematic radical quotient, distinct from the auxiliary positive graph topology ON=independent-B5-curved-strict-carrier
real_structure: relative Gamma-natural antilinear coflip on the strict complex; no source-owned time orientation or physical boundary real form is added
grading: fermionic Rarita--Schwinger field and parameter with BRST parity shift; the normal field component enforces a spinor constraint rather than a bosonic Hamiltonian constraint
action_owner: repository-construction strict fixed-background quadratic massless Rarita--Schwinger action; the metric is not varied and no scalar lapse field occurs
target: typed comparison of the strict canonical boundary multiplier and constraint with a gravitational lapse/constraint before any contour, ensemble, edge, entropy or probability transfer MAP-TYPE=evaluation
```

## Result first

The strict action has enough structure for a **classical linear covariant
boundary phase precursor**, but it has no Jacobson/ADM lapse analogue in its
declared field content.

For the first-order quadratic expression

```text
S_RS[psi] = integral <psi_a, Gamma^{a b c} partial_b psi_c>,
```

choose the held positive non-null half-cylinder normal `n=dr`. Complete
antisymmetry of `Gamma^{a b c}` makes every normal-derivative incidence
`Gamma^{a n c}` tangential in both `a` and `c`. Thus the normal component
`psi_n` carries no normal derivative. Its variation enforces the tangential
spinor constraint

```text
C(q) = Gamma^{n i j} partial_i q_j = 0,
```

with the adjoint constraint obtained after tangential integration by parts.
The normal component is therefore a multiplier, but it is a Grassmann-odd
rank-128 spinor multiplier for a spinor-valued constraint. An ADM lapse is a
Grassmann-even scalar multiplier for an even scalar Hamiltonian constraint.
They are not the same object.

There is not even a nonzero Spin-equivariant linear map that silently turns
the current multiplier into a scalar. The central element `-1` of Spin acts as
`-1` on the spinor and trivially on the scalar. For an equivariant linear map
`f:S->1`, equivariance gives `f(-s)=f(s)` while linearity gives
`f(-s)=-f(s)`, hence `f=0` in characteristic zero. Choosing a spinor covector
would add exactly the external selector the packet is required not to invent.

So the typed verdict is:

```text
neutral classical boundary phase precursor: available
spinor constraint and multiplier: available
bosonic scalar Hamiltonian constraint/lapse: absent from current action
Jacobson real lapse contour: wrong type
physical boundary/ensemble: not owned
quantum BFV measure and convergence test: not owned
boundary charge/observable algebra: not owned
conserved positive reduced pairing: not owned
```

## Signature-correct boundary carrier

The signature convention is plus-first. The strict packet stands on ambient
`(9,5)`, and the selected half-cylinder normal is positive. Its tangential
boundary therefore has signature `(8,5)`, not a one-time or Riemannian Cauchy
slice. The source-aligned `(7,7)` horn is a different real Clifford problem and
is not inferred from this result.

At the held exact normal symbol,

```text
dim U1 = 14*128 = 1792,
radical = im A_n = n tensor S,       dim radical = 128,
dim(U1/radical) = 1664,
inertia(G_n on U1/radical) = (832,832).
```

Thus the Green boundary form descends to a nondegenerate neutral kinematic
quotient. This supplies the action-owned boundary pairing needed before a
classical BFV reduction. It is not yet the physical reduced phase space:
tangential constraint reduction, gauge quotient, a BFV charge, global analytic
domain, physical boundary condition, and any quantum measure remain separate.

The held stage-compatible result already proves that the exact marked `e4`
class survives the algebraic and reduced hit gauge quotients and is excluded by
the miss domain. That is compatible with the constraint-first ordering here.
It does not make the class an edge mode. Edge status requires a boundary charge
and observable algebra, neither of which the strict packet supplies.

## The Jacobson battery, applied without type collapse

| stage | current strict B5 disposition | reason |
|---|---|---|
| full constraint and gauge projection | available only at classical linear marked-class grade | the strict complex, closed hit/miss realizations and bounded separator exist |
| physical boundary and ensemble | owner absent | the flat half-cylinder is a mathematical test boundary, not a source-selected causal diamond, horizon or observer system |
| both-sign multiplier integration | wrong type | the multiplier is an odd spinor; there is no even real scalar lapse to integrate above and below zero |
| short-distance vacuum/fluctuation convergence | owner absent | no quantum measure, contour or physical Gaussian state-count object is defined |
| boundary charge/observable algebra | owner absent | no BFV boundary charge or algebra identifies the marked class as an edge observable |
| conserved positive pairing on reduced cohomology | owner absent | the prior exact result proves positive completions exist but remain domain/metric-unselected |

Constraint-first ordering survives as a method port. Direct lapse-contour
transfer closes as `WRONG_TYPE` on the current fixed-background action. The
remaining boundary/ensemble and convergence tests remain useful design
requirements, but they have no current object to evaluate and therefore select
neither `L_hit`, `L_miss`, a proper extension subclass, none, nor all.

## Ownership and claim ceiling

The absence result is inventory-relative and exact: the strict action varies a
vector-spinor on a fixed metric background and contains no independent even
scalar lapse or dynamical metric momentum. It does **not** prove that no lapse
can arise after coupling a dynamical metric/coframe, selecting a different
action, building a source-global background, or introducing a typed physical
boundary theory. A bilinear spinor composite is also not the missing linear
multiplier; using one would change the action and require its own owner.

No complete quantum BV--BFV theory, total cohomology comparison,
self-adjointness, Fredholm theorem, positive physical state space, entropy law,
source-selected global `Met(X)`, particle result, canon movement, public
posture change, or GU verdict is claimed.

## Reproduction and continuation

`tests/channel-swings/b5_signature_typed_bvbfv_owner_probe.py` certifies the
fourteen-dimensional antisymmetric incidence, absence of normal kinetic data
for `psi_n`, exact boundary-rank/inertia arithmetic, plus-first signature
descent, Grassmann and representation mismatch, the Spin central-character
obstruction, field inventory, six-stage battery dispositions, and hostile
controls.

The direct B5 lapse port is now closed on the current action. Reopen with
`B5-DYNAMICAL-GRAVITY-OR-PHYSICAL-BOUNDARY-BFV-REOPENER`: either an
action/source-owned dynamical metric or coframe yielding an even scalar
Hamiltonian constraint and lapse, or a named physical causal boundary and
ensemble with a BFV charge and observable algebra. Any reopened packet must
also carry the selected closed domain, gauge descent, measure/convergence
object where invoked, and a conserved positive pairing on reduced physical
cohomology. Until then the Jacobson methods remain banked on the broader
gravity/entropy frontier rather than imported into this strict B5 model.
