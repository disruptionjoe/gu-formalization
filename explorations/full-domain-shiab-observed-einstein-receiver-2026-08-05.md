---
artifact_type: exploration
created: 2026-08-05
title: "The selected degree-two Shiab's Riemann-response factor route cannot carry the restriction-first Einstein target; a pre-Shiab Gauss receiver survives"
grade: "EXACT local algebraic no-factor theorem plus a parameter-free Gauss receiver formula; first variation and equation dual are replayed on a rational fixed-orthogonal fixture. Source-action Euler ownership, moving-section closure, global descent and a physical Green/BFV domain remain open."
named_gate: FULL_DOMAIN_SHIAB_ACTION_ADAPTER_PLUS_OBSERVED_EQUATION_RECEIVER
gate_before: SELECTED_AMBIENT_SHIAB_KNOWN__OBSERVED_EQUATION_RECEIVER_OPEN
gate_after: SELECTED_POST_SHIAB_FACTOR_ROUTE_KILLED__PRE_SHIAB_GAUSS_RECEIVER_CONSTRUCTED_LOCALLY__ACTION_OWNERSHIP_OPEN
route_disposition: KILL_SELECTED_POST_SHIAB_RECEIVER__ADVANCE_PRE_SHIAB_GAUSS_ACTION_OWNER
source_collision: SOURCE_CONFIRMS_INGREDIENTS__SOURCE_SILENT_ON_RECEIVER
fork_assumed: SIGNATURE-AMBIENT
fork_stack_acknowledged: "Lorentz (1,3) horizontal plus trace-reversed Frobenius (6,4) vertical gives the active (7,7) horn; no K95 result is imported."
search_space_dim: "complete 10-dimensional observed Sym2 target inside the Bianchi-selected candidate's complete Riemann kernel; zero fitted coefficients"
free_object_delta: 0
residue_touched:
  - id: LT-GR1
    grade: T0
    note: EVIDENCE_ONLY_NO_GRADE_MOVEMENT
  - "LT-GR1b:T4"
canon_verdict_change: none
---

# The selected Shiab's Riemann-response factor route cannot carry the restriction-first Einstein target

## Result first

The proposed receiver depending only on the selected degree-two Shiab output
does not merely need a better normalization. It cannot factor the
restriction-first Einstein target on the declared algebraic-Riemann carrier.

Let the active tangent space split as `E = H + N`, with `dim H = 4` and
`dim N = 10`. On algebraic Riemann curvature, the Bianchi-selected candidate
member of the displayed adjoint-Shiab family has the already-certified
complete response

```text
S_sel I_Riem = -2 J G_14.
```

The new exact result is

```text
rank( G_4 res_H | ker(G_14) ) = 10.
```

Every possible observed symmetric two-tensor occurs on curvature that the
candidate sends to zero. The predecessor theorem identifies `-2 J G_14` as
the complete candidate output on the algebraic-Riemann carrier, so its
`Cl^5` component also vanishes there. Therefore there is no deterministic
linear **or nonlinear** map `A` satisfying

```text
G_4(res_H R) = A(S_sel(R))
```

on the declared Riemann carrier. Zero curvature and the kernel witnesses have
the same Shiab output but different observed outputs. An invertible
Hodge/Krein primalizer, the exact four-plus-ten receiver, or any other
downstream map cannot reconstruct information already erased upstream.

This is a row-local route kill, not a kill of GU, K77, the Bianchi-selected
displayed-family candidate in its other possible roles, the complete `E_act`
with its additional action-owned terms, or observed gravity generally.

## Layer 0: the objects are not synonyms

| label | actual object | role here |
| --- | --- | --- |
| Bianchi-selected adjoint-Shiab candidate | `Omega^2(Y,ad P) -> Omega^13(Y,ad P)` | one candidate member of the displayed action family; its complete Riemann response is `-2 J G_14`; action/Helmholtz selection remains open |
| reconstructed spinor contraction | later Clifford/spinor map | separate operator; its Riemann decoding is Ricci and has the same kernel witness |
| ambient Einstein contraction | `Riem(TY) -> Sym^2(T*Y)` | loses ambient Ricci-flat/Weyl data |
| observed Einstein contraction | `Riem(TX) -> Sym^2(T*X)` after section restriction/Gauss | sees horizontal curvature hidden in the ambient Ricci-flat kernel |
| fibre trace reversal | bilinear form on `Sym^2(T*X)` | supplies the vertical metric/Hodge/pairing; not an Einstein trace |
| field observation | `(s*,res_s^V)` | exact four-plus-ten field coordinate map |
| equation observation | cotangent dual/inverse transpose | preserves field/equation pairing but cannot undo upstream annihilation |
| induced defect variation | moving section, density, normal jets and `II` | live route that can see pre-Shiab curvature |

The source confirms the upstairs action, augmented torsion and section/pullback
grammar. It is silent on the faithful Einstein receiver and on the claim that
the Gauss map below is already the full K77 Euler map.

## Exact kernel theorem

Use pseudo-orthogonal `H` and `N` metrics. Define an algebraic curvature tensor
by sectional weights

```text
k_HH = 1,  k_HN = -3/10,  k_NN = 2/15.
```

Every horizontal ambient Ricci row is `3 - 10(3/10) = 0`; every normal row is
`-4(3/10) + 9(2/15) = 0`. Hence `Ric_14 = G_14 = S_sel = 0`. Its horizontal
restriction is constant curvature one, so `G_4 = -3 g_H`, nonzero.

That scalar example is only one direction. For every horizontal Lorentz-
tracefree `S`, set

```text
R_S = S KN g_H - (1/5) S KN g_N.
```

The first term contributes `2S` to horizontal ambient Ricci and the second
contributes `-2S`; normal Ricci vanishes because `tr_H S = 0`. But restricting
first gives `G_4(R_S|H) = 2S`. Six off-diagonal plus three diagonal tracefree
choices span the nine-dimensional traceless sector. Together with the scalar
witness, their observed outputs have exact rank ten.

The SymPy certificate checks every family member coefficientwise. An
independent Sage/QQ reconstruction reproduces ambient Ricci zero, observed
rank ten and the Gauss first variation without importing the SymPy code.

## One parameter-free local surviving construction

Observation must retain gravitational curvature **before** the candidate
ambient contraction. On the declared gravitational curvature carrier use the
canonical algebraic-Riemann projector `P_R`: antisymmetrize each index pair,
symmetrize the pair exchange, then remove the four-form/Bianchi component.

For a supplied section `s` with second fundamental form `II_s`, define

```text
Gamma_s(F,II) = res_H(P_R F) + Q(II),
Q(II)_abcd = <II_ac,II_bd>_N - <II_ad,II_bc>_N,
E_obs(F,II) = G_4(Gamma_s(F,II)).
```

The coefficients are fixed by the algebraic Riemann and Gauss identities;
none is fitted to Standard Model or GR output. The construction:

- accepts the full declared gravitational `Cl^2` curvature through a canonical
  projection instead of silently calling generic adjoint curvature “Riemann”;
- detects the complete candidate-Shiab Riemann-kernel witness;
- obeys Riemann pair symmetries and algebraic Bianchi;
- is tensorial under the `O(H) x O(N_s)` stabilizer of the fixed orthogonal
  product fixture;
- leaves the Bianchi-selected candidate as a separate displayed-family
  operator whose action/Helmholtz selection is still open;
- does not construct the generic `Cl^5` or normal-residue equation blocks;
  those remain explicit obligations for the next total receiver.

Here `N_s = ds(TX)^perp` is the metric-normal bundle used by Gauss. The earlier
four-plus-ten receiver uses the canonical vertical bundle `V_s = ker(d pi)`.
They coincide in this orthogonal block fixture but need not coincide for a
moving or nonhorizontal section. The section-dependent `N_s <-> V_s`
transition is unbuilt. It does not establish full moving inhomogeneous-gauge
equivariance or global Observerse descent.

## First variation and equation dual

The local derivative is forced:

```text
D Gamma_(F,II)[dF,dII]
  = res_H(P_R dF) + DQ_II[dII],

DQ_II[dII]_abcd
  = <dII_ac,II_bd> + <II_ac,dII_bd>
    - <dII_ad,II_bc> - <II_ad,dII_bc>.
```

The formula follows coefficientwise from the product rule; the probe replays
four nontrivial slots as exact dual-number coefficients. On a rational fixture
with two live `II` coordinates it constructs the Jacobian of the ten observed
equations and verifies the equation receiver is its transpose by the pairing
identity. This is a formal local cotangent dual, not an analytic adjoint on a
closed Krein/Green domain.

## Variational boundary: why the wave stops here

The no-duplicate weld keeps `I1B` once upstairs. Adding a second localized
copy would be a new theory with profile, normal-density and double-counting
debts. The honest successor is instead a jet-level Euler-receiver square:

1. begin with the already-built action Euler
   `E_act = S(barF) + (D_T barF)^! S^! T + * kappa T`;
2. differentiate the complete observation jet, including the four-plus-ten
   coefficients, retained normal first jets, section, density, Hodge/Krein,
   soldering and both connections;
3. use its cotangent dual, whose zero-jet connection block is the existing
   `M^-T` receiver;
4. construct and split the result into the observed Einstein block plus the
   currently open normal and `Cl^5` residue blocks;
5. compare with direct tagged variation of the one bulk action and the full
   Noether totalization, not the printed endpoint or `D_B E_act` alone.

This wave has constructed the tensor map needed by that square. It has **not**
proved that the K77 source action emits it.

## Seven-axis audit

### Layer 0 precondition

Cleared as typed above. In particular, `G_14`, `G_4`, the fibre trace reversal,
field pullback, equation dual and defect variation are not collapsed.

### L1 — object and carrier

The no-go is on the complete algebraic Riemann carrier inside the
Bianchi-selected candidate's adjoint-curvature domain. The repair acts on generic pair curvature only after
the explicit canonical Riemann projection; other Clifford grades remain named
extra blocks.

### L2 — symmetry and naturality

The kernel theorem is basis-independent under the product stabilizer. The
repair formula is tensorial on the fixed orthogonal `H + N_s` fixture. The
normal-to-vertical transition, full moving IG equivariance and
compensator-aware descent are open.

### L3 — differential/action owner

The local first variation and cotangent dual are exact. Equality with the full
K77 action Euler, including moving `s`, density, Hodge, soldering and normal
jets, is open.

### L4 — constraints and quotient

No gauge/BV quotient is used to delete the ten-dimensional kernel. A future
restricted-domain revival must independently derive
`ker(S_sel) intersect D subset ker(G_4 res_H)`; it may not declare the kernel
gauge after the fact.

### L5 — domain and pairing

Finite rational pairings and the formal transpose are exact. No positive
Hilbert replacement, closed Krein domain, Green operator or BFV phase space is
claimed.

### L6 — observed physics identification

The target tensor is the four-dimensional Einstein tensor of the Gauss
curvature. This is a conditional mathematical target, not yet the physical
stress-coupled Einstein equation.

### L7 — prediction and falsification

The selected post-Shiab factor route is exactly falsified. The alternative has
a sharp next test: direct tagged variation must reproduce the Gauss receiver
coefficientwise while retaining all extra blocks and Ward owners.

## Ledger movement

- `LT-GR1b`: `OVER_DETERMINED/SCOPE_ERROR` becomes
  `OVER_DETERMINED/GENUINE_FALSIFICATION` for the **selected post-Shiab
  factorization route**, recorded in the row's explicit `construction_scope`.
  The row is not read as a falsification of GU gravity.
- `LT-GR1`: remains exactly at its v0.1 `NEEDS/ONE_BIT` grade and distance. The
  Gauss construction is appended evidence, not a resolution or tightening of
  the `|II|^2` versus mean-curvature-norm functional horn.
- `LT-SM3b`, `LT-GR5`, `LT-GR6`, all anomaly/count rows and P1/P2/P3 do not
  move.

The v0.1 ledger remains immutable. The changes appear as explicit migrations
in v0.2. Counts are unchanged, but route tightness improves; a flat verdict
count is not zero progress.

## Next gate

`OBSERVED_GAUSS_RECEIVER_SOURCE_ACTION_OWNERSHIP_AND_MOVING_DEFECT_VARIATIONAL_CLOSURE`

Exit only when direct tagged variation of the single K77 bulk action agrees
coefficientwise with the complete observation-jet cotangent dual, including
the observed Einstein block, `Cl^5`/normal residue, moving section and density,
Hodge/Krein, soldering, normal jets and the full Ward/Noether totalization.

## Exact scope

Killed:

- selected post-Shiab ambient-to-observed Einstein factorization;
- every scalar normalization of naive restriction;
- recovery by downstream primalization or the four-plus-ten receiver;
- direct degree-13 form pullback as an equation map.

Alive:

- pre-Shiab curvature restriction and the Gauss correction;
- the Bianchi-selected candidate as a displayed-family ambient operator;
- action-derived restricted domains, if independently constructed;
- the one-bulk-action moving-defect Euler square;
- K77 and GU generally.

Untouched: observed GR, Standard Model recovery, cosmology, anomaly
cancellation, global domain, P1/P2/P3, Wave 3, canon and public posture.
