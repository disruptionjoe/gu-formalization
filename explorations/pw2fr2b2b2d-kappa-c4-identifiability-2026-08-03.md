---
title: PW2F-R2B2B2D — active kappa1 quartic identifiability
date: 2026-08-03
status: NORMAL_KAPPA1_C4_SUBBANK_CONSTRUCTED__RELAXED_U4_COMPLETIONS_RETRACTED_BY_SUCCESSOR
lane: Eric-guided Lane 1; source, active reconstruction, and Curt comparator kept separate
run: RUN-20260803-110644-gu-formalization-pw2fr2b2b2d-kappa-c4-identifiability
registry: lab/process/pw2fr2b2b2d-kappa-c4-identifiability-registry.json
probe: tests/channel-swings/pw2fr2b2b2d_kappa_c4_identifiability_probe.py
---

# Result

> **2026-08-03 successor correction.** The two objects later called
> “mathematical completion witnesses” were only relaxed homogeneous-quartic,
> owner-symmetric constraint-ledger completions. They did not prove geometric
> realizability or full-slot non-identifiability. R2B2B2E subsequently built a
> dependency-complete quadratic-distortion jet ledger and proved that every
> non-normal return stops at degree three or below on the conditional active
> principal branch. Thus its conditional principal `U4` bank is zero and the
> normal bank survives. The historical record below is retained to preserve
> how the missing construction was exposed.

The first complete quartic sub-bank on the corrected active source tangent is
now constructed, but hostile review shows why it is not yet the complete
`kappa1` coefficient or the complete `I1` bank.

The pinned primary-source packet explicitly places

\[
\frac{\kappa_1}{2}\langle T,*T\rangle
\]

inside the first bosonic action. On the accepted conditional
fixed-`(epsilon,varpi)`, principal-`Z1`, observed-base reconstructed
Levi--Civita metric branch, the ten owner tangents are

\[
\delta B=\operatorname{Ad}_{\epsilon^{-1}}\delta\Gamma,
\qquad
\delta T=-\delta q
=\delta\Gamma-\delta B.
\]

Using this corrected tangent rather than the old
`deltaT=-deltaB_full` shortcut, the executable reconstructs the full
35-monomial homogeneous quartic bank of the normal mass-Hessian return

\[
M^{\rm normal}_{4,ij}(\eta)
=\langle\delta T_i^{(2)}(\eta),
*\delta T_j^{(2)}(\eta)\rangle .
\]

Seven of the 35 monomial matrices are nonzero and their flattened coefficient
rank is one. All 35 matrices are owner-symmetric. Three dense held-out
conormals reconstruct exactly, and direct two-parameter differentiation of
the mass action reproduces a live entry `-8/25`. The old shortcut gives a
different dense quartic tensor, so the source-coordinate correction is
actually exercised.

# Independent formal-adjoint and Green check

Every coefficient has total derivative order four, so its formal adjoint is
`(+1) A_alpha^T`. All 35 independently computed adjoints equal the original
matrices. On the dense native conormal ray `(1,-1,2,3)`, the active owner block
`(0,1,2)` has rank three and the separately built fourth-order Green
concomitant satisfies the exact bulk-minus-divergence identity. An asymmetric
coefficient plant fails the same formal-adjoint gate.

This closes formal-adjoint/Green verification only for the normal mass
subroute. The Green concomitant is boundary evidence, not a bulk cancellation.

# Hostile repair: normal is not complete off shell

The pre-registered attempt asked for the complete `kappa1` mass-slot bank.
Variational/PDE review rejected that grade. For the pulled action

\[
\frac{\kappa_1}{2}\langle T(g),R(g)T(g)\rangle,
\]

the off-shell metric Hessian also contains the background residual return

\[
\kappa_1\langle T,RD^2T\rangle
\]

and moving Hodge/density/Krein/lowerer/outer-pairing terms. The predecessor
order ledger admits those returns through order four, but their actual
coefficient values are not constructed. Therefore the full coefficient has
the typed form

\[
M_4=M^{\rm normal}_4+U_4,
\]

where `U4` remains open.

This was a decisive missing-construction diagnostic. The constraints at this gate knew only
that `U4` is quartic and must satisfy the owner/formal-adjoint conditions. Both
`U4=0` and `U4=-M4_normal` satisfy those checked constraints, while the first
leaves a nonzero full coefficient and the second cancels it. These are
relaxed algebraic constraint-ledger completion witnesses, not realized
geometric jets. They proved that R2B2B2D alone could not determine the full
`kappa1` C4 tensor; they did not prove geometric non-identifiability.

Consequently the complete first-action bank is presently

\[
C^{I1}_4=A_4+\kappa_1
\left(M^{\rm normal}_4+U_4\right),
\]

with the transgression/moving-coefficient bank `A4`, the missing `U4` returns,
and the active value/normalization of `kappa1` all open. The result does not
prove that the actual total `kappa1` coefficient is nonzero; `U4` may cancel or
reinforce the normal bank.

# Layer 0 and source disposition

- `SOURCE-CONFIRMS`: the first bosonic action and the placement of the
  `kappa1/2` distortion-mass slot.
- `REPOSITORY-DERIVES`: the conditional active `(9,5)` rotor, corrected
  `deltaT=-deltaq` tangent, normal quartic mass bank, formal-adjoint/Green
  check, and relaxed-completion missing-construction diagnostic.
- `SOURCE-SILENT`: the active source-to-real-form port, active `kappa1` value
  or normalization, `U4`, `A4`, the complete `I1`/`I2B` banks, and physics.

Source epsilon, repository `h/u`, and reduction epsilon remain distinct.
Source `(7,7)` and active trace-reversed `(9,5)` pairings remain distinct.
`I1` and `I2B` remain separate actions. Source-explicit placement is not a
source-selected value.

# What this earns and what remains open

Earned:

1. a complete 35-monomial normal `kappa1` mass-Hessian C4 bank on the corrected
   conditional active tangent;
2. direct, held-out, formal-adjoint, and Green agreement for that subroute;
3. exact rejection of the old `deltaT=-deltaB_full` bank; and
4. a constructive proof that the relaxed order/symmetry ledger alone does not
   identify the full off-shell `kappa1` or `I1` C4 coefficients.

Still open:

- successor status: R2B2B2E proves the conditional active principal
  `T*D2T` and moving-pairing/lowerer `U4` bank is zero;
- the remaining first-action transgression/moving-coefficient `A4` bank;
- active `kappa1` value/normalization and the coefficientwise
  `NONE/ANY/UNIQUE(kappa1)` proportionality test;
- the separate complete off-shell `I2B` C4 bank and its independent Green
  reconciliation;
- vertical/mixed conormals, partial-`Z1`, section tangents, the global epsilon
  port, C3/C2, characteristic, analytic domain, BV quotient, observation, and
  physics.

The inherited observed-base pre-Shiab incidence and symbolic `J3=0` remain
unchanged. This quartic ambiguity does not reopen either closed C5 ceiling.
P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; the conjunctive
`TG-1 AND TG-2 AND TG-3` gate remains `NOT_PROMOTED`.

# Next gate

Resume at
`PW2F-R2B2B2F-I1-TRANSGRESSION-A4-AND-KAPPA1-PROPORTIONALITY`.
Construct the distinct `A4` bank, then run the
coefficientwise exceptional-`kappa1` classifier with independent Green
agreement. Only after the full `I1` family is typed should the already-admitted
off-shell `I2B` C4 bank be assembled.

# Executable evidence

- Main probe after successor correction: `18 exact + 2 source + 21 type + 4
  planted = 45` PASS on final replay.
- Normal bank: 35 monomials, seven nonzero blocks, flattened coefficient rank
  one, three dense held-outs, live direct entry `-8/25`.
- Independent check: 35/35 formal adjoints, live rank-three native-ray Green
  block, asymmetric plant rejected.
