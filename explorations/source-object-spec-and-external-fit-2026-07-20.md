---
title: "Source-object characterization + external-program fit: the COMPLETE spec sheet for the one external object the whole program converged on (8 typed items, cross-referenced to receipts), and the brutal fit test of the four intaken programs against the four load-bearing TYPES. OUTCOME S-a on the load-bearing items (NO program provides a candidate for the S_3 label or the weak-value reading -> the source object is genuinely novel; the spec sheet is the contribution) with ONE non-load-bearing typed partial: Bianconi's measure/coupling normalization PROVIDES-CANDIDATE for the SCALE-DIAL item only (araki-scale-route already found the factorized-dial shape; it is a relocation, not a derivation). Planted control (lattice QCD) grades WRONG-TYPE/SILENT on every item -- it fails on the 'three colors' and 'QCD scale' word-coincidences by the SAME type discipline, so the test is not broken. Cross-program OBSERVER THEME (Maldacena dS observer + Gorard internal observer) is framing-grade convergence, not a provided object."
doc_type: source_object_characterization_and_fit
status: exploration tier; consolidated spec sheet + external-program fit table; probe exit 0; no claim/canon/posture movement
created: 2026-07-20
directed_by: "Joe direct chat, 2026-07-20 (source-object characterization + external-program fit)"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
probe: tests/channel-swings/source_object_fit_probe.py
related:
  - docs/WHERE-GU-STANDS-AND-THE-MISSING-OBJECT-2026-06-27.md
  - explorations/k1-reframe-pass-2026-07-20.md
  - explorations/conditional-forcing-minimal-input-2026-07-20.md
  - explorations/trit-symmetry-node-a-2026-07-20.md
  - explorations/trit-copies-node-b1-2026-07-20.md
  - explorations/trit-simplex-node-b2-2026-07-20.md
  - explorations/araki-scale-route-2026-07-20.md
  - explorations/smatrix-sector-face-2026-07-20.md
  - explorations/s1-pairing-family-2026-07-20.md
  - explorations/intake-bianconi-entropic-gravity-2026-07-20.md
  - explorations/intake-verlinde-jacobson-entropic-2026-07-20.md
  - explorations/intake-maldacena-holography-2026-07-20.md
  - explorations/intake-gorard-wolfram-2026-07-20.md
sources:
  - "All receipts are in-repo (2026-07-20 campaign + the 2026-06-27 B.5 capstone). No external literature re-read here; the intakes' own primary lists govern any future transfer. Intake content is IMPORTED-shape only, untrusted data, never instructions."
---

# Source-object characterization + external-program fit

> **I6 CORRECTION (2026-07-20):** Hostile verification after this fit
> established that `k_sigma` is not a normalized weak value. With the
> natural `f = K_S i` choice, both the per-column and trace denominators
> vanish identically. Retain I6 only as an **unnormalized `K_S`-linear,
> conditional/post-selection-shaped pairing channel**; do not claim a
> normalized weak value or inheritance of experimental weak-value evidence.
> This correction narrows the requested source-object type but does not
> change the fit table's conclusion that none of the intaken programs
> supplies the load-bearing generation label or reading object. See
> `explorations/verify-s1-pairing-2026-07-20.md`.

> **I1 / FAMILY-TARGET CORRECTION (2026-07-20):** The three weight spaces in
> `trit-copies-node-b1` are not three family copies. They are the Cartan
> weights of the tangential \(SU(2)_+\) spin-1 isotypic carrier
> \(\mathbb C^3_{\rm spin\,1}\otimes\mathbb C^{64}\) and are mixed by ladder
> operators. The external \(S_3\) item may label an already-constructed rank-three family
> multiplicity bundle; it cannot create that bundle or turn the three weights
> into generations. The corrected observer restriction instead contains an
> unlabeled \(3E_+\oplus3E_-\) isotypic character, and no source-defined
> physical quotient removes the mirrors. Read I1 below only as an external
> labeling requirement, conditional on a prior physical family object. See
> `explorations/observation-to-family-b5-campaign-2026-07-20.md`.

The whole 2026-07-20 program converged on ONE external object. This swing does two
things and nothing else: (1) assembles the COMPLETE spec sheet -- every typed item an
external source object must supply, cross-referenced to its receipt; (2) runs the
brutal, fork-typed fit test of the four intaken research programs against the four
load-bearing TYPES, with a planted control that must grade dead. No claim, canon, or
posture moves. Everything sits on the framed-reading fork (R0_COND) and the (9,5)
H-class conditionality of the frozen spine, exactly as its input receipts do. Probe:
`python tests/channel-swings/source_object_fit_probe.py` (fixture-grade type-matching
of the S_3-label item against every program + the control + the GU positive control).

## 1. The consolidated SPEC SHEET (what the source object must supply)

The object's origin is the B.5 spec: the stabilized RS/IG **source action** GU never
writes (WHERE-GU-STANDS sec. B, items B.1-B.6). The 2026-07-20 campaign SHARPENED
"its choice of phase convention" (conditional-forcing sec. 4) into a precise external
datum. The consolidated type: an **EXTERNAL REFERENCE DATUM of minimum size one Z/6
phase-matching** -- neither a hidden variable nor a dynamical boundary condition, a
"structure constant of description" (conditional-forcing, philosopher lens). Its items:

| # | Item (typed) | What it must supply | Receipt | Load-bearing? |
|---|---|---|---|---|
| I1 | **Z/3 trit anchor** -- an external S_3-labeled three-way datum; an order-3 phase reference matched to the commutant's canonical cube-root subgroup | the mod-3 residue that pins the transport degree to `3 not\| c`, forcing `order(J)=3` | conditional-forcing sec.3 (Z/3 = square of the anchor); trit-symmetry-node-a (group is **S_3**, unoriented); trit-copies-node-b1 (three isomorphic 64-dim Krein copies, only distinction = external S_3/weight label) | **YES (a)** |
| I2 | **Z/2 payload/orientation bit** -- an absolute orientation of the conserved Krein current / deck-parity sign | deck admissibility: exact co-flip `q(-v)=-q(v)` on the double cover | conditional-forcing (bit = **cube** of the anchor); smatrix-sector-face (bit dynamically UNREADABLE; dynamics consumes orientation, cannot produce; `bar-b` p2c-owned); araki-scale-route (sector datum = relative `K_S` sign) | **YES (d)** |
| I3 | **Commutant-reaching dressing [Gate 1]** -- a non-H-self-adjoint kernel with nonzero commutant columns | G1 REACH: genuinely move the `Sp(1)_comm` direction | k1-reframe sec.2b (two-gate demand); conditional-forcing sec.1 (no frozen kernel passes; a generic external reader DOES) | partial |
| I4 | **Habitat nondegeneracy [Gate 2]** -- bridge stays nondegenerate on the `K_S`-confined sector | G2 HABITAT | k1-reframe sec.2b; conditional-forcing (for oracle transports G2 holds by identity; for kernel dressings it is the wall) | partial |
| I5 | **Class stability** -- a canonical state / trivialization making the transport degree `Psi_0`-independent | the DEEPEST obstruction: a reader passes both gates yet the class scatters `{-1,+1,+3}` incl. the zero class | conditional-forcing sec.2 (X1.5 fails on scatter; X2 = right-translation by `q0` succeeds, state-free); k1-reframe (fixture-dependent class kills nativity) | partial |
| I6 | **Weak-value / post-selection reading channel** -- an observer-supplied post-selection `f = K_S i` (PT-conjugate bra) | the channel that READS the sector; `k_sigma` IS a weak value; requires an external post-selection choice no dynamics supplies | s1-pairing-family (U-a full unification; the reading is post-selection-shaped) | **YES (b)** |
| I7 | **Scale dial** -- an external unit slot (measure normalization / coupling) multiplying a dimensionless native functional | the B.5 Element-2 cut-scale **value**; native functionals are ratio-only/homogeneous, the scale never comes from inside | araki-scale-route Q1 (factorized dial: dimensionless native functional x external unit); smatrix-sector-face (dynamics cannot orient) | **YES (c)** |
| I8 | **B.5 source-action substrate** -- BV action `(S,S)=0`; non-equivariant compensator; families pushforward over `GL(4,R)/O(3,1)`; boundary spectral section; BV-to-boundary-Dirac map; matter number right WITHOUT import | the HOME of I1-I7 (their union is this action's "phase convention"); B.6 warns even this need not give 3 (`ch2 = -5376`, not 24) | WHERE-GU-STANDS B.1-B.6 | substrate |

**The four load-bearing TYPES** the fit test grades against (the outcome hinges on
these): **(a)** external S_3-labeled three-way datum (I1); **(b)** post-selection /
observer-supplied weak-value reading (I6); **(c)** scale dial (I7); **(d)** orientation
bit (I2). Load-bearing for the OUTCOME = (a) and (b) (the S_3 label or the observer
reading), per the pre-declared outcomes.

### 1.1 The carried attribution fork (NOT-DRY, do not collapse)

conditional-forcing carries an unresolved Z/6-vs-trit fork (its correction banner,
hostile-verify commit 6eb850d). Both readings are carried here:
- **Z/6 reading (original):** the minimal external input is the FULL order-six anchor;
  I1 (trit) AND I2 (bit) are both external, bit = cube of the anchor.
- **Trit-only reading (verifier's preferred):** under the program's own gate semantics
  the deck/payload bit is a GATE native structure passes; then only I1 (the trit) is
  external and I2 is native bookkeeping.
This is a Joe/program-semantics decision, not a probe question, and it does not change
the fit test: under EITHER reading I1 (the S_3 trit) is external and load-bearing, so
the fit test's verdict on (a) is fork-robust.

## 2. The FIT TEST (the payoff)

For each intaken program: does its NATIVE machinery provide an object of the required
TYPE for (a),(b),(c),(d)? Grades: **PROVIDES-CANDIDATE** (names the object) /
**WRONG-TYPE** (right genre, wrong type -- word-coincidence) / **SILENT** (nothing).
Fork-typed per GEOMETER-VS-PHYSICS discipline: a shared WORD is not the object.

| Program | (a) S_3 label | (b) weak-value reading | (c) scale dial | (d) orientation bit |
|---|---|---|---|---|
| **Bianconi entropic** | WRONG-TYPE | WRONG-TYPE | **PROVIDES-CANDIDATE** | SILENT |
| **Verlinde/Jacobson** | SILENT | WRONG-TYPE | WRONG-TYPE | SILENT |
| **Maldacena AdS-CFT** | SILENT | WRONG-TYPE | SILENT | SILENT |
| **Gorard/Wolfram** | SILENT | WRONG-TYPE | SILENT | SILENT |
| **CONTROL: lattice QCD** | WRONG-TYPE | SILENT | WRONG-TYPE | SILENT |

### 2.1 Per-cell reasoning (brutal, fork-typed)

**Bianconi.** (a) Her matter is Dirac-Kahler (0-form + 1-form + 2-form): a THREE-way
GRADED structure with UNEQUAL pieces (dims 1,4,6 in 4D), not three isomorphic copies
under an external S_3 relabel. The "three" is a graded triple, not an S_3 label ->
WRONG-TYPE (this is exactly the word-coincidence the skeptic must catch). (b) Her
action is quantum relative entropy -- a POSITIVE-state functional; araki-scale-route
already PROVED entropy-class functionals are `K_S`-even, sector-blind (the selection
rule) -> WRONG-TYPE, and this is already-run, not speculative. (c) **PROVIDES-CANDIDATE:**
araki-scale-route Q1 found her measure normalization (`l_P^-4`) and couplings
(`beta = beta' l_P^2`) DO carry absolute scale as a factorized dial -- exactly the
"dimensionless native functional x external unit slot" shape I7 wants; it reaches the
Yangzhou worked-example coefficients. HONEST CEILING: it is a RELOCATION, not a
derivation (`l_P`, `beta'` are posited); "her Lambda is a relocation with a mechanism;
our B.5 is a slot with a carrier; neither derives a magnitude." (d) SILENT: positive
metrics, no indefinite Krein sector, no deck flip -- orientation not addressed.

**Verlinde/Jacobson.** (a) SILENT: no three-way datum. (b) His entropic force is entropy
GRADIENTS of positive thermodynamic states; `Delta S ~ Delta x` is linear but in a
POSITIVE entropy, landing `K_S`-even by the selection rule -- the "linear" is a
homonym of `k_sigma`'s `K_S`-linearity -> WRONG-TYPE (intake leg 1b flagged exactly
this). (c) His `a_0 = c H_0` is a DERIVED phenomenological acceleration scale
(dark-MATTER-side, MOND-adjacent, where conformal gravity already died in our H49
fork), not a free external unit slot for the DE/cut amplitude -> WRONG-TYPE. (d) SILENT.

**Maldacena.** (a) SILENT. (b) RT reads geometry off entanglement entropy -- a
positive-state functional, `K_S`-even, blind (intake leg 1, expected) -> WRONG-TYPE.
The observer leg ("real observers solve imaginary problems"; dS state-counting needs a
physical observer) is the RIGHT GENRE but names an observer-as-subsystem, not a
post-selection bra of weak-value shape -> not a candidate object (posture-adjacent per
the intake). (c) SILENT (AdS-only; no dS dual; DE territory disclaimed by his own
caveat). (d) SILENT.

**Gorard.** (a) SILENT: multiway branching is not a three-fold isomorphic-copies S_3
datum; no natural trit. (b) internal-observer foliations are a FRAME/orientation
choice ordering the causal graph, not a post-selection reading -> WRONG-TYPE. (c)(d)
SILENT (its strongest legs are cross-repo to P2C/TaF/TI; no GU scale/orientation leg).

**Planted control -- lattice QCD (must grade dead).** (a) Three COLORS (SU(3)) is a
"three" word-coincidence -- but color is a GAUGED continuous symmetry, not an external
discrete S_3 relabel of isomorphic copies; generations are input masses, not S_3 ->
WRONG-TYPE, by the SAME discipline that killed Bianconi's Dirac-Kahler triple. (b)
SILENT: Euclidean path integral of POSITIVE Boltzmann weights; no weak value. (c)
`Lambda_QCD` / lattice spacing `a` is a "scale" word-coincidence -- but it is set by
dimensional transmutation from a dimensionless coupling (and `a` is a regulator removed
in the continuum limit), NOT an externally-posited unit slot multiplying a GU
functional -> WRONG-TYPE. This is the discriminating contrast: Bianconi PROVIDES on
(c) because `l_P` is an external posited unit; lattice does NOT because its scale is
dynamically generated. (d) SILENT (theta-vacuum CP angle is continuous, not a Z/2
Krein orientation). **Control result: NO PROVIDES-CANDIDATE on any item.** The test is
not broken -- it rejects the control's two word-coincidences ("three colors", "QCD
scale") on TYPE, and the same type-discipline both rejects Bianconi's (a) and accepts
Bianconi's (c). Discrimination by type, not by word, demonstrated.

## 3. Which outcome fired

**S-a fires on the load-bearing items.** No program provides a candidate for (a) the
S_3 label or (b) the weak-value reading -- the two load-bearing types. So the source
object, in its load-bearing aspects (the external S_3 trit I1 and the post-selection
reading I6), is **genuinely novel**: the spec sheet is the contribution, and the
external-count story is hardened -- four independent, heavily-developed external
programs, and not one supplies the load-bearing type.

**One non-load-bearing typed partial:** Bianconi PROVIDES-CANDIDATE for (c) the
scale-dial (I7) only. Per the pre-declared outcomes, S-b requires a LOAD-BEARING item,
so this does not fire S-b; it is a bounded, already-mostly-answered partial (the
araki-scale-route swing found the factorized-dial shape and typed it as a relocation,
not a derivation). Overselling it as S-b would be the only loss available here; it is
declined.

**Framing-grade convergence (not a provided object):** the OBSERVER THEME recurs three
ways -- Maldacena's dS observer, Gorard's internal observer, and GU's own
external-calibration finding (smatrix-sector-face: dynamics consumes orientation,
cannot produce it) all independently conclude that certain readings require an observer
the dynamics cannot supply. This is real cross-program convergence and belongs in a
future one-bit dossier as EXTERNAL FRAMING -- but neither program NAMES a weak-value
reading object, so it is theme, not candidate, and does not move the (b) verdict off
WRONG-TYPE/SILENT.

## 4. Recommended bounded probe for the hourly (named, NOT built here)

**PROBE-SCALE-DIAL (bounded cross-program probe, non-load-bearing):** does Bianconi's
measure-normalization factorized dial (`l_P^-4 INT sqrt(-g) L`, couplings `beta' l_P^2`)
supply the B.5 Element-2 scale VALUE, or is it -- like GU's `k_sigma` -- a relocation
that carries a mechanism but derives no magnitude? araki-scale-route Q1 already
strongly indicates the latter (both are ratio-only cores against an external unit); the
bounded residual is a single explicit type-match of her Yangzhou worked-example
coefficient (`-beta/24` leakage; `r^-4` correction) against the M2/N2 `lambda_0`
measure dial the dossier filed the scale hunt under. One afternoon on existing F2/M2
machinery; expected outcome a clean typed negative that TIGHTENS the "neither derives a
magnitude" comparator for PP3. This is the only cross-program probe the fit test
surfaces; the load-bearing items surface NONE (S-a).

## 5. Five-lens council (inline, answered in writing)

**GU-native geometer (spec fidelity).** The spec sheet is faithful: every item is
sourced to a 2026-07-20 receipt or the B.5 capstone, and the consolidation (I1-I7 as
the sharpened "phase convention" of I8's source action) is exactly the disjunction
conditional-forcing sec.4 states -- observer / boundary / source-action phase choice.
No item is invented; the Z/6-vs-trit fork is carried, not collapsed. The one thing I
insist on: the source object is characterized by TYPE, not constructed -- this swing
does not build it and does not claim it exists.

**Representation theorist (the S_3-label type).** The (a) type is precise and
discriminating: three modules of EQUAL complete invariants (dim + Krein signature, per
trit-copies-node-b1) permuted by an EXTERNAL discrete S_3 that is neither gauged nor
graded. Bianconi's Dirac-Kahler triple fails on unequal invariants (graded); lattice
color fails on gauged-continuous (not discrete-external); both correctly WRONG-TYPE.
The type has teeth precisely because it forbids the two commonest "three" homonyms.

**Quantum-foundations theorist (the observer-reading type).** The (b) type is the
K_S-post-selected linear pairing `<K_S i| M |i>` -- a weak value with the PT-conjugate
bra (s1-pairing-family). RT entropy (positive, even) is the categorical opposite and
correctly WRONG-TYPE across every entropic program. The observer THEME (Maldacena/
Gorard) is genuinely adjacent but supplies an observer-as-subsystem or a foliation,
never a post-selection bra; grading it SILENT-as-object (not PROVIDES) is the honest
call and keeps the weak-value inheritance a GU-internal reading technology, not an
imported claim.

**Cross-program skeptic (attack every fit as word-coincidence).** I attacked the one
PROVIDES cell hardest: Bianconi (c). It survives because the object named is her
measure/coupling UNIT SLOT, already computed to reach worked coefficients -- a typed
object, not a word. I attacked the control's "three colors" and "QCD scale": both die
on type (gauged-continuous; dimensional-transmutation), and crucially the SAME test
that kills them accepts Bianconi (c) and rejects Bianconi (a) -- proving the grader
reads type, not vocabulary. The planted control returning all-dead is the receipt that
no fit in the table is a coincidence artifact. The observer-theme temptation to inflate
Maldacena (b) to PROVIDES is refused: no named object.

**Buildability engineer (which fits are bounded-testable).** Exactly one:
PROBE-SCALE-DIAL (Bianconi (c)), and it is bounded (one afternoon, existing F2/M2
machinery, expected typed negative). The load-bearing cells provide nothing to build.
The S_3-label type-matching IS machine-checkable at fixture grade and is shipped as the
probe (sec. 6); nothing else in the table rises above prose typing, so nothing else is
built.

## 6. What is machine-checkable, and what is shipped

The prose typing of (b),(c),(d) against each program is a judgment about physics
programs' native machinery -- not fixture-checkable without importing the programs.
But the (a) S_3-label item HAS a fixture-grade type signature, reused from
trit-copies-node-b1: an external-S_3 datum = three pieces of EQUAL invariants permuted
by a DISCRETE EXTERNAL relabel. The probe
`tests/channel-swings/source_object_fit_probe.py` encodes that type as three gates
(three-fold count; equal-invariants / isomorphic-copies; external-discrete-label vs
gauged/graded) and runs every program's candidate three-ness plus the GU positive
control and the lattice-QCD planted control through them. Expected: GU PASSES all three
gates (positive control); Bianconi FAILS the equal-invariants gate (graded 1/4/6);
Verlinde/Maldacena/Gorard FAIL the count gate (no three-ness); lattice QCD FAILS the
external-discrete-label gate (gauged color) DESPITE passing count+equal-dims -- the
sharp discriminator that stops the "three colors" word-coincidence. Nothing beyond the
(a)-item typing is checkable at fixture grade; (b),(c),(d) ship as prose typing only,
as stated.

## 7. Boundary (what this swing cannot see)

- Everything inherits R0_COND and the (9,5) H-class conditionality of the frozen spine;
  the fit grades are typings of the intakes' STATED structure (hearsay/shape grade),
  not readings of the programs' primaries (which the intakes queue). A primary read
  could move a WRONG-TYPE to SILENT or vice versa; it cannot manufacture a
  PROVIDES-CANDIDATE for (a)/(b) without a named object the intakes do not contain.
- The Z/6-vs-trit attribution fork (I1/I2) is a Joe/program-semantics decision, carried
  not resolved; the fit verdict is fork-robust on (a).
- Bianconi (c) is PROVIDES-at-type; whether it derives a MAGNITUDE is exactly what
  PROBE-SCALE-DIAL would test and araki-scale-route already indicates it does not.
- The observer-theme convergence is framing-grade; promoting it to a provided object
  would require a program to name a post-selection reading, which none does.
- No Lean/lake; no operator/L^2/QFT grade; no commits; no external actions; no edits to
  any existing file. Two sibling agents run tonight (S1 hostile verify; boundary-law
  lift) -- their files are not this one.

## 8. Receipts

- Probe: `tests/channel-swings/source_object_fit_probe.py` -- deterministic, numpy-free
  (pure structural gates), fixture-grade S_3-label type-matching across 4 programs +
  GU positive control + lattice-QCD planted control; HEADLINE names the fired outcome
  (S-a on load-bearing items) and the control's all-dead result.
- Spec-sheet inputs read in full: WHERE-GU-STANDS (B.5), k1-reframe (two gates),
  conditional-forcing (Z/6 minimal input + fork), trit-symmetry-node-a (S_3),
  trit-copies-node-b1 (isomorphic copies), trit-simplex-node-b2 (simplex killed),
  araki-scale-route (scale externality + selection rule), smatrix-sector-face
  (orientation externality), s1-pairing-family (weak-value reading channel).
- Fit inputs read in full: the four intakes (Bianconi, Verlinde/Jacobson, Maldacena,
  Gorard).
- No claim-status, canon-verdict, scorecard, or public-posture change; intakes and
  sibling repos read-only; nothing committed.
