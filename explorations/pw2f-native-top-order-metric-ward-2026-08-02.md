---
title: PW2F active-native top-order metric composition and diffeomorphism-Ward schema
date: 2026-08-02
status: scoped hostile-review correction; complete top order remains open
lane: Eric-guided active-native construction; Curt remains a formal comparator
run: RUN-20260802-223738-gu-formalization-pw2f-native-top-order-metric-ward
grade: >-
  LC-ALTERNATION SUBROUTE PASS WITH HOSTILE CORRECTION. All 140 induced-Y14
  Levi-Civita symbol columns have zero total alternation, but this does not
  settle the derived connection K=h^-1 D_B h. Hostile review constructed the
  omitted (Ad_(h^-1)-1)D_gB summand and found it live on 129/140 exact
  Hodge-null columns. It also found a rank-ten bare third-order block at the
  held-out vertical conormal e10; the zero block occurs only on the selected
  seven horizontal conormals. The generic affine-second-jet Euler/Green
  theorem, selected fixed-h/fixed-Shiab rank-seven C2 panel, and separate raw
  moving-Shiab rank-ten bank survive, but they do not decide the complete
  first-action order. A formal
  diffeomorphism-Noether/Green schema is derived separately; its literal
  active-native coefficient and rank remain open with the complete lower C2
  owner. No public source port, global Y14 theorem, physical quotient,
  observation theorem, or physics recovery is claimed.
---

# PW2F active-native top-order metric composition and metric Ward

## Result in plain English

PW2E told us that a fourth metric derivative *might* be needed. PW2F tried to
compute the actual highest-order route before building it. The hostile review
caught an omitted piece, so the fourth-derivative question is still open.

One real subresult is geometric. A variation of a Levi-Civita connection is
torsion-free, and the total alternation that feeds one part of the repository's
grade-3/11 bridge kills that variation exactly. But that controls only the
`delta u` contribution from `Alt(delta B)`. It does not control the complete
derived-connection variation in

\[
g_X\longrightarrow\Gamma_Y(g)\longrightarrow T
\longrightarrow\operatorname{Alt}(T)\longrightarrow h
\longrightarrow K.
\]

For (K=h^{-1}D_Bh), the exact variation also contains

\[
\delta K=D_{B+K}(h^{-1}\delta h)
 +(\operatorname{Ad}_{h^{-1}}-1)\delta B.
\]

The second summand is nonzero on 129 of the 140 exact columns in a nontrivial
Hodge-null bridge. Motion of the reduction, alternation, and internal Hodge
inside (h) is also unassembled. Those terms may survive or cancel only after
the complete action return is calculated.

The one-half/one-third curvature block was correctly put in transgression
normal form. Separately, a universal comparator proves that *if* an action is
affine in its metric second jet, its fourth-order Euler coefficient vanishes.
That theorem, its full Green packet, and the independent Sage check remain
valid. They are conditional evidence, not a verdict that the native first
action has the required affine form.

The construction did not become empty. On seven preregistered horizontal
conormals the fixed-h/fixed-Shiab synthetic-source curvature-input coefficient
supplies seven independent rows across ten metric owners. The eight raw
moving-Shiab coefficient slots remain live and their separate bank has rank
ten. A held-out vertical conormal (e_{10}), however, has a live rank-ten
bare third-order block. These are uncomposed coefficient ranks, not complete
Euler or physical ranks.

## Layer 0: which objects were actually compared

PW2F keeps the following objects separate:

| Source or construction object | Not identified with |
|---|---|
| source \(\epsilon\in H\) | repository-derived \(h=\exp u(T,g)\) |
| source \(T_\omega\) | synthetic \(T_{\rm syn}\), ordinary torsion, or literal \(K_{\rm full}\) |
| public Shiab contraction | active grade projector or active trace/Phi/Hodge fixture |
| fixed \(\varpi\) variation | fixed total \(A\) or fixed \(B\) variation |
| action-coefficient rank | Euler rank, quotient rank, or physical-response rank |
| \(\Xi=D\Upsilon\) | internal Ward, diffeomorphism Ward, BV differential, or conservation law |

The result is therefore repository mathematics on the active
\(\operatorname{Spin}_0(9,5)\subset Sp(32,32;\mathbb H)\) component. It is not
reported as a calculation of the unported public \((7,7)/U(64,64)\) operator.

## Divergent specialist preregistration

Three lenses independently set the calculation order and kill conditions
before the probe was written.

1. **Exact Clifford/reductive/Y14.** Keep source \(\epsilon\) external and
   unidentified; compose the Levi-Civita graph with repository-derived \(h\)
   without renaming either. Work highest order first. Test the torsion-free
   alternation while retaining every other term in \(\delta K\). Preserve
   trace reversal and every ten-owner JVP/VJP route.
2. **Variational/PDE.** Normalize the transgression before automatic
   differentiation. Compare direct action Euler differentiation with reverse
   Krein formal adjoints and retain every Green concomitant. Build \(D^3G\)
   only after a nonzero fourth-order coefficient survives.
3. **Primary-source/Layer-0.** Record every decisive collision as
   `SOURCE-CONFIRMS`, `SOURCE-CORRECTS`, or `SOURCE-SILENT`; never turn source
   silence into a no-go or attribute the synthetic bridge to Weinstein.

The preregistered expected verdict was that unphysical highest order would
cancel and a lower second-order block would remain. Hostile review forced the
opposite report: the calculation had not included the whole object.

## Exact construction

### 1. Normalize before differentiating

The written block

\[
F_B+\frac12D_BT+\frac13[T,T]
\]

was first checked against the exact fixed-total-connection transgression form

\[
\frac12(F_A+F_B)-\frac16[T,T].
\]

This prevents an independent \(dK\) from being invented during variation.

### 2. Compute the bridge's principal metric route

For each of the 14 coordinate conormals and each of the 10
\(\operatorname{Sym}^2T^*X\) owners, the probe constructs the actual
induced-Y14 Levi-Civita spin one-form

\[
\sigma(D_g\Gamma_Y)(\xi)h.
\]

All 140 columns are live, and every one satisfies

\[
\operatorname{Alt}\!\left(\sigma(D_g\Gamma_Y)(\xi)h\right)=0.
\]

The control is not vacuous: a planted non-Levi-Civita grade-two one-form has
nonzero alternation. This closes only the `Alt(delta B)` contribution to
`delta u`; it does not prove that \(D_gh\) or \(D_gK\) vanishes. On an exact
nontrivial Hodge-null bridge,
\((\operatorname{Ad}_{h^{-1}}-1)D_gB\) is live on 129 of 140 columns. The
moving reduction/Alt/internal-Hodge contribution to \(h^{-1}\delta h\), and
the complete action return, remain open.

### 3. Prove the conditional general Euler-order theorem

For ten metric fields let

\[
L=A_i(g,g')g_i''+B(g,g').
\]

Direct Euler differentiation gives no \(g^{(4)}\) term. The only possible
third-order matrix is

\[
H_{ij}
=\frac{\partial A_i}{\partial g_j'}
-\frac{\partial A_j}{\partial g_i'},
\qquad H^T=-H.
\]

That skew owner matrix times an odd derivative is formally self-adjoint, as
Helmholtz requires. In this *structural comparator*, the direct Gateaux route
and reverse formal-adjoint route agree with a nonzero two-layer Green packet.
SageMath independently reproduces the fourth-order zero, \(H=C-C^T\), and the
comparator Green identity over an exact symbolic polynomial ring. The native
coefficient banks are not substituted into this Lagrangian, so no native
direct/reverse/Green claim follows yet.

This theorem does **not** say every mixed third-order graph block vanishes.
The independent B2C15N action still has the exact opposite mixed coefficients
\(+9\) and \(-9\) between its two graph owners. That hostile control prevents
the metric self-block result from being overgeneralized.

### 4. Measure selected fixed and held-out contributions

The selected conormal panel is

\[
e_0,e_1,e_2,e_3,e_0+e_1,e_0+e_2,e_0+e_3.
\]

For each conormal and each metric owner the probe forms

\[
\left\langle
T_0,\;\odot\bigl(\xi\wedge
\sigma(D_g\Gamma_Y)(\xi)h_i\bigr)
\right\rangle.
\]

The rank grows exactly \(1,2,3,4,5,6,7\). The bare Levi-Civita
connection-incidence contribution paired against the same fixed Shiab outputs
is zero on every conormal in this *horizontal* panel. A hostile one-form proves
this is not forced by the matcher. More decisively, the held-out vertical
conormal \(e_{10}\) produces a nonzero \(10\times10\) contribution of rank
ten, and \(e_0+e_{10}\) produces rank five. These matrices are not skew, so
they cannot be the complete odd-order Euler coefficient: missing transpose,
graph, and coefficient-motion returns must be assembled before applying the
Helmholtz test.

Separately, the full moving coefficient inventory retains all eight slots:

`trace_gamma`, `Phi1_first`, `Hodge_first`, `Phi1_outer`, `Phi2`,
`Hodge_inner`, `Hodge_middle`, and `Hodge_outer`.

That \(544\times10\) bank has rank ten. It is algebraic in the total metric
\(H\), but its base-metric pullback may inherit derivatives through
\(D_gG_Y=Z_0\delta g+Z_1\nabla\delta g\). It has not been composed with the
rank-seven panel, outer distortion, density/Krein/lowerers, or Green returns.

## Diffeomorphism Ward: what is now earned

PW2F derives the correct formal schema from a declared natural lift. In an
exact scalar-plus-lapse comparator,

\[
L=\frac{(\phi')^2}{2N},\qquad
\delta_\xi\phi=\xi\phi',\qquad
\delta_\xi N=\xi N'+\xi'N,
\]

the direct variation is \(d(\xi L)\), both Euler owners are nonzero, and

\[
E_\phi\phi'+E_NN'-d(NE_N)=0
\]

holds off shell. The Noether current

\[
J_\xi=\Theta(\mathcal L_\xi\Phi)-\iota_\xi L
\]

closes the live bulk and boundary pieces exactly. A separate nonabelian matrix
fixture retains the internal gauge Ward, proving that the two identities have
not been collapsed.

The formal schema is earned; the literal active-native GU evaluation is not.
It first needs the complete \(\delta K\)/top-order owner, then the lower
\(C_2\) owner, and the full transformation of the independent source roots. A
gauge-covariant diffeomorphism may be used later only after its difference
from the natural Lie derivative is discharged by the separately certified
internal Ward.

## Primary-source collisions

The new machine-readable manifest hashes the complete source files and each
exact locator slice. Draft rows now carry an evidence chain from the official
author-PDF digest through the rendered/manual transcription receipt; normalized
repository prose is not itself called primary. A separate scoped absence
receipt records exact searched files, hashes, semantic query families, and a
non-global caveat.

- `SOURCE-CONFIRMS`: the written first-action grammar, formal Hodge/pairing,
  Zorro/Levi-Civita/Y/spin chain, gauge-rotated connection difference, and the
  positive eddy requirement.
- `SOURCE-CORRECTS`: raw \((3,7)\) Frobenius, the TOE episode's unnamed
  contraction-versus-projection correction, and
  \(\Xi=D\Upsilon\)-as-Ward readings.
- `SOURCE-SILENT` in the declared corpus: the active metric coefficient and
  Ward current, identity of \(\odot_\epsilon\) with \(\odot_\omega\), and the
  complete epsilon/metric plus density/Krein/Green variation policy.

Silence narrows attribution; it kills no source architecture.

## New exact-tool cross-checks

- **SageMath 10.9:** five independent structural constant-coefficient
  polynomial jet/Helmholtz/Green checks, not a native action assembly.
- **python-flint 0.9.0:** exact rational rank seven for the curvature-input
  bank. The moving coefficient bank is multiquadratic, so an exact rational
  matrix would be ill-typed; instead Arb reconstructs its radicals from exact
  inputs and rigorously excludes zero from an interval enclosing a
  SymPy-selected \(10\times10\) determinant. Ten columns then force rank ten.

No nonrigorous approximate or ML result decides a zero, rank, or cancellation.

## Validation

- native top-order probe: `18 exact + 4 source + 13 type + 9 planted = 44 PASS`;
- diffeomorphism-Noether/Green probe: `6 exact + 2 source + 7 type + 5 planted = 20 PASS`;
- primary-source collision manifest: `58 exact + 3 type + 8 planted = 69 PASS`;
- Sage exact cross-check: `5 PASS`;
- FLINT/Arb exact-rank cross-check: `4 PASS`;
- PW2E/PW2D predecessor regressions and the 91-check campaign scaffold: PASS.

## What is not claimed

PW2F does not establish:

- the public source-to-active real-form bundle/action port;
- a global \(Y^{14}\) atlas theorem;
- the complete \(D_gh/D_gK\) metric top-order coefficient or full skew
  \(Y^{14}\) third-order Euler symbol;
- the complete lower \(C_2\) metric Euler matrix or native diffeomorphism-Ward rank;
- a gauge/BV quotient, analytic Green domain, hyperbolicity, or BFV phase space;
- observation pushdown, no leakage, Standard-Model/GR recovery, or a physical
  prediction;
- any role for P1/P2/P3 in a continuous coefficient or jet;
- a Curt-lane merge, third-lane promotion, canon change, status change, or
  public-posture change.

## Next gate

`PW2F-R-COMPLETE-DERIVED-K-TOP-ORDER-AND-FULL-Y14-C3-HELMHOLTZ-CLASSIFICATION`

PW2F-R must construct the complete
\(\delta K=D_{B+K}(h^{-1}\delta h)+(\operatorname{Ad}_{h^{-1}}-1)\delta B\)
route, including moving reduction/Alt/internal Hodge, compose every total-to-base
metric coefficient and Green return, and then assemble the full \(Y^{14}\)
odd-order symbol. The final \(C_3\) owner matrix must satisfy the skew
Helmholtz condition. Only after the complete fourth/third-order verdict is
earned may PW2G proceed to lower \(C_2\) and the literal native Ward.
