---
title: PW2F-R2B2B1 — source split and co-moving second-graph prerequisite
date: 2026-08-03
status: SCOPED_FINITE_PREREQUISITE_PASS_COMPLETE_C5_C4_OPEN
lane: Eric-guided Lane 1; source and active-reconstruction columns separated
run: RUN-20260803-070259-gu-formalization-pw2fr2b2b1-source-comoving-graph
---

# Result

R2B2B1 closes one prerequisite and leaves the advertised R2B2B C5/C4
calculation open. An exact finite noncommutative source-shaped chart now keeps

\[
B_{\rm full}=\Gamma+q,\qquad
T=\varpi-q,\qquad
A_{\rm total}=B_{\rm full}+T=\Gamma+\varpi
\]

as separate identities in two connection components. At fixed source
`varpi`, two independent tangent directions give

\[
\delta T=-\delta q,\qquad
\delta B_{\rm full}=\delta\Gamma+\delta q.
\]

The old shortcut `deltaT=-deltaB_full` therefore misses the live
`-deltaGamma` return on both directions.

The same fixture then moves `Gamma`, `q`, `varpi`, `B`, `T`, `A`, and
curvature through one exact affine/homogeneous frame

\[
h=\exp(\tau U)=1+\tau U,\qquad U^2=0.
\]

The direct mixed derivative of

\[
\widehat B=h^{-1}Bh+h^{-1}dh
\]

agrees with an independently assembled five-term `Bhat2` formula. It is
nonzero in both components. Two hostile controls delimit the result: freezing
`h` annihilates this mixed graph, and a commuting linear-orbit fixture also
annihilates it.

Finally, an exact quadratic finite action fixture verifies the off-shell
pullback identity

\[
D^2(S\circ F)=J_F^*H_SJ_F+\langle E_S,D^2F\rangle.
\]

The second term is live on the noncommuting co-moving graph, so the
first-JVP-only comparator misses it exactly. The independently differentiated
restrict-first Hessian equals the two-term vary-upstairs chain-rule assembly.
An on-shell control kills the second return and reduces to `J*H J`.

# Scope: what this does and does not construct

This is a finite structural prerequisite. Its independent `q` directions do
not construct the actual epsilon-to-`q` Frechet map, and its declared moving
`h` does not construct the native `u(T)` law. It does not identify source
`epsilon` with repository `h=exp(u)`, construct the public-to-active
right-`H`/Krein carrier bridge, or instantiate the full induced-`Y14` graph.
It proves that a live co-moving second graph is possible and must be priced; it
does not prove that this graph reaches differential order four in the native
operator.

In particular, it does not compute:

- complete source-coordinate or active-native `C5/C4`;
- the source-directed `I1` Hessian;
- the manuscript `I2B=||Upsilon_B||^2` Hessian;
- density, Krein, input/output-lowerer, or outer-pairing coefficient slots;
- same-pipeline contracted moving-Shiab liveness;
- an independent reverse/formal-adjoint or native presymplectic Green route;
- `C3/C2`, a characteristic, analytic domain, BV quotient, observation
  equation, or physics.

# Layer 0 and primary-source collision

The re-pinned primary material continues to confirm connection-fundamental /
metric-emergent architecture, moving theta/section grammar, a connection
difference, upstairs equation followed by pullback, trace reversal, and the
gauge-rotated Levi-Civita connection in the contorsion slot. It corrects the
named TOE map from projection to contraction. The TOE global-section remarks
remain internally disputed and therefore `SOURCE-UNCERTAIN`.

The source does not supply repository `h`, `theta1`, `Bhat2`, the
epsilon-to-`h` bridge, the active carrier port, complete C5/C4, or `kappa1`.
Those pieces are `SOURCE-SILENT`. The exact finite graph here is
`REPOSITORY-DERIVED`. `D2(I1 composed with F)`, manuscript `I2B`, and
second-order equations remain different objects.

# Specialist pre-assessment and hostile review

Three lenses were pre-registered before construction:

1. exact differential geometry required the source split before conjugation,
   one frame afterward, an independent `Bhat2` assembly, and curvature
   conjugacy;
2. variational/PDE required direct versus two-term Hessian agreement, a live
   off-shell return, and frozen/commuting/on-shell controls; and
3. source/Layer 0 required the moving graph to remain repository-derived and
   `I1` separate from `I2B`.

All three hostile post-reviews pass after retaining the finite structural
scope. The geometry review does not admit a global or native-`Y14` graph. The
PDE review does not infer differential order or C4 from a finite mixed second
derivative. The source review admits no epsilon-to-`h` or action-homonym
collapse.

# Campaign boundary and next gate

P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`; the conjunctive
`TG-1 AND TG-2 AND TG-3` gate remains `NOT_PROMOTED`. No canon, scientific
status, public posture, scheduler, publication, characteristic, domain,
quotient, observation, or physics change occurs.

The next gate is
`PW2F-R2B2B2-ACTUAL-NATIVE-SOURCE-GRAPH-COEFFICIENT-SLOTS-I1-I2B-C5-C4`:
port the constructed dependency into the actual right-`H`/Krein induced-Y14
graph, account for the five coefficient slots, run the same-pipeline moving
control, compute complete C5 before C4, and keep the `I1` and `I2B` Hessians
separate.

# Executable evidence

- Main exact graph: `21 exact + 9 type + 7 planted = 37 PASS`.
- Primary-source collision: `2 exact + 9 source + 9 type + 6 planted = 26 PASS`.
- Exact-geometry hostile review: `PASS_AFTER_SCOPE`.
- Variational/PDE hostile review: `PASS_AFTER_SCOPE`.
- Source/Layer-0 hostile review: `PASS_AFTER_SCOPE`.
