---
title: "K77 Wave 2: Curt locates a zero-order connection port, but reality does not select the trace-q coefficient"
status: active_research
doc_type: construction_result
created: 2026-08-04
lane: 1
gate: RENDEZVOUS-ACTION-CURRENT-RIESZ-SUPERIG-WARD
result: "PARTIAL__FULL16_TRACE_Q_FAMILY_ASSEMBLED__CURT_ZERO_ORDER_PORT_NARROWED__NATIVE_REALITY_SELECTS_NOTHING__MAJORANA_RIVAL_EMPTY__COMMON_TWO_LAYER_ACTION_EULER_SELECTION_OPEN"
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# K77 trace-q coefficient / zero-order / reality selection

## Result first

The complete trace-`q` version of the displayed draft-9.16 grammar is now
assembled at exact carrier grade.  One common coefficient pair

\[
 P_{\alpha,\beta}(q,A)
 =\alpha\,\gamma(q)A+\beta\,A\gamma(q)
\]

is used in all four top-left Shiab cells of the sixteen-cell matrix.  The
remaining eight nonzero cells retain the ordinary connection/wedge and
Krein-adjoint placements, and the source-preferred southeast four cells stay
zero.  Cellwise retuning is not allowed.

Curt Jaimungal's Iceberg gives a useful but narrower answer than it first
appears to give.  It directs us to place a vertical component of the unified
connection as a zero-order, off-diagonal chiral term in an upstairs Dirac
operator.  It does **not** identify which `varpi_rs` block carries the physical
Higgs, state a barred/unbarred reality map, or choose `alpha:beta`.

The later Curt--Weinstein conversation is a direct source collision:

- Weinstein says Curt's treatment did not preserve the Einstein--Dirac layer
  followed by a second Yang--Mills--Higgs Lagrangian;
- he separately says an adjoint-valued Higgs is not the representation of the
  real Higgs.

The disposition is therefore `SOURCE-CORRECTS`, not a dismissal of Curt.
Curt has located a zero-order port worth building; Weinstein prevents the
compact minimal-coupling diagram from being treated as the unique Eric-lane
action.

No tested reality condition selects the remaining coefficient:

1. The source-faithful classical draft has four independent barred/unbarred
   fields.  Its Dirac bilinear is defined for every coefficient pair.
2. The native real `Cl(7,7)` conjugation fixes left and right separately, so
   its coefficient-selection rank is zero.
3. A real Majorana fixed locus is an additional rival, not a source theorem.
   On the complete form-index times spinor Krein pairing, both the principal
   self/skew systems and the zero-order self/skew systems have exact rank two.
   Hence their only solution is `alpha=beta=0`.  Majorana reality does not
   select the commutator or anticommutator; it kills this frozen trace-`q`
   family.
4. The two degree-sensitive reality solutions multiply every one-to-one
   Shiab cell by the same overall minus sign.  That cannot change any of those
   ranks.
5. Moving `q`, Hodge, density, pairing, and connection terms are lower order.
   They remain necessary for a surviving action but cannot repair the
   principal Majorana obstruction.

The source-faithful projective invoice therefore remains

```text
free projective coefficients: 1
source-derived selecting rank: 0
constraint surplus:           -1
```

This is the stopping point for isolated selector hunting.  The coefficient
family must now enter a common **two-layer variational action**.  Only actual
Euler equations, a typed adapter between the Einstein--Dirac and
Yang--Mills--Higgs layers, or a source-derived restricted connection orbit may
legitimately select it.

The exact probe passes:

```text
7 source + 19 type + 24 exact + 6 planted = 56 PASS
```

Wave 2 remains partial.  Wave 3 does not open.  P1/P2/P3 are unchanged and
unused.

## Plain English

We now know exactly where the extra trace direction can be inserted in the
fermion matrix: it modifies the four boxes that use the Shiab contraction.
There is still one dial—put the trace direction on the left, on the right, or
use a mixture.

Curt's Iceberg says something valuable about the other boxes.  A vertical
piece of the connection can look like a scalar after we observe four
dimensions, so it can enter the Dirac operator as a mass-like or Higgs-like
term.  But his later conversation with Eric makes clear that Curt compressed
two stages of the proposed theory into one, and that the actual Higgs
representation is subtler than “an adjoint scalar.”

The obvious reality shortcuts do not choose the dial.  Ordinary real
conjugation accepts every setting.  Forcing a Majorana action is worse: with
the full indefinite pairing included, it rejects every nonzero setting.  That
is a scoped failure of this operator family under that additional reality
condition, not a failure of `(7,7)` or of GU.

The efficient next move is to stop asking symmetry alone to choose the dial.
Write the two action layers and their adapter, vary them, and see whether the
resulting equations overdetermine it.

## 1. Layer 0

| phrase | object used here | not identified with it |
| --- | --- | --- |
| barred field | one of four independent classical Berezin variables in the draft | automatically the Dirac adjoint or Majorana conjugate of an unbarred field |
| native real structure | ordinary conjugation on the real `Cl(7,7)` module | a chosen Majorana integration domain for the complex `U(64,64)` action |
| `C`-reality | an antilinear involution/fixed-locus condition | the Krein bilinear `B` or formal adjoint by itself |
| left/right chiral block | Curt's `S_L/S_R` support after complexification | left/right multiplication by `gamma(q)` around the Shiab map |
| Higgs candidate | vertical, scalar-like pullback component of an ad-valued one-form | the physical Standard Model Higgs representation after reduction |
| Yukawa candidate | zero-order off-diagonal Dirac support | the actual `P0/rho/Y_K/Y_C/C` family matrix and reality completion |
| two layers | Einstein--Dirac and a second Yang--Mills--Higgs Lagrangian | one already-identified minimally coupled action |
| selection | a rank-one constraint on the projective coefficient line | sensitivity of currents or covariance of the full plane |

This gate tests action reality.  It does not test observed chirality, a
Majorana mass channel, a seesaw spectrum, or a generation count.

## 2. Primary-source collision, including Curt's Iceberg

The durable extraction is
`lab/sources/curt-iceberg-fermion-zero-order-reinspection-2026-08-04.md`.
The load-bearing windows are:

- Iceberg `01:02:06--01:05:08`: rolled fermion operator and southeast-zero
  seesaw analogy;
- Iceberg `01:35:26--01:39:13`: upstairs Dirac action from a distinguished
  connection plus gauge potential;
- Iceberg `01:40:01--01:48:57`: vertical scalar-like component, chiral split,
  and Yukawa-by-minimal-coupling reconstruction;
- Iceberg `01:49:33--01:50:20`: real 128 carrier versus complex Dirac
  presentation;
- paired conversation `00:41:50--00:42:44`: Weinstein's two-Lagrangian
  correction;
- paired conversation `01:10:37--01:10:57`: the physical-Higgs adjoint
  warning;
- paired conversation `02:36:02--02:37:37`: nonchiral parent and VEV-induced
  effective chiral decoupling.

| question | disposition |
| --- | --- |
| should a connection-supplied zero-order chiral block be built? | `SOURCE-CONFIRMS` as a construction target |
| is Curt's single minimal-coupling action Eric's complete action? | `SOURCE-CORRECTS` |
| is the physical Higgs simply the adjoint-valued trace component? | `SOURCE-CORRECTS / ADAPTER REQUIRED` |
| which `varpi_rs` cell carries it? | `SOURCE-SILENT` |
| what identifies barred with unbarred fields? | `SOURCE-SILENT`; the 2021 draft instead states independence |
| which q coefficient is selected? | `SOURCE-SILENT` |

## 3. The sixteen-cell construction

Using the source row order

```text
(bar-zeta-minus, bar-zeta-plus, bar-nu-minus, bar-nu-plus)
```

and column order

```text
(zeta-plus, zeta-minus, nu-plus, nu-minus),
```

the constructed typed ledger is

\[
\begin{pmatrix}
q\Phi\varpi_{++} & q\Phi(d_0+\varpi_{+-})
  & \varpi_{++} & d_0+\varpi_{+-}\\
q\Phi(d_0+\varpi_{-+}) & q\Phi\varpi_{--}
  & d_0+\varpi_{-+} & \varpi_{--}\\
-\bar\varpi_{++}^{\times} & -d_0^{\times}-\bar\varpi_{+-}^{\times}
  & 0 & 0\\
-d_0^{\times}-\bar\varpi_{-+}^{\times} & -\bar\varpi_{--}^{\times}
  & 0 & 0
\end{pmatrix}.
\]

Here `qPhi` abbreviates the common two-parameter family

\[
 q\Phi_{\alpha,\beta}
 =\alpha\,\gamma(q)\Phi+\beta\,\Phi\gamma(q).
\]

This is an assembly of the conditional repaired family, not an attribution
that the draft inserts `q`.  The four q cells comprise both first-order and
zero-order terms, so a coefficient must work uniformly on both.

The executable instantiates the zero-order direction with an actual even,
`B`-skew spin-connection generator in the allowed `Spin(7,7)` algebra.  A
single admissible direction is enough to refute a claimed reality property of
the unrestricted connection family.  If a future source-derived Higgs orbit
excludes that direction, the narrower orbit must be written and retested; the
present result does not pre-kill such an unbuilt restriction.

## 4. Reality branches

### 4.1 Source-faithful complex Dirac branch

The draft says the barred and unbarred fields are distinct at classical
level.  Therefore

\[
 S_F(\bar\chi,\chi)
 =\int\bar\chi\,D_{\alpha,\beta}\chi
\]

is a valid complex bilinear for any coefficient pair.  Two exact independent
bar-field fixtures distinguish the two coefficient directions with rank two.
That is sensitivity, not a selecting equation.  A Hermitian completion may
pair a term with its conjugate for every coefficient; it also does not select.

### 4.2 Native real conjugation

All real-K77 Clifford, trace-`q`, principal, and tested connection matrices are
real.  Ordinary conjugation therefore fixes left and right individually:

```text
C-reality selection rank = 0.
```

Realness of the carrier is not the same as imposing a Majorana field domain.

### 4.3 Optional Majorana rival

A Majorana quadratic action identifies the bar with a transpose through the
pairing, so the Grassmann kernel must have the appropriate formal transpose
class.  The complete one-form-index times spinor Krein adjoint gives four
exact two-column systems:

| system | exact rank | nonzero projective survivor |
| --- | ---: | --- |
| principal Krein-self | 2 | none |
| principal Krein-skew | 2 | none |
| zero-order Krein-self | 2 | none |
| zero-order Krein-skew | 2 | none |

This corrects the tempting spinor-only inference that the commutator should
be self and the anticommutator skew.  The complete form-index map `A` is not
itself a scalar adjoint eigenvector, so diagonalizing only the operation that
exchanges left/right is insufficient.  Every nonzero coefficient fails on
the declared fixture.

The kill scope is exact but narrow:

```text
real Majorana fixed locus
+ current q-repaired gamma-trace Shiab family
+ predecessor's full form-index Krein primalization
+ connection family containing the tested even Spin(7,7) direction.
```

It is not a no-go for all real fermion actions, all Shiab operators, all
restricted Higgs orbits, or `(7,7)`.

## 5. Degree reality and lower-order terms

The preceding gate found

\[
(r_1,r_0,c_1,c_0)=(-s,+s,+s,-s),\qquad s=\pm1.
\]

For every one-form-to-one-form Shiab cell,
`r1*c1=-1` on both solutions.  This is one invertible overall sign and leaves
all coefficient ranks unchanged.

When `q` moves, both left and right emit nonzero `dq` terms.  Moving Hodge,
density, pairing, and connection pieces remain construction obligations.
They are subprincipal or lower-order terms and cannot alter a rank-two failure
of the principal reality equations.

## 6. Constraint surplus and datum ledger

| item | count/status |
| --- | --- |
| trace receiver direction | geometry-owned as `q=t_g/2` |
| projective left/right coefficient | 1 free parameter |
| native conjugation constraints | rank 0 |
| source zero-order selector | rank 0 |
| Majorana rival | empty, not a selector |
| source-faithful surplus | `0-1=-1` |
| P1/P2/P3 | unchanged and unused |

The datum cannot simply force this coefficient yet: no receiving arrow has
been constructed and the existing P1/P2/P3 types are not coefficient
projectors.  Adding a new coefficient datum at surplus `-1` would merely name
the unresolved freedom.  The action must first be allowed to generate a
constraint.

## 7. Inline hostile specialist review

1. **Source historian:** sustained the Iceberg locators but required the
   paired Weinstein correction to control the action architecture.
2. **Spin geometer:** required separate real-carrier and complex-Dirac
   branches; complexification is not a reality theorem.
3. **Krein specialist:** rejected the spinor-only commutator shortcut and
   required the full form-index adjoint.
4. **Fermionic-action specialist:** separated independent Dirac bars from a
   Majorana Grassmann fixed locus.
5. **Gauge theorist:** accepted the even `Spin(7,7)` connection witness but
   narrowed its kill to connection domains containing that direction.
6. **Higgs phenomenologist:** rejected “adjoint scalar equals physical Higgs”
   without the reduction/projection adapter Weinstein says is needed.
7. **Variational analyst:** confirmed lower-order motion cannot repair a
   principal transpose obstruction and refused to count sensitivity as Euler
   selection.
8. **Representation theorist:** confirmed off-diagonal chiral support does
   not determine Clifford ordering.
9. **Exact-computation engineer:** required both degree-reality signs, both
   principal transpose classes, both zero-order classes, and planted
   cell-retuning/frozen-q failures.
10. **Science council:** stopped the isolated selector sequence and routed
    the surviving complex coefficient family into a common two-layer action.

### Material repairs made by review

- The initial attractive inference “Majorana selects the commutator” was
  false.  It used only the left/right exchange and ignored that the complete
  middle map is not an adjoint eigenvector.  Exact rank-two systems replaced
  it.
- Curt's minimal-coupling derivation was initially being treated as one
  source action.  The paired Weinstein correction restored the two-layer
  fork.
- “Higgs block” was narrowed to “zero-order vertical connection candidate”;
  physical representation, observation, and `P0/rho/Y_K/Y_C/C` remain open.
- The Majorana kill was narrowed from all K77 fermions to the declared
  q-repaired Shiab family and any connection domain containing the tested
  admissible even direction.

The summary-over-artifact reviewer found and corrected the first and third
overstatements.  The superseded-object reviewer found that defending Curt's
single-layer diagram would ignore Weinstein's later correction.

## 8. What remains open

- actual two-layer Einstein--Dirac / Yang--Mills--Higgs action and adapter;
- source-derived restriction, if any, on the allowed `varpi_rs` orbit;
- full moving Hodge, Krein pairing, density, metric, and soldering variation;
- full source-`H` descent or an honest reduction;
- complete `P0/rho/Y_K/Y_C/C` placement;
- common variational and analytic domain;
- observation, physical Higgs/Yukawa, masses, particles, chirality,
  generations, and anomaly/count questions.

## 9. Next gate

```text
K77_COMMON_TWO_LAYER_DIRAC_YANG_MILLS_HIGGS_ACTION_EULER_COEFFICIENT_SELECTION
```

Construct the Einstein--Dirac layer and the separate Yang--Mills--Higgs layer
with an explicit shared-connection adapter.  Carry `alpha:beta` as a declared
projective coefficient, vary both layers including moving q/Hodge/pairing,
and compute whether the resulting Euler system has coefficient-selection
rank one, rank zero, or is inconsistent.  This is a return to the common
action, not another isolated symmetry selector.  Wave 3 remains closed.
