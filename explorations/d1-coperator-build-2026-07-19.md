---
title: "D1 build: the interacting C-operator on gu's structures via Bender-Mannheim technology, with the degeneracy check"
status: active_research
doc_type: exploration
created: 2026-07-19
directed_by: "Joe direct chat, 2026-07-19 (blockbuster follow-through swing: the D1 build)"
axiom: lab/process/boundary-adapter-standing-axiom.md
extends: explorations/pt3-w229-membership-audit-2026-07-19.md
inputs:
  - explorations/mannheim-pt-intake-d1-method-2026-07-19.md
  - explorations/pt3-w229-membership-audit-2026-07-19.md
  - tests/channel-swings/pt3_w229_membership_probe.py
  - explorations/channel-swing-CH-QM-2026-07-19.md
  - tests/channel-swings/ch_qm_graded_quotient_toy.py
  - explorations/channel-swing-CH-SRC-2026-07-19.md
  - tests/channel-swings/ch_src_minimal_action_toy.py
  - canon/ghost-parity-krein-synthesis.md
  - explorations/big-swing-2026-07-06/R1-pu-pt-vs-ghost-parity.md
  - explorations/W203-branch3-source-action-fixed-coefficients-2026-07-14.md
primary_sources_read_in_full:
  - "arXiv:2104.03708 (Mannheim, 'Antilinear Symmetry and the Ghost Problem in Quantum Field Theory', v2 2021) -- full text"
  - "arXiv:2004.00376 (Mannheim, 'Ghost Problems from Pauli-Villars to Fourth-Order Quantum Gravity and their Resolution', 2020) -- full text"
runnable:
  - tests/channel-swings/d1_coperator_build_probe.py
claim_status_change: none
canon_verdict_change: none
public_posture_change: none
---

# D1 build: the interacting C-operator, constructed, and the degeneracy check

D1 as stated by the membership audit: the W229 MEMBER verdict is conditional
on the INTERACTING C-operator existing and grading records positive; the
kinematic K_S proxy is already verified; the named watch item is a second
anchor freedom not identifiable with the kinematic orientation. This swing
constructs the C-operator with the Bender-Mannheim technology on gu's own
toy structures (the CH-QM/CH-SRC fixtures: Krein form, baked-in orientation,
record pairs, record register), verifies the grading, and runs the armed
degeneracy falsification at gu's exactly-degenerate vacuum anchors.

Everything conditional under the standing axiom (`R0_COND`). Technology
import only, never the ontology (Section 5). No claim status, canon verdict,
or posture moves.

**Headline (Section 4): C-determination FAILS at gu's vacuum-anchor
degeneracy exactly as the external-bit result requires — but by
NON-UNIQUENESS (an admissible continuum on a diagonalizable, sign-blind
degeneracy), NOT by the Pais-Uhlenbeck Jordan-block form. Both failure
shapes are exhibited side by side and are distinct. The external Z/2 stands,
and the datum dynamics cannot supply is exactly the anchor eps, nothing
larger of Z/2 type.**

## 0. Five-lens council (inline, before execution)

**Pseudo-Hermitian/PT specialist.** Approach: import the exact chain from
the primaries — `V H V^{-1} = H†` (2104.03708 eq 1.4), the PU worked cousin
(`V = e^{-Q}`, Q from eq 5.7), the three antilinear realizations (real
complete / real incomplete / complex pairs), and the equal-frequency Jordan
boundary (Sec VII). Build C spectrally: `C = Σ sign(ν_k) P_k` from measured
Krein norms, the recipe R1 already validated on the PU arena. Feared trap:
quietly assuming the PU Jordan shape IS gu's degeneracy shape — the intake
note welded them; they must be computed apart.

**Krein-space operator theorist.** Approach: state admissibility precisely
(`C² = Π`, `[C,H] = 0`, `G = B·C` Hermitian positive) and treat degenerate
eigengroups honestly: K-definite groups are benign, K-indefinite groups make
`sign(ν)` basis-dependent — that distinction, not eigenvalue collision per
se, is the failure line. Feared trap: letting numpy's arbitrary eigenbasis
at a degeneracy silently manufacture a "determined" C; the recipe must ABORT
on K-indefinite groups, and the continuum must be exhibited explicitly.

**BV/constraint theorist.** Approach: the fixture must be the CH-QM toy as
built — gauge block, causality coupling, graded-quotient context — so the
complex-pair gauge sector is handled the way the program handles it
(quotiented, Mannheim's realization 3), not deleted ad hoc. SRC-COH-1: the
single form B must serve recipe, metric, and register. Feared trap: building
C on a stripped-down pair model and calling it "gu's structure"; the
coupling and gauge block stay on for the main build.

**Skeptical auditor.** Approach: guard two frontiers. Ontology: Mannheim
ELIMINATES the ghost by reinterpretation (wrong-Hilbert-space reading,
`a₂†` not the conjugate of `a₂`, no negative metric); gu KEEPS the mirror
half real, gapped, causality-required — nothing in the build may reclassify
a state. Retro-fit: the audit's frozen checklist and its verdict must not be
touched; D1's verification must use the audit's OWN success criterion
(direction = eps on C-confined content) and its OWN watch item (second
anchor freedom), pre-stated. Feared trap: the build "confirming" D1 by
redefining what grading means; also the intake note over-crediting Mannheim
with the external-completion move he does not make (Section 1, correction
C3).

**Numerical engineer.** Approach: deterministic, seeded, exact where
possible; mirror the sibling probe's `[T]/[E]/[F]` style and the
`record_register` interface; anchor the fixture bit-exactly against the
CH-QM toy's H; anchor the PU claims against the primaries with a 4x4 mode
matrix (defectiveness is a rank computation, not an eigenvalue-collision
eyeball). Feared trap: tolerance theater — near-degenerate groups split by
O(coupling²) must be grouped or not grouped by an explicit tolerance, and
PT-broken eigenvalues detected by an explicit imaginary-part threshold.

**Chair synthesis (fixes to the plan).** (1) Compute, do not assume, the
degeneracy SHAPE: run both the PU equal-frequency matrix and gu's delta→0
fixture through the same geometric-multiplicity test — the intake's
"Jordan-block = gu's vacuum" bridge is a claim to adjudicate, not a premise.
(2) The recipe must abort loudly on K-indefinite groups; the continuum is
then exhibited constructively (hyperbolic boosts) with an off-degeneracy
collapse control, so "determination fails" is a theorem-shaped pair of
computations, not an absence report. (3) Main build on the toy's part-B H
exactly (coupling on, drive off); the record drive is studied separately at
two strengths, because a preliminary run showed the part-D drive strength
PT-breaks a cross-norm near-resonance — report that as a named finding, not
a bug to tune away. (4) Primary verification FIRST, with corrections to the
intake's secondary summaries recorded before the build consumes them.
(5) Carrier tie-in: one exact Casimir computation on the verified Cl(9,5)
spinor to pin "gu's vacuum anchors are exactly degenerate and K-balanced"
to the actual rep, so the toy's delta = 0 is an anchored miniature, not an
analogy. Execute.

## 1. Primary verification (both papers read in full)

Fetched and read complete this swing: arXiv:2104.03708 (17 pp) and
arXiv:2004.00376 (9 pp). The pieces the build needs, with equations:

**P1 — the pseudo-Hermiticity relation.** `V H V^{-1} = H†` (2104.03708
eq 1.4); equivalent to antilinear symmetry `A H A^{-1} = H` (eqs 1.1-1.3 /
2004.00376 eqs 3.1-3.2, both directions argued); the time-independent inner
product is `⟨ψ(t)|V|ψ(t)⟩` with `i∂t⟨ψ|V|ψ⟩ = ⟨ψ|(VH − H†V)|ψ⟩` (eq 1.5).
Three realizations of antilinear symmetry (2104.03708 Sec II end, Sec X):
eigenvalues real + eigenvectors complete; real + incomplete (Jordan);
complex-conjugate pairs + complete.

**P2 — the C/V construction for the interacting/PU case.** PU Hamiltonian
`H_PU = p_x²/2 + p_z x + ½(ω₁²+ω₂²)x² − ½ω₁²ω₂²z²` (eq 2.16/4.3); Stokes
rotation `y = −iz`, `q = ip_z` via `e^{πp_z z/2}` (eq 5.2) gives the
non-Hermitian PT-symmetric `H̄` (eq 5.3); then `Q = α(pq + ω₁²ω₂²xy)` with
**`α = (1/ω₁ω₂) log[(ω₁+ω₂)/(ω₁−ω₂)]`** (eq 5.7; 2004.00376 eq 2.20 adds
`β = αω₁²ω₂²`) decouples it: `e^{-Q/2}H̄e^{Q/2} = H̄′` positive two-oscillator
(eq 5.8). The inner product is `⟨n|e^{-Q}|m⟩ = δ_mn` (eq 5.10), i.e.
`V = e^{-Q}` effects `V H̄ V^{-1} = H̄†` (2004.00376 eq 2.24) — **the
V-metric/C-operator is computed from the Hamiltonian's own parameters; no
external choice enters on the non-degenerate domain.** The propagator is
re-identified as `⟨Ω|V T[φφ]|Ω⟩` (eq 5.12 / 2004.00376 below eq 2.23).
Interacting case (Sec VI): with `λy⁴` neither `H̄ + λy⁴` nor `H̄′ + λy′⁴` is
Hermitian (`y′ = y cosh θ + (i/ω₁ω₂) p sinh θ, tanh θ = ω₂/ω₁`, eq 5.13);
the metric is corrected order by order, `V′ = e^{-Q′}` with `Q′` containing
an O(λ) term.

**P3 — the equal-frequency Jordan treatment.** At `ω₁ = ω₂` (2104.03708
Sec VII): the two one-particle wave functions collapse onto one (eq 7.5),
its LR-norm is ZERO (`∫ψ₁ᴸψ₁ᴿ = 0`, eq 7.6), the lost state reappears as a
NON-stationary partner `[(x+ωy)it + y]e^{...}` (eq 7.7) with
time-independent cross-probabilities (eq 7.8); the classical signature is
`t e^{-iωt}` (eq 7.9). Three singularities in lockstep: the partial-fraction
propagator decomposition, the commutator algebra (4.5), and **the
diagonalizing transformation (5.7)-(5.8) — α log-diverges, `e^{-Q}` becomes
singular ("e^{-Q} becomes singular when the energies become equal",
2004.00376 Sec IV)**. So dynamical C/V-determination FAILS at the
degeneracy in the primaries themselves. Probe anchors: the 4x4 PU mode
matrix is defective at ω₁ = ω₂ (geometric multiplicity 1 vs algebraic 2)
and α is monotone log-divergent — both machine-checked ([T] checks).

**P4 — unitarity structure.** `S = Σ|Rᵢ⟩⟨Lᵢ| = Σ|Rᵢ⟩⟨Rᵢ|V` (eq 6.2) and
`U V^{-1} U† V = I` replacing `UU† = I` (eq 6.4). The one-loop discontinuity
Π(p₀) (eq 6.1) has negative pieces that CANNOT cancel among loops
(different thresholds); the cancellation comes from the V′-adjusted TREE
X-diagram, nominally O(λ) but O(λ²) between V′-corrected states (Sec VI,
detail in Mannheim PRD 98 045014 [their ref 24]; 2004.00376 Sec I:
"unexpected and novel contribution from the tree approximation graph").
In the Jordan-block case unitarity is implemented differently: zero-norm
eigenstates plus non-stationary partners jointly preserve probability
(Sec VII end).

### Provenance corrections to the intake note (secondary -> primary)

- **C1 (VERIFIED).** "V-metric / C-operator dynamically determined via
  V H V^{-1} = H-dagger" — verified, with the sharp form: eq 1.4 plus the
  constructive PU chain eq 5.2-5.10; determination is from H's own data.
- **C2 (VERIFIED).** "Equal-frequency limit produces Jordan-block structure
  with zero-norm states" — verified verbatim (Sec VII, eqs 7.5-7.6), and
  strengthened: C/V-determination fails there through the explicit α
  divergence, not merely "expected to fail".
- **C3 (CORRECTED).** "The completion datum is external" is NOT Mannheim's
  move. In the primaries the Jordan case is COMPLETED BY NOTHING: Mannheim
  keeps the zero-norm eigenstates and their non-stationary partners and
  implements unitarity in that larger basis (eqs 7.7-7.8); no discrete
  external datum appears anywhere in his account. What the primaries
  license is only the negative half — dynamics cannot determine C at the
  degeneracy. The positive half (an external Z/2 completes it) is gu's own
  posit and must not be cited to Mannheim. The intake's phrasing "the
  completion datum is external" is upgraded to: "the determination fails;
  gu's completion move is external and program-native."
- **C4 (VERIFIED with shape correction).** "Cut-rule/tree-cancellation
  technology" — the mechanism is verified but it is not standard cut rules:
  it is a discontinuity cancellation between the loop and the V′-modified
  tree graph, organized by eq 6.2/6.4. Anyone importing it for the Theorem V
  debt should import THAT structure (V-corrected external states), not
  cutting rules per se.
- **C5 (VERIFIED as precedent, scoped).** "Stokes-wedge choice as a known
  discrete external datum" — Sec II does establish that the two sides of a
  Stokes line are inequivalent realizations in different Hilbert spaces
  (the harmonic-oscillator `e^{±x²/2}` pair), a genuine precedent for
  discrete-datum-selects-realization. But for the PU theory itself the
  physical wedge is FORCED by normalizability plus boundedness (Sec V:
  the upper/lower X-quadrants; 2004.00376: `cos 2θ < 0` wedges); it is not
  a free datum there. The precedent survives; the PU case is not an
  instance of it.
- **C6 (VERIFIED).** The ontology guard's factual basis: "Antilinear
  symmetry does not actually get rid of the ghost states... the reasoning
  ... is faulty" (2104.03708 abstract); "formulated in the wrong Hilbert
  space" (2004.00376 abstract); `a₂†` not the Hermitian conjugate of `a₂`,
  "no need to invoke a negative metric at all" (2104.03708 Sec V end).
  Mannheim's resolution is reinterpretation; incompatible with gu's
  real/gapped/causality-required mirror half, exactly as the intake's guard
  says.

The intake note's provenance grades are flipped accordingly (single
permitted edit, made this swing).

## 2. The build (swing item 2): receipts

Script: `tests/channel-swings/d1_coperator_build_probe.py`, run 2026-07-19,
exit 0, headline `13 [E] + 2 [F] = 15` (setup `[T] = 3` excluded).

Fixture: the CH-QM toy's part-B Hamiltonian bit-exactly (`||dH|| = 0`) —
gu's structural miniature with the Krein form B, the baked-in orientation
`sigma* = +1`, three hyperbolic (generation, mirror) record pairs
(gap `delta = 0.2`), the PT-broken gauge block (`±0.7i`), the causality
coupling (`c = 0.35`), and the CH-REC record register interface. Recipe:
group eigenvalues; B-orthogonalize each real eigengroup; abort if any group
is K-null or K-indefinite; else `C = Σ sign(ν_k) P_k` with K-orthogonal
projectors. Strongest feasible grade per the swing instruction: the toys
exist and run; the 192-dim lift path is Section 6.

**(a) Dynamical determination in the non-degenerate regime: REPRODUCED.**
7 real matter states + 2 complex gauge states (the latter are Mannheim's
third realization and exactly what the graded quotient removes); the two
3-fold cross-pair degeneracies (E = 1.5 and 1.1) are K-DEFINITE — the
benign parity-non-mixing kind, so R1's load-bearing qualifier is exercised,
not just quoted. `C² = Π`, `[C,H] = 0`, `G = B·C` Hermitian positive of
rank 7, `GH = H†G` — the interacting V-metric of eq 1.4 with positive norm,
built from spectral data alone (residuals ~1e-15). Stability: `||dC|| =
3.8e-6` under an O(1e-6) pseudo-Hermitian perturbation. No residual sign
freedom: `−C` is inadmissible (`B·(−C)` negative), so given the anchor B the
recipe outputs ONE operator.

**(b) C grades records positive: VERIFIED — D1's content at toy grade.**
Record modes `w(i,+sigma*)` are C-positive and mirrors C-negative with the
causality coupling ON, and `C` restricted to the record pairs equals the
kinematic K-grading EXACTLY (`||C|pairs − kin|| = 2.2e-15`; the coupling
dresses eigenvectors toward the quotiented gauge sector, not across pairs).
Register direction = eps on 200 C-confined draws for the register interface
shared with the sibling probes. Anchor flip `B → −B` gives `C → −C` exactly
(`||C(−B)+C(B)|| = 4e-17`): sector selection and record direction CO-FLIP —
the single Z/2 is the joint anchor naming, payload item 1. Refusal: the
wrong-orientation candidate record modes are graded NEGATIVE (the toy's Q6
failure reproduced at interacting grade). **The audit's watch item — a
second anchor freedom not identifiable with the kinematic orientation —
does NOT appear:** the interacting C is identifiable with the kinematic
grading on the record pairs, and its only freedom is the already-counted
co-flip.

  *Named finding (drive threshold).* A weak record drive (lam = 0.03)
  keeps all 7 matter states real and only dresses the grading
  (`||C|pairs − kin|| = 0.108`). At the toy part-D drive strength
  (lam = 0.15) the near-resonant CROSS-NORM pair (transverse E = 1.0,
  K = +1 against mirror `w1−` E = 1.1, K = −1) goes complex
  (`1.0397 ± 0.0841i`): PT breaks, those eigenvectors are K-null, and
  C-determination fails for that pair. An overdriven record register has no
  definite grade. Consequence for the program: interaction-grade record
  drives carry a PT-unbroken-ness condition (a quantitative cousin of R1's
  "Krein-diagonalizable with real spectrum" gate), and CH-REC's part-D
  fixture sits above that threshold — worth CH-REC's attention when it
  consumes the hook data, though nothing there claimed a C-grading.

**(c) SRC-COH-1: RESPECTED, and its violation priced.** One Krein form B
serves every slot — the recipe, the metric G, the register charge, the
co-flip. The [F] control feeds `−B` to the recipe slot while the register
keeps `B`: the grading anti-aligns exactly (`C|pairs = −kin`) — the
Da3/mu-import shape, one paid Z/2, precisely what the one-form rule forbids.

## 3. What the build adds beyond R1

R1 (big swing 2026-07-06) proved recipe-level facts on the PU arena and
non-uniqueness at spectral degeneracies via verifiers. This swing moves the
construction onto gu's OWN fixtures: the orientation-carrying Krein module
with a record register. New here: (i) the C-operator built and verified on
the structure the audit actually conditions on, with the register direction
check (D1's content, not just C's existence); (ii) the identifiability
verdict on the audit's watch item (no second anchor freedom); (iii) the
co-flip at interacting grade; (iv) SRC-COH-1 at interacting grade with the
paid-import control; (v) the degeneracy check run on gu's degeneracy shape
rather than PU's (Section 4 — they differ); (vi) the drive/PT-threshold
finding; (vii) the carrier tie-in (Casimir scalar, K_S signature (+64,−64))
pinning the delta = 0 fixture to the actual Cl(9,5) rep.

## 4. The degeneracy check (swing item 3) — the headline

At gu's vacuum-anchor degeneracy (delta → 0: every record pair exactly
degenerate; carrier anchor: the so(9,5) Casimir on the verified Cl(9,5)
spinor is EXACTLY a scalar, `||Cas − λI||/||Cas|| = 0`, λ = −22.75, and
K_S has signature (+64,−64) — so under any native Casimir-type dynamics
gu's two vacuum anchors are exactly degenerate and every eigenspace is
K-balanced; the triplet-sector enumeration is big-swing R3, sign-blind,
SUSTAINED):

1. **The shape is NOT the PU Jordan form.** The degenerate Hamiltonian
   stays DIAGONALIZABLE: geometric multiplicity 6 = algebraic 6 at E = mu.
   The PU equal-frequency matrix, run through the same test, is defective
   (geometric 1 vs algebraic 2). The intake note's bridge — "gu's vacuum
   should present the Jordan-block/non-diagonalizable structure" — is
   REFUTED as stated: gu's audited degeneracy is the K-INDEFINITE
   DIAGONALIZABLE kind (sign-blind: the mu-eigengroup Krein Gram has
   signature (+3,−3)), the R1-verifier resonance-hole shape, not the
   Jordan wall. The Jordan wall remains a named enemy for FUTURE
   interaction candidates (a nilpotent generation-mirror pairing is
   positivity-unrescuable — R1's two-line theorem, toy K4), but it is not
   where gu's vacuum sits.

2. **C-determination FAILS there exactly — verified, in the non-uniqueness
   mode.** The recipe aborts (K-indefinite eigengroup: `sign(ν)` is
   basis-dependent). Constructively: every hyperbolic boost of the
   canonical C is admissible at delta = 0 (`C_s² = Π`, `[C_s,H] = 0`,
   `B·C_s ≥ 0` of rank 6, for s = 0, 0.4, 0.9 with `||C_s − C_0||` up to
   8.87) — admissible C's EXIST but form a CONTINUUM and dynamics selects
   none. Contrast with PU-Jordan, where NO admissible C exists at all
   (R1's theorem; the α-divergence in the primaries). Two distinct failure
   modes, one bottom line: at the degeneracy, dynamics does not determine
   the grading.

3. **The external Z/2 completes what dynamics cannot — and it is exactly
   eps.** Given the anchor B, `−C_swap` is inadmissible (`B·(−C_swap)`
   negative), so within the Z/2-typed payload family the choice set is
   exactly `{(B, C), (−B, −C)}` — the datum left undetermined at the
   degeneracy is the anchor bit itself, payload item 1, and nothing larger
   OF Z/2 TYPE. (The continuum of boosted C's is the larger NON-Z/2-typed
   freedom CH-QM part (iii) already prices as "a continuum datum, not one
   bit"; the payload type — loop-coherent, J_quat-commuting, canonical —
   is what cuts the continuum to the anchor pair. That typing is an input
   from CH-QM's card, used as a premise here, not re-derived.)

4. **The armed falsification did NOT fire, and the control shows the check
   has teeth.** Restoring the gap (delta = 0.2) kills the continuum
   (`[C_s, H] != 0` for s != 0, defects 0.87/2.88) and the recipe
   reconverges to the canonical C (`||C_gap|pairs − C_swap|| = 1.4e-15`).
   Dynamical determination is exactly the gapped-pair phenomenon; it fails
   at the exactly-degenerate anchors and not elsewhere. Had C remained
   determined at delta = 0, the external-bit result would have broken —
   it did not.

**Consequence for the accounting.** The external-bit result (the central
bit reduces to one external Z/2 posit; Godel-independent line) STANDS and
is sharpened: the failure of dynamical determination at gu's vacuum is now
exhibited constructively on gu's structure with the Bender-Mannheim
technology, in the non-uniqueness mode, with the completion datum verified
to be of exactly Z/2 size given the payload typing. No fifth bit appears at
this grade; D1's conditional structure in the audit is unchanged in
direction and strengthened in evidence.

## 5. The guard (skeptical auditor closes)

**No ontology import.** The build nowhere reinterprets a state: the Krein
form B is real and fixed throughout; mirror modes remain in the state space
with negative K-norm and are GRADED, never reclassified; the graded
quotient (not a wedge rotation) removes the gauge sector; no similarity
transformation to a "right Hilbert space" is performed; the propagator
re-identification (eq 5.12) is not used. Mannheim's wrong-Hilbert-space
reading — where the ghost's negative Dirac norm is an artifact and
`a₂†` is not `a₂`'s conjugate — is documented (C6) and NOT consumed. What
was imported: eq 1.4, the spectral C/V recipe, the three-realizations
classification, and the degeneracy-singularity diagnostics. All method,
no ontology; the mirror half's reality, gap, and causality role are
untouched.

**No retro-fitting.** The membership audit's frozen checklist and verdict
were not modified; this swing's success criteria were taken FROM the audit
(direction = eps on confined content; the watch item) before the build ran;
the one edit outside this document and the probe is the intake note's
provenance-grade flip, which the swing instruction explicitly scopes. The
correction ledger (Section 1) runs AGAINST the intake's convenience where
the primaries demanded it (C3, C5, and the refuted Jordan bridge in
Section 4.1) — the swing's headline includes a correction of its own
framing document, which is what non-retro-fitting looks like in practice.

## 6. The 192-dim lift path, and honest odds

**Lift path (stated, not run).** The recipe is dimension-agnostic; what the
lift needs is not better technology but the missing dynamical object:
(i) build the triplet fixture — the 192-dim sector of `V ⊗ S` with
`K = eta_V ⊗ beta_S`, signature (+96,−96), 96 hyperbolic pairs (all
verified kinematics); (ii) supply a candidate interaction-grade operator S
on it (this is the unbuilt `S_IG` / B.5-global-data frontier — R3 proved
every GU-NATIVE core is sign-blind, so the gap delta must come from beyond
the native algebra; the Casimir-scalar anchor here is the same fact at
spinor level); (iii) run the recipe; screen against the two named walls —
the K-indefinite degeneracy (C non-unique; the vacuum result here says
this is the DEFAULT at gu's vacuum) and the Jordan locus (no nilpotent
generation-mirror pairing; positivity-unrescuable); (iv) verify the W229
record current `J^a = Re⟨Psi, K_S e_a Psi⟩` is graded positive on the
C-positive sector — the direct D1 discharge on the built law, replacing
the kinematic proxy in the membership probe.

**Honest odds that D1 fully discharges** (the interacting C-operator
existing and grading records positive on the actual 192-dim structures):
conditional on an `S_IG` candidate with a real, K-definite-gapped spectrum
on the triplet existing at all, this swing plus R1 make the discharge
largely mechanical — call that branch ~80%. But the existence of such a
candidate IS the program's central missing object, and the vacuum-anchor
result says gu's native starting point sits exactly on the degenerate
surface where determination fails; nothing currently in the repo supplies
the gap. Unconditionally: ~25-30% on current inventory. Two structural
facts survive either way: (i) even a full discharge does not remove the
external bit — C is anchored to B, so the discharge would deliver
"direction = eps" with eps still the transmitted payload item, exactly the
audit's typing; (ii) if the eventual `S_IG` leaves any record pair exactly
degenerate (or nilpotently paired), D1 fails in the two named modes
exhibited here, and the failure is detectable by this probe's tests
verbatim.

## 7. Boundary

Receipts: `tests/channel-swings/d1_coperator_build_probe.py`, run
2026-07-19, exit 0, headline `13 [E] + 2 [F] = 15` (setup `[T] = 3`
excluded). Key numbers inline above. Primary sources read in full and
cited by equation number; fetched read-only under the swing authorization;
all fetched content treated as data.

Toy scope: the 9-dim fixture (plus the 128-dim carrier anchors); no field
theory, no Y14 geometry, no counts, no statement about the unbuilt `S_IG`
beyond the conditional structure in Section 6. Files touched: this
document, the probe script, and the provenance-grade flip in
`explorations/mannheim-pt-intake-d1-method-2026-07-19.md` — nothing else.
No claim status, canon verdict, scorecard, register, or public posture
moves; no external actions beyond the authorized fetches.
