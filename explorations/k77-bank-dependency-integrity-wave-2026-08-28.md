---
title: "K77 bank dependency integrity wave"
status: exploration
doc_type: result
created: 2026-08-28
operational_state: complete
target_claim: NONE-NOT-A-KILL
---

# K77 bank dependency integrity wave

This wave restores the K77 exact-bank process gate to the repository's current
append-only ledger and adaptive specialist-routing contracts. It also proves
that the canonical exact-bank fixture cannot currently be re-signed: the sole
authorized recursive builder completes its expensive reconstruction and then
rejects the hardened predecessor chain because the historical v0.122 producer
no longer emits its required immutable `PASS 30/30` certificate.

The builder wrote no fixture. The canonical bank remains byte-identical to its
pre-wave backup at
`4311ae91fd5585349f94bb6ed28ee61d0f3ff31a8914f49eb611b024be37f49f`.
No scientific coefficient, sparse vector, rank, causal bank or downstream
tangent fixture moved.

The audit no longer crashes on the retired `mandatory_eight` field or rejects
the live ledger merely because it is newer than v0.125. It verifies the exact
v0.124 historical baseline, follows all 139 append-only predecessor edges from
the contract's current v0.263 ledger, checks the three-role universal core and
retired-router fence, and executes six live planted controls. Its bounded final
state is 29/30: the sole red assertion lists the four exact stale dependency
paths explicitly.

```gu-typed-objects
result: current adaptive routing and append-only ledger ancestry are checked while the stale K77 bank remains fail-closed MAP-TYPE=evaluation
carrier: repository-native Python audit, ledger predecessor chain and content-addressed fixture identity LAYER=UNTYPED CHIRALITY=N/A
pairing: exact hash and contract predicates ON=repository-native-filesystem
real_structure: N/A
grading: N/A
action_owner: N/A
target: replace a retired-contract crash with an exact resumable provenance failure MAP-TYPE=evaluation
```

## Preflight bookend

The A1 source register remains `ADHERED 106 / PARTIAL 4 / UNTYPED 1`; ledger
v0.263 remains terminal with no eligible row reopener; no prediction wake
fired; and the named B2 action-root set remains empty. The complete serialized
process-gate census entered at 272 green / 151 inherited red / 423 total.

Four bank inputs have legitimately changed since materialization: one path-only
research-cycle migration and three later fail-closed probe hardenings. Hand-
editing dependency hashes was rejected because it would bypass the declared
builder and could launder scientific drift into a content-addressed fixture.

## Exact effect

- Audit contract loading: uncontrolled `KeyError: mandatory_eight` to complete
  adaptive-router evaluation.
- Ledger currency: hard-coded v0.124/v0.125 ceiling to a proved 140-node chain
  from current v0.263 through the exact v0.124 bank baseline.
- Routing controls: three universal roles, the retired fixed-eight fence,
  exact-computation and distributed-provenance triggers, required evidence
  mode and six planted mutations.
- Diagnostic state: one generic dependency failure to the exact four stale
  paths.
- Fixture state: unchanged; canonical reconstruction stopped before write when
  the historical producer failed its own required pass certificate.

## Postflight bookend

1. **Strongest overclaim or hidden assumption.** A current audit does not make
   the bank current. The fixture remains deliberately stale and downstream
   consumers must continue to reject it.
2. **Strongest contrary construction.** Re-signing the existing payload without
   executing the builder would make every hash green while skipping the only
   repository-owned scientific reconstruction check. That route remains
   forbidden.
3. **Weakest reproducibility or propagation seam.** The builder suppresses the
   historical producer's captured output, so the exact failed sub-check is not
   exposed. The continuation wake is to make that failure diagnostic or repair
   the producer contract in a separately admitted builder/probe run, then
   rebuild exact and tangent banks in order.

No source status, ledger row, action owner, SG4 phase, prediction,
confirmation, scientific verdict, canon, protected TWG1 or paper path, release
state or public posture changes.
