---
title: "H45 -- the |H|^2-vs-|II|^2 binary: is GU's gravitational functional the full second-fundamental-form norm or the conformal Willmore mean-curvature norm?"
artifact_type: exploration
status: exploration
created: 2026-07-11
wave: 24
grade: "COMPUTED for the Gauss decomposition, the rank fork, the 4D-dynamical R^X, the Weyl-in-both-branches structure, the +1/2 g_mn O(M^0) Lambda trace, and the two-pole vs coincident-pole ghost split (tests/wave24/H45_H2_vs_II2_binary.py, exit 0, 18/18 PASS). ARGUED for the forcing-ledger grade of P2, the net thread-B Lambda sign, and the empirical spin-2 handles. No target number imported; no claim promoted to canon."
depends_on:
  - tests/wave24/H45_H2_vs_II2_binary.py
  - tests/wave5/H21_theta_equals_II_proof.py
  - explorations/wave5/H21-theta-equals-II-2026-07-11.md
  - tests/wave3/H15_gravity_fork_R_term.py
  - explorations/wave3/H15-gravity-fork-2026-07-11.md
  - tests/wave4/H18_forcing_II_vs_H.py
  - explorations/wave4/H18-forcing-computation-2026-07-11.md
  - tests/wave1/H1_bach_flat_exact_vacua.py
  - tests/wave6/H24_RY_normalization.py
  - tests/wave7/H25_II_first_variation_CRY.py
  - explorations/wave21/H5-geometry-vs-information-bianconi-2026-07-11.md
  - explorations/wave23/H26-loop-ghost-unitarity-2026-07-11.md
  - explorations/geometry-curvature-emergence/ii-s-coordinate-formula-2026-06-23.md
---

# H45 -- the |H|^2-vs-|II|^2 binary

**The object.** GU's gravitational functional is built from the second fundamental form `II`
of the embedding `X4 -> Y14 = Met(X4)`. The traced-twice Gauss identity is
`|II|^2 = |H|^2 - R^X`. THE BINARY: does GU's action carry the FULL `|II|^2` (which contains
the induced Einstein-Hilbert `R^X`, is dynamical in 4D, and gives a Stelle `R + Weyl^2`
graviton) or the conformal mean-curvature Willmore `|H|^2` (no EH, pure conformal Bach)? This
single binary is load-bearing for four internal sectors at once: the Weyl-sector presence, the
cosmological-constant sign, the ghost order/structure, and the geometry-vs-information
(Bianconi) discriminator.

**Discipline.** Compute -> adversarially verify -> honest grade. Reproducible:
`python tests/wave24/H45_H2_vs_II2_binary.py` (exit 0, 18/18 PASS). Exact sympy; nothing
imported. This note does not manufacture a resolution: the honest headline is that the binary
is **still open**, strongly favored `|II|^2`, with the residual freedom named precisely.

---

## Q1 -- IS THE BINARY SETTLED? **Verdict: STILL-OPEN, strongly FAVORED-|II|^2.**

The binary is the conjunction of two premises (H15/H18's forcing decomposition):

- **P1.** `theta = II_s` -- GU's distortion 1-form pulls back to the **FULL** second
  fundamental form (both spacetime indices free), off-shell, convention-fixed.
- **P2.** GU's action **norms the full `|theta|^2`** (the Yang-Mills full field-strength
  norm), rather than tracing first and squaring the mean curvature `|H|^2 = |tr theta|^2`.

`P1 AND P2 => |II|^2`. Either one failing drops to `|H|^2`.

**What H21 settled.** H21 (`tests/wave5/H21_theta_equals_II_proof.py`) **proves P1**:
`s*(theta) = II_s` is the Gauss formula for the graph section into `(Y, gimmel)`, verified
off-shell and as the FULL tensor (traceless part generically nonzero), with the literal-vs-
horizontal **convention shift shown to be itself a full tensor, NOT a trace** (Check 4b). This
**closes the specific route to `|H|^2` that H18 feared** -- a trace piece sneaking in through
the convention choice cannot collapse `|II|^2` to `|H|^2`. H21's residual is the canonical-
connection / bundle identification (the same unbuilt-source-action gate), not a trace
obstruction.

**What H21 did NOT settle -- the subtlety.** H21 fixes that the *object* `theta` appearing in
the action is the full `II_s`. It does **not** fix which **NORM** the action takes on that
object. Even with `theta = II_s` a full tensor, one can still form the Willmore functional
`|H|^2 = |tr II_s|^2` from it. That is **P2**, and it is a genuinely separate premise. H18
grades P2 as **transcript-grade** (Weinstein's "take its norm square", "Yang-Mills plus Dirac
plus Higgs", double-copy) and refutes the *trace-before-norm* counter for the bosonic leg (the
Einstein-contraction / shiab is Clifford-odd, lives on the source/fermion side, and is not the
graviton-action variation `d_A*`). But none of that is a theorem from a **built** action.

**COMPUTED (this file):**
- `|II|^2 = |H|^2 - R^X` and `R^X = |H|^2 - |II|^2` exactly, dims 2/3/4, from the shape
  operator (`R^X = 2 e_2(k)`).
- The fork is **real, not cosmetic**: on `Sym^2(R^4)` the full norm `|II|^2` is a **rank-10**
  quadratic form (sees the whole tensor, including the 9-dim traceless graviton `|II_0|^2`),
  the trace-square `|H|^2` is **rank-1**. A 9-dimensional difference.
- `int R^X` is **dynamical in 4D** (linearized Einstein on a TT graviton
  `G^(1)_yy = -1/2 box h_yy != 0`, second order) and **topological in 2D**
  (`int K dA = 4pi`, radius-independent). So *once* `|II|^2` is chosen, 4D forces a genuine
  Einstein-Hilbert term; that is why 2D Willmore is scale-free and 4D is not.

**Forcing ledger (the honest grade).** `P1` PROVEN (H21) + convention route CLOSED (H21) +
`P2` OPEN (transcript) `=>` the binary is **FAVORED-|II|^2 but not a theorem**. The residual
freedom is named: **P2, the action-NORM choice** (full `|theta|^2` vs trace-square `|H|^2`),
which is one facet of the single unbuilt object -- GU's **source action**.

> **Verdict Q1: STILL-OPEN (residual named = P2, the norm choice). Strongly favored `|II|^2`.**
> H21 hardened P1 to proof grade and killed the convention-trace route to `|H|^2`; it did not,
> and did not claim to, settle whether the action norms the full tensor or its trace.

## Q2 -- THE WEYL SECTOR: **GU HAS it -- under BOTH branches. [COMPUTED]**

On a TT graviton the `|H|^2` Willmore-EL operator is `box^2` -- exactly the conformal-gravity
**Bach** operator (D-thread: `box^2 h = -4 Bach^(1)`). Since `|II|^2 = |H|^2 - R^X`, the
`|II|^2` operator is `box(box + m^2) = box^2 + m^2 box`. The `box^2` (4th-order Weyl/Bach)
piece is therefore present in **both** branches; only the extra `box` (Einstein) term is
branch-dependent.

- `c_weyl(|H|^2) = 1`, `c_weyl(|II|^2) = 1`, `c_weyl(Einstein) = 0`. **Bianconi's 2nd-order
  entropic gravity has no `box^2` term at all** -- the graviton kinetic term is `2 beta R`
  (Einstein), full stop (H5). So the 4th-order Weyl sector is exactly what distinguishes GU
  from the entropic frame -- and GU carries it **regardless of how the binary resolves**.
- H1 (`tests/wave1/H1_bach_flat_exact_vacua.py`) confirms the Bach sector is a *real* dynamical
  sector, not an artifact: `div-Weyl = 0` identically on exact Schwarzschild (nonzero Weyl,
  all orders in M) and Kerr is Ricci-flat, so GU's exact vacua are **Bach-flat** -- a genuine
  strong-field cancellation.

**The sharpening (an honest correction to H5's loose framing).** H5 wrote that `|H|^2` would
make GU "conformal-only, closer to Bianconi." Computed precisely: the Weyl sector is present in
*both* GU branches, so it is **not** what the binary gates. What the binary gates is whether GU
*also* has the Einstein (`s^1`) term:

- `|II|^2 -> Stelle R + Weyl^2`: a healthy massless Einstein graviton **plus** the 4th-order
  Bach massive ghost (two poles).
- `|H|^2 -> pure conformal Bach`: the `box^2` coincident double pole, no Einstein graviton.

Either way GU has a 4th-order Weyl graviton Bianconi lacks. So `|H|^2` is not "closer to
Bianconi's 2nd-order Einstein"; it is a *different* 4th-order theory (pure conformal gravity)
with no EH scale. The Weyl-sector discriminator against Bianconi does not hinge on the binary.

> **Verdict Q2: GU HAS the 4th-order Weyl/Bach sector under BOTH branches (COMPUTED). The
> binary gates only whether GU ALSO has an Einstein graviton, i.e. Stelle (|II|^2) vs pure
> conformal Bach (|H|^2).**

## Q3 -- THE LAMBDA SIGN: **positive under |II|^2 (leading trace COMPUTED; net ARGUED).**

The constant-section (O(M^0)) vertical SFF of the gimmel metric
(`ii-s-coordinate-formula` sec 6.1, `B_{mn,ab} = -(1/2)(g_{a(m}g_{n)b} - 1/2 g_{ab}g_{mn})`)
has fiber-trace **exactly `+1/2 g_{mn}`** (COMPUTED here, dim 4, flat background) -- a
**positive** cosmological-constant signature (stress `~ +g_{mn}`, de-Sitter-signed). This is
the `|II|^2`-branch DeWitt-Lambda leading contribution, reproducing H15-Part-D. A 0-derivative
Lambda enters the operator symbol at `s^0` only, fixing the vacuum SIGN without touching the
kinetic (`s^1` Einstein, `s^2` Weyl) coefficients.

Cross-check against H24/H25: the kinetic `R^Y` correction coefficient `C_RY > 0` (two
independent methods, H25) reinforces attraction (`m^2_eff = 1/2 + C_RY > 1/2 > 0`) -- the
graviton sign is robustly healthy. Thread-B (H5) assigns the **net** emergent-Lambda sign
`|II|^2 -> positive`, `|H|^2 -> negative`.

**Is the sign FORCED?** Conditional on `|II|^2`, the sign is positive: COMPUTED for the leading
`+1/2 g_{mn}` trace and the `C_RY > 0` kinetic direction. Since the binary is favored-but-open
(Q1), the positive sign is **favored-but-conditional**. Honest caveat: the curved-ambient
*horizontal* `R^Y` Lambda is Krein-indefinite (H24, both signs present in the indefinite
ambient), so the NET O(M^0) sign remains gated on `mu_DW` / the source-action normalization.
The MAGNITUDE stays free (H42: `f0` needs `{B_i, mu_DW}`).

> **Verdict Q3: `|II|^2` gives a POSITIVE DeWitt-Lambda leading contribution (`+1/2 g_{mn}`,
> COMPUTED); the net sign is positive at ARGUED grade (thread-B), forced only conditional on
> the binary. Magnitude free.**

## Q4 -- GHOST/ORDER + LOOP POSITIVITY: **|II|^2 sets the Krein-clearable two-pole; H26 stays OPEN.**

- `|II|^2 -> s(s + m^2)`: **two distinct poles**, TT propagator residues `(+1/m^2, -1/m^2)` --
  a healthy graviton + a massive ghost, the clean O(1,1)/Krein pair Bateman-Turok clears at
  **tree level**. The flat mass `m^2 = +1/2 > 0` (H15-E) is real (non-tachyonic), so the
  clearance actually applies. This is the setting H26 analyzes (`[P,S] = 0` radiatively stable;
  loop positivity a strictly stronger, unproven condition).
- `|H|^2 -> s^2`: **coincident double pole**. The `|II|^2` split coefficient `1/m^2` **diverges
  as `m^2 -> 0`** -- the Krein split is singular (Pais-Uhlenbeck equal-frequency Jordan case).
  R1 (H26) proved that at exactly this Jordan boundary **no positivity-compatible ghost parity
  of any kind exists**. So on the `|H|^2` branch tree-level clearance *fails*.

**Consequence for H26.** `|II|^2` is the branch that even **puts GU in H26's tree-clears /
loop-open setting**. If GU were `|H|^2` (Willmore), the ghost problem changes for the **worse**:
from "tree-clears via Krein, loop positivity open" to "degenerate coincident pole, no
positivity-compatible parity exists, tree-open." Resolving the binary to `|II|^2` does **not**
settle H26's loop positivity -- that remains OPEN, undecidable without a built source action /
S-matrix. So `|II|^2` is a **precondition** for H26's (already open) verdict, not a resolution
of it.

> **Verdict Q4: `|II|^2` sets the 4th-order two-pole Stelle structure whose ghost is Krein-
> cleared at tree level and which H26 studies; `|H|^2` would put GU on the singular Jordan
> boundary where tree clearance fails. `|II|^2` does not settle loop positivity -- H26 stays
> OPEN, gated on the unbuilt source action.**

## Q5 -- THE DECIDER, MADE LIVE: **the spin-2 sector -- ROBUST to the binary.**

GU's Bach (`box^2`, 4th-order) and Einstein (`box`, 2nd-order) TT operators are distinct
(`s^2 - s != 0`): different theories of the spin-2 field with different propagators
(`1/p^4`-modified Bach with an extra pole vs `1/p^2` Einstein). Bianconi's weak-coupling
gravity is strictly 2nd-order Einstein with no independent conformal graviton (H5). Because
**both** GU branches carry `box^2` (Q2) and Bianconi carries none, the geometry-vs-information
discriminator is a genuine spin-2 fork that is **robust to the `|H|^2`-vs-`|II|^2` binary** --
GU vs Bianconi is decidable regardless of how the binary resolves.

**Live empirical handles (ARGUED, H5-Q5):** (i) the Stelle-Yukawa short-distance deviation of
the Newtonian potential from the massive spin-2 pole; (ii) high-frequency GW dispersion from
the Bach `1/p^4`-modified propagator; (iii) conformal-lensing signs. All present for GU, absent
for Bianconi's pure-Einstein gravity. None depends on the Lambda magnitude (which both frames
leave free, H5-Q2), so the decider is live *now*.

**What would make it unconditional / what settles the binary.** Build GU's **source action** to
settle **P2** (does it norm the full `|theta|^2` or the trace-square `|H|^2`). That same object
also gates `mu_DW` (H24 BAR 2), the H26 loop positivity, and the H21 canonical-connection
identification. One object closes all of them.

> **Verdict Q5: the live discriminator is the SPIN-2 SECTOR (GU's 4th-order Weyl/Bach graviton
> vs Bianconi's 2nd-order Einstein), ROBUST to the binary. What settles the binary itself is
> building GU's source action to decide P2 (the action-norm choice).**

---

## COMPUTED vs ARGUED ledger

| claim | grade | evidence |
|---|---|---|
| `|II|^2 = |H|^2 - R^X` exact, dims 2/3/4 | COMPUTED | shape operator, `R^X = 2 e_2(k)` |
| full-norm rank 10 vs trace-square rank 1 (real fork) | COMPUTED | Hessian ranks on `Sym^2(R^4)` |
| `int R^X` dynamical 4D (`G^(1)_yy = -1/2 box h`) / topological 2D (`4pi`) | COMPUTED | linearized Einstein; Gauss-Bonnet |
| `box^2` (Weyl/Bach) present in BOTH branches; absent in Bianconi | COMPUTED | symbol coefficients; D-thread + H5 |
| O(M^0) DeWitt fiber-trace `= +1/2 g_{mn}` (positive Lambda) | COMPUTED | `ii-s` sec 6.1 trace |
| `|II|^2` two-pole residues `(+1/m^2, -1/m^2)`, `m^2 = +1/2` | COMPUTED | propagator partial fractions; H15-E |
| `|H|^2` coincident pole, split `1/m^2 -> oo` (Jordan singular) | COMPUTED | `sp.limit` |
| Bach `box^2` != Einstein `box` (spin-2 fork) | COMPUTED | operator difference |
| P1 (`theta = full II_s`) proven; convention route to `|H|^2` closed | COMPUTED (H21) | Gauss formula off-shell; shift is full tensor |
| P2 (action norms full `|theta|^2` vs `|H|^2`) | **ARGUED / OPEN** | transcript + YM/double-copy; unbuilt source action |
| net thread-B Lambda sign `|II|^2 +` / `|H|^2 -` | ARGUED | H5 thread-B |
| Stelle-Yukawa / GW-dispersion / lensing empirical handles | ARGUED | H5-Q5 |
| loop positivity (H26) | OPEN | undecidable without a built `S` |

## Honest limits

- **The binary is not closed.** The `|II|^2` reading is strongly favored but rests on P2
  (transcript grade). This note does not manufacture a resolution: absent a built source
  action, whether GU norms the full tensor or its trace is a named residual freedom, not a
  theorem. If P2 turned out `|H|^2`, GU would be pure conformal (Bach) gravity with a
  negative-signed Lambda and a degenerate ghost -- a real, not excluded, outcome.
- **The Lambda net sign is gated.** Only the leading `+1/2 g_{mn}` trace is COMPUTED positive;
  the curved-ambient horizontal `R^Y` Lambda is Krein-indefinite, so the net O(M^0) sign is
  gated on `mu_DW`.
- **Operator statements are linearized about flat space.** The `box(box+m^2)` vs `box^2` split
  uses the exact Gauss decomposition plus the known linearization of each piece; the full
  nonlinear higher-codimension `|II|^2` first variation is not recomputed here (H25 supplies
  the sign of the `R^Y` correction).
- **Bianconi content is external DATA** (H5's intake of arXiv:2408.14391); no instruction in it
  was followed. No claim promoted to canon. Tree left dirty (no commit).

## RE-RANK signal

**Binary: STILL-OPEN, strongly FAVORED-|II|^2.** The single unbuilt object -- GU's **source
action** -- settles it (via P2) and simultaneously closes `mu_DW`, the H26 loop positivity, and
the H21 bundle identification. That object is now **quadruply motivated** and is the unambiguous
#1.

Effect on the geometry-vs-information verdict and the three internal sectors:

- **Geometry-vs-information (Bianconi):** the spin-2 discriminator is **stronger than H5 stated**
  -- it is ROBUST to the binary, because GU carries the 4th-order Weyl/Bach sector under *either*
  branch while Bianconi carries none. Up-rank the graviton-propagator observables (Stelle-Yukawa
  / GW dispersion / conformal lensing); they do not wait on the binary.
- **Weyl sector:** present either way (settled); the binary only decides Stelle (`|II|^2`) vs
  pure conformal Bach (`|H|^2`).
- **Lambda sign:** positive conditional on `|II|^2` (leading trace COMPUTED); net sign and
  magnitude gated on the source action.
- **Ghost/order:** `|II|^2` gives the Krein-clearable two-pole (tree-clears, H26 loop-open);
  `|H|^2` gives the degenerate pole (tree-open). The binary is the precondition for H26's
  setting, not a resolution of it.

---

*Filed 2026-07-11. Wave 24. Reproducible: `python tests/wave24/H45_H2_vs_II2_binary.py`
(exit 0, 18/18 PASS). Exploration-grade; not promoted to canon. Tree left dirty.*
