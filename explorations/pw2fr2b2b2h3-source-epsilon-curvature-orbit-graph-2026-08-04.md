---
title: "PW2F-R2B2B2H3 conditional source-epsilon curvature-orbit graph"
status: reconstruction
doc_type: exploration
updated_at: "2026-08-04"
---

# PW2F-R2B2B2H3 conditional source-epsilon curvature-orbit graph

## Question

H2 closed the fixed-background active residual/operator/primalizer second jet,
but that calculation held the spin curvature fixed. The next gate asked for
the source-epsilon curvature graph before either separate quartic bank.

This swing asks one narrower exact question:

> On the admitted conditional active Spin(9,5) port, does the source
> connection graph carry an independent Maurer--Cartan curvature coefficient,
> or does the complete moving curvature/operator graph close by gauge
> covariance through `(1,r,s,rs)`?

The source epsilon, the conditional active rotor, repository `h=exp(u)`, and
reduction epsilon remain distinct. A local active answer cannot by itself
construct the global public-source-to-active real-form bundle morphism.

## Independent connection-level construction

The first fixture is deliberately independent of the sparse Clifford
operator. It uses two noncommuting exact nilpotent matrix directions and a
coordinate-dependent source transformation `epsilon(x,y;r,s)`. For a
nonflat noncommuting connection `Gamma`, it constructs

```text
B_epsilon = epsilon^-1 Gamma epsilon + epsilon^-1 d epsilon.
```

Direct differentiation gives the exact identity

```text
F(B_epsilon) = epsilon^-1 F(Gamma) epsilon
```

before any coefficient extraction. The mixed source-coordinate curvature
slot is live in all four matrix entries and equals the independently assembled
four-family product rule

```text
(D_rs epsilon^-1) F
+ (D_r epsilon^-1) F (D_s epsilon)
+ (D_s epsilon^-1) F (D_r epsilon)
+ F (D_rs epsilon).
```

If `epsilon^-1 d epsilon` is omitted from the connection, the mixed curvature
defect is nonzero in all four entries. The Maurer--Cartan term is therefore
essential at connection level, but it supplies no independent curvature
coefficient after the complete connection graph is assembled: it cancels into
homogeneous curvature conjugacy.

## Conditional active Spin(9,5) graph

The active graph uses two noncommuting grade-two Spin directions and their
formal exponential/inverse jets through `(1,r,s,rs)`. The jets multiply to
identity through mixed order. Applying them to the accepted reconstructed
spin curvature gives exact coordinate counts

```text
F_epsilon = (360, 132, 121, 121).
```

Both first slots and the mixed slot equal independent commutator/product-rule
assembly. Every slot remains in internal Clifford grade two and passes the
active right-H, Krein-skew, and C-plus word checks.

The operator is then rebuilt from moved ingredients rather than defined by a
transported answer:

- moved normalized trace;
- moved `Phi1` and `Phi2`;
- fixed external metric/Hodge on this pure source-gauge orbit;
- moved curvature;
- native grade projector; and
- final Shiab reduction.

The explicitly assembled result equals the transported active residual in
every jet slot. The sparse residual coordinate counts are

```text
Upsilon_epsilon = (13, 2, 2, 2).
```

Curvature-only motion and epsilon-soldered-operator-only motion are both live,
but neither equals the complete mixed residual. Their combination is required.
The fully co-moving residual norm is exactly

```text
P(Upsilon_epsilon,Upsilon_epsilon)
  = (981/64, 0, 0, 0).
```

Thus the source gauge orbit is an exact null direction of the active
residual-square action at this conditional local grade even though its
curvature and residual coordinates move nontrivially.

Earned result:

```text
CONDITIONAL_ACTIVE_LOCAL_SOURCE_EPSILON_CONNECTION_CURVATURE_OPERATOR_ORBIT_GRAPH_CLOSED
```

## Hostile boundary

This is not the complete global H3 demanded by the previous broad gate. The
public source uses an H-valued epsilon and a `(7,7)`-type presentation; the
repository calculation uses a conditional active Spin(9,5) right-H/Krein
port. No inspected source or current construction supplies the global
real-form bundle morphism, transition functions, or descent atlas that would
identify those objects.

The exact local result sharpens the blocker: the missing item is not another
local Maurer--Cartan curvature coefficient. It is the typed global
source-to-active morphism (or an exact proof that the source branch exits the
active port's scope). Until that gate closes, neither complete 35-monomial
`I1 A4` nor `I2B C4` bank is admitted.

The result also does not perform multi-index Green/Helmholtz reduction, select
`kappa1`, close the live C3 return, or establish a domain, quotient,
observation map, characteristic, or physics result.

## Campaign fences and next gate

P1/P2/P3 remain unchanged and unused. Curt remains
`FORMALLY_SEPARATE_INSIDE_ERIC_LANE`. `TG-1 AND TG-2 AND TG-3` remains
`NOT_PROMOTED`.

Resume at
`PW2F-R2B2B2H4-SOURCE-TO-ACTIVE-REAL-FORM-BUNDLE-MORPHISM-OR-SCOPE-EXIT-THEN-SEPARATE-C4-BANKS`.
Construct the global typed port or prove an exact scope exit before assembling
the separate quartic banks, running multi-index Green/Helmholtz, or classifying
the projective `kappa1` rows.
