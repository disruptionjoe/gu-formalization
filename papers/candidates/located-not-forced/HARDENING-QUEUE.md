---
title: "Located, Not Forced — v2.15 executable hardening campaign"
status: complete
doc_type: hardening-queue
scope: paper-local
created: 2026-07-07
updated: 2026-07-23
source: "v2.14 ten-lens review; two independent hardening scouts; Science Council; Joe decisions"
run_type: directed-progress
run_ref: GUD-20260723T160054Z-lnf-v215-hardening
---

# Located, Not Forced — v2.15 executable hardening campaign

This queue supersedes the July 7 H1-H10 list as the live paper-local execution
surface. The earlier work remains provenance: the 31-check harness, class-C
census, Lean finite core, boundary table, prior-art delta, reviewer packet, and
load-bearing-number manifest exist and are the v2.14 baseline. They are not
evidence that the new issues below are already closed.

## Settled policy

- Preserve the current title and “located, not forced” identity by default.
  Any central reframe returns to Joe before editing.
- The exact certified theorem domain is `C_fin`. The broader semantic
  invariant class is `C_inv`, conditional/open at computed grade. A bounded
  lifting test is authorized; an unbounded classification campaign is not.
- Zenodo is the first public stage for internally hardened work and never waits
  for external review. External review is optional later and required only for
  a claim of external establishment.
- Markdown is canonical. TeX/PDF/package regeneration is deferred.
- Program-native Krein/torsion objects and standard positive-Hilbert/integer
  objects must never be silently substituted for each other.

## Stop and return conditions

Return to Joe before continuing if:

1. a viable physical-signature reconstruction changes the central class-C or
   antilinear verdict;
2. a framing convention branch erases, rather than sign-flips, the nonzero
   `3`-primary component;
3. independent carrier routes disagree on `192`, `(96,96)`, or
   `(2,2,2,2,0)`;
4. a load-bearing statement is false; or
5. the title, central thesis, or novelty posture must materially change.

## Ranked work packages

### V15-1 — physical-signature transfer audit

**Status:** COMPLETE. **Priority:** 1. **Owner:** mathematical audit.

Rebuild or sharply delimit the transfer from the compact/complexified
`(4,0)+(5,5)` calculation to the physical Lorentzian `(3,1)+(6,4)` split.
State which of `192`, `(3,2,16)+(3,2,16bar)`, `(96,96)`,
`(2,2,2,2,0)`, and antilinear preserve/swap behavior survives.

Acceptance: a side-by-side executable or exact certificate; no Wick-rotation
or real-form claim by analogy; manuscript and status-table consequences mapped.

### V15-2 — exact `C_fin` theorem and bounded `C_inv` lifting test

**Status:** COMPLETE. **Priority:** 2. **Owner:** theorem scope.

Define `C_fin` as the encoded 15-generator finite grammar and `C_inv` as the
broader semantic invariant class. Quantify Theorem 1 exactly over `C_fin`.
Record the carrier-faithfulness and extensional-normalization obligations for
lifting to `C_inv`. Attempt one bounded noncircular lifting milestone.

Acceptance: every theorem-domain noun maps to a constructor or typed premise;
no catch-all “sector data” is silently certified; a failed or open lift is
reported without weakening the `C_fin` theorem.

### V15-3 — codomain-separated Lean theorem

**Status:** COMPLETE. **Priority:** 3. **Owner:** formal finite core.

Replace the surrogate assignment of a power-of-two modulus to every row with a
typed separation among finite torsion, integer equality/divisibility,
representation dimensions, and diagnostics. CRT projection must accept only
torsion-valued inputs.

Acceptance: Lean proves the manuscript’s actual bounded conclusion; integer
rows cannot typecheck as CRT inputs; no `sorry`, custom axiom, or artificial
integer modulus.

### V15-4 — carrier-faithfulness packet

**Status:** COMPLETE. **Priority:** 4. **Owner:** representation evidence.

Consolidate the real algebra, quaternionic module, complex realization,
gamma-trace kernel, selected triplet, chirality blocks, Krein form, and
`(2,2,2,2,0)` Hom-space packet. Distinguish direct computation, independent
symbolic support, and open physical-real-form transfer.

Acceptance: one reviewer-facing packet and clean commands reproduce the exact
carrier values; disagreements trigger the campaign stop.

### V15-5 — framed-cycle convention and sensitivity packet

**Status:** COMPLETE. **Priority:** 5. **Owner:** framing evidence.

Specify the framed `RP3` cycle, orientation, stable-framing reference,
`pi_3(SO(3))->pi_3(SO)` stabilization, `p1/2`, Adams normalization, CRT images,
both signs, and each imported convention. Test alternative sign and
factor-of-two branches.

Acceptance: the packet derives or explicitly assumes every link; both signs
and CRT projections are displayed; the full class remains order 12 and only
the projection order 3; any erasing branch triggers the stop.

### V15-6 — typed finite Krein transversality

**Status:** COMPLETE. **Priority:** 6. **Owner:** formal operator lemma.

Retype Theorem 2 over the minimal finite-dimensional complex Hermitian/Krein
structure. Use `intersectionDifference`, not “net chiral index” or “physical”
terminology. Keep intersection nullity, projection-rank balance, and Fredholm
chirality separate.

Acceptance: formal scalar/form/dimension/map types match the manuscript;
formal identifiers and comments contain no physical-handedness or Fredholm
overclaim.

### V15-7 — release claim-and-premise ledger

**Status:** COMPLETE. **Priority:** 7. **Owner:** evidence semantics.

Supersede the stale July 14 claim map without rewriting its historical record.
Give each current claim a stable ID, codomain, premise set, grade, GU
dependency, evidence role, paths, and scope exits. Make validation semantic,
not merely token/existence-based.

Acceptance: every status-table row resolves to a current claim ID; mutation
tests reject the known superseded formulations; class-C and transfer premises
are first-class evidence entries.

### V15-8 — reviewer route, canonical references, and release identity

**Status:** COMPLETE FOR CANONICAL MARKDOWN. **Priority:** 8. **Owner:** publication evidence.

Make `REVIEWER.md` a fast, copy-pasteable replication route; fix CLI and
environment claims; make canonical Markdown bibliographically self-sufficient;
align manuscript/deposit version semantics and immutable source identity while
leaving DOI/date unset until publication.

Acceptance: documented commands exist and pass; no claim relies on stale TeX
for its bibliography; no unreleased artifact claims a release date; external
review is clearly optional for Zenodo and mandatory only for external-grade
promotion.

### V15-9 — canonical manuscript integration

**Status:** COMPLETE. **Priority:** 9. **Owner:** lead.

Integrate only surviving results into the canonical Markdown. Add a compact
premise-result-interpretation-residual spine without changing the title or
turning the paper into a campaign report.

Acceptance: abstract, introduction, theorem statements, status table,
limitations, and conclusion agree; every conditional is visible at first use.

### V15-10 — hostile closeout

**Status:** COMPLETE. **Priority:** 10. **Owner:** adversarial verifier.

Run the manifest validator, full harness, targeted Lean checks, census and
framing checks, stale-language scans, claim-ledger mutations, and a final
hostile scope review. Update this queue with evidence and exact residuals.

Acceptance: all required checks pass; every defect is fixed, explicitly
deferred with rationale, or returned under a stop condition.

## Progress contract

A run selects the highest-ranked unblocked package, advances it to a checkable
disposition, records evidence here, and reranks. Negative results and bounded
failed lifts are progress. Repetition of already-green v2.14 checks without
advancing a current package is not progress.

## Closeout disposition

Closed 2026-07-23 with no title-level or central-thesis stop condition.

- **V15-1:** the compact carrier remains complex `192`, chirality `96+96`,
  Krein signature `(96,96)`, and census `(2,2,2,2,0)`. A single physical
  Lorentzian Hodge half has the same complex dimension and chirality count but
  is Krein-null with census `(2,2,0,0,0)`; its conjugation-stable closure is
  complex `384`, Krein `(192,192)`, with census `(4,4,4,4,0)`. The manuscript
  now states this fork instead of claiming transfer by analogy.
- **V15-2:** Theorem 1 is exact on the encoded finite grammar `C_fin`. The
  bounded C1--C5 lift closes on the compact carrier; C6 beyond the seven named
  rows and unrestricted `C_inv` remain open.
- **V15-3:** the Lean finite core now separates finite torsion, integer
  equality/divisibility, representation dimensions, and diagnostics. CRT
  accepts only explicitly mapped torsion inputs.
- **V15-4:** the carrier-faithfulness packet separates direct computation,
  independent symbolic support, and the open physical-real-form boundary.
- **V15-5:** both sign branches and declared factor-of-two conventions retain
  full order `12` and nonzero order-`3` projection. Erasing the projection
  requires changing the tangential/framing premise, not a viable convention
  change within the stated object.
- **V15-6:** the finite transversality lemma is typed over complex Hermitian
  sesquilinear data and uses intersection difference without physical or
  Fredholm overclaim.
- **V15-7:** the current claim-and-premise ledger assigns stable IDs to all
  sixteen status rows; semantic validation and five mutation tests pass.
- **V15-8:** the canonical Markdown is self-contained, the reviewer route and
  environment are current, and the Zenodo scaffold has no fabricated DOI,
  publication date, or immutable release identity. Final TeX/PDF/package
  rebuilding remains intentionally deferred until posting preparation.
- **V15-9:** v2.15 integrates the surviving results, qualifications, evidence
  roles, references, limitations, and conclusion without changing the title.
- **V15-10:** hostile closeout is recorded in
  `review/V15-10-hostile-closeout-2026-07-23.md`.

Final receipts: the release-evidence validator passed; all `31/31`
reproduction targets passed; the physical-signature audit passed `53/53`;
claim-ledger mutations passed `5/5`; framing and enumeration discriminators
passed; targeted Lean builds passed with no `sorry`, `admit`, or custom axiom;
JSON, YAML/CFF, Python compilation, stale-language review, and
`git diff --check` passed.

Exact residuals are now declared work, not hidden defects: unrestricted
`C_inv`; C6 beyond the named rows; the true `Y14` source/action selector;
physical realization beyond the half/closure fork; external independent
replication; and final TeX/PDF/Zenodo artifact parity. None is an ordinary
hourly continuation of this closed campaign.
