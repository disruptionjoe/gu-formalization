# Located, Not Forced v2.15 — typed transfer hardening

**Date:** 2026-07-23  
**Canonical source:** `located-not-forced-generation-count-2026-06-29.md`  
**Release artifacts:** deferred until final TeX/PDF/Zenodo reconciliation

## Disposition

The title and central “located, not forced” verdict survive. The hardening
found one mandatory scope correction: the compact/complexified carrier packet
does not transfer unchanged to one physical Lorentzian Hodge half. That
correction is now explicit at first use and in the status table.

## Changes

1. Defined exact `C_fin` and semantic `C_inv`; retained completeness only for
   the fifteen-constructor finite type and recorded the bounded C1--C5 lift.
2. Retyped the finite Lean theorem so only genuine finite-torsion rows have
   moduli; integer equality/divisibility, representation dimensions, and
   diagnostics remain separate. CRT requires explicit mapped torsion input.
3. Replaced the realified theorem skeleton with a finite complex Hermitian
   transversality theorem using `intersectionDifference`.
4. Added the physical-signature audit:
   compact `(192,(96,96),(2,2,2,2,0))`; Lorentzian half
   `(192,K-null,(2,2,0,0,0))`; stable closure
   `(384,(192,192),(4,4,4,4,0))`.
5. Added the sign-complete framing packet: full classes `+/-2` have order 12;
   their projected 3-primary representatives are `8` and `16`, each order 3.
6. Added a current machine-readable claim-and-premise ledger with stable IDs,
   semantic validation, and mutation tests. The July 14 map remains historical.
7. Made the canonical Markdown bibliographically self-contained and added
   data/code availability plus a fast reviewer route.
8. Recorded the Zenodo-first policy: external review is optional for public
   deposit and required only before an external-validation claim.

## Evidence receipts

- release manifest: PASS;
- claim-ledger mutations: 5/5 PASS;
- numerical release harness: 31/31 PASS;
- physical-signature audit: 53/53 PASS;
- framing sensitivity: PASS with the declared GU tangential-identification
  premise;
- targeted Lean modules and certificates: PASS.

The exact command/output inventory is maintained in `REVIEWER.md`, the v2.15
review packets, and the campaign run record.
