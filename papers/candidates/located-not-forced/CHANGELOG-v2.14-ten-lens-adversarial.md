# v2.14 ten-lens adversarial hardening

**Date:** 2026-07-23  
**Canonical source:** `located-not-forced-generation-count-2026-06-29.md`  
**Review ledger:** `review/H11-ten-lens-adversarial-adjudication-2026-07-23.md`

This Markdown-only revision:

- corrects the `Cl(9,5)=M(64,H)` carrier from “128-dimensional real” to a
  128-dimensional complex realization of the 256-dimensional real module `H^64`;
- narrows the false arbitrary-direct-sum spinor lemma to irreducible complex
  spinor dimensions;
- exposes the compact/complexified `Spin(4)` calculation and leaves Lorentzian
  bundle transfer reconstruction-grade;
- displays the `(3,2,16)` and `(3,2,16bar)` carrier blocks;
- adds the exact-script versus Lean versus unrestricted completeness boundary;
- types the Krein form as Hermitian and replaces antilinear eigenspace language
  with image-subspace language;
- removes uniqueness wording for the framed carrier and names external
  framing-normalization replication as a residual;
- reclassifies `16 // 16 = 1` as a family-unit normalization rather than a
  generation-count integer;
- limits anomaly claims to the coefficients and form degrees actually tested;
- removes the non-invariant numerical “frame charge” proxy;
- distinguishes a source-action gate audit from construction of the true
  `Y14` operator;
- scopes the title to an audit and removes a non-load-bearing exploration-grade
  global-anomaly aside.

No TeX, PDF, or generated release-package payload was rebuilt. Scaffold Markdown
and `CITATION.cff` metadata were aligned to prevent stale copy-paste, but the adjacent
TeX and package remain non-release artifacts. They must not be submitted until final
reconciliation, compilation, render inspection, manifest regeneration, and metadata
review.
