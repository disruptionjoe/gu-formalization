---
title: "What the outside system would add: an antilinear boundary SELECTOR times an order-3 framing CARRIER, glued by anomaly inflow (NOT temporal)"
status: active
doc_type: result
created: 2026-06-28
grade: "the import SHAPE is forced and its carrier leg is verifiable; the unification into one operator and the delivery of the integer 3 are NOT established. Temporal repos contribute zero load-bearing content. Not a rescue of GU."
method: "firewall-import persona sprint: 11 lenses (6 canonical groups + MMO/world-model + temporal-issuance native + TaF native + anomaly-inflow + CPT-arrow + Krein-selection + strategy) graded against a precise 4-leg import spec, clustered, adversarially critiqued, synthesized. 14 agents, ~1.51M subagent tokens."
depends_on:
  - canon/firewall-boundary-hypothesis.md
  - canon/source-action-seiberg-witten-RESULTS.md
  - canon/two-primary-lemma.md
  - canon/swing-ghost-parity-no-chiral-selection.md
  - canon/boundary-einvariant-and-the-tangential-fork.md
  - canon/no-go-quaternionic-parity-generation-sector.md
---

# What the outside system would add (firewall-import sprint)

We asked the question the source-action build forced: if a firewall boundary separates GU's provably-balanced
interior from the chiral count, **what would an outside system add to make it work?** Every persona answered
against a precise 4-leg spec: the import must (i) be J_quat-ANTILINEAR (break quaternionic closure), (ii)
supply the Z/2 ghost-parity that keeps the positive-norm half of each generation-mirror Krein pair, (iii) act
at the boundary (the RP^3 spine), (iv) carry odd-torsion 3-primary data.

## The answer (structural-plausible)

The missing object is a **GU / stable-homotopy-native FUSED pair, NOT a temporal import**: an antilinear
boundary SELECTOR times an order-3 framing CARRIER, bound by anomaly inflow. Stated as one object (the
leading candidate, C3):

> A single **antilinear (J_quat-antilinear / antiunitary) APS / Dai-Freed boundary spectral section** at the
> RP^3 spine -- a particle-hole-symmetry-breaking term (Altland-Zirnbauer class CII) whose spectral
> projector `P_{>=0}` IS the Z/2 ghost-parity keeping the positive-norm half of each generation-mirror pair
> (SELECTOR, legs i+ii), and whose reduced spectral asymmetry `eta-bar` IS the framed-bordism Adams
> e-invariant `e_R = 1/12` (class 2 in `pi_3^s = Z/24 = Z/8 (+) Z/3`, 3-primary part order 3) carried through
> the gravitational `-p_1/24` tangential framing channel (CARRIER, legs iii+iv), glued by anomaly inflow
> (Callan-Harvey / Dai-Freed).

It is the **only** object that threads all four legs, and it is load-bearing on each leg **decomposed**. But
its **unification** into one operator on GU's actual matter sector (the Bismut-Cheeger fibered-boundary step:
that ONE antilinear operator both breaks `C = J_quat.G` and has reduced `eta` with nonzero order-3 part) is
unbuilt, and the **order-3-class -> integer-3** bridge is open. Both are gated on GU's single unbuilt object,
the stabilized RS/IG matter Dirac/source action. **It is NOT load-bearing as "forces exactly 3 generations."**

## The decomposition: SELECTOR times CARRIER (the real structural content)

The sprint's durable finding is that the import is **two jobs that no single GU-native object does**, and
the literature names both:

- **SELECTOR (C2) -- must be ANTILINEAR.** Machine-proven UNIQUE escape from the even-index wall: every
  *linear* Krein-isometric ghost parity conserves the chiral index (net `= -2.4e-15` across signatures
  (9,5), (7,7), (14,0)); the only net-chiral positive sector (+96) is reached by an **antilinear re-grading**,
  not a Krein unitary (`canon/swing-ghost-parity-no-chiral-selection.md`). This SHARPENS the source-action
  build's Krein-isometry no-go into a precise positive statement: **the chiralizer must be antilinear** (a
  CPT / particle-hole / Tomita-conjugation / Krein re-grading object; the measured antilinearity defect ~85).
  Meets (i)+(ii). Order-2 on its own -- carries no count (fails iv).
- **CARRIER (C1) -- the order-3 framing.** The framed-bordism Adams e-invariant `e_R = 1/12`, order 3, via
  the gravitational `-p_1/24` channel. The **unique** order-3 object on the board and the **unique** place an
  odd count can live (the 2-primary lemma, theorem-grade, GU-independent). Meets (iii)+(iv) at
  load-bearing-verifiable grade. But it is a passive invariant (a meter): it breaks no symmetry (fails i),
  selects no half (fails ii). The carrier, not the chiralizer.
- **Negative control (C6) confirms the split:** RP^3's own deck Z/2 is free from boundary topology, but RP^3
  is entirely 2-primary (charge-q Dirac `eta = (2q^2 - 4q + 1)/8`, 2-primary for every integer charge). So
  the **order-3 MUST come from the FRAMING channel, not from any selector or from the manifold.** This
  sharply confirms the C1/C2 (carrier vs selector) split.

The frame (C4) under which they operate is **anomaly inflow** (Callan-Harvey / Dai-Freed / Witten): a
globally non-chiral bulk consistent with chiral boundary modes -- the exact structural mirror of the firewall.

## The temporal verdict: NO (the lead pointed at the shape, not the content)

Temporal-issuance / time-as-finality do **not** supply the import. Honest, fixture-checked grading
(seductive-unverified):

- The "computer-checked loop product -1" parity cocycle is **NOT derived** -- `E015-holonomy-fixture.md`
  records `bare_ext_s_derives_nontrivial_holonomy = FALSE`; the -1 is a **stipulated** transport functor.
- **Decisive on leg (iv): there is NO 3-primary object anywhere in temporal-issuance** -- every "3" is a BFT
  quorum threshold (`f < n/3`), intrinsically 2-primary, coprime to the required Z/3. The TI native lens
  concedes it.
- On leg (i): TI has **no quaternionic/Krein structure to be antilinear against**, so "J_quat-antilinear" is
  replaced by irreversibility-flavor -- a category error (irreversibility is a process property; antilinearity
  is conjugate-linearity on a vector space).
- "Phantom incomparability" is a **rhyme, not GU's structure**: TaF's own `T36-spin-index-audit.md` is
  self-honest that "PO1 is index-shaped, not an index theorem" (no Dirac operator, symbol, kernel count, eta,
  or cobordism datum), and lists "finite gluing obstruction = Cech = Dirac index" as an overclaim to avoid.
  So "T27 witness count 0 -> 2" is a Boolean obstruction polarity, not a Callan-Harvey chirality count.
- The CPT hook (CP <=> T) is standard QFT available to GU **directly**, and even granted yields a 2-primary
  T-odd Z/2, not the order-3 carrier.
- The repos' own cross-repo governance forbids exactly this trade ("no escape-hatch trades; TaF finality must
  never be used to discharge a GU proof obligation").

**Net:** the temporal lead correctly supplied the selection SHAPE (a Z/2 selector at a boundary) and the
T-breaking MOTIVATION (CPT). It supplies none of the load-bearing computation; the order-3 lives in the
gravitational framing channel, all of it GU / stable-homotopy-native.

## The MMO / world-model answer (you emphasized it)

The MMO architect lens **independently reproduced the same SELECTOR x CARRIER two-object decomposition** the
physics lenses reached -- a genuine architecture-level convergence on the *shape* (two objects, not one). Its
reading: (A) the SELECTOR = a server-authoritative **instantiation commit** -- when the observer's frustum
(the metric/signature choice) reaches the RP^3 spine, the engine writes the visible positive-norm half to the
non-rollbackable past and culls the occluded negative-norm ghost half (a non-invertible projection that cannot
commute with J_quat, since J_quat pairs +norm with -norm); (B) the CARRIER = the tangential streaming-framing.
**Honest grade:** the convergence on shape is real and useful, but the only non-metaphorical residue is
exactly C2/C3 (selector) + C1 (carrier). The instantiation-commit is C2/C3 re-dressed in game-engine
vocabulary (adds no computation, runs on the existing `ghost_parity_krein.py`), and the "fourth vote for the
tangential fork" rests on a pun on "frame" (a GPU frame bundle is not GU's Spin(4) tangent-frame split). The
MMO model IS the selector-times-carrier decomposition relabeled -- a mnemonic and a shape-confirmation, not an
independent source of content.

## Honest caveat: the firewall premise is weakening, not strengthening

This bounds every claim above (`firewall-boundary-hypothesis.md` Rounds 2-3):
- The **STRONG form** (boundary proven NECESSARY via the quaternionic-parity wall) is **confirmed dead** --
  the parity wall is a (9,5) representation artifact (`J^2 = -1`), refuted by the (7,7) re-run where
  `J^2 = +1` and no wall exists.
- Only the **WEAK form** survives (GU under-determines the count regardless of signature) -- the conditional
  Multiplicity Theorem.
- The **POSITIVE form** (a GU-forced external index supplies 3) is open / partially refuted: GU pins neither
  the base nor the value (K3 gives `ch2 = -5376`, not the 24 the story needs; the 24 was a disguised
  chi-import, rejected).
- GU's own cleanest verified result (the Pati-Salam Spin(7,7) chain derives exactly **one** generation) and
  its "2+1 effective" narrative tilt against the strong reading.
- The carrier's order-3 is conditional on the TANGENTIAL fork (reconstruction-grade via three first-principles
  derivations; two of three index-theory referees called it "undetermined-needs-specialist"; the
  gauge-coefficient reading gives `e = 3/8`, 3-part zero, kill complete).
- The 2-primary lemma's blindness corollary is itself contingent on the torsion-count reading (a literal
  integer-index reading of `net = 0` would FORBID 3).

## The decisive next computation

Relocate the interior antilinear re-grading (the operator that yields the net-chiral +96 by breaking
`C = J_quat.G`) to the RP^3 boundary spine and compute its **reduced `eta-bar`** under APS / Dai-Freed
boundary conditions via the Bismut-Cheeger fibered-boundary reduction. Test: does the **same** antilinear
operator that performs the selection (the +96) land reduced `eta` ODD `= e_R = 1/12` (order-3 nonzero), rather
than the flat-prior `eta = 0`? If yes, selector and carrier **unify** into one operator and C3 is built; if
the boundary reduction kills the +96 or sends `eta` even, they are distinct objects bound only by anomaly
inflow. **Crucially this is computable on a MODEL Clifford-RS boundary operator without first building GU's
full source action** -- it is the fibered-boundary reduction of an already-machine-checked interior form, the
one concrete step that converts the decomposed-leg-by-leg result into (or out of) a single spec-complete
object.

## Bottom line

The outside system, if there is one, adds a precise object: an **antilinear boundary selector fused with an
order-3 framing carrier under anomaly inflow** -- and its shape is forced, its carrier leg verifiable. But its
unification and its delivery of the integer 3 are not established, and **temporal issuance contributes none of
the load-bearing content** (it named the shape and the motivation only). This is not a rescue of GU; it is a
sharp, honest map of exactly what an external chiralizer would have to be, and exactly which one computation
would decide whether it can be a single object.
