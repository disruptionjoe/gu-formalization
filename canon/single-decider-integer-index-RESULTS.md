---
title: "The single decider: GATED, not FORCED. The literal generation integer is gated on the unbuilt RS source action; the only honest computable integer is 1; the count-operator couples GAUGE. The strong reading dies twice over. No fabricated 3."
status: active
doc_type: result
created: 2026-06-29
grade: "computed-but-gated. 26/26 checks across 4 scripts + an independent from-scratch re-verification, exit 0, all controls reproduced exactly. The bulk-topology integers and the fork are computed-confirmed; the literal net-chiral integer is honestly gated on the unbuilt twisted Rarita-Schwinger source action. No 3 was computed or fabricated (the deciders assert integer_is_3 = False)."
method: "single-decider build workflow: 3 construct angles (bulk inflow characteristic class; fibered-boundary reduction; tangential-vs-gauge fork) -> compute (ran python on the actual 4+10 / Spin(10)-16 geometry) -> adversarial verify (independent from-scratch re-run, explicit fabrication checks) -> synthesize per the decision rule. 6 agents."
depends_on:
  - canon/three-generations-locate-not-force-CRT-RESULTS.md
  - canon/boundary-eta-of-mu-RESULTS.md
  - canon/boundary-einvariant-and-the-tangential-fork.md
  - canon/two-primary-lemma.md
  - papers/drafts/publication-plan-2026-06-28.md
---

# The single decider

The one move left that was not re-describing the empty slot: compute the net chiral generation index as an
integer on GU's *actual* 14-manifold. We built it and ran it. **The verdict is GATED, not FORCED -- and the
strong "GU forces three" reading dies twice over, with no fabricated 3 anywhere.**

## Verdict

- **The literal net-chiral generation integer is GATED** on the unbuilt stabilized twisted Rarita-Schwinger /
  IG source action -- the same `+8` leg that has blocked the count at every locus. Every analytic route to
  it failed (ten Atiyah-Singer routes gave `{960, -288, -384, -192, -336, -128, 128, -8, -480, 60}`, none
  `= 16`); the build **explicitly refused it rather than fabricating it.**
- **The only honest computable generation integer is 1** -- GU's own verified Pati-Salam `Spin(7,7) ->
  Spin(6) x Spin(4)` chain gives 16 chiral states = exactly **one** anomaly-free generation (`Tr Y = Tr Q =
  0`, `16 // 16 = 1`). "2 + 1 effective." The evidence does not merely fail to reach three; it **tilts toward
  one.**
- **The net-count-producing operator couples GAUGE** (`e = 3/8`, denom `8 = 2^3`, class 9 in `Z/24`, 3-part
  **zero**) -- a genuine, operator-dependent, convention-independent decouple, not a flat choice.
- So **FORCE is dead twice over**: the integer is not 3 (it is gated, and the only honest one is 1), AND the
  fork lands gauge (3-part zero). Under either branch of the fork, three is not earned.
- **The order-3 carrier (`Lambda^2_+`, `e_R = 1/12`, 3-primary) is real but VECTORLIKE and HOMOTOPY-FIXED**
  -- it LOCATES the order-3 slot; it does not fill it with a count.

## What is computed (26/26 checks, exit 0, independently re-verified)

- **All five controls reproduced exactly:** `ch2(S_X)[K3] = -5376 = -2^8 . 3 . 7` (live so(14) Chern-Weil
  spin multiplier `dim_S/8 = 16` across 5 seeds and so(4,6,8,10); the disguised-`chi` 24 via `2chi+3sigma=0`
  REJECTED -- a `chi`-route gives `ch2 = 0`, `chi` never used); `A-hat(K3) = -p_1/24 = 2` exact,
  cross-validated by `int_{(K3)^4} [A-hat]_16 = 2^4 = 16`; tangential `e_R = p_1/48 = 1/12` from `p_1 = 4`;
  charge-`q` Dirac `eta = (2q^2-4q+1)/8` (denom `8` for every `q`, 2-primary); Pati-Salam `Spin(7,7)` ->
  exactly **1** anomaly-free generation.
- **Bulk-topology-forced integers:** LINEAR net-chiral index `= 0` (the `Lambda^2_+` triplet is vectorlike,
  `+96/-96`); Pati-Salam `= 1`; spin-1/2 leg `= 8 . A-hat(K3) = 16 = 2^4` (the `A-hat(K3) = 2` is literally
  the "2" of "2+1"); the 16-twisted bulk characteristic number `ind16 = -544 = -2^5 . 17`. **None equals 3.**
- **The fork, by live frame charge on the verified `Cl(9,5) = M(64,H)` substrate:** the carrier `Lambda^2_+`
  has net self-dual frame charge `33.94` (TANGENTIAL, `p_1 = 4`) but is vectorlike (net chiral 0); the only
  operator that produces a nonzero net count (the `+96` antilinear chiralizer `C = J_quat . G`) has frame
  charge **exactly 0.00e+00** (GAUGE) -- structural and convention-independent, since any `id_14 (x) U` is
  traceless on the frame factor (proven with a random `U`). So the generation index couples GAUGE.
- **The fibered-boundary even-fiber transparency:** `A-hat[S^6] = 0`, `eta(S^6) = 0` (round-Dirac symmetric
  spectrum), `chi(S^6) = 2` in the wrong (non-chiral Euler) channel -- so the `S^6`-bundle reduction lands the
  2-primary lens `eta` on the `RP^3` spine. The Bismut-Cheeger/Dai adiabatic-limit machinery is a published
  theorem and the even-fiber index-zero fact is computed.

## What is gated (honestly marked, NOT fabricated)

1. **The `+8` RS leg / the literal net-chiral integer** -- gated on the unbuilt stabilized twisted
   Rarita-Schwinger / IG source action. Every analytic route failed; `16` is absent from the ten route
   values; so the **1-vs-3 question is genuinely gated**, not resolved by fitting.
2. **The full analytic Bismut-Cheeger fibered-boundary reduction theorem** for the non-product 9-dim
   `S^6`-bundle over `RP^3` -- the machinery is a theorem and the even-fiber transparency is computed, but its
   application to GU's *actual* twisted-RS boundary operator (the Dai `tau` / spectral-flow term on the real
   operator) is gated. "The spine is `RP^3`" is not that theorem.
3. **The families pushforward** `pi_! : ch(S)/Y14 -> ch(S_X)/X4` is NOT DEFINED (the fiber `GL(4,R)/O(3,1)`
   is non-convex), so `ch2(S_X)[K3] = -5376` is a bulk characteristic number, not yet THE families index.
4. **`order-3-class -> integer-3`** is a theorem of nothing (APS / Dai-Freed / Bismut-Cheeger relate boundary
   `eta` to indices, determinant lines, anomaly phases, cobordism classes -- never an integer family count);
   `e_R = 1/12` is HOMOTOPY-FIXED, identical for 1 or 5 generations -- a possible category error.

## Integrity note

The build was instructed to be ruthless about any "3" and to check it against the four fabrication patterns
the program has already caught (the `24 = chi` import, the reverse-engineered `+8`, the circular rank-4, the
fitted holonomy). It came back with **no 3** -- the deciders assert `integer_is_3 = False`, and the `+8` leg
was **explicitly refused rather than fabricated.** An independent from-scratch script reproduced all 26
checks. This is the discipline at its strongest: the hardest object in the program, built and run, returned a
gated/located verdict with zero fabrication.

## What it does to the paper

It sharpens Paper 1 into its strongest *honest* form and does **not** earn the "GU forces exactly three"
headline. Referee-proof and unchanged-but-strengthened: the **2-primary blindness lemma** (GU-independent),
the **CRT two-arena structure** `pi_3^s = Z/8 (+) Z/3`, and the genuine **order-3 carrier** (`Lambda^2_+`,
`e_R = 1/12`) sitting in the unique CRT-disjoint sector every obstruction is blind to -- now shown by running
code to be **tangential but vectorlike and homotopy-fixed** (it locates, it does not fill). The paper states
plainly: **located, not forced**; the single named open conjecture is `order-3-class -> integer-3` (a
candidate category error); the one remaining decider -- an integer index on GU's actual 14-manifold -- stays
**gated** on the unbuilt source action and a proven Bismut-Cheeger reduction for the non-product `S^6`-bundle;
and the only honest computable generation integer is **1**, which tilts the evidence against three.

This is the campaign's endpoint on the generation count: the slot is found and genuinely carries an order-3
object; the occupant is not computable without building the source action; and every honest integer on the
real geometry is 1, not 3.
