*.pdf
.lake/
__pycache__/

# Scratch / working state only (temp, caches, intermediate renders).
# Durable research artifacts belong in this repository; secrets never do.
# Keep generated scratch and caches below `_local/`.
_local/

# Legacy local-only working records (never published)
steward/runs/
